id: mupibox
version: "615"
title: MuPiBox — Wissen (Pi/DietPi Kinder-Musikbox)
description: >
  Projekt-Wissen für die MuPiBox-Installation und -Optimierung auf Raspberry Pi /
  DietPi: die hart erarbeiteten Workarounds (rpi-eeprom/nodesource auf Trixie, der
  hängende Bluetooth-Schritt), die Pi-5-KMS-/Display-Lektionen, MuPiHAT-Hardware
  (MAX98357A-Audio, BQ25792-Charger), die Perf-/Cache-Optimierungen — plus ein
  „achimsumbau"-Block zu Achims KONKRETEM Gerät (Pi 5, DietPi Trixie, MuPiHAT PCB
  3.4.1, Waveshare 5" DSI). Web-recherchierte Einträge tragen ein `source:`-Feld
  zum Verifizieren; achim-Einträge nennen die Session/das Fork-Repo als Quelle.
  Wird vom remote-step-installer als llmwiki geladen (`wiki fetch <repo>`, dann
  `--wiki mupibox`) und dient Mensch UND LLM als Kontext + Diagnose-/Config-/Perf-Regeln.

entries:
  - kind: signature
    id: mupi-rpi-eeprom-trixie
    title: "rpi-eeprom fehlt (Trixie)"
    sev: med
    match: "Unable to locate package rpi-eeprom"
    cause: "rpi-eeprom ist unter DietPi/Debian 13 Trixie nicht im aktiven Repo — für die MuPiBox NICHT nötig"
    fix: "diesen Schritt ÜBERSPRINGEN (EEPROM-Update ist nicht Teil der App-Installation)"
    tags: [trixie, apt, workaround]
    related: [mupi-nodesource-trixie]
    body: |
      Beim frischen Setup brach das `autosetup` hier ab, weil `rpi-eeprom` auf Trixie
      nicht auflösbar ist. Für die MuPiBox irrelevant → im Recipe `optional` / skip.

  - kind: signature
    id: mupi-nodesource-trixie
    title: "nodesource/Node-Install zickt auf Trixie"
    sev: med
    match: "nodesource|E: Unable to locate package nodejs|node_2[0-9]\\.x"
    cause: "das nodesource-Repo hatte auf Trixie Lücken → Node liess sich nicht ziehen"
    fix: "nodesource auf die bookworm-Suite stellen ('node_22.x bookworm main') ODER nvm nutzen"
    tags: [node, trixie, apt, workaround]
    related: [mupi-rpi-eeprom-trixie]
    body: |
      Zwei Auswege in dieser Reihenfolge: (1) nodesource-Repo-Zeile auf `bookworm`
      statt `trixie` (ABI-kompatibel genug), dann `apt-get update && apt-get install
      -y nodejs`. (2) sonst `nvm` + Node 22 (kein Root-apt). Recipe hat beides als Workaround.

  # ══ Config-Regeln (modell-gegatet) — Pi-5-KMS/Display-Lektionen ════════════
  - kind: config
    id: wlan-freq-list-fesselt-auf-toten-kanal
    title: "freq_list in wpa_supplicant.conf = Box verschwindet, sobald der Router den Kanal wechselt"
    sev: high
    file: /etc/wpa_supplicant/wpa_supplicant.conf
    present: "freq_list"
    why: >
      `freq_list=<MHz>` beschraenkt wpa_supplicant auf GENAU diese Frequenzen — es scannt
      keine anderen mehr. Gedacht ist die Zeile als Verbindungs-Beschleuniger (kein
      Vollscan). Sie ist aber eine ZEITBOMBE: FRITZ!Box & Co. waehlen ihren 2,4-GHz-Kanal
      selbsttaetig. Wechselt der Router den Kanal, sucht die Box weiter auf dem alten —
      und ist ab sofort spurlos weg. Kein Fehler im Log, kein Assoziationsversuch, nichts:
      die Box haelt das Netz schlicht fuer nicht vorhanden.
      AM GERAET ERLEBT (2026-07-30, Pi 4): Box war unter 192.168.178.99 online, danach
      tagelang unerreichbar (kein ping, kein mDNS, kein ARP-Eintrag im ganzen /24).
      wpa_supplicant.conf trug `freq_list=2472` (Kanal 13), alle drei Zugangspunkte des
      Netzes funkten inzwischen auf 2457 MHz (Kanal 10) — auf 2472 sendete NICHTS mehr.
      Nach dem Entfernen der Zeile verbindet sie sich wieder normal.
      Gegenprobe von aussen (ohne die Box anzufassen):
      `nmcli -t -f SSID,FREQ,CHAN,SIGNAL dev wifi list | grep <SSID>` zeigt die Kanaele,
      auf denen das Netz WIRKLICH funkt — mit der freq_list-Zahl vergleichen.
      Die Zeile stammt weder aus dem MuPiBox-Repo noch aus dem remote-step-installer
      (greps leer) — sie wird von Hand gesetzt. Wer sie setzt, muss sie bei jedem
      Router-Kanalwechsel nachziehen; das ist es fast nie wert.
    fix: "cp -a {f} {f}.vor-freqlist; sed -i '/^[[:space:]]*freq_list=/d' {f}"
    source: "am Geraet diagnostiziert (2026-07-30): SD-Karte am Laptop gelesen, freq_list=2472 vs. reale Kanal-10-APs; nach Entfernen wieder erreichbar"
    tags: [wlan, netzwerk, box-unerreichbar, wpa_supplicant]

  - kind: config
    id: mupi-pi5-fkms-blackscreen
    title: "Legacy fkms auf Pi 5 = Schwarzbild"
    sev: high
    file: config.txt
    present: "vc4-fkms-v3d"
    models: [pi5]
    why: "Der Pi 5 hat KEINEN fkms/Legacy-Grafiktreiber → schwarzer Schirm. Nur full KMS (vc4-kms-v3d)."
    fix: "sed -i '/vc4-fkms-v3d/d' {f}; grep -q '^dtoverlay=vc4-kms-v3d' {f} || echo dtoverlay=vc4-kms-v3d >> {f}"
    source: "https://www.raspberrypi.com/documentation/computers/config_txt.html"
    tags: [pi5, display, kms]

  - kind: config
    id: mupi-pi5-opengl-disable-blackscreen
    title: "rpi-opengl disable macht Pi 5 schwarz"
    sev: high
    file: config.txt
    present: "dtoverlay=vc4-fkms"
    models: [pi5]
    why: "Das autosetup ruft `dietpi-set_hardware rpi-opengl disable` MODELL-UNABHÄNGIG — auf dem Pi 5 (KMS-only) bleibt danach GAR KEIN GPU-Treiber → schwarzes DSI-Bild ('schon wieder kaputt')."
    fix: "In config.txt vc4-fkms-v3d + framebuffer_width/height entfernen, dtoverlay=vc4-kms-v3d setzen (autosetup überspringt den disable auf Pi 5 seit Fork-Commit 804d7b29)"
    source: "Achims Fork, Commit 804d7b29 + documentation/pi5-fresh-setup.md (verifiziert am Gerät)"
    tags: [pi5, display, autosetup, blackscreen]

  - kind: config
    id: mupi-dsi-overlay-conflict
    title: "zwei DSI-Panel-Overlays gleichzeitig"
    sev: high
    file: config.txt
    present_all: ["vc4-kms-dsi-7inch", "waveshare-panel"]
    why: "offizielles 7\"-Panel UND Waveshare-Panel-Overlay zusammen → Konflikt, Display bleibt dunkel/flackert"
    fix: "nur EIN Panel-Overlay behalten (bei Achim: NUR vc4-kms-dsi-7inch — siehe achim-display, am Gerät bewiesen)"
    tags: [display, dsi]

  - kind: config
    id: mupi-framebuffer-ignored
    title: "framebuffer_* unter KMS wirkungslos"
    sev: med
    file: config.txt
    present: "framebuffer_width"
    requires: "vc4-kms-v3d"
    why: "framebuffer_width/height werden unter KMS ignoriert (Legacy-Rest, verwirrt nur)"
    fix: "sed -i '/^framebuffer_width=/d; /^framebuffer_height=/d' {f}"
    tags: [display, kms, cleanup]

  - kind: config
    id: mupi-gpu-mem-pi5
    title: "gpu_mem auf Pi 5 sinnlos"
    sev: info
    file: config.txt
    present: "gpu_mem"
    models: [pi5]
    why: "der Pi 5 hat unified memory → gpu_mem wird ignoriert (harmlose Altlast)"
    fix: "sed -i '/^gpu_mem/d' {f}"
    tags: [pi5, cleanup]

  - kind: config
    id: mupi-chromium-sd-cache
    title: "Chromium-Cache auf SD"
    sev: perf
    file: chromium-autostart.sh
    absent: "--disk-cache-dir"
    why: "der Chromium-Kiosk schreibt seinen Disk-Cache auf die SD (Wear + langsam)"
    fix: "in der Chromium-Startzeile ({f}) '--disk-cache-dir=/tmp' ergänzen (tmpfs = RAM)"
    tags: [chromium, perf, sd-wear]

  # ══ Perf: Tool-Konsolidierung / Austausch (MuPiBox-spezifisch) ══════════════
  - kind: perf
    id: mupi-two-node-processes
    title: "zwei Node-Prozesse zusammenlegen"
    unit: "node"
    action: consolidate
    why: "MuPiBox fährt backend-api UND backend-player als getrennte Node-Prozesse (+ pm2) plus PHP-Admin — viel für eine kleine Box"
    action_text: "die zwei Node-Dienste app-seitig zu EINEM Prozess zusammenlegen (größere Änderung); PHP-Admin nur on-demand"
    tags: [node, ram, boot]

  - kind: perf
    id: mupi-chromium-ramcache
    title: "Chromium: RAM-Cache + KMS-Flags"
    unit: "chromium"
    action: swap
    why: "schnellerer Kiosk-Start + weniger SD-Writes"
    action_text: "--disk-cache-dir=/tmp (tmpfs) + GPU/KMS-Flags (wie im Perf-Pass erarbeitet)"
    tags: [chromium, perf]

  # ══ MuPiHAT-Hardware (web-recherchiert, mit Quellen) ═══════════════════════
  - kind: note
    id: mupi-mupihat-hardware
    title: "MuPiHAT — was die Platine ist"
    source:
      - "https://github.com/stopfkuchen/MuPiHAT"
      - "https://manuals.plus/mupihat/audio-amp-usv-power-battery-fast-charger-manual"
    tags: [mupihat, hardware, audio, power]
    body: |
      MuPiHAT = HAT für den mobilen, akkubetriebenen MuPiBox-Player (Audio-Amp +
      Ladeteil + On/Off-Shim auf EINER Platine):
      - Audio: 2×3 W Class-D **MAX98357A** (Stereo/Mono umschaltbar), I2S.
      - Laden: TI **BQ25792**, 2S Li-Ion (7,4 V), USB-C-PD, ≤ **4 A** für den Pi, UPS/Akku-Fallback.
      - On/Off-Shim mit Taster (sauberes Herunterfahren + Hard-Off), Anschlüsse für
        Buttons/LEDs, optional RFID.
      - Web-Dashboard (BQ25792-Register live, 5-s-Refresh) + JSON-API.

  - kind: howto
    id: mupi-mupihat-pi5-power
    title: "MuPiHAT Rev 3.x & Pi 5 — der 5V-Ramp"
    source: "https://github.com/stopfkuchen/MuPiHAT"
    tags: [mupihat, pi5, power]
    body: |
      Laut MuPiHAT-README ist **Rev 3.x nicht voll Pi-5-kompatibel**: der Pi-5-PMIC
      will einen schnellen sauberen 5V-Anstieg, der HAT rampt in ~20 ms → zu langsam,
      der Pi 5 erkennt das Power-On evtl. nicht sauber. Eine neue, voll-Pi5-taugliche
      Version ist im README angekündigt. Teil-Workaround: Pi-5-Bootloader-EEPROM
      `POWER_ON=1` (`rpi-eeprom-config --edit`) für Auto-Boot bei anliegender GPIO-
      Spannung — der Ramp bleibt aber das Risiko. Fallback: Pi 5 aus einem echten
      5V/5A-USB-C-Netzteil speisen (HAT nur Audio/Buttons/I²C). Achims PCB 3.4.1 ist
      NEUER als die im README dokumentierte 3.2 → siehe achim-mupihat-341.

  # ══ Display / GPIO (web-recherchiert, mit Quellen) ═════════════════════════
  - kind: howto
    id: mupi-waveshare-dsi-config
    title: "Waveshare 5\" DSI LCD am Pi 5 konfigurieren"
    source: "https://www.waveshare.com/wiki/5inch_DSI_LCD"
    tags: [display, dsi, pi5, waveshare]
    body: |
      Der Pi 5 hat ZWEI MIPI-DSI-Ports; das Waveshare-Wiki empfiehlt DSI1 (Default).
      In `/boot/firmware/config.txt` ans Ende:
        dtoverlay=vc4-kms-v3d
        dtoverlay=vc4-kms-dsi-waveshare-panel,5_0_inch
      Für DSI0 stattdessen `…,5_0_inch,dsi0`. **Rev-Gotcha:** nur DSI-LCD **Rev2.2**
      läuft direkt am Pi 5; bei **Rev2.1 (oder älter)** kann die Pi-5-Power-Detection
      die Kondensatoren des Panels als Kurzschluss fehldeuten → der Pi bootet nicht.

  - kind: config
    id: mupi-waveshare-rev21-noboot
    title: "Waveshare-DSI Rev2.1 → Pi 5 bootet nicht"
    sev: high
    file: config.txt
    present: "waveshare-panel"
    models: [pi5]
    why: "Bei Waveshare-DSI-LCD Rev2.1/älter kann die Pi-5-Power-Detection die Panel-Kondensatoren als Kurzschluss fehldeuten → kein Boot (schwarz, kein grünes Blinken)."
    fix: "Panel-Revision prüfen; Rev2.2 nutzen (läuft direkt). Sonst am Pi 4 betreiben."
    source: "https://www.waveshare.com/wiki/5inch_DSI_LCD"
    tags: [display, dsi, pi5, waveshare]

  - kind: howto
    id: mupi-rpi-lgpio
    title: "RPi.GPIO läuft nicht auf Pi 5 → rpi-lgpio"
    source:
      - "https://raspberry.tips/en/raspberrypi-tutorials/raspberry-pi-5-gpio-rpigpio-not-working-alternatives"
      - "https://rpi-lgpio.readthedocs.io/"
    tags: [gpio, pi5, migration]
    body: |
      Der Pi 5 hat den neuen I/O-Chip **RP1**; klassisches `RPi.GPIO` greift per
      /dev/mem direkt auf CPU-Register → auf dem Pi 5 tot (die GPIO-Register sitzen
      im RP1). **rpi-lgpio** ist ein DROP-IN-Ersatz (gleiche API, nutzt lgpio darunter),
      der Python-Code bleibt unverändert. Kann NICHT neben rpi-gpio im selben Env leben:
        apt remove python3-rpi.gpio  &&  apt install python3-rpi-lgpio
      Betrifft in der MuPiBox `scripts/fan/fan_control.py` + `scripts/mupibox/led_control.py`.

  - kind: note
    id: mupi-dietpi-trixie
    title: "DietPi-Standard ist jetzt Debian 13 Trixie"
    source:
      - "https://dietpi.com/blog/?p=4014"
      - "https://www.debian.org/News/2025/20250809"
    tags: [dietpi, trixie, debian]
    body: |
      DietPi liefert Images per Default in **Debian 13 „Trixie"** (stable, seit
      2025-08-09); Bookworm (12) und Forky (14) gibt es über „All image downloads".
      Heißt: Trixie ist normal (kein Downgrade-Rat), einzelne Pakete brauchen ggf.
      einen Workaround (rpi-eeprom-skip, nodesource→bookworm, MAX98357A-Audio unten).

  # ══ AUDIO: MuPiHAT MAX98357A (korrigiert + Trixie-Regression, mit Quelle) ══
  - kind: howto
    id: mupi-max98357a-audio
    title: "MuPiHAT-Audio = MAX98357A auf GPIO16 (NICHT hifiberry)"
    source: "https://github.com/stopfkuchen/MuPiHAT"
    tags: [audio, mupihat, i2s]
    body: |
      Der MuPiHAT-Amp ist der **MAX98357A** (I2S), NICHT ein generischer hifiberry-DAC.
      Overlays (aus dem MuPiHAT-Repo, Device-Tree → auch Pi 5 mit aktuellem Kernel):
        dtoverlay=max98357a,sdmode-pin=16     # Amp, SD-Mode-Pin GPIO16
        dtoverlay=i2s-mmap
        dtparam=i2c_arm=on / i2c1=on / i2c_arm_baudrate=50000   # BQ25792-Batterieleser
        dtoverlay=gpio-poweroff,gpiopin=4,active_low=1
        dtoverlay=gpio-shutdown,gpio_pin=17,active_low=1,gpio_pull=up
      Kein Ton? Karte da (`aplay -l`), ALSA-Default auf sie, MAX-Overlay gesetzt.

  - kind: howto
    id: mupi-max98357a-trixie-silent
    title: "MAX98357A stumm auf Pi 5 + Trixie (obwohl alles erkannt)"
    source: "https://forums.raspberrypi.com/viewtopic.php?t=395006"
    tags: [audio, mupihat, i2s, trixie, pi5, regression]
    body: |
      Bekannte **Regression**: auf Pi 5 + Debian 13 Trixie gibt der MAX98357A KEINEN
      Ton, obwohl das Device erkannt wird, Module laden und die GPIOs beim Abspielen
      togglen — auf Bookworm lief dieselbe HW. (Forum-Thread gelockt, keine offizielle
      Ursache; der Workaround ist empirisch wirksam.) Workaround:
      1) `/boot/firmware/config.txt`: `dtoverlay=max98357a` (oder `googlevoicehat-soundcard`),
         und `dtparam=audio=on` AUSKOMMENTIEREN (`#dtparam=audio=on`).
      2) `/etc/asound.conf`: dmix auf die Karte + softvol fürs Volume.
      3) optional systemd-Service, der dauerhaft `/dev/zero` per `aplay` streamt →
         killt die Klicks am Anfang/Ende.
      4) Fallback: `raspi-config` → Audio → „MAX98357A" wählen, wenn ALSA meckert.
      DIREKT relevant für Achims Box (Pi 5 + Trixie + MuPiHAT) — siehe achim-build.

  # ══ How-tos / Lektionen (allgemein) ════════════════════════════════════════
  - kind: howto
    id: mupi-bluetooth-hang
    title: "Bluetooth-Schritt hängt (der 69%-Freeze)"
    tags: [bluetooth, hang, workaround]
    body: |
      DAS Kern-Symptom, aus dem der remote-step-installer entstand: `dietpi-software
      install 5` bzw. `bluetooth enable` im autosetup HÄNGT (kein Fehlertext), der
      Monolith fror bei ~69 % ohne Abbruch ein. Keine Fehler-Signatur (es hängt still)
      → mit **Timeout** fahren, notfalls **abbrechen** (Agent-Killer-Feature). Braucht
      die Box kein BT: `systemctl mask bluetooth` + Schritt skippen. Für BT-Kopfhörer:
      BT einzeln, kontrolliert, mit Timeout aktivieren.

  - kind: howto
    id: mupi-spotify-web-sdk
    title: "Spotify: Web-SDK-Playback + Token-Proxy + PKCE"
    tags: [spotify, auth, web-sdk]
    source: "https://developer.spotify.com/documentation/web-playback-sdk"
    body: |
      Playback läuft im Chromium-Kiosk über das **Spotify Web Playback SDK** (Widevine,
      localhost-gated) — die Player-Seite muss **HTTP** bleiben, der Token kommt über
      einen **/player-Proxy**. Login ist **pro Origin**; same-origin `apiUrl` + Box-
      Config seeden. Achims Fork nutzt **PKCE** (client_id only, KEIN Secret) im Frontend.
      REDIRECT-URI-Gotcha (Spotify-Regel ab 27.11.2025): `http://localhost` ist NICHT
      mehr erlaubt — nur `https` ODER die Loopback-IP `http://127.0.0.1:PORT` (NICHT der
      Hostname `localhost`). librespot (Audio) authentifiziert SEPARAT (login5).

  - kind: howto
    id: mupi-cover-cache
    title: "Cover-/Media-Cache + Warm-Start"
    tags: [perf, cache, cover]
    body: |
      Perf-Pass: Media-Cache + **alle Cover beim Start vorwärmen** → flüssigeres
      Blättern, keine Ruckler beim ersten Anzeigen. Cover ohne Umrandung (Option B).
      Weitergedacht (deferred): tmpfs-RAM-disk für den Cache — dann periodischer Flush
      bei Unplug nötig, sonst Datenverlust.

  - kind: note
    id: mupi-install-order
    title: "Installationsreihenfolge + Hänger-Stellen"
    tags: [install, order]
    body: |
      Schrittfolge (Recipe `mupibox.yaml`): rpi-eeprom (SKIP auf Trixie) → apt-update
      → deps (python3-rpi-lgpio!) → node (Workarounds!) → npm-globals → App entpacken/
      installieren → audio → **bluetooth (HÄNGT — Timeout!)** → admin (`dietpi-software
      install 5` triggert BT erneut → auch Timeout) → chromium (timeout ~600) → Display
      (Waveshare/DSI) → finalize → BT-Kopfhörer. Die zwei gefährlichen Stellen:
      **bluetooth** und **admin** (BT-Retrigger).

  # ══ Login / Admin / bekannte Probleme (offizielle MuPiBox-Doku mupibox.de) ═
  - kind: howto
    id: mupi-login-passwords
    title: "MuPiBox: Login, Passwörter, Admin-Adresse"
    source:
      - "https://mupibox.de/anleitungen/einstellungen/schnellzugriff-passwoerter/"
      - "https://github.com/splitti/MuPiBox"
    tags: [login, password, admin, howto]
    body: |
      Standard-Zugänge einer MuPiBox — das Image setzt das globale Passwort auf
      **`mupibox`** (NICHT das DietPi-Default `dietpi`):
      - **SSH / System:** User `dietpi`, Passwort `mupibox` (root ebenso, nicht empfohlen).
      - **Samba / FTP (Netzwerkfreigabe):** User `dietpi`, Passwort `mupibox`.
      - **Admin-Webinterface:** `http://mupibox` bzw. `http://<Box-IP>` (Hostname in den
        Einstellungen änderbar). Historisch OHNE Passwort; neuere Versionen können das
        Admin-Panel schützen (in der Community als Default `MuP1B0x` genannt — am eigenen
        Stand prüfen).
      - **Musik-/Bibliotheks-Verwaltung (backend-api):** `http://mupibox:8200` bzw. `:8200` an der IP.
      Gilt für Standard-Installationen; ein eigener Fork/Umbau kann abweichen.

  - kind: howto
    id: mupi-undervoltage-throttle
    title: "Blitz-Symbol oben rechts = Unterspannung/Throttling"
    source: "https://mupibox.de/faq/"
    tags: [power, undervoltage, faq]
    body: |
      Ein Blitz-Symbol oben rechts = **Unterspannung** (throttling) — schwaches Netzteil/
      Kabel oder dünne Jumper am OnOffShim; verursacht Aussetzer/Instabilität. Abhilfe:
      hochwertiges, kurzes microUSB-Kabel (wenig geknickt); den OnOffShim DIREKT löten
      statt dünner Jumper; in den Performance-Einstellungen „Overclock SD Card" an,
      „Initial Turbo" aus, CPU-Governor powersave/conservative, SWAP aus, Onboard-WLAN
      je nach USB-Dongle. (Deckt sich mit der Diagnose-Signatur `under-voltage` aus dmesg.)

  - kind: howto
    id: mupi-audio-output-select
    title: "Audio-Ausgabe wählen / Klinke rauscht"
    source: "https://mupibox.de/faq/"
    tags: [audio, faq]
    body: |
      Die 3,5-mm-Klinke funktioniert, kann aber Störgeräusche machen — für sauberen Ton
      einen I2S-Amp nutzen (HifiBerry-Varianten oder, wie bei Achim, MAX98357A). Im
      Admin-Interface ist das Ausgabegerät wählbar: HDMI, 3,5-mm-Klinke, HifiBerry, USB-DAC.

  - kind: note
    id: mupi-webui-nosound-bydesign
    title: "Kein Ton beim Abspielen ÜBER das Webinterface (Absicht)"
    source: "https://mupibox.de/faq/"
    tags: [audio, webui, faq]
    body: |
      Startet man Musik über das WEB-Interface, kommt bewusst KEIN Ton aus der Box — das
      ist Absicht (Display/Interface würden sonst nicht synchron laufen). Kein Fehler;
      die Wiedergabe steuert man am Touchscreen der Box.

  - kind: howto
    id: mupi-spotify-idle-noplay
    title: "Spotify spielt nach langer Pause nicht (bekanntes Issue #45)"
    source: "https://github.com/splitti/MuPiBox/issues/45"
    tags: [spotify, known-issue, token]
    body: |
      Bekanntes Problem: nach Stunden/Tagen Nichtnutzung startet ein Spotify-Titel nicht
      mehr. Kurzfix: Box **neu starten**; hilft das nicht, die **Spotify-Konfiguration
      neu einrichten** (Token abgelaufen/ungültig). Deckt sich mit unserem Befund, dass
      der backend-player an abgelaufenen/leeren Spotify-Creds hing (Token-Refresh) — in
      Achims Fork abgefangen (kein Boot-Crash mehr).

  - kind: note
    id: mupi-hardware-recommended
    title: "Empfohlene Hardware (Standard-Doku, älteres Wissen)"
    source: "https://mupibox.de/faq/"
    tags: [hardware, faq]
    body: |
      Offiziell empfohlen: **Raspberry Pi 4 mit 1 GB RAM** (läuft ruckelfrei sogar vom
      USB-Stick); passive Kühlkörper reichen meist; feste Tasten (wie bei Phoniebox)
      werden NICHT unterstützt. HINWEIS: älteres Wissen — Achims Umbau läuft auf einem
      **Pi 5** mit MAX98357A-Audio (siehe achim-build), weicht also bewusst ab.

  - kind: note
    id: mupi-librespot-gpio-notes
    title: "librespot + GPIO-Migration (Release-Notes)"
    source: "https://github.com/splitti/MuPiBox/blob/main/news.txt"
    tags: [librespot, gpio, spotify, changelog]
    body: |
      Aus den MuPiBox-Release-Notes: librespot auf **dev_0.6** (Build 2025-03-05, Token
      via login5); GPIO-Umstieg vom veralteten **sysfs-gpio auf libgpiod** (sysfs ist in
      aktuellem Debian raus); Spotify verlangt für Dritt-Apps Zertifikate → die MuPiBox
      nutzt ein **self-signed Cert**. Stable-Fokus weiterhin Debian Bookworm (Trixie =
      neuer, siehe gen-dietpi-versions im generic-Paket).

  # ══ librespot/login5 · Jellyfin · PHP-Admin (recherchiert, mit Quellen) ════
  - kind: signature
    id: mupi-librespot-bad-credentials
    title: "librespot: Bad credentials (Spotify-Audio-Login)"
    sev: high
    match: "Bad credentials|Login failed with reason|authentication.*deprecat"
    cause: "Spotify hat den User/Passwort-Login ENTFERNT → altes librespot kann sich fürs Audio nicht mehr anmelden"
    fix: "login5-fähiges librespot (MuPiBox: dev_0.6) + OAuth/zeroconf statt Passwort; Spotify neu einrichten"
    source:
      - "https://github.com/librespot-org/librespot/issues/1330"
      - "https://github.com/librespot-org/librespot/issues/1274"
    tags: [spotify, librespot, auth, login5]
    related: [librespot-fremdes-konto-uebernommen]
    body: |
      VORHER AUSSCHLIESSEN: dieselbe Zeile 'Bad credentials' entsteht auch,
      wenn ein fremdes Handy librespot per Spotify Connect uebernommen und die
      Zugangsdaten ueberschrieben hat — siehe
      [librespot-fremdes-konto-uebernommen]. Steht in credentials.json ein
      fremdes Konto, ist ein librespot-Update hier wirkungslos. Erst das Konto
      vergleichen (scripts/librespot/librespot-konto.py), dann dieser Eintrag.

      librespot ist die Audio-Engine (spielt den Ton), GETRENNT von der Web-API
      (Metadaten/Steuerung). Spotify hat Username/Passwort abgeschaltet → ein altes
      librespot bekommt „Bad credentials". Lösung: login5-fähige Version (MuPiBox liefert
      dev_0.6) + Anmeldung über OAuth bzw. **zeroconf/Spotify Connect** (die Box erscheint
      als Connect-Gerät, Spotify reicht das Token). Details: mupi-librespot-login5.

  - kind: howto
    id: mupi-librespot-login5
    title: "librespot & login5 — wie sich Spotify-Audio anmeldet"
    source:
      - "https://github.com/librespot-org/librespot/issues/1274"
      - "https://github.com/librespot-org/librespot/discussions/1311"
      - "https://github.com/splitti/MuPiBox/blob/main/news.txt"
    tags: [spotify, librespot, auth, login5, zeroconf]
    body: |
      **login5** = Spotifys neuere Auth-API: nach einem OAuth-Flow lässt sich eine
      vollwertige Session mit gespeicherten Credentials ablegen. Hintergrund: Spotify hat
      den klassischen **Username/Passwort-Login ENTFERNT** (betrifft librespot,
      Kodi-Spotify, Spotty, snapcast …). Moderne Wege: (1) **zeroconf/Spotify Connect** —
      die Box meldet sich als Connect-Gerät im Netz an, die Spotify-App wirft die
      Wiedergabe drauf, das Token kommt per Handshake; (2) **OAuth → gecachte Credentials**
      (login5). MuPiBox nutzt librespot **dev_0.6** (Build 2025-03-05, login5-fähig).
      WICHTIG: der librespot-Audio-Login ist SEPARAT vom Web-API-Login (den macht Achims
      Fork per PKCE für Metadaten/Steuerung) — beide unabhängig einrichten.

  - kind: howto
    id: mupi-jellyfin-setup
    title: "Jellyfin an die MuPiBox anbinden (Server + API-Key)"
    source:
      - "https://jellyfin.org/"
      - "Achims Fork (JellyfinService, /jellyfin-Seite, AdminInterface jellyfin.php) — am Sim verifiziert"
    tags: [jellyfin, media, setup, howto]
    body: |
      MuPiBox kann lokale Medien von einem **Jellyfin-Server** ziehen (neben Spotify/
      lokalen Dateien). Zwei Angaben genügen: **Server-URL** + **API-Key**.
      - API-Key in Jellyfin erzeugen: Dashboard → Advanced → **API Keys** → „Create".
      - In der MuPiBox eintragen: Admin `jellyfin.php` bzw. die `/jellyfin`-Seite (Achims
        Fork) speichert `{server, apiKey}` (Frontend: localStorage `mupibox_jellyfin_cfg`).
        Alben/Playlists erscheinen als Home-Kacheln (Cover-Flip zeigt die Titel).
      - Gotcha (Fork): die Cover-URL trägt Server + `api_key` in der Query → gespeicherte
        Jellyfin-Alben bleiben auch ohne Live-Config spiel-/flippbar, solange Token gültig
        + Server erreichbar. Der Browser ruft Jellyfin DIREKT (cross-origin) → der
        Jellyfin-Server muss CORS für die Box-Origin erlauben.

  - kind: signature
    id: mupi-php-deprecation
    title: "PHP-Deprecation im Admin (8.2–8.4)"
    sev: med
    match: "Deprecated:.*[Dd]ynamic propert|Creation of dynamic property|Deprecated:.*[Pp]assing null|utf8_encode|utf8_decode"
    cause: "Der PHP-Admin läuft auf neuem PHP (Trixie = 8.4) und trifft 8.x-Deprecations"
    fix: "Eigenschaften deklarieren / #[AllowDynamicProperties]; null-Guards; utf8_encode/decode → mb_convert_encoding"
    source: "https://www.php.net/manual/en/migration82.deprecated.php"
    tags: [php, admin, deprecation, trixie, historisch]
    body: |
      HISTORISCH — PHP hat das Projekt am 19.08.2026 verlassen (E47, 52d21406;
      siehe [[fork-erbt-auch-die-wege]]). Das AdminInterface gibt es nicht mehr,
      diese Signatur kann auf einer heutigen Box nicht mehr zuschlagen. Sie
      bleibt fuer BESTANDSBOXEN stehen, die den Ausbau nie mitbekommen haben —
      und als Beleg dafuer, wonach der alte Admin aussah. Nichts hier ist noch
      zu tun.

      Häufige Deprecations alter PHP-Apps auf 8.2–8.4: dynamische Objekt-Eigenschaften
      (8.2), null an nicht-nullbare interne Parameter (8.1), `${var}`-String-Interpolation
      (8.2), entfernte `utf8_encode/decode` (8.2). Sichtbar als „Deprecated:"-Zeilen im
      lighttpd/PHP-Log. Fix-Details: mupi-php84-admin.

  - kind: howto
    id: mupi-php84-admin
    title: "Der PHP-Admin auf Debian Trixie (PHP 8.4)"
    source:
      - "https://packages.debian.org/trixie/php"
      - "https://www.php.net/manual/en/migration84.php"
    tags: [php, admin, lighttpd, trixie, historisch]
    body: |
      HISTORISCH — ERLEDIGT DURCH AUSBAU, NICHT DURCH REPARATUR. Das To-do am
      Ende („den Admin fuer 8.3/8.4 sauber machen") ist am 19.08.2026
      gegenstandslos geworden: PHP hat das Projekt verlassen (E47, 52d21406),
      samt `AdminInterface/`, lighttpd und dem Bauskript. An seiner Stelle
      steht die Angular-Verwaltung (`src/frontend-admin`), die als `www-admin/`
      im deploy.zip reist. Siehe [[fork-erbt-auch-die-wege]]. Wer hier landet,
      soll wissen, dass es die App nicht mehr gibt — der Text bleibt als
      Beschreibung dessen, was ersetzt wurde.

      Das MuPiBox-AdminInterface ist eine PHP-App (~28 Seiten), von **lighttpd** serviert.
      PHP-Version je Debian-Basis: **Bullseye 7.4 · Bookworm 8.2 · Trixie 8.4**. Eine
      frische MuPiBox auf Trixie läuft also auf **PHP 8.4** → alter Admin-Code wirft
      Deprecation-Warnungen (siehe mupi-php-deprecation). Zusätzlich wendet php-cgi unter
      Debian 13 mit lighttpd standardmäßig **strengere Permission-Limits** an (Pfade/Rechte
      prüfen). To-do (Achims Backlog): den Admin für 8.3/8.4 sauber machen (Eigenschaften
      deklarieren, null-Guards, Interpolation, Encoding) — parallel zum geplanten
      Spotify-Caching-Select im Admin.

  # ══ OPTIMIERUNG MÖGLICH — neuere Versionen/Methoden = Gewinn (recherchiert) ═
  - kind: optim
    id: optim-node-24-lts
    title: "Node 22 → Node 24 (Active LTS)"
    current: "Node 22 (Maintenance LTS, Support bis Apr 2027) für die zwei Backends"
    better: "Node 24 — Active LTS (Support bis Apr 2028), neuere V8"
    gain: "längerer Sicherheits-Support + V8-Perf; sauberer Startup"
    since: "Node 24 = Active LTS seit 2026 (nodesource/nvm)"
    how: "nodesource-Repo auf node_24.x (bookworm-Suite auf Trixie, s. mupi-nodesource-trixie) ODER `nvm install 24`; danach backend-api + backend-player testen"
    source: "https://nodejs.org/en/blog/announcements/evolving-the-nodejs-release-schedule"
    tags: [node, upgrade]
    body: |
      Stand Juli 2026: Node 24 = Active LTS, Node 22 = Maintenance, Node 26 = Current
      (LTS ab Okt 2026). Für die MuPiBox lohnt der Sprung auf 24 (mehr Perf + längerer
      Support). Vorher Express/Deps-Kompatibilität prüfen. Ab Okt 2026 wird JEDE
      Node-Major zur LTS + Versionsnummern folgen dem Kalenderjahr.

  - kind: optim
    id: optim-mplayer-mpv
    title: "mplayer → mpv (lokale Wiedergabe)"
    current: "mplayer (Slave-Modus) für die lokale Audio-Wiedergabe des backend-player"
    better: "mpv — der aktiv gepflegte Nachfolger (mplayer/mplayer2 sind unmaintained)"
    gain: "moderner + effizienter (FFmpeg, HW-Decode) und ein sauberes JSON-IPC statt des fragilen Slave-Protokolls"
    since: "mpv ist in Debian/DietPi (apt install mpv)"
    how: "Player-Wrapper auf mpv umstellen: `--input-ipc-server` statt mplayer-Slave; loadfile/`pt_step`→playlist-Kommandos anpassen"
    source: "https://wiki.archlinux.org/title/Mpv"
    tags: [audio, mpv, mplayer, upgrade]
    body: |
      Der backend-player steuert lokale Titel über **mplayer** (`pt_step`, `loadlist`).
      mplayer ist unmaintained; **mpv** (Fork 2012, aktiv) ist der moderne Ersatz — bessere
      Codecs, HW-Decode, und ein stabiles JSON-IPC (`--input-ipc-server`). Umbau = B2-Größe
      (Player-Interface), aber robuster (räumt nebenbei den mplayer-pause-Toggle-Drift weg).

  - kind: optim
    id: optim-chromium-cog
    title: "Chromium-Kiosk → Cog/WPE WebKit (nur ohne Web-SDK)"
    current: "Chromium im Kiosk-Modus fürs Touch-UI (größter RAM-/Boot-Fresser)"
    better: "Cog + WPE WebKit — schlanker Embedded-Kiosk-Stack (~28 MB, HW-beschleunigt)"
    gain: "deutlich weniger RAM + schnellerer Boot als Chromium auf dem Pi"
    since: "WPE WebKit/cog in Debian (apt install cog)"
    how: "NUR wenn das Spotify Web Playback SDK NICHT im Browser läuft — Audio dann headless über librespot; sonst braucht Widevine/SDK weiter Chromium"
    source:
      - "https://greenchapel.dev/2026/01/04/wpe-webkit-cog-a-kiosk-ui-stack/"
      - "https://webkit.org/wpe/"
    tags: [chromium, cog, wpe, kiosk, ram, gated]
    body: |
      Chromium frisst am meisten RAM/Bootzeit. **Cog + WPE WebKit** ist ein viel schlankerer
      Kiosk-Stack (~28 MB, HW-beschleunigt, startet in Bruchteil der Zeit). ABER: MuPiBox
      spielt Spotify per **Web Playback SDK** (Widevine) IM Browser → das braucht Chromium.
      Der Swap lohnt nur gepaart mit „Audio headless über librespot" (Browser raus aus dem
      Audiopfad, s. mupi-librespot-login5). Ohne diese Umstellung bleibt Chromium nötig.

  - kind: optim
    id: optim-php-84-opcache
    title: "PHP-Admin: OPcache auf 8.4 (Trixie)"
    current: "PHP-Admin ohne bewusst gesetztes OPcache (Bookworm = PHP 8.2)"
    better: "PHP 8.4 (auf Trixie ohnehin da) mit aktiviertem OPcache"
    gain: "OPcache spart das Neu-Parsen pro Request (spürbar für die ~28 Seiten). JIT bringt hier WENIG — der Admin ist I/O-lastig, nicht CPU-lastig (ehrlich eingeordnet)"
    since: "Trixie = PHP 8.4; OPcache ist Standard-Extension"
    how: "erst 8.4-kompatibel machen (mupi-php84-admin), dann `opcache.enable=1` in der php.ini prüfen; JIT nur bei echter CPU-Last erwägen"
    source:
      - "https://www.php.net/manual/en/book.opcache.php"
      - "https://phpbenchlab.com/php-7-4-vs-8-0-vs-8-1-vs-8-2-vs-8-5-the-performance-evolution-2026/"
    tags: [php, admin, opcache, trixie, upgrade, historisch]
    body: |
      HINFAELLIG seit dem 19.08.2026: Es gibt keinen PHP-Admin mehr, den man
      mit OPcache beschleunigen koennte (E47, 52d21406 —
      [[fork-erbt-auch-die-wege]]). Der Gewinn ist stattdessen ANDERS
      eingefahren worden: die ~28 Seiten sind weg, lighttpd und php-cgi laufen
      nicht mehr mit. Bleibt als Beispiel dafuer, einen Gewinn ehrlich
      einzuordnen, statt ihn zu ueberverkaufen.

      Nicht überverkaufen: PHP-**JIT** hilft CPU-lastigen Apps (80-95 % in Frameworks),
      der MuPiBox-Admin ist aber I/O-lastig (JSON lesen/schreiben) → JIT <5 %. Der reale
      Gewinn ist **OPcache** (kein Neu-Parsen pro Request) + die inkrementellen
      8.2→8.4-Verbesserungen (auf Trixie sowieso da). Voraussetzung: 8.4-sauberer Admin.

  - kind: optim
    id: optim-two-node-single
    title: "backend-api + backend-player → EIN Dienst"
    current: "zwei Node-Prozesse (api + player) unter pm2 + separates PHP-Admin"
    better: "die zwei Node-Dienste zu EINEM Prozess/Service zusammenlegen; Admin on-demand"
    gain: "weniger RAM (ein V8-Heap statt zwei) + schnellerer Boot (pm2 kann entfallen)"
    since: "app-seitige Änderung, versionsunabhängig"
    how: "gemeinsamer Entry-Point ODER eine schlanke systemd-Unit statt pm2; Ports/IPC intern; lighttpd/PHP-Admin nur bei Bedarf starten"
    tags: [node, consolidation, ram, boot]
    body: |
      Größere, aber lohnende App-Änderung (vgl. den perf-Eintrag mupi-two-node-processes):
      ein V8-Heap statt zwei spart RAM; ohne pm2 (oder als schlanke systemd-Unit) bootet es
      schneller. Das PHP-Admin muss nicht dauerhaft laufen — on-demand.

      STAND 22.08.2026: Der PHP-Teil dieses Vorschlags hat sich von selbst
      erledigt — lighttpd und php-cgi laufen seit dem 19.08. gar nicht mehr mit
      (E47, [[fork-erbt-auch-die-wege]]); die Verwaltung ist Angular und wird
      vom Backend mitgeliefert. Was OFFEN bleibt, ist der eigentliche Punkt:
      api und player sind weiterhin ZWEI Node-Prozesse.

  # ══ ACHIMSUMBAU — Achims KONKRETES Gerät (verifiziert aus den Sessions) ════
  - kind: note
    id: achim-build
    title: "Achims Aufbau (Ist-Stand)"
    source: "eigene Sessions 2026-07, verifiziert am Gerät + Fork /home/achim/Downloads/MuPiBox"
    tags: [achimsumbau, hardware, overview]
    body: |
      - **Raspberry Pi 5**, frische DietPi-Installation auf **Debian 13 Trixie**.
      - **MuPiHAT PCB 3.4.1** (Audio MAX98357A/GPIO16 + BQ25792-Laderegler, 2S Li-Ion).
      - Touchscreen **Waveshare 5" DSI LCD (800×480), an DSI1** (NICHT das offizielle
        7", NICHT DSI0/disp0 — beide früheren Annahmen waren falsch).
      - Software = eigener **MuPiBox-Fork** (4.3.0-Basis) mit vielen Modernisierungen
        (System-Player + now-playing-Widget, PKCE-Spotify, Jellyfin, opt-in HTTPS,
        Cover-Cache) und der gepatchten autosetup/make-boot-sd-Kette.
      - Kann NICHT ferngewartet werden (SSH nur Passwort, nicht-interaktiv) → Lieferform
        = Skripte, die Achim auf der Box ausführt. GENAU der Fall für den remote-step-installer.

  - kind: howto
    id: achim-pi5-blackscreen-fix
    title: "Achims Schwarzbild-Ursache + Fix"
    source: "Achims Fork Commit 804d7b29 + documentation/pi5-fresh-setup.md (am Gerät verifiziert)"
    tags: [achimsumbau, pi5, display, blackscreen]
    body: |
      Das „schon wieder kaputt/schwarz" kam vom autosetup: `dietpi-set_hardware
      rpi-opengl disable` lief MODELL-UNABHÄNGIG → auf dem Pi 5 (KMS-only) kein
      GPU-Treiber → schwarzes DSI-Bild. Fix im Fork: autosetup ÜBERSPRINGT den disable
      auf einem Pi 5. Aktuelle Box OHNE Neuflashen heilen — in `/boot/firmware/config.txt`:
      `vc4-fkms-v3d` + `vc4-kms-dsi-7inch` + `framebuffer_width|height` löschen, dann
      `dtoverlay=vc4-kms-v3d` + `dtoverlay=vc4-kms-dsi-waveshare-panel,5_0_inch`, reboot.
      Latenter Bug: autosetup schreibt den framebuffer auf HARTKODIERT `/boot/config.txt`
      statt `${BOOT_CONFIG}` (= /boot/firmware auf Bookworm/Trixie) — unter KMS harmlos.

  - kind: howto
    id: achim-mupihat-341
    title: "Achims MuPiHAT 3.4.1 bootet sauber am Pi 5"
    source: "am Gerät verifiziert (2026-07); vs. README-Stand https://github.com/stopfkuchen/MuPiHAT (nur bis 3.2)"
    tags: [achimsumbau, mupihat, pi5, power]
    body: |
      Achims Platine ist **PCB 3.4.1** — NEUER als die im README dokumentierte 3.2
      (das README verspricht eine voll-Pi5-taugliche Version „coming soon"). Am Gerät:
      der Pi 5 bootet auf der 3.4.1 sauber → der 5V-Ramp ist offenbar behoben (die
      3.4.1 IST vermutlich die versprochene Pi-5-Version), das öffentliche Repo hängt
      nur hinterher. Fallback, falls eine Box doch zickt: Pi 5 aus 5V/5A-USB-C speisen.

  - kind: howto
    id: achim-display
    title: "Achims Display-Config (5\" DSI, 800x480) — KORRIGIERT am Gerät"
    source: "am Gerät verifiziert 2026-07-25 (Testbild vollflächig, 800x480, richtige Orientierung)"
    tags: [achimsumbau, display, dsi, pi5]
    body: |
      RICHTIG (am 25.07.2026 am Gerät bewiesen), in `/boot/firmware/config.txt`:
        dtoverlay=vc4-kms-v3d
        dtoverlay=vc4-kms-dsi-7inch          # 800x480 — DAS ist Achims Panel
      FALSCH war die frühere Annahme `dtoverlay=vc4-kms-dsi-waveshare-panel,5_0_inch`:
      dieser Overlay-Parameter bedeutet laut `/boot/firmware/overlays/README`
      **720x1280** (ein Portrait-Panel). Damit blieb der Schirm KOMPLETT schwarz, es
      erschien auch kein Touch-Eingabegerät. Mit `vc4-kms-dsi-7inch` kam sofort ein
      Bild: fb0 = 800x480 @ 32 bpp, Rahmen an allen vier Kanten sichtbar, Ecken/
      Orientierung korrekt. Merke: die 5"-Zeile des Waveshare-Overlays passt NUR zum
      720x1280-Modell; für ein 800x480-DSI-Panel ist das offizielle 7-Zoll-Overlay das
      richtige (gleiche Auflösung, gleicher Timing-Satz).
      Für einen dual-boot-SD (Pi 4 UND Pi 5) einen `[pi5]`-Block anhängen und mit
      `[all]` schliessen — config.txt-Filter sind STICKY, ein offener `[pi5]`-Block
      „vergiftet" alles Folgende. Der Pi 4 behält seine Baseline.

  - kind: note
    id: achim-config-gotchas
    title: "config.txt-Fallen in Achims Fork"
    source: "Achims Fork (scripts/mupihat/enable_mupihat.sh), 2026-07"
    tags: [achimsumbau, config, autosetup, gotcha]
    body: |
      - `enable_mupihat.sh` / `disable_mupihat.sh` schreiben den LEGACY-Pfad
        `/boot/config.txt`, NICHT `/boot/firmware/config.txt` (Bookworm/Trixie) → beim
        erneuten Ausführen anpassen, sonst wirkt die Änderung nicht.
      - autosetup Zeilen ~553/554 schreiben den framebuffer hartkodiert nach
        `/boot/config.txt` statt `${BOOT_CONFIG}` (= /boot/firmware) — unter KMS harmlos,
        aber latenter Bug.
      - `[pi5]`-Blöcke IMMER mit `[all]` schliessen (Filter sind sticky).
      - Boot-Verzeichnis auf Trixie/Bookworm = `/boot/firmware` (der Installer erkennt
        das via `$BOOT_DIR`).

  - kind: howto
    id: achim-boot-sd
    title: "Achims fork-freie Boot-SD + Offline-Update"
    source: "Achims Fork (scripts/make-boot-sd.sh, documentation/hardware-test.md), 2026-07"
    tags: [achimsumbau, boot-sd, build, deploy]
    body: |
      UEBERHOLT SEIT DEM 23.09.2026 IN ZWEI PUNKTEN: das Paket schliesst `bin/librespot`
      NICHT mehr aus (autosetup bricht ohne das Binaer hart ab, den wget-Rueckfall gibt
      es nicht mehr), und es packt nur noch die Ordner, aus denen autosetup liest —
      14 MB mit librespot 0.8.0. Siehe
      [[der-ersatzwert-eines-pfades-ist-der-rechner-dessen-der-ihn-schrieb]].
      Der Rest unten beschreibt den Stand vom Juli 2026.

      **Fresh-SD:** `scripts/make-boot-sd.sh <boot-partition>` baut `MuPiBox-<ver>.tgz`
      aus HEAD, legt autosetup + DietPi-Custom-Script + `AUTO_SETUP_CUSTOM_SCRIPT_EXEC=1`
      ab. `MUPI_LOCAL_SRC` überspringt die GitHub-wgets → das SD-Tarball EXKLUDIERT
      `bin/librespot` (librespot wird eh gewget) → **11 MB** statt 170 MB. `www.zip`
      (Admin) ist PREBUILT — nach Admin-Edits neu bauen (`zip -r ../release/www.zip .`),
      sonst laufen alte Assets. Boot-Partition mountet am Laptop unter
      `/run/media/$USER/<FAT-SERIAL>`, nicht `/media/$USER/bootfs`.
      **Bestehende Box (keine Blank-SD):** in-place gegen den gemounteten rootfs. App-
      Dirs `~/.mupibox/*` sind uid 1000 (dietpi) → als lokaler User schreibbar; `/var/www`
      + `/etc` sind root → Admin-Update via `www.zip` + `apply-admin.sh` einmal mit sudo
      auf der gebooteten Box. `deploy.zip` enthält KEIN `server/config/` → data.json +
      mupiboxconfig.json (Medien/Tokens) bleiben unberührt. Immer vorher sichern.

  - kind: howto
    id: frontend-ausspielen-ohne-luecke
    title: "Frontend ausspielen: www als Verweis, Tausch ohne Luecke"
    source: "Am Geraet erarbeitet, 2026-07 (zweimal weisser JSON-Bildschirm im Kiosk)"
    tags: [achimsumbau, deploy, frontend, kiosk]
    body: |
      **Symptom:** nach dem Ausspielen zeigt der Kiosk eine weisse „Pretty
      Print"-JSON-Seite und kommt von allein nicht zurueck.

      **Ursache:** der Tausch `mv www www.alt && mv www.neu www` hat eine Luecke
      von Millisekunden, in der `www` NICHT existiert. Wer genau dann laedt —
      der Kiosk, oder der eingebaute Fassungs-Waechter, der alle 8 s prueft —
      bekommt statt der App eine API-Antwort und bleibt darauf stehen.

      **Loesung:** `www` ist ein VERWEIS auf `www.rel/<stand>`. Ein Verweis
      laesst sich mit `mv -T` in EINEM Schritt ersetzen (rename(2) ist atomar),
      es gibt also keinen Moment ohne `www`:

          B=/home/dietpi/.mupibox/Sonos-Kids-Controller-master
          mkdir -p $B/www.rel/r$$ && tar xzf /tmp/www.tgz -C $B/www.rel/r$$
          test -f $B/www.rel/r$$/index.html || exit 1     # erst pruefen
          chown -R dietpi:dietpi $B/www.rel/r$$
          ln -sfn www.rel/r$$ $B/www.tmp && mv -T $B/www.tmp $B/www
          # alte Staende danach aufraeumen

      **Wieder herausholen, wenn es doch passiert:** `xdotool` ist NICHT
      installiert, der Kiosk darf nicht einzeln neu gestartet werden (killt X,
      braucht dann eine Konsole) — bleibt `systemctl reboot`.

  - kind: signature
    id: mupi-dietpi-firstrun-incomplete
    title: DietPi-Erstinstallation unfertig → dietpi-software verweigert alles
    sev: high
    match: DietPi has not fully been installed
    cause: Die DietPi-Erstinstallation ist nie abgeschlossen (/boot/dietpi/.install_stage < 2). dietpi-software
      verweigert dann JEDE Installation und fragt in Endlosschleife nach — der Schritt haengt scheinbar
      ewig (der klassische 69%-Freeze).
    fix: 'Stufe pruefen: cat /boot/dietpi/.install_stage (2 = fertig). Sonst: sed -i "s/^AUTO_SETUP_AUTOMATED=.*/AUTO_SETUP_AUTOMATED=1/"
      /boot/dietpi.txt && G_INTERACTIVE=0 /boot/dietpi/dietpi-login < /dev/null'
    source: eigener Lauf 2026-07-25 an Achims Pi 5 (DietPi v10.5.2, Trixie); Ursache im Quelltext /boot/dietpi/dietpi-login
      (Prompt_on_Failure) verifiziert

  - kind: config
    id: mupi-waveshare5-wrong-overlay
    title: '5-Zoll-DSI schwarz: waveshare-panel,5_0_inch ist 720x1280'
    sev: high
    file: config.txt
    present: waveshare-panel,5_0_inch
    why: Der Overlay-Parameter 5_0_inch steht laut overlays/README fuer ein 720x1280-Portrait-Panel. Ein
      800x480-DSI-Panel bleibt damit KOMPLETT schwarz (auch kein Touch-Geraet).
    fix: 'sed -i "s|^dtoverlay=vc4-kms-dsi-waveshare-panel,5_0_inch|#&|" {f} && echo dtoverlay=vc4-kms-dsi-7inch
      >> {f} && reboot   # 7inch-Overlay = 800x480'
    source: 'am Geraet verifiziert 2026-07-25 (Pi 5, DietPi Trixie): mit 5_0_inch schwarz, mit vc4-kms-dsi-7inch
      sofort Bild'

  - kind: config
    id: mupi-consoleblank
    title: Bildschirm wird nach 10 Minuten schwarz (Konsolen-Blanking)
    sev: med
    file: cmdline.txt
    absent: consoleblank
    why: Ohne consoleblank=0 schaltet Linux die Textkonsole nach 600 s ab — sieht aus wie ein defektes
      Display, obwohl die Hintergrundbeleuchtung an ist.
    fix: 'sed -i "1s/$/ consoleblank=0/" {f}   # sofort: setterm --blank 0 > /dev/tty1'
    source: am Geraet verifiziert 2026-07-25

  - kind: signature
    id: mupi-asound-hardcoded-card0
    title: 'Kein Ton + Player-Absturzschleife: asound.conf verdrahtet plughw:0'
    sev: high
    match: 'audio open error: Unknown error 524|amixer sget Master|Mixer attach default error'
    cause: 'Die Karten-Reihenfolge ist zwischen Neustarts NICHT stabil (Pi 5: MAX98357A mal card 0, mal
      card 2, die HDMI-Ausgaenge wandern). Die asound.conf des Forks verdrahtet aber plughw:0 fest ->
      zeigt auf HDMI ohne Bildschirm -> JEDE Wiedergabe scheitert mit ENOTSUPP (524), PulseAudio faellt
      auf auto_null zurueck, der softvol-Regler Master entsteht nie, und der Player stirbt beim Start
      an `amixer sget Master` (pm2 startete ihn 58x neu).'
    fix: 'asound.conf auf NAMEN umstellen: slave.pcm "plughw:CARD=MAX98357A,DEV=0", control.card "MAX98357A",
      dazu ctl.!default auf dieselbe Karte. Der softvol-Regler entsteht zudem erst bei der ersten Wiedergabe
      und ist nach jedem Boot weg -> per systemd-Dienst vor pm2 anlegen (kurz /dev/zero durch default
      spielen).'
    source: 'eigener Lauf 2026-07-25 an Achims Pi 5 (DietPi Trixie): aplay auf allen Formaten ENOTSUPP,
      /proc/asound/cards zeigte vertauschte Reihenfolge; nach Namensbezug spielte es sofort und die Schleife
      stoppte'

  - kind: config
    id: mupi-wifi-powersave
    title: 'Box faellt immer wieder aus dem Netz: WLAN-Energiesparmodus'
    sev: high
    file: cmdline.txt
    absent: __nie__
    why: 'Mit "power_save on" schlaeft das Funkmodul ein: SSH stirbt mitten im Betrieb, die Box wirkt
      abgestuerzt und ist Minuten spaeter wieder da — ohne Unterspannung (throttled=0x0) und ohne Abbruch-Meldungen
      im Log. Am Geraet dreimal erlebt, bis es gefunden war.'
    fix: 'iw dev wlan0 set power_save off  +  systemd-Unit, die es bei jedem Boot setzt (Recipe-Schritt
      wlan-stabil). Pruefen: iw dev wlan0 get power_save'
    source: eigener Lauf 2026-07-25 an Achims Pi 5 (DietPi Trixie, WLAN -64 dBm)

  - kind: signature
    id: mupi-api-data-hangs
    title: 'Oberflaeche dreht endlos: /api/data antwortet NIE (fehlende Zustandsdatei)'
    sev: high
    match: api/data|Cannot ensure player ready|active_data
    cause: 'Der Backend-Handler ist `existsSync(f) && readFile(f, ...)`. Fehlt die Datei, wird GAR KEINE
      Antwort gesendet — die Anfrage haengt unendlich, die Oberflaeche zeigt ewig den Ladekreis. Betroffen:
      server/config/data.json, active_data.json, resume.json, active_resume.json (die legt weder autosetup
      noch eine Vorlage an).'
    fix: 'Fehlende Dateien mit leerer Liste anlegen: for f in data.json active_data.json resume.json active_resume.json;
      do [ -s "$APP/server/config/$f" ] || echo "[]" > "$APP/server/config/$f"; done; chown dietpi:dietpi
      ...'
    source: 'eigener Lauf 2026-07-25 an Achims Pi 5: /api/data lief in jeden Timeout (25 s, 0 Byte), nach
      dem Anlegen HTTP 200 in 1,5 ms'
    related: [widevine-gehoert-dem-kiosk-nutzer, mv-auf-einen-verweis-ersetzt-ihn]
    body: |
      ZWEI EINTRAEGE ANTWORTEN AUF 'Cannot ensure player ready' — dieser und
      [widevine-gehoert-dem-kiosk-nutzer]. Die Ursachen sind verschieden, die
      Logzeile ist dieselbe. Erst die Frage, dann der Eintrag:

        curl -m 5 -o /dev/null -w '%{http_code} %{time_total}\n' \
             http://localhost:8200/api/data

      Kein Code / Timeout nach 25 s  -> DIESER Eintrag (fehlende Zustandsdatei).
      HTTP 200 in Millisekunden      -> die Daten kommen an, der Ladekringel
                                        liegt am DRM-Modul: siehe
                                        [widevine-gehoert-dem-kiosk-nutzer].

      UND `active_data` STEHT EBENFALLS IN ZWEI EINTRAEGEN. Die eine
      Frage, die trennt, beantwortet ein `ls -l` im Konfigordner:

        ls -l ~dietpi/.mupibox/Sonos-Kids-Controller-master/server/config

      Die Datei FEHLT ganz            -> DIESER Eintrag (die Anfrage
                                         haengt, nichts antwortet).
      Die Datei ist DA, aber eine
      gewoehnliche Datei, wo ein
      Verweis stehen muesste          -> [[mv-auf-einen-verweis-ersetzt-ihn]]
                                         (dort antwortet alles brav und
                                         zeigt den falschen Stand).

      Der Unterschied ist am Symptom nicht zu sehen: einmal dreht die
      Oberflaeche ewig, einmal laeuft sie fluessig und luegt.

  - id: widevine-gehoert-dem-kiosk-nutzer
    kind: signature
    match: 'state: not_loaded|Cannot ensure player ready|un\s+libwidevinecdm0'
    title: 'Spotify-Web-SDK bleibt ''not_loaded'': Widevine liegt im Home des falschen Nutzers'
    symptom: 'Die Oberflaeche zeigt endlos den Ladekringel; im Backend-Log wiederholt ''[Spotify SDK]
      ensurePlayerReady() called, state: not_loaded'' und ''Cannot ensure player ready - device is offline''.
      Netz und sdk.scdn.co sind erreichbar (HTTP 200) — es liegt NICHT am Internetzugang.'
    cause: 'Das Debian-Paket libwidevinecdm0 legt das DRM-Modul per postinst im Home des INSTALLIERENDEN
      Nutzers ab (bei apt als root: /root/.config/chromium/WidevineCdm) sowie unter /opt/WidevineCdm.
      Der Kiosk-Chromium laeuft aber als ''dietpi'' und sucht in /home/dietpi/.config/chromium — findet
      nichts und kann den geschuetzten Spotify-Stream nicht abspielen.'
    fix: "libwidevinecdm0 installieren UND dem Kiosk-Nutzer zugaenglich machen:\n  apt-get install -y\
      \ --no-install-recommends libwidevinecdm0\n  install -d -o dietpi -g dietpi /home/dietpi/.config/chromium\n\
      \  cp -a /root/.config/chromium/WidevineCdm /home/dietpi/.config/chromium/\n  chown -R dietpi:dietpi\
      \ /home/dietpi/.config/chromium\nPruefen: ls -d /home/dietpi/.config/chromium/WidevineCdm\nIm Rezept\
      \ erledigt das der Schritt 'chromium' (mupibox.yaml)."
    source: 'am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07): dpkg meldete ''un libwidevinecdm0 <none>''
      (gar nicht installiert), nach der Installation lag das Modul nur unter /root/.config/chromium und
      /opt/WidevineCdm'
    related: [mupi-api-data-hangs]
    body: |
      DIESELBE LOGZEILE, ANDERE URSACHE: 'Cannot ensure player ready' steht auch
      im `match:` von [mupi-api-data-hangs] (dort haengt /api/data, weil eine
      Zustandsdatei fehlt). Dieser Eintrag gilt erst, wenn /api/data sauber mit
      200 antwortet und der Kringel TROTZDEM bleibt. Das Unterscheidungsmerkmal
      steht im Log daneben: 'state: not_loaded' bzw. 'un libwidevinecdm0' kommt
      nur hier vor.

  - id: etc-mupibox-gehoert-dem-backend
    kind: signature
    match: mkdir '?/etc/mupibox/tls|TLS auto-generation failed
    title: 'Backend-Log: EACCES beim Anlegen von /etc/mupibox/tls'
    symptom: 'Im pm2-Log des Backends wiederholt: ''TLS auto-generation failed, serving HTTP: EACCES:
      permission denied, mkdir /etc/mupibox/tls''.'
    cause: /etc/mupibox wird beim Aufsetzen als root angelegt; das Backend laeuft aber als Nutzer 'dietpi'
      und will dort beim Start sein TLS-Verzeichnis erzeugen.
    fix: "Verzeichnis dem Backend-Nutzer geben, Gruppe www-data mit setgid, damit der PHP-Admin die Konfiguration\
      \ weiter speichern kann:\n  install -d -o dietpi -g www-data -m 2775 /etc/mupibox\n  install -d\
      \ -o dietpi -g dietpi  -m 755  /etc/mupibox/tls\n  chown dietpi:www-data /etc/mupibox/mupiboxconfig.json\
      \ && chmod 664 dito\nFuer den Betrieb folgenlos (es wird HTTP ausgeliefert), aber es verrauscht\
      \ das Log und verdeckt echte Fehler. Im Rezept erledigt das der Schritt 'dirs' (mupibox-app.yaml)."
    source: am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07)

  - id: api-routen-ohne-gegenzweig-haengen
    kind: signature
    match: /api/mupihat|/api/wlan|mupihat\.json|Oberflaeche wird immer langsamer|index\.php.*(haengt|0
      bytes)
    title: 'Box wird mit der Laufzeit immer langsamer: Backend-Routen antworten NIE (Verbindungen laufen
      voll)'
    symptom: Direkt nach dem Neustart flüssig, dann zunehmend zäh; Ladekringel dreht endlos; der PHP-Admin
      auf Port 80 liefert Kopfzeilen und dann 0 Byte (auch nach 70 s). Statische Dateien bleiben dabei
      schnell (6-500 ms) — die Box ist NICHT ausgelastet (Last < 0,3).
    cause: 'Mehrere GET-Routen im gebündelten server.js sind als ''existsSync(f) && readFile(f, …)'' geschrieben
      — OHNE Gegenzweig. Fehlt die Datei, wird der Antwortweg nie betreten und die Anfrage bleibt für
      immer offen. Die Oberfläche fragt sie zyklisch ab; jede offene Anfrage belegt dauerhaft eine der
      sechs Verbindungen, die ein Browser pro Server hält. Nach wenigen Zyklen ist das Kontingent verbraucht
      und JEDE weitere Anfrage wartet endlos — daher die mit der Laufzeit wachsende Trägheit. Das Backend
      selbst blockiert NICHT (gesunde Routen antworten weiter in 10-20 ms), es ist reine Verbindungsverknappung
      im Browser. Betroffen: /api/data, /api/activedata, /api/monitor, /api/activeresume, /api/mupihat,
      /api/wlan. Richtig gemacht sind /api/albumstop, /api/network, /api/resume (Ternär mit e.json({})).'
    fix: "Alle erwarteten Zustandsdateien anlegen (leeres Array genügt — genau das liefert der Fehlerzweig\
      \ des Codes selbst):\n  C=~dietpi/.mupibox/Sonos-Kids-Controller-master/server/config\n  for f in\
      \ data.json active_data.json resume.json active_resume.json wlan.json; do\n    [ -s \"$C/$f\" ]\
      \ || echo '[]' > \"$C/$f\"; done\n  chown dietpi:dietpi \"$C\"/*.json\nACHTUNG /api/mupihat: die\
      \ Datei liegt NICHT im Konfigordner, sondern unter /tmp/mupihat.json (im Bündel fest verdrahtet)\
      \ — und /tmp ist nach jedem Start leer. Ohne MuPiHAT schreibt sie niemand, also bei jedem Boot anlegen\
      \ lassen:\n  echo 'f /tmp/mupihat.json 0644 dietpi dietpi - []' > /etc/tmpfiles.d/mupibox.conf\n\
      \  systemd-tmpfiles --create /etc/tmpfiles.d/mupibox.conf\nPrüfen (vom Laptop): jede Route muss\
      \ antworten —\n  for r in mupihat wlan data activedata monitor; do curl -s -o /dev/null -m 8 \\\n\
      \    -w \"$r %{http_code} %{time_total}s\\n\" http://mupibox:8200/api/$r; done\nIm Rezept erledigt\
      \ das der Schritt 'konfig' (mupibox-app.yaml)."
    source: 'am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07): index.php lieferte 70 s lang 0 Byte, danach
      200 in 860 ms; /api/mupihat von Dauerhänger auf 0,21 s. Die Pfadvariable wurde aus dem minifizierten
      server.js ausgelesen (Bh = "/tmp/mupihat.json").'
    related: [endpunkt-ohne-datei-antwortet-nie]
    body: |
      `/api/mupihat` UND `/api/wlan` STEHEN IN ZWEI EINTRAEGEN. Die eine
      Frage, die trennt: IN WELCHER SCHICHT WIRD REPARIERT?

          Eine laufende Box wird zaeh, die Datei fehlt DORT
          -> DIESER Eintrag: die Zustandsdateien anlegen (`echo '[]'`),
             /tmp/mupihat.json ueber tmpfiles.d bei jedem Start.

          Der Quelltext soll den Fall ueberhaupt nicht mehr haben
          -> [[endpunkt-ohne-datei-antwortet-nie]]: der fehlende
             else-Zweig in server.ts, gefunden ueber das MUSTER, nicht
             ueber eine zaehe Box.

      Es ist derselbe Fehler, zweimal gemessen — einmal am Verhalten,
      einmal an der Form. Wer nur den einen liest, legt Dateien an und
      wundert sich beim naechsten neuen Endpunkt; wer nur den anderen
      liest, baut den Code um und hat die Boxen im Feld nicht geheilt.

  - id: spotify-einrichten-ueber-https-remote-setup
    kind: howto
    title: 'Spotify einrichten: nur über den HTTPS-Modus des Backends (Port 8443) — UEBERHOLT seit E22'
    body: "UEBERHOLT SEIT DEM 04.08.2026 — NICHT MEHR SO EINRICHTEN (BACKLOG E22/R1-R3).\nSchritt 3 weiter unten ist seitdem FALSCH und fuehrt sicher in 'INVALID_CLIENT: Insecure redirect URI': das Frontend baut die Redirect-URI nicht mehr aus der Adresse der Seite, sondern schickt die FESTE Adresse http://127.0.0.1:8200/spotify (SPOTIFY_RUECKWEG_VORGABE in src/frontend-box/src/app/spotify-auth.ts). Wer https://<box>:8443/spotify ins Dashboard eintraegt, traegt etwas ein, das nie mehr geschickt wird.\nDER WEG HEUTE: die Einrichtungsseite ueber http://<box>:8200/spotify oeffnen (kein Zeugnis, keine Warnung — die Seite ist gar kein Rueckweg mehr), im Spotify-Dashboard http://127.0.0.1:8200/spotify eintragen, anmelden, und die Adresse der Fehlerseite unter 127.0.0.1 im SELBEN Tab in das Feld auf der Seite einfuegen. Siehe [[spotify-rueckweg-haengt-an-der-adresse]].\nDER HTTPS-MODUS SELBST BLEIBT (Port 8443, selbstsigniertes Zeugnis) — er wird fuer die Spotify-Einrichtung nur nicht mehr GEBRAUCHT.\n\nWAS AB HIER STEHT, IST GESCHICHTE:\nDas Frontend baut die Redirect-URI als `${window.location.origin}/spotify` und BLOCKT selbst\
      \ jede unsichere Herkunft:\n    insecureRedirect(){ const h=location.hostname;\n      return location.protocol==='http:'\
      \ && h!=='127.0.0.1' && h!=='[::1]' }\nÜber http://<hostname>/ (der PHP-Admin auf Port 80) ist die\
      \ Einrichtung deshalb UNMÖGLICH — genau nach Spotifys Regel: erlaubt sind nur https ODER die Loopback-IP,\
      \ der Name 'localhost' reicht NICHT.\n\nDafür hat das Backend einen eigenen Modus 'HTTPS (remote\
      \ setup)': es erzeugt beim Start ein selbstsigniertes Zertifikat in /etc/mupibox/tls (SAN: localhost,\
      \ <hostname>, 127.0.0.1, <LAN-IP>) und lauscht zusätzlich auf Port 8443. HTTP:8200 läuft unverändert\
      \ weiter — der Kiosk braucht es.\n\nVORAUSSETZUNG: /etc/mupibox muss dem Backend-Nutzer gehören,\
      \ sonst scheitert die Zertifikatserzeugung still mit EACCES und es bleibt bei HTTP (siehe [etc-mupibox-gehoert-dem-backend]).\n\
      \nAblauf:\n  1. https://<box>:8443/ im Browser öffnen, die Warnung zum selbstsignierten Zertifikat\
      \ einmalig annehmen.\n  2. Auf developer.spotify.com/dashboard eine App anlegen.\n  3. Als Redirect-URI\
      \ EXAKT eintragen: https://<box>:8443/spotify — sinnvollerweise zusätzlich die Variante mit der\
      \ LAN-IP, falls der Name nicht überall auflöst.\n  4. Client-ID in die MuPiBox-Einrichtung übernehmen\
      \ (der Fork nutzt PKCE, es wird KEIN Client-Secret gebraucht) und den Spotify-Login durchlaufen.\n\
      \nAuf der Box selbst geht es NICHT: der Kiosk läuft auf http://localhost:8200, und 'localhost' fällt\
      \ bei Spotify wie im Fork durch. Die Einrichtung ist bewusst ein Fernzugriff — daher der Name des\
      \ Modus. Ein Wechsel der Kiosk-Adresse auf http://127.0.0.1:8200 wäre denkbar, verschiebt aber die\
      \ Herkunft und damit localStorage/Login (Login gilt pro Origin)."
    source: 'am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07): nach dem Rechte-Fix meldete das Backend
      ''Auto-generated self-signed TLS cert (SAN: DNS:localhost,DNS:mupibox,IP:127.0.0.1,IP:192.168.178.102)''
      und ''HTTPS (remote setup) started at https://localhost:8443''; https://mupibox:8443/spotify antwortete
      mit 200. Redirect-Regel: https://developer.spotify.com/documentation/web-api/concepts/redirect_uri'

  - id: frontend-api-port-hart-verdrahtet
    kind: signature
    match: hostname\}:8200/api|saveToBackend|Spotify-Token werden nicht gespeichert|nicht https
    title: Spotify-Login meldet Erfolg, Token landen nie auf der Box (API-Port fest auf 8200)
    symptom: Der Spotify-Login auf der HTTPS-Einrichtungsseite (:8443) laeuft durch, aber /etc/mupibox/mupiboxconfig.json
      bleibt leer (clientId/refreshToken/accessToken). Im Backend-Log steht dazu NICHTS — es kommt gar
      keine Anfrage an.
    cause: 'Das gebaute Frontend baut seine API-Adresse als `${window.location.protocol}//${window.location.hostname}:8200/api`
      — das PROTOKOLL kommt von der Seite, der PORT ist fest. Auf der HTTPS-Seite ergibt das https://<host>:8200,
      und 8200 spricht nur HTTP: der TLS-Handschlag scheitert. `saveToBackend()` faengt das mit `catch{return
      false}` ab, ohne es zu melden — der Nutzer sieht ''verbunden'', gespeichert wurde nichts.'
    fix: "Im ausgelieferten Bundle (www/chunk-*.js) den festen Port durch den der Seite ersetzen:\n  `${window.location.protocol}//${window.location.hostname}:8200/api`\n\
      \  → `${window.location.protocol}//${window.location.hostname}:${window.location.port===\"8443\"\
      ?\"8443\":\"8200\"}/api`\n(dasselbe fuer /player). Bedingt statt location.host, damit der Kiosk\
      \ auf http://localhost:8200 unveraendert weiterlaeuft. Im Rezept erledigt das der Schritt 'api-port-fix'\
      \ (mupibox-app.yaml), idempotent und mit Sicherung .vor-port-fix.\nDauerhaft gehoert die Korrektur\
      \ in die Frontend-QUELLE des Forks.\nNEBENBEI: der Link im PHP-Admin zeigt auf Port 80 (http) —\
      \ darueber ist die Einrichtung prinzipiell unmoeglich, die Adresse https://<box>:8443/spotify muss\
      \ man von Hand eintippen."
    source: 'am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07): https://mupibox:8200/api/spotify/config
      -> 000, https://mupibox:8443/api/spotify/config -> 200; nach dem Bundle-Fix speicherte der Login.'

  - id: leere-bibliothek-ohne-check-internet-dienst
    kind: signature
    match: active_data\.json|keine Alben|Bibliothek leer|mupi_check_internet|check_network\.py.*requests
    title: Oberfläche zeigt keine Alben, obwohl welche eingetragen sind (active_data.json bleibt leer)
    symptom: Im Admin angelegte Alben erscheinen nicht. data.json ist gefüllt (z.B. 3 kB, 7 Einträge),
      active_data.json aber nur '[]'. /api/data liefert eine leere Liste.
    cause: 'Die Oberfläche liest NICHT data.json, sondern active_data.json — die gefilterte ''gerade erreichbare''
      Fassung. Gespiegelt wird sie von /usr/local/bin/mupibox/check_network.sh, einer DAUERSCHLEIFE (sleep
      10), die als Dienst laufen muss. Der Fork liefert dafür config/services/mupi_check_internet.service
      — aber wer die App von Hand aufsetzt, installiert die 21 Dienstdateien leicht gar nicht, und dann
      spiegelt niemand. ZWEITE Ursache: check_network.sh ruft check_network.py, das `import requests`
      braucht; fehlt python3-requests, stirbt es mit ImportError, bevor es etwas schreibt — die Schleife
      läuft dann zwar, bewirkt aber nichts.'
    fix: "  apt-get install -y python3-requests\n  install -m 644 <fork>/config/services/*.service /etc/systemd/system/\n\
      \  systemctl daemon-reload\n  systemctl enable --now mupi_check_internet\nNach ~10 s hat active_data.json\
      \ die Größe von data.json.\nNICHT blind alle einschalten: mupi_idle_shutdown fährt die Box herunter,\
      \ mupi_hat/mupi_fan brauchen MuPiHAT-Hardware, librespot/spotifyd sind bei Web-SDK-Wiedergabe ungenutzt,\
      \ und pulseaudio im System-Modus gefährdet eine laufende Tonausgabe. Sinnvoll sind mupi_check_internet,\
      \ mupi_check_monitor (füllt monitor.json) und mupi_change_checker. Im Rezept erledigt das der Schritt\
      \ 'systemd-dienste' (mupibox-app.yaml)."
    source: 'am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07): data.json 3047 B / 7 Einträge, active_data.json
      3 B; nach Dienststart 3047 B und /api/data lieferte alle 7 Alben.'

  - id: netzstatus-wolke-durchgestrichen
    kind: signature
    match: /api/network|network\.json.*404|durchgestrichene Wolke|File Not Found.*network
    title: Wolkensymbol bleibt durchgestrichen, obwohl die Box online ist
    symptom: 'Die Oberfläche zeigt dauerhaft „kein Internet“ (durchgestrichene Wolke), während Alben laden
      und die Box nachweislich online ist. /api/network antwortet mit 404 und „File Not Found: ./server/config/network.json“.'
    cause: 'Schreiber und Leser meinen verschiedene Dateien. ALLE Skripte des Forks (check_network.sh,
      get_network.sh) setzen NETWORKCONFIG=/tmp/network.json; das Backend liest aber server/config/network.json.
      Die Datei dort entsteht nie, also 404 und die Oberfläche nimmt „offline“ an. Der Netzzustand SELBST
      ist korrekt: /tmp/network.json enthält {"onlinestate":"online"} und check_network.py (requests.get
      auf google.com) liefert sauber.'
    fix: "Die beiden Stellen verbinden:\n  C=~dietpi/.mupibox/Sonos-Kids-Controller-master/server/config\n\
      \  ln -sfn /tmp/network.json \"$C/network.json\"\n  chown -h dietpi:dietpi \"$C/network.json\"\n\
      Danach liefert /api/network {\"onlinestate\":\"online\"} mit 200.\nDer Link darf ins flüchtige /tmp\
      \ zeigen: check_network.sh schreibt die Datei innerhalb von ~10 s nach dem Start neu (der Link hängt\
      \ so lange kurz in der Luft, existsSync meldet dann false -> 404, danach ist es still). Im Rezept\
      \ erledigt das der Schritt 'konfig' (mupibox-app.yaml).\n\nACHTUNG - DIESER RAT WAR FALSCH und ist am Geraet widerlegt worden: NICHT verknuepfen. Die Fork-Skripte schreiben /tmp/network.json per `cat <<< $(jq ...) > datei` rund ein DUTZEND Mal hintereinander neu (host, ip, mac, wifi, ...), und die Datei ist dabei jedes Mal kurz LEER. Liest das Backend direkt hinein (also ueber den Link), liefert /api/network im Sekundentakt 'Unexpected end of JSON input'. Die Folge geht weit ueber ein haessliches Log hinaus: die Oberflaeche wertet /api/network aus, haelt sich fuer OFFLINE - und laedt das Spotify-SDK dann GAR NICHT ERST (loadSDKScript: 'Device is offline'). Kein SDK, keine Wiedergabe. Siehe [wiedergabe-startet-nicht-weil-app-sich-fuer-offline-haelt]. RICHTIG ist ein eigener Abgleich, der nur VOLLSTAENDIGES JSON uebernimmt und es per mv unteilbar einsetzt (remote-step-installer/tools/mupibox-network-sync.sh im remote-step-installer, eingerichtet vom Schritt 'systemd-dienste')."
    source: 'am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07): vor dem Link 404 „File Not Found: ./server/config/network.json“,
      danach 200 mit onlinestate online.'
    related: [wolke-offline-weil-ip-feld-fehlt, wiedergabe-startet-nicht-weil-app-sich-fuer-offline-haelt]
    body: |
      DIESER EINTRAG ALLEIN MACHT DIE WOLKE NICHT WIEDER GANZ. Er beseitigt den
      404 — das ist der erste von ZWEI Schritten. Antwortet /api/network danach
      mit 200 und {"onlinestate":"online"} und bleibt die Wolke durchgestrichen,
      dann fehlt der Antwort das `ip`-Feld, nach dem das Frontend filtert:
      weiter bei [wolke-offline-weil-ip-feld-fehlt]. Wer nur hier liest, haelt
      einen halben Weg fuer den ganzen und sucht den Rest an der falschen Stelle.

  - id: wolke-offline-weil-ip-feld-fehlt
    kind: signature
    match: e\.ip !== void 0|ip !== undefined|durchgestrichene Wolke|get_network\.sh.*auskommentiert
    title: 'Wolke bleibt durchgestrichen: das Frontend verlangt ein ip-Feld, das niemand schreibt'
    symptom: Auch wenn /api/network mit 200 und {"onlinestate":"online"} antwortet, zeigt die Oberfläche
      weiter „kein Internet“.
    cause: 'Das Frontend filtert die Antwort: `isOnline(){ network$.pipe(filter(e => e.ip !== undefined),
      map(e => e.onlinestate === ''online'')) }`. Ohne ip-Feld kommt NICHTS durch und das Symbol bleibt
      im Grundzustand offline. Das vollständige Objekt (host, ip, mac, wifi, gateway, dns, subnet) schreibt
      ausschließlich get_network.sh — und dessen Aufruf ist im Fork AUSKOMMENTIERT (scripts/mupibox/mupi_startup.sh:
      ''#sudo /usr/local/bin/mupibox/./get_network.sh''). Es läuft also NIE, auch nicht mit aktiviertem
      mupi_startstop.service. check_network.sh setzt per jq nur .onlinestate.'
    fix: "Eigenen Timer anlegen, statt fremde Skripte zu ändern (überlebt ein Fork-Update):\n  /etc/systemd/system/mupi-network-info.service\
      \  (Type=oneshot,\n     ExecStart=/usr/local/bin/mupibox/get_network.sh, After=network-online.target)\n\
      \  /etc/systemd/system/mupi-network-info.timer    (OnBootSec=25, OnUnitActiveSec=120)\n  systemctl\
      \ enable --now mupi-network-info.timer\nDer Timer muss wiederholen: /tmp/network.json ist nach jedem\
      \ Start weg, und check_network.sh legt sie ohne ip-Feld neu an.\nAusserdem wireless-tools installieren\
      \ — ohne iwconfig bleiben wifisignal/wifilink leer.\nIm Rezept: Schritt 'systemd-dienste' (mupibox-app.yaml),\
      \ Paket in 'deps' (mupibox.yaml)."
    source: 'am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07): /api/network lieferte erst nur onlinestate,
      nach dem Timer host/ip/mac/wifi/wifisignal -62 dBm/wifilink 68 %.'
    related: [netzstatus-wolke-durchgestrichen]
    body: |
      DER SCHRITT DAVOR: antwortet /api/network ueberhaupt nicht mit 200, sondern
      mit 404 („File Not Found: ./server/config/network.json“), dann greift dieser
      Eintrag noch gar nicht — erst [netzstatus-wolke-durchgestrichen] lesen. Die
      beiden Ursachen liegen hintereinander auf demselben Symptom, nicht nebeneinander.

  - id: kiosk-schwarz-weil-x-hdmi-bevorzugt
    kind: signature
    match: xrandr.*primary|DSI.*disabled|Panel schwarz|Touch reagiert nicht
    title: Kiosk startet, Panel bleibt schwarz — X macht den NICHT angeschlossenen HDMI zum Hauptschirm
    symptom: 'Chromium läuft (Prozesse vorhanden), das DSI-Panel bleibt schwarz. `xrandr --query` zeigt
      HDMI-1 als ''primary'' mit 1024x768, obwohl nichts angesteckt ist, und den DSI-Ausgang als disabled.
      FOLGEFEHLER: sobald das Bild da ist, reagiert der Touchscreen nicht — er bleibt auf den abgeschalteten
      HDMI-Ausgang abgebildet.'
    cause: 'X wählt beim Start selbst einen Hauptausgang und bevorzugt HDMI, auch wenn dort nichts hängt.
      Der DSI-Ausgang bleibt aus. Die Touch-Zuordnung folgt der Ausgabe: das Touchgerät zeigt weiterhin
      auf den Ausgang, dem X es zugewiesen hat.'
    fix: "Nach dem Start von X den DSI-Ausgang erzwingen UND den Touch darauf abbilden:\n  DSI=$(xrandr\
      \ --query | awk '/ connected/ && /DSI/ {print $1; exit}')\n  xrandr --output \"$DSI\" --auto --primary\n\
      \  for h in $(xrandr --query | awk '/ disconnected/ {print $1}'); do xrandr --output $h --off; done\n\
      \  for id in $(xinput list --id-only); do    # Namen mit touch/ft5x06/goodix/edt\n      xinput map-to-output\
      \ \"$id\" \"$DSI\"; done\nMuss WARTEN, bis X läuft (Schleife über ~30 s), und gehört in chromium-autostart.sh,\
      \ nicht in einen Boot-Dienst. Im Rezept: remote-step-installer/tools/force-dsi-output.sh, eingehängt vom Schritt 'kiosk'."
    source: 'am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07): erst schwarzes Panel trotz laufendem Chromium,
      nach dem xrandr-Eingriff Bild; Touch reagierte erst nach map-to-output.'
    related: [mupi-touch-ohne-interrupt]
    body: |
      'Touch reagiert nicht' STEHT IN ZWEI EINTRAEGEN — hier und in
      [mupi-touch-ohne-interrupt]. Hier ist es ein FOLGEFEHLER der Bildausgabe:
      der Touch zeigt auf den falschen Ausgang. Dort liefert der Kernel gar keine
      Ereignisse. Die eine Frage, die trennt:

        timeout 20 cat /dev/input/event* > /tmp/ev.raw   # dabei tippen
        stat -c%s /tmp/ev.raw

      Groesser als 0  -> Ereignisse kommen an, sie landen nur am falschen
                         Ausgang: DIESER Eintrag (xinput map-to-output).
      0 Byte          -> der Kernel liefert nichts, xrandr hilft nicht:
                         [mupi-touch-ohne-interrupt].
      War das Panel vorher schwarz, ist es fast immer dieser Eintrag.

  - id: zram-statt-swap-auf-der-box
    kind: note
    title: 2-GB-Box ohne Swap wird unter Chromium unerreichbar — zram statt Auslagerungsdatei
    body: "Ein Pi mit 2 GB und laufendem Chromium-Kiosk hat ohne Swap keinen Puffer: die Box wird erst\
      \ zäh und dann über Minuten gar nicht mehr erreichbar (am Gerät DREIMAL erlebt, bevor die Ursache\
      \ klar war). Eine Auslagerungsdatei auf der SD-Karte wäre die naheliegende, aber schlechte Antwort\
      \ — sie kostet Schreibzyklen und ist langsam.\n\nzram ist komprimierter Swap IM Arbeitsspeicher:\
      \ kein SD-Verschleiß, und Browser-Heap komprimiert zstd sehr gut.\n  apt-get install -y --no-install-recommends\
      \ zram-tools\n  # /etc/default/zramswap:  ALGO=zstd   PERCENT=50\n  systemctl enable --now zramswap\n\
      Ergebnis auf der 2-GB-Box: rund 1005 MB zram-Swap.\nNur einrichten, wenn noch KEIN Swap aktiv ist.\
      \ Im Rezept: Schritt 'zram' (mupibox.yaml)."
    source: am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07)

  - id: spotify-einrichten-per-ssh-tunnel
    kind: howto
    title: 'Spotify von einem ANDEREN Rechner einrichten: SSH-Tunnel auf 127.0.0.1 statt HTTPS'
    body: |
      UEBERHOLT SEIT DEM 04.08.2026 IN DER BEGRUENDUNG, NICHT IM WEG (BACKLOG
      E22/R1-R3). Der Tunnel funktioniert weiter und ist sogar der EINZIGE Weg,
      auf dem die Rueckleitung noch von allein ankommt — was hier ueber den
      GRUND stand, stimmt aber nicht mehr:

        * `insecureRedirect()` GIBT ES NICHT MEHR. Die Seite prueft ihre eigene
          Adresse nicht mehr, weil sie gar kein Rueckweg mehr ist.
        * Sie leitet die Redirect-URI NICHT mehr aus `${location.origin}` ab,
          sondern schickt die FESTE `SPOTIFY_RUECKWEG_VORGABE`
          (`http://127.0.0.1:8200/spotify`).
        * Den Knopf „Ueber 127.0.0.1 oeffnen" gibt es nicht mehr — er schickte
          vom Laptop auf DESSEN eigene Rueckschleife, also ins Leere.

      WAS BLEIBT — SPOTIFYS REGEL: als Redirect-URI nur `https://…` ODER das
      Loopback-LITERAL `http://127.0.0.1:PORT` bzw. `http://[::1]:PORT`;
      `localhost` ist ausdruecklich KEINE Ausnahme.

      DER TUNNEL, WOFUER ER HEUTE GUT IST: er macht die HERKUNFT der geoeffneten
      Seite zu echtem `127.0.0.1:8200` — derselben Herkunft, auf die Spotify
      zurueckleitet. Damit landet die Rueckleitung in DEMSELBEN Tab, in dem die
      Anmeldung begann, der PKCE-Verifier liegt dort, und der Kopierschritt aus
      E22/R2 entfaellt:
        ssh -f -N -L 8200:127.0.0.1:8200 dietpi@<box>
        # dann im Browser:  http://127.0.0.1:8200/spotify
      Im Spotify-Dashboard eintragen: http://127.0.0.1:8200/spotify — dieselbe
      Adresse wie ohne Tunnel, denn sie haengt seit R1 an nichts Wanderndem.

      OHNE TUNNEL GEHT ES GENAUSO, nur mit einem Handgriff mehr: Box ueber
      `http://<ip>:8200/spotify` oeffnen (kein Zeugnis, keine Warnung),
      anmelden, die Adresse der Fehlerseite unter 127.0.0.1 kopieren und im
      SELBEN Tab in das Feld auf der Seite einfuegen. Siehe
      [[spotify-rueckweg-haengt-an-der-adresse]].

      AUF DER BOX SELBST GEHT ES NICHT — der Grund ist heute ein anderer als
      hier stand. Nicht eine Pruefung lehnt ab, sondern die HERKUNFT: der Kiosk
      laeuft auf `http://localhost:8200`, die Rueckleitung geht auf
      `http://127.0.0.1:8200`. Dieselbe Box, aber ein anderer sessionStorage —
      also `no_pkce_state`, und am Beruehrschirm gibt es keine Adresszeile zum
      Zurueckkommen (E22/R4, lesend gemessen 04.08.2026).
    source: 'am Gerät durchgeführt (Pi 5 / DietPi Trixie, 2026-07): über den Tunnel meldete die Seite
      unsicher=false, der Login lief durch und clientId/refreshToken/accessToken landeten in /etc/mupibox/mupiboxconfig.json.
      Regel: https://developer.spotify.com/documentation/web-api/concepts/redirect_uri'

  - id: kiosk-rollbalken-und-autostart-reihenfolge
    kind: note
    title: 'Touch-Kiosk: Rollbalken abschalten — und warum Anhängen an chromium-autostart.sh nicht reicht'
    body: "Auf dem Touch-Panel erscheint im Album-Bereich ein Rollbalken. Auf einem Kiosk hat der nichts\
      \ verloren: gescrollt wird mit dem Finger, der Balken ist nur optischer Ballast. Chromium kann ihn\
      \ selbst weglassen — `--hide-scrollbars`. Das Scrollen bleibt dabei vollständig erhalten, es wird\
      \ nur nichts gezeichnet.\n\nNicht mit einem Layoutfehler verwechseln: das Dokument selbst lief NICHT\
      \ über (scrollHeight 480 = clientHeight bei 800x480, kein Element höher als der Schirm). Der Balken\
      \ gehört einem inneren Ionic-Bereich — `ion-content` scrollt in seinem SHADOW-DOM (`.inner-scroll`),\
      \ ein `querySelectorAll('*')` findet ihn deshalb nicht; man muss die Shadow-Roots mitdurchsuchen.\n\
      \nSTOLPERSTELLE bei der Umsetzung: `/var/lib/dietpi/dietpi-software/installed/chromium-autostart.sh`\
      \ startet Chromium MITTEN in der Datei (Zeile ~106 von 137). Eine Zeile ans Ende anzuhängen wirkt\
      \ daher NUR für Hintergrundbefehle (so hängt die DSI-Korrektur dort, und das ist richtig) — eine\
      \ OPTIONS-Zuweisung am Dateiende käme zu spät und bliebe folgenlos. Sie muss hinter die LETZTE `CHROMIUM_OPTS=`-Zeile:\n\
      \  L=$(grep -n '^CHROMIUM_OPTS=' \"$F\" | tail -1 | cut -d: -f1)\n  sed -i \"${L}a CHROMIUM_OPTS=\\\
      \"\\${CHROMIUM_OPTS} --hide-scrollbars\\\"\" \"$F\"\n  bash -n \"$F\"        # danach immer die\
      \ Syntax prüfen\nUnd es muss bei JEDER Installation neu geschehen: die Datei kommt aus dem Fork\
      \ und wird vom Schritt 'extras' jedes Mal überschrieben. Im Rezept: Schritt 'kiosk' (mupibox-app.yaml)."
    source: 'am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07): im laufenden Chromium war --hide-scrollbars
      nach dem Eingriff aktiv; Messung im Browser ergab 0 px Überlauf des Dokuments bei 800x480.'

  - id: wiedergabeleisten-blaugrau-gegen-neutrale-flaeche
    kind: note
    title: 'Kosmetik: der Streifen unten wirkt „grau“ — die Leisten sind blaugrau, die Fläche neutral'
    body: "In der Album-Ansicht ist alles bis zum Mini-Player dunkel, der Streifen darunter aber sichtbar\
      \ anders. Gemessen im Bildschirmfoto: Fläche #111111, Streifen #131324 — mit deutlichem Blaustich\
      \ (B=36 gegen R=G=19).\n\nUrsache ist kein Fehler, sondern eine Absicht im Fork:\n  .npb-status\
      \ { background: rgba(18, 24, 32, .85); backdrop-filter: blur(8px) }\n  .npb        { background:\
      \ rgba(18, 24, 32, .78); backdrop-filter: blur(10px) }\n`rgb(18,24,32)` ist blaugrau, die Seite\
      \ dahinter aber neutral #121212 (Ionics Dunkelpalette: --ion-background-color #121212, --ion-toolbar-background\
      \ #1f1f1f, --ion-item-background #1e1e1e — schon von Haus aus DREI verschiedene Töne). Der backdrop-filter\
      \ nimmt zusätzlich Farbe von dahinterliegenden Covern auf, weshalb die Farbe in KEINER Regel als\
      \ Literal steht und eine Textsuche ins Leere läuft.\n\nAngleichen (Milchglas bleibt erhalten), ans\
      \ Ende von www/styles.css:\n  .npb, .npb-status, .npb-fav-fab { background: rgba(18,18,18,.85) !important;\
      \ }\nMuss nach JEDER App-Installation neu gesetzt werden: deploy.zip bringt styles.css jedes Mal\
      \ frisch mit. Im Rezept: Schritt 'leisten-ton' (mupibox-app.yaml), optional — wer den Fork-Look\
      \ will, lässt ihn weg.\
      \ \n\nNACHTRAG 2026-07: der Wert steht jetzt in der QUELLE\
      \ (now-playing-bar.component.ts, alle drei Leisten auf 0.85 statt 0.78) und ueberlebt damit\
      \ jeden Neubau. Der Flicken in die fertige, GEHASHTE styles-*.css war fragil: ein sauberer\
      \ Neubau von www entfernte ihn wortlos, und der blaeuliche Untergrund schimmerte wieder\
      \ durch. Der Rezept-Schritt ist damit ueberfluessig — er setzt dieselbe Regel nur nochmal\
      \ mit !important."
    source: 'am Gerät gemessen (Pi 5 / DietPi Trixie, 2026-07): Pixelwerte aus einem scrot-Foto, Regeln
      aus dem laufenden Kiosk über das DevTools-Protokoll gelesen; danach berechnete .npb-status rgba(18,18,18,0.85)
      = identisch zum body.'

  - id: kiosk-live-untersuchen-devtools
    kind: howto
    title: 'Den LAUFENDEN Kiosk untersuchen: Bildschirmfoto + DevTools-Protokoll'
    body: "Für optische Fragen („welches Element ist das?“, „woher kommt die Farbe?“) reicht Raten nicht,\
      \ und die App im eigenen Browser nachzustellen scheitert oft (ohne DRM bricht das Spotify-SDK ab,\
      \ die Kachelliste bleibt leer). Zwei Wege führen direkt ans laufende Bild:\n\n1. BILDSCHIRMFOTO\
      \ — beweist, WAS zu sehen ist, und erlaubt exakte Farbmessung:\n     apt-get install -y --no-install-recommends\
      \ scrot\n     su dietpi -c 'DISPLAY=:0 scrot -o /tmp/schirm.png'\n     ssh dietpi@<box> 'cat /tmp/schirm.png'\
      \ > kiosk.png\n   Danach Pixel auslesen (PNG ist verlustfrei) statt nach Augenmaß zu urteilen.\n\
      \n2. DEVTOOLS-PROTOKOLL — beantwortet, WARUM: welches Element malt einen Punkt, welche Regel gilt,\
      \ was ist berechnet. Vorübergehend einschalten:\n     # hinter die letzte CHROMIUM_OPTS-Zeile in\
      \ chromium-autostart.sh:\n     CHROMIUM_OPTS=\"${CHROMIUM_OPTS} --remote-debugging-port=9222 --remote-allow-origins=*\"\
      \n     systemctl restart getty@tty1\n     ssh -f -N -L 9222:127.0.0.1:9222 dietpi@<box>\n     curl\
      \ -s http://127.0.0.1:9222/json/list      # webSocketDebuggerUrl der Seite\n   Auswerten per Runtime.evaluate.\
      \ Nützlich sind `document.elementsFromPoint(x,y)` (wer malt hier?) und ein Durchlauf über `document.styleSheets`\
      \ (welche Regel greift?).\n\nWICHTIG: den Debug-Port danach WIEDER ENTFERNEN. Er erlaubt jedem lokalen\
      \ Prozess, den Browser fernzusteuern — auf einer Kiosk-Box nichts, was dauerhaft offen bleiben sollte:\n\
      \     sed -i '/remote-debugging-port/d' <autostart> && systemctl restart getty@tty1\n     ss -ltn\
      \ | grep 9222      # muss leer sein"
    source: am Gerät angewandt (Pi 5 / DietPi Trixie, 2026-07), um die Herkunft des blaugrauen Streifens
      zu klären — Textsuche in CSS/JS hatte nichts gefunden, weil die Farbe erst durch backdrop-filter
      entsteht.

  - id: keine-cover-in-der-album-ansicht
    kind: signature
    match: items\(track\(id,uri,name\)\)|albumCover|keine Cover|nur 10 Titel
    title: Album-/Flip-Ansicht ohne Cover — und nach 10 Titeln ist Schluss
    symptom: Auf der Startseite haben die Kacheln Bilder, beim Öffnen eines Albums im Player fehlen sie.
      Ebenso fehlen Albumname und Interpret je Titel, und eine Playlist endet nach 10 Einträgen.
    cause: "Das Backend fordert bei Spotify ausdrücklich nur drei Felder an:\n  getPlaylistItems(id, \"\
      DE\", \"items(track(id,uri,name))\", Math.min(limit,10), offset)\nDie Oberfläche liest aber genau\
      \ das, was dabei wegfällt:\n  albumCover: track.album?.images?.[0]?.url || \"\"\n  albumId / albumName\
      \ / albumTotal (album.total_tracks) / albumArtist (artists[0].name)\n`track.album` ist also undefiniert\
      \ — es fehlt nicht nur das Bild, sondern die gesamte Album-Information. Die Kacheln auf der Startseite\
      \ sind davon NICHT betroffen: deren cover-URL steht fest in data.json, weshalb der Fehler leicht\
      \ zu übersehen ist.\nUnabhängig davon deckelt `Math.min(limit,10)` die Titelliste, während die Oberfläche\
      \ in 50er-Schritten blättert (`if (c.length < 50) break`) — sie hält die kurze Antwort für das Ende\
      \ und bricht nach 10 Titeln ab."
    fix: "In server.js (gebündelt) beide Stellen ersetzen:\n  \"items(track(id,uri,name))\"\n  → \"items(track(id,uri,name,artists(name),album(id,name,images,total_tracks)))\"\
      \n  Math.min(a,10)  →  Math.min(a,50)      (nur in genau diesem Aufruf)\n  pm2 restart server\n\
      PRÜFEN mit umgangenem Zwischenspeicher — der Schlüssel ist `playlist_tracks_<id>_<limit>_<offset>`,\
      \ ein unveränderter Aufruf liefert sonst die ALTE Antwort und man hält den Fix für wirkungslos (genau\
      \ so passiert):\n  curl '.../api/spotify/playlist/<id>/tracks?limit=7'\nErwartet: Felder album/artists\
      \ vorhanden, album.images nicht leer.\nDauerhaft gehört das in die Backend-QUELLE des Forks; im\
      \ Rezept erledigt es der Schritt 'cover-fix' (mupibox-app.yaml), da server.js aus deploy.zip stammt.\nQUELLE (die eigentliche Behebung): src/backend-api/src/services/spotify-api.service.ts, Methode getPlaylistTracks - dort stehen Feldliste und Math.min(limit,10). Das Frontend fragt in spotify-auth.service.ts (getPlaylistDecomposition) laengst die vollstaendige Liste ab; die beiden Stellen waren auseinandergelaufen.\nBAUEN: der Fork ist ein npm-Workspace (workspaces src/*); npm run build im backend-api buendelt per esbuild nach src/deploy/server.js, src/deploy.sh packt daraus bin/nodejs/deploy.zip. AUSSERHALB des Workspace zu bauen loest ANDERE Abhaengigkeits-Versionen auf und erzeugt ein komplett anderes Buendel (+5 kB, andere Minifizierungs-Namen) - man taeuscht sich dann ueber den Umfang der Aenderung. Im Workspace bauen.\nEin Buendel-Patch darf sich NICHT auf minifizierte Variablennamen stuetzen (Math.min(a,10)): nach einem Neubau heissen sie anders und die Korrektur greift still nur halb. Die Feldliste ist dagegen ein STRING-Literal und ueberlebt die Minifizierung unveraendert."
    source: 'am Gerät belegt (Pi 5 / DietPi Trixie, 2026-07): vorher Felder nur [id,name,uri] und 10 Titel,
      danach [album,artists,id,name,uri] mit 3 Bildern und 12 Titeln.'

  - id: wiedergabe-startet-nicht-weil-app-sich-fuer-offline-haelt
    kind: signature
    match: Device is offline|player health check failed|Cleaning up player due to token/auth|Unexpected
      end of JSON input
    title: 'Titel startet nicht: die App haelt sich fuer offline und laedt das Spotify-SDK gar nicht'
    symptom: 'Alben und Cover werden angezeigt, aber kein Titel startet. Im Log: ''Spotify player health
      check failed - cannot start playback'', ''[Spotify SDK] Cleaning up player due to token/auth failure'',
      ''ensurePlayerReady() result: false state: ready''. Im Kiosk-Browser ist window.Spotify UNDEFINIERT
      und es haengt kein sdk.scdn.co-Skript in der Seite — auch nach einem Neuladen nicht.'
    cause: "Der SDK-Lader hat eine Vorbedingung:\n  loadSDKScript(){ if (!this.isOnline) { reject('Device\
      \ is offline'); return } ... }\n`isOnline` kommt aus /api/network. Liefert diese Route auch nur\
      \ zeitweise Fehler, gilt die Box als offline und das SDK wird NIE geladen — die Wiedergabe ist tot,\
      \ obwohl Token, Widevine und Netz einwandfrei sind (alles einzeln geprueft: refresh_token liefert\
      \ bei Spotify anstandslos einen frischen Zugriffstoken, requestMediaKeySystemAccess('com.widevine.alpha')\
      \ ist verfuegbar, isSecureContext true).\nDie Fehlerquelle war eine flatternde Datei: die Fork-Skripte\
      \ schreiben /tmp/network.json per `cat <<< $(jq ...) > datei` ein DUTZEND Mal hintereinander neu\
      \ und sie ist dabei jedes Mal kurz leer. Wer das Backend per Verknuepfung direkt dort hineinlesen\
      \ laesst, erntet im Sekundentakt 'Unexpected end of JSON input'.\nZweiter Teil der Falle: ist die\
      \ Datei einmal leer, scheitert JEDES weitere jq und schreibt wieder Leere — der Defekt haelt sich\
      \ selbst am Leben. Der Fork legt sie nur an, wenn sie FEHLT; in get_network.sh fehlt an der Stelle\
      \ sogar das Umlenkungszeichen (`sudo echo -n \"[]\" ${NETWORKCONFIG}` schreibt ins Leere)."
    fix: "Das Backend NICHT in die flatternde Datei lesen lassen, sondern atomar abgleichen — und die\
      \ Quelldatei heilen, falls sie kaputt ist:\n  jq -e . /tmp/network.json >/dev/null 2>&1 || printf\
      \ '{}' > /tmp/network.json\n  /usr/local/bin/mupibox/get_network.sh\n  jq -e . /tmp/network.json\
      \ >/dev/null || exit 0     # unvollstaendig -> alte behalten\n  install -m644 -o dietpi -g dietpi\
      \ /tmp/network.json <config>/network.json.neu\n  mv -f <config>/network.json.neu <config>/network.json\n\
      Per Timer alle ~20 s. Fertig als remote-step-installer/tools/mupibox-network-sync.sh im remote-step-installer, eingerichtet\
      \ vom Schritt 'systemd-dienste'.\nPRUEFEN, in dieser Reihenfolge — jeder Schritt schliesst eine\
      \ Ursache aus:\n  1. /api/network mehrfach abfragen: muss IMMER 200 sein, mit ip UND onlinestate.\n\
      \  2. Im Kiosk (DevTools-Port, siehe [kiosk-live-untersuchen-devtools]):\n     typeof window.Spotify\
      \  -> muss 'object' sein, nicht 'undefined'.\n  3. Erst dann Token/Widevine verdaechtigen.\nNach\
      \ der Reparatur laedt das SDK beim naechsten Seitenaufbau von selbst."
    source: 'am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07): window.Spotify war ''undefined'' und 0
      SDK-Skripte in der Seite; nach dem atomaren Abgleich ''object'' mit 1 Skript. Die Ursache war eine
      ZUVOR von uns selbst eingebaute Verknuepfung — siehe die Korrektur in [netzstatus-wolke-durchgestrichen].'

  - id: spotify-sprungziel-um-eins-verschoben
    kind: signature
    match: resumeOffset--|spotify:album:.*:\d+:0|Kapitel 2 startet Kapitel 1|falscher Titel
    title: Titelauswahl startet den Titel DAVOR (Kapitel 2 spielt Kapitel 1)
    symptom: In der Titelliste einen Titel antippen startet den vorherigen. Bei Titel 1 faellt es nicht
      auf - dort ist es zufaellig richtig.
    cause: "Der Player-Dienst zieht vom uebergebenen Wert NOCH eins ab:\n  let resumeOffset = activeSpotifyId.split(':')[3]\n\
      \  if (resumeOffset > 0) resumeOffset--        // spotify-control, playMe()\n  offset: { position:\
      \ resumeOffset }\nEr erwartet also eine 1-BASIERTE Titelnummer, bei der 0 'von vorn' bedeutet -\
      \ ein Ueberbleibsel der Fortsetzen-Funktion, die resumespotifytrack_number genau so ablegt. Das\
      \ Frontend schickte in playSpotifyAlbumOffset/playSpotifyPlaylistOffset dagegen den 0-basierten\
      \ Offset (t.nr - 1), also einen zu wenig. Bei 0 greift der Abzug nicht, deshalb war Titel 1 unauffaellig."
    fix: "Die beiden Sprung-Methoden schicken +1, die Aufrufer bleiben 0-basiert:\n  `spotify/now/spotify:album:<id>:${Math.max(0,\
      \ Math.round(offset)) + 1}:0`\n  `spotify/now/spotify:playlist:<id>:${Math.max(0, Math.round(offset))\
      \ + 1}:0`\nGegenprobe an allen Aufrufern: der Fortsetzen-Pfad schickt bereits 1-basiert (resumespotifytrack_number),\
      \ der Frischstart schickt 0 - beide bleiben damit richtig.\nPRUEFEN: nicht raten, sondern messen\
      \ - im Kiosk mitschneiden, welche Nummer getippt wurde, und 2 s spaeter /me/player/currently-playing\
      \ abfragen (item.track_number)."
    source: 'am Geraet gemessen (Pi 5 / DietPi Trixie, 2026-07): getippt Kapitel 2 -> lief Kapitel 1 (zweimal),
      getippt Kapitel 1 -> lief Kapitel 1.'

  - id: cover-flip-ohne-bilder-und-tote-flips
    kind: signature
    match: nocover_mupi|trackCoverUrl|flippable|cover-flip|mupibox_media_cache
    title: Titelliste zeigt MuPi-Platzhalter, und nach der Titelwahl geht der Flip nicht mehr auf
    symptom: 'Drei Erscheinungen mit einer gemeinsamen Wurzel: (a) jede Zeile der Flip-Liste zeigt den
      MuPiBox-Platzhalter statt des Album-Bildes, (b) manche Flips zeigen Bilder, andere nicht, (c) nach
      dem Antippen eines Titels laesst sich der Flip im Player gar nicht mehr oeffnen.'
    cause: '(a) Der Anzeigetyp hat ein Feld `cover` und trackCoverUrl() bevorzugt es - aber getPlaylistTracks/getAlbumTracks
      lieferten nur nr/name/display. Ohne cover faellt die Liste auf den LOKALEN trackcover-Endpunkt zurueck,
      den es fuer Spotify-Inhalte nicht gibt. Dasselbe beim Mixtape: PlaylistTrackInfo trug albumCover,
      die Zerlegung warf es weg.

      (b) Der Unterschied kam aus dem Browser-Zwischenspeicher (localStorage mupibox_media_cache_v1):
      Eintraege aus der Zeit VOR der Korrektur haben kein cover und werden weiter ausgeliefert.

      (c) Der Flip oeffnet nur, wenn `media.id` ODER `media.playlistid` vorhanden ist. playTrack navigierte
      zum Player OHNE Zustand; der baut sein Medium dann aus dem laufenden Titel - ohne beide Felder.
      Frueher fiel das kaum auf, weil man dorthin nur ueber das kleine Play-Symbol kam.'
    fix: "(a) In getPlaylistTracks `fields=items(track(name,album(images)))` anfordern und je Titel cover\
      \ setzen; getAlbumTracks holt das Albumbild einmal und haengt es an jeden Titel; MixtapeTrack bekommt\
      \ ein optionales cover, die Zerlegung reicht albumCover durch.\n(b) Beim Aendern der zwischengespeicherten\
      \ FORM die Version hochziehen: STORE_KEY 'mupibox_media_cache_v1' -> '_v2'. Sonst wirkt die Korrektur\
      \ wie wirkungslos, und Handarbeit am Browser hilft nur EINEM Geraet.\n(c) Das Medium mitgeben, aber\
      \ den Neustart unterbinden:\n    router.navigate(['/player'], { state: { media, alreadyPlaying:\
      \ true } })\n  und im Player `isExternalPlayback = !!navState.alreadyPlaying`. Ohne das Kennzeichen\
      \ startet der Player das Medium neu und landet wieder bei Titel 1.\nMESSEN statt raten: der Klick\
      \ auf das Cover kam an, `flipped` blieb false und es gab KEINEN Fehler - ein stiller frueher Ausstieg\
      \ an der flippable-Bedingung."
    source: am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07)

  - id: vorlesen-ohne-stimme-bleibt-still
    kind: signature
    match: speechSynthesis|speech-dispatcher|espeak|vorlesen|kein Ton beim langen Druck
    title: Vorlesen (langer Druck) bleibt still - speechSynthesis meldet sich trotzdem als vorhanden
    symptom: Langer Druck auf einen Titel blinkt zwar, es kommt aber kein Ton. Auch der Rueckfall auf
      die Box-Ausgabe greift nicht.
    cause: Die Pruefung lautet `if ('speechSynthesis' in window) { ... return }`. Die Schnittstelle EXISTIERT
      in Chromium immer, auch ohne installierte Stimme - der Code haelt sie fuer verfuegbar, kehrt zurueck
      und der Rueckfall (Box-TTS ueber mplayer) wird nie erreicht. Auf einem nackten DietPi ist KEIN Sprachdienst
      installiert.
    fix: '  apt-get install -y --no-install-recommends speech-dispatcher espeak-ng

      Danach hat espeak-ng deutsche Stimmen (Pruefen: `espeak-ng --voices=de | wc -l`, spd-say ist vorhanden).
      Im Rezept in den Schritt ''deps'' aufgenommen.

      Robuster waere zusaetzlich, auf getVoices().length zu pruefen und sonst den Rueckfall zu nehmen
      - sonst bleibt es auf jeder Installation ohne Stimme still.

      HINWEIS zur Bedienung: kurzer Klick spielt den Titel, langer Druck (~500 ms) liest vor. Im Fork
      war es umgekehrt belegt (Klick las vor, gespielt wurde nur ueber das kleine Symbol).'
    source: 'am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07): weder speech-dispatcher noch espeak installiert;
      nach der Installation 9 deutsche Stimmen.'

  - id: fork-bauen-ohne-sudo-ausgabe-umlenken
    kind: howto
    title: Den Fork bauen, wenn Verzeichnisse root gehoeren - Ausgabe umlenken statt Rechte aendern
    body: "Nach einem frueheren `sudo npm install` gehoeren im Fork mehrere Pfade root: node_modules (alle\
      \ Ebenen), src/deploy, src/frontend-box/.angular und die esbuild-Metadatei. `npm run build` scheitert\
      \ dann NICHT am Bauen, sondern am WEGSCHREIBEN ('Failed to write to output file ... Permission denied')\
      \ - und deploy.sh packt anschliessend die ALTEN Dateien zu einem scheinbar neuen deploy.zip zusammen.\n\
      \nMan kann bauen, ohne sudo anzufassen: nur die Ausgabe umlenken.\n  cd src/backend-api    && npx\
      \ esbuild src/backend-api/src/server.ts --bundle --minify --platform=node \\\n                            --target=node22\
      \ --outfile=<schreibbar>/server.js\n  cd src/backend-player && npx esbuild src/backend-player/src/spotify-control.ts\
      \ --bundle --minify \\\n                            --platform=node --target=node22 --outfile=<schreibbar>/spotify-control.js\n\
      \  cd src/frontend-box   && npx ng build --configuration production --output-path=<schreibbar>/www\n\
      Danach wie deploy.sh packen: server.js + spotify-control.js + www/ (Inhalt von www/browser eine\
      \ Ebene hoch) + README.md, dann `zip -r deploy.zip .` und nach bin/nodejs/ kopieren.\n\nENTSCHEIDEND\
      \ ist, IM Workspace zu bauen (npx nimmt dessen node_modules). Ein Bau in einer isolierten Kopie\
      \ loest andere Abhaengigkeits-Versionen auf: das Backend-Buendel wuchs um 4963 Bytes und hatte durchgehend\
      \ andere Minifizierungs-Namen - man taeuscht sich dann gruendlich ueber den Umfang der eigenen Aenderung.\
      \ Im Workspace gebaut waren es +49 Bytes.\nStruktur des fertigen Pakets immer gegen die Fassung\
      \ aus git pruefen (`unzip -l` und die oberste Ebene vergleichen), bevor es auf die Box geht."
    source: 'am Geraet durchgefuehrt (2026-07): so gebaut und ueber den Rezept-Schritt ''app-paket'' ausgerollt,
      ohne Besitzrechte zu aendern.'

  - id: www-altlasten-und-gehashte-dateinamen
    kind: note
    title: 'Ausrollen loescht nichts: Altlasten im www und die Falle mit styles.css'
    body: 'Der Schritt ''app-paket'' entpackt mit `unzip -o` - das UEBERSCHREIBT, aber loescht nie. Da
      Angular die Dateinamen ueber den Inhalt hasht (chunk-XXXX.js, styles-XXXX.css), bleibt nach jedem
      Ausrollen ein Satz alter Dateien liegen: am Geraet 84 Dateien, davon 27 Leichen.


      Das ist mehr als Unordnung. Wer eine Datei nach festem Namen flickt, erwischt womoeglich eine Leiche:
      der Angleich der Leistenfarbe schrieb in www/styles.css, geladen wurde aber styles-24SYTT42.css
      - der Schritt meldete danach zufrieden ''schon angeglichen'', obwohl er wirkungslos war. Korrektur:
      ueber ALLE styles*.css laufen, nicht ueber einen festen Namen.


      Beim Aufraeumen vorsichtig sein: unter www liegen auch theme-data/ und der media-Verweis - ein pauschales
      Loeschen des Verzeichnisses waere falsch. Nur die Bau-Artefakte der obersten Ebene (chunk-*, main-*,
      polyfills-*, styles-*) kaeme in Frage. (Noch nicht umgesetzt.)'
    source: am Geraet festgestellt (2026-07)

  - id: im-kiosk-messen-statt-raten
    kind: howto
    title: 'Bedienfehler im Kiosk messen: ein Mitschnitt in der laufenden Seite'
    body: "Bei Meldungen wie 'der Flip geht nicht mehr auf' oder 'es startet der falsche Titel' fuehrt\
      \ Nachdenken ueber den Code schnell in die Irre - beide Male lag die erste Vermutung daneben. Schneller\
      \ ist ein Mitschnitt IM laufenden Kiosk (DevTools-Port, siehe [kiosk-live-untersuchen-devtools]):\n\
      \n  window.__rec = []\n  document.addEventListener('click', e => {\n    const row = e.target.closest('.tl-item');\
      \ if (!row) return\n    const eintrag = { nr: row.querySelector('.tl-nr')?.textContent,\n      \
      \                name: row.querySelector('.tl-name')?.textContent }\n    window.__rec.push(eintrag)\n\
      \    setTimeout(async () => {                       // was laeuft 2,5 s spaeter?\n      const cfg\
      \ = JSON.parse(localStorage.getItem('mupibox_spotify_cfg') || '{}')\n      const r = await fetch('https://api.spotify.com/v1/me/player/currently-playing',\n\
      \                            { headers: { Authorization: 'Bearer ' + cfg.accessToken } })\n    \
      \  const j = r.status === 200 ? await r.json() : null\n      eintrag.laeuft = j?.item?.name; eintrag.laeuft_nr\
      \ = j?.item?.track_number\n    }, 2500)\n  }, true)\n\nDer Nutzer bedient normal, danach `window.__rec`\
      \ auslesen. Damit war in einem Schritt klar: getippt Kapitel 2 -> lief Kapitel 1, also ein Versatz\
      \ um eins statt eines kaputten Klicks. Fuer Zustandsfragen zusaetzlich Klassen/Stile des betroffenen\
      \ Elements 300 ms und 1 s nach dem Klick festhalten - so zeigte sich, dass der Flip sehr wohl wieder\
      \ aufging, nur nicht innerhalb der 600-ms-Sperre.\nHINWEIS zum eigenen Werkzeug: beim Auswerten\
      \ per Protokoll KEIN Runtime.enable senden - der Konsolenstrom der App ueberdeckt sonst die Antwort."
    source: am Geraet angewandt (2026-07)

  - id: player-zeigt-cover-des-vorherigen-titels
    kind: signature
    match: liveCoverAtEntry|playbackStartedForMedia|Cover des vorherigen|current\$.*album.*images
    title: Player zeigt beim Oeffnen kurz das Cover des vorher gespielten Albums
    symptom: Ein anderes Album antippen - im Player steht erst das Bild des vorherigen, nach einigen Sekunden
      springt es um.
    cause: "Das Cover folgt dem GERADE laufenden Titel, ohne Pruefung der Zugehoerigkeit:\n  current$.subscribe(sp\
      \ => { if (media?.type==='spotify' && sp?.item?.album?.images?.[0]?.url)\n                     \
      \          this.cover = sp.item.album.images[0].url ... })\nDirekt nach dem Oeffnen meldet Spotify\
      \ noch das vorherige Stueck. Zwei Fallen dabei:\n  * Die Seite wird beim Blaettern WIEDERVERWENDET\
      \ - `cover` haelt den alten Wert, bis das erste Ereignis eintrifft (daher die Sekunden).\n  * `playMedia()`\
      \ kehrt zurueck, sobald Spotify den BEFEHL annimmt; das Geraet schaltet erst spaeter um. Eine Sperre\
      \ 'erst nach Wiedergabestart' allein reicht deshalb NICHT.\nCurrentSpotify fuehrt weder context-URI\
      \ noch Album-Kennung - ueber IDs ist keine Zuordnung moeglich."
    fix: "Am INHALT festmachen statt am Zeitpunkt:\n  private liveCoverAtEntry: string | null = null\n\
      \  // beim Eintritt: das gerade gemeldete Cover merken (= das des vorherigen Stuecks)\n  this.liveCoverAtEntry\
      \ = this.currentPlayedSpotify?.item?.album?.images?.[0]?.url || null\n  if (!this.playbackStartedForMedia)\
      \ this.cover = this.media?.cover || <platzhalter>\n  // im Abonnement:\n  const gehoertHierher =\
      \ playbackStartedForMedia && liveCover !== liveCoverAtEntry\nSolange dasselbe Bild gemeldet wird\
      \ wie beim Oeffnen, ist es per Definition das alte. Dasselbe Album erneut zu oeffnen bleibt beim\
      \ eigenen Cover - dort ist es ohnehin richtig. Bei EXTERNER Wiedergabe startet die Sperre offen:\
      \ da IST das Laufende das Gezeigte."
    source: 'am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07): erster Versuch nur mit der Zeit-Sperre
      zeigte das alte Bild weiterhin einige Sekunden; mit dem Inhalts-Vergleich stimmte das Cover sofort
      (Bildschirmfoto: Lilo & Stitch mit passendem Titel).'

  - id: boot-animation-mit-echtem-fortschritt
    kind: howto
    title: Boot-Animation mit echtem Fortschritt statt statischem Bild
    body: "Der Fork bringt nur ein STATISCHES Splash-Bild mit (mupi_splash.service -> splash_screen.sh\
      \ -> `fbv <bild aus mupibox.startSplash>`), und `fbv` ist auf einem nackten DietPi nicht einmal\
      \ installiert; die konfigurierte Datei (/boot/splash.png) fehlt dort ebenfalls. Der Dienst war bei\
      \ uns gar nicht eingerichtet.\n\nVoraussetzungen auf der Box (geprueft): /dev/fb0 mit 800x480 und\
      \ 32 bpp, rund 15 s Bootzeit (2,5 s Kernel + 13 s Userspace) - genug fuer eine sinnvolle Anzeige.\
      \ PIL ist NICHT installiert, dafuer liegen 744 Konsolenschriften unter /usr/share/consolefonts (PSF).\n\
      \nUmsetzung (remote-step-installer/tools/mupibox-boot-splash.py im remote-step-installer, Schritt 'boot-animation'):\n\
      \  * Pixel per mmap direkt nach /dev/fb0, Format BGRA (32 bpp).\n  * Schrift aus einer PSF-Konsolenschrift\
      \ - Kopf lesen, Glyphen sind Bitmuster. Damit echte Beschriftungen ohne Zusatzpaket (PSF1: Magic\
      \ 36 04, 8 px breit; PSF2: Magic 72 b5 4a 86 mit Breite/Hoehe im Kopf).\n  * FORTSCHRITT AUS MEILENSTEINEN,\
      \ nicht aus einem Timer: ip in /tmp/network.json, Port 8200 offen, mupi_check_internet aktiv, Xorg\
      \ da, Chromium da. Der Balken springt nie zurueck, und wenn ein Schritt laenger dauert, steht er\
      \ ehrlich still, statt eine Dauer vorzugaukeln.\n  * Beendet sich selbst, sobald Chromium laeuft\
      \ (ab da gehoert der Schirm dem Kiosk), sonst nach 120 s. Jeder Fehler endet still - eine Animation\
      \ darf den Start NIE aufhalten.\n\nPRUEFEN OHNE NEUSTART: getty@tty1 stoppen, das Skript kurz laufen\
      \ lassen, `dd if=/dev/fb0` abziehen und die Rohdaten in ein PNG wandeln (BGRA -> RGB, zlib, drei\
      \ PNG-Chunks). So sieht man das Ergebnis, ohne die Box neu zu starten - danach getty@tty1 wieder\
      \ starten, sonst bleibt der Kiosk aus."
    source: 'am Geraet gebaut und geprueft (Pi 5 / DietPi Trixie, 2026-07): Framebuffer-Abzug zeigte Titel,
      animierte Balken und den Fortschritt bei 4/6 - korrekt, da fuer den Test Anzeige und Oberflaeche
      fehlten.'

  - kind: signature
    id: mupi-touch-ohne-interrupt
    title: "Touchscreen reagiert nicht oder nur sehr träge (Treiber ohne Unterbrechung)"
    sev: high
    match: "reagiert nicht auf Druck|Touch.*(traege|träge|manchmal)|edt_ft5x06|generic ft5x06|keine Eingabeereignisse"
    cause: "der Kerneltreiber edt_ft5x06 bindet sauber an den Controller, bekommt aber NIE eine Unterbrechung — und er hat keinen Abfragemodus, kann also prinzipbedingt nichts liefern"
    fix: "Touch-Bruecke im Benutzerraum: den Controller per I2C abfragen und die Beruehrungen als uinput-Geraet melden (remote-step-installer, Schritt 'touch-bruecke')"
    tags: [touch, pi5, dsi, kernel, uinput, i2c, trixie]
    related: [kiosk-schwarz-weil-x-hdmi-bevorzugt]
    body: |
      VORHER AUSSCHLIESSEN: blieb das Panel vor dem Fehler SCHWARZ, ist es
      wahrscheinlich nicht dieser Eintrag, sondern
      [kiosk-schwarz-weil-x-hdmi-bevorzugt] — dort ist der tote Touch ein
      Folgefehler der Ausgabewahl (Touch auf den abgeschalteten HDMI abgebildet),
      und Schritt 1 unten liefert dann Bytes statt 0. Beide Eintraege antworten
      auf 'Touch reagiert nicht'; dieser gilt nur bei 0 Byte in Schritt 1.

      SYMPTOM war irrefuehrend: der Bildschirm wirkte "manchmal, aber sehr traege",
      was nach einer ueberlasteten Box aussieht. Die Box war es NICHT — Last 0,09,
      API-Antworten in 1,5 ms.

      SO WIRD ES SAUBER EINGEGRENZT (die Reihenfolge ist der Wert dieses Eintrags):
        1. Kommen ueberhaupt Ereignisse an?
             timeout 20 cat /dev/input/event* > /tmp/ev.raw   # dabei tippen
             stat -c%s /tmp/ev.raw     -> 0 Byte = der Kernel liefert nichts
        2. Antwortet der Controller selbst? (i2c-tools noetig, modprobe i2c-dev)
             i2cget -y -f 11 0x38 0xA3     -> Chip-ID, hier 0x54
             i2cget -y -f 11 0x38 0x02     -> Anzahl Beruehrungen, waehrend man tippt
           Kommen hier Werte != 0x00, ist die HARDWARE in Ordnung: Panel, Kabel und
           Controller arbeiten. Ergebnis bei uns: 190 Meldungen in 20 s, Koordinaten
           sauber 0..800 x 0..480, keine Spiegelung noetig.
        3. Uebertragungsfehler ausschliessen:
             cat /sys/bus/i2c/devices/11-0038/{crc_errors,header_errors}  -> beide 0
        4. Die eigentliche Ursache:
             grep -iE "ft5|0038|edt" /proc/interrupts   -> KEINE Zeile
           Ohne Interrupt-Leitung liest edt_ft5x06 den Controller nie aus.

      NICHT auf das Overlay schieben: `vc4-kms-dsi-7inch` ist fuer ein 800x480-Panel
      RICHTIG (die Waveshare-Overlays nutzen alle den Goodix-Treiber und haben gar
      keine 800x480-Variante — 5_0_inch waere 720x1280 und der Schirm bliebe schwarz).
      Auch `report_rate` ist eine Sackgasse: der generische Controller meldet dort 0
      und laesst sich nicht beschreiben.

      LOESUNG: remote-step-installer/tools/mupibox-touch-bridge.py fragt den Controller mit 60 Hz ueber
      /dev/i2c-11 ab und meldet die Beruehrungen als echtes MEHRFINGER-Geraet
      (uinput, INPUT_PROP_DIRECT, ABS_MT_*). Ein reines Zeigergeraet genuegt NICHT —
      nur mit INPUT_PROP_DIRECT erzeugt Chromium Touch-Ereignisse, sonst fehlen
      Wischen und Scrollen. Sie startet mit --auto und haelt sich RAUS, wenn der
      Kerneltreiber eine Unterbrechungsleitung hat, damit sie auf intakter Hardware
      keinen funktionierenden Treiber verdraengt.

      STOLPERSTEIN beim Bauen: struct input_event mit struct.pack("<llHHi", ...) ist
      FALSCH — mit dem "<"-Praefix ist "l" nur 4 Byte, der Kernel erwartet die native
      long-Groesse. Das Ereignis kommt 16 statt 24 Byte lang an und uinput antwortet
      mit EINVAL. Richtig ist das native Format "@llHHi".
    source: "am Geraet gemessen und behoben (Pi 5 / DietPi Trixie, 2026-07): 0 Rohbytes trotz 20 s Tippen, gleichzeitig 190 Beruehrungsmeldungen per I2C"

  - kind: signature
    id: mupi-kiosk-nutzer-ohne-gruppe-audio
    title: "Player-Dienst stirbt in Neustartschleife (Nutzer nicht in Gruppe audio)"
    sev: high
    match: "amixer.*(Master|kann nicht|cannot find)|spotify-control.*(errored|restart)|keine Soundkarte|no soundcards found"
    cause: "der Kiosk-Nutzer war nicht in der Gruppe audio und sah deshalb gar keine Soundkarte; `amixer sget Master` scheitert und reisst den Player-Dienst mit"
    fix: "usermod -aG audio,video,render dietpi — danach pm2-Dienste neu starten"
    tags: [audio, alsa, pm2, rechte, dietpi]
    body: |
      Der Fehler tarnt sich: NIRGENDWO steht "keine Berechtigung". `aplay -l` meldet
      schlicht "no soundcards found", als waere die Hardware nicht da — obwohl das
      Overlay geladen und die Karte vorhanden ist.

      FALLE BEIM MESSEN (mir selbst passiert): per SSH als dietpi gemessen sieht man
      genau dieses Bild und schliesst auf ein Hardware- oder Overlay-Problem. Als root
      gemessen ist die Karte sofort da. Bei Audio- und dmesg-Fragen also IMMER
      dazusagen, ALS WER gemessen wurde — `kernel.dmesg_restrict=1` verfaelscht die
      dmesg-Sicht auf dieselbe Weise.

      Folge auf der Box: spotify-control ist beim Booten 7x abgestuerzt und von pm2
      immer wieder neu gestartet worden. video und render gleich mitgeben, sonst
      fehlt dem Kiosk-Browser die Hardwarebeschleunigung.
    source: "am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07): nach usermod meldet aplay -l 'card 0: MAX98357A'"

  - kind: signature
    id: mupi-xinput-fehlt-touch-mapping
    title: "Touch-Zuordnung wird still übersprungen (xinput fehlt)"
    sev: med
    match: "xinput: (command )?not found|Touch.*nicht gemappt|force-dsi-output"
    cause: "force-dsi-output.sh legt den Touch per xinput auf den DSI-Ausgang; fehlt das Paket, ueberspringt das Skript den Schritt STILL — kein Fehler im Log"
    fix: "apt-get install -y xinput (steckt im Schritt 'deps' des Systemrezepts)"
    tags: [touch, x11, dsi, kiosk]
    body: |
      Ein stiller Ausfall ohne Fehlermeldung: das Skript laeuft durch, nur die
      Zuordnung passiert nie. Mit installiertem xinput steht sie im Log:
        Touch '11-0038 generic ft5x06 (79)' auf DSI-1-2 gemappt

      Bei EINEM angeschlossenen Bildschirm ist die Zuordnung meist entbehrlich (die
      Transformationsmatrix ist ohnehin die Einheitsmatrix) — sie wird erst wichtig,
      sobald zusaetzlich HDMI haengt. Trotzdem gehoert xinput zu den Abhaengigkeiten,
      sonst arbeitet man bei Touch-Problemen mit einem Skript, das nur so TUT, als
      haette es etwas getan.
    source: "am Geraet belegt (Pi 5 / DietPi Trixie, 2026-07)"

  - kind: signature
    id: mupi-dietpi-wifi-datei-ersetzt
    title: "Box kommt nie ins WLAN, Schirm endet bei 'first run setup failed' (dietpi-wifi.txt ersetzt statt umgesetzt)"
    sev: high
    match: "first run setup failed|unknown install state|aWIFI_EAPID|dietpi-wifi"
    cause: "dietpi-wifi.txt wurde NEU GESCHRIEBEN statt umgesetzt: DietPi liest die Datei als Bash-Skript mit FESTEM Variablensatz — fehlende Variablen (aWIFI_EAP, aWIFI_IDENTITY, aWIFI_PASSWORD, aWIFI_PHASE1/2, aWIFI_CERT) und erfundene Namen (aWIFI_EAPID/aWIFI_EAPPHASE gibt es nicht) lassen das WLAN still scheitern"
    fix: "die VORHANDENE dietpi-wifi.txt der Karte zeilenweise umsetzen (nur aWIFI_SSID[0]/aWIFI_KEY[0]/aWIFI_KEYMGR[0] ersetzen, Rest unberuehrt) — sdprep.patch_wifi_txt im remote-step-installer"
    tags: [dietpi, wifi, erstboot, trixie]
    body: |
      Die Kette dahinter: der automatische Erstlauf (AUTO_SETUP_AUTOMATED=1)
      braucht Internet; kommt das WLAN nie, bietet DietPi den interaktiven
      Neuversuch an — auf dem Schirm steht dann "first run setup failed" bzw.
      "unknown install state", was wie ein KARTEN- oder Setupfehler aussieht,
      aber nur die FOLGE des fehlenden Netzes ist.

      VERIFIZIEREN STATT RATEN: das zwischengespeicherte DietPi-Abbild laesst
      sich am Rechner auspacken (xz -dc | head/tail auf die FAT-Partition,
      mtools/mcopy zum Lesen) — dann sieht man die ECHTEN Dateien, gegen die
      die eigene Bearbeitung laufen muss, statt beim Booten zu raten.
    source: "am Geraet bezahlt (Pi 5 / DietPi Trixie, 2026-07): mehrere Boot-Runden, bis das Abbild ausgepackt und verglichen wurde"

  - kind: signature
    id: mupi-wlan-laendercode-gb
    title: "WLAN-Ländercode steht standardmäßig auf GB (und gehört in dietpi.txt, nicht dietpi-wifi.txt)"
    sev: med
    match: "AUTO_SETUP_NET_WIFI_COUNTRY_CODE|WIFI_COUNTRY_CODE|5 ?GHz.*unsichtbar"
    cause: "DietPis Standard ist AUTO_SETUP_NET_WIFI_COUNTRY_CODE=GB in dietpi.txt; ein WIFI_COUNTRY_CODE in dietpi-wifi.txt wird IGNORIERT (die Variable existiert dort gar nicht)"
    fix: "in dietpi.txt AUTO_SETUP_NET_WIFI_COUNTRY_CODE=DE setzen"
    tags: [dietpi, wifi, laendercode, 5ghz]
    body: |
      Mit britischen Kanalregeln kann ein deutsches 5-GHz-Netz (v. a. auf
      DFS-Kanaelen wie 5300 MHz) unerreichbar bleiben, waehrend 2,4 GHz
      funktioniert. Dazu: AUTO_SETUP_NET_ETHERNET_ENABLED=0 setzen, wenn WLAN
      gewaehlt ist — ein vorhandener, aber defekter LAN-Adapter (Link ja, DHCP
      nein) haelt den Erstlauf sonst auf, obwohl WLAN eingerichtet waere.
    source: "aus dem echten Abbild gelesen (DietPi Trixie RPi5, 2026-07)"

  - kind: signature
    id: mupi-config-txt-treiber-auskommentiert
    title: "Bildschirm bleibt schwarz: KMS-Treiber steht in config.txt nur AUSKOMMENTIERT da"
    sev: high
    match: "dtoverlay=vc4-kms-v3d|Panel.*schwarz|Bildschirm.*(schwarz|dunkel)"
    cause: "DietPi liefert '#dtoverlay=vc4-kms-v3d,noaudio' aus — ein simpler Textvergleich haelt den Treiber fuer aktiv und ergaenzt nur das Panel-Overlay, das ihn aber ausdruecklich voraussetzt"
    fix: "config.txt ZEILENWEISE pruefen und nur AKTIVE dtoverlay-Zeilen zaehlen; fehlt vc4-kms-v3d aktiv, beide Zeilen anhaengen (Treiber + Panel)"
    tags: [dietpi, display, dsi, config-txt, pi5]
    body: |
      Symptom: Fehlersuche-Modus an, trotzdem dunkles Panel — weil nur
      dtoverlay=vc4-kms-dsi-7inch ergaenzt wurde. Merksatz: bei config.txt-
      Pruefungen zaehlen nur Zeilen, die NICHT mit # beginnen. (Beruhigend:
      diese DietPi-config.txt hat KEINE Bedingungsbloecke [pi5]/[all] —
      Anhaengen am Dateiende ist zulaessig.)
    source: "am Geraet bezahlt (2026-07): Karte mit 'aktiviertem' Debug-Schirm blieb schwarz"

  - kind: signature
    id: mupi-prescript-verklemmung
    title: "Erstboot friert bei 'Automation_Custom_PreScript.sh please wait' ein (systemd-Verklemmung)"
    sev: high
    match: "Automation_Custom_PreScript|please wait"
    cause: "das PreScript lief 'systemctl enable --now' auf einen Dienst mit Netz-Ordnung (After=network.target) — der Erstboot wartet auf den Dienst, der Dienst auf das Netz, das Netz kommt erst NACH dem Erstboot"
    fix: "im PreScript NIE wartend starten: keine Netz-Ordnung in der Unit, enable OHNE --now, Sofortstart per systemd-run (eigene Einheit, ueberlebt den Erstboot-Dienst) oder systemctl start --no-block"
    tags: [dietpi, erstboot, systemd, verklemmung]
    body: |
      Grundwissen dazu: /boot/Automation_Custom_PreScript.sh wird beim ERSTEN
      Boot ausgefuehrt, ausdruecklich VOR dem Netz (Log:
      /var/lib/dietpi/logs/dietpi-firstboot.log) — der richtige Haken fuer
      alles, was auch ohne Netz laufen muss. Das Post-Skript
      Automation_Custom_Script.sh laeuft dagegen erst NACH erfolgreichem
      Erstlauf, also nie, wenn das Netz fehlt.
    source: "am Geraet bezahlt (2026-07): Schirm stand dauerhaft bei 'please wait'"

  - kind: howto
    id: mupi-wlan-selbstdiagnose-per-karte
    title: "WLAN-Fehlersuche ohne Netz: die Karte diagnostiziert die Box und bringt den Befund mit zurück"
    body: |
      Eine Box, deren WLAN nicht kommt, ist stumm UND (ohne Debug-Schirm)
      blind — jede Hypothese kostet einen Boot und liefert ein Bit. Der
      Ausweg: ein Automation_Custom_PreScript.sh richtet einen Dienst ein, der
      5 Minuten lang alle 15 s den Funkzustand einsammelt (rfkill, iw reg get,
      ip -4 addr, iw dev wlan0 link, wpa_cli status, Scan-Cache, journalctl
      nach wpa/dhcp/brcmfmac) und auf die BOOT-PARTITION schreibt (sync nach
      jeder Runde; WLAN-SCHLUESSEL bewusst nie in die Datei — FAT ist
      Klartext). Karte zurueck in den Rechner -> ./sdprep --diagnose deutet
      den Befund.

      DIE DEUTUNGS-KETTE (wo reisst es?): Funk -> Anmeldung -> Adresse.
        * wlan0 fehlt/rfkill        -> Treiber/Hardware
        * Ziel-SSID in keinem Scan  -> Band/Reichweite/versteckt
        * sichtbar, nie COMPLETED   -> Schluessel oder Router-Filter;
          WRONG_KEY + reason=15 = 4-Wege-Handshake gescheitert. ACHTUNG:
          eine Fritzbox mit MAC-Filter laesst assoziieren und beantwortet den
          Handshake nie — sieht ebenfalls wie WRONG_KEY aus
        * COMPLETED, DHCPDISCOVER unbeantwortet -> Routerseite (Filter,
          Gastnetz-Isolation, Adresspool)

      DIE TEURE LEHRE DES ERSTEN ECHTEN LAUFS: WRONG_KEY, obwohl Karte,
      NM-Profil und Schwester-Netz denselben Schluessel trugen (per
      SHA256-Hash verglichen, nie angezeigt). Beweis per Gegenprobe: der
      LAPTOP scheiterte an derselben SSID genauso — das GESPEICHERTE
      NetworkManager-Profil war schlicht VERALTET (Passwort irgendwann
      geaendert; nie aufgefallen, weil der Laptop immer das Schwesternetz
      nutzt). Merksatz: nur der Schluessel des Netzes, mit dem der Rechner
      GERADE online ist, ist bewiesen richtig. Ein nur-gespeichertes Profil
      kann luegen.
    source: "am Geraet gebaut und beim ersten Einsatz bezahlt gemacht (Pi 5 / DietPi Trixie, 2026-07): Befund 'anmeldung' mit WRONG_KEY fuehrte zur Gegenprobe am Laptop und zum veralteten Profil"

  - kind: signature
    id: mupi-touch-verdreht
    title: "Touch reagiert an der falschen Stelle (Controller 180° zum Panel verdreht)"
    sev: med
    match: "Touch.*(verdreht|gedreht|falsche Stelle)|Beruehrung.*versetzt"
    cause: "der Touch-Controller sitzt im Gehaeuse anders herum als das Panel; die Rohkoordinaten muessen gedreht werden"
    fix: "Drehung ausmessen und setzen — bei der Touch-Bruecke `--drehung 0|90|180|270` in der Dienstdatei; beim Kerneltreiber die Coordinate Transformation Matrix per xinput"
    tags: [touch, dsi, kalibrierung, pi5]
    body: |
      SO WIRD RICHTIG GEMESSEN (und genau daran bin ich zuerst gescheitert):
      eine BEKANNTE Ecke antippen und nachsehen, welche Rohwerte ankommen.
        systemctl stop mupibox-touch-bridge
        /opt/mupibox-tools/touch-bridge.py --probe 15    # dabei OBEN LINKS tippen
      Meldet er dort Werte nahe dem MAXIMUM beider Achsen (gemessen: x~750
      y~415 bei 800x480), sind es 180 Grad.

      DER FEHLSCHLUSS, der Zeit kostet: zu pruefen, ob die WERTEBEREICHE passen
      (0..800 x 0..480) und daraus "stimmt" zu schliessen. Ein 180-Grad-Versatz
      besteht diese Pruefung muehelos — die Bereiche sind ja identisch. Nur die
      ZUORDNUNG einer bekannten Ecke beweist die Ausrichtung.

      Bei 90/270 tauschen zusaetzlich Breite und Hoehe des gemeldeten Geraets.
    source: "am Geraet gemessen und behoben (Pi 5 / DietPi Trixie, 2026-07)"

  - kind: signature
    id: mupi-stromausfall-bootdateien
    title: "Box startet nach hartem Ausschalten nicht mehr (config.txt zerstört, systemd-Unit 0 Bytes)"
    sev: high
    match: "startet nicht|kein Display.*kein SSH|free cluster|not properly unmounted|Unit .* is masked"
    cause: "ein Installationsschritt hat bootkritische Dateien geaendert, die Daten hingen noch im Zwischenspeicher, dann wurde hart ausgeschaltet — die zuletzt geschriebene Datei ist zerstoert"
    fix: "fsck.fat -a -w auf die Boot-Partition, dann config.txt neu schreiben; vorbeugend nach JEDEM Schritt sync (steckt jetzt im Agent des remote-step-installers)"
    tags: [boot, sd-karte, fat, systemd, dietpi]
    body: |
      DAS SCHADENSBILD: gruene LED dauerhaft an, kein Bild, kein Netz. Auf der
      Karte war genau EINE Datei unlesbar (Ein-/Ausgabefehler beim Lesen von
      config.txt), alles andere — auch der 10-MB-Kernel — las sich einwandfrei.

      WICHTIG ZU WISSEN: der vfat-Treiber meldet fuer eine GEKAPPTE CLUSTER-KETTE
      denselben E/A-Fehler wie fuer einen echten Medienfehler. Es sieht nach
      sterbender Karte aus, ist aber ein Dateisystemschaden. fsck sagte es
      deutlich:
        /config.txt Contains a free cluster (77677). Assuming EOF.
        Dirty bit is set. Fs was not properly unmounted.
      und meldete KEINEN bad cluster — die Karte war gesund.

      ZWEITES OPFER, leicht zu uebersehen: eine systemd-Unit-Datei mit 0 Bytes.
      Systemd behandelt sie als MASKIERT, der Dienst laeuft nie wieder an, und
      `systemctl is-enabled` sagt "masked" statt "kaputt". Nach jedem harten
      Ausschalten deshalb suchen:
        find /etc/systemd/system -maxdepth 1 -type f -size 0

      VORBEUGUNG: der Agent macht jetzt nach jedem Schritt os.sync(), BEVOR er
      ihn als fertig meldet — kostet Millisekunden und schuetzt jedes Rezept,
      nicht nur die, die daran gedacht haben. Und: bei einer haengenden Box
      NICHT sofort den Stecker ziehen, erst per SSH nachsehen.
    source: "am Geraet passiert und repariert (Pi 5 / DietPi Trixie, 2026-07)"

  - kind: note
    id: mupi-installation-besteht-aus-zwei-rezepten
    title: "Eine vollständige Installation sind ZWEI Rezepte (System + App)"
    body: |
      Klassischer Irrtum: das System-Rezept (mupibox.yaml — Pakete, Audio,
      Display, Chromium) laeuft komplett durch, alles gruen — und man haelt die
      Box fuer fertig. Die eigentliche APP steckt aber in mupibox-app.yaml
      (Frontend, Backend, Admin, pm2-Dienste, Kiosk-Autostart). Ohne sie:
      Chromium startet, aber es gibt nichts anzuzeigen.

      Woran man es SOFORT sieht:
        cat /boot/dietpi/.dietpi-autostart_index      # 11 = Kiosk gesetzt
        ls /home/dietpi/.mupibox/*/server.js          # App vorhanden?
        su - dietpi -c "pm2 list"                     # server + spotify-control

      Im remote-step-installer nimmt die TUI seit 2026-07 mehrere Rezepte als
      EINE Liste (`--recipe mupibox,mupibox-app`), damit dieser Irrtum gar nicht
      erst entstehen kann.

      Ebenfalls pruefenswert, weil sonst nach dem Neustart alles leer ist:
      pm2 ueberlebt einen Neustart NUR mit `pm2 save` + `pm2 startup`
      (-> pm2-dietpi.service enabled und ~/.pm2/dump.pm2 vorhanden).
    source: "am Geraet erlebt (2026-07): 'alle Punkte im TUI durch' — und die halbe Box fehlte"

  - kind: note
    id: mupi-admin-bestandsaufnahme
    title: "Admin-Oberfläche: Bestandsaufnahme vor der Modernisierung (Stand 2026-07)"
    body: |
      Aufgenommen am Fork (AdminInterface/www), BEVOR etwas umgebaut wurde —
      damit spaeter nachvollziehbar ist, was es konnte und wo es klemmte.

      UMFANG: 28 PHP-Seiten, 5616 Zeilen, kein Framework, kein Aufbauwerkzeug.
      Unterverzeichnisse: images/, includes/ (header+footer), lang/ (NUR en.json).

      SEITEN UND WAS SIE KOENNEN (Reihenfolge wie im Menue):
        index.php        Startseite/Uebersicht
        content.php      Fernbedienung: bettet die Box-Oberflaeche als <embed> ein
        mupi.php         (1577 Z, die groesste) zentrale Einstellungen: Anzeige,
                         Drehung, Lautstaerke, Startverhalten, CPU-Regler …
        mupihat.php      MuPiHAT: Akku/Ladezustand, Luefter, Batterie-Symbol
        media.php        Medienverwaltung (Spotify-Alben/Playlists, Cover-Abruf)
        cover.php        Cover-Pflege
        bluetooth.php    BT: suchen, koppeln, entfernen (ruft die *_bt.sh-Skripte)
        spotify.php      Spotify-Zugang: Client-ID + PKCE-Anmeldung (Weiterleitung)
        jellyfin.php     Jellyfin-Anbindung
        network.php      (555 Z) WLAN: Netze, Passwoerter, wpa_cli
        smart.php        (591 Z) "Smart"-Funktionen (MQTT, WLED, Automatik)
        service.php      Dienste starten/stoppen
        tweaks.php       Systemschrauben (CPU-Regler, Swap …)
        logviewer.php    Protokolle ansehen
        jsoneditor.php   mupiboxconfig.json direkt bearbeiten (CodeMirror)
        admin.php        (517 Z) Wartung: Update, Backup, Debug-Schalter
      Dazu ohne Menueeintrag: backup/fullbackup, pm2logs, backend, debug, vnc,
      support_data, logout und vier update_*icon-Helfer.
      Kopfleiste: Herunterfahren, Neustart, Chromium neu starten, Mediendatenbank
      neu laden, Abmelden.

      ANMELDUNG: session_start + password_verify gegen den Hash in
      mupiboxconfig.json (interfacelogin.password). Das ist solide gemacht — und
      zugleich das EINZIGE Tor.

      BAUWEISE: alles laeuft ueber die Shell — 307 exec()-Aufrufe, davon 110 mit
      sudo. Die Oberflaeche ist also im Kern eine Sammlung von Formularen, die
      Systembefehle als root absetzen.

      DREI BEFUNDE, DIE EINE MODERNISIERUNG TRAGEN SOLLTE:

      1. OHNE INTERNET IST DER ADMIN KAPUTT. 13 Fremdquellen im header.php aus
         9 verschiedenen Hosts (bootstrapcdn, jsdelivr, cloudflare, jquery.com,
         fontawesome, gstatic, iconify, sogar raw.githubusercontent.com). Es gibt
         KEINE oertliche Kopie. Eine Kinder-Musikbox steht aber oft in einem Netz
         ohne Internet — dann laedt weder CSS noch JS. Alles buendeln (npm/vendor
         oder schlicht mitgeliefert) waere der erste Schritt.

      2. BOOTSTRAP 3 UND 5 GLEICHZEITIG. header.php laedt bootstrap 3.4.1 UND
         bootstrap@5.3.3. Die Klassennamen ueberschneiden sich teils mit anderer
         Bedeutung — das erklaert kosmetische Merkwuerdigkeiten und macht jede
         Layout-Aenderung zum Ratespiel. EINE Fassung waehlen.

      3. EINGABEN GEHEN UNGEPRUEFT IN sudo-BEFEHLE. 259 $_POST-Zugriffe stehen
         nur 5 escapeshellarg und 7 htmlspecialchars gegenueber. Konkrete Stellen,
         an denen ein Formularwert per Zeichenkettenverkettung in einen
         sudo-Aufruf wandert:
           bluetooth.php:29  remove_bt.sh ".$_POST['remove_mac']
           bluetooth.php:42  pair_bt.sh   ".$_POST['bt_device']
           network.php:178   wpa_cli remove_network ".$_POST['wifinr']
           mupi.php:31/37/43 display_hdmi_rotate / lcd_rotate aus $_POST
         Das Login schuetzt davor, aber es ist die einzige Huerde: wer im Heimnetz
         das Passwort hat (oder einen CSRF unterbringt), hat root. escapeshellarg
         ueberall + CSRF-Token waeren billig und wirksam.

      WEITERES, kleiner aber sichtbar:
        * lang/ existiert, enthaelt aber nur en.json — Mehrsprachigkeit ist
          angefangen und liegt brach (die Oberflaeche ist gemischt EN/DE).
        * PHP 8.3 ist das Ziel; die Seiten sind im Zuge dieser Sitzung schon
          teilweise 8.3-sauber gemacht worden, aber nicht durchgaengig geprueft.
        * autoconnect_bt.sh nutzt `bluetoothctl paired-devices` — den Befehl gibt
          es in BlueZ 5.82 NICHT mehr (heisst `devices Paired`), das automatische
          Wiederverbinden kann also nicht funktionieren.
    source: "am Fork aufgenommen und gegen die laufende Box geprueft (Pi 5 / DietPi Trixie, 2026-07)"

  - kind: note
    id: mupi-offene-optimierungen
    title: "Offener Rückstand: Optimierungen, die angeboten und zurückgestellt wurden"
    body: |
      Gesammelt, damit nichts verloren geht. Aufgeteilt nach der Frage, die bei
      der Admin-Modernisierung zaehlt: WAS ERLEDIGT SICH VON SELBST, wenn der
      PHP-Admin durch Angular + die vorhandene Node-API ersetzt wird?

      ── ERLEDIGT SICH DURCH EINEN TECHNOLOGIEWECHSEL ──────────────────────
      Diese Punkte sind PHP-Altlasten. Wer sie in PHP repariert und den Admin
      spaeter ersetzt, hat die Arbeit zweimal gemacht:

      * ~293 PHP-8.3-Warnungen ueber die 28 Seiten. Verteilung: mupi.php 94,
        smart.php 68, network.php 28, admin.php 27, media.php 20,
        bluetooth.php 17; der gemeinsame Header/Footer, jellyfin.php und
        spotify.php sind schon sauber. Mechanisch: $data[…][…] -> ?? '' und
        $_POST/$_GET -> isset()/!empty(). Sichtbar machen mit
        MUPI_ADMIN_DEBUG=1 (harness/docker-compose.yml, Profil admin).
      * Bootstrap 3.4.1 UND 5.3.3 gleichzeitig geladen (Ionic loest das).
      * 13 Fremdquellen aus 9 CDN-Hosts, keine oertliche Kopie -> ohne Internet
        ist der Admin unbenutzbar. Ein Angular-Build buendelt alles von selbst.
      * lang/ mit nur en.json (angefangene Mehrsprachigkeit). ACHTUNG: das
        Frontend hat SELBST noch KEINE i18n-Bibliothek — hier waere also nichts
        zu erben, sondern etwas neu zu entscheiden.

      ── BLEIBT IN JEDEM FALL ──────────────────────────────────────────────
      * Eingaben, die in privilegierte Befehle laufen. Heute: 259 $_POST gegen
        5 escapeshellarg, konkrete Stellen siehe [[mupi-admin-bestandsaufnahme]].
        Nach einem Wechsel wandert das Problem nach Node — BESSER, weil an einer
        pruefbaren Stelle statt an 307, aber NUR mit einer FESTEN LISTE erlaubter
        Operationen. Ein allgemeiner "fuehre diesen Befehl aus"-Endpunkt waere
        schlechter als der heutige Zustand.
      * Spotify-Zwischenspeicherstufe als Bedienelement (Aus/Mittel/Maximal ->
        spotifyCacheLevel = off|medium|max in mupiboxconfig.json). Das FRONTEND
        kann es bereits (MediaCacheService liest es ueber /api/config, Standard
        medium): off = kein Zwischenspeicher, medium = Playlists je Oeffnen ueber
        snapshot_id pruefen (~1 Abruf statt ~20), max = 7 Tage Haltbarkeit +
        vollstaendiges Vorladen. Fehlt NUR das Auswahlfeld + das Schreiben in die
        Konfiguration. Bis dahin ueber den JSON-Editor setzbar.

      ── FRONTEND-LEISTUNG: ANGEBOTEN, NICHT GEMACHT ───────────────────────
      Unabhaengig vom Admin. Alle drei brauchen Messung AUF der Box:
      * content-visibility:auto + contain-intrinsic-size fuer die Kacheln
        ausserhalb des Sichtfensters. Der Swiper baut ALLE Folien; das liesse den
        Browser die unsichtbaren ueberspringen — groesster Gewinn bei grosser
        Bibliothek, birgt aber Sprungrisiko im Layout.
      * Die Hoerspiel-Zerlegung (getPlaylistDecomposition) STROEMEND anzeigen:
        Alben erscheinen, sobald ihre Seite da ist, statt nach allen ~20 Abrufen.
      * Zonenlose Aenderungserkennung (12 von 16 Komponenten sind schon OnPush,
        die App haengt aber noch an zone.js). Echte Umstellung, riskant.

      ── ZURUECKGESTELLT: RAM-Platte fuer den Chromium-Zwischenspeicher ─────
      Der Zwischenspeicher liegt heute auf der SD. Ihn auf eine tmpfs zu legen
      (--disk-cache-dir, 64-128 MB) spart SD-Verschleiss und Kaltlese-Zeit.
      DER HAKEN, der die Umsetzung bestimmt: die Box wird AUSGESTECKT, nicht
      heruntergefahren. Ein reiner RAM-Zwischenspeicher waere nach jedem Ziehen
      leer — es braucht also einen regelmaessigen Ruecklauf auf die SD (Timer,
      ~15 min) plus Wiederherstellen beim Start, sonst ist beim Hochfahren nichts
      zu laden da. Behebt NICHT das Ruckeln beim Umblaettern (das war CSS und ist
      erledigt) und auch nicht den ersten Netzabruf.

      ── NACHTRAG 2026-07-26: was davon inzwischen erledigt ist ────────────
      Am Geraet nachgeprueft, nicht aus dem Gedaechtnis.

      ERLEDIGT
      * Die Angular-Verwaltung LAEUFT auf der Box (/admin antwortet 200).
        Stand 2026-07-30 (Repo-Abgleich): 13 Seiten + Anmeldung — uebersicht,
        darstellung, dienste, system, protokolle, aktualisierung,
        konfiguration, medien (dienstuebergreifende Suche Spotify+Jellyfin),
        kinderzeit, vorlesen, bluetooth, netzwerk, mupihat (Akku-KURVE).
        Damit sind die vier PHP-Altlasten oben fuer die NEUE
        Oberflaeche gegenstandslos: keine 8.3-Warnungen, kein doppeltes
        Bootstrap, keine CDN-Abhaengigkeit.
      * „Feste Liste erlaubter Operationen" ist umgesetzt, nicht nur
        beabsichtigt: feste Tabellen (system.ts AKTIONEN, protokolle.ts
        DATEIEN) bzw. eine aus systemd ERMITTELTE Erlaubnisliste
        (dienste.ts ERLAUBTE_PRAEFIXE). Einen allgemeinen
        „fuehre-Befehl-aus"-Endpunkt gibt es nirgends.
      * Spotify-Zwischenspeicherstufe als BEDIENELEMENT ist da
        (konfiguration.ts, Feld `spotifyCacheStufe`, feste Auswahl
        off|medium|max auf dem obersten Schluessel `spotifyCacheLevel`).
        Der Punkt „fehlt NUR das Auswahlfeld" ist damit zu.

      WEITERHIN OFFEN
      * Der PHP-Admin ist NICHT entfernt: 28 .php-Dateien liegen weiter unter
        /var/www, samt des gefaehrlichen Update-Knopfes (curl | sudo bash
        gegen UPSTREAM, waehrend dieser Baum weit voraus ist). Solange beides
        parallel liegt, ist die Altlast nur verdeckt, nicht weg.
      * i18n: hier war nichts zu erben und es ist auch nichts entstanden — die
        neue Verwaltung ist einsprachig deutsch, das Box-Frontend hat
        weiterhin keine i18n-Bibliothek.
      * Die drei Frontend-Leistungspunkte: unveraendert offen.
      * RAM-Platte fuer den Chromium-Zwischenspeicher: unveraendert offen.
        Der Zwischenspeicher liegt heute unter
        /home/dietpi/.mupibox/chromium_cache (`--disk-cache-dir` ist also
        gesetzt) — nur eben auf der SD statt auf einer tmpfs.
    source: "aus den Arbeitsnotizen zusammengetragen (2026-07); Zahlen am Fork und an der laufenden Box geprueft; Nachtrag am Geraet geprueft (2026-07-26)"

  - kind: note
    id: mupi-neue-verwaltung-huelle-vor-dem-tor
    title: "Neue Verwaltungsoberflaeche: die Huelle gehoert VOR das Anmeldetor"
    body: |
      Die Ablloesung des PHP-Admins hat mit der Anmeldung begonnen (Angular 20
      unter src/frontend-admin, ausgeliefert vom Backend unter /admin).

      DIE FALLE, die dabei fast zugeschnappt waere: das Anmeldetor (torBauen in
      auth.ts) haengt als Zwischenschicht OHNE Pfadangabe im Server, gilt also
      fuer ALLES, was danach registriert wird. Legt man die Dateien der
      Oberflaeche dahinter, antwortet die ANMELDESEITE SELBST mit 401 — man
      kaeme nur herein, wenn man schon drin waere. Merksatz: das Schloss gehoert
      an die Tuer, nicht an die Klingel.

      Richtig ist: die HUELLE (index.html, css, js) vor das Tor, alle DATEN
      (/api/...) dahinter. Wer die Seite ohne Anmeldung oeffnet, sieht dann
      genau nichts ausser dem Passwortfeld. In server.ts stehen deshalb
      express.static('/admin') und der SPA-Rueckfall VOR app.use(torBauen(...)).

      ZWEITE FALLE, gleiche Datei: `path.join(__dirname, ...)` auf Modulebene.
      Im ausgelieferten Bundle (esbuild --platform=node, CJS) gibt es __dirname,
      unter tsx laeuft dieselbe Datei aber als ES-Modul, wo der Name schlicht
      nicht existiert — der Zugriff wirft und reisst den ganzen Server mit. Die
      bestehende Zeile mit express.static('www') hat denselben Fehler, faellt
      aber nie auf, weil sie nur im Produktionszweig laeuft und `npm run serve`
      im Entwicklungsmodus startet. Abfrage mit `typeof __dirname !== 'undefined'`.

      DRITTE STELLE, die zu falschen Schluessen fuehrt: supertest verbindet sich
      ueber die RUECKSCHLEIFE, und die laesst das Tor absichtlich durch (sonst
      bliebe der Bildschirm der Box nach dem Einschalten leer). Eine Pruefung
      "und /api/data bleibt 401" schlaegt dort folgerichtig fehl — das Abweisen
      von aussen gehoert auf die Baustein-Ebene geprueft, wo die Adresse frei
      setzbar ist.

      ENTWURFSENTSCHEIDUNGEN, mit Grund:
        * KEINE Fremdquellen. Der PHP-Admin zog 13 Dateien von 9 Hosts und war
          damit ohne Internet unbenutzbar — genau dort, wo eine Kinder-Musikbox
          oft steht. Die neue Oberflaeche ist ein Bundle von 74 kB plus
          Systemschriften.
        * KEIN Ionic. Der Admin wird von Eltern am Laptop/Handy bedient, nicht
          vom Kind am Touchpanel; die Mobil-App-Huelle bringt hier nichts.
        * Der Bau landet in ../deploy/www-admin, also NEBEN server.js und www —
          genau da sucht path.join(__dirname,'www-admin'), und deploy.zip wird
          nach Sonos-Kids-Controller-master/ entpackt, sodass es von selbst
          mitreist.
    source: "am Fork gebaut und im Browser Ende zu Ende geprueft (Anmelden, Neuladen, Abmelden, HttpOnly), 2026-07"

  - kind: note
    id: mupi-src-deploy-gehoert-root
    title: "Bau schlaegt mit EACCES fehl: src/deploy gehoert root"
    body: |
      Symptom: `ng build` meldet zwar "Output location: .../src/deploy/...",
      schreibt aber nichts und endet mit
      `EACCES: permission denied, mkdir '.../src/deploy/www-admin'`.

      Ursache: src/deploy und sein ganzer Inhalt gehoeren root — Ueberbleibsel
      eines Baus, der einmal mit sudo lief. Betroffen sind ALLE drei
      Arbeitsbereiche, denn sie schreiben alle dorthin:
        frontend-box   ng build --output-path=../deploy/www
        frontend-admin outputPath ../deploy/www-admin
        backend-api    esbuild --outfile=../deploy/server.js

      Abhilfe einmalig:  sudo chown -R "$USER" src/deploy
      (oder src/deploy loeschen — es ist ohnehin in .gitignore und wird beim
      naechsten Bau neu erzeugt).

      Vorbeugen: den Fork NIE mit sudo bauen. Siehe auch
      fork-bauen-ohne-sudo-ausgabe-umlenken.
    source: "am Fork beobachtet (2026-07), Ordner datiert vom 24.07."

  - kind: signature
    id: mupi-backend-throw-im-rueckruf
    title: "Backend plötzlich tot nach einer Medien-Änderung (throw im Rückruf)"
    sev: high
    match: "Backend.*(tot|weg|nicht erreichbar).*(add|edit|delete)|Server stirbt beim Speichern"
    cause: "`throw` in einem asynchronen Rueckruf faengt niemand — das beendet in heutigem Node den ganzen Prozess"
    fix: "in jedem Rueckruf den Fehler protokollieren und antworten (500) statt zu werfen"
    tags: [backend, node, robustheit]
    body: |
      server.ts hatte ELF `throw` in asynchronen Rueckrufen, verteilt ueber die
      Schreibwege /api/add, /api/edit, /api/delete, /api/addresume,
      /api/editresume und /api/addwlan (6x beim Schreiben selbst, 5x beim
      Entfernen der Sperrdatei danach). Ein ungefangener throw im Rueckruf
      beendet den PROZESS — nicht nur die Anfrage.

      GEMESSEN, nicht vermutet (wlan.json auf nur-lesbar gesetzt, dann
      POST /api/addwlan):
        vorher:  Verbindung reisst mitten in der Anfrage ab, Server TOT
        nachher: 500, Server lebt

      Warum das keine Theorie ist: diese Box hatte nach einem harten Ausschalten
      schon ein nur-lesbar eingehaengtes Dateisystem (siehe
      mupi-stromausfall-bootdateien), und eine volle Karte wirkt genauso. In dem
      Zustand nahm die ERSTE Medienaenderung das komplette Backend mit —
      Oberflaeche, Wiedergabe, API, alles weg bis zum Stromstecker.

      MERKSATZ fuer dieses Backend: in einem Rueckruf gibt es niemanden, der
      faengt. Fehler dort gehoeren protokolliert und beantwortet. Dasselbe gilt
      fuer eine Promise-Kette OHNE .catch() — der Start-Leser der config.json
      hatte genau diesen Fehler und haette eine halb geschriebene Konfiguration
      mit dem Serverstart quittiert.
    source: "am Fork gefunden und per Gegenprobe belegt (2026-07)"

  - kind: note
    id: mupi-admin-dienste-und-netzwerk-neu
    title: "Neue Verwaltung: Dienste und Netzwerk ohne Shell-Zeichenketten"
    body: |
      Die zweite und dritte Seite der neuen Verwaltung (nach der Anmeldung).
      Beide loesen PHP-Seiten ab, die Formularwerte per Verkettung in
      sudo-Befehle gaben.

      DIENSTE (loest service.php ab). Zwei Regeln schliessen die ganze
      Fehlerklasse aus:
        1. Nie eine Shell — execFile mit einem ARRAY von Argumenten. Ein Name
           mit `;` oder `$(…)` ist dann bloss ein Name, den systemd nicht kennt.
        2. Nie ein Name vom Anrufer — die Liste kommt von systemd SELBST
           (`systemctl list-unit-files`, gefiltert ueber eine Praefix-Freigabe
           mupi_/mupibox-/mupi- plus eine kleine Fremdliste smbd/proftpd/…).
           Was hereinkommt, muss in dieser Liste stehen.
      Die Liste ist ABSICHTLICH nicht fest verdrahtet: dieses Projekt hat
      mupibox-touch-bridge, -boot-splash und -alsa-init erst dazubekommen, eine
      feste Liste haette alle drei verpasst.
      Die Oberflaeche trennt, was die alte Seite vermischt hat: „laeuft gerade"
      (start/stop, sofort) von „startet mit" (enable/disable, erst beim naechsten
      Hochfahren). Was systemd als `static` fuehrt, bekommt GAR KEINEN Schalter —
      einer, der nichts tut, ist schlimmer als keiner.

      NETZWERK (loest network.php ab). Zustand wird LIVE gelesen statt aus einer
      Datei, an der ein Hintergrundskript vielleicht gerade schreibt:
      `ip -j addr` liefert fertiges JSON (kein Zeilenparsen, keine
      Sprachabhaengigkeit), `os.hostname()` braucht gar keinen Prozess. Jeder
      Teil einzeln abgesichert — fehlt `iw`, ist die Adresse trotzdem da.

      WARUM DIE SSID-PRUEFUNG WIRKLICH ZAEHLT: scripts/mupibox/add_wifi.sh ist
      eine ROOT-Schleife, die alle 2 Sekunden wlan.json liest und den Namen
      UNQUOTIERT in wpa_supplicant.conf schreibt (add_wifi.sh:48:
      `echo '\tssid="'${SSID}'"' | sudo tee -a`). Wer in diese Datei schreiben
      darf, schreibt in die WLAN-Konfiguration der Box — und POST /api/addwlan
      beschrieb sie UNGEPRUEFT, vor dem Anmeldetor aus dem ganzen LAN erreichbar.
      Deshalb: keine Steuerzeichen, keine Anfuehrungszeichen, kein Rueckstrich,
      und die Laenge in BYTE (802.11: 32) statt in Zeichen — 17 Umlaute sind 34
      Byte. Datei mit Modus 0600, GENAU EIN Eintrag (das Skript liest eine Liste
      per `jq -r .[].ssid`; mehrere Eintraege werden dort zu mehreren Zeilen in
      EINER Variablen).

      BEWUSST NICHT GETAN: den Netzwerk-Unterbau auf NetworkManager/nmcli
      umstellen. Die Box laeuft auf wpa_supplicant (die Selbstdiagnose sammelt
      `wpa_cli status`); das NetworkManager-Profil in
      mupi-wlan-selbstdiagnose-per-karte war der LAPTOP bei der Gegenprobe.
      Modernisiert wurde der CODE, nicht der Unterbau — ein Stack-Wechsel an
      einer Box, die gerade erst zuverlaessig ins Netz kommt, ist kein Fortschritt.
    source: "am Fork gebaut, im Browser gegen echtes systemd und echten Netzzustand geprueft (2026-07)"

  - kind: note
    id: mupi-admin-system-und-protokolle
    title: "Neue Verwaltung: System- und Protokoll-Seite (und das Muster dahinter)"
    body: |
      Vierte und fuenfte Seite der neuen Verwaltung. Beide folgen demselben
      Muster wie Dienste und Netzwerk — es hat sich als tragfaehig erwiesen und
      sollte fuer die naechsten Seiten weiterverwendet werden:

        1. Keine Shell. execFile mit Argument-ARRAY.
        2. Kein Pfad und kein Befehl vom Anrufer. Feste Tabelle, der Anrufer
           schickt nur eine Kennung.
        3. Die Freigabepruefung nutzt Object.hasOwn, NICHT `in`. Mit `in` waeren
           'toString', 'constructor' und '__proto__' gueltige Kennungen — der
           Zugriff haette dann eine Funktion aus der Prototypenkette geliefert.
           Am Geraet geprueft: alle drei antworten 400.

      SYSTEM (loest die Kopfleiste des PHP-Admins ab: Herunterfahren, Neustart,
      Chromium neu starten, Medien neu einlesen).
        * Laufzeit, Speicher und Auslastung kommen von Node selbst (os.uptime,
          os.totalmem, os.loadavg) — kein Fremdprozess noetig. Nur Temperatur
          (/sys/class/thermal/thermal_zone0/temp, MILLIGRAD) und Plattenplatz
          (df -B1 /) muessen geholt werden, jeweils einzeln abgesichert.
        * Kein Sensor -> null, NICHT 0. Sonst stuende dort "0 °C" und jemand
          sucht einen Fehler, den es nicht gibt.
        * Herunterfahren/Neustart fragen NACH und stehen getrennt unten. Im
          alten Admin sassen sie als kleine Symbole neben harmlosen Knoepfen —
          ein Fehlgriff kostete den Weg zum Geraet.
        * Der Server antwortet ZUERST und handelt dann: ein Neustart nimmt
          genau den Prozess mit, der antworten soll. Die Oberflaeche wertet
          einen abgerissenen Aufruf bei diesen beiden deshalb NICHT als Fehler.
        * Ab 90 % voller Karte warnt die Seite. Grund siehe
          mupi-backend-throw-im-rueckruf: eine volle Karte trat hier schon als
          "Backend ploetzlich tot" auf.

      PROTOKOLLE (loest logviewer.php ab und geht darueber hinaus).
        * Zusaetzlich zu den sechs Dateien der alten Seite sind die JOURNALE der
          Dienste lesbar (journalctl -u <unit>), gefiltert durch DIESELBE
          Freigabe wie die Dienste-Seite. Genau die braucht man, wenn ein Dienst
          "gestoert" anzeigt.
        * Es wird IMMER nur das ENDE gelesen: Lesefenster aus der Zeilenzahl
          geschaetzt, gedeckelt auf 2 MB. Ein Protokoll kann hunderte MB haben;
          vollstaendig eingelesen bringt es einen Pi zum Stehen. Nach einem
          Sprung mitten in die Datei wird die erste (angeschnittene) Zeile
          verworfen.
        * Einstufung PRAEFIX-/wortbasiert, nie per Teilzeichenkette — sonst
          leuchtet "keine Fehler aufgetreten" rot. Dieselbe Lehre wie beim
          LogCutter, wo `...WARNINGS=true` als Warnung zaehlte.
        * Nicht vorhandene Dateien werden AUSGEGRAUT gezeigt statt weggelassen:
          "server-error.log fehlt" ist eine gute Nachricht, aber nur, wenn man
          sieht, dass danach gesucht wurde.

      AUSLIEFERUNG: der Bau landet in ../deploy/www-admin, also neben server.js,
      und reist mit deploy.zip auf die Box. deploy.sh BRICHT AB, wenn
      deploy/www-admin/index.html fehlt — bei MUPI_ADMIN=node waere das eine Box
      ohne jede Verwaltung, und das faellt sonst erst am Geraet auf.
    source: "am Fork gebaut, im Browser gegen echte Werte geprueft (Temperatur, df, journalctl), 2026-07"

  - kind: signature
    id: mupi-update-knopf-loescht-den-fork
    title: "Der Update-Knopf im PHP-Admin loescht den Fork"
    sev: high
    match: "Update.*(Fork|eigene Aenderungen).*weg|nach dem Update.*alles zurueck"
    cause: "admin.php ruft `curl … | sudo bash` und installiert das UPSTREAM-Zip ueber die Box — unabhaengig davon, was dort laeuft"
    fix: "vor jedem Update die HERKUNFT pruefen (Quelle UND Abstand zur Quelle), nicht nur die Fassungsnummer"
    tags: [update, fork, admin, datenverlust]
    body: |
      admin.php:137 fuehrt als root aus:

        curl -L https://raw.githubusercontent.com/splitti/MuPiBox/main/update/
             start_mupibox_update.sh | sudo bash -s -- stable

      Das Skript zieht dann das UPSTREAM-Archiv (…/splitti/MuPiBox/archive/refs/
      tags/<v>.zip) und installiert es ueber die Box. Wer einen Fork faehrt,
      verliert damit alles — ohne Rueckfrage und ohne Weg zurueck.

      DIE FALLE, DIE MAN LEICHT UEBERSIEHT: es genuegt NICHT, die Quell-Adresse
      zu vergleichen. Am Geraet gemessen (2026-07): dieser Fork hatte als
      `origin` die Adresse von UPSTREAM und lag 98 Commits davor. Ein Vergleich
      der Adressen sagte "passt zusammen"; installiert war 4.2.0, angeboten
      4.2.4 — ein reiner Versionsvergleich sagte "Update verfuegbar". Der Klick
      haette 98 Commits verworfen.

      WAS WIRKLICH ZAEHLT ist der ABSTAND zur Quelle. Deshalb stempelt
      src/deploy.sh jetzt eine herkunft.json neben server.js:
        { quelle, commit, zweig, version, eigeneCommits, unsauber, gebautAm }
      `eigeneCommits` = `git rev-list --count @{upstream}..HEAD`,
      `unsauber`     = geaenderte, nicht eingecheckte Dateien.

      Die Beurteilung (src/backend-api/src/aktualisierung.ts) prueft in DIESER
      Reihenfolge — die Reihenfolge ist der ganze Punkt:
        1. gibt es ueberhaupt ein Angebot?      -> keinAngebot
        2. kennt die Box ihre Herkunft?         -> herkunftUnbekannt
        3. gleiche Quelle?                      -> fremdeQuelle
        4. Abstand zur Quelle = 0?              -> eigenbau
        5. erst JETZT die Fassungsnummer        -> neuer / aktuell / unklar
      Ein Platzhalter wie "X.X.X" (Upstream fuehrt den im dev-Kanal) gilt als
      UNVERGLEICHBAR, nie als neuer — sonst boete die Box dauerhaft ein Update
      an, das keines ist.

      Bewusst gibt es (noch) KEINEN Anwenden-Knopf in der neuen Verwaltung: ein
      Update, das sich nicht zurueckdrehen laesst, gehoert erst dann hinein,
      wenn es an einer echten Box erprobt ist.
    source: "am Fork nachgemessen und Ende zu Ende gegen das echte version.json geprueft (2026-07)"

  - kind: note
    id: mupi-konfiguration-schreiben
    title: "mupiboxconfig.json sicher schreiben (Typ, Sicherung, Atomaritaet)"
    body: |
      Beim Ersetzen von mupi.php aufgenommen. Drei Dinge, an denen man sich
      still verletzt:

      1. DER TYP BLEIBT. In dieser Datei stehen ZAHLEN ALS ZEICHENKETTEN
         ("40", "0", "128"), weil die Shell-Skripte sie mit `jq -r` lesen. Wer
         beim Speichern eine echte Zahl schreibt, aendert das Dateiformat unter
         den Skripten weg — ein Fehler, der erst Wochen spaeter und dann
         unerklaerlich auftritt. Beim Zurueckschreiben immer die vorherige Form
         uebernehmen.

      2. DER ALTE SCHREIBWEG WAR UNGESICHERT:
           file_put_contents('/tmp/.mupiboxconfig.json') -> sudo mv
         Keine Sicherung, nicht atomar, vorhersagbarer Pfad in /tmp. Besser:
         .bak anlegen -> danebenliegende Datei schreiben -> fsync -> rename.
         Umbenennen im selben Dateisystem ist atomar; danach gibt es entweder
         die alte oder die neue Fassung, nie eine halbe. Wichtig auf einer Box,
         der schon einmal mitten im Schreiben der Strom ausging.

      3. RECHTE: die Datei gehoert dietpi:www-data 664 (siehe
         etc-mupibox-gehoert-dem-backend) — das Backend schreibt sie DIREKT,
         `sudo mv` ist nicht noetig.

      WAS DIE BOX UNBRAUCHBAR MACHT (vor dem Schreiben pruefen, nicht danach):
        * interfacelogin.state = true bei LEEREM password -> niemand kommt mehr
          herein. Der realistische Fall: eine Box ohne je gesetztes Passwort,
          bei der jemand die Anmeldung einschaltet.
        * mupibox.audioDevice leer -> kein Ton.
        * startVolume > maxVolume.
      Eine Konfiguration, die eine dieser Bedingungen verletzt, gar nicht erst
      schreiben — auch nicht auf ausdruecklichen Wunsch.

      FALLSTRICK BEI DEN AUSWAHLLISTEN: `installedThemes` ist eine Liste von
      ZEICHENKETTEN, `googlettslanguages` aber eine Liste von OBJEKTEN
      ({ "iso639-1": "de", "Language": "German" }). Wer nur auf Zeichenketten
      filtert, bekommt eine leere Sprachauswahl und lehnt jeden Wert ab. Am
      gerenderten Formular aufgefallen, nicht im Test — Formulare anschauen.

      BEWUSST AUSSEN VOR: die Bildschirm-Drehung. Sie steht in /boot/config.txt,
      nicht hier, und ein falscher Wert dort ist ein schwarzer Bildschirm nach
      dem Neustart (mupi-config-txt-treiber-auskommentiert). mupi.php schreibt
      sie ueber `sudo su - dietpi -c "… G_CONFIG_INJECT … $_POST …"` — also
      zusaetzlich eine Einschleusstelle.
    source: "am Fork gebaut und gegen eine echte Konfigurationsdatei geprueft (2026-07)"

  - kind: note
    id: mupi-bootwache-drehung
    title: "Rueckfall fuer riskante /boot/config.txt-Aenderungen (Bootwache)"
    body: |
      Vorarbeit, damit die Bildschirm-Drehung ueberhaupt in einer Oberflaeche
      angeboten werden darf. Ein falscher Wert dort ist ein schwarzer Bildschirm
      nach dem Neustart, und der Weg zurueck fuehrt sonst ueber Kartenleser und
      fremden Rechner.

      DIE FEINHEIT, DIE DEN GANZEN ENTWURF BESTIMMT: ein schwarzer Bildschirm
      BOOTET SAUBER. Netz, SSH, Dienste laufen — nur die Anzeige ist tot. Eine
      Wache, die "hat das System gebootet?" fragt, nickt also genau den Fehler
      ab, den sie verhindern soll. Sie muss an einer BESTAETIGUNG haengen.

      ABLAUF:
        1. vor der Aenderung die laufende Fassung als config.txt.gut sichern,
           und zwar auf der BOOT-Partition (FAT, von jedem Rechner lesbar) —
           das bleibt der Rettungsweg, wenn die Box gar nicht mehr hochkommt
           und keine Wache mehr laufen kann
        2. Marke setzen: "unbestaetigt, seit <Zeit>"
        3. wer sieht, dass die Anzeige stimmt, bestaetigt (geht auch vom Laptop,
           das Netz laeuft ja) -> Marke weg, neue Fassung wird die bewaehrte
        4. keine Bestaetigung binnen Frist (15 min) -> zuruecknehmen + Neustart

      ALS TIMER, NICHT ALS STARTDIENST. Die Frist laeuft im BETRIEB ab: Box
      startet neu, kommt hoch, und erst dann vergehen die 15 Minuten. Ein
      Startdienst erlebt diesen Moment nie.

      ZWEI VERWEIGERUNGEN, die wichtig sind:
        * Marke OHNE Sicherung -> nichts einspielen. Raten waere schlimmer als
          warten.
        * Marke mit Zeitstempel aus der ZUKUNFT (ein Pi ohne Echtzeituhr
          springt) gilt als abgelaufen, sonst wartet sie ewig.

      Beim Zuruecknehmen wird die kaputte Fassung als config.txt.verworfen
      aufgehoben (Fehlersuche) und os.sync() gerufen — eine Wiederherstellung,
      die nur im Zwischenspeicher steht, war schon einmal der Grund, warum die
      Box beim naechsten Start wieder die kaputte Fassung hatte (siehe
      mupi-stromausfall-bootdateien).

      NACHTRAG — DER ZEITGEBER IST NICHT DER NORMALFALL. Wenn die Dienste
      laufen, ist die Box ERREICHBAR: SSH geht, die Verwaltung im Browser geht.
      Und genau das ist ja der Fehlerfall — ein schwarzer Bildschirm bootet
      sauber. Man nimmt also sofort zurueck, statt 15 Minuten zu warten:

        ueber SSH:         sudo /opt/mupibox-tools/bootwache.py --zuruecknehmen
        in der Verwaltung: System -> "Bildschirm-Drehung zuruecknehmen"

      Beides ruft DIESELBE Stelle. Eine automatische Ruecknahme wuerde die Box
      dagegen von selbst neu starten — womoeglich mitten im Lied, und wer die
      neue Drehung absichtlich wollte, aber nichts von der Bestaetigung wusste,
      bekaeme sie kommentarlos weggenommen. Der Zeitgeber ist deshalb OPT-IN
      und nur fuer Boxen OHNE SSH gedacht, bei denen der Bildschirm die einzige
      Bedienung ist. Der eigentliche Kern ist die SICHERUNG (config.txt.gut auf
      der FAT-Partition), nicht die Automatik.

      FALLE BEIM TESTEN (echter Fehler, vom Test gefunden):
      `def bootdir(kandidaten=BOOT_KANDIDATEN)` bindet die Vorgabe BEIM
      DEFINIEREN. Ein Test, der die Modulvariable umsetzt, wirkt dann nicht —
      er lief still gegen das ECHTE /boot und bestand aus dem falschen Grund.
      Vorgabe erst im Rumpf holen.

      Stand: remote-step-installer/tools/mupibox-bootwache.py + .service + .timer im
      remote-step-installer, 29 Tests. Die Wache muss sich erst an der echten
      Box als harmlos erweisen, bevor die Drehung sich auf sie verlaesst.
    source: "entworfen und getestet am Fork (2026-07), noch nicht am Geraet erprobt"

  - kind: signature
    id: mupi-api-config-gibt-alles-preis
    title: "/api/config liefert die GANZE Konfiguration aus — samt Passwort-Hash und Token"
    sev: high
    match: "api/config.*(passwort|token|apiKey)|Konfiguration.*komplett.*ausgeliefert"
    cause: "der Weg gibt mupiboxconfig.json unveraendert zurueck; darin stehen interfacelogin.password, spotify.refreshToken und jellyfin.apiKey"
    fix: "Geheimnisse vor der Antwort leeren (Form beibehalten), z.B. ohneGeheimnisse() in konfiguration.ts"
    tags: [api, geheimnisse, konfiguration]
    body: |
      GET /api/config antwortet mit dem kompletten Inhalt von
      /etc/mupibox/mupiboxconfig.json. Darin stehen:
        interfacelogin.password  (bcrypt-Hash des Verwaltungspassworts)
        spotify.refreshToken / accessToken / clientSecret
        jellyfin.apiKey
        telegram.token, mqtt.password
      Vor der Einfuehrung des Anmeldetors war der Weg aus dem ganzen LAN offen.

      DER PUNKT, DER DIE BEHEBUNG EINFACH MACHT: der Weg hat genau EINEN
      Verbraucher — src/frontend-box/src/app/media-cache.service.ts — und der
      liest daraus genau EIN Feld: `spotifyCacheLevel` (auf oberster Ebene).
      Nachgeprueft per Suche ueber src/, AdminInterface/ und scripts/.

      Behebung: die bekannten Geheimnisse LEEREN statt zu loeschen — die Form
      bleibt gleich, damit nichts stolpert, das die Felder erwartet.

      MERKSATZ FUER NEUE FELDER: ein Zugangsmerkmal gehoert nie in eine
      Antwort. In der neuen Verwaltung gibt es dafuer die Feldart 'geheim' —
      sie ist NUR schreibbar, die Antwort sagt bloss { gesetzt: true|false }.
      Auch die Rueckmeldung "vorher -> nachher" muss geschwaerzt werden, sonst
      ist es dasselbe Leck durch die Hintertuer.
    source: "am Fork gefunden und Ende zu Ende geprueft: drei eingebaute Geheimnisse tauchen in keiner Antwort mehr auf (2026-07)"

  - kind: note
    id: mupi-spotify-jellyfin-in-neuer-verwaltung
    title: "Spotify und Jellyfin in der neuen Verwaltung"
    body: |
      SPOTIFY ist im Grunde nur eine Drehscheibe: die eigentliche Anmeldung
      laeuft in der App der Box selbst (PKCE, ohne Client-Secret). In die
      Verwaltung gehoeren deshalb nur Client-ID, der Verbindungszustand und ein
      RICHTIGER Link zu dieser Anmeldung.

      Der Link ist die Stelle, an der man sich vertut — und schon vertan hat:
      Spotify nimmt eine Rueckleitung NUR ueber https an, und https liegt auf
      8443, nicht auf 8200. Ausserdem muss der Link zur BOX zeigen, auch wenn
      die Verwaltung gerade vom Laptop aus offen ist: Host aus der aktuellen
      Adresse, aber immer https + TLS-Port.

      UND ER DARF KEINE BEDINGUNG TRAGEN (nachgetragen 03.08.2026). Beim Umzug
      der Spotify-Felder von der Konfigurationsseite auf die neue Seite
      „Streaming-Dienste" bekam der Link ein „nur zeigen, wenn noch nicht
      eingerichtet" mit. Das klingt aufgeraeumt und nimmt genau den Rueckweg
      weg, den man am dringendsten braucht: Ein abgelaufener Zugang
      (box-token-laeuft-ab-sdk-laeuft-trotzdem) und eine uebernommene fremde
      Anmeldung (librespot-fremdes-konto-uebernommen) lassen das
      Zugangs-Merkmal beide STEHEN — der Stand bleibt „bereit", der Link
      verschwindet, und neu anmelden ginge nur noch, wenn man die Adresse
      auswendig kennt.

      Die Lehre ist allgemeiner als Spotify: „eingerichtet" heisst in dieser
      Verwaltung „die Angaben sind vollstaendig", nicht „es funktioniert" — die
      Seite fragt ausdruecklich NIEMANDEN an. Einen Bedienweg an so einen Stand
      zu haengen, blendet ihn genau in dem Moment aus, in dem die Angaben da
      sind und trotzdem nichts geht.

      ZWISCHENSPEICHER-STUFE: `spotifyCacheLevel` (off | medium | max) liegt
      auf der OBERSTEN Ebene der Konfiguration, nicht unter `spotify` — weil
      media-cache.service.ts es genau so aus /api/config liest. Wer es
      verschiebt, schaltet den Zwischenspeicher still ab.

      JELLYFIN hat in der Vorlage GAR KEINE Gruppe; jellyfin.php legt sie bei
      Bedarf an: { server, apiKey }. Der Server wird geprueft (nur http/https,
      abschliessende Schraegstriche abschneiden — der Jellyfin-Aufruf haengt
      seinen eigenen Pfad an, und mancher Server antwortet auf einen doppelten
      Schraegstrich mit 404). Der apiKey ist ein Geheimnis und damit
      schreib-only (siehe mupi-api-config-gibt-alles-preis).
    source: "am Fork gebaut und im Browser geprueft (2026-07)"

  - kind: signature
    id: mupi-bluetooth-skripte-kaputt
    title: "Bluetooth koppelt nicht / verbindet sich nie wieder (drei Fehler in den alten Skripten)"
    sev: high
    match: "bluetooth.*(koppelt nicht|verbindet nicht|paired-devices|Invalid command)"
    cause: "die mitgelieferten *_bt.sh-Skripte rufen einen in BlueZ 5.82 entfernten Befehl und enthalten zwei Tippfehler"
    fix: "bluetoothctl direkt rufen (mit Agent), Erfolg aus der AUSGABE lesen, MAC streng pruefen"
    tags: [bluetooth, bluez, admin]
    body: |
      Am Geraet nachgemessen (Pi 5, BlueZ 5.82). Alle drei Punkte einzeln
      belegt, nicht vermutet:

      1. `autoconnect_bt.sh` ruft `bluetoothctl paired-devices`. Antwort in
         5.82: „Invalid command in menu main: paired-devices". Der Befehl
         heisst jetzt `devices Paired`. Das automatische Wiederverbinden lief
         also NIE.

         NACHTRAG 2026-07-27: es hatte sogar ZWEI voneinander unabhaengige
         Todesursachen. Noch VOR der ersten bluetoothctl-Zeile wartet das
         Skript in einer Endlosschleife auf einen Prozess namens
         `chromium-browser` — den gibt es hier nicht, das Debian-Paket nennt
         die Datei `chromium` (`pgrep -f chromium-browser` -> 0 Treffer). Es
         waere also nie ueber die Warteschleife hinausgekommen. Ausgeliefert
         war der Dienst ohnehin `disabled`. Skript und Unit sind jetzt aus dem
         Fork ENTFERNT (Ersatz: mupi-bt-reconnect); wer in einem fremden
         MuPiBox-Abbild danach sucht, findet es dort weiterhin.

      2. `pair_bt.sh` schreibt `defaut-agent` statt `default-agent`. Der
         Standard-Agent wird damit nie gesetzt — Geraete mit „Just Works"-
         Kopplung koppeln nicht. Ein zweiter Tippfehler in derselben Datei
         (`ouput=$(...)` gesetzt, `echo $output` ausgegeben) sorgt dafuer, dass
         die Ausgabe IMMER leer ist und man nie erfaehrt, was schiefging.

      3. `bluetooth.php` schiebt $_POST['bt_device'] bzw. ['remove_mac']
         unquotiert in einen sudo-Aufruf (Zeilen 29 und 42).

      WAS AM GERAET GILT (geprueft):
        * `bluetoothctl` laeuft als `dietpi` OHNE sudo — auch koppeln.
          (dietpi ist in dietpi/audio/video/render, NICHT in bluetooth.)
        * `bluetoothctl --timeout N scan on` blockiert N Sekunden; danach
          stehen die Funde ohnehin in `bluetoothctl devices` — die Suchausgabe
          muss man gar nicht zerlegen.
        * `bluetoothctl info <mac>` liefert ein `Icon:`-Feld (audio-card,
          audio-headset …). Das ist verlaesslicher und einfacher als den
          Klassencode zu zerlegen.
        * `bluetoothctl` endet auch bei MISSERFOLG oft mit Code 0 und sagt das
          Ergebnis nur im Text. Erfolg deshalb aus der AUSGABE lesen — und ohne
          klares Wort lieber Misserfolg annehmen.
        * `hcitool` ist hier noch vorhanden (scan_bt.sh nutzt es), aber
          veraltet und findet kein BLE. Nicht verwenden.

      BEDIENUNG, aus der Praxis: eine 10-Sekunden-Suche findet auf dieser Box
      6 Geraete, davon 3 ohne Namen (BlueZ setzt dann die Adresse mit
      Bindestrichen als Ersatznamen: Handys, Fitnessbaender, Beacons). Diese
      wegblenden, sonst sucht man seinen Lautsprecher zwischen Zahlenreihen.
      Und koppeln/vertrauen/verbinden in EINEM Zug ausfuehren — ohne `trust`
      verbindet sich ein Lautsprecher nach dem Einschalten nie wieder allein.

      OFFEN: dass der Ton danach auch AUS dem Lautsprecher kommt, ist eine
      eigene Frage (bluealsa laeuft auf der Box; die Umschaltung zwischen
      internem Verstaerker MAX98357A und BT-Ausgabe ist noch nicht gebaut).
    source: "am Geraet gemessen und die neue Seite live dagegen geprueft (2026-07)"

  - kind: note
    id: mupi-tonausgabe-umschalten
    title: "Ton auf einen Bluetooth-Lautsprecher umschalten (ALSA, ohne PipeWire)"
    body: |
      Am Geraet ausgemessen (Pi 5, bluealsa, kein laufendes PulseAudio):

        /etc/asound.conf  default -> softvol "Master" -> plughw:MAX98357A
        bluealsa bietet   <mac>  A2DP (SBC) 48 kHz
        Ton erzeugen      mplayer (-slave -idle) und Chromium — BEIDE ohne
                          eigenes -ao, also am ALSA-Standard

      DER KNIFF: `softvol` funktioniert AUCH VOR bluealsa, und der Regler darf
      dabei auf der MAX98357A-Karte bleiben (die existiert unabhaengig davon,
      ob sie den Ton ausgibt). Am Geraet geprueft: es spielt, und `Master`
      regelt. Damit bleibt es bei EINEM Lautstaerkeregler — alle Skripte, die
      `amixer … Master` rufen (chromium-autostart.sh, mupi_startup.sh,
      shutdown_sound.sh) funktionieren unveraendert weiter. Ohne diesen Kniff
      haette man zwei Regler mit verschiedener Wirkung.

      Bluetooth-Fassung:
        pcm.mupibox {
            type softvol
            slave.pcm { type bluealsa; device "<MAC>"; profile "a2dp" }
            control.name "Master"
            control.card "MAX98357A"
        }

      ZWEI GRENZEN, die man kennen muss:
        1. Der Kanal ist BELEGT, solange gespielt wird ("Device or resource
           busy"). Das Umschalten wirkt erst, wenn die Tonerzeuger neu oeffnen
           — also pm2 restart spotify-control + restart_kiosk.sh, und eine
           laufende Wiedergabe bricht dabei ab.
        2. Faellt der Lautsprecher aus, waehrend er eingestellt ist, ist die
           Box STUMM. ALSA kennt keinen selbsttaetigen Rueckfall. Deshalb:
           Bluetooth nur waehlbar, solange das Geraet VERBUNDEN ist, und der
           Weg zurueck auf intern immer offen.

      PIPEWIRE waere die saubere Loesung fuer beides (Stroeme live verschieben,
      automatischer Rueckfall) — aber das ganze Lautstaerkemodell der Box haengt
      an diesem einen softvol-Regler, und ein Stack-Wechsel laesst sich aus der
      Ferne nicht gefahrlos erproben. Bewusst verschoben. Wieder aufgreifen,
      wenn der RECORDER kommt: dafuer will man einen Monitor des Ausgangs, und
      genau das liefert PipeWire nebenbei, waehrend man mit blankem ALSA
      Loopback-Geraete basteln muss.

      NEBENBEFUND: die in mupiboxconfig.json eingetragenen Klaenge
      (mupibox.startSound / shutSound -> …/sysmedia/sound/startup.wav bzw.
      shutdown.wav) EXISTIEREN auf der Box nicht.
    source: "am Geraet gemessen und die neue Seite live dagegen geprueft (2026-07)"

  - kind: signature
    id: mupi-kiosk-restart-ueber-ssh-toetet-x
    title: "Kiosk-Neustart ueber SSH toetet X und startet ihn nicht wieder"
    sev: high
    match: "xserver terminated|X.*terminated.*startet nicht|Bildschirm schwarz nach restart_kiosk"
    cause: "restart_kiosk.sh startet chromium-autostart.sh im HINTERGRUND; endet die SSH-Sitzung, nimmt SIGHUP den neuen Prozess mit"
    fix: "nicht ueber SSH neu starten — oder abgekoppelt; im Zweifel `sudo systemctl reboot`, das ist der Weg, den die Box taeglich geht"
    tags: [kiosk, x11, ssh, dietpi]
    body: |
      PASSIERT (2026-07, am Geraet): nach

        ssh dietpi@box "sudo -u dietpi /usr/local/bin/mupibox/restart_kiosk.sh &"

      war der Bildschirm schwarz. Im Xorg-Log stand sauber
      „Server terminated successfully (0)", und es lief WEDER xinit NOCH chromium.

      WARUM: restart_kiosk.sh macht `killall -r chromium` — und weil Chromium
      der Client von xinit ist, endet damit auch X. Danach startet das Skript
      chromium-autostart.sh im HINTERGRUND. Endet die SSH-Sitzung, bekommt
      diese Prozessgruppe SIGHUP und stirbt, bevor X wieder oben ist.

      Ein `setsid nohup … & disown` half NICHT: DietPi startet den Kiosk ueber
      AUTOSTART-INDEX 11, also aus einer Anmeldesitzung auf der Konsole
      (`dietpi-autostart` ist als Dienst INACTIVE). Aus einer SSH-Sitzung fehlt
      die tty; im Protokoll steht dann „TERM environment variable not set".

      WAS FUNKTIONIERT:
        * `sudo systemctl reboot` — nach rund 20 s ist die Box wieder da, und
          der normale Bootweg bringt den Kiosk zuverlaessig hoch.
        * Derselbe Aufruf AUS EINEM DIENST heraus (z.B. die Node-API unter pm2)
          funktioniert einwandfrei — dort gibt es keine SSH-Sitzung, die
          endet. Am Geraet gegengeprueft: die Verwaltung schaltete die
          Tonausgabe um, startete den Kiosk neu, und er blieb oben.

      NACHTRAG — AUCH AUS EINEM DIENST GEHT ES NICHT. Der erste Eindruck
      taeuschte: ein Neustart aus der Node-API (pm2) schien zu funktionieren,
      der Kiosk starb aber ebenfalls, nur verzoegert. Der Grund ist derselbe
      und liegt tiefer: chromium-autostart.sh ruft `startx … -- tty2` und
      braucht damit eine KONSOLE. Beim Booten kommt die aus einer
      root-Anmeldung auf tty1 (der laufende Kiosk ist root, PPID 1) — ein
      Dienst hat keine.

      MERKSATZ: den Kiosk GAR NICHT aus der Ferne neu starten. Wer die Anzeige
      erneuern will, startet die BOX neu (`systemctl reboot`, ~25 s, geht
      immer). In der neuen Verwaltung gibt es aus genau diesem Grund keinen
      Knopf „Anzeige neu starten" mehr — einer, der die Anzeige zuverlaessig
      killt und nicht zurueckholt, ist schlimmer als keiner.

      Leichtere Wege gibt es auf dieser Box nicht: xdotool, wmctrl und ein
      Chromium-Fernsteuerport fehlen alle, die laufende Seite laesst sich also
      auch nicht einfach neu laden.

  - kind: note
    id: mupi-bt-ausgabe-ueberlebt-neustart-stumm
    title: "Nach dem Neustart stumm: Bluetooth-Ausgabe bleibt eingestellt, der Lautsprecher ist aus"
    body: |
      IN DER PRAXIS EINGETRETEN (2026-07), nicht nur befuerchtet: die Box war
      auf Bluetooth-Ausgabe gestellt, wurde neu gestartet — und war stumm.
      /etc/asound.conf ist persistent, der Lautsprecher aber ausgeschaltet.
      Ein Verbindungsversuch endete mit
      `org.bluez.Error.Failed br-connection-page-timeout`.

      ALSA kennt keinen selbsttaetigen Rueckfall. Die Verwaltung erkennt den
      Zustand (`/api/ton` meldet `stumm: true`) und der Rueckweg auf den
      internen Verstaerker ist ein Klick — aber wer das nicht weiss, steht vor
      einer stummen Box.

      ERSCHWEREND: das automatische Wiederverbinden funktionierte auf dieser
      Box ohnehin nicht, weil autoconnect_bt.sh `bluetoothctl paired-devices`
      ruft — den Befehl gibt es in BlueZ 5.82 nicht mehr (siehe
      mupi-bluetooth-skripte-kaputt).

      ERLEDIGT (2026-07-27), auf ZWEI Wegen, und beide zaehlen:

        * UNTER PIPEWIRE IST DIE PRAEMISSE WEG. Es gibt keine asound.conf mehr,
          und ein ausgeschalteter Lautsprecher ist schlicht KEINE Senke —
          PipeWire spielt auf der internen weiter. Der Fall „eingestellt, aber
          aus, also stumm" kann so nicht mehr entstehen. Die geplante Wache
          („auf intern zurueckschreiben") ist damit gegenstandslos; wer sie
          unter PipeWire nachruestet, streitet nur mit PipeWire.
        * Das Wiederverbinden macht jetzt mupibox-bt-reconnect.timer
          (remote-step-installer, siehe mupi-bt-reconnect). autoconnect_bt.sh
          und sein Dienst sind aus dem Fork ENTFERNT.
    source: "am Geraet erlebt und behoben (2026-07); unter PipeWire nachgemessen (2026-07-27)"

  - kind: signature
    id: mupi-fortschrittsbalken-springt
    title: "Der Liedfortschritt springt statt zu laufen (ion-range rastet auf ganze Prozent)"
    sev: low
    match: "Fortschritt.*(springt|schrittweise|ruckelt)|Balken.*Schritte"
    cause: "<ion-range step=\"1\"> rastet auf ganze Prozent — bei einem Drei-Minuten-Lied ein Sprung alle ~1,8 s"
    fix: "step=\"0.1\" an beiden Reglern UND den Wert gebrochen liefern; ein CSS-Uebergang allein hilft nicht"
    tags: [frontend, player, kosmetik]
    body: |
      DER IRRWEG ZUERST, weil er naheliegt: ein `transition: width 1s linear`
      auf ::part(bar-active) und ::part(knob) sieht nach der Loesung aus — der
      Wert kommt schliesslich im Sekundentakt (updateProgress legt sich mit
      setTimeout(…, 1000) selbst wieder vor). Es half NICHT.

      DER WIRKLICHE GRUND ist die AUFLOESUNG, nicht die Animation:
      `<ion-range step="1">` rastet auf ganze Prozent. Bei einem
      Drei-Minuten-Lied ist 1 % rund 1,8 Sekunden — der Balken kann sich also
      gar nicht oefter bewegen, egal wie weich man ihn animiert.

      BEHEBUNG (beide Stellen: player.page.html und die Now-Playing-Leiste):
        * step="0.1" statt step="1"
        * den Wert gebrochen liefern. Bei Spotify war er es schon
          ((progress_ms/dur)*100); LOKAL kam er aus `lo.progressTime`, und das
          ist GANZZAHLIG — deshalb dort aus elapsed/duration rechnen und
          progressTime nur als Rueckfall behalten (now-playing.ts).
        * der 1-s-Uebergang bleibt sinnvoll, aber nur MIT der feineren
          Rasterung. Bei Spruengen (Titelwechsel 98 % -> 0 %, Spulen) muss er
          unterdrueckt werden, sonst wandert der Balken rueckwaerts durchs
          Bild — dafuer eine Klasse, die nur bei kleinem Vorwaertsschritt sitzt.

  - kind: signature
    id: mupi-player-poppt-bei-titelwechsel-auf
    title: "Der volle Player klappt bei jedem Titelwechsel auf"
    sev: med
    match: "Player.*(poppt|klappt).*(auf|hoch)|springt.*Player.*Titelwechsel"
    cause: "trackChangeDetected$ feuert bei JEDEM Wechsel; die einzige Bremse war 'kein App-Befehl seit 5 s' — das erfuellt ein Lied, das von selbst endet, immer"
    fix: "nur bei NEU beginnender Wiedergabe navigieren: vorher still (kein Vortitel oder pausierter Vorzustand)"
    tags: [frontend, player, spotify]
    body: |
      ExternalPlaybackNavigatorService soll den vollen Player oeffnen, wenn
      jemand von aussen auf die Box castet. Seine Bedingung war:

        filter(() => Date.now() - playerService.lastLocalCommandAt > 5000)

      Gedacht war das gegen den „Weiter"-Knopf der Mini-Leiste (der stempelt
      lastLocalCommandAt). Ein Lied, das VON SELBST endet, erzeugt aber
      ebenfalls keinen App-Befehl — also galt jeder normale Titelwechsel als
      „von aussen gestartet".

      Die fehlende Unterscheidung ist NICHT die Zeit, sondern: lief vorher
      schon etwas? Beim natuerlichen Uebergang gibt es einen Vortitel UND einen
      unpausierten Vorzustand; bei einer wirklich neu erscheinenden Wiedergabe
      nicht. Diese Entscheidung gehoert dorthin, wo der Vorzustand bekannt ist
      (spotify-player.service, wo previousPlayerState ohnehin gefuehrt wird),
      und wird vom Navigator nur noch abgefragt.

      `trackChangeDetected$` selbst NICHT einschraenken — media.service haengt
      auch daran und braucht jeden Wechsel.

  - kind: note
    id: mupi-fork-ausliefern-ablauf
    title: "Fork auf die Box ausliefern: der Ablauf, der funktioniert"
    body: |
      Zusammengetragen aus einer Sitzung, in der zweimal der Bildschirm schwarz
      wurde. Die Reihenfolge ist nicht Geschmack, jeder Schritt hat einen Grund.

      1. IN EIN EIGENES VERZEICHNIS BAUEN. src/deploy gehoert root (Ueberbleibsel
         eines sudo-Baus, siehe mupi-src-deploy-gehoert-root), also:
           esbuild … --outfile=$T/server.js
           ng build --output-path=$T/www-admin      (Verwaltung)
           ng build --configuration production --output-path=$T/box  (Box-Oberflaeche)
         Kein sudo noetig.

      2. ERST HOCHLADEN, DANN PRUEFEN, DANN UMSCHALTEN. Nach `www.neu`/
         `server.js.neu` kopieren und `test -f …/index.html && test -s …/server.js`
         — ERST danach umbenennen. Grund: ein fehlgeschlagener ng build LEERT
         das Zielverzeichnis vorher; wer blind ausliefert, schiebt einen leeren
         Ordner auf die Box (/admin antwortete danach 404).

      3. BEI www DIE NUTZERTEILE RETTEN. `theme-data/` und der Symlink
         `active_theme.css` sind KEIN Bauergebnis. Vor dem Tausch in den neuen
         Ordner kopieren. Vollstaendig ersetzen statt ueberschreiben — sonst
         sammeln sich gehashte Altlasten (siehe www-altlasten-…), aber dann
         gehen auch von Hand geflickte Dateien verloren (genau so verschwand
         der Grauton der Leisten; er gehoert deshalb in die QUELLE).

      4. BACKEND NEU STARTEN: `pm2 restart server`. Unbedenklich.

      5. DIE ANZEIGE NICHT ANFASSEN. Nicht per SSH, NICHT aus einem Dienst —
         siehe mupi-kiosk-restart-ueber-ssh-toetet-x. Wenn die Box-Oberflaeche
         neu geladen werden muss: `sudo systemctl reboot` (~25 s, geht immer).

      6. DANACH ALLE SEITEN PRUEFEN, nicht nur die geaenderte. Zweimal in
         dieser Sitzung war das Geprüfte heil und etwas Ungeprüftes kaputt.

      STOLPERSTEIN BEIM HERKUNFTSSTEMPEL: `grep -c … || echo 0` liefert bei
      NULL Treffern ZWEI Zeilen — grep gibt „0" aus UND endet mit Code 1, dann
      feuert das `||` zusaetzlich. Das zerriss die herkunft.json, und die
      Aktualisierungsseite meldete „Herkunft unbekannt". Faellt lokal nie auf,
      weil der Arbeitsbaum meist schmutzig ist; die Box hatte einen sauberen.
      `grep -v … | wc -l` endet immer mit 0.
    source: "in einer Ausliefer-Sitzung am Geraet zusammengetragen (2026-07)"

  - kind: howto
    id: mupi-wlan-gebootete-karte-umstellen
    title: "WLAN einer BEREITS GEBOOTETEN Karte umstellen (am Kartenleser)"
    body: |
      Die Box zieht um, das Netz heisst anders — und die Karte war schon einmal
      an. Dann fuehren drei Dateien in die Irre, und ausgerechnet die einzige
      frei beschreibbare ist die wirkungslose:

        /boot/firmware/dietpi-wifi.txt  FAT, gehoert dem Anwender. Wird aber NUR
                                        beim allerersten Start ausgewertet
                                        (dietpi-firstboot). Attrappe.
        /boot/add_wifi.json             liest mupi_wifi.service bei JEDEM Start
                                        (startup.sh). Steht aber im Normalfall
                                        auf dem Vorlagentext "Your Wifi-Name"
                                        und ist damit inaktiv; ausserdem
                                        root:root auf ext4. Attrappe.
        /etc/wpa_supplicant/wpa_supplicant.conf   DAS HIER. root:root, 0600.

      Es geht also nicht ohne einen privilegierten Schreibvorgang. Fertig dafuer:

        udisksctl mount -b /dev/sdX2          # die ext4-Partition
        python3 controller/sdwlan.py --trocken
        python3 controller/sdwlan.py
        udisksctl unmount -b /dev/sdX2

      (remote-step-installer, controller/sdwlan.py; nimmt ohne Argumente das
      AKTIVE WLAN dieses Rechners und findet die Karte selbst.)

      DAS ALTE NETZ BLEIBT STEHEN — das ist Absicht, keine Schlamperei. In
      /etc/network/interfaces ist eth0 auskommentiert (`#allow-hotplug eth0`),
      WLAN ist der EINZIGE Weg in die Box. Waere der neue Schluessel falsch,
      muesste die Karte wieder in den Leser. Zwei Netzbloecke nebeneinander
      kosten nichts; `priority=2` laesst das neue gewinnen, solange beide
      sichtbar sind.

      IN DIE DATEI GEHT NUR DER HASH, nie der Klartext — siehe
      mupi-wlan-schluessel-ohne-klartext. Die Box macht es in startup.sh selbst
      so (sie verwirft die `#psk="…"`-Zeile von wpa_passphrase).

      VORHER PRUEFEN, ob das Zielnetz ueberhaupt auf 2,4 GHz sendet:
        nmcli -f SSID,CHAN,FREQ dev wifi list
      Ein reines 5-GHz-Netz waere fuer aeltere Pis unerreichbar (Pi 5 kann
      beides). Faellt sonst erst auf, wenn die Box stumm bleibt.

      DANACH: die Box holt sich eine NEUE IP, die alte gilt nicht mehr. Die
      Verwaltung ist dann unter http://<neue-IP>:8200/admin/.
    source: "am Geraet durchgefuehrt (2026-07), Karte eines laufenden Pi 5"

  - kind: howto
    id: mupi-wlan-schluessel-ohne-klartext
    title: "WLAN-Schluessel besorgen und zum PSK rechnen, ohne ihn irgendwo abzulegen"
    body: |
      Der Rechner KENNT das Passwort ja bereits — es muss niemand abtippen (ein
      Tippfehler faellt sonst erst auf, wenn die Box nach dem Neustart stumm
      bleibt). Drei Schritte, jeder mit einer Falle:

      1. HOLEN — und zwar mit `-e no`:

           nmcli -s -e no -g 802-11-wireless-security.psk connection show "$SSID"

         `-g` bedeutet Terse-Modus, und dort maskiert nmcli ':' und '\' mit
         einem Backslash (man nmcli: `--escape`, Vorgabe **yes**). Ein Passwort
         mit Doppelpunkt kaeme als `a\:b` zurueck und landete FALSCH auf der
         Karte. Betraf auch sdprep.wifi_secret() — dort seit 2026-07 behoben.
         Siehe nmcli-terse-maskiert-doppelpunkt.

         Kommt nichts zurueck: `psk-flags: 1` heisst agent-owned, das Passwort
         liegt im Schluesselbund des Anwenders, NICHT in
         /etc/NetworkManager/system-connections/. Dann kommt auch `sudo` nicht
         daran — es muss im Benutzerkontext geholt werden.

      2. RECHNEN. WPA-PSK ist woertlich PBKDF2-HMAC-SHA1, 4096 Runden, 32 Byte,
         mit der SSID als SALZ (IEEE 802.11i H.4):

           hashlib.pbkdf2_hmac("sha1", pw.encode(), ssid.encode(), 4096, 32).hex()

         NICHT `wpa_passphrase` aufrufen: das Werkzeug liest die Passphrase von
         einem TTY und bricht ohne eines ab ("tcgetattr: Inappropriate ioctl for
         device"); als Argument stuende sie in der Prozessliste.

         NetworkManager legt je nach Netz die Passphrase ODER den fertigen
         64-Hex-PSK ab. Letzteren nochmal zu hashen ergaebe stillen Unsinn —
         auf 64 Hex-Zeichen pruefen und durchreichen.

      3. GEGENPRUEFEN, bevor die Karte zurueck ins Geraet geht. Ein falscher
         Hash faellt sonst erst auf, wenn die Box nicht mehr ins Netz kommt:

           wpa_passphrase IEEE password
           -> f42c6fc52df0ebef9ebb4b90b38a5f902e83fe1b135a70e23aed762e9710a12e
           wpa_passphrase ThisIsASSID ThisIsAPassword
           -> 0dc0d6eb90555ed6419756b9a15ec3e3209b63df707dd508d14581f8982721af

         (tests/sdwlan_test.py prueft genau diese Vektoren.)

      DER HASH IST SO GUT WIE DAS PASSWORT — er reicht zum Verbinden
      vollstaendig. Also nicht ins Protokoll, nicht auf den Bildschirm, nicht in
      ein Sitzungsmitschnitt. In einer Sitzung ist genau das passiert, weil eine
      Maskierung per sed nicht griff und der Befehl trotzdem weiterlief: erst
      maskieren PRUEFEN, dann ausgeben.
    source: "am Geraet erarbeitet (2026-07); Testvektoren aus IEEE 802.11i H.4 via wpa_passphrase"

  - kind: signature
    id: nmcli-terse-maskiert-doppelpunkt
    title: "nmcli -g liefert 'a\\:b' statt 'a:b' — maskierter Wert landet falsch weiter"
    body: |
      SIGNATUR: ein aus nmcli geholter Wert enthaelt Backslashes, die im
      Original nicht stehen — typisch vor einem ':' oder einem weiteren '\'.
      Folge: WLAN-Passwort mit Doppelpunkt wird falsch uebernommen, die Box
      kommt nach dem Erstboot nicht ins Netz. Faellt beim Schreiben NIE auf.

      URSACHE: `-g/--get-values` ist eine Abkuerzung fuer `--mode tabular
      --terse --fields`, und in Terse-Modus maskiert nmcli ':' und '\'.
      man nmcli, `-e | --escape {yes|no}`: "If omitted, default is yes."

      ABHILFE: `-e no` mitgeben.

        nmcli -s -e no -g 802-11-wireless-security.psk connection show "$SSID"

      PRUEFEN, ob man betroffen ist, ohne das Geheimnis auszugeben:

        P=$(nmcli -s -g …); printf '%s' "$P" | tr -cd '\\' | wc -c

      0 = keine Maskierung. Betrifft jedes -g-Feld, nicht nur Passwoerter —
      auch SSIDs mit Doppelpunkt (die es geben darf) und Verbindungsnamen.
    source: "beim Umstellen des WLAN einer Karte gefunden (2026-07); man nmcli"

  - kind: signature
    id: mupi-themawechsel-teilstring
    title: "Themawechsel greift nicht: setting_update.sh vergleicht per Teilstring"
    body: |
      SIGNATUR: In der Konfiguration steht das neue Thema, die Oberflaeche zeigt
      aber weiter das alte. Kein Fehler, kein Protokolleintrag — es passiert
      schlicht nichts.

      URSACHE, in scripts/mupibox/setting_update.sh:

        newTheme=$(ls -l ${THEME_FILE} | grep ${NEW_THEME})
        if (( ${#newTheme} == 0 )); then … ln -s …/${NEW_THEME}.css … ; fi

      Der Vergleich ist ein TEILSTRING-Test auf der `ls -l`-Ausgabe. Zeigt der
      Verweis auf `deepblue.css` und man waehlt `blue`, dann findet
      `grep blue` den Treffer in „deepblue.css" — die Bedingung haelt den
      Zustand fuer schon richtig und haengt den Verweis NICHT um.

      SCHLIMMER: `grep` sieht die GANZE `ls -l`-Zeile — Rechte, Eigentuemer,
      Datum, Verweisquelle UND Verweisziel:

        lrwxrwxrwx 1 dietpi dietpi 39 Jul 26 22:36 …/www/active_theme.css
          -> /home/dietpi/MuPiBox/themes/MupiNew.css

      Ein Thema namens `dietpi`, `themes`, `www`, `home` oder `Jul` wuerde
      also IMMER als „schon aktiv" gelten und sich nie einschalten lassen.

      REALE PAARE im heutigen Bestand (Name kommt in einem anderen vor):
        blue  steckt in  deepblue
        red   steckt in  darkred
        dark  steckt in  darkred
      Auf `blue`, `red` oder `dark` umzuschalten ist also genau dann kaputt,
      wenn vorher das jeweils laengere Thema aktiv war.

      ABHILFE beim Umschalten von Hand: den Verweis selbst setzen, nicht auf das
      Skript hoffen —

        ln -sfn /home/dietpi/MuPiBox/themes/<name>.css \
                /home/dietpi/.mupibox/Sonos-Kids-Controller-master/www/active_theme.css

      SAUBER waere im Skript ein Vergleich auf das Verweisziel statt auf die
      Zeile:  [ "$(readlink -f "$THEME_FILE")" = "$THEMEN/$NEW_THEME.css" ]
    source: "beim Anlegen eines eigenen Themas gelesen (2026-07)"

  - kind: howto
    id: mupi-neues-thema-anlegen
    title: "Eigenes Farbthema anlegen (drei Stellen, sonst taucht es nicht auf)"
    body: |
      Ein Thema ist EINE CSS-Datei — aber es haengt an drei Stellen, und wer
      eine vergisst, sucht lange:

      1. DIE DATEI:  /home/dietpi/MuPiBox/themes/<Name>.css
         (NICHT www/theme-data/ — dort liegen nur die Bild-Beigaben mancher
         Themen. Die eigentlichen CSS-Dateien liegen alle im obigen Ordner.)
         Eigentuemer dietpi:dietpi, 644.

      2. DIE AUSWAHLLISTE:  mupibox.installedThemes in mupiboxconfig.json.
         Eine Liste von ZEICHENKETTEN. Das Feld „Farbthema" der neuen
         Verwaltung liest seine Auswahl daraus (konfiguration.ts,
         auswahlAus: ['mupibox','installedThemes']). Fehlt der Name dort, ist
         das Thema nicht auswaehlbar, auch wenn die Datei existiert.
         ANHAENGEN, nicht `unique_by` — das sortiert die Liste um.

      3. DER VERWEIS:
         www/active_theme.css -> /home/dietpi/MuPiBox/themes/<Name>.css
         Setzt normalerweise setting_update.sh aus mupibox.theme — aber siehe
         mupi-themawechsel-teilstring, das greift nicht immer.

      Schreibweg fuer die Konfiguration: siehe mupi-konfiguration-schreiben
      (Sicherung -> Nachbardatei -> fsync -> umbenennen, danach
      dietpi:www-data 664 zuruecksetzen — als root angelegte Dateien gehoeren
      sonst root:root 644 und das Backend kann sie nicht mehr schreiben).

      WAS EIN THEMA UEBERHAUPT FAERBT (am Beispiel „blue"):
        --ion-background-color   Flaeche der Seite            (#44afe2)
        --ion-color-light        Kopfleiste, Kachel-Beschriftung, Regler (#054b61)
      Die Kopfleiste ist `<ion-toolbar color="light">`; das Thema definiert
      `--ion-color-light` um und faerbt sie damit mit. Die App selbst will dort
      `--background: #1f1f1f`, das verliert gegen die Farbklasse. OHNE Thema
      sind Leiste (#1f1f1f) und Flaeche (#121212) fast gleich — es sieht nach
      EINEM Ton aus. Erst ein Thema spreizt sie sichtbar zu zweien. Wer eine
      durchgehende Flaeche will:  ion-toolbar { --background: <BG> !important; }
    source: "am Geraet angelegt (2026-07): Thema MupiNew als Kopie von blue"

  - kind: signature
    id: rsi-agent-put-mode-verbindungsabbruch
    title: "Agent bricht bei /put die Verbindung ab statt zu antworten"
    body: |
      SIGNATUR: `a.put(...)` scheitert mit
        AgentError: Remote end closed connection without response
      Der Agent laeuft weiter, /status antwortet, der Tunnel steht. Man sucht
      im Netz — der Fehler liegt im eigenen Aufruf.

      URSACHE: put_file bekam `mode` und rechnete `int(str(mode), 8)`. Bei
      einem Python-Literal 0o755 ist das `int("493", 8)` -> ValueError. Gefangen
      wurde nur OSError, also starb der Anfrage-Handler mitten in der Antwort,
      und der Client sah nur den Abbruch.

      RICHTIG AUFRUFEN:  a.put(quelle, ziel, mode="755")   (oktal, als Zeichenkette)
      Seit 2026-07 nimmt der Agent BEIDES an (int = fertiger Modus, Zeichenkette
      = oktal) und liefert bei Unsinn eine saubere Fehlermeldung statt eines
      Abbruchs. Getestet: tests/put_test.py.

      ALLGEMEINER: ein Handler, der eine andere Ausnahme als die gefangene
      wirft, erzeugt beim Aufrufer ein Netzproblem-Bild. Wo eine Antwort
      versprochen ist, muss auch bei Unsinn eine kommen.
    source: "beim Anlegen des Themas MupiNew gestolpert (2026-07)"

  - kind: signature
    id: pm2-update-leert-die-prozessliste
    title: "Nach `pm2 update` ist die Box tot: leere Prozessliste, Dienst failed"
    body: |
      SIGNATUR: Port 8200 antwortet nicht mehr, `pm2 list` zeigt eine LEERE
      Tabelle, `systemctl status pm2-dietpi` steht auf
      `failed (Result: protocol)` mit „Start request repeated too quickly".
      Die Box selbst laeuft, SSH geht, nur MuPiBox ist weg.

      ANLASS: ein Node-Major-Wechsel (22 -> 26). `pm2 update` ist der
      dokumentierte Weg, den Verwalter-Prozess selbst auf das neue node zu
      heben — er beendet den Daemon und startet ihn neu. Die Anwendungen
      kommen dabei NUR zurueck, wenn der gespeicherte Stand
      (~/.pm2/dump.pm2) passt. Tut er es nicht, ist die Liste hinterher leer
      und niemand sagt es.

      ZWEITER, GEMEINERER TEIL: `pm2 update` und schon ein blosses
      `pm2 list` starten den Daemon AUSSERHALB von systemd. Die Unit ist
      aber `Type=forking` mit `PIDFile=/home/dietpi/.pm2/pm2.pid` und
      `ExecStart=pm2 resurrect`. Laeuft bereits ein Daemon, spaltet
      `resurrect` keinen neuen ab und schreibt keine PID-Datei -> systemd
      sieht sein Protokoll verletzt, versucht es fuenfmal und gibt auf.
      Der Dienst bleibt `failed`, obwohl die Anwendungen laufen — und das
      faellt erst beim naechsten Stromausfall auf.

      ABHILFE (stellt her UND beweist den Kaltstart, ohne Neustart):
        sudo -u dietpi pm2 save
        sudo -u dietpi pm2 kill
        systemctl reset-failed pm2-dietpi
        systemctl start pm2-dietpi        # jetzt forkt resurrect richtig
      Danach `systemctl is-active` = active und die PID-Datei muss DA sein.
      Beides pruefen — „die Anwendungen laufen wieder" heisst nicht, dass
      der Bootweg heil ist.

      BESSER GLEICH SO: beim Node-Wechsel `pm2 update` GAR NICHT benutzen.
        sudo -u dietpi pm2 save && systemctl restart pm2-dietpi
      Das hebt die Anwendungen ebenso auf das neue node (sie werden neu
      gestartet) und laesst systemd den Daemon besitzen.
    source: "am Geraet passiert beim Wechsel auf Node 26 (2026-07-26); Box war ~3 min ohne Dienste"

  - kind: howto
    id: mupi-node-major-wechsel
    title: "Node auf der Box wechseln (Major), mit Rueckweg auf der Platte"
    body: |
      Node kommt per apt aus dem nodesource-Repo (`dpkg -l nodejs` ->
      `…-1nodesource1`). Ein Wechsel ERSETZT die alte Fassung — der Rueckweg
      muss deshalb VORHER lokal liegen, sonst haengt er am Netz.

      WICHTIG ZUR QUELLE: nodesource benutzt inzwischen die Suite
      `nodistro` (distributionsunabhaengig). Der aeltere Rat aus
      mupi-nodesource-trixie („Repo-Zeile auf bookworm statt trixie") trifft
      damit NICHT mehr zu — es ist nur
      `/etc/apt/sources.list.d/nodesource.sources`, Zeile `URIs:`,
      `node_22.x` -> `node_26.x`.

      ABLAUF:
        # 1. Rueckweg sichern, SOLANGE die alte Quelle eingetragen ist
        mkdir -p /root/node-rueckweg && cd /root/node-rueckweg
        apt-get download nodejs          # ~37 MB
        cp -a /etc/apt/sources.list.d/nodesource.sources .
        # 2. umstellen
        sed -i 's|/node_22\.x|/node_26.x|' /etc/apt/sources.list.d/nodesource.sources
        apt-get update && apt-cache policy nodejs      # erst schauen
        apt-get install -y nodejs
        # 3. Version pruefen, BEVOR die Dienste angefasst werden
        node -v
        # 4. Dienste heben — NICHT `pm2 update`, siehe
        #    pm2-update-leert-die-prozessliste
        sudo -u dietpi pm2 save && systemctl restart pm2-dietpi

      RUECKWEG:
        cp -a /root/node-rueckweg/nodesource.sources /etc/apt/sources.list.d/
        apt-get update
        dpkg -i /root/node-rueckweg/nodejs_22*.deb
        sudo -u dietpi pm2 save && systemctl restart pm2-dietpi

      PRUEFEN, nicht hoffen: `pm2 jlist` auswerten — Status `online` UND
      `restart_time` = 0. Eine Absturzschleife sieht in `pm2 list` sekundenlang
      wie „online" aus; nur der kletternde Zaehler verraet sie.
    source: "am Geraet durchgefuehrt: 22.23.1 -> 26.5.0 (2026-07-26), Pi 5 / DietPi Trixie / arm64"

  - kind: howto
    id: mupi-kiosk-haengt-app-oder-browser
    title: "Oberflaeche haengt (Album nur ein Kringel): App kaputt oder nur DIESER Browser?"
    body: |
      SYMPTOM: auf dem Bildschirm der Box laedt eine Ansicht nicht — z. B. ein
      Album zeigt nur den Ladekringel. Die Versuchung ist gross, die letzte
      Aenderung zu verdaechtigen (bei uns: der Wechsel auf Node 26) und
      zurueckzurollen.

      DIE ENTSCHEIDENDE FRAGE ZUERST: ist die APP kaputt oder nur die
      BROWSER-SITZUNG auf der Box? Das laesst sich in zwei Minuten klaeren,
      ohne irgendetwas anzufassen — dieselbe App vom selben Server in einem
      ZWEITEN Browser oeffnen:

        # Umleiter, weil der Browser fuer fremde Adressen eine Freigabe will
        python3 boxproxy.py http://<box>:8200     # 127.0.0.1:8123 -> Box
        # dann 127.0.0.1:8123 oeffnen und dieselbe Ansicht anklicken

      Laeuft es dort -> die App ist gesund, es haengt die Kiosk-Sitzung.
      Haengt es dort auch -> der Fehler ist echt, jetzt lohnt die Suche.

      VORHER die billigen Gegenproben, in dieser Reihenfolge:
        curl -s -o /dev/null -w '%{http_code}' http://<box>:8200/api/data
        curl -s http://<box>:8200/api/spotify/playlist/<id> | head -c 300
      Ein 200 sagt NICHTS ueber den Inhalt — den Rumpf ansehen. Bei uns war
      beides tadellos (100 Titel mit Bildern), also lag es nicht am Backend.

      ZWEITES ANZEICHEN, das die Kiosk-Sitzung verraet: die Oberflaeche
      protokolliert ueber POST /api/logs ins Backend mit. Kommen von
      `MuPiBox-Frontend|http://localhost:8200` seit Stunden KEINE Zeilen mehr,
      obwohl Chromium laeuft, dann ist die Seite nicht mehr am Leben:

        grep -h 'MuPiBox-Frontend|http://localhost:8200' \\
             /home/dietpi/.pm2/logs/server-*.log | tail -3

      ACHTUNG, SELBSTBETRUG: wer nur nach 'MuPiBox-Frontend' greppt, findet
      auch die Zeilen des EIGENEN Umleiters (Herkunft 127.0.0.1:8123) und
      haelt sie fuer die Box. Immer auf localhost:8200 einschraenken.

      ABHILFE: `systemctl reboot` (~20-25 s). Den Kiosk NICHT einzeln neu
      starten — siehe mupi-kiosk-restart-ueber-ssh-toetet-x.

      WANN ES PASSIERT: wenn die Seite geladen wurde, waehrend das Backend
      weg war. Bei uns hatte pm2 die Dienste ~3 min nicht laufen
      (pm2-update-leert-die-prozessliste), und die Box war kurz zuvor von
      selbst neu gestartet — im Kernel-Log stehen im Minutentakt
      `Undervoltage detected!`, ein Brownout ist also die naheliegende
      Erklaerung. Die Angular-App faengt sich davon nicht von allein.
    source: "am Geraet eingekreist (2026-07-26/27): Album-Kringel nach Node-Wechsel — App war gesund, Kiosk-Sitzung tot"

  - kind: howto
    id: mupi-pipewire-umstieg
    title: "Von bluealsa auf PipeWire umsteigen — die ganze Kette"
    body: |
      Am Geraet durchgefuehrt (Pi 5 / DietPi Trixie / arm64, 2026-07-27).
      Es sind DREI Huerden, und keine davon steht in einer Anleitung
      zusammen. Wer nur „apt install pipewire" macht, hat danach keinen Ton
      und keine Ahnung, warum.

      ── HUERDE 1: ZWEI BENUTZER ERZEUGEN TON ──────────────────────────
      PipeWire ist ein BENUTZER-Dienst. Auf der Box lief aber:
        root    chromium   <- Kiosk, und darueber kommt Spotify heraus
        dietpi  mplayer/node
      Zwei Instanzen koennen sich eine Soundkarte nicht teilen. Also muss
      der Kiosk als dietpi laufen — siehe mupi-kiosk-als-dietpi-anleitung. Das ist
      der eigentliche Umbau; alles andere ist Kleinkram.

      ── HUERDE 2: DER SOFTVOL-BEHELF MUSS WEG ─────────────────────────
      Die Lautstaerke lief ueber `amixer … Master`, einen softvol-Regler,
      den init-alsa-softvol.sh beim Start erst ERZEUGEN muss (1 s Stille
      durchs Geraet). Vier Programme riefen amixer direkt auf. Erst
      buendeln (mupi-lautstaerke.sh erkennt wpctl vs. amixer selbst), DANN
      umstellen — sonst regelt der Lautstaerkeknopf hinterher nichts mehr,
      lautlos und ohne Fehlermeldung.

      ── HUERDE 3: BLUETOOTH BRAUCHT EINEN AKTIVEN SEAT ────────────────
      Siehe mupi-wireplumber-kein-bluetooth-ohne-seat. Das ist die
      teuerste: alles ist installiert, alles laeuft, und Bluetooth geht
      trotzdem nicht.

      ── ABLAUF ────────────────────────────────────────────────────────
        # 0. RUECKWEG ZUERST
        cp -a /etc/asound.conf /root/ton-vor-pipewire/
        # (+ Skript, das bluealsa und asound.conf zurueckholt)

        # 1. Kiosk auf dietpi umstellen (eigener Eintrag), Neustart, PRUEFEN

        # 2. Pakete
        apt install pipewire pipewire-pulse wireplumber libspa-0.2-bluetooth pipewire-alsa

        # 3. alten Stapel abschalten
        systemctl disable --now bluealsa bluealsa-aplay
        systemctl disable --now mupibox-alsa-init
        # ACHTUNG: DAS `disable` GENUEGT NICHT. Am 04.08.2026 nachgemessen:
        # die Unit ist disabled und laeuft trotzdem bei jedem Start, weil
        # mupibox-player.service und mupibox-server.service sie ueber
        # `Wants=` hochziehen — und ihr `amixer -q sset Master 70%` trifft
        # unter PipeWire die ECHTE Senke. Siehe
        # [[alsa-init-laeuft-obwohl-abgeschaltet]].
        mv /etc/asound.conf /etc/asound.conf.vor-pipewire   # dann gilt
        # /etc/alsa/conf.d/99-pipewire-default.conf

        # 4. PipeWire fuer Benutzersitzungen scharf
        systemctl --global enable pipewire.socket pipewire-pulse.socket wireplumber.service

        # 5. Seat-Pruefung fuer Bluetooth abschalten (Huerde 3), Neustart

      ── STOLPERSTEIN BEI SCHRITT 4 ────────────────────────────────────
      Die apt-Installation aktiviert die Sockets GLOBAL
      (/etc/systemd/user/sockets.target.wants/). PipeWire startet dann beim
      naechsten Hochfahren von selbst und streitet mit bluealsa um die
      Karte — ohne dass jemand etwas umgeschaltet haette. Wer nicht sofort
      weitermacht: `systemctl --global disable pipewire.socket
      pipewire-pulse.socket wireplumber.service pipewire-pulse.service`.

      ── WAS ES BRINGT ─────────────────────────────────────────────────
      * Der Lautsprecher wird beim Verbinden VON SELBST Standard-Senke.
      * Kein softvol-Startbehelf mehr — und damit auch nicht mehr der
        Absturzausloeser aus mupi-exec-rueckruf-toetet-dienst.
      * `pactl`-Aufrufe in chromium-autostart.sh funktionieren wieder; die
        liefen seit dem Wechsel auf blankes ALSA ins Leere (PulseAudio ist
        installiert, lief aber nie).
      * Ein ausgeschalteter Lautsprecher ist keine stumme Box mehr: er ist
        dann schlicht keine Senke.

      ── WAS DANACH NOCH NICHT PASST ───────────────────────────────────
      Die Verwaltungsseite „Tonausgabe" liest /etc/asound.conf. Unter
      PipeWire ist Umschalten ein `wpctl set-default`; das Backend muss
      nachgezogen werden.
    source: "am Geraet durchgefuehrt (2026-07-27), Pi 5 / DietPi Trixie / arm64"

  - kind: signature
    id: mupi-wireplumber-kein-bluetooth-ohne-seat
    title: "PipeWire: Bluetooth verweigert mit br-connection-profile-unavailable"
    body: |
      SIGNATUR:
        bluetoothctl connect <MAC>
        Failed to connect: org.bluez.Error.Failed br-connection-profile-unavailable
      und in `wpctl status` taucht das Geraet NIE als Senke auf — obwohl
      PipeWire laeuft, libspa-0.2-bluetooth installiert ist, bluealsa aus
      ist, bluetoothd laeuft und der Benutzer in der Gruppe `bluetooth`
      steht. Es wird schlicht kein A2DP-Endpunkt bei bluetoothd registriert.

      URSACHE: WirePlumber gibt Bluetooth-Geraete nur der Sitzung auf dem
      AKTIVEN Seat frei. Im Kiosk-Aufbau qualifiziert sich keine — am
      Geraet gemessen:

        loginctl: Sitzung 1  seat0  tty  Active=no   <- einzige mit Seat
                  Sitzung 3  -      tty  Active=yes  <- aktiv, ohne Seat

      Die tty1-Anmeldung sitzt auf seat0, wird aber inaktiv, sobald X den
      Bildschirm auf tty2 uebernimmt (`startx … -- tty2`). Damit ist die
      einzige Sitzung mit Seat nicht aktiv, und die aktiven haben keinen.

      ABHILFE — /etc/wireplumber/wireplumber.conf.d/80-bluez-ohne-seat.conf:

        wireplumber.profiles = {
          main = {
            monitor.bluez.seat-monitoring = disabled
          }
        }

      Danach `systemctl --user restart wireplumber pipewire`. Der
      Lautsprecher verbindet sich, erscheint als Senke und wird automatisch
      Standard. (Die WirePlumber-Doku nennt genau diesen Fall: ein
      Benutzer, der unabhaengig von der aktiven Sitzung Bluetooth nutzen
      darf.)

      NICHT DIE URSACHE, obwohl es danach aussieht: fehlende Gruppe
      `bluetooth`, fehlendes libspa-bluez5, laufendes bluealsa. Alles drei
      wurde geprueft und war in Ordnung.

      MERKE: das Problem entsteht ERST dadurch, dass der Kiosk als
      normaler Benutzer laeuft. Als root auf tty1 stellte es sich nie.
    source: "am Geraet gemessen (2026-07-27); WirePlumber 0.5.8, PipeWire 1.4.2"

  - kind: howto
    id: mupi-kiosk-als-dietpi-anleitung
    title: "Kiosk als normaler Benutzer statt als root starten"
    body: |
      NOETIG FUER PipeWire (ein Benutzerdienst kann nicht zwei Benutzer
      bedienen, siehe mupi-pipewire-umstieg). Sonst nicht anfassen.

      RICHTIGSTELLUNG 08.08.2026: auf Achims Pi 5 (.169) steht in
      `getty@tty1` heute `agetty -a dietpi -J %I $TERM`, und `chromium` wie
      `startx` laufen als `dietpi` — die Umstellung, um die es in diesem
      Eintrag GEHT, ist dort erledigt. Das `sed` weiter unten wuerde auf
      dieser Box nichts mehr finden. Der Abschnitt bleibt stehen, weil er den
      AUSGANGSZUSTAND beschreibt: eine Box, die noch nie umgestellt wurde
      (oder ein frisches DietPi-Abbild), sieht genau so aus.
      Nachbareintrag [[mupi-kiosk-als-dietpi]] traegt weiter „OFFEN:" im
      Titel — fuer den Pi 5 ist es das nicht mehr.

      WIE DER KIOSK STARTET (Stand 2026-07, VOR der Umstellung):
        getty@tty1 mit `agetty -a root` (autologin)
          -> /etc/bashrc.d/dietpi.bash ruft /boot/dietpi/dietpi-login
          -> Run_AutoStart "$(</boot/dietpi/.dietpi-autostart_index)"   # 11
          -> exec /var/lib/dietpi/dietpi-software/installed/chromium-autostart.sh
          -> startx … -- -nocursor tty2
      Wichtig: der Index steht in /boot/dietpi/.dietpi-autostart_index (11),
      NICHT in dietpi.txt (dort steht der Installations-Startwert, hier 0).

      UMSTELLEN:
        sed -i 's/agetty -a root/agetty -a dietpi/' \
          /etc/systemd/system/getty@tty1.service.d/dietpi-autologin.conf
        systemctl daemon-reload
      (Der Dateiname MUSS dietpi-autologin.conf bleiben — dietpi-login
      prueft genau darauf, sonst ueberspringt es den Autostart.)

      DANN FEHLEN ZWEI DINGE, und beide enden im schwarzen Bild:

      1. X darf /dev/tty0 nicht oeffnen:
           (EE) parse_vt_settings: Cannot open /dev/tty0 (Permission denied)
         /dev/tty0 ist crw------- root:tty — Modus 0600, die GRUPPE hilft
         also NICHT. Man braucht den setuid-Wrapper:
           apt install xserver-xorg-legacy
           /etc/X11/Xwrapper.config:
             allowed_users=anybody
             needs_root_rights=yes
         Danach laeuft Xorg selbst als root, gestartet vom Benutzer — genau
         so ist es gedacht.

      2. Gruppen: dietpi braucht `tty`, `input` (und fuer PipeWire-Bluetooth
         `bluetooth`) zusaetzlich zu audio/video/render.

      NEBENWIRKUNG: Chromium bekommt ein FRISCHES Profil unter
      /home/dietpi statt /root. Gespeicherter Browserzustand ist weg.

      PRUEFEN (ohne auf den Schirm zu sehen):
        ps -eo user:10,args | grep -E 'chromium|Xorg'
        -> chromium/startx/xinit als dietpi, Xorg als root = richtig
      Und im X-Protokoll: /home/dietpi/.local/share/xorg/Xorg.0.log.
      ACHTUNG beim Lesen — nach einem geglueckten Start schreibt das
      root-Xorg woanders hin, die Datei bleibt also mit den ALTEN Fehlern
      liegen und sieht aus wie ein frischer Absturz.

      RUECKWEG: agetty-Zeile auf root zuruecksetzen, daemon-reload, Neustart.
      Der Fehlerfall ist harmlos, weil die Aenderung erst beim Booten wirkt:
      geht es schief, hat man eine Konsole statt eines Kiosks — SSH und
      Netz laufen weiter.
    source: "am Geraet durchgefuehrt (2026-07-27)"

  - kind: signature
    id: mupi-exec-rueckruf-toetet-dienst
    title: "Wiedergabedienst startet alle 20 s neu — throw in einem exec-Rueckruf"
    body: |
      SIGNATUR: nichts reagiert mehr — Pause, Stop, Titelwechsel laufen ins
      Leere; der Knopf in der Oberflaeche wird kurz gruen und springt zurueck
      auf rot. In ~/.pm2/logs/spotify-control-out.log steht im 20-Sekunden-
      Takt „Server started at http://localhost:5005".

      URSACHE: spotify-control fragt jede Sekunde die Lautstaerke ab und
      hatte im Rueckruf ein `throw e`:

        exec("/usr/bin/amixer sget Master | grep 'Right:'", (e, stdout) => {
          if (e instanceof Error) { … throw e }        // toetet den Prozess
          …stdout.split('[')[1].split('%')[0]           // wirft ebenfalls
        })

      Ein Wurf in einem exec-Rueckruf beendet in Node den GANZEN Prozess.
      Ausloeser war die Bluetooth-Ausgabe: der softvol-Regler meldet dann
      keine „Right:"-Zeile, grep endet mit Code 1 — und weg ist der Dienst.

      DER KNOPF ERKLAERT SICH DAMIT: die Oberflaeche schaltet optimistisch
      um, der Befehl trifft einen Dienst im Wiederanlauf, die naechste
      Abfrage holt den alten Zustand zurueck.

      DREI STELLEN hatten dieselbe Konstruktion; zwei warteten nur auf ihren
      Einsatz (eine davon ein `find` ueber ein Medienverzeichnis, das es auf
      dieser Box gar nicht gibt).

      ABHILFE: melden statt sterben, letzten bekannten Wert behalten, und
      den Wert per regulaerem Ausdruck `\[(\d{1,3})%\]` lesen statt per
      split-Kette. Das `grep 'Right:'` faellt dabei ganz weg — es sollte nur
      eine Zeile herausgreifen, endet aber bei Mono mit „kein Treffer".

      GENERELL: `throw` in einem Rueckruf ist auf dieser Box ein
      Dienstmord. Im API-Backend war dasselbe elfmal zu reparieren.
    source: "am Geraet diagnostiziert (2026-07-27), nach dem Umschalten auf Bluetooth"

  - kind: signature
    id: mupi-spotify-pause-stoppt-den-ton-nicht
    title: "Pause: Knopf schaltet um, Musik laeuft weiter"
    body: |
      SIGNATUR: der Pause-Knopf bleibt gedrueckt (Zustand stimmt), aber der
      Ton laeuft weiter. Kein Fehler im Protokoll.

      URSACHE: das Backend pausiert mit `spotifyApi.pause()` — der Spotify
      WEB-API. Die meldet Erfolg. Den Ton erzeugt aber das Web Playback SDK
      IM BROWSER, und dem hat niemand etwas gesagt.

      ABHILFE: zusaetzlich direkt am SDK pausieren.
      SpotifyPlayerService hatte pause()/play() bereits — sie wurden von
      nirgendwo aufgerufen, toter Code. Der Aufruf gehoert in
      PlayerService.sendCmd, weil Player-Seite UND Mini-Player dort
      durchlaufen; das Backend bekommt den Befehl weiterhin (es fuehrt den
      Zustand und schreibt die Fortsetzen-Datei).

      NICHT VERWECHSELN mit mupi-exec-rueckruf-toetet-dienst: dort kam der
      Befehl gar nicht erst an. Erst als das behoben war, wurde DIESER
      Fehler ueberhaupt sichtbar. Reihenfolge beim Suchen: kommt der Befehl
      an? -> tut er, was er soll? -> zeigt die Oberflaeche es richtig?

      DRITTER IN DERSELBEN KETTE: der Knopf zeigte nur den abgefragten
      Zustand (1x pro Sekunde), die optimistische Umschaltung war
      auskommentiert. Fuer 1 s blieb das alte Symbol stehen — lang genug,
      dass man erneut tippt und die eigene Eingabe aufhebt. Loesung: sofort
      umschalten, der Wunsch gewinnt bis zu 2,5 s ODER bis die Abfrage
      zustimmt (laeuft die Frist ab, gewinnt die Wahrheit), plus 300 ms
      Doppeltipp-Schutz.

      LOGSTUFE BEACHTEN: config.json steht auf logLevel "error". Alle
      log.debug-Zeilen — auch „Playback paused" — landen im Nichts. Wer
      hier sucht, sieht ein schweigendes Protokoll und haelt es fuer
      Fehlerfreiheit.
    source: "am Geraet gefunden (2026-07-27)"

  - kind: note
    id: mupi-bt-reconnect
    title: "Bluetooth-Lautsprecher verbindet sich nach dem Einschalten nicht von selbst"
    body: |
      BlueZ verbindet ein bekanntes, vertrautes Geraet NICHT von sich aus,
      wenn es beim Booten aus war. Es reagiert nur auf einen ABRISS einer
      bestehenden Verbindung — und darauf, dass das GERAET die Verbindung
      sucht. Viele Lautsprecher tun Letzteres nicht; der hier verwendete
      Xiaomi nicht. In einer Sitzung am Geraet musste FUENFMAL von Hand
      verbunden werden.

      An der Box geprueft, bevor etwas gebaut wurde: `/etc/bluetooth/main.conf`
      ist vollstaendig leer (alle Abschnitte ohne Eintrag, reine Vorgaben) —
      es gibt also keinen Schalter, den man nur umlegen muesste. Die Box muss
      die Sache selbst anfangen.

      GEBAUT: `remote-step-installer/tools/mupibox-bt-reconnect.py` + `.service` + `.timer` im
      remote-step-installer, Rezeptschritt `bt-reconnect`. Der Zeitgeber
      laeuft 25 s nach dem Start und danach alle 30 s; ein Versuch gegen ein
      ausgeschaltetes Geraet kostet nichts.

      BEWUSSTE NICHT-ZIELE, damit sie niemand „nachruestet":
        * NICHT koppeln. Koppeln ist eine bewusste Handlung des Menschen; ein
          Dienst, der von selbst koppelt, ist ein Sicherheitsproblem.
        * NICHT die Standard-Senke setzen. AM GERAET GEMESSEN: PipeWire macht
          den zurueckkehrenden Lautsprecher von allein wieder zur
          Standard-Senke (`pactl get-default-sink` zeigt ihn direkt nach dem
          Verbinden). Wer hier eingreift, streitet nur mit PipeWire.
        * KEINE Geraete anfassen, die nicht als Audio erkennbar sind — ein
          vertrautes Handy geht das nichts an (`Icon: audio-*` oder
          UUID „Audio Sink").

      DER DIENST LAEUFT ALS ROOT, PipeWire als `dietpi`. Das ist kein Problem:
      bluetoothctl geht ueber den System-D-Bus, und root sieht dieselben
      Geraete. Nachgemessen.

      `SuccessExitStatus=0 1` in der Unit ist Absicht: ein fehlgeschlagener
      Verbindungsversuch ist KEIN Dienstfehler (der Lautsprecher ist dann
      schlicht aus). Sonst faerbt sich `systemctl` rot, obwohl alles stimmt.

      NACHTRAG 2026-07-27 (am Morgen, nach dem ersten Neustart): NICHT
      ERSCHRECKEN, wenn nach einem Neustart `Paired: no` dasteht. Der Xiaomi
      verbindet OHNE BONDING — im Zustand steht `Paired: yes, Bonded: no`,
      und in /var/lib/bluetooth/<adapter>/<geraet>/info gibt es KEINEN
      [LinkKey]-Abschnitt, nur [General] mit Name/Class/Trusted. Es wird also
      gar kein dauerhafter Schluessel ausgehandelt; die „Kopplung" ist eine
      Sitzungssache und KANN einen Neustart nicht ueberleben.

      Wer das als „die Kopplung geht verloren" jagt, sucht einen Fehler, den
      es nicht gibt (ich habe erst /var/lib/bluetooth, das Dateisystem und
      die Adapteradresse geprueft — alles in Ordnung). Richtig ist genau die
      hier gebaute Bauform: `Trusted: yes` + aktives Wiederverbinden. Am
      Morgen bestaetigt — Lautsprecher eingeschaltet, und der Zeitgeber holte
      ihn ohne Zutun:
        [bt-reconnect] verbinde … / … verbunden
      danach war er auch wieder Standard-Senke.
    source: "an der Box gebaut und durch Trennen/Wiederverbinden nachgewiesen (2026-07-27)"

  - kind: note
    id: mupi-alsa-rueckfallebene-haengt-zusammen
    title: "ALSA-Reste sind KEINE einzelnen Altlasten, sondern eine Rueckfallebene"
    body: |
      Beim Aufraeumen nach dem PipeWire-Umstieg sieht vieles nach Karteileiche
      aus. Drei dieser Dinge gehoeren aber ZUSAMMEN und sind nur GEMEINSAM zu
      entscheiden — wer eines davon einzeln wegraeumt, nimmt sich den Rueckweg
      und merkt es erst, wenn PipeWire auf einer Box nicht will:

        1. Paket `bluez-alsa-utils` + die Dienste `bluealsa` / `bluealsa-aplay`
           (auf der Box installiert, disabled)
        2. `remote-step-installer/tools/init-alsa-softvol.sh` + `mupibox-alsa-init.service` — kommen
           aus UNSEREM Rezept `mupibox-app.yaml`, nicht von upstream; der
           pipewire-Schritt schaltet sie ab
        3. Der ALSA-Pfad in `src/backend-api/src/ton.ts`
           (`asoundConf` / `parseAsound` / `darfUmschalten` samt Tests), der
           genau die Konfiguration fuer 1. erzeugt

      DIE FRAGE LAUTET ALSO NICHT „kann Datei X weg", sondern: unterstuetzen
      wir noch Boxen OHNE PipeWire? Ja -> alle drei bleiben, sie sind dann
      keine Altlast, sondern Rueckfallebene. Nein -> alle drei zusammen weg,
      und `server.ts` verliert seine Zweigleisigkeit.

      WIRKLICH unbedingt tot (unabhaengig vom Tonstapel) war nur
      autoconnect_bt (siehe mupi-bluetooth-skripte-kaputt) — das ist entfernt.
    source: "beim Altlasten-Durchgang belegt (2026-07-27)"

  - kind: falle
    id: mupi-mpv-gebaut-aber-nie-eingeschaltet
    title: "mpv LAEUFT seit 04.08.2026 — und warum dieser Eintrag vorher das Gegenteil behauptete"
    tags: ['mpv', 'mplayer', 'playerEngine', 2026]
    source: "am Geraet nachgesehen (2026-08-04, Box .169)"
    body: |
      STAND HEUTE (04.08.2026, nachgesehen und nicht geglaubt):

        /etc/mupibox/mupiboxconfig.json                    playerEngine: mpv
        /home/dietpi/.mupibox/spotifycontroller-main/...   playerEngine: mpv
        gesetzt am                                         04.08.2026, 08:18 Uhr
        Prozess                                            mpv laeuft, IPC-Socket da
        Neustartschleife                                   keine

      mpv IST die Wiedergabe-Maschine dieser Box. Nicht mehr mplayer.

      ── WARUM DAS HIER SO AUSFUEHRLICH STEHT ─────────────────────────────
      Dieser Eintrag sagte bis heute: „mpv ist installiert und getestet, laeuft
      aber nicht, weil playerEngine fehlt." Das stimmte am 27.07. und war am
      04.08. seit dem Morgen falsch. Am Abend des 04.08. wurde er trotzdem
      gelesen, geglaubt und einem Menschen als aktueller Stand vorgetragen —
      der sich zu Recht wunderte, weil er die Umstellung selbst erlebt hatte.

      DIE LEHRE IST NICHT „das Wiki war falsch". Ein Eintrag haelt fest, was zu
      SEINEM Zeitpunkt galt. Die Lehre ist:

        Ein Eintrag ueber einen ZUSTAND (etwas ist an, aus, gesetzt, leer) hat
        eine Haltbarkeit. Ein Eintrag ueber eine URSACHE hat keine.

      „playerEngine steht in KEINER Konfigurationsdatei" ist eine
      Zustandsaussage und gehoert nachgesehen, bevor man sie weiterreicht — der
      Eintrag nannte den Pruefbefehl (`pgrep -a mpv`) sogar selbst, und genau
      der wurde uebersprungen. Die Ursache daneben („der Rueckfall auf mplayer
      ist still, man merkt nicht, dass man ihn benutzt") gilt weiter.

      ── WAS BEIM UMSTELLEN DAMALS SCHIEFGING (gilt weiter) ───────────────
      `playerEngine` auf mpv, Dienst neu gestartet, und binnen drei Sekunden
      lagen ACHT mpv-Prozesse da, ein Neustart alle ~380 ms:

        TypeError: Cannot read properties of null (reading 'split')

      Die Hoerer zerlegten `filename` und `path` direkt. mplayer meldete beide
      im Leerlauf NIE, mpv beantwortet sie mit `null`, und ein Wurf im Rueckruf
      beendet in Node den ganzen Prozess. Repariert in der Uebersetzungsschicht
      (mpv-protokoll.ts: `darfGemeldetWerden` haelt null und undefined fuer
      diese beiden Felder zurueck). Siehe [[mupi-mpv-leerlauf-null]].

      ── WAS mpv BRINGT UND WAS NICHT ────────────────────────────────────
      BRINGT: JSON-IPC ueber einen Unix-Socket statt Textbefehle auf stdin —
      damit sind zwei Fehler baulich weg (ein Pfad mit %20 verlor seine
      Anfuehrungszeichen, ein Dateiname mit % warf eine Ausnahme). Ausserdem
      `observe_property` statt Abfrage im Sekundentakt, Pause als Eigenschaft
      statt als Umschalter, und ein gepflegtes Projekt statt eines seit 2012
      unmaintainten.

      BRINGT NICHT: den glatten Fortschrittsbalken. Die Stufen liegen an zwei
      Ein-Sekunden-Takten weiter oben — siehe
      [[mupi-fortschritt-zwei-sekundentakte]].

      OFFEN GEBLIEBEN war [[mpv-anlauf-wartet-auf-falschen-titel]] — repariert
      am 04.08. in der neuen Oberflaeche, siehe
      [[anlauf-nachsehen-statt-warten]].
  - kind: signature
    id: mupi-mpv-leerlauf-null
    title: "Nach dem Umschalten auf mpv stirbt spotify-control im Sekundenbruchteil-Takt"
    sev: high
    match: "Cannot read properties of null.*split|mpv.*Absturz|viele mpv-Prozesse"
    cause: "mpv beantwortet filename/path im LEERLAUF mit null; die Hoerer zerlegen den Wert direkt, und null.split beendet den Node-Prozess"
    fix: "im Wrapper schlucken (darfGemeldetWerden), zusaetzlich typeof-Wache in den Hoerern"
    tags: [mpv, player, absturz]
    body: |
      AM GERAET PASSIERT (2026-07-27). Symptom: `playerEngine` auf `mpv`
      gestellt, Dienst neu gestartet — und binnen drei Sekunden lagen ACHT
      mpv-Prozesse mit acht IPC-Sockets da. pm2 zaehlte alle ~380 ms einen
      Neustart.

      Im Protokoll:
        TypeError: Cannot read properties of null (reading 'split')

      URSACHE: die Hoerer in spotify-control.ts zerlegen zwei Eigenschaften
      direkt:

        player.on('filename', v => v.split('.mp3')[0] …)
        player.on('path',     v => v.split('/')[7])

      mplayer hat diese Eigenschaften NIE gemeldet, solange nichts geladen
      war. mpv beantwortet sie im Leerlauf mit `null`. Ein `throw` im
      Rueckruf beendet in Node den ganzen Prozess (dieselbe Fehlerklasse wie
      mupi-exec-rueckruf-toetet-dienst).

      REPARIERT in der UEBERSETZUNGSSCHICHT, nicht bei den Aufrufern: der
      Wrapper hat die Aufgabe, mplayers Verhalten nachzubilden.
      `darfGemeldetWerden(name, wert)` schluckt einen leeren filename/path —
      und NUR diese beiden. Bei time_pos/length/percent_pos ist `null` eine
      echte Aussage („keine Position"), die die Hoerer gefahrlos
      weiterschreiben. Zusaetzlich eine typeof-Wache in beiden Hoerern: ein
      toter Wiedergabedienst ist zu teuer, um an einer Schicht zu haengen.

      SO ERKENNT MAN ES WIEDER, OHNE INS PROTOKOLL ZU SEHEN:
        ls /tmp/mupibox-mpv-*.sock | wc -l      # >1 ist immer falsch
        pgrep -c mpv                            # >1 ist immer falsch
        pm2 jlist | … restart_time              # steigt im Sekundentakt
    source: "an der Box erlebt, repariert und durch 40 s stabilen Betrieb bestaetigt (2026-07-27)"

  - kind: signature
    id: esbuild-schreibfehler-endet-mit-null
    title: "Bau meldet Erfolg, spielt aber alten Code aus (src/deploy gehoert root)"
    sev: high
    match: "Permission denied.*deploy|esbuild.*Done in|alte Fassung ausgespielt"
    cause: "src/deploy gehoert root, esbuild kann nicht schreiben — endet aber trotzdem mit Rueckgabecode 0"
    fix: "an einen schreibbaren Ort bauen (--outfile=/tmp/…) und von dort ausspielen; Zeitstempel der Ausgabedatei pruefen"
    tags: [bau, deploy, falle]
    body: |
      AM 2026-07-27 fast hereingefallen. `npm run build` im backend-player
      gab aus:

        ✘ [ERROR] Failed to write to output file: … Permission denied
        1 error
          ../deploy/spotify-control.js  1.3mb ⚠️
        ⚡ Done in 1402ms

      und endete mit **rc=0**. Die letzten zwei Zeilen sehen nach Erfolg aus;
      die Datei in src/deploy war drei Tage alt und gehoerte root.

      WAERE ICH DARAUF HEREINGEFALLEN, haette ich alten Code ausgespielt und
      geschlossen, meine Reparatur wirke nicht — der teuerste Irrtum, den
      diese Falle produziert.

      IMMER PRUEFEN, nicht dem Rueckgabecode glauben:
        ls -la --time-style=+%H:%M <ausgabedatei>

      UMGEHUNG ohne root: `npx esbuild … --outfile=/tmp/<name>.js` bzw.
      `ng build --output-path=/tmp/<name>` und von dort ausspielen.
      Dauerhafte Loesung: `sudo chown -R "$USER" src/deploy` (steht noch aus).
    source: "beim Ausspielen der mpv-Reparatur bemerkt (2026-07-27)"

  - kind: signature
    id: mupi-verwaltung-langsam-systemctl
    title: "Die Verwaltung fuehlt sich zaeh an — ein systemctl-Aufruf kostet 335 ms"
    sev: medium
    match: "admin.*langsam|Verwaltung.*zaeh|Seitenwechsel dauert"
    cause: "systemctl list-unit-files --type=service braucht auf dem Pi 335 ms und steckte in ZWEI Endpunkten"
    fix: "die Namensliste kurz merken (kurzGemerkt, 30 s); den ZUSTAND niemals puffern"
    tags: [admin, leistung, systemd]
    body: |
      GEMESSEN STATT GERATEN (Pi 5, 2026-07-27, je fuenf Messungen):

        /api/uebersicht      1-2 ms
        /api/protokolle      340 ms
        /api/dienste         435 ms

      Weder das Bundle (428 KB) noch Polling (die Verwaltung pollt gar nicht)
      waren schuld. Es war EIN Befehl, den sich beide langsamen Endpunkte
      teilten:

        systemctl list-unit-files --type=service   335 ms  (206 Einheiten)
        systemctl show <namen>                      64 ms
        /bin/true                                    2 ms

      Also systemds eigene Arbeit, NICHT der Prozessstart. `/api/protokolle`
      zahlte die vollen 335 ms nur, um zu beschriften, welches Protokoll zu
      welchem Dienst gehoert — neben sieben fs.stat-Aufrufen, die nichts
      kosten.

      REPARIERT mit `kurzGemerkt()` (dienste.ts): die NAMENSLISTE wird 30 s
      gemerkt, der ZUSTAND (laeuft/aktiviert) NIE — sonst zeigt die
      Verwaltung nach einem Klick den alten Stand. Nach jeder eigenen Aktion
      wird sofort vergessen. Gleichzeitige Anfragen teilen sich einen Lauf,
      ein Fehlschlag wird nicht gemerkt.

      NACHHER: /api/protokolle 340 -> 2 ms, /api/dienste 435 -> ~110 ms warm.
      Die restlichen 110 ms sind `systemctl show` und bleiben mit Absicht.
    source: "an der Box gemessen, repariert und nachgemessen (2026-07-27)"

  - kind: signature
    id: mupi-jellyfin-album-nur-ein-titel
    title: "Ein Jellyfin-Album spielt nur den ersten Titel"
    sev: high
    match: "Album.*nur ein (Lied|Titel)|immer das selbe Lied"
    cause: "playMedia wartet intern (stopCurrent) und schickt seinen replace-Befehl SPAETER als die Schleife, die die uebrigen Titel anhaengt"
    fix: "auf playMedia warten, bevor angehaengt wird (sendRequest gibt jetzt ein Versprechen zurueck)"
    tags: [jellyfin, player, reihenfolge]
    body: |
      NICHT MPV. mplayers `loadfile … 0` ersetzt genauso — der Fehler war
      immer da, Jellyfin-Alben wurden nur nie benutzt. Wer das mpv anlastet,
      sucht an der falschen Stelle.

      playJellyfinAlbum startete den ersten Titel und haengte die uebrigen an:

        this.playMedia(tracks[0])                                    // async!
        for (const t of tracks.slice(1)) this.queueJellyfinTrack(t)  // laeuft SOFORT

      `playMedia` wartet intern erst auf `provider.playUrl()` und dann auf
      `stopCurrent()` — sein Befehl geht also ZULETZT raus, waehrend die
      synchrone Schleife alle Anhaenge laengst abgefeuert hat. Und der
      Abspielbefehl ist ein `replace`: er warf die eben angehaengten wieder
      weg. Uebrig blieb genau einer, der erste.

      REPARIERT: `sendRequest` gibt jetzt ein Versprechen zurueck, das
      erfuellt ist, wenn der Dienst geantwortet hat. Aufrufer, die das nicht
      brauchen, merken nichts — der Befehl geht wie bisher sofort raus.
      `playMedia` wartet darauf, und das Album haengt erst danach an.

      MERKREGEL: eine `async`-Funktion, die man nicht abwartet, schickt ihre
      Anfrage NACH dem synchronen Code, der danach steht. Wo eine Reihenfolge
      zaehlt (erst ersetzen, dann anhaengen), muss sie erzwungen werden.
    source: "an der Box diagnostiziert und repariert (2026-07-27)"

  - kind: signature
    id: mupi-jellyfin-titel-fehlt
    title: "Bei Jellyfin steht nur der Albumname, die Titelzeile bleibt leer"
    sev: medium
    match: "nur das Album|Titel fehlt.*jellyfin"
    cause: "die Titelzeile der Player-Vorlage kannte nur library/rss/spotify — Jellyfin fiel in den leeren Zweig"
    fix: "Getter titelVomAbspieler (library + jellyfin + jellyfin-album)"
    tags: [jellyfin, oberflaeche]
    body: |
      ERST MESSEN: der Dienst hatte den Titel die ganze Zeit. Waehrend ein
      Album lief:

        curl -s http://127.0.0.1:5005/local
        currentType       = 'jellyfin'
        currentTrackname  = 'Mittelmaessiges Leben'
        album             = 'Kapelle Petra'

      Also reine Anzeige. In player.page.html:

        @if (media.type ==='library')   -> currentTrackname
        @else @if (media.type ==='rss') -> media.title
        @else                           -> <div></div>   <- Jellyfin hier

      Oben stand `media.title` (der ALBUMname), unten nichts.

      Ein Getter `titelVomAbspieler` entscheidet jetzt, ob der Name vom
      lokalen Abspieler kommt — das gilt fuer die Bibliothek UND fuer
      Jellyfin, beide laufen ueber denselben Abspieler.

      BEWUSST NICHT ANGEFASST: die Titelnummer rechts oben. `currentTracknr`
      zaehlt Metadaten-Ereignisse statt Albumpositionen, `totalTracks` wird
      fuer Jellyfin nie gesetzt. Eine Zahl anzuzeigen, der man nicht traut,
      ist schlechter als keine.
    source: "an der Box gemessen und repariert (2026-07-27)"

  - kind: note
    id: mupi-fortschritt-zwei-sekundentakte
    title: "Warum der Fortschrittsbalken stufig lief — und warum mpv das nicht loest"
    body: |
      Die beiden naheliegenden Ursachen waren laengst behoben: der Regler
      steht auf `step="0.1"`, und die Prozente werden aus elapsed/duration
      gerechnet statt aus dem ganzzahligen progressTime
      (mupi-fortschrittsbalken-springt).

      Uebrig bleibt der TAKT, und das sind ZWEI Ein-Sekunden-Stufen, die
      nichts voneinander wissen:

        mpv       -> Dienst        getProps alle 1000 ms
        Dienst    -> Oberflaeche   visibleInterval(1000)
        Oberflaeche -> Balken      transition: width 1s linear

      Der CSS-Uebergang ist nach einer Sekunde fertig und der Balken STEHT,
      bis der naechste Wert kommt — der bis zu zwei Sekunden alt sein kann.
      Gleiten, stehen, gleiten, stehen.

      MPV HAT DAS NICHT GELOEST UND KONNTE ES NICHT. Es meldet zwar von
      selbst (`observe_property` ist verdrahtet, der Wert im Dienst ist also
      frisch), aber die OBERFLAECHE fragt weiterhin sekuendlich ab. Wer die
      Stufen loswerden will, muss zwischen den Meldungen lokal weiterzaehlen
      — dann ist der Abfragetakt gleichgueltig.

      GEBAUT: `fortschrittJetzt(anker, jetzt)` in now-playing.ts (rein,
      getestet). Pause friert ein — sonst laeuft der Balken im Stillstand
      weiter, und das ist schlimmer als eine Stufe. Ohne brauchbare Dauer
      (Radio) gibt es `null` zurueck, damit der Aufrufer beim gemeldeten Wert
      bleibt statt zu raten. Beide Anzeigen ticken viermal je Sekunde
      (visibleInterval, haelt bei ausgeschaltetem Bildschirm an), die
      CSS-Uebergaenge stehen auf 250 ms.

      STOLPERSTEIN IM MINI-PLAYER: er rechnet in einem `combineLatest`. Bei
      einem Takt-Tick liefert das DASSELBE Zustandsobjekt noch einmal. Wer
      den Anker bei jedem Durchlauf neu setzt, laesst ihn mitkriechen und
      hebt das Weiterzaehlen auf. Deshalb wird die OBJEKTKENNUNG verglichen:
      gleiche Kennung = nur ein Tick, neue Kennung = echte Meldung.
    source: "gemessen und gebaut (2026-07-27)"

  - kind: note
    id: mupi-jellyfin-anmeldung-nur-im-browser
    title: "Quick Connect ueberlebt nichts: die Anmeldung liegt nur im localStorage, pro Origin"
    body: |
      FRAGE, DIE DAZU FUEHRTE: „wir hatten doch Quick Connect gebaut, wo ist
      das hin?" — Der CODE ist vollstaendig da (jellyfin.ts mit
      QuickConnect/Enabled, /Initiate, /Connect, AuthenticateWithQuickConnect,
      Tests, Seite /jellyfin) und auch im ausgelieferten Bundle. Verloren war
      die ANMELDUNG.

      jellyfin.service.ts sagt es selbst:
        const CONFIG_KEY = 'mupibox_jellyfin_cfg'
        // localStorage for now; the box's encrypted-secret store is a later refinement.

      Nachgesehen im Kiosk-Profil
      (/home/dietpi/.config/chromium/Default/Local Storage, Origin
      http://localhost:8200): dort lagen `mupibox_spotify_cfg` und der
      Medien-Cache — kein einziges Jellyfin-Byte. Und in
      /etc/mupibox/mupiboxconfig.json fehlte der Schluessel `jellyfin` ganz.

      Also dieselbe Falle wie bei Spotify (mupibox-spotify-Eintrag): eine
      Anmeldung im localStorage haengt am ORIGIN und ueberlebt weder einen
      Kartenwechsel noch ein geloeschtes Profil noch einen anderen Origin.

      WAS HILFT: Server + Schluessel zusaetzlich in der Verwaltung eintragen
      (Konfiguration -> „Jellyfin-Server" / „Jellyfin API-Schluessel"). Das
      liegt box-weit und ueberlebt. Sauberer waere, dass Quick Connect das
      nach erfolgreicher Anmeldung SELBST tut — noch nicht gebaut.

      NEBENBEFUND: auf dieser Box liegt keine einzige lokale Musikdatei
      (0 mp3/m4a/flac unter /home/dietpi/MuPiBox, /media, /mnt), kein
      media-Verzeichnis, keine playlist.m3u. Wer einen Wiedergabefehler
      sucht, sollte das wissen, bevor er den lokalen Pfad verdaechtigt.
    source: "an der Box nachgesehen (2026-07-27)"

  - kind: note
    id: mupi-lautstaerke-stand-2026-07-27
    title: "Lautstaerke: was gilt, was fehlt — und eine offene Frage"
    body: |
      GETRENNTE LAUTSTAERKE JE AUSGANG GIBT ES SCHON. Unter PipeWire hat jede
      Senke ihren eigenen Wert; am Geraet gemessen standen intern 40 % und
      Bluetooth 12 % gleichzeitig. Der Wert liegt auf Platte
      (~/.local/state/wireplumber/default-routes, als channelVolumes; 0.12
      erscheint dort als 0.001728, weil PipeWire kubisch speichert) und
      ueberlebt ein Trennen: auf 33 % gesetzt, getrennt, wiederverbunden — 33 %.
      Da ist NICHTS zu bauen.

      DER HOECHSTWERT WIRKT NUR ZUR HAELFTE. `mupibox.maxVolume` wird in
      `setVolume()` geprueft — das ist die +5-TASTE. Der SCHIEBEREGLER geht
      durch `setVolumeTo()`, und das klemmt nur auf 0-100:

        const target = Math.max(0, Math.min(100, Number.parseInt(raw, 10) || 0))

      Ein Kind, das den Regler nach rechts zieht, bekommt 100 %, egal was in
      maxVolume steht. Die richtige Stelle waere der Flaschenhals
      `mupi-lautstaerke.sh` — dort kaeme KEIN Aufrufer daran vorbei (Regler,
      Tasten, Telegram, PHP). 

      ERLEDIGT am 01.08.2026: genau dort eingebaut. `klemme()` liest
      `mupibox.maxVolume` aus der Konfiguration und begrenzt darauf statt auf
      100; `get` meldet weiter den WAHREN Wert. Fehlt die Angabe oder jq, gilt
      100 — eine Box, deren Konfiguration sich nicht lesen laesst, darf nicht
      ploetzlich leise sein. Am Geraet vorgefuehrt (Pi 5): mit maxVolume 55
      wurden aus `set 80` und `set 100` jeweils 55; mit 100 blieb alles wie
      zuvor. Damit ist auch ein SCHIEBEREGLER unbedenklich geworden — siehe
      lautstaerke-tasten-statt-regler.

      OFFEN UND UNGEKLAERT: der Nutzer berichtet, eine eingestellte
      Lautstaerke ueberlebe nicht. Zwei Kandidaten geprueft und BEIDE
      verworfen:
        * shutdown_sound.sh setzt beim Herunterfahren `pactl set-sink-volume
          @DEFAULT_SINK@ <startVolume>` — aber sein einziger Aufrufer
          mupi_startstop.service ist DISABLED.
        * init-alsa-softvol.sh setzt beim Start `amixer sset Master` — der
          Dienst mupibox-alsa-init ist ebenfalls DISABLED.
      Auf dieser Box setzt beim Start dem Anschein nach NIEMAND eine feste
      Lautstaerke. Die Ursache ist also noch nicht gefunden.

      LEHRE DARAUS (die ich mir selbst haette sparen koennen): eine
      plausible Codestelle ist kein Beweis. Erst pruefen, ob sie ueberhaupt
      AUSGEFUEHRT wird — `systemctl is-enabled` kostet eine Sekunde.
    source: "an der Box gemessen (2026-07-27); Ursache der fehlenden Persistenz offen"

  - kind: note
    id: mupi-frontend-tests-laufen-nicht
    title: "ng test laeuft auf diesem Rechner nicht — Frontend-Aenderungen sind ungeprueft"
    body: |
      `npx ng test --watch=false --browsers=ChromeHeadless` scheitert mit

        ERROR [karma-server]: Error: Found 1 load error

      und zwar AUCH mit allen Aenderungen gestasht — also unabhaengig vom
      Code. Der Bundle-Bau selbst laeuft durch (`✔ Browser application bundle
      generation complete`).

      FOLGE: alle Frontend-Aenderungen vom 2026-07-27 (Jellyfin-Album,
      Titelzeile, Fortschritt in beiden Balken) sind typgeprueft
      (`npx tsc --noEmit -p tsconfig.json`, rc=0), aber NICHT durch Tests
      belegt. Die acht neuen Tests in now-playing.spec.ts sind geschrieben
      und wurden nie ausgefuehrt.

      WICHTIG FUER DIE NAECHSTE SITZUNG: das ist eine echte Luecke, kein
      Formfehler. Wer hier weiterarbeitet, sollte zuerst den Testlaeufer
      reparieren — sonst haeuft sich ungeprueftes Frontend an.

      Die Backend-Tests laufen normal (node:test via tsx). ACHTUNG auf den
      Stil: backend = `node:test` + `assert`, frontend = Karma + Jasmine
      (`describe/it/expect`). Wer im falschen Stil schreibt, bekommt
      „describe is not defined" bzw. einen Ladefehler.
    source: "beim Absichern der Frontend-Aenderungen festgestellt (2026-07-27)"

  - kind: signature
    id: pkill-f-toetet-den-eigenen-befehl
    title: "pkill -f killt den eigenen Aufruf — der Lauf endet mit -15 und ohne Ausgabe"
    sev: low
    match: "exit -15|Lauf ohne Ausgabe|pkill.*nichts passiert"
    cause: "pkill -f <muster> vergleicht die ganze Befehlszeile; die eigene Shell enthaelt das Muster ebenfalls"
    fix: "PIDs erst mit pgrep -af ansehen und gezielt beenden, oder ein Muster waehlen, das im eigenen Aufruf nicht vorkommt"
    tags: [shell, falle, fernwartung]
    body: |
      Am 2026-07-27 zweimal hintereinander hereingefallen: ein
      `pkill -f 'mpv-probe'` ueber den Agenten beendete die eigene Shell,
      weil deren Befehlszeile die Zeichenkette „mpv-probe" enthielt. Ergebnis:
      Rueckgabecode -15, KEINE Ausgabe — und der Eindruck, der Agent sei
      kaputt.

      Besonders tueckisch aus der Ferne: man sieht nur „nichts kam zurueck"
      und sucht den Fehler in der Verbindung.

      Sicher ist:
        ps -o pid,args -C mpv        # erst ansehen
        kill <pid>                   # dann gezielt
    source: "zweimal erlebt (2026-07-27)"

  - kind: signature
    id: mupi-kiosk-haengt-auf-json-nach-deploy
    title: "Kiosk zeigt rohes JSON (\"Pretty-print\") statt der Oberflaeche"
    sev: high
    match: "pretty.?print|JSON statt|Kiosk zeigt.*json"
    cause: "beim Ausspielen ist www fuer einen Wimpernschlag leer (erst rm, dann tar) — ein Neuladen genau dann laedt das, was der Server ohne App liefert"
    fix: "www zu einem Verweis machen und ihn mit mv -T atomar umhaengen; Waechter nur bei 200 + text/html neu laden lassen"
    tags: [deploy, kiosk, falle]
    body: |
      AM GERAET PASSIERT (2026-07-27). Der Kiosk stand auf einer JSON-Ansicht
      des Browsers und kam nicht zurueck — ohne App laeuft kein JavaScript
      mehr, also auch nicht der Fassungs-Waechter, der sich haette erholen
      koennen. Nur ein Neustart half.

      URSACHE, zwei Teile, beide selbstgemacht:
        1. Das Ausspielen lief als `rm -rf ./*` und danach `tar xzf` IN
           dasselbe Verzeichnis. Dazwischen gibt es ein Fenster ohne
           index.html.
        2. Der Fassungs-Waechter (app.component) prueft alle 8 s per HEAD und
           laedt bei geaendertem ETag neu — er faellt also mit hoher
           Wahrscheinlichkeit genau in dieses Fenster, weil das Ausspielen
           der Anlass fuer beides ist.

      ERSTER VERSUCH, UND WARUM ER NICHT REICHTE:
        mv $B/www $B/www.alt && mv $B/www.neu $B/www
      Das sind ZWEI Umbenennungen, und dazwischen gibt es `www` nicht. Hier
      stand einmal „damit gibt es keinen Augenblick ohne App" — falsch: am
      2026-07-27 ist genau das ein ZWEITES Mal passiert. Das Fenster ist nur
      Millisekunden gross, aber das Ausspielen ist der Anlass fuer das
      Neuladen, also trifft es sich.

      RICHTIG AUSSPIELEN: `www` ist ein VERWEIS, und ein Verweis laesst sich in
      EINER atomaren Umbenennung ersetzen (rename(2)):
        S=r$(date +%s); mkdir -p $B/www.rel/$S
        tar xzf paket.tgz -C $B/www.rel/$S
        test -f $B/www.rel/$S/index.html            # pruefen, VOR dem Tausch
        chown -R dietpi:dietpi $B/www.rel/$S
        ln -sfn www.rel/$S $B/www.tmp && mv -T $B/www.tmp $B/www
        # ALTE STAENDE STEHEN LASSEN, die letzten drei behalten:
        ls -1dt $B/www.rel/*/ | tail -n +4 | xargs -r rm -rf

      WARUM DER VORIGE STAND BLEIBEN MUSS: die App laedt 53 Teile NACH
      (`loadComponent` je Seite), mit Streuwert im Dateinamen. Eine Seite, die
      VOR dem Tausch geladen wurde, holt sich beim naechsten Seitenwechsel ein
      Buendel aus IHREM Stand. Ist der geloescht, kommt 404 — die Navigation
      scheitert, und im schlimmsten Fall reagiert die Oberflaeche gar nicht
      mehr. Dann hilft nur Neuladen, und der Waechter kann das nicht mehr
      anstossen, weil er selbst zu der toten App gehoert.
      Einmalig umstellen (dabei entsteht die Luecke ein letztes Mal):
        mv $B/www $B/www.rel/r1 && ln -s www.rel/r1 $B/www

      ZUSAETZLICH im Waechter: nur neu laden, wenn die Antwort ein 200 mit
      `text/html` ist. Ein 404 oder eine JSON-Antwort ist kein Grund, die
      laufende Oberflaeche wegzuwerfen.
    source: "an der Box erlebt und behoben (2026-07-27)"

  - kind: signature
    id: spotify-favorit-ohne-kennung-spielt-nicht
    title: "Spotify-Favorit startet nicht — Player springt sofort zurueck"
    sev: high
    match: "Favorit.*spielt nicht|health check failed|springt zurueck"
    cause: "der Stern speichert nur Typ/Kategorie/Interpret/Titel; media-provider.ts liefert fuer 'spotify' ohne id/playlistid null, playMedia scheitert und die Seite navigiert zurueck"
    fix: "den Favoriten in der Medienliste der Box nachschlagen (fetchMediaData(kategorie), Abgleich ueber Interpret+Titel) — das liefert das vollstaendige Medium samt Kennung"
    tags: [favoriten, spotify, player, falle]
    body: |
      Gefunden am 2026-07-27 beim Bau des Favoritenfensters. Der Fehler ist
      ALT und betraf auch die bisherige Favoritenseite: ein Spotify-Favorit
      liess sich noch nie abspielen — auch einzeln nicht.

      Die Kette: `toggleFavorite` schreibt nur type/category/artist/title/cover
      nach `mupibox_favorites_v1`. Beim Oeffnen wird daraus ein Media gebaut,
      `playMedia` fragt den Anbieter, und der sieht so aus:

        { type: 'spotify', async playUrl(media) {
            if (media.playlistid) return '…'
            if (media.id)         return '…'
            …
            return null } }

      `null` heisst „abgebrochen": der Player meldet den fehlgeschlagenen
      Health-Check und ruft `navController.back()`. Von aussen sieht es aus,
      als habe der Tipp nicht gezaehlt.

      WAS NICHT REICHT: ab jetzt die Kennung mitschreiben. Alte Eintraege
      haben sie nicht, und niemand setzt seine Favoriten neu. Das Nachschlagen
      in der Medienliste repariert auch die.

      Lokale Alben waren nie betroffen — die spricht der Player ueber
      Kategorie/Interpret/Titel an, und genau das steht im Favoriten.

      DIESELBE FEHLENDE KENNUNG killt auch den ALBUM-FLIP: `onCoverClick`
      prueft ausdruecklich auf `id` bzw. `playlistid` und kehrt sonst
      wortlos um — die Karte dreht sich einfach nicht.

      BEIM NACHSCHLAGEN `fetchRawMedia()` NEHMEN, NICHT `fetchMediaData()`:
      die aufbereitete Liste schickt fuer JEDEN Eintrag eine eigene Anfrage zu
      Spotify (getMediaByID/ByPlaylistID/ByQuery …). Das dauert bei einer
      grossen Sammlung, und ohne Spotify-Verbindung kommt gar nichts zurueck.
      Die Rohliste ist EIN Abruf auf /api/data und traegt id, playlistid und
      showid bereits.

      UND ABWARTEN: das Nachschlagen laeuft im Hintergrund, waehrend das
      Fenster schon bedienbar ist. Ein schneller Tipp navigierte vorher — das
      Versprechen muss vor dem Navigieren abgewartet werden.
    source: "beim Bau des Favoritenfensters gefunden (2026-07-27)"

  - kind: howto
    id: alben-hintereinander-abspielen
    title: "Mehrere Alben hintereinander abspielen — drei getrennte Wege"
    source: 'Favoriten-Knopf „Alle abspielen“, 2026-07-27'
    tags: [player, spotify, jellyfin, warteschlange]
    body: |
      Es gibt KEINE gemeinsame Warteschlange. Je nach Herkunft ein anderer Weg:

        lokal          playerService.queueMedia(media)
                       -> queue/<Kategorie>:<Interpret>:<Titel>
        Jellyfin       playerService.queueJellyfinAlbum(id, cover)
        Spotify        TITELWEISE ueber die Spotify-Warteschlange
                       POST /v1/me/player/queue?uri=spotify:track:<id>

      SPOTIFY nimmt nur TITEL-uris, keine Album-uris. Also erst
      `getAlbumTracks(albumId)` (liefert die uri seit 2026-07-27 mit), dann je
      Titel eine Anfrage. Verlangt ein AKTIVES Geraet — der Web-Player der Box,
      sobald er spielt — und die Berechtigung `user-modify-playback-state`
      (ist in `spotify-auth.ts` enthalten). Danach schaltet Spotify selbst
      weiter; die Box muss kein Albumende erkennen, und dafuer gibt es auch
      kein Signal: `albumStop$` ist die EINSTELLUNG „nach dem Album anhalten",
      kein Ereignis.

      REIHENFOLGE, nicht verhandelbar: erst anreihen, wenn `playMedia`
      aufgeloest ist. Der Start setzt die Warteschlange zurueck, vorher
      Angereihtes ist sofort wieder weg. Dasselbe Muster benutzt
      `playJellyfinAlbum` fuer die Titel eines Albums.

      Je Titel eine Anfrage — bei langen Listen deckeln (im Player 80).

  - kind: signature
    id: css-prozent-schriftgroesse-bezieht-sich-auf-die-schrift
    title: "Symbol winzig: font-size in Prozent meint die SCHRIFT des Elternteils"
    sev: low
    match: "Symbol zu klein|Stern winzig|font-size.*%"
    cause: "font-size: 62% rechnet gegen die geerbte Schriftgroesse (~16px), nicht gegen die Kachel; die Kachel hatte durch flex gar kein eigenes Mass"
    fix: "die Kachelkante ausrechnen und als Laenge setzen, dann calc(var(--kachel) * 0.86)"
    tags: [css, oberflaeche, falle]
    body: |
      Am 2026-07-27 im Favoritenfenster: die Sterne kamen etwa zehn Pixel gross
      heraus. `.fo-kachel ion-icon { font-size: 62% }` sah nach „62 % der
      Kachel" aus, meint aber 62 % der geerbten SCHRIFTGROESSE.

      Auf der alten Favoritenseite stand es richtig, weil die Kachel eine
      eigene Laenge hatte: `--fav-tile: clamp(140px, calc(100vh - 310px), 230px)`
      und `font-size: calc(var(--fav-tile) * 0.62)`.

      Im Fenster kam die Kachelbreite dagegen erst aus der Flex-Verteilung —
      es gab schlicht nichts, worauf sich Prozent haetten beziehen koennen.
      Loesung: die Kante aus der Kartengroesse ausrechnen
      (`(Karte - 40px) / 3`) und daraus Stern und Ziffer ableiten.

  - kind: howto
    id: resume-balken-ohne-sprung
    title: "Weiterhoeren: der Balken zeigt die Stelle, bevor der Player dort ist"
    source: "player.page.ts, 2026-07-27"
    tags: [player, resume, oberflaeche]
    body: |
      Der Player kann nicht an einer Stelle STARTEN: `resumePlayback` startet am
      Anfang und spult erst rund zwei Sekunden spaeter
      (`setTimeout(… seekPosition(…), 2000)`). Bis dahin meldet er
      wahrheitsgemaess 0 — der Balken stand am Anfang und schnellte dann vor.

      Loesung ist NICHT, den Sprung zu glaetten, sondern die bekannte Stelle
      gleich zu zeigen: beim Eintritt wird aus
      `resumespotifyprogress_ms / resumespotifyduration_ms` das Ziel gesetzt,
      und gemeldete Werte werden ignoriert, solange sie weit davon entfernt
      sind. Kommt der echte Wert dort an (±2 %), uebernimmt wieder er.

      WICHTIG die Frist (15 s): kommt der Sprung wider Erwarten nicht, darf die
      Anzeige nicht ewig etwas behaupten, das nicht eintritt.

      NUR mit bekannter Laenge, also bei Spotify — dort steht sie im
      Resume-Eintrag. Lokale Medien und RSS speichern nur die verstrichene
      Zeit; ohne Laenge gibt es keinen Prozentwert, und dort zu raten waere
      schlechter als der Sprung.

  - kind: howto
    id: oberflaeche-lokal-mit-attrappe-pruefen
    title: "Oberflaeche lokal pruefen: Attrappe der Box-Schnittstellen"
    source: "zweimal die Ursache gefunden (2026-07-27)"
    tags: [entwicklung, pruefen, frontend, werkzeug]
    body: |
      OHNE Attrappe ist ein lokaler Test WERTLOS und schlimmer als keiner: die
      Oberflaeche fragt im Sekundentakt /api/... ab, ein einfacher
      `python3 -m http.server` antwortet mit 404, und dann reagiert die
      Statusleiste auf gar nichts mehr — man diagnostiziert einen Fehler, den
      es nur im Testaufbau gibt. Genau darauf bin ich hereingefallen.

      Ein kleiner Server, der die Dateien UND plausible leere Antworten
      liefert, macht die Oberflaeche dagegen vollstaendig pruefbar:

        /api/monitor /api/albumstop /api/mupihat /api/config  -> {}
        /api/resume                                           -> []
        /api/data      -> die Medienliste (hier stehen die KENNUNGEN drin)
        /player/state  -> {"is_playing": false, "item": null}
        /player/local  -> {"playing": true, "currentPlayer": "mplayer", …}
                          laesst den Mini-Player erscheinen
        alles unter /api/ und /player/ sonst -> {} bzw. []
        unbekannter Pfad ohne Punkt -> index.html (Angular-Routen)

      Die Adressen stehen in environments/environment.ts und zeigen auf
      `<host>:8200/api` bzw. `:8200/player` — beim lokalen Test also auf
      DENSELBEN Server wie die Dateien.

      SO GEPRUEFT (Beispiele, die sich bewaehrt haben):
        * Favoriten anlegen: localStorage `mupibox_favorites_v1` setzen
        * Was beim Player ankommt: history.pushState umhaengen und die
          uebergebenen `state.media` mitschreiben — so faellt sofort auf,
          wenn die Kennung fehlt
        * Animationen: getComputedStyle(el).transform MITTEN im Uebergang
          messen (rund 120 ms nach dem Ausloeser), nicht danach
        * Echte Schirmgroesse einstellen (800x480), sonst stimmt jede
          Rechnung mit vh/vw nicht

  - kind: howto
    id: mini-player-ein-und-ausfahren
    title: "Mini-Player: montiert vs. sichtbar — sonst gibt es keinen Weg hinaus"
    source: "now-playing-bar.component.ts, 2026-07-27"
    tags: [oberflaeche, animation, leiste]
    body: |
      Ein `@if` entfernt sein Element SOFORT. Ein entferntes Element kann sich
      nicht mehr verabschieden — deshalb gibt es zwei Zustaende:

        montiert = etwas laeuft (bleibt im Baum)
        visible  = montiert UND nicht auf der Player- oder Verwaltungsseite

      Nicht sichtbar heisst: Klasse `npb-weg`, zur Seite gefahren,
      `pointer-events: none`, und der Lautstaerkeregler wird gar nicht erst
      gebaut.

      ZWEI MECHANISMEN, und das ist kein Versehen:
        hinaus  Klassenwechsel am BESTEHENDEN Element -> transition
        herein  meist ein FRISCH eingehaengtes Element -> @keyframes
      Die Animation haengt an `:not(.npb-weg)`, sonst liefe sie auch, wenn die
      Leiste versteckt eingehaengt wird (Wiedergabe startet, waehrend die
      Player-Seite offen ist): sie faehrt einmal herein und sofort wieder weg.

      KEIN fill-mode auf der Animation. Mit einem gilt das letzte Bild weiter,
      und dessen `opacity: 1` schlaegt das Ausblenden beim Album-Flip
      (`body.mf-open`) — eine Animation gewinnt gegen eine gewoehnliche
      Angabe. Die Leiste laege dann sichtbar auf der offenen Karte.

      Verwaltungsseiten ohne Leiste und ohne die beiden Knoepfe: die Liste
      steht als `OHNE_LEISTE` oben in der Datei (settings, edit, add, wifi,
      jellyfin, spotify).

  - kind: howto
    id: anfassbar-direktive
    title: "Anordnen: eine Direktive fuer alle anfassbaren Elemente"
    source: "anfassbar.directive.ts + fuehrungen.component.ts, 2026-07-27"
    tags: [oberflaeche, ziehen, architektur]
    body: |
      `[mupiAnfassbar]="<stueck>"` macht ein Element im Bearbeiten-Modus
      anfassbar. Stuecke heute: `mp` (Mini-Player), `fav`, `res`, `cover`
      (Albumbild im Player), `titel` (Albumtitel im Player).

      WARUM EINE DIREKTIVE: die Ziehlogik stand in zwei Komponenten und
      haette fuer Bild und Titel ein drittes und viertes Mal geschrieben
      werden muessen. Wer sie irgendwo anbringt, muss nur (a) das Stueck
      benennen und (b) im Stil auf die Variablen hoeren:
        --mupi-<stueck>-x / -y   Platz, als Anteil des Schirms
        --mupi-<stueck>-f        Groessenfaktor (der Mini-Player: --mupi-mp)
      Die Variablen setzt der Dienst nur, wenn wirklich gezogen wurde.

      BEDIENUNG: ziehen macht EINE Sache, HALTEN (600 ms ohne Bewegung)
      schaltet um — bei freien Elementen Verschieben ↔ Groesse, bei
      Raster-Stuecken Groesse ↔ Abstand. Was gerade gilt, sagt die Umrandung:
      blau gestrichelt = die Grundeinstellung, rosa = Groesse, gruen = Abstand.

      WARUM KEIN DOPPELTIPP (war es frueher): auf einer Albumkachel oeffnet
      der das Album. Eine Geste, die zwei Dinge bedeutet, ist keine. Halten
      kollidiert mit nichts. Eine Bewegung ueber 8px bricht das Halten ab und
      wird zum Ziehen — sonst muesste man beim Ziehen darauf achten, schnell
      genug zu sein.

      Im Anordnen-Modus oeffnet ein Tipp auf eine Kachel AUCH NICHTS mehr
      (`!bearbeiten() && elementClicked.emit(...)`): sonst landete man im
      Album, waehrend man an der Kachel arbeitet.

      EINRASTEN (`rasten()`): innerhalb von 7px an die Kanten der Nachbarn
      (links/mitte/rechts, oben/mitte/unten) UND an gleiche Abstaende. Die
      Linien zeigt `mupi-fuehrungen` als eigene Schicht ueber allem —
      durchgezogen = gleiche Kante, gestrichelt = gleicher Abstand. Nichts
      wird erzwungen: ohne Kandidaten in Reichweite bleibt die Position genau
      dort, wo der Finger sie hingelegt hat.

      RASTER-STUECKE (`[mupiNurGroesse]="true"`): Albumkacheln (`alben`) und
      ihre Beschriftung (`bezeichnung`) stehen in einer Reihe — eine einzelne
      woanders hinzuschieben ergaebe nichts. Dort aendert ZIEHEN direkt die
      Groesse, ohne Doppeltipp, und weil alle denselben Faktor teilen, wachsen
      sie gemeinsam. Sie tragen bewusst KEIN `data-stueck`: dutzendfach
      vorhanden, wuerden sie das Einrasten mit Kandidaten zumuellen.

      `alben` schreibt auf `bilder` — denselben Wert, den die drei Stufen auf
      der Darstellungsseite setzen. Zwei Wege auf eine Groesse, nicht zwei
      Groessen fuer dieselbe Sache.

      AM RASTER gilt DIESELBE Regel wie ueberall: ziehen macht EINE Sache
      (die Groesse), der Doppeltipp schaltet um — bei den freien Elementen auf
      Verschieben, bei den Raster-Stuecken auf den ABSTAND. Die Umrandung sagt
      es: rosa = Groesse, gruen = Abstand. Die Achse folgt der Sache (Kacheln
      stehen nebeneinander, die Beschriftung unter ihrem Bild).

      Frueher aenderte eine schraege Bewegung BEIDES gleichzeitig — man zog
      etwas breiter und verschob dabei den Abstand. Das war der Grund fuers
      Umbauen. Historisch (nicht mehr gueltig):
      waagerecht die Groesse, senkrecht der ABSTAND zum Bild
      (`abstandBez`, -12..60px, wirkt als `margin-top` der `.title-row` ueber
      `--mupi-bez-abstand`). An einer KACHEL ist es der Abstand der Kacheln
      untereinander (`abstandAlben`, 0..240). Im Raster gibt es nichts zu
      verschieben, die senkrechte Achse war also frei — und das ist, was man
      von ihr will.

      DER KACHELABSTAND GEHT AN SWIPER (`params.spaceBetween` + `update()`),
      nicht an einen Rand im Stylesheet: swiper rechnet die Folienbreite selbst
      aus und schreibt sie inline — ein Rand daneben schiebt die Kachel aus
      ihrer Mitte oder erzeugt eine vierte, halbe Folie. Der gezogene Weg
      zaehlt dort DREIFACH: swiper verteilt den Abstand auf drei sichtbare
      Folien und verschmaelert sie dabei, von 30 gezogenen Pixeln kamen nur
      rund 10 als Luecke an.

      BEZUG STATT AUSGESCHLOSSEN: Raster-Stuecke tragen `data-bezug` (nicht
      `data-stueck`). Sie LIEFERN Linien — man will den Weiterhoeren-Knopf an
      einer Albumkachel ausrichten —, nehmen aber selbst nie eine Position an.
      Damit eine Reihe Kacheln nicht zehnmal dieselbe Kante anbietet, werden
      die Kandidaten in `rasten()` auf ganze Pixel entdoppelt.

      STOLPERSTEIN Schriftgroesse: der Faktor muss AN DIE BESTEHENDE Regel
      (`swiper.component.scss`, `ion-card-title { font-size: 14px }`). Eine
      zweite, schwaechere Regel weiter unten griff gar nicht — gemessen 14px,
      obwohl sie 16.5 verlangte.

      ZWEI STOLPERSTEINE, beide erst im Gebrauch aufgefallen:
        * Die Bedienleiste hing an den Eckknoepfen — die sind auf der
          Player-Seite aus, dort gab es sie also nicht.
        * Im Modus sind die gewohnten Klicks stillgelegt, man kam also gar
          nicht zum Player. Die Leiste hat jetzt „Start" und „Player".

  - kind: howto
    id: darstellung-live-anordnen
    title: "Am Bildschirm anordnen: Bearbeiten-Modus an den echten Elementen"
    source: "now-playing-bar.component.ts + darstellung.service.ts, 2026-07-27"
    tags: [oberflaeche, einstellungen, ziehen]
    body: |
      „Einstellungen → Darstellung → Am Bildschirm anordnen" schaltet
      `DarstellungService.bearbeiten` ein und geht auf die Startseite. Dort
      bekommen Mini-Player, Stern und Uhr eine gestrichelte Umrandung, ein
      rotes ✕ an der rechten oberen Ecke und der Mini-Player einen runden
      Griff unten rechts. Eine schwebende Leiste oben beendet den Modus.

      DER MODUS WIRD NICHT GESPEICHERT. Die Box startet oft von allein — im
      Bearbeiten-Modus hochzufahren waere ein Zustand, in dem ein Kind mit
      einem Fingertipp die Oberflaeche zerlegt.

      WAEHREND DES MODUS sind die gewohnten Aktionen stillgelegt
      (`openPlayer`/`openFavorites`/`openResume` kehren sofort um). Sonst
      oeffnet jeder Griff das, was man gerade verschieben will.

      DIE KREUZE SITZEN IM ELEMENT, nicht daneben: so folgen sie jeder
      Position ohne zweite Rechnung. In den beiden Knoepfen sind es <span>,
      keine <button> — ein Knopf im Knopf ist ungueltiges HTML; ihr
      pointerdown stoppt, bevor der Knopf davon erfaehrt.

      AUSBLENDEN ueber das ✕ setzt DIESELBEN Schalter, die die
      Darstellungsseite zeigt (miniPlayer 0 / favKnopf / resumeKnopf) — nichts
      geht verloren, es kommt von dort zurueck.

      DIE BEDIENLEISTE ist selbst verschiebbar (Griff ⠿ links): sie liegt ueber
      dem, was man anordnet. Ihr Platz wird NICHT gespeichert — beim naechsten
      Mal soll sie oben sein, wo man sie erwartet. Sie traegt ausserdem
      „Zuruecksetzen" (zwei Schritte, wie der Papierkorb der Favoriten) und
      „Speichern" fuer ein benanntes Thema.

      THEMEN liegen in einem EIGENEN Schluessel `mupibox_themen_v1`, nicht im
      laufenden Stand. Sonst schachtelte jedes Speichern die Sammlung in sich
      selbst, und ein Zuruecksetzen raeumte die Themen mit weg. Jedes geladene
      Thema laeuft durch dieselbe Pruefung (`pruefen()`) wie der laufende
      Stand — eine von Hand bearbeitete Datei darf die Oberflaeche nicht
      zerlegen.

      Das kleine Anordnen-Feld (`anordnen.component.ts`) bleibt daneben
      bestehen: es taugt, wenn der Schirm gerade belegt ist, und fuer eine
      Verwaltungsansicht.

  - kind: howto
    id: darstellung-einstellungen
    title: "Darstellung: was sich einstellen laesst und wo es angreift"
    source: "Einstellungen → Darstellung, 2026-07-27"
    tags: [oberflaeche, einstellungen, darstellung]
    body: |
      Seite `/darstellung` (Kachel „Darstellung" in den Einstellungen), Dienst
      `darstellung.service.ts`, Speicher `mupibox_darstellung_v1` im
      localStorage des Kiosk-Browsers — wie die Favoriten.

      WAS EINGESTELLT WIRD UND WO ES ANGREIFT:
        Mini-Player aus/klein/normal/gross  --mupi-mp  (0 = nie eingehaengt)
        Favoriten-Knopf an/aus              eigener @if in der Leiste
        Weiterhoeren-Knopf an/aus           dito; ohne Stern rueckt er auf 8px
        Albumbilder klein/normal/gross      --mupi-tile-user (swiper.scss)
        Tasten im Player                    --mupi-btn (player.page.scss)
        Startseite audiobook/music/other    home.page.ts, Segment folgt

      WARUM CSS-VARIABLEN: keine Komponente muss den Dienst kennen, sie
      rechnen ohnehin mit Variablen. Eine Aenderung wirkt sofort, ohne
      Neuladen — man will das Ergebnis sehen, waehrend man es einstellt.
      Deshalb hat die Seite auch keinen Speichern-Knopf, und deshalb bleibt
      der Mini-Player auf DIESER Seite sichtbar (auf den uebrigen
      Verwaltungsseiten nicht — siehe OHNE_LEISTE).

      DREI FESTE STUFEN statt Schieberegler (0.75 / 1 / 1.35): das wird auf
      einem kleinen Touchscreen bedient, oft im Stehen. Eine Stufe, die man
      trifft, ist mehr wert als ein Regler, den man nicht trifft.

      STOLPERSTEIN Breite: der Faktor muss AUSSEN um das min() stehen —
      `calc(min(50vw, 400px) * var(--mupi-mp))`. Stand er nur am 400px-Ast,
      deckelte 50vw auf dem 800px-Schirm der Box vorher ab, und „gross" wurde
      nur hoeher statt breiter.

      STOLPERSTEIN Bildgroesse: `--mupi-tile-user` wird NUR gesetzt, wenn es
      von „normal" abweicht. Sonst ersetzte eine feste Zahl die eingebaute
      Rechnung `clamp(140px, 100vh - 310px, 230px)`, die sich an die
      Schirmhoehe anpasst — auf einem anderen Geraet ein Rueckschritt.

      GRENZE: der Speicher haengt am Browser. Wer ihn leert, faengt bei den
      Voreinstellungen an. Soll die BOX es behalten, muss es in die
      Konfiguration wandern, und dann muss die PHP-Verwaltung davon wissen.

  - kind: signature
    id: css-textersetzung-zerschneidet-selektorliste
    title: "Element sitzt ploetzlich oben links — Textersetzung hat eine Selektorliste zerschnitten"
    sev: medium
    match: "sitzt oben links|left: 0px.*top: 0px|Position kaputt nach CSS-Aenderung"
    cause: "eine Regel wurde per Textersetzung VOR einer bestehenden eingefuegt und landete mitten in einer mehrzeiligen Selektorliste; der abgetrennte Selektor haengt danach an der neuen Regel"
    fix: "die Stelle nach der Aenderung wirklich ANSEHEN, nicht nur den Build gruen sehen; neue Regeln ans ENDE des Blocks setzen"
    tags: [css, falle, werkzeug]
    body: |
      Am 2026-07-27 passiert: eine neue Regel wurde vor dem Kommentar
      „/* Ohne den Stern … */" eingefuegt. Dort stand aber:

        .npb-fav-fab.ueber-leiste,
        .npb-resume-fab.ueber-leiste { bottom: 50px; }

      Der Schnitt lag ZWISCHEN den beiden Zeilen. Danach hiess es

        .npb-fav-fab.ueber-leiste,
        .npb.frei { left: …; top: …; bottom: auto; }

      — der Stern bekam die Regel des Mini-Players, dessen Variablen nicht
      gesetzt waren, und landete bei left/top 0. Der Build war gruen, TypeScript
      ebenfalls: CSS in einem Vorlagen-String wird nicht geprueft.

      MERKE: Eine Regel, die andere ueberstimmen soll, gehoert ans ENDE des
      Blocks (gleiche Gewichtung -> die spaetere gewinnt). Und nach jeder
      Textersetzung in CSS die Umgebung lesen.

  - kind: signature
    id: setpointercapture-wirft-und-verschluckt-die-horcher
    title: "Element laesst sich anfassen, aber nicht ziehen"
    sev: medium
    match: "drag geht nicht|pointermove kommt nicht an|setPointerCapture"
    cause: "setPointerCapture wirft, wenn der Zeiger nicht aktiv ist; stand der Aufruf VOR dem Anhaengen der pointermove-Horcher, werden diese nie gesetzt"
    fix: "erst die Horcher anhaengen, die Uebernahme zuletzt und in try/catch — sie ist eine Verbesserung, keine Voraussetzung"
    tags: [oberflaeche, ziehen, falle]
    body: |
      Beim Anordnen-Feld: das Stueck bekam beim Anfassen seine aktiv-Klasse
      (der Handler lief also), reagierte aber auf keine Bewegung. Ursache war
      die Reihenfolge im Handler — `setPointerCapture` stand vor den
      `addEventListener`-Zeilen und warf, womit die Bewegungs-Horcher nie
      gesetzt wurden.

      Faellt bei echten Fingern seltener auf als beim Pruefen mit
      synthetischen PointerEvents (dort gibt es keinen aktiven Zeiger), ist
      aber dieselbe Falle: ein toter Zeiger, ein abgebrochener Griff.

  - kind: howto
    id: groesse-ziehen-relativ-rechnen
    title: "Groesse ziehen: aus der BEWEGUNG rechnen, nicht aus der Lage"
    source: "Anordnen-Feld, 2026-07-27"
    tags: [oberflaeche, ziehen]
    body: |
      Ein Element, das am RAND klebt, laesst sich ueber seine absolute Lage
      nicht vergroessern: der Mini-Player steht rechts, sein rechter Rand ist
      schon fast 1.0, und `(zeiger - links) / grundbreite` ergibt dort immer
      wieder denselben Faktor. Man muesste ihn erst wegschieben — das erwartet
      niemand.

      RICHTIG: beim Anfassen den Faktor und den Anfasspunkt merken, dann

        faktor = faktor0 + (zeigerJetzt - zeigerStart) / grundbreite

      Und wenn es ueber den Rand waechst, die POSITION nachziehen statt den
      Faktor zu deckeln — sonst haengt das halbe Element ausserhalb und nimmt
      seine Knoepfe mit.

  - kind: howto
    id: admin-zwei-oberflaechen-und-umleitung
    title: "Es gibt ZWEI Verwaltungen — und /admin auf Port 80 ist keine davon"
    source: "an der Box nachgesehen und umgeleitet, 2026-07-27"
    tags: [admin, lighttpd, netz]
    body: |
      SYMPTOM: „die Admin-Seite ist nicht erreichbar". Sie ist es doch — nur
      unter einer anderen Adresse, als man tippt.

        http://<box>:8200/admin/   die MODERNISIERTE Verwaltung (Angular).
                                   Liefert das NODE-Backend aus:
                                   `app.use('/admin', express.static(www-admin))`,
                                   Ordner `www-admin` NEBEN `server.js`.
        http://<box>/index.php     die WERKS-Oberflaeche (PHP, lighttpd,
                                   Dokumentwurzel /var/www, ~27 .php-Dateien).
        http://<box>/              zeigte frueher auf die alte, seit dem
                                   Umhaengen auf die neue.
        http://<box>/admin         gab es nicht -> 404. Genau das tippt man.

      Der QR-Code auf der Einstellungsseite der Box zeigt auf `http://<ip>`
      OHNE Port — er fuehrt seit dem Umhaengen also von selbst zur neuen
      Verwaltung, ohne dass an ihm etwas geaendert werden musste.

      UMGEHAENGT (Stand 2026-07-27): der EINGANG auf Port 80 ist jetzt die
      NEUE Verwaltung. Wer nur die IP eingibt, landet dort.
      `/etc/lighttpd/conf-available/50-mupibox-admin.conf`, verknuepft nach
      `conf-enabled`. `mod_redirect` war schon geladen.

        $HTTP["host"] =~ "^([^:]+)" {
            url.redirect = (
                "^/$"           => "http://%1:8200/admin/",
                "^/admin$"      => "http://%1:8200/admin/",
                "^/admin/(.*)$" => "http://%1:8200/admin/$1"
            )
        }

      DIE ALTE IST NICHT WEG, nur nicht mehr die Startseite: `/index.php`
      und alle uebrigen `.php`-Seiten antworten unveraendert und ohne
      Weiterleitung. `/index.php` wird BEWUSST nicht umgeleitet — sonst
      zeigte der Weg zur alten Oberflaeche im Kreis.

      ZURUECKDREHEN: die Zeile `"^/$"` entfernen und lighttpd neu einlesen.
      Deshalb steht sie einzeln.

      ZWEI MUSTER, KEIN GEMEINSAMES. `^/admin/?(.*)$` sah kuerzer aus, fing
      aber auch `/admin.php` — die alte PHP-Seite wurde auf
      `:8200/admin/.php` umgeleitet und war weg. An der Box gemessen, sofort
      nach dem ersten Versuch.

      Der Host kommt aus der Anfrage (`%1`), nicht fest eingetragen: die Box
      ist mal ueber IP, mal ueber Namen erreichbar. Der Port wird
      abgeschnitten, sonst stuende er doppelt im Ziel.

      Vor dem Uebernehmen IMMER `lighttpd -tt -f /etc/lighttpd/lighttpd.conf`
      — ein Tippfehler in der Konfiguration nimmt sonst die ganze alte
      Oberflaeche mit.

  - kind: howto
    id: chromium-kiosk-passwortmanager-aus
    title: "Kiosk: Passwortmanager und Gedoens abschalten"
    source: "der Passwortmanager oeffnete sich ueber der Bedienung, 2026-07-27"
    tags: [kiosk, chromium, box]
    body: |
      Der Passwortmanager ging beim Tippen in ein Feld von selbst auf und lag
      ueber der Bedienung. Abgeschaltet wird er als RICHTLINIE, nicht ueber
      die Startschalter: der Kiosk startet aus der ANMELDESITZUNG des
      Benutzers dietpi (Vater ist `init`, keine eigene Unit), und die
      Schalterzeile kann an mehreren Stellen stehen.

        /etc/chromium/policies/managed/mupibox-kiosk.json
          PasswordManagerEnabled       false
          PasswordLeakDetectionEnabled false
          AutofillAddressEnabled       false
          AutofillCreditCardEnabled    false
          AutoFillEnabled              false
          TranslateEnabled             false
          DefaultNotificationsSetting  2      (blockieren)
          PromptForDownloadLocation    false
          BrowserSignin                0
          SyncDisabled                 true

      ZUSAETZLICH `/etc/chromium.d/60-mupibox-kiosk` mit
      `--password-store=basic` — das Verzeichnis liest der Debian-Wrapper
      `/usr/bin/chromium` beim Start ein (`for file in /etc/chromium.d/*`).
      Damit fragt Chromium nicht nach einem Schluesselbund, den es auf der
      Box nicht gibt.

      GREIFT ERST BEIM NAECHSTEN START des Browsers. Den Kiosk NICHT einzeln
      neu starten (killt X, braucht dann eine Konsole) — `systemctl reboot`.

  - kind: howto
    id: oberflaeche-speicherschluessel
    title: "Was die Oberflaeche im Browser-Speicher der Box ablegt"
    source: "src/frontend-box, Stand 2026-07-27"
    tags: [frontend, speicher, uebersicht]
    body: |
      ALLES hier liegt im localStorage des KIOSK-BROWSERS, nicht in der
      Konfiguration der Box. Es uebersteht einen Neustart, aber kein Leeren
      des Browser-Speichers, und es wandert nicht auf eine andere Box.

        mupibox_darstellung_v1   die aktuelle Darstellung (Groessen, Plaetze,
                                 Abstaende, Startseite, was ein-/ausgeblendet
                                 ist)
        mupibox_themen_v1        benannte Themen, Name -> ganzer Stand.
                                 BEWUSST getrennt vom laufenden Stand: sonst
                                 schachtelte jedes Speichern die Sammlung in
                                 sich selbst, und ein Zuruecksetzen raeumte
                                 die Themen mit weg
        mupibox_favorites_v1     die drei Favoritenlisten, Schluessel
                                 `typ|interpret|titel`. Traegt seit 2026-07-27
                                 auch id/playlistid — aeltere Eintraege NICHT,
                                 die werden ueber die Medienliste aufgeloest
        mupibox_media_cache_v2   Medien-Zwischenspeicher
        mupibox_spotify_cfg      Spotify-Zugang (+ pkce_* waehrend der
                                 Anmeldung), mupibox_jellyfin_cfg /
                                 _deviceid entsprechend
        mupibox_spotify_rueckweg NUR wenn jemand den Spotify-Rueckweg von Hand
                                 geaendert hat. Die VORGABE
                                 http://127.0.0.1:8200/spotify wird
                                 ABSICHTLICH nicht abgelegt — sonst
                                 ueberschattete eine alte Kopie eine spaeter
                                 geaenderte Vorgabe. Siehe
                                 [[spotify-rueckweg-haengt-an-der-adresse]]
        mupibox_tastatur_skala   Groesse der Bildschirmtastatur
        mupibox_neu_zuletzt_v1   NEUE Oberflaeche: der Schluessel des zuletzt von
                                 hier gestarteten Werks. Rettet den Bezug ueber ein
                                 Neuladen — siehe flip-liste-nach-neuladen-ohne-bezug.
                                 Wird NUR zusammen mit einer Pruefung gegen das
                                 Laufende verwendet, nie allein

      NICHT gespeichert, mit Absicht: der Bearbeiten-Modus (die Box startet
      oft von allein — im Bearbeiten-Modus hochzufahren waere ein Zustand, in
      dem ein Fingertipp die Oberflaeche zerlegt) und der Platz seiner
      Bedienleiste (sie soll oben sein, wo man sie erwartet).

      WENN ES DIE BOX BEHALTEN SOLL, muss es in die Konfiguration wandern —
      und dann muss auch die PHP-Verwaltung davon wissen. Das ist bewusst
      noch nicht passiert.

  - kind: howto
    id: schwebende-fenster-favoriten-resume
    title: "Favoriten und Weiterhoeren als schwebende Fenster"
    source: "favorites-overlay / resume-overlay, 2026-07-27"
    tags: [oberflaeche, favoriten, resume]
    body: |
      Beide oeffnen ueber allem statt als eigene Seite: man will kurz
      nachsehen und dann entweder etwas hoeren oder da bleiben, wo man war.
      Ein Seitenwechsel nimmt das Zweite weg.

      MASSE SIND DIESELBEN wie beim Album-Flip — ein zentriertes Quadrat
      `min(100vh - 16px, 100vw - 32px)`, 24px Radius, gleicher Rand und
      Schatten. Es soll DASSELBE Fenster sein, nicht ein aehnliches. Die
      Listen benutzen die geteilte Vorlage `tl-*` aus global.scss.

      FAVORITEN: die Kachelauswahl legt die Karte um die drei Sterne
      (`.fo-kompakt`), die LISTE behaelt das volle Quadrat. Ein ✕ blendet
      nicht die Liste aus, sondern loescht einen Eintrag — in ZWEI Schritten
      (erster Tipp faerbt den Korb rot). Kinder bedienen das.

      „ALLE ABSPIELEN" gibt es je Liste, mit einer ZAHL am Knopf: sie nennt,
      was wirklich laufen wird, nicht wie viele Eintraege dastehen. Angereiht
      wird auf drei Wegen (siehe alben-hintereinander-abspielen); was sich
      nicht anreihen laesst, ist nicht mitgezaehlt.

      FARBEN: gruenes ▶ heisst „von vorn", blaues ▶ heisst „weiterhoeren".
      Dieselbe Farbe fuer beides waere eine falsche Zusage.

      ── DIE REGEL GILT AUCH IN DER NEUEN OBERFLAECHE (seit 02.08.2026) ──
      Sie ist dort in die LANES durchgezogen (Playlist → Alben → Titel), und
      zwar mit einem Unterschied, der genannt gehoert: Das Gegenstueck zu Blau
      ist dort NICHT gruen, sondern NEUTRAL (`rgba(46,42,59,.62)`). Der
      Play-Knopf liegt in den Lanes AUF dem Cover; eine gruene Scheibe mitten
      im Bild naehme der Kachel genau das, woran ein Kind sie erkennt. Das
      Begriffspaar heisst dort also „neutral = von vorn / blau = weiterhoeren".
      Nachgerechnet (tools/kontrast.mjs): Abstand blau zu neutral 73 CIELAB,
      bei Deuteranopie 84, bei Protanopie 81 — Schwelle 20.

      UND DIE FARBE TRAEGT NICHT ALLEIN: Das blaue Zeichen hat eine andere
      FORM (Dreieck ueber einem angefangenen Balken), und die Beschriftung
      heisst „Weiterhören" statt „Abspielen". Einzelheiten unter
      [[blauer-play-knopf-in-den-lanes]].

  - kind: howto
    id: darstellung-auf-der-box-statt-im-browser
    title: "Darstellung liegt auf der BOX — sonst kommt die Verwaltung nicht heran"
    source: "/api/darstellung + Verwaltungsseite, 2026-07-27"
    tags: [darstellung, admin, backend]
    body: |
      DER GRUND: die Darstellung lag im localStorage des KIOSK-Browsers. Die
      Verwaltung laeuft in einem ANDEREN Browser auf einem anderen Geraet und
      sieht davon nichts. Wer das Aussehen von dort einstellen koennen will,
      braucht es an einer Stelle, die beide sehen.

        GET  /api/darstellung   -> { aktuell, themen }   ({aktuell:null} = nie gespeichert)
        PUT  /api/darstellung   <- { aktuell, themen }
        Datei: server/config/darstellung.json (neben data.json, gehoert dietpi)

      GESCHRIEBEN WIRD MIT UMBENENNEN als letztem Schritt, Zwischendatei im
      GLEICHEN Verzeichnis (nur dort ist rename atomar). Eine halb
      geschriebene Datei laese sich beim Start nicht parsen und hiesse
      „alles auf Anfang".

      DAS BACKEND PRUEFT NUR DIE GROBE FORM (Objekt/kein Array). Welche Werte
      gueltig sind, weiss `darstellung.service.ts` — es prueft ohnehin jedes
      Feld beim Laden. Zweimal gepflegt liefe es beim ersten neuen Feld
      auseinander.

      DIE BOX BEHAELT IHRE KOPIE im localStorage als Zwischenspeicher (sofort
      da, traegt auch wenn das Backend still ist), fragt aber alle 3 s beim
      Backend nach (`ABGLEICH_MS`) und uebernimmt bei Unterschied. NUR bei
      echtem Unterschied — sonst schriebe jeder Abgleich den Speicher neu und
      stiesse ueberall eine Neuberechnung an. Die Box schreibt NICHT zurueck,
      wenn sie uebernimmt (sonst Ping-Pong).

      VERWALTUNGSSEITE `/admin/darstellung`: dieselben Einstellungen plus
      Themen — und eine VORSCHAU, die die ECHTE Oberflaeche in einem iframe
      zeigt (gleiche Herkunft, Port 8200, deshalb genuegt ein relativer Pfad),
      im Format 800x480 und auf 0.55 herunterskaliert. KEIN Nachbau: eine
      zweite Darstellung waere eine zweite Wahrheit und genau dann falsch,
      wenn man sich auf sie verlaesst.

      AUFBAU DER SEITE: Vorschau linksbuendig, darueber was sie selbst betrifft
      (Anordnen, Startseite/Player, Neu laden, Groesse), darunter die
      Einstellungen. Das FELD um die Vorschau haelt immer den Platz der
      groessten Vorschau, die ins Fenster passt — deshalb springt beim Zoomen
      nichts, was darunter steht. Wieviel passt, wird aus der Fensterbreite
      gerechnet und bei jeder Aenderung nachgefuehrt; der EINGESTELLTE Wert
      bleibt dabei unangetastet und kommt zurueck, wenn das Fenster wieder
      breiter ist.

      DIE BEDIENLEISTE der Box wird im Rahmen ABGESCHALTET (`?leiste=0`): sie
      laege ueber dem, was man anordnet. Ihre Knoepfe stehen ohnehin auf der
      Seite.

      ZIEHEN GEHT AUCH DORT — im Rahmen, mit derselben Logik wie am Geraet.
      Der Weg hinein ist die ADRESSE: die Oberflaeche liest beim Start
      `?anordnen=1` und schaltet ihren Bearbeiten-Modus ein. Von aussen liesse
      er sich nicht setzen (der Rahmen ist ein eigenes Dokument mit eigenem
      Zustand). Der Schalter in der Verwaltung laedt den Rahmen deshalb neu;
      beim AUSschalten holt sie den Stand neu, denn das Gezogene lief ueber
      die Box.

      NOETIG DAFUER war eine Ruhezeit: die Oberflaeche schreibt beim Ziehen
      mehrmals pro Sekunde, und eine Antwort des 3-s-Abgleichs, die VOR der
      Aenderung losging, wuerde sie zurueckdrehen. `RUHE_NACH_SCHREIBEN_MS`
      (5 s, groesser als der Abgleich) unterdrueckt das Uebernehmen kurz nach
      einer eigenen Aenderung.

  - kind: signature
    id: player-pfad-ist-im-backend-belegt
    title: "/player liefert JSON — der Pfad gehoert dem Backend, nicht der Oberflaeche"
    sev: medium
    match: "weisse Seite|player.*JSON|Tiefeneinstieg.*player"
    cause: "server.ts hat app.use('/player', …) als Weiterreicher zum Player-Dienst (Port 5005); der Frontend-Pfad /player ist dadurch dauerhaft verdeckt"
    fix: "nie auf /player tief einsteigen — `/` laden und die Oberflaeche selbst dorthin navigieren lassen (?seite=player)"
    tags: [backend, frontend, routen, falle]
    body: |
      NACHTRAG 29.08.2026 — DER RAT OBEN IST ueberholt. `fix:` sagt „nie
      auf /player tief einsteigen"; das war der Umweg, solange es keine
      Weiche gab. Seit [[player-route-liefert-json]] gebaut ist, steht
      sie in src/backend-api/src/server.ts (`istSeitenaufruf`, gerufen in
      `app.use('/player', …)`): ein GET mit `Accept: text/html` und
      `Sec-Fetch-Mode: navigate` bzw. leer geht an die Oberflaeche, alles
      andere an den Abspieldienst. Ein Tiefeneinstieg auf /player ist
      damit erlaubt.

      BEIDE EINTRAEGE FUEHREN „player…json" IM `match:`. Die eine Frage,
      die trennt: WELCHEN STAND HAT DIE BOX?

          Vor der Weiche (oder eine andere Route, die ein Frontend-Pfad
          verdeckt)                 -> DIESER Eintrag, und seine Regel
                                       gilt weiter: bevor eine neue Seite
                                       angelegt wird, `grep "app.use('/"`
          Nach der Weiche, und der weisse Schirm ist trotzdem da
                                    -> [[player-route-liefert-json]], dort
                                       steht die Gegenprobe mit `curl`

      Am 2026-07-27 im Verwaltungs-Rahmen aufgefallen: der Knopf „Player"
      zeigte eine weisse Flaeche. `curl http://<box>:8200/player` liefert

        {"status":"ok","error":"none"}   (application/json)

      DEM KIOSK faellt das nie auf: er laedt `/` und wechselt danach INNERHALB
      der App. Nur wer die Adresse direkt aufruft — ein iframe, ein Lesezeichen,
      ein Neuladen auf der Player-Seite — bekommt die JSON-Antwort.

      Dieselbe Klasse wie „Kiosk haengt auf JSON": eine Backend-Route steht vor
      einer Frontend-Route. Wer eine neue Seite in der Oberflaeche anlegt,
      sollte pruefen, ob ihr Pfad im Backend schon vergeben ist —
      `grep "app.use('/" server.ts`.

      /home und /admin/... funktionieren, dort gibt es die entsprechenden
      Rueckfaelle bzw. keine Kollision.
    related: [player-route-liefert-json]


  - kind: note
    id: bt-audio-stottert-koexistenz
    title: "Bluetooth-Ton stottert: WLAN und BT teilen sich den Funkbaustein"
    sev: medium
    match: "Unexpected start frame \\(len 0\\)|Bluetooth.*stock|A2DP.*aussetz"
    tags: [bluetooth, audio, funk, pi, koexistenz]
    body: |
      Symptom: hoerbares Stottern ueber einen Bluetooth-Lautsprecher, im
      Kernelprotokoll dazu `Bluetooth: Unexpected start frame (len 0)` —
      am Geraet gemessen rund 50 pro Minute, und die Zahl SKALIERT MIT DER
      WIEDERGABE (im Leerlauf ~22/min, waehrend Musik ~200/min).

      AM GERAET AUSGESCHLOSSEN (jeweils gemessen, nicht vermutet):
        * Unterspannung/Drosselung   throttled=0x0, 47-49 °C
        * Prozessorlast              load 0,2-0,6; mpv bei 9 % CPU
        * Konfiguration              A2DP aktiv, richtige Standard-Senke,
                                     PipeWire laeuft, keine xrun/underrun
        * WLAN-Stromsparen           power_save off
        * WLAN-Band                  5580 MHz (also NICHT 2,4 GHz-Nachbarschaft)
        * Abfragen der Oberflaeche   /api/bluetooth 76-81 ms, /api/netzwerk 33 ms
        * mupibox-bt-reconnect       Zeitgeber GESTOPPT -> Fehlerrate stieg
                                     sogar; er ist NICHT die Ursache
        * groesserer Tonpuffer       force-quantum 1024 -> 2048 aendert die
                                     Fehlerrate nicht (50 -> 49/min)

      URSACHE: der Pi hat EINEN Funkbaustein fuer WLAN und Bluetooth. Die Box
      holt die Musik ueber WLAN und schickt sie ueber Bluetooth wieder hinaus
      — beides durch denselben Chip. Das ist ein bekanntes Koexistenz-Problem
      (Linux-Bluetooth-Liste, Raspberry-Pi-Forum, raspberrypi/linux #5454);
      5 GHz hilft nur gegen die BAND-Nachbarschaft, nicht gegen den geteilten
      Baustein.

      WAS HILFT — AM GERAET BEWIESEN:
        USB-BLUETOOTH-DONGLE (hier ein CSR 0a12:0001). Gemessen bei laufender
        Musik:

          eingebauter Adapter (UART)   ~50 Fehlerrahmen je Minute
          USB-Dongle                     0 Fehlerrahmen in 10 Minuten

        Das ist kein "etwas besser", sondern weg. Zweite Moeglichkeit (nicht
        gegengemessen): LAN-Kabel, das nimmt umgekehrt das WLAN vom Chip.

      UMSCHALTEN: Verwaltung -> Bluetooth -> Adapter [eingebaut | Bluetooth-
      Dongle] (/api/bt-adapter). Der Schalter faehrt erst den neuen hoch, dann
      den alten herunter.

      ZWEI FALLEN BEIM UMSCHALTEN:
        1. KOPPLUNGEN HAENGEN AM ADAPTER. Auf dem neuen ist die Liste leer,
           der Lautsprecher muss einmal neu gekoppelt werden. Solange er nur
           gefunden, aber nicht gekoppelt ist, erscheint er OHNE NAMEN
           (`Device 00:9E:.. 00-9E-..`) — und die Verwaltung blendet namenlose
           Geraete aus. Er sieht dann aus, als waere er gar nicht da.
        2. DIE hciX-NUMMERN VERTAUSCHEN SICH beim Neuaufzaehlen. Nach dem
           Umschalten war hci0 = Bus USB (Dongle) und hci1 = Bus UART
           (eingebaut) — vorher umgekehrt. NIE nach der Nummer gehen, immer
           nach `Bus:` aus `hciconfig`. Ein `/org/bluez/hci0/...` sagt fuer
           sich genommen NICHTS darueber, welche Hardware das ist.

      NICHT WEITERSUCHEN in: Puffergroessen, Codec (SBC ist bei einfachen
      Lautsprechern das einzige Angebot), Diensten, Abfragetakten. Das ist
      alles gemessen und war es nicht.

  - kind: note
    id: mupi-check-erste-station
    title: "mupi-check: bei jeder Stoerung ZUERST das ausfuehren"
    sev: high
    match: "geht nicht|kein Ton|laedt nicht|stockt|Fehlersuche"
    tags: [diagnose, werkzeug, ton, bluetooth, spotify, kiosk]
    body: |
      `tools/mupi-check.py` (auf der Box als `/usr/local/bin/mupi-check`)
      prueft die GANZE Kette und sagt bei jedem Befund, was er bedeutet:

        mupi-check                # alles
        mupi-check ton bluetooth  # einzelne Bereiche
        mupi-check --json         # maschinenlesbar

      Bereiche: dienste, netz, schnittstellen, medien, spotify, ton,
      bluetooth, kiosk, auslieferung.

      NICHT von Hand anfangen. Die Fehlersuche hier kostet sonst jedes Mal
      dieselbe halbe Stunde, und die entscheidenden Befunde sind solche, auf
      die man nicht als erstes sieht: „Chromium hat 0 Verbindungen zum
      Tonserver" oder „/player liefert JSON statt HTML".

  - kind: note
    id: kein-ton-aber-jellyfin-geht
    title: "Kein Spotify-Ton, Jellyfin spielt: Chromium haengt nicht am Tonserver"
    sev: high
    match: "kein Ton|Spotify.*(stumm|kein Ton)|Jellyfin.*(geht|klappt)"
    tags: [ton, spotify, chromium, pipewire]
    body: |
      SYMPTOM: Spotify meldet Wiedergabe (`/player/state` -> is_playing true,
      Titel stimmt), es kommt aber kein Ton — waehrend Jellyfin einwandfrei
      spielt.

      URSACHE: die beiden nehmen VERSCHIEDENE Wege.
        Jellyfin -> mpv           eigener Prozess, eigene Tonverbindung
        Spotify  -> Web-SDK       IM KIOSK-BROWSER

      Ist Chromiums Verbindung zum Tonserver weg (z. B. weil PipeWire neu
      gestartet ist oder die Bluetooth-Senke verschwand), spielt mpv weiter
      und der Browser bleibt stumm. Am Geraet gemessen:

        pactl list clients | grep -c chromium   -> 0     <- der Beleg
        pactl list sink-inputs                  -> leer

      Ein Seitenneuladen genuegt oft NICHT; der Browser muss neu starten,
      also die Box. Den Kiosk-Dienst NICHT einzeln neu starten — das reisst
      X mit.

      VERWANDT: steht in `/player/state` gar kein Geraet (`device: ?`,
      `is_active: None`), ist das Web-SDK ueberhaupt nicht angemeldet. Dann
      hilft ebenfalls nur ein Neustart.

      `mupi-check ton` prueft beides in einem Schritt.

  - kind: falle
    id: player-route-liefert-json
    title: "/player ist Route UND Proxy — Browser bekam JSON (weisser Schirm)"
    sev: high
    match: "pretty print|weisser Schirm|JSON.*Schirm|player.*json"
    tags: [kiosk, backend, falle]
    cause: "/player ist gleichzeitig eine Route der Oberflaeche und der Reverse-Proxy zum Abspieldienst; ein Seitenaufruf bekam die rohe JSON-Antwort"
    fix: "im Backend nach dem unterscheiden, was der Aufrufer WILL: Accept: text/html + Sec-Fetch-Mode navigate/leer -> Einzelseiten-Anwendung; alles andere -> Proxy"
    body: |
      DIESER EINTRAG IST DIE REPARATUR VON
      [[player-pfad-ist-im-backend-belegt]] — beide fuehren „player…json"
      im `match:`. Die eine Frage, die trennt: STEHT DIE WEICHE SCHON?
      `grep -n istSeitenaufruf src/backend-api/src/server.ts` antwortet
      seit dem Bau mit Zeile ~2187. Steht sie, ist der Rat „nie auf
      /player tief einsteigen" drueben ueberholt und ein weisser Schirm
      ein Rueckfall, den die Gegenprobe unten zeigt. Steht sie nicht,
      gilt drueben der Umweg.

      Wer die Seite neu laedt, waehrend der Spieler offen ist, sah den
      weissen „Pretty Print"-Schirm. Dreimal aufgetreten; zweimal habe ich
      es faelschlich fuer eine Luecke beim Ausspielen gehalten.

      Gegenprobe, beide muessen stimmen:
        curl -H "Accept: text/html" .../player        -> text/html
        curl .../player/state                          -> application/json
    related: [player-pfad-ist-im-backend-belegt]

  - kind: falle
    id: su-anmeldeshell-im-dienst
    title: "`su -` aus einem Dienst: 5 Sekunden pro Aufruf"
    sev: medium
    match: "langsam|Sekunden|timeout.*5000"
    cause: "`su -` startet eine ANMELDE-Shell; ohne Terminal beendet sie nicht und laeuft in die Zeitsperre"
    fix: "`runuser -u <benutzer> -- env VAR=… befehl` — keine Anmeldung, 11 ms statt 5000"
    tags: [backend, leistung, falle]
    body: |
      /api/bluetooth brauchte 6,1 s, obwohl JEDER Einzelschritt darin unter
      40 ms lag. Von Hand ist es nicht zu sehen: mit Terminal beendet
      dieselbe Shell sofort. Der Vergleich „Summe der Teile viel kleiner als
      das Ganze" ist der Weg zu solchen Fehlern — genau dafuer misst
      mupi-check jeden Endpunkt einzeln.

  - kind: note
    id: usb-stecker-brauchen-keine-treiber
    title: "WLAN- und Bluetooth-Stecker: keine Treiber noetig"
    tags: [usb, treiber, wlan, bluetooth, rezept]
    body: |
      Am Geraet geprueft (Kernel 6.18.34+rpt-rpi-2712):

        RTL8822BU (0bda:b812)   rtw88_8822bu   Kernel-Modul
        CSR-Dongle (0a12:0001)  btusb          Kernel-Modul
        ASUS BT500 (0b05:190e)  btusb          Kernel-Modul
        Firmware                /lib/firmware/rtw88/rtw8822b_fw.bin vorhanden
        /lib/modules/*/updates/ leer — nichts von Hand nachinstalliert

      Also KEIN Nachbau, kein DKMS. Fuer ein REZEPT auf einem frischen
      System bleiben nur zwei Punkte:

        1. Realtek-Firmware sicherstellen (Paket `firmware-realtek`), sonst
           bleibt der WLAN-Stecker ohne Firmware stumm.
        2. Eine Schnittstellen-Datei fuer den zweiten Adapter anlegen:
           /etc/network/interfaces.d/mupibox-wlan1 mit `iface wlan1 inet dhcp`
           und `wpa-conf`, BEWUSST OHNE allow-hotplug — sonst hat die Box
           beim Start zwei Wege ins selbe Netz und zwei Standardrouten.

      Das Umschalten selbst braucht kein Rezept: es steht in der Verwaltung
      (Netzwerk -> WLAN-Adapter, Bluetooth -> Adapter).

  - kind: note
    id: darstellung-elemente-und-anzeigen
    title: "Darstellung: Elemente-Reiter, freie Standanzeigen, Zeit-Anzeigen"
    tags: [oberflaeche, darstellung, verwaltung]
    body: |
      Die Darstellungsseite der Verwaltung ist nach ELEMENT geordnet, nicht
      nach Einstellung: Elemente -> Mini-Player | Buttons | Icons.

      Icons: Bluetooth, WLAN-Signalstaerke, Wolke, „WLAN + Wolke zusammen"
      (die Kette: durchgestrichen ohne Netz -> Balken -> Balken mit Wolke;
      am Kabel das LAN-Zeichen) sowie vier Zeit-Anzeigen (Uhr, Timer,
      Restdauer, Heute). Je Anzeige zwei Entscheidungen: OB und WO — in der
      Kopfleiste (dort umsortierbar) oder frei schwebend auf jeder Seite.

      ZWEI DINGE, die man wissen muss:
        * Ist die Kopfleiste aus, schweben alle Anzeigen. Eine Anzeige in
          einer nicht vorhandenen Leiste waere schlicht weg.
        * KEINE EMOJI in Anzeigen. Die Box hat keine Emoji-Schrift
          (`fc-list | grep -ci emoji` -> 0); ein 🕘 faellt auf DejaVu Sans
          zurueck, das das Zeichen nicht kennt, und die Stelle bleibt leer.
          Auf dem Entwicklungsrechner sieht man es, auf dem Geraet nicht.
          Ionicons liegen als SVG bei und haengen an keiner Schrift.

  - kind: falle
    id: spotify-anmeldung-je-herkunft
    title: "Titelliste bleibt auf Lade Titel: Anmeldung haengt an der HERKUNFT"
    sev: high
    match: "Lade Titel|Flipliste|Titel.*(laedt|leer)|Liste.*fuellt sich nicht"
    cause: "die Spotify-Anmeldung liegt im BROWSER und gilt je Origin; ueber die IP geoeffnet fehlt sie, waehrend der Kiosk auf localhost sie hat"
    fix: "zum Nachstellen IMMER http://localhost:8200 am Geraet nehmen; `mupi-check anmeldung` sagt, ob das Web-SDK ueberhaupt angemeldet ist"
    tags: [spotify, oberflaeche, diagnose, falle]
    body: |
      SYMPTOM: beim Aufklappen eines Albums steht dauerhaft Lade Titel, obwohl
      der Dienst die Titel liefert:

        curl .../api/spotify/playlist/<id>/tracks   -> 200, 50 Eintraege

      ZWEI VERSCHIEDENE URSACHEN, die gleich aussehen:

        1. MESSFEHLER. Die Anmeldung gehoert zur HERKUNFT. Der Kiosk laeuft
           auf http://localhost:8200 und ist dort angemeldet; wer die Box
           ueber http://<ip>:8200 aufmacht, ist es NICHT — dieselbe gesunde
           Box sieht dann kaputt aus. Mir selbst passiert, mitten in der
           Fehlersuche.
        2. ECHT. Das Web-SDK ist gar nicht angemeldet (`/player/state` ->
           `device: ?`, `is_active: None`). Dann bleiben Titellisten leer UND
           es kommt kein Spotify-Ton — waehrend Jellyfin weiter spielt, weil
           das ueber mpv laeuft und nicht ueber den Browser.

      UNTERSCHEIDEN: `mupi-check anmeldung`. Der Bereich sagt beides und nennt
      die Herkunft ausdruecklich.

      NEBENBEFUND, behoben: die Medienliste zeigte bei einem FEHLSCHLAG
      ebenfalls Lade Titel — ein Fehler war von Laden nicht zu unterscheiden.
      Sie nennt jetzt den Grund (die Spieler-Seite machte es immer richtig).

  - kind: note
    id: spotify-kette-pruefen
    title: "Spotify spielt nicht: die Kette Glied fuer Glied"
    sev: high
    match: "Spotify.*(nicht|kein Ton|laedt nicht)|Web-SDK|kein Geraet"
    tags: [spotify, diagnose, sdk, widevine]
    body: |
      ERST MESSEN: `mupi-check spotifykette anmeldung musikdienste ton`.

      Die Kette, nach Aussagekraft geordnet — faellt ein frueheres Glied aus,
      sind die spaeteren bedeutungslos:

        Zugangsdaten -> Erneuerung -> Konto (Premium) -> Widevine -> SDK -> Ton

      WAS NORMAL IST und keine Ursache:
        * Ein abgelaufener accessToken. Der gilt eine Stunde; entscheidend ist
          allein, ob die ERNEUERUNG geht (refresh_token + client_id gegen
          accounts.spotify.com, ohne Geheimnis, PKCE).
        * Eine leere `deviceId` in mupiboxconfig.json heisst nur: das SDK hat
          sich hier noch nie angemeldet. Sie ist Folge, nicht Ursache.

      AM GERAET GEMESSEN und damit AUSGESCHLOSSEN (Stand 2026-07-27):
        Erneuerung   200, neuer Token, 3600 s
        Konto        premium (das SDK spielt NUR mit Premium)
        Widevine     vorhanden (auf ARM keine Selbstverstaendlichkeit;
                     ohne DRM startet das Web-SDK gar nicht)
        Dienste      api.spotify.com und Jellyfin erreichbar
        Listen       /api/spotify/playlist/<id>/tracks liefert die Titel

      Bleibt das SDK trotzdem unangemeldet (`/player/state` -> device "?"),
      liegt es NICHT an Zugangsdaten, Konto, DRM oder Netz — dann ist der
      Kiosk-Browser selbst dran. Chromium hat dann typischerweise auch keine
      Verbindung zum Tonserver (`pactl list clients | grep -c chromium` -> 0).

      MERKE: das SDK meldet sich nur auf localhost an (localhost-gated). Wer
      die Box ueber ihre IP oeffnet, sieht NIE ein Geraet — das ist kein
      Fehler, sondern die Absicherung. Siehe [spotify-anmeldung-je-herkunft].

  - kind: falle
    id: box-token-laeuft-ab-sdk-laeuft-trotzdem
    title: "kein Geraet, obwohl das SDK laeuft: der Token DER BOX war abgelaufen"
    sev: high
    match: "kein Geraet|device.*\\?|player/state.*leer|Spotify.*steuert nicht"
    cause: "das Web-SDK meldet sich im BROWSER an; der Abspieldienst der Box benutzt aber den in mupiboxconfig.json gespeicherten accessToken, und den erneuerte niemand"
    fix: "die Oberflaeche gibt den frischen Token nach jeder Erneuerung UND einmal beim Start an die Box zurueck (spotify-auth.service: refresh() + ensureBoxConfig())"
    tags: [spotify, token, sdk, falle]
    related: [player-state-zeigt-nur-laufende-wiedergabe]
    body: |
      'kein Geraet' UND '/player/state leer' STEHEN IN ZWEI EINTRAEGEN. Dieser
      nennt eine URSACHE, [player-state-zeigt-nur-laufende-wiedergabe] warnt
      davor, aus derselben Beobachtung ueberhaupt etwas zu schliessen. Wer hier
      anfaengt, ohne dort gelesen zu haben, richtet womoeglich einen Token, der
      nie kaputt war. Die eine Frage zuerst:

        curl -s -H "Authorization: Bearer <frischer Token>" \
             https://api.spotify.com/v1/me/player/devices | jq '.devices[].name'

      MuPiBox steht in der Liste -> das SDK ist angemeldet, der leere
                                    /player/state sagt nur 'spielt gerade
                                    nichts': [player-state-zeigt-nur-laufende-wiedergabe].
      Liste leer / 401             -> der Token DER BOX ist abgelaufen: DIESER
                                      Eintrag.

      ZWEI TOKEN, die man auseinanderhalten muss:

        im BROWSER   localStorage mupibox_spotify_cfg - erneuert sich selbst
        in der BOX   /etc/mupibox/mupiboxconfig.json  - erneuerte NIEMAND

      Symptom: die Konsole des Kiosks meldet sauber

        [Spotify SDK] Ready with Device ID: 3cb0c9a1...

      und Spotify listet das Geraet („MuPiBox") - trotzdem sagt
      /player/state „kein Geraet", die Box kann nicht steuern, und
      mupiboxconfig.json hat eine leere deviceId.

      Der Grund: der Abspieldienst fragt Spotify mit dem Token DER BOX. Der
      gilt eine Stunde. Danach 401, und der Dienst sieht schlicht nichts.

      GEGENPROBE (beides noetig, sonst zieht man den falschen Schluss):
        Token der Box   curl .../v1/me mit dem gespeicherten Token -> 401?
        Geraeteliste    curl .../v1/me/player/devices mit einem FRISCHEN
                        Token -> steht MuPiBox drin?
      Steht es drin und der gespeicherte ist 401, ist es genau dieser Fall.

      `mupi-check spotifykette` prueft beides getrennt und benennt es.

  - kind: note
    id: kiosk-konsole-mitlesen
    title: "Konsole des Kiosk-Browsers von aussen mitlesen"
    tags: [kiosk, diagnose, chromium]
    body: |
      Ohne die Konsole raet man. So kommt man dran - beides braucht einen
      NEUSTART der Box, denn Chromium nimmt die Optionen nur beim Start:

        Datei: /var/lib/dietpi/dietpi-software/installed/chromium-autostart.sh
        In der exec-Zeile ergaenzen:
          --enable-logging=stderr --v=0        -> Konsole in eine Datei
          --remote-debugging-port=9222         -> Fehlersuch-Port (nur lokal)
        und die Ausgabe umlenken:  >/var/log/mupi-kiosk.log 2>&1

      Danach:
        grep -i "spotify sdk" /var/log/mupi-kiosk.log
        curl -s localhost:9222/json/list   (offene Seiten)

      DREI FALLEN, teuer bezahlt:
        1. `json/close` auf die EINZIGE Seite beendet Chromium - und weil der
           Kiosk als `startx chromium` laeuft, nimmt das X mit. Der Schirm ist
           dann schwarz und nur ein Neustart hilft. NIE die letzte Seite
           schliessen; zum Neuladen `Page.reload` benutzen.
        2. `json/new` verlangt seit Chrome 111 ein PUT, nicht GET.
        3. Die Sicherung nicht vergessen: chromium-autostart.sh.vor-diagnose
           zurueckspielen, wenn die Suche vorbei ist - der Port soll NICHT
           dauerhaft offen bleiben.

  - kind: falle
    id: spotify-sdk-playback-error-drm
    title: "Spotify: SDK angemeldet, nimmt Befehle an - und spielt nicht (Playback error)"
    sev: high
    match: "Playback error|spielt nicht|kein Ton.*Spotify|Titel wird uebersprungen"
    tags: [spotify, sdk, widevine, drm, kiosk]
    related: [spotify-web-sdk-tonabruf-tot]
    body: |
      'Playback error' STEHT IN ZWEI EINTRAEGEN — und der andere hat den
      groesseren Hebel: [spotify-web-sdk-tonabruf-tot] haelt fest, dass Spotify
      den Tonabruf des Web-SDK GENERELL mit 403 abweist. Trifft das zu, ist
      hier nichts zu richten, weil das SDK als Ganzes nicht mehr taugt. Zuerst
      also im Netzprotokoll des Kiosk nachsehen:

        storage-resolve ... 403  -> [spotify-web-sdk-tonabruf-tot]; Ton macht
                                    das native librespot, shouldUsePlayer() ist
                                    fest false.
        kein storage-resolve-403 -> DIESER Eintrag (DRM/Widevine im Kiosk).

      SYMPTOM, genau so gemessen (2026-07-27):

        Konsole   [Spotify SDK] Ready with Device ID: a19faf5c...
        Spotify   Geraeteliste enthaelt "MuPiBox"
        Befehl    PUT /v1/me/player/play?device_id=... -> 204 angenommen
        Konsole   [Spotify SDK] Track change detected: <Titel>
                  [Spotify SDK] Playback error: Playback error     <- HIER
        Ton       pactl list sink-inputs -> 0     (nichts wird erzeugt)

      Das Muster "Titelwechsel, sofort Playback error, naechster Titel" ist in
      den Raspberry-Pi-Foren als "springt nach 0 Sekunden weiter" bekannt und
      gilt dort als DRM-/Widevine-Sache.

      AUSGESCHLOSSEN, alles am Geraet gemessen - NICHT nochmal pruefen:
        Token (Browser)      erneuert sich, gueltig
        Token (Box)          nach dem Fix gueltig, alle DREI Ablagen
        Konto                premium
        Netz/Dienste         api.spotify.com erreichbar, Listen laden
        Autoplay-Sperre      --autoplay-policy=no-user-gesture-required steht
                             bereits in CHROMIUM_OPTS
        Widevine vorhanden   libwidevinecdm0 4.10.2662.3 installiert, dazu
                             eine zweite Kopie 4.10.3057.0 im Benutzerprofil
        Tonweg               Standardsenke ist der Bluetooth-Lautsprecher,
                             Jellyfin spielt darueber einwandfrei

      Der Fehler sitzt also in der MEDIENKETTE des Browsers, nicht in
      Zugangsdaten, Konto, Netz oder Tonausgabe.

      VERSUCHT UND GESCHEITERT - nicht wiederholen:
        1. Profil-CDM beiseitegeschoben (4.10.3057.0 aus
           ~/.config/chromium/WidevineCdm), damit nur das paketierte
           4.10.2662.3 bleibt, dann Neustart. Ergebnis: UNVERAENDERT,
           weiterhin Playback error und 0 Tonquellen. Es liegt also nicht
           daran, WELCHE der beiden Kopien geladen wird.
        2. Der Positionszaehler von Spotify (progress_ms) taeuscht: er lief
           auf 8962 ms weiter, waehrend lokal nichts erzeugt wurde. Das ist
           die SERVER-Sicht, kein Beleg fuer Ton. Nur `pactl list sink-inputs`
           zaehlt.

      AUSGESCHLOSSEN, zusaetzlich gemessen:
        Browser-Ton  ein Testton ueber Web-Audio erzeugt SOFORT eine
                     Tonquelle namens Chromium. Der Weg Browser -> Tonserver
                     -> Lautsprecher ist also in Ordnung; das SDK scheitert
                     nicht am Ton, sondern davor. (`mupi-check browserton`)

      NAECHSTE SCHRITTE (noch nicht erledigt):
        2. Die Widevine-Lizenzanfrage mitlesen (chrome://media-internals oder
           die Netzabfragen auf ...widevine-license...). Ein HTTP 500 dort ist
           laut Spotify-Forum serverseitig und NICHT die Box.
        3. Codecs pruefen: das SDK braucht AAC/MP4.

      QUELLEN
        forums.raspberrypi.com/viewtopic.php?t=372102  (protected content)
        forums.raspberrypi.com/viewtopic.php?t=319522  (DRM unter Chromium)
        github.com/spotify/web-playback-sdk/issues/117 (widevine-license 500)

      WERKZEUG: `mupi-check kioskkonsole` liest genau diese Meldungen und
      zeigt CDM- und Browserfassung nebeneinander.

  - kind: falle
    id: player-state-zeigt-nur-laufende-wiedergabe
    title: "/player/state ist KEIN Anmeldezeugnis"
    sev: medium
    match: "player/state.*leer|kein Geraet"
    cause: "/player/state gibt die LAUFENDE Wiedergabe zurueck; spielt nichts, kommt nichts - das sagt nichts ueber die Anmeldung"
    fix: "die Anmeldung an der Geraeteliste pruefen: /v1/me/player/devices mit frischem Token; enthaelt sie MuPiBox, ist das SDK angemeldet"
    tags: [spotify, diagnose, falle]
    related: [box-token-laeuft-ab-sdk-laeuft-trotzdem]
    body: |
      DIESER EINTRAG SAGT, WAS DIE BEOBACHTUNG NICHT BEWEIST — er nennt keine
      Ursache. Bleibt das Geraet nach der Geraetelisten-Probe wirklich weg, ist
      die haeufigste Ursache der abgelaufene Token der Box:
      [box-token-laeuft-ab-sdk-laeuft-trotzdem]. Beide Eintraege antworten auf
      'kein Geraet' und 'player/state leer'; dieser hier kommt zuerst, weil er
      die Fehldeutung ausraeumt, bevor jemand am Token schraubt.

      Ich habe daraus zweimal den falschen Schluss gezogen und laenger in die
      falsche Richtung gesucht: "kein Geraet" hiess schlicht "es spielt gerade
      nichts". mupi-check sagt das jetzt ausdruecklich dazu.

  - kind: falle
    id: spotify-web-api-nicht-mehr-aus-dem-browser
    title: "Spotify-Web-API weist BROWSER-Aufrufe ab (403) - Server geht"
    sev: high
    match: "403|Playlists laden fehlgeschlagen|Titel.*leer|Suche.*fehlgeschlagen"
    cause: "Spotify beantwortet Web-API-Aufrufe mit Origin-Kopfzeile (also aus jedem Browser) mit 403 und LEEREM Koerper - ausgenommen die Wiedergabe-Endpunkte"
    fix: "alle Katalog-/Bibliotheksabfragen ueber das Hintergrundstueck der Box fuehren (/api/spotify/...), nicht direkt aus der Oberflaeche gegen api.spotify.com"
    tags: [spotify, api, browser, falle, 2026]
    body: |
      AM GERAET GEMESSEN (2026-07-27), gleicher Token, nur die Origin-Kopfzeile
      unterscheidet sich:

        Endpunkt                           ohne Origin   mit Origin
        /me/player/devices                     200           200
        /me/player                             200           200
        /me                                    200           403
        /me/playlists                          200           403
        /playlists/{id}/items                  200           403
        /search                                200           403

      Die Herkunft ist dabei EGAL - http, https, localhost, LAN-IP: sobald
      eine Origin-Kopfzeile mitgeht, kommt 403. Es ist also keine Frage von
      TLS, Herkunft oder Berechtigung.

      ERKENNUNGSZEICHEN: der 403 hat einen LEEREN Koerper. Eine fehlende
      Berechtigung meldet Spotify als JSON mit Text; leer heisst "so nicht".

      WAS DAS ERKLAERT:
        * /spotify (Einrichtung) meldet "Playlists laden fehlgeschlagen"
        * Titellisten beim Aufklappen bleiben leer
        * die Oberflaeche zeigt trotzdem Listen - die kommen aus dem
          Zwischenspeicher des Hintergrundstuecks, nicht aus dem Browser
        * NUR die Wiedergabe-Steuerung funktioniert weiter aus dem Browser,
          deshalb meldet sich das Web-SDK sauber an und nimmt Befehle an

      NICHT WEITERSUCHEN in: Token, Konto, Berechtigungen, TLS, Herkunft,
      Widevine. Alles gemessen und in Ordnung.

      RICHTIGE LOESUNG: die Oberflaeche darf api.spotify.com nicht mehr direkt
      fragen. Jede Katalog-/Bibliotheksabfrage gehoert hinter /api/spotify/...
      der Box - dort laeuft sie serverseitig ohne Origin und geht durch.

      WERKZEUG: `mupi-check spotifyrechte` misst genau das (und zwar korrekt
      serverseitig - eine Messung aus dem Browser zeigt ueberall 403 und
      fuehrt in die Irre).

      QUELLE: developer.spotify.com/blog/2026-02-06-update-on-developer-access-and-platform-security

  - kind: falle
    id: spotify-rueckweg-haengt-an-der-adresse
    title: "Spotify-Einrichtung: der Rueckweg haengt an der Adresse der Box"
    sev: med
    match: "INVALID_CLIENT: Insecure redirect URI|Illegal redirect_uri"
    cause: "redirectUri() baute den Rueckweg aus `window.location.origin` — er wanderte also mit der Adresse, unter der die Seite gerade offen ist"
    fix: "BEHOBEN am 04.08.2026 (E22/R1-R3): redirectUri() gibt die feste Adresse `http://127.0.0.1:8200/spotify` zurueck (Spotifys einzige HTTP-Ausnahme), und der Code wird ueber ein Einfuegefeld von Hand zurueckgebracht"
    tags: [spotify, oauth, pkce, tls, falle, 2026]
    body: |
      STAND 04.08.2026: R1-R3 SIND GEBAUT. Was unten als Ursache steht, ist
      Geschichte — es steht hier, weil die Symptome bei einer Bestandsbox
      weiterlaufen, SOLANGE DER NEUE RUECKWEG NICHT IM SPOTIFY-DASHBOARD
      NACHGETRAGEN IST. Wer nach der Aktualisierung „Insecure redirect URI"
      sieht, hat nicht den alten Fehler, sondern einen fehlenden Eintrag.
      Abgemeldet wird durch die Aenderung NIEMAND (der Rueckweg wird nur beim
      Autorisieren und beim Tausch verglichen, nicht beim Erneuern).

      DREI BESCHWERDEN, EINE URSACHE. Die Ursache stand in einer Zeile
      (src/frontend-box/src/app/spotify-auth.service.ts:50):

        redirectUri(): string { return `${window.location.origin}/spotify` }

        * „Spotify nimmt die Adresse nicht" — vom Laptop ist der Ursprung
          http://<ip>:8200, also HTTP und NICHT Loopback.
        * „der Browser sagt nicht vertrauenswuerdig" — der Ausweg war
          https://<box>:8443, und dort steht ein SELBSTSIGNIERTES Zeugnis
          (scripts/gen-selfsigned-cert.sh, CN=MuPiBox).
        * „im anderen WLAN muesste ich die IP neu eintragen" — andere Adresse =
          anderer Rueckweg; Spotify vergleicht EXAKT.

      DAS ZERTIFIKAT IST DAS SYMPTOM, NICHT DIE URSACHE. Wer nur es behebt, hat
      die dritte Beschwerde weiterhin.

      SPOTIFYS REGEL (seit 2025): HTTP-Rueckwege sind abgeschafft — AUSSER dem
      IP-LITERAL `http://127.0.0.1:PORT` und `http://[::1]:PORT`. `localhost`
      ist ausdruecklich NICHT erlaubt. Genau darauf startet der Kiosk aber
      (scripts/chromium-autostart.sh:101, MUPI_URL="http://localhost:8200").
      Die Oberflaeche weiss die Regel bereits — spotify.page.ts nennt sie im
      Kommentar und hat den Knopf „Ueber 127.0.0.1 oeffnen"
      (spotify.page.html:131).

      DER WEG, DER IN JEDEM NETZ TRAEGT: einen FESTEN Rueckweg
      http://127.0.0.1:8200/spotify eintragen. Die Box normal ueber
      http://<ip>:8200/spotify oeffnen (ohne HTTPS — diese Seite ist ja kein
      Rueckweg), anmelden, Spotify leitet auf 127.0.0.1, dort lauscht am Laptop
      nichts -> Fehlerseite, ABER der Code steht in der Adresszeile. Adresse
      kopieren, zurueck in den Box-Tab, einfuegen.
      Es traegt, weil der PKCE-Verifier im sessionStorage DESSELBEN Tabs liegt
      und die Navigation ueberlebt. Derselbe Eintrag deckt beide Faelle: an der
      Box (Kiosk auf der Rueckschleife) landet die Rueckleitung wirklich dort.

      AUSGESCHLOSSEN — DynDNS loest es fuer eine WANDERNDE Box NICHT:
        * Der Ansatz ist im Kern richtig (Name + Let's Encrypt ueber DNS-01;
          DNS-01 braucht KEINE Portfreigabe, die Box bleibt von aussen
          unerreichbar, und der A-Eintrag darf auf eine private Adresse zeigen).
          Zu Hause ist das sauber.
        * Aber: DNS-REBIND-SCHUTZ verwirft oeffentliche Namen auf private IPs —
          die FritzBox ab Werk, und 192.168.178.x IST ihr Bereich. Zu Hause
          traegt man eine Ausnahme ein; im Ferienhaus oder bei Oma kommt man an
          den Router NICHT heran.
        * Viele DynDNS-Anbieter nehmen private Adressen gar nicht entgegen.
        * Nach jedem Netzwechsel Aktualisierung + Ablauf der alten Antwort.
        * Erneuerung alle 90 Tage braucht Internet AM ORT; faellt sie aus,
          kippt readTlsCert() STILL auf das selbstsignierte Zeugnis zurueck —
          die Warnung ist wieder da, ohne Meldung.
      Ein Name ist ausserdem kein Vertrauen: ein selbstsigniertes Zeugnis auf
      box.meinname.de ist genauso unvertrauenswuerdig wie eines auf einer IP.

      GEMESSEN AN DER BOX AM 04.08.2026 (E22/R4) — und es widerlegt eine
      Annahme aus dem Backlog:

        Kiosk-Start   ps: chromium ... --homepage http://localhost:8200/neu/
        Speicher      Local Storage/leveldb kennt GENAU EINE Herkunft:
                      _http://localhost:8200 — darin mupibox_darstellung_v1,
                      _media_cache_v2, _neu_licht_v1, _p_gast_neu_zuletzt_v1,
                      _spielzeit_v1
        HTTPS-Weiche  chromium-autostart.sh:105 probiert https://localhost:8200
                      und nimmt es, wenn es antwortet. ES ANTWORTET NIE:
                        https://localhost:8200 -> 000 (kein TLS auf 8200)
                        http://localhost:8200  -> 200
                        https://localhost:8443 -> 200
                      Die Probe fragt den FALSCHEN PORT (TLS liegt auf 8443).
                      Praktisch laeuft der Kiosk also immer auf http/localhost;
                      der Kommentar darueber („preferring https ... needed so
                      Spotify accepts the redirect URI") beschreibt einen Zweig,
                      den es nicht gibt — und haette er ihn, waere er falsch:
                      https://LOCALHOST verbietet Spotify ebenso.

      WAS DARAUS FOLGT — DIE BACKLOG-ANNAHME „an der Box ist gar kein Einfuegen
      noetig" STIMMT NICHT: die Rueckleitung landet auf http://127.0.0.1:8200,
      der Kiosk steht auf http://localhost:8200. Das ist DIESELBE Box, aber eine
      ANDERE HERKUNFT — und Verifier/State liegen im sessionStorage der
      Herkunft. Die Anmeldung endet dort mit `no_pkce_state`, und am
      Beruehrschirm gibt es keine Adresszeile, um zurueckzukommen. AM GERAET
      SELBST ist die Anmeldung damit gar nicht abschliessbar; der Weg ist der
      von einem anderen Rechner aus (R1+R2), und genau dafuer ist er gebaut.

      DER KIOSK WURDE ABSICHTLICH NICHT UMGEZOGEN. Ein Wechsel auf
      http://127.0.0.1:8200 loeste das — liesse aber die oben gemessenen fuenf
      Schluessel an der alten Herkunft zurueck ([[oberflaeche-speicherschluessel]]),
      und die Box saehe fabrikneu aus. Das ist ein eigenes Vorhaben mit
      Uebernahme, keine Zeilenaenderung.

      NIE TUN: DynDNS MIT Portfreigabe. Solange /api/data und
      /api/spotify/config ohne Anmeldung lesbar sind und cors() jeder Seite das
      Mitlesen erlaubt, gaebe man damit den Jellyfin-Schluessel und den
      Spotify-Refresh-Token ins offene Netz.

      WAS GEBAUT IST (04.08.2026, src/frontend-box):
        R1  spotify-auth.ts: SPOTIFY_RUECKWEG_VORGABE =
            http://127.0.0.1:8200/spotify. redirectUri() gibt sie zurueck;
            abweichend nur ueber localStorage mupibox_spotify_rueckweg, das die
            Einrichtungsseite als Feld anbietet.
        R2  sucheAusAdresse() loest den Frageteil aus einer EINGEFUEGTEN
            Adresse (ganze Adresse, nur Frageteil, mit/ohne `?`, mit
            angehaengtem `#…`, in Anfuehrungszeichen). handleRedirect(text)
            nimmt ihn statt window.location.search. Auf der Seite: Feld
            „Adresse aus dem Browser hier einfuegen".
        R3  Der Rueckweg wird beim Start der Anmeldung WEGGELEGT
            (sessionStorage mupibox_spotify_pkce_rueckweg) und beim Tausch von
            dort geholt. Sonst genuegte eine Aenderung im Feld zwischendurch,
            und Spotify antwortete mit `invalid_grant` — das liest sich wie
            „Code abgelaufen" und fuehrt ans falsche Ende.
        ENTFALLEN: insecureRedirect()/onLocalhost()/switchToLoopback(). Sie
            fragten „taugt die Adresse DIESER Seite als Rueckweg?" — seit R1
            ist die Seite gar kein Rueckweg. insecureRedirect() haette die
            Anmeldung vom Laptop aus weiter BLOCKIERT, und switchToLoopback()
            schickte dort auf die Rueckschleife des LAPTOPS, also ins Leere.

      GEGENGELESEN AM 04.08.2026 — ZWEI BEFUNDE, BEIDE BEHOBEN. Beide sind
      Faelle von „die Adresse stimmt an EINER Stelle":

        1. DER ALTE PHP-ADMIN NANNTE WEITER DIE ALTE ADRESSE.
           AdminInterface/www/spotify.php ist die Seite, auf der eine
           Einrichtung ANFAENGT (Schritt 1: „Add this Redirect URI"), und sie
           nannte unveraendert `https://<box>:8443/spotify`. Wer ihr folgte,
           trug ins Spotify-Dashboard etwas ein, das seit R1 nie mehr
           geschickt wird, und bekam beim Verbinden
           `INVALID_CLIENT: Insecure redirect URI` — auf SPOTIFYS Seite, wo
           keine Erklaerung der Box mehr hinkommt.
           BEHOBEN: spotify.php nennt denselben festen Rueckweg wie
           SPOTIFY_RUECKWEG_VORGABE, und ihr Link auf die Einrichtungsseite
           geht wieder ueber http://<box>:8200 statt https://<box>:8443. Der
           Umweg ueber 8443 hatte GENAU EINEN Grund — die Rueckleitung —, und
           der ist mit R1 weg; er kostete die Zertifikatswarnung, also eine
           der drei Beschwerden, die E22 abschafft.
           NICHT VERGESSEN: ausgeliefert wird AdminInterface/release/www.zip,
           NICHT www/ ([[php-admin-knopf-sperren-nicht-ausblenden]], Falle 2).
           Neu gebaut mit
           `python3 tools/php-admin-fremdbezug-schau.py --zip-neu`; dabei kam
           nebenbei der MQTT-Fix vom selben Tag mit, der ebenfalls nur in
           www/ lag.

        2. DIE NEUANMELDUNG EINER ANGEMELDETEN BOX NAHM SICH DAS EINFUEGEFELD
           SELBST WEG. Der Weg aus R2 fuehrt AUS der Seite heraus und zu Fuss
           zurueck; das Zurueckkommen ist ein NEUER Seitenaufbau, also lief
           `ionViewWillEnter` erneut. Bei einer Box, die schon angemeldet ist
           und neu angemeldet werden soll — genau das, wozu `scopeHint` bei
           fehlenden Rechten woertlich raet („Trennen" + neu verbinden) —, war
           `loadConfig()` seit dem Trennen leer, die BOX meldete aber weiter
           `eingerichtet`. Die Seite zeigte daraufhin die Karte „Verbunden",
           und das Feld war WEG: der Benutzer hielt den Code in der
           Zwischenablage und fand nichts mehr, wohin damit.
           BEHOBEN: `anmeldungLaeuft()` (Verifier im sessionStorage DIESES
           Tabs) schlaegt die Auskunft der Box — solange eine Anmeldung
           offen ist, schuldet dieser Tab ihr das Feld. Ein Test in
           spotify.page.spec.ts nagelt es fest (ohne den Zweig faellt er).

      NOCH EINMAL GEMESSEN (04.08.2026, lesend an der Box):
        /api/spotify/config -> {"deviceName":"MixPiBox","eingerichtet":true}
        http://192.168.178.169:8200/spotify -> 200 (der SPA-Rueckfall in
        server.ts:9543 liefert index.html, der Pfad existiert also wirklich)
      DIE BOX IST ALSO ANGEMELDET GEBLIEBEN, und der Kiosk-Start wurde nicht
      angefasst — der E22-Commit beruehrt keine Datei unter scripts/.

      NICHT MEHR ZU FUERCHTEN: [[frontend-api-port-hart-verdrahtet]] (der
      Login auf :8443 meldete Erfolg und speicherte still nichts, weil das
      Frontend seine API fest auf :8200 baute) trifft die QUELLE nicht mehr —
      environment.prod.ts nimmt `window.location.origin`, und
      src/frontend-box/package.json baut mit `--configuration production`.
      environment.ts (der Entwicklungsstand) hat den festen Port noch.

      EIN ZWEITES MAL GEGENGELESEN AM 04.08.2026 — DREI BEFUNDE, ALLE BEHOBEN.
      Alle drei sind Faelle von „der Ablauf hat mehr Enden als die zwei, die
      Spotify vergleicht":

        3. EINE ANGEFANGENE ANMELDUNG HATTE KEINEN AUSGANG. Befund 2 (oben)
           laesst eine laufende Anmeldung die Auskunft der Box schlagen — aber
           eine Anmeldung endet im Code nur auf EINEM Weg, dem geglueckten
           (handleRedirect raeumt zum Schluss auf). Der WAHRSCHEINLICHSTE
           Ausgang ist ein anderer, und es ist genau der, den E22 selbst
           erzeugt: steht der neue Rueckweg noch nicht im Spotify-Dashboard,
           bleibt der Benutzer bei Spotify an „INVALID_CLIENT: Insecure
           redirect URI" haengen. Es kommt KEIN Code, KEINE Fehlerseite unter
           127.0.0.1, NICHTS zum Einfuegen — Verifier und State liegen
           trotzdem weiter im sessionStorage des Tabs. Die Einrichtungsseite
           behauptete danach bis zum Schliessen des Tabs „eine Anmeldung
           laeuft", verlangte eine Adresse, die es nicht gibt, und
           VERSCHWIEG DABEI, DASS DIE BOX ANGEMELDET IST. Dasselbe gilt fuer
           die Absage bei Spotify (`error=access_denied`): auch sie laesst die
           Ablagen stehen, weil handleRedirect vorher wirft.
           BEHOBEN: `anmeldungVerwerfen()` im Dienst raeumt die vier
           sessionStorage-Schluessel; die Seite bietet „Angefangene Anmeldung
           verwerfen" an — NUR wenn eine laeuft, sonst hiesse der Knopf im
           Regelfall nichts und wuerde im Ernstfall uebersehen. Danach
           entscheidet ionViewWillEnter neu, statt einen zweiten Zustand zu
           bauen. Zwei Tests: einer faellt ohne den Knopf, einer haelt fest,
           dass er sonst nicht dasteht.

        4. DER KOMMENTAR VERSPRACH EINE UEBERSETZUNG, DIE NIEMAND JE ZU SEHEN
           BEKAM. Ueber `errText()` in spotify.page.ts steht, `invalid_grant`
           fuehre ohne Uebersetzung ans falsche Ende — die Tabelle wird aber
           NUR im Zweig OHNE HTTP-Status gelesen, und invalid_grant ist
           Spotifys Antwort auf den TAUSCH, kommt also als HTTP 400 mit
           {error:'invalid_grant'} an. Auf dem Schirm stand „HTTP 400 —
           Invalid authorization code". Das liest sich wie „abgelaufen" und
           schickt den Benutzer die ganze Anmeldung noch einmal durchlaufen,
           waehrend in Wahrheit die Adresse auseinanderlaeuft, die Spotify
           ZWEIMAL vergleicht — also genau der Fehler, vor dem der Kommentar
           warnen wollte.
           BEHOBEN: zweiter Topf fuer Spotifys eigene Kennungen am
           Tausch-Endpunkt (invalid_grant, invalid_client). Ein Test flusht ein
           echtes HTTP 400 und verlangt, dass „Redirect-URI" auf dem Schirm
           steht und „HTTP 400" nicht.

        5. ZWEI ANLEITUNGEN UND DREI KOMMENTARE NANNTEN WEITER DIE ALTE
           ADRESSE. documentation/hardware-test.md liess
           `https://<box-ip>:8200/spotify` registrieren — zweimal falsch (TLS
           liegt auf 8443, und seit R1 wird gar keine Adresse pro Rechner mehr
           geschickt). server.ts (zwei Stellen) und chromium-autostart.sh
           begruendeten den HTTPS-Modus bzw. die https-Weiche weiter mit „the
           redirect URI must be https".
           DER KOMMENTAR IM KIOSK-START IST DER GEFAEHRLICHSTE: er lud dazu
           ein, die tote Probe (`https://localhost:8200` -> 000) auf 8443 zu
           „reparieren" — und das haette den Kiosk auf eine ANDERE HERKUNFT
           gestellt und die Box fabrikneu aussehen lassen. Er sagt das jetzt
           ausdruecklich, samt der Auflage, bei einem Umzug JEDEN
           mupibox_*-Schluessel der Herkunft mitzunehmen (die fuenf gemessenen
           sind der Bestand EINER Box, nicht die Liste —
           [[oberflaeche-speicherschluessel]]). Geaendert wurde dort NUR der
           Kommentar, keine ausgefuehrte Zeile.

      DAS WERKZEUG DAZU: tools/spotify-rueckweg-abgleich.py. Befund 1 und
      Befund 5 sind dieselbe Sorte Fehler, einmal von Hand gefunden und einmal
      fast uebersehen — also gehoert sie einem Werkzeug. Es liest
      SPOTIFY_RUECKWEG_VORGABE als Wahrheit, prueft, dass sie ueberhaupt eine
      Adresse ist, die Spotify noch nimmt (IP-LITERAL, nicht `localhost`),
      verlangt sie WOERTLICH an jeder Stelle, die einem Menschen eine Adresse
      zum Eintragen nennt — AUCH im ausgelieferten release/www.zip, nicht nur
      in www/ —, und klagt jede Anleitung an, die noch
      `https://…:8443/spotify`, `https://…:8200/spotify` oder
      `http://localhost:…/spotify` empfiehlt. Eine alte Adresse als GESCHICHTE
      zu nennen bleibt erlaubt: das Merkwort („HIER STAND", „used to say", …)
      faerbt auf seinen Kommentarblock ab — sonst klagt das Werkzeug genau die
      Stelle an, die den Irrtum ERKLAERT, und man gewoehnt sich an, es zu
      uebergehen. Mit `--box <adresse>` misst es zusaetzlich, NUR LESEND, ob
      die Box unter dem Rueckweg-Pfad antwortet und ob sie noch angemeldet ist.

        python3 tools/spotify-rueckweg-abgleich.py --box 192.168.178.169

      Vorhaben und Schritte: BACKLOG E22 (R1-R4); haengt an E15/S3.

  - kind: falle
    id: spotify-web-sdk-tonabruf-tot
    title: "Spotify-Web-SDK: Tonabruf endgueltig tot (storage-resolve 403)"
    sev: high
    match: "Playback error|storage-resolve|Spotify player health check failed"
    cause: "Spotify beantwortet den Tonabruf des Web-SDK (storage-resolve) mit 403, sobald eine Origin-Kopfzeile mitgeht - sogar mit Spotifys eigener SDK-Herkunft sdk.scdn.co"
    fix: "Web-SDK nicht mehr verwenden; Ton macht das native librespot (Spotify Connect) - shouldUsePlayer() ist fest false"
    tags: [spotify, sdk, librespot, falle, 2026]
    related: [spotify-sdk-playback-error-drm]
    body: |
      DER ANDERE EINTRAG ZU 'Playback error' IST
      [spotify-sdk-playback-error-drm] (fehlendes Widevine im Kiosk). Er gilt
      nur, solange der Tonabruf ueberhaupt noch beantwortet wird. Zeigt das
      Netzprotokoll storage-resolve mit 403, ist DIESER Eintrag der
      uebergeordnete: dann hilft kein DRM-Modul mehr.

      DIE KOMPLETTE MESSKETTE (2026-07-27, alles am Geraet):
        * SDK meldet sich an: "Ready with Device ID" - Geraet erscheint in der Liste
        * play-Befehl: 204, der Vermittler teilt das Stueck zu ("Track change detected")
        * dann NICHTS: kein Lizenz-, kein Tonabruf - nur "Playback error" ohne Grund
        * die SDK-eigene Netzarbeit ist im Seiten-Netzwerkmitschnitt UNSICHTBAR -
          sie laeuft im iframe sdk.scdn.co/embedded (eigenes Debug-Ziel!)
        * IM IFRAME gemessen: seektable 200, dann storage-resolve **403**
        * dieselbe storage-resolve-Adresse, gleicher Token, per curl OHNE Origin: 200
          samt CDN-Tonadressen; MIT "Origin: https://sdk.scdn.co": 403

      AUSGESCHLOSSEN (alles gemessen, nichts davon war es): Token, Konto
      (premium), Berechtigungen (streaming vorhanden), Widevine (CDM erzeugt
      einen echten 1679-Byte-Lizenzantrag), Codecs (MSE+Element koennen alles),
      Autoplay-Schalter, Netz, Tonweg (Testton kommt raus).

      MESSFALLEN dieses Abends:
        * "Corked: no" bei pactl beweist NICHTS - librespot/Chromium halten die
          Tonsenke offen, auch wenn nichts spielt. Nur eine NEU auftauchende
          Quelle plus Spotifys Fortschritt zaehlt.
        * Ein ZWEITER SDK-Spieler in derselben Seite wird nie ready (der erste
          haelt den CDM) - Messspieler-Experimente sind wertlos.
        * Der Netzwerkmitschnitt der SEITE sieht die SDK-Abrufe nicht - immer
          das iframe-Ziel (type=iframe in /json/list) belauschen.

      FOLGE: Umbau auf librespot (siehe spotify-librespot-architektur).

  - kind: falle
    id: librespot-app-token-vergiftet-sitzung
    title: "librespot: jeder Titel 'not available' = ZU ALTE Fassung (BEWIESEN)"
    sev: high
    match: "is not available|Track should be available, but no alternatives found"
    cause: "librespot bis 0.7.x holt die Metadaten vom ALTEN Endpunkt, der die Tondatei nicht mehr mitliefert -> files ist leer -> not available. Der App-Token ist UNSCHULDIG (A/B-Test)"
    fix: "librespot >= 0.8.0 verwenden (get_extended_metadata). Selber bauen - es gibt keine fertigen Binaerdateien"
    tags: [spotify, librespot, auth, falle, 2026]
    body: |
      SYMPTOM (2026-07-27, librespot 0.6.0-dev vom MuPiBox-Upstream):

        WARN  <spotify:track:...> is not available
        ERROR Track should be available, but no alternatives found.

      Die Tonsenke oeffnet sich dabei (pactl zeigt PipeWire ALSA [librespot],
      Corked: no) - OHNE dass je ein Ton entsteht. Nicht davon taeuschen lassen.

      WAS DIE MELDUNG WIRKLICH HEISST (Quelltext player.rs,
      find_available_alternative - in 0.6 und 0.8 identisch): sie faellt NUR,
      wenn
        * die Verfuegbarkeitspruefung OK ist (also KEINE Laender-/Kontosperre)
        * UND `files` LEER ist - die Metadaten enthalten keine Tondatei
        * UND es keine Alternativfassung gibt
      Es ist also ein METADATEN-Problem, keine Rechte-Sperre.

      WAHRSCHEINLICHSTE URSACHE: zu altes librespot. Bis einschliesslich 0.7.x
      ruft spclient `get_metadata("track", id)` (alter Endpunkt); 0.8.0 ruft
      `get_metadata(ExtensionKind::TRACK_V4, uri)`. Der Changelog von 0.8.0
      sagt genau das: "Fixed a problem where the metadata didn't include the
      audio file by switching to get_extended_metadata". Das passt exakt auf
      `files.is_empty()`.

      A/B-TEST GEFAHREN (2026-07-28), gleiche Anmeldung per --access-token,
      gleiches Konto, gleiches Album - nur die Fassung getauscht:

        0.6.0-dev   "no alternatives found" bei JEDEM Titel, kein Ton
        0.8.0       Fortschritt 11697 -> 17861 ms, Tondatei geladen
                    (2,5 MB im Zwischenspeicher), 0x "no alternatives found"

      Damit ist die urspruengliche Vermutung WIDERLEGT: der App-Token
      "vergiftet" die Sitzung NICHT. Sie war plausibel (dasselbe Konto scheitert
      im Browser an Spotifys Sperre) - aber falsch. Es war allein die Fassung.

      FOLGE: die Token-Ansaat in config/templates/env-librespot ist WIEDER
      drin - eine frische Box meldet sich damit selbst an und braucht KEINE
      Handy-Uebergabe. (Der dortige Test hiess im Original `"$access-token"`,
      also $access + Text "-token" = immer wahr; auf $access_token berichtigt.)

      MERKE: der MuPiBox-Upstream liefert nur bis dev_0.6_20250806 (Stand
      2026-07). Wer dessen Binaerdatei nimmt, ist ein Jahr hinten - und laeuft
      in genau diesen Fehler.

  - kind: architektur
    id: spotify-librespot-architektur
    title: "Spotify seit 2026-07: librespot spielt, die Box reicht Listen durch"
    tags: [spotify, librespot, architektur, 2026]
    body: |
      DIE DREI TEILE (nach Spotifys Browser-Sperre, siehe
      spotify-web-api-nicht-mehr-aus-dem-browser + spotify-web-sdk-tonabruf-tot):

      1. LISTEN/SUCHE: die Oberflaeche fragt NIE mehr api.spotify.com direkt,
         sondern /api/spotify/web/<pfad> der Box (server.ts). Serverseitig,
         ohne Origin -> geht. Erlaubnisliste in src/backend-api/src/spotify-web.ts
         (getestet in spotify-web.spec.ts): nur lesende Katalog-/Bibliothekspfade
         plus GENAU EINE Wiedergabe-Ausnahme: me/player/devices (lesend, fuer die
         Geraetepruefung). Steuerbefehle bleiben draussen.

      2. TON: librespot (0.6.0-dev vom MuPiBox-Upstream, /usr/bin/librespot)
         als systemd-Dienst librespot.service - User=dietpi +
         XDG_RUNTIME_DIR=/run/user/1000 (PipeWire gehoert der Benutzersitzung;
         die alte root+pulseaudio-Unit fand keinen Tonserver). Geraetename =
         mupibox.host ("MuPiBox"). FASSUNG >= 0.8.0 zwingend (siehe Falle
         librespot-app-token-vergiftet-sitzung). Anmeldung: Token-Ansaat aus
         der Box-Konfiguration beim ersten Start, danach eigene
         credentials.json - keine Handy-Uebergabe noetig.

      3. STEUERUNG: unveraendert spotify-control (5005) ueber die Web-API -
         die Wiedergabe-Endpunkte beantwortet Spotify weiterhin aus jedem
         Kontext. setActiveDevice() waehlt seit heute NACH NAMEN (mupibox.host)
         statt devices[0] - devices[0] erwischte Geister (Browser-Tabs, Handys).

      FRONTEND: spotify-player.service.ts - shouldUsePlayer() fest false
      (SDK-Rente, Begruendung im Code), ensurePlayerReady() wartet auf das
      librespot-Geraet in der Liste (ueber die Box-Durchreiche, geht aus jeder
      Herkunft). media.service pollt /player/state im 2-s-Takt (billig: der
      Zustand ist im Wiedergabedienst zwischengespeichert).

      EINMALIGE EINRICHTUNG einer neuen Box: Spotify-App auf dem Handy ->
      Geraete -> "MuPiBox" waehlen. Danach liegt credentials.json in
      /home/dietpi/.cache/spotify und librespot meldet sich selbst an.

      PRUEFUNG: mupi-check spotifykette (librespot-Dienst, Anmeldung, Geraet,
      vergiftete-Sitzung-Erkennung) + mupi-check spotifyrechte (Origin-Vergleich).
  - kind: referenz
    id: librespot-versionen-2026
    title: "librespot-Versionen: was aktuell ist und woher man es bekommt"
    tags: [spotify, librespot, versionen, 2026]
    body: |
      STAND 2026-07-27:

        librespot (Rust, librespot-org)   0.8.0 vom 2025-11-10  <- aktuell
                                          0.7.1 / 0.7.0  (Aug 2025)
        MuPiBox-Upstream-Binaerdatei      dev_0.6_20250806      <- ein Jahr alt
        go-librespot (devgianlu)          0.7.4 vom 2026-06-19

      WICHTIG: librespot-org veroeffentlicht KEINE fertigen Binaerdateien (die
      Releases haben null Anhaenge) - deshalb haengen alle Distributionen an
      selbstgebauten Staenden. Debian trixie hat gar kein Paket.

      SELBER BAUEN auf dem Pi 5 (arm64, 2 GB RAM):
        apt-get install build-essential pkg-config libasound2-dev
        rustup (minimal) installieren
        CARGO_BUILD_JOBS=2   <- WICHTIG, mit 4 Kernen reicht der Speicher nicht
        cargo build --release --no-default-features \
          --features "alsa-backend,rodio-backend,with-libmdns"

      go-librespot ist die Alternative MIT fertigen arm64-Binaerdateien
      (~5,5 MB) und wird aktiv gepflegt - aber es ist ein anderes Programm
      (eigene YAML-Konfiguration, eigene REST-Schnittstelle), kein Ersatz Zeile
      fuer Zeile.

      NEUES IN 0.7/0.8, das uns angeht:
        0.7.0  Mercury -> Dealer fuer Connect-Nachrichten (Spotify raeumt alte
               Endpunkte ab; wer auf Mercury bleibt, faellt irgendwann aus)
        0.8.0  get_extended_metadata (siehe librespot-app-token-vergiftet-sitzung),
               Geraete-Aliase, Wiedergabe-Uebernahme per SpClient
  - kind: falle
    id: spotify-control-liegt-woanders
    title: "spotify-control.js liegt in EINEM ANDEREN Ordner als server.js"
    sev: high
    match: "Aenderung wirkt nicht|verhaelt sich unveraendert|spotify-control"
    cause: "pm2 startet spotify-control.js aus /home/dietpi/.mupibox/spotifycontroller-main/, nicht aus Sonos-Kids-Controller-master/ wo server.js liegt"
    fix: "beim Ausliefern in BEIDE Orte kopieren; pruefen mit `pm2 describe spotify-control | grep 'script path'`"
    tags: [deploy, spotify, pm2, falle, 2026]
    body: |
      DIE FALLE: die beiden Hintergrundstuecke liegen NICHT beieinander.

        server.js           /home/dietpi/.mupibox/Sonos-Kids-Controller-master/
        spotify-control.js  /home/dietpi/.mupibox/spotifycontroller-main/   <-- !

      Wer spotify-control.js neben server.js legt (was naheliegt, es liegt dort
      auch eine Kopie herum), aendert eine Datei, die NIEMAND ausfuehrt. Die
      Box laeuft unveraendert weiter, Protokollzeilen aus dem neuen Code
      erscheinen nie - und man sucht den Fehler im Code statt im Pfad.

      GENAU SO PASSIERT (2026-07-28): zwei Korrekturen am Wiedergabedienst
      (Geraetewahl nach Namen, Erstwiedergabe nach dem Boxstart) wurden
      ausgeliefert und blieben wirkungslos. Aufgefallen erst, als eine neue
      Protokollzeile partout nicht auftauchte.

      IMMER PRUEFEN, welche Datei wirklich laeuft:
        pm2 describe spotify-control | grep "script path"

      Das Auslieferskript src/deploy_on_device.sh kopiert jetzt in beide Orte.

  - kind: falle
    id: spotify-uebernahme-legt-librespot-lahm
    title: "transferMyPlayback auf frisches librespot = Stille (403)"
    sev: high
    match: "context is not available|Invalid state|403.*play|erster Antipper"
    cause: "eine Uebernahme (transferMyPlayback) auf ein frisch gestartetes librespot laesst es einen leeren Zusammenhang laden; es landet in einem kaputten Zustand und der folgende Abspielbefehl bekommt 403"
    fix: "NICHT uebernehmen - nur die Geraetekennung aufloesen und mit device_id abspielen; Spotify aktiviert das Geraet dabei selbst"
    tags: [spotify, librespot, connect, falle, 2026]
    body: |
      SYMPTOM: nach dem Einschalten der Box tut der erste Antipper nichts
      (Befehl wird mit 200 quittiert), der zweite spielt.

      A/B GEMESSEN (2026-07-28), gleiche Lage, frisch gestartetes librespot:

        mit Uebernahme:   transfer 204 -> play 403 -> STUMM
                          librespot: 5x "context is not available. type: Default"
                                        "failed to handle request: Invalid state"
        ohne Uebernahme:  play 204 -> SPIELT

      Die Abspieloptionen sind UNSCHULDIG: mit/ohne offset, Zahl oder Text -
      alle drei Formen spielen, solange keine Uebernahme davor lief.

      WARUM: transferMyPlayback ohne laufenden Zusammenhang schiebt librespot
      einen "Default"-Kontext unter, den es nicht aufloesen kann. Danach lehnt
      Spotify den Abspielbefehl fuer dieses Geraet ab.

      RICHTIG in spotify-control: `geraetAufloesen()` holt die Geraeteliste,
      sucht die Box am NAMEN (mupibox.host) und merkt nur die Kennung -
      `playMe()` spielt dann mit device_id. Die alte setActiveDevice() mit
      Uebernahme bleibt nur noch fuer Fremdfaelle stehen.

      DAZU: nach einem librespot-Neustart ist die gemerkte Kennung tot. Der
      "Device not found"-Zweig verwirft sie jetzt (activeDevice = null), sonst
      laufen alle Wiederholungen in dieselbe tote Kennung.

  - kind: falle
    id: librespot-panik-beim-kaltstart
    title: "librespot stuerzt beim Booten ab: NoDeviceAvailable"
    sev: mid
    match: "NoDeviceAvailable|panicked at .*rodio.rs|Player shut down unexpectedly"
    cause: "librespot startet, bevor der Tonserver der Benutzersitzung eine Senke hat; der rodio-Ausgang bricht hart ab"
    fix: "im ExecStart auf eine Tonsenke warten (pactl list short sinks); 2026-08-02 nachgemessen: 51 ms, nicht 90 s"
    tags: [librespot, boot, systemd, falle, 2026]
    body: |
      GEMESSEN (2026-07-28, Pi 5): beim Kaltstart zwei Fehlstarts, erst der
      dritte lief - systemd faengt es mit Restart=always ab, aber es kostet
      ueber eine Minute, in der die Box Befehle annimmt und nicht spielt.

        panicked at playback/src/audio_backend/rodio.rs:222:61:
        called `Result::unwrap()` on an `Err` value: NoDeviceAvailable
        ERROR librespot_playback::player] Player Commands Error: channel closed
        ERROR librespot] Player shut down unexpectedly

      LOESUNG in librespot.service:

        ExecStartPre=/bin/bash -c 'for i in $(seq 1 90); do \
          pactl list short sinks 2>/dev/null | grep -q . && exit 0; sleep 1; done; exit 0'

      Danach: 0 Abstuerze, 0 Neustarts (gegengeprueft ueber zwei Neustarts).
      Das `exit 0` am Ende ist Absicht - laeuft die Wartezeit ab, soll
      librespot trotzdem starten und es selbst versuchen.

      ── NACHGEMESSEN 2026-08-02: KOSTET DIE SCHLEIFE 90 s? NEIN ────────
      Verdacht war, `pactl` scheitere im Dienstkontext (User=dietpi, keine
      Sitzung, "Failed to load cookie file") und die Schleife laufe deshalb
      bei JEDEM Start voll durch. Mit tools/librespot-warten-messen.sh in
      einer transienten Unit gemessen, die User und Environment aus der
      echten Unit uebernimmt:

        Bedingung `pactl list short sinks | grep -q .`      27 ms   OK
        die GANZE Schleife (seq 1 90)                       51 ms   OK
        `source /etc/librespot/env-librespot` (5x jq)       37 ms   OK

      Die Bedingung ist im Dienstkontext also GESUND. Der Grund: die Unit
      setzt selbst `Environment=XDG_RUNTIME_DIR=/run/user/1000` - das ist
      genau das, was von Hand fehlt. Wer `pactl` per `sudo` in der
      SSH-Sitzung probiert, misst einen anderen Kontext und sieht die
      Cookie-Meldung, die der Dienst nie zu sehen bekommt.

      WANN DIE 90 s DOCH KOMMEN: nur ohne Benutzersitzung. Ohne
      /run/user/1000/pulse scheitert pactl in 33 ms, und drei Schleifen-
      runden kosten 3,04 s - also 1 s je Runde, mal 90 = die vollen 90 s.
      Am BOOT ist das Fenster winzig (Protokoll des letzten Starts):

        13,11 s  librespot.service gestartet   <- Schleife beginnt
        13,67 s  /run/user/1000 angelegt
        14,21 s  pipewire-pulse bereit         <- Schleife bricht ab

      Der Boot kostet also rund 1,1 s, nicht 90. Die 90 s treffen den Fall
      "librespot wird neu gestartet, waehrend die Kiosk-Sitzung weg ist" -
      und dann gibt es ohnehin keinen Ton, gleich wie lange man wartet.
      Wichtig fuer den Waechter (scripts/librespot/librespot-waechter.sh):
      solange die Sitzung steht, kostet sein `systemctl restart` 51 ms.

      DIE FALLE BEIM NACHMESSEN - eine Probe, die sich selbst zufrieden-
      stellt: Wer `pactl list short sinks 2>&1 | grep -q .` misst statt
      `2>/dev/null` wie in der Unit, schiebt die FEHLERMELDUNGEN in die
      Roehre. `grep -q .` findet sie und meldet Erfolg. Das Werkzeug hat
      genau so zuerst "auch ohne Tonserver alles gut" gemeldet. Dieselbe
      Sorte wie mupi-audio-guard-stumm.

      TAUGLICHKEIT DER ALTERNATIVEN, im Fehlerfall gegengemessen:
        test -S /run/user/1000/pulse/native   20 ms, scheitert richtig  OK
        wpctl status                          39 ms, PipeWire direkt    OK
        aplay -l | grep ^card                 43 ms - MELDET AUCH OHNE
                                              TONSERVER ERFOLG. Falsche
                                              Entwarnung, unbrauchbar.
        aplay -D default (Stille)           1219 ms je Runde - zu teuer

      Auf ALSA statt PulseAudio zu warten ist also NICHT die Loesung,
      obwohl librespot ueber ALSA spielt (kein LIBRESPOT_BACKEND gesetzt,
      rodio -> cpal -> ALSA -> pipewire-alsa): `aplay -l` liest nur
      /proc/asound und weiss vom Tonserver nichts.
  - kind: architektur
    id: dienste-umschalten
    title: "Umschalten Jellyfin <-> Spotify: zwei Tonmaschinen, eine Regel"
    tags: [spotify, jellyfin, ton, umschalten, 2026]
    body: |
      ZWEI UNABHAENGIGE TONMASCHINEN, die nichts voneinander wissen:

        Spotify         librespot   eigener Prozess, laeuft DAUERHAFT
        Jellyfin/lokal  mpv         wird je Wiedergabe gestartet und beendet

      Das Hintergrundstueck fuehrt nur EINEN `currentPlayer`. Deshalb schickt
      die Oberflaeche vor jedem Start ein `stopCurrent()` an /player/<raum>/stop;
      der Raum ist egal, der Auffangbereich nimmt jeden. Dort wird je nach
      currentPlayer `spotifyApi.pause()` (Spotify) oder `player.stop()` (mpv)
      ausgefuehrt.

      DIE GROSSE MESSFALLE: eine offene Tonquelle beweist GAR NICHTS.
      librespot haelt seine PipeWire-Quelle IMMER offen - auch pausiert, und
      pactl meldet sie sogar als nicht stillgelegt (Corked: no). Beim
      Umschalten sieht man deshalb regelmaessig ZWEI Quellen und haelt das
      faelschlich fuer Ueberlagerung.

      BEWEIS, dass es kein Fehler ist (2026-07-28, Pegel am Mithoerausgang der
      Standardsenke gemessen):

        alles gestoppt   STILL      (librespots Quelle war dabei OFFEN)
        Jellyfin         0.776
        Spotify          0.317
        Jellyfin         0.788
        Spotify          0.322
        gestoppt         STILL

      Also: Hin- und Herschalten funktioniert, ohne Ueberlagerung, ohne
      Aussetzer. Wer es nachmessen will: `mupi-check umschalten` - das spielt
      kurz ab und misst genau diese Folge.

      RICHTIG MESSEN heisst hier: parec auf <senke>.monitor, ~3 s, Spitzenwert.
      Kuerzere Scheiben (<1 s) liefern nichts, weil parec erst anlaufen muss,
      und eine schlafende Senke liefert gar keine Daten - das IST Stille, aber
      es sieht wie ein Messfehler aus.

      WARUM NICHT MIT PIPEWIRE UMHAENGEN: Umhaengen (`pw-link`, Senke wechseln)
      beantwortet eine ANDERE Frage - WOHIN der Ton geht (Klinke, Bluetooth) -
      und nicht, WELCHE Quelle spielt. Und librespot toeten statt pausieren
      waere schaedlich: es verschwindet dann aus Spotifys Geraeteliste und
      braucht Sekunden, bis es wieder da ist. Pausieren ueber die Web-API ist
      richtig - und gemessen wird es dabei wirklich still.

      NUR ALS ROOT PRUEFEN GEHT NICHT: pactl/parec gehoeren dem Tonbenutzer
      (uid 1000). Als root kommt "XDG_RUNTIME_DIR is not owned by us"; in
      mupi-check laeuft es deshalb ueber `runuser -u dietpi`. Dazu schreibt
      pactl eine Warnung ueber eine fehlende Keksdatei MIT ins Ergebnis - den
      Senkennamen also herausfiltern, nicht die ganze Ausgabe nehmen.
  - kind: plan
    id: mitschnitt-plan
    title: "PLAN: Mitschnitt/Archiv mit Dienst-Anmeldung, Puffer und Stueckelung"
    tags: [plan, mitschnitt, recorder, spotify, jellyfin, 2026]
    body: |
      ENTWURF vom 2026-07-28, NICHT gebaut. Ziel: die Box legt gehoerte Titel
      dauerhaft ab - aber nur dort, wo es noetig ist, und ohne dass jemand
      etwas bedienen muss.

      == BINDENDE VORGABE (Nutzer, 2026-07-28) - VOR ALLEM ANDEREN ==

      Der MITSCHNITT ist eine TESTFUNKTION. Sie wird gebaut, aber sie darf
      niemandem passieren.

        1. STANDARDMAESSIG AUS. Nicht "aus als Vorgabe, die man mal setzt" -
           aus im Auslieferungszustand, aus nach jeder Neuinstallation, aus
           fuer jeden neu hinzukommenden Dienst.
        2. JE DIENST EINZELN waehlbar. KEIN globaler Mitschnitt-Schalter.
           Wer Spotify einschaltet, hat damit nichts ueber einen kuenftigen
           anderen Dienst gesagt. Ein Dienst, der sich neu anmeldet (siehe
           Punkt 1 des Plans), kommt AUS herein - immer.
        3. KLAR ALS TESTZWECK BESCHRIFTET, an der Stelle, an der man ihn
           einschaltet. Nicht in einer Hilfeseite, nicht im Wiki - dort, wo
           der Schalter sitzt.
        4. HINWEIS AUF RECHT UND AGB beim Einschalten, mit ausdruecklicher
           Bestaetigung (nicht nur ein Klick auf den Schalter):
             * Die Rechtslage ist LAENDERSACHE. In Deutschland gibt es die
               Privatkopie (§53 UrhG), anderswo gilt anderes. Der Betreiber
               muss das fuer SEIN Land pruefen - die Software weiss es nicht
               und darf nicht so tun.
             * Die AGB des jeweiligen Dienstes koennen es unabhaengig vom
               Gesetz verbieten. Spotifys AGB tun das.
             * ES KANN ZUM VERLUST DES DIENSTES KOMMEN. Sperrung oder
               Kuendigung des Kontos ist ein reales Risiko. Auf dieser Box
               waere das besonders bitter: es ist das FAMILIENKONTO - weg ist
               dann nicht das Archiv, sondern das Abspielen ueberhaupt.
        5. SICHTBAR, WAEHREND ER LAEUFT (Transparenzanzeige, siehe Punkt 2) -
           und jederzeit abschaltbar.

      == ES IST DIE ENTSCHEIDUNG DES BETREIBERS ==

      Die Software STELLT DIE MOEGLICHKEIT BEREIT. Ob, wofuer und unter
      welchem Recht sie genutzt wird, entscheidet allein der Mensch, der die
      Box betreibt. Das muss aus der Umsetzung HERVORGEHEN, nicht nur
      irgendwo behauptet werden:

        * KEINE VOREINSTELLUNG, DIE ENTSCHEIDET. Nichts ist vorab an, nichts
          ist als "empfohlen" markiert, es gibt keinen Assistenten, der zum
          Einschalten fuehrt. Wer nichts tut, hat keinen Mitschnitt.
        * KEINE WERTUNG IM TEXT - in keine Richtung. Der Schalter wirbt nicht
          ("endlich deine Musik sichern") und predigt nicht. Er nennt die
          Tatsachen und ueberlaesst den Schluss dem Betreiber.
        * KEINE RECHTSAUSKUNFT. Die Software sagt NICHT, dass etwas erlaubt
          oder verboten ist - sie kann das Land nicht kennen und den Fall
          nicht beurteilen. Sie sagt: das ist zu pruefen, und zwar von dir.
        * DIE BESTAETIGUNG IST DIE ENTSCHEIDUNG. Ein Schalter allein genuegt
          nicht; es braucht ein ausdrueckliches Ja auf einen Text, der Recht,
          AGB und Kontorisiko benennt. Wer das wegklickt, hat entschieden -
          und soll das auch so erlebt haben.
        * NACHVOLLZIEHBAR: festhalten, WANN und fuer WELCHEN Dienst
          freigegeben wurde. Nicht zur Ueberwachung, sondern damit spaeter
          niemand raetselt, ob das jemand wollte oder ob es "von selbst"
          angegangen ist.
        * EIN UPDATE ENTSCHEIDET NIE MIT. Eine neue Fassung darf nichts
          einschalten, keine Freigabe auf einen anderen oder neuen Dienst
          uebertragen und keine Bestaetigung als schon erteilt annehmen. Im
          Zweifel: aus, neu fragen.
        * WIDERRUFBAR: Ausschalten wirkt sofort. Bereits Aufgenommenes wird
          dabei NICHT heimlich geloescht - die Box sagt, was da ist, und der
          Betreiber entscheidet auch darueber.

      Kurz: die Box baut das Werkzeug, sie benutzt es nicht von sich aus.

      WICHTIGE ABGRENZUNG, damit die Warnung nicht verwaessert:
      Sie haengt an der METHODE, nicht am Archivieren an sich. Das
      Kapabilitaetsmodell (Punkt 1) macht das sauber:

        art: 'kopie'       HARMLOS. Eigene Dateien von der eigenen Jellyfin-
                           Instanz bzw. der eigenen Platte. Keine Warnung,
                           keine Testkennzeichnung, darf standardmaessig an
                           sein.
        art: 'mitschnitt'  Traegt die komplette Vorgabe oben.

      Wer also nur Jellyfin und lokale Dateien offline haelt, bekommt keine
      einzige dieser Huerden zu sehen - zu Recht.

      == LEITGEDANKE ==
      NICHT MITSCHNEIDEN, WAS MAN KOPIEREN KANN. Ein Mitschnitt ist immer die
      schlechtere Kopie: Echtzeit, verlustbehaftet, laut/leise-abhaengig,
      abhaengig davon dass wirklich abgespielt wird.

      GEMESSEN (2026-07-28): Jellyfin liefert unter
      `/Audio/<id>/stream?static=true` die ORIGINALDATEI (Content-Type
      audio/mpeg, 8 MB, Container mp3 = genau das, was auf dem Server liegt).
      Das ist DIESELBE URL, die die Box ohnehin zum Abspielen baut - ein
      "Download" ist dort also nur Speichern statt Abspielen, kein neuer Weg.
      (`/Items/<id>/Download` beantwortet HEAD mit 405; mit GET vermutlich auch
      brauchbar, ungetestet - static=true genuegt.)
      Spotify dagegen gibt die Datei nie heraus -> nur dort ist Mitschnitt noetig.

      == 1. DIENSTE MELDEN SICH AN (die Registrierung) ==
      Jeder Dienst erklaert, WIE er archiviert werden kann. Ein neuer Dienst
      bringt seine Erklaerung mit, der Recorder muss ihn nicht kennen:

        art: 'kopie'      der Dienst nennt eine URL zur Originaldatei
                          -> holen, taggen, fertig. Kein Ton noetig.
                          (jellyfin; spaeter Subsonic/Navidrome/lokale Dateien)
        art: 'mitschnitt' nur der Tonweg ist verfuegbar
                          -> Echtzeit ueber den Sink-Monitor. (spotify)
        art: 'keins'      darf/soll nicht archiviert werden (z.B. Radio, TTS)

      Dazu je Dienst: wie komme ich an Titel/Interpret/Album/Cover/Laenge
      (fuer Tags) und woran erkenne ich einen TITELWECHSEL (siehe 3.).
      Sinnvoller Ort: neben den bestehenden `media-provider`-Eintraegen im
      Frontend bzw. als kleine Tabelle im Wiedergabedienst - dort weiss man
      ohnehin schon, welcher Dienst gerade laeuft.

      == 2. TRANSPARENZ-MODUS (der Puffer) ==
      Der Recorder laeuft im Hintergrund und legt VOLLSTAENDIG gehoerte Titel
      von selbst ab. Kein Knopf, keine Bedienung.

      Aufbau: fortlaufender Mitschnitt in einen ROHPUFFER; bei jedem
      Titelwechsel wird der zugehoerige Bereich herausgeschnitten. War der
      Titel vollstaendig -> kodieren und ablegen; war er es nicht -> als
      BRUCHSTUECK behalten (siehe 4.).

      WICHTIG (heute gemessen): PipeWire legt eine untaetige Senke schlafen,
      dann liefert parec GAR NICHTS. Ein Dauerlaeufer muss also entweder die
      Senke wachhalten oder - besser - erst bei Wiedergabebeginn anlaufen. Die
      Box weiss den Moment: `/player/state` bzw. `/player/local`.

      == 3. TITELGRENZEN ==
      spotcap (der bestehende Recorder am Rechner) schneidet an MPRIS-
      Ereignissen. Das geht hier NICHT: librespot hat kein MPRIS (Issue #1596,
      offen). Die Box hat aber etwas Besseres - sie weiss es selbst:

        /player/state   Spotify: Titel, Album, progress_ms, is_playing
        /player/local   mpv:     currentTrackname, currentPlayer

      EINE Quelle fuer BEIDE Tonmaschinen. Heute wird sie alle 2 s abgefragt;
      fuer saubere Schnitte waehrend einer Aufnahme dichter abfragen (0,25 s)
      oder ein Ereignis einbauen.

      SCHNITTPOSITION nicht ueber die Uhr rechnen, sondern wie spotcap:
      geschriebene PCM-Bytes / Byterate. Das ist drift- und latenzfrei.

      == 4. BRUCHSTUECKE ZUSAMMENSETZEN ==
      Der Fall des Nutzers: ein Stueck Lied hoeren, wegspringen, spaeter
      zurueckkommen - und der Recorder setzt den Titel aus den Teilen zusammen.

      JEDES BRUCHSTUECK BEKOMMT: Titel-Id, Startposition im Titel (progress_ms
      beim Beginn), Dauer. Damit liegen die Teile schon grob auf der Zeitachse
      des Titels - eine Tonsuche ist fuer die grobe Lage NICHT noetig.

      DIE NAHTSTELLE GENAU TREFFEN - und hier ist der Kniff:
      Beide Teile kommen aus DERSELBEN dekodierten Quelle. Im Ueberlappungs-
      bereich sind die Abtastwerte deshalb IDENTISCH, solange die Lautstaerke
      dazwischen nicht veraendert wurde (der Mitschnitt sitzt HINTER dem
      Lautstaerkeregler). Also:

        Fall A (Regel): kurzes Fenster aus Teil 2 im Ueberlappungsbereich von
                        Teil 1 EXAKT suchen (Bytevergleich/Hash) -> abtastgenau,
                        billig, kein Rechenaufwand der Rede wert.
        Fall B (Lautstaerke geaendert): beide Fenster auf gleichen Effektivwert
                        normieren, dann Kreuzkorrelation. Auf einem Pi 5 nur
                        sinnvoll mit vorheriger Verkleinerung (auf 8 kHz
                        heruntersetzen, grob suchen, dann in kleinem Fenster
                        fein) - und braucht numpy (nicht geprueft, ob da).

      ABSICHTLICH UEBERLAPPEN: beim Fortsetzen ein paar Sekunden VOR der
      bekannten Position mitschneiden. Das kostet nichts und macht Fall A erst
      moeglich.

      EINFACHERE ALTERNATIVE, ernsthaft erwaegen: die Luecke spaeter gezielt
      NACHSPIELEN statt zu stueckeln (spotcap kann das schon: "nur fehlende
      Titel"). Nachts, still ueber einen Null-Sink. Kein Zusammensetzen, keine
      Nahtstellen, keine Korrelation - dafuer laeuft die Box laenger.

      == 5. ABLAGE ==
      - Bruchstuecke als ROH-PCM halten, erst beim vollstaendigen Titel EINMAL
        kodieren. Niemals kodierte Teile aneinanderhaengen (Verlust auf Verlust,
        und die Rahmengrenzen passen nicht).
      - ACHTUNG SD-KARTE: das Wurzelverzeichnis liegt auf `mmcblk0p2` (119 GB).
        Dauerhaftes Roh-PCM-Schreiben (~10 MB/min) frisst Schreibzyklen. Also
        Puffer in den Arbeitsspeicher (tmpfs) und nur fertige Titel auf die
        Karte - oder gleich auf eine Netzfreigabe.
      - Buchfuehrung wie spotcap: ein Index je Titel mit "vollstaendig ja/nein"
        und welche Bereiche vorliegen. Vollstaendig ab ~95 % Abdeckung.

      == 6. WAS FEHLT ==
      - `ffmpeg` ist auf der Box NICHT installiert (fpcalc auch nicht) - fuers
        Kodieren/Taggen noetig.
      - Rechenzeit teilt sich mit dem Kiosk (Pi 5, ~1,2 GB frei). Ein Encode
        nebenher ist machbar, aber es ist ein Kindergeraet: es darf nicht
        ruckeln. Kodieren am besten dann, wenn nichts spielt.

      == 7. ENTSCHIEDEN (Nutzer, 2026-07-28): ALLES BAUEN, ABER WAEHLBAR ==
      Mit denselben Vorbehalt wie in [quellen-verschmelzen] Punkt 7: waehlbar
      ja, aber nicht als Haufen unabhaengiger Haekchen.

        Puffer        'bei Wiedergabe' (Vorgabe) | 'dauerhaft'
                      dauerhaft muss die Senke wachhalten (sonst liefert parec
                      nichts) und schreibt staendig - nur mit Puffer im
                      Arbeitsspeicher sinnvoll.
        Umfang        'nur spotify' (Vorgabe) | 'alles was klingt'
                      "nur spotify" heisst: dessen Stream abgreifen, nicht die
                      Standardsenke. Jellyfin/lokal braucht keinen Mitschnitt
                      (die Datei gibt es ja) - siehe Leitgedanke.
        Luecken       'stueckeln' | 'nachts nachspielen' | BEIDES (Vorgabe)
                      Die schliessen sich NICHT aus, sie ergaenzen sich:
                      zusammensetzen, was schon da ist - und nur was dann noch
                      fehlt, nachts nachspielen. Reine Stueckelung laesst
                      Loecher, reines Nachspielen laesst die Box unnoetig
                      lange laufen.
        Ziel          SD (Vorgabe) | USB | Netzfreigabe
                      SD nur fuer fertige Titel; Rohpuffer NIE dauerhaft auf
                      die Karte (Schreibzyklen, / liegt auf mmcblk0p2).

      VORGABE FUER DEN MITSCHNITT SELBST: AUS, JE DIENST EINZELN, mit
      Bestaetigung und Testkennzeichnung - siehe die bindende Vorgabe ganz
      oben in diesem Eintrag. Die Einstellungen hier (Puffer/Umfang/Luecken/
      Ziel) gelten erst, NACHDEM ein Dienst ueberhaupt freigegeben wurde.

      == 8. RECHT UND FAMILIE ==
      Der Nutzer hat das fuer spotcap selbst geprueft: Privatkopie (§53 UrhG),
      keine DRM-Umgehung, ABER die Spotify-AGB verbieten es (Kontorisiko), und
      Kopien muessen privat bleiben. Auf der Box kommt hinzu: es ist ein
      FAMILIENGERAET. Ein Dauermitschnitt nimmt alles auf, was die Kinder
      hoeren - also sichtbar anzeigen, dass er laeuft, und abschaltbar machen.

      == 9. NICHT NEU ERFINDEN ==
      `~/Downloads/SpotifyRecorder` (spotcap) hat das Schwere schon: Aufnahme
      vom Sink-Monitor, Schnitt an Titelgrenzen, Tags samt Cover, Index mit
      Vollstaendigkeit, Isolation ueber Null-Sink, "nur fehlende Titel".
      Das hier ist eine PORTIERUNG mit anderer Titelquelle (Punkt 3), nicht
      ein neuer Bau.
  - kind: plan
    id: quellen-verschmelzen
    title: "PLAN: EIN Werk, mehrere Quellen - Reihenfolge, Offline, Konfigseite"
    tags: [plan, bibliothek, spotify, jellyfin, offline, 2026]
    body: |
      ENTWURF vom 2026-07-28. Gehoert zu [mitschnitt-plan].

      NACHTRAG 2026-08-02: Punkt 3 (Aufloesung beim Abspielen) IST gebaut -
      siehe [verschmelzung-aufloesung-stufe1]. Ebenfalls gemessen: die
      Ueberschneidung zwischen den Diensten ist auf dieser Box derzeit NULL
      (22 Eintraege, 21 spotify, 1 jellyfin, 0 lokal). Der ABGLEICH bleibt
      deshalb ungebaut - es gibt nichts, woran man ihn pruefen koennte.

      == 0. DIE STRATEGISCHE EINSICHT (belegt, nicht gefuehlt) ==
      Der Nutzer sagte "Spotify ist das fragilste Glied". Das stimmt messbar.
      An EINEM Tag (2026-07-27/28) fiel Spotify auf DREI unabhaengige Arten aus:

        1. Web-API beantwortet Browser-Aufrufe nicht mehr (403, Plattformumbau)
        2. Web-SDK bekommt seinen Tonabruf verweigert (403, storage-resolve)
        3. librespot zu alt -> jeder Titel "not available" (Metadaten-Endpunkt)

      Dazu bauartbedingt: braucht Premium, braucht Netz, braucht ein aktives
      Konto, laeuft nur in Echtzeit, und die AGB verbieten das Archivieren.

      Jellyfin und lokal fielen an dem Tag KEIN EINZIGES MAL aus - Jellyfin
      lieferte die Originaldatei beim ersten Versuch.

      FOLGE FUER DEN BAU: Spotify ist die ENTDECKUNGS- und Streaming-Schicht,
      NICHT das Archiv. Alles, was dauerhaft verfuegbar sein soll, muss lokal
      oder in Jellyfin liegen. Die Quellen-Reihenfolge lokal -> jellyfin ->
      spotify ist deshalb nicht Geschmack, sondern die Absicherung.

      == 1. IDENTITAET: woran erkennt man dasselbe Album? ==
      AM GERAET GEMESSEN (2026-07-28, "Das Lumpenpack - Die Zukunft wird gross"):

        Jellyfin  MusicBrainzAlbum 275f966a-...  MusicBrainzReleaseGroup 99910011-...
                  12 Titel, 2817 s, Jahr 2017
        Spotify   external_ids.upc 3616846596675
                  12 Titel, Jahr 2017

      Beide tragen also eine ECHTE Kennung - nur verschiedene. Erkennungskette:

        a) EXAKT ueber MusicBrainz: Spotifys UPC -> MusicBrainz kennt Barcodes
           (`/ws/2/release?query=barcode:<upc>`) -> Release-MBID -> gegen
           Jellyfins MusicBrainzAlbum. Braucht Netz, ist auf 1 Anfrage/s
           begrenzt und verlangt einen eigenen User-Agent -> EINMAL abfragen,
           Ergebnis dauerhaft merken.
        b) RELEASE-GROUP statt Release: erkennt auch andere Pressungen/Editionen
           desselben Albums. Meist das, was man WILL.
        c) OHNE NETZ, als Regelfall voellig ausreichend: normalisierter
           Interpret + Titel, BESTAETIGT durch Titelanzahl und Gesamtdauer
           (+-2 %) bzw. Jahr. Im Messfall haette das gereicht.
        d) FINGERABDRUCK (Song-ID): spotcap hat das SCHON gebaut
           (spotcap_fp.py, Chromaprint/fpcalc; Schwellen an der echten
           Bibliothek kalibriert: derselbe Titel 0,035-0,046 / fremder
           0,36-0,41 -> gleich <=0,25, falsch >=0,35). Das ist die einzige
           Erkennung, die den TON vergleicht statt der Beschriftung - sie
           trifft auch dann, wenn Tags fehlen, falsch oder anders geschrieben
           sind. Fuer schlampig getaggte lokale Dateien ist sie das einzige
           brauchbare Mittel.

           ZWEI EINSCHRAENKUNGEN, ehrlich:
             * Er braucht den TON. Lokal und Jellyfin liegen als Datei vor ->
               sofort pruefbar. SPOTIFY nicht - dessen Fingerabdruck gibt es
               erst, NACHDEM etwas mitgeschnitten wurde. Die Erkennung ist
               also unsymmetrisch: lokal<->jellyfin sofort, gegen Spotify
               weiter ueber Metadaten (a-c).
             * `fpcalc` ist auf der Box NICHT installiert (gemessen 2026-07-28).

           DAFUER ZWEI GESCHENKE:
             * spotcap schreibt den Fingerabdruck bereits als Tag
               ACOUSTID_FINGERPRINT in jede Datei -> ein Archiv aus spotcap
               traegt seine Kennung mit, nichts neu zu rechnen.
             * ueber AcoustID fuehrt der Fingerabdruck zur MusicBrainz-Kennung
               -> damit bekommt auch eine voellig untaggte lokale Datei
               Anschluss an Jellyfins MusicBrainzAlbum (Punkt a). Braucht Netz
               und einen (kostenlosen) AcoustID-Schluessel.

           SAUBERSTE REGEL FUER ALBEN, wenn Fingerabdruecke vorliegen: die
           Titelmengen vergleichen statt der Titelanzahl. 12 von 14 Titeln
           gleich = dasselbe Album plus Bonustitel (Deluxe) - das ist eine
           AUSSAGE, waehrend "12 != 14, also verschieden" nur eine Vermutung
           ist. Loest die Deluxe/Remaster-Falle richtig herum: ein Remaster
           ist eine ANDERE Aufnahme, sein Fingerabdruck weicht ab.

        e) HANDBETRIEB: "das ist dasselbe" / "das ist NICHT dasselbe" muss
           speicherbar sein. Automatik wird sich irren.

      FALLE: Deluxe/Remaster/Live tragen denselben Interpret+Titel, sind aber
      andere Alben. NIE allein auf Name+Interpret verschmelzen - immer
      Titelanzahl ODER >=80 % uebereinstimmende Titelnamen verlangen. Bei
      Sammlungen ("Various Artists") ist der Interpret unbrauchbar.

      == 2. DAS MODELL: ein Werk, mehrere Quellen ==
      Statt drei Kacheln fuer dasselbe Album EINE Kachel mit Herkunftsmarken
      (die Dienst-Symbole gibt es schon: dienststatus.component.ts).

        Werk
          schluessel   normalisiert(interpret|titel) [+ MBID wenn bekannt]
          quellen      [{art:'lokal', pfad}, {art:'jellyfin', id}, {art:'spotify', id}]
          titel[]      {nr, name, dauer, da:{lokal:bool, jellyfin:bool, spotify:bool}}

      VERFUEGBARKEIT JE TITEL, nicht je Album - denn ein Album kann in Jellyfin
      vollstaendig sein und aus Spotify nur halb mitgeschnitten. Genau diese
      Buchfuehrung hat spotcabs RecordedIndex schon (vollstaendig/teilweise).

      == 3. AUFLOESUNG BEIM ABSPIELEN ==
      Reihenfolge einstellbar, Vorgabe: lokal -> jellyfin -> spotify.

      REGEL 1 (Album zuerst): gibt es EINE Quelle, die das GANZE Album hat,
      nimm die - auch wenn eine hoeher priorisierte Quelle nur Teile hat.
      Grund: jeder Quellenwechsel kostet eine Pause (Stopp + Start der anderen
      Tonmaschine, heute gemessen ~1-2 s). Zwischen Titeln ist das ertraeglich,
      bei JEDEM Titel nervt es.

      REGEL 2 (dann je Titel): hat keine Quelle alles, loese je Titel nach der
      Reihenfolge auf. Ein archivierter Titel spielt lokal, ein fehlender
      faellt auf Spotify zurueck. Das ist zugleich die Antwort auf "wenn
      offline verfuegbar, offline spielen" - es ergibt sich von selbst.

      REGEL 3 (ohne Netz): faellt Spotify aus, spielt das Werk trotzdem, nur
      eben die vorhandenen Titel. Kein Fehler, sondern ein Teilangebot - das
      muss die Oberflaeche auch so zeigen (z. B. "8 von 12 Titeln offline").

      == 4. OFFLINE VERFUEGBAR MACHEN (der Zeitplan) ==
      Je Werk ein Schalter "offline halten". Ein Fenster (z. B. taeglich
      02:00-05:00, Wochentage waehlbar) arbeitet die Liste ab.

      ZWEI SEHR VERSCHIEDENE KOSTEN - der Planer muss das wissen:
        kopie      (jellyfin/lokal) Sekunden, kein Ton, jederzeit machbar
        mitschnitt (spotify)        ECHTZEIT - ein 47-min-Album dauert 47 min,
                                    und die Box spielt dabei (still ueber einen
                                    Null-Sink, siehe mitschnitt-plan)

      Also: Kopien sofort, Mitschnitte nur im Fenster und nur wenn niemand
      hoert. spotcap hat Warteschlange + Planer bereits (spotcap_schedule.py) -
      dieselbe Form.

      == 5. DIE KONFIGSEITE ==
      Gehoert in die VERWALTUNG (/admin), nicht auf den Kinderschirm - es ist
      eine Elternsache. Aufbau analog zum Optionen-Tab von spotcap:

        Quellen        Reihenfolge per Ziehen (lokal / jellyfin / spotify),
                       je Dienst an/aus
        Mitschnitt     JE DIENST ein Schalter, alle AUS, als Testfunktion
                       beschriftet, Einschalten nur mit Bestaetigung
                       (Recht/AGB/Kontoverlust - siehe mitschnitt-plan).
                       Dienste mit art:'kopie' erscheinen hier GAR NICHT.
        Puffer         an/aus, Groesse bzw. Dauer, Ziel (SD/USB/Freigabe),
                       "nur vollstaendige Titel behalten"
        Offline        Zeitfenster von-bis + Wochentage, Bandbreitengrenze,
                       "nur wenn nichts spielt"
        Verschmelzen   Regel scharf/locker, Liste der Handentscheidungen
                       (verbunden / getrennt), Knopf "neu abgleichen"
        Transparenz    Anzeige "nimmt auf" an/aus (Familiengeraet!)

      == 6. WAS ES SCHON GIBT ==
      - Dienst-Symbole je Quelle: dienststatus.component.ts
      - Vollstaendigkeits-Buchfuehrung + Warteschlange + Planer: spotcap
      - lockerer Album-Abgleich: spotcaps core.album_have (Album exakt,
        Interpret lose) - genau der Baustein fuer 1c
      - Song-ID per Fingerabdruck: spotcap_fp.py (fpcalc, kalibrierte
        Schwellen, Ergebnis als ACOUSTID_FINGERPRINT-Tag in der Datei) -
        der Baustein fuer 1d; fpcalc muss auf der Box noch nachinstalliert
        werden
      - Jellyfin-Originaldatei per stream?static=true (gemessen)
      - Umschalten zwischen den Tonmaschinen funktioniert (mupi-check umschalten)

      == 7. ENTSCHIEDEN (Nutzer, 2026-07-28): ALLES BAUEN, ABER WAEHLBAR ==

      ACHTUNG BEIM UMSETZEN: "alles waehlbar" darf NICHT drei unabhaengige
      Haekchen bedeuten. Drei Schalter sind acht Zustaende, von denen niemand
      mehr als zwei je ausprobiert - und die Fehler tauchen in den anderen
      sechs auf. Richtig ist EINE STUFE, die man hochdreht; jede Stufe arbeitet
      nur an dem, was die vorige NICHT entscheiden konnte:

        Stufe 1  LOCKER (immer an, kein Netz, nichts nachzuinstallieren)
                 normalisierter Interpret+Titel, bestaetigt durch Titelanzahl
                 bzw. Gesamtdauer. Entscheidet die grosse Mehrheit.
        Stufe 2  FINGERABDRUCK (braucht fpcalc)
                 laeuft NUR ueber die Zweifelsfaelle aus Stufe 1 (aehnlich,
                 aber nicht sicher) sowie ueber Dateien ohne brauchbare Tags.
                 Vergleicht Titelmengen -> loest Deluxe/Remaster richtig.
        Stufe 3  ACOUSTID + MUSICBRAINZ (braucht Netz + kostenlosen Schluessel)
                 nur fuer den Rest: was Stufe 2 nicht zuordnen konnte, und die
                 Bruecke Spotify-UPC <-> Jellyfin-MBID.

      Vorteile dieser Form: jede Stufe ist billig, weil sie nur den REST
      bearbeitet; man kann bei Stufe 1 stehenbleiben und hat ein
      funktionierendes System; und jede Zuordnung traegt mit, WELCHE Stufe sie
      getroffen hat -> die Verwaltung kann anzeigen "verbunden per
      Fingerabdruck" statt eines Orakels.

      DIE ANDEREN BEIDEN PUNKTE erledigen sich mit der Stufe:
        * Release oder ReleaseGroup: waehlbar, Vorgabe ReleaseGroup
          (Editionen zusammen - das ist meist das Gemeinte).
        * Widersprechende Quellen (Jellyfin 12, Spotify 14 Titel): ab Stufe 2
          ist das keine Frage mehr, sondern eine Messung - gleiche Titelmenge
          plus Zusatztitel = EIN Werk mit Bonustiteln. Bei Stufe 1 allein:
          zwei Werke, mit Hinweis "aehnlich" und Knopf zum Verbinden.

      VORGABEN, damit es ohne Einstellen laeuft (wichtiger als die Schalter):
        Erkennung        Stufe 1
        Reihenfolge      lokal -> jellyfin -> spotify
        Verschmelzen     ReleaseGroup, lockere Regel
        Mitschnitt       AUS, je Dienst einzeln, Testfunktion mit
                         Bestaetigung (Familiengeraet + AGB + Kontorisiko)
        Offline-Fenster  aus, bis jemand eines setzt
  - kind: architektur
    id: kinderzeit
    title: "Kinderzeit: wie lange, wann, an welchen Tagen - serverseitig durchgesetzt"
    tags: [kinderzeit, eltern, backend, 2026]
    body: |
      GEBAUT 2026-07-28. Elternbereich in der Verwaltung (/admin -> Kinderzeit).

      WO ENTSCHIEDEN WIRD - und warum nicht im Browser:
      Die Zeitanzeige auf der Box zaehlte frueher in localStorage mit. Ein
      Kind, das die Seite neu laedt, haette sein Guthaben zurueckgesetzt.
      Gezaehlt und entschieden wird deshalb im backend-api, das auch den
      Abspielbefehl in der Hand hat.

      DIE TEILE:
        src/backend-api/src/kinderzeit.ts       reine Regel (getestet, 25 Faelle)
        src/backend-api/src/server.ts           Buchfuehrung + Durchsetzung
        frontend-admin/.../seiten/kinderzeit.ts Elternseite
        frontend-box/.../zeitanzeige.component  Anzeige "x min uebrig"

      DER KONTROLLPUNKT ist der /player-Proxy in backend-api: JEDER
      Abspielbefehl der Oberflaeche geht dort durch. Ein abgewiesener Start
      bekommt 403 samt GRUND (`{kinderzeit:true, grund, restMin, fensterAb,
      fensterBis}`) - damit die Oberflaeche dem Kind sagen kann, WARUM, statt
      einfach nichts zu tun.

      WICHTIGSTE REGEL DER UMSETZUNG: nur STARTBEFEHLE werden abgewiesen.
      Anhalten, Lautstaerke, Zustand abfragen gehen IMMER durch (`istStartbefehl`,
      getestet). Eine Zeitgrenze, die das Ausschalten verhindert, waere
      schlimmer als gar keine - deshalb steht in der Funktion das Verneinen
      ZUERST und gewinnt.

      GEZAEHLT WIRD NUR, WAS KLINGT: die Quelle ist /tmp/playerstate
      ('play'/'pause'), das der Wiedergabedienst fuer BEIDE Tonmaschinen
      schreibt - billig, lokal, kein Netz. Pause und Leerlauf kosten kein
      Guthaben.

      NACHSICHT AM ENDE (`nachsichtMin`, Vorgabe 5): laeuft die Zeit ab, darf
      der LAUFENDE Titel noch zu Ende gehen. Ein Kind mitten im Lied
      abzuschneiden ist der sichere Weg zu Traenen und zum Verdacht, die Box
      sei kaputt. An gesperrten Tagen gibt es keine Nachsicht (da lief nichts).

      KAPUTTE KONFIGURATION SPERRT NICHT AUS: `regelnNormalisieren` biegt alles
      Unlesbare auf die freundliche Seite (frei, kein Fenster, unbegrenzt). Der
      Fehlerfall ist bewusst "es laeuft weiter" - eine kaputte Datei darf kein
      Kind aussperren.

      SCHNITTSTELLE:
        GET  /api/kinderzeit             Regeln (Eltern)
        PUT  /api/kinderzeit             Regeln setzen
        GET  /api/kinderzeit/stand       Urteil + verbraucht/Rest/Bonus (Box+Eltern)
        POST /api/kinderzeit/bonus       {minuten} heute schenken (auch negativ)
        POST /api/kinderzeit/zuruecksetzen  Tag auf null - EINSCHLIESSLICH Bonus

      Dateien: config/kinderzeit.json (Regeln), config/kinderzeit-verbrauch.json
      (Tageszaehler, hoechstens jede Minute geschrieben - SD-Schreibzyklen).

      AM GERAET BEWIESEN (2026-07-28), Regel "1 Minute, keine Nachsicht":
        +50s  verbraucht 0 min, Rest 1, frei,         spielt
        +60s  verbraucht 1 min, Rest 0, aufgebraucht, STOPP von selbst
        neuer Start danach -> HTTP 403
        nach +15 geschenkten Minuten -> spielt wieder
      Ausserdem geprueft: Tag gesperrt -> 403; Fenster noch zu -> 403;
      Anhalten und Zustand -> immer 200.

      ZWEI FALLEN, beide beim Bauen aufgelaufen:
        * setInterval im Modulrumpf HAELT DEN TESTPROZESS OFFEN. server.spec.ts
          importiert server.ts, also hing der ganze Testlauf bis zum Zeitablauf.
          Loesung: `.unref()` - im Betrieb haelt ohnehin der Webserver die
          Schleife offen.
        * "Zuruecksetzen" behielt anfangs den BONUS. Das ist ueberraschend: der
          Bonus wurde gegeben, WEIL die Zeit um war - bleibt er stehen, bekommt
          das Kind unbemerkt Grundzeit plus Geschenk. Faellt nur im Test auf
          ("Rest 16" bei einer Regel von 1 Minute). Jetzt setzt es alles zurueck.

      PRUEFUNG: `mupi-check kinderzeit` - zeigt Stand und Rest und prueft
      ausdruecklich, dass ANHALTEN weiterhin geht.

  - kind: falle
    id: librespot-sitzung-stirbt-still
    title: "librespot lebt, aber die Box ist aus Spotifys Geraeteliste verschwunden"
    sev: high
    match: "Connection to server closed|Geraet fehlt|vanished from spotify"
    cause: "librespots Sitzung kann sterben, waehrend der Prozess weiterlaeuft - systemd sieht einen gesunden Dienst, Restart=always greift nicht"
    fix: "Waechter, der das SYMPTOM misst (steht die Box in Spotifys Geraeteliste?) und bei zweimaligem Fehlen neu startet"
    tags: [librespot, spotify, waechter, falle, 2026]
    body: |
      AM GERAET BEOBACHTET (2026-07-28): die Box spielte plotzlich nicht mehr.
      Befund:

        systemctl is-active librespot   -> active
        NRestarts                       -> 0
        laeuft seit                     -> 09:23 (Stunden)
        Geraeteliste bei Spotify        -> KEINS
        journal                         -> ERROR librespot_core::session]
                                           Connection to server closed.

      Der Prozess lebt also, aber seine Sitzung ist tot. systemd kann das nicht
      sehen - `Restart=always` greift nur, wenn etwas ABSTUERZT. Niemand merkt
      es, bis ein Kind auf ein Album tippt und nichts passiert. Im Projekt
      selbst bekannt: librespot-org Issues #1700 und #1726 (beide offen).

      Ein `systemctl restart librespot` holt das Geraet sofort zurueck.

      LOESUNG (gebaut): scripts/librespot/librespot-waechter.sh +
      librespot-waechter.timer, alle 5 Minuten. Er misst das SYMPTOM - steht
      der Boxname in Spotifys Geraeteliste? - und holt die Liste ueber die
      EIGENE Durchreiche der Box (/api/spotify/web/me/player/devices), braucht
      also keinen Token im Skript.

      Drei Sicherungen, alle gegengeprueft:
        * Dienst gar nicht aktiv -> nichts tun (systemd ist zustaendig)
        * waehrend der Wiedergabe -> nie neu starten (/tmp/playerstate)
        * erst beim ZWEITEN Fehlen -> ein einzelner Aussetzer (Netz, Spotify
          langsam) ist kein Grund; sonst macht der Waechter es schlimmer
      Auch: keine Antwort von der Durchreiche heisst NICHT "Geraet weg" (eher
      startet die Box gerade) -> ebenfalls kein Neustart.

      NACHTRAG 2026-08-02: der Waechter allein reicht NICHT. Es gibt eine
      ZWEITE Ursache mit demselben Symptom, gegen die ein Neustart nichts
      ausrichtet - siehe librespot-fremdes-konto-uebernommen. Der Waechter
      sieht seitdem erst nach, WARUM das Geraet fehlt, bevor er handelt.

  - kind: falle
    id: librespot-fremdes-konto-uebernommen
    title: "Spielen vom fremden Handy meldet librespot bei DESSEN Spotify-Konto an"
    sev: high
    match: "devices leer|Geraeteliste leer|Bad credentials|active device is <>"
    cause: "Spotify Connect vom fremden Geraet uebernimmt librespot und ueberschreibt credentials.json mit dem fremden Konto - die Box steht danach in der Geraeteliste jenes Kontos, nicht in ihrer eigenen"
    fix: "Konten vergleichen (scripts/librespot/librespot-konto.py); zum Richten ZUERST frischen accessToken hinterlegen, DANN credentials.json entfernen"
    tags: [librespot, spotify, konto, waechter, falle, 2026]
    related: [mupi-librespot-bad-credentials]
    body: |
      'Bad credentials' STEHT IN ZWEI EINTRAEGEN — hier und in
      [mupi-librespot-bad-credentials] (dort: Spotify hat den
      Passwort-Login abgeschafft, altes librespot kann sich gar nicht mehr
      anmelden). Die Zeile ist dieselbe, die Ursache nicht. Die Frage, die
      trennt, ist WESSEN Konto in den Zugangsdaten steht:

        python3 scripts/librespot/librespot-konto.py

      Fremdes Konto -> DIESER Eintrag (uebernommen per Spotify Connect).
      Eigenes Konto -> [mupi-librespot-bad-credentials]; im Log steht dann
                       daneben 'Login failed with reason' bzw. ein Hinweis auf
                       die abgekuendigte Anmeldung.

      AM GERAET BEOBACHTET (2026-08-02): "die Box spielt nicht mehr, nachdem
      ich von einem anderen Geraet was gespielt habe."

      Der Befund sah aus wie librespot-sitzung-stirbt-still, war aber etwas
      anderes:

        systemctl is-active librespot   -> active
        Geraeteliste der Box            -> KEINS
        librespot-Protokoll (mit -v)    -> successfully put connect state
                                           active device is <>
                                           Sent ping / Received pong (im Takt)

      librespot war also NICHT krank. Es war angemeldet, hielt seine
      Verbindung und wartete auf Befehle - nur eben fuer ein ANDERES Konto.
      Spielt jemand per Spotify Connect vom Handy auf die Box, uebernimmt
      librespot dessen Anmeldung und schreibt credentials.json neu.

      Gemessen mit scripts/librespot/librespot-konto.py:

        Konto der Box:        Kennung#bc031ccb   (premium)
        Konto von librespot:  Kennung#99c862fc
        ==> ZWEI VERSCHIEDENE KONTEN

      Die Box stand in der Geraeteliste des fremden Kontos. Ihre eigene Liste
      war leer - und der Waechter startete darum alle fuenf Minuten neu. Jeder
      Neustart las dieselbe uebernommene credentials.json und meldete sich
      wieder beim fremden Konto an. Ein Waechter, der immer dasselbe tut und
      nie nachsieht, ob es hilft, ist keiner.

      DIE FALLE BEIM RICHTEN: credentials.json einfach loeschen macht es
      SCHLIMMER. env-librespot benutzt den `accessToken` aus mupiboxconfig.json
      nur, solange keine credentials.json existiert - er ist die Ansaat fuer
      die erste Anmeldung, und im Kommentar dort steht ausdruecklich, er duerfe
      "laengst abgelaufen sein". Das stimmt nur, SOLANGE die credentials.json
      lebt. Ohne sie faellt librespot auf genau diesen alten Token zurueck:

        ERROR librespot] could not initialize spirc: Permission denied
        { Login failed with reason: Bad credentials }

      Danach Neustartkreis alle 12 Sekunden, NRestarts stieg auf 11.
      Richtige Reihenfolge: ERST frischen Token holen und eintragen, DANN die
      fremde Anmeldung beiseitelegen, DANN neu starten.

      ZUGANG ERNEUERN - so wie die Box es tut (server.ts, webApiToken):
      client_id im Rumpf, KEIN Secret, keine Basic-Auth. Die Box ist bei
      Spotify eine PKCE-Anwendung, clientSecret ist leer. Mit Basic-Auth
      antwortet Spotify `invalid_client`, was aussieht, als sei der Zugang der
      Box zurueckgezogen - dabei fragt nur das Werkzeug falsch.

      GEBAUT:
        * scripts/librespot/librespot-konto.py - vergleicht die Konten
          (Rueckgabe 0 gleich / 1 verschieden / 2 nicht feststellbar) und
          richtet mit --reparieren. Gibt nur sha1-Kurzkennungen aus, nie
          Kontonamen oder Token.
        * tools/spotify-konto-abgleich.sh - speist genau dieses Skript per ssh
          ein, damit es keine zweite Fassung gibt, die anders urteilt.
        * librespot-waechter.sh sieht beim zweiten Fehlen erst nach: fremdes
          Konto -> neu anmelden; nicht feststellbar -> abwarten (ein Neustart
          auf Verdacht macht aus einer Netzstoerung eine Tonstoerung); Konto
          stimmt -> wie bisher neu starten.

  - kind: falle
    id: drop-in-reihenfolge-ueberstimmt
    title: "Eine systemd-Ergaenzung wirkt nicht, weil eine spaeter sortierte sie ueberschreibt"
    sev: mid
    match: "drop-in|Ergaenzung wirkt nicht|-v wirkt nicht"
    cause: "systemd liest Ergaenzungen in Namensreihenfolge; die LETZTE gewinnt - '90-x.conf' verliert gegen 'nicht-blockieren.conf', weil '9' vor 'n' kommt"
    fix: "Diagnose-Ergaenzungen zz- nennen und danach `systemctl cat` lesen, statt der eigenen Datei zu glauben"
    tags: [systemd, librespot, werkzeug, falle, 2026]
    body: |
      Beim Nachsehen, warum librespot schweigt (2026-08-02), legte
      tools/librespot-mitlesen.sh eine Ergaenzung `90-mitlesen.conf` an, die
      `-q` durch `-v` ersetzt. Das Protokoll blieb leer. Der naheliegende
      Schluss - "librespot sagt wirklich nichts" - war falsch.

      Neben der eigenen Datei lag `nicht-blockieren.conf`. systemd liest
      Ergaenzungen in Namensreihenfolge, die spaetere gewinnt: 'n' kommt nach
      '9'. Sie setzte ExecStart zurueck auf `-q`. Das Werkzeug las also brav
      das Protokoll eines Prozesses mit, den es nie gespraechig gemacht hatte.

      ZWEITE FALLE derselben Sitzung: die Diagnose-Ergaenzung hatte die
      Warteschleife der Box uebernommen (`bis zu 90 s auf eine Audiosenke
      warten`). Damit beobachtete das Werkzeug 90 Sekunden lang einen Prozess,
      den es selbst noch gar nicht gestartet hatte. Wer beim Start zusieht,
      darf den Start nicht verzoegern.

      DRITTE, aus derselben Familie: die Bereitschaftspruefung suchte nach
      IRGENDEINEM '"name"' in Spotifys Geraeteliste. Steht ein Handy im Netz,
      meldet Spotify das mit - "nach 42 s bereit", obwohl die Box selbst nicht
      angemeldet war. Gesucht wird jetzt ihr eigener Name.

      MERKSATZ: `systemctl cat <unit>` zeigt, was WIRKLICH gilt. Der eigenen
      Datei zu glauben, ist dasselbe wie einer Attrappe zu glauben.
  - kind: architektur
    id: medienverwaltung
    title: "Medienverwaltung in der Verwaltung: Bibliothek + Meta-Suche ueber alle Dienste"
    tags: [medien, verwaltung, spotify, jellyfin, 2026]
    body: |
      GEBAUT 2026-07-28. Verwaltung -> Medien.

      TEILE:
        src/backend-api/src/medien.ts      reine Logik (getestet, 26 Faelle)
        src/backend-api/src/server.ts      /api/medien*, Meta-Suche
        frontend-admin/.../seiten/medien.ts  die Seite

      SCHNITTSTELLE:
        GET    /api/medien[?q=]            Bibliothek mit SCHLUESSEL je Eintrag
        POST   /api/medien                 aufnehmen (409 bei Dublette)
        PATCH  /api/medien/<schluessel>    Kategorie/Titel/Interpret aendern
        DELETE /api/medien/<schluessel>    entfernen
        GET    /api/medien/suche?q=&dienst=  Meta-Suche (spotify|jellyfin|alle)

      == UEBER SCHLUESSEL, NICHT UEBER INDIZES (der Kern) ==
      Die ALTEN Endpunkte /api/edit und /api/delete nehmen einen LISTENINDEX
      entgegen. Das ist gefaehrlich:
        * der Index verschiebt sich, sobald jemand etwas einfuegt/loescht
        * /api/data liest active_data.json, geschrieben wird data.json -
          und im OFFLINE-Betrieb ist active_data = offline_data, also eine
          voellig andere Liste mit anderen Indizes
      Wer darauf aufbaut, loescht irgendwann den falschen Eintrag. Die neuen
      Endpunkte nutzen `medienSchluessel(e)` = Dienst + Kennung des Dienstes
      (id/playlistid/showid/audiobookid/spotify_url), sonst Interpret+Titel
      normalisiert. Der Schluessel ueberlebt Umbenennen und Umsortieren.
      Bei ZWEI gleichen Schluesseln gibt `findeIndex` -1 zurueck: dann ist
      nicht entscheidbar, welcher gemeint war - lieber nichts tun.

      == SPERRE RICHTIG HALTEN ==
      Die alten Endpunkte entfernen die Sperrdatei SOFORT nach dem Anstossen
      des Lesens - also lange bevor geschrieben ist. `medienAendern` gibt sie
      erst im finally frei und schreibt ueber tmp + Umbenennen.

      == AENDERN IST ENG ==
      `aenderungAnwenden` uebernimmt NUR category/title/artist/cover. Kennung
      und Typ bleiben, auch wenn jemand sie mitschickt - sonst zeigt der
      Eintrag nach dem Bearbeiten auf nichts mehr. Getestet.

      == FALLE: JELLYFIN SUCHT NUR IM TITEL ==
      Jellyfins `searchTerm` durchsucht nur den NAMEN des Objekts, also den
      ALBUMTITEL - NICHT den Interpreten. Am Geraet gemessen: "Zukunft" findet
      "Die Zukunft wird gross", "lumpenpack" findet NICHTS, obwohl der
      Interpret "Das Lumpenpack" heisst. Das sieht aus, als haette Jellyfin
      nichts - und die Suche nach dem Interpreten ist der haeufigste Fall.

      RICHTIG sind ZWEI Abfragen, deren Ergebnisse ueber die Id vereinigt
      werden:
        /Items?searchTerm=<q>&IncludeItemTypes=MusicAlbum&Recursive=true
        /Artists?searchTerm=<q>  ->  /Items?AlbumArtistIds=<ids>&IncludeItemTypes=MusicAlbum
      Danach: "lumpenpack" -> 2 Alben, "kinderlieder" -> 6.

      == FALLE: JELLYFIN-SCHLUESSEL LIEGT NUR IN DER OBERFLAECHE ==
      In /etc/mupibox/mupiboxconfig.json steht unter `jellyfin` NUR die
      Adresse - der Zugangsschluessel lebt im Browser (je Herkunft). Fuer eine
      serverseitige Suche fehlt er also. Loesung ohne Nachfragen beim Nutzer:
      `jellyfinZugangAusListe` gewinnt Server UND Schluessel aus der
      Bild-Adresse eines vorhandenen Jellyfin-Eintrags (die traegt
      `?api_key=`). Ist noch kein Jellyfin-Album aufgenommen, sagt die Antwort
      das als Hinweis - statt still nichts zu liefern.

      NACHTRAG 22.08.2026: BEIDES UEBERHOLT. Die `jellyfin`-Gruppe der
      Konfiguration traegt inzwischen `server` UND `apiKey` (am Geraet
      gemessen), und die Suche wohnt im Plugin [jellyfin-zieht-ins-plugin] —
      sie liest den Zugang aus kontext.konfig, nicht mehr aus Coveradressen.
      Der Absatz oben beschreibt die Lage bis E80 und gilt noch fuer die im
      Kern verbliebenen Wege (Cover-/Strom-Weiterleitung, QuickConnect).

      == NOCH NICHT GEBAUT ==
      Downloads (offline verfuegbar machen) - siehe [mitschnitt-plan] und
      [quellen-verschmelzen]: fuer Jellyfin waere es ein reines Kopieren
      (stream?static=true liefert die Originaldatei), fuer Spotify ein
      Mitschnitt und damit an die dortigen Bedingungen gebunden.
  - kind: plan
    id: eigene-listen-gemischt
    title: "PLAN: eigene Listen, quer ueber Spotify + Jellyfin + lokal"
    tags: [plan, medien, listen, 2026]
    body: |
      ENTWURF vom 2026-07-28, NICHT gebaut. Gehoert zu [medienverwaltung].

      WUNSCH: aus einzelnen Titeln eigene Listen bauen - und zwar GEMISCHT
      ueber die Dienste (ein Spotify-Titel, ein Jellyfin-Titel, eine lokale
      Datei in derselben Liste).

      == WARUM DAS NUR LOKAL GEHT (und das ist gut) ==
      Eine Spotify-Playlist kann nur Spotify-Titel enthalten. Gemischt geht
      also ausschliesslich als EIGENE Liste der Box. Nebenbei erspart das die
      Berechtigung `playlist-modify-*`, die dem Token der Box fehlt (gemessen
      2026-07-28) und die eine Neuanmeldung erfordert haette. Die lokale
      Loesung ist die einfachere UND die maechtigere.

      == ABLAGE ==
      Ein neuer Eintragstyp in data.json:

        { "type": "mix", "category": "music", "title": "Einschlafen",
          "titel": [
            {"quelle":"spotify",  "uri":"spotify:track:...", "title":..., "artist":..., "dauerMs":...},
            {"quelle":"jellyfin", "id":"<item-id>",          "title":..., ...},
            {"quelle":"lokal",    "pfad":"...",              "title":..., ...}
          ] }

      JELLYFIN NUR MIT DER ITEM-ID, nicht mit fertiger Adresse: die Adresse
      traegt den Zugangsschluessel, und der gehoert nicht in jede Zeile einer
      Liste. Beim Abspielen wird sie aus der Id neu gebaut (der Schluessel wird
      ohnehin schon aus einem vorhandenen Eintrag gewonnen, siehe
      medienverwaltung).

      == WIEDERGABE - der eigentliche Knackpunkt ==
      Eine gemischte Liste heisst: Titel 1 auf librespot, Titel 2 auf mpv,
      Titel 3 wieder librespot. Jemand muss das TREIBEN.

      NICHT im Kiosk-Browser: ein Neuladen der Seite wuerde die laufende Liste
      verlieren.

      RICHTIG im backend-api, genau wie die Kinderzeit - und aus demselben
      Grund: dort laufen schon ein Takt und der /player-Proxy, durch den JEDER
      Befehl geht.

        Zustand   welche Liste laeuft, welcher Titel, seit wann
        Takt      alle paar Sekunden: ist der Titel zu Ende?
        Weiter    naechsten Titel auf der RICHTIGEN Maschine starten

      ENDE ERKENNEN, ohne jede Maschine einzeln zu befragen: /tmp/playerstate
      springt auf 'pause', wenn nichts mehr laeuft - fuer BEIDE Maschinen
      (dieselbe Quelle, die die Kinderzeit zaehlt).

      UNTERSCHEIDUNG "zu Ende" gegen "der Nutzer hat angehalten": ein
      Anhalten kommt als /player/<raum>/pause oder /stop durch den Proxy - der
      backend-api SIEHT das. Also: Pause durch den Nutzer -> Liste anhalten,
      nicht weiterschalten. Pause ohne vorangegangenen Befehl -> Titel war zu
      Ende -> naechster. Das ist der saubere Weg, und er faellt fast umsonst
      ab, weil der Kontrollpunkt schon existiert.

      WECHSELKOSTEN: ein Maschinenwechsel dauert 1-2 s (gemessen, siehe
      dienste-umschalten). Zwischen Titeln ertraeglich. Eine Liste, die bei
      JEDEM Titel wechselt, klingt aber zerhackt - deshalb beim Bauen der
      Liste nicht kuenstlich mischen, sondern die Reihenfolge lassen, wie der
      Mensch sie legt.

      == ZUSAMMENSPIEL MIT DER KINDERZEIT ==
      Laeuft die Zeit waehrend einer Liste ab, greift die Nachsicht wie sonst:
      der LAUFENDE Titel darf zu Ende gehen, der naechste wird nicht mehr
      gestartet. Der Listen-Takt muss also vor jedem Weiterschalten dieselbe
      Pruefung machen wie der Proxy - sonst umginge die eigene Liste die
      Zeitgrenze.

      == OFFEN ==
      1. Fortsetzen: merkt sich die Box die Stelle in einer Liste (wie bei
         Alben ueber resume.json)?
      2. Mischen/Wiederholen - oder bewusst weglassen (Kindergeraet)?
      3. Was passiert, wenn ein Titel nicht mehr da ist (Spotify entfernt,
         Jellyfin geloescht)? Vorschlag: ueberspringen und im Protokoll
         vermerken, nicht die ganze Liste anhalten.
  - kind: architektur
    id: medienverwaltung-listen
    title: "Eigene Listen der Box + dienstuebergreifende Angebote (Verwaltung)"
    tags: [medien, listen, verwaltung, spotify, jellyfin, 2026]
    body: |
      GEBAUT 2026-07-28, Fortsetzung von [medienverwaltung].

      == 1. SUCHE: Art-Filter und Einzeltitel ==
      `/api/medien/suche?q=&dienst=&art=` - `art` ist alle | album | playlist |
      show | titel. Bei einer gezielten Art steigt das Limit von 8 auf 20.

      JELLYFIN kann ALBEN und TITEL (Audio), aber keine Listen/Hoerbuecher -
      danach wird es uebersprungen statt leer zu melden. WICHTIG bei der
      Interpretensuche: fuer ALBEN ist das Feld `AlbumArtistIds`, fuer TITEL
      `ArtistIds`. Nimmt man nur eines, fehlen in der einen Richtung die
      Gastbeitraege und in der anderen die Alben.

      == 2. DASSELBE WERK IN MEHREREN DIENSTEN ==
      `gruppiereTreffer` (medien.ts, getestet) vergibt gleiche `gruppe` +
      `auchIn`. Regel bewusst FLACH: normalisierter Titel, Interpret LOSE
      (Jellyfin haengt Gaeste an: "Das Lumpenpack; Peter Balboa"), Titelanzahl
      nur wenn BEIDE sie kennen. Im Zweifel ZWEI Eintraege - Deluxe und
      Standard duerfen nicht verschmelzen. Zwei Treffer DESSELBEN Dienstes nie.

      Dieselbe Funktion gruppiert auch die BIBLIOTHEK (`/api/medien` liefert
      `gruppe` mit). Die Verwaltung zeigt eine Zeile je Gruppe mit einer Marke
      je Quelle; die ABLAGE bleibt unberuehrt, die Box zeigt weiter beide
      Eintraege. Umbenennen und Kategorie treffen die GANZE Gruppe - eine Zeile
      zu sehen und nur die Haelfte zu aendern waere die schlimmere Ueberraschung.

      == 3. "GIBT ES DAS AUCH WOANDERS?" ==
      `GET /api/medien/<schluessel>/andere` sucht mit dem TITEL des Eintrags
      (der Interpret ist oft anders geschrieben und wuerde eher verengen),
      wirft den Eintrag als Massstab in dieselbe Gruppierung und meldet nur,
      was in SEINE Gruppe faellt. Die Verwaltung prueft das automatisch
      (abschaltbar, Merker im Browser) und kann alle Angebote auf einmal
      uebernehmen - NACHEINANDER, weil jede Aufnahme unter Sperre schreibt.

      == 4. EIGENE LISTEN (gemischt) ==
      `/api/listen*`, Ablage `config/listen.json`.

      NICHT in data.json - mit Absicht: die Wiedergabe gemischter Listen ist
      NICHT gebaut (siehe [eigene-listen-gemischt]). Laegen sie in der
      Bibliothek, erschiene auf der Box eine Kachel, die beim Antippen nichts
      tut. Erst wenn sie spielen koennen, gehoeren sie dorthin.

      Ein ALBUM oder eine LISTE wird beim Hineinlegen AUFGELOEST
      (`titelEinesWerks`) - wer ein Album hineinlegt, meint dessen Titel.
      Aufgeloest wird SERVERSEITIG, weil dort die Zugaenge liegen.

      == 5. FALLE: EIN ALBUM ALS TITEL ==
      Einer JELLYFIN-Kennung sieht man NICHT an, ob sie ein Album oder ein
      Stueck meint. `/api/listen/:id/titel` hat deshalb ein Album durchgewunken
      - der Eintrag sah gueltig aus und haette nie gespielt (am Geraet
      passiert: "Nackt" mit der Album-Kennung).

      LOESUNG: der Server FRAGT Jellyfin (`/Items?ids=<id>` -> `Type`). Ist es
      kein `Audio`, wird aufgeloest statt abgewiesen - egal ueber welchen Knopf
      der Klick kam. Bei SPOTIFY verraet die URI die Art von selbst
      (`spotify:track:` gegen `spotify:album:`), das prueft `listenTitelAus`
      (medien.ts, getestet).

      == 6. REGELN DER OBERFLAECHE (Nutzerwunsch, zweimal eingefordert) ==
      KEINE JavaScript-Fenster - kein `confirm`, kein `prompt`. Stattdessen:
        * Namen eingeben -> Eingabefeld klappt an Ort und Stelle auf
        * Loeschen -> der Knopf wird zu "wirklich?" und faellt nach 4 s zurueck
      Und: ein Knopf sagt, WOHIN er legt ("+ alle Titel -> <Liste>"), und wird
      NICHT abgeschnitten - eine beschnittene Beschriftung ist schlimmer als
      eine kurze.

      == 7. OFFEN ==
      Wiedergabe der Listen. Reine Spotify-Listen waeren einfach (Spotify
      spielt eine URI-Liste selbst ab), gemischte brauchen den Takt aus
      [eigene-listen-gemischt].

  - kind: falle
    id: box-kachel-zeigt-fremde-quelle
    title: "OFFEN: Kachel zeigt zwei Dienste, drinnen ist nur einer"
    sev: mid
    match: "zwei icons|sources|Kachel.*Dienst"
    cause: "unbestaetigt - nicht reproduzierbar, solange nur EIN Eintrag des Interpreten in der Bibliothek liegt"
    fix: "noch keiner - beide Fassungen anlegen und nachmessen"
    tags: [box, anzeige, offen, 2026]
    body: |
      NUTZERBEFUND (2026-07-28): auf der Box zeigt die Kachel "Das Lumpenpack"
      ZWEI Quellenzeichen (Jellyfin + Spotify), beim Hineinklicken ist aber nur
      Spotify da.

      WAS GEMESSEN IST: in der Bibliothek lag zu dem Zeitpunkt nur EIN
      Lumpenpack-Eintrag (Spotify). Ein zweites Zeichen ist damit schon zu viel.

      WO ES HERKOMMT (Verdacht, NICHT belegt): `media.service.ts` baut
      `sourcesByArtist` aus der EXPANDIERTEN Medienliste, gruppiert nach
      `artist`. Die Kette in `updateMedia` faechert jeden Eintrag auf (Playlist
      -> viele Titel) und endet fuer Unbekanntes bei `of([item])`. Ein
      jellyfin-album wird also durchgereicht - es MUESSTE drinnen auftauchen.
      Also entweder ein veralteter Stand im Kiosk-Browser oder ein Fehler in
      der Auffaecherung.

      SO NACHSTELLEN: beide Fassungen desselben Interpreten aufnehmen
      (Verwaltung -> Medien -> "andere Dienste"), Kiosk neu laden, Kachel
      antippen. Zeigt sie zwei Zeichen und listet nur eine Quelle, ist es ein
      echter Fehler in `fetchMediaFromArtist`/`updateMedia`.

      NICHT VERWECHSELN mit der Zusammenfassung in der VERWALTUNG - die ist
      Absicht (eine Zeile, mehrere Marken) und betrifft die Box nicht.
  - kind: howto
    id: audit-2026-07-27-darstellung
    title: "Audit der Darstellungs-Arbeit — was dabei herauskam"
    source: "Durchsicht nach einem Tag Arbeit an Box, Verwaltung und Backend, 2026-07-27"
    tags: [audit, darstellung, backend, frontend]
    body: |
      GEPRUEFT: 287 Tests backend-api, 66 backend-player — gruen. Alle vier
      Arbeitsbereiche typgepruefte. Die Frontend-Tests (`ng test`, Karma)
      laufen weiterhin NICHT; das ist eine offene Luecke, keine Aussage ueber
      den Code.

      DREI ECHTE FUNDE, alle behoben:

      1. SCHREIBLAWINE. Jede Aenderung ging sofort zur Box, und Ziehen aendert
         mehrmals pro Sekunde etwas. EINE Zieh-Bewegung waren rund 40 Anfragen
         und ebenso viele Schreibvorgaenge auf `darstellung.json` — auf eine
         SD-Karte, das Teil, das an so einer Box zuerst stirbt. Jetzt entprellt
         (`SCHREIB_RUHE_MS` 700): eine Bewegung, ein Schreibvorgang. Nachgemessen
         mit einer zaehlenden Attrappe: 40 Bewegungen -> 1 PUT.
         WICHTIG dabei: `zuletztGeschrieben` wird bei der ERSTEN Aenderung
         gesetzt, nicht erst beim Senden — sonst reisst der 3-s-Abgleich waehrend
         des Ziehens die Position zurueck (genau das Rennen, das die Ruhezeit
         schliesst).

      2. ZWISCHENDATEI-RENNEN. Das Backend schrieb nach
         `darstellung.json.<pid>.tmp`. Zwei gleichzeitige Anfragen teilten sich
         diesen Namen und schrieben sich mitten hinein. Jetzt mit fortlaufender
         Nummer.

      3. HORCHER-LECK. Die Verwaltungsseite haengte einen resize-Horcher von
         Hand an; er blieb bei jedem Verlassen der Seite stehen. Jetzt
         `@HostListener('window:resize')` — Angular raeumt ihn ab.

      BEWUSST NICHT GEAENDERT:
        * Der 3-s-Abgleich der Box laeuft ohne Ende — sie ist ein Kiosk, das
          ist ihr Zweck.
        * `Darstellung` ist in der Verwaltung ein zweites Mal beschrieben
          (eigene Anwendung, kein geteiltes Paket). Ein Kommentar sagt, dass
          beide zusammenpassen muessen. Ein gemeinsames Paket waere die
          sauberere Loesung und ist der naechste Schritt, wenn die Schnittstelle
          noch waechst.

  - kind: knowhow
    id: vorlesen-tts
    title: "Vorlesen: Kachelnamen sprechen, Silben fuer Leseanfaenger (Piper)"
    tags: [vorlesen, tts, piper, silben, kinder, barrierefreiheit, 2026]
    body: |
      WOFUER: ein Kind im Vorschulalter kann die Kachel nicht lesen. Es tippt
      sie an, weil es das Bild kennt. Wer den Namen dazu hoert, lernt ihn. Wer
      gerade lesen lernt, dem hilft die Trennung in SPRECHSILBEN - genau die,
      die in der ersten Klasse geuebt wird.

      DREI MODI (Verwaltung -> Vorlesen, `config/vorlesen.json`):
        aus       - Vorgabe. Es wird nichts geholt und nichts verzoegert.
        antippen  - Ein Tipp spricht den Namen, danach beginnt die Musik.
        lernen    - Der erste Tipp zeigt den Namen GROSS in Sprechsilben
                    (Vollbild-Einblendung, global in app.component.html) und
                    liest ihn Silbe fuer Silbe; erst der zweite Tipp spielt.

      DIE ENTSCHEIDENDE MESSUNG (auf dem Geraet, 2026-07-28):
        Piper kalt (Prozessstart je Aufruf) ......... 2,2 s
        Piper warm (Modell bleibt geladen) .......... 0,17 s
        Dauerdienst kostet dafuer .................. 208 MB RSS
        Die Box hat 2010 MB gesamt, davon ~983 MB frei.

      ERGEBNIS: KEIN Dauerdienst, sondern ZWISCHENSPEICHER. Ein Zehntel des
      Arbeitsspeichers dauerhaft fuer eine Funktion, die ein paar Mal am Tag
      laeuft, ist der falsche Handel. Jeder Text wird EINMAL gesprochen und
      liegt danach als WAV in `~/.mupibox/vorlesen-cache`. Ueber die
      Schnittstelle gemessen: erstes Mal 2,85 s, danach 0,013 s (219x). Da
      dieselbe Kachel immer wieder angetippt wird, wartet praktisch nie jemand.

      WAS DIE SILBENREGELN KOENNEN UND WAS NICHT (`vorlesen.ts`, 45 Tests):
      Zwischen zwei Silbenkernen gilt: kein Mitlaut -> dazwischen trennen
      ("Feu-er", "Sta-ti-on"); EIN Mitlaut -> er beginnt die naechste Silbe
      ("Va-ter", "Zu-cker"); MEHRERE -> alle bis auf den letzten bleiben
      ("Fens-ter", "Kat-ze").
        * `sch ch ck ph th rh sh qu` sind EIN Laut und werden nie getrennt.
        * `tz` und `ng` stehen bewusst NICHT dabei - Duden trennt sie
          ("Kat-ze", "Fin-ger"). `st` ist seit 1996 trennbar ("Fens-ter").
        * Das H gilt IMMER als Mitlaut, nie als Dehnung. Zaehlte man "eh" als
          Silbenkern, kaeme "geh-en" statt "ge-hen" heraus.
        * `qu`: das U ist dort KEIN Silbenkern, sonst "Que-lle".

      DIE BEKANNTE GRENZE - ZUSAMMENGESETZTE WOERTER. Steht an der Wortfuge nur
      EIN Mitlaut, schiebt die Regel ihn in die naechste Silbe:
        "Pummeleinhorn" -> "Pum-me-lein-horn"  (richtig: Pum-mel-ein-horn)
        "Hoerspiele"    -> "Hoers-pie-le"      (richtig: Hoer-spie-le)
      Ohne Woerterbuch ist die Fuge nicht zu finden, und ein Woerterbuch der
      Albumtitel gibt es nicht. DESHALB: die Regeln machen den Vorschlag, das
      Feld `silben` am Medieneintrag schlaegt ihn (`lernTrennung`). Die Seite
      sagt das offen, statt eine Richtigkeit zu behaupten, die bei jedem
      zweiten Kinderalbum scheitert. Ein Elternteil korrigiert ohnehin nur die
      Handvoll Titel, an denen das Kind wirklich liest.
      Gegenprobe an echten Titeln: "Guck mal die-se Bie-ne da Summ Summ" ist
      vollstaendig richtig.

      STIMMEN. Installiert unter `~/.mupibox/piper-stimmen`, gefunden werden
      sie ueber die `.onnx`-Dateien - die Liste steht NICHT im Code, damit man
      nachladen kann, ohne die Anwendung anzufassen. Vorgabe `de_DE-ramona-low`
      (vom Nutzer ausgesucht, nachdem alle vier deutschen Stimmen ueber den
      LAUTSPRECHER DER BOX vorgespielt wurden - eine Stimme sucht man nicht
      nach Datenblatt aus). Auch geladen: `en_US-amy-medium`,
      `vi_VN-vais1000-medium`. Die eigenstaendigen deutschen FRAUENstimmen gibt
      es bei Piper nur in niedriger Guete; nur Thorsten hat `medium`/`high`.
      Jede Stimme kostet 20-110 MB auf der Karte, deshalb steht die Groesse
      dabei und jede laesst sich entfernen.

      SICHERHEIT: der Stimmname wird zu einem DATEIPFAD. Zwei Schranken, beide
      noetig - `stimmeIdGueltig` (striktes Muster ohne Punkt und Schraegstrich,
      faengt `../../etc/passwd`) UND `stimmeErlaubt` gegen die WIRKLICH
      vorhandenen Dateien. Am Geraet geprueft: `?stimme=../../etc/passwd` ->
      `{"error":"stimmeUnbekannt"}`.

      NIE BLOCKIEREN. `VorleseService.sprich` hat einen Zaun von 4 s: haengt
      das Netz oder fehlt die Stimme, spielt die Box trotzdem. Ein Kind, das
      tippt und bei dem nichts passiert, haelt die Box fuer kaputt - das waere
      schlimmer, als den Namen nicht zu hoeren. Und bei `modus: aus` gibt der
      Dienst SOFORT zurueck, ohne Netzaufruf: eine Funktion, die auch
      ausgeschaltet Zeit kostet, waere ein Rueckschritt fuer alle, die sie
      nicht wollen.

      EINRICHTUNG (einmalig, war auf der Box noetig):
        apt install python3.13-venv        # sonst scheitert venv an ensurepip
        python3 -m venv ~/.mupibox/piper-venv
        ~/.mupibox/piper-venv/bin/pip install piper-tts
        cd ~/.mupibox/piper-stimmen && ~/.mupibox/piper-venv/bin/python \
          -m piper.download_voices de_DE-ramona-low
      Die Weboberflaeche von Piper (`piper.http_server`) braucht zusaetzlich
      `flask` und synthetisiert per POST auf `/synthesize` mit JSON - ein GET
      auf `/` liefert nur die HTML-Seite (kostete hier eine Viertelstunde).

  - kind: idea
    id: ebook-mitlesen
    title: "Idee: E-Books vorlesen mit Mitlesen (Leseuebung)"
    tags: [vorlesen, ebook, lesen-lernen, idee, offen, 2026]
    body: |
      IDEE DES NUTZERS (2026-07-28, ausdruecklich als VORMERKUNG, nicht als
      Auftrag): die Box liest ein E-Book vor und blendet den Text dazu ein, so
      dass ein Kind MITLESEN und damit lesen ueben kann.

      EINSCHAETZUNG: machbar, und die Grundlagen liegen schon. Nichts daran
      braucht einen neuen Dienst oder ein Abo.
        * Sprechen: Piper laeuft bereits lokal (siehe [vorlesen-tts]).
        * Silben: `vorlesen.ts` trennt bereits in Sprechsilben - fuer
          Leseanfaenger genau die richtige Darstellung.
        * Anzeige: die Vollbild-Einblendung des Lernmodus ist die Keimzelle.

      DER EINE KNIFFLIGE PUNKT ist das MITWANDERN der Markierung. Piper liefert
      keine Zeitmarken pro Wort. Der gangbare Weg, ohne ein zweites Modell:
      SATZWEISE oder wortweise synthetisieren, die Dauer jedes Stuecks aus der
      erzeugten WAV lesen (Kopfdaten, kein Abspielen noetig) und die Markierung
      stueckweise weiterschieben. Das ist genau, nicht geschaetzt, und der
      Zwischenspeicher passt: ein Buch wird einmal gesprochen und dann beliebig
      oft gelesen. Vorsicht beim Platz - ein ganzes Kapitel als WAV ist gross;
      hier waere Opus statt WAV der erste Schritt.

      FORMAT: ein EPUB ist ein ZIP mit XHTML-Dateien. Auslesen ist geradeaus,
      und `js/artifacts.js` im Schwesterprojekt zeigt, dass ein ZIP-Leser ohne
      Abhaengigkeit machbar ist. PDF waere deutlich muehsamer - EPUB zuerst.

      OFFENE FRAGEN, bevor jemand anfaengt:
        * Wo kommen die Buecher her? (eigene Dateien, Bibliothek, Jellyfin?)
        * Was ist mit dem Urheberrecht? Vorlesen im Familienkreis ist etwas
          anderes als ein Mitschnitt - siehe die Regeln in [mitschnitt-plan].
        * Reicht die Anzeige der Box (5" DSI) fuer Fliesstext, oder braucht es
          eine Seite mit sehr wenigen Woertern gleichzeitig?

  - kind: knowhow
    id: pruefskript-leitplanken
    title: "tools/pruefen.sh — alles Pruefbare in einem Lauf"
    tags: [tests, werkzeug, leitplanken, bau, 2026]
    body: |
      WOFUER (Wunsch des Nutzers, 2026-07-28): "ich wuerde gerne irgendwie
      Leitplanken haben, dass wir nicht an einer Stelle was machen und woanders
      zerschiessen". Die Teile dieses Projekts haengen weiter zusammen, als es
      aussieht, und gemerkt hat man den Bruch bisher erst auf der Box.

      AUFRUF:
        tools/pruefen.sh --schnell   nur die Kerntests (~6 s)
        tools/pruefen.sh             + Typen + alle drei Baue (~40 s)
        tools/pruefen.sh --box       + mupi-check auf dem Geraet (~110 s)
      Am Ende steht EINE Zeile: "Alles in Ordnung." oder welche Schritte
      gebrochen sind.

      WARUM DER BAU MITGEPRUEFT WIRD - der teuerste Stolperstein des Projekts:
      `src/deploy` gehoert root. esbuild kann dorthin nicht schreiben, meldet
      den Fehler, gibt aber "Done" UND EXITCODE 0 aus. Genau so wurde hier
      schon eine unveraenderte Datei ausgeliefert und lange gesucht. Das Skript
      baut deshalb an einen eigenen Ort UND SIEHT NACH, ob die Datei wirklich
      da ist, statt dem Exitcode zu glauben. Dieselbe Pruefung gilt fuer
      `index.html` der beiden Oberflaechen - fehlt sie, waere die Verwaltung
      bzw. die Box nach dem Tausch tot (ist genau so schon passiert).

      SOFORT ETWAS GEFUNDEN: der erste Lauf brachte einen echten Typfehler in
      `spotify-control.ts` ans Licht (eine 200er Antwort ohne `access_token`
      lief mit `null` weiter und waere erst tief in einem spaeteren Aufruf
      gescheitert - mit einer Meldung, die nichts mit der Ursache zu tun hat).

      NOCH OFFEN: UI-Tests fuer die Box-Oberflaeche. Die zuletzt gemeldeten
      Rueckschritte (Fortschrittsleiste springt, Mini-Player klappt von selbst
      zu, Cover laedt beim Wiederoeffnen nicht) sind genau die Sorte, die
      Kerntests NICHT sehen. Siehe die offene Aufgabe im Sitzungsprotokoll.

      ── WER WIRD EIGENTLICH GEPRUEFT? GEZAEHLT 2026-08-02 ───────────────
      Die Abdeckung ist genau UMGEKEHRT zur Baurichtung: am besten geprueft
      ist das, was abgeloest werden soll.

        src/frontend-box    ALTE Box-Oberflaeche (Angular/Ionic)
                            35 Testdateien, DREI Stufen im Pruefskript -
                            Kerntests ohne Browser, alle Tests mit echtem
                            Chrome/DOM (373 s), dazu Node-Specs
        NewDesign/          NEUE Box-Oberflaeche (unter /neu/), 3609 Zeilen
                            app.js + 1854 Zeilen CSS
                            tools/player-befehle.py --oberflaeche vergleicht
                            die verschickten Befehle gegen den Verteiler in
                            spotify-control.ts (llmwiki
                            playpause-gibt-es-nicht). Sie prueft NICHT, ob
                            die Oberflaeche tut, was sie soll - nur, dass sie
                            keine Befehle sendet, die die Box mit 200
                            quittiert und ignoriert.
                            SEIT 2026-08-02 dazu 7 Verhaltenstests im echten
                            Browser: tools/e2e/neu-oberflaeche.test.mjs
                            (Schritt "Tests neue Oberflaeche", ~2 s).
        src/frontend-admin  NEUE Verwaltung, 23 TypeScript-Dateien
                            SEIT 2026-08-02 34 Tests: alle VIER Dienste
                            (AnmeldeDienst, DiensteDienst, NetzwerkDienst,
                            SystemDienst), die Wache und die Seite
                            Kinderzeit. Das test-Ziel in angular.json fehlte
                            und wurde ergaenzt. Schritt "Tests neue
                            Verwaltung", rund 0,4 s.
                            OFFEN: 14 der 15 Seiten (6541 der 6899 Zeilen),
                            darunter die dicksten - darstellung.ts (1239),
                            medien.ts (1186), mupihat.ts (923).
        AdminInterface/     ALTE PHP-Verwaltung, 30 PHP-Dateien - nichts.

        src/backend-api     33 Testdateien (730 Tests)
        src/backend-player   5 Testdateien (89 Tests)

      Beide neuen Oberflaechen haben damit ein Netz, aber ein duennes: bei
      NewDesign/ deckt es den Abspielweg und die Zustaende ab, nicht das
      Aussehen; bei der Verwaltung den Anmeldeweg, nicht die Seiten. Wie das
      gebaut ist und warum so, steht in tests-fuer-die-neuen-oberflaechen.

  - kind: knowhow
    id: ton-aussetzer-messen
    title: "Knacken beim Hoeren: Stream oder Bluetooth? (mupi-ton)"
    tags: [ton, bluetooth, pipewire, aussetzer, diagnose, werkzeug, achimsumbau, 2026]
    body: |
      DIE FRAGE: es knackt. Liegt es am Zuspieler (Spotify, Jellyfin) oder am
      Tonweg (PipeWire, Bluetooth)? Raten kostet Stunden - messen dauert eine
      Minute.

      DAS WERKZEUG: `mupi-ton` (auf der Box unter /usr/local/bin, Quelle
      `tools/mupi-ton.py`).
        mupi-ton --nur-weg   nur zeigen, was eingestellt ist (spielt nichts)
        mupi-ton --sek 8     messen

      WIE ES TRENNT: es spielt einen 440-Hz-Sinus, dessen JEDER Ton vorher
      feststeht, und liest ueber den PipeWire-Monitor (`parec` auf
      `<sink>.monitor`) mit, was wirklich hinausgeht. Bei 440 Hz und 48 kHz
      kann sich der Wert von einem Ton zum naechsten nur um einen berechenbaren
      Betrag aendern - jeder groessere Sprung IST ein Knacken, da wird nichts
      geschaetzt. Dazu zaehlt es Luecken (Stille mitten im Ton) und die
      xruns des Tonservers.

      DIE AUSSAGE, und nur diese:
        Mitschrift sauber + es knackt hoerbar -> der Fehler liegt HINTER
          PipeWire, also auf der Funkstrecke.
        Mitschrift schmutzig -> der Fehler liegt DAVOR (Zuspieler, PipeWire,
          zu wenig Rechenzeit).
      WICHTIGE GRENZE: der Monitor zeigt, was PipeWire ABGESCHICKT hat, nicht
      was der Lautsprecher gespielt hat. Verliert der Funk danach Pakete, sieht
      die Mitschrift trotzdem sauber aus. Genau diese Trennung ist der Zweck.

      MESSUNG AN ACHIMS BOX (2026-07-28, Teufel ROCKSTER Cross):
        8,00 s von 8 s mitgelesen, 0 Spruenge, 0 Luecken, 0 xruns.
      Also: was PipeWire hinausschickt, ist SAUBER. Die gehoerten Aussetzer
      entstehen dahinter. Der Nutzer hoerte sie bei einer LOKALEN Wav-Datei
      (Sprachprobe) - damit ist auch jeder Netz-Zuspieler ausgeschlossen.

      DER FUND, auf den sonst niemand kommt: die Box hat ZWEI Bluetooth-
      Adapter, und der Ton laeuft ueber den falschen.
        hci1  UART (eingebaut, Pi 5)  ACL-MTU 1021 Byte, 8 Puffer
        hci0  USB-Stick               ACL-MTU  310 Byte, 10 Puffer  <- traegt den Ton
      Der Stick ist ein `0a12:0001 Cambridge Silicon Radio` - der billige
      CSR-Klon, den es ueberall gibt. 310 Byte je Paket statt 1021 heisst: fuer
      dieselbe Musik dreimal so viele Pakete. Genau daran bricht A2DP zuerst.
      `mupi-ton` nennt deshalb jetzt, WELCHER Adapter den Ton traegt und ob ein
      besserer danebensteckt.

      WAS DAMIT SCHON AUSGESCHLOSSEN IST:
        * Zuspieler/Netz - die Probe war eine lokale Datei.
        * PipeWire - 0 xruns, Mitschrift lueckenlos.
        * Codec - es laeuft aptX, nicht das schwache SBC.
        * WLAN-Stoerung - das WLAN liegt auf 5 GHz (5580 MHz), Bluetooth auf
          2,4 GHz. Beim Pi teilen sich WLAN und Bluetooth Chip und Antenne;
          auf 2,4 GHz waere das der Hauptverdaechtige, hier nicht.
        * Funkqualitaet - Verbindungsguete 255 von 255, also bestmoeglich.
      UEBRIG BLEIBT der USB-Stick als Traeger.

      WARUM DER STICK STECKT (vom Nutzer, 2026-07-28): er wurde ABSICHTLICH
      gesteckt, um Bluetooth und WLAN zu trennen - beim Pi teilen sich beide
      dasselbe Funkmodul. "Nimm den eingebauten Adapter" waere deshalb ein
      Rueckschritt, kein Fix. Wer das nicht weiss, empfiehlt genau das Falsche.

      DIE BOX HAT KEINEN EINGEBAUTEN LAUTSPRECHER (Stand 2026-07-28, "noch
      nicht"). Bluetooth ist der EINZIGE Tonweg. Zwei Folgen: der sonst
      naheliegende Gegenversuch "spiel es ueber die Klinke" geht hier nicht,
      und JEDER Versuch am Bluetooth trennt den Ton - deshalb muss jeder
      Umzug in einem Befehl umkehrbar sein.

      DIE LOESUNG LAG SCHON IM GERAET. `bt-wechsel` (Werkzeug, siehe unten)
      fand DREI Adapter:
        hci0  USB   BT 4.0  ACL-MTU  310 Byte, 10 Puffer  0a12:0001  <- traegt den Ton
        hci1  UART  BT 5.0  ACL-MTU 1021 Byte,  8 Puffer  (eingebaut, teilt sich mit WLAN)
        hci2  USB   BT 5.1  ACL-MTU 1021 Byte,  6 Puffer  0b05:190e  ASUS USB-BT500
      Der ASUS USB-BT500 (Realtek RTL8761B) steckte bereits und war ungenutzt:
      3,3-mal groessere Pakete als der CSR-Klon, Bluetooth 5.1 statt 4.0, und
      als USB-Geraet weiterhin vom WLAN getrennt. Das ist die richtige Wahl
      fuer diese Box.

      ERLEDIGT UND BESTAETIGT (2026-07-28): nach `bt-wechsel --auf hci2` ist
      es sauber - GEMESSEN (8,00 von 8 s, keine Spruenge, keine Luecken, keine
      xruns) UND GEHOERT. Auch der Umschalt-Test (Jellyfin -> Spotify ->
      Jellyfin -> Spotify) lief ohne Befund durch. Die Diagnose "zu kleine
      Pakete" hat sich damit bestaetigt.

      EIN FALSCHER ALARM DAZWISCHEN, den man kennen sollte: mitten in der
      Nachpruefung kam gar kein Ton mehr, und alles sah gesund aus -
      bluetoothctl "Connected: yes", Uebertragung "active", 48 kB/s ueber den
      Funk. Der LAUTSPRECHER WAR AUS. Nur das Systemprotokoll sagte es
      ("bt-reconnect: nicht erreichbar (aus?)"). Wer nur einmal vor der
      Messung hinsieht, misst gegen ein Geraet, das mitten in der Messung
      verschwindet - deshalb prueft `mupi-ton` jetzt davor UND danach, und
      `mupi-check tonweg` prueft den Zustand der UEBERTRAGUNG statt der
      Verbindung (die meldet noch "verbunden", wenn niemand mehr zuhoert).

      NEBENBEFUND: das WLAN steht bei -69 dBm mit 2227 Wiederholungen. Das ist
      nicht gut und koennte fuer STREAMS (Spotify/Jellyfin) eine eigene
      Fehlerquelle sein - fuer das hier gemessene Knacken aber nicht, denn die
      Probe kam von der Platte.

      ZWEITES WERKZEUG: `bt-wechsel` (Quelle `tools/bt-wechsel.sh`) bewertet
      alle Adapter nach dem, was fuer A2DP zaehlt - ACL-MTU zuerst, dann die
      Bluetooth-Fassung, dann der Chip - und zieht den Lautsprecher auf
      Wunsch um. Es zaehlt einen UART-Adapter ABSICHTLICH herunter (siehe
      oben) und loescht nie eine alte Kopplung.
      Zwei Auslesefehler, die nur beim Ausfuehren auffielen und die man
      wiederholt, wenn man sie nicht kennt: /sys/class/bluetooth listet auch
      VERBINDUNGEN wie "hci0:72" - die sahen wie ein weiterer Adapter aus
      (Filter: `^hci[0-9]+$`). Und "BD Address" und "ACL MTU" stehen bei
      `hciconfig -a` auf EINER Zeile, weshalb ein `$3` in awk Teile der
      Geraeteadresse als MTU anzeigte.

      STOLPERSTEIN BEIM BAU DES WERKZEUGS, damit ihn niemand wiederholt: die
      erste Fassung meldete "nur 6,6 s von 8 s angekommen" und schrie Alarm.
      Das war ihr EIGENES Messfenster - der Bluetooth-Ausgang haelt einen
      grossen Puffer, `paplay` ist lange vor dem letzten Ton fertig. Jetzt
      wartet sie 2,5 s nach und benennt einen Randverlust ohne Luecken
      ausdruecklich als Fensterschnitt, nicht als Fehler der Box. Ein Werkzeug,
      das falschen Alarm schlaegt, ist schlimmer als keins.

  - kind: howto
    id: werkzeuge-benutzen
    title: "Die Pruefwerkzeuge benutzen (pruefen.sh, mupi-check, mupi-ton, bt-wechsel)"
    tags: [werkzeug, tests, diagnose, howto, ton, bluetooth, 2026]
    body: |
      VIER WERKZEUGE, und jedes beantwortet eine andere Frage. Wer das falsche
      nimmt, sucht lange.

      ── AM RECHNER (im Projektordner) ────────────────────────────────────

      tools/pruefen.sh --schnell     ~6 s   nur die Kerntests
      tools/pruefen.sh               ~40 s  + Typen + alle drei Baue
      tools/pruefen.sh --box        ~110 s  + mupi-check auf dem Geraet

      Am Ende steht EINE Zeile: "Alles in Ordnung." oder welche Schritte
      gebrochen sind. VOR jedem Ausliefern mindestens `--schnell`, vor einem
      groesseren Wurf `--box`.

      WARUM ES DEN BAU MITPRUEFT: `src/deploy` gehoert root. esbuild kann
      dorthin nicht schreiben, meldet den Fehler UND gibt trotzdem "Done" mit
      Exitcode 0 aus. So wurde hier schon eine unveraenderte Datei ausgeliefert
      und lange gesucht. Das Skript baut an einen eigenen Ort und SIEHT NACH,
      ob die Datei da ist. Dasselbe gilt fuer `index.html` der Oberflaechen -
      fehlt sie, waere die Verwaltung nach dem Tausch tot (ist passiert).

      ── AUF DER BOX ──────────────────────────────────────────────────────

      sudo mupi-check                 alle ~70 Pruefungen
      sudo mupi-check <gruppe> …      nur diese Gruppen

      Nuetzliche Gruppen:
        tonweg          Kommt der Ton bis zum Lautsprecher? Welcher Funkadapter
                        traegt ihn, und steckt ein besserer daneben?
        umschalten      Jellyfin -> Spotify -> Jellyfin, am LAUTSPRECHER
                        gemessen (Pegel je Schritt, Stille davor und danach).
        umschaltfolge   Zweimal DERSELBE Dienst, dann quer. Der normale Test
                        wechselt immer die Tonmaschine; ein Sprung zu einem
                        anderen Spotify-Titel WAEHREND Spotify laeuft ist ein
                        anderer Fall - dort kann etwas haengenbleiben, ohne
                        dass der Wechseltest es je sieht.
        umschaltstress  Zehn Wechsel OHNE Pause, misst je die Anlaufzeit.
        spotifykette  librespot-Fassung, Anmeldung, Geraeteliste.
        kinderzeit    Regeln, Guthaben, Sperren.
        browserton    Kann der Kiosk ueberhaupt Ton erzeugen?

      mupi-ton --nur-weg        zeigt Ausgang, Codec, Adapter (spielt nichts)
      mupi-ton --sek 8          misst mit einem Sinus, dessen jeder Ton
                                vorher feststeht
      bt-wechsel                bewertet alle Funkadapter
      bt-wechsel --auf hci2     zieht den Lautsprecher dorthin um (umkehrbar)

      ── WENN ES KNACKT ODER STUMM IST: DIESE REIHENFOLGE ─────────────────

      1. `sudo mupi-check tonweg`
         Sagt in drei Zeilen, ob der Lautsprecher ueberhaupt empfaengt und ob
         der richtige Funkadapter den Ton traegt. FAST IMMER hoert es hier auf.

      2. Lautsprecher AUS? Das ist der haeufigste Fall und sieht heimtueckisch
         aus: bluetoothctl meldet noch "Connected: yes", eine Momentaufnahme
         zeigt eine aktive Uebertragung - und trotzdem kommt nichts. Nur das
         Systemprotokoll verraet es:
           journalctl --since -30min | grep bt-reconnect
           -> "nicht erreichbar (aus?)"
         Deshalb prueft `tonweg` die UEBERTRAGUNG, nicht die Verbindung.

      3. `mupi-ton --sek 8`, und dabei ZUHOEREN.
         Mitschrift sauber + hoerbar sauber -> alles gut.
         Mitschrift sauber + es knackt      -> der Fehler liegt HINTER
                                               PipeWire, also am Funk.
         Mitschrift schmutzig               -> davor (Zuspieler, PipeWire).

      4. `bt-wechsel` - traegt ein schwacher Adapter den Ton?

      5. `sudo mupi-check umschalten` - der Dienstwechsel am Lautsprecher
         gemessen.

      ── DIE GROESSTE MESSFALLE: parec puffert 64 KB ───────────────────────

      GEMESSEN (2026-07-28), und es kostete einen kompletten Fehlalarm:
        Fenster 1 s -> 0 Byte
        Fenster 2 s -> 0 Byte
        Fenster 3 s -> 65536 Byte
      `parec` schreibt erst, wenn sein 64-KB-Puffer voll ist. Wer kuerzer als
      drei Sekunden mitschneidet, bekommt eine LEERE Datei - und liest das als
      "still". Genau so meldete der Belastungstest zehnmal "kein Ton",
      waehrend am Lautsprecher alle zehn zu hoeren waren.

      ZWEI FOLGEN fuer jeden, der hier etwas misst:
        * `pegel()` erzwingt jetzt mindestens drei Sekunden.
        * Wer eine ZEIT braucht (wann setzt der Ton ein?), darf nicht mehrere
          kurze Proben nehmen - das ginge nur in Drei-Sekunden-Schritten.
          Stattdessen EINMAL durchgehend mitschneiden und in der Aufnahme
          suchen, ab welcher Stelle es laut wird. Das ist auf Millisekunden
          genau (so macht es `umschaltstress`).

      ── ANLAUFZEITEN AN ACHIMS BOX (2026-07-28, nach dem Adapterwechsel) ──

        Jellyfin  sofort      (mpv, lokaler Strom)
        Spotify   0,6-0,9 s   (librespot muss aus der Cloud anlaufen)
        Zehn Wechsel ohne Pause: 10 von 10 mit Ton, im Schnitt 0,4 s.
      Ein Sprung INNERHALB eines Dienstes wird als "ohne Unterbrechung"
      gemeldet, nicht als "0,0 s" - dort wird vorher nicht angehalten, eine
      Null hiesse also faelschlich "sofort gestartet" statt "nie abgerissen".

      Und: jeder Wechsel nimmt einen ANDEREN Titel. Denselben zweimal zu
      spielen ist nicht der echte Fall - ein Kind springt zu einem anderen
      Song, und dort kann sich ein Dienst anders verhalten.

      ── WORAUF MAN BEI mupi-ton ACHTEN MUSS ──────────────────────────────

      Es misst ueber den PipeWire-Monitor, und der traegt ALLES, was zum
      Ausgang geht. Drei Fallen, alle schon eingebaut abgefangen, aber gut zu
      wissen:
        * Laeuft nebenher Musik, wuerde die MISCHUNG gemessen und jede Note
          als Knacken gezaehlt (es meldete einmal 41849 Knackser an einer
          gesunden Anlage). Das Werkzeug weigert sich jetzt.
        * librespot haelt seinen Datenstrom AUCH IM PAUSENZUSTAND offen -
          deshalb schaut es zusaetzlich in /tmp/playerstate.
        * Der Lautsprecher kann MITTEN in der Messung verschwinden. Es prueft
          deshalb davor UND danach.
      Und: der Monitor zeigt, was PipeWire ABGESCHICKT hat, nicht was der
      Lautsprecher gespielt hat. Genau diese Trennung ist sein Zweck.

      ── ERGEBNIS DES ERSTEN ECHTEN EINSATZES (2026-07-28) ─────────────────

      Aussetzer beim Hoeren. Mitschrift sauber (8,00 von 8 s) -> der Fehler
      lag hinter PipeWire. `bt-wechsel` zeigte: der Ton lief ueber einen
      CSR8510-Klon mit 310 Byte je Paket, waehrend ein ASUS USB-BT500 mit
      1021 Byte ungenutzt daneben steckte. Nach `bt-wechsel --auf hci2` war es
      sauber - gemessen UND gehoert, und auch der Umschalt-Test
      (Jellyfin/Spotify, vier Wechsel) lief ohne Befund durch.

  - kind: knowhow
    id: fortschrittsleiste-sprung
    title: "Springende Fortschrittsleiste: Spotifys verspaetete Antwort"
    tags: [player, fortschritt, spotify, messen, 2026]
    body: |
      DAS SYMPTOM: die Fortschrittsleiste sprang. Nicht bei Jellyfin, nur bei
      Spotify, und nur kurz nach dem Start.

      SO WURDE ES GEFUNDEN (statt geraten): die Box liest alle zwei Sekunden
      `/state` beim Wiedergabedienst (Port 5005) und zeichnet daraus den
      Balken - `media.service.ts`. Wer diesen Endpunkt mitschreibt, sieht
      GENAU das, was das Kind sieht, ohne die Oberflaeche anzufassen.
      Mitgeschrieben:

        0,9 s nach dem Start  ->  Position 105.010 ms
        3,0 s                 ->  Position 105.010 ms
        5,5 s                 ->  Position   1.690 ms   <- Ruecksprung 103 s

      Spotifys Wiedergabe-Schnittstelle meldet nach einem Startbefehl also
      noch mehrere Sekunden den Stand des VORIGEN Stuecks. `/state` fragt
      Spotify direkt - es ist KEIN eigener Zwischenspeicher der Box.
      Bei Jellyfin/mpv tritt es nicht auf (gemessen: kein einziger
      Ruecksprung) - der meldet lokal ueber `/local` mit `progressTime`.

      DIE LOESUNG (`src/backend-player/src/fortschritt.ts`, rein + geprueft):

        EIN STUECK KANN NICHT WEITER SEIN ALS DIE ZEIT,
        DIE SEIT DEM STARTBEFEHL VERGANGEN IST.

      Meldet Spotify mehr, gilt die selbst gezaehlte Zeit. `useSpotify` merkt
      sich dafuer `startpunkt = {seit, ab}`; die Startposition steht als
      letztes Glied im Befehl (".../<track>:<ab-sekunde>"), bei Wiederaufnahme
      also groesser 0. Toleranz 2,5 s gegen Netz- und Rundungsunschaerfe.

      BEWUSST NICHT: die Meldung eine feste Zeit lang verwerfen. Wie lange
      Spotify braucht, schwankt - hier zwischen 3 und 5 Sekunden gemessen.
      Eine feste Frist waere mal zu kurz und mal zu lang; die Regel oben
      braucht keine.

      NACHGEMESSEN: `mupi-check fortschritt` -> kein Ruecksprung mehr, weder
      bei Spotify noch bei Jellyfin.

      DURCHGEHENDER LAUF: die Oberflaeche rechnet bereits VIERMAL je Sekunde
      aus einem Anker weiter (`fortschrittJetzt`, 250-ms-Takt + passende
      250-ms-Ueberblendung). Die wahrgenommenen Stufen waren also der
      Ruecksprung, nicht der Takt. Damit es durchgehend BLEIBT, laeuft in
      `now-playing.spec.ts` jetzt ein Test ueber 40 Takte mit alle zwei
      Sekunden neu eintreffenden Ankern samt Netzunschaerfe: jeder Schritt
      muss vorwaerts gehen, keiner darf uebergross sein.

  - kind: knowhow
    id: box-tests-liefen-nie
    title: "Die Tests der Box-Oberflaeche starteten gar nicht (EACCES + kein Chrome)"
    tags: [tests, angular, karma, vitest, werkzeug, 2026]
    body: |
      GEFUNDEN 2026-07-28 beim Versuch, UI-Tests als Leitplanke einzuhaengen:
      `ng test` der Box-Oberflaeche lief NIE durch. Zwei Huerden hintereinander:

        1. `EACCES: permission denied, mkdir '.angular/cache/...'`
           Der Ordner `.angular` gehoerte root (aus einem Lauf als root).
        2. `No binary for ChromeHeadless browser` - Karma braucht einen
           Browser, auf einem reinen Baurechner ist keiner installiert.

      Beides zusammen sah aus wie "Tests kaputt" und nicht wie
      "Testumgebung kaputt" - deshalb fiel es lange nicht auf. FOLGE: zwei
      Tests waren VERALTET und schlugen fehl, ohne dass es jemand sah.
        * `now-playing.spec.ts` erwartete den Fortschritt aus `progressTime`
          (30), waehrend die Umsetzung ihn bewusst aus `elapsed/duration`
          rechnet (12). Der Test war falsch, nicht der Code.
        * `spotify-browse.spec.ts` kannte das `cover` je Mixtape-Titel nicht,
          das inzwischen mitgegeben wird (die Stuecke stammen aus
          verschiedenen Alben, ein gemeinsames Bild gibt es nicht).

      BEHOBEN:
        * `angular.json` -> `cli.cache.path` auf
          `../../node_modules/.cache/angular-box` (gehoert dem Benutzer).
        * Die REINE Logik braucht keinen Browser: `npx vitest run --globals
          src/app/<datei>.spec.ts`. Vier Dateien laufen so (now-playing,
          utils, jellyfin, spotify-browse) - 41 Tests in rund fuenf Sekunden,
          eingehaengt in `tools/pruefen.sh`.
        * Jasmine-eigene Ausdruecke ersetzt, damit BEIDE Laeufer sie
          verstehen: `toBeTrue()` -> `toBe(true)`, `jasmine.objectContaining`
          -> einzelne Feldpruefungen.

      ERLEDIGT (2026-07-28, noch am selben Tag): auch die KOMPONENTEN-Tests
      laufen. Es musste nichts installiert werden - ein brauchbarer Chrome lag
      laengst da:

          ~/.cache/ms-playwright/chromium-*/chrome-linux64/chrome

      `tools/pruefen.sh` SUCHT ihn jetzt, statt ihn zu verlangen: Playwright,
      dann /usr/bin/chromium, chrome, vivaldi (Chromium-basiert, taugt
      genauso). Findet es keinen, prueft es nur die reine Logik und SAGT DAS
      AUCH ("Komponenten-Tests uebersprungen") - stillschweigend weniger
      pruefen waere schlimmer als gar nicht pruefen.
      Ergebnis: 109 von 109 Tests der Box-Oberflaeche, erstmals inklusive der
      Tests mit echtem DOM.

      ZWEI GESCHWINDIGKEITEN, und das ist Absicht: der volle Lauf dauert ueber
      FUENF MINUTEN (fast alles davon der Angular-Bau). Im schnellen Lauf waere
      das der Tod des schnellen Laufs - eine Pruefung, die fuenf Minuten
      braucht, wird vor dem Ausliefern nicht mehr benutzt. Deshalb:
        pruefen.sh --schnell   reine Logik, 14 s
        pruefen.sh             + alle 109 Oberflaechen-Tests, Typen, drei Baue
        pruefen.sh --box       + 96 Pruefungen auf dem Geraet

      STOLPERSTEIN: ein `import { describe, expect, it } from 'vitest'` laesst
      sich von Karma NICHT aufloesen und reisst den ganzen Lauf ab. Beide
      Laeufer stellen diese Namen global bereit - also gar nicht importieren.

      DIE BOX SELBST hat zwar Chromium (fuer den Kiosk), taugt aber nicht als
      Testrechner: dort liegen weder die Quellen noch node_modules, und den
      Angular-Bau auf einen 2-GB-Pi zu schieben waere der falsche Weg.

  - kind: knowhow
    id: player-rueckschritte-behoben
    title: "Drei Player-Fehler: Mini-Player klappt zu, Cover fehlt, doppeltes Quellen-Zeichen"
    tags: [player, cover, mini-player, quellen, tests, 2026]
    body: |
      Drei gemeldete Rueckschritte, alle DURCH LESEN DES CODES gefunden (nicht
      geraten) und je durch einen Test festgeschrieben.

      ── 1. DER PLAYER KLAPPTE MITTEN IM LIED ZUR LISTE ZURUECK ────────────

      `player.page.ts` zaehlte `goBackTimer++`, sobald die Quelle "spielt
      nicht" meldete, und kehrte ab 10 zur Liste zurueck. DER ZAEHLER WURDE
      NIE ZURUECKGESETZT (nur `= 0` bei der Deklaration). Solche Meldungen
      kommen aber auch, wenn nichts zu Ende ist: kurz nach dem Start (Spotify
      meldet noch den alten Stand), beim Titelwechsel, bei einem Netzhaenger.
      Nach zehn solchen Momenten IRGENDWANN in der Sitzung klappte der Player
      zu - je laenger gehoert wurde, desto sicherer.

      Gemeint war "zehnmal HINTEREINANDER". Jetzt `rueckkehrZaehler` in
      `now-playing.ts`: laeuft es wieder, faellt der Zaehler auf null. Der
      aussagekraeftigste Test stellt einen langen Hoerabend nach - 100
      Titelwechsel mit je einem Stocker und Wiedergabe dazwischen. Unter der
      alten Regel kehrt er beim zehnten Wechsel zurueck, jetzt nie.

      ── 2. DAS COVER FEHLTE BEIM WIEDEROEFFNEN DESSELBEN ALBUMS ───────────

      Die Player-SEITE uebernahm das Live-Cover nur, wenn es sich von dem beim
      Eintritt UNTERSCHEIDET (Schutz davor, kurz das Bild des vorigen Stuecks
      zu zeigen - Spotify meldet es 3-5 s weiter, siehe
      [fortschrittsleiste-sprung]). Oeffnet man den Player fuer das Album, das
      GERADE LAEUFT, ist es dasselbe Bild - die Sperre ging nie wieder auf.
      Jetzt ist sie ZEITLICH begrenzt (`coverJetzt`, `COVER_STALE_MS` 6 s):
      solange die Antwort veraltet sein KANN, gilt der Unterschied als Beweis,
      danach gehoert das Live-Bild dem laufenden Medium.

      ── 3. DER MINI-PLAYER ZEIGTE BEI JELLYFIN GAR KEIN COVER ────────────

      Anderes Bauteil (`now-playing-bar`), anderer Fehler. Der Schluessel fuer
      die Cover-Karte wurde aus dem DATEIPFAD gebaut (`segs[1]|segs[2]`), was
      fuer eine lokale Datei "kategorie/Interpret/Album" passt. Am Geraet
      gemessen, waehrend Jellyfin lief:

          path             (LEER)
          album            "Kapelle Petra"     <- der INTERPRET, nicht das Album
          currentTrackname "Es war nicht alles schlecht"

      Der Schluessel wurde also "|" und traf nie - obwohl die Karte den
      richtigen Eintrag ENTHIELT ("Kapelle Petra|HAMM"). Er wurde nur nie
      gefragt.
      `coverFuerLokal` probiert jetzt erst den Pfad, dann das `album`-Feld
      gegen BEIDE Haelften des Schluessels. Wichtig: das Feld wird von den
      Quellen uneinheitlich befuellt (Jellyfin: Interpret, lokal: Album) -
      eine Bedeutung zu unterstellen wuerde den Fehler nur verschieben.

      ── 4. ZWEI QUELLEN-ZEICHEN, EINE QUELLE DRIN ────────────────────────

      Die Kachel zeigte Jellyfin UND Spotify, drin war nur Spotify. Ursache
      war strukturell: die ZEICHEN entstanden in `fetchArtistData`, der INHALT
      in `fetchMediaFromArtist` - zwei Stellen mit je eigenem Filter. Beide
      gehen jetzt durch `medienVonInterpret`/`quellenVonInterpret` (media.ts),
      damit die Zeichen von Bauart her die Quellen dessen sind, was ein
      Antippen liefert. `quellenPassen` benennt ein gebrochenes Versprechen.

      NICHT behoben und im Kommentar so vermerkt: beide Wege holen ihre Liste
      in ZWEI GETRENNTEN Netz-Auffaecherungen (`fetchMedia` ruft je Eintrag
      Spotify/Jellyfin). Liefert der zweite weniger, weichen sie trotz
      gemeinsamer Logik ab. Dafuer braeuchte es EINE gemeinsame Momentaufnahme
      - eigene Baustelle.
      Der gemeldete Fall selbst liess sich mit der heutigen Bibliothek nicht
      nachstellen (nur ein Spotify-Eintrag fuer den Interpreten, kein
      Jellyfin) - behoben ist die Fehlerklasse, nicht ein Nachstellversuch.

  - kind: knowhow
    id: neustart-deckt-auf
    title: "Was ein Neustart aufdeckte: alter Kiosk-Stand und der zurueckgefallene Funkadapter"
    tags: [kiosk, bluetooth, neustart, auslieferung, diagnose, 2026]
    body: |
      EIN NEUSTART ALS PRUEFMITTEL (2026-07-28) - er brachte zwei Dinge ans
      Licht, die einen ganzen Arbeitstag lang unsichtbar waren.

      ── 1. DER BILDSCHIRM ZEIGTE DEN GANZEN TAG DEN ALTEN STAND ──────────

      Den ganzen Tag wurden Oberflaechen-Aenderungen ausgeliefert und mit
      "HTTP 200" fuer erfolgreich gehalten. Der Kiosk-Browser lief aber seit
      09:23 durch und hatte seine Seite VOR allen Aenderungen geladen. Die
      letzte Auslieferung war um 18:31 - am Bildschirm war trotzdem der Stand
      von morgens zu sehen.

      Folge: ein gemeldeter Fehler ("das kleine Coverbild im Mini-Player
      fehlt") war laengst behoben, nur eben nicht sichtbar. Die Suche danach
      lief ins Leere, weil die Rechnung ("das Cover WIRD gefunden") und der
      Augenschein ("es ist nicht da") beide stimmten - nur fuer
      verschiedene Programmstaende.

      "DIE DATEI LIEGT RICHTIG" UND "DER BILDSCHIRM ZEIGT SIE" SIND ZWEI
      VERSCHIEDENE AUSSAGEN. Dafuer gibt es jetzt `mupi-check kioskaktuell`:
      es vergleicht die Laufzeit des Browsers mit dem juengsten Stand in
      `www/`. Nur eine Warnung - ein alter Kiosk ist voellig normal, solange
      nichts Neues ausgeliefert wurde; erst die KOMBINATION ist der Befund.

      ── 2. DER TON FIEL AUF DEN SCHWACHEN STICK ZURUECK ──────────────────

      Nach dem Neustart lief der Ton wieder ueber den CSR-Klon (ACL-MTU 310
      Byte) statt ueber den ASUS (1021) - obwohl in der Verwaltung laengst der
      ASUS gewaehlt war UND die Wahl korrekt gespeichert ist (nach ADRESSE, in
      `/etc/mupibox/bt-adapter`; sie hat den Neustart einwandfrei ueberlebt).

      Nicht hingesehen hat `bt-reconnect.py`: es rief `bluetoothctl connect
      <mac>` OHNE Adapterwahl auf, also nahm BlueZ seinen eigenen Standard -
      und der ist nach einem Boot beliebig. Der Lautsprecher ist an BEIDEN
      Adaptern gekoppelt, also gewann der erstbeste.
      Behoben: das Skript liest dieselbe Datei wie das Backend und schickt
      `select <mac>` mit. Danach am Geraet geprueft: Lautsprecher wieder auf
      dem ASUS, Warnung weg.

      ── 3. EIN FEHLER IM PRUEFWERKZEUG SELBST ────────────────────────────

      `mupi-check tonweg` riet "Umziehen mit: bt-wechsel --auf hci2". Die
      hci-NUMMERN WANDERN aber beim Neustart: vorher war hci2 der ASUS,
      danach hci1 (und hci2 der eingebaute Funk). Der Rat hatte also nach dem
      Booten auf den falschen Stecker gezeigt. Jetzt nennt er die ADRESSE und
      verweist auf die Verwaltung, die sich die Adresse merkt.
      MERKSATZ: in Ratschlaegen und in Konfiguration NIE eine hci-Nummer
      benutzen - immer die Adresse. Das Backend macht es seit jeher richtig
      (Kommentar in server.ts), die Werkzeuge drumherum nicht.

  - kind: knowhow
    id: titelangaben-falsch
    title: "\"Titel 3 von 12\" stimmte in beiden Zahlen nicht"
    tags: [player, mpv, jellyfin, anzeige, messen, 2026]
    body: |
      DER ANSTOSS kam vom Nutzer: "was man noch checken kann, ob Tracknr
      stimmt und Titel und Gesamtzahl Titel". Gemessen am Geraet
      (2026-07-28), Album "HAMM" mit 12 Titeln:

          Titelnummer   gemeldet 3    wahr 1
          Gesamtzahl    gemeldet 0    wahr 12
          Titelname     richtig

      Die Box zeigte also "Titel 3 von -".

      ZWEI URSACHEN, beide in spotify-control.ts:
        * `currentTracknr` wurde bei JEDEM metadata-Ereignis um eins erhoeht.
          Gezaehlt wurden damit EREIGNISSE, nicht Titel - und mpv meldet fuer
          EINE Datei mehrere.
        * `totalTracks` wurde auf dem Jellyfin-Weg nie gesetzt.

      BEHOBEN, indem mpv gefragt wird statt gezaehlt: `playlist-pos-1` (die
      1-basierte Stelle in der Warteschlange - genau das, was "Titel n" meint)
      und `playlist-count`. Beide in EIGENSCHAFTEN (mpv-protokoll.ts) und im
      Sekundentakt mitgefragt. Der alte Zaehlweg bleibt fuer den
      mplayer-Aufsatz erhalten, der diese Eigenschaften nicht kennt - eine
      Sperre kippt beim ersten echten Wert um, damit mit mpv nie beides wirkt.
      Nachgemessen: 1 von 12.

      WICHTIG BEIM NACHSTELLEN - der Weg der OBERFLAECHE ist ein anderer als
      der naheliegende. Drei Anlaeufe waren noetig, bis richtig gemessen wurde:
        1. Einzeltitel ueber `/player/current/jellyfin/<url>/<name>` - so
           startet die Oberflaeche NICHT.
        2. `/player/current/jfqueue/<albumId>/…` - loest das Album NICHT auf,
           `currentTrackname` war dann die Album-KENNUNG.
        3. RICHTIG: die Oberflaeche (player.service.ts `playJellyfinAlbum`)
           holt die Titelliste SELBST vom Jellyfin-Server, spielt den ersten
           Titel und haengt den Rest einzeln per `jfqueue` an.
      Wer anders misst, misst etwas anderes - und "totalTracks ist leer" war
      bei Anlauf 1 sogar richtig.

      Als Dauerpruefung: `mupi-check titelangaben` vergleicht Nummer, Name und
      Gesamtzahl gegen die Titelliste vom Jellyfin-Server und startet dabei
      genau die Abfolge der Oberflaeche.

  - kind: knowhow
    id: eine-quelle-fuer-den-fortschritt
    title: "Eine Quelle statt zwei Leisten - und drei eigene Fehlgriffe dabei"
    tags: [player, fortschritt, cover, e2e, tests, 2026]
    body: |
      DIE FRAGE DES NUTZERS war zugleich die Diagnose (2026-07-28): "ob man 2
      unterschiedliche Leisten braucht oder die eine nur skaliert - dann faellt
      auch die Wartung zweier weg". Genau daran lag es: die kleine Leiste
      (now-playing-bar) und die grosse Ansicht (player.page) rechneten den
      Fortschritt GETRENNT aus denselben Meldungen. Im Browser gemessen lief die
      kleine ruhig, die grosse sprang sechsmal in zwoelf Sekunden zurueck.

      DIE WERTE ZEIGTEN DAS MUSTER:
          +0,133  +0,133  0,000  +0,801  ...  -0,267   <== zurueck
      Zwischen zwei Meldungen zaehlt ein Takt vorwaerts; die eintreffende
      Meldung ist einen Moment ALT und zog den Balken zurueck.

      JETZT: `fortschritt.service.ts` rechnet EINMAL, beide Ansichten zeigen
      nur noch an. Nie rueckwaerts, ausser der Sprung ist echt (Titelwechsel,
      Spulen). Und ADAPTIV getaktet - der Balken misst in PROZENT, eine Sekunde
      ist bei 2 Minuten 0,83 %, bei 10 Minuten 0,17 %; ein fester Takt macht bei
      kurzen Titeln fuenfmal so grosse und damit sichtbare Schritte
      (`taktFuerDauer`: ein Schritt ~0,1 %, zwischen 60 und 250 ms).

      ── DREI EIGENE FEHLGRIFFE, damit sie niemand wiederholt ──────────────

      1. EINEN VON ZWEI SCHREIBERN ZU SICHERN NUETZT NICHTS. Der erste Versuch
         hinderte die MELDUNG daran, einen kleineren Wert zu setzen - und
         aenderte nichts, weil der Rueckzieher vom TAKT kam: die Meldung hatte
         den Anker ersetzt, der Takt rechnete aus dem aelteren.

      2. "KEINE RUECKSPRUENGE" GILT AUCH FUER EINEN BALKEN, DER STILLSTEHT.
         Nachdem die grosse Ansicht an den Dienst gehaengt war, der (noch) 0
         lieferte, stand sie auf 0 - und der e2e-Test nannte das "ruhig". Ein
         Test, der Stillstand fuer Ruhe haelt, ist schlimmer als keiner. Er
         verlangt jetzt ausdruecklich BEWEGUNG.

      3. EIN SCHUTZ, DER DAS RICHTIGE ZURUECKHAELT. Die Sperre gegen das Bild
         des VORIGEN Stuecks (Spotify meldet es 3-5 s weiter) schlug beim
         Oeffnen aus der kleinen Leiste IMMER zu: dort ist das "Bild beim
         Eintritt" dasselbe wie das laufende. Ergebnis: Leiste zeigte das
         Cover, grosse Ansicht sechs Sekunden den Platzhalter. Lief die
         Wiedergabe schon (`extern`), gibt es kein voriges Stueck - dann gilt
         das laufende Bild sofort.

      ── DIE E2E-PRUEFUNGEN (tools/e2e/) ──────────────────────────────────

      Beide fahren einen ECHTEN Browser gegen die ECHTE Box - ueber das
      DevTools-Protokoll, mit dem Chromium, der ohnehin auf dem Rechner liegt
      (Playwright-Zwischenspeicher, sonst chromium/vivaldi). Kein neues Paket.
        player-gross-klein.mjs      klein -> gross -> klein: ueberall ein Bild?
        fortschritt-klein-gross.mjs springt der Balken - und bewegt er sich?
      Eingehaengt in `tools/pruefen.sh --box`. Die reinen Tests pruefen die
      REGEL, diese hier, ob am Ende wirklich etwas auf dem Bildschirm steht -
      und genau DAZWISCHEN lagen alle drei Fehler oben.

  - kind: knowhow
    id: ausliefern-regeln
    title: "Ausliefern: atomar tauschen, und die Box laedt selbst neu"
    tags: [auslieferung, kiosk, deploy, 2026]
    body: |
      DREI REGELN, alle aus Fehlern desselben Tages (2026-07-28).

      ── 1. DANEBENLEGEN UND UMBENENNEN, NICHT LOESCHEN UND KOPIEREN ──────

      Der naheliegende Weg ist:
          rm -rf $B/www && cp -a /tmp/neu $B/www
      Dazwischen ist das Verzeichnis LEER. Genau in diesen Sekundenbruchteil
      fiel eine Anfrage, und im Serverprotokoll stand
      `ENOENT: no such file or directory, stat '.../www-admin/index.html'`.
      Nichts Bleibendes, aber vermeidbar:
          rm -rf $B/www.neu && cp -a /tmp/neu $B/www.neu
          rm -rf $B/www.alt && mv $B/www $B/www.alt && mv $B/www.neu $B/www
      Der Tausch ist damit ein einzelner Schritt, und `www.alt` ist gleich der
      Rueckweg.

      IMMER VORHER PRUEFEN, ob `index.html` im neuen Verzeichnis liegt. Ohne
      sie ist die Oberflaeche nach dem Tausch tot - genau so ist die Verwaltung
      hier schon einmal ausgefallen.

      ── 2. DIE BOX MERKT EINE NEUE OBERFLAECHE SELBST ────────────────────

      Der Kiosk-Browser laeuft durch: eine neue Oberflaeche auszuliefern
      aenderte am BILDSCHIRM nichts, hier einen ganzen Tag lang. Ein F5 laesst
      sich von aussen nicht ausloesen, ohne einen Fernwartungs-Port zu oeffnen,
      den hier niemand offen haben will.

      Jetzt fragt die Oberflaeche selbst: `GET /api/oberflaeche/stand` liefert
      den Namen des Buendels (er traegt einen Hash) plus dessen Aenderungszeit.
      `OberflaechenstandService` merkt sich den ERSTEN Wert und laedt neu,
      sobald er sich aendert - nur bei einer echten Aenderung, hoechstens
      einmal, und ein leerer Wert (Serveraussetzer) laedt NICHT neu. Eine Box,
      die staendig neu laedt, waere schlimmer als eine, die einen Tag alt ist.
      Die Wiedergabe laeuft in librespot bzw. mpv, NICHT im Browser - ein
      Neuladen unterbricht die Musik also nicht.

      ── 3. WAS `mupi-check kioskaktuell` SAGEN KANN UND WAS NICHT ────────

      Es sieht nur die Laufzeit des BROWSERPROZESSES. Damit ist "aktuell"
      BEWEISBAR (startete er nach der Auslieferung, kann er nichts Aelteres
      zeigen) - "veraltet" aber NICHT: ein F5 laesst den Prozess unberuehrt.
      Die erste Fassung behauptete trotzdem "Seite ist aelter" und meldete das
      auch noch, nachdem der Nutzer neu geladen hatte. Sie sagt jetzt "nicht
      beweisbar" und nennt den Grund. Eine Pruefung, die mehr behauptet als sie
      weiss, verliert genau dort ihren Wert, wo man sich auf sie verlassen
      wollte.

  - kind: knowhow
    id: vorlesen-beim-halten
    title: "Vorlesen beim Halten war eingebaut - und stumm"
    tags: [vorlesen, tts, long-press, kinder, messen, 2026]
    body: |
      DAS VORLESEN BEIM HALTEN GAB ES SCHON: `long-press.directive.ts` ist
      ausdruecklich dafuer gebaut (500 ms halten, Bewegung bricht ab, der
      folgende Klick wird verschluckt, Fortschrittsanzeige waehrend des
      Haltens - "der Anzeiger ist kein Schmuck, er ist die halbe Bedienung").
      Verdrahtet war es an den Titelzeilen in Medienliste, Player-Flip und
      Favoriten.

      ES HAT NUR NIE EINEN TON GEMACHT. Auf der Box gemessen (2026-07-28):

          'speechSynthesis' in window  ->  true
          speechSynthesis.getVoices()  ->  LEER

      Die Schnittstelle ist da, es ist nur keine einzige Stimme installiert.
      `speak()` kehrt dann ERFOLGREICH zurueck und tut nichts - und weil
      `speakText` bei Erfolg zurueckkehrt, lief auch der vorgesehene Rueckfall
      nie. Eingebaut, verdrahtet, dokumentiert - und stumm. Genau die Sorte
      Fehler, die man nur durch Messen findet: der Code sieht richtig aus.

      BEHOBEN: `speakText` (utils.ts) waehlt jetzt den Weg (`sprachwegWaehlen`,
      rein + geprueft):
        1. piper   - die Sprachausgabe der Box (/api/vorlesen/sprich), gespielt
                     ueber ein <audio> im Browser. Mischt sich UEBER die Musik
                     und laesst die Wiedergabeliste in Ruhe.
        2. browser - nur, wenn `getVoices()` wirklich etwas liefert.
        3. box     - zuletzt: die Box spricht ueber mplayer, das ERSETZT die
                     Wiedergabeliste. Notloesung, kein Normalfall.

      NEU AUCH AUF COVER UND UEBERSCHRIFT (Wunsch des Nutzers): Antippen
      behaelt seine Bedeutung (das Cover klappt zur Titelliste um), wer den
      Namen wissen will, HAELT drauf. Was gesagt wird, ist eine gepruefte
      Regel (`vorleseTextFuerAlbum`): der Interpret kommt nur dazu, wenn er
      etwas hinzufuegt - "Bosse Live, von Bosse" kann so nicht entstehen.

      NICHT VERWECHSELN mit dem Modus `antippen` aus [[vorlesen-tts]]: der
      spricht beim TIPPEN und ist von Haus aus AUS. Beim Testen eingeschaltet
      und vergessen, las die Box bei jedem Wechsel vor - das war kein Fehler,
      sondern eine offene Einstellung.

  - kind: knowhow
    id: spotify-nachlauf-muster
    title: "Spotifys Nachlauf: nicht ein Feld, die GANZE Antwort"
    tags: [spotify, player, muster, messen, 2026]
    body: |
      DIE FRAGE DES NUTZERS (2026-07-28): "gibt es noch weitere Funktionen, die
      dem Muster unterliegen können - nicht dass ich alles einzeln ansprechen
      muss". Berechtigt, und die Messung gibt eine klare Antwort.

      DAS MUSTER: nach einem Startbefehl hat Spotifys Wiedergabe-Schnittstelle
      ihn noch nicht verarbeitet und antwortet mit dem Stand VON VORHER. Beim
      Wechsel auf ein anderes Album, 0,2 s nach dem Befehl:

          Titel     "Guck mal diese Biene da Summ Summ"   (das vorige Stueck)
          Dauer     176229 ms                             (die vorige Dauer)
          Kontext   die vorige Playlist
          is_playing false
          progress_ms  bis zu 105 s (die Stelle des vorigen Stuecks)

      Es ist also NICHT ein Feld, sondern die vollstaendige Antwort. Wer je
      Feld ausbessert, behandelt Symptome und wird beim naechsten Feld erneut
      ueberrascht - genau das war hier zweimal der Fall (erst die Position,
      dann der Abspiel-Knopf).

      WAS DAS SICHTBAR ANRICHTETE:
        * Die Fortschrittsleiste sprang bis zu 103 s zurueck.
        * Der Abspiel-Knopf sprang auf "abspielen", obwohl gespielt wurde -
          in BEIDEN Ansichten, weil beide dieselbe Quelle lesen.
        * Das Cover der grossen Ansicht blieb der Platzhalter (dort wirkte
          zusaetzlich ein eigener Schutz, siehe [[player-rueckschritte-behoben]]).

      DER VERLAESSLICHE BEWEIS steht in der Antwort selbst: die Box WEISS, was
      sie befohlen hat (`currentMeta.activeSpotifyId`). Meldet Spotify einen
      anderen `context.uri`, ist alles darin von vorher.
      `zustandVeraltet` (fortschritt.ts, rein + geprueft) sagt das EINMAL, und
      der Fall wird protokolliert - so faellt ein neu betroffenes Feld im Log
      auf, statt darauf zu warten, dass es jemand meldet.

      RICHTIGGESTELLT werden die beiden Felder, die sichtbar luegen: Position
      (`richtig`) und `is_playing` (`spieltWirklich`). Titel und Dauer hinken
      rund eine Sekunde nach - das faellt kaum auf, und etwas Ehrliches laesst
      sich dort nicht erfinden. Steht so im Kommentar.

      NICHT VERGESSEN: der Startpunkt MUSS bei Pause und Stopp geloescht
      werden (in `writeplayerstatePause`). Sonst wuerde eine ECHTE Pause kurz
      nach dem Start als veralteter Bericht abgetan - und ein Knopf, der eine
      gedrueckte Pause ignoriert, ist schlimmer als einer, der kurz flackert.

      BEI JELLYFIN/mpv GIBT ES DAS NICHT: der meldet lokal und sofort
      (nachgemessen, kein einziger Ruecksprung).

  - kind: note
    id: mupi-oberflaeche-navigation
    title: "Oberflaeche der Box — wie man zu einem Titel kommt (fuer Tests)"
    source:
      - "Session 2026-07-28, im laufenden Browser gegen die echte Box nachgesehen"
    tags: [oberflaeche, tests, e2e, navigation, jellyfin]
    body: |
      ERST HIER NACHSEHEN, DANN TESTEN. Diese Struktur wurde dreimal falsch
      geraten, bevor sie einmal nachgesehen wurde — die drei Fehlgriffe stehen
      unten, damit sie sich nicht wiederholen.

      **Die Startseite zeigt NICHT alle Eintraege.** Sie filtert nach
      KATEGORIE (`category` im Datensatz: `music` | `audiobook`). Bei Achims
      Box standen 9 Eintraege in `/api/data`, aber nur 2 Kacheln auf dem
      Schirm — beide `audiobook`. Alles unter `music` (darunter der einzige
      Jellyfin-Eintrag) war schlicht in der anderen Kategorie und ueber die
      Startseite gar nicht erreichbar. Wer eine bestimmte Kachel sucht, muss
      also erst die Kategorie treffen.

      **`swiper-slide` ist nicht gleich Inhalt.** Auf `/medialist` sind die
      ERSTEN `swiper-slide` weiterhin die Navigationskacheln der Ebene darueber;
      der eigentliche Eintrag ist die LETZTE. Ein
      `document.querySelector('swiper-slide ion-card').click()` trifft deshalb
      die Navigation und bleibt auf derselben Seite stehen — es sieht aus, als
      haenge die Box, dabei hat der Test nur auf sich selbst geklickt.

      **Quellen-Zeichen taugen nicht zur Auswahl.** Die Klasse lautet zwar
      `.src-badge.src-{quelle}`, aber auf der Startseite war ueberhaupt nur
      `src-spotify` vorhanden, und der Typ heisst `jellyfin-album`, nicht
      `jellyfin`. Verlaesslich ist der Weg ueber `/api/data`
      (`type` / `category` / `artist`) und von dort ueber den Kuenstlernamen
      zur Kachel.

      **Jellyfin fuer Oberflaechen-Vergleiche, Spotify fuer den Alltag.**
      Jellyfin steht im Haus, Spotify in der Cloud. Wer Anlaufzeiten der
      OBERFLAECHE an einer Spotify-Kachel misst, misst zur Haelfte die
      Internetstrecke, und die Zahl schwankt danach mit der Leitung statt mit
      dem Programm. Spotify trotzdem messen — es ist der Alltag der Box —, aber
      getrennt ausweisen, nicht im selben Vergleich.

      **Der gepruefte Weg zu einem Titel** (im Browser nachgelaufen, nicht
      hergeleitet): auf JEDER Ebene ist der Eintrag die LETZTE `swiper-slide`,
      die ersten sind die Navigationskacheln der Ebene darueber.

          /home             2 Kacheln  -> letzte klicken
          /medialist        3 Kacheln  -> letzte klicken
          /medialist-albums 16 Kacheln -> letzte klicken
          Titelliste        19x .tl-item   ("1Die Cherry Town Meisterschaft ...")

      Die TIEFE haengt am Eintrag (hier drei Stufen), also nicht festverdrahten,
      sondern laufen, bis `.tl-item` da ist. `.tl-item` ist die richtige Marke
      fuer eine Titelzeile; `ion-item`/`ion-list` gibt es auf diesen Seiten
      nicht.

      **Gemessene Anlaufzeiten** (Pi 5, warme Seite, 2026-07-28): Startseite
      ~0,9 s, Medienliste 0,05 s, Titelliste ~2,3 s (drei Ebenen), Player offen
      0,06 s, Bild 0,00 s, Leiste 0,00 s.

      Ein einzelner Lauf zeigte die Leiste erst nach 1,9 s, und das wurde hier
      voreilig als Befund notiert - drei Wiederholungen ergaben 0,00 s. Es war
      der Lauf, der die Wiedergabe erst startete, nicht die Oberflaeche. Merke:
      eine Zahl aus EINEM Lauf ist kein Befund, erst die Wiederholung macht sie
      zu einem.

      Benutzt von `tools/e2e/player-anlauf.mjs` (MuPiBox-Repo).

  - kind: signature
    id: mupi-quellen-mischen
    title: "Zwei Tonquellen gleichzeitig (Spotify + Jellyfin mischen)"
    sev: high
    match: "pactl list sink-inputs zeigt librespot UND mpv"
    cause: "Eine neue Quelle wird gestartet, ohne die laufende zu beenden - die Box schaltet nicht um, sie mischt"
    fix: "vor dem Start der zweiten Quelle /player/current/stop rufen; dauerhaft muss der Start einer Quelle die andere selbst beenden"
    tags: [ton, spotify, jellyfin, librespot, mpv]
    body: |
      Am 2026-07-28 vom Nutzer GEHOERT ("ohje wir mixen") und danach
      kontrolliert nachgestellt:

          nach Spotify-Start:       ['PipeWire ALSA [librespot]']
          nach Jellyfin OHNE Stopp: ['PipeWire ALSA [librespot]', 'mpv']

      Beide Stroeme bleiben an der Tonsenke offen, man hoert beide Titel
      uebereinander.

      **Warum der Umschalttest das nie gesehen hat:** er misst den PEGEL nach
      einem Wechsel - und Pegel ist da, sogar doppelt. Wer nur fragt "kommt
      Ton?", bekommt beim Mischen ein Ja. Deshalb zaehlt die neue Gruppe
      `mupi-check quellenmix` die STROEME statt der Lautstaerke.

      **Achtung, falsche Entwarnung:** ist nur EIN Strom zu sehen, heisst das
      nicht automatisch "sauber umgeschaltet" - es kann auch heissen, dass die
      zweite Quelle gar nicht angelaufen ist und weiter die erste spielt. Die
      Pruefung meldet in dem Fall bewusst "nicht geprueft, kein Freispruch"
      statt ok. Genau dieser Fall trat beim ersten Lauf ein.

      librespot haelt seinen PipeWire-Kanal uebrigens dauerhaft offen, auch
      ohne Wiedergabe - ein offener Kanal allein ist also noch kein Ton.

  - kind: howto
    id: mupi-oberflaeche-aufraeumen
    title: "Nach einem Testlauf die Oberflaeche aufraeumen (ohne F5, ohne Kiosk-Neustart)"
    tags: [tests, kiosk, oberflaeche, aufraeumen]
    body: |
      Ein Testlauf laesst die Box mitten in der Bibliothek stehen - offener
      Player, gestoppte Wiedergabe, irgendeine Titelliste. Wer danach
      hinschaut, haelt das leicht fuer einen Fehler.

      **Der Weg:** `POST /api/oberflaeche/neuladen` haengt einen Zaehler an den
      Wert von `GET /api/oberflaeche/stand`. Die Oberflaeche fragt diesen Wert
      ohnehin einmal je Minute (`TAKT_MS = 60_000`) und laedt bei einer
      AENDERUNG neu — es wird also nichts erzwungen, die Box bekommt nur einen
      Grund. Eingebaut in `tools/aufraeumen.sh`, `tools/pruefen.sh --box` und
      die drei e2e-Tests.

          curl -X POST http://<box>:8200/api/oberflaeche/neuladen
          -> {"status":"ok","angefordert":2}
          stand: main-XYZ.js|1785262979|r2

      Der Zaehler liegt nur im Arbeitsspeicher; nach einem Serverneustart
      beginnt er wieder bei 0, was nichts schadet (dann laedt die Box ohnehin).
      Er wird NUR angehaengt, wenn er groesser 0 ist — sonst haette allein die
      Einfuehrung des Feldes jede laufende Box einmal grundlos neu geladen.

      Die Musik laeuft weiter: sie spielt in librespot/mpv, nicht im Browser.

      **WAS DAS NICHT KANN — wichtig:** eine EINGEFRORENE Seite retten. Steht
      deren JavaScript, steht auch der Zeitgeber, und sie fragt nie wieder nach.
      Am 2026-07-28 genau so erlebt: Box eingefroren, Signal gesetzt, nichts
      geschah. Dagegen hilft nur `sudo systemctl reboot` (rund 20 s, danach war
      der Kiosk sofort wieder da). Ein Kiosk-Neustart hilft NICHT — weder ueber
      SSH noch aus einem Dienst heraus (siehe
      mupi-kiosk-restart-ueber-ssh-toetet-x; im Server steht die Notiz „am
      Geraet zweimal erlebt: schwarzer Bildschirm bis zum Neustart der Box").

      **Stolperstein beim Einbauen:** die Oberflaeche laeuft ueber https auf
      8443, die API ueber plain http auf 8200. Ein blosses Ersetzen des Ports
      ergibt `https://box:8200`, das wirft, und ein `catch` verschluckt es —
      das Aufraeumen blieb still aus, ohne dass irgendwo etwas stand. Adresse
      neu bauen und einen Fehlschlag AUSGEBEN.

  - kind: howto
    id: mupi-messmodus-live
    title: "Live messen, waehrend jemand die Box bedient (Messmodus)"
    tags: [tests, messen, oberflaeche, kiosk, werkzeuge]
    body: |
      Bei "der Player ist langsam" oder "der Flip geht nicht auf" fuehrt
      Nachdenken ueber den Code in die Irre — in diesem Projekt schon mehrfach.
      Der Messmodus zeichnet stattdessen auf, WAS angetippt wurde und wie lange
      es bis zur sichtbaren Reaktion dauerte, waehrend normal bedient wird.

          node tools/mitmessen.mjs <box> --an       einschalten und zusehen
          node tools/mitmessen.mjs <box>            nur zusehen
          node tools/mitmessen.mjs <box> --aus      ausschalten
          Strg-C                                    leitet die Regeln ab

      **Der Schalter liegt auf dem SERVER** (`/api/messung/modus`), nicht im
      Browserspeicher: ein Touch-Kiosk hat keine Konsole, in der man ihn setzen
      koennte. `--an` fordert zugleich ein Neuladen an, damit die Box ihn
      abholt. Er ist standardmaessig AUS — eine Kinderbox soll nicht dauernd
      aufzeichnen, was ein Kind antippt. Messungen liegen NUR im
      Arbeitsspeicher (Deckel 500).

      **Bewusst NICHT ueber den DevTools-Port**, obwohl das der uebliche Weg
      waere (siehe kiosk-live-untersuchen-devtools): der verlangt eine
      geaenderte Startzeile plus Neustart, darf nicht offen bleiben, und auf ihm
      ist hier schon zweimal der Bildschirm schwarz geblieben.

      ── DREI FALLEN, alle im ersten Lauf am Geraet gestellt ───────────────

      1. **Der Messboden sah aus wie ein Messwert.** Die erste Fassung sah alle
         50 ms nach; prompt stand bei fast jeder schnellen Reaktion exakt
         "50 ms". Das war die Aufloesung, nicht die Antwort der Oberflaeche.
         Jetzt horcht ein `MutationObserver` und meldet auf die Millisekunde
         (gemessen danach: 25, 5, 1 ms statt 4x 50).

      2. **Zu grobes Zustandsmodell = Fehlalarm.** Anfangs galten nur vier
         Merkmale (Pfad, Kachelzahl, Titelzahl, Player offen). Pause,
         Lautstaerke und Titelwechsel beruehren keines davon — 52 von 54 Klicks
         wurden als "ohne sichtbare Reaktion" gemeldet, also "kaputt", wo nichts
         kaputt war. Ergaenzt um Wiedergabe/Lautstaerke/Titel/Auswahl PLUS ein
         Auffangnetz `Anzeige` fuer alles Uebrige.

      3. **Das Auffangnetz zaehlte die eigene Beruehrung.** Ein Tipp faerbt das
         getippte Element sofort ein (Ripple, `:active`) — eine DOM-Aenderung
         nach einer Millisekunde. Das Netz meldete reihenweise "1 ms". Gezaehlt
         wird jetzt nur, was AUSSERHALB des angetippten Elements passiert.

      ── WEITERE LEHREN ───────────────────────────────────────────────────

      * **Nie die eigene formatierte Ausgabe zurueckauswerten.** Die Spalte mit
        der Beschriftung stiess bei langen Texten an die naechste
        ("Sonstiges (SoundtracTitelliste"), und eine Auswertung, die das
        zurueckliest, zog daraus falsche Zahlen. Immer die Rohdaten von
        `GET /api/messung` nehmen.
      * **Nicht eingeordnete Elemente schicken ihre echten Klassennamen mit**
        (`klassen`, nur bei `Sonstiges`). Damit sagt die naechste Runde, was in
        die Erkennungstabelle gehoert, statt dass wieder geraten wird.
      * **Wortteile statt Teilstrings:** `includes('play')` trifft auch
        `playlist` und `app-player` — eine KACHEL wurde so als "Player-Knopf"
        gemeldet. Klassennamen Wort gegen Wort vergleichen.
      * **Unter drei Messungen keine Regel.** Ein Einzelwert sah hier schon
        einmal nach einem Befund aus und war der Anlauf.
      * **Das Abtastintervall sieht aus wie ein Messwert.** Nach dem 50-ms-Boden
        stand bei jeder Leisten-Messung exakt "101 ms" - das war der 100-ms-Takt
        des Beobachters plus eins. Gemeint war "hat sich nie bewegt", und genau
        das steht jetzt dort. Dieselbe Falle, zweimal an verschiedenen Stellen.
      * **Selektoren nur dort feuern lassen, wo sie hingehoeren.** Die
        Aufbau-Messung lief bei JEDEM Seitenwechsel und meldete reihenweise
        "fehlt" ueber Player-Elemente, die auf einer Kachelseite gar nicht
        vorkommen. Jetzt nur beim Oeffnen des Players - und ein fehlendes
        Element schickt die tatsaechlich vorhandenen Klassen mit.
      * `pgrep -f <muster>` findet die eigene Suche mit — beim Aufraeumen
        `ps -eo pid,args | grep ... | grep -v grep` nehmen, sonst meldet man
        "laeuft noch", wo nichts mehr laeuft.

      ── ERSTE ERGEBNISSE (Pi 5, 135 Klicks, 2026-07-28) ──────────────────

          Zurueck        51 ms  (50-64,  16 Klicks)   <- belastbar, sehr eng
          Player-Knopf   55 ms  (50-87,   4 Klicks)
          Titelzeile    205 ms  (50-688,  3 Klicks)
          Kachel        300 ms  (51-750,  5 Klicks)
          Sonstiges     107 ms  (50-1724, 54 Klicks)

      Auffaellig und noch NICHT erklaert: ein sehr exakter 450-ms-Block
      (450/450/450/453/455/489) — so genau ist keine Ladezeit, das riecht nach
      einer festen Animation oder Sperre. Und ein langer Schwanz bis 2915 ms
      fuer dieselbe Handlung (Titelliste oeffnen), der NICHT nach Animation
      aussieht. Beide Zahlen stammen aus der Fassung MIT dem 50-ms-Boden, sind
      also noch einmal frisch zu messen.

    source:
      - "Session 2026-07-28, gemessen am Geraet"

  - kind: signature
    id: mupi-knopf-schnell-ton-langsam
    title: "Knopf reagiert in 22 ms, der Ton erst nach 4 Sekunden"
    sev: high
    match: "Anzeige springt sofort um, man hoert aber noch nichts / noch weiter"
    cause: "Zwischen Knopf und Ton liegen librespot bzw. mpv, der Puffer des Tonservers und der eines Bluetooth-Lautsprechers"
    fix: "am MONITOR der Tonsenke messen (mupi-check / Messmodus mit Tonwache), nicht an der Anzeige"
    tags: [ton, messen, player, bluetooth, messmodus]
    body: |
      Am Geraet gemessen (2026-07-28, Pi 5, Bluetooth-Ausgang, echte Klicks des
      Nutzers):

          Player-Knopf gedrueckt -> Knopf/Anzeige reagiert       22 ms
                                 -> Ton setzt WIRKLICH ein     3890 ms
                                 -> Ton verstummt WIRKLICH     2558 ms

      Faktor rund 100 zwischen dem, was die Anzeige suggeriert, und dem, was
      man hoert. Wer nur die Oberflaeche misst, haelt die Box fuer flink und
      versteht nicht, warum sie sich traege anfuehlt.

      **Konsequenz fuers Messen:** eine Zeitmessung an der Anzeige beantwortet
      NICHT die Frage "wann stoppt es". Dafuer die Tonwache benutzen
      (`node tools/mitmessen.mjs <box> --an`, siehe mupi-messmodus-live); sie
      liest den Monitor der Tonsenke mit und ordnet jeden Ton-Wechsel dem
      ausloesenden Klick zu. Ohne Klick bleibt die Dauer LEER, statt sich einen
      Ursprung auszudenken.

      Nebenbefund derselben Runde: `Aufbau Bild — zieht 272 ms nach`. Das Cover
      ist also da und aendert sich danach noch einmal — das ist messbar der
      "das Bild laedt nach"-Effekt, den man beim Bedienen sieht.

      NOCH NICHT ERKLAERT: woher die knapp 4 s beim Start kommen (Puffer des
      Bluetooth-Lautsprechers? Anlauf von mpv? beides?) und ob die Werte am
      Klinken-/Lautsprecherausgang genauso aussehen. Je zwei Messungen - ein
      klarer Befund, aber noch keine Regel.
    source:
      - "Session 2026-07-28, Messmodus mit Tonwache am Geraet"

  - kind: optim
    id: mupi-vorlesen-schnell
    title: "Vorlesen dauerte 14 s: Modell bleibt geladen, und die Stimme entscheidet den Rest"
    tags: [vorlesen, piper, tts, perf]
    body: |
      GEMESSEN auf der Box (2026-07-28), ein neuer Titel:

          piper je Aufruf neu gestartet     14,4 s
          als laufender Dienst (gleiche Stimme)   7-10 s
          aus dem Zwischenspeicher            0,004 s

      Die Zeit steckte NIE im Sprechen, sondern im Laden des Sprachmodells -
      das kam bei jedem einzelnen Titel erneut von der Karte. Piper laeuft
      jetzt als Dienst (`python -m piper.http_server`, Port 5100), wird bei der
      ERSTEN Anfrage gestartet (viele Boxen lesen nie vor) und behaelt das
      Modell. Der alte Programmaufruf bleibt als Rueckfall: langsam ist besser
      als stumm.

      Zur LAUTSTAERKE der Ansage siehe mupi-vorlesen-lautstaerke - die ist
      eine eigene Falle und hat mit dem Tempo nichts zu tun.

      **Der groessere Hebel ist die STIMME.** Derselbe Satz ueber den warmen
      Dienst:

          de_DE-thorsten-high    2,14 s
          de_DE-ramona-low       0,30 s
          de_DE-eva_k-x_low      0,27 s

      Eine `-high`-Stimme rechnet auf einem Pi rund siebenmal laenger als eine
      `-low`. Wer Tempo will, nimmt `-low`; wer Thorsten will, sollte die Titel
      der Bibliothek im Hintergrund vorproduzieren, damit der erste Druck aus
      dem Zwischenspeicher kommt.

      **DAS TEMPO GEHOERT AN DEN DIENST-START.** `--length-scale` laesst sich
      NICHT je Anfrage mitgeben; Stimme und Tempo stehen fest, sobald der
      Dienst laeuft. Beim Umbau wurde das Tempo vergessen - eingestellt war 2
      (halbe Geschwindigkeit fuer Kinder), gesprochen wurde in Normaltempo,
      also doppelt so schnell. Gemeldet wurde das als „liest etwas
      Unverstaendliches". Aendert sich Stimme ODER Tempo, muss der Dienst neu
      starten. Und der Zwischenspeicher muss geleert werden: die Dateien lagen
      unter dem RICHTIGEN Namen, hatten aber den falschen Inhalt.

      Nebenrechnung: Tempo 2 heisst auch doppelt so viel Ton, also doppelte
      Synthesezeit - mit `thorsten-high` sind das rund 6 s je neuem Satz.

      **Stolpersteine beim Messen** (alle selbst gestellt): der Endpunkt heisst
      `/synthesize` und will POST+JSON - ein GET auf `/` liefert die Webseite,
      und eine 153-Byte-Antwort ist keine Sprache, sondern ein Fehler. Wer den
      Text durch mehrere Shell-Schichten reicht, verliert die Anfuehrungszeichen
      (265 Bytes = derselbe Fehler); die Anfrage besser auf der Box mit Python
      bauen. Und `pkill -f piper` trifft auch den Dienst des Servers - beim
      Messen eigene Ports nehmen und gezielt die eigene PID beenden.
    source:
      - "Session 2026-07-28, am Geraet gemessen"

  - kind: howto
    id: mupi-themen-und-classic
    title: "Themen (Classic / New / Tiger) und woher der klassische Stand belegt ist"
    tags: [darstellung, themen, classic, oberflaeche]
    body: |
      Drei mitgelieferte Themen, umschaltbar unter **Verwaltung → Darstellung**:

        Zeile „Thema"     Name eintippen + Speichern -> legt den GERADE
                          eingestellten Zustand unter dem Namen ab. Ein
                          vorhandener Name UEBERSCHREIBT.
        Zeile „Abgelegt"  Klick auf den Namen LAEDT und wendet sofort an.
                          Die drei mitgelieferten haben kein ✕.

      SEIT 2026-07-29 zeigt der Waehler, welches Thema laeuft („Jetzt: Tiger
      Mupi") bzw. dass von Hand nachgestellt wurde. Vorher stand das nirgends,
      und das war die haeufigste Verwirrungsquelle. Erkannt wird es durch
      VERGLEICH: passt der Stand zu einem abgelegten Thema? Bei mehreren
      Treffern gewinnt das mit MEHR Feldern - eines mit zwei Feldern passt
      schnell zufaellig. Kein Treffer heisst „eigene Einstellung" und ist ein
      ehrliches Ergebnis, kein Fehler.

      Die mitgelieferten drei sind als solche gekennzeichnet und haben kein ✕.
      Dafuer gibt es den Knopf **„Voreinstellungen bearbeiten"**: je
      mitgeliefertem Thema erscheinen dann

        „Übernehmen"  legt den JETZIGEN Stand unter diesem Namen ab
        „Original"    holt den AUSLIEFERUNGSSTAND zurueck

      Das zweite ist der Punkt: „aenderbar, aber nicht loeschbar" hiess vorher
      nur, dass ein GELOESCHTES Thema zurueckkommt. Wer eines ueberschrieben
      hatte, kam nicht mehr an das Original - es stand im Code, aber kein Weg
      fuehrte hin. `GET /api/darstellung` liefert dafuer jetzt zusaetzlich
      `auslieferung` (die Staende aus `themen.ts`).

      ACHTUNG BEIM AENDERN EINER VOREINSTELLUNG IM CODE: eine gespeicherte
      Fassung GEWINNT (siehe `ergaenzen`). Wer `themen.ts` anpasst, sieht davon
      auf einer bestehenden Box nichts, bis dort einmal „Original" gedrueckt
      wird. Das ist gewollt - sonst ueberschriebe ein Update die eigene
      Anpassung -, aber es sieht beim Testen aus wie „wirkt nicht".

      ── WOHER „CLASSIC" STAMMT ──────────────────────────────────────────

      NICHT von mupibox.de (das ist die Projektseite, ohne Farbwerte, /docs
      antwortet 410) und NICHT aus der Sonos-Grundlage von 2021 - die ist zu
      alt und fuehrt in die Irre. Massgeblich ist der letzte Commit VOR den
      eigenen Aenderungen: **3b1a49c7** (splitti, 2026-05-07). Dort steht:

        swiper.component.scss   .circle-card { 230px; border-radius: 50%;
                                border: 6px solid var(--ion-color-dark) }
        player.page.scss        ion-button OHNE border-radius -> eckig
        player.page.html        zwei Lautstaerkeknoepfe
                                (volume-low-outline / volume-high-outline),
                                ion-range fuer den Fortschritt IST vorhanden,
                                KEIN Stern, KEIN Cover-Flip
        home.page.html          oben links ein Knopf `timer-outline` -> resume()
        medialist.page.ts       coverClicked -> direkt in den Player

      `var(--ion-color-dark)` ergibt in der DUNKLEN Oberflaeche rgb(244,245,248),
      also fast weiss - das ist GEWOLLT (heller Ring auf dunklem Grund). Der
      Versuch, das als Fehler zu „korrigieren", war selbst der Fehler.

      ── FALLEN, alle am Geraet aufgelaufen ──────────────────────────────

      * **Zwei Karten je Kachel.** Eine Regel auf `swiper-slide ion-card`
        trifft Bild UND Namen - die runde Umrandung lief um den Titeltext.
        Nur `.img-row ion-card` ansprechen, `.title-row` ausnehmen.
      * **Dasselbe Bauteil in den Einstellungen.** Die Einstellungsseite nutzt
        den Swiper ebenfalls; ihre Eintraege bekamen den Albumring mit.
        `app-settings` ausnehmen.
      * **`--mupi-tile` steht AM ELEMENT.** Ein Wert auf `body` wird davon
        ueberschrieben und bleibt wirkungslos. Die Formel lautet
        `clamp(140px, calc(100vh - 310px), 230px)`; die 310px sind der Platz
        fuer Kopfleiste, Mini-Player, Fortschrittsleiste. Wo es die nicht gibt
        (Classic), bleibt auf 437px Hoehe nichts uebrig und die Kachel faellt
        auf die Untergrenze 140px.
      * **`zeigeLeiste=false` am Swiper** setzt `scrollbar="false"`, und dessen
        eigener Code raeumt dann ein nie angelegtes Element ab:
        „Cannot read properties of null (reading 'remove')". Die Leiste lieber
        per Stil verbergen.
      * **Ein Lifecycle fuer MEHRERE Swiper.** `SwiperIonicEventsHelper` nutzte
        `viewChild` (Einzahl); mit mehreren Reihen blieben alle ausser der
        ersten leer. Und `ionViewDidEnter` feuert EINMAL - Reihen, die spaeter
        aus einer Abfrage entstehen, muessen es nachgemeldet bekommen.
        Befund sah beide Male gleich aus: richtige Ueberschriften, null Kacheln.
      * **Ein zweiter Browser ueberschreibt die Darstellung.** Die Oberflaeche
        schickt ihren Stand zur Box zurueck; jede frisch geladene Sitzung
        (auch eine Test-Sitzung) setzt damit das Aktive. Beim Messen im Blick
        behalten.
      * **Eine „Rettung" kann Eigenes ueberschreiben.** Die mitgelieferten
        Themen werden serverseitig ergaenzt, wenn sie fehlen - anfangs mit der
        Fassung AUS DEM CODE, wodurch eine von Hand skalierte Kachelgroesse
        (1.8) wieder auf 1 stand. Regel jetzt: GESPEICHERTES gewinnt, die
        mitgelieferte Fassung kommt nur, wenn gar nichts da ist.

      ── VERHALTEN GEHOERT INS THEMA ─────────────────────────────────────

      `aussehen`, `beimVerlassen` ('weiter'|'stopp'), `flipKarte` - so nimmt
      ein Themenwechsel alles zurueck. Ein erster Entwurf koppelte das Stoppen
      an den fehlenden Mini-Player; plausibel, aber ein verstecktes zweites
      Regelwerk, das niemand findet.

      ── WARUM EINE ANZEIGE IM KOPF FEHLT: DREI TORE ─────────────────────

      `kopfAnzeigen()` in `home.page.ts` laesst eine Anzeige nur durch, wenn
      ALLE DREI stimmen - eines allein zu setzen bewirkt nichts:

          d.kopfOrdnung()   enthaelt die Kennung ueberhaupt?   z.B. ['bt','netz']
          d.zeigeXxx()      ist sie eingeschaltet?             zeigeBt: true
          d.imKopf(s)       gehoert sie in die LEISTE?         kopfIcons: {bt:true,…}

      Das dritte ist das ueberraschende: was `imKopf` verneint, verschwindet
      nicht - es SCHWEBT frei und steht dann auf jeder Seite statt nur im
      Kopf. Wer „die Anzeige ist weg" meldet, muss also erst sagen, WO sie
      fehlt.

      Nachsehen laesst sich das ohne Umweg ueber die Oberflaeche, direkt in
      `server/config/darstellung.json` unter `themen.<Name>`.

      Die KACHELFORM (`kachelForm` rund|abgerundet|eckig, `kachelRand` +
      Farbe + Breite) steht bewusst NICHT in den Themen: sie ist eine
      eigenstaendige Wahl und soll ueber einen Themenwechsel hinweg bleiben.
      Die Farbe wird als `#rrggbb` geprueft, bevor sie in eine CSS-Regel geht.

      Wie das Dienst-Zeichen ueber den Kreis und ueber den Ring hinaus kommt,
      steht in mupi-kachel-ueberstand - dort auch, warum Prozentangaben dafuer
      untauglich sind.
    source:
      - "Session 2026-07-28/29, am Geraet geprueft"

  - kind: gotcha
    id: mupi-vorlesen-lautstaerke
    title: "Die Ansage bruellt: die Box-Lautstaerke gilt nicht fuer den Browser"
    tags: [vorlesen, tts, lautstaerke, pipewire, browser]
    body: |
      ERLEBT am Geraet (2026-07-29): „der tts hat mich gerade angebruellt und
      der ton vom lied war sehr leise". Beides zugleich - das ist der Hinweis.

      ── URSACHE ─────────────────────────────────────────────────────────

      Die Box-Lautstaerke (`volume: 40`) wird an librespot und mpv
      durchgereicht. Die Ansage kommt aber aus dem KIOSK-BROWSER, und der
      kennt diesen Wert nicht - ein `<audio>`-Element startet immer bei
      `volume = 1`. Steht die Box auf 40 %, ergibt das:

          Musik      40 %   (und beim Vorlesen nochmal gedaempft)
          Ansage    100 %

      Es ist also kein Fehler der Daempfung, sondern eine Luecke: der eine Weg
      war geregelt, der andere nie. Wer nur an der Daempfung dreht,
      verschiebt das Missverhaeltnis, statt es aufzuloesen.

      ── BEHOBEN ─────────────────────────────────────────────────────────

      Die gemeinsame Stimme (siehe mupi-vorlesen-schnell) liest die
      Box-Lautstaerke und setzt sie auf sich selbst. Weil der Wert ueber das
      Netz kommt, startet sie bei einem gedaempften Vorgabewert (0.4) und
      zieht erst nach - so kann sie in der Wartezeit nicht losbruellen.
      Faellt die Abfrage aus, bleibt es beim Vorgabewert: lieber etwas zu
      leise als jemanden anschreien.

      **PFADFALLE - `/player/local`, NICHT `/local`.** Der Player sitzt hinter
      dem Proxy; `environment.backend.playerUrl` endet auf `/player`. Ein
      `fetch('/local')` laeuft ins Leere, faellt still in den Vorgabewert und
      sieht damit aus wie „geht nicht". Am Geraet gegengeprueft, dass der Pfad
      wirklich antwortet - nicht nur, dass der Code keinen Fehler wirft.

      ── FOLGE FUER DIE DAEMPFUNG ────────────────────────────────────────

      `LEISE_PROZENT` 20 → 45 (`src/backend-api/src/daempfen.ts`). Die 20 %
      waren die Gegenwehr gegen eine Ansage auf voller Lautstaerke. Da die
      Ansage jetzt mitzieht, genuegt ein sanftes Zuruecktreten - ein Test
      haelt den Bereich fest (>= 40, < 70), damit niemand aus Versehen wieder
      auf den alten Wert zurueckfaellt.

      REGEL: Lautstaerke ist nichts Globales auf dieser Box. Jeder Tonweg
      (librespot, mpv, Browser) hat seinen eigenen Regler. Wer einen neuen
      Tonweg dazunimmt, muss ihn ausdruecklich anbinden.
    source:
      - "Session 2026-07-29, am Geraet erlebt und behoben"

  - kind: gotcha
    id: mupi-kachel-ueberstand
    title: "Etwas ragt nicht ueber die Kachel: es ist `contain`, nicht `overflow`"
    tags: [oberflaeche, classic, css, kacheln, ionic]
    body: |
      AUFGABE (2026-07-29): das Dienst-Zeichen soll im Classic-Aussehen auf dem
      runden Cover liegen und ueber den Ring hinausragen.

      ── DIE FALSCHE FAEHRTE, ZWEIMAL GELEGT ─────────────────────────────

      Erster Versuch: `overflow: visible` auf die Kachel, `z-index: 10` aufs
      Zeichen. Ergebnis am Geraet: das Zeichen war ABGESCHNITTEN - auf der
      einen Kachel halbiert, auf der naechsten ganz weg.

      Die Suche nach dem Schuldigen lief ueber die Vorfahren und fragte
      `overflow` ab. Sie meldete fuer JEDEN Vorfahren `visible` - also
      angeblich kein Zuschnitt, obwohl der Zuschnitt sichtbar da war und exakt
      der Kreisform folgte. Danach war der naechste Verdacht der Shadow-Root
      von `ion-card` (`parentElement` laeuft nicht hinein). Auch falsch: dort
      steckt nur ein blanker `slot`.

      ── DIE URSACHE ─────────────────────────────────────────────────────

      `ion-card` bringt **`contain: content`** mit. Das ist `layout paint
      style` - und das PAINT-Containment schneidet an der Randform ab, hier
      also am `border-radius: 50%`.

      **`overflow` meldet dabei unveraendert `visible`.** Wer nur danach
      sucht, findet nie etwas. Gemessen wurde es erst, als die Abfrage
      `clipPath`, `mask`, `contain`, `filter` und `transform` MIT abfragte
      statt nur `overflow`.

          contain: layout style   ->  Layout- und Stil-Kapselung bleiben,
                                      nur das Malen darf hinaus

      REGEL: „Etwas wird abgeschnitten" hat auf dieser Oberflaeche mindestens
      drei mögliche Ursachen - `overflow`, `clip-path`/`mask` und `contain`.
      Die Vorfahren-Abfrage muss alle drei zeigen, sonst beweist ein
      „nichts schneidet ab" gar nichts.

      ── UND DIE LEERE ECKE ──────────────────────────────────────────────

      Die Kachel ist QUADRATISCH, der Kreis darin einbeschrieben - die untere
      rechte Ecke ist leer. Ein Zeichen, das dorthin ausweicht, klebt neben
      dem Cover statt darauf; „ragt 4 px ueber die Kachel" war messbar wahr
      und trotzdem nicht das Gewuenschte.

      Der Punkt der Kreiskante auf 45 Grad liegt (1-cos45)/2 = **14,65 %** der
      Kantenlaenge vom Rand entfernt. Halbe Zeichenbreite zurueck, und es
      sitzt mittig auf der Kante:

          right:  calc(14.65% - 20px)   /* 20px = halbe Zeichenbreite */
          bottom: calc(14.65% - 20px)

      Rechnerisch statt in festen Pixeln, weil die Kachelgroesse an der
      Fensterhoehe haengt (`--mupi-tile: clamp(140px, calc(100vh - 310px),
      230px)`) - gemessen 300 px auf dem einen, 380 px auf dem anderen Schirm.

      ── NACHMESSEN, NICHT ANSCHAUEN ─────────────────────────────────────

      Ein Ueberstand ist auf einem Foto nicht von „sitzt buendig" zu
      unterscheiden. Am Geraet `rb.right - rc.right` von Zeichen und Kachel
      vergleichen - UND einen Bildausschnitt ansehen. Die reine Zahl sagte
      hier „4 px darueber", waehrend das Zeichen in Wahrheit weggeschnitten
      war: die Box eines Elements existiert auch dann, wenn nichts davon
      gemalt wird.

      ── VERWANDTE STOLPERSTELLE ─────────────────────────────────────────

      `--mupi-tile` steht AM ELEMENT, nicht am `body` - eine Ueberschreibung
      auf `body`-Ebene bleibt wirkungslos. Wer die Kachelgroesse aendern will,
      muss `.circle-card`/`swiper-slide` treffen.
    source:
      - "Session 2026-07-29, am Geraet gemessen und im Bild geprueft"

  - kind: falle
    id: mupi-kerntests-node26
    title: "Zwei Backend-Tests scheitern IMMER unter Node 26 - nicht sich selbst verdaechtigen"
    tags: [tests, node, backend, werkzeug]
    body: |
      STAND 2026-07-29: `npx tsx --test src/*.spec.ts` im Backend meldet
      Ausfaelle, die NICHTS mit der eigenen Aenderung zu tun haben. Ich bin in
      EINER Sitzung dreimal darauf hereingefallen und habe zweimal faelschlich
      „Kerntests kaputt" gemeldet.

      ── DIE BEIDEN ALTLASTEN ────────────────────────────────────────────

      1. `server.spec.ts`

             ReferenceError: __dirname is not defined in ES module scope
               at server.ts: app.use(express.static(path.join(__dirname,'www')))

         Unter tsx/Node 26 laeuft server.ts als ES-Modul, dort gibt es kein
         `__dirname`. Im ausgelieferten Bundle (esbuild, CJS) gibt es eines -
         siehe die aeltere Notiz zu genau dieser Zeile. Der Test reisst schon
         beim IMPORT ab, es laeuft also keine einzige Zusicherung.

      2. `auth.integration.spec.ts`

             'test did not finish before its parent and was cancelled'

         Kindtests mit `done`-Rueckruf, deren Elternteil unter dem neueren
         Laeufer frueher fertig ist. Auf dem SAUBEREN Baum: 0 bestanden,
         2 gescheitert - also nicht durch eine Aenderung ausgeloest.

      ── DAS VERFAHREN, DAS DAS KLAERT ───────────────────────────────────

      Bevor man den Ausfall der eigenen Arbeit zuschreibt: gegen den sauberen
      Baum laufen lassen. Zwei Zeilen, zehn Sekunden:

          git stash -q && npx tsx --test src/<datei>.spec.ts ; git stash pop -q

      Scheitert es dort genauso, ist es eine Altlast. Das ist billiger als
      jede Ueberlegung, WARUM es die eigene Aenderung sein koennte - und es
      beantwortet die Frage, statt sie plausibel zu machen.

      Zum Weiterarbeiten die kaputte Datei ausklammern, statt die Zahl zu
      ignorieren:

          npx tsx --test $(ls src/*.spec.ts | grep -v server.spec.ts)
          -> 500 bestanden (Stand 2026-07-29)

      ── FRUEHERE FEHLDIAGNOSE, GLEICHE FAMILIE ──────────────────────────

      Zweimal wurde „Kerntests backend-player kaputt" gemeldet, obwohl nur ein
      Zeitablauf zugeschlagen hatte - weil PARALLEL gebaut und ausgeliefert
      wurde. Ein Testlauf neben einem Angular-Bau auf derselben Maschine ist
      kein Testlauf. Erst warten, dann messen.

      OFFEN: beide Altlasten sind reparierbar (`__dirname` per
      `import.meta.url` ersetzen bzw. die `done`-Tests auf `async` umstellen).
      Bis dahin gehoert dieser Eintrag gelesen, BEVOR jemand die eigene
      Aenderung zurueckdreht.
    source:
      - "Session 2026-07-29, gegen den sauberen Baum gegengeprueft"

  - kind: falle
    id: mupi-cdp-skript-cwd
    title: "Pruefskript nach /tmp gelegt: `Cannot find package 'ws'`"
    tags: [werkzeug, cdp, node]
    body: |
      Die kleinen Wegwerf-Skripte, die per CDP am Geraet nachsehen (siehe
      werkzeuge-benutzen), brauchen `ws`. Node sucht Pakete ab dem ORDNER DER
      DATEI aufwaerts - liegt das Skript unter `/tmp`, gibt es dort kein
      `node_modules`:

          Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'ws'
            imported from /tmp/chk.mjs

      Also IM Projektordner ablegen und danach wegraeumen:

          cd ~/Downloads/MuPiBox && cat > ./chk.mjs <<'EOF' … EOF
          node ./chk.mjs ; rm -f ./chk.mjs

      Nicht `/tmp` - auch wenn es sich als Ablage fuer Wegwerfbares richtig
      anfuehlt. (Der Bildausschnitt danach darf sehr wohl nach /tmp.)
    source:
      - "Session 2026-07-29"

  - kind: offen
    id: mupi-jellyfin-stumm
    title: "GELOEST: Jellyfin gab keinen Ton — dem Abspieldienst fehlte XDG_RUNTIME_DIR"
    tags: [jellyfin, ton, geloest, messen, pipewire, systemd]
    body: |
      GELOEST AM 2026-08-01. Die Ursache steht ganz unten unter DIE AUFLOESUNG;
      alles davor ist der Messstand von 2026-07-29 und bleibt stehen, weil er
      zeigt, wie der Fehler aussah.

      NICHT GELOEST, Stand 2026-07-29. Hier steht, was gemessen ist, damit die
      naechste Runde nicht bei null anfaengt.

      ── DER BEFUND ──────────────────────────────────────────────────────

      Mit der Tonwache am Monitor der Tonsenke (siehe mupi-messmodus-live):

          Spotify starten   -> Pegel 0.389 / 0.361
          Jellyfin starten  -> Pegel 0.000

      Die Oberflaeche verhaelt sich dabei unauffaellig: der Titel wird
      uebernommen, die Anzeige laeuft. Es ist also KEIN reiner Bedienfehler
      und kein sichtbarer Absturz - es kommt schlicht nichts am Ausgang an.

      ── WAS DAMIT NOCH NICHT GESAGT IST ─────────────────────────────────

      Ein Pegel von 0.000 sagt nur „am Ausgang kommt nichts an". Er
      unterscheidet NICHT zwischen:

        * es wird gar nicht erst abgespielt (Abruf/Adresse/Anmeldung),
        * es spielt, landet aber auf einer anderen Senke,
        * es spielt auf der richtigen Senke, ist aber stummgeschaltet,
        * das Format wird nicht unterstuetzt und bricht still ab.

      NOCH NICHT GEPRUEFT (in dieser Reihenfolge sinnvoll):
        1. `pactl list sink-inputs` WAEHREND Jellyfin laeuft - taucht
           ueberhaupt ein Strom auf, und auf welcher Senke?
        2. Laeuft der Abruf? Antwortet die Jellyfin-Adresse mit Ton-Daten,
           oder scheitert schon die Anmeldung?
        3. Was passiert bei einem anderen Titel/Format?

      REGEL, die hier gilt: erst das Werkzeug, das den Befund beziffert - der
      Griff nach `pactl` waehrend der Wiedergabe beantwortet Punkt 1 in
      Sekunden und halbiert den Suchraum.

      ── DIE AUFLOESUNG (2026-08-01) ────────────────────────────────────

      DEM ABSPIELDIENST FEHLTE `XDG_RUNTIME_DIR`. PipeWire ist ein
      SITZUNGSdienst und horcht unter /run/user/<uid>. Ein Systemdienst bekommt
      diese Variable nicht von selbst - auch nicht, wenn er als derselbe
      Benutzer laeuft. `mupibox-player.service` hatte sie nicht,
      `librespot.service` schon. Genau daher der gemessene Unterschied:
      Spotify 0.389, Jellyfin 0.000.

      WAS MPV OHNE SIE TUT - und warum es nicht wie ein Tonproblem aussieht:
      Es meldet `current-ao: None`, nimmt `loadfile` mit `success` an, legt die
      Datei in die Liste und STARTET SIE NIE. `playlist-count` steigt,
      `playlist-pos-1` bleibt -1, `idle-active` bleibt true. Die Oberflaeche
      sieht dabei unauffaellig aus, weil der Dienst seinen eigenen
      `playing`-Schalter setzt. Man sucht also im Tonweg, waehrend die
      Wiedergabe nie begonnen hat.

      DER BEWEIS WAR EINE GEGENPROBE, kein Schluss: dasselbe mpv, dieselben
      Argumente (`--idle=yes --no-video --no-terminal --msg-level=all=warn
      --input-ipc-server=...`), einmal aus einer SSH-Sitzung gestartet und
      einmal aus dem Dienst. Aus der Sitzung - die traegt XDG_RUNTIME_DIR -
      spielt es dieselbe Jellyfin-Adresse sofort: `current-ao: pipewire`,
      `playlist-pos-1: 1`, `time-pos` laeuft. Aus dem Dienst nicht.

      DIE SCHRITTE DORTHIN, jeder hat einen Verdacht ausgeschlossen:
        * Adresse direkt geholt -> HTTP 200, 5,6 MB audio/mpeg (Jellyfin ist
          unschuldig)
        * `loadfile` DIREKT an mpvs Socket, an der Box vorbei -> dasselbe
          Verhalten (nicht die MuPiBox)
        * `set_property playlist-pos-1 1` -> antwortet `success`, Wert bleibt
          -1 (ein `success` ist hier KEIN Beleg)
        * Umgebungen verglichen -> der Unterschied stand in /proc/<pid>/environ

      BEHOBEN: `Environment=XDG_RUNTIME_DIR=/run/user/1000` in
      mupibox-player.service. Im Rezept des remote-step-installer
      (mupibox-app.yaml, Schritt der die Units schreibt); auf einer laufenden
      Box sofort per Zusatzstueck
      `/etc/systemd/system/mupibox-player.service.d/pipewire.conf`.
      Danach am Geraet gemessen: `time-pos` laeuft, `duration` 140 s, und
      `wpctl status` zeigt einen mpv-Strom auf dem Bluetooth-Lautsprecher.

      NEBENBEI GELOEST: Auch die Titelzaehlung stimmte danach (`Titel 1/12`
      statt `12/12`). Solange nichts spielt, meldet mpv nie eine Stelle, und
      `mplayerZaehlt` bleibt auf `true` - jedes metadata-Ereignis zaehlt dann
      hoch. Siehe [[titelangaben-falsch]].
    source:
      - "Session 2026-07-28/29, mit der Tonwache gemessen"
      - "geloest und am Geraet gegengeprueft 2026-08-01"

  - kind: offen
    id: mupi-classic-albumliste
    title: "OFFEN: im Classic ist die Albumliste noch eine Aufklapp-Karte"
    tags: [classic, oberflaeche, offen]
    body: |
      NICHT ERLEDIGT, Stand 2026-07-29.

      Gewuenscht (mehrfach gesagt): im klassischen Aussehen soll die Auswahl
      der Titel als EINZELNE EINTRAEGE erscheinen, wie in der Liste davor -
      ausdruecklich KEINE „Popup"-Karte, die sich ueber die Oberflaeche legt.

      Gebaut ist bisher: die Aufklapp-Karte ist geblieben.

      Zum Nachsehen, wie es im Original aussah: der Auscheckstand VOR den
      eigenen Commits (siehe mupi-themen-und-classic - dasselbe Vorgehen hat
      dort schon die eckigen Bedienelemente und die fehlenden Favoriten
      geklaert). Nicht mupibox.de als Vorlage nehmen; die Seite zeigt eine
      andere Fassung.
    source:
      - "Session 2026-07-29, offen geblieben"

  - kind: falle
    id: mupi-plaetze-weisse-liste
    title: "Anzeigen liessen sich schieben, sassen nach dem Start wieder am alten Ort"
    tags: [darstellung, anordnen, oberflaeche, falle]
    body: |
      GEFUNDEN 2026-07-29 auf der Suche nach „wie platziere ich die Anzeigen".

      Das Anordnen-Feld laesst seit langem ALLES ziehen: Mini-Player, Stern,
      Weiterhoeren, Cover, Titel, die Kategorien-Knoepfe, die Anzeigen
      (Bluetooth, Funk, Wolke, Netz) und die Zeiten (Uhr, Timer, Rest, heute).
      Die Pruefung beim LADEN nahm aber nur drei Namen an:

          for (const k of ['mp', 'fav', 'res'] as const)   // <- alles andere weg

      Alles Uebrige wurde stillschweigend verworfen. Man konnte also schieben,
      es sah richtig aus, und nach dem naechsten Start sass es wieder am
      eingebauten Platz. Das WIRKT wie „speichert nicht" und schickt die Suche
      zum Speichern, zum Backend, zum localStorage - dabei war es ein Filter
      drei Zeilen weiter.

      ── URSACHE: LISTE UND TYP LIEFEN AUSEINANDER ───────────────────────

      Der Typ `Stueck` zaehlte 21 Namen auf, die Weisse Liste drei, und das
      Interface `Plaetze` fuenf. Drei Wahrheiten ueber dieselbe Sache.

      BEHOBEN, indem es nur noch EINE gibt:

          export const STUECKE = ['mp','fav',…,'jellyfin'] as const
          export type Stueck = (typeof STUECKE)[number]
          export type Plaetze = Partial<Record<Stueck, Lage>>

      Der Typ wird aus der Liste ABGELEITET, und die Pruefung geht dieselbe
      Liste durch. Damit ist dieser Fehler nicht mehr moeglich, statt nur
      behoben.

      REGEL: Wo eine Weisse Liste zur Laufzeit neben einem Typ steht, driften
      beide. Den Typ aus der Liste ableiten - nicht umgekehrt und nicht
      nebeneinander.

      Nebenbefund: Plaetze sind ANTEILE (x als Bruchteil der Schirmbreite, y
      der Hoehe), damit sie auf jedem Schirm sitzen. Eine Vorgabe im Thema
      sieht deshalb so aus:

          plaetze: { uhr: {x:0.45,y:0.02}, bt: {x:0.82,y:0.02}, netz: {x:0.9,y:0.02} }
    source:
      - "Session 2026-07-29, beim Platzieren der Tiger-Anzeigen gefunden"

  - kind: knowhow
    id: mupi-einstellungen-erreichbar
    title: "Die Einstellungen oeffnen sich durch langes Halten - und das steht nirgends"
    tags: [oberflaeche, einstellungen, bedienung]
    body: |
      Die Einstellungen sind ABSICHTLICH versteckt: ein langes Halten (2 s)
      haelt Kinderhaende fern. Zwei Luecken dabei, beide 2026-07-29 geschlossen:

      1. **Es lag nur auf der Kopfleisten-Gruppe.** Themen OHNE Leiste
         (Classic, Tiger) hatten damit ueberhaupt keinen Weg hinein - die
         Anzeigen schweben dort frei. Jetzt traegt JEDE Anzeige das lange
         Halten (`.npb-anz-fab`, `.npb-zeit-fab`, `.npb-dienst-fab` in
         `now-playing-bar.component.ts`), samt Fortschrittsring; ohne
         Rueckmeldung weiss niemand, ob er noch halten muss.

      2. **Wer es nicht weiss, tippt.** Nach DREI kurzen Tipps innerhalb von
         2,5 s erscheint „Zum Öffnen lange gedrückt halten" - in jedem Thema,
         denn es haengt nicht am Aussehen, sondern daran, wie die
         Einstellungen erreichbar sind.

      Das Zaehlwerk steht rein und geprueft in
      `src/frontend-box/src/app/tippzaehler.ts` (`tippen(stand, jetzt)`, die
      Uhrzeit kommt vom Aufrufer - nur so laesst es sich pruefen, ohne zu
      warten). Zwei Entscheidungen darin sind absichtlich:

        * NICHT beim ersten Tipp - ein einzelner ist oft ein Versehen oder
          gilt der Anzeige selbst. Erst die Wiederholung ist das Zeichen.
        * Nach dem Hinweis faengt die Reihe von vorn an - sonst erschiene er
          bei jedem weiteren Tipp erneut und wuerde selbst zum Aergernis.
    source:
      - "Session 2026-07-29"

  - kind: falle
    id: mupi-tsc-prueft-keine-vorlagen
    title: "`tsc --noEmit` ist grün, der Angular-Bau scheitert - Vorlagen prüft nur er"
    tags: [angular, tests, werkzeug, falle]
    body: |
      ERLEBT 2026-07-29, zweimal hintereinander.

      `npx tsc --noEmit -p tsconfig.app.json` meldete SAUBER, und der
      anschliessende `ng build` brach ab:

          @if (a.stern) {          -> Property 'stern' does not exist
          Error occurs in the template of component HomePage.

      Grund: `tsc` sieht nur die .ts-Dateien. Die VORLAGEN (die HTML-Dateien
      und die `template:`-Zeichenketten) werden allein vom Angular-Compiler
      geprueft. Wer ein Feld in einer berechneten Liste ergaenzt und es in der
      Vorlage benutzt, muss den TYP der Signatur mitziehen - und merkt es
      erst im Bau.

      REGEL: `tsc --noEmit` ist eine schnelle Vorpruefung, KEIN Freispruch.
      Vor dem Ausliefern immer `ng build` laufen lassen; „Typen sauber"
      allein heisst nichts, sobald eine Vorlage im Spiel ist.

      ── ZWEITE FALLE IN DERSELBEN DATEI: RUECKWAERTS-HOCHKOMMA ──────────

      Ein CSS-Kommentar innerhalb eines `styles: [\`...\`]`-Blocks enthielt
      ein Rueckwaerts-Hochkomma (Klasse in Anfuehrung gesetzt). Das BEENDET
      die Vorlagen-Zeichenkette mitten im Stil. Die Fehlermeldung nennt die
      Ursache nicht:

          FatalDiagnosticError: Failed to resolve styles at position 1 to a
          string - Value could not be determined statically.

      In `template:`/`styles:`-Zeichenketten also KEINE Rueckwaerts-
      Hochkommas und kein `${` in Kommentaren. Klassennamen dort schlicht
      ohne Anfuehrung schreiben.
    source:
      - "Session 2026-07-29"

  - kind: knowhow
    id: mupi-leere-huelle-anzeige
    title: "Das schwarze leere Kaestchen: Huelle ohne Inhalt"
    tags: [oberflaeche, anzeigen, css]
    body: |
      GEMELDET 2026-07-29: „i do not want to see the black emty field".

      Die schwebenden Anzeigen bestehen aus zwei Teilen: der HUELLE
      (`.npb-anz-fab` in `now-playing-bar.component.ts` - dunkles Kaestchen
      mit Weichzeichner) und dem INHALT (`mupi-netzstatus` usw.).

      Der Inhalt blendet sich selbst aus, wenn es nichts zu sagen gibt -
      Bluetooth ohne Verbindung zeigt nichts. Die Huelle blieb aber stehen,
      weil sie nur an der EINSTELLUNG haengt (`zeigeBt()`), nicht am Zustand.
      Ergebnis: ein dunkles Kaestchen, das nichts bedeutet und wie ein Fehler
      aussieht.

      GELOEST rein im Stil, ohne neue Verdrahtung:

          :host(:not(.npb-ordnen)) .npb-anz-fab:not(:has(> * > *)) {
            display: none;
          }

      `:has()` gibt es im gebuendelten Chromium. Der `.npb-ordnen`-Vorbehalt
      ist WICHTIG: beim Anordnen muessen auch gerade stumme Anzeigen sichtbar
      bleiben, sonst kann man sie nie an ihren Platz ziehen - genau dieselbe
      Ueberlegung, die bei den Zeit-Anzeigen schon im Code stand.

      Merksatz: Wer eine Anzeige „ausblendet", muss sagen, ob er den INHALT
      oder die HUELLE meint. Beides zu verwechseln erzeugt entweder ein leeres
      Kaestchen oder eine Anzeige, die man nicht mehr platzieren kann.
    source:
      - "Session 2026-07-29, am Geraet geprueft (0 leere Huellen)"

  - kind: howto
    id: mupi-andere-zielhardware
    title: "Andere Zielhardware (Pi 4 statt Pi 5): braucht es ein eigenes Rezept?"
    tags: [installer, rezept, hardware, pi4, pi5]
    body: |
      GEPRUEFT 2026-07-29 gegen die Rezepte UND die laufende Box.

      ── ANTWORT: WAHRSCHEINLICH NEIN ────────────────────────────────────

      Das meiste ist hardware-neutral oder passt sich selbst an:

        python3-rpi-lgpio       schon portabel (Notiz: "laeuft auf Pi 4 UND 5")
        vc4-kms-v3d +           laeuft auf beiden; am Geraet gegengeprueft:
        vc4-kms-dsi-7inch       genau diese zwei Zeilen, OHNE dsi0/dsi1-Zusatz
        Touch-Bruecke           haelt sich selbst raus, wenn der Kerneltreiber
                                eine Unterbrechungsleitung hat
        Audio (MAX98357A, I2C)  auf beiden richtig
        MuPiHAT-5V-Anlauf       ENTFAELLT auf dem Pi 4 - das war die Pi-5-Sorge
        skip-rpi-eeprom         ist ohnehin ein Platzhalter (run: "true")

      ── ABER: DAS FORMAT KENNT KEINE BEDINGUNGEN ────────────────────────

      Die Schritt-Felder sind `id, name, note, run, check, optional, timeout,
      workarounds, put` - es gibt KEIN `when:`. Wer doch hardware-abhaengig
      werden muss, hat zwei Wege:

        1. INNERHALB des Schritts verzweigen:
             case "$(cat /proc/device-tree/model | tr -d '\0')" in
               *"Pi 5"*) … ;; *) … ;;
             esac
        2. Ein zweites Rezept `mupibox-pi4.yaml`

      Nimm 1. Zwei Rezepte ueber dieselbe Sache driften auseinander - genau
      der Fehler, der bei `plaetze` (mupi-plaetze-weisse-liste) und beim
      `Stueck`-Typ schon aufgelaufen ist. EIN Rezept bleibt die Wahrheit.

      ── WAS DAFUER EINGEBAUT WURDE ──────────────────────────────────────

      Ein erster Schritt `hardware` schreibt Modell, Kern, Distribution,
      DietPi-Fassung, Speicher und BOOT_DIR ins Protokoll - er aendert nichts,
      kann nicht fehlschlagen und haelt nichts auf. Damit sagt jeder Lauf,
      worauf er lief; ohne das ist bei einem gescheiterten Schritt nicht mehr
      zu klaeren, ob die Hardware schuld war.

      ZWEI SHELL-FALLEN darin, beide belegt:
        * `tr -d '\0' < /proc/device-tree/model 2>/dev/null` meldet auf einer
          Maschine OHNE diese Datei trotzdem einen Fehler: die Shell bemaengelt
          die Umleitung, BEVOR das nachgestellte 2>/dev/null greift. Also
          `cat … 2>/dev/null | tr -d '\0'`.
        * Ein `|| echo -`-Rueckfall HINTER einer Pipe greift nie - es zaehlt
          der letzte Befehl der Pipe, und der gelingt. Also in eine Variable
          lesen und `${V:--}` benutzen.

      Der Display-Schritt hiess "(Pi 5, volles KMS)". Das las sich wie
      "nur Pi 5" und war falsch; jetzt nur noch "(volles KMS)".

      ── WAS OFFEN IST (nicht behaupten, sondern messen) ──────────────────

      Trixie auf dem Pi 4 ist NIE gelaufen. Die Notizen „Trixie+Pi5-
      Regression" (Onboard-Audio) und der 69-%-Haenger bei Bluetooth stammen
      alle vom Pi 5. Ebenso ungetestet: `gpu_mem` (DietPi setzt 16 MB; unter
      KMS normalerweise egal).

      VORGEHEN: ohne Anpassung durchlaufen lassen und mitschreiben. Das
      Protokoll sagt dann, welcher Schritt klemmt - dann GEZIELT den einen
      anpassen, statt vorher auf Verdacht ein zweites Rezept zu bauen.
    source:
      - "Session 2026-07-29, Rezepte gelesen + gegen die laufende Pi-5-Box geprueft"

  - kind: howto
    id: mupi-app-lauf-checkliste
    title: "Vor dem App-Rezept: vier Dinge, die sonst mitten im Lauf auffliegen"
    tags: [installer, rezept, app, checkliste]
    body: |
      Am Geraet durchgefahren (Pi 4, 2026-07-29). Die 13 Schritte selbst laufen
      glatt - die Zeit geht fuer die Dinge DAVOR drauf.

      ── 1. DAS PAKET IST ALT UND VIELLEICHT UNVOLLSTAENDIG ──────────────

      `bin/nodejs/deploy.zip` wird von Hand bestueckt, es gibt KEIN Bauskript
      dafuer. Es lag drei Tage zurueck - die ganze Arbeit des Tages haette
      gefehlt, ohne dass ein Schritt gemeckert haette.

      Schlimmer: bei `MUPI_ADMIN=node` prueft `app-paket` ausdruecklich
      `test -f $MUPI_APP/www-admin/index.html`. Im alten Paket war
      **www-admin gar nicht drin** - der Schritt waere gescheitert.

      Also vorher frisch schnueren (src/deploy gehoert root, siehe
      llmwiki: an einen eigenen Ort bauen):

          esbuild … --target=node26 --outfile=/tmp/paket/server.js
          esbuild … --target=node26 --outfile=/tmp/paket/spotify-control.js
          ng build --output-path /tmp/paket-box     # -> /tmp/paket/www
          cp -a src/frontend-admin/www-admin /tmp/paket/
          (cd /tmp/paket && zip -qr deploy-neu.zip server.js spotify-control.js www www-admin)

      Und danach nachzaehlen, dass alle VIER Teile drin sind.

      NACHTRAG 22.08.2026 — DER SATZ „ES GIBT KEIN BAUSKRIPT" IST FALSCH.
      `src/deploy.sh` baut das Paket und legt es selbst nach
      `bin/nodejs/deploy.zip`, und `tools/kartenlauf-bereit.py` sagt vorher,
      ob es ueberhaupt alt ist (`--bauen` baut gleich). Die Handarbeit oben
      bleibt nur als Notnagel stehen, wenn der Bau nicht durchgeht. WAS
      RICHTIG BLEIBT, ist der Befund: das Zip veraltet still — siehe
      [[artefakte-veralten-still-vor-dem-kartenlauf]].

      ── 2. MUPI_ADMIN STEHT AN ZWEI STELLEN ─────────────────────────────

      In `mupibox.yaml` UND `mupibox-app.yaml`. Die frühere Notiz behauptete,
      der Schritt erkenne selbst, ob ein Webserver da ist - das traegt NICHT:
      er prueft nur, ob lighttpd INSTALLIERT ist. Auf einer Box, die frueher
      einmal mit 'php' aufgesetzt wurde, ist er das. Ergebnis: beide
      Verwaltungen nebeneinander, lighttpd laeuft fuer nichts.

      ── 3. DER AGENT HORCHT NUR AUF DER BOX ─────────────────────────────

      `curl http://<box>:8099/sysinfo` von aussen ergibt 000 - das ist KEIN
      Zeichen dafuer, dass er fehlt. Er bindet auf localhost; `./connect
      <adresse>` legt den Tunnel und pairt. Ich habe daraus faelschlich
      geschlossen, der Agent laufe nicht.

      **Der Tunnel stirbt mit jedem Neustart der Box.** Danach meldet rsi
      „connect failed: Connection refused" - einfach `./connect` erneut
      (oder gleich `--watch`, das ueberlebt Neustarts).

      ── 4. DER KIOSK BRAUCHT EINEN NEUSTART ─────────────────────────────

      Der `kiosk`-Schritt setzt `dietpi-autostart 11`; wirksam wird es erst
      beim Hochfahren. Vorher: App da, Dienste online, API 200 - und trotzdem
      kein Bild. Das ist KEIN Fehler.

      ── EINZELN FAHREN GEHT ─────────────────────────────────────────────

          ./rsi list --recipe recipes/mupibox-app.yaml
          ./rsi run  <schritt-id> --recipe recipes/mupibox-app.yaml

      Ein Schritt je Aufruf, mit Ausgabe und `exit=` - gut, wenn man nicht
      die ganze TUI offen halten will.
    source:
      - "Session 2026-07-29, App-Rezept auf dem Pi 4 durchgefahren"

  - kind: falle
    id: mupi-php-abschalten-reicht-nicht
    title: "lighttpd abschalten reicht nicht — php-fpm ist ein EIGENER Dienst"
    tags: [php, speicher, dienste, verwaltung]
    body: |
      Nach dem Umstieg auf die Node-Verwaltung (`MUPI_ADMIN=node`) soll der
      PHP-Weg weg. `systemctl disable --now lighttpd` fuehlt sich wie das
      Ende an - danach liefen aber weiterhin **5 php-fpm-Prozesse mit 70 MB**
      fuer einen Webserver, der nichts mehr ausliefert.

          systemctl disable --now lighttpd
          systemctl disable --now php8.4-fpm     # <- DAS fehlte

      Gemessen (Pi 4, 1844 MB): 6 Prozesse / 73 MB vorher, 0 danach; frei
      stieg auf 1301 MB.

      **REIHENFOLGE:** vorher pruefen, dass die Node-Verwaltung wirklich
      antwortet — `curl -L http://localhost:8200/admin/` muss 200 liefern.
      Sonst steht die Box ohne JEDE Verwaltung da.

      Verwandt: eine Node-Aktualisierung meldet „Detected old npm client,
      removing…". `pm2` liegt global unter npm - danach nachsehen, ob es
      ueberlebt hat (`command -v pm2`) und ob die Dienste noch online sind.
      Bei uns ging es gut, aber nicht selbstverstaendlich.
    source:
      - "Session 2026-07-29, am Geraet gemessen"

  - kind: falle
    id: mupi-journal-leer-ist-keine-antwort
    title: "»Im Protokoll steht nichts« — als dietpi sieht man das System-Journal GAR NICHT"
    tags: [diagnose, journal, rechte, falle]
    body: |
      Mehrfach am 2026-07-29 daraus falsche Schluesse gezogen.

          journalctl -b -u <dienst>        # als dietpi -> LEER
          sudo journalctl -b -u <dienst>   # -> die ganze Wahrheit

      Ohne sudo meldet journalctl nur: „Hint: You are currently not seeing
      messages from other users and the system." — als HINWEIS, nicht als
      Fehler, und bei `-u <dienst>` sieht man ueberhaupt nichts. Das liest
      sich wie „der Dienst hat nichts protokolliert" und heisst in Wahrheit
      „du darfst es nicht sehen".

      Konkret hat es gekostet: das Startbild galt als „gar nicht gestartet",
      obwohl es zweimal gelaufen und jedes Mal mit einer klaren Begruendung
      ausgestiegen war. Erst mit sudo stand die Ursache da.

      REGEL: bei Dienst-Diagnose auf der Box IMMER `sudo journalctl`. Ein
      leeres Protokoll ist kein Befund, solange die Rechte nicht geklaert
      sind — genauso wenig wie „overflow ist visible" bewies, dass nichts
      abschneidet (mupi-kachel-ueberstand).
    source:
      - "Session 2026-07-29"

  - kind: signature
    id: vcgencmd-display-power-am-pi5-weg
    title: >-
      `vcgencmd display_power` gibt es auf dieser Box nicht mehr — zwei Dienste
      laufen seitdem leer, einer schreibt 1,9 Fehlerzeilen pro Sekunde
    sev: high
    match: >-
      too many arguments|display_power|Command not registered|vc_gencmd_read_response|monitor\.json
    symptom: >-
      Das Knopflicht dimmt nicht, wenn der Bildschirm ausgeht. WLED wechselt die
      Helligkeit nicht. monitor.json steht seit Tagen still. Und `journalctl -u
      mupi_powerled` besteht zu 99,9 % aus derselben Zeile.
    cause: >-
      `vcgencmd display_power` ist auf diesem Pi (bcm2712, Kernel 6.18, vc4-drm)
      nicht mehr registriert. Der Aufruf endet mit rc=255 und schreibt ZWEI Zeilen
      nach stdout — beide Auswerter rechnen mit EINER Zahl.
    fix: >-
      Den Bildschirmzustand am Backlight ablesen statt am Firmware-Befehl:
      /sys/class/backlight/*/bl_power (0 = an, 4 = aus). Das Verzeichnis ist ohne
      sudo lesbar. In get_monitor.sh gibt es diesen Weg schon — er ist nur
      unerreichbar (siehe unten).
    tags:
    - falle
    - signature
    - pi5
    - knopflicht
    - dienste
    - protokolle
    - 2026
    source: Protokolle der Box gelesen, 07.08.2026 (192.168.178.169, nur lesend)
    related:
    - mupi-dienste-durchgesehen
    - mupi-journal-leer-ist-keine-antwort
    - protokolle-lesen-werkzeug
    - journal-ist-fluechtig-und-wird-geflutet
    body: |
      WAS DER BEFEHL WIRKLICH TUT (an der Box gemessen, als dietpi UND als root):

          $ vcgencmd display_power
          vc_gencmd_read_response returned -1
          error=1 error_msg="Command not registered"
          $ echo $?
          255

      Zwei Zeilen, kein Wert. Daran haengen zwei Auswerter, und beide gehen
      still kaputt — jeder auf seine eigene Art.

      ── 1. mupi_start_led.sh: LAUT KAPUTT ────────────────────────────────────

          displayState=`vcgencmd display_power | grep -o '.$'`   # Zeile 126
          if [ ${displayState} -eq 1 ] && ...                    # Zeile 128
          elif [ ${displayState} -eq 0 ] && ...                  # Zeile 138

      `grep -o '.$'` nimmt das letzte Zeichen JEDER Zeile, also zwei Zeichen:
      `1` und `"`. Unquotiert wird daraus `[ 1 " -eq 1 ]` — zwei Argumente zu
      viel:

          mupi_start_led.sh: line 128: [: too many arguments
          mupi_start_led.sh: line 138: [: too many arguments

      1764-mal in 925 Sekunden Laufzeit. Das sind 1,9 Zeilen pro Sekunde und
      rund 165.000 am Tag — allein aus diesem einen Skript.

      DIE FOLGE IST NICHT NUR LAERM. Beide Zweige sind IMMER falsch, also wird
      `OLD_STATE` nie fortgeschrieben und `ledDim` nie umgestellt. /tmp/.power_led
      steht dauerhaft auf dem Startwert:

          "led_dim_mode": 0     (= hell, seit dem Booten unveraendert)

      Das Dimmen des Knopflichts bei ausgehendem Bildschirm passiert nie. Der
      WLED-Helligkeitswechsel in denselben Zweigen passiert ebenfalls nie.

      ── 2. get_monitor.sh: STILL KAPUTT ──────────────────────────────────────

          MONITOR=$(sudo -H -u root bash -c "vcgencmd display_power")
          MONITOR=(${MONITOR##*=})
          POWER=-1
          if [ ${MONITOR} == "-1" ]; then
            POWER=$(cat /sys/class/backlight/*/bl_power)
          fi

      `${MONITOR##*=}` schneidet bis zum LETZTEN `=` ab. Aus
      `error_msg="Command not registered"` wird `"Command not registered"`, und
      die Klammern machen daraus ein Feld, dessen erstes Element `"Command`
      lautet. An der Box nachgestellt:

          MONITOR=["Command]

      Damit ist `[ ${MONITOR} == "-1" ]` falsch — und genau daran haengt der
      Rueckfall auf bl_power. DER RICHTIGE WEG STEHT SCHON DA UND IST TOTER CODE.
      Anschliessend sind auch `== "0"` und `== "1"` falsch, monitor.json wird
      nie geschrieben:

          -rw-r--r-- 1 dietpi dietpi 22 Aug  4 23:45 .../server/config/monitor.json
          { "monitor": "On" }

      Aenderungszeit 04.08. — drei Tage alt, waehrend die Schleife weiterlaeuft.
      Kein Fehler im Protokoll, nur 904 sudo-Aufrufe von `vcgencmd display_power`
      in 925 Sekunden (rund einer je Sekunde), jeder mit zwei pam-Zeilen.

      HEUTE HARMLOS, MORGEN NICHT: der Wert steht auf "On", und die Oberflaeche
      blockiert Eingaben nur bei "Off". Waere er beim Einfrieren auf "Off"
      gestanden, haette die Box jede Beruehrung ignoriert — ohne eine einzige
      Fehlermeldung.

      ── WAS DABEI IN ORDNUNG IST ─────────────────────────────────────────────

      Das Backlight selbst meldet sich sauber und ohne sudo:

          /sys/class/backlight/11-0045/bl_power = 0    (an)
          /sys/class/backlight/11-0045/brightness = 51

  - kind: knowhow
    id: journal-ist-fluechtig-und-wird-geflutet
    title: >-
      Das Journal liegt nur im Arbeitsspeicher — und drei Schleifen fluten es mit
      rund einer Million Zeilen am Tag
    sev: high
    match: >-
      journalctl|--list-boots|/run/log/journal|Storage=volatile|Protokoll voll
    symptom: >-
      `journalctl --list-boots` kennt genau EINEN Start. Nichts von gestern,
      nichts von letzter Woche. Wer nach der Ursache eines Haengers von vorgestern
      sucht, findet nichts — und haelt das faelschlich fuer „ruhig".
    cause: >-
      /var/log/journal gibt es nicht, nur /run/log/journal (tmpfs). Jeder Neustart
      loescht alles. Gleichzeitig erzeugen drei Schleifen sieben Zehntel des
      gesamten Aufkommens, sodass der Ringpuffer auch INNERHALB eines Starts
      schnell ueberschrieben wird.
    fix: >-
      Zwei Dinge, unabhaengig voneinander: (a) die Schleifen leiser machen —
      mupihat auf WARNING, get_monitor/mupi_start_led reparieren (siehe
      vcgencmd-display-power-am-pi5-weg); (b) erst DANACH ueber ein dauerhaftes
      Journal nachdenken. In dieser Reihenfolge — ein dauerhaftes Journal unter
      einer Million Zeilen am Tag schreibt die SD-Karte kaputt.
    tags:
    - knowhow
    - protokolle
    - dienste
    - sd-karte
    - 2026
    source: Protokolle der Box gelesen, 07.08.2026 (192.168.178.169, nur lesend)
    related:
    - mupi-journal-leer-ist-keine-antwort
    - vcgencmd-display-power-am-pi5-weg
    - protokolle-lesen-werkzeug
    - mupi-bt-reconnect
    - mupi-dienste-durchgesehen
    body: |
      GEMESSEN AM 07.08.2026, Fenster 925 Sekunden (15,4 Minuten Laufzeit):
      10.395 Journalzeilen. Das sind 11,2 Zeilen JE SEKUNDE, hochgerechnet rund
      970.000 am Tag.

      DIE FUENF HAEUFIGSTEN WORTLAUTE, nach Rumpf gruppiert:

          1764x  mupi_start_led.sh: line N: [: too many arguments
          1119x  pam_unix(sudo:session): session closed for user root
          1081x  pam_unix(sudo:session): session opened for user root
           904x  root : COMMAND=/usr/bin/bash -c 'vcgencmd display_power'
           184x  je INFO-Zeile von mupihat.py (14 Zeilen je Durchgang = 2576)

      Die drei Verursacher dahinter:

          mupi_powerled       1764 Zeilen   2 Fehler je Sekunde (Fehler, s.u.)
          mupi_check_monitor  2712 Zeilen   1 sudo je Sekunde + 2 pam-Zeilen
          mupi_hat            2576 Zeilen   14 INFO-Zeilen alle 5 Sekunden

      Zusammen 7052 von 10.395 — gut zwei Drittel. Mit mupi-network-info (420,
      alle 30 s ein Dienststart mit vier sudo-Aufrufen) sind es sieben Zehntel.

      ZWEI DAVON SIND SCHLECHTES PROTOKOLLIEREN, EINER IST EIN FEHLER:

      * mupihat.py schreibt jeden Messdurchgang komplett mit INFO — Schwellen,
        Temperaturen, Spannungen, Stroeme. Das sind Werte, die ohnehin in
        /tmp/mupihat.json stehen und dort abgeholt werden. Ins Journal gehoert
        davon nichts ausser Ausreissern. Der Loglevel gehoert auf WARNING.
      * get_monitor.sh braucht kein sudo. Es fragt `vcgencmd display_power`
        ueber `sudo -H -u root bash -c` ab — und laeuft selbst schon als root.
        Der bessere Wert steht ohne jede Berechtigung in
        /sys/class/backlight/*/bl_power.
      * mupi_start_led.sh ist schlicht kaputt, siehe den eigenen Eintrag.

      WARUM DAS MEHR IST ALS LAERM: das Journal liegt in /run/log/journal, also
      im Arbeitsspeicher. Es hat eine feste Groesse. Bei 11 Zeilen je Sekunde
      faellt alles heraus, was aelter als ein paar Stunden ist — auch dann, wenn
      gar nicht neu gestartet wurde. Die eine Zeile, die den naechtlichen Haenger
      erklaeren wuerde, ist am Morgen weg, bevor sie jemand liest.

      ── WAS AUSSERDEM SCHLEIFT, ABER LEISE ───────────────────────────────────

      mupibox-bt-reconnect laeuft ueber einen Timer mit OnUnitActiveSec=30s,
      also alle 30 Sekunden — dauerhaft, ohne Abbruchbedingung. Beide vertrauten
      Lautsprecher sind aus, also meldet er zweimal „nicht erreichbar (aus?)"
      und bluetoothd protokolliert dazu:

          34x  Unable to get Hands-Free Voice gateway SDP record: Host is down
          15x  avdtp_connect_cb() connect to ..: Device or resource busy
           8x  Unable to get ... SDP record: Device or resource busy

      Das ist eine Schleife, die etwas versucht, was erst gelingen KANN, wenn
      jemand einen Lautsprecher einschaltet. Sie stoert nichts, aber sie sollte
      nach einigen erfolglosen Laeufen den Takt strecken, statt bis zum
      Ausschalten der Box alle 30 Sekunden das Funkmodul zu belegen.

      NEBENBEFUND ZU DEN BT-ADAPTERN: die Box hat DREI Controller
      (00:15:83:F9:C5:4F „mupibox #1" [default], 08:BF:B8:56:CE:44 „mupibox #2",
      88:A2:9E:48:F6:50 „mupibox"). Jeder hat seine eigene Geraeteliste.
      `bluetoothctl devices Trusted` auf dem Standardadapter zeigt „Teufel
      ROCKSTER Cross" und „Smart Soundbar 10158750" — bt-reconnect.py arbeitet
      dagegen mit „Teufel ROCKSTER Cross" und „小米蓝牙音箱". Es sieht also einen
      ANDEREN Adapter als der Standard. Das ist beabsichtigt (das Skript waehlt
      ausdruecklich aus), aber es heisst: wer mit `bluetoothctl` von Hand
      nachsieht, sieht nicht das, womit der Dienst arbeitet.

  - kind: falle
    id: uhr-springt-beim-booten-journal-zeit-luegt
    title: >-
      Laufzeit und Journal widersprechen sich um eine halbe Stunde — die Uhr
      springt beim Booten, und alle fruehen Zeitstempel sind falsch
    sev: mid
    match: >-
      Time jumped backwards|timestamp .* in the future|uptime|list-boots|timesyncd
    symptom: >-
      `journalctl --list-boots` sagt, der Start war um 23:00:01. `uptime` sagt,
      die Box laeuft seit 15 Minuten. Es ist 23:43. Beides kann nicht stimmen.
    cause: >-
      Beim Booten steht die Uhr auf einem alten Wert, bis systemd-timesyncd das
      Netz hat. Alle Meldungen bis dahin bekommen den falschen Zeitstempel;
      journald merkt es und rotiert („Time jumped backwards, rotating").
    fix: >-
      Fuer Zeitrechnungen NICHT den Zeitstempel des ersten Journal-Eintrags
      nehmen, sondern /proc/uptime. Bei Ratenangaben („X-mal je Sekunde") ist
      das der Unterschied zwischen richtig und um den Faktor 3 zu niedrig.
    tags:
    - falle
    - protokolle
    - zeit
    - booten
    - 2026
    source: Protokolle der Box gelesen, 07.08.2026 (192.168.178.169, nur lesend)
    related:
    - journal-ist-fluechtig-und-wird-geflutet
    - protokolle-lesen-werkzeug
    body: |
      AN DER BOX GEMESSEN, alles im selben Augenblick:

          journalctl --list-boots  ->  Start Fri 2026-08-07 23:00:01 CEST
          date                     ->  Fri 2026-08-07 23:43:34 CEST
          /proc/uptime             ->  925 Sekunden

      23:43:34 minus 925 s ergibt 23:28:09 — und tatsaechlich springt bei
      23:28:37 alles an, was ein Netz braucht (pipewire, librespot,
      mupi-network-info). Die 43 Minuten zwischen „23:00:01" und 23:28 hat es
      nie gegeben. Alle 690 fruehen Zeilen tragen denselben Stempel 23:00:01.

      DIE SPUREN IM PROTOKOLL, die das bestaetigen:

          systemd-journald[141]: Time jumped backwards, rotating.        (2x)
          phpsessionclean.timer: Not using persistent file timestamp
                                 Fri ... CEST as it is in the future.

      Eine RTC ist vorhanden (/dev/rtc0) und geht jetzt richtig; `timedatectl`
      meldet „System clock synchronized: yes", aber „NTP service: inactive"
      (es synchronisiert timesyncd, nicht ntpd). Der Sprung passiert trotzdem,
      weil die Uhr erst NACH dem Start des Journals gestellt wird.

      WARUM DAS BEIM PROTOKOLLLESEN WEHTUT: wer die Haeufigkeit einer Meldung
      auf den scheinbaren Startzeitpunkt bezieht, rechnet mit 43 Minuten statt
      mit 15 und untertreibt jede Rate um den Faktor 2,8. Genau daran waere die
      Aussage „1764 Fehler in 43 Minuten" (harmlos klingend) statt „zwei Fehler
      JE SEKUNDE" (offensichtlich kaputt) gescheitert.

      GEGENPROBE, die den Verdacht bestaetigt hat: die Schleife in
      mupi_start_led.sh hat `sleep 1` und schreibt zwei Fehlerzeilen je
      Durchgang. 1764 Zeilen / 925 s = 1,9 je Sekunde — passt. Geteilt durch
      2613 s waeren es 0,68, was zu keiner Schleife mit `sleep 1` passt.

  - kind: howto
    id: protokolle-lesen-werkzeug
    title: >-
      tools/protokolle-lesen.py — die Protokolle der Box ZAEHLEN statt aufzaehlen
    tags:
    - howto
    - werkzeug
    - protokolle
    - 2026
    source: Protokolle der Box gelesen, 07.08.2026
    related:
    - werkzeuge-benutzen
    - arbeitsweise-werkzeug-statt-wegwerfbefehl
    - journal-ist-fluechtig-und-wird-geflutet
    - mupi-journal-leer-ist-keine-antwort
    body: |
      „Es gibt Warnungen" ist kein Befund. „1764-mal dieselbe Warnung in 15
      Minuten" ist einer. Das Werkzeug macht aus dem einen das andere: es holt
      die Protokolle lesend von der Box, ersetzt Zahlen, Zeiten, MACs, IPs und
      Geraetepfade durch Platzhalter, gruppiert nach dem verbleibenden Wortlaut
      und nennt Anzahl und Zeitspanne.

          python3 tools/protokolle-lesen.py                  # Ueberblick
          python3 tools/protokolle-lesen.py --dienst mupi_powerled
          python3 tools/protokolle-lesen.py --strom          # Spannung, Waerme
          python3 tools/protokolle-lesen.py --dateien        # /var/log/...
          python3 tools/protokolle-lesen.py --roh 'mupihat'  # Volltext

      NUR LESEN. Es ruft journalctl, dmesg, systemctl show und cat auf. Es
      loescht nichts, rotiert nichts, startet nichts neu.

      `--strom` beantwortet die Frage, die bei einem Pi als erste zu stellen
      ist — „manchmal haengt sie" ist meistens Unterspannung:

          vcgencmd get_throttled     Bit0  gerade unterspannt
                                     Bit2  gerade gedrosselt
                                     Bit16 seit dem Start unterspannt gewesen
                                     Bit18 seit dem Start gedrosselt gewesen

      STAND 07.08.2026 an dieser Box: `throttled=0x0`, 50,5 °C, HAT-Ladechip
      29 °C, „Temperature Regulation Status: Normal". Kein einziger Treffer auf
      undervoltage/throttl/thermal in dmesg oder Journal. Die Stromversorgung
      ist NICHT die Ursache von irgendetwas — das ist ein Ergebnis, kein
      Nicht-Ergebnis, und es spart beim naechsten Haenger einen halben Abend.

      Dabei aufgefallen: die Box lief in dieser Nacht am AKKU, nicht am Netz
      (/tmp/mupihat.json: `"Vbus": 0`, `"Ibat": -498`, `"Charger_Status": "Not
      Charging"`, SOC 100 %, 8015 mV). Das ist kein Fehler, aber es heisst: ohne
      Netzteil laeuft sie irgendwann leer, und niemand bekommt es mit.

      ZWEI FALLEN, die im Werkzeug schon beruecksichtigt sind:

      * Als `dietpi` sieht man das System-Journal nicht („No journal files were
        opened due to insufficient permissions") — es braucht `sudo -n`.
        Siehe mupi-journal-leer-ist-keine-antwort.
      * Die Zeitspanne wird ueber /proc/uptime bestimmt, nicht ueber den ersten
        Journal-Eintrag. Siehe uhr-springt-beim-booten-journal-zeit-luegt.
  - kind: falle
    id: mupi-splash-16bpp
    title: "Kein Startbild auf dem Pi 4: der Framebuffer hat 16 bpp, das Skript wollte 32"
    tags: [splash, pi4, framebuffer, installer]
    body: |
      SYMPTOM: nach der Installation kein Startbild. Der Dienst ist
      `enabled`, `is-active` sagt `inactive`, `Result=success` — es sieht
      nach „laeuft halt kurz und ist fertig" aus.

      URSACHE (nur mit `sudo journalctl` sichtbar, siehe
      mupi-journal-leer-ist-keine-antwort):

          Boot-Animation nicht moeglich: nur 32 bpp unterstuetzt, hier 16

      **Der Pi 4 faehrt seinen Framebuffer mit 16 bpp hoch, der Pi 5 mit 32.**
      Das Skript stieg deshalb sofort aus — sauber, mit Begruendung, und
      trotzdem unsichtbar.

      BEHOBEN in `remote-step-installer/tools/mupibox-boot-splash.py`: intern wird weiter mit 32 bpp
      (BGRA) gezeichnet, und beim Uebertragen einmal nach RGB565 gewandelt
      (`nach565`). Den ganzen Zeichencode je Farbtiefe umzubauen waere viel
      Flaeche fuer wenig Gewinn.

      RGB565 heisst: 5 Bit Rot, 6 Bit Gruen, 5 Bit Blau, little endian. Das
      zusaetzliche Bit gehoert zu Gruen, weil das Auge dort am genauesten ist
      — vertauscht man das, sieht man nur einen leichten Farbstich, den
      niemand einem Bitmuster zuordnet. Deshalb steht die Umrechnung als
      EIGENE Funktion da und hat 11 Pruefungen
      (`tests/splash565_test.py`): ein Startbild laesst sich schlecht testen,
      eine Farbumrechnung sehr gut.

      AM GERAET BELEGT (Pi 4, 2026-07-29): vorher sofortiger Ausstieg,
      danach 26 s Laufzeit durch den ganzen Startvorgang, kein Fehler.

      NEBENBEI: der Schritt `boot-animation` ist im App-Rezept als `[opt]`
      gefuehrt. Wer die Schritte einzeln faehrt, laesst ihn leicht aus — dann
      fehlt das Startbild, ohne dass irgendetwas kaputt waere.
    source:
      - "Session 2026-07-29, am Pi 4 gefunden und behoben"

  - kind: optim
    id: mupi-boot-32s-auf-13s
    title: "Boot 32 s -> 13 s: die Wartezeit war der DHCP-Rueckfall, NICHT das WLAN"
    tags: [boot, perf, wlan, dhcp, pi4]
    body: |
      AM GERAET GEMESSEN (Pi 4, Trixie, 2026-07-29):

          vorher   1,5 s Kern + 30,3 s Userspace = 31,8 s
          nachher  1,5 s Kern + 11,7 s Userspace = 13,2 s
          ifup@wlan0.service:  23,1 s -> 5,0 s

      ── DER BEFUND, DER ALLES ERKLAERT ──────────────────────────────────

      `systemd-analyze critical-chain`:

          graphical.target @30,2s
          └─pm2-dietpi.service @26,9s
            └─network.target @26,8s
              └─ifup@wlan0.service +23,1s

      Drei Viertel des Starts hingen am WLAN. Der naheliegende Schluss waere
      „das Funknetz ist langsam" — und der ist FALSCH. Der Zeitverlauf:

          5,96 s  dhclient startet und fragt nach einer Adresse
          6,08 s  DHCPREQUEST … ins Leere
          8,34 s  wpa_supplicant faengt ERST JETZT an zu assoziieren
          8,52 s  verbunden — ab hier ginge alles
         16,85 s  DHCPDISCOVER (Intervall 11)  -> keine Antwort
         27,96 s  DHCPDISCOVER               -> Antwort nach 30 ms
         28,28 s  Adresse da

      Das Netz stand bei 8,5 s, die Adresse kam bei 28,3 s. Zwanzig Sekunden
      steckten in der Rueckfallstaffel von dhclient, weil seine ersten
      Anfragen 2,4 s VOR der Assoziation rausgingen.

      ── DIE BEHEBUNG: DREI ZEILEN ───────────────────────────────────────

      In `/etc/dhcp/dhclient.conf`:

          initial-interval 1;
          backoff-cutoff 2;
          retry 5;

      Damit wird die Anfrage direkt nach der Assoziation nachgeholt statt
      nach 8 und 11 Sekunden. Im Rezept als Schritt `schneller-starten`.

      Dazu drei Dienste, die diese Box nicht braucht (zusammen ~1,5 s):
      `rpi-eeprom-update`, `keyboard-setup`, `console-setup`.

      ── WAS NICHT FUNKTIONIERT HAT (ehrlich festgehalten) ───────────────

      `pm2-dietpi.service` vom Netz loesen: eine Ergaenzung mit `After=`
      (Zuruecksetzer) + `After=local-fs.target` WIRD gelesen (`systemctl cat`
      zeigt sie, local-fs kommt an) — aber `network.target` bleibt in der
      effektiven Liste, und die kritische Kette war nach einem Neustart
      UNVERAENDERT. Die wirkungslose Datei wurde wieder entfernt: lieber
      nichts als etwas, das spaeter jemand fuer die Loesung haelt.

      Teilerklaerung: `mupibox-alsa-init.service` setzt sich selbst
      `Before=pm2-dietpi.service` — eine Ordnung von der ANDEREN Seite laesst
      sich im eigenen Unit nicht zuruecksetzen. Woher `network.target` kommt,
      ist damit aber noch nicht geklaert.

      ── VORGEHEN, DAS SICH BEWAEHRT HAT ─────────────────────────────────

      EINE Aenderung, dann neu starten, dann messen. Zwei Aenderungen auf
      einmal und man weiss hinterher nicht, welche gewirkt hat — beim
      zweiten Versuch (pm2) waere sonst der DHCP-Gewinn faelschlich ihm
      zugeschrieben worden.
    source:
      - "Session 2026-07-29, drei Messneustarts am Pi 4"

  - kind: falle
    id: mupi-weiss-beim-uebergang
    title: "Weisser Blitz zwischen Startbild und Oberflaeche: es ist der KMS-Moduswechsel"
    tags: [kiosk, x11, dsi, boot, pi4]
    body: |
      EINGEKREIST am 2026-07-29 (Pi 4, 800x480 DSI). FUENF Erklaerungen wurden
      geprueft und WIDERLEGT — wer das nicht weiss, probiert sie alle nochmal:

        1. X-Wurzelfenster            `-background none` gesetzt -> Weiss blieb
        2. Chromiums Startfarbe       `--default-background-color=000000ff`,
                                      in der laufenden Befehlszeile
                                      nachgeprueft -> Weiss blieb
        3. Seitenfarbe                Inline-Style + `color-scheme: dark light`
                                      in index.html, ausgeliefert und
                                      nachgeprueft -> Weiss blieb
        4. Terminalwechsel            Konsole UND X auf tty2, kein Wechsel
                                      mehr -> Weiss blieb
        5. Startbild                  ganz abgeschaltet, Journal belegt „lief
                                      nicht" -> Weiss blieb

      DER ENTSCHEIDENDE TEST: die Seite ueber `/api/oberflaeche/neuladen` neu
      laden lassen, waehrend X und Chromium DURCHLAUFEN. Ergebnis: sauber,
      kein Weiss. Damit ist das erste Malen der Seite ausgeschlossen und nur
      noch der START von X uebrig — der KMS-Moduswechsel. Ein DSI-Panel ohne
      Signal zeigt oft Weiss.

      NICHT BEHOBEN — und Plymouth hat es AUCH NICHT geloest. Am Geraet
      durchprobiert (2026-07-29), alle drei Wege:

        1. Plymouth laufen lassen, X daneben starten
           -> plymouthd haelt /dev/dri/card1 als MASTER (fuser: F...m),
              X bekommt die Karte nicht, Xorg.0.log bleibt 0 Byte,
              die Box startet OHNE BILD.
        2. `plymouth deactivate` vor startx
           -> gibt den Master NICHT frei. Gleiches Ergebnis wie 1.
              (Ray Strode beschreibt genau diesen Weg; auf Plymouth 24.004
              unter Trixie am Pi 4 traegt er nicht.)
        3. `plymouth quit --retain-splash` VOR startx
           -> mechanisch KORREKT: X startet (Xorg.0.log 17890 Byte),
              Plymouth beendet sich, Fenster steht, Bild bleibt stehen.
              UND DAS WEISS KOMMT TROTZDEM.

      Damit ist es beantwortet: der Moduswechsel liegt UNTERHALB dessen, was
      Plymouth, X oder Chromium beeinflussen koennen. Kein Programm kann ihn
      ueberdecken — auch keines, das extra dafuer gebaut wurde.

      Plymouth kostete dabei ~2 s Startzeit und wurde wieder abgebaut.
      STOLPERSTEIN beim Rueckbau: das PAKET bringt seinen eigenen Startverweis
      mit (`/usr/lib/systemd/system/sysinit.target.wants/plymouth-start.service`).
      Ihn unter `/etc` zu entfernen genuegt NICHT — plymouthd startete weiter
      und blockierte X erneut. Loesung: `systemctl mask plymouth-start.service`.

      ── WARUM DER PI 5 DAS NICHT ZEIGT (Vermutung mit Messwert) ─────────

          Pi 5:  card0 -> drm-rp1-dsi   <- das Panel
                 card2 -> vc4-drm       <- hier laeuft X
          Pi 4:  card1 -> vc4-drm       <- Panel UND X, DIESELBE Karte

      Am Pi 5 haengt DSI am RP1 als EIGENE Karte. X setzt den Modus auf vc4 —
      das Panel haengt woanders und laeuft weiter, verliert sein Signal also
      nie. Dazu passt ein Befund, der dort als PROBLEM auffiel: der Schirm
      zeigte die Konsole statt X, weil die Kopie zwischen den Karten nicht
      ankam. Aergerlich, aber es belegt, dass das Panel DURCHGEHEND etwas
      zeigte. Nicht gegengetestet (kein Pi 5 verfuegbar) — es koennte auch
      nur schneller und damit unsichtbar sein.

      ── STAND ──────────────────────────────────────────────────────────

      Hinnehmbar. Mit dem eigenen, nativ zeichnenden Startbild ist der
      Uebergang kurz; das Restweiss wurde als in Ordnung befunden. Wer es
      doch angehen will, muss unterhalb von X ansetzen (Panel-Timing,
      Treiber) — nicht bei Farbwerten, Terminalwahl oder Splash-Programmen.
      Die sind ALLE durch.

      ── ZWEI FEHLVERSUCHE, DIE ZEIT GEKOSTET HABEN ──────────────────────

      **X auf tty1 legen** (statt tty2), um den Terminalwechsel zu vermeiden:
      machte es SCHLIMMER. X lag dann auf tty1, das aktive Terminal blieb 2 —
      X war gar nicht sichtbar. Richtig herum ist es, die KONSOLE zu X zu
      holen (`console=tty2` in cmdline.txt), nicht X zur Konsole.

      **`Conflicts=getty@tty1.service` am Startbild**, damit es vor X endet:
      hat die Box LAHMGELEGT. DietPi startet den Kiosk ueber die
      AUTOANMELDUNG auf tty1 — getty abzuschiessen heisst, den Kiosk
      abzuschiessen. `Before=graphical.target` kam dazu und liess das Ziel auf
      das Startbild warten. SSH lief weiter, darueber war es zu retten.
      MERKSATZ: tty1 nicht anfassen.

      ── UND EIN MESSFEHLER ──────────────────────────────────────────────

      Ein `pkill -f 'chromium --fast'` traf nur die xinit-Zeile, nicht den
      Browser. Danach standen 7 Chromium-Prozesse und ein Fenster da — was
      wie ein gelungener Neustart aussah. Es war aber gar nichts passiert;
      `ps -eo pid,etimes` zeigte Laufzeiten seit dem Booten. Aufgefallen ist
      es nur, weil der Mensch davor sagte „scheinbar nichts passiert".

      REGEL: nach einer Aktion pruefen, ob die AKTION stattgefunden hat (PID,
      Laufzeit), nicht nur, ob der Zustand danach plausibel aussieht.
    source:
      - "Session 2026-07-29, am Geraet eingekreist"

  - kind: optim
    id: mupi-startzeiten-gemessen
    title: "Startzeiten und Stellschrauben: was WIRKLICH bremst (Pi 4 und Pi 5 gemessen)"
    tags: [boot, perf, messwerte, pi4, pi5, systemd]
    body: |
      ALLE ZAHLEN AM GERAET GEMESSEN (2026-07-29), jede Aenderung EINZELN mit
      Neustart gegengeprueft. Streuung zwischen zwei gleichen Startes: rund
      1 s - Unterschiede darunter sind kein Befund.

      ── ERGEBNIS ────────────────────────────────────────────────────────

                          Pi 4 (.99)        Pi 5 (.48)
          Boot            31,8 -> ~15 s     18,2 -> 13,4 s
          App bereit      ~23,5 -> 3,3 s    -> 3,3 s
          X startet       23,7 -> 13,8 s    15,5 s
          Fenster steht   34,9 -> 25,1 s    21,3 s

      ── DIE STELLSCHRAUBEN, nach Wirkung ────────────────────────────────

      **1. DHCP-Rueckfallzeit (Pi 4: -18 s).** Der groesste Einzelposten.
      Nicht das WLAN war langsam (verbunden nach 8,5 s), sondern dhclient
      startete 2,4 s VOR der Assoziation, seine ersten Anfragen gingen ins
      Leere, und dann griff die Rueckfallstaffel mit 8 und 11 Sekunden - die
      Adresse kam erst bei 28,3 s. Siehe mupi-boot-32s-auf-13s.

          /etc/dhcp/dhclient.conf:
          initial-interval 1;  backoff-cutoff 2;  retry 5;

      **2. systemd statt pm2 (beide: App 20 s frueher bereit).** Der Kiosk
      wartet in seinem Autostart auf localhost:8200, BEVOR er X startet - die
      Startzeit der App IST also die Zeit bis zum Bild. Mit pm2 war sie bei
      ~23,5 s bereit, mit schlichten systemd-Units bei 3,3 s.
      WICHTIG: kein `After=network.target` - die App bedient localhost.
      STOLPERSTEIN: pm2 muss WIRKLICH weg, sein Hauptprozess haelt sonst die
      Ports und die Units laufen in eine EADDRINUSE-Schleife. `pm2 stop` und
      sogar `pm2 kill` per `su - dietpi` reichten nicht; der God-Prozess lief
      weiter und musste direkt beendet werden. Vorher `dump.pm2` sichern.

      **3. Warteschleife des Kiosk (Pi 4: -5,4 s).** Sie probierte erst HTTPS
      mit 2 s Zeitlimit, dann HTTP, dann 1 s Schlaf. HTTP zuerst und 0,2 s
      Takt: X ab 18,3 statt 23,7 s.

      **4. librespot von der kritischen Kette (Pi 5: -5 s).** Es stand mit
      5-6 s auf der Kette und graphical.target wartete. Die Zeit ging in ein
      `ExecStartPre`, das auf eine Tonsenke wartet (bis 90 s). Als
      ExecStartPre gilt der Dienst als "startet noch", und weil er
      WantedBy=multi-user.target ist, wartet das System mit. Das Warten IN
      den ExecStart verlegen - dann gilt er sofort als gestartet.

      **5. PHP abschalten (Pi 5: 88 MB, Pi 4: 73 MB).** Bei MUPI_ADMIN=node
      liefert lighttpd nichts mehr aus und laeuft trotzdem. `lighttpd`
      abzuschalten GENUEGT NICHT - `php8.4-fpm` ist ein EIGENER Dienst und
      lief mit 5 Prozessen weiter. Vorher pruefen, dass die Node-Verwaltung
      mit 200 antwortet, sonst steht die Box ohne jede Verwaltung da.

      **6. Drei unnoetige Dienste (~1,5 s):** rpi-eeprom-update,
      keyboard-setup, console-setup.

      ── WAS NICHTS GEBRACHT HAT (damit es niemand nochmal probiert) ──────

      * `pm2` per systemd-Ergaenzung vom Netz loesen: `After=` zuruecksetzen
        WIRKTE NICHT, die kritische Kette blieb nach einem Neustart
        unveraendert. Datei wieder entfernt.
      * `video=DSI-1:800x480-32@60` (Farbtiefe der Konsole an X angleichen):
        Luecke 11,10 s gegen 10,49 s - im Rauschen. Wieder entfernt.
      * X auf tty1 statt tty2: MACHTE ES SCHLIMMER (X lag auf tty1, aktives
        Terminal blieb 2 - X war unsichtbar).

      ── DER REST ────────────────────────────────────────────────────────

      Nach allem haengen BEIDE Boxen an derselben Stelle: `ifup@wlan0`, und
      das schwankt bei identischer Konfiguration zwischen 3,8 und 10,1 s.
      Das ist die Funkanmeldung selbst. Chromium braucht danach noch einmal
      rund 11 s bis zum Fenster - das ist der Browser, nicht die Kette.
    source:
      - "Session 2026-07-29, zwei Boxen, je fuenf bis sieben Messneustarts"

  - kind: falle
    id: mupi-audio-guard-stumm
    title: "Rezeptfehler: der Verstaerker blieb stumm, weil eine Pruefung sich selbst traf"
    tags: [audio, rezept, mupihat, falle]
    body: |
      GEFUNDEN 2026-07-29 beim Vergleich Pi 4 gegen Pi 5. Der Audio-Schritt
      im System-Rezept machte das hier:

          sed -i 's/^dtparam=audio=on/#dtparam=audio=on/' "$CFG"
          grep -qE '^#?dtparam=audio=' "$CFG" || echo 'dtparam=audio=off' >> "$CFG"

      Das `#?` in der Pruefung trifft die Zeile, die eine Zeile vorher SELBST
      auskommentiert wurde. Also war die Bedingung immer erfuellt und
      `dtparam=audio=off` wurde NIE angehaengt. Uebrig blieb die
      Firmware-Vorgabe - und die ist auf dem Pi 4 "an".

      AM GERAET SICHTBAR:

          vorher:  card 0: Headphones          <- Onboard aktiv
                   Vorgabesenke mailbox.stereo-fallback
          nachher: card 0: MAX98357A
                   Vorgabesenke soc_sound.stereo-fallback

      Der Ton ging also an die Onboard-Klinke statt an den
      MuPiHAT-Verstaerker. Das betraf JEDE Installation, nicht nur diese Box.

      BEHOBEN: ausdruecklich auf `^dtparam=audio=off` pruefen, nicht auf
      `^#?dtparam=audio=`.

      MERKSATZ: eine Pruefung, die eine Zeile davor selbst erzeugt hat, ist
      keine Pruefung. Dasselbe Muster wie der Pipe-Rueckfall, der nie feuert
      (mupi-kerntests-node26) - beides sind Wachen, die sich selbst
      zufriedenstellen.
    source:
      - "Session 2026-07-29, am Geraet belegt und im Rezept behoben"

  - kind: config
    id: mupi-pi4-pi5-configs-angleichen
    title: "Pi 4 und Pi 5 gleich konfigurieren - und was man NICHT angleichen darf"
    tags: [config, pi4, pi5, rezept]
    body: |
      Verglichen am Geraet (2026-07-29), Ziel: beide Boxen aus DEMSELBEN
      Rezept gleich erzeugen.

      ── ANGLEICHEN ──────────────────────────────────────────────────────

          dtparam=audio      on  -> off    (sonst stumm, siehe
                                            mupi-audio-guard-stumm)
          gpu_mem_*          76  -> 16     unter vollem KMS verwaltet der
                                           vc4-Treiber den Speicher selbst;
                                           76 MB waren verschenkt
          temp_limit         75  -> 65     geschlossenes Gehaeuse im
                                           Kinderzimmer
          console=           tty2 -> tty1  (tty2 war ein Versuch gegen das
                                            weisse Aufblitzen, brachte nichts)

      Danach unterscheiden sich die beiden config.txt nur noch in der
      REIHENFOLGE einer identischen Zeile.

      ── NIEMALS ANGLEICHEN ──────────────────────────────────────────────

          root=PARTUUID      eigene Karte je Box
          serielle Konsole   Pi 4 ttyS0, Pi 5 ttyAMA0 - ANDERER UART

      Wer die kopiert, macht die Box unbootbar. Sie sehen wie
      Konfigurationsunterschiede aus und sind Hardware.

      ── NOCH NICHT ANGEGLICHEN (Stand 2026-07-29) ───────────────────────

      Die TONKETTE unterscheidet sich grundlegend:

                          Pi 4              Pi 5
          Tonserver       pulseaudio        pipewire
          pipewire-Paket  nein              ja
          /etc/asound.conf vorhanden        fehlt
          librespot       disabled          enabled
          Touch-Bruecke   nicht aktiv       aktiv

      WOHER DER UNTERSCHIED KOMMT: PipeWire kam mit dem UMBAU DES PI 5, nicht
      aus der Pi-4-Linie. Der Pi 4 faehrt also den urspruenglichen Tonweg
      (pulseaudio + /etc/asound.conf), der Pi 5 den neueren. Es ist KEIN
      vergessenes Nachziehen, sondern zwei Staende nebeneinander.

      Damit haengt auch das Lautstaerkemodell am Pi-5-Umbau, nicht am Pi 4
      (siehe mupi-vorlesen-lautstaerke: Lautstaerke ist auf dieser Box nichts
      Globales - librespot, mpv und der Browser haben je einen eigenen
      Regler). VOR einer Umstellung des Pi 4 ist zu entscheiden, WIE die
      Lautstaerke kuenftig geregelt wird - nicht danach. Solange das offen
      ist, sind zwei Tonwege nebeneinander der ehrlichere Zustand als ein
      halb umgestellter.

      Die Touch-Bruecke ist unkritisch: sie haelt sich selbst raus, wenn der
      Kerneltreiber eine Unterbrechungsleitung hat.
    source:
      - "Session 2026-07-29, beide Boxen verglichen"

  - kind: falle
    id: mupi-rsi-falsche-box
    title: "rsi lief an der falschen Box: `connect` legt keinen neuen Tunnel an"
    tags: [installer, werkzeug, falle, diagnose]
    body: |
      ZWEIMAL am 2026-07-29 passiert, beide Male unbemerkt.

      `rsi` spricht IMMER mit `localhost:8099` - also mit der Box, auf die der
      Tunnel gerade zeigt. `./connect <adresse>` legt aber KEINEN neuen Tunnel
      an, wenn schon einer laeuft. Es meldet freundlich

          Port 8099: vorhandener Tunnel funktioniert - wird genutzt

      und benutzt den ALTEN, egal zu welcher Box er fuehrt. Ein
      `./connect 192.168.178.99` direkt vor `./rsi run …` ist also KEINE
      Garantie, dass der Schritt am Pi 4 landet.

      AUFGEFALLEN ist es nur, weil ein Schritt „steht schon richtig" meldete,
      waehrend die Datei auf der gemeinten Box unveraendert war. Beim zweiten
      Mal, weil das Ziel mitgedruckt wurde.

      ── DAS WERKZEUG DAGEGEN ────────────────────────────────────────────

      `./rsi-auf <adresse> <rsi-argumente>` raeumt den Tunnel weg, verbindet
      neu, LIEST DAS MODELL und bricht ab, wenn es nicht passt:

          MUPI_ERWARTET="Pi 4" ./rsi-auf 192.168.178.99 run audio --recipe …

      Lieber gar nicht laufen als am falschen Rechner. Zusammen mit
      `tools/welche-box.sh` (MuPiBox-Projekt) ist das die Antwort auf
      „checke immer, auf welchem System du bist".

      Verwandt: derselbe Tag, andere Ebene - der NAME `mupibox` zeigte
      zweimal auf verschiedene Rechner, und ein VPN mit ueberlappendem Netz
      leitete Pakete an die falsche Adresse (feedback-Notiz im Memory,
      mupi-andere-zielhardware).
    source:
      - "Session 2026-07-29"

  - kind: offen
    id: mupi-kiosk-als-dietpi
    title: "OFFEN: Kiosk als dietpi statt root - Autologin allein genuegt nicht"
    tags: [kiosk, x11, rechte, offen, pi4, pi5]
    body: |
      BEFUND (2026-07-29): die beiden Boxen starten den Kiosk als
      VERSCHIEDENE Nutzer.

          Pi 4   Chromium als root    (agetty -a root   -> xinit)
          Pi 5   Chromium als dietpi  (agetty -a dietpi -> startx)

      Der Autostart waehlt selbst: `[ "$USER" = root ] || STARTX=startx`.
      Die Wahrheit steht in
      `/etc/systemd/system/getty@tty1.service.d/dietpi-autologin.conf` -
      NICHT in dietpi.txt, dort stand auf BEIDEN Boxen
      `AUTO_SETUP_AUTOSTART_LOGIN_USER=root`, gelebt wurde es verschieden.

      ── WARUM ES ZAEHLT ─────────────────────────────────────────────────

      Chromium sucht sein Widevine im HOME des laufenden Nutzers. Liegt es
      unter /home/dietpi und laeuft der Browser als root, bleibt das
      Spotify-Web-SDK auf "not_loaded" (siehe
      widevine-gehoert-dem-kiosk-nutzer). Und ein Browser als root ist auf
      einer Kinderbox unnoetig.

      ── WAS NICHT GENUEGT HAT ───────────────────────────────────────────

      Am Pi 4 versucht: Autologin auf dietpi, dazu

        * `/etc/X11/Xwrapper.config` mit `allowed_users=anybody` und
          `needs_root_rights=yes` (der Pi 5 hat sie, der Pi 4 hatte keine)
        * Gruppen `tty` und `input` ergaenzt (der Pi 5 hat sie)

      ERGEBNIS: dietpi war auf tty1 angemeldet, `getty@tty1` aktiv - aber der
      Autostart wurde fuer ihn NIE aufgerufen. Kein Protokolleintrag,
      `Xorg.0.log` 0 Byte, kein Bild. Zurueck auf `-a root`, dann lief es
      sofort wieder.

      ── WO WEITERZUSUCHEN IST ───────────────────────────────────────────

      Der Aufrufhaken fuer `chromium-autostart.sh` liegt NICHT in
      `/home/dietpi/.bashrc` oder `.profile` - auf keiner der beiden Boxen.
      Auf dem Pi 5 funktioniert es trotzdem; wo dort der Haken sitzt, ist
      ungeklaert. Naechster Schritt waere, auf dem Pi 5 die Anmeldung
      mitzuschneiden (`bash -x`, oder pam/systemd-logind-Seite ansehen).

      ── STAND ──────────────────────────────────────────────────────────

      Der Rezeptschritt `kiosk-nutzer` wurde deshalb WIEDER ENTFERNT - er
      haette eine frische SD ohne Bild hinterlassen. Der Entwurf liegt als
      `recipes/ENTWURF-kiosk-nutzer.yaml` daneben, ausdruecklich nicht
      eingebunden. Ein halb funktionierender Schritt in einem Deployment ist
      schlimmer als keiner.
    source:
      - "Session 2026-07-29, am Geraet versucht und zurueckgedreht"

  - kind: falle
    id: mupi-spotify-drei-ursachen
    title: "»Spotify geht nicht mehr«: DREI Ursachen uebereinander - und jede sieht aus wie die andere"
    tags: [spotify, diagnose, deploy, pipewire, falle]
    body: |
      AM GERAET ENTWIRRT (Pi 5, 2026-07-29). Eine Stunde Suche, weil drei
      unabhaengige Ursachen dasselbe Bild ergaben: "Verbindung zu Spotify
      klappt nicht, Flip-Liste bleibt leer".

      ── URSACHE 1: Staende passen nicht zusammen ────────────────────────

      Oberflaeche und server.js stammten aus VERSCHIEDENEN Baustaenden - die
      Oberflaeche rief `/api/spotify/web/...`, das Backend kannte den Weg
      nicht. Der SPA-Auffang antwortete mit der HTML-Seite bzw. 404, ALLE
      Dienste waren dabei gruen.

      Wie es dazu kam: der rsi-Tunnel zeigte auf die falsche Box
      (mupi-rsi-falsche-box), dadurch landete ein Pi-4-Deploy auf dem Pi 5.
      Beim "Reparieren" wurde dann ERST das Backend zurueckgedreht (Frontend
      blieb neu), spaeter wieder vor - zweimal auseinandergerissen.

      SIGNATUR: ein /api-Weg, der mit `text/html` antwortet, existiert im
      Backend nicht. REGEL: Frontend und Backend IMMER zusammen ausliefern -
      ein deploy.zip = ein Stand.

      WERKZEUG: `mupi-check standpasst` liest die /api-Wege aus den
      AUSGELIEFERTEN www-Buendeln und fragt jeden beim Backend an. Achtung
      beim Muster: `(?<![a-zA-Z0-9.])/api/...`, sonst greift es mitten in
      fremde Adressen (accounts.spotify.com/api/token -> Fehlalarm, passiert).

      ── URSACHE 2: PipeWire MERKT sich 20 % fuer librespot ──────────────

      librespots Tonstrom stand auf 20 % (-41,94 dB) - der ALTE
      Daempfungswert des Vorlesens, nie zurueckgesetzt. Mal 40 % Master sind
      das ~8 % Gesamtlautstaerke: "keine Verbindung" war in Wahrheit "fast
      stumm". PipeWire persistiert das je Anwendung
      (module-stream-restore, ~/.local/state/wireplumber/stream-properties) -
      der Wert UEBERLEBT NEUSTARTS.

      PRUEFEN:  pactl list sink-inputs   (Volume der librespot-Zeile)
      HEILEN:   pactl set-sink-input-volume <nr> 100%

      OFFEN: warum die Daempfung nicht zurueckgesetzt wurde - das
      Zurücksetzen braucht eine Abbruchsicherung wie die Uebergabe-Skripte.

      ── URSACHE 3: Playlist BEIM ANBIETER geloescht ─────────────────────

      `DAS PUMMELEINHORN - DIE NEUEN HOERSPIELE` (0gJjI6IOi...) existiert bei
      Spotify nicht mehr: 404 "Resource not found" VON api.spotify.com selbst,
      durch die intakte Durchreiche. Sieht in der Netzwerkkonsole exakt aus
      wie Ursache 1. Daran kann die Box nichts heilen - nur die Verwaltung
      (neu verknuepfen oder entfernen).

      DIE BOX PRUEFT DAS JETZT SELBST: `verfuegbar.ts` (rein, 12 Tests) stuft
      Ein-Titel-Proben ein - nur Spotifys EIGENER 404 heisst geloescht,
      alles Unklare (Netz, Token, 429) laesst den alten Zustand STEHEN, damit
      ein Netzausfall nie die Mediathek durchstreicht. Lauf: 90 s nach dem
      Start (der Boot ist heilig) + alle 12 h, `.unref()` (sonst haengt der
      Testlauf - die setInterval-Falle der Kinderzeit). Ergebnis unter
      `GET /api/medien/verfuegbarkeit`; die Kacheln grauen aus und tragen
      eine Schaerpe „nicht mehr verfuegbar" - das Kind sieht WARUM nichts
      kommt, statt auf eine leere Liste zu starren.

      ── MERKSATZ ────────────────────────────────────────────────────────

      Bei "Spotify kaputt" die drei Ebenen TRENNEN, bevor man irgendwo dreht:
      `mupi-check standpasst` (Staende + Anbieter-Seite in einem),
      `mupi-check spotifykette` (Anmeldung/Token/Geraet),
      `pactl list sink-inputs` (hoerbar oder nur leise?).
      Und zur Klarstellung der Architektur: librespot SPIELT nur; die LISTEN
      holt die Box ueber ihre Web-API-Durchreiche (seit dem SDK-Rueckzug).
    source:
      - "Session 2026-07-29, am Pi 5 entwirrt; Werkzeuge + Kennzeichnung eingebaut"

  - kind: howto
    id: mupi-akku-anzeige
    title: "Akkubetrieb: was der MuPiHAT liefert, und wie die Anzeige feiner wird"
    tags: [mupihat, akku, bq25792, anzeige, i2c]
    body: |
      AM GERAET (Pi 5 auf Akku, 2026-07-29). Beim ersten Akkubetrieb kam KEIN
      Akkusymbol. Drei getrennte Ursachen, alle inzwischen behoben und ins
      Rezept gezogen.

      ── 1. DER LESER LIEF GAR NICHT: smbus2 fehlte ──────────────────────

      `mupi_hat.service` stand auf FAILED, `/tmp/mupihat.json` enthielt `[]` -
      obwohl der Ladebaustein BQ25792 auf **I2C 0x6b** einwandfrei antwortete.
      `mupihat_bq25792.py` importiert `smbus2`, und das Modul war auf KEINER
      Box installiert und in KEINEM Rezept. Eine frische SD haette denselben
      toten HAT.

          apt-get install -y python3-smbus2 i2c-tools
          systemctl reset-failed mupi_hat && systemctl restart mupi_hat

      (Debian-Paket, kein pip: Trixie ist "externally managed".)
      Rezeptschritt `mupihat-abhaengigkeit`.

      Der zweite Dienst `mupi_hat_control` (warnt + faehrt bei leerem Akku
      herunter) scheiterte an einem Fehler in SEINER Unit:
      `ExecStop=/bin/kill -TERM $(cat /run/mupi_hat_control.pid)` - systemd
      macht KEINE Befehlsersetzung und liest `$(cat` als Variablennamen
      ("Invalid environment variable name"). Ergaenzung mit leerer erster
      Zeile plus Shell-Aufruf; Rezeptschritt `mupihat-stopp-reparieren`.

      ── 2. DAS SYMBOL WAR ABGESCHALTET - an ZWEI Stellen ────────────────

      `hat_active` steht in ZWEI Dateien, die zwangslaeufig auseinanderlaufen:
        /etc/mupibox/mupiboxconfig.json  -> mupihat.hat_active   (PHP-Verwaltung schreibt hier)
        .../server/config/config.json    -> node-sonos-http-api.hat_active (Oberflaeche liest hier)

      Statt beide gleichzuhalten leitet `/api/sonos` es jetzt AB: liefert der
      HAT eine plausible Messung, gibt es einen Akku, also gibt es ein Symbol
      (`hat.ts`, rein + 7 Tests; der gesetzte Schalter bleibt als ODER).
      Wichtig dabei: `[]` in mupihat.json ist KEINE Messung - der Leser legt
      die Datei auch an, wenn er nichts lesen kann.

      ── 3. DIE ANZEIGE WAR ZU GROB - und WARUM sie es sein musste ───────

      Der BQ25792 ist ein **Laderegler mit ADC**, KEIN Fuel-Gauge: er misst
      VBAT/IBAT/VBUS, zaehlt aber keine Ladungsmenge. Die einzige verfuegbare
      Groesse ist die Spannung. `battery_soc()` machte daraus fuenf Stufen
      (100/75/50/25/0 %) - bei einem 10-Ah-Pack liegen zwischen zwei Stufen
      gut zwei Stunden Spielzeit.

      Feiner geht es nur ueber die Entladekurve, und die ist NICHT linear:
      4,20-3,90 V/Zelle = oberste 20 %, 3,90-3,50 V = ganze 60 % (flach),
      3,50-3,00 V = unterste 20 %. Beim 2S-Pack verdoppelt.
      `akkustand.ts` (rein, 15 Tests) interpoliert deshalb STUECKWEISE
      zwischen den fuenf Stuetzpunkten des eingestellten Profils und rechnet
      den Lasteinbruch heraus (Ruhespannung = gemessen - Ibat x 70 mOhm,
      gedeckelt auf 250 mV - der Widerstand ist geschaetzt).
      Probe: 7000 mV ergibt LINEAR 50 %, ueber die Kurve aber 31 %.
      Ergebnis als `Bat_SOC_fein` in `/api/mupihat`; `Bat_SOC` bleibt
      unveraendert.

      ── DAS AKKUPROFIL: ein stiller Fehlschlag ──────────────────────────

      `selected_battery` stand auf `"USB-C mode"`, die Liste fuehrt aber
      `"USB-C mode (no battery)"`. Kein Treffer -> der Treiber nahm still
      seine eingebauten Ansmann-Schwellen (Abschaltung 6800 mV). Achims Pack
      ist ein **ENERpower Li-Ion 7,2 V / 10 Ah / 72 Wh, 2S2P**, dessen eigenes
      Profil erst bei 6150 mV abschaltet - der Unterschied sind gut zwei
      Zehntel der Kapazitaet. Rezeptschritt `mupihat-akkuprofil` setzt es und
      PRUEFT hinterher, dass der Name wirklich in der Liste steht.

      ── GEMESSENE WERTE (Pi 5, Kiosk laeuft, nichts spielt) ─────────────

          Vbat   7,43-7,51 V     Ibat  -620 bis -880 mA     -> 4,6-5,6 W
          Temp   28,5-29 Grad    Vbus  0 (am Akku)
          Restzeit bei 10 Ah und 57 %: ~7,5 h

      Vbus SCHWANKT nach dem Abziehen noch Sekunden nach (5045 -> 640 -> 37
      -> 0). Der ehrliche Hinweis auf "Akkubetrieb" ist der NEGATIVE Ibat,
      nicht Vbus - und "laedt" ist `Ibat > 20`, NICHT `IBus > 0` (im
      Akkubetrieb stand IBus kurz auf 28 mA) und erst recht nicht
      `Charger_Status.includes('Charging')` - der Leerlauf meldet woertlich
      "Not Charging".

      ── DAS DATENBLATT SAGT ES SELBST (Ver. 2.4, 25.01.2025) ───────────

      **Die vier LEDs (S. 12), im Wortlaut:**

          PWR       5V Power Supply             LED OFF: 5V Power from HAT is OFF
                                                GREEN: 5V is present
          STATUS    Battery Charger Status      LED OFF: Charging Completed or Disabled
                                                LED ON: Charging in progress
                                                LED BLINKS at 1Hz: fault condition occurs
          PLUG      USB-C Plug Event            LED Short Flash: USB-C Plug event
          CAP_MIS   USB-C Capability Mismatch   LED OFF: No USB-C present
                                                LED ON: No Capability Mismatch
                                                LED TOGGLE: Capability Mismatch USB PD

      Fuer PWR ist ueberhaupt KEIN Blinken vorgesehen - nur an oder aus. Eine
      blinkende PWR heisst also woertlich: die 5 V kommen und gehen. Genau das
      wurde gemessen (5042 -> 1379 -> 5044 mV). CAP_MIS "TOGGLE" benennt
      dieselbe Sache von der PD-Seite, PLUG blitzt bei jedem Steckereignis -
      und jede Neuerkennung IST eines.

      **Die Anforderung (S. 21):** "It is recommended to use a USB-PD 3.0
      compatible charger with **20W** power."

      **Das Eingangslimit (S. 15):** "The Input Current is limited to **2.2A**.
      This limit is defined by hardware configuration in order to operate the
      HAT safely without additional heat sink." — und: "It is possible to
      overwrite the input current limit by SW (**use without guarantee**)."

      DARAUS FOLGT ZWEIERLEI:
        * Ein gemessenes Limit von **500 mA** heisst: es kam GAR KEIN
          PD-Vertrag zustande. Die Quelle gilt dem Regler als einfacher
          USB-Anschluss (BC1.2 SDP). Mit einem echten 20-W-PD-Netzteil stuende
          dort ein Vielfaches.
        * Die Konfigurationsliste fuehrt "high 2700" — das liegt **UEBER** der
          Hardware-Grenze von 2,2 A, die das Datenblatt fuer den Betrieb ohne
          Kuehlkoerper nennt. Nicht waehlen.

      Die Platine kann laut Datenblatt 3,6 bis 12 V am Eingang und ist
      "Compliant to USB-C Power Delivery 3.1".

      ── GEGENTEST: HAT MIT STROM, ABER OHNE AKKU ───────────────────────

      Am 2026-07-30 ergab sich der Fall von selbst (Akku ausgebaut, Strom
      über den HAT, SW2 umgelegt). Der Laderegler meldet dann:

          Vbat 690 mV · BatteryConnected 0 · Bat_SOC "" · Bat_Stat SHUTDOWN

      Alle Sicherungen dieses Tages greifen dabei GLEICHZEITIG:

      * `Bat_SOC_fein` FEHLT — `feinerStand` verlangt 3000…13000 mV, und
        690 mV ist kein Akku. Keine erfundene Zahl.
      * Der leere String bei `Bat_SOC` wird NICHT zu 0 % (`Number('')` ist
        null, nicht NaN — siehe [[mupi-akkuverlauf]]).
      * `hat_active` steht auf false, also KEIN Akkusymbol — abgeleitet aus
        der Messung, nicht aus einem Schalter.
      * Die **Akkukurve nimmt keinen Punkt an** (117 Punkte, letzter noch mit
        7175 mV vom angeschlossenen Akku). Sonst zeigte sie einen dramatischen
        Absturz auf 0 %, wo nur jemand den Stecker gezogen hat.
      * Das **Protokoll zeichnet trotzdem alles auf** — das ist Absicht: eine
        Diagnose-Aufzeichnung soll roh sein, die Verlaufskurve nicht.
      * `mupi_hat.service` läuft (der Regler antwortet ja), `mupi_hat_control`
        beendet sich sauber mit „No Battery connected" — und fährt die Box
        NICHT wegen `Bat_Stat: SHUTDOWN` herunter, weil er zuerst
        `BatteryConnected` prüft.

      Null fehlgeschlagene Dienste in dieser Lage.

      ── WERKZEUG ───────────────────────────────────────────────────────

          mupi-check akku

      prueft Chip, beide Dienste, Profil, Ladestand (fein), Verbrauch in Watt,
      Restzeit (Kapazitaet aus dem Profilnamen) und den Weg zur Oberflaeche.
      Beim Bauen selbst hineingelaufen: `lauf()` nimmt eine LISTE, kein
      Shell-Kommando - ein String scheiterte, und weil die Fehlermeldung den
      Befehl enthielt, war `"da" in ergebnis` WAHR (aus `echo da`). Die
      Pruefung meldete "Chip antwortet", ohne je einen Chip gesehen zu haben.
    source:
      - "Session 2026-07-29, Pi 5 am Akku; TI-Datenblatt BQ25792 (Laderegler + ADC, kein Coulomb-Zaehler)"

  - kind: falle
    id: mupi-wolke-frei-stehend
    title: "Anzeige abschalten: kopfIcons ist NICHT der Ausschalter (sie schwebt dann)"
    tags: [oberflaeche, darstellung, anzeigen, falle]
    body: |
      Die einzelne WOLKE sollte weg (sie sagt dasselbe wie die zusammengefasste
      Netzanzeige, die Balken UND Woelkchen zeigt - zwei Symbole, eine Aussage).

      FALSCH: `kopfIcons.wolke = false`. Das entscheidet nur, ob die Anzeige
      IN der Kopfleiste sitzt. Wer sie dort abschaltet, bekommt sie als FREI
      STEHENDE Anzeige zurueck - und zwar auf JEDER Seite statt nur auf der
      Startseite. Genau so passiert (home.page.html sagt es sogar im
      Kommentar: "die uebrigen schweben frei").

      RICHTIG: `zeigeWolke = false`. Analog gibt es `zeigeStaerke`.
      Sinnvoll dabei: `kopfIcons.wolke` auf `true` STEHEN lassen - dann sitzt
      sie beim Wiedereinschalten gleich wieder am richtigen Platz.

      Im Thema `New MuPiBox` ist es jetzt so hinterlegt (themen.ts).

      ── UND: die Darstellung wird als GANZES geschrieben ────────────────

      `PUT /api/darstellung` erwartet `{aktuell:{...}, themen:{...}}`. Wer nur
      das innere Objekt schickt, bekommt **200 OK** und danach
      `aktuell: null` - die gesamte Darstellung ist weg. Am Geraet passiert;
      Rueckweg ist derselbe PUT mit richtiger Huelle. Vor dem Schreiben also
      GET holen, Feld aendern, in der Huelle zurueckschicken.
    source:
      - "Session 2026-07-29, am Geraet"

  - kind: howto
    id: mupi-akkuverlauf
    title: "Akkuverlauf: Lade-/Entladekurve aufzeichnen und in der Verwaltung zeigen"
    tags: [mupihat, akku, verwaltung, grafik, sd-karte]
    body: |
      Ein Prozentwert sagt nicht, ob er steigt oder faellt. Die Box zeichnet
      deshalb auf: jede Minute ein Punkt (Spannung, Strom, Ladestand), und die
      Verwaltung hat einen eigenen Bereich **MuPiHAT** mit einer Grafik wie die
      Akku-Anzeige eines Telefons.

      ── WO WAS LIEGT ────────────────────────────────────────────────────

          src/backend-api/src/akkuverlauf.ts        rein, 22 Tests
          .../server/config/akkuverlauf.json        die Reihe (max 10080 Punkte = 7 Tage)
          GET /api/mupihat/verlauf?stunden=24       Punkte + Abschnitte + Restzeit
          src/frontend-admin/src/app/seiten/mupihat.ts   die Seite (SVG von Hand)

      ── DIE REGELN, und warum jede von ihnen dasteht ────────────────────

      **Messen jede Minute, SCHREIBEN nur alle zehn.** Die Box laeuft von
      einer SD-Karte, und die stirbt an Schreibzugriffen. Ein verlorener
      Zehn-Minuten-Rest nach hartem Ausschalten ist der geringere Schaden.
      Beim geordneten Herunterfahren (SIGTERM/SIGINT) wird der Rest gesichert.

      **Ein Punkt aus der VERGANGENHEIT wird verworfen** (`anhaengen`), und
      Punkte aus der ZUKUNFT werden beim Laden weggeraeumt (`bereinigen`):
      der Pi hat keine gepufferte Uhr und springt beim ersten Netzkontakt oft
      um Jahre. Ohne das ist jede Kurve Unsinn.

      **Ein einzelner Ausreisser beendet keinen Abschnitt** (`mindest=2`):
      der Strom schwankt bei jedem Titelwechsel; sonst zerfaellt eine Nacht am
      Ladegeraet in hundert Schnipsel.

      **Beim Ausduennen bleibt der LETZTE Punkt immer erhalten** - er ist der
      aktuelle Stand und darf nicht wegfallen.

      **Die Restzeit kommt aus Strom und Kapazitaet, NICHT aus der Steigung
      der Kurve.** Im flachen Mittelteil einer Li-Ion-Entladung (60 % der
      Kapazitaet!) ist die Steigung fast null - eine Hochrechnung daraus
      ergaebe Tage. Die Kapazitaet wird aus dem NAMEN des Akkuprofils gelesen
      ("ENERpower 2S2P 10.000mAh" -> 10000), weil die Konfiguration sie
      nirgends als Zahl fuehrt; gelingt das nicht, wird nicht hochgerechnet.

      ── FALLE, die die Tests fingen (nicht das Geraet) ──────────────────

      `Number('')` ist **0**, nicht NaN. Ein FEHLENDER Ladestand waere damit
      als 0 % in die Reihe gegangen und spaeter als leerer Akku gelesen
      worden - und genau diesen leeren String liefert `battery_soc()`, wenn
      der Pack unter die unterste Schwelle faellt. Immer die LAENGE pruefen,
      bevor ein String zur Zahl wird.

      ── DIE GRAFIK ─────────────────────────────────────────────────────

      Von Hand gezeichnetes SVG, keine Diagramm-Bibliothek: fuer EINE Kurve
      waere ein halbes Megabyte ueber das WLAN der Box nicht zu rechtfertigen.
      Ladephasen liegen als gruene Baender hinter der Linie - so sieht man,
      ob ein Anstieg vom Kabel kam. Erste und letzte Zeitmarke werden am Rand
      AUSGERICHTET statt zentriert, sonst ragt die letzte aus dem Bild.

      ── EHRLICHKEIT DER ZAHL ───────────────────────────────────────────

      Der BQ25792 ist ein Laderegler mit ADC, KEIN Fuel-Gauge - er zaehlt
      keine Ladungsmenge. Jeder Prozentwert ist aus der Spannung abgeleitet
      und im flachen Mittelteil entsprechend unscharf. Deshalb steht auf der
      Seite die gemessene SPANNUNG gleichberechtigt daneben.
    source:
      - "Session 2026-07-29, am Pi 5 auf Akku gebaut und gemessen"

  - kind: howto
    id: mupi-akku-lernen
    title: "Akku: Innenwiderstand und echte Kapazitaet aus dem eigenen Verlauf lernen"
    tags: [mupihat, akku, bq25792, messen, lernen]
    body: |
      GIBT ES DAS SCHON? Nein. Im Original (`mupihat.py`, `mupihat_bq25792.py`)
      steht NULL zu Verlauf, Lernen, Kapazitaet, Coulomb, Impedanz oder
      Alterung — nachgezaehlt, nicht vermutet. Der Treiber vergleicht sechs
      Spannungen und schreibt den Augenblick. Was echte Fuel-Gauge-Bausteine
      koennen (TI bq27xxx "Impedance Track", Maxim ModelGauge: Kapazitaet und
      Impedanz ueber Zyklen lernen), kann der BQ25792 NICHT — er ist ein
      Laderegler mit ADC.

      MAN KANN ES ABER IN SOFTWARE NACHBAUEN, weil der Chip STROM misst.

      ── 1. INNENWIDERSTAND aus Lastwechseln ─────────────────────────────

      Klemmenspannung = Ruhespannung + I x R. Aendert sich der Strom zwischen
      zwei DICHT beieinanderliegenden Messungen, folgt R = dV / dI.
      (Einheiten: mV/mA = Ohm, mal 1000 sind Milliohm.)

      Zwei Bedingungen, sonst misst man etwas anderes:
        * hoechstens 3 min Abstand — sonst ist der Ladestand SELBST gefallen
          (bei 600 mA aus 10 Ah rund 0,1 %/min, also 1-2 mV: gegen 50 mV
          Lastsprung vernachlaessigbar, gegen 5 mV nicht);
        * mindestens 150 mA Sprung — darunter misst man Rauschen.
      Genommen wird der MEDIAN, nicht der Mittelwert: ein Messwert genau
      waehrend des Lastwechsels zoege den Mittelwert mit.

      ── 2. KAPAZITAET durch eigenes Zaehlen ─────────────────────────────

      Strom mal Zeit IST Ladung — ein Coulomb-Zaehler in Software, gebaut aus
      unserer eigenen Reihe (Trapez ueber je zwei Punkte). Ueber den laengsten
      stetigen Hub im Ladestand: `gezaehlte mAh / Hub x 100` = Kapazitaet.
      Mindestens 20 Prozentpunkte Hub und 15 min, sonst keine Aussage.

      DAS MISST DEN PACK VON HEUTE, nicht den vom Aufkleber — ein drei Jahre
      alter 10-Ah-Pack hat keine 10 Ah mehr. Die Abhaengigkeit ehrlich
      benannt: der Ladestand kommt aus der Herstellerkurve; ist die falsch,
      ist auch diese Kapazitaet falsch. Ein VOLLER Zyklus (leer bis voll)
      machte sie davon unabhaengig — der naechste Schritt, wenn einer
      aufgezeichnet ist.

      ── LUECKEN NIE HOCHRECHNEN ────────────────────────────────────────

      War die Box aus, weiss niemand, was floss. Abstaende ueber 15 min
      werden UEBERSPRUNGEN statt mit dem letzten bekannten Strom gefuellt.

      ── ZURUECKGESPEIST ────────────────────────────────────────────────

      Der gemessene Widerstand geht in `feinerStand` (Lastkorrektur), die
      gemessene Kapazitaet in die Restzeit. Die Verwaltungsseite kennzeichnet
      jede Zahl als **gemessen** oder **angenommen** — ohne diese
      Unterscheidung sieht eine Schaetzung genauso verbindlich aus wie eine
      Messung.

      ── AM ECHTEN PACK GEMESSEN (2026-07-30) ───────────────────────────

      Lastsprünge am Akku (vier Kerne an/aus) ergaben **199 mOhm aus sechs
      Wechseln** — gegen die **70 mOhm**, die ich als Erfahrungswert
      angenommen hatte. Ein Faktor drei.

      Das ist keine Kleinigkeit: bei 500 mA sind das **100 mV** Lastkorrektur
      statt 35, und auf der ENERpower-Kurve (v_50 7300 / v_75 7700 mV, also
      400 mV auf 25 Punkte) sind 65 mV rund **vier Prozentpunkte** Ladestand,
      die unter Last zu niedrig angezeigt wurden.

      Der Wert enthält Verkabelung und Stecker und ist der SCHEINBARE
      Widerstand auf Sekunden-Zeitskala (der Spannungseinbruch hat einen
      ohmschen und einen langsamen Anteil). Für die Lastkorrektur ist genau
      das richtig — korrigiert werden soll der Einbruch, den man misst.

      ── ZWEI ZEITSKALEN, sonst lernt es NIE ────────────────────────────

      Die Lastphase dauerte 40 s, die Akkukurve zeichnet einmal je Minute
      auf — beide Nachbarpunkte sahen denselben Strom, die Box meldete
      "nichts gelernt". Dieselbe Rechnung auf der SEKUNDEN-Aufzeichnung fand
      sechs Wechsel.

      Seitdem: **Widerstand aus der dichten Aufzeichnung**, **Kapazität aus
      der langen Kurve** (ein 20-Punkte-Hub passt in kein Sekundenfenster).

      Und eine Falle, die erst danach sichtbar wurde: das Gelernte wird 15
      Minuten gepuffert, und die Oberfläche fragt direkt nach einem Neustart
      ab — der LEERE Befund wurde festgeschrieben. Der Puffer wird jetzt nach
      jeder Messung verworfen.

      ── WAS DAFUER NOETIG IST ──────────────────────────────────────────

      Widerstand: Lastwechsel (spielen und pausieren) — im Alltag binnen
      Stunden da. Kapazitaet: eine Entladung ueber >= 20 Prozentpunkte.
      Nach 12 min konstanter Last stand am Geraet ehrlich "nichts gelernt",
      waehrend der Ladungszaehler schon 109 mAh zeigte.

      ── FALLE BEIM TESTEN (meine, nicht die des Codes) ─────────────────

      Die ersten synthetischen Daten streuten Lastpausen ein, OHNE den
      Ladestand entsprechend langsamer fallen zu lassen — ein Pack, in dem
      weniger Strom mehr Ladung verbraucht. Der Code mass diesen unmoeglichen
      Pack korrekt, und der Test gab ihm Unrecht. Synthetische Daten muessen
      physikalisch stimmig erzeugt werden: Ladestand AUS der geflossenen
      Ladung ableiten, nicht nebenher setzen.
    source:
      - "Session 2026-07-29; Original-Treiber nachgezaehlt (0 Treffer), Verfahren am synthetischen Pack belegt"

  - kind: signature
    id: mupi-versorgungs-led-blinkt
    title: "Versorgungs-LED blinkt bei angestecktem USB — das Netzteil wird verworfen"
    tags: [mupihat, bq25792, akku, netzteil, usb-c]
    body: |
      **Bild:** USB steckt, die Spannungsversorgungs-LED BLINKT, und der Akku
      wird trotzdem nicht geladen.

      **Es ist kein Defekt.** Die Fehlerregister des BQ25792 (0x20/0x21) waren
      am Geraet BEIDE 0x00 — keine Ueberspannung, kein Temperatur- oder
      Zeitfehler. Der Laderegler ERKENNT die Quelle, VERWIRFT sie und faengt
      von vorn an. Dieser Takt IST das Blinken.

      ── GEMESSEN (2026-07-29, Pi 5 am MuPiHAT) ─────────────────────────

          Zeit       Vbus   IBus   Ibat   Status
          23:46:44   5042     30   -625   Not Charging
          23:46:47   1546      0   -628   Not Charging     <- Eingang weg
          23:46:51   1379      0   -617   Not Charging
          23:47:00   5044     29   -616   Not Charging     <- wieder da

      Etwa 15 s Zyklus. Der Akku traegt die Box DURCHGEHEND weiter
      (Ibat ~ -625 mA), vom USB kommen nur ~29 mA.

      ── DIE REGISTER SAGEN ES DEUTLICH ─────────────────────────────────

          REG 0x1C  wechselt zwischen 0x10 und 0x00
                    -> VBUS_STAT (Bits 4:1) springt zwischen
                       0b1000 = "Not qualified adaptor" und 0b0000 = "No input"
          REG 0x1D  Bit 1 (DPDM_STAT) togglet -> die D+/D--Erkennung laeuft
                    IMMER WIEDER NEU
          REG 0x1B  = 0x0F -> Power Good, VBUS present, KEINE DPM-Regelung
          REG 0x20/0x21 = 0x00 -> keinerlei Fehler

      **EINMAL LESEN GENUEGT NICHT**: der Zustand wechselt, und wer einmal
      liest, erwischt mal 0x8 und mal 0x0 und haelt es fuer den Dauerzustand.

      ── URSACHE ────────────────────────────────────────────────────────

      Die Quelle ist zu schwach. Der Pi 5 mit Schirm zieht rund 1 A bei 5 V,
      BEVOR ueberhaupt Ladestrom fliesst. Ein PC-Anschluss, ein Handy-
      Ladegeraet oder ein duennes/langes Kabel brechen unter dieser Last ein;
      der Regler laesst die Quelle los, sie erholt sich unbelastet, er
      erkennt sie neu — Schleife.

      ABHILFE: ein **USB-C-PD-Netzteil** (der MuPiHAT ist dafuer gebaut,
      bis 4 A) und ein **kurzes, stromfestes Kabel**.

      ── ZWEITER BEFUND, unabhaengig davon ──────────────────────────────

      `Input_Current_Limit` steht bei **500 mA**. Die Konfiguration fuehrt
      zwar eine Liste `current_limit` (safe 1790 / medium 2200 / high 2700),
      aber es gibt KEIN Feld `selected_current_limit`, und `mupihat.py` ruft
      `set_input_current_limit()` NIE auf — die Liste ist tot. Selbst mit
      einem guten Netzteil bleibt es bei dem, was die Erkennung hergibt.

      VORSICHT beim "Reparieren": das Limit HOEHER zu setzen, als die Quelle
      liefern kann, macht das Takten SCHLIMMER, nicht besser. Erst ein
      taugliches Netzteil, dann das Limit.

      ── WERKZEUG ───────────────────────────────────────────────────────

          mupi-check akku

      liest 0x1C achtmal ueber zwoelf Sekunden und meldet
      "Netzteil wird verworfen: kein Eingang / NICHT ANERKANNT" samt der
      VBUS-Spanne. Beim Bauen zweimal selbst hineingelaufen: eine feste
      Schwelle "VBUS < 3000 = Einbruch" hielt 3576..4945 mV fuer stabil
      (jetzt: mehr als 500 mV Einbruch), und "Netzteil steckt = VBUS > 4000"
      schwieg ausgerechnet WAEHREND des Einbruchs ueber den Fehler, den es
      finden soll.
    source:
      - "Session 2026-07-29, am Geraet gemessen; Registerbedeutungen aus dem BQ25792-Datenblatt"

  - kind: note
    id: mupi-mupihat-revisionen
    title: "MuPiHAT: die Platinenstände, und was sich je Stand geändert hat"
    tags: [mupihat, hardware, revision, usb-c, pd]
    body: |
      Aus dem Änderungsverzeichnis des Datenblatts (MuPiHAT+ Ver. 2.4,
      25.01.2025, S. 27) und den drei Steckerlisten (S. 5–9). Wer mehrere
      Platinen hat, braucht das — sie unterscheiden sich in Dingen, die
      wehtun.

      ── 2.0 · 01/2024 ──────────────────────────────────────────────────
      Erster Stand. Eigenes, älteres Datenblatt („Datasheet MupiHat old").

      ── 2.2 · 03/2024 ──────────────────────────────────────────────────
      * GPIO-Stecker auf normale 2,54-mm-Stiftleiste
      * LED des Einschaltknopfs auf **GPIO13** (PWM)
      * Aufbau für niedrigere Temperatur überarbeitet
      * PTC-Sicherung auf **2,6 A**
      * **Eingangsstrom-Grenze auf 1,75 A** (später 2,2 A)
      * **Eingangsspannung bis 12 V**
      * Weckfunktion für Powerbank

      ── 2.3 · 04/2024 ──────────────────────────────────────────────────
      * ERRATUM: Powerbank-Weckfunktion wegen eines Fehlers wieder ENTFERNT
      * Beschreibung, wie sich die Eingangsstrom-Grenze ändern lässt

      ── 3.0 · 07/2024 ──────────────────────────────────────────────────
      * MuPiHAT 3.0, 9 Stecker
      * J1 (Taster) und J15 (LED) als **Molex KK 2,54 mm**
      * **ERRATUM, nur 3.0: die Beschriftung von J2 ist VERTAUSCHT** — der
        als 5 V bezeichnete Pin ist GND und umgekehrt. Das kann Hardware
        kosten.

      ── 3.1 · 11/2024 ──────────────────────────────────────────────────
      * J1/J15 auf **JST XH 2,50 mm** gewechselt (10 Stecker)

      ── 3.2 · 01/2025 ──────────────────────────────────────────────────
      * NEU: **J3** — Anschluss für eine externe Lade-Status-LED (11 Stecker)
      * Dafür SW1.1 auf OFF: schaltet die LED auf der Platine ab und legt den
        Strom auf die Stiftleiste. Ein Vorwiderstand ist auf dem HAT.

      ── 3.4.1 · nach 01/2025 ───────────────────────────────────────────
      * NEUER als das vorliegende Datenblatt (Ver. 2.4 endet bei 3.2)
      * Am Gerät belegt: der Pi 5 startet darauf sauber (siehe
        [[achim-mupihat-341]])

      ── GILT FÜR ALLE 3.x ──────────────────────────────────────────────

      **Schalter und Brücken** (S. 12): SW1.1 Status-LED (Vorgabe AN),
      SW1.2 Thermistor auf der Platine (AN), SW1.3 On/Off-Shim (AN);
      **SW2 Akku-Konfiguration — links: Akku/Lader benutzen, rechts: KEIN
      Akku/Lader**; SW3 Stereo/Mono.

      **JP2** (Vorgabe geschlossen) speist den Pi vom HAT. Wörtlich aus dem
      Datenblatt (S. 15): *„The JUMPER JP 2 must be open if Power is supplied
      not from HAT but from RaspberryPi. If Jumper JP2 is closed and 5V supply
      is provided from RaspberryPi, **the HAT will be damaged**."*

      **Eingang:** USB-C, 3,6 bis 12 V, „Compliant to USB-C Power Delivery
      3.1". Empfohlen (S. 21): **USB-PD-3.0-Netzteil mit 20 W**.
      Eingangsstrom hardwareseitig auf **2,2 A** begrenzt, „in order to
      operate the HAT safely without additional heat sink" (S. 15).
      Ruhestrom am Eingang ~60 mA bei 5 V.

      ── DEN STAND AUSLESEN: DOCH, ÜBER DIE EINGANGSGRENZE ──────────────

      **Am 2026-07-30 mit ZWEI Platinen im direkten Vergleich belegt:**

          MuPiHAT 2.2   ->  Eingangsgrenze 1780 mA
          MuPiHAT 3.4.1 ->  Eingangsgrenze 2230 mA

      Das deckt sich mit den Papieren: die Änderungsliste nennt für **2.2**
      „Input Current Limit to **1.75A**", das Datenblatt für **3.x**
      „limited to **2.2A** … without additional heat sink". Die Grenze ist
      laut Datenblatt „defined by hardware configuration" — sie steckt also
      in der PLATINE, nicht in der Software, und ist damit ein echtes
      Merkmal.

      Die Box sagt den Stand deshalb jetzt selbst
      (`standAusGrenze`, `/api/mupihat/diagnose`). Auf dem Pi 4 mit der 2.2
      lautete die Antwort „vermutlich 2.2 oder 2.3" — richtig, ohne dass
      jemand etwas eingetragen hatte.

      **ZWEI EINSCHRÄNKUNGEN, sonst wird daraus eine falsche Sicherheit:**

      * Es gilt NUR mit einem tauglichen Netzteil. Ohne eines steht dort der
        AUSGEHANDELTE Wert (etwa 500 mA an einem einfachen USB-Anschluss),
        und der sagt nichts über die Platine. Unter 1000 mA gibt es deshalb
        KEINE Aussage statt einer schlechten.
      * 2.2 und 2.3 unterscheidet es nicht — die 2.3 änderte nur Software
        und ein Erratum, nicht die Grenze.

      ── WAS NICHT TAUGT ────────────────────────────────────────────────

      * `REG 0x48` des Ladereglers nennt Typ und Ausgabe des **Chips**
        (BQ25792) — auf der 2.2 UND der 3.4.1 gemessen: beide `0x08`. Taugt
        also nicht einmal über die Hauptversionen hinweg.
      * Das **HAT-EEPROM** (I2C 0x50 auf Bus 0) wäre der vorgesehene Weg —
        auf der 2.2 ist der Chip verbaut, aber **unbeschrieben** (alles
        `0xFF`). Der Pi 5 hat Bus 0 gar nicht freigeschaltet.
      * Welche Bausteine am I2C antworten. Am Gerät (3.4.1): **0x6b** (der
        Laderegler) und ein **0x20**, der im Datenblatt NICHT vorkommt und
        dessen Register auf fast alle Adressen 0x04 zurückgeben — nicht
        identifiziert. Sein Fehlen könnte auf eine einfachere Ausführung
        deuten, beweisen lässt sich damit nichts.
      * Eingangsspannung über 5,5 V wäre ein BEWEIS für USB-PD (nur ein
        ausgehandelter Vertrag liefert mehr als 5 V).
      * Das ausgehandelte Eingangslimit sagt etwas über das NETZTEIL, nicht
        über die Platine.

      Deshalb: in der Verwaltung unter **MuPiHAT** eintragen. Die Seite zeigt
      danach die Änderungen des gewählten Stands.
    source:
      - "MuPiHAT+ Datasheet & Manual Ver. 2.4 (25.01.2025); Messungen am Gerät 2026-07-29/30"

  - kind: falle
    id: mupi-pd-netzteil-gibt-bei-5v-nichts
    title: "PD-Netzteil lädt den Laptop, aber nicht die Box — und Registerzwang hilft nicht"
    tags: [mupihat, bq25792, usb-c, pd, netzteil, falle]
    body: |
      **Einwand, der naheliegt:** „An dem Kabel lädt mein Laptop problemlos,
      die Box toggelt trotzdem. Kann man das per Register erzwingen?"

      **GEMESSEN (2026-07-30), und die Antwort ist nein.** Erzwingen lässt es
      sich — es hilft nur nicht.

      ── DER VERSUCH ────────────────────────────────────────────────────

          mupihat-eingang.py --setzen 1500      # AUTO_INDET_EN=0, IINDPM=1500 mA

      Ergebnis: der Regler zog tatsächlich **1426 mA** — und die
      Eingangsspannung brach dabei von **5044 auf 3979 mV** ein. Daraus:

          (5044 - 3979) mV / 1426 mA = 0,75 Ohm

      Ein gutes USB-C-Kabel liegt **unter 0,10 Ohm**. Die Quelle gibt bei 5 V
      schlicht nichts her; VINDPM (4,3 V) nahm daraufhin korrekt zurück — der
      Schutz hat funktioniert.

      ── WARUM DER LAPTOP TROTZDEM LÄDT ─────────────────────────────────

      Der Laptop handelt einen **PD-Vertrag bei 20 V** aus. „Lädt den Laptop"
      sagt deshalb NICHTS darüber, ob dieselbe Quelle bei **5 V** anderthalb
      Ampere liefert. Ohne Vertrag hält sich ein PD-Netzteil auf den
      USB-Vorgabestrom zurück.

      ── WARUM DER LADEREGLER DEN VERTRAG NICHT MACHT ───────────────────

      Der BQ25792 stuft ausschließlich über **D+/D− (BC1.2)** ein. USB-PD
      läuft über die **CC-Leitungen** und gehört einem PD-Controller. Der
      Laderegler kann also nicht „in einen Kompatibilitätsmodus fallen und
      sich wie non-PD verhalten" — er verhält sich BEREITS so. Die Lücke ist
      auf der Quellenseite.

      Die Bits `HVDCP_EN` / `EN_9V` / `EN_12V` (REG 0x11, am Gerät alle 0)
      helfen NICHT: die gehören zu Qualcomm-artigem HVDCP, nicht zu USB-PD.
      Ein reines PD-Netzteil antwortet darauf nicht.

      ── WAS WIRKLICH HILFT ─────────────────────────────────────────────

      1. Ein **Netzteil ohne PD-Zwang**, das bei 5 V liefert. Am selben Gerät
         belegt: als DCP erkannt, **2114 mA** Eingangsstrom, Schnellladung,
         Limit 2230 mA, 61 °C.
      2. Ein **PD-Trigger-/Decoy-Modul**, das einen festen Vertrag aushandelt.
         Der HAT nimmt 3,6–12 V — bei 9 oder 12 V fließt für dieselbe
         Leistung weniger Strom, also weniger Verlust im Kabel.

      ── DAS WERKZEUG, DAS DEN STREIT ENTSCHEIDET ───────────────────────

          mupihat-eingang.py --pruefen

      fordert kurz einen definierten Strom an, misst den Spannungseinbruch und
      rechnet den Innenwiderstand von Quelle + Kabel aus — und stellt danach
      IMMER auf Auslieferungsverhalten zurück (im `finally`).
      Richtwerte: unter 0,15 Ohm gut · bis 0,35 grenzwertig · darüber reicht
      es nicht. Fließt fast nichts, ist es ein reines PD-Netzteil.

      **Grenzen, die das Werkzeug einhält:** höchstens 2000 mA (Hardware
      2,2 A, Datenblatt S. 15), und **VINDPM bleibt unangetastet** — genau
      diese Schwelle verhindert, dass eine schwache Quelle zusammenbricht.
      Das Datenblatt zum Überschreiben der Grenze: „use without guarantee".
    source:
      - "Session 2026-07-30, am Pi 5 gemessen (beide Netzteile im direkten Vergleich)"

  - kind: howto
    id: mupi-hat-fehlerbericht
    title: "HAT-Protokoll: aufzeichnen, interaktiv ansehen, als PDF/Excel/CSV/JSON herunterladen"
    tags: [mupihat, diagnose, bericht, pdf, excel, bq25792]
    body: |
      Ein blinkendes Licht ist ein VERLAUF, kein Zustand. Ein einzelner Blick
      zeigt mal das eine, mal das andere — festhalten lässt es sich nur als
      Reihe.

      ── INTERAKTIV ANSEHEN ─────────────────────────────────────────────

      Unter der Aufzeichnung liegt die Kurve: VBUS rot, IBUS blau, die
      VINDPM-Schwelle 4,3 V gestrichelt, und die Abschnitte, in denen der
      Regler die Quelle VERWORFEN hat, rot hinterlegt. Beim Überfahren steht
      der genaue Messwert daneben — samt Rohregister 0x1C, sodass sich jede
      Aussage an der Stelle nachprüfen lässt, an der sie herkommt.

      **ZEITVERSATZ, den man kennen muss:** Spannungen und Ströme kommen aus
      `/tmp/mupihat.json` (der HAT-Dienst schreibt es etwa im Sekundentakt),
      die Register liest der Server DIREKT im Messaugenblick. Beide können
      also bis zu einer Sekunde auseinanderliegen. Bei 15 s Takt egal — an
      einer FLANKE steht dann aber neben „kein Eingang" schon wieder ein
      Eingangsstrom von zwei Ampere (in der Ablesung gesehen). Kein
      Widerspruch, sondern der Versatz: Flanken nicht einzeln bewerten.

      ── AUFZEICHNEN ────────────────────────────────────────────────────

      Verwaltung → **MuPiHAT** → „Protokoll" → 2 oder 5 Minuten. Aufgezeichnet wird **jede Sekunde**: Zeit, VBUS, IBUS, VBAT,
      IBAT, Temperatur, Ladestand, Limit, erkannte Quelle, Ladezustand und
      die ROHREGISTER 0x1B/0x1C/0x1D/0x20/0x21/0x26/0x27.

      **Warum im Sekundentakt:** der Einbruch dauert rund 15 s. Der
      Minutentakt der Akkukurve verpasst ihn komplett; zwei Minuten zeigen
      ihn rund zehnmal.

      ── DIE MERKER-REGISTER 0x26/0x27 ──────────────────────────────────

      Gleiche Bitbelegung wie die Störungsregister 0x20/0x21, aber sie halten
      fest, was **passiert IST**, nicht was gerade IST. Eine Störung von einer
      halben Sekunde steht im Status fast nie, im Merker sehr wohl.

      **Merker sind LESEND LÖSCHEND** — wer sie abfragt, nimmt sie weg.
      Deshalb werden sie über die ganze Messung eingesammelt statt einzeln
      betrachtet.

      Am Gerät blieben sie beim taktenden Eingang **leer**. Das SCHWÄCHT den
      Fall nicht, es stärkt ihn: nichts ist defekt, der Regler verwirft nur
      immer wieder die Quelle.

      ── DIE DATEIEN ────────────────────────────────────────────────────

          /api/mupihat/protokoll.pdf?revision=…   Bericht mit Befunden + Kurve
          /api/mupihat/protokoll.xlsx             Excel
          /api/mupihat/protokoll.csv              CSV
          /api/mupihat/protokoll.json             Rohdaten

      Alles selbst erzeugt, **ohne fremde Bibliothek**
      (`src/backend-api/src/protokollexport.ts`, 16 Tests): XLSX ist ein ZIP
      mit XML („stored", also unkomprimiert — gültig und spart den ganzen
      Deflate-Teil, mit eigenem CRC32); PDF ist Text mit Querverweistabelle,
      und die Kurve wird mit `m`/`l`/`S` direkt gezeichnet. Alle drei Formate
      teilen sich EINE Spaltendefinition, damit sie nicht auseinanderlaufen.

      Im Diagramm liegt die **VINDPM-Schwelle (4,3 V) gestrichelt** über der
      Kurve — ob der Einbruch darunter ging, ist genau die Frage, die ein
      Techniker stellt.

      ── ZWEI KODIERUNGSFALLEN, beide am erzeugten Dokument gesehen ──────

      1. Ein PDF mit `/WinAnsiEncoding` will **EIN Byte je Zeichen**. Mit
         UTF-8 wurde aus „Störungen" ein „StÃ¶rungen" — und jede
         `/Length`-Angabe stimmte nicht mehr, weil sie Bytes zählt.
      2. CSV braucht für deutsches Excel **Semikolon UND BOM**: ohne
         Semikolon landet alles in einer Spalte, ohne BOM wird aus
         „Ladegerät" ein „LadegerÃ¤t".

      ── UND EIN INHALTLICHER ────────────────────────────────────────────

      Der erste erzeugte Bericht WIDERSPRACH SICH: „Eingangsspannung
      4962…4996 mV, in Ordnung" über einer Kurve, die auf 672 mV einbrach —
      weil die Befunde aus einer frischen 12-Sekunden-Messung kamen statt aus
      dem aufgezeichneten Lauf. Befunde eines Berichts müssen aus DEN DATEN
      des Berichts stammen.

      ── GEPRÜFT MIT WERKZEUGEN, nicht nach Augenmaß ────────────────────

          unzip -t datei.xlsx      -> gültiges ZIP, 5 Teile
          qpdf --check datei.pdf   -> keine Syntax- oder Stream-Fehler
          pdftotext datei.pdf      -> Text kommt zurück
          pdftoppm -png            -> die Kurve ist wirklich da
    source:
      - "Session 2026-07-30, am Pi 5 aufgezeichnet (100 Messungen, zehn Einbruchzyklen)"

  - kind: note
    id: mupi-hat-leds-lesbar
    title: "Lassen sich die vier HAT-LEDs auslesen? Zwei ja (abgeleitet), zwei nein"
    tags: [mupihat, led, bq25792, i2c]
    body: |
      **Nachgemessen, nicht vermutet.** Keine der vier LEDs hängt am I2C — es
      gibt kein Register, das ihren Zustand meldet. Zwei lassen sich aber
      EXAKT ableiten, weil das Datenblatt die Regel nennt und die Eingänge
      alle vorliegen.

      **STATUS — ableitbar.** Der BQ25792 treibt sie über seinen eigenen
      STAT-Pin. Am Gerät geprüft: `REG 0x12` Bit 6 (`DIS_STAT`) = 0, der Pin
      ist also aktiv. Die Regel (Datenblatt S. 12): aus = fertig/abgeschaltet,
      an = lädt, 1 Hz = Störung. Alle drei Eingänge stehen in `REG 0x1C`
      (CHG_STAT) und `0x20`/`0x21` (Störungen).

      **PWR — ableitbar.** Reine Hardware am 5-V-Zweig. `PG_STAT`
      (`REG 0x1B`, Bit 3) plus die gemessene VBUS sagen dasselbe.

      **PLUG und CAP_MIS — nicht lesbar.** Beide gehören zur USB-C-Seite. Am
      I2C antwortet dort nichts Benanntes (der Baustein 0x20 ist nicht
      identifiziert, siehe [[mupi-mupihat-revisionen]]). Sie lassen sich nur
      ERSCHLIESSEN: jede Neuerkennung ist ein Steckereignis (PLUG blitzt),
      und ein PD-Konflikt lässt CAP_MIS wechseln.

      Wer eine Board-Ansicht mit blinkender LED bauen will, hat also für zwei
      LEDs eine belastbare Grundlage und muss die anderen beiden als
      „erschlossen" kennzeichnen — sonst zeigt die Oberfläche eine Sicherheit,
      die es nicht gibt.

      **Zum Bild der Platine:** Schaltplan und Layout liegen nur als GRAFIKEN
      im Datenblatt des Herstellers. Fremde Zeichnungen in die Software zu
      übernehmen ist eine Rechtefrage; die Steckerlisten dagegen sind Daten.
      Der saubere Weg ist deshalb eine EIGENE, vereinfachte Zeichnung
      (Umriss, J1–J15, die vier LEDs, SW1–3, JP2) aus den Tabellen.
    source:
      - "Session 2026-07-30, am Gerät geprüft (DIS_STAT=0); Datenblatt Ver. 2.4 S. 12"

  - kind: note
    id: mupi-pi4-kein-mupihat
    title: "Der Pi 4 hat KEINEN MuPiHAT — was das für den Abgleich bedeutet"
    tags: [pi4, mupihat, abgleich, hardware]
    body: |
      Nachgemessen (2026-07-30): auf dem Pi 4 (192.168.178.99) ist der
      I2C-Bus 1 **völlig leer** — kein BQ25792 auf 0x6b, gar nichts. Der
      Verstärker **MAX98357A ist da** (`aplay -l` -> card 0), der hängt aber
      an I2S, nicht an I2C. Ein Audio-Teil ohne Ladeteil ist also möglich und
      genau der Fall.

      **Folge für den Abgleich Pi 5 -> Pi 4:** alles Akku-bezogene gehört dort
      NICHT hin und ist kein Rückstand:

          smbus2, i2c-Gruppe, mupihat-eingang.py, die Stopp-Korrektur der
          Unit, das ENERpower-Akkuprofil, hat_active

      `hat_active=false`, `selected_battery="USB-C mode"` und ein inaktiver
      `mupi_hat.service` sind auf dieser Box der RICHTIGE Zustand.

      Und es zeigt sich, dass die Ableitung trägt: `/api/sonos` meldet
      `hat_active: false`, weil KEINE plausible Messung vorliegt — also kein
      Akkusymbol, ohne dass jemand etwas einstellen musste
      ([[mupi-akku-anzeige]]). `mupi-check akku` sagt „Keine Akku-Platine
      erkannt — bei einer Box am Netzteil in Ordnung" und meldet 0 Fehler.

      **Regel:** vor dem Nachziehen hardwareabhängiger Einstellungen erst die
      Hardware messen, nicht die Konfiguration der anderen Box kopieren.

  - kind: note
    id: mupi-dienste-durchgesehen
    title: "Die Dienste durchgesehen: was läuft, was absichtlich aus ist, was weg kann"
    tags: [dienste, systemd, aufraeumen, mqtt]
    body: |
      Durchgang über alle Einträge der Verwaltungsseite **Dienste**
      (2026-07-30, Pi 5 und Pi 4). Ergebnis vorweg: **null fehlgeschlagene
      Dienste** auf beiden Boxen.

      ── ZUERST UNTERSCHEIDEN, sonst sucht man Fehler, die keine sind ────

      „inactive" heißt NICHT „kaputt". Drei verschiedene Dinge sehen gleich
      aus:

      * **Dauerdienst, der laufen soll** — hier gehört „inactive" untersucht.
      * **Einmal-Dienst** (`Type=oneshot`), der beim Start seine Arbeit tat
        und sich beendet hat. „inactive (dead)" ist der NORMALZUSTAND.
        Betrifft `mupibox-netz-watchdog-boot`, `mupi-network-info`,
        `mupibox-bt-reconnect`, `librespot-waechter`.
      * **Abgeschaltete Funktion** (`disabled`) — eine Entscheidung, kein
        Fehler.

      `systemctl show <unit> -p Type -p Result` beantwortet das in einem Zug.

      ── DER EINZIGE ECHTE FEHLER: mupi_mqtt — INZWISCHEN REPARIERT ─────

      Siehe [[mupi-mqtt-reparatur]]. Drei Fehler übereinander: zwei fehlende
      Python-Module, ein fehlender Konfigurationsschlüssel und ein Schalter,
      der gar nichts tat. Alles behoben, auf beiden Boxen.

      ── ENTFERNT: eigenes Messgerüst ───────────────────────────────────

      `luecke.service` („Misst, wann das Fenster der Oberfläche steht") war
      MEIN Gerüst aus der Boot-Optimierung. Es lief bei jedem Start mit,
      obwohl die Messung längst erledigt war. Auf beiden Boxen entfernt.

      **Regel daraus:** ein Messgerüst, das man in ein System hängt, gehört
      auf eine Liste — sonst läuft es dort in einem halben Jahr noch.

      ── ABSICHTLICH AUS, richtig so ────────────────────────────────────

      `mupi_fan` (kein Lüfter: `fan_active=false`), `pulseaudio` (auf dem
      Pi 5 läuft PipeWire), `spotifyd` (durch **librespot** abgelöst),
      `mupi_splash` (durch `mupibox-boot-splash` abgelöst), `mupi_vnc`,
      `mupi_novnc`, `dietpi-dashboard`, `mupi_telegram`, `mupi_wifi`,
      `mupi_autoconnect-wifi`, `mupi_startstop`, `mupi_powerled`.

      ── RICHTIGSTELLUNG 08.08.2026: `mupi_powerled` ist AN ─────────────

      Die Liste oben stimmte am 30.07.2026 und stimmt fuer `mupi_powerled`
      seit dem 06./07.08.2026 NICHT MEHR. An der Box .169 nachgemessen
      (08.08.2026, nur `systemctl is-enabled`):

          mupi_startstop   disabled   ← unveraendert richtig
          mupi_powerled    enabled    ← und `active`

      Eingeschaltet wurde er beim Bau des Knopflichts. Der Unterschied ist
      nicht kosmetisch: solange der Dienst AUS war, konnte kein Fehler in
      der LED-Kette sichtbar werden — genau deshalb lagen dort drei bis vier
      Fehler jahrelang unbemerkt nebeneinander (siehe
      [[chromium-heisst-auf-dietpi-chromium]]). Ein abgeschalteter Dienst
      versteckt seine Fehler, er behebt sie nicht.

      WARUM DIESE ZEILE GEFAEHRLICH IST und nicht nur veraltet: dieser
      Eintrag IST die Liste, auf die man sich verlaesst, wenn man fragt
      „laeuft das absichtlich nicht?". Wer sie im Kopf hat, sucht einen
      LED-Fehler gar nicht erst. Zustandslisten dieser Art gehoeren vor dem
      Weitergeben nachgemessen — [[wiki-zustand-hat-haltbarkeit]].

      **Zwei Doppelungen fallen in der Liste auf** und stiften Verwirrung:
      „Spotify-Wiedergabe" (librespot, an) neben „Spotify-Wiedergabe
      (spotifyd)" (aus), und zwei Startbilder. Beides ist korrekt
      eingestellt, sieht in der Übersicht aber wie eine Wahl aus, die es
      nicht mehr gibt.

      ── OFFEN, und für den Akkubetrieb wichtig ─────────────────────────

      `mupi_idle_shutdown` ist AUS, und in der Konfiguration steht
      `timeout.idlePiShutdown = "0"` — die Box schaltet sich also NIE von
      selbst ab. Am Netz egal; **am Akku läuft der Pack leer**, während die
      Box nur dasteht (gemessen: 4,6–6,4 W, siehe [[mupi-akku-anzeige]]).
      Ein Wert von etwa 30 Minuten wäre naheliegend — das ist aber eine
      Verhaltensänderung und gehört entschieden, nicht nebenbei gesetzt.
      (`timeout.idleDisplayOff` steht auf 10 Minuten und wirkt bereits.)
    source:
      - "Session 2026-07-30, beide Boxen durchgegangen"

  - kind: howto
    id: mupi-mqtt-reparatur
    title: "MQTT instandsetzen: zwei fehlende Module, ein fehlender Schlüssel, ein toter Schalter"
    tags: [mqtt, dienste, systemd, python, homeassistant]
    body: |
      `mupi_mqtt.service` war der EINZIGE Eintrag in `systemctl --failed`.
      Dahinter lagen **drei** voneinander unabhängige Fehler — jeder davon
      hätte allein gereicht.

      ── 1. ZWEI Module fehlten, nicht eines ────────────────────────────

          ModuleNotFoundError: No module named 'netifaces'

      Das war nur der erste. Nach `python3-netifaces` kam `paho.mqtt`
      hinterher; von den drei Importen war allein `alsaaudio` da. Dieselbe
      Sorte wie `smbus2` beim MuPiHAT ([[mupi-akku-anzeige]]) — ein Modul,
      das in keinem Rezept stand.

          apt-get install -y python3-netifaces python3-paho-mqtt

      **Lehre:** nach dem ersten fehlenden Import nicht aufhören. Alle
      Importe auf einmal prüfen, sonst repariert man in Runden.

      ── 2. Ein Konfigurationsschlüssel fehlte ──────────────────────────

      Der Abschnitt `mqtt` hat 13 Schlüssel, das Skript liest **14**:

          Skript erwartet : active broker clientId debug ha_active ha_topic
                            name password port refresh refreshIdle timeout
                            topic username
          FEHLT           : name

      `name` ist der Gerätename in der **Home-Assistant-Erkennung**
      (`"device": {"name": mqtt_name, …}`). Der Rechnername passt und ist je
      Box eindeutig.

      Ermitteln lässt sich so etwas ohne Raten: die `jsonconfig['mqtt'][…]`
      im Skript gegen die Schlüssel der Konfiguration mengenmäßig
      vergleichen.

      ── 3. Der Schalter `active` tat NICHTS ────────────────────────────

      `mqtt.py` liest `mqtt.active` (Zeile 908) und **benutzt den Wert
      nirgends**. Der Dienst versuchte also auch ausgeschaltet, den
      Beispiel-Broker `mqtt-example-broker.com` zu erreichen.

      **ExecCondition statt Eingriff ins Original** — eine Ergänzung, die
      ein MuPiBox-Update überlebt:

          # /etc/systemd/system/mupi_mqtt.service.d/nur-wenn-aktiv.conf
          [Service]
          ExecCondition=/usr/bin/python3 -c "import json,sys; sys.exit(0 if (json.load(open('/etc/mupibox/mupiboxconfig.json')).get('mqtt') or {}).get('active') else 1)"

      **Warum ExecCondition und nicht ExecStartPre:** scheitert eine
      `ExecCondition`, gilt der Dienst als ÜBERSPRUNGEN (`Result=exec-condition`),
      nicht als fehlgeschlagen. Genau das ist richtig — „nicht eingerichtet"
      ist kein Fehler. Mit `ExecStartPre` stünde er wieder in `--failed`,
      und man hätte das Symptom nur verschoben.

      ── BEIDE RICHTUNGEN BELEGT ────────────────────────────────────────

          active=false  ->  inactive, Result=exec-condition, NICHT in --failed
          active=true   ->  active, Result=success, läuft

      Rezeptschritt `mqtt-reparieren`; auf Pi 5 und Pi 4 angewandt.

      **Noch offen für den, der MQTT wirklich nutzen will:** einen echten
      Broker eintragen (`mqtt.broker` steht noch auf dem Beispielwert) und
      `active` einschalten. Der Dienst ist `enabled`, startet also von selbst
      mit, sobald der Schalter steht.
    source:
      - "Session 2026-07-30, auf beiden Boxen repariert und in beide Richtungen geprüft"

  - kind: falle
    id: mupi-aussehen-angleichen
    title: "Zweite Box sieht anders aus: das Aussehen steckt an DREI Stellen"
    tags: [darstellung, themen, abgleich, kiosk]
    body: |
      „Das ist ne komische Mischung" — und zwar zu Recht. Wer zwei Boxen
      gleich aussehen lassen will, muss an drei Stellen gleichzeitig
      hinsehen; jede allein erklärt das Bild nicht.

      ── 1. Die Themen waren RÜMPFE ─────────────────────────────────────

          Hauptbox:  MuPiBox Classic 48 · New MuPiBox 44 · Tiger Mupi 44 Felder
          Zweitbox:  MuPiBox Classic 17 · New MuPiBox 11 · Tiger Mupi 18 Felder

      Ein Thema mit 11 Feldern setzt beim Anwenden elf Werte und lässt die
      übrigen 37 stehen, wie sie gerade waren. **Das IST die Mischung.**

      ── 2. Das ALTE Farbthema, ein zweiter Mechanismus ─────────────────

      Neben der Darstellung gibt es noch den ursprünglichen MuPiBox-Weg:
      `www/active_theme.css` ist ein **VERWEIS** nach
      `~/MuPiBox/themes/<name>.css`, gesetzt aus `mupibox.theme` durch
      `setting_update.sh`. 36 Byte Dateigröße = Länge des Verweisziels, nicht
      der Inhalt — wer `stat` traut und `cat` nicht, sucht lange.

      Die Zweitbox stand auf `blue`, die Hauptbox auf einem eigenen
      `MupiNew`, das dort gar nicht existierte. Also: Datei mitliefern UND
      `mupibox.theme` setzen.

      ── 3. Der Kiosk schreibt den ALTEN Stand zurück ───────────────────

      Nach dem Schreiben der Darstellung sprang alles zurück. Der laufende
      Kiosk hält den Stand im Speicher und speichert ihn wieder. Abhilfe ohne
      Neustart (der Kiosk darf NICHT einzeln neu gestartet werden):

          touch $B/www/main-*.js

      Der eingebaute Wächter vergleicht Dateiname + Zeitstempel über
      `/api/oberflaeche/stand`, lädt neu und übernimmt.

      ── UND DIE HÜLLE, schon einmal teuer bezahlt ──────────────────────

      `PUT /api/darstellung` will `{aktuell, themen}`. Das innere Objekt
      allein ergibt **200 OK** und danach `aktuell: null` — die ganze
      Darstellung weg (siehe [[mupi-wolke-frei-stehend]]).

      Rezeptschritt `darstellung-angleichen` macht alle drei Schritte samt
      Hüllen-Prüfung. Er ÜBERSCHREIBT das Aussehen — für eine frische
      Zweitbox richtig, für eine eingerichtete nicht.
    source:
      - "Session 2026-07-30, Pi 4 an Pi 5 angeglichen"

  - kind: offen
    id: mupi-rueckstand-2026-07-30
    title: "Rückstand nach dem Akku-/MuPiHAT-Tag (2026-07-30)"
    tags: [rueckstand, akku, mupihat, ton, verwaltung]
    body: |
      Was an DIESEM Tag offen blieb — mit dem Grund, warum es liegen blieb.
      Der ältere Rückstand steht in [[mupi-offene-optimierungen]]; die drei
      Einzelpunkte [[mupi-kiosk-als-dietpi]], [[mupi-jellyfin-stumm]] und
      [[mupi-classic-albumliste]] gelten unverändert weiter.

      ── ENTSCHEIDUNG NÖTIG, nicht Arbeit ───────────────────────────────

      **`idlePiShutdown` steht auf 0** — die Box schaltet sich NIE von selbst
      ab. Am Netz egal; am Akku läuft der Pack leer, während sie nur dasteht
      (gemessen 4,6–6,4 W im Leerlauf). Am 2026-07-30 ist der Pi 5 vermutlich
      genau daran ausgegangen. Rund 30 Minuten wären naheliegend, aber das ist
      eine Verhaltensänderung — sie gehört entschieden, nicht nebenbei
      gesetzt. (`idleDisplayOff` = 10 min wirkt bereits.)

      **PipeWire-Lautstärkemodell** (steht seit dem Pi-5-Umbau, siehe
      [[mupi-lautstaerke-stand-2026-07-27]]). Heute kam ein Argument DAFÜR,
      es endlich zu klären: die von PipeWire dauerhaft gemerkte
      librespot-Dämpfung von 20 % war die halbe „Spotify geht nicht"-Ursache
      ([[mupi-spotify-drei-ursachen]]). Solange die Frage offen ist, bleibt
      der Pi 4 bei PulseAudio — die beiden Boxen sind hier ABSICHTLICH
      verschieden.

      ── MESSBAR, aber noch nicht gemacht ───────────────────────────────

      **Die librespot-Dämpfung braucht eine Abbruchsicherung.** Warum sie nie
      zurückgesetzt wurde, ist ungeklärt; der Rückweg braucht dasselbe
      `trap`/EXIT wie die Übergabe-Skripte.

      **Die echte Kapazität fehlt noch.** Der Lernteil braucht einen Hub von
      mindestens 20 Prozentpunkten; am 2026-07-30 kam er über 57 → 45 % nicht
      hinaus. Einmal die Box am Akku leerlaufen lassen, dann rechnet sie aus,
      wie viel von den 10 Ah der Pack heute noch hat.

      **Ausreißer in den Strommessungen.** `IBus` lieferte einmal 9892 mA und
      einmal 2446 mA — bei einer Hardwaregrenze von 1780 bzw. 2230 mA. Das
      sind ADC-Fehlmessungen. Für die Kurve unkritisch, für eine Auswertung
      sollte alles oberhalb der Eingangsgrenze verworfen werden.

      ── ANGEBOTEN, auf Zuruf ───────────────────────────────────────────

      **Board-Ansicht mit blinkender LED.** Zwei der vier LEDs sind belastbar
      ableitbar, zwei nur erschließbar ([[mupi-hat-leds-lesbar]]). Schaltplan
      und Layout liegen nur als Grafiken im Hersteller-Datenblatt — eine
      EIGENE, vereinfachte Zeichnung aus den Steckerlisten wäre der saubere
      Weg. Rechtefrage, deshalb nicht ungefragt begonnen.

      **`spotifyd` ganz aus der Dienstliste nehmen.** Er ist als „abgelöst"
      markiert ([[mupi-dienste-durchgesehen]]) und hat auf KEINER Box ein
      Programm dahinter — der einzige der drei, bei dem Entfernen gefahrlos
      wäre.

      ── HARDWARE, beim nächsten Anfassen ───────────────────────────────

      **JP2 am Pi 5 prüfen.** Wenn der Strom über den Pi statt über den HAT
      kommt und JP2 geschlossen ist, sagt das Datenblatt „the HAT will be
      damaged". Am 2026-07-30 bestand diese Lage zeitweise.

      **Akkuprofil des Pi 4**, falls dort je ein ANDERER Pack hängt: aktuell
      steht ENERpower, weil es derselbe Akku ist, der zwischen den Boxen
      wandert. Mit einem anderen Pack muss das Profil mitwandern — eine zu
      tiefe Abschaltschwelle schadet den Zellen.

      ── SPAETER ERLEDIGT ───────────────────────────────────────────────

      **Gelöschte Mediathek-Einträge in der Verwaltung** — am 2026-08-02
      gebaut: eigene Karte „Bei Spotify nicht mehr vorhanden", Sammelentfernen
      mit datierter Sicherung und Rückweg, und die neue Oberfläche
      kennzeichnet tote Kacheln jetzt ebenfalls. Die Entscheidung, WAS
      angeboten werden darf, liegt rein und getestet in `aufraeumen.ts`.
      Siehe [[mediathek-aufraeumen]].

      ── ERLEDIGT an diesem Tag ─────────────────────────────────────────

      Akkuanzeige + feiner Ladestand + Verlaufskurve + Lernteil
      ([[mupi-akku-anzeige]], [[mupi-akkuverlauf]], [[mupi-akku-lernen]]) ·
      HAT-Diagnose + Protokoll-Ausleitung ([[mupi-hat-fehlerbericht]]) ·
      Platinenstand aus der Eingangsgrenze ([[mupi-mupihat-revisionen]]) ·
      Dienste durchgesehen, MQTT repariert ([[mupi-dienste-durchgesehen]],
      [[mupi-mqtt-reparatur]]) · Aussehen der zweiten Box angeglichen
      ([[mupi-aussehen-angleichen]]) · alles in die Rezepte gezogen.
    source:
      - "Session 2026-07-30"

  - id: mixpi-maskottchen-familie
    title: "MixPi: ein Maskottchen als Bauteil — Zustaende, Zuordnungsdatei und die Regeln, die es vor Laerm bewahren"
    body: >
      Die Box bekommt ein Maskottchen ("MixPi", ein Wesen mit Kopfhoerern) fuer die
      Stellen, an denen sonst nichts oder ein nuechterner Satz stuende. Der Rename
      MuPiBox -> MixPiBox ist angekuendigt (Backlog E14: 1073 Vorkommen in 151 Dateien).

      NICHT EIN BILD, SONDERN EINE FAMILIE. Jeder Zustand hat sein eigenes:
      `mixpi-<zustand>.png`, klein und deutsch. Zehn: hoert (Standard), spielt,
      kein-bild, schlaeft, farben, gestalten, blase, sucht, passwort, fehler.
      Zusammen 596 kB. Erzeugt werden sie mit `tools/maskottchen-bauen.py` aus
      Vorlagen in `NewDesign/bilder/quellen/` - welche Vorlage welcher Zustand
      wird, ist eine ENTSCHEIDUNG und steht in `maskottchen.json` im Feld
      `quelle`. Vierzehn weitere Vorlagen haben keinen Ausloeser im Code und
      bleiben unerzeugt unter `_sammlung` (Vorschlag: das Kind waehlt sein
      Begruessungsbild selbst). Zum Aufbereiten siehe
      [[bilder-aufbereiten-drei-fallen]].
      Ablage `NewDesign/bilder/`, PNG mit durchsichtigem Grund; die Bau-Regel in
      `src/frontend-box/angular.json` kopiert alles nach `www/neu`, damit es unter
      `/neu/bilder/...` OHNE NETZ erreichbar ist.

      DIE ZUORDNUNG STEHT IN EINER DATEI, NICHT IM CODE: `NewDesign/maskottchen.json`
      haelt je Zustand `datei`, `alt` (Bildbeschreibung), `text` (Vorgabesatz oder
      null), `vorlesen` (ob Vorlesen sinnvoll ist) und `wo`. Grund: die klassische
      Angular-App und die neue Oberflaeche sollen dieselbe Quelle lesen, statt jede
      ihre eigene Liste zu pflegen — sonst driften die Zustaende auseinander, sobald
      einer dazukommt.

      DREI REGELN, DIE DAS BILD VOR DEM ABGLEITEN INS DEKORATIVE BEWAHREN:
      (1) Nie ohne Text — das Bild traegt die Stimmung, der Satz traegt die Auskunft.
      (2) Nie als Ladeanzeige — wer laedt, will Bewegung sehen, kein stehendes Bild.
      (3) Hoechstens eines je Bildschirm — sonst wird aus einem Begleiter ein Rudel.

      ZU (3), ZUSTAND VOM 05.08.2026 abends: Die Regel war zwischenzeitlich
      GEBROCHEN und ist es teilweise noch. Vom 03. bis 05.08.2026 stand ein
      MixPi im Wappen unten links UND eines oben links im Zeichen „wer hoert" —
      auf der Startseite also zwei, beim Spielen ohne Cover drei (`#mp-cover`
      faellt auf `mixpi-kein-bild.png`). Seit das Wappen sein Bild verloren hat
      ([[ein-wunsch-zwei-zielorte-ein-gebautes-ziel]]) ist der Normalfall
      wieder EINES. Nicht geheilt sind: der Leer- und der Fehlerzustand
      (`.zustand-bild` mit `mixpi-sucht`/`mixpi-fehler`) und das Ersatzcover in
      der Kachel, das der SERVER per 302 schickt — bei einem Dienstausfall
      traegt es jede Kachel gleichzeitig. Wer die Regel ganz halten will, muss
      dort weitermachen, nicht am Zeichen oben links.

      ORTSANGABE VON `hoert`, berichtigt: „Zeichen ‚wer hoert‘ oben links,
      solange kein Kind ein eigenes Bild gewaehlt hat — das Standardbild". Bis
      zum 03.08.2026 stand es in der Kopfzeile, danach kurz im Wappen. Der
      Wert in NewDesign/maskottchen.json und die Tabelle in
      NewDesign/MASKOTTCHEN.md sind mitgezogen.

      DIE WICHTIGSTE UNTERSCHEIDUNG: `kein-bild` (Schulterzucken) gegen `fehler`
      (rotes Warndreieck). Ein fehlendes Cover ist ALLTAG — Spotify antwortet nicht,
      der Jellyfin-Server ist aus, die lokale Datei traegt kein eingebettetes Bild.
      Wer das wie einen Defekt zeichnet, dem glaubt man das Warndreieck nicht mehr,
      wenn wirklich etwas kaputt ist. Umgekehrt gilt: `fehler` ERSETZT NIE die
      Fehlermeldung — der Grund muss im Klartext danebenstehen, sonst weiss der
      Erwachsene nicht, was zu tun ist.

      ZWEI AUSNAHMEN VON REGEL (1), beide aus der Form begruendet: `kein-bild` steht
      IN der Kachel an Stelle des Covers, wo der Titel bereits daneben steht — ein
      zusaetzliches "kein Bild" waere Laerm. Und weil es bei einem Dienstausfall
      JEDE Kachel traegt, muss es bei rund 160 px lesbar und klein sein (Richtwert
      unter 20 kB), nicht bei 1024. Zweite Ausnahme: `gestalten` im Theme-Designer,
      wo der Kontext die Auskunft schon gibt.

      EINHAENGEPUNKT FUER DAS ERSATZBILD: `src/backend-api/src/server.ts` — die
      Konstante `KEIN_BILD` ist der Rueckfall von `GET /api/bild/:schluessel` und
      bedient damit die neue Oberflaeche. Es ersetzt das heutige `nocover_mupi.png`
      (58 kB), das ausserdem an vier Stellen der Angular-App steckt
      (artwork.service.ts, spotify-browse.ts, now-playing.ts, medialist.page.html).
      Die dort auszutauschen ist eine SICHTBARE Aenderung an der alten Oberflaeche —
      die beiden sollen getrennt bleiben, also nur nach Ruecksprache.

      DIE SPRECHBLASE IST EIN BAUTEIL, KEIN BILD: `mixpi-blase.png` hat eine LEERE,
      durchsichtige Blase. Der Text wird ueberlagert, nicht eingebrannt — dadurch
      kann dasselbe Bild jeden Tag etwas anderes sagen. Das Textfeld wird in PROZENT
      der Bildgroesse angegeben (nicht in Pixeln), sonst rutscht es beim Skalieren
      heraus; Schriftgroesse per `clamp()` mitwachsend.

      UND SIE KANN SPRECHEN: die Box hat bereits eine Stimme —
      `GET /api/vorlesen/sprich?text=...`, mit `&silben=1` in Silben zerlegt, fuer
      Leseanfaenger gedacht (`vorlese.service.ts`, Modi aus | antippen | lernen).
      Ein Tipp auf die Blase liest vor, was darin steht: keine neue Technik, nur die
      Verbindung zweier vorhandener Dinge. ABER: Vorlesen ist eine Einstellung, kein
      Automatismus — steht der Modus auf `aus`, bleibt die Blase still.

      `mixpi-passwort.png` fuellt eine Luecke im PIN-Dialog der Einstellungssperre
      (die Einstellung heisst `mupibox.einstellungssperre`; einen eigenen Eintrag
      dazu gibt es hier noch nicht), der bisher nur eine nuechterne Zahlenreihe zeigt.
      Der Satz dazu ("Das duerfen nur Grosse.") erklaert einem Kind ohne Vorwurf,
      warum es hier nicht weitergeht.
    tags: [oberflaeche, maskottchen, mixpi, neue-oberflaeche, gestaltung, barrierefreiheit]
    source:
      - "Session 2026-07-31 — Bilder vom Nutzer geliefert, Regeln beim Verdrahten entstanden"
      - "NewDesign/MASKOTTCHEN.md und NewDesign/maskottchen.json im Repo achim/box"

  - id: arbeitsweise-werkzeug-statt-wegwerfbefehl
    title: "Arbeitsweise: wer untersucht, schreibt ein Werkzeug — kein Wegwerf-Einzeiler"
    body: >
      GRUNDSATZ (vom Betreiber gesetzt, 2026-07-31): Untersuchen und Testen heisst,
      eine benannte Datei in `tools/` zu schreiben, die beim naechsten Mal wieder
      laeuft — nicht eine Kette aus grep und awk, die nach der Antwort verschwindet.
      Dazu gehoert ein Eintrag hier ins Wissenspaket.

      WARUM: Ein Werkzeug kostet einmal Aufwand, ein nachgebauter Einzeiler jedes
      Mal neu — und er wird beim zweiten Mal SCHLECHTER, weil die Feinheiten des
      ersten Laufs nicht mehr da sind. Beispiel aus derselben Sitzung, in der die
      Regel entstand: die Boxen wurden per Ping-Sweep von Hand gesucht, obwohl
      `tools/welche-box.sh` genau dafuer schon existierte. Beim Sweep fiel ausserdem
      auf, dass 254 GLEICHZEITIGE Pings unzuverlaessig sind (einmal 27 Treffer,
      einmal 3) und man in Bloecken scannen muss — eine Erkenntnis ueber das MESSEN
      selbst, die in einer Chat-Antwort verloren gegangen waere.

      WO WERKZEUGE HINGEHOEREN — zwei Orte, nicht einer:
      `MuPiBox/tools/` fuer das Untersuchen, Messen und Pruefen am Entwicklungsrechner
      (`mupi-check.py` prueft die ganze Kette von unten nach oben, `welche-box.sh`
      bestimmt die Maschine aus dem Geraetebaum statt aus dem Namen, `mitmessen.mjs`
      misst Klick-Reaktionszeiten und leitet daraus Normalwerte ab, `pruefen.sh`
      faehrt alles Pruefbare in einem Lauf, `wiki-schau.py` haelt DIESES Wissenspaket
      zusammenhaengend, `mupi-ton.py`, `mupihat-eingang.py`).
      `remote-step-installer/tools/` fuer das, was AUF der Box laeuft und vom Rezept
      eingerichtet wird (`mupibox-network-sync.sh`, `mupibox-touch-bridge.py`,
      `force-dsi-output.sh`, `mupibox-bootwache.py`).

      DIE HAUSFORM: Jedes Werkzeug hier beginnt mit einem Kopfkommentar "WOZU" bzw.
      "WOFUER", der die KONKRETEN KOSTEN nennt, die zu ihm gefuehrt haben — nicht
      abstrakt ("nuetzlich fuer Diagnose"), sondern belegt: "der Name mupibox hat an
      EINEM Tag zweimal auf verschiedene Rechner gezeigt ... Kosten: eine halbe
      Stunde und zwei Richtigstellungen". Dazu ein Abschnitt "WAS ES NICHT TUT".
      Beides zusammen sagt dem naechsten Leser, wann das Werkzeug das richtige ist
      und wann nicht — und verhindert, dass jemand ein zweites daneben baut.

      REIHENFOLGE: erst in `tools/` nachsehen, ob es schon etwas gibt, dann
      erweitern; erst als Letztes neu anlegen. Dieselbe Regel gilt fuer dieses
      Wissenspaket: hier nachschlagen, statt neu zu erfinden.
    tags: [arbeitsweise, werkzeuge, tools, diagnose, konvention]
    source:
      - "Betreiber-Vorgabe, Session 2026-07-31"
      - "belegt an den vorhandenen Werkzeugen in achim/box tools/ und achim/remoteinstaller tools/"

  - kind: falle
    id: wiki-doppelte-id-faellt-durch-safe-load
    title: "Eine id war zweimal vergeben — und die uebliche Pruefung sagte trotzdem \"gueltig\""
    sev: mittel
    match: "doppelte id|pack.yaml|safe_load|Verweis ins Leere|Eintrag nicht gefunden"
    cause: "`yaml.safe_load` prueft die FORM der Datei, nicht die Stimmigkeit der Eintraege — ids stehen als Werte in einer Liste, und doppelte Werte sind YAML gleichgueltig"
    fix: "`tools/wiki-schau.py --pruefen` (haengt in tools/pruefen.sh): doppelte ids, Eintraege ohne id, Verweise ins Leere in BEIDEN Klammerformen, doppelte Schluessel"
    tags: ['wiki', 'pack.yaml', 'werkzeuge', 'pruefung', 'falle', 2026]
    source: "beim Aufloesen der doppelten id am 02.08.2026 gefunden und mit dem Werkzeug nachgestellt"
    body: |
      DER FALL: `mupi-kiosk-als-dietpi` stand ZWEIMAL da — einmal als howto
      "Kiosk als normaler Benutzer statt als root starten", einmal als offener
      Punkt "Autologin allein genuegt nicht". Zwei verschiedene Dinge unter
      einem Namen. Wer die id nachschlaegt, bekommt irgendeinen der beiden.

      WARUM ES SO LANGE UEBERLEBTE: Die uebliche Pruefung lautet
      `yaml.safe_load(open('pack.yaml'))` und sagt "gueltig". Das ist richtig
      und beantwortet die falsche Frage. Die ids sind WERTE in einer Liste,
      nicht Schluessel einer Abbildung — doppelte Werte sind YAML voellig
      gleichgueltig. Kein Absturz, keine Meldung. Der Zustand hielt zwei
      Wochen und drei Sitzungen; zwei davon haben ihn GEMELDET und bewusst
      liegengelassen, weil nichts ihn erzwang. Genau daran erkennt man den
      Unterschied zwischen "aufgeschrieben" und "abgesichert".

      DIE ZWEITE HAELFTE DESSELBEN FEHLERS: Aufloesen heisst umbenennen, und
      ab da zeigt jeder `[[…]]`-Verweis auf die alte Kennung anderswo ins Leere. Auch das meldet
      niemand — ein Verweis, der nirgendwohin fuehrt, sieht im Text aus wie
      einer, der trifft. Beim Aufloesen musste ein solcher Verweis von Hand
      mitgezogen werden, und er stand in der EINFACHEN Klammerform
      (`[id]`), nicht in der doppelten. Deshalb prueft das Werkzeug beide.

      DABEI GLEICH MITGEFUNDEN: zwei Verweise, die nie ein Ziel hatten —
      `mupi-einstellungssperre` und `wiki-zuerst-lesen`. Beide Namen
      klingen wie Eintraege, beide wurden im Fliesstext erfunden und nie
      angelegt. Sie stehen jetzt ohne Klammern da, weil ein Verweis, der
      nichts verspricht, ehrlicher ist als einer, der ins Leere zeigt.
      (NACHTRAG 08.08.2026: an DIESER Stelle standen sie trotzdem weiter in
      Klammern — der Satz beschrieb, was gemeint war, nicht was dastand. Wer
      einen toten Verweis als BEISPIEL zitiert, erzeugt ihn dabei ein zweites
      Mal; jede Pruefung, die Klammern zaehlt, meldet ihn zu Recht. Beispiele
      gehoeren ohne Klammern gesetzt. Nachgemessen mit
      [[pack-verweise-werkzeug]].)

      NOCH EINE STILLE STELLE, dieselbe Bauart: doppelte SCHLUESSEL in einem
      Eintrag. Da schluckt `safe_load` wirklich etwas — es behaelt den
      LETZTEN. Ein zweites `body:` loescht das erste ohne ein Wort, und
      "gueltig" sagt die Pruefung auch dann. Wird mitgeprueft.

      DIE REICHWEITE DIESES EINTRAGS ENDET AN `llmwiki/`. Steht der tote
      Verweis in einem Quelltextkommentar, einem Werkzeugkopf oder einem
      Handbuch, findet ihn `wiki-schau.py` NIE — dafuer gibt es
      [[verweis-ins-gedaechtnis-steht-ueberall-nur-nicht-im-paket]] und
      `tools/wiki-verweise-im-code-pruefen.py`. Beide Eintraege fuehren
      „Verweis ins Leere" im `match:`; die trennende Frage ist allein, in
      WELCHER Datei die Kennung steht.
    related: [verweis-ins-gedaechtnis-steht-ueberall-nur-nicht-im-paket]

  - id: box-im-netz-suchen-zwei-fallen
    title: "Eine Box im Netz suchen: warum gleichzeitige Pings luegen und die Standardroute ins VPN fuehrt"
    body: >
      Die Boxen bekommen ihre Adresse per DHCP. Ist eine laenger aus, taucht sie
      woanders wieder auf — dann steht im Inventar die alte Adresse und man muss
      suchen. Dabei gibt es ZWEI Fallen, die beide still danebenliegen statt zu
      scheitern; das ist das Gefaehrliche daran.

      FALLE 1 — ALLE ADRESSEN GLEICHZEITIG ANPINGEN LIEFERT FALSCHE ERGEBNISSE.
      254 nebenlaeufige `ping`-Aufrufe ueberlasten den Weg: Antworten gehen
      verloren. Am 2026-07-31 gemessen — DERSELBE Aufruf fand einmal 27 Rechner,
      kurz darauf nur 3. Wer daraus schliesst "die Box ist weg", sucht danach am
      falschen Ende. In vier Bloecken zu 64 ist das Ergebnis reproduzierbar (im
      Beispiel stabil 30 Rechner) und dauert trotzdem nur Sekunden.

      FALLE 2 — `ip route get 1.1.1.1` LIEFERT NICHT DAS HEIMNETZ. Auf diesem
      Entwicklungsrechner laeuft ein VPN; die Standardroute zeigt nach
      10.14.0.0/24. Eine Suche dort findet genau einen Rechner und meldet "keine
      Box gefunden" — obwohl die Box im 192.168.178.0/24 haengt und laeuft. Das
      Praefix gehoert deshalb aus dem INVENTAR genommen
      (`remote-step-installer/boxen.yaml`), nicht aus der Route: Boxen sucht man
      da, wo Boxen wohnen.

      BILLIGES ERKENNUNGSMERKMAL: nicht jeden gefundenen Rechner per SSH befragen
      — das dauert je Rechner Sekunden und klopft fremde Geraete im Netz ab. Wer
      auf Port 8200 antwortet, ist eine Box (das Backend liefert dort aus, je
      nach Zertifikatslage http oder https — beides probieren).

      FERTIG IN: `tools/welche-box.sh --suchen` im Repo achim/box. Es gleicht den
      Fund anschliessend mit dem Inventar ab und sagt, welche Adresse dort
      nachzuziehen ist — sonst sucht man beim naechsten Mal wieder.
      Siehe [[arbeitsweise-werkzeug-statt-wegwerfbefehl]].
    tags: [netzwerk, diagnose, werkzeuge, box-unerreichbar, dhcp]
    source:
      - "am eigenen Netz gemessen, 2026-07-31 (Pi 5 unter .169 gefunden, Pi 4 aus)"

  - id: bilder-aufbereiten-drei-fallen
    title: "Erzeugte Bilder fuer die Box aufbereiten: drei Fallen, die alle STILL danebenliegen"
    body: >
      Wer Bildmaterial fuer die Oberflaeche bekommt (Maskottchen, Symbole, Kacheln),
      laeuft in drei Fallen, die eines gemeinsam haben: Sie scheitern nicht, sie
      liefern ein FALSCHES Ergebnis, das auf dem Entwicklungsrechner richtig
      aussieht. Alle drei am 2026-07-31 an 26 gelieferten MixPi-Bildern erlebt.

      FALLE 1 — "TRANSPARENT" IM DATEINAMEN HEISST NICHTS. Von 24 Dateien hatte
      KEINE einen Alphakanal, auch die vier nicht, die `...-transparent.png`
      hiessen. Auf hellem Grund faellt das nie auf; sobald die Box ein dunkles
      Thema traegt, sitzt die Figur in einem hellgrauen Kasten. Pruefen laesst es
      sich nur im Bild selbst: `Image.open(f).mode` bzw. der Anteil der Pixel mit
      Alpha 0. ACHTUNG, zweite Stufe derselben Falle: ein VORHANDENER Alphakanal
      ist nicht dasselbe wie ein BENUTZTER - ein RGBA-Bild mit lauter 255 ist
      genauso undurchsichtig.

      FALLE 2 — DIE FIGUR HAT FAST DIE FARBE DES HINTERGRUNDS. MixPi ist weiss
      (254), der Hintergrund hellgrau (247): sechs Stufen. Jede Maske nach Farbe
      oder Helligkeit radiert die Figur zur Haelfte mit weg. Was traegt, ist die
      dunkle KONTUR: nicht nach Farbe maskieren, sondern vom Bildrand her FLUTEN.
      Die Flut bleibt an der Kontur stehen, und weisse Flaechen innerhalb bleiben
      erhalten, obwohl sie farblich kaum vom Hintergrund zu unterscheiden sind.
      Am Beispielbild wurden so 23 % der Bildflaeche gerettet, die eine Farbmaske
      ausradiert haette.

      Dazu gehoert eine Nachlese: Hintergrund, der den Bildrand NICHT beruehrt
      (zwischen Kopfbuegel und Kopf), bleibt als heller Fleck stehen. Er ist am
      MEDIAN eines ganzen Gebiets erkennbar - der wackelt bei tausenden Pixeln
      nicht, waehrend am Einzelpixel nichts zu entscheiden ist. Entscheidend ist
      ein ABSOLUTER Vergleich mit dem Hintergrund draussen (Abstand <= 3), kein
      relativer: Ein erster Versuch nahm das groesste eingeschlossene Gebiet als
      "Figurenweiss" und kippte bei zwei Bildern um - dort wurden prompt die
      reinweissen Flaechen ausgestanzt, Loecher mitten in der Figur.

      FALLE 3 — DAS TRANSPARENZ-KARO KANN EINGEBRANNT SEIN. Werkzeuge zeigen
      Transparenz als graues Schachbrett; beim Ausgeben landet es als echte Pixel
      in der Datei. Der Hintergrund hat dann ZWEI Farben (gemessen: 237 und 254,
      blockweise wechselnd). Wer nur eine entfernt, behaelt das halbe Karo - und
      weil die hellere davon genau das Figurenweiss trifft, sieht das Ergebnis
      auf weissem Grund SOGAR RICHTIG aus.

      DIE GEGENPROBE, die alle drei sichtbar macht: das Ergebnis auf SCHWARZEM
      Grund ansehen. Dort faellt jeder stehengebliebene Saum, jedes Karo und jeder
      helle Fleck sofort auf. Auf weissem Grund niemals.

      NEBENBEI DAS GEWICHT: 26 MB fuer 26 Bilder. Freigestellt, zugeschnitten und
      auf 256 px gebracht sind es rund 60 kB je Bild. Die Vorlagen gehoeren
      deshalb in einen Ordner, den die Bau-Regel AUSNIMMT
      (`bilder/quellen/**` in `src/frontend-box/angular.json`) - sonst liegen sie
      auf der SD-Karte.

      FERTIG IN achim/box: `tools/bilder-pruefen.py` (findet solche Bilder),
      `tools/bilder-freistellen.py` (behebt alle drei Faelle, `--probe` legt die
      Gegenprobe auf Schwarz ab), `tools/kontaktbogen.py` (viele Bilder
      nummeriert auf einem Blatt, auf dunklem Grund),
      `tools/maskottchen-bauen.py` (erzeugt die benannten Bilder aus
      `NewDesign/maskottchen.json`). Siehe
      [[arbeitsweise-werkzeug-statt-wegwerfbefehl]].
    tags: [oberflaeche, bilder, transparenz, werkzeuge, gestaltung, mixpi]
    source:
      - "an 26 gelieferten MixPi-Bildern erlebt und behoben, Session 2026-07-31"

  - id: pruefen-schritte-die-still-luegen
    title: "Pruefschritte, die LUEGEN statt zu scheitern - drei Faelle aus tools/pruefen.sh"
    body: >
      Am 2026-07-31 meldete `tools/pruefen.sh` zwei von zehn Schritten als Fehler.
      Beide lagen am PRUEFEN, nicht am Geprueften. Beim Beheben kam ein dritter
      Fall dazu, der noch unangenehmer ist: ein Schritt, der GRUEN aussieht,
      obwohl er nichts geprueft hat.

      Der gemeinsame Nenner: Alle drei sind stumm. Ein Pruefschritt, der immer rot
      ist, ist schlimmer als keiner - man gewoehnt sich an ihn und uebersieht den
      Tag, an dem er zu Recht rot wird.

      FALL 1 — esbuild-wasm STUERZT AB, WENN MAN SEINE AUSGABE UMLEITET.
      `RangeError: Invalid array length` in `fs.writeSync`
      (esbuild-wasm/bin/esbuild:69, Node 26). Der Bau selbst GELINGT; nur das
      Schreiben der Warnungen bringt den Prozess um. Am Bildschirm - ohne
      Umleitung - laeuft derselbe Aufruf durch, und genau das verschleppt die
      Suche: Wer den Befehl von Hand nachstellt, sieht kein Problem.
      ABHILFE: `--log-level=error`. Fehler brechen weiter ab, nur die Warnungen
      fallen weg.

      FALL 2 — KARMA MELDET "Found 1 load error" OHNE DEN GRUND. Eine einzige
      nicht ladbare Datei reisst den GANZEN Browserlauf mit (hier 289 Tests).
      Zwei Ursachen steckten dahinter:
      (a) `expect(wert, meldung)` ist die Schreibweise von VITEST; Jasmine nimmt
      nur einen Wert und uebersetzt das nicht. Richtig ist
      `expect(wert).withContext(meldung)`.
      (b) Eine Datei mit `import { describe, it } from 'node:test'` kann Karma
      nicht laden. Solche Dateien gehoeren erkennbar benannt (`*.node.spec.ts`),
      in `angular.json` unter `test.options.exclude` vom Karma-Lauf ausgenommen -
      NICHT im tsconfig, dort greift es nicht, weil der Karma-Bauer die Dateien
      selbst einsammelt - und in einem eigenen Schritt gefahren.
      DEN GRUND SIEHT MAN NUR im vollen Ausgabetext von `ng test`, nicht in der
      Zusammenfassung.

      FALL 3 — VITEST FUEHRT `node:test`-DATEIEN NICHT AUS, DRUCKT ABER IHRE
      ZAHLEN. Der Import meldet die Tests bei Nodes eigenem Laeufer an, der sie
      NEBENHER abspult und "pass 19" ausgibt. Vitest selbst findet dabei NULL
      Tests, meldet "no tests" und endet mit 1. Wer auf die gedruckten Zahlen
      schaut, haelt es fuer bestanden; wer auf den Exitcode schaut, haelt es fuer
      gebrochen. Beides ist falsch. RICHTIG ist `npx tsx --test <datei>` - so
      macht es backend-api ohnehin.

      DIE PROBE, die jeden neuen Pruefschritt begleiten sollte: einen Test
      absichtlich kaputtmachen und nachsehen, ob der Schritt WIRKLICH bricht.
      Ohne diese Gegenprobe weiss man nur, dass er gruen sein kann - nicht, dass
      er rot werden kann. Siehe [[arbeitsweise-werkzeug-statt-wegwerfbefehl]].
    tags: [tests, werkzeuge, pruefen, karma, esbuild, vitest, arbeitsweise]
    source:
      - "an tools/pruefen.sh im Repo achim/box behoben, Session 2026-07-31"

  - id: neue-oberflaeche-ohne-box-ansehen
    title: "Die neue Oberflaeche ohne Box ansehen — und warum eine Attrappe dem Vertrag folgen MUSS"
    body: >
      `NewDesign/index.html` laesst sich mit Doppelklick oeffnen, aber ueber
      `file://` scheitert JEDER Abruf: die Seite bleibt beim „wird geladen"
      stehen. Wer etwas an ihr aendert, muesste sonst bauen und auf die Box
      schieben, nur um zu sehen, ob ein Bild an der richtigen Stelle steht — das
      dauert Minuten, und deshalb macht man es nicht.

      FERTIG IN achim/box: `node tools/neu-vorschau.mjs` startet in einer
      Sekunde einen Server mit erfundenen Antworten auf der Rueckschleife
      (http://localhost:8299/neu/). Der Zustand ist im Betrieb umschaltbar:
      `curl localhost:8299/vorschau/{spielt|pause|still|ohne-cover|mit-cover}`.

      DIE LEHRE, die dabei mehr wert ist als das Werkzeug: EINE ATTRAPPE, DIE
      DEM VERTRAG NICHT FOLGT, PRUEFT NICHTS — UND LUEGT DABEI IN DIE FALSCHE
      RICHTUNG. Im ersten Versuch antwortete `/player/local` mit
      `title`/`state`/`position`, wie man es erwarten wuerde. Die Oberflaeche
      zeigte daraufhin GAR KEINEN Player. Das sah aus wie ein Fehler in der
      frisch geaenderten Seite und war einer in der Attrappe — man sucht dann an
      der falschen Stelle, und zwar mit voller Ueberzeugung.

      Die echten Feldnamen stehen in `now-playing.ts:39-57` und heissen
      `currentPlayer` (muss 'mplayer' sein), `playing`, `currentTrackname`,
      `album`, `path`, `timePos`, `duration`. Wer eine Attrappe baut, LIEST sie
      dort ab und schreibt die Fundstelle daneben — sonst ist die Attrappe eine
      zweite, unbelegte Meinung darueber, wie die Schnittstelle aussieht.

      NEBENBEI GEPRUEFT, was sonst niemand prueft: Bei 800x480 — der echten
      Groesse des Waveshare-DSI an der Box — sind `body` und `.rahmen` exakt
      800x480, `scrollWidth` gleich `clientWidth`, also kein Querlauf. Ein
      Bildschirmfoto taeuscht hier: Der Browser zeichnet das verkleinerte
      Fenster in seinen groesseren Bereich, und der leere Rand ringsum sieht aus
      wie ein Layout-Fehler. GLAUBEN sollte man den gemessenen Rechtecken
      (`getBoundingClientRect`), nicht dem Bild.

      WAS DIE VORSCHAU NICHT ERSETZT: den Lauf auf dem Geraet. Ob es hinter
      Chromium im Kiosk taugt, ob die Schrift lesbar ist und ob ein Kinderfinger
      die Knoepfe trifft, sagt nur die Box.
    tags: [oberflaeche, neue-oberflaeche, werkzeuge, tests, attrappe, entwicklung]
    source:
      - "beim Verdrahten der Maskottchen gebaut und benutzt, Session 2026-07-31"

  - id: ausrollen-was-nur-am-geraet-auffaellt
    title: "Ausrollen auf die Box: drei Dinge, die im Test NICHT auffallen koennen"
    body: >
      Am 2026-07-31 wurde die neue Oberflaeche samt Maskottchen auf den Pi 5
      gebracht. Alle Tests waren gruen, die Vorschau am Entwicklungsrechner sah
      richtig aus — und trotzdem kamen beim Ausrollen drei Dinge heraus, die
      keine Pruefung am Schreibtisch finden KANN.

      1) DAS SKRIPT AUF DER BOX WAR DEM REPO VORAUS.
      `/var/lib/dietpi/dietpi-software/installed/chromium-autostart.sh` trug
      zwei Zeilen, die in `scripts/chromium-autostart.sh` FEHLTEN:
      `--hide-scrollbars` und — schwerwiegend — den Aufruf von
      `force-dsi-output.sh`, der Notbremse gegen das SCHWARZE PANEL. Wer das
      Repo-Skript ausrollt, entfernt beides. Der Schaden zeigt sich erst beim
      NAECHSTEN Start, und verdaechtig ist dann die zuletzt geaenderte
      Oberflaeche, nicht das Skript.
      REGEL: Vor dem Ausrollen einer Datei, die auf der Box VON HAND oder von
      einem anderen Rezept angefasst worden sein kann, erst holen und
      vergleichen (`scp` + `diff`), dann ZUSAMMENFUEHREN. Nie blind ersetzen.

      2) DIE ECHTEN DATEN ENTHALTEN FAELLE, DIE KEIN TEST ERFINDET.
      `/api/bild` unterscheidet „nie ein Cover hinterlegt" (404, die Kachel
      zeigt ihren Anfangsbuchstaben) von „hinterlegt, kam nicht an" (Maskottchen).
      Am Geraet zeigte sich: `spotify.service.ts` traegt bei Eintraegen ohne Bild
      die Adresse des ALTEN Platzhalters als `cover` ein
      (`'../assets/images/nocover_mupi.png'`, dort an sechs Stellen). Fuer den
      Endpunkt sah das aus wie ein hinterlegtes Cover — die Unterscheidung lief
      ins Leere, und zwar genau bei den Eintraegen, fuer die sie gebaut wurde.
      Von 11 Eintraegen traf es einen. Behoben, indem ein Cover, das auf
      `nocover_mupi` zeigt, als GAR KEINES gilt.
      REGEL: Nach dem Ausrollen die ECHTE Liste ansehen
      (`server/config/active_data.json`) und zaehlen, welche Formen darin
      wirklich vorkommen.

      3) /dev/fb0 IST NICHT DAS BILD AUF DEM SCHIRM.
      Auf dem Pi 5 mit vollem KMS zeichnet Xorg ueber DRM; der alte
      fbdev-Puffer bleibt SCHWARZ, sobald X uebernommen hat. Ein daraus
      erzeugtes Bild sieht aus wie ein totes Panel und beweist NICHTS.
      Was wirklich Auskunft gibt, ohne ein Werkzeug nachzuinstallieren:
        cat /sys/class/drm/card*-DSI-*/status    -> connected
        cat /sys/class/drm/card*-DSI-*/enabled   -> enabled
        head -1 /sys/class/drm/card*-DSI-*/modes -> 800x480
        ps -eo pid,rss,args | grep chromium      -> ein --type=renderer mit
                                                    dreistelliger MB-Zahl heisst,
                                                    dass wirklich gezeichnet wird
      Und: `pgrep -a -x chromium | grep -o -- "--homepage [^ ]*"` zeigt, WELCHE
      Oberflaeche der Kiosk geladen hat.

      DER WEG, der sich bewaehrt hat: bauen in ein temporaeres Verzeichnis (nie
      nach `src/deploy`, das gehoert root), auf der Box ALLES sichern
      (`server.js.vor-<stempel>`, `www/neu.vor-<stempel>`, Konfiguration), nur
      `www/neu` und `server.js` uebertragen (die klassische Oberflaeche hatte
      keine Laufzeitaenderung), im ERGEBNIS nachsehen statt dem Zeitstempel zu
      glauben (`grep` nach der neuen Zeichenkette im gebauten Bundle), Dienst
      neu starten, API-Antwort abwarten — und erst dann `mupibox.oberflaeche`
      umschalten und neu starten.
      Siehe [[arbeitsweise-werkzeug-statt-wegwerfbefehl]] und
      [[neue-oberflaeche-ohne-box-ansehen]].
    tags: [ausrollen, box, kiosk, dsi, diagnose, arbeitsweise, neue-oberflaeche]
    source:
      - "Ausrollen auf den Pi 5 (192.168.178.169), Session 2026-07-31"

  - id: regal-je-interpret-gegen-verschmelzen
    title: "Regal je Interpret ist NICHT das Verschmelzen von Quellen — zwei Fragen, die staendig verwechselt werden"
    body: >
      Beim Bau der neuen Oberflaeche wurden zwei Dinge vermengt, die gleich
      klingen und verschieden sind. Die Verwechslung kostet Arbeit in die
      falsche Richtung, deshalb steht sie hier.

      REGAL JE INTERPRET: Mehrere ALBEN desselben Interpreten bekommen eine
      Ebene. Verschmolzen wird dabei NICHTS — zwei Alben bleiben zwei Werke,
      sie stehen nur beieinander. Die Regel ist billig (normalisierter
      Interpret) und braucht weder Netz noch Fingerabdruck.

      VERSCHMELZEN VON QUELLEN: Dasselbe ALBUM aus mehreren Diensten wird EINE
      Kachel. Das ist die grosse Sache mit drei Erkennungsstufen
      ([[quellen-verschmelzen]]), und dort gilt ausdruecklich: NIE allein auf
      Interpret+Titel verschmelzen, weil Deluxe, Remaster und Live dieselben
      Namen tragen.

      Wer das eine fuer das andere haelt, baut entweder eine Navigationsebene
      mit einer Maschinerie, die sie nicht braucht — oder er verschmilzt Alben
      nach einer Regel, vor der der eigene Entwurf warnt.

      WO DIE REGAL-REGEL HINGEHOERT: in den SERVER, als Feld am Werk
      (`interpretSchluessel` in werke.ts, gebildet mit dem vorhandenen
      `normal()`). Sonst gibt es sie dreimal: `media.service.ts`
      (sourcesByArtist), `media.ts` (medienVonInterpret) und die neue Seite.
      Genau daraus stammt der bis heute offene Fall
      [[box-kachel-zeigt-fremde-quelle]].

      DREI REGELN, die beim Bau aus dem Verhalten folgten:
      (1) KEIN Schluessel ohne Interpret — und auch keiner, wenn nach dem
      Normalisieren nichts uebrig bleibt (aus "???" wird die leere
      Zeichenkette). Sonst landen alle namenlosen Eintraege in EINEM Regal.
      (2) ERST AB ZWEI Werken ein Regal. Bei einem waere es eine Tuer vor einer
      Tuer: ein Tipp mehr fuer genau dasselbe.
      (3) Die Sammelkachel muss ANDERS AUSSEHEN — ein Mosaik aus bis zu vier
      Covern, nicht das Cover des ersten Albums. Ein Kind, das nicht liest,
      waehlt nach Bildern; eine Sammelkachel mit einem Albumcover verspricht
      dieses Album und liefert eine Liste.

      UND EINER, der nur am Bildschirm auffiel: Bei genau ZWEI Werken darf
      nicht nur das erste Mosaikfeld ueber beide Zeilen laufen — dann bleibt
      rechts unten ein Viertel leer und die Kachel sieht aus wie ein halb
      geladenes Bild. Die Messung meldete dabei brav die richtige Kachelgroesse.
    tags: [oberflaeche, neue-oberflaeche, navigation, werke, gestaltung]
    source:
      - "beim Bau der Regal-Ebene, Session 2026-07-31/08-01"

  - id: zugangsschluessel-im-browser
    title: "Die Box gibt Jellyfin-Schluessel und Spotify-Token an jeden im Netz heraus — und beides ist tragend"
    body: >
      AM 2026-08-01 VON EINEM ANDEREN RECHNER IM NETZ GEMESSEN, ohne Anmeldung,
      beide HTTP 200:

      `GET /api/data` enthaelt den JELLYFIN-SCHLUESSEL. Er steckt als
      `api_key=<32 Zeichen>` in der Cover-Adresse jedes Jellyfin-Eintrags.

      `GET /api/spotify/config` gibt `clientId` und `refreshToken` heraus (auf
      der Messbox 131 Zeichen); `clientSecret` waere dabei, wenn er gesetzt
      ist. Die Box meldete dazu `anmeldungNoetig: false`.

      BEIDES IST KEIN VERSEHEN, SONDERN TRAGEND — und genau das macht es
      unbequem:

      * `spotify-auth.service.ts` liest `clientId` und `refreshToken` von dort
        und speist damit die Spotify-Anmeldung der bestehenden Oberflaeche.
        Die Einrichtung laeuft ausdruecklich ueber eine FREMDE Herkunft
        (`https://<box>:8443`, siehe [[spotify-einrichten-ueber-https-remote-setup]]).
        Ein Zugriffsschutz nur auf die Rueckschleife bricht sie also.
      * Die bestehende Oberflaeche baut ihre Jellyfin-STROMADRESSEN im Browser
        aus dem Schluessel aus `/api/data`. Nimmt man ihn weg, spielt dort kein
        Jellyfin mehr.

      DIE FOLGE FUER DIE NEUE OBERFLAECHE, und sie ist lehrreich: Beim Bau
      galt die Regel „kein Schluessel im Browser". Sie ist als Grundsatz
      richtig — sie KAUFT aber nichts, solange derselbe Schluessel nebenan
      ueber `/api/data` an jeden Browser geht. Eine Tuer abzudichten, waehrend
      das Fenster offen steht, kostet Arbeit ohne Wirkung. Deshalb wurde
      entschieden, die Jellyfin-Wiedergabe zuerst SO zu bauen, wie die alte
      Oberflaeche es tut, und den Umbau als eigenes Vorhaben zu fuehren
      (BACKLOG E15).

      WAS DER UMBAU BRAUCHT: Jellyfin-Cover und -Stroeme ueber die Box fuehren
      (fuer Cover ist es in `/api/bild/:schluessel` SCHON gebaut — dort wird
      `api_key` aus der Adresse entfernt), die Spotify-Erneuerung serverseitig
      erledigen (`webApiToken()` gibt es), und danach `/api/spotify/config` auf
      `deviceName` eindampfen. Die BESTEHENDE Angular-App muss dabei mit
      angefasst werden; das war vorher ausgeschlossen und ist fuer dieses
      Vorhaben ausdruecklich zugelassen.

      NICHT GROESSER MACHEN, ALS ES IST: Die Box haengt im Heimnetz. Wer dort
      ist, kommt heran — das ist die Lage, nicht der Sonderfall. Wie schwer es
      wiegt, haengt daran, wer sonst in diesem Netz ist.


      ══ NACHGEMESSEN 08.08.2026 — DIE EINE HAELFTE IST ZU, DIE ANDERE NICHT

      Wer diesen Eintrag heute auf „behoben" setzt, schreibt eine Unwahrheit.
      Beide Wege noch einmal von der Box aus abgefragt (nur GET, .169:8200):

          GET /api/spotify/config   -> {"deviceName":"MixPiBox",
                                        "eingerichtet":true}   45 Bytes
                                       ZU. Kein clientId, kein refreshToken.
          GET /api/data  OHNE Origin -> HTTP 200, 7764 Bytes,
                                        `api_key` steht drin.  OFFEN.
          GET /api/data  mit fremdem Origin
                                     -> HTTP 403 `fremde-herkunft`

      DER UNTERSCHIED IST DER GANZE PUNKT: Seit dem 07.08.2026 liegt ein
      Herkunftsriegel vor der API ([[herkunftsriegel-und-der-namenstausch]]).
      Er schuetzt vor der FREMDEN WEBSEITE, die ein Elternteil im selben Netz
      aufmacht — und das war der wahrscheinlichste Weg. Er schuetzt NICHT vor
      `curl` im Heimnetz, denn Werkzeuge ohne `Origin` sollen ausdruecklich
      durchkommen (so hat der Betreiber entschieden, siehe dort).

      Der Jellyfin-Schluessel geht also weiterhin an jeden heraus, der im Netz
      ist und eine Anfrage OHNE Herkunft stellt. Was den Eintrag aendert, ist
      nur die Groesse der Tuer, nicht ihre Existenz: BACKLOG E15 (Cover und
      Stroeme ueber die Box fuehren) bleibt offen, und erst danach faellt der
      Schluessel wirklich aus `/api/data` heraus.
    tags: [sicherheit, jellyfin, spotify, api, oberflaeche, backlog]
    source:
      - "von aussen gemessen (curl gegen 192.168.178.169:8200), 2026-08-01"
      - "NACHTRAG 2026-08-04: der Spotify-Teil ist behoben, siehe [[spotify-zugang-bleibt-auf-der-box]]; der Jellyfin-Teil zur Haelfte, siehe [[jellyfin-ueber-die-box-statt-schluessel-im-browser]]"

  - id: spotify-zugang-bleibt-auf-der-box
    title: "Der Browser braucht keinen Spotify-Zugang mehr — und die Frage, die vorher zu messen war"
    kind: architektur
    tags: [spotify, sicherheit, api, oberflaeche, e15, 2026]
    source:
      - "gebaut und gemessen am 2026-08-04 (BACKLOG E15/S3+S4)"
      - "tools/spotify-erneuerung-wer.py, tools/zugangsschluessel-messen.py"
      - "NACHTRAG 2026-08-04 (Gegenlesen): der Zweig, der die eingerichtete Box als verbunden zeigt, griff nie — siehe [[merker-den-eine-anfrage-setzt-steht-beim-aufschlagen-noch-nicht]]. Und die Kopie des Erneuerungsmerkmals liegt im localStorage jedes Browsers WEITER, der die alte Fassung je geladen hat; kein Weg raeumt sie weg (Marke `_fromBox`)"
    body: |
      WAS SICH GEAENDERT HAT: `/api/spotify/config` nennt seit dem 04.08.2026
      nur noch `deviceName` und `eingerichtet` (ein Boolean). Vorher gab der
      Weg `clientId` und `refreshToken` an JEDEN im Netz heraus, ohne
      Anmeldung — kein Versehen, sondern tragend.

      DER EINE REST, der es tragend machte, war am Ende eine Zeile:
      `queueTrack`. Alles Lesende laeuft laengst ueber die Durchreiche
      `/api/spotify/web/…` (weil Spotify Browser-Aufrufe mit 403 beantwortet,
      siehe [[spotify-web-api-nicht-mehr-aus-dem-browser]]). Uebrig blieb das
      ANHAENGEN an die Warteschlange — schreibend, und Wiedergabe ist das
      Einzige, was Spotify aus einem Browser noch beantwortet. Der Weg heisst
      jetzt `POST /api/spotify/warteschlange` und laesst NUR
      `spotify:track:`/`spotify:episode:` mit 22 Zeichen durch.

      BEWUSST NICHT in die Lese-Erlaubnisliste `WEB_API_ERLAUBT` aufgenommen:
      dann waere die Durchreiche keine lesende mehr, und die naechste
      schreibende Form kaeme umsonst mit.

      DIE FRAGE, DIE VORHER ZU MESSEN WAR — und sie ist die eigentliche
      Lehre: Kostet das jemandem seine Anmeldung? Eine Neuanmeldung geht NUR
      an der Box (Spotifys Rueckweg braucht 127.0.0.1, siehe
      [[spotify-rueckweg-haengt-an-der-adresse]]), also waere sie teuer.

      GEMESSEN, NICHT AUS DEM QUELLTEXT GESCHLOSSEN (04.08.2026):

          1  Abspieldienst gibt Zugang heraus      200  JA  (303 Zeichen)
          2  Hintergrundstueck ohne Browser        200  JA
          3  Erneuerung bei Spotify                200  JA  (3600 s,
                                                    KEIN neues Erneuerungsmerkmal)

      Die Box erneuert sich SELBST: der Abspieldienst liest
      `{clientId, refreshToken}` bei JEDER Erneuerung frisch von der Platte
      (spotify-control.ts, `freshSpotifyCreds`), das Hintergrundstueck nimmt
      `webApiToken()`. Der Browser ist nicht mehr in der tragenden Kette.

      DAMIT IST [[box-token-laeuft-ab-sdk-laeuft-trotzdem]] UEBERHOLT, was
      die Abhaengigkeit angeht: „den erneuerte NIEMAND" beschreibt einen
      frueheren Stand. Fuer die Ursachensuche traegt der Eintrag weiter.

      NEBENBEFUND, der eine Annahme bestaetigt: Spotify tauscht das
      PKCE-Erneuerungsmerkmal beim Erneuern NICHT aus. Die Kopie auf der
      Platte bleibt gueltig.

      DIE FALLE, DIE FAST ENTSTANDEN WAERE: Ein Browser ohne eigene Anmeldung
      haette auf der Einrichtungsseite das ANMELDEFORMULAR gesehen — eine
      eingerichtete Box saehe abgemeldet aus, und jemand meldete sie ein
      zweites Mal an, obwohl nichts fehlt. Die Seite fragt deshalb die Box
      (`boxIstVerbunden()`) und sagt geradeheraus: „Die Box ist angemeldet.
      Dieser Browser kennt den Zugang nicht — er braucht ihn auch nicht."

      WER HIER SPAETER ETWAS ANFASST: Der Test
      `spotify-zugang.integration.spec.ts` durchsucht den ROHTEXT der Antwort
      nach den Geheimnissen, nicht die Feldnamen — ein neues Feld unter
      anderem Namen faellt sonst durch. Und `/api/spotify/config` wird
      ASYNCHRON geladen (server.ts, `readJsonFile(…).then(…)`); der erste
      Aufruf nach dem Import antwortet sonst 500, und ein Test, der das als
      Fehlschlag meldet, zeigt auf die falsche Stelle.

  - id: jellyfin-ueber-die-box-statt-schluessel-im-browser
    title: "Jellyfin ueber die Box: Cover fertig, Ton gebaut aber nicht verdrahtet — und warum"
    kind: architektur
    tags: [jellyfin, sicherheit, api, oberflaeche, e15, 2026]
    source:
      - "gebaut und gemessen am 2026-08-04 (BACKLOG E15/S1+S2)"
      - "tools/jellyfin-weiterreicher-probe.sh gegen den echten Jellyfin 192.168.178.199:8899"
    body: |
      ZWEI NEUE WEGE, beide ohne `api_key` in der Adresse — die Box holt und
      schickt den Zugang als Kopfzeile `X-Emby-Token`:

          GET /api/bild/jellyfin/:kennung     das Cover   (E15/S1, FERTIG)
          GET /api/jellyfin/strom/:kennung    der Ton     (E15/S2, gebaut)

      DIE KENNUNG IST DER GRIFF, NICHT DER WERKSCHLUESSEL — und das ist eine
      Korrektur an dem, was E15 vorgezeichnet hatte. Zwei Gruende:

        * `medienSchluessel()` liegt im Server. Die Oberflaeche muesste ihn
          NACHBAUEN, und zwei Fassungen derselben Regel laufen auseinander,
          sobald jemand eine anfasst — still. (ard.ts musste das einmal und
          traegt den Hinweis; ein zweites Mal waere ein Muster.)
        * Der Werkschluessel trifft nur, was in der Medienliste steht. Die
          TITEL eines Jellyfin-Albums stehen dort NICHT, und ihre Cover
          leckten denselben Schluessel weiter.

      DIE KLASSISCHE OBERFLAECHE HAT GENAU EINE NAHT fuer Bilder:
      `ArtworkService`. Jede Kachel holt ihr Cover dort — Startseite,
      Medienliste, Weiterhoeren, Weiterhoeren-Fenster. Deshalb war S1 eine
      Datei und nicht fuenf.

      DER TEURE FEHLER IST NICHT „nicht erkannt", SONDERN „alles umgebogen":
      ein Spotify-Cover durch `/api/bild/jellyfin/…` gejagt ergibt LEERE
      Kacheln, und zwar alle auf einmal. In `jellyfin.spec.ts` steht deshalb
      neben jedem „wird umgebogen" ein „bleibt".

      BEREICHSABRUFE SIND BEIM TON KEIN BEIWERK: mpv holt eine Datei nicht am
      Stueck, sondern springt darin. Verschluckt der Weiterreicher `Range`,
      spielt die Box weiter und kann nur nicht mehr springen — das MERKEN DER
      POSITION waere STILL kaputt. Durchgereicht werden `Range` hin und
      `Content-Range`/`Content-Length`/`Accept-Ranges` zurueck.

      DURCHGELEITET, NICHT GEPUFFERT (`Readable.fromWeb` + `pipe`):
      `arrayBuffer()` haette ein ganzes Album im Speicher eines Pi liegen.
      Und KEIN Zwischenspeicher, anders als bei den Covern — ein Cover ist
      58 kB und wird oft gebraucht, ein Album einige hundert MB und selten
      zweimal hintereinander. Die SD-Karte waere der Verlierer.

      GEMESSEN GEGEN DEN ECHTEN JELLYFIN (04.08.2026; das Hintergrundstueck
      lief dabei auf dem Entwicklungsrechner, die Box wurde nur gelesen):

          DIREKT  Bereichsabruf 206  audio/mpeg  bytes 0-1023/5683931
          UEBER   Bereichsabruf 206  audio/mpeg  bytes 0-1023/5683931
          Dauer   direkt 140,329592 s / ueber die Box 140,329592 s
          Sprung auf Byte 200000: dieselben Bytes
          Cover ueber die Box: image/jpeg, 47837 Byte

      WARUM DER TON TROTZDEM NOCH NICHT VERDRAHTET IST — zwei Gruende, und
      der zweite ist der wichtigere:

        1. Ob MPV AUF DER BOX darueber spielt, ist ohne Ausrollen nicht zu
           hoeren. ffprobe derselben libav-Familie findet Laenge und Sprung;
           das ist ein starkes Indiz, kein Beleg.
        2. Danach haengt die Jellyfin-WIEDERGABE am backend-api. Heute laeuft
           sie unabhaengig davon. Das ist eine Entscheidung ueber einen
           gemeinsamen Ausfallpunkt, kein Handgriff.

      UND DER DRITTE BROWSER-WEG, den E15 nicht nannte: die TITELLISTE eines
      Jellyfin-Albums holt die klassische Oberflaeche weiterhin unmittelbar
      bei Jellyfin (`getAlbumTracks`, `X-Emby-Token` aus dem Browser).
      Serverseitig gibt es sie schon — `/api/werke/<s>/inhalt`. Solange sie
      offen ist, kann `/api/data` den `api_key` nicht verlieren.

      NACHTRAG 04.08.2026: `/api/werke/<s>/inhalt` traegt den Schluessel SELBST
      noch — in `befehl` und `anhaengen`, gemessen. Und der Grund, warum
      `/api/data` ihn nicht verlieren darf, liegt tiefer als hier beschrieben:
      die Box hat gar keinen eigenen Schluessel, sie erntet ihn von dort.
      Siehe [[jellyfin-schluessel-fliesst-vom-browser-in-die-box]].

  - id: jellyfin-album-aus-der-neuen-oberflaeche
    title: "Jellyfin-Album abspielen: erster Titel starten, Rest anhaengen — und die Reihenfolge ist nicht beliebig"
    body: >
      Ein Jellyfin-Album ist KEIN Ein-Befehl-Vorgang. Es braucht zwei
      Befehlsformen und die richtige Reihenfolge:

        jellyfin/<kodierte Stromadresse>/<titel>:title:artist:<interpret>
        jfqueue/<kodierte Stromadresse>/<titel>:title:artist:<interpret>

      Die Stromadresse ist
      `<server>/Audio/<itemId>/stream?static=true&api_key=<schluessel>`.
      `static=true` heisst DIRECT PLAY — der Server liefert die Originaldatei,
      statt sie umzurechnen; ohne das meldet mpv eine falsche Laenge.

      DIE REIHENFOLGE IST DER PUNKT: Der Startbefehl ersetzt die Liste
      (`replace`). Geht er NACH den Anhaenge-Befehlen hinaus, wirft er sie
      wieder weg und das Album spielt genau ein Lied. Erst starten, dann
      anhaengen. Die bestehende App hat dieselbe Stelle und denselben
      Kommentar (player.service.ts:200-205).

      WER DIE BEFEHLE BAUT: In der bestehenden App der Browser — er holt sich
      den Jellyfin-Schluessel aus der Coveradresse in `/api/data`. Fuer die
      neue Oberflaeche baut sie der SERVER und liefert sie in
      `/api/werke/<schluessel>/inhalt` je Titel als `befehl` und `anhaengen`
      gleich mit. Der Schluessel steckt damit weiterhin im Browser (er ist Teil
      der Adresse), aber an EINER Stelle statt in zwei Oberflaechen — und wenn
      er dort verschwindet ([[zugangsschluessel-im-browser]], BACKLOG E15),
      aendert sich nur der Server.

      KINDERZEIT: Nur der ERSTE Befehl faellt unter START (`jellyfin` steht im
      Muster). `jfqueue` faellt NICHT darunter, und das ist richtig — es
      beginnt keine Wiedergabe, sondern haengt an das an, was der erste Befehl
      bereits erlaubt bekommen hat. Wurde der abgelehnt, darf gar nichts
      angehaengt werden; die Oberflaeche steigt dort aus.

      AM GERAET GEMESSEN (Pi 5, 2026-08-01, Album "HAMM" von Kapelle Petra,
      12 Titel): Der Start meldet `currentPlayer='mplayer'`, `playing=true`,
      `currentTrackname='Mittelmaessiges Leben'`; nach dem Anhaengen steht
      `totalTracks=12`. mpv liest die Metadaten der Stroeme (Genre, Jahr,
      encoder) — die Verbindung zu Jellyfin steht also.

      OFFEN, NICHT GELOEST: `currentTracknr` stand danach auf 12 statt auf 1 —
      das Anhaengen zaehlt die Titelnummer hoch. Die Ursache liegt in
      spotify-control.ts: `mplayerZaehlt` startet auf `true` und kippt erst,
      wenn ein `playlist_pos`-Ereignis mit einem Wert >= 1 eintrifft; bis
      dahin erhoeht JEDES metadata-Ereignis die Nummer. Auf der Messbox haengt
      KEIN Lautsprecher (nur HDMI-Ziele, nichts angeschlossen), es lief also
      nichts wirklich — und ohne Wiedergabe meldet mpv womoeglich nie eine
      Stelle. Ob der Zaehler mit funktionierender Ausgabe richtig steht, ist
      damit NICHT entschieden. Verwandt: [[titelangaben-falsch]].

      ZUM NACHMESSEN, sobald Ton anliegt: Album starten, dann
      `curl .../player/local` beobachten — `currentTracknr` muss 1 sein und
      mit dem Titelwechsel steigen, `totalTracks` 12.
    tags: [jellyfin, wiedergabe, neue-oberflaeche, mpv, kinderzeit]
    source:
      - "am Geraet ausgeloest und gemessen, 2026-08-01"

  - id: flip-liste-nach-neuladen-ohne-bezug
    title: "Neue Oberflaeche: Titelauswahl in der Flip-Liste tut nichts (nach jedem Neuladen)"
    kind: signature
    symptom: >
      Musik laeuft, aber die umgedrehte Titelkarte sagt „Das lief nicht von
      hier — die Titel kenne ich nicht.", oder sie zeigt die Liste und ein Tipp
      auf eine Zeile bewirkt NICHTS: kein Ton, kein Sprung, keine Fehlermeldung.
      Tritt zuverlaessig nach jedem Neustart der Box und nach jedem Ausrollen
      auf, verschwindet, sobald man einmal selbst eine Kachel antippt.
    cause: >
      `zuletztGestartet` in NewDesign/app.js ist reines Sitzungsgedaechtnis. Es
      entsteht nur in `spielen()` und `albumSpielen()` und ist nach jedem
      Neuladen der Seite wieder null. Ein Kiosk laedt staendig neu. `karteAuf()`
      zeigte dann den ehrlichen Hinweis, und `titelSpringen()` fiel still durch
      sein `if (!w) return` — ohne Meldung, weil die Ruecksprungstelle vor jeder
      Rueckmeldung liegt. Von aussen sieht das aus wie eine kaputte Titelauswahl
      oder wie „Spotify spielt nicht".
    fix: >
      `laufendesWerk()` statt `zuletztGestartet` an beiden Stellen. Es findet
      das Werk in drei Stufen:
      (1) Sitzungsgedaechtnis, falls vorhanden;
      (2) SPOTIFY EXAKT — der Zustand des Abspieldienstes (/state auf 5005)
          nennt `context.uri` = `spotify:playlist:<id>`; diese Id mit den
          Werk-Kennungen vergleichen;
      (3) JELLYFIN/LOKAL/RADIO/RSS — dort gibt es keine Kennung im Zustand
          (der Jellyfin-Pfad traegt die Kennung des einzelnen TITELS, nicht die
          des Albums). Deshalb wird der Schluessel beim Starten in
          `localStorage` unter `mupibox_neu_zuletzt_v1` gemerkt und beim
          Wiederfinden gegen `passtZuLaufendem()` geprueft. Der gemerkte
          Schluessel allein genuegt NICHT — er koennte von gestern sein.
      Fuer das COVER reicht weiterhin die Plausibilitaetspruefung allein; fuer
      die TITELLISTE nicht, denn sie bestimmt, welchen Befehl ein Tipp
      ausloest — ein Fehlgriff spraenge in ein fremdes Album.
    why: >
      Der Reflex war, den Fehler beim Titelsprung selbst zu suchen (die
      +1-Falle aus spotify-sprungziel-um-eins-verschoben lag nahe). Gemessen
      war die Kette bis zum Abspieldienst aber vollstaendig heil:
      `tools/spotify-sprung.py` schickte denselben Befehl von aussen, und
      Spotify spielte die richtige Stelle. Erst das schloss den Backend-Pfad
      aus und liess nur die Oberflaeche uebrig.
    source: "am Geraet gemessen (Pi 5 / DietPi, 2026-08-01), NewDesign/app.js"
    tags: ['frontend', 'neue-oberflaeche', 'spotify', 'jellyfin', 'speicher']

  - id: spotify-web-durchreiche-nur-devices
    title: "Messen scheitert an der eigenen Frage: /api/spotify/web/... antwortet 400"
    kind: signature
    symptom: >
      Eine Abfrage wie `/api/spotify/web/me/player/currently-playing` gibt
      HTTP 400 zurueck, obwohl Spotify laeuft und Ton kommt. Sieht beim Messen
      aus wie „spielt nicht" und fuehrt in die falsche Richtung.
    cause: >
      Die Durchreiche der Box (src/backend-api/src/spotify-web.ts) hat eine
      Erlaubnisliste. Von den Wiedergabe-Pfaden ist GENAU EINER frei:
      `me/player/devices` (lesend, fuer die Geraetepruefung). Alles andere wird
      abgewiesen — mit Absicht, damit Steuerbefehle nicht aus dem Browser
      kommen koennen.
    fix: >
      Den laufenden Zustand NICHT ueber die Durchreiche erfragen, sondern beim
      Abspieldienst: `http://<box>:5005/state`. Der reicht Spotifys eigene
      Zustandsantwort durch (is_playing / item / context / progress_ms) und ist
      derselbe Weg, den die Oberflaeche im 2-s-Takt nimmt.
      ACHTUNG bei der Nummer: `item.track_number` ist die Nummer im ALBUM. In
      einer Playlist ist das etwas anderes als die Position, auf die gesprungen
      wurde — eine Playlist reiht Titel aus vielen Alben. Wer eine
      Sprung-Position beweisen will, nimmt ein ALBUM.
    source: "beim Bau von tools/spotify-sprung.py aufgelaufen, 2026-08-01"
    tags: ['spotify', 'backend', 'messen', 'werkzeug']

  - id: darstellung-feld-faellt-still-heraus
    title: "Neue Darstellungs-Einstellung verschwindet nach kurzer Zeit wieder"
    kind: signature
    symptom: >
      Ein neuer Schalter in /admin/darstellung laesst sich stellen und wirkt
      auch — aber nach dem naechsten Anfassen irgendeiner ANDEREN Einstellung
      ist er wieder auf dem Ausgangswert. Keine Fehlermeldung, nichts im
      Protokoll. Der Verdacht faellt auf das Speichern oder auf die Box; beides
      ist unschuldig.
    cause: >
      `darstellung.service.ts` der BOX baut den Stand in `pruefen()`
      FELDWEISE NEU — es reicht kein Objekt durch, sondern zaehlt jedes
      erlaubte Feld einzeln auf (Absicht: fremde oder kaputte Werte kommen gar
      nicht erst in die Oberflaeche). Ein Feld, das dort NICHT steht, faellt
      beim naechsten Schreiben der klassischen Oberflaeche heraus. Das Backend
      merkt davon nichts: es prueft nur die grobe Form (Objekt/kein Array,
      siehe darstellung-auf-der-box-statt-im-browser).
    fix: >
      Eine neue Einstellung braucht DREI Eintragungen, nicht eine:
        1. src/frontend-box/src/app/darstellung.service.ts —
           Schnittstelle `Darstellung`, `STANDARD` UND `pruefen()`.
           Die dritte ist die, die vergessen wird.
        2. src/frontend-admin/src/app/seiten/darstellung.ts —
           Schnittstelle, `STANDARD` und der Knopf.
        3. Wer sie auswertet (NewDesign/app.js in `anwenden()`, oder die
           klassische Oberflaeche).
      Bei Ja/Nein-Schaltern `roh.feld === true` schreiben, nicht
      `roh.feld !== false`: Eine aeltere darstellung.json kennt das Feld nicht,
      und eine Box soll ihr Verhalten durch ein Update nicht aendern.
      ABGESICHERT in darstellung.service.spec.ts — der Test prueft nicht, was
      der Schalter tut, sondern dass er die Reise durch `pruefen()` ueberlebt.

      PRAEZISIERT 19.08.2026 (E44): seit dem ZUSAMMENFUEHREN im PUT
      (server.ts, 16.08., "Fehlen heisst: dazu sage ich nichts") frisst das
      Zurueckschreiben der klassischen Oberflaeche fremde Felder NICHT mehr —
      die drei Eintragungen braucht ein Feld nur noch dort, wo es BEDIENT
      oder ANGEWANDT wird. Felder NUR fuer die neue Oberflaeche (E44:
      albumTippSpielt, reihenFaktor, vollbildBlaettern) bleiben mit ABSICHT
      aus darstellung.service.ts der Box heraus: truege sie deren STANDARD,
      wuerde ein Geraete-Speichern den expliziten Vorgabewert zuruecksenden
      und den Admin-Stand ueberschreiben — das Fehlen ist der Schutz.
    source: "beim Einbau von platzBeimBlaettern aufgefallen, 2026-08-01"
    tags: ['frontend', 'darstellung', 'verwaltung', 'falle']

  - id: platz-beim-blaettern
    title: "Platz beim Blaettern: Leiste und Mini-Player fahren beim Rollen ein"
    body: >
      Auf 800 x 480 nehmen die Kategorienleiste (88 px) und der Mini-Player
      (84 px) zusammen ein Fuenftel der Breite und ein Sechstel der Hoehe
      dauerhaft weg. Waehrend des Blaetterns braucht beides niemand.

      GEMESSEN am Geraet (2026-08-01): Buehne 712 -> 800 px waehrend des
      Rollens, Mini-Player-Kissen 688 -> 250 px (rechts verankert, 12 px Rand).
      Nach 1400 ms Ruhe kommen beide zurueck.

      DER MINI-PLAYER VERSCHWINDET NICHT, ER SCHRUMPFT. Das ist die Lehre aus
      mini-player-ein-und-ausfahren: Er ist der Weg zum grossen Player und zur
      Titelliste. Faellt er weg, gibt es waehrend des Blaetterns keinen Weg
      mehr zum Laufenden — und ausgerechnet beim Blaettern will man oft
      zurueck. Die drei Bedienknoepfe bleiben, nachgemessen 64 x 64 px
      (9,0 x 9,0 mm) und per elementFromPoint als wirklich treffbar bestaetigt.

      DIESELBE RUHEZEIT FUER BEIDES. Zwei Uhren hiessen zwei Bewegungen kurz
      nacheinander; die zweite haette keinen erkennbaren Anlass.

      WAS AUSLOEST: jedes Rollen INNERHALB der Buehne — waagerecht wie
      senkrecht. Gehorcht wird mit `capture: true` an der Buehne, nicht an den
      Elementen selbst: Rollereignisse blubbern nicht, laufen aber durch die
      Erfassungsphase. Das ist entscheidend, weil waagerechte Reihen zur
      Laufzeit entstehen und vergehen (die „Oft gehoert"-Reihe wird bei jeder
      Aktualisierung neu gebaut) — wer sich an die Elemente haengt, muss das
      bei jedem Neuzeichnen nachziehen.

      Die Buehne ist die richtige Grenze: Darin liegt alles, was Alben zeigt.
      Draussen liegen die Kategorienleiste und die Titelliste des grossen
      Players — dort blaettert man nicht durch Alben, dort ist man angekommen.
      Am Geraet gegengeprueft: waagerecht in der Reihe loest aus, senkrecht in
      der Buehne loest aus, Leiste und Titelliste NICHT.

      Der grosse Player schaltet es beim Oeffnen ab, sonst liefe die
      Rueckfahrt hinter ihm ab.

      DIE BLINDE ZEIT (400 ms nach dem Zurueckfahren) ist NICHT optional —
      siehe platz-beim-blaettern-schaukelt-sich-auf.

      WER IN EINER LANE STOEBERT, BLAETTERT NICHT. Solange eine Lane offen ist
      (Playlist -> Alben -> Titel), gilt eine LAENGERE Ruhezeit: 4000 statt
      1400 ms. Die kurze liess die Leiste sonst MITTEN IM STOEBERN
      zurueckkommen und schob alles um 88 px zur Seite, waehrend die Hand
      gerade ein Album antippen wollte. Am Bildschirm nachgestellt:
      t=0 EIN (Leiste weg), t=2210 AUS (Leiste wieder da) — ohne dass jemand
      gerollt haette.

      ÜBERHOLT (03.08.2026): Hier stand bis dahin eine SPERRE — solange eine
      Lane offen war, wurde weder ein- noch ausgefahren. Sie hat in beide
      Richtungen geklemmt und ist weg; ob eine Lane offen ist, wird jetzt
      GEFRAGT statt gemerkt und waehlt nur noch die Zeit. Siehe
      [[lane-sperre-klemmte-in-beide-richtungen]] und
      [[ruhezeit-mit-offener-lane]].

      EBENFALLS NEUER STAND (03.08.2026): Das eingefahrene Kissen gibt jetzt
      auch HOEHE frei (Buehne 332 -> 416 px) und traegt vier Knoepfe statt
      drei — der Lautstaerke-Knopf bleibt stehen. Dazu die Wischgeste vom
      linken Rand als Abkuerzung. Siehe
      [[eingefahrenes-kissen-gibt-hoehe-frei]] und
      [[randwisch-holt-die-leiste-zurueck]].

      EIN/AUS ueber `platzBeimBlaettern` in der Darstellung
      (/admin/darstellung -> Elemente -> Mini-Player). Vorgabe AUS: eine
      Oberflaeche, die sich waehrend der Bedienung veraendert, will man erst
      gesehen haben. Beim Hinzufuegen des Feldes gilt
      darstellung-feld-faellt-still-heraus.
    source: "NewDesign/app.js + app.css, am Geraet gemessen 2026-08-01"
    tags: ['neue-oberflaeche', 'darstellung', 'anordnung']

  - id: platz-beim-blaettern-schaukelt-sich-auf
    title: "Einfahrende Leiste kommt zurueck und ist sofort wieder weg — die Oberflaeche hoert auf sich selbst"
    kind: signature
    symptom: >
      Zwei Meldungen, die nach zwei Fehlern klingen: „der Player zappelt" und
      „wenn man nicht zurueck scrollt, bleibt die Leiste weg". Gemessen mit
      nachgeahmtem Fingerrollen (30 Schritte ueber 1 s, dann Ruhe):

        t=   8  EIN   (gerollt)
        t=2375  AUS   (Ruhezeit abgelaufen, richtig)
        t=2423  EIN   <- von nichts und niemandem
        t=3877  AUS
        t=3895  EIN   ... und so fort

      Ein einzelner programmierter Sprung zeigt den Fehler NICHT — dort kommt
      genau ein Rollereignis. Es braucht das durchgehende Rollen.
    cause: >
      ZWEI verschiedene Ursachen hinter den zwei Symptomen.

      (1) BLEIBT WEG: Faehrt die Leiste ein, wird die Buehne von 712 auf 800 px
      breit. Die Kacheln werden dadurch breiter UND hoeher; die Inhaltshoehe
      waechst um 66 px (832 -> 898, gemessen). Der Browser passt die
      Rollstellung an — und das ist ein Rollereignis wie jedes andere. Es kommt
      beim eigenen Zuhoerer an, wird fuer eine Fingerbewegung gehalten und
      faehrt sofort wieder ein.

      (2) ZAPPELT: `width: auto`, `align-self` und `justify-content` sind NICHT
      interpolierbar. Der Browser blendet zwischen zwei Laengen ueber, aber
      nicht zwischen einer Laenge und „so breit wie der Inhalt". Diese
      sprangen sofort, waehrend die Nachbarn ueber 220 ms schrumpften.
    fix: >
      (1) Eine BLINDE ZEIT von 400 ms nach dem Zurueckfahren (Uebergang 220 ms
      plus ein bis zwei Bilder fuer die Neuberechnung). NUR nach dem
      Zurueckfahren — beim Einfahren duerfen Rollereignisse durch, denn wer
      weiterrollt, soll genau das bewirken.

      (2) Eine GERECHNETE LAENGE statt `auto`, und `align-self`/
      `justify-content` gar nicht erst anfassen. Fuer den Mini-Player:
      3 Knoepfe (3*64) + 4 Abstaende (4*10) + Rand (2*10) = 252 px. Dazu
      `flex-grow: 0` am Textteil — `flex-basis: 0` nimmt ihm die Breite, nicht
      den Drang, den freien Platz zu fuellen.

      ALLGEMEIN: Wer eine Oberflaeche auf Rollen reagieren laesst UND dabei die
      Anordnung aendert, baut eine Rueckkopplung. Die eigene Aenderung muss
      erkennbar sein, sonst haelt die Oberflaeche sich selbst fuer den
      Benutzer.
    source: "am Geraet gemessen (Pi 5, 800x480), NewDesign/app.js + app.css, 2026-08-01"
    tags: ['neue-oberflaeche', 'anordnung', 'falle', 'rollen']

  - id: playpause-gibt-es-nicht
    title: "Play-Knopf tut nichts: `playpause` ist kein Befehl der Box"
    kind: signature
    symptom: >
      Der Play-/Pause-Knopf bewirkt nichts. Die Box antwortet mit HTTP 200,
      im Protokoll steht kein Fehler, der Ton laeuft weiter.
    cause: >
      Der Verteiler des Abspieldienstes (spotify-control.ts, der
      Auffang-Zuhoerer am Dateiende) vergleicht `command.name` der Reihe nach
      gegen `pause`, `play`, `stop`, `next`, `previous` und ein Dutzend
      weitere. `playpause` ist NICHT darunter. Der Aufruf faellt durch alle
      Zweige — und es gibt keinen Zweig, der einen unbekannten Befehl
      bemaengelt. Die Box quittiert also, was sie nicht verstanden hat.

      VERFUEHRERISCH: In `PlayerCmds` (player.service.ts) STEHT
      `PLAYPAUSE = 'playpause'`. Es ist ein TOTER Eintrag — gesendet wird er
      von nirgends. Die klassische Oberflaeche entscheidet selbst und schickt
      `pause` oder `play`. Eine Aufzaehlung im Frontend ist kein Beleg dafuer,
      dass das Hintergrundsystem etwas kennt. (Ebenfalls tot: `CLEARQUEUE`.)
    fix: >
      Selbst entscheiden und `play` oder `pause` schicken. Dazu drei Dinge, die
      zusammengehoeren (siehe mupi-spotify-pause-stoppt-den-ton-nicht):
      SOFORT umschalten statt auf die Abfrage zu warten (die kommt im 1- bis
      2-Sekunden-Takt; so lange saehe der Tipp wirkungslos aus und man tippt
      nach), der Wunsch gewinnt fuer 2,5 s ODER bis die Box zustimmt, plus
      300 ms Doppeltipp-Schutz.

      WERKZEUG: `tools/player-befehle.py` liest die gueltigen Befehle aus dem
      Verteiler und stellt sie der Aufzaehlung gegenueber:
          tools/player-befehle.py --pruefe playpause play pause
      `tools/neu-vorschau.mjs` weist unbekannte Befehle seit 2026-08-01 mit
      400 ab — strenger als die Box, mit Absicht: Wer beim Bauen einen Befehl
      erfindet, soll es sofort merken.
    source: "am Geraet gemessen, 2026-08-01"
    tags: ['abspieldienst', 'falle', 'neue-oberflaeche', 'werkzeug']

  - id: jellyfin-titelsprung-ueber-listenposition
    title: "Jellyfin: Titelauswahl meldet 'Das kann die Box hier noch nicht abspielen'"
    kind: signature
    symptom: >
      Ein Jellyfin-Album laeuft, die Titelliste auf der Kartenrueckseite steht
      vollstaendig da (Titel, Laengen) — aber ein Tipp auf eine Zeile schickt
      GAR NICHTS und meldet „Das kann die Box hier noch nicht abspielen".
    cause: >
      Die neue Oberflaeche waehlte den Sprungbefehl nach dem Dienst:
      `dienst === 'lokal' ? tracknr:N : abspielBefehl(w, nr)`. Fuer Jellyfin
      lief es damit ueber `abspielBefehl` — und das gibt fuer ein
      Jellyfin-ALBUM mit Absicht `null` zurueck, weil es dort keinen
      Ein-Befehl-Start gibt (das Album wird von `albumSpielen()` in seine Titel
      aufgeloest).
    fix: >
      Wer ueber mpv laeuft, springt ueber die LISTENPOSITION — nicht ueber eine
      neue Adresse. `albumSpielen()` haengt das ganze Jellyfin-Album als
      mpv-Warteschlange an (`jellyfin` fuer den ersten Titel, `jfqueue` fuer den
      Rest); was dort laeuft, IST eine mpv-Liste. Also `tracknr:N` fuer
      `lokal` UND `jellyfin`. Radio und RSS bleiben aussen vor: einzelne
      Stroeme ohne Liste.

      GEMESSEN nach der Aenderung: Tipp auf Zeile 9 -> `tracknr:9` ->
      mpv `playlist-pos-1` = 9 von 12, media-title „Zwischending".

      OFFEN GEBLIEBEN: `currentTrackname` folgt dem Sprung NICHT — der
      Abspieldienst setzt ihn bei Jellyfin aus dem Startbefehl und fuehrt ihn
      beim Positionswechsel nicht nach. `currentTracknr` stimmt, der Name
      bleibt auf dem zuerst gestarteten Titel stehen.
    source: "am Geraet gemessen (Pi 5), 2026-08-01"
    tags: ['jellyfin', 'mpv', 'neue-oberflaeche', 'abspieldienst']

  - id: lautstaerke-tasten-statt-regler
    title: "Lautstaerke in der Kinderoberflaeche: zwei Tasten, kein Schieberegler"
    body: >
      DAS IST EINE SICHERHEITSENTSCHEIDUNG, keine Geschmacksfrage.

      `mupibox.maxVolume` wird im Abspieldienst NUR in `setVolume()` geprueft —
      das ist der Weg der ±5-Tasten (`volume/+5`, `volume/-5`). Ein
      Schieberegler geht durch `setVolumeTo()`, und das klemmt bloss auf
      0 bis 100. Ein Kind, das den Regler nach rechts zieht, bekaeme 100 %,
      egal was eingestellt ist (Einzelheiten:
      mupi-lautstaerke-stand-2026-07-27, dort auch der richtige Ort fuer eine
      Behebung: der Flaschenhals mupi-lautstaerke.sh).

      Solange diese Luecke offen ist, gehoert in eine KINDERoberflaeche kein
      Regler. Dazu kommt die Geste: Eine Taste trifft man, einen Regler muss
      man halten und ziehen — mit einem kleinen Finger auf 800x480 der
      schlechtere Weg.

      GEBAUT in der neuen Oberflaeche (2026-08-01): zwei Tasten AUSSEN in der
      Knopfreihe des grossen Players, die Wiedergabe bleibt in der Mitte. Alle
      fuenf Knoepfe 84x84 px = 11,8 x 11,8 mm, am Bildschirm nachgemessen und
      per elementFromPoint als treffbar bestaetigt.

      DER STAND ZEIGT SICH NUR BEIM AENDERN und geht nach 1,8 s wieder — eine
      dauerhafte Prozentzahl waere eine Zahl mehr auf einem Bildschirm, auf dem
      es um Bilder geht. Beim Tippen ist sie dagegen genau die Rueckmeldung,
      die fehlt, weil man den Unterschied erst nach einem Augenblick hoert.

      SOFORT ZEIGEN, nicht auf die Abfrage warten: Der Wert kommt im
      Sekundentakt aus `/player/local` (`currentMeta.volume` reist dort mit —
      keine eigene Abfrage noetig). Ohne eigenes Echo saehe ein Tipp eine
      Sekunde lang wirkungslos aus.

      DIE GRENZE WIRD GELESEN, NICHT NACHGEBAUT: `maxVolume` kommt aus
      `/api/config` und dient NUR dazu, den Lauter-Knopf am Anschlag
      abzublenden. Durchgesetzt wird sie weiter im Abspieldienst — zwei
      Wahrheiten zu pflegen waere der Anfang vom Auseinanderlaufen.

      PRUEFEN OHNE BOX: `tools/neu-vorschau.mjs` beantwortet `/api/config` mit
      einem NIEDRIGEN maxVolume (60) und veraendert seine Lautstaerke bei
      `+5`/`-5` wirklich. Bei 100 liesse sich der Anschlag gar nicht
      vorfuehren — und genau er ist der Teil, der ein Kind schuetzt.
    source: "NewDesign, am Bildschirm gemessen; Befehle am Geraet geprueft (6->11->16->11, ALSA folgt) 2026-08-01"
    tags: ['neue-oberflaeche', 'lautstaerke', 'kindersicherung']

  - id: spotify-vorwaerts-spulen-wirkungslos
    title: "Spulen: rueckwaerts springt, VORWAERTS passiert nichts (librespot)"
    kind: signature
    symptom: >
      Ein absoluter Sprung im laufenden Spotify-Titel wirkt nur nach HINTEN.
      Nach vorn nimmt die Kette den Befehl an (HTTP 200, kein Fehler im
      Protokoll) und der Titel laeuft einfach weiter, als waere nichts
      geschickt worden.

      AM GERAET GEMESSEN (Pi 5, librespot 0.8.0, 2026-08-01), Titel 183 s:

          Ziel      Richtung      vorher   danach   erwartet   Ergebnis
          60000     vorwaerts       7949    11642      63000   nichts
          20000     vorwaerts      11890    15064      23000   nichts
         100000     vorwaerts      15273    18493     103000   nichts
           5000     rueckwaerts    18679     7803       8000   GESPRUNGEN
         150000     vorwaerts       7981    25269     153000   nichts

      Der Rueckwaerts-Sprung landete auf 200 ms genau. Vorwaerts blieb in
      VIER von vier Versuchen wirkungslos, unabhaengig von der Weite.
    cause: >
      EINGEGRENZT auf librespot, alles davor ausgeschlossen. Die Beschreibung
      „vorwaerts geht nicht" war dabei ZU GROB — nachgemessen gilt:

          Versatz    Ergebnis
           +2000 ms  GESPRUNGEN
           +5000 ms  nichts
          +15000 ms  nichts
          +40000 ms  nichts
          -10000 ms  GESPRUNGEN

      Es ist also nicht die RICHTUNG. Es ist auch nicht die WEITE — das war
      mein zweiter Fehlschluss. Es ist der ZUSTAND:

          waehrend der Wiedergabe gesprungen    0 von 4 getroffen
          angehalten, gesprungen, weiter        4 von 4 getroffen
          (angehalten: Ziel 73248 -> 73242, auf 6 ms genau)

      Waehrend die Wiedergabe LAEUFT, wird ein Sprung nach vorn still
      verworfen; im angehaltenen Zustand trifft derselbe Aufruf auf die
      Millisekunde. Die zuvor beobachtete Grenze bei zwei Sekunden war nur der
      Rest, den die laufende Wiedergabe ohnehin gleich erreicht haette.

      DER FRAGE ZU VERDANKEN: „Was mich wundert, warum es im alten Frontend
      funktioniert." Der Weg der alten Oberflaeche ist NACHWEISLICH derselbe
      (`seekPosition` schickt nur `seekpos:<ms>`, kein zweiter Weg) — also
      konnte es nicht am Frontend liegen, sondern nur an der BEDIENUNG. Wer
      erst anhaelt und dann zieht, trifft.

      Der Aufruf SELBST gelingt: seit `seekAbsolut` den Fehlerfall auf `warn`
      protokolliert (statt auf `debug`, das bei logLevel 'error' im Nichts
      landet), schweigt das Protokoll auch bei den wirkungslosen Spruengen —
      `spotifyApi.seek` loest also erfolgreich auf. Spotify nimmt an,
      librespot fuehrt nicht aus.

      Ausgeschlossen und einzeln geprueft:
        * `currentMeta.currentPlayer` steht auf 'spotify', der richtige Zweig
          wird also genommen.
        * Der Befehl kommt an: `seekpos:1` bewegt die Wiedergabe an den Anfang.
          Ein Sprung findet also statt, nur eben nicht nach vorn.
        * Andere Steuerbefehle wirken zuverlaessig (pause/play 3 von 3,
          Titelsprung ebenfalls).
      Bleibt die Abspielkette: librespot laeuft mit `-q`, sagt also nichts.
      Verdacht: Der Strom ist nur bis zur aktuellen Stelle geladen, und ein
      Sprung darueber hinaus wird still verworfen.
    fix: >
      OFFEN. Fuer die Oberflaeche heisst das: Ein Fortschrittsregler darf nicht
      behaupten, der Sprung sei erfolgt. Die neue Oberflaeche zeigt das Ziel
      hoechstens 6 s und faellt dann auf den gemeldeten Stand zurueck
      (`ZIEL_GILT_MS`) — der Regler rutscht dabei sichtbar zurueck, und genau
      das ist die ehrliche Auskunft.
      ABHILFE, DIE WIRKT: anhalten, springen, weiterspielen — als DREI
      GETRENNTE Befehle an die Box (`pause`, `seekpos:<ms>`, `play`), mit rund
      1,5 s dazwischen. 4 von 4 getroffen.

      NICHT im Wiedergabedienst verketten! Genau dieser Umweg wurde in
      `seekAbsolut` gebaut und wieder ENTFERNT: innerhalb einer Anfrage
      verkettet half er NICHT (0 von 4, auch mit 1,5 s Atempausen), kostete
      aber drei Sekunden Stille. Woran der Unterschied liegt, ist offen —
      dass er besteht, ist gemessen. Die neue Oberflaeche macht deshalb die
      drei Befehle selbst (NewDesign/app.js, `springen.loslassen`).

      MIT `-v` MITGELESEN (tools/librespot-mitlesen.sh, das die Aenderung
      danach selbst zuruecknimmt): librespot sagt zum Sprung NICHTS. Das ist
      allerdings KEIN Befund — die Gegenprobe mit `next`, einem nachweislich
      wirksamen Befehl, ergab ebenfalls „No entries". Auf dieser Stufe
      protokolliert librespot gar keine Steuerbefehle. Wer weitersucht,
      braucht `RUST_LOG=trace`, nicht `-v`.

      LEHRE: Ein fehlendes Protokoll ist erst dann ein Hinweis, wenn geprueft
      ist, dass ueberhaupt etwas protokolliert WUERDE.
    source: "am Geraet gemessen (Pi 5 / DietPi Trixie, librespot 0.8.0), 2026-08-01"
    tags: ['spotify', 'librespot', 'spulen', 'offen']

  - id: seekpos-null-sprang-rueckwaerts
    title: "Regler ganz nach links sprang 30 Sekunden ZURUECK statt an den Anfang"
    kind: signature
    symptom: >
      Den Fortschrittsregler ganz nach links ziehen springt nicht an den
      Anfang, sondern dreissig Sekunden zurueck. Bei einem Titel, der laenger
      als 30 s laeuft, sieht das aus wie ein ungenauer Regler.
    cause: >
      `seek(progress)` im Wiedergabedienst unterschied ABSOLUT und RELATIV an
      der GROESSE des Wertes: `> 1` hiess „springe dorthin", alles andere hiess
      „dreissig Sekunden vor bzw. zurueck" (`seek+30` ruft `seek(1)`,
      `seek-30` ruft `seek(0)`). Ein Regler, der auf 0..100 klemmt, darf aber 0
      schicken — und landete damit im Relativ-Zweig. Dasselbe bei 1.
      Betraf auch die bestehende Oberflaeche (now-playing-bar.component.ts
      klemmt auf 0..100), war dort nur nie aufgefallen.
    fix: >
      Getrennte Funktionen statt einer ueberladenen: `seekAbsolut(ziel)` und
      `seekRelativ(vorwaerts)`. Die Aufrufer sagen jetzt, WAS sie meinen,
      statt es durch die Zahl anzudeuten. Am Geraet gegengeprueft: `seekpos:1`
      landet auf ~3,8 s nach 4 s Wartezeit, also am Anfang.

      DIE EINHEIT BLEIBT JE ABSPIELER VERSCHIEDEN und ist keine Marotte,
      sondern die Schnittstelle der Maschinen:
          Spotify   MILLISEKUNDEN   (spotifyApi.seek nimmt position_ms)
          mplayer   PROZENT         (mpv: `seek <n> absolute-percent`)
      Beide Oberflaechen rechnen entsprechend um. Wer hier etwas aendert, muss
      beide mitnehmen.
    source: "src/backend-player/src/spotify-control.ts, am Geraet geprueft 2026-08-01"
    tags: ['backend', 'spulen', 'falle']

  - id: telegram-haken-stirbt-vor-der-abschaltung
    title: "Protokoll voller Python-Abstuerze bei jedem Titelwechsel — obwohl Telegram AUS ist"
    kind: signature
    symptom: >
      Das Systemprotokoll von librespot.service laeuft bei jedem
      Abspielereignis voll:

        ModuleNotFoundError: No module named 'telepot'
        librespot::player_event_handler] On event program
        /usr/bin/python3 /usr/local/bin/mupibox/telegram_Track_Spotify.py
        returned exit code 1

      Am Geraet gezaehlt (2026-08-01): fuenf gestorbene Python-Prozesse in acht
      Sekunden — bei einer Box, auf der Telegram AUSGESCHALTET ist
      (`telegram.active: false`, Token leer). Es kostet nicht nur Platz im
      Protokoll: Jedes Ereignis startet einen eigenen Python-Prozess.
    cause: >
      Die Telegram-Skripte hatten ihre Abschaltpruefung DAHINTER:

        import telepot                          # <- stirbt hier
        ...
        if not config['telegram']['active']:    # <- kaeme nie an
            quit()

      Ist telepot nicht installiert — der Normalfall, wenn Telegram nicht
      benutzt wird —, endet das Skript beim Import, bevor es die Einstellung
      ueberhaupt lesen kann. Die Abschaltung war also vorhanden und
      UNERREICHBAR.

      Betroffen sind besonders die Haken, die librespot als
      `LIBRESPOT_ONEVENT` ruft (env-librespot): die laufen bei JEDEM
      Titelwechsel, Anhalten und Stellenwechsel.
    fix: >
      `import telepot` hinter die Pruefung stellen. Die Reihenfolge ist dann:
      json einlesen -> `active` pruefen -> bei aus still beenden -> erst danach
      telepot importieren. Erledigt in allen zehn Skripten mit Pruefung
      (scripts/telegram/); `telegram_end_publish.py` hat gar keine und wurde
      bewusst NICHT geaendert — dort waere es neues Verhalten, und es haengt
      nicht am Ereignispfad.

      GEGENGEPRUEFT am Geraet: Start, zwei Titelwechsel und Pause erzeugten
      danach 0 Fehlerzeilen (vorher 5 in 8 s), Rueckgabewert des Skripts 0.
    source: "am Geraet gemessen (Pi 5 / DietPi Trixie, librespot 0.8.0), 2026-08-01"
    tags: ['telegram', 'librespot', 'protokoll', 'falle']

  - id: lanes-playlist-alben-titel
    title: "Playlist → Alben → Titel als aufklappende Lanes (neue Oberflaeche)"
    body: >
      DIE MITTLERE EBENE hat der neuen Oberflaeche gefehlt. Eine Spotify-
      Playlist enthaelt nur Titel; bei einer Hoerspiel-Sammlung sind das
      dreihundert Kapitel in einer Reihe. Gesucht sind die ALBEN.

      AUFKLAPPEN STATT NEUE SEITE. Die Lane haengt sich hinter die REIHE der
      angetippten Kachel ins Raster (`grid-column: 1/-1`) und schiebt alles
      darunter nach unten. Der Weg bleibt damit sichtbar: Man sieht, woher man
      kommt, und ein zweiter Tipp auf dieselbe Kachel bringt einen zurueck.
      Auf 800x480 ist das mehr wert als eine saubere Seitenhierarchie — einen
      „Zurueck"-Knopf muss man erst finden, eine offene Kachel sieht man.

      DER PFAD: Die geoeffnete Kachel behaelt einen farbigen Rahmen, die Lane
      eine Linie in derselben Farbe an der linken Kante, und eine Spitze zeigt
      auf die Kachelmitte (`--pfeil`, aus den Rechtecken gerechnet). Die zweite
      Ebene ist eingerueckt und traegt die zweite Farbe — man sieht auf einen
      Blick, wie tief man ist.

      DIE REIHE WIRD GEMESSEN, NICHT GERECHNET. Naheliegend waere
      `Math.floor(index / spalten)`. Das geht schief, sobald eine Lane offen
      ist: Sie belegt eine ganze Rasterzeile und schiebt alles dahinter um eine
      Reihe. Kacheln derselben Reihe haben aber immer denselben `offsetTop`,
      und der stimmt auch mit Lane dazwischen.

      EIN TIPP KLAPPT AUF, EIN PLAY-KNOPF STARTET. Bisher galt ueberall „ein
      Tipp = Ton". Das bleibt — ausser bei einer Spotify-Playlist, denn dort
      startet ein Tipp dreihundert Kapitel am Stueck. Damit „Ton" trotzdem
      einen Tipp entfernt bleibt, bekommt die Kachel einen eigenen Play-Knopf
      (44 px, in der Ecke, mit `stopPropagation` — sonst oeffnete er die Lane
      gleich mit). Album-Kacheln in der Lane genauso.

      DER VERSATZ IST 0-BASIERT, der Abspieldienst will 1-basiert. Die +1 macht
      `abspielBefehl` — dieselbe Stelle wie beim Titelsprung, damit die Falle
      (spotify-sprungziel-um-eins-verschoben) nur einmal im Code steht.

      ALLE DREI EBENEN SIND REIHEN, auch die Titel. Erst waren die Titel eine
      senkrechte LISTE — naheliegend, weil ein Titel kein eigenes Bild hat.
      Als Reihe ist es besser: Alle Ebenen bedienen sich dann GLEICH (schieben
      statt rollen), und ein Kachelziel trifft ein Kinderfinger sicherer als
      eine 44 px hohe Zeile in einer Liste, die selbst scrollt.
      Bei den Titeln traegt die NUMMER die Auskunft, nicht das Bild: Alle Titel
      eines Albums teilen sich dasselbe Cover. Sie steht gross auf der Kachel
      (96 x 128 px), das Cover liegt blass darunter.

      DER RUECKWEG GEHOERT NACH LINKS UND MUSS SICHTBAR SEIN. Anfangs kam man
      nur zurueck, indem man dieselbe Kachel noch einmal antippte — das muss
      man wissen, und ein Kind weiss es nicht. Jede Ebene hat jetzt ihren
      eigenen Pfeil am Zeilenanfang; der der Titel-Ebene schliesst NUR sie, der
      der Alben-Ebene die ganze Kette. (Der Knopf des REGALS sass bereits
      links — gemeint war die Lane.)

      SPOTIFY LIEFERT HOECHSTENS 100 TITEL JE ABRUF. Der Endpunkt holt deshalb
      seitenweise bis 1000. Vorher fehlten zwei Drittel — und schlimmer: Die
      10 vermeintlich „losen" Titel waren gar keine, sondern das an der
      100er-Grenze abgeschnittene Album. Eine halbe Liste erzeugt FALSCHE
      Ergebnisse, nicht nur unvollstaendige (8 Alben/10 lose vorher, 20/0
      nachher). Die Frist musste mit: 4 s reichen fuer einen Abruf, nicht fuer
      zwanzig.
      Am Geraet gegengeprueft: Album ab Versatz 22 -> `:23:0` startete das
      richtige Album, Titel mit Versatz 6 -> `:7:0` landete auf „Kapitel 05".
    source: "NewDesign/app.js + app.css, Endpunkt /api/werke/<schluessel>/alben, 2026-08-01"
    tags: ['neue-oberflaeche', 'navigation', 'spotify']

  - id: attrappe-luegt-durch-weglassen
    title: "Die Vorschau-Attrappe prueft still das Nichts — fuenf Faelle"
    kind: signature
    symptom: >
      Eine Probe gegen `tools/neu-vorschau.mjs` meldet Erfolg oder „ruhig", und
      am Geraet ist der Fehler trotzdem da. Oder umgekehrt: Die Vorschau sieht
      anders aus als die Box, und man beurteilt eine Anordnung, die es so nie
      gibt.
    cause: >
      Nicht falsche Antworten, sondern FEHLENDE und HALBE. Drei Faelle, alle
      selbst hineingelaufen:

      1. ENDPUNKT ANTWORTET LEER. `/api/darstellung` gab `{}` zurueck.
         `anwenden()` steigt bei fehlendem `aktuell` aus — damit war ALLES, was
         an der Darstellung haengt, in der Vorschau nie eingeschaltet. Eine
         Probe auf „Platz beim Blaettern" meldete „ruhig", weil die Sache gar
         nicht lief.

      2. ENDPUNKT ANTWORTET HALB. Danach lieferte sie nur
         `{ platzBeimBlaettern: true }`. `anwenden()` lief, setzte aber fuer
         alles Uebrige seine Ausweichwerte — und die Kacheln wurden RUND, weil
         `kachelForm` fehlte. Die Box hat 'abgerundet'.

      3. ZU WENIG DATEN. Zwei Alben statt sechs: Damit rollt die Album-Reihe
         nicht waagerecht, und die halbe Wechselwirkung tritt gar nicht auf.

      4. EIN FELD FEHLT, UND DER FEHLER SIEHT AUS WIE EIN ERGEBNIS
         (2026-08-02). `/api/werke/<s>/inhalt` lieferte NIE `befehl` und
         `anhaengen` — die beiden Felder, die der Server fuer JELLYFIN-Titel
         setzt (`jfBefehle`, server.ts). Ein Jellyfin-ALBUM hat keinen
         Ein-Befehl-Start; `spielen()` faellt auf `albumSpielen()` durch, das
         `titel.filter((t) => t.befehl)` bildet. Ohne die Felder ist die Liste
         leer, die Oberflaeche meldet „Dieses Album hat keine abspielbaren
         Titel" und schickt NICHTS. In tools/interpretseite-befehle.mjs stand
         dafuer „(keiner)" — und weil ein Jellyfin-Werk ohnehin kein
         `spotify:` schicken darf, ging genau das als „ok" durch. Ein
         Pruefsatz, der das Nichts fuer richtig haelt. Zweifach behoben: die
         Attrappe liefert die Felder, und das Werkzeug wertet „(keiner)"
         jetzt als Fehlgriff.

      5. EIN GANZER DIENST FEHLT — und der Kommentar davor stand schon da
         (2026-08-04). Ueber Fall 3 steht in `tools/neu-vorschau.mjs`
         woertlich, dass die Attrappe frueher nur drei von fuenf Diensten
         fuehrte und man deshalb „drei von fuenf gesehen und den Rest fuer
         erledigt gehalten" haette. Beim sechsten Dienst (`ard`, BACKLOG
         E4/A7) war es wieder so: die Plakette war gebaut, aber JEDES
         Werkzeug, das an dieser Vorschau misst — raster-marke-schau,
         lane-marken-schau, marke-grenzfaelle, marke-tanzt —, haette fuenf von
         sechs Zeichen gesehen und die Sache fuer geprueft gehalten. Und Fall
         4 wiederholte sich gleich mit: die Titelliste der neuen Kachel
         brauchte `befehl`/`anhaengen`, sonst haette sie „keine abspielbaren
         Titel" gemeldet.
         DIE LEHRE IST NICHT „an ard denken", sondern: eine Attrappe wird
         nicht EINMAL ehrlich. Sie faellt bei jeder neuen SORTE zurueck, und
         gemerkt wird es nur, wenn jemand die Frage stellt „welche Sorten gibt
         es, und fuehre ich von jeder eine?". Fuer die Dienste stellt sie
         seit dem 04.08.2026 ein Werkzeug: [[dienstliste-an-fuenf-orten]].

      6. UND NOCH AM SELBEN TAG, ANDERSHERUM: SIE SAGTE NEIN, WO DIE BOX JA
         SAGT (2026-08-04, beim Gegenlesen). In `tools/neu-vorschau.mjs` stand
         die Liste der Startverben von Hand — `library, queue, radio, rss,
         jellyfin, jfqueue, deletelocal, say`. `ard`/`ardqueue` fehlten, obwohl
         der Verteiler sie seit E4/A5 kennt. Ein RICHTIG gebauter ARD-Start
         bekam damit 400 und die Oberflaeche meldete „Das hat gerade nicht
         geklappt" — ein Fehlalarm an genau der Stelle, an der jemand einen
         echten Fehler sucht. Faelle 1-5 sind Attrappen, die zu viel
         durchgehen lassen; dieser ist die Gegenrichtung, und er kostet mehr:
         eine falsche Zusage laesst man irgendwann auffliegen, eine falsche
         Absage schickt jemanden in die Irre. Behoben, indem die Vorschau die
         Verben jetzt aus `spotify-control.ts` LIEST (dieselben zwei Formen wie
         tools/player-befehle.py) und ohne Verteiler beim Start abbricht.
         Siehe [[kachel-sieht-richtig-aus-und-spielt-nicht]].

      7. DERSELBE DIENST, DERSELBE FEHLER, EINEN TAG SPAETER (2026-08-05).
         Die Folgen-Lane der ARD und ihr Weiterhoeren wurden gebaut
         ([[ard-folgenliste-rollt]]) — und die Attrappe lieferte unter
         `/inhalt` bei `ard` weder `id` (die FOLGENKENNUNG) noch `weiterAb`,
         und in `/api/weiterhoeren` gab es ueberhaupt keine ARD-Zeile. Das ist
         Fall 4 in Reinform, nur eine Sorte weiter: Ein Werkzeug haette eine
         aufgeklappte Lane mit richtigen Kacheln gesehen und die Sache fuer
         geprueft gehalten, waehrend ein Tipp darin die gesuchte Folge nie
         gefunden und die Sendung wortlos von vorn gestartet haette
         („Diese Folge gibt es nicht mehr"). Die Ansage waere sogar
         PLAUSIBEL gewesen — die Verweildauer der ARD laeuft wirklich ab.
         Eine Attrappe, deren Luecke wie ein echtes Ergebnis aussieht, ist die
         teuerste Sorte. Fall 5 sagte „ein ganzer DIENST fehlt"; hier fehlten
         zwei FELDER desselben Dienstes, und beide waren die, an denen die
         neue Regel haengt.

      Dazu der aeltere Fall, der schon im Kopf der Datei steht: Antworten, die
      dem VERTRAG nicht folgen (`title`/`state` statt
      `currentPlayer`/`playing`), und der stille 200 auf jeden Spielerbefehl —
      der haette `playpause` nie auffallen lassen.
    fix: >
      Beim Ergaenzen der Attrappe gilt: Felder von einer ECHTEN Box ablesen
      (`curl <box>:8200/api/darstellung`), nicht ausdenken. Genug Daten
      liefern, damit die Wechselwirkungen auftreten. Und: Bevor eine Probe als
      „ruhig" gilt, mit einer GEGENPROBE zeigen, dass sie ueberhaupt anschlagen
      WUERDE — ein echtes Rollen muss einfahren, sonst prueft die ruhige
      Messung nichts.
    source: "beim Bau der Lanes dreimal aufgelaufen, 2026-08-01/02; Fall 5 und 6 beim Anschliessen der ARD, 2026-08-04; Fall 7 bei ihrer Folgen-Lane, 2026-08-05"
    tags: ['werkzeug', 'attrappe', 'pruefen', 'falle']

  - id: mixpi-die-geschichte
    title: "MixPis sind eine ART, kein Maskottchen — und darum haengt die Mehrbenutzer-Idee daran"
    body: >
      DER RAHMEN, IN DEM DIESE OBERFLAECHE GEBAUT WIRD, vom Betreiber am
      02.08.2026 erzaehlt:

      MixPis sind MUSIKWESEN VOM MIXPI-PLANETEN. Es gibt sehr viele von ihnen,
      und sie sind alle verschieden — so wie Menschen. Weitere entstehen mit
      Meshy und liegen als Kandidaten in `NewDesign/maskottchen.json` unter
      `_sammlung`, bis sie eine Aufgabe bekommen.

      WARUM DAS MEHR IST ALS EINE NETTE GESCHICHTE — zwei Folgen:

      1. ES GIBT NICHT „DAS" MASKOTTCHEN. Die Zuordnungsdatei bildet Bilder auf
         Zustaende ab (hoert, spielt, schlaeft …); das bleibt richtig. Aber die
         Bilder sind nicht EIN Wesen in verschiedenen Lagen, sondern
         VERSCHIEDENE Wesen. Wer neue hinzufuegt, erweitert eine Art — keinen
         Zeichensatz. Das aendert, wie man ueber Ergaenzungen nachdenkt.

      2. JEDES KIND BEKOMMT SEIN EIGENES. Geplant ist eine Anmeldung mehrerer
         Kinder (Liam, Kalea, …) mit eigenen Hoerspielen, eigenem Weiterhoeren,
         eigener Kinderzeit. Das MixPi ist dabei das ERKENNUNGSZEICHEN, nicht
         Dekor: Ein Kind, das noch nicht liest, erkennt seinen Zugang am Wesen
         — nicht am Namen und schon gar nicht an einem Passwort. Eine Anmeldung
         mit Namensfeld und Tastatur waere der sichere Weg, die Sache fuer die
         Zielgruppe unbenutzbar zu machen.

      WAS DARAN HAENGT (Einzelheiten im BACKLOG unter E18): `gespielt.json`,
      `resume.json` und die Favoriten sind heute EINE Ablage fuer EINE Box und
      brauchen einen Besitzer; die Kinderzeit rechnet fuer die Box, nicht je
      Kind. Die Medienliste dagegen darf gemeinsam bleiben — zu trennen sind
      Verlauf, Weiterhoeren und Zeit, nicht die Bibliothek.

      Und die Frage, die man zuerst beantworten muss: Was passiert, wenn
      NIEMAND angemeldet ist? Eine Box, die erst nach einer Anmeldung Musik
      macht, ist morgens um sieben das falsche Geraet.
    source: "vom Betreiber erzaehlt, 02.08.2026; NewDesign/MASKOTTCHEN.md"
    tags: ['mixpi', 'geschichte', 'mehrbenutzer', 'plan']

  - id: cover-nach-neustart-langsam
    title: "Nach dem Neustart dauert es, bis die Cover da sind — sie kommen aus dem Internet"
    kind: signature
    symptom: >
      Nach einem Neustart der Box bleiben die Kacheln eine ganze Weile leer
      (bzw. zeigen den Anfangsbuchstaben), bis nach und nach die Cover
      erscheinen. Im laufenden Betrieb faellt es nicht auf.
    cause: >
      `/api/bild/<schluessel>` liefert das Bild NICHT selbst, sondern
      antwortet mit einer Weiterleitung:

          HTTP/1.1 302 Found
          Cache-Control: public, max-age=86400
          Location: https://image-cdn-fa.spotifycdn.com/image/…

      Jedes Cover ist damit ein Gang ins Internet. Warm gemessen (Pi 5, WLAN,
      11 Werke): zusammen 3,0 s, einzelne bis 0,9 s. Kalt und mit mehr Werken
      entsprechend laenger.

      DER BROWSER SOLLTE DAS ABFANGEN — tut es aber nicht: Der Kiosk laeuft mit
      `--disk-cache-dir=/home/dietpi/.mupibox/chromium_cache --disk-cache-size=134217728`,
      das Verzeichnis gehoert dietpi und ist beschreibbar — und enthaelt NULL
      Dateien. Warum, ist noch offen.

      NACHTRAG 04.08.2026 — DAS STIMMT NICHT MEHR: derselbe Ordner enthaelt
      jetzt 532 Dateien und 46,9 MB, ueber vier Kaltstarts hinweg stabil
      (gemessen mit tools/grundmessung.py). Der Chromium-Cache arbeitet also.
      Was am 02.08. leer war, ist heute voll; der damalige Befund war
      entweder ein frisch geleerter Cache oder eine andere Ursache. Die
      Folgerung "der Browser faengt es nicht ab" traegt damit NICHT mehr —
      der eigene Cover-Speicher bleibt trotzdem richtig, denn er liefert die
      Bytes selbst statt einer Weiterleitung ins Internet.

      UND WO DER COVER-SPEICHER WIRKLICH LIEGT: NICHT unter /etc/mupibox.
      server.ts nimmt `${configBasePath}/coverspeicher`, und configBasePath
      ist RELATIV ('./server/config') — also relativ zum Arbeitsverzeichnis
      des Dienstes:
      /home/dietpi/.mupibox/Sonos-Kids-Controller-master/server/config/coverspeicher
      (gemessen: 308 Dateien, 8,3 MB). `mupibox.configPath` in der
      mupiboxconfig.json steht auf **null** und fuehrt in die Irre — ein
      Werkzeug, das ihm folgt, meldet "nicht vorhanden" fuer einen vollen
      Speicher.

      NICHT DIE URSACHE (geprueft, nicht vermutet): der Themenwechsel.
      `/active_theme.css` ist 1,1 kB gross und in 16 ms da, `/neu/` in 43 ms.
    fix: >
      ERLEDIGT am 02.08.2026: `/api/bild` holt eine Netzadresse jetzt EINMAL,
      legt sie unter `<config>/coverspeicher` ab und liefert danach die Bytes
      selbst. Am Geraet gemessen, ueber alle elf Werke:

          1. Durchgang (kalt)          1,69 s
          2. Durchgang (aus dem Speicher) 0,03 s
          3. Durchgang                 0,03 s

      Also das 56-Fache. Auf der Platte liegen dafuer 612 kB.

      AUF DIE PLATTE, NICHT INS RAM — der naheliegende Gedanke war, die Bilder
      im Arbeitsspeicher zu halten und nur selten zu sichern, um die SD-Karte
      zu schonen. Die Zahlen sagen etwas anderes: 58 kB je Cover, auf 200
      Werke gerechnet 11 MB, und jedes wird GENAU EINMAL geschrieben.
      SD-Karten verschleissen an wiederholtem Schreiben, nicht daran. Ein
      RAM-Speicher mit gelegentlichem Sichern braeuchte eine Uhr, ein Laden
      beim Start und verlaere beim Ziehen des Steckers alles seit dem letzten
      Sichern.

      DER SCHLUESSEL IST DIE ADRESSE, nicht der Werkschluessel: Zwei Werke
      koennen dasselbe Cover tragen, und ein geaendertes Cover bekommt eine
      neue Adresse und damit von selbst einen neuen Namen.

      GESCHRIEBEN WIRD MIT UMBENENNEN als letztem Schritt — ein Stromausfall
      mitten im Schreiben hinterliesse sonst eine halbe Datei, die beim
      naechsten Start als gueltiges Bild gilt.

      NEBENBEI: Die Box zeigt ihre Cover jetzt auch OHNE Netz.

      Der urspruengliche Gedanke war:
      Die Bilder AUF DER BOX zu halten
      und aus `/api/bild` die Bytes selbst zu liefern statt weiterzuleiten:
      kein Gang ins Internet, ueberlebt jeden Neustart, und die Box zeigt ihre
      Cover auch ohne Netz — dasselbe Prinzip, nach dem schon die Schriften
      mitgeliefert werden.

      ACHTUNG, WIDERSPRUCH IM WIKI: `mupi-chromium-sd-cache` empfiehlt
      `--disk-cache-dir=/tmp`, um die SD-Karte zu schonen. `/tmp` ist eine
      tmpfs — der Zwischenspeicher waere damit nach JEDEM Neustart leer und
      genau dieses Problem garantiert. Wer die SD schonen will, muss die Cover
      serverseitig ablegen, nicht den Browserspeicher ins RAM legen.
    source: "am Geraet gemessen (Pi 5 / DietPi, 2026-08-02)"
    tags: ['cover', 'zwischenspeicher', 'start', 'offen']

  - kind: architektur
    id: versionen-rolling-mit-stueckliste
    title: "Paketfassungen: die Rezepte pinnen nichts — bewusst, aber jetzt sichtbar"
    tags: ['installation', 'rezept', 'versionen', 'reproduzierbarkeit', 2026]
    source: "aus den Rezepten gelesen und an der Box aufgenommen (2026-08-02)"
    body: |
      NACHGESEHEN 2026-08-02, weil die Frage aufkam, ob wir Fassungen
      festlegen. BEFUND: so gut wie nicht. In allen Rezepten steht KEIN
      einziges `paket=fassung`, kein `apt-mark hold`, keine `preferences.d`.

      FESTGELEGT ist nur dreierlei, und zwar per ARTEFAKT, nicht per Nummer:

        librespot          bin/librespot/0.8.0/librespot-arm64 im Repo
        App/Backend/UI     bin/nodejs/deploy.zip, am Entwicklungsrechner
                           gebaut, Abhaengigkeiten eingebacken
        Node               nur der MAJOR (setup_26.x, check auf ^v26\.)

      OFFEN ist alles andere: 37 apt-Pakete ohne Fassung (aus den Rezepten
      gezaehlt, nicht geschaetzt), `npm install -g pm2 @ionic/cli`, Nodes
      Minor/Patch aus nodesource und das Basis-Image selbst.

      DAS HEISST: die Installation IST schon ein Rolling Release - nur ein
      unkontrolliertes. Nicht "wir aktualisieren bewusst", sondern "der
      Installationszeitpunkt entscheidet". Zwei Boxen im Abstand von Monaten
      sind nicht dieselbe Box. Ein `apt-get upgrade` steht nirgends (nur
      `apt-get update`), bestehende Boxen driften also nicht - aber jede
      frische Installation greift ins Ungewisse.

      ── ENTSCHEIDUNG (2026-08-02): ROLLING BLEIBT, ABER SICHTBAR ────────
      Nicht pinnen. Hart zu pinnen erzeugt Wartungslast und schneidet eine
      internetverbundene Kinderbox von Sicherheitsupdates ab. Stattdessen
      wird aufgeschrieben, was eine FUNKTIONIERENDE Box hatte:

        tools/stueckliste.py <box>                zeigen
        tools/stueckliste.py <box> --schreiben    -> dateien/stueckliste.txt
        tools/stueckliste.py <box> --vergleichen  Box gegen die Ablage
        tools/stueckliste.py --pakete             was die Rezepte anfassen

      Der Rezeptschritt `stueckliste` (letzter Schritt in mupibox-app.yaml)
      vergleicht jede frische Installation dagegen und MELDET Unterschiede.
      Er ist `optional: true` und endet immer mit exit 0: ein Schritt, der
      wegen einer neueren Paketfassung abbricht, waere ein Pin durch die
      Hintertuer.

      Modell und Kern werden getrennt als "Umfeld" ausgewiesen - zwischen
      Pi 4 und Pi 5 unterscheiden sie sich voellig zu Recht, und eine Warnung,
      die auf jeder zweiten Box steht, liest nach kurzer Zeit niemand mehr.

      DIE PAKETLISTE WIRD GELESEN, NICHT GEPFLEGT: sie kommt aus den
      `apt-get install`-Zeilen der Rezepte selbst. Eine abgeschriebene Liste
      waere in dem Moment falsch, in dem jemand ein Paket ergaenzt - und
      niemand haette es gemerkt. Dasselbe Prinzip wie das Auslesen der
      wirksamen ExecStart-Zeile in tools/librespot-warten-messen.sh.

      FALLE BEIM ABLEITEN (getreten und behoben): in
      `apt-get install A || apt-get install B` verschluckt ein gieriges `.*`
      den zweiten Aufruf. Der Rueckfall libasound2 fehlte lautlos in der
      Liste. Jetzt wird die Zeile an JEDER Fundstelle zerlegt.
      Zweite Feinheit: dpkg kennt auch NICHT installierte Pakete (ebender
      Rueckfall libasound2, auf Trixie von libasound2t64 abgeloest) und gibt
      sie mit LEERER Fassung zurueck - das saehe im Vergleich aus wie ein
      Lesefehler, steht deshalb als "nicht installiert" da.

      WOFUER DAS GUT IST: Dieses Wiki ist im Kern ein Katalog von Faellen, in
      denen sich draussen etwas bewegt hat und die Box stumm oder schwarz war
      - mupi-nodesource-trixie, mupi-max98357a-trixie-silent,
      mupi-rpi-eeprom-trixie, librespot-versionen-2026. Jedes Mal begann die
      Suche bei null, weil niemand wusste, was die funktionierende Box vorher
      hatte. Jetzt steht es in einer Zeile.

      VORLAGE NEU AUFNEHMEN, wenn eine Box nachweislich rundlaeuft - NICHT
      von einer, die gerade repariert wird. Sonst wird ein Defekt zur Vorgabe.

      ── DABEI AUFGEFALLEN, NOCH OFFEN ──────────────────────────────────
      * `npm-globals` (mupibox.yaml) installiert weiterhin pm2, ungepinnt,
        und `check: pm2 -v` VERLANGT es - obwohl pm2 nach der Messung
        abgeschafft wurde (App bereit 23,5 s -> 3,3 s, siehe
        mupi-startzeiten-gemessen) und das App-Rezept schreibt, es muesse
        "WIRKLICH weg".
      * Der Node-Workaround `nodesource-bookworm` setzt Node 22 ueber ein
        bookworm-Repo. Laut mupi-node-major-wechsel gilt der Rat nicht mehr
        (nodesource benutzt `nodistro`), und 22 wuerde ohnehin am `check`
        auf ^v26\. scheitern.

  - kind: architektur
    id: benennung-mixpibox-drei-toepfe
    title: "Umbenennung MuPiBox -> MixPiBox: was sichtbar ist, was intern, was unantastbar — und warum in der Oberflaeche NIE eine Konstante steht"
    tags: ['benennung', 'mixpi', 'umbenennung', 'oberflaeche', 'installer', 'konfiguration', 2026]
    source: "Topf A umgesetzt und an der lebenden Box abgeglichen (2026-08-02)"
    body: |
      DER AUSLOESER WAR EINE LUEGE IN DER KOPFZEILE. Am 2026-08-02 stand in
      NewDesign/index.html fest verdrahtet `<div class="kopf-titel">MuPiBox`,
      waehrend die Box laut ihrer eigenen Konfiguration schon `MixPiBox` hiess
      (GEMESSEN, nur lesend: GET http://<box>:8200/api/config, 5.672 Byte in
      165 ms, `mupibox.host = "MixPiBox"`). Niemandem war es aufgefallen, weil
      die beiden Stellen nie nebeneinander zu sehen sind.

      DARAUS DIE REGEL: In der Oberflaeche steht der KONFIGURIERTE Name, nie
      eine Konstante. Ein zweiter fester Text ("jetzt aber MixPiBox") haette
      genau denselben Zustand in einem Jahr wieder erzeugt.

      ── DIE DREI TOEPFE (gezaehlt mit tools/umbenennung-inventur.py) ────
      Muster `mupi`, gross/klein egal, ZEILEN, ohne node_modules/.git/
      package-lock/Binaerdateien/www/dist:

        Topf   Dateien   Zeilen   Was
        A            6       36   sichtbarer Text
        B          163     1220   Kommentare, Doku, Selektoren, CSS-Variablen
        C          117     1450   unantastbar
        ALLE       286     2706

      Nach dem Umsetzen von Topf A: 28 Zeilen. Die Reste in den A-Dateien sind
      Konfigschluessel (`mupibox.maxVolume`), Angular-Selektoren
      (`mupi-rahmen`), der HARDWARENAME `MuPiHAT` und Kommentare — alles B/C,
      das zufaellig in A-Dateien wohnt.

      ── TOPF C, UNANTASTBAR, und warum ────────────────────────────────
      * 16 systemd-Units `config/services/mupi_*.service` plus Timer.
      * Pfade, die zur LAUFZEIT auf jeder Box existieren: `/etc/mupibox/` (68
        Dateien), `/usr/local/bin/mupibox/` (54), `Sonos-Kids-Controller-master`
        (35, der LIVE-Installationsort und bereits der zweite Altname im Baum),
        `/home/dietpi/MuPiBox/sysmedia/`. Umbenennen hiesse: Migrationsschritt
        auf JEDER bestehenden Box.
      * npm-Paketnamen (`mupibox`, `mupibox-frontend-box`, …).
      * Die Gruppe `mupibox` in mupiboxconfig.json — 22 Schluessel.
      * DIE THEMENNAMEN `'MuPiBox Classic'`, `'New MuPiBox'`, `'Tiger Mupi'`
        (src/frontend-admin/src/app/seiten/darstellung.ts, 12 Zusicherungen in
        themen.spec.ts). Das sind SCHLUESSEL in der gespeicherten Themenliste
        jeder Box, keine Beschriftungen. Wer sie umbenennt, verwaist die
        vorhandenen Themen — und ein halb gesetztes Thema laesst die restlichen
        Felder stehen, die bekannte "komische Mischung" aus
        [[mupi-aussehen-angleichen]].
      * Fremde Geraetenamen: Jellyfin `client`/`device` = 'MuPiBox'
        (jellyfin.service.ts, geprueft in jellyfin.spec.ts), Spotify-Web-SDK
        `'MuPiBox Web Player'`.
      * Alles, was der remoteinstaller anfasst: autosetup, update, Dockerfile,
        scripts/, harness/, mupictl, der Bootbildschirm.

      ── DIE DREIFACHBELEGUNG VON `mupibox.host` ───────────────────────
      Ein einziger Wert bedient DREI Dinge:
        1. Anzeigename der Box
        2. Spotify-Connect-Geraetename (LIBRESPOT_NAME, config/templates/
           env-librespot) — und spotify-control.ts sucht das Geraet
           NAMENSGENAU
        3. Systemhostname — AdminInterface/www/mupi.php ruft beim Speichern
           `sudo /boot/dietpi/func/change_hostname`
      Deshalb wird er GELESEN und nirgends im Code nachgebaut. Ihn zu aendern
      laesst die Box aus Spotifys Geraeteliste verschwinden und unter neuem
      Namen wieder auftauchen.

      ── WAS UMGESETZT WURDE (2026-08-02) ──────────────────────────────
      * NewDesign: `kopf-titel` bekam eine `id` (die KLASSE musste bleiben,
        app.css:287 haengt daran). `laut.grenzeHolen()` wurde zu
        `laut.grenzeAus(d)` und der Abruf zog in `konfigHolen()` — EIN Abruf
        von `/api/config`, zwei Verbraucher: Lautstaerkegrenze und Boxname.
        Kein neuer Endpunkt, keine Backend-Aenderung: `ohneGeheimnisse()`
        (konfiguration.ts) streicht aus der Gruppe `mupibox` nur
        `einstellungsPin`, `host` ueberlebt.
      * Verwaltung: die Kopfzeile (`rahmen.ts`) zeigt ebenfalls `mupibox.host`.
        Begruendung: sobald es zwei Boxen gibt (Backlog E13), ist sie die
        einzige Stelle, an der man sieht, WELCHE man gerade verwaltet. Die 14
        Routentitel bleiben fester Text ('… – MixPiBox') — Angulars `title` ist
        ein statischer String, dynamisch waere ein TitleStrategy-Umbau.
      * settings.page.ts: 'Do you want to reboot or shutdown the MuPiBox?'
        wurde 'Soll die Box neu starten oder ausgehen?' — OHNE Namen. Das
        stimmt auf jeder Box, und ein englischer Satz weniger.

      ── FALLE: bei leerem Namen NICHTS setzen ─────────────────────────
      `boxnameSetzen()` steigt bei leerer Zeichenkette aus. Wuerde es leeren
      Text setzen, fiele die Kopfzeile in der Hoehe zusammen — `kopf-titel` ist
      das einzige Element mit voller Schriftgroesse in `kopf-text`. Lieber der
      Rueckfall aus index.html als ein Loch.

      ── FALLE: die Vorschau-Attrappe lieferte `host` GAR NICHT ────────
      tools/neu-vorschau.mjs gab bei `/api/config` nur `maxVolume` und
      `startVolume` aus. Wer den neuen Kopftitel dagegen gemessen haette, haette
      den RUECKFALL gesehen und ihn fuer das Ergebnis gehalten — genau das
      Muster aus [[attrappe-luegt-durch-weglassen]]. Die Attrappe liefert jetzt
      `host: 'MixPiBox Vorschau'`, absichtlich ANDERS als der Rueckfall, damit
      sichtbar ist OB gesetzt wurde. Im Browser gegen die Vorschau geprueft:
      document.title und kopf-titel beide "MixPiBox Vorschau", Reglergrenze 60
      (also kam maxVolume aus demselben Abruf mit), keine Konsolenfehler.

      ── BEWUSST LIEGENGEBLIEBEN (Topf B) ──────────────────────────────
      * Die `--mupi-*`-CSS-VARIABLEN sind der gefaehrlichste Posten des ganzen
        Vorhabens: sie sind zwischen src/frontend-box/src/app/
        darstellung.service.ts und NewDesign/app.css + app.js GETEILT. Ein
        Umbau nur auf einer Seite faellt STILL aus, ohne Fehlermeldung. Nur in
        EINEM Zug, und nicht mit einer Umbenennung sichtbarer Texte vermischt.
      * Die Selektoren `mupi-swiper`, `mupi-loading`, `mupi-netzstatus`,
        `mupi-dienststatus`, `mupi-zeitanzeige`, `mupihat-icon`.
      * Das Log-Praefix `[MuPiBox-Server]` (278 Zeilen in server.ts). Sieht nach
        Topf A aus, ist aber B: Protokollzeilen werden von Auswertungen gelesen
        und teils gefiltert.
      * Bootbildschirm und Bilder (splash.png, goodbye.png, MuPiLogo.*,
        /var/www/images/mupi_round_trans.png). Der Bootbildschirm liegt im
        remoteinstaller (remote-step-installer/tools/mupibox-boot-splash.py, `titel = "MuPiBox"` in
        `malen()`); eine Aenderung dort landet beim naechsten Ausrollen auf
        JEDER Box. Wenn er den Boxnamen lesen soll: `/etc/mupibox/
        mupiboxconfig.json` ist beim Booten lange vor dem Netz da, ABER die
        PSF-Konsolenschrift ist 30 px breit (auf 800 px ~26 Zeichen) und die
        Blockhoehe rechnet mit EINER Titelzeile — ohne Kappung auf ~24 Zeichen
        laeuft ein langer Name seitlich heraus, bei jedem Start.
      * `news.txt` holt AdminInterface/www/index.php zur Laufzeit aus dem
        FREMDEN Repo splitti/MuPiBox. Die alte PHP-Verwaltung zeigt weiter den
        Altnamen, egal was hier geschieht.

      ── OFFEN, VOR DEM NAECHSTEN SCHRITT ZU ENTSCHEIDEN ───────────────
      Ein MixPi ist laut [[mixpi-die-geschichte]] ein WESEN, kein Geraetetyp.
      Ob die Box dauerhaft "MixPiBox" heisst oder einen Eigennamen bekommt,
      haengt an der Mehrbenutzer-Idee (Backlog E18) und sollte entschieden
      sein, BEVOR weitere Texte umbenannt werden — nicht danach.

  - id: dienst-zeichen-auf-den-kacheln
    kind: entscheidung
    title: "Dienst-Zeichen auf den Kacheln der neuen Oberflaeche — und die zweite Stelle, an der sie still herausfallen"
    tags: ['oberflaeche', 'neue-oberflaeche', 'kacheln', 'dienste', 'css', 'werkzeug', 'falle', '2026']
    source: "gebaut und in der Vorschau gemessen, 2026-08-02"
    body: |
      WAS: Jede Kachel der neuen Oberflaeche traegt seit 2026-08-02 eine
      kleine runde Plakette, die den DIENST zeigt: Spotify, Jellyfin, eine
      Datei auf der Box, Radio, Podcast. Code: `DIENST_MARKEN`,
      `markenBauen()`, `markenWort()` in NewDesign/app.js; `.kachel-marken`
      und `.marke*` in NewDesign/app.css.

      WOZU ES GUT IST — nicht Schmuck: Ohne Netz spielen die lokalen Sachen
      weiter und die aus Spotify nicht. Wer das Zeichen sieht, versteht den
      Unterschied, ohne ihn erklaert zu bekommen. Genau deshalb steht die
      Marke auch dann da, wenn ein Cover da ist: das Bild sagt nie, woher es
      kommt.

      ── WAS NICHT NEU ERFUNDEN WURDE ──────────────────────────────────
      Die Pfade fuer Spotify (drei Bogen) und Jellyfin (echte Marke) sind aus
      der klassischen Oberflaeche UEBERNOMMEN — dienststatus.component.ts und
      swiper.component.html haben sie, samt Farben #1db954 / #7d4bb8 und dem
      Blau #2d7dd2 fuer lokale Dateien. Zwei eigene Fassungen derselben Marke
      waeren eine Wahrheit zu viel gewesen. INLINE als Pfad, nicht als Datei:
      fuenfzig Kacheln duerfen keine fuenfzig Extraanfragen ausloesen, und die
      Box hat weder eine Emoji-Schrift noch mitgelieferte Logodateien.

      NEU sind nur Radio und RSS, die es in der klassischen Oberflaeche gar
      nicht gab:
        radio  Orangerot #e2571f, DIESELBE Wellenfigur wie der Kategorie-Knopf
               „Radio" (KATEGORIEN, id 'other'). Wer oben auf die Wellen tippt
               und unten dieselben Wellen sieht, muss nichts dazulernen.
        rss    Himbeere #c2255c. Die Welt faerbt RSS orange — genau das waere
               hier die schlechteste Wahl gewesen, weil es dann einen Farbton
               neben Radio laege und die beiden auf 29 px nicht mehr zu
               trennen waeren.

      'anderes' BEKOMMT KEIN ZEICHEN. Der Wert heisst „wir wissen es nicht"
      (medien.ts dienstVon); ein graues Fragezeichen auf jedem Cover waere
      Flaeche ohne Auskunft.

      ── DREI MASSE, die man sonst zweimal falsch macht ────────────────
      1. LINKS UNTEN, NICHT RECHTS. Rechts unten liegen `.tipp-spiel` (der
         Play-Knopf einer Playlist-Kachel) und `.regal-zahl`, beide bei
         right: 6px. In der klassischen Oberflaeche ist genau dieser
         Zusammenstoss schon passiert — der Kommentar bei `.cover-play` in
         swiper.component.scss haelt fest, dass der Knopf unter den
         Quellen-Plaketten verschwand. In der Vorschau nachgemessen:
         77 px Abstand, `ueberlappt: false`.
      2. GROESSE clamp(22px, calc(var(--kachel) * 0.19), 34px) — bei der
         Kachel von 150 px also 29 px (4,0 mm bei 0,14 mm/px), Figur 66 %
         davon = 19 px. Die Zahl ist NICHT geraten: die klassische
         Oberflaeche hat ihre Plakette vergroessert und den Grund ins Blatt
         geschrieben — „bei 26px war die Marke 16px breit und auf einem Cover
         aus zwei Metern Abstand kaum als Spotify oder Jellyfin zu erkennen".
         Die 64-px-Regel gilt hier NICHT: `pointer-events: none`, die
         Plakette ist Auskunft und kein Beruehrziel.
      3. DUNKEL: die Farbflaechen bleiben (an ihnen erkennt man den Dienst),
         nur der weisse Ring geht von 0,9 auf 0,55 zurueck. Er sitzt auf einem
         Cover, das abends ohnehin die hellste Flaeche im Zimmer ist.

      ── DAS REGAL: die Zeichen sind von BAUART her ehrlich ────────────
      Eine Regal-Kachel (mehrere Werke eines Interpreten) zeigt die Dienste
      ALLER Werke darin, gebildet aus `r.werke` — also aus genau der Liste,
      die `regalOeffnen` gleich anzeigt. Das ist die Gegenrichtung zu
      [[box-kachel-zeigt-fremde-quelle]], das bis heute offen ist: dort
      entstanden Zeichen und Inhalt an ZWEI Stellen mit je eigenem Filter, und
      die Kachel versprach Jellyfin, wo nur Spotify drin war. Hier gibt es
      diese zweite Stelle nicht.

      ── DIE FALLE, DIE DIESE ARBEIT SELBST ERZEUGT HAT ────────────────
      Ein Dienst braucht ZWEI Eintraege: die Figur in app.js (DIENST_MARKEN)
      und die Farbe in app.css (.marke-<dienst>). Fehlt eine davon, faellt der
      Dienst STILL durch — die Plakette bleibt farblos oder leer, und auf
      einem Cover sieht das nach Absicht aus. Dieselbe Fehlerklasse wie
      [[darstellung-feld-faellt-still-heraus]], nur an anderer Stelle.

      DAGEGEN GIBT ES JETZT EINE PRUEFUNG: `tools/dienst-marken-schau.mjs`
      liest BEIDE Seiten am Quelltext und meldet, was nur an einer steht.
      `--pruefen` gibt 1 zurueck; der Schritt haengt in tools/pruefen.sh
      („Dienst-Zeichen der Kacheln"). Ohne Schalter schreibt das Werkzeug eine
      HTML-Seite, die alle Zeichen in 22/29/34 px und 4-fach zeigt, hell und
      dunkel und auf drei GEMEINEN Untergruenden: Spotify-Gruen (verschluckt
      die gruene Plakette), Weiss (verschluckt den Ring), Schwarz (verschluckt
      den Schatten). Es haelt KEINE eigene Kopie der Pfade — eine Schau mit
      eigenen Pfaden zeigte irgendwann etwas anderes als die Oberflaeche, und
      man glaubte ihr.

      GEGENPROBE GEMACHT, wie es [[attrappe-luegt-durch-weglassen]] verlangt:
      `.marke-rss` aus app.css entfernt -> „FEHLT: 'rss' hat ein Zeichen in
      app.js, aber keine Farbe in app.css", Rueckgabe 1. Wieder eingesetzt ->
      wieder gruen. Eine Pruefung, die nie angeschlagen hat, prueft nichts.

      ── UND WIEDER DIE ATTRAPPE ───────────────────────────────────────
      tools/neu-vorschau.mjs kannte nur lokal, spotify und jellyfin. Wer die
      Zeichen daran geprueft haette, haette drei von fuenf gesehen und Radio,
      Podcast und den Fall „unbekannter Dienst" fuer erledigt gehalten, ohne
      sie je gesehen zu haben. Die Liste WERKE fuehrt jetzt alle fuenf Dienste
      plus einen Eintrag mit `dienst: 'anderes'` (der KEIN Zeichen bekommen
      darf), und `kennungFuer()` gibt Radio und RSS eine Adresse statt einer
      Id — werke.ts artVon sagt ausdruecklich, dass deren Stream-Adresse in
      `id` steht.

      ── GEMESSEN IN DER VORSCHAU (800x480, 2026-08-02) ────────────────
      Alle fuenf Plaketten 29x29 px. Regal „Kiddinx" zeigt zwei (Spotify +
      Jellyfin) neben der Zahl, ohne Ueberschneidung. Kachel „Raetselhaftes"
      (dienst 'anderes') zeigt keine. Mit Cover, ohne Cover (Buchstaben-Ersatz
      in der Mitte, Plakette in der Ecke) und in beiden Lichtern geprueft. Das
      aria-label der Kachel traegt den Dienst mit („Die drei ??? von Europa,
      Spotify"), die Plaketten selbst sind `aria-hidden` — sonst saegte eine
      Vorlesestimme dasselbe zweimal.

      NICHT GEMACHT, mit Absicht: Die kleinen „Oft gehoert"-Kacheln
      (`.oft-bild`, 104 px) bleiben ohne Zeichen — eine 29-px-Plakette waere
      dort ein Viertel der Flaeche, und dasselbe Werk steht im Raster darunter
      ohnehin mit Zeichen. Ebenso die Alben- und Titel-Kacheln in den Lanes
      einer Playlist: die Playlist selbst traegt den Dienst schon, und in ihr
      ist alles aus derselben Quelle.

      OFFEN: Hat eine Box NUR Spotify, traegt jede der fuenfzig Kacheln
      dieselbe gruene Plakette — Flaeche ohne Unterschied. Eine Regel „nur
      zeigen, wenn die Bibliothek mehr als einen Dienst hat" waere ehrlicher,
      erzeugt aber den Eindruck, die Zeichen seien kaputt, wenn sie
      verschwinden. Bewusst nicht entschieden; ein Schalter in „Darstellung"
      waere die dritte Moeglichkeit und braeuchte die drei Eintraege aus
      [[darstellung-feld-faellt-still-heraus]].

  - kind: falle
    id: pm2-reste-nach-der-systemd-umstellung
    title: "pm2 war weg, aber vier Stellen riefen es weiter — alle lautlos"
    sev: mid
    match: "pm2 restart|\\.pm2/logs|Spotify Services restarted"
    cause: "die Umstellung auf systemd-Units ersetzte die Dienste, nicht ihre Aufrufer; pm2 blieb installiert, also schlug nichts fehl"
    fix: "Aufrufer auf systemctl/journalctl umstellen und pm2 nicht mehr mitinstallieren"
    tags: ['pm2', 'systemd', 'installation', 'falle', 2026]
    source: "am Geraet belegt und in beiden Repos behoben (2026-08-02)"
    body: |
      Am 2026-07-29 zogen die Dienste von pm2 auf systemd-Units um
      (mupi-startzeiten-gemessen: App bereit 23,5 s -> 3,3 s). Die UNITS waren
      damit richtig - die AUFRUFER nicht. Weil pm2 als Programm liegen blieb,
      schlug nichts fehl; es geschah nur nichts mehr. Vier Stellen, gefunden
      am 2026-08-02:

      1. `server.ts` rief beim Umschalten der Tonausgabe
         `execFile('pm2', ['restart','spotify-control'], …, () => {})` auf.
         Leerer Rueckruf = kein Fehler, nirgends. Das Umschalten startete den
         Player also seit Tagen nicht mehr neu.
         JETZT: der vorhandene Helfer `systemctl(['restart','mupibox-player'],
         true)` mit `.catch`, das den Fehlschlag ins Protokoll schreibt.

      2. `scripts/mupibox/spotify_restart.sh` prueft `[ -e /usr/bin/pm2 ]` und
         druckt am Ende IMMER "Spotify Services restarted" - auch wenn kein
         Zweig zutraf. Eine Erfolgsmeldung ohne Erfolg beendet die Fehlersuche
         an der falschen Stelle.
         JETZT: systemd zuerst, pm2 nur als Rueckfall fuer alte Boxen, und die
         Ausgabe nennt, WAS neu gestartet wurde; sonst Rueckgabewert != 0.

      3. `protokolle.ts` verwies auf vier Dateien unter
         /home/dietpi/.pm2/logs/. GEMESSEN: sie standen seit dem 29.07. 20:51
         still - dem Tag der Umstellung. Da die Dateien aber noch EXISTIERTEN,
         lieferte die Verwaltung sie anstandslos aus. Die Protokollseite zeigte
         vier Tage alte Zeilen und sah dabei aus wie immer.
         MERKSATZ: ein LEERES Protokoll faellt auf, ein EINGEFRORENES nicht.
         JETZT: entfernt. Ersatz gab es bereits - die Journale ueber
         /api/protokolle/dienst/<name> mit mupibox-server und mupibox-player.
         Der Test dazu pruefte `length === 6`; eine blosse Anzahl sagt nichts
         darueber, ob die Eintraege noch stimmen. Er prueft jetzt die Namen
         UND dass kein Pfad mehr auf .pm2 zeigt.

      4. `spotify-control.ts` schrieb seinen Zaehler nach
         /home/dietpi/.pm2/logs/<start>_counter. Auf einer frisch aufgesetzten
         Box entsteht das Verzeichnis gar nicht mehr - jeder Schreibversuch
         waere mit ENOENT ins Journal gelaufen. Liest ohnehin niemand.
         JETZT: /tmp/mupibox-zaehler-<start>.json.

      ── UND DAS REZEPT INSTALLIERTE ES WEITER ──────────────────────────
      `npm-globals` in mupibox.yaml lief als `npm install -g pm2 @ionic/cli`
      mit `check: pm2 -v`. Das System-Rezept baute also auf, was das
      App-Rezept (Schritt `dienste-systemd`) wenige Minuten spaeter wieder
      abraeumte. GESTRICHEN. @ionic/cli dazu: der wird nur zum BAUEN von
      src/frontend-box gebraucht, und gebaut wird am Entwicklungsrechner - die
      Box bekommt fertige Dateien (bin/nodejs/deploy.zip).

      NICHT DEINSTALLIERT, und das ist wichtig: auf einer AELTEREN Box laeuft
      womoeglich noch ein pm2, und genau dessen Binaer braucht
      `dienste-systemd`, um die Prozesse abzuraeumen. Waere pm2 vorher schon
      entfernt, blieben sie als Waisen laufen und hielten die Ports 8200 und
      5005 - die EADDRINUSE-Schleife aus mupi-startzeiten-gemessen. Also: nicht
      mehr NEU anlegen, vorhandenes in Ruhe lassen.

      ── DIE LEHRE ──────────────────────────────────────────────────────
      Wer eine Laufzeitumgebung austauscht, muss ihre AUFRUFER suchen, nicht
      nur ihre Dienste. Ein `grep -rn pm2` haette alle vier Stellen am selben
      Tag gefunden. Solange das alte Programm installiert bleibt, meldet
      NICHTS einen Fehler - das ist die teuerste Sorte Umstellung.

      ── DER FUENFTE AUFRUFER, 27 TAGE SPAETER (29.08.2026) ─────────────
      `tools/mupi-check.py` - ausgerechnet das Erstdiagnose-Werkzeug, das
      Handbuch Paragraf 10 als ersten Schritt empfiehlt - fragte weiter
      `pm2 jlist` und `pm2 describe spotify-control`. Auf jeder heutigen
      Box hiess das: WARN "nicht lesbar" statt einer Antwort auf die
      Kernfrage "laufen die Dienste?". JETZT: `systemctl is-active` plus
      `NRestarts` fuer mupibox-server, mupibox-player und librespot;
      mupi_mqtt bewusst lax (seine ExecCondition macht inactive ohne
      eingerichtetes MQTT zum Sollzustand, erst failed zaehlt); der
      spotify-control-Pfad kommt aus `systemctl show -p ExecStart`.
      Die Zaehlung "vier Stellen" oben war also selbst ein Beispiel der
      eigenen Lehre: gefunden wird, wo gesucht wird - tools/ hatte 2026-08-02
      niemand abgesucht.

  - kind: falle
    id: pruefen-sh-haengt-auf-auth-integration
    title: "ERLEDIGT: tools/pruefen.sh schien zu haengen — es war ein Test, der nie endete"
    sev: low
    match: "Promise resolution is still pending but the event loop has already resolved"
    cause: "src/backend-api/src/auth.integration.spec.ts endete NIE von selbst; der Lauf hing unbegrenzt"
    fix: "GILT NICHT MEHR — der Lauf ist nach 8 s durch. Beim Einzelaufruf NODE_ENV=test setzen, siehe [[backend-tests-echte-zahl-und-node-env]]"
    tags: ['tests', 'werkzeug', 'falle', 'erledigt', 2026]
    source: "gemessen 2026-08-02; am selben Tag abends bei der Gesamtabnahme (Stand d4081965) als erledigt nachgemessen"
    body: |
      NACHTRAG VOM ABEND DES 02.08.2026 — DAS HIER IST GESCHICHTE. Bei der
      Gesamtabnahme lief `npm run test` im backend-api in 8 s durch: 943
      Tests, 238 Suiten, 0 Fehler, und `auth.integration.spec.ts` war dabei.
      Auch `tools/pruefen.sh` kommt an "Kerntests backend-api ok 8s" vorbei.

      Was BLEIBT, ist die Lehre darunter (CPU-Zeit statt Laufzeit messen) —
      und eine neue Stolperstelle: Wer die Datei EINZELN startet, muss
      `NODE_ENV=test` mitgeben, sonst kommt der Server nicht hoch und
      supertest meldet „Cannot read properties of undefined (reading
      'address')". Das ist ein Messfehler, kein Befund.

      ── DER URSPRUENGLICHE BEFUND ──────────────────────────────────────
      `tools/pruefen.sh` steht nach "Kerntests backend-api" still und laeuft
      NIE von selbst weiter. `auth.integration.spec.ts` endet nicht - der
      Testlaeufer bricht sie auch nicht ab.

      Die Meldung lautet:
        'Promise resolution is still pending but the event loop has already
         resolved'
      Sie erscheint ERST, wenn der Prozess von aussen beendet wird. Der Lauf
      zaehlt sie dann als "cancelled: 1"; die uebrigen 730 Tests bestehen und
      sind zu diesem Zeitpunkt laengst durch.

      ── DIE ZAHLENFALLE (selbst hineingetappt) ─────────────────────────
      Drei Laeufe meldeten "duration_ms" von 199 s, 259 s und 1552 s. Daraus
      liest man leicht "der Laeufer bricht nach rund 200-260 s ab" - falsch.
      Die Zahlen waren jedes Mal genau das eigene `timeout` davor. Die
      duration_ms MISST NUR, wie lange man selbst gewartet hat.

      NACHGEWIESEN am Prozess statt an der Zahl: nach 25 Minuten stand die
      CPU-Zeit bei 00:00:02 und die Last bei 0,0 % - der Prozess arbeitete
      nicht, er schlief. Eine Laufzeit sagt nichts darueber, ob etwas
      voranschreitet; die verbrauchte CPU-Zeit schon.

          ps -o pid,etime,time,pcpu -p <pid>     zweimal, ein paar Sekunden
                                                 auseinander

      VORBESTEHEND, NICHT SELBST GEBAUT: mit `git stash push -- src/`
      gegengeprueft - ohne jede fremde Aenderung haengt sie genauso. Wer eine
      eigene Aenderung verdaechtigt, spart sich das Suchen mit derselben Probe.

      FOLGE FUER DIE ARBEIT: `tools/pruefen.sh` laeuft ohne Zutun nicht durch
      und sprengt damit auch die 10 Minuten, die ein einzelner Werkzeugaufruf
      hoechstens laufen darf. Zwei Wege:
        * im Hintergrund starten und den haengenden Prozess erschlagen -
          `pkill -f "tsx --test"`; die Pruefung laeuft danach normal zu Ende
          (backend-player, Box-Oberflaeche, Typen, Baue).
        * oder gezielt einzeln pruefen:
            npm --prefix src/backend-api run test      (mit `timeout`!)
            npx --prefix src/backend-api tsc --noEmit -p src/backend-api/tsconfig.json

      NEBENBEFUND: der Testlauf hinterlaesst `config/kinderzeit-verbrauch.json`
      im Repo (server.ts legt sie unter `configBasePath` an, und der zeigt im
      Test auf `config/`). Sie steht in keiner .gitignore und taucht deshalb
      nach jedem Lauf als unversionierte Datei auf. NOCH OFFEN.

  # ══ „Weiterhoeren" in der neuen Oberflaeche (2026-08-02) ═══════════════════
  - kind: architektur
    id: weiterhoeren-reihe-neue-oberflaeche
    title: '„Weiterhoeren" statt „Oft gehoert": zwei Dateien, eine Reihe'
    source: "weiterhoeren.ts + /api/weiterhoeren + NewDesign/app.js, 2026-08-02"
    tags: [oberflaeche, resume, backend, neu, 2026]
    body: |
      ERSETZT, NICHT ERGAENZT. „Was hoere ich oft?" beantwortet das Raster
      darunter mit; „wo war ich stehengeblieben?" beantwortete auf der neuen
      Startseite NICHTS. Bei einem Hoerspiel von einer Stunde ist das die
      haeufigste Frage. Auf 800x480 kostet jede Reihe ein Drittel der
      sichtbaren Hoehe — zwei nebeneinander waeren keine Abkuerzung mehr.

      ── ZWEI DATEIEN, WEIL KEINE ALLEIN REICHT ────────────────────────
        resume.json    sagt WO   (Titelnummer und Stelle im Titel)
        gespielt.json  sagt WANN (`zuletzt` je Medium)

      `resume.json` fuehrt KEINEN Zeitstempel; die Eintraege stehen in
      Einfuegereihenfolge (`push`). Ein eigenes Zeitfeld haetten nur die
      Eintraege der NEUEN Oberflaeche gehabt — die des klassischen Players
      stuenden dann fuer immer hinten, ohne dass man saehe warum. Der Verlauf
      dagegen wird von BEIDEN Oberflaechen gefuettert (player.service.ts
      `gespieltMelden`, NewDesign `gespieltMelden`).

      Verbunden werden beide ueber `gespielt.ts meldungAusEintrag()` — dieselbe
      Formel, nicht nachgebaut. ACHTUNG: dieser Schluessel ist NICHT
      `medienSchluessel`. Der Verlauf benutzt die ROHEN Namen
      (`library:WDR|Die Maus`), `medienSchluessel` normalisiert
      (`lokal:t:wdr|die maus`). Beim ersten Anlauf verwechselt — die Sortierung
      sah dann aus, als griffe der Verlauf gar nicht.

      ── DIE SCHWELLEN, und warum sie so sind ──────────────────────────
      (src/backend-api/src/weiterhoeren.ts, geprueft in weiterhoeren.spec.ts)

        Titelnummer > 1        -> IMMER weiterhoerbar. Die gespeicherte Zeit
                                  zaehlt ab dem Anfang DIESES Stuecks; 28
                                  Sekunden bei Titel 2 sind der Normalfall,
                                  kein Gegenbeweis.
        MIN_POSITION_S = 30    -> nur fuer Titel 1 und nur, wo es Sekunden
                                  gibt (Spotify). Wer eine halbe Minute
                                  hineinhoert und aufhoert, meint „gefiel
                                  nicht", nicht „weiterhoeren".
        MIN_ANTEIL = 0,02      -> dasselbe fuer mpv-Eintraege, die NUR Prozent
                                  kennen. Zwei Prozent sind bei drei Minuten
                                  vier Sekunden, bei einer Stunde eine Minute.
        FERTIG_ANTEIL = 0,97   -> praktisch durch. NUR OHNE Titelnummer
                                  entscheidbar: die gespeicherte Laenge ist die
                                  des laufenden TITELS, nicht des Albums.

      Reihenfolge: zuletzt Angefangenes zuerst. Ohne Verlaufszeit nach hinten,
      untereinander in Dateireihenfolge von hinten (spaeter = neuer).

      ── FORTSETZEN: ZWEI WEGE, WEIL ES ZWEI MASCHINEN SIND ────────────
        Spotify  EIN Befehl. `spotify/now/spotify:<typ>:<id>:<nr>:<ms>` —
                 Titelnummer 1-basiert und unverschoben, Stelle in
                 MILLISEKUNDEN. Ein `seekpos:` hinterher waere der Weg, der
                 bei laufender Wiedergabe NICHT wirkt.
        mpv      DREI Befehle mit Wartezeiten: Album starten, `tracknr:N`
                 (2000 ms spaeter), `seekpos:<prozent>` (weitere 1500 ms).
                 Einheit PROZENT. Nie genau 0 schicken.

      ── WAS BEWUSST NICHT GEMERKT WIRD ────────────────────────────────
      Radio (ein Strom hat keine Stelle) und Jellyfin (die klassische
      Oberflaeche hat dafuer keinen Fortsetzen-Weg — eine Kachel, die „weiter"
      verspricht und von vorn anfaengt, ist schlimmer als keine).

      ── ERLEDIGT AM 02.08.2026, hier stand vorher „OFFEN" ─────────────
      Ein Werk, das zu ENDE gehoert wurde, blieb in der Reihe stehen:
      `resume.json` wusste nicht, wie viele Titel ein Werk hat, und „Titel 12
      zu 99 %" ist ohne diese Zahl nicht von „mittendrin" zu unterscheiden.
      Jetzt steht die Zahl in `resumeGesamtTitel` — bei Spotify faellt sie
      beim Aufloesen der Playlist-Position mit ab, bei mpv meldet die
      Oberflaeche `totalTracks` mit. Kein zusaetzlicher Netzabruf. Der ganze
      Vorgang steht in [[weiterhoeren-playlist-position]].

      ── WELCHE ZEILEN SIE ZEIGT, ENTSCHEIDET SEIT 02.08.2026 DIE AUSWAHL ─
      Die Reihe zeigt nur Eintraege, deren Werk in der Liste steht, die das
      Raster gerade zeigt (Kategorie und Regal). Bis dahin verschwand sie in
      jeder gewaehlten Kategorie GANZ und stand im Regal eines Interpreten
      ungefiltert da. Begruendung und Messung: [[weiterhoeren-folgt-der-auswahl]].

  - kind: falle
    id: weiterhoeren-playlist-position
    title: 'Weiterhoeren sprang bei Playlisten ins falsche Kapitel — track_number ist die ALBUM-Nummer'
    sev: hoch
    source: "weiterhoeren.ts + NewDesign merkStand, Befund und Reparatur 2026-08-02"
    match: "resumespotifytrack_number|item.track_number|offset.position|falsches Kapitel|resumeVersatzGeprueft"
    cause: "gemerkt wurde `item.track_number` (die Nummer des Stuecks in SEINEM Album), fortgesetzt wird aber ueber `offset.position` IM ABSPIELZUSAMMENHANG — bei einer Playlist aus vielen Alben sind das zwei verschiedene Zahlen"
    fix: "Die Position aus der TITELLISTE des Werks aufloesen (`item.uri` gegen `versatz`) und den aufgeloesten Stand mit `resumeVersatzGeprueft: true` markieren. Ohne Marke gilt die Zahl bei einer Playlist als unbrauchbar."
    tags: [resume, weiterhoeren, spotify, playlist, neue-oberflaeche, falle, 2026]
    body: |
      VIER EINTRAEGE TEILEN SICH `track_number` — die eine Frage, die
      zwischen ihnen entscheidet, ist WELCHE OBERFLAECHE und WELCHE STUFE:

        was du in der Hand hast              -> lies
        ─────────────────────────────────────────────────────────────────
        neue Oberflaeche, Zahl beim MERKEN   -> hier
        klassische Oberflaeche, dieselbe     -> [[klassische-oberflaeche-merkt-album-nummer]]
        Zahl stimmt, das ABSPIELEN springt   -> [[resume-playlistposition-schickt-in-die-falsche-folge]]
        warum die Zahl ueberhaupt unscharf   -> [[resume-titelnummer-bei-playlists-unscharf]]

      Beide Oberflaechen schreiben in dieselbe `resume.json`; wer nur die
      eine repariert, misst die andere gruen und hoert trotzdem Kapitel 3.

      DAS SZENARIO, an dem es auffiel: Eine Hoerspiel-Playlist mit 300
      Kapiteln. Das Kind hoert Kapitel 47; dieses Stueck ist auf SEINEM Album
      Titel 3. Gemerkt wurde 3. Der Tipp auf „Weiterhoeren" startete damit
      Kapitel 3 der Playlist — fast am Anfang, an einer Stelle, die das Kind
      nie gehoert hat. Und der Fortschritt (`progress_ms`) landete auf einem
      ganz anderen Stueck.

      GEMESSEN AM ALTEN STAND (48f532ba, 02.08.2026), nicht geschlossen:

        stelleAus({playlistid: …}, {titelNr: 3, bisher: 92.5, dauer: 600})
          -> resumespotifytrack_number = 3
        weiterhoerbare([...])[0].titelNr = 3        (erwartet: 47)

      WARUM ES NIEMAND SAH: „3" ist in beiden Zaehlweisen eine gueltige Zahl.
      Bei einem ALBUM sind sie sogar immer gleich — und Alben waren der Fall,
      an dem entwickelt und geprueft wurde. Auf der Box lagen am 02.08.2026
      neun gemerkte Stellen, davon acht Alben; die einzige Playlist stand auf
      0 („von vorn") und log deshalb nicht.

      ── WAS SPOTIFY HERGIBT UND WAS NICHT ─────────────────────────────
      Die Wiedergabe-Auskunft (`/state` des Abspieldienstes) nennt die
      Position im Zusammenhang NICHT. Sie nennt:
        context.uri        welche Playlist laeuft
        item.uri           welches Stueck laeuft
        item.track_number  dessen Nummer in SEINEM Album  <- die Falle
      Die Position ergibt sich erst aus der TITELLISTE der Playlist. Die holt
      der SERVER (`titelEinesWerks`, seitenweise, mit Zwischenspeicher von
      einer Stunde) — der Browser darf api.spotify.com nicht rufen.

      ── DIE +1 STEHT AN GENAU EINER STELLE ────────────────────────────
      `versatz` ist Spotifys eigene, 0-BASIERTE Zaehlung; der Abspieldienst
      will 1-basiert, weil er intern wieder eins abzieht. Die Addition steht
      deshalb in `zusammenhangAus` und NIRGENDS sonst — dieselbe Ueberlegung
      wie bei `abspielBefehl`.

      DER VERSATZ, NICHT DER RANG IM FELD: `titelEinesWerks` laesst gesperrte
      und entfernte Stuecke weg, zaehlt den Versatz aber weiter. Wer die Stelle
      im Feld naehme, laege hinter jeder Luecke um eins daneben.

      LINKED_FROM HAT VORRANG: Spotify tauscht in manchen Laendern ein Stueck
      gegen eine andere Ausgabe aus („relinking"). Dann traegt `item.uri` die
      GESPIELTE Ausgabe, waehrend in der Playlist noch die urspruengliche
      steht. Gesucht wird die, die in der Liste steht.

      ── ALTE STAENDE: NICHT RATEN, SONDERN MARKIEREN ──────────────────
      `resume.json` ueberlebt jede Aktualisierung, und die KLASSISCHE
      Oberflaeche (`saveResumeFiles`) schreibt bis heute die Album-Nummer.
      Einem Eintrag sieht man die Zaehlweise nicht an. Deshalb traegt nur der
      neue Weg `resumeVersatzGeprueft: true`; fehlt die Marke, gilt die Zahl
      bei einer PLAYLIST als unbrauchbar und der Eintrag faellt aus der Reihe
      (`nummerVerlaesslich`). Bei Alben aendert sich nichts — dort waren beide
      Zaehlweisen immer gleich.

      IM ZWEIFEL LIEBER GAR NICHTS MERKEN: Laesst sich die Position nicht
      aufloesen (kein Netz, kein Token, Stueck nicht mehr in der Liste),
      antwortet `POST /api/weiterhoeren` mit `{"status":"nichtAufloesbar"}`
      und schreibt NICHTS. Die vorige Stelle bleibt stehen — sie ist
      hoechstens einen Merktakt (15 s) alt. Ein Werk, das aus der Reihe
      faellt, kostet einen Tipp mehr im Raster; ein falscher Sprung klingt
      fuer ein Kind nicht nach Fehler, sondern nach kaputt.

      ── DIE GESAMTZAHL FIEL DABEI AB ──────────────────────────────────
      `resumeGesamtTitel` kommt aus derselben Aufloesung (Spotify) bzw. aus
      `totalTracks` der mpv-Warteschlange (die Oberflaeche meldet es mit).
      Damit ist „der letzte Titel und praktisch durch" entscheidbar, und ein
      durchgehoertes Werk faellt aus der Reihe statt fuer immer stehenzubleiben.
      Bei mpv sind beide Fehlrichtungen harmlos: zu wenig gemeldet heisst „zu
      frueh aus der Reihe" (steht noch im Raster), zu viel heisst „wie bisher".

      NACHSEHEN, WAS AUF EINER BOX LIEGT:
        node tools/weiterhoeren-stellen.mjs --box <adresse>
      Es stellt `GET /api/resume` und `GET /api/weiterhoeren` nebeneinander
      und benennt jeden Eintrag in alter Zaehlweise. Es rechnet die Regel
      NICHT nach — was in der Reihe steht, sagt der Server.
    related: [resume-playlistposition-schickt-in-die-falsche-folge, resume-titelnummer-bei-playlists-unscharf, klassische-oberflaeche-merkt-album-nummer]

  - kind: falle
    id: resume-lokal-ist-prozent-nicht-sekunden
    title: 'resumelocalprogressTime ist PROZENT — „Weiter bei 1:30" fuer 90 %'
    sev: medium
    source: "merkposition.ts vs. spotify-control.ts, gefunden 2026-08-02"
    match: "Weiter bei|resumelocalprogressTime|resumerssprogressTime|seekPosition"
    cause: "der Player legt `currentPlayedLocal.progressTime` ab, und das setzt der Abspieldienst im Ereignis `percent_pos` — es sind PROZENT, keine Sekunden"
    fix: "Sekunden gibt es NUR bei Spotify (resumespotifyprogress_ms). Fuer mpv-Eintraege den Anteil zeigen, nicht eine Uhrzeit."
    tags: [resume, einheiten, player, falle, 2026]
    body: |
      DIE EINHEITEN IN resume.json SIND JE DIENST VERSCHIEDEN:

        resumespotifyprogress_ms   MILLISEKUNDEN   (spotifyApi nimmt position_ms)
        resumelocalprogressTime    PROZENT 0..100  (mpv percent_pos)
        resumerssprogressTime      PROZENT 0..100  (dasselbe, RSS laeuft ueber mpv)

      NACHWEIS: `spotify-control.ts` hat genau eine Stelle, die
      `currentMeta.progressTime` schreibt — `player.on('percent_pos', …)`.
      `player.page.ts saveResumeFiles` uebernimmt diesen Wert unveraendert.

      WER ES RICHTIG HATTE: `resumePlayback` gibt ihn an
      `playerService.seekPosition()` weiter, und `seekpos:` ist fuer mplayer
      ebenfalls PROZENT. Der Sprung stimmte also immer.

      WER ES FALSCH HATTE: `merkposition.ts` (`positionSekunden`) las den Wert
      als Sekunden. Der blaue Knopf eines lokalen Albums beschriftete sich mit
      „Weiter bei 1:30", wo 90 PROZENT gemeint waren. Eine Zahl, die plausibel
      aussieht, wird nie hinterfragt — deshalb stand es dort monatelang, samt
      Test, der es festschrieb.

      Behoben am 2026-08-02: `positionSekunden` liefert jetzt `null` fuer
      mpv-Eintraege, daneben steht `positionAnteil` (0..1) fuer beide Wege, und
      `weiterText` schreibt „Weiter bei 90 %" statt einer erfundenen Uhrzeit.
      Dieselbe Trennung in `src/backend-api/src/weiterhoeren.ts`.

      MERKREGEL: Wo in dieser Box eine Zahl herumliegt, gehoert die Einheit an
      den Namen oder in den Kommentar. „progressTime" sagt nichts.

  - kind: falle
    id: startpunkt-ab-war-sekunden-statt-millisekunden
    title: "Nach dem Fortsetzen war Spotifys Nachlauf-Schutz still abgeschaltet"
    sev: medium
    source: "spotify-control.ts playSpotify + fortschritt.ts, gefunden 2026-08-02"
    match: "Balken springt|richtig\\(|startpunkt|resumeProgess|ab-sekunde"
    cause: "das letzte Glied des Abspielbefehls ist MILLISEKUNDEN (playMe reicht es als position_ms weiter), wurde fuer `startpunkt.ab` aber als Sekunden gelesen und mit 1000 malgenommen"
    fix: "fortschritt.startpunktAus() benutzen — eine geprueste Stelle statt zweier Deutungen desselben Feldes"
    tags: [player, spotify, fortschritt, einheiten, falle, 2026]
    body: |
      DER BEFEHL:  `spotify:<typ>:<kennung>:<titelnummer>:<stelle-in-ms>`

      `playMe()` liest das letzte Glied als `position_ms` — Millisekunden.
      Der Befehlsverteiler las DASSELBE Glied als Sekunden:

          const abTeil = Number.parseInt(String(command.name).split(':').pop())
          startpunkt = { seit: Date.now(), ab: abTeil > 0 ? abTeil * 1000 : 0 }

      FOLGE: nach einem Fortsetzen bei 1:32 stand `ab` bei 92.500.000 ms, also
      ueber 25 Stunden. `fortschritt.richtig()` vergleicht
      `gemeldet <= ab + vergangen + TOLERANZ` — das war von da an IMMER wahr,
      und die Absicherung gegen Spotifys verspaetete Antworten (der gemessene
      Ruecksprung von 105 s auf 0,9 s) lief bis zum naechsten Start von vorn
      ins Leere.

      WARUM ES NIE AUFFIEL: Es wirkt nur nach einem Fortsetzen, und das war
      bisher der seltene Fall (nur ueber die „Weiterhoeren"-Seite der
      klassischen Oberflaeche). Der Fehler zeigte sich als gelegentlich
      springender Balken — also als etwas, das man dem Netz zuschreibt. Mit der
      Weiterhoeren-Reihe der neuen Oberflaeche wird Fortsetzen zum Normalfall.

      BEHOBEN, INDEM DIE EINHEIT UMZOG: `fortschritt.startpunktAus(name, jetzt)`
      ist rein und geprueft (fortschritt.spec.ts). Im Verteiler steht kein
      Rechenschritt mehr.

  - kind: falle
    id: addresume-vergleicht-undefined-ids
    title: "/api/addresume haelt zwei Playlists fuer denselben Eintrag"
    sev: low
    source: "server.ts /api/addresume, gelesen 2026-08-02"
    match: "resume.*doppelt|resume.*ueberschrieben|addresume"
    cause: "`data.findIndex(item => item.id === req.body.id)` — bei Playlists ist `id` auf beiden Seiten undefined, und `undefined === undefined` ist wahr"
    fix: "ueber `medienSchluessel()` vergleichen (id || playlistid || showid || audiobookid || Titel+Interpret)"
    tags: [backend, resume, falle, 2026]
    body: |
      Zwei Fehler in einer Zeile, und sie heben sich NICHT auf:

        * ZU VIEL: zwei verschiedene Playlists (beide ohne `id`) gelten als
          derselbe Eintrag — die zweite gemerkte Stelle ueberschreibt die
          erste.
        * ZU WENIG: derselbe Eintrag mit einer anderen Kennungsart (mal
          `showid`, mal `audiobookid`) gilt als neu — dann steht er doppelt.

      NICHT GEAENDERT, weil daran die klassische Oberflaeche haengt und der
      Fehler dort seit jeher so wirkt. Der NEUE Weg `POST /api/weiterhoeren`
      vergleicht ueber `medienSchluessel()` und legt keine Dubletten an; die
      Weiterhoeren-Reihe zeigt eine doppelte Stelle ausserdem nur EINMAL (die
      hintere gewinnt, dort wird angehaengt).
    related: [addresume-sperre-nur-ueber-das-lesen]

  - kind: falle
    id: resume-titelnummer-bei-playlists-unscharf
    title: "Spotify nennt die Titelnummer im ALBUM, nicht in der Playlist"
    sev: low
    source: "player.page.ts saveResumeFiles / NewDesign merkStand, 2026-08-02"
    match: "track_number|falsche Folge|Playlist.*springt"
    cause: "`item.track_number` aus /me/player ist die Nummer des Stuecks in SEINEM Album; die Playlist-Position nennt Spotifys Wiedergabe-Auskunft gar nicht"
    tags: [spotify, resume, falle, 2026]
    body: |
      Bei einem ALBUM stimmt die Nummer (Albumposition = Abspielposition).
      Bei einer PLAYLIST aus mehreren Alben stimmt sie nicht: fortgesetzt wird
      an der Playlist-Position, die zufaellig dieselbe Zahl traegt.

      BEIDE OBERFLAECHEN LEGEN DENSELBEN WERT AB — bewusst. Ein zweiter,
      anderer Wert waere schlimmer als ein bekannt unscharfer: dann haengt es
      davon ab, welche Oberflaeche zuletzt gehoert hat.

      Ein besserer Wert waere zu haben, kostet aber einen Web-API-Abruf der
      Playlist beim Merken (Position von `item.id` in der Titelliste). NOCH
      NICHT GEBAUT.
    related: [resume-playlistposition-schickt-in-die-falsche-folge, weiterhoeren-playlist-position, klassische-oberflaeche-merkt-album-nummer]

  - kind: howto
    id: tests-fuer-die-neuen-oberflaechen
    title: "Die beiden neuen Oberflaechen testen — ohne sie anzufassen"
    tags: ['tests', 'newdesign', 'frontend-admin', 'werkzeug', 2026]
    source: "gebaut und mutationsgeprueft 2026-08-02"
    body: |
      AUSGANGSLAGE (gezaehlt 2026-08-02): die Abdeckung war genau umgekehrt zur
      Baurichtung. Die ALTE Angular-Box-Oberflaeche hatte 35 Testdateien, die
      NEUEN gar keine - NewDesign/ bei 3609 Zeilen app.js, src/frontend-admin
      bei 23 Dateien. "Gruen" hiess dort nur "laesst sich uebersetzen".

      ── NewDesign/ : tools/e2e/neu-oberflaeche.test.mjs ──────────────────
      DREI ENTSCHEIDUNGEN, und jede hat einen Grund:

      1. KEINE ZEILE AN app.js. Die Datei ist eine IIFE ohne einen einzigen
         Export - importierbar ist daraus nichts. Sie fuer Tests aufzubohren
         (window.__TEST__ = {...}) haette die Produktionsdatei veraendert.
         Stattdessen laeuft sie als das, was sie ist: eine Seite im Browser.
         Geprueft wird VERHALTEN, nicht Innerei.

      2. KEINE EIGENE ATTRAPPE. Die Antworten kommen aus
         `tools/neu-vorschau.mjs`, das den Vertrag schon traegt - samt
         umschaltbarer Zustaende (voll/leer/kaputt/fehlt/spielt/still/
         kinderzeit). Eine zweite, selbstgebaute Attrappe waere genau der
         Fehler aus neue-oberflaeche-ohne-box-ansehen noch einmal.

      3. KEINE NEUE ABHAENGIGKEIT. vitest und jsdom sind NICHT im Baum
         installiert (`npx vitest` in pruefen.sh laeuft nur aus dem
         npx-Zwischenspeicher!). Der Browser wird per CDP ueber `ws`
         gesteuert, wie in den uebrigen tools/e2e/*.mjs.

      7 Tests, rund 2 s. Ohne Browser ueberspringt sich der Lauf, statt rot
      zu werden.

      ── src/frontend-admin : angular.json hatte KEIN test-Ziel ───────────
      `package.json` enthielt zwar `"test": "ng test"`, aber in angular.json
      standen nur `build` und `serve` - der Aufruf konnte nie funktioniert
      haben. Ergaenzt in der schlanken Form: Karma-Baumeister OHNE
      `src/frontend-box/src/test.ts` und OHNE `karma.conf.js` (ab Angular 16 sucht er die
      *.spec.ts selbst), dazu ein `tsconfig.spec.json`. Alle noetigen Pakete
      (karma 6.4.4, jasmine-core, zone.js) lagen bereits im Hauptbaum.

      34 Tests: alle vier Dienste, die Wache und die Seite Kinderzeit.
      Geprueft wird jeweils die ZUSAGE AUS DEM KOPF DER DATEI, nicht die
      Verkabelung — die Dienste sind absichtlich duenn, wertvoll sind ihre
      FEHLERTEXTE und Vorgabewerte:
        * NetzwerkDienst: der woertliche Grund des Backends geht VOR den
          eigenen Saetzen; `gesichert: a.gesichert !== false` heisst, dass ein
          fehlendes Feld als "gesichert" gilt (andersherum laedt es dazu ein,
          das Passwort wegzulassen); `fensterSek ?? 120`.
        * SystemDienst: ein ausbleibender Rueckgabewert beim Neustart ist KEIN
          Fehler — die Box nimmt den antwortenden Prozess mit. Der Satz muss
          neutral bleiben, sonst meldet die Verwaltung nach jedem geglueckten
          Neustart einen Fehlschlag.
        * Kinderzeit: "aus bedeutet aus" (`r.aktiv === true`, nicht `!!`),
          Montag geht auf die WOCHENTAGE und nicht aufs Wochenende, fehlende
          Tage werden aufgefuellt (die Felder binden direkt an `tage[x]`).

      SEITEN TESTEN, OHNE SIE ANZUFASSEN: die geprueften Glieder sind
      `protected`/`private` — das gilt nur fuer den Uebersetzer, zur Laufzeit
      sind es gewoehnliche Eigenschaften. Der Zugriff laeuft ueber ein
      Hilfs-Interface, das genau die benutzten Namen nennt (kein pauschales
      `any`), damit auffaellt, wenn eines verschwindet. Der Konstruktor der
      Seite laedt schon los; der Test muss beide Abrufe bedienen und die
      Fixture am Ende `destroy()`en, sonst laeuft ihr 15-Sekunden-Takt weiter.

      WICHTIG BEIM AUFRUF: `npx --prefix src/frontend-admin ng test` scheitert
      mit "not available when running the Angular CLI outside a workspace".
      Es muss `(cd src/frontend-admin && npx ng test …)` sein - so macht es
      pruefen.sh beim Box-Schritt auch.

      ── DIE MUTATIONSPROBE IST PFLICHT, NICHT KUER ──────────────────────
      Gruene Tests beweisen nichts, solange sie nicht auch ROT werden koennen -
      sonst sind sie die naechste Wache, die sich selbst zufriedenstellt
      (mupi-audio-guard-stumm). Alle vier Male hat es gebissen, je genau der
      richtige Test:

        app.js: Abspielweg auf `${API}/spielen/…` umgebogen
                -> nur "spielt AUSSCHLIESSLICH ueber /player/…" faellt
        app.js: `leiste.hidden = false` -> `true`
                -> nur "zeigt den Mini-Player" faellt
        wache.ts: `if (lage.angemeldet)` -> `if (true)`
                -> nur "schickt zur Anmeldung" faellt
        anmeldung.dienst.ts: Fehlerzweig auf angemeldet:true
                -> nur "nimmt bei unerreichbarem Backend NICHT angemeldet an"

      Danach `git status` gegenpruefen, dass die Mutationen wirklich weg sind.

      ── ZWEI FEHLER IM TEST, DIE DER ERSTE LAUF FAND ────────────────────
      Beide sassen im Test, nicht im Code - und beide waeren als "Fehler in
      der Oberflaeche" fehlgedeutet worden:

      * Unter /player/ gibt es ZWEI Sorten: `/player/local` und
        `/player/state` sind ABFRAGEN (app.js:2752, 2764), nur
        `/player/<raum>/<befehl>` ist ein BEFEHL (app.js:1844). Nur der laeuft
        durch die Kinderzeit-Pruefung. Wer nicht trennt, meldet `/player/local`
        als Regelbruch.
      * `anmelden()`/`abmelden()` awaiten INTERN weiter: nach dem `flush` der
        ersten Antwort geht `/api/auth/state` erst im naechsten Takt hinaus.
        Ohne ein `await takt()` dazwischen meldet HttpTestingController
        "found none".

  - kind: falle
    id: qr-formatkopie-verkehrt-herum
    title: "Ein QR-Code, der sich lesen laesst und trotzdem falsch ist"
    sev: mid
    match: "qr\\.py|Formatkopie|_format_setzen"
    cause: "die erste Kopie der Formatangabe wurde vom niederwertigsten Bit an geschrieben statt vom hoechstwertigen; Leser kommen ueber die ZWEITE Kopie trotzdem an die Maske"
    fix: "Feld fuer Feld gegen qrencode -8 vergleichen, nicht nur zurueckzulesen versuchen"
    tags: ['qr', 'einrichtung', 'tests', 'falle', 2026]
    source: "beim Bau von remote-step-installer/tools/qr.py gefunden (2026-08-02)"
    body: |
      WOFUER ES DEN KODIERER GIBT: Beim ersten Start soll die Box auf ihrem
      eigenen Schirm einen QR-Code zeigen, den ein Handy abfotografiert - damit
      entfaellt der Laptop, der heute per `sdprep` die WLAN-Daten auf die SD
      schreibt. Der Code muss also auf einer FRISCHEN DietPi entstehen: kein
      pip, kein nachinstalliertes Programm, im Zweifel kein Netz. Deshalb
      `remote-step-installer/tools/qr.py` in reiner Standardbibliothek -
      dieselbe Regel wie beim Agenten und bei der Boot-Animation.

      ── DER FEHLER ──────────────────────────────────────────────────────
      Die Formatangabe (Fehlerkorrekturstufe + Maske) steht ZWEIMAL im Code.
      Meine erste Kopie lief vom niederwertigsten Bit an, die zweite vom
      hoechstwertigen. Richtig ist: BEIDE vom hoechstwertigen.

      UND ES FIEL NICHT AUF. `zbarimg` las alle Probecodes anstandslos zurueck
      - Leser kommen ueber die zweite Kopie (und die BCH-Korrektur) an die
      Maske. Ein Rueckleseversuch allein haette diese Klasse Fehler NIE
      gefunden: er prueft das Ergebnis, nicht den Aufbau.

      GEFUNDEN hat es erst der Vergleich Feld fuer Feld mit `qrencode`.

      ── DIE MERKSAETZE ──────────────────────────────────────────────────
      * EINE ZWEITE, UNABHAENGIGE UMSETZUNG findet, was ein Rueckleseversuch
        durchwinkt. Bei allem, was einer Norm folgt, ist der Fremdvergleich
        die staerkere Probe.
      * ABER ER MUSS DASSELBE VERGLEICHEN. Zwei Fallen dabei, beide
        hineingetappt:
          - qrencode zerlegt den Text standardmaessig in GEMISCHTE MODI
            (Ziffernblöcke kompakter). remote-step-installer/tools/qr.py kann nur Byte-Modus. Ohne
            `-8` vergleicht man zwei verschiedene, jeweils gueltige
            Kodierungen und nennt den Unterschied einen Fehler.
          - Die MASKENWAHL haengt an vier Strafregeln, und in deren
            Randfaellen unterscheiden sich Umsetzungen legitim. Der Test liest
            deshalb qrencodes Maske aus dessen Formatbits und baut DIESELBE
            nach; verglichen wird alles Uebrige.
        Mit beidem: 5 von 5 Faellen deckungsgleich, bis aufs letzte Feld.
      * EIN ZU KLEINES BILD SIEHT AUS WIE EIN KAPUTTER KODIERER. Bei einem
        Pixel je Feld liest zbarimg gar nichts. Der erste Verdacht galt dem
        Kodierer; es war die Skalierung (jetzt Skala 8 im Test).

      ── STAND ───────────────────────────────────────────────────────────
      remote-step-installer/tools/qr.py: Byte-Modus, Fassung 1-10, Stufe M. Reicht mit Abstand -
      eine Adresse braucht Fassung 3, ein WLAN-Code selten mehr als 4.
      tests/qr_test.py: 23 Pruefungen (Fremdvergleich, Rueckleseprobe,
      Fassungswahl, Formatbits gegen die genormte Tabelle, Ueberlauf).
      Ohne qrencode/zbarimg ueberspringt sich der jeweilige Teil, statt rot zu
      werden - beide sind Werkzeuge des Entwicklungsrechners, nicht der Box.

  - kind: howto
    id: einrichtungsschirm-qr-auf-der-box
    title: "Der Einrichtungsbildschirm: QR-Code auf dem Schirm der Box — und drei Fallen dabei"
    tags: ['einrichtung', 'installer', 'framebuffer', 'mixpi', 'qr', 2026]
    source: "gebaut und in der Vorschau geprueft (2026-08-02)"
    body: |
      ZIEL: Beim ersten Start soll niemand mehr einen Laptop brauchen. Die Box
      zeigt auf ihrem eigenen 800x480-Schirm, wohin das Handy gehen muss —
      als QR-Code und als Adresse zum Abtippen.
      `remote-step-installer/tools/einrichtung-schirm.py`.

      NICHTS DAVON IST NEU GEBAUT: Framebuffer, PSF-Schriften, das Abkoppeln
      der Konsole und DIE FARBEN DES FORKS kommen per Import aus
      `mupibox-boot-splash.py` — die Animation laeuft unmittelbar vorher auf
      demselben Schirm, ein zweiter Farbsatz waere sofort ein zweiter Look.
      Der Code selbst kommt aus `qr.py` (siehe qr-formatkopie-verkehrt-herum).

      ── FALLE 1: NUR ASCII AUF DEN SCHIRM ───────────────────────────────
      Der PSF-Leser spricht Glyphen ueber `ord(zeichen)` als INDEX an, nicht
      ueber die Unicode-Tabelle der Schrift. Ein Mittelpunkt „·" landete damit
      auf Glyphe 0 und stand als „0" im Bild: „Port 8098 0 /einrichtung".
      Dasselbe gilt fuer „…". Also ASCII schreiben — und beim naechsten Text
      daran denken, dass Umlaute in einem BOXNAMEN genauso betroffen waeren.

      ── FALLE 2: DAS MASKOTTCHEN IST AUF EINER BLANKEN BOX NICHT DA ─────
      MixPi liegt in `NewDesign/bilder/` — also in der App, die zu diesem
      Zeitpunkt noch gar nicht installiert ist. Dieselbe Henne-Ei-Lage wie bei
      hostapd. Deshalb reist das Bild MIT DEM INSTALLER:
      `remote-step-installer/dateien/mixpi-hoert.png`; die App-Pfade stehen
      als spaetere Kandidaten dahinter.
      Genommen wird der ZUSTAND `hoert`, der Standard aus
      `NewDesign/maskottchen.json`. Einen Zustand „einrichten" gibt es nicht
      und er wurde hier auch nicht erfunden: laut
      mixpi-maskottchen-familie gehoert die Zuordnung in jene Datei, nicht in
      den Code.

      ── FALLE 3: DER NAME — UND WO DIE REGEL AN IHRE GRENZE KOMMT ───────
      benennung-mixpibox-drei-toepfe sagt: in der Oberflaeche steht der
      KONFIGURIERTE Name, nie eine Konstante. Hier gibt es aber einen echten
      Grenzfall: dieser Schirm laeuft VOR der Installation, da existiert
      `/etc/mupibox/mupiboxconfig.json` noch nicht — es ist nichts da, was man
      lesen koennte. Also: Konfiguration lesen, wenn sie da ist; sonst
      „MixPiBox". Sobald sie existiert, gilt wieder ausschliesslich sie.

      ── ANSEHEN OHNE BOX ────────────────────────────────────────────────
          python3 remote-step-installer/tools/einrichtung-vorschau.py --schriften <ordner> -o v.png
          python3 remote-step-installer/tools/einrichtung-vorschau.py --schriften <ordner> --ohne-netz

      KEINE ATTRAPPE: gezeichnet wird mit dem ECHTEN Code. Die `Schirm`-Klasse
      wird ohne ihren Konstruktor erzeugt (`__new__`) und bekommt statt des
      Framebuffers einen gewoehnlichen Puffer; alle Zeichenwege bleiben
      dieselben. Nur `zeigen()` entfaellt.
      Die PSF-Schriften fehlen am Entwicklungsrechner meist — von der Box
      holen:
          scp dietpi@<box>:/usr/share/consolefonts/Lat15-DejaVuBold30x16.psf.gz .
          scp dietpi@<box>:/usr/share/consolefonts/Lat15-TerminusBold24x12.psf.gz .

      ── WAS DIE TESTS WIRKLICH PRUEFEN ──────────────────────────────────
      `tests/einrichtung_schirm_test.py` (20 Pruefungen, KEIN Framebuffer):
      Adresswahl (Kabel vor Funk, echte Adresse vor 169.254er), Boxname mit
      allen Rueckfaellen, Maskottchen-Fund — und die Frage, an der so ein
      Schirm wirklich scheitert: IST DER CODE GROSS GENUG? Die verfuegbare
      Hoehe ist ausgerechnet (320 Punkte), daraus folgt Skala 8, und `zbarimg`
      liest den Code in genau dieser Groesse zurueck. Zusaetzlich liest
      zbarimg den FERTIG GEZEICHNETEN Schirm aus der Vorschau — damit ist der
      Weg vom Text bis zum Bild Ende zu Ende belegt.

  - kind: architektur
    id: mediathek-aufraeumen
    title: "Verschwundene Playlisten aufraeumen: was angeboten werden darf — und was nie"
    tags: ['mediathek', 'spotify', 'verwaltung', 'loeschen', 'sicherung', 2026]
    source: "gebaut und lokal gegen ein laufendes Backend gemessen (2026-08-02)"
    body: |
      Fortsetzung von mupi-spotify-drei-ursachen (Ursache 3) und
      verfuegbar.ts. Die Box KENNZEICHNETE tote Eintraege bereits; jetzt kann
      die Verwaltung sie auch entfernen. Der Unterschied ist der ganze
      Aufwand:

          Ausgrauen nimmt der naechste Pruflauf zurueck.
          Entfernen nimmt niemand zurueck.

      Deshalb hat das Entfernen eine ZWEITE, strengere Huerde als das
      Kennzeichnen — sie liegt rein und getestet in
      `src/backend-api/src/aufraeumen.ts` (17 Pruefungen in
      `aufraeumen.spec.ts`, 6 weitere ueber die Schnittstelle in
      `aufraeumen.integration.spec.ts`).

      ── DIE DREI BEDINGUNGEN VON laufBrauchbar() ────────────────────────
      Ein ganzer Pruflauf wird VERWORFEN (kein Fortschreiben, kein Zaehler),
      wenn eine davon nicht gilt:

        1. MINDESTENS EIN 'traegt'. Ohne ein einziges Lebenszeichen ist nicht
           belegt, dass Netz und Token in diesem Lauf funktioniert haben. Ein
           Konto ohne Rechte antwortet auf ALLES mit 404 — ohne diese
           Bedingung boete die Box die ganze Mediathek zum Loeschen an.
        2. MINDESTENS DIE HAELFTE eindeutig. Bei 429 (Drosselung) faellt der
           Rest eines Laufes reihenweise auf `unklar`; die wenigen Antworten
           davor stimmen, ergeben aber kein Bild.
        3. WENIGER ALS DIE HAELFTE der eindeutigen Antworten 'geloescht'. Eine
           Mediathek, in der ueber Nacht jeder zweite Spotify-Eintrag
           verschwindet, gibt es nicht — die Ursache laege dann bei uns.

      GEMESSEN am 2026-08-02 an einem lokal gestarteten Backend OHNE
      Spotify-Zugang: alle Eintraege `unklar`, Lauf verworfen, Protokollzeile
      „Lauf verworfen — Kein einziger Eintrag hat geantwortet". Genau der
      Netzausfall-Fall, und er bietet nichts an.

      ── UND DANN NOCH ZEIT: vorschlaegeBilden() ─────────────────────────
      Angeboten wird ein Eintrag erst nach DREI brauchbaren Laeufen UND
      mindestens 24 Stunden seit der ersten Meldung. Beides ist noetig: ein
      reiner Laufzaehler waere voll, wenn jemand dreimal auf „jetzt pruefen"
      drueckt — und gerade der Fall, gegen den die Frist steht (eine auf
      privat gestellte oder regional gesperrte Playlist antwortet mit
      Spotifys EIGENEM 404), waere damit nicht abgefangen. Bei dem
      12-Stunden-Takt der Box heisst das: frueherstens nach 24 h, in der Regel
      nach 36 h. Ein einziges 'traegt' setzt Zaehler UND Zeitpunkt zurueck.

      ── DIE FALLE, DIE ALLES ANDERE WERTLOS MACHT: SCHLUESSEL != KENNUNG ─
          medienSchluessel (medien.ts)   nimmt  id  VOR  playlistid
          kennungVon (verfuegbar.ts)     nimmt  playlistid  VOR  id

      Ein Eintrag mit BEIDEN Feldern wird also unter der einen Zeichenkette
      geprueft und muesste unter der anderen entfernt werden. Baut die
      Verwaltung den Schluessel aus der gemeldeten Kennung, faellt der
      falsche Eintrag oder gar keiner — und die Meldung sagt trotzdem
      „entfernt". Deshalb liefert der Server BEIDE Felder je Vorschlag, und
      die Verwaltung baut nie selbst einen daraus.
      GEMESSEN mit `tools/verfuegbarkeit-abgleich.py` an einem Eintrag mit
      `playlistid` und `id`: Schluessel `spotify:al-auch`, Kennung `pl-tot`.
      Der aus der Kennung gebaute Schluessel wird vom Server abgewiesen
      (`{"error":"nichtVorgeschlagen"}`).

      ── ZWEITER SICHERHEITSGURT: DER SERVER PRUEFT NACH ─────────────────
      `POST /api/medien/aufraeumen` nimmt nur Schluessel an, die
      `vorschlaegeBilden` in DIESEM Moment auch anbietet. Nicht aus
      Misstrauen gegen die Verwaltung, sondern damit kein zweiter, leiserer
      Weg zum Sammelloeschen entsteht, an dem die ganze Absicherung
      vorbeigeht.

      ── SICHERUNGEN: OHNE RUECKWEG WIRD NICHT ENTFERNT ──────────────────
      Zwei Stufen, beide neu:
        * `medienAendern` legt bei JEDER Aenderung `data.json.bak` an — das
          fordert mupi-konfiguration-schreiben seit jeher fuer die
          Konfiguration, `data.json` hatte es bis heute nicht.
        * Vor dem Sammelentfernen zusaetzlich eine benannte, datierte Kopie
          `config/data-vor-aufraeumen-<ISO>.json`. Die letzten drei bleiben.
          Schlaegt das Anlegen fehl, wird NICHTS entfernt (500).
      DER RUECKWEG GEHT AUCH OHNE VERWALTUNG:
          cp config/data-vor-aufraeumen-2026-08-02T13-46-05.json config/data.json
      Ueber die Verwaltung: Knopf „rückgängig" bzw.
      `POST /api/medien/aufraeumen/zurueck` mit dem Dateinamen. Der Name wird
      streng am Muster geprueft, nicht nur auf „..".

      ── WEITERE PUNKTE, DIE SONST STILL DANEBENLIEGEN ───────────────────
      * ALLES IN EINEM SCHREIBVORGANG unter EINER Sperre. N einzelne DELETE
        haetten bei einem Abbruch einen halb aufgeraeumten Zustand
        hinterlassen, zu dem keine Sicherung passt.
      * DOPPELTE SCHLUESSEL werden gar nicht erst angeboten: `findeIndex`
        gibt bei zwei gleichen -1 zurueck, es faellt nichts. Die Zeile traegt
        stattdessen „doppelter Schlüssel — von Hand entfernen".
      * DER STAND LIEGT AUF DER PLATTE (`config/verfuegbarkeit.json`, gebuendelt
        geschrieben wie gespielt.json). Ohne das faenge jeder Backend-Neustart
        bei null an und die Drei-Laeufe-Schwelle waere bei 12-Stunden-Takt nie
        erreichbar — die Karte bliebe fuer immer leer.
      * 150 ms PAUSE je Anfrage plus eine Laufsperre (409 „laeuft"), damit ein
        mehrfach gedrueckter Knopf die Box nicht in die Drosselung faehrt.
      * SHOWS UND HOERBUECHER fielen bis heute still durch: `kennungVon`
        kannte `showid`/`audiobookid` nicht. Dabei heisst der Pruefpfad je Art
        anders — `episodes` bzw. `chapters` statt `tracks`. Mit `tracks`
        antwortet eine LEBENDE Show mit 404 in Spotifys eigener Fehlerform
        und waere faelschlich „geloescht".
      * „BEI SPOTIFY" MUSS IN DER UEBERSCHRIFT STEHEN. Jellyfin und lokale
        Eintraege werden gar nicht geprueft; eine Karte „nicht mehr
        vorhanden" taeuschte Vollstaendigkeit vor. Die Karte verschwindet auch
        im Leerfall nicht — sonst weiss niemand, dass geprueft wird.
      * BEIDE OBERFLAECHEN KENNZEICHNEN JETZT. Die klassische tat es schon
        (.sw-tot); die neue bekommt `fehlt: true` je Werk aus `/api/werke` —
        der Server rechnet Kennung auf Schluessel um, damit die Oberflaeche
        keine zweite Liste und keine Kennungslogik fuehrt.

      ── WERKZEUG ───────────────────────────────────────────────────────
          tools/verfuegbarkeit-abgleich.py [box]        # nur lesend
      Stellt je Eintrag Schluessel, Kennung und Urteil gegenueber, meldet
      SCHLUESSEL!=KENNUNG und doppelte Schluessel, und zaehlt, wie viele
      Eintraege gar nicht pruefbar sind. Rueckgabe 1 bei Auffaelligkeiten.

  - kind: falle
    id: vorschau-laedt-nichts-im-unsichtbaren-reiter
    title: "Die Vorschau bleibt bei „Medien werden geladen …“ — der Reiter ist nur nicht sichtbar"
    tags: ['vorschau', 'neue-oberflaeche', 'werkzeuge', 'fehlersuche', 2026]
    source: "am 2026-08-02 eine halbe Stunde gekostet"
    body: |
      SYMPTOM: `tools/neu-vorschau.mjs` laeuft, `/api/werke` liefert im curl
      sauber 12 Werke — die Seite zeigt trotzdem endlos „Medien werden
      geladen …". Kein Fehler in der Konsole, keine abgewiesene Zusage, und
      im Netzwerkmitschnitt endet ALLES nach `/api/config`.

      URSACHE: `NewDesign/app.js` haengt jeden wiederkehrenden Abruf an
      `sichtbarerTakt()`, und der startet nur bei
      `document.visibilityState === 'visible'`. Wird die Seite in einem
      Browserreiter geladen, der im Hintergrund liegt (oder in einem
      ferngesteuerten Browser, dessen Fenster gar nicht angezeigt wird), holt
      sie NICHTS. Das ist Absicht und richtig — auf der Box soll ein
      abgeschalteter Schirm keine Abrufe erzeugen —, sieht aber exakt aus wie
      eine kaputte Oberflaeche.

      WORAN MAN ES ERKENNT, bevor man sucht: `/api/config` wird abgerufen
      (das laeuft einmalig beim Aufbau), aber `/api/werke`, `/api/darstellung`
      und `/player/state` nicht. Genau diese Luecke ist die Signatur.

      WARUM ES SO TEUER IST: Der Abruf steckt in `werkeHolen()`, und dessen
      `catch` meldet nur, „solange noch nichts dasteht". Es gibt also weder
      eine Fehlermeldung noch ein Ereignis `error` noch eine unbehandelte
      Zusage. Man sucht einen Absturz, den es nicht gibt — und verdaechtigt
      der Reihe nach die eigene letzte Aenderung.

      ABHILFE beim Messen aus einem ferngesteuerten Browser: Sichtbarkeit
      vortaeuschen, dann das Ereignis nachreichen —

          Object.defineProperty(document, 'visibilityState',
            { configurable: true, get: () => 'visible' })
          Object.defineProperty(document, 'hidden',
            { configurable: true, get: () => false })
          document.dispatchEvent(new Event('visibilitychange'))

      Danach baut sich die Seite in weniger als einer Sekunde auf.

      NEBENBEI ZWEIMAL WIEDER HINEINGETRETEN: `pkill -f` mit einem Muster,
      das in der EIGENEN Befehlszeile steht, beendet die eigene Shell
      (Rueckgabe 144, keine Ausgabe) — siehe
      pkill-f-toetet-den-eigenen-befehl. Auch die Klammerform
      `neu-[v]orschau` hilft nicht, wenn spaeter in derselben Zeile
      `node tools/neu-vorschau.mjs` steht. Was hilft: das Muster aus Stuecken
      zusammensetzen, `pkill -f "neu""-vors""chau"` — dann steht die gesuchte
      Zeichenkette nirgends am Stueck.
  - kind: falle
    id: medienschluessel-kollision-gleiche-playlistid
    title: "Zwei Eintraege, ein Schluessel: dieselbe playlistid in zwei Kategorien"
    sev: medium
    source: "data.json der Box, gemessen 2026-08-02 mit tools/quellen-ueberschneidung.mjs"
    match: "kann nicht loeschen|kann nicht aendern|falsche Kachel|doppelter Schluessel|findeIndex"
    cause: "`medienSchluessel()` nimmt die Kennung des Dienstes, aber NICHT `category` - zwei Aufnahmen derselben Playlist unter music und audiobook ergeben denselben Schluessel"
    fix: "den doppelten Eintrag in der Verwaltung aufloesen; grundsaetzlich waere `category` im Schluessel noetig, das erzwingt aber eine Wanderung von gespielt.json, resume.json und den Favoriten"
    tags: [medien, schluessel, verwaltung, falle, 2026]
    body: |
      AUF DER BOX GEMESSEN (2026-08-02), im Betrieb, nicht konstruiert:

          spotify:2QqQXuDKNR8HK1cFxf0NhW   Zeile  9   category=music
          spotify:2QqQXuDKNR8HK1cFxf0NhW   Zeile 10   category=audiobook

      Beide tragen dieselbe `playlistid` UND dieselbe `spotify_url`, denselben
      Interpreten und denselben Titel. Sie unterscheiden sich NUR in
      `category`, und die geht in den Schluessel nicht ein.

      DREI FOLGEN, ALLE HEUTE WIRKSAM - und alle DREI verhalten sich
      VERSCHIEDEN. Das ist das eigentlich Teure daran:

        * `findeIndex()` (medien.ts) gibt bei zwei Treffern -1. Die Verwaltung
          kann KEINEN der beiden aendern oder loeschen - sie meldet "nicht
          gefunden", obwohl der Eintrag sichtbar dasteht.
        * `eintragZuSchluessel()` (server.ts) nimmt per `.find()` STILL den
          ersten. `/api/werke/<s>/inhalt` und `/api/bild/<s>` antworten also
          mit dem Musik-Eintrag, auch wenn die Hoerbuch-Kachel getippt wurde.
        * `werkAus()` liefert ZWEI Werke mit demselben `schluessel` - die neue
          Oberflaeche hat doppelte Schluessel im Raster.

      WARUM ES FUER DIE VERSCHMELZUNG WICHTIG IST: jeder Entwurf schluesselt
      auf `schluessel`. `verschmelzeWerke()` (verschmelzung.ts) weist deshalb
      JEDE Zuordnung ab, die einen mehrdeutigen Schluessel nennt - sonst
      verschmilzt sie ein geratenes Werk. Dieselbe Zurueckhaltung wie
      findeIndex, nur an einer dritten Stelle.

      NACHMESSEN:  npx tsx tools/quellen-ueberschneidung.mjs
      Es meldet mehrdeutige Schluessel samt Zeilennummer, Kategorie und den
      Kennungsfeldern, die der Eintrag traegt.

      NICHT NEBENBEI MITREPARIERT. Zwei Wege, beide mit Preis:
        a) `category` in den Schluessel aufnehmen - behebt es grundsaetzlich,
           entwertet aber ALLE gespeicherten Schluessel: gespielt.ts fuehrt
           `key` in derselben Form, dazu resume.json und die Favoriten. Ohne
           Wanderung verliert die Box ihren Verlauf und ihr Weiterhoeren.
        b) den doppelten Eintrag aufloesen (er ist vermutlich versehentlich
           zweimal aufgenommen) und `eintragZuSchluessel()` bei Mehrdeutigkeit
           `undefined` liefern lassen statt still den ersten - dann verhaelt es
           sich wie `findeIndex()`.
      Der Preis von b: heute oeffnet die Kachel wenigstens IRGENDETWAS,
      danach gar nichts mehr. Das ist ehrlicher, aber es ist eine Entscheidung
      und keine Aufraeumarbeit.

  - kind: falle
    id: normal-macht-aus-scharfem-s-eine-luecke
    title: "normal() zerlegt „gross\u201c und „gro\u00df\u201c in zwei verschiedene Woerter"
    sev: medium
    source: "medien.ts normal(), gemessen 2026-08-02"
    match: "findet dasselbe Album nicht|gruppiereTreffer trifft nicht|Regal doppelt|Umlaut|scharfes s"
    cause: "`normalize('NFKD')` zerlegt Umlaute (ue -> u), aber NICHT das scharfe s - danach faellt es als Nicht-Buchstabe der Regel `[^a-z0-9]+ -> Leerzeichen` zum Opfer"
    fix: "vor dem Normalisieren ss ersetzen - ABER: das aendert die Ersatzschluessel `<dienst>:t:<interpret>|<titel>` und braucht deshalb eine Wanderung"
    tags: [medien, suche, verschmelzen, deutsch, falle, 2026]
    body: |
      GEMESSEN, alles mit `normal()` aus medien.ts:

          "Die Zukunft wird gross"  ->  "die zukunft wird gross"
          "Die Zukunft wird groß"   ->  "die zukunft wird gro"      (!)
          "Straße"                  ->  "stra e"                    (!)
          "Müller"                  ->  "muller"                    (richtig)
          "Für Elise"               ->  "fur elise"                 (richtig)

      Die Umlaute stimmen - NFKD zerlegt sie in Buchstabe + Zeichen, und das
      Zeichen wird abgeraeumt. Das scharfe s hat KEINE solche Zerlegung; es
      bleibt als einzelnes Zeichen stehen und wird dann durch ein LEERZEICHEN
      ersetzt. Aus einem Wort werden zwei.

      WEN ES TRIFFT:
        * `gruppiereTreffer()` - Stufe 1 der Quellenverschmelzung vergleicht
          normalisierte Titel. Schreibt ein Dienst "gross" und der andere
          "groß", trifft sie nie. Ausgerechnet das Messbeispiel des
          Wissenspakets [quellen-verschmelzen] ist so ein Titel.
        * `interpretSchluessel` (werke.ts) - zwei Schreibweisen desselben
          Interpreten ergeben ZWEI Regale.
        * `medienSchluessel()` im Ersatzfall `<dienst>:t:<interpret>|<titel>` -
          betrifft alle LOKALEN Alben, die keine Kennung haben.

      DESHALB NICHT EINFACH ZU BEHEBEN: der letzte Punkt macht `normal()` zu
      einem Teil der IDENTITAET. Ein `replace(/ß/g, 'ss')` davor waere eine
      Zeile - aber danach heissen lokale Alben anders, und Verlauf
      (gespielt.json), Weiterhoeren (resume.json) und Favoriten zeigen ins
      Leere. Es braucht dieselbe Wanderung wie
      [medienschluessel-kollision-gleiche-playlistid] Weg a.

      ZWISCHENLOESUNG, wenn es akut wird: die Ersetzung NUR im Vergleichspfad
      (gruppiereTreffer, Suche), nicht in `medienSchluessel`. Zwei Regeln, aber
      nur eine davon ist Identitaet - und das ist ohnehin schon so.

  - kind: architektur
    id: verschmelzung-aufloesung-stufe1
    title: "Verschmelzung Schritt 1: die Aufloesung (welche Quelle spielt) liegt rein und getestet"
    tags: [verschmelzen, quellen, backend, werke, 2026]
    body: |
      GEBAUT 2026-08-02. Erster Schritt des Plans [quellen-verschmelzen];
      der ABGLEICH ist ausdruecklich NICHT dabei.

      == DIE MESSUNG, DIE DIE REIHENFOLGE UMGEDREHT HAT ==
      `npx tsx tools/quellen-ueberschneidung.mjs` am 2026-08-02:
        22 Eintraege - 21 spotify, 1 jellyfin, 0 lokal
        Ueberschneidung zwischen Diensten: NULL
      Damit ist BACKLOG E9/V1 beantwortet. Es gibt derzeit KEIN Werk, das zu
      verschmelzen waere - jeder Abgleich waere gegen einen leeren Fall gebaut
      worden, und man haette ein Treffverhalten geglaubt, das nie eines hatte.
      Gebaut wurde deshalb die REGEL, die eine gegebene Zuordnung ausnutzt:
      sie ist ohne Box und ohne Netz vollstaendig pruefbar.
      (Der Erkunder derselben Sitzung zaehlte 12 Eintraege, gemessen wurden
      22. Der Katalog waechst zwischendurch - deshalb ein Werkzeug und keine
      Erinnerung.)

      == DIE TEILE ==
        src/backend-api/src/verschmelzung.ts       reine Regel
        src/backend-api/src/verschmelzung.spec.ts  30 Faelle
        src/backend-api/src/werke.ts               `werkeAus` reicht durch
        tools/quellen-ueberschneidung.mjs          die Messung, wiederholbar

      == ERKENNUNG UND AUFLOESUNG SIND GETRENNT ==
      `verschmelzung.ts` erfindet KEINE Zuordnung, es verbraucht eine. Die
      Erkennung hat mit `gruppiereTreffer()` (medien.ts) schon eine getestete
      Antwort, ab Stufe 2 kaeme der Fingerabdruck dazu. Beides in einer Datei
      hiesse, die teure und fehleranfaellige Erkennung mit der billigen,
      immer gleichen Aufloesung zu vermengen - und man koennte die Aufloesung
      nicht mehr pruefen, ohne vorher etwas zu erkennen.

      == ZWEI DINGE, DIE GETRENNT BLEIBEN (die wichtigste Entscheidung) ==
        `schluessel` ist IDENTITAET - daran haengen gespielt.json,
            resume.json, Favoriten und /api/bild. Er kommt vom FUEHRENDEN Werk
            der Zuordnung und aendert sich NICHT, wenn eine Quelle dazukommt.
            Sonst verloere die Box ihren Verlauf genau in dem Moment, in dem
            jemand ein Album zusaetzlich lokal ablegt.
        `quellen` ist BEVORZUGUNG - die Reihenfolge sagt, WOMIT gespielt wird,
            und darf sich jederzeit aendern, ohne dass etwas verlorengeht.
      Weil `verschmelzeWerke()` die Quellen sortiert ablegt, IST `quellen[0]`
      die bevorzugte Quelle - `quelleVon()` in NewDesign/app.js liest das schon
      heute so. Die Oberflaeche muss dafuer nichts dazulernen.

      == VIER FAELLE, IN DENEN NICHT VERSCHMOLZEN WIRD ==
        1. zwei Werke DESSELBEN Dienstes (Standard und Deluxe) - dieselbe
           Zurueckhaltung, die `gruppiereTreffer` schon uebt
        2. verschiedene `art` - der Abspielbefehl entsteht aus art UND dienst,
           eine Kachel hat aber nur EINE art
        3. ein MEHRDEUTIGER Schluessel, siehe
           [medienschluessel-kollision-gleiche-playlistid]
        4. ein Schluessel in zwei Zuordnungen - die erste gilt, die zweite
           faellt GANZ weg; Ketten (A=B, B=C) werden nicht aufgeloest
      Eine Zuordnung mit unbekannter `stufe` wird ABGEWIESEN statt auf "hand"
      gebogen: die Stufe ist das, woran der Mensch ablesen soll, wem er die
      Zusammenfassung verdankt (Wissenspaket Punkt 7, "verbunden per
      Fingerabdruck" statt eines Orakels).

      == „BEIM ANBIETER GELOESCHT" GILT JETZT FUERS WERK ==
      `fehlt` ueberlebt das Verschmelzen nur, wenn ALLE Quellen weg sind. Ein
      Album, das bei Spotify verschwunden ist, aber in Jellyfin liegt, ist
      nicht weg - die einzige Kachel auszugrauen, die noch spielt, waere die
      falsche Auskunft.

      == WAS BEWUSST FEHLT ==
        * REGEL 1 des Plans (eine Quelle, die das GANZE Album hat, schlaegt
          eine hoeher priorisierte mit nur Teilen) braucht Verfuegbarkeit JE
          TITEL (`titel[].da`). `Werk` fuehrt heute nur Quellen auf
          Werkebene. Die Regel gehoert in denselben Baustein - erst dann.
        * DER ANSCHLUSS: `/api/werke?verschmelzen=1` gibt es NICHT, die Box
          sieht unveraendert aus. Offen ist die Frage, ob das Zusammenfassen
          zuerst nur der VERWALTUNG dienen soll (so trennt es
          [medienverwaltung-listen] Punkt 2 ausdruecklich) oder gleich der Box.
        * DIE ABLAGE: `config/verschmelzung.json` gibt es noch nicht. Sie
          gehoert NICHT in data.json - was noch nicht spielen kann, gehoert
          nicht in die Bibliothek ([medienverwaltung-listen] Punkt 4).
          tools/quellen-ueberschneidung.mjs gibt seine Vorschlaege bereits in
          genau der Form aus, die dort hineingehoert.

      NACHTRAG 2026-08-03: alle drei Punkte unter "WAS BEWUSST FEHLT" sind
      erledigt - siehe [verschmelzung-eingeschaltet-stufe1]. Nur REGEL 1
      (ganzes Album schlaegt Teilbestand) steht weiter aus.

  - kind: architektur
    id: verschmelzung-eingeschaltet-stufe1
    title: "Verschmelzung Schritt 2: Erkennung, Ablage, Schalter - eingeschaltet und gemessen"
    tags: [verschmelzen, quellen, backend, verwaltung, darstellung, 2026]
    body: |
      GEBAUT 2026-08-03. Schritt 2 nach [verschmelzung-aufloesung-stufe1]:
      dort lag nur die AUFLOESUNG (welche Quelle spielt), sie war absichtlich
      ein Nichtstuer, weil die Ueberschneidung am 2026-08-02 NULL war.

      == WAS SICH GEAENDERT HAT: DIE ZAHL ==
      `npx tsx tools/quellen-ueberschneidung.mjs` am 2026-08-03 (nur lesend):
        26 Eintraege - 24 spotify, 2 jellyfin
        Ueberschneidung: 2   jellyfin+spotify "Kapelle Petra - HAMM"
                             jellyfin+spotify "Das Lumpenpack - Die Zukunft
                                               wird gross"
        mehrdeutig:      1   spotify:2QqQXuDKNR8HK1cFxf0NhW steht ZWEIMAL
      Der Nutzer hat zwischen dem 02. und dem 03. Jellyfin-Inhalte dazugelegt.
      Damit gab es zum ersten Mal etwas zu verschmelzen - und erst jetzt war
      der Abgleich gegen einen echten Fall baubar statt gegen einen leeren.

      == DIE MESSUNG NACHHER, mit demselben Code wie der Server ==
      `npx tsx tools/verschmelzung-probe.mjs` (NEU, nur lesend):
        Kacheln vorher 26  ->  nachher 24   (genau 2 weniger)
        Kapelle Petra - HAMM
          Quellen jellyfin + spotify, spielt ueber jellyfin (locker)
          Identitaet bleibt jellyfin:7d9a37e2732ca78bc68eacb3232960c0
        Das Lumpenpack - Die Zukunft wird gross
          Quellen jellyfin + spotify, spielt ueber jellyfin (locker)
          Identitaet bleibt spotify:0PyuOAMz1lv0z737JcQNOg
      BEACHTE DIE ZWEITE ZEILE: die Identitaet ist der SPOTIFY-Schluessel,
      gespielt wird ueber JELLYFIN. Genau das ist die Trennung, die
      verschmelzung.ts im Kopf verlangt - `schluessel` ist Identitaet (daran
      haengen gespielt.json, resume.json, Favoriten, /api/bild), `quellen[0]`
      ist Bevorzugung. Wer beides zusammenwirft, nimmt dem Kind seinen Verlauf.

      == DIE TEILE ==
        src/backend-api/src/abgleich.ts            die ERKENNUNG (rein)
        src/backend-api/src/abgleich.spec.ts       31 Faelle
        src/backend-api/src/verschmelzung.integration.spec.ts  13 Faelle
        src/backend-api/src/server.ts              Ablage + Schnittstelle
        src/frontend-admin/.../seiten/verschmelzung.ts  die Seite "Doppelte"
        tools/verschmelzung-probe.mjs              die Messung, wiederholbar

      == WER FUEHRT? DER ERSTE IN data.json ==
      Der fuehrende Schluessel ueberlebt, die anderen gehen darin auf. Gewaehlt
      wird der ERSTE Treffer der Katalogreihenfolge - die Bibliothek waechst
      hinten (`[...liste, e]`), der erste ist also der aeltere Eintrag und
      damit der, an dem am ehesten schon ein Verlauf haengt. Das ist die
      Wahl, die am wenigsten wegnimmt, und sie ist ohnehin umkehrbar.

      == AUF KNOPFDRUCK, NICHT VON SELBST - und warum ==
      Stufe 1 vergleicht NAMEN. Deluxe, Remaster und Live tragen dieselben.
      Der Schutz des Wissenspakets ist die TITELANZAHL, und die kennt ein
      `Werk` heute nicht (sie kostet je Eintrag einen Abruf beim Dienst).
      `zuordnungenVorschlagen` nimmt sie deshalb als OPTION entgegen
      (`titelAnzahl`); wer sie hat, bekommt den Deluxe-Schutz sofort, ab
      Stufe 2 fuellt der Fingerabdruck sie. Solange sie fehlt, ist jede
      Zusammenlegung eine Behauptung - und etwas an der Mediathek eines Kindes
      zu aendern, ohne dass jemand gefragt hat, ist die falsche Vorgabe.

      == ZWEI EBENEN ZUM ABSCHALTEN, beide gebaut ==
        GANZ AUS  Darstellungsfeld `verschmelzen` ("Doppelte zusammenfassen",
                  Vorgabe AUS). Reise durch ALLE Stellen, wie es
                  [darstellung-feld-faellt-still-heraus] verlangt:
                  darstellung.service.ts (Schnittstelle + STANDARD +
                  pruefen()), darstellung.service.spec.ts, frontend-admin
                  seiten/darstellung.ts (Schnittstelle + STANDARD + Knopf),
                  NewDesign/app.js (`zustand.verschmelzen`, Abfrageteil).
                  AUS heisst: /api/werke antwortet Byte fuer Byte wie vorher.
        EINZELN   "trennen" auf der Seite "Doppelte". Das geloeste Paar landet
                  in `getrennt` und ueberlebt jeden weiteren Abgleich - ohne
                  das waere der Knopf eine Beruhigung bis zum naechsten Druck
                  auf "abgleichen". "wieder zulassen" nimmt es zurueck.

      == DIE ABLAGE: config/verschmelzung.json ==
        { "zuordnungen": [{schluessel, auch[], stufe}], "getrennt": [ [a,b] ] }
      NEBEN data.json und nicht darin. Drei Gruende: (1) Umkehrbarkeit - die
      Bibliothek wird nie angefasst, "aus" heisst wirklich "wie vorher", mit
      beiden Verlaeufen; (2) von Hand korrigierbar, wie es Stufe "hand"
      verlangt - zwei Zeilen JSON kann man oeffnen, 26 Eintraege mit
      eingebetteten Covern nicht; (3) es braucht die dataLock-Sperre nicht und
      nimmt sie niemandem weg.
      `ablageAus()` wirft einzelne kaputte Zeilen WEG statt an ihnen zu
      scheitern - die Datei soll von Hand aenderbar sein, also wird sie
      irgendwann von Hand krumm sein.

      == ABGEGLICHEN WIRD AUF data.json, NICHT auf active_data.json ==
      active_data.json ist im ONLINE-Betrieb ein Symlink auf data.json
      (check_network.sh legt ihn an), OFFLINE zeigt sie auf offline_data.json
      und ist eine kuerzere Liste. Fuende der Abgleich seine Paare dort,
      verschwaenden die Zuordnungen genau dann, wenn das Netz weg ist - also
      im Moment ihres eigentlichen Zwecks (lokal statt Spotify spielen).
      Umgekehrt ist eine Zuordnung mit einem gerade fehlenden Schluessel
      harmlos: `verschmelzeWerke` uebergeht ihn.

      == DIE REIHENFOLGE IM SERVER, die man einmal falsch herum baut ==
      In /api/werke wird ERST `fehlt` markiert, DANN verschmolzen - deshalb
      steht dort `verschmelzeWerke()` selbst und nicht
      `werkeAus({verschmelzen:true})`. Andersherum fiele die Regel "fehlt gilt
      fuers WERK, nicht fuer den Eintrag" still unter den Tisch: das Feld
      saesse ja an einer Kachel, und niemand saehe, dass ein Album ausgegraut
      ist, obwohl Jellyfin es noch spielt.

      == DER MEHRDEUTIGE SCHLUESSEL: gemeldet, nicht verschwiegen ==
      Siehe [medienschluessel-kollision-gleiche-playlistid]. `verschmelzeWerke`
      weist eine Zuordnung darauf ab - richtig, aber STILL. Deshalb meldet
      `zuordnungenVorschlagen` mehrdeutige Schluessel jetzt ausdruecklich, die
      Seite "Doppelte" zeigt sie GANZ OBEN, und
      `POST /api/medien/doppelte/entfernen` kann die ueberzaehlige Zeile
      entfernen - ueber die ZEILENNUMMER plus Schluessel, weil die Nummer
      allein wandert und der Schluessel allein mehrdeutig ist. Sicherung ueber
      denselben Weg wie das Aufraeumen, damit es denselben Rueckweg hat.
      NICHT zusammengefuehrt wird: die beiden unterscheiden sich in `category`
      (music/audiobook), und das ist eine Elternentscheidung darueber, in
      welchem Abschnitt der Startseite die Kachel steht.

      == WAS WEITER FEHLT ==
        * REGEL 1 (ganzes Album schlaegt Teilbestand) - braucht `titel[].da`.
        * Stufe 2/3 (Fingerabdruck, AcoustID). Der Andockpunkt steht:
          `AbgleichOptionen.titelAnzahl`.
        * Eine Zusammenlegung VON HAND anzulegen ("das ist dasselbe") gibt es
          noch nicht - nur das Trennen. Stufe "hand" wird bereits GEACHTET
          (`uebernehmen` laesst sie stehen), es fehlt bloss der Knopf.
        * Die klassische Angular-Oberflaeche verschmilzt nicht: sie liest
          nicht /api/werke. Das ist bewusst so und kein Versehen.

      NACHTRAG 2026-08-03: Das ABSPIELEN folgte der Bevorzugung noch NICHT -
      siehe [verschmelzung-abspielen-stufe3]. Dort ist es gemessen und behoben.

  - kind: falle
    id: verschmelzung-abspielen-stufe3
    title: "Verschmelzung Schritt 3: die verschmolzene Kachel war TOT - Identitaet fuehrte, Bevorzugung spielte"
    sev: high
    match: "Dieses Album hat keine abspielbaren Titel|Das kann die Box hier noch nicht abspielen|verschmelzen.*inhalt"
    cause: "`/api/werke/<schluessel>/inhalt` loeste den Schluessel auf und bekam den FUEHRENDEN Eintrag - der ist nach dem Verschmelzen nicht der bevorzugte"
    fix: "`?verschmelzen=1` an /inhalt und /api/weiterhoeren; `eintragZurBevorzugtenQuelle` im Server, `identitaetsKarte` und `fortsetzenMit` als reine Regeln"
    tags: [verschmelzen, quellen, player, weiterhoeren, einheiten, falle, 2026]
    related: [kachel-sieht-richtig-aus-und-spielt-nicht]
    body: |
      'Das kann die Box hier noch nicht abspielen' STEHT IN ZWEI EINTRAEGEN.
      Der andere ist [kachel-sieht-richtig-aus-und-spielt-nicht] — dort faellt
      ein DIENST in den `default: return null` von `abspielBefehl()`. Hier ist
      der Dienst bekannt und die KACHEL verschmolzen. Die Frage, die trennt:
      tritt es bei JEDER Kachel dieses Dienstes auf (dann der andere Eintrag)
      oder nur bei verschmolzenen (dann dieser)? Eine unverschmolzene Kachel
      desselben Dienstes antippen entscheidet es in einem Griff.

      GEMESSEN AM 2026-08-03 mit tools/verschmelzung-befehle.mjs (neu): ein
      eigener Browser gegen die Vorschau, `window.fetch` ueberschrieben, der
      WIRKLICH abgeschickte Befehl mitgelesen. Dasselbe Vorgehen wie
      tools/interpretseite-befehle.mjs - und derselbe Grund: der Befehl steht
      in keiner Meldung und in keinem Protokoll, nur auf der Leitung.

      == DER BEFUND, VOR DER REPARATUR ==
        Die drei ???  Quellen jellyfin + spotify, bevorzugt jellyfin
          Tipp auf die Kachel  -> (kein Befehl)
                                  „Dieses Album hat keine abspielbaren Titel."
          Weiterhoeren         -> (kein Befehl)
                                  „Das kann die Box hier noch nicht abspielen."
      Die verschmolzene Kachel war also TOT, obwohl BEIDE Quellen spielbereit
      waren. Zwei Kacheln haetten gespielt, eine spielte nicht.

      == WARUM, UND ES SIND DREI VERSCHIEDENE GRUENDE ==

      1. `/inhalt` FOLGTE DEM SCHLUESSEL, NICHT DER BEVORZUGUNG.
         `schluessel` ist Identitaet und kommt vom FUEHRENDEN Eintrag; bei
         „Das Lumpenpack" ist das der von SPOTIFY (er steht im Katalog vorn),
         gespielt wird aber ueber JELLYFIN. `eintragZuSchluessel()` lieferte
         also den Spotify-Eintrag, `titelEinesWerks` Spotify-Titel - und die
         tragen kein `befehl`-Feld. `albumSpielen()` filtert genau darauf
         (`titel.filter(t => t.befehl)`), behielt nichts und entschuldigte
         sich beim Kind.
         BEHOBEN: `/api/werke/<s>/inhalt?verschmelzen=1` loest ueber
         `eintragZurBevorzugtenQuelle()` auf - quellen[0], und wenn noetig
         ueber `auchSchluessel` zum Eintrag des anderen Dienstes.

      2. WEITERHOEREN KANNTE DEN JELLYFIN-WEG NICHT.
         `abspielBefehl` gibt fuer ein Jellyfin-ALBUM `null` - das heisst dort
         „nicht mit EINEM Befehl", nicht „geht nicht". `spielen()` faengt das
         ab und nimmt `albumSpielen()`; `weiterSpielen()` tat es NICHT und
         meldete „kann die Box noch nicht".
         WARUM ES NIE AUFFIEL: `istWeiterhoerbar()` laesst Jellyfin gar nicht
         in die Reihe. Eine gemerkte Stelle zu einem Jellyfin-Album gibt es
         nicht - wohl aber eine ueber SPOTIFY gemerkte zu einem Album, das
         nach dem Verschmelzen ueber Jellyfin spielt. Erst die Verschmelzung
         fuehrt beides zusammen.

      3. DER GESCHLUCKTE SCHLUESSEL VERLOR SEINE KACHEL.
         `/api/weiterhoeren` bildet den resume-Schluessel auf
         `medienSchluessel()` des ROHEINTRAGS ab. Ist dieser Eintrag
         geschluckt worden, zeigt die Zeile auf ein Werk, das `/api/werke`
         nicht mehr kennt: Die Kachel steht da, der Tipp sagt „Das gibt es
         nicht mehr in der Bibliothek". Auf der Box trifft das „Kapelle Petra"
         - dort fuehrt Jellyfin, gemerkt wurde ueber Spotify.
         BEHOBEN: `identitaetsKarte()` (verschmelzung.ts) fuehrt geschluckt ->
         fuehrend. NUR in diese Richtung; umgekehrt gaebe es keine eindeutige
         Antwort. Die Stelle SELBST wird nicht umgeschrieben, nur
         nachgeschlagen - so heilt ein „Schalter aus" alles auf einen Schlag.

      == DIE EINHEITENFALLE, ZUM DRITTEN MAL IN DIESEM PROJEKT ==
      Spotify merkt sich MILLISEKUNDEN, mpv will PROZENT
      ([resume-lokal-ist-prozent-nicht-sekunden]). Nach dem Verschmelzen merkt
      SPOTIFY und spielt JELLYFIN, also mpv. Ein durchgereichtes
      `seekpos:92500` ist fuer mpv „ganz ans Ende" - das Kind bekaeme den
      naechsten Titel statt seiner Stelle.

      `fortsetzenMit()` (weiterhoeren.ts, rein und geprueft) rechnet um, und
      zwar nur in EINE Richtung:
        Spotify -> mpv   ueber `anteil` = ms / Titeldauer. BEIDE Zahlen stehen
                         in resume.json (`resumespotifyprogress_ms`,
                         `resumespotifyduration_ms`) - das ist gerechnet, nicht
                         geschaetzt. 92,5 s von 240 s sind 38,5 %.
        mpv -> Spotify   NICHT. Bei mpv steht nur der Prozentwert da, die
                         Laenge des Titels nirgends. Millisekunden daraus zu
                         bauen hiesse, eine Dauer zu erfinden. Dann lieber
                         `null`: der Titel faengt von vorn an.
      DIE TITELNUMMER WANDERT UNVERAENDERT MIT. Sie zaehlt in beiden Welten
      dasselbe, und es ist dasselbe Album - sonst waere es nicht verschmolzen.
      Der Server schickt ausserdem `quelle` mit, damit die Oberflaeche nicht
      ein zweites Mal raten muss, welche Maschine gleich laeuft.

      == DAS AUSWEICHEN: GEBAUT, ABER NUR DER START ==
      Der Benutzer wollte „wir koennen ja mit spotify spielen aber falls dann
      verfuegbar auf local fertig machen". Davon ist die ERSTE Haelfte gebaut:
      `spielen()` geht die Quellen der Reihe nach durch; kann die bevorzugte
      nicht (Jellyfin aus, 502 auf `/inhalt`), startet die naechste. Gemessen
      mit `/vorschau/inhalt-kaputt`: die Kachel schickt dann
      `spotify/now/spotify:album:<id>:0:0` statt zu schweigen.
      Dafuer nimmt `/inhalt` zusaetzlich `quelle=<dienst>` entgegen - und
      antwortet mit 404, wenn das Werk diesen Dienst gar nicht fuehrt. Ein
      stiller Rueckfall auf die fuehrende Quelle quittierte ein Ausweichen,
      das nie stattgefunden hat.
      KINDERZEIT IST KEIN AUSFALL: ein 403 beendet den Versuch endgueltig,
      sonst waere das Ausweichen ein Weg, die Regel zu umgehen, bis irgendein
      Dienst doch spielt.
      NUR DER LETZTE VERSUCH SPRICHT. Ein „Der Jellyfin-Server antwortet
      nicht", dem eine Sekunde spaeter Musik folgt, ist keine Auskunft,
      sondern ein Schreck.

      DIE ZWEITE HAELFTE - MITTEN in der Wiedergabe wechseln - ist AUSDRUECKLICH
      NICHT gebaut, und das ist eine Entscheidung, keine Luecke: Sie braucht
      die Verfuegbarkeit JE TITEL (`titel[].da`, REGEL 1 aus
      [quellen-verschmelzen]), die ein `Werk` heute nicht kennt, und jeder
      Quellenwechsel kostet 1-2 s Stille ([dienste-umschalten]). Bei JEDEM
      Titel waere das ein Stottern statt eines Vorteils.

      == WAS SCHON GING UND NICHT ANGEFASST WURDE ==
      DIE KACHEL SELBST. `diensteVon()` liest `quellen[]` als Liste,
      `markenBauen()` baut je Quelle eine Plakette, und der Vorlesetext nennt
      beide Dienste - das war seit [dienst-zeichen-auf-den-kacheln] richtig
      gebaut und musste nur gemessen werden: zwei Plaketten, 61x29 px im
      161 px breiten Cover, „Die drei ??? von Europa, Jellyfin und Spotify".
      Wer hier etwas Neues gebaut haette, haette eine zweite Wahrheit erzeugt.

      == DIE ATTRAPPE MUSSTE ES ERST KOENNEN ==
      tools/neu-vorschau.mjs kannte gar keinen verschmolzenen Fall - kein
      Werk mit zwei Quellen, und `/api/darstellung` lieferte das Feld
      `verschmelzen` nicht einmal. Ohne beides fragt die Oberflaeche nie mit
      `verschmelzen=1`, und jede Messung haette das Nichts geprueft
      ([attrappe-luegt-durch-weglassen], fuenfte und sechste Sorte). Neu:
      `/vorschau/verschmolzen-an` legt zwei zweite Ausgaben in die Liste
      (12 -> 14 -> 12 Kacheln) und schaltet das Darstellungsfeld.
      DIE BEIDEN FAELLE SIND ABSICHTLICH VERSCHIEDEN HERUM: einmal fuehrt der
      Spotify-Eintrag und Jellyfin spielt (der teure Fall), einmal fuehrt
      Jellyfin und Jellyfin spielt (der bequeme). Wer nur den bequemen misst,
      bekommt ein „geht" geschenkt.

      == UND EINE LEHRE UEBER WERKZEUGE ==
      Das neue Werkzeug stellte die Vorschau auf `verschmolzen-an` und legte
      sie NICHT zurueck. Der naechste Schritt (interpretseite-befehle.mjs)
      meldete daraufhin „Die drei ??? schickt jellyfin statt spotify:album:" -
      richtig gemessen, gegen eine Lage, die niemand bestellt hatte. Ein
      Werkzeug, das eine gemeinsame Attrappe umstellt, MUSS sie zurueckstellen.

      == DIE TEILE ==
        src/backend-api/src/verschmelzung.ts       `identitaetsKarte`
        src/backend-api/src/weiterhoeren.ts        `fortsetzenMit`, `ueberMpv`
        src/backend-api/src/server.ts              /inhalt + /api/weiterhoeren
        src/backend-api/src/verschmelzung-abspielen.integration.spec.ts
        NewDesign/app.js  `ausQuelleSpielen`, `albumSpielen`, `weiterSpielen`
        tools/verschmelzung-befehle.mjs            die Messung, wiederholbar
      1027 Tests gruen (vorher 1006), tsc sauber.

  - kind: falle
    id: pair-code-ohne-bremse-im-lan
    title: "Sechs Ziffern und kein Deckel — was beim Weg ins LAN aufgefallen ist"
    sev: high
    match: "PAIR_CODE|/pair|PAIR_MAX_FEHLER"
    cause: "der Agent nahm beliebig viele Pair-Versuche an; im LAN sind 10^6 Moeglichkeiten in Minuten durch"
    fix: "harter Deckel (PAIR_MAX_FEHLER), konstante Vergleichszeit, Code auf den Schirm der Box"
    tags: ['sicherheit', 'agent', 'einrichtung', 'falle', 2026]
    source: "beim Bau des Einrichtungsassistenten gefunden (2026-08-02)"
    body: |
      GEFUNDEN, BEVOR ES WEHTAT. Der Agent (agent/agent.py) fuehrt beliebige
      Befehle als ROOT aus. Bisher war das vertretbar, weil er auf 127.0.0.1
      lauscht und man ihn durch einen SSH-Tunnel erreicht: wer dort schon ist,
      hat ohnehin gewonnen. Der Pair-Code hat sechs Ziffern, und `/pair` nahm
      BELIEBIG VIELE Versuche an.

      Der Einrichtungsassistent aendert die Lage grundlegend: damit ein Handy
      den Agenten erreicht, muss er ins LAN (--host 0.0.0.0). Dann ist der
      Code die einzige Tuer — und eine Million Versuche sind ueber ein Heimnetz
      in Minuten durchprobiert.

      ── WAS NICHT GENUEGT HAETTE ────────────────────────────────────────
      Eine wachsende Wartezeit allein. Der Agent bedient jede Anfrage in einem
      EIGENEN THREAD (ThreadingHTTPServer) — ein Angreifer raet parallel und
      wartet nur einmal. Die Wartezeit bremst das bequeme Durchprobieren und
      macht es im Protokoll sichtbar, mehr nicht.

      ── WAS WIRKLICH SCHUETZT ───────────────────────────────────────────
      1. HARTER DECKEL: nach PAIR_MAX_FEHLER (8) Fehlversuchen nimmt der Agent
         gar keinen Code mehr an — auch nicht den richtigen. Erst ein Neustart
         setzt zurueck, und der vergibt einen neuen Code.
         Der Deckel ist GLOBAL, nicht je Absender: wer im selben Netz steht,
         kann seine Adresse wechseln. Preis ist, dass jemand die Einrichtung
         lahmlegen kann; auf einer Kinderbox im Heimnetz ist das das kleinere
         Uebel als eine offene Wurzel.
      2. Ein geglueckter Versuch setzt den Zaehler zurueck — sonst summierten
         sich Vertipper ueber eine ganze Einrichtung und die Box spraeche
         irgendwann grundlos nicht mehr mit dem Handy.
      3. `secrets.compare_digest` statt `==`: sonst verraet die Antwortzeit,
         wie viele Ziffern schon stimmen.
      4. DER CODE STEHT AUF DEM SCHIRM DER BOX (einrichtung-schirm.py
         --paircode). Damit kommt zur Kenntnis die KOERPERLICHE ANWESENHEIT
         dazu: einrichten kann nur, wer die Box sehen kann. Das ist der
         eigentliche Schutz — nicht die Geheimhaltung der sechs Ziffern.

      ── GEPRUEFT ────────────────────────────────────────────────────────
      `tests/pairing_test.py` (10 Pruefungen, gegen einen ECHTEN Agenten).
      Der wichtigste Fall ist nicht "falscher Code wird abgewiesen", sondern:
      nach dem Deckel wird auch der RICHTIGE Code abgewiesen und es kommt kein
      Token. Sonst waere die Sperre nur Zierde.

  - kind: architektur
    id: einrichtungsassistent-benannte-aktionen
    title: "Der Assistent fuers Handy: benannte Aktionen statt /run"
    tags: ['einrichtung', 'installer', 'agent', 'sicherheit', 2026]
    source: "gebaut und im Browser durchgespielt (2026-08-02)"
    body: |
      DIE SEITE, DIE AUF DEM HANDY LAEUFT: `agent/einrichtung.html`, vom
      Agenten unter `/einrichtung` ausgeliefert. Vollstaendig in sich — kein
      Nachladen von irgendwoher, denn beim ersten Start hat die Box oft noch
      kein Internet und ein Stil von einem fremden Rechner waere eine weisse
      Seite. Der Agent setzt zusaetzlich eine Content-Security-Policy, die
      genau das erzwingt.
      Farben aus NewDesign/app.css, hell UND dunkel: der Benutzer sieht gleich
      danach dieselbe Box, eine zweite Farbwelt fuer zehn Minuten Einrichtung
      waere ein Bruch.

      ── DER KERN: `/einrichtung/aktion` STATT `/run` ────────────────────
      `/run` nimmt eine beliebige Befehlszeile und schickt sie durch eine
      Shell. Fuer den Controller ist das richtig — wer im SSH-Tunnel sitzt,
      hat die Box ohnehin. Der Assistent laeuft aber in einem BROWSER im WLAN.
      Bekaeme der `/run`, waere jedes Formularfeld eine Wurzelschale.

      Stattdessen nennt der Browser einen NAMEN aus `EINRICHT_AKTIONEN`. Was
      daraus wird, steht ausschliesslich dort. Drei Schranken, jede geprueft:
        1. Unbekannter Name -> 400, und es wird KEIN Lauf angelegt.
        2. Jeder Wert geht durch einen eigenen Pruefer (`_pruefe_geraet`
           akzeptiert nur, was in /sys/class/net wirklich existiert).
        3. Felder, die nicht in der Tabelle stehen, werden WEGGEWORFEN — sonst
           waere die Tabelle nur eine Empfehlung und `cmd` mitzuschicken
           brächte `/run` durch die Hintertuer zurueck.
      Dazu `start_run` mit LISTE statt Zeichenkette: `shell=False`. Ein
      Netzname `; rm -rf /` ist als Listenelement nur ein Wort.

      ── EventSource KANN KEINE KOPFZEILEN ──────────────────────────────
      Beim Durchspielen im Browser aufgelaufen: die Seite haengt sich mit
      `EventSource` an `/stream/<id>`, und EventSource setzt KEINE
      Authorization-Kopfzeile — das ist eine Festlegung des Browsers, kein
      Versaeumnis. Ohne Abhilfe gaebe es am Handy keine Live-Ausgabe, also
      genau das, wofuer der Assistent da ist.
      ABHILFE: `_authed_strom()` nimmt zusaetzlich `?token=` an — AUSSCHLIESSLICH
      fuer /stream/. Vertretbar, weil /stream/ nur die Ausgabe eines bereits
      gestarteten Laufs liefert (startet nichts, aendert nichts) und der Agent
      keine Anfragen protokolliert (`log_message` ist absichtlich leer).
      NICHT fuer /run, /put oder /einrichtung/aktion.
      ZWEITER STOLPERSTEIN derselben Sache: `do_GET` verglich den ROHEN Pfad,
      also lief `/stream/<id>?token=…` in den 404-Zweig. Jetzt wird der
      Abfrageteil vorher abgetrennt.

      ── IM BROWSER DURCHGESPIELT ───────────────────────────────────────
      Agent lokal gestartet, Seite auf 375x812 geoeffnet: falscher Code ->
      „Der Code stimmt nicht. Noch 6 Versuche.", richtiger Code -> Liste der
      Aktionen, Aktion angetippt -> Ausgabe lief ZEILENWEISE ein („lo",
      „wlan0"), danach „Fertig.".
      MERKE fuer den naechsten, der das nachstellt: ein Klick ueber die
      Browsersteuerung kommt nicht an, solange der Vorschaubereich nicht
      sichtbar ist (nichts wird gerendert). Der Handler laesst sich aber per
      `element.click()` aus dem Skriptweg ausloesen — sonst sucht man den
      Fehler in der Seite, wo keiner ist.

      ── AUSLIEFERUNG ───────────────────────────────────────────────────
      `install-agent.sh` kopiert `einrichtung.html` und das Maskottchen mit,
      WENN sie danebenliegen — optional, der Agent laeuft auch ohne und sagt
      dann unter /einrichtung, was fehlt. Das Maskottchen kommt aus
      `dateien/mixpi-hoert.png` und wird NICHT zusaetzlich im agent-Ordner
      gehalten: dasselbe Binaerabbild zweimal im Repo ist eine Fehlerquelle
      („welches ist das aktuelle?"), kein Vorteil.

  - kind: entscheidung
    id: mehrbenutzer-vorbereitung
    title: "Mehrbenutzer: es gibt ab jetzt einen Besitzer, und er heisst `gast`"
    tags: ['mehrbenutzer', 'profile', 'mixpi', 'kinderzeit', 'verlauf', 'entscheidung', 2026]
    source: "gebaut am 02.08.2026; Plan aus [[mixpi-die-geschichte]] und BACKLOG E18"
    body: |
      DIE FRAGE, DIE ZUERST ZU BEANTWORTEN WAR, steht in
      [[mixpi-die-geschichte]]: „Was passiert, wenn NIEMAND angemeldet ist?
      Eine Box, die erst nach einer Anmeldung Musik macht, ist morgens um
      sieben das falsche Geraet."

      DIE ANTWORT IST DER GANZE ENTWURF: es gibt keinen Zustand „niemand".
      Ohne Auswahl ist `gast` dran. Damit aendert sich am Verhalten NICHTS —
      die Box spielt wie bisher —, aber jede Ablage hat einen Namen, unter dem
      sie liegt. Der Anmeldebildschirm ist danach ein Umschalter, kein Umbau.

      Ein Profil ist eine FIGUR, kein Konto: Kennung + Anzeigename + Figur,
      kein Passwort, keine Tastatur. Ein Kind, das noch nicht liest, erkennt
      seinen Zugang am Wesen.

      ── WAS GETRENNT IST ────────────────────────────────────────────────
          gespielt.json              -> gespielt.<kennung>.json
          kinderzeit-verbrauch.json  -> kinderzeit-verbrauch.<kennung>.json
          kinderzeit.json            -> { standard, je: { <kennung>: Regeln } }
          mupibox_favorites_v1       -> mupibox_p_<kennung>_favorites_v1
          mupibox_neu_zuletzt_v1     -> mupibox_p_<kennung>_neu_zuletzt_v1

      ── WAS GEMEINSAM BLEIBT, und warum ─────────────────────────────────
      Die BIBLIOTHEK (data.json) — so entschieden in [[mixpi-die-geschichte]]
      und BACKLOG E18. `medien.ts` kennt nur `category`; ein Besitzfeld
      muessten auch die PHP-Verwaltung und die Verwaltungsoberflaeche lernen.

      HELL/DUNKEL — eine Eigenschaft des RAUMS, nicht des Kindes (abends ist
      es fuer alle dunkel). Dazu ein technischer Grund: NewDesign/index.html
      liest den Schluessel in einem Einzeiler VOR app.js, um das Aufblitzen zu
      vermeiden; dort ist weder eine Kennung bekannt noch ein Netzaufruf
      moeglich.

      DIE LAUTSTAERKEGRENZE — sie wird in `scripts/mupibox/mupi-lautstaerke.sh`
      `klemme()` durchgesetzt, dem einzigen Flaschenhals, an dem kein Aufrufer
      vorbeikommt. Das Skript kennt keinen Besitzer. Eine Grenze je Kind ginge
      NUR dort; wer sie zusagt, sagt mehr zu, als hier gebaut ist.

      `mupibox_neu_interpreten_v1` — ein Faktum ueber Spotify, kein Besitz. Es
      je Kind zu fuehren hiesse, denselben Abruf n-mal zu machen.

      ── DIE UEBERNAHME: UMBENENNEN, NICHT KOPIEREN ──────────────────────
      Auf der PLATTE wird umbenannt (`ablageUebernehmen`, server.ts). Zwei
      Wahrheiten waeren schlimmer als eine alte. Sie muss IDEMPOTENT sein —
      auf dieser Box liefen schon zwei Node-Prozesse gleichzeitig
      ([[mupi-two-node-processes]]): existiert das Ziel, wird NICHTS angefasst.

      IM BROWSER dagegen wird KOPIERT und der alte Schluessel bleibt stehen.
      Dort ist er die einzige Rueckfahrkarte; ausserdem tragen aeltere
      Favoriteneintraege laut [[oberflaeche-speicherschluessel]] keine
      id/playlistid und werden ueber die Medienliste aufgeloest.

      DIE UEBERNAHME GILT NUR FUER DEN GAST. Was vor den Profilen dalag,
      gehoerte allen, die an der Box standen. Es einem Kind zuzuschlagen, das
      gerade zufaellig gewaehlt ist, hiesse ihm etwas unterzuschieben.

      ── DER BESITZER KOMMT NICHT VOM CLIENT ─────────────────────────────
      POST /api/gespielt hat ZWEI Meldewege (`key` von der Angular-App,
      `schluessel` von der neuen Oberflaeche). Duerfte die Meldung den
      Besitzer mitbringen, meldete die eine Oberflaeche unter Gast und die
      andere unter dem Kind — derselbe Titel stuende zweimal im Verlauf.
      Genau diese Doppelung ist bei der SCHLUESSELFORM schon einmal passiert.
      Der Server liest `aktiv` selbst; ein Test prueft es ausdruecklich.

      ── DIE REGEL HEISST `standard`, NICHT „je Kind" ────────────────────
      Waere die Kinderzeit-Regel profilgebunden, muesste beim Umstellen
      geraten werden, wem die bisherige gehoert. Mit `standard` gehoert sie
      weiter der BOX, `je` sagt nur, wo jemand abweicht — und ein neues Kind
      ERBT die Hausregel, statt unbegrenzt zu starten.

      Die drei Eigenschaften der Kinderzeit sind unberuehrt: nur Startbefehle
      werden abgewiesen, der laufende Titel darf zu Ende gehen, kaputte
      Konfiguration sperrt NICHT aus. Ein unlesbares `profile.json` ergibt den
      Gast, nicht einen Stopp — dieselbe Haltung wie `regelnNormalisieren`.

      ── WO DER ANMELDEBILDSCHIRM SPAETER ANSETZT ────────────────────────
      Er braucht keine neue Technik, nur zwei vorhandene Dinge:
        * Backend: POST /api/profil/aktiv (steht, mit Test).
        * Oberflaeche: ein Tipp auf `#kopf-bild` (NewDesign/index.html) oeffnet
          ein SCHWEBENDES Fenster mit grossen Figurkacheln, gebaut wie das
          Favoritenfenster ([[schwebende-fenster-favoriten-resume]]:
          zentriertes Quadrat `min(100vh - 16px, 100vw - 32px)`, 24 px Radius).
          Ein Seitenwechsel waere falsch — man will kurz wechseln und dann da
          bleiben, wo man war. Beruehrziele mindestens 64 px.
        * KEIN Namensfeld, KEINE Tastatur, KEIN Passwort. Wer das ANLEGEN
          schuetzen will, schuetzt die VERWALTUNG mit der vorhandenen
          PIN-Sperre (mupibox.einstellungssperre) — nicht das Waehlen an der
          Box. Zu `mixpi-passwort.png` gehoert der Satz „Das duerfen nur
          Grosse."
        * Wer nichts waehlt, ist Gast. Musik geht sofort.

      ── WAS NOCH BOX-WEIT IST (Stufe 2) ─────────────────────────────────
      `resume.json` (Weiterhoeren): daran haengen vier Routen,
      `saveResumeFiles()`, zwei Sperrdateien und `active_resume.json`. Die
      ZEIT kommt schon aus dem Verlauf des aktiven Profils — „wann war ich
      dran?" ist profilgebunden, „wo bin ich stehengeblieben?" noch nicht.
      Ebenfalls offen: ob das aktive Profil je auf Gast zurueckfaellt. Ohne
      Rueckfall laeuft Kaleas Konto weiter, wenn Liam die Box anmacht — genau
      der Fall, in dem eine Kinderzeit als kaputt erlebt wird.

  - kind: falle
    id: speicherschluessel-sechsfach-verstreut
    title: "Ein Speicherschluessel an sechs Stellen — beim Umzug vergisst man genau eine"
    sev: high
    match: "mupibox_favorites_v1|FAV_KEY|favMap|mupibox_p_"
    cause: "derselbe localStorage-Schluessel stand roh in sechs Dateien; ein Namensraum je Profil trifft dann nur fuenf davon"
    fix: "erst einen Dienst (favoriten.service.ts), dann den Namensraum — und tools/namensraum-schau.mjs dagegenstellen"
    tags: ['falle', 'oberflaeche', 'mehrbenutzer', 'speicher', 'werkzeug', 2026]
    source: "beim Bau der Mehrbenutzer-Vorbereitung, 02.08.2026"
    body: |
      `mupibox_favorites_v1` stand an SECHS Stellen im Quelltext der
      klassischen Oberflaeche, jede mit eigenem
      `JSON.parse(localStorage.getItem(...))`:

          home.page.ts:393
          favorites.page.ts:88
          player.page.ts:396 (FAV_KEY), 414, 463
          now-playing-bar.component.ts:1393 (FAV_KEY), 1400, 1436
          favorites-overlay.component.ts:376, 520, 522

      Solange der Name FEST war, ging das gut. Sobald er einen BESITZER
      traegt, ist jede vergessene Stelle ein STILLER Fehler: kein Absturz,
      keine Meldung — ein Kind sieht auf der Startseite seine Favoriten und im
      Player die des Geschwisters. Dieselbe Fehlerklasse wie
      [[darstellung-feld-faellt-still-heraus]].

      DIE REIHENFOLGE IST DER GANZE TRICK: erst den Dienst einziehen
      (`src/frontend-box/src/app/favoriten.service.ts`), alle sechs Stellen
      darauf umstellen, DANN den Namensraum einbauen. Andersherum baut man den
      Namensraum an fuenf Stellen ein und uebersieht die sechste.

      ── DIE FORMEL STEHT AN DREI STELLEN, und das laesst sich nicht
      vermeiden — drei Programme brauchen sie:

          src/backend-api/src/profile.ts                speicherName()
          NewDesign/app.js                              speicher.name()
          src/frontend-box/src/app/favoriten.service.ts name()

      Dagegen steht `tools/namensraum-schau.mjs --pruefen` (Schritt in
      `tools/pruefen.sh`). Es liest alle drei am QUELLTEXT und vergleicht sie
      gegeneinander; eine eigene Kopie der Formel waere die vierte Wahrheit.
      Es prueft ausserdem, dass kein Schluessel wieder in zwei Dateien steht.

      ZWEI DINGE, DIE ES BEIM BAU SELBST GELERNT HAT:
      1. Der erste Entwurf las eine FESTE DATEILISTE — und uebersah in der
         Gegenprobe prompt einen rohen Zugriff in home.page.ts, weil die Datei
         nicht auf der Liste stand. Eine Pruefung, die nur prueft, woran man
         ohnehin denkt, prueft nichts. Jetzt geht es den Baum durch.
      2. Es las zuerst KOMMENTARE mit und meldete die Formel als „an zwei
         Stellen verschieden", weil profile.ts sie im Kopfkommentar mit einem
         Beispiel erklaert. Ein Werkzeug, das an guter Dokumentation
         scheitert, erzieht dazu, die Dokumentation wegzulassen.

      GEWOLLTE DOPPELUNGEN tragen den Vermerk `NAMENSRAUM-AUSNAHME: <Grund>`
      im Quelltext daneben (hell/dunkel in index.html + app.js, die
      Sitzungsmarke `mupibox_neu_rueckweg`). Der Grund steht damit dort, wo
      jemand ihn liest, wenn er die Zeile anfasst — nicht in einer Liste im
      Werkzeug, die als erstes veraltet.

      GEGENPROBE GEMACHT (die Regel aus [[attrappe-luegt-durch-weglassen]]):
      alle drei Faelle absichtlich erzeugt und geprueft, dass das Werkzeug
      anschlaegt — Formel auseinander (Ende 1), roher Zugriff in home.page.ts
      (Ende 1), Gast heisst woanders anders (Ende 1). Danach zurueckgenommen.

  - kind: falle
    id: endpunkt-ohne-datei-antwortet-nie
    title: "Vier Endpunkte schwiegen, wenn ihre Datei fehlte — kein Fehler, nur Stille"
    sev: high
    match: "if \\(fs.existsSync\\(.*\\)\\) \\{[\\s\\S]*jsonfile.readFile|/api/mupihat|/api/wlan|/api/activeresume|/api/data"
    cause: "das Muster `if (fs.existsSync(datei)) { ...antworten... }` ohne else-Zweig laesst die Anfrage offen, statt eine Absage zu schicken"
    fix: "frueher Ausstieg `if (!fs.existsSync(datei)) return res.json([])` — dieselbe Form, die der Lesefehler-Zweig zwei Zeilen tiefer ohnehin liefert"
    tags: ['falle', 'server', 'express', 'mupihat', 'wlan', 'oberflaeche', 2026]
    source: "Gesamtpruefung nach sieben Umbauten, 02.08.2026"
    body: |
      DIESER EINTRAG IST DIE CODE-SEITE VON
      [[api-routen-ohne-gegenzweig-haengen]] — beide fuehren
      `/api/mupihat` und `/api/wlan` im `match:`. Die eine Frage, die
      trennt: WIRD DER QUELLTEXT GEAENDERT ODER DIE BOX? Hier steht der
      fehlende else-Zweig und seine Reparatur; drueben steht, wie sich
      dasselbe am Geraet anfuehlt (Oberflaeche wird mit der Laufzeit
      immer zaeher, weil jede offene Anfrage eine Browser-Verbindung
      belegt) und wie man eine BESTEHENDE Box wieder flott bekommt.

      GEFUNDEN BEI DER GESAMTPRUEFUNG (2026-08-02), nicht durch einen Fehler,
      sondern durch Suchen nach einem MUSTER. Sieben Endpunkte in server.ts
      sind nach derselben Form gebaut:

          app.get('/api/xyz', (_req, res) => {
            if (fs.existsSync(datei)) {
              jsonfile.readFile(datei, (error, data) => { ... })
            }
          })

      Drei davon hatten einen else-Zweig (`/api/resume`, `/api/network`,
      `/api/albumstop`), VIER nicht:

          /api/mupihat        mupihat.json        fehlt auf jeder Box OHNE MuPiHAT
          /api/wlan           wlan.json           fehlt bei frischer Einrichtung
          /api/activeresume   active_resume.json  fehlt, bis erstmals gemerkt wurde
          /api/data           active_data.json    fehlt bei leerer Mediathek

      Fehlt die Datei, laeuft die Anfrage in KEIN `res` hinein. Express
      antwortet dann gar nicht; die Verbindung bleibt offen, bis der Client
      aufgibt. Es gibt keinen Fehler, keinen Protokolleintrag, keinen
      Statuscode — nur Stille.

      ── WAS ES KOSTET ────────────────────────────────────────────────────
      `NewDesign/app.js` fragt `/api/mupihat` im 30-Sekunden-Takt (TAKT_HAT)
      und `json()` setzt KEINE Zeitgrenze. Auf einer Box ohne MuPiHAT sind das
      120 haengende Abfragen je Stunde, jede mit einem offenen Socket auf
      beiden Seiten, an einem Geraet, das tagelang durchlaeuft. Die
      Verwaltungsseite `mupihat.ts` wartet mit `firstValueFrom` und bekommt
      nie eine Absage — die Seite bleibt beim Laden stehen.

      ── WARUM `[]` UND NICHT 404 ─────────────────────────────────────────
      Weil genau das schon der LESEFEHLER-Zweig jedes dieser vier Endpunkte
      liefert. Eine zweite Form fuer denselben Fall waere eine Wahrheit zu
      viel. Die Verbraucher pruefen ohnehin auf Inhalt (`BatteryConnected`
      beim HAT, Laenge bei den Listen); bei `[]` bleibt das Akkuzeichen
      einfach weg, und genau so soll es sein.

      ── DER TEST BRAUCHT EINE FRIST ──────────────────────────────────────
      `src/backend-api/src/fehlende-datei.integration.spec.ts` startet den
      Server mit einem LEEREN Konfigurationsverzeichnis und prueft alle vier.
      Jeder Fall hat `{ timeout: 8000 }`, und das ist kein Beiwerk: Faellt der
      Zweig wieder weg, ANTWORTET der Endpunkt nicht — er schweigt. Ohne
      Frist wartete `node --test` darauf unendlich (das ist die Vorgabe), und
      aus einem roten Balken wuerde ein haengender Pruefsatz, den irgendwann
      jemand abschaltet. GEGENGEPROBT: die alte Form von `/api/wlan` wieder
      eingesetzt -> genau dieser eine Fall faellt nach 8 s durch, die anderen
      drei bleiben gruen.

      ── DIE ATTRAPPE VERSTECKTE ES ZUSAETZLICH ───────────────────────────
      `tools/neu-vorschau.mjs` bediente `/api/netzwerk` und `/api/mupihat`
      GAR NICHT und schickte 404. `netzHolen()` und `hatHolen()` blenden ihre
      Anzeige bei einem misslungenen Abruf aus — in der Vorschau waren die
      Funkbalken, die Wolke und das Akkuzeichen deshalb NIE zu sehen. Wer die
      Kopfzeile aenderte (Boxname, Hoehe, Abstaende), mass sie ohne die drei
      Zeichen, die rechts daneben stehen, und hielt sie fuer geprueft. Dritte
      Sorte aus [[attrappe-luegt-durch-weglassen]]. Die Attrappe liefert beide
      jetzt, umschaltbar ueber `/vorschau/netz-{gut|mittel|schwach|kabel|
      ohne-internet|weg}`, `/vorschau/hat-{akku|laedt|weg}` und
      `/vorschau/akku-<prozent>`.
    related: [api-routen-ohne-gegenzweig-haengen]

  - kind: falle
    id: kennungsform-verraet-nicht-die-art
    title: "22 Zeichen sind kein Album — die Interpretenseite schickte Playlisten als Alben"
    sev: high
    match: "spotify:album:|In deiner Box|Die Titel ließen sich nicht laden"
    cause: "Die Oberflaeche schloss aus der FORM der Spotify-Kennung (22 Zeichen aus Buchstaben und Ziffern) auf ein ALBUM. Playlisten und Shows haben genau dieselbe Form."
    fix: "Nicht die Kennung fragen, sondern das Werk: `werk.art === 'album'` UND `quellen[0].dienst === 'spotify'` (NewDesign/app.js, interpretKachelBauen)."
    tags: ['falle', 'oberflaeche', 'spotify', 'interpret', 'neu', 2026]
    source: "Gegenlesen der sieben Umbauten, 02.08.2026 — gemessen mit tools/interpretseite-befehle.mjs"
    related: [freigeschalteter-interpret-verliert-seine-box-reihe]
    body: |
      DIESER EINTRAG SETZT VORAUS, DASS DIE REIHE „IN DEINER BOX" DASTEHT —
      er handelt davon, dass die Kachel darin falsch verweist. Fehlt die
      Reihe ganz, obwohl die Kachel davor ein Werk gezaehlt hat, ist es der
      andere Fall mit demselben Symptomwort:
      [[freigeschalteter-interpret-verliert-seine-box-reihe]] (dort gewinnt
      der Spotify-Name gegen den Bibliotheksnamen).

      DAS KIND TIPPT UND NICHTS GESCHIEHT. Auf der Interpretenseite steht ganz
      oben die Reihe „In deiner Box". Der Spielknopf einer Spotify-PLAYLIST
      dort schickte:

          spotify/now/spotify:album:<PLAYLIST-Id>:1:0

      `playMe()` (spotify-control.ts) baut daraus `context_uri =
      spotify:album:<id>`. Spotify kennt diesen Zusammenhang nicht — es
      spielt nichts. Der Tipp auf dieselbe Kachel (statt auf den Knopf)
      holte die Titel von `/api/spotify/album/<PLAYLIST-Id>` und meldete
      „Die Titel ließen sich nicht laden."

      DIE URSACHE IST EIN SCHLUSS AUF DIE FORM:

          const spotifyAlbum = /^[A-Za-z0-9]{22}$/.test(e.kennung)
          fremd: !!werk && !spotifyAlbum

      Spotify-Kennungen sind IMMER 22 Zeichen aus Buchstaben und Ziffern —
      bei einem Album, bei einer Playlist, bei einer Show, bei einem Titel
      und bei einem Interpreten. Die Form sagt „das ist eine Spotify-Id",
      NICHT „das ist ein Album". `werke.ts artVon` fuehrt die Art als eigenes
      Feld, genau damit sie nicht zweimal entschieden wird.

      WEN ES TRIFFT: jede Spotify-Playlist und jede Show der Mediathek, die
      unter einem Interpreten steht. Auf dieser Box sind das die MEISTEN
      Spotify-Eintraege — Hoerspielreihen liegen als Playlist vor.

      GEMESSEN (02.08.2026, tools/interpretseite-befehle.mjs gegen
      tools/neu-vorschau.mjs, Chromium 800x480, `fetch` mitgelesen):

          vorher   Bibi Blocksberg  art=playlist -> spotify:album:...    FALSCH
          nachher  Bibi Blocksberg  art=playlist -> spotify:playlist:... ok
          nachher  Die drei ???     art=album    -> spotify:album:...    ok
          nachher  Die Maus         art=album (lokal) -> musicsearch/... ok

      WARUM ES NIEMAND SAH: Die Attrappe quittiert jeden Spielerbefehl mit
      `{ok:true}` — sie kann nicht wissen, dass in `spotify:album:` eine
      Playlist-Id steckt. Und die echte Box schweigt genauso. Der Fehler
      steht in keiner Meldung und in keinem Protokoll, nur auf der Leitung.
      Deshalb liest das Werkzeug mit, statt am Quelltext zu raten; es haengt
      als Schritt „Befehle der Interpretenseite" in tools/pruefen.sh.

      DASSELBE MUSTER ANDERSWO PRUEFEN: `spotifyId()` und
      `/^[A-Za-z0-9]{22}$/` stehen in der neuen Oberflaeche mehrfach. Als
      GUELTIGKEITSpruefung sind sie richtig, als ARTbestimmung nie.

  - kind: falle
    id: bereitschaft-sagt-nein-wenn-sie-nichts-weiss
    title: "„Die Box meldet sich gerade neu bei Spotify an“ — obwohl sie spielt"
    sev: high
    match: "/api/spotify/bereit|spotify-antwortet-nicht|meldet sich gerade neu"
    cause: "Der Waechter vor dem Abspielen wertete JEDE Fehlerantwort von /me/player/devices als „nicht bereit“ — auch 429 (Drosselung), 5xx und einen vorzeitig verworfenen Zugang."
    fix: "Eine Fehlerantwort ist keine Auskunft: `bereit: true, grund: 'nicht-pruefbar'`. Nur die LEERE Geraeteliste bleibt ein Nein."
    tags: ['falle', 'spotify', 'server', 'oberflaeche', 2026]
    source: "Gegenlesen der sieben Umbauten, 02.08.2026"
    body: |
      `/api/spotify/bereit` wird VOR jedem Spotify-Start gefragt (app.js
      `starte()`), und ein Nein VERHINDERT den Start — es kommt nur die
      Meldung „Die Box meldet sich gerade neu bei Spotify an."

      Der `catch`-Zweig derselben Funktion sagt richtig: „Nicht feststellbar
      ist NICHT dasselbe wie nicht bereit." Der Zweig darueber tat das
      Gegenteil:

          if (!antwort.ok) res.json({ bereit: false, grund: 'spotify-antwortet-nicht' })

      SZENARIO, DAS AUF DIESER BOX WIRKLICH VORKOMMT: Die
      Verfuegbarkeitspruefung feuert zweimal am Tag zwei Dutzend Abrufe im
      150-ms-Takt gegen api.spotify.com, und die Verwaltung hat einen Knopf
      „jetzt pruefen". Spotify antwortet dann 429. Von da an weist die
      Oberflaeche JEDE Spotify-Kachel ab — waehrend librespot verbunden ist
      und einwandfrei spielen wuerde. Dasselbe bei 5xx und bei einem Zugang,
      den Spotify vor seiner Frist verwirft (401; der Zugang liegt bis
      `expires_in - 60 s` zwischengespeichert).

      DIE VERWANDTE FALLE STAND SCHON EINEN SCHRITT FRUEHER im selben
      Endpunkt: der Boxname wurde zuerst aus `config.json` gelesen statt aus
      `mupiboxconfig.json` und ergab den Ersatzwert „MuPiBox", waehrend die
      Box „MixPiBox" heisst. Zweimal dieselbe Sorte Fehler in zwanzig Zeilen:
      ein Waechter, der im Zweifel nein sagt, erzeugt genau die Stoerung,
      gegen die er gebaut ist.

      ERLEDIGT AM 02.08.2026: `bereitschaftUrteil` sagte auch dann nein, wenn
      der Boxname nicht in der Geraeteliste stand — waehrend `geraetAufloesen()`
      im Abspieldienst auf `geraete[0]` zurueckfiel und spielte. Beide fragen
      jetzt dieselbe Funktion. Siehe [[geraetewahl-eine-regel-zwei-dienste]].

  - kind: falle
    id: 60-sekunden-takt-raeumt-die-lane-weg
    title: "Der Weiterhoeren-Takt baute das ganze Raster neu — und warf jede offene Lane weg"
    sev: mittel
    match: "weiterHolen|raster.textContent|laneEinhaengen|platzBeimBlaettern"
    cause: "weiterHolen() rief `zeichnen()`, und das leert `#raster`. Die Lanes haengen als Geschwister der Kacheln IM Raster."
    fix: "Nur die eigene Reihe malen (`weiterZeichnen()`), das Raster gehoert `werkeHolen` — und das malt nur bei geaendertem Stand."
    tags: ['falle', 'oberflaeche', 'neu', 'lane', 2026]
    source: "Gegenlesen der sieben Umbauten, 02.08.2026"
    body: |
      `weiterHolen` STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die trennt:
      BEWEGT SICH ETWAS VON SELBST?

          Eine offene Lane klappt nach spaetestens einer Minute ohne
          Zutun zu, die Kopfleiste bleibt danach weg
          -> DIESER Eintrag (der Takt baute das Raster neu)
          Es bewegt sich nichts, die Reihe „Weiterhoeren" zeigt nur zu
          WENIGE Kacheln
          -> [[weiterhoeren-deckel-vor-dem-sieb]] (der Abrufdeckel stand
             vor den Sieben, weggeworfene Zeilen frassen die Plaetze)

      Der eine Fehler ist eine Bewegung, der andere ein Fehlen — und
      beide haengen an derselben Funktion.

      `sichtbarerTakt(TAKT_WEITER, weiterHolen)` laeuft alle 60 Sekunden und
      rief `zeichnen()`. Dort steht `raster.textContent = ''`, und
      `laneEinhaengen()` haengt die aufgeklappte Lane mit
      `raster.insertBefore(...)` genau dort hinein.

      DREI FOLGEN, ALLE DREI AM KIND SICHTBAR:

        1. Ein Kind blaettert durch die Alben einer Playlist. Nach
           spaetestens einer Minute klappt alles von selbst zu — mitten in
           der Bewegung, ohne dass jemand etwas getan haette.
        2. `platzBeimBlaettern.sperren(false)` steht ALLEIN in
           `laneSchliessen()`. Wird die Lane weggeraeumt statt geschlossen,
           bleibt die Sperre stehen: die Kopfleiste faehrt von da an nicht
           mehr ein, bis jemand neu laedt.
        3. Alle Cover werden neu geholt. Genau diese Begruendung steht schon
           in `werkeHolen`, wo ein Neuaufbau deshalb ausdruecklich
           unterbleibt, solange sich `stand` nicht geaendert hat — der
           Nachbar hielt sich daran, dieser Takt nicht.

      Der Takt gab es schon fuer „Oft gehoert"; die Weiterhoeren-Reihe hat
      ihn uebernommen. Ein uebernommener Takt bringt seine Nebenwirkung mit.

      ES SIND VIER AUFRUFER, NICHT DREI. Am 02.08.2026 wurden
      `leisteBauen` (Kategoriewechsel), `regalOeffnen` und `regalZu`
      versorgt; `werkeHolen` blieb offen und hat mit 15 Sekunden den
      schnellsten Takt. Siehe [[raster-neuaufbau-vierte-stelle]].
    related: [lane-sperre-klemmte-in-beide-richtungen, raster-neuaufbau-vierte-stelle, weiterhoeren-deckel-vor-dem-sieb]

  - kind: falle
    id: resume-playlistposition-schickt-in-die-falsche-folge
    title: "Weiterhoeren bei einer Spotify-Playlist landet in der falschen Folge"
    sev: high
    match: "resumespotifytrack_number|track_number|Folge [0-9]"
    cause: "`item.track_number` ist die Nummer im ALBUM des Stuecks, nicht die Position in der Playlist. Fortgesetzt wird aber mit `offset.position` IN DER PLAYLIST."
    fix: "nicht behoben — beide Oberflaechen legen denselben Wert ab. Siehe [[resume-titelnummer-bei-playlists-unscharf]]."
    tags: ['falle', 'spotify', 'weiterhoeren', 'resume', 2026]
    source: "Gegenlesen der sieben Umbauten, 02.08.2026"
    body: |
      Der Eintrag [[resume-titelnummer-bei-playlists-unscharf]] nennt das
      „unscharf". Das ist zu freundlich; hier steht, was ein Kind erlebt.

      SZENARIO: Eine Hoerspiel-Playlist mit 300 Kapiteln. Das Kind hoert
      Kapitel 47. Dieses Stueck ist auf SEINEM Album Titel 3.

        gemerkt   resumespotifytrack_number = 3, progress_ms = 92500
        Kachel    „Folge 3"                       — falsch, es war Kapitel 47
        Tipp      spotify:playlist:<id>:3:92500
        Player    offset.position = 2  ->  Kapitel 3 der Playlist, bei 1:32

      Es spielt also eine ANDERE Folge, und die auch noch mittendrin. Fuer
      ein Kind ist das schlimmer als „faengt von vorn an": es klingt nach
      einer Stelle, die es nie gehoert hat.

      BEI EINEM ALBUM STIMMT ES. Der Fehler trifft nur Playlisten — und die
      sind auf dieser Box die haeufigste Form fuer Hoerspielreihen.

      NEU IST NICHT DER FEHLER, SONDERN SEINE LAGE. Die klassische
      Oberflaeche legt in `saveResumeFiles` denselben Wert ab und setzt
      genauso fort (player.service.ts). Bisher steckte das im schwebenden
      Weiterhoeren-Fenster; seit dem 02.08.2026 steht die Reihe GANZ OBEN auf
      der Startseite der neuen Oberflaeche und beschriftet die Kachel
      zusaetzlich mit „Folge N" — eine Behauptung, die die alte Oberflaeche
      nicht aufgestellt hat.

      DER SAUBERE WEG waere, beim Merken die Playlist-Position zu bestimmen:
      Spotifys Wiedergabe-Auskunft nennt `context.uri` und `item.uri`; die
      Position ergibt sich aus der Playlist. Das ist ein Web-API-Abruf beim
      Merken — er gehoert in den SERVER (POST /api/weiterhoeren), nicht in
      die Oberflaeche. Bis dahin waere sogar „gar keine Nummer merken"
      ehrlicher als eine falsche.
    related: [resume-titelnummer-bei-playlists-unscharf, weiterhoeren-playlist-position, klassische-oberflaeche-merkt-album-nummer]

  - kind: howto
    id: wlan-im-assistenten-zwei-stufen
    title: "WLAN im Einrichtungsassistenten: erst einstellen, dann erst festschreiben"
    tags: ['einrichtung', 'wlan', 'agent', 'sicherheit', 2026]
    source: "gebaut nach dem Vorbild im Backend, im Browser durchgespielt (2026-08-02)"
    body: |
      DER KNIFF STAMMT AUS DEM BACKEND (server.ts, /api/netzwerk/verbinden) und
      ist zu gut, um ihn im Assistenten nicht zu wiederholen:

        `select_network` schaltet die uebrigen Netze NUR IM SPEICHER ab.
        Solange niemand `save_config` ruft, steht auf der Platte weiterhin der
        alte Stand.

      Der Rueckweg ist damit ein Neustart — oder `wpa_cli reconfigure`. Wer
      sich beim Passwort vertippt, waere sonst dauerhaft aus dem eigenen Netz
      ausgesperrt; auf einer Box ohne Tastatur heisst das Karte ausbauen.

      Im Agenten sind es deshalb DREI Aktionen:
        wlan-verbinden    add_network / set_network / select_network
        wlan-bestaetigen  save_config      <- erst hier wird es dauerhaft
        wlan-verwerfen    reconfigure      <- der alte Stand gilt wieder
      Die Seite fragt nach `wlan-verbinden` ausdruecklich „Hat es geklappt?"
      und nennt beide Wege.

      ── DIE GRENZEN SIND ABGESCHRIEBEN, MIT ANSAGE ──────────────────────
      SSID: hoechstens 32 BYTE (nicht Zeichen! 17 Umlaute sind 34 Byte), keine
      Steuerzeichen, kein `"` und kein `\`. PSK: 8 bis 63 Zeichen, leer = offenes
      Netz, dieselben Verbote.
      Quelle: `src/backend-api/src/netzwerk.ts`. Dort ist begruendet, warum
      genau diese Zeichen verboten sind — sie koennten aus einer Zeile in
      wpa_supplicant.conf ausbrechen, und beide Werte gehen in
      Anfuehrungszeichen an `wpa_cli set_network`.
      DIE DOPPELUNG IST UNVERMEIDLICH: der Agent ist reines Python auf einer
      blanken Box und kann das TypeScript nicht einbinden. Deshalb steht die
      Fundstelle im Kommentar — wer dort die Grenzen aendert, muss hier
      nachziehen.

      ── DAS PASSWORT TAUCHT NIRGENDS AUF ────────────────────────────────
      Nicht in der Antwort, nicht im Lauf-Datensatz, nicht in einer
      Fehlermeldung (die nennt den SCHRITT, nie seine Werte). Sonst stuende es
      im Browserverlauf des Handys. Beides wird geprueft.

      ── EIN TEST DARF DEN RECHNER NICHT UMKONFIGURIEREN ─────────────────
      WICHTIG fuer jeden, der hier Tests ergaenzt: der Entwicklungsrechner hat
      eine echte Funkschnittstelle. Ein GUELTIGER Verbindungsversuch im Test
      wuerde `wpa_cli` wirklich aufrufen und das WLAN des Rechners umstellen.
      `tests/einrichtung_aktionen_test.py` prueft deshalb ausschliesslich die
      Schranke DAVOR: ungueltige Werte enden mit 400, bevor irgendein
      wpa_cli-Aufruf passiert. Der Weg dahinter gehoert auf eine Box.

      ── ZWEI KLEINIGKEITEN, die erst der Browser zeigte ─────────────────
      * Der Agent lieferte die Feldnamen ALPHABETISCH — das Formular fragte
        erst nach dem Passwort und dann nach dem Namen. Jetzt in der
        Reihenfolge ihrer Deklaration (Python behaelt sie bei).
      * `input[type=text]` im Stil traf auch den WLAN-Namen und machte ihn
        gross und gespreizt wie das sechsstellige Code-Feld. Der Stil gilt
        jetzt nur noch fuer `#code`.

  - kind: howto
    id: wps-im-assistenten-und-die-neue-adresse
    title: "WPS im Assistenten — und wie das Handy die Box danach wiederfindet"
    tags: ['einrichtung', 'wlan', 'wps', 'agent', 2026]
    source: "gebaut 2026-08-02, Mechanik geprueft ohne WPS auszuloesen"
    body: |
      WPS PER KNOPFDRUCK: `wpa_cli wps_pbc` oeffnet ein Fenster von rund zwei
      Minuten, in dem der Router nach SEINEM Knopfdruck die Zugangsdaten
      uebergibt. Danach schreibt wpa_supplicant sie SELBST in seine
      Konfiguration.

      DESHALB GIBT ES BEI WPS KEIN 'VERWERFEN' wie bei der Eingabe von Hand:
      der Zwei-Stufen-Rueckweg (select_network ohne save_config, siehe
      wlan-im-assistenten-zwei-stufen) greift nicht, weil nicht wir speichern.
      Misslingt WPS, holt `reconfigure` den gespeicherten Stand zurueck — mehr
      ist nicht noetig, denn ohne Erfolg hat sich auf der Platte nichts
      geaendert.

      ── DAS EIGENTLICHE PROBLEM: DIE NEUE ADRESSE ───────────────────────
      Nach erfolgreichem WPS haengt die Box in einem ANDEREN Netz und hat eine
      ANDERE Adresse. Die Seite, auf der man gerade steht, ist damit tot — und
      das Handy weiss nicht, wohin.

      DIE ANTWORT STAND SCHON DA: Der Bildschirm der Box liest seine Adresse
      alle zwei Sekunden neu und zeichnet den QR-Code neu
      (`einrichtung-schirm.py`). Sobald die neue Adresse da ist, haengt sie am
      Geraet — einmal hinsehen, abfotografieren, weiter. Kein mDNS, keine App,
      kein Suchlauf im Subnetz.
      Der Strom nennt die neue Adresse ausserdem als letzte Zeile, solange die
      alte Verbindung noch steht.

      ── EINE DRITTE SORTE AKTION: PYTHON MIT AUSGABESTROM ───────────────
      WPS dauert bis zu zwei Minuten. Als synchrone Antwort wuerde der Browser
      aufgeben, und ein Assistent, der zwei Minuten schweigt, sieht aus wie
      einer, der haengt. `start_lauf_python()` legt deshalb denselben
      Lauf-Datensatz an wie ein Unterprozess und fuellt ihn aus einem Thread —
      der SSE-Strom kennt keinen Unterschied.
      ACHTUNG: kein Unterprozess heisst KEINE Prozessgruppe, `/abort` kann hier
      also nichts erschlagen. Der Datensatz traegt `abbrechbar: False`, und die
      Arbeit haelt ihr Zeitlimit selbst ein.

      ── WPS IST EINE BEQUEMLICHKEIT, KEIN ERSATZ ────────────────────────
      Bei neueren Routern ist WPS ab Werk ABGESCHALTET. Der Weg ueber Name und
      Passwort bleibt deshalb der Hauptweg; WPS steht daneben. Scheitert es,
      sagt der Strom genau das — statt nur "misslungen".

      ── EIN TEST DARF WPS NICHT AUSLOESEN ───────────────────────────────
      `wps_pbc` wirkt auf die ECHTE Funkschnittstelle. Auf dem
      Entwicklungsrechner wuerde ein Test dessen WLAN anfassen. Geprueft wird
      deshalb: dass es die Aktion gibt, dass sie hinter dem Tor liegt, und die
      MECHANIK dahinter (start_lauf_python, Zerlegung von `wpa_cli status`) —
      Letzteres direkt am Modul statt ueber HTTP.

      ── UND WARUM KEINE ANDROID-APP ─────────────────────────────────────
      Erwogen und verworfen. Sie muesste als APK von der Seite geladen werden,
      also ueber "Installation aus unbekannten Quellen" — man brachte Eltern
      damit genau das bei, wovor jede Sicherheitsberatung warnt. iPhones waeren
      wieder aussen vor (dasselbe Argument wie bei Bluetooth). Und sie loeste
      ein Problem, das der Bildschirm der Box umsonst loest.

  - kind: architektur
    id: wlan-wechsel-die-box-prueft-sich-selbst
    title: "Der Regelkreis beim WLAN-Wechsel: die Box gibt sich die Entwarnung selbst"
    tags: ['einrichtung', 'wlan', 'wps', 'watchdog', 'agent', 2026]
    source: "gebaut 2026-08-02, Selbstpruefung am Rechner erprobt"
    body: |
      DAS LOCH IM VORHANDENEN MUSTER: `remote-step-installer/tools/mupibox-netz-watchdog.py` ist ein
      Totmannschalter nach `commit confirmed` — vor der Aenderung einen Wecker
      stellen, danach entwarnen. Sein Kniff ist, dass die Entwarnung nur ueber
      eine FUNKTIONIERENDE Verbindung kommen kann und damit ein Beweis ist,
      kein Knopf.

      Beim Einrichtungsassistenten geht das NICHT: sobald die Box das WLAN
      wechselt, ist die Seite auf dem Handy tot. Es ist niemand mehr da, der
      entwarnen koennte.

      LOESUNG: die Box prueft sich selbst — und entwarnt nur, wenn sie es
      BEWIESEN hat.

      ── DREI STUFEN, WEIL EINE NICHTS SAGT ──────────────────────────────
        1. Adresse da?      -> DHCP hat geantwortet
        2. Gateway pingbar? -> das oertliche Netz traegt
        3. Name aufloesen + Verbindung nach draussen -> es geht ins Internet
      Bei einer einzigen Pruefung hiesse ein Fehlschlag nur „irgendwo klemmt
      es". So steht da, WO — und das braucht jemand, der vor einer stummen Box
      steht. Stufe 3 zielt auf Spotify: ein Netz, das alles kann ausser
      Spotify, ist fuer diese Box kein Netz.

      ── DER ABLAUF BEIM WPS ─────────────────────────────────────────────
        Watchdog scharf (5 min)  ->  wps_pbc  ->  warten  ->  Selbstpruefung
          draussen erreichbar -> entwarnung, fertig
          nicht erreichbar    -> KEINE Entwarnung; laeuft der Wecker ab,
                                 gilt wieder der alte Stand
        Fehlt der Watchdog (frisch aufgesetzte Box), laeuft es OHNE Netz —
        das wird gesagt, aber nicht verhindert. So haelt es auch das Backend.

      ── DIE FALLE: /proc/net/route IST LITTLE ENDIAN ────────────────────
      Die Datei nennt das Gateway als Hexzahl in UMGEKEHRTER Byte-Reihenfolge.
      `0100A8C0` ist 192.168.0.1, NICHT 1.0.168.192. Wer nicht dreht, pingt
      eine Adresse, die es nicht gibt, und haelt das Netz fuer kaputt —
      ausgerechnet in dem Moment, in dem entschieden wird, ob zurueckgerollt
      wird. `gateway_lesen()` ist deshalb pure und einzeln geprueft.

      ── UND DER SCHIRM SAGT ES AUCH ─────────────────────────────────────
      Nach dem Wechsel steht auf dem Bildschirm der Box nicht nur die neue
      Adresse als QR-Code, sondern auch „Verbunden mit <Netzname>". Ohne das
      raet man, ob die Adresse daneben noch die alte ist.
      Dafuer traegt die Adresszeile jetzt den Port gleich mit
      (`192.168.178.55:8099`) — der Agent liefert die Seite auch unter „/"
      aus, `/einrichtung` muss also niemand abtippen. Das sparte die Zeile,
      die die WLAN-Auskunft braucht.

      ── WARUM KEIN QR-FOTO FUER DIE WLAN-DATEN ──────────────────────────
      Erwogen (Router-Aufkleber abfotografieren, Felder fuellen lassen) und
      verworfen, mit gemessener Begruendung:
        * `getUserMedia` braucht einen SICHEREN KONTEXT. Die Box liefert ueber
          http://192.168.x.y — dort ist `navigator.mediaDevices` schlicht
          undefined. (Gemessen: auf 127.0.0.1 ist isSecureContext true, das
          taeuscht — localhost gilt immer als sicher.)
        * `BarcodeDetector` fehlte im geprueften Chromium und gibt es auf iOS
          gar nicht.
        * Bliebe `<input capture>` plus ein SELBST GESCHRIEBENER Entzifferer.
          Der ist deutlich mehr als der Kodierer (Entzerrung, Rasterfindung,
          Reed-Solomon RUECKWAERTS) und scheitert am ehesten am Foto selbst.
      WPS loest dasselbe Problem — „das Passwort nicht abtippen" — mit einem
      Knopfdruck am Router statt mit Bilderkennung.

  - kind: howto
    id: einrichtung-ap-und-captive-portal
    title: "Das eigene WLAN der Box — und warum sich die Seite von selbst oeffnet"
    tags: ['einrichtung', 'wlan', 'hostapd', 'captive-portal', 2026]
    source: "gebaut 2026-08-02, reine Teile und Portal geprueft"
    body: |
      DER RUECKFALL OHNE KABEL: `remote-step-installer/tools/einrichtung-ap.py` macht ein eigenes
      WLAN auf (`MixPiBox-Einrichtung`), das Handy verbindet sich per QR-Code
      ohne Tippen, und der Assistent laeuft wie sonst.
      KABEL BLEIBT DER ERSTE WEG: solange die Box am Kabel haengt, ist sie im
      richtigen Netz und das Handy muss sein WLAN nicht wechseln.

      ── DAS HUEHNEREI-PROBLEM, EHRLICH ──────────────────────────────────
      `hostapd` und `dnsmasq` sind WEDER auf dem DietPi-Image NOCH ohne Netz
      nachinstallierbar. Ein AP „aus dem Nichts" ist unmoeglich; die Pakete
      muessen beim Vorbereiten der SD mitkommen oder einmal ueber Kabel geholt
      werden. `bereit()` sagt, was fehlt, statt es zu versuchen.
      (Am Geraet geprueft: beide fehlen, `iw` ist da, der Chip kann AP,
      Band 1 mit Kanaelen 1-13.)

      ── EIN FUNKBAUSTEIN, EINE ROLLE ────────────────────────────────────
      Der Pi kann nicht verlaesslich gleichzeitig ein Netz aufspannen UND sich
      in ein fremdes einbuchen. Deshalb wird wpa_supplicant angehalten, solange
      der AP laeuft, und beim Beenden wieder losgelassen. Nach `stoppen` bleibt
      nichts uebrig — auch nicht das Passwort. Ein AP, der die Einrichtung
      ueberlebt, waere ein offenes Tor im Kinderzimmer.

      ── WIE HANDYS EIN PORTAL ERKENNEN ──────────────────────────────────
      Nach dem Verbinden ruft jedes System eine FESTE Adresse ab und prueft die
      Antwort:
        Android   /generate_204 (connectivitycheck.gstatic.com)  will 204, leer
        iOS/macOS /hotspot-detect.html (captive.apple.com)        will „Success"
        Windows   /connecttest.txt (msftconnecttest.com)          will festen Text
      Kommt etwas ANDERES, oeffnet das Geraet die Seite von selbst — das
      Verhalten aus dem Hotel-WLAN.

      DER TRICK IST ALSO NICHT DIE UMLEITUNG, SONDERN DASS DIE ERWARTUNG NICHT
      ERFUELLT WIRD. Wer auf /generate_204 brav 204 antwortet, hat KEIN Portal,
      und niemand sieht, woran es lag. Genau darauf zielen die Tests.

      ZWEI TEILE, EINES ALLEIN TUT NICHTS:
        1. `address=/#/192.168.4.1` in der dnsmasq-Konfiguration — jeder Name
           zeigt auf die Box.
        2. Ein winziger Umleiter auf PORT 80, der jede Anfrage mit 302 auf
           `http://192.168.4.1:8099/einrichtung` beantwortet.
      Der Umleiter laeuft als eigener Prozess (`einrichtung-ap.py portal`).
      Scheitert er, weil Port 80 belegt ist, ist das kein Grund den AP
      hinzuwerfen — die Adresse steht ja als QR-Code auf dem Schirm.

      ── DER SCHIRM ZEIGT IM AP-FALL EINEN ANDEREN CODE ──────────────────
      Nicht die Adresse, sondern die WLAN-ZUGANGSDATEN
      (`WIFI:S:…;T:WPA;P:…;;`) — das Handy ist ja noch gar nicht im selben
      Netz, ein Adress-QR waere nutzlos. Die Unterzeile sagt das auch
      („Mit diesem WLAN verbinden"), sonst haelt jemand die Kamera drauf und
      wundert sich, dass sein Handy nach einem Netz fragt statt eine Seite zu
      oeffnen.
      MASKIEREN NICHT VERGESSEN: `;` `:` `,` `\` muessen im WIFI-Text
      maskiert werden. Ein Netzname mit Semikolon zerlegt den Code sonst still.

      ── WAS DIE TESTS WIRKLICH SICHERN ──────────────────────────────────
      Nicht „hostapd startet" (das sagt nur eine Box), sondern die STILLEN
      Fehler: fehlt `wpa=2`, ist das Netz OFFEN — vor der Tuer eines Programms,
      das als root laeuft. Deshalb wird ausdruecklich auf WPA2/CCMP geprueft
      UND auf die Abwesenheit von WPA1 und TKIP. Dazu: dass Schirm und AP
      dieselbe Adresse und denselben Arbeitsordner meinen — laufen die
      auseinander, steht auf dem Bildschirm eine Adresse, unter der nichts
      antwortet.

  - kind: architektur
    id: interpretenseite-was-spotify-hergibt
    title: "Die Interpretenseite ist ganz — aber Spotify gibt je Abruf nur 50 heraus, und Die drei ??? hat 308 Alben"
    tags: ['architektur', 'spotify', 'interpret', 'neu', 'oberflaeche', 2026]
    source: "Nachlese der Interpretenseite, 02.08.2026 — gemessen ueber die Durchreiche der Box und mit tools/interpretseite-befehle.mjs"
    body: |
      DER STAND. Die Seite gibt es vollstaendig: Server-Endpunkt
      `/api/interpret/:id`, die reine Zusammenstellung in
      `interpretenseite.ts` (mit spec), die Bilddurchreiche
      `/api/bild/extern`, die Freigabe `artists/<id>/top-tracks` in
      `spotify-web.ts`, die Seite in `NewDesign/app.js` samt Rueckweg, und der
      Schalter „Ganze Diskografie" ist an ALLEN drei Stellen von
      `darstellung.service.ts` eingetragen (Schnittstelle, STANDARD,
      pruefen()) plus Verwaltung und spec. Drei Tueren fuehren hinein: die
      runde Kachel der Interpreten-Reihe, eine Kachel mit `art === 'interpret'`
      (aus `artistid`), und der Interpretenname im grossen Player.

      ── WAS SPOTIFY ZEIGT UND WAS WIR ZEIGEN ────────────────────────────
          Bild, Name, Genres        ja
          monatliche Hoerer         NEIN — die Web-API kennt sie nicht. Sie
                                    liefert `followers.total`; bei Alin Coen
                                    54.602 gegen 112.866 auf open.spotify.com.
                                    Eine ANDERE Zahl, nicht dieselbe kleiner.
          Beliebteste Titel         ja (top-tracks, 10 Stueck)
          Alben                     ja
          Singles und EPs           ja
          Compilations              ja, seit 02.08.2026 als „Sammlungen"
          Erscheint auf             bewusst NICHT (siehe unten)
          Fans moegen auch          unmoeglich — `related-artists` ist seit
                                    2024-11-27 abgeschaltet, an der Box mit
                                    HTTP 404 nachgemessen.

      ── DIE 50er-GRENZE IST DAS EIGENTLICHE THEMA ───────────────────────
      Spotify deckelt `limit` bei 50. GEMESSEN am 02.08.2026 ueber
      `<box>:8200/api/spotify/web/artists/<id>/albums?include_groups=...`:

          Interpret          album  single  compilation  appears_on
          Die drei ???         308       0            0           0
          Bibi Blocksberg      241      15            0           1
          Rolf Zuckowski        53      21           44          60
          Alin Coen              5      15            0          27

      Bei den ersten drei kommen also 50 an. Eine Reihe mit 50 Kacheln und
      ohne Zahl behauptet „das ist alles" — und genau dieser Irrtum EINE
      EBENE TIEFER war der Anlass fuer die ganze Seite („eine Playlist ist
      eine AUSWAHL"). Deshalb reist `total` aus derselben Antwort mit
      (`gesamt`, kostet keinen Abruf), der Bauer setzt es NUR beim
      Abschneiden, und die Ueberschrift sagt „Alben · 50 von 308".

      NICHT GESETZT WIRD ES, wenn nur die ENTDOPPLUNG etwas weggenommen hat:
      verglichen wird `total` mit der Zahl der GEHOLTEN Eintraege, nicht mit
      den uebriggebliebenen. Sonst stuende die Zahl bei jedem Interpreten da,
      bei dem Spotify eine Fassung doppelt fuehrt.

      OFFEN BLEIBT DAMIT: Folge 42 der drei ??? ist ueber diese Seite nicht
      erreichbar — nur die neuesten 50 (Spotify liefert absteigend nach
      `release_date`, ebenfalls gemessen). Blaettern koennte man
      (`offset=50/100/…`, gemessen 0,36 bis 1,24 s je Seite ueber die Box),
      aber 308 Kacheln sind auf 800 px bei 118 px je Kachel 36.000 px
      Wischweg. Das ist eine SUCHE, keine Reihe — und damit eine eigene
      Aufgabe. Die Zahl in der Ueberschrift sagt bis dahin wenigstens die
      Wahrheit.

      ── SAMMLUNGEN SIND NICHT „ERSCHEINT AUF" ───────────────────────────
      `include_groups=compilation` sind EIGENE Zusammenstellungen des
      Interpreten; `appears_on` sind FREMDE Platten mit einem Gastauftritt.
      Die 44 Sammlungen von Rolf Zuckowski gehoeren ihm und fehlten
      vollstaendig. Die 60 `appears_on` bleiben draussen: auf einer Kinderbox
      ist das die Reihe, die in die Irre fuehrt.

      ── GEMESSEN, NICHT BEHAUPTET ───────────────────────────────────────
      `tools/interpretseite-befehle.mjs` prueft seit 02.08.2026 ALLE Reihen,
      nicht nur „In deiner Box" — sie laufen durch dieselbe Funktion, aber
      durch verschiedene Zweige. Es liest die Antwort von `/api/interpret/`
      mit (sonst gaebe es keine Sollwerte fuer `albumKennung`/`titelNr`) und
      dazu die Masse:

          Rueckweg          64x64 px   (einziger Ausgang, volle Flaeche)
          Kachel            118x153 px
          Interpretenbild   88x88 px
          Spielknopf        44x44 px   (liegt IN der Kachel, kein eigenes Ziel)
          Kontrast „von 308"  hell 3,41:1   dunkel 5,93:1

      Der helle Wert liegt unter WCAG-AA fuer Fliesstext (4,5:1). Er ist
      `--muted`, also derselbe Grauton wie ueberall sonst in dieser
      Oberflaeche; ihn nur hier anzuheben waere eine zweite Wahrheit. Wer ihn
      anhebt, hebt ihn global.

      ── EIN FUND NEBENBEI ───────────────────────────────────────────────
      Die Zahl stand als eigenes `<span>` mit `margin-left` in der
      Ueberschrift. Sichtbar richtig — aber `textContent` liest „Alben5 von
      308", und das ist, was eine Vorlesestimme sagt. Der Trennpunkt gehoert
      IN den Text, nicht in den Abstand.

  - kind: entscheidung
    id: geraetewahl-eine-regel-zwei-dienste
    title: "Welches Geraet ist die Box? Eine Regel fuer den Waechter UND den Abspieldienst"
    sev: mittel
    match: "bereitschaftUrteil|geraetAufloesen|geraetWaehlen|box-fehlt|ersatzgeraet"
    cause: "Dieselbe Frage wurde an zwei Stellen verschieden beantwortet: der Waechter verlangte den Namen NAMENSGENAU, der Abspieldienst fiel auf `geraete[0]` zurueck"
    fix: "`geraetWaehlen` in src/backend-api/src/spotify-web.ts — beide Seiten fragen sie; Rueckfall nur noch bei GENAU EINEM Geraet"
    tags: ['spotify', 'librespot', 'server', 'abspieldienst', 'entscheidung', 2026]
    source: "Gegenpruefung Befund A, umgesetzt und gemessen am 02.08.2026"
    body: |
      DER BEFUND: `bereitschaftUrteil` (spotify-web.ts) urteilte „nicht
      bereit", sobald `mupibox.host` nicht namensgenau in Spotifys
      Geraeteliste stand. `geraetAufloesen` (spotify-control.ts) war an
      derselben Stelle grosszuegig und nahm `geraete[0]`.

      DAS SZENARIO, DAS ES AUSLOEST: `mupibox.host` ist DREIFACH belegt —
      Anzeigename, LIBRESPOT_NAME (config/templates/env-librespot,
      scripts/librespot/librespot-start.sh) und Systemhostname. librespot
      liest den Namen NUR BEIM START. Wer ihn in der Verwaltung aendert und
      librespot nicht neu startet, hat eine Box, die bei Spotify weiter unter
      dem ALTEN Namen steht. Die Oberflaeche sagte dann „Die Box meldet sich
      gerade neu bei Spotify an" und VERHINDERTE das Abspielen — waehrend der
      Abspieldienst dasselbe Geraet anstandslos bespielt haette.

      ── DIE REGEL, DIE JETZT AN EINER STELLE STEHT ──────────────────────
      `geraetWaehlen(geraete, name)` gibt zurueck, WIE gewaehlt wurde:

          name           namensgenauer Treffer            -> bereit
          ohne-namen     wir kennen unseren Namen nicht,
                         also erstes Geraet               -> bereit
          ersatz         Name passt auf nichts, aber es
                         steht GENAU EIN Geraet da        -> bereit
                                                             (grund: ersatzgeraet)
          mehrdeutig     mehrere Geraete, keines passt    -> NICHT bereit
                                                             (grund: box-fehlt)
          keine-geraete  leere Liste                      -> NICHT bereit

      WARUM DER ALTE RUECKFALL `geraete[0]` NICHT BLEIBT: In der Liste stehen
      auch Geister — ein Browser-Tab vom Rechner, das Telefon der Eltern.
      Faellt die Wahl darauf, spielt Spotify unsichtbar im Nachbarzimmer,
      waehrend das Kind vor einer stillen Box sitzt (dieselbe Falle steht bei
      `setActiveDevice`, und dort ist sie schlimmer: es folgt eine
      UEBERNAHME). Eindeutig ist der Rueckfall nur, wenn es NICHTS ANDERES
      gibt. Bei mehreren wird nicht geraten — dann stimmt „nicht bereit" fuer
      beide Seiten.

      ── WIE DER ABSPIELDIENST AN DIE REGEL KOMMT ────────────────────────
      spotify-control.ts holt sie ueber den Paketrand:

          const { geraetWaehlen } =
            require('../../backend-api/src/spotify-web') as typeof import('../../backend-api/src/spotify-web')

      ALS `typeof import(...)` GETYPT und nicht blind gefordert: so prueft
      `tsc --noEmit` im backend-player die Stelle wirklich mit, und die Datei
      bleibt ein Skript (ein `import` machte sie zum Modul, und alles oben
      haengt am Skript-Zuschnitt). GEPRUEFT, dass die Regel wirklich im
      ausgelieferten Buendel landet und nicht nur im Typ:
          npx esbuild src/backend-player/src/spotify-control.ts --bundle --platform=node ... 
          grep -o mehrdeutig <ausgabe>   -> zweimal gefunden

      ── NEBENFUND, DER VORHER STILL WAR ─────────────────────────────────
      Spotify darf `id` null lassen. Frueher landete dieses null unbesehen in
      `activeDevice` (die Stelle war `any`), und der folgende Abspielbefehl
      ging mit `device_id: undefined` hinaus. Jetzt gilt: ohne Kennung ist es
      kein Geraet.

      ── AM GERAET GELESEN (02.08.2026, nur lesend) ──────────────────────
          GET <box>:8200/api/config              -> mupibox.host = "MixPiBox"
          GET .../api/spotify/web/me/player/devices
              -> genau EIN Geraet: "MixPiBox", type Speaker, id 40 Zeichen
          GET .../api/spotify/bereit             -> {"bereit":true,"name":"MixPiBox","geraete":1}
      Auf dieser Box steht also genau ein Geraet in der Liste — die
      Umbenennung fiele damit in den Zweig `ersatz` und spielt weiter.

      WERKZEUG: `tools/geraetewahl-abgleich.mjs` stellt Boxname, Geraeteliste
      und das Urteil der Box nebeneinander und sagt, wenn der Name
      auseinanderlaeuft (`--pruefen` gibt dann 1 zurueck). Es rechnet die
      Regel NICHT nach — eine dritte Meinung waere genau der Fehler, den
      dieser Umbau beseitigt.

  - kind: falle
    id: addresume-sperre-nur-ueber-das-lesen
    title: "/api/addresume gab die Sperre frei, bevor geschrieben war — eine gemerkte Stelle ging verloren"
    sev: mittel
    match: "resume.lock|addresume|resume.json ueberschrieben|Stelle verschwunden"
    cause: "`fs.unlink(resumeLock)` stand HINTER dem Anstossen von `jsonfile.readFile`, also lief es los, waehrend Lesen, Aendern und Schreiben noch bevorstanden"
    fix: "Sperre setzen, dann alles in try/finally — lesen, aendern, atomar schreiben, erst im finally freigeben (wie POST /api/weiterhoeren es seit jeher macht)"
    tags: ['server', 'resume', 'weiterhoeren', 'falle', 2026]
    source: "Gegenpruefung Befund B, belegt und behoben am 02.08.2026"
    body: |
      GEMESSEN, NICHT VERMUTET (src/backend-api/src/resume-sperre.integration.spec.ts,
      resume.json mit 1200 Eintraegen, damit die Luecke zwischen „Sperre weg"
      und „geschrieben" so breit ist wie auf einer SD-Karte): zwei Anfragen
      im Abstand von 3 ms bekamen BEIDE „ok", beide meldeten „New entry
      added" — und in der Datei stand am Ende nur EINE der beiden Stellen.
      Nach dem Umbau bekommt die zweite „locked" und nichts geht verloren.

      DAS SZENARIO IST AUF DIESER BOX ECHT: die neue Oberflaeche laeuft auf
      dem Bildschirm der Box, die klassische gleichzeitig im Browser eines
      Telefons. Beide merken sich beim Umschalten eine Stelle. Der Schaden
      ist unsichtbar — die Kachel steht da, sie startet nur von vorn.

      DAZU GLEICH MITGENOMMEN:
        * `fs.openSync(resumeLock,'w')` gab einen Dateizeiger zurueck, den
          niemand schloss. Jetzt `fs.writeFileSync`.
        * Geschrieben wird mit UMBENENNEN als letztem Schritt (Zwischendatei
          im GLEICHEN Verzeichnis, nur dort ist rename atomar) — wie
          /api/weiterhoeren. Eine halb geschriebene resume.json ist beim
          naechsten Lesen keine Liste mehr, und dann waere JEDE Stelle weg.

      NICHT GEAENDERT: der Vergleich ueber `id`
      [addresume-vergleicht-undefined-ids]. Daran haengt die klassische
      Oberflaeche seit jeher.

      NOCH OFFEN, DERSELBE ZUSCHNITT: /api/add, /api/edit, /api/delete und
      /api/editresume geben ihre Sperre weiterhin zu frueh frei.

      SPERRPFAD UMLENKBAR (NUR FUER TESTS): `MUPIBOX_LOCK_DIR` verschiebt
      `.data.lock` und `.resume.lock`; ohne die Variable bleibt es /tmp. Auf
      der Box MUSS es /tmp bleiben — die klassische Oberflaeche und
      remove_max_resume.sh kennen die Pfade fest. Im Test ist die Umlenkung
      noetig, weil `node --test` die Spec-Dateien PARALLEL in eigenen
      Prozessen startet: an einer gemeinsamen Sperrdatei uebersprangen zwei
      Laeufe einander die Schreibvorgaenge und waeren gruen gewesen, ohne je
      geschrieben zu haben.
    related: [addresume-vergleicht-undefined-ids]

  - kind: falle
    id: aufraeum-sicherung-vor-der-entscheidung
    title: "Die Sicherung entstand, bevor feststand, ob etwas entfernt wird — drei leere verdraengen die echte"
    sev: mittel
    match: "data-vor-aufraeumen|aufraeumSicherungAnlegen|SICHERUNGEN_BEHALTEN|medienAendern"
    cause: "`aufraeumSicherungAnlegen()` lief VOR `medienAendern`; wurde die Aenderung danach abgewiesen (gesperrt, nichts gefunden), blieb eine folgenlose Sicherung stehen — und es bleiben nur die drei juengsten"
    fix: "Als Haken `vorSchreiben` IN medienAendern: hinter der Entscheidung, vor dem ersten Byte, unter derselben Sperre"
    tags: ['server', 'mediathek', 'aufraeumen', 'sicherung', 'falle', 2026]
    source: "Gegenpruefung Befund C, belegt und behoben am 02.08.2026"
    body: |
      DAS SZENARIO: Ein Elternteil raeumt 5 Eintraege weg — die Sicherung
      dazu ist die einzige, die zaehlt. In einem zweiten, veralteten
      Verwaltungsfenster wird danach noch gedrueckt, ohne dass etwas
      entfernt wird. Jedes Mal entstand eine leere Sicherung, und nach drei
      davon war die ECHTE herausgefallen (SICHERUNGEN_BEHALTEN = 3). Eine
      Sicherung, die man verliert, weil NICHTS passiert ist, ist schlimmer
      als keine.

      FOLGENLOS IST NICHT AUSGEDACHT — es genuegt, dass `medienAendern` die
      Sperre nicht bekommt: die Box selbst schreibt data.json beim
      Hinzufuegen, und dann kommt 409 „gesperrt" zurueck. Die zweite Tuer ist
      `nichtGefunden`: `findeIndex` gibt bei zwei gleichen Schluesseln -1
      zurueck, es faellt nichts.

      BELEGT (aufraeumen.integration.spec.ts, „legt KEINE Sicherung an, wenn
      die Bibliothek gerade gesperrt ist"): mit `.data.lock` in der Hand kam
      409, und vorher stand eine neue `data-vor-aufraeumen-*.json` da, die
      nichts sicherte.

      FALLE IM TEST SELBST, die ihn erst still gruen machte: der Name der
      Sicherung hat SEKUNDENAUFLOESUNG. Lief der Test in derselben Sekunde
      wie sein Vorgaenger, trug die neue Sicherung denselben Namen,
      ueberschrieb die vorhandene — und die ANZAHL blieb gleich. Der Test war
      gruen, obwohl genau der Fehler passiert war, den er sucht. Deshalb
      wartet er die Sekunde ab.

      DIE FORM DER LOESUNG: `medienAendern(aendern, vorSchreiben?)`. Der
      Haken laeuft nur, wenn wirklich geschrieben wird, und noch unter
      derselben Sperre — die Sicherung traegt damit GENAU den Stand, der
      gleich ersetzt wird. Wirft er, wird nichts geschrieben
      (`sicherungFehlgeschlagen`, 500): OHNE SICHERUNG WIRD NICHT ENTFERNT
      gilt unveraendert.

  - kind: entscheidung
    id: titelkarte-abgeloest-durch-lanes
    title: "Die Titelkarte des grossen Players ist entfernt — die Lanes koennen mehr"
    sev: info
    match: "karteAuf|gross-hinten|gh-liste|gh-zeile|titelSpringen|Titelkarte"
    cause: "Seit den Lanes fuehrt der bessere Weg zu jedem Titel dorthin; der Tipp aufs Cover zeigt das Bild gross. Die Karte hatte keinen Einstiegspunkt mehr und lag als toter Zweig da"
    fix: "Ersatzlos entfernt (app.js, index.html, app.css) — das GEMESSENE Wissen aus titelSpringen steht hier"
    tags: ['neue-oberflaeche', 'player', 'aufraeumen', 'entscheidung', 2026]
    source: "entfernt und gegen tools/neu-vorschau.mjs im Browser nachgemessen, 02.08.2026"
    body: |
      WAS ES WAR: Der grosse Player war ein Blatt mit zwei Seiten. Ein Tipp
      aufs Cover drehte es um (`rotateY(180deg)`), hinten stand die
      Titelliste des LAUFENDEN Werks, ein Tipp auf eine Zeile sprang dorthin.
      Dazu gehoerten `karteAuf`/`karteZu`, `inhaltHolen`, `listeZeichnen`,
      `titelSpringen`, `laufendeZeileMarkieren`, `hinweisZeigen`, die
      `.gross-hinten` im HTML und rund 80 Zeilen `.gh-*` im CSS.

      WARUM SIE WEG IST — und zwar nicht, weil sie schlecht war:
      [[lanes-playlist-alben-titel]] hat den besseren Weg gebracht. Die Lane
      zeigt die VOLLSTAENDIGE Titelliste eines Albums, mit Bild je Titel, und
      sie ist erreichbar, OHNE dass etwas laufen muss. Die Karte konnte nur,
      was gerade spielte. Mit dem Umbau bekam der Tipp aufs Cover ausserdem
      eine neue Aufgabe (`albumGrossAuf`, Bild formatfuellend) — damit war
      der einzige Einstiegspunkt der Karte weg. Sie lag danach mehrere
      Ausbauschritte lang unerreichbar da, samt Kommentaren, die ein
      Verhalten versprachen, das es nicht mehr gab („bleibt es beim
      ehrlichen Hinweis in `karteAuf`").

      ── WAS GERETTET WERDEN MUSSTE, WEIL ES AM GERAET GEMESSEN WAR ──────
      Beides stand NUR in `titelSpringen`. Wer je wieder einen Sprung IN DAS
      LAUFENDE Werk baut, faengt hier an:

      1. EIN SPRUNG NACH DEM ANDEREN, Sperre 1500 ms (gemessen 2026-08-01).
         Zwei Spotify-Spruenge kurz hintereinander gehen schief: der zweite
         landete auf Titel 1, einmal sogar in einer voellig anderen
         Playlist. Mit Pause dazwischen stimmt jedes Ziel (1, 2, 6, 7, 8, 9,
         12 durchgemessen). Genau das tut ein Kind — es tippt nach, wenn
         nicht sofort etwas passiert. Ohne die Sperre ist die Titelauswahl
         also ausgerechnet fuer den wahrscheinlichsten Bediener kaputt.

      2. WER UEBER MPV LAEUFT, SPRINGT UEBER DIE LISTENPOSITION, absolut.
         `tracknr:N` (1-basiert) setzt im Abspieldienst `playlist-pos-1`;
         dass mpv das zur Laufzeit annimmt, ist gemessen
         (tools/mpv-eigenschaft.py). Der frueher relative Schritt rief
         `pt_step` — im mpv-Aufsatz eine Funktion, die eine Zeile nach
         stderr schreibt und NICHTS tut, waehrend der Dienst `{status:'ok'}`
         meldete. Und: JELLYFIN gehoert dazu, nicht nur `lokal` — siehe
         [[jellyfin-titelsprung-ueber-listenposition]].

      WAS BLEIBT: `laufendesWerk()` — die Karte war nur einer seiner
      Aufrufer. `stelleMerken` haengt weiter daran; ohne diese Antwort
      merkte sich die Box nach jedem Neuladen des Kiosk keine Stelle mehr.

      NACHGEMESSEN NACH DEM ENTFERNEN (Chromium 800x480 gegen
      tools/neu-vorschau.mjs): Startseite 12 von 12 Eintraegen, grosser
      Player oeffnet, Tipp aufs Cover zeigt das Bild formatfuellend,
      `gross-hinten` und `gh-symbol` sind nicht mehr im DOM, keine Meldung
      auf der Konsole.

  - kind: falle
    id: browser-pane-misst-den-alten-stand
    title: "Zweimal dieselbe URL im Browser-Pane misst den ALTEN Stand — obwohl der Server no-store schickt"
    sev: mittel
    match: "wird geladen …|Medien werden geladen|raster leer|kopf-unter"
    cause: "Erneutes `navigate` auf eine bereits geladene URL liefert die Seite aus dem Speicher des Panes; die frisch geschriebene app.js wird nicht geholt"
    fix: "Cache-Buster anhaengen (`/neu/index.html?frisch=1`) oder eine andere Adresse derselben Seite nehmen"
    tags: ['werkzeug', 'neue-oberflaeche', 'messen', 'falle', 2026]
    source: "selbst hineingetappt am 02.08.2026, mit einer zweiten Vorschau auf dem alten Stand gegengeprueft"
    body: |
      DAS BILD: Nach dem Entfernen der Titelkarte zeigte die Vorschau ein
      leeres Raster, `kopf-unter` blieb auf „wird geladen …", und im
      Netzwerkmitschnitt fehlte `/api/werke` vollstaendig — nur `/api/config`
      und `/api/profile` gingen hinaus. Das sieht exakt aus wie ein Anlauf,
      der an einer Ausnahme abbricht: eine Regression, die man gerade selbst
      gebaut hat.

      ES WAR KEINE. Dieselbe Vorschau, aufgerufen als
      `/neu/index.html?frisch=1`, zeigte sofort „12 von 12 Eintraegen" und
      alle Abrufe. Der Unterschied war allein die URL.

      NICHT DER SERVER: `tools/neu-vorschau.mjs` setzt auf JEDE Antwort
      `Cache-Control: no-store` (nachgesehen mit `curl -sI`, sowohl fuer
      `/neu/` als auch fuer `/neu/app.js`). Es ist der Pane, der bei
      `navigate` auf eine bereits offene URL nichts Neues holt.

      ── DIE PROBE, DIE ZEIT SPART ──────────────────────────────────────
      Bevor man die eigene Aenderung verdaechtigt: den Stand VOR der
      Aenderung danebenstellen und dasselbe messen.

          git archive HEAD NewDesign tools | tar -x -C /tmp/alt
          cd /tmp/alt && node tools/neu-vorschau.mjs --port 8300

      Zeigt der alte Stand dasselbe Bild, liegt es nicht an der Aenderung.
      Genau das war hier der Fall — und es kostete trotzdem eine halbe
      Stunde, weil erst danach der Cache-Buster ausprobiert wurde.

      VERWANDT, ABER ETWAS ANDERES: Attrappen luegen auch durch WEGLASSEN
      von Feldern. Hier war die Attrappe unschuldig.

  - kind: note
    id: backend-tests-echte-zahl-und-node-env
    title: "943 Backend-Tests — und der Integrationstest haengt nicht mehr, er braucht NODE_ENV=test"
    sev: info
    match: "auth.integration.spec|Promise resolution is still pending|tests 943"
    cause: "Die dokumentierte Zahl war ungefaehr und zu niedrig; die alte Haenger-Falle ist ueberholt"
    fix: "Gezaehlt: backend-api 943 Tests / 238 Suiten in 8 s, backend-player 93 / 21 in 2 s"
    tags: ['tests', 'backend', 'zahlen', 2026]
    source: "gezaehlt am 02.08.2026 auf dem Entwicklungsrechner, Stand d4081965"
    body: |
      DIE ECHTEN ZAHLEN (npm run test, alle gruen, 0 Fehler):
        backend-api      943 Tests, 238 Suiten,  8 s   (43 *.spec.ts)
        backend-player    93 Tests,  21 Suiten,  2 s

      Wer eine gerundete Zahl im Kopf hat, sieht einen echten Ausfall wie
      einen Rundungsfehler aus. Deshalb steht sie hier mit Datum und Stand.

      ── DIE ALTE FALLE GILT NICHT MEHR ─────────────────────────────────
      [[pruefen-sh-haengt-auf-auth-integration]] beschrieb, dass
      `src/backend-api/src/auth.integration.spec.ts` NIE endet und
      `tools/pruefen.sh` deshalb stehenbleibt. Am 02.08.2026 nachgemessen:
      „Kerntests backend-api" ist nach 8 s durch, der Gesamtlauf zaehlt 943
      bestandene Tests, und die Datei ist dabei.

      WAS MAN DABEI FALSCH MACHEN KANN: Die Datei EINZELN und ohne die
      Umgebungsvariable zu starten. Dann faellt sie um —

          npx tsx --test src/backend-api/src/auth.integration.spec.ts
          -> TypeError: Cannot read properties of undefined (reading 'address')

      weil der Server unter `NODE_ENV != test` gar nicht erst hochkommt und
      supertest ins Leere greift. Mit der Variablen, so wie es das
      npm-Skript tut, sind es 8 Tests in 3,9 s:

          npx cross-env NODE_ENV=test tsx --test src/backend-api/src/auth.integration.spec.ts

      Ein Fehlschlag beim Einzelaufruf ist also KEIN Befund, sondern ein
      Messfehler.

  - kind: entscheidung
    id: weiterhoeren-folgt-der-auswahl
    title: '„Weiterhoeren" zeigt nur, was zur Auswahl gehoert — dieselbe Liste wie das Raster'
    tags: [oberflaeche, neu, weiterhoeren, auswahl, regal, kategorie, 2026]
    source: "gemeldet und behoben 02.08.2026, gemessen mit tools/weiter-auswahl-schau.mjs"
    body: |
      DER SATZ DES BENUTZERS: „es soll nur die betreffenden titel anzeigen
      welche in der auswahl auch dabei sind bspw wenn ich europa hoerspiel bin
      sehe ich die zukunft wird gross was keinen bezug zu den hoerspielen hat.
      in der uebersicht oder in dem album oder betreffenden interpreten ist das
      ok"

      ── ERST MESSEN, DANN GLAUBEN ─────────────────────────────────────
      Die naheliegende Erklaerung war „die Reihe wird gar nicht gefiltert".
      Sie war FALSCH. `weiterZeichnen()` trug seit „Oft gehoert" die Zeile
      `if (werke.kategorie !== 'alle' || …) hidden = true`. Gemessen
      (tools/weiter-auswahl-schau.mjs, vor der Aenderung, Attrappe):

        Uebersicht (Alles)   alle 5 Stellen              richtig
        Kategorie Musik      REIHE GANZ WEG              zu viel
        Kategorie Hoerbuch   REIHE GANZ WEG              zu viel
        Regal „Kiddinx"      alle 5 Stellen              ZU WENIG  <- der Fehler
        Lane offen           alle 5 Stellen              unveraendert
        Interpretenseite     REIHE GANZ WEG              so gewollt

      ZWEI FEHLER AUF EINMAL, in entgegengesetzte Richtungen. Im REGAL eines
      Interpreten griff die Kategoriepruefung nicht, denn ein Regal laesst
      `werke.kategorie` auf „alle" stehen — genau der Zustand, den der
      Benutzer „europa hoerspiel" nennt. Und in einer gewaehlten Kategorie war
      sie zu grob: Sie blendete die Reihe komplett aus, ausgerechnet unter
      „Hörbuch", wo „wo war ich stehengeblieben?" am haeufigsten gefragt wird.

      ── DIE REGEL, UND WARUM ES KEINE ZWEITE WAHRHEIT GIBT ─────────────
      Die Reihe zeigt einen Eintrag, wenn SEIN WERK in der Liste steht, die
      das Raster gerade zeigt. Verglichen wird der `schluessel` — derselbe
      stabile Werkschluessel, nach dem auch die Kacheln gebaut werden. Er
      steht ohnehin in jeder Zeile, weil der Server ihn zum Starten
      heraussucht (`werkSchluesselKarte`), und eine Zeile OHNE ihn hat
      `weiterHolen` schon vorher aussortiert.

      Die Kategorie der ZEILE zu vergleichen waere der Fehler gewesen:
      `/api/weiterhoeren` traegt sie nicht mit, und sie mitzuschicken hiesse,
      dieselbe Zuordnung ein zweites Mal zu fuehren.

      In app.js:  `nachKategorie()` -> `nachRegal()` -> `auswahlSchluessel()`
                  -> `weiterZurAuswahl(liste, auswahl)`
      Dasselbe `nachRegal()` benutzt `anordnen()` fuer das Raster. EIN Filter,
      zwei Verbraucher.

      ── `null` HEISST „NICHT FILTERN", UND DAS IST DER WICHTIGE TEIL ───
      `auswahlSchluessel()` gibt in zwei Faellen `null` zurueck:

        UEBERSICHT (Kategorie „alle", kein Regal). Hier IST alles die Auswahl.
          Ein Mengenvergleich waere nicht nur ueberfluessig, sondern
          gefaehrlich: ein Eintrag, dessen Werk sich aus irgendeinem Grund
          nicht in `werke.liste` wiederfindet, verschwaende sonst UEBERALL —
          auch von der Startseite. Das waere schlimmer als der Fehler.
        MEDIENLISTE NOCH NICHT DA ODER KAPUTT. Die Reihe kommt aus einer
          ANDEREN Datei (resume.json) und ist oft schon da, waehrend
          /api/werke noch laedt oder gerade haengt. Wer dann filterte, naehme
          dem Kind den einen Weg, der noch funktioniert. Geprueft im e2e-Test
          („bleibt stehen, wenn die Medienliste ausfaellt", Zustand `kaputt`).

      ── DIE LANE IST KEINE ENGERE AUSWAHL ─────────────────────────────
      Eine aufgeklappte Playlist (Playlist -> Alben -> Titel) haengt IM Raster
      und ERWEITERT es; alle anderen Kacheln stehen weiter daneben. Wuerde die
      Reihe dabei auf dieses eine Werk zusammenschnurren, aendert sich der
      obere Bildschirmrand, obwohl unten nur etwas dazugekommen ist. Gemessen:
      das Raster zeigt vor und nach dem Aufklappen dieselben Werke.

      ── DIE DREI FRAGEN, DIE MITENTSCHIEDEN WERDEN MUSSTEN ────────────
        LEER -> GANZ WEG, nicht „leer mit Hinweis". Auf 480 px Hoehe kostet
          die Reihe eine von vier sichtbaren Zeilen; die fuer den Satz „hier
          ist nichts" auszugeben ist zu teuer.
        UEBERSCHRIFT BLEIBT „Weiterhören". Sie verspricht nichts mehr, was sie
          nicht haelt. Ein Zusatz („Weiterhören in Hörbuch") wiederholte nur,
          was die gedrueckte Kachel der Leiste und der Regalkopf schon sagen.
        INTERPRETENSEITE BLEIBT OHNE REIHE, obwohl der Benutzer sie unter den
          erlaubten Stellen nannte. „ok" heisst erlaubt, nicht verlangt.
          Entscheidend ist ein Grund, den es vorher nicht gab: gefiltert
          zeigte die Reihe dort genau seine Werke — und die stehen einen
          Zentimeter darunter schon als erste Reihe „In deiner Box".

      ── NEBENBEFUND, gleich mitbehoben ────────────────────────────────
      [[60-sekunden-takt-raeumt-die-lane-weg]] beschreibt, dass ein
      `zeichnen()` bei offener Lane die Sperre von `platzBeimBlaettern` stehen
      laesst. Der Takt war repariert, DREI weitere Stellen nicht:
      Kategoriewechsel, `regalOeffnen` und `regalZu` rufen alle `zeichnen()`.
      Sie rufen jetzt vorher `laneSchliessen()`.

      ── DIE ATTRAPPE LOG WIEDER DURCH WEGLASSEN ───────────────────────
      `tools/neu-vorschau.mjs` lieferte unter /api/weiterhoeren fuenf Zeilen —
      ALLE `audiobook`. Der gemeldete Fall (Musiktitel im Hoerspiel) konnte
      darin gar nicht entstehen; jede Filterregel haette dagegen richtig
      ausgesehen. Seit dem 02.08.2026 ist „Rolf Zuckowski" (music) dabei, und
      der e2e-Test haengt an genau dieser Zeile.
      Siehe [[attrappe-luegt-durch-weglassen]].

      NACHMESSEN:
        node tools/weiter-auswahl-schau.mjs            # Tabelle je Zustand
        node tools/weiter-auswahl-schau.mjs --pruefen  # in tools/pruefen.sh
      Die Erwartung rechnet das Werkzeug aus /api/werke und /api/weiterhoeren
      zweit — nicht aus app.js. Beim ersten Lauf zaehlte es die Kacheln einer
      per `hidden` ausgeblendeten Reihe mit und meldete Fehler, wo keine
      waren; die Reihe raeumt ihre Kacheln beim Ausblenden nicht weg. Und es
      fragte OHNE `?max=` — damit uebernahm die Erwartung stillschweigend
      denselben Deckel wie die Seite und konnte ihn nie melden; seit dem
      Gegenlesen fragt sie mit `?max=20`.

      ── NACHTRAG AUS DEM GEGENLESEN (02.08.2026) ──────────────────────
      Die Regel war richtig, ihre Reihenfolge nicht: Der Abrufdeckel stand
      VOR beiden Sieben, also fielen Zeilen weg, bevor gefiltert wurde —
      [[weiterhoeren-deckel-vor-dem-sieb]]. Und von den vier Stellen, die
      eine offene Lane wegwerfen konnten, waren erst drei versorgt —
      [[raster-neuaufbau-vierte-stelle]].

      ── NACHTRAG 03.08.2026: DIE REGEL HAT EINEN ZWEITEN VERBRAUCHER ──
      Die runde Interpreten-Reihe folgt seitdem derselben Auswahl. Dabei
      wurde `auswahlSchluessel()` in `auswahlWerke()` (der Filter) und zwei
      Abbildungen zerlegt; die Funktionen stehen jetzt bei `nachKategorie`/
      `nachRegal` statt hier im Weiterhoeren-Abschnitt. Die Frage „ein Werk
      oder alle?", die es hier nicht gab, ist dort entschieden:
      [[interpreten-folgen-der-auswahl]].

  - kind: falle
    id: klammerzaehler-liest-kommentare-mit
    title: 'dienst-marken-schau starb an einem „Radio" — der Klammerzaehler las Kommentare mit'
    sev: mittel
    match: "SyntaxError: Unexpected token '\\}'|markenAus|DIENST_MARKEN|anonymous_script"
    cause: "`markenAus()` in tools/dienst-marken-schau.mjs uebersprang Zeichenketten, aber KEINE Kommentare. Ein Kommentar im Bestand schliesst die deutsche Anfuehrung mit einem GERADEN \" — ab da galt alles als Zeichenkette."
    fix: "Zeilen- und Blockkommentare im Zaehler ueberspringen (erledigt 02.08.2026)."
    tags: [werkzeug, pruefen, oberflaeche, neu, falle, 2026]
    source: "aufgefallen beim Umbau der Weiterhoeren-Reihe, 02.08.2026"
    body: |
      DIESER EINTRAG BRAUCHT EINE FEHLERMELDUNG. Fehlt ein Dienst
      lautlos — keine Meldung, kein roter Test, er steht nur nirgends —,
      ist es der andere Eintrag mit `DIENST_MARKEN` im `match:`:
      [[dienstliste-an-fuenf-orten]]. Hier bricht ein Werkzeug hoerbar
      ab; die Falle ist nur, dass seine Meldung auf die falsche Datei
      zeigt.

      DIE MELDUNG ZEIGT AUF DIE FALSCHE DATEI:

        Dienst-Zeichen der Kacheln   FEHLER
          <anonymous_script>:3572
          SyntaxError: Unexpected token '}'
            at markenAus (tools/dienst-marken-schau.mjs:68)

      Man sucht daraufhin in `NewDesign/app.js` nach einer kaputten Klammer.
      Dort ist keine — `node --check NewDesign/app.js` sagt seit jeher ja.

      WAS WIRKLICH GESCHAH: `markenAus()` holt das Objektliteral
      `DIENST_MARKEN` per Klammerzaehlung aus app.js. Zeichenketten
      uebersprang es, Kommentare nicht. Mitten im Objekt steht

          // DIESELBE FIGUR WIE DER KATEGORIE-KNOPF „Radio" (KATEGORIEN, …)

      und die deutsche Anfuehrung wird dort mit einem GERADEN `"`
      geschlossen. Ab diesem Zeichen las der Zaehler alles als Zeichenkette,
      bis irgendwo weiter unten in app.js zufaellig wieder ein `"` kam.

      DASS ES JAHRELANG LIEF, WAR GLUECK. Die schliessende Klammer fiel
      zufaellig ausserhalb des verrutschten Bereichs. Ein paar Zeilen neuer
      KOMMENTAR an ganz anderer Stelle der Datei kippten das Glueck — die
      Schau brach, obwohl an ihrem Gegenstand nichts geaendert worden war.

      DIE LEHRE, die ueber dieses Werkzeug hinausgeht: Wer Quelltext per
      Klammer- oder Zeichenzaehlung ausschneidet, MUSS Kommentare mit
      ueberspringen. In diesem Bestand stehen lange deutsche Kommentare mit
      Anfuehrungen, Klammern und Apostrophen ueberall — sie sind hier keine
      Ausnahme, sondern die Regel. Betroffen waeren nach demselben Muster
      `tools/zeit-auszug.py` und jedes kuenftige Werkzeug dieser Bauart.

  # ══ Gegenlesen der Weiterhoeren-Auswahl (2026-08-02) ══════════════════════
    related: [dienstliste-an-fuenf-orten]

  - kind: falle
    id: weiterhoeren-deckel-vor-dem-sieb
    title: 'Der Abrufdeckel stand VOR den Sieben — weggeworfene Zeilen frassen den Platz der Kacheln'
    sev: mittel
    match: "WEITER_ANZAHL|weiterHolen|max=6|weiterZurAuswahl|slice\\(0, WEITER"
    cause: "`weiterHolen` fragte `/api/weiterhoeren?max=WEITER_ANZAHL` und schnitt sofort auf sechs. Beide Siebe kommen erst danach: `!e.schluessel` (Werk nicht mehr in der Bibliothek) und `weiterZurAuswahl` (Werk gehoert nicht zur Auswahl). Was der Server im Sechser-Fenster wegschnitt, konnte keines mehr zurueckholen."
    fix: "Mit `WEITER_HOLEN = 20` holen (Obergrenze des Servers) und ERST NACH `weiterZurAuswahl` auf `WEITER_ANZAHL` schneiden (behoben 02.08.2026)."
    tags: [falle, oberflaeche, neu, weiterhoeren, auswahl, 2026]
    source: "Gegenlesen von f33def5b, gemessen an der Box 192.168.178.169 am 02.08.2026 (nur gelesen)"
    body: |
      HIER FEHLEN KACHELN, HIER BEWEGT SICH NICHTS. Klappt dagegen eine
      offene Lane nach spaetestens einer Minute von SELBST zu, ist es der
      andere Eintrag mit `weiterHolen` im `match:`:
      [[60-sekunden-takt-raeumt-die-lane-weg]]. Beide sitzen im
      Weiterhoeren-Takt; die Frage, die trennt, ist, ob man zu wenig
      SIEHT oder ob einem etwas WEGGENOMMEN wird.

      DIE MESSUNG AN DER BOX (2026-08-02, ausschliesslich lesend):

        resume.json                    9 Stellen  (= die Obergrenze; mupibox.resume,
                                       Vorgabe 9, scripts/mupibox/remove_max_resume.sh)
        GET /api/weiterhoeren?max=6    6 Zeilen, davon 3 mit `schluessel`
        GET /api/weiterhoeren?max=20   7 Zeilen, davon 3 mit `schluessel`

      Die siebte Zeile erreichte die Seite also gar nicht. Vier der sieben
      Zeilen hatten dort ohnehin kein Werk mehr — sie belegten im
      Sechser-Fenster vier Plaetze, ohne je eine Kachel zu werden.

      ZWEI SIEBE STEHEN HINTER DEM DECKEL, und beide koennen nur wegnehmen:

        `!e.schluessel`      das Werk ist nicht mehr in der Bibliothek
                             (der Server liefert dann keinen Werkschluessel)
        `weiterZurAuswahl`   das Werk gehoert nicht zur gewaehlten Kategorie
                             bzw. nicht ins offene Regal — NEU seit
                             [[weiterhoeren-folgt-der-auswahl]]

      Vor dem Umbau fiel das kaum auf: In einer gewaehlten Kategorie war die
      Reihe ohnehin ganz weg. Seit sie dort etwas zeigt, ist der Deckel an
      der falschen Stelle sichtbar — und zwar als das Gegenteil dessen, was
      der Umbau wollte.

      DAS SZENARIO, das ein Kind trifft: Zwei Stellen sind zu „Kiddinx"
      gemerkt. Davor liegen drei Stellen, deren Werke inzwischen aus der
      Bibliothek genommen wurden. Im Regal „Kiddinx" steht dann EINE Kachel
      statt zwei — die zweite hat die Seite nie bekommen. Bei einer
      Kategorie mit wenigen Werken (Musik auf einer Hoerspiel-Box) faellt die
      Reihe auf dieselbe Weise ganz weg, obwohl die Kachel einen Zentimeter
      tiefer im Raster steht und die Stelle gemerkt ist.

      DIE ZWEITE RICHTUNG DESSELBEN FEHLERS ist der PROFILWECHSEL. `zuletzt`
      kommt aus `gespielt.json` des AKTIVEN Profils, `resume.json` ist
      box-weit. Stellen eines anderen Kindes bekommen deshalb `zuletzt: 0`
      und sortieren ganz nach hinten — sie fallen als erstes aus dem Fenster.
      An der Box am 02.08.2026 traf das 2 der 7 Zeilen.

      DIE REGEL: Ein Deckel gehoert ans ENDE einer Kette von Sieben, nie an
      den Anfang. Sonst zaehlt gegen den Platz, was gleich weggeworfen wird.

      EIN ZWEITER FEHLER KAM DABEI ANS LICHT: Solange dieser hier wirkte,
      wurde die Reihe nie voll — und deshalb fiel nie auf, dass die
      versprochenen sechs Kacheln bei 800 px gar nicht nebeneinander passen.
      Siehe [[weiterhoeren-sechste-kachel-passt-nicht]].

      NACHMESSEN:
        node tools/weiter-auswahl-schau.mjs   # Zeile „Uebersicht", „Regal Kiddinx"
        node --test tools/e2e/neu-oberflaeche.test.mjs
      Gegengeprueft mit ausgehebelter Regel: zwei e2e-Tests fallen dann um,
      und das Werkzeug meldet vier Abweichungen.
    related: [weiterhoeren-sechste-kachel-passt-nicht, 60-sekunden-takt-raeumt-die-lane-weg]

  - kind: falle
    id: raster-neuaufbau-vierte-stelle
    title: 'werkeHolen war die VIERTE Stelle, die eine offene Lane wegwarf — und die schnellste'
    sev: mittel
    match: "werkeHolen|TAKT_WERKE|platzBeimBlaettern|platz-machen|laneSchliessen"
    cause: "`werkeHolen()` ruft bei geaendertem Fingerabdruck `zeichnen()`, und das leert `#raster`, in dem die Lane haengt. `sperren(false)` steht ausschliesslich in `laneSchliessen()`."
    fix: "`laneSchliessen()` vor `zeichnen()` in `werkeHolen` (behoben 02.08.2026)."
    tags: [falle, oberflaeche, neu, lane, 2026]
    source: "Gegenlesen von f33def5b, 02.08.2026"
    body: |
      [[60-sekunden-takt-raeumt-die-lane-weg]] beschreibt die Falle, f33def5b
      hat DREI Aufrufer versorgt (Kategoriewechsel in `leisteBauen`,
      `regalOeffnen`, `regalZu`). Es sind VIER. `werkeHolen` fehlte — und es
      ist der schnellste Takt von allen: `TAKT_WERKE` ist 15 Sekunden,
      `TAKT_WEITER` 60.

      WAS EIN KIND MERKT: Ein Erwachsener speichert in der Verwaltung etwas,
      waehrend das Kind eine Playlist aufgeklappt hat. Innerhalb von 15
      Sekunden aendert sich der Fingerabdruck (`werkeStand` = Anzahl + mtime
      von activedata.json), `zeichnen()` leert das Raster, die Lane ist weg —
      und `platzBeimBlaettern.gesperrt` bleibt TRUE. Von da an faehrt die
      Kopfleiste beim Blaettern nicht mehr ein und nimmt dem Raster auf 480 px
      dauerhaft ihre Hoehe. Sie kommt erst zurueck, wenn das Kind irgendwann
      die naechste Lane oeffnet (dort steht `laneSchliessen()` in
      `albenOeffnen`) — bis dahin sieht es aus, als haette sich die Seite
      verklemmt.

      WARUM ES NIEMAND GEMESSEN HATTE: `tools/neu-vorschau.mjs` lieferte unter
      `/api/werke` den FESTEN Stand `vorschau-1`. Damit stieg `werkeHolen` in
      der Vorschau nach dem ersten Laden IMMER frueh aus, und der ganze Zweig
      „Stand geaendert -> Raster neu malen" war dort tot. Seit dem 02.08.2026
      traegt die Attrappe `vorschau-<anzahl>-<liste>-<cover>`, also einen
      Fingerabdruck, der sich mit der Antwort aendert — wie im Server.
      Wieder [[attrappe-luegt-durch-weglassen]].

      GEMESSEN WIRD DIE WIRKUNG, nicht das Feld: `gesperrt` liegt in der IIFE
      und ist von aussen nicht lesbar. `tools/weiter-auswahl-schau.mjs` oeffnet
      eine Lane, aendert die Medienliste, wartet auf den Neuaufbau, rollt und
      liest `document.body.classList.contains('platz-machen')`:

        Kopfleiste nach Neuaufbau  faehrt ein      (mit laneSchliessen)
        Kopfleiste nach Neuaufbau  BLEIBT STEHEN   (ohne — gegengeprueft)
    related: [60-sekunden-takt-raeumt-die-lane-weg, lane-sperre-klemmte-in-beide-richtungen]

  - kind: falle
    id: weiterhoeren-sechste-kachel-passt-nicht
    title: 'Sechs Weiterhoeren-Kacheln passen bei 800 px NICHT — es fehlt genau eine Luecke'
    sev: niedrig
    match: "WEITER_ANZAHL|weiter-reihe|weiter-kachel|overflow-x"
    cause: "`WEITER_ANZAHL = 6` trug die Begruendung „sechs passen bei 800 px nebeneinander“. 6*104 px Kachel + 5*14 px Luecke = 694 px gegen 680 px Platz."
    fix: "`WEITER_ANZAHL = 5` (5*104 + 4*14 = 576 px, 104 px uebrig). Nachgemessen in tools/weiter-auswahl-schau.mjs, Zeile „Volle Reihe bei 800 px“."
    tags: [falle, oberflaeche, neu, weiterhoeren, 800x480, 2026]
    source: "Gegenlesen von f33def5b, gemessen 02.08.2026 im Fenster 800x480"
    body: |
      GEMESSEN (Chrome headless, Fenster genau 800x480, Vorschau):

          #weiter-reihe clientWidth      680 px
          .weiter-kachel                 104 px
          gap                             14 px
          sechs   6*104 + 5*14 =         694 px   ->  14 px ZU BREIT
          fuenf   5*104 + 4*14 =         576 px   -> 104 px uebrig

      Es fehlt exakt EINE Luecke. `.weiter-reihe` hat `overflow-x: auto`, die
      sechste Kachel stand also zu rund 90 Prozent da und liess sich nicht ganz
      erreichen — ein waagerechter Rollbalken ist auf diesem Schirm kein
      Bedienelement, das ein Kind findet.

      WARUM ES NIEMAND SAH: Sechs Kacheln kamen praktisch nie zusammen. Der
      Abrufdeckel stand vor den Sieben ([[weiterhoeren-deckel-vor-dem-sieb]])
      und ass jedes Mal einen bis vier Plaetze; an der Box waren es am
      02.08.2026 drei startbare Zeilen von sieben. Die Behauptung im Kommentar
      wurde also nie eingeloest und nie widerlegt — sie wurde erst falsch
      SICHTBAR, als die Reihe zum ersten Mal wirklich voll wurde.

      DIE LEHRE: Eine Zahl mit einer Passt-Begruendung im Kommentar ist eine
      Behauptung ueber Bildpunkte. Sie gehoert gemessen, nicht geschaetzt — und
      die Messung gehoert in ein Werkzeug, sonst faellt die naechste Aenderung
      an Kachelbreite oder Luecke wieder still daneben.
    related: [weiterhoeren-deckel-vor-dem-sieb]

  - kind: entscheidung
    id: auswahl-umriss-an-der-kachel-nicht-am-bild
    title: 'Der Auswahl-Umriss gehoert um die KACHEL — und die rollende Reihe schnitt ihn ab'
    tags: [oberflaeche, neu, lane, auswahl, 800x480, 2026]
    source: "gemeldet und behoben 02.08.2026, gemessen mit tools/lane-marken-schau.mjs"
    body: |
      DER SATZ DES BENUTZERS: „bei auswahl von alben sieht man keinen
      kompletten auswahl umriss auch bei den titeln."

      ZWEI URSACHEN, und beide waren echt.

      ── 1. DER UMRISS HING AM BILD ────────────────────────────────────
      Die Regel lautete `.lane-kachel.auf .lane-bild { outline: … }` — er
      fasste also das Cover ein, nicht die Kachel. Die Beschriftung darunter
      („Der Wurzelwicht / 11 Titel") gehoert aber zur Auswahl und stand
      ausserhalb. Ein Umriss, der nur den halben Gegenstand einfasst, sagt
      nicht, WAS ausgewaehlt ist. Dasselbe galt fuer `.kachel.auf` im Raster
      und `.leute-kachel.auf` bei den Interpreten. Jetzt eine Regel fuer alle
      drei, um die ganze Kachel.

      ── 2. DIE REIHE BESCHNITT IHN ────────────────────────────────────
      `overflow-x: auto` beschneidet auf BEIDEN Achsen. Die Reihen hatten
      oben gar keinen Platz und unten 4 px; ein 3-px-Umriss mit 2 px Abstand
      braucht 5 px auf jeder Seite.

      GEMESSEN (Chromium headless, Fenster genau 800x480, Vorschau):

                                            vorher   nachher
        Album-Umriss oben abgeschnitten       5 px     0 px
        Titel-Umriss oben abgeschnitten       5 px     0 px
        Weiterhoeren-Ring oben abgeschnitten  4 px     0 px
        Luft ueber dem ersten Buehnenkind     2 px     8 px

      `overflow: visible` waere die naheliegende und FALSCHE Antwort: dann
      rollt die Reihe nicht mehr, und die hinteren Alben der Playlist sind
      nicht mehr erreichbar. Der Platz kommt deshalb aus `padding` und wird
      mit einem gleich grossen negativen `margin` wieder abgegeben.

      DASS DAS NICHTS VERSCHIEBT, IST GEMESSEN, NICHT BEHAUPTET:

        erste Kachel buendig bei        0 px  ->  0 px
        Platzbedarf der Album-Reihe   172 px  -> 168 px
        Platzbedarf der Titel-Reihe   132 px  -> 128 px
        Beruehrziel Album/Titel   118x168 / 96x128 px, unveraendert

      Die Reihen werden also um 4 px KUERZER (die alten 4 px `padding-bottom`
      gehen in den 7 px auf). Die Buehne bekommt 6 px oben dazu, damit eine
      Markierung in der ERSTEN Zeile ganz dasteht — netto bleibt auf 480 px
      Hoehe alles beim Alten.

      ── DIE FARBE IST NICHT DAS PROBLEM, ABER SIE IST SCHWACH ─────────
      Gerechnet (tools/kontrast.mjs): der warme Gelbton `--mp-akzent` hat
      gegen die cremefarbene Reihe nur 1,52 : 1. Die Farbe bleibt trotzdem —
      sie sagt, in welcher EBENE man ist ([[lanes-playlist-alben-titel]]),
      und eine zweite Bedeutung derselben Farbe waere schlimmer als ein
      schwacher Kontrast. Getragen wird der Umriss deshalb von einem duennen
      dunklen Saum daneben (`box-shadow` mit Streuung, im Dunkeln hell statt
      dunkel), nicht von der Farbe allein.

      ── DIE FALLE BEIM MESSEN ─────────────────────────────────────────
      Chrome meldet fuer `outline-style: none` trotzdem eine Breite
      (`medium` = 3px). Wer nur `outlineWidth` liest, findet an jeder Kachel
      einen Umriss und misst anschliessend Phantome. Und `box-shadow` ist
      nicht gleich Umriss: `.kachel-bild` traegt einen Schlagschatten
      (0 5px 0 0). Als Saum zaehlt nur ein Schatten OHNE Versatz.

      NACHMESSEN:
        node tools/lane-marken-schau.mjs --pruefen
        node tools/lane-marken-schau.mjs --bild /tmp/lane.png   # dazu ein Bild
        node tools/kontrast.mjs

  - kind: falle
    id: laufender-titel-war-nur-ein-aufleuchten
    title: 'Der laufende Titel war in der Lane nach 1,2 Sekunden nicht mehr zu erkennen'
    sev: mittel
    match: "lane-kachel.laeuft|classList.add\\('laeuft'\\)|setTimeout.*1200|lane-kachel.spielt"
    cause: "`laeuft` ist ein kurzes Aufleuchten nach dem Tippen, kein Zustand. Ein Zeitgeber nahm es nach 1,2 s wieder weg — danach sagte nichts mehr, welcher Titel laeuft. Dazu sahen `.auf` und `.laeuft` fast gleich aus (beide ein 3-px-Umriss)."
    fix: "Ein DRITTER Zustand `.spielt`, dauerhaft und aus der Wiedergabe-Auskunft gespeist; `.laeuft` wird zum Puls, damit es nicht mehr wie eine Auswahl aussieht (behoben 02.08.2026)."
    tags: [falle, oberflaeche, neu, lane, spotify, 2026]
    source: "gemeldet 02.08.2026, gemessen mit tools/lane-marken-schau.mjs"
    body: |
      DER SATZ DES BENUTZERS: „wenn ein titel spielt soll auch die auswahl in
      der titel 'liste' erhalten bleiben".

      ERST MESSEN, DANN GLAUBEN. Die naheliegende Erklaerung war „die Lane
      schliesst sich beim Starten" — sie war FALSCH. Gemessen (Vorschau,
      800x480, vor der Aenderung):

        Lane nach dem Starten          steht noch offen
        Titelliste nach dem Starten    steht noch offen
        Album noch markiert            ja
        dauerhaft „laeuft gerade"      NICHTS MARKIERT   <- das war es

      Die Kette der vier Neuaufbau-Stellen war also schon in Ordnung
      ([[60-sekunden-takt-raeumt-die-lane-weg]],
      [[raster-neuaufbau-vierte-stelle]]). Was fehlte, war ein Zustand.

      ── DREI DINGE, DIE NICHT GLEICH AUSSEHEN DUERFEN ─────────────────
        .auf     „das ist der aufgeklappte Eintrag"  -> UMRISS um die Kachel
        .spielt  „das laeuft gerade"                 -> MARKE im Bild, bleibt
        .laeuft  „der Tipp ist angekommen"           -> PULS, 0,6 s

      Vorher waren `.auf` und `.laeuft` beides ein 3-px-Umriss, nur in einer
      anderen Farbe — und der eine verschwand nach 1,2 Sekunden wieder. Ein
      Umriss fuer zwei verschiedene Dinge ist keine Auskunft.

      ── WELCHER TITEL LAEUFT? EIN BEWEIS, KEINE VERMUTUNG ─────────────
      Zwei Bedingungen muessen zusammenkommen — dieselbe Ueberlegung wie in
      `laufendesWerk()`:

        1. `context.uri` (Spotify) nennt DIESE Playlist. Die Kennung steht
           als `data-zusammenhang` an der Lane.
        2. `item.uri` nennt DIESES Stueck. Die Kennung steht als `data-uri`
           an der Titelkachel und als `data-uris` (alle Stuecke) an der
           Album-Kachel — ein Album gilt als laufend, sobald eines seiner
           Stuecke laeuft, auch ohne aufgeklappte Titelliste.

      NICHT UEBER DIE TITELNUMMER: `item.track_number` ist die Nummer im
      ALBUM des Stuecks, nicht die Position in der Playlist
      ([[weiterhoeren-playlist-position]]). Sie markierte eine Kachel — die
      falsche. `linked_from` hat Vorrang (Spotify tauscht in manchen Laendern
      Ausgaben aus).

      DIE GEGENPROBE GEHOERT DAZU: Laeuft etwas ANDERES (mpv, ein lokales
      Hoerbuch), darf in der Spotify-Lane keine Kachel behaupten, sie liefe.
      Gemessen: 0 markierte Kacheln. Ohne diese Probe prueft man nur, dass
      ueberhaupt etwas markiert wird — nicht, dass es das Richtige ist.

      DAFUER MUSSTE DIE ATTRAPPE ERST WAHRHEITSFAEHIG WERDEN: `/player/state`
      der Vorschau lieferte IMMER `item: null`, der ganze Spotify-Zweig war
      dort tot. Neue Lage `curl localhost:8299/vorschau/spotify`; sie meldet
      ABSICHTLICH einen anderen Titel (Versatz 6) als die gemerkte Stelle
      (Versatz 3) — nur so ist zu sehen, dass „laeuft gerade" und „hier geht
      es weiter" zwei Dinge sind. [[attrappe-luegt-durch-weglassen]]

      MARKE UND FARBE: die Marke sitzt oben LINKS im Bild (unten rechts liegt
      der Play-Knopf), Zeichen weiss auf `--accentDark`. Auf `--accent`
      (#FF6B57) traegt Weiss nur 2,80 : 1, auf #D9422F sind es 4,40 : 1
      (tools/kontrast.mjs). Die Beschriftung der Kachel faerbt mit — ein
      zweiter Traeger derselben Auskunft.

      NACHMESSEN:
        node tools/lane-marken-schau.mjs --pruefen

  - kind: entscheidung
    id: blauer-play-knopf-in-den-lanes
    title: 'Der blaue Play-Knopf in den Lanes — wo eine BRAUCHBARE Stelle liegt, und nur dort'
    tags: [oberflaeche, neu, lane, weiterhoeren, resume, backend, 2026]
    source: "lane-weiter.ts + /api/werke/<s>/alben + NewDesign/app.js, 02.08.2026"
    body: |
      DER WUNSCH: „falls zuvor ein titel gespielt wurde soll ein blauer
      playbuton auf dem titel escheinnen bzw auf dem album um an der stelle
      weiter zu spielen".

      DIE FARBREGEL WURDE NICHT ERFUNDEN, sondern durchgezogen — sie steht
      seit dem schwebenden Weiterhoeren-Fenster fest
      ([[schwebende-fenster-favoriten-resume]]). In den Lanes ist das
      Gegenstueck zu Blau NEUTRAL statt gruen, weil der Knopf dort auf dem
      Cover liegt; die Begruendung steht dort.

      ── DIE REGEL LIEGT IM BACKEND, NICHT IN app.js ───────────────────
      `src/backend-api/src/lane-weiter.ts`, geprueft in `lane-weiter.spec.ts`.
      In app.js laege sie hinter einem Netzabruf und waere nur am lebenden
      Geraet pruefbar — bei einer Regel mit diesen Randfaellen zu teuer. Der
      Server haengt das Ergebnis als `weiterAb` an jedes Album und jeden Titel
      von `GET /api/werke/<schluessel>/alben`; die Oberflaeche faerbt nur noch
      ein. KEIN zusaetzlicher Netzabruf je Kachel: die Stelle wird EINMAL
      gelesen, waehrend die Alben ohnehin zusammengesetzt werden.

      ── WAS EINE STELLE BRAUCHBAR MACHT ───────────────────────────────
      Vorgesiebt hat `weiterhoerbare()` — daran haengen `nummerVerlaesslich`
      (ein Playlist-Stand ohne `resumeVersatzGeprueft` traegt die ALBUM-Nummer
      und ist unbrauchbar, [[weiterhoeren-playlist-position]]), `istDurch` und
      die Mindestschwellen. Der Deckel dieser Funktion steht dabei auf
      `stellen.length` und NICHT auf 6: hier wird EINE Zeile gesucht, und ein
      Deckel gehoert ans Ende einer Kette von Sieben
      ([[weiterhoeren-deckel-vor-dem-sieb]]).

      Danach entscheidet lane-weiter.ts:

        TITEL     blau  <=>  versatz + 1 === titelNr  UND  positionMs > 0
        ALBUM     blau  <=>  titelNr - 1 steht in der Versatzliste des Albums
                             UND (es ist nicht dessen erster Titel ODER
                                  positionMs > 0)

      DIE ZWEITE BEDINGUNG IST DIE, DIE MAN VERGISST. Steht die Stelle bei
      0 ms auf dem ersten Titel, ist „weiterhoeren" Zeichen fuer Zeichen
      derselbe Befehl wie „von vorn". Ein blauer Knopf, der dasselbe tut wie
      der neutrale daneben, ist keine Auskunft, sondern eine Behauptung ohne
      Inhalt.

      DIE VERSAETZE WERDEN AUFGEZAEHLT, NICHT AUSGERECHNET. `ersterVersatz +
      anzahl` liegt daneben, sobald die Playlist eine Luecke hat:
      `titelEinesWerks` laesst gesperrte und entfernte Stuecke weg, zaehlt den
      Versatz aber weiter. Ein Album auf 10, 11, 13, 14 endet mit der Rechnung
      bei 13 — der letzte Titel fiele heraus, 12 kaeme faelschlich hinzu.

      ── ZWEI ZAEHLWEISEN AUF ENGSTEM RAUM ─────────────────────────────
        a.ersterVersatz / t.versatz   0-BASIERT. Die +1 macht `abspielBefehl`,
                                      und zwar als EINZIGE Stelle.
        weiterAb.titelNr              SCHON 1-BASIERT (aus resume.json), geht
                                      unveraendert hinaus — wie in
                                      `weiterSpielen`.
      Wer hier noch einmal addiert, landet einen Titel zu weit. Deshalb wird
      der Befehl GEMESSEN und nicht gelesen (tools/lane-marken-schau.mjs
      haengt sich an `window.fetch`):

        blauer Knopf (Album wie Titel)
          -> /player/current/spotify/now/spotify:playlist/<id>:4:92500
        Tipp auf die Titelkachel (Versatz 0)
          -> /player/current/spotify/now/spotify:playlist/<id>:1:0

      DIE STELLE GEHOERT IN DEN STARTBEFEHL, nicht in ein `seekpos:` danach —
      ein Sprung nach vorn wirkt bei laufender Spotify-Wiedergabe nicht
      ([[spotify-vorwaerts-spulen-wirkungslos]]).

      ── DER KNOPF ERSETZT, ER KOMMT NICHT DAZU ────────────────────────
      Zwei Knoepfe auf einer 118-px-Kachel waeren zwei Ziele, die sich
      beruehren. „Von vorn" bleibt ueber die Kachel selbst erreichbar. Auf
      TITEL-Kacheln gibt es ueberhaupt nur dann einen Knopf, wenn eine Stelle
      vorliegt: ein neutraler waere dort dasselbe wie der Tipp daneben.

      ZUR GROESSE — 44 px und nicht 64, mit Begruendung statt stillschweigend:
      Die 9-mm-Regel (ISO 9241-411) gilt der kleinsten Trefferflaeche fuer
      eine Handlung, die sonst nicht erreichbar ist. Dieser Knopf sitzt IN
      einer Kachel, die selbst 118x168 px misst, und dieselbe Stelle ist
      ausserdem ueber die Weiterhoeren-Reihe der Startseite erreichbar. Ein
      64-px-Kreis deckte auf einer 96-px-Titelkachel zwei Drittel des Covers
      zu. Schon der 44er verdeckte die grosse Titelnummer — die Kachel rueckt
      sie deshalb nach oben (`.mit-knopf`), und zwar nur die eine, die auch
      einen Knopf hat. Am Bild gesehen, nicht in den Zahlen.

      ── FARBE UND FORM ────────────────────────────────────────────────
      Gerechnet (tools/kontrast.mjs, 02.08.2026):

        weisses Zeichen auf #1B6EF3 (hell)      4,59 : 1
        weisses Zeichen auf #3B87FF (dunkel)    3,45 : 1
        blauer Knopf gegen helle Flaeche        4,32 : 1
        blauer Knopf gegen dunkle Flaeche       5,10 : 1
        blau <-> neutral, CIELAB      73 / Deuteranopie 84 / Protanopie 81
        blau <-> gruen (klassisch)   145 / 77 / 90

      Zwei Blautoene, weil ein Ton, der auf Creme genug traegt, auf #1A1726 zu
      dunkel wird. Und die Farbe traegt NICHT allein: das Zeichen ist ein
      Dreieck ueber einem angefangenen Balken (statt nur ein Dreieck), die
      Vorlesestimme hoert „Weiterhören" statt „Abspielen".

      ERSTER ENTWURF DES ZEICHENS WAR ZU FEIN: Balken bei y=18, 2,6 hoch —
      auf 20 px gerendert keine zwei Bildpunkte, am Bild nicht zu sehen. Die
      Form muss auf dem Schirm ankommen, nicht in der Zeichnung stimmen.

      ── WAS DIE ATTRAPPE DAFUER LERNEN MUSSTE ─────────────────────────
      `/api/werke/<s>/alben` der Vorschau lieferte weder `uri` noch
      `weiterAb`, und ihre gemerkten Stellen steckten in `/api/weiterhoeren`
      eingeschlossen. Zwei Antworten aus zwei Erfindungen sind nie
      widerspruchsfrei: Der blaue Knopf saesse auf einem Album, in dem gar
      keine Stelle liegt. Jetzt speisen sich beide aus derselben Liste
      (`WEITER_ROH`), so wie im Server beide aus resume.json kommen.
      Ausserdem fehlte der Schalter `alben-{voll|leer|angeschnitten}` ganz,
      obwohl `lage.alben` seit jeher ausgewertet wird.
      [[attrappe-luegt-durch-weglassen]]

      NACHMESSEN:
        node --test src/backend-api/src/lane-weiter.spec.ts   (im Backend: npm run test)
        node tools/lane-marken-schau.mjs --pruefen
        node tools/kontrast.mjs --pruefen

  - kind: falle
    id: lane-sperre-klemmte-in-beide-richtungen
    title: 'Die einfahrende Leiste klemmte in BEIDE Richtungen — schuld war eine gemerkte Sperre'
    sev: mittel
    match: "platzBeimBlaettern|gesperrt|sperren\\(|laneOffen|RUHE_MIT_LANE_MS"
    cause: "Ein gemerkter Schalter (`gesperrt`) statt einer abgeleiteten Frage. `sperren(true)` loeschte die laufende Uhr und stellte keine neue; `sperren(false)` stand allein in `laneSchliessen()`."
    fix: "Die Sperre ist weg. `laneOffen()` sieht im Baum nach (`document.querySelector('.lane')`) und WAEHLT NUR DIE RUHEZEIT. Eine einzige Stelle stellt die Uhr (`frist()`)."
    tags: [falle, oberflaeche, neu, lane, anordnung, 2026]
    source: "gemeldet 02.08.2026, behoben und gemessen 03.08.2026 mit tools/leiste-platz-schau.mjs"
    body: |
      ZWEI MELDUNGEN, die nach zwei Fehlern klingen, und es war einer:

        „wenn ich bis titel ausgewaehlt habe wird die leiste nie wieder
         eingeblendet"                                -> KLEMMTE VERSTECKT
        „es gibt auch den fall das es nicht ausgeblendet wird selbst wenn
         ich scolle und alles ausgeklappt ist"        -> KLEMMTE SICHTBAR

      ── WAS GENAU GESCHAH ─────────────────────────────────────────────
      VERSTECKT: Wer rollte (Leiste faehrt ein, Uhr laeuft) und INNERHALB der
      Ruhezeit eine Playlist aufklappte, verlor die Leiste dauerhaft.
      `sperren(true)` loeschte die laufende Uhr, liess `eingefahren` aber
      stehen — und `sperren(false)` stellte keine neue. Gemessen vor dem
      Umbau: „kommt zurueck nach: NIE (bis 9 s gewartet)".

      SICHTBAR: Umgekehrt tat mit offener Lane ein Rollen gar nichts mehr
      (`if (!this.an || this.gesperrt) return`). Wer sich durch Alben und
      Titel arbeitete, blaetterte auf 712 statt 800 px und auf 332 statt
      416 px Hoehe — dauerhaft.

      UND SIE LECKTE. Jeder Weg, der `raster.textContent = ''` ausfuehrt,
      warf die Lane weg, ohne `laneSchliessen()` zu rufen — dort allein stand
      `sperren(false)`. Vier Stellen waren nacheinander betroffen
      ([[60-sekunden-takt-raeumt-die-lane-weg]],
      [[raster-neuaufbau-vierte-stelle]]). Jede einzeln zu versorgen heisst,
      auf die naechste zu warten.

      ── DIE LEHRE, und sie ist groesser als dieser Fehler ──────────────
      EIN ZUSTAND, DER SICH AUS DEM ABLEITET, WAS WIRKLICH DASTEHT, KANN
      NICHT KLEMMEN. `laneOffen()` fragt den Baum (`.lane` gibt es genau
      dann, wenn eine Lane haengt — im Raster wie auf der Interpretenseite).
      Verschwindet die Lane auf irgendeinem Weg, ist die Antwort im selben
      Augenblick eine andere, ohne dass irgendjemand daran gedacht haben
      muss. Die fuenf `laneSchliessen()`-Aufrufe bleiben trotzdem stehen (sie
      raeumen den Umriss an der geoeffneten Kachel weg), aber ein Vergessen
      hinterlaesst keinen Klemmzustand mehr.

      ZWEITE HAELFTE DERSELBEN REGEL: Es gibt nur noch EINE Stelle, die die
      Uhr stellt (`frist()`), und sie loescht nie eine Uhr, ohne sofort eine
      neue zu stellen. Damit gibt es keinen Weg mehr in den Zustand
      „eingefahren, aber niemand holt es zurueck".

      ── EINE NEBENFALLE, die den ersten Testlauf gekostet hat ─────────
      `laneSchliessen()` rief `eigeneAenderung()` AUCH DANN, wenn gar keine
      Lane offen war — also 700 ms Taubheit gegen echte Fingerbewegungen.
      `werkeHolen` kommt alle 15 Sekunden hier vorbei; frueher oder spaeter
      faellt ein Rollen in dieses Fenster. Genau so ist der Test „Rollen
      faehrt die Leiste ein" gescheitert, ohne dass etwas kaputt war. Jetzt
      meldet `laneSchliessen()` nur, wenn wirklich etwas zu schliessen war.

      REIHENFOLGE ZAEHLT: `eigeneAenderung()` stellt die Uhr und fragt dabei
      `laneOffen()`. Sie gehoert deshalb NACH `raster.insertBefore(...)` und
      NACH dem Entfernen — sonst gilt die Frist der Lage von vorhin.

      ── NACHMESSEN ────────────────────────────────────────────────────
        node tools/leiste-platz-schau.mjs --pruefen
        node --test tools/e2e/neu-oberflaeche.test.mjs

      Gemessen 03.08.2026 (Chromium 800x480, Vorschau):

        Gegenprobe: Rollen faehrt ein                 ja
        Ruhezeit ohne Lane bis zurueck              1471 ms
        Lane auf, WAEHREND die Uhr laeuft: zurueck  2839 ms   (vorher NIE)
        mit offener Lane: faehrt Rollen ein           ja      (vorher nein)
        Ruhezeit MIT Lane bis zurueck               4053 ms

      Die Gegenprobe ist Pflicht: Ohne den Nachweis, dass ein gewoehnliches
      Rollen ueberhaupt einfaehrt, misst „bleibt eingefahren" das Nichts
      ([[attrappe-luegt-durch-weglassen]]).
    related: [60-sekunden-takt-raeumt-die-lane-weg, raster-neuaufbau-vierte-stelle]

  - kind: entscheidung
    id: ruhezeit-mit-offener-lane
    title: 'Vier Sekunden statt 1,4 — die Ruhezeit haengt davon ab, ob eine Lane offen steht'
    tags: [oberflaeche, neu, lane, anordnung, entscheidung, 2026]
    source: "NewDesign/app.js (RUHE_MIT_LANE_MS), gemessen 03.08.2026"
    body: |
      VORGESCHLAGEN VOM BENUTZER: „das villeicht koennen wir eine andere zeit
      waehlen wenn ausgeklappt ist." Sein Gedanke: Wer sich durch Alben und
      Titel arbeitet, blaettert laenger am Stueck.

      GEWAEHLT: 1400 ms ohne Lane, 4000 ms mit Lane. Es sind 4000 ms
      VOLLSTAENDIGER RUHE — jedes Rollen und jede eigene Aenderung (Lane auf,
      Lane zu, dritte Ebene) stellt die Uhr neu.

      ── WORAUF DIE ZAHL BERUHT ────────────────────────────────────────
      UNTERGRENZE, GEMESSEN: Eine Lane steht nicht sofort. Vom Antippen bis
      zum letzten Rollereignis vergehen in der Vorschau 862–870 ms bei 20
      Rollereignissen (tools/leiste-platz-schau.mjs, „Wie lange braucht eine
      Lane, bis sie steht?"). Auf der Box kommt `/api/werke/<s>/alben` dazu
      und beim ersten Mal die Cover — kalt bis 0,9 s je Bild
      ([[cover-nach-neustart-langsam]]).

      OBERGRENZE, BEGRUENDET: Ganz wegbleiben darf sie nicht, das war die
      andere Beschwerde. Ueber etwa zehn Sekunden haelt niemand mehr einen
      Zusammenhang zwischen der eigenen Handlung und dem, was der Schirm tut.

      NICHT GEMESSEN, und das gehoert hingeschrieben: wie lange ein Kind
      zwischen zwei Wischern in einer Album-Reihe innehaelt. Ohne dieses Mass
      ist jede Zahl in diesem Fenster geraten. Gewaehlt ist die Seite, auf der
      ein Fehlgriff billiger ist: Zu lang heisst, die Leiste kommt spaeter —
      waehrend man blaettert, will man sie ohnehin nicht. Zu kurz heisst, die
      Anordnung springt unter der Hand, und genau das wurde gemeldet.

      WARUM NICHT WIEDER SPERREN: siehe
      [[lane-sperre-klemmte-in-beide-richtungen]]. Eine laengere Zeit loest
      dasselbe Problem, ohne einen Zustand zu erzeugen, in dem etwas klemmen
      kann.

  - kind: entscheidung
    id: randwisch-holt-die-leiste-zurueck
    title: 'Wischen vom linken Rand holt die eingefahrene Leiste — und wie es sich vom Zurueckblaettern unterscheidet'
    tags: [oberflaeche, neu, geste, 800x480, anordnung, 2026]
    source: "NewDesign/app.js, gemessen 03.08.2026 mit tools/leiste-platz-schau.mjs"
    body: |
      GEWUENSCHT: „dann noch die wisch geste vom linken rad rein das es wieder
      einblenden schon wenn man es braucht." („rad" = Rand.)

      SIE IST EINE ABKUERZUNG, KEIN EINZIGER WEG. Die Ruhezeit bringt die
      Leiste ohnehin zurueck (1400 bzw. 4000 ms). Ein Kind, das die Geste
      nicht kennt, wartet und bekommt dasselbe — das war Bedingung.

      ── DAS SCHWIERIGE IST NICHT DAS HOLEN, SONDERN DAS UNTERSCHEIDEN ──
      Alle drei Ebenen der Lanes sind waagerecht rollende Reihen, und „nach
      rechts ziehen" heisst dort „zurueckblaettern". Waere die Geste damit
      dasselbe, schoebe jedes Zurueckblaettern die Leiste herein und alles um
      88 px zur Seite — mitten in der Bewegung. Zwei Bedingungen:

        1. STARTZONE: nur die aeussersten 40 px (5,6 mm bei 0,14 mm/px).
           Schmaler waere fuer einen Kinderfinger nicht zuverlaessig zu
           treffen, breiter reichte in die erste Kachel hinein.
        2. DIE REIHE DARUNTER STEHT SCHON GANZ LINKS (`scrollLeft <= 1`, nach
           oben durch die Vorfahren gesucht). Ist sie es nicht, will der Zug
           zurueckblaettern. Steht sie am Anfang, kann er nichts anderes
           bedeuten — es gibt nichts mehr, wohin zurueckzublaettern waere.

      Dazu 64 px Weg (dieselbe 9-mm-Marke wie fuer Beruehrziele, darunter
      waere es ein Wackeln beim Tippen) und hoechstens 48 px Schraeglauf.
      Einmal verworfen bleibt verworfen: Sonst zaehlte ein Bogen, der spaeter
      wieder waagerecht wird, doch noch als Wisch.

      ── KEIN STREIFEN IM BAUM ─────────────────────────────────────────
      Die Geste haengt an `document` mit `capture: true, passive: true` und
      ruft KEIN `preventDefault`. Sie HOERT nur zu. Ein unsichtbarer Streifen
      am Rand waere die naheliegende Bauart und die falsche: Er naehme den
      Reihen darunter die Beruehrung weg. `capture: true`, damit sie auch
      ankommt, wenn jemand unterwegs die Weitergabe anhaelt (die Play-Knoepfe
      auf den Kacheln tun das).

      ── SIE BRAUCHT EINE EIGENE BLINDE ZEIT ───────────────────────────
      Beim Wischen liegt der Finger NOCH auf dem Schirm und zieht weiter; die
      Reihe unter ihm rollt dabei. Ohne Schutz faehrt die Leiste im selben Zug
      wieder ein, die man gerade geholt hat. `holen()` setzt deshalb 900 ms
      blind (dieselbe Zahl wie das weiche Hereinholen einer Lane) — und
      `zeigen()` ueberschreibt `blindBis` nicht mehr, sondern nimmt das
      Maximum, sonst ginge die laengere Frist genau dort verloren.

      GEMESSEN (tools/leiste-platz-schau.mjs, 03.08.2026):

        Wisch vom Rand holt die Leiste                 ja
        Wisch aus der MITTE laesst sie weg             ja
        gerollte Reihe: Wisch am Rand laesst sie weg   ja

  - kind: entscheidung
    id: eingefahrenes-kissen-gibt-hoehe-frei
    title: 'Der eingefahrene Mini-Player gibt jetzt auch HOEHE frei — ueber einen negativen Aussenabstand'
    tags: [oberflaeche, neu, anordnung, 800x480, mini-player, 2026]
    source: "NewDesign/app.css, gemessen 03.08.2026 mit tools/leiste-platz-schau.mjs"
    body: |
      GEMELDET: „wenn der mini player verkleinert bleiben die seiten schwarz
      dann muesste man ja nicht verkleinern. es ist nur sinnig wenn es auch
      den platz freigibt geht das?"

      ER HATTE RECHT. Das Kissen liegt IM FLUSS (`flex: 0 0 auto`). Beim
      Einfahren schrumpfte es auf eine gerechnete Breite, seine ZEILE behielt
      aber die volle Hoehe: Die Buehne blieb bei 332 px, vorher wie nachher.
      Was er als schwarze Flaeche sah, war freigewordener Platz, den niemand
      benutzte. Der Player gab Breite ab, aber keine Hoehe — und Hoehe ist auf
      480 px das Knappe.

      ── DER WEG: NEGATIVER OBERER AUSSENABSTAND ──────────────────────
      `body.platz-machen .mp { margin-top: calc(-1 * (var(--mp-hoehe) + var(--mp-rand))); }`

      WARUM NICHT `position: absolute`: Ein Positionswechsel mitten in der
      Bewegung ist genau die Art Sprung, an der ein frueherer Anlauf
      gescheitert ist — `width: auto`, `align-self`, `justify-content` sind
      NICHT interpolierbar, gemeldet als „der Player zappelt"
      ([[platz-beim-blaettern-schaukelt-sich-auf]]). Ein Aussenabstand ist
      eine Laenge wie jede andere und blendet sauber ueber. NACHGEMESSEN:
      waehrend des Uebergangs 11 verschiedene Zwischenwerte (0 -16 -30 -44
      -55 -70 -75 -78 -81 -83 -84), es faehrt also, es springt nicht.

      WARUM OBEN UND NICHT UNTEN: Ein negativer UNTERER Abstand nimmt der
      Zeile zwar auch ihren Platz, verschiebt das Kissen aber nicht — die
      Buehne waechst nach unten, das Kissen rutscht mit und stuende ausserhalb
      des Schirms. Der obere zieht es an seinen alten Ort zurueck. GEMESSEN:
      Oberkante 396 -> 396 px, also unveraendert.

      ── DIE BUEHNE BEKOMMT DENSELBEN BETRAG ALS UNTEREN RAND ─────────
      Ohne das lag beim Rollen ans Ende die letzte Kachelreihe unter dem
      schwebenden Kissen (gemessen 74 px hoch ueber 314 px Breite — genau die
      Beschriftungen von drei Kacheln). Der Rand ist LEERE Flaeche am Ende der
      Liste, kein Platzverlust.

      UND ER MACHT DIE SACHE RUHIGER: Buehne und Inhalt wachsen um denselben
      Betrag, `scrollHeight - clientHeight` bleibt gleich. Wer ganz unten
      steht, dessen Rollstellung muss der Browser gar nicht erst berichtigen —
      und genau so eine Berichtigung ist es, die als Fingerbewegung ankommt
      und die Rueckkopplung ausloest. Das Blindfenster
      (BLIND_NACH_RUECKKEHR_MS) greift zusaetzlich. Gegengemessen: nach EINEM
      Rollen genau ein Wechsel EIN und ein Wechsel AUS in 6 Sekunden, kein
      Schaukeln.

      ── PLATZGEWINN (800x480, Vorschau, 03.08.2026) ──────────────────
        Buehne Breite    712 -> 800 px
        Buehne Hoehe     332 -> 416 px      (vorher: 332 -> 332)
        Kissen Breite    688 -> 326 px
        Kachel Hoehe     199 -> 221 px
        von der naechsten Reihe sichtbar   120 -> 182 px

      Eine ganze zusaetzliche Kachelreihe kommt dabei NICHT heraus (1 -> 1) —
      die Kacheln wachsen mit der Breite. Sichtbar wird mehr von der
      naechsten Reihe, und die angeschnittene Reihe ist das Zeichen, dass es
      weitergeht.

      ── DER VIERTE KNOPF: LAUTSTAERKE ────────────────────────────────
      Gewuenscht: „dann bauchen wir noch einen lautstaerke button analog zum
      mini player im mini mini player also wenn alles weggeklappt ist."
      Hier stand `display: none` mit der Begruendung, die Breitenrechnung
      stimme nur mit drei Knoepfen. Der Wunsch ist der bessere: Eingefahren
      ist genau der Zustand, in dem man laenger blaettert.

      ES IST DERSELBE KNOPF UND DASSELBE FENSTER, keine zweite
      Lautstaerkelogik — `#mp-laut` bleibt einfach stehen. Die Rechnung zaehlt
      jetzt vier: 4 Knoepfe + 5 Abstaende + Rand = 326 statt 252 px. Und die
      Knopfbreite wird nicht mehr mit der nackten 64 angenommen, sondern aus
      derselben Formel geholt wie `.mp-knopf` selbst (`--mupi-btn`): Wer in
      der Verwaltung „Tasten groesser" stellte, bekam sonst ein Kissen, das
      seinen eigenen Knoepfen zu schmal ist.

      NACHGEMESSEN, weil ein Knopf, der dasteht, noch keine Bedienung ist:
      alle vier 64x64 px und treffbar, der Fortschrittsring am Spiel-Knopf
      bleibt (er haengt an `.mp-haupt`, nicht an der Kissenbreite), und das
      Lautstaerke-Fenster geht auch eingefahren auf — 704 px, x 48..752 auf
      800 px Schirm, der Regler an BEIDEN Enden treffbar.

      DER SATZ, DER HIER STAND, WAR DIE FALLE — er lautete: „Es haengt an der
      MITTE des Kissens (`left: 50%`), und weil das Kissen mittig bleibt, steht
      es unveraendert." Der Nebensatz war eine VORAUSSETZUNG, die wie eine
      Feststellung klang. Zwei Stunden spaeter zog das eingefahrene Kissen nach
      rechts (b24cb1c1), und das Fenster ging mit — x 273..977, das laute Ende
      unerreichbar. Seit der Behebung haengt es `fixed` am SCHIRM und nicht
      mehr am Kissen; die 48..752 gelten jetzt in jedem Zustand. Ganze
      Geschichte: [[laut-fenster-hing-an-der-kissenmitte]].

  - kind: entscheidung
    id: album-gross-bedienung-an-den-rand
    title: 'Die grosse Albumansicht: die Bedienung gehoert an den Rand, das Cover ganz ins Bild'
    tags: [oberflaeche, neu, album, 800x480, barrierefreiheit, 2026]
    source: "NewDesign/app.css + index.html, gemessen 03.08.2026 mit tools/album-gross-schau.mjs"
    body: |
      DER WUNSCH KAM VON DER NUTZERIN, woertlich weitergegeben: „dann im der
      alben cover ansicht die bedien elemente muessen an den rand das cover
      muss ganz sichtbar sein sagt meine tochter."

      SIE HAT RECHT, und die Rechnung gibt ihr die Flaeche umsonst: Ein Cover
      ist QUADRATISCH, der Schirm ist 800 x 480. Formatfuellend wird das Bild
      464 x 464 und laesst links und rechts je 168 px voellig ungenutzt —
      waehrend Titel und Knoepfe unten AUF dem Bild lagen.

      DREI ANLAEUFE, die Reihenfolge lohnt sich zu kennen:
        1. Text und Knoepfe DARUNTER — dem Cover blieben 311 von 480 Punkten.
        2. Text und Knoepfe DARAUF — das Cover war voll da, aber unten
           verdeckt. Genau das wurde beanstandet.
        3. Text und Knoepfe DANEBEN, in den Randspalten, die ein
           quadratisches Cover ohnehin frei laesst.

      DAS BILD WIRD DADURCH KEINEN PUNKT KLEINER: 464 x 464 px, vorher wie
      nachher (gemessen). Es wird nur nichts mehr davon verdeckt.

      `--ag-rand: clamp(96px, 21vw, 168px)` — 168 px bei 800, und 800 - 2*168
      = 464 ist genau die Bildhoehe (480 - 2*8). Auf einem schmaleren Schirm
      schrumpfen die Spalten mit, bleiben aber ueber der Breite des groessten
      Knopfes (84 px).

      KEIN `display: grid` MEHR: Alle vier Stuecke sind absolut gesetzt — aus
      demselben Grund, aus dem `.ag-bild` es schon war (ein `<img>` ist ein
      ERSETZTES Element und holt sich im Raster seine natuerliche Groesse
      zurueck). Ein Raster darueber waere eine zweite Meinung ueber dieselben
      Koordinaten.

      WAS DAMIT WEGFAELLT: der dunkle Verlauf ueber der unteren Bildkante und
      der Schattenwurf an der Schrift. Beides waren Notbehelfe gegen einen
      Untergrund, den man nicht kennt; auf `--bg` traegt schlicht `--ink`.

      SENKRECHT, UND DIE REIHENFOLGE STEHT IM MARKUP: lauter / anhalten /
      leiser von oben nach unten — oben mehr, unten weniger, wie jeder Regler
      der Welt. Nicht als `column-reverse` im Blatt: Sonst laeuft die
      Vorlesereihenfolge der sichtbaren entgegen.

      DER TITEL UMBRICHT, statt mit „…" abzuschneiden. 144 px Textbreite sind
      fuer einen Serientitel eine Zeile zu wenig, und ein Kind, das gerade
      lesen lernt, kann mit einem abgeschnittenen Wort nichts anfangen.
      Zeilendeckel 6, nachgemessen: „Folge 3 — Die Maus" 1 Zeile, „Bibi
      Blocksberg — Die Kartoffelsuppe und der verhexte Besen (Folge 42)"
      (70 Zeichen) 4 Zeilen. Sechs Zeilen sind 120 px von den 394, die die
      Spalte unter dem Rueckweg-Knopf hat.

      GEMESSEN WIRD DIE BEMALTE FLAECHE, NICHT DER KASTEN: `object-fit:
      contain` heisst, dass das `<img>` groesser sein kann als das Bild darin.
      Wer das Element schneidet, findet Ueberlappungen, die es nicht gibt, und
      uebersieht die, die es gibt. tools/album-gross-schau.mjs rechnet die
      bemalte Flaeche aus dem Seitenverhaeltnis der Quelle.

      NACHMESSEN (hell UND dunkel, beide Male 0 px² Ueberlappung, Knoepfe
      64/64/84 px und getroffen):
        node tools/album-gross-schau.mjs --pruefen

  - kind: falle
    id: laneoffen-fand-die-versteckte-lane
    title: 'Die verwaiste Lane der Interpretenseite — die abgeleitete Frage las ein Element, das niemand mehr sieht'
    sev: mittel
    match: "laneOffen|querySelector\\('\\.lane'\\)|interpretSeiteZu|interpret-reihen|RUHE_MIT_LANE_MS"
    cause: "`laneOffen()` fragte `document.querySelector('.lane')`. `interpretSeiteZu()` setzt `#interpret` aber nur auf `hidden` und raeumt `#interpret-reihen` nicht — eine dort aufgeklappte Titelliste (`.lane .lane-tief`) bleibt im Baum stehen und wurde weiter mitgezaehlt."
    fix: "`laneOffen()` zaehlt nur Lanes, die im Bild liegen: `getClientRects().length` ist leer, sobald ein Vorfahr `display:none` traegt (behoben 03.08.2026)."
    tags: [falle, oberflaeche, neu, lane, anordnung, interpret, 2026]
    source: "Gegenlesen von 2cd28c69 und f685de83, gemessen 03.08.2026 mit tools/lane-nachlese.mjs"
    body: |
      [[lane-sperre-klemmte-in-beide-richtungen]] ersetzt einen GEMERKTEN
      Schalter durch eine abgeleitete Frage — richtig, und die beiden
      gemeldeten Klemmzustaende sind damit weg. Ein Rest desselben Fehlers
      blieb trotzdem stehen, und er ist lehrreich, weil er zeigt, WO eine
      abgeleitete Frage danebengreifen kann: nicht im Merken, sondern im
      LESEN.

      ── DAS SZENARIO, DAS EIN KIND TRIFFT ─────────────────────────────
      Es tippt auf einen Interpreten, klappt dort bei einem Album die
      Titelliste auf (`albumTitelOeffnen` legt `div.lane.lane-tief` in
      `#interpret-reihen` ab), und geht mit dem Pfeil zurueck auf die
      Startseite. `interpretSeiteZu()` setzt `#interpret` auf `hidden` — die
      Titelliste bleibt darin stehen, unsichtbar, aber im Baum.

      Von da an gilt auf der STARTSEITE die lange Ruhezeit, obwohl dort gar
      nichts aufgeklappt ist: Die Kopfleiste kommt nach dem Blaettern fast
      drei Sekunden zu spaet zurueck. Sie kommt zurueck — es klemmt nicht —
      aber sie fuehlt sich an, als haenge sie. Der Zustand haelt an, bis das
      Kind zufaellig eine andere Interpretenseite oeffnet; erst dort raeumt
      `reihen.textContent = ''` auf.

      GEMESSEN (tools/lane-nachlese.mjs, Chromium 800x480, Vorschau):

        Ruhezeit frisch geladen                       1470 ms
        Ruhezeit nach Interpretenseite auf/Titel/zu   4110 ms   <- der Fehler
        dieselbe Messung nach der Reparatur           1467 ms
        Ruhezeit MIT wirklich offener Lane            4058 ms   (unveraendert)

      Die letzte Zeile ist die Gegenprobe und gehoert dazu: Eine Reparatur,
      die einfach nie mehr „Lane offen" sagt, saehe in den ersten drei Zeilen
      genauso gut aus.

      ── DIE REPARATUR, UND WARUM NICHT DIE ANDERE ─────────────────────
      `laneOffen()` zaehlt jetzt nur, was im Bild liegt:

          for (const l of document.querySelectorAll('.lane'))
            if (l.getClientRects().length) return true

      `getClientRects()` ist LEER, sobald irgendein Vorfahr `display:none`
      traegt — genau das bewirkt `hidden`. Die Frage lautet damit nicht mehr
      „gibt es irgendwo ein solches Element", sondern „steht eines da", und
      das ist die Frage, um die es geht.

      DIE ANDERE REPARATUR WAERE `#interpret-reihen` BEIM VERLASSEN ZU
      LEEREN. Sie behebt genau diesen einen Weg und keinen zweiten. Es ist
      dieselbe Bauart, die schon einmal vier Anlaeufe gekostet hat
      ([[raster-neuaufbau-vierte-stelle]]): eine Aufraeumpflicht, die an jeder
      neuen Stelle wieder vergessen werden kann.

      ── DIE LEHRE ─────────────────────────────────────────────────────
      Ein abgeleiteter Zustand kann nicht klemmen — aber er kann die falsche
      Quelle ablesen. „Existiert im Baum" und „steht auf dem Schirm" sind
      zwei verschiedene Fragen, und eine Oberflaeche, die Seiten per `hidden`
      wechselt statt sie abzuraeumen, macht den Unterschied jedes Mal
      wirksam.

      NACHMESSEN:
        node tools/lane-nachlese.mjs --pruefen     # Abschnitt „Verwaiste Lane"
        node tools/leiste-platz-schau.mjs --pruefen # die Gegenprobe mit echter Lane

  - kind: architektur
    id: blauer-knopf-die-kette-ist-geprueft
    title: 'Der blaue Knopf: geprueft ist jetzt die KETTE, nicht nur ihr letztes Glied'
    tags: [oberflaeche, neu, lane, weiterhoeren, resume, backend, test, 2026]
    source: "Gegenlesen von 2cd28c69, src/backend-api/src/lane-weiter.spec.ts, 03.08.2026"
    body: |
      [[blauer-play-knopf-in-den-lanes]] beschreibt die Arbeitsteilung:
      `lane-weiter.ts` prueft AUSDRUECKLICH NICHT, ob die gemerkte Nummer die
      richtige Zaehlweise traegt — das tut `weiterhoerbare()` davor
      (`nummerVerlaesslich`, `istDurch`, die Mindestschwellen). Die Teilung
      ist richtig. Sie war nur an keiner Stelle GEPRUEFT: beide Seiten fuer
      sich gruen, und die Naht dazwischen ungedeckt.

      DAS IST DIE GEFAEHRLICHSTE STELLE DES GANZEN UMBAUS. Ein blauer Knopf
      auf einem Stand der ALTEN Zaehlweise springt in ein Kapitel, das das
      Kind nie gehoert hat — und das klingt nicht nach einem Fehler, sondern
      nach kaputt ([[resume-playlistposition-schickt-in-die-falsche-folge]]).
      Eine Naht, an der so etwas haengt, gehoert festgeschrieben, nicht
      erzaehlt.

      SECHS FAELLE STEHEN JETZT IN `lane-weiter.spec.ts` und fahren die Kette
      nach, die `laneWeiterZeile` in server.ts geht — ohne die zwei
      Dateizugriffe:

        ALTE ZAEHLWEISE      Playlist-Stand ohne `resumeVersatzGeprueft`
                             (track_number 3 = Album-Nummer, gemeint war
                             Kapitel 47): `weiterhoerbare` liefert GAR KEINE
                             Zeile, also faerbt nichts.
        GEPRUEFT             dieselbe Stelle mit der Marke und der 47: die
                             Zeile kommt durch, `stelleAmTitel(z, 46)` trifft,
                             der Nachbar und das Nachbaralbum bleiben neutral.
        DURCHGEHOERT         letzter Titel der Playlist zu 99 %: nichts wird
                             blau — sonst spielte der Knopf drei Sekunden und
                             waere fertig.
        KNAPP DAVOR          derselbe letzte Titel bei 50 %: wieder blau.
                             Ohne diese Gegenprobe waere der Fall darueber
                             auch dann gruen, wenn alles weggeworfen wuerde.
        ALBUM STATT PLAYLIST ein Spotify-ALBUM kommt OHNE die Marke durch —
                             dort waren beide Zaehlweisen immer gleich. Auch
                             das ist eine Gegenprobe: die erste Erwartung darf
                             nicht daran liegen, dass grundsaetzlich nichts
                             durchkommt.
        BENANNT, NICHT       ein ALBUM MITTEN in der Playlist, dessen letzter
        BEHOBEN              Titel zu 99 % durch ist, bleibt blau. `istDurch`
                             fragt nach dem WERK, nicht nach dem Album.

      ── DER LETZTE FALL, EHRLICH BENANNT ──────────────────────────────
      Titel 10 von 50 ist zu 99 % durch, die Playlist laengst nicht. Der
      blaue Knopf auf „Album 1" spielt dann ein paar Sekunden und geht in
      Album 2 ueber. Bei einer PLAYLIST ist genau das die richtige
      Fortsetzung — deshalb steht der Fall als Erwartung im Test und ist
      nicht abgestellt. Wer ihn spaeter aendert, aendert diese Erwartung
      bewusst und nicht aus Versehen.

      Anders waere es bei einem ALBUM als eigenem Werk; dort gibt es aber gar
      keine Lane (`/alben` antwortet mit `grund: 'keinePlaylist'`).

      ── WAS DIE KETTE SONST NOCH GARANTIERT, nachgesehen ──────────────
      KEIN NETZABRUF JE KACHEL, gegengelesen und bestaetigt: `/api/werke/<s>/alben`
      liest die Stelle EINMAL (`laneWeiterZeile`), und `titelOeffnen` baut die
      dritte Ebene aus `a.stuecke`, die schon im Speicher liegen — ein
      Aufklappen der Titelliste kostet null Abrufe. Dazu kommen zwei
      DATEIzugriffe je `/alben` (resume.json, gespielt.json) gegen bis zu
      zwanzig Spotify-Seiten im selben Aufruf.

      NACHMESSEN:
        cd src/backend-api && npm run test          # lane-weiter.spec.ts, 18 Faelle
        node tools/lane-marken-schau.mjs --pruefen  # der wirklich abgeschickte Befehl

  - kind: falle
    id: messwerkzeuge-hingen-nicht-an-pruefen-sh
    title: 'Fuenf Messwerkzeuge standen in den Commit-Nachrichten und in keinem Lauf'
    sev: mittel
    match: "pruefen.sh|GEPRUEFT:|--pruefen"
    cause: "Die Nachrichten zu 2cd28c69 und f685de83 fuehren „GEPRUEFT: lane-marken-schau --pruefen, kontrast --pruefen, leiste-platz-schau, album-gross-schau\" auf. Keines dieser Werkzeuge hing an tools/pruefen.sh — sie liefen einmal von Hand und danach nie wieder."
    fix: "Fuenf Schritte in tools/pruefen.sh ergaenzt (kontrast, lane-marken-schau, album-gross-schau, lane-nachlese, leiste-platz-schau), zusammen rund 170 s (03.08.2026)."
    tags: [falle, werkzeug, pruefung, prozess, 2026]
    source: "Gegenlesen von 2cd28c69 und f685de83, 03.08.2026"
    body: |
      DASSELBE WAR SCHON EINMAL SO: `tools/wiki-schau.py` sagte im Kopf seit
      jeher, es haenge an `pruefen.sh` — es hing nie, und beim ersten
      wirklichen Lauf meldete es sofort drei Verweise ins Leere. Die Sorte
      Aussage, die man glaubt, ohne sie nachzusehen.

      EIN WERKZEUG, DAS NUR SEIN ERBAUER STARTET, IST BEIM NAECHSTEN MAL
      KEINES. Es misst dann nicht mehr die Sache, sondern nur noch den Tag,
      an dem es geschrieben wurde. Genau die Fehler, die diese fuenf fangen,
      sind ausserdem die, die kein anderer Schritt sieht:

        kontrast            Farben, die nebeneinander liegen und trotzdem
                            dasselbe bedeuten koennten — blau/neutral/gelb,
                            zusaetzlich Deuteranopie und Protanopie.
        lane-marken-schau   Auswahl-Umriss, sein Beschnitt in der rollenden
                            Reihe und der WIRKLICH abgeschickte Abspielbefehl.
                            Die +1-Falle steht sonst nur im Kommentar.
        album-gross-schau   dass das grosse Cover unverdeckt bleibt —
                            gemessen wird die BEMALTE Flaeche, hell wie
                            dunkel, mit langem Serientitel.
        lane-nachlese       die Reste des Gegenlesens: verwaiste Lane, letzte
                            Kachel einer gerollten Reihe, Blaettern nach dem
                            Randwisch, vier Durchgaenge ein/aus.
        leiste-platz-schau  beide Klemmzustaende der Leiste, die Ruhezeiten,
                            der Hoehengewinn des Kissens.

      GEMESSENE LAUFZEIT (03.08.2026): kontrast 0 s, album-gross-schau 5 s,
      lane-marken-schau 18 s, lane-nachlese 47 s, leiste-platz-schau 97 s —
      zusammen 167 s. Zum Vergleich: „Alle Tests Box-Oberflaeche" braucht im
      selben Lauf 312 s. Der Preis ist also bezahlbar, und er faellt nur beim
      vollen Lauf an (`--schnell` bricht vorher ab).

      Alle fuenf melden sich ohne Browser AB, statt rot zu werden — dieselbe
      Regel wie bei den vorhandenen Browser-Schritten.

  - kind: entscheidung
    id: gegenlesen-lanes-was-nicht-kaputt-war
    title: 'Gegenlesen 03.08.2026: was gemessen und in Ordnung befunden wurde'
    tags: [oberflaeche, neu, lane, leiste, 800x480, abnahme, 2026]
    source: "tools/lane-nachlese.mjs, lane-marken-schau, leiste-platz-schau, album-gross-schau, kontrast — 03.08.2026"
    body: |
      EIN GEGENLESEN, DAS NUR DIE FUNDE AUFSCHREIBT, LAEDT DAZU EIN, DIESELBEN
      FRAGEN NOCH EINMAL ZU STELLEN. Deshalb steht hier, was ausdruecklich
      GEMESSEN und in Ordnung befunden wurde — mit den Zahlen, damit die
      naechste Aenderung sie brechen kann, statt sie stillschweigend
      mitzunehmen.

      ── DIE ROLLENDE REIHE UND IHR UMRISS ─────────────────────────────
      Die Sorge: Der Umriss-Platz kommt aus `padding` und geht als negatives
      `margin` wieder hinaus. Fuer die ERSTE Kachel war das nachgemessen, fuer
      die LETZTE nicht — und das rechte `padding` eines Rollbehaelters ist
      historisch die Stelle, an der Browser sich unterscheiden.

        Kacheln in der Album-Reihe          6
        Reihe rollt waagerecht              ja, 120 px Weg
        bis ans Ende gerollt                120 von 120 px
        rechts von der letzten Kachel frei  7 px  (= das volle `padding`)
        Seite/Buehne rollen waagerecht      nein  (der negative Abstand steht
                                                  nirgends ueber)

      Chromium haelt das rechte `padding` im Flex-Rollbehaelter also ein. Die
      Reihe blaettert weiter — `overflow: visible` waere die falsche Antwort
      gewesen und ist es nicht geworden.

      ── DIE WISCHGESTE NIMMT NICHTS WEG ───────────────────────────────
      Nach einem Wisch vom linken Rand rollt dieselbe Reihe unveraendert
      (120 von 120 px). Sie kann auch nichts wegnehmen: `passive: true`, kein
      `preventDefault`, kein Streifen im Baum. Und sie greift ohnehin nur,
      wenn die Reihe darunter schon ganz links steht — dort gibt es nichts
      mehr zurueckzublaettern.

      ── ES ZAPPELT NICHT, UND DAS RASTER SPRINGT NICHT ────────────────
      Der frueher gescheiterte Anlauf ([[platz-beim-blaettern-schaukelt-sich-auf]]) hing an einem
      Positionswechsel; jetzt ist es eine Laenge. Vier Durchgaenge
      hintereinander, jeder ein Rollen und ein Zurueckkommen:

        Takt                                   EIN aus EIN aus EIN aus EIN aus
        erste Kachel links/oben, Buehnenhoehe  104/445/332 — in ALLEN VIER
                                               Durchgaengen derselbe Wert

      Kein Durchgang blieb eingefahren, keiner fuhr zweimal ein. Die Anordnung
      steht danach auf dem Bildpunkt genau da, wo sie vorher stand.

      ── DIE DREI ZUSTAENDE SIND UNTERSCHEIDBAR ────────────────────────
      `.auf` (Umriss um die ganze Kachel), `.spielt` (Marke im Bild, bleibt
      stehen) und `.laeuft` (Puls, 0,6 s) benutzen drei verschiedene Mittel,
      nicht dieselbe Farbe in drei Toenen. CIELAB-Abstand blau gegen die
      Laeuft-Marke 121,7 (Deuteranopie 157, Protanopie 146), blau gegen den
      Auswahl-Umriss 145,3.

      ── DAS GROSSE COVER ──────────────────────────────────────────────
      0 px² Ueberlappung in hell UND dunkel, auch mit einem 70 Zeichen langen
      Serientitel (4 von 6 Zeilen). Gemessen wird die BEMALTE Flaeche, nicht
      der Kasten.

      ── KEINE ZUSAETZLICHEN NETZABRUFE ────────────────────────────────
      Siehe [[blauer-knopf-die-kette-ist-geprueft]]: `weiterAb` reitet auf
      `/api/werke/<s>/alben` mit, die dritte Ebene kommt ohne Abruf aus.

      NACHMESSEN — alle fuenf haengen seit dem 03.08.2026 an tools/pruefen.sh
      ([[messwerkzeuge-hingen-nicht-an-pruefen-sh]]):
        node tools/lane-nachlese.mjs --pruefen
        node tools/lane-marken-schau.mjs --pruefen
        node tools/leiste-platz-schau.mjs --pruefen
        node tools/album-gross-schau.mjs --pruefen
        node tools/kontrast.mjs --pruefen
  - kind: falle
    id: verschmelzung-legt-zu-viel-zusammen
    title: "Stufe „locker“ legt zu viel zusammen: Kategorie und fehlender Interpret"
    sev: high
    source: "tools/verschmelzung-grenzfaelle.mjs, gemessen beim Gegenlesen 2026-08-03"
    match: "Kachel weg|Hoerbuch fehlt|falsches Album|zwei Alben eine Kachel|verschmilzt zu viel"
    cause: "`gruppiereTreffer()` ist eine SUCHregel: sie vergleicht `kategorie` gar nicht, und `interpretenPassen()` gibt bei leerem Feld true. Beim Verschmelzen heisst das: verschiedene Abschnitte werden zusammengelegt, und ohne Interpret entscheidet der TITEL ALLEIN."
    fix: "`warumNicht()` in src/backend-api/src/abgleich.ts — sie uebergeht solche Gruppen und MELDET sie (`uebergangen: 'kategorie' | 'interpretFehlt'`); die Verwaltung zeigt sie jetzt an"
    tags: [verschmelzen, quellen, abgleich, medien, falle, 2026]
    body: |
      GEGENGELESEN AM 2026-08-03. Die Verschmelzung war eingeschaltet, gemessen
      (26 -> 24 Kacheln) und abschaltbar — aber gemessen war sie nur an den
      ECHTEN Daten der Box, und dort sind beide Ueberschneidungen richtig. Die
      teuren Faelle sind die KNAPP DANEBEN liegenden, und die kommen in einem
      Katalog mit 26 Eintraegen einfach noch nicht vor.

          npx tsx tools/verschmelzung-grenzfaelle.mjs --alle

      Neun Faelle, jeder sagt selbst, was er erwartet. ZWEI davon waren echte
      Ueberverschmelzungen:

        MUSIK GEGEN HOERBUCH
          spotify  „Der kleine Drache Kokosnuss" — Ingo Siegner  category=music
          jellyfin „Der kleine Drache Kokosnuss" — Ingo Siegner  category=audiobook
          -> EINE Kachel, Abschnitt `music` (der des FUEHRENDEN Werks).
          Die neue Oberflaeche siebt nach `kategorie`
          (app.js: `liste.filter(w => w.kategorie === werke.kategorie)`).
          FUER EIN KIND: die Kachel ist aus „Hoerbuch" VERSCHWUNDEN, ohne dass
          irgendwo etwas geloescht wurde. Es sucht sie, wo sie immer war.

        EIN INTERPRET FEHLT
          spotify  „Weihnachtslieder" — Rolf Zuckowski
          jellyfin „Weihnachtslieder" — (kein Interpret)
          -> EINE Kachel. `interpretenPassen()` gibt bei leerem Feld `true`
          („einer sagt nichts, dann trennt es auch nicht"), also blieb der
          TITEL ALLEIN als Beweis. Genau das verbietet [quellen-verschmelzen]:
          „NIE allein auf Name+Interpret verschmelzen" — auf den Namen allein
          erst recht nicht. Und es ist kein ausgedachter Fall: gerade den
          schlampig getaggten Dateien fehlt der Interpret, und gerade sie
          heissen „Weihnachtslieder", „Kinderlieder", „Folge 1", „Best Of".

      WARUM DIE BEIDEN REGELN NICHT IN `gruppiereTreffer` GEHOEREN: die ist fuer
      die SUCHE gebaut, und dort sind sie falsch — wer „HAMM" sucht, will beide
      Treffer nebeneinander, egal in welchem Abschnitt sie stehen. Sie stehen
      deshalb in `warumNicht()` (abgleich.ts), wo die Zusammenlegung entsteht.

      DIE KATEGORIE-REGEL IST KEINE NEUE ENTSCHEIDUNG, sondern dieselbe, die
      der Server beim doppelten Schluessel schon trifft: `/api/medien/doppelte/entfernen`
      fuehrt die beiden Zeilen ausdruecklich NICHT zusammen, weil `category`
      entscheidet, in welchem Abschnitt die Kachel steht — eine
      ELTERNENTSCHEIDUNG. Siehe [medienschluessel-kollision-gleiche-playlistid].

      DER PREIS IST NULL, gemessen: beide echten Paare der Box stehen in
      `music` und tragen einen Interpreten (data.json, 26 Eintraege, 2026-08-03).
      Es bleibt bei 26 -> 24.

      DREI FAELLE BLEIBEN OFFEN und sind im Werkzeug als `bekannt` markiert,
      jeder mit seiner Begruendung:

        Studio gegen Live, GLEICH beschriftet   Das IST die Grenze der Stufe
          „locker". Der Schutz waere `AbgleichOptionen.titelAnzahl` — die
          Andockstelle steht, gefuellt wird sie ab Stufe 2 (Fingerabdruck).
          Getragen wird es bis dahin vom Knopfdruck: es geschieht nichts von
          selbst, und „trennen" nimmt es zurueck.
        „Die drei ???" in „Die drei ??? Kids"   `interpretenPassen` nimmt
          `x.includes(y)`. Strukturell nicht von „Kapelle Petra" gegen
          „Kapelle Petra feat. …" zu unterscheiden, wo die Teilzeichenkette
          gewollt ist.
        „groß" gegen „gross"   trifft NIE — siehe
          [normal-macht-aus-scharfem-s-eine-luecke]. Behebbar nur mit einer
          Schluesselwanderung.

      NACHMESSEN: `npx tsx tools/verschmelzung-grenzfaelle.mjs` haengt seit dem
      2026-08-03 an tools/pruefen.sh. Rueckgabe 1 nur bei einer NEUEN
      Abweichung — bekannte zaehlen nicht, sonst sieht niemand mehr hin.

  - kind: falle
    id: verschmolzenes-album-bleibt-in-weiterhoeren
    title: "Verschmolzen ueber Jellyfin gespielt: das Album wird nie „durch“"
    sev: medium
    source: "src/backend-api/src/weiterhoeren.ts stelleAus, gegengelesen 2026-08-03"
    match: "Weiterhoeren geht nicht weg|Album schon durch|letzte Sekunden|resumeGesamtTitel"
    cause: "`stelleAus` nahm die Gesamtzahl im Spotify-Zweig NUR aus dem aufgeloesten Zusammenhang. Bei einer verschmolzenen Kachel meldet aber mpv, ohne `titelUri` — `zusammenhangFuer` gibt sofort null zurueck, und die von mpv gemeldete `totalTracks` fiel unter den Tisch."
    fix: "`const gesamtSpotify = gesamt || gemeldet` — der aufgeloeste Zusammenhang behaelt den Vorrang, die gemeldete Zahl ist der Rueckfall"
    tags: [verschmelzen, weiterhoeren, resume, quellen, falle, 2026]
    body: |
      DER FALL, GENAU: Eine verschmolzene Kachel traegt die Identitaet des
      SPOTIFY-Eintrags, gespielt wird aber ueber JELLYFIN. Auf der Box ist das
      der Regelfall und kein Sonderfall („Das Lumpenpack"). Dann meldet die
      Oberflaeche einen mpv-Stand:

          np.art === 'lokal'  ->  titelNr = mpv currentTracknr
                                  gesamt  = mpv totalTracks
                                  titelUri = ''            (mpv hat keine)

      Im Server loest `eintragZuSchluessel` den fuehrenden SPOTIFY-Eintrag auf,
      `zusammenhangFuer` sieht `uri === ''` und gibt SOFORT `null` zurueck
      (`if (!kennung || !uri) return null` — kein Netzabruf, das ist richtig so).
      Damit war `zus.gesamt` = 0, und der Spotify-Zweig von `stelleAus` schrieb
      `resumeGesamtTitel` gar nicht.

      WAS EIN KIND DAVON HAT: `istDurch` braucht die Gesamtzahl („Titel 12 zu
      99 %" ist ohne sie nicht von „mittendrin" zu unterscheiden). Ein zu Ende
      gehoertes Hoerspiel blieb also FUER IMMER in der Weiterhoeren-Reihe
      stehen, und ein Tipp darauf landete in den letzten drei Sekunden. Genau
      der Fehler, der am 02.08.2026 fuer den reinen mpv-Weg schon einmal
      behoben wurde — die Verschmelzung hat ihn durch die Hintertuer
      zurueckgeholt.

      DER RUECKFALL IST GEFAHRLOS: Bei echtem Spotify-Abspielen meldet die
      Oberflaeche ueberhaupt keine Gesamtzahl (`gesamt` nur bei
      `np.art === 'lokal'`), der Rueckfall greift dort also gar nicht erst.
      Und die Richtung des Irrtums ist harmlos — meldet mpv zu wenig, faellt
      ein Album zu frueh aus der Reihe und faengt ehrlich von vorn an; meldet
      es zu viel, bleibt alles wie bisher.

      GEPRUEFT: src/backend-api/src/weiterhoeren.spec.ts, drei Faelle unter
      „die Gesamtzahl bei mpv".

  - kind: falle
    id: veraltete-vorschau-meldet-falsches-rot
    title: "Eine seit gestern laufende Vorschau macht aus jeder Messung ein Rot"
    sev: medium
    source: "tools/verschmelzung-befehle.mjs, 2026-08-03"
    match: "keine verschmolzene Kachel|lief /vorschau/|Vorschau kennt nicht|Messung findet nichts"
    cause: "Die Messwerkzeuge fassen eine LAUFENDE Vorschau bewusst nicht an. Ist die aelter als die gemessene Neuerung, kennt sie deren `/vorschau/<lage>` nicht — nimmt den Pfad aber an: der `else`-Zweig am Ende der Kette legt jeden unbekannten Namen still auf `lage.spielt` und antwortet mit 200."
    fix: "nach dem Umlegen die Antwort PRUEFEN (`lage.verschmolzen === 'an'`) und die veraltete Vorschau benennen, statt einen Befund zu melden"
    tags: [werkzeug, vorschau, messen, falle, 2026]
    body: |
      AM 2026-08-03 PASSIERT, beim Gegenlesen der Verschmelzung: Auf dem
      Rechner lief seit dem Vorabend (23:59) eine `node tools/neu-vorschau.mjs`
      aus der Zeit VOR der Verschmelzung. `tools/verschmelzung-befehle.mjs`
      fasst eine laufende Vorschau absichtlich nicht an — sie koennte jemandem
      gehoeren, der gerade misst (dieselbe Regel wie in lane-marken-schau.mjs).
      Also legte es `/vorschau/verschmolzen-an` gegen einen Server um, der das
      Wort nicht kennt:

          curl /vorschau/lage  ->  {"lage":{... kein Feld `verschmolzen` ...}}
          /api/werke?verschmelzen=1  ->  12 Kacheln, 0 verschmolzen

      Das Werkzeug meldete daraufhin „keine verschmolzene Kachel" und endete
      mit 1. Ein ROT, das keines war — und seit es an tools/pruefen.sh haengt,
      waere es ein Fehlalarm im Gesamtlauf gewesen.

      ZWEI DINGE, DIE ES SO TUECKISCH MACHEN:
        * Die Vorschau antwortet auf JEDEN `/vorschau/<x>` mit 200 und `{lage}`.
          Der Statuscode sagt also nichts. (Nebenbei: `/vorschau/lage` setzt
          dabei `lage.spielt = 'lage'` — auch das Nachsehen veraendert etwas.)
        * Nichts an der Ausgabe deutet auf das Alter des Servers. Man sucht den
          Fehler in dem, was man gerade gebaut hat.

      DIE PROBE: die Antwort des Umlegens lesen und nachsehen, ob das Feld
      wirklich gesetzt wurde. Steht es nicht drin, ist die Vorschau aelter als
      die Sache, die gemessen werden soll.

      BEENDEN — das Muster aus Stuecken, sonst trifft es die eigene Zeile
      (siehe pkill-f-toetet-den-eigenen-befehl):

          pkill -f "neu""-vors""chau"

      NACH DEM NEUSTART lief dieselbe Messung sauber durch: 14 -> 12 Kacheln,
      beide verschmolzenen Kacheln mit zwei Plaketten, Tipp und Weiterhoeren
      an `jellyfin/…`, `seekpos:` in PROZENT, und das Ausweichen nach Spotify,
      wenn Jellyfin nicht antwortet.

  - id: next-verlaesst-die-playlist-nicht
    title: '„Beim Weiterschalten verlaesst die Box die Playlist" — nachgemessen: tut sie nicht'
    kind: knowhow
    match: "next verlaesst|context wechselt|andere Playlist|skipToNext|spielt-gerade geht nicht mit"
    tags: [spotify, player, next, messen, befund-widerlegt, 2026]
    source: "an der Box gemessen (192.168.178.169, 2026-08-03) mit tools/spotify-weiterschalten.py"
    body: |
      DER VERDACHT: Zur Meldung „beim Wechsel des Titels geht die Marke
      ‚spielt gerade' nicht mit" lag ein Befund vor, `/player/current/next`
      wechsle den ABSPIELZUSAMMENHANG auf eine andere Playlist:

        vorher   Intro: Das Eischneerodelfest  | spotify:playlist:2QqQ… (Pummeleinhorn)
        nachher  Hello Kitty Titelsong         | spotify:playlist:1DYD… (Hello Kitty)

      Daraus folgte die Deutung, die Marke sei im Recht und die Wiedergabe im
      Unrecht. BEIDES IST FALSCH — der Zusammenhang bleibt.

      GEMESSEN (2026-08-03, tools/spotify-weiterschalten.py, jeweils 3-4 s
      Wartezeit nach jedem Befehl, weil Spotifys Zustand nachhinkt):

        Playlist, 5x next hintereinander
          Intro -> Introsong -> Kapitel 01 -> 02 -> 03 -> 04
          zusammenhang bleibt spotify:playlist:2QqQ…            5 von 5
        Playlist, schneller Wechsel A->B und sofort next        bleibt
        Album (spotify:album:53tv…), 3x next                    bleibt
        Selbsttaetiger Titelwechsel (2-s-Takt mitgeschrieben)   bleibt
          10:51:35 Introsong  pos=56189 -> 10:51:53 Kapitel 01  pos=2205

      AM ENDE DER PLAYLIST (Titel 239 von 239): `next` springt auf Titel 1
      DERSELBEN Playlist und HAELT AN (`is_playing: false`, `progress_ms: 0`).
      Spotifys Nachlauf („Autoplay" auf fremde Inhalte) greift auf dieser Box
      also NICHT — bei `repeat_state: off` und `smart_shuffle: false` endet es
      in Stille, nicht bei einem fremden Hoerspiel. Wer also fuerchtet, ein
      Kind hoere ploetzlich etwas anderes: dieser Weg ist es nicht.

      WOHER DER FALSCHE BEFUND KAM: „Hello Kitty Titelsong" ist der ERSTE
      Titel der Hello-Kitty-Playlist. Genau dieser Zustand entsteht, wenn man
      `spotify/now/spotify:playlist:1DYD…:1:0` schickt — heute Zeichen fuer
      Zeichen nachgestellt. Ein `next` auf „Intro: Das Eischneerodelfest"
      liefert dagegen immer „Pummeleinhorn Introsong" (3 von 3). Die Ablesung
      stammte also von einem STARTBEFEHL derselben Messreihe, nicht vom
      `next`. Zwei Werkzeuge, die nacheinander an dieselbe Box sprechen,
      schreiben sich gegenseitig den Zustand um.

      LEHRE: Ein Zustand nach einem Befehl ist nur dann ein Beweis, wenn
      dazwischen NICHTS ANDERES an dieselbe Box gesprochen hat. Deshalb misst
      tools/spotify-weiterschalten.py vorher und nachher IN EINEM LAUF und
      liest zusaetzlich Spotifys eigene Warteschlange mit — sie zeigt vor dem
      Weiterschalten, was ueberhaupt als naechstes kommen kann.

      IM QUELLTEXT PASST ES DAZU: `next()` in spotify-control.ts ruft nichts
      als `spotifyApi.skipToNext()`; kein Zweig setzt dabei
      `currentMeta.activeSpotifyId` neu — das tut allein der Startbefehl
      (Zeile 1456). Die Stellen, die von sich aus `playMe()` nachschieben
      (Token-Erneuerung, „Device not found", NO_ACTIVE_DEVICE), spielen
      IMMER DIESELBE gemerkte Kennung. Ein Wechsel auf ein fremdes Werk ist
      dort nicht vorgesehen.

      WAS DAMIT OFFEN BLEIBT: die Meldung des Nutzers selbst. Der
      Abspieldienst liefert alles, was `laneLaufendMarkieren()` braucht, und
      zwar auch ueber die Durchreiche (`/player/state` auf 8200 traegt
      `context` und `item.uri`), und die Album-Zerlegung liefert zu JEDEM
      Stueck eine `uri` (239 Titel, 20 Alben, 0 ohne `uri`). Die Ursache muss
      also in der Oberflaeche liegen — nicht in der Wiedergabe.

  - kind: falle
    id: player-lag-unter-den-kachel-plaketten
    title: "Die Kachel-Beschriftungen flogen ueber den Mini-Player — z-index zu kurz gezaehlt"
    sev: mid
    match: "labels ueber dem player|player nicht ganz vorne|z-index mini-player"
    cause: >-
      `.mp` stand auf z-index 2 (gezaehlt wurden die Geschwister: Buehne und
      Kategorienleiste), aber die Kacheln tragen INNEN eigene Stufen —
      .kachel-marken 3, .kachel-fehlt 4
    fix: ".mp auf 5 — ueber allem auf der Buehne, unter dem grossen Player (6) und den Meldungen (8)"
    tags: [oberflaeche, neue-oberflaeche, z-index, mini-player, falle, 2026]
    body: |
      GEMELDET (2026-08-03): „die labels und bezeichnungen fliegen noch ueber
      den player der player ist nicht ganz forne".

      Die 2 war NICHT falsch gewaehlt, sie war zu kurz gedacht. Der Kommentar
      an der Stelle sagte „ueber der Buehne, unter der Kategorienleiste (die
      hat 3)" — gezaehlt wurden also die GESCHWISTER. Innerhalb der Kacheln
      liegen aber eigene Stufen:

        .mp             2
        .kachel-marken  3    (die Dienst-Plaketten, seit 02.08.)
        .kachel-fehlt   4

      Solange das Kissen NEBEN dem Inhalt lag, beruehrten sie sich nie. Seit
      es beim Einfahren Hoehe freigibt und ueber dem Inhalt SCHWEBT
      ([eingefahrenes-kissen-gibt-hoehe-frei]), liegen sie uebereinander — und
      die Plaketten gewannen.

      LEHRE: Wer ein Element zum Schweben bringt, muss die Stufen der Elemente
      zaehlen, UEBER die es schwebt — nicht die seiner Nachbarn. Die Frage ist
      nicht „was liegt neben mir", sondern „was liegt jetzt unter mir".

  - kind: entscheidung
    id: schalter-negativ-benennen-statt-regel-brechen
    title: "Ein Schalter, dessen Vorgabe AN ist — negativ benennen statt pruefen() aufweichen"
    tags: [darstellung, verwaltung, schalter, konvention, 2026]
    body: |
      GEWUENSCHT (2026-08-03): „kannst du den kleinen indikator es wird
      gespielt auf dem album cover animiert machen. (ausschaltbar im admin
      menue fuer schwaechere hardware)". Die Animation soll also LAUFEN, und
      man soll sie abschalten koennen.

      DAS STOESST GEGEN ZWEI HAUSREGELN AUF EINMAL:
        * `pruefen()` in darstellung.service.ts liest jedes Feld mit `=== true`
          und nicht `!== false` — damit macht kein fremder Wert ('ja', 1, {})
          versehentlich ein „an".
        * Ein neues Feld steht auf `false`, damit eine Box ihr Verhalten nicht
          durch ein Update aendert.

      Ein Feld `markeAnimiert: true` haette BEIDE gebrochen: es braeuchte
      `!== false`, und der Standard waere `true`.

      GELOEST DURCH BENENNUNG, nicht durch eine Ausnahme: Das Feld heisst
      `ruhigeMarke` — „die Marke stillstellen". Vorgabe `false` heisst dann
      „nichts stillgestellt", also Bewegung. `=== true` bleibt, der Standard
      bleibt `false`, und der Test in darstellung.service.spec.ts bewacht es
      wie die anderen.

      DER KNOPF IN DER VERWALTUNG SAGT TROTZDEM, WAS MAN SIEHT:
      „Spielt-Marke bewegt sich / steht still" — der Benutzer soll den
      Feldnamen nie lesen muessen.

      LEHRE: Wenn die gewuenschte Vorgabe gegen die Konvention laeuft, ist die
      Frage nicht „welche Regel biege ich", sondern „wie muss das Feld heissen,
      damit beide stimmen".

  - kind: entscheidung
    id: marke-tanzt-scaleY-nicht-hoehe
    title: "Die Spielt-Marke tanzt — mit scaleY, nicht mit height"
    tags: [oberflaeche, neue-oberflaeche, animation, pi, 2026]
    body: |
      Die Marke „spielt gerade" war schon immer ein Aussteuerungszeichen aus
      drei Balken (`laufendMarke()`), nur eingefroren. Sie tanzen zu lassen war
      keine neue Idee, sondern eine eingeloeste.

      BEWEGT WIRD MIT `transform: scaleY()`, NICHT mit `height` oder `y`:
      eine Hoehe zu aendern zwingt den Browser zu Layout UND Anstrich, und
      zwar fuer jedes sichtbare Cover, dauerhaft, auf einem Pi. Eine
      Transformation laeuft auf der Grafikeinheit und beruehrt das Layout
      nicht. `transform-box: fill-box` ist dabei Pflicht — ohne sie streckt
      sich jeder Balken um den URSPRUNG des SVG statt um seine eigene Mitte,
      und die Figur wandert aus dem Kreis heraus.

      DREI TAKTE, TEILERFREMD GENUG (760/520/900 ms, mit negativen
      Verzoegerungen): gleich lange Takte saehen aus wie ein Pulsieren, nicht
      wie Musik.

      DREI BREMSEN, in dieser Reihenfolge:
        1. `prefers-reduced-motion` — gilt IMMER, auch bei eingeschaltetem
           Schalter. Wer beides zusammenlegt, nimmt der einen Gruppe die Wahl
           und der anderen den Schutz.
        2. `body.ruhige-marke` — der Schalter aus der Verwaltung
           ([schalter-negativ-benennen-statt-regel-brechen]).
        3. Nur markierte Kacheln tragen die Marke; es laufen nie mehr als ein
           bis zwei Animationen gleichzeitig.

      Die Klasse haengt am `body`, nicht an den Marken: die entstehen und
      vergehen bei jedem Zustandstakt, und wer den Schalter an sie haengt, muss
      ihn bei jeder neuen Marke nachziehen und vergisst es.

      GEMESSEN (tools/marke-tanzt.mjs, Chromium 800x480, Balkenhoehe zweimal
      im Abstand von 250 ms):

        marke-bewegt   vorher [2.06, 8.06, 1.38]  nachher [3.12, 6.8, 1.45]
        marke-ruhig    vorher [3.5, 8.17, 1.46]   nachher [3.5, 8.17, 1.46]

      DAS WERKZEUG SETZT DIE MARKE SELBST EIN, statt sie zu erspielen — und
      das ist eine bewusste Grenze: Ob sie am richtigen Titel ERSCHEINT, misst
      lane-marken-schau.mjs gruendlicher. Der erste Anlauf wollte sie sich
      erspielen, fand keine (die Vorschau laesst nicht jedes Werk laufen) und
      haette „nichts gemessen" fuer ein Ergebnis gehalten.

  - kind: falle
    id: backticks-in-angular-vorlagen
    title: "Ein Backtick im Kommentar beendet die Angular-Vorlage"
    sev: low
    match: "TS1005|Argument expression expected|template literal"
    cause: "Angular-Vorlagen stehen in Template-Literalen; ein Backtick im HTML-Kommentar schliesst sie mitten im Markup"
    fix: "In Vorlagen KEINE Backticks — auch nicht in Kommentaren. Feldnamen ohne Auszeichnung schreiben."
    tags: [angular, verwaltung, falle, 2026]
    body: |
      Beim Einbauen des Schalters „Spielt-Marke" (2026-08-03) sollte ein
      HTML-Kommentar den Feldnamen in Backticks setzen, wie ueberall sonst im
      Projekt. Die Vorlage steht aber in einem Template-Literal — der erste
      Backtick beendete sie mitten im HTML:

        src/frontend-admin/src/app/seiten/darstellung.ts(508,44): error TS1005: ',' expected.
        src/frontend-admin/src/app/seiten/darstellung.ts(738,1): error TS1135: Argument expression expected.

      Die zweite Meldung zeigt auf das DATEIENDE, nicht auf die Ursache — man
      sucht also am falschen Ort.

      Verwandt und schon bekannt: die Regel, Commit-Nachrichten ueber eine
      DATEI zu schreiben (Backticks in `git commit -m` fuehrt die Shell aus —
      das steht im Gedaechtnis des Benutzers, nicht in diesem Paket, deshalb
      hier ohne Verweis) und der Klammerzaehler in
      [klammerzaehler-liest-kommentare-mit]. Dreimal dasselbe Zeichen, dreimal
      eine andere Ebene.

  - kind: falle
    id: drei-orte-eine-anzeige
    title: 'Das Cover-Vollbild zog beim Titelwechsel nichts nach — weil jeder der drei Orte seine eigenen Zuweisungen hatte'
    sev: mittel
    match: "ag-titel|ag-unter|albumGrossAuf|agMalen|data-anzeigeort|data-np|anzeigeMalen|Vollbild aktualisiert nicht"
    cause: "Mini-Player, grosser Player und Cover-Vollbild zeigen DASSELBE, hatten in NewDesign/app.js aber je eigene Zuweisungen. Zwei liefen im 2-Sekunden-Takt (mpMalen), die des Vollbilds nur beim Oeffnen (albumGrossAuf). Beim naechsten Titel blieben dort Titel, Interpret UND Cover des vorigen stehen."
    fix: "Die Orte stehen jetzt im HTML (data-anzeigeort + data-np), app.js liest sie aus dem Baum und malt sie in EINER Schleife (anzeigeMalen). Ein vierter Ort kann nicht mehr vergessen werden. Behoben 03.08.2026."
    tags: [falle, oberflaeche, neu, player, cover, anzeige, 2026]
    source: 'gemeldet 03.08.2026 (Meldung: auch im voll bild aktualiierte es die infromationen nicht), behoben und mit tools/e2e/neu-oberflaeche.test.mjs abgesichert'
    body: |
      `data-anzeigeort` STEHT IN ZWEI WEITEREN EINTRAEGEN, und die Frage,
      die trennt, ist: WAS IST KAPUTT?

          Ein Ort zeigt das FALSCHE (veraltete Titel/Cover)
            -> dieser Eintrag und der Zwilling unten: ein Laufzeitfehler
               der Oberflaeche, sichtbar am Geraet.
          Ein Ort ist RICHTIG gebaut, steht aber in keiner Doku
            -> [[oberflaeche-ohne-routentabelle-faellt-durch-jede-seitenwache]]:
               die Selbstverdrahtung, die HIER die Reparatur war, ist dort
               die Ursache des Doku-Drifts - ein Ort, der sich selbst
               anmeldet, zwingt niemanden, ihn aufzuschreiben.

      `anzeigeMalen` STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die
      trennt, stellt man VOR dem Suchen, mit offenem Vollbild und einem
      Titeluebergang:

          Bild UND Text bleiben beide stehen   -> DIESER Eintrag
                                                  (die Zuweisungen lagen
                                                  nur im Oeffnen-Weg)
          Das Bild wechselt, der NAME bleibt
          auf Folge 1                          -> [[titelname-wird-nachgeschlagen-wie-das-cover]]

      Der zweite Fall ist erst DURCH die Reparatur des ersten sichtbar
      geworden: solange nichts nachzog, war die Anzeige wenigstens in
      sich stimmig.

      DER SATZ DES BENUTZERS: „beim wechsel des titels geht die marke spielt
      gerade nicht mit. auch im voll bild aktualiierte es die infromationen
      nicht kannst du das checken". Der zweite Halbsatz ist dieser Eintrag;
      der erste ist [[next-verlaesst-die-playlist-nicht]].

      ── DER FEHLER ────────────────────────────────────────────────────
      `ag-titel` und `ag-unter` wurden an GENAU EINER Stelle gesetzt: in
      `albumGrossAuf()`, also beim OEFFNEN. `agMalen()` — das aus dem
      2-Sekunden-Takt kam — faerbte nur Symbol und Lautstaerke-Anschlaege.
      Das Cover (`ag-bild`) stand ebenfalls nur dort. Wer die Ansicht offen
      liess, sah beim naechsten Stueck weiterhin das vorige, Bild und Text.

      DER FEHLER WAR NICHT DIE VERGESSENE ZEILE. Es gab DREI Orte, die
      dasselbe anzeigen, und jeder hatte seine eigene Sammlung von
      Zuweisungen — neun an fuenf Elemente in `mpMalen`, drei weitere in
      `albumGrossAuf`. Kein Ort wusste vom anderen. Dass zwei nachgezogen
      wurden und einer nicht, faellt in so einer Anordnung niemandem auf; es
      faellt erst dem Kind auf, das die Ansicht offen laesst.

      DIESELBE Teilung gab es ein zweites Mal, unbemerkt: die
      Lautstaerke-Anschlaege. `laut.knoepfe()` schaltete `gr-leiser` und
      `gr-lauter`, die beiden des Vollbilds hingen an `agMalen` — zwei
      Schreiber fuer dieselbe Frage aus zwei Ecken der Datei.

      ── WIE ES REPARIERT IST ──────────────────────────────────────────
      DIE LISTE DER ORTE STEHT IM HTML, nicht mehr in app.js:

        data-anzeigeort="<Name>"   die Huelle (ihr `hidden` = sichtbar?)
        data-np="titel"            Titel des laufenden Stuecks
        data-np="unter"            Interpret/Album (darf fehlen — der
                                   Mini-Player hat keine Unterzeile)
        data-np="cover"            das <img>
        data-np="symbol"           der <path> fuer Spiel/Pause
        data-laut="leiser|lauter"  fuer die Anschlaege (eigene Quelle: der
                                   Lautstaerkewert, nicht der Titel)

      `anzeigeMalen(np)` in NewDesign/app.js laeuft ueber alles, was
      `anzeigeOrte()` im Baum findet — einmal gesucht, dann gemerkt. Ein
      VIERTER Ort bekommt dieselben Merkmale und wird mitgemalt, ohne dass in
      app.js eine Zeile dazukommt.

      DREI KLEINIGKEITEN, die dabei mitgingen und je fuer sich haetten
      beissen koennen:

        1. DIE MERKGROESSE GEGEN DAS BILDBLINKEN haengt jetzt am ELEMENT
           (`dataset.bildJetzt`), nicht mehr an einer gemeinsamen Variablen
           (`zustand.coverJetzt`). Mit einer gemeinsamen waere das Vollbild
           erneut leer geblieben: Ein Ort, der spaeter aufgeht, hat sein Bild
           noch nie gesetzt bekommen — die gemeinsame Groesse haette gesagt
           „diese Adresse steht doch schon".

        2. AUCH VERDECKTE ORTE WERDEN GEMALT. Nur Sichtbares zu malen spart
           Bruchteile einer Millisekunde und holt denselben Fehler zurueck:
           Wer das tut, muss beim Aufgehen einer Ansicht ans Nachziehen
           denken — und genau dieses Denken war der Fehler.

        3. ENDET DIE WIEDERGABE, gehen ALLE Huellen zu. Vorher stand dort
           `$('gross').hidden = true` und nur das; das Vollbild blieb mit
           einem formatfuellenden Cover eines Stuecks stehen, das laengst
           nicht mehr spielte.

      ── WAS NICHT ZUSAMMENGEFUEHRT WURDE, und warum ──────────────────
      DER FORTSCHRITT (`balkenMalen`) bleibt, wie er ist. Er hat das Problem
      nicht: Balken, Ring, Regler und Zeiten haben schon heute genau EINEN
      Schreiber, und die Orte zeigen ihn ABSICHTLICH verschieden — der
      Mini-Player einen Balken, der grosse Player einen ziehbaren Regler mit
      zwei Zeiten, das Vollbild gar nichts. Ein gemeinsames Schema haette
      dort mehr Ausnahmen als Faelle.

      ── DIE ZWEI TESTS ────────────────────────────────────────────────
      In tools/e2e/neu-oberflaeche.test.mjs, beide vor der Reparatur ROT:

        1. VERHALTEN, im Browser: beide Ansichten aufmachen, Titel wechseln,
           dann muessen ALLE SICHTBAREN Orte den neuen Titel, die neue
           Unterzeile und das neue Cover zeigen. Der Test zaehlt die Orte
           NICHT auf — er liest sie ueber `data-anzeigeort` aus dem Baum,
           sonst bewachte er wieder nur die drei bekannten. Gegen den leeren
           Durchlauf besteht er darauf, alle drei bekannten Huellen sichtbar
           zu finden.
           ROT VORHER: „Cover-Vollbild zeigt noch ‚Kapitel 7: Das
           Eischneerodelfest' statt ‚Kapitel 8: Der Schneemann taut'".

        2. QUELLTEXT, ohne Browser: KEINE Kennung eines `data-np`-Elements
           darf in app.js vorkommen. Der zentrale Maler findet sie ueber das
           Merkmal; steht eine Kennung doch dort, ist das der Anfang der
           naechsten zweiten Wahrheit. Der Test sagt es, BEVOR sie
           auseinanderlaeuft — der Verhaltenstest allein bliebe gruen,
           solange eine handverdrahtete Anzeige zufaellig stimmt.
           ROT VORHER: elf Kennungen.

      ── DIE ATTRAPPE KONNTE DEN FALL GAR NICHT STELLEN ────────────────
      Wieder [[attrappe-luegt-durch-weglassen]], diesmal fehlte kein Feld,
      sondern der ZEITLICHE VERLAUF: tools/neu-vorschau.mjs kannte nur EINEN
      Spotify-Zustand. Zwei aufeinanderfolgende `/player/state` mit
      verschiedenem `item.uri` liessen sich nicht stellen — wer „zieht die
      Anzeige beim Titelwechsel nach?" daran gemessen haette, haette nichts
      gemessen. Neu: `curl localhost:8299/vorschau/spotify-weiter` schaltet
      auf den naechsten Titel.

      UND: Den Titeln fehlte `item.album.images`. `coverAdresse()` liest bei
      Spotify ausschliesslich dieses Feld; ohne es faellt sie auf „was von
      hier aus gestartet wurde" zurueck, und DAS aendert sich beim
      Weiterschalten nie. Das Cover haette in der Vorschau also selbst dann
      stillgestanden, wenn die Oberflaeche alles richtig macht — eine Probe
      auf „das Cover geht mit" waere unbestehbar gewesen.

      NACHMESSEN:
        node --test tools/e2e/neu-oberflaeche.test.mjs
        node tools/neu-vorschau.mjs                       # dann im Browser
        curl localhost:8299/vorschau/spotify
        curl localhost:8299/vorschau/spotify-weiter

      GEAENDERTE DATEIEN: NewDesign/index.html (Merkmale), NewDesign/app.js
      (anzeigeOrte/anzeigeMalen/coverSetzen, agMalen entfaellt),
      tools/neu-vorschau.mjs, tools/e2e/neu-oberflaeche.test.mjs.
    related: [titelname-wird-nachgeschlagen-wie-das-cover, oberflaeche-ohne-routentabelle-faellt-durch-jede-seitenwache]

  - kind: knowhow
    id: marke-verlaesst-die-offene-titelliste
    title: 'Die Marke „spielt gerade" WANDERT — bis der naechste Titel zu einem anderen Album gehoert'
    match: "Marke geht nicht mit|laneLaufendMarkieren|spielt gerade verschwindet|Albumgrenze|lane-marke-wandert"
    tags: [oberflaeche, neu, lanes, marke, spotify, messen, 2026]
    source: 'beim Gegenlesen gemessen 03.08.2026 mit tools/lane-marke-wandert.mjs gegen tools/neu-vorschau.mjs'
    body: |
      DIE OFFENE FRAGE: [[next-verlaesst-die-playlist-nicht]] hat den Verdacht
      widerlegt, die Box verlasse beim Weiterschalten die Playlist — und den
      naechsten Schritt in den Browser verwiesen: „Die Ursache muss in der
      Oberflaeche liegen." Genau dort ist jetzt gemessen worden.

      GEMESSEN (03.08.2026, `node tools/lane-marke-wandert.mjs`, eigener
      Browser gegen die Vorschau, Playlist -> Album -> Titel aufgeklappt):

        Zustand                                markierte Titelkachel
        item.uri = spotify:track:alb0-6        „Kapitel 7"    (richtig)
        nach 1x weiter, alb0-7                 „Kapitel 8"    (richtig)
        nach 2x weiter, alb1-10                — KEINE —

      DIE MARKE WANDERT ALSO MIT — solange der naechste Titel im GEOEFFNETEN
      Album liegt. `laneLaufendMarkieren()` haengt an `dienstHolen()`, also am
      2-Sekunden-Takt; sie braucht dafuer keinen eigenen Abruf und keine
      Nachhilfe. Das ist der haeufige Fall, und er ist in Ordnung.

      ── WAS DER NUTZER VERMUTLICH GESEHEN HAT ─────────────────────────
      DER DRITTE SCHRITT IST DER INTERESSANTE. Laeuft der naechste Titel in
      einem ANDEREN Album derselben Playlist, steht seine Kachel gar nicht im
      Baum — die aufgeklappte Titelliste zeigt ja nur EIN Album. In der
      offenen Liste ist dann NICHTS mehr markiert. Die Marke ist nicht falsch:
      sie sitzt eine Ebene hoeher auf der Albumkachel („Der Wurzelwicht",
      ueber `data-uris`), und das ist wahr. Wer aber auf die Titelliste
      schaut — und die schaut man an, wenn man sie eben aufgeklappt hat —,
      sieht die Marke verschwinden. Das liest sich als „die Marke geht nicht
      mit".

      AN DER BOX TRIFFT DAS ALLE ZEHN BIS DREIZEHN TITEL ein, naemlich am
      Ende jedes Albums (gemessen: 239 Titel in 20 Alben, llmwiki
      [[lanes-playlist-alben-titel]]).

      NICHT REPARIERT, ABSICHTLICH: Der naheliegende Griff waere, die offene
      Titelliste selbsttaetig auf das neue Album umzuschalten. Das ist KEINE
      Kleinigkeit, sondern eine Entscheidung gegen die Navigation des
      Benutzers: Wer sich gerade ein anderes Album ansieht, waehrend etwas
      laeuft, bekaeme die Liste unter dem Finger weggezogen. Wer das baut,
      faengt hier an — und misst es mit demselben Werkzeug nach.

      DIE MESSUNG WAR ERST HEUTE MOEGLICH, und zweimal fehlte dafuer etwas an
      der Attrappe — beides [[attrappe-luegt-durch-weglassen]]:
        1. Die Vorschau kannte nur EINEN Spotify-Zustand (behoben mit
           `/vorschau/spotify-weiter`, siehe [[drei-orte-eine-anzeige]]).
        2. Und danach lagen BEIDE Titel im selben Album `alb0`. Damit liess
           sich der Fall, der oben steht, immer noch nicht stellen. Die
           Vorschau hat seit dem Gegenlesen einen DRITTEN Titel
           (`spotify:track:alb1-10`), der die Albumgrenze ueberschreitet —
           bei gleichbleibendem `context.uri`, so wie es an der Box gemessen
           ist.

      NACHMESSEN:
        node tools/lane-marke-wandert.mjs
        node tools/lane-marke-wandert.mjs --pruefen   # Ende 1 bei Abweichung

      NEBENBEI MITGEPRUEFT (tools/lane-marken-schau.mjs, am wirklich
      abgeschickten Aufruf, nicht am Quelltext): die +1-Falle stimmt weiter.
      Ein Tipp auf die erste Titelkachel schickt
      `spotify/now/spotify:…:1:0` (Versatz 0 + 1), der blaue
      Weiterhoeren-Knopf `…:4:92500` (titelNr aus resume.json, schon
      1-basiert, NICHT noch einmal erhoeht).

  - kind: falle
    id: autoplay-behauptung-reicht-weiter-als-die-messung
    title: '„Am Ende hoert ein Kind Stille" ist NICHT gemessen — gemessen ist nur `next` am Playlist-Ende'
    sev: mittel
    match: "Autoplay|Nachlauf|smart_shuffle|repeat off|Ende der Playlist|fremde Inhalte"
    cause: 'Die Messung vom 03.08.2026 hat EINEN Fall geprueft: ein MANUELLES `next` auf dem letzten Titel einer PLAYLIST. Daraus wurde der Satz „Spotifys Nachlauf auf fremde Inhalte greift auf dieser Box nicht".'
    fix: "Die Behauptung auf den gemessenen Fall zurueckschneiden. Der Fall, auf den es ankommt — Inhalt laeuft von SELBST aus, ohne dass jemand tippt —, ist ungemessen."
    tags: [falle, spotify, autoplay, messen, kinderschutz, 2026]
    source: 'beim Gegenlesen am 03.08.2026 aufgefallen; Gegenanzeige aus /state der Box, lesend'
    body: |
      „Nachlauf" STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die trennt:
      GEHT ES UM DIE BEHAUPTUNG ODER UM DIE BREMSE?

          Ein SATZ ueber Spotifys Autoplay soll gelten (Doku, Backlog,
          Wiki) — reicht die Messung so weit?   -> DIESER Eintrag
          Eine Kachel ist falsch markiert, waehrend der Nachlaeuft
          -> [[nachlauf-markierte-die-lane-aber-nicht-das-raster]]
             (die Bremse `jetztLaeuft().art === 'spotify'` stand nur in
             einer der beiden Antworten derselben Funktion)

      Der eine Eintrag prueft eine Aussage, der andere einen Code-Weg.
      Beide heissen nach demselben Verhalten von Spotify.

      WAS IN [[next-verlaesst-die-playlist-nicht]] STEHT: „Spotifys Nachlauf
      („Autoplay" auf fremde Inhalte) greift auf dieser Box also NICHT — bei
      `repeat_state: off` und `smart_shuffle: false` endet es in Stille, nicht
      bei einem fremden Hoerspiel."

      WAS DAFUER GEMESSEN WURDE: EIN Fall. Auf dem letzten Titel (239 von 239)
      einer PLAYLIST wurde ein `next` GESCHICKT; danach stand die Wiedergabe
      auf Titel 1 derselben Playlist und `is_playing: false`.

      DAS IST NICHT DERSELBE FALL. Ein manuelles `next` am Listenende ist etwas
      anderes als Inhalt, der von selbst ausklingt — und nur der zweite Fall
      ist der, der zaehlt: Ein Kind tippt am Ende nichts, es hoert einfach
      weiter zu. Spotifys Autoplay setzt genau dann ein.

      DIE GEGENANZEIGE, lesend geholt (03.08.2026, 11:34 Uhr, also 40 Minuten
      nach der Messreihe von 10:51-10:55):

        GET http://<box>:5005/state
          is_playing   true
          context      spotify:album:0fWSpghYzVJdPa8S9k5nmL
          item         „Ich will was werden - Teil 3"
          device       MixPiBox, volume_percent 100

      Der Zusammenhang ist ein DRITTES Album — weder die gemessene Playlist
      (`spotify:playlist:2QqQ…`) noch das gemessene Album
      (`spotify:album:53tv…`). Das ist KEIN Beweis fuer Autoplay: es kann
      genauso gut jemand im Haus gestartet haben, und die pm2-Protokolle
      schreiben keine Startbefehle mit (letzter Eintrag 29.07.). Es ist aber
      genau die Beobachtung, die die Behauptung nicht haben duerfte, wenn sie
      stimmt — und sie steht ungeprueft neben ihr.

      WIE MAN ES RICHTIG MESSEN WUERDE: einen KURZEN Zusammenhang (ein Album
      mit wenigen Titeln, oder ab dem vorletzten Titel starten) ausklingen
      lassen, ohne einen einzigen Befehl zu schicken, und danach `context.uri`
      lesen. Kein `next`. Genau das Nicht-Tippen ist der Fall.

      UND DER ZWEITE TEIL DERSELBEN SACHE: Die Box lief nach der Messreihe
      40 Minuten unbeaufsichtigt weiter, bei Lautstaerke 100.
      tools/spotify-weiterschalten.py hat `--start` und `--weiter`, aber
      nichts, was den Zustand danach zuruecknimmt. Wer misst, indem er
      abspielt, muss auch aufhoeren koennen — sonst ist die naechste Messung
      an derselben Box schon verdorben, und das ist genau der Fehler, den
      dieser Eintrag im Nachbareintrag beschreibt („zwei Werkzeuge, die
      nacheinander an dieselbe Box sprechen").
    related: [nachlauf-markierte-die-lane-aber-nicht-das-raster]

  - kind: architektur
    id: interpreten-freischalten-ablage
    title: "Interpreten freischalten und ablehnen — die Erkennung zieht ins Backend, die Entscheidung in eine eigene Datei"
    tags: [interpret, backend, ablage, spotify, erkennung, verwaltung, neu, 2026]
    source: "Gebaut 2026-08-03; gemessen mit tools/interpreten-erkennung.mjs gegen Box 192.168.178.169 (nur lesend)"
    body: |
      DER WUNSCH: „erkannte artisten freischalten koennen ... das man die
      discografie insgesammt anzeigt. und eine Artist suche." Ein per Suche
      gefundener Interpret steht danach in der runden Reihe der Box, AUCH wenn
      kein einziges seiner Alben in der Bibliothek liegt.

      == DIE MESSUNG, die die Regel geaendert hat ==
      `npx tsx tools/interpreten-erkennung.mjs` (neu, nur lesend: ssh auf
      data.json plus je Name ein GET auf /api/spotify/web/search der Box).
      AM 2026-08-03:
        26 Eintraege -> 26 Werke -> 16 Interpretennamen
        erkannt  9   EMMA6, Kapelle Petra, Lichterkinder, Das Pummeleinhorn,
                     Red Hot Chili Peppers, Das Lumpenpack, Kleine Prinzessin,
                     Prinzessin Lillifee, Der kleine Major Tom
        nicht    7   EUROPA Hörspiele & Kinderlieder, kidsclubedutainment,
                     lismio: Kids - Hörbücher & Musik, matze.sp.hh, jasche-98,
                     𝓛𝓮𝓸𝓷𝓲𝓮♡, 🩵Jojo 🩵

      DER ERSTE ENTWURF WAR DAMIT WIDERLEGT. Er verglich die Namen mit
      `normal()` (medien.ts) — mit dem plausiblen Argument, das sei doch
      derselbe Schluessel, unter dem `werke.ts` die Werke eines Interpreten
      gruppiert, also eine Regel weniger. `normal()` wirft aber ALLES weg, was
      kein Buchstabe und keine Ziffer ist:

          normal("🩵Jojo 🩵")  ->  "jojo"      und Spotify hat eine Saengerin
          normal("JoJo")       ->  "jojo"      dieses Namens.

      Gemessen: `normal()` gewann auf dieser Bibliothek KEINEN richtigen
      Treffer dazu und genau EINEN falschen — ausgerechnet „Jojo", den Fall,
      der dieses ganze Feature ausgeloest hat. Ohne die Messung waere die
      Regel „aufgeraeumter" gewesen und schlechter.

      DIE LEHRE, allgemeiner: Gruppierung und Erkennung sind ZWEI FRAGEN und
      duerfen nicht denselben Vergleich benutzen. „Gehoeren diese Werke
      zusammen?" darf grosszuegig sein (Preis eines Fehlers: ein Regal zu
      wenig). „Ist dieser Name ein Interpret bei Spotify?" darf es nicht
      (Preis: eine fremde Person im Regal des Kindes). Dieselbe Verwechslung
      beschreibt [regal-je-interpret-gegen-verschmelzen] an anderer Stelle.
      `namensSchluessel` (interpreten.ts) ist deshalb NFKC +
      Kleinschreibung + Leerzeichen zusammenfassen — und entfernt KEIN Zeichen.

      == WAS SICH GEGENUEBER app.js SONST NOCH AENDERT ==
      OHNE KENNUNG KEIN JA. `istInterpret` gab bei einem Treffer ohne
      brauchbare `id` `{ja:true, id:''}` zurueck; die Kachel stand in der Reihe
      und wies sich beim Antippen selbst ab („Ohne Kennung gibt es keine
      Seite"). Fuer ein Kind, das noch nicht liest, sieht das aus wie eine
      kaputte Box.
      Sonst urteilen alte und neue Regel auf dieser Bibliothek GLEICH — der
      Gewinn liegt in der Ablage, nicht im Vergleich.

      == WARUM DIE ERKENNUNG INS BACKEND GEHOERT ==
      Dieselbe Begruendung wie bei der Album-Zerlegung. `istInterpret` merkte
      sich sein Urteil im sessionStorage (`mupibox_neu_interpreten_v1`) — der
      endet mit dem Tab. Folgen, alle drei gemeldet:
        1. Nach jedem Neustart 16 Spotify-Abrufe, bevor die erste runde Kachel
           steht.
        2. Der Tabletbrowser der Eltern hatte eine eigene Meinung.
        3. Die Entscheidung war nicht zu ueberstimmen — „Jojo" kam wieder.
      Serverseitig liegt sie jetzt in EINER Karte (12 h Frist, nur im
      Arbeitsspeicher). Ein AUSFALL wird NICHT gemerkt, sonst haelt ein
      WLAN-Aussetzer von einer Sekunde zwoelf Stunden.

      == DIE ABLAGE: config/interpreten.json ==
        { "frei":      [{schluessel, id, name, quelle, bild?, seit?}],
          "abgelehnt": [{schluessel, name, seit?}] }
      NEBEN data.json, Bauart wie config/verschmelzung.json, atomar
      geschrieben (daneben + rename im GLEICHEN Verzeichnis). Drei Gruende wie
      dort: Umkehrbarkeit, von Hand korrigierbar, braucht die dataLock-Sperre
      nicht. `interpretenAblageAus` wirft einzelne kaputte ZEILEN weg statt an
      ihnen zu scheitern und BILDET DEN SCHLUESSEL NEU, statt ihn zu glauben —
      stuende dort von Hand ein anderer, faende die Reihe die Werke nicht mehr.
      Eine Ablehnung darf auch als blosser String dastehen; so schreibt man sie
      von Hand hin.

      DAS ABLEHNEN IST DER HALBE PUNKT. Die Automatik urteilt bei jedem Start
      neu und kommt jedes Mal zum selben Schluss; ein Wegklicken, das nur bis
      zum naechsten Start haelt, ist keine Entscheidung, sondern eine
      Verzoegerung. Abgelehnt wird nach NAMEN und nicht nach Kennung —
      abgelehnt wird ja gerade das, was KEIN Interpret ist und oft gar keine
      Kennung hat. ABGELEHNT SCHLAEGT ALLES, auch eine „ja"-Automatik; nie
      steht ein Name in beiden Listen.

      == FREIGESCHALTETE OHNE EIGENE WERKE — der neue Fall ==
      Woher die Reihe ihre Eintraege NAHM: `interpretenZeichnen()` (app.js)
      gruppierte /api/werke nach `interpretSchluessel`. Ein Interpret ohne Werk
      konnte darin gar nicht vorkommen — es gab keine Zeile, aus der er
      entstehen konnte. `interpretenReihe` (interpreten.ts) legt ihn deshalb
      DAZU: hinten, nach Namen sortiert, damit die gewohnten Gesichter nicht
      wandern. `anzahl: 0` sagt es ausdruecklich.
      DIE MEDIENLISTE BLEIBT UNBERUEHRT (so vom Benutzer entschieden). Ein
      Interpret laesst sich nicht abspielen, nur oeffnen; stuende er in
      data.json, muesste ihn jede abspielende Stelle eigens uebergehen.
      SEIN BILD geht ueber `/api/bild/extern?u=…` (bildDurchgereicht,
      interpretenseite.ts) und NIE direkt ueber das CDN — sonst faellt weg, was
      der Coverspeicher leistet. In der Ablage steht die ROHE Adresse; die
      Umbiegung bleibt an einer Stelle. Fremde Herkunft wird beim Freischalten
      WEGGELASSEN, nicht abgewiesen: der Interpret ist deswegen nicht weniger
      richtig.

      == DIE SUCHE ==
      `search` steht in WEB_API_ERLAUBT bereits — GEPRUEFT, ein neuer Pfad ist
      NICHT noetig und waere eine Erlaubnis zu viel. `suchtrefferAus` laesst
      nur Treffer mit einer 22-Zeichen-Kennung durch (eine Zeile, die sich
      nicht freischalten laesst, wirft nur Fragen auf) und haengt an jede
      Zeile, was schon entschieden ist — sonst boete die Suche einen Knopf
      „freischalten" fuer jemanden an, der laengst in der Reihe steht.

      == DIE TEILE ==
        src/backend-api/src/interpreten.ts              die REGEL (rein)
        src/backend-api/src/interpreten.spec.ts         40 Faelle
        src/backend-api/src/interpreten.integration.spec.ts  13 Faelle
        src/backend-api/src/server.ts                   Ablage + Schnittstelle
        tools/interpreten-erkennung.mjs                 die Messung, wiederholbar

        GET  /api/interpreten[?verschmelzen=1]  -> {reihe, versteckt, frei, abgelehnt}
        GET  /api/interpreten/suche?q=…[&limit=]
        POST /api/interpreten/frei        {id, name, bild?, quelle?}
        POST /api/interpreten/abgelehnt   {name}
        POST /api/interpreten/offen       {name}
        POST /api/interpreten/zuruecksetzen

      `versteckt` reist MIT und ist kein Beiwerk: ohne diese Liste haette die
      Verwaltung keinen Weg, einen Interpreten der EIGENEN Bibliothek
      freizuschalten, den Spotify unter diesem Namen nicht kennt — und das ist
      auf dieser Box der haeufigste Fall (7 von 16).

      == WAS NOCH FEHLTE — und am selben Tag nachgezogen wurde ==
      Beides steht seit dem 03.08.2026 (siehe [interpreten-verwaltungsseite]):
        * NewDesign/app.js holt die Reihe von GET /api/interpreten;
          `istInterpret` und der sessionStorage `mupibox_neu_interpreten_v1`
          sind WEG. Ein Besitzer statt zwei.
        * Die Seite /interpreten in der Verwaltung (Erkannte, Suche,
          Freigeschaltete) nach dem Vorbild von „Doppelte" (/verschmelzung).
      Offen bleibt nichts Zwingendes. Der Schalter `diskografie` bleibt, wie er
      ist; ein freigeschalteter Interpret ohne Werke zeigt ohne ihn nur Kopf
      und beliebteste Titel — GEMESSEN und begruendet im Nachbareintrag.

  - kind: architektur
    id: interpreten-verwaltungsseite
    title: "Die Seite „Interpreten“ — und warum beim Freischalten der BIBLIOTHEKSNAME mitgeschickt wird, nicht der von Spotify"
    tags: [interpret, verwaltung, oberflaeche, spotify, erkennung, neu, 2026]
    source: "Gebaut 03.08.2026; gemessen gegen tools/neu-vorschau.mjs (Attrappe) und tools/verwaltung-vorschau.mjs, Zahlen der Box aus tools/interpreten-erkennung.mjs"
    body: |
      DIE SEITE: src/frontend-admin/src/app/seiten/interpreten.ts, Route
      /interpreten, im Kopfmenue neben „Doppelte". Drei Teile, drei Fragen:
        AUS DEINER BIBLIOTHEK   „Nicht in der Reihe" zuerst (das braucht eine
                                Entscheidung), darunter „In der Reihe". An
                                jeder Zeile steht, WORAUF sich die Erkennung
                                stuetzt: Name, Spotify-Kennung, Zahl der
                                eigenen Werke.
        SUCHEN UND FREISCHALTEN auch fuer jemanden ohne ein einziges Werk.
        FREIGESCHALTET / ABGELEHNT   was gilt, und je Zeile der Rueckweg.
      Vorbild ist /verschmelzung bis in die Bauart: Karten, ein Knopf je Zeile,
      zwei Klicks fuer alles, was wegnimmt, und zu jeder Entscheidung ein Weg
      zurueck.

      == DER KERN, und er ist leicht falsch herum zu bauen ==
      Der HAEUFIGSTE Fall dieser Box ist ein Interpret der EIGENEN Bibliothek,
      den Spotify unter DIESEM Namen nicht kennt — sieben von sechzehn
      (tools/interpreten-erkennung.mjs, 03.08.2026): „EUROPA Hoerspiele &
      Kinderlieder", „lismio: Kids - Hoerbuecher & Musik" und so fort. Fuer ihn
      gibt es „bei Spotify suchen": der Knopf merkt sich seinen
      BIBLIOTHEKSNAMEN als Ziel, fuellt das Suchfeld damit vor (er ist der
      Ausgangspunkt zum Kuerzen) — und POST /api/interpreten/frei bekommt
      danach `{ id: <Kennung des Spotify-Treffers>, name: <Bibliotheksname> }`.

      WARUM NICHT DER SPOTIFY-NAME:

          Schluessel der Ablage = normal(name)           (interpreten.ts)
          Schluessel des Regals = normal(werk.interpret) (werke.ts)

      Derselbe `normal()`. Mit dem Spotify-Namen entsteht ein ZWEITER
      Schluessel — und damit eine zweite runde Kachel mit „0 Werke" neben der
      ersten, die weiter ausgeblendet bleibt. Zwei Kacheln fuer eine Person,
      und keine davon vollstaendig. Es gaebe dabei nirgends einen Fehler zu
      sehen; deshalb steht der Unterschied jetzt als Testpaar in
      interpreten.spec.ts („freigeschaltet unter dem BIBLIOTHEKSNAMEN" /
      „… und unter dem SPOTIFY-NAMEN waeren es ZWEI Kacheln").
      GEMESSEN in der Vorschau: „Verschiedene" (1 eigenes Werk, von Spotify
      nicht erkannt) mit der Kennung von „Rolf Zuckowski" freigeschaltet steht
      danach MIT seinem Werk und an seinem alten Platz in der Reihe.

      == FREISCHALTEN GEGEN „GANZE DISKOGRAFIE" — zwei Schalter, zwei Fragen ==
        FREISCHALTEN      entscheidet, WER in der runden Reihe steht.
        GANZE DISKOGRAFIE entscheidet, WAS auf seiner Seite steht.
      Sie haengen an genau EINER Stelle zusammen, und die verdient einen
      Hinweis auf der Seite: Ein Freigeschalteter OHNE eigene Werke hat keine
      Reihe „In deiner Box". Steht der Schalter AUS, holt /api/interpret Alben,
      Singles und Sammlungen gar nicht erst (server.ts, `diskografie`).
      GEMESSEN am 03.08.2026 gegen die Attrappe: mit Schalter 36
      Reihen-Eintraege (10 Titel + 5 Alben + 15 Singles + 6 Sammlungen), ohne
      ihn EINE Reihe. Wer also jemanden per Suche freischaltet und den Schalter
      aus laesst, bekommt eine Kachel, hinter der fast nichts steht — deshalb
      warnt die Seite genau dann, und nur dann. UMGEKEHRT GILT NICHTS: der
      Schalter allein bringt niemanden in die Reihe.

      == WAS GILT, SOLANGE NICHTS ENTSCHIEDEN IST ==
      DIE AUTOMATIK, genau wie vorher. Eine leere Reihe waere ein Rueckschritt
      gegenueber dem Stand von gestern, und die Vorgabe bei Ausfall bleibt
      „lieber zeigen". Die Seite faengt deshalb mit dem an, was die Box schon
      TUT, statt mit einer leeren Liste, die man erst fuellen muss. Erst eine
      Ablehnung nimmt etwas weg.

      == KLEINIGKEITEN, die eine Begruendung haben ==
        * `frei[].bild` ist die ROHE CDN-Adresse und gehoert NICHT in ein
          `<img src>`. Die Verwaltung nimmt das Bild aus `reihe` — dort ist es
          schon durch /api/bild/extern gebogen (`bildVon`).
        * „ohne Kennung" stand einmal neben der Marke „bei Spotify nicht
          gefunden": dasselbe zweimal, und die Zeile wurde davon zweizeilig
          (114 px statt 62 px, gemessen bei 883 px Zeilenbreite).
        * `festschreiben` bei einem automatisch Erkannten aendert die Anzeige
          nicht — es nimmt der Automatik nur die Frage ab (`zuPruefen`) und
          macht das Urteil ueberstimmbar.
        * Die Seite laedt NACHEINANDER: erst /api/darstellung (dort steht
          `verschmelzen`), dann /api/interpreten mit diesem Abfrageteil. Ohne
          ihn zaehlte sie zwei Werke, wo die Box eine Kachel zeigt.

      == DIE TEILE ==
        src/frontend-admin/src/app/seiten/interpreten.ts   die Seite
        src/frontend-admin/src/app/app.routes.ts           Route /interpreten
        src/frontend-admin/src/app/rahmen.ts               der Menuepunkt
        NewDesign/app.js                                   `interpretenHolen`
        tools/verwaltung-vorschau.mjs                      Verwaltung ohne Box

  - kind: falle
    id: attrappe-stirbt-statt-zu-luegen
    title: "Die Attrappe log nicht durch Weglassen — sie starb: `for (const w of werke())` riss den ganzen Vorschauserver mit"
    tags: [falle, werkzeug, vorschau, attrappe, messen, interpret, 2026]
    source: "Beim Nachmessen der Interpreten-Reihe gefunden, 03.08.2026, tools/neu-vorschau.mjs"
    body: |
      DER BEFUND: Der neue Zweig `/api/interpreten` in tools/neu-vorschau.mjs
      schrieb `for (const w of werke(zusammen))`. `werke()` gibt aber
      `{ stand, werke }` zurueck, keine Liste (an drei anderen Stellen derselben
      Datei steht korrekt `werke(true).werke`). Der erste Abruf warf
      „werke is not a function or its return value is not iterable" — und weil
      das im Anfrage-Behandler eines nackten `createServer` geschieht, riss es
      den GANZEN Vorschauserver mit. Danach antwortete gar nichts mehr.

      WARUM DAS SCHLIMMER IST ALS EIN FEHLENDER ZWEIG: Der Nachbareintrag
      [attrappe-luegt-durch-weglassen] warnt davor, dass eine Attrappe ein Feld
      nicht liefert und man das Nichts fuer ein Ergebnis haelt. Hier war es
      lauter und trotzdem verfuehrerischer: Wer die Seite danach neu laedt,
      sieht eine tote Seite und sucht den Fehler in der OBERFLAECHE. Der Zweig
      war ausserdem mit einem ausfuehrlichen Kommentar versehen, der genau
      erklaerte, warum er unbedingt dastehen muesse — er sah also aus, als sei
      er gemessen worden. Er war nie ein einziges Mal gelaufen.

      DREI WEITERE FEHLER IM SELBEN ZWEIG, alle derselben Sorte („die Attrappe
      antwortet anders als das Geraet"), alle erst beim Ausprobieren sichtbar:
        1. `jsonAus(res, {error:'sucheLeer'}, 400)` — `jsonAus` nahm nur ZWEI
           Argumente. Der Fehlerfall antwortete mit 200. `jsonAus` hat jetzt
           einen dritten Parameter `stand`.
        2. Die Suche gab `id: interpretKennung(n)` aus. Diese Funktion liefert
           `null` fuer alles, was die ERKENNUNG nicht kennt — also auch fuer
           „Alin Coen", den einzigen Fall, um den es ging. Der echte Server
           wirft eine Zeile ohne 22-Zeichen-Kennung weg (`suchtrefferAus`), die
           Verwaltung haette also nie einen Knopf „freischalten" zu sehen
           bekommen. WER IN DER SUCHE STEHT, HAT EINE KENNUNG; erkannt zu
           werden ist die zweite, engere Frage.
        3. Die Attrappenablage hielt das Bild schon fertig umgebogen
           (`/api/bild/extern?u=…`), waehrend in config/interpreten.json die
           ROHE Adresse steht. Damit waere nie aufgefallen, dass `frei[].bild`
           nicht in ein `<img src>` gehoert.

      DIE LEHRE, kurz: Ein Werkzeug, das nie gelaufen ist, ist kein Werkzeug,
      sondern eine Behauptung — und ein ausfuehrlicher Kommentar darueber macht
      es glaubwuerdiger, nicht richtiger. Der erste Schritt nach dem Schreiben
      eines Vorschauzweigs ist EIN `curl` darauf, nicht der naechste Zweig.

  - kind: falle
    id: freigeschalteter-interpret-verliert-seine-box-reihe
    title: "Die Kachel zaehlte das Werk, die Seite dahinter kannte es nicht — „In deiner Box\" verglich nur Spotifys Namen"
    match: "schluesselDesNamens|schluesselDerBox|interpretenseiteBauen|In deiner Box"
    cause: "`interpretenseiteBauen` bildete `normal(name)` aus `artist.name ?? roh.name` — der SPOTIFY-Name gewinnt, sobald `artists/<id>` antwortet. Die Freischaltung koppelt aber absichtlich einen BIBLIOTHEKSNAMEN an die Kennung eines anders heissenden Spotify-Treffers."
    fix: "`schluesselDerBox` — eine Menge aus `normal(artist.name)` UND `normal(roh.name)`; die Entdopplung ueber `gesehen` faengt den Fall ab, dass beide dasselbe treffen. src/backend-api/src/interpretenseite.ts, drei Faelle in interpretenseite.spec.ts."
    tags: [falle, interpret, freischalten, spotify, backend, neu, 2026]
    source: "Gegenlesen der Interpreten-Freischaltung, 03.08.2026 — nachgestellt mit tools/interpret-freigeschaltet-seite.mjs (ohne Box, ohne Netz)"
    related: [kennungsform-verraet-nicht-die-art]
    body: |
      „IN DEINER BOX" STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die trennt:
      IST DIE REIHE UEBERHAUPT DA?

          Reihe fehlt / Werk nicht darin        -> DIESER Eintrag
                                                   (Namensvergleich)
          Reihe da, aber der Tipp tut nichts
          bzw. „Die Titel ließen sich nicht
          laden"                                -> [[kennungsform-verraet-nicht-die-art]]

      Beides sieht auf derselben Seite gleich still aus — keine Fehlermeldung,
      nur nichts. Der Unterschied ist sichtbar, bevor man tippt.

      DER FALL, den ein Kind erlebt: Die runde Kachel „EUROPA Hörspiele &
      Kinderlieder" steht in der Reihe und zaehlt ihr eigenes Hoerspiel mit
      (`anzahl: 1`, die Vorlesestimme sagt „ein Eintrag"). Ein Tipp darauf, und
      die Ueberschrift heisst ploetzlich „EUROPA" — und das Hoerspiel, wegen
      dem die Kachel da war, steht auf der Seite NICHT MEHR. Kein Fehler, keine
      Meldung; die Reihe „In deiner Box" fehlt einfach.

      WARUM ES GERADE DEN HAEUFIGSTEN FALL TRAF. Die Freischaltung ist fuer
      den Interpreten der EIGENEN Bibliothek gebaut, den Spotify unter DIESEM
      Namen nicht kennt — sieben von sechzehn auf dieser Box
      (tools/interpreten-erkennung.mjs, 03.08.2026). Die Verwaltung schickt
      deshalb ausdruecklich `{ id: <Spotify-Kennung>, name: <Bibliotheksname> }`
      (siehe [interpreten-verwaltungsseite]) — das Auseinandergehen der beiden
      Namen ist also nicht der Grenzfall, sondern der Zweck.

      Die Seite dahinter rechnete aber weiter mit EINEM Namen:

          const schluesselDesNamens = normal(name)      // name = artist.name ?? roh.name
          werke.filter(w => w.interpretSchluessel === schluesselDesNamens)

          normal("EUROPA")                          -> "europa"
          normal("EUROPA Hörspiele & Kinderlieder")  -> "europa horspiele kinderlieder"

      `roh.name` war als blosser ERSATZ gedacht, falls `artists/<id>` nicht
      antwortet. Solange die Reihe allein aus Namensgleichheit entstand, war
      das dasselbe und die Unterscheidung folgenlos. Die Freischaltung hat aus
      dem Ersatz eine zweite Auskunft gemacht, ohne dass die Seite davon
      erfuhr.

      WARUM ES BEIM BAUEN NICHT AUFFIEL — und das ist die eigentliche Lehre:
      Gemessen wurde die REIHE („steht danach MIT seinem Werk und an seinem
      alten Platz"), und die stimmte. Die SEITE, auf die dieselbe Kachel
      fuehrt, wurde nicht mitgemessen. Ein Feature endet nicht dort, wo die
      neue Kachel steht, sondern dort, wo der Finger des Kindes ankommt.

      GEMESSEN (03.08.2026, tools/interpret-freigeschaltet-seite.mjs — rein,
      ohne Box und ohne Netz):
        vorher   Reihe: 1 Kachel, 1 Werk   Seite: 0 Reihen, „In deiner Box" FEHLT
        nachher  Reihe: 1 Kachel, 1 Werk   Seite: 1 Reihe,  „In deiner Box" 1 Kachel

      WAS STEHEN BLEIBT: Die Ueberschrift der Seite nennt weiterhin Spotifys
      Namen („EUROPA"), nicht den angetippten. Das ist eine Auskunft und kein
      Verlust — aber wer es aendern will, aendert `kopf.name`, nicht die Reihe.

  - kind: falle
    id: erkennungskarte-merkt-ausfall-nicht-und-der-takt-haemmert
    title: "„Einen Ausfall merken wir nicht\" — plus 15-Sekunden-Takt ergibt 16 Spotify-Abrufe alle 15 Sekunden, dauerhaft"
    match: "ERKENNUNG_MS|ERKENNUNG_AUSFALL_MS|interpretenErkannt|zuPruefen"
    cause: "`if (treffer) interpretenErkannt.set(...)` merkte NUR Erfolge. `zuPruefen` gibt jeden unentschiedenen Namen wieder heraus, und NewDesign/app.js holt /api/interpreten im TAKT_WERKE (15 s). Solange Spotify nicht antwortet, faengt jeder Takt von vorn an."
    fix: "Zweite, KURZE Frist: `ERKENNUNG_AUSFALL_MS = 60_000`. Der Ausfall wird gemerkt, aber nur eine Minute. ACHTUNG, das war nur die halbe Miete — WAS gemerkt wird, entscheidet [ausfall-ueberschreibt-die-erkennung-und-die-reihe-wird-tot]. Beide Fristen stehen seit 03.08.2026 in src/backend-api/src/interpreten.ts (mit spec), nicht mehr in server.ts."
    tags: [falle, interpret, spotify, takt, pi, backend, 2026]
    source: "Gegenlesen der Interpreten-Freischaltung, 03.08.2026 — gelesen, nicht an der Box ausgeloest"
    body: |
      ZWEI BEGRUENDUNGEN, DIE EINZELN STIMMEN UND ZUSAMMEN NICHT.

        server.ts:  „EIN AUSFALL WIRD NICHT GEMERKT. Sonst haelt ein
                     WLAN-Aussetzer von einer Sekunde zwoelf Stunden lang die
                     Erkennung fest."   — richtig.
        app.js:     `sichtbarerTakt(TAKT_WERKE, () => interpretenHolen())`,
                     TAKT_WERKE = 15000.   — auch richtig.

      Zusammen heisst das: solange Spotify nicht antwortet, fragt der Server
      bei JEDEM Takt alle unentschiedenen Namen erneut nach. Auf dieser Box
      sind das 16; mit dem Bildschirm der Box UND dem Tablet der Eltern gut
      zwei Abrufe je Sekunde, ohne Ende und ohne dass jemand etwas tut.

      DER SCHLIMMERE ZWEIG IST DER TOKEN. Ist das Zugangsmerkmal abgelaufen
      und die Erneuerung schlaegt fehl, wirft `webApiToken` bei JEDEM dieser
      Abrufe und jeder versucht die Erneuerung erneut — 16 Anlaeufe an
      `accounts.spotify.com` alle 15 Sekunden. Genau damit faengt man sich eine
      Drosselung (429) ein, und die trifft dann nicht nur die runde Reihe,
      sondern das ABSPIELEN.

      DIE VORHER-LOESUNG WAR HIER BESSER, ohne es zu wissen: `istInterpret` in
      app.js fragte einmal je Sitzung und merkte sich das Ergebnis im
      sessionStorage. Sie hatte drei andere Fehler (llmwiki
      [interpreten-freischalten-ablage]) — aber sie lief nicht im Takt. Beim
      Umzug einer Entscheidung vom Browser in den Server wandert sie
      IMMER in eine Schleife: der Browser fragt periodisch, der sessionStorage
      tat es nicht.

      DIE REGEL, allgemeiner: Wer eine Karte („cache") baut, muss BEIDE Faelle
      merken. Nur den Erfolg zu merken heisst, im Fehlerfall gar keine Karte zu
      haben — und der Fehlerfall ist der, in dem die Abrufe teuer sind. Die
      Frist darf verschieden sein (12 h gegen 1 min), das Merken selbst nicht.

      GERECHNET, nicht an der Box ausgeloest (ein Ausfall laesst sich dort
      nicht lesend herbeifuehren): 16 Namen x 4 Takte/min x 2 Oberflaechen =
      128 Abrufe je Minute; mit der Minutenfrist 32.
    related: [ausfall-ueberschreibt-die-erkennung-und-die-reihe-wird-tot]

  - kind: entscheidung
    id: leere-interpretenseite-nennt-den-ausfall
    title: "Eine leere Interpretenseite sagt jetzt „Spotify antwortet gerade nicht\" statt der Bibliothek die Schuld zu geben"
    match: "int-hinweis|interpretReihenZeichnen|ausfaelle"
    tags: [entscheidung, oberflaeche, neu, interpret, ausfall, 2026]
    source: "Gegenlesen der Interpreten-Freischaltung, 03.08.2026"
    body: |
      `interpretReihenZeichnen()` (NewDesign/app.js) hatte bei null Reihen
      genau zwei Saetze, und beide BEHAUPTEN, dass es nichts gibt:

        Schalter an:  „Zu diesem Interpreten ist hier nichts zu finden."
        Schalter aus: „Nichts aus der Box. Die ganze Diskografie holt die Box
                       nur, wenn der Schalter …"

      Faellt Spotify aus, kommen ALLE Reihen als `ausfaelle` zurueck und die
      Seite ist leer — dann sind beide Saetze falsch. Bei einem
      FREIGESCHALTETEN INTERPRETEN OHNE EIGENE WERKE ist das die ganze Seite:
      Name, Bild und ein falscher Satz. Die Angabe war da (`seite.ausfaelle`),
      sie wurde der Funktion nur nicht gereicht.

      `ausfaelle` reist jetzt mit, und bei einem Ausfall steht dort „Spotify
      antwortet gerade nicht. Gleich noch einmal probieren." — dieselbe
      Haltung wie in `interpretZeile` („der Ausfall wird benannt, nicht
      verschwiegen") und wie im Fangzweig von `interpretSeiteOeffnen`, der
      denselben Satz schon sagte. Drei Orte, eine Auskunft.

  - kind: knowhow
    id: interpreten-reihe-netz-gemessen
    title: "Der Gewinn der Umstellung, endlich gemessen: 8 Abrufe auf 1 im Anlauf — und 0 auf 4 je Minute im Dauerbetrieb"
    match: "interpreten-netz-messen|istInterpret|interpretenHolen|TAKT_WERKE"
    tags: [messung, oberflaeche, neu, interpret, netz, takt, 2026]
    source: "tools/interpreten-netz-messen.mjs, 03.08.2026, gegen tools/neu-vorschau.mjs"
    body: |
      DIE BEHAUPTUNG WAR NIE GEMESSEN. [interpreten-freischalten-ablage] nennt
      „nach jedem Neustart 16 Spotify-Abrufe, bevor die erste runde Kachel
      steht". Diese 16 sind die ZAHL DER NAMEN aus
      tools/interpreten-erkennung.mjs, nicht die Zahl der Abrufe, die die
      Oberflaeche wirklich stellte. Gemessen wurde sie erst jetzt — mit
      tools/interpreten-netz-messen.mjs, das BEIDE Staende derselben Seite im
      echten Browser gegen dieselbe Attrappe laedt (den alten ueber
      `git show 7e02c2da^:NewDesign/app.js`, per CDP untergeschoben) und die
      hinausgehenden Anfragen zaehlt.

      GEMESSEN (03.08.2026, Attrappe mit VIER Interpretennamen, je 50 s):

                                          ALT      NEU
        ms bis zur ersten Kachel          195       82
        ANLAUF, erste 5 s
          /api/spotify/web/search           8        0
          /api/interpreten                  0        1
          je Interpretenname             2.00     0.25
        DAUERBETRIEB, je Minute           0.0      4.0
        zur Kontrolle: /api/werke           4        4

      DER ALTE STAND FRAGTE JEDEN NAMEN ZWEIMAL — 8 Abrufe fuer 4 Namen, und
      das war vorher niemandem aufgefallen. `zeichnen()` ruft
      `interpretenZeichnen()`, und `zeichnen()` laeuft beim Start zweimal
      (Profil, dann Werke). Jeder Lauf startete seine eigene async-Schleife
      ueber alle Namen; die zweite war unterwegs, BEVOR die erste ihren ersten
      `interpretCache.set` geschrieben hatte. Der Wachtposten dagegen
      (`if (reihe.childElementCount) return`) sitzt am ZEICHNEN, nicht am
      FRAGEN — er verhinderte die doppelte Reihe, nicht die doppelten Abrufe.
      Auf der Box mit 16 Namen hiess das also bis zu 32 Abrufe vor der ersten
      Kachel, nicht 16. Die Zahl im aelteren Eintrag ist damit zu klein, nicht
      zu gross.

      DIE LEHRE DARAN, allgemein: Eine Sperre gegen doppelte ARBEIT muss vor
      dem teuren Schritt stehen, nicht hinter ihm. Ein Cache, der erst nach
      dem `await` gefuellt wird, schuetzt nicht gegen den zweiten Aufrufer, der
      waehrend des `await` startet.

      DAS DAUERBETRIEBS-FELD IST DER PREIS und steht hier ausdruecklich neben
      dem Gewinn: der alte Stand fragte nach dem Anlauf NIE WIEDER (0,0 je
      Minute), der neue fragt im Takt (4,0 je Minute, TAKT_WERKE = 15 s). Das
      ist genau der Umzug, den
      [erkennungskarte-merkt-ausfall-nicht-und-der-takt-haemmert] beschreibt.
      Solange Spotify antwortet, kostet er auf dem Server nichts (12-h-Karte);
      erst im Ausfall wird er teuer — und deshalb muss dort auch der Ausfall
      gemerkt werden.

      WER DIE ZAHLEN NACHRECHNET: `node tools/interpreten-netz-messen.mjs`.
      Es aendert nichts, braucht keine Box und kein Netz nach draussen. Die
      absoluten Zahlen gehoeren der Attrappe (4 Namen); der Faktor ist das
      Ergebnis, und er waechst mit der Zahl der Namen — im Anlauf 2 Abrufe je
      Name gegen einen einzigen fuer alle.

  - kind: falle
    id: ausfall-ueberschreibt-die-erkennung-und-die-reihe-wird-tot
    title: "Der Ausfall merkte sich, ueberschrieb dabei aber das Gewusste — 9 lebende Kacheln wurden 16 tote"
    match: erkennungFortschreiben|interpretNachschlagen|interpretenErkannt|ERKENNUNG_AUSFALL_MS
    cause: "`interpretenErkannt.set(schluessel, {urteil, …})` schrieb das Ausfall-Urteil `{ja:true,id:null}` UEBER das abgelaufene, gute Urteil. „Lieber zeigen\" ist richtig, wenn man nichts weiss — nicht, wenn man etwas weiss und gerade nicht nachfragen kann."
    fix: "`erkennungFortschreiben(name, treffer, vorher)` in src/backend-api/src/interpreten.ts (rein, mit spec): bei `treffer === null` gilt das ZULETZT GEWUSSTE Urteil weiter, auch abgelaufen; „lieber zeigen\" nur, wenn gar nichts bekannt ist."
    tags: ['falle', 'interpret', 'spotify', 'cache', 'backend', 'neu', 2026]
    source: "Gegenlesen der Umstellung auf GET /api/interpreten, 03.08.2026 — am Code nachvollzogen und als spec festgeschrieben (interpreten.spec.ts, 6 neue Faelle)"
    body: |
      DIE ZWEITE HAELFTE VON
      [erkennungskarte-merkt-ausfall-nicht-und-der-takt-haemmert]. Dort wurde
      richtig erkannt, dass ein Ausfall GEMERKT werden muss. Was dabei offen
      blieb: WAS gemerkt wird. Gemerkt wurde das Ausfall-Urteil selbst — und
      das ist schlechter als das, was schon dastand.

      DER ABLAUF, und er ist der Normalfall einer Box, die wochenlang durchlaeuft:

          t = 0      Spotify antwortet. Von 16 Namen bekommen 9 ein `ja` mit
                     Kennung, 7 ein `nein`. Die Reihe zeigt 9 Kacheln, jede
                     oeffnet ihre Interpretenseite.
          t = 12 h+  Die Frist (ERKENNUNG_MS) ist abgelaufen — und ausgerechnet
                     jetzt ist das WLAN weg, oder Spotify drosselt, oder die
                     8-Sekunden-Frist (INTERPRET_FRIST_MS) bricht ab.
          FOLGE      `interpretAusTreffern(name, null)` gibt {ja:true,id:null}
                     fuer JEDEN Namen, und das ueberschrieb die Karte.

      WAS DAS KIND SAH: statt 9 Kacheln stehen 16 da, und KEINE EINZIGE
      oeffnet sich — `interpretSeiteOeffnen` weist jede ohne 22-Zeichen-Kennung
      mit „Zu diesem Interpreten weiß Spotify hier nichts." ab. Die sieben, die
      nie in der Reihe stehen sollten, sind zurueck; darunter „🩵Jojo 🩵",
      genau der Fall, fuer den dieses Feature gebaut wurde. Alle 60 Sekunden
      von vorn, solange der Ausfall dauert.

      DER SESSIONSTORAGE KONNTE DAS NICHT, und das ist der unangenehme Teil:
      er hielt sein Urteil die ganze Sitzung, ohne Frist. Die Umstellung ins
      Backend war in jeder anderen Hinsicht richtig — aber sie hat an dieser
      Stelle eine Faehigkeit verloren, die vorher da war, ohne dass jemand sie
      als Faehigkeit benannt haette.

      DIE REGEL, allgemein und uebertragbar auf jede Karte mit Frist:
      ABGELAUFEN HEISST „BITTE NACHFRAGEN", NICHT „FALSCH". Schlaegt die
      Nachfrage fehl, ist das abgelaufene Faktum immer noch das beste
      verfuegbare. Eine Vorgabe fuer den Notfall („lieber zeigen") darf nur
      dort greifen, wo WIRKLICH NICHTS bekannt ist — sonst ersetzt ein
      Netzfehler Wissen durch Raten.

      DAS „NEIN" MUSS GENAUSO ERHALTEN BLEIBEN wie das „ja". Das war beim
      Beheben der zweite Reflex, und er war falsch herum: nur die Kennung zu
      retten haette die 9 Kacheln lebendig gehalten, die 7 unerwuenschten aber
      trotzdem hereingelassen. Auf dieser Box ist das „nein" die haeufigere
      Auskunft (7 von 16).

      NICHT AN DER BOX AUSGELOEST — ein Spotify-Ausfall laesst sich dort nicht
      lesend herbeifuehren. Stattdessen als reine Regel herausgezogen und
      geprueft: `erkennungFortschreiben` in interpreten.ts, sechs Faelle in
      interpreten.spec.ts. Die beiden Fristen (ERKENNUNG_MS = 12 h,
      ERKENNUNG_AUSFALL_MS = 60 s) stehen seitdem DORT und nicht mehr in
      server.ts — hinter einem Netzaufruf waeren sie nur am lebenden Geraet
      pruefbar gewesen.
    related: [erkennungskarte-merkt-ausfall-nicht-und-der-takt-haemmert]

  - kind: falle
    id: fingerabdruck-traegt-nicht-was-die-kachel-zeigt
    title: "Der billige Aenderungstest trug den Namen nicht — und der Kommentar behauptete, er tue es"
    match: interpreten.stand|werke.stand|Fingerabdruck
    cause: "`liste.map((p) => `${p.schluessel}…${p.id}…${p.bild}…${p.anzahl}`)` — `schluessel` ist `normal(name)` und aendert sich bei einer Korrektur der SCHREIBWEISE nicht. Der Name selbst stand nicht im Abdruck."
    fix: "`p.name` in den Abdruck aufnehmen (NewDesign/app.js, `interpretenHolen`)."
    tags: ['falle', 'oberflaeche', 'neu', 'interpret', 2026]
    source: "Gegenlesen der Umstellung auf GET /api/interpreten, 03.08.2026"
    body: |
      NACHSTELLEN: In der Verwaltung den Interpreten eines Werks von
      „die drei ???" auf „Die drei ???" verbessern. Die Kacheln im Raster
      zeigen sofort die neue Schreibweise (sie haengen an /api/werke); die
      RUNDE Kachel zeigt die alte, bis jemand die Seite neu laedt.

      WARUM: `normal()` wirft alles weg, was kein Buchstabe und keine Ziffer
      ist, und macht klein — die Korrektur aendert den `schluessel` also
      NICHT. Auch `id`, `bild` und `anzahl` bleiben gleich. Der Abdruck ist
      identisch, `interpretenHolen` kuerzt ab, und die neue Liste wird gar
      nicht erst uebernommen.

      DER KOMMENTAR STAND SCHON RICHTIG DA — „Der Fingerabdruck traegt ALLES,
      was die Kachel zeigt (Name, Kennung, Bild, Anzahl)". Nur der Code
      darunter tat es nicht. Das ist die teurere Sorte Fehler: wer den
      Kommentar liest, prueft die Zeile nicht mehr nach.

      DIE REGEL: Ein Abdruck, der einen Neuzeichnen-Schritt spart, muss ueber
      das gefuehrt werden, was ANGEZEIGT wird — nicht ueber das, was den
      Eintrag IDENTIFIZIERT. Beides faellt genau dann auseinander, wenn die
      Identitaet normalisiert ist. Dieselbe Frage stellt sich an jedem
      `*.stand` in NewDesign/app.js.
  - kind: architektur
    id: spielt-marke-eine-frage-zwei-formen
    title: "Die Marke „spielt gerade“ im Raster — zwei Fragen, EIN Vergleich"
    match: spielt-marke|data-spielt|spieltMarkieren|laneLaufendMarkieren|playing indikator|markenplatz
    tags: ['architektur', 'oberflaeche', 'neu', 'marke', 'raster', 'lane', 2026]
    source: "Auftrag „der playing indikator muss noch auf oberster ebene sichtbar sein“ (E20/O1), 03.08.2026"
    body: |
      DER SATZ DES BENUTZERS: „der playing indikator muss noch auf oberster
      ebene sichtbar sein". Die Marke gab es seit dem 02.08. nur in der LANE
      ([[laufender-titel-war-nur-ein-aufleuchten]]); im Raster sah man am
      Mini-Player, DASS etwas laeuft, aber nicht WELCHE Kachel.

      ── ES SIND ZWEI FRAGEN, UND DAS IST DER GANZE PUNKT ───────────────
      In der Lane ist eine Kachel ein TITEL:  „laeuft GENAU dieses Stueck?"
      Im Raster ist eine Kachel ein WERK:     „laeuft IRGENDETWAS daraus?"

      Der naheliegende Weg — eine zweite Schleife mit einem zweiten Vergleich
      — ist genau die Bauart, die beim Cover-Vollbild eingeholt hat: drei Orte
      mit je eigenen Zuweisungen, zwei nachgezogen, einer nicht, und
      aufgefallen ist es dem Kind, das die Ansicht offen liess
      ([[drei-orte-eine-anzeige]]).

      ── DIE LOESUNG: DER ORT SAGT, WOFUER ER STEHT ─────────────────────
      Wie beim Vollbild steht die Liste der Orte im BAUM, nicht in app.js.
      Jede Kachel traegt in `data-spielt` eine Liste von KENNUNGEN, ihr
      Bildkasten traegt `data-markenplatz`, und `spieltMarkieren()` bildet
      einmal die Schnittmenge mit dem, was laeuft. Keine Fallunterscheidung,
      kein Klassenname im Code. Ein VIERTER Ort bekommt dieselben Merkmale
      und wird mitmarkiert, ohne dass in app.js eine Zeile dazukommt.

      ZWEI FORMEN VON KENNUNG, und die FORM traegt die Frage:

          stueck|<playlist>|<uri>    Lane-Titelkachel  (eine)
          stueck|<playlist>|<uri>…   Lane-Albumkachel  (alle ihre Stuecke)
          werk|<schluessel>          Raster-Kachel     (eine)
          werk|<schluessel>…         Regal-Kachel      (alle Werke darin)

      Album- und Regal-Kachel sind damit derselbe Fall: „ich stehe fuer alles,
      was ein Tipp auf mich oeffnet". Das ist die Gegenrichtung zu
      [[box-kachel-zeigt-fremde-quelle]] — die Liste, aus der die Kennungen
      kommen, ist genau die, die der Tipp anzeigt.

      DER ZUSAMMENHANG GEHOERT IN DIE KENNUNG, nicht daneben. Vorher stand er
      als `data-zusammenhang` an der Lane und wurde per `closest()`
      dazugeholt — eine zweite Zutat, die man beim naechsten Ort vergisst, und
      das Raster hat gar keine Lane ueber sich, an die man sie haengen
      koennte. Ohne ihn markierte ein Titel, der in ZWEI Playlisten steht, in
      beiden.

      ── DER NAME MUSSTE MIT ────────────────────────────────────────────
      `.lane-laeuft` -> `.spielt-marke`, `laneLaufendMarkieren` ->
      `spieltMarkieren`, `data-uri`/`data-uris` -> `data-spielt`. Ein zweiter
      Satz CSS-Regeln fuers Raster waere zwei Wahrheiten ueber dieselbe Marke
      gewesen; so gelten die drei Bremsen aus
      [[marke-tanzt-scaleY-nicht-hoehe]] (prefers-reduced-motion,
      `body.ruhige-marke`, nur markierte Kacheln tragen ueberhaupt eine
      Marke) ueberall, ohne irgendwo wiederholt zu werden. Der Schalter haengt
      am `body` — genau deshalb greift er auch fuer Marken, die es beim
      Umlegen noch gar nicht gab.

      ── DIE ECKEN, GEMESSEN STATT ANGESEHEN ────────────────────────────
      Auf einer Raster-Kachel (150 px) sind drei Ecken besetzt: unten links
      `.kachel-marken` (Dienst-Plakette, 28,5 px), unten rechts `.tipp-spiel`
      (44 px) bzw. `.regal-zahl` (30 px), mittig `.kachel-fehlt`. Oben links
      ist die einzige freie. Gemessen an der GEMEINSTEN Kachel („Das
      Pummeleinhorn" traegt Plakette, Play-Knopf UND Fehlt-Schaerpe),
      Vorschau 800x480, 03.08.2026:

          gegen .kachel-fehlt     24 px frei   <- der knappste Abstand
          gegen .kachel-marken    95 px frei
          gegen .tipp-spiel      112 px frei
          gegen .regal-zahl      132 px frei

      Keine Ueberschneidung. Die Marke bleibt bei 26 px, auch auf der
      groesseren Kachel: mitwachsen zu lassen (wie `.marke` es mit `--kachel`
      tut) haette sie in der Lane auf 96-px-Titelkacheln neben der grossen
      Folgennummer ebenfalls vergroessert.

      DIE BESCHRIFTUNG FAERBT MIT, im Raster wie in der Lane — ein zweiter
      Traeger derselben Auskunft. Im Raster ist er sogar der auffaelligere:
      die Beschriftung steht dort UNTER dem Cover, nicht darauf.

      NACHMESSEN:
        node tools/raster-marke-schau.mjs --pruefen   # das Raster
        node tools/lane-marken-schau.mjs --pruefen    # die Lane, unveraendert
        node tools/lane-marke-wandert.mjs --pruefen   # wandert sie mit
        node tools/marke-tanzt.mjs --pruefen          # bewegt sie sich

      GEAENDERTE DATEIEN: NewDesign/app.js (spieltMarkieren, stueckKennung,
      werkKennung, laufendeKennungen, kachelBauen, regalKachelBauen, die
      beiden Lane-Bauer), NewDesign/app.css (.spielt-marke),
      tools/raster-marke-schau.mjs (neu), tools/lane-marke-wandert.mjs,
      tools/lane-marken-schau.mjs, tools/marke-tanzt.mjs, tools/pruefen.sh.

      OFFEN, mit Absicht nicht entschieden: Die kleinen Kacheln der Reihen
      „Weiterhoeren" und „Oft gehoert" bleiben ohne Marke — dasselbe Werk
      steht im Raster darunter ohnehin markiert, und „weiterhoeren" ist die
      andere Frage. Ebenso bleibt die Marke stehen, wenn PAUSIERT ist: sie
      sagt „das ist das Laufende", nicht „es toent gerade". Die tanzenden
      Balken sind dabei streng genommen eine kleine Luege; sie beim Pausieren
      anzuhalten hiesse, auch die Lane zu aendern, und das gehoert in eine
      eigene Messung.

      NICHT GEDECKT, und das ist eine Grenze, keine Nachlaessigkeit: Laeuft
      etwas LOKALES oder ueber Jellyfin, das niemand von dieser Seite aus
      gestartet hat (frisch geladener Kiosk, gestartet vom Telefon), bleibt
      das Raster unmarkiert. `/player/local` meldet keine Werk-Kennung, und
      ein Rueckfall auf blosse Namensaehnlichkeit ist in `laufendesWerk`
      ausdruecklich ausgeschlossen. Lieber keine Marke als eine an der
      falschen Kachel.

  - kind: falle
    id: zuletztgestartet-war-der-erste-zeuge-statt-des-letzten
    title: "Wer erst lokal und dann vom Telefon Spotify startet, verlor die Marke — der schwaechste Zeuge stand vorn"
    match: laufendesWerk|zuletztGestartet|laufendesWerkMitBeweis|merkStand|beweis
    cause: "`laufendesWerk()` gab `zuletztGestartet` zurueck, BEVOR es `context.uri` ueberhaupt ansah. Das Gedaechtnis an eine ABSICHT schlug damit den Beweis."
    fix: "Reihenfolge umgedreht: Zusammenhang -> zuletztGestartet -> Gemerktes. Der Zusammenhang zaehlt nur, wenn `jetztLaeuft()` auch `art === 'spotify'` sagt. NewDesign/app.js `laufendesWerkMitBeweis`."
    tags: ['falle', 'oberflaeche', 'neu', 'spotify', 'marke', 2026]
    source: "beim Messen der Raster-Marke aufgefallen, tools/raster-marke-schau.mjs, 03.08.2026"
    body: |
      DIESER EINTRAG HANDELT VON DER REIHENFOLGE DER ZEUGEN, nicht von
      der Quelle. Traegt das Werk MEHRERE Quellen und laeuft ueber die
      zweite, ist es der andere Eintrag mit `laufendesWerk` im `match:`:
      [[marke-sah-nur-die-bevorzugte-quelle]]. Hier hat das Werk nur eine
      — es war nur nicht das zuletzt gestartete.

      NACHSTELLEN (in der Vorschau gemessen, nicht ueberlegt): Kachel „Die
      Maus" antippen (lokal, mpv). Dann `/vorschau/spotify` — Spotify spielt
      die Playlist „Bibi Blocksberg". ERGEBNIS VORHER: im Raster ist NICHTS
      markiert. Das Regal „Kiddinx" muesste die Marke tragen.

      WARUM: `laufendesWerk()` gab `zuletztGestartet` („Die Maus") zurueck,
      ohne `context.uri` anzusehen. Die Raster-Marke fragt zusaetzlich nach
      der Plausibilitaet (sonst bliebe sie nach dem Ende der Wiedergabe
      stehen) — und „Die Maus" passt zu „Kapitel 7: Das Eischneerodelfest"
      natuerlich nicht. Also fiel alles heraus.

      DIESELBE STELLE HAT NOCH EINEN ZWEITEN VERLIERER: `merkStand()` merkte
      sich in dieser Lage GAR NICHTS mehr — es fragt dieselbe Funktion und
      prueft mit derselben Plausibilitaet nach. Die Stelle des laufenden
      Spotify-Kapitels ging verloren, still.

      DIE REGEL: Der STAERKSTE Beweis gewinnt, nicht der billigste.
      `zuletztGestartet` ist das Gedaechtnis an eine ABSICHT dieser Seite —
      der schwaechste Zeuge, den es hier gibt. Spotifys `context.uri` ist der
      staerkste. Dass das Schwache vorn stand, war eine Abkuerzung aus einer
      Zeit mit genau einem Aufrufer.

      DIE BREMSE, DIE DABEI DAZUGEHOERT: Der Zusammenhang zaehlt NUR, wenn
      Spotify auch die Quelle des Tons ist (`jetztLaeuft().art === 'spotify'`;
      der Vorrang von LOKAL steht dort schon, Herkunft now-playing.ts
      toNowPlaying). `/player/state` traegt seinen `context` noch eine Weile
      weiter, wenn librespot schweigt und in Wahrheit mpv spielt. Ohne die
      Bremse behauptete die Antwort „Spotify" waehrend eines lokalen
      Hoerbuchs — und zwar ausgerechnet nach jedem Neuladen des Kiosk, wenn
      `zuletztGestartet` leer ist. Diesen Fall hatte die ALTE Fassung
      ebenfalls; er ist mit derselben Aenderung mitweg.

      NICHTS ZURUECKSCHREIBEN. Naheliegend waere, `zuletztGestartet` auf das
      erkannte Werk zu setzen. Das ist die Nachlauf-Falle
      ([[spotify-nachlauf-muster]]): Nach dem Start von Playlist B meldet
      Spotify ein bis zwei Sekunden lang noch A, und die Rueckschreibung
      machte aus dem Nachlauf einen dauerhaften Irrtum. Die ANTWORT darf
      schwanken, das GEDAECHTNIS nicht.

      DER BEWEIS GEHOERT SEITDEM ZUR ANTWORT: `laufendesWerkMitBeweis()` gibt
      `{ werk, beweis }` mit 'zusammenhang' | 'gemerkt' | 'gestartet'.
      `laufendesWerk()` ist die duenne Huelle darum. Wer nur ein COVER malt,
      darf sich mit 'gestartet' begnuegen (ein falsches Bild ist haesslich,
      mehr nicht); wer „laeuft GERADE" behauptet, muss nachfragen. Vorher
      stand diese Unterscheidung nur als Kommentar in `merkStand()` — jeder
      neue Aufrufer musste sie erraten, und das ist die Art Wissen, die beim
      zweiten Aufrufer verlorengeht.
    related: [marke-sah-nur-die-bevorzugte-quelle]

  - kind: entscheidung
    id: raster-marke-schau-gegenprobe
    title: "tools/raster-marke-schau.mjs — und die Gegenprobe, die sie an sich selbst bestanden hat"
    match: raster-marke-schau|Marke im Raster
    tags: ['werkzeug', 'oberflaeche', 'neu', 'marke', 2026]
    source: "03.08.2026"
    body: |
      WAS ES PRUEFT, in dieser Reihenfolge:

        0. QUELLTEXT, ohne Browser: `classList.toggle('spielt'` darf an genau
           EINER Stelle stehen, `el('div', 'spielt-marke')` ebenso. Das ist
           der eigentliche Waechter — ein Verhaltenstest bliebe gruen, solange
           zwei Fassungen zufaellig einig sind, und genau so faengt
           [[drei-orte-eine-anzeige]] an.
        1. Kann die Attrappe den Fall ueberhaupt stellen? Verglichen wird
           `context.uri` der Vorschau gegen die Kennung des Werks aus
           /api/werke. Ohne diese Frage VORWEG misst man das Nichts
           ([[attrappe-luegt-durch-weglassen]]).
        2. Spotify spielt „Bibi Blocksberg" -> markiert ist das Regal
           „Kiddinx", und nur das. (Im Raster steht das REGAL, nicht die
           Playlist — beide Kiddinx-Werke bilden eines. Wer das uebersieht,
           sucht nach einer Kachel, die es dort gar nicht gibt.)
        3. Die Ecken, gerechnet: Ueberschneidung und Abstand gegen
           `.kachel-marken`, `.tipp-spiel`, `.regal-zahl`, `.kachel-fehlt` —
           einmal an der markierten Kachel und einmal an der GEMEINSTEN
           („Das Pummeleinhorn" traegt alle drei; dort wird die Marke fuer die
           Messung untergeschoben und gleich wieder abgeraeumt, dieselbe
           bewusste Grenze wie in tools/marke-tanzt.mjs).
        4. GEGENPROBE: mpv spielt, gestartet hat es niemand von hier -> KEINE
           Kachel darf es behaupten. Danach dieselbe Kachel antippen -> jetzt
           genau sie.
        5. Wiedergabe zu Ende -> die Marke muss WEG sein.
        6. Die drei Bremsen am selben Element: Bewegung normal,
           `body.ruhige-marke` still, `prefers-reduced-motion` still (ueber
           CDP `Emulation.setEmulatedMedia`), dazu `transform-box: fill-box`.
        7. Dasselbe im Dunkeln.

      WENN DIE MARKIERTE KACHEL KEINE ANDERE AUFSCHRIFT TRAEGT, MELDET ES DAS
      ALS FEHLER. Sonst waere der Eckentest nicht bestanden, sondern nicht
      gelaufen — dieselbe stille Luege, gegen die das ganze Werkzeug gebaut
      ist.

      DIE GEGENPROBE HAT ES SELBST BESTANDEN, und zwar ungeplant: Der erste
      Lauf meldete DREI Abweichungen. Zwei waren echte Fehler in der
      Oberflaeche ([[zuletztgestartet-war-der-erste-zeuge-statt-des-letzten]]).
      Die dritte war ein Fehler IM WERKZEUG: Es verbot das Wort
      `lane-laeuft` im Quelltext ueberhaupt — und schlug prompt bei dem
      Kommentar an, der die Umbenennung ERKLAERT. Eine Pruefung, die die
      Begruendung ihrer selbst verbietet, ist keine Pruefung; gesucht wird
      seitdem die WIRKSAME Stelle (Regel am Zeilenanfang in der CSS,
      Klassenzeichenkette in der JS).

      Der Schritt haengt in tools/pruefen.sh („Marke im Raster"), direkt
      hinter „Marke tanzt". Ohne Browser meldet er sich ab, statt rot zu
      werden — der Quelltextteil laeuft trotzdem.

  - kind: falle
    id: nachlauf-markierte-die-lane-aber-nicht-das-raster
    title: "Der Nachlauf markierte die LANE und nicht das Raster — zwei Antworten in EINER Funktion"
    match: nachlauf|spielt-marke|laufendeKennungen|stueckKennung
    tags: ['oberflaeche', 'neu', 'marke', 'spotify', 'falle', 2026]
    source: "03.08.2026, tools/marke-grenzfaelle.mjs"
    body: |
      HIER IST DER NACHLAUF DER GEGNER, NICHT DER GEGENSTAND. Wer
      dagegen pruefen will, ob ein SATZ ueber Spotifys Autoplay ueberhaupt
      gemessen ist („am Ende hoert ein Kind Stille"), landet beim
      anderen Eintrag mit „Nachlauf" im `match:`:
      [[autoplay-behauptung-reicht-weiter-als-die-messung]]. Dieser hier
      setzt voraus, dass die Bremse gewollt ist, und handelt nur davon,
      dass sie an einer von zwei Stellen fehlte.

      GEGENGELESEN AM 03.08.2026, einen Commit nach
      [[spielt-marke-eine-frage-zwei-formen]]. Dort ist der Vergleich fuer
      Lane und Raster an EINE Stelle gezogen worden — und genau in dieser
      einen Stelle standen weiterhin ZWEI Antworten auf „was laeuft".

      ── DER FEHLER ────────────────────────────────────────────────────
      `laufendeKennungen()` baut zwei Kennungen. Die WERK-Kennung ging ueber
      `laufendesWerkMitBeweis`, und die fragt zuerst `jetztLaeuft().art ===
      'spotify'` — die Bremse gegen Spotifys Nachlauf
      ([[spotify-nachlauf-muster]]). Die STUECK-Kennung daneben las
      `zustand.dienst.context.uri` und `item.uri` ROH, ohne diese Frage.

      FOLGE, GEMESSEN und nicht ueberlegt: Das Kind tippt ein lokales
      Hoerbuch an. `/player/state` traegt seinen alten Zusammenhang und sein
      altes `item` noch weiter, weil librespot nur schweigt. In der offenen
      Titelliste tanzt daraufhin weiter die Marke an einem Spotify-Kapitel,
      das nicht toent — waehrend das Raster daneben (richtig) NICHTS
      markiert. Zwei Marken auf einem Bildschirm, die sich widersprechen.

          Lage `nachlauf`, mpv spielt „Die Maus"
          vorher   Lane markiert „Das Eischneerodelfest" UND „Kapitel 7"
                   Raster markiert nichts
          nachher  beide nichts

      ── DIE REPARATUR IST EINE ZEILE, DIE FRAGE DAHINTER NICHT ────────
      `const sp = np && np.art === 'spotify' ? zustand.dienst : null`, und
      `np` wird einmal oben geholt statt zweimal in der Funktion. Damit
      haengen BEIDE Kennungen an derselben Bremse.

      DIE LEHRE, und sie ist die eigentliche: „Der Vergleich steht an EINER
      Stelle" ist noch keine Antwort. Es kommt darauf an, dass die eine
      Stelle auch EINE VORAUSSETZUNG hat. Zwei Zweige in einer Funktion, von
      denen einer eine Bedingung prueft und der andere nicht, sind dasselbe
      Auseinanderlaufen wie zwei Funktionen — nur schlechter zu sehen, weil
      sie untereinander stehen.

      WARUM ES NIEMAND BEMERKT HATTE: Die Attrappe konnte den Fall nicht
      stellen. `tools/neu-vorschau.mjs` kannte „Spotify spielt" (mit
      `context` und `item`) ODER „nichts laeuft" (`item: null`) — den
      Zustand DAZWISCHEN, in dem beide Kanaele antworten und sich
      widersprechen, gab es nicht. Wieder
      [[attrappe-luegt-durch-weglassen]], diesmal fehlte kein Feld, sondern
      der WIDERSPRUCH. Neu: `/vorschau/nachlauf`.

      NACHMESSEN:
        node tools/marke-grenzfaelle.mjs --pruefen

      GEAENDERTE DATEIEN: NewDesign/app.js (`laufendeKennungen`),
      tools/neu-vorschau.mjs (Lage `nachlauf`), tools/marke-grenzfaelle.mjs.
    related: [autoplay-behauptung-reicht-weiter-als-die-messung]

  - kind: falle
    id: marke-sah-nur-die-bevorzugte-quelle
    title: "Ein verschmolzenes Werk blieb unmarkiert, wenn es ueber seine ZWEITE Quelle lief"
    match: quelleVon|quellen\[0\]|laufendesWerk|verschmolzen
    tags: ['oberflaeche', 'neu', 'marke', 'verschmelzung', 'falle', 2026]
    source: "03.08.2026, tools/marke-grenzfaelle.mjs"
    body: |
      `laufendesWerk` STEHT IN ZWEI EINTRAEGEN, beide in derselben
      Funktion. Die eine Frage, die trennt: TRAEGT DAS WERK MEHRERE
      QUELLEN?

          Ja — verschmolzen, gespielt ueber die ZWEITE
          -> DIESER Eintrag (`quellen[0]` ist die Bevorzugung, nicht die
             Identitaet)
          Nein — aber vorher lief etwas ANDERES (erst lokal, dann
          Spotify vom Telefon)
          -> [[zuletztgestartet-war-der-erste-zeuge-statt-des-letzten]]
             (die Zeugen standen in der falschen Reihenfolge)

      In beiden Faellen bleibt das Raster unmarkiert und nichts meldet
      sich. Der Unterschied steht in der Kachel, nicht im Protokoll.

      `laufendesWerkMitBeweis()` suchte das laufende Werk so:

          const q = quelleVon(w)                 // = quellen[0]
          if (!q || q.dienst !== 'spotify') continue

      `quellen[0]` ist die BEVORZUGUNG — „womit spielen wir es?"
      ([[verschmelzung-aufloesung-stufe1]]). Hier wird aber etwas anderes
      gefragt: „ist DAS hier dieses Werk?". Bei einem verschmolzenen Werk
      faellt beides auseinander, und zwar auf der Box: „Kapelle Petra — HAMM"
      und „Das Lumpenpack" tragen Jellyfin UND Spotify und werden ueber
      JELLYFIN gespielt. Wirft jemand vom Telefon dasselbe Album ueber
      Spotify an, nennt `context.uri` die Kennung der ZWEITEN Quelle — und
      die Kachel blieb stumm.

      GEMESSEN (Vorschau, „Die drei ???", Quellen jellyfin + spotify):
        vorher   Spotify spielt die zweite Quelle -> KEINE Marke
        nachher  Spotify spielt die zweite Quelle -> „Die drei ???"

      DIE REPARATUR IST EINE SCHLEIFE UEBER `w.quellen` statt `quellen[0]`.
      Kein neues Risiko: verglichen werden weiter nur Spotify-Kennungen, und
      mehrdeutig wird es davon nicht — `verschmelzeWerke()` weist jede
      Zuordnung mit mehrdeutigem Schluessel ohnehin ab
      ([[medienschluessel-kollision-gleiche-playlistid]]).

      ES TRIFFT MEHR ALS DIE MARKE. `laufendesWerk()` ist auch der Weg, ueber
      den `merkStand()` die Weiterhoeren-Stelle ablegt und ueber den das
      Cover faellt. Ein verschmolzenes Werk, das ueber Spotify von aussen
      lief, verlor also nicht nur die Marke, sondern auch seine Stelle.

      DIE FRAGE, DIE MAN SICH BEI JEDEM `quelleVon()` STELLEN MUSS: Frage ich
      nach dem WEG (dann quellen[0]) oder nach der IDENTITAET (dann alle)?
      Die uebrigen Aufrufer in NewDesign/app.js fragen nach dem Weg —
      `abspielBefehl`, `diensteVon`, das Aufklappen der Playlist-Lane — und
      bleiben deshalb, wie sie sind.
    related: [zuletztgestartet-war-der-erste-zeuge-statt-des-letzten]

  - kind: entscheidung
    id: marke-grenzfaelle-werkzeug
    title: "tools/marke-grenzfaelle.mjs — die Raender der Marke, samt Waechter gegen die veraltete Attrappe"
    match: marke-grenzfaelle|Marke an den Raendern
    tags: ['werkzeug', 'oberflaeche', 'neu', 'marke', 2026]
    source: "03.08.2026"
    body: |
      tools/raster-marke-schau.mjs misst EINDEUTIGE Lagen (Spotify spielt
      eine Playlist des Rasters; mpv spielt, gestartet hat es niemand). Die
      teuren Faelle liegen daneben. Dieses Werkzeug stellt vier:

        1. VERSCHMOLZENES WERK ueber seine ZWEITE Quelle
           ([[marke-sah-nur-die-bevorzugte-quelle]]).
        2. DER DOPPELTE SCHLUESSEL: dieselbe Playlist steht zweimal im
           Katalog, `music` und `audiobook`
           ([[medienschluessel-kollision-gleiche-playlistid]]). ERGEBNIS:
           kein Fehler, und zwar von Bauart her — die Marke haengt an der
           KENNUNG, und die IST der Schluessel. Welchen der beiden Zwillinge
           `laufendesWerkMitBeweis` findet, ist deshalb gleichgueltig. Genau
           deswegen ist es gemessen worden: ohne die Messung waere „der
           erste Treffer gewinnt" ein offener Verdacht geblieben.
        3. NACHLAUF ([[nachlauf-markierte-die-lane-aber-nicht-das-raster]]).
        4. WIE VIELE MARKEN GLEICHZEITIG TANZEN. Der Kommentar in app.css
           sagte „nie mehr als eine oder zwei" — geschaetzt. GEZAEHLT im
           schlimmsten Fall (Spotify laeuft, Regal offen, Titelliste des
           laufenden Albums aufgeklappt): DREI Marken, NEUN Balken. Alle neun
           sind `scaleY` auf der Grafikeinheit
           ([[marke-tanzt-scaleY-nicht-hoehe]]), also kein Befund — aber jetzt
           eine Zahl.

      ZWEI SCHALTER MUSSTEN IN DIE ATTRAPPE, sonst misst man das Nichts:
      `/vorschau/nachlauf`, `/vorschau/kontext-<uri>` und `/vorschau/doppelt-an`.

      DER WAECHTER, DER DABEI ENTSTAND — und er ist der wiederverwendbare
      Teil: Der Umschalter der Vorschau hat einen AUFFANGZWEIG, alles
      Unbekannte landet in `lage.spielt`. Eine Vorschau, die noch von vor den
      neuen Lagen laeuft, quittiert `/vorschau/nachlauf` also brav mit 200 —
      und spielt danach gar nichts mehr. Beim ersten Anlauf ist genau das
      passiert: zwei gemeldete Befunde, beide nur eine alte Attrappe
      ([[veraltete-vorschau-meldet-falsches-rot]]).
      `stellen(was, pruefe)` liest deshalb die zurueckgegebene `lage` und
      BRICHT AB, statt Rot fuer heilen Code zu melden.

      NOCH EINE STOLPERSTELLE DERSELBEN SORTE, gleich zweimal: Ein verwaister
      headless-Browser aus einer frueheren Sitzung haelt den FESTEN
      Fernsteuerport (tools/lane-marken-schau.mjs: 9351). Der neue Lauf
      startet seinen Browser, der bindet nicht, und das Werkzeug redet
      stattdessen mit dem ALTEN — es haengt dann ohne Ausgabe, bis jemand es
      abbricht. `pgrep -af remote-debugging-port=9351` zeigt es, `pkill`
      raeumt es weg.

      NACHMESSEN:
        node tools/marke-grenzfaelle.mjs --pruefen   # haengt in pruefen.sh

  # ══ Admin-Board: Deckung und Funktion gegen den alten PHP-Admin ════════════
  - kind: architektur
    id: admin-deckung-g2-g3
    title: "Wie weit deckt das neue Admin-Board die Box ab? (G2/G3, gemessen 03.08.2026)"
    match: "Deckungsluecke|Funktionsluecke|welche Config-Keys|PHP-Admin.*fehlt|G2|G3"
    tags: ['admin', 'board', 'konfiguration', 'php', 'inventur', 2026]
    source: "03.08.2026, Pi 5 .169, tools/admin-deckung-inventur.py"
    body: |
      GEMESSEN am 03.08.2026 gegen die laufende Box (.169), wiederholbar mit
      `python3 tools/admin-deckung-inventur.py` (liest nur, auch auf der Box).

      DECKUNG (G2): Die Box fuehrt 89 Schluessel (84 Skalare, 5 Listen). Das
      Board bedient 30 davon — 28 ueber die FELDER-Tabelle in
      `src/backend-api/src/konfiguration.ts`, den Rest abseits davon (Passwort,
      PIN, Version, Akkutyp, zwei Auswahllisten). 59 bleiben ohne Bedienweg:
      MQTT 14 · WLED 10 · Spotify-Zugang 6 · OnOffShim 6 · Chromium 4 ·
      Telegram 3 · MuPiHAT 3 · Luefter 1 · pm2 1 · mupibox 11.

      DIE AUFFAELLIGSTE EINZELLUECKE ist `mupibox.oberflaeche` — der Schalter,
      der entscheidet, WELCHE Oberflaeche der Kiosk startet (klassisch oder
      neu). Vier Leser (chromium-autostart.sh, server.ts, rueckweg.ts,
      NewDesign/index.html), und im ganzen Board kommt das Wort nicht ein
      einziges Mal vor. Beim Erproben braucht man ihn taeglich — er ist nur
      per SSH erreichbar.

      ACHT SCHLUESSEL LIEST NIEMAND (nur die Vorlagen legen sie an):
        mupibox.playerMode     Doppelgaenger; gelesen wird
                               node-sonos-http-api.playerMode aus www.json
        mupibox.AudioDevice    grosses A; daneben liegt audioDevice mit
                               kleinem a und vier Lesern
        mupibox.AudioDevices   die 28er-Auswahlliste zum toten AudioDevice
        mupibox.customTheme    wirksam ist mupibox.theme
        pm2.ramlog             seit systemd gibt es keine pm2-Protokolle mehr
        wled.startup_id        das Boot-Preset wird nie geschickt
        wled.boot_active       der zugehoerige Schalter dazu
        mupihat.current_limit  mupihat_bq25792.py setzt 2200 mA HARTKODIERT

      DREI FELDER ZEIGEN INS LEERE (Feld da, Schluessel fehlt auf der Box):
      `einstellungssperre` → mupibox.einstellungssperre (NEU gegenueber der
      Bestandsaufnahme vom 30.07., die kannte nur zwei), `spotifyPlaylistSuche`
      → spotify.disableScraperForPlaylists, `jellyfinSchluessel` →
      jellyfin.apiKey.

      FUNKTION (G3): 28 PHP-Dateien, daraus 34 Funktionen geprueft. 16 fehlen
      dem Board, 2 gibt es nur als Endpunkt ohne Bedienweg:
        FEHLT       VNC · Cover-Upload · MQTT · Telegram · WLED · Backup-ZIP ·
                    Support-Paket · Chromium-Protokoll · RRD-Diagramme ·
                    Spotify zuruecksetzen · Bildschirm drehen · resX/resY ·
                    Hintergrundbild/Helligkeit · SD-Uebertaktung ·
                    JSON-Editor · Update einspielen
        NUR ENDPUNKT  POST /api/oberflaeche/neuladen (niemand ruft es) und
                    POST /api/telegram/screen (ruft die Box, nicht das Board)

      DREI DAVON SIND ABSICHT und sollten es bleiben: der JSON-Editor (durch
      die Feldtabelle ersetzt), das Bildschirm-Drehen und die drei
      config.txt-Schrauben aus tweaks.php — alle drei schreiben per `sed -i`
      in /boot/config.txt, und genau dieses Muster hat diese Box schon einmal
      unbootbar gemacht ([[mupi-stromausfall-bootdateien]]). Der Kommentar
      ueber SCHRAUBEN in server.ts nennt das bereits als Ausschlussgrund.

      VNC IST KLEINER ALS SEIN RUF: vnc.php hat 13 Zeilen und bettet nur
      `http://<host>:6080/vnc_lite.html` ein. Das ist ein Link, keine Funktion.

      DIE ENTSCHEIDUNG je Luecke ist G4 und gehoert dem Benutzer; die Tabellen
      mit Einschaetzung stehen in BACKLOG.md unter E6.

  - kind: falle
    id: admin-abgleich-grep-luegt
    title: "Der Abgleich „hat das Board das schon?\" luegt auf drei Arten"
    sev: medium
    match: "Deckung.*grep|paths\\(scalars\\)|Kommentar.*Beleg|Dienstname.*vorhanden"
    cause: "Volltextsuche unterscheidet nicht zwischen Bedienelement, Kommentar und Dienstnamen; jq unterschlaegt false"
    fix: "Kommentare wegwerfen, nach Pfadformen statt Blattnamen suchen, dienste.ts nicht zum Board zaehlen, mit Python statt jq zaehlen"
    tags: ['admin', 'inventur', 'werkzeug', 'falle', 2026]
    source: "03.08.2026, beim Bau von tools/admin-deckung-inventur.py"
    body: |
      Drei Falschmeldungen, jede hat einen Anlauf gekostet. Alle drei sehen im
      Ergebnis gleich aus: eine Luecke wird als geschlossen gemeldet.

      1. `jq -r 'paths(scalars)'` UNTERSCHLAEGT jeden booleschen Schluessel
         mit Wert `false` — auf dieser Box elf Stueck, darunter genau
         `mqtt.active`, `telegram.active`, `wled.active`. Wer damit zaehlt,
         uebersieht die ABGESCHALTETEN Funktionen, also die interessanten.
         `jq -r '[paths]'` zeigt sie; besser gleich Pythons json-Modul.

      2. EIN KOMMENTAR IST KEIN BEDIENELEMENT. `konfiguration.ts` begruendet
         im Kopfkommentar, warum es fuer `resX`/`resY` ABSICHTLICH kein Feld
         gibt, und nennt `jsoneditor.php` als abgeloest. Eine Volltextsuche
         nach „resX" und „jsoneditor" findet genau diese Begruendung — und
         meldet beide fehlenden Funktionen als vorhanden. Dieselbe Sorte:
         `['telegram', ['token']]` in der Maskierliste (Geheimnisse
         schwaerzen) sah aus wie eine Telegram-Einstellseite.

      3. EINEN DIENST STARTEN ZU KOENNEN IST NICHT DIESELBE FAEHIGKEIT.
         `src/backend-api/src/dienste.ts` fuehrt die Dienstnamen `mupi_vnc`
         und `mupi_mqtt`. Ein Suchbegriff „vnc" trifft dort — und VNC gilt
         als vorhanden, obwohl es im Board keinen Bildschirm gibt, nur einen
         Start-/Stopp-Knopf.

      UND DIE GEGENRICHTUNG, damit man nicht ueberkorrigiert: wer
      `konfiguration.ts` GANZ ausschliesst (weil es die Feldtabelle selbst
      ist), verliert Jellyfin und Spotify — deren einziger Beleg im Board sind
      die Feldnamen `jellyfinServer` und `spotifyClientId` genau dort. Die
      Datei muss gelesen, aber als Leser eines Schluessels ausgeschlossen
      werden.

      NOCH EINE: Nach dem blossen BLATTNAMEN zu greppen („active", „port",
      „name") liefert Hunderte Falschtreffer. Gesucht wird nach PFADFORMEN
      (`.gruppe.blatt`, `['gruppe']['blatt']`, `gruppe?.blatt`); nur wo keine
      trifft, wird schwach nachgesucht und das Ergebnis GEKENNZEICHNET.
      Grenze dieser Methode: liest jemand die Gruppe FLACH aus einer anderen
      Datei (so wie `config?.playerMode`), sieht der Schluessel verwaist aus.
      Deshalb steht die Vorlagen-Spalte daneben — ein Schluessel, der NUR in
      config/templates/ und harness/seed/ vorkommt, ist wirklich verwaist.

      NACHMESSEN:
        python3 tools/admin-deckung-inventur.py --g2   # Deckung
        python3 tools/admin-deckung-inventur.py --g3   # Funktion

  # ══ Die Marke am GERAET, nicht an der Attrappe ═════════════════════════════
  - kind: falle
    id: attrappe-gruen-geraet-rot-loser-titel
    title: "Attrappe gruen, Geraet rot: der laufende Titel gehoerte zu keinem ganzen Album"
    match: "indikatoren.*weg|Marke fehlt.*Lane|lose.*Titel|keine ganzen Alben"
    tags: ['oberflaeche', 'neu', 'marke', 'lane', 'attrappe', 'messen', 2026]
    source: "03.08.2026, Box .169, tools/marke-am-geraet.mjs"
    body: |
      GEMELDET am 03.08.2026, woertlich: „nun sind die indikatoren bei album
      und titel weg ich wollte ja alles". Ausgeliefert war kurz zuvor
      [[spielt-marke-eine-frage-zwei-formen]] (Marke auch im Raster) und der
      Gegenlese-Commit dazu.

      ── DER NAHELIEGENDE VERDACHT WAR FALSCH ──────────────────────────────
      Vermutet wurde die neue Bremse: `laufendeKennungen()` haengt seit
      [[nachlauf-markierte-die-lane-aber-nicht-das-raster]] auch die
      STUECK-Kennung an `jetztLaeuft().art === 'spotify'`. Greift die zu
      streng, faellt die Lane-Marke ueberall weg. Zweiter Verdacht: die
      Umbenennung `.lane-laeuft` -> `.spielt-marke`, `data-uri` ->
      `data-spielt` sei irgendwo nicht mitgezogen.

      BEIDES AM GERAET WIDERLEGT, mit den echten Antworten der Box:

          /player/local currentPlayer   "spotify"   -> art = 'spotify'
          Raster        Marke da        26x26 px
          Album-Lane    Marke da        26x26 px    (Titel AUS EINEM ALBUM)
          Titel-Lane    Marke da        26x26 px

      Der Vergleich stimmt an allen drei Orten. Auch die CSS-Regel gilt
      ueberall — nachgemessen wurde nicht nur, DASS das Element im Baum
      steht, sondern dass es ein Rechteck von 26x26 px hat.

      ── DIE URSACHE: EINE SORTE VON TITEL OHNE ORT ────────────────────────
      Die Box spielte „Alles Teil des Plans" aus der Playlist „EMMA6 -
      Complete". `/api/werke/<s>/alben` zerlegt diese Playlist in DREI ganze
      Alben und ZWOELF `lose` Titel — und der laufende war einer der losen.

      Fuer lose Titel stand in der Album-Lane bis dahin nur ein SATZ („12
      Titel gehören zu keinem ganzen Album"). Keine Kachel. Es gab also
      nichts, was eine Marke haette tragen koennen — und die zwoelf Titel
      waren aus der Lane ueberhaupt nicht zu erreichen, weder zu starten
      noch weiterzuhoeren.

      Vorher fiel das nicht auf, weil dann ALLE drei Orte stumm blieben und
      das stimmig aussah. Erst die neue Raster-Marke stellte den Widerspruch
      auf einen Bildschirm: oben markiert, unten nichts.

      ── DIE LEHRE, UND SIE IST TEURER ALS DER FEHLER ──────────────────────
      DREI Werkzeuge meldeten „keine Abweichung": lane-marken-schau,
      raster-marke-schau, marke-grenzfaelle. Alle drei messen gegen
      tools/neu-vorschau.mjs — und alle drei spielten einen Titel AUS EINEM
      ALBUM. Wieder [[attrappe-luegt-durch-weglassen]], aber eine Stufe
      groeber als bisher: Diesmal fehlte weder ein Feld
      ([[attrappe-stirbt-statt-zu-luegen]]) noch der zeitliche Verlauf
      (Titelwechsel) noch der Widerspruch (Nachlauf), sondern eine ganze
      SORTE VON DATEN, die es auf jeder Box gibt.

      EINE PRUEFUNG, DIE EINE SORTE NIE STELLT, IST FUER DEREN REGRESSION
      BLIND — und zwar dauerhaft, nicht zufaellig. Die Frage vor jeder
      Attrappen-Messung lautet deshalb nicht „stelle ich den Fall?", sondern
      „welche Sorten gibt es ueberhaupt, und stelle ich von jeder eine?".

      ── GERICHTET ────────────────────────────────────────────────────────
      Die losen Titel bekommen in `albenOeffnen` eine eigene Kachel „Einzelne
      Titel". Sie ist ein Album wie jedes andere, nur eines, das der Server
      nicht gebildet hat: dieselbe Schleife, dieselben `data-spielt`-
      Kennungen, dieselbe Titel-Lane. In `spieltMarkieren` kam KEINE Zeile
      dazu — genau dafuer ist [[spielt-marke-eine-frage-zwei-formen]] gebaut.
      `weiterAb` kommt unveraendert vom Server (hoechstens ein loser Titel
      traegt eine Stelle, und deren `titelNr` ist schon 1-basiert).

      EINE ZWEITE, KLEINE SACHE FIEL DABEI AN: In `titelListeBauen` hatte das
      Albumbild Vorrang vor dem des Stuecks. Fuer ein Album ist das richtig
      (dessen Stuecke bringen gar kein `bild` mit), fuer die losen Titel
      falsch — das sind Singles mit je eigenem Cover, und jede haette das
      Bild der ersten getragen. Reihenfolge umgedreht: `t.bild || album.bild`.

      NACHMESSEN:
        node tools/marke-am-geraet.mjs --pruefen     # gegen die BOX
        node tools/marke-grenzfaelle.mjs --pruefen   # Fall 4, gegen die Attrappe

  - kind: howto
    id: marke-am-geraet-spiegel
    title: "tools/marke-am-geraet.mjs — die Oberflaeche mit den ECHTEN Antworten der Box messen, ohne sie zu veraendern"
    match: "marke-am-geraet|Spiegel der Box|gegen die Box messen|Attrappe gruen"
    tags: ['werkzeug', 'oberflaeche', 'neu', 'marke', 'messen', 'box', 2026]
    source: "03.08.2026"
    body: |
      WOZU: Alle Marken-Werkzeuge messen gegen tools/neu-vorschau.mjs. Am
      03.08.2026 hat genau dieser Abstand eine Regression durchgelassen
      ([[attrappe-gruen-geraet-rot-loser-titel]]). Dieses Werkzeug schliesst
      ihn: dieselbe Oberflaeche aus dem Arbeitsverzeichnis, aber die echten
      Antworten der Box.

      ── DER KNIFF: EIN SPIEGEL, DER NUR LIEST ─────────────────────────────
      Die Oberflaeche einfach im Browser gegen die Box zu oeffnen waere
      FALSCH — `merkStand()` schickt beim Laufen ein `POST /api/weiterhoeren`
      und schriebe eine Weiterhoeren-Stelle in die resume.json. Die Messung
      veraenderte das Geraet, das sie misst.

      Der Spiegel (127.0.0.1:8298) reicht deshalb NUR GET weiter. Alles
      andere endet bei ihm und wird mitgeschrieben:

          GET  /api/…, /player/state, /player/local   -> zur Box
          POST /api/weiterhoeren, /api/gespielt       -> verschluckt
          GET  /player/<raum>/<befehl>                -> verschluckt
          /neu/*                                      -> aus NewDesign/

      Die Dateien kommen ABSICHTLICH aus dem Arbeitsverzeichnis und nicht von
      der Box: gemessen werden soll der Stand, an dem gearbeitet wird — ohne
      Bauen und ohne Ausrollen.

      ── WAS-WAERE-WENN, OHNE DIE BOX ANZUFASSEN ──────────────────────────
      `--stueck <uri>` und `--kontext <uri>` tauschen im SPIEGEL aus, was
      `/player/state` meldet. Die Box spielt dabei unveraendert weiter, was
      sie spielt. So laesst sich jede Playlist des Katalogs und jeder Titel
      darin stellen, ohne dem Kind davor die Musik umzuschalten.

      ── WAS ES MISST ─────────────────────────────────────────────────────
      Alle DREI Orte in einem Lauf: Raster -> Tipp -> Album-Lane -> Tipp ->
      Titel-Lane. Die Erwartung kommt aus den Antworten der Box (welches Werk
      traegt `context.uri`, in welchem Album steht `item.uri`), nicht aus
      app.js — sonst prueft man seine eigene Vermutung.

      UND ES MISST SICHTBARKEIT, nicht nur Anwesenheit: Rechteck mindestens
      12x12 px, `visibility`, `opacity`. Waere bei der Umbenennung
      `.lane-laeuft` -> `.spielt-marke` eine CSS-Regel liegengeblieben,
      stuende das Element weiter im Baum und ein Test auf „gibt es das
      Element?" bliebe gruen, waehrend der Benutzer nichts sieht.

      AUFRUF:
        node tools/marke-am-geraet.mjs                 # Tabelle
        node tools/marke-am-geraet.mjs --pruefen       # haengt in pruefen.sh
        node tools/marke-am-geraet.mjs --box 10.0.0.5:8200
        node tools/marke-am-geraet.mjs --stueck spotify:track:…
        node tools/marke-am-geraet.mjs --kontext spotify:playlist:…
        node tools/marke-am-geraet.mjs --laut          # jeden Abruf zeigen

      Es endet still mit 0, wenn keine Box antwortet — in pruefen.sh soll ein
      Arbeitsplatz ohne Box nicht rot werden.

  - kind: architektur
    id: verwaltung-suche-bestand-aus-den-seiten
    title: "Die Einstellungssuche der Verwaltung: der Bestand wird ERHOBEN, nicht gepflegt"
    match: "Einstellung suchen|Suchbestand|such-bestand|verwaltung-suchbestand|Suche Verwaltung|Schalter nicht gefunden"
    tags: ['verwaltung', 'admin', 'suche', 'werkzeug', 'test', 'angular', 2026]
    source: "03.08.2026"
    body: |
      WOZU SIE ZUERST KAM: Einstellungen sollen zwischen Konfiguration,
      Darstellung, Medien und einem neuen Streaming-Reiter wandern. Danach
      steht nichts mehr dort, wo der Benutzer es zuletzt gesehen hat. Die
      Suche ist die VERSICHERUNG fuer diesen Umbau, nicht seine Zugabe —
      deshalb steht sie vor ihm.

      ── ZWEI ZUSAGEN, die sie von einer ueblichen Suche unterscheiden ────
      1. Gesucht wird der SICHTBARE TEXT, nicht der Feldname. Man tippt
         „Doppelte", nicht `verschmelzen` oder `ruhigeMarke`. Der Bestand
         enthaelt deshalb ausschliesslich das, was auf dem Knopf steht.
      2. Der Treffer NENNT DIE SEITE: „Doppelte zusammenfassen — Darstellung
         › Elemente". Ein blosser Sprung sagt gar nichts; so lernt man
         nebenbei, wie sortiert ist.

      ── DIE HEIKLE FRAGE: WOHER DER BESTAND KOMMT ───────────────────────
      Eine von Hand gepflegte Liste laeuft auseinander, sobald jemand einen
      Schalter hinzufuegt — und das ist der LEISESTE Fehler, den es gibt: es
      geht nichts kaputt, der Schalter ist bloss nicht mehr auffindbar.

      Zur Laufzeit aus dem Baum lesen geht NICHT: die Seiten sind einzeln
      nachgeladene Routen (loadComponent in app.routes.ts), im Baum steht
      immer nur die EINE offene Seite. Wer alle durchsuchen wollte, muesste
      alle bauen, samt ihrer Abrufe an die Box.

      Deshalb DREI Quellen, alle ohne Pflegeliste:
        * tools/verwaltung-suchbestand.mjs erhebt den Bestand aus den
          VORLAGEN der Seiten und schreibt
          src/frontend-admin/src/app/such-bestand.ts. Erneuern: --schreiben.
          WIE VIELE es sind, steht hier bewusst NICHT mehr (hier stand „190
          Eintraege aus 17 Seiten, Stand 04.08.2026"; am 31.08.2026 waren es
          322 aus 20 — die Zahl waechst mit jeder Seite und war um Faktor 1,7
          daneben). Der Aufruf OHNE `--schreiben` gibt sie aus und ist die
          einzige Stelle, die nicht veraltet.
        * Die Felder der Konfigurationsseite holt die Suche SELBST bei
          /api/konfiguration — dieselbe Quelle, aus der die Seite sie hat.
          Ihre Titel („Höchste Lautstärke") stehen in keiner Vorlage,
          sondern in FELDER (src/backend-api/src/konfiguration.ts).
        * DIE SYSTEMAKTIONEN, seit 04.08.2026 und aus genau demselben Grund:
          „Box neu starten" & Co. stehen in AKTIONEN
          (src/backend-api/src/system.ts) und kommen ueber GET /api/system;
          in der Vorlage steht nur {{ a.titel }}. `aktionenHolen()` in
          suche.ts holt sie nach, `AKTION_ORT` macht aus dem gemeldeten
          `bereich` ('medien' | 'box') die Seite. Wieviele solcher Stellen es
          UEBERHAUPT gibt und welche davon ueberhaupt hineingehoeren, steht
          in [[interpolation-faellt-aus-dem-suchbestand]] — mit Zahlen.

      ── DIE PRUEFUNG IST DER EIGENTLICHE TEIL ───────────────────────────
      tools/verwaltung-suche-vollstaendig.test.mjs (haengt in pruefen.sh als
      „Suchbestand der Verwaltung") macht aus dem leisen Fehler einen lauten:
        * Sie erhebt neu und vergleicht ZEICHEN FUER ZEICHEN mit der Datei.
        * Sie schickt eine ERFUNDENE Seite mit allen Bauarten von Knopf
          durch. Ohne das koennte die Erhebung eine ganze Bauart uebersehen —
          dann passt die Liste zu den Seiten und ist trotzdem unvollstaendig.
        * Sie meldet Seiten ohne Kopfleisten-Eintrag (erlaubt: anmeldung) und
          Seiten ohne eine einzige Einstellung (erlaubt: uebersicht).

      ── FALLEN AUS DEM ERSTEN ANLAUF ────────────────────────────────────
      * `[class.an]="w().miniPlayer > 0"` steht so in darstellung.ts. Das >
        IM ATTRIBUTWERT beendete den einfachen Ausdruck `<button[^>]*>`, und
        als Name des Knopfes kam der halbe Aufruf heraus.
      * @if-Bloecke duerfen nur INNERHALB eines Knopfes/einer Beschriftung
        weggeworfen werden. Auf der Darstellungsseite liegt fast die ganze
        Seite in einem @if, das den Reiter abfragt — wer dort Bloecke
        wegwirft, wirft die Seite weg.
      * Fett gesetzt wird zweierlei: der Titel einer Karte (<div><b>…) und
        eine Hervorhebung im Satz (<p>… <b>nicht</b> …). Nur das erste ist
        eine Beschriftung; unterschieden wird am Tag DAVOR.
      * Ein Satz ist keine Beschriftung. Der Punkt am Ende trennt sie
        zuverlaessiger als jede Laengengrenze.
      * Die Erhebung schneidet die Vorlage von der Stelle "template:" bis
        zum naechsten Backtick heraus. Das geht nur, WEIL in Angular-Vorlagen
        keine Backticks stehen duerfen ([[backticks-in-angular-vorlagen]]) —
        die Regel ist hier Voraussetzung, nicht Stil.

      ── SUCHREGELN (suche.filter.ts, mit eigenem spec) ──────────────────
      Umlaute fallen auf den Grundbuchstaben (ä→a, ß→ss), ue→u aber NICHT:
      aus „Neues Passwort" wuerde sonst „Nus Passwort". Typografische
      Anfuehrungszeichen fliegen heraus (im Bestand steht „endet um …", auf
      der Tastatur liegt "). Alle Suchwoerter muessen vorkommen; der RANG
      kommt aber nur vom sichtbaren Text, sonst waere jeder Eintrag der Seite
      „Doppelte" so gut wie der gesuchte Schalter auf der Darstellungsseite.

      ── GESEHEN, NICHT NUR GETESTET (03.08.2026) ────────────────────────
      Im Browser gegen die Attrappe:
        node tools/neu-vorschau.mjs
        cd src/frontend-admin
        npx ng serve --proxy-config ../../tools/verwaltung-vorschau.mjs
      „doppelte" bringt „Doppelte zusammenfassen — Darstellung › Elemente",
      „lautstarke" (ohne Umlaut) die beiden Konfigurationsfelder, ein Klick
      fuehrt hin und raeumt die Liste weg. neu-vorschau.mjs bedient dafuer
      seit heute auch /api/konfiguration — sonst haette die Vorschau kein
      einziges Konfigurationsfeld gezeigt und wieder durch WEGLASSEN
      getaeuscht ([[attrappe-luegt-durch-weglassen]]).

  - kind: entscheidung
    id: ein-rueckweg-fuer-alle-ebenen
    title: "Ein systemweiter Rueckweg statt sechs — feste Ecke, ueber allem, und ausgegraut statt weg"
    match: "systemweiter zurueck|zurueck button|ein Rueckweg|naechsterRueckweg|#zurueck|Ausgang der Ebene"
    tags: ['oberflaeche', 'neu', 'navigation', 'barrierefreiheit', '800x480', 'werkzeug', 2026]
    source: "03.08.2026, gemessen mit tools/rueckweg-schau.mjs gegen tools/neu-vorschau.mjs"
    body: |
      GEWUENSCHT, woertlich: „an die freie stelle soll ein systemweiter
      zurueck button hin".

      ── WAS ES VORHER GAB: SECHS AUSGAENGE IN FUENF GROESSEN ─────────────
        Regal              die ganze Kopfzeile, 64 px hoch
        Lane               ein Pfeil von 44 px in der Lane-Kopfzeile
        Titelliste         derselbe Pfeil noch einmal, eine Ebene tiefer
        Interpretenseite   64 px, links neben Bild und Name
        grosses Cover      64 px, links oben, mit eigenem Schattenwurf
        grosser Player     ein Kreuz von 64 px, rechts oben im Blatt
      Zwei davon lagen unter der 9-mm-Marke (44 px = 6,2 mm), und keiner stand
      da, wo der vorige gestanden hatte. Wer den Weg hinaus suchte, suchte ihn
      auf jeder Ebene neu.

      ── DIE ENTSCHEIDENDE STELLE IST DER Z-INDEX, NICHT DIE ANORDNUNG ────
      Der grosse Player liegt bei z-index 6, das grosse Cover bei 8, beide
      `position: absolute; inset: 0` — sie decken die Kopfzeile ZU. Ein Knopf,
      der in der Kopfzeile haengt, ist unter ihnen unsichtbar, und dann
      braeuchte jede Vollbild-Ansicht doch wieder einen eigenen. Der eine
      Rueckweg ist deshalb `position: fixed` mit z-index 9 und steht als
      GESCHWISTER der Vollbild-Ansichten im Baum, nicht in der Kopfzeile.

      GENAU DAS IST DER FALL, DEN EIN WERKZEUG STELLEN MUSS: Bei z-index 1
      besteht der Knopf jede Groessen- und Lagepruefung und ist trotzdem tot.
      tools/rueckweg-schau.mjs fragt deshalb `elementFromPoint` in der Mitte
      des Knopfes — die Mutationsprobe (z-index 9 -> 1) meldet „der Knopf
      liegt unter gross / unter album-gross und ist nicht zu treffen", und
      zwar NUR auf diesen beiden Ebenen.

      ── ER WANDERT MIT DER SEITENLEISTE, UM 88 px, UND DAS IST RICHTIG ───
      `--zurueck-links` steht auf 96 px (88 Leiste + 8) und unter
      `body.platz-machen` auf 8 px. Faehrt die Leiste beim Blaettern ein,
      rueckt ALLES um 88 px nach links; der Knopf bleibt dabei genau dort, wo
      die Kopfzeile anfaengt. Die Kopfzeile hat dafuer `padding-left: 82px` —
      EINE Zahl fuer beide Lagen, weil in beiden 10 px Luft zum Text bleiben.
      Steht eine Vollbild-Ansicht offen, ist die Leiste immer ausgefahren
      (`platzBeimBlaettern.zeigen()` beim Oeffnen des Players), dort ist die
      Ecke also unverruecklich.

      ── WAS HEISST „ZURUECK" AUF DER STARTSEITE? ────────────────────────
      Nichts — und deshalb ist der Knopf dort AUSGEGRAUT und nicht weg. Ein
      Knopf, der kommt und geht, ist eine Stelle, die man jedes Mal neu sucht;
      ausserdem lernt ein Kind so, WO der Ausgang liegt, bevor es ihn braucht.
      `disabled` sagt es auch einer Sprachausgabe, `pointer-events: none`
      verhindert die Druckbewegung, die sonst etwas verspraeche.

      ── DIE REGEL IST REIN UND LIEGT AN EINER STELLE ────────────────────
      `naechsterRueckweg(lage)` (NewDesign/app.js) beantwortet: Welche der
      uebereinanderliegenden Ebenen ist die oberste? Reihenfolge, und sie ist
      die BAUREIHENFOLGE:
        album-gross -> player -> titelliste -> lane -> interpret -> regal
      `null` heisst „es gibt nichts, wohin" und graut den Knopf aus.

      DIE LAGE WIRD AUS DEM BAUM GELESEN, nicht gemerkt — dieselbe
      Entscheidung wie bei `laneOffen()` und aus demselben Grund
      ([[raster-neuaufbau-vierte-stelle]]): Das Raster wird an mindestens vier
      Stellen neu gebaut, und eine Lane kann dabei verschwinden, ohne dass
      jemand einen Schalter zuruecklegt. Ein gemerkter Zustand waere genau
      dann falsch, wenn es darauf ankommt.

      NACHGEZOGEN WIRD ZWEIMAL: in der BLASENPHASE nach jedem Tipp irgendwo
      auf der Seite (dann ist die gelesene Lage schon die neue) und im
      250-ms-Takt. Der Tipp macht es sofort, der Takt faengt, was spaeter
      kommt — eine Lane entsteht erst, wenn die Alben da sind. Eine Liste von
      Stellen, an denen jemand daran denken muesste, gibt es damit nicht.

      ── ERSETZT, NICHT ERGAENZT ─────────────────────────────────────────
      Alle sechs alten Knoepfe sind aus dem Baum verschwunden. tools/
      rueckweg-schau.mjs zaehlt sie ab (`.lane-zurueck`, `#ag-zurueck`,
      `#gross-zu`, `#interpret-zurueck`, `#regal-zurueck`) — einmal auf der
      Startseite und einmal bei OFFENER Lane, denn die Lane baut ihre
      Kopfzeile erst beim Aufklappen. Der Regal-Kopf bleibt als reine
      Ueberschrift stehen: er sagt, in wessen Regal man steht.

      ── SECHS WERKZEUGE HINGEN AN DEN ALTEN KNOEPFEN ────────────────────
      leiste-platz-schau, lane-nachlese, interpretseite-befehle,
      verschmelzung-befehle, album-gross-schau und e2e/neu-oberflaeche.test
      klickten `#regal-zurueck`, `#interpret-zurueck` oder `#gross-zu`, um
      irgendwohin zu kommen. Alle sechs zeigen jetzt auf `#zurueck`. Wer eine
      Navigationshilfe entfernt, muss die Werkzeuge mitnehmen — sonst
      scheitert der naechste Lauf an etwas, das gar nicht kaputt ist.

      NACHMESSEN:
        node tools/pruef-neu-regeln.js          # die reine Regel
        node tools/rueckweg-schau.mjs --pruefen # die Oberflaeche, 7 Ebenen

  - kind: entscheidung
    id: boxname-teilen-am-wortende-box
    title: '„MixPiBox“ auf zwei Zeilen: geteilt wird am Wortende Box — sonst gar nicht'
    match: "boxnameTeilen|MixPi und Box|Name zweizeilig|Boxname teilen|wappen-oben"
    tags: ['oberflaeche', 'neu', 'benennung', 'mixpi', 'test', 2026]
    source: "03.08.2026"
    body: |
      GEWUENSCHT, woertlich: „MixPiBox soll geteilt werden zu MixPi und Box
      und untereinander stehen links unten".

      DER VORBEHALT, DER VORHER BESPROCHEN WURDE: Seit
      [[benennung-mixpibox-drei-toepfe]] liest die Oberflaeche den KONFIGURIERTEN Namen aus
      `mupibox.host`. Eine feste Teilung nach sechs Zeichen ergaebe bei
      „MixPiZwei" ein „MixPiZ" und ein „wei" — und niemand saehe die beiden
      Stellen je nebeneinander. Genau so hat die Kopfzeile schon einmal ein
      Jahr lang „MuPiBox" behauptet.

      DIE REGEL (NewDesign/app.js, `boxnameTeilen`):
        * Endet der getrimmte Name auf „box" (Gross-/Kleinschreibung egal) UND
          bleibt davor etwas uebrig -> zwei Zeilen, Rest / letzte drei Zeichen.
        * Sonst -> EINE Zeile. Das ist keine Notloesung: „MixPiZwei" hat keine
          zweite Zeile, und eine erfundene waere schlimmer als keine.
        * Leer, null, keine Zeichenkette -> `null`, und der Aufrufer setzt
          NICHTS. Der Rueckfall aus index.html bleibt stehen; wuerde leerer
          Text gesetzt, fiele der Block in der Hoehe zusammen.

      DIE AUSGABE IST DER EINGETRAGENE NAME, ZEICHEN FUER ZEICHEN: aus
      „mupibox" wird „mupi" / „box", nicht „mupi" / „Box". Gross-/
      Kleinschreibung entscheidet ueber das ERKENNEN, nicht ueber die Anzeige.
      „Box" allein wird nicht geteilt — uebrig bliebe eine leere erste Zeile.

      DER REITER-TITEL BLEIBT UNGETEILT (`document.title`): ein Reiter hat nur
      eine Zeile.

      GEPRUEFT WIRD DIE REINE REGEL, NICHT DER BILDSCHIRM: NewDesign/app.js ist
      eine IIFE ohne Exporte. tools/neu-regeln-auszug.py schneidet die Funktion
      AUS DER QUELLE heraus (ueber die Klammern gezaehlt, nicht ueber eine
      Endmarke — die Funktionen sind eingerueckt, ein `\n}` gibt es nicht), und
      tools/pruef-neu-regeln.js zieht den Auszug bei JEDEM Lauf neu, bevor es
      prueft. Ein Auszug, den man von Hand erneuern muesste, waere beim
      naechsten Mal veraltet und meldete gruen
      ([[veraltete-vorschau-meldet-falsches-rot]]).

      UND DIE ATTRAPPE MUSSTE MITZIEHEN: tools/neu-vorschau.mjs meldete unter
      `mupibox.host` bis dahin „MixPiBox Vorschau" — ein Name, der NICHT auf
      „Box" endet. Wer die Teilung dagegen gemessen haette, haette den
      ungeteilten Fall gesehen und fuer den geteilten gehalten. Der Standard
      heisst jetzt „VorschauBox", und es gibt drei Sorten:
        /vorschau/name-mitbox   /vorschau/name-ohnebox   /vorschau/name-lang

      DER LANGE NAME IST GEDECKELT (3 Zeilen, `-webkit-line-clamp`): ohne
      Deckel braucht „Kinderzimmer Erdgeschoss Box" FUENF Zeilen. Die
      Kategorien sind BEDIENUNG, der Name ist Auskunft — die Abwaegung ist
      eindeutig. tools/rueckweg-schau.mjs misst den verbleibenden Platz mit.

      DIE ZAHLEN DANEBEN WAREN FALSCH, und zwar von Anfang an. Hier stand
      „das Wappen dann 150 der 460 px der Leiste. Die vier Kategorien brauchen
      274; ab sechs Zeilen faengt die Liste an zu rollen und eine Kategorie
      rutscht unter den Rand." NACHGEMESSEN am 05.08.2026 mit
      tools/leiste-geometrie-messen.mjs:
        * das Wappen ist bei drei Zeilen 117 px hoch (mit Bild), nicht 150;
        * die vier Kategorien brauchen 282 px (4 x 66 + 3 x 6), nicht 274 —
          die 274 stammen aus der Zeit vor `--griff: 66px`;
        * UND DIE LISTE ROLLT BEI KEINER ZEILENZAHL. scrollHeight war in jeder
          gemessenen Lage gleich clientHeight (265 = 265).
      WAS STATTDESSEN GESCHIEHT, ist der schlechtere der beiden Faelle: Die
      vier Knoepfe SCHRUMPFEN von 66 auf 61,8 px, weil `.kat` `height` traegt
      (nicht `min-height`) und `flex-shrink` auf 1 steht. Eine rollende Liste
      sieht man; ein um 4,2 px kleineres Ziel sieht niemand — es aendert nur
      die Millimeter (33 statt 22 zu kleine Ziele im ganzen Haus). Gegenprobe:
      mit `.kat { flex-shrink: 0 }` rollt sie sehr wohl.
      SEIT DAS WAPPEN KEIN BILD MEHR TRAEGT (05.08.2026 abends, siehe
      [[ein-wunsch-zwei-zielorte-ein-gebautes-ziel]]) ist der Fall entschaerft:
      das Wappen sinkt auf 71 px, den Kategorien bleiben 311 px, die Knoepfe
      stehen wieder auf vollen 66 px.

  - kind: entscheidung
    id: laufende-fassung-kommt-aus-der-herkunft
    title: 'Die „laufende Versionsnummer“: zwei von drei Kandidaten stehen still — gemessen'
    match: "laufende versionsnr|Fassung anzeigen|eigeneCommits|wappen-fassung|welche Version laeuft"
    tags: ['oberflaeche', 'neu', 'versionen', 'herkunft', 'messung', 2026]
    source: "03.08.2026, lesend an Box .169 gemessen"
    body: |
      GEWUENSCHT, woertlich: „ganz unten eine laufende versionsnr klein".

      „LAUFEND" MUSSTE ERST GEKLAERT WERDEN. Es gibt drei Kandidaten, und
      zwei davon stehen still. GEMESSEN, nur lesend, am 03.08.2026:

        package.json `version`      "1.0.0" — seit dem Fork unveraendert; sie
                                    wandert ueber src/deploy.sh als
                                    `herkunft.version` bis in die
                                    Aktualisierungsseite und sieht dort aus
                                    wie eine Angabe.
        mupiboxconfig.json
        `mupibox.version`           GET /api/config auf der Box: das Feld ist
                                    da und LEER. Es beschreibt nur das
                                    Update-Skript des Upstream, das an diesem
                                    Fork nie laeuft. Wer es genommen haette,
                                    haette eine leere Zeile gebaut.
        `herkunft.eigeneCommits`    103 — und diese Zahl steigt mit JEDER
                                    Auslieferung, weil src/deploy.sh sie beim
                                    Bauen aus `git rev-list --count
                                    @{upstream}..HEAD` einstempelt.

      ALSO DIE DRITTE, und daneben der Commit (7 Zeichen). Die Zahl ist die,
      die man lesen und vergleichen kann („die Box hat 103, mein Baum hat
      107"), der Commit ist die, die eindeutig ist. Angezeigt als `103 ·
      d95f86e`, 9 px, ganz unten in der Seitenleiste.

      WOHER SIE KOMMT: `/api/aktualisierung` ist der EINZIGE Weg, auf dem
      herkunft.json heute nach draussen kommt (GET, gemessen: liefert
      `{urteil, installiert, angeboten, grund, kanal, herkunft,
      angebotsQuelle, feedFehler}`). Er fragt dabei das Versionsverzeichnis
      auf GitHub ab (8 s Zeitschranke) und antwortet auch, wenn das misslingt.
      Das ist teuer fuer eine Fussnote — deshalb wird er GENAU EINMAL beim
      Start gerufen und nicht im Takt. Auf einer Kiosk-Box heisst das: einmal
      pro Neustart.

      KEIN NEUER ENDPUNKT, KEINE BACKEND-AENDERUNG. Das war eine Auflage
      dieser Sitzung (in src/backend-api lief parallel eine andere Arbeit),
      hat sich aber ohnehin als das Richtige erwiesen: die Angabe existierte
      schon, sie war nur nirgends zu sehen.

      OHNE ANTWORT BLEIBT DIE ZEILE LEER, nicht „unbekannt" — die Hoehe steht
      trotzdem (`min-height: 11px`), damit beim Eintreffen nichts springt. Ein
      Wort, das nichts sagt, kostet denselben Platz wie die Angabe.

      DIE ATTRAPPE LIEFERT ABSICHTLICH ANDERE ZAHLEN (777 · abcdef7): Wer sie
      auf dem Schirm sieht, weiss, dass er die Vorschau misst und nicht ein
      Ueberbleibsel — dasselbe Muster wie beim Boxnamen
      ([[attrappe-luegt-durch-weglassen]]).

  - kind: falle
    id: wappen-hiess-marke-und-kollidierte
    title: "Das Bild schrumpfte auf 27 px: die neue Klasse `.marke` gab es schon"
    sev: mittel
    match: "wappen|marke-bild|Bild gequetscht|Klasse schon vergeben|clamp\\(22px"
    cause: "die neue Klasse `.marke` (Bild + Name + Fassung in der Seitenleiste) traf dieselben Elemente wie die bestehende `.marke` — die runde Dienst-Plakette auf den Kacheln mit `width: clamp(22px, …, 34px)`"
    tags: ['oberflaeche', 'neu', 'css', 'falle', 2026]
    source: "03.08.2026, am Bildschirm gesehen"
    body: |
      WAS PASSIERTE: Am 03.08.2026 zog das Maskottchen samt Boxname und
      Fassung aus der Kopfzeile in die Seitenleiste. Der Block bekam die
      Klasse `.marke` — und sah falsch aus: das 44-px-Bild war 27 px breit,
      die Namensspalte 29 px, der Name lief in vielen Zeilen aus der Leiste
      heraus.

      DIE URSACHE: `.marke` war seit [[dienst-zeichen-auf-den-kacheln]]
      VERGEBEN. Sie ist die runde Dienst-Plakette auf jeder Kachel und traegt
      `width: clamp(22px, calc(var(--kachel) * 0.19), 34px)` plus `height`
      und `border-radius: 50%`. Beide Regelsaetze trafen dasselbe Element.

      KEIN FEHLER, KEINE MELDUNG, KEIN TEST WURDE ROT. CSS hat keinen
      Namensraum und keine Kollisionswarnung; die zweite Regel gilt einfach
      mit. Gefunden wurde es NUR, weil ein Bildschirmfoto gemacht wurde —
      Zahlen aus dem Baum haetten „Element ist da" gemeldet.

      GERICHTET: Der Block heisst `.wappen` (`.wappen-bild`, `.wappen-name`,
      `.wappen-oben`, `.wappen-unten`, `.wappen-fassung`). Der Name ist auch
      inhaltlich der bessere: eine „Marke" ist in dieser Oberflaeche seit
      jeher etwas, das an einer Kachel klebt.

      DIE LEHRE, und sie ist billig zu befolgen: Vor einer neuen CSS-Klasse in
      einer 2600-Zeilen-Datei einmal `grep -n "^\.name" app.css`. Und: ein
      Umbau der Anordnung ist erst geprueft, wenn er ANGESEHEN wurde.
      tools/rueckweg-schau.mjs misst seitdem ausdruecklich die Bildgroesse
      (`< 40 px` gilt als gequetscht) — die Mutationsprobe dazu ist, das Bild
      auf ein anderes Maskottchen zu stellen.

      ZUSTANDSNACHTRAG (05.08.2026 abends, [[wiki-zustand-hat-haltbarkeit]]):
      `.wappen` traegt KEIN Bild mehr. Es steht oben links im Zeichen „wer
      hoert" (`#ich-bild`) — der Betreiber hat den urspruenglichen Wunsch
      berichtigt, siehe [[ein-wunsch-zwei-zielorte-ein-gebautes-ziel]]. Die
      URSACHE dieses Eintrags (CSS hat keinen Namensraum) gilt unveraendert;
      der Block heisst weiter `.wappen` und traegt weiter Name und Fassung.
      DIE WACHE IST MITGEWANDERT und nicht weggefallen: die `< 40 px`-Messung
      in tools/rueckweg-schau.mjs misst jetzt `#ich-bild`, und
      tools/ich-zeichen-schau.mjs prueft dieselbe Groesse an derselben Stelle.
      Waere sie mit dem Bild ersatzlos verschwunden, haette die neue Stelle
      keine — und genau das ist die Sorte Verlust, die niemand meldet.

  - kind: falle
    id: rueckweg-lag-ueber-der-meldung
    title: "Der eine Rueckweg lag ueber der Meldung — 64 x 64 px taten etwas anderes als der Rest des Schirms"
    sev: hoch
    match: "Meldung z-index|zurueck ueber Meldung|Kinderzeit Tipp|rueckweg-nachlese|meldung 10"
    cause: "`.meldung` lag bei z-index 8, der systemweite Rueckweg bei 9 — ein Tipp in seine Ecke schloss nicht die Meldung, sondern verliess eine Ebene dahinter"
    tags: ['oberflaeche', 'neu', 'navigation', 'css', 'falle', 'barrierefreiheit', 2026]
    source: "03.08.2026, gemessen mit tools/rueckweg-nachlese.mjs gegen tools/neu-vorschau.mjs"
    body: |
      GEFUNDEN BEIM GEGENLESEN von [[ein-rueckweg-fuer-alle-ebenen]], am
      selben Tag, an dem der eine Rueckweg gebaut wurde.

      ── WAS PASSIERTE ───────────────────────────────────────────────────
      `.meldung` (Kinderzeit, „Das kann die Box hier noch nicht abspielen")
      ist `position: absolute; inset: 0` und deckt den ganzen Schirm zu. Sie
      geht nach 4,5 Sekunden von selbst weg — und ein Tipp IRGENDWOHIN
      schliesst sie sofort. Das war ihr ganzer Sinn: warten muss niemand.

      Seit dem 03.08.2026 lag der eine Rueckweg mit z-index 9 ueber ihren 8.
      In seiner Ecke, 64 x 64 px oben links, tat ein Tipp deshalb etwas
      voellig anderes als ueberall sonst: Er schloss die Meldung NICHT,
      sondern verliess eine Ebene DAHINTER, die niemand sieht. GEMESSEN: die
      Lage ging von `regal` auf `(keine)`, waehrend der Schirm unveraendert
      die Meldung zeigte.

      ── WARUM DAS DER TEUERSTE FALL IST, DEN DIESER UMBAU HABEN KONNTE ───
      Der ganze Zweck des einen Rueckwegs ist: EINE Ecke, immer dieselbe, ein
      Kind lernt sie. Genau dieses Kind tippt bei einer Meldung dorthin, wo es
      den Ausgang gelernt hat, sieht keine Wirkung, tippt noch einmal — und
      JEDER Tipp nimmt still eine Ebene weg. Nach 4,5 Sekunden ist die
      Meldung fort und es steht woanders, ohne einen Zusammenhang zu sehen.
      Die Navigationshilfe wird so zur Falle.

      ── GERICHTET: DIE MELDUNG AUF 10 ───────────────────────────────────
      Sie ist die EINZIGE Stufe in app.css, die hoeher liegt als der Rueckweg
      — und das ist die richtige Ausnahme: Die Meldung ist die einzige
      Flaeche, die SELBST ein Ziel ist. Der Ausgang ist dadurch hoechstens so
      lange verdeckt, wie die Meldung zu sehen ist, und ein Tipp auf sie
      raeumt ihn sofort wieder frei.

      Die Gegenrichtung (`zurueckGehen` schliesst erst die Meldung) waere die
      zweite Stelle gewesen, an der jemand an die Meldung denken muss — die
      Stufe ist eine.

      ── DIE LEHRE, UND SIE IST ALLGEMEIN ────────────────────────────────
      Wer ein Element ueber ALLES legt, muss die Liste dessen, was darunter
      liegt, VOLLSTAENDIG durchgehen — nicht nur die Vollbild-Ansichten, an
      die man beim Bauen denkt. Der Umbau hatte Player (6) und grosses Cover
      (8) sauber bedacht; die Meldung (8) stand in derselben Datei und wurde
      uebersehen, weil sie kein Ort ist, den man „aufsucht".
      `grep -n "z-index" app.css` ist ein Befehl.

      NACHMESSEN: node tools/rueckweg-nachlese.mjs --pruefen
      MUTATIONSPROBE: `.meldung` auf z-index 8 -> „ein Tipp in die
      Rueckweg-Ecke schliesst die Meldung nicht".

  - kind: falle
    id: fusszeile-verschwieg-den-unsauberen-bau
    title: "Die Fusszeile nannte einen Commit, aus dem der ausgelieferte Stand gar nicht kam"
    sev: mittel
    match: "unsauber|herkunft.unsauber|d95f86e\\+|fassungBeschriften|welcher Stand laeuft"
    cause: "`fassungHolen` las aus der Herkunft nur `eigeneCommits` und `commit` — `unsauber` (beim Bauen geaenderte, nicht eingecheckte Dateien) blieb ungelesen"
    tags: ['oberflaeche', 'neu', 'versionen', 'herkunft', 'test', 2026]
    source: "03.08.2026, beim Gegenlesen gefunden; src/deploy.sh und aktualisierung.ts gegengelesen"
    body: |
      NACHTRAG ZU [[laufende-fassung-kommt-aus-der-herkunft]].

      DIE FRAGE, DIE DIE ZEILE BEANTWORTEN SOLL, ist „welcher Stand laeuft
      hier wirklich". Genau darauf gab sie im haeufigen Fall die falsche
      Antwort.

      src/deploy.sh stempelt DREI Angaben in herkunft.json:
        commit          der Commit, auf dem der Baum stand
        eigeneCommits   `git rev-list --count @{upstream}..HEAD`
        unsauber        `git status --porcelain | grep -v "^??" | wc -l`
      `/api/aktualisierung` reicht die Herkunft UNVERAENDERT durch (server.ts),
      `unsauber` ist also da. Die Fusszeile las es nicht.

      WER AUS EINEM GEAENDERTEN BAUM AUSLIEFERT — und das ist an dieser Box
      der Normalfall, nicht die Ausnahme — bekam „103 · d95f86e" zu sehen.
      Das behauptet genau das, was nicht stimmt: dass auf der Box der Stand
      von d95f86e laeuft. Man vergleicht mit seinem eigenen Baum bei d95f86e,
      findet keinen Unterschied und sucht den Fehler danach an der falschen
      Stelle.

      GERICHTET: ein PLUS am Commit, „103 · d95f86e+". Kein Wort — Platz ist
      fuer neun Zeichen, und `git describe` haengt seit jeher ein Zeichen an,
      wenn der Baum nicht sauber war. Ohne Commit haengt das Plus an der Zahl
      („103+"), sonst fiele der Hinweis ausgerechnet dort weg, wo ohnehin
      weniger dasteht. Fehlt `unsauber` ganz (aeltere Box, das Feld ist
      optional), gibt es KEIN Plus — was man nicht weiss, behauptet man nicht.

      DIE REGEL IST REIN UND WIRD GEPRUEFT: `fassungBeschriften` steht neben
      `boxnameTeilen` und `naechsterRueckweg` in tools/neu-regeln-auszug.py,
      der Auszug wird bei jedem Lauf neu aus der Quelle gezogen.

      UND DIE ATTRAPPE MUSSTE MITZIEHEN — dieselbe Falle wie beim Boxnamen
      ([[attrappe-luegt-durch-weglassen]]): tools/neu-vorschau.mjs meldete
      fest `unsauber: 0`. Wer dagegen gemessen haette, haette den sauberen
      Fall gesehen und fuer beide gehalten. Es gibt jetzt
      `/vorschau/fassung-unsauber` und `/vorschau/fassung-sauber`.

      NACHMESSEN:
        node tools/pruef-neu-regeln.js
        node tools/rueckweg-nachlese.mjs --pruefen

  - kind: architektur
    id: rueckweg-nachlese-werkzeug
    title: "tools/rueckweg-nachlese.mjs — die Ecken, die rueckweg-schau offen laesst"
    match: "rueckweg-nachlese|Nachlese zum Rueckweg|Kontrast des Knopfes|Titelliste Interpretenseite pruefen"
    tags: ['werkzeug', 'oberflaeche', 'neu', 'navigation', 'barrierefreiheit', 2026]
    source: "03.08.2026"
    body: |
      WOZU EIN ZWEITES WERKZEUG NEBEN [[ein-rueckweg-fuer-alle-ebenen]]:
      tools/rueckweg-schau.mjs stellt sieben Sorten Ebene und misst Groesse,
      Lage, Ausgegrautsein und „ein Tipp, eine Ebene". Das ist die Hauptsache.
      Die Nachlese fragt, was DANEBEN liegt — und jede ihrer fuenf Fragen
      hatte beim ersten Lauf ein Ergebnis:

        1. DIE TITELLISTE AUF DER INTERPRETENSEITE. Sie entsteht in
           `albumTitelOeffnen`, die im Raster in `titelOeffnen` — zwei
           Funktionen, zwei Elternteile, EINE Ebene. rueckweg-schau stellt nur
           die im Raster. Ergebnis: in Ordnung, und jetzt auch bewacht.
        2. DIE MELDUNG. Ergebnis: ein echter Fehler, siehe
           [[rueckweg-lag-ueber-der-meldung]].
        3. DIE FUSSZEILE BEI EINEM UNSAUBEREN BAU. Ergebnis: ein echter
           Fehler, siehe [[fusszeile-verschwieg-den-unsauberen-bau]].
        4. DER KONTRAST DES KNOPFES auf jedem seiner drei Untergruende, in
           hell UND dunkel. Ergebnis: in Ordnung — und eine Erkenntnis, die
           die Begruendung im Quelltext praezisiert (unten).
        5. DIE KONSOLE auf dem ganzen Weg. Ergebnis: nur `/active_theme.css`
           und `/favicon.ico`, beides Luecken der ATTRAPPE und keine der
           Seite. Sie sind im Werkzeug benannt und geduldet — ein Werkzeug,
           das immer rot ist, liest bald niemand mehr.

      ── WAS DIE KONTRASTMESSUNG WIRKLICH GEZEIGT HAT ────────────────────
      app.css begruendet die Fuellung des Knopfes mit „ein Pfeil ohne eigene
      Flaeche verschwindet auf einem der Untergruende". GEMESSEN (800x480,
      Chromium, 03.08.2026) traegt die Fuellung nur an EINER Stelle:

        Untergrund              Fuellung : Grund     Pfeil : Grund
        Kopfzeile hell                1,16 : 1          13,09 : 1
        grosses Cover hell            1,16 : 1          13,09 : 1
        grosser Player hell          11,33 : 1           1,00 : 1   <-- hier
        Kopfzeile dunkel              1,48 : 1          15,08 : 1
        grosser Player dunkel         1,17 : 1          11,92 : 1

      Auf dem abgedunkelten Grund des grossen Players in HELL ist der Pfeil
      (`--ink`) vom Untergrund nicht zu unterscheiden — 1,00 : 1. Dort und nur
      dort ist es die Fuellung, die den Knopf traegt. Ueberall sonst ist es
      umgekehrt. Beides zusammen ist nie schlecht, und genau das prueft das
      Werkzeug: den BESSEREN der beiden Wege gegen die 3 : 1 aus WCAG 1.4.11.
      Jeden fuer sich zu fordern waere falsch — die Norm verlangt Kontrast fuer
      das, WORAN man das Bedienelement erkennt, nicht fuer jede seiner Kanten.

      ── EIN BEFUND OHNE REPARATUR, ABSICHTLICH ──────────────────────────
      Die Regal-Zeile (`.regal-name`) war bis 03.08.2026 SELBST der Rueckweg
      — die ganze Zeile war ein Knopf. Sie traegt weiter dieselbe Fuellung
      (`--line2`), denselben Radius (18px) und dieselben 64 px Hoehe wie der
      neue Knopf, nur den Pfeil hat sie verloren. Gemessen: alle drei Werte
      identisch, `tippbar: nein`. Wer gelernt hat, dass man sie antippt, tippt
      weiter ins Leere. Das Werkzeug meldet es als HINWEIS und nicht als
      Fehler — es ist eine Frage der Gestaltung, nicht der Richtigkeit, und
      die Zeile sagt weiterhin richtig, in wessen Regal man steht.

      EIGENER FERNSTEUERPORT (9364): rueckweg-schau nimmt 9361. Zwei Werkzeuge
      auf demselben Port haengen sich gegenseitig auf, sobald beide in
      tools/pruefen.sh stehen — und dort stehen sie jetzt beide.

      AUFRUF
        node tools/rueckweg-nachlese.mjs
        node tools/rueckweg-nachlese.mjs --pruefen

  # ══ Die Gliederung des Verwaltungs-Boards ══════════════════════════════════
  - kind: entscheidung
    id: admin-gliederung-leitfrage
    title: "Das Board sortiert nach einer Leitfrage, nicht nach der Datei (G4/G5/G6)"
    match: "wohin gehoert (der|die|das) Schalter|Board.*Gliederung|welche Seite.*Feld|Konfiguration oder Darstellung"
    tags: ['admin', 'board', 'gliederung', 'entscheidung', 2026]
    source: "03.08.2026, umgesetzt in konfiguration.ts (BEREICHE) + drei Seiten der Verwaltung"
    body: |
      ENTSCHIEDEN am 03.08.2026, nachdem G2/G3 gezeigt hatten, dass vierzehn Seiten
      ohne Gruppierung nicht mehr zu ueberblicken sind. Die Frage „wohin gehoert
      dieser Schalter?" wird ab jetzt NICHT nach Gewohnheit beantwortet
      („steht doch in mupiboxconfig.json, also Konfiguration"), sondern nach
      einer Leitfrage:

          Konfiguration = was die Box IST        (Netz, Zugaenge, Hardware)
          Darstellung   = was man SIEHT          (Farben, Groessen, Bewegung)
          Medien        = was DA IST             (Werke, Quellen, Doppeltes)

      WAS DARAUS FOLGTE, jeder Punkt einzeln:

        thema (mupibox.theme)          Konfiguration -> Darstellung
            Ein Farbthema ist die reinste Form von „was man sieht". Es lag
            nur deshalb bei der Konfiguration, weil es in derselben Datei
            steht wie der Name der Box.

        oberflaeche (mupibox.oberflaeche)  NEU -> Darstellung
            Entscheidet, WELCHE der beiden Oberflaechen der Kiosk startet.
            Sie steht neben der Live-Vorschau, weil genau dort verglichen wird.

        kioskModus (chromium.kiosk)    Konfiguration -> Darstellung
            „Vollbild oder Browserleisten" ist eine Aussage darueber, was auf
            dem Schirm zu sehen ist.

        bildlauf (chromium.sccrollanimation)  Konfiguration -> Darstellung
            Bewegung. Die Leitfrage nennt sie ausdruecklich.

        grafikbeschleunigung (chromium.gpu)  BLEIBT bei Konfiguration
            Klingt nach Aussehen, ist aber eine Hardware-Eigenschaft: sie
            entscheidet nicht, WAS zu sehen ist, sondern ob es stabil laeuft.

        Spotify (4 Felder) und Jellyfin (2)  Konfiguration -> Streaming-Dienste
            Ein Anbieter ist keine Eigenschaft der Box, sondern eine Quelle.

        verschmelzen  Darstellung -> Medien
            Aendert die ANZAHL der Werke: aus zwei Kacheln wird eine. Es
            raeumt den Bestand auf, es faerbt ihn nicht.

        diskografie   Darstellung -> Medien
            Loest NETZABRUFE aus, einen je erkanntem Interpreten. Was Daten
            HOLT, ist keine Darstellung.

      ── WIE DAS TECHNISCH GEHT, OHNE ZWEITE WAHRHEIT ────────────────────
      Die Zuordnung steht als PFLICHTFELD `bereich` an jedem Eintrag der
      FELDER-Tabelle in src/backend-api/src/konfiguration.ts, dazu die Tabelle
      BEREICHE mit `seite: 'konfiguration' | 'darstellung' | 'streaming'`.
      /api/konfiguration liefert beides aus; die drei Seiten FILTERN, sie
      pflegen keine eigene Liste. Stuende die Zuordnung in den Vorlagen, gaebe
      es sie dreimal.

      DIE FALLE, die das aufmacht, ist dieselbe wie bei der feldweisen
      Uebernahme in darstellung.service.ts: ein Feld ohne (oder mit falschem)
      Bereich erscheint auf KEINER Seite — kein Fehler, keine Meldung, es ist
      weg. Dagegen gibt es drei Tests in konfiguration.spec.ts: jedes Feld
      nennt einen bekannten Bereich, kein Bereich bleibt leer, und jeder
      Bereich zeigt auf eine der drei Seiten.

      ── WAS BEIM VERSCHIEBEN NICHT PASSIERT IST, UND WARUM ──────────────
      KEIN einziges Feld hat den SPEICHERORT gewechselt. `verschmelzen` und
      `diskografie` liegen weiter in darstellung.json, die Konfigurationsfelder
      weiter in mupiboxconfig.json. Nur die BEDIENUNG ist gewandert. Das ist
      Absicht: ein Feld, das durch pruefen() der darstellung.service.ts reist,
      nimmt jeder Box, die den Schalter schon gestellt hat, still ihre
      Einstellung. Wer spaeter doch umzieht, braucht eine Uebernahme des
      Bestands samt Test.

      DIE ZWEITE FALLE DABEI, gefunden beim Umbau: die Darstellungsseite
      schreibt per PUT den GANZEN Stand. `verschmelzen` und `diskografie`
      muessen deshalb in ihrer Schnittstelle UND in STANDARD stehenbleiben,
      obwohl sie dort nicht mehr bedient werden. Wer sie „aufraeumt", loescht
      sie bei der naechsten Aenderung an der Darstellung aus darstellung.json.

      ── DIE SUCHE MUSSTE MIT ────────────────────────────────────────────
      suche.ts schickte jeden Konfigurationstreffer nach /konfiguration. Das
      war richtig, solange es EINE Seite gab, und wurde mit dem Umzug still
      falsch — ein Treffer, der auf die falsche Seite fuehrt, ist schlimmer
      als keiner, weil man dort weitersucht. Sie bildet jetzt `bereich` ->
      `seite` ab, mit /konfiguration als Rueckfall.

      NACHMESSEN:
        node tools/verwaltung-suchbestand.mjs --schreiben
        node --test tools/verwaltung-suche-vollstaendig.test.mjs
        cd src/backend-api && npm run test

  - kind: falle
    id: conf-update-loescht-schluessel-den-noch-jemand-liest
    title: "Der Umzug NIMMT einen Schluessel weg — und die Warteschleife wird zur Dauerschleife"
    sev: high
    match: "conf_update\\.sh.*del|mediaCheckTimer|change_checker|sleep null|ungueltiges Zeitintervall|CHECK_TIMER"
    cause: "update/conf_update.sh loescht `.mupibox.mediaCheckTimer` bei JEDEM Lauf ersatzlos; change_checker.sh setzt den Wert in `sleep` ein"
    fix: "Loeschzeile entfernen (nicht nur nachlegen — sie frisst auch den EINGESTELLTEN Wert), `standard` am Feld, tools/konfig-umzug-probe.py in pruefen.sh"
    tags: ['admin', 'konfiguration', 'migration', 'update', 'dienste', 'falle', 2026]
    related: [medienwache-sieht-nur-die-kategorieebene]
    source: "03.08.2026, tools/konfig-umzug-probe.py (fuehrt conf_update.sh gegen eine nachgebaute Box wirklich aus)"
    body: |
      DIE ZEILE, um die es geht, steht seit 1.0.8 in update/conf_update.sh:

          /usr/bin/jq 'del(.mupibox.mediaCheckTimer)'

      Sie gehoert zu einem Dreierblock — `googlettslanguages`,
      `mediaCheckTimer`, `AudioDevices` werden gemeinsam entfernt, und drei
      Zeilen spaeter wird nur `googlettslanguages` wieder angelegt. Im
      Ursprungsprojekt ist das richtig: dort mustert
      start_mupibox_update.sh auch `mupi_change_checker.service` aus.

      IN DIESEM FORK IST ES FALSCH. Der Leser lebt noch:
      scripts/mupibox/change_checker.sh, config/services/mupi_change_checker.service,
      und seit dem Verwaltungsumbau vom 03.08.2026 gibt es sogar ein Feld dafuer
      („Medien pruefen alle … Sekunden").

      WAS PASSIERT, Schritt fuer Schritt:

          CHECK_TIMER=$(jq -r .mupibox.mediaCheckTimer $CONFIG)   # -> "null"
          ...
          sleep ${CHECK_TIMER}    # sleep: ungueltiges Zeitintervall »null«, Code 1

      Der `sleep` am Schleifenende faellt sofort durch. Aus der Warteschleife
      wird eine Dauerschleife, die `stat` ueber jedes Medienverzeichnis jagt —
      auf einem Pi, der nebenher Musik abspielt. Es gibt keine Meldung, keinen
      Fehlerzustand des Dienstes, nichts auf dem Bildschirm.

      WARUM NACHLEGEN ALLEIN NICHT REICHT, und das ist die eigentliche Lehre:
      Der erste Anlauf ergaenzte nur ein „falls fehlt, setze 300" am Ende des
      Skripts. Damit war der Fall „Schluessel fehlt" behoben — aber der Fall
      „Box hat 600 eingestellt" nicht: das Skript laeuft von oben nach unten,
      Zeile 10 loescht die 600, und unten kommt 300 zurueck. Der Test sah es
      sofort, der Quelltext nicht.

      WARUM DIE BESTANDSAUFNAHME ES NICHT FAND: Sie fragte die BOX, welche
      Board-Schluessel dort fehlen — und dort stand `mediaCheckTimer` noch. Er
      verschwindet erst beim naechsten Update. Ein Abgleich gegen einen
      IST-Stand kann diese Fehlerklasse gar nicht sehen. Man muss das
      Migrationsskript laufen lassen.

      DIE REGEL DARAUS: Ein `del(...)` in conf_update.sh braucht denselben
      Beleg wie ein „abgeloest" an einem Dienst — WER liest den Schluessel
      nicht mehr, und woher wissen wir das? Ohne Beleg ist es eine Behauptung
      ueber Code, den man gerade nicht ansieht.

      NACHMESSEN:
        python3 tools/konfig-umzug-probe.py          # Bericht
        python3 tools/konfig-umzug-probe.py --pruefen  # haengt im Gesamtlauf
        CHECK_TIMER=null; sleep $CHECK_TIMER; echo $?  # -> 1

  - kind: falle
    id: board-feld-ohne-schluessel-auf-der-box
    title: "„Feld zeigt ins Leere\" heisst nicht falsch benannt — die Box ist aelter"
    sev: medium
    match: "Feld.*ins Leere|Schluessel fehlt auf der Box|einstellungssperre.*fehlt|disableScraperForPlaylists"
    cause: "Die Vorlage legt den Schluessel an, aber nur bei einer NEUEN Installation; eine laufende Box sieht sie nie wieder"
    fix: "standard am Feld (bestimmte Anzeige) PLUS eine Zeile in update/conf_update.sh (der Schluessel entsteht wirklich)"
    tags: ['admin', 'board', 'konfiguration', 'migration', 'falle', 2026]
    source: "03.08.2026, tools/admin-deckung-inventur.py gegen Pi 5 .169"
    body: |
      DIE MELDUNG des Abgleichs lautet „3 Felder des Boards, deren Schluessel
      es auf der Box nicht gibt": `einstellungssperre` ->
      `mupibox.einstellungssperre`, `spotifyPlaylistSuche` ->
      `spotify.disableScraperForPlaylists`, `jellyfinSchluessel` ->
      `jellyfin.apiKey`.

      DER NAHELIEGENDE SCHLUSS IST FALSCH. Es sind keine Tippfehler und keine
      abweichenden Namen — alle drei Pfade stehen woertlich in
      config/templates/mupiboxconfig.json. Nachgesehen:

          python3 -c "import json;d=json.load(open('config/templates/mupiboxconfig.json'));print(sorted(d['spotify']))"

      Was fehlt, ist der Schluessel auf DIESER Box. Die Vorlage schreibt eine
      NEUE Installation; eine Box, die schon laeuft, bekommt sie nie wieder zu
      sehen. Der Weg zu einer laufenden Box heisst update/conf_update.sh — und
      dort standen die drei nicht.

      WARUM ES TROTZDEM „RICHTIG" AUSSAH, und das ist der eigentliche Fehler:
      ohne Wert lieferte /api/konfiguration `undefined`, das Auswahlfeld zeigte
      daraufhin seinen ERSTEN Eintrag („Aus — jeder kommt hinein"). Der
      angezeigte Zustand stimmte also — aus Zufall, nicht aus Absicht. Am Tag,
      an dem die erste Auswahl eine andere ist, zeigt die Seite etwas an, das
      nirgends steht.

      REPARIERT AM 03.08.2026, in zwei Haelften, die beide noetig sind:
        1. `standard` am Feld (konfiguration.ts) — die Anzeige ist bestimmt,
           und der Wert ist DER, DER OHNEHIN GILT (`aus`, `false`, leer).
           `holeWert` bleibt bewusst ROH, `holeWertOderStandard` ist neu:
           sonst meldete `unterschiede` keine Aenderung, obwohl der Schluessel
           neu angelegt wird.
        2. Vier Zeilen in update/conf_update.sh (die drei plus
           `mupibox.oberflaeche`) — erst damit entsteht der Schluessel wirklich.

      DIE PRUEFUNG dagegen steht in konfiguration.spec.ts unter „Fehlende
      Schluessel haben eine bestimmte Antwort".

  - kind: falle
    id: mqtt-name-fehlt-dienst-still-tot
    title: "mupi_mqtt: enabled und trotzdem tot — mqtt.py liest einen Schluessel, den niemand anlegt"
    sev: medium
    match: "mupi_mqtt.*(laeuft nicht|tot|enabled)|mqtt\\.name|mqtt.active wirkt nicht|KeyError.*mqtt"
    cause: "mqtt.py liest jsonconfig['mqtt']['name']; weder Vorlage noch conf_update.sh legen diesen Schluessel an"
    fix: "mqtt.name anlegen (oder im Skript auf mupibox.host ausweichen) UND Restart=on-failure statt on-abort"
    tags: ['mqtt', 'dienste', 'konfiguration', 'falle', 2026]
    source: "03.08.2026, tools/mqtt-schluessel-abgleich.py (nur Quelltext, ohne Box)"
    body: |
      GEFRAGT WAR: warum ist `mupi_mqtt` eingeschaltet, laeuft aber nicht?

      DER ERSTE VERDACHT — „`mqtt.active` steht auf false" — IST WIDERLEGT.
      mqtt.py liest den Schalter in `mqtt_active` (Zeile 908) und wertet ihn
      danach NIRGENDS aus. Dasselbe gilt fuer `mqtt.debug`. Das sind zwei
      Schalter, die nichts schalten; abgeschaltet wird MQTT allein dadurch,
      dass man den Dienst nicht startet.

      WAS DAS WERKZEUG STATTDESSEN FINDET:

          mqtt.py liest 14 Schluessel unter `mqtt`.
          Repo (Vorlage + conf_update.sh) legt 13 an.
          Nirgends angelegt:  mqtt.name   (mqtt.py:905, ganz am Anfang von main)

      Ein fehlender Schluessel ist in Python ein `KeyError` — noch bevor
      irgendeine Verbindung versucht wird. Der Prozess endet mit Code 1, also
      „exited", nicht „aborted". Und die Unit sagt:

          Restart=on-abort

      `on-abort` greift bei einem gewoehnlichen Fehlerausstieg NICHT. Ergebnis:
      der Dienst ist tot, systemd versucht es nie wieder, und
      `systemctl is-enabled` sagt weiter „enabled". Genau das Bild, das
      gemeldet wurde.

      NOCH EINE FALLE IM SELBEN SKRIPT, falls es doch startet:
      `check_server_availability` gibt NIE `False` zurueck — es ist eine
      Endlosschleife mit Wiederholung. Ein unerreichbarer Broker haelt den
      Dienst also im Zustand „active (running)", ohne dass er je etwas tut.
      „Laeuft" heisst bei diesem Dienst nicht „funktioniert".

      ── NACHTRAG 03.08.2026 ABENDS: DAS STAND HIER SCHON ───────────────

      Und das ist die eigentliche Lehre dieses Eintrags. [[mupi-mqtt-reparatur]]
      hat am 30.07.2026 GENAU DIESE DREI Befunde am Geraet gehabt — fehlende
      Module, fehlendes `mqtt.name`, wirkungsloser `active`-Schalter — und sie
      auf BEIDEN Boxen behoben. Der Abschnitt oben wurde am 03.08. aus dem
      Quelltext neu hergeleitet, ohne vorher nachzusehen. Kosten: ein
      Werkzeug, das eine beantwortete Frage beantwortet.

      DER BEFUND BLEIBT TROTZDEM GUELTIG — nur ist er ein anderer, als er
      aussah. Repariert wurde im remote-step-installer (Rezeptschritt
      `mqtt-reparieren`) und auf den Geraeten. IN DIESEM REPO fehlt beides
      weiterhin:

          config/templates/mupiboxconfig.json   kein `mqtt.name`
          update/conf_update.sh                 legt `mqtt.name` nicht nach
          config/services/mupi_mqtt.service     keine ExecCondition-Ergaenzung

      Eine frische SD aus diesem Repo bringt den Fehler also zurueck. Das ist
      ein Fall fuer das Zwei-Repos-Delta (BACKLOG E12), kein neuer Fehler.

      WARUM DIE BEIDEN TEILE NUR ZUSAMMEN GEHEN: `mqtt.name` allein
      nachzulegen macht den Dienst startfaehig — und weil `active` nichts
      schaltet, versucht er dann auf JEDER frischen Box, den Beispiel-Broker
      `mqtt-example-broker.com` zu erreichen. Erst die ExecCondition macht
      „nicht eingerichtet" wieder zu „uebersprungen" statt zu „laeuft ins
      Leere". Wer nur den Schluessel nachtraegt, tauscht einen toten Dienst
      gegen einen sinnlos laufenden.

      WARUM MQTT DESHALB NICHT INS BOARD KAM (G4, 03.08.2026): 14 Felder zu
      bauen fuer einen Dienst, dessen Hauptschalter im Original nichts tut und
      der aus diesem Repo heraus beim Start stirbt, waere Pflegeaufwand fuer
      eine Fassade. Erst den Ausrollweg nachziehen, dann bedienbar machen.
      Telegram (3 Felder, 17 Leser, wirksamer Schalter) ist stattdessen
      hineingekommen.

      NACHMESSEN:
        python3 tools/mqtt-schluessel-abgleich.py

  - kind: entscheidung
    id: dienst-abgeloest-gegen-auf-dieser-box-aus
    title: "„Abgeloest\" ist nicht „aus\": zwei Einstufungen, die man nicht verwechseln darf"
    match: "abgeloest.*Dienst|mupi_vnc.*abgeloest|Dienste.*Abschnitt|pulseaudio.*abgeloest"
    tags: ['dienste', 'admin', 'board', 'entscheidung', 2026]
    source: "03.08.2026, src/backend-api/src/dienste.ts (ABGELOEST, HINWEIS, abschnittVon)"
    body: |
      ENTSCHIEDEN am 03.08.2026. Die Systemdienste-Seite kannte bis dahin EINE
      Liste und EINE Markierung („abgeloest"). Das reichte nicht, weil drei
      voellig verschiedene Sachverhalte gleich aussahen:

        ABGELOEST            Es gibt einen NACHFOLGER, der dasselbe tut.
                             Wer den alten einschaltet, hat danach zwei Dinge
                             fuer dieselbe Aufgabe.
                             -> eigener Abschnitt unten, mit Angabe WOVON.

        AUF DIESER BOX AUS   Eine ganz normale Wahlmoeglichkeit, die hier
                             gerade niemand gewaehlt hat: mupi_vnc, mupi_fan,
                             mupi_telegram, mupi_mqtt.
                             -> bleibt in der normalen Liste, ohne Schild.
                             Ein „abgeloest" an mupi_fan haelt jemanden davon
                             ab, den Luefter einzuschalten.

        KOMMT DARAUF AN      pulseaudio. Auf dem Pi 5 uebernimmt PipeWire, auf
                             dem Pi 4 ist es der AKTIVE Tonweg (am Geraet
                             gemessen 30.07.2026). „Abgeloest" waere fuer die
                             eine Box wahr und fuer die andere eine
                             Aufforderung, sich den Ton abzuschalten.
                             -> HINWEIS ohne Urteil, normale Liste.

      DAMIT BLEIBEN IN `ABGELOEST` GENAU ZWEI, und jeder traegt seinen BELEG
      im Code (ohne Beleg veraltet eine Einstufung still):

        spotifyd.service     durch librespot. Beleg: `dpkg -l spotifyd` leer,
                             kein ausfuehrbares spotifyd im Pfad — der Dienst
                             KANN nicht starten. Bleibt, falls jemand ihn
                             nachinstalliert.
        mupi_splash.service  durch mupibox-boot-splash. Beleg: beide Units da,
                             splash_screen.sh noch vorhanden und ausfuehrbar.
                             Bleibt als Rueckfall.

      DIE ZWEITE FALLE bei pulseaudio steht weiter im Hinweistext, weil sie
      unabhaengig davon gilt: die Liste zeigt den SYSTEM-Dienst, PulseAudio
      laeuft aber als NUTZERdienst. Der Eintrag steht deshalb auf „aus", auch
      wenn gerade Musik spielt.

      Ein GESTOERTER abgeloester Dienst wandert NICHT nach oben. „Kaputtes
      zuerst" gilt innerhalb eines Abschnitts — ein Rueckfall, der scheitert,
      ist trotzdem nichts, worum man sich kuemmern muss.

  - kind: architektur
    id: admin-streaming-dienste-seite
    title: "Die Seite „Streaming-Dienste\" — und warum „Dienste\" umbenannt werden musste"
    match: "Streaming-Dienste|Anbieter.*eingerichtet|Jellyfin.*unvollstaendig|Systemdienste"
    tags: ['admin', 'board', 'streaming', 'spotify', 'jellyfin', 2026]
    source: "03.08.2026, src/backend-api/src/streaming.ts + seiten/streaming.ts"
    body: |
      NEU am 03.08.2026. Die haeufigste Frage beim Aufsetzen einer Box —
      „welche Quellen hat sie, und welche davon antworten?" — hatte keinen
      Ort. Die Antwort lag an drei Stellen verstreut: zwei Jellyfin- und vier
      Spotify-Felder mitten in der Konfigurationsliste, ein Absatz zur
      Spotify-Anmeldung darunter, und die Medienseite, die stumm nichts fand,
      wenn etwas fehlte.

      JE ANBIETER SICHTBAR: was er KANN (Katalog / Suche / Abspielen), ob er
      eingerichtet ist, WARUM nicht, und die Felder, die ihn einrichten.
      Vier Zustaende, und `geplant` ist ausdruecklich kein Mangel:

        bereit          alles da
        unvollstaendig  eine Haelfte fehlt — WELCHE, steht dabei
        aus             nichts eingetragen; eine Wahl, kein Fehler
        geplant         im Repo gibt es dafuer keine Zeile Code (ARD Sounds
                        = Backlog E4, Deezer = vorgemerkt)

      ZWEI DINGE, DIE DIE SEITE BEWUSST NICHT TUT:
        * Sie FRAGT NIEMANDEN. „Eingerichtet" heisst „die Angaben sind
          vollstaendig", nicht „der Server antwortet". Das ist eine ehrliche
          und billige Aussage; die teure gehoert an einen Knopf, den jemand
          drueckt, nicht auf jeden Seitenaufruf.
        * Sie entscheidet nichts selbst. Die Regeln stehen in
          src/backend-api/src/streaming.ts und sind dort getestet.

      DIE FALLE, die den ersten Anlauf gekostet haette: `refreshToken` wird von
      `ohneGeheimnisse` GELEERT. Wer die Anmeldung aus der geschwaerzten
      Konfiguration ablesen will, meldet jede Box als „nicht angemeldet".
      `spotifyAngemeldet` kommt deshalb getrennt in `anbieterLage` hinein —
      und der Kommentar an der Schnittstelle sagt, warum.

      DIE NAMENSKOLLISION: Die bestehende Seite „Dienste" zeigt
      systemd-Units. Zwei Menuepunkte, die beide „Dienste" heissen und
      voellig Verschiedenes zeigen, sind kein Ordnungsproblem, sondern ein
      Fehlgriff, den man taeglich macht. Die alte Seite heisst deshalb jetzt
      „Systemdienste"; der WEG bleibt `/dienste`, damit kein Lesezeichen
      zerbricht.

  - kind: falle
    id: umzug-macht-ortstests-rot
    title: "Nach dem Verschieben einer Einstellung: drei Tests halten den ALTEN Ort fest"
    sev: medium
    match: "Suchtreffer.*falsche Seite|Darstellung › Elemente|suche.spec.*rot|Test haelt den alten Ort|Umzug.*Test rot"
    cause: "Tests, die den ORT eines Schalters zusichern, sind nach einer Umsortierung zwangslaeufig rot — sie sind der Melder, nicht das Opfer"
    fix: "Ort im Test NACHZIEHEN statt lockern; wo es passt, zusaetzlich pruefen, dass der alte Ort NICHT mehr trifft"
    tags: ['admin', 'suche', 'test', 'falle', 2026]
    source: "03.08.2026, beim Uebernehmen des Verwaltungsumbaus (G4/G5/G6)"
    body: |
      LAGE: Der Verwaltungsumbau vom 03.08.2026 hat „Doppelte zusammenfassen"
      und „Ganze Diskografie" von der Darstellungs- auf die Medienseite
      verschoben. `tools/verwaltung-suchbestand.mjs` erhebt den Suchbestand
      aus den Seiten, also zog der Bestand korrekt mit — und danach war die
      Verwaltung rot:

          suche.filter.spec.ts  „findet den Schalter fuer doppelte Alben"
                                erwartet 'Doppelte zusammenfassen @ Darstellung'
          suche.filter.spec.ts  „findet die Diskografie auf beiden Seiten"
                                erwartet 'Darstellung'
          suche.spec.ts         „nennt an jedem Treffer den Ort"
                                erwartet 'Darstellung › Elemente'

      DAS IST KEIN SCHADEN, DAS IST DIE LEISTUNG. Diese drei Tests sichern
      nicht zu, dass etwas GEFUNDEN wird — das taete auch ein `toContain`
      ohne Ort. Sie sichern zu, dass der Treffer an den RICHTIGEN Ort fuehrt.
      Ein Suchtreffer, der auf die falsche Seite schickt, ist schlimmer als
      gar keiner: man sucht dort weiter.

      DIE VERSUCHUNG, und warum sie falsch ist: Es ist ein Handgriff, die
      Seitenangabe aus dem Test zu nehmen („Hauptsache, es wird gefunden").
      Danach ist der Test beim naechsten Umzug still, und das Board hat wieder
      Treffer, die ins Leere fuehren. Nachgezogen wurde deshalb der ORT, und
      bei der Diskografie steht jetzt zusaetzlich:

          expect(seiten).not.toContain('Darstellung')

      sonst faende man den Knopf an einer Stelle, an der er nicht mehr wirkt.

      DIE REIHENFOLGE, die Arbeit spart:
        1. Seiten umbauen
        2. node tools/verwaltung-suchbestand.mjs --schreiben
        3. node --test tools/verwaltung-suche-vollstaendig.test.mjs
        4. ng test --configuration ci --watch=false   (hier kommen die drei)
      Wer 4 auslaesst, merkt es erst, wenn jemand sucht.

      NEBENBEFUND BEIM SELBEN LAUF, weil er dieselbe Bauart hat: In der
      BOX-Oberflaeche war `darstellung.service.spec.ts` seit b24cb1c1 rot —
      „laesst die uebrigen Schalter unberuehrt" prueft `w.ruhigeMarke === true`,
      setzt den Schalter in der Eingabe aber nicht. Ein roter Test, den
      niemand mehr liest, deckt genau nichts mehr ab.

  - kind: falle
    id: funktion-die-nur-ihr-eigener-test-benutzt
    title: "Eine Funktion, die nur ihr eigener Test ruft, sieht aus wie ein tragender Teil"
    sev: low
    match: "toter Code.*Test|nur im Test benutzt|geteilt.*niemand ruft|zweite Formulierung derselben Regel"
    cause: "Beim Bauen entsteht die 'saubere' Backend-Funktion, die Oberflaeche loest es dann doch selbst — beide bleiben stehen"
    fix: "Vor dem Festschreiben: jeden neuen Export gegen den Baum greppen. Nur Test-Treffer = weg, samt Test"
    tags: ['aufraeumen', 'test', 'falle', 'admin', 2026]
    source: "03.08.2026, beim Uebernehmen des Verwaltungsumbaus"
    body: |
      GEFUNDEN beim Gegenlesen einer angefangenen Arbeit: drei frische Exporte,
      die im ganzen Baum ausser in ihrem eigenen Test niemand ruft.

          dienste.ts        geteilt(liste)       teilt in normal/abgeloest.
                                                 Der Server schickt EINE Liste
                                                 mit `abschnitt` je Dienst, die
                                                 Seite filtert selbst.
          konfiguration.ts  bereichsangabe(id)   BEREICHE geht als Ganzes
                                                 hinaus; die Verwaltung baut
                                                 sich eine Map.
          streaming.ts      FAEHIGKEIT_TEXT      dieselben drei Woerter stehen
                                                 in der Oberflaeche noch einmal
                                                 — ueber die Leitung geht nur
                                                 die Kennung.

      WARUM DAS TEUER IST, obwohl es „nur" ungenutzt ist: Ein Export mit
      Kommentar und gruenem Test liest sich beim naechsten Umbau wie eine
      getragene Regel. Wer dann die Regel aendert, aendert sie an der falschen
      Stelle — die Seite tut weiter, was sie immer tat. Und bei
      FAEHIGKEIT_TEXT waeren es zwei Wahrheiten gewesen: beim ersten
      Umbenennen laufen sie auseinander, und keine Pruefung merkt es.

      WAS AN DIE STELLE KAM, statt einer stillen Loeschung: an jeder der drei
      Stellen steht jetzt ein Kommentar, der sagt, dass es das gab, warum es
      weg ist und wo die Regel WIRKLICH steht. Sonst baut es der naechste
      wieder. Der Test von `geteilt` ist nicht verschwunden, sondern auf den
      Weg umgestellt, den die Seite geht: `sortiert` ueber die ganze Liste,
      dann nach `abschnittVon` filtern. Er prueft damit erstmals das, was
      wirklich passiert.

      HANDGRIFF VOR JEDEM FESTSCHREIBEN:
        git diff --stat            # welche Exporte sind neu?
        grep -rn "<name>" src | grep -v "\.spec\."
      Kein Treffer heisst: weg damit.

  - kind: referenz
    id: doku-karte-statt-lexikon
    title: "Wo die Dokumentation steht — und was bewusst NICHT drinsteht"
    tags: ['doku', 'arbeitsweise', 2026]
    source: "angelegt 2026-08-03"
    body: |
      `dokumentation/mixpibox.md` im Box-Repo ist die KARTE des Vorhabens: wie
      die Teile zusammenhaengen und wo man nachsieht. Neun Abschnitte —
      Ueberblick, die vier Repos, Hardware, Architektur (Dienste mit Ports,
      beide Oberflaechen, beide Verwaltungen, Tonkette, Kiosk), Installation,
      Betrieb, Entwicklung, Fehlersuche, und ausdruecklich „Was gerade im Bau
      ist".

      SIE WIEDERHOLT DIESES PAKET NICHT. Die teuer erkauften Einzelheiten —
      welche Messung welche Vermutung widerlegt hat, welcher Workaround warum
      noetig war — stehen hier. Die Doku nennt den Eintrag beim Namen, statt
      die Antwort ein zweites Mal zu behaupten. Zwei Wahrheiten ueber dieselbe
      Sache sind schlimmer als eine unvollstaendige, und die zweite veraltet
      immer zuerst.

      WER ETWAS NEUES HERAUSFINDET, schreibt es HIERHER (und zieht die version
      hoch). In die Doku gehoert nur, was sich an der KARTE aendert: ein neuer
      Dienst, ein neuer Port, ein Baustein mehr — oder etwas, das aus
      Abschnitt 9 herauswaechst, weil es fertig ist.

      DIE README des Box-Repos beschrieb bis zum 03.08.2026 den URSPRUNG
      (splitti/MuPiBox, mupibox.de) mit einem Kasten darueber. UEBERHOLT:
      seit 03.08.2026 ist sie die README DIESES FORKS — siehe
      [[readme-fork-drei-heikle-stellen]].

  - kind: entscheidung
    id: readme-fork-drei-heikle-stellen
    title: "Die README eines Forks: Paypal, Support und Urheber — drei Stellen, an denen Weglassen und Stehenlassen beide falsch sind"
    tags: ['doku', 'readme', 'fork', 'herkunft', 'lizenz', 2026]
    source: "umgesetzt 03.08.2026, Commit 9e6ccbd6 im Box-Repo"
    body: |
      AUSGANGSLAGE: Die README beschrieb 63 Zeilen lang das URSPRUNGSPROJEKT
      (splitti/MuPiBox) — elfmal "MuPiBox", Ueberschriften "Website & Support",
      "Requirements", "How to install", alle auf mupibox.de zeigend. Der
      Wegweiser-Kasten von 18b4f74c hing zwar darueber, aber darunter stand
      weiter das falsche Projekt. Wer hier landete, brauchte drei
      Bildschirmseiten, um zu merken, dass er woanders ist.

      ── DIE DREI STELLEN, AN DENEN BEIDE NAHELIEGENDEN WEGE FALSCH SIND ──

      1. DIE PAYPAL-ADRESSEN zeigen auf nero und splitti, also auf den
         URSPRUNG. Entfernen sieht nach Undank aus. Stehenlassen unter
         "Spend a coffee via paypal to" liest sich, als sammle dieser Fork
         Geld fuer fremde Arbeit ein — der Leser weiss an dieser Stelle
         laengst nicht mehr, wessen Seite er liest.
         GELOEST, indem die Adressen BLEIBEN und die UEBERSCHRIFT die Frage
         beantwortet, bevor sie entsteht:
             "Unterstuetzen — geht an die Urheber des Ursprungs"
             "Dieser Fork sammelt kein Geld."
         Die Ueberschrift traegt die Aussage, nicht eine Fussnote: sie wird
         auch von dem gelesen, der nur ueberfliegt.

      2. DIE SUPPORT-ADRESSEN (Discord, Website, Youtube) gehoeren ebenfalls
         dem Ursprung. Sie ganz oben als Anlaufstelle stehen zu lassen,
         schickt Fehler dieses Forks an Leute, die den Code nie gesehen haben
         und ihn nicht nachvollziehen koennen — das ist kein
         Dokumentationsfehler, sondern eine Belaestigung Dritter.
         GELOEST durch TRENNUNG VON ORT UND ROLLE: die Adressen wandern nach
         UNTEN unter "Das Ursprungsprojekt" (dort sind sie Herkunftsangabe),
         und weiter vorn steht ein eigener Abschnitt "Support geht NICHT an
         den Ursprung".

      3. DIE URHEBERNENNUNG. LICENSE.md ist MIT, Copyright "MuPiBox.de", und
         MIT verlangt ausdruecklich, dass Vermerk und Lizenztext in allen
         Kopien erhalten bleiben. Die Liste "Based on" (16 Eintraege, von
         DietPi ueber Sonos-Kids-Controller bis zu den Klangdateien) bleibt
         deshalb VOLLSTAENDIG UND UNVERAENDERT stehen; Lizenz und
         Copyright-Zeile werden im Text genannt statt nur verlinkt.
         Unabhaengig von der Lizenz gilt: ein Fork, der seine Herkunft
         verwischt, ist nicht in Ordnung.

      ── WEGGEFALLEN: DIE BADGES ──
      Release-, Issue- und Discord-Badges zogen ihre Zahlen aus
      splitti/MuPiBox. In einem Fork zeigen sie FREMDE Zahlen als die eigenen
      an — "stable release 4.3.0" und "N Issues" beziehen sich auf ein Repo,
      das mit diesem Baum nichts mehr zu tun hat. Das ist keine
      Geschmacksfrage, sondern eine falsche Angabe.

      ── SPRACHE: DEUTSCH, MIT ENGLISCHEM KASTEN OBEN ──
      Kommentare, Wissenspaket, BACKLOG und dokumentation/ sind deutsch. Eine
      englische README waere die einzige englische Datei gewesen und haette
      genau bis zur ersten Datei getragen, die sie beschreibt. ABER: der
      Abweis-Satz ("This is a hard fork … if you are looking for the original,
      go to github.com/splitti/MuPiBox") steht auf ENGLISCH ganz oben — er muss
      in der Sprache stehen, in der die URSPRUNGS-README steht, sonst faengt er
      den nicht ab, der von dort kommt. Die Begruendung fuer den Sprachwechsel
      steht in der Datei selbst, im letzten Abschnitt.

      ── DIE FORM, DIE SICH BEWAEHRT HAT: DREI SPALTEN ──
      "Was diese Abzweigung anders macht" ist eine Tabelle mit
      was / WO im Code / STAND. Die dritte Spalte ist die wichtige — sie
      zwingt dazu, Halbfertiges als halbfertig zu benennen, statt es
      wegzulassen (dann fehlt es) oder mitzuzaehlen (dann luegt die Liste).
      Zwei Beispiele aus dem Bestand:
        * Verschmelzung: Regel gebaut und getestet, aber am Geraet gemessen
          (02.08.2026) 21 Spotify- und 1 Jellyfin-Eintrag — die
          Ueberschneidung ist NULL, es gibt heute nichts zu verschmelzen.
        * Profile: Ablagen haben einen Besitzer (gast); mehrere Kinder mit
          eigenem Zugang sind GEPLANT (BACKLOG E18), nicht gebaut.

      ── GEPRUEFT STATT BEHAUPTET ──
      Jede Zahl in der README ist abgezaehlt worden, nicht geschaetzt:
      22 Units in config/services, 68 Werkzeuge in tools/, 51 Testdateien in
      src/backend-api, 16 Verwaltungsseiten + Anmeldung (app.routes.ts im
      HEAD, nicht im Arbeitsbaum), NewDesign ~9100 Zeilen. Dass NewDesign/
      nach www/neu ausgeliefert wird, steht in
      src/frontend-box/angular.json (assets: input ./../../NewDesign, output
      neu) — nicht in deploy.sh, wo man es zuerst sucht.

      EIN STOLPERSTEIN BEIM SCHREIBEN: die "17 Schritte" von pruefen.sh aus
      dokumentation/mixpibox.md stimmen nicht mehr (heute deutlich mehr).
      Eine Zahl aus einer zweiten Doku abzuschreiben ist genau der Fehler,
      den [[doku-karte-statt-lexikon]] verhindern will — in der README steht
      jetzt, WAS geprueft wird, und keine Anzahl.

  - kind: falle
    id: neue-ueberschrift-erbt-fremde-sucheintraege
    title: "Eine neue Ueberschrift ganz oben faerbt jeden Eintrag darunter um — 19 Treffer zeigten in den falschen Abschnitt"
    sev: medium
    match: "falscher Abschnitt.*Suche|Aus der Konfiguration der Box.*Groesse|Suchbestand.*Ueberschrift|bereich.*erbt|h2 oben eingefuegt"
    cause: "tools/verwaltung-suchbestand.mjs gibt jedem Eintrag die zuletzt gelesene Ueberschrift als `bereich` — eine neue h2 am Seitenanfang beansprucht alles, was bis zur naechsten h2 folgt"
    fix: "Dem Abschnitt, der bisher OHNE Ueberschrift auskam, eine eigene geben (hier: „Vorschau\"). Danach node tools/verwaltung-suchbestand.mjs --schreiben"
    tags: ['admin', 'suche', 'falle', 2026]
    source: "03.08.2026, Gegenlesen des Verwaltungsumbaus (G5)"
    body: |
      GEFUNDEN beim Gegenlesen: Der Umbau vom 03.08. setzte auf der
      Darstellungsseite den Abschnitt „Aus der Konfiguration der Box" GANZ NACH
      OBEN — vor die Vorschau. Er ist dort richtig aufgehoben; man vergleicht
      genau hier.

      DIE NEBENWIRKUNG stand im erzeugten Suchbestand, nicht im Quelltext.
      Diese Seite hatte bis dahin ihre ERSTE Ueberschrift erst bei „Elemente".
      Alles davor — die Vorschau samt Regler und alles, was am angetippten
      Element haengt — trug deshalb einen leeren Abschnitt. Mit der neuen h2
      ganz oben erbten neunzehn Eintraege sie:

          Groesse            @ Darstellung › Aus der Konfiguration der Box
          Startseite der Box @ Darstellung › Aus der Konfiguration der Box
          Thema              @ Darstellung › Aus der Konfiguration der Box
          Abstand der Alben  @ Darstellung › Aus der Konfiguration der Box
          … und fuenfzehn weitere

      In diesem Abschnitt stehen vier Konfigurationsfelder (Farbthema,
      Vollbild, weicher Bildlauf, Wahl der Oberflaeche) und sonst nichts. Wer
      „Groesse" sucht, wird also dorthin geschickt, findet nichts und sucht
      DORT weiter — genau der Schaden, den [[umzug-macht-ortstests-rot]] fuer
      die Seiten-Ebene beschreibt, nur eine Stufe feiner.

      WARUM ES KEIN TEST GEMELDET HAT: Die Pruefungen sichern zu, dass ein
      Eintrag DA ist und auf der richtigen SEITE liegt
      (tools/verwaltung-suche-vollstaendig.test.mjs prueft `treffer.seite`).
      Der ABSCHNITT wurde nirgends zugesichert. Der Zeichen-fuer-Zeichen-
      Vergleich der abgelegten Datei schlaegt auch nicht an: man erhebt neu,
      schreibt, und die falsche Zuordnung ist ab da „richtig".

      DIE LEHRE, allgemein: Der Bestand wird LINEAR erhoben. Eine Ueberschrift
      ist deshalb kein Titel, sondern ein Geltungsbereich bis zur naechsten.
      Wer oben eine einfuegt, muss pruefen, was darunter alles mit hineinfaellt
      — und der billigste Weg ist, dem bisher namenlosen Teil ebenfalls eine
      Ueberschrift zu geben.

      HANDGRIFF:
        node tools/verwaltung-suchbestand.mjs        # nur zeigen
        git diff src/frontend-admin/src/app/such-bestand.ts
      Wandert in der Diff ein `bereich` an Eintraegen, die man gar nicht
      angefasst hat, ist genau das passiert.

  - kind: falle
    id: seite-umbenannt-kachel-vergessen
    title: "Eine Seite umbenennen, die Uebersichtskachel aber nicht — die Suche gab den Volltreffer an die Kachel"
    sev: medium
    match: "Dienste.*Uebersicht|Suche.*falsche Seite.*dienste|Kachel heisst noch|Systemdienste.*nicht gefunden"
    cause: "Der Name einer Seite steht an DREI Stellen: Route (title), Kopfleiste und Uebersichtskachel. Umbenannt wurden nur die ersten beiden"
    fix: "Beim Umbenennen alle drei anfassen; danach `treffer(SUCH_BESTAND, '<alter Name>')` ausgeben lassen und ansehen, wohin der erste Treffer zeigt"
    tags: ['admin', 'suche', 'falle', 2026]
    source: "03.08.2026, Gegenlesen des Verwaltungsumbaus (G6)"
    body: |
      LAGE: Am 03.08.2026 wurde die Seite „Dienste" zu „Systemdienste", weil
      daneben „Streaming-Dienste" entstand ([[admin-streaming-dienste-seite]]).
      Geaendert wurden `app.routes.ts` (title) und `rahmen.ts` (Kopfleiste). Die
      KACHEL auf der Uebersicht hiess weiter „Dienste".

      WAS DAS IN DER SUCHE ANRICHTETE — nachgemessen mit dem echten Bestand:

          Frage „dienste"
            1. Dienste            -> Uebersicht          [/]        <- Rang 0
            2. alle Dienste       -> Medien
            3. Streaming-Dienste  -> Streaming-Dienste   [/streaming]
            4. andere Dienste …   -> Medien
            5. Systemdienste      -> Systemdienste       [/dienste]

      `rangFuer` gibt fuer Gleichheit den besten Rang (0). Die Kachel hiess
      GENAU „Dienste", also gewann sie — und fuehrte auf die Startseite. Die
      gemeinte Seite stand auf Platz fuenf, weil „systemdienste" EIN Wort ist:
      kein Wortanfang, nur ein Enthaltensein (Rang 3).

      Der Umbenennung war also genau das misslungen, was sie bezwecken sollte:
      wer „Dienste" tippt, landete nirgends.

      BEHOBEN: die Kachel heisst „Systemdienste", und es gibt eine zweite fuer
      „Streaming-Dienste" (die Seite hatte auf der Uebersicht gar keine).
      Beide fallen im Bestand mit dem Eintrag ihrer eigenen Seite zusammen —
      der Umweg ueber „/" ist damit weg.

      DIE ALLGEMEINE REGEL: Ein Seitenname steht in diesem Board an DREI
      Stellen — `app.routes.ts` (title), `rahmen.ts` (Kopfleiste),
      `seiten/uebersicht.ts` (Kachel). Die ersten beiden faellt einem beim
      Umbenennen ein; die dritte ist die, die man zuerst liest.

      NACHMESSEN (Werkzeug reicht nicht, man muss hinsehen):
        treffer(SUCH_BESTAND, '<alter Name>') ausgeben und den ERSTEN
        Treffer ansehen. Zeigt er auf „/", ist eine Kachel stehengeblieben.

      IM SELBEN DURCHGANG: Die Kachel „Konfiguration" warb weiter mit
      „Farbthema" — das war mit G5 zur Darstellung gewandert. Eine Kachel ist
      der Wegweiser, den man VOR dem Suchen liest; sie darf als einzige nicht
      auf einen Ort zeigen, an dem nichts mehr steht.

  - kind: falle
    id: ausgelagertes-auswahlfeld-wird-zur-knopfwand
    title: "Ein Auswahlfeld auf eine Seite mit anderer Formensprache umziehen: aus der Klappliste werden 28 Knoepfe"
    sev: low
    match: "28 Knoepfe|installedThemes.*Knopf|Farbthema.*Darstellungsseite|Auswahl als Knopfreihe"
    cause: "Die Darstellungsseite zeichnet jede Auswahl als einen Knopf je Eintrag — richtig bei zwei Werten, unbrauchbar bei einer Liste aus der Konfiguration"
    fix: "Grenze in der ANZEIGE: bis 5 Eintraege Knoepfe, darueber eine Klappliste. Die Grenze gehoert zur Seite, nicht ans Feld"
    tags: ['admin', 'darstellung', 'falle', 2026]
    source: "03.08.2026, Gegenlesen des Verwaltungsumbaus (G5)"
    body: |
      Der Umbau holte vier Felder von der Konfigurations- auf die
      Darstellungsseite. Dort werden Auswahlen nicht als Klappliste gezeichnet,
      sondern als Knopfreihe — was fuer `mupibox.oberflaeche` genau richtig ist:
      zwei Werte, beide Namen ohne Klick lesbar.

      Das vierte Feld ist `mupibox.theme`, und dessen Liste kommt nicht aus dem
      Code, sondern aus der Konfiguration: `mupibox.installedThemes`. In
      config/templates/mupiboxconfig.json stehen dort 28 Namen (blue …
      aurora). Die Seite haette also 28 Knoepfe ganz oben gezeichnet — ueber
      der Vorschau, um die es auf dieser Seite eigentlich geht. Auf der
      Konfigurationsseite, von der das Feld kam, war es eine Klappliste.

      DAS MUSTER, das dahintersteckt und wiederkommt: Beim Verschieben eines
      Feldes zwischen Seiten wandert die DATENDEFINITION mit, die
      DARSTELLUNGSREGEL aber nicht. Man prueft „steht das Feld noch richtig in
      der Datei?" (ja) und „erscheint es?" (ja) — und uebersieht, dass die neue
      Seite es ANDERS zeichnet. Sichtbar wird das erst mit echten Daten: mit
      der drei-Eintrag-Attrappe aus den Tests sieht die Knopfreihe gut aus.
      Vgl. [[attrappe-luegt-durch-weglassen]] — hier luegt sie durch KUERZE.

      Die Grenze steht in der Seite (`KNOEPFE_MAX = 5`) und nicht am Feld: sie
      ist eine Eigenschaft dieser Anzeige. Auf der Konfigurationsseite ist
      jede Auswahl eine Klappliste, egal wie kurz.

  - kind: falle
    id: backtick-im-html-kommentar-frisst-die-halbe-seite
    title: "Der Backtick, der die Vorlage beendet, stand in einem KOMMENTAR — und der Suchbestand verlor 51 Eintraege"
    sev: high
    match: "Suchbestand.*Eintraege fehlen|51 Eintraege|Backtick.*Kommentar|Vorlage endet mitten|template literal.*HTML-Kommentar"
    cause: "In einem HTML-Kommentar INNERHALB der Angular-Vorlage einen Bezeichner in Backticks gesetzt — der erste Backtick beendet das Template-Literal"
    fix: "In Vorlagen NIE Backticks, auch nicht in <!-- -->. Wer einen Bezeichner nennen will, schreibt ihn ohne Auszeichnung"
    tags: ['angular', 'admin', 'falle', 2026]
    source: "03.08.2026, beim Gegenlesen selbst hineingelaufen"
    body: |
      [[backticks-in-angular-vorlagen]] sagt die Regel. DIESER Eintrag sagt,
      wie man trotzdem hineinlaeuft — denn genau das ist am 03.08.2026 beim
      Gegenlesen passiert, mit der Regel vor Augen.

      DIE STELLE war kein HTML und kein Text auf dem Bildschirm, sondern ein
      erklaerender HTML-Kommentar in der Vorlage. Darin sollte ein
      Konfigurationsschluessel genannt werden, und die Gewohnheit setzt einen
      Bezeichner in Backticks — so, wie es in JEDEM anderen Kommentar dieses
      Projekts richtig ist. In einer Vorlage endet damit das Template-Literal
      mitten im Kommentar.

      WIE ES AUFFIEL, und das ist der eigentliche Wert dieses Eintrags: NICHT
      durch den Compiler, sondern durch das Werkzeug — eine Sekunde spaeter:

          node tools/verwaltung-suchbestand.mjs --schreiben
          137 Eintraege geschrieben     (vorher 188)
          git diff  ->  51 Zeilen weg, alle von der angefassten Seite

      `vorlageVon` schneidet vom ersten Backtick nach `template: \`` bis zum
      naechsten. Die Vorlage brach also genau dort ab, wo der Kommentar stand;
      alles danach war fuer die Erhebung nicht mehr da. Ein Blick auf die Zahl
      und die Diff hat gereicht.

      DIE LEHRE: Die Erhebung des Suchbestands ist nebenbei die schaerfste
      Pruefung auf abgeschnittene Vorlagen, die es in diesem Baum gibt — und
      die schnellste. Sie laeuft in 60 ms, waehrend `ng build` erst nach
      Sekunden meckert und `ng test` die Seite gar nicht anfasst, solange kein
      spec sie importiert.

      HANDGRIFF NACH JEDER AENDERUNG AN EINER VORLAGE:
        node tools/verwaltung-suchbestand.mjs | tail -2
      Faellt die Zahl, ist eine Vorlage abgeschnitten.

  - kind: entscheidung
    id: umzug-speicherort-festnageln
    title: "Beim Umsortieren einer Verwaltung gehoert der SPEICHERORT in einen Test, nicht in einen Kommentar"
    match: "Speicherort.*Test|verliert.*Einstellung.*still|pfad festnageln|Umzug.*Box verliert"
    tags: ['admin', 'konfiguration', 'test', 'entscheidung', 2026]
    source: "03.08.2026, Gegenlesen des Verwaltungsumbaus (konfiguration.spec.ts)"
    body: |
      DIE FRAGE beim Gegenlesen war: Verliert eine Box, die eine Einstellung
      schon gestellt hat, sie durch den Umbau? Die ANTWORT war ja — kein Feld
      hat seinen Pfad in der Datei gewechselt, es ist nur die Bedienung
      gewandert. Belegt war das an vier Stellen in Kommentaren.

      WARUM DAS NICHT REICHT: Ein Kommentar haelt niemanden auf. Wer beim
      naechsten Aufraeumen `thema` nach `darstellung.theme` legt — weil das
      Feld ja auf der Darstellungsseite steht und dort hingehoert —, hat auf
      JEDER laufenden Box das eingestellte Farbthema stillgelegt. Die Box liest
      weiter `mupibox.theme`, die Verwaltung schreibt woandershin, und beide
      behaupten etwas anderes. Keine Meldung, kein Fehler; nur eine Box, die
      ploetzlich wieder blau ist. Dieselbe Sorte stiller Ausfall wie ein Feld
      ohne Bereich, nur teurer, weil er alle Boxen zugleich trifft.

      GETAN: In konfiguration.spec.ts steht jetzt „Der Umzug hat den
      SPEICHERORT nicht angefasst" — zehn Felder, jedes mit seinem Pfad
      festgenagelt, dazu eine Gegenprobe mit einer Box, die alles schon
      gestellt hat (Farbthema „matrix", Oberflaeche „neu", Jellyfin
      eingerichtet): sie bekommt IHRE Werte zu sehen und nicht die Vorgaben.

      DIE REGEL, die daraus folgt: Wer eine Einstellung auf eine andere SEITE
      schiebt, schreibt im selben Zug einen Test, der ihren Pfad in der Datei
      festhaelt. Der Test kostet drei Zeilen und ist das Einzige, was den
      Unterschied zwischen „umgezogen" und „verloren" verteidigt.

      UND DANN HOER DORT NICHT AUF. Dieser Test sichert den PFAD — er sagt,
      dass die Verwaltung weiter dorthin schreibt, wo die Box liest. Er sagt
      NICHTS darueber, ob der Schluessel dort auch stehenbleibt. Genau
      dazwischen lag der teuerste Befund desselben Gegenlesens: nicht die
      Verwaltung nahm einer Box etwas weg, sondern das Migrationsskript
      ([[conf-update-loescht-schluessel-den-noch-jemand-liest]]). Die zweite
      Frage lautet deshalb: was passiert mit diesem Schluessel beim naechsten
      Update? Beantwortet wird sie nicht am Quelltext, sondern indem man
      update/conf_update.sh laufen laesst — tools/konfig-umzug-probe.py tut
      genau das und haengt in tools/pruefen.sh.

  - kind: falle
    id: laut-fenster-hing-an-der-kissenmitte
    title: 'Der Lautstaerke-Regler war halb unerreichbar — das Fenster hing an der Kissenmitte, und die Mitte zog um'
    sev: hoch
    match: "mp-laut-fenster|left: 50%.*mp|Lautstaerke.*ragt aus dem Schirm|leiser geht, lauter nicht"
    cause: "`.mp-laut-fenster` war `position: absolute` am Kissen (`.mp` ist `position: relative`) und sass mit `left: 50%` auf dessen MITTE. b24cb1c1 stellte das Kissen auf `align-self: flex-end` — eingefahren 326 px breit, rechte Kante 788, Mitte also 625 statt 400. Das 704 px breite Fenster nahm die 225 px eins zu eins mit: x 273..977 auf 800 px Schirm."
    fix: "`position: fixed` statt `absolute`, damit `left: 50%` die Mitte des SCHIRMS meint, und `bottom: calc(var(--mp-rand) + var(--mp-hoehe) + 10px)` statt `calc(100% + 10px)` (bei `fixed` waere 100 % die Schirmhoehe). Ergebnis x 48..752 in JEDEM Zustand, 0 px Wandern waehrend der Bewegung. Behoben 03.08.2026."
    tags: [falle, oberflaeche, neu, lautstaerke, mini-player, 800x480, barrierefreiheit, 2026]
    source: "NewDesign/app.css, gemessen 03.08.2026 mit tools/laut-fenster-schau.mjs und tools/leiste-platz-schau.mjs"
    body: |
      AM GERAET KAM ES AN ALS: „leiser geht, lauter nicht." Fuer ein Kind ist
      das die schlimmste Haelfte — man kann sich in eine Lautstaerke
      hineinsteigern, aber nicht mehr heraus.

      ── WO DER FEHLER STAND, UND WO ER NICHT STAND ───────────────────
      Nicht am Lautstaerke-Fenster. Der Commit, der es kaputt machte, hat es
      nicht angefasst: b24cb1c1 stellte das KISSEN von mittig auf rechts
      („der eingeklappte miniplayer soll sich ganz rechts einordnen"). Das
      Fenster hing als absolut positioniertes Kind daran und wurde
      mitgezogen, ohne in der Aenderung vorzukommen.

      GEMESSEN STATT GERATEN, und das lohnte sich: Zwei Trennverzeichnisse
      (`git worktree add --detach`) auf f685de83 und b24cb1c1, in beiden
      dasselbe Werkzeug. f685de83 x 48..752 und gruen, b24cb1c1 x 273..977
      und rot. Damit war der Verdacht kein Verdacht mehr, und die beiden
      spaeteren Umbauten (36740ac1, dd4f611d) waren aus dem Spiel, ohne dass
      man ihren Quelltext lesen musste.

      ── DIE EIGENTLICHE LEHRE: EINE VORAUSSETZUNG, DIE NIE JEMAND SCHRIEB ──
      `left: 50%` an einem absolut positionierten Kind heisst „die Mitte des
      Ankers". Dass das dieselbe Stelle war wie die Mitte des Schirms, galt
      nur, solange das Kissen mittig stand — eine Bedingung, die niemand
      irgendwo hinschrieb, weil sie so lange zutraf, dass sie nicht wie eine
      Bedingung aussah. Im Wiki stand sie sogar, aber als Beruhigung: „weil
      das Kissen mittig bleibt, steht es unveraendert."

      DIE UNTERSCHEIDUNG, die daraus folgt und die weit ueber diesen Fall
      hinausgeht: Die Ausrichtung des Kissens ist eine GESCHMACKSFRAGE, sie
      darf sich jederzeit wieder aendern. Dass der Regler ganz auf dem Schirm
      liegt, ist KEINE — sonst ist er halb unbedienbar. Haengt man beides an
      denselben Anker, reisst die freie Entscheidung die unfreie mit. Der Fix
      trennt sie: `position: fixed`. Was mit dem Kissen geschieht, geht das
      Fenster nichts mehr an.

      WAS `fixed` DAFUER VORAUSSETZT (die neue Falle, die man dafuer
      eintauscht): Traegt irgendein VORFAHR `transform`, `filter`,
      `backdrop-filter`, `perspective`, `contain: paint` oder `will-change`
      darauf, wird er zum Bezugsrahmen und alles ist wie vorher. Geprueft:
      `body` und `.spalte` tragen nichts davon. `body.platz-machen .leiste`
      hat `transform: translateX(-100%)`, ist aber GESCHWISTER von `.mp`.
      Diese Voraussetzung ist der Preis — sie ist aber eine Regel des
      Browsers und keine stille Verabredung zwischen zwei Regelsaetzen.

      ── WARUM DER GESAMTLAUF ES NICHT FING ──────────────────────────
      tools/leiste-platz-schau.mjs prueft das Fenster — aber NUR eingefahren.
      Genau die Sorte Luecke aus [[attrappe-gruen-geraet-rot-loser-titel]]:
      nicht „stelle ich den Fall?", sondern „welche SORTEN gibt es?". Neu ist
      deshalb tools/laut-fenster-schau.mjs, und es misst fuenf:

        1. ausgefahren, Standardbreite
        2. eingefahren, Standardbreite            <- der gemeldete Fall
        3. Kissen schmaler (`--mupi-mp-b` 0.5)
        4. Tasten groesser (`--mupi-btn` 1.3)
        5. WAEHREND der 220-ms-Bewegung

      DREI DAVON WAREN ROT, nicht einer. Sorte 3 traf sogar den
      AUSGEFAHRENEN Zustand: Wer in der Verwaltung den Player schmaler
      stellte, schob das Fenster ebenfalls nach rechts hinaus (x 264..968) —
      ein zweiter Weg in denselben Schaden, den niemand gemeldet hatte, weil
      niemand diesen Regler benutzt hat. Nach der Behebung stehen alle fuenf
      auf x 48..752.

      SORTE 5 HAT EINEN HAKEN, den der erste Anlauf uebersah: Beim EINfahren
      macht `geruehrt()` das Fenster absichtlich zu. Die Abtastung lief also
      ins Leere und meldete eine leere Reihe — eine Messung, die nichts misst
      und dabei gruen aussieht. Gemessen wird deshalb die AUSFAHRT (per
      Randwisch, weil die Ruhezeit laenger ist als die 4 s, nach denen sich
      das Fenster selbst schliesst). Vorher wanderte die linke Kante dort
      ueber 176 px (273 -> 97), nachher 0 px: 48 48 48 …

      ── ZAHLEN ──────────────────────────────────────────────────────
        vorher   eingefahren            x 273..977   (Schirm 800)
        vorher   Kissen halbe Breite    x 264..968
        vorher   Tasten 1.3             x 235..939
        nachher  alle fuenf Sorten      x 48..752, Regler beidseitig treffbar

      Der senkrechte Ort ist unveraendert: 8 + 76 + 10 = 94 px vom unteren
      Rand, das Fenster steht wie zuvor 10 px ueber der Kissenoberkante (396).
      `--mp-hoehe` und `--mp-rand` an `:root` bekommen damit ihren VIERTEN
      Verbraucher — der Kommentar dort zaehlt drei auf und ist nachgezogen.

  - kind: falle
    id: readme-befehl-der-nichts-startet
    title: "Eine Proxy-Konfiguration sieht aus wie ein Server: `node tools/verwaltung-vorschau.mjs` endet wortlos mit 0"
    sev: high
    match: "verwaltung-vorschau.*nichts|Vorschau startet nicht|node .*mjs.*keine Ausgabe|proxy-config.*Vorschau|README.*Befehl.*tut nichts"
    cause: "tools/verwaltung-vorschau.mjs ist eine Angular-Proxy-Konfiguration (`export default [...]`), kein Server. node laedt sie, wertet das Array aus und endet mit Ruecklaufwert 0 — ohne Ausgabe und ohne dass etwas lauscht"
    fix: "Zwei Fenster: `node tools/neu-vorschau.mjs` (die Attrappe, Port 8299) und `cd src/frontend-admin && npx ng serve --proxy-config ../../tools/verwaltung-vorschau.mjs`, dann http://localhost:4200/admin/"
    tags: ['readme', 'werkzeuge', 'vorschau', 'falle', 2026]
    source: "03.08.2026, Gegenlesen der neuen README — der Befehl wurde ausgefuehrt, nicht nur gelesen"
    body: |
      GEFUNDEN beim Gegenlesen der README vom 03.08.2026. Sie bot unter „Ohne
      Box arbeiten" zwei Zeilen an, die nebeneinander standen und gleich
      aussahen:

          node tools/neu-vorschau.mjs          # die neue Box-Oberflaeche
          node tools/verwaltung-vorschau.mjs   # die neue Verwaltung

      Die erste stimmt (gemessen: HTTP 200 auf http://localhost:8299/neu/).
      Die zweite startet NICHTS:

          $ node tools/verwaltung-vorschau.mjs
          $ echo $?
          0
          $ ss -ltn | grep -E '4200|8299'      # nichts

      DIE LEHRE IST NICHT „Tippfehler in der README". Sie ist: ein `.mjs` in
      tools/ sieht aus wie ein Werkzeug, das man aufruft. Diese Datei ist aber
      ein DATENSTUECK — ein Array, das `ng serve --proxy-config` einliest.
      Beides sind ausfuehrbare Node-Module, und Node beschwert sich nicht: ein
      Modul, das nur exportiert, laeuft erfolgreich durch. Der Ruecklaufwert
      ist 0. Ein Werkzeug, das nichts tut, ist von einem Werkzeug, das
      funktioniert, allein am Ruecklaufwert NICHT zu unterscheiden.

      WARUM DAS SCHLIMMER IST ALS EIN TOTER LINK: Wer einem toten Link folgt,
      sieht einen Fehler und sucht weiter. Wer diesem Befehl folgte, sah eine
      leere Zeile und danach wieder die Eingabeaufforderung — und schloss
      daraus, dass das PROJEKT kaputt ist, nicht die Anleitung. Das ist die
      teuerste Sorte Fehler in einer README: sie beschaedigt das Vertrauen in
      alles andere, was dort steht.

      GEGENMITTEL: Befehle in einer README werden AUSGEFUEHRT, nicht gelesen.
      `tools/readme-behauptungen-pruefen.sh` haelt den Befund fest (Abschnitt
      8) und faellt um, wenn die Zeile zurueckkommt.

  - kind: falle
    id: readme-wo-spalte-zeigt-auf-den-namensvetter
    title: "oberflaeche.ts ist NICHT der Umschalter der Oberflaeche — der Dateiname log, die README schrieb ihn ab"
    sev: medium
    match: "oberflaeche.ts.*Umschalter|welche Oberflaeche.*oberflaeche.ts|klassisch.*oberflaeche.ts|standKennung.*Umschalter"
    cause: "src/backend-api/src/oberflaeche.ts enthaelt `standKennung`/`KEIN_STAND` — das Signal zum NEULADEN. Der Umschalter klassisch/neu ist ein Konfigurationsfeld in konfiguration.ts, gelesen von scripts/chromium-autostart.sh. Zwei verschiedene Dinge, ein Wortstamm"
    fix: "Feld: src/backend-api/src/konfiguration.ts (id 'oberflaeche', pfad ['mupibox','oberflaeche'], standard 'klassisch'). Leser: scripts/chromium-autostart.sh (`jq '.mupibox.oberflaeche // \"klassisch\"'`), server.ts, NewDesign/index.html, src/frontend-box/src/app/rueckweg.ts"
    tags: ['readme', 'oberflaeche', 'konfiguration', 'falle', 2026]
    source: "03.08.2026, Gegenlesen der neuen README — grep 'klassisch' in der genannten Datei: kein Treffer"
    body: |
      Die README-Tabelle „Was diese Abzweigung anders macht" hat eine Spalte
      WO. Sie ist die wertvollste der drei — sie laesst sich nachschlagen. Und
      genau deshalb faellt es auf, wenn sie luegt.

      Zeile „Umschalter zwischen den beiden Box-Oberflaechen" nannte
      `src/backend-api/src/oberflaeche.ts`. Diese Datei hat 51 Zeilen und
      enthaelt zwei Ausfuhren:

          standKennung(buendel, sekunden, zaehler)   -> string
          KEIN_STAND = ''

      Das ist das Neulade-Signal: die Box fragt `GET /api/oberflaeche/stand`
      und laedt neu, wenn sich der Wert AENDERT. Mit „welche der beiden
      Oberflaechen startet der Kiosk" hat es nichts zu tun. Das Wort
      „klassisch" kommt in der Datei nicht ein einziges Mal vor.

      DIE PRUEFUNG, DIE DAS FINDET, IST EINE ZEILE:
          grep -c klassisch src/backend-api/src/oberflaeche.ts   ->  0

      WARUM DER FEHLER SO LEICHT PASSIERT: Der Dateiname ist eine gute
      Vermutung. „Umschalter der Oberflaeche" -> `oberflaeche.ts`. Der
      Namensstamm stimmt, die Sache nicht. Beim Schreiben einer Uebersicht
      arbeitet man von der Funktion zum Ort — und der naheliegende Ort ist
      fast richtig. Fast richtig ist bei einer Wegbeschreibung wertlos: wer
      die Datei aufschlaegt, findet 51 Zeilen ueber etwas anderes und weiss
      nicht, ob er falsch gesucht oder die Funktion nicht gefunden hat.

      NEBENBEFUND, in die README uebernommen: Umschalten wirkt erst nach einem
      NEUSTART der Box (`neustart: true` am Feld; der Kiosk waehlt beim Start).
      Das steht im Hinweistext des Feldes und fehlte in der README — wer
      umschaltet und sofort hinsieht, haelt den Schalter fuer kaputt.

  - kind: entscheidung
    id: unterseiten-statt-sechzehn-reiter
    title: "„Doppelte\" und „Interpreten\" wandern unter Medien — als Unterseiten, nicht als Abschnitte"
    match: "Unterseite|eltern.*Suchbestand|Doppelte unter Medien|Interpreten unter Medien|Kopfleiste zu lang|sechzehn Reiter"
    tags: ['admin', 'verwaltung', 'suche', 'entscheidung', 'gliederung', 2026]
    source: "03.08.2026, Fortsetzung der Sortierung (E6/G5), Auftrag des Benutzers"
    body: |
      DER SATZ DES BENUTZERS: „ingerpeten und doppelte gehoeren auch in den
      medien tab". Die Kopfleiste zaehlte sechzehn Eintraege nebeneinander —
      das ist keine Gliederung mehr, das ist eine Liste.

      ── DIE FRAGE, die man nicht umgehen darf ───────────────────────────
      Werden sie ABSCHNITTE der Medienseite oder eigene Seiten mit einem Weg
      VON Medien aus? Beides ist vertretbar, also wird gerechnet:

          medien.ts          1556 Zeilen, 5 Abschnitte
          interpreten.ts      690 Zeilen, 6 Abschnitte
          verschmelzung.ts    498 Zeilen, 6 Abschnitte
                            ───────────────────────────
          zusammen           2744 Zeilen, 17 Abschnitte

      ENTSCHIEDEN: eigene Seiten, Weg von Medien aus. Drei Gruende, und der
      dritte ist der, der nicht bloss Geschmack ist:
        1. Siebzehn Abschnitte untereinander sind dieselbe Liste wie
           sechzehn Reiter, nur senkrecht.
        2. Beide Seiten haben eine EIGENE Ablage (config/interpreten.json,
           config/verschmelzung.json) und einen eigenen Ablauf mit Rueckweg.
        3. Man muesste an der Aufraeumkarte VORBEISCROLLEN, die EINTRAEGE
           LOESCHT, um an die Karten zu kommen, die nur ZUORDNEN. Genau davor
           warnen die Kopfkommentare beider Seiten seit ihrer Entstehung —
           das war kein neues Argument, es stand schon da.

      ── WAS DABEI FAST LAUTLOS KAPUTTGEGANGEN WAERE ─────────────────────
      `tools/verwaltung-suchbestand.mjs` erhob den Bestand ueber die
      KOPFLEISTE: `for (const k of kopfleiste)`. Nimmt man zwei Seiten aus der
      Leiste, fallen ihre rund dreissig Sucheintraege heraus — kein Fehler,
      keine rote Pruefung, man findet sie nur nicht mehr. Das ist exakt die
      Fehlerklasse, gegen die es diesen Bestand ueberhaupt gibt
      ([[verwaltung-suche-bestand-aus-den-seiten]]).

      GEBAUT WURDE DESHALB `unterseitenLesen()`: Eine Seite ohne
      Leisteneintrag gehoert zu der Leisten-Seite, deren Vorlage
      `routerLink="<weg>"` enthaelt. KEINE PFLEGELISTE — eine solche stuende
      noch richtig da, waehrend der Weg auf der Seite laengst weg ist. Null
      oder mehr als ein Elternteil ist ein Befund, kein Urteil des Werkzeugs;
      `verwaltung-suche-vollstaendig.test.mjs` schlaegt darauf an.

      ── DER ORT EINES TREFFERS HAT JETZT DREI STUFEN ────────────────────
      `SuchEintrag` bekam ein optionales `eltern`, `ortVon()` setzt zusammen:

          Medien › Doppelte › Von Hand getrennt

      WARUM NICHT EINFACH `seite: 'Medien › Doppelte'`: Dann truege das Feld
      zwei Angaben in einer Zeichenkette, und jeder Test, der die SEITE
      zusichert, muesste sie wieder auseinandernehmen. Ausserdem waeren die
      bestehenden Ortstests still gruen geblieben — die Seite ist ja nicht
      umgezogen, nur ihr Eingang. Mit `eltern` sagt der Test genau das:
      `expect(ortVon(...)).toBe('Medien › Doppelte')`.

      ── DIE ALTEN ADRESSEN ──────────────────────────────────────────────
      `/verschmelzung` und `/interpreten` sind UNVERAENDERT. Es ist die
      Bedienung gewandert, nicht die Adresse — kein Lesezeichen zerbricht,
      und es braucht keine Umleitung. Ein eigener Test haelt das fest.

  - kind: entscheidung
    id: aktionsbereich-am-eintrag-statt-in-der-seite
    title: "„Medien neu einlesen\" zieht auf die Medienseite — und die Zuordnung steht am Eintrag im Backend"
    match: "medien-neu|Aktionsbereich|bereich.*AKTIONEN|Medien neu einlesen|m3u_generator.*Knopf"
    tags: ['admin', 'verwaltung', 'system', 'entscheidung', 2026]
    source: "03.08.2026, Fortsetzung der Sortierung (E6/G5)"
    body: |
      DIE FRAGE DES BENUTZERS WAR NICHT „umziehen?", sondern „falls es noch
      relevant ist". Also zuerst nachgesehen, ob der Abschnitt „Medien" auf der
      Systemseite ueberhaupt noch etwas tut.

      ── WAS ER WIRKLICH ANBOT ───────────────────────────────────────────
      Er war eine Schleife ueber alle NICHT einschneidenden Aktionen. In
      AKTIONEN (backend-api/src/system.ts) gibt es davon genau EINE:
      `medien-neu` -> `sudo /usr/local/bin/mupibox/m3u_generator.sh`.

      ── LEBT SIE? JA, UND DER BELEG IST NICHT DER QUELLTEXT DES KNOPFES ──
      `scripts/mupibox/change_checker.sh` ruft m3u_generator.sh von selbst —
      aber NUR, wenn sich die Aenderungszeit eines Medienverzeichnisses
      innerhalb von `mupibox.mediaCheckTimer` geaendert hat:

          difference=$(($(date +%s) - $(stat -c %Z "${dir}") - ${CHECK_TIMER}))

      Wer Dateien per SSH oder Netzfreigabe hinlegt und die Zeitstempel
      behaelt, wartet vergeblich. Und der Wert selbst ist fragil
      ([[conf-update-loescht-schluessel-den-noch-jemand-liest]]). Ein
      Handbetrieb daneben ist also kein Rest, sondern die Rueckfallebene.
      DOPPELT ist er auch nicht: das „Aufraeumen" der Medienseite meint
      Spotify-Eintraege, die dort verschwunden sind — eine andere Frage.

      ERGEBNIS: lebendig und einzig -> umziehen, nicht entfernen.

      ── WIE DIE ZUORDNUNG GEMACHT WURDE, und warum nicht anders ─────────
      Der Eintrag traegt jetzt `bereich: 'medien' | 'box'` — wie die
      Konfigurationsfelder ihren `bereich` tragen (G5). ZWEI Seiten zeigen
      Teile derselben Liste; haette jede selbst gewusst, was ihr gehoert,
      gaebe es zwei Meinungen, und beim naechsten Umsortieren merkt keine
      Pruefung, wenn sie auseinanderlaufen. `system.spec.ts` nagelt beide
      Listen namentlich fest.

      ── WAS DABEI FAST EINE LUEGE GEWORDEN WAERE ────────────────────────
      Der erste Anlauf liess die Schleife auf der Systemseite stehen, unter
      `@if (sanfte().length > 0)` und der Ueberschrift „Wartung" — „damit die
      naechste Aktion dieser Art einen Ort hat". Der Suchbestand nahm die
      Ueberschrift auf, denn er liest Quelltext und keine Bedingungen:

          { text: 'Wartung', seite: 'System', weg: '/system', bereich: '' }

      Wer „wartung" sucht, waere auf eine Seite geschickt worden, auf der es
      diesen Abschnitt nie gibt. Ein Treffer, der ins Leere fuehrt, ist
      schlimmer als keiner — man sucht dort weiter. Der Block ist deshalb ganz
      raus; die Absicherung leistet stattdessen der namentliche Test im
      Backend, der rot wird, sobald jemand eine sanfte Box-Aktion eintraegt.

      DIE LEHRE, allgemein: Ein leerer Abschnitt „fuer spaeter" ist im
      Suchbestand kein Platzhalter, sondern ein Versprechen.

  - kind: falle
    id: backtick-im-vorlagenkommentar-kuerzt-den-suchbestand
    title: "Ein Backtick im HTML-Kommentar liess die Medienseite von 28 auf 4 Sucheintraege fallen — ohne eine einzige Fehlermeldung"
    sev: high
    match: "Suchbestand plotzlich kurz|Medien 4 Eintraege|Backtick.*Vorlage.*still|vorlagen-backticks"
    cause: "Die Vorlage steht in einem Template-Literal. Ein Backtick in einem HTML-Kommentar schliesst sie mitten im Markup — und wenn der Rest zufaellig gueltiges TypeScript ergibt, uebersetzt es sich klaglos"
    fix: "tools/vorlagen-backticks.mjs prueft, ob hinter dem Ende jeder Vorlage das steht, was dort stehen MUSS. Haengt in tools/pruefen.sh"
    tags: ['angular', 'verwaltung', 'suche', 'falle', 'werkzeug', 2026]
    source: "03.08.2026, beim Umzug von „Doppelte\" und „Interpreten\" unter Medien"
    body: |
      [[backticks-in-angular-vorlagen]] sagt die Regel und nennt als Folge zwei
      Uebersetzerfehler. DIESER Eintrag ist der Fall, in dem es KEINE
      Fehlermeldung gab.

      In einen HTML-Kommentar auf der Medienseite kam eine Begruendung, und
      darin standen Dateinamen in Backticks — so wie ueberall sonst im
      Projekt:

          <!-- … `scripts/mupibox/change_checker.sh` ruft m3u_generator.sh … -->

      Der erste Backtick beendete die Vorlage. Alles danach wurde TypeScript,
      ging durch, und uebrig blieb eine Seite mit einem Viertel ihres Inhalts.

      GEMERKT HAT ES DER SUCHBESTAND, und zwar nur, weil man hinsah:

          node tools/verwaltung-suchbestand.mjs
            29 Medien      (vorher)
             4 Medien      (nachher)

      Kein Test war rot. `verwaltung-suche-vollstaendig.test.mjs` vergleicht
      die abgelegte Datei mit der NEU ERHOBENEN — und beide waren gleich
      falsch. `npx tsc --noEmit` schwieg. `ng build` schwieg.

      ── WAS DARAUS FOLGT, und es ist mehr als „aufpassen" ───────────────
      Eine Regel, die nur im Wiki steht, ist eine Bitte. Der Schaden hier ist
      lautlos und trifft eine erzeugte Datei, die niemand zeilenweise liest.
      Also ein Werkzeug:

          tools/vorlagen-backticks.mjs        Bericht, Code 1 bei Befund
          tools/vorlagen-backticks.test.mjs   mit erfundenem Fall
          in tools/pruefen.sh als „Backticks in Vorlagen"

      WIE ES PRUEFT — die Stelle, an der man beim Bauen falsch abbiegt: Nach
      dem Backtick SUCHEN geht nicht. Er ist ja das Zeichen, das die Vorlage
      beendet; nach dem Ausschneiden gibt es ihn per Definition nicht mehr.
      Geprueft wird deshalb, was DAHINTER steht: hinter einer heilen Vorlage
      folgt ein Komma oder das Ende des Decorators. Steht dort Prosa oder
      HTML, hat ein Backtick zu frueh geschlossen.

      NEBENBEI GILT DASSELBE fuer `styles: ` — dort faellt es nur eher auf,
      weil CSS seltener Backticks braucht.

  - kind: entscheidung
    id: interpreten-folgen-der-auswahl
    title: 'Die runde Interpreten-Reihe folgt der Auswahl — und „ein Werk genuegt" ist die Entscheidung'
    tags: [oberflaeche, neu, interpreten, auswahl, kategorie, regal, 2026]
    source: "gewuenscht und gebaut 03.08.2026, gemessen mit tools/interpreten-auswahl-schau.mjs"
    body: |
      DER SATZ DES BENUTZERS: „die interpreten sollen in gleicher logic wie die
      weiterhoeren nur die angezeigt werden welche sich auch in der sektion
      befinden bzw in hoerbuechern nur die interpreten von hoerbuechern nicht
      die gesamte liste wie in der alles uebersicht"

      Es ist ausdruecklich DIESELBE Regel wie
      [[weiterhoeren-folgt-der-auswahl]] — also wurde keine zweite gebaut,
      sondern die vorhandene an EINER Stelle erweitert.

      ── DIE EINE STELLE, UND WAS SICH DARAN AENDERTE ──────────────────
      Vorher stand in app.js:

        auswahlSchluessel()  ->  filtern UND auf Werkschluessel abbilden

      Die Interpreten-Reihe braucht dieselbe Auswahl, aber eine ANDERE
      Abbildung (Interpretenschluessel). Wer dafuer eine zweite Funktion mit
      einer zweiten Kopie der `null`-Regel schreibt, hat zwei Antworten auf
      dieselbe Frage. Deshalb aufgeteilt in EINEN Filter und zwei duenne
      Abbildungen:

        auswahlWerke()        nachRegal(nachKategorie(werke.liste)) | null
          auswahlSchluessel()      Set der Werkschluessel      -> Weiterhoeren
          auswahlInterpreten()     Set der Interpretenschluessel -> runde Reihe

      Die drei stehen jetzt zusammen bei `nachKategorie`/`nachRegal`, nicht
      mehr im Weiterhoeren-Abschnitt: die Auswahl gehoert keinem ihrer
      Verbraucher. `null` heisst weiterhin „nicht filtern" — Uebersicht und
      fehlende Medienliste, Begruendung siehe der Weiterhoeren-Eintrag.

      ── DIE FRAGE, DIE ES BEI „WEITERHOEREN" NICHT GAB ────────────────
      Eine gemerkte Stelle gehoert zu EINEM Werk. Ein Interpret gehoert zu
      MEHREREN. Also: gilt er als „in der Auswahl", wenn EINES seiner Werke
      dort steht, oder erst wenn alle?

      ENTSCHIEDEN: EINES GENUEGT. Begruendung — nur so zieht die runde Kachel
      mit dem RASTER darunter gleich. Unter „Hörbuch" zeigt das Raster die
      Hoerspiele von Kiddinx; verlangte man „alle Werke in der Kategorie",
      verschwaende die runde Kachel, sobald derselbe Verlag auch EIN Lied
      herausgibt — waehrend seine Hoerspiele einen Zentimeter tiefer stehen.
      Die Reihe behauptete dann, es gebe ihn hier nicht, und das Raster
      widerspraeche ihr sofort. Mit „eines genuegt" gilt sauber:

        runde Kachel sichtbar  GENAU DANN, wenn unten mindestens ein Werk
                               von ihm im Raster steht.

      ── DER FREIGESCHALTETE OHNE EIGENE WERKE ─────────────────────────
      Seit 03.08.2026 kann in config/interpreten.json jemand stehen, von dem
      keine Zeile in data.json liegt („Alin Coen"). Er hat damit KEINE
      Kategorie und faellt in JEDER gewaehlten Kategorie heraus. Das ist
      entschieden, kein Nebeneffekt: Ihn unter „Hörbuch" zu zeigen hiesse zu
      behaupten, hier gebe es Hoerbuecher von ihm — die Seite dahinter zeigt
      „In deiner Box" aber gar nicht erst und besteht nur aus der
      Diskografie. In der UEBERSICHT steht er weiter (dort ist die Auswahl
      `null`), und genau dafuer ist der Weg zur ganzen Diskografie gedacht.

      ── WARUM NICHT `p.werke`, obwohl /api/interpreten sie mitliefert ──
      Die Reihe traegt je Eintrag die Werkschluessel. Sie zu benutzen waere
      eine ZWEITE Wahrheit darueber, was zu wem gehoert — aus einem anderen
      Abruf zu einem anderen Zeitpunkt. `/api/werke` und `/api/interpreten`
      laufen im selben 15-Sekunden-Takt, aber in getrennten Antworten; liegen
      sie um einen Takt auseinander, zeigte die Reihe jemanden, dessen Kachel
      unten fehlt. Die Auswahl kommt aus `werke.liste`, also kommt auch die
      Zugehoerigkeit von dort — ueber `Werk.interpretSchluessel`, denselben
      Schluessel, nach dem `nachRegal` das Regal baut.

      `filter(Boolean)` IST NICHT ZIERRAT: Werke ohne brauchbaren Interpreten
      tragen gar keinen `interpretSchluessel` (werke.ts `interpretTaugt` —
      „External Playback" von „Unknown"). Ohne den Filter laege `undefined`
      in der Menge.

      ── DIE ATTRAPPE KONNTE DIE FRAGE GAR NICHT STELLEN ───────────────
      In tools/neu-vorschau.mjs waren bis 03.08.2026 ALLE VIER erkannten
      Interpreten (WDR, Kiddinx, Europa, Hörspiel) reine `audiobook`-
      Interpreten. Unter „Hörbuch" standen damit alle, unter „Musik" keiner —
      und „eines genuegt" und „alle muessen" sehen dabei GLEICH aus. Die
      Messung haette gruen geleuchtet, ohne die Entscheidung je zu beruehren.
      Seit „Kiddinx Kinderlieder" (music) in WERKE steht, traegt Kiddinx Werke
      in ZWEI Kategorien. Siehe [[attrappe-luegt-durch-weglassen]] — hier in
      der Form „eine ganze SORTE von Daten fehlt", nicht nur ein Feld.

      Das Messwerkzeug meldet das inzwischen SELBST als Fehler: findet es
      keinen Interpreten in mehr als einer Kategorie, bricht es ab, statt
      still gruen zu faerben.

      FALLE BEIM ANHAENGEN: `kennungFuer` und der Werkschluessel der Vorschau
      haengen am laufenden Index, und die zweiten Ausgaben (PARTNER) rechnen
      `WERKE.length + n`. Dieselbe Zahl stand ein zweites Mal als Zeichenkette
      in der Weiterhoeren-Attrappe (`'vorschau:13'`). Mit dem dreizehnten Werk
      rutschte die gerechnete auf 14, die geschriebene blieb — die gemerkte
      Stelle „Kapelle Petra" haette auf ein Werk gezeigt, das es nicht gibt.
      Jetzt rechnet `partnerIndex(n)` an EINER Stelle.

      Nebenbefund: der Kommentar ueber den drei Zahlen (13/15/13) war schon
      vorher falsch — die Liste trug zwoelf Werke. Eine Zahl in einem
      Kommentar altert lautlos.

      ── GEMESSEN (03.08.2026, tools/interpreten-auswahl-schau.mjs) ─────
        Uebersicht (Alles)     WDR, Kiddinx, Europa, Hörspiel
        Kategorie Musik        Kiddinx                       (nur er hat Musik)
        Kategorie Hoerbuch     WDR, Kiddinx, Europa, Hörspiel
        Kategorie Radio        REIHE GANZ WEG                (keiner erkannt)
        Regal Kiddinx          REIHE GANZ WEG                (aeltere Regel)
        Interpretenseite       REIHE GANZ WEG                (aeltere Regel)
        Freigeschaltet, Ueber. + Alin Coen
        Freigeschaltet, Hoerb. OHNE Alin Coen
        Medienliste kaputt     alle                          (Rueckfall)
        Beruehrziel Kachel     92x127 px                     (>= 64 px)

      GEGENPROBE gefahren: mit dem alten `interpreten.reihe` roh meldet das
      Werkzeug 6 Abweichungen. Ein Messwerkzeug, das gegen den alten Stand
      nicht faellt, misst nichts.

      NACHMESSEN:
        node tools/interpreten-auswahl-schau.mjs            # Tabelle je Zustand
        node tools/interpreten-auswahl-schau.mjs --pruefen  # in tools/pruefen.sh

  - kind: falle
    id: laufende-vorschau-teilt-zustand
    title: 'Eine schon laufende Vorschau laesst die Messwerkzeuge ihren Zustand teilen — „Marke im Raster" bricht dann'
    sev: mittel
    match: "die Marke im Raster bewegt sich nicht|Bewegung normal +none, 0s|raster-marke-schau"
    cause: "Jedes Messwerkzeug startet tools/neu-vorschau.mjs nur, WENN keine laeuft, und killt am Ende nur die selbst gestartete. Laeuft schon eine (z. B. von Hand angeworfen), benutzen alle Werkzeuge DIESELBE und erben die Schalter des Vorgaengers. Dieselbe Bauart beim BROWSER: ein verwaister Kopflos-Chromium haelt den Debug-Port, und der naechste Lauf redet mit SEINER alten Seite."
    fix: "Vor tools/pruefen.sh nachsehen: `pgrep -af tools/neu-vorschau.mjs` und `ps -eo pid,cmd | grep remote-debugging-port=93`. Laeuft etwas davon, beenden — oder die Meldung als Messfehler lesen, nicht als Befund."
    tags: [werkzeug, pruefen, oberflaeche, neu, vorschau, falle, browser, 2026]
    source: "aufgefallen 03.08.2026 beim Gesamtlauf zur Interpreten-Reihe; marke-tanzt behoben und nachgemessen 03.08.2026"
    body: |
      DIE MELDUNG SIEHT AUS WIE EIN CSS-FEHLER:

        Marke im Raster                   FEHLER
          Bewegung normal    none, 0s, transform-box fill-box
          FEHLER  die Marke im Raster bewegt sich nicht — sie erbt die
                  Regeln der Lane nicht

      Man sucht daraufhin in NewDesign/app.css nach `marke-balken`. Dort ist
      nichts kaputt: EINZELN aufgerufen meldet dasselbe Werkzeug
      „marke-balken, 0.76s" und keine Abweichung.

      GEMESSEN (03.08.2026, drei Laeufe hintereinander):

        raster-marke-schau allein                        0   marke-balken
        marke-tanzt --pruefen, danach raster-marke       1   none, 0s
        dasselbe OHNE vorher laufende Vorschau        0/0   beide gruen

      WAS GESCHIEHT: `tools/marke-tanzt.mjs` stellt `/vorschau/marke-ruhig`
      und stellt es nicht zurueck. Solange jedes Werkzeug seine EIGENE
      Vorschau startet und am Ende killt, ist das folgenlos — das naechste
      bekommt einen frischen Server. Wer aber vorher von Hand
      `node tools/neu-vorschau.mjs` gestartet hat (etwa um zwischendurch
      `curl /api/werke` zu fragen), nimmt allen Werkzeugen genau diesen Schutz
      weg: keines startet mehr eine eigene, keines killt eine, und der
      Zustand wandert von Schritt zu Schritt.

      DIE LEHRE FUER NEUE WERKZEUGE: Was ein Werkzeug an der Vorschau
      umstellt, stellt es im `finally` zurueck — auch wenn es die Vorschau
      nicht selbst gestartet hat. `tools/interpreten-auswahl-schau.mjs` macht
      das seit dem ersten Tag so (`zuruecksetzen()` fuer `frei-`, `nein-`
      und `voll`).

      ACHTUNG — HIER STAND EINE BEHAUPTUNG, DIE NICHT STIMMTE (richtiggestellt
      bei der Abnahme am 04.08.2026). Der Text sagte, `tools/marke-tanzt.mjs`
      raeume seit dem 03.08.2026 ebenfalls im `finally` auf, und nannte dazu
      eine gemessene gruene Kette. NACHGESEHEN AM BAUM: die Datei enthaelt
      weder `zuruecksetzen` noch ein `finally` (gegrept, 187 Zeilen), sie ist
      zeichengleich mit dem Stand aus b3ce4fa4, und ihre Schleife endet auf
      `/vorschau/marke-ruhig`, ohne etwas zurueckzustellen. Die Aenderung ist
      offenbar geschrieben und wieder verlorengegangen (Dateidatum 23:02,
      Inhalt = HEAD). WER SIE NACHTRAEGT, misst die Kette oben neu — die dort
      genannten Zahlen sind bis dahin KEIN Beleg.

      DIE WERKZEUGE STELLEN ALSO WEITER NICHTS ZURUECK — `raster-marke-schau`
      etwa laesst `spotify` und `voll` stehen, `marke-tanzt` laesst
      `marke-ruhig` stehen. Es faellt nur nicht auf, weil jedes seine Lagen am
      Anfang selbst setzt und im Gesamtlauf jedes seine EIGENE Vorschau
      startet und killt. Nachgemessen am 04.08.2026: `tools/pruefen.sh`
      vollstaendig und ohne Vorschau von Hand — alle 42 Schritte gruen,
      darunter „Marke tanzt" und unmittelbar danach „Marke im Raster". Die
      Falle besteht unveraendert; sie schlaegt erst zu, wenn jemand von Hand
      eine Vorschau laufen laesst.

      ── DIESELBE FALLE MIT DEM BROWSER ─────────────────────────────────────

      Beim Nachmessen am 03.08.2026 fiel die Schwesterbauart auf, und sie
      kostete mehr Zeit als die eigentliche Ursache. Jedes Werkzeug startet
      seinen eigenen Kopflos-Chromium auf einem festen Debug-Port
      (`marke-tanzt` 9351, `raster-marke-schau` 9357) und killt ihn am Ende.
      Wird das Werkzeug aber abgebrochen — oder killt man versehentlich seine
      ELTERNSCHALE, etwa mit einem `pkill -f`, dessen Muster auf die eigene
      Kommandozeile passt —, bleibt der Chromium verwaist am Port haengen.

      Der naechste Lauf merkt davon NICHTS: `spawn` gelingt, das neue Chrome
      kann 9357 nur nicht binden und beendet sich still, und
      `fetch(127.0.0.1:9357/json/list)` beantwortet der ALTE Browser. Gemessen
      wird dann dessen Seite mitsamt allem, was der vorige Lauf dort angetippt
      hat. Das sah so aus:

        mpv laeuft, nichts gestartet    „Die Maus"
        FEHLER  1 Kachel(n) behaupten „laeuft", obwohl niemand weiss, was
                mpv da spielt

      — reproduzierbar in DREI Laeufen hintereinander, auch mit jeweils
      frischer Vorschau, und darum ueberzeugend wie ein echter Befund. In
      Wahrheit trug die Seite des verwaisten Browsers noch das
      `zuletztGestartet` aus einem frueheren Lauf. Nach `kill` des Verwaisten
      war derselbe Schritt sofort wieder gruen.

      WORAN MAN ES ERKENNT: `ps -eo pid,etimes,cmd | grep
      remote-debugging-port=93` — steht dort ein Prozess, der AELTER ist als
      der laufende Aufruf, misst man dessen Seite. Und beim Aufraeumen kein
      `pkill -f` auf ein Muster, das im eigenen Befehl vorkommt; die Schale
      erschlaegt sich sonst selbst und laesst genau die Waisen zurueck, um die
      es ging.

  - kind: falle
    id: umzug-laesst-die-verweise-darauf-zurueck
    title: "Ein Schalter zieht um — und die Verweise AUF ihn bleiben stehen"
    sev: mittel
    match: "in der Darstellung umstellen|Verweis zeigt auf die falsche Seite|Schalter nicht auf der Seite|Umzug Verweise"
    cause: "Beim Umsortieren wird die Seite gesucht, auf der der Schalter STEHT. Die Seiten, die IHN NENNEN, stehen woanders und fallen nicht auf — grep nach dem Feldnamen findet sie nicht, denn sie nennen ihn im Fliesstext."
    fix: "Nach jedem Umzug: grep nach dem SICHTBAREN NAMEN des Schalters (nicht dem Feldnamen) ueber alle Vorlagen — und die routerLinks der Fundstellen nachziehen."
    tags: ['admin', 'verwaltung', 'falle', 'umzug', 'gegenlesen', 2026]
    source: "Gegenlesen der vier Umzuege, 03.08.2026 (Commit 4b2a00c4)"
    body: |
      GEFUNDEN BEIM GEGENLESEN, in der laufenden Oberflaeche und nicht im Diff.

      Am 03.08.2026 wanderten „Doppelte zusammenfassen" und „Ganze
      Diskografie" von der Darstellungsseite auf die MEDIENSEITE (G5). Beide
      Unterseiten, die diese Schalter nur ANZEIGEN, sagten weiterhin:

          Zusammenfassen ist AUS   [in der Darstellung umstellen]  -> /darstellung
          Ganze Diskografie ist AN [in der Darstellung umstellen]  -> /darstellung

      Der Verweis war NICHT tot — die Seite gibt es ja. Er schickte nur
      ausgerechnet die Leute, die den Schalter suchen, auf eine Seite, auf der
      er nicht mehr steht. Dieselbe Sorte Fehler wie die Uebersichts-Kachel,
      die „Anzeige neu starten" versprach; die wurde im selben Umbau behoben,
      diese hier blieb liegen.

      WARUM ES NIEMAND SIEHT:
        * Der Uebersetzer nicht — `/darstellung` ist eine gueltige Route.
        * Der Suchbestand nicht — Verweise (`<a>`) werden gar nicht erhoben.
        * Ein grep nach dem FELDNAMEN (`verschmelzen`, `diskografie`) auch
          nicht: die Fundstellen nennen den Schalter im Fliesstext bei seinem
          sichtbaren Namen.
        * Und ein Werkzeug kann es nicht finden, denn es kann nicht wissen,
          was ein Verweis VERSPRICHT. Es ist eine Frage an den Menschen.

      DIE PRUEFFRAGE FUER DEN NAECHSTEN UMZUG lautet deshalb nicht „wo steht
      der Schalter jetzt?", sondern: WER REDET UEBER IHN? Vor dem Verschieben
      einmal nach dem sichtbaren Namen greppen — der Treffer, der KEIN
      Bedienelement ist, ist der, den man sonst vergisst.

  - kind: falle
    id: unterseite-ohne-rueckweg
    title: "Eine Unterseite ohne Rueckweg: kein Leisteneintrag leuchtet, und zurueck geht nur der Browser"
    sev: mittel
    match: "kein Leisteneintrag aktiv|routerLinkActive leuchtet nicht|Unterseite Rueckweg|verwaltung-wege-schau"
    cause: "Bis 03.08.2026 stand JEDE Seite der Verwaltung in der Kopfleiste — 'wo bin ich' beantwortete die leuchtende Marke. Mit der ersten Unterseite faellt diese Antwort ersatzlos weg, und niemand vermisst sie, weil beim Bauen alle ueber den neuen Weg HINEIN gehen."
    fix: "Jede Unterseite bekommt oben einen Rueckweg auf ihre Elternseite. Geprueft von tools/verwaltung-wege-schau.mjs (haengt in pruefen.sh)."
    tags: ['admin', 'verwaltung', 'gliederung', 'falle', 'gegenlesen', 'werkzeug', 2026]
    source: "Gegenlesen der vier Umzuege, 03.08.2026 (Commit 4b2a00c4)"
    body: |
      GEMESSEN in der laufenden Verwaltung gegen die Attrappe:

          /admin/verschmelzung   nav a.hier -> []   main a -> nur /darstellung
          /admin/interpreten     nav a.hier -> []   main a -> nur /darstellung

      Vorher leuchtete dort „Doppelte" bzw. „Interpreten" in der Kopfleiste.
      Nach dem Umzug leuchtet GAR NICHTS: die Seite steht nicht mehr in der
      Leiste, und „Medien" leuchtet nicht mit (routerLinkActive vergleicht den
      Pfad, und /verschmelzung faengt nicht mit /medien an).

      DAS SZENARIO, das es teuer macht — und der Grund, warum es beim Bauen
      nicht auffaellt: Wer die Seiten baut, kommt IMMER ueber den neuen Weg
      auf der Medienseite herein und weiss deshalb, wo er ist. Wer ueber ein
      Lesezeichen kommt (die Adressen sind mit Absicht unveraendert!) oder
      ueber die Suche der Verwaltung, landet auf einer Seite, deren Name in
      der Kopfleiste nicht vorkommt — und der einzige Verweis auf der Seite
      zeigte auch noch woandershin ([[umzug-laesst-die-verweise-darauf-zurueck]]).

      BEHOBEN mit einem „← Medien" oben auf beiden Seiten. Gemessene Hoehe des
      Beruehrziels 47 px: in Textgroesse waeren es 22 — die Verwaltung wird
      meistens am Handy bedient.

      NEUES WERKZEUG, weil eine Regel ohne Pruefung eine Bitte ist:
      `tools/verwaltung-wege-schau.mjs` (+ .test.mjs, in pruefen.sh als
      „Wege der Verwaltung"). Es liest Kopfleiste, Routen und Unterseiten aus
      denselben Funktionen wie der Suchbestand — keine zweite Meinung — und
      meldet drei Dinge:

          TOTER WEG       routerLink auf einen Pfad, den app.routes.ts nicht
                          kennt (Angular leitet still auf die Uebersicht um)
          OHNE RUECKWEG   Unterseite ohne Verweis auf ihre Elternseite
          VERWAIST        Seite, zu der weder Leiste noch Seite einen Weg hat

      Gegen den Stand VOR der Behebung meldet es beide Seiten und endet mit 1;
      das wurde nachgestellt, nicht behauptet. Verweise in HTML-Kommentaren
      zaehlen dabei nicht — diese Vorlagen nennen alte Wege beim Namen.

  - kind: falle
    id: umzug-nimmt-die-meldungszeile-nicht-mit
    title: "Der Knopf zieht um, seine Antwort bleibt — zwei Meldungszeilen, die sich gegenseitig loeschen"
    sev: mittel
    match: "Meldung am Seitenende|meldung wird ueberschrieben|erledigt verschwindet|Antwort auf den Knopfdruck"
    cause: "Eine Seite hat EINE Meldungszeile an EINEM Ort. Zieht ein Knopf hinzu, teilt er sie sich mit allem anderen — und der naechste Handgriff (oft ein Nachladen im selben Ablauf) ersetzt seine Antwort."
    fix: "Was zusammen bedient wird, meldet zusammen: eigene Zeile in der eigenen Karte. Dauerhafte Auskuenfte (Ladefehler) gehoeren ohnehin nicht in die Zeile fuer Ereignisse."
    tags: ['admin', 'verwaltung', 'oberflaeche', 'falle', 'gegenlesen', 2026]
    source: "Gegenlesen der vier Umzuege, 03.08.2026 (Commit 4b2a00c4)"
    body: |
      ZWEIMAL DASSELBE MUSTER, beide beim Gegenlesen gemessen, beide eine
      Folge davon, dass ein Bedienelement auf eine fremde Seite gezogen wurde.

      ── 1. MEDIENSEITE: die Antwort landet zwei Bildschirme tiefer ──────
      „Medien neu einlesen" kam von der Systemseite, wo die Meldungen OBEN bei
      den Knoepfen stehen. Auf der Medienseite steht `meldung()` ganz unten,
      hinter der ganzen Bibliothek. Gemessen bei 800 px Breite und LEERER
      Bibliothek:

          Knopf     y =  774
          Meldung   y = 1649      Abstand 875 px, Sichtfenster 480 px

      Mit einer echten Bibliothek wird der Abstand groesser, nicht kleiner.

      SCHLIMMER ALS WEIT WEG: `aktionAusloesen` ruft nach dem Erfolg
      `laden()`, und `laden()` schreibt im Fehlerfall in DIESELBE Zeile. Ein
      gelungenes Einlesen bei kaputter Bibliothek meldete also „erledigt." und
      ersetzte das Millisekunden spaeter durch „Die Bibliothek konnte nicht
      geladen werden." — die Antwort war weg, bevor sie jemand lesen konnte.
      Genau so gemessen gegen die Attrappe.

      BEHOBEN mit `aktionMeldung`, einer eigenen Zeile in der eigenen Karte:
      Abstand jetzt 80 px, und `laden()` kommt nicht mehr daran.

      ── 2. KINDERZEIT: der Timer loescht die Warnung ────────────────────
      Der Schlummer-Timer kam auf die Kinderzeit-Seite und schreibt in
      dieselbe `meldung()` wie die Regeln. Ablauf, gemessen:

          /api/kinderzeit faellt aus -> „Die Regeln konnten nicht geladen
                                        werden."
          Tipp auf „Timer starten"  -> „Die Box schaltet sich in 30 Minuten
                                        ab."   (die Warnung ist weg)

      Was dann dasteht, ist eine Tabelle voller VORGABEWERTE, und nichts sagt
      es mehr. Der Unterschied ist nicht kosmetisch: `meldung` ist die Zeile
      fuer „was gerade passiert ist" und darf ueberschrieben werden — „die
      Regeln, die du siehst, sind nicht die der Box" gilt weiter, bis es
      behoben ist.

      BEHOBEN mit `regelnFehler`: eigenes Feld, eigener Ort (oben, umrandet),
      und der Text sagt jetzt auch, was er bedeutet („… sind Vorgabewerte und
      nicht die Einstellung dieser Box"). Ein Test in kinderzeit.spec.ts haelt
      fest, dass ein gestarteter Timer die Warnung stehen laesst; ohne die
      Behebung ist er rot (nachgestellt).

      ── DER OFFENE REST, groesser als beide ─────────────────────────────
      Solange die Regeln NICHT geladen sind, ist die Tabelle trotzdem
      bedienbar — und „Montag auf alle Wochentage uebertragen" schickt dann
      ein PUT mit den VORGABEWERTEN an die Box. Das ueberschreibt echte Regeln
      mit leeren. Das ist aelter als der Umzug und hier nicht behoben; es
      gehoert in einen eigenen Gang (Kinderzeit sperren, solange
      `regelnFehler` steht).

  - kind: falle
    id: attrappe-kennt-den-fehlschlag-nicht
    title: "Eine Attrappe, die nur gelingen kann, zeigt genau den Zweig nicht, um den es ging"
    sev: mittel
    match: "POST setzt immer an|stopp true ignoriert|Attrappe Fehlschlag|schlummer-stur|Timer abbrechen wirkungslos"
    cause: "Attrappen werden am Normalfall entlang gebaut: der Endpunkt antwortet ok. Der Zweig, der eine Aenderung ueberhaupt noetig machte, ist aber fast immer der FEHLERFALL — und der ist ohne einen eigenen Schalter nicht vorfuehrbar."
    fix: "Zu jedem Endpunkt, an dem die Oberflaeche einen Fehlerzweig hat, gehoert ein Schalter in tools/neu-vorschau.mjs, der ihn ausloest."
    tags: ['attrappe', 'vorschau', 'werkzeug', 'falle', 'gegenlesen', 2026]
    source: "Gegenlesen der vier Umzuege, 03.08.2026 (Commit 4b2a00c4)"
    body: |
      DREI LUECKEN IN tools/neu-vorschau.mjs, alle beim Gegenlesen gefunden,
      alle derselben Bauart: was die Oberflaeche kann, konnte die Attrappe
      nicht zeigen ([[attrappe-luegt-durch-weglassen]]).

      ── 1. POST /api/schlummer setzte IMMER „an" ────────────────────────
      Auch bei `{ stopp: true }`. Gemessen: die Seite meldete „Der Timer wurde
      abgebrochen", zeigte wieder das Zahlenfeld — und ein GET danach sagte
      weiter `laeuft: true`. Ein Neuladen holte den Timer zurueck, den man
      gerade abgestellt zu haben glaubte.

      DAS BITTERE DARAN: Der Umzug des Timers am 03.08.2026 hat an genau
      diesem Knopf etwas GERADEGEZOGEN (nur melden, was gelungen ist). Der
      Beleg „gesehen, nicht nur getestet" konnte diesen Zweig gar nicht
      erfasst haben — die Attrappe kannte ihn nicht. Sie prueft still das
      Nichts.

      Behoben: der Koerper wird gelesen, `stopp` unterschieden — wie im echten
      Server (server.ts, POST /api/schlummer). Dazu NEU
      `curl localhost:8299/vorschau/schlummer-stur`: ein Timer laeuft und
      laesst sich nicht abstellen (POST -> 500). Erst damit ist die Zeile
      „Der Timer liess sich nicht abbrechen — er laeuft weiter." ueberhaupt
      auf dem Bildschirm zu sehen; sie ist der ganze Grund der Aenderung.

      ── 2. /api/kinderzeit gab es gar nicht (404) ───────────────────────
      Die Kinderzeit-Seite meldete deshalb IMMER „Die Regeln konnten nicht
      geladen werden.". Solange der Schlummer-Timer auf der Systemseite stand,
      war das egal; seit er auf DIESER Seite steht, kann man ihn ohne den
      Zweig nur neben einer kaputten Seite ansehen.
      Neu: GET/PUT /api/kinderzeit, GET /api/kinderzeit/stand, bonus,
      zuruecksetzen — und `curl /vorschau/kz-an` / `kz-aus`.
      NICHT verwechseln mit `/vorschau/kinderzeit`: das laesst den
      ABSPIELBEFEHL an 403 scheitern und sagt nichts ueber die Regeln.

      ── 3. POST /api/system/:id fehlte ──────────────────────────────────
      Die Attrappe kannte nur GET /api/system. Die Karte „Neu einlesen" war
      also zu sehen, der Knopf aber nicht zu druecken — jedes POST fiel in den
      404-Zweig, und die Seite meldete „Die Box hat nicht geantwortet.". Das
      sieht aus wie ein Fehler der SEITE; die Attrappe luegt hier in die
      teure Richtung.
      Neu: POST /api/system/:id antwortet ok, eine unbekannte Kennung mit 400
      (derselbe Zweig, der in der Oberflaeche „Diese Aktion gibt es nicht."
      heisst). Die Aktionsliste steht dafuer an EINER Stelle im Werkzeug
      (SYSTEM_AKTIONEN) und bedient beide Zweige — sonst boete die Vorschau
      einen Knopf an, den ihr eigener zweiter Zweig abweist.

  - kind: falle
    id: schritt-am-geraet-immer-rot-ohne-box
    title: "Ein Pruefschritt „wenn erreichbar“, der ohne Box rot wird"
    sev: niedrig
    match: "Marke am Geraet.*FEHLER|Die Box antwortet nicht.*nichts zu messen|marke-am-geraet"
    cause: "tools/marke-am-geraet.mjs endete im Zweig „keine Box“ mit `process.exit(PRUEFEN ? 1 : 0)` — also mit einem Fehler, obwohl Kopfkommentar und pruefen.sh beide sagen, der Schritt ueberspringe sich selbst."
    fix: "Behoben 03.08.2026: der Zweig endet mit 0 und schreibt „Schritt uebersprungen\". Wer die Box wirklich verlangt, nimmt tools/pruefen.sh --box (dort steht mupi-check)."
    tags: ['werkzeug', 'pruefen', 'falle', 'gegenlesen', 2026]
    source: "Gegenlesen der vier Umzuege, 03.08.2026 — der Gesamtlauf war rot, ohne dass etwas kaputt war"
    body: |
      GEMESSEN im Gesamtlauf, ohne erreichbare Box:

          Marke am Geraet (wenn erreichbar) FEHLER 3s
            Box 192.168.178.169:8200 — nur lesend
            Die Box antwortet nicht (fetch failed) — nichts zu messen.

      Der Schritt HEISST „wenn erreichbar", sein Kopfkommentar sagt „und
      ueberspringt sich selbst, wenn keine Box antwortet", und das Werkzeug
      tat es nicht. An jedem Rechner ohne Box war der Gesamtlauf damit
      dauerhaft rot.

      WARUM DAS TEURER IST, ALS ES AUSSIEHT — der Kopf von pruefen.sh sagt es
      selbst, an anderer Stelle: „Ein Pruefschritt, der immer rot ist, ist
      schlimmer als keiner: Man gewoehnt sich an ihn und uebersieht den Tag,
      an dem er zu Recht rot wird."

      NUR DIESER EINE ZWEIG wurde geaendert. Antwortet die Box und liefert
      etwas Unerwartetes (kein Werk zur Kennung, Marke fehlt), bleibt der
      Schritt rot — das ist ja der Messgegenstand.
  # ══ Weiterhoeren: der Tipp spielt, aber die Anzeige steht ══════════════════
  - kind: falle
    id: weiterhoeren-stop-und-start-im-wettlauf
    title: "„Es spielt, zeigt aber keinen Fortschritt“ — stop und Start laufen bei Spotify um die Wette"
    match: "spielt aber zeigt keinen fortschritt|player steht noch auf play|is_playing.*false.*progress.*0|Weiterhoeren.*Knopf bleibt"
    sev: hoch
    cause: >
      `weiterSpielen` schickt `stop` und unmittelbar danach den Spotify-Startbefehl.
      `stop()` im Abspieldienst (spotify-control.ts:791) ruft `spotifyApi.pause()` und
      wartet das Versprechen NICHT ab - es antwortet sofort 200. Damit sind ZWEI
      Netzaufrufe gleichzeitig zu Spotify unterwegs, rund zehn Millisekunden
      auseinander. Kommt die Pause als zweite an, meldet /player/state danach
      dauerhaft `is_playing: false` und `progress_ms: 0`, waehrend librespot spielt.
    fix: >
      Atempause zwischen beiden - `SPOTIFY_ATEMPAUSE_MS = 1500` in NewDesign/app.js,
      und nur dann, wenn vorher wirklich Spotify lief (`/player/local currentPlayer`).
      Dieselbe Zahl und derselbe Grund wie in `springen.loslassen`.
    tags: ['falle', 'weiterhoeren', 'resume', 'spotify', 'oberflaeche', 'neu', 'wettlauf', 'messen', 2026]
    source: "03.08.2026, Box .169, tools/weiterhoeren-am-geraet.mjs + tools/weiterhoeren-wettlauf.mjs"
    body: |
      GEMELDET am 03.08.2026, woertlich: „wir muessen auch nochmal die
      weiterhoeren logic checken da scheint ein bug zu sein manchmal kann man
      ein eintrag anwaehlen es spielt aber zeigt keinen fortschritt der player
      steht noch auf play".

      Also: Ton ja, Anzeige nein. Und „manchmal".

      ── DIE MESSUNG, UND WARUM SIE OHNE TON NICHTS WERT WAERE ──────────────
      „Es spielt, zeigt aber nichts" und „es spielt gar nicht" sehen an beiden
      Schnittstellen GLEICH aus. Erst der Spitzenpegel am Mithoerausgang
      entscheidet. Gemessen wurde deshalb dreierlei zugleich: /player/local,
      /player/state und der Ton.

          ohne Atempause   3 von 4 Runden fehlerhaft
          mit 1500 ms      0 von 4 Runden fehlerhaft
          Ton in ALLEN acht Runden   Spitze 0,27 bis 0,34

      Das „manchmal" ist die eine Runde von vieren, die durchging.

      ── DIE ZEITLEISTE EINER FEHLERHAFTEN RUNDE ───────────────────────────
      Vorlauf „Alltag im All" laeuft. Dann der Tipp (stop, sofort Start von
      Album `53tv…`, Titel 2, ab 125412 ms):

            0 ms   stop geschickt          /player/state leer
          180 ms   Start geschickt
          200 ms   „Alltag im All"  is_playing=true   (Spotifys Nachlauf)
         1200 ms   „Give It Away"   is_playing=FALSE  progress_ms=0
         …
        22000 ms   „Give It Away"   is_playing=FALSE  progress_ms=0

      Einundzwanzig Sekunden ohne jede Erholung, waehrend der Lautsprecher
      spielt. Die Oberflaeche zeigt daraufhin WAHRHEITSGEMAESS das
      PLAY-Symbol und einen Balken auf 0 - sie hat keinen Fehler, sie hat nur
      eine Quelle, die luegt.

      ── WARUM DIE VORHANDENE NACHLAUF-KORREKTUR NICHT GREIFT ──────────────
      `fortschritt.ts` buegelt Spotifys verspaetete Antworten aus, aber nur
      innerhalb von `TOLERANZ_MS = 2500`. Das ist richtig so und darf NICHT
      hochgesetzt werden: `spieltWirklich` wuerde sonst eine echte Pause kurz
      nach dem Start verschlucken. Hier sind es 21 Sekunden - das ist kein
      Nachlauf mehr, das ist ein anderer Zustand.

      ── DIE GEGENPROBEN, die den Verdacht erst zum Befund machen ───────────
        OHNE das `stop`     44 von 44 Ablesungen „laeuft" - ABER der Start
                            landete bei 0,7 s statt an der gemerkten Stelle.
                            Das `stop` wegzulassen ist also KEINE Loesung
                            ([[spotify-vorwaerts-spulen-wirkungslos]]: ein
                            Sprung nach vorn wirkt bei laufender Wiedergabe
                            nicht).
        Box RUHTE vorher    3 Laeufe, alle fehlerfrei. `/player/local` meldete
                            `currentPlayer: ""` - dann schickt `stop()` gar
                            keinen Spotify-Aufruf, und es gibt nichts, womit
                            der Start um die Wette laufen koennte.

      Die zweite Gegenprobe ist zugleich die Bedingung der Reparatur: gewartet
      wird NUR, wenn `/player/local` `currentPlayer: "spotify"` meldet.

      ── WAS DAMIT IM BACKLOG BEANTWORTET IST ──────────────────────────────
      E24 fragte unter W1: „Welcher Dienst? Der Spotify-Weg ist EIN Befehl
      ohne Wartezeiten - trifft der Fehler ihn auch, ist die Wartezeit-These
      falsch." ANTWORT: Ja, er trifft den Spotify-Weg. Die dort vermutete
      Ursache (zu kurze feste Wartezeiten im mpv-Weg) ist fuer DIESEN Fall
      widerlegt. W3 („laeuft der Ton dabei?") ist mit dem Pegel beantwortet.

      ── SEIT DEM 04.08.2026 AN DER URSACHE REPARIERT ──────────────────────
      Die feste Atempause war die Reparatur am SYMPTOM. Sie kostete die Zeit
      IMMER, sie stand nur im Weiterhoeren-Weg, und sie muesste in JEDE
      Oberflaeche geschrieben werden, die diesen Dienst anspricht. Jetzt
      wartet `stop()` selbst - siehe [[anhalten-wartet-auf-spotify]].
      Damit ist die Atempause vom Regelfall zum RUECKFALL geworden, und die
      drei Punkte, die hier offen standen, sind beantwortet:

      * DER GEWOEHNLICHE KACHELTIPP (E24/O1) ist miterledigt. Er geht durch
        dieselbe Funktion `anhaltenUndAbstand`, und die 1,5 s Stille, wegen
        derer die Entscheidung beim Benutzer lag, fallen gar nicht erst an -
        der Dienst bestaetigt in 76 bis 93 ms.
      * DIE SAUBERERE REPARATUR (E24/O2) ist gebaut UND gemessen, ohne
        auszurollen: tools/anhalten-am-dienst.mjs startet den wirklichen
        Abspieldienst und ersetzt nur die Spotify-Bibliothek durch eine
        Attrappe mit einstellbarer Pausendauer.
      * DIE 800-MS-FRAGE (E24/O3) hat sich erledigt. 1500 ms sind jetzt eine
        OBERGRENZE und keine Wartezeit; im Alltag kostet sie nichts. Wer sie
        senken wollte, spart nur im Ausfall.

      NACHMESSEN:
        node tools/weiterhoeren-wettlauf.mjs --runden 4 --warten 1500   # Box
        node tools/anhalten-am-dienst.mjs --pruefen        # der echte Dienst
        node tools/weiterhoeren-atempause.mjs --pruefen                 # Attrappe

  - kind: entscheidung
    id: anhalten-wartet-auf-spotify
    title: "`stop` antwortet erst, wenn Spotify die Pause bestaetigt hat — mit Obergrenze"
    match: "stop.*wartet|angehalten|ANHALTE_FRIST_MS|anhaltenUndAbstand|Atempause.*Rueckfall"
    sev: hoch
    tags: ['entscheidung', 'abspieler', 'spotify', 'wettlauf', 'zeitschranke', 'messen', 'neu', 2026]
    source: "04.08.2026, spotify-control.ts + NewDesign/app.js, tools/anhalten-am-dienst.mjs + tools/pause-zeitschranke-messen.mjs"
    body: |
      DIE URSACHE VON [[weiterhoeren-stop-und-start-im-wettlauf]] SITZT IM
      ABSPIELDIENST, nicht in der Oberflaeche: `stop()` rief
      `spotifyApi.pause()` und antwortete sofort 200, ohne das Versprechen
      abzuwarten. Seit dem 04.08.2026 wartet es ab.

      ── WAS DAS ABWARTEN KOSTET, UND WARUM ES EINE OBERGRENZE BRAUCHT ─────
      Die Frage MUSSTE vor der Reparatur beantwortet werden: `stop` geht bei
      JEDEM Kacheltipp hinaus. Gemessen (tools/pause-zeitschranke-messen.mjs):

          http-manager.js der Bibliothek ruft `.timeout()` NIRGENDS auf.
          superagent hat ohne diesen Aufruf keine eigene Frist.

          Wirt antwortet sofort (hier im Haus)          5 ms
          Wirt nimmt an und SCHWEIGT                    laeuft nach 201 s noch
          Adresse geroutet, aber tot — AUF DER BOX      134,8 s
          Rundlauf Box .169 -> api.spotify.com          76 bis 93 ms

      Die 134,8 s sind genau die sechs Wiederholungen des Verbindungsaufbaus
      (`tcp_syn_retries=6` auf der Box, `tcp_retries2=15`). Ohne Obergrenze
      haette ein DSL-Ausfall jeden Antipper zwei Minuten lang haengen lassen —
      schlimmer als der Fehler, der repariert wurde.

      `ANHALTE_FRIST_MS = 1500` ist deshalb eine OBERGRENZE, KEINE Wartezeit.
      Im Alltag kostet sie nichts (93 ms gemessen). Die Zahl ist nicht neu
      erfunden: 1500 ms stehen im Projekt schon zweimal fuer „zwei
      Spotify-Befehle brauchen Abstand".

      ── DIE ANTWORT SAGT JETZT, WIE ES AUSGING ────────────────────────────
      `/<raum>/stop` antwortet `{status:'ok', error:'none', angehalten:<wort>}`
      mit vier moeglichen Woertern:

          bestaetigt        Spotify hat quittiert — nichts mehr unterwegs
          nichts-zu-tun     es lief nichts, es ging kein Aufruf hinaus
          fehlgeschlagen    der Aufruf ist zu Ende, nur eben schlecht
          frist-abgelaufen  1,5 s gewartet, nichts gehoert — die Pause IST
                            noch unterwegs

      Die Oberflaeche (`anhaltenUndAbstand` in NewDesign/app.js) haelt nur bei
      den letzten beiden Faellen noch selbst Abstand — und dann, wie bisher,
      nur wenn vorher wirklich Spotify lief.

      ── DER WICHTIGSTE FALL IST DER FEHLENDE ────────────────────────────
      FEHLT das Feld ganz, ist ein ALTER Abspieldienst am anderen Ende, und
      die Oberflaeche faellt auf die feste Atempause zurueck. Das ist kein
      Aberglaube: server.js und spotify-control.js liegen auf der Box in ZWEI
      VERSCHIEDENEN Ordnern und lassen sich einzeln erneuern
      ([[spotify-control-liegt-woanders]]).

      Die Attrappe kann diesen Fall deshalb ausdruecklich stellen
      (`curl localhost:8299/vorschau/anhalten-alt`). Ohne ihn zeigte sie nur
      die Welt, in der die Reparatur schon ueberall angekommen ist — genau
      [[attrappe-luegt-durch-weglassen]].

      ── WAS *NICHT* ABGEWARTET WIRD ──────────────────────────────────────
      Der eigene Zustand. `currentPlayer` und die uebrigen Felder werden
      SOFORT geleert, vor dem Warten. Zwei Gruende: `/player/local` muss
      augenblicklich „hier laeuft nichts" sagen (die Oberflaeche fragt das VOR
      dem `stop`), und ein ZWEITES `stop` waehrend des Wartens fuende sonst
      `currentPlayer` noch auf 'spotify' und schickte eine zweite Pause.

      ── UND WO NICHT GEWARTET WIRD ───────────────────────────────────────
      `albumSpielen` und der mpv-Zweig von `weiterSpielen` gehen weiter ohne
      Abstand. Was dort folgt, ist ein mpv-Start; es geht KEIN zweiter
      Spotify-Befehl hinaus, mit dem eine Pause um die Wette laufen koennte.
      Der Wettlauf braucht ZWEI Spotify-Befehle.

      ── WAS DIE REPARATUR GEKOSTET HAT, UND WAS SIE OFFEN LAESST ─────────
      Damit `stop()` abgewartet werden kann, MUSSTE der Befehlsverteiler
      `async` werden — und das hat eine eigene Falle aufgemacht, siehe
      [[express4-async-zuhoerer-antwortet-nicht]]. Sie ist seit dem
      05.08.2026 mit einem Auffangnetz zugedeckt.

      OFFEN GEBLIEBEN IST DAS NACHTIPPEN. `currentPlayer` wird VOR dem Warten
      geleert (aus gutem Grund, siehe oben). Ein zweites `stop`, das waehrend
      des Wartens hereinkommt, findet deshalb nichts mehr und meldet
      `nichts-zu-tun` — die Oberflaeche haelt darauf hin KEINEN Abstand,
      obwohl die ERSTE Pause noch unterwegs ist. Gemessen am 05.08.2026
      (tools/anhalten-am-dienst.mjs, Fall 5; Pause 900 ms, nachgetippt nach
      200 ms):

          zweites stop   Antwort nach   3 ms   nichts-zu-tun
          erstes  stop   Antwort nach 905 ms   bestaetigt

      Das ist KEIN Rueckschritt — vor der Reparatur raste jeder einzelne Tipp,
      nicht nur der nachgetippte. Es ist der Rest, der bleibt. Die Behebung
      waere, das laufende Pausen-Versprechen im Dienst zu merken und ein
      zweites `stop` daran anzuhaengen (BACKLOG E24/O6).

      NACHMESSEN:
        node tools/anhalten-am-dienst.mjs --pruefen      # der echte Dienst
        node tools/weiterhoeren-atempause.mjs --pruefen  # beide Tippwege

  - kind: falle
    id: express4-async-zuhoerer-antwortet-nicht
    title: "Ein `async`-Zuhoerer in express 4: ein Wurf wird zu GAR KEINER Antwort"
    match: "app.use\\(async|express.*async.*haengt|Kachel leuchtet ewig|keine Antwort vom Abspieldienst|unhandledRejection.*express|Verteiler.*async"
    sev: hoch
    cause: "express 4 fasst einen SYNCHRONEN Zuhoerer in ein try/catch (Layer.handle_request) und antwortet auf einen Wurf mit 500. Auf das VERSPRECHEN eines `async`-Zuhoerers sieht sein Router nirgends hin — eine Ablehnung verschwindet still, und `res.send` wird nie erreicht."
    fix: "Einen duennen SYNCHRONEN Zuhoerer darueberlegen, der die async-Funktion aufruft und `.catch(...)` anhaengt: protokollieren und, wenn `!res.headersSent`, mit 500 antworten. So gebaut in spotify-control.ts ueber `befehlVerteilen`. Nachmessen: tools/verteiler-wirft-messen.mjs --pruefen."
    tags: ['falle', 'express', 'abspieler', 'verteiler', 'haenger', 'messen', 'neu', 2026]
    source: "05.08.2026, src/backend-player/src/spotify-control.ts + tools/verteiler-wirft-messen.mjs (express 4.21.2)"
    body: |
      WER EINEN EXPRESS-ZUHOERER `async` MACHT, TAUSCHT „500" GEGEN „NICHTS".
      Das faellt beim Bauen nicht auf, weil der Dienst weiterlaeuft und alle
      anderen Anfragen normal beantwortet werden. Es haengt nur die EINE.

      GEFUNDEN BEIM GEGENLESEN VON E24/O2. Der Befehlsverteiler des
      Abspieldienstes war bis zum 04.08.2026 synchron; damit `stop()`
      abgewartet werden konnte ([[anhalten-wartet-auf-spotify]]), wurde er
      `async`. Gemessen am wirklich gestarteten Dienst
      (tools/verteiler-wirft-messen.mjs, `GET /<raum>/radio/%`):

          Verteiler synchron (Stand vor E24/O2)   Antwort 500 nach     6 ms
          Verteiler `async`, ohne Auffangnetz     KEINE Antwort nach 5000 ms
          Verteiler `async`, mit Auffangnetz      Antwort 500 nach     5 ms

      UND ES GIBT ETWAS ZU WERFEN, DAS KEIN ANGRIFF IST. Der Verteiler ruft
      `decodeURIComponent` selbst (Zweige `radio`, `jellyfin`, `ard`, `rss`)
      und ueber `deleteLocal`. Ein einzelnes `%` im Namen wirft dort URIError —
      genau die Sorte, gegen die befehlspfad.ts ausdruecklich absichert („ES
      WIRD NIE GEWORFEN") und die dieses Projekt an mpv-protokoll.ts schon
      einmal bezahlt hat („50% off.mp3").

      WARUM „NICHTS" HIER SCHLIMMER IST ALS 500: `spielerBefehl` in
      NewDesign/app.js ruft `fetch` OHNE Frist. Ein Tipp, dessen Antwort nie
      kommt, laesst die Oberflaeche mitten in ihrer Folge stehen — in
      `weiterSpielen` wird das `finally`, das das Leuchten der Kachel
      wegnimmt, nie erreicht. Die Kachel leuchtet dann, bis jemand die Seite
      neu laedt.

      DER DIENST SELBST STIRBT NICHT: spotify-control.ts hat seit B2 ein
      `process.on('unhandledRejection')`, das nur protokolliert (gemessen:
      Fall 3 des Werkzeugs, der Dienst antwortet danach weiter). Genau
      deshalb ist die Falle so leise.

      DIE ANTWORT MUSS 500 SEIN, NICHT 200. Die Oberflaeche liest bei `stop`
      den Koerper und faellt bei einer Antwort, die nicht `ok` ist, auf die
      feste Atempause zurueck (`anhaltenUndAbstand`). Ein geschoenter Erfolg
      naehme ihr genau diese Entscheidung ab.

      NACHMESSEN:
        node tools/verteiler-wirft-messen.mjs --pruefen
        (haengt in tools/pruefen.sh als „Auffangnetz unter dem Verteiler")

  - kind: falle
    id: spotify-bibliothek-ohne-zeitschranke
    title: "spotify-web-api-node setzt NIE eine Zeitschranke — der Kern entscheidet"
    match: "spotifyApi.*haengt|Spotify.*Timeout|superagent.*timeout|player.*zwei Minuten|ETIMEDOUT.*spotify"
    sev: mittel
    cause: "src/http-manager.js der Bibliothek ruft `.timeout()` an keiner Stelle auf. superagent setzt ohne diesen Aufruf keine eigene Frist — es gilt allein, was der Kern an TCP-Wiederholungen vorsieht."
    fix: "Jeden Aufruf, dessen Antwort abgewartet wird, in eine eigene Zeitschranke fassen (src/backend-player/src/zeitschranke.ts). Messen mit tools/pause-zeitschranke-messen.mjs."
    tags: ['falle', 'spotify', 'netz', 'zeitschranke', 'bibliothek', 'messen', 'neu', 2026]
    source: "04.08.2026, node_modules/spotify-web-api-node/src/http-manager.js + tools/pause-zeitschranke-messen.mjs"
    body: |
      WER EIN VERSPRECHEN VON `spotifyApi.*` ABWARTET, WARTET MOEGLICHERWEISE
      SEHR LANGE. Die Bibliothek reicht ihre Anfragen an superagent weiter und
      setzt dabei nirgends `.timeout()`. Ohne diesen Aufruf legt superagent
      keinen Wecker; es bleibt bei dem, was der Kern tut.

      GEMESSEN AM 04.08.2026 (tools/pause-zeitschranke-messen.mjs, das den
      HttpManager DER BIBLIOTHEK mit umgebogenem Wirt benutzt — also Zeile fuer
      Zeile denselben Weg):

          Wirt antwortet sofort (hier im Haus)          5 ms
          Wirt nimmt an und SCHWEIGT                    laeuft nach 201 s noch
          Adresse geroutet, aber tot — AUF DER BOX      134,8 s

      Die 134,8 s sind die sechs Wiederholungen des Verbindungsaufbaus
      (`/proc/sys/net/ipv4/tcp_syn_retries` steht auf der Box auf 6). Ein
      bereits stehender Verbindungsstrang haelt noch laenger durch:
      `tcp_retries2` steht auf 15, das sind rund fuenfzehn Minuten.

      ZWEI SORTEN AUSFALL, DIE SICH VOELLIG VERSCHIEDEN ANFUEHLEN:
        Route weg (WLAN aus)   ENETUNREACH/EHOSTUNREACH, in Millisekunden
        Route da, Ziel tot     zwei Minuten schweigen (DSL-Ausfall)
      Nur die zweite ist gefaehrlich — und sie ist die haeufigere.

      WER DAS UEBERSIEHT, BAUT EINE BOX, DIE BEI JEDEM ANTIPPER HAENGT. Genau
      diese Frage stand vor der Reparatur an `stop()`
      ([[anhalten-wartet-auf-spotify]]) und hat die Obergrenze erzwungen.

  - kind: falle
    id: mpv-anlauf-wartet-auf-falschen-titel
    title: "Die 2000 ms im mpv-Fortsetz-Weg laden den FALSCHEN Titel"
    match: "MPV_ANLAUF_MS|tracknr.*kommt nicht an|seekpos.*wirkungslos|resumePlayback.*2000|Weiterhoeren.*faengt von vorn"
    sev: hoch
    cause: "`weiterSpielen` schickt `starte -> warte 2000 -> tracknr:N -> warte 1500 -> seekpos:P`. Warteschlange und Titelsprung stehen aber nach zehn Millisekunden; Zeit braucht nur das LADEN des gewaehlten Titels — und das faengt erst an, wenn er gewaehlt ist. Die 2000 ms laden Titel EINS."
    fix: "NOCH NICHT REPARIERT (Stand 04.08.2026). Die Richtung steht in BACKLOG E24: pruefen statt warten — `/player/state` abfragen, bis der gewaehlte Titel geladen ist, dann springen. Messen mit tools/mpv-anlauf-messen.mjs."
    tags: ['falle', 'mpv', 'weiterhoeren', 'jellyfin', 'wartezeit', 'messen', 'offen', 'neu', 2026]
    source: "04.08.2026, tools/mpv-anlauf-messen.mjs mit dem echten mpv-Aufsatz"
    body: |
      `MPV_ANLAUF_MS` STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die
      trennt: WER IRRT SICH — DIE BOX ODER DAS WERKZEUG?

          Das Kind hoert den falschen Titel, Weiterhoeren faengt von
          vorn an                 -> DIESER Eintrag
          Der Messbericht schreibt „NaN ms", die Box tut nichts
          Auffaelliges            -> [[messwerkzeug-ueberlebt-seine-messgroesse]]
                                     (die Konstante war weg, der regex
                                     traf nicht, und `undefined` rechnet
                                     in JavaScript brav weiter)

      Beide handeln von derselben Konstanten, aber nur einer davon ist
      ein Fehler AN DER BOX.

      DIE BEIDEN ZAHLEN WAREN GERATEN, und der Kommentar in app.js sagt es
      selbst: „die klassische Oberflaeche wartet an derselben Stelle 2000 ms —
      hier abgeschrieben, nicht neu gewaehlt". Jetzt sind sie gemessen.

      ── GEMESSEN (04.08.2026, 12 Titel, echter mpv-Aufsatz, `--ao=null`) ──

                                          Datei     HTTP mit 3000 ms Vorlauf
        loadlist -> playlist-count steht    7 ms      5 ms   (gewartet: 2000)
        set playlist-pos-1 kommt an        10 ms     11 ms   (gewartet: 1500)
        set percent-pos kommt an          639 ms   3734 ms
        ── zusammen                       656 ms   3750 ms   (abgesessen: 3500)

      ── DER BEFUND ────────────────────────────────────────────────────────
      Die 2000 ms vor `tracknr:` kaufen NICHTS. Warteschlange und Titelsprung
      stehen nach zehn Millisekunden. Was Zeit braucht, ist das LADEN des
      gewaehlten Titels — und das faengt erst an, wenn er gewaehlt IST. Die
      2000 ms werden also damit verbracht, Titel EINS zu laden, den niemand
      hoeren will; fuer den richtigen bleiben dann 1500 ms.

      ── DIE GEGENPROBE MACHT DARAUS EINEN FEHLER ─────────────────────────
      Stur nach der Uhr geschickt, wie `weiterSpielen` es tut, landet der
      Sprung bei einer 3000-ms-Quelle NICHT: Titel 7 steht, die Stelle ist
      0 %. Bei einer lokalen Datei kommt er an. Genau das gemeldete Bild —
      „das Stueck wird angezeigt, aber es faengt von vorn an".

      ── WARUM DER NETZ-FALL DER WICHTIGE IST ─────────────────────────────
      Auf der Box .169 liegt KEINE einzige lokale Mediendatei (nachgesehen am
      04.08.2026: `/media` ist leer, `data.json` fuehrt 22 Spotify-,
      2 Jellyfin- und 1 ARD-Eintrag). Was dort ueber mpv laeuft, kommt IMMER
      ueber das Netz. Der Datei-Lauf ist die freundliche Haelfte der Wahrheit.

      ── WAS DER MESSWERT NICHT SAGT ──────────────────────────────────────
      Gemessen wurde auf einem Arbeitsrechner, nicht auf dem Pi. Die Zahlen
      sind UNTERGRENZEN. Sie reichen trotzdem fuer den Befund: die Schritte,
      auf die gewartet wird, brauchen zehn Millisekunden — das wird auf einem
      Pi nicht zu 2000.

      NACHMESSEN:
        npx tsx tools/mpv-anlauf-messen.mjs --netz 3000
    related: [messwerkzeug-ueberlebt-seine-messgroesse]

  - kind: howto
    id: anhalten-und-anlauf-messen
    title: "Die drei neuen Messwerkzeuge zu E24 — Zeitschranke, Dienst, mpv-Anlauf"
    match: "pause-zeitschranke-messen|anhalten-am-dienst|mpv-anlauf-messen"
    tags: ['werkzeug', 'messen', 'abspieler', 'spotify', 'mpv', 'neu', 2026]
    source: "04.08.2026"
    body: |
      tools/pause-zeitschranke-messen.mjs
        Wie lange laeuft `spotifyApi.pause()` bei totem Netz? Benutzt den
        HttpManager DER BIBLIOTHEK mit umgebogenem Wirt — derselbe Weg wie
        `pause()`, nur ohne Spotify-Konto. Vier Faelle: erreichbar, stumm,
        fort, und derselbe stumme Fall mit Zeitschranke.
          node tools/pause-zeitschranke-messen.mjs --geduld 12000
          node tools/pause-zeitschranke-messen.mjs --box 192.168.178.169
        Der Box-Lauf ist NUR LESEND: ein `PUT /v1/me/player/pause` OHNE
        Schluessel, das Spotify mit 401 abweist, bevor es etwas anhaelt.

      tools/anhalten-am-dienst.mjs
        Startet den WIRKLICHEN Abspieldienst (spotify-control.ts mit tsx) und
        spricht ihn ueber HTTP an. Ersetzt werden nur drei Module: die
        Spotify-Bibliothek (Attrappe mit einstellbarer Pausendauer), die
        Wiedergabe-Maschine (damit kein mplayer startet) und die beiden
        leeren, root gehoerenden Konfigdateien.
          node tools/anhalten-am-dienst.mjs --pruefen
        ZWEI FALLEN, die es beim Bauen gekostet hat und die fuer JEDES solche
        Werkzeug gelten:
          * Der Verteiler prueft `command.name.includes('spotify:')` auf dem
            UNDEKODIERTEN `req.url`. Ein kodiertes `%3A` trifft ihn NICHT.
          * Ueber `npx` gestartet und `npx` beendet: der Enkel lebt weiter und
            haelt den Port. Die folgenden Durchgaenge scheitern still an
            EADDRINUSE, geantwortet hat weiter der ERSTE. Deshalb tsx direkt,
            `detached`, `kill(-pid)` — und abwarten, bis der Port frei ist.

      tools/mpv-anlauf-messen.mjs
        Wie lange braucht mpv, bis die Warteschlange steht, der Titelsprung
        ankommt und in den Titel gesprungen werden kann? Mit dem ECHTEN
        mpv-Aufsatz und ohne Ton (`--ao=null`).
          npx tsx tools/mpv-anlauf-messen.mjs --netz 3000
          npx tsx tools/mpv-anlauf-messen.mjs --box 192.168.178.169
        Der Box-Lauf schickt sein Messkript ueber die STANDARDEINGABE
        (`node -`) und hinterlaesst dort keine Datei. Auf .169 findet er
        mangels lokaler Mediendateien nichts zu messen und sagt das.

  - kind: howto
    id: weiterhoeren-am-geraet-messen
    title: "tools/weiterhoeren-am-geraet.mjs — den Weiterhoeren-Tipp an der Box nachfahren, mit Ton"
    match: "weiterhoeren-am-geraet|Weiterhoeren messen|Ton laeuft aber Anzeige"
    tags: ['werkzeug', 'weiterhoeren', 'messen', 'box', 'ton', 'neu', 2026]
    source: "03.08.2026"
    body: |
      WOZU: Den Tipp auf eine Weiterhoeren-Kachel an der BOX nachfahren und
      dabei mitschreiben, was die Oberflaeche in genau diesem Augenblick
      ANZEIGEN wuerde. Die Regeln (`jetztLaeuft`, `fortschritt`,
      `spieltAnzeige`) sind aus NewDesign/app.js nachgebaut - app.js ist eine
      Browser-Datei ohne Ausfuhr. Wer sie dort aendert, aendert sie hier mit.

      ── DREI SACHEN, OHNE DIE DIE MESSUNG NICHTS TAUGT ────────────────────
      1. DER TON. `--ton` misst ueber SSH den Spitzenpegel am Mithoerausgang
         der Standardsenke (wie tools/mupi-check.py `pegel`, mindestens 3 s -
         parec puffert 64 kB, bevor es das erste Byte schreibt). OHNE ihn ist
         „es spielt, zeigt aber nichts" von „es spielt gar nicht" NICHT zu
         unterscheiden. Eine offene PipeWire-Quelle beweist dabei GAR NICHTS
         ([[dienste-umschalten]]).
      2. MEHRFACH ABLESEN, im 250-ms-Takt. Der Fehler steckt ZWISCHEN zwei
         Ablesungen. Eine einzelne zeigt ihn nicht.
      3. `--vorlauf 20`. Aus einer RUHENDEN Box heraus ist der Fall gar nicht
         zu stellen - dort ist das `stop` folgenlos. Erst wenn wirklich etwas
         LAEUFT, entsteht der Wettlauf. Genau daran ist der erste Anlauf
         vorbeigelaufen: drei fehlerfreie Laeufe, und der Fehler war trotzdem
         da.

      ── WAS ES AN DER BOX AENDERT ─────────────────────────────────────────
      Es startet und haelt an, mehr nicht, und am Ende steht IMMER ein `stop`
      (finally). Es schickt bewusst KEIN `POST /api/weiterhoeren` - der echte
      Tipp taete das (`stelleMerken`), fuer die Anzeigefrage ist es ohne
      Belang, und eine verstellte resume.json waere ein Schaden am Geraet.
      ACHTUNG: die Box selbst schreibt beim `stop` ihre Resume-Stelle fort;
      die Zeilen von /api/weiterhoeren aendern sich also zwischen zwei Laeufen.

      ── DIE DREI WERKZEUGE UND IHRE ARBEITSTEILUNG ────────────────────────
        weiterhoeren-am-geraet.mjs   EINE Zeitleiste, ausfuehrlich. Zum Sehen.
        weiterhoeren-wettlauf.mjs    N Runden je Variante, nur Trefferquoten.
                                     Einen Wettlauf zaehlt man nicht an einem
                                     Lauf ab.
        weiterhoeren-atempause.mjs   Gegen die ATTRAPPE, in Sekunden statt
                                     Minuten. Prueft nicht die Wirkung
                                     (die entsteht bei Spotify), sondern den
                                     AUSLOESER: den Abstand zwischen `stop`
                                     und dem Startbefehl. Braucht das
                                     Befehlsprotokoll der Vorschau
                                     (/vorschau/befehle, seit 03.08.2026).

      ── EINE LUEGE DER ATTRAPPE, DIE DABEI AUFFIEL ────────────────────────
      tools/neu-vorschau.mjs meldete unter der Lage `spotify` in /player/local
      `currentPlayer: 'mplayer'`. Am Geraet steht dort `"spotify"`. Es fiel nie
      auf, weil `jetztLaeuft` das Feld in dieser Lage gar nicht LIEST (es faellt
      ueber `playing: false` in den Spotify-Zweig). Seit die Reparatur an genau
      diesem Feld entscheidet, wird es gelesen - und die Attrappe waere der
      Grund gewesen, warum die Reparatur „nicht wirkt". Wieder
      [[attrappe-luegt-durch-weglassen]], diesmal nicht durch Weglassen,
      sondern durch einen falschen WERT in einem Feld, das niemand brauchte.

  - kind: falle
    id: php-admin-knopf-sperren-nicht-ausblenden
    title: "Den Fork-loeschenden Knopf sperren: im HANDLER, und das ZIP nicht vergessen"
    sev: high
    tags: [admin, php, update, fork, auslieferung, datenverlust]
    body: |
      Fortsetzung von [[mupi-update-knopf-loescht-den-fork]]: dort steht, WARUM
      der Knopf gefaehrlich ist. Hier steht, was beim Entschaerfen schiefgeht.
      Umgesetzt am 03.08.2026, im Container gemessen.

      ── DIE ENTSCHEIDUNG: SPERREN, NICHT UMLEITEN ───────────────────────
      Drei Wege standen zur Wahl — Knopf entfernen, auf die Herkunftspruefung
      der neuen Verwaltung umleiten, oder sperren.
      UMLEITEN GEHT NICHT, und der Grund ist leicht zu uebersehen: die neue
      Verwaltung hat BEWUSST keinen Anwenden-Knopf (aktualisierung.ts beurteilt
      nur). Es gibt also gar kein Ziel, auf das man umleiten koennte. Wer es
      trotzdem baut, baut den gefaehrlichen Knopf ein zweites Mal — nur an einer
      Stelle, wo man ihn nicht mehr vermutet.

      ── FALLE 1: DAS HTML SPERREN REICHT NICHT ──────────────────────────
      Der naheliegende Griff ist der `<input type="submit">`. Er ist falsch.
      Der alte PHP-Admin hat von der Rueckschleife aus KEINE Anmeldung
      (BACKLOG G8: torBauen laesst 127.0.0.1 durch). Ein handgebautes
        POST admin.php  mupibox_update=1
      trifft den Handler unabhaengig davon, ob ein Knopf gezeichnet wurde.
      Die Sperre gehoert in den POST-ZWEIG. Das HTML sagt nur noch, warum.
      Und der Zweig bleibt STEHEN (statt geloescht zu werden), damit ein
      solcher POST eine Antwort bekommt, statt still durchzufallen.

      ── FALLE 2: AUSGELIEFERT WIRD DAS ZIP, NICHT www/ ──────────────────
      Das ist die teure. `autosetup/autosetup.sh` entpackt
      `AdminInterface/release/www.zip` nach /var/www/ — NICHT das Verzeichnis
      `AdminInterface/www/`. Wer die .php-Dateien aendert und das Zip vergisst,
      hat auf einer frischen SD-Karte GAR NICHTS geaendert. Die Quelle sieht
      sauber aus, `git diff` zeigt die Sperre, die Box bekommt den alten Knopf.
      Gemessen: www/ und Zip waren an dem Tag bereits auseinander
      (spotify.php lag im Zip drei Monate zurueck, seit Commit f297656b).
      Zusatzfalle in AdminInterface/zip.sh: ohne `rm` schreibt `zip -r` IN ein
      bestehendes Archiv — geloeschte Dateien bleiben darin stehen.

      ── FALLE 3: DIE SPERRMELDUNG WAR UNSICHTBAR ────────────────────────
      Erster Anlauf: exec entfernt, $CHANGE_TXT gesetzt, fertig — und die Seite
      antwortete wortlos. `includes/footer.php` zeigt $CHANGE_TXT nur
      `if( $change )`. Ein Knopf, der schweigend nichts tut, sieht aus wie ein
      kaputter Knopf, und der Naechste "repariert" ihn.
      Der einzige wahre Wert OHNE Nebenwirkung ist `$change=4`:
        1, 2 -> schreiben mupiboxconfig.json
        3    -> setting_update.sh + set_hostname.sh + restart_kiosk.sh
        99   -> footer.php haengt ein zweites "</ul></div>" an
        4    -> kein Zweig. Genau das ist der Zweck.

      ── NEBENBEFUND, DER GROESSER IST ALS ER KLINGT ─────────────────────
      DER ALTE ADMIN WAR AUF EINER BOX OHNE INTERNET TOT. index.php, admin.php
      und tweaks.php holten beim Rendern
      `https://raw.githubusercontent.com/splitti/MuPiBox/main/version.json`.
      Ohne Netz gibt json_decode NULL, und `count(null)` ist unter PHP 8 ein
      FATALER TypeError. Gemessen gegen git HEAD, Container ohne Netz:
      index.php, admin.php und service.php lieferten "Fatal error".
      Bei service.php war es nicht einmal das Netz, sondern Zeile 91:
        $rc = $output[count($output)-1];
      $output wird nur in den POST-Zweigen gesetzt; beim blossen Aufrufen ist
      es undefiniert. $rc wurde nie gelesen — eine tote Zeile, die die Seite
      umbrachte, auf JEDER Box mit PHP 8.
      Lehre: ein Netzaufruf beim Rendern ist nicht nur langsam, er ist eine
      Absturzstelle. Und `count()` auf etwas, das nur manchmal existiert, ist
      unter PHP 8 kein Warnhinweis mehr, sondern das Ende der Seite.

      ── WAS BEWUSST BLIEB ───────────────────────────────────────────────
      smb.conf, proftpd.conf und der rtl88x2bu-Treiber (service.php,
      network.php) holen ebenfalls fremden Code mit erhoehten Rechten — aber
      sie loeschen den Fork nicht, und die neue Verwaltung hat keinen Ersatz
      dafuer. Sperren haette echte Funktionen ohne Nachfolger weggenommen.
      Sie sind kommentiert und stehen in der festen Duldungsliste des
      Werkzeugs; eine NEUE Fundstelle faellt dadurch auf.

      ── WER G7 ZU ENDE BRINGEN WILL ─────────────────────────────────────
      Den Entpack-Schritt aus autosetup.sh zu loeschen reicht NICHT. Zwei
      Symlinks haengen an /var/www:
        ln -sf /var/www/images/mupif.png -> …/Sonos-Kids-Controller-master/www/mupi.png
        ln -sf …/media/cover            -> /var/www/cover
      Das erste holt die BOX-OBERFLAECHE ihr Maskottchen aus dem Admin-Ordner.
      Leeres /var/www heisst also kaputtes Bild in der Box, nicht nur im Admin.
      Erst umhaengen, dann entfernen.

      ── WIEDERHOLBAR ────────────────────────────────────────────────────
        python3 tools/php-admin-fremdbezug-schau.py     # statisch, prueft auch das Zip
        python3 tools/php-admin-fremdbezug-schau.py --zip-neu
        tools/php-admin-knopf-probe.sh                 # drueckt die Knoepfe wirklich

      Die Probe laeuft in php:8.3-cli mit `--network none` und www als :ro.
      Das ist die Vorkehrung, auf die man sich verlassen kann — denn
      harness/admin-entrypoint.sh mappt `sudo` auf `env`, damit die Seiten
      rendern. In diesem Sandkasten WUERDE ein `sudo bash` also wirklich
      laufen. Ohne Netz kann es nur nichts holen.
    source:
      - "Session 2026-08-03, im Container gemessen (php:8.3-cli, --network none), Box .169 lesend gegengeprueft"

  - kind: knowhow
    id: neue-oberflaeche-spricht
    title: "Die neue Oberflaeche spricht — und der erste Satz kostet 5 Sekunden"
    tags: [vorlesen, tts, piper, neue-oberflaeche, kinder, messen, 2026]
    body: |
      DER BEFUND (03.08.2026): `NewDesign/app.js` enthielt KEINEN einzigen
      Sprech-Aufruf — kein /api/vorlesen, kein speechSynthesis, kein Piper. Die
      klassische Oberflaeche spricht dagegen (piper 17x, /api/vorlesen 5x in
      vorlese.service.ts und utils.ts). Wer das Vorlesen eingeschaltet hatte
      und auf die neue Oberflaeche wechselte, bekam STILLE.

      WARUM DAS NIEMANDEM AUFFIEL: Sprache ist der einzige Ausgang dieser Box,
      der KEIN PIXEL BEWEGT. Jede Pruefung war gruen, jedes Bildschirmfoto sah
      richtig aus. Man findet so etwas nur, indem man ausdruecklich danach
      sucht — oder indem ein Kind vor der Box steht und nichts hoert.

      NICHT VERWECHSELN: `aria-label` gab es 16x. Das ist fuer
      VORLESEPROGRAMME (Screenreader), nicht fuer die Stimme der Box. Zwei
      Zuhoererschaften, zwei Texte:
        aria-label    „Die drei ???, Spotify"  — der Dienst gehoert hinein
        data-sprich   „Die drei ??? von Europa, abspielen"  — fuer ein Kind
                      ist „Spotify" Rauschen, „abspielen" dagegen die
                      Auskunft, auf die es wartet
      Der Benutzer hatte ausdruecklich gewuenscht, dass die Stimme sagt, was
      der Tipp TUT — nicht nur, wie die Kachel heisst.

      ── DIE MESSUNG, DIE ALLES ENTSCHEIDET (Box .169, 04.08.2026) ────────
      `tools/vorlesen-am-geraet.mjs`, eingestellt war `de_DE-thorsten-high`
      bei Tempo 2:

          ein neuer Satz, Piper kalt ....... 9654 ms
          ein neuer Satz, Piper warm ....... 5134 ms   (zweite Messung 5582)
          derselbe Satz nochmal ............   62 ms   (Zwischenspeicher)

      DER WARME DIENST HILFT BEI DIESER STIMME KAUM. Das widerspricht
      [mupi-vorlesen-schnell] NICHT, es ergaenzt es: dort ging es um das Laden
      des MODELLS (14,4 s -> 0,3 s mit ramona-low). Bei einer `-high`-Stimme
      steckt die Zeit in der SYNTHESE selbst, und die faellt bei jedem neuen
      Satz erneut an. Tempo 2 verdoppelt sie noch einmal (doppelt so viel Ton).

      FOLGE: 5,1 s sind mehr als der Zaun von 4 s (`WARTE_MAX_MS` in
      vorlese.service.ts, `VL_WARTE_MAX_MS` in NewDesign/app.js). Beim ERSTEN
      Tipp auf eine Kachel laeuft die Musik los, BEVOR der Satz fertig ist.
      Ab dem zweiten Mal kommt er aus dem Zwischenspeicher und ist sofort da.

      GEGENMITTEL, eingebaut: `warmlaufen()` produziert die Saetze der
      SICHTBAREN Kacheln vorab (fetch, kein Audio — es soll nichts toenen).
      Drei Zaeune, und ohne sie waere es gefaehrlich:
        1. NUR IM LEERLAUF. Piper rechnet auf diesem Pi rund eine Sekunde je
           Sekunde Sprache. Neben einem laufenden Hoerspiel waere das eine
           Bequemlichkeit gegen das Stocken der Wiedergabe eingetauscht. Der
           Zaun gilt auch MITTEN in der Schleife.
        2. NICHT, SOLANGE DER SPIELZUSTAND UNBEKANNT IST. `sichtbarerTakt`
           arbeitet sofort einmal; beim Seitenstart ist `/player/local` aber
           noch unterwegs, und `jetztLaeuft(null, null)` meldet dann
           faelschlich „nichts laeuft" — ausgerechnet beim Kiosk-Start mit
           laufendem Hoerspiel.
        3. Nur was auf dem Schirm steht, und jeder Satz nur einmal.

      DAS IST EINE UMGEHUNG, KEINE LOESUNG. Sie hilft nur, solange die Box
      vorher einmal Ruhe hatte. Die Loesung waere eine `-low`-Stimme (rund
      siebenmal schneller) — wie die Box KLINGT, gehoert aber dem Benutzer und
      wird nicht nebenbei entschieden.

      ── WAS UEBERNOMMEN WURDE, NICHT NEU ERFUNDEN ───────────────────────
        Schalter    config/vorlesen.json ueber GET /api/vorlesen. DERSELBE,
                    den die klassische Oberflaeche liest und die Verwaltung
                    schreibt. Ein zweiter Schalter in darstellung.json waere
                    [darstellung-feld-faellt-still-heraus] gewesen.
        Daempfung   POST /api/ton/daempfen an/aus. Die Regel, WAS gedaempft
                    wird, steht in daempfen.ts und ist dort geprueft.
        Lautstaerke aus `laut.wert` — die neue Oberflaeche holt `/player/local`
                    ohnehin im Sekundentakt. Die klassische holt ihn bei JEDER
                    Ansage neu (utils.ts `boxLautstaerke`); das ist eine
                    zweite Anfrage fuer dieselbe Antwort.
        Zaun        4000 ms, dieselbe Zahl wie WARTE_MAX_MS.
        Modi        aus / antippen / lernen, wie in [vorlesen-tts].

      ── DIE ENTSCHEIDUNG, DIE MAN SONST SPAETER AUSGRAEBT ───────────────
      KEIN ZUHOERER AM DOKUMENT, sondern ein UMSCHLAG je Knopf (`sprichDann`).
      Ein einzelner Zuhoerer in der Fangphase waere weniger Code — er muesste
      den Tipp aber schlucken und nach dem Sprechen WIEDERHOLEN. Bis dahin
      koennen 4 s vergangen sein, und der 15-s-Takt baut das Raster womoeglich
      neu: die Kachel, an die der wiederholte Tipp ginge, gibt es dann nicht
      mehr, und der Tipp waere STILL verloren. Der Umschlag haelt statt des
      Elements die FUNKTION fest, und die ueberlebt jedes Neuzeichnen.

      Damit ist auch „nicht bei jedem Takt reden" keine Zusage, sondern
      Bauart: gesprochen wird ausschliesslich in `sprichDann` und
      `warmlaufen`. Die Seite malt viermal je Sekunde — haenge das Sprechen am
      Zeichnen, redete die Box ununterbrochen.

      ── WIEDERHOLBAR ────────────────────────────────────────────────────
        node tools/vorlesen-am-geraet.mjs        # kalt/warm/Speicher an der Box
        node tools/neu-sprechen-schau.mjs        # tippt an, liest ab, was gesagt wuerde
        curl localhost:8299/vorschau/vorlesen-{aus|antippen|lernen|ohne-piper}
        curl localhost:8299/vorschau/gesprochen  # samt Daempfungsprotokoll
      Beides haengt in tools/pruefen.sh (das zweite im Regellauf, das erste
      unter --box).
    source:
      - "Session 2026-08-04, an Box .169 gemessen und in NewDesign/app.js eingebaut"

  - kind: falle
    id: attrappe-schalter-faellt-in-den-auffangzweig
    title: "Ein neuer Schalter der Attrappe ohne `else` — und der Spielzustand hiess ploetzlich anders"
    tags: [attrappe, vorschau, werkzeuge, falle, neue-oberflaeche, 2026]
    body: |
      ERLEBT AM 04.08.2026 beim Einbau des Sprechwegs. In
      `tools/neu-vorschau.mjs` endet die lange Schalterkette unter
      `/vorschau/...` mit einem AUFFANGZWEIG: alles, was keine Regel trifft,
      gilt als SPIELZUSTAND (`else lage.spielt = was`).

      Der neue Schalter wurde davor als EIGENES `if` eingehaengt:

          if (was.startsWith('vorlesen-')) lage.vorlesen = …
          if (was === 'ohne-cover') …
          else if …
          else lage.spielt = was            // <- und hier landete er AUCH

      FOLGE: `curl /vorschau/vorlesen-antippen` setzte den Modus RICHTIG und
      danach `lage.spielt = 'vorlesen-antippen'`. Die Attrappe meldete
      fortan „nichts laeuft", und die Gegenprobe „waehrend der Wiedergabe wird
      nicht vorproduziert" schlug fehl.

      DAS TEUERE DARAN IST NICHT DER FEHLER, SONDERN WO MAN IHN SUCHT: Der
      Befund sah aus wie „die Oberflaeche haelt ihren eigenen Zaun nicht ein".
      Gesucht wurde also im frisch geschriebenen Code — mit voller
      Ueberzeugung, denn der war ja neu. Gefunden wurde es erst, als eine
      Spur die ENTSCHEIDUNG mitschickte (`laeuft=false`, obwohl die Box eben
      noch `playing: true` gemeldet hatte).

      ZWEI LEHREN:
        * EIN SCHALTER GEHOERT IN DIE KETTE, nicht davor. In derselben Datei
          steht diese Warnung schon einmal („`spotify-weiter` MUSS VOR dem
          Auffangzweig stehen") — sie ist nicht neu, sie ist wieder
          zugeschnappt. Ein Auffangzweig, der ALLES nimmt, macht jedes
          vergessene `else` zu einem stillen Nebeneffekt.
        * WER EINEN ZAUN PRUEFT, MISST DIE ENTSCHEIDUNG, NICHT NUR IHR
          ERGEBNIS. „Es wurde vorproduziert" sagt nicht, WARUM. Eine Spur mit
          den Eingangswerten (`modus`, `lokal`, `dienst`, `laeuft`) beantwortet
          in einem Lauf, was sonst eine Stunde Raterei kostet.

      NEBENBEI, UND ES HAT DAS GLEICHE NOCHMAL GEKOSTET: Eine Vorschau, die
      von einem FRUEHEREN Lauf noch lief, wurde nicht neu gestartet (das ist
      Absicht, siehe [laufende-vorschau-teilt-zustand]) — sie trug aber den
      Code von VOR der Reparatur. Die Pruefung blieb rot, obwohl die Datei
      richtig war. Wer eine Attrappe aendert, muss sie auch beenden.
    source:
      - "Session 2026-08-04, beim Einbau des Vorlesens gefunden"

  - kind: falle
    id: ansage-eingebaut-weg-darunter-geaendert
    title: "Eine Ansage eingebaut — und dabei still den Weg darunter geaendert"
    tags: [vorlesen, neue-oberflaeche, falle, gegenlesen, 2026]
    body: |
      04.08.2026, beim Einbau des Vorlesens in die neue Oberflaeche. Der
      Umschlag `sprichDann(kachel, satz, name, tat)` sollte NUR eine Ansage
      davorlegen. Beim Schreiben der Saetze entstand aber ein neuer Zweig:

          if (w.fehlt) sprichDann(…, () => spielen(w, knopf))     // NEU
          else if (istPlaylist) sprichDann(…, () => albenOeffnen(w, knopf))
          else sprichDann(…, () => spielen(w, knopf))

      Vorher gab es den ersten Zweig NICHT. Eine Playlist, die nicht mehr da
      ist, klappte trotzdem auf; seither rief sie `spielen()` und zeigte nur
      noch eine Meldung. Das ist eine Verhaltensaenderung, die niemand
      beschlossen hatte — sie fiel beim Schreiben des Satzes nebenbei an,
      weil der Satz („ist nicht mehr da") eine Fallunterscheidung NAHELEGTE.

      GEMERKT WURDE ES ZWEI WERKZEUGE WEITER: `weiter-auswahl-schau.mjs`
      meldete „die Lane ging nicht auf — Vorschau geaendert?". Die Kachel sah
      dabei unveraendert aus, und der Verdacht fiel zuerst auf die Attrappe.

      REGEL: WER EINE ANSAGE EINBAUT, FASST DEN WEG DARUNTER NICHT AN. Der
      Satz ist eine EIGENSCHAFT der Kachel, kein Grund, ihr Ziel zu aendern.
      In diesem Fall stand die Regel sogar schon im Code — im Kommentar der
      Schaerpe: „DIE KACHEL BLEIBT ANTIPPBAR. Sie soll sich anfuehlen wie
      vorher."

      NEBENBEI, UND ES GILT FUER JEDE ANSAGE: DER HINWEIS GEHOERT VOR DAS VERB.
        „Pummeleinhorn, nicht mehr da, öffnen"     ein Satz
        „Pummeleinhorn, öffnen, ist nicht mehr da" ein Anhaengsel
      Wer zuhoert statt zu lesen, nimmt das Letzte oft nicht mehr auf — beim
      Lesen kann man zurueckspringen, beim Hoeren nicht.
    source:
      - "Session 2026-08-04, im Gesamtlauf gefunden"

  - kind: falle
    id: messwerkzeug-laesst-zustand-stehen
    title: "Ein Messwerkzeug stellt die Vorschau um — und der naechste Lauf erbt sie"
    tags: [werkzeuge, vorschau, attrappe, falle, tests, 2026]
    body: |
      `tools/neu-vorschau.mjs` wird von den Messwerkzeugen ABSICHTLICH nicht
      neu gestartet, wenn sie schon laeuft ([laufende-vorschau-teilt-zustand]).
      Die Kehrseite kam am 04.08.2026 heraus: `neu-sprechen-schau.mjs` schaltet
      dabei `/vorschau/vorlesen-antippen` — und liess es stehen.

      FOLGE IM GESAMTLAUF: Zwei Schritte spaeter tippte
      `weiter-auswahl-schau.mjs` eine Playlist an und sah 1200 ms spaeter nach
      der Lane. Die Seite sprach aber erst, und die Lane war noch zu. Der
      Fehler stand damit in einer Datei, die in dieser Sitzung niemand
      angefasst hatte — die teuerste Sorte.

      REGEL: WAS EIN WERKZEUG AN DER GETEILTEN VORSCHAU UMSTELLT, STELLT ES
      ZURUECK — und zwar im `finally`, nicht am Ende des Erfolgswegs. Sonst
      erbt der naechste Lauf einen Zustand, den er nicht gewaehlt hat, und
      niemand sieht den Zusammenhang.

      DIE ZWEITE HAELFTE DERSELBEN LEHRE: Wer eine Attrappe REPARIERT, muss
      die laufende auch BEENDEN. Nach der Korrektur blieb die Pruefung rot,
      weil die Vorschau aus dem vorigen Lauf noch den alten Code trug.

      UND EINE, DIE ZWEI LAEUFE GEKOSTET HAT — `pkill -f` TRIFFT SICH SELBST.
      `pkill -9 -f neu-vorschau` beendet auch die SHELL, die diesen Befehl
      ausfuehrt: deren Kommandozeile enthaelt die Zeichenkette ebenfalls. Das
      Ergebnis ist ein Lauf ohne jede Ausgabe und mit Rueckgabewert 1 — es
      sieht aus wie ein abgestuerztes Werkzeug. Verlaesslich ist:

          ps -eo pid,args | awk '$2=="node" && $3 ~ /neu-vorschau\.mjs$/ {print $1}' \
            | xargs -r kill -9
    source:
      - "Session 2026-08-04, beim Gegenlesen des Gesamtlaufs"

  - kind: knowhow
    id: google-tts-abgeloest
    title: "Google TTS abgeloest: ein Sprechweg statt zwei — und warum der alte nie lief"
    tags: [vorlesen, tts, piper, google, abspieldienst, 2026]
    body: |
      AUFTRAG (04.08.2026): Piper ist laengst da, Google TTS lebte DANEBEN
      weiter. Zwei Sprechwege im Haus, und niemand wusste, wozu der zweite gut
      war. Die Antwort nach dem Messen: er war zu gar nichts gut — er lief
      naemlich nicht.

      ── WAS AM GERAET HERAUSKAM (Box .169, tools/sprechwege-am-geraet.sh) ──

      Der Abspieldienst (`spotify-control.ts`) holte Ansagen ueber
      `google-tts-api` und legte sie als MP3 unter
      `/home/dietpi/MuPiBox/tts_files` ab. Daran war DREIERLEI kaputt, und
      jedes einzelne haette gereicht:

      1. DAS VERZEICHNIS GAB ES NICHT. `autosetup.sh:269` legt es an, diese
         Box hatte es nicht (mehr). Im Protokoll woertlich:
             Error: ENOENT ... open '/home/dietpi/MuPiBox/tts_files/Probe Google.mp3'
         Also: Ton bei Google geholt, Netz belastet — und dann weggeworfen.
         Der Fehler landete in `.catch(console.error)`; am Bildschirm sah man
         nichts.

      2. DIE GANZE TONDATEI STAND IM SYSTEMPROTOKOLL. `console.log({ base64 })`
         schrieb jede Ansage vollstaendig als Base64 in den Journal — beim
         Messen rund 5 kB Rauschen fuer zwei gesprochene Woerter.

      3. **DER ZWEIG FEUERTE NUR MIT ANHAENGSEL — die eigentliche Lehre.**
         Geprueft wurde `command.dir.includes('say/')`. Aber:
             path.parse('/current/say/Hallo').dir     -> '/current/say'   FALSE
             path.parse('/current/say/Hallo/40').dir  -> '/current/say/Hallo'  TRUE
         `path.parse` schneidet den letzten Teil ab, der Schraegstrich am Ende
         FEHLT. Der Zweig wurde nur erreicht, weil die Oberflaeche die
         Lautstaerke anhaengt — `config.tts.volume` aus `GET /api/sonos`, auf
         dieser Box "40". Wer dort nichts stehen hat, dessen Box blieb still,
         ohne eine Zeile im Protokoll.

         REGEL DARAUS: In diesem Dienst wird das VERB geprueft
         (`verb === 'say'`), nicht mit `dir.includes(...)` gesucht. Die
         anderen Zweige machen es laengst so, und aus demselben Grund.

      ── DIE FALLE, DIE DAS ERST SCHARF MACHTE ───────────────────────────

      **Ein Sprechbefehl faellt in die STEUERKETTE durch.** Am Geraet belegt
      (tools/say-befehl-durchfall.sh):

          vorher   recording = False
          GET /current/say/recordon
          nachher  recording = True     <- der TEXT wurde als BEFEHL ausgefuehrt

      `path.parse('/current/say/recordon').name` ist `recordon` — der Text.
      Weiter unten im Handler schaltet eine lange else-if-Kette auf genau
      dieses `command.name`: `stop`, `pause`, `next`, `reboot`, `shutoff`,
      `recordon`, `setvolume:`, `seekpos:` … Eine Kachel, die „stop" heisst,
      haette also ihren Namen gesagt UND die Wiedergabe angehalten; „reboot"
      haette die Box neu gestartet.

      DASS ES NICHT AUFFIEL, lag ausgerechnet am Fehler oben: der Zweig
      feuerte nur mit angehaengter Lautstaerke, und dann ist `command.name`
      die Zahl „40" und trifft nichts. **Wer den einen Fehler behebt, macht
      den anderen scharf.** Der Sprechzweig antwortet deshalb jetzt selbst und
      kehrt zurueck (`return`), statt durchzufallen.

      LEHRE ALLGEMEIN: In diesem Handler ist `command.name` NICHT „der Befehl",
      sondern „das letzte Wegstueck". Wer einen Zweig baut, dessen letztes
      Wegstueck BENUTZERTEXT ist, muss ihn abschliessen.

      MESSFALLE DABEI (kostete einen Lauf): der Zustand der Box steht unter
      `/local`, NICHT unter `/state`. `/state` liefert eine Spotify-foermige
      Antwort ohne `recording` — ein fehlender Schluessel sieht aus wie „kein
      Durchfall". Ein Nein aus dem falschen Endpunkt ist kein Nein.

      ── DIE MESSUNG, DIE DIE ENTSCHEIDUNG TRUG ──────────────────────────

          Piper ueber /api/vorlesen/sprich, kalt ....... 1,69 s
          dasselbe, warm (Zwischenspeicher) ........... 0,0016 s
          Box erreicht translate.google.com ........... HTTP 200 in 0,19 s

      Das Netz war also NICHT das Problem. Google war trotzdem der falsche
      Weg: Piper rechnet auf der Box, braucht kein Netz, schickt keine
      Kachelnamen aus dem Kinderzimmer nach draussen und hat den
      Zwischenspeicher schon.

      ── WIE ES JETZT LAEUFT: GAR KEIN HERUNTERLADEN MEHR ────────────────

      Der Abspieldienst spielt die Schnittstelle des Servers DIREKT ab:
          http://127.0.0.1:8200/api/vorlesen/sprich?text=…
      Am Geraet geprueft — mpv UND mplayer spielen diese Adresse anstandslos,
      und mpv oeffnet dafuer das echte ALSA-Geraet sauber (22050 Hz mono s16,
      „which is what we requested"). Damit entfaellt der ganze Dateikram und
      mit ihm die Fehlerklasse aus Punkt 1: ein fehlendes Ablageverzeichnis
      kann nicht mehr stumm machen, weil es keines mehr gibt. Zwischen-
      gespeichert wird trotzdem — aber EINMAL, im Server, gemeinsam mit der
      Oberflaeche.

      Der Port folgt derselben Umgebungsvariablen wie im Server
      (`MUPIBOX_HTTP_PORT`), damit eine Verschiebung nicht die Haelfte
      mitnimmt. Und es ist 127.0.0.1, nicht der Rechnername: ein Vorlesen,
      das an einem kaputten WLAN scheitert, waere genau der Fehler, den wir
      hier gerade loswerden.

      ── WAS PIPER NICHT KANN (ausdruecklich, statt verschwiegen) ────────

      Von den 21 Sprachen der alten Google-Liste deckt der Piper-Bestand 20
      ab. Es fehlt GENAU EINE: **JAPANISCH (`ja`)**. Gemessen 04.08.2026
      gegen den Stimmenkatalog (171 Stimmen, 49 Sprachfamilien),
      tools/sprachdeckung-piper-google.py.

      FALLE BEIM NACHMESSEN: `voices.json` liegt NICHT im installierten
      Piper-Paket — dort sucht man vergeblich (kostete hier zwei Anlaeufe).
      `piper.download_voices` holt den Katalog zur Laufzeit von HuggingFace.

      ── DIE DATENSCHLUESSEL BLEIBEN STEHEN ──────────────────────────────

      `mupibox.ttsLanguage` und `mupibox.googlettslanguages` stehen weiter in
      der Konfiguration JEDER Box und werden von `update/conf_update.sh`
      weiter geschrieben. Entfernt wurde nur das BOARD-FELD „Sprache beim
      Vorlesen" — ein Auswahlfeld, das man bedienen kann und das nichts
      bewirkt, ist schlimmer als keines: es behauptet eine Wirkung.

      WARUM NICHT AUF PIPER-STIMMEN UMGEBOGEN: dann gaebe es die Stimmenwahl
      an ZWEI Stellen derselben Verwaltung, in zwei Dateien
      (mupiboxconfig.json und vorlesen.json). Zwei Schalter fuer eine Sache
      laufen auseinander — dieselbe Falle wie bei `hat_active`. Die Stimme
      waehlt man unter **Verwaltung → Vorlesen**, dort mit Probeknopf. Die
      Suche fuehrt weiter hin, „Stimme"/„Sprache"/„Vorlesen" stehen in
      such-bestand.ts.

      Die Schluessel sind ab jetzt TOT, nicht falsch.
      `tools/admin-deckung-inventur.py` fuehrt sie deshalb ausdruecklich als
      bewusst inaktiv, damit sie nicht als Deckungsluecke erscheinen.

      ── NEBENGEWINN ────────────────────────────────────────────────────

      Mit `google-tts-api` faellt `axios@0.21.4` GANZ aus dem
      Abhaengigkeitsbaum — der Posten, den AUDIT-2026-08-03 als „hoch"
      fuehrte. Eine Abhaengigkeit, die man loeschen kann, ist die einzige,
      die man nie wieder aktualisieren muss.

      ── WAS OFFEN BLIEB ────────────────────────────────────────────────

      `config.tts.volume` (aus `/api/sonos`, hier "40") wird von der
      Oberflaeche an den Pfad gehaengt, wurde aber NOCH NIE angewendet — der
      alte Code schnitt sie ab und warf sie weg. Was „40" bedeuten soll
      (Anteil der Musiklautstaerke? absoluter mpv-Wert?), steht nirgends. Sie
      wird deshalb weiterhin verworfen; die Ansage laeuft auf `volumeStart`
      wie jeder andere Weg durch diese Maschine. Das ist eine Frage an den
      Benutzer, keine Vermutung fuer den Code.

      Ebenso offen: `scripts/mupibox/setting_update.sh` kopiert `ttsLanguage`
      weiter nach `spotifycontroller.json`, wo es niemand mehr liest. Harmlos,
      aber sinnlose Arbeit bei jedem Lauf.

      ── DIE WICHTIGSTE OFFENE STELLE: PIPER STEHT IN KEINEM INSTALLER ───

      Beim Gegenlesen gefunden, nicht im Auftrag: **`autosetup.sh` installiert
      Piper NICHT.** Er wurde auf der Entwicklungsbox von Hand eingerichtet
      (siehe [vorlesen-tts], Abschnitt EINRICHTUNG). Auf einer frisch
      aufgesetzten Box gibt es also weder venv noch Stimmen — und damit nach
      dieser Abloesung gar keinen Sprechweg mehr im Abspieldienst.

      Solange das so ist, MUSS der Ausfall wenigstens sichtbar sein. Der
      Abspieldienst fragt deshalb `GET /api/vorlesen` und meldet im Klartext,
      warum er still bleibt. Drei Punkte, die man dabei falsch machen kann:

        * `bereit` allein GENUEGT NICHT. Es meldet nur, dass das PROGRAMM da
          ist. Ohne eine einzige `.onnx`-Datei kann es trotzdem nicht
          sprechen — die Box waere still, waehrend das Protokoll „bereit"
          behauptet. Geprueft wird deshalb `bereit && stimmen > 0`.
        * ANTWORTET DER SERVER GAR NICHT, ist das etwas ANDERES als „keine
          Stimme". Er koennte neu starten. Dann wird es trotzdem versucht;
          ein Nein merkt sich der Dienst nur, wenn er eines bekommen hat.
        * Die Frage laeuft nach einer Minute ab, damit eine nachgeladene
          Stimme greift, ohne dass jemand den Dienst neu startet.

      ZU TUN (Vorschlag, gehoert dem Benutzer): Piper in `autosetup.sh`
      aufnehmen — `apt install python3-venv`, venv anlegen, `piper-tts`
      installieren, EINE Stimme mitliefern (`de_DE-ramona-low`, 20 MB, die
      schnellste der deutschen). Ohne diesen Schritt kann eine frische Box
      nicht sprechen, und das faellt erst dem Kind auf.
    source:
      - "Session 2026-08-04, am Geraet gemessen (Box .169), tools/sprechwege-am-geraet.sh und tools/sprachdeckung-piper-google.py"

  - kind: falle
    id: interpolation-faellt-aus-dem-suchbestand
    title: "Die Suche kennt 109 Beschriftungen der Verwaltung nicht — jede von ihnen entsteht erst zur Laufzeit"
    sev: med
    match: "Suche findet Knopf nicht|Systemaktion nicht gefunden|Box neu starten Suche|Interpolation Suchbestand|a.titel Suche|Oberflaeche neu laden Suche"
    cause: "tools/verwaltung-suchbestand.mjs erhebt den SICHTBAREN Text aus den Vorlagen und wirft jedes {{ ... }} weg. Steht in einem Knopf NUR eine Interpolation, kennt die Suche diesen Knopf gar nicht"
    fix: "Erst ZAEHLEN (tools/vorlagen-interpolationen.mjs), dann unterscheiden: kommt der Text von aussen, holt ihn die Suche selbst; steckt er als Zeichenkette im Ausdruck, gehoert er in eine feste Beschriftung daneben"
    tags: ['verwaltung', 'admin', 'suche', 'angular', 'werkzeug', 'falle', 2026]
    source: "04.08.2026, Auftrag „die Suche findet die Systemaktionen nicht\"; gezaehlt mit tools/vorlagen-interpolationen.mjs, am Geraet nachgemessen an Box .169"
    body: |
      DER GEMELDETE FALL war klein: fuenf Knoepfe fehlten in der Suche („Box
      neu starten", „Box ausschalten", „Bildschirm-Drehung zuruecknehmen",
      „Oberflaeche neu laden", „Medien neu einlesen"). Der Wert dieses
      Eintrags liegt in dem, was beim Nachzaehlen herauskam.

      ── DIE ZAHL, BEVOR MAN DIE ZWEITE STELLE EINZELN NACHRUESTET ────────
      In den 17 Seiten der Verwaltung stehen 377 Interpolationen. 109 davon
      sitzen in einer Beschriftung, die danach KEINEN festen Text mehr traegt
      — die Suche kennt diese 109 Knoepfe also nicht. Aber erst die Teilung
      darunter macht daraus eine Arbeitsliste, und sie ist die eigentliche
      Lehre:

          56  VON AUSSEN   {{ a.titel }}, {{ g.name }} — der Text steht gar
                           nicht in der Vorlage. NUR HIER hilft ein zweiter
                           Weg (die Suche holt die Liste selbst).
          53  IM TEXT      {{ laeuft() ? 'Läuft…' : 'Suchen' }} — der feste
                           Name STECKT im Ausdruck. Kein zweiter Weg noetig;
                           meistens traegt ohnehin eine Ueberschrift daneben
                           denselben Namen, und der Eintrag waere ein zweiter
                           fuer dieselbe Sache.
          27  gekuerzt     fester Text UND Wert („Lautstärke {{ w() }} %").
                           Der feste Teil steht im Bestand. In Ordnung.

      UND DIE GRENZE, die kein Muster zieht: von den 56 gehoeren laengst
      nicht alle in die Suche. {{ album.titel }} und {{ geraet.name }} sind
      DATEN der Box; sie wechseln stuendlich, und ein Suchbestand aus ihnen
      waere schon beim Bauen falsch. Hinein gehoeren nur die FESTEN TABELLEN
      des Backends — dieselbe Sorte wie FELDER und AKTIONEN.

      ── WAS DAVON BEHOBEN IST (04.08.2026) ──────────────────────────────
      AKTIONEN (src/backend-api/src/system.ts, ueber GET /api/system): die
      Suche holt sie jetzt selbst — `aktionenHolen()` in
      src/frontend-admin/src/app/suche.ts, genau wie `konfigHolen()` fuer
      /api/konfiguration ([[verwaltung-suche-bestand-aus-den-seiten]]).

      ── WAS OFFEN IST, mit Namen, damit niemand neu sucht ────────────────
        * DIENSTE     src/backend-api/src/dienste.ts        Systemdienste
        * SCHRAUBEN   src/backend-api/src/server.ts:2550    System
        * die AUSWAHLEINTRAEGE der Konfigurationsfelder (`auswahl` an FELDER;
          die Suche liest heute nur `titel` und `hinweis`)
        * MODI        seiten/vorlesen.ts — eine feste Liste im FRONTEND, die
          die Erhebung trotzdem nicht sieht: sie steht im TypeScript des
          Bauteils, nicht in seiner Vorlage.

      ── WARUM NICHT EINFACH DIE TITEL INS FRONTEND SCHREIBEN ─────────────
      Dann gaebe es sie zweimal. Beim ersten Umbenennen im Backend laufen sie
      auseinander — und schlimmer: eine Box mit einem anderen Serverstand
      haette Knoepfe, die die Suche nicht kennt, UND Treffer, die es dort
      nicht gibt. Ein Treffer, der ins Leere fuehrt, ist schlimmer als keiner:
      man sucht dort weiter. Siehe [[funktion-die-nur-ihr-eigener-test-benutzt]].

  - kind: falle
    id: rueckfall-ohne-bereich-ist-der-normalfall
    title: "Der „aeltere Server\"-Zweig war nicht theoretisch — die laufende Box ging genau ihn"
    sev: med
    match: "bereich fehlt api/system|aelterer Serverstand|Rueckfall Bereichsangabe|Aktion ohne bereich"
    cause: "Frontend und Backend liegen im selben Baum und werden zusammen ausgerollt — daraus folgt NICHT, dass die Box beides schon hat. Zwischen Commit und Ausrollen ist der alte Stand der laufende"
    fix: "Den Rueckfall nicht raten, sondern am Geraet messen und nachsehen, wo der Knopf auf DIESEM Stand wirklich steht (tools/verwaltung-suche-aktionen.mjs)"
    tags: ['verwaltung', 'suche', 'box', 'messung', 'falle', 2026]
    source: "04.08.2026, Box .169, node tools/verwaltung-suche-aktionen.mjs"
    body: |
      BEIM BAU der Aktionssuche stand die uebliche Zeile da: „fehlt `bereich`
      (aelterer Server), bleibt es beim alten Ort". Ein Zweig, den man
      schreibt und nicht ernst nimmt.

      DANN WURDE GEMESSEN. Die Box .169 antwortete auf GET /api/system mit
      VIER Aktionen — und KEINE trug `bereich`:

          {"id":"medien-neu","titel":"Medien neu einlesen","einschneidend":false}

      Das Feld kam am 03.08.2026 dazu; die Box lief noch auf dem Stand davor.
      Der „Sonderfall" war also der einzige Fall, den es an diesem Abend
      ueberhaupt gab.

      ── WAS DIE MESSUNG AN DER ENTSCHEIDUNG AENDERTE ─────────────────────
      Geraten war: ohne Angabe nach „System" — dort standen frueher alle
      Aktionen. Nachgesehen ergab etwas anderes:

        * seiten/system.ts zeichnet `harte()` = alle EINSCHNEIDENDEN Aktionen
          und fragt `bereich` gar nicht ab. Fuer die stimmt „System" wirklich.
        * seiten/medien.ts zeichnet `filter(a => a.bereich === 'medien')`.
          Ohne das Feld ist die Karte leer und wird nicht gezeichnet.

      Eine SANFTE Aktion ohne `bereich` hat auf so einer Box also NIRGENDWO
      einen Knopf. Sie in die Suche zu nehmen hiesse, auf eine Seite zu
      schicken, auf der nichts steht. Sie faellt deshalb heraus — der
      Rueckfall geht nach `einschneidend`, nicht nach einer Vermutung.

      DIE LEHRE, allgemein: Ein Rueckfallzweig fuer „aeltere Boxen" ist keine
      Hoeflichkeit gegenueber der Vergangenheit. Solange nicht ausgerollt ist,
      ist er der Weg, den JEDE Box geht — und er verdient dieselbe Sorgfalt
      wie der Hauptweg. Ein Verdacht aus der Attrappe haette hier das Falsche
      gebaut; erst das Geraet hat es gesagt.

  # ══ Backlog gegen den echten Stand ═════════════════════════════════════════
  - kind: falle
    id: backlog-fertig-verdeckt-die-eigenen-reste
    title: "Ein Punkt auf FERTIG verdeckt seine eigenen Reste — die Entscheidung wurde getroffen, die Umsetzung fiel durch"
    sev: high
    match: "steht als erledigt|FERTIG aber fehlt|Feld fehlt trotz Entscheidung|Backlog stimmt nicht|G4|G6"
    cause: "In einem Punkt stecken ZWEI Dinge: die Entscheidung (V-Phase) und die Umsetzung (U-Phase). Wird der Punkt als Ganzes auf FERTIG gesetzt, weil das MEISTE gebaut ist, faellt jede einzelne beschlossene und nicht gebaute Sache heraus — und niemand sucht mehr danach, weil daneben FERTIG steht."
    fix: "Beim Abgleich die Entscheidungstabelle Zeile fuer Zeile gegen die Umsetzungstabelle halten. Was in KEINER von beiden steht (weder „kam ins Board\" noch „bewusst NICHT\"), ist durchgefallen. Und danach mit dem Werkzeug messen, nicht mit dem Dokument."
    tags: [backlog, prozess, verwaltung, konfiguration, falle, 2026]
    source: "Backlog-Abgleich 04.08.2026, python3 tools/admin-deckung-inventur.py gegen Box .169 (lesend)"
    related: [backlog-veraltet-nach-oben-nicht-nach-unten]
    body: |
      „BACKLOG STIMMT NICHT" STEHT IN ZWEI EINTRAEGEN, und sie beschreiben
      GEGENLAEUFIGE Fehler. Die eine Frage, die trennt: Welchen Status traegt
      die Zeile, um die es geht?

          FERTIG, aber die Sache fehlt am Gerät/im Code -> DIESER Eintrag
          OFFEN, aber die Sache ist längst gebaut       ->
                                [[backlog-veraltet-nach-oben-nicht-nach-unten]]

      Wer nur den anderen liest, glaubt, ein Backlog irre immer nur nach
      oben — und übersieht genau die Lücke, auf der ein FERTIG als Deckel
      liegt.

      GEFUNDEN AM 04.08.2026 beim Abgleich von BACKLOG.md gegen den echten
      Stand. E6/G4 („je Luecke entscheiden") trug FERTIG, G6 („umsetzen")
      ebenfalls. Beides sah stimmig aus — G6 hat sogar einen eigenen Abschnitt
      „Bewusst NICHT gemacht" mit Gruenden.

      ── DER FUND ────────────────────────────────────────────────────────
      Die G2-Tabelle entschied fuer die Gruppe „Klang und Bild beim
      Start/Ende" woertlich: „INS BOARD. Die vier Klang-/Bild-Schluessel sind
      harmlos und sichtbar (Kind hoert und sieht sie)".

      In der Tabelle „Was ins Board kam" stehen sie NICHT.
      Unter „Bewusst NICHT gemacht" stehen sie AUCH NICHT.

      Nachgemessen sind mupibox.startSound, shutSound, startSplash und
      shutSplash bis heute ungedeckt, und in FELDER (konfiguration.ts) kommt
      keiner von ihnen vor. Die Entscheidung wurde getroffen und ist zwischen
      zwei Tabellen desselben Abschnitts verschwunden.

      ── WARUM DAS SCHLIMMER IST ALS EIN VERGESSENER PUNKT ───────────────
      Ein vergessener Punkt faellt beim naechsten Lesen auf. Ein Punkt, der
      FERTIG traegt, wird beim naechsten Lesen UEBERSPRUNGEN. Der Deckel liegt
      also genau auf der Luecke. Dieselbe Bauart wie
      [[darstellung-feld-faellt-still-heraus]], nur eine Etage hoeher: dort
      faellt ein Feld aus dem Stand, hier eine Entscheidung aus dem Vorhaben.

      ── DIE PROBE, DIE ES FINDET ────────────────────────────────────────
      Nicht das Dokument lesen, sondern das Werkzeug laufen lassen — und zwar
      dasselbe, mit dem die Zahl damals erhoben wurde:

          python3 tools/admin-deckung-inventur.py           # G2/G3
          python3 tools/admin-deckung-inventur.py --markdown

      Ergebnis 04.08.2026 gegen Box .169 (lesend), neben dem Stand vom 03.08.:

          gedeckt    30 -> 34
          ungedeckt  59 -> 55
          ohne Leser  8 ->  7
          ins Leere zeigende Felder  3 -> 3

      ── ZWEITER FUND DERSELBEN MESSUNG: „ERLEDIGT" IST ZWEIDEUTIG ───────
      Die drei ins Leere zeigenden Felder standen im Backlog als „erledigt".
      Richtig ist: update/conf_update.sh legt die Schluessel nach (Z. 368-389,
      nachgesehen), jedes Feld hat einen `standard` — aber DIESES UPDATE IST
      AUF DER BOX NIE GELAUFEN (Ausrollen ist untersagt). Das Werkzeug meldet
      sie deshalb weiter.

      ERLEDIGT IM AUSLIEFERUNGSWEG ist nicht ERLEDIGT AUF DER BOX. Wer das
      zusammenwirft, schickt den Naechsten auf die Suche nach einem Fehler,
      den es nicht gibt — oder haelt einen echten fuer behoben. Dieselbe
      Trennung, die [[rueckfall-ohne-bereich-ist-der-normalfall]] am selben
      Abend an der anderen Stelle gefunden hat: zwischen Commit und Ausrollen
      ist der alte Stand der laufende.

  - kind: falle
    id: e-nummer-lebt-nur-in-der-commit-nachricht
    title: "Zehn ausgelieferte E-Nummern standen in keiner Rangliste — die Wachen halten den BAUM gegen die Doku, und eine E-Nummer liegt nicht im Baum"
    sev: high
    match: "E-Nummer fehlt|BACKLOG unvollstaendig|welche Nummer ist die naechste|Rangliste ohne E-Nummern|Backlog kennt E8"
    cause: "Alle Doku-Wachen leiten ihre Sollmenge aus dem BAUM ab — Plugin-Ordner, RECHTE, Endpunkte, Ablagen, Seiten. Eine E-Nummer hat dort keinen Traeger. Sie entsteht in einer Commit-Betreffzeile und ist danach nirgends, wenn niemand sie eintraegt. Die Probe meldet gruen, weil sie die Frage gar nicht stellt."
    fix: "python3 tools/e-register-deckung.py — liest die Commit-Betreffzeilen, die MIT einer E-Nummer anfangen, und haelt sie gegen BACKLOG.md. Haengt in tools/doku-luecken-probe.sh. Findet sie gar keine E-Commits, meldet sie eine Warnung statt gruen."
    tags: [backlog, doku, prozess, werkzeug, wache, falle, 2026]
    source: "Doku-Lauf 24.08.2026, python3 tools/e-register-deckung.py gegen BACKLOG.md und git log"
    body: |
      GEMESSEN AM 24.08.2026. BACKLOG.md ist die einzige Stelle im Projekt, an
      der eine E-Nummer mit Zustand steht (OFFEN/FERTIG/TEILERLEDIGT/…);
      `tools/backlog-status-schau.py` zaehlt 232 solche Zellen daraus. Die
      Kopfzeile sagte „Stand: 2026-08-20 — E55 bis E74 am Ende dieses
      Dokuments".

      Gegen die Commits gehalten fehlten ZEHN ausgelieferte Nummern
      vollstaendig — nicht veraltet, sondern gar nicht vorhanden:

          E53  Mitschnitt in dieselbe Kategorie wie das Original   88d047ba
          E75  Spielstand ueber Soloists WebSocket                 a6262c04
          E76  Anbieter einzeln abschaltbar                        d0522272
          E80  Kern-Konfiguration reist in den Kontext             74efee2e
          E81  Jellyfin zieht ins Plugin                           88635abf
          E82  Engine-Plugins als Gesichter der Tonmaschinen       aa4da7fd
          E85  Wortsack statt Feldgleichheit                       4f2eddc5
          E86  Abgleich abstufbar (+ E86/2: kam nicht an)          415e8675
          E89  „Kachel sagt es" — Marke beim ANLEGEN               c8b86f43
          E90  gemerkte Stelle fuer Plugin-Inhalt                  700c2e02

      E77, E78, E79, E84, E87 und E88 standen da. Es ist also KEIN Ausfall der
      Gewohnheit, sondern ein Sieb: wer zufaellig einen Abschnitt schrieb, kam
      hinein, wer nur einen Commit lieferte, nicht.

      ── WARUM KEINE DER VIERZEHN WACHEN ES SAH ──────────────────────────
      `tools/doku-luecken-probe.sh` buendelt inzwischen ein gutes Dutzend
      Deckungspruefungen. Jede einzelne baut ihre Sollmenge aus dem BAUM:
      `ls plugins/`, `RECHTE` aus `plugin-vertrag.ts`, die Routen aus
      `app.routes.ts`, die Ablagen, die Beschriftungen. Das ist die Staerke
      dieser Wachen — die Sollmenge ist nicht von Hand gepflegt.

      Und es ist genau ihre Blindstelle. Eine E-Nummer hat keinen Traeger im
      Baum. Sie steht in einer Commit-Betreffzeile, und die liest keine der
      Wachen. Eine Menge, die aus dem Baum kommt, kann nichts ueber Dinge
      sagen, die nicht im Baum liegen — auch dann nicht, wenn sie vollzaehlig
      gruen meldet.

      ── DIE STUFE UEBER [[backlog-fertig-verdeckt-die-eigenen-reste]] ───
      Der Abgleich vom 04.08. fand neun Punkte, die AELTER waren als der Code,
      und zog daraus die Lehre: „in diesem Projekt veraltet ein Backlog nicht
      durch Nachlaessigkeit, sondern durch Tempo."

      Dieser Fund ist dieselbe Ursache, eine Stufe schaerfer: der Punkt ist
      nicht veraltet, er EXISTIERT NICHT. Und das ist der leisere Fehler. Ein
      veralteter Punkt faellt beim naechsten Lesen auf, weil er etwas
      Falsches behauptet. Ein fehlender behauptet gar nichts — er sieht aus
      wie ein Dokument, in dem alles steht.

      ── DIE REGEL, DIE DARAUS FOLGT ─────────────────────────────────────
      Wo eine Zaehlung ueber etwas laufen soll, das NUR in der Versionsgeschichte
      lebt (E-Nummern, Wiki-Fassungen, Messdaten in Commit-Nachrichten), muss
      die Wache `git log` lesen. Ein Baum-Scan kann es grundsaetzlich nicht.

      ── DIE PROBE ──────────────────────────────────────────────────────
          python3 tools/e-register-deckung.py     # 0 = kein Rueckstand
          bash tools/doku-luecken-probe.sh        # haengt darin

      Gegenprobe gefahren auf einer KOPIE im Kratzbrett, NICHT am Original:
      BACKLOG.md war zu dem Zeitpunkt nicht committet, und eine Sabotage, die
      man mit `git checkout` zuruecknimmt, wirft dann die eigene Arbeit mit
      weg. E90 aus der Kopie genommen -> rot mit Namen und Commit. Und die Wache meldet eine WARNUNG
      statt gruen, wenn sie gar keine E-Betreffzeile mehr findet — sonst
      ueberlebt sie ein umbenanntes Nachrichtenformat
      ([[gegenprobe-statt-gruen-glauben]]).

  - kind: falle
    id: inventur-zahl-waechst-zurueck
    title: "„Topf A erledigt\" war nie ein Zustand — die Inventur waechst mit jeder neuen Datei zurueck"
    sev: medium
    match: "Umbenennung|Topf A|umbenennung-inventur|Bestandsaufnahme stimmt nicht mehr|Zahl gewachsen"
    cause: "Eine Inventur ueber einen WACHSENDEN Baum misst einen Zeitpunkt, keinen Zustand. Wird ihr Ergebnis als „erledigt\" ins Backlog geschrieben, gilt es genau bis zur naechsten neuen Datei — und niemand zaehlt nach, weil es ja dasteht."
    fix: "Die Zaehlung an tools/pruefen.sh haengen und die Zahl im Lauf ausgeben — nicht als Fehler (Wachstum ist erlaubt), sondern als Wert, den man sieht. Ein „erledigt\" im Dokument braucht dann das Datum seiner Messung daneben."
    tags: [umbenennung, inventur, werkzeug, backlog, prozess, falle, 2026]
    source: "python3 tools/umbenennung-inventur.py, 02.08.2026 gegen 04.08.2026, derselbe Baum"
    body: |
      GEMESSEN, ZWEI TAGE AUSEINANDER, MIT DEMSELBEN WERKZEUG:

          Topf            02.08.   04.08.   Aenderung
          A sichtbar          28       30        +2
          B intern          1220     1660      +440
          C unantastbar     1450     1470       +20
          zusammen          2706     3160      +454

      Im Backlog stand „Topf A: erledigt". Das war am 02.08. wahr. Zwei Tage
      neue Oberflaeche, neue Verwaltungsseiten und neue Werkzeuge haben +454
      Zeilen mitgebracht, davon zwei sichtbare.

      ── WAS DARAN NICHT DIE SCHULD DER ARBEIT IST ───────────────────────
      Niemand hat etwas zurueckgenommen. Topf A ist deshalb gewachsen, weil
      NEUE Dateien entstanden sind, die den Namen tragen — und das ist beim
      Bauen richtig so. Falsch ist nur der Satz im Dokument: „erledigt"
      beschreibt einen ZUSTAND, gemessen wurde ein ZEITPUNKT.

      DIE 30 SICHTBAREN ZEILEN VON HEUTE, damit man sie nicht sucht:
        NewDesign/index.html                       12
        src/frontend-admin/src/app/rahmen.ts        9
        src/frontend-box/.../settings.page.ts       4
        src/frontend-admin/src/app/app.routes.ts    3
        src/frontend-admin/src/index.html           1
        src/frontend-admin/.../seiten/anmeldung.ts  1

      ── DIE ALLGEMEINE FORM ─────────────────────────────────────────────
      Jede Inventur ueber diesen Baum hat dieselbe Eigenschaft — die
      Deckungsluecke des Boards (admin-deckung-inventur.py) genauso wie diese.
      [[arbeitsweise-werkzeug-statt-wegwerfbefehl]] sagt, WARUM man ein
      Werkzeug schreibt; hier steht, was danach noch fehlt: das Werkzeug muss
      auch LAUFEN. Ein Werkzeug, das nur sein Erbauer startet, misst beim
      naechsten Mal nicht mehr die Sache, sondern den Tag seiner Entstehung
      ([[messwerkzeuge-hingen-nicht-an-pruefen-sh]]).

  - kind: architektur
    id: ard-audiothek-schnittstelle-a1
    title: "Die ARD Audiothek (ARD Sounds) — was die GraphQL-Schnittstelle wirklich hergibt"
    match: "ARD|Audiothek|ARD Sounds|ardaudiothek|GraphQL|E4|A1"
    tags: ['ard', 'audiothek', 'graphql', 'streaming', 'backlog', 'e4', 2026]
    source: "04.08.2026, tools/ard-audiothek-probe.py gegen api.ardaudiothek.de/graphql, ohne Box"
    body: |
      BACKLOG E4/A1 beantwortet. Gemessen, nicht gelesen — die Schnittstelle
      ist NICHT dokumentiert, deshalb steht sie als Werkzeug da:
      `tools/ard-audiothek-probe.py` (Unterbefehle schema, zugang, suche,
      sendung, folge, kategorien, haltbarkeit, kachel).

      ── KEIN SCHLUESSEL. Nicht die Deezer-Lage. ──────────────────────────
        POST https://api.ardaudiothek.de/graphql
        ohne jede Kopfzeile ausser Content-Type      -> 200
        mit erfundenem Bearer-Merkmal                -> 200 (wird ignoriert)
        Introspektion                                -> offen, 350 Typen
        Access-Control-Allow-Origin                  -> *
        20 Anfragen hintereinander                   -> 20x 200, keine Drosselung
      Das `*` heisst: sogar der Kiosk-Browser der Box duerfte direkt fragen.
      Ein Durchreichen ueber den Server ist trotzdem der bessere Weg (E4/A4),
      aber nicht mehr aus Zwang.

      ── DIE VIER WURZELFELDER, DIE ZAEHLEN ───────────────────────────────
        search(query, limit, offset, type: All|Items|ProgramSets|…)
        programSet(id)      eine Sendung  = die Kachel
        item(id)            eine Folge    = der Titel
        editorialCategories redaktionelle Regale; „Fuer Kinder" = 42914714
      Eine Suche gibt es, mit getrennten Toepfen: `search{ programSets{…}
      items{…} }`, beide mit `totalCount`. Ein Sendungstreffer bringt
      id, coreId, title, synopsis, numberOfElements, image{url,url1X1},
      publicationService{title} — also alles, was eine Kachel braucht, in
      EINER Runde.

      ── DIE TONADRESSE IST EINE NACKTE MP3 ───────────────────────────────
        Item.audios[] -> { url, downloadUrl, allowDownload, mimeType }
      Beispiel: wdrmedien-a.akamaihd.net/…/…_MP3-128.mp3, keine Umleitung,
      kein Merkmal in der Adresse, mimeType audio/mp3. ffprobe bestaetigt
      reines MP3, und seine Dauer (319,99 s) deckt sich mit dem Feld
      `duration` (319). Damit ist E4 wirklich der „easy case": ein neuer
      `type` + ein Verb im backend-player, das dieselbe generische
      mpv/mplayer-Strecke bedient wie Jellyfin. Kein neuer Abspieler.

      ── ABER: DIE ADRESSE HAT EINE VERWEILDAUER ──────────────────────────
        Item.audioList[] -> { href, availableFrom, availableTo, … }
      `availableTo` ist echt und wird durchgesetzt. Belegt an Folge 16657073
      („Willi, der Kater, der immer groesser wurde"): availableTo
      2026-06-29, am 2026-08-04 antwortete die Adresse mit 404 text/html —
      waehrend die Schnittstelle die Folge WEITER auflistete.

      FOLGE FUER A5: die Tonadresse gehoert NICHT in data.json. In den
      Eintrag gehoeren die Kennungen der ARD (Sendung, und bei Bedarf die
      Folge); die Adresse wird beim Abspielen frisch aufgeloest. Genau so
      haelt ein Eintrag ueber Jahre. Wer die Adresse einbackt, baut eine
      Kachel mit Verfallsdatum — und merkt es erst, wenn ein Kind drueckt.

      ── NUTZUNGSBEDINGUNGEN: ABSPIELEN JA, SPEICHERN NEIN ────────────────
      www.ardsounds.de/nutzungsbedingungen, gelesen 04.08.2026:
        * „duerfen ausschliesslich zu privaten, nichtkommerziellen Zwecken
          … genutzt werden" — eine Kinderbox im Wohnzimmer ist genau das.
        * „Die Vervielfaeltigung, Aenderung, Neuzusammenstellung,
          Wiederpublizierung, Verbreitung oder SPEICHERUNG … bedarf der
          vorherigen schriftlichen Zustimmung der produzierenden
          Landesrundfunkanstalt."
      Abspielen (streamen) ist damit gedeckt, HERUNTERLADEN ausdruecklich
      nicht — das trifft BACKLOG E10a („Download von ARD-Episoden") und
      nicht E4. Die Schnittstelle selbst markiert es pro Folge:
      `audios[].allowDownload`. Wenn E10a je gebaut wird, muss dieses Feld
      die Bedingung sein, nicht eine Annahme.
      Am Rande, ehrlich vermerkt: api.ardaudiothek.de/robots.txt sagt
      „User-agent: * / Disallow: /". Das richtet sich an Suchmaschinen-
      Sammler, nicht an einen Abspieler, der holt, was ein Mensch gerade
      angetippt hat — aber es ist ein Hinweis, dass eine BREITE, laufende
      Absammlung des Katalogs nicht erwuenscht ist. Auf Zuruf fragen: ja.
      Den Katalog spiegeln: nein.

  - kind: falle
    id: ard-items-ohne-ton-und-die-falschen-zahlen
    title: "Die Haelfte der ARD-„Folgen\" hat gar keinen Ton — und keine der drei Zahlen stimmt"
    sev: high
    match: "audios.*leer|ARD.*keine Folgen|numberOfElements|isPublished|EVENT_LIVESTREAM|Audiothek.*Dublette"
    cause: "programSet.items und items liefern OHNE Bedingung den ganzen Rohbestand: unveroeffentlichte Vorab-Dubletten (audios: []), depublizierte Folgen, dazu EVENT_LIVESTREAM und SECTION zwischen den Episoden."
    fix: "Jede items-Abfrage mit condition:{isPublished:true} stellen — OHNE itemType:EPISODE (siehe die Korrektur im Text; die erste Fassung dieses Eintrags empfahl es und lag falsch). Livestreams zusaetzlich beim LESEN aussortieren, denn search{items} nimmt gar keine condition an."
    tags: ['ard', 'audiothek', 'graphql', 'falle', 2026]
    source: "04.08.2026, tools/ard-audiothek-probe.py haltbarkeit/sendung; Korrektur am selben Tag mit tools/ard-modul-probe.ts bedingung"
    body: |
      ── KORREKTUR AM 04.08.2026, NOCH AM SELBEN TAG ─────────────────────
      Dieser Eintrag empfahl zuerst `condition:{isPublished:true,
      itemType:EPISODE}`. Das war an EINER Sendung geprueft und ist an
      anderen FALSCH. Nachgemessen ueber sieben Sendungen des Kinder-Regals
      (`npx tsx tools/ard-modul-probe.ts bedingung`):

        Sendung                mit itemType:EPISODE     nur isPublished:true
        MausZoom               168, neueste 2023-03-29  915, neueste 2026-08-03
        Herzfunk                59, neueste 2023-03-27  241, neueste 2026-07-28
        MausHoerspiel kurz     195, neueste 2026-06-13  198, neueste 2026-06-13
        Betthupferl            914, neueste 2026-08-03  915, neueste 2026-08-03

      DER GRUND steht in den Rohdaten von MausZoom: dort tragen die Eintraege
      MIT Ton `itemType: SECTION` (isPublished true, richtiger Folgentitel),
      waehrend die `EPISODE`-Eintraege die tonlosen Huellen sind. Wer nach
      EPISODE filtert, wirft bei dieser Sendung genau das Spielbare weg und
      behaelt drei Jahre alte Reste — ohne Fehlermeldung. Eine Kachel, die
      2023 stehenbleibt, sieht auf der Box aus wie ein Fehler der Box.

      RICHTIG IST: `condition:{isPublished:true}` allein, und das Aussortieren
      der Livestreams beim LESEN. Letzteres ist kein Feinschliff, sondern
      noetig: `search{ items }` nimmt die `condition` gar nicht an (HTTP 400,
      „Unknown argument \"condition\" on field \"SearchResult.items\""), und
      ein Filter, den es nur in der Abfrage gibt, fehlt genau dort, wo er am
      noetigsten ist. Beides steht in `src/backend-api/src/ard.ts`
      (`NUR_ECHTE_FOLGEN` und `folgenAus()`).

      Alles Folgende gilt unveraendert — nur eben mit der schmaleren
      Bedingung.

      DER ERSTE ANLAUF sah gut aus und war falsch. Ein Blick auf die
      neuesten Folgen einer Sendung liefert brauchbare Ergebnisse; sobald man
      tiefer greift, kippt es:

          60 neueste Folgen OHNE Bedingung      -> 40 davon mit audios: []
          dieselben 60 MIT isPublished:true     ->  0 davon ohne Ton

      Die tonlosen sind Vorab-DUBLETTEN: derselbe Beschreibungstext, aber der
      Titel ist bloss der Sendungsname („MausZoom — Kindernachrichten" statt
      „Bald nur noch weibliche Meeresschildkroeten"). Wer sie durchreicht,
      baut eine Liste, in der jede zweite Zeile gleich heisst und nichts tut.

      ── DIE ZAHLEN, DIE ALLE DREI ETWAS ANDERES MEINEN ──────────────────
                                    MausZoom   MausHoerspiel kurz
        numberOfElements                 915                  198
        items.totalCount roh            1951                  829
        items.totalCount gefiltert       168                  195

      Keine davon ist „so viele Folgen hat die Kachel". `numberOfElements`
      steht am ProgramSet und liegt bei MausZoom um Faktor 5,4 daneben. Wer
      die Zahl auf eine Kachel schreibt, zeigt eine Zahl, der man nicht
      trauen kann — dieselbe Lehre wie bei der Titelnummer im Jellyfin-Fall
      ([[mupi-jellyfin-titel-fehlt]]). Zaehlen muss man selbst, gefiltert.

      ── DER ANGENEHME NEBENEFFEKT ───────────────────────────────────────
      `isPublished:true` erledigt die Verweildauer gleich mit: die eine
      nachweislich tote Folge (16657073, 404) faellt damit von selbst aus
      der Liste. Das ist KEIN Ersatz fuer das Aufloesen beim Abspielen
      ([[ard-audiothek-schnittstelle-a1]]) — die Schnittstelle darf zwischen
      Auflisten und Antippen depublizieren —, aber es macht den Regelfall
      sauber.

      ── AUCH DABEI, WENN MAN NICHT FILTERT ──────────────────────────────
      itemType kennt EPISODE, EVENT_LIVESTREAM, SECTION, EXTRA, NOT_FOUND.
      Ein Livestream mitten in einer Hoerspiel-Liste ist fuer ein Kind kein
      Grenzfall, sondern schlicht eine Kachel, die nicht endet.

      ── UND DIE BILDADRESSE ─────────────────────────────────────────────
      image.url1X1 kommt mit einer PLATZHALTERSTELLE:
        …/images/urn:ard:image:a602…?w={width}&ch=6bf4…
      Unveraendert gespeichert -> HTTP 400 application/problem+json. Erst
      `{width}` durch eine Zahl ersetzt (w=512 -> 200 image/jpeg, 82 kB)
      ergibt ein Bild. Eine Attrappe, die den Platzhalter schon ersetzt
      mitliefert, wuerde diese Falle verschweigen
      ([[attrappe-gruen-geraet-rot-loser-titel]]).

  - kind: falle
    id: verb-als-zeichenkette-gesucht-dradio-enthaelt-radio
    title: "„dradio\" enthaelt „radio\" — warum der Abspieler nach POSITION zerlegen muss"
    sev: high
    match: "dir.includes|Verb des Abspielbefehls|dradio|ardqueue|jfqueue|split\\('radio/'\\)|zwei Zweige auf einmal"
    cause: "Der Befehlsverteiler in spotify-control.ts entschied mit `command.dir.includes('radio')` bzw. `includes('queue')`. Das sucht eine ZEICHENKETTE, wo eine STELLE gemeint ist — sobald ein Dienst FREMDE Adressen durchreicht, steht das Wort irgendwo mitten im Pfad."
    fix: "Verb und Adresse nach Position zerlegen: src/backend-player/src/befehlspfad.ts (verbAusPfad/adresseAusPfad), Zweige auf `verb === '…'` umstellen. `library`/`deletelocal` bleiben bei includes — dort ist das Wort NICHT das Verb (die Oberflaechen schicken `musicsearch/library/album/…`)."
    tags: ['abspieler', 'backend-player', 'falle', 'ard', 'befehle', 2026]
    source: "04.08.2026, tools/ard-modul-probe.ts zweige — beim Bau von BACKLOG E4/A5"
    body: |
      DIESER EINTRAG HANDELT VON EINEM TON, NICHT VON ZWEIEN. Hoert man
      nach einem Dienstwechsel den alten Titel WEITERLAUFEN, waehrend der
      neue anfaengt, ist es der andere Eintrag mit denselben Verben
      `ardqueue`/`jfqueue` im `match:`:
      [[anhaengen-startet-die-zweite-tonmaschine]]. Hier laeuft genau
      eine Tonmaschine — sie hat nur den falschen Zweig genommen.

      GEFUNDEN, WEIL DIE ARD FREMDE ADRESSEN DURCHREICHT. Alles von
      Deutschlandfunk und Deutschlandradio kommt ueber den Ausspielpartner

          podcast-mp3.dradio.de

      und „dradio" ENTHAELT „radio". Ein ARD-Befehl fuer eine Kakadu-Folge
      (das Kinderhoerspiel des Deutschlandfunks — also genau die Sorte
      Kachel, um die es bei E4 geht) zog damit ZUSAETZLICH den Radio-Zweig:

        * `currentType` stand kurz auf 'radio' — das ist keine Beschriftung,
          sondern eine Weiche: Endlosstrom, keine Dauer, kein Springen.
        * `dir.split('radio/').pop()` fand kein „radio/" und gab den GANZEN
          Pfad zurueck, der dann als Adresse abgespielt wurde.

      Zwei Ladevorgaenge fuer einen Antipper, einer davon Unsinn.

      DIESELBE FALLE, ZWEITE STELLE: `ardqueue` und `jfqueue` ENTHALTEN
      „queue". Beide zogen also auch den Zweig fuers LOKALE Anhaengen und
      schoben eine `…/media/<Titel>/title/artist/<Interpret>/playlist.m3u`
      in die Warteschlange, die es nicht gibt.

      WAS NICHT MITGEZOGEN WERDEN DARF: `library` und `deletelocal` bleiben
      bei `includes`. Die Oberflaechen schicken dort
      `musicsearch/library/album/…` (media-provider.ts, NewDesign/app.js) —
      das Verb ist `musicsearch`, „library" steht an dritter Stelle. Wer
      diese Zeilen „der Einheitlichkeit halber" mitzieht, legt die lokale
      Bibliothek still.

      DER STILLE TEIL: Der Abspieldienst quittiert einen unbekannten Befehl
      mit HTTP 200 und tut nichts — es gibt keinen Zweig, der etwas
      bemaengelt. Ein Befehl, der ZWEI Zweige zieht, meldet erst recht
      nichts. Gepruefte Gegenwehr: `tools/player-befehle.py --oberflaeche`
      liest beide Formen (`dir.includes('x')` UND `verb === 'x'`), und
      `befehlspfad.spec.ts` rechnet den alten und den neuen Weg
      nebeneinander aus, damit der Normalfall beweisbar gleich bleibt.
    related: [anhaengen-startet-die-zweite-tonmaschine]

  - kind: falle
    id: grep-zaehlt-zeilen-und-sieht-keine-schleife
    title: "Zwei Zaehlfehler, die dieselbe Ursache haben: gegrept statt aufgeloest"
    sev: mittel
    match: "Zahlen im Backlog stimmen nicht|grep -c zaehlt|template: statt .html|setProperty in einer Schleife|Untergrenze"
    cause: "Ein `grep` liest Text, keine Struktur. Es zaehlt ZEILEN statt Vorkommen, es sieht keine Vorlage, die in `template:` steht, und es findet keinen Namen, der erst in einer Schleife zusammengesetzt wird."
    fix: "Bestandslisten erheben, nicht zaehlen — und wo ein Werkzeug eine Form NICHT aufloesen kann, muss es das LAUT sagen und mit 1 enden, statt stillschweigend zu wenig zu melden."
    tags: ['werkzeug', 'falle', 'inventur', 'box', 'gegenlesen', 2026]
    source: "E23/P1, 2026-08-04 — tools/eltern-bereich-inventur.mjs und tools/mupi-variablen-abgleich.mjs"
    body: |
      ZWEI STELLEN, DERSELBE FEHLER, beide erst beim Nachmessen gefunden.

      ── 1. DIE BESTANDSLISTE DES ELTERN-BEREICHS ───────────────────────
      BACKLOG E23 nannte seine eigene Tabelle „eine UNTERGRENZE", weil
      `bluetooth`, `einstellungssperre` und `lautstaerke` keine `.html`
      haben. Richtig — aber die Untergrenze war nicht das einzige Problem:

          add: 26 Eingaben behauptet    ->  15 gemessen
                                            (die 22 ion-select-option sind
                                             Auswahleintraege, keine Felder)
          wifi: 14 Knoepfe behauptet    ->  11 gemessen
          darstellung: nicht gezaehlt   ->  17 Knoepfe, die knopfreichste Seite

      `grep -c` zaehlt ZEILEN, nicht Vorkommen. Zwei Knoepfe in einer Zeile
      sind einer; ein Knopf ueber drei Zeilen ist drei.

      UND DIE „DREI UNGEZAEHLTEN SEITEN" WAREN KEINE DREI SEITEN:
        * `bluetooth`      ist eine vollwertige Route (eingebettete Vorlage)
        * `einstellungssperre` ist KEINE Seite, sondern Waechter + PIN-Dialog
                            — das TOR, nicht ein Punkt dahinter
        * `lautstaerke`    ist gar nicht im Bereich: der Regler sitzt im
                            Player und im Mini-Player
        * DAFUER fehlte ein vierter, den niemand auf der Liste hatte:
          `anordnen.component.ts`, eingebettet in die Darstellungsseite
        * und 13 DIALOGE, die keine Seite haben. „Reboot / Shutdown" IST
          der Dialog.

      ── 2. DIE --mupi-VARIABLEN ────────────────────────────────────────
      AUDIT-2026-08-03 §5.4 zaehlte „12 im Dienst, 10 in app.css, 8 in
      beiden, --mupi-cover-f und --mupi-titel-f nur in der neuen Datei".
      Gemessen: 28 setzt der Dienst, 10 die neue Oberflaeche, 10 beide,
      18 nur der Dienst, KEINE nur die neue.

      Die zwei angeblich fehlenden setzt der Dienst sehr wohl:

          for (const k of ['fav','res','cover','titel','bezeichnung','status'])
            s.setProperty(`--mupi-${k}-f`, ...)

      Ein Muster nach `--mupi-[a-z-]+` findet dort NICHTS. Zehn Variablen
      fehlten der Zaehlung allein aus diesem Grund, zehn weitere aus einer
      zweiten Schleife fuer die freien Plaetze.

      ── DIE FALLE IM WERKZEUG SELBST ───────────────────────────────────
      Der erste Anlauf des Aufloesers warf alle Schleifen eines Rumpfes in
      einen Topf. Beide Schleifen in `anwenden()` binden ein `k` — also
      wurde `--mupi-${k}-f` AUCH mit den Werten der zweiten Schleife
      eingesetzt und erfand ein `--mupi-mp-f`, das es nirgends gibt. Das
      Werkzeug meldete eine Abweichung, die es selbst erzeugt hatte. Seither
      wird nach GELTUNGSBEREICH gebunden: es zaehlt die innerste Schleife,
      in deren Rumpf der Aufruf wirklich steht. Nachgestellt in
      `tools/mupi-variablen-abgleich.test.mjs`, nicht behauptet.

      MERKE: Ein Pruefschritt, der zu wenig findet, ist schlimmer als
      keiner — er beruhigt. Deshalb endet der Aufloeser mit 1, sobald eine
      Form uebrigbleibt, die er nicht kennt.

  - kind: falle
    id: box-eltern-bereich-verwaiste-seiten
    title: "Drei Seiten der Box, zu denen kein Weg fuehrt — zwei davon mit Zugangsdaten und ohne PIN"
    sev: hoch
    match: "jellyfin Seite nicht erreichbar|spotify Seite nicht erreichbar|favorites Seite|canActivate fehlt|verwaiste Route Box"
    cause: "Die Einstiegskacheln wurden umgebaut; die Routen blieben. `defaultHref=\"/settings\"` sieht nach Anbindung aus, ist aber nur der RUECKWEG — er beweist nicht, dass jemand hinfuehrt."
    fix: "Erreichbarkeit AUS DEM QUELLTEXT erheben (wer ruft navigate/routerLink?), nicht aus dem Rueckweg schliessen. tools/eltern-bereich-inventur.mjs --pruefen, haengt in pruefen.sh."
    tags: ['box', 'oberflaeche', 'sicherheit', 'falle', 'werkzeug', 2026]
    source: "E23/P1, 2026-08-04, am Quelltext gemessen"
    body: |
      GEMESSEN in src/frontend-box/src (2026-08-04):

          /favorites   Rueckweg -> /home       navigate hierhin: KEINER
          /jellyfin    Rueckweg -> /settings   navigate hierhin: KEINER
          /spotify     Rueckweg -> /settings   navigate hierhin: KEINER

      Alle drei sind nur ueber die Adresszeile erreichbar. Dieselbe
      Fehlerklasse wie [[unterseite-ohne-rueckweg]] in der Verwaltung — nur
      steht hier mehr dahinter.

      DAS TEURE DARAN: `/jellyfin` und `/spotify` tragen Serveradresse,
      Benutzer und Schluessel — und stehen in `app.routes.ts` OHNE
      `canActivate: [einstellungssperreGuard]`. Wer die Adresse kennt, kommt
      ohne PIN hinein. `/add` ist ebenfalls ohne Waechter, aber das ist
      BEGRUENDET und steht als Kommentar in der Route: es ist nur ueber das
      bewachte `/edit` erreichbar.

      WARUM ES NIEMAND GEMERKT HAT — und das ist der uebertragbare Teil:
      Ein `defaultHref` sieht aus wie eine Anbindung. Es ist aber nur die
      Antwort auf „was, wenn der Navigationsstapel leer ist". Wer nach
      Rueckwegen sucht, findet drei gut angebundene Seiten. Gesucht werden
      muss nach dem WEG HINEIN.

      ZWEITER BEFUND AUS DEMSELBEN LAUF: `add.page.ts` traegt ebenfalls
      `ViewEncapsulation.None`. BOX-MENUE-ANALYSE.md nennt nur `wifi`. Es
      sind ZWEI Seiten, die ihr SCSS ungekapselt und dauerhaft ins Dokument
      haengen.

  - kind: knowhow
    id: entwurf-gibt-gliederung-nicht-masse
    title: "Der Entwurf des Eltern-Bereichs haelt die 9-mm-Marke selbst nicht — an einer Stelle waere er ein Rueckschritt"
    tags: ['box', 'oberflaeche', 'entwurf', 'beruehrziele', 'e23', 2026]
    source: "E23/P4, 2026-08-04 — tools/eltern-bereich-inventur.mjs --ziele und --vorlage"
    body: |
      Der Wunsch lautete „so wie in der Vorlage". Bevor danach gebaut wird,
      gehoert gemessen, ob die Vorlage die eigene Marke des Projekts haelt.

      SIE IST VERGLEICHBAR, ohne Umrechnen: `NewDesign/MixPiBox-standalone.html`
      ist auf `width: 800px; height: 480px` gezeichnet — derselbe Schirm,
      dieselben 0,14 mm/px.

      GEMESSEN IM ENTWURF (Eltern-Bereich), 7 von 8 unter 9 mm:

          36 px = 5,04 mm   Aktionsknopf oben rechts
          38 px = 5,32 mm   Knopf in einer Zeile
          46 px = 6,44 mm   PIN-Taste UND Reiterknopf
          48 px = 6,72 mm   Zeile im Reiterfeld
          52 px = 7,28 mm   Abspielen
          62 px = 8,68 mm   PIN-Taste, Breite
         148 px            Reiterspalte (einziges Mass darueber)

      GEMESSEN IM BESTAND: 18 von 47 Groessenangaben unter der Marke, vier
      knapp darunter (64 px = 8,96 mm — das Projekt rundet die Marke selbst
      auf 64, so der Kommentar in sperre.dialog.ts).

      DER PUNKT, AN DEM DIE VORLAGE SCHLECHTER IST ALS DER BESTAND:
      Die heutige PIN-Tastatur der Box (`.sp-taste`) ist 76x76 px =
      10,64 mm — das einzige Bedienelement des Bereichs deutlich UEBER der
      Marke. Der Entwurf setzt an derselben Stelle 62x46 px an. Wer ihn 1:1
      uebernimmt, macht ausgerechnet die PIN-Eingabe kleiner.

      LEHRE: Der Entwurf gibt die GLIEDERUNG (PIN-Schirm + sieben Faecher)
      und die ANMUTUNG vor — nicht die MASSE. Jede Groesse wird beim
      Uebertragen auf >= 64 px gehoben. Sonst sieht der Umbau neu aus und
      trifft sich schlechter.

      NEBENBEI, WEIL ES DIE P2-FRAGE BEANTWORTET: Der Entwurf gliedert in
      WLAN, Bluetooth, „Ton & Zeit", „Anzeige", „Kinder", „Bibliothek",
      „System". Die Box fuellt vier davon. „Ton & Zeit" (Lautstaerke-Limit,
      Einschlaf-Timer, Nachtmodus) und „Kinder" (Profile) gibt es GAR NICHT,
      „System" nur als Shutdown-Dialog. Der Bereich ist also nicht zu voll,
      sondern an drei Stellen leer.

  # ══ E1-Grundmessung: Boot, Speicher, Zeit bis zum Bild (04.08.2026) ═══════
  - id: grundmessung-zeit-bis-zum-bild
    kind: howto
    title: "Wie lange die Box wirklich bis zum Bild braucht — 19 bis 24 s, und wo sie hingehen"
    tags: [boot, perf, messung, kiosk, e1, e5]
    source: >
      Vier Kaltstarts am Geraet, 04.08.2026, Pi 5 / DietPi Trixie,
      Kernel 6.18.34-1+rpt1 aarch64, 2 GB RAM, WLAN (kein Kabel),
      Oberflaeche "neu". Gemessen mit tools/grundmessung.py --kalt.
    body: |
      DIE ZAHL, DIE ZAEHLT: vom Einschalten bis zum brauchbaren Bild vergehen
      **19,4 / 21,9 / 23,0 / 23,8 Sekunden**. Der Schirm ist bis dahin
      SCHWARZ — kein Splash, keine Fortschrittsanzeige, nichts.

      ZUSAMMENSETZUNG (Sekunden seit Kernelstart, alle Werte auf EINER Uhr):

          ~5   s   Kernel + Dienste, bis das Backend auf :8200 hoert
                   (gemessen 4,86 / 5,03 / 5,57 — sehr stabil)
          4-9  s   WARTEN AUF DHCP UEBER WLAN — fuer den Kiosk passiert nichts
          ~1   s   getty-Autologin und chromium-autostart.sh
          ~9   s   Chromium vom Prozessstart bis zum fertigen Bild
                   (gemessen 8,37 / 8,82 / 9,34 — ebenfalls stabil)
          ─────
          19-24 s  bis zum Bild

      DIE ERWARTUNG WAR FALSCH, UND ZWAR ZUGUNSTEN DER BOX. Der Verdacht aus
      dem Backlog (E1/M3) lautete: Chromium male mit
      `--default-background-color=44afe2ff` sofort eine blaue Flaeche, man
      messe "erstes Bild" und freue sich zu frueh. Gemessen ist das Gegenteil.
      Der Schirm bleibt schwarz (Anteil nicht-dunkler Flaeche exakt 0,000) bis
      19,1 / 21,4 / 23,5 s und steht EINE Abtastung spaeter (+0,3 bis +0,4 s)
      vollstaendig da, mit ueber 11 000 verschiedenen Farben und allen Covern.
      Erstes Bild und brauchbarer Inhalt FALLEN ZUSAMMEN; der Abstand liegt
      unter der Aufloesung der Messung. Das Panel geht erst an, wenn Chromium
      sein erstes echtes Vollbild hat.

      Damit ist E1/M3 anders beantwortet als gefragt: es gibt kein "Bild, das
      nichts zeigt". Es gibt 19 bis 24 Sekunden Schwarz. Fuer E11c
      (Sichtbarkeit) ist das die eigentliche Nachricht.

      FUER E5/B1 ("booten optimieren ODER durchlaufen lassen"): der einzige
      grosse und zugleich sinnlose Posten ist das DHCP-Warten (siehe
      [kiosk-wartet-auf-dhcp]). Die uebrigen rund 15 s sind echte Arbeit —
      5 s Dienste und 9 s Chromium. Ohne Chromium-Ersatz (siehe
      optim-chromium-cog) ist da wenig zu holen.
    related: [kiosk-wartet-auf-dhcp, schirm-messen-ueber-x11vnc, speicher-waechst-erst-im-betrieb]

  - id: kiosk-wartet-auf-dhcp
    kind: signature
    title: "Der Kiosk startet erst, wenn das WLAN eine Adresse hat — 4 bis 9 s Warten auf nichts"
    sev: high
    tags: [boot, netz, dhcp, kiosk, e5]
    match: "ifup@wlan0|getty@tty1.*spaet|DHCPDISCOVER|Bootzeit schwankt|startet mal schneller"
    symptom: >
      Die Box braucht mal 19, mal 24 Sekunden bis zum Bild — ohne dass sich
      etwas geaendert haette. `systemd-analyze` meldet je nach Lauf zwischen
      6,9 s und 14,3 s Gesamtzeit.
    cause: |
      `getty@tty1.service` — und damit die GANZE Kiosk-Kette (Autologin ->
      chromium-autostart.sh -> startx -> Chromium) — wird konsequent 0,07 s
      nach `ifup@wlan0.service` aktiv. Gemessen ueber vier Kaltstarts:

          ifup@wlan0 fertig    getty@tty1 aktiv
              8,83 s               8,90 s
             11,81 s              11,90 s
             14,13 s              14,21 s

      `ifup@wlan0` dauerte 5,5 bis 10,7 s, und GENAU DIESE STREUUNG IST die
      Streuung der Bootzeit. Der Grund steht im Protokoll:

          [ 4,616] DHCPREQUEST for 192.168.178.169 on wlan0 …
          [ 6,637] DHCPDISCOVER on wlan0 … interval 8
          [ 7,052] wlan0: Associated with dc:39:6f:xx:xx:xx
          [13,895] DHCPDISCOVER on wlan0 … interval 21

      dhclient schickt seinen ersten Antrag bei 4,6 s los — wpa_supplicant
      assoziiert aber erst bei 7,1 s. Die Antwort KANN nicht kommen, also
      laeuft der Zeitablauf ab, und danach steigen die Intervalle (8, dann 21).

      DAS BITTERE DARAN: das Backend hoert seit Sekunde 5 auf :8200. Die
      Anwendung ist die ganze Zeit fertig und niemand sieht sie. Gewartet wird
      auf eine Netzadresse, die der Kiosk zum Anzeigen der lokalen Seite gar
      nicht braucht (`http://localhost:8200`).
    fix: |
      ES GIBT DAFUER SCHON ETWAS — nicht neu erfinden:

          scripts/mupibox/dhcp-schneller.sh --einbauen

      haengt `initial-interval 1; backoff-cutoff 2; retry 5;` an
      /etc/dhcp/dhclient.conf an (ANHAENGEN, nicht ersetzen — siehe
      [dhclient-conf-nicht-ersetzen]). Es wird aus BEIDEN Ausrollwegen
      gerufen, ist wiederholbar und hat einen gefahrenen Rueckweg:

          sudo /usr/local/bin/mupibox/dhcp-schneller.sh --zuruecknehmen

      Die Entscheidung samt Zahlen steht in [boot-nur-ein-hebel-dhcp], am Pi 4
      brachte dieselbe Aenderung 31,8 s -> 13,2 s ([mupi-boot-32s-auf-13s]).
      NACHMESSEN NICHT VERGESSEN — am Pi 5 ist der Gewinn noch NICHT bestaetigt
      (erwartet 3-4 s): `tools/grundmessung.py <box> --kalt`.

      DIE DREI KANDIDATEN AUS DER ERSTEN FASSUNG, und warum sie hier stehen
      bleiben — wer den Hebel oben gefahren hat und immer noch wartet, hat nur
      noch diese drei, und zwei davon sind teuer:
        * Statische IP statt DHCP. ACHTUNG: die erste Fassung dieses Eintrags
          schrieb "BACKLOG E5/B3 nennt sie schon" — DAS IST FALSCH, B3 ist
          `disable_splash`/`boot_delay` (und verworfen), und im BACKLOG steht
          eine statische IP nirgends. Sie ist ein Vorschlag ohne Vorgeschichte,
          und sie verlagert das Problem in den Router.
        * `ifup@wlan0` nicht mehr als Vorbedingung fuehren, d. h. den Kiosk aus
          der `network.target`-Ordnung loesen — er braucht nur localhost. Das
          ist der einzige Weg, der die 4-9 s GANZ streicht statt sie zu
          verkuerzen, aber er greift in die Debian-Ordnung ein.
        * dhclient erst nach der Assoziation starten.
      GEGENPROBE NICHT VERGESSEN: die Box HOLT ihre Cover ueber das Netz
      nach, wenn der Speicher kalt ist (siehe cover-nach-neustart-langsam).
      Ein Kiosk, der vor dem Netz startet, zeigt dann leere Kacheln — das
      waere genau der Fall, den E1/M3 vermeiden wollte. Der DHCP-Hebel oben hat
      dieses Problem NICHT: er holt die Adresse frueher, nicht spaeter.
    source: >
      Vier Kaltstarts am Geraet, 04.08.2026 (Pi 5 / DietPi Trixie, WLAN),
      tools/grundmessung.py --kalt. Belege: systemctl show
      ifup@wlan0/getty@tty1 -p ActiveEnterTimestampMonotonic und
      journalctl -b -o short-monotonic. Der fix-Abschnitt am 04.08.2026 beim
      Gegenlesen nachgezogen: er nannte den umgesetzten Hebel nicht und
      verwies fuer die statische IP auf eine Backlog-Nummer, die von etwas
      anderem handelt.
    related: [boot-nur-ein-hebel-dhcp, dhclient-conf-nicht-ersetzen, mupi-boot-32s-auf-13s, grundmessung-zeit-bis-zum-bild]

  - id: speicher-waechst-erst-im-betrieb
    kind: signature
    title: "Der grosse Speicherposten entsteht im Betrieb, nicht beim Booten — piper haelt 698 MB"
    sev: high
    tags: [ram, perf, piper, vorlesen, messung, e1, e5]
    match: "wenig Speicher|out of memory|Box wird langsam|swap|piper|vorlesen.*RAM"
    symptom: >
      Frisch gestartet laeuft die Box flott. Nach Stunden wird sie zaeh, es
      wird ausgelagert, und `vcgencmd get_throttled` meldet Drosselung.
    cause: |
      GEMESSEN AM GERAET (04.08.2026, Pi 5, 2 GB), PSS statt RSS:

          frisch gebootet     744 MB belegt   1280 MB verfuegbar   Swap  0 MB
          nach 9,7 Stunden   1318 MB belegt    590 MB verfuegbar   Swap 91 MB

      Der Unterschied ist fast vollstaendig EIN Prozess:

          piper (Vorlesestimme)   698 MB PSS = 34,7 % des gesamten RAM
          Chromium (11 Prozesse)  332-420 MB PSS
          backend-api              81-84 MB
          backend-player           58-68 MB
          Xorg                     51-74 MB

      Also: der Vorleseserver allein braucht MEHR als Chromium mit allen elf
      Prozessen. Er wird vom backend-api als Kind gestartet
      (`piper-venv/bin/python -m piper.http_server -m de_DE-thorsten-high.onnx`)
      und bleibt danach stehen — bei der Messung seit 1533 s ohne Last (0,0 %
      CPU). Wer nur frisch gebootet misst, sieht ihn nie.

      Nach 9,7 h stand ausserdem `get_throttled` auf 0xe0000: Takt war
      gedeckelt, es WURDE gedrosselt, weiche Temperaturgrenze erreicht. Frisch
      gebootet: 0x0. Eine Messung auf einer gedrosselten Box misst die
      Drosselung mit, ohne es zu sagen — deshalb gehoert der Wert in jeden
      Messbericht.
    fix: >
      Nicht behoben, nur gemessen (die Vorlesestimme liegt im backend-api,
      nicht in diesem Gebiet). Naheliegend: den piper-Server nach Leerlauf
      beenden und bei Bedarf neu starten — er hat bei der Messung 698 MB
      gehalten, ohne irgendetwas zu tun. Fuer E5/B1 heisst der Befund: die
      Box degradiert nicht vom Booten, sondern vom Laufen.
    source: >
      tools/grundmessung.py am Geraet, 04.08.2026: ein Lauf nach 9,7 h
      Betriebsdauer gegen drei Laeufe unmittelbar nach dem Kaltstart.
      PSS aus /proc/<pid>/smaps_rollup.

  - id: schirm-messen-ueber-x11vnc
    kind: howto
    title: "Den echten Bildschirm der Box messen — ueber den x11vnc, der ohnehin laeuft"
    tags: [messung, kiosk, vnc, framebuffer, werkzeug]
    body: |
      WOFUER: Fragen wie "wann ist das Bild da?" oder "steht da schon etwas?"
      lassen sich nur an echten Pixeln beantworten. Auf der Box ist dafuer
      NICHTS installiert — kein scrot, kein import, kein ffmpeg (geprueft,
      nicht vermutet).

      /dev/fb0 HILFT NICHT MEHR, sobald Xorg laeuft. Unter KMS ist der
      Framebuffer danach durchgehend null: 819 200 Bytes gelesen, kein
      einziges Byte ungleich 0, waehrend auf dem Panel die volle Oberflaeche
      stand. Der Weg aus [boot-animation-mit-echtem-fortschritt] traegt also
      NICHT in den laufenden Betrieb hinein.

      DER AUSWEG: chromium-autostart.sh startet bei jedem Boot
      `x11vnc -ncache 10 -forever -display :0` — ohne Passwort, auf Port 5900,
      auf allen Schnittstellen. Ein RFB-Client von rund 60 Zeilen genuegt:
      Handschlag "RFB 003.008", Sicherheitstyp 1 (None), SetPixelFormat,
      SetEncodings mit nur Raw, dann FramebufferUpdateRequest. Umgesetzt in
      `tools/grundmessung.py` (Klasse `Schirm`).

      DREI FALLEN, ALLE AM GERAET ZUGESCHNAPPT:

      1. `-ncache 10` macht den SERVERPUFFER ZEHN BILDSCHIRME HOCH. Gemeldet
         werden 800x5760 statt 800x480. Wer die gemeldete Groesse anfordert,
         zieht 18 MB Cachemuell ueber das WLAN und wertet ihn auch noch aus.
         Immer nur das obere Rechteck 0,0,800,480 anfordern.

      2. x11vnc SCHICKT CopyRect (Kodierung 1), OBWOHL der Client nur Raw
         angemeldet hat — Protokollverstoss, aber gelebte Praxis; die Quelle
         liegt im Cachebereich unterhalb Zeile 480. Die erste Fassung warf
         darauf eine Ausnahme und baute die Verbindung neu auf: 1,25 s
         verloren, und ausgerechnet in diesen 1,25 s sprang der Schirm von
         schwarz auf fertig. Die Messung hatte genau die Stelle nicht, um die
         es ging. Richtig: die vier Bytes wegwerfen und SOFORT neu anfordern.

      3. WORAN MAN DIE PHASE ERKENNT. Gemessen: schwarzer Schirm = ZWEI
         Farben (nicht eine — ein Rest aus dem Textmodus), Anteil
         nicht-dunkler Flaeche 0,000. Fertige Oberflaeche = 11 154 Farben bei
         16 bpp, Anteil 0,98. Dazwischen liegt nichts. Wer "mehr als eine
         Farbe" als Kriterium nimmt, meldet den schwarzen Schirm als Bild —
         genau so lief die erste Fassung. Es zaehlt der HELLE ANTEIL, nicht
         die blosse Zahl der Farben.

      KOSTEN: ein Vollbild 800x480 braucht 750 kB bei 16 bpp (1500 kB bei 32)
      und rund 0,5 s ueber WLAN. Fuer eine Abtastung im Sekundentakt reicht
      das; 16 bpp halbiert die Netzlast und trennt die Phasen genauso klar.

      ZEITACHSE: die Proben werden auf SEKUNDEN SEIT KERNELSTART gerechnet,
      damit sie zu /proc/<pid>/stat (Feld 22 / CLK_TCK), zu
      `systemctl show -p …TimestampMonotonic` (Mikrosekunden) und zu
      `journalctl -o short-monotonic` passen. ACHTUNG: `systemd-analyze`
      rechnet als einziges in USERSPACE-Sekunden, also OHNE die 0,77 s
      Kernelzeit — wer beides nebeneinanderlegt, sucht 0,8 s, die es gibt.
    source: >
      Am Geraet gebaut und angewandt, 03./04.08.2026 (Pi 5 / DietPi Trixie).
      Beweisbilder: schwarzer Schirm bei 23,4 s (2 Farben), volle Oberflaeche
      mit allen Covern bei 24,4 s (11 154 Farben).
    related: [grundmessung-zeit-bis-zum-bild, kiosk-live-untersuchen-devtools, im-kiosk-messen-statt-raten]

  - kind: falle
    id: piper-fehlt-im-ausrollweg
    title: "Piper lag nur auf der Entwicklungsbox — kein Ausrollweg kannte ihn"
    sev: high
    match: "vorlesen.*bereit.*false|piper.*fehlt|frische SD.*spricht nicht|/api/vorlesen.*bereit"
    cause: "autosetup.sh, update/start_mupibox_update.sh und der remote-step-installer enthielten NULL Treffer fuer 'piper'; er war am 28.07.2026 von Hand eingerichtet worden"
    fix: "scripts/mupibox/piper-einrichten.sh (venv + piper-tts + flask + Vorgabestimme), aufgerufen aus beiden Ausrollwegen; nachmessen mit tools/piper-installationsweg-abgleich.py"
    tags: ['vorlesen', 'piper', 'installation', 'autosetup', 'ausrollweg', 'falle', 2026]
    source: "04.08.2026, am Quelltext gemessen — auf der Box wurde NICHTS installiert"
    body: |
      GEFRAGT WAR (Nacht auf den 04.08.2026): Google TTS ist abgeloest, die neue
      Oberflaeche spricht ueber Piper — was passiert auf einer frischen SD-Karte?

      ANTWORT: sie spricht GAR NICHT. Und das war vorher schon so; neu ist nur,
      dass Sprechen jetzt ein BEWORBENES MERKMAL ist. Vorher fiel niemandem auf,
      dass eine Funktion fehlt, die es offiziell nicht gab.

      DIE MESSUNG, DIE ALLES ENTSCHIED — drei Wege, null Treffer:

          grep -ri piper autosetup/ config/ scripts/ update/     0
          grep -ri piper <remote-step-installer>                 0

      Piper kam ueber KEINEN Weg auf eine Box. Er lag allein auf der
      Entwicklungsbox, dort am 28.07.2026 von Hand eingerichtet
      ([[vorlesen-tts]], Abschnitt EINRICHTUNG). Zwei Boxen liefen damit, und
      niemand hat die Luecke gesehen — weil beide Boxen von Hand nachgezogen
      worden waren.

      WARUM DER FEHLER STILL IST — und das ist die eigentliche Lehre:
      `GET /api/vorlesen` meldet `bereit` als schlichtes `fs.existsSync(piperBin)`
      (server.ts). Ein FEHLENDER Piper und ein VERSCHOBENER Pfad sehen darin
      identisch aus. Der Abspieldienst fragt vorher (`bereitAus` in
      backend-player/src/sprechen.ts, es gilt Programm UND mindestens eine
      Stimme) und tut dann eben nichts. Kein Protokolleintrag, keine Meldung.

      ── VIER STELLEN, EIN PFAD ──────────────────────────────────────────

          server.ts     piperVenv   /home/dietpi/.mupibox/piper-venv
          server.ts     stimmenDir  /home/dietpi/.mupibox/piper-stimmen
          vorlesen.ts   STIMME_VORGABE = de_DE-ramona-low
          das Einrichtungsskript muss GENAU diese drei treffen

      Deshalb gibt es dafuer jetzt eine Leitplanke statt eines Kommentars:
      `tools/piper-installationsweg-abgleich.py` (laeuft in tools/pruefen.sh).

      ── DREI FALLEN IM EINRICHTUNGSSKRIPT ───────────────────────────────

      1. `python3 -m venv` scheitert ohne das Paket `python3-venv` an
         ensurepip — und die Fehlermeldung nennt die VERSIONIERTE Form
         (`python3.13-venv`), nicht das Metapaket. Wer die Zahl in ein
         Installationsskript schreibt, hat sie beim naechsten Debian falsch.
         Geprueft wird deshalb `import ensurepip`, nicht die Fehlermeldung.

      2. `piper.http_server` braucht FLASK, und `piper-tts` bringt es NICHT
         mit. Fehlt es, faellt der Server still auf den Einzelaufruf zurueck:
         2,2 s statt 0,17 s je Satz. Das meldet niemand — das ist eine Box,
         die „irgendwie langsam" ist. Und `python3-flask` aus apt hilft
         nicht: ein venv ohne `--system-site-packages` sieht Systempakete
         nicht.

      3. Der venv und die Stimmen muessen DIETPI gehoeren. Der Server laeuft
         unter pm2 als dietpi und laedt ueber `POST /api/vorlesen/stimme`
         selbst weitere Stimmen nach — er schreibt also in das Verzeichnis.

      NOCH OFFEN, ehrlich hingeschrieben: auf 32 Bit (armhf) gibt es kein
      onnxruntime-Rad, `pip install piper-tts` muesste dort uebersetzen. Ob
      das durchlaeuft, ist UNGEPRUEFT. Der Schritt bricht deshalb den
      Installationslauf nicht ab: eine Box ohne Vorlesen ist keine kaputte Box.

      NACHMESSEN:
        python3 tools/piper-installationsweg-abgleich.py
        ssh <box> "sudo /usr/local/bin/mupibox/piper-einrichten.sh --pruefen"
    related: [vorlesen-tts, google-tts-abgeloest, mupi-installation-besteht-aus-zwei-rezepten]

  - kind: falle
    id: update-ueberschreibt-librespot
    title: "Der Update-Weg haette eine spielende Box stummgeschaltet"
    sev: high
    match: "librespot.*dev_0\\.6|kein Ton nach Update|Spotify.*not available|librespot -V"
    cause: "autosetup.sh UND update/start_mupibox_update.sh holten beide dev_0.6_20250806 per wget — eine Fassung ohne Ton — obwohl bin/librespot/0.8.0/ im Repo liegt"
    fix: "0.8.0 aus dem Paket bevorzugen; im Update-Weg ein bereits vorhandenes >= 0.8.0 gar nicht anfassen (librespot -V pruefen)"
    tags: ['librespot', 'spotify', 'update', 'ausrollweg', 'falle', 2026]
    source: "04.08.2026, am Quelltext gemessen (tools/librespot-ausrollweg-abgleich.py)"
    body: |
      BEKANNT WAR: librespot muss >= 0.8.0 sein, sonst kommt kein Ton (A/B am
      Geraet, 28.07.2026, scripts/librespot/README.md). Bekannt war auch, dass
      autosetup die stumme `dev_0.6` liefert — das steht als E12/X1 im Backlog.

      NEU UND SCHLIMMER: `update/start_mupibox_update.sh` tat DASSELBE. Auf
      beiden laufenden Boxen liegt 0.8.0, hingebracht vom remote-step-installer.
      Ein Update haette es mit `wget -O /usr/bin/librespot` ueberschrieben — eine
      Box, die vorher spielte, waere danach still gewesen.

      DAS IST DIE TEUERSTE RICHTUNG DIESER FEHLERKLASSE. Eine frische Karte, die
      etwas nicht kann, ist ein Mangel. Ein Update, das etwas WEGNIMMT, sieht aus
      wie eine Verschlechterung durch die Pflege — und niemand sucht die Ursache
      im Update, sondern in Spotify.

      DIE REGEL DARAUS: ein Update darf einen Bestandteil nur dann ersetzen,
      wenn es einen BESSEREN mitbringt. Wenn nicht, ist Nichtstun die richtige
      Handlung, und sie muss ausdruecklich im Code stehen:

          elif /usr/bin/librespot -V | grep -qE 'librespot 0\.(8|9|[1-9][0-9])'
              -> nichts tun, Meldung ins Protokoll

      ── DIE ZWEITE HAELFTE: DER WAECHTER ────────────────────────────────

      `librespot-waechter.service`, `.timer` und `librespot-waechter.sh` lagen
      seit langem im Repo — und KEIN Ausrollweg hat je eines davon angefasst.
      Eine Unit ohne ihr Skript ist schlimmer als gar keine: sie sieht
      eingerichtet aus. Was sie faengt, faellt sonst niemandem auf — librespots
      Sitzung kann sterben, waehrend der Prozess weiterlaeuft; die Box
      verschwindet aus Spotifys Geraeteliste, `Restart=always` greift nicht,
      und systemd meldet einen gesunden Dienst.

      EINE FALLE BEIM EINSCHALTEN, die einen Anlauf gekostet haette: die
      Dienstschleife in autosetup.sh haengt `.service` an jeden Namen. Ein
      Timer in dieser Liste wuerde als `librespot-waechter.timer.service`
      gesucht — und still nicht gefunden. Er braucht eine eigene Zeile.

      WEITER OFFEN: fuer armhf gibt es hier kein 0.8.0-Binaer, und
      librespot-org veroeffentlicht keine fertigen Dateien. Dieser Zweig bleibt
      stumm; er sagt es jetzt wenigstens im Installationsprotokoll.

      NACHMESSEN:
        python3 tools/librespot-ausrollweg-abgleich.py
        ssh <box> "librespot -V; systemctl is-active librespot-waechter.timer"
    related: [mqtt-name-fehlt-dienst-still-tot, piper-fehlt-im-ausrollweg]

  - kind: knowhow
    id: mqtt-reparatur-jetzt-im-repo
    title: "MQTT: die Reparatur vom 30.07. steht endlich auch im Repo"
    tags: ['mqtt', 'dienste', 'systemd', 'ausrollweg', 2026]
    source: "04.08.2026, Commit im Fork; am Quelltext geprueft, nicht am Geraet"
    body: |
      SCHLIESST [[mqtt-name-fehlt-dienst-still-tot]] und den Repo-Teil von
      [[mupi-mqtt-reparatur]]. Beide sagten dasselbe: repariert war nur auf den
      Geraeten und im remote-step-installer, eine frische SD brachte den Fehler
      zurueck.

      WAS JETZT IM REPO STEHT

          config/templates/mupiboxconfig.json   mqtt.name = "MuPiBox"
          update/conf_update.sh                 legt mqtt.name nach
          config/services/mupi_mqtt.service     ExecCondition auf mqtt.active
                                                Restart=on-failure (war on-abort)

      WARUM "MuPiBox" UND NICHT DER RECHNERNAME. Der Installer nahm
      `socket.gethostname()` — richtig fuer eine bereits eingerichtete Box.
      Beim AUSROLLEN ist der Rechnername aber noch nicht gesetzt: DietPi heisst
      frisch „DietPi", und das waere als Geraetename in der Hausautomatik
      schlechter als „MuPiBox". Eindeutig wird eine Box ohnehin ueber
      `clientId` (daraus baut mqtt.py die unique_id), und den muss der Benutzer
      je Box setzen. Vorlage und conf_update.sh sagen jetzt DASSELBE — sonst
      verhielte sich eine frische Box anders als eine gewachsene.

      EINE VERBESSERUNG GEGENUEBER DER GERAETE-REPARATUR, und sie ist keine
      Kosmetik: die ExecCondition prueft ueber `str(...)`, nicht ueber den
      nackten Wahrheitswert. In dieser Konfiguration stehen Schalter mal als
      `false`, mal als `"false"` — `jq --arg` schreibt IMMER eine Zeichenkette
      (dieselbe Wurzel wie `maxVolume: "100"`). Ein `if m.get('active')` waere
      bei der Zeichenkette `"false"` WAHR gewesen; der Schalter haette genau
      anders herum gewirkt als angeschrieben.

          str(True)   -> 'true'    startet
          str(False)  -> 'false'   uebersprungen
          str("true") -> 'true'    startet
          str("false")-> 'false'   uebersprungen
          fehlt/None  -> 'none'    uebersprungen
          Datei weg   -> Ausnahme, Code 1, uebersprungen (sichere Richtung)

      DAS WERKZEUG MELDET JETZT AUCH DEN HALBEN STAND.
      `tools/mqtt-schluessel-abgleich.py` prueft zusaetzlich die ExecCondition
      und geht mit 1 hinaus, wenn der Schluessel da ist und die Bedingung
      fehlt. Genau dieser Zwischenstand ist die Falle: er tauscht einen toten
      Dienst gegen einen, der sinnlos gegen den Beispiel-Broker laeuft. Der
      Schritt laeuft in tools/pruefen.sh mit.

      UNVERAENDERT OFFEN: `mqtt.active` und `mqtt.debug` werden von mqtt.py
      weiterhin gelesen und nirgends ausgewertet. Abgeschaltet wird MQTT allein
      dadurch, dass der Dienst nicht startet — und genau das leistet die
      ExecCondition. Und `check_server_availability` gibt weiterhin NIE False
      zurueck: ein unerreichbarer Broker haelt den Dienst in „active (running)",
      ohne dass er je etwas tut.
    related: [mqtt-name-fehlt-dienst-still-tot, mupi-mqtt-reparatur, piper-fehlt-im-ausrollweg]

  # ══ E5-Entscheidung: was am Booten gemacht wird — und was nicht (04.08.2026) ══
  - id: boot-nur-ein-hebel-dhcp
    kind: optim
    title: "E5/B1 entschieden: EIN Hebel am Booten (DHCP-Rueckfall) — alle anderen verworfen"
    tags: [boot, perf, dhcp, wlan, e5, entscheidung, kiosk]
    body: |
      DIE FRAGE aus BACKLOG E5/B1 lautete "booten optimieren ODER durchlaufen
      lassen". Beantwortet mit Zahlen statt mit Bauchgefuehl: OPTIMIEREN, aber
      GENAU EINE SACHE. Die uebrigen Punkte des Epics sind nach der Messung
      verworfen — und das ist das eigentliche Ergebnis.

      ── DER EINE HEBEL, gemessen ────────────────────────────────────────

      Der Kiosk haengt an der normalen Debian-Ordnung; nichts daran ist
      MuPiBox-eigen (`systemctl show -p After` an der Box nachgesehen):

          ifup@wlan0.service -> network.target -> systemd-user-sessions.service
                             -> getty@tty1 -> Autologin dietpi
                             -> chromium-autostart.sh -> Chromium

      Er zeigt aber `http://localhost:8200` und braucht keine Adresse. Aus dem
      Protokoll EINES Starts (`tools/bootkette-schau.py`, Box .169):

          [ 4,120]  DHCPDISCOVER auf wlan0, interval 8  <- ins Leere
          [ 6,595]  wlan0: Associated with dc:39:6f:xx:xx:xx
          [ 6,609]  CTRL-EVENT-CONNECTED                <- ab hier ginge alles
          [11,560]  DHCPDISCOVER, interval 14           <- erst jetzt wieder
          [11,675]  DHCPOFFER von 192.168.178.1         <- Antwort in 115 ms
          [11,792]  bound
          [11,809]  ifup@wlan0 aktiv
          [11,896]  getty@tty1 aktiv (+0,087 s)

          ZURUECKHOLBAR = 11,560 - 6,595 = 4,96 s
          Gesamtstart    = 0,77 s Kernel + 11,21 s Userspace = 11,98 s

      Also: fast die Haelfte des Userspace-Starts ist Warten auf einen Router,
      der in 115 Millisekunden antwortet. Grund: dhclients erster Antrag ging
      2,5 s VOR der Assoziation raus, danach greift die Rueckfallstaffel
      (Intervall 8, dann 14).

      BEHEBUNG — dieselben drei Zeilen wie am Pi 4 (dort 31,8 s -> 13,2 s
      gemessen, siehe [mupi-boot-32s-auf-13s]):

          initial-interval 1;   erster Nachschlag nach ~1 s statt ~4-8 s
          backoff-cutoff 2;     Intervall waechst nie ueber 2 s (Vorgabe 16)
          retry 5;              naechster Anlauf nach 5 s statt 5 MINUTEN

      Alle drei machen dhclient HARTNAECKIGER, nicht ungeduldiger. Eine Box
      ohne DHCP-Server bekommt danach auch keine Adresse — sie fragt nur
      oefter.

      Erwartung am Pi 5: 3 bis 4 s weniger (nach der Assoziation vergeht noch
      bis zu backoff-cutoff). Wichtiger als die Sekunden: DIE STREUUNG FAELLT
      WEG. systemd-analyze meldete ueber vier Kaltstarts 6,9 bis 14,3 s, und
      diese Spanne war fast vollstaendig ifup@wlan0 (5,5 bis 10,7 s).

      ── WAS VERWORFEN WURDE, UND WARUM DAS DAS ERGEBNIS IST ─────────────

      B3 `disable_splash=1` / `boot_delay=0` — VERWORFEN, 0 s Gewinn:
        * disable_splash=1 STEHT BEREITS in der config.txt der Box, und die
          cmdline hat `quiet loglevel=3 logo.nologo vt.global_cursor_default=0
          systemd.show_status=false`. Es ist nichts mehr abzuschalten.
        * boot_delay ist OHNE Eintrag schon 0. Die Vorgabe hinzuschreiben
          aendert nichts.
        * Dagegen steht: eine kaputte /boot/config.txt hat GENAU DIESE BOX
          schon einmal unbootbar gemacht ([mupi-stromausfall-bootdateien],
          [mupi-config-txt-treiber-auskommentiert]). Null Gewinn gegen diese
          Fallhoehe ist keine Abwaegung, sondern eine klare Antwort.

      B2 Dienste ausmisten — VERWORFEN, die Praemisse ist widerlegt:
        * x11vnc als "Dauerposten auf der 2-GB-Box": 28 MB PSS. Der
          Dauerposten ist piper mit 698 MB ([speicher-waechst-erst-im-betrieb]).
        * mupi_vnc, mupi_novnc, dietpi-dashboard, mupi_telegram: alle vier
          `disabled`. Sie sind nicht "immer an", sie sind aus.
        * `systemctl --failed`: 0.
        * Offen bleibt genau eins: LIBRESPOT_ONEVENT startet bei jedem
          Track-Ereignis telegram_Track_Spotify.py, obwohl mupi_telegram aus
          ist. Laufzeit, nicht Boot.

      B8 Crontab-Polls (save_rrd 15 s / get_network 30 s) — VERWORFEN: auf der
      Box laeuft KEINER davon. `crontab -l` ist fuer root UND dietpi leer,
      /etc/cron.d enthaelt nur e2scrub_all und php. Die Annahme stammte aus
      der Vorlage crontab.template, die dort nie ankam.

      B5 Kiosk — erledigt oder gegenstandslos: `--hide-scrollbars` steht seit
      31.07. im Fork-Script, und die 30-Runden-curl-Probeschleife kostet
      GEMESSEN nichts (sie prueft :8200, und das antwortet seit Sekunde 5 —
      lange bevor der Kiosk ueberhaupt startet).

      B7 log2ram streichen — es gab nie welches. Repo-weit 0 Treffer ausser im
      Backlog selbst. Uebrig blieb der zweite Teil: zram, jetzt in
      config/templates/dietpi.txt (AUTO_SETUP_SWAPFILE_LOCATION=zram).

      ── DIE LEHRE, DIE MEHR WERT IST ALS DIE SEKUNDEN ───────────────────

      Von sechs Punkten des Epics haben FUENF nach der Messung keinen Gegner
      mehr gehabt: drei Praemissen waren falsch (x11vnc, Crontab, log2ram),
      zwei Hebel standen laengst (disable_splash, --hide-scrollbars). Nur EINER
      war echt — und der war zugleich der groesste. Wer die Liste ohne Messung
      abgearbeitet haette, haette fuenfmal Risiko gegen nichts getauscht,
      darunter zweimal an /boot/config.txt.
    fix: |
      scripts/mupibox/dhcp-schneller.sh --einbauen (aus autosetup.sh gerufen).
      RUECKWEG, gefahren statt behauptet:
          sudo /usr/local/bin/mupibox/dhcp-schneller.sh --zuruecknehmen
      22 Tests in tools/dhcp-schneller.test.sh. Nachmessen:
          tools/bootkette-schau.py 192.168.178.169
          tools/grundmessung.py    192.168.178.169 --kalt
    source: >
      Session 2026-08-04, Box .169 (Pi 5 / DietPi Trixie, WLAN), nur lesend
      gemessen mit tools/bootkette-schau.py und tools/grundmessung.py.
      Am Geraet EINGEBAUT ist der Hebel noch NICHT.
    related:
      - grundmessung-zeit-bis-zum-bild
      - kiosk-wartet-auf-dhcp
      - mupi-boot-32s-auf-13s
      - speicher-waechst-erst-im-betrieb
      - zram-statt-swap-auf-der-box

  - id: dhclient-conf-nicht-ersetzen
    kind: falle
    title: "dhclient.conf durch eine Vorlage ERSETZEN nimmt der Box die DNS-Server"
    tags: [dhcp, netz, installer, vorlage, falle]
    match: "dhclient.conf|Namensaufloesung weg|kein DNS nach Update"
    body: |
      Der erste Entwurf fuer den DHCP-Hebel war eine Vorlage
      config/templates/dhclient-mupibox.conf mit drei Zeilen, die autosetup
      nach /etc/dhcp/dhclient.conf schiebt — so wie es das mit asound.conf,
      smb.conf und einem Dutzend anderer Dateien tut.

      DAS WAERE FALSCH GEWESEN. /etc/dhcp/dhclient.conf ist NICHT leer:

          option rfc3442-classless-static-routes code 121 = array of …;
          send host-name = gethostname();
          request subnet-mask, broadcast-address, time-offset, routers,
                  domain-name, domain-name-servers, domain-search, host-name,
                  … ntp-servers;

      Die `request`-Liste ist es, die den DHCP-Server ueberhaupt nach
      DNS-Servern und NTP fragt. Wer die Datei ersetzt, nimmt der Box die
      Namensaufloesung — und sucht danach ueberall, nur nicht in einer Datei,
      die "Boot schneller machen" heisst.

      RICHTIG: Sicherung anlegen, einen Block mit Marken ANHAENGEN, und beim
      Zuruecknehmen genau diesen Block wieder entfernen. So gebaut in
      scripts/mupibox/dhcp-schneller.sh.

      ZWEI FEHLER, DIE DER TEST GEFUNDEN HAT — beide beim ersten Anlauf drin:

      1. Der Block wurde mit einer LEERZEILE davor angehaengt (`echo ""`),
         huebscher zu lesen. Der Rueckweg OHNE Sicherung bekam sie nicht
         wieder weg, weil awk nur ZWISCHEN den Marken entfernt: die Datei war
         nach dem Zuruecknehmen um eine Zeile laenger als vorher. Ein
         Rueckweg, der "fast" den Ausgangszustand herstellt, ist keiner.
      2. Endet die Datei ohne Zeilenumbruch (von Hand bearbeitet), klebte die
         Anfangsmarke an der letzten Zeile und machte sie zu einem Kommentar.

      Beide Faelle stehen jetzt als Test da. Die Lehre ist nicht "awk ist
      heikel", sondern: EIN RUECKWEG WIRD GEFAHREN, NICHT BESCHRIEBEN. Beide
      Fehler waren unsichtbar, solange man nur `cmp` gegen den Zustand MIT
      Sicherung laufen liess.
    source: "Session 2026-08-04, tools/dhcp-schneller.test.sh (22 Punkte)"
    related: [boot-nur-ein-hebel-dhcp, mupi-boot-32s-auf-13s]

  - id: bootkette-aufmachen
    kind: howto
    title: "Warum der Kiosk wartet: die Bootkette lesend aufmachen"
    tags: [boot, werkzeug, systemd, messen, e5]
    body: |
      WOFUER: tools/grundmessung.py sagt WIE LANGE es bis zum Bild dauert.
      tools/bootkette-schau.py sagt WARUM — und rechnet den einen Posten aus,
      der sich zurueckholen laesst.

          tools/bootkette-schau.py 192.168.178.169
          tools/bootkette-schau.py 192.168.178.169 --json

      Es aendert nichts an der Box: `systemctl show`, `systemd-analyze` und
      `sudo journalctl -b`. Es zeigt vier Dinge:

        1. Gesamtzeit und die groessten Posten (systemd-analyze blame).
        2. Die Kette ifup@wlan0 -> network.target -> systemd-user-sessions ->
           getty@tty1 mit monotonen Marken — man SIEHT die 0,07 s Kopplung.
        3. "Warten auf NICHTS": Abstand zwischen CTRL-EVENT-CONNECTED und dem
           naechsten DHCP-Versuch. Das ist die zurueckholbare Zahl.
        4. Eine Tabelle Hebel BOX gegen REPO. "Box ja / Repo nein" ist das
           Zwei-Repos-Delta: die laufende Box kann es, eine frisch aufgesetzte
           nicht. Genau so faellt auf, was nur im remote-step-installer lebt.

      ZWEI FALLEN, beide beim Bauen zugeschnappt:

      * `journalctl -b` OHNE sudo liefert dem Benutzer dietpi NICHTS — kein
        Fehler, keine Meldung, eine LEERE Ausgabe. Wer darauf "keine
        DHCP-Zeilen -> alles gut" baut, misst die Berechtigung statt der Box.
        Deshalb wird gezaehlt, ob ueberhaupt Zeilen ankamen, und der Bericht
        sagt ausdruecklich, dass Leere kein Freispruch ist.
      * Dieselbe DHCP-Zeile steht ZWEIMAL im Protokoll, einmal unter
        `dhclient[730]` und einmal unter `ifup[730]` — derselbe Prozess, zwei
        Kennungen. Ohne Entdoppeln zaehlt man jedes Ereignis doppelt.
    source: "Session 2026-08-04, gebaut und an Box .169 angewandt"
    related: [grundmessung-zeit-bis-zum-bild, kiosk-wartet-auf-dhcp, boot-nur-ein-hebel-dhcp]

  - id: eltern-bereich-muster-ohne-ionic
    kind: howto
    title: "Der Eltern-Bereich als Zustand statt als Adresse — das Muster vom 04.08."
    tags: [oberflaeche, neu, einstellungen, sperre, e23, muster, 2026]
    body: |
      EIN MUSTER ZUM ANSEHEN, KEIN UMBAU. Gewuenscht woertlich: „ich haette
      einen moderneren Eltern-Konfigurationsbereich, so wie in der Vorlage".
      Gebaut ist EINE Seite in der rahmenwerkfreien Oberflaeche — das Tor und
      EIN gefuelltes Fach (Bluetooth). Der alte Weg bleibt unveraendert.

      WO ES LIEGT
        NewDesign/index.html   `#eltern` (Tor + Flaeche)
        NewDesign/app.js       `eltern`, `sperrModus`, `torFertig`,
                               `zifferDazu`, `btZeile`, `elternEinstieg`
        NewDesign/app.css      ab „ELTERN-BEREICH"
        tools/eltern-muster-schau.mjs   der Pruefschritt (in pruefen.sh)

      ZWEI GESTEN AUF EINEM KNOPF, und zwar in EINER Hand: Ein kurzer Tipp
      aufs Zahnrad geht wie bisher nach /settings, langes Halten (700 ms)
      oeffnet das Muster. Der erste Griff war ein zweiter Zuhoerer mit
      `capture: true`, der den alten wegfangen sollte — DAS GEHT NICHT: am
      Ziel-Element ruft der Browser Fang- und Blasen-Zuhoerer in der
      Reihenfolge auf, in der sie EINGETRAGEN wurden. Der spaeter eingetragene
      Faenger kommt also nach dem frueheren Blasen-Zuhoerer, und
      `stopImmediatePropagation` kaeme zu spaet.

      DIE SPERRE IST DAS RISIKO, NICHT DIE OPTIK. Die Angular-App bewacht mit
      einer `CanActivateFn` an fuenf Routen. Diese Oberflaeche hat KEINEN
      Router — ihre Bildschirme sind Zustand, keine Adresse, also gibt es kein
      `canActivate`. Drei Saetze, die daraus folgen und die nicht offensichtlich
      sind:

        * DREI SORTEN SPERRE, nicht zwei: „aus", „rechnen", „pin"
          (sperrlogik.ts). Wer nur die PIN baut, laesst „rechnen" still
          durchfallen.
        * UNLESBARE KONFIGURATION HEISST „aus" — das ist der VERTRAG, keine
          Bequemlichkeit. Eine Box, die sich selbst aussperrt, weil das Backend
          gerade hochfaehrt, ist nicht mehr erreichbar: man kaeme an ihr selbst
          nicht mehr ins WLAN.
        * BEIM ZUMACHEN WIRD WEGGERAEUMT, NICHT ZUGEDECKT. Gemessen am
          04.08.: nach dem Verlassen stand das Tor korrekt wieder da, die
          Bluetooth-Zeilen der vorigen Sitzung lagen aber weiter im Baum unter
          einem `hidden`. Am Bildschirm sah alles richtig aus. Ein einziger
          Stilfehler, der die Flaeche wieder aufdeckt, haette die Auskunft
          hinter der Sperre gezeigt, ohne dass jemand eine PIN eingibt.

      DER PRUEFSCHRITT MISST DIE WIRKUNG, NICHT DAS VORHANDENSEIN: nicht „gibt
      es ein Tor?", sondern „Sperre an, keine PIN — stehen die Bluetooth-Zeilen
      im Baum?". Ein Tor, das da ist und nichts zurueckhaelt, besteht jede
      Pruefung, die nur nach ihm sucht. Dazu vier Lagen (aus/pin/rechnen/
      kaputt), das Wiederzugehen beim Verlassen, der ALTE Weg, alle Masse und
      hell wie dunkel.

      DIE MASSE KOMMEN NICHT AUS DEM ENTWURF, sondern gegen ihn
      ([[entwurf-gibt-gliederung-nicht-masse]]): PIN-Taste 84x64 statt 62x46,
      Fach-Knopf 168x64 statt 46, Zeile 76 statt 48. Gemessen sind alle
      Bedienelemente >= 64 px UND sie werden getroffen (elementFromPoint).

      VIER FAECHER, NICHT SIEBEN. Der Entwurf zeichnet sieben; „Ton & Zeit"
      und „Kinder" hat die Box GAR NICHT, „Bibliothek" gehoert ans Board. Ein
      Reiter, hinter dem nichts liegt, ist eine Attrappe, die durch Weglassen
      luegt. Die drei Faecher, die es nur in der Angular-App gibt, tragen einen
      Pfeil — ein Tipp darauf ist ein voller Seitenwechsel, kein Umschalten.

      ZUM ANSEHEN OHNE BOX:
          node tools/neu-vorschau.mjs
          curl localhost:8299/vorschau/sperre-pin      # PIN ist 2468
          curl localhost:8299/vorschau/sperre-rechnen
          curl localhost:8299/vorschau/sperre-kaputt   # /api/config gibt 500
          node tools/eltern-muster-schau.mjs --bild /tmp/eltern.png
    source: "Session 2026-08-04, E23/P1-P4; gemessen gegen tools/neu-vorschau.mjs"
    related: [entwurf-gibt-gliederung-nicht-masse, box-eltern-bereich-verwaiste-seiten, mupi-einstellungen-erreichbar, rueckweg-verdeckt-die-ueberschrift]

  - id: rueckweg-verdeckt-die-ueberschrift
    kind: falle
    sev: mittel
    title: "Der Rueckweg lag auf dem Wort — und jede Zahl des Pruefschritts war gruen"
    match: "zurueck ueber Titel|Kopfzeile verdeckt|zurueck-links|eltern-kopf padding"
    cause: >
      Der eine Rueckweg steht NICHT am linken Rand, sondern bei
      `--zurueck-links` (96px, 8px unter `body.platz-machen`). Eine neue
      Kopfzeile rechnete mit „64 + 12 Luft" ab dem Rand und schob ihren Titel
      auf 92 px — der Knopf reicht bis 160.
    fix: >
      Den Einzug AUS DER VARIABLEN rechnen, nicht schaetzen:
      `padding-left: calc(var(--zurueck-links, 96px) + 60px)`. Dann stimmt er
      in beiden Lagen. Geprueft von tools/eltern-muster-schau.mjs („Kopfzeile"),
      gegen den kaputten Stand nachgestellt: 4 FEHLER, Ende 1.
    tags: [oberflaeche, neu, navigation, css, falle, werkzeug, 2026]
    source: "Session 2026-08-04, gefunden am Bildschirmfoto des Eltern-Musters"
    body: |
      DIE EIGENTLICHE LEHRE IST NICHT DIE ZAHL, SONDERN WER SIE GEFUNDEN HAT.

      Der Pruefschritt zum Eltern-Muster misst jedes Bedienelement: Groesse
      >= 64 px, wird es getroffen (elementFromPoint), ragt es ueber 800x480
      hinaus. Alle vier Durchgaenge — Tor und Bereich, hell und dunkel —
      standen auf gruen. Der Knopf stand trotzdem MITTEN AUF dem Wort
      „Eltern-Bereich", 64 von seinen Punkten verschluckt.

      WARUM ES DURCHFIEL: Gemessen wurden Bedienelemente GEGENEINANDER. Eine
      Ueberschrift ist keines. Sie kann also beliebig verdeckt werden, ohne
      dass irgendeine Trefferflaeche schrumpft — kein Knopf wird kleiner, keiner
      wird verfehlt, nichts ragt hinaus. Ein Werkzeug, das nur Knoepfe kennt,
      sieht die halbe Seite nicht.

      GEFUNDEN HAT ES DAS BILD. Das ist der Grund, warum der Auftrag „miss es
      UND mach ein Bild davon" lautet und nicht „miss es": die beiden finden
      verschiedene Sorten Fehler, und keine der beiden Sorten ist die
      seltenere.

      DIESELBE FEHLERKLASSE WIE [[rueckweg-lag-ueber-der-meldung]]: Der eine
      Rueckweg liegt mit z-index 9 ueber ALLEM und nimmt sich seinen Platz,
      ohne zu fragen. Wer eine neue vollflaechige Ebene baut, rechnet mit
      seiner LAGE — nicht mit dem Rand des Schirms.

      GEGENMITTEL, und es ist eine Zahl geworden: Der Schritt misst jetzt die
      Ueberdeckung von `.eltern-titel`/`.eltern-unter` mit `#zurueck` und
      verlangt mindestens 12 px Luft. Zwei Flaechen, die sich exakt beruehren,
      sehen aus wie ein Fehler — und die naechste Schriftgroesse macht daraus
      wieder eine Ueberdeckung.
    related: [rueckweg-lag-ueber-der-meldung, ein-rueckweg-fuer-alle-ebenen, eltern-bereich-muster-ohne-ionic]

  - id: dietpi-txt-vorlage-ist-kein-ausrollweg
    kind: falle
    title: "config/templates/dietpi.txt aendert nichts — die Vorlage kommt auf keine Karte"
    tags: [dietpi, karte, erstinstallation, swap, zram, ausrollweg, falle, e5]
    match: "dietpi.txt|AUTO_SETUP_SWAPFILE|zram fehlt|Karte hat /var/swap"
    body: |
      GEFUNDEN BEIM GEGENLESEN am 04.08.2026, nachdem BACKLOG E5/B7 auf FERTIG
      gesetzt worden war. Der Eintrag stimmte fuer das Repo und nicht fuer die
      Wirklichkeit.

      DIE ANNAHME: `config/templates/dietpi.txt` ist die dietpi.txt der Box.
      Wer dort `AUTO_SETUP_SWAPFILE_LOCATION=zram` eintraegt, hat zram.

      GEMESSEN, im Repo nachgesehen — DREI Wege, und keiner nimmt die Vorlage:

        scripts/make-boot-sd.sh      legt Tarball, autosetup.sh und
                                     Automation_Custom_Script.sh auf die Karte
                                     und aendert an DEREN dietpi.txt genau
                                     zwei Schluessel: AUTO_SETUP_CUSTOM_SCRIPT_EXEC
                                     und (mit --wifi) die WLAN-Schluessel.
                                     `config/templates/dietpi.txt` wird nie
                                     kopiert.
        scripts/flash-mupibox-sd.sh  sucht dietpi.txt NUR, um die
                                     Bootpartition zu erkennen (Zeile 45).
        autosetup/autosetup.sh       laeuft erst NACH DietPis Erstlauf. Da ist
                                     der Swap schon angelegt.

      Die Vorlage ist also eine Absichtserklaerung. Sie zu aendern fuehlt sich
      an wie eine Aenderung an der Box und ist keine.

      DER BEWEIS AN DER LAUFENDEN BOX (.169, 04.08.2026, nur lesend):

          /boot/dietpi.txt : AUTO_SETUP_SWAPFILE_SIZE=0
                             AUTO_SETUP_SWAPFILE_LOCATION=/var/swap
          /proc/swaps      : /dev/zram0  partition  1029200  Priority 100

      Beides gleichzeitig wahr. dietpi.txt WIRKT NUR BEIM ERSTLAUF; das zram
      kam spaeter vom remote-step-installer und steht in der Datei nicht. WER
      DEN ZUSTAND EINER LAUFENDEN BOX AUS IHRER dietpi.txt ABLIEST, LIEST DIE
      FALSCHE DATEI — in beide Richtungen: die Datei kann etwas behaupten, was
      nicht laeuft, und verschweigen, was laeuft.

      UND DAS MESSGERAET LOG MIT. `tools/bootkette-schau.py` stellt fuer jeden
      Hebel BOX gegen REPO. Fuer zram fragte die Spalte REPO die VORLAGE ab —
      also meldete sie "Repo ja", waehrend keine frische Karte zram bekam.
      Genau die Luecke, die das Werkzeug sichtbar machen soll, war in ihm
      unsichtbar. Eine Spalte "REPO" ist nur so viel wert wie das, was sie
      REPO nennt: der AUSROLLWEG, nicht die Absicht.

      REGEL: Bevor ein Punkt auf FERTIG geht, den Weg von der geaenderten
      Datei bis zum Geraet benennen koennen. "Steht im Repo" ist keiner.
      Dasselbe Muster wie bei librespot-konto.py und der DSI-Zeile in
      chromium-autostart.sh — nur diesmal in der Datei, die den Befund
      aufschreiben sollte.
    fix: |
      scripts/make-boot-sd.sh setzt die Swap-Schluessel jetzt in die dietpi.txt
      DER KARTE, in derselben Machart wie AUTO_SETUP_CUSTOM_SCRIPT_EXEC:
      vorhandene (auch auskommentierte) aendern, fehlende anhaengen.
          AUTO_SETUP_SWAPFILE_SIZE=1        (auto; bei zram = 50 % des RAM)
          AUTO_SETUP_SWAPFILE_LOCATION=zram
      Zurueck: auf der Karte wieder /var/swap eintragen, BEVOR der Pi das erste
      Mal startet; danach nur noch ueber dietpi-config.
      Geprueft von tools/karte-dietpi-schluessel.test.sh (14 Punkte) — der Test
      faehrt make-boot-sd.sh gegen ein Verzeichnis mit einer dietpi.txt, wie
      DietPi sie ausliefert, und prueft auch, dass fremde Schluessel (Sprache,
      Taktregler) ueberleben und ein zweiter Lauf nichts mehr aendert.
      tools/bootkette-schau.py fragt fuer zram jetzt scripts/make-boot-sd.sh.
    source: >
      Session 2026-08-04 (Gegenlesen von E1/E5), Repo durchgesehen und an
      Box .169 nur lesend gegengeprueft.
    related:
      - zram-statt-swap-auf-der-box
      - boot-nur-ein-hebel-dhcp
      - speicher-waechst-erst-im-betrieb
      - bootkette-aufmachen

  - id: wait-for-network-ist-nicht-der-bremser
    kind: note
    title: "AUTO_SETUP_BOOT_WAIT_FOR_NETWORK=0 bringt dem Kiosk NICHTS — nachgesehen, nicht vermutet"
    tags: [boot, dietpi, systemd, kiosk, e5, geprueft]
    body: |
      NAHELIEGENDER VERDACHT beim Gegenlesen von E5 am 04.08.2026: Der Kiosk
      wartet auf das Netz, und DietPi hat dafuer einen eigenen Schalter —
      `AUTO_SETUP_BOOT_WAIT_FOR_NETWORK=1` steht in der dietpi.txt der Box UND
      in config/templates/dietpi.txt. Waere er der Bremser, waere eine 0 dort
      ein groesserer und harmloserer Hebel als das DHCP-Feintuning
      ([boot-nur-ein-hebel-dhcp]) — kein Anfassen von /etc/dhcp, kein Skript.

      ER IST ES NICHT. An der Box nachgesehen (nur lesend), was der Schalter
      ueberhaupt tut — `/boot/dietpi/func/dietpi-set_software`,
      Funktion `Boot_Wait_For_Network()`:

          Wert 1 -> /etc/systemd/system/dietpi-postboot.service.d/dietpi.conf
                    mit  Wants=network-online.target
                         After=network-online.target
          Wert 0 -> genau diese Datei wieder loeschen

      Er beruehrt AUSSCHLIESSLICH `dietpi-postboot.service`. Und die liegt
      nicht in der Kiosk-Kette:

          systemctl show dietpi-postboot -p Before -p After
            Before = shutdown.target multi-user.target
            After  = basic.target sysinit.target dietpi-preboot.service
                     system.slice network-online.target

          systemctl show getty@tty1 -p After
            ... systemd-user-sessions.service ... (KEIN dietpi-postboot)

          systemctl show systemd-user-sessions -p After
            ... network.target ...   <- HIER haengt es, Debian-Vorgabe

      Der Bremser ist `network.target`, und davor haengt `ifup@wlan0.service`
      (Type=oneshot, laeuft bis DHCP gebunden hat). Das ist Debian, nicht
      DietPi — der DietPi-Schalter kann daran nichts aendern.

      WAS DAS WERT IST: eine Aenderung, die NICHT gemacht wurde, weil vorher
      nachgesehen wurde. Wer sie gemacht haette, haette die Bootzeit gemessen,
      keinen Unterschied gefunden und die Ursache anschliessend woanders
      gesucht — mit einem stillschweigend geaenderten Verhalten der Box im
      Ruecken.
    source: >
      Session 2026-08-04, Box .169 nur lesend (dietpi-set_software gelesen,
      systemctl show fuer dietpi-postboot, getty@tty1, systemd-user-sessions).
    related:
      - boot-nur-ein-hebel-dhcp
      - kiosk-wartet-auf-dhcp
      - bootkette-aufmachen

  # ══ Gegenlesen des Ausrollwegs (04.08.2026) ══════════════════════════════
  - kind: falle
    id: schalter-ohne-unit-luefter
    title: "Der Luefter-Schalter zeigte auf eine Unit, die keine frische Karte hat"
    sev: high
    match: "mupi_fan|Luefter.*schaltet nicht|systemctl enable.*does not exist|Unit .* not found"
    cause: "config/services/mupi_fan.service wurde von KEINEM Ausrollweg angelegt, obwohl scripts/fan/* ueber beide kam — und beide Verwaltungen den Luefter zum Einschalten anbieten"
    fix: "Unit in autosetup.sh und update/start_mupibox_update.sh anlegen (nur anlegen, nicht enablen); nachmessen mit tools/ausrollweg-deckung.py"
    tags: ['ausrollweg', 'luefter', 'systemd', 'verwaltung', 'falle', 2026]
    source: "04.08.2026, beim Gegenlesen am Quelltext gefunden — nichts auf der Box angefasst"
    body: |
      DER VIERTE FUND DERSELBEN BAUART, und der erste, der nicht durch Zufall
      kam. Piper ([[piper-fehlt-im-ausrollweg]]), der librespot-Waechter
      ([[update-ueberschreibt-librespot]]) und MQTT
      ([[mqtt-reparatur-jetzt-im-repo]]) waren drei Einzelfunde. Erst als die
      Frage EINMAL fuer jede Unit und jedes Skriptverzeichnis gestellt wurde,
      fiel der vierte heraus.

      ── WAS DARAN SCHLIMM IST ───────────────────────────────────────────

      Nicht die fehlende Datei. Der SCHALTER, DER INS LEERE ZEIGT:

          scripts/fan/fan_control.py        kam ueber BEIDE Wege an
          config/services/mupi_fan.service  kam ueber KEINEN

          AdminInterface/www/mupi.php:399   sudo systemctl enable mupi_fan.service
          src/backend-api/src/dienste.ts    fuehrt den Luefter ausdruecklich als
                                            NORMALE Wahlmoeglichkeit

      Der Kommentar in dienste.ts sagt es sogar selbst: ein „abgeloest" am
      Luefter waere „eine Falschauskunft, und zwar eine, die jemanden davon
      abhaelt, den Luefter einzuschalten". Der Eintrag stand also bewusst in
      der normalen Liste — und auf einer frischen Karte konnte man ihn nicht
      einschalten. `exec()` im PHP-Admin wirft die Fehlermeldung weg; der
      Schalter sieht danach aus wie an. Ergebnis: ein heisser Pi und Eltern,
      die glauben, der Luefter laeuft.

      ── DIE LEHRE, die ueber diesen einen Fall hinausgeht ───────────────

      Bisher hiess die Frage: „fehlt eine Datei?" Sie muss heissen: „gibt es
      einen Bedienknopf, dessen Gegenstueck auf einer frischen Karte fehlt?"
      Das ist eine andere Suche — sie geht von der OBERFLAECHE aus, nicht vom
      Repo. Deshalb liest tools/ausrollweg-deckung.py nicht nur
      config/services/ und scripts/, sondern auch backend.php und mupi.php:
      eine ungedeckte Unit ist ein Mangel, eine ungedeckte Unit MIT SCHALTER
      ist eine Luege.

      ── EINE FALLE BEIM BAUEN DES WERKZEUGS ─────────────────────────────

      Kommentarzeilen muessen raus, bevor man greppt. In autosetup.sh steht
      seit dem 04.08.2026 ein langer Kommentarblock, in dem `librespot-konto.py`
      und `scripts/box` namentlich vorkommen. Ein stumpfes grep haelt beide
      fuer ausgerollt — den einen zu Recht, den anderen nicht. Genau so
      entsteht ein gruener Haken ueber einem Loch.

      ── WAS BEWUSST NICHT GEDECKT IST, mit Grund ────────────────────────

          spotifyd.service          abgeloest (dienste.ts, mit Beleg)
          mupi_change_checker       der Update-Weg raeumt sie ausdruecklich weg
          scripts/hwdetect/         laeuft beim SD-Bestuecken, nicht auf der Box
          scripts/online/           von Hand, wenn ein RTL88x2BU-Stick dranhaengt
          scripts/box/              WIRKLICH offen (BACKLOG E12/X5) — dort fehlt
                                    nicht nur der Weg, sondern auch die Unit

      Der letzte Punkt steht im Werkzeug als „bekannt offen" und macht die
      Pruefung NICHT rot. Grund: eine Pruefung, die immer rot ist, liest nach
      zwei Wochen niemand mehr — und dann faellt der naechste echte Befund
      darin nicht auf.

      NACHMESSEN:
        python3 tools/ausrollweg-deckung.py
        ssh <box> "systemctl cat mupi_fan.service >/dev/null && echo da"
    related: [piper-fehlt-im-ausrollweg, update-ueberschreibt-librespot, mqtt-reparatur-jetzt-im-repo]

  - kind: knowhow
    id: piper-kosten-und-vorgabe-aus
    title: "Piper: 244 MB fuer ein Merkmal, das ab Werk aus ist — und 17 s ohne flask"
    tags: ['vorlesen', 'piper', 'ausrollweg', 'speicher', 2026]
    source: "04.08.2026, Gegenlesen: am Quelltext nachgerechnet, Groessen an Box .169 nur GELESEN"
    body: |
      DREI ZAHLEN, die beim ersten Anlauf fehlten oder falsch waren.

      ── 1. WAS DER SCHRITT KOSTET ──────────────────────────────────────

          venv (piper-tts 1.6.0, onnxruntime, numpy, Flask)   184 MB
          de_DE-ramona-low.onnx (+ .onnx.json, 4 KB)           60 MB
                                                             ------
                                                            ~244 MB, aus dem Netz

      Platz ist nicht das Problem (117 GB Karte, 5,7 GB belegt), ZEIT ist es —
      dafuer steht die Zeitgrenze von 1200 s in beiden Ausrollwegen.

      ── 2. VORLESEN IST AB WERK AUS ────────────────────────────────────

      `EINSTELLUNGEN_VORGABE.modus` in src/backend-api/src/vorlesen.ts steht
      auf 'aus', und ohne vorlesen.json bleibt es dabei. Jede frische Karte
      zahlt die 244 MB also auch dann, wenn den Schalter nie jemand umlegt.
      Das gehoert offen hingeschrieben — der erste Anlauf klang, als spraeche
      eine frische Box ab Werk.

      WARUM DER SCHRITT TROTZDEM IM AUSROLLWEG STEHT: der Eltern-Bereich kann
      nur STIMMEN nachladen (POST /api/vorlesen/stimme), keinen venv bauen —
      dafuer braeuchte er root. Verschoebe man ihn ohne diesen Weg, haette man
      einen Schalter, der sich umlegen laesst und nichts tut. Genau die Sorte
      Fehler, gegen die der ganze Schritt gebaut wurde. (BACKLOG E12/X12.)

      ── 3. OHNE FLASK SIND ES 17 s, NICHT 2,2 s ────────────────────────

      Die Zahl 2,2 s stand zuerst in [[piper-fehlt-im-ausrollweg]] und im
      Einrichtungsskript. Sie ist die Zeit des EINZELAUFRUFS — aber sie ist
      nicht die Zeit, die der Benutzer wartet. Am Quelltext nachgerechnet
      (server.ts, piperDienstStarten):

          spawn piper.http_server   ohne flask sofort tot
          Warteschleife             30 x 500 ms = 15 s laufen TROTZDEM ab
          danach Einzelaufruf       + 2,2 s
                                    ------
                                    ~17 s je NEUEM Satz

      Und es wiederholt sich: der `close`-Behandler setzt piperDienstBereit
      auf null zurueck, der naechste ungespeicherte Satz wartet wieder 15 s.
      Der Zaun im Abspieler (4 s) bricht das vorher ab — das Kind hoert also
      GAR NICHTS und die Musik faengt an. Nicht „irgendwie langsam", sondern
      „liest meistens nicht vor".

      ── DAZU: EINE STIMME SIND ZWEI DATEIEN ────────────────────────────

      Neben jeder *.onnx liegt eine *.onnx.json (rund 4 KB, Lautschrift und
      Abtastrate). Nur die .onnx zu pruefen war der Fehler der ersten Fassung
      des Einrichtungsskripts: reisst die Leitung nach dem 60-MB-Modell ab,
      meldet der Bericht „1 Stimme, bereit", und jeder Sprechversuch
      scheitert. Dieselbe Sorte Luege wie `bereit: fs.existsSync(piperBin)`.
      ACHTUNG, NOCH OFFEN: `vlStimmenIds()` in server.ts zaehlt weiterhin nur
      *.onnx — die Verwaltung zeigt eine halbe Stimme also weiterhin als
      waehlbar an.

      ── UND EIN SPEICHERPOSTEN, DER DAZUGEHOERT ────────────────────────

      Der Piper-Dienst bleibt nach dem ersten Satz stehen. Nach 9,7 h waren es
      698 MB PSS = 34,7 % des RAM — der groesste einzelne Posten der Box und
      der Grund, warum dietpi.txt jetzt zram statt /var/swap setzt
      (BACKLOG E12/X2, war dort faelschlich noch als OFFEN vermerkt).

      OFFEN UND ABSICHTLICH OHNE ZAHL: die Lizenz haengt an der EINZELNEN
      Stimme (HuggingFace rhasspy/piper-voices), nicht an Piper, und ist fuer
      de_DE-ramona-low nie nachgeschlagen worden. Solange das so ist: keine
      Stimme auf ein Abbild packen, das weitergegeben wird (BACKLOG E12/X11).
    related: [piper-fehlt-im-ausrollweg, vorlesen-tts, google-tts-abgeloest, schalter-ohne-unit-luefter]

  - id: kopiert-ist-nicht-aufgerufen
    kind: falle
    title: "Ein Einrichtungsskript liegt auf der Box und lief nie — der Update-Weg kopierte es nur"
    tags: [ausrollweg, installer, update, dhcp, boot, falle, e5, e12]
    match: "liegt auf der Box|rollt aus|einbauen|Update bringt es nicht|nur autosetup"
    body: |
      GEFUNDEN BEIM GEGENLESEN am 04.08.2026, einen Tag nachdem
      [dietpi-txt-vorlage-ist-kein-ausrollweg] dieselbe Frage eine Etage
      hoeher gestellt hatte. Dort war die Antwort "die Datei kommt gar nicht
      an". Hier kommt sie an — und passiert trotzdem nichts.

      DER FALL: `scripts/mupibox/dhcp-schneller.sh` (BACKLOG E5/B1, der eine
      gemessene Bootzeit-Hebel: 4,96 s Warten auf nichts, siehe
      [boot-nur-ein-hebel-dhcp]) war NUR in `autosetup/autosetup.sh`
      eingehaengt. `update/start_mupibox_update.sh` kopiert es mit —
      `mv ${MUPI_SRC}/scripts/mupibox/* /usr/local/bin/mupibox/` nimmt alles
      mit — aber ruft es nie auf.

      WAS DAS HEISST: der Hebel erreicht ausschliesslich FRISCHE KARTEN. Jede
      laufende Box behaelt ihre Wartezeit, fuer immer, denn ein Update ist der
      einzige Weg, der sie je erreicht. Nachgesehen an .169 (nur lesend):
      ihre /etc/dhcp/dhclient.conf traegt das Datum 03.05.2025 und enthaelt
      den Block nicht — ausgerechnet die Box, an der die 4,96 s gemessen
      wurden, haette sie nie losbekommen.

      UND DAS WERKZEUG SAH ES NICHT. `tools/ausrollweg-deckung.py` wurde am
      selben Tag genau gegen diese Fehlerklasse gebaut und meldete
      `scripts/mupibox/` auf beiden Wegen "rollt aus" — richtig und nutzlos,
      denn es fragt, ob eine Datei ANGEFASST wird, nicht, ob sie AUFGERUFEN
      wird. Fuer Werkzeuge, die spaeter jemand startet, ist "liegt da" die
      richtige Frage. Fuer EINRICHTUNGSSKRIPTE, die einmal etwas umstellen und
      danach nie wieder laufen, ist sie falsch.

      DIE UNTERSCHEIDUNG, um die es geht:

          scripts/mqtt/mqtt.py               Werkzeug   — "liegt da" genuegt,
                                                          eine Unit startet es
          scripts/mupibox/dhcp-schneller.sh  Einrichtung — muss LAUFEN, sonst
                                                          ist nichts passiert
          scripts/mupibox/piper-einrichten.sh Einrichtung — stand richtig in
                                                          BEIDEN Wegen

      piper war der Gegenbeweis im selben Verzeichnis: dort war es richtig
      gemacht, und gerade das machte den Unterschied unsichtbar — die Liste
      sah vollstaendig aus, weil ein Beispiel darin stimmte.

      DREI SORTEN "kommt nicht an", jetzt alle benannt:
        1. Datei fehlt im Weg          — librespot-konto.py, mupi_fan.service
        2. Vorlage statt Weg           — config/templates/dietpi.txt
        3. KOPIERT, ABER NIE GERUFEN   — dhcp-schneller.sh (dieser Eintrag)
    fix: |
      `update/start_mupibox_update.sh` ruft `dhcp-schneller.sh --einbauen`
      jetzt an derselben Stelle wie autosetup.sh (nach dem chmod-Block), mit
      demselben `|| echo` — scheitert es, laeuft das Update weiter und die Box
      bootet so langsam wie bisher. Gefahrlos zu wiederholen: das Skript
      haengt einen markierten Block an und tut beim zweiten Lauf nichts.
      Rueckweg unveraendert:
          sudo /usr/local/bin/mupibox/dhcp-schneller.sh --zuruecknehmen

      GEGEN DEN RUECKFALL: `tools/ausrollweg-deckung.py` hat einen dritten
      Abschnitt. Die Liste `AUFRUF_NOETIG` nennt Einrichtungsskripte, und
      gesucht wird der AUFRUFPFAD `/usr/local/bin/mupibox/<name>` im
      kommentarfreien Text beider Wege — `chmod 755 …/mupibox/*` und
      `mv …/scripts/mupibox/*` nennen keines davon, ein Treffer ist also
      wirklich ein Aufruf. Gegen den Stand vor der Behebung nachgestellt: der
      Abschnitt haette dhcp-schneller.sh im Update-Weg als Loch gemeldet.

      WER DORT ETWAS EINTRAEGT, sagt damit "gehoert in BEIDE Wege" — also auch
      auf laufende Boxen. Nichts eintragen, was beim Update Schaden anrichten
      koennte; beide heutigen Eintraege sind wiederholbar und haben einen
      Rueckweg.
    source: >
      Session 2026-08-04 (Gegenlesen von E1/E5), Repo durchgesehen und an
      Box .169 nur lesend gegengeprueft (dhclient.conf ohne Block, Datei vom
      03.05.2025). An der Box wurde nichts veraendert.
    related:
      - dietpi-txt-vorlage-ist-kein-ausrollweg
      - boot-nur-ein-hebel-dhcp
      - dhclient-conf-nicht-ersetzen
      - piper-fehlt-im-ausrollweg
      - schalter-ohne-unit-luefter

  - id: server-antwortet-200-auf-alles
    kind: falle
    sev: high
    title: "HTTP 200 beweist an dieser Box gar nichts — nur der Content-Type tut es"
    match: "200 aber kein JSON|Endpunkt da\\?|gibt es die Route|curl -o /dev/null -w %\\{http_code\\}|api/.* 200|Angular-Startseite statt JSON"
    cause: "Der Server liefert fuer JEDEN unbekannten Pfad die Angular-Startseite aus (SPA-Rueckfall). Das gilt auch fuer /api/-Pfade — es gibt keinen Zweig, der einen unbekannten API-Pfad mit 404 abweist."
    fix: "Nie am Statuscode pruefen, ob ein Endpunkt ausgeliefert ist, sondern am Content-Type: application/json = da, text/html = NICHT da. `curl -s -o /dev/null -w '%{http_code} %{content_type} %{size_download}'`."
    tags: ['server', 'api', 'messen', 'falle', 'ausrollweg', 2026]
    source: "04.08.2026, curl gegen Box .169:8200, nur lesend. Die Box trug den Stand von 08:12 (ohne ARD)."
    body: |
      GEMESSEN, alles am selben Nachmittag, alles gegen dieselbe laufende Box:

        Pfad                       Code  Content-Type   Groesse
        /api/gibtesnicht            200  text/html      9688 B
        /api/ard/suche              200  text/html      9688 B
        /api/ard/suche?q=maus       200  text/html      9688 B
        /api/medien                 200  application/json  9866 B
        /api/streaming              200  application/json  1023 B

      Die ersten drei sind BYTEGLEICH: es ist dreimal `index.html` der
      Angular-App. `/api/gibtesnicht` ist frei erfunden, `/api/ard/suche`
      existiert im Repo seit dem 04.08. — nur eben nicht auf DIESER Box, die
      den Stand von 08:12 trug. Beide sehen von aussen identisch aus.

      WER SO PRUEFT, BEKOMMT IMMER JA. Die uebliche schnelle Frage lautet
      `curl -o /dev/null -w '%{http_code}'` und liefert 200 — fuer eine Route,
      die es gibt, fuer eine, die noch nicht ausgerollt ist, und fuer eine,
      die es nie geben wird. Das ist keine schwache Pruefung, sondern eine,
      die IMMER besteht; sie misst nichts.

      WAS STATTDESSEN: der Content-Type. `application/json` heisst „der
      Endpunkt ist da und hat geantwortet", `text/html` heisst „die
      SPA-Rueckfallseite" — also NICHT da. Die Groesse taugt als
      Zweitmerkmal: die Rueckfallseite ist bei jedem Pfad exakt gleich gross.

      DASSELBE GILT FUER DEN ABSPIELDIENST, aus einem anderen Grund und mit
      derselben Wirkung: er quittiert einen UNBEKANNTEN Befehl mit 200 und
      tut nichts ([[playpause-gibt-es-nicht]]). Zwei verschiedene Ursachen,
      eine Lehre — an dieser Box ist 200 die Vorgabe und keine Auskunft.
    related:
      - playpause-gibt-es-nicht
      - api-routen-ohne-gegenzweig-haengen
      - pruefen-schritte-die-still-luegen
      - im-kiosk-messen-statt-raten

  - id: dienstliste-an-fuenf-orten
    kind: falle
    sev: high
    title: "Ein neuer Dienst muss an fuenf Orten nachgetragen werden — und keiner meldet sich, wenn er fehlt"
    match: "neuer Dienst|dienstVon|DIENST_MARKEN|DIENST_NAME|marke-<dienst>|mediaSourceOf|media-provider|Dienst fehlt|ard fehlt"
    cause: "Die Liste der Dienste steht in jedem Verbraucher noch einmal: als Typ-Union, als Objektschluessel, als CSS-Klasse, als Anbieter-Eintrag. Kein Lader verbindet sie — NewDesign laeuft ohne Bauschritt, app.css ist CSS, und die Verwaltung ist eine eigene Angular-App ohne eine Zeile aus backend-api."
    fix: "Wo ein gemeinsamer Lader existiert, ableiten (werke.ts: `dienst: ReturnType<typeof dienstVon>`). Wo nicht: `tools/dienste-deckung.mjs --pruefen` haelt die eine Quelle gegen alle Verbraucher und wird rot, wenn einer fehlt. Der Schritt haengt in tools/pruefen.sh („Deckung der Dienstliste\")."
    tags: ['dienste', 'architektur', 'werkzeug', 'falle', 'ard', 'e4', 2026]
    source: "04.08.2026, beim Anschliessen der ARD (BACKLOG E4/A6-A8). Die Liste der fuenf Orte wurde am Quelltext erhoben."
    body: |
      `DIENST_MARKEN` STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die
      trennt: MELDET SICH ETWAS?

          Ein Werkzeug bricht mit `SyntaxError: Unexpected token '}'`
          ab und zeigt dabei auf app.js
          -> [[klammerzaehler-liest-kommentare-mit]] (der Zaehler las
             einen Kommentar mit; in app.js ist nichts kaputt)

          NICHTS meldet sich — der Dienst fehlt einfach in Oberflaeche,
          Verwaltung oder Filter
          -> DIESER Eintrag (fuenf Orte, keiner davon wird rot)

      Die beiden sind Gegenstuecke: der eine ist ein lauter Fehler an
      der falschen Adresse, der andere ein stummer an der richtigen.

      DER BEFUND. Am 04.08.2026 war die ARD Audiothek im Backend fertig
      gebaut und geprueft: `ard.ts` (rein, 51 Tests), `/api/ard/suche`,
      `/api/ard/kinder`, der Typ `ard` in `dienstVon`/`artVon`, die Verben
      `ard`/`ardqueue` im Abspieldienst. Sie kam trotzdem an FUENF Stellen
      nicht an — und KEINE davon hat sich gemeldet. Kein Test wurde rot, kein
      Bau brach, keine Meldung erschien.

        1  backend-api/src/streaming.ts   Anbieterliste: stand 'geplant',
                                          waehrend es laengst gebaut war
        2  frontend-admin .../medien.ts   DIENST_NAME + Suchfilter: kein Weg,
                                          eine Kachel anzulegen ausser von Hand
        3  frontend-box media.service.ts  iif-Kette; die Kachel fiel auf
                                          `of([item])` und tat beim Antippen
                                          nichts (media-provider kannte 'ard'
                                          nicht)
        4  NewDesign/app.js + app.css     DIENST_MARKEN und `.marke-ard` —
                                          die Plakette blieb leer
        5  tools/dienst-marken-schau.mjs  prueft die fuenf bekannten Dienste
                                          GEGENEINANDER und weiss von einem
                                          sechsten nichts. Gruen, waehrend die
                                          Plakette fehlte.

      DAZU KAMEN BEIM AUFRAEUMEN ZWEI WEITERE, die niemand gezaehlt hatte:
      `werke.ts` fuehrte dieselbe Aufzaehlung noch einmal, mit dem Kommentar
      „keine zweite Liste" darueber (sie war eine, sie stimmte nur zufaellig
      noch), und in der Verwaltung fuehrten `dienstSchluessel()` und
      `dienstName()` ZWEI verschiedene Typ-zu-Dienst-Ketten mit leicht
      verschiedenem Ergebnis. Der Unterschied fiel nie auf, weil beide fuer
      die drei haeufigen Faelle dasselbe sagten — bis `ard` dazukam: die
      Bibliothek schrieb „Anderes", waehrend das Zeichen daneben `ard` hiess.

      ── WARUM NICHT EINFACH EINE DATEI ────────────────────────────────
      Die Frage wurde gestellt und beantwortet, nicht weggewinkt. Die
      Verbraucher haben NICHT DENSELBEN LADER:

        NewDesign/app.js   laeuft ohne Bauschritt im Kiosk-Browser. Es gibt
                           keinen Bundler, der ein TypeScript-Modul des
                           Servers hineinreichen koennte.
        NewDesign/app.css  ist CSS. Eine Farbe je Dienst ist eine Regel, kein
                           Datensatz.
        frontend-admin     eigene Angular-App, eigenes tsconfig; sie
                           importiert bis heute KEINE Zeile aus backend-api
                           (nachgesehen 04.08.2026). Ein Pfad-Alias dorthin
                           zoege die Servertypen in den Browser-Bau.

      WO ES GING, IST ES GEMACHT: `werke.ts` leitet seine Liste jetzt wirklich
      ab (`dienst: ReturnType<typeof dienstVon>`), und `verschmelzung.ts`
      haengt schon an werke.ts — aus drei Listen wurde eine.

      ── UND FUER DEN REST: tools/dienste-deckung.mjs ──────────────────
      Es liest die eine Quelle (den Rueckgabetyp von `dienstVon()` in
      medien.ts) und haelt sie gegen jeden Verbraucher. Drei Dinge macht es
      anders als ein blosser Vergleich:

        * BEWUSSTE AUSLASSUNGEN STEHEN DRIN, MIT GRUND. `anderes` bekommt
          kein Zeichen, weil es „wir wissen es nicht" heisst; `mediaSourceOf`
          beschriftet nur die drei Bibliotheken, die man DURCHBLAETTERT. Wer
          weglassen will, traegt es ein; wer vergisst, wird rot. Der
          Unterschied zwischen „bewusst" und „vergessen" ist damit
          aufgeschrieben statt geraten.
        * DIE GEGENRICHTUNG. Ein Name beim Verbraucher, den `dienstVon` gar
          nicht kennt, ist fast immer ein Tippfehler — und ein Tippfehler in
          einem CSS-Klassennamen faellt sonst NIE auf, weil eine Regel, die
          nichts trifft, nicht meckert.
        * DIE QUELLE GEGEN SICH SELBST. Rueckgabetyp gegen die `return`-Zweige
          im Rumpf: ein Name im Typ, den kein Zweig vergibt, wird nie
          vergeben.

      GEGENPROBE GEMACHT, wie [[attrappe-luegt-durch-weglassen]] es verlangt:
      `.marke-ard` aus app.css entfernt -> „FEHLT: 'ard' fehlt (Farbe der
      Plakette)", Rueckgabe 1. Wieder eingesetzt -> weg. Beim Bauen war das
      Werkzeug ausserdem echt rot und hat die zwei damals noch offenen Stellen
      selbst benannt — es ist nicht gruen geboren.

      ── NACHTRAG VOM GEGENLESEN (04.08.2026): ES WAREN NICHT FUENF ────
      Mit einer GEGENPROBE JE STELLE erhoben — `ard` herausnehmen, alle
      Pruefschritte laufen lassen, wieder einsetzen. Drei weitere Stellen waren
      gruen und sind seither Verbraucher dieses Werkzeugs: `dienstSuche()` in
      server.ts (die Verwaltung faende die ARD nicht mehr), `dienstSchluessel()`
      in der Verwaltung (die Bibliothek naennte sie „Anderes") und der
      Typ-Zweig in media.service.ts — ausgerechnet die Stelle, um die es in
      E4/A8 ueberhaupt ging. Und eine SECHSTE liess sich am Quelltext gar nicht
      abgleichen: [[kachel-sieht-richtig-aus-und-spielt-nicht]]. Die Zahl im
      Titel ist also die des ERSTEN Zaehlens; die Lehre ist, dass man sie
      MESSEN muss, statt sie zu zaehlen.

      DIESELBE BAUART, andere Etage: [[drei-orte-eine-anzeige]],
      [[spielt-marke-eine-frage-zwei-formen]],
      [[darstellung-feld-faellt-still-heraus]], [[dienst-zeichen-auf-den-kacheln]].
    related:
      - drei-orte-eine-anzeige
      - spielt-marke-eine-frage-zwei-formen
      - darstellung-feld-faellt-still-heraus
      - dienst-zeichen-auf-den-kacheln
      - attrappe-luegt-durch-weglassen
      - arbeitsweise-werkzeug-statt-wegwerfbefehl
      - klammerzaehler-liest-kommentare-mit

  - id: ard-anschluss-an-beide-oberflaechen
    kind: entscheidung
    title: "Die ARD sichtbar machen — vier Anschlussstellen, und was an jeder NICHT gebaut wurde"
    match: "ARD anschliessen|E4/A6|E4/A7|E4/A8|ARD Formular|ARD Plakette|ard.service.ts|abspielbefehl"
    tags: ['ard', 'audiothek', 'oberflaeche', 'verwaltung', 'e4', 'entscheidung', 2026]
    source: "04.08.2026, BACKLOG E4/A6-A8 gebaut; Plaketten in tools/dienst-marken-schau.mjs bei 22/29/34/116 px angesehen."
    body: |
      Das Modul war fertig ([[ard-audiothek-schnittstelle-a1]]), der
      Abspielweg auch. Was fehlte, waren die ANSCHLUSSSTELLEN — vier Stueck,
      und an jeder war die naheliegende Loesung die falsche.

      ── A6 VERWALTUNG: KEIN ZWEITES FORMULAR ──────────────────────────
      Der naheliegende Weg waere eine eigene ARD-Suchmaske gewesen —
      `/api/ard/suche` liefert je Treffer einen fertigen `vorschlag`, das ist
      halbe Arbeit. Sie haette eine zweite Trefferliste gebraucht, ein zweites
      Umbenennen-vor-dem-Aufnehmen, eine zweite Kategoriewahl und einen
      zweiten Weg zum Anlegen: genau die Bauart aus
      [[dienstliste-an-fuenf-orten]].
      GEBAUT IST: `dienstSuche()` (server.ts) liefert die Sendungen in
      DERSELBEN Trefferform wie Spotify und Jellyfin. Alles Vorhandene wirkt
      ohne eine neue Zeile Oberflaeche — auch die Gruppierung ueber Dienste
      hinweg, die eine ARD-Sendung und ein gleichnamiges Spotify-Hoerbuch als
      EIN Werk mit zwei Quellen zeigt. Neu sind vier Zeilen: ein Eintrag im
      Auswahlfeld, ein Klartextname, ein Farbzeichen, eine Art („Sendung").

      NUR ALS `show`, denn eine Sendung ist eine Folgenreihe (dasselbe sagt
      `artVon`). KEINE `titelAnzahl` — `numberOfElements` liegt bei MausZoom
      um Faktor 5,4 daneben ([[ard-items-ohne-ton-und-die-falschen-zahlen]]),
      und in `gruppiereTreffer` zaehlt das Feld als Unterscheidungsmerkmal
      mit, triebe also echte Dubletten auseinander.

      ── A7 PLAKETTE: NICHT DAS ARD-LOGO ───────────────────────────────
      Spotify und Jellyfin stehen mit ihrer ECHTEN Marke auf den Kacheln
      ([[dienst-zeichen-auf-den-kacheln]]), also lag das ARD-Zeichen nahe. Es
      ist ein WORTZEICHEN aus drei Buchstaben; die Figur auf der Plakette ist
      19 px breit, also rund 6 px je Buchstabe. Das waere ein grauer Fleck,
      der so tut, als sage er etwas — und ein fremdes Wortzeichen zu
      verunstalten ist schlechter, als es gar nicht zu benutzen.
      GEBAUT IST ein SENDEMAST. Er sagt „Rundfunk" und ist von `radio`
      unterscheidbar, obwohl beide mit Wellen zu tun haben: `radio` ist eine
      Rosette um einen Punkt in der Mitte (symmetrisch, ohne Oben und Unten),
      der Mast ist unten schwer und oben spitz. Bei 19 px zaehlt die
      Silhouette, nicht das Detail.

      FARBE #0f766e, Farbton 175. Der Farbkreis war voll: radio 17, spotify
      141, lokal 210, jellyfin 268, rss 339. Die groesste Luecke lag zwischen
      Spotify und lokal — 34 Grad zu jedem Nachbarn, was FUER SICH zu wenig
      ist. Deshalb kommt die Helligkeit dazu: das Petrol ist deutlich dunkler
      und weniger bunt als beide. Ausdruecklich NICHT das ARD-Blau, das laege
      neben `lokal`.
      ANGESEHEN, NICHT GERECHNET: in tools/dienst-marken-schau.mjs bei
      22/29/34/116 px, hell und dunkel, auf Spotify-Gruen, Weiss und Schwarz.

      ── A8 KLASSISCHE OBERFLAECHE: DAS RSS-MUSTER ─────────────────────
      Eine ARD-Sendung ist genau das, was ein Podcast dort schon ist: EIN
      Eintrag in data.json, hinter dem viele Folgen liegen. `getRssFeed()`
      loest ihn in eine Liste von `Media` auf, jede Folge wird eine Kachel.
      `ArdService.getSendung()` hat dieselbe Signatur; rein getrennt wie bei
      Jellyfin (`ard.ts` neben `ard.service.ts`).

      ZWEI ABWEICHUNGEN VOM RSS-MUSTER, beide aus derselben Ursache — die
      Tonadresse der ARD laeuft ab:
        * DER ABSPIELBEFEHL WIRD DURCHGEREICHT, NICHT GEBAUT. Alle anderen
          Anbieter in media-provider.ts setzen ihre URL selbst zusammen;
          `ard` ist der eine, der `media.abspielbefehl` zurueckgibt. Der
          Befehl entsteht im Server (`abspielWeg`), Sekunden vor dem
          Abspielen. Ihn im Browser ein zweites Mal zusammenzusetzen waere
          [[spielt-marke-eine-frage-zwei-formen]] noch einmal.
        * `id` IST NICHT DIE TONADRESSE. Bei `rss` ist sie es. Hier traegt
          `id` die stabile Folgenkennung — sie geht in den Verlauf und in
          „Oft gehoert", und ein Schluessel, der jede Woche ein anderer ist,
          waere dort wertlos.

      WARUM VOR DER iif-KETTE UND NICHT IN IHR: die Kette fragt von aussen
      nach innen nach FELDERN (`query`, `artistid`, `showid`, `playlistid`),
      nicht nach dem Typ. Ein ARD-Eintrag traegt seine Kennung in `id` und
      fiel darum bis ganz nach innen durch, auf `of([item])` („Single
      album") — die Kachel erschien als Album und tat beim Antippen nichts.
      Eine weitere Schachtelungsebene haette die Kette zwoelf tief gemacht.

      ── WAS BEWUSST OHNE ARD BLEIBT ───────────────────────────────────
      `mediaSourceOf` (frontend-box/media.ts) beschriftet nur die drei
      Bibliotheken, die man DURCHBLAETTERT; Radio, Podcast und ARD bekommen
      dort kein Zeichen. Das steht mit Begruendung in
      `tools/dienste-deckung.mjs` und ist damit eine Entscheidung, keine
      Luecke.

      ── KORREKTUR VOM GEGENLESEN (04.08.2026): ES WAREN FUENF ─────────
      In der Ueberschrift stand „vier Anschlussstellen", und der Backlog
      behauptete dazu, die NEUE Oberflaeche „zeigt und spielt eine ARD-Kachel
      bereits vollstaendig". Beides war angenommen und nicht gemessen. Die
      Kachel erschien richtig und tat beim Antippen nichts —
      [[kachel-sieht-richtig-aus-und-spielt-nicht]]. Die fuenfte Stelle ist
      `abspielBefehl()`/`ausQuelleSpielen` in NewDesign/app.js; sie war die
      einzige, die sich am Quelltext gar nicht abgleichen liess, weil an dieser
      Kachel jede pruefbare EIGENSCHAFT stimmte.
    related:
      - ard-audiothek-schnittstelle-a1
      - ard-items-ohne-ton-und-die-falschen-zahlen
      - verb-als-zeichenkette-gesucht-dradio-enthaelt-radio
      - dienstliste-an-fuenf-orten
      - dienst-zeichen-auf-den-kacheln
      - attrappe-luegt-durch-weglassen

  - id: ard-folgenliste-rollt
    kind: entscheidung
    title: "Die ARD-Folgenliste ROLLT — deshalb merkt Weiterhoeren die Folge und nicht die Nummer"
    match: "resumeardfolge|ardFolgeAnNummer|ardSendungAb|folgenOeffnen|ARD weiterhoeren|ARD Folgen aufloesen|ard einzelne Folgen|rollende Folgenliste"
    tags: ['ard', 'audiothek', 'weiterhoeren', 'resume', 'neue-oberflaeche', 'e4', 'entscheidung', 2026]
    source: "05.08.2026, drei Meldungen des Benutzers zur ARD auf Box .169; gebaut in weiterhoeren.ts, server.ts, NewDesign/app.js. Geprueft: weiterhoeren.spec.ts (6 Faelle) und ard.integration.spec.ts (6 Faelle, nock statt Netz); am Verhalten gemessen mit tools/ard-folgen-lane.mjs (neu)."
    body: |
      DREI MELDUNGEN, EINE URSACHE. Der Benutzer probierte die ARD aus und
      meldete: „ard sounds zeigt nur alben loest aber nicht auf einzelne
      tracks" und „die angespielten tracks landen nicht in weiterhoeren".
      Beides ging auf denselben Punkt zurueck — die Kette Werk -> Titel und
      die Kette Werk -> Stelle kannten `ard` nicht, obwohl die AUSKUNFT
      laengst da war: `/api/werke/<s>/inhalt` loest eine Sendung seit dem
      04.08.2026 in Folgen auf, samt Titel, Bild, Dauer, Folgenkennung und je
      einem fertigen Befehl. Benutzt hat das nur `albumSpielen` — und zwar um
      ALLES am Stueck abzuspielen. Es fehlte der Anschluss, nicht die
      Auskunft. Dieselbe Bauart wie [[dienstliste-an-fuenf-orten]], nur eine
      Etage weiter.

      ── DER PUNKT, AN DEM DIE ARD ANDERS IST ALS ALLES ANDERE ─────────
      Hinter einer Spotify-Playlist steht eine Liste, die sich aendert, wenn
      ein ERWACHSENER sie aendert. Hinter einer ARD-Sendung steht eine Liste,
      die sich VON SELBST aendert: Kommt eine neue Folge dazu und ist
      „neueste zuerst" eingestellt (die Vorgabe), ruecken alle anderen um eins
      nach hinten. Eine gemerkte Warteschlangennummer zeigt am naechsten Tag
      in eine Folge, die das Kind nie gehoert hat.

      Das ist dieselbe Falle wie [[weiterhoeren-playlist-position]] — nur
      schnappt sie hier ohne Zutun zu, und deshalb genuegt keine Marke wie
      `resumeVersatzGeprueft`. Gemerkt wird die KENNUNG der Folge
      (`resumeardfolge`), und beim Fortsetzen wird sie in der FRISCH geholten
      Liste wiedergefunden. `nummerVerlaesslich()` weist eine ARD-Stelle ohne
      Kennung ab: sie ist nicht ungenau, sondern unbrauchbar.

      ── ZWEI AUFLOESUNGEN IN ENTGEGENGESETZTE RICHTUNGEN ──────────────
      Sie stehen jetzt nebeneinander in `POST /api/weiterhoeren`:

        spotify  Die Maschine sagt, WELCHES Stueck laeuft (`item.uri`);
                 gesucht ist die Position.   -> `zusammenhangFuer`
        ard      mpv sagt die POSITION (`currentMeta.currentTracknr`);
                 gesucht ist, welche FOLGE das ist. -> `ardFolgeAnNummer`

      Beide liefern denselben Typ (`Zusammenhang`), beide merken im Zweifel
      GAR NICHTS, und beide antworten dann `nichtAufloesbar` statt
      `nichtMerkbar` — der Unterschied ist keine Kosmetik: „nichtMerkbar"
      heisst „hier gibt es grundsaetzlich nichts zu merken" (Radio, Jellyfin),
      hier gibt es sehr wohl etwas, es liess sich nur gerade nicht ermitteln.

      ── WOHER DIE REIHENFOLGE KOMMT, OHNE EINEN ZWEITEN ABRUF ─────────
      `/inhalt` hat die geordnete Folgenliste Sekunden vor dem Abspielen in
      der Hand — daraus baut die Oberflaeche die mpv-Warteschlange. Genau dort
      wird sie gemerkt (`ardFolgenSpeicher`, eine Stunde frisch, wie der
      Spotify-Versatzspeicher). Die Meldung alle 15 s kostet damit KEINEN
      Netzabruf. Nach einem Serverneustart wird einmal geholt.
      NUR DIE KENNUNGEN, NIE DIE TONADRESSEN: die laufen ab
      ([[ard-audiothek-schnittstelle-a1]]), und ein Zwischenspeicher voller
      abgelaufener Adressen waere genau die Falle, gegen die das ganze Modul
      gebaut ist.

      ── DIE WARTESCHLANGE IST IMMER DIE GANZE SENDUNG ─────────────────
      Naheliegend waere, beim Fortsetzen erst ab der gesuchten Folge
      anzuhaengen — dann braeuchte es keinen Sprung. Das geht schief: mpv
      zaehlt `currentTracknr` in SEINER Warteschlange. Faengt die bei Folge 7
      an, meldet mpv eine „1", und der Server schriebe beim naechsten Merken
      die falsche Folge nach resume.json. `ardSendungAb()` haengt deshalb
      IMMER die ganze Liste an und springt hinterher mit `tracknr:`
      (`titelUndStelle`, wartet auf `totalTracks >= n`).

      ── WAS DIE OBERFLAECHE DAVON ZEIGT ───────────────────────────────
      Eine ARD-Kachel KLAPPT AUF, wie eine Spotify-Playlist — Tipp oeffnet,
      Play-Knopf startet alles. Es gibt aber nur EINE Ebene: eine Sendung
      zerfaellt nicht in Alben ([[lanes-playlist-alben-titel]] hat drei), sie
      IST die Folgenreihe. `folgenOeffnen()` fuellt die Lane aus `/inhalt`;
      gezeichnet wird mit demselben `titelListeBauen` wie ueberall, damit
      keine zweite Kachelbauart entsteht (und damit keine zweite Stelle, an
      der jemand die Marke „spielt gerade" nachziehen muesste).
      `/api/werke/<s>/alben` bleibt bei `keinePlaylist` — eine zweite
      ARD-Abfrage dort waere eine zweite Stelle, an der die Verweildauer zu
      bedenken ist, und die zweite vergisst man.

      DER BLAUE KNOPF HAENGT AN DER KENNUNG, nicht am Platz: `/inhalt` traegt
      je Folge ein `weiterAb` mit der Nummer aus DIESER Liste. Steht die
      gemerkte Folge inzwischen woanders, faerbt trotzdem die richtige Kachel.
      Und die Stelle darin ist PROZENT, nicht Millisekunden — eine ARD-Folge
      laeuft ueber mpv ([[resume-lokal-ist-prozent-nicht-sekunden]]).

      ── WAS DIE KLASSISCHE OBERFLAECHE NICHT ZEIGT ────────────────────
      Sobald die neue Oberflaeche ARD-Stellen schreibt, stehen in resume.json
      Eintraege mit `type: 'ard'`. In `media.service.ts` fielen sie durch die
      ganze iif-Kette bis auf `of([item])` und ergaeben EINE Kachel, hinter
      der nichts steckt: `saveResumeFiles` nimmt `abspielbefehl` ausdruecklich
      weg (damit keine ablaufende Adresse in eine Datei geraet), und
      `media-provider.ts` reicht bei `ard` genau dieses Feld durch. Die
      klassische Weiterhoeren-Liste laesst ARD-Stellen deshalb aus — dieselbe
      Entscheidung wie bei Jellyfin: eine Kachel, die „weiter" verspricht und
      von vorn anfaengt, ist schlimmer als keine.

      ── EIN NEUNTER VERBRAUCHER IN dienste-deckung.mjs ────────────────
      Die Aufzaehlung in `istWeiterhoerbar()` ist genau die Sorte Stelle, gegen
      die [[dienstliste-an-fuenf-orten]] gebaut ist — und ihr Fehlen ist die
      STILLSTE von allen, weil es aussieht wie ein ERGEBNIS: eine
      Weiterhoeren-Reihe ohne die Sendung heisst genauso gut „da war noch
      nichts". Gegengeprueft wie die drei Verbraucher vom 04.08.: `ard`
      herausgenommen -> Schritt rot, wieder eingesetzt -> gruen.

      ── DIE ATTRAPPE LOG ZUM SIEBTEN MAL DURCH WEGLASSEN ──────────────
      `tools/neu-vorschau.mjs` lieferte bei `ard` unter `/inhalt` kein `id`
      und kein `weiterAb`, und in `/api/weiterhoeren` gab es gar keine
      ARD-Zeile. Jedes Werkzeug haette eine aufgeklappte Lane gesehen und die
      Sache fuer geprueft gehalten — waehrend ein Tipp darin wortlos beim
      Anfang gelandet waere. [[attrappe-luegt-durch-weglassen]] ist damit um
      einen Fall reicher, und es ist derselbe Dienst wie beim sechsten.

      ── UND WARUM `ard` WEITER NICHT VERSCHMILZT ──────────────────────
      Die Begruendung von E4/A4 (Commit 7e3fd3d5) wurde beim Bau nachgeprueft
      und TRAEGT — die Arbeit an der Folgenliste hat sie sogar erhaertet,
      siehe [[quellen-verschmelzen]] und den Kommentar an
      `QUELLEN_REIHENFOLGE`. Neu dazu kommt ein DRITTES Argument: `quellen[0]`
      entscheidet nicht nur, WOMIT gespielt wird, sondern auch, in welcher
      Einheit die gemerkte Stelle herausgeht (`fortsetzenMit`). Die Einheit
      rechnet der Server um — die FOLGENKENNUNG aber ist ausserhalb der ARD
      bedeutungslos. Eine verschmolzene Kachel, deren bevorzugte Quelle
      wechselt, verloere ihre Stelle also STILL.
    related:
      - ard-anschluss-an-beide-oberflaechen
      - ard-audiothek-schnittstelle-a1
      - weiterhoeren-playlist-position
      - weiterhoeren-reihe-neue-oberflaeche
      - lanes-playlist-alben-titel
      - resume-lokal-ist-prozent-nicht-sekunden
      - attrappe-luegt-durch-weglassen
      - quellen-verschmelzen

  - id: offline-liste-jq-klammer
    kind: falle
    sev: high
    title: "Ohne Internet zeigte die Box GAR NICHTS — eine fehlende jq-Klammer seit Februar 2024"
    match: "offline_data|offline_resume|check_network|get_network|jq syntax error|nichts mehr zu sehen ohne Netz|leere Bibliothek offline"
    cause: "In scripts/mupibox/check_network.sh stand der jq-Filter fuer offline_data.json sechsmal im Klartext — und mit einer fehlenden schliessenden Klammer: `select(.type != \"radio\" | select(.type != \"rss\")`. jq bricht mit Syntaxfehler ab und schreibt nichts; das Skript setzt `[` und `]` trotzdem drumherum, also entsteht `[]`."
    fix: "Der Ausdruck steht in beiden Skripten (check_network.sh und get_network.sh) jetzt EINMAL als `NUR_OHNE_NETZ`, geheilt und um `ard` ergaenzt. `tools/dienste-deckung.mjs` haelt beide gegeneinander und gegen die Dienstliste; es liest ausdruecklich die VARIABLE, wird also rot, wenn der Ausdruck wieder an die Aufrufstellen wandert."
    tags: ['offline', 'bash', 'jq', 'ausrollweg', 'falle', 'dienste', 2026]
    source: "04.08.2026 beim Anschliessen der ARD; jq-Fehler am Arbeitsplatz nachgestellt, offline_data.json an Box .169 nur lesend angesehen."
    body: |
      DER FEHLER, nachgestellt:

        $ jq '.[] | select(.type != "spotify") | select(.type != "radio" | select(.type != "rss")' < data.json
        jq: error: syntax error, unexpected end of file, expecting ';' or ')'
        jq: 1 compile error
        (Rueckgabe 3, KEINE Ausgabe)

      Das Skript schreibt trotzdem:

        echo -n "["                >  offline_data.json
        echo -n $(jq '…' < data)   >> offline_data.json     # nichts
        echo -n "]"                >> offline_data.json

      Ergebnis: `[]`. Und `active_data.json` ist im Offline-Zustand ein
      symbolischer Verweis genau darauf — die Oberflaeche zeigt dann eine
      LEERE Bibliothek. Nicht „Spotify fehlt", sondern nichts: auch keine
      lokalen Aufnahmen und keine Jellyfin-Alben, also ausgerechnet das, was
      ohne Internet noch spielt.

      WARUM ES ZWEI JAHRE NIEMANDEM AUFFIEL: `get_network.sh` schreibt
      DIESELBE Datei mit dem RICHTIGEN Ausdruck und ist meistens zuerst dran.
      An .169 nachgesehen (nur lesend, 04.08.2026): offline_data.json
      enthaelt zwei Jellyfin-Alben und traegt den Zeitstempel von data.json —
      die heile Fassung. Der Fehler wartet auf den Lauf, bei dem
      check_network.sh zuerst kommt (Wechsel nach offline, nachdem data.json
      neuer als offline_data.json wurde).

      DAS IST DIE LEHRE, nicht die Klammer: derselbe Ausdruck stand ZEHNMAL
      im Klartext (sechsmal hier, viermal dort). Wer ihn an einer Stelle
      repariert, hat neun uebrig — und dass zwei Skripte dieselbe Datei mit
      VERSCHIEDENEN Filtern schreiben, faellt an keiner einzelnen Stelle auf.
      Dieselbe Bauart wie [[dienstliste-an-fuenf-orten]], nur in bash.

      UND DER ANLASS, aus dem es gefunden wurde: `ard` fehlte in der Liste.
      Eine ARD-Kachel waere ohne Netz stehengeblieben und haette beim
      Antippen nichts getan — auf einer Kinderbox der schlechteste Fall, denn
      ein Kind sieht das Bild und drueckt.

      WAS OHNE NETZ BLEIBT, und warum (die Einordnung steht in
      tools/dienste-deckung.mjs, jeder Dienst mit Begruendung):
        faellt weg  spotify, radio, rss, ard
        bleibt      lokal (liegt auf der Box), jellyfin (eigenes Netz, ohne
                    Internet weiter erreichbar), anderes
      `anderes` bleibt mit Absicht: ihn wegzunehmen hiesse, einem Menschen
      seinen eigenen von Hand eingetragenen Eintrag zu verstecken, ohne zu
      wissen, ob er ueberhaupt Netz braucht.
    related:
      - dienstliste-an-fuenf-orten
      - leere-bibliothek-ohne-check-internet-dienst
      - wiedergabe-startet-nicht-weil-app-sich-fuer-offline-haelt
      - pruefen-schritte-die-still-luegen

  - id: kachel-sieht-richtig-aus-und-spielt-nicht
    kind: falle
    sev: high
    title: "Die Kachel war in jeder pruefbaren Eigenschaft richtig — und tat beim Antippen nichts"
    match: "Das kann die Box hier noch nicht abspielen|abspielBefehl|switch \\(q.dienst\\)|default: return null|Kachel tut nichts|neue Oberflaeche spielt nicht"
    cause: "`abspielBefehl()` in NewDesign/app.js ist ein `switch (q.dienst)` mit Zweigen fuer lokal, spotify, radio, rss, jellyfin — und einem `default: return null`. Ein neuer Dienst faellt dort hinein. Der Rueckfall in `ausQuelleSpielen` (Weg ueber `albumSpielen`, wenn es keinen Ein-Befehl-Start gibt) war auf `dienst === 'jellyfin' && art === 'album'` eingeschraenkt und half ihm nicht."
    fix: "Fuer Dienste ohne Ein-Befehl-Start einen AUSDRUECKLICHEN `case` mit `return null` schreiben (nicht dem `default` ueberlassen) und sie im Rueckfall von `ausQuelleSpielen` nennen. Gehalten wird es von `tools/kachel-spielt-je-dienst.mjs` (Schritt „Jede Kachel spielt\" in tools/pruefen.sh): es tippt je Dienst auf eine Kachel und liest MIT, was auf die Leitung geht."
    tags: ['oberflaeche', 'neue-oberflaeche', 'kacheln', 'dienste', 'werkzeug', 'falle', 'ard', 'e4', 2026]
    source: "04.08.2026, beim Gegenlesen von BACKLOG E4. In tools/neu-vorschau.mjs gemessen, nicht am Quelltext gedeutet."
    body: |
      DER BEFUND. Im Backlog stand woertlich, `NewDesign/` „zeigt und spielt eine
      ARD-Kachel bereits vollstaendig". Angenommen, nicht gemessen. Gemessen
      antwortete die Kachel:

          ard   MausHoerspiel kurz   (keiner)
                Meldung: „Das kann die Box hier noch nicht abspielen."

      Auf einer Box, die es konnte: `/api/werke/ard:<kennung>/inhalt` liefert je
      Folge einen FERTIGEN `befehl` und `anhaengen`, und der Abspieldienst kennt
      die Verben `ard`/`ardqueue` seit E4/A5.

      ── WARUM KEIN EINZIGER PRUEFSCHRITT DAS FAND ─────────────────────────
      Weil an dieser Kachel JEDE pruefbare EIGENSCHAFT stimmte. Sie stand im
      Raster, hatte Titel, Cover, Art „show", die richtige Plakette (Sendemast)
      und die richtige Farbe (#0f766e). Alle Werkzeuge dieses Hauses pruefen
      genau solche Eigenschaften:

        dienst-marken-schau     Zeichen (app.js) gegen Farbe (app.css)
        dienste-deckung         die Dienstliste gegen ihre Verbraucher
        raster/lane-marke-*     wo die Plakette sitzt und wie gross sie ist

      Keines davon fragt: TUT SIE AUCH ETWAS? Der Unterschied zwischen „geht"
      und „geht nicht" war ein einziger Satz, den nur sieht, wer TIPPT — und auf
      einer Kinderbox tippt zuerst ein Kind.

      DAS IST DIE LEHRE, nicht der fehlende `case`: Eine Eigenschaft laesst sich
      am Quelltext abgleichen, eine WIRKUNG nicht. Fuer die Wirkung muss etwas
      auf die Leitung gehen, und man muss mitlesen. Dasselbe Vorgehen wie in
      [[verschmelzung-abspielen-stufe3]] und
      [[playpause-gibt-es-nicht]].

      ── DIE ATTRAPPE LOG DABEI ZUM SECHSTEN MAL ──────────────────────────
      Nach der Reparatur ging der richtige Befehl hinaus — und die Vorschau
      antwortete 400 „UNBEKANNTER SPIELERBEFEHL". In tools/neu-vorschau.mjs
      stand die Liste der Startverben VON HAND:

          const START = ['library','queue','radio','rss','jellyfin','jfqueue','deletelocal','say']

      `ard` und `ardqueue` fehlten. Das ist die schlimmste Sorte Attrappenluege:
      sie sagt NEIN, wo die Box JA sagt, und schickt damit jemanden auf die
      Suche nach einem Fehler, den es nicht gibt. Behoben, indem die Liste jetzt
      aus dem Verteiler (`spotify-control.ts`) GELESEN wird — dieselben zwei
      Formen, die auch tools/player-befehle.py liest. Fehlt der Verteiler, bricht
      die Vorschau beim Start ab; eine zweite Liste oder ein „alles erlauben"
      waere beides schlechter. Siehe [[attrappe-luegt-durch-weglassen]].

      ── WAS SONST NOCH UNGEDECKT WAR ──────────────────────────────────────
      Mit einer Gegenprobe je Stelle erhoben (`ard` herausnehmen, alle
      Pruefschritte laufen lassen, wieder einsetzen). Drei waren GRUEN und sind
      jetzt Verbraucher in tools/dienste-deckung.mjs:

        server.ts        dienstSuche()        die Verwaltung faende die ARD
                                              nicht mehr, ohne eine Meldung
        medien.ts (adm)  dienstSchluessel()   die Bibliothek naennte jeden
                                              ARD-Eintrag „Anderes"
        media.service.ts der Typ-Zweig        die Kachel fiele in der
                                              klassischen Oberflaeche wieder
                                              STILL heraus — die Stelle, um die
                                              es in E4/A8 ueberhaupt ging

      Rot waren dagegen: der Rueckgabetyp von `dienstVon` (Deckung + Tests),
      `streaming.ts` (streaming.spec), `artVon` in werke.ts (Tests), der
      ard-Zweig von `/inhalt` (ard.integration.spec), DIENST_NAME, das
      Auswahlfeld der Suche (Suchbestand-Test), media-provider.ts, app.js,
      app.css und beide Netz-Skripte.
    related:
      - dienstliste-an-fuenf-orten
      - ard-anschluss-an-beide-oberflaechen
      - attrappe-luegt-durch-weglassen
      - dienst-zeichen-auf-den-kacheln
      - playpause-gibt-es-nicht
      - pruefen-schritte-die-still-luegen
      - arbeitsweise-werkzeug-statt-wegwerfbefehl
      - verschmelzung-abspielen-stufe3

  - id: merker-den-eine-anfrage-setzt-steht-beim-aufschlagen-noch-nicht
    title: "Ein Merker, den eine Anfrage setzt, steht beim Aufschlagen der Seite noch nicht"
    kind: falle
    sev: mittel
    match: "boxIstVerbunden|Merker.*Anfrage|Seite entscheidet zu frueh|fire-and-forget.*Startzustand|zeigt Anmeldeformular obwohl angemeldet"
    cause: "Der Anstoss geschieht beim Start OHNE Abwarten; die Seite liest den Merker im selben Atemzug, in dem die Anfrage hinausgeht"
    fix: "Die Seite die ANTWORT abwarten lassen. Die laufende Anfrage zurueckgeben, damit Anstosser und Abwarter sich eine teilen"
    tags: [oberflaeche, spotify, falle, zustand, e15, 2026]
    source: "beim Gegenlesen von BACKLOG E15, 2026-08-04 — behoben in spotify.page.ts / spotify-auth.service.ts"
    body: |
      GEFUNDEN AN EINER STELLE, DIE AUSDRUECKLICH GEGEN EINE TEURE
      VERWECHSLUNG GEBAUT WAR. Seit E15/S4 gibt `/api/spotify/config` die
      Zugangsdaten nicht mehr heraus; ein Browser ohne eigene Anmeldung hat
      also nichts im Speicher. Damit die Einrichtungsseite eine ANGEMELDETE
      Box nicht als abgemeldet zeigt (jemand meldet sie sonst ein zweites Mal
      an, und das geht NUR an der Box), fragt sie die Box:

          if (!c && this.sp.boxIstVerbunden()) { …verbunden… }

      DER ZWEIG WAR DA UND GRIFF NIE. `boxIstVerbunden()` liest ein Feld, das
      `ensureBoxConfig()` setzt — angestossen in `app.component` beim Start,
      OHNE Abwarten. Die Antwort braucht einen Netzweg; `ionViewWillEnter`
      laeuft davor. Das Feld stand also immer auf „nein".

      DAS TUECKISCHE IST, WO ES AUFFAELLT UND WO NICHT:

          Kiosk               man tippt sich durch — die Antwort ist
                              laengst da, die Seite sieht RICHTIG aus
          https://<box>:8443/spotify   unmittelbar aufgeschlagen, der
                              vorgesehene Einrichtungsweg — FALSCH

      Am Geraet erprobt haette es also bestanden und ausgerechnet im
      Ernstfall versagt.

      DIE REGEL DARAUS: Ein Zustand, der aus einer Anfrage stammt, hat ZWEI
      Werte mehr als gedacht — „noch nicht gefragt" und „Frage gescheitert".
      Beide sehen aus wie „nein". Wer ihn synchron liest, liest im
      Zweifel den falschen. Entweder wartet der Leser auf die Antwort, oder
      der Wert muss die drei Faelle unterscheiden koennen.

      WAS AN DIE STELLE KAM: `ensureBoxConfig()` gibt die LAUFENDE Frage
      zurueck, statt jedem Frager eine neue zu stellen; die Seite wartet sie
      ab. Der Merker faellt nach dem Ende weg — ein Start ohne erreichbare Box
      darf nicht bis zum Neuladen „nicht eingerichtet" heissen.

      GEPRUEFT AN DEM, WAS AUF DEM BILDSCHIRM STEHT, nicht an einem Feld: der
      Schaden entsteht beim Hinsehen. Neben jedem „Die Box ist angemeldet"
      steht deshalb ein „und KEIN Anmeldeformular" — ohne das koennten beide
      Zweige nebeneinander stehen und der Test waere trotzdem gruen.
    related:
      - spotify-zugang-bleibt-auf-der-box
      - spotify-einrichten-ueber-https-remote-setup
      - attrappe-luegt-durch-weglassen

  - id: jellyfin-schluessel-kommt-vom-ERSTEN-eintrag-der-liste
    title: "Der Jellyfin-Schluessel der Box kommt vom ERSTEN Eintrag der Medienliste"
    kind: falle
    sev: mittel
    match: "jellyfinZugangAusListe|alle Jellyfin-Cover.*leer|api_key.*veraltet|Jellyfin-Schluessel gewechselt"
    cause: "In mupiboxconfig.json steht `jellyfin.server`, aber KEIN `apiKey` — der Server nimmt den Schluessel aus dem ersten jellyfin-Eintrag der Medienliste"
    fix: "Beim Schluesselwechsel die Coveradressen der Bestandseintraege mitziehen, sonst vergiftet der erste Eintrag alle uebrigen"
    tags: [jellyfin, sicherheit, konfiguration, e15, 2026]
    source: "am 2026-08-04 an der Box gemessen: GET /api/config -> jellyfin: {server: …}, kein apiKey-Feld"
    body: |
      `GET /api/bild/jellyfin/:kennung` und `GET /api/jellyfin/strom/:kennung`
      (BACKLOG E15/S1+S2) brauchen Server UND Schluessel. Beides holt der
      Server aus `jellyfinZugangAusListe(medien)` — dem ERSTEN Eintrag mit
      `dienst == jellyfin`, dessen Coveradresse ein `api_key=` traegt.

      WARUM NICHT AUS DER KONFIGURATION: dort steht er nicht. Am 04.08.2026
      an der Box gemessen enthaelt die `jellyfin`-Gruppe von
      /etc/mupibox/mupiboxconfig.json NUR `server` — das Feld `apiKey` fehlt
      ganz (`ohneGeheimnisse` leert nur vorhandene Felder, und es kam keins
      zurueck). Die Medienliste ist also die einzige serverseitige Quelle.
      Das ist kein Versaeumnis dieser Wege, sondern ihre Lage.

      NACHTRAG 22.08.2026: Die Konfiguration traegt inzwischen `apiKey`
      (QuickConnect legt ihn dort ab, am Geraet gemessen). Suche, Titelliste
      und Objektart-Frage lesen ihn seither ueber das Plugin aus
      kontext.konfig [jellyfin-zieht-ins-plugin]; NUR die hier beschriebenen
      Kernwege (Bild-/Strom-Weiterleitung) klauben weiter aus der Liste —
      fuer sie gilt die Falle unveraendert.

      DIE FALLE: Der erste Eintrag entscheidet fuer ALLE. Wer in Jellyfin
      einen neuen API-Schluessel erzeugt und den alten zurueckzieht, hat
      danach Bestandseintraege mit dem ALTEN Schluessel in der Coveradresse.
      Steht einer davon vorn, gehen ALLE Jellyfin-Cover leer aus — auch die
      Eintraege, die danach mit gueltigem Schluessel angelegt wurden. Vorher
      (die Adresse ging unmittelbar an Jellyfin) verlor nur der Eintrag sein
      Bild, dessen eigener Schluessel abgelaufen war.

      WORAN MAN ES ERKENNT: nicht an einer Meldung. `/api/bild/jellyfin/…`
      antwortet mit „kein Cover hinterlegt" — dem NORMALFALL fuer ein Stueck
      ohne Bild. Die Kacheln zeigen dann ihren Anfangsbuchstaben, und das
      sieht nach einer Jellyfin-Eigenheit aus, nicht nach einem falschen
      Schluessel.
    related:
      - jellyfin-ueber-die-box-statt-schluessel-im-browser
      - zugangsschluessel-im-browser
      - mupi-jellyfin-anmeldung-nur-im-browser

  - id: kiosk-hat-keine-unit
    kind: falle
    sev: high
    title: Der Kiosk hat keine Unit — OnFailure faengt den schwarzen Schirm NIE
    match: OnFailure|Kiosk startet nicht|Bildschirm bleibt schwarz.*Dienste laufen|kein Dienst ist failed
    tags:
    - boot
    - kiosk
    - systemd
    - sichtbarkeit
    - chromium
    cause: 'Chromium startet aus chromium-autostart.sh ueber den Autologin von getty@tty1. Bleibt es
      aus, ist fuer systemd nichts geschehen: getty laeuft, die Sitzung laeuft, KEIN Dienst ist ''failed''.
      Es gibt keine Unit, an die sich OnFailure haengen liesse.'
    fix: Ein Waechter, der nicht systemd fragt, sondern `pgrep -f '[c]hromium'` — scripts/mupibox/mupibox-kioskwache.py
      mit Zeitgeber (BACKLOG E11c/S4)
    body: "WARUM DAS SO LEICHT UEBERSEHEN WIRD: Man baut das Fehlerbild ueber\n`OnFailure=` (E11c/S3),\
      \ prueft es an einem Dienst, sieht es erscheinen —\nund haelt den schwarzen Schirm damit fuer\
      \ abgedeckt. Ist er nicht. Der\nhaeufigste schwarze Schirm hat gar keinen gescheiterten Dienst.\n\
      \nZWEI LUECKEN, und beide haben dieselbe Form:\n  1. Der KIOSK: kein Dienst, sondern eine Anmeldesitzung.\n\
      \  2. Die ANWENDUNG: im Repo achim/box laeuft sie unter pm2 (autosetup.sh:\n     `pm2 start server`,\
      \ `pm2 save`) und hat ebenfalls keine Unit. Auf den\n     Boxen im Haus dagegen SCHON — mupibox-server.service\
      \ und\n     mupibox-player.service kommen vom remote-step-installer. Wer nur an\n     einer laufenden\
      \ Box nachsieht, haelt die Units fuer selbstverstaendlich\n     und wundert sich, dass eine frische\
      \ Karte sie nicht hat.\n\nDIE FOLGERUNG FUER DAS FEHLERBILD: Es darf nicht ueber Zeilen in\nconfig/services/\
      \ angehaengt werden, sondern als Zusatzstueck (drop-in)\nneben die Unit — nur so erreicht es auch\
      \ Units, die dieses Repo gar nicht\nliefert (scripts/mupibox/fehlerbild-anhaengen.sh).\n\nUND\
      \ DIE WICHTIGSTE ZURUECKHALTUNG: Laeuft Chromium, malt das Fehlerbild\nNICHT. Ein Dienst kann\
      \ Stunden nach dem Start scheitern; ein Bild ueber die\nlaufende Oberflaeche zu legen macht aus\
      \ einem kleinen Fehler einen grossen —\nder Kiosk waere weg, die Musik liefe weiter, und ein Kind\
      \ kaeme nicht zurueck."
  - id: fehlerbild-uebermalt-von-der-boot-animation
    kind: falle
    sev: high
    title: Wer nur EINMAL malt, verliert gegen den, der 20-mal je Sekunde malt
    match: Framebuffer|/dev/fb0|Fehlerbild|Boot-Animation|OnFailure.*Start|Bild erscheint nicht
    tags:
    - boot
    - framebuffer
    - sichtbarkeit
    - systemd
    - e11c
    cause: 'Zwei Programme schreiben denselben Framebuffer. mupibox-fehlerbild.py malt GENAU EINMAL
      und laesst das Bild dann stehen; mupibox-boot-splash.py malt 20-mal je Sekunde (BILD_PRO_S).
      Laufen beide, ist das Fehlerbild nach 50 ms ueberschrieben und kommt NIE WIEDER — der Prozess
      haelt danach eine Viertelstunde einen Schirm, auf dem das Bild der ANIMATION steht.'
    fix: 'mupibox-fehlerbild.py wartet, solange `pgrep -f ''[m]upibox-boot-splash.py'' trifft
      (darf_malen(), Deckel 150 s), und prueft dabei weiter auf Chromium — dieselbe Regel, die
      mupibox-kioskwache.py von Anfang an hatte'
    source: "beim Gegenlesen von E11c am 2026-08-04 am Quelltext gefunden, bevor irgendetwas je gelaufen ist"
    body: |
      WARUM DAS KEIN SONDERFALL IST, SONDERN DER NORMALFALL:
      Die Animation laeuft bis zu 120 s. Das brauchbare Bild kommt nach
      gemessenen 19,4 bis 23,8 s (tools/grundmessung.py, 04.08.2026). Ein
      Dienst, der beim STARTEN scheitert, scheitert also mitten in der
      Animation — `mupibox-server.service` etwa faellt mit
      Restart=on-failure/RestartSec=2 nach den systemd-ueblichen fuenf
      Versuchen in rund 10 s auf "failed". Chromium ist dann noch nicht da,
      also zieht auch die Zurueckhaltung "laeuft der Kiosk, malen wir nicht"
      nicht. Das Fehlerbild fiel damit ausgerechnet in dem Fenster aus, fuer
      das es gebaut wurde.

      DIE FORM DES FEHLERS — sie kehrt wieder, sobald ein zweites Programm
      auf /dev/fb0 zeichnet: Es geht nicht um "wer darf", sondern um WIE OFT
      jeder malt. Ein Standbild verliert immer gegen eine Schleife. Wer eine
      neue Anzeige auf den Framebuffer legt, muss deshalb zwei Fragen
      beantworten: male ich einmal oder dauernd, und wer malt sonst noch?

      WAS NICHT DAS PROBLEM WAR (nachgesehen, damit es niemand zweimal
      untersucht): das doppelte Abkoppeln der Framebuffer-Konsole. KonsoleAus
      koppelt nur ab, was gerade gebunden IST (`bind` == "1"), und bindet nur
      zurueck, was es selbst abgekoppelt hat. Der zweite Prozess merkt sich
      also nichts und bindet beim Ende auch nichts unter dem ersten wieder an.

      DIE ZWEITE SICHERUNG AN DERSELBEN STELLE: `RuntimeMaxSec=` in der Unit
      raeumt den Prozess ueber SIGTERM ab, und bei SIGTERM laeuft in Python
      KEIN `finally` — die Framebuffer-Konsole bliebe abgekoppelt und der
      Schirm stumm bis zum Neustart. Am 04.08.2026 nachgestellt: ohne
      Handler kein `finally`, mit `signal.signal(SIGTERM, lambda *_:
      sys.exit(0))` laeuft es. Beide Skripte haben den Handler jetzt, und die
      Anzeigefrist des Fehlerbilds laeuft ab PROZESSSTART (Wartezeit
      eingerechnet), damit sie unter RuntimeMaxSec bleibt.
    related:
    - kiosk-hat-keine-unit
    - boot-schwarz-19-bis-24-sekunden
    - angelegt-ist-nicht-eingeschaltet
  - id: angelegt-ist-nicht-eingeschaltet
    kind: falle
    sev: medium
    title: Angelegt ist nicht eingeschaltet — die dritte Etage der Ausrollweg-Frage
    match: systemctl enable|Unit laeuft nicht|liegt in /etc/systemd/system|frische Karte
    tags:
    - ausrollweg
    - systemd
    - installer
    - werkzeug
    cause: Eine Unit kann sauber unter /etc/systemd/system/ liegen und trotzdem nie laufen, weil kein
      Ausrollweg `systemctl enable` sagt. Vor der Box ist das nicht zu unterscheiden von 'gar nicht
      da'.
    fix: 'tools/ausrollweg-deckung.py, Abschnitt 4 (Liste EINGESCHALTET_NOETIG) — dazu die Gegenrichtung:
      `systemctl enable` auf eine Unit, die keine frische Karte hat, laeuft ins Leere und meldet nichts'
    body: "DREI ETAGEN DERSELBEN FRAGE, alle drei am 04.08.2026 einzeln bezahlt:\n  1. Kommt die Datei\
      \ ueberhaupt auf die Box?   (librespot-waechter, Luefter)\n  2. Wird sie AUFGERUFEN?         \
      \             (dhcp-schneller.sh)\n  3. Wird die Unit EINGESCHALTET?              (Boot-Animation,\
      \ E11c/S2)\n\nDIE STELLE, AN DER SICH DER LESER ENTSCHEIDET, ist die Schleife in\nautosetup.sh:\n\
      \n    for service in mupi_wifi mupi_check_internet …; do\n        systemctl enable ${service}.service\n\
      \nSie haengt `.service` an JEDEN Eintrag an. Wer einen TIMER hineinschreibt,\nlaesst systemd nach\
      \ `mupibox-kioskwache.timer.service` suchen — die gibt es\nnicht, und `systemctl enable` sagt\
      \ dazu nichts, was in `>&3` noch auffiele.\nIn autosetup.sh steht dazu seit langem ein Kommentar;\
      \ ein Kommentar ist\nkeine Pruefung.\n\nWIE MAN DEN LESER FALSCH BAUT (hier passiert): Die erste\
      \ Fassung ergaenzte\n`.service` nur, wenn im Namen kein Punkt stand — und meldete genau diese\n\
      Falle als in Ordnung. Ein Werkzeug, das den Fehler nachbaut, den es sucht,\nist keines. Richtig\
      \ ist, die Schleife WOERTLICH nachzubilden und im Rumpf\nnachzusehen, was sie anhaengt.\n\nGEGENPROBE,\
      \ die dieses Werkzeug seither bestehen muss (in einer Kopie des\nBaums fahren, jede Zeile muss\
      \ ROT werden):\n  * das `systemctl enable` der Unit entfernen\n  * den Timer in die .service-Schleife\
      \ schreiben\n  * den Aufruf des Einrichtungsskripts in einem der Wege streichen\n\nWAS AUCH DIE\
      \ DRITTE ETAGE NICHT SIEHT: einen VIERTEN Weg — die Karte selbst\n(scripts/make-boot-sd.sh, config/templates/dietpi.txt).\
      \ Dort sass zram/B7:\ndie Vorlage trug den richtigen Schluessel, und kein Weg brachte sie auf\
      \ eine\nKarte. Das Werkzeug kennt nur autosetup.sh und update/start_mupibox_update.sh."
  - id: boot-schwarz-19-bis-24-sekunden
    kind: note
    title: 'Gemessen: 19 bis 24 s SCHWARZ beim Start — es gibt kein leeres Zwischenbild'
    source: >
      DOPPELUNG, festgestellt 08.08.2026: dieselbe Messung (vier Kaltstarts,
      04.08.2026, tools/grundmessung.py) steht ein zweites Mal in
      [[grundmessung-zeit-bis-zum-bild]], dort ausfuehrlicher und mit den
      Geraetedaten. DIESER Eintrag ist der kurze Auszug fuer die
      Sichtbarkeits-Arbeit. Wer die Zahlen neu misst, muss BEIDE anfassen —
      sonst stehen ab dann zwei Wahrheiten im selben Pack, und die zweite
      findet nur, wer nach dem richtigen Wort sucht. Messung: am Geraet
      (Pi 5 / DietPi Trixie, 04.08.2026, vier Kaltstarts,
      tools/grundmessung.py, Commit 7bb092fd).
    related:
      - grundmessung-zeit-bis-zum-bild
      - boot-animation-mit-echtem-fortschritt
      - kiosk-hat-keine-unit
    body: "tools/grundmessung.py, vier Kaltstarts am 04.08.2026 (Pi 5 / DietPi Trixie,\n2 GB, WLAN).\
      \ Die Zahlen, an denen jede Sichtbarkeits-Arbeit haengt:\n\n    ~5   s   Kernel + Dienste, bis\
      \ das Backend auf :8200 hoert (4,86/5,03/5,57)\n    4-9  s   WARTEN AUF DHCP UEBER WLAN — fuer\
      \ den Kiosk passiert nichts\n    ~1   s   getty-Autologin und chromium-autostart.sh\n    ~9  \
      \ s   Chromium vom Prozessstart bis zum fertigen Bild (8,37/8,82/9,34)\n    ─────\n    19,4 bis\
      \ 23,8 s bis zum Bild\n\nDIE ERWARTUNG WAR FALSCH, UND ZWAR ZUGUNSTEN DER BOX. Der Verdacht war,\n\
      Chromiums `--default-background-color=44afe2ff` male sofort eine blaue\nFlaeche und man freue\
      \ sich zu frueh. Gemessen ist das Gegenteil: der Schirm\nist SCHWARZ (heller Anteil 0,000) und\
      \ steht eine Abtastung spaeter (+0,3 bis\n+0,4 s) fertig da, mit ueber 11 000 Farben. Erstes Bild\
      \ und brauchbarer\nInhalt fallen zusammen.\n\nWAS DARAUS FOLGT: Die Frage aus E1/M3 ('wie lange\
      \ zwischen erstem Bild und\nbrauchbarem Inhalt?') ist gegenstandslos. Die Zahl, die zaehlt, sind\
      \ die 19\nbis 24 Sekunden Schwarz DAVOR — und die fuellt man nicht durch Optimieren,\nsondern\
      \ indem man in dieser Zeit etwas zeigt (BACKLOG E11c/S2).\n\nDER GROESSTE SINNLOSE POSTEN darin:\
      \ getty@tty1 wird konsequent 0,07 s nach\nifup@wlan0 aktiv (8,83->8,90, 11,81->11,90, 14,13->14,21).\
      \ Der Kiosk wartet\nalso auf eine WLAN-Adresse, obwohl die Seite, die er zeigt, unter\nhttp://localhost:8200\
      \ liegt. Entschaerft durch dhcp-schneller.sh (E5/B1,\n4,96 s gemessen; am Pi 4 brachte dieselbe\
      \ Aenderung 31,8 s -> 13,2 s)."
  - kind: falle
    id: anlauf-nachsehen-statt-warten
    title: "Der Sprung in die gemerkte Stelle wartet auf den ZUSTAND, nicht auf die Uhr"
    tags: ['mpv', 'weiterhoeren', 'seekpos', 'messen', 2026]
    source: "gebaut und am Geraet gemessen (2026-08-04)"
    body: |
      In `NewDesign/app.js` standen zwei geratene Zahlen: nach dem Albumstart
      2000 ms warten, `tracknr:` schicken, 1500 ms warten, `seekpos:` schicken.
      Herkunft laut Kommentar: „die klassische Oberflaeche wartet an derselben
      Stelle 2000 ms — hier abgeschrieben, nicht neu gewaehlt."

      Gemessen ([[mpv-anlauf-wartet-auf-falschen-titel]]) kaufen die 2000 ms
      NICHTS: Warteschlange und Titelsprung stehen nach zehn Millisekunden. Sie
      werden damit verbracht, Titel EINS zu laden, den niemand hoeren will;
      fuer den richtigen bleiben 1500 ms — bei einer Netzquelle zu wenig.

      ── WAS JETZT DASTEHT (`titelUndStelle`) ────────────────────────────
        1. Warteschlange abwarten, bis `totalTracks >= nr` (gemessen 5-7 ms).
        2. `tracknr:` schicken.
        3. Warten, bis der gewaehlte Titel WIRKLICH offen ist.
        4. `seekpos:` schicken, nachsehen, hoechstens zweimal nachfassen.

      ── DER BELEG FUER „TITEL IST OFFEN" IST `duration` ─────────────────
      UND NICHT `currentTracknr`. Das ist die Falle an dieser Stelle: der
      Abspieldienst setzt `currentMeta.currentTracknr = target` beim mpv-Sprung
      SOFORT selbst — lange bevor der Titel geladen ist. Die Zahl wuerde immer
      ja sagen. `duration` steht erst, wenn mpv die Datei offen hat.

      `currentPlayer` wird als Frischemerkmal mitgeprueft: `stop` leert es, der
      Start setzt es. Ohne das koennte die Laenge des VORIGEN Titels als Beleg
      durchgehen — spotify-control raeumt `duration` beim Anhalten nicht weg.

      ── OHNE BELEG WIRD NICHT NACHGEFASST ───────────────────────────────
      Am 04.08. stand auf der Box bei laufender Wiedergabe `duration: null`. Wo
      die Laenge fehlt, ist nicht entscheidbar, ob der Sprung ankam. Blind zu
      wiederholen hiesse, neun Sekunden lang alle 700 ms an dieselbe Stelle
      zurueckzuspringen — UND DAS HOERT MAN. Ohne Beleg bleibt es deshalb bei
      EINEM Sprung: genau der alte Weg, nur ohne die abgesessenen 3,5 Sekunden
      davor. Der schlechteste Fall ist damit so gut wie vorher, der gute
      deutlich besser.

      Dieselbe Form wie [[anhalten-wartet-auf-spotify]]: eine Frist ist eine
      Decke gegen das Haengenbleiben, keine abzusitzende Zeit.

      NACHMESSEN: `node tools/anlauf-am-geraet.mjs --befehl '<abspielbefehl>'`
  - kind: falle
    id: auch-der-abspieldienst-antwortet-200-auf-alles
    title: "Auch der ABSPIELDIENST nimmt jeden Pfad mit 200 — der Content-Type rettet hier nicht"
    tags: ['messen', 'abspieldienst', 'attrappe', 'protokoll', 2026]
    source: "am Geraet gemessen (2026-08-04, Box .169, Port 5005)"
    body: |
      [[server-antwortet-200-auf-alles]] haelt fest, dass der Server (Port 8200)
      auf jeden Pfad 200 antwortet, und nennt den Content-Type als Ausweg: nur
      er unterscheidet eine echte Route von der SPA-Rueckfallseite.

      DIESER AUSWEG TRAEGT BEIM ABSPIELDIENST NICHT. Gemessen auf Port 5005:

        /gibtesnichtxyz    200   application/json; charset=utf-8
        /local             200   application/json; charset=utf-8

      Code und Content-Type sind GLEICH. Ein Auffangbereich nimmt jeden Pfad.
      Es gibt hier also keine Antwort, aus der man lesen koennte, ob ein Befehl
      seinen Zweig ueberhaupt erreicht hat.

      SO PASSIERT (04.08.2026): ein Messwerkzeug schickte einen Jellyfin-Start,
      las `response.ok`, meldete „angenommen" — und stuetzte darauf den Befund
      „der Befehl wurde angenommen und tat nichts". Der Befund war keiner. Was
      wirklich beobachtet war: die Felder in /local aenderten sich nicht.

      WAS ZAEHLT: allein die WIRKUNG. Fuer den Abspieldienst heisst das
      /player/local vorher und nachher vergleichen, oder ins Journal sehen.
      Nicht die Antwort.

      ── ZWEI STOLPERSTELLEN GLEICH DANEBEN ──────────────────────────────
      Die pm2-Protokolle unter /home/dietpi/.pm2/logs/ sind ALT (letzte Zeilen
      vom 29.07.). Die Dienste laufen laengst unter systemd:
      `mupibox-player.service` und `mupibox-server.service`. Wer in den
      pm2-Dateien sucht, liest eine Woche alten Stand und haelt ihn fuer das
      Neueste.

      Und `journalctl -u mupibox-player.service` OHNE sudo antwortet
      „No journal files were opened due to insufficient permissions" — eine
      leere Ausgabe, die wie „nichts passiert" aussieht. Mit `sudo -n` kommt
      alles.

  - id: jellyfin-schluessel-fliesst-vom-browser-in-die-box
    kind: architektur
    title: >-
      Der Jellyfin-Schluessel fliesst BROWSER -> data.json -> BOX, nicht
      umgekehrt — und darum war ensureBoxConfig() tot
    tags: [jellyfin, sicherheit, api, oberflaeche, konfiguration, e15, 2026]
    source:
      - gemessen am 2026-08-04 an der Box 192.168.178.169 (nur gelesen, nichts ausgerollt)
      - tools/jellyfin-verbindung-im-browser-messen.py
      - BACKLOG E15, Abschnitt „Der Schluessel muss die RICHTUNG wechseln"
    body: |
      DIE VERWALTUNG HAT EIN FELD „Jellyfin API-Schluessel". ES TRAEGT NICHTS.
      Im ganzen Baum liest `jellyfin.apiKey` aus der mupiboxconfig.json GENAU
      EINE Stelle — `streaming.ts:124` —, und die rechnet nur einen
      Anzeigestand aus. Kein Cover, kein Ton, keine Titelliste haengt daran.

      WORAUS DIE BOX IHREN SCHLUESSEL WIRKLICH NIMMT: aus den COVERADRESSEN der
      data.json (`jellyfinZugangAusListe` in medien.ts). Dorthin geschrieben
      hat ihn die klassische Oberflaeche, als jemand dort ein Album aufnahm
      (`jellyfin.page.ts` -> `addAlbum` legt `cover` samt `api_key` ab). Die
      Richtung ist also BROWSER -> data.json -> BOX. Man erwartet das
      Gegenteil, und diese Erwartung kostet:

        * „/api/data vom api_key befreien" nimmt der Box ihre EINZIGE Quelle.
          Jellyfin fiele danach ganz aus — nicht nur die Cover.
        * Ein vorn stehender Eintrag mit altem Schluessel nimmt ALLEN Jellyfin-
          Covern das Bild, still. Siehe
          [[jellyfin-schluessel-kommt-vom-ERSTEN-eintrag-der-liste]].
        * Die Seite „Streaming-Dienste" LUEGT auf einer laufenden Box:
          gemessen am 04.08.2026 meldete sie `unvollstaendig` mit „Server
          eingetragen, aber kein API-Schluessel. Jellyfin antwortet dann auf
          nichts" — waehrend Jellyfin auf derselben Box spielte.

      DARAN STARB `ensureBoxConfig()` IN jellyfin.service.ts. Sie holte
      /api/config und saete `{server, apiKey}` in den localStorage, damit die
      boxweite Einstellung auch in der klassischen Oberflaeche gilt. Gemessen
      liefert /api/config aber:

          jellyfin: {"server": "http://192.168.178.199:8899"}

      Kein `apiKey`. ZWEI URSACHEN, DIESELBE WIRKUNG — und sie zu verwechseln
      heisst, die falsche Stelle zu reparieren: auf DIESER Box steht das Feld
      gar nicht erst in der Datei (die Konfiguration ist aelter als das Feld,
      siehe [[board-feld-ohne-schluessel-auf-der-box]]); zusaetzlich WUERDE
      `ohneGeheimnisse()` es leeren, wenn es dort stuende. Der Zweig
      `if (jf?.server && jf?.apiKey)` konnte nicht mehr greifen; uebrig blieb
      der else-Zweig, der bei jedem Start eine frueher gesaete Kopie loeschte.

      ENTSCHIEDEN AM 04.08.2026: `ensureBoxConfig()` ist ERSATZLOS ENTFALLEN,
      nicht repariert. Sie zum Laufen zu bringen hiesse, /api/config den
      Schluessel wieder an jeden im Netz herausgeben zu lassen — genau der Weg,
      den E15 zugemacht hat. Die Oberflaeche braucht ihn nicht, sobald die drei
      Wege ueber die Box laufen. Zwei davon TRAGEN BEREITS, am selben Tag an
      der Box gemessen:

          /api/bild/jellyfin/:kennung    200  image/jpeg  47837 Byte
          /api/jellyfin/strom/:kennung   206  audio/mpeg  bytes 0-1023/5683931
          /api/werke/<s>/inhalt          200  12 Titel — ABER api_key steckt
                                              in `befehl` und `anhaengen`

      DIE REIHENFOLGE IST NICHT BELIEBIG. Zuerst muss die Box einen EIGENEN
      Schluessel bekommen (`jellyfin.apiKey` lesen, `jellyfinZugangAusListe`
      als Rueckfall dahinter) — erst danach darf der Schluessel aus data.json
      verschwinden. Wer die Reihenfolge dreht, macht eine Box stumm.

      NICHT MITGEGANGEN IST DIE RAEUMUNG: eine geerbte `_source: 'box'`-Kopie
      im localStorage bleibt liegen. Das Feld `_source` bleibt deshalb im Typ
      stehen, obwohl es niemand mehr schreibt — es ist der einzige Griff, an
      dem sich eine geerbte Boxkopie noch von einem echten Login in diesem
      Browser unterscheiden laesst. Siehe
      [[oberflaeche-speicherschluessel]] und
      [[jellyfin-ueber-die-box-statt-schluessel-im-browser]].

      BEIM MESSEN SELBST GELERNT: der Weiterreicher der Box gibt seine
      Kopfzeilen KLEIN zurueck (`content-range`). Ein `dict(...)` ueber urllibs
      Kopfzeilen verliert deren Unempfindlichkeit gegen Gross-/Kleinschreibung
      — die erste Fassung des Werkzeugs meldete daraufhin „TRAEGT NICHT / kein
      Sprung" bei einer Antwort, die in Wahrheit 206 mit
      `content-range: bytes 0-1023/5683931` war. Ein falsches ROT ist teurer
      als gar keine Messung.

  - kind: falle
    id: anhaengen-startet-die-zweite-tonmaschine
    title: "Zwei Toene zugleich: ein `ardqueue`/`jfqueue` nach dem Dienstwechsel startet mpv neben librespot"
    match: "titel spielt weiter|zwei toene|dienstwechsel.*ton|ardqueue|jfqueue|append-play|currentPlayer.*spotify.*mpv spielt|stop haelt nur spotify"
    sev: hoch
    cause: >
      `player.queue()` ist in mpv `loadfile <adresse> append-play`, und
      APPEND-PLAY FAENGT AN ZU SPIELEN, wenn nichts laeuft. Der Zweig
      `jfqueue`/`ardqueue` im Abspieldienst fasste `currentMeta.currentPlayer`
      NICHT an. Eine ARD-Sendung wird als EIN Tipp gestartet, aber als 30
      Befehle geschickt (`albumSpielen`); tippt jemand mittendrin eine
      Spotify-Kachel, laufen die uebrigen Anhaeng-Befehle weiter und starten mpv
      NEBEN librespot. `currentPlayer` meldet dabei weiter 'spotify' — ein
      spaeteres `stop` haelt also nur Spotify an, und die ARD-Folge ist ueber die
      Oberflaeche gar nicht mehr abzustellen.
    fix: >
      ZWEI Stellen, beide an der Wurzel. (1) Der Abspieldienst hat seit dem
      05.08.2026 EINEN Ort fuer den Dienstwechsel (`tonmaschineUebernehmen` in
      spotify-control.ts); jeder Startzweig geht hindurch, und der
      Anhaeng-Zweig VERWIRFT einen Befehl, solange Spotify den Ton besitzt.
      (2) Die Anhaeng-Schleife in NewDesign/app.js bricht ab, sobald ein
      neuerer Tipp `startLauf` hochgezaehlt hat — und `ausQuelleSpielen` zaehlt
      seither ueberhaupt erst hoch. Nachmessen:
      `node tools/dienstwechsel-am-dienst.mjs --pruefen` (echter Dienst,
      Attrappen) und `node tools/dienstwechsel-am-geraet.mjs --alle` (Box, Ton).
    tags: ['falle', 'abspieler', 'mpv', 'spotify', 'ard', 'jellyfin', 'dienstwechsel', 'ton', 'messen', 'neu', 2026]
    source: "05.08.2026, Box .169, tools/dienstwechsel-am-geraet.mjs + tools/stop-wirkt-am-geraet.mjs + tools/dienstwechsel-am-dienst.mjs"
    body: |
      `ardqueue` UND `jfqueue` STEHEN IN ZWEI EINTRAEGEN. Die eine Frage,
      die trennt: SIND ES ZWEI TOENE ODER EIN FALSCHER?

          Der alte Ton laeuft weiter, waehrend der neue anfaengt
          -> DIESER Eintrag (zwei Tonmaschinen nebeneinander)
          Ein Antipper laedt ZWEIMAL, der Strom hat ploetzlich keine
          Dauer und laesst sich nicht mehr springen
          -> [[verb-als-zeichenkette-gesucht-dradio-enthaelt-radio]]
             („dradio" enthaelt „radio", der Radio-Zweig zieht mit)

      Beide sitzen im Anhaeng-Verb, deshalb dieselben Wortlaute im
      `match:` — aber der eine ist eine Frage des BESITZES am Ton, der
      andere eine der ZERLEGUNG des Befehlspfads.

      GEMELDET am 04.08.2026, woertlich: „und es kann sein das der titel
      weiterspielt wenn man den dienst wechselt". Nachgeschoben am 05.08.:
      „das ton problem gilt fuer alle dienste untereinander".

      ── DER VERDACHT WAR RICHTIG UND FUEHRTE TROTZDEM DANEBEN ──────────────
      Naheliegend war: mpv und librespot wissen nichts voneinander, der
      Wechsel haelt den alten nicht an. Gemessen wurde deshalb ZUERST das,
      was der Verdacht behauptet — alle neun Paare, die auf .169 zu stellen
      sind (spotify/jellyfin/ard, je hin und her und auf sich selbst; lokal
      und rss gibt es dort nicht).

          Spotify -> Spotify | -> Jellyfin | -> ARD      sauber
          Jellyfin -> Spotify | -> Jellyfin | -> ARD     sauber
          ARD -> Spotify | -> Jellyfin | -> ARD          sauber

      NEUN VON NEUN SAUBER. Und `stop` wirkt auch: mit einer Aufnahme in
      Scheiben von 250 ms gemessen (tools/stop-wirkt-am-geraet.mjs) war es
      nach 0,75 s (Spotify) bzw. 0,25 s (mpv) still, in je drei Runden.

      ── DIE MESSUNG LOG DURCH WEGLASSEN ───────────────────────────────────
      Das Werkzeug bildete die Oberflaeche nach — aber NICHT ganz. Ein
      Jellyfin-Album und eine ARD-Sendung werden von `albumSpielen` (und von
      `playJellyfinAlbum` in der klassischen Oberflaeche) nicht mit EINEM
      Befehl gestartet, sondern mit einem pro Titel:

          stop  ->  erster Titel  ->  ardqueue/jfqueue fuer JEDEN weiteren

      Gemessen wurden nur `stop` und der erste Titel. Die 29 Anhaeng-Befehle
      der Maus-Sendung fehlten — und genau in ihnen sass der Fehler. Wieder
      [[attrappe-luegt-durch-weglassen]], diesmal im Messwerkzeug selbst.

      ── DER BEFUND, sobald der Fall vollstaendig gestellt war ──────────────
      tools/dienstwechsel-am-geraet.mjs, Sorten `nachzuegler-danach` und
      `nachzuegler-mitten` (05.08.2026, Box .169, 30 Titel):

          ARD laeuft, Wechsel auf Spotify, dann EIN Anhaeng-Befehl:
              Ausgang 0,5099 -> 0,8439   mpv SPIELT   currentPlayer: spotify
              mpv angehalten:  0,4958    — es klang WEITER

          Wechsel MITTEN in die Anhaeng-Schleife (nach dem 4. Befehl):
              Ausgang 0,4940 -> 0,6140   mpv SPIELT   currentPlayer: spotify
              mpv angehalten:  0,4857

      Zwei Toene zugleich. Und der naechste Lauf begann mit einer „RUHE"-
      Ablesung von 0,5082 bei SPIELENDEM mpv — das `stop` am Ende hatte nur
      Spotify angehalten, weil `currentPlayer` 'spotify' sagte. Die Folge lief
      weiter und war ueber die Oberflaeche nicht mehr abzustellen. Genau der
      gemeldete Satz.

      ── WARUM APPEND-PLAY UND NICHT APPEND ────────────────────────────────
      mpv-protokoll.ts: `queue: ['loadfile', was, 'append-play']`. Das ist
      richtig fuer den Zweck (die zweite Folge soll von selbst anlaufen, wenn
      die erste durch ist) — und tuecksich, sobald der Befehl VERSPAETET
      kommt: dann laeuft nichts, und „append-play" heisst „fang an".

      ── DIE EIGENTLICHE URSACHE IST DIE BAUART ────────────────────────────
      Der Abspieldienst hat den Dienstwechsel NIRGENDS vollzogen. Es gab SECHS
      Zweige, die `currentMeta.currentPlayer` einfach ueberschrieben —
      `library`, `radio`, `rss`, `jellyfin`, `ard`, `useSpotify` — und KEINER
      hielt die andere Maschine an. Angehalten wurde ausschliesslich, weil die
      Oberflaeche vorher ein `stop` schickte. Die Absicherung lag also in
      einem ANDEREN Programm, und zwar in ZWEIEN: NewDesign/app.js und
      player.service.ts fuehren je eine eigene Kopie davon.

      Das ist [[drei-orte-eine-anzeige]] eine Etage tiefer: EINE Sache
      (welche Maschine spielt), an sechs Stellen geschrieben, abgesichert von
      aussen. Ein siebter Zweig, eine dritte Oberflaeche, ein alter Stand —
      und die Box spielt zweistimmig.

      ── WIE ES REPARIERT IST ──────────────────────────────────────────────
      1. `tonmaschineUebernehmen(neu)` in spotify-control.ts ist DER EINE ORT.
         Jeder Startzweig geht hindurch; ist eine andere Maschine dran, wird
         sie vorher angehalten (bei Spotify ABGEWARTET, mit derselben
         Obergrenze ANHALTE_FRIST_MS wie `stop`). Der Rumpf ist derselbe wie
         bei `stop` — abgetrennt als `anhaltenNachSpieler`, damit es nicht
         zweimal dasteht.
      2. Der Zweig `jfqueue`/`ardqueue` VERWIRFT einen Befehl, solange
         `currentPlayer` 'spotify' sagt, und setzt sonst 'mplayer' —
         `append-play` kann die Wiedergabe starten, und ein spaeteres `stop`
         muss sie finden.
      3. `queueAlbum` („Als naechstes spielen", lokal) geht im Startfall
         ebenfalls durch den einen Ort. Die Fallunterscheidung steht VOR dem
         Wechsel, sonst hiesse `currentPlayer` danach immer 'mplayer' und das
         Anhaengen waere nie wieder ein Anhaengen.
      4. In NewDesign/app.js bricht die Anhaeng-Schleife ab, sobald
         `startLauf` weitergezaehlt hat — und `ausQuelleSpielen` zaehlt
         seither ueberhaupt hoch. Vorher taten das nur `starte` und
         `albumSpielen`; ein gewoehnlicher Kacheltipp ueberholte also nichts.

      DIE OBERFLAECHEN SCHICKEN IHR `stop` WEITER. Es kostet nichts und haelt
      die Anzeige sauber — es ist nur nicht mehr die EINZIGE Absicherung.

      ── WAS AUSDRUECKLICH NICHT PASSIERT ──────────────────────────────────
      Beim Wechsel mpv -> mpv (zweite Folge derselben Sendung) wird NICHT
      angehalten. `loadfile … replace` raeumt selbst auf; ein Halt waere reine
      Stille zwischen zwei Folgen. Das ist als Gegenprobe festgehalten
      (Fall 5 in tools/dienstwechsel-am-dienst.mjs).

      ── WAS OFFEN BLEIBT, und es soll niemand fuer erledigt halten ────────
      Ein Nachzuegler, der eintrifft, waehrend GAR NICHTS laeuft (nach einem
      gewoehnlichen `stop`), startet die Wiedergabe weiterhin — nur ist sie
      jetzt wenigstens anhaltbar. Sauber waere eine Startlauf-Nummer, die der
      Befehl MITBRINGT; heute kennt nur die Oberflaeche ihre eigene.

      NACHMESSEN:
        node tools/dienstwechsel-am-dienst.mjs --pruefen     # echter Dienst
        node tools/dienstwechsel-am-geraet.mjs --alle        # Box, am Ton
        node tools/stop-wirkt-am-geraet.mjs --alle --runden 3
    related: [verb-als-zeichenkette-gesucht-dradio-enthaelt-radio]

  - kind: howto
    id: dienstwechsel-und-verstummen-messen
    title: "Die drei Werkzeuge zum Dienstwechsel — Paare, Verstummen, echter Dienst"
    match: "dienstwechsel-am-geraet|stop-wirkt-am-geraet|dienstwechsel-am-dienst|zwei toene messen"
    tags: ['werkzeug', 'messen', 'abspieler', 'ton', 'dienstwechsel', 'neu', 2026]
    source: "05.08.2026"
    body: |
      tools/dienstwechsel-am-geraet.mjs
        Fahrt einen Dienstwechsel an der BOX nach und entscheidet AM TON, ob
        zwei Maschinen zugleich klingen. Getrennt wird so: mpv fragt sich
        SELBST ueber seinen IPC-Socket (laeuft `time-pos` weiter, gibt es Ton),
        und fuer die entscheidende Ablesung wird mpv PAUSIERT — was dann noch
        klingt, kann nur librespot sein.
          node tools/dienstwechsel-am-geraet.mjs --liste
          node tools/dienstwechsel-am-geraet.mjs --alle
          node tools/dienstwechsel-am-geraet.mjs --sorte nachzuegler-mitten
        DIE SORTEN SIND DAS VOLLE KREUZPRODUKT der Dienste, die auf DIESER Box
        ein Werk haben — nicht eine Auswahl. Dazu die beiden Nachzuegler-Faelle,
        die den gemeldeten Fehler stellen.
        DREI FALLEN, die es beim Bauen gekostet hat:
          * Der erste Spotify-Eintrag der Bibliothek war „External Playback"
            mit der Kennung `t:unknown|external playback`. Der Startbefehl ging
            hinaus, der Dienst antwortete 200, es kam kein Ton — und der Lauf
            meldete „UNENTSCHIEDEN", was wie ein Boxfehler aussieht. Geprueft
            wird jetzt auf 22 Zeichen base62.
          * Die entscheidende Ablesung setzt `pause=true` auf dem mpv DER BOX
            und nahm es nie zurueck. `stop` raeumt die Warteschlange, aber
            nicht dieses Merkmal — der naechste Lauf mass eine stumme Box und
            hielt das fuer einen Befund. Jetzt gibt es `mpvFortsetzen()`, und
            es laeuft auch im `finally`.
          * `starten()` schickte nur `stop` + ersten Titel. Die Oberflaeche
            schickt bei Album/Sendung EINEN Befehl JE TITEL. Genau in den
            fehlenden sass der Fehler.

      tools/stop-wirkt-am-geraet.mjs
        Wird es nach `stop` wirklich still — und WANN? Nimmt auf der Box auf,
        schickt das `stop` VON DORT (localhost, kein Netzweg dazwischen) und
        zerlegt die Aufnahme in Scheiben von 250 ms.
          node tools/stop-wirkt-am-geraet.mjs --alle --runden 3
        WARUM NICHT EIN SPITZENWERT: er sagt „irgendwann in diesen vier
        Sekunden war es laut" und unterscheidet NICHT zwischen 0,3 s Nachlauf
        (in Ordnung) und vier Sekunden Weiterspielen (der Fehler). Die erste
        Ablesung nach `stop` stand auf 0,3960 und sah nach Befund aus — sie war
        der Nachlauf.
        GEMESSEN (05.08.2026, .169, je 3 Runden): Spotify 0,75 s bis zur
        Stille, Jellyfin und ARD je 0,25 s, `angehalten: bestaetigt` in allen.

      tools/dienstwechsel-am-dienst.mjs
        Der WIRKLICHE Abspieldienst mit tsx, ueber HTTP angesprochen, mit
        Attrappen fuer Spotify und die Wiedergabe-Maschine. Der Unterschied zu
        tools/anhalten-am-dienst.mjs: die Spieler-Attrappe SCHREIBT JEDEN
        AUFRUF MIT. Ohne das liesse sich „mpv wurde angehalten" nicht von „es
        ist nichts passiert" unterscheiden — und genau dieser Unterschied ist
        der Befund.
          node tools/dienstwechsel-am-dienst.mjs --pruefen
        Fuenf Faelle, davon einer die Gegenprobe (mpv -> mpv haelt NICHT an).
        Gegen den Stand VOR der Reparatur: 5 Abweichungen. Danach: keine.

  - kind: falle
    id: alsa-init-laeuft-obwohl-abgeschaltet
    title: "mupibox-alsa-init ist disabled und laeuft trotzdem bei jedem Start — Wants= zieht sie hoch"
    summary: >-
      Der Eintrag mupi-pipewire-umstieg sagt „der pipewire-Schritt schaltet
      mupibox-alsa-init ab". Das stimmt nur zur Haelfte: die Unit-Datei ist
      disabled, aber mupibox-player.service und mupibox-server.service tragen
      Wants=mupibox-alsa-init.service — sie laeuft damit bei JEDEM Start
    tags: [ton, alsa, pipewire, systemd, lautstaerke, e12, 2026]
    source:
      - gemessen am 2026-08-04 an der Box 192.168.178.169 (nur gelesen, nichts geaendert)
      - systemctl is-enabled/is-active mupibox-alsa-init.service; systemctl show mupibox-player.service -p Wants
    body: |
      WAS GEMESSEN WURDE (04.08.2026, Box .169, nur lesend):
        systemctl is-enabled mupibox-alsa-init.service  -> disabled
        systemctl is-active  mupibox-alsa-init.service  -> active (exited),
                                                           RemainAfterExit=yes
        gestartet 09:01:45, also beim Hochfahren
      Der Grund steht in den beiden Diensten, die jeder Box wichtig sind:
      mupibox-player.service UND mupibox-server.service tragen beide
      `Wants=mupibox-alsa-init.service` und `After=…`. `disable` entfernt nur
      den Verweis aus multi-user.target.wants — eine Wants=-Zeile in einer
      ANDEREN Unit zieht die Unit trotzdem hoch. „Abgeschaltet" heisst hier
      also nicht „laeuft nicht".

      UND SIE IST NICHT FOLGENLOS. init-alsa-softvol.sh ruft
        aplay -D default …
        amixer -q sset Master 70%
      beide OHNE `-c`. Unter PipeWire ist der Standard-Regler die
      PipeWire-Senke (99-pipewire-default.conf legt nicht nur pcm.!default,
      sondern auch ctl.!default auf pipewire). Die Unit stellt damit bei jedem
      Start die Lautstaerke der ECHTEN Senke auf 70 % — und geht dabei an der
      Obergrenze mupibox.maxVolume vorbei, die in mupi-lautstaerke.sh sitzt.

      NICHT FESTGESTELLT: ob der Zweig an DIESEM Boot wirklich gelaufen ist.
      `journalctl -u mupibox-alsa-init.service -b` meldet „No entries", die
      Unit endete mit Status 0. Heute steht die Senke auf 40 %, also hat
      danach etwas anderes den Wert gesetzt.

      WAS DARAUS FOLGT
      Wer die Unit wirklich stilllegen will, muss das `Wants=` aus
      mupibox-player.service und mupibox-server.service entfernen — `systemctl
      disable` genuegt nicht. Fuer eine frische Karte aus dem box-Repo stellt
      sich die Frage nicht: dort gibt es weder Unit noch Skript, und seit dem
      PipeWire-Umstieg (BACKLOG E12/X6) auch keinen softvol-Regler mehr, den
      jemand erzeugen muesste.

  - kind: falle
    id: i2c-bus-vorhanden-chip-nicht
    title: "Ein vorhandener I2C-Bus beweist keinen angeschlossenen Chip — und aus einem Geraeteskript wird beim Ausrollen ein Neustart-Karussell"
    summary: >-
      /dev/i2c-11 gibt es auf einem Pi 5 immer, auch ohne Panel. Ein Skript,
      das den Bus oeffnet und die Adresse mit I2C_SLAVE_FORCE setzt, merkt
      davon nichts — es merkt es erst an den Lesefehlern. Wer so ein Skript
      auf JEDE Karte ausrollt und die Unit einschaltet, bekommt auf jeder
      Box ohne diesen Chip Restart=on-failure im Dauerlauf
    tags: [i2c, touch, systemd, ausrollweg, e12, 2026]
    source:
      - gefunden am 2026-08-04 beim Gegenlesen von BACKLOG E12/X5 (scripts/box/touch-bridge.py)
      - Geraetestand nur lesend gesehen an 192.168.178.169 (Pi 5, DietPi Trixie)
    body: |
      DIE DREI SCHRITTE, DIE ZUSAMMEN DIE FALLE ERGEBEN:
        1. `os.open("/dev/i2c-11")` gelingt — auf einem Pi 5 gibt es diesen
           Bus IMMER (nachgesehen: i2c-1, i2c-11, i2c-13, i2c-14, ohne jede
           dtparam-Zeile in der config.txt).
        2. `ioctl(fd, I2C_SLAVE_FORCE, 0x38)` gelingt AUCH ohne Gegenstelle —
           FORCE fragt niemanden, es setzt nur die Zieladresse.
        3. Erst der erste Lese-/Schreibversuch scheitert (ENXIO).
      Ein Skript, das nur 1 und 2 prueft, haelt sich also fuer zustaendig und
      laeuft los.

      WAS DARAUS WIRD, WENN MAN ES AUSROLLT: touch-bridge.py war fuer GENAU
      EINE Box geschrieben (FT5x06 ohne Interrupt-Leitung, siehe
      [[mupi-touch-ohne-interrupt]]).
      Am 04.08.2026 kam die Unit ueber autosetup UND den Update-Weg auf jede
      Karte und wurde eingeschaltet. Auf jeder Box ohne diesen Controller:
      200 Lesefehler (~10 s), Ruecklauf 1, Restart=on-failure, RestartSec=2 —
      alle zwoelf Sekunden von vorn, endlos. Dazu bei jeder Runde ein
      uinput-Geraet an- und wieder abgemeldet, unter dem laufenden X-Server.
      Kein Test faellt darueber um, und ein Fehlerbild gibt es dafuer auch
      nicht: die Box fuehlt sich nur seltsam an.

      DIE REGEL: Ein Geraeteskript, das aus einer Bastelei in einen
      Ausrollweg wandert, braucht als Erstes eine PROBE — antwortet an
      dieser Adresse ueberhaupt jemand? Und die Antwort „nein" ist kein
      Fehler, sondern Ruecklauf 0. Erst danach darf es Betriebsmittel
      anlegen (hier: das uinput-Geraet). Behoben in scripts/box/touch-bridge.py
      (zehn Leseversuche ueber eine Sekunde). ACHTUNG: dieselbe Datei liegt
      auch im remote-step-installer — Aenderungen muessen dort nachgezogen
      werden, sonst setzt der naechste Installerlauf die alte Fassung zurueck.

  - kind: falle
    id: amixer-master-wirkt-unter-pipewire
    title: "Die Annahme „amixer sset Master laeuft unter PipeWire ins Leere“ ist falsch — ctl.!default zeigt auf PipeWire"
    summary: >-
      Eine verbreitete Annahme in diesem Projekt war, dass alte
      amixer-Aufrufe nach dem PipeWire-Umstieg wirkungslos sind. Am Geraet
      gemessen ist das Gegenteil richtig — sie treffen die PipeWire-Senke.
      Wirkungslos ist nur `amixer -c <Karte> sset Master`
    tags: [ton, alsa, pipewire, lautstaerke, messung, e12, 2026]
    source:
      - gemessen am 2026-08-04 an der Box 192.168.178.169 (nur gelesen)
      - wpctl get-volume @DEFAULT_AUDIO_SINK@ vs amixer sget Master vs amixer -c MAX98357A sget Master
    body: |
      IM GLEICHSTAND GEMESSEN (04.08.2026, Box .169):
        wpctl get-volume @DEFAULT_AUDIO_SINK@   ->  Volume: 0.40
        amixer sget Master                      ->  26214 [40%]
        mupi-lautstaerke.sh get                 ->  40
        amixer -c MAX98357A sget Master         ->  53 %   <- der tote Rest

      DER GRUND: /usr/share/alsa/alsa.conf.d/99-pipewire-default.conf setzt
      nicht nur `pcm.!default`, sondern auch `ctl.!default { type pipewire }`.
      Ein `amixer` OHNE `-c` spricht damit die PipeWire-Senke an.

      WAS TATSAECHLICH TOT IST: der softvol-Regler „Master" auf der Tonkarte
      selbst (numid=1, iface=MIXER, access „read write user"). alsa-restore
      holt ihn beim Start aus /var/lib/alsa/asound.state zurueck, aber er
      liegt in keinem Abspielweg mehr — /etc/asound.conf existiert nicht mehr,
      PipeWire oeffnet die Karte direkt. Die 53 % gegen die klingenden 40 %
      sind der Beweis: die beiden Werte sind auseinandergelaufen.

      WARUM DAS WICHTIG IST: Wer eine Zeile mit `amixer sset Master` sieht und
      sie fuer wirkungslos haelt, sucht den Fehler an der falschen Stelle. Der
      wirkliche Grund, solche Aufrufe auf mupi-lautstaerke.sh umzustellen, ist
      nicht die fehlende Wirkung, sondern die UMGANGENE OBERGRENZE
      (mupibox.maxVolume, Funktion klemme()). Siehe BACKLOG E12/X7.

      ── ABER: ES KOMMT DARAUF AN, WER FRAGT (nachgetragen 04.08.2026 nachts,
      beim Gegenlesen, ebenfalls nur lesend an .169 gemessen) ──
      Die Messung oben ist als BENUTZER dietpi entstanden, in dessen Sitzung
      PipeWire laeuft (XDG_RUNTIME_DIR=/run/user/1000). Als root sieht dieselbe
      Box so aus:
        sudo amixer scontrols  -> amixer: Mixer attach default error: Host is down
        sudo wpctl status      -> Could not connect to PipeWire
        sudo sh -c 'echo $XDG_RUNTIME_DIR'  -> leer;  ls /run/user  -> nur 1000
      Der ALSA-Umweg ueber ctl.!default landet beim PipeWire-Plugin, und das
      findet ohne Sitzung keinen Socket. PipeWire ist ein BENUTZERdienst — wer
      ihn erreichen will, muss in der richtigen Sitzung stehen.

      WAS DAS FUER DIE BOX HEISST: alles, was als root laeuft, regelt auf einer
      PipeWire-Box GAR NICHTS — weder ueber amixer noch ueber wpctl noch ueber
      pactl, und auch nicht ueber mupi-lautstaerke.sh (backend() meldet dort
      „keiner"). Betroffen sind mqtt.py (mupi_mqtt.service: User=root),
      telegram_receiver.py, shutdown_sound.sh und off_trigger.sh (postboot.d,
      also root) sowie mupi.php (www-data). Das war vor der Buendelung genauso
      und ist keine Verschlechterung — aber wer „mqtt setzt jetzt die
      Lautstaerke mit Obergrenze" liest, glaubt sonst etwas, das auf dieser Box
      nicht stattfindet. In der dietpi-Sitzung (Kiosk, Abspieldienst) wirkt
      alles wie oben gemessen. Siehe BACKLOG E12/X13(d).

  - kind: entscheidung
    id: tonstapel-frische-karte-pipewire-laufende-box-nicht
    title: "Tonstapel im Ausrollweg: PipeWire auf der frischen Karte, kein Zwangsumstieg beim Update"
    summary: >-
      autosetup.sh stellt eine frische Karte auf PipeWire um;
      update/start_mupibox_update.sh stellt eine laufende Box NICHT um,
      sondern bessert nur nach. Dazu die Falle, die ein Update sonst gerissen
      haette: pulseaudio-module-bluetooth holt PulseAudio auf eine
      PipeWire-Box zurueck
    tags: [ton, pipewire, pulseaudio, autosetup, update, ausrollweg, e12, 2026]
    source:
      - Entscheidung am 2026-08-04 im box-Repo umgesetzt (autosetup/autosetup.sh, update/start_mupibox_update.sh)
      - Zustand der Box 192.168.178.169 am 2026-08-04 nur lesend gemessen
      - BACKLOG E12/X6
    body: |
      DER WIDERSPRUCH LAG IM EIGENEN REPO, nicht nur zwischen Repo und Box:
      config/services/librespot.service ist ausdruecklich fuer PipeWire
      geschrieben (User=dietpi, XDG_RUNTIME_DIR=/run/user/1000, ExecStartPre
      wartet auf `pactl list short sinks`), waehrend autosetup.sh System-
      PulseAudio anlegte, einschaltete UND startete. Auf einer frischen Karte
      gibt es dann kein /run/user/1000/pulse — die Warteschleife lief bis zu
      20 s ins Leere und endete trotzdem mit exit 0. Jeder Start kostete das,
      und gemeldet wurde nichts.

      ── DIE ENTSCHEIDUNG ──────────────────────────────────────────────
      FRISCHE KARTE (autosetup.sh): PipeWire. Sie hat nichts zu verlieren,
      und alles andere im Repo ist dafuer geschrieben. Umgesetzt sind
      genau die Punkte, die auf der laufenden Box gemessen wurden:
        * Pakete pipewire pipewire-bin pipewire-pulse pipewire-alsa
          wireplumber libspa-0.2-bluetooth — statt pulseaudio-module-bluetooth
        * pulseaudio-utils BLEIBT (davon kommt pactl, und genau darauf wartet
          librespot.service)
        * systemctl --global enable pipewire.socket pipewire-pulse.socket
          wireplumber.service — die BENUTZEREBENE, die am leichtesten
          vergessen wird
        * 80-bluez-ohne-seat.conf nach /etc/wireplumber/wireplumber.conf.d/
        * /etc/asound.conf -> /etc/asound.conf.vor-pipewire (damit faellt auch
          die feste plughw:0-Verdrahtung weg, siehe
          [[mupi-asound-hardcoded-card0]])
        * die zwei INS LEERE ZEIGENDEN Verweise
          /etc/systemd/user/default.target.wants/pulseaudio.service und
          …/sockets.target.wants/pulseaudio.socket wegraeumen
        * pulseaudio.service wird weiter AUSGELIEFERT, aber nicht mehr
          eingeschaltet — das ist der Rueckweg, nicht der Betriebsweg.
          ACHTUNG, DAS GILT NUR FUER DIE UNIT-DATEI (nachgetragen 04.08.2026
          nachts beim Gegenlesen): das PAKET entfernen beide Zweige
          (`apt-get --yes remove pulseaudio pulseaudio-module-bluetooth`).
          Ohne /usr/bin/pulseaudio scheitert die Unit mit 203/EXEC. Der
          Rueckweg hat deshalb DREI Schritte, und in dieser Reihenfolge:
            apt-get install pulseaudio pulseaudio-module-bluetooth
            mv /etc/asound.conf.vor-pipewire /etc/asound.conf
            systemctl enable --now pulseaudio.service   # dann Neustart
          Die Feineinstellungen unter /etc/pulse/ ueberleben das remove
          (kein purge). Wer nur die beiden alten Schritte macht, hat
          asound.conf zurueck, PipeWire aus dem Weg und trotzdem keinen Ton.

      LAUFENDE BOX (Update-Weg): KEIN Umstieg. Er beruehrt asound.conf, den
      Kiosk-Benutzer und die Benutzersitzung; scheitert er mitten in einem
      Update, steht am Ende eine stumme Box im Kinderzimmer. Der Weg erkennt
      den Stapel und macht nur, was nichts umwerfen kann. Wer den Umstieg
      will, sagt es ausdruecklich:
          sudo MUPI_AUDIO=pipewire … start_mupibox_update.sh
      und dann wird er GANZ gemacht oder gar nicht (Wache auf `wpctl`).

      ── DIE FALLE, DIE DAS NAECHSTE UPDATE GERISSEN HAETTE ─────────────
      In beiden Paketlisten stand `pulseaudio-module-bluetooth`. Dieses eine
      Paket zieht den ganzen PulseAudio-Dienst mit. Die Pruefung davor ist
      `dpkg -l … | egrep '^ii'` — auf der PipeWire-Box steht PulseAudio auf
      „rc" (entfernt, Konfigurationsreste), und „rc" ist nicht „ii". Das
      naechste Update haette PulseAudio also ZURUECKGEHOLT. Zusammen mit den
      beiden Verweisen unter /etc/systemd/user/*.wants/, die heute nur
      deshalb harmlos sind, weil ihr Ziel fehlt, waeren daraus zwei
      Tonserver auf einer Soundkarte geworden — und das Update haette es
      verursacht.

      NICHT GEMESSEN: kein Kaltstart, keine frisch bespielte Karte, kein Pi 4.
      Die Aussagen ueber den Ist-Zustand stammen aus einem rein lesenden Lauf
      an .169 am 04.08.2026; alles ueber die frische Karte ist Absicht des
      Rezepts, nicht Messung.

  - kind: falle
    id: box-werkzeuge-liegen-in-opt-mupibox-tools
    title: "touch-bridge.py und bt-reconnect.py gehoeren nach /opt/mupibox-tools/ — nicht nach /usr/local/bin/mupibox/"
    summary: >-
      Das box-Repo rollt scripts/box/ seit dem 04.08.2026 aus. Zielpfad ist
      /opt/mupibox-tools/, weil der remote-step-installer die Dateien dort
      ablegt und beide Units genau diesen Pfad nennen — ein anderer Pfad
      haette auf jeder laufenden Box eine ZWEITE Fassung hinterlassen
    tags: [ausrollweg, touch, bluetooth, systemd, e12, 2026]
    source:
      - umgesetzt am 2026-08-04 in autosetup/autosetup.sh und update/start_mupibox_update.sh
      - Zustand /opt/mupibox-tools/ der Box 192.168.178.169 am 2026-08-04 gelesen
      - BACKLOG E12/X5
    body: |
      ZWEI REPOS LEGEN DIESELBEN DATEIEN AB. Der remote-step-installer legt
      seit langem
        /opt/mupibox-tools/touch-bridge.py     (mupibox-touch-bridge.service)
        /opt/mupibox-tools/bt-reconnect.py     (mupibox-bt-reconnect.service)
        /opt/mupibox-tools/netz-watchdog.py
      Das box-Repo hatte bt-reconnect.py unter scripts/box/ liegen, ohne dass
      ein Ausrollweg das Verzeichnis je anfasste, und touch-bridge.py gar
      nicht — obwohl die Verwaltung beide Dienste auffuehrt
      (src/backend-api/src/dienste.ts:249 und :266).

      WARUM DER PFAD NICHT FREI WAEHLBAR IST: Wer sie nach
      /usr/local/bin/mupibox/ legt, hat auf jeder laufenden Box hinterher ZWEI
      Fassungen derselben Datei — die alte unter /opt bleibt liegen, und die
      neue Unit zeigt woandershin. Zwei Fassungen sind schlimmer als eine
      fehlende: beim naechsten Suchen findet jemand die falsche. Genau diese
      Falle wurde bei mupibox-boot-splash.py schon einmal aufgeraeumt (dort
      raeumt der Update-Weg die alte Kopie ausdruecklich weg).

      DARAUS FOLGT EINE PFLICHT FUER BEIDE SEITEN: Aendert jemand
      touch-bridge.py oder bt-reconnect.py im remote-step-installer, muss die
      Fassung unter scripts/box/ im box-Repo mitgezogen werden — sonst
      ueberschreibt das naechste Update die neuere mit der aelteren. Stand
      04.08.2026 sind beide Fassungen byteweise gleich.

      NOCH OFFEN: die Touch-Unit ruft `--drehung 180`. Das ist an GENAU EINEM
      Gehaeuse gemessen. Auf Hardware, wo der Kerneltreiber selbst Ereignisse
      liefert, ist es folgenlos (`--auto` haelt sich heraus). Wo die Bruecke
      einspringt und der Controller anders herum sitzt, tippt das Kind an der
      falschen Stelle — die Nachmess-Anleitung steht deshalb in der Unit.

  - kind: falle
    id: klassische-oberflaeche-merkt-album-nummer
    title: "Die KLASSISCHE Box-Oberflaeche merkte bei Playlisten die Album-Nummer — 87 war richtig, 6 wurde geschrieben"
    sev: hoch
    match: "saveResumeFiles|resumespotifytrack_number|merkstand.ts|stelleMerken|item.track_number"
    cause: >-
      `player.page.ts saveResumeFiles` setzte `resumespotifytrack_number =
      currentPlayedSpotify.item.track_number` — die Nummer des Stuecks in SEINEM
      Album. `player.service.ts resumeMedia` setzt aber ueber
      `spotify:playlist:<id>:<nr>:<ms>` fort, also ueber die Position IN DER
      PLAYLIST
    fix: >-
      Bei einer Spotify-PLAYLIST meldet die klassische Oberflaeche jetzt an
      `POST /api/weiterhoeren` (`merkstand.ts merkWeg` + `MediaService.stelleMerken`)
      und schreibt gar nichts mehr selbst. Der Server loest die Position aus der
      Titelliste auf und markiert sie mit `resumeVersatzGeprueft`
    source:
      - Messung an der Box 192.168.178.169 am 2026-08-04 (GET /player/state waehrend Position 87)
      - Reparatur 2026-08-05 in src/frontend-box/, BACKLOG F5
    tags: [resume, weiterhoeren, spotify, playlist, klassische-oberflaeche, falle, 2026]
    body: |
      DIE FORTSETZUNG VON [[weiterhoeren-playlist-position]]. Dort wurde der
      Fehler fuer die NEUE Oberflaeche behoben; die klassische schrieb weiter
      die alte Zahl, und beide schreiben in dieselbe `resume.json`.

      ── MIT ZAHLEN, NICHT MIT ARGUMENTEN ──────────────────────────────
      Kurze Wiedergabe an der Box, danach `GET /player/state`:

        item.track_number = 6      item.name   = "Alltag im All"
        item.album.name   = "11: Wer rettet Ming und Hu?"  total_tracks = 12
        context.uri       = spotify:playlist:0dOx2eLhuDusJvgYXYzzWP

      Gegenprobe ueber die Durchreiche der Box: Position 87 der Playlist (157
      Stuecke) ist „Alltag im All", und dieses Stueck ist auf seinem Album
      Titel 6 von 12. Die klassische Oberflaeche haette 6 geschrieben — 81
      Kapitel daneben. In derselben Minute schrieb die laufende NEUE
      Oberflaeche denselben Eintrag mit 87 und `resumeVersatzGeprueft: true`.

      ── WARUM ES IN resume.json TROTZDEM NICHT LAG ────────────────────
      `/proc/<pid>/cmdline` des Kiosk-Chromium: `--homepage
      http://localhost:8200/neu/`. Auf der Box laeuft die NEUE Oberflaeche —
      jede gemerkte Stelle entsteht ueber den aufloesenden Serverweg, 0 von 9
      Stellen standen in alter Zaehlweise da. Die klassische ist ausgerollt und
      unter `http://<box>:8200/` erreichbar: der Fehler braucht einen Griff
      zum Telefon. WER NUR DIE DATEI ANSIEHT, HAELT DEN FEHLER FUER WEG.

      ── DIE REPARATUR IST EIN VERZICHT, KEIN NACHBAU ──────────────────
      Die Position kennt nur, wer die Titelliste der Playlist hat, und die holt
      der Server (`titelEinesWerks`, mit Zwischenspeicher) — der Browser darf
      `api.spotify.com` nicht rufen. Diese Antwort gab es schon:
      `POST /api/weiterhoeren`. Die klassische Oberflaeche beantwortet die
      Frage jetzt gar nicht mehr, sie MELDET (`merkstand.ts`, rein und
      pruefbar; `MediaService.stelleMerken`, ein Aufruf ohne Warten).

        Playlist                       -> POST /api/weiterhoeren
        Playlist, aber kein item.uri   -> GAR NICHTS (vorige Stelle bleibt)
        Album / Show / Hoerbuch        -> wie bisher /api/addresume
        library / rss                  -> wie bisher

      WARUM ALBEN AUF DEM ALTEN WEG BLEIBEN, und das ist kein Zaudern: Dort
      IST die Album-Nummer die Abspielposition, es gibt also nichts
      aufzuloesen. Und ein Album, das ueber einen INTERPRETEN geoeffnet wurde,
      steht unter Umstaenden gar nicht als eigener Eintrag in
      `active_data.json` — `eintragZuSchluessel` faende `spotify:<albumid>`
      dann nicht, und es waere still NICHTS gemerkt. Eine Playlist ist auf
      dieser Box dagegen immer eine eigene Kachel.

      ── DER SCHLUESSEL MUSS GENAU medienSchluessel SEIN ───────────────
      `spotify:<kennung>`, und die Kennung ist `id` VOR `playlistid` — dieselbe
      Reihenfolge wie in `backend-api/src/medien.ts`. Wer hier `playlistid`
      bevorzugt, schickt bei einem Eintrag mit beidem einen Schluessel, den der
      Server nicht findet: 404, und es ist still nichts gemerkt. Dieselbe
      Doppelung mit demselben Hinweis wie `ardWerkSchluessel` in `ard.ts`.

      ── DIE DRITTE ANTWORT, DIE NIEMAND VERMISST HAT ──────────────────
      In `media.service.ts` liegt ein SDK-Zweig, der die Playlist-Position
      korrekt ausrechnet (`playlist.current_track_position`, Suche des Stuecks
      in der Titelliste). Er ist seit der SDK-Rente unerreichbar —
      `spotify-player.service.ts shouldUsePlayer()` gibt `false` zurueck. Mit
      ihm ist unbemerkt die richtige Zahl verschwunden, und der tote Code sieht
      bis heute aus, als sei die Frage beantwortet. Wer in dieser Datei nach
      einer Wahrheit sucht, muss zuerst fragen, ob der Zweig ueberhaupt laeuft.

      ── WAS OFFEN BLEIBT ──────────────────────────────────────────────
      Ein ALTER Eintrag ohne `resumeVersatzGeprueft` wird in der klassischen
      Oberflaeche weiter benutzt; sie kennt `nummerVerlaesslich` nicht. Solche
      Staende heilen sich beim naechsten Hoeren. Und
      [[addresume-vergleicht-undefined-ids]] bleibt, wird durch diese Aenderung
      aber seltener ausgeloest — das Erst-Merken einer Playlist geht nicht mehr
      ueber `/api/addresume`.
    related: [resume-playlistposition-schickt-in-die-falsche-folge, resume-titelnummer-bei-playlists-unscharf, weiterhoeren-playlist-position]

  - id: startlauf-abgelesen-statt-vergeben
    kind: falle
    title: "Die Startlauf-Nummer ABGELESEN statt vergeben — der Sprung traf das fremde Werk"
    match: "startLauf|meins = startLauf|ardSendungAb|tracknr nach dem Wechsel|abgeloest|nachzuegler sprung|albumSpielen return ok"
    sev: hoch
    cause: >
      `NewDesign/app.js` fuehrt eine fortlaufende `startLauf`-Nummer, damit ein
      ueberholter Tipp keine Befehle mehr schickt. `ardSendungAb` und
      `weiterSpielen` LASEN sie aber erst NACH dem Start ab
      (`const meins = startLauf`). Wurde der Start in der Zwischenzeit
      ueberholt, stand dort die Nummer des FREMDEN Tipps — und der Vergleich in
      `titelUndStelle` konnte nie mehr anschlagen. Verschaerft wurde es am
      05.08.2026: seither BRICHT die Anhaeng-Schleife bei Ueberholung ab, gab
      aber `'ok'` zurueck; der Aufrufer hielt den abgebrochenen Lauf fuer
      gelungen und schickte `tracknr:`/`seekpos:` hinterher.
    fix: >
      `laufBeginnen()` VERGIBT die Nummer und gibt sie zurueck; `starte` und
      `albumSpielen` nehmen sie als Argument entgegen, statt sie selbst zu
      vergeben. Abgelesen wird sie nirgends mehr. Und die abgebrochene Schleife
      antwortet seit dem 06.08.2026 `'abgeloest'` statt `'ok'` — eine vierte
      Antwort neben ok/kinderzeit/geht-nicht, die weder Erfolg noch Fehlschlag
      heisst, sondern „schweig jetzt". `spielen()` behandelt sie wie Erfolg und
      probiert KEINE weitere Quelle. Nachmessen:
      `node tools/ard-folgen-lane.mjs --pruefen`, Fall `nachzuegler`.
    tags: ['falle', 'neue-oberflaeche', 'ard', 'jellyfin', 'weiterhoeren', 'wettlauf', 'gegenlesen', 2026]
    source: "06.08.2026 beim Gegenlesen von E4/A10 und E26/F7; gemessen mit tools/ard-folgen-lane.mjs (Fall `nachzuegler`, Vorschau, 150 ms Bremse je Befehl)"
    body: |
      ── DER FALL, IN EINEM SATZ ───────────────────────────────────────────
      Ein Kind tippt „weiterhören" auf einer ARD-Folge. Die Sendung wird
      gestartet (ein Befehl JE Folge, auf .169 dreissig Stueck), und ganz zum
      Schluss wird gesprungen: `tracknr:` auf die Folge, `seekpos:` in die
      Stelle. Waehrend die Schleife noch laeuft, tippt es eine andere Kachel.
      Der Sprung trifft dann die frische Wiedergabe.

      ── GEMESSEN, NICHT ERSCHLOSSEN ───────────────────────────────────────
      tools/ard-folgen-lane.mjs, Fall `nachzuegler` (Vorschau, Chromium
      800x480, 150 ms Bremse je Spielerbefehl — die Vorschau antwortet sonst
      in Bruchteilen einer Millisekunde, die Box braucht je Befehl einen
      Rundlauf ueber WLAN):

          VORHER   nach dem fremden Tipp gingen noch hinaus:
                     tracknr:2  seekpos:41.5  seekpos:41.5  seekpos:41.5
                   Ziel war das lokale Album „Die Maus", das gerade angefangen
                   hatte. Also Kapitel 2 davon, bei 41,5 Prozent.
          NACHHER  0 Spruenge, 0 Nachzuegler.

      Die DREI `seekpos:` sind kein Tippfehler: `titelUndStelle` fasst
      zweimal nach, wenn der Sprung nicht ankommt. Ein Fehler dieser Sorte
      macht sich also nicht einmal bemerkbar, sondern dreimal.

      ── WARUM DIE VORHANDENE SICHERUNG NICHT GRIFF ────────────────────────
      Sie war da und sie war richtig gedacht — nur an einer Stelle falsch
      benutzt:

          startLauf += 1            in `starte()` und `albumSpielen()`
          const meins = startLauf   im AUFRUFER, NACH dem Start
          if (meins !== startLauf) return 'abgeloest'    in `titelUndStelle`

      Zwischen Zeile 1 und Zeile 2 liegen bei einer ARD-Sendung dreissig
      Netzbefehle. Tippt jemand dazwischen, zaehlt `starte()` des FREMDEN
      Tipps hoch — und Zeile 2 liest genau diese neue Zahl ab. Der Vergleich
      in Zeile 3 stimmt danach fuer immer.

      DAS IST DIESELBE BAUART WIE [[drei-orte-eine-anzeige]], nur zeitlich:
      EINE Sache (welcher Tipp ist der juengste) an zwei Stellen — einmal
      vergeben, einmal abgelesen — und dazwischen darf nichts passieren.
      Genau das laesst sich nicht garantieren.

      ── UND DIE ZWEITE HAELFTE: `return 'ok'` BEI ABBRUCH ─────────────────
      Am 05.08.2026 bekam die Anhaeng-Schleife eine Bremse
      ([[anhaengen-startet-die-zweite-tonmaschine]]): sie bricht ab, sobald
      `startLauf` weitergezaehlt hat. Sie gab dabei `'ok'` zurueck. Fuer den
      Ton war das richtig — es gingen keine `ardqueue` mehr hinaus. Fuer den
      NACHLAUF war es falsch: `ardSendungAb` liest `'ok'` als „gestartet" und
      springt. Eine Reparatur an der einen Haelfte machte die andere
      sichtbar.

      ── WIE ES JETZT AUSSIEHT ─────────────────────────────────────────────
          const meins = laufBeginnen()              // VERGEBEN, nicht lesen
          const raus = await albumSpielen(w, …, meins)
          if (raus !== 'ok') return raus            // 'abgeloest' faellt hier
          await titelUndStelle(nr, prozent, meins)

      `laufBeginnen()` ist die einzige Stelle, die `startLauf` hochzaehlt.
      `starte(w, befehl, lauf)` und `albumSpielen(…, lauf)` nehmen eine schon
      vergebene Nummer entgegen und vergeben nur dann selbst, wenn keine kommt
      — die Aufrufer ohne Nachlauf (Raster, Lane) bleiben unveraendert.

      ── WORAUF ES SICH SONST NOCH AUSWIRKT ────────────────────────────────
      Derselbe Ablauf gibt es fuer JELLIFYN-Alben: `weiterSpielen` ->
      `albumSpielen` -> `titelUndStelle`. Der Fehler war dort derselbe und ist
      mit derselben Zeile weg.

      NICHT betroffen ist der Abspieldienst: seine Absicherung
      ([[anhaengen-startet-die-zweite-tonmaschine]]) haengt an `currentPlayer`
      und nicht an einer Nummer der Oberflaeche. Ein `tracknr:` wird von ihm
      nur bei laufendem mpv ausgefuehrt — bei einem Spotify-Start verpufft es
      also. Es trifft dafuer JEDE mpv-Wiedergabe: lokal, Jellyfin, RSS, ARD.
    siehe:
      - anhaengen-startet-die-zweite-tonmaschine
      - ard-folgenliste-rollt
      - weiterhoeren-stop-und-start-im-wettlauf
      - drei-orte-eine-anzeige

  - id: ard-stelle-ueberlebt-den-quellenwechsel
    kind: falle
    title: "Eine ARD-Stelle ueberlebte den Quellenwechsel als NUMMER — Kapitel 7 des fremden Werks"
    match: "stelleGiltFuer|VON_VORN|fortsetzenMit titelNr|ard verschmolzen weiterhoeren|ARD Stelle fremde Quelle"
    sev: mittel
    cause: >
      `fortsetzenMit` bringt eine gemerkte Stelle in die Einheit der Maschine,
      die gleich spielt — die POSITION also. Die TITELNUMMER reiste dabei
      ausdruecklich unveraendert mit, mit der Begruendung „es ist DASSELBE
      Album, sonst waere es nicht verschmolzen worden". Fuer eine ARD-Stelle
      gilt das nicht: hinter einer Sendung steht eine ROLLENDE Folgenliste, die
      sich mit keiner fremden Liste deckt. Wird die Kachel mit einem
      Spotify-Werk verschmolzen, ging aus „Folge 7 der ARD-Sendung" ein
      `abspielBefehl(w, 7, …)` auf das SPOTIFY-Werk hervor.
    fix: >
      `stelleGiltFuer(zeile, dienst)` in weiterhoeren.ts beantwortet die Frage
      VOR der Einheit: traegt die Zeile eine Folgenkennung (`folge`), gilt sie
      nur, solange auch `ard` spielt. Sonst geht `VON_VORN` hinaus — titelNr 0,
      keine Position, kein Anteil. Angewandt an genau einer Stelle
      (`GET /api/weiterhoeren` in server.ts). Geprueft in weiterhoeren.spec.ts
      und, ueber den ganzen Weg, in ard.integration.spec.ts („VERSCHMOLZEN MIT
      SPOTIFY").
    tags: ['falle', 'ard', 'verschmelzung', 'weiterhoeren', 'resume', 'gegenlesen', 'e4', 2026]
    source: "06.08.2026 beim Gegenlesen von E4/A10; belegt mit ard.integration.spec.ts (Reparatur herausgenommen -> rot, wieder eingesetzt -> gruen)"
    body: |
      ── DIE BEGRUENDUNG STAND DA, DIE DURCHSETZUNG FEHLTE ─────────────────
      `verschmelzung.ts` begruendet ausfuehrlich, warum `ard` NICHT in
      `QUELLEN_REIHENFOLGE` steht. Punkt 3 dieser Begruendung, am 05.08.2026
      dazugeschrieben, nennt genau diesen Fall:

          „Eine verschmolzene Kachel, deren bevorzugte Quelle wechselt,
           verloere ihre Stelle STILL: die Zeile bliebe stehen, der Tipp
           finge von vorn an."

      Der Satz ist richtig — nur tat der Code die zweite Haelfte nicht. Die
      Stelle wurde nicht ganz weggenommen, sondern nur halb: `fortsetzenMit`
      nimmt ihr die POSITION (Prozent gilt bei Spotify nicht), die NUMMER blieb
      stehen.

      ── ES BRAUCHT KEINE FEHLBEDIENUNG ────────────────────────────────────
      Und das ist der Grund, warum es kein Randfall ist: `abgleich.ts`
      SCHLAEGT die Zuordnung von sich aus vor. Eine ARD-Sendung und ein
      Spotify-Hoerbuch gleichen Titels und Interpreten fallen in EINE Gruppe
      (belegt in ard.spec.ts, „faellt mit einem Spotify-Hoerbuch desselben
      Namens in EINE Gruppe"). Ein Erwachsener, der „Die Sendung mit der Maus"
      bei der ARD UND bei Spotify sieht, nimmt den Vorschlag an — er ist ja
      richtig, es IST dieselbe Sendung. Nur sind es nicht dieselben Folgen in
      derselben Reihenfolge.

      ── WAS DABEI HERAUSKAM ───────────────────────────────────────────────
          gemerkt      ARD, Folge 16400000, Warteschlangennummer 2, 41,5 %
          verschmolzen mit dem Spotify-Zwilling, quellen[0] = spotify
          Zeile        quelle 'spotify', positionProzent null … titelNr 2
          Tipp         abspielBefehl(w, 2, 0) -> Kapitel 2 DES SPOTIFY-WERKS

      Kein Fehler auf dem Schirm, keine Zeile im Protokoll. Fuer ein Kind
      klingt es nach kaputt.

      ── WARUM DIE ANTWORT „VON VORN" HEISST UND NICHT „ZEILE WEG" ─────────
      Die Zeile stehenzulassen ist richtig: es gibt etwas, das angefangen
      wurde, und die Kachel spielt. Nur die STELLE darin ist wertlos. Eine
      Folge noch einmal zu hoeren kostet ein Kind nichts; in einer fremden
      Folge zu landen ist der teure Fehler. Dieselbe Regel wie bei
      [[weiterhoeren-playlist-position]]: im Zweifel lieber von vorn.

      ── WO DIE REGEL STEHT ────────────────────────────────────────────────
      In weiterhoeren.ts neben `fortsetzenMit`, nicht in server.ts. Es sind
      zwei Haelften EINER Frage — „gilt die Stelle?" und „in welcher Einheit?"
      —, und die zweite ist ohne die erste sinnlos. `fortsetzenMit` selbst
      wurde NICHT umgebaut: ihre Antwortform ist an elf Stellen geprueft, und
      ein neues Feld darin haette elf Zusicherungen angefasst, um eine Regel
      unterzubringen, die gar nicht von der Einheit handelt.
    siehe:
      - ard-folgenliste-rollt
      - weiterhoeren-playlist-position
      - quellen-verschmelzen

  - kind: referenz
    id: verwaltung-gliederung-stand-2026-08-04
    title: "Die Gliederung der Verwaltung, Stand 04.08.2026 — 14 Reiter, 2 Unterseiten, 31 Konfigurationsfelder"
    match: welche Seiten hat die Verwaltung|wieviele Reiter|Bestandsaufnahme Verwaltung|verwaltung-gliederung
    tags:
      - admin
      - verwaltung
      - gliederung
      - werkzeug
      - referenz
      - 2026
    source: 04.08.2026, erhoben mit tools/verwaltung-gliederung.mjs, gegengeprueft am Geraet (Box .169, Bundle vom 04.08. 22:19)
    body: |
      NEUES WERKZEUG: `node tools/verwaltung-gliederung.mjs [--alle|--json]`.
      Es liest Kopfleiste, Routen, Abschnitte, Bedienelemente und das
      Token-Inventar aus DENSELBEN Quellen wie die Oberflaeche — und aus
      `konfiguration.ts`, weil die Felder dort in KEINER Vorlage stehen. Wer
      nur die Vorlagen liest, haelt /konfiguration, /darstellung und /streaming
      fuer leer ([[attrappe-luegt-durch-weglassen]] in anderer Gestalt).

      ── DER STAND, DEN ES MELDET ─────────────────────────────────────────
      14 Reiter in der Kopfleiste (rahmen.ts:49-83), 17 Seiten im Router,
      2 Unterseiten (Doppelte /verschmelzung, Interpreten /interpreten — beide
      unter Medien), 1 Seite vor dem Tor (Anmeldung). 13 Kacheln auf der
      Uebersicht, eine je Reiter ausser der Uebersicht selbst; die beiden
      Unterseiten haben KEINE Kachel, sie haengen allein an medien.ts:393/397.
      31 Konfigurationsfelder, verteilt auf drei Seiten: 21 Konfiguration,
      4 Darstellung, 6 Streaming.

      ── DREI WUENSCHE WAREN AM 04.08. SCHON ERFUELLT ─────────────────────
      Wer die Sitzungsnotizen liest, sucht sonst nach Arbeit, die getan ist.
      GEPRUEFT IM QUELLTEXT UND IM AUSGELIEFERTEN BUENDEL auf der Box:
        * Schlummer-Timer NICHT mehr auf der Systemseite — er steht unter
          Kinderzeit (kinderzeit.ts:267). Im Buendel der Box liegt
          „Schlummer-Timer" im Kinderzeit-Stueck, der System-Teil enthaelt
          das Wort null mal.
        * „Medien neu einlesen" NICHT mehr auf der Systemseite — medien.ts:423.
        * Doppelte und Interpreten NICHT mehr in der Kopfleiste — Unterseiten
          von Medien. Das ausgelieferte Rahmen-Stueck kennt 13 Wege, die
          beiden sind nicht darunter.

      ── ZWEI VERWEISE, DIE EIN UMZUG STEHENGELASSEN HAT ──────────────────
      Beide sind die Bauart aus [[umzug-laesst-die-verweise-darauf-zurueck]]:
      nichts ist rot, es stimmt bloss nicht mehr.
        1. konfiguration.ts:90-95 — der Bereich heisst „Medien und Vorlesen"
           und sein Hinweis endet mit „in welcher Sprache sie liest". Seit
           dem 04.08.2026 ist „Sprache beim Vorlesen" ERSATZLOS entfallen
           (Begruendung an Ort und Stelle, konfiguration.ts:295-324). In dem
           Bereich stehen nur noch zwei Felder, und keins hat mit Vorlesen zu
           tun: „Medien pruefen alle … Sekunden" und „Zuletzt Gehoertes
           merken". Die Ueberschrift verspricht eine Einstellung, die dort
           nie war und anderswo auch nicht mehr so heisst.
        2. Derselbe Kommentar (konfiguration.ts:309) sagt, die Suche finde
           die Stimme ueber „Stimme", „Sprache", „Vorlesen". „Sprache" findet
           sie NICHT: das Wort kommt im ganzen Suchbestand nicht vor, und der
           HINWEIS eines Bereichs wird gar nicht erhoben — suche.ts:286 nimmt
           vom Bereich nur den `titel`. Nachgezaehlt, nicht vermutet.

      ── WAS DIE AUFNAHME AUSSERDEM ZEIGT (E7b/L2, erstmals mit Zahlen) ────
      Harte Farben neben den CSS-Variablen, je Seite:
      Medien 41, MuPiHAT 39, Vorlesen 18, Bluetooth 17, Kinderzeit 9,
      Darstellung 8, Interpreten 3, Protokolle 2 — die uebrigen neun Seiten 0.
      Und eine Formfrage, die ein naiver Zaehler verschluckt: 16 Seiten
      schreiben `styles: BACKTICK`, mupihat.ts:491 schreibt `styles: [BACKTICK`.
      Wer nur die erste Form kennt, meldet ausgerechnet die Seite mit den
      zweitmeisten eigenen Farben als „0 harte Farben".

      Zwei Seiten tragen ueberhaupt keine Zwischenueberschrift und sind
      trotzdem lang: Kinderzeit (497 Zeilen) und Streaming-Dienste (417).
      Fuer den Suchbestand heisst das: jeder ihrer Eintraege steht mit LEEREM
      Abschnitt da.
    siehe:
      - umzug-laesst-die-verweise-darauf-zurueck
      - unterseite-ohne-rueckweg
      - verwaltung-suche-bestand-aus-den-seiten
      - admin-gliederung-leitfrage

  - id: backlog-statuswoerter-zaehlbar
    kind: entscheidung
    title: "Sieben Statuswoerter im Backlog — und warum `grep -c OFFEN` dort nicht zaehlt"
    match: "Backlog zaehlen|Statuswort|ERLEDIGT oder FERTIG|wie viel ist offen|backlog-status-schau"
    sev: mittel
    cause: >
      BACKLOG.md fuehrte zehn Woerter fuer zwei Zustaende: FERTIG, ERLEDIGT,
      BEHOBEN, ENTSCHIEDEN, GEBAUT, GELOEST, DA, BEANTWORTET, HALB, TEILWEISE,
      ZURUECKGESTELLT und „AN DER URSACHE BEHOBEN". Wer wissen wollte, wie viel
      noch aussteht, musste raten oder alles lesen. Und eine Zaehlung ueber
      `grep` faellt zusaetzlich auf ZITATE herein — E12/X2 traegt FERTIG und
      enthaelt im Begruendungstext das Wort „OFFEN".
    fix: >
      Sieben Woerter, jedes Item traegt genau eines, und das Statuswort ist das
      ERSTE Wort der Statuszelle — alles danach ist Begruendung. Gezaehlt wird
      mit `python3 tools/backlog-status-schau.py`; es liest nur die
      Statusspalte von Tabellen und davon nur das erste Wort. `--pruefen`
      faellt, sobald jemand ein achtes Wort einfuehrt.
    tags: ['backlog', 'prozess', 'entscheidung', 'werkzeug', 2026]
    source: "Backlog-Abgleich 04.08.2026 (nachts), Aufgabe #35; Werkzeug tools/backlog-status-schau.py"
    body: |
      ── DIE MENGE, UND WAS SIE ABLOEST ──────────────────────────────────
        OFFEN          beschrieben, noch nicht begonnen (auch: bewusst
                       zurueckgestellt — der Grund steht daneben)
        LAEUFT         in Arbeit
        TEILERLEDIGT   teils gebaut, UND der Rest ist benannt
        BLOCKIERT      wartet auf eine Auskunft oder ein anderes Item
        ENTSCHEIDUNG   braucht eine Wahl des Betreibers, kein Code
        FERTIG         gebaut, entschieden oder behoben
        VERWORFEN      bewusst nicht gemacht

        ERLEDIGT BEHOBEN ENTSCHIEDEN GEBAUT GELOEST DA BEANTWORTET
        „AN DER URSACHE BEHOBEN"                            -> FERTIG
        HALB TEILWEISE                                      -> TEILERLEDIGT
        ZURUECKGESTELLT „BENANNT, NICHT BEHOBEN"            -> OFFEN
        GESTRICHEN ENTFAELLT                                -> VERWORFEN

      ANNAHME ist KEIN Status, sondern eine Marke im Text („nur an der
      eingeschalteten Box pruefbar"). Sie steht neben dem Satz, fuer den sie
      gilt, nicht in der Statusspalte.

      ── WARUM DIE UNTERSCHEIDUNG FERTIG/TEILERLEDIGT DIE TEUERSTE IST ────
      TEILERLEDIGT heisst NUR dann TEILERLEDIGT, wenn der Rest BENANNT ist.
      Sonst ist es ein FERTIG mit schlechtem Gewissen und deckt genau die
      Luecke zu, die [[backlog-fertig-verdeckt-die-eigenen-reste]] beschreibt.
      Beispiel aus demselben Abgleich: E24 stand auf „AN DER URSACHE BEHOBEN",
      obwohl O4 fuer die klassische Oberflaeche und O6 offen sind — jetzt
      TEILERLEDIGT, mit beiden Resten in derselben Zeile.

      ── WAS DIE ZAHL IST UND WAS NICHT ──────────────────────────────────
      Sie zaehlt TABELLENZEILEN, nicht Aufwand. E28 bringt fuenfzehn
      OFFEN-Zeilen mit, weil dort jede Frage eine eigene Zeile bekam; E9/V6
      ist eine Zeile und traegt mehr Arbeit als die halbe N-Reihe. Und was nur
      im Fliesstext steht (E16s „bleibt das Regal?", E19), zaehlt gar nicht
      mit — die Zaehlung ist eine untere Schranke des Offenen, nie eine obere.

      Stand 04.08.2026 nachts, nach dem Abgleich:
        176 Statuszellen   FERTIG 84  VERWORFEN 5   -> ist zu 89
                           OFFEN 66  TEILERLEDIGT 16
                           BLOCKIERT 3  ENTSCHEIDUNG 2 -> steht aus 87
    siehe:
      - backlog-fertig-verdeckt-die-eigenen-reste
      - backlog-veraltet-nach-oben-nicht-nach-unten

  - id: backlog-veraltet-nach-oben-nicht-nach-unten
    kind: falle
    title: "Ein Backlog in einem schnellen Projekt veraltet IMMER in dieselbe Richtung — es meldet Arbeit, die getan ist"
    match: "Backlog stimmt nicht|Punkt steht auf OFFEN ist aber fertig|X3 Boot-Animation offen|was soll ich als naechstes"
    sev: high
    cause: >
      Wer etwas baut, schreibt es an der Stelle auf, an der er gerade steht —
      im Wiki, im Kommentar, im Commit, im Epic, das er anfasst. Der Punkt in
      einem ANDEREN Epic, der dieselbe Sache unter anderer Nummer fuehrt,
      bleibt stehen. Also veraltet ein Backlog nicht zufaellig in beide
      Richtungen, sondern systematisch in EINE: es meldet Arbeit, die es
      nicht mehr gibt.
    fix: >
      Beim Abgleich JEDEN OFFEN/TEILERLEDIGT-Punkt gegen Code UND Geraet
      halten, nicht nur die, bei denen man etwas vermutet. Und beim Bauen die
      Gegenrichtung mitpruefen: „Unter welcher ANDEREN Nummer steht das noch?"
      — Querverweise wie „(=S2)" oder „(=P6)" im Punkttitel sind der Hinweis,
      dass es zwei Zeilen fuer eine Sache gibt.
    tags: ['backlog', 'prozess', 'falle', 'abgleich', 2026]
    source: "Backlog-Abgleich 04.08.2026 (nachts), Aufgabe #35 — neun Punkte geprueft und alle neun in dieselbe Richtung falsch"
    related: [backlog-fertig-verdeckt-die-eigenen-reste]
    body: |
      DIE GEGENRICHTUNG STEHT NEBENAN. Dieser Eintrag zaehlt Punkte, die
      OFFEN sagen und fertig sind. [[backlog-fertig-verdeckt-die-eigenen-reste]]
      zaehlt den umgekehrten Fall: eine Zeile auf FERTIG, unter der eine
      beschlossene, nie gebaute Sache liegt. Trennfrage ist der Status der
      Zeile, nicht das Gefuehl — OFFEN prueft man gegen den Code, FERTIG
      gegen die Entscheidungstabelle daneben.

      Die Aussage „veraltet IMMER in dieselbe Richtung" gilt fuer die
      STATUSZELLEN, die hier gezaehlt wurden. Sie gilt NICHT fuer den Inhalt
      eines als fertig markierten Punktes; genau dort sitzt der andere Fall.

      ── DER BEFUND, GEZAEHLT ────────────────────────────────────────────
      Am 04.08.2026 wurde jeder OFFEN- und TEILERLEDIGT-Punkt in BACKLOG.md
      einzeln gegen den Commit-Stand und die Box .169 gehalten. NEUN Punkte
      waren aelter als der Code — und alle neun in derselben Richtung. KEIN
      einziger meldete umgekehrt „fertig" fuer etwas Ungebautes.

        E12/X3   Boot-Animation      OFFEN, aber = E11c/S2, seit 04.08. FERTIG
        E1/M1-M3 Baseline            3x OFFEN, aber in E5/B1+B2 gemessen —
                                     und dort schon als „M1 ✔" abgehakt
        E20/O1   Spielt-Marke oben   offen, aber gebaut (dataset.spielt)
        E20/O2   Kopfzeile+Rueckweg  offen, aber gebaut (#zurueck, z-index 9)
        E24/O4   mpv wartet          „NICHT repariert", aber Commit 1be88f7f
        E21/F3/P6 librespot          OFFEN, aber L1/L3/L4/L6 fertig
        E8       roter Pruefschritt  offener Befund, aber behoben

      ── DIE ZWEI BAUARTEN DAHINTER ──────────────────────────────────────
      1. EIN ITEM, ZWEI NUMMERN. X3 „(=S2)", P6 „(=X1)", F3 „-> E21". Wer S2
         fertigstellt, macht das im E11c-Abschnitt — X3 in E12 sieht er nicht.
         Der Querverweis im Titel ist genau die Stelle, an der man beim Bauen
         nachziehen muesste, und genau die, die man nicht liest.
      2. ABHAENGIGKEIT ABGEHAKT, EIGENE ZEILE NICHT. E5/B1 fuehrte „M1 ✔" als
         erfuellte Abhaengigkeit, waehrend M1 in E1 auf OFFEN stand. Das ist
         der leiseste Fall: das Dokument WIDERSPRICHT SICH SELBST und beide
         Haelften sehen fuer sich stimmig aus.

      ── WARUM DAS TEURER IST ALS ES KLINGT ──────────────────────────────
      Der Schaden faellt beim naechsten Anlauf an: Wer nach der naechsten
      Aufgabe sucht, nimmt einen OFFEN-Punkt und faengt an. An neun Stellen
      haette er etwas gebaut, das schon dasteht — im schlimmsten Fall ein
      ZWEITES Mal und anders ([[drei-orte-eine-anzeige]]).

      ── DIE PROBE ───────────────────────────────────────────────────────
        python3 tools/backlog-status-schau.py --offen
      und dann jede Zeile am Code oder am Geraet nachsehen. Die Zeit dafuer
      ist die Zeit, die sonst ein einziger doppelt gebauter Punkt kostet.
    siehe:
      - backlog-statuswoerter-zaehlbar
      - backlog-fertig-verdeckt-die-eigenen-reste
      - drei-orte-eine-anzeige

  - kind: falle
    id: php-verwaltung-regelte-nichts-weil-die-sitzung-fehlte
    sev: high
    title: "`sudo su dietpi -c` reicht nicht: ohne XDG_RUNTIME_DIR regelt auch die richtige uid nichts"
    match: "Host is down|Connection refused|Lautstaerke tut nichts|mupi.php|www-data|XDG_RUNTIME_DIR|su dietpi"
    cause: "PipeWire ist ein BENUTZERdienst. `sudo` setzt die Umgebung zurueck (Defaults env_reset), und `su ohne -` traegt XDG_RUNTIME_DIR nicht nach — der ALSA-Umweg ueber ctl.!default findet dann keinen Socket, obwohl die uid stimmt."
    fix: "Die Adresse der EIGENEN Sitzung nachtragen: `[ -z \"$XDG_RUNTIME_DIR\" ] && [ -w /run/user/$(id -u) ] && export XDG_RUNTIME_DIR=/run/user/$(id -u)`. Steht seit 05.08.2026 in scripts/mupibox/mupi-lautstaerke.sh (sitzung_sichern) — damit ist es EINMAL geloest statt in jedem Aufrufer."
    tags: [ton, pipewire, lautstaerke, php, verwaltung, sitzung, e12, 2026]
    source:
      - gemessen am 2026-08-04/05 an der Box 192.168.178.169 (Pi 5, DietPi Trixie, PipeWire)
      - Werkzeug: lautstaerke-aus-fremder-sitzung.sh (stellt die Wege nach und liest nur)
      - BACKLOG E12/X13(a)
    body: |
      DER EINTRAG [[amixer-master-wirkt-unter-pipewire]] sagt: als root regelt
      auf einer PipeWire-Box NICHTS. Der naheliegende Schluss daraus war
      falsch — es liegt nicht am BENUTZER allein, sondern an der ADRESSE der
      Sitzung. Am selben Geraet gemessen:

        sudo su dietpi -c '… backend'                        -> keiner
        sudo su dietpi -c 'XDG_RUNTIME_DIR=/run/user/1000 …' -> pipewire, get 40

      In BEIDEN Faellen ist die uid 1000, also der richtige Benutzer. Der
      einzige Unterschied ist XDG_RUNTIME_DIR. `sudo` raeumt die Umgebung ab
      (Defaults env_reset in /etc/sudoers), und `su BENUTZER -c` (ohne `-`)
      setzt HOME/USER/LOGNAME, aber NICHT XDG_RUNTIME_DIR.

      WAS DAS FUER mupi.php HIESS. Die alte PHP-Verwaltung rief
        sudo su dietpi -c '/usr/bin/amixer sget Master | grep "Right:" | …'
        sudo su dietpi -c '/usr/bin/pactl set-sink-volume @DEFAULT_SINK@ N%'
      Ergebnis am Geraet: „amixer: Mixer attach default error: Host is down"
      bzw. „Connection refused". Die Anzeige blieb leer, der Schieberegler tat
      NICHTS — und niemand sah einen Fehler, weil PHP mit `exec()` nur die
      letzte AUSGABEZEILE nimmt und die Meldung nach stderr geht.

      DER MERKSATZ: „laeuft als dem richtigen Benutzer" ist NICHT dasselbe wie
      „ist in dessen Sitzung". Wer eine Tonfrage stellt, muss BEIDES pruefen —
      `id -u` UND `echo $XDG_RUNTIME_DIR`.

      WO DIE LOESUNG HINGEHOERT: nicht in den Aufrufer. mupi-lautstaerke.sh ist
      der Flaschenhals, durch den alle muessen (dort sitzt auch die Obergrenze
      `mupibox.maxVolume`), also traegt es die Adresse selbst nach. Was es
      ABSICHTLICH NICHT tut, ist den BENUTZER wechseln: wer als root
      hereinkommt (mqtt.py, shutdown_sound.sh, off_trigger.sh), bekommt weiter
      `backend -> keiner`. Ein `runuser` dorthin waere eine Entscheidung ueber
      Rechte UND ueber drei Dienste, die heute stillschweigend nichts tun und
      danach ploetzlich etwas taeten — das gehoert dem Betreiber
      (BACKLOG E12/X13(d)). /run/user/0 gibt es auf dieser Box nicht,
      nachgesehen.

      GEGENPROBE, DIE MAN NICHT VERGESSEN DARF: die Dienste, die es schon
      richtig machen, duerfen sich nicht aendern. mupibox-player.service und
      mupibox-server.service laufen als `User=dietpi` und haben
      XDG_RUNTIME_DIR=/run/user/1000 im /proc/<pid>/environ — fuer sie ist der
      Zusatz ein Nichtstun. Nachgemessen: get 40, up 45, down 40, wpctl 0.40.

      NOCH EINE STELLE, DIE MAN LEICHT UEBERSIEHT: mupi.php kann nur dann
      ueberhaupt `sudo` rufen, wenn /etc/sudoers.d/www-data existiert.
      autosetup.sh legt es an (Zeile ~744, „www-data ALL=(ALL:ALL) NOPASSWD:
      ALL"). Auf der Box .169 FEHLT es — dort ist die alte PHP-Verwaltung
      ohnehin abgeschaltet (lighttpd und php8.4-fpm sind disabled/inactive,
      Port 80 tot). Wer die Seite dort messen will, misst also erst einmal gar
      nichts. Der Weg, der trotzdem etwas beweist: die Seite mit `php <datei>`
      im Kommandozeilen-PHP rendern und die Zahl im `<output>` gegen
      `wpctl get-volume` halten (so gemacht: beide 40).
    related:
      - amixer-master-wirkt-unter-pipewire
      - mupi-lautstaerke-stand-2026-07-27
      - su-anmeldeshell-im-dienst

  - kind: falle
    id: schalter-zeigt-aus-und-die-einheit-laeuft
    sev: medium
    title: "In der Dienstliste steht »aus«, und die Einheit laeuft — `Wants=` in einer FREMDEN Unit"
    match: "eingeschaltet.*false.*active|disabled.*aber laeuft|Wants=|alsa-init|Schalter tut nichts"
    cause: "`eingeschaltet` kommt aus UnitFileState (enabled/disabled), `zustand` aus ActiveState. Ein `Wants=` in einer ANDEREN Unit zieht eine disabled Unit trotzdem hoch — beide Angaben sind richtig und widersprechen sich trotzdem."
    fix: "Den Grund an die Zeile schreiben (HINWEIS in src/backend-api/src/dienste.ts), nicht die Angabe faelschen. Wirklich stilllegen kann nur, wer die `Wants=`-Zeilen entfernt."
    tags: [dienste, systemd, verwaltung, ton, e12, 2026]
    source:
      - gemessen am 2026-08-04/05 an der Box 192.168.178.169
      - systemctl is-enabled/is-active; systemctl show -p Wants; curl localhost:8200/api/dienste
      - BACKLOG E12/X13(b)
    body: |
      AM GERAET, AN EINEM TAG, IN DIESER REIHENFOLGE GEMESSEN:

        systemctl is-enabled mupibox-alsa-init.service   -> disabled
        systemctl is-active  mupibox-alsa-init.service   -> active (exited)
        systemctl show mupibox-player.service -p Wants   -> mupibox-alsa-init.service
        systemctl show mupibox-server.service -p Wants   -> mupibox-alsa-init.service
        curl localhost:8200/api/dienste                  -> {"name":"mupibox-alsa-init.service",
                                                            "eingeschaltet":false,
                                                            "zustand":"active", …}

      Die Verwaltung zeigt daraufhin einen Schalter auf „aus" neben einer
      Einheit, die laeuft. Wer ihn umlegt, glaubt etwas abgeschaltet zu haben
      und hat nichts getan — `disable` nimmt nur den Verweis aus
      multi-user.target.wants.

      WARUM DAS HIER MEHR IST ALS EIN SCHOENHEITSFEHLER: init-alsa-softvol.sh
      ruft `amixer -q sset Master 70%` OHNE `-c`. Unter PipeWire zeigt
      ctl.!default auf die PipeWire-Senke — der Aufruf trifft also die ECHTE
      Lautstaerke und geht dabei an `mupibox.maxVolume` vorbei
      ([[amixer-master-wirkt-unter-pipewire]]).

      WAS NICHT GEMACHT WURDE UND WARUM: die beiden `Wants=`-Zeilen stehen in
      mupibox-player.service und mupibox-server.service, und die beiden
      Unit-DATEIEN liegen NICHT im box-Repo (config/services/ kennt sie nicht),
      sondern im remote-step-installer. Von hier aus laesst sich der Zug also
      nicht loesen — nur BESCHREIBEN. Der Hinweis steht seit 05.08.2026 in
      src/backend-api/src/dienste.ts (HINWEIS) und nennt den Grund beim Namen,
      damit niemand am falschen Schalter zieht.

      UND WARUM NICHT „ABGELOEST": auf einem Pi 4 ohne PipeWire legt die
      Einheit den softvol-Regler an, den der Abspieldienst dort braucht.
      Dieselbe Regel wie bei pulseaudio — „abgeloest" waere auf der einen Box
      wahr und auf der anderen die Aufforderung, sich den Ton wegzuschalten.
    related:
      - alsa-init-laeuft-obwohl-abgeschaltet
      - amixer-master-wirkt-unter-pipewire
      - dienst-abgeloest-gegen-auf-dieser-box-aus

  - kind: entscheidung
    id: windows-freigabe-war-nie-da
    title: "smbd stand seit jeher in autosetup — samba wurde nie installiert. Herausgenommen, statt es einzuschalten"
    match: "smbd|samba|smb.conf|smbpasswd|Dateifreigabe|Windows-Netz"
    tags: [autosetup, ausrollweg, dienste, samba, entscheidung, e12, 2026]
    source:
      - gemessen am 2026-08-04 an der Box 192.168.178.169
      - dpkg -l | grep samba; ls /etc/samba; systemctl is-enabled smbd
      - BACKLOG E12/X13(c)
    body: |
      DREI STELLEN IN autosetup/autosetup.sh gehoerten zur Windows-Freigabe,
      und ALLE DREI sind seit jeher fehlgeschlagen:

        :452  mv -f config/templates/smb.conf /etc/samba/smb.conf
        :731  (echo mupibox; echo mupibox) | smbpasswd -s -a dietpi
        :972  systemctl enable smbd.service   (in der Einschaltschleife)

      AM GERAET NACHGESEHEN (04.08.2026, Box .169):
        systemctl is-enabled smbd   -> not-found
        ls /etc/samba               -> No such file or directory
        dpkg -l | grep samba        -> nur samba-libs (Abhaengigkeit), kein samba

      UND IM REZEPT SELBST: `packages2install` enthaelt kein samba, und es gibt
      keinen `dietpi-software`-Aufruf dafuer (die verwendeten IDs sind 5, 84,
      89, 113, 200). Es hat also NIE eine Freigabe gegeben — nicht „sie ist
      kaputtgegangen", sondern „sie war nie da". Gesehen hat es niemand, weil
      alle drei Zeilen nach >&3 schreiben.

      ── DIE ENTSCHEIDUNG: HERAUSGENOMMEN ──────────────────────────────
      Nicht, weil eine Freigabe schlecht waere, sondern weil das Einschalten
      nicht EINE Zeile waere: ein Paket, ein Dienst, der auf JEDER frisch
      bespielten Karte ins Netz horcht, eine SCHREIBBARE Freigabe auf
      /home/dietpi/MuPiBox/media und ein Passwort — das im Quelltext stand
      („mupibox"). Wem die Box gehoert, entscheidet so etwas, nicht das
      Rezept. Die Vorlage config/templates/smb.conf bleibt liegen; sie ist der
      halbe Rueckweg.

      DIE FRAGE AN DEN BETREIBER steht in BACKLOG E12/X13(c): soll die Box
      eine Windows-Freigabe fuer die Medien anbieten? Wenn ja, gehoert `samba`
      in `packages2install`, die Vorlage wieder nach /etc/samba, und das
      Passwort in die Einrichtung statt in den Quelltext.

      WAS ABSICHTLICH BLEIBT: `smbd.service` und `nmbd.service` in
      ERLAUBTE_EXTRA (src/backend-api/src/dienste.ts). Die Liste der
      schaltbaren Dienste kommt von systemd selbst — auf einer Box ohne samba
      taucht dort nichts auf (nachgemessen: /api/dienste meldet 30 Dienste,
      keiner davon smbd). Wer samba von Hand nachinstalliert, kann es dann
      ueber die Verwaltung schalten. Das ist eine Wahlmoeglichkeit, kein
      toter Schalter.
    related:
      - dienst-abgeloest-gegen-auf-dieser-box-aus
      - tonstapel-frische-karte-pipewire-laufende-box-nicht

  - kind: falle
    id: durchgestrichenes-statuswort-zaehlt-mit
    title: "Ein ~~durchgestrichenes~~ Statuswort wurde MITGEZAEHLT — die Zahl stand falsch herum"
    match: "backlog-status-schau|Statuswort zaehlen|durchgestrichen|~~FERTIG~~|Backlog Zaehlung stimmt nicht"
    sev: high
    cause: >
      tools/backlog-status-schau.py liest das ERSTE Wort der Statuszelle. Um
      Auszeichnung loszuwerden, tat es `zelle.replace("~~", "")` — es nahm also
      die Streichungs-MARKE weg und liess den gestrichenen TEXT stehen.

      In BACKLOG.md steht bei E6/G4:

          ~~FERTIG 2026-08-03~~ -> **TEILERLEDIGT (korrigiert 2026-08-04).**

      Nach dem replace las das Werkzeug „FERTIG 2026-08-03 -> TEILERLEDIGT" und
      nahm das erste Wort: FERTIG. Der Punkt zaehlte als „ist zu", waehrend das
      Dokument im selben Satz das Gegenteil sagte.
    fix: >
      Die Spanne SAMT INHALT entfernen, bevor gelesen wird
      (`re.compile(r"~~.*?~~", re.DOTALL)`), dann fuehrende Reste wie „->"
      abschneiden. Und: bleibt danach NICHTS uebrig, ist die Zelle nicht leer,
      sondern unbeantwortet — sie meldet sich als „(ohne Statuswort)" und faellt
      bei `--pruefen` durch, statt still aus der Zaehlung zu fallen.
    tags: [backlog, werkzeug, zaehlen, markdown, 2026]
    source: "Gegenlesen des Backlog-Abgleichs, 04.08.2026 nachts; tools/backlog-status-schau.py"
    body: |
      „durchgestrichen" STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die
      trennt: WER LIEST DIE ZURUECKGENOMMENE STELLE?

          Ein WERKZEUG zaehlt sie mit, weil es `~~` fuer Zierat haelt
          -> DIESER Eintrag (die Zahl steht falsch herum)

          Ein MENSCH liest sie weiter, weil sie in einem Code-Block oder
          ASCII-Bild steht, wo `~~` gar nicht darstellbar ist
          -> [[korrektur-endet-am-code-block]]

      Kurz: hier ueberlebt der Widerruf die AUSZEICHNUNG, dort ueberlebt
      er den ORT. Wer nur den einen kennt, streicht sauber durch und
      glaubt, fertig zu sein.

      ── DIE REGEL DAHINTER, UND SIE IST GROESSER ALS DIESES WERKZEUG ─────
      DURCHGESTRICHENES IST ZURUECKGENOMMEN. Ein Werkzeug, das Auszeichnung
      „saeubert", loescht dabei die Bedeutung: `**` und `~~` sehen beide wie
      Zierat aus, aber `**` sagt „lies das", und `~~` sagt „lies das NICHT".
      Wer beide gleich behandelt, dreht genau die Aussagen um, die jemand
      bewusst korrigiert hat — also die interessanten.

      ── WARUM DAS SCHLIMMER IST ALS EIN AUSFALL ──────────────────────────
      Ein Punkt, der aus der Zaehlung faellt, macht die Summe zu klein und
      niemand merkt es. Ein Punkt, der mit dem ZURUECKGENOMMENEN Wert zaehlt,
      macht die Summe plausibel und falsch herum: er schiebt eine Zelle von
      „steht aus" nach „ist zu". Genau in die Richtung, in der man sich irren
      will. Vgl. [[backlog-veraltet-nach-oben-nicht-nach-unten]].

      ── GEMESSEN 04.08.2026 ───────────────────────────────────────────────
      181 Statuszellen. Vor der Behebung 90 aus / 91 zu; danach 91 / 90.
      Genau eine Zelle — aber es war die einzige mit einer Streichung, und sie
      war der einzige Punkt im Dokument, der zweimal in Folge berichtigt wurde.
    related:
      - backlog-statuswoerter-zaehlbar
      - backlog-fertig-verdeckt-die-eigenen-reste
      - backlog-veraltet-nach-oben-nicht-nach-unten
      - korrektur-endet-am-code-block

  - kind: falle
    id: messwerkzeug-ueberlebt-seine-messgroesse
    title: "Das Werkzeug las eine Konstante, die es nicht mehr gab — und schrieb NaN in den Messbericht"
    match: "NaN ms|mpv-anlauf-messen|MPV_ANLAUF_MS|Werkzeug misst nichts mehr|Konstante entfernt Werkzeug"
    sev: high
    cause: >
      tools/mpv-anlauf-messen.mjs holte sich seine Vergleichszahlen per regex
      AUS NewDesign/app.js (`const MPV_ANLAUF_MS = (\d+)`) — bewusst, damit
      niemand sie abschreibt und veralten laesst. Der Fix zu BACKLOG E24/O4
      (Commit 1be88f7f) hat die beiden Konstanten ENTFERNT: die neue
      Oberflaeche sieht mit `titelUndStelle` nach, statt nach der Uhr zu warten.

      Danach lieferte `[]`.match(...) undefined, `Number(undefined)` = NaN, und
      das Werkzeug rechnete stumm weiter. Es meldete „MPV_ANLAUF_MS aus app.js:
      NaN ms" und „zusammen 5310 ms, wo die Oberflaeche NaN ms fest absitzt".
    fix: >
      Nach dem Lesen pruefen (`Number.isFinite`) und mit einer ERKLAERUNG
      abbrechen (Ende 2), die sagt, dass das Fehlen das Ergebnis ist, nicht die
      Panne — plus ein ausdruecklicher Weg fuer den historischen Vergleich
      (`--anlauf 2000 --sprung 1500`). Und der Kopf des Berichts sagt jetzt,
      WOHER die Zahl kam: „aus app.js" waere gelogen, sobald sie von der
      Kommandozeile kommt.
    tags: [werkzeug, messen, mpv, backlog, 2026]
    source: "Gegenlesen 04.08.2026 nachts; tools/mpv-anlauf-messen.mjs, BACKLOG E24/O4"
    body: |
      `MPV_ANLAUF_MS` STEHT AUCH IN [[mpv-anlauf-wartet-auf-falschen-titel]].
      Die eine Frage, die trennt: steht im Bericht eine ZAHL oder `NaN`?
      Eine Zahl heisst, das Werkzeug misst — dann ist der Fehler an der
      Box (falscher Titel geladen), und der andere Eintrag gilt. `NaN`
      heisst, das Werkzeug hat seine eigene Messgroesse verloren; dann
      gilt dieser hier, und die Box ist unschuldig.

      ── DIE ALLGEMEINE FORM ───────────────────────────────────────────────
      „Aus dem Quelltext lesen statt abschreiben" ist die richtige Bauart — sie
      hat hier aber eine zweite Haelfte, die gern vergessen wird: WAS TUT DAS
      WERKZEUG, WENN DIE STELLE VERSCHWINDET? Ein regex, der nicht trifft, ist
      in JavaScript kein Fehler, sondern `undefined`, und daraus wird eine Zahl,
      mit der man weiterrechnen darf. Der Bericht sieht dann aus wie eine
      Messung.

      Ein Werkzeug, das eine Reparatur nachweist, ueberlebt die Reparatur —
      und ist danach das Erste, was luegt.

      ── WAS ES NICHT MISST ────────────────────────────────────────────────
      Die KLASSISCHE Oberflaeche hat dieselbe Stelle unveraendert: vier feste
      2000-ms-Fristen in src/frontend-box/src/app/player/player.page.ts
      resumePlayback() (Z. 1074/1076/1081/1091). Dieses Werkzeug haengt an
      NewDesign/app.js und sieht davon nichts. Die fuenfte 2000 derselben Datei
      (Z. 1174) gehoert zu saveResumeFiles() und ist eine andere Sache — sie
      wartet vor PlayerCmds.MAXRESUME, nicht vor einem Sprung.
    related:
      - anlauf-nachsehen-statt-warten
      - mpv-anlauf-wartet-auf-falschen-titel
      - veraltete-vorschau-meldet-falsches-rot

  - id: sicherung-mitten-in-der-wiederherstellung
    kind: falle
    title: "Der Stand entstand MITTEN im Zurueckspielen — halb neu, halb kaputt, und sah gueltig aus"
    match: "mupibox-sicherung.path|PathChanged.*mupiboxconfig|von-karte|vor-wiederherstellung|neueste"
    sev: high
    tags: ["sicherung", "wiederherstellung", "systemd", "falle", 2026]
    source: "an der Box .169 passiert und gemessen, 04.08.2026 (E29/B3)"
    cause: >
      mupibox-sicherung.path loest bei jeder Aenderung von mupiboxconfig.json
      einen Stand aus. Die Wiederherstellung schreibt ihre Dateien in
      ALPHABETISCHER Reihenfolge — `etc/mupibox/…` kommt vor
      `server/config/data.json`. Der Wachhund schnappt also genau dazwischen zu.
    fix: >
      Sperre unter /run (Klasse Sperre in mupibox-sicherung.py): waehrend einer
      Wiederherstellung legt `--anlegen` nichts an und faellt trotzdem nicht
      durch. /run ist beim naechsten Start leer — eine Sperre, die einen
      Absturz ueberlebt, waere schlimmer als keine; zusaetzlich verfaellt sie
      nach 10 Minuten.
    body: |
      DER ABLAUF, mit Uhrzeiten aus dem Protokoll:

        23:35:39.28  mupibox-wiederherstellung.service faengt an
        23:35:40.xx  mupibox-sicherung.path sieht mupiboxconfig.json sich
                     aendern und legt einen Stand an — mit der NEUEN
                     Konfiguration und der ALTEN, noch kaputten Bibliothek
        23:35:40.46  die Wiederherstellung ist fertig

      Danach war dieser Zwitter der JUENGSTE Stand. Ein Aufruf mit «neueste»
      lieferte damit eine Box, die es nie gegeben hat: 25 Eintraege in der
      Konfiguration, 20 in der Bibliothek. Genau so ist die Bibliothek der Box
      im Verlauf einer Nacht einmal von 25 auf 20 gefallen — und niemand
      haette es der Sicherung angelastet, denn sie hatte ja „funktioniert".

      DIE LEHRE, die ueber diesen Fall hinausgeht: EIN STAND AUS EINEM
      ZWISCHENZUSTAND IST SCHLIMMER ALS KEIN STAND. Kein Stand ist ein
      sichtbares Loch; ein halber sieht aus wie ein gueltiger und wird beim
      naechsten Mal ohne Zoegern eingespielt. Wo ein Beobachter (Pfad-Wachhund,
      Zeitgeber, Cronjob) einen Zustand einfriert, den ein anderer Vorgang
      gerade in mehreren Schritten umbaut, braucht es eine Sperre — nicht
      Glueck bei der Reihenfolge.

      WARUM ES NICHT AUFGEFALLEN WAERE: beim zweiten Neustart feuerte der
      Pfad-Wachhund gar nicht, weil paths.target erst NACH der
      Wiederherstellung erreicht war. Dieselbe Anlage, dasselbe Skript, anderes
      Ergebnis — ein Wettlauf, der mal so und mal so ausgeht. Wer ihn einmal
      gruen sieht, haelt ihn fuer behoben.

      NACHMESSEN:
        sudo touch /run/mupibox-sicherung-laeuft
        mupibox-sicherung.py --anlegen --grund probe   # legt nichts an, Code 0
        sudo rm /run/mupibox-sicherung-laeuft
    related:
      - aufraeum-sicherung-vor-der-entscheidung
      - mupi-konfiguration-schreiben

  - id: sicherung-ausgelassene-schluessel-nie-schreiben
    kind: knowhow
    title: "Was die Sicherung auslaesst, darf die Wiederherstellung nicht loeschen"
    match: "GEHEIM|ausgelassen|entgeheimen|zeiger_uebernehmen|refreshToken|wlan.json pw"
    tags: ["sicherung", "zugangsdaten", "wiederherstellung", 2026]
    source: "beim Bau von mupibox-sicherung.py, gegen die echte Box geprueft (04.08.2026, E29/B6)"
    body: |
      SOLANGE E29/B6 OFFEN IST, gehen Zugangsdaten NICHT in eine Sicherung.
      Auf .169 sind das neun Zeiger: die drei Spotify-Felder in
      mupiboxconfig.json, dieselben noch einmal in server/config/config.json,
      das Verwaltungspasswort, Telegram, MQTT — und, leicht zu uebersehen,
      `pw` in server/config/wlan.json: das WLAN-PASSWORT IM KLARTEXT. Eine
      Sicherung auf ein Netzlaufwerk truege den Hausschluessel mit.

      DIE UMKEHRUNG IST DER TEIL, AN DEM MAN SICH SCHNEIDET. Wer den Schluessel
      aus dem Archiv laesst und beim Zurueckspielen die ganze Datei schreibt,
      LOESCHT ihn auf der Box — die Wiederherstellung nimmt einem also genau
      das weg, was sie aus Vorsicht nicht mitgenommen hat. Aus einem
      unvollstaendigen Stand wird so ein schaedlicher.

      DIE REGEL: ein ausgelassener Schluessel wird NIE geschrieben, auch nicht
      als Platzhalter. Er behaelt den Wert, der GERADE auf der Box steht. Steht
      dort keiner, sagt der Bericht namentlich, welcher neu einzugeben ist.

      KEIN PLATZHALTER — und dafuer gibt es hier einen Beleg: `sleep null` aus
      [[conf-update-loescht-schluessel-den-noch-jemand-liest]] war genau das,
      ein Wert, den niemand gesetzt hat und den trotzdem jeder liest. Der
      Schluessel wird deshalb ENTFERNT, nicht ersetzt.

      UND: DIE SICHERUNG MUSS SAGEN, WAS IHR FEHLT. Sonst spielt jemand zurueck,
      glaubt die Box sei wieder wie vorher, und merkt es am WLAN-Ausfall im
      Urlaub. Jeder Stand traegt deshalb ein LIESMICH.txt, das die fehlenden
      Zeiger im Klartext auffuehrt.

      SO PRUEFT MAN ES, OHNE EINEN SCHLUESSEL ANZUZEIGEN: die echten Werte aus
      der laufenden Box lesen und im entpackten Archiv SUCHEN — und dazu eine
      Gegenprobe mit etwas, das drin sein MUSS (Netzname, Rechnername). Ohne
      die Gegenprobe prueft man nur, dass die Suche nichts findet.
    related:
      - mupi-konfiguration-schreiben
      - conf-update-loescht-schluessel-den-noch-jemand-liest

  - id: sicherung-rueckweg-ueber-die-karte
    kind: knowhow
    title: "Zurueckspielen ohne SSH: eine Textdatei auf der FAT-Partition"
    match: "mupibox-wiederherstellen.txt|mupibox-wiederherstellung.service|boot/firmware/mupibox-sicherung|--von-karte"
    tags: ["sicherung", "wiederherstellung", "boot", "karte", 2026]
    source: "zweimal ueber einen echten Neustart der Box .169 gefahren (04.08.2026, E29/B3)"
    body: |
      EIN RUECKWEG, DER SSH BRAUCHT, IST IM ERNSTFALL KEINER. Wenn die Box
      nichts mehr anzeigt, hat man selten einen Rechner mit eingerichtetem
      Schluessel daneben — aber immer einen Kartenleser. Auf DietPi Trixie ist
      /boot/firmware die FAT-Partition (127 MB, 94 MB frei); ein Stand wiegt
      12 KB.

      DER ABLAUF:
        1. Karte in einen beliebigen Rechner (Windows, macOS, Linux lesen FAT)
        2. im Ordner mupibox-sicherung/ liegen die letzten Staende + LIESMICH.txt
        3. eine Textdatei `mupibox-wiederherstellen.txt` ins WURZELVERZEICHNIS
           legen; Inhalt: EINE Zeile — der Dateiname eines Standes, oder
           `neueste`
        4. Karte zurueck, einschalten
        5. der Befund steht danach als `mupibox-wiederherstellen-bericht.txt`
           auf derselben Partition — auch bei schwarzem Bildschirm

      EIN ARCHIV AUF DER KARTE LOEST NICHTS AUS. Erst die Textdatei. Eine
      Sicherung, die man versehentlich einspielt, waere ein neuer Weg, Daten zu
      verlieren.

      DIE MARKE WIRD IN JEDEM FALL UMBENANNT (.erledigt-<zeit>.txt), auch wenn
      es schiefging. Sonst liefe die Wiederherstellung bei JEDEM Start wieder
      los — eine Bootschleife ist schlimmer als ein Tippfehler, und der Bericht
      nennt die vorhandenen Staende namentlich.

      GEMESSEN: Start 23:35:39.28, fertig 23:35:40.46, mupibox-server startete
      6 ms spaeter. Die Ordnung `Before=mupibox-server.service` haelt, weil
      beide im selben Boot-Vorgang stehen. WICHTIG: der Dienst muss
      EINGESCHALTET sein, bevor man ihn braucht — er tut ohne Marke nichts
      (ConditionPathExists), aber nachtraeglich einschalten kann man ihn an
      einer toten Box nicht mehr.

      WER ALS ROOT SICHERT, MUSS ZURUECKGEBEN: --von-karte laeuft als root und
      legt dabei einen Stand „vorher" in /home/dietpi an. Ohne chown gehoert er
      root, die spaetere Auslese als dietpi scheitert still, und die Zahl der
      Staende waechst wieder ins Unbegrenzte — der Zustand, gegen den E29
      angetreten ist, eingeschleppt durch den Rueckweg selbst.
    related:
      - mupi-bootwache-drehung
      - mupi-wlan-selbstdiagnose-per-karte
      - mupi-stromausfall-bootdateien

  - id: altlasten-nach-herkunft-nicht-nach-groesse
    kind: knowhow
    title: "99 beiseite gelegte Dateien, 163 MB — und 0,1 MB davon sind unersetzlich"
    match: "mupibox-altlasten.py|einordnen|nachbaubar|boxzustand|vor-aufraeumen|www.alt"
    tags: ["aufraeumen", "altlasten", "sicherung", 2026]
    source: "gemessen und gefahren an der Box .169, 04.08.2026 (E29/B5)"
    body: |
      GEMESSEN (nicht die Zahl aus dem BACKLOG — 89 / 80,2 MB liess sich nicht
      nachbauen, das Zaehlverfahren dazu ist nirgends festgehalten):

        beiseite gelegt   99      163,1 MB     26.07. bis 04.08.
          nachbaubar      82      163,0 MB     99,9 %
          boxzustand      14        0,1 MB      0,1 %
          unklar           5        0,1 MB

      DIE AUSSAGE STECKT IM VERHAELTNIS. Fast alles davon ist Bau-Ergebnis
      (server.js.*, www*/, www-admin*/, *.tgz) und aus dem Quelltext neu zu
      bauen; ein Stand vom 27.07. ist ohnehin wertlos. Unersetzlich sind die
      0,1 MB Box-Zustand. AUFGERAEUMT WIRD DESHALB NACH HERKUNFT, NIE NACH
      GROESSE — wer nach Groesse geht, raeumt genau das Falsche zuerst weg.

      DREI SPALTEN, NICHT ZWEI. Die dritte („unklar") ist die wichtigste: dort
      landete `mupi-lautstaerke.sh.vor-20260801-1822`, wo die LAUFENDE Datei
      vom Repo abweicht und niemand weiss, welche Seite vorn liegt. Raten waere
      hier teurer als liegen lassen.

      DER ORT SCHLAEGT DIE ENDUNG. Eine Regel „*.js ist Bau-Ergebnis" ist eine
      Vermutung ueber den Inhalt, der Ort ist eine Tatsache: ein
      `monitor.js.bak` in server/config ist Box-Zustand. Der Selbsttest hat
      genau diese Verwechslung gefunden, bevor sie etwas gekostet hat.

      JE STAMM BLEIBT DER JUENGSTE LIEGEN. `www.alt` ist nicht Muell, sondern
      DER RUECKWEG der letzten Auslieferung ([[ausliefern-regeln]]). Ein
      Aufraeumen, das ihn mitnimmt, entfernt ihn in dem Moment, in dem er am
      wahrscheinlichsten gebraucht wird.

      GEERNTET WIRD VOR DEM LOESCHEN, UND ENTGEHEIMT DABEI: die acht
      mupiboxconfig-Fassungen auf .169 trugen ZWEI VERSCHIEDENE
      Spotify-Dauerzugaenge. Beiseite gelegte Dateien streuen abgeloeste
      Schluessel ueber die Karte — eine Altlast ist kein Grund, laxer zu sein,
      sondern einer, genauer zu sein.

      GEFAHREN: 78 Altlasten / 155,9 MB entfernt; Bibliothek, Oberflaeche und
      Dienste danach unveraendert (25 Eintraege ueber /api/data, www/index.html
      und www-admin/index.html vorhanden).
    related:
      - www-altlasten-und-gehashte-dateinamen
      - ausliefern-regeln
      - aufraeum-sicherung-vor-der-entscheidung

  - kind: falle
    id: jellyfin-weiterhoeren-zwei-tore
    title: "Jellyfin fehlte in der Weiterhoeren-Reihe — und die Aufzaehlung war das ZWEITE Tor, nicht das erste"
    match: jellyfin weiterhoeren|jellyfin landet nicht in weiterhoeren|nichtMerkbar jellyfin|istWeiterhoerbar jellyfin
    tags:
      - jellyfin
      - weiterhoeren
      - resume
      - falle
      - messen
      - 2026
    source: 06.08.2026, am Kinderzimmer-Geraet .169 gemessen mit tools/stelle-je-dienst-am-geraet.mjs; gebaut in weiterhoeren.ts, media.service.ts, dienste-deckung.mjs; geprueft in weiterhoeren.spec.ts (5 Faelle) und mit tools/rotprobe.py
    body: |
      DIE NAHELIEGENDE ERKLAERUNG WAR FALSCH, und sie war es auf die teure
      Art: sie stimmte fast. `istWeiterhoerbar()` zaehlt die Dienste auf, die
      eine gemerkte Stelle bekommen — spotify, lokal, rss, ard —, und
      `jellyfin` fehlte dort tatsaechlich. Wer das ergaenzt, hat GAR NICHTS
      geaendert.

      ── WAS AM GERAET WIRKLICH PASSIERTE ──────────────────────────────
      Album „HAMM" ueber Jellyfin, Titel 2, 12 s gespielt, dann angehalten:

        merkStand-Koerper: {"schluessel":"jellyfin:7d9a…","titelNr":2,
                            "bisher":14.53,"dauer":200.67}
        POST /api/weiterhoeren -> 200  {"status":"nichtMerkbar"}
        resume.json UNVERAENDERT (md5 identisch, 9 -> 9 Eintraege)
        Eintraege mit type jellyfin*: 0

      `stelleAus()` (weiterhoeren.ts) fiel fuer `jellyfin` durch alle Zweige
      (spotify/lokal/rss/ard) auf `return null`. Der Server schrieb NICHTS.
      `istWeiterhoerbar` kam erst danach dran und hatte nie etwas zu filtern.
      ES SIND ZWEI TORE, und das erste steht weiter vorn als der Verdacht.

      MERKREGEL: Wenn eine Liste offensichtlich einen Eintrag zu wenig hat,
      ist die Frage nicht „was fehlt in der Liste", sondern „kommt ueberhaupt
      etwas an ihr an". Die Deckungspruefung
      (tools/dienste-deckung.mjs, [[dienstliste-an-fuenf-orten]]) sieht die
      Liste — sie kann nicht sehen, dass davor schon Schluss ist.

      ── EIGENE FELDER, UND ZWAR AUS EINEM HARTEN GRUND ────────────────
      `resumejellyfincurrentTracknr` und `resumejellyfinprogressTime`, NICHT
      `resumelocal*`. Ein Jellyfin-Album laeuft zwar ueber dieselbe Maschine
      wie ein lokales (mpv, also PROZENT —
      [[resume-lokal-ist-prozent-nicht-sekunden]]), aber der KLASSISCHE Player
      liest `resumelocalalbum` und sucht damit ein Verzeichnis auf der
      SD-Karte. Ein Jellyfin-Album hat keines; die Kachel dort faende nichts
      und finge wortlos von vorn an. Unter eigenen Namen ist die Stelle fuer
      die klassische Oberflaeche unsichtbar, und sie laesst sie zusaetzlich
      ausdruecklich aus (`media.service.ts`) — dieselbe Loesung wie bei ARD
      ([[ard-folgenliste-rollt]]).

      ES GENUEGT NICHT, `type !== 'jellyfin'` ZU SCHREIBEN: in data.json steht
      `jellyfin-album`. `dienstVon` prueft `startsWith`, die Auslassung in
      media.service.ts muss dasselbe tun.

      ── DIE NUMMER STIMMT, AUCH DAS IST GEMESSEN ──────────────────────
      Nach `tracknr:2` meldet mpv `currentTracknr: 2`, und am Titelende zaehlt
      sie OHNE jeden Befehl von aussen auf 3 weiter (`duration` und `timePos`
      gehen mit). Sie ist die Abspielposition in der Warteschlange und taugt
      als Schluessel — anders als bei Spotify, wo `track_number` die
      Album-Nummer ist ([[weiterhoeren-playlist-position]]).

      ── WAS DABEI AUFFIEL UND OFFEN BLIEB ─────────────────────────────
      Die beiden Alben liegen auf .169 DOPPELT (je einmal `jellyfin-album`,
      einmal `spotify`). Nach dem Verschmelzen fuehrt bei „HAMM" die
      Jellyfin-Kennung, bei „Die Zukunft wird gross" die Spotify-Kennung —
      BEIDE spielen ueber Jellyfin. Bei der spotify-gefuehrten Kachel schreibt
      der Server folgerichtig eine SPOTIFY-Stelle, mit mpv-Sekunden × 1000 in
      `resumespotifyprogress_ms` und der JELLYFIN-Titellaenge in
      `resumespotifyduration_ms` (gemessen: 202475 ms = auf die Millisekunde
      die `dauerMs` des Jellyfin-Titels). Kein Messartefakt — der Stand von
      VORHER trug schon 155559, die Laenge des Jellyfin-Titels „Intro".
      NICHT ANGEFASST und NICHT gemessen, ob das beim Fortsetzen sauber
      zurueckgerechnet wird. Steht als BACKLOG E32/W6; es ist eine Frage der
      Verschmelzung ([[quellen-verschmelzen]]), nicht des Weiterhoerens.
    related:
      - weiterhoeren-playlist-position
      - resume-lokal-ist-prozent-nicht-sekunden
      - ard-folgenliste-rollt
      - dienstliste-an-fuenf-orten
      - quellen-verschmelzen

  - kind: entscheidung
    id: folge-wiederfinden-drei-stufen
    title: "Kennung, dann Titel, dann Position — und Interpret und Laenge ausdruecklich NICHT"
    match: folgeWiederfinden|welche folge war das|folge neu eingesetzt|404 obwohl gelistet|resumeardfolgentitel
    tags:
      - ard
      - audiothek
      - weiterhoeren
      - resume
      - entscheidung
      - messen
      - 2026
    source: 06.08.2026, gebaut in weiterhoeren.ts (folgeWiederfinden) und server.ts (/inhalt); Zahlen aus der Messung an den 30 Maus-Folgen vom 04.08.2026; geprueft in weiterhoeren.spec.ts (5 Faelle) und ard.integration.spec.ts (3 Faelle, nock)
    body: |
      DIE KENNUNG IST DAS VERLAESSLICHE — aber sie ist es NICHT IMMER, und das
      ist gemessen: am 04.08.2026 antwortete Folge 16657073 mit 404, WAEHREND
      die Schnittstelle sie weiter auflistete. Die Audiothek nimmt Folgen
      heraus und setzt sie wieder ein, dann traegt derselbe Ton eine ANDERE
      Kennung. Fuer ein Kind sieht das aus wie „diese Folge gibt es nicht
      mehr" — fuer eine Folge, die in der Liste steht.

      ── DIE REGEL, an EINER Stelle (`folgeWiederfinden`, weiterhoeren.ts) ──

        1. DIE KENNUNG. Trifft sie, ist die Frage beantwortet; der Rest wird
           gar nicht erst gefragt.
        2. DER TITEL — erst wenn die Kennung nicht trifft. Verglichen wird
           ueber `normal()` (derselbe Kern wie im Rest des Hauses): Gross- und
           Kleinschreibung, Betonungszeichen und Satzzeichen fallen weg.
        3. DIE POSITION ALS SCHIEDSRICHTER, und NUR wenn derselbe Titel
           mehrfach dasteht. Sie entscheidet nie fuer sich allein — genau das
           waere die rollende Falle ([[ard-folgenliste-rollt]]).

      IM ZWEIFEL 0, also NICHTS. Eine falsch geratene Folge klingt fuer ein
      Kind nicht nach Fehler, sondern nach kaputt.

      ── WARUM INTERPRET UND LAENGE NICHT MITSPIELEN ───────────────────
      An den 30 Folgen der Maus-Sendung gezaehlt (04.08.2026):

        verschiedene Interpreten  1   (alle „Die Maus")
        verschiedene Laengen      1   (alle 3606000 ms — eine NENNlaenge)
        verschiedene Titel       30   (kein einziger doppelt)

      Beide haetten bei JEDEM Vergleich „passt" gesagt. Eine Pruefung, die nie
      nein sagen kann, ist keine — sie haette nur das Gefuehl erzeugt,
      mehrfach abgesichert zu sein. Und der Interpret traegt GRUNDSAETZLICH
      nichts bei: gesucht wird immer INNERHALB einer Sendung, dort ist er
      naturgemaess konstant. Der Test haelt die Signatur ausdruecklich fest,
      damit wer sie erweitert, diese Messung liest.

      ── DER TITEL IST KEIN PFLICHTFELD ────────────────────────────────
      Die KENNUNG bleibt Pflicht (ohne sie wird nichts gemerkt). Der Titel
      nicht: jede Stelle von vor dem 06.08.2026 hat ihn nicht, und eine
      brauchbare Stelle wegen einer Kosmetik wegzuwerfen waere teurer als der
      fehlende Rueckfall.

      ── DIE REGEL STEHT NUR EINMAL ────────────────────────────────────
      `/inhalt` wendet sie an und markiert die gefundene Folge mit `weiterAb`;
      die Oberflaeche FOLGT dieser Markierung (`ardSendungAb(…, ausStelle)`),
      statt selbst zu suchen. Zwei Fassungen derselben Regel liefen genau dann
      auseinander, wenn es darauf ankommt. `ausStelle` ist dabei nur wahr,
      wenn der Tipp aus der GEMERKTEN Stelle kommt — ein Tipp auf eine
      Folgenkachel meint GENAU diese Folge, dort waere ein Ausweichen ein
      stiller Fehlgriff.

      ── EINE LUECKE, DIE ERST DER TITEL GEFAEHRLICH MACHTE ────────────
      `VON_VORN` raeumte bisher Nummer und Position weg — `folge` blieb
      stehen, obwohl `stelleGiltFuer` die Stelle gerade fuer ungueltig erklaert
      hatte. Solange niemand das Feld LAS, war das folgenlos. Der Folgentitel
      wird gelesen: er steht auf der Kachel. „Der Schneemann taut" auf einer
      Kachel, waehrend die Box ein Spotify-Hoerbuch von vorn anfaengt, ist eine
      Anzeige, die luegt. Beide Felder gehen jetzt mit weg — als `undefined`,
      damit sie bei `JSON.stringify` ganz herausfallen.

      ── UND DIE TONADRESSE BLEIBT WEITER DRAUSSEN ─────────────────────
      Der Speicher haelt jetzt Kennung UND Titel; beide laufen nicht ab. Die
      Tonadressen tun es ([[ard-audiothek-schnittstelle-a1]]), und ein
      Zwischenspeicher voller abgelaufener Adressen waere genau die Falle,
      gegen die das ganze Modul gebaut ist. Der Titel kommt aus `f.titel`,
      nicht aus `abspielWeg` zurueckgerechnet.
    related:
      - ard-folgenliste-rollt
      - weiterhoeren-playlist-position
      - ard-audiothek-schnittstelle-a1

  - kind: falle
    id: cover-folgt-der-position-nicht-dem-start
    title: "Das Folgen-Cover wird NACHGESCHLAGEN, nicht beim Start gemerkt — sonst bleibt es stehen, wenn mpv von selbst weiterrueckt"
    match: cover bleibt stehen|falsches cover|sendungsbild statt folgenbild|coverAdresse mpv|folgenBildAn
    tags:
      - oberflaeche
      - neu
      - player
      - cover
      - ard
      - jellyfin
      - falle
      - 2026
    source: 06.08.2026, am Geraet .169 gemessen (Titeluebergang mitgeschrieben); gebaut in NewDesign/app.js; geprueft in tools/pruef-neu-regeln.js (11 Faelle) und tools/e2e/neu-oberflaeche.test.mjs (Browserlauf)
    body: |
      BEI mpv MELDET `/local` KEIN BILD — nur Pfad, Interpret und eine
      Warteschlangennummer. `coverAdresse` fiel deshalb auf das Bild des WERKS
      zurueck: im Player stand dreissig Folgen lang dasselbe Sendungsbild,
      waehrend die Lane daneben das Folgenbild laengst zeigte. Dieselbe
      Auskunft, zwei verschiedene Antworten ([[drei-orte-eine-anzeige]]).

      ── DIE NAHELIEGENDE REPARATUR WAERE DIE FALSCHE ──────────────────
      Naheliegend: beim Start das Bild der gestarteten Folge merken. Das geht
      genau so lange gut, bis die Wiedergabe VON SELBST weitergeht.

      AM GERAET MITGESCHRIEBEN (06.08.2026, ARD „Die Maus", 20 s vor Schluss
      eingestiegen):

        [22:51:53] WECHSEL nr 1 -> 2   (ohne jeden Befehl von aussen)
        [22:51:53] nr=2 t=1.1  dauer=3606.2
        [22:52:53] nr=2 t=60.5 dauer=3606.2

      Ein beim Start festgehaltenes Bild bliebe hier stehen — die Anzeige
      zeigte den Zustand von vorhin und SAEHE DABEI RICHTIG AUS. Das ist
      dieselbe Sorte Fehler wie F1 eine Etage hoeher.

      ── GESPEICHERT WIRD DIE LISTE, NICHT DAS BILD ────────────────────
      `laufendeFolgen = { schluessel, liste }` — und zwar GENAU die Liste, die
      `albumSpielen` anhaengt (nach `filter((t) => t.befehl)`). Nachgeschlagen
      wird bei jedem Malen ueber die Nummer, die die Maschine gerade meldet:
      `folgenBildAn(liste, nr)`, 1-basiert, ausserhalb der Liste NICHTS.

      DREI DINGE, DIE JE FUER SICH BEISSEN WUERDEN:
        * DER SCHLUESSELVERGLEICH GENUEGT NICHT. Ein verschmolzenes Werk kann
          heute ueber Jellyfin (mit Liste) und morgen ueber Spotify (ohne)
          laufen — DERSELBE Schluessel, eine Liste, die nicht mehr gilt.
          `starte()` (jeder Ein-Befehl-Start) loescht sie deshalb.
        * ERST NACH DEM START ABLEGEN, nicht davor: scheitert der Start, liefe
          sonst eine fremde Wiedergabe unter dieser Liste weiter.
        * KEIN BILD IST NICHT DER LEERE STRING. `src=""` laedt die SEITE neu;
          der Aufrufer braucht `null`, um auf die naechste Stufe zu gehen.

      ── DIE ATTRAPPE KONNTE DEN FALL BIS DAHIN GAR NICHT STELLEN ──────
      In tools/neu-vorschau.mjs war `currentTracknr` FESTGENAGELT (3), und je
      Folge fehlte das `bild`. Ein Werkzeug haette eine Anzeige gesehen, die
      sich nicht bewegt, und das fuer ein Ergebnis gehalten — waehrend die
      Frage nie gestellt wurde. Beides nachgetragen, dazu die Hebel
      `/vorschau/mpv-weiter` und `/vorschau/mpv-erste`.
      [[attrappe-luegt-durch-weglassen]] um einen Fall reicher, diesmal in der
      Sorte „fehlender ZEITLICHER VERLAUF".
    related:
      - drei-orte-eine-anzeige
      - attrappe-luegt-durch-weglassen
      - mpv-trackname-geht-nie-mit

  - kind: falle
    id: mpv-trackname-geht-nie-mit
    title: "currentTrackname folgt bei mpv NIE — es ist ein Dauerzustand, kein Nachlauf (F1)"
    match: currentTrackname|titelname bleibt stehen|falscher titelname jellyfin|F1 jellyfin titelname
    tags:
      - player
      - mpv
      - jellyfin
      - ard
      - anzeige
      - falle
      - messen
      - 2026
    source: 06.08.2026, am Kinderzimmer-Geraet .169 mitgeschrieben (ARD und Jellyfin gegengeprueft), tools/stelle-je-dienst-am-geraet.mjs. NICHT behoben.
    body: |
      GEMESSEN, UND ZWAR ZWEIMAL AN VERSCHIEDENEN QUELLEN:

        ARD (alle Folgen gleich lang, deshalb allein nicht beweisend):
          nach dem Wechsel nr 1 -> 2 stand `currentTrackname` volle 60 s
          weiter auf „Fälschung".

        Jellyfin (Titel verschieden lang — die Gegenprobe):
          [22:53:52] nr=1 t=139.1 dauer=140.4 name="Mittelmäßiges Leben"
          [22:53:54] WECHSEL nr 1 -> 2
          [22:53:54] nr=2 t=0.8   dauer=200.7 name="Mittelmäßiges Leben"
          [22:54:23] nr=2 t=29.5  dauer=200.7 name="Mittelmäßiges Leben"

      `duration` folgt, `timePos` folgt, `currentTracknr` folgt —
      `currentTrackname` NIE. Weder beim natuerlichen Uebergang noch nach
      `tracknr:`. Es ist also kein Nachlauf von ein paar Sekunden, den man
      abwarten koennte, sondern ein DAUERZUSTAND. Wer ihn als Nachlauf
      behandelt (warten, noch einmal fragen), wartet fuer immer.

      NEBENBEFUND: `album` traegt bei mpv den INTERPRETEN („Kapelle Petra",
      „Die Maus"), nicht den Albumtitel.

      FOLGE FUER DIE ANZEIGE: Der Player zeigt bei mpv ab dem zweiten Titel
      dauerhaft den falschen Titelnamen. Das ist BACKLOG F1, und es sitzt im
      ABSPIELDIENST, nicht in der Oberflaeche.

      DER WEG DAHIN IST DERSELBE WIE BEIM COVER
      ([[cover-folgt-der-position-nicht-dem-start]]): die Liste liegt in der
      Oberflaeche vor, die Position meldet die Maschine — der Name waere
      nachzuschlagen statt zu glauben. NICHT GEBAUT (Stand 06.08.2026); nur
      das Cover ist umgestellt.
    related:
      - cover-folgt-der-position-nicht-dem-start
      - drei-orte-eine-anzeige
      - mpv-media-title-traegt-je-dienst-verschieden

  - kind: falle
    id: titel-ist-keine-adresse
    title: "Eine angeklebte Bildadresse im Titel — eine Wache beim SPEICHERN, weil die Ursache nicht belegt ist"
    match: nocover_mupi.png im titel|EMMA6 - Complete|adresse im titel|titelOhneAdresse|mitGeputztemTitel
    tags:
      - medien
      - verwaltung
      - data-json
      - falle
      - 2026
    source: Befund 04.08.2026 in data.json der Box .169 (bereinigt, Sicherungen *.vor-titelputz-20260804T222712); Wache gebaut 06.08.2026 in medien.ts, angeschlossen in server.ts; geprueft in medien.spec.ts (7 Faelle) und titelwache.integration.spec.ts (4 Faelle)
    body: |
      DER ECHTE WERT, Zeichen fuer Zeichen:

        title = "EMMA6 - Completehttp://192.168.178.169:8200/assets/images/nocover_mupi.png"

      Also der Titel, OHNE Trennzeichen verklebt mit der Adresse eines
      Platzhalterbildes. Auf dem Schirm war die Kachel unlesbar — und
      `medienSchluessel()` bildet aus so einem Titel einen Schluessel, an dem
      Verlauf und Weiterhoeren haengen. Die Zeile schadet mehr, als sie
      aussieht.

      ── DIE URSACHE IST NICHT BELEGT, UND DAS BLEIBT SO ───────────────
      Im ganzen Quelltext steht fuer dieses Bild ueberall der RELATIVE Pfad
      (`assets/images/nocover_mupi.png`) und nirgends eine absolute Adresse
      mit der IP der Box. Es gibt keine Stelle, auf die man zeigen koennte.
      DESHALB KEINE URSACHENREPARATUR — eine erfundene Ursache zu „beheben"
      erzeugt nur die Gewissheit, dass es nicht wiederkommt.

      WAS SICH ZEIGEN LAESST, IST DER WEG IN DIE DATEI: `/api/add` und
      `/api/edit` legen den Rumpf der Anfrage UNVERAENDERT ab
      (`data.push(req.body)`, `data.splice(i, 1, req.body.data)`). Sie gehen
      an `neuerEintrag`/`aenderungAnwenden` vorbei. Dort liegt jetzt
      `mitGeputztemTitel`.

      ── DIE EINZIGE REGEL, DIE ZAEHLT: SIE DARF NICHTS KAPUTTMACHEN ───
      Sie schneidet NUR zusammenhaengende `http://`- und `https://`-Laeufe
      heraus und laesst alles andere Zeichen fuer Zeichen stehen.
        * Ohne `://` passiert GAR NICHTS. „AC/DC", „Die drei ???",
          „Folge 12: Der Fluch", „TKKG Junior 1/2" — unangetastet.
        * Bliebe nach dem Schnitt NICHTS uebrig, bleibt der Titel wie er war.
          Ein Eintrag, der wirklich nur eine Adresse als Namen traegt, ist
          eine Merkwuerdigkeit — aber eine SICHTBARE. Eine Kachel ohne
          Beschriftung ist auf der Box nicht wiederzufinden.
      Die Haelfte der Testfaelle prueft, dass sie NICHTS tut. Eine Wache, die
      einen legitimen Titel zerstoert, waere schlimmer als der Fehler: der
      eine Eintrag von .169 war sichtbar kaputt, ein still beschnittener Titel
      waere es nicht.

      ── BEIM SPEICHERN, NICHT BEIM LESEN ──────────────────────────────
      Was schon in data.json steht, bleibt stehen, bis jemand es anfasst. Eine
      Wache, die auch beim Lesen putzte, VERSTECKTE den Schaden, statt ihn zu
      verhindern — und man saehe nie wieder, ob es noch einmal passiert.

      ── DIE ZWEITE FRAGE IST NICHT DIE ERSTE ──────────────────────────
      `titelOhneAdresse` kann richtig rechnen und trotzdem auf keinem Weg
      liegen, der wirklich schreibt. Genau das ist am 04.08.2026 anderswo
      passiert („kopiert ist nicht aufgerufen", Commit 425dc317). Deshalb gibt
      es NEBEN dem Regeltest einen Integrationstest, der die Routen aufruft
      und auf der PLATTE nachliest — die Antwort taugt hier nicht als Auskunft
      ([[server-antwortet-200-auf-alles]]).
    related:
      - server-antwortet-200-auf-alles
      - attrappe-luegt-durch-weglassen

  - kind: howto
    id: rotprobe-war-der-test-ohne-die-aenderung-rot
    title: "tools/rotprobe.py — je EINE Zeile zuruecknehmen und verlangen, dass die Pruefung rot wird"
    match: rotprobe|test ohne die aenderung rot|bewacht der test die aenderung|gruen beweist nichts
    tags:
      - werkzeug
      - pruefen
      - tests
      - howto
      - 2026
    source: 06.08.2026 gebaut, beim Bau von BACKLOG E32 (fuenf Punkte an Weiterhoeren und am Player); 17 Eingriffe, 17-mal rot
    body: |
      EIN GRUENER TEST BEWEIST NUR, DASS ER GRUEN IST. Er beweist NICHT, dass
      er die Aenderung bewacht, fuer die er geschrieben wurde. Ein Test, der
      auch ohne sie gruen bliebe, ist schlimmer als keiner: er sieht aus wie
      eine Absicherung und ist eine Zusicherung, die niemand eingeloest hat.
      Dieselbe Familie wie [[attrappe-luegt-durch-weglassen]], nur eine Ebene
      hoeher — dort log die Attrappe, hier der Test.

      AUFRUF
        python3 tools/rotprobe.py             alle Eingriffe
        python3 tools/rotprobe.py --liste     nur zeigen
        python3 tools/rotprobe.py --nur 2     nur die Eingriffe zu Punkt 2
        python3 tools/rotprobe.py --behalten  Arbeitskopie stehen lassen

      ── WIE ES ARBEITET, UND WARUM SO ─────────────────────────────────
      1. WEGWERF-ARBEITSKOPIE (`git worktree`, losgeloest) mit dem heutigen
         Stand: `git diff HEAD` fuer verfolgte Dateien, dazu die neuen
         (unverfolgten) unter den genannten Pfaden. AM ECHTEN BAUM WIRD NICHTS
         ANGEFASST — an diesem Baum arbeitet mehr als einer, und ein Werkzeug,
         das zum Messen kurz eine Quelldatei verbiegt, verbaeje dem anderen
         seinen Testlauf.
      2. GRUNDLINIE: jede Pruefung einmal UNVERAENDERT. Eine Pruefung, die
         schon so rot ist, beweist beim Eingriff nichts — sie wird als
         UNGEPRUEFT gemeldet statt als Erfolg.
      3. JE EIN EINGRIFF: eine benannte Textstelle wird durch die Fassung VOR
         der Aenderung ersetzt, die zugehoerige Pruefung MUSS rot werden,
         danach wird zurueckgesetzt.

      DER EINGRIFF WIRD UEBER SEINEN WORTLAUT BENANNT, nicht ueber eine
      Zeilennummer (die verrutscht bei jedem Nachbarn). Kommt der Wortlaut
      nicht genau EINMAL vor, bricht der Lauf ab, statt irgendetwas zu
      ersetzen.

      ── ZWEI FALLEN, DIE BEIM BAU ZUSCHNAPPTEN ────────────────────────
      * `src/backend-api/node_modules` IST AUF DIESEM RECHNER LEER — npm hebt
        die Pakete in die WURZEL. Verlinkt man nur den unteren Ordner, faellt
        jeder Testlauf mit ERR_MODULE_NOT_FOUND aus, und das sieht in der
        Grundlinie aus wie ein kaputter Test.
      * `node --test --test-name-pattern` meldet bei einem Muster, auf das
        NICHTS passt, brav Ende 0 mit lauter uebersprungenen Faellen. Die
        Grundlinie waere gruen, der Eingriff „unbemerkt", und niemand saehe,
        dass gar nichts gemessen wurde. Das Werkzeug verlangt deshalb
        zusaetzlich `ℹ pass N` mit N > 0.

      ── WAS ES NICHT KANN ─────────────────────────────────────────────
      Es prueft die REGEL, nicht das Geraet. Und eine Aenderung, deren
      Pruefung uebersprungen wird, kann es nicht rot faerben — sie taucht als
      UNGEPRUEFT auf, statt still zu fehlen. Es ist NICHT in tools/pruefen.sh
      eingehaengt: ein Durchlauf kostet einige Minuten (die Browserfaelle
      dauern), und ein Pruefschritt, den man wegen der Dauer ueberspringt,
      wird als naechstes weggeschaut.
    related:
      - attrappe-luegt-durch-weglassen
      - server-antwortet-200-auf-alles

  - id: admin-kopfleiste-vier-gruppen
    kind: entscheidung
    title: "Vierzehn Reiter werden vier Gruppen — mit den Woertern der Leitfrage, nicht mit neuen"
    match: "gruppentitel|class=\"gruppe\"|Was da ist|Wenn etwas klemmt|OHNE_GRUPPE|gruppenLesen"
    tags: ["admin", "verwaltung", "gliederung", "entscheidung", "e7b", 2026]
    source: "05.08.2026, Wunsch #47 des Betreibers; im Browser gegen die Attrappe nachgemessen (tools/neu-vorschau.mjs + ng serve)"
    body: |
      DER SATZ DES BETREIBERS, zweimal dasselbe: „also wo sinnvoll
      unterseiten" und „das wir da mal struktur reinbekommen :)". Die
      Kopfleiste zaehlte vierzehn gleichrangige Eintraege nebeneinander. Das
      ist keine Gliederung, sondern eine Liste — man liest sie jedes Mal von
      vorn, weil nichts sagt, wo man aufhoeren darf zu suchen.

      ── DIE UEBERSCHRIFTEN SIND NICHT ERFUNDEN, SONDERN UEBERNOMMEN ──────
      Sie sind die LEITFRAGE aus [[admin-gliederung-leitfrage]], mit der seit
      dem 03.08.2026 schon die einzelnen FELDER sortiert werden — nur auf die
      SEITEN angewandt:

          Was da ist               Medien · Streaming-Dienste
          Was man sieht und hoert  Darstellung · Kinderzeit · Vorlesen
          Was die Box ist          Konfiguration · Netzwerk · Bluetooth · MuPiHAT
          Wenn etwas klemmt        Systemdienste · System · Protokolle · Aktualisierung

      WARUM NICHT „Inhalte · Technik · Wartung": das waere eine ZWEITE Vokabel
      neben der schon entschiedenen, und sie muesste erst gelernt werden. Die
      Leitfrage beantwortet man sich selbst — das ist ihr ganzer Zweck.

      Die Uebersicht (13 Kacheln) traegt DIESELBEN vier Ueberschriften in
      DERSELBEN Reihenfolge. Vorher standen ihre Kacheln in einer DRITTEN
      Ordnung, weder der der Leiste noch einer erkennbaren eigenen. Wer eine
      Gliederung baut und die Uebersicht daneben unsortiert laesst, hat keine
      gebaut, sondern eine zweite danebengestellt.

      ── WAS AUSDRUECKLICH NICHT GEMACHT WURDE ───────────────────────────
      KEINE Klappmenues und KEINE weiteren Unterseiten. Beides kuerzte die
      Leiste weiter, beides VERSTECKT aber Seiten — und was man nicht sieht,
      sucht man nicht. Das ist eine Entscheidung des Betreibers und steht als
      Frage im Backlog (E6/G18), samt der Kandidaten (Protokolle,
      Aktualisierung, MuPiHAT unter System), damit sie niemand neu sucht.

      Es ist NICHTS verschwunden: alle vierzehn Wege stehen weiter da, einen
      Klick entfernt. Geaendert hat sich die Reihenfolge, nicht die
      Erreichbarkeit.

      ── DER TITEL STEHT NEBEN DEN EINTRAEGEN, NICHT DARUEBER ────────────
      Das war ein Umbau NACH dem ersten Anlauf, und zwar wegen einer Messung.
      Im Browser bei 1440 px Fensterbreite, Hoehe des header-Elements:

          flache Leiste wie vorher      201 px
          gruppiert, Titel DARUEBER     243 px   (+42, +21 %)
          gruppiert, Titel DANEBEN      201 px   (+0)

      Die Titelzeile darueber zwang die vier Gruppen in ZWEI Reihen. Der Kopf
      klebt oben (sticky) — die 42 px fehlen auf JEDER Seite und bei jedem
      Bildlauf, auf einem 768-px-Schirm ein Zwanzigstel des Fensters. Fuer eine
      Zwischenueberschrift zu teuer, zumal sie daneben genauso liest.
      GESCHAETZT hatte ich vorher „kostet ja fast nichts".

      ── GEPRUEFT WIRD DIE GLIEDERUNG, NICHT BEHAUPTET ───────────────────
      Eine Gruppe ist ein `<section class="gruppe">` mit `<span
      class="gruppentitel">` — FLACH, ohne verschachtelte Kaesten, damit
      `gruppenLesen()` in tools/verwaltung-wege-schau.mjs sie mit einem
      Ausdruck lesen kann. Neuer Befund OHNE GRUPPE: ein Leisteneintrag, der
      in keiner Gruppe steht. Ausgenommen ist genau die Uebersicht
      (`OHNE_GRUPPE`) — sie ist der Weg an den Anfang, kein Thema neben den
      anderen.
    related:
      - admin-gliederung-leitfrage
      - unterseiten-statt-sechzehn-reiter
      - attributreihenfolge-nimmt-den-eintrag-aus-der-leiste
      - zwei-schreiber-auf-einer-klasse

  - id: attributreihenfolge-nimmt-den-eintrag-aus-der-leiste
    kind: falle
    title: "class VOR routerLink — und der Reiter faellt lautlos aus Leiste UND Suche"
    match: "kopfleiseLesen|<a\\s+routerLink|NICHT GELESEN|routerLink als erstes Attribut"
    tags: ["admin", "verwaltung", "suche", "werkzeug", "falle", 2026]
    source: "05.08.2026, beim Umbau der Kopfleiste selbst hineingelaufen; gemessen an tools/verwaltung-suchbestand.mjs"
    body: |
      DER AUSDRUCK, um den es geht, steht in tools/verwaltung-suchbestand.mjs
      in `kopfleiseLesen()` und verlangt `routerLink` als ERSTES Attribut nach
      dem a-Tag. Er ist seit dem 03.08.2026 unveraendert und war nie falsch —
      bis zum 05.08. hatte auch schlicht kein Leisteneintrag eine Klasse.

      WAS PASSIERTE: Der Umbau der Gliederung gab der Uebersicht als erstem
      Eintrag ueberhaupt ein `class="heim"`, und zwar VOR das routerLink. Der
      Eintrag war im Browser voll da und anklickbar — aber:

          Kopfleiste        14 Reiter    ->  13
          Suchbestand      191 Eintraege ->  190

      und BEIDE Pruefungen blieben GRUEN. Die Uebersicht rutschte in den Topf
      „Seite ohne Leisteneintrag", und den gibt es seit den Unterseiten
      ([[unterseiten-statt-sechzehn-reiter]]) voellig zu Recht. Aus einem
      Reiter wurde also stillschweigend eine Unterseite ohne Elternteil.

      DIE FEHLERKLASSE ist genau die, gegen die es den erhobenen Suchbestand
      ueberhaupt gibt ([[verwaltung-suche-bestand-aus-den-seiten]]): es geht
      nichts kaputt, man findet es nur nicht mehr. Und sie ist besonders
      tueckisch, weil die Ursache in einer Datei steht, die man gar nicht
      angefasst hat.

      ── DIE GEGENMASSNAHME: ZWEI LESER, VERSCHIEDEN STRENG ──────────────
      tools/verwaltung-wege-schau.mjs meldet ab jetzt NICHT GELESEN. Es
      vergleicht, was `wegeInDatei()` in der Vorlage des Rahmens findet (ein
      Ausdruck OHNE Annahme ueber die Attributreihenfolge) mit dem, was
      `kopfleiseLesen()` liefert. Ist der erste Leser groszuegiger als der
      zweite, ist die Differenz der Befund.

      Das ist billiger und ehrlicher, als den strengen Ausdruck aufzuweichen:
      dieser haengt an einem char-fuer-char-Vergleich des ganzen Bestands, und
      wer ihn lockert, faengt sich Treffer aus HTML-Kommentaren ein.

      ── DIE REGEL FUER DIE VORLAGE ──────────────────────────────────────
      In rahmen.ts steht `routerLink` VORN, alles andere dahinter. Der Grund
      steht als Kommentar daneben — und ohne Regex-Schreibweise, denn eine
      Zeichenklasse mit Gegenschraegstrich ueberlebt das Template-Literal
      nicht: JavaScript frisst den Gegenschraegstrich beim Einlesen, und der
      Kommentar behauptet danach etwas Falsches (auch das ist passiert).
    related:
      - verwaltung-suche-bestand-aus-den-seiten
      - unterseiten-statt-sechzehn-reiter
      - backtick-im-html-kommentar-frisst-die-halbe-seite

  - id: zwei-schreiber-auf-einer-klasse
    kind: falle
    title: "[class.hier] neben routerLinkActive: die Bindung loescht, was die Direktive setzt"
    match: "routerLinkActive|class.hier|UNTERSEITE_VON|leuchtet\\("
    tags: ["admin", "verwaltung", "angular", "gliederung", "falle", 2026]
    source: "05.08.2026, im Browser gegen die Attrappe gemessen (nav a.hier je Weg)"
    body: |
      DREI EINTRAEGE NENNEN `routerLinkActive` UND `class.hier`. Die eine
      Frage, die trennt: WAS HAST DU IN DER HAND?

        einen konkreten Fall (Elternseite     -> hier
        leuchtet auf einer Unterseite nicht)
        die Regel allein, ohne den Fall       -> [[zwei-schreiber-auf-derselben-klasse]]
        gruene Pruefungen und trotzdem        -> [[gruene-pruefung-sieht-keine-gezeichnete-klasse]]
        dunkle Leiste

      Der zweite ist die DOPPELUNG dieses Eintrags vom selben Tag (siehe
      dort im `source`), der dritte sagt, warum kein Werkzeug es sieht.

      DIE AUFGABE WAR: Auf einer Unterseite soll der Leisteneintrag der
      ELTERNSEITE leuchten. `routerLinkActive` kann das nicht — es vergleicht
      Pfade, und /verschmelzung faengt nicht mit /medien an. Also eine
      gerechnete Klasse daneben:

          <a routerLink="/medien" routerLinkActive="hier"
             [class.hier]="elternLeuchtet() === '/medien'">

      DAS ERGEBNIS, gemessen statt angenommen (`nav a.hier` je Weg):

          /verschmelzung   -> Medien       (schien zu gehen)
          /interpreten     -> NICHTS
          /medien          -> NICHTS       <-- vorher ging das

      BEIDE SCHREIBEN DIESELBE KLASSE. `routerLinkActive` setzt sie
      imperativ, die Klassenbindung setzt sie deklarativ — und die Bindung
      gewinnt: ist ihr Ausdruck falsch, ENTFERNT sie die Klasse, die die
      Direktive gerade gesetzt hat. Auf /medien war der Elternausdruck falsch
      (man steht ja auf der Seite selbst, nicht auf einer Unterseite), also
      flog das Leuchten weg.

      WAS DARAN TEUER IST: Der Umbau, der das Leuchten REPARIEREN sollte,
      hatte es auf der Elternseite KAPUTTGEMACHT — und zwar an einer Stelle,
      die man nach so einer Aenderung am wenigsten nachprueft, weil sie
      „vorher ja ging". Ohne den Durchlauf ueber ALLE Wege waere nur die
      Unterseite geprueft worden, und die sah gut aus.

      ── DIE LOESUNG: EIN AUSDRUCK, EIN SCHREIBER ────────────────────────
      Auf dem betroffenen Eintrag steht KEIN routerLinkActive mehr:

          leuchtet(weg) = aktuellerWeg === weg || UNTERSEITE_VON[aktuellerWeg] === weg

      Die anderen dreizehn Eintraege behalten die Direktive — sie haben keine
      Unterseiten, und zwei Bauarten nebeneinander sind hier billiger als eine
      Handrechnung an jedem Eintrag.

      ── UND DIE LISTE WIRD GEPRUEFT ─────────────────────────────────────
      `UNTERSEITE_VON` in rahmen.ts ist eine Liste, was dieses Projekt sonst
      vermeidet. Sie ist unvermeidbar: /verschmelzung ist im Router ein
      GESCHWISTER von /medien (damit die Adressen unveraendert bleiben), die
      Elternschaft ist zur Laufzeit also nicht ablesbar. Gehalten wird sie wie
      `AKTION_ORT` in suche.ts — tools/verwaltung-wege-schau.mjs meldet ELTERN
      UNEINIG und vergleicht gegen die ERHOBENE Elternschaft, in BEIDE
      Richtungen: ein fehlender Eintrag laesst nichts leuchten, ein
      ueberzaehliger den falschen Reiter.

      ── NACHTRAG ZU [[unterseite-ohne-rueckweg]] ────────────────────────
      Jener Eintrag meldete am 03.08.2026 zwei Dinge — „kein Leisteneintrag
      leuchtet" UND „kein Rueckweg". Behoben wurde damals nur der Rueckweg,
      und der Eintrag las sich danach wie erledigt. Die erste Haelfte lag
      sechzehn Tage still da. WER EINEN BEFUND MIT ZWEI TEILEN BEHEBT, SOLL
      BEIDE NACHMESSEN — hier haette ein Blick auf `nav a.hier` genuegt.
    related:
      - unterseite-ohne-rueckweg
      - unterseiten-statt-sechzehn-reiter
      - admin-kopfleiste-vier-gruppen
      - zwei-schreiber-auf-derselben-klasse
      - gruene-pruefung-sieht-keine-gezeichnete-klasse

  - id: verwaltung-interaktionsmuster-l3
    kind: referenz
    title: "Womit die 17 Verwaltungsseiten bedient werden — E7b/L3, erstmals gezaehlt"
    match: "interaktionsmuster|INTERAKTIONSMUSTER|RUECKMELDE_WOERTER|L3"
    tags: ["admin", "verwaltung", "e7b", "werkzeug", "referenz", 2026]
    source: "05.08.2026, node tools/verwaltung-gliederung.mjs — am Quelltext gemessen, nicht am Bild"
    body: |
      L3 STAND SEIT DEM 31.07.2026 IM BACKLOG UND WAR NIE GEZAEHLT WORDEN.
      Ohne Zahlen ist L5 („Referenz-Seite kueren") eine Geschmacksfrage und L6
      („angleichen") ohne Maszstab — deshalb zuerst zaehlen.

      ── DIE BEFUNDE ─────────────────────────────────────────────────────
      SPEICHERN: 2 Seiten sammeln, 14 schreiben SOFORT. Eine klebende
      Fuszleiste mit Speichern/Verwerfen haben nur Konfiguration und
      Streaming-Dienste; ueberall sonst wirkt jeder Klick augenblicklich. Das
      ist der EINSCHNEIDENDSTE Unterschied der Verwaltung und am Bild nicht zu
      erkennen: wer von einer Sammel-Seite auf eine Sofort-Seite wechselt,
      drueckt aus Gewohnheit einen Schalter „zum Ausprobieren" — und hat ihn
      gestellt.

      KASTEN: `.karte` nutzen 9 von 17 (21 Vorkommen). Die anderen acht haben
      sehr wohl Kaesten — sie heiszen `abschnitt`, `schild`, `kachel`,
      `gruppe`, `kopf`, `diagnose`.

      RUECKMELDUNG: JEDE Seite hat einen Ort dafuer, aber unter VIER Woertern
      (fehler, meldung, warn, stand). Keine sagt gar nichts.

      LADEZUSTAND: 13 von 17. Ohne: Uebersicht (statisch, in Ordnung) sowie
      Darstellung, Doppelte, Interpreten — die drei stehen beim Warten stumm
      leer.

      ── ZWEI FEHLMESSUNGEN AUF DEM WEG, beide beim Gegenpruefen gefunden ─
      Beide dieselbe Bauart, und es ist die Bauart aus
      [[admin-abgleich-grep-luegt]]: der Ausdruck traf die Schreibweise, die
      der Messende ERWARTET hatte, und meldete alles andere als „nicht
      vorhanden".

      1. Die Wortgrenzen-Suche nach `fehler` meldete Kinderzeit als EINZIGE
         Seite ohne Fehlerort. Sie hat einen — er heiszt `regelnFehler`, und
         zwischen „n" und „F" steht keine Wortgrenze.
      2. Gezaehlt wurde die Klasse `karte`, und acht Seiten kamen mit 0
         heraus. Die 0 las sich wie „diese Seite hat keine Struktur" und hiesz
         „diese Seite benutzt ein anderes Wort".

      Der Rettungsversuch — die Klasse UM die h2 lesen — war noch schlechter:
      die Konfigurationsseite bekam „keine h2", obwohl sie vier hat (sie
      stehen dort lose in einem @for, ohne Kasten darum). Wieder eine Null,
      die etwas anderes bedeutet als sie sagt. DESHALB steht in der Tabelle
      jetzt eine schlichte ZAHL, deren Bedeutung im Spaltentitel steht, und
      daneben die WORTLISTE — nicht ein Urteil.

      ── DIE GRENZE DIESER ZAHLEN ────────────────────────────────────────
      Gemessen wird am QUELLTEXT. Sie sagen, WELCHE Bauteile eine Seite
      benutzt, nicht ob sie gut aussieht. Fuer „sieht gleich aus" braucht es
      einen Menschen vor dem Browser.

      ── WAS FUER L5 DARAUS FOLGT ────────────────────────────────────────
      Mehrheitsmuster: sofort schreiben · .karte · Rueckmeldung ·
      Ladezustand. Vier Seiten tragen es vollstaendig; darunter ist VORLESEN
      (462 Zeilen) die einzige, die dabei lesbar kurz ist. Medien hat dasselbe
      Muster, aber 1753 Zeilen; Kinderzeit hat keine Zwischenueberschrift;
      Doppelte und Interpreten haben keinen Ladezustand.

      GEKUERT IST DAMIT NICHTS. Darin steckt eine Frage, die dem Betreiber
      gehoert: EIN Speicher-Verhalten oder ZWEI? Sie steht im Backlog
      (E7b/L5) mit drei Wegen, von denen keiner offensichtlich richtig ist.
    related:
      - admin-abgleich-grep-luegt
      - verwaltung-gliederung-stand-2026-08-04
      - admin-kopfleiste-vier-gruppen

  - kind: falle
    id: vergleich-aus-derselben-zuweisung
    title: "Ein Vergleich, dessen beide Seiten aus DERSELBEN Zuweisung stammen, kann nicht nein sagen"
    match: schluessel vergleich immer wahr|laufendesWerk ungeprueft|zuletztGestartet unbesehen|passtZuLaufendem fehlt|cover fremde wiedergabe|falsches folgenbild
    tags:
      - oberflaeche
      - neu
      - player
      - cover
      - pruefung
      - falle
      - 2026
    source: 06.08.2026, beim Gegenlesen von E32/W5 gefunden; repariert in NewDesign/app.js `coverAdresse`; rot gemacht mit tools/rotprobe.py (`cover-laesst-fremdes-los`) und tools/e2e/neu-oberflaeche.test.mjs
    body: |
      DIE GESTALT DER FALLE, unabhaengig von dieser Stelle: Eine Bedingung
      sieht aus wie eine Pruefung, vergleicht aber zwei Werte, die aus
      DERSELBEN Zuweisung stammen. Sie ist dann immer wahr — und der
      Kommentar daneben beschreibt einen Schutz, den es nicht gibt.

      ── DER FALL ────────────────────────────────────────────────────────
      `coverAdresse` schlaegt seit dem 06.08.2026 das Folgenbild ueber die
      laufende Warteschlangennummer nach ([[cover-folgt-der-position-nicht-dem-start]]).
      Abgesichert war das mit:

          if (laufendeFolgen && w && laufendeFolgen.schluessel === w.schluessel)

      Daneben stand: „NUR FUER DAS WERK, ZU DEM SIE GEHOERT. Ohne diesen
      Vergleich truege eine Wiedergabe, die jemand vom Telefon angeworfen
      hat, die Bilder der vorigen Sendung."

      DAS LEISTET ER NICHT. In `albumSpielen` stehen beide Zuweisungen acht
      Zeilen auseinander:

          zuletztGestartet = w
          …
          laufendeFolgen = { schluessel: w.schluessel, liste: titel }

      und `laufendesWerk()` gibt `zuletztGestartet` AUSDRUECKLICH unbesehen
      zurueck — es steht so an `laufendesWerkMitBeweis`, Stufe 2: „Ungeprueft
      — wer daraus ,laeuft gerade' macht, muss selbst nachfragen." Der
      Vergleich ist damit wahr, sobald `laufendeFolgen` ueberhaupt steht.

      ── WAS DAMIT DURCHKAM ──────────────────────────────────────────────
      Es gibt an dieser Box mehr als einen Weg, mpv anzuwerfen: die
      KLASSISCHE Oberflaeche auf demselben Schirm, ein Telefon, die
      PHP-Verwaltung. Dabei ERSETZT mpv die Warteschlange. `currentTracknr`
      zaehlt danach in der NEUEN Liste, nachgeschlagen wurde in der ALTEN
      (bis zu 30 Folgen lang) — die Leiste zeigte das Bild einer Folge, die
      gar nicht mehr lief. VOR dem 06.08.2026 war es richtig: die Stufe
      darunter (`coverFuerLokal`) fragt den gemeldeten Pfad und findet das
      Werk, das WIRKLICH spielt. Die neue Stufe draengte sich davor.

      ── DIE ANTWORT STAND SCHON IM HAUS ─────────────────────────────────
      `passtZuLaufendem(w, np)` ist fuer genau diese Frage gebaut. Stufe 3
      DERSELBEN Funktion fragt sie laengst, und `stelleMelden` fragt sie,
      bevor es eine Stelle schreibt („Eine Stelle unter dem falschen Werk ist
      schlimmer als keine, denn sie sieht richtig aus"). Sie fehlte nur an
      der neuen Stufe.

      ── WORAN MAN DIE FALLE ERKENNT, BEVOR SIE BEISST ───────────────────
      Zwei Fragen an jede Bedingung, die etwas absichern soll:
        1. KANN SIE UEBERHAUPT FALSCH WERDEN? Wenn nein: sie prueft nichts.
        2. WOHER KOMMEN BEIDE SEITEN? Stehen sie im selben Block
           nebeneinander, ist die Antwort meistens schon 1.
      Und: eine Auskunft, die im Quelltext „ungeprueft" heisst, wird durch
      einen Vergleich mit sich selbst nicht geprueft.

      ── DER FALL LIESS SICH NICHT STELLEN ───────────────────────────────
      Die Attrappe meldete an `/player/local` IMMER dasselbe Werk. Deshalb
      war der Fehler unsichtbar, obwohl es fuer die andere Richtung („folgt
      das Cover der Nummer?") einen Browserlauf gab. Neu ist der Hebel
      `mpv-fremd`/`mpv-eigen` in tools/neu-vorschau.mjs — er meldet ein Werk,
      das es in der Liste WIRKLICH gibt, damit sich „schaltet die Oberflaeche
      um?" von „findet sie bloss nichts?" unterscheiden laesst. Wieder
      [[attrappe-luegt-durch-weglassen]], hier als fehlender Fall.
    related:
      - cover-folgt-der-position-nicht-dem-start
      - attrappe-luegt-durch-weglassen
      - drei-orte-eine-anzeige
      - rotprobe-war-der-test-ohne-die-aenderung-rot

  - id: unit-nur-in-autosetup-erreicht-keine-bestehende-box
    kind: falle
    title: "Eine Unit nur in autosetup.sh: frische Karte ja, laufende Box NIE"
    match: "config/services/.*\\.(service|timer|path)|systemd/system/mupibox-|ausrollweg-deckung"
    tags: ["ausrollweg", "autosetup", "update", "systemd", "dienste", "falle", 2026]
    source: "05.08.2026 mit tools/ausrollweg-deckung.py gefunden, an E29 (mupibox-sicherung*) belegt"
    body: |
      ES GIBT ZWEI WEGE AUF EINE BOX, UND SIE SIND NICHT DIESELBEN:

        autosetup/autosetup.sh          eine frisch bespielte Karte
        update/start_mupibox_update.sh  eine BESTEHENDE, laufende Box

      Wer eine neue Unit nur in autosetup.sh eintraegt, hat sie fuer jede
      Box gebaut, die es noch nicht gibt. Alle laufenden bleiben ohne sie —
      dauerhaft, ueber beliebig viele Updates.

      ── WARUM ES SO SCHWER AUFFAELLT ────────────────────────────────────
      SKRIPTE kommen ueber BEIDE Wege, und zwar per Wildcard:

          mv ${MUPI_SRC}/scripts/mupibox/* /usr/local/bin/mupibox/

      UNITS dagegen stehen in beiden Dateien EINZELN ZEILE FUER ZEILE. Ein
      neues Skript reicht sich also von selbst nach; eine neue Unit nicht.
      Das Ergebnis ist eine halbe Funktion: das Werkzeug liegt auf der Box
      und laesst sich von Hand aufrufen, aber nichts ruft es auf. Von Hand
      geprueft sieht alles richtig aus.

      ── WIE ES SICH ZEIGTE (E29) ────────────────────────────────────────
      Vier Sicherungs-Units lagen nur in autosetup.sh. Auf jeder laufenden
      Box gab es damit mupibox-sicherung.py, aber:
        * keinen selbsttaetigen Stand (weder .path noch .timer),
        * und KEIN mupibox-wiederherstellung.service — die Textdatei auf der
          Karte ([[sicherung-rueckweg-ueber-die-karte]]) haette dort
          schlicht nichts getan.
      Der Rueckweg ohne SSH existierte auf einer bestehenden Box also nicht,
      und gemerkt haette es erst der, der ihn im Ernstfall faehrt.

      ── DAS WERKZEUG, DAS ES SOFORT ZEIGT ───────────────────────────────
          python3 tools/ausrollweg-deckung.py

      Es stellt beide Wege nebeneinander. Gesucht ist die Zeile

          <unit>    rollt aus    —

      Ein „—" in der zweiten Spalte ist entweder ein Fehler oder braucht
      einen ausgeschriebenen Grund (der Bericht hat dafuer eine Spalte).
      NACH JEDER NEUEN UNIT einmal laufen lassen.

      Verwandt, aber anders: [[piper-fehlt-im-ausrollweg]] — dort kannte
      KEIN Weg das Programm. Hier kennt es einer, und genau das macht die
      Luecke unsichtbar.
    related:
      - piper-fehlt-im-ausrollweg
      - sicherung-rueckweg-ueber-die-karte
      - ausliefern-regeln

  - id: kein-traceback-in-eine-datei-die-ohne-ssh-gelesen-wird
    kind: falle
    title: "Ein Python-Traceback als Auskunft fuer den Betreiber — auf der Karte"
    match: "PROBE\\.txt|--probe|stand_oeffnen|BadGzipFile|mupibox-sicherungsprobe"
    tags: ["sicherung", "probe", "fehlermeldung", "karte", "falle", 2026]
    source: "05.08.2026 an der Box .169 nachgestellt: EIN verdrehtes Byte in einem echten Archiv"
    body: |
      DIESER EINTRAG TEILT SICH ZWEI SYMPTOMWOERTER MIT ZWEI ANDEREN.
      Die Fragen, die entscheiden, stehen vor jeder Suche:

          `--probe`        WELCHES Werkzeug traegt den Schalter?
                           `mupibox-sicherung.py --probe` (Sicherung
                           zurueckspielen)      -> DIESER Eintrag
                           `tools/ausliefern.py --probe` (Ausrollweg,
                           meldet www/admin ewig „veraendert")
                           -> [[probe-kann-www-nie-gleich-nennen]]

          `stand_oeffnen`  WAS STEHT IN PROBE.txt?
                           Ein Traceback              -> DIESER Eintrag
                           Ein sauberes Urteil, das trotzdem falsch
                           gruen ist (2,4 % der Bit-Dreher unbemerkt)
                           -> [[stand-pruefen-las-den-gzip-strom-nie-zu-ende]]

      Beide Nachbarn liegen in derselben Funktion; der Unterschied ist,
      ob sie ABBRICHT oder LUEGT.

      DIE REGEL: Eine Datei, die jemand OHNE SSH liest, darf keinen
      Traceback enthalten. Sie ist eine Auskunft, kein Protokoll.

      ── WIE ES PASSIERT IST ─────────────────────────────────────────────
      `mupibox-sicherung.py --probe` spielt den juengsten Stand in ein
      Wegwerf-Verzeichnis zurueck und vergleicht Byte fuer Byte. Sein Urteil
      landet als PROBE.txt auf der FAT-Partition — dort liest es jemand mit
      einem Kartenleser, ohne SSH und ohne Python.

      stand_oeffnen() wirft ZWEI Sorten:
        * SystemExit          falsche Pruefsumme, unmoeglicher Pfad im Archiv
        * gewoehnliche        ein Bit-Dreher gibt BadGzipFile, eine
                              abgeschnittene Datei EOFError

      probe() fing KEINE davon. Bei einem beschaedigten Archiv — also genau
      in dem Fall, fuer den die Probe da ist — stand der Traceback in
      PROBE.txt. `--pruefen` machte es seit jeher richtig:

          except (SystemExit, Exception) as e:   # noqa: BLE001

      SystemExit MUSS eigens genannt werden: er haengt an BaseException, und
      `except Exception` faengt ihn NICHT.

      ── WORAN MAN DIE STELLE ERKENNT ────────────────────────────────────
      Zwei Fragen an jede Ausgabe:
        1. WER LIEST DAS? Steht am anderen Ende ein Betreiber statt eines
           Entwicklers, ist ein Ausnahmetyp keine Antwort.
        2. IST DER FEHLERFALL DER NORMALFALL? Eine Probe existiert fuer den
           Fall, dass etwas kaputt ist. Dann ist der Fehlerpfad der
           HAUPTPFAD und braucht mehr Sorgfalt als der gute, nicht weniger.

      Der Satz sagt jetzt zusaetzlich, wie es weitergeht („Ein aelterer Stand
      kann es noch sein: `--pruefen` sagt, welcher") — eine Meldung ohne
      naechsten Schritt laesst den Leser genauso stehen wie ein Traceback.
    related:
      - sicherung-rueckweg-ueber-die-karte
      - sicherung-probe-woechentlich
      - probe-kann-www-nie-gleich-nennen
      - stand-pruefen-las-den-gzip-strom-nie-zu-ende

  - id: sicherung-probe-woechentlich
    kind: knowhow
    title: "Eine Sicherung, die nie zurueckgespielt wurde, ist keine — also woechentlich proben"
    match: "mupibox-sicherungsprobe|--probe|ExecStopPost|PROBE\\.txt"
    tags: ["sicherung", "probe", "systemd", "wiederherstellung", 2026]
    source: "05.08.2026 an der Box .169 gebaut und gefahren (E29/B3), 23 Selbsttests"
    body: |
      EIN ARCHIV, DAS SICH NICHT MEHR OEFFNEN LAESST, SIEHT BIS ZUM
      ERNSTFALL AUS WIE EIN GUTES. Es liegt da, die Groesse stimmt, das
      Datum stimmt. Deshalb genuegt es nicht, einen Rueckweg zu BAUEN — er
      muss ohne Zutun GEFAHREN werden.

          mupibox-sicherungsprobe.timer     sonntags 5:30, Persistent=true
          mupibox-sicherungsprobe.service   -> mupibox-sicherungsprobe.sh

      ── DREI ENTSCHEIDUNGEN, DIE NICHT OFFENSICHTLICH SIND ──────────────

      1. Persistent=true IST HIER PFLICHT, nicht Schmuck. Eine Box, die
         abends ausgeht, erreicht nie einen Sonntagmorgen um 5:30. Ohne
         Persistent faende die Probe schlicht NIE statt — der Zeitgeber
         stuende auf „enabled" und taete nichts. Der nachgeholte Lauf ist
         hier der NORMALFALL, nicht die Ausnahme.

      2. DAS URTEIL GEHT AUF DIE KARTE, nicht nur ins Journal. Ein Ergebnis
         im Journal braucht SSH — und SSH ist genau das, was im Ernstfall
         fehlt. PROBE.txt liegt neben den Staenden auf der FAT-Partition,
         erste Zeile „URTEIL: BESTANDEN" oder „URTEIL: NICHT BESTANDEN".
         `mupibox-sicherung.py --auf-karte` raeumt dort nur `*.tar.gz` weg,
         PROBE.txt bleibt also liegen.

      3. DAS KOPIEREN HAENGT AN ExecStopPost=, NICHT AN EINEM ZWEITEN
         ExecStart=. Faellt die Probe durch, bricht systemd die
         ExecStart-Kette ab — das Urteil kaeme dann ausgerechnet im
         FEHLERFALL nicht auf die Karte, also genau dann nicht, wenn es
         jemand braucht. ExecStopPost laeuft so oder so.

         Die Probe selbst laeuft als dietpi (sie liest nur Staende und
         schreibt nach /tmp); das Kopieren braucht root, deshalb `+`. Und
         `-`, weil eine Box ohne Kartenordner deswegen nicht auf „failed"
         stehen soll.

      ── WAS DIE PROBE NICHT ANFASST ─────────────────────────────────────
      Nichts. Sie schreibt ausschliesslich nach /tmp
      (tempfile.TemporaryDirectory), in die Urteilsdatei und auf die Karte.
      Weder /etc/mupibox noch server/config werden beruehrt. Eine Probe, die
      die Box veraendert, waere ein neuer Weg, Daten zu verlieren.

      ── GEMESSEN (05.08.2026, .169) ─────────────────────────────────────
        * Unit gefahren: Result=success, PROBE.txt auf der Karte, BESTANDEN
        * mit EINEM verdrehten Byte im Archiv, ueber systemd-run gegen einen
          Wegwerf-Ordner: Result=exit-code, ExecMainStatus=1, Urteil ein
          Satz statt eines Tracebacks
          ([[kein-traceback-in-eine-datei-die-ohne-ssh-gelesen-wird]])
        * die 13 echten Staende blieben unberuehrt
    related:
      - sicherung-rueckweg-ueber-die-karte
      - kein-traceback-in-eine-datei-die-ohne-ssh-gelesen-wird
      - unit-nur-in-autosetup-erreicht-keine-bestehende-box
      - sicherung-mitten-in-der-wiederherstellung
  - kind: falle
    id: eigene-hunks-schuetzt-nicht-vor-fremdem-schreiber
    title: "eigene-hunks.py schuetzt vor fremden HUNKS, nicht vor einem fremden SCHREIBER im selben Index"
    tags: ['git', 'parallel', 'index', 2026]
    source: "beim Parallelbetrieb von vier Vorgaengen passiert (2026-08-05)"
    body: |
      `tools/eigene-hunks.py` legt gezielt nur die eigenen Hunks einer Datei in den
      Index — gegen den Fall, dass mehrere Vorgaenge dieselbe DATEI aendern. Das
      loest es auch.

      ES SCHUETZT ABER NICHT GEGEN EINEN ZWEITEN SCHREIBER IM SELBEN INDEX.

      PASSIERT IN DER NACHT AUF DEN 05.08.2026, bei vier gleichzeitig laufenden
      Vorgaengen: Zwischen `git add` und `git commit` eines Laufs hat ein anderer
      Lauf seinerseits Dateien in den Index gestellt. Der Commit `9ff7d7b8` nahm
      sieben fremde Dateien mit — konfiguration.ts, drei Verwaltungsdateien, drei
      Werkzeuge. Zurueckzunehmen war es nicht mehr: der andere Vorgang hatte
      bereits daraufgesetzt.

      NICHTS GING VERLOREN. Der Schaden ist, dass Arbeit unter einer Nachricht
      steht, die von ihr nichts weiss — und das faellt erst Wochen spaeter auf,
      wenn jemand fragt „warum wurde das geaendert?" und die Begruendung nicht
      passt.

      DER INDEX IST EIN GEMEINSAMER ZUSTAND, die Arbeitskopie auch. `git add` und
      `git commit` sind zwei Schritte, und dazwischen liegt Zeit.

      WAS HILFT
        * `git commit -o <datei> ...` (nur diese Pfade, unabhaengig vom Index) —
          setzt aber voraus, dass die Datei ganz einem gehoert.
        * Vor dem Commit `git diff --cached --name-only` lesen und vergleichen mit
          dem, was man selbst angefasst hat. Passt es nicht: NICHT committen,
          sondern melden.
        * Bei laengeren Arbeiten `isolation: 'worktree'` — ein eigener Baum hat
          einen eigenen Index.

      WAS NICHT HILFT: `tools/eigene-hunks.py` allein. Es entscheidet, WELCHE Hunks
      hineinkommen, nicht WER sonst noch hineinschreibt.

  - kind: falle
    id: gruene-pruefung-sieht-keine-gezeichnete-klasse
    title: "Alle Pruefungen gruen, und in der Leiste leuchtet nichts — was Quelltext-Werkzeuge hier prinzipiell nicht sehen"
    match: "leuchtet nicht|routerLinkActive|class.hier|Leisteneintrag|leiste-leuchtet-schau"
    tags: [admin, verwaltung, angular, pruefung, werkzeug, 2026]
    source: "05.08.2026, beim Gegenlesen des Kopfleisten-Umbaus in einer Wegwerf-Arbeitskopie nachgestellt"
    body: |
      NACHGESTELLT, NICHT VERMUTET. In einer Wegwerf-Kopie des Baums wurde die
      Zeile von [[zwei-schreiber-auf-derselben-klasse]] zurueckgesetzt — am
      „Medien"-Eintrag stand wieder routerLinkActive neben dem gerechneten
      [class.hier]. Gemessen wurde danach:

          verwaltung-wege-schau --pruefen      Ende 0, kein Wort
          Kopfleiste                           14 Eintraege
          Suchbestand                          195 Eintraege
          ELTERN UNEINIG                       leer
          verwaltung-wege-schau.test.mjs       alle Faelle gruen
          tsc, biome                           sauber

      Im Browser leuchtete auf /verschmelzung und /interpreten dabei GAR NICHTS.

      DIE REGEL DAHINTER, und sie gilt ueber diesen Fall hinaus: JEDES Werkzeug
      dieses Projekts, das die Verwaltung prueft, liest QUELLTEXT. Es sieht
      Listen, Wege, Attribute und Namen. Es sieht NIE, welche Klasse am Ende
      wirklich am Element haengt. Ein Fehler, der erst beim Zeichnen entsteht
      — zwei Schreiber, eine Bindung, die die andere ueberholt, ein @if, das
      nie wahr wird — ist fuer alle diese Pruefungen unsichtbar, und zwar
      nicht aus Versehen, sondern der Bauart nach.

      DAS WERKZEUG DAFUER IST tools/leiste-leuchtet-schau.mjs (seit 05.08.2026,
      haengt in tools/pruefen.sh). Es faehrt jeden Weg der Verwaltung im
      Browser an und liest `nav a.hier` — verlangt wird GENAU EINER und der
      richtige. Auf einer Unterseite ist das ihr Elternteil aus UNTERSEITE_VON,
      GELESEN und nicht ein zweites Mal hingeschrieben. Die Rueckwege werden
      ANGEKLICKT, nicht nur gefunden.

      ES BAUT BEI JEDEM LAUF SELBST, in ein eigenes Verzeichnis unter /tmp. Ein
      vorhandenes src/deploy/www-admin ist der Stand von irgendwann; ein
      „leuchtet richtig" darueber waere genau die Sorte gruen, gegen die das
      Werkzeug antritt.

      ZWEI FALLEN BEIM BAUEN, beide beim Schreiben hineingelaufen:
        * `npx ng build` ruft in diesem Baum das SKRIPT `ng` aus package.json
          auf, nicht das Programm — `--output-path` wird verschluckt, gebaut
          wird nach src/deploy, und gemessen haette man den alten Stand.
          Deshalb direkt node_modules/.bin/ng.
        * `--output-path` ersetzt die ganze `outputPath`-Angabe aus
          angular.json (dort steht `browser: ""`, die Dateien liegen flach).
          Damit kehrt der Vorgabewert `browser/` zurueck. Der Ort wird deshalb
          GESUCHT, nicht angenommen.

      GEGENPROBE IST PFLICHT und ist eingebaut: es faehrt zum Schluss einen Weg
      an, den es nicht gibt, und verlangt dort den Uebersichts-Eintrag. Ohne das
      pruefte eine Messung, die gar nichts anderes sehen kann.
    related: [zwei-schreiber-auf-derselben-klasse, zwei-schreiber-auf-einer-klasse]

  - kind: falle
    id: zwei-schreiber-auf-derselben-klasse
    title: "routerLinkActive UND [class.x] am selben Element: die Bindung loescht, was die Direktive setzt"
    match: "routerLinkActive|class\\.hier|ZWEI SCHREIBER|Eintrag bleibt dunkel"
    tags: [angular, admin, verwaltung, 2026]
    source:
      - "05.08.2026, im Umbau der Kopfleiste passiert und beim Gegenlesen mit einer Pruefung versehen"
      - "DOPPELUNG, festgestellt 08.08.2026: dieselbe Lehre vom selben Tag steht als [[zwei-schreiber-auf-einer-klasse]] ein zweites Mal im Pack. Dort ist der FALL beschrieben (Elternseite leuchtet auf Unterseiten), hier die REGEL. Zwei Eintraege, eine Ursache — wer einen berichtigt, muss den anderen mitnehmen."
    related:
      - zwei-schreiber-auf-einer-klasse
      - gruene-pruefung-sieht-keine-gezeichnete-klasse
    body: |
      BEIDE SCHREIBEN DIESELBE KLASSE. Angular wertet die Bindung aus, nachdem
      die Direktive gesetzt hat; eine Bindung auf `false` NIMMT die Klasse weg.
      Das Element bleibt dunkel — auf genau den Seiten, fuer die man es
      gerechnet hat.

      VERBOTEN IST NUR DAS NEBENEINANDER. Dreizehn Eintraege der Kopfleiste
      tragen routerLinkActive und sollen es behalten. Wer eine Elternschaft
      rechnen will, nimmt die Direktive an DIESEM einen Element weg.

      DIE PRUEFUNG DAZU steht seit 05.08.2026 in tools/verwaltung-wege-schau.mjs
      (`zweiSchreiberIn`, meldet ZWEI SCHREIBER). Sie laeuft ueber die Vorlagen
      des Rahmens UND aller Seiten, versteht mehrere Klassen in einem
      routerLinkActive ("hier aktiv") und laesst ein Beispiel im Kommentar in
      Ruhe — sonst muesste man die Warnung loeschen, um gruen zu werden.

      SIE ALLEIN GENUEGT NICHT: sie kennt EINE Schreibweise. Was am Ende wirklich
      leuchtet, misst nur [[gruene-pruefung-sieht-keine-gezeichnete-klasse]].

  - kind: howto
    id: karma-specs-der-verwaltung-brauchen-chrome-bin
    title: 'Kein Chrome? „No binary for ChromeHeadless“ heisst nur: CHROME_BIN fehlt'
    match: "No binary for ChromeHeadless|ng test.*admin|Karma.*Verwaltung|CHROME_BIN"
    tags: [tests, angular, karma, verwaltung, 2026]
    source: '05.08.2026 nachgemessen: 80 von 80 Faellen gruen auf demselben Rechner, auf dem der Lauf davor „kein Browser“ gemeldet hatte'
    body: |
      AM 05.08.2026 wurde ein Umbau der Verwaltung mit „die Karma-Specs sind
      NICHT GEMESSEN — auf diesem Rechner ist kein Browser" abgeschlossen. Auf
      demselben Rechner, in derselben Nacht:

          cd src/frontend-admin
          CHROME_BIN=$HOME/.cache/ms-playwright/chromium-*/chrome-linux64/chrome \
            npx ng test --watch=false --browsers=ChromeHeadless
          -> TOTAL: 80 SUCCESS

      Karma sucht nicht, es VERLANGT `CHROME_BIN`. Ein Chrome liegt hier
      laengst da (Playwright bringt einen mit) — das steht seit dem 28.07.2026
      im Wiki ([[box-tests-liefen-nie]]), und tools/pruefen.sh SUCHT ihn seit
      damals selbst (`browser_finden`, Schritt „Tests neue Verwaltung", gruen
      in 6 s).

      DIE LEHRE IST NICHT DIE UMGEBUNGSVARIABLE. Sie ist: ein „geht auf diesem
      Rechner nicht" gehoert vor dem Aufschreiben gegen das Wiki und gegen
      pruefen.sh gehalten. Beide wussten es besser, und ein unnoetiges „nicht
      gemessen" kostet den Naechsten genauso viel Zeit wie ein falsches
      „fertig".
  - kind: knowhow
    id: mpv-nimmt-den-titelnamen-an-force-media-title
    title: "F1: mpv KENNT den Folgennamen nicht — aber man kann ihn ihm mitgeben (force-media-title)"
    match: "F1 titelname beheben|media-title|force-media-title|titelname abspieldienst|weg a oder weg b titelname"
    tags: ['player', 'mpv', 'jellyfin', 'ard', 'anzeige', 'messen', 'F1', 2026]
    source: "05.08.2026 an der Box .169 gemessen (mpv 0.40.0), tools/mpv-titelquelle-am-geraet.mjs. VORBEREITET, NICHT GEBAUT."
    body: |
      Anschluss an [[mpv-trackname-geht-nie-mit]]. Dort steht die WIRKUNG (der
      Name geht nie mit). Hier steht, WO man es repariert — und warum die
      naheliegende Antwort die falsche ist.

      ══ 1. `media-title` GEHT MIT — der Wert taugt aber nur bei Jellyfin ══
      Am Uebergang 1 -> 2, von selbst, ohne Befehl von aussen:

        JELLYFIN   currentTrackname  "Mittelmaessiges Leben"   bleibt stehen
                   media-title       "Nicht alleine"           geht mit

        ARD        media-title  "Faelschung | Die Maus zum Hoeren (0101.0101.70707070})"
                   /inhalt      "Faelschung"

      Die Eigenschaft folgt also zuverlaessig der Datei. Bei der ARD tragen die
      Merkmale der Ausspielpartner aber den Sendungsnamen mit — genau die
      Doppelung, die `titleClean` in ard.ts wegnimmt, und genau der Grund, aus
      dem der metadata-Hoerer in spotify-control.ts 'ard' ausschliesst.

      WER HIER AUFHOERT ZU MESSEN, SCHLIESST „der Abspieldienst geht nicht" —
      und das ist fuer das LESEN richtig und fuer die Sache falsch.

      ══ 2. MAN MUSS mpv NICHT FRAGEN, MAN KANN IHM SAGEN ══
      `force-media-title` ist in mpv eine DATEI-Option, kein globaler Schalter.
      Gemessen (mpv 0.40.0):

        loadfile <adr> replace     -1 {"force-media-title": "…"}   -> success
        loadfile <adr> append-play -1 {"force-media-title": "…"}   -> success

      Danach traegt JEDER Warteschlangeneintrag seinen eigenen Namen, und
      `media-title` wechselt am Uebergang mit:
        "ERZWUNGEN 1: Faelschung"  ->  "ERZWUNGEN 2: Zu Besuch"

      Auch `observe_property media-title` ist erlaubt: mpv schiebt die
      Aenderung VON SELBST (gemessen, ohne Nachfrage). Der Wrapper beobachtet
      ohnehin alles aus EIGENSCHAFTEN (mpv-wrapper.ts) — ein Eintrag dort
      genuegt, kein zweiter Sekundentakt.

      ══ 3. DER NAME IST SCHON DA — er wird nur weggeworfen ══
      Der Anhaengbefehl traegt ihn bereits:

        ardqueue/<adresse>/Zu Besuch:title:artist:Die Maus
        jfqueue/<adresse>/Nicht alleine:title:artist:Kapelle Petra

      Der Zweig `jfqueue`/`ardqueue` in spotify-control.ts benutzt davon heute
      nur `command.dir`. `command.name` — der richtige Name jeder einzelnen
      Folge — wird nicht gelesen. Es ist also KEIN Beschaffungsproblem und
      braucht keinen zusaetzlichen Abruf und keine Vertragsaenderung.

      ══ 4. WARUM NICHT EINE LISTE IM DIENST FUEHREN ══
      Naheliegend waere, im Abspieldienst mitzuschreiben („Eintrag 2 heisst
      …") und ueber `playlist_pos` nachzuschlagen — dieselbe Bauart wie
      `laufendeFolgen` in der Oberflaeche. DAS DRIFTET, und zwar nachweislich:
      der jfqueue/ardqueue-Zweig VERWIRFT einen Befehl, wenn Spotify gerade den
      Ton besitzt (dokumentiert und am Ton gemessen, 05.08.2026). Eine
      verworfene Folge fehlt dann in mpvs Warteschlange, steht aber in der
      mitgefuehrten Liste — ab da sind ALLE Namen um eins verschoben.

      `force-media-title` kann das nicht: der Name liegt IM Eintrag. Wird der
      Eintrag verworfen, ist der Name mit ihm weg.

      ══ ZWEI FALLEN BEIM BAUEN, beide schon bezahlt ══
        * Die kurze Form `loadfile <adr> append {optionen}` beantwortet mpv
          0.40 mit "invalid parameter". Der Index `-1` ist PFLICHT. Ohne ihn
          gibt es keinen Namen und keine Meldung in der Oberflaeche, nur einen
          stillen Fehlschlag im Socket.
        * Zwischen zwei Titeln schiebt mpv `media-title = null`. Ein Hoerer,
          der das weiterschreibt, leert bei JEDEM Uebergang kurz die
          Titelzeile — dieselbe Sorte Fehler, gegen die `darfGemeldetWerden`
          fuer filename/path gebaut ist.
        * Bei `observe_property` kommt der AKTUELLE Wert sofort mit. Das ist
          keine Aenderung, sieht aber wie eine aus.

      ══ STAND ══
      playerEngine steht am Geraet auf "mpv" (05.08.2026 nachgesehen, nicht
      geglaubt). Der Kiosk zeigt http://localhost:8200/neu/, also die NEUE
      Oberflaeche — die klassische ist mitgeheilt, aber sie ist nicht das, was
      das Kind sieht. Das Argument fuer den Abspieldienst ist deshalb NICHT in
      erster Linie „beide Oberflaechen", sondern: der Name ueberlebt das
      Neuladen des Kiosk und gilt auch, wenn jemand vom Telefon startet — die
      Oberflaechen-Loesung gilt nur innerhalb der Sitzung, die selbst gestartet
      hat.

      NICHT GEBAUT. Gemessen und vorbereitet.
    related:
      - mpv-trackname-geht-nie-mit
      - cover-folgt-der-position-nicht-dem-start
      - titelangaben-falsch
      - mupi-mpv-gebaut-aber-nie-eingeschaltet
  - kind: falle
    id: sicherung-leere-datei-besteht-jede-pruefung
    title: "Eine LEERE data.json in der Sicherung besteht jede Pruefung — und loescht beim Einspielen die Bibliothek"
    sev: high
    match: "inhalt_klage|stand_bauen_stabil|jsonfile.writeFile|api/add data.json|leere data.json|halbe data.json"
    cause: "`POST /api/add` schreibt data.json mit `jsonfile.writeFile` auf die ZIELDATEI — die wird beim Oeffnen auf 0 gekuerzt und erst danach gefuellt. Die Sicherung liest mit einem schlichten open()/read() und haelt keine Sperre; faellt sie in dieses Fenster, sichert sie 0 Bytes."
    fix: "`inhalt_klage` + `stand_bauen_stabil` in mupibox-sicherung.py: jede `.json` muss lesbar sein, und es wird zweimal gelesen und verglichen. Klage = harter Halt (kein Stand); Unruhe = nur ein Vermerk."
    tags: ['sicherung', 'wiederherstellung', 'wettlauf', 'data.json', 'E29', 2026]
    source: "05.08.2026 an .169 gemessen — tools/data-json-schreibfenster.sh, tools/sicherung-grenzfaelle.py (G1, G5, G6)"
    body: |
      DIE ZAHL, UM DIE ES GEHT: 20,8 %. So oft sah ein gleichzeitiger Leser
      data.json mit NULL BYTES, gemessen mit node, der echten Groesse (8 KB)
      und 4000 Schreibvorgaengen (tools/data-json-schreibfenster.sh).

      ZWEI WEGE SCHREIBEN DIESELBE DATEI, und nur einer tut es richtig:
        medienAendern / darstellungSchreiben   tmp -> rename   nie halb sichtbar
        POST /api/add  (server.ts ~7930)       fs.writeFile    sehr wohl

      NICHT „HALB", SONDERN „LEER" — und das ist der eigentliche Fund. Eine
      halbe Datei faellt beim naechsten json.loads auf. Eine LEERE ist
      widerspruchsfrei: ihre sha256 stimmt, das Archiv oeffnet sich,
      `--pruefen` sagte OK, `--probe` bestand, der Rueckweg lief Byte fuer
      Byte durch. Der Fehler zeigt sich in genau einem Augenblick — dem, in
      dem jemand den Stand einspielt. Dann ist die Bibliothek weg.

      GENAU SO IST DIE FRAGE DES BETREIBERS GEMEINT: „Eine halbe data.json in
      der Sicherung ist schlimmer als keine." Kein Stand ist ein sichtbares
      Loch. Ein leerer sieht aus wie ein guter und wird ohne Zoegern
      eingespielt.

      DER ZWEITE, GROESSERE FALL steckt daneben: ein Stand ist KEIN
      Schnappschuss. Die 18 Dateien werden NACHEINANDER gelesen; wer
      dazwischen speichert, bekommt zwei Dateien aus zwei Zeitpunkten
      (gemessen: 15615 von 33241 Staenden unter Dauerlast). Dieselbe Ursache
      wie in [[sicherung-mitten-in-der-wiederherstellung]], nur ohne
      Wiederherstellung.

      DAS MITTEL, DAS EINE SICHERUNG ALLEIN HAT: zweimal lesen und
      vergleichen. Eine Sperre waere sauberer, aber sie muesste auf BEIDEN
      Seiten stehen, und die andere Seite ist der Server. Weil die
      Inhalts-Kennung ALLE Dateien deckt, faengt ein und dasselbe Mittel
      beide Faelle.

      UND DIE UNTERSCHEIDUNG, AUF DIE ES ANKOMMT:
        * Klage (leer/unlesbar) -> GAR KEIN STAND. Harter Halt.
        * unruhig (es aendert sich etwas) -> Stand wird angelegt, mit
          Vermerk. Kein Stand waere hier schlechter als ein unscharfer.

      NACHGEMESSEN AN DERSELBEN MESSUNG, DIE DEN FEHLER ZEIGTE (G6): 0
      kaputte Staende von 30, und es entstehen weiterhin welche (8 Laeufe
      verweigerten). Eine Vorkehrung, die einfach immer verweigert, waere
      keine.

      WAS DIE SICHERUNG NICHT KANN: `.data.lock` des Servers liegt in /tmp
      (server.ts, `lockBasePath`), nicht in server/config — die gleichnamige
      DRAUSSEN-Zeile trifft nichts. Und `/api/add` gibt die Sperre frei,
      solange der Schreibvorgang noch laeuft (`fs.unlink` steht ausserhalb
      des Rueckrufs). Selbst wer die Sperre fragte, waere also nicht
      geschuetzt.
    related:
      - sicherung-mitten-in-der-wiederherstellung
      - stand-pruefen-las-den-gzip-strom-nie-zu-ende
      - mupi-konfiguration-schreiben
      - sicherung-probe-woechentlich
  - kind: falle
    id: stand-pruefen-las-den-gzip-strom-nie-zu-ende
    title: "Ein Pruefer mit blinden Flecken ist schlimmer als keiner — `--pruefen` uebersah 2,4 % aller Bit-Dreher"
    sev: mittel
    match: "stand_oeffnen|bitdreher|tarfile ende-merkmal|gzip crc|--pruefen blind|selbsttest wuerfelt"
    cause: "`tarfile` haelt am Ende-Merkmal des tar an und liest den Abspann des gzip nie — die Pruefsumme des Stroms wird damit nie geprueft. Die sha256 im Stand decken nur die Dateien ab, die in `dateien` STEHEN (LIESMICH.txt und Fuellbytes nicht)."
    fix: "In `stand_oeffnen` den gzip-Strom nach dem Auspacken zu Ende lesen (`gzip.open` + read-Schleife). Danach: 0 von 12077 Stellen echt blind."
    tags: ['sicherung', 'pruefen', 'tar', 'gzip', 'selbsttest', 'E29', 2026]
    source: "05.08.2026 an .169 gemessen — tools/stand-bitdreher-deckung.py, jedes einzelne Byte umgedreht"
    body: |
      `stand_oeffnen` STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die
      trennt: WAS STEHT IN PROBE.txt? Ein Python-Traceback heisst, die
      Funktion ist ueber einen Fehler gestolpert und niemand hat ihn
      gefangen — das ist
      [[kein-traceback-in-eine-datei-die-ohne-ssh-gelesen-wird]]. Ein
      sauberes „in Ordnung" heisst hier gar nichts: DIESER Eintrag
      handelt davon, dass dasselbe `stand_oeffnen` gruen meldet, obwohl
      das Archiv beschaedigt ist. Abbrechen und Luegen sehen in
      derselben Zeile verschieden aus, kommen aber aus derselben Naht.

      WORAN ES AUFFIEL, und das ist der lehrreichste Teil: der Selbsttest
      dreht EIN Byte in der MITTE des Archivs um und erwartet, dass die Probe
      durchfaellt. Auf der Box fiel er EINMAL rot und danach VIERMAL gruen —
      gleiche Fassung, gleiches md5. Ein Test, dessen Ergebnis an der Groesse
      des Archivs haengt, ist kein Test, sondern ein Wuerfel.

      DESHALB NICHT DIE MITTE, SONDERN JEDES BYTE. Das Werkzeug
      tools/stand-bitdreher-deckung.py dreht der Reihe nach jede einzelne
      Stelle um und fragt, ob `stand_oeffnen` darueber stolpert:

        12 KB-Stand (echt):   286 von 12077 unbemerkt   2,4 %
        1,7 KB-Stand:         208 von  1711 unbemerkt  12,2 %

      Je KLEINER der Stand, desto groesser der blinde Anteil — und desto
      wahrscheinlicher, dass ausgerechnet die Mitte hineinfaellt. Genau das
      hat der Selbsttest ausgewuerfelt.

      DIE URSACHE IST EINE ARBEITSTEILUNG, DIE NIEMAND ZU ENDE GEDACHT HAT:
        * die sha256 im Stand pruefen die DATEIEN — aber nur die, die in
          `dateien` stehen. LIESMICH.txt steht dort nicht.
        * die Pruefsumme des gzip-Stroms pruefte den REST — nur wurde sie
          nie erreicht, weil `tarfile` am Ende-Merkmal des tar anhaelt und
          den Abspann nie liest.
      Zwischen zwei Pruefungen, die einander fuer zustaendig hielten, lag
      ein Streifen, den keine ansah.

      DIE LEHRE UEBER DIESEN FALL HINAUS: EIN PRUEFER MIT BLINDEN FLECKEN IST
      SCHLIMMER ALS KEINER. Er sagt nicht „weiss ich nicht", er sagt
      BESTANDEN — und dieses Urteil steht woechentlich als PROBE.txt auf der
      Karte ([[sicherung-probe-woechentlich]]). `--pruefen` und `--probe`
      sind das EINZIGE, was zwischen einer muede gewordenen Karte und einem
      stillen Datenverlust steht.

      NICHT GEMERKT IST NICHT DASSELBE WIE SCHAEDLICH, und ohne diese
      Unterscheidung waere der Befund wertlos. Nach der Reparatur bleiben 65
      Stellen unbemerkt (Byte 4–68, der gzip-Kopf mit Zeitstempel und
      Betriebssystem). Das Werkzeug packt jede davon aus und vergleicht: alle
      65 liefern hinten Byte fuer Byte dasselbe. Dort ist NICHTS zu merken,
      und ein Alarm waere falsch. Echt blind: 0.

      DIE VIER ZEILEN, DIE ES BEHEBEN, kosten nichts (ein Stand ist 12 KB) —
      das Archiv wird nach dem Auspacken einmal zu Ende gelesen, damit die
      Pruefsumme des Stroms ueberhaupt zur Anwendung kommt.
    related:
      - sicherung-leere-datei-besteht-jede-pruefung
      - sicherung-probe-woechentlich
      - kein-traceback-in-eine-datei-die-ohne-ssh-gelesen-wird
  - kind: howto
    id: rueckweg-erst-gefahren-ist-er-bewiesen
    title: "Einen Rueckweg beweist nur, wer ihn faehrt: sichern, verstellen, zurueckspielen, nachsehen"
    match: "rueckweg-wirklich-fahren|wiederherstellung pruefen|sicherung gegenlesen|zeigeUhr verstellen"
    tags: ['sicherung', 'wiederherstellung', 'messen', 'werkzeug', 'E29', 2026]
    source: "05.08.2026 an .169 gefahren, 11 von 11 — tools/rueckweg-wirklich-fahren.sh"
    body: |
      `--probe` faehrt den Rueckweg gegen ein WEGWERF-Verzeichnis. Das
      beweist, dass sich ein Archiv oeffnen und auspacken laesst — es beweist
      NICHT, dass die laufende Box danach wieder so dasteht wie vorher.
      Dazwischen liegen die Rechte, die Ziele, der Server, der die Datei
      gerade liest, und die ausgelassenen Schluessel.

      DER ABLAUF, DER ES BEWEIST (tools/rueckweg-wirklich-fahren.sh):
        1. Stand anlegen, ANGEHEFTET (`--behalten`) — sonst zaehlt er gegen
           das Kontingent und schiebt einen fremden Stand heraus.
        2. nachsehen, dass der Stand den Wert von VORHER wirklich traegt
           (`tar -xzO` + md5). Ohne diesen Schritt beweist der Rest nichts.
        3. einen harmlosen Schalter WIRKLICH verstellen — ueber die API, also
           auf demselben Weg wie die Verwaltung.
        4. `--trocken` zuerst: es darf sich nichts aendern.
        5. zurueckspielen, und den alten Wert in DATEI UND API nachsehen.
        6. nachsehen, dass der Rueckweg selbst einen Rueckweg hat: der Stand
           «vor-wiederherstellung» muss den VERSTELLTEN Wert tragen.
        7. die eigenen Staende wieder wegnehmen.

      DER SCHALTER: `aktuell.zeigeUhr` in darstellung.json. Er zeigt eine Uhr
      an oder nicht — er kann die Box weder stumm noch unerreichbar machen,
      und `darstellungSchreiben` schreibt daneben und benennt um.

      DIE FALLE, DIE DEN LAUF EINMAL AN DEN FALSCHEN STELLEN GRUEN GEMACHT
      HAT: `true`/`false` sind in der Shell Zeichenketten und in Python
      Schluesselwoerter. Ein durchgereichtes `true` gibt einen NameError, die
      Verstellung fand gar nicht statt — und Schritt 5 verglich den alten
      Wert seelenruhig mit sich selbst. Umgedreht wird deshalb IN Python
      (`not d[...]`), nicht in der Shell.

      WORAN MAN SO ETWAS ERKENNT: ein Pruefschritt, der auch dann gruen ist,
      wenn der Schritt davor nichts getan hat, prueft nichts. Deshalb steht
      in Schritt 2 und 3 jeweils ein eigener Nachweis, DASS die Vorbereitung
      gewirkt hat — erst danach darf der eigentliche Vergleich zaehlen.
    related:
      - sicherung-probe-woechentlich
      - sicherung-rueckweg-ueber-die-karte
      - sicherung-leere-datei-besteht-jede-pruefung
      - ausliefern-regeln
  - id: mpv-media-title-traegt-je-dienst-verschieden
    kind: falle
    title: "mpv `media-title` folgt dem Titel — bei Jellyfin richtig, bei ARD mit der Doppelung (F1, Weg B)"
    match: "media-title|force-media-title|currentTrackname|titleClean|F1"
    tags: ["player", "mpv", "jellyfin", "ard", "anzeige", "falle", "messen", 2026]
    source: "05.08.2026 an .169 gemessen — tools/titelname-je-quelle.mjs (mpv-IPC-Socket + /player/local nebeneinander)"
    body: |
      [[mpv-trackname-geht-nie-mit]] sagt, DASS `currentTrackname` nicht
      mitgeht. Offen war, WO man es repariert. Der Abspieldienst waere der
      bessere Ort — dann waere die KLASSISCHE Oberflaeche mitgeheilt. Die
      Messung sagt: der naheliegende Weg dorthin traegt nur halb.

      GEMESSEN, im selben Augenblick drei Quellen nebeneinander (Dienst ueber
      /player/local, mpv ueber seinen IPC-Socket, Massstab aus
      /api/werke/<s>/inhalt):

        JELLYFIN
          mpvPos=3  media-title="Freibad Pommes"   liste[3]="Freibad Pommes"
                    dienst="Mittelmaessiges Leben"  <- Titel 1, seit Minuten
          Der Uebergang 1 -> 2 wurde mitgeschrieben: media-title geht mit,
          `currentTrackname` nicht.

        ARD
          mpv-Metadaten: title = "Zu Besuch | Die Maus zum Hoeren (…)"
          Das ist GENAU die Doppelung, die `titleClean` in ard.ts wegnimmt —
          Folgentitel plus Sendungsname plus Datum.

      FOLGE: Ein blosser Hoerer auf `media-title` im Abspieldienst heilte
      Jellyfin und VERDAERBE ARD. Genau deshalb schliesst der
      `metadata`-Hoerer in spotify-control.ts 'ard' (und rss/radio/jellyfin)
      heute ausdruecklich aus — das ist keine Nachlaessigkeit, sondern eine
      Sperre gegen diesen Fall.

      DER TRAGFAEHIGE WEG IM DIENST waere, den Namen beim EINREIHEN
      mitzugeben: mpv nimmt `force-media-title` als Datei-Option an
      `loadfile` an. Das aendert aber das Befehlsformat (`ard/…`, `jfqueue/…`)
      und beruehrt backend-api und BEIDE Oberflaechen — eine Entscheidung des
      Betreibers, nicht ein Nebensatz in einem Commit. NICHT GEBAUT.

      GEBAUT IST STATTDESSEN DER WEG IN DER NEUEN OBERFLAECHE:
      [[titelname-wird-nachgeschlagen-wie-das-cover]].

      ZWEI FALLEN, DIE DIE MESSUNG ZWEIMAL WERTLOS GEMACHT HABEN:
        * `seekpos:` NIMMT BEI mpv PROZENT, NICHT SEKUNDEN (spotify-control.ts,
          `seekAbsolut`: "mplayer PROZENT"). `seekpos:95` auf einem 140-s-Titel
          hiess 95 %, `seekpos:189` wurde auf 100 % geklemmt. Die Warteschlange
          lief in Sekunden durch und war leer, bevor die erste Messung stand —
          die Ausgabe sah aus wie „es laeuft nichts" und hiess „das Werkzeug
          hat es beendet".
        * DER ABSPIELDIENST ANTWORTET AUF 5005 AUF JEDEN PFAD MIT 200 UND
          JSON. Ein Abruf auf `5005/player/local` (statt `/local`, oder statt
          `8200/player/local`) liefert `{"status":"ok"}` — alle Felder leer.
          Das liest sich wie „der Dienst meldet nichts".
    related:
      - mpv-trackname-geht-nie-mit
      - titelname-wird-nachgeschlagen-wie-das-cover
      - cover-folgt-der-position-nicht-dem-start
      - server-antwortet-200-auf-alles

  - id: titelname-wird-nachgeschlagen-wie-das-cover
    kind: falle
    title: "Der Titelname wird NACHGESCHLAGEN, nicht geglaubt — und `np.titel` darf dabei nicht angefasst werden (F1)"
    match: "folgenNameAn|titelAnzeige|anzeigeMalen|passtZuLaufendem|F1"
    tags: ["oberflaeche", "neu", "player", "anzeige", "ard", "jellyfin", "falle", 2026]
    source: "05.08.2026 gebaut in NewDesign/app.js; geprueft in tools/pruef-neu-regeln.js (14 Faelle) und tools/e2e/neu-oberflaeche.test.mjs (Browserlauf, mit Gegenprobe)"
    body: |
      DIESER EINTRAG SETZT VORAUS, DASS DIE ANZEIGE UEBERHAUPT NACHZIEHT.
      Bleibt beim Titelwechsel ALLES stehen — Bild wie Text —, ist es der
      andere Eintrag mit demselben Symptomwort `anzeigeMalen`:
      [[drei-orte-eine-anzeige]] (dort wurden die Felder nur beim
      Oeffnen gesetzt). Hier wechselt das Bild und nur der NAME haengt.

      SEIT DAS COVER DER POSITION FOLGT, WIDERSPRICHT DIE ANZEIGE SICH SELBST:
      Das Bild wechselt beim Titeluebergang, der Name bleibt auf Folge 1
      stehen. Vorher war beides falsch und wenigstens in sich stimmig — das
      ist der Grund, warum F1 mit dem Cover DRINGEND geworden ist und nicht
      etwa unauffaelliger.

      DIE REPARATUR IST DIESELBE NACHSCHLAGEREI, AUS DERSELBEN ZEILE:
      `folgenNameAn(liste, nr)` steht neben `folgenBildAn`, gerufen aus
      `titelAnzeige(np)` neben `coverAdresse(np)`, beide einmal je Malen in
      `anzeigeMalen`. Dieselben drei Waechter: nur `art === 'lokal'`, nur fuer
      das Werk, zu dem die Liste gehoert, und nur solange es auch zu hoeren
      ist (`passtZuLaufendem`). Sagt einer nein, bleibt es beim gemeldeten
      Namen — also beim Verhalten von vorher.

      ── DIE STELLE, AN DER MAN ES FALSCH MACHT ────────────────────────
      Naheliegend waere, `np.titel` gleich in `jetztLaeuft` richtigzustellen.
      Das waere genau der Fehler, der beim Cover beim Gegenlesen auffiel
      ([[cover-folgt-der-position-nicht-dem-start]]): `passtZuLaufendem`
      vergleicht das Werk GEGEN `np.titel`/`np.unter`. Speiste man den
      nachgeschlagenen Namen dort ein, praefte sich der Nachschlagepfad gegen
      sein eigenes Ergebnis — eine Pruefung, die nicht mehr nein sagen kann.

      DASS DER WAECHTER TROTZDEM GREIFT, liegt an `np.unter`: bei ard und
      jellyfin setzt der Dienst `album` aus dem BEFEHL (`parts[1]`, der
      Interpret bzw. der Sendungsname), nicht aus der Datei — dieses Feld
      bleibt also nicht stehen. Der Abgleich laeuft ueber den Interpreten,
      nicht ueber den Titel, und genau deshalb funktioniert er, waehrend der
      Titel falsch ist.

      Auch `zustand.anker.titel` bleibt am gemeldeten Namen. Er dient dem
      ERKENNEN einer neuen Meldung, nicht der Anzeige.

      ── EIN LEERER NAME IST KEIN NAME ─────────────────────────────────
      `folgenNameAn` gibt bei leer, nur-Leerzeichen, fehlend oder Nicht-Text
      `null` zurueck. Kaeme '' durch, loeschte der Aufrufer die Titelzeile —
      und eine leere Titelzeile liest sich als „es laeuft nichts", waehrend
      Ton aus dem Lautsprecher kommt. Deshalb steht die Regel als EIGENE
      Funktion neben `folgenBildAn` und nicht als gemeinsames
      `folgenFeldAn(…, feld)`: beim Bild heisst `null` „geh eine Stufe
      tiefer", beim Namen „nimm, was der Dienst meldet".

      Der Listenname geht durch dieselbe Reinigung wie der gemeldete
      (`sauberterTitel`) — sonst spraenge die Schreibweise („Folge 12_ Der
      Fluch") beim Wechsel der Quelle.

      ── DIE GEGENPROBE GEHOERT ZUM TEST ───────────────────────────────
      Mit `np.titel` statt `titelAnzeige(np)` faellt der Browserlauf durch
      (nachgestellt, nicht behauptet). Die Attrappe meldet `currentTrackname`
      FEST als „Folge 3 — Die Maus", waehrend `currentTracknr` wandert; steht
      dieser Text am Ende in der Titelzeile, hat die Seite geglaubt statt
      nachgeschlagen.

      NICHT GEMESSEN: am Geraet. Die auf der Box liegende Fassung von
      NewDesign/app.js ist aelter als der Baum — sie kennt `folgenBildAn`
      noch gar nicht.
    related:
      - cover-folgt-der-position-nicht-dem-start
      - mpv-trackname-geht-nie-mit
      - mpv-media-title-traegt-je-dienst-verschieden
      - drei-orte-eine-anzeige

  - id: beruehrziele-neue-oberflaeche-gemessen
    kind: knowhow
    title: "Die Beruehrziele der neuen Oberflaeche, getastet statt gerechnet — 97 von 165 unter 9 mm (E8/T2)"
    match: "beruehrziele-neu|elementFromPoint|9241-411|tipp-spiel|E8"
    tags: ["oberflaeche", "neu", "beruehrziele", "messen", "werkzeug", "e8", "f6", 2026]
    source: "05.08.2026, tools/beruehrziele-neu.mjs gegen tools/neu-vorschau.mjs, 800x480, 10 Schirme"
    body: |
      0,14 mm/px, Marke 9 mm (ISO 9241-411) = 64,3 px. Gemessen wird die
      SCHMALERE Seite der Trefferflaeche — 200x20 px sind flaechenmaessig
      ueppig und mit dem Finger trotzdem nicht zu treffen.

      GETASTET, NICHT GERECHNET: `width: 44px` ist eine Absicht. padding,
      border und transform kennt `getBoundingClientRect`; ein Pseudoelement
      mit negativem `inset` NICHT; eine Ebene darueber macht keine Zahl
      kleiner; `overflow: hidden` eines Vorfahren schneidet ab. Gemessen wird
      deshalb ueber `document.elementFromPoint`, von einem treffenden Punkt
      aus nach aussen.

      DIE ZAHLEN (10 Schirme, 165 erreichbare Ziele, 97 unter der Marke):

        1,40 mm  .leute-kachel bei offenem Laut-Fenster (92x10) — das Fenster
                 deckt die Reihe bis auf 10 px zu, und der Rest BLEIBT
                 TIPPBAR. Am Bild gesehen, in keiner Groessenangabe zu finden.
        3,50 mm  .tipp-spiel am Rand der Lane-Reihe (25x45, beschnitten)
        4,76 mm  #mp-laut-regler 582x34 — der schmalste eigene Regler
        6,16 mm  .tipp-spiel 44x44 — der Knopf, um den es bei F6 geht
        6,30 mm  #gr-schieber 540x44
        8,96 mm  64x64 — die HAEUFIGSTE Groesse ueberhaupt (Kopfknoepfe,
                 Mini-Player, PIN-Tasten, Fachknoepfe, Rueckweg). Das Projekt
                 rundet die Marke selbst auf 64 px ab.

        engste Abstaende
          0,14 mm  #zurueck <-> .fach-knopf „WLAN" (1 px!)
          0,84 mm  .kat „Alles" <-> „Musik" (6 px)
          1,26 mm  .tor-taste „1" <-> „2" (9 px)
          0,00 mm  .kachel <-> #mp-zurueck (der Mini-Player stoesst ans Raster)

      WAS DAS FUER F6 HEISST — und es ist eine Zahl, keine Meinung:
      Eine Titelkachel ist 96 px breit (`.lane-kachel.stueck`), eine
      Albumkachel 118 px. ZWEI Knoepfe auf 9 mm nebeneinander brauchen 128 px
      plus Abstand — mehr, als beide Kacheln breit sind. Selbst zwei 44er
      brauchen mit 2 mm Abstand 102 px und passen auf 96 px NICHT. „Beide
      Knoepfe aufs Cover" ist damit ausgeschlossen, solange die Kachel so
      breit ist wie heute.

      DREI TRENNUNGEN, OHNE DIE DER BERICHT RAUSCHEN IST — jede war ein
      Fehlschlag im ersten Lauf:
        * NUR IM SICHTBAREN TEIL SUCHEN. Eine Raster-Kachel ragt unten heraus,
          ihre Mitte liegt bei y=700; dort trifft nichts, weil es den Punkt
          nicht gibt. Alle vier Kacheln der Startseite standen als „verdeckt"
          im Bericht und waren gut sichtbar und gut treffbar.
        * ABGESCHALTETE KNOEPFE SIND KEINE ZIELE. Der Rueckweg steht auf der
          Startseite mit `disabled` + `pointer-events: none` da (ausgegraut
          statt weg). Voll im Bild und nicht zu treffen — das sieht aus wie
          eine verdeckende Ebene und ist Absicht.
        * VERDECKT ist nicht ZU KLEIN und nicht ANGESCHNITTEN. Steht der
          grosse Player offen, liegen 26 Knoepfe zu Recht darunter. Gezaehlt
          wird, was VOLL IM BILD ist und trotzdem unter der Marke liegt.

      UND EINE BEIM ANSTEUERN DER SCHIRME: EIN TIPP AUF EINE ALBUM-KACHEL
      OEFFNET NICHTS, ER STARTET. Nur eine Spotify-Playlist und eine
      ARD-Sendung klappen auf (`kachelBauen`: `istPlaylist || istArdSendung`)
      — von aussen erkennbar daran, dass NUR diese beiden einen eigenen
      Play-Knopf im Bild tragen.

      DAS BILD GEHOERT DAZU, nicht statt dessen ([[rueckweg-verdeckt-die-ueberschrift]]):
      `--bild` legt je Schirm ein PNG ab. Das Laut-Fenster quer ueber der
      Interpreten-Reihe steht in keiner Zahl und auf jedem Bild.

      AUFRUF
        node tools/beruehrziele-neu.mjs [--alle] [--md] [--schirm <name>]
                                        [--bild /pfad/vorsilbe] [--pruefen]
      Schirme: start, raster, lane, lane-tief, interpret, player, cover-voll,
      laut, eltern-tor, eltern-flaeche.
    related:
      - rueckweg-verdeckt-die-ueberschrift
      - entwurf-gibt-gliederung-nicht-masse
      - blauer-play-knopf-in-den-lanes
      - neue-oberflaeche-ohne-box-ansehen
  - kind: howto
    id: f1-titelname-geht-mit-gebaut
    title: "F1 GEBAUT: der Titelname reist als force-media-title im Warteschlangeneintrag mit"
    match: "F1 gebaut|titelname geht mit|force-media-title gebaut|currentTrackname folgt|titelname abspieldienst behoben"
    tags: ['player', 'mpv', 'jellyfin', 'ard', 'anzeige', 'F1', 'gebaut', 2026]
    source: "06.08.2026 gebaut und an Box .169 gemessen (mpv 0.40.0), tools/f1-titelname-geht-mit.mjs. Commit a2214129."
    body: |
      [[mpv-nimmt-den-titelnamen-an-force-media-title]] sagt, DASS es geht.
      Hier steht, was daraus gebaut wurde und was die Gegenprobe ergab.

      ══ DIE DREI STELLEN ══════════════════════════════════════════════════
        mpv-protokoll.ts   `ladeBefehl` haengt bei gegebenem Titel
                           `-1 {"force-media-title": …}` an den `loadfile`.
                           OHNE Titel bleibt der Befehl WORTGLEICH wie vorher
                           — lokale Bibliothek und Vorlesen aendern sich nicht.
                           Dazu `media_title` in EIGENSCHAFTEN (damit wird es
                           beobachtet) und in `darfGemeldetWerden`.
        mpv-wrapper.ts     `play(f, titel?)` / `queue(f, titel?)`, plus der
                           RUECKFALL (siehe unten).
        spotify-control.ts jellyfin/ard geben `parts[0]` an `playURL` weiter;
                           jfqueue/ardqueue lesen `command.name`, den sie
                           vorher weggeworfen haben. Ein Hoerer auf
                           `media_title` schreibt `currentTrackname` — aber
                           NUR bei currentType jellyfin/ard, also genau den
                           beiden Typen, denen wir den Namen auch mitgeben.

      ══ DIE GEGENPROBE AM GERAET — sie ist der eigentliche Beweis ═════════
      Dieselbe Messung, einmal mit der alten und einmal mit der neuen Fassung
      (`spotify-control.js` getauscht, Dienst neu gestartet, Jellyfin-Album
      „HAMM", Uebergang befohlen UND von selbst):

        ALT   nr 1 „Mittelmaessiges Leben"  ✔
              nr 2 „Mittelmaessiges Leben"  ✘   (mpv media-title: „Nicht alleine")
              nr 3 „Mittelmaessiges Leben"  ✘   (mpv media-title: „Freibad Pommes")
              -> 33 von 37 Messungen falsch

        NEU   nr 1 „Mittelmaessiges Leben"  ✔
              nr 2 „Nicht alleine"          ✔
              nr 3 „Freibad Pommes"         ✔
              -> 37 Messungen, kein Fehlgriff

      BEI DER ARD IST DER GEWINN GROESSER, und er ist der Grund fuer den
      erzwungenen Titel: mpv meldet jetzt „Faelschung" statt
      „Faelschung | Die Maus zum Hoeren (0101.0101.70707070})". Der erzwungene
      Titel GEWINNT gegen die Merkmale der Ausspielpartner — ein blosser
      Hoerer auf `media-title` haette hier die Doppelung angezeigt
      ([[mpv-media-title-traegt-je-dienst-verschieden]]).

      ══ DER RUECKFALL, und warum er kein Zierrat ist ══════════════════════
      Die Langform (`-1` plus Datei-Optionen) gibt es erst in neueren mpv. Ein
      aelteres antwortet mit „invalid parameter" — STILL, im Socket, waehrend
      der Dienst der Oberflaeche weiter `{status:'ok'}` schickt. Dann wuerde
      GAR NICHTS geladen: aus einem falschen Titelnamen wuerde eine stumme
      Box. Der Aufsatz merkt sich deshalb je `loadfile` mit Titel den Befehl
      OHNE Titel und schickt ihn nach, wenn mpv ablehnt. Genau EINMAL — ein
      zweites `append-play` haengte die Folge sonst doppelt ein.

      ══ WAS NICHT GEHEILT IST ═════════════════════════════════════════════
      `path.parse(req.url).name` SCHNEIDET AM LETZTEN PUNKT AB. Ein Befehl
      `…/ardqueue/<adr>/Folge 1. Teil:title:artist:X` liefert als `name` nur
      „Folge 1"; der Rest gilt als Dateiendung. Das trifft die Zweige
      radio/rss/jellyfin/ard SEIT JEHER und ist NICHT neu — F1 zerlegt
      absichtlich Zeichen fuer Zeichen gleich, damit die erste Folge nicht
      anders heisst als die zweite. Wer es behebt, behebt es fuer alle vier
      Zweige auf einmal (`req.url` statt `command.name`).

      ══ WEG A BLEIBT STEHEN ═══════════════════════════════════════════════
      `folgenNameAn`/`titelAnzeige` in NewDesign/app.js wurden NICHT entfernt.
      Auf einer mplayer-Box gibt es kein `media-title`; dort traegt weiterhin
      der Nachschlag in der Oberflaeche. Beide sagen dasselbe, weil beide aus
      derselben Titelliste stammen.
    related:
      - mpv-nimmt-den-titelnamen-an-force-media-title
      - mpv-media-title-traegt-je-dienst-verschieden
      - mpv-trackname-geht-nie-mit
      - titelname-wird-nachgeschlagen-wie-das-cover
  - kind: entscheidung
    id: f6-zwei-knoepfe-nicht-aufs-cover
    title: "F6: zwei Knoepfe auf gleicher Hoehe — in der Lane, weil sie auf kein Cover passen"
    match: "F6|zwei knoepfe|von vorne knopf|weiterhoeren knopf|blauer weisser knopf|lane-wahl"
    tags: ['oberflaeche', 'neu', 'lane', 'weiterhoeren', '800x480', 'F6', 'entscheidung', 2026]
    source: "06.08.2026 gebaut; gemessen mit tools/beruehrziele-neu.mjs und im Browserlauf nachgemessen. Commit f623edc8."
    body: |
      DER WUNSCH (Aufgabe #48, Worte des Betreibers): „der blaue knopf ist
      fuer weiterspielen falls es was gibt und der weisse von vorne, wichtig
      die knoepfe muessen auf gleicher hoehe sein".

      DIE LUECKE: Es gab EINEN Knopf, der nur die Farbe wechselte. Lag zu
      einem Album eine gemerkte Stelle vor, war er blau und setzte fort — und
      dann fuehrte KEIN Weg mehr zum Anfang desselben Albums (ein Tipp auf die
      Album-Kachel klappt die Titel auf, er startet nichts).

      ══ DIE RECHNUNG, DIE DEN ORT ENTSCHEIDET ═════════════════════════════
        Album-Kachel  118 px breit      Titel-Kachel  96 px breit
        .tipp-spiel    44 px  =  6,16 mm — schon EINZELN unter der Marke
        zwei Knoepfe zu 9 mm  =  2 x 64 px  =  128 px PLUS Abstand

      128 passen weder auf 118 noch auf 96. Selbst zwei 44er brauchen mit 2 mm
      Abstand 102 px. „BEIDE KNOEPFE AUFS COVER" IST RECHNERISCH
      AUSGESCHLOSSEN — das ist keine Geschmacksfrage und muss nicht noch
      einmal erwogen werden ([[beruehrziele-neue-oberflaeche-gemessen]] hat die
      Zahlen).

      ══ WO SIE STATTDESSEN SITZEN ═════════════════════════════════════════
      In der aufgeklappten TITEL-Lane, unter deren Kopfzeile (`.lane-wahl`).
      Die Lane liegt ueber die ganze Breite (`grid-column: 1 / -1`).
      NACHGEMESSEN nach dem Bau: beide Knoepfe 64 px hoch, 176 px breit,
      Oberkante beide bei y = 116, 12 px Abstand. Die 9-mm-Marke wird hier
      gehalten — als eine der wenigen Stellen der Oberflaeche.

      ══ ZWEI ENTSCHEIDUNGEN, DIE DER BETREIBER ANDERS TREFFEN KANN ════════
        * SIE ERSCHEINT NUR MIT GEMERKTER STELLE. Ohne sie gibt es keine
          Wahl: der Knopf auf dem Cover IST dann „von vorn". Immer sichtbar
          kostete auf 480 px Hoehe dauerhaft Platz.
        * DER KNOPF AUF DEM COVER BLEIBT UNVERAENDERT (44 px, blau bei
          gemerkter Stelle). Er ist der kurze Weg fuer den haeufigen Fall.

      ══ DIE FALLE, DIE MAN AM SCHIRM NICHT SIEHT ══════════════════════════
      `albumSpielenAusLane` liest von sich aus `a.weiterAb`. Ein „Von
      vorne"-Knopf, der sie OHNE den Schalter `vonVorn` ruft, findet die
      gemerkte Stelle und spielt doch wieder dort weiter: richtige Aufschrift,
      gegenteilige Wirkung. Der Browserlauf prueft deshalb nicht das
      Aussehen, sondern dass die beiden Knoepfe VERSCHIEDENE Befehle
      schicken; tools/rotprobe.py faerbt genau diese Zeile
      (`f6-von-vorne-faengt-vorn-an`).

      ══ NICHT GEBAUT ══════════════════════════════════════════════════════
      Fuer eine ARD-SENDUNG und fuer die Werk-Ebene im Raster gibt es die
      Leiste nicht — dort liegt die gemerkte Stelle nicht an einem Album.
      Ob sie dort auch hingehoert, ist eine Frage an den Betreiber.
    related:
      - beruehrziele-neue-oberflaeche-gemessen
      - blauer-play-knopf-in-den-lanes
      - folge-wiederfinden-drei-stufen
  - id: f1-ansage-ueberschreibt-den-folgennamen
    kind: falle
    title: "F1-Nachlese: die ANSAGE schrieb den Folgennamen weg — die Weiche stand auf dem Typ, nicht auf dem Namen"
    match: "F1 nachlese|say ueberschreibt|sprichText media-title|currentTrackname ansage|vorlesen titelzeile|sprich?text"
    tags: ["player", "mpv", "vorlesen", "ard", "jellyfin", "anzeige", "falle", "F1", 2026]
    source: "06.08.2026 an Box .169 gemessen, mit Gegenprobe gegen die Fassung von vor F1 — tools/f1-vorlesen-ueberschreibt-schau.mjs"
    body: |
      [[f1-titelname-geht-mit-gebaut]] baut den media_title-Hoerer mit dieser
      Begruendung: er schreibe nur bei `currentType` 'jellyfin'/'ard', „also
      genau den beiden Typen, denen wir den Namen AUCH MITGEBEN". DIESER SATZ
      WAR NICHT WAHR, und der Fall dazu ist eine Zeile weiter unten in
      derselben Datei.

      `sprichText` (Zweig `say`, der letzte Rueckfall des Vorlesens in der
      KLASSISCHEN Oberflaeche, player.service.ts) laedt eine ganz andere Datei
      in DASSELBE mpv — ohne Namen und ohne `currentType` anzufassen. mpv
      meldet daraufhin den Titel der Ansage, die Weiche steht weiter auf
      'ard', der Hoerer schreibt.

      GEMESSEN, mit Gegenprobe (Datei getauscht, Dienst neu gestartet):

        MIT F1     vorher  currentTrackname = "Faelschung"
                   nachher currentTrackname = "sprich?text=Bibi Blocksberg"
        VOR F1     vorher  "Faelschung"   nachher  "Faelschung"

      Es ist also KEIN Altbestand, sondern mit F1 entstanden. Vorher wurde
      `media-title` gar nicht beobachtet, und der `filename`-Hoerer schreibt
      nur, wenn ueberhaupt noch kein Name dasteht
      (`if (!currentMeta.currentTrackname)`) — diese Schranke fehlte dem neuen
      Hoerer.

      BEHOBEN, INDEM DIE AUSSAGE WAHR GEMACHT WIRD, nicht indem eine Ausnahme
      dazukommt: die Ansage bekommt den Namen mit, der ohnehin schon in der
      Anzeige steht (`player.play(adresse, currentMeta.currentTrackname ||
      undefined)`). Damit traegt auch dieser Eintrag einen Namen, den WIR
      gewaehlt haben; die Titelzeile steht waehrend der Ansage still.

      NACHGEMESSEN, dass die Ansage dabei WIRKLICH SPIELT — sonst haette die
      Messung eine stumme Box fuer ein gutes Ergebnis gehalten. Ueber den
      mpv-Socket: `path` wechselt auf die Vorlese-Adresse, `time-pos` laeuft
      0,68 -> 1,49 s, `media-title` bleibt „Faelschung".

      NICHT ABGEDECKT, ausdruecklich: steht GAR KEIN Name in der Anzeige, geht
      auch keiner mit, und mpv meldet wie bisher die Adresse.

      DIE LEHRE, allgemeiner: Wo eine Weiche auf einem ZUSTAND steht
      (`currentType`), muss man alle Wege suchen, die den Zustand NICHT
      anfassen und trotzdem in dieselbe Maschine schreiben. `sprichText` war
      genau so ein Weg — und er stand in derselben Datei, 30 Zeilen entfernt.
    related:
      - f1-titelname-geht-mit-gebaut
      - mpv-nimmt-den-titelnamen-an-force-media-title
      - mpv-trackname-geht-nie-mit
      - mupi-vorlesen-lautstaerke
  - id: f6-wahlleiste-schiebt-das-cover-aus-dem-bild
    kind: offen
    title: "F6-Nachlese: die Wahlleiste kostet 76 px — und die gehen vom COVER ab (7 % statt 71 % sichtbar)"
    match: "F6 nachlese|wahlleiste cover|lane-wahl platz|weiterhoeren von vorne platz|laneInsBild cover"
    tags: ["oberflaeche", "neu", "F6", "beruehrziele", "480px", "offen", 2026]
    source: "06.08.2026 gemessen — tools/f6-wahlleiste-nachmessen.mjs, zwei volle Durchgaenge (mit/ohne Leiste), 800x480 headless gegen tools/neu-vorschau.mjs"
    body: |
      [[f6-zwei-knoepfe-nicht-aufs-cover]] rechnet richtig aus, dass zwei 9-mm-
      Knoepfe NICHT nebeneinander auf ein 118-px-Cover passen, und legt sie
      deshalb in die aufgeklappte Titel-Lane. Die Rechnung ist WAAGERECHT.
      Senkrecht geht sie nicht auf, und das war nicht gemessen.

      DIE LEISTE STEHT IM FLUSS und macht die Lane um 76 px hoeher (64 Knopf +
      12 Abstand). Die Lane rollt sich beim Aufklappen selbst ins Bild
      (`laneInsBild`), und der Rollbereich ist auf 800x480 nur 332 px hoch.
      Album-Reihe (168) + Titel-Lane (260) = 428 px. Die fehlenden 96 px gehen
      NACH OBEN weg — und dort steht das Cover des Albums, das man gerade
      geoeffnet hat.

      GEMESSEN, zwei volle Durchgaenge, EIN Unterschied (im zweiten ist
      `.lane-wahl` VOR dem Aufklappen auf display:none — derselbe Code, nur
      ohne Platzbedarf; das ist der Zustand von vor F6):

                                        mit Leiste   ohne Leiste
        Album-Kachel (Cover + Titel)       34 %          80 %
        das Coverbild selbst                7 %          71 %
        Titelkacheln 1-6                  100 %         100 %

      Keine Titelkachel verliert etwas — der Rollstand faengt alles ab. Es
      trifft AUSSCHLIESSLICH das Cover. Das ist genau das, was ausdruecklich
      sichtbar bleiben sollte.

      GESCHNITTEN WIRD GEGEN DIE BUEHNE, NICHT GEGEN DAS FENSTER. Im ersten
      Anlauf war gegen `innerHeight` gerechnet — dann meldet eine Kachel, die
      halb unter der Kopfleiste steckt, brav „100 % im Bild", und die
      Vor-F6-Lage saehe faelschlich makellos aus.

      DIE WAHL GEHOERT DEM BETREIBER, deshalb NICHT geaendert. Es gibt keinen
      Weg, der nichts kostet:
        * `laneInsBild` so kappen, dass die geoeffnete Kachel stehenbleibt —
          dann rutschen die Titelkacheln um 76 px nach unten, hinter den
          Mini-Player.
        * Die Knoepfe in die Kopfzeile der Lane (`.lane-kopf`) statt in eine
          eigene Reihe — kostet nur noch die Differenz zur bisherigen
          Kopfzeilenhoehe, aber nicht null.
        * Die Leiste nur zeigen, wenn die Lane ohnehin oben steht.
        * So lassen: die Leiste ist die Antwort auf die Frage, die man
          gerade hat; das Cover hat man eben angetippt.

      NEBENBEFUND, gemessen: die Luecke zwischen den beiden Knoepfen ist
      12 px = 1,68 mm. Dieselbe Norm, aus der die 9-mm-Marke stammt
      (ISO 9241-411), empfiehlt 2 mm = 14,3 px zwischen zwei Zielen — und die
      beiden hier tun GEGENSAETZLICHES. Drei Pixel wuerden es richten.
    related:
      - f6-zwei-knoepfe-nicht-aufs-cover
      - beruehrziele-neue-oberflaeche-gemessen
      - rueckweg-verdeckt-die-ueberschrift
  - id: mpv-no-terminal-verschluckt-alle-warnungen
    kind: falle
    title: "mpv laeuft mit --no-terminal: seine Warnungen erreichen den Dienst NIE — und niemand hoerte auf 'stderr'"
    match: "mpv stderr|no-terminal|mpv warnungen|player stderr hoerer|msg-level"
    tags: ["player", "mpv", "protokoll", "falle", 2026]
    source: "06.08.2026 an Box .169 nachgesehen: unerreichbare Adresse geladen, kein einziger mpv-Ausgabetext im journal"
    body: |
      DAS DRITTE LOCH STEHT NEBENAN: selbst wenn der Hoerer sitzt und mpv
      etwas schickt, verschluckt der Dienst die Meldung noch einmal — er
      laeuft mit `logLevel: error`, und `log.warn` gibt dort nichts aus
      ([[abspieldienst-loglevel-error-macht-warnungen-stumm]]). Beide
      Eintraege fuehren „player stderr hoerer" im `match:`, weil man beim
      Nachruesten BEIDE braucht. Reihenfolge: erst dieser Eintrag (kommt
      ueberhaupt etwas an?), dann der andere (wird es ausgegeben?).

      ZWEI LOECHER UEBEREINANDER, und beide sehen im Code wie das Gegenteil
      aus.

      1. mpv-wrapper.ts leitet `proc.stderr` an ein Ereignis 'stderr' weiter,
         mit dem Kommentar „mpvs Warnungen nicht verschlucken — sonst sucht
         man Fehler im Dunkeln". mpv wird aber mit `--no-terminal` gestartet,
         und das schaltet stdin/stdout/stderr GANZ ab. Aus diesem Rohr kommt
         nie etwas. NACHGESEHEN: eine unerreichbare Adresse geladen
         (127.0.0.1:9) — kein Wort im Protokoll.

      2. spotify-control.ts hatte auf 'stderr' GAR KEINEN Hoerer. Ein
         EventEmitter wirft dafuer nicht, er verwirft still. Damit fielen die
         drei Meldungen des Aufsatzes selbst ins Nichts:
           * „loadfile mit force-media-title abgelehnt — noch einmal ohne
             Titelnamen" (der F1-Rueckfall). Auf einer Box mit aelterem mpv
             waeren ALLE Folgennamen wieder falsch gewesen, ohne eine Zeile.
           * „roher Befehl ohne Entsprechung uebergangen" (exec bei mpv).
           * „mpv laesst sich nicht starten: …" — Box stumm, einziger Hinweis.

      BEHOBEN IST NUR (2): ein `player.on('stderr', …)` schreibt sie jetzt mit
      log.warn ins journal. (1) ist NICHT angefasst — `--no-terminal` gegen
      `--no-input-terminal` zu tauschen holt mpvs eigene Warnungen zurueck,
      aendert aber das Verhalten der Tonmaschine und die Menge im Protokoll.
      Das gehoert gemessen, nicht nebenbei geaendert.

      WORAN MAN SO ETWAS ERKENNT: ein Kommentar, der eine Meldung verspricht
      („Gemeldet wird es trotzdem"), ohne dass irgendwo ein Empfaenger steht.
      `grep "on('stderr'"` ist billiger als die Suche danach, warum eine Box
      stumm ist.
    related:
      - f1-titelname-geht-mit-gebaut
      - f1-ansage-ueberschreibt-den-folgennamen
      - server-antwortet-200-auf-alles
      - abspieldienst-loglevel-error-macht-warnungen-stumm
  - kind: knowhow
    id: sicherung-als-schritt-einer-auslieferung
    title: "`--anlegen` kostet 132 ms — aber Rueckgabewert 0 heisst NICHT, dass ein Stand da ist"
    match: "mupibox-sicherung.py --anlegen|sicherung-kosten|vor-Auslieferung|Sperre.gilt|imbau"
    sev: hoch
    tags: ['sicherung', 'ausliefern', 'E29', 'messen', 'rueckgabewert', 2026]
    source: "05.08.2026 an .169 gemessen — tools/sicherung-kosten.py, gegen die AUSGELIEFERTE Fassung (/usr/local/bin/mupibox/mupibox-sicherung.py, md5 91fdd3b2…)"
    body: |
      E29/B5 will, dass eine Auslieferung `mupibox-sicherung.py --anlegen`
      anstoesst, statt eine weitere `.vor-<stempel>`-Datei anzulegen. Bevor man
      sich darauf verlaesst, sind das die vier Zahlen und die eine Falle.

      ══ WAS ES KOSTET (Median aus 12 bzw. 5 Laeufen, .169) ══
        ganzer Befehl, von der Schale aus        132 ms
          davon Python-Start und Import           75 ms
          davon die Pause zwischen zwei Lesungen  50 ms  (fest verdrahtet)
          Lesen der 18 Dateien, EINMAL           2,4 ms
          Archiv schreiben inkl. zweimal fsync   17,5 ms
        dasselbe unter `sudo` als root           205 ms
      Ein Stand wiegt 12 KB. Die Kosten liegen also FAST NUR in Python-Start
      und der eingebauten Pause — an der Zahl der Dateien liegt es nicht. Fuer
      eine Auslieferung ist das kein Handel, sondern ein Rundungsfehler.

      Als root laeuft es sauber: `_dietpi_zurueckgeben` gibt Datei UND
      Verzeichnis an dietpi zurueck (gemessen: uid 1000, Modus 0600), dietpi
      kann danach selbst wieder ausraeumen.

      ══ DIE FALLE: 0 HEISST NICHT „ES GIBT EINEN STAND" ══
      `--anlegen` gibt in DREI verschiedenen Lagen 0 zurueck, und in zweien
      davon entsteht NICHTS:
        * es wurde ein Stand angelegt                      -> 0
        * eine Wiederherstellung laeuft (/run/mupibox-sicherung-laeuft)
          -> 0, KEIN Stand (gemessen: 0 Dateien im Ziel)
        * `--wenn-anders` und nichts hat sich geaendert    -> 0, kein Stand
      Wer nur `$?` prueft, glaubt in zwei von drei Faellen, er habe eine
      Sicherung. Der Aufrufer MUSS nachsehen, dass die Datei da und frisch
      ist — `--liste --json` nennt `erzeugt` je Stand.
      Die anderen Werte: 1 = kein Stand, weil eine Datei beim Lesen leer war
      (Normalfall bei gleichzeitigem `POST /api/add`, siehe unten);
      2 = gar kein Befehl uebergeben; `--pruefen` 1, wenn ein Stand kaputt ist.

      ══ DER GLEICHZEITIGE SCHREIBER: DIE REPARATUR GREIFT ══
      Nachgestellt mit einem Schreiber, der data.json (8 KB) im Muster von
      `jsonfile.writeFile` umbricht (4 ms offen und leer, 4 ms Pause),
      40 Laeufe gegen einen ABZUG des Baums:
        ungeschuetzt (stand_bauen, EIN Lesen, keine Pruefung)
                                        21 von 40 sahen data.json leer  52,5 %
        `--anlegen` (die ausgelieferte Fassung)
                                        32 Staende, davon MIT LEERER DATEI: 0
                                         8 Laeufe verweigerten (Code 1)
                                        13 der 32 tragen den Vermerk `unruhig`
      Das bestaetigt [[sicherung-leere-datei-besteht-jede-pruefung]] am Geraet.
      FOLGE FUER DEN AUFRUFER: unter Last verweigert etwa jeder fuenfte Lauf.
      Das ist richtig so — aber eine Auslieferung, die daran abbricht, braucht
      einen zweiten und dritten Versuch, sonst scheitert sie an einem Kind,
      das gerade etwas hinzufuegt.

      ══ DAS KONTINGENT RAEUMT — ABER `.behalten` ZAEHLT NICHT MIT ══
      Gemessen: 15 lose + 3 angeheftete Staende, BEHALTEN=10 -> genau 5 lose
      fallen, 10 lose und 3 angeheftete bleiben. 40 ANGEHEFTETE -> 0 fallen.
      Also: eine Auslieferung darf NIEMALS `--behalten` benutzen, sonst baut
      sie dieselbe Halde wieder auf, nur an einem anderen Ort. Achtung,
      `--ernten` heftet IMMER an (`name_bauen(stand, True)`).
      Zweite Kante: der Name hat SEKUNDENAUFLOESUNG. Zwei Staende in derselben
      Sekunde mit demselben Grund tragen denselben Namen — der zweite
      ueberschreibt den ersten still.

      ══ VOLLER DATENTRAEGER: EIN HALBES ARCHIV BLEIBT LIEGEN ══
      Nachgestellt mit RLIMIT_FSIZE=4096 (ENOSPC-Ersatz ohne root, gleicher
      Weg durch den Code): `OSError: [Errno 27]` faellt UNGEFANGEN aus
      `anlegen` heraus — an der Schale also ein Traceback, Code 1. Danach
      liegt `…-voller-traeger.tar.gz.imbau` im Verzeichnis, und dort BLEIBT
      es: `staende_lesen` sieht nur `*.tar.gz`, die Auslese kennt `.imbau`
      nicht. Das Werkzeug gegen Altlasten hinterlaesst damit eine eigene.
      Auf .169 ist das heute folgenlos (107 GB frei, ein Stand 12 KB) und
      liegt aktuell auch keine `.imbau` herum — aber ein Aufrufer sollte vor
      dem Tausch nicht nur auf Code 0 sehen.

      ══ WAS EIN STAND NICHT ENTHAELT — der wichtigste Satz fuers Ausliefern ══
      `BAEUME` sind /etc/mupibox und server/config. server.js,
      spotify-control.js, www und www-admin sind NICHT dabei. Ein Stand ist
      der Rueckweg fuer die NUTZERDATEN, nicht fuer den ausgelieferten Code.
      Die `.vor-<stempel>`-Dateien von server.js und die `www.alt` ersetzt er
      also NICHT — dafuer braucht die neue Auslieferung ihre eigene Regel
      (hoechstens EINE Erzeugung, die naechste raeumt die vorige weg).
    related:
      - sicherung-leere-datei-besteht-jede-pruefung
      - sicherung-mitten-in-der-wiederherstellung
      - aufraeum-sicherung-vor-der-entscheidung
      - ausliefern-regeln

  - kind: falle
    id: altlasten-werkzeug-sieht-www-vor-ohne-bindestrich-nicht
    title: "54 MB Auslieferungsreste, die mupibox-altlasten.py nicht kennt: `www.vor`, `.vor2`, `.rel`, `.kaputt`"
    match: "mupibox-altlasten.py MUSTER|www\\.vor2|www\\.kaputt|www\\.rel|admin\\.rel"
    sev: mittel
    cause: "`MUSTER` verlangt nach `.vor` einen BINDESTRICH (`\\.vor-[a-zA-Z0-9-]+$`) oder eine Zahl. Die im Juli benutzten Namen `www.vor`, `www.vor2`, `www.rel`, `www.kaputt`, `admin.rel` treffen keine einzige Zeile — der Ordner wird gefunden, aber nicht als Altlast erkannt."
    tags: ['altlasten', 'ausliefern', 'E29', 'werkzeug', 'falle', 2026]
    source: "05.08.2026 an .169 nebeneinandergelegt: `mupibox-altlasten.py --json` gegen `du -sh` im Auslieferungsordner"
    body: |
      `mupibox-altlasten.py --json` meldete an .169 19 Funde, 190 KB. Im
      selben Verzeichnis lagen gleichzeitig:

        www.vor 11M   www.vor2 11M   www.vor3 11M   www.vor4 11M
        www.kaputt 2,1M   www.rel 6,1M   admin.rel 1,6M
        www-admin.vor 452K   www-admin.vor2 428K      ── zusammen 54 MB

      vom 26.–28.07., also 280-mal so viel wie alles, was das Werkzeug sieht.
      Ordner werden durchaus mitgesammelt (`sammeln()` nimmt `ordner`
      ausdruecklich dazu) — es liegt allein an den Mustern.

      DIE LEHRE: eine Musterliste bildet die Namen ab, die man BEIM SCHREIBEN
      im Kopf hatte. Die Praxis von Hand erfindet aber jedes Mal neue —
      genau weil sie von Hand ist. Solange die Auslieferung ihre Reste selbst
      benennt und selbst wegraeumt (E29/B5), ist das Werkzeug die zweite
      Verteidigungslinie, nicht die erste. Wer ihm allein glaubt, haelt eine
      Box fuer aufgeraeumt, auf der 54 MB liegen.

      NACHMESSEN, und zwar nebeneinander — nie nur das Werkzeug fragen:
        python3 /usr/local/bin/mupibox/mupibox-altlasten.py --json
        du -sh /home/dietpi/.mupibox/Sonos-Kids-Controller-master/*
    related:
      - www-altlasten-und-gehashte-dateinamen
      - sicherung-als-schritt-einer-auslieferung
      - ausliefern-regeln

  - kind: howto
    id: ausrollweg-ausliefern-py
    title: "Auf die LAUFENDE Box ausliefern: tools/ausliefern.py (einer statt von Hand)"
    tags: ['ausliefern', 'deploy', 'E29', 'werkzeug', 'altlasten', 2026]
    source: "05.08.2026 gebaut und an .169 durchgemessen: Probelauf, zwei echte Tausche, zweimal zurueckgedreht, vier Abbruchwege"
    body: |
      EIN Weg vom Arbeitsrechner auf die laufende Box. Er ersetzt
      `src/deploy_on_device.sh` (das seit dem 05.08.2026 mit Code 64 abbricht)
      und die Handarbeit, aus der die 99 Altdateien (163 MB) entstanden.

        python3 tools/ausliefern.py --probe    # alles pruefen, NICHTS tauschen
        python3 tools/ausliefern.py            # bauen, sichern, tauschen, nachmessen
        python3 tools/ausliefern.py --nur server,player
        python3 tools/ausliefern.py --zurueckdrehen
        mupictl --device <host> deploy [--probe …]      # reicht dorthin durch

      NICHT VERWECHSELN: `src/deploy.sh` bleibt gueltig — es packt
      `bin/nodejs/deploy.zip` fuer eine FRISCHE Karte. Dieser Weg hier bedient
      die laufende Box.

      ── DER TAUSCH HAT KEINE LUECKE MEHR, OHNE VERZEICHNISUMBAU ────────────
      ext4 kann zwei Verzeichnisse in EINEM Schritt vertauschen:
      `renameat2(AT_FDCWD, a, AT_FDCWD, b, RENAME_EXCHANGE)`. Am Geraet
      nachgemessen (aarch64, /home/dietpi auf ext4, Syscall-Nummer 276):
      rc 0, Inhalte getauscht. Damit gibt es keinen Augenblick, in dem `www`
      fehlt — und der Verweis-Umbau aus [[frontend-ausspielen-ohne-luecke]]
      ist dafuer NICHT noetig. Das ist wichtig, weil `unzip -o` aus
      autosetup/update sonst in einen Verweis hineinschriebe.
      Ist `www` auf einer Box DOCH ein Verweis, bricht der Weg ab statt zu raten.
      Dateien: harte Verknuepfung als Rueckweg, dann `rename(2)` — auch atomar.

      ── GENAU EINE RUECKDREH-ERZEUGUNG ────────────────────────────────────
      Der Rueckweg heisst IMMER `<ziel>.zurueck` (fester Name), und die
      naechste Auslieferung ueberschreibt ihn. Nachgemessen: zwei
      Auslieferungen hintereinander liessen die Zahl der Eintraege im
      Auslieferungsordner bei 28 und den belegten Platz bei 5 849 816 KB —
      unveraendert. Frueher wuchs beides bei jedem Lauf.
      `--zurueckdrehen` VERTAUSCHT (auch Dateien), der Rueckweg fuehrt also in
      beide Richtungen; nachgewiesen ueber die Inode-Nummern (385769/384405 →
      384405/385769 → zurueck).

      ── WAS ER NICHT GLAUBT ───────────────────────────────────────────────
      * NICHT dem Bau-Rueckgabewert. Nach `npm run build` wird der ZEITSTEMPEL
        aller vier Ausgaben geprueft; ist eine aelter als der Baubeginn, ist
        Schluss (zweiter Anlauf davor). Grund: [[esbuild-schreibfehler-endet-mit-null]]
        und der WASM-Absturz, siehe unten.
      * NICHT `mupibox-sicherung.py --anlegen`s Rueckgabewert (in drei Lagen 0,
        in zweien entsteht nichts) — der Stand wird ueber `--liste --json`
        NACHGEWIESEN, bis zu drei Versuche. NIEMALS `--behalten`.
      * NICHT HTTP 200. Nachgemessen wird die md5 am Geraet, dass der Dienst
        ueber 5 s hinweg dieselbe Startzeit und denselben Neustartzaehler
        behaelt, und dass `GET /`, `GET /admin/` und
        `GET /api/oberflaeche/stand` den NEUEN `main-*.js` nennen.

      ── WAS GLEICH IST, WIRD NICHT GETAUSCHT ──────────────────────────────
      Vor dem Tausch wird der laufende Stand gemessen. Ist ein Ziel identisch,
      entfaellt Tausch UND Neustart — und der vorhandene Rueckweg bleibt heil.
      `--erzwingen` hebt das auf (fuer einen zerschossenen Ordner).

      ── RUECKGABEWERTE (alle vier Abbruchwege am Geraet geprueft) ──────────
        0 fertig / nichts zu tun   1 Umgebung   2 Bau   3 Pruefung vor dem Tausch
        4 Sicherung nicht nachweisbar   5 Tausch   6 danach   7 Probelauf beanstandet
      1–4 fassen den Betriebsordner NICHT an. Das Zwischenlager
      (`/home/dietpi/.mupibox/.ausliefern`) wird auch nach einem Abbruch
      geraeumt — mit EINER Ausnahme: bricht der Tausch mittendrin ab, bleibt
      es stehen, weil dort der alte Inhalt liegen koennte.
    related:
      - ausliefern-regeln
      - mupi-kiosk-haengt-auf-json-nach-deploy
      - frontend-ausspielen-ohne-luecke
      - spotify-control-liegt-woanders
      - esbuild-schreibfehler-endet-mit-null
      - sicherung-als-schritt-einer-auslieferung
      - altlasten-werkzeug-sieht-www-vor-ohne-bindestrich-nicht

  - kind: falle
    id: esbuild-wasm-rangeerror-haengt-an-stderr
    title: "Bau stuerzt mit „RangeError: Invalid array length\" ab — sobald stderr auf eine DATEI zeigt"
    match: "RangeError: Invalid array length|esbuild-wasm|writeOrBuffer"
    sev: hoch
    cause: "`node_modules/.bin/esbuild` ist ein Verweis auf esbuild-WASM (via @angular-devkit/build-angular → esbuild-wasm@0.25.9), und dessen Shim leitet `fs.writeSync` auf `process.stderr.write` um. Ist stderr eine regulaere Datei oder /dev/null, ist `process.stderr` ein SyncWriteStream — dessen Schreibpfad wirft unter Node 26 in `writeOrBuffer`."
    fix: "stderr durch eine PIPE laufen lassen (`subprocess.PIPE`, oder `2>&1 | cat`), nicht in eine Datei umlenken. Ersatzweise `--log-level=silent`. Danach trotzdem den ZEITSTEMPEL der Bauausgabe pruefen."
    tags: ['bau', 'esbuild', 'deploy', 'falle', 2026]
    source: "05.08.2026 als Matrix gemessen: derselbe Befehl, nur die Umlenkung geaendert"
    body: |
      Der Absturz ist NICHT „gelegentlich", sondern deterministisch — er haengt
      allein daran, wohin stderr zeigt:

        stdout   stderr                        rc   Ausgabedatei
        Pipe     Pipe                          0    1156423
        Datei    Datei                         1    fehlt
        Pipe     Datei                         —    fehlt
        Datei    Datei + --log-level=silent    0    1156423

      Der nativ ausgefuehrte `node_modules/esbuild/bin/esbuild` (ELF, kein
      JS-Shim, 0.25.10) liefert in ALLEN Varianten rc 0 und 1156423 Bytes.

      DAS TEUERSTE DARAN IST NICHT DER ABSTURZ. Danach bleibt eine VORHANDENE
      `src/deploy/server.js` unangetastet liegen (md5 vorher = nachher), und
      je nach Pipeline steht `rc=0` daneben. Genau so liefert man eine ALTE
      server.js aus und schliesst, die eigene Reparatur wirke nicht.
      Deshalb prueft `tools/ausliefern.py` den ZEITSTEMPEL, nicht den
      Rueckgabewert und nicht die blosse Existenz.

      ZWEITE, UNABHAENGIGE URSACHE derselben Meldung „1 error":
      `src/backend-api/backend-api.esbuild-meta.json` gehoert root (24.07.),
      der npm-Aufruf enthaelt `--metafile=…`. esbuild meldet „Failed to write
      to output file … Permission denied" und endet mit **0** — die Signatur
      [[esbuild-schreibfehler-endet-mit-null]], nur ist die Datei inzwischen
      die Metadatei statt server.js. Gemessen am 05.08.2026: server.js WURDE
      dabei trotzdem frisch geschrieben. `src/deploy` selbst gehoert wieder
      achim — [[mupi-src-deploy-gehoert-root]] ist als ZUSTANDSaussage
      ueberholt, das root-Erbe steckt nur noch in dieser Metadatei und in
      `src/backend-api/cache`, `src/frontend-box/.angular`.
    related:
      - esbuild-schreibfehler-endet-mit-null
      - mupi-src-deploy-gehoert-root
      - ausrollweg-ausliefern-py

  - kind: falle
    id: rueckweg-gleicher-inode-meldet-erfolg
    title: "Rueckweg und Ziel als EIN Inode: --zurueckdrehen meldet Erfolg und tut nichts"
    match: "zurueckdrehen|Zurueckgedreht|RENAME_EXCHANGE|.zurueck"
    sev: hoch
    cause: "`os.link(ziel, zurueck)` und `os.replace(neu, ziel)` in modus_tauschen sind zwei Schritte. Reisst der Lauf DAZWISCHEN ab, sind Ziel und Rueckweg zwei Namen fuer denselben Inode. RENAME_EXCHANGE auf zwei Namen desselben Inodes ist wirkungslos — der Weg trug das Ziel trotzdem unter 'zurueck' ein."
    fix: "Vor dem Vertauschen `os.stat(zurueck).st_ino == os.stat(ziel).st_ino` pruefen und als `ohne_wirkung` melden (Rueckgabe 5), nicht als Erfolg. Behoben in tools/ausliefern.py, c307c8df."
    tags: ['ausliefern', 'deploy', 'rueckweg', 'falle', 'inode', 2026]
    source: "05.08.2026 beim Gegenlesen an .169 gefunden — die Box lag genau so da"
    body: |
      SO SAH ES AUS, und zwar nicht erdacht, sondern vorgefunden:

          385812 -rw-r--r-- 2 dietpi dietpi 1327305 spotify-control.js
          385812 -rw-r--r-- 2 dietpi dietpi 1327305 spotify-control.js.zurueck

      Ein Inode, zwei Verweise. Der Helfer antwortete darauf
      `{"zurueck": ["player"], "fehler": []}` — und die Datei lag unveraendert
      da. Wer das liest, glaubt, er sei zurueck.

      WIE MAN ES ERKENNT: `ls -li <ziel> <ziel>.zurueck`. Gleiche Inode-Nummer
      in Spalte 1 (und Verweiszahl 2) heisst: es gibt hier keinen Rueckweg,
      egal was gemeldet wird. `tools/ausliefern-gegenlesen.py` liest die
      Inode-Nummern von sich aus mit.

      WIE MAN HERAUSKOMMT: den Stand neu ausliefern (`--erzwingen`) — danach
      hat der Rueckweg wieder einen eigenen Inode. Es gibt KEINEN Weg zurueck
      auf den Stand davor; der ist mit dem Abriss verloren gegangen.

      ALLGEMEINER: ein Rueckweg, der still nichts tut, ist schlimmer als gar
      keiner. Er wird genau in dem Augenblick geglaubt, in dem man ihn braucht.

  - kind: knowhow
    id: ausliefern-py-gegengelesen-durch-benutzen
    title: "tools/ausliefern.py gegengelesen — durch Benutzen, an .169"
    tags: ['ausliefern', 'deploy', 'pruefen', 'werkzeug', 'E29', 2026]
    source: "05.08.2026: Probelauf, zwei Abbruchwege, echte Auslieferung mit WIRKLICH neuem Inhalt, Rueckweg, Zaehlung"
    body: |
      Gelesen wurde nicht, sondern gefahren. Was dabei BELEGT ist — und was
      dabei als falsch aufgefallen ist, steht in
      [[rueckweg-gleicher-inode-meldet-erfolg]] und in c307c8df.

      ── WAS TRAEGT ───────────────────────────────────────────────────────
      * Probelauf (`--probe`, mit Bau): rc 0. Danach alle vier Ziele Hash fuer
        Hash unveraendert, Inodes unveraendert, kein Dienst neu gestartet,
        kein Zwischenlager zurueckgeblieben.
      * ECHTE Auslieferung mit WIRKLICH neuem Inhalt — die Luecke, die der
        Erbauer selbst als "nicht gemessen" hinterlassen hatte. `www` ging von
        main-OCUFZNF5.js auf main-HSNDLVOV.js; `GET /` und
        `GET /api/oberflaeche/stand` nannten danach den neuen Namen.
      * RUECKWEG mit echtem Unterschied: zurueck auf 3c56db887ec1 UND auf den
        urspruenglichen Inode 384632. `GET /` wieder main-OCUFZNF5.js.
      * Abbruch ohne index.html: rc 3 in Schritt 3, nichts hochgeladen, Box
        unberuehrt. Die boxseitige Wache dahinter (sie greift erst, wenn die
        lokale umgangen wurde) wurde EINZELN ausgeloest und meldet ebenfalls.
      * `www` als VERWEIS — vom Erbauer als ungemessen hinterlassen — direkt am
        Helfer ausgeloest: "ist ein VERWEIS ... Von Hand entscheiden."
      * DIE ZAEHLUNG STIMMT. Zwei Auslieferungen hintereinander (--erzwingen,
        server+player+admin) liessen Eintraege im Auslieferungsordner bei 20,
        belegten Platz bei 79 180 KB und die Zahl der Rueckdreh-Erzeugungen
        bei 4. Die Halde hoert wirklich auf.
      * DIE SICHERUNG WIRD ANGELEGT, nicht nur gerufen. Ins Archiv gesehen:
        12 115 B, 20 Eintraege, darunter data.json, config.json, kinderzeit.json.

      ── WORAN MAN SICH NICHT FESTHALTEN DARF ─────────────────────────────
      Die Baumhashes von ausliefern.py und die von ausliefern-gegenlesen.py
      sind VERSCHIEDENE Zahlen fuer denselben Baum (der eine rechnet ueber
      Pfad+md5-Zeilen, der andere ueber `find | sort | md5sum`). Dasselbe www
      heisst hier f160d21d45fd und dort a977eb493599. Wer sie gegeneinander
      haelt, sieht einen Unterschied, den es nicht gibt — vergleichbar ist
      jedes Werkzeug nur mit sich selbst ueber die Zeit.

      ── WAS WEITER OFFEN IST ─────────────────────────────────────────────
      * `--erzwingen` auf ein Ziel, das sich NICHT unterscheidet, loescht die
        vorhandene Rueckdreh-Erzeugung und ersetzt sie durch denselben Inhalt.
        Der Rueckweg auf einen aelteren Stand ist danach weg. Das folgt aus
        "genau EINE Erzeugung" und ist kein Fehler — aber es ueberrascht.
      * Ein Tausch, der WIRKLICH mittendrin abbricht (nicht der Abriss der
        Verbindung, sondern ein Fehler in renameat2 selbst), ist weiter nur im
        Code vorhanden.
      * Der ausgelieferte `www` im Baum gehoert einer Parallelsitzung
        (unfestgeschrieben). Er wurde zum Messen kurz auf die Box gelegt und
        vollstaendig zurueckgedreht; die Box laeuft mit den Bytes von vorher.
    related:
      - ausrollweg-ausliefern-py
      - rueckweg-gleicher-inode-meldet-erfolg
      - ausliefern-regeln
      - sicherung-als-schritt-einer-auslieferung
  - kind: knowhow
    id: e29b6-was-die-box-an-krypto-hat
    title: "E29/B6 vor dem Bau gemessen: die Box hat KEIN python3-cryptography, und `openssl enc` kann kein AEAD"
    tags: ['sicherung', 'zugangsdaten', 'verschluesselung', 'E29', 'messen', 2026]
    source: "05.08.2026 an .169 gemessen — tools/sicherung-geheim-vorpruefung.py, gegen die ausgelieferte Fassung (md5 91fdd3b2…)"
    body: |
      DIE ENTSCHEIDUNG DES BETREIBERS (05.08.2026) lautet: Zugangsdaten sollen
      MIT in die Sicherung, aber VERSCHLUESSELT, und die Verschluesselung ist
      OPTIONAL mit einem Passwort. Zusaetzlich soll `akkuverlauf.json` mit.
      Bevor davon eine Zeile gebaut wird, sind das die Zahlen.

      ── WAS AN KRYPTOGRAPHIE DA IST (und was NICHT) ──────────────────────
      * python3 3.13.5, OpenSSL 3.5.6 dahinter.
      * python3-cryptography FEHLT. Ebenso python3-nacl. Es gibt in python3
        also KEIN AES-GCM und KEIN ChaCha20-Poly1305 — kein AEAD.
        Nachruesten waere moeglich (Debian trixie main, 1115 kB Download,
        4533 kB belegt, zieht python3-bcrypt und python3-cffi-backend mit),
        aber es muesste auf JEDE Box ausgeliefert werden, nicht nur auf
        diese: [[unit-nur-in-autosetup-erreicht-keine-bestehende-box]].
      * `openssl enc` KANN KEIN AEAD — die Fassung sagt woertlich
        "enc: AEAD ciphers not supported". Wer damit verschluesselt, bekommt
        Vertraulichkeit OHNE Echtheitspruefung; ein falsches Passwort
        liefert dann Muell statt eines Fehlers, und der Muell wuerde
        eingespielt. Das ist genau der Fall, der ausgeschlossen gehoert.
      * hashlib.scrypt und hashlib.pbkdf2_hmac sind da (Standardbibliothek,
        hinter dem OpenSSL des Systems). Die SCHLUESSELABLEITUNG ist also
        nicht das Problem; scrypt mit n=16384 kostet 49 ms. Es fehlt der
        Cipher, nicht die Ableitung.
      * gpg 2.4.7 ist da und leistet, was verlangt ist: `--symmetric
        --cipher-algo AES256`, Passwort ueber `--passphrase-fd 0` (also
        NICHT ueber die Befehlszeile, wo `ps` es jedem zeigte), und ein
        FALSCHES Passwort faellt durch ("Bad session key"). Kosten warm
        390 ms je Aufruf, kalt (frisches GNUPGHOME, Agent startet) 1350 ms.
        649 B Klartext werden zu 734 B.
        ABER: gpg schreibt hier SEIPD v1 mit MDC, nicht OCB
        (`gpg --list-packets` sagt `aead 0`). Das ist eine
        Aenderungserkennung, kein formales AEAD. Und gpg braucht ein
        GNUPGHOME und startet einen Agenten — im Bootpfad von
        mupibox-wiederherstellung.service (als root, vor dem Server) ist
        das ein neues bewegliches Teil.

      ── WIE VIEL ES UEBERHAUPT ZU SCHUETZEN GIBT ─────────────────────────
      Von den elf Zeigern in `GEHEIM` sind auf .169 FUENF belegt, zusammen
      649 Zeichen: accessToken (303), refreshToken (131) und
      interfacelogin/password (60) in mupiboxconfig.json, refreshToken (131)
      in server/config/config.json, wlan.json /*/pw (24). Sechs sind leer,
      zwei davon (jellyfin/apikey, jellyfin/apiKey) treffen gar nicht — nur
      neun stehen deshalb in LIESMICH.txt. Die zu schuetzende Menge ist ein
      halbes Kilobyte, nicht ein Archiv.

      ── DER AKKUVERLAUF, DURCH PACKEN GEMESSEN (nicht geschaetzt) ────────
        Stand OHNE                       11 748 B
        Stand MIT (heute, 4423 Punkte)   42 943 B   Faktor 3,7
        Stand MIT am DECKEL              84 427 B   Faktor 7,2
      Der Deckel ist keine Vermutung: akkuverlauf.ts kappt bei
      MAX_PUNKTE = 10080, also sieben Tage im Minutentakt (roh 511 KB,
      gepackt 82 KB — gzip drueckt die Messreihe auf ein Sechstel).
      DIE FAT-PARTITION IST NICHT DER ENGPASS: 127 MB gross, 93 MB frei;
      die sieben Staende, die `--auf-karte` dort haelt, kaemen selbst am
      Deckel auf 577 KB = 0,6 % des freien Platzes.

      DER ENGPASS IST EIN ANDERER, UND ER STEHT NICHT IM PLATZ:
      `--wenn-anders` vergleicht `inhalt_kennung`, und die deckt ALLE
      Dateien ab. akkuverlauf.json aendert sich alle zehn Minuten von selbst
      (server.ts, AKKU_SCHREIB_JE=10) — die Kennung ist damit IMMER anders,
      und `--wenn-anders` unterdrueckt NIE mehr einen Lauf. Damit ist die
      Vorkehrung aus [[aufraeum-sicherung-vor-der-entscheidung]] ausser
      Kraft: folgenlose Staende entstehen wieder und verdraengen im
      Kontingent von 10 die eine, auf die es ankommt. Wer akkuverlauf.json
      aufnimmt, muss sie deshalb aus der Kennung HERAUSRECHNEN — sonst
      kauft man 30 KB Messreihe mit dem Verlust des Verdraengungsschutzes.

      ── DIE STELLE, DIE DEN AUSSCHLAG GIBT ───────────────────────────────
      `--auf-karte` legt die Staende auf eine FAT-Partition. FAT KENNT KEINE
      RECHTE: im Haus liegen sie als 0600, auf der Karte als rwxr-xr-x, und
      jeder, der die Karte in einen beliebigen Rechner steckt, liest sie.
      Solange die Zugangsdaten ausgelassen werden, ist das folgenlos. Sobald
      sie mitgehen, faellt die Entscheidung "optional" genau hier: eine
      Sicherung mit Klartext-Zugangsdaten auf der Karte traegt den
      Hausschluessel spazieren. Der vorsichtige Weg ist deshalb, dass
      Zugangsdaten NUR verschluesselt mitgehen — und ohne Passwort gar nicht,
      so wie heute.
    related:
      - sicherung-ausgelassene-schluessel-nie-schreiben
      - sicherung-rueckweg-ueber-die-karte
      - sicherung-probe-woechentlich
      - aufraeum-sicherung-vor-der-entscheidung
      - unit-nur-in-autosetup-erreicht-keine-bestehende-box

  - kind: knowhow
    id: ablage-besitz-gemessen-e18-stufe2
    title: "Wem gehoert was: zwei von neunzehn Ablagen tragen eine Kennung — der Rest ist E18 Stufe 2"
    tags: ['mehrbenutzer', 'profile', 'e18', 'ablage', 'resume', 'symlink', 'werkzeug', 2026]
    source: "am 05.08.2026 am Quelltext UND an der Box .169 erhoben; Werkzeug tools/ablage-besitz-schau.py"
    body: |
      DIE MESSUNG, NICHT DIE ERINNERUNG. `tools/ablage-besitz-schau.py` liest
      `server.ts` und die Box und stellt beides nebeneinander — weil die zwei
      Antworten AUSEINANDERLAUFEN und man nur aus dem Unterschied planen kann.

      ── WAS DER QUELLTEXT KENNT (server.ts, 19 Ablagen) ──────────────────
      JE PROFIL sind genau ZWEI, und beide gehen durch `ablageName()`:
          gespielt.json              Zeile 6516, ablageName@6517/6527/6553
          kinderzeit-verbrauch.json  Zeile  696, ablageName@790/808/892
      BOX-WEIT sind die uebrigen 17, darunter die vier, die E18 Stufe 2
      angehen: `resume.json` (495), `kinderzeit.json` (695), `listen.json`
      (3355) und `profile.json` (664) selbst.

      ── WAS WIRKLICH AUF DER BOX LIEGT (05.08.2026, .169) ────────────────
      In /home/dietpi/.mupibox/Sonos-Kids-Controller-master/server/config —
      der Weg kommt aus der systemd-Unit (WorkingDirectory) plus
      `configBasePath`, NICHT aus /home/dietpi/MuPiBox (dort liegt nur
      `themes`; wer dorthin sieht, findet nichts und haelt die Box fuer leer).

      Eine Kennung tragen genau zwei Dateien:
          gespielt.gast.json               10 261 B, 31 Eintraege
          kinderzeit-verbrauch.gast.json        59 B
      `profile.json` GIBT ES NICHT. `/api/profile` antwortet trotzdem
      vollstaendig (nur `gast`, `aktiv: gast`) — die Datei entsteht erst beim
      ersten Schreiben. Wer nur die Box ansieht, haelt die Profile fuer nicht
      gebaut; wer nur den Quelltext liest, plant einen Umzug fuer eine Datei,
      die es noch nicht gibt. Ebenso fehlen `albumstop.json` und
      `interpreten.json`.

      ── RICHTIGSTELLUNG 08.08.2026 (die Box hat seither geschrieben) ────

      Zwei Zustandsaussagen von oben gelten nicht mehr. Nachgemessen an .169
      am 08.08.2026, nur lesend:

          profile.json          253 B, geschrieben 07.08. 21:41
                                zwei Profile: `gast` und `kalea`,
                                `aktiv: kalea`
          profile/gast/  profile/kalea/  profile/probe/   liegen da
          kinderzeit.json       825 B, geschrieben 07.08. 18:40,
                                traegt jetzt `standard` — die NEUE Form

      Damit ist auch die Begruendung von oben eingeloest: die Datei „entsteht
      erst beim ersten Schreiben" — sie ist geschrieben worden. Wer den alten
      Satz liest und die Sicherung danach baut, laesst genau die Datei
      draussen, die als einzige sagt, DASS es ein Kind gibt
      ([[profile-json-fiel-aus-der-sicherung]]).

      WAS WEITER STIMMT (am 08.08.2026 nachgesehen, nicht abgeschrieben):
      in `app.routes.ts` heisst KEIN Weg `profile` oder `profil` (die Liste
      ist inzwischen laenger, die Luecke dieselbe) — es gibt weiter keine
      Profilseite. Und `profile/*/auswahl.json` liegt auf
      dieser Box noch NICHT, obwohl die Sicherung das Muster schon fuehrt.

      ── DIE REGELN SIND SCHON JE PROFIL — DIE DATEI UND DIE VERWALTUNG NICHT
      Ein Befund, der die Aufgabe KLEINER macht, als sie im BACKLOG steht:
      `regelSatzNormalisieren` (kinderzeit.ts:168) kennt `{standard, je}`
      LAENGST, und `GET/PUT /api/kinderzeit?profil=<kennung>` sind gebaut und
      am Geraet nachgemessen (`?profil=gast` liefert Regeln, `?profil=liam`
      liefert 400 `unbekanntesProfil`). Box-weit ist nur ZWEIERLEI:
        * die DATEI: kinderzeit.json auf .169 traegt noch die ALTE flache Form
          (`aktiv`, `tage`, `nachsichtMin`, kein `standard`). Sie wird beim
          Lesen auf `standard` gebogen — es fehlt nichts, sie ist nur noch nie
          in der neuen Form geschrieben worden.
          [RICHTIGSTELLUNG 08.08.2026: seit dem Schreiben am 07.08. 18:40
          traegt sie `standard`. Der Satz beschreibt jetzt den WEG dorthin,
          nicht mehr den Stand — und das ist der Grund, ihn stehen zu lassen:
          eine Box, die seither nicht geschrieben hat, ist noch flach.]
        * die VERWALTUNG: `seiten/kinderzeit.ts` ruft `/api/kinderzeit` OHNE
          `?profil=` (332 lesend, 354 schreibend) und schreibt damit immer die
          Hausregel.
      Und es gibt UEBERHAUPT KEINE Profilseite: `app.routes.ts` fuehrt 15
      Wege, keiner heisst `profile`. `PUT /api/profile` steht mit Test da und
      wird von NICHTS aufgerufen. Ein zweites Profil laesst sich heute nur mit
      curl anlegen — genau deshalb steht auf der Box nur `gast`.

      ── resume.json: wer es anfasst (mit Zeile) ──────────────────────────
      IM SERVER, vier Routen und eine gemeinsame Lesefunktion:
          GET  /api/resume         6935  liest roh
          GET  /api/weiterhoeren   7059  ueber resumeStellenLesen() 6968
          POST /api/weiterhoeren   7213  Sperre 7303/7305/7321, schreibt 7312-7314
          POST /api/addresume      7988  Sperre 7989/7995/8043, liest 8004, schreibt 8031-8033
          POST /api/editresume     8155  Sperre 8157/8161/8201, liest 8162, schreibt 8185
          GET  /api/activeresume   7800  liest den VERWEIS (7801)
      dazu `laneWeiterZeile()` 6998 ueber dieselbe Lesefunktion.
      DIE ZWEI SPERRDATEIEN liegen in /tmp (`lockBasePath`, 511-513), NICHT
      in server/config: `/tmp/.resume.lock` und `/tmp/.data.lock`. Sie sind
      box-weit und in VIER Shell-Skripten fest verdrahtet (check_network.sh,
      get_network.sh, clearresume.sh, remove_max_resume.sh). Eine Sperre je
      Profil hiesse, sie an fuenf Stellen zu aendern — dieselbe Fehlersorte
      wie [[dienstliste-an-fuenf-orten]].
      AUSSERHALB DES SERVERS schreiben ausserdem `remove_max_resume.sh`
      (schneidet auf `mupibox.resume`; gemessen: 9 Eintraege in resume.json)
      und `clearresume.sh` direkt in die Datei.
      IN DER OBERFLAECHE: media.service.ts 299 (editresume), 311 (addresume),
      337 (weiterhoeren), 462 (activeresume).

      ── DIE SYMLINKS SIND DIE EIGENTLICHE FALLE ─────────────────────────
      `active_data.json` und `active_resume.json` sind VERWEISE, keine
      Dateien (am Geraet nachgesehen: -> data.json bzw. -> resume.json). Sie
      werden von `check_network.sh` umgehaengt, das auf der Box wirklich
      laeuft (PID 521, Benutzer dietpi, aus mupi_check_internet.service) und
      die Pfade ABSOLUT fest verdrahtet hat. Fuer einen Ordner je Profil
      heisst das vier Dinge, die einzeln STILL danebengehen:

        1. EIN VERWEIS ZEIGT AUF EINEN PFAD, NICHT AUF EINEN INHALT. Zieht
           resume.json nach `profile/<kennung>/`, zeigt active_resume.json
           ins Leere. `GET /api/activeresume` prueft mit `fs.existsSync`
           (7801) — und das ist auf einem TOTEN Verweis FALSCH. Die Antwort
           ist dann `[]` mit Status 200: keine Fehlermeldung, kein Protokoll,
           nur eine Box, die nichts mehr fortsetzt.
        2. DAS SKRIPT LEGT DEN VERWEIS WIEDER AN — auf den ALTEN Pfad, alle
           zehn Sekunden. Was der Server umzieht, macht ein Skript rueckgaengig,
           von dem der Server nichts weiss.
        3. `ablageUebernehmen()` (721) benutzt `fs.renameSync`. Auf einen
           Verweis angewandt benennt das den VERWEIS um, nicht sein Ziel — der
           Verweis landete im Profilordner und zeigte weiter nach draussen.
           Die Uebernahme muss Verweise UEBERSPRINGEN, nicht mitziehen.
        4. `offline_resume.json` wird vom Skript per jq AUS resume.json
           erzeugt. Je Profil gaebe es entweder n Offline-Dateien oder eine,
           die einem Profil gehoert — eine Frage an den Betreiber, keine
           Technikfrage.

      ── UND DIE SICHERUNG ZAEHLT MIT ────────────────────────────────────
      `scripts/mupibox/mupibox-sicherung.py` fuehrt je Verzeichnis FLACHE
      Muster (`gespielt.*.json`, `kinderzeit-verbrauch.*.json`). Ein
      Unterordner je Profil trifft keines davon: die Sicherung liefe weiter
      gruen und nickte Staende OHNE Verlauf und OHNE Zeitkonto ab. Wer den
      Ordner baut, muss die Muster mitziehen (fremdes Gebiet — ansagen).
    related:
      - mehrbenutzer-vorbereitung
      - dienstliste-an-fuenf-orten
      - speicherschluessel-sechsfach-verstreut
      - mixpi-die-geschichte
  - kind: knowhow
    id: f1-nachgemessen-und-der-mplayer-rueckweg
    title: "F1 am 05.08.2026 nachgemessen: gruen an beiden Quellen — und was am mplayer-Rueckweg wirklich passiert"
    match: 'F1 nachgemessen|force-media-title nachgemessen|media_title mplayer|PROPERTY_UNAVAILABLE|playlist-finish|currentTrackname gemessen'
    tags: ['player', 'mpv', 'mplayer', 'jellyfin', 'ard', 'F1', 'messen', 2026]
    source: "05.08.2026 an .169 nachgemessen — tools/f1-titelname-geht-mit.mjs (--jellyfin und --ard), tools/f1-vorlesen-ueberschreibt-schau.mjs; Quelltext src/backend-player/src/"
    body: |
      [[f1-titelname-geht-mit-gebaut]] ist eine ZUSTANDSAUSSAGE und hat damit
      Haltbarkeit. Hier steht die Nachmessung — und drei Dinge, die dort
      nicht stehen.

      ── DER ZUSTAND, NACHGESEHEN STATT GEGLAUBT ─────────────────────────
      Die ausgelieferte Datei ist Zeichen fuer Zeichen der Bau aus dem Baum:
        md5 fda2d6878825f8591f8e0a6eec29c864
        = src/deploy/spotify-control.js
        = /home/dietpi/.mupibox/spotifycontroller-main/spotify-control.js
      mpv 0.40.0, playerEngine "mpv" (in /etc/mupibox/mupiboxconfig.json
      nachgesehen). 145 von 145 Tests in src/backend-player gruen.

        jellyfin  nr 1 "Mittelmaessiges Leben"  nr 2 "Nicht alleine"   GRUEN
        ard       nr 1 "Mit dem Rad"            nr 2 "Faelschung"      GRUEN
        (je 34 Messungen, kein Fehlgriff, Uebergang VON SELBST)
        Ansage waehrend ARD: "Mit dem Rad" vorher wie nachher.

      ── DIE FRAGE „KANN DER AUFSATZ DATEI-OPTIONEN?" IST BEANTWORTET,
         ABER NICHT DURCH EIN „success" ────────────────────────────────
      Der Beweis steht im ARD-Ergebnis: dort meldet der Dienst „Faelschung"
      und NICHT „Faelschung | Die Maus zum Hoeren (…)". Genau diese
      Doppelung stuende da, wenn der Rueckfall gelaufen waere — dann traegt
      der Eintrag keinen erzwungenen Namen, und `media-title` faellt auf die
      Merkmale des Ausspielpartners zurueck. Der ARD-Lauf ist damit die
      SCHAERFERE Probe als der Jellyfin-Lauf; Jellyfin allein saehe auch dann
      richtig aus, wenn nichts erzwungen wuerde.
      Dazu: im journal der letzten drei Stunden kein einziges
      „force-media-title abgelehnt".

      ── WAS DAMIT NICHT GEMESSEN IST ────────────────────────────────────
      DER RUECKFALL SELBST. An dieser Box hat er nie gefeuert, weil mpv 0.40
      die Langform annimmt. Geprueft ist er ausschliesslich in
      mpv-wrapper.spec.ts ("wiederholt einen abgelehnten loadfile OHNE den
      Namen", "fasst NICHT nach, wenn der loadfile durchgeht"). Auf einer Box
      mit aelterem mpv ist er UNGEMESSEN — und dort haengt an ihm nicht die
      Anzeige, sondern der Ton.

      ── DER mplayer-RUECKWEG: eine Falle, die heute folgenlos ist ────────
      F1 hat `player.on('track-change', () => player.getProps(['media_title']))`
      dazugelegt (spotify-control.ts:296). Bei mplayer wird daraus
      `pausing_keep_force get_property media_title` — eine Eigenschaft, die
      mplayer nicht kennt. Es antwortet `ANS_ERROR=PROPERTY_UNAVAILABLE`, und
      mplayer-wrapper.ts:111 macht daraus `out.emit('playlist-finish')`.

      DAS IST HEUTE FOLGENLOS, und zwar aus einem einzigen Grund: auf
      'playlist-finish' hoert NIEMAND. Repo-weit gibt es das Wort nur in den
      beiden Aufsaetzen und in ihren Tests, in spotify-control.ts gar nicht.
      Und es ist auch nicht neu — Zeile 212 fragt seit dem mpv-Umbau schon
      `playlist_pos` und `playlist_count` ab, die mplayer ebenso wenig kennt.

      WER ALSO EINEN 'playlist-finish'-HOERER EINBAUT (das Naheliegendste der
      Welt: „Liste zu Ende, naechstes Werk"), baut sich auf einer
      mplayer-Box einen Fehlalarm bei JEDEM Titelwechsel ein — dreifach.
      Die Reparatur gehoert dann in mplayer-wrapper.ts, nicht in den Hoerer.

      Der NAME selbst geht bei mplayer gefahrlos verloren: `play`/`queue`
      dort nehmen nur ein Argument, das zweite faellt ins Leere, und
      `parsers.ts` kennt kein `media_title` — der Hoerer feuert also nie.
      Damit bleibt es bei mplayer beim Verhalten von vor F1: der Name kommt
      allein aus dem Befehl. Das ist richtig so und nicht zu heilen, solange
      mplayer keine Datei-Optionen hat.

      ── DIE WERKZEUGE LASSEN DOCH ETWAS ZURUECK ─────────────────────────
      Beide F1-Werkzeuge sagen im Kopf „Es schreibt KEINE Nutzerdatei". Das
      stimmt fuer die WERKZEUGE und nicht fuer den LAUF: der Server fuehrt
      beim Spielen von selbst Buch. Nach den Messungen tragen
      `resume.json`, `offline_resume.json` und `gespielt.gast.json` die
      Testwerke („HAMM", „Die Maus zum Hoeren", „Kapelle Petra"), und die
      Dateien sind genau in den Zeitfenstern der Laeufe geschrieben worden.
      Wer misst, veraendert hier Weiterhoeren-Marken. Vorher sichern.

      ── NEBENBEFUND, nicht F1 ───────────────────────────────────────────
      Nach `stop` meldet /player/local `"playing":true`, waehrend alle
      anderen Felder leer sind. `currentMeta.playing` haengt an der
      pause-Eigenschaft (spotify-control.ts:207, `playing = !val`), und mpv
      im Leerlauf meldet `pause=false`. Nicht angefasst.
    related:
      - f1-titelname-geht-mit-gebaut
      - f1-ansage-ueberschreibt-den-folgennamen
      - mpv-nimmt-den-titelnamen-an-force-media-title
      - mpv-trackname-geht-nie-mit
  - kind: knowhow
    id: e29b7-gpg-kann-doch-aead-und-gnupg-ist-nur-angeweht
    title: "Nachtrag zu E29/B6: gpg KANN AEAD (`--force-ocb`) — aber gnupg steht in KEINER Paketliste"
    match: "force-ocb|aead 2|OCB|gnupg autosetup|krypto-vorrat-schau|maxmem|memory limit exceeded|sqv"
    tags: ['sicherung', 'zugangsdaten', 'verschluesselung', 'E29', 'messen', 'autosetup', '2026']
    source: "05.08.2026 an .169 gemessen — tools/krypto-vorrat-schau.py; korrigiert und ergaenzt [[e29b6-was-die-box-an-krypto-hat]]"
    body: |
      DREI DINGE, DIE DIE VORHERIGE MESSUNG NICHT HATTE. Der Bestand dort
      stimmt (kein python3-cryptography, `openssl enc` kein AEAD, hashlib.scrypt
      da) — diese drei aendern aber, welches Verfahren man waehlt.

      ── 1. gpg LEISTET AEAD, es ist EIN Schalter ─────────────────────────
      Die Aussage „gpg schreibt MDC, kein formales AEAD" gilt nur fuer die
      VORGABE. Beides an derselben Box, an denselben 649 Byte:

        --symmetric                  :symkey enc packet: version 4, aead 0
                                     :encrypted data packet:          86 B
        --symmetric --force-ocb      :symkey enc packet: version 5, aead 2
                                     :aead encrypted packet: cipher=9
                                        aead=2 cb=16                160 B

      `aead 2` ist OCB — echtes AEAD, kein Anhaengsel. Beide Fassungen
      scheitern LAUT: falsches Passwort und EIN verdrehtes Byte geben je
      rc=2 und NULL Byte auf der Ausgabe (nachgemessen, beide Wege). Der
      Unterschied liegt nicht in der Lautstaerke, sondern darin, ob eine
      Pruefung im Verfahren steht oder daneben.
      KOSTEN: 390 ms warm, 1445 ms kalt (Agent muss erst starten).
      PREIS DES SCHALTERS: SEIPD v2/OCB liest erst GnuPG >= 2.4. Die Box hat
      2.4.7, heutiges Gpg4win auch — ein altes 2.2 auf einem fremden Rechner
      koennte den Stand NICHT oeffnen. Wer die Karte notfalls an einem
      beliebigen Windows-Rechner aufmachen koennen will, kauft das mit dem
      schwaecheren MDC. Das ist eine Wahl, keine Selbstverstaendlichkeit.

      ── 2. gnupg IST NUR ANGEWEHT — und das ist der eigentliche Fund ─────
      Die Frage war nicht „ist gpg da", sondern „ist es nach einer
      NEUINSTALLATION da". Gemessen an der ctime der dpkg-Merkzettel, mit
      `bash` als Zeugen fuer den Image-Stand:

        bash, openssl, sqv        15.06.2026   AUS DEM IMAGE
        gnupg, gpg                26.07.2026   +41,5 Tage
        build-essential, mplayer  26.07.2026   +41,5 Tage   (autosetup)
        nodejs                    26.07.2026   +41,8 Tage   (autosetup)

      gnupg kam also mit dem autosetup-Lauf — steht aber in KEINER
      Paketliste. Es ist ein Recommends von `dpkg-dev`/`build-essential`,
      und zwar der LETZTE Eintrag einer Alternativenkette:
        Recommends: sq | sqop | rsop | gosop | pgpainless-cli | gpg-sq | gnupg
      Waehlt apt irgendwann einen frueheren Zweig, ist gnupg WEG — und
      niemand merkt es, bis jemand einen verschluesselten Stand oeffnen will.
      DAZU PASST: Debians `apt` haengt in Trixie an `sqv`, NICHT mehr an
      `gpgv`; `gpgv` ist auf .169 gar nicht installiert. Die alte
      Gewissheit „gpg ist auf jedem Debian" GILT NICHT MEHR.
      FOLGE: welches Verfahren auch gewaehlt wird — sein Paket gehoert
      NAMENTLICH in `packages2install` (autosetup.sh Zeile 73). Sonst haengt
      der Rueckweg an einem Zufall. Und fuer laufende Boxen gilt
      [[unit-nur-in-autosetup-erreicht-keine-bestehende-box]]: bei gnupg ist
      das folgenlos (schon ueberall da), bei python3-cryptography nicht.

      ── 3. hashlib.scrypt STUERZT ueber n=2^14, wenn man maxmem vergisst ─
        n=2^14  ohne maxmem   53 ms      mit maxmem   51 ms
        n=2^15  ohne maxmem   FEHLER     mit maxmem   98 ms
        n=2^16  ohne maxmem   FEHLER     mit maxmem  195 ms
        n=2^17  ohne maxmem   FEHLER     mit maxmem  395 ms
      Der Fehler heisst `ValueError: [digital envelope routines] memory limit
      exceeded` und kommt aus OpenSSLs Vorgabe von 32 MB; scrypt braucht
      128*N*r Byte. Die vorige Messung nannte „n=16384 kostet 49 ms" — das
      ist genau der groesste Wert, der OHNE maxmem noch durchgeht. Wer
      naeher an heutige Empfehlungen will (n=2^15 aufwaerts), MUSS
      `maxmem=128*n*r + Reserve` mitgeben, sonst faellt es erst auf der
      Box um, nicht auf dem Arbeitsrechner.

      ── WAS SONST NOCH DA IST ────────────────────────────────────────────
      node v26.5.0 kann `aes-256-gcm` und `scryptSync` (Roundtrip gemessen,
      falscher Tag faellt auf) und steht NAMENTLICH in autosetup.sh — ohne
      node gibt es keinen MuPiBox-Server, es ist also die einzige Zusage,
      die nicht extra erkauft werden muss. Preis: eine Sprachgrenze mitten
      im Werkzeug, das ausgerechnet dann tragen muss, wenn sonst nichts mehr
      geht. `age` ist NICHT da (waere 1.2.1 in Trixie), `python3-nacl` auch
      nicht.

      ── DIE FRAGE, DIE KEIN WERKZEUG BEANTWORTET ─────────────────────────
      WER TIPPT DAS PASSWORT, UND WANN? `--anlegen` laeuft selbsttaetig (Timer,
      und als Schritt jeder Auslieferung) — dort ist NIEMAND da, der etwas
      eingeben koennte. `--von-karte` laeuft beim BOOT als root vor dem
      Server — dort erst recht nicht. Ein Passwort, das trotzdem beide
      erreichen soll, muesste auf der Box liegen, und dann ist es kein Schutz
      mehr, sondern Theater.
      DARAUS FOLGT ZWANGSLAEUFIG: Zugangsdaten koennen nur in einen
      Stand, der VON HAND und mit Eingabe angestossen wurde; die
      selbsttaetigen Staende bleiben ohne — so wie heute. Und der Rueckweg
      von der Karte spielt alles ANDERE ein und meldet namentlich, welche
      Felder verschluesselt danebenliegen und mit welchem Befehl man sie
      nachtraegt. Das ist keine Einschraenkung des Verfahrens, sondern die
      Form, die die Entscheidung „optional mit pw" ueberhaupt haben kann.
    related:
      - e29b6-was-die-box-an-krypto-hat
      - sicherung-ausgelassene-schluessel-nie-schreiben
      - sicherung-rueckweg-ueber-die-karte
      - unit-nur-in-autosetup-erreicht-keine-bestehende-box
      - kein-traceback-in-eine-datei-die-ohne-ssh-gelesen-wird
  - kind: architektur
    id: bibliothek-verlaesst-den-server-an-25-stellen
    title: "E18 Stufe 2: die EINE Stelle zum Filtern gibt es nicht von selbst — die Bibliothek verlaesst server.ts an 25 Stellen, in drei Sorten"
    match: 'wer sieht was|Zuweisung Medien Profil|api/werke filtern|aktiveMedienLesen|sichtbare Medien|E18 Stufe 2 Medien'
    tags: ['e18', 'mehrbenutzer', 'profile', 'medien', 'bibliothek', 'server', 2026]
    source: "05.08.2026 am Quelltext ausgezaehlt (tools/bibliothek-ausgaenge.mjs) und an .169 nachgemessen (curl auf 127.0.0.1:8200)"
    body: |
      BACKLOG E18 verlangt: „Gefiltert wird im SERVER, nicht in der
      Oberflaeche … sonst gibt es zwei Antworten auf dieselbe Frage, und die
      zweite ist ueber einen anderen Weg erreichbar." Das ist richtig — nur
      sagt es nicht, WELCHE Stelle das ist. Ausgezaehlt: die Bibliothek
      verlaesst `src/backend-api/src/server.ts` an 27 Fundstellen in 25
      Umgebungen, und die tun DREI verschiedene Dinge, die von aussen gleich
      aussehen.

      ── DIE DREI SORTEN ─────────────────────────────────────────────────
      BOXSICHT (6) — was ein Kind sieht. NUR diese brauchen den Filter:
        GET /api/data                     die KLASSISCHE Oberflaeche baut
                                          ihre ganze Bibliothek hieraus
                                          (frontend-box media.service.ts
                                          `fetchMedia` -> `updateMedia`, dazu
                                          `fetchRawMedia` fuer Bearbeiten und
                                          Favoritenfenster). Sie liest die
                                          Datei DIREKT (`jsonfile.readFile`),
                                          nicht ueber `aktiveMedienLesen()`.
                                          Wer sie vergisst, hat den „anderen
                                          Weg" gebaut, vor dem der BACKLOG
                                          warnt.
        GET /api/werke                    die Kacheln der neuen Oberflaeche
        GET /api/interpreten              die runde Reihe
        GET /api/interpret/:id            „In deiner Box"
        GET /api/weiterhoeren             loest gegen die Bibliothek auf
        fn eintragZurBevorzugtenQuelle()  loest die ANGETIPPTE Kachel auf

      TECHNISCH (11) — dieselbe Liste, andere Frage. HIER DARF NICHT
      GEFILTERT WERDEN: sechs davon fischen nur den JELLYFIN-ZUGANG aus der
      Liste (`jellyfinZugangAusListe`, er steckt in der Cover-Adresse eines
      beliebigen Jellyfin-Eintrags — siehe
      [[jellyfin-schluessel-kommt-vom-ERSTEN-eintrag-der-liste]]). Wer den
      Filter in `aktiveMedienLesen()` legt, nimmt der Box den
      Jellyfin-Schluessel in dem Moment, in dem einem Profil kein
      Jellyfin-Eintrag gehoert. Kein Fehler, keine Meldung — Jellyfin ist
      einfach still weg. Dazu: `eintragZuSchluessel()` (Cover),
      `werkSchluesselKarte()` (Bruecke), `werkeStand()` (Fingerabdruck).

      VERWALTUNG (8) — die Elternseite. Sie MUSS alles sehen: man kann nicht
      zuweisen, was man nicht sieht. Sie liest ohnehin `data.json`
      (`medienLesen`), die Box `active_data.json` (`aktiveMedienLesen`) —
      diese Trennung besteht schon und traegt.

      ── WAS DARAUS FOLGT ────────────────────────────────────────────────
      Der Filter kann WEDER in `aktiveMedienLesen()` sitzen (die elf
      technischen Leser haengen daran) NOCH je Route nachgetragen werden —
      das ist genau die Fehlersorte aus [[dienstliste-an-fuenf-orten]], an
      der `kinderzeit.json` und `listen.json` box-weit geblieben sind.
      Er gehoert in einen EIGENEN Leser daneben, und `/api/data` muss dafuer
      zuerst auf `aktiveMedienLesen()` umgestellt werden.

      `tools/bibliothek-ausgaenge.mjs` haelt die Einteilung fest (jede
      Einordnung mit GRUND) und wird rot, sobald eine Stelle dazukommt oder
      wegfaellt. Haengt in `tools/pruefen.sh`.

      ── DER ZWEITE, INDIREKTE AUSGANG ───────────────────────────────────
      Nach Umgebungen zu zaehlen reicht NICHT. `eintragZuSchluessel()` steht
      einmal in der Liste und bedient FUENF Aufrufer mit verschiedenen
      Zwecken: das Cover (harmlos) — aber auch den INHALT hinter einer
      Kachel, und das IST die Zuweisung. Er ist deshalb `gemischt` und muss
      GETEILT werden, sonst gibt es die eine Stelle nicht.

      Und die Falle darin, die man sonst erst an der Box merkt:
      `/api/werke/:schluessel/inhalt` laeuft OHNE `verschmelzen=1` — und das
      ist die VORGABE — gar nicht ueber `eintragZurBevorzugtenQuelle()`,
      sondern ueber `eintragZuSchluessel()` (server.ts:5267-5269). Wer den
      Filter nur in den bevorzugten Weg legt, filtert im Normalbetrieb gar
      nichts. `/api/werke/:schluessel/alben` haengt ebenfalls dort.

      ── ZWEI FALLEN, DIE DABEI AUFFIELEN ────────────────────────────────
      1. `werkeStand()` baut den Fingerabdruck aus ANZAHL und Aenderungszeit
         der Datei. Sehen zwei Profile gleich viele Werke, ist der Abdruck
         identisch — die neue Oberflaeche merkt beim Profilwechsel im
         15-s-Takt NICHTS und zeigt die Kacheln des anderen Kindes weiter.
         Die Kennung muss in den Abdruck.
      2. Ein Besitzfeld AM EINTRAG in `data.json` waere nicht haltbar:
         `POST /api/edit` ersetzt den ganzen Eintrag durch das, was das
         Formular schickt (`data.splice(i, 1, req.body.data)`), und die
         klassische Oberflaeche ruft das im BETRIEB — `player.page.ts:1017`
         schreibt beim Verlassen eines Spotify-Albums zurueck, wenn der
         Zufallsschalter umgelegt wurde. Ein Kind koennte die Zuweisung
         durch Antippen loeschen. Die Zuweisung gehoert deshalb in eine
         EIGENE Datei neben `data.json`, ueber `medienSchluessel` verknuepft
         — dieselbe Bauart und dieselben drei Gruende wie bei
         `verschmelzung.json` (server.ts, Abschnitt „Verschmelzung: die
         Ablage").

      ── ZUSTAND AM 05.08.2026 AN .169 (Haltbarkeit beachten) ────────────
        data.json 25 Eintraege, Felder: type/category/title/artist/cover
        (alle 25), playlistid 13, spotify_url 10, id 11, shuffle 1.
        KEIN Besitzfeld irgendwo.
        /api/data 25, /api/werke 25 — beide voll, beide box-seitig.
        profile.json existiert NICHT; /api/profile liefert nur `gast`.
        gespielt.gast.json und kinderzeit-verbrauch.gast.json liegen da,
        kinderzeit.json/listen.json/resume.json ohne Kennung.
        kinderzeit.json steht noch in der ALTEN, flachen Form —
        `regelSatzNormalisieren` biegt sie beim Lesen auf `{standard, je}`.
        KEIN doppelter `medienSchluessel` mehr in data.json — die Kollision
        aus [[medienschluessel-kollision-gleiche-playlistid]] ist auf DIESER
        Box aufgeraeumt. Die URSACHE gilt weiter, der Zustand nicht: eine
        Zuweisung ueber den Schluessel muss den Fall trotzdem aushalten.

      ── WAS HIER NICHT STEHT, WEIL ES SCHON WOANDERS STEHT ──────────────
      Die ABLAGE-Seite derselben Stufe (welche Datei traegt eine Kennung,
      was `resume.json` anfasst, warum `active_data.json` ein VERWEIS ist
      und was ein Ordner je Profil dabei still kaputt macht) steht in
      [[ablage-besitz-gemessen-e18-stufe2]]. Dort steht auch, dass es in der
      Verwaltung UEBERHAUPT KEINE Profilseite gibt — das ist der eigentliche
      Engpass fuer eine Zuweisungsseite: sie braeuchte eine Profilauswahl,
      und es gibt nichts auszuwaehlen.

      EIN BEFUND VON DORT AENDERT DIE RECHNUNG HIER NICHT, gehoert aber
      dazu: `active_data.json` ist ein VERWEIS auf `data.json`. Solange die
      Box online ist, lesen `aktiveMedienLesen()` und `medienLesen()` also
      DIESELBEN Bytes. Die Trennung BOXSICHT/VERWALTUNG ist damit eine
      Trennung der ABSICHT, nicht der Datei — sie traegt trotzdem, denn
      offline sind es wirklich zwei Listen.
    related:
      - dienstliste-an-fuenf-orten
      - mehrbenutzer-vorbereitung
      - ablage-besitz-gemessen-e18-stufe2
      - jellyfin-schluessel-kommt-vom-ERSTEN-eintrag-der-liste
      - medienschluessel-kollision-gleiche-playlistid
  - id: f2-marke-titelwechsel-je-dienst
    kind: offen
    title: "F2 gemessen: die Marke geht bei mpv (Jellyfin, ARD, lokal) NIE mit — es gibt dort gar keine Titelkennung"
    match: "F2 marke titelwechsel|marke geht nicht mit jellyfin|marke ard folge|spieltMarkieren mpv|laufendeKennungen spotify only|data-spielt fehlt folge"
    tags: ["oberflaeche", "neu", "marke", "lanes", "jellyfin", "ard", "mpv", "messen", "F2", 2026]
    source: "05.08.2026 im Browser gegen tools/neu-vorschau.mjs gemessen (tools/marke-titelwechsel-je-dienst.mjs, 5 Faelle, eigener Browser ueber tools/leihgabe.mjs); die entscheidende Feldfrage zusaetzlich AM GERAET .169 gegengeprueft. NICHT repariert."
    body: |
      DIE MELDUNG: „beim wechsel des titels geht die marke spielt gerade nicht
      mit". [[next-verlaesst-die-playlist-nicht]] hat den Abspieldienst
      freigesprochen, [[marke-verlaesst-die-offene-titelliste]] hat den
      Spotify-Weg im Browser gemessen und den Fall „naechster Titel liegt in
      einem anderen Album" gefunden. BEIDES SAH NUR SPOTIFY AN. Die Box spielt
      aber ueber ZWEI Maschinen, und die zweite (mpv: Jellyfin, ARD, lokal) war
      in dieser Frage nie gemessen.

      ── GEMESSEN, FUENF FAELLE, 800x480 ─────────────────────────────────
      `node tools/marke-titelwechsel-je-dienst.mjs`

        Fall                              Titelkacheln  mit data-spielt  Marke beim Wechsel
        1 Spotify-Album IN einer Playlist      10             10          wandert mit
          — bis der naechste Titel ein
            anderes Album trifft                                          verschwindet*
        2 Spotify-Werk der Art `album`          0              0          keine Titelebene
        3 Jellyfin-Album                        0              0          keine Titelebene
        4 ARD-Sendung (Folgen-Lane)            14              0          KANN NICHT SITZEN
        5 Gegenprobe: Lane offen, mpv
          uebernimmt (Lage `nachlauf`)         10             10          FAELLT WEG

        * das ist der schon bekannte Fall aus [[marke-verlaesst-die-offene-titelliste]]

      Die RASTER-Marke (Werk-Ebene) stand in allen Faellen richtig und blieb
      beim Titelwechsel stehen — das ist ihre Aufgabe, sie sagt „aus diesem
      Werk laeuft etwas".

      DASS IN 3 UND 4 WIRKLICH ETWAS GESTARTET WURDE, ist nachgewiesen und
      nicht angenommen: Das Befehlsprotokoll der Attrappe (`/vorschau/befehle`)
      zaehlt je 14 Befehle — ein Start und dreizehn `jfqueue`/`ardqueue`. Das
      musste nachgetragen werden, denn die Raster-Marke allein beweist es
      NICHT: `albumSpielen` setzt `zuletztGestartet = w` schon, sobald die
      Titelliste da ist (app.js:4189), also VOR dem ersten Befehl.

      ── ES SIND ZWEI SPERREN HINTEREINANDER, NICHT EINE ─────────────────
      Wer nur die erste behebt, hat nichts gewonnen; wer nur die zweite
      behebt, ebenfalls nicht.

      SPERRE 1 — DIE KACHEL TRAEGT GAR KEINE KENNUNG.
      `titelListeBauen` (NewDesign/app.js:3316) baut sie aus `t.uri`:

          const kennung = stueckKennung(zush, t.uri)
          if (kennung) k.dataset.spielt = kennung        // :3317

      `t.uri` gibt es bei mpv-Quellen NICHT. AM GERAET .169 nachgesehen
      (05.08.2026, `/api/werke/<s>/inhalt`):

          ard:69106028   30 Folgen  Felder: anhaengen befehl bild dauerMs id
                                            interpret nr titel weiterAb   -> 0x uri
          jellyfin:7d9a… 12 Titel   Felder: anhaengen befehl dauerMs id
                                            interpret nr titel            -> 0x uri

      Im Server ist das kein Versehen, sondern die Quellenlage: der ard-Zweig
      von `/inhalt` (server.ts:5391 ff.) fuehrt `uri` gar nicht, und
      `titelEinesWerks` setzt es im jellyfin-Zweig (server.ts:3619 ff.) nicht —
      Jellyfin hat `Id`, keine Spotify-Adresse. `stueckKennung` gibt bei leerer
      `uri` ausdruecklich '' zurueck (app.js:2768-2772), damit eine halbe
      Kennung nie trifft. Ergebnis: 14 Folgenkacheln, 0 mit `data-spielt`, und
      `spieltMarkieren()` sieht sie nicht einmal an — es fragt
      `querySelectorAll('[data-spielt]')` (app.js:2851).

      SPERRE 2 — SELBST MIT KENNUNG GAEBE ES NICHTS ZU VERGLEICHEN.
      `laufendeKennungen()` baut die Stueck-Kennung NUR aus dem Spotify-Zustand
      (NewDesign/app.js:2817):

          const sp = np && np.art === 'spotify' ? zustand.dienst : null

      Sagt `jetztLaeuft()` „lokal" — und das sagt es bei JEDEM mpv-Ton —, ist
      `sp` null, `uri` leer, und die Liste des Laufenden enthaelt hoechstens
      noch die WERK-Kennung (:2834-2838). Es gibt dann keine einzige
      `stueck|…`-Kennung, gegen die eine Titelkachel treffen koennte.

      FALL 5 IST DER BEWEIS DAFUER, und deshalb steht er in dem Werkzeug: Dort
      ist der Traeger DA (Spotify-Lane offen, zehn Kacheln mit Kennung) und
      `item.uri` bleibt unveraendert stehen — sobald mpv den Ton uebernimmt,
      faellt die Marke trotzdem weg. Die Sperre sitzt also im VERGLEICH, nicht
      am Merkmal. (Die Zeile ist Absicht und richtig begruendet: ohne sie tanzt
      die Marke an einem Spotify-Kapitel weiter, waehrend ein Hoerbuch laeuft —
      [[spotify-nachlauf-muster]]. Sie ist der Grund, nicht der Fehler.)

      ── WO DIE MARKE ALSO GENAU STEHENBLEIBT ────────────────────────────
      Sie bleibt auf der RASTER-KACHEL des Werks stehen — richtig, aber
      unbeweglich. Auf der Titelebene erscheint sie bei mpv NIE, weder vor noch
      nach dem Wechsel. Wer eine ARD-Sendung aufklappt und Folge 3 antippt,
      sieht dreissig Kacheln ohne jede Marke, waehrend Folge 3 spielt. Das
      liest sich als „die Marke geht beim Titelwechsel nicht mit" — sie ist nie
      dagewesen.

      Bei Spotify wandert sie richtig mit, mit der EINEN bekannten Ausnahme an
      der Albumgrenze ([[marke-verlaesst-die-offene-titelliste]]).

      ── WAS ES BRAUCHTE (nicht gebaut) ──────────────────────────────────
      Der Weg liegt fertig daneben und ist beim COVER schon gegangen
      ([[cover-folgt-der-position-nicht-dem-start]]): `laufendeFolgen`
      (app.js:3917) haelt genau die Liste, gegen die mpv zaehlt, und
      `currentTracknr` sagt die Nummer. `folgenBildAn(liste, nr)` (app.js:3926)
      schlaegt daraus das Bild nach — dieselbe Nachschlagerei wuerde die
      Marke tragen. Eine mpv-Kennung („folge|<werkschluessel>|<nr>") an beiden
      Stellen, und beide Sperren fallen zugleich.

      DER WAECHTER MUSS MIT, sonst wird es die Falle aus
      [[f1-ansage-ueberschreibt-den-folgennamen]]: `laufendeFolgen.schluessel
      === w.schluessel` allein kann NIE nein sagen (beide Seiten stammen aus
      derselben Zuweisung); es braucht `passtZuLaufendem` daneben, so wie es
      app.js:6772 beim Cover schon tut.

      NICHT ZU RETTEN IST DAMIT: Fall 2 und Fall 3. Ein Spotify-Album und ein
      Jellyfin-Album klappen im Raster ueberhaupt nicht auf (app.js:3632-3638 —
      nur Playlist und ARD-Sendung tun das), es gibt dort also keine Kachel,
      die eine Titelmarke tragen koennte. Das ist eine Entscheidung ueber die
      NAVIGATION und gehoert dem Betreiber, nicht dieser Messung.

      ── GRENZEN DIESER MESSUNG, ehrlich ─────────────────────────────────
      Gemessen wurde gegen die ATTRAPPE. `/player/local` dort meldet immer
      denselben Namen; einen Jellyfin-Titel bei Namen kann sie nicht melden.
      Damit `passtZuLaufendem` ueberhaupt zustimmt, wurde fuer Jellyfin
      `mpv-fremd` gestellt (Album „Kiddinx" = Interpret des Werks) und fuer die
      ARD die Vorgabe („Die Maus"). Das faerbt nichts schoen — es stellt den
      Fall her, in dem die Marke ueberhaupt eine Chance haette. AM GERAET
      nachgemessen ist bisher nur die entscheidende Feldfrage (`uri` fehlt);
      der Browserteil ist an .169 NICHT nachgestellt worden.

      ── FALLE FUER DEN NAECHSTEN ────────────────────────────────────────
      `tools/raster-marke-schau.mjs` prueft, ob die Marke sich BEWEGT (die
      tanzenden Balken), NICHT ob sie beim Titelwechsel MITGEHT. Aehnlicher
      Name, andere Frage; gruen dort beweist hier nichts. Diese Aufgabe ist
      genau daran schon einmal zu frueh geschlossen worden.

      Und: `/vorschau/kontext-…` ist eine KLEBRIGE Lage. Ein Fall, der sie
      stellt, verstellt jeden Fall danach; `kontext-standard` gehoert an den
      Anfang jedes weiteren. Hier prompt passiert — Fall 5 meldete „vorher
      keine Marke", und das war der Nachhall von Fall 2, kein Befund.

      NOCH EINE, aus demselben Lauf: Das Befehlsprotokoll fuehrt unter `befehl`
      nur das LETZTE Glied des Weges, unter `pfad` den ganzen. Wer nach
      „jellyfin" im letzten Glied sucht, findet nie etwas und meldet „es ging
      kein Startbefehl hinaus" — waehrend vierzehn hinausgingen. Das sah eine
      Runde lang aus wie ein Befund ueber die Oberflaeche und war einer ueber
      die Messung.

      NACHMESSEN:
        node tools/marke-titelwechsel-je-dienst.mjs --pruefen
        node tools/lane-marke-wandert.mjs --pruefen     # nur der Spotify-Weg
    related:
      - marke-verlaesst-die-offene-titelliste
      - next-verlaesst-die-playlist-nicht
      - spielt-marke-eine-frage-zwei-formen
      - mpv-trackname-geht-nie-mit
      - cover-folgt-der-position-nicht-dem-start
      - spotify-nachlauf-muster
      - attrappe-luegt-durch-weglassen

  - kind: knowhow
    id: marke-zeichen-steht-nie-im-bild
    title: "F2: Die Marke wandert richtig — man SIEHT sie nur nie. Nichts rollt die markierte Kachel herein"
    match: "F2 marke sichtbar|spielt-marke ausserhalb|lane-reihe rollt nicht|marke nicht zu sehen|marke-sichtbar-nach-albumwechsel"
    tags: ["oberflaeche", "neu", "lanes", "marke", "F2", "messen", "werkzeug", 2026]
    source: "05.08.2026, tools/marke-sichtbar-nach-albumwechsel.mjs gegen eigene neu-vorschau, 800x480, mit Bildern"
    body: |
      [[marke-verlaesst-die-offene-titelliste]] sagt: die Marke wandert mit,
      und ueber die Albumgrenze sitzt sie eine Ebene hoeher auf der
      Albumkachel — „sie ist nicht falsch". DAS IST EINE AUSSAGE UEBER DEN
      BAUM. Die Meldung des Nutzers ist eine ueber den SCHIRM, und die war
      bis heute nicht gestellt.

      ══ GEMESSEN (800x480, drei Zustaende) ═════════════════════════════════

        Zustand                     Zeichen (.spielt-marke)   Kachel (.spielt)
        laeuft alb0-6 (Kapitel 7)   ausserhalb, 816,250       151 px rechts raus
        nach 1x weiter (alb0-7)     ausserhalb, 924,250       259 px rechts raus
        ueber die Albumgrenze       ausserhalb (oben be-      Albumkachel oben
        (alb1-10)                   schnitten von .buehne)    angeschnitten

      IN KEINEM DER DREI ZUSTAENDE steht das Zeichen im Bild — auch nicht in
      dem, den lane-marke-wandert.mjs voellig zu Recht als RICHTIG meldet.
      Beide Werkzeuge haben recht: das eine misst, ob markiert wird, das
      andere, ob man es sieht.

      DIE URSACHE IST NICHT `spieltMarkieren`, SONDERN DASS NICHTS ROLLT.
      `.lane-reihe` ist ein waagerechter Roller (scrollBreite 1082 bei
      sichtBreite 608 fuer 10 Titel). `laneInsBild()` holt die Lane SENKRECHT
      ins Bild, wenn sie aufgeht; danach ruehrt kein Weg die waagerechte
      Rollposition mehr an. Ab Titel 7 von 10 steht die markierte Kachel
      draussen und bleibt dort, waehrend die Marke von Kachel zu Kachel
      weiterwandert. An der Box sind es 239 Titel in 20 Alben — im Schnitt 12
      je Album, also liegt rund die HAELFTE jedes Albums ausserhalb.

      WAS DER BENUTZER STATT DESSEN SIEHT: den eingefaerbten Titel der
      Albumkachel in der Reihe darueber (rot). Die Reihe ist oben
      angeschnitten, ihr Zeichen liegt darueber ausserhalb — der Titeltext
      bleibt aber lesbar. „Gar nichts" waere also uebertrieben; „die Marke"
      sieht er nie.

      ── ZWEI FALLEN, BEIDE ECHT PASSIERT ──────────────────────────────────
      1. HEADLESS GIBT 800x337, nicht 800x480, trotz `--window-size`. Auf
         337 px Hoehe ist die `.buehne` um 703 px weitergescrollt und JEDE
         Marke liegt „ausserhalb" — der erste Lauf meldete lauter Fehler ueber
         einen Schirm, den es nicht gibt. `Emulation.setDeviceMetricsOverride`
         ist Pflicht; in tools/beruehrziele-neu.mjs steht sie seit dem 05.08.
      2. BACKTICKS IN EINEM KOMMENTAR, der in einem Schablonenliteral steht,
         beenden es. Dieselbe Warnung steht in lane-marke-wandert.mjs und war
         abgeschrieben schneller wiederholt als gelesen.

      NICHT ENTSCHIEDEN, WEIL ES DEM BETREIBER GEHOERT: ob die Lane der Marke
      NACHROLLEN soll. Es ist derselbe Einwand wie beim selbsttaetigen
      Albumwechsel — wer sich gerade etwas ansieht, bekaeme die Reihe unter
      dem Finger weggezogen. Der vorsichtige Weg waere, NUR beim Aufgehen der
      Lane zur laufenden Kachel zu rollen (dann hat der Benutzer gerade selbst
      getippt) und danach nie wieder. Die Zahl dafuer liefert das Werkzeug
      mit: `rollenNoetig` sagt je Kachel, wie viele Pixel fehlen.

      NACHMESSEN:
        node tools/marke-sichtbar-nach-albumwechsel.mjs --bild /pfad/vorsilbe
    related:
      - marke-verlaesst-die-offene-titelliste
      - next-verlaesst-die-playlist-nicht
      - raster-marke-schau-gegenprobe
      - beruehrziele-neue-oberflaeche-gemessen

  - kind: entscheidung
    id: beruehrziele-eingeteilt-74-von-95-sind-ein-pixel
    title: "E8/T2 geordnet: 74 der 95 zu kleinen Ziele fehlt EIN Pixel — echt zu klein sind vier Bedienelemente"
    match: "E8 ordnen|beruehrziele einteilen|64 px 8,96 mm|e8-ordnen|e8-kosten"
    tags: ["oberflaeche", "neu", "beruehrziele", "e8", "entscheidung", "werkzeug", 2026]
    source: "05.08.2026, tools/e8-ordnen.py auf tools/beruehrziele-neu.mjs --alle, Kosten aus tools/e8-kosten-messen.mjs"
    body: |
      [[beruehrziele-neue-oberflaeche-gemessen]] liefert die ZAHLEN. Hier
      steht die Reihenfolge — und die haengt an einer einzigen Rechnung:

        9 mm = 64,3 px (0,14 mm/px)      64 px = 8,96 mm

      74 DER 95 BEFUNDE SIND 64-px-KNOEPFE, denen 0,3 px fehlen. Das Projekt
      rundet die Marke selbst auf 64 px ab; diese Ziele sind keine
      Bedienprobleme, sondern eine Rundung. ECHT ZU KLEIN SIND 21 BEFUNDE AUF
      VIER BEDIENELEMENTEN. Wer nicht trennt, plant fuer 95 Ziele, wo vier
      gemeint sind.

      Die 95 Zeilen sind ueberhaupt nur 22 verschiedene Bedienelemente — der
      Mini-Player steht auf sechs Schirmen und wird sechsmal gezaehlt.

      ══ GRUPPE 1 — TRIFFT EIN KIND (12 Elemente, 56 Befunde) ═══════════════

      ECHT ZU KLEIN, nach Dringlichkeit:

        3,50–6,16 mm  .tipp-spiel (44x44, 15x) — der Play-Knopf auf JEDER
          Kachel, der haeufigste Griff der Oberflaeche. KOSTEN: 21 px mehr.
          Auf einer 96-px-Titelkachel waeren 65 px = 68 % ihrer Breite; er
          verdeckte Bild, Folgennummer (.stueck-nr, mittig) und stiesse an
          die Dienst-Plakette (.kachel-marken, unten links). KEIN
          Groessenschrauben, sondern ein Umbau der Kachel. Dieselbe Zahl, die
          bei F6 „zwei Knoepfe passen nicht" ergab.
        4,76 mm  #mp-laut-regler (Griff 34 px auf 14-px-Bahn). KOSTEN: 31 px;
          das Fenster muesste von 58 auf ~90 px wachsen und deckte die
          Interpreten-Reihe dann GANZ zu (heute bleiben 10 px). Beruehrt
          direkt die offene Frage unten.
        6,30 mm  #gr-schieber (540x44 unsichtbar ueber dem 12-px-Balken).
          KOSTEN: 21 px; darueber #gr-unter, darunter .gross-tasten — einer
          von beiden muesste weichen.

      EIN PIXEL FEHLT (und es ist ueberall billig):
        #mp-spiel/#mp-vor/#mp-zurueck/#mp-laut/.mp-bild/.mp-text — die
          Mini-Player-Reihe: 6 Kinder in 688 px, 70 px Luft, Luecken 10 px.
          64→66 px kostet 10 px von 20 px echtem Spielraum. PASST OHNE UMBAU.
        #zurueck — liegt absolut ueber allem, kein Nachbar in der Reihe.
          ABER im Eltern-Bereich steht .fach-knopf „WLAN" 1 px daneben.
        #ag-lauter/#ag-leiser — 268 px senkrechte Luft. PASST muehelos.

      ══ GRUPPE 2 — TRIFFT EINEN ERWACHSENEN (9 Elemente, 35 Befunde) ═══════
      Alle bei 8,96 mm, alle ein Pixel:
        .tor-taste (11 Tasten des PIN-Tors) — ACHTUNG: die Luecke betraegt
          9 px. 64→66 px liesse 7 px (0,98 mm) und damit UNTER 2 mm Abstand.
        .fach-knopf, #fach-tat, .zeile-tat (Eltern-Bereich) — hier ist der
          ABSTAND das Problem, nicht die Groesse: 1 px zu #zurueck.
        #licht-knopf, #bt-knopf, #einst-knopf (Kopfleiste) — 307 px Luft.

      ══ GRUPPE 3 — KEIN ECHTES ZIEL (1 Element, 4 Befunde) ═════════════════
        1,40 mm  .leute-kachel bei offenem Laut-Fenster (92x10 statt 92x127).
          Kein Ziel, sondern ein FEHLGRIFF-RISIKO. Die Kachel selbst ist mit
          12,88 mm reichlich gross; zu entscheiden ist nichts an ihrer
          Groesse.

      NICHT GEZAEHLT, WEIL ROLLPOSITION UND NICHT GESTALTUNG: .kachel
      „Raetselhaftes" (4,62 mm, 183x33 angeschnitten) und .lane-kachel.stueck
      „Titel 7" (5,74 mm, 41x130 angeschnitten). Letztere ist DIESELBE Kachel,
      die in [[marke-zeichen-steht-nie-im-bild]] mit der Marke aus der Reihe
      laeuft — halb draussen ist sie auch halb so gross.

      ── DIE FRAGE, DIE DEM BETREIBER GEHOERT (unverändert offen) ───────────
      Soll das Laut-Fenster Tipps dahinter ABFANGEN? Heute bleibt von den
      Interpreten-Kacheln ein 10-px-Streifen tippbar, und ein Fehlgriff am
      Regler navigiert weg statt zu schliessen. Die Antwort haengt mit dem
      Regler zusammen: waechst er auf 9 mm, deckt das Fenster die Reihe
      ohnehin ganz zu und die Frage erledigt sich von selbst.

      ZAHLEN SIND KEIN BEFUND, SONDERN EIN MESSWERT MIT DATUM: am 05.08.
      standen 97 von 165, heute 95 von 163. Der Bestand der Vorschau
      entscheidet mit, wie viele `.tipp-spiel` im Bild stehen. Die EINTEILUNG
      haelt, die Gesamtzahl wandert.

      NACHMESSEN:
        node tools/beruehrziele-neu.mjs --alle > /tmp/e8.txt
        python3 tools/e8-ordnen.py /tmp/e8.txt      # Ende 1 = neues Element
        node tools/e8-kosten-messen.mjs
    related:
      - beruehrziele-neue-oberflaeche-gemessen
      - marke-zeichen-steht-nie-im-bild
      - rueckweg-verdeckt-die-ueberschrift
  - kind: falle
    id: mplayer-verwirft-den-titelnamen-und-muss-es
    title: "Der F1-Titelname kommt bei mplayer WIRKLICH an — und wer ihn dort durchreicht, macht aus Abspielen ein Einreihen"
    match: 'mplayer titelname|force-media-title mplayer|zweites argument loadfile|play zwei argumente|F1 mplayer|loadfile anhaenge-schalter'
    tags: ['player', 'mplayer', 'mpv', 'F1', 'typen', 'test', 'falle', 2026]
    source: "05.08.2026 gebaut und gemessen; src/backend-player/src/mplayer-wrapper.ts + .spec.ts, tools/rotprobe.py. Commit 59131ac8."
    body: |
      [[f1-titelname-geht-mit-gebaut]] gibt den Folgennamen als zweites
      Argument an `player.play` / `player.queue`. Bei mpv reist er als
      Datei-Option `force-media-title` mit. DIE ANNAHME „bei mplayer kommt
      das gar nicht erst an" IST FALSCH — und genau daran haengt eine Falle.

      ── ES KOMMT AN ─────────────────────────────────────────────────────
      spotify-control.ts:117 holt BEIDE Aufsaetze ueber dasselbe
      untypisierte `require`:

          const createPlayer = playerEngine === 'mpv'
            ? require('./mpv-wrapper') : require('./mplayer-wrapper')

      Der Uebersetzer sieht dort `any`. Der Aufrufer uebergibt in beiden
      Faellen zwei Argumente; bei mplayer nimmt `play` nur eines, und das
      zweite faellt ins Leere. Es faellt ins Leere, WEIL die Umsetzung es
      wegwirft — nicht, weil es nicht angekommen waere.

      ── WARUM DAS DURCHREICHEN TEUER WAERE ──────────────────────────────
      mplayer im Slave-Modus liest das ZWEITE Wort eines `loadfile` als
      seinen Anhaenge-Schalter:

          loadfile /media/lied.mp3         -> abspielen
          loadfile /media/danach.mp3 1     -> einreihen   (so baut `queue`)

      Wer den Titelnamen dort anhaengt, macht aus JEDEM Startbefehl ein
      Einreihen — auf einer mplayer-Box startete nichts mehr. Aus einem
      falschen Titelnamen (Schoenheitsfehler) waere eine stumme Box
      geworden. Dieselbe Rechnung wie beim Rueckfall in [[f1-titelname-geht-mit-gebaut]].

      ── WANN DER FEHLGRIFF NAHELIEGT ────────────────────────────────────
      Nicht heute — heute verbietet gar nichts das zweite Argument, weil
      die Weiche `any` ist. Aber SOBALD JEMAND SIE TYPISIERT (die
      B1-`Player`-Abstraktion steht als Absicht im Kopf von
      mplayer-wrapper.ts), meldet der Uebersetzer „zu viele Argumente" an
      einer Zeile, die seit Monaten laeuft. Der kuerzeste Weg zu Gruen ist
      dann, den Parameter aufzunehmen und weiterzugeben. Genau der Weg
      fuehrt in die stumme Box.

      ── WAS DAGEGEN JETZT DASTEHT (05.08.2026) ──────────────────────────
      * `MplayerPlayer.play(fileOrUrl, titel?)` / `.queue(fileOrUrl, titel?)`
        nehmen den Titel im VERTRAG entgegen, und der Kommentar daneben sagt,
        dass er verworfen wird. Die Umsetzung darunter ist zeichengleich
        unveraendert — weniger Parameter sind dem Uebersetzer recht. Damit
        entsteht der Uebersetzerfehler oben gar nicht erst.
      * mplayer-wrapper.spec.ts: „F1: der Titelname wird verworfen — die
        Befehlszeile bleibt wortgleich". Derselbe Befehl mit und ohne Namen,
        verglichen gegen den LAUF ohne Namen UND gegen den Wortlaut. Beide
        Testtitel tragen ein Leerzeichen; durchgereicht stuenden sie in
        Anfuehrungszeichen hinter dem Pfad.
      * `tools/rotprobe.py`, Punkt 6: zwei Eingriffe, die den Fehlgriff
        EINSETZEN statt einen frueheren Stand zurueckzunehmen — an
        mplayer-wrapper.ts hat F1 nichts geaendert, es gibt nichts
        zurueckzunehmen. Beide werden rot; damit sind es NEUN F1-Naehte.

      ── WAS SICH DAMIT NICHT AENDERT, nachgerechnet ─────────────────────
      Es sind Typen, ein Test und ein Werkzeug. Der Bau liefert Byte fuer
      Byte dieselbe Datei wie vorher:
        md5 fda2d6878825f8591f8e0a6eec29c864
        = src/deploy/spotify-control.js = die Datei auf .169.
      Kein Neustart noetig, keiner gemacht. 146 von 146 Tests gruen (vorher
      145), `check-types` gruen.

      ── NACHBARSCHAFT, damit niemand zweimal sucht ──────────────────────
      Der ANDERE mplayer-Rueckweg aus F1 —
      `getProps(['media_title'])` -> `ANS_ERROR=PROPERTY_UNAVAILABLE` ->
      `out.emit('playlist-finish')` — ist NICHT davon beruehrt und bleibt
      offen; er steht in
      [[f1-nachgemessen-und-der-mplayer-rueckweg]].

      NACHMESSEN:
        cd src/backend-player && npm test && npm run check-types
        python3 tools/rotprobe.py --nur f1
    related:
      - f1-titelname-geht-mit-gebaut
      - f1-nachgemessen-und-der-mplayer-rueckweg
      - mpv-nimmt-den-titelnamen-an-force-media-title
      - attrappe-luegt-durch-weglassen

  - id: amend-am-gemeinsamen-baum-frisst-fremde-commits
    kind: falle
    title: "`git commit --amend` an einem Baum, an dem mehrere arbeiten, frisst den Commit des anderen"
    match: "amend gemeinsamer baum|fremden commit ueberschrieben|amend fremde hunks|git amend mehrere agenten|commit verschwunden amend"
    tags: ["git", "zusammenarbeit", "falle", 2026]
    source: "05.08.2026 selbst passiert und wieder repariert (Reflog); MuPiBox-Baum, drei Vorgaenge gleichzeitig"
    body: |
      WAS PASSIERT IST: Ein eigener Commit (nur eine neue Datei in tools/) sollte
      um eine Verbesserung derselben Datei ergaenzt werden — `git add <meine
      datei>` und `git commit --amend`. Zwischen dem eigenen Commit und dem
      Amend hatten ZWEI andere Vorgaenge committet. `--amend` fasst aber IMMER
      den aktuellen HEAD an, und der war laengst nicht mehr der eigene.

      ERGEBNIS: Der Commit eines Fremden („pruefen(abspieler): was mplayer mit
      dem Folgennamen macht, stand nirgends", drei Dateien) verschwand. Seine
      Dateien lagen danach unter EINER fremden Nachricht, die von ihnen nichts
      weiss — genau der Schaden, gegen den `tools/eigene-hunks.py` gebaut wurde,
      nur eine Ebene hoeher: dort geht es um fremde HUNKS im Index, hier um
      einen fremden COMMIT.

      AUFGEFALLEN IST ES AN DER ZEILE „4 files changed", wo eine erwartet war.
      Wer nur die Nachricht liest und nicht die Statistik, merkt es nicht.

      REPARIERT UEBER DAS REFLOG, ohne Verlust:

        git reflog                       # den fremden Commit wiederfinden
        git reset --soft <fremder-hash>  # er ist wieder HEAD, Index bleibt
        git commit -F <nachricht>        # die eigene Aenderung obendrauf

      `--soft` ist der ganze Punkt: Der Arbeitsbaum wird nicht angefasst, und
      der Index behaelt genau das, was drinstand. Nach dem Zuruecksetzen zeigte
      `git diff --cached --stat` wieder nur die EINE eigene Datei.

      DIE REGEL DARAUS: An einem gemeinsam bearbeiteten Baum wird NICHT
      ge-amendet. Eine Verbesserung bekommt einen ZWEITEN Commit — das ist
      ohnehin ehrlicher, weil sie eine eigene Begruendung hat. Wer trotzdem
      amenden will, prueft VORHER `git log --oneline -1`, ob HEAD noch der
      eigene ist, und HINTERHER die Dateiliste des Commits.

      GILT AUCH FUER DAS TIPPFEHLER-AMEND. Der erste Amend dieses Tages war
      harmlos (HEAD war noch der eigene) und hat genau deshalb Vertrauen
      erzeugt, das der zweite dann gekostet hat.
    related:

  - id: e29b6-zugangsdaten-verschluesselt-und-akkuverlauf-mit
    kind: architektur
    title: "E29/B6 gebaut: zwei Lagen (weglassen / verschluesselt), und der Akkuverlauf geht mit — aber nicht in die Kennung"
    match: "zugangsdaten verschluesselt sicherung|mit-zugangsdaten|zugangsdaten.gpg|behaelter sicherung|akkuverlauf mitsichern|beiwerk kennung|E29 B6 gebaut"
    tags: ["sicherung", "zugangsdaten", "verschluesselung", "akkuverlauf", "E29", 2026]
    source: "05.08.2026 gebaut, an .169 gefahren und ausgeliefert; tools/zugangsdaten-am-geraet.py (35 Punkte), --selbsttest 126, tools/rotprobe.py --nur E29/B6 (12 Eingriffe alle rot)"
    body: |
      DIE ENTSCHEIDUNG DES BETREIBERS WAR: Zugangsdaten MIT, aber verschluesselt,
      und die Verschluesselung OPTIONAL mit Passwort. Gebaut sind deshalb genau
      ZWEI Lagen — eine dritte („Klartext") gibt es absichtlich nicht:

        weglassen        Vorgabe, unveraendert. Ohne Passwort kein Geheimnis
                         im Archiv; der Stand sagt namentlich, was fehlt.
        verschluesselt   `--anlegen --mit-zugangsdaten`. Die elf Stellen aus
                         GEHEIM wandern in EINEN Behaelter `zugangsdaten.gpg`
                         (gpg --symmetric --force-ocb --cipher-algo AES256).

      ── DREI ENTWURFSENTSCHEIDUNGEN, DIE MAN SONST FALSCH TRIFFT ─────────

      1. NUR DIE GEHEIMNISSE, NIE DAS GANZE ARCHIV. Sonst faellt der Rueckweg
         ohne SSH (E29/B3) und die woechentliche Probe kann nichts mehr
         pruefen. Wer das Passwort verliert, bekommt Bibliothek, gemerkte
         Stellen, Kinderzeit und Darstellung trotzdem zurueck.

      2. DER BEHAELTER LIEGT NEBEN DEN DATEIEN, NICHT IN IHNEN. Ein Chiffrat
         IN mupiboxconfig.json haette drei Wachen ausgehebelt, die es schon
         gibt: `konfig_pruefen` haette es als gueltiges `interfacelogin
         .password` durchgewinkt (Box waere ausgesperrt und die Pruefung
         zufrieden), `probe()` haette Chiffrat mit Chiffrat verglichen und
         BESTANDEN gemeldet, und ein Zurueckspielen ohne Passwort haette das
         Chiffrat in die laufende Konfiguration geschrieben.

      3. DER BEHAELTER STEHT NICHT IN `stand["dateien"]`. Dort haengen Ziel,
         Byte-Vergleich der Probe und die `inhalt_kennung` dran — fuer einen
         Klumpen, der nirgendwohin geschrieben wird, waere jedes davon
         falsch. Und weil gpg bei jedem Lauf ein frisches Salz nimmt, waere
         `--wenn-anders` mit ihm in der Kennung fuer immer wirkungslos. Seine
         sha256 steht deshalb in `stand["zugangsdaten"]["sha256"]` und wird
         in `stand_oeffnen` VON HAND geprueft (samt Fall „Eintrag fehlt").

      ── DIE RANGFOLGE BEIM ZURUECKSPIELEN, je Schluessel ─────────────────
        1. entschluesselter Wert aus dem Archiv (nur mit Passwort und
           gehaltener Echtheitspruefung)
        2. sonst der Wert, der JETZT auf der Box steht (bisheriges Verhalten)
        3. sonst: gemeldet, aber NICHT geschrieben — auch nicht als
           Platzhalter ([[sicherung-ausgelassene-schluessel-nie-schreiben]]
           gilt unveraendert weiter)

      ── DIE KONKRETE STELLE, sonst haengt das Passwort am falschen Netz ──
      `/*/pw` in wlan.json ist EIN Zeiger, aber MEHRERE Werte. Sie der Reihe
      nach zurueckzuschreiben geht schief, sobald ein Netz dazwischen gar
      kein Passwort hat: aus drei Netzen mit zwei Passwoertern werden zwei
      falsch belegte. Der Behaelter merkt sich deshalb je Wert die AUFGELOESTE
      Stelle (`/0/pw`, `/2/pw`) — `zeiger_stellen` liefert sie, `zeiger_setzen`
      legt sie zurueck und erfindet dabei NIE einen Listenplatz, den es nicht
      mehr gibt (ein Netz zu erfinden waere schlimmer als eines ohne Passwort).

      ── WER TIPPT DAS PASSWORT: die Form folgt daraus ────────────────────
      `--anlegen` laeuft selbsttaetig, `--von-karte` beim Boot als root. In
      beiden ist niemand da. Also gilt: Zugangsdaten nur in einen Stand, den
      jemand VON HAND angestossen hat. Der Rueckweg von der Karte spielt alles
      andere ein und NENNT im Bericht die Felder, die verschluesselt liegen
      geblieben sind, samt Befehl zum Nachholen. `--mit-zugangsdaten` heftet
      den Stand automatisch an (`.behalten.`) — sonst verdraengten ihn die
      taeglichen Laeufe nach spaetestens zehn Malen.

      ── DER AKKUVERLAUF GEHT MIT — ABER ALS „BEIWERK" ────────────────────
      GEMESSEN an .169 (05.08.2026): Groesse ist NICHT das Problem.
        akkuverlauf.json roh   189,6 KB (4433 Punkte, 43,8 B je Punkt)
        Stand ohne / mit       11,5 KB -> 42,0 KB   (Faktor 3,7)
        am Deckel (10080 Pkt)  82,4 KB              (Faktor 7,2)
        auf der Karte          7 Staende x 82,4 KB = 0,60 % des freien Platzes
      DAS PROBLEM IST DIE KENNUNG. Der Server schreibt die Reihe alle zehn
      Minuten (server.ts, AKKU_SCHREIB_JE=10). Zaehlte sie in `inhalt_kennung`,
      waere die IMMER anders: `--wenn-anders` unterdrueckte nie mehr einen
      Lauf, jeder Anstoss legte einen vollen Stand an, das Kontingent von 10
      rollte durch — und damit fiele die Vorkehrung aus
      [[aufraeum-sicherung-vor-der-entscheidung]].
      LOESUNG: `BEIWERK` — die Datei geht MIT, bleibt aber aus der Kennung
      heraus. Nebenwirkung, die zaehlt: aeltere Staende ohne Beiwerk behalten
      genau dieselbe Kennung, `--wenn-anders` vergleicht ueber die Umstellung
      hinweg richtig. Am Geraet nachgemessen: neuer Stand 43 316 B / 19
      Dateien / 207 ms, der Lauf danach meldete „unveraendert — kein neuer
      Stand".
      WIE DIESE AUSSAGE GEPRUEFT WURDE — und wie sie es NICHT werden konnte:
      Der naheliegende Versuch (warten, bis der Server die Reihe schreibt, dann
      `--anlegen --wenn-anders`) ist an einer LAUFENDEN Box WERTLOS. Genau so
      gefahren (14:02:37 geschrieben, 195870 -> 196300 B): es entstand ein neuer
      Stand — aber der Vergleich der beiden Staende zeigte, dass sich in
      denselben vier Minuten auch network.json, offline_resume.json und
      resume.json geaendert hatten. Die Messung sagt also nichts ueber den
      Akkuverlauf.
      DER TRAGFAEHIGE BEWEIS ist rein rechnerisch und rein lesend: die Kennung
      eines ECHTEN Standes noch einmal ausrechnen, einmal mit seiner eigenen
      Messreihe und einmal mit der eines anderen Standes (verschiedene sha256).
      Ergebnis identisch, und identisch mit der im Stand hinterlegten; die
      Gegenprobe mit verbogener data.json ergibt eine andere. Werkzeug dafuer:
      `tools/kennung-beweis.py`.
      WAS DABEI NEBENBEI SICHTBAR WURDE: an einer Box, auf der jemand hoert,
      aendert sich resume.json ohnehin staendig — `--wenn-anders` unterdrueckt
      dort selten. Es unterdrueckt aber sehr wohl in den Faellen, auf die es
      ankommt (mehrere Auslieferungsschritte kurz hintereinander, Box ueber
      Nacht in Ruhe), und genau die waeren mit dem Akkuverlauf in der Kennung
      verloren gegangen. Nachgemessen: der zweite Lauf im selben Augenblick
      meldete „unveraendert — kein neuer Stand".

      ZWEITE FOLGE: `jsonfile.writeFile` schreibt auf die ZIELDATEI, die Reihe
      hat also dasselbe Nullbyte-Fenster wie data.json. Eine Messreihe darf
      aber nie die Sicherung der BIBLIOTHEK verhindern — ein leer erwischtes
      Beiwerk faellt deshalb aus DIESEM Stand heraus und wird benannt, statt
      den ganzen Lauf zu verweigern (`klagen_ueber` ueberspringt BEIWERK).

      ── DIE FALLE, DIE 60 SEKUNDEN GEKOSTET HAETTE ───────────────────────
      `gpg --list-packets` (die Strukturpruefung der Probe, ohne Passwort)
      WARTET ohne `--pinentry-mode loopback` auf ein Eingabefenster, das es
      auf einer Box ohne Bildschirm nie geben wird. Gemessen, gleiche Datei:
        ohne loopback  60,4 s      mit loopback  0,005 s
      Beide Male rc=2 und dieselbe Kopfzeile — der Unterschied ist NUR die
      Wartezeit. In der woechentlichen Probe waeren das 60 s in einer Unit
      mit TimeoutStartSec.

      ── ZWEI SCHREIBWEISEN DERSELBEN KOPFZEILE ───────────────────────────
      Wer nur eine kennt, baut einen Pruefer, der auf dem anderen Rechner
      blind ist:
        Box .169, gpg 2.4.7    „gpg: AES256.OCB encrypted session key"
        Arbeitsplatz, 2.4.9    „:symkey enc packet: version 5, …, aead 2, …"
      `ist_aead()` kennt beide und sagt bei `aead 0` (nur MDC) NEIN.

      ── WAS DIE WOECHENTLICHE PROBE JETZT ZUSAETZLICH PRUEFT ─────────────
      Sie sucht NICHT im juengsten Stand, sondern im juengsten MIT Behaelter —
      der juengste ist auf einer echten Box fast immer der selbsttaetige ohne
      Zugangsdaten, und wer nur ihn ansieht, prueft die Verschluesselung NIE
      und sagt trotzdem BESTANDEN. Findet sie einen, prueft sie: ist es noch
      ein AEAD-Paket, und GIBT ES GPG NOCH. Das zweite ist der eigentliche
      Wert — gnupg ist auf der Box nur angeweht
      ([[e29b7-gpg-kann-doch-aead-und-gnupg-ist-nur-angeweht]]); faellt es
      heraus, sind alle verschluesselten Staende unlesbar, und ohne diese
      Pruefung merkte es erst der Ernstfall. `gnupg` steht jetzt namentlich
      in autosetup.sh UND in update/start_mupibox_update.sh.

      ── KOSTEN, an .169 gemessen ────────────────────────────────────────
        anlegen ohne Behaelter     89 ms      mit Behaelter   369 ms
        Behaelter selbst          452 B
        oeffnen (richtiges PW)    262 ms     falsches PW     267 ms
        --pruefen  8 ms           --probe    53 ms
      Das Passwort geht ueber einen vererbten Dateizeiger (`--passphrase-fd`,
      `pass_fds`), NIE ueber argv (`ps` zeigt Argumente jedem Benutzer), und
      der Klartext geht ueber Rohre, nie ueber eine Datei. Eigenes
      Wegwerf-GNUPGHOME, Agent wird danach beendet (`gpgconf --kill all`).

      ── NICHT GEMESSEN, ausdruecklich ────────────────────────────────────
      Es wurde KEIN Stand mit den ECHTEN Zugangsdaten der Box angelegt: dazu
      muesste ein Passwort gewaehlt werden, und das gehoert dem Betreiber.
      Alle Messungen liefen gegen einen Wegwerf-Baum mit erfundenen Marken.
    related:
      - sicherung-ausgelassene-schluessel-nie-schreiben
      - e29b7-gpg-kann-doch-aead-und-gnupg-ist-nur-angeweht
      - sicherung-probe-woechentlich
      - sicherung-rueckweg-ueber-die-karte
  - id: f1-klassische-oberflaeche-nachgemessen
    kind: knowhow
    title: "F1 gegengelesen: die KLASSISCHE Oberflaeche zeigt den Folgennamen wirklich mit — auf beiden Flaechen, an beiden Diensten"
    match: "F1 klassische oberflaeche|npb-title|toolbar-tracktitle|titelVomAbspieler|mitgeheilt|F1 gegenlesen"
    tags: ['player', 'oberflaeche', 'klassisch', 'mpv', 'jellyfin', 'ard', 'F1', 'messen', 2026]
    source: "05.08.2026 an .169 im echten Browser gemessen — tools/f1-klassische-oberflaeche-am-geraet.mjs (--jellyfin und --ard)"
    body: |
      DER SATZ, UM DEN ES GEHT, steht in
      [[mpv-media-title-traegt-je-dienst-verschieden]]: „der Abspieldienst
      waere der bessere Ort — dann waere die KLASSISCHE Oberflaeche
      MITGEHEILT". Das ist die Begruendung dafuer, F1 im Dienst zu bauen und
      nicht in der Oberflaeche. GEPRUEFT WAR SIE NIE:
      [[f1-titelname-geht-mit-gebaut]] und tools/f1-titelname-geht-mit.mjs
      messen `/player/local` — also was der DIENST meldet, nicht was die
      klassische Oberflaeche daraus macht.

      ── GEMESSEN, mit einem echten Browser gegen die Box ────────────────
      Zwei Flaechen, beide ueber einen Uebergang, den NIEMAND befohlen hat
      (kurz vors Ende gesetzt, dann zugesehen):

        jellyfin  Mini-Leiste     1 „Mittelmaessiges Leben"  2 „Nicht alleine"
                  grosse Ansicht  2 „Nicht alleine"          3 „Freibad Pommes"
        ard       Mini-Leiste     1 „Mit dem Rad"            2 „Faelschung"
                  grosse Ansicht  2 „Faelschung"             3 „Zu Besuch"

      Kein Fehlgriff. Bei der ARD steht „Faelschung" und NICHT
      „Faelschung | Die Maus zum Hoeren (…)" — die Doppelung, die ein blosser
      `media-title`-Hoerer geliefert haette, ist auch in der Oberflaeche weg.
      Die Mini-Leiste hing an genau EINER von elf Messungen einen Takt
      hinterher; `local$` ist ein Abruf im Sekundentakt
      (media.service.ts, `visibleInterval(1000)`), das ist kein Fehler.

      ── DIE ZWEI FLAECHEN SIND NICHT DIESELBE STELLE ───────────────────
        `.npb-title`            Mini-Leiste, now-playing-bar. Haengt an
                                `toNowPlaying` und damit an `currentTrackname`
                                — UNABHAENGIG vom Medientyp. Auf fast jeder
                                Seite da. Laesst sich in Einstellungen ->
                                Darstellung abschalten; dann misst dort
                                niemand mehr etwas.
        `.toolbar-tracktitle`   grosse Ansicht. Zeigt `currentTrackname` NUR,
                                wenn `titelVomAbspieler` wahr ist, und das
                                haengt am `media.type` DER SEITE:
                                'library' | 'jellyfin' | 'jellyfin-album'.

      WER DIE GROSSE ANSICHT UEBER DIE MINI-LEISTE OEFFNET (der Weg, den ein
      Mensch bei schon laufender Wiedergabe nimmt), bekommt sie ueber
      `handleExternalPlayback`: dort wird der Typ aus `currentType`
      zurueckgebaut, und alles ausser radio/rss wird 'library'. ARD und
      Jellyfin fallen also BEIDE in den Zweig, der den Namen zeigt — deshalb
      ist die ARD-Messung oben gruen.

      ── WAS DAMIT NICHT GEMESSEN IST ───────────────────────────────────
      Der Weg ueber die ARD-KACHEL der klassischen Oberflaeche selbst. Dann
      traegt die Seite ein `media` mit `type: 'ard'` (ard.ts, media-provider.ts),
      und `titelVomAbspieler` sagt dazu NEIN — die Titelzeile bliebe leer.
      Das ist AELTER als F1 und faellt heute nicht auf, weil die klassische
      Oberflaeche fuer die ARD gar nicht anhaengt: `jfqueue` steht in
      player.service.ts, `ardqueue` kommt im ganzen frontend-box NICHT vor.
      Ohne Warteschlange gibt es keinen Uebergang und nichts, was
      stehenbleiben koennte. Wer der klassischen Oberflaeche das Anhaengen
      fuer die ARD beibringt, muss 'ard' in `titelVomAbspieler` mit
      aufnehmen — sonst hat er eine leere Titelzeile statt einer falschen.
      NICHT am Geraet durchgeklickt, aus dem Quelltext gelesen.
    related:
      - f1-titelname-geht-mit-gebaut
      - mpv-media-title-traegt-je-dienst-verschieden
      - titelname-wird-nachgeschlagen-wie-das-cover
      - f1-nachgemessen-und-der-mplayer-rueckweg
  - id: abspieldienst-loglevel-error-macht-warnungen-stumm
    kind: falle
    title: "Der Abspieldienst laeuft mit logLevel error — jedes log.warn und log.info darin ist STUMM"
    sev: high
    match: "logLevel|console-log-level|log.warn stumm|Wiedergabe-Maschine im journal|player stderr hoerer|force-media-title abgelehnt protokoll"
    cause: "spotify-control.ts baut seinen Logger mit `console-log-level({level: config.server.logLevel})`, und in /home/dietpi/.mupibox/spotifycontroller-main/config/config.json steht \"logLevel\": \"error\". Bei dieser Stufe geben `log.info` und `log.warn` NICHTS aus — nur `log.error` erscheint."
    fix: "Meldungen, die einen FEHLSCHLAG beschreiben, mit `log.error` schreiben (so geschehen fuer den 'stderr'-Hoerer des Aufsatzes). Ob die Box insgesamt auf 'warn' gehoert, ist eine Frage an den Betreiber."
    tags: ['player', 'protokoll', 'mpv', 'falle', 'F1', 'messen', 2026]
    source: "05.08.2026 an .169 gemessen; console-log-level ortlich nachgestellt (level error: info und warn geben nichts aus). Commit 5f9db5ae."
    body: |
      „PLAYER STDERR HOERER" TRIFFT ZWEI EINTRAEGE, und sie liegen
      HINTEREINANDER auf demselben Weg, nicht nebeneinander:

          mpv schreibt gar nichts erst       -> [[mpv-no-terminal-verschluckt-alle-warnungen]]
          (--no-terminal, und lange kein
          Hoerer auf 'stderr')
          Der Dienst bekommt es, gibt es
          aber nicht aus (logLevel error)    -> DIESER Eintrag

      DIE FRAGE, DIE TRENNT: erscheinen die schlichten `console.log`-Zeilen
      („track name is …") im journal, waehrend eine `log.warn`-Zeile fehlt?
      Dann ist es die Stufe — dieser Eintrag. Fehlt dagegen JEDE Spur von
      mpv selbst, ist es der andere. Wer nur den Hoerer nachruestet und die
      Meldung mit `log.warn` schreibt, hat NICHTS gewonnen: sie bleibt stumm.

      DIE FALLE IST NICHT, DASS ETWAS FEHLT — sondern dass eine SUCHE IM
      PROTOKOLL wie ein Beweis aussieht und keiner ist.

      ── WIE ES AUFFIEL ──────────────────────────────────────────────────
      Ein Werkzeug wollte wissen, welche Tonmaschine laeuft, und suchte die
      Zeile „[Spotify Control] Wiedergabe-Maschine: mpv" im journal. Sie steht
      seit dem mpv-Umbau im Quelltext und erscheint dort NIE — waehrend die
      schlichten `console.log`-Zeilen („track name is …") jederzeit dastehen.
      `console.log` geht am Logger vorbei; `log.info` nicht.

      ── WAS DAMIT ALLES STUMM IST ───────────────────────────────────────
      In spotify-control.ts ein Dutzend `log.warn` und acht `log.info`,
      darunter:
        * „loadfile mit force-media-title abgelehnt" — der Beleg dafuer, dass
          der F1-Rueckfall gefeuert hat ([[f1-titelname-geht-mit-gebaut]]).
        * „<verb> verworfen: Spotify spielt" — der Grund, aus dem eine Folge
          in der Warteschlange fehlt.
        * „mpv laesst sich nicht starten: …" — die stumme Box.
      Der 'stderr'-Hoerer, den F1 ausdruecklich dafuer gebaut hat („damit es
      jemand hoert"), schrieb mit `log.warn` und war damit genauso stumm wie
      gar keiner. Er steht seit 5f9db5ae auf `log.error`.

      ── DIE FOLGE FUER DAS MESSEN ───────────────────────────────────────
      „Im Protokoll steht kein einziges Mal X" beweist ueber diese Box GAR
      NICHTS, solange X ueber `log.warn`/`log.info` geschrieben wird. Genau
      dieser Satz stand als Beleg gegen den F1-Rueckfall in
      [[f1-nachgemessen-und-der-mplayer-rueckweg]]. Der Befund dort bleibt
      richtig, aber aus dem ZWEITEN Grund, der dort auch steht: bei der ARD
      meldet der Dienst „Faelschung" ohne die Doppelung, und die stuende da,
      waere der Rueckfall gelaufen.

      ── WORAN MAN DIE TONMASCHINE STATTDESSEN ERKENNT ───────────────────
      Am KINDPROZESS. Der Aufsatz startet seine Maschine sofort beim Laden:

        ps -eo ppid,args | awk -v p=$(systemctl show -p MainPID --value \
            mupibox-player.service) '$1==p'

      Das ergibt `mpv --idle=yes …` bzw. `mplayer -slave …` und luegt nicht
      (tools/mplayer-noch-heil.mjs macht es so).

      ── NEBENBEI: mpv SCHREIBT SELBST NICHTS ────────────────────────────
      Der Aufsatz reicht mpvs Fehlerkanal an dasselbe Ereignis weiter. Da
      kommt nie etwas an: mpv laeuft mit `--no-terminal`, und damit schreibt
      es selbst bei einer Adresse, die es nicht oeffnen kann, NULL Byte nach
      stderr (an .169 nachgestellt). Was den Hoerer erreicht, sind
      ausschliesslich die drei Meldungen des Aufsatzes selbst.
    related:
      - f1-titelname-geht-mit-gebaut
      - f1-nachgemessen-und-der-mplayer-rueckweg
      - server-antwortet-200-auf-alles
      - mpv-no-terminal-verschluckt-alle-warnungen
  - id: mplayer-rueckfall-am-ton-nachgemessen
    kind: knowhow
    title: "Der mplayer-Rueckfall traegt noch: umgestellt, gehoert, zurueckgestellt (0,0000 -> 0,9743)"
    match: "mplayer noch heil|playerEngine mplayer|rueckfall mplayer ton|mplayer-noch-heil"
    tags: ['player', 'mplayer', 'mpv', 'ton', 'messen', 'F1', 2026]
    source: "05.08.2026 an .169 gemessen — tools/mplayer-noch-heil.mjs (Spitzenpegel am Mithoerausgang)"
    body: |
      ZUSTANDSAUSSAGE — sie hat Haltbarkeit und gehoert vor dem naechsten Glauben
      noch einmal nachgesehen.

      `playerEngine` steht auf .169 auf „mpv"; der mplayer-Weg ist die VORGABE
      des Projekts und wird dort nie gefahren. Seit F1 bekommt der Aufsatz
      ZWEI Argumente — bei mplayer muss das zweite folgenlos verschwinden
      ([[mplayer-verwirft-den-titelnamen-und-muss-es]]). Ob der Weg als GANZES
      noch traegt, sagte bis hierhin niemand.

      GEMESSEN (Jellyfin, „HAMM", Titel 1 starten und Titel 2 anhaengen):

        playerEngine mplayer, Dienst neu gestartet
          Kindprozess des Dienstes:  mplayer     (nicht geraten, abgelesen)
          Mithoerausgang still:      Spitze 0,0000
          waehrend der Wiedergabe:   Spitze 0,9743
          Dienst meldet:             „Mittelmaessiges Leben", nr 1, timePos 11 s

      Der Name kommt bei mplayer allein aus dem BEFEHL und ist dort richtig —
      das ist das Verhalten von vor F1 und soll so bleiben. Das Anhaengen
      (`loadfile <adr> 1`) hat die laufende Wiedergabe nicht angeruehrt.

      DANACH ZURUECKGESTELLT: `playerEngine` wieder „mpv", Datei zeichengleich
      wie vorher (md5 verglichen), Dienst neu gestartet, Kindprozess wieder
      mpv. Das Werkzeug nimmt beides im `finally` zurueck, auch bei Abbruch.

      NICHT GEMESSEN: ein Titeluebergang UNTER mplayer (dafuer haette der Lauf
      140 s dauern muessen) und die lokale Bibliothek. Gemessen ist der
      Anfang: mplayer startet, spielt hoerbar, und der Name stimmt.
    related:
      - mplayer-verwirft-den-titelnamen-und-muss-es
      - f1-nachgemessen-und-der-mplayer-rueckweg
      - optim-mplayer-mpv
  - id: e29b6-gegengelesen-zwei-lecks
    kind: falle
    title: "E29/B6 adversarisch gegengelesen: das Passwort stand in gpgs Umgebung, und das WLAN-Passwort hing am falschen Netz"
    match: "e29b6 gegenlesen|zeiger_uebernehmen|_listen_zuordnung|_kind_umgebung|proc environ passwort|wlan passwort falsches netz|e29b6-adversarisch"
    sev: high
    tags: ["sicherung", "zugangsdaten", "verschluesselung", "wlan", "gegenlesen", "E29", 2026]
    source: "05.08.2026 an .169 gemessen und behoben; tools/e29b6-adversarisch.py (96 Punkte), --selbsttest 126 -> 133, Rotprobe je Eingriff"
    cause: >
      Zwei Loecher neben der verriegelten Tuer. Erstens reichte
      `env={**os.environ, …}` die Umgebungsvariable mit dem Passwort an jedes
      gpg-Kind weiter — die Befehlszeile war sauber, `/proc/<gpg>/environ`
      nicht. Zweitens ordnete `zeiger_uebernehmen` Listeneintraege ueber den
      LISTENPLATZ zu, nicht ueber ihren Inhalt.
    fix: >
      `_kind_umgebung()` baut die Umgebung fuer jedes gpg-Kind neu und laesst
      PW_UMGEBUNG weg. `_listen_zuordnung()` erkennt einen Eintrag an seinem
      uebrigen Inhalt (bei wlan.json: der ssid) und nimmt nur einen
      EINDEUTIGEN Treffer.
    body: |
      GEFUNDEN WURDEN SIE NICHT DURCH LESEN, sondern durch eine Messung, die
      der Erbauer nicht vorgesehen hatte: waehrend eines ECHTEN Laufs jeden
      eigenen Prozess in /proc abtasten — Befehlszeile UND Umgebung.

      ── ERSTES LECK: /proc/<gpg>/environ ────────────────────────────────
      Die Frage „steht das Passwort in argv?" war geprueft und beantwortet
      (nein, es geht ueber `--passphrase-fd`). Die Frage daneben war es
      nicht. `env={**os.environ, "GNUPGHOME": heim, …}` reicht die GANZE
      eigene Umgebung weiter — an gpg, an gpg-agent, an gpgconf.
      Das zaehlt, obwohl `/proc/<pid>/environ` nur dem Eigentuemer gehoert:
      derselbe Benutzer ist hier auch Server und Abspieldienst.

      WAS AUCH NACH DEM FIX GILT und deshalb im Docstring von
      `passwort_holen` steht — DIE DREI EINGABEWEGE SIND NICHT GLEICH SICHER:
        Tastatur   am dichtesten
        stdin      ebenso; gemessen: das Passwort stand in KEINEM Prozess,
                   auch nicht im eigenen
        Umgebung   bequem, aber waehrend des Laufs fuer jeden Vorgang
                   desselben Benutzers lesbar. `unsetenv` hilft NICHT:
                   /proc/self/environ zeigt unter Linux den Block aus dem
                   execve, nicht den aktuellen.

      ── ZWEITES LECK, das schwerere: das Passwort am falschen Netz ───────
          im Stand (aelter)    [ {ssid: A}, {ssid: B} ]    pw ausgelassen
          auf der Box (heute)  [ {ssid: B, pw: <B>} ]      A wurde entfernt
          bisher            -> [ {ssid: A, pw: <B>}, {ssid: B} ]
      Hat sich nur die REIHENFOLGE gedreht, tauschen zwei Netze ihre
      Passwoerter und beide sind falsch.

      DAS IST NICHT DER WEG MIT DEM BEHAELTER. Fuer den hatte
      `zeiger_stellen` genau diese Falle schon geschlossen (die aufgeloeste
      Stelle `/0/pw` geht in den Behaelter). Es war der Weg OHNE — die
      VORGABE, ohne Passwort, und genau der, der beim Zurueckspielen von der
      Karte laeuft. Also im Ernstfall, wenn niemand mehr hinsieht. Und es
      faellt nicht auf: alle Netze stehen drin, alle haben ein Passwort, die
      Box verbindet nur nicht mehr.
      DIE LEHRE, die ueber diesen Fall hinausgeht: WER EINE FALLE AN EINER
      STELLE SCHLIESST, MUSS DIE ZWEITE STELLE SUCHEN, AN DER DERSELBE WERT
      DENSELBEN WEG GEHT. Hier lagen sie zwanzig Zeilen auseinander.

      ── WIE ES UEBERLEBEN KONNTE: ein Test, der nichts prueft ───────────
      Im Selbsttest stand fuer genau diesen Fall:
          chk("kuerzere Liste auf der Box -> kein Absturz, nur weniger",
              zeiger_uebernehmen(…, kurz, "/*/pw") in (True, False))
      `in (True, False)` besteht JEDER Rueckgabewert. Die Zeile fragte, ob es
      ABSTUERZT — nicht, WOHIN der Wert ging. Ein gruener Test, der nichts
      behauptet, ist schlimmer als kein Test: er belegt ein Feld, auf dem
      sonst noch jemand nachgesehen haette. Beim Gegenlesen lohnt es sich,
      gezielt nach `in (True, False)`, `is not None` und `>= 0` zu suchen.

      ── WAS GEPRUEFT WURDE UND HAELT ────────────────────────────────────
      Alles Uebrige aus E29/B6 hat gehalten, und zwar gemessen, nicht
      geglaubt (tools/e29b6-adversarisch.py, 96 Punkte an .169):
        * falsches Passwort: bricht ab; der Zielbaum ist danach Byte fuer
          Byte und mtime fuer mtime UNVERAENDERT. Gegenprobe: das richtige
          Passwort oeffnet ihn (ohne die waere der Punkt wertlos).
        * ohne Passwort: Bibliothek, gemerkte Stellen, Kinderzeit und
          Darstellung kommen zurueck; der Bericht nennt jedes verschluesselte
          Feld namentlich und zeigt dabei keinen Wert.
        * Klartextsuche Mitglied fuer Mitglied im Archiv: nichts, mit
          Gegenprobe auf ssid und Rechnername.
        * ein ECHTER Stand vom 04.08. (ohne Behaelter) laesst sich weiter
          einspielen.
        * die woechentliche Probe faellt DURCH bei verbogenem Behaelter,
          verbogener Bibliothek und fehlendem gpg.
        * kein gpg-agent uebrig, kein Wegwerf-GNUPGHOME in /tmp, nichts im
          Journal, nichts in einer Verlaufsdatei der Shell.

      ── UND EIN BEFUND, DER NICHT E29/B6 GEHOERT ────────────────────────
      `/boot/firmware/dietpi-wifi.txt` traegt WIFI_KEY[0] im KLARTEXT (25
      Zeichen, gesetzt). Das ist DietPis eigene Datei, nicht die Sicherung —
      aber es aendert die Lage bei der offenen Frage aus E29/B6 („soll das
      Passwort auf die Karte?"): die Karte ist bereits kein geheimnisfreier
      Ort. GEMESSEN wurde ausserdem, dass dieser Schluessel NICHT derselbe
      ist wie `pw` in server/config/wlan.json — die Suche nach dem einen fand
      den anderen nicht. Welcher der gueltige ist, wurde nicht geprueft.

      ── EINE FALLE BEIM MESSEN SELBST ───────────────────────────────────
      Ein `os.fdopen(0, "r")` im Pruefwerkzeug besitzt fd 0 und schliesst ihn,
      sobald das Objekt faellt. Danach liefert `os.pipe()` im geprueften
      Skript die 0 als niedrigsten freien Zeiger, `--passphrase-fd 0` wird von
      der stdin-Roehre des Kindes ueberschrieben, und JEDES Entschluesseln
      scheitert mit „no valid OpenPGP data found". Der Punkt „ein falsches
      Passwort scheitert" wurde dabei GRUEN, ohne je ein Passwort geprueft zu
      haben. Seitdem: `closefd=False`, eine Kontrolle auf offene 0/1/2, und
      vor jeder Negativprobe die POSITIVE Gegenprobe.
    related:
      - e29b6-zugangsdaten-verschluesselt-und-akkuverlauf-mit
      - sicherung-ausgelassene-schluessel-nie-schreiben
      - sicherung-rueckweg-ueber-die-karte
      - sicherung-probe-woechentlich
  - kind: architektur
    id: bereich-je-profil-ein-ordner-statt-namenszusatz
    title: 'E18 Stufe 2: ein ORDNER je Profil — und die drei Formen, die beim Umzug
      gleichzeitig herumliegen'
    sev: high
    match: Ordner je Profil|Bereich je Profil|profile/<kennung>|bereichUebernehmen|ablageOrt|BEREICH_ABLAGEN|gespielt.gast.json
      umgezogen|E18 Stufe 2 Ablage
    cause: 'Ein Namenszusatz je DATEI (`gespielt.gast.json`) heisst, dass bei jeder
      neuen benutzerbezogenen Ablage jemand daran DENKEN muss. Genau so sind kinderzeit.json
      und listen.json box-weit geblieben.'
    fix: 'Ein Ordner je Profil (`server/config/profile/<kennung>/`), angelegt beim
      Anlegen des Profils; `BEREICH_ABLAGEN` in profile.ts ist die EINE Liste, die
      Umzug, Anlegen und die Deckungspruefung der Sicherung treibt.'
    tags:
      - e18
      - mehrbenutzer
      - profile
      - ablage
      - umzug
      - sicherung
      - werkzeug
      - 2026
    source: 05.08.2026 gebaut und an .169 ausgeliefert; Entscheidung des Betreibers
      vom selben Tag (BACKLOG E18, „Entscheidungen des Betreibers", Punkt 1)
    body: |
      DIE ENTSCHEIDUNG UND IHR GRUND. Bis zum 05.08.2026 trug jede Ablage eines
      Profils einen Namenszusatz: `gespielt.gast.json`,
      `kinderzeit-verbrauch.gast.json`. Das funktioniert — aber es verlangt, dass
      jemand bei JEDER neuen benutzerbezogenen Datei daran denkt. Genau daran hat
      es zweimal gefehlt: `kinderzeit.json` und `listen.json` blieben box-weit.
      Dieselbe Fehlersorte wie [[dienstliste-an-fuenf-orten]]. Ein ORDNER je
      Profil dreht es um: wer eine Ablage in den Bereich legt, bekommt die
      Trennung geschenkt; wer sie danebenlegt, muss es begruenden.

          server/config/profile/<kennung>/gespielt.json
          server/config/profile/<kennung>/kinderzeit-verbrauch.json
          server/config/profile/<kennung>/listen.json

      DER DATEINAME BLEIBT, WAS ER WAR — nur eine Etage tiefer. Wer im
      Verzeichnis nachsieht, sucht `gespielt.json` und findet es.

      ── DREI FORMEN LIEGEN GLEICHZEITIG HERUM ───────────────────────────
      Auf einer Box, die seit Juli laeuft, kann jede Ablage in DREI Formen
      dastehen, und der Umzug muss alle drei kennen:

          gespielt.json               box-weit, vor dem 02.08.2026
          gespielt.gast.json          mit Namenszusatz, ab dem 02.08.2026
          profile/gast/gespielt.json  im Bereich, ab dem 05.08.2026

      REIHENFOLGE BEIM ABHOLEN: erst der Namenszusatz, dann die box-weite Form.
      Der Zusatz ist die JUENGERE Wahrheit. Andersherum schriebe man den Stand
      von vorgestern ueber den von gestern — und beide sehen gueltig aus.
      DIE BOX-WEITE FORM GEHOERT NUR DEM GAST. Ohne Auswahl war alles der Gast;
      duerfte ein anderes Profil sie holen, erbte das erste angelegte Kind den
      Verlauf der ganzen Box.

      ── VIER STELLEN, AN DENEN ES STILL DANEBENGEHT ─────────────────────
      1. EIN VERWEIS DARF NICHT MITZIEHEN. `fs.renameSync` auf einen Symlink
         benennt den VERWEIS um, nicht sein Ziel — er laege danach im Bereich
         und zeigte weiter nach draussen. Geprueft wird mit `lstatSync`, nicht
         mit `existsSync`: auf einem TOTEN Verweis ist `existsSync` FALSCH.
         Die Naht heisst `if (!s.isFile())` — ein `isSymbolicLink()`-Zweig
         DAVOR waere wirkungslos (die Rotprobe hat genau das aufgedeckt: der
         Eingriff blieb gruen, weil `!isFile()` den Verweis ohnehin abfing).
      2. EIN GESCHEITERTER UMZUG DARF NICHTS VERSTECKEN. Laesst sich der Ordner
         nicht anlegen (volle oder schreibgeschuetzte Karte), zeigte die Box
         einen LEEREN Verlauf — ohne Meldung, und gemerkt haette es jemand erst
         Wochen spaeter. Deshalb liest `bereichLesePfad()` mit RUECKFALL: der
         Bereich, wenn er da ist, sonst die alte Stelle. Das ist KEIN zweiter
         Wahrheitsort — geschrieben wird immer nur in den Bereich, und sobald
         dort etwas liegt, wird die alte Stelle nie wieder angesehen.
      3. MITTENDRIN ABBRECHEN MUSS ERLAUBT SEIN. Jede Ablage zieht EINZELN,
         `rename` ist innerhalb eines Dateisystems atomar, und der Lauf ist
         idempotent (er prueft das ZIEL). Ein Abbruch laesst einen Teil
         umgezogen; der naechste Start macht weiter.
      4. NICHTS WIRD WEGGEWORFEN. Liegen beide alten Formen da, zieht die
         juengere um und die aeltere BLEIBT LIEGEN. Ein geloeschtes Profil
         behaelt seinen Ordner — die Verwaltung fragt (noch) nicht nach, und ein
         versehentlich geloeschtes Kind haette sonst alles verloren.

      ── WAS NICHT MITZIEHT, MIT GRUND ───────────────────────────────────
      `kinderzeit.json` ist der REGELSATZ `{standard, je}` und gehoert allen
      zusammen; je Bereich abgelegt faende `regelnFuer()` die Hausregel nicht
      mehr. „Zeitbudget je Kind" steckt IM Satz, nicht im Ordner — und ist im
      Server laengst gebaut (siehe [[ablage-besitz-gemessen-e18-stufe2]]).
      `resume.json` war eine EIGENE Stufe: vier Routen, zwei Sperrdateien in
      /tmp, vier Shell-Skripte und ein SYMLINK (`active_resume.json`) haengen
      daran. UEBERHOLT SEIT DEM 05.08.2026 ABENDS — es ist umgezogen, und der
      alte Ort ist eine BRUECKE geworden: [[resume-bruecke-am-alten-ort]].
      Der Halbsatz „den check_network.sh alle zehn Sekunden neu legt" stand
      hier falsch; er feuert bei jedem NETZWECHSEL, nicht im Takt (gemessen,
      [[resume-wege-gemessen-e18-stufe3]]).

      ── DIE SICHERUNG ZIEHT NICHT VON SELBST MIT — BEHOBEN AM 05.08.2026 ─
      `scripts/mupibox/mupibox-sicherung.py` sammelte FLACH ein (`os.listdir`
      ueber `server/config`, fnmatch gegen eine namentliche Liste). Ein
      UNTERORDNER trifft kein Muster, also fiel der ganze Bereich heraus —
      und gemerkt haette man es erst beim ZURUECKSPIELEN. Das ist die
      schlimmste Reihenfolge, die ein Fehler haben kann: er faellt genau in
      dem Augenblick auf, in dem man sich auf die Sicherung verlaesst.
      GEHOLT WIRD ER JETZT UEBER DREI TEILE:
        * `tiefe_wurzeln()` liest aus HINEIN ab, welche Unterordner betreten
          werden — ein Muster mit `/` sagt WAS gesichert wird UND dass dafuer
          hineingesehen werden muss. Eine zweite Liste „welche Ordner betreten
          wir" waere irgendwann abgewichen. Der `coverspeicher` bleibt damit
          aussen vor, ohne dass es jemand extra verbieten muss.
        * die drei Muster `profile/*/gespielt.json`,
          `profile/*/kinderzeit-verbrauch.json`, `profile/*/listen.json`. Die
          FLACHEN Formen bleiben DANEBEN stehen — eine Box, die noch nicht neu
          gestartet hat, traegt sie noch.
        * `rest_heil()`. In `ziel_von` und in `ziel` stand je ein
          `"/" in rest` — jeder Unterordner verboten, und das WAR die
          Absicherung gegen einen Archiveintrag, der aus dem Baum
          hinausfuehrt. Sie ist nicht weggefallen, sondern genauer geworden
          (jedes STUECK des Pfades) und steht nur noch EINMAL da.
      OHNE DEN DRITTEN TEIL WAERE DER REST WIRKUNGSLOS GEWESEN: das Archiv
      haette den Verlauf getragen und ihn beim Zurueckspielen wortlos
      uebersprungen. Der Selbsttest loescht deshalb WIRKLICH (Liams
      gespielt.json, den ganzen Bereich des Gasts) und sieht danach nach.
      151 Selbsttests (vorher 133). `tools/bereich-sicherung-deckung.py`
      haengt jetzt in `tools/pruefen.sh` — erst seit es gruen ist, denn ein
      Dauerrot erzieht dazu, Rot zu uebersehen
      ([[veraltete-vorschau-meldet-falsches-rot]]).
      NOCH NICHT AUSGEROLLT (Stand 05.08.2026 abends). Die Box .169 laeuft auf
      der alten Fassung, und fuer `scripts/` gibt es in `tools/ausliefern.py`
      keinen Weg — der kennt nur server, player, www, admin. Am Geraet
      nachgemessen mit `tools/bereich-sicherung-am-geraet.py` (rein lesend,
      legt keinen Stand an): installiert 16 Dateien und `server/config/profile`
      als „nicht eingeordnet", Arbeitsbaum 19 samt aller drei Ablagen.

      ── EIN RUECKBAU DES SERVERS DREHT DIE ABLAGE NICHT ZURUECK ─────────
      Wer `server.js` ueber `tools/ausliefern.py --zurueckdrehen` zurueckholt,
      bekommt einen Server, der `gespielt.gast.json` sucht und nicht findet: die
      Datei liegt in `profile/gast/`. Der Verlauf ist NICHT weg, aber die Box
      zeigt ihn nicht. Zurueckdrehen heisst hier: erst die Dateien wieder
      herausbewegen, dann den Dienst. (Die Auslieferung selbst legt vorher einen
      Sicherungsstand an — dort liegt die FLACHE Form noch drin.)
    related:
      - ablage-besitz-gemessen-e18-stufe2
      - resume-bruecke-am-alten-ort
      - dienstliste-an-fuenf-orten
      - speicherschluessel-sechsfach-verstreut
      - bibliothek-verlaesst-den-server-an-25-stellen
      - sicherung-leere-datei-besteht-jede-pruefung
      - rotprobe-war-der-test-ohne-die-aenderung-rot

  - kind: falle
    id: f2-marke-bei-mpv-und-im-bild
    title: "F2 repariert: die Marke war bei mpv NIE da — und bei Spotify nie im Bild. Zwei Fehler, eine Meldung"
    match: "F2 marke repariert|folge-kennung mpv|markeInsBild|marke ard folge|data-spielt fehlt ard|lane rollt zur marke"
    tags: ["oberflaeche", "neu", "lanes", "marke", "F2", "ard", "jellyfin", "mpv", 2026]
    source: "05.08.2026 gebaut und gemessen; NewDesign/app.js, tools/e2e/neu-oberflaeche.test.mjs (2 Faelle), tools/rotprobe.py (5 Eingriffe, alle rot), tools/marke-beim-aufklappen.mjs (neu). Commit 5c6a1db1."
    body: |
      EINE MELDUNG, ZWEI FEHLER — und jeder allein hat diese Aufgabe schon
      einmal zu frueh schliessen lassen. Wer nur einen davon sucht, findet ihn,
      repariert ihn und die Meldung bleibt.

      ══ FEHLER 1: BEI mpv GAB ES DIE MARKE UEBERHAUPT NICHT ════════════════

      [[f2-marke-titelwechsel-je-dienst]] hat es gemessen: eine ARD-Sendung
      zeigt 14 Folgenkacheln, KEINE davon traegt `data-spielt`. Zwei Sperren
      hintereinander, und beide sind richtig begruendet:

        1. `stueckKennung` braucht `t.uri`. AM GERAET NACHGESEHEN (Box .169,
           `/api/werke/<schluessel>/inhalt`, 05.08.2026):
             spotify   nr titel interpret dauerMs bild uri
             ard       nr titel interpret dauerMs bild id befehl anhaengen weiterAb
             jellyfin  nr titel interpret dauerMs      id befehl anhaengen
           Kein `uri` heisst: Kennung leer, Kachel ohne Merkmal, und
           `spieltMarkieren()` sieht sie nicht einmal an
           (`querySelectorAll('[data-spielt]')`).
        2. `laufendeKennungen` setzt `sp` bei mpv-Ton absichtlich auf null.
           Ohne das tanzte die Marke an einem Spotify-Kapitel weiter, waehrend
           ein Hoerbuch laeuft ([[spotify-nachlauf-muster]]) — das ist der
           GRUND, nicht der Fehler.

      DIE ANTWORT IST EINE DRITTE FORM DER KENNUNG, neben `stueck|…` und
      `werk|…` aus [[spielt-marke-eine-frage-zwei-formen]]:

          folge|<schluessel>|<id>

      Sie zaehlt ueber die WARTESCHLANGE, weil mpv nichts anderes hergibt —
      genau der Weg, den das Cover schon geht
      ([[cover-folgt-der-position-nicht-dem-start]]). `folgeKennung()` baut sie
      an beiden Enden: an der Kachel (`titelListeBauen`) und beim Laufenden
      (`laufendeKennungen`).

      `laufendeFolgeJetzt(np)` ist die EINE Stelle, die sagt, welcher Eintrag
      der gemerkten Liste gerade toent. Vier Glieder, und nur das vierte kann
      nein sagen:

          np.art === 'lokal'      sonst gilt gar keine Warteschlange
          laufendeFolgen          ueberhaupt eine Liste da?
          Schluesselvergleich     KANN NICHT NEIN SAGEN — beide Seiten stammen
                                  aus derselben Zuweisung in `albumSpielen`
                                  ([[vergleich-aus-derselben-zuweisung]])
          passtZuLaufendem        der einzige echte Waechter

      DIESE BEDINGUNG STAND SCHON ZWEIMAL AUSGESCHRIEBEN DA (`coverAdresse`
      und `titelAnzeige`). Sie sind NICHT mitgezogen worden, und das ist eine
      offene Schuld statt eines Versehens: beide gehoeren zu F1/E32, und drei
      Eingriffe in tools/rotprobe.py zitieren ihren WORTLAUT
      (`cover-fragt-die-position`, `cover-laesst-fremdes-los`,
      `cover-an-der-position`). Wer sie zusammenlegt, macht eine eigene
      Aenderung mit eigener Messung — die Funktion, die sie beide ersetzt,
      liegt fertig daneben.

      ══ FEHLER 2: BEI SPOTIFY WANDERTE SIE RICHTIG, MAN SAH SIE NUR NIE ════

      Das ist [[marke-zeichen-steht-nie-im-bild]], und die Reparatur heisst
      `markeInsBild(reihe)`: beim AUFGEHEN einer Lane wird die markierte Kachel
      waagerecht herangerollt (`scrollBy`, nicht `scrollIntoView` — letzteres
      rollt die Vorfahren mit und macht die Arbeit von `laneInsBild` ein
      zweites Mal, anders).

      GEMESSEN (tools/marke-beim-aufklappen.mjs, 800x480, 05.08.2026):
        ARD, Folge 7 von 14 laeuft      Kachel bei 669,244 — Zeichen SICHTBAR
        Spotify, Kapitel 7 von 10       Kachel bei 655,244 — Zeichen SICHTBAR
      Vorher stand das Zeichen bei 816,250 bzw. 924,250, also neben dem Schirm.

      NUR BEIM AUFGEHEN, UND DAS IST ABSICHT. Eine Reihe, die von selbst
      wegrollt, waehrend ein Kind sie ansieht, ist derselbe Fehler wie ein
      Knopf, der unter dem Finger wegfaehrt. Beim Aufgehen ist der Fall
      eindeutig: der Benutzer hat gerade selbst getippt.

      ══ WAS DAMIT NICHT ERLEDIGT IST — ALLES DREI GEHOERT DEM BETREIBER ════

        * Bleibt eine Lane offen und mpv rueckt weiter, laeuft die Marke wieder
          hinaus. GEMESSEN: nach einem Titelwechsel fehlen 104 px. Soll die
          Reihe der Marke auch DANN nachrollen?
        * Ueber die Albumgrenze sitzt die Marke eine Ebene hoeher auf der
          Albumkachel, und die Reihe darueber ist SENKRECHT angeschnitten
          (`.buehne`, nicht die Lane). Ein selbsttaetiges senkrechtes
          Nachrollen zoege dem Benutzer die ganze Seite weg.
        * Ein Jellyfin- und ein Spotify-Album klappen im Raster GAR NICHT auf
          (app.js `kachelBauen`). Dort kann keine Marke auf Titelebene sitzen —
          eine Navigationsentscheidung, kein Fehler.

      ══ EIN WERKZEUG HAT SICH DABEI ALS FALSCH HERAUSGESTELLT ══════════════

      tools/marke-sichtbar-nach-albumwechsel.mjs tastete die Marke mit
      `elementFromPoint` ab, um „verdeckt" zu erkennen. Die Marke traegt aber
      `pointer-events: none` (damit ein Tipp auf sie die KACHEL ausloest) — sie
      kann damit NIE ihre eigene Antwort sein. Solange sie ausserhalb des
      Bildes stand, fiel das nie auf; sobald das Nachrollen sie hereinholte,
      meldete das Werkzeug prompt „verdeckt von .IMG". Ein Werkzeug, das JEDE
      sichtbare Marke als verdeckt meldet, beweist nichts mehr. Es macht die
      Durchreiche jetzt fuer die Dauer des Tastens zu und danach wieder auf.

      NACHMESSEN:
        node tools/marke-beim-aufklappen.mjs --pruefen --bild /tmp/auf
        node tools/marke-titelwechsel-je-dienst.mjs --pruefen
        node --test --test-name-pattern 'ARD-Folge|ins Bild gerollt' tools/e2e/neu-oberflaeche.test.mjs
        python3 tools/rotprobe.py --nur f2-
    related:
      - marke-zeichen-steht-nie-im-bild
      - f2-marke-titelwechsel-je-dienst
      - marke-verlaesst-die-offene-titelliste
      - spielt-marke-eine-frage-zwei-formen
      - vergleich-aus-derselben-zuweisung
      - cover-folgt-der-position-nicht-dem-start
      - spotify-nachlauf-muster
      - rotprobe-war-der-test-ohne-die-aenderung-rot

  - kind: entscheidung
    id: griff-66-statt-64
    title: "E8/T2 gebaut: `--griff` von 64 auf 66 px — 95 zu kleine Ziele werden 21, und was die 21 kosten"
    match: "griff 66|beruehrziele repariert|64 px zu klein|9241-411 64,3 px|zurueck ueberdeckt wlan|tor-taste hoehe"
    tags: ["oberflaeche", "neu", "beruehrziele", "e8", "css", "entscheidung", 2026]
    source: "05.08.2026 gebaut und getastet; NewDesign/app.css, gemessen mit tools/beruehrziele-neu.mjs und tools/zurueck-luft-messen.mjs (neu). Commit 5c6a1db1."
    body: |
      DIE EINE ZAHL, UM DIE ES GEHT:

          0,14 mm je Bildpunkt      9 mm (ISO 9241-411) = 64,29 px
          64 px = 8,96 mm           66 px = 9,24 mm

      Der Kopf von app.css schrieb „64,3 -> aufgerundet 64 Bildpunkte". Das war
      ABGERUNDET, und daran hingen 74 der 95 zu kleinen Ziele
      ([[beruehrziele-eingeteilt-74-von-95-sind-ein-pixel]]). `--griff` steht
      jetzt auf 66.

      66 UND NICHT 65: 65 px waeren 9,10 mm und reichten rechnerisch. Die
      0,14 mm/px sind aber selbst gerundet — 800x480 auf 5" Diagonale sind
      0,136 mm/px, und danach waeren 9 mm erst 66,1 px. Zwei Bildpunkte kosten
      hier nichts und machen die Zahl gegen beide Rechnungen belastbar.

      ══ GETASTET, VORHER UND NACHHER (tools/beruehrziele-neu.mjs, 10 Schirme)

          zu klein          95 von 163   ->   21 von 163
          erreichbare Ziele        163   ->          163   (unveraendert)
          nicht treffbar     je Schirm   ->    unveraendert — NICHTS ist durch
                                              die Vergroesserung verdeckt worden

      DIE ZWEITE ZEILE IST DIE WICHTIGE. Ein groesserer Knopf kann einen
      Nachbarn UEBERDECKEN, und dann wird keine Zahl kleiner: das Ziel meldet
      weiter seine volle Groesse und ist trotzdem nicht zu treffen
      ([[rueckweg-verdeckt-die-ueberschrift]]).

      ══ DER EIN-PIXEL-ABSTAND WAR DER GRUND, WARUM DIE 2 PX NICHT ALLEIN
         KOMMEN DURFTEN ══════════════════════════════════════════════════════

      `#zurueck` liegt `position: fixed` bei `top: 0` und ist `--griff` hoch —
      er endet immer genau dort, wo die Kopfzeile des Eltern-Bereichs endet.
      Die erste Taste des Faechers („WLAN") begann an derselben Kante:
      GEMESSEN 0,00 mm (tools/zurueck-luft-messen.mjs; beruehrziele-neu.mjs sah
      dieselbe Stelle als 0,14 mm — ein Bildpunkt). Mit 66 px haette der Knopf
      sie UEBERDECKT.

      Ein Fehlgriff dort verlaesst nicht den Eltern-Bereich, sondern OEFFNET
      DIE ALTE OBERFLAECHE — ein voller Seitenwechsel. Die Luft steht jetzt an
      `.eltern-kopf` als `calc(var(--griff) + 16px)` und folgt der Variablen:
      0,00 mm -> 2,24 mm.

      ══ `.tor-taste`: DIE EINSCHAETZUNG VOM 05.08. WAR ZU VORSICHTIG ════════

      [[beruehrziele-eingeteilt-74-von-95-sind-ein-pixel]] sagte: „ACHTUNG, die
      Luecke betraegt 9 px. 64->66 liesse 7 px (0,98 mm) und damit UNTER 2 mm".
      Das stimmt fuer die BREITE — und die Breite ist 84 px (11,8 mm) und war
      nie das Problem. Zu klein war die HOEHE. Und eine Rasterluecke schrumpft
      nicht, wenn ihre Zellen wachsen. NACHGEMESSEN: engster Abstand vorher
      1,26 mm, nachher 1,26 mm; elf Tasten mehr ueber der Marke.
      DIE LEHRE: Groesse und Abstand getrennt nachrechnen, und dabei die
      SCHMALERE Seite ansehen. Aus Vorsicht vor einem Abstand, der sich gar
      nicht aendert, waeren elf Tasten zu klein geblieben.

      ══ WAS BLEIBT — 21 BEFUNDE AUF VIER BEDIENELEMENTEN, ALLE OFFEN ════════

        15x  3,50–6,16 mm  .tipp-spiel (44 px) — der Play-Knopf auf JEDER
             Kachel. Kosten: 21 px. Auf einer 96-px-Titelkachel waeren das 68 %
             ihrer Breite; er verdeckte Bild, Folgennummer und stiesse an die
             Dienst-Plakette. KEIN Groessenschrauben, ein Umbau der Kachel.
         1x  4,76 mm  #mp-laut-regler — das Fenster muesste von 58 auf ~90 px.
         1x  6,30 mm  #gr-schieber — darueber #gr-unter, darunter
             .gross-tasten; einer muss weichen.
         4x  1,40 mm  .leute-kachel als 92x10-Reststreifen unter dem
             Laut-Fenster. Kein Ziel, ein Fehlgriff-Risiko — und die OFFENE
             FRAGE DES BETREIBERS („soll das Laut-Fenster Tipps dahinter
             abfangen?") ist unveraendert unbeantwortet.

      Jeder dieser vier kostet Platz, den ein ANDERES Bedienelement hergeben
      muesste. Das ist eine Frage der Anordnung, keine der Groesse.

      ══ DREI NACKTE 64 IN RECHNUNGEN — EINE WAR SCHON FALSCH ═══════════════

      Der Einzug der Eltern-Kopfzeile stand als
      `calc(var(--zurueck-links) + 60px)` da, wobei die 60 aus „64 + 12 - 16"
      kamen. Mit `--griff: 66` waere die Ueberschrift wieder 2 px unter den
      Knopf gerutscht. Ebenso in tools/rueckweg-schau.mjs: dort stand die 64
      als Grenze fuer Knopfgroesse UND Kopfzeilenhoehe, und das Werkzeug
      meldete nach der Aenderung eine Kopfzeile als zu hoch, die genau richtig
      war. Beide holen die Zahl jetzt aus `--griff` — das Werkzeug liest sie
      zur Laufzeit aus der Seite.

      NACHMESSEN:
        node tools/beruehrziele-neu.mjs            # Gesamtzahl
        node tools/zurueck-luft-messen.mjs --pruefen
        node tools/rueckweg-schau.mjs --pruefen
    related:
      - beruehrziele-neue-oberflaeche-gemessen
      - beruehrziele-eingeteilt-74-von-95-sind-ein-pixel
      - rueckweg-verdeckt-die-ueberschrift
      - f6-zwei-knoepfe-nicht-aufs-cover

  - kind: falle
    id: eigene-hunks-vom-nachbarn-mitgenommen
    title: "Die andere Richtung der Amend-Falle: `git add <datei>` des Nachbarn nimmt DEINE Hunks mit"
    match: "hunk fremd commit|git add nimmt fremde aenderung|eigene-hunks vergessen|meine aenderung in fremdem commit"
    tags: ["git", "zusammenarbeit", "falle", "werkzeug", 2026]
    source: "05.08.2026 passiert: fuenf F2-Eingriffe in tools/rotprobe.py landeten im Commit 2d1cc2bf, der von E29/B6 handelt"
    body: |
      [[amend-am-gemeinsamen-baum-frisst-fremde-commits]] beschreibt den Fall,
      in dem MAN SELBST fremde Arbeit ueberschreibt. Es gibt ihn auch
      andersherum, und er ist stiller: Man legt seine Aenderung in eine Datei,
      an der ein NACHBAR arbeitet, und der schreibt sie mit `git add <datei>`
      in SEINEN Commit — unter einer Nachricht, die von ihr nichts weiss.

      WAS PASSIERT IST: Fuenf Eingriffe zu BACKLOG F2 wurden in
      tools/rotprobe.py angehaengt (ans Ende, sauber getrennt von den
      E18-Eintraegen darueber). Zwei Commits spaeter standen sie in
      `2d1cc2bf sicherung(E29/B6): die Zugangsdaten verschluesselt mitnehmen —
      und die Messreihe`. Verloren ist nichts, alles laeuft; die Historie sagt
      nur etwas Falsches darueber, warum diese Zeilen da sind.

      NICHT REPARIEREN. Ein `git rebase` oder `--amend` an einem fremden Commit
      ist genau die Falle nebenan, und die kostet mehr als eine schiefe
      Nachricht.

      WAS STATT DESSEN HILFT — und es ist unangenehm, weil es Arbeit VORHER
      ist:

        * ANGEHAENGTES SOFORT FESTSCHREIBEN. tools/rotprobe.py, pack.yaml und
          tools/pruefen.sh sind Sammeldateien; wer dort etwas anhaengt und es
          eine Stunde liegen laesst, verschenkt es. Mit
          `python3 tools/eigene-hunks.py <datei> <zeilen>` und einem eigenen
          Commit dauert das eine Minute.
        * WER SELBST `git add <datei>` TIPPT, sieht vorher in `git diff` hinein.
          `git add -i`/`-p` gibt es hier nicht — dafuer ist eigene-hunks.py da.
        * DIE HUNK-NUMMERN GELTEN NUR, SOLANGE HEAD STILLSTEHT. An diesem Baum
          bewegt sich HEAD alle paar Minuten; zwischen `--zeigen` und dem
          Commit darf nichts dazwischen.

      NEBENBEFUND AUS DEMSELBEN TAG: pack.yaml traegt dieselbe Gefahr. Zwei
      Vorgaenge, die gleichzeitig anhaengen, landen im selben Diff-Hunk, und
      dann kann eigene-hunks.py sie nicht mehr trennen. Wer anhaengt, schreibt
      moeglichst bald fest.
    related:
      - amend-am-gemeinsamen-baum-frisst-fremde-commits
      - rotprobe-war-der-test-ohne-die-aenderung-rot

  - kind: falle
    id: git-checkout-datei-nimmt-den-index
    title: "`git checkout <datei>` holt den INDEX, nicht HEAD — und loescht damit die ungespeicherte Arbeit der Nachbarn"
    match: "git checkout datei index|pack.yaml eintraege weg|eigene-hunks danach checkout|ungespeicherte aenderung verloren"
    tags: ["git", "zusammenarbeit", "falle", "wiki", 2026]
    source: "05.08.2026 selbst passiert und wiederhergestellt: vier fremde pack.yaml-Eintraege waren weg, nur eine Sicherung im gemeinsamen Zwischenspeicher hat sie gerettet"
    body: |
      DIE ZWEI ZEILEN, DIE ES ANRICHTEN, sehen beide harmlos aus:

          python3 tools/eigene-hunks.py pack.yaml 27752   # nur MEINEN Hunk
          git checkout pack.yaml                          # „aufraeumen"

      `git checkout <datei>` stellt die Datei aus dem INDEX her, nicht aus HEAD.
      Nach `eigene-hunks.py` steht im Index „HEAD + mein Hunk". Die zweite Zeile
      schreibt also genau das in den Arbeitsbaum — und alles, was DORT noch
      stand und niemandem gehoerte, der gerade committet hat, ist weg. Ohne
      Rueckfrage, ohne Meldung, ohne Reflog: ungespeicherte, nie eingelagerte
      Aenderungen liegen in KEINEM Git-Objekt.

      WAS ES GEKOSTET HAT: vier fertige pack.yaml-Eintraege aus zwei fruehreren
      Sitzungen ([[f2-marke-titelwechsel-je-dienst]],
      [[marke-zeichen-steht-nie-im-bild]],
      [[beruehrziele-eingeteilt-74-von-95-sind-ein-pixel]],
      [[amend-am-gemeinsamen-baum-frisst-fremde-commits]]) und einen fuenften
      aus einer dritten ([[bibliothek-verlaesst-den-server-an-25-stellen]]).
      Beide Vorgaenger hatten sie ausdruecklich NICHT committet, weil sie im
      selben Diff-Hunk lagen wie fremde Arbeit — die Vorsicht, die sie schuetzen
      sollte, hat sie ungeschuetzt liegen lassen.

      ══ WIEDERHERGESTELLT, UND WORAUS ══════════════════════════════════════
      Nicht aus Git. Aus einer Sicherung, die eine ANDERE Sitzung im
      gemeinsamen Zwischenspeicher abgelegt hatte
      (`…/scratchpad/pack.vorher.yaml`, 14:28 Uhr). Die fuenf Rohbloecke wurden
      daraus herausgeschnitten und HINTER IHREM URSPRUENGLICHEN VORGAENGER
      wieder eingesetzt, damit die Reihenfolge bleibt; danach Zeichen fuer
      Zeichen gegen die Sicherung verglichen (`yaml.safe_load`, Gleichheit je
      Eintrag) und `tools/wiki-schau.py` gruen. Dass es diese Sicherung gab,
      war Glueck und kein Verfahren.

      ══ DIE REGELN, DIE DARAUS FOLGEN ══════════════════════════════════════
        * `git checkout <datei>` und `git restore <datei>` an einem gemeinsamen
          Baum GAR NICHT. Wer den Index leeren will, nimmt `git reset <datei>`
          — das laesst den Arbeitsbaum in Ruhe.
        * VORHER EINE KOPIE. Eine Zeile (`cp pack.yaml <zwischenspeicher>/`)
          kostet nichts und ist der einzige Rueckweg, den es gibt.
        * ANGEHAENGTES SOFORT FESTSCHREIBEN, auch wenn es im Hunk eines anderen
          liegt — siehe [[eigene-hunks-vom-nachbarn-mitgenommen]]. Eine schiefe
          Commit-Nachricht ist ein kleinerer Schaden als eine geloeschte
          Messung.
    related:
      - eigene-hunks-vom-nachbarn-mitgenommen
      - amend-am-gemeinsamen-baum-frisst-fremde-commits

  - kind: falle
    id: bereich-gegengelesen-was-noch-box-weit-ist
    title: E18 Stufe 2 adversarisch gegengelesen — der Umzug verliert nichts, aber drei Dinge stehen offen
    sev: med
    match: weiterhoeren fremdes Profil|resume.json box-weit Profil|Sicherung zurueckgespielt Bereich|profil= 200 400|haeufigste 20 statt 31
    cause: >-
      Zwischen Stufe 2 (Verlauf, Listen, Kinderzeit-Konto je Kind) und Stufe 3
      (resume.json) liegt ein Zustand, in dem die einen Dinge getrennt sind und
      die anderen nicht. Ausgeliefert ist dieser Zustand seit dem 05.08.2026.
    fix: >-
      Den Zustand messen statt ihn zu vermuten:
      tools/profiltrennung-lecksuche.ts (haengt in pruefen.sh) legt je Route
      fest, was herauskommen DARF — auch das, was heute noch box-weit ist.
    tags: [e18, mehrbenutzer, profile, resume, weiterhoeren, sicherung, gegenlesen, 2026]
    source: >-
      05.08.2026, adversarisches Gegenlesen von E18 Stufe 2; gemessen an einer
      Kopie des echten Bestands der Box .169 und an der laufenden Box selbst
    body: |
      WAS GEHALTEN HAT. Der Umzug am ECHTEN Bestand der Box (nicht an einer
      selbstgeschriebenen Ausgangslage): 31 Verlaufseintraege vorher, 31
      nachher, jeder mit derselben Zahl — auf der Platte UND ueber die
      Schnittstelle gezaehlt. 25 Medien sichtbar. Beide Verweise
      (`active_data.json`, `active_resume.json`) noch Verweise und noch auf
      eine Datei zeigend. Keine Datei ausser den drei Ablagen hat das
      Verzeichnis verlassen. Ein unvollstaendiger Umzug wird beim naechsten
      Lauf fortgesetzt, ohne das schon Umgezogene anzufassen (nicht einmal der
      Zeitstempel aendert sich). Werkzeug: `tools/bereich-echtbestand-probe.ts`.

      ══ 1. „WEITERHOEREN" ZEIGT FREMDE STELLEN ═══════════════════════════
      `resume.json` ist box-weit (Stufe 3). `weiterhoerbare()` nimmt JEDE
      Stelle auf; der Verlauf des aktiven Profils bestimmt nur die
      REIHENFOLGE (`zeiten.get(m.key) ?? 0`), nicht die Auswahl. Liam sieht
      deshalb mit Titel, Bild und Fortsetzen-Knopf, wo Kalea stehengeblieben
      ist — hinten in der Reihe, aber er sieht es. `/api/resume` und
      `/api/activeresume` kennen ueberhaupt kein Profil.
      DAS IST KEIN FEHLER DER AUSLIEFERUNG, sondern ihr Zwischenstand. Es
      steht hier, weil ein Zwischenstand nur auszuhalten ist, wenn er gemessen
      ist. Wird die betreffende Zeile in
      `tools/profiltrennung-lecksuche.ts` eines Tages rot, ist Stufe 3 gebaut.
      WAS DICHT IST, ebenfalls gemessen: Verlauf, eigene Listen,
      Kinderzeit-Konto. Und der Besitzer kommt nicht vom Client — eine
      Meldung, die `profil: kalea` BEHAUPTET, landet bei dem, der dran ist.
      ZUWEISUNG VON MEDIEN AN EIN KIND GIBT ES NICHT (Stufe 4 ist nicht
      gebaut); die Bibliothek gehoert der Box, so entschieden in
      [[mixpi-die-geschichte]]. „Medien, die ihm nicht zugewiesen sind" kann
      es heute also gar nicht geben — die Frage lautet richtig: „Dinge, die
      einem ANDEREN Kind gehoeren".

      ══ 2. EINE ZURUECKGESPIELTE SICHERUNG IN ALTER FORM WIRKT NICHT ═════
      Ein Betreiber spielt eine Sicherung von VOR dem Umzug zurueck. Sie
      bringt `gespielt.<kennung>.json` mit. Im Bereich liegt schon etwas.
      `bereichUebernehmen` sieht das Ziel und ruehrt nichts an — genau die
      Idempotenz, die sie haben MUSS —, und gelesen wird ebenfalls der
      Bereich. Die zurueckgespielte Datei liegt da und WIRKT NICHT, ohne dass
      irgendetwas es sagt. Der Betreiber sieht den Stand von JETZT und haelt
      ihn fuer den wiederhergestellten.
      DER WEG DAGEGEN IST DIE SICHERUNG SELBST (sie zieht seit dem 05.08.2026
      den Bereich mit, siehe
      [[bereich-je-profil-ein-ordner-statt-namenszusatz]]), NICHT ein Vorrang
      der alten Form — der weichte die Idempotenz auf, und die ist auf dieser
      Box nicht verhandelbar ([[mupi-two-node-processes]]).
      SOLANGE DIE NEUE SICHERUNG NICHT AUSGEROLLT IST, gilt: erst die Dateien
      von Hand in den Bereich bewegen, dann den Dienst starten.

      ══ 3. ZWEI STELLEN, ZWEI ANTWORTEN AUF „GIBT ES DIESES PROFIL?" ═════
      Im selben Quelltext, an der Box nachgemessen:
          GET /api/gespielt?profil=gibtesnicht        -> 200, leere Liste
          GET /api/kinderzeit/stand?profil=gibtesnicht -> 400
      Der Verlauf prueft `kennungPruefen(k)` — nur die FORM. Die Kinderzeit
      prueft `profilStand.profile.some(...)` — die MITGLIEDSCHAFT.
      ES GEHT DABEI NICHTS VERLOREN und es entsteht kein Ordner fuer ein
      Phantomprofil (`bereichSchreibPfad` wird nie mit einer ungeprueften
      Kennung gerufen — an der Box nachgesehen, es blieb bei einem Ordner).
      Was entsteht, ist eine Verwaltungsseite, die einen TIPPFEHLER wie „noch
      nichts gehoert" aussehen laesst.
      NICHT REPARIERT, WEIL DIE RICHTUNG DEM BETREIBER GEHOERT: streng heisst,
      dass der Verlauf eines GELOESCHTEN Profils — dessen Ordner absichtlich
      stehenbleibt — nicht mehr ansehbar ist. Lose heisst, dass Tippfehler
      still bleiben. Beides ist vertretbar, aber es muss EINES sein.

      ══ WAS DER RUECKFALL NOCH TUT, und im Quelltext nicht steht ═════════
      `bereichLesePfad()` faellt auf die alte Stelle zurueck — dokumentiert
      ist der Grund „falls der Umzug nicht gelingen konnte". Es gibt einen
      ZWEITEN: `gespieltStaende` und `kzKonten` lesen je Kennung EINMAL von
      der Platte. Ein Umzug NACH dem ersten Lesen (`bereicheHerrichten()`
      laeuft auch nach jedem `PUT /api/profile`) koennte deshalb im
      Zwischenspeicher verschwinden. Tut er nicht — weil schon der ERSTE
      Zugriff ueber den Rueckfall denselben Inhalt sieht, den der Umzug
      spaeter nur verschiebt. Wer den Rueckfall eines Tages fuer
      ueberfluessig haelt, weil „der Umzug ja laengst durch ist", nimmt diese
      zweite Wirkung mit weg. Gemessen in
      `tools/bereich-umzug-nach-dem-lesen.ts`, und der Lauf bleibt dafuer
      stehen.

      ══ ZWEI FALLEN FUER DEN NAECHSTEN, DER NACHMISST ════════════════════
        * `/api/gespielt` liefert ZWEI Listen. `haeufigste` siebt alles mit
          `anzahl < 2` heraus („was einmal lief, ist nicht oft gehoert"). Auf
          der Box .169 sind das 11 von 31 — wer `haeufigste` zaehlt, meldet
          einen Verlust, den es nicht gibt. Ungesiebt ist `zuletzt`.
        * `/api/werke` antwortet mit einem OBJEKT `{stand, werke}`, nicht mit
          einer Liste. `len()` darauf ergibt 2 und sieht aus wie eine fast
          leere Bibliothek. Beides ist beim Gegenlesen passiert.
        * Eine Attrappe fuer `resume.json` MUSS die echten Feldnamen tragen
          (`resumespotifytrack_number`, `resumespotifyprogress_ms`, …).
          Erfundene (`progress`, `tracknr`) ergeben Stellen, die gar nicht
          weiterhoerbar sind — und dann meldet der Lauf Dichtheit, wo keine
          ist ([[neue-oberflaeche-ohne-box-ansehen]]).
    related:
      - bereich-je-profil-ein-ordner-statt-namenszusatz
      - mehrbenutzer-vorbereitung
      - weiterhoeren-reihe-neue-oberflaeche
      - rotprobe-war-der-test-ohne-die-aenderung-rot
      - neue-oberflaeche-ohne-box-ansehen

  - kind: offen
    id: marke-haengt-an-der-sitzung-die-startete
    title: "F2 gegengelesen: die mpv-Marke gibt es nur in der Sitzung, die den Ton gestartet hat"
    match: "marke nach neuladen weg|laufendeFolgen null|folge-kennung ohne start|marke ohne eigenen start|kiosk neu geladen marke"
    tags: ["oberflaeche", "neu", "marke", "lanes", "F2", "ard", "mpv", "offen", 2026]
    source: "06.08.2026, tools/marke-ohne-eigenen-start.mjs (neu) gegen tools/neu-vorschau.mjs, 800x480; die Feldfrage zusaetzlich an der Box .169 gegengeprueft. NICHT repariert."
    body: |
      [[f2-marke-bei-mpv-und-im-bild]] hat die Marke auf die mpv-Folgenkachel
      gebracht, und sie sitzt dort auch — SOLANGE DIESELBE SEITE DEN TON
      GESTARTET HAT. Die dritte Kennungsform `folge|<schluessel>|<id>` zaehlt
      ueber `laufendeFolgen`, und diese Liste legt `albumSpielen` beim Start ab.
      Sie steht NUR IM ARBEITSSPEICHER.

      GEMESSEN (`node tools/marke-ohne-eigenen-start.mjs`):

        Fall                                 Kacheln  mit data-spielt  markiert
        1 eigener Start (Play-Knopf Raster)     14          14            1
        2 nach dem Neuladen der Seite           14          14            0
        3 Folgenkachel angetippt (ardSendungAb) 14          14            1

      FALL 2 IST DER BEFUND, und die Zahlen daneben sagen genau, wo er sitzt:
      Der TRAEGER ist vollstaendig da (14 von 14 Kacheln tragen `data-spielt` —
      der kommt aus der Antwort des Servers und ueberlebt jedes Neuladen). Es
      gibt nur nichts mehr, wogegen er treffen koennte. Die RASTER-Marke steht
      weiter richtig; sie haengt an `laufendesWerk()` und findet ihr Werk auch
      ohne Liste wieder.

      DAS IST KEIN RANDFALL AUF DIESEM GERAET:
        * Der Kiosk-Browser laedt beim Einschalten neu, waehrend mpv
          weiterspielt — die Wiedergabe ueberlebt den Neustart der Oberflaeche.
        * Startet jemand vom Telefon oder aus der klassischen Oberflaeche, hat
          diese Seite nie eine Liste angelegt.
        * `starte()` (Ein-Befehl-Start) setzt `laufendeFolgen` ausdruecklich auf
          null — richtig begruendet, siehe dort.

      DIESELBE FEHLERKLASSE WIE [[flip-liste-nach-neuladen-ohne-bezug]]: eine
      Auskunft, die nur im Speicher steht, ist nach dem naechsten Laden weg —
      und die Oberflaeche sieht danach genauso aus wie VOR der Reparatur.

      NICHT REPARIERT, WEIL ES ETWAS KOSTET UND DIE WAHL DEM BETREIBER GEHOERT:
      Der Weg waere, `laufendeFolgen` nachzuholen, wenn bei mpv-Ton ein Werk
      erkannt ist und keine Liste vorliegt — ein zusaetzliches
      `/api/werke/<s>/inhalt` je Neuladen. Bei der ARD ist dieser Abruf NICHT
      billig: er loest dreissig Tonadressen frisch auf (die laufen ab, siehe
      `ardSendungAb`). Die Frage lautet also nicht „soll die Marke da sein",
      sondern „darf das Neuladen der Oberflaeche die ARD anfragen".

      WAS BEIM COVER ANDERS IST: `coverAdresse` faellt bei fehlender Liste auf
      das Werkbild zurueck. Die Marke hat keinen Rueckfall — sie ist dann eben
      nicht da. Das ist richtig so (eine falsche Marke waere schlimmer), aber es
      erklaert, warum derselbe Mangel beim Cover nie auffiel.

      ── DIE FELDFRAGE, AM GERAET NACHGESEHEN (Box .169, 06.08.2026) ──────
      `/api/werke/ard:69106028/inhalt`: 30 Folgen, Felder wie am 05.08.,
      `id` ueberall gesetzt, NULL leer, NULL doppelt, `uri` 0-mal. Die Kennung
      `folge|ard:69106028|16805831` ist am echten Bestand also wohlgeformt und
      eindeutig.

      EINE ZWEITE ABHAENGIGKEIT, DIE MAN IHR NICHT ANSIEHT: `laufendeFolgeJetzt`
      fragt `passtZuLaufendem(w, np)`, und das sucht Werktitel ODER Interpret in
      `np.titel + np.unter`. Bei mpv ist `np.unter` = `currentMeta.album`, und
      das ist NICHT das Album, sondern der INTERPRET aus dem Befehl
      (spotify-control.ts, Zweige `ard`/`jellyfin`: `parts[1]` aus
      `<Titel>:title:artist:<Interpret>`). Auf .169 traegt die einzige
      ARD-Sendung „Die Maus" als Interpret, der Folgentitel lautet „Mit dem Rad"
      — die Wache haengt dort ALLEIN am Interpreten. Ein ARD- oder
      Jellyfin-Eintrag OHNE Interpret in data.json bekaeme also weder Marke noch
      Cover noch Folgennamen, und zwar still. Nicht gemessen, weil es auf dieser
      Box keinen solchen Eintrag gibt.
    related:
      - f2-marke-bei-mpv-und-im-bild
      - f2-marke-titelwechsel-je-dienst
      - flip-liste-nach-neuladen-ohne-bezug
      - cover-folgt-der-position-nicht-dem-start
      - vergleich-aus-derselben-zuweisung
      - titelname-wird-nachgeschlagen-wie-das-cover

  - kind: knowhow
    id: verdeckung-messen-werkzeug
    title: "tools/verdeckung-messen.mjs — wer liegt auf wem, in Bildpunkten (und warum die Zahl „nicht treffbar“ das nicht beantwortet)"
    match: "ueberdeckung messen|verdeckt wen|verdeckung-messen|ueberlappung neue oberflaeche|bemalte flaeche object-fit"
    tags: ["oberflaeche", "neu", "beruehrziele", "werkzeug", "messen", "e8", "cover", 2026]
    source: "06.08.2026 gebaut und gelaufen; tools/verdeckung-messen.mjs, neun Schirme, 800x480, alter Baum (5c6a1db1^) gegen neuen (5c6a1db1) in zwei git-worktrees."
    body: |
      DIE LUECKE, DIE ES SCHLIESST: [[griff-66-statt-64]] belegt „nichts ist
      durch die Vergroesserung verdeckt worden" mit der Zahl „nicht treffbar:
      unveraendert" aus tools/beruehrziele-neu.mjs. DIESE ZAHL KANN DAS NICHT
      BELEGEN. Sie kennt nur die VOLLSTAENDIGE Ueberdeckung — ein Ziel, das an
      keinem Punkt mehr zu treffen ist. Ein Knopf, der zu einem Drittel unter
      einem anderen liegt, meldet unveraendert seine volle Groesse und einen
      Treffer in der Mitte. Die Aussage war richtig; der Beleg trug sie nicht.

      GEZAEHLT WIRD PAARWEISE DIE SCHNITTFLAECHE, ueber drei Sorten Dinge statt
      nur ueber Knoepfe — ZIEL, BILD, WORT. Die dritte ist die Lehre aus
      [[rueckweg-verdeckt-die-ueberschrift]]: eine Ueberschrift ist kein
      Bedienelement und kann beliebig verdeckt werden, ohne dass eine einzige
      Trefferflaeche schrumpft.

      ══ DREI DINGE, DIE DER ERSTE LAUF FALSCH MACHTE ═══════════════════════
        * NUR, WAS VORN LIEGT. Steht der grosse Player offen, bleibt die Buehne
          dahinter im Baum und ist nach jeder Stilangabe „sichtbar": 577 000 px²
          auf dem Cover-Vollbild, alles hinter derselben Ebene. Mit der Frage
          „ist dieses Ding an irgendeinem Punkt das OBERSTE?" bleiben dort 0 px²
          — und die sind der Befund.
        * BEMALTE FLAECHE STATT KASTEN (`object-fit: contain`), dieselbe
          Rechnung wie in tools/album-gross-schau.mjs.
        * EIN VORFAHRE ZAEHLT ALS TREFFER: ein Bild in einem Knopf gibt bei
          `elementFromPoint` den Knopf zurueck und ist trotzdem zu sehen.

      Was es NICHT liest, ist Deckkraft: ein durchsichtiger Verlauf ueber einem
      Cover zaehlt hier wie eine volle Ueberdeckung.

      ══ WAS `--griff: 64 -> 66` WIRKLICH GEKOSTET HAT ══════════════════════
      Zwei Baeume, dasselbe Werkzeug, dieselbe Attrappe:

        Schirm            Paare      Ueberdeckung px²   davon mit einem BILD
        start             36 -> 36    44302 -> 44398     15411 -> 14892
        raster            28 -> 28    30700 -> 29701     15452 -> 14932
        lane              23 -> 23    47780 -> 49983     30427 -> 31427
        lane-tief         22 -> 22    35450 -> 37392     27173 -> 28144
        player             2 ->  2      566 ->   566         0 ->     0
        cover-voll         0 ->  0        0 ->     0         0 ->     0
        laut              44 -> 44    69326 -> 69422     27923 -> 27404
        eltern-tor         0 ->  0        0 ->     0         0 ->     0
        eltern-flaeche     0 ->  0        0 ->     0         0 ->     0

      DIE ERSTE SPALTE IST DIE WICHTIGE: KEIN EINZIGES NEUES PAAR. Nichts ist
      durch das Wachsen unter etwas geraten, das vorher nicht schon darauf lag.

      DIE ZWEITE ZEIGT, DASS ES TROTZDEM NICHT NICHTS WAR. Vier Knoepfe liegen
      `fixed` ueber der rollenden Buehne — `#zurueck` und die drei der
      Kopfleiste. Wo einer von ihnen ganz auf einer Kachel steht, wuchs seine
      Auflage von 64x64 = 4096 px² auf 66x66 = 4356 px², also je +260 px². Auf
      dem Schirm `lane` sind das +1000 px² auf COVERBILDERN. Auf `start`,
      `raster` und `laut` ging es zurueck (-519 px²), weil der Mini-Player seine
      Kinder anders verteilt.

      DAS COVER-VOLLBILD — der ausdrueckliche Wunsch der Tochter des Betreibers
      ([[album-gross-bedienung-an-den-rand]]) — steht vorher wie nachher bei
      0 px². Die Bedienung liegt in den Randspalten, die ein quadratisches Cover
      ohnehin frei laesst; `#zurueck` waechst dort in die freie Spalte hinein.
      Von tools/album-gross-schau.mjs unabhaengig bestaetigt (Knoepfe 66/66/84,
      „Rueckweg verdeckt nichts", hell und dunkel).

      UND DIE STELLE, UM DIE DIE E8-AENDERUNG GEBAUT WURDE: `eltern-tor` und
      `eltern-flaeche` stehen bei 0 px². Die befuerchtete Ueberdeckung
      `#zurueck` <-> „WLAN" ist NICHT eingetreten; die Luft an `.eltern-kopf`
      haelt.

      AUFRUF
        node tools/verdeckung-messen.mjs
        node tools/verdeckung-messen.mjs --json > /tmp/vorher.json
        node tools/verdeckung-messen.mjs --vergleich /tmp/vorher.json
        node tools/verdeckung-messen.mjs --schirm cover-voll
        node tools/verdeckung-messen.mjs --griff 64   # nur die Variable stellen
    related:
      - griff-66-statt-64
      - rueckweg-verdeckt-die-ueberschrift
      - album-gross-bedienung-an-den-rand
      - beruehrziele-neue-oberflaeche-gemessen
      - f6-wahlleiste-schiebt-das-cover-aus-dem-bild

  - kind: falle
    id: marke-gilt-nur-wo-ein-messwerkzeug-hinsieht
    title: "E8 gegengelesen: 95 -> 21 stimmt — und die letzte nackte 64 stand dort, wo kein Schirm hinsieht"
    match: "wahl-knopf 64|f6 knopf zu klein|griff nicht mitgezogen|beruehrziele attrappe 8299|95 von 163 nachgemessen"
    tags: ["oberflaeche", "neu", "beruehrziele", "e8", "f6", "werkzeug", "falle", 2026]
    source: "06.08.2026 gegengelesen; zwei git-worktrees (5c6a1db1^ / 5c6a1db1), tools/beruehrziele-neu.mjs, tools/f6-wahlleiste-nachmessen.mjs, tools/rotprobe.py. Repariert in Commit 5da56cb3 und 19f68f2c."
    body: |
      DIE ZAHL HAELT, UNTER GLEICHEN BEDINGUNGEN NACHGEMESSEN: derselbe Lauf
      gegen den alten Baum ergibt 95 von 163, gegen den neuen 21 von 163. Die
      vier uebrigen Bedienelemente sind genau die aus [[griff-66-statt-64]]
      (`.tipp-spiel` 15x, `#mp-laut-regler`, `#gr-schieber`, `.leute-kachel` 4x).

      Die aeltere Zahl 97 von 165 ist NICHT dieselbe Messung: sie stammt vom
      05.08. und von einem anderen Bestand der Attrappe. Das steht in
      [[beruehrziele-eingeteilt-74-von-95-sind-ein-pixel]] ausdruecklich so da
      („die EINTEILUNG haelt, die Gesamtzahl wandert"), und es hat sich beim
      Nachmessen bestaetigt. Wer 97 und 21 nebeneinanderstellt, vergleicht zwei
      Baeume UND zwei Bestaende.

      ══ WAS DIE 21 NICHT ENTHAELT, UND DAS IST DER BEFUND ══════════════════
      `.wahl-knopf` — die beiden F6-Knoepfe „weiterhören" und „von vorne" —
      stand mit `min-height: 64px` als NACKTE ZAHL da und ist bei der
      Umstellung auf `--griff: 66px` nicht mitgegangen. Es war die letzte nackte
      64 im ganzen Stilblatt.

      GESEHEN HAT SIE NIEMAND, WEIL KEIN SCHIRM SIE ZEIGT. Die Wahlleiste
      entsteht nur an einem Album MIT gemerkter Stelle; keiner der zehn Schirme
      von tools/beruehrziele-neu.mjs stellt diesen Fall her. Die Gesamtzahl
      „21 von 163" ist deshalb ARM UM DIESEN EINEN — vorher wie nachher.

      DIE LEHRE IST GROESSER ALS DER KNOPF: Eine Zahl aus einem Messwerkzeug
      gilt fuer die Schirme, die es ansteuert, und fuer keine anderen. „Die
      ganze Oberflaeche haelt die 9 mm bis auf vier Elemente" heisst in
      Wahrheit „die zehn angesteuerten Schirme halten sie bis auf vier".

      UND DIE BEIDEN PRUEFUNGEN, DIE DEN KNOPF BEWACHEN, TRUGEN DIE MARKE
      ABGESCHRIEBEN: der e2e-Fall verlangte `h >= 64` mit dem Satz „die Marke
      sind 64 px (9 mm)", der rotprobe-Eingriff zitierte `min-height: 64px`.
      Beide standen auf gruen und hielten eine Zahl fest, die das Haus laengst
      geaendert hatte — dieselbe Bauart, die tools/rueckweg-schau.mjs am 05.08.
      schon einmal abgelegt hat. Der Fall liest `--griff` jetzt ZUR LAUFZEIT aus
      der Seite; gegen den alten Baum ist er rot („Knopf nur 64 px hoch, die
      Marke sind 66 px").

      WAS DIE ZWEI BILDPUNKTE KOSTEN (tools/f6-wahlleiste-nachmessen.mjs):
        Knopfhoehe                              64 -> 66 px
        Coverbild des offenen Albums sichtbar    5 % -> 3 %
        Kachel des offenen Albums sichtbar      33 % -> 32 %
        Titelkacheln 1-6                       100 % -> 100 %
      Die Leiste kostet das Coverbild ohnehin 69 % -> 5 %
      ([[f6-wahlleiste-schiebt-das-cover-aus-dem-bild]]) — DIESE Frage ist die
      offene, nicht die Hoehe eines Knopfes.

      ══ EINE FALLE IM MESSWEG SELBST, heute hereingefallen ═════════════════
      tools/beruehrziele-neu.mjs startete seine Attrappe IMMER auf 8299, auch
      wenn eine andere Adresse mitgegeben war — genau der Fehler, der in
      tools/leihgabe.mjs seit dem 04.08. abgestellt und aufgeschrieben ist. Der
      Vergleichslauf gegen den alten Baum meldete daraufhin acht von zehn
      Schirmen „NICHT ERREICHT" und darunter „ZUSAMMEN: 6 von 6". Das sieht aus
      wie ein Ergebnis; richtig waren 95 von 163.

      DER SCHLIMMERE AUSGANG WAERE DER ANDERE GEWESEN: laeuft auf 8299 eine
      FREMDE Vorschau (an diesem Baum arbeiten mehrere gleichzeitig, und heute
      liefen zwei), misst das Werkzeug klaglos gegen den fremden Baum und meldet
      eine vollstaendige, falsche Zahl. Repariert: Port aus dem Ziel, und die
      Schlusszeile sagt „ZUSAMMEN (UNVOLLSTAENDIG)", statt den Hinweis eine
      Zeile tiefer zu verstecken.
    related:
      - griff-66-statt-64
      - beruehrziele-eingeteilt-74-von-95-sind-ein-pixel
      - beruehrziele-neue-oberflaeche-gemessen
      - f6-wahlleiste-schiebt-das-cover-aus-dem-bild
      - rotprobe-war-der-test-ohne-die-aenderung-rot
      - laufende-vorschau-teilt-zustand

  - kind: knowhow
    id: resume-wege-gemessen-e18-stufe3
    title: "E18 Stufe 3 vermessen: acht Stellen fassen resume.json an, EINE kennt ein Profil — und weiterhoerbare() siebt nicht"
    match: 'E18 Stufe 3|resume.json je Kind|resume je Profil|weiterhoerbare siebt nicht|resumeStellenLesen Profil|activeresume toter Verweis|resume Sperre je Profil'
    tags: ['mehrbenutzer', 'profile', 'e18', 'resume', 'weiterhoeren', 'symlink', 'sperre', 'werkzeug', 2026]
    source: "05.08.2026 am Quelltext UND an der Box .169 erhoben; Werkzeug tools/resume-wege-schau.py (liest nur)"
    body: |
      DIE ZEILENNUMMERN IN [[ablage-besitz-gemessen-e18-stufe2]] SIND SCHON
      VERALTET. Sie stammen vom Vormittag des 05.08. und lagen am selben Abend
      um 177 Zeilen daneben. Deshalb SUCHT `tools/resume-wege-schau.py` die
      Stellen, statt sie aufzuzaehlen — wer eine Zahl braucht, laesst das
      Werkzeug laufen, statt hier abzuschreiben.

      ── ACHT STELLEN, UND WOHER SIE EIN PROFIL NAEHMEN (05.08. abends) ──
          GET  /api/resume        7112  liest resume.json roh   KEIN Profil
          fn   resumeStellenLesen 7145  liest resume.json       KEIN Profil
          fn   laneWeiterZeile    7175  ueber resumeStellenLesen  profilAktiv()
          GET  /api/weiterhoeren  7236  ueber resumeStellenLesen  profilAktiv()
          POST /api/weiterhoeren  7390  SCHREIBT, Sperre        KEIN Profil
          GET  /api/activeresume  7977  liest den VERWEIS       KEIN Profil
          POST /api/addresume     8165  SCHREIBT, Sperre        KEIN Profil
          POST /api/editresume    8332  SCHREIBT, Sperre        KEIN Profil
      Die beiden Stellen MIT Profil benutzen es nur fuer die ZEIT aus
      gespielt.json — nicht fuer die Auswahl. KEINE der drei schreibenden
      Stellen kennt einen Besitzer.

      ── DIE REGEL, DIE DER BAUM SCHON HAT: LESEN DARF NENNEN, SCHREIBEN NICHT
      `POST /api/gespielt` (6765) holt den Besitzer mit `profilAktiv()` und
      sagt im Kommentar, warum: „DER BESITZER KOMMT NICHT VOM CLIENT." Ein
      `?profil=` gibt es nur LESEND (`/api/gespielt` 6844, `/api/kinderzeit*`
      6217/6228, `kzKennungAus` 6203). Der EINZIGE Weg, auf dem eine Kennung
      vom Client hereinkommt und etwas bewegt, ist `POST /api/profil/aktiv`
      (6177) — der Umschalter selbst, und er weist eine unbekannte Kennung mit
      400 ab. E18/3 muss dieser Regel folgen: eine Meldung, die
      `profil: kalea` behauptet, waere der erste Bruch.

      ZWEI VERSCHIEDENE PRUEFUNGEN FUER DIESELBE FRAGE — am Geraet gemessen:
          GET /api/gespielt?profil=niemand         HTTP 200, leerer Verlauf
          GET /api/kinderzeit/stand?profil=niemand HTTP 400 unbekanntesProfil
      Der Verlauf prueft nur das MUSTER (`kennungPruefen`), die Kinderzeit die
      MITGLIEDSCHAFT. Wer fuer resume eine dritte Antwort erfindet, hat drei.

      ── weiterhoerbare() SIEBT NICHT, ES SORTIERT (weiterhoeren.ts:802) ──
      Der Verlauf geht NUR in eine Zeitkarte ein: `zeileAus(s, zeiten.get(m.key)
      ?? 0)`. Das einzige Sieb ist `istWeiterhoerbar(s)` — Dienst, Zaehlweise,
      „schon durch", Mindestschwelle. Kein Wort von einem Besitzer.

      AM GERAET IST DAS SICHTBAR, OHNE EIN ZWEITES PROFIL ANZULEGEN: von sechs
      Zeilen der Weiterhoeren-Reihe tragen ZWEI `zuletzt: 0`
      (`library:Die Maus|Die Maus`, `spotify:1dnfyzPLmuVgfIItWSE72Q`) — ihr Werk
      steht in resume.json, aber in gespielt.json des Gasts gar nicht. Sie
      stehen trotzdem in der Reihe. Bei zwei Kindern zeigt die Reihe also
      beides, und die Zeit entscheidet nur die Reihenfolge.

      DAS SIEB GEHOERT NICHT IN weiterhoerbare(). Ein `if (!zeiten.has(m.key))
      return` waere ein Sieb ueber einen STELLVERTRETER und geht zweimal
      daneben:
        * es loeschte HEUTE zwei von sechs Zeilen an der Box .169 — Stellen,
          die dem Gast gehoeren;
        * `vermerken()` deckelt den Verlauf bei MAX_EINTRAEGE = 200 und wirft
          das AELTESTE weg (gespielt.ts:37/79). Eine gemerkte Stelle, deren
          Verlaufseintrag herausgefallen ist, verschwaende damit still.
      Das Sieb gehoert an die QUELLE: `resumeStellenLesen(kennung)` liest
      `profile/<kennung>/resume.json`. Dann siebt niemand — es liegt nur das
      Richtige da.

      ── DIE SPERRE BRAUCHT KEIN PROFIL, UND ZWAR AUS EINEM GRUND, DER NICHT
         „ZU TEUER" HEISST ─────────────────────────────────────────────────
      Sie schuetzt ein Lesen-Aendern-Schreiben auf EINER Datei. Liegt je Kind
      eine eigene Datei da, koennen zwei Schreiber einander gar nicht mehr
      zerstoeren — eine Sperre je Profil brauchte man fuer die Datei nicht.
      ENTSCHEIDEND IST DER ANDERE ARM: `/tmp/.resume.lock` ist die einzige
      Verabredung zwischen dem Server und VIER root-Skripten (check_network.sh,
      get_network.sh, clearresume.sh, remove_max_resume.sh). Eine Sperre je
      Profil naehme diese Verabredung WEG, solange die Skripte nicht mitziehen
      — man haette nicht mehr Nebenlauf, sondern weniger Schutz. Box-weit
      bleiben heisst: der Server serialisiert n Kinder (Kosten: ein
      uebersprungener Merkpunkt, der 15 s spaeter wiederkommt) und bleibt mit
      den Skripten verzahnt (Nutzen: keine zerschriebene Datei).

      ── DIE MINE IST SCHLIMMER ALS „DAS SKRIPT LEGT DEN VERWEIS NEU" ────
      GENAUER ALS BISHER AUFGESCHRIEBEN: `check_network.sh` legt
      `active_resume.json` NICHT alle zehn Sekunden neu an. Der `ln -s` steht
      in beiden Zweigen INNERHALB von `if [ "${ONLINESTATE}" != "${OLDSTATE}" ]`
      (Zeilen 85/94-101 online, 143/152-159 offline) — er feuert beim Start des
      Dienstes und bei JEDEM Wechsel online/offline. An der Box .169 ist das
      heute kein seltenes Ereignis: der Verweis wurde am 05.08. um 15:52 und
      erneut um 16:20:21 neu gelegt, und um 16:20:11 zeigte
      `GET /api/activeresume` 2 statt 9 Eintraegen (der Verweis stand da auf
      `offline_resume.json`).
      `get_network.sh` legt UEBERHAUPT KEINEN Verweis — es kennt die Pfade nur
      als Variablen und erzeugt `offline_resume.json` aus `resume.json`. Wer
      das Wiki nach „vier Skripte legen den Verweis" liest, sucht an drei
      Stellen umsonst.

      UND DER TEILE, DER WIRKLICH WEHTUT: ein TOTER Verweis heilt NIE WIEDER
      von selbst. Nachgestellt und gemessen:
          test -f <toter Verweis>   ->  NEIN
          ln -s ziel <toter Verweis> ->  „Die Datei existiert bereits", exit 1
      Das Skript nimmt bei `[ ! -f ... ]` genau den Zweig OHNE `rm` — es kann
      den Verweis also nicht ersetzen, und der `elif`-Zweig mit `rm` wird nie
      erreicht, weil `-f` nie wieder wahr wird. Zieht `resume.json` in einen
      Bereich, bleibt `active_resume.json` fuer immer tot.
      Dazu prueft `GET /api/activeresume` (7978) mit `fs.existsSync` — in Node
      gemessen: auf einem toten Verweis FALSE (`lstatSync` sieht ihn sehr wohl).
      Die Antwort ist `[]` mit Status 200. Die klassische Oberflaeche zeigt
      dann eine leere Weiterhoeren-Liste (resume.page.ts:225,
      resume-overlay.component.ts:207 ueber media.service.ts:462) — ohne
      Meldung, ohne Protokolleintrag.

      ── ZWEI SCHREIBER AM SERVER VORBEI, UND EINER WIRD VOM PLAYER GERUFEN
      `remove_max_resume.sh` schneidet resume.json auf `mupibox.resume` (auf
      .169 gemessen: 9) und wird NICHT von cron gerufen, sondern vom
      Abspieldienst: `spotify-control.ts:2406` auf den Befehl `maxresume`, den
      die klassische Oberflaeche 2 s nach jedem `addRawResume` schickt
      (player.page.ts, `PlayerCmds.MAXRESUME`). Ein Bereich je Kind hiesse: das
      Skript schneidet weiter die ALTE Datei — der Deckel je Kind waere ohne
      Wirkung, und niemand merkt es.
      AUF DIESER BOX GIBT ES KEINE CRONTAB (`crontab -l` fuer dietpi und root:
      „no crontab"; /etc/cron.d nur e2scrub_all und php). Die Zeilen in
      `config/templates/crontab.template` (get_network.sh alle 30 s) sind auf
      .169 NICHT installiert — wer die 30-Sekunden-Annahme aus dem AUDIT
      uebernimmt, plant gegen etwas, das hier nicht laeuft.

      ── WAS DIE SICHERUNG BRAUCHT (fremdes Gebiet, angesagt) ────────────
      `scripts/mupibox/mupibox-sicherung.py` fuehrt `resume.json` FLACH (Zeile
      166) und kennt drei Bereichsmuster (183-185). Ein
      `profile/*/resume.json` traefe keines — die Sicherung liefe gruen und
      nickte Staende OHNE gemerkte Stellen ab. Dieselbe Reihenfolge des
      Schadens wie am 05.08. bei den Bereichen: er faellt beim ZURUECKSPIELEN
      auf.

      ── DREI FRAGEN, DIE DEM BETREIBER GEHOEREN (nicht vorweggenommen) ──
        1. `offline_resume.json` wird per jq aus resume.json erzeugt
           (check_network.sh 124-142). Je Kind eine — oder eine, die dem
           gerade aktiven Kind gehoert?
        2. „Zuletzt Gehoertes merken (Anzahl)" steht auf 9. Heisst das 9 JE
           KIND (dann 9*n auf der Karte) oder 9 fuer die Box?
        3. `active_resume.json` ist ein Verweis auf EINE Datei. Bei n Bereichen
           muesste er beim Profilwechsel umgehaengt werden — vom Server, der
           heute nichts davon weiss, oder gar nicht mehr (die klassische
           Oberflaeche liest dann direkt eine profilbezogene Route).
    related:
      - ablage-besitz-gemessen-e18-stufe2
      - bereich-je-profil-ein-ordner-statt-namenszusatz
      - dienstliste-an-fuenf-orten
      - weiterhoeren-deckel-vor-dem-sieb
      - addresume-sperre-nur-ueber-das-lesen
      - server-antwortet-200-auf-alles
      - kopiert-ist-nicht-aufgerufen

  - kind: architektur
    id: thema-erreicht-die-neue-oberflaeche-nur-mit-important
    title: "Wie ein Farbthema auf den Schirm kommt — und warum es die neue Oberflaeche heute nicht faerbt"
    tags: [themen, darstellung, neue-oberflaeche, farben, active_theme]
    source: "am Geraet gemessen 2026-08-05, tools/thema-weg-schau.mjs"
    body: |
      GEMESSEN, NICHT GELESEN. Vor einem Farbwaehler fuer die neue Oberflaeche
      musste feststehen, wohin sein Wert fliesst. Der Weg hat FUENF Stellen:

        1. DIE DATEI      /home/dietpi/MuPiBox/themes/<Name>.css   (39 Dateien)
        2. DER NAME       mupibox.theme in /etc/mupibox/mupiboxconfig.json
        3. DER SCHREIBER  /usr/local/bin/mupibox/setting_update.sh
        4. DER VERWEIS    www/active_theme.css -> themes/<Name>.css
        5. DER LESER      index.html — und app.css kommt DANACH

      ── WER SCHREIBT DEN VERWEIS, UND WANN ──────────────────────────────

      NUR `setting_update.sh` (Zeilen 12-20). Aufgerufen wird es heute an genau
      EINER lebenden Stelle: `POST /api/konfiguration` im Server ruft
      `sudo /usr/local/bin/mupibox/setting_update.sh` NACH jedem erfolgreichen
      Speichern (server.ts, „Die Skripte der Box aus der neuen Konfiguration
      nachziehen"). Am Geraet belegt: drei Aenderungen an `oberflaeche` am
      05.08. um 16:28 erzeugten drei sudo-Zeilen im Protokoll und legten den
      Verweis an (root:root).

      DIE ANDEREN ZWEI WEGE SIND TOT:
        * `mupi_shutdown.sh` Zeile 57 — der Aufruf ist AUSKOMMENTIERT.
        * `set_deviceid.sh` Zeile 15 ruft es, aber `set_deviceid.sh` selbst hat
          im ganzen System KEINEN Aufrufer (gesucht in /etc, /usr/local/bin,
          server.js, /home/dietpi/MuPiBox).

      FOLGE, die zaehlt: der Verweis wird NUR nachgezogen, wenn jemand in der
      Verwaltung IRGENDEINE Einstellung speichert — und dann sogar, wenn das
      Thema gar nicht gemeint war. Ein Neustart tut es NICHT (am 05.08. nach
      einem Neustart um 16:27 nachgesehen: Verweis weiter weg).

      ── DER VERWEIS UEBERLEBT EIN AUSROLLEN NICHT ───────────────────────

      Am 05.08. um 16:08 war `www/active_theme.css` WEG. Er ist kein
      Bauergebnis; ein Ausrollen, das www/ austauscht, nimmt ihn mit und legt
      ihn nicht wieder an (steht als Warnung schon in mupi-aufraeumen-www,
      wurde trotzdem wahr). Danach stand in der Konfiguration weiter
      `theme: MupiNew`, und GEFAERBT WURDE NICHTS.

      DAS SIEHT MAN AN HTTP 200 NICHT. Der Server liefert auf jeden Pfad die
      index.html ([[server-antwortet-200-auf-alles]]):
        /active_theme.css  ->  200  text/html  9682 B
      Der Browser holt sie, versucht sie als Stilblatt zu lesen und kommt auf
      NULL REGELN. Im Browser sichtbar als `document.styleSheets` mit
      `cssRules.length == 0`. Beweis war der INHALTSTYP, nicht der Status.

      Gegenprobe mit eingehaengtem Thema (tools/thema-weg-schau.mjs --thema
      MupiNew): klassische Oberflaeche sofort blau,
      `--ion-background-color #44afe2`, `--ion-color-light #054b61`, body
      rgb(68,175,226). OHNE Verweis stand dort #121212 / #2a2a2a — die
      Ionic-Vorgabe. Der MECHANISMUS ist also heil; es fehlte nur der Verweis.

      ── ERREICHT EIN THEMA DIE NEUE OBERFLAECHE? JA — ABER ──────────────

      `/neu/index.html` bindet `/active_theme.css` MIT fuehrendem Schraegstrich
      ein, VOR `app.css`. Gemessen mit einem eigens eingehaengten Probeblatt
      (`--probe`):

        Weg offen (Datei wird geliefert und geparst)   JA
        `:root{--bg:#123456}`      OHNE !important     WIRKUNGSLOS
        `:root{--ink:#654321 !important}`              WIRKT

      GRUND: gleiche Spezifitaet, und `app.css` kommt SPAETER. Wer je Farben
      der neuen Oberflaeche ueber eine Themendatei setzen will, braucht
      entweder `!important`, oder das Blatt muss NACH app.css eingebunden
      werden, oder die Werte gehen einen ganz anderen Weg (Stil am Element,
      /api/darstellung — dort kommen `--kachel`, `--kachel-abstand` und
      `--mp-hoehe` heute schon als `--mupi-*` her).

      ── WARUM HEUTE TROTZDEM KEIN THEMA FAERBT ──────────────────────────

      Alle 39 Themen zusammen setzen genau 11 Variablen:
        --background --background-hover-opacity --bar-background
        --bar-background-active --indicator-height --ion-background-color
        --ion-color-contrast --ion-color-light --ion-item-background
        --ion-text-color --knob-background
      KEINE EINZIGE davon liest die neue Oberflaeche. Sie kennt `--bg`,
      `--surface`, `--ink`, `--muted`, `--line`, `--line2`, `--accent`,
      `--accentInk`, `--accentDark`, `--pill`, `--pillInk`, `--hl`,
      `--mp-akzent`, `--weiter-blau`, `--tipp-neutral`.

      Und die Selektoren treffen ueberwiegend nicht: `ion-*`, `.ion-page`,
      `.home-content`, `.medialist-*`, `.player-*`, `.circle-card`, `.md body`
      — nichts davon gibt es unter /neu/. Es bleibt `body { --ion-*: … }`,
      das trifft, aber niemand liest es.

      ── ZAHLEN, die man sonst herleitet ─────────────────────────────────

      * app.css `:root` setzt 22 Variablen, davon 16 Farben und 6 Groessen
        (--umriss-luft, --griff, --kachel, --kachel-abstand, --mp-hoehe,
        --mp-rand). `:root[data-licht='dunkel']` ueberschreibt 12.
        ACHTUNG: eine aeltere Notiz nannte 18/9 — am 05.08. nachgezaehlt
        stimmt das nicht mehr.
      * 39 Dateien in themes/, aber nur 29 in `mupibox.installedThemes`.
        NICHT waehlbar, obwohl vorhanden: axolotl, clone-wars, custom,
        dinosaur, enterprise, pikachu, spiderman, supermario, unicorn,
        wall-e. Umgekehrt fehlt keine Datei. `wall-e.css` ist 0 Bytes.
      * `MupiNew.css` (Jul 26 22:36) ist bis auf einen sechszeiligen
        Kopfkommentar BYTEGLEICH mit `blue.css`. Der Name legt einen Bezug
        zur neuen Oberflaeche nahe — es gibt KEINEN. Sie faerbt Ionic,
        sonst nichts. Angelegt in der Sitzung vom 26.07.
        (siehe mupi-neues-thema-anlegen).
      * `--ink #2E2A3B` auf `--bg #FFF7EC` misst 13,09 : 1 (tools/kontrast.mjs)
        — reichlich ueber 4,5. Ein Waehler, der beide frei laesst, kann das
        auf 1,0 druecken; die Zahl gehoert neben den Waehler.

      ── WAS DAS FUER EINEN FARBWAEHLER HEISST ───────────────────────────

      Der Weg ueber `themes/*.css` ist NICHT der Weg der neuen Oberflaeche —
      er faerbt Ionic, haengt an einem Verweis, den ein Ausrollen mitnimmt,
      und wird nur beim Speichern irgendeiner Einstellung nachgezogen. Wer
      Farben fuer /neu/ dorthin legt, baut eine ZWEITE Quelle neben app.css
      ([[drei-orte-eine-anzeige]]). `/api/darstellung` traegt dagegen schon
      heute Groessen der neuen Oberflaeche als `--mupi-*` — das ist der Weg,
      der bereits funktioniert.

      OFFEN und dem Betreiber gehoerend: sollen die Plakettenfarben der
      Dienste (rss 339, lokal 211, ard 175, spotify 141, radio 17,
      jellyfin 268 — eine gemessene Runde um den Farbkreis) ueberhaupt in den
      Waehler? Frei waehlbar koennen zwei Dienste auf 29 px aus zwei Metern
      ununterscheidbar werden.
    related:
      - mupi-neues-thema-anlegen
      - mupi-themawechsel-teilstring
      - mupi-themen-und-classic
      - darstellung-auf-der-box-statt-im-browser
      - server-antwortet-200-auf-alles
      - drei-orte-eine-anzeige
      - kopiert-ist-nicht-aufgerufen

  - kind: architektur
    id: resume-bruecke-am-alten-ort
    title: "E18 Stufe 3: resume.json zieht um, und der alte Ort wird zur BRUECKE — weil vier root-Skripte den neuen Pfad nie lernen werden"
    match: 'resume Bruecke|resume je Kind gebaut|active_resume Bruecke|resume symlink alter Ort|stat folgt dem Verweis nicht|lutimes Bruecke|offline_resume Zeitstempel|resumeBrueckeRichten|resumeUebernehmen'
    tags: ['mehrbenutzer', 'profile', 'e18', 'resume', 'symlink', 'bruecke', 'sicherung', 'skripte', 2026]
    source: "05.08.2026 gebaut, Commit e475713e im Box-Repo; alle Aussagen unten gemessen (tools/resume-bruecke-probe.py, bruecke.integration.spec.ts, tools/rotprobe.py --nur E18/3). NICHT ausgeliefert — siehe der letzte Abschnitt."
    body: |
      DER BESTAND LIEGT JETZT IN `server/config/profile/<kennung>/resume.json`,
      und `resume.json` steht in `BEREICH_ABLAGEN` (profile.ts) — derselben
      EINEN Liste wie die drei aus Stufe 2. Das Sieb, das Liam davon abhaelt,
      Kaleas Stellen zu sehen, steht damit an der QUELLE und nicht in
      `weiterhoerbare()`; die Begruendung dafuer in
      [[resume-wege-gemessen-e18-stufe3]] gilt unveraendert.

      ── DER TEURE TEIL WAR DER ALTE ORT ─────────────────────────────────
      Vier root-Skripte fassen `server/config/resume.json` an, und sie werden
      den neuen Pfad NIE lernen: `update/start_mupibox_update.sh` und
      `autosetup/autosetup.sh` schieben mit `mv ${MUPI_SRC}/scripts/mupibox/*`
      bei jedem Update die Upstream-Fassung aller vier zurueck. Wer sie
      anpasst, hat eine Loesung, die bis zum naechsten Update haelt.

      DESHALB STEHT AM ALTEN ORT EINE BRUECKE: ein RELATIVER Verweis auf
      `profile/<aktiv>/resume.json`, gelegt und wiederhergestellt von
      `resumeBrueckeRichten()` (server.ts). Relativ, damit er ein
      Zurueckspielen auf eine andere Karte und ein Testverzeichnis ueberlebt.
      Was die Skripte dort tun, trifft dann das Kind, das dran ist:

        check_network.sh    `ln -s ${RESUME_FILE} ${ACTIVERESUME_FILE}` — die
                            Kette active_resume -> Bruecke -> Bereich loest
                            sich auf. DER SERVER FASST active_resume.json NIE
                            AN. Das ist der Kern: das Skript darf ihn bei
                            jedem Netzwechsel neu legen, auch in seiner alten
                            Fassung nach einem Update — es kommt dasselbe
                            heraus.
        get_network.sh      `[ ! -f ${RESUME_FILE} ]` ist auf einer LEBENDEN
                            Bruecke falsch. Der leere Stummel, der den Umzug
                            binnen 20 s zunichtemachte, entsteht gar nicht.
        offline_resume.json wird per jq aus dem alten Ort erzeugt und gehoert
                            damit dem aktiven Kind. Das beantwortet die erste
                            der drei offenen Fragen aus
                            [[resume-wege-gemessen-e18-stufe3]] auf dem
                            vorsichtigen Weg: EINE Datei, die dem gehoert, der
                            dran ist — ohne dass jemand n Dateien benennen
                            muss.
        remove_max_/clear…  treffen den Bestand des aktiven Kindes. Damit sind
                            „hoechstens 9" und „Clear all resume media" JE
                            KIND — die zweite offene Frage, ebenfalls so, wie
                            sie von selbst herausfaellt.

      ── DIE ZWEI, DIE DIE BRUECKE MITNEHMEN ─────────────────────────────
      `remove_max_resume.sh` (`mv /tmp/.resume.json ${RESUME}`, FREMDES
      Dateisystem) und `clearresume.sh` (`mv ${DATA}.tmp ${DATA}`, gleiches)
      schreiben ihr Ergebnis ueber den alten Ort. GEMESSEN, beide Faelle: der
      Verweis ist danach WEG und liegt als echte Datei da, waehrend im Bereich
      noch der Stand von davor steht. GNU `mv` ersetzt den Verweis, es schreibt
      nicht durch ihn hindurch.

      DAFUER GIBT ES DIE UEBERNAHME (`resumeUebernehmen`): eine ECHTE Datei am
      alten Ort ist die Arbeit eines Skripts und wandert in den Bereich. Per
      `link` + `rename`, damit der alte Ort in KEINEM Augenblick unbesetzt ist
      — waere er auch nur einen Wimpernschlag weg und liefe genau dann
      get_network.sh (alle 20 s), legte es dort `[]` an. Ohne die Uebernahme
      waeren Deckel und „Clear all" ab dem Umzug wirkungslos, und zwar
      lautlos: beide melden nichts.

      EINE LEERE LISTE WIRD ANGENOMMEN, NICHT ABGEWIESEN. `clearresume.sh`
      raeumt legitim auf `[]` ab (auf .169 tragen ALLE Eintraege
      `category: "resume"`, nachgezaehlt); `get_network.sh` schreibt dasselbe
      `[]` als Stummel. Von der Serverseite sind die beiden nicht zu
      unterscheiden. Also: uebernehmen UND den bisherigen Stand als
      `resume.json.vorher` daneben aufheben.

      ── DIE FALLE, DIE FAST DURCHGERUTSCHT WAERE ────────────────────────
      `stat --format='%Y'` FOLGT EINEM VERWEIS NICHT. GNU `stat` braucht dafuer
      `-L`, und keines der beiden Skripte gibt es an — beide erzeugen
      `offline_resume.json` nur, wenn der alte Ort NEUER ist. Sie messen also
      die BRUECKE, nicht den Bestand dahinter, und der Zeitstempel eines
      Verweises steht fest, seit er gelegt wurde.

      OHNE ZUTUN BLIEBE DIE OFFLINE-LISTE ALSO AUF DEM STAND DES LETZTEN
      PROFILWECHSELS stehen, waehrend die gemerkten Stellen weiterwandern —
      sichtbar erst ohne Netz, Wochen spaeter. Vor dem Umzug gab es das
      Problem nicht: da war der alte Ort eine echte Datei, und jedes Schreiben
      setzte ihren Zeitstempel mit. Jedes Schreiben frischt deshalb jetzt den
      VERWEIS auf — `fs.lutimesSync`, NICHT `fs.utimesSync`: letzteres folgt
      dem Verweis und faerbte genau die Datei, die `stat` hier nicht ansieht.
      Der Eingriff `e18s3-aufgefrischt-wird-der-verweis` in `tools/rotprobe.py`
      tauscht die beiden gegeneinander und wird bemerkt.

      MERKSATZ FUER DIESEN BAUM: wo ein Shell-Skript `stat` sagt, meint es
      `lstat`. Wo Node `existsSync`/`utimes` sagt, meint es `stat`/`utimes` MIT
      Folgen. Die beiden Sprachen sind an dieser Naht genau entgegengesetzt.

      ── EIN KIND OHNE BESTAND: TOTE BRUECKE, UND SIE HEILT AUS ──────────
      Ein frisch angelegtes Profil hat noch keine `resume.json`; die Bruecke
      zeigt dann ins Leere. GEMESSEN: `get_network.sh` schreibt DURCH den toten
      Verweis (`echo -n "[]" > ${RESUME_FILE}` legt das ZIEL an), der Zustand
      heilt also von selbst aus, und `GET /api/activeresume` antwortet bis
      dahin `[]` — die richtige Antwort fuer ein Kind, das noch nichts
      angefangen hat.

      ── WAS SICH NICHT GEAENDERT HAT, MIT GRUND ─────────────────────────
      * DIE SPERRE BLEIBT BOX-WEIT (`/tmp/.resume.lock`). Sie ist die einzige
        Verabredung zwischen Server und den vier Skripten; eine Sperre je
        Profil naehme sie weg, solange die Skripte nicht mitziehen — das waere
        nicht mehr Nebenlauf, sondern weniger Schutz.
      * DIE KLASSISCHE OBERFLAECHE BRAUCHTE KEINE ZEILE. `saveResumeFiles`,
        `/api/activeresume`, CLEARRESUME und MAXRESUME gehen weiter den alten
        Weg — genau dafuer ist die Bruecke da.
      * DER BESITZER KOMMT NICHT VOM CLIENT. Die drei schreibenden Routen
        nehmen `profilAktiv()`, EINMAL gefragt und fuer Lesen UND Schreiben
        benutzt (ein Wechsel dazwischen legte sonst die Stelle des einen in
        den Bestand des anderen). Kein `?profil=` auf `/api/resume`: es
        brauchte eine DRITTE Antwort auf „was ist eine unbekannte Kennung?" —
        `/api/gespielt` sagt 200 mit leerem Stand, `/api/kinderzeit` 400.

      ── OFFEN, UND ES IST DER GRUND, WARUM NICHTS AUSGELIEFERT WURDE ────
      DIE SICHERUNG DECKT DEN BESTAND NICHT MEHR. `mupibox-sicherung.py`
      sammelt Verweise als VERWEIS ein, nicht als Inhalt
      (`if os.path.islink(voll): verweise[…] = os.readlink(voll)`), und
      `profile/*/resume.json` trifft kein Muster in `HINEIN` — beides direkt
      am Modul nachgemessen, nicht aus dem Quelltext geschlossen. Die
      gemerkten Stellen fielen damit aus der Sicherung heraus, und das faellt
      erst beim ZURUECKSPIELEN auf — die schlimmste Reihenfolge, die ein
      Fehler haben kann, und dieselbe wie bei Stufe 2.

      ES FEHLT EINE ZEILE in `scripts/mupibox/mupibox-sicherung.py`, in
      `HINEIN['server/config']` neben den drei Geschwistern:

          "profile/*/resume.json",

      Das ist FREMDES GEBIET (dort laeuft E29), deshalb steht sie nicht drin.
      `tools/bereich-sicherung-deckung.py` und damit `tools/pruefen.sh` sind
      bis dahin ROT — absichtlich, und mit einem Hinweis daneben. Wer den
      Schritt gruen sehen will, traegt die Zeile nach; wer ihn wegnimmt, hat
      die Wache abgeschafft, die den Fehler ueberhaupt gefunden hat.

      SOLANGE DIESE ZEILE FEHLT, GEHOERT E18/3 NICHT AUF DIE BOX. Der Baum
      traegt sie; .169 fuhr am 05.08. abends unveraendert weiter.

      ── ERLEDIGT, NOCH AM SELBEN ABEND (Nachtrag) ───────────────────────
      DIESER ABSCHNITT IST UEBERHOLT. Die Zeile steht seit `de1229f4` in
      `mupibox-sicherung.py`, am Modul mit dem echten Bestand von .169
      nachgemessen; `tools/bereich-sicherung-deckung.py` ist gruen. E18/3 ist
      danach auf .169 AUSGELIEFERT worden (`tools/ausliefern.py --nur server`)
      und am Geraet nachgemessen: 9 Stellen vorher, 9 nachher, und die Box
      setzt an der gemerkten Stelle fort.

      Beim Gegenlesen kam ausserdem ein Fehler heraus, der AELTER ist als die
      Bruecke: die gemeinsame Sperrdatei `/tmp/.resume.lock` verlor in vier
      von acht Messreihen eine quittierte Stelle. Alles dazu — samt der zwei
      Steuerlaeufe, die den Verursacher eingrenzen — steht in
      [[e18s3-gegengelesen-und-ausgeliefert]].
    related:
      - resume-wege-gemessen-e18-stufe3
      - bereich-je-profil-ein-ordner-statt-namenszusatz
      - ablage-besitz-gemessen-e18-stufe2
      - mehrbenutzer-vorbereitung
      - dienstliste-an-fuenf-orten
      - rotprobe-war-der-test-ohne-die-aenderung-rot
      - veraltete-vorschau-meldet-falsches-rot

  - kind: architektur
    id: farbwaehler-neue-oberflaeche-eine-quelle
    title: "Farbwaehler fuer die neue Oberflaeche: der Wert geht durch das Farbthema — und nur so"
    match: "farbwaehler|farbthema route|api/farbthema|farben neue oberflaeche|--line ohne draht|line2 statt line|farbwaehler-wirkung|farbwaehler-verwaltung-probe|root:not data-licht"
    sev: high
    tags: [themen, darstellung, neue-oberflaeche, farben, verwaltung, kontrast, 2026]
    source: "gebaut und gemessen 05.08.2026; tools/farbwaehler-wirkung.mjs, tools/farbwaehler-verwaltung-probe.mjs, Commit 14a137ed"
    body: |
      Der Vorlaeufer [[thema-erreicht-die-neue-oberflaeche-nur-mit-important]]
      hat den WEG gemessen. Hier steht, was daraus GEBAUT wurde und welche
      Fallen dabei zugeschnappt waeren.

      ── WOHIN DER WERT FLIESST ──────────────────────────────────────────

        Verwaltung (Darstellung -> „Farben der neuen Oberflaeche")
              |  PUT /api/farbthema   { farben: { "--bg": "#RRGGBB", … } }
              v
        themes/<mupibox.theme>.css     ein MARKIERTER Block am Ende
              |  Verweis www/active_theme.css (setting_update.sh)
              v
        /neu/index.html  <link href="/active_theme.css">  VOR app.css
              v
        die neue Oberflaeche

      DER BLOCK SIEHT SO AUS, und beide Eigenheiten sind gemessen:

        :root:not([data-licht=dunkel]) {
          --bg: #EAF4FF !important;
        }

      `!important`, weil das Blatt VOR app.css kommt und bei gleicher
      Spezifitaet das spaetere gewinnt. `:not([data-licht=dunkel])`, weil
      `!important` an `:root` sonst den DUNKLEN Stand erschlaegt — dessen
      Regel traegt kein `!important` und verloere. Am Geraet nachgemessen:
      fuenf Farben wirken hell, alle fuenf bleiben dunkel auf ihren eigenen
      Werten, und die Themendatei kam byteweise zurueck.

      ── DIE FALLE, DIE JEDER BAUT, DER DIE LISTE AUS app.css ZIEHT ──────
      `--line` steht ZWEIMAL in app.css (hell #F3EDE2, dunkel #2E2840) und
      wird NULL Mal mit `var()` gelesen — in app.css nicht, in app.js nicht,
      in index.html nicht. Ein Regler darauf saehe vollkommen richtig aus und
      faerbte nichts. Die Linien haengen an `--line2` (20 Verwendungen).
      In derselben `:root`-Liste stehen ausserdem SECHS Groessen
      (--umriss-luft --griff --kachel --kachel-abstand --mp-hoehe --mp-rand)
      und ein `rgba()` (--tipp-neutral), das kein `<input type="color">`
      anzeigen kann. Wer die Liste ungefiltert nimmt, bietet einen
      Farbwaehler fuer einen Abstand an.
      Verwendungen zaehlen, nicht Deklarationen: `tools/farbwaehler-wirkung.mjs`
      ohne Argumente druckt die Tabelle.

      ── ZWEITE FALLE: KOMMENTARE ERST WEG, DANN KLAMMERN ZAEHLEN ────────
      Der `:root`-Block wird aus app.css gelesen, indem geschweifte Klammern
      gezaehlt werden. Wer VORHER nicht entkommentiert, hoert an der ersten
      Klammer in einem Kommentar auf — und `:root` in app.css traegt lange
      erklaerende Absaetze. Genau so herum stand es im ersten Anlauf: `--line2`
      fehlte still, und der Fehler kam erst im Browser heraus, weil der
      DATEI-Test eine kuerzere app.css benutzte. Noch schlimmer ohne
      Entkommentieren: `indexOf(:root)` trifft dann eine ERWAEHNUNG von
      `:root` in einem Kopfkommentar, und der Waehler bleibt ganz leer.

      ── EINE QUELLE, DREIMAL DURCHGEHALTEN ──────────────────────────────
        Vorgaben      aus `/neu/app.css`, ueber HTTP von derselben Box —
                      also aus der Datei, aus der der Schirm liest. KEINE
                      Kopie der Werte in der Verwaltung.
        Abweichungen  nur im erzeugten Block. Was gleich der Auslieferung
                      ist, wird gar nicht erst eingetragen.
        Auswahl       einmal in `FARBFELDER` (darstellung.ts). Das Backend
                      prueft nur die FORM (`--name`, `#RRGGBB`) und fuehrt
                      ausdruecklich keine zweite Liste.
      Laesst sich app.css nicht lesen, bleibt der Abschnitt ZU. Fuenf Felder
      auf Schwarz waeren schlimmer als keine: der erste Klick schriebe dieses
      Schwarz in die Box. Erkannt wird der Fehlschlag daran, dass keine
      Variable gefunden wurde — NICHT an `r.ok`
      ([[server-antwortet-200-auf-alles]]).

      ── ZWEI DINGE, OHNE DIE ES „NICHT WIRKT" HIESSE ────────────────────
      1. DER VERWEIS. `www/active_theme.css` ist kein Bauergebnis und
         verschwindet, wenn ein Ausrollen `www/` austauscht. Die Route zieht
         ihn deshalb SELBST nach — aber NUR, wenn er nicht schon stimmt.
         Grund: `setting_update.sh` legt nicht bloss den Verweis, es schreibt
         AUCH config.json, die Konfiguration des Abspieldienstes,
         spotifyd.conf und die X11-Standby-Datei neu. Das je gewaehlter Farbe
         einmal waere eine Menge fremder Dateien fuer „ich habe blau
         gewaehlt". Nachgesehen wird mit `realpath` auf beiden Seiten (ein
         toter Verweis wirft und zaehlt als falsch), und NACH dem Lauf noch
         einmal — das Skript endet mit 0, auch wenn sein `ln -s` scheitert.
         Achtung: `POST /api/konfiguration` zieht den Verweis NICHT nach,
         wenn sich kein Feld geaendert hat — es kehrt vorher um
         (`geaendert.length === 0`). Fehlt der Verweis trotz allem, SAGT die
         Verwaltung das hin; sonst ist alles richtig geschrieben, nichts zu
         sehen, und man sucht den Fehler beim Farbwaehler.
      2. DAS NEULADEN. Die neue Oberflaeche liest das Blatt EINMAL beim
         Laden, der Kiosk laeuft durch. Nach dem Speichern geht deshalb ein
         `POST /api/oberflaeche/neuladen` hinterher. Die Box merkt es beim
         naechsten Abgleich (TAKT_STAND 60 s) und laedt hoechstens einmal je
         Aenderung; nach dem Neuladen setzt sie ihren Vergleichswert neu, die
         ZWEITE Aenderung braucht also bis zu zwei Minuten. Die Musik laeuft
         in librespot/mpv und reisst nicht ab.

      ── WAS DER WAEHLER NICHT KANN, UND WARUM ───────────────────────────
      * DEN DUNKLEN STAND. Er behaelt seine eigenen Farben — sonst leuchtete
        die Box abends hell. Steht so auch an der Bedienung.
      * DIE PLAKETTENFARBEN DER DIENSTE. Sie stehen fest in den
        `.marke-*`-Regeln, nicht in `:root`, und sind eine abgemessene Runde
        um den Farbkreis (rss 339, lokal 211, ard 175, spotify 141, radio 17,
        jellyfin 268), damit auf 29 px aus zwei Metern kein Dienst mit einem
        anderen verwechselt wird. Frei waehlbar koennte man zwei
        ununterscheidbar machen. OFFEN und dem Betreiber gehoerend.
      * VERBIETEN. Der Kontrast `--ink` auf `--bg` (ausgeliefert 13,09 : 1)
        steht als ZAHL daneben und sagt unter 4,5 ausdruecklich, dass es auf
        der Box schwer zu lesen wird — verboten wird nichts.

      ── DAS FARBTHEMA IST DER SPEICHERORT, UND DAS HAT EINE FOLGE ───────
      Wer in der Konfiguration ein ANDERES Farbthema waehlt, bekommt dessen
      Farben (also meist: wieder die ausgelieferten). Das ist stimmig — ein
      Thema ist ein Aussehen —, aber es ueberrascht, wenn man es nicht weiss.
      Die Datei wird beim ERSTEN Mal als `<Name>.css.vor-farben` daneben
      gesichert und beim zweiten Mal NICHT erneut; sonst sicherte man die
      eigene Fassung ueber die ausgelieferte. Der von Hand gepflegte
      `--ion-*`-Teil bleibt unberuehrt, ein Farbthema traegt danach beides.

      ── WERKZEUGE ───────────────────────────────────────────────────────
      `tools/farbwaehler-wirkung.mjs`         Bestand, Verwendungen, Wirkung
                                              am Geraet; `--datei <x.css>`
                                              legt ein FERTIGES Blatt ein
      `tools/farbwaehler-verwaltung-probe.mjs`  16 Punkte vom Klick bis in
                                              die Datei, gegen das GEBAUTE
                                              Buendel, ohne die Box anzufassen
    related:
      - thema-erreicht-die-neue-oberflaeche-nur-mit-important
      - drei-orte-eine-anzeige
      - server-antwortet-200-auf-alles
      - mupi-neues-thema-anlegen
      - mupi-themawechsel-teilstring
      - darstellung-auf-der-box-statt-im-browser

  - kind: falle
    id: eigene-hunks-verschluckt-den-nachbarn
    title: "eigene-hunks.py laesst eine Aenderung weg, die neben einer fremden steht — und der Arbeitsbaum merkt es nie"
    match: 'eigene-hunks Hunk verschmolzen|Commit uebersetzt nicht|Cannot find name nach dem Commit|Hunk geschnitten HEAD kaputt|worktree add tsc pruefen'
    tags: ['git', 'zusammenarbeit', 'falle', 'werkzeug', 2026]
    source: "05.08.2026 selbst passiert (E18/3, Commit e475713e), gefunden und repariert mit 48d4f41f"
    body: |
      WAS PASSIERT IST: In server.ts lagen zwei Vorgaenge nebeneinander — eine
      neue Route des Farbwaehlers und, ein paar Zeilen darunter, die Aenderung
      an `/api/resume`. `tools/eigene-hunks.py` zeigte einen Hunk an der Stelle
      des Farbwaehlers; er wurde als FREMDER weggelassen, voellig richtig. Nur
      steckten in demselben Hunk drei EIGENE Zeilen: `git diff` nimmt drei
      Zeilen Zusammenhang, und damit waren die beiden Aenderungen EIN Hunk.
      Weiter unten stand ein zweiter, eigener Hunk derselben Route — der ging
      mit. Uebrig blieb ein Commit, der auf einen Namen zeigt (`pfad`), den
      niemand mehr anlegt.

      WARUM ES NICHT AUFFIEL: Der ARBEITSBAUM war die ganze Zeit gruen — dort
      liegt ja alles. `npx tsc --noEmit`, `npm test`, jede Rotprobe: gruen.
      Kaputt war nur der Stand, den man committet hat, und den sieht man nicht,
      solange man ihn nicht auscheckt.

      DIE PRUEFUNG, DIE ES FINDET (kostet zwanzig Sekunden):

          git worktree add --detach /tmp/pruefbaum HEAD
          ln -sfn <baum>/node_modules            /tmp/pruefbaum/node_modules
          ln -sfn <baum>/src/backend-api/node_modules \
                  /tmp/pruefbaum/src/backend-api/node_modules
          cd /tmp/pruefbaum/src/backend-api && ../../node_modules/.bin/tsc --noEmit
          git worktree remove --force /tmp/pruefbaum

      `npx tsc` im Wegwerfbaum geht NICHT („This is not the tsc command you are
      looking for") — npx sucht sein eigenes Verzeichnis ab, bevor der Verweis
      greift. Der Binaerpfad direkt tut es.

      DIE REGEL: Wer Hunks schneidet, uebersetzt HINTERHER DEN COMMIT, nicht
      den Arbeitsbaum. Und wer im `--zeigen` einen fremden Hunk sieht, sieht
      NACH, ob darin auch Eigenes steht — die Ueberschrift eines Hunks nennt
      nur seine erste Zeile.

      REPARIERT MIT EINEM ZWEITEN COMMIT, nicht mit `--amend`: dazwischen lag
      schon der Commit des anderen Vorgangs
      ([[amend-am-gemeinsamen-baum-frisst-fremde-commits]]).
    related:
      - amend-am-gemeinsamen-baum-frisst-fremde-commits
      - rotprobe-war-der-test-ohne-die-aenderung-rot
      - resume-bruecke-am-alten-ort
  - id: e18s3-gegengelesen-und-ausgeliefert
    kind: falle
    title: "E18/3 adversarisch gegengelesen: die Trennung haelt auf allen Wegen — aber die gemeinsame Sperrdatei verlor jede vierte Messreihe eine Stelle"
    match: "e18s3 gegenlesen|resumeNacheinander|resume sperrdatei verloren|e18s3-gegenprobe|gemerkte stelle verloren|resume lost update|bruecke upstream fassung"
    sev: high
    tags: ["e18", "mehrbenutzer", "profile", "resume", "bruecke", "sperrdatei", "wettlauf", "sicherung", "gegenlesen", 2026]
    source: "05.08.2026; tools/e18s3-gegenprobe.py (A/B/B0/B1/C), src/backend-api/src/e18s3-gegenlesen.integration.spec.ts (22 Faelle), am Geraet .169 ausgeliefert und nachgemessen. Commits 6f87174f, df253050, de1229f4, 65a578cb."
    cause: >
      Server und die vier root-Skripte teilen sich `/tmp/.resume.lock`, und
      BEIDE Seiten pruefen und setzen sie in ZWEI Schritten (`[ -f ]` +
      `touch` in der Shell, `existsSync` + `writeFileSync` im Server). Sieht
      get_network.sh die Sperre einen Wimpernschlag vor dem Server nicht,
      loescht es sie am Ende seines Blocks — die Sperre des Servers ist weg,
      waehrend er noch schreibt.
    fix: >
      `resumeNacheinander` (server.ts) serialisiert die Schreibvorgaenge
      DIESES Prozesses; /api/weiterhoeren und /api/addresume liegen in
      derselben Kette, die Pruefung auf die Sperrdatei liegt DARIN. Die
      Sperrdatei bleibt als Verabredung mit den Skripten. Reparieren liesse es
      sich nicht in der Shell: jedes Update schiebt die Skripte zurueck.
    body: |
      ── WAS HAELT (und zwar auf jedem Weg, nicht nur auf dem gebauten) ──
      Mit zwei Kindern gegengelesen, an .169 UND im Test: Liam sieht auf
      KEINEM Weg, wo Kalea stehengeblieben ist — `/api/weiterhoeren` leer,
      `/api/resume` 404, `/api/activeresume` `[]`, `/api/gespielt` leer, und
      die DATEI am alten Ort (das, was die root-Skripte sehen) zeigt in
      Liams Bereich. Ein Client kann auch nichts BEHAUPTEN: `?profil=kalea`
      auf /api/resume und /api/weiterhoeren wird ignoriert, `profil` im Rumpf
      von /api/weiterhoeren, /api/addresume und /api/editresume ebenso — alle
      drei nehmen `profilAktiv()`. Eine Kennung mit Pfadanteilen weist
      `POST /api/profil/aktiv` mit 400 ab, ohne die Bruecke zu verstellen.

      DER EINZIGE WEG, auf dem eine Kennung von aussen etwas bewegt, bleibt
      `POST /api/profil/aktiv` — und der ist ungeschuetzt, mit Absicht (ein
      Profil ist eine FIGUR, kein Konto). Wer im LAN an die Box kommt, kann
      also umschalten. Das ist die gebaute Entscheidung, keine Luecke, aber es
      gehoert gesagt.

      ── DER FEHLER, DEN ERST DAS RENNEN ZEIGTE ──────────────────────────
      120 Meldungen an einen ECHTEN Server, waehrend die ECHTEN Netz-Skripte
      ohne Pause danebenliefen, achtmal wiederholt: in VIER von acht Laeufen
      fehlte hinterher eine Stelle, die der Server mit `status: "ok"`
      quittiert hatte. Zwei Steuerlaeufe grenzen den Verursacher ein und sind
      der eigentliche Wert dieser Messung:

        --nur B0   dieselben parallelen Meldungen, KEINE Skripte   0 von 8
        --nur B1   Skripte, die die Sperrdatei nicht anfassen      0 von 8
        --nur B    alles wie auf der Box                           4 von 8

      Es liegt also an der SPERRDATEI, nicht an der Bruecke — und es ist
      AELTER als die Bruecke: vor E18/3 traf es dieselbe Datei box-weit.
      Aufgefallen ist es erst, als jemand mit den echten Skripten dagegen
      gerannt ist. MERKSATZ: eine Sperrdatei, die zwei Parteien in ZWEI
      Schritten pruefen und setzen, ist keine Sperre. Sie sieht nur so aus,
      und sie sieht in jedem einzelnen Durchlauf richtig aus.

      ZWEITER, LEISERER GEWINN DER REPARATUR: vorher prallten gleichzeitige
      Meldungen an der Sperrdatei ab („gesperrt") und waren fuer 15 s
      verloren — von 120 kamen rund 30 durch. Jetzt 114 bis 120. Auf der Box
      melden ZWEI Oberflaechen im Takt; das war kein Randfall.

      NICHT REPARIERT: `/api/editresume`. Es hat den alten Zuschnitt
      (Rueckrufe, und die Sperre faellt schon vor dem Schreiben) — das
      umzubauen ist kein Ausbessern mehr. Es ist ausserdem der seltene Weg,
      ein Mensch, der etwas aendert. OFFEN.

      ── DIE UPSTREAM-FASSUNG DER SKRIPTE ────────────────────────────────
      Die Bruecke ist dafuer gebaut, dass jedes Update mit
      `mv ${MUPI_SRC}/scripts/mupibox/*` die Upstream-Fassung zurueckschiebt.
      Gemessen mit den Dateien aus `5f9c7d8e~1`: sie haelt in beiden
      Netzzustaenden, der Bestand bleibt vollstaendig, `clearresume.sh` und
      `remove_max_resume.sh` nehmen sie genauso mit wie in der Fassung aus dem
      Baum. Was der jq-Fehler von 2024 trifft, ist die OFFLINE-Liste (sie
      bleibt `[]`) — nicht der Bestand. Haengt jetzt in `tools/pruefen.sh`.

      ── AM GERAET GEFAHREN, NICHT GELESEN (.169, 05.08.2026 abends) ─────
      Ausgeliefert mit `tools/ausliefern.py --nur server`, davor eine Kopie
      der Nutzerdaten (`/home/dietpi/config-vor-e18s3.tgz`) und der Stand, den
      der Ausrollweg selbst anlegt. Gemessen NACH dem Neustart:

        * 9 Eintraege vorher, 9 nachher, die Kennungsliste Byte fuer Byte
          gleich (md5 e6fe26c6…). Der Bestand liegt in
          `profile/gast/resume.json`, am alten Ort steht die Bruecke.
        * DIE BOX SETZT FORT. `tools/weiterhoeren-am-geraet.mjs --nr 1`
          gefahren: „Alltag im All" startete bei der gemerkten Stelle
          (84,2 s) und lief; 39 von 39 Ablesungen „laeuft". Danach stand in
          `profile/gast/resume.json` ms=103370 statt 84239 — die neue Stelle
          ist DURCH die Bruecke im Bereich gelandet.
        * Der Verweis war frisch (gleicher Zeitstempel wie sein Ziel), und
          `offline_resume.json` war DANACH neu — die Offline-Liste folgt.
        * Zweites Profil `probe` angelegt und umgeschaltet: alle vier Wege
          leer, `?profil=gast` liefert nichts, Gasts 9 Eintraege unberuehrt.
          Die tote Bruecke heilte aus, als `get_network.sh` `[]` durch sie
          hindurch schrieb. Danach zurueckgedreht: `probe` entfernt,
          `profile/probe/` und `profile.json` von Hand geloescht.

      ── DIE SICHERUNG WAR WIRKLICH BLIND ────────────────────────────────
      Nicht nur behauptet: am Modul mit dem echten Bestand von .169 gemessen,
      `profile/gast/resume.json` landete in `bestandsaufnahme()["unbekannt"]`.
      Die eine Zeile `"profile/*/resume.json"` steht jetzt in
      `mupibox-sicherung.py`; `bestandsaufnahme()` betrat den Bereich ohnehin
      schon (os.walk ueber `tiefe_wurzeln`). Am Geraet nachgewiesen: der Stand
      vom 17:41 enthaelt die 4306 Byte aus dem Bereich, und beide Verweise
      stehen unter `verweise`.

      OFFEN UND DEM BETREIBER VORGELEGT: `resume.json.vorher` (die
      Sicherheitskopie aus `resumeUebernehmen`) und `resume.json.neu`
      (Zwischendatei, bleibt nur nach einem Absturz liegen) stehen in KEINER
      der beiden Listen und werden bei jedem Sicherungslauf als „nicht
      eingeordnet, NICHT gesichert" gemeldet. Ob `.vorher` in die Sicherung
      gehoert oder nach DRAUSSEN, ist eine Entscheidung ueber den Inhalt einer
      Sicherung — sie faellt nicht nebenbei. Bis dahin verschmutzt sie genau
      die Meldung, die man nicht ueberlesen darf.

      ── UND EIN ZWEITER BEFUND, BEIM AUFRAEUMEN GESTOLPERT ──────────────
      `-gt` IST STRIKT, UND `stat --format='%Y'` HAT SEKUNDEN. Beide Skripte
      erzeugen `offline_resume.json` nur neu, wenn der alte Ort ECHT NEUER
      ist. Ein Profilwechsel setzt den Zeitstempel der Bruecke auf JETZT
      (`resumeBrueckeAuffrischen`) — faellt der in dieselbe Sekunde, in der
      auch die Offline-Liste geschrieben wurde, ist `-gt` falsch und die Liste
      bleibt stehen.

      AM GERAET PASSIERT (.169, 05.08.2026, beim Zwei-Profile-Versuch):
      umgeschaltet auf ein Kind ohne Bestand -> get_network.sh erzeugte
      `offline_resume.json` als `[]` -> in derselben Sekunde zurueck auf den
      Gast -> beide Zeitstempel 1785944578, und die Offline-Liste blieb LEER,
      obwohl der Gast 9 Stellen hat. Von Hand geheilt (Datei geloescht,
      get_network.sh gerufen: wieder 634 B, dieselben zwei Eintraege wie
      vorher). Auf einer spielenden Box heilt es der naechste Merktakt (15 s)
      von selbst — auf einer, an der gerade niemand hoert, NICHT. Und
      sichtbar wird es erst ohne Netz.

      NICHT REPARIERT, weil die Wahl dem Betreiber gehoert: die Bruecke eine
      Sekunde in die ZUKUNFT zu datieren machte `-gt` in jedem Fall wahr, ist
      aber ein Zeitstempel, der luegt. Die Offline-Liste beim Profilwechsel zu
      LOESCHEN erzwingt den Neuaufbau, laesst die Box aber bis zu 20 s ohne
      Offline-Liste dastehen. Beides hat einen Preis; keiner davon ist meiner.

      ── EINE FOLGE DES UMZUGS, DIE MAN SEHEN MUSS ───────────────────────
      Der box-weite Bestand geht an den GAST, auch wenn beim Update ein
      ANDERES Kind aktiv ist (gemessen). Verloren ist nichts — aber das Kind,
      das im Augenblick des Updates dran war, steht danach vor einer leeren
      Reihe, bis jemand umschaltet. Auf .169 war der Gast dran; auf einer Box
      mit Profilen waere es sichtbar.
    related:
      - resume-bruecke-am-alten-ort
      - resume-wege-gemessen-e18-stufe3
      - bereich-je-profil-ein-ordner-statt-namenszusatz
      - bereich-gegengelesen-was-noch-box-weit-ist
      - server-antwortet-200-auf-alles

  - kind: falle
    id: farbwaehler-gegengelesen-am-bild
    title: "Farbwaehler gegengelesen: er faerbt wirklich und ueberlebt den Neustart — aber die Kontrastzahl deckte nur eine der beiden Flaechen"
    match: "farbwaehler gegenlesen|farbwaehler-gegenlesen|kontrast nur bg|tor-taste unlesbar|farbfeld schwarz|--surface gleich --ink|farbe ueberlebt neustart|thema ohne neue variablen|aussperren neue oberflaeche"
    sev: high
    tags: [farben, kontrast, neue-oberflaeche, verwaltung, gegenlesen, barrierefreiheit, 2026]
    source: "05.08.2026 adversarisch gegengelesen an .169; tools/farbwaehler-gegenlesen.mjs (23 Punkte, mit --neustart), tools/farbwaehler-verwaltung-probe.mjs (25 Punkte). Commits d94c92e9, 0460f619, 919dd97b."
    cause: >
      Der Waehler meldete `--ink` auf `--bg` und NUR das. `--surface` ist erst
      seit dem Waehler frei stellbar; solange es die ausgelieferte Farbe
      trug, folgte es dem Grund von selbst und eine Zahl reichte. Mit fuenf
      unabhaengigen Reglern reicht sie nicht mehr.
    fix: >
      `schwaechsterKontrast` (frontend-admin/src/app/farben.ts) geht die
      Paarungen durch, auf denen Schrift wirklich sitzt (`SCHRIFTPAARE`), und
      meldet das SCHLECHTESTE mit seinem Ort. Ausserdem bietet `farbFelder()`
      nur noch an, wofuer eine Vorgabe in app.css steht.
    body: |
      Der Bau steht in [[farbwaehler-neue-oberflaeche-eine-quelle]]. Hier
      steht, was das GEGENLESEN ergeben hat — gemessen am BILD, nicht am
      Stilblatt. Der Unterschied ist der Punkt: `getComputedStyle` beweist,
      dass ein Wert ankommt; ob er einen Bildpunkt aendert, beweist nur der
      Schirmabzug. `tools/farbwaehler-gegenlesen.mjs` dekodiert das PNG selbst
      und liest echte Punkte.

      ── WAS HAELT (alles am Geraet .169, 05.08.2026) ────────────────────
      faerbt es       fuenf schreiende Farben gesetzt: alle fuenf am Schirm,
                      der Grund IM BILD #FF00FF (33116 Punkte). Der dunkle
                      Stand blieb auf seinen eigenen Werten, im Bild #1A1726.
      Neuaufbau       zweiter Seitenaufbau: Farbe steht noch.
      NEUSTART DER    `systemctl reboot`: Box kam wieder, Themendatei md5
      BOX             unveraendert, Verweis richtig, Farbe im Bild. Nur
                      `setting_update.sh` fasst `themes/` an, und auch das
                      nur den Verweis.
      fremdes Thema   Umschalten auf `aurora`: ALLE fuenf fallen auf die
                      Auslieferung zurueck. KEINE halb gefaerbte Oberflaeche —
                      und der Grund ist hart: KEINES der 39 mitgelieferten
                      Themen kennt einen der neuen Namen (`grep -l -- --bg`
                      im Themenordner: leer). Es gibt nichts, was sich
                      teilweise ueberlagern koennte.
      Rueckweg        Man KANN sich aussperren (`--bg`=`--ink`=`--surface`:
                      265573 von 384000 Bildpunkten dieselbe Farbe). Die
                      VERWALTUNG bleibt aber lesbar: `www-admin` laedt
                      `active_theme.css` GAR NICHT und benutzt eigene
                      Merkmalsnamen (`--grund`/`--schrift`). Gemessen: Grund
                      rgb(18,18,18) gegen Schrift rgb(242,242,247).
      Zwischenspeicher `/active_theme.css` kommt mit `Cache-Control:
                      public, max-age=0` — jeder Aufbau fragt nach. Eine alte
                      Farbe nach dem Neuladen ist also nicht zu befuerchten.

      ── DIE ZWEI FEHLER, DIE ES GEFUNDEN HAT ────────────────────────────
      1. DIE KONTRASTZAHL DECKTE NUR DEN GRUND. Grund und Schrift auf ihren
         Werten lassen und NUR `--surface` auf die Schriftfarbe ziehen: die
         Anzeige meldete unbeirrt „13,09 : 1 (reichlich)", waehrend auf der
         Box die Seitenleiste ein dunkler Balken war und das PIN-Tastenfeld
         eine leere Flaeche. Die Regel, die es aufdeckt, traegt BEIDE
         Merkmale:

             .tor-taste { background: var(--surface); color: var(--ink); }
             gemessen: Flaeche rgb(46,42,59), Ziffer rgb(46,42,59) -> 1,00 : 1

         Das ist die Stelle, die zwischen dem Kind und den Einstellungen
         steht. Eine Zahl, die daneben „reichlich" sagt, ist SCHLIMMER als
         gar keine — sie war als Netz gedacht und hatte sein Loch genau dort.
         WER EINEN WAEHLER FUER MEHRERE FLAECHEN BAUT, MISST JEDE PAARUNG,
         AUF DER SCHRIFT SITZT, und meldet die schlechteste.

      2. EIN FARBFELD OHNE VORGABE STAND AUF SCHWARZ. Der Schutz „ohne
         Vorgaben kein Waehler" war ein Alles-oder-nichts: er greift, wenn
         app.css GAR NICHT lesbar ist. Fehlt nur EINE der fuenf Variablen,
         fiel `farbe()` still auf `'#000000'` zurueck — mit „wie
         ausgeliefert" daneben, und der erste Griff haette Schwarz in die Box
         geschrieben. Kein ausgedachter Fall: `--line` stand zweimal in
         app.css und wurde null Mal gelesen; Merkmalsnamen dieser Oberflaeche
         kommen und gehen.

      ── WAS OFFEN BLIEB ─────────────────────────────────────────────────
      * `--muted`, `--hl`, `--rowBg`, `--pill` und die uebrigen elf gehen NICHT
        mit. Auf dem Schirmabzug mit gruener Flaeche stehen die Beschriftungen
        der Seitenleiste (`--muted` #8B8397) und die Kachel „Alles" (`--hl`
        creme) unveraendert da. Das ist gewollt (fuenf Regler statt sechzehn),
        sieht aber wie ein halber Anstrich aus, sobald man kraeftige Farben
        waehlt. NICHT GEMESSEN, wie das mit gedaempften Farben wirkt.
      * NICHT GEMESSEN: was ein MuPiBox-Update mit `/home/dietpi/MuPiBox/themes/`
        macht. Auf .169 ist das kein git-Baum und nur `setting_update.sh` fasst
        ihn an — aber der Installationsweg wurde nicht ausgefuehrt.
      * `--accent` steht im dunklen Block von app.css GAR NICHT. Eine im
        Hellen gewaehlte Akzentfarbe wirkt dunkel deshalb nicht; dunkel
        behaelt #FF6B57.
    related:
      - farbwaehler-neue-oberflaeche-eine-quelle
      - thema-erreicht-die-neue-oberflaeche-nur-mit-important
      - server-antwortet-200-auf-alles
      - drei-orte-eine-anzeige
      - mupi-themawechsel-teilstring

  - kind: falle
    id: reset-am-gemeinsamen-baum-frisst-fremde-commits
    title: "`git reset HEAD~n` frisst am gemeinsamen Baum denselben fremden Commit wie `--amend` — nur leiser"
    match: "reset --soft HEAD~2 fremder commit|reflog fremden commit zurueckholen|commit verschwunden gemeinsamer baum|reset gemeinsamer baum"
    sev: high
    tags: ['git', 'zusammenarbeit', 'falle', 2026]
    source: "05.08.2026 selbst passiert und am Reflog wieder eingesammelt"
    body: |
      [[amend-am-gemeinsamen-baum-frisst-fremde-commits]] warnt vor `--amend`.
      `git reset --soft HEAD~2` ist derselbe Fehler mit anderem Namen und
      faellt SPAETER auf: zwischen dem eigenen Commit und dem `reset` hatte ein
      anderer Vorgang committet. `HEAD~2` zaehlte deshalb nicht die zwei
      eigenen, sondern einen eigenen und den FREMDEN — und der war weg.

      GEMERKT hat man es nur daran, dass `git log --oneline -2` nach dem Reset
      einen anderen Commit oben zeigte als erwartet. Das ist duenn. Wer am
      gemeinsamen Baum zurueckgeht, nennt den ZIELCOMMIT beim Namen
      (`git reset <sha>`), nicht `HEAD~n`.

      ZURUECK GEHT ES UEBER DAS REFLOG, und zwar vollstaendig: `git reflog`
      zeigt den fremden Commit noch, `git reset <sha>` (gemischt, nicht hart)
      setzt HEAD und Index zurueck und laesst den Arbeitsbaum in Ruhe — die
      eigenen, noch nicht festgeschriebenen Aenderungen ueberstehen das.

      UND DIE LEHRE DARUEBER: der Grund fuer den Reset war, zwei Commits neu
      zu schneiden, weil `git diff` zwei benachbarte Aenderungen in EINEN Hunk
      gelegt hatte ([[eigene-hunks-verschluckt-den-nachbarn]], derselbe Tag,
      zweites Mal). Beides zusammen heisst: am gemeinsamen Baum schneidet man
      Hunks VOR dem ersten Commit sauber — hinterher ist der Weg zurueck
      teurer, als die schoenere Reihenfolge wert ist.
    related:
      - amend-am-gemeinsamen-baum-frisst-fremde-commits
      - eigene-hunks-verschluckt-den-nachbarn

  - kind: falle
    id: vorgabewert-ueberlebt-den-umzug-seines-ordners
    title: "Ein Vorgabewert, der zu einem Pfad wird, ueberlebt den Umzug des Ordners — und zeigt danach ins Leere"
    match: "figur zeigt ins leere|vorgabewert alter ordner|gast figur mixpi-hoert|dateiname aus alter fassung 404"
    sev: medium
    tags: ['box', 'server', 'profile', 'wanderung', 'falle', 2026]
    source: "05.08.2026, E18 Stufe 4 — am Geraet gemessen (curl .169:8200/api/profile)"
    body: |
      `GAST_FIGUR = 'mixpi-hoert.png'` war der Vorgabewert des Gastprofils,
      und der Name wurde zu `/neu/bilder/<figur>`. Am 05.08.2026 zogen die
      waehlbaren Figuren nach `bilder/figuren/` um. Der Name blieb — und zeigt
      seitdem ins Leere.

      ER KOMMT DURCH JEDE PRUEFUNG. `figurPruefen` ist eine REINE Funktion und
      kann gar nicht wissen, ob es die Datei gibt: klein geschrieben, endet auf
      `.png`, kein Pfadanteil — durch. Nur der Server sieht auf der Platte
      nach, und der tat es bis dahin nur beim SETZEN.

      GEMESSEN, NICHT VERMUTET: `curl 192.168.178.169:8200/api/profile` gab an
      dem Tag `{"kennung":"gast","figur":"mixpi-hoert.png"}` zurueck. Auf
      Platte lag noch keine `profile.json` — der Wert kam frisch aus dem Code.
      Wer aber vorher EIN Profil anlegt, schreibt ihn fest, und dann ueberlebt
      er die Aktualisierung.

      WAS DIE WANDERUNG NICHT SEIN DARF: ein Sonderfall fuer diesen einen
      Dateinamen. Die Regel ist die allgemeine — `GET /api/profile` nennt nur
      Figuren, zu denen es eine Datei gibt. Damit verschwindet auch ein PNG,
      das der Betreiber spaeter loescht, statt als toter Verweis stehenzubleiben.

      UND SIE GEHT NIE AUF PLATTE. Waere der Bilderordner einmal nicht lesbar
      (mitten in einer Auslieferung etwa), loeschte ein Schreiben die Wahl
      JEDES Kindes unwiderruflich. Nur in der Antwort auszulassen kostet
      denselben Fall eine Silhouette fuer eine Anfrage.

      DASS KEINE WAHL VERLOREN GEHT, ist nachgesehen und nicht angenommen: in
      der Fassung davor gab es weder `/api/figuren` noch
      `POST /api/profil/figur` noch ein Feld in der Verwaltung. NIEMAND konnte
      je eine Figur waehlen, also ist jeder gespeicherte Wert die Vorgabe.
    related:
      - attrappe-luegt-durch-weglassen
      - server-antwortet-200-auf-alles
      - wiki-zustand-hat-haltbarkeit

  - kind: entscheidung
    id: laut-fenster-schlaegt-kategorienknopf
    title: "Der Reglerweg im Laut-Fenster wiegt schwerer als 18 px an einem Kategorienknopf"
    match: "radio kategorie 7.56 mm|laut-fenster verdeckt leiste|beruehrziel regression leiste|22 statt 21 zu kleine ziele"
    tags: ['box', 'oberflaeche', 'beruehrziele', 'leiste', 'abwaegung', 2026]
    source: "05.08.2026 — tools/beruehrziele-neu.mjs gegen HEAD gemessen, Arbeitskopie ueber git worktree"
    body: |
      Damit oben links Platz fuer das Zeichen „wer hoert" entsteht, rueckte der
      Kategorienblock der Leiste um `--griff + 16` px nach unten. GEMESSEN,
      was das kostet: auf dem Schirm mit OFFENEM Laut-Fenster sank die letzte
      Kategorie „Radio" von 9,24 auf 7,56 mm (Rechteck 72x66, getastet 54x67).
      Im ganzen Haus 22 statt 21 zu kleine Ziele.

      DER GRUND STEHT FEST: `#mp-laut-fenster` steht `fixed` bei x 48..752 und
      deckt die Leiste ab x 48. Ein Tipp auf den verdeckten Teil tut NICHTS —
      der Zuhoerer fuer „Danebentippen schliesst" steigt bei `#mp-laut-fenster`
      ausdruecklich aus.

      GENAUER GEMESSEN (05.08.2026 abends): DAS FENSTER IST KEIN RECHTECK. Es
      traegt `border-radius: 999px`, seine linke Kante ist also ein HALBKREIS
      (r 29 um y 357). Wie viel ein Kategorienknopf verliert, haengt deshalb
      daran, wie nah seine Mitte an y 357 liegt: 25,8 px entfernt bleiben
      55,8 px frei, 2,8 px entfernt nur noch 40,1. DARAUS FOLGT ETWAS
      PRAKTISCHES: Jede senkrechte Verschiebung dieses Blocks ist teuer, auch
      eine um bloss 23 px — sie schiebt „Radio" von der duennen Backe in die
      dicke. Wer hier plant, rechnet nicht mit einer Kante, sondern mit einem
      Kreis.

      ES BLEIBT TROTZDEM SO. Das Fenster liesse sich von 704 auf 624 px
      verschmaelern, dann gaebe es die Leiste frei. Genau diese Laenge ist aber
      selbst eine Behebung: mit 190 px war „kleiner Hub, grosse Wirkung" die
      Meldung vom Geraet. Ein dauerhaft ungenauer Regler ist teurer als ein
      Knopf, der 18 px schmaler ist, solange ein Fenster offen steht, das man
      ohnehin gleich wieder zumacht.

      WIE MAN SO ETWAS UEBERHAUPT MERKT: Die Gesamtzahl allein sagt es nicht.
      Gemessen wurde gegen HEAD in einer losgeloesten Arbeitskopie
      (`git worktree add --detach`, `node_modules` per Symlink) — 21 dort, 22
      hier, und die Differenz war benennbar. Seit demselben Tag sagt
      `beruehrziele-neu.mjs` auch WODURCH ein Ziel schrumpft: es tastet den
      Punkt, an dem der Lauf innerhalb des eigenen Rechtecks stehenbleibt, und
      nennt die Ebene, die dort liegt.
    related:
      - entwurf-gibt-gliederung-nicht-masse
      - rueckweg-verdeckt-die-ueberschrift
      - vorschau-wird-geliehen

  - kind: falle
    id: ein-wunsch-zwei-zielorte-ein-gebautes-ziel
    title: "Ein Satz nannte ZWEI Zielorte — gebaut wurde EINER, und zwei Tage lang sah das richtig aus"
    sev: mittel
    match: "gans links in die ecke|zwei zielorte|wunsch falsch gelesen|mixpi figur oben links|wappen bild umzug|damals ging was schief"
    cause: 'ein Wunschsatz nannte zwei Dinge mit je eigenem Zielort (das BILD „ganz links in die Ecke“, der NAME „links unten“); gelesen wurde er als EIN Zielort fuer beides, und das Bild wanderte mit dem Namen'
    tags: ['anforderung', 'kommunikation', 'oberflaeche', 'neu', 'falle', 2026]
    source: "03.-05.08.2026 — Berichtigung des Betreibers, danach gemessen mit tools/leiste-geometrie-messen.mjs und tools/beruehrziele-neu.mjs"
    body: |
      DER WUNSCH, woertlich (Betreiber, 03.08.2026):

        „das mixpi bild soll nicht mehr wechseln beim spielen sondern statisch
         beim standart bleiben und GANS LINKS IN DIE ECKE RUTSCHEN. MixPiBox
         soll geteilt werden zu MixPi und Box und untereinander stehen LINKS
         UNTEN und ganz unten eine laufende versionsnr klein"

      DIE BERICHTIGUNG, zwei Tage spaeter:

        „ich wollte niemals die mixpi figur unten links, ich wollte sie oben
         links, schon vorbereitend fuer den platz des bildes. damals ging was
         schief :)"

      WAS SCHIEFGING: Der Satz nennt DREI Dinge (Bild, Name, Fassung) und
      ZWEI Zielorte. „Gans links in die ecke" gehoert zum BILD, „links unten"
      zum NAMEN. Gebaut wurde es, als meinten beide Ortsangaben dieselbe
      Stelle: Bild und Name zogen gemeinsam nach unten links und wurden dort
      zum „Wappen" der Box. Gemeint war die Ecke OBEN links — ausdruecklich
      als Vorbereitung fuer den Platz, an dem zwei Tage spaeter das Zeichen
      „wer hoert" entstand.

      WARUM ES NICHT AUFFIEL, UND DAS IST DER TEURE TEIL: Das Ergebnis war in
      sich stimmig. Es sah gut aus, es war begruendet, es war gemessen, und
      es bekam ein eigenes Werkzeug (tools/rueckweg-schau.mjs prueft seitdem
      die Bildgroesse im Wappen). Um die falsche Stelle herum wuchsen zwei
      Tage lang Kommentare, die sie BEGRUENDETEN — in app.js stand „das Bild
      ist mit dem Umzug in die Seitenleiste zum WAPPEN der Box geworden", in
      app.css „die Stelle kostet nichts", im Wissenspaket eine Ursache, die
      das Wappen als „Bild + Name + Fassung" beschrieb. NICHTS DAVON WAR
      FALSCH — ausser dem Ort. Eine falsch verstandene Anforderung erzeugt
      keine Fehlermeldung; sie erzeugt Belege.

      DIE ERKENNBARE WARNUNG GAB ES: Der Entwurf des Betreibers
      (NewDesign/MixPiBox-standalone.html) zeichnet in der linken Leiste GANZ
      OBEN einen runden 60x60-Benutzerknopf und ein MixPi oben neben dem
      Titel — unten links steht dort NICHTS dergleichen, weder Boxname noch
      Fassung. Wer den Entwurf neben den Satz gelegt haette, waere gestolpert.
      Siehe [[entwurf-gibt-gliederung-nicht-masse]]: Der Entwurf taugt nicht
      fuer Masse, aber sehr wohl fuer die Frage „wo gehoert es hin".

      DIE LEHRE, und sie ist billig: EIN SATZ MIT MEHREREN SUBJEKTEN HAT
      MOEGLICHERWEISE MEHRERE ZIELORTE. Bevor gebaut wird, den Satz in seine
      Teile zerlegen und JEDEM Teil seinen Ort zuordnen — schriftlich, in der
      Rueckfrage oder im Kommentar. Wo eine Ortsangabe fuer zwei Dinge
      gleichzeitig gelten soll, ist das eine Annahme und keine Anforderung.
      Und Annahmen ueber Anforderungen gehoeren zurueckgefragt, nicht
      hingeschrieben.

      WAS DIE BERICHTIGUNG DANN KOSTETE — nicht das Verschieben, das Aufraeumen
      DAHINTER. Das 44-px-Bild aus dem Wappen zu nehmen macht es 46 px kuerzer
      (44 Bild + 2 gap). Diese 46 px fielen der Kategorienliste zu, und die
      richtete mit `justify-content: safe center` aus: sie haette den Zugewinn
      auf beide Seiten geteilt und alle vier Kategorien 23 px NACH UNTEN
      geschoben — in die runde Backe des Lautstaerke-Kissens. GEMESSEN faellt
      „Radio" dabei von 7,56 auf 5,60 mm. DIE ANZAHL DER ZU KLEINEN ZIELE
      MELDET DAS NICHT: 22 von 193, vorher wie nachher. Mit `flex-start`
      bleibt jeder Knopf auf ±0,2 px stehen und „Radio" bei 7,56 mm.
      Eine Berichtigung ist also nicht das Rueckgaengigmachen einer Zeile,
      sondern das Nachziehen von allem, was sich inzwischen daran angelehnt
      hat: sechs Kommentarbloecke, vier Wiki-Eintraege, zwei Messwerkzeuge und
      eine Ausrichtung, die vorher folgenlos war.
    related:
      - entwurf-gibt-gliederung-nicht-masse
      - wiki-zustand-hat-haltbarkeit
      - laut-fenster-schlaegt-kategorienknopf
      - wappen-hiess-marke-und-kollidierte
      - vorgabewert-ueberlebt-den-umzug-seines-ordners

  - kind: entscheidung
    id: leerer-wert-heisst-nicht-leere-anzeige
    title: 'Ein leerer Wert heisst nicht „kein Bild zeigen“ — der Server kennt nur seinen Ordner'
    match: "OHNE_FIGUR|leerer vorgabewert|kein bild gewaehlt|ichBildPfad|standardbild oberflaeche|vorgabe im backend"
    tags: ['box', 'server', 'oberflaeche', 'neu', 'profile', 'zustaendigkeit', 2026]
    source: "05.08.2026 — gebaut und gemessen gegen tools/neu-vorschau.mjs auf eigenem Port"
    body: |
      DIE FRAGE: Ein Kind hat sich noch kein eigenes Bild ausgesucht. Was steht
      dann im Zeichen „wer hoert" oben links?

      NICHT NICHTS. Seit dem 05.08.2026 steht dort das MixPi — der Betreiber
      hat diese Ecke ausdruecklich als „platz des bildes" bezeichnet
      ([[ein-wunsch-zwei-zielorte-ein-gebautes-ziel]]), und
      `mixpi-hoert.png` ist laut NewDesign/maskottchen.json „das Standardbild".

      UND DIE VORGABE STEHT TROTZDEM NICHT IM SERVER. `OHNE_FIGUR` bleibt die
      LEERE Zeichenkette. Das ist keine Bequemlichkeit, sondern eine Frage der
      Zustaendigkeit:

        DER SERVER WEISS, WAS IN `bilder/figuren/` LIEGT — er liest den Ordner
        und nennt in `GET /api/profile` nur Figuren, zu denen es eine Datei
        gibt. Er weiss NICHT, welche Bilder die OBERFLAECHE mitbringt; die
        sind mit ihr ausgeliefert worden und tauchen in seinem Ordner nie auf.

      WER DIE VORGABE TROTZDEM INS BACKEND SCHREIBT, baut eine 404: Jeder Wert
      dieses Feldes wird in der Oberflaeche zu `<figurordner>/<wert>`, also zu
      `bilder/figuren/mixpi-hoert.png` — und das gibt es nicht. Die eigene
      Wanderung des Servers striche den Wert ausserdem sofort wieder weg. Genau
      diese Falle ist an dieser Box schon einmal zugeschnappt:
      [[vorgabewert-ueberlebt-den-umzug-seines-ordners]].

      DIE REGEL LAUTET DESHALB: Der Server sagt, WAS ES GIBT. Die Oberflaeche
      entscheidet, WAS SIE ZEICHNET, wenn es nichts gibt. Ein leerer Wert ist
      eine Aussage ueber die Daten, keine Anweisung an die Anzeige.

      DREI LAGEN, DIE VERSCHIEDEN AUSSEHEN MUESSEN — und das ist der Grund,
      warum das ueberhaupt aufgeschrieben gehoert:
        „nichts gewaehlt"        → das MixPi (`ichBildPfad` gibt die Vorgabe)
        „gewaehlt, laedt nicht"  → die Silhouette (`onerror`)
        „gewaehlt, laedt"        → die Figur des Kindes
      Wer die Vorgabe auch bei einem KAPUTTEN Figurnamen einspringen laesst,
      legt die ersten beiden zusammen. Dann sieht „das Bild dieses Kindes ist
      verschwunden" aus wie „es hat sich halt nichts ausgesucht", und niemand
      sucht je nach der fehlenden Datei.

      DIE SILHOUETTE IST SEITDEM DER NOTFALL, NICHT DER NORMALFALL. Der
      Kommentar an ihr behauptete zwei Tage lang das Gegenteil („es gibt heute
      genau EIN MixPi, also ist das nicht der Ausnahme-, sondern der
      Normalfall") — richtig, solange bei leerer Wahl nichts dastand. Ein
      Kommentar, der seinen eigenen Normalfall falsch benennt, ist teurer als
      keiner: Der Naechste plant mit ihm.

      GEPRUEFT WIRD DIE REINE REGEL (`ichBildPfad`, ueber
      tools/neu-regeln-auszug.py in tools/pruef-neu-regeln.js) UND die Wirkung
      am Schirm (tools/ich-zeichen-schau.mjs, Abteilung 7). Zwei Eingriffe in
      tools/rotprobe.py faerben beides rot: einer nimmt die Vorgabe zurueck,
      einer schickt sie durch den Figurenordner.
    related:
      - vorgabewert-ueberlebt-den-umzug-seines-ordners
      - ein-wunsch-zwei-zielorte-ein-gebautes-ziel
      - attrappe-luegt-durch-weglassen
      - mixpi-maskottchen-familie
  - kind: howto
    id: audit-2026-08-05-delta
    title: "Delta-Audit 05.08.2026 — Update-Quelle zeigt auf Upstream, zwei resume-Restloecher"
    source: "AUDIT-2026-08-05.md im Fork-Repo; 6 Pruef-Dimensionen + adversariale Gegenpruefung (8 von 9 bestaetigt, 1 widerlegt)"
    tags: [audit, update, resume, mehrbenutzer, perf, 2026]
    body: |
      GEPRUEFT: das Delta seit b3ce4fa4 (203 Commits, ~80k Zeilen — E18-Profile,
      F1/F2, Google-TTS-Abloesung, Sicherung, Farbwaehler). Messlaeufe: 1398
      backend-api + 146 player + 26 e2e Tests gruen, tsc sauber, biome 83 F.
      Volltext: AUDIT-2026-08-05.md im Repo.

      DIE DREI HOCH-BEFUNDE (alle adversarial bestaetigt):

      1. DAS UPDATE-SKRIPT LAEDT WEITER DAS UPSTREAM-PAKET (splitti/MuPiBox,
         start_mupibox_update.sh:141). Gegen Upstream nachgemessen: alle
         Nachreichungs-Bloecke des Forks laufen ins Leere (scripts/box/,
         E29-Units, kioskwache … gibt es dort nicht, mv scheitert still nach
         >&3), und was es Upstream GIBT, ueberschreibt die Fork-Fixes
         (check_network-jq-Fix, mqtt-Injection-Fix, deploy.zip ersetzt die
         ganze E18-App). Ein gruen aussehendes Update baut die Box still auf
         Upstream zurueck. Fix: version.json/MUPIBOX_URL auf den Fork +
         Fork-Marker-Tor (fehlt scripts/box/ im Paket -> Abbruch). Auch
         conf_update.sh laeuft AUS DEM ZIP (Z. 962) — die reparierte
         Fork-Fassung laeuft beim echten Update nie.

      2. PROFILWECHSEL ZIEHT DEN RESUME-STAND DES VORIGEN KINDES MIT
         (server.ts:6454): erst wird `aktiv` umgeschaltet, DANN
         resumeBrueckeRichten() — liegt am alten Ort eine echte Datei (der
         gemessene Normalfall nach remove_max_resume/clearresume), landet
         Kind As Stand in profile/<B>/resume.json. Fix: Uebernahme VOR dem
         Umschalten, danach Bruecke neu.

      3. /api/editresume IST DER DRITTE SCHREIBER von resume.json und liegt
         NICHT in resumeNacheinander (server.ts:8952): Sperre faellt vor dem
         Schreiben, Direktschreiben ohne Zwischendatei. Der klassische Player
         schreibt darueber alle 30 s — der df253050-Verlust (4 von 8) lebt
         ueber diesen Weg weiter.

      GEPRUEFT UND WIDERLEGT (nicht nochmal suchen): die festen Zwischennamen
      .bruecke/.uebernommen sind KEIN Wettlauf — der ganze Block ist synchron,
      die Event-Loop serialisiert; eindeutige Tmp-Namen braucht nur das
      ASYNCHRONE kzSchreiben. Und ZWEI zuerst vorgeschlagene Fixes waeren
      schaedlich gewesen: 15-s-Timeout auf btTun (Koppeln braucht serverseitig
      bis ~105 s) und tonmaschineUebernehmen in sprichText (pausierte Spotify
      ohne Resume; der Normalweg spricht absichtlich UEBER die Musik).

      MERKPOSTEN aus Speed/Perf/Design: resume.json wird jetzt alle 15 s
      geschrieben (~240 Zyklen/Hoerstunde auf die SD), kein Negativ-Cache bei
      der Titel-Aufloesung (Fehlschlag -> alle 15 s bis zu 20 Spotify-Seiten
      neu), Boot-Splash rechnet 20 fps per-Pixel-Python in der Boot-Minute,
      NewDesign +65 % (gzip 213 kB) weiter unkomprimiert, der Suchbestand-Test
      der Verwaltung ist ROT (verwaltung-suchbestand.mjs --schreiben fehlt),
      und das Ich-Zeichen umgeht sprichDann + hat kein aria-expanded. Das
      66-px-Ziel dagegen ERFUELLT das neue Zeichen (9,24 mm, 95 -> 21
      Unterschreiter, gemessen).

      RANGLISTE 03.08. NACHGESEHEN: 4 erledigt (librespot-Ausrollweg,
      mupi-variablen-abgleich, M1-M3, F1/F2), 1 teilweise (fetch ohne signal
      12 -> 10, davon 4 NEUE im Spotify-Block 9095/9225/9254/9305), 7 offen —
      darunter der ganze Billig-Block (npm update 20.3.25+, cors(),
      json()-Frist, --target=node22, compression+maxAge, deploy-Check
      www/neu). Der steht seit zwei Audits.
    related:
      - audit-2026-07-27-darstellung
      - resume-wege-gemessen-e18-stufe3
  - kind: falle
    id: sperr-vorgabe-wirkt-nicht-am-standard
    title: >-
      Die Vorgabe der Einstellungssperre steht an drei Orten — und der
      naheliegende ist keiner davon
    source: >-
      E31/P1, 06.08.2026 — tools/tor-kette-messen.mjs und
      tools/eltern-tor-schau.mjs gegen eine eigene Vorschau; Konfiguration der
      Box .169 lesend ueber SSH
    tags:
      - einstellungssperre
      - eltern-bereich
      - konfiguration
      - vorgabewert
      - falle
      - 2026
    body: |
      DER AUFTRAG LAUTETE: „eine frische Box darf nicht offen stehen". Der
      naheliegende Griff ist `standard: 'aus'` -> `'rechnen'` am Feld
      `einstellungssperre` in src/backend-api/src/konfiguration.ts. Er bewirkt
      NICHTS — und er ist schlimmer als nichts.

      WARUM ER NICHTS BEWIRKT, gemessen und nicht geschlossen:
        * `standard` wird ausschliesslich von `holeWertOderStandard`
          eingesetzt, und das hat genau EINEN Aufrufer: `feldNachAussen`.
          Dessen einziger Aufrufer ausserhalb der Tests ist
          `GET /api/konfiguration` — der Weg, den das Verwaltungs-Board liest.
        * Die BOX liest `GET /api/config` (server.ts). Der Weg gibt die
          Konfigurationsdatei ROH heraus (nur `ohneGeheimnisse`), kennt keinen
          Feldkatalog und setzt keine Vorgabe ein. Was in der Datei fehlt,
          fehlt in der Antwort.
        * AN DER BOX .169 BELEGT: `mupibox.einstellungssperre` fehlt in
          /etc/mupiboxconfig.json GANZ, und `/api/konfiguration` meldet
          trotzdem `wert: "aus"`.
      Wer also nur den `standard` dreht, bekommt ein Board, das
      „Rechenaufgabe" ANZEIGT, waehrend jedes Kind hineinkommt — eine
      Attrappe, die durch Anzeigen luegt. Verwandt mit
      [[board-feld-ohne-schluessel-auf-der-box]], das diesen `standard`
      eingefuehrt hat, mit der ausdruecklichen Begruendung, der Wert sei
      „der, der ohnehin gilt". Genau das ist er nicht mehr, sobald die
      Leseseite etwas anderes tut.

      DIE DREI ORTE, DIE WIRKLICH ENTSCHEIDEN — und keiner allein genuegt:
        1. FRISCHE BOX: config/templates/mupiboxconfig.json schreibt den
           Schluessel EXPLIZIT aus, autosetup.sh schiebt die Vorlage an die
           Stelle der Konfiguration. Ein gesetzter Wert gewinnt gegen jeden
           `standard`. Eine frische Box faellt also gar nicht in den Fall
           „Schluessel fehlt".
        2. BESTEHENDE BOX OHNE DEN SCHLUESSEL: die Ruecknahme beim LESEN,
           `sperrModus()` in NewDesign/app.js (und wortgleich `alsModus()` in
           src/frontend-box/.../sperrlogik.ts). Hier wird aus „fehlt" ein
           Modus.
        3. DIE ANZEIGE: der `standard`. Er muss MITGEZOGEN werden, damit das
           Board nicht das Gegenteil dessen behauptet, was die Box tut — aber
           er gehoert in denselben Hunk, nicht in einen eigenen.
      VIERTER ORT, ausserhalb: update/conf_update.sh schreibt bei jedem Update
      „aus" in jede Box, in der der Schluessel fehlt. Solange das so bleibt,
      macht das naechste Update jede Reparatur an 2. wirkungslos — der Wert
      STEHT dann da, und ein stehender Wert gewinnt.

      ZWEI FAELLE, DIE VORHER IN EINEM `catch` LAGEN, und das war der Kern:
      „Schluessel fehlt in einer GELESENEN Datei" und „Datei gar nicht lesbar"
      hatten dieselbe Antwort. Der erste ist der offene Fall jeder alten Box,
      der zweite der Vertrag „niemand sperrt sich selbst aus". Sie muessen
      auseinander, bevor an der Vorgabe etwas gedreht wird.

      WARUM DIE VORGABE `rechnen` IST UND NICHT `pin`: In der
      Installationsvorlage ist `einstellungsPin` leer. Das Backend vergleicht
      dann gegen einen leeren Hash und sagt auf JEDE Eingabe nein — mit drei
      Versuchen nachgestellt, alle abgewiesen. `pin` als Vorgabe SPERRT AUS,
      statt aufzufallen, und ausgerechnet das WLAN, mit dem man die Box wieder
      aufmachen muesste, laege dahinter. Die Bremse in `pruefeKonfig`
      (verweigert „pin" ohne PIN) greift NUR auf dem Weg ueber
      `POST /api/konfiguration`; eine von Hand geaenderte Datei, ein
      Migrationsskript oder ein Wiederherstellen aus einer Sicherung kommt an
      ihr vorbei. Eine Rechenaufgabe dagegen braucht keine Einrichtung, kein
      Backend und keine PIN.

      WIE MAN ES PRUEFT, OHNE ZU GLAUBEN: tools/neu-vorschau.mjs kannte bis
      zum 06.08.2026 `sperre-aus|rechnen|pin|kaputt`, aber KEINE Lage OHNE den
      Schluessel — „aus" und „fehlt" sind zwei verschiedene Aussagen, und die
      wichtigere war nicht stellbar. Seither gibt es `sperre-fehlt` (Schluessel
      weg) und `sperre-leer` (Schluessel da, leer).
      `node tools/eltern-tor-schau.mjs` geht alle sieben Lagen durch und misst
      am SCHIRM, nicht am Quelltext.
    related:
      - board-feld-ohne-schluessel-auf-der-box
      - attrappe-luegt-durch-weglassen
      - drei-orte-eine-anzeige
  - kind: entscheidung
    id: eltern-geste-vier-ecken
    title: >-
      Die Geste vor dem Eltern-Bereich: vier Ecken im Uhrzeigersinn — und was
      dagegen alles schon vergeben war
    source: >-
      E31/P1, 06.08.2026 — tools/gesten-inventur.mjs (41 Zuhoerer erhoben),
      tools/eltern-tor-schau.mjs (45 Aussagen am Schirm gemessen)
    tags:
      - eltern-bereich
      - einstellungssperre
      - geste
      - bedienung
      - entscheidung
      - 2026
    body: |
      ENTSCHIEDEN (Betreiber, 05.08.2026): „Geste UND Zahlenfolge, waehlbar."
      `einstellungssperre` hat seit dem 06.08.2026 eine vierte Stufe `geste`
      neben `aus`, `rechnen` und `pin`.

      DIE GESTE IST: die vier Ecken der gesperrten Flaeche nacheinander
      antippen, im Uhrzeigersinn beginnend links oben. Zwischen zwei Ecken
      hoechstens vier Sekunden; jede Beruehrung daneben, jede falsche Ecke und
      jede zu lange Pause faengt die Folge von vorn an. Eine falsche Ecke, die
      zufaellig die ERSTE ist, faengt bei eins neu an — sonst braeuchte ein
      Elternteil nach einem Vertipper einen Leertipp dazwischen und wuesste
      nicht, warum es nicht geht.

      DIE DREI BEDINGUNGEN DES AUFTRAGS, und wie sie eingeloest sind:
        1. NICHT DURCH HERUMTIPPEN. Die vier Ecken decken zusammen 19,2 % der
           Flaeche (120 px Kante auf 768x390); ein zufaelliger Tipp trifft die
           JEWEILS richtige mit rund 1 : 21, vier hintereinander mit rund
           1 : 180 000. Gemessen wurden ausserdem sechs falsche Folgen — gegen
           den Uhrzeigersinn, an der falschen Ecke begonnen, viermal dieselbe
           Ecke, sechsmal in die Mitte, mit einem Tipp dazwischen, mit einer
           Ecke doppelt. Keine oeffnet.
        2. OHNE ANLEITUNG WIEDERHOLBAR. „Die vier Ecken im Uhrzeigersinn" ist
           ein Satz, kein Handgriff. Die Ziele sind 120 px = 16,8 mm, fast das
           Doppelte der 9-mm-Marke, und man zielt in eine ECKE, nicht auf einen
           Punkt. Einhaendig. Der Fortschritt steht in der Mitte des Schirms,
           in derselben Anzeige wie PIN und Rechenaufgabe.
        3. KAPERT NICHTS. Der Zuhoerer haengt am `#eltern-tor` — ausserhalb
           des stehenden Tors gibt es die Geste GAR NICHT. Und die Ecken sind
           GEOMETRIE, keine Knoepfe: vier unsichtbare Elemente im Baum waeren
           vier Beruehrziele, die in jeder Messung als unbeschriftete
           Bedienelemente staenden.

      WAS IN NewDesign SCHON VERGEBEN IST (erhoben mit
      tools/gesten-inventur.mjs, 41 Zuhoerer) — damit der Naechste nicht wieder
      sucht:
        * `#einst-knopf`, 700 ms Halten -> Eltern-Bereich (`elternEinstieg`);
          kurzer Tipp -> /settings, danach gebremst.
        * Randwisch am `document` (capture, passive): nur bei eingefahrener
          Leiste UND `clientX <= 40`, loest ab 64 px waagerecht aus, verworfen
          ab 48 px schraeg. Er verlangt einen ZUG — ein Tipp ohne Bewegung
          loest ihn nicht aus.
        * Zwei `click`-Zuhoerer am `document`: das Laut-Fenster schliessen und
          den Rueckweg nachziehen. Beide verbrauchen keine Geste.
        * 30 weitere `click`-Zuhoerer, KEINER mit Zeitmessung.

      ZWEI VERBREITETE IRRTUEMER, beide gemessen widerlegt:
        * „Langes Halten auf einer Kachel und auf dem Cover ist vergeben."
          Das gilt fuer src/frontend-box/ (LongPressDirective: 2000 ms ->
          Einstellungen, 500 ms -> Vorlesen). In NewDesign liegt das Vorlesen
          auf `click` (`sprichDann`: erster Tipp liest, zweiter spielt) —
          langes Halten auf einer Kachel ist dort FREI.
        * „`contextmenu` und Mehrfinger sind frei." Im Quelltext ja: kein
          `dblclick`, kein `contextmenu`, kein einziges `touch*` in NewDesign.
          Ob Chromium im Kiosk `contextmenu` ueberhaupt feuert, ist NICHT
          gemessen, und der Browser selbst belegt zwei Finger fuer Zoom und
          Rollen. Eine Sperre auf eine Vermutung zu stellen war der Grund,
          beides zu verwerfen.

      WO DIE GESTE STEHT, UND WO NICHT. Der Schirm der Box sagt „Mit einer
      Geste gesperrt" und „Die Geste steht in der Verwaltung unter ‚Zugang und
      Sperren'" — er sagt NICHT, welche. Ein Tor, das seine eigene Loesung
      anschreibt, ist keine Sperre; ein Tor, das gar nichts sagt, sperrt den
      zweiten Elternteil aus. Die Beschreibung steht vollstaendig im `hinweis`
      des Feldes.
      OFFENER PUNKT DAZU: die Suche der Verwaltung findet sie NICHT. In
      such-bestand.ts steht kein einziges Feld aus FELDER, weil der Erzeuger
      (tools/verwaltung-suchbestand.mjs) `@if`/`@for`-Bloecke samt Inhalt
      entfernt — und die Feldliste IST ein `@for`. Wer „Geste" sucht, findet
      nichts und muss von selbst auf /konfiguration gehen.

      WARUM `geste` KEINE VORGABE IST: eine Geste, die niemand eingerichtet
      hat, steht nirgends auf dem Schirm — auf einer frischen Box waere sie
      eine Aussperrung mit Ansage. Die Vorgabe ist `rechnen`, siehe
      [[sperr-vorgabe-wirkt-nicht-am-standard]].

      NEBENBEFUND, der beim Bauen mitkam: das Ziffernfeld musste in dieser
      Sorte WEG, nicht unsichtbar — `.tor-feld` traegt `display: grid`, und
      das schlaegt das `hidden`-Merkmal. Ohne die Zeile
      `.tor-feld[hidden] { display: none }` setzt app.js `hidden = true`, der
      Bildschirm sieht richtig aus, und zwoelf Tasten sind weiter bedienbar.
    related:
      - sperr-vorgabe-wirkt-nicht-am-standard
      - entwurf-gibt-gliederung-nicht-masse
      - attrappe-luegt-durch-weglassen

  - id: sperre-aus-der-sicht-des-kindes-messen
    kind: falle
    title: "Eine Sperre besteht jede Einzelpruefung und faellt trotzdem in Sekunden — drei Wege daran vorbei"
    match: "sperre gemessen kind|einstellungssperre umgehung|rechenaufgabe durchprobieren|geste ertasten|warm-kalt|fortschrittsanzeige verraet|tor vorbei settings|kind-am-tor"
    sev: high
    tags: ["eltern", "einstellungssperre", "e31", "sicherheit", "pruefen", "gegenlesen", "geste", 2026]
    source: >
      06.08.2026, tools/kind-am-tor.mjs (23 Aussagen, alle drei Befunde vorher rot),
      an der Vorschau gefahren und an der Box .169 lesend gegengeprueft. Behoben in
      den Commits „der kurze Tipp aufs Zahnrad ging am Tor vorbei", „die
      Rechenaufgabe liess sich in fuenf Sekunden durchprobieren", „die Anzeige
      verriet, ob eine Ecke richtig war".
    symptom: >
      Das Tor vor dem Eltern-Bereich war vollstaendig gebaut und jede Pruefung
      gruen: die richtige Antwort oeffnet, sechs falsche Folgen oeffnen nicht,
      der Bereich wird beim Verlassen wieder gesperrt, was dahinterliegt wird
      weggeraeumt. Trotzdem war er in Sekunden zu oeffnen, ohne die Loesung zu
      kennen. Nichts daran wird rot, nichts sieht falsch aus.
    cause: >
      DIE PRUEFUNGEN NAHMEN DIE SICHT DESSEN EIN, DER DIE LOESUNG KENNT. Sie
      fragen „geht es auf?". Die Frage, die eine Sperre entscheidet, ist eine
      andere: „was kostet es, sie ohne die Loesung aufzubekommen?". Drei
      Antworten darauf, und jede war unabhaengig von den anderen:

      1. EIN ZWEITER WEG DANEBEN. Derselbe Knopf trug zwei Gesten: langes
         Halten oeffnete den gesperrten Bereich, KURZES Tippen sprang
         unmittelbar in die alte Oberflaeche (`/settings`), an der Sperre
         vorbei. Dass dort ein zweiter Waechter steht, beruhigt nur, solange
         niemand nachsieht, WORAUF der seinen Modus abbildet: `alsModus` in
         sperrlogik.ts liest einen FEHLENDEN Schluessel als „aus" — also
         ausgerechnet auf den Boxen, um derentwillen die Vorgabe geaendert
         wurde. Zwei Waechter, zwei Meinungen, und die schwaechere gewinnt,
         weil sie an der Tuer steht, die einen Tipp naeher liegt.
         AN DER BOX BELEGT: Schluessel fehlt, `oberflaeche: neu`, und
         `/settings` steht ohne Nachfrage offen (0 Dialoge, 6 Eintraege,
         darunter „Leeren").

      2. KEINE BREMSE. Die Rechenaufgabe war unbegrenzt oft und ohne jede
         Verzoegerung zu stellen — GEMESSEN 2,55 Versuche je Sekunde. Und
         gerechnet werden muss sie nicht: a × b mit a,b aus 3..9 sind 49 gleich
         wahrscheinliche Paare, aber nur 26 Ergebnisse. Die 24 entsteht viermal.
         Wer immer dieselbe Zahl tippt, trifft mit 8,2 % je Versuch, im Mittel
         beim zwoelften — nach fuenf Sekunden. DIE ZAHL DER MOEGLICHKEITEN IST
         NICHT DIE HUERDE; die Huerde ist die Zahl der Versuche je Minute.
         Wer eine Bremse baut, denkt an zwei Dinge: sie muss die ersten ein,
         zwei Fehlgriffe verschenken (ein Erwachsener vertippt sich), und sie
         muss AUSSERHALB des gesperrten Bereichs zaehlen — sonst setzt „hinaus
         und wieder herein" sie mit zwei Beruehrungen zurueck.

      3. EINE FORTSCHRITTSANZEIGE IST EIN ORAKEL. Die Geste (vier Ecken im
         Uhrzeigersinn) zeigte, wie viele Ecken schon stimmten. Damit
         beantwortete der Schirm nach JEDER Beruehrung „war das richtig?". Aus
         einer Folge, die man raten muss, wird eine, die man ertastet: ein
         Raster abtippen, den Punkt behalten, der die Zahl steigen laesst.
         GEMESSEN: 107 Beruehrungen bis der Bereich offen stand — gegenueber
         1 : 180 000 fuer blindes Tippen. Dieselbe Bauart wie eine
         Passwortpruefung, die sagt, bis zum wievielten Zeichen es stimmte.
         Der Beleg, dass es ausnutzbar war, stand im eigenen Haus: das
         Pruefwerkzeug benutzte dieses Orakel selbst, um die Kantenlaenge einer
         Ecke zu ertasten.

      4. DIE WAHRSCHEINLICHKEIT GILT FUER EIN MODELL, NICHT FUER EINEN
         ANGREIFER. „Vier Ecken sind 1 : 180 000" stimmt fuer einen Finger, der
         GLEICHVERTEILT auf die Flaeche tippt. Es stimmt nicht fuer ein Kind,
         das gemerkt hat, dass es auf die ECKEN ankommt — mehr muss es nicht
         gemerkt haben, es muss die Folge nicht kennen, einmal zusehen reicht.
         Damit sind es 4 hoch 4 = 256 Folgen, und weil jede Beruehrung zugleich
         der Anfang der naechsten ist, faellt das Tor nach ein paar hundert
         Beruehrungen. GEMESSEN: 150, also zwei Minuten. Wer eine Sperre in
         Wahrscheinlichkeiten begruendet, schreibt DAZU, fuer welches Verhalten
         sie gilt — und misst das andere Verhalten getrennt.
    fix: >
      EIN EIGENES WERKZEUG, DAS DIE ANDERE SICHT EINNIMMT (tools/kind-am-tor.mjs).
      Es misst nicht, ob das Tor steht, sondern was es kostet: wie viele Versuche
      in ein Zeitfenster passen (mit einer Antwort, die NIE stimmen kann — sonst
      ist die Messung ein Wuerfelspiel), ob die Anzeige nach einer richtigen und
      nach einer falschen Beruehrung dasselbe sagt, und ob ein kurzer Tipp die
      Oberflaeche verlaesst, waehrend eine Sperre gesetzt ist. Dazu die
      Gegenrichtung in denselben Lauf: unlesbare Konfiguration und
      ausdrueckliches „aus" muessen weiterhin OHNE Frage durchlassen, sonst hat
      man statt einer Sperre eine Aussperrung gebaut.
    body: |
      DREI FALLEN BEIM MESSEN SELBST, alle im ersten Anlauf hineingelaufen:

      * BERUEHRUNGEN ZAEHLEN IST NICHT VERSUCHE ZAEHLEN. Eine Bremse laesst die
        Tasten stehen und nimmt sie bloss nicht mehr an. Wer die Tipps zaehlt,
        zaehlt Versuche, die es nie gab, und meldet eine gebremste Sperre als
        ungebremst. Nach dem Eintippen nachsehen, ob die Zahl auch in der
        Anzeige steht.
      * NICHT DEN GANZEN BAUM NACH DER LOESUNG DURCHSUCHEN. Die Frage „steht die
        Antwort schon im Baum?" schlug bei der Antwort 56 an — im Ziffernfeld
        steht „…456789…". Und bei der Antwort 9 im Merkmal `data-taste="9"`.
        Gesucht wird in den Texten des Tors und in den Merkmalen, das Tastenfeld
        bleibt draussen, und verglichen wird als GANZE Zahl.
      * MIT DER DURCHSCHNITTSRATE HOCHZURECHNEN IST ZU FREUNDLICH. Eine Bremse,
        die steigt, hat am Anfang ihre hoechste Rate. Gerechnet wird mit dem
        LETZTEN gemessenen Abstand.
  - kind: entscheidung
    id: eltern-bereich-ist-eine-seite
    title: >-
      Der Eltern-Bereich ist eine Seite und kein Deckel — und was diese eine
      Form an anderen Zahlen kostet
    source: >-
      E31/P3, 06.08.2026 — Entscheidung des Betreibers nach drei Bildern;
      gemessen mit tools/eltern-seite-messen.mjs, tools/beruehrziele-neu.mjs,
      tools/eltern-tor-schau.mjs, festgehalten in tools/eltern-seite-schau.mjs
    tags:
      - eltern-bereich
      - oberflaeche
      - neu
      - beruehrziele
      - z-index
      - e31
      - 2026
    body: |
      DER SATZ, AUF DEN ES ZURUECKGEHT: „ich finde den entwurf gut wo nicht
      alles zu gedeckt ist". Bis dahin trug `#eltern` `position: fixed;
      inset: 0` und deckte den ganzen Schirm zu; der Entwurf
      (NewDesign/MixPiBox-standalone.html) legt seinen Eltern-Bereich als SEITE
      in die Inhaltsspalte, neben die 88-px-Leiste und ueber das Kissen des
      Mini-Players.

      ── DIE FORM, DIE ES GEWORDEN IST ─────────────────────────────────
      `.eltern { inset: 0 0 0 88px }` — volle Hoehe, Leiste daneben. Das
      KISSEN faehrt ein, solange der Bereich offen ist (eigener Zustand
      `body.eltern-offen`, der NUR die vier Kissenregeln von `platz-machen`
      uebernimmt, nicht dessen fuenfte fuer die Leiste). Eingefahren steht es
      bei x 429..788, y 396..472; die Faecherspalte liegt bei x 104..272 —
      157 px Luft, keine Ueberschneidung. Damit behaelt die Spalte ihre vollen
      390 px und traegt weiterhin FUENF Faecher.

      ── WARUM NICHT DIE REINE FORM DES ENTWURFS ───────────────────────
      `bottom: 84px` (Kissen bleibt, wo es ist) gibt der Spalte 306 px. Vier
      Faecher brauchen 4 * 66 + 3 * 15 = 309. ES FEHLEN 3 PX, die Spalte rollt,
      „System" faellt auf 8,82 mm. Die 3 px sind nicht billig zu holen:
      zwischen y 82 und der Kissenkante bei 396 stehen 314 px, gebraucht werden
      309 + 15 Luft. Der naheliegende Ausweg — `gap` der Faecher zurueck auf 6 —
      gaebe die 2-mm-Reparatur wieder her.

      ── WAS DIE FORM AN ANDEREN ZAHLEN KOSTET, und das ist die Lehre ──
      Aufdecken heisst: Ziele, die anderswo schon eng standen, WANDERN auf einen
      Schirm mit strengeren Forderungen. Drei Beispiele aus diesem einen Umbau,
      und keines davon war vorherzusehen, ohne es zu messen:

      1. `.kat-liste { gap: 6px }` = 0,84 mm und `.mp { gap: 10px }` = 1,40 mm
         standen seit je auf dem Startschirm und waren dort nie der engste
         Abstand. Auf den Eltern-Schirmen gelten 2 mm. Beide mussten auf 15 px —
         eine Aenderung an JEDEM Schirm des Hauses, ausgeloest von einer Form.
      2. MIT DEM GROESSEREN ABSTAND MUSS DIE BREITE MITWACHSEN. Das eingefahrene
         Kissen rechnete `4 * --griff + 70px` (5 * 10 + 20). Mit gap 15 endet
         „Naechster Titel" bei x 803 — 3 px ueber dem Schirm, getastet 63 px =
         8,82 mm. Zwei geschriebene Fassungen desselben Abstands; jetzt lesen
         beide `--mp-gap`.
      3. DIE GESTE WURDE LEICHTER, OHNE DASS JEMAND SIE ANGEFASST HAT. Ihre
         Ecke war eine feste Zahl (120 px). Das Tor schrumpfte von 768x390 auf
         680x296 — vier Ecken deckten damit 30,1 statt 20,2 Prozent, ein
         zufaelliger Tipp traf die jeweils richtige mit 1 : 13 statt 1 : 20.
         Die Kante folgt seither der Flaeche (`gesteEckeKante`): hoechstens ein
         Fuenftel, gedeckelt bei 120 px, mindestens `--griff`.

      ── UND DIE FRAGE, DIE MIT DER FORM AUFGEHT ───────────────────────
      Waehrend das TOR steht, ist die Leiste bedienbar. GEMESSEN, was das
      wirklich kann: die Kategorie wechseln (harmlos — es zeichnet nur das
      Raster dahinter neu, das Tor steht nach jedem Tipp) und das PROFIL
      wechseln. Das zweite ist der Befund: `werWaehlen` ruft
      `window.location.reload()`, und die Bremse des Tors liegt im Speicher der
      Seite — sie ueberlebt „hinaus und wieder herein", aber kein Neuladen.
      Drei Beruehrungen statt 590 Sekunden Wartezeit.

      ENTSCHIEDEN: Die LEISTE bleibt lebendig (der Betreiber wollte „nicht
      alles zugedeckt", die Pille wandert sichtbar mit, es ist keine Attrappe).
      Das ZEICHEN „wer hoert" wird SICHTBAR stillgelegt, solange der Bereich
      offen ist — gedaempft plus `disabled`, wie `.zurueck[disabled]`. Es kostet
      gemessen NULL Beruehrziele und macht das engste Paar weiter statt enger.

      Der zweite Grund fuer dieselbe Zeile: `#ich-fenster` und `#eltern` haben
      BEIDE z-index 8, und `#eltern` steht spaeter im Baum. Das Fenster ging
      also HINTER dem Bereich auf — das Blatt bei 168,8 / 464x464, der Punkt
      mitten darin traf `div.fach-zeile`. Ein offener Dialog, den niemand sieht,
      und der eine Rueckweg raeumte ihn beim ERSTEN Tipp weg, ohne sichtbare
      Wirkung (wortgleich [[rueckweg-lag-ueber-der-meldung]]).
      EINE FREIE STUFE GAB ES NICHT: gebraucht wird
      `.eltern` < `.mp` < `#ich-fenster` < `#zurueck` (9) < `#meldung` (10),
      und unter 9 sind nur 7 und 8 frei, waehrend der grosse Player 6 haelt.
      Drei Ebenen auf zwei Stufen gehen nicht auf — die Stelle, an der es zu
      loesen ist, ist der KNOPF und nicht die Schichtung.

      ── ZWEI FALLEN BEIM MESSEN DIESER FORM ───────────────────────────
      * EIN ZIEL UNTER DEM KISSEN FAELLT AUS DER LISTE DER TREFFBAREN HERAUS —
        und damit aus jeder Abstandsrechnung. Der engste Abstand wird dadurch
        scheinbar GROESSER. Die Ueberdeckung gehoert getrennt gemessen.
      * `pointer-events: none` MACHT EIN BILD UNSICHTBAR FUER `elementFromPoint`,
        nicht fuer das Auge. Die Maskottchen-Zaehlung meldete das stillgelegte
        Zeichen als „verdeckt" und war damit bequem gruen. Wer Sichtbarkeit
        misst, schaltet `pointer-events` fuer die Dauer der Messung ab und legt
        es zurueck.
  - kind: falle
    id: klick-sturm-laedt-nichts-mehr
    title: "Kleinkind-Klicksturm: warum danach 'nichts mehr laedt' — sechs bestaetigte Wege, zwei davon dauerhaft"
    source: "Betreiber-Meldung 06.08.2026 (2-Jaehriger, schnelles wahlloses Tippen); tools/klick-sturm-probe.mjs + 3 Pruefer + adversariale Gegenpruefung"
    tags: [klicksturm, kinder, neu-oberflaeche, box, vorlesen, spotify, 2026]
    body: |
      GEMESSEN (tools/klick-sturm-probe.mjs, echte Koordinaten-Tipps mit Saat,
      gegen die Attrappe): Die NEUE Oberflaeche verklemmt sich CLIENTSEITIG
      NICHT — 8 Runden x 60 Tipps mit 35 ms Abstand, jedes Mal erholt. Das
      Symptom entsteht erst im ZUSAMMENSPIEL mit dem Backend/den Einstellungen.

      DIE WEGE, nach Dauer sortiert:

      DAUERHAFT (bis Neustart/Neuladen):
      1. KLASSISCHE Oberflaeche: der /monitor-Poll (app.component.ts:125) hat
         kein catchError; EIN Status-0 (Backend-Neustart, Netzabriss) toetet
         den Strom, toSignal SPEICHERT den Fehler und wirft ihn bei JEDEM
         Change-Detection-Durchlauf am Wurzel-Template neu — die GESAMTE UI
         friert ein, jeder Tipp wirkungslos. Gegengeprueft bis in
         fesm2022/rxjs-interop.mjs. Gleiche Bauart: current$/local$ in
         media.service.ts:53 (shareReplay speichert Fehler, Player wirft
         nach ~11 s jeden raus); ard/rss ohne catchError leert ganze
         Kategorien (media.service.ts:382; bis zum SDK-Ausbau 29.08.2026 Zeile 564). Vorbild fuer den Fix steht im
         Haus: network.service.ts:21-29.
      2. NEUE Oberflaeche, Vorlesen-Modus 'lernen': KEINE benannte Kachel
         kann JE oeffnen. Die Vollbild-Einblendung #lern (z-10, ueber dem
         Rueckweg z-9) schluckt den versprochenen zweiten Tipp, und ihr
         Klick-Zuhoerer (app.js:5237) ruft zuruecksetzen() -> gehoert=''
         (app.js:5545) — der Bestaetigungszweig (app.js:5414) ist per Touch
         unerreichbar. Selbst am Code verifiziert.
      3. Debug-Overlay der klassischen Oberflaeche: 5 schnelle Tipps unten
         links (90x90 px, genau am Favoriten-Stern) -> schwarzes Feld ueber
         der unteren Schirmhaelfte, ueberlebt Navigationen.

      MINUTEN, selbstheilend (wirkt fuer den Betreiber wie 'tot'):
      4. NEUE Oberflaeche, Vorlesen an: jeder Tipp auf einen NEUEN Satz =
         eine Piper-Synthese (5,1 s am Geraet); uebernehmen() pausiert nur
         den Klang, bricht die HTTP-Anfrage NIE ab; ab 6 offenen sprich-GETs
         ist Chromiums 6er-Verbindungspool voll -> ALLES wartet (auch
         /api/werke, Cover, Miniplayer). Ab ~5 Saetzen kippt der
         25-s-Dienst-Timeout zusaetzlich in die unbegrenzt parallele
         execFile-Herde (je 60-MB-Modell) — Speicherdruck auf der 2-GB-Box.
      5. Spotify-Sturm: titelEinesWerks feuert je Tipp bis 20 Seiten-GETs,
         KEIN In-Flight-Dedup, KEIN Retry-After-Lesen, KEIN Negativ-Cache
         (server.ts:3965/5252/7959); dazu webApiToken ohne In-Flight-Sperre
         (9221) — im Ablauf-Fenster feuern N Anfragen N Token-POSTs. Nach dem
         429 laedt ALLES Spotify leer, und der 15-s-Merktakt klopft weiter
         und haelt das Fenster warm, auch wenn das Kind laengst aufgehoert
         hat. Ueberlebt ein Neuladen der Oberflaeche.
      6. Fristlose fetches (json() app.js:230 ohne AbortSignal; server-seitig
         9225/9254/9305/9095): haengender Upstream haelt Browser-Slots
         10 s - 5 min; /alben bis 20 s je Verbindung.

      AUSSERDEM GEFUNDEN: unbehandelter TypeError im Eltern-Tor
      (torMalen app.js:8573 liest aufgabe.a bei aufgabe=null; 27x in einem
      Probelauf, Saat 777 --zaeh 9000) — eigenes Arbeitspaket.

      GEPRUEFT UND ENTLASTET (nicht nochmal verdaechtigen): laneOffen fragt
      den DOM statt eines Merkers; Stale-Wachen an allen Oeffnern;
      startLauf-Nummern; resumeNacheinander (waechst unter Sturm NICHT,
      finally loest immer); Event-Loop-Blockade durch sync-fs (Mikrosekunden,
      widerlegt); undici staut backendseitig nicht (300/300 nachgemessen).

      SCHNELLDIAGNOSE AN DER BOX: (a) welche Oberflaeche?
      jq .mupibox.oberflaeche mupiboxconfig.json  (b) Vorlesen-Modus
      'lernen'? -> Weg 2. (c) Heilt es nach 2-3 min von selbst -> Wege 4-6;
      bleibt es tot bis Neustart -> Wege 1-3.
    related:
      - klick-sturm-werkzeug
      - vorlesen-tts
      - audit-2026-08-05-delta
  - kind: howto
    id: klick-sturm-werkzeug
    title: "tools/klick-sturm-probe.mjs — wahlloses Schnelltippen nachstellen, mit Saat und Zaeh-Modus"
    source: "gebaut 06.08.2026 zur Betreiber-Meldung; Muster aus tools/leihgabe.mjs + e2e"
    tags: [werkzeug, klicksturm, e2e, vorschau, 2026]
    body: |
      WAS: prasselt echte KOORDINATEN-Klicks (Input.dispatchMouseEvent, nicht
      el.click — trifft, was obenauf liegt, wie der Finger) in Runden auf die
      neue Oberflaeche und prueft nach jeder Runde mit Kind-Mitteln (#zurueck
      druecken, Kachel tippen), ob noch etwas laedt. OHNE Neuladen zwischen
      den Runden, damit sich Schaden aufaddiert. Eigene Attrappe auf freiem
      Port, eigener Browser (leihgabe.mjs).

          node tools/klick-sturm-probe.mjs                  # gruen = erholt sich
          node tools/klick-sturm-probe.mjs --seed 777 --zaeh 9000

      --zaeh N haelt /api/- und /player/-Antworten per CDP-Fetch-Abfangen
      N ms fest (Attrappe bleibt unangetastet) — die Box-Lage 'Backend traege'.
      Damit sichtbar: der Verbindungsstau (6er-Pool, Rueckstau 49-59 Anfragen
      nach dem Sturm) und der torMalen-TypeError. Die Saat steht im Protokoll;
      ein roter Lauf laesst sich exakt nachfahren.

      GRENZE (im Kopf der Datei benannt): Attrappe antwortet sofort und
      fehlerlos — serverseitige Mechanismen (429, Piper-Stau) sieht die Probe
      nur ueber --zaeh angenaehert; der Rest braucht die Box.
    related:
      - klick-sturm-laedt-nichts-mehr
      - vorschau-wird-geliehen

  # ══ Knopflicht und Haltedauer (06.08.2026, am Code nachgemessen) ═══════════
  - kind: knowhow
    id: mupi-knopflicht-kette
    title: "Das Licht im Einschaltknopf — die ganze Kette"
    source: "MuPiHAT-Datenblatt (Aenderungsverzeichnis, Platinenstand 2.2) + am Code nachgemessen 06.08.2026"
    tags: [mupihat, led, taster, hardware, verwaltung]
    body: |
      Der beleuchtete Einschaltknopf haengt laut Datenblatt seit **Platinenstand
      2.2 (03/2024)** an **GPIO13 mit PWM** (Stecker **J15**, der Taster daneben
      an **J1**). PWM ist der Grund, warum es drei Werte gibt und nicht einen:
      **aus**, **hell**, **gedimmt**.

      Die Kette hat VIER Glieder, und das ist beim Suchen wichtig:

          Verwaltung (konfiguration.ts FELDER)
            -> /etc/mupibox/mupiboxconfig.json  shim.ledEnabled / ledBrightnessMax / ledBrightnessMin
            -> mupi_start_led.sh  (liest im Sekundentakt, schreibt /tmp/.power_led)
            -> led_control.py     (besitzt den PWM-Ausgang, liest /tmp/.power_led)

      `/tmp/.power_led` kennt: `led_gpio`, `led_max_brightness`,
      `led_min_brightness`, `led_dim_mode` (**0 = hell, 1 = gedimmt**) und seit
      4.3.1 `led_enabled`. Leser der Datei ist `led_control.py` — der zweite,
      nie benutzte C-Leser (led_control.c, Aufruf in mupi_start_led.sh
      auskommentiert, las mit `json_object_get_int`) ist am 29.08.2026 mitsamt
      Kompilat aus Repo und beiden Ausrollwegen ausgebaut.

      FEHLT `shim.ledEnabled` (Box vor 4.3.1), gilt ueberall **AN**: in
      `led_an()`, in `ist_an()`, im `standard` des Feldes. Ein dunkler Knopf
      nach einem Update saehe nach einem Defekt aus, und gesucht wuerde am Kabel.

      Nachmessen ohne Pi: `python3 tools/knopflicht-probe.py` (stellt RPi.GPIO,
      prueft die echten Dateien, 40 Pruefungen). Auf der Box:
      `mupi-check knopflicht`.
    related:
      - mupi-knopflicht-drei-fehler
      - mupi-haltedauer-bruchzahl

  - kind: falle
    id: mupi-knopflicht-drei-fehler
    title: "Drei Fehler nebeneinander: das Dimmen gab es nie"
    match: "led_dim_mode|power_led|Helligkeit wirkt erst nach Neustart"
    tags: [mupihat, led, bug]
    body: |
      Bis 4.3.1 lagen in der LED-Kette DREI Fehler, von denen keiner sichtbar
      war — die LED tat einfach nichts, und niemand konnte sagen warum:

      1. **Der Vergleich ging nie auf.** `led_control.py` verglich
         `JSON_DATA["led_dim_mode"] == "0"` gegen eine ZEICHENKETTE;
         geschrieben wurde der Wert mit `jq '.led_dim_mode = 0'`, also als
         ZAHL. `0 == "0"` ist in Python **falsch**. Die Schleife lief jede
         Sekunde und tat nichts. Der C-Leser daneben war NICHT betroffen
         (`json_object_get_int`) — deshalb faellt es beim Nebeneinanderhalten
         der beiden nicht auf.
      2. **Die Schleife warf ihre Lesung weg.** `mupi_start_led.sh` holte
         `ledMax`/`ledMin` jede Sekunde frisch aus der Konfiguration und
         benutzte sie danach NIE — in die Datei kam nur `led_dim_mode`. Eine
         geaenderte Helligkeit wirkte erst nach einem Neustart.
      3. **`led_dim_mode` stand verkehrt herum.** Bildschirm AN schrieb 1
         (= dimmen), Bildschirm AUS schrieb 0 (= hell) — waehrend die
         WLED-Zeilen in DERSELBEN Verzweigung es richtig machen (an = normal,
         aus = gedimmt).

      Fehler 1 verdeckte Fehler 3: solange nichts passierte, konnte auch nichts
      verkehrt herum passieren. Wer nur einen davon behebt, sieht sofort den
      naechsten — das ist der Grund, warum das hier zusammensteht.

      GEGENPROBE: `tools/knopflicht-probe.py` prueft beide Schreibweisen
      (Zahl UND Zeichenkette) und faellt durch, wenn einer der drei
      zurueckkommt; `mupi-check knopflicht` sieht sie auf der laufenden Box.

  - kind: falle
    id: mupi-haltedauer-bruchzahl
    title: "Haltedauer 2,25 s = Box faehrt beim ANTIPPEN herunter"
    match: "pressDelay|Button held for|ungueltiger arithmetischer Operator"
    tags: [mupihat, taster, shutdown, bug]
    body: |
      „BUTTON HELD FOR" FUEHRT ZU ZWEI EINTRAEGEN. Dieser hier gilt nur,
      wenn die Zeile ueberhaupt IM PROTOKOLL STEHT — dann laeuft
      `off_trigger.sh` und rechnet falsch. Steht sie nirgends und der Taster
      tut trotzdem nichts, liest ihn niemand:
      [[taster-wird-von-niemandem-ueberwacht]]. Die Frage dazwischen:

          ls /var/lib/dietpi/postboot.d/     -> off_trigger.sh da? DIESER
          systemctl is-enabled mupi_startstop   Eintrag. Leer/disabled ->
                                                der andere.

      Die alte Oberflaeche (`mupi.php`, Abschnitt „Power") bot die Haltedauer
      als Schieber mit **Schrittweite 0,25** an. Wer 2,25 einstellte, bekam
      `"2.25"` in die Konfiguration — und `scripts/OnOffShim/off_trigger.sh`
      zaehlt damit:

          for ((i=0; i<PRESS_DELAY; i++))

      Bash kann das nicht („Ungueltiger arithmetischer Operator, Fehler-
      verursachendes Zeichen ist .25") und bricht die Schleife ab, **BEVOR**
      sie ein einziges Mal prueft, ob die Taste noch gedrueckt ist.
      `button_held` steht zu dem Zeitpunkt schon auf `true`.

      ERGEBNIS: die Box faehrt beim **kuerzesten Antippen** herunter — und das
      Protokoll meldet dazu brav „Button held for 2.25 seconds". Genau deshalb
      sucht man an der falschen Stelle.

      ZWEITE HALBZEIT, gefunden beim Testschreiben: `pruefeFeld` in
      `konfiguration.ts` liess eine **Zahl** 2.25 unveraendert durch und
      kuerzte nur eine **Zeichenkette** "2.25" per `parseInt`. Derselbe Wert,
      zwei Ergebnisse, je nachdem wie die Oberflaeche ihn schickt — die neue
      Verwaltung haette den Fehler also weiter erzeugen koennen. Seit 4.3.1
      `Math.trunc` fuer beide Wege.

      OBERGRENZE 5 SEKUNDEN, und das ist keine Vorsicht: laut Datenblatt
      schaltet derselbe Taster (J1) **ab 6 Sekunden HART ab**, in Hardware.
      Stuenden 10 Sekunden in der Konfiguration, naehme der HAT bei 6 den Strom
      weg — mitten im Schreiben — und `off_trigger.sh` kaeme nie bis zu seinem
      `poweroff`. Es saehe aus, als taete die Box genau das, was eingestellt ist.
    related:
      - mupi-knopflicht-kette
      - taster-wird-von-niemandem-ueberwacht

  - kind: falle
    id: mupi-ausliefern-nur-vier-dinge
    title: "ausliefern.py liefert VIER Dinge — scripts/ ist nicht dabei"
    match: "ausliefern|Ausrollweg|Aenderung wirkt auf der Box nicht|scripts/mupibox"
    tags: [auslieferung, deploy, scripts, luecke]
    body: |
      DIESER EINTRAG SAGT, WOHIN KOPIERT WIRD — nicht, was daraus folgt.
      Wer aus einer Liste von Ausrollwegen den Satz „liegt damit auf
      jeder Box" bilden will, braucht den anderen Eintrag mit demselben
      Symptomwort: [[vereinigung-der-wege-belegt-keine-allgemeinheit]].
      Die Vereinigung mehrerer Wege belegt, dass IRGENDEIN Weg die Datei
      ablegt; „jede Box hat sie" braucht den Durchschnitt.

      `tools/ausliefern.py` ist der EINE Ausrollweg vom Arbeitsrechner auf die
      Box — aber er kennt genau **vier** Ziele (die Tabelle im Kopf der Datei):

          server.js              -> $APPDIR/server.js
          spotify-control.js     -> eigener Ordner (spotifycontroller-main!)
          www                    -> Box-Oberflaeche
          www-admin              -> Verwaltung

      **`scripts/` ist NICHT dabei.** Alles unter `scripts/mupibox/`,
      `scripts/OnOffShim/`, `scripts/fan/` … landet auf der Box nur ueber
      `autosetup.sh` (Erstaufbau) oder `update/start_mupibox_update.sh` — im
      Alltag also von Hand.

      WOHIN sie gehoeren, damit man es nicht jedes Mal sucht:

          scripts/mupibox/*        -> /usr/local/bin/mupibox/
          scripts/OnOffShim/off_trigger.sh -> /var/lib/dietpi/postboot.d/
                                              (so legt autosetup.sh:795 sie ab)
          scripts/OnOffShim/poweroff.sh    -> /usr/lib/systemd/system-shutdown/

      WARUM DAS EINE FALLE IST und keine Fussnote: eine Aenderung, die BEIDE
      Haelften betrifft — ein neues Feld in `konfiguration.ts` UND das Skript,
      das es liest — sieht nach dem Ausliefern **halb ausgerollt** aus. Das
      Board zeigt den neuen Schalter, die Box tut nichts, und nichts meldet
      einen Fehler. Genau so lag es am 06.08.2026 beim Knopflicht
      ([[mupi-knopflicht-kette]]): Backend ueber ausliefern.py, die drei
      Skripte ohne Weg.

      Wer eine solche Aenderung ausrollt, prueft danach BEIDE Haelften — das
      Feld im Board **und** das Verhalten auf der Box.
    related:
      - mupi-fork-ausliefern-ablauf
      - mupi-knopflicht-kette
      - vereinigung-der-wege-belegt-keine-allgemeinheit

  - kind: falle
    id: mupi-werkzeug-ohne-sammellaeufer
    title: "Ein Werkzeug, das nicht in pruefen.sh haengt, sieht aus wie eine Zusicherung"
    match: "pruefen.sh|Probe laeuft nicht mit|0 Treffer"
    tags: [pruefen, werkzeuge, luecke]
    body: |
      DIESELBEN „0 TREFFER" MELDET [[chromium-heisst-auf-dietpi-chromium]] —
      dort ist der gesuchte NAME veraltet (`chromium-browser` gibt es auf
      DietPi nicht), die Suche selbst ist richtig. Hier ist der Name richtig
      und der ORT leer: das Werkzeug steht in keinem Laeufer. Trennfrage:
      stand der Suchbegriff in `tools/pruefen.sh` (dann dieser Eintrag) oder
      in einem Skript/Prozess auf der Box (dann der andere)?

      `tools/pruefen.sh` ist der Sammellaeufer — was dort nicht als `schritt`
      eingetragen ist, laeuft nie, ausser jemand ruft es von Hand auf.

      DAS TUECKISCHE ist nicht die fehlende Pruefung, sondern der falsche
      Eindruck: eine Probe, die im Baum LIEGT, liest sich wie eine
      Zusicherung. Beim naechsten Umbau an derselben Stelle glaubt man, sie
      liefe mit — und baut auf einer Pruefung auf, die seit Wochen niemand
      ausgefuehrt hat.

      BEKANNTE FAELLE (Stand 06.08.2026):
        * `tools/umbenennung-inventur.py` — Backlog E14, 0 Treffer in pruefen.sh
        * `tools/knopflicht-probe.py` — Backlog E33/K5, neu und noch nicht
          eingetragen

      GEGENPROBE, wenn man wissen will, ob eine Probe wirklich mitlaeuft:

          grep -c "<werkzeugname>" tools/pruefen.sh

      Und die zweite Frage gleich dazu, denn sie ist genauso wichtig: schlaegt
      die Probe ueberhaupt an? Ein Werkzeug, das nie „Befund" sagt, misst
      nichts. Der Weg dahin ist, den Fehler absichtlich wieder einzubauen und
      zu verlangen, dass es rot wird — `tools/rotprobe.py` macht genau das in
      einer Wegwerf-Arbeitskopie.
    related:
      - mupi-knopflicht-kette
      - chromium-heisst-auf-dietpi-chromium
  # ══ Rechte am Endpunkt (07.08.2026) ══════════════════════════════════════
  - kind: falle
    id: endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok
    title: "Der WLAN-Adapterwechsel rief ifup OHNE sudo — der Dienst laeuft als dietpi, und der Endpunkt meldete trotzdem ok"
    sev: high
    match: "ifup|ifdown|failed to open lockfile|/run/network/ifstate|wlan-adapter|Umschaltung laeuft"
    cause: "Der Serverdienst laeuft als dietpi (User= in mupibox-server.service); ifup/ifdown brauchen root, denn /run/network gehoert root und ist fuer dietpi nicht schreibbar. Der Aufruf stand ohne sudo, mit 2>/dev/null und einem Rueckruf, der den Fehler verwarf."
    fix: "sudo -n mit vollem Pfad (dieselben Befehlslisten wie funk.ts) UND vor der Antwort pruefen, ob ueberhaupt geschaltet werden kann — sonst 503 statt ok. Nachmessen: tools/rechte-am-geraet.sh <adresse>"
    tags: ['verwaltung', 'netzwerk', 'wlan', 'rechte', 'sudo', 'attrappe', 'falle', 2026]
    source: "07.08.2026 an Box 192.168.178.169 nur lesend nachgemessen (tools/rechte-am-geraet.sh). ACHTUNG, hier stand bis zum 21.08. faelschlich 'behoben in src/backend-api/src/server.ts, Test wlan-adapter.integration.spec.ts' — der Fix war NIE im Baum, den Test gab es nie. Tatsaechlich behoben erst am 21.08.2026 (Commit 981686bd), und dort nur halb: die 503-Vorpruefung fehlt weiterhin. Siehe behoben-in-ist-eine-zustandsaussage-und-war-falsch."
    body: |
      DIE MESSUNG, auf die es ankommt — an der Box, nicht am Quelltext:

          mupibox-server.service   laeuft als dietpi (uid 1000)
          sudo -n                  steht bereit: (ALL : ALL) NOPASSWD: ALL
          /run/network             drwxr-xr-x root root — dietpi darf NICHT
          ifdown als dietpi        failed to open lockfile
                                   /run/network/ifstate.…: Permission denied

      Der Endpunkt POST /api/wlan-adapter rief

          sh -c "ifup wlan1 2>/dev/null; sleep 8; …"

      und antwortete davor bereits {ok:true, hinweis:'Umschaltung laeuft'}.
      Er hat also NIE einen Adapter gewechselt. Kein Protokolleintrag, keine
      Fehlermeldung, nichts — stderr ging nach /dev/null, der Rueckruf war
      () => undefined.

      ── WARUM DAS TEURER IST ALS EIN FEHLER ─────────────────────────────

      Nicht das Nichtstun kostet, sondern das „ok" dabei. Wer die Meldung
      glaubt, sucht die Ursache danach beim Stick, beim Treiber, am Router —
      an jeder Stelle ausser der richtigen. Dieselbe Bauart wie
      [[php-verwaltung-regelte-nichts-weil-die-sitzung-fehlte]] (PHP warf die
      Meldung weg) und [[auch-der-abspieldienst-antwortet-200-auf-alles]]
      (200 sagt nichts ueber Wirkung).

      ── ZWEI FRAGEN, DIE MAN NICHT VERWECHSELN DARF ─────────────────────

      1. FINDET der Dienst das Programm? Der PATH einer SSH-Sitzung ist ein
         anderer als der eines systemd-Dienstes. Hier: der Dienst hat
         /usr/sbin im PATH und findet ifup — die SSH-Sitzung nicht. Wer in
         der Sitzung misst, misst das Falsche und bekommt „command not
         found" statt der Rechtefrage.
      2. DARF er es ausfuehren? Das ist die eigentliche Frage.

      ── DIE PROBE, DIE TRAEGT ───────────────────────────────────────────

      `ifdown <erfundener-name>` beweist NICHTS: ifupdown prueft den NAMEN
      zuerst und endet mit „unknown interface", bevor es an die Rechte
      kommt. Genau daran ist die erste Messung gescheitert — sie sah aus wie
      „alles erlaubt". Es braucht eine EIGENE Schnittstellendatei:

          printf 'iface mupi-probe0 inet manual\n' > /tmp/probe.interfaces
          /sbin/ifdown --force -i /tmp/probe.interfaces mupi-probe0

      Der Name ist damit bekannt, das Geraet gibt es trotzdem nicht: der
      Lauf kommt bis zur Sperrdatei. OHNE sudo ist das ungefaehrlich per
      Definition — gelingt es nicht, war kein Recht da; gelaenge es, betraefe
      es ein Geraet, das es nicht gibt. An einer Box, die NUR ueber WLAN
      haengt, ist das der Unterschied zwischen einer Messung und einem
      Ausschluss aus der eigenen Box.

      ── WAS DARAUS FOLGT, ueber diesen Fall hinaus ──────────────────────

      Wer ERST antwortet und DANN schaltet (und das muss man hier: der
      Befehl kappt die Leitung, ueber die die Antwort liefe), kann den
      Fehlschlag hinterher nur noch ins Protokoll schreiben. Also gehoert
      die Frage VOR die Antwort:

          sudo -n true   -> geht nicht? dann 503, nicht ok

      Und das Ergebnis muss GEMESSEN werden, nicht behauptet: ob der neue
      Adapter traegt, sagt die ADRESSE, nicht der Rueckgabewert von ifup.

      ── WEITERE STELLEN DERSELBEN BAUART ────────────────────────────────

      /api/bt-adapter ruft `sudo hciconfig …` OHNE `-n` (server.ts). Auf
      dieser Box faellt das nicht auf, weil NOPASSWD gilt; auf einer Box
      ohne diese Regel wartet der Dienst auf eine Passworteingabe, bis die
      Frist von 5 s zuschlaegt. Noch nicht angefasst (07.08.2026).

      Nachmessen laesst sich das alles mit `tools/rechte-am-geraet.sh` —
      Dienstbenutzer, sudo-Regeln, Helferliste, PATH des Dienstes und die
      Probe in einem Lauf, rein lesend.
    related:
      - php-verwaltung-regelte-nichts-weil-die-sitzung-fehlte
      - auch-der-abspieldienst-antwortet-200-auf-alles
      - schalter-ohne-unit-luefter
      - usb-stecker-brauchen-keine-treiber

  - kind: howto
    id: weg-ins-admin-menue-wappen-halten
    title: >-
      Der Weg ins Admin-Menue: „MixPi Box" unten links 1,2 s halten — und warum
      das nirgends auf dem Schirm steht
    source: >-
      E51, 07.08.2026 — NewDesign/app.js (`elternEinstieg`, WAPPEN_HALTEN_MS),
      NewDesign/freigabe.json, src/backend-api/src/konfiguration.ts;
      Sperrart an .169 lesend geprueft (`pin`)
    tags:
      - bedienung
      - eltern-bereich
      - einstellungssperre
      - neue-oberflaeche
      - howto
    body: |
      ── SO KOMMT MAN HINEIN ─────────────────────────────────────────────

      Unten links steht das Wappen „MixPi Box". Es 1,2 Sekunden GEDRUECKT
      HALTEN. Waehrend des Haltens laeuft ein Ring um das Wappen; ist er
      voll, geht das Admin-Menue auf. Loslassen bricht ab — der Ring
      verschwindet im selben Augenblick, nicht erst am Ende einer Animation.

      Danach kommt die Sperre. Welche, sagt `mupibox.einstellungssperre` in
      /etc/mupibox/mupiboxconfig.json:

          aus       kein Tor, es geht sofort auf
          rechnen   eine Rechenaufgabe (VORGABE)
          pin       vier bis acht Ziffern
          geste     die vier Ecken im Uhrzeigersinn (nur neue Oberflaeche)

      AUF ACHIMS BOX STEHT `pin` (07.08.2026 gelesen).

      Wer die Sperre einmal geloest hat, wird zwei Minuten lang nicht noch
      einmal gefragt — auch nicht beim Wechsel in die klassische Oberflaeche.
      Das ist Absicht und steht mit Begruendung in NewDesign/freigabe.json.

      MIT TASTATUR statt Finger: das Wappen antabben, Eingabe- oder
      Leertaste. Kein Halten — eine Haltegeste ist ueber die Tastatur nicht
      erreichbar, und der Ring waere dort ohnehin sinnlos.

      ── WARUM DAS AUFGESCHRIEBEN GEHOERT ────────────────────────────────

      WEIL ES AUF DEM SCHIRM NICHT STEHT. Bis zum 06.08.2026 gab es ein
      Zahnrad; ein Tipp darauf ging in die Verwaltung. Das Zahnrad ist
      ersatzlos entfallen — ein Knopf, der auf EINEN Tipp den
      Verwaltungsbereich aufmacht, ist auf der Startseite eines Kindes das
      Gegenteil einer Sperre.

      Seitdem gibt es KEINEN sichtbaren Hinweis mehr auf den Weg hinein.
      Der Ring ist Rueckmeldung WAEHREND der Geste, kein Hinweis DAVOR: wer
      nicht anfaengt zu halten, sieht ihn nie. Das `aria-label` des Wappens
      sagt „Admin-Menü öffnen — gedrückt halten", aber das hoert nur, wer
      eine Sprachausgabe benutzt.

      DAS IST EIN ABGEWOGENER TAUSCH, KEIN VERSEHEN: ein Hinweis fuer das
      Elternteil ist derselbe Hinweis fuer ein neugieriges Kind, das den
      ganzen Nachmittag Zeit hat, ihn auszuprobieren. Die Geste ist die
      Sperre vor der Sperre.

      ABER: er macht diesen Eintrag zum Rueckfall. Wer eine Box uebernimmt,
      neu aufsetzt oder ein halbes Jahr nicht hineingesehen hat, findet den
      Weg NUR hier oder im Code. Steht er nirgends, ist der Besitzer aus
      seinem eigenen Geraet ausgesperrt.

      ── WENN ES GAR NICHT MEHR GEHT ─────────────────────────────────────

      PIN vergessen, Geste vergessen: ueber SSH `mupibox.einstellungssperre`
      in /etc/mupibox/mupiboxconfig.json auf `aus` setzen, hineingehen, neu
      setzen, zurueckstellen. Das ist der einzige Weg vorbei — und der Grund,
      warum eine Kinderbox ohne SSH-Zugang eine Kinderbox mit einem Risiko
      ist.

      Ueber das Netz ist NICHTS gewonnen: die klassische Oberflaeche auf
      Port 8200 (und 8443 per https) traegt dieselbe Sperre; sie ist derselbe
      Waechter, nur in Angular. Auf Port 80 lauscht nichts.

      ── WAS BEIM AENDERN ZU BEACHTEN IST ────────────────────────────────

      Die 1200 ms stehen als EINE Konstante (`WAPPEN_HALTEN_MS`), und der
      Ring liest sie. Wer die Dauer aendert und den Ring vergisst, bekommt
      einen Ring, der voll ist, bevor es aufgeht — oder umgekehrt.

      Das Wappen ist ausserdem ein BERUEHRZIEL und muss die 9-mm-Marke
      halten (`min-height: var(--griff)`). Als es zum Knopf wurde, fehlte
      das und es stand einen halben Tag mit 54 px = 7,6 mm im Baum.
    related:
      - eltern-geste-vier-ecken
      - eltern-bereich-ist-eine-seite
      - drei-orte-eine-anzeige
      - sperr-vorgabe-wirkt-nicht-am-standard

  - kind: falle
    id: repariertes-update-skript-laeuft-nie
    title: >-
      Das reparierte Update-Skript im Fork ist nicht das, was laeuft — der Knopf
      holt sich seines aus dem Netz
    source: >-
      E-F8, 07.08.2026 — update/start_mupibox_update.sh im Fork repariert
      (Commit b4385c81), danach am Geraet nachgemessen: /var/www/admin.php:137,
      raw.githubusercontent.com/splitti/MuPiBox/main abgerufen und verglichen
    tags:
      - update
      - fork
      - datenverlust
      - falle
    body: |
      WER `update/start_mupibox_update.sh` IM FORK REPARIERT, HAT DAMIT NICHTS
      REPARIERT, WAS DIE BOX AUSFUEHRT.

      admin.php:137 laedt das Skript bei JEDEM Klick frisch aus dem Netz:

        curl -L https://raw.githubusercontent.com/splitti/MuPiBox/main/
             update/start_mupibox_update.sh | sudo bash -s -- stable

      Es liegt also nirgends auf der Box (`find / -name start_mupibox_update.sh`
      -> nichts). Die Fassung im Fork wird nie gelesen. Am 07.08.2026 gegen die
      abgerufene Fassung gehalten: `grep -c mupi_bestand` -> 0. Die ganze
      Sicherung des Bestands, die an diesem Tag gebaut wurde, existiert dort
      nicht.

      Und das fremde Skript nimmt MEHR mit als nur die Nutzerdaten. Zeile 274:

        rm -R /home/dietpi/.mupibox/Sonos-Kids-Controller-master/

      Nicht `server/config`, sondern der GANZE Baum — danach wird das
      Upstream-`deploy.zip` darueber ausgepackt. Der Fork ist danach weg und
      durch die Serienfassung ersetzt. Zusaetzlich `rm -R /var/www/*` (Zeile
      580) und ein `pm2 stop server`, obwohl diese Box unter systemd laeuft.

      WARUM DIE REPARATUR TROTZDEM RICHTIG WAR: sie gilt fuer jeden Weg, der
      das Skript AUS DEM FORK nimmt (eigener Auslieferweg, eigener Kanal, ein
      spaeterer Knopf in der neuen Verwaltung). Sie ist nur kein Schutz vor dem
      Knopf, den es heute gibt. Das ist ein Unterschied, den man beim Lesen des
      Commits nicht sieht — deshalb steht er hier.

      ── LAGE AN ACHIMS BOX, 07.08.2026 (Zustand, verfaellt) ─────────────

      Der Knopf ist derzeit NICHT ERREICHBAR: `lighttpd` ist inactive UND
      disabled, auf Port 80 antwortet nichts (curl -> 000), und lighttpd steht
      auch nicht in der Dienstliste der neuen Verwaltung — er laesst sich von
      dort also nicht einschalten. Der ganze alte PHP-Admin liegt dunkel.

      Das erklaert nebenbei zwei andere Befunde desselben Tages: die 0 % in
      mupi.php und die dortige Helligkeitsstufe sind auf DIESER Box
      unerreichbar. Beides bleibt richtig zu reparieren (andere Boxen fahren
      lighttpd), es ist hier nur kein akuter Weg.

      WER LIGHTTPD WIEDER EINSCHALTET, schaltet damit auch diesen Knopf wieder
      scharf. Das ist der Satz, der beim naechsten „warum laeuft der alte Admin
      eigentlich nicht mehr" gebraucht wird.
    related:
      - mupi-update-knopf-loescht-den-fork
      - conf-update-loescht-schluessel-den-noch-jemand-liest
      - pm2-update-leert-die-prozessliste
      - php-verwaltung-regelte-nichts-weil-die-sitzung-fehlte

  # ══ Nachtschicht 07./08.08.2026 — was gemessen wurde und noch nirgends stand
  - kind: knowhow
    id: wiki-zustand-hat-haltbarkeit
    title: "Ursachen gelten weiter, Zustandsaussagen verfallen — und man sieht ihnen das nicht an"
    sev: med
    tags: ['wiki', 'pflege', 'zustand', 'methode', 2026]
    source: "08.08.2026 am Bestand erhoben (565 Eintraege, davon 208 mit mindestens einer Zustandsaussage); die drei Beispiele einzeln an Box 192.168.178.169 nachgemessen"
    body: |
      IN EINEM WISSENSSPEICHER STEHEN ZWEI SORTEN SAETZE NEBENEINANDER, und
      nur eine davon haelt:

        URSACHE  „`Number(null)` ist 0 und besteht jede Endlichkeitspruefung."
                 Gilt in zehn Jahren noch.
        ZUSTAND  „`profile.json` gibt es auf der Box nicht."
                 Galt am 05.08. um 14 Uhr. Am 07.08. um 21:41 nicht mehr.

      SIE SEHEN GLEICH AUS. Beide sind in derselben Zeile, im selben Ton, mit
      derselben Sorgfalt gemessen. Der Leser kann ihnen nicht ansehen, welcher
      von beiden ein Verfallsdatum hat — und deshalb glaubt er beiden gleich.

      ── WAS DAS KOSTET, an drei Faellen desselben Tages ──────────────────

      1. [[mupi-dienste-durchgesehen]] fuehrte `mupi_powerled` unter
         „ABSICHTLICH AUS, richtig so". Am 08.08. ist er `active`/`enabled`.
         Dieser Eintrag IST die Liste, auf die man sich verlaesst, wenn man
         fragt „laeuft das absichtlich nicht?" — wer sie glaubt, sucht einen
         LED-Fehler gar nicht erst.
      2. [[ablage-besitz-gemessen-e18-stufe2]] sagte „`profile.json` GIBT ES
         NICHT". Sie liegt da, 253 B, zwei Kinder, `aktiv: kalea`. Wer die
         Sicherung nach dem alten Satz baut, laesst genau die Datei draussen,
         die als einzige sagt, DASS es ein Kind gibt.
      3. [[mupi-kiosk-als-dietpi-anleitung]] gab ein `sed` fuer
         `agetty -a root`. Auf dieser Box steht `-a dietpi`; das `sed` faende
         nichts und liefe still ins Leere.

      Alle drei waren beim Schreiben RICHTIG. Niemand hat geschlampt. Die
      Aussage ist nur aelter geworden als die Box.

      ── DIE REGEL, die daraus folgt ─────────────────────────────────────

      EINE ZUSTANDSAUSSAGE WIRD NICHT GELOESCHT, SONDERN DATIERT UND
      RICHTIGGESTELLT. Der alte Satz bleibt stehen, darunter kommt, was heute
      gemessen ist, mit Datum. Zwei Gruende, und der zweite ist der wichtigere:

        * Die Geschichte ist Teil des Wissens. „Sie entsteht erst beim ersten
          Schreiben" erklaert, WARUM sie fehlte — der Satz wird durch das
          Nachmessen eingeloest, nicht widerlegt.
        * ANDERE BOXEN SIND NOCH IM ALTEN ZUSTAND. `kinderzeit.json` traegt
          auf .169 seit dem 07.08. die neue Form. Eine Box, die seither nicht
          geschrieben hat, ist noch flach. Wer den alten Satz loescht, nimmt
          dem Naechsten die Beschreibung seiner eigenen Lage weg.

      ── WORAN MAN EINE ZUSTANDSAUSSAGE ERKENNT (beim Schreiben) ──────────

      Sie nennt einen Messwert ohne Ursache: „liegt da", „gibt es nicht",
      „ist aus", „steht auf", „antwortet mit", eine Dateigroesse, eine
      Versionsnummer, ein Dienstname mit `active`/`disabled`. Wer so einen
      Satz schreibt, setzt ein Datum davor — nicht in `source:`, sondern IN
      DEN SATZ. Dann sieht der naechste Leser das Verfallsdatum.

      ── UND WAS NICHT FUNKTIONIERT (ein verworfener Versuch) ─────────────

      Der Gedanke lag nahe, das maschinell zu deuten: „neben `mupi_powerled`
      steht `aus`, der Dienst laeuft → Befund". Am Bestand gemessen: 30
      Zeilen getroffen, 26 davon falsch — und der EINE Eintrag, der wirklich
      falsch war, stand in einer Komma-Aufzaehlung und waere durchgerutscht.
      Verworfen. Nur die Form OHNE Spielraum laesst sich hart pruefen („es
      gibt die Datei nicht" und sie liegt da). Der Rest ist Handarbeit, und
      das ist die eigentliche Nachricht dieses Eintrags.
    related:
      - mupi-dienste-durchgesehen
      - ablage-besitz-gemessen-e18-stufe2
      - mupi-kiosk-als-dietpi-anleitung
      - vorschau-wird-geliehen

  - kind: falle
    id: vorschau-wird-geliehen
    title: "Eine geliehene Vorschau traegt den Zustand des vorigen Werkzeugs — vier gemeldete Abweichungen waren keine"
    sev: med
    match: "Abweichung|kein Ring|nichts gefunden|Vorschau|preview_start|localhost:4200"
    cause: "Die Vorschau ist EINE laufende Anwendung, kein frischer Prozess je Messung. Wer sie sich borgt, misst den Bildschirm, den das Werkzeug davor hinterlassen hat — Weg, Zustand, offene Ueberlagerung, laufende Wiedergabe."
    fix: "Vor dem Messen den Zustand HERSTELLEN, nicht voraussetzen: eigenen Port starten, Weg ansteuern, den Vorzustand einzeln pruefen und den Lauf ABBRECHEN, wenn er fehlt (‚nichts da, um das ein Ring liegen koennte‘ ist kein Befund, sondern ein Aufbaufehler)."
    tags: ['messen', 'vorschau', 'werkzeuge', 'methode', 'falle', 2026]
    source: "07./08.08.2026, bei vier Ring- und Ueberdeckungsmessungen in Folge aufgetreten"
    body: |
      DER FEHLER SIEHT AUS WIE EIN BEFUND, und das ist das ganze Problem.

      Ein Messwerkzeug faehrt die Vorschau an, sucht den Ring um die laufende
      Kachel, findet keinen und meldet: „Abweichung — kein Ring". Die Meldung
      ist WAHR. Sie bedeutet nur nicht, was sie zu bedeuten scheint: es lief
      nichts, um das ein Ring haette liegen koennen. Der Vorgang davor hatte
      die Wiedergabe angehalten, und die Vorschau hat sich das gemerkt.

      VIERMAL AN EINEM TAG. Jedes Mal wurde am Bauteil gesucht, das gar nicht
      dran war.

      ── WARUM ES SICH NICHT VON SELBST AUFLOEST ──────────────────────────

      Ein frischer Prozess hat einen frischen Zustand — daran ist man beim
      Messen gewoehnt. Eine Vorschau ist das GEGENTEIL: sie laeuft weiter,
      genau damit man nicht jedes Mal neu bauen muss. Der Gewinn (Sekunden
      statt Minuten) IST die Ursache des Fehlers. Und sie faellt nicht auf,
      weil zwei Werkzeuge desselben Vorgangs meist denselben Zustand wollen;
      erst wenn zwei VERSCHIEDENE Vorgaenge sie sich teilen, geht es schief.

      DAZU KOMMT DER STILLE NACHBAR: Wer aus einem eigenen Arbeitsbaum misst
      und dabei die Vorschau des HAUPT-Baums erwischt, misst fremden Code und
      merkt nichts davon — der Weg stimmt ja, die Seite kommt. Deshalb gehoert
      zu jedem Messlauf ein EIGENER Port, nicht der naechstbeste.

      ── DIE ZWEI ZEILEN, DIE ES ABFANGEN ────────────────────────────────

      In jedes Werkzeug, das etwas AM BILD misst:

        1. Den Vorzustand einzeln pruefen und BENENNEN, bevor die eigentliche
           Messung laeuft („laeuft eine Wiedergabe? steht die Kachel? ist der
           Weg der richtige?").
        2. Fehlt er, ABBRECHEN mit einem Satz, der den Unterschied macht:
           nicht „Abweichung", sondern „Aufbau fehlt: es lief nichts".

      Der Unterschied kostet zehn Zeilen und spart eine halbe Nacht Suche an
      der falschen Stelle.
    related:
      - verdeckung-messen-werkzeug
      - wiki-zustand-hat-haltbarkeit

  - kind: falle
    id: chromium-heisst-auf-dietpi-chromium
    title: "`chromium-browser` gibt es auf DietPi nicht — vier Stellen suchen danach, alle vier finden nichts"
    sev: high
    match: "chromium-browser|pidof /usr/lib/chromium|pgrep -f .chromium|0 Treffer|Knopflicht pulst"
    cause: "`chromium-browser` war der Name unter Raspbian. DietPi liefert das Paket als `chromium`; auf .169 gibt es nur `/usr/bin/chromium`, der laufende Prozess ist `/usr/lib/chromium/chromium`. Jede Suche nach dem alten Namen liefert LEER — und leer ist in Shell und Python fast immer der harmlos aussehende Zweig."
    fix: "Ueberall auf `chromium` pruefen: der kurze Name TRIFFT BEIDE (die lange Form enthaelt ihn), eine Namensliste ist unnoetig. `pidof /usr/lib/chromium-browser/chromium-browser` -> `pgrep -f '/chromium.*http'`. Nachmessen: `pgrep -c -f chromium` auf der Box."
    tags: ['dietpi', 'chromium', 'kiosk', 'led', 'wled', 'namen', 'falle', 2026]
    source: "08.08.2026 an Box 192.168.178.169 nur lesend nachgemessen (ls, pidof, pgrep, ps, jq); Fundstellen im Baum per grep"
    body: |
      „0 TREFFER" STEHT AUCH IN [[mupi-werkzeug-ohne-sammellaeufer]] — dort
      heisst es „die Probe haengt in keinem Laeufer", hier „der gesuchte Name
      gibt es nicht". Die eine Frage, die trennt, ist: WORIN wurde gesucht?

          grep -c chromium-browser <datei>   -> 0, weil der NAME veraltet ist
                                                (DIESER Eintrag)
          grep -c <werkzeug> tools/pruefen.sh -> 0, weil die PROBE nirgends
                                                haengt: der andere Eintrag

      DIE MESSUNG, in vier Zeilen — am Geraet, nicht am Quelltext (08.08.2026):

          ls /usr/bin/chromium*         -> nur /usr/bin/chromium
          command -v chromium-browser   -> nichts
          ps: der laufende Kiosk        -> /usr/lib/chromium/chromium
          pidof /usr/lib/chromium-browser/chromium-browser -> rc=1, leer
          pidof chromium                -> 7 Prozesse

      ── VIER STELLEN SUCHEN DEN ALTEN NAMEN ─────────────────────────────

      Das ist der Punkt: es ist KEIN einzelner Tippfehler, es ist ein Name,
      der aus einer anderen Zeit stammt und sich durch den ganzen Baum zieht.

        1. `scripts/mupibox/led_control.py` — `ps -ef | grep chromium-browser`.
           Die Warteschleife „pulsiere, solange der Kiosk startet" ging NIE
           aus: das Knopflicht pulste endlos. Behoben (Commit 4eac71bc); der
           Grund, warum es erst jetzt auffiel, steht unten.
        2. `scripts/mupibox/mupi_start_led.sh:100/103` — LIEGT SO AUF DER BOX:

               pid=`pidof /usr/lib/chromium-browser/chromium-browser`
               while [ -z "${pid}" ] ; do … sleep 3 ; done

           Das ist eine ENDLOSSCHLEIFE mit `sleep 3`, und sie steht VOR der
           `while true`-Hauptschleife des Skripts. Wer sie betritt, kommt nie
           zur LED-Steuerung. HEUTE HARMLOS, weil sie im `if wled.active`
           haengt und `wled.active` auf dieser Box `false` ist (nachgesehen).
           WER WLED EINSCHALTET, VERLIERT DIE GANZE LED-SCHLEIFE — und wird
           das Problem bei WLED suchen, nicht beim Browsernamen.
        3. `/var/lib/dietpi/dietpi-software/installed/chromium-autostart.sh`
           Zeile 141 und 150: `pgrep -f "chromium-browser" | while read pid;
           do sudo renice -n -10 -p "$pid"; done`. Gemessen: `pgrep -f
           "chromium-browser"` trifft NICHTS, `pgrep -f chromium` trifft 12.
           Der Kiosk laeuft also auf `nice 0` statt `-10` — nachgesehen in
           `ps -eo pid,ni,comm`: alle Chromium- und Node-Prozesse stehen auf 0.
           EINE PERFORMANCE-MASSNAHME, DIE SEIT JAHREN NICHT WIRKT und die
           niemand vermissen kann, weil nichts fehlschlaegt.
           (Zeile 80/81 desselben Skripts hat einen Rueckfall auf `chromium` —
           DESHALB startet der Kiosk ueberhaupt. Genau dieser Rueckfall hat
           verdeckt, dass der Name anderswo falsch ist.)
        4. led_control.c:159 — dieselbe Suche in C. Unbenutzt, und seit dem
           29.08.2026 samt C-Weg aus dem Repo ausgebaut; die Zeile bleibt hier
           als Beleg, wie weit der falsche Name gestreut war.

      ── WARUM ES NIEMAND GEMERKT HAT — drei Gruende, jeder allein genug ──

        * LEER IST DER STILLE ZWEIG. `pidof` ohne Treffer gibt rc=1 und
          nichts aus; `pgrep | while read` laeuft null Runden durch; `grep`
          ohne Treffer ist eine leere Zeichenkette. Keiner der drei sagt
          „nicht gefunden" — sie tun einfach nichts.
        * DER DIENST WAR AUS. `mupi_powerled.service` war auf dieser Box
          `disabled`. Solange nichts lief, konnte auch nichts endlos pulsen.
          Er ist seit dem 06./07.08.2026 `enabled` — und damit wurde derselbe
          Fehler ueber Nacht sichtbar („sie blinkt jetzt").
        * DER RUECKFALL IM AUTOSTART. Weil der Kiosk startet, wirkt der Name
          im ganzen System bestaetigt.

      ── DIE REGEL FUER DAS NAECHSTE MAL ─────────────────────────────────

      EIN NAME IN EINEM SUCHMUSTER IST EINE ANNAHME UEBER DAS BETRIEBSSYSTEM,
      und sie gehoert einmal geprueft, nicht geraten. Wer ein Skript aus der
      Raspbian-Welt uebernimmt, laesst diesen Einzeiler auf der Box laufen:

          grep -rn "chromium-browser" /usr/local/bin/mupibox/ \
              /var/lib/dietpi/dietpi-software/installed/

      Und beim SCHREIBEN: den kuerzesten Namen nehmen, der noch trennscharf
      ist. `chromium` trifft `chromium` UND `chromium-browser`; eine Box, die
      noch den alten Namen fuehrt, wird also weiterhin erkannt. Was `grep
      chromium` allein NICHT kann, ist das Kiosk-Fenster von Renderer- und
      GPU-Hilfsprozessen trennen — dafuer steht `grep http` daneben: nur das
      Fenster traegt eine Adresse in der Befehlszeile.
    related:
      - mupi-kiosk-als-dietpi
      - mupi-dienste-durchgesehen
      - vcgencmd-display-power-am-pi5-weg
      - mupi-werkzeug-ohne-sammellaeufer

  - kind: falle
    id: taster-wird-von-niemandem-ueberwacht
    title: "Den Einschalttaster ueberwacht auf dieser Box NIEMAND — ein Druck bewirkt nur den harten Griff der Platine bei 6 s"
    sev: high
    match: "off_trigger|postboot.d|mupi_startstop|Button held for|Taster reagiert nicht|geht nicht aus"
    cause: "`off_trigger.sh` wird von `autosetup.sh` nach /var/lib/dietpi/postboot.d/ gelegt. Auf .169 liegt es dort NICHT (das Verzeichnis enthaelt nur readme.txt), unter /usr/local/bin/mupibox/ auch nicht, und `mupi_startstop.service` ist disabled. Damit liest kein Prozess GPIO17."
    fix: "Erst MESSEN, dann suchen: `ls /var/lib/dietpi/postboot.d/` und `systemctl is-enabled mupi_startstop`. Ist beides leer/disabled, ist der Taster nicht kaputt — er wird nur nicht gelesen. Das Skript gehoert von Hand hin (scripts/ liegt nicht im Auslieferweg)."
    tags: ['taster', 'shutdown', 'mupihat', 'postboot', 'auslieferung', 'sackgasse', 2026]
    source: "08.08.2026 an Box 192.168.178.169 nur lesend nachgemessen (ls, systemctl is-enabled)"
    body: |
      DIE ZEILE „BUTTON HELD FOR" GEHOERT DEM ANDEREN FALL. Sie steht auch
      im `match:` von [[mupi-haltedauer-bruchzahl]] — und dort STEHT sie
      wirklich im Protokoll, weil `off_trigger.sh` laeuft und nur falsch
      rechnet. Dieser Eintrag hier ist der Fall, in dem sie NIE erscheint:
      kein Prozess liest GPIO17, also protokolliert auch keiner. Wer die
      Zeile sucht und findet, ist beim anderen Eintrag richtig; wer sie nicht
      findet, misst weiter mit den drei Zeilen unten.

      DIE MESSUNG (08.08.2026, .169), drei Zeilen, alle drei leer:

          ls /var/lib/dietpi/postboot.d/     -> nur readme.txt
          ls /usr/local/bin/mupibox/off_trigger.sh -> gibt es nicht
          systemctl is-enabled mupi_startstop      -> disabled

      ── WAS DAS FUER EINEN TASTENDRUCK HEISST ───────────────────────────

      Kein Prozess sieht GPIO17. Ein kurzer Druck bewirkt NICHTS. Was noch
      wirkt, ist die Platine selbst: der Taster an J1 nimmt laut MuPiHAT-
      Datenblatt ab **6 Sekunden** HART den Strom weg — ohne Dateisysteme zu
      schliessen, ohne `poweroff`, ohne dass irgendwer etwas mitschreibt.

      DAS IST DIE SCHLIMMSTE ART VON „FUNKTIONIERT": es geht aus. Der
      Betreiber haelt die Taste lang genug, die Box ist dunkel, alles scheint
      in Ordnung. Dass dabei jedes Mal mitten in einen Schreibvorgang gegriffen
      wird, sieht man erst, wenn eine JSON-Datei halb ist.

      ── WARUM ES NIEMAND GEMERKT HAT ────────────────────────────────────

      Weil es keinen Unterschied gibt, den man SIEHT. Beide Wege enden mit
      einer dunklen Box; nur die Dauer und das Dateisystem unterscheiden sich.
      Und die eingestellte Haltedauer in der Verwaltung suggeriert, dass
      jemand sie liest — was passierte, wenn sie WIRKLICH gelesen wuerde,
      steht unter der Kennung `mupi-haltedauer-bruchzahl` (Haltedauer 2,25 s
      laesst `off_trigger.sh` an einer Bruchzahl scheitern, BEVOR die Schleife
      zum ersten Mal prueft; die Box faehrt dann beim Antippen herunter). Hier
      ohne Verweisklammern, weil dieser Eintrag zum Zeitpunkt des Schreibens
      noch nicht festgeschrieben war — [[pack-verweise-werkzeug]] haette den
      Verweis sonst zu Recht als tot gemeldet.

      ── UND DIE URSACHE DAHINTER, die grosse ────────────────────────────

      `off_trigger.sh` liegt unter `scripts/OnOffShim/` — und `scripts/` ist
      im Auslieferweg vom Arbeitsrechner auf die Box NICHT enthalten. Eine
      Reparatur an diesem Skript kann tagelang im Baum liegen und die Box nie
      erreichen, ohne dass irgendetwas es meldet. Am 06./07.08.2026 ist genau
      das VIERMAL passiert (Startbild, Sicherung, Medien-Einlesen,
      Knopflicht). Wer an `scripts/` arbeitet, prueft nach dem Ausliefern die
      Datei AUF DER BOX, nicht den Commit.

      ── PRUEFFRAGE, bevor man am Taster sucht ───────────────────────────

      „Liest ueberhaupt jemand diesen Anschluss?" — drei `ls`/`systemctl`
      oben beantworten sie in fuenf Sekunden und ersparen das Messgeraet.

      ══ INSTALLIERT AM 16.08.2026 (Box .57, Phase 1) ══════════════════
      Dieselben drei leeren Antworten auch auf der neuen Box gemessen,
      dann entschieden und gebaut: off_trigger.sh + taster_wache.py
      liegen unter /usr/local/bin/mupibox/, gestartet von der EIGENEN
      Unit mupi_taster.service (nicht postboot.d - dort schreibt das
      Update hinein, und zwei laufende Fassungen merkte niemand).
      ausliefern.py traegt beide jetzt in SKRIPT_EINZELN. Verifiziert:
      "Press delay set to 2 seconds", "guard ready, GPIO17 status: 1".
      Ein Druck von 2 s faehrt sauber herunter.

      PHASE 2 OFFEN: poweroff.sh (system-shutdown-Haken, befiehlt dem
      Shim den Strom-Schnitt nach dem Halt) traegt noch gpioset aus
      libgpiod 1.x - auf Debian 13 nicht vorhanden. Folge: nach dem
      sauberen Halt kann die Versorgung anbleiben; 6 s halten schneidet
      dann hart, unschaedlich, weil das System steht. Der Umbau auf
      lgpio ist derselbe Weg wie bei der Wache - er braucht einen
      Testtermin, an dem die Box herunterfahren darf.
    related:
      - mupi-akku-anzeige
      - sackgasse-die-frage-die-eine-bedienung-pruefbar-macht
      - mupi-haltedauer-bruchzahl

  - kind: falle
    id: profile-json-fiel-aus-der-sicherung
    title: "Die einzige Datei, die sagt DASS es ein Kind gibt, fiel aus jeder Sicherung — und die Warnung ging auf stdout"
    sev: high
    match: "nicht eingeordnet, NICHT gesichert|profile.json|standNormalisieren\\(null\\)|Kind ist weg|Ordner da, Kind weg"
    cause: "Die Musterliste in mupibox-sicherung.py fuehrte `gespielt.*.json` und `kinderzeit-verbrauch.*.json`, aber nicht `profile.json`. Das Skript SAGTE es sogar („! nicht eingeordnet, NICHT gesichert: server/config/profile.json“) — nur steht diese Zeile auf stdout eines Werkzeugs, das im Alltag nichts und niemand von Hand aufruft."
    fix: "`profile.json` und `profile/*/…` in die Muster. Und: jede Ablage, die in `BEREICH_ABLAGEN` (src/backend-api/src/profile.ts) dazukommt, muss in der Sicherung nachgezogen werden — `tools/bereich-sicherung-deckung.py` misst genau das."
    tags: ['sicherung', 'profile', 'mehrbenutzer', 'stdout', 'e18', 'falle', 2026]
    source: "07.08.2026 im Sandkasten mit dem echten Aufbau von .169 gemessen (sichern, alles wegraeumen, zurueckspielen, Datei fuer Datei zaehlen); Muster heute in scripts/mupibox/mupibox-sicherung.py"
    body: |
      DIE ZEILE „nicht eingeordnet, NICHT gesichert" FUEHRT ZU ZWEI
      EINTRAEGEN. Trennen laesst sich am DATEINAMEN, den sie nennt:

          server/config/profile.json         -> DIESER Eintrag (07.08.2026)
          interpreten.json, albumstop.json,
          profile/*/verlauf.json             -> [[werkzeug-meldet-ohne-empfaenger]]
                                                (E65, 20.08.2026)

      Es ist derselbe Fehler zweimal, im Abstand von zwei Wochen, mit
      derselben unbeachteten Zeile. Wer nur hier liest, haelt die Luecke fuer
      geschlossen — der andere Eintrag zeigt, dass sie danach dreimal
      wiederkam, und zieht die Lehre daraus.

      DER TEUERSTE EINZELNE VERLUST IM GANZEN STAND, und er sah nach nichts aus.

      DIE PROBE: sichern, alles wegraeumen, zurueckspielen, Datei fuer Datei
      zaehlen. `server/config/profile.json` kam als EINZIGE Ablage nicht wieder.

      ── WAS DANACH PASSIERT WAERE, wenn es ernst gewesen waere ──────────

      `server.ts` liest `profile.json`, findet nichts und nimmt
      `standNormalisieren(null)` — das ist der Gast, allein. Die Ordner
      `profile/kalea/` mit Verlauf, Zeitkonto, Listen UND Medienauswahl liegen
      danach VOLLSTAENDIG DA und sind von der Oberflaeche aus nicht mehr
      erreichbar.

          Keine Fehlermeldung. Keine Luecke. Kein rotes Feld.
          Das Kind ist einfach nicht mehr da.

      Und wer es neu anlegt, bekommt seine Sachen nur zurueck, wenn der Name
      zufaellig dieselbe Kennung ergibt.

      ── DIE ZWEITE HAELFTE, die eigentliche Lehre ───────────────────────

      DAS SKRIPT HAT ES GESAGT. Woertlich:

          ! nicht eingeordnet, NICHT gesichert: server/config/profile.json

      Diese Zeile ging auf stdout. Ein Werkzeug, das aus einem anderen Programm
      heraus aufgerufen wird, schreibt seine wichtigste Aussage damit ins
      Nichts — niemand liest die Ausgabe eines Schritts, der mit 0 endet.

      EINE WARNUNG, DIE NUR AUF stdout STEHT, IST KEINE WARNUNG. Sie muss den
      Rueckgabewert aendern, in einen Bericht wandern, den jemand abfragt, oder
      wenigstens auf stderr gehen. Dieselbe Sorte Fehler wie
      [[endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok]]: gesagt, aber an
      niemanden.

      ── WARUM AUSGERECHNET DIESE DATEI ──────────────────────────────────

      Weil sie klein und unscheinbar ist (253 B) und wie ein Index aussieht,
      nicht wie Inhalt. Der Inhalt — Verlauf, Zeitkonto, Listen — liegt in
      `profile/<kennung>/` und war gesichert. Nur der Zettel, auf dem steht,
      dass es diese Kennung ueberhaupt gibt, fehlte. Bei Sicherungen gilt
      deshalb: DAS VERZEICHNIS IST WICHTIGER ALS DER BESTAND. Ohne Bestand ist
      ein Kind arm; ohne Verzeichnis existiert es nicht.

      Dasselbe gilt seit E18 Stufe 4 fuer `profile/*/auswahl.json`: sie ist der
      einzige Ort, an dem steht, WAS ein Kind sehen darf. Geht sie verloren,
      kippt das Kind still von „nur diese sechs" auf „alles" — denn eine leere
      Auswahl bedeutet „alles". Auf .169 liegt sie am 08.08.2026 noch nicht;
      das Muster ist schon da, bevor die Datei kommt, und genau so herum ist
      es richtig.
    related:
      - ablage-besitz-gemessen-e18-stufe2
      - mehrbenutzer-vorbereitung
      - endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok
      - werkzeug-meldet-ohne-empfaenger

  - kind: falle
    id: mv-auf-einen-verweis-ersetzt-ihn
    title: "`mv` auf einen Verweis ersetzt ihn — `cat >` schreibt hindurch"
    sev: high
    match: "mv \\$\\{TMP|active_resume|active_data|Bruecke|Verweis kaputt|renameSync"
    cause: "`mv quelle ziel` legt am Ort von `ziel` eine DATEI ab. War `ziel` ein Verweis, ist er danach weg — der Verweis wurde ersetzt, nicht sein Ziel beschrieben. `cat > ziel` dagegen oeffnet den Weg und folgt dem Verweis."
    fix: "In jedem Skript, das eine Ablage neu schreibt, `cat > $ziel` statt `mv tmp $ziel`. Es ist nicht atomar — das ist der Preis, und er ist hier kleiner als eine gekappte Bruecke."
    tags: ['shell', 'symlink', 'resume', 'profile', 'e18', 'falle', 2026]
    source: "05.–07.08.2026 in scripts/mupibox/remove_max_resume.sh aufgetreten und behoben; die Begruendung steht dort im Quelltext (Zeilen 33-39 und 157-164)"
    body: |
      `active_data` STEHT IN ZWEI EINTRAEGEN, und die Frage, die trennt,
      ist NICHT „welcher Dateiname", sondern: IST DIE DATEI DA? Fehlt sie
      ganz, haengt die Route und die Oberflaeche dreht ewig — das ist
      [[mupi-api-data-hangs]]. Liegt sie da, ist der Fehler hier: an
      ihrer Stelle steht eine gewoehnliche Datei, wo eine BRUECKE stehen
      muesste, und ab da lesen Server und Kind zwei verschiedene Staende.
      Ein `ls -l` unterscheidet beides in einer Zeile, ein `curl` nicht.

      SEIT E18 STUFE 3 IST `server/config/resume.json` KEINE DATEI MEHR,
      sondern eine BRUECKE: ein Verweis auf `profile/<aktiv>/resume.json`. Das
      ist gebaut worden, damit die vier Shell-Skripte und die vier Server-Wege,
      die den alten Pfad fest verdrahtet haben, weiterlaufen koennen, ohne dass
      man sie alle anfasst.

      `remove_max_resume.sh` schnitt die Liste auf `mupibox.resume` und legte
      das Ergebnis so ab:

          mv ${TMP_RESUME} ${RESUME}

      DANACH WAR DIE BRUECKE WEG. An ihrer Stelle lag eine gewoehnliche Datei
      mit dem gekuerzten Inhalt — box-weit, nicht mehr beim Kind. Das Kind
      schrieb weiter in `profile/<kennung>/resume.json`, gelesen wurde ab
      sofort die Datei daneben. Zwei Staende, kein Fehler, keine Meldung.

      ── DER UNTERSCHIED IN EINEM SATZ ───────────────────────────────────

          mv   ersetzt den EINTRAG IM VERZEICHNIS  -> der Verweis ist weg
          cat >  oeffnet den WEG                    -> der Verweis bleibt,
                                                       das Ziel wird beschrieben

      Dasselbe gilt in Node: `fs.renameSync` auf einen Verweis benennt den
      VERWEIS um, nicht sein Ziel — `ablageUebernehmen()` zog damit einen
      Verweis in den Profilordner, der weiter nach draussen zeigte. Wer
      Ablagen umzieht, muss Verweise UEBERSPRINGEN, nicht mitnehmen.

      ── WARUM ES NIEMAND GEMERKT HAT ────────────────────────────────────

      `mv tmp ziel` ist das Standardmuster fuer „atomar ersetzen", und es ist
      in dieser Rolle RICHTIG — solange `ziel` eine Datei ist. Der Tag, an dem
      aus `ziel` ein Verweis wird, steht in einem anderen Commit, in einem
      anderen Verzeichnis, meist von jemand anderem. Das Skript aendert sich
      nicht, seine Bedeutung schon.

      DIE PRUEFFRAGE beim Einbauen einer Bruecke: „Welche Programme schreiben
      an diesen Ort, und schreiben sie mit `mv`?" Es genuegt nicht, die LESER
      zu zaehlen. `grep -rn "mv .*resume" scripts/` dauert zehn Sekunden.

      Und auch dort, wo es heute keine Bruecke gibt (`offline_resume.json`),
      steht inzwischen `cat >` — aus demselben Grund, aus dem man einen
      Schalter immer in dieselbe Richtung legt: damit die naechste Bruecke
      nicht wieder eine Suche kostet.
    related:
      - resume-bruecke-am-alten-ort
      - ablage-besitz-gemessen-e18-stufe2
      - mupi-api-data-hangs

  - kind: architektur
    id: herkunftsriegel-und-der-namenstausch
    title: "Der Herkunftsriegel vor der API — und warum `Origin` gegen `Host` allein den Namenstausch nicht faengt"
    sev: high
    tags: ['sicherheit', 'api', 'cors', 'dns-rebinding', 'herkunft', 'e15', 2026]
    source: "gebaut 07.08.2026 (src/backend-api/src/herkunft.ts + herkunft.spec.ts, eigene-namen.ts); am 08.08.2026 gegen Box .169 nachgemessen: /api/data ohne Origin -> 200, mit fremdem Origin -> 403 fremde-herkunft"
    body: |
      AUSGANGSLAGE: In `server.ts` stand `app.use(cors())` VOR dem Anmeldetor
      und galt fuer alle Wege; in der ausgelieferten Vorlage ist
      `interfacelogin.state = false`. Zusammen heisst das: die Box antwortete
      JEDER Herkunft mit `Access-Control-Allow-Origin: *` und ohne Anmeldung.
      Eine beliebige Webseite, die ein Elternteil im selben Heimnetz aufmacht,
      holte sich mit einem `fetch('http://mupibox:8200/api/konfiguration')` die
      Konfiguration, die Kinderprofile und die Zugangsdaten der Dienste — und
      durfte die Antwort auch LESEN, weil der Stern das erlaubt.

      ── DIE ENTSCHEIDUNG DES BETREIBERS (07.08.2026) ────────────────────

          „Anfragen MIT fremder Herkunft werden abgelehnt, alles OHNE Herkunft
           (die eigene Oberflaeche, curl, die Verwaltung) laeuft weiter."

      Abgelehnt hat er eine Anmeldung als Vorgabe, und die Begruendung ist die
      wichtigere Haelfte: „ein vergessenes Passwort ist eine neue Sackgasse
      ohne SSH" ([[sackgasse-die-frage-die-eine-bedienung-pruefbar-macht]]).
      Ein Riegel, den der Betreiber selbst nicht mehr aufbekommt, ist kein
      Gewinn.

      ── WAS „EIGENE HERKUNFT" HEISST, UND WARUM ES AUS DER ANFRAGE KOMMT ─

      Verglichen wird `Origin` gegen `Host` DERSELBEN ANFRAGE. Nichts ist fest
      eingetragen — die Box ist mal `192.168.178.169`, mal `mupibox.local`,
      mal `localhost` (Kiosk), mal `MuPiBox`, und jeder dieser Namen ist fuer
      den Browser eine eigene Herkunft. Wer hier eine LISTE pflegen muesste,
      sperrt frueher oder spaeter die Verwaltung aus.

      ── UND JETZT DER PUNKT, DER DIESEN EINTRAG NOETIG MACHT ────────────

      `Origin` gegen `Host` zu halten faengt die gewoehnliche fremde Seite —
      aber NICHT DEN NAMENSTAUSCH (DNS-Rebinding).

      Zeigt der Name einer fremden Seite nach dem ersten Laden auf die Box —
      und der Angreifer beherrscht seine eigene Namensaufloesung, das ist die
      ganze Kunst daran —, dann tragen `Host` UND `Origin` DENSELBEN fremden
      Namen. Fuer den Browser ist das gleichherkuenftig; er meldet sogar
      `Sec-Fetch-Site: same-origin`. Beide Riegel sehen nichts Fremdes.
      GEMESSEN: `POST /api/konfiguration` mit `Host: boese.example` kam mit
      200 durch und aenderte die Lautstaerke in `mupiboxconfig.json`.

      DAS IST DIE LEHRE, und sie gilt ueber diese Box hinaus: EIN VERGLEICH
      ZWEIER FELDER DERSELBEN ANFRAGE PRUEFT NUR IHRE UEBEREINSTIMMUNG, NICHT
      IHRE WAHRHEIT. Beide kommen vom selben Absender. Wer sie gegeneinander
      haelt, hat einen Riegel gebaut, den der Absender aufmachen kann, indem
      er BEIDE dreht.

      DAGEGEN HILFT NUR EINES: die Box muss WISSEN, WIE SIE SELBST HEISST, und
      einen `Host` zurueckweisen, der keiner ihrer Namen ist. `eigene-namen.ts`
      leitet diese Namen zur LAUFZEIT ab (Schnittstellen, Rechnername,
      Konfiguration, reservierte Heimnetz-Endungen) — und geht im Zweifel AUF
      statt zu, weil ein Riegel, der die eigene Verwaltung aussperrt, am
      Kuechentisch nicht mehr zu oeffnen ist.

      ── DREI ENTSCHEIDUNGEN, DIE MAN SONST NACHBAUEN WUERDE ─────────────

        * DAS SCHEMA WIRD NICHT VERGLICHEN, nur Rechnername und Port. Ob DIESE
          Verbindung ueber TLS lief, weiss der Server aus dem Socket; ob die
          SEITE ueber TLS geladen wurde, sagt nur `Origin`. Stuende je ein
          Zwischenstueck davor, das TLS abschliesst, waere ein Schemavergleich
          genau die Sackgasse, die niemand melden kann.
        * `Origin: null` WIRD ABGEWIESEN. Den Wert gibt es wirklich (`file://`,
          `<iframe sandbox>` ohne `allow-same-origin`, manche Weiterleitung) —
          und er ist der Wert, den ein Angreifer am leichtesten herstellt. Wer
          ihn durchlaesst, hat den Riegel fuer genau den Fall geoeffnet, gegen
          den er gebaut ist. Werkzeuge, die eine lokale HTML-Datei gegen die
          Box laufen lassen, holen die Seite VON DER BOX oder setzen
          `MUPIBOX_HERKUNFT_ZUSATZ`.
        * DER FORMULAR-POST IST DER GRUND, WARUM NICHT NUR DIE KOPFZEILE WEG
          MUSS. `<form method="post" action="http://box:8200/api/…">` auf einer
          fremden Seite braucht KEINE Vorabfrage. Ein Riegel, der nur das LESEN
          verhindert, laesst das SCHREIBEN offen. Dieser faengt ihn, weil der
          Browser bei JEDEM POST ueber Herkunftsgrenzen `Origin` setzt — auch
          beim Formular.

      ── WAS ER NICHT TUT, und das gehoert in denselben Eintrag ──────────

      `curl` im Heimnetz kommt durch, absichtlich. Am 08.08.2026 gemessen:
      `GET /api/data` OHNE `Origin` -> HTTP 200, 7764 Bytes, mit dem
      Jellyfin-`api_key` darin. Der Riegel schuetzt vor der fremden WEBSEITE,
      nicht vor dem Werkzeug im selben Netz — siehe
      [[zugangsschluessel-im-browser]]. Wer ihn fuer das eine haelt, waehrend
      er das andere ist, hoert an der falschen Stelle auf zu arbeiten.
    related:
      - zugangsschluessel-im-browser
      - sackgasse-die-frage-die-eine-bedienung-pruefbar-macht
      - endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok

  - kind: falle
    id: nichts-geht-komprimiert-ueber-die-leitung
    title: "app.js geht mit 1,00 MB roh ueber die Leitung — es gibt keine Komprimierung, an keiner Stelle"
    sev: med
    match: "Content-Encoding|compression|express.static|Start dauert|4,1 MB|app.js 1 MB"
    cause: "Alle drei `express.static`-Einhaengungen in server.ts liefern roh aus. `compression` ist nicht eingebunden und steht in KEINER package.json — es ist nicht abgeschaltet, es war nie da. Dazu `Cache-Control: public, max-age=0`: jeder Start fragt jede Datei neu nach."
    fix: "`compression()` VOR die static-Einhaengungen (Text drueckt sich auf etwa ein Viertel). Und fuer die gehashten Bundles `max-age` hochsetzen — ihr Name aendert sich bei jedem Bau, sie koennen unbegrenzt gelten."
    tags: ['perf', 'start', 'express', 'kompression', 'cover', 2026]
    source: "08.08.2026 von der Box aus gemessen (curl -H 'Accept-Encoding: gzip, br' gegen localhost:8200/neu/); Quelltextstand src/backend-api/src/server.ts"
    body: |
      DIE MESSUNG — mit `Accept-Encoding: gzip, br`, also der Browser hat
      ausdruecklich gefragt (08.08.2026, .169):

          /neu/ (index)        68 781 B    keine Content-Encoding-Kopfzeile
          app.js            1 048 310 B    keine Content-Encoding-Kopfzeile
          app.css             290 962 B    keine Content-Encoding-Kopfzeile
          /active_theme.css     9 688 B    keine Content-Encoding-Kopfzeile
          ────────────────────────────
          1 417 741 B  ≈ 1,42 MB  —  BEVOR EIN EINZIGES COVER GELADEN IST

      Dazu auf jeder Antwort: `Cache-Control: public, max-age=0`.

      ── DREI BEFUNDE, DIE ZUSAMMENGEHOEREN ──────────────────────────────

      1. KEINE KOMPRIMIERUNG, UND ZWAR NIRGENDS. `grep -n "compression"` ueber
         `server.ts` und die `package.json`: null Treffer. Es ist nicht
         konfiguriert und wieder ausgeschaltet worden — es hat nie jemand
         eingebaut. Bei JavaScript und CSS bringt gzip typisch das Vierfache;
         aus 1,34 MB Text wuerden ueberschlaegig 300–350 kB.
      2. `max-age=0` HEISST: JEDER START FRAGT ALLES NEU AN. Der Kiosk startet
         genau einmal je Einschalten, aber er startet bei JEDEM Einschalten —
         und das ist bei einer Kinderbox mehrmals taeglich. Die Bundles tragen
         einen Hash im Namen (`main-5MJQKB6L.js`); ihr Inhalt kann sich unter
         demselben Namen gar nicht aendern. `max-age=0` ist fuer sie die
         teuerste moegliche Einstellung.
      3. DIE COVER KOMMEN OBENDRAUF. Im `coverspeicher` auf .169 liegen 496
         Dateien mit zusammen 14 MB; einzelne sind 33–42 kB, gemessen. Sie
         werden in Originalgroesse ausgeliefert und in Kacheln von rund
         120x120 gezeichnet — die Flaeche, die wirklich gebraucht wird, ist ein
         Bruchteil der geladenen. Ein Bild komprimiert nicht mehr (JPEG/PNG
         sind es schon); hier hilft nur KLEINER AUSLIEFERN, nicht besser
         packen. Das ist der Unterschied zu Punkt 1 und der Grund, warum beides
         nicht dieselbe Massnahme ist.

      ── WARUM ES NIEMAND GEMERKT HAT ────────────────────────────────────

      Weil der Kiosk ueber `localhost` laedt. Auf der Rueckschleife kostet ein
      Megabyte fast nichts an UEBERTRAGUNG — der Preis faellt woanders an:
      Chromium muss 1 MB JavaScript PARSEN, auf einem Pi, waehrend gleichzeitig
      alles andere hochfaehrt. Und wer die Verwaltung von einem zweiten Rechner
      ueber WLAN aufmacht, zahlt die volle Rechnung.

      Beim Start bis zum Bild sind rund 9 s Chromium gemessen
      ([[grundmessung-zeit-bis-zum-bild]]) — DORT liegt dieser Posten, nicht
      im Netzwerk. Deshalb faellt er beim Messen der Leitung nicht auf.

      ── DIE REIHENFOLGE, wenn jemand das angeht ─────────────────────────

      Erst `compression()` (eine Zeile, wirkt auf alles), dann `max-age` fuer
      die gehashten Bundles, dann die Cover in Kachelgroesse. Die ersten zwei
      sind Konfiguration, die dritte ist Arbeit.
    related:
      - grundmessung-zeit-bis-zum-bild
      - mupi-cover-cache

  - kind: falle
    id: probe-kann-www-nie-gleich-nennen
    title: "`ausliefern.py --probe` meldet www und admin IMMER als veraendert — der Baumhash KANN nicht gleich werden"
    sev: med
    match: "--probe|veraendert|Baumhash|browser/|main-.*\\.js"
    cause: "Das Zwischenlager `src/deploy/www` fuehrt die Dateien unter `browser/` (Angular-Ausgabeform), auf der Box liegen sie FLACH. Zwei verschiedene Baumformen ergeben zwei verschiedene Hashes, egal wie gleich der Inhalt ist. Zweitens sammeln sich auf der Box alte gehashte Bundles an, weil kopiert und nicht ersetzt wird."
    fix: "Beim Vergleich die Ebene `browser/` abziehen und DATEIWEISE vergleichen, nicht als Baum. Und beim Ausliefern die alten `main-*.js`/`chunk-*.js` entfernen — sonst waechst die Box zu und der Vergleich bleibt unmoeglich."
    tags: ['auslieferung', 'deploy', 'probe', 'angular', 'falle', 2026]
    source: "08.08.2026 nachgesehen: src/deploy/www/ enthaelt browser/, 3rdpartylicenses.txt, prerendered-routes.json; auf .169 liegt www/ flach und traegt VIER main-*.js"
    body: |
      `--probe` STEHT AN ZWEI WERKZEUGEN. Die eine Frage, die trennt:
      WELCHES Werkzeug hat den Schalter? Hier ist es `tools/ausliefern.py`
      — der Ausrollweg vom Arbeitsrechner auf die Box. Der Schalter am
      Sicherungs-Werkzeug (`mupibox-sicherung.py --probe`, das einen
      Stand zurueckspielt und sein Urteil als PROBE.txt auf die Karte
      legt) ist ein anderer Fall:
      [[kein-traceback-in-eine-datei-die-ohne-ssh-gelesen-wird]].
      Beide melden „stimmt nicht", ohne dass etwas kaputt ist — aber nur
      einer davon wird ohne SSH gelesen.

      WER EINE PRUEFUNG BAUT, DIE NIE GRUEN WERDEN KANN, HAT KEINE PRUEFUNG
      GEBAUT — er hat eine Meldung gebaut, die man wegzuklicken lernt. Genau
      das ist hier passiert, und es ist die schaedlichste Sorte: `--probe`
      sagt bei JEDEM Lauf „www und admin veraendert", auch wenn nichts
      veraendert wurde. Nach dem dritten Mal glaubt es niemand mehr — und beim
      vierten Mal war wirklich etwas anders.

      ── URSACHE 1: ZWEI BAUMFORMEN ──────────────────────────────────────

          Zwischenlager  src/deploy/www/browser/{app.js, chunk-*.js, assets/}
                         src/deploy/www/3rdpartylicenses.txt
                         src/deploy/www/prerendered-routes.json
          Auf der Box    www/{app.js, chunk-*.js, assets/, active_theme.css}

      Der Inhalt kann Byte fuer Byte gleich sein — die BAEUME sind es nie. Ein
      Hash ueber den Baum vergleicht also Aepfel mit demselben Apfel in einer
      anderen Kiste.

      ── URSACHE 2: DIE BOX WAECHST ZU ───────────────────────────────────

      Am 08.08.2026 auf .169 gezaehlt: VIER Dateien `main-*.js`
      (`main-5MJQKB6L`, `-ANRDNYB5`, `-HSNDLVOV`, `-Q2U5MC4F`). Im Bau gibt es
      genau EINE. Angular haengt einen Inhalts-Hash an den Namen; wer beim
      Ausliefern KOPIERT statt zu ERSETZEN, laesst jede alte Fassung liegen.
      `index.html` verweist auf eine davon, die anderen drei sind tot — sie
      kosten Platz, verwirren jeden, der die Box durchsieht, und machen einen
      Dateiabgleich unmoeglich, weil auf der Box mehr liegt als im Bau.

      Der Zaehlstand ist nebenbei ein Alter: die Zahl steigt mit jeder
      Auslieferung. Sie war zuletzt drei.

      ── WAS EINE BRAUCHBARE PROBE STATTDESSEN TUT ───────────────────────

        1. `browser/` abziehen und den Vergleich auf der Dateiebene fuehren.
        2. Nur die Dateien vergleichen, die `index.html` WIRKLICH nennt —
           dann sind die Karteileichen egal.
        3. Und die Karteileichen trotzdem MELDEN, mit Anzahl. Das ist die
           Zeile, die man beim naechsten „warum ist die Karte so voll" braucht.

      ── DIE ALLGEMEINE LEHRE ────────────────────────────────────────────

      EINE PRUEFUNG, DIE IMMER ANSCHLAEGT, IST GENAU SO WERTLOS WIE EINE, DIE
      NIE ANSCHLAEGT — nur teurer, weil sie jedes Mal Aufmerksamkeit kostet.
      Wer eine neue Probe schreibt, laesst sie einmal gegen einen Stand
      laufen, von dem er WEISS, dass er gleich ist. Bleibt sie dann nicht
      still, ist die Probe kaputt, nicht der Stand.
    related:
      - mupi-fork-ausliefern-ablauf
      - ausliefern-regeln
      - kein-traceback-in-eine-datei-die-ohne-ssh-gelesen-wird

  - kind: falle
    id: number-null-ist-null
    title: "`Number(null)` ist 0 und besteht jede Endlichkeitspruefung — zweimal an einem Tag"
    sev: med
    match: "Number\\(null\\)|Number\\(''\\)|Number.isFinite|0 %|0 min|Ladestand 0"
    cause: "`Number(null)` ergibt 0, `Number('')` ergibt 0, `Number(undefined)` dagegen NaN. Eine Pruefung mit `Number.isFinite` faengt deshalb genau die zwei Faelle NICHT, in denen ‚keine Angabe‘ gemeint war — und macht aus fehlendem Wissen eine glaubwuerdige Null."
    fix: "Vor dem Umwandeln auf `null`, `undefined` und `''` einzeln pruefen und `null` zurueckgeben. Ein Wert, den es nicht gibt, muss bis zur Anzeige `null` bleiben; dort wird er zu ‚—‘, nicht zu 0."
    tags: ['typescript', 'javascript', 'null', 'akku', 'weiterhoeren', 'falle', 2026]
    source: "07.08.2026 zweimal am selben Tag aufgetreten und behoben: src/backend-api/src/akkuverlauf.ts (punktAus) und src/backend-api/src/weiterhoeren.ts (fortsetzenMit); die Begruendung steht an beiden Stellen im Quelltext"
    body: |
      ZWEIMAL AN EINEM TAG, an zwei Enden des Programms, von zwei verschiedenen
      Aufgaben aus. Das ist der Grund fuer diesen Eintrag: es ist kein Fehler,
      es ist ein MUSTER.

          Number(null)       ->  0        <- die Falle
          Number('')         ->  0        <- dieselbe Falle
          Number(undefined)  ->  NaN      <- faellt auf
          Number('abc')      ->  NaN      <- faellt auf

      `Number.isFinite(0)` ist `true`. Die uebliche Schutzzeile

          const n = Number(x); if (!Number.isFinite(n)) return null

      faengt also genau die zwei Faelle NICHT, die „keine Angabe" bedeuten.

      ── FALL 1: AUS „KEIN LADESTAND" WURDE 0 % ──────────────────────────

      `akkuverlauf.ts` liest `Bat_SOC` vom MuPiHAT. Der Treiber liefert einen
      LEEREN STRING, wenn der Pack unter die unterste Schwelle faellt. Ohne
      Laengenpruefung waere daraus eine 0 in der Kurve geworden — und eine 0
      in einer Akkukurve sieht aus wie ein leerer Akku, nicht wie ein
      fehlender Messwert. Genau in dem Augenblick, in dem der Betreiber
      hinsieht, haette die Anzeige gelogen.

      ── FALL 2: AUS „KEIN PROZENT GEMERKT" WURDE „BEI 0 % WEITERMACHEN" ──

      `fortsetzenMit()` in `weiterhoeren.ts`: `positionProzent = null` heisst
      „nichts gemerkt". Als 0 durchgereicht heisst es „fang von vorn an" —
      und der Zweig, der die Stelle aus `anteil` ausrechnet, wird nie
      erreicht, weil ja schon ein „gueltiger" Prozentwert da ist. Beim ersten
      Anlauf ist genau das passiert.

      ── DIE GEMEINSAME LEHRE, und sie ist groesser als JavaScript ───────

      DIE NULL IST EINE ANTWORT, KEIN PLATZHALTER. Sobald „ich weiss es nicht"
      und „es ist null" denselben Wert bekommen, ist die Unterscheidung
      unwiederbringlich weg — kein spaeterer Code kann sie zurueckholen. Und
      sie faellt nie beim Programmieren auf, sondern immer erst in der
      Anzeige, wo eine plausible Zahl steht.

      DIE REGEL: `null` reist mit. Es wird nicht in der Mitte in eine Zahl
      uebersetzt, sondern bis zur Anzeige durchgereicht und dort zu „—".
      In beiden Faellen sieht die Schutzfunktion danach so aus:

          const zahl = (x: unknown): number | null => {
            if (x === null || x === undefined || x === '') return null
            const n = Number(x)
            return Number.isFinite(n) && n >= 0 ? n : null
          }

      Wer diese vier Zeilen kopiert, hat beide Faelle erledigt.
    related:
      - mupi-akku-anzeige
      - leerer-wert-heisst-nicht-leere-anzeige

  - kind: knowhow
    id: sackgasse-die-frage-die-eine-bedienung-pruefbar-macht
    title: "‚Sackgasse‘: die eine Frage, die aus einem Bauchgefuehl eine Pruefung macht"
    sev: high
    tags: ['bedienung', 'kinder', 'eltern', 'pruefen', 'methode', 'sackgasse', 2026]
    source: "Begriff und Fragestellung entstanden 06.–08.08.2026 beim Durchgehen der Bedienwege am Geraet; neun gefunden, sechs davon geschlossen"
    body: |
      EINE SACKGASSE IST EINE BEDIENUNG OHNE WEG ZURUECK — AM GERAET.

      Der Begriff selbst ist das Wissen. Er macht aus „das fuehlt sich riskant
      an" eine Frage, die man BEANTWORTEN kann, und zwar immer dieselbe:

          Ein Elternteil tut das.
          Kein zweites Geraet. Kein SSH. Keine Anleitung.
          KOMMT ES ZURUECK?

      Drei Worte davon tragen die ganze Last:

        * „EIN ELTERNTEIL" — nicht der Entwickler, nicht der, der es gebaut
          hat. Jemand, der die Kennung nicht kennt und nicht weiss, dass es
          eine Konfigurationsdatei gibt.
        * „KEIN ZWEITES GERAET" — der haeufigste stille Ausweg. „Dann macht
          man es halt im Browser vom Laptop" ist KEINE Antwort: die Box steht
          in einem Kinderzimmer, und der Laptop ist unten.
        * „KOMMT ES ZURUECK" — nicht „ist es kaputt". Eine Sackgasse macht
          selten etwas kaputt. Sie macht etwas UNERREICHBAR, und das sieht
          harmlos aus.

      ── WARUM MAN DANACH SUCHEN MUSS ────────────────────────────────────

      Eine Sackgasse meldet sich NIE von selbst. Wer hineinlaeuft, kann per
      Definition nicht berichten — er sitzt ja fest. Es gibt keinen
      Protokolleintrag, keine Fehlermeldung, keinen Absturz. Es gibt nur einen
      Betreiber, der aufgibt. Deshalb ist die einzige Art, sie zu finden, das
      systematische Durchgehen JEDER Bedienung mit der Frage oben — vorher,
      nicht nachher.

      ── DIE ZAEHLUNG (Stand 08.08.2026) ─────────────────────────────────

      Neun gefunden, sechs geschlossen. Die Zahl selbst ist die Nachricht:
      neun sind mehr, als jemand geraten haette, und keine einzige war
      vorher gemeldet.

      ── WIE SIE AUSSEHEN — die drei wiederkehrenden Formen ──────────────

        1. EIN SCHALTER, DER SICH SELBST AUSSPERRT. Funk aus schaltet auch den
           Weg ab, ueber den man ihn wieder einschaltet. Die Abhilfe ist nie
           „den Schalter wegnehmen", sondern eine Bedingung, die den letzten
           Weg schuetzt.
        2. EINE HUERDE OHNE ZWEITEN SCHLUESSEL. Eine Anmeldung als Vorgabe ist
           deshalb beim Herkunftsriegel ABGELEHNT worden: „ein vergessenes
           Passwort ist eine neue Sackgasse ohne SSH"
           ([[herkunftsriegel-und-der-namenstausch]]). Eine Sperre, die man
           nur mit einem zweiten Geraet aufbekommt, hat den Schutz gegen das
           Aussperren getauscht.
        3. EIN WEG, DER NIRGENDWO STEHT. Wenn die einzige Art, ins
           Eltern-Menue zu kommen, eine Geste ist, die niemand sieht, ist die
           Box fuer den, der sie nicht kennt, verschlossen — auch ohne Sperre.

      ── UND DIE GEGENPROBE, die man leicht vergisst ─────────────────────

      Auch das BAUEN eines Riegels ist eine Bedienung. Wer eine Sackgasse
      schliesst, prueft dieselbe Frage fuer den Riegel selbst — sonst tauscht
      er nur eine gegen eine andere. Diese Gegenfrage hat beim Herkunftsriegel
      und beim Funkschalter jeweils den Entwurf geaendert.
    related:
      - herkunftsriegel-und-der-namenstausch
      - eltern-geste-vier-ecken
      - taster-wird-von-niemandem-ueberwacht

  - kind: falle
    id: fester-radius-greift-in-der-runden-ecke-ins-leere
    title: "Ein fester Radius greift in einer runden Ecke ins Leere — und der erste helle Punkt ist der Saum, nicht die Bahn"
    sev: med
    match: "border-radius|Ring|Kontrast|1,11|Saum|Antialiasing|greift ins Leere"
    cause: "Bei `border-radius` liegt ein umlaufender Rand auf der DIAGONALEN weiter aussen als an der Kantenmitte. Wer mit EINEM Abstand misst, greift in der Ecke daneben. Und wer von innen nach aussen den ersten Punkt sucht, der sich vom Grund abhebt, findet den antialiasierten Saum — eine Mischfarbe, kein Ring."
    fix: "Den Abstand je Richtung rechnen (in der Ecke `r·√2` statt `r`) ODER den Ring polar abfahren. Und beim Kontrast NICHT den ersten helleren Punkt nehmen, sondern das MAXIMUM ueber ein Fenster, das den Saum ueberspringt."
    tags: ['messen', 'css', 'border-radius', 'antialiasing', 'kontrast', 'werkzeuge', 2026]
    source: "07./08.08.2026 beim Nachmessen des Rings um die laufende Kachel; Rechnung und Begruendung stehen in tools/cover-ring-schau.mjs (Zeilen 243 und 283 ff.)"
    body: |
      ZWEI MESSFEHLER AN DERSELBEN STELLE, und beide melden „kein Ring", waehrend
      der Ring da ist. Sie stehen zusammen, weil sie zusammen auftreten.

      ── FEHLER 1: EIN ABSTAND FUER ALLE RICHTUNGEN ──────────────────────

      An der KANTENMITTE sitzt ein Rand um `r + d` vom Bezugspunkt entfernt.
      In der ECKE folgt er dem Bogen, und der laeuft auf der Diagonalen weiter
      hinaus. Mit `r = 16` und `d = 26` gerechnet:

          Kantenmitte    16 + 26              = 42,0 px
          Ecke (diagonal) 16·√2 + 26  ≈ 22,6 + 26 = 48,6 px

      Ein Werkzeug, das mit einem festen Abstand von 39,5 px in die Ecke
      greift, landet IM LOCH — zwischen Kachel und Ring — und meldet
      wahrheitsgemaess „hier ist nichts". Die Abweichung ist mit 6,6 px kleiner
      als jeder Verdacht und groesser als jeder Ring.

      DIE ABHILFE ist nicht, den Abstand zu vergroessern (dann faellt man an
      der Kantenmitte hinaus), sondern ihn JE RICHTUNG zu rechnen — oder den
      Ring polar abzufahren und das Maximum zu nehmen.

      ── FEHLER 2: DER SAUM SIEHT AUS WIE DIE BAHN ───────────────────────

      Wer von innen nach aussen den ERSTEN Punkt sucht, der sich vom Grund
      abhebt, findet nicht den Ring — er findet den antialiasierten Saum der
      Kachelkante. Das ist eine Mischfarbe aus Kachel und Grund, ein bis zwei
      Pixel breit, und sie hebt sich messbar ab.

          gemessen am Saum          1,11 : 1
          gemessen auf der Bahn    13    : 1

      DER UNTERSCHIED IST FAKTOR ZWOELF, und trotzdem hat das Werkzeug den
      falschen Wert ohne Murren geliefert: 1,11 ist ja ein Kontrast, nur eben
      keiner, den ein Mensch sieht. Es meldete „Ring vorhanden, aber viel zu
      blass" — und geschickt wurde jemand, der die Farbe heller machen sollte.

      DIE ABHILFE: nicht der ERSTE Punkt, sondern das MAXIMUM ueber ein
      Fenster, das breit genug ist, den Saum zu ueberspringen und schmal genug,
      nicht in den naechsten Bildbestandteil zu laufen.

      ── DIE LEHRE, die ueber CSS hinausgeht ─────────────────────────────

      EIN MESSWERT, DER PLAUSIBEL AUSSIEHT, IST DIE TEUERSTE ART VON FALSCH.
      Beide Fehler oben haetten sich sofort verraten, wenn das Werkzeug neben
      der Zahl gesagt haette, WO es gemessen hat — Punkt, Richtung, Abstand.
      Deshalb gehoert in jedes Bildmesswerkzeug eine Ausgabe der MESSSTELLE,
      nicht nur des Ergebnisses. Sie kostet drei Zeilen und macht die Zahl
      nachpruefbar, statt sie nur zu behaupten.
    related:
      - farbwaehler-gegengelesen-am-bild
      - beruehrziele-eingeteilt-74-von-95-sind-ein-pixel
      - vorschau-wird-geliehen

  - kind: howto
    id: pack-verweise-werkzeug
    title: "pack.yaml pruefen: gueltiges YAML und kein Verweis ins Leere — auch fuer den Stand im Index"
    tags: ['wiki', 'pflege', 'werkzeuge', 'git', 2026]
    source: "gebaut 08.08.2026, tools/pack-verweise.py"
    body: |
      `python3 tools/pack-verweise.py` beantwortet vier Fragen in einem Lauf:
      laedt die Datei ueberhaupt, ist jede Kennung nur einmal vergeben, trifft
      jedes `related`-Ziel eine Kennung, und trifft jedes `[[…]]` im Fliesstext
      eine. Rueckgabe 0/1 — damit taugt es fuer `tools/pruefen.sh`.

      ── WARUM `--index` DER EIGENTLICHE GRUND IST ───────────────────────

      An dieser Datei arbeitet mehr als einer gleichzeitig, und mit
      `tools/eigene-hunks.py` legt man nur SEINE Hunks in den Index. Damit gibt
      es zwei Staende: den Arbeitsbaum (alles) und den Index (das, was der
      Commit schreibt). Ein Verweis auf einen Eintrag, den der NACHBAR
      angelegt hat, stimmt im Arbeitsbaum und geht nach dem Commit ins Leere.

          python3 tools/pack-verweise.py           # Arbeitsbaum
          python3 tools/pack-verweise.py --index   # was der Commit schreibt

      Der zweite Lauf ist der, der zaehlt. Der erste sagt nur, dass man sich
      nicht vertippt hat.

      ── DIE URSACHE, DIE ES GEFUNDEN HAT ────────────────────────────────

      Vier leere `related`-Ziele im Bestand, alle mit derselben Ursache: sie
      zeigten auf `vorschau-wird-geliehen` und `wiki-zustand-hat-haltbarkeit` —
      das sind DATEINAMEN aus dem Gedaechtnis unter `~/.claude/…/memory/`,
      keine Wiki-Kennungen. ZWEI Wissensspeicher, EINE Verweis-Schreibweise,
      und nichts, was den Unterschied meldet. Repariert wurden sie am
      08.08.2026 nicht durch Loeschen der Verweise, sondern indem es die
      Eintraege jetzt WIRKLICH gibt: der Verweis war richtig gemeint.
    related:
      - vorschau-wird-geliehen
      - wiki-zustand-hat-haltbarkeit
      - protokolle-lesen-werkzeug

  - kind: falle
    id: dietpi-abbild-ohne-python
    title: "Das DietPi-Abbild hat kein Python — und minimal heisst: ohne Standardbibliothek"
    tags: ['dietpi', 'python', 'vorstart', 'dpkg', 'einrichtung', 2026]
    source: "Kartenlaeufe 08.08.2026, jede Stufe am Geraet erlebt; Abbild per debugfs nachgezaehlt"
    body: |
      1254 Dateien in /usr/bin des Abbilds (RPi5/Trixie): bash, perl,
      wpa_supplicant, iw, dhclient — python3 NICHT. Es kommt erst per apt,
      also mit Netz. Wer die Box OHNE Netz einrichten will (Agent, AP,
      DHCP/DNS — alles Python), braucht ausgerechnet das eine, was ohne Netz
      nicht kommt.

      DIE LOESUNG: sieben .deb (~7 MB) reisen auf der Karte mit und werden im
      Vorstart per dpkg gesetzt. Der Weg dorthin bestand aus DREI Fallen,
      jede hat einen Kartenlauf gekostet:

        1. libexpat1 fehlt dem Abbild — dpkg laesst dann ALLES unkonfiguriert
           ("dependency problems - leaving unconfigured"). Die Abhaengigkeiten
           der Pakete LESEN (dpkg-deb -f … Depends) und gegen den dpkg-Status
           DES ABBILDS pruefen, nicht raten.
        2. dpkg braucht alle Pakete ZUSAMMEN und ZWEIMAL: Pre-Depends verlangt
           die Abhaengigkeit KONFIGURIERT, und die Shell sortiert *.deb nach
           Bytes — "python3-" (45) vor "python3." (46), genau falsch herum.
           Zweiter Lauf + dpkg --configure -a raeumt auf. Und nicht auf den
           Namen python3 bestehen: den Symlink legt erst der Wrapper,
           python3.13 ist derselbe Interpreter.
        3. python3.13-minimal LIEF — und starb an `import secrets`. Die
           Standardbibliothek steckt in libpython3.13-stdlib (dazu media-types
           und netbase, die dem Abbild fehlen). Die Pruefung muss lauten
           "TAUGT Python" (import secrets, http.server), nicht "existiert es"
           — die Existenzpruefung uebersprang die Reparatur.

      PAKETWAHL AUS DEM DEBIAN-POOL: dort liegen ALLE Staende nebeneinander
      (buster bis sid). Auswahl nach Stand-Marker (+deb13uN) und gleicher
      Version fuer Bibliothek und Interpreter — "das Neueste" waere ein
      sid-Paket gegen eine fremde libc, sichtbar erst am Geraet.
    related:
      - vorstart-statt-prescript
      - kopiert-ist-nicht-aufgerufen

  - kind: falle
    id: vorstart-statt-prescript
    title: "Automation_Custom_PreScript.sh existiert nicht — der Vorstart ist eine eigene systemd-Unit"
    tags: ['dietpi', 'vorstart', 'systemd', 'erstboot', 2026]
    source: "zwei verlorene Kartenlaeufe 08.08.2026; alle dietpi-Skripte des Abbilds durchsucht"
    body: |
      ZWEI LAEUFE schrieben brav ein Automation_Custom_PreScript.sh auf die
      Boot-Partition — DietPi kennt diesen Haken NICHT. Der Name kommt in
      KEINEM Skript des Abbilds vor (dietpi-software, dietpi-login,
      dietpi-update, alle func/-Dateien durchsucht). Es gibt nur
      Automation_Custom_Script.sh, und das laeuft am ENDE der
      Erstinstallation — hinter der Stelle, an der es ohne Netz knallt
      ("Unknown install state/First run setup failed").
      NEBENBEFUND: die aeltere WLAN-Selbstdiagnose (diagnose_prescript) hing
      am selben Phantom-Haken und ist nie gelaufen.

      WAS TRAEGT: eine eigene Unit auf der ROOT-Partition — systemd fragt
      DietPi nicht um Erlaubnis. Die Luecke in DietPis eigener Ordnung:

          dietpi-preboot.service    Before=network-pre.target
          dietpi-postboot.service   After=dietpi-preboot.service
          dietpi-login (tty1)       startet den First-Run-Setup  <- ohne Netz tot

      mixpibox-vorstart.service laeuft After=dietpi-preboot,
      Before=dietpi-postboot getty@tty1 — genau dazwischen. Type=oneshot
      HAELT den Boot, waehrend der Assistent laeuft (TimeoutStartSec ueber
      der Wartezeit!). Einschalten auf einer fremden Wurzel ist ein SYMLINK
      in multi-user.target.wants/ — genau das tut systemctl enable.

      DREI ABSICHERUNGEN, jede aus einem stummen Boot geboren:
        * RequiresMountsFor=/boot/firmware — Skript und Log liegen dort.
        * ExecStartPre schreibt Zeitstempel + Mounttabelle nach /var/lib/
          (Root ueberlebt; "lief der Dienst?" ist keine Glaubensfrage mehr).
        * Der Vorstart kopiert bei JEDEM Start ALLES von der FAT-Partition
          nach /opt und exec-t sich bei Aenderung selbst (nach bash -n).
          Der Rechner schreibt nur noch FAT — der sudo-Spiegelschritt auf
          die Root-Partition riss vorher DREIMAL. Und der Abgleich darf
          nicht nur die eigene Datei vergleichen: wer bloss Nachbarn
          erneuert, liess die Box sonst mit alter Fassung laufen.
    related:
      - dietpi-abbild-ohne-python
      - fat-log-darf-nie-toeten
      - kopiert-ist-nicht-aufgerufen

  - kind: falle
    id: fat-log-darf-nie-toeten
    title: "RAMlog frisst Beweise, dreckige FAT wird nur-lesend — und exec >>log beendet die Shell"
    tags: ['dietpi', 'protokoll', 'fat', 'ramlog', 'vorstart', 2026]
    source: "ein unlesbarer 20-min-Lauf und ein wortlos toter Boot, 08./09.08.2026"
    body: |
      DREI VERWANDTE FALLEN, eine Kette:

      1. /var/log IST TMPFS (fstab des Abbilds: "tmpfs /var/log … size=50M").
         Ein Protokoll dort ist nach dem Ausschalten WEG — ausgerechnet nach
         dem gescheiterten Start, den es erklaeren sollte. Protokolle des
         Vorstarts gehoeren auf die FAT-BOOT-PARTITION: sie ueberlebt und ist
         an jedem Rechner ohne root lesbar. Nach jeder Zeile sync.
         (Die Falle stand bereits in einer Commit-Nachricht — und wurde
         Stunden spaeter trotzdem betreten. Aufschreiben schuetzt nicht;
         nur der Pfad im Code schuetzt.)

      2. WER NACH /dev/null LEITET, DEBUGGT BLIND: die Agent-Ausgabe ging
         dorthin, und ein kompletter 20-Minuten-Lauf mit mindestens einem
         WLAN-Wechselversuch hinterliess NICHTS. Der wichtigste Vorgang des
         Abends war der einzige unsichtbare. Erst Schritte zeitgestempelt
         ins ueberlebende Log (nie Passwoerter), dann urteilen.

      3. EINE DRECKIGE FAT (Karte aus laufender Box gezogen -> Dirty-Bit)
         haengt Linux beim ersten Schreibfehler auf NUR-LESEN — und
         `exec >>datei` auf ein unbeschreibbares Ziel BEENDET die Shell.
         Ein Boot starb wortlos an seiner ersten Zeile: kein Schirm, kein
         AP, kein Log — aber /opt aktuell, denn die Kopie davor LIEST nur.
         Seitdem: erst `touch` aufs Log, bei Fehlschlag Rueckfall /tmp —
         loggen darf NIE toeten. Und: Strom weg VOR dem Kartenziehen.
         fsck.fat -a raeumt das Bit (Exit 1 = "wurde repariert" — bricht
         &&-Ketten!).

      MERKSATZ ZUR BOX-UHR: ohne NTP steht sie auf Abbild-Bauzeit. Die
      REIHENFOLGE im Log ist verlaesslich, die Zeitstempel nicht — und der
      erste ECHTE Zeitstempel im Log ist zugleich der Beweis, dass die Box
      Internet hatte.
    related:
      - vorstart-statt-prescript
      - protokolle-lesen-werkzeug

  - kind: knowhow
    id: ap-ohne-hostapd-wpa-mode2
    title: "Der Einrichtungs-AP ohne hostapd: wpa_supplicant mode=2 — und seine drei Minen"
    tags: ['wlan', 'ap', 'wpa_supplicant', 'einrichtung', 'pi5', 2026]
    source: "am Pi 5 durchgekaempft 08./09.08.2026, jeder Punkt am Geraet belegt"
    body: |
      hostapd und dnsmasq sind auf dem Abbild nicht da und ohne Netz nicht zu
      holen — aber UNNOETIG: wpa_supplicant (immer da, wenn die Box WLAN kann)
      spannt mit mode=2 selbst ein Netz auf. AM PI 5 BELEGT. DHCP + DNS +
      Captive Portal gehen in ~250 Zeilen Standardbibliothek (kleiner-dhcp.py):
      jeder DNS-Name antwortet mit der Box, die Verbindungspruefungen von
      Android/iOS/Windows bekommen NICHT ihre erwartete Antwort — das Handy
      oeffnet die Seite von selbst. AAAA bekommt eine LEERE Antwort statt
      einer falschen, sonst wartet das Handy auf IPv6.

      DIE DREI MINEN, in der Reihenfolge ihres Auftretens:

      1. RFKILL: das Programm fehlt auf dem Abbild — der Kernel-Schalter
         existiert trotzdem. Entsperren direkt ueber /sys/class/rfkill/*/soft
         (0 hinein). Dazu die Module: das Abbild blacklistet cfg80211
         (dietpi-disable_wifi.conf), brcmfmac laedt aber per SDIO-Erkennung
         und zieht cfg80211 als Abhaengigkeit mit — die Sperrliste blockt nur
         den direkten Autoload. Freischalten wie DietPis eigenes
         `wifimodules enable` schadet nie.

      2. DIE EIGENE ADRESSE IST KEIN NETZ: die Warteschleife des Vorstarts
         hielt die frisch gesetzte 192.168.4.1 fuer "Netz da" und riss den
         ERSTEN funktionierenden AP nach Sekunden selbst ab. "Fremdnetz" =
         globale Adresse, die NICHT die eigene ist.

      3. AP_SCAN=2 SABOTIERT DEN CLIENT-WECHSEL: die AP-Instanz laeuft mit
         ap_scan=2 (noetig fuer mode=2) — im Client-Betrieb heisst das "nicht
         scannen, blind assoziieren", und der Beitritt scheitert MIT RICHTIGEM
         Passwort. Vor select_network: ap_scan 1. Im Rueckweg: ap_scan 2,
         sonst kommt der eigene AP nicht zurueck. Und Erfolg ist erst
         wpa_state=COMPLETED MIT ssid=Zielnetz — direkt nach dem Umschalten
         meldet der Status noch das COMPLETED des eigenen AP (Wettlauf).

      DIE UEBERGABE INS HEIMNETZ: ERST ANTWORTEN, DANN WECHSELN — die Seite
      auf dem Handy stirbt mit dem AP, eine spaetere Antwort erreicht
      niemanden ("danach passiert nichts erkennbares"). Rueckmeldung kann nur
      der SCHIRM DER BOX geben (Hinweiszeile aus /run + Kopie ins FAT-Log).
      Nach dem Handschlag: AP-Adresse weg, dhclient, wpa_supplicant.conf und
      interfaces FESTSCHREIBEN (exakt DietPis sed bei WIFI_ENABLED=1), und
      bei falschem Passwort der RUECKWEG zum eigenen AP — sonst strandet
      jeder Tippfehler die Einrichtung. `stoppen` muss die Uebergabe kennen:
      dieselbe wpa_supplicant-Instanz traegt danach die HEIMNETZ-Verbindung.
    related:
      - einrichtung-ap-und-captive-portal
      - handy-glaubt-dem-gespeicherten-profil

  - kind: entscheidung
    id: handy-glaubt-dem-gespeicherten-profil
    title: "Offenes Einrichtungsnetz mit Code statt WPA2 — und warum der Netzname wechseln musste"
    tags: ['einrichtung', 'wlan', 'sicherheit', 'handy', 2026]
    source: "Einrichtungsversuche mit echtem Handy, 08./09.08.2026"
    body: |
      DREI ENTSCHEIDUNGEN AUS ECHTEM HANDY-KONTAKT:

      1. DAS EINRICHTUNGSNETZ IST OFFEN. Das WPA2-Passwort war eine zweite
         Tuer vor derselben ersten (die Tuer ist der Pair-Code auf dem
         Schirm: 6 Ziffern, harter Fehlerdeckel, Anwesenheit noetig) — und
         der WPA2-Handschlag mit dem wpa_supplicant-AP scheiterte am Handy
         obendrein ("Passwort falsch" trotz korrektem Passwort). Ein offenes
         Netz hat keinen Handschlag, an dem etwas scheitern kann. Es lebt
         Minuten, kein NAT, stoppen raeumt restlos.

      2. HANDYS GLAUBEN IHREM GESPEICHERTEN PROFIL, NICHT DER REALITAET:
         wer die WPA2-Zwischenfassung des Netzes gespeichert hatte, wurde
         beim nun OFFENEN Netz weiter nach einem Passwort gefragt. "Netz
         vergessen" muss man erst finden — ein NEUER NETZNAME ist billiger
         (MixPi Start). Ein einmal verbrannter Name darf nie wiederkommen.

      3. WER UEBER DAS EIGENE NETZ DER BOX KOMMT, BRAUCHT KEINEN CODE MEHR:
         die Verbindung selbst ist der Naehe-Beweis. Der automatische
         Schluessel ist BESCHRAENKT (nur Einrichtungswege — nie /run, /exec,
         /put, /shutdown): ein Nachbar in Funkreichweite koennte
         schlimmstenfalls waehrend der Einrichtungsminuten ein WLAN
         eintragen, nie Befehle als root ausfuehren. Aus dem HEIMNETZ bleibt
         der Code Pflicht — dort hoeren mehr Geraete mit. Bedingungen beide
         noetig: AP laeuft (ssid-Datei als Beleg) UND Absender aus dessen
         Netz; localhost bekommt nichts geschenkt.

      DAZU GELERNT: die WLAN-Liste zum Antippen wird VOR dem AP-Start
      gescannt (ein Funkbaustein, eine Rolle — im AP-Betrieb geht kein Scan)
      und zwischengespeichert; Antippen fragt nur noch das Passwort, mit
      "Passwort anzeigen" — ein vertipptes WLAN-Passwort ist am Handy der
      Normalfall, nicht die Ausnahme.
    related:
      - ap-ohne-hostapd-wpa-mode2
      - zugangsschluessel-im-browser
  - kind: falle
    id: ard-numberofelements-luegt-auch-bei-null
    title: "ARD `numberOfElements`: die Null luegt genauso wie die grosse Zahl"
    tags: [ard, audiothek, graphql, messen, 2026]
    source: "Gemessen am 15.08.2026 gegen api.ardaudiothek.de, beim Bau des Kinderregals"
    body: |
      Dass `numberOfElements` einer ARD-Sendung zu hoch liegt, war bekannt
      (MausZoom: Faktor 5,4). NEU und teurer ist die andere Richtung: die
      Zahl luegt auch, wenn sie NULL ist.

        radioMikro           behauptet 0    liefert 5 Folgen, alle mit Ton
        Professorin Domino   behauptet 0    liefert 5 Folgen, alle mit Ton
        Ukrain. Maerchen     behauptet 0    liefert 5 Folgen, alle mit Ton
        Betthupferl          behauptet 926  liefert 5 Folgen, KEINE mit Ton

      WARUM DAS GEFAEHRLICH IST: `numberOfElements < 1` als Filter sieht wie
      eine Selbstverstaendlichkeit aus - man will keine Kachel anbieten,
      hinter der nichts liegt. Genau dieser Filter haette im Regal "Fuer
      Kinder" drei gute Sendungen weggeworfen und die eine stumme behalten.
      Er war schon geschrieben und ist nur deshalb nicht hineingekommen,
      weil vor dem Einbau gemessen wurde.

      WORAN MAN ES STATTDESSEN ERKENNT: einzig an der Folgenliste mit ihrem
      Tonfeld (`items{ nodes { audios { url } } }`). Wer wissen will, ob
      etwas abspielbar ist, muss die Folgen holen; eine Kennzahl am
      Sendungsobjekt beantwortet die Frage nicht.

      GILT NICHT FUER SAMMLUNGEN: bei `editorialCollections` ist
      `numberOfElements: 0` bisher ehrlich gewesen (leere Sammlungen gibt es
      wirklich). Zwei verschiedene Objekttypen, zwei verschiedene
      Verlaesslichkeiten - die eine Messung traegt nicht zur anderen.
  - kind: falle
    id: spa-antwortet-immer-200
    title: "Ein 200 von einer Einzelseiten-App ist kein Beweis, sondern Hoeflichkeit"
    tags: [verwaltung, box, messen, http, 2026]
    source: "Gemessen am 15.08.2026 auf der Box, nachdem die Figurenbilder in der Verwaltung fehlten"
    body: |
      Auf die Frage "sind diese Bilder erreichbar" wurde der Status abgefragt:

          curl -o /dev/null -w "%{http_code}" .../bilder/figuren/x.png -> 200

      und daraus "in Ordnung" geschlossen. FALSCH. Die Einzelseiten-App
      beantwortet JEDEN unbekannten Weg mit ihrer index.html - es kommt immer
      200 zurueck, und im Bild steckt HTML. Erst der Inhaltstyp trennt:

          /bilder/figuren/x.png       200  text/html    9806 B  <- Ersatzseite
          /neu/bilder/figuren/x.png   200  image/png   31189 B  <- das Bild

      DIE REGEL: Wer prueft, ob eine DATEI ausgeliefert wird, prueft
      `content_type` und die Groesse. Der Status taugt bei einer SPA nur, um
      einen abgestuerzten Server zu erkennen, nicht um eine fehlende Datei zu
      finden. Werkzeug dafuer: tools/bilder-wirklich-da.py.

      DIE URSACHE DAHINTER war ein relativer Pfad: `ordner` aus /api/figuren
      ist relativ zur BOX-Oberflaeche, die unter /neu/ laeuft. Aus der
      Verwaltung zeigt er ins Leere. Geloest NICHT durch ein "/neu/" in der
      Verwaltung (dann raet die naechste Oberflaeche wieder), sondern indem
      der Server die brauchbare Adresse selbst nennt (`webOrdner`).

      ZWEI NEBENFALLEN beim Bau des Werkzeugs, beide kosten sonst eine
      Viertelstunde: das curl-Format verliert ueber `ssh` seine Anfuehrung
      (shlex.join statt " ".join), und der Inhaltstyp traegt SELBST ein
      Leerzeichen ("text/html; charset=utf-8") - ein Aufteilen an
      Leerzeichen zerlegt genau das Feld, um das es geht. Trenner: "|".
  - kind: entscheidung
    id: hoerzeit-per-herzschlag
    title: "Hoerzeit wird am Herzschlag gemessen, nicht an der Wanduhr"
    tags: [statistik, verlauf, profile, messen, 2026]
    source: "Gebaut am 15.08.2026 (verlauf.ts), Betreiber wollte 'welchen dienst wie lange'"
    body: |
      Die naheliegende Rechnung "Stoppzeit minus Startzeit" ist falsch: eine
      Box, die um 15:00 startet und um 19:00 stumm in der Ecke steht, weil das
      Kind weggelaufen ist, haette vier Stunden Hoerzeit - und die Statistik
      behauptete etwas, das nie stattgefunden hat.

      STATTDESSEN ZAEHLT DER HERZSCHLAG. Die Box-Oberflaeche meldet ohnehin
      alle 2 s (TAKT_DIENST in NewDesign/app.js), WAS gerade laeuft; der
      Server bucht das an POST /api/weiterhoeren mit. Die Dauer ist die Summe
      der Abstaende - gemessene Zeit statt geschaetzter.

      DREI GRENZEN, die es ohne Messung nicht braucht, mit ihr aber sehr wohl:
        LUECKE_MS 30 s      ohne Schlag laenger als das ist die Sitzung zu Ende
        SCHLAG_MAX_MS 10 s  so viel darf EIN Abstand hoechstens gutschreiben,
                            sonst erfindet eine haengende Verbindung Minuten
        MIN_SEKUNDEN 5      darunter wird nichts aufgeschrieben, sonst besteht
                            der Verlauf aus Fehlgriffen beim Durchblaettern

      Eine zurueckspringende Uhr (NTP nach dem Start!) darf die Zeit nicht
      negativ machen - dafuer gibt es einen eigenen Zweig und einen Test.

      ER LAEUFT FUER ALLE PROFILE MIT, ohne Einschalten und ohne Auswahl. Ein
      Mitschnitt, den man erst anstellen muss, ist am Tag der Frage leer -
      und die Frage kommt immer rueckwirkend ("was hat sie gestern gehoert?").
      Geschrieben wird nur beim ABSCHLUSS einer Sitzung: alle zwei Sekunden
      eine Datei zu ersetzen kostet SD-Karten.
  - kind: falle
    id: grenze-deckelt-still
    title: "Zwei Deckel hintereinander - eine kuerzere Liste sieht nie nach einem Fehler aus"
    tags: [ard, verwaltung, messen, 2026]
    source: "Gemessen am 15.08.2026 beim Bau der ARD-Regale"
    body: |
      Beim Holen der ARD-Kategorien standen mehrere Regale auf exakt 200
      Sendungen - verdaechtig glatt. Nachgemessen mit 300, 500 und 1000:
      "Doku & Reportage" traegt 297, und bei 500 wie 1000 kommen dieselben
      297. Also war 200 der eigene Deckel, nicht die Groesse des Regals.

      NACH DEM ANHEBEN GAB DIE BOX TROTZDEM NUR 100 ZURUECK. Der zweite
      Deckel sass im eigenen Modul: `grenze(n, hoechstens = 100)` in ard.ts
      kappt jede Anzahl still. Der Server fragte 400 und bekam 100.

      DIE EIGENTLICHE LEHRE ist nicht die Zahl, sondern die Form des
      Fehlers: eine kuerzere Liste sieht NIE nach einem Fehler aus, sie sieht
      nach einem kleineren Regal aus. Es gibt keine Fehlermeldung, keinen
      roten Balken, nichts. Gefunden wurde es nur, weil nach dem Ausliefern
      am Geraet NACHGEZAEHLT wurde.

      Angehoben wurde deshalb NUR die Kategorie-Abfrage; Suche und
      Regalliste behalten die 100, dort ist sie richtig. Drei Tests halten
      es fest, darunter der Gegenbeweis, dass weiterhin gedeckelt wird
      (99999 -> 400).
  - kind: falle
    id: backticks-beenden-jede-vorlage
    title: "Ein Backtick im Kommentar INNERHALB einer Angular-Vorlage reisst die Datei ab"
    tags: [verwaltung, angular, werkzeug, 2026]
    source: "Am 15.08.2026 an EINEM Tag zweimal getreten (flex-basis, aria-hidden)"
    body: |
      In einem Kommentar innerhalb von `template:` oder `styles:` schreibt man
      beilaeufig einen Bezeichner in Anfuehrung - flex-basis, aria-hidden,
      overflow. Der Backtick BEENDET das Template-Literal. Danach ist die Datei
      syntaktisch Unsinn, und der Compiler meldet an voellig anderer Stelle:

          NG1001: Decorator argument must be literal.   <- Zeile 149
          TS2362: left-hand side of an arithmetic ...   <- Zeile 149
          TS1005: ',' expected                          <- HIER steht er

      Man sucht dann in Zeile 149, wo nichts ist. Der Fehler steht 170 Zeilen
      weiter unten. Genau das kostet jedes Mal eine Viertelstunde.

      DIE REGEL: in Kommentaren innerhalb einer Vorlage keine Backticks.
      Namen ohne Anfuehrung schreiben oder Anfuehrungszeichen verwenden.

      DAS WERKZEUG STATT DER WARNUNG (denn die Warnung allein hat nicht
      gereicht - ich bin an einem Tag zweimal hineingetreten):

          python3 tools/backtick-in-vorlage.py

      Es liest jede Vorlage der Verwaltung und meldet Backticks DARIN, mit
      Datei und Zeile - also dort, wo der Fehler wirklich steht. Gegenprobe
      gemacht: 42 Dateien sauber, ein eingesetzter Backtick wird gefunden.
  - kind: falle
    id: gebaut-und-unerreichbar
    title: "Gebaut und unerreichbar - die Liste, die zum Fertigen fuehrt, fehlt"
    tags: [verwaltung, box, oberflaeche, 2026]
    source: "Am 15./16.08.2026 ZWEIMAL an einem Tag getreten"
    body: |
      Zweimal an einem Tag dasselbe Muster: etwas ist fertig gebaut,
      ausgeliefert und funktioniert - und niemand kommt hin.

        1. Die Seite "Profile" (frueher "Kinderzeit"). Ueberschrift, Route,
           Uebersichtskachel und Suchbestand waren umbenannt. Der MENUEPUNKT
           hiess weiter "Kinderzeit". Betreiber: "wo kann ich die
           spielstatistik sehen".
        2. Die Bilder-Tastatur als fuenfte Passwort-Art. Server erlaubte sie,
           die Verwaltung bot sie an, die Eingabe war gebaut - die Frage
           "Woraus soll dein Passwort bestehen?" an der Box kannte nur vier.
           Betreiber: "ist sie nicht eingehaengt".

      DIE URSACHE IST NIE DER CODE, sondern eine AUFZAEHLUNG, an die man nicht
      denkt: ein Menue, ein Auswahlfeld, eine Wortliste. Sie steht woanders
      als das Gebaute und faellt beim Testen nicht auf, weil man selbst den
      direkten Weg nimmt.

      GEGENMITTEL: nach jedem neuen Ding die Listen ZAEHLEN, nicht hoffen.
      Beim zweiten Mal so gemacht und dabei eine dritte Luecke gefunden, die
      sonst erst dem Betreiber aufgefallen waere:

          Server (PASSWORT_ARTEN)  bilder farben muster zahlen zeichen
          Wortliste (PW_ART_WORT)  bilder farben muster zahlen zeichen   <- fehlte
          Auswahl an der Box       bilder farben muster zahlen zeichen   <- fehlte

      Ohne die Wortliste haette die Box "Dein Passwort (?)" gefragt.
  - kind: falle
    id: angular-select-zwei-quellen
    title: "Angular-Auswahlfeld: value am select UND selected an den Optionen streiten sich"
    tags: [verwaltung, angular, 2026]
    source: "Betreiber am 16.08.2026: 'wenn auto gemalt angezeigt wird ist bonbon ausgewaehlt'"
    body: |
      Ein Auswahlfeld zeigte durchgaengig einen anderen Eintrag an, als
      tatsaechlich galt. Im Blatt stand BEIDES:

          <select [value]="w().form">
            @for (f of FORMEN; track f.id) {
              <option [value]="f.id" [selected]="w().form === f.id">

      Der Browser waehlt nach dem selected-Attribut, Angular setzt danach den
      Wert am Select - und je nachdem, was zuletzt greift, zeigt das Feld
      etwas anderes an als gilt. EINE Quelle genuegt; das selected an den
      Optionen faellt weg.

      VORHER MESSEN, WO ES NICHT HERKOMMT: der erste Verdacht war, dass die
      Liste in der Box und die in der Verwaltung auseinandergelaufen sind
      (dieselben Ids in derselben Reihenfolge sind Pflicht). Ein Vergleich
      der beiden Id-Listen hat das in zehn Sekunden ausgeschlossen und eine
      falsche Faehrte erspart.
  - kind: falle
    id: css-klasse-am-body-ist-kein-element
    title: "body.fp-an ist eine Klasse AM BODY - `.fp-an` als Element trifft nichts"
    tags: [box, css, 2026]
    source: "Betreiber am 16.08.2026: 'auto gemalt ist nur ein block'"
    body: |
      Fuer die gemalten Fortschritts-Punkte war eine Regel geschrieben:

          body.ff-bild .fp-an { ... }

      Sie griff nie. `.fp-an` ist eine Klasse AM BODY (sie schaltet den Punkt
      ueberhaupt erst ein), kein Element im Baum. Der Punkt selbst ist
      `.mp-balken i::after` bzw. `.gross-balken i::after`.

      DAS ERGEBNIS SAH NICHT NACH EINEM CSS-FEHLER AUS, sondern nach einer
      falschen Zuordnung: die Farbflaeche der Grundregel blieb stehen, also
      erschien ein Quadrat - und der Betreiber vermutete, es sei das falsche
      Bild ausgewaehlt.

      Wer eine bestehende Anzeige ueberschreibt, liest ZUERST ihre Regel und
      uebernimmt deren Selektor. Dazu gehoerte hier auch `background: none`
      (die Farbe leuchtete sonst als Quadrat hinter dem Bild) und das
      Entfernen des hellen Randes, der das Bild als Kasten umrandete.
  - kind: entscheidung
    id: freistellen-flutfuellen
    title: "Vorlagen freistellen: Flutfuellen vom Rand, nicht 'alles Weisse transparent'"
    tags: [bilder, werkzeug, 2026]
    source: "16.08.2026, 17 KI-Vorlagen fuer Tastatur und Fortschrittsbalken"
    body: |
      Die naheliegende Regel "Pixel heller als X wird durchsichtig" ist
      falsch, sobald das MOTIV Weiss traegt - und das tut es fast immer: der
      Wuerfel ist fast ganz weiss, der Geist auch, der Panda hat ein weisses
      Gesicht. Die Regel stanzt genau dort Loecher hinein, auf 1024 px kaum
      zu sehen, auf 44 px ein Fehler.

      RICHTIG: Flutfuellen VOM RAND. Durchsichtig wird nur, was mit dem
      Bildrand zusammenhaengt. Was innen liegt, bleibt, egal wie hell.

      ZWEITE FALLE, DER SAUM: die Ecken sind (254,254,254), nicht 255, und
      untereinander nicht einmal gleich; der Uebergang zum Motiv ist weich.
      Wer hart schneidet, bekommt einen hellen Rand um jede Figur - auf
      dunklem Grund ein Heiligenschein. Die Deckkraft am Uebergang MITZIEHEN
      statt umschalten.

      Dazu: auf das Motiv beschneiden und quadratisch auffuellen (die
      Erzeuger setzen es nicht mittig), und die Zielgroesse nach der
      VERWENDUNG waehlen, nicht nach Gefuehl - dann in ECHTER Groesse
      gegenlesen. Werkzeug: tools/vorlagen-freistellen.py.
  - kind: entscheidung
    id: icons-grundformen-statt-freihand
    title: "Icons aus Grundformen bauen, nicht freihand - und die HALTUNG entscheidet"
    tags: [bilder, svg, 2026]
    source: "15./16.08.2026, elf gezeichnete Tastenbilder (spaeter durch KI-Vorlagen ersetzt)"
    body: |
      Beim Schreiben von SVG-Pfaddaten sieht man das Ergebnis erst nach dem
      Rendern. Bei Kreisen und Rechtecken ist das egal - Wuerfel, Stern,
      Panda, Erde gelangen auf Anhieb. Bei organischen Silhouetten raet man
      Bezier-Kontrollpunkte und korrigiert ins Blaue: das Einhorn brauchte
      DREI Anlaeufe und war dreimal ein Schwein, die Katze zwei.

      REGEL: aus Ellipsen, Dreiecken und Rechtecken bauen, deren Lage sich
      NACHRECHNEN laesst. Bei der Erde steht der Beweis als assert im
      Werkzeug (Abstand vom Mittelpunkt plus Radius kleiner als der Kugel-
      radius), nicht als Hoffnung.

      UND DIE HALTUNG IST WICHTIGER ALS DIE SORGFALT. Beide misslungenen
      Bilder wurden beim ERSTEN Anlauf richtig, nachdem die Ansicht wechselte:
        Einhorn: von der Seite ist das Horn ein Strich am Bildrand und ein
                 rosa Rumpf mit kurzen Beinen IST ein Schwein. Von VORN nimmt
                 das Horn ein Viertel des Bildes ein.
        Katze:   von vorn ist ein runder Kopf mit Ohrbeulen von einem Baeren
                 nicht zu unterscheiden. VON DER SEITE traegt der
                 geschwungene Schwanz die Erkennung.
      Wer dreimal nachbessert, sollte die ANSICHT wechseln statt die Kurven.
  - kind: referenz
    id: was-je-profil-gilt
    title: "Was je Profil gilt und was der ganzen Box - gemessen am Geraet"
    tags: [profile, ablage, 2026]
    source: "Am 16.08.2026 auf der Box nachgesehen (config/profile/<kind>/ gegen config/)"
    body: |
      JE PROFIL (config/profile/<kennung>/):
        darstellung.json          Farbthema, Licht, Mini-Player (Variante,
                                  Breite, Hoehe), Startseite, Kachelgroessen,
                                  Statusleiste, Fortschritts-Punkt und -Form,
                                  Kategorien-Aufbau
        resume.json               Hoerstaende
        gespielt.json             Verlauf, "oft gehoert"
        kinderzeit-verbrauch.json die heute verbrauchte Zeit
        auswahl.json              welche Medien sichtbar sind (fehlt, solange
                                  das Profil alles sieht - leer heisst alles)
        verlauf.json              der Mitschnitt (ab 15.08.2026)

      GEMEINSAM (config/):
        klang.json      Ton, Equalizer, Deckel - das ist der Lautsprecher im
                        Raum und nicht das Kind
        kinderzeit.json die Hausregel (mit eigenen Regeln JE KIND darin)
        data.json       die Bibliothek selbst
        config.json, network.json, wlan.json, plugin-*.json

      Beim Profilwechsel greift also jeweils der eigene Darstellungssatz.
      Gemessen statt vermutet: Kalea hatte 20 Einstellungen gespeichert, der
      Gast sieben.
  - kind: falle
    id: darstellung-zwei-schreiber
    title: "Zwei Schreiber auf einer Datei - der zweite loescht, was er nicht kennt"
    tags: [profile, darstellung, server, 2026]
    source: "Betreiber am 16.08.2026: 'bei kalea hat es gerade nicht mehr das einhorn geladen'"
    body: |
      Eine frisch gesetzte Darstellungs-Einstellung verschwand beim
      Profilwechsel. GEMESSEN, nicht vermutet: Kaleas darstellung.json trug
      56 Einstellungen, und `fortschrittForm` war nicht falsch gesetzt,
      sondern GAR NICHT MEHR DA. Beim Gast (aelterer Stand) stand sie noch.
      Zwei Dateien nebeneinander sagen mehr als jede Theorie.

      URSACHE: `PUT /api/darstellung` ersetzte `aktuell` VOLLSTAENDIG, und es
      gibt ZWEI Schreiber darauf.
        * Die VERWALTUNG setzt EINEN Schluessel.
        * Die BOX schreibt spaeter IHREN GANZEN Stand zurueck - und kennt den
          Schluessel nicht, weil ihre Seite geladen wurde, bevor es ihn gab.
        * Der Profilwechsel laedt die Box neu. Also schreibt sie genau dann.

      Ein voller Ersatz macht damit das Feld des einen Schreibers zur Waffe
      gegen den anderen, und zwar lautlos. Geloest durch ZUSAMMENFUEHREN: ein
      Schreiber kann nur noch AENDERN, was er kennt.

      DIE KEHRSEITE GEHOERT MITGEPRUEFT, sonst ist sie die naechste stille
      Falle: Abschalten geht weiter, dafuer schickt man den Schluessel MIT
      seinem neuen Wert (false, '', 0). Bloss weglassen heisst ab jetzt
      "dazu sage ich nichts" und nicht mehr "loesch das".

      WARUM ERST JETZT: der Fehler lag seit dem Profil-Umbau da und wartete
      auf ein NEUES FELD - neue Schluessel sind genau die, die ein Schreiber
      noch nicht kennt. Wer eine geteilte Ablage baut, prueft das am Tag des
      Umbaus, nicht wenn das erste neue Feld dazukommt.
  - kind: falle
    id: zwei-aufrufer-eine-regel
    title: "Profilwechsel: es gibt ZWEI Wege, und der benutzte ist nicht der offensichtliche"
    tags: [box, profile, oberflaeche, 2026]
    source: "Betreiber am 16.08.2026: 'das mit dem stoppen und wieder laden klappt nicht'"
    body: |
      Beim Profilwechsel sollte die Wiedergabe anhalten. Eingebaut, geliefert
      - und es lief weiter.

      URSACHE: ZWEI Stellen rufen `POST /api/profil/aktiv`.
        `kinder.umschalten`  der naheliegende Weg
        `ich.werWaehlen`     das Profilfenster MIT PASSWORT - und das ist
                             der Weg, den man an der Box wirklich nimmt
      Geaendert war nur der erste. Im Kommentar des zweiten stand sogar noch
      die alte Annahme: "Die Wiedergabe stoert das nicht."

      Dasselbe Muster wie [gebaut-und-unerreichbar], nur mit einem zweiten
      AUFRUFER statt einer zweiten Liste. Gegenmittel ist dasselbe: die
      Aufrufstellen ZAEHLEN, bevor man sich fertig glaubt.

      UND EINE FALLE BEIM ZUSAMMENLEGEN: die beiden Wege liegen in ZWEI
      VERSCHIEDENEN Objekten (`kinder`, `ich`). Eine gemeinsame Naht als
      METHODE an einem der beiden ist am anderen `undefined` und zerbricht
      den Wechsel ganz - sichtbar erst am Geraet. Freie Funktion nehmen.

      DIE REIHENFOLGE IST NICHT BELIEBIG:
        VOR der Passwortfrage   die Stelle melden - noch als altes Kind,
                                sonst erbt das neue sie
        NACH dem Erfolg         anhalten und leeren - wer sein Passwort
                                falsch tippt, soll nicht die Musik seines
                                Geschwisters abwuergen
  - kind: falle
    id: rohes-play-belebt-nichts
    title: "Nach einem stop kann ein rohes play nichts wiederbeleben"
    tags: [box, abspielweg, profile, messen, 2026]
    source: "Betreiber am 16.08.2026: 'was aber nicht geht es wieder zu starten'"
    body: |
      Seit der Profilwechsel anhaelt, raeumt `stop` die Tonmaschine: mpv ist
      idle, `currentPlayer` leer. Der Spielknopf im Mini-Player schickte aber
      nur den nackten play/pause-Umschalter - und der trifft eine geleerte
      Maschine nicht. VORHER wirkte der Knopf nur deshalb, weil die Musik
      beim Wechsel nie stoppte. Wer irgendwo ein stop einbaut, muss den
      Spielknopf mitdenken.

      Der Rueckfall: ist nichts geladen, startet der Knopf das juengste Werk
      aus "weiter hoeren" ueber `weiterSpielen` - mit Kinderzeit-Pruefung,
      richtiger Maschine und gemerkter Stelle. Nie von selbst, nur auf Druck.

      DREI MESSFALLEN aus derselben Suche, alle selbst gestellt:
        * `runuser -u dietpi` braucht ROOT. Als dietpi aufgerufen liefert es
          LEER - und "keine sink-inputs" war eine Falschmessung. Als dietpi
          direkt `pactl` rufen.
        * `/mupibox/ard/<SENDUNGSNUMMER>` nimmt der Abspieldienst mit HTTP
          200 an und laedt dann NICHTS ("track name is" leer, Pfad =
          Arbeitsverzeichnis). Der echte Befehl traegt die TONADRESSE aus
          /api/werke/<s>/inhalt. Ein 200 ist auch hier kein Beweis.
        * Ein Test-Abspielen auf der Box spielt WIRKLICH - nachts um eins im
          Kinderzimmer. Sofortiger stop gehoert in denselben Messblock wie
          der Start.

      Ob wirklich Ton fliesst, sagt die Kette: mpv-IPC (`idle-active`,
      `time-pos` laeuft) plus `pactl list sink-inputs` (uncorked, app mpv).
      Der Zustand des Abspieldienstes allein sagt es NICHT - der meldete
      pause=False und currentTrackname gesetzt, waehrend mpv idle war.
  - kind: referenz
    id: gast-ist-schaltbar
    title: "Der Gast ist schaltbar (E39) - aus heisst unsichtbar, nie geloescht"
    tags: [profile, gast, box, 2026]
    source: "Gebaut und am Geraet bewiesen am 16.08.2026"
    body: |
      Der Gast traegt seit E39 zwei Felder im Profilstand: `gastAktiv`
      (nur ein AUSDRUECKLICHES false schaltet ab - Tippfehler in der Datei
      duerfen den Rueckfall nicht verstecken) und `gastBis` (Frist in ms,
      TRAEGE geprueft beim naechsten Blick, kein Timer - der stuerbe mit dem
      Prozess).

      AUS heisst: keine Kachel an der Box, kein Abmelden-Ziel, Wechsel auf
      ihn wird am SERVER abgewiesen (400 gastAus). Er bleibt im Stand - er
      ist der Rueckfall und Besitzer von Dateien; ein geloeschtes Kind
      faellt weiter auf ihn.

      EINSCHALTEN LEERT IHN: resume, gespielt, verlauf, auswahl, verbrauch
      werden entfernt (Besuchskonto). `uebernehmenVon` kopiert die
      MEDIENLISTE eines Profils - kopiert, nicht geteilt, nie den
      Abspielstand. Endpunkt: POST /api/profil/gast {an, stunden?,
      uebernehmenVon?}.

      DER GAST HEISST GAST: profileNormalisieren streift jeden fremden Namen
      ab (die Figur bleibt waehlbar). Ein umbenannter Gast ("Papa") hat den
      ganzen Bedarf ausgeloest - er sah aus wie ein normales Konto und
      verhielt sich nicht so.

      OHNE GAST VERLANGT DIE BOX EINE ANMELDUNG: eine SITZUNGS-Marke
      (sessionStorage) unterscheidet den Kaltstart (blockierendes
      Anmeldefenster ohne Schliessen-Knopf) vom Neuladen eines gewoehnlichen
      Wechsels. Offener Rest: dasselbe Profil laesst sich ohne Passwort
      wieder betreten - der Server prueft Schloesser nur bei ECHTEN Wechseln.

      PUT /api/profile MUSS die zwei Felder mitreichen (standNormalisieren
      liest sie aus dem Rohwert) - sonst setzt jedes Speichern der
      Profilliste den Schalter still auf AN zurueck.
  - kind: falle
    id: altlast-sieht-aus-wie-der-fehler
    title: "Die Altlast einer Panne sieht aus wie die Panne selbst - Zeitstempel lesen"
    tags: [profile, resume, messen, 2026]
    source: "16.08.2026, Profilwechsel-Test ('noch nicht')"
    body: |
      Ein Fehler (Stopp NACH dem Wechsel schrieb Liams Lied in Kaleas
      Hoerstaende) war an der Wurzel behoben - und der Betreiber sah beim
      Nachtest trotzdem dasselbe Bild: Liams Lied bei Kalea. "noch nicht".

      GEMESSEN statt nachgebessert: beide resume.json nebeneinander, MIT
      ZEITSTEMPELN. Liams Datei trug sein Lied korrekt (01:50:19, vom neuen
      Server-Stopp geschrieben). Kaleas Datei trug ihre eigenen Eintraege
      PLUS die ALTLAST der ersten Panne - und deren Zeitstempel passte zum
      Play-Druck, nicht zum Wechsel: der Weiter-hoeren-Rueckfall des
      Spielknopfs startet "das juengste Werk", also genau die Altlast, und
      frischte sie dabei auf. Das Fossil fuetterte sich selbst.

      REGEL: Wenn ein behobener Fehler "immer noch" auftritt, zuerst die
      DATEN auf Rueckstaende der alten Fassung pruefen (Zeitstempel gegen
      den Handlungsablauf halten), bevor man den Fix anzweifelt. Und beim
      Aufraeumen: Sicherung IM SELBEN Befehlsblock (resume.json.vor-aufraeumen-HHMM).
  - kind: falle
    id: spotify-konto-rest-wandert
    title: "Spotifys pausierter Zustand ist KONTOWEIT - er wandert ueber Profilwechsel"
    tags: [box, spotify, profile, anzeige, 2026]
    source: "16.08.2026, drei Fehlermeldungen bis zur Rueckfrage 'wo genau?'"
    body: |
      Nach einem Profilwechsel zeigte der Mini-Player dem NEUEN Kind das
      pausierte Lied des vorigen. Dreimal gejagt, zweimal an der falschen
      Stelle (Hoerstaende, Stopp-Reihenfolge) - die Daten waren laengst
      sauber, GEMESSEN mit einem Waechter-Lauf ueber alle resume-Dateien
      waehrend eines vollen Wechsels: kein Fremd-Schreiber.

      DIE QUELLE: Spotifys Schnittstelle meldet nach der Pause noch lange
      "<Titel>, is_playing=false" - fuer das KONTO, nicht fuer ein Profil.
      jetztLaeuft nimmt diesen Zweig, sobald der lokale leer ist (und der
      ist nach dem Wechsel-Stopp immer leer). Deshalb wanderten NUR
      Spotify-Lieder, nie ARD: mpv hat keinen Konto-Zustand.

      DER FIX: eine Frische-Marke (sessionStorage, einen Seitenaufbau lang).
      Solange sie gilt, zeigt jetztLaeuft PAUSIERTE Reste aus keinem Zweig;
      sie erlischt, sobald irgendetwas wirklich laeuft.

      DIE LEHRE FUER DIE JAGD: "welcher Dienst ist betroffen, welcher nie?"
      war der Schluessel (nur Spotify -> Konto-Zustand), und die Rueckfrage
      "WO erscheint es - Kachel, Mini-Player, hoerbar?" haette zwei
      Fehlversuche gespart, wenn sie frueher gestellt worden waere.
  - kind: falle
    id: sonde-in-tmp-stirbt-mit
    title: "Eine Sonde in /tmp stirbt mit dem Ereignis, das sie messen soll"
    tags: [box, netzabriss, messen, werkzeug, 2026]
    source: "16.08.2026 - der erste Abriss MIT Sonde endete im Neustart, /tmp war leer"
    body: |
      Die Netzabriss-Sonde lief aus /tmp und schrieb nach /tmp. Der erste
      Abriss, den sie erleben durfte, endete in einem NEUSTART - und /tmp
      samt Protokoll war weg. Ausgerechnet der Moment, fuer den sie gebaut
      war, ist verloren.

      REGEL: Eine Messung, deren Ereignis den Rechner mitreissen kann
      (Absturz, Neustart, Stromlos), gehoert mit Skript UND Protokoll auf
      bestaendigen Speicher plus Autostart - sonst misst sie alles ausser
      dem Ernstfall. Jetzt: /home/dietpi/netzabriss/sonde.py als
      systemd-Dienst (netzabriss-sonde.service), Protokoll daneben,
      selbst gedeckelt. Quelle im Repo: tools/box/netzabriss-sonde.py.

      STAND DER ABRISS-JAGD (16.08.2026): Hauptspur ist die
      WLAN/BT-KOEXISTENZ auf dem Kombichip (BCM4345). Neue Datenpunkte:
      der Betreiber hoert AUSSCHLIESSLICH ueber Bluetooth ("ich habe nur
      bluetooth"), unmittelbar vor dem Abriss stotterte BT extrem, und
      "immer auf Akku" passt (Akku = mobil = BT-Box). Die Box haengt im
      5-GHz-Netz (5180 MHz, -58 dBm) - die Sonde soll zeigen, ob sie im
      Stottermoment auf 2,4 GHz faellt. Powersave war es nachweislich
      nicht (frueher gemessen, zudem "power save disabled" im Boot-Log).

      NEBENBEFUND: die Pi-5-Uhr startet ohne RTC-Batterie bei einer
      Phantasiezeit und springt erst mit NTP. dmesg --time-format iso
      rechnet rueckwirkend um, journalctl-Zeilen vor dem Netz nicht -
      wer Zeitreihen vergleicht, prueft erst, welche Uhr galt.

      NACHTRAG (selber Tag): Betreiber fragte "verwende ich nicht den
      bt500" - auf der Box stecken ZWEI Controller: hci0 = 88:A2:9E
      (eingebauter Kombichip, DEFAULT) und hci1 = 08:BF:B8 (BT500,
      Realtek). Der Standard ist der EINGEBAUTE - der Ton laeuft also
      vermutlich ueber den Chip, der sich den Funk mit dem WLAN teilt,
      obwohl der Stick daneben steckt. Die Sonde protokolliert seither
      je Takt, an welchem hci Verbindungen haengen, dazu Throttle-Bits
      und Vbat/Ibat. Der BT500 ist damit der AUSWEG: Ton ueber hci1
      (oder eingebautes BT ganz abschalten, dtoverlay=disable-bt)
      trennt BT vom WLAN-Chip.

      ZWEITER NACHTRAG (selber Tag, und er widerlegt den ersten): die
      Kopplungs-Ablage /var/lib/bluetooth zeigt den Lautsprecher UNTER DEM
      BT500 (08:BF:B8) gepaart, der eingebaute Controller traegt KEINE
      Kopplung. Der Ton laeuft also doch ueber den Stick - der Betreiber
      hatte recht ("verwende ich nicht den bt500"). bluetoothctl [default]
      sagt dazu NICHTS aus: default war der eingebaute, gepaart war am
      Stick. Wer wissen will, wo Audio laeuft, liest die KOPPLUNGEN
      (/var/lib/bluetooth/<controller>/<geraet>), nicht die default-Marke.

      Damit ist die Koexistenz-These weitgehend GEFALLEN (BT auf eigenem
      USB-Chip, WLAN auf 5 GHz). USB-Autosuspend am Stick ebenfalls raus
      (power/control=on). Verbleibende Verdaechtige fuer Stottern+Abriss:
        1. STROM auf Akku (Brownout: erst BT-Stottern, dann WLAN, dann
           der beobachtete NEUSTART - ein Treiberabsturz startet keine
           Box neu, ein Spannungseinbruch schon). Die Sonde loggt
           throttled/Vbat/Ibat jetzt je Takt.
        2. 2,4-GHz-Umfeld der BT-Strecke selbst (Nachbarn, Entfernung).
      Naechster Schritt: das naechste Stottern einfach geschehen lassen
      und die Sonde lesen - sie ueberlebt jetzt auch einen Neustart.

      DRITTER NACHTRAG - DIE SONDE HAT GELIEFERT (13:45 Uhr, ~10 s Loch
      bei gutem Signal): wpa_supplicant meldet
          CTRL-EVENT-DISCONNECTED reason=3 locally_generated=1
          ignore list count incremented to 2, ignoring for 10 seconds
      reason=3 + locally_generated=1 heisst: DIE BOX SELBST trennt, nicht
      der Router (kein Mesh-Steering). Sekunden davor: sudo wpa_cli status
      aus den hauseigenen Skripten, und um 13:44:37 wurde wpa_supplicant
      NEU INITIALISIERT - 20 min nach dem Boot. Hauptverdaechtige sind
      damit die MuPiBox-NETZWERKSKRIPTE (check_network/get_network ueber
      mupi_check_internet.service): sie pruefen im 20-s-Takt und flappen
      das Netz offenbar selbst, bis wpa_supplicant den Router auf die
      Ignorierliste setzt. Das erklaert Stottern (Spotify-Puffer leert
      sich je Flap), lange Abrisse (Ignorierliste + weiterflappen) und
      dass ein Neustart "hilft" (frischer Skriptzustand). NAECHSTER
      SCHRITT: check_network.sh/get_network.sh lesen - WAS loest ifdown/
      reassociate aus, und warum urteilt es bei gesundem Netz "kaputt"?
  - kind: referenz
    id: was-die-sicherung-nicht-traegt
    title: "Was die Box-Sicherung traegt - und die drei Luecken, die bleiben"
    tags: [sicherung, box, systemd, 2026]
    source: "Am 16.08.2026 gemessen, nachdem der Betreiber nach der Wiederherstellbarkeit fragte"
    body: |
      /api/sicherung legt vor JEDER Auslieferung von selbst einen Stand an
      (10 lagen vor). Was drin ist: `server/config` (Bibliothek, Profile,
      Hoerstaende, Verlauf, Kinderzeit, Auswahl) und `etc/mupibox`. Das
      deckt alles ab, was ein Kind je eingestellt oder gehoert hat.

      DREI LUECKEN, und die erste hat mich selbst erwischt:

      1. /etc/systemd/system IST NICHT DABEI. Wer einen Dienst direkt auf
         der Box anlegt, hat ihn nirgends gesichert - die Programme liegen
         im Repo, aber der EINE Eintrag, der sie startet, verschwindet mit
         der Karte. Am 16.08. genau so passiert: drei Units (Taster,
         Knopflicht, Sonde) existierten nur auf der Box. Jetzt unter
         scripts/systemd/ samt einrichten.sh.
      2. KEIN KARTENABBILD. Musik, Bilder, Betriebssystem sind nicht darin;
         nach einem Kartentausch muss neu aufgesetzt werden. Die Sicherung
         bringt danach den INHALT zurueck, nicht die Box.
      3. SIE LIEGT IM DOWNLOAD-ORDNER des Arbeitsrechners. Geht der kaputt
         oder wird geleert, ist der Stand weg (sagt die Oberflaeche selbst).

      PRUEFFRAGE nach jeder Arbeit AUF der Box (nicht im Repo): "Wenn die
      Karte jetzt stirbt - steht das, was ich gerade getan habe, irgendwo
      sonst?" Fuer Dateien unter /etc/systemd, /boot/firmware und
      /usr/lib/systemd lautet die Antwort per Vorgabe NEIN.
  - kind: falle
    id: probe-die-den-ernstfall-ausloest
    title: "Eine Probe, die den Ernstfall ausloest, ist keine Probe"
    tags: [gpio, box, messen, 2026]
    source: "16.08.2026 - die Trockenprobe des Ausschaltknopfs schaltete die laufende Box hart aus"
    body: |
      Fuer Phase 2 des Ausschaltknopfs (Strom-Schnitt nach dem Halt) war eine
      TROCKENPROBE eingebaut: sie sollte pruefen, ob GPIO4 und GPIO27 frei
      sind, ohne etwas zu schalten. Sie rief dafuer

          lgpio.gpio_claim_output(h, pin, 0)   # belegen
          lgpio.gpio_free(h, pin)              # gleich wieder freigeben

      und hat die laufende Box hart ausgeschaltet. NULL auf dem poweroffPin
      IST das Abschaltsignal (active_low) - das BELEGEN schaltet bereits.
      Ergebnis: harter Schnitt im Betrieb, EXT4 musste beim naechsten Start
      das Journal abspielen und drei orphan inodes aufraeumen. Also genau
      der Schaden, den der ganze Umbau abschaffen sollte.

      REGEL: Bei einer Probe an einem Ausgang zuerst fragen, was das BELEGEN
      allein schon tut - nicht nur, was das spaetere Setzen taete. Wer nur
      wissen will, ob eine Leitung frei ist, meldet sie als EINGANG an
      (`gpio_claim_input`): das belegt sie genauso und treibt nichts.

      ZWEITENS: die Antwort stand schon da. `/sys/kernel/debug/gpio` zeigte
      GPIO4 und GPIO27 auf gpiochip0 OHNE Besitzer - die Probe brachte gar
      keine neue Auskunft. Eine Messung, deren Ergebnis man bereits hat,
      ist reines Risiko.

      DRITTENS, zum Ausschaltknopf selbst: der Unfall hat bewiesen, was
      Phase 2 wissen wollte - GPIO4 auf Null nimmt der Platine wirklich den
      Strom. Teuer bezahlt, aber gemessen.

  - id: pipewire-abgriff-je-quelle
    kind: falle
    title: "Eine einzelne Tonquelle abgreifen: --target <id> luegt, PIPEWIRE_NODE auch, pw-link trifft (NACHTRAG: --target <object.serial> ist trotzdem PFLICHT)"
    match: "pipewire einzelne quelle aufnehmen|pw-record target funktioniert nicht|nur eine anwendung mitschneiden|librespot abgreifen|monitor hoert alles|piper ansage im mitschnitt|stream output audio aufnehmen"
    sev: high
    tags: ["ton", "pipewire", "mitschnitt", "e28", "librespot", "messen", 2026]
    source: >
      16.08.2026, tools/box/mitschnitt-machbar.py auf der Box .57 an laufender
      Musik gemessen. Vier Laeufe, davon zwei mit falschem Ergebnis (siehe
      messinstrument-gegen-bekanntes-signal). Gebaut in plugins/mixpi-mitschnitt.
    symptom: >
      Ein Mitschnitt soll NUR eine Anwendung hoeren - auf dieser Box nur
      Spotify, nicht die Piper-Ansagen ("Noch fuenf Minuten"), die ueber
      dieselbe Senke sprechen. Der naheliegende Weg ist der Monitor der
      Standardsenke; er nimmt alles auf, was zu hoeren ist, und schreibt die
      Ansage mitten ins Lied.

      Der Ausweg ist da: eine abspielende Anwendung ist in PipeWire ein
      eigener Knoten der Klasse Stream/Output/Audio (hier
      `alsa_playback.librespot`), mit eigenen Ausgangsports. Nur laesst er
      sich nicht so abgreifen, wie es in jeder Anleitung steht.
    cause: >
      DREI WEGE, DIE MAN ZUERST PROBIERT, UND ALLE DREI GEHEN STILL DANEBEN:

      1. `pw-record --target <id>` nimmt die Knotennummer eines
         Stream/Output/Audio widerspruchslos an - und verlinkt trotzdem auf
         den Monitor der Standardsenke. KEINE Fehlermeldung, kein Hinweis.
         Die Aufnahme laeuft, sie enthaelt nur das Falsche.
      2. `PIPEWIRE_NODE=<id> pw-record …` verhaelt sich genauso.
      3. Zwei gleichzeitige Aufnahmen heissen BEIDE `pw-record:input_FL`.
         Danach ist nicht mehr zu sagen, welcher Port zu welcher Datei
         gehoert, und ein `pw-link` haengt womoeglich beide an dieselbe
         Quelle. Erkennbar daran, dass zwei angeblich verschiedene Wege
         exakt denselben Messwert liefern - das war hier der erste Hinweis.

      Gemessen (Fremdton bei 3000 Hz, der sich wie eine Ansage verhaelt, gegen
      eine Kontrolle bei 3300 Hz): am Senken-Monitor steigt er auf das
      2178-fache der Kontrolle, am Quellknoten auf das 2,8-fache - also gar
      nicht. Die Trennung ist vollstaendig.
    fix: >
      Es geht nur mit ausdruecklichem `pw-link`, und in dieser Reihenfolge:

      NACHTRAG 23.08.2026 — SCHRITT 1 IST UNVOLLSTAENDIG, und die Luecke hat
      Aufnahmen gekostet: `--target` ist hier nicht ueberfluessig, sondern
      PFLICHT. Ohne ihn sucht sich `pw-record` die Standardquelle SELBST, und
      das ist der Lautsprecher-Monitor; laeuft die Verkabelung aus Schritt 3/4
      auch nur einmal daneben, enthaelt die Datei den ganzen Raum statt einer
      Anwendung. Richtig ist `--target <object.serial>` (Seriennummer, nicht
      Id) ZUSAETZLICH zum `pw-link`. Was oben ueber `--target <id>` steht,
      bleibt wahr: die Id verlinkt nicht zuverlaessig. Siehe
      `mitschnitt-nahm-den-lautsprecher-auf`.

      1. `pw-record` mit EIGENEM Namen UND `--target <object.serial>` starten:
         `pw-record -P '{ node.name = "mein-mitschnitt" }' --target <serial> … datei.flac`
      2. Rund eine Sekunde warten, bis die Ports in der Graphik stehen.
      3. Die SELBSTGEWAEHLTE Verbindung loesen (`pw-link -l` lesen, jede
         Zeile `|<- …` unter dem eigenen input-Port mit `pw-link -d`
         abhaengen). Ohne diesen Schritt laeuft der Monitor MIT in dieselbe
         Datei, und die Ansagen sind wieder drin.
      4. `pw-link <quelle>:output_FL <mein-mitschnitt>:input_FL`, ebenso FR.
      5. GEGENPROBE: `pw-link -l` erneut lesen und nachsehen, woran es
         WIRKLICH haengt. Nicht darauf vertrauen, dass Schritt 4 gewirkt hat.

      ZWEI EIGENSCHAFTEN, DIE MAN EINPLANEN MUSS:

      Der Quellknoten existiert nur, WAEHREND gespielt wird. Hoert die
      Anwendung auf, ist er weg, und `pw-link` darauf sagt "No such file or
      directory". Das ist der Normalfall, kein Fehler.

      FLAC BRAUCHT KEIN FFMPEG. Auf dieser Box gibt es weder ffmpeg noch flac
      noch oggenc - `pw-record` schreibt ueber libsndfile echtes FLAC, wenn
      die Datei so heisst (Signatur `fLaC` geprueft). Faktor 7,8 gegenueber
      WAV, aus 660 MB/h werden 85. Beim Beenden SIGTERM, nicht SIGKILL: sonst
      bleibt eine 422-Byte-Datei mit Kopf und nichts dahinter.
    related: ["messinstrument-gegen-bekanntes-signal", "pkill-toetet-die-eigene-shell", "mitschnitt-nahm-den-lautsprecher-auf"]

  - id: messinstrument-gegen-bekanntes-signal
    kind: regel
    title: "Ein Messinstrument, das man nie an ein bekanntes Signal haelt, misst unbemerkt Unsinn"
    match: "messung falsch aber plausibel|goertzel dezimieren|filter misst falsche frequenz|kontrollfrequenz|pegelsprung beweist nichts|probe traegt nicht|messwerkzeug selbst pruefen"
    sev: high
    tags: ["messen", "werkzeug", "ton", "e28", "gegenprobe", 2026]
    source: >
      16.08.2026, tools/box/mitschnitt-machbar.py. Zwei von vier Messlaeufen
      lieferten ein falsches Ergebnis, das wie ein echtes aussah.
    symptom: >
      Ein Werkzeug sollte entscheiden, ob ein Testton in einer Aufnahme steckt.
      Es meldete zweimal hintereinander "der Ton ist in KEINER der beiden
      Aufnahmen" - eine in sich schluessige Auskunft, die zur Deutung einlud,
      die Box koenne den Ton nicht wiedergeben oder ein Filter daempfe ihn weg.
      Beides falsch. Der Ton war da; das Instrument sah ihn nicht.
    cause: >
      ZWEI FEHLER, BEIDE UNAUFFAELLIG:

      1. DER FILTER SASS AUF DER FALSCHEN FREQUENZ. Der Goertzel-Filter nahm
         aus Geschwindigkeitsgruenden jede zweite Probe (`range(0, n, 2)`),
         rechnete seinen Koeffizienten aber weiter mit der VOLLEN Abtastrate.
         Wer dezimiert, halbiert die effektive Rate und muss sie mitteilen -
         sonst misst der Filter die halbe Frequenz. Der Code sieht dabei
         voellig normal aus, und das Ergebnis ist eine Zahl, keine Fehlermeldung.

      2. EIN PEGELSPRUNG BEWEIST NICHTS, SOLANGE MUSIK LAEUFT. Nachdem der
         Filter richtig sass, meldete er einen Anstieg um das 11,5-fache in
         einer Aufnahme, die den Testton gar nicht enthielt - das war ein
         Beckenschlag. Ein Schwellwert allein haette daraus "der Ton ist drin"
         gemacht und die ganze Untersuchung ins Gegenteil verkehrt.
    fix: >
      ZWEI GEWOHNHEITEN, die beide Fehler gefangen haetten, bevor sie etwas
      kosteten:

      ERSTENS: Das Instrument gegen ein BEKANNTES Signal halten, bevor man
      ihm glaubt - und zwar im Werkzeug selbst, nicht im Kopf. Hier: der
      Filter muss an der selbst erzeugten Sinusdatei anschlagen (8000) und an
      Stille nicht (0). Schlaegt er dort nicht an, bricht das Werkzeug ab,
      statt eine Zahl zu liefern. Das sind zehn Zeilen und sie ersetzen jedes
      Nachdenken darueber, ob man dem Ergebnis trauen darf.

      ZWEITENS: Eine KONTROLLE mitmessen, die sich bei der gesuchten Wirkung
      NICHT aendern darf. Hier eine Nachbarfrequenz (3300 statt 3000 Hz).
      Gefragt wird dann nicht "ist der Wert gestiegen?", sondern "ist er
      staerker gestiegen als die Kontrolle?". Aus 11,5-fach gegen 4,1-fach
      wird "nur 2,8-fach deutlicher" - also nichts. Aus 8852-fach gegen
      4,1-fach wird "2178-fach deutlicher" - also eindeutig.

      Ein Werkzeug, das "nichts gefunden" meldet, hat zwei moegliche Gruende,
      und es muss sie unterscheiden koennen: es ist nichts da, oder es kann
      nichts sehen. Dieses sagt jetzt "die Probe selbst hat nicht getragen",
      wenn der Testton nicht einmal dort auftaucht, wo er auftauchen MUSS.
    related: ["pipewire-abgriff-je-quelle", "gegenprobe-statt-gruen-glauben", "einmal-hinsehen-ist-keine-messung"]

  - id: dienst-erbt-kein-xdg-runtime-dir
    kind: falle
    title: "Host is down: ein systemd-Dienst findet PipeWire nicht, von Hand geht alles"
    match: "pw_context_connect failed|Host is down pipewire|XDG_RUNTIME_DIR fehlt|pw-record aus dem dienst|pactl aus systemd|von hand geht es aus dem dienst nicht"
    sev: high
    tags: ["ton", "pipewire", "systemd", "umgebung", "e28", 2026]
    source: >
      16.08.2026, gemessen mit tools/box/pipewire-erreichbar.py auf der Box .57,
      beim Bau von plugins/mixpi-mitschnitt.
    symptom: >
      Ein Programm, das Ton aufnimmt oder umschaltet (`pw-record`, `pw-link`,
      `pactl`), laeuft ueber ssh von Hand tadellos. Aus dem Server heraus —
      also aus `mupibox-server.service` — scheitert derselbe Aufruf mit:

          pw_context_connect() failed: Host is down

      Die Meldung liest sich wie ein Ton- oder Netzproblem. Sie ist keines.
      Und der Unterschied faellt beim Probieren NIE auf, weil man beim
      Probieren immer angemeldet ist.
    cause: >
      PipeWire haelt seinen Socket unter `/run/user/<uid>/pipewire-0` und
      findet ihn ueber die Umgebungsvariable `XDG_RUNTIME_DIR`. Eine ANMELDUNG
      setzt sie (pam_systemd). Ein systemd-Dienst erbt sie NICHT — auch dann
      nicht, wenn er unter demselben Benutzer laeuft.

      Auf dieser Box laeuft `mupibox-server.service` als `dietpi`, und
      `systemctl show -p Environment` ist leer. In `/proc/<pid>/environ` des
      Servers fehlen `XDG_RUNTIME_DIR`, `PULSE_SERVER` und
      `PIPEWIRE_RUNTIME_DIR` allesamt.

      Gemessen, derselbe Aufruf zweimal:
        ohne die Variable                 0 Bytes, "Host is down"
        mit XDG_RUNTIME_DIR=/run/user/1000   573484 Bytes
    fix: >
      Die Variable beim Starten des Kindprozesses MITGEBEN, statt sie im Dienst
      zu erwarten:

          const uid = process.getuid?.() ?? 1000
          spawn('pw-record', argumente, {
            env: { ...process.env, XDG_RUNTIME_DIR: `/run/user/${uid}` },
          })

      ABLEITEN, NICHT EINTRAGEN: `/run/user/1000` ist heute richtig und beim
      naechsten Benutzer falsch. Ein bereits gesetztes `XDG_RUNTIME_DIR` hat
      Vorrang.

      Der andere Weg waere `Environment=XDG_RUNTIME_DIR=/run/user/1000` in der
      Unit. Er ist schlechter: er gilt dann fuer ALLES im Server, obwohl nur
      ein einzelnes Plugin Ton anfasst, und er steht an einer Stelle, die beim
      Lesen des Plugin-Quelltextes niemand sieht.

      ZUM SUCHEN: `tools/box/pipewire-erreichbar.py` probiert beide Faelle
      durch und sagt, welcher traegt — statt zu raten, ob es an Rechten, am
      Benutzer oder am Socket liegt.
    related: ["pipewire-abgriff-je-quelle", "einmal-hinsehen-ist-keine-messung"]

  - id: plugin-ereignis-darf-nicht-warten
    kind: falle
    title: "Ein Plugin-Ereignis hat 8 Sekunden - wer laenger arbeitet, wird abgeraeumt und laesst Kindprozesse zurueck"
    match: "ueber der frist wird abgebrochen|hat 8000 ms nicht geantwortet|plugin startet neu versuch|verwaister prozess plugin|worker abgeraeumt kindprozess laeuft weiter|plugin ereignis blockiert"
    sev: high
    tags: ["plugin", "wirt", "e28", "mitschnitt", "nebenlaeufigkeit", 2026]
    source: >
      16.08.2026, beim Bau von plugins/mixpi-mitschnitt. Im Journal des Servers
      zu sehen, die Ursache aber erst nach mehreren Fehlsuchen am Ton.
    symptom: >
      Ein Plugin verhaelt sich unerklaerlich: Zustand, den es sich gerade
      gemerkt hat, ist beim naechsten Aufruf weg. Beim Mitschnitt sah es so
      aus, als sei jedes Stueck nur eine Sekunde lang — und im Ablageordner lag
      eine Rohdatei, zu der es keinen laufenden Mitschnitt gab.

      Im Journal steht die Erklaerung, aber sie liest sich harmlos:

          <kennung> ueber der Frist — wird abgebrochen
          <kennung> bei "wiedergabeGestartet": hat 8000 ms nicht geantwortet
          <kennung> startet neu (Versuch 1 von 3)
    cause: >
      `plugin-wirt.ts` gibt jedem Aufruf FRIST_MS = 8000 ms. Wer laenger
      braucht, wird nicht etwa gewarnt: der WORKER WIRD ABGERAEUMT und neu
      gestartet.

      Der Handler wartete hier auf den PipeWire-Knoten von librespot, und der
      entsteht erst, wenn wirklich Ton fliesst — nach dem Ereignis, nicht
      davor. Also lief die Frist regelmaessig ab.

      DAS SCHLIMMERE IST DIE HAELFTE, DIE WEITERLAEUFT. Der Worker verschwindet,
      seine KINDPROZESSE nicht: `pw-record` schrieb munter weiter, hielt den
      Abgriff besetzt und fuellte eine Datei, die im neuen Worker niemand mehr
      kennt. Ein Plugin, das nur rechnet, verliert bei einem Abbruch nichts —
      eines, das Prozesse startet oder Dateien schreibt, hinterlaesst Muell.
    fix: >
      EIN EREIGNIS IST EINE MITTEILUNG, keine Arbeitsanweisung, auf deren
      Erledigung jemand wartet. Entgegennehmen, quittieren, und die Arbeit
      daneben laufen lassen:

          starten(e, kontext.protokoll).catch((f) => kontext.protokoll(...))
          // KEIN await

      Das `.catch` ist Pflicht: ohne es wird aus einem Fehler eine
      unbehandelte Zurueckweisung, die niemand sieht.

      ZWEITENS, und ebenso wichtig: BEIM START DIE RESTE RAEUMEN. Ein Plugin,
      das Prozesse startet, muss damit rechnen, dass es schon einmal
      abgeraeumt wurde. Vor dem eigenen Start also nachsehen, ob noch ein
      eigener Prozess laeuft, und ihn beenden — ueber `pgrep -x` und einen
      Abgleich der Befehlszeile in /proc, nie ueber `pkill -f` (das traefe die
      eigene Shell mit).

      Dieselbe Vorsorge heisst im Schwesterprojekt SpotifyRecorder
      `cleanup_stale_isolation` und ist dort aus demselben Grund entstanden.

      WORAN MAN ES ERKENNT: Einstellungen aendern laedt das Plugin ebenfalls
      neu ("Einstellungen geaendert, Plugin startet neu"). Wer waehrend eines
      laufenden Vorgangs am Schalter dreht, loest denselben Fall aus.
    related: ["pipewire-abgriff-je-quelle", "dienst-erbt-kein-xdg-runtime-dir", "pkill-toetet-die-eigene-shell"]

  - id: dieselbe-zuordnung-dreimal-im-code
    kind: falle
    title: "Dieselbe Zuordnung stand dreimal im Code — der dritte Fall hiess dann www-admin"
    match: "ausliefern zwischenlager fehlt|plugins ausliefern geht nicht|www if n == www else www-admin|dritter baum|zuordnung kopiert dreimal|paketname"
    sev: med
    tags: ["ausliefern", "werkzeug", "plugin", "wartbarkeit", 2026]
    source: >
      16.08.2026, beim Ausliefern von plugins/mixpi-mitschnitt.
      tools/ausliefern.py und scripts/box/mupibox-tauscher.py.
    symptom: >
      Ein neues Ausliefer-Ziel (ein dritter Verzeichnisbaum neben `www` und
      `admin`) liess sich nicht ausliefern. Der Tauscher meldete am Geraet:

          plugins: Zwischenlager fehlt
          plugins: im Zwischenlager steht None, gebaut wurde 3ab241ce1223

      Beides klang nach einem Uebertragungsfehler. Es war keiner — hochgeladen
      wurde tadellos, nur eben der falsche Ordner.
    cause: >
      Die Zuordnung „welcher Baum heisst im Paket wie?" stand DREIMAL im Code,
      jedes Mal als derselbe Ausdruck:

          "www" if n == "www" else "www-admin"

      Solange es genau zwei Baeume gab, war das an allen drei Stellen richtig.
      Beim dritten wurde der Plugin-Ordner als `www-admin` eingepackt — und am
      Ziel lag nichts. Nur EINE der drei Stellen hatte einen Rueckfall
      (`Path(ziel).name`), die beiden anderen nicht; deshalb passte auch die
      Fehlermeldung nicht zur Ursache.

      DER ZWEITE RIEGEL lag im Tauscher auf der Box: er verlangte von JEDEM
      Baum eine `index.html`. Auch das war richtig, solange beide Baeume
      Oberflaechen waren. Ein Plugin-Ordner hat nie eine.
    fix: >
      Die Zuordnung an EINER Stelle ableiten, nicht an dreien wiederholen:

          def paketname(name):
              if name == "www": return "www"
              if name == "admin": return "www-admin"
              return Path(ZIELE[name]["ziel"]).name

      Und die Erwartung an den Inhalt vom AUFTRAG bestimmen lassen, nicht vom
      Werkzeug raten: der Auftrag traegt jetzt `muss_enthalten: "index.html"`
      fuer die beiden Oberflaechen und nichts fuer alles andere. Wo nichts
      steht, traegt der Hash-Vergleich die Last — und der ist ohnehin die
      schaerfere Probe, denn er vergleicht Byte fuer Byte mit dem
      Arbeitsrechner.

      WORAN MAN DIE SORTE FEHLER ERKENNT: eine Sonderbehandlung, die als
      `if x == A else B` geschrieben ist, hat kein Zuhause fuer C. Sie faellt
      erst auf, wenn C kommt — und dann als Meldung, die nach etwas anderem
      klingt. Wer den zweiten Fall kopiert, legt den dritten schon hin.
    related: ["pipewire-abgriff-je-quelle", "plugin-ereignis-darf-nicht-warten"]

  - id: bluetoothctl-verlaufsdatei-frisst-die-karte
    kind: falle
    title: "bluetoothctl schreibt bei JEDEM Aufruf seine Verlaufsdatei neu — ein Gigabyte je Stunde"
    match: "hohe schreiblast sd-karte|leerlauf schreibt gigabyte|bluetoothctl_history|write_bytes node hoch|verschleiss sd karte messen|wer schreibt auf die karte"
    sev: high
    tags: ["bluetooth", "sd-karte", "verschleiss", "messen", "werkzeug", 2026]
    source: >
      16.08.2026, an der Box .57 gemessen mit tools/box/schreiblast-messen.py
      und tools/box/schreiber-finden.py. Behoben im Commit
      "Ein Gigabyte je Stunde fuer eine Datei, die niemand liest".
    symptom: >
      Die Box schreibt im LEERLAUF ueber 1 GB je Stunde auf die SD-Karte —
      ohne Mitschnitt, ohne Wiedergabe, ohne dass jemand etwas bedient. Auf
      einem Geraet, dessen Kartenlebensdauer an geschriebenen Bytes haengt,
      ist das der teuerste Posten ueberhaupt.

      Nichts daran sieht falsch aus: das Journal belegt nur 36 MB, Chromium
      schreibt 36 MB/h, und in `server/config` aendert sich minutenlang
      nichts.
    cause: >
      `bluetoothctl` benutzt readline und schreibt bei JEDEM Aufruf die Datei
      `$HOME/.cache/.bluetoothctl_history` — KOMPLETT NEU, nicht angehaengt.

      Die Oberflaeche fragt den Bluetooth-Zustand etwa alle drei Sekunden ab;
      der Server startet also rund 1000 Mal je Stunde ein `bluetoothctl`. Die
      Verlaufsdatei war auf 1.022.953 Byte und 67.678 Zeilen gewachsen:

          1080 Aufrufe/h x 1 MB = 1080 MB/h
          am Serverprozess gemessen: 1063 MB/h

      Ein TEUFELSKREIS: je oefter aufgerufen, desto laenger die Liste, desto
      mehr Schreiblast. Und die Last erscheint unter dem AUFRUFENDEN Prozess
      (hier node), nicht unter bluetoothctl — wer im eigenen Code sucht,
      findet nichts.
    fix: >
      Beim Aufruf HOME auf ein tmpfs zeigen lassen:

          execFile('bluetoothctl', args, {
            env: { ...process.env, HOME: process.env.XDG_RUNTIME_DIR || `/run/user/${uid}` },
          })

      Dann landet die Verlaufsdatei im Arbeitsspeicher — oder gar nicht, weil
      dort kein `.cache` existiert. `bluetoothctl` sucht in HOME KEINE
      Konfiguration; Kopplungen liegen unter /var/lib/bluetooth, Einstellungen
      unter /etc/bluetooth. Die bestehende Datei einmal loeschen.

      Gemessen: 1114 MB/h vorher, 67 MB/h nachher.

      ══ WIE MAN SO ETWAS FINDET ══════════════════════════════════════════

      1. Je Prozess messen: `write_bytes` aus /proc/<pid>/io, zweimal mit
         Abstand. NICHT `wchar` — das zaehlt auch, was nie auf der Platte
         landet (tmpfs, Cache), und genau der Unterschied ist hier der Punkt.
      2. Sagt ein Prozess "ich war es", aber im eigenen Code steht keine
         passende Schreibstelle: nach KINDPROZESSEN suchen. Im strace sind
         sie an /etc/passwd, /etc/group, ld.so.cache und libc.so.6 zu
         erkennen, die im Dutzend geoeffnet werden — jeder neue Prozess liest
         sie.
      3. Fremde Programme schreiben Verlaufs-, Cache- und Sperrdateien in
         HOME. Wer sie hundertfach je Stunde aufruft, sollte wissen, wohin.
    related: ["dienst-erbt-kein-xdg-runtime-dir", "einmal-hinsehen-ist-keine-messung"]

  - id: dienst-steckt-im-schluessel
    kind: falle
    title: "Derselbe Titel aus zwei Quellen hat zwei Schluessel — und die Profil-Auswahl kennt nur einen"
    match: "kachel angelegt taucht nicht auf|library eintrag unsichtbar|auswahlFiltern filtert lokal|medienSchluessel dienst|verschmelzung rettet nicht|eintrag in data.json aber nicht api/data"
    sev: high
    tags: ["medien", "profil", "verschmelzung", "e41", "mitschnitt", 2026]
    source: >
      19.08.2026, E41. Offline belegt mit einem Durchlauf gegen die echten
      Funktionen, am Geraet bestaetigt mit einem dritten Album (ALBUM_AB=2).
    symptom: >
      Ein neuer library-Eintrag wird angelegt und vom Server mit Schluessel
      bestaetigt (POST /api/medien -> 200, ok:true). Er steht in data.json und
      in active_data.json — aber /api/data liefert ihn nicht, und auf der Box
      erscheint keine Kachel. Kein Fehler in irgendeinem Journal.

      Verwirrend dazu: an einem anderen Tag ist derselbe Eintrag ploetzlich DA,
      ohne dass jemand etwas geaendert haette.
    cause: >
      /api/data filtert mit der MEDIENAUSWAHL des aktiven Profils
      (auswahlFiltern), und `medienSchluessel` traegt den DIENST im
      Schluessel:

          spotify-Eintrag:  spotify:7zkg…
          Mitschnitt:       lokal:t:kuenstler|titel

      Dasselbe Album, zwei Schluessel. In der Auswahl des Kindes steht nur der
      Spotify-Schluessel — der lokale faellt heraus. Die VERSCHMELZUNG rettet
      ihn nicht: auswahlFiltern laeuft VOR ihr (server.ts, erst 8298, dann
      8308).

      Das "mal da, mal weg" erklaert sich ueber das AKTIVE PROFIL: ein Profil
      ohne Auswahl sieht alles (leer heisst alles, hatAuswahl), eines mit
      Auswahl nur ihre Schluessel. Wer nach einem Neustart als Papa misst,
      sieht anderes als tags zuvor als Kind.
    fix: >
      Wer programmgesteuert einen Eintrag anlegt, der ein GEGENSTUECK eines
      vorhandenen ist (Mitschnitt zum Stream-Album), traegt seinen Schluessel
      in die Auswahl des aktiven Profils nach — ueber den vorhandenen Endpunkt
      POST /api/profil/auswahl/werk (denselben wie der Quick-Select). Das
      zeigt nichts Neues: das Profil hoert dieses Album gerade, es steht
      ohnehin in seiner Auswahl, nur unter der anderen Kennung. Ohne Auswahl
      meldet der Endpunkt `unveraendert` und ruehrt nichts an.

      Das aktive Profil kommt aus GET /api/profile, Feld `aktiv` —
      /api/profil/aktiv gibt es nur als POST, zum Setzen.

      BEIM PRUEFEN: erst die drei Schichten trennen (data.json,
      active_data.json, /api/data) — das unterscheidet "wird nicht
      geschrieben" von "wird beim Lesen gefiltert". Und einen Test mit einem
      FRISCHEN Album fahren: bei bekannten verwirft `keep`-Logik die neue
      Aufnahme korrekt, die Kette laeuft nie, und der Test prueft nichts.

      OFFEN als Entwurfsfrage: auswahlFiltern koennte verschmolzene
      Geschwister mitnehmen (erst verschmelzen, dann filtern). Das waere die
      Antwort auf das Muster statt auf den Fall — aber ein Eingriff in die
      Kette, die Kinder-Mediatheken bestimmt.
    related: ["pipewire-abgriff-je-quelle", "gegenprobe-statt-gruen-glauben"]

  - id: soloist-erste-messung
    kind: wissen
    title: "Spotify Soloist auf der Box: Knoten heisst spotify, Schluessel ist spak_, getInfo liegt auf /zc"
    match: "soloist installieren|soloist api key|soloist pipewire knoten|soloist nicht sichtbar spotify app|soloist login failed|soloist websocket ctl"
    sev: med
    tags: ["soloist", "spotify", "e42", "pipewire", "mitschnitt", 2026]
    source: >
      19.08.2026, E42/S1 an der Box .57 gemessen. Probe unter
      /home/dietpi/soloist-probe, lief NEBEN librespot.
    symptom: >
      Wer Soloist (Spotifys offiziellen Headless-Client) auf der Box
      einrichtet, stolpert nacheinander ueber drei Dinge: der vorhandene
      spotify.clientId wird abgewiesen ("login failed: make sure --api-key is
      valid"); das Geraet erscheint nicht in der Handy-App; und wer den
      PipeWire-Knoten unter "soloist" sucht, findet nichts.
    cause: >
      1. Der --api-key ist ein EIGENER, kontogebundener Schluessel (spak_…)
         von der Dashboard-Seite "Spotify Soloist API Key" — nicht die
         Client-ID einer App.
      2. Die Handy-App CACHT die Geraeteliste: erst nach echtem Beenden und
         Neustarten der App erschien "MixPiBox-Probe". Der Connect-Endpunkt
         antwortet auf Pfad /zc (nicht /) — wer /?action=getInfo prueft,
         haelt den Dienst faelschlich fuer kaputt.
      3. Der PipeWire-Knoten heisst schlicht `spotify` (app "Spotify"),
         F32LE 44100 Hz — der Lossless-Pfad. Er BLEIBT in der Pause stehen,
         anders als librespots Knoten, der bei jedem Stopp verschwindet.
    fix: >
      Merkzettel: spak_-Schluessel im Dashboard erzeugen (mit dem Konto, das
      sich verbinden soll) und als spotify.soloistApiKey ablegen. Suchwort
      fuer den Recorder-Abgriff: `spotify`. Pruefen mit
      `curl http://<box>:<port>/zc?action=getInfo` und
      `./soloist ctl now --json -D <datadir>` (ctl braucht den Datenordner,
      sonst "cannot determine websocket port"). Der Build meldet seinen
      90-Tage-Verfall selbst ("client expires in N days") — der Updater kann
      ihn abfragen statt Dateidaten zu schaetzen. Soloist verlinkt sich am
      Entzerrer vorbei direkt auf die interne Senke; fuer die Klangkette
      --pipewire-device benutzen. Kosten: 0,8 % CPU, 58 MB.
    related: ["pipewire-abgriff-je-quelle", "dienst-erbt-kein-xdg-runtime-dir"]

  - id: systemd-frisst-escapte-anfuehrungszeichen
    kind: falle
    title: "ExecCondition mit bash -c und escapten Anfuehrungszeichen: systemd entwertet sie, und die Unit startet nie wieder"
    match: "ExecCondition jq|systemd escape anfuehrungszeichen|condition unmet jq compile error|is not defined at top-level systemd|unit startet nicht nach drop-in|exec-condition skipped"
    sev: high
    tags: ["systemd", "shell", "e42", "unit", 2026]
    source: >
      19.08.2026, E42/S2. Das Drop-In engine.conf legte librespot still;
      im Journal stand der jq-Fehler im Klartext.
    symptom: >
      Eine Unit mit frisch gelegtem Drop-In startet nicht mehr: "Skipped due
      to exec-condition", Condition "unmet". Im Journal davor eine
      jq-Meldung wie "soloist/0 is not defined at <top-level>" — jq hat den
      VERGLEICHSWERT als Funktionsaufruf gelesen.
    cause: >
      Die Zeile war nach Shell-Gewohnheit geschrieben:

          ExecCondition=/bin/bash -c '/usr/bin/jq -e "(.x // \\"a\\") != \\"b\\"" datei'

      systemd verarbeitet Escape-Sequenzen in Unit-Dateien SELBST: aus \\"
      wird ein blankes ". Die Bash bekommt dadurch verschachtelte doppelte
      Anfuehrungszeichen, die sich gegenseitig beenden — jq erhaelt a und b
      UNQUOTIERT und bricht mit Compile-Fehler ab. Exit != 0 heisst bei
      ExecCondition: Unit wird uebersprungen. Auf dieser Box hiess das: die
      TONMASCHINE der Familie startete nicht mehr, wegen zweier
      Anfuehrungszeichen.
    fix: >
      Das Programm DIREKT als ExecCondition aufrufen, ohne bash -c — systemds
      eigene einfache Anfuehrungszeichen schuetzen die inneren doppelten:

          ExecCondition=/usr/bin/jq -e '(.x // "a") != "b"' datei

      Allgemein: wer in einer Unit-Zeile \\" schreibt, hat fast immer eine
      Schicht zu viel. Erst pruefen, ob der Befehl ohne Shell auskommt; die
      Shell nur fuer Pipes/Schleifen, und dann die inneren Werte ohne
      doppelte Anfuehrungszeichen formulieren. GEGENPROBE nach jedem
      Unit-Wechsel: systemctl restart + is-active + Journal — eine
      uebersprungene Condition macht KEINEN failed-Zustand und faellt sonst
      niemandem auf.
    related: ["backticks-beenden-jede-vorlage", "pkill-toetet-die-eigene-shell"]

  - id: normalisieren-ersetzt-nicht
    kind: falle
    title: "Wer beim Normalisieren neu baut statt zu ergaenzen, wirft die Felder weg, an denen alles haengt"
    match: "map wirft felder weg|alle kacheln gleiches bild|klick spielt immer erste|dritte listenbauart|normalisierung verliert bild befehl|spread statt neubau"
    sev: high
    tags: ["oberflaeche", "ard", "e43", "muster", 2026]
    source: >
      19.08.2026, E43. NewDesign/app.js albumTitelOeffnen; bewiesen am Geraet
      (6 Folgen, 6 Bilder; mpv spielte nach dem Fix exakt die UUID von Folge 3).
    symptom: >
      In EINER Ansicht zeigen alle Eintraege dasselbe Bild, und jeder Klick
      startet den ersten — waehrend Server-Daten, Schwesteransichten und die
      Wiedergabe selbst korrekt sind ("obwohl es sehr wohl unterschiedlich
      ist wenn ich es abspiele"). Reparaturen aendern nichts, weil sie in der
      FALSCHEN Bauart derselben Liste sitzen.
    cause: >
      Eine Normalisierung, die das Objekt NEU BAUT statt zu ergaenzen:

          stuecke = (d.titel || []).map((t, i) => ({
            nr: Number(t.nr) || i + 1,
            titel: String(t.titel || ''),
            dauerMs: ...,
          }))

      `bild`, `befehl` und `id` fielen stumm weg. Ohne `bild` griff der
      Rueckfall aufs Album-Cover (alle gleich); ohne `befehl` blieb nur
      `abspielBefehl`, das fuer ARD ABSICHTLICH null liefert — der Rueckfall
      spielte von vorn. ZWEI Symptome, EINE Zeile.

      Der zweite Anteil: es war die DRITTE Bauart derselben Titelliste
      (albumTitelOeffnen neben titelListeBauen und den Alben-Lanes). Die
      Reparaturen vom 15.08. sassen alle in der ersten — die dritte hatte
      niemand auf der Liste. Erkennungszeichen im DOM: der Lane-Kopf sagte
      "60 Titel, vollstaendig", wo die ARD-Bauart "Folgen" sagt.
    fix: >
      Normalisiert wird OBENDRAUF, nicht STATTDESSEN:

          stuecke = (d.titel || []).map((t, i) => ({ ...t, nr: ..., titel: ..., dauerMs: ... }))

      Und die Sonderwege der Schwesterbauart woertlich uebernehmen: eigenes
      Bild zuerst (t.bild || album.bild), ARD-Folgen ueber die Folgenkennung
      starten (ardSendungAb, Liste als schonDa) — am fertigen ard/-Befehl
      erkannt, dem sichersten Zeugen.

      BEIM SUCHEN: wenn Server und eine Ansicht gesund sind und eine andere
      krank, die kranke im DOM ansprechen (outerHTML einer Kachel, Wortlaut
      des Kopfes) — der WORTLAUT verraet die Bauart, und damit die Datei-
      stelle, schneller als jede Code-Lektuere.
    related: ["gegenprobe-statt-gruen-glauben", "backticks-beenden-jede-vorlage"]

  - id: bedienung-wohnt-in-der-darstellung
    kind: pattern
    title: "Verhalten ist auch Darstellung: Bedien-Schalter reisen als darstellung.json-Felder"
    match: "verhaltens-feld|bedienung je profil|theme schaltet verhalten|mupi-bruecke|album-tipp spielt|vollbild blaettern|bedienart als thema"
    sev: medium
    tags: ["darstellung", "themes", "e44", "muster", 2026]
    source: >
      E44, 19.08.2026: die MuPi-Bruecke (Album-Tipp spielt sofort, grosse
      Cover-Reihen, Vollbild-Blaettern) — am Geraet bewiesen mit
      tools/bruecke-schau.mjs und tools/blaettern-schau.mjs.
    body: >
      Wer einen BEDIEN-Schalter je Profil, themefaehig und mit Live-Vorschau
      will, baut KEINE neue Ablage: darstellung.json kann das alles schon
      (je Profil im Bereich, box-weite THEMEN als geteilte Presets,
      mitgelieferte Themen in themen.ts, 3-Sekunden-Abgleich der Box-UI,
      Verwaltungs-Vorschau = die ECHTE Oberflaeche im Rahmen).

      Der Bauplan (E44, drei Felder in ~200 Zeilen):
        1. Feld in der Verwaltung (interface + STANDARD + Karte) — sie ist
           der Editor.
        2. anwenden() in NewDesign/app.js: Optik als CSS-Variable oder
           body-Klasse; VERHALTEN in ein Modul-Objekt (`bedienung`), das die
           Kachelbauer an der Tat-Stelle befragen — eine CSS-Variable kann
           kein Verhalten schalten.
        3. Wo mehrere Bauarten dieselbe Frage stellen (Album-Tipp!), EINE
           Entscheidungsfunktion (albumTippVerdrahten) — die E43-Lehre.
        4. Preset als viertes mitgeliefertes Thema in themen.ts — Themen
           sind TEILMENGEN, nur die Felder, die das Thema AUSMACHEN.
        5. `=== true` fuer jeden neuen Schalter: eine aeltere
           darstellung.json darf das Verhalten nicht aendern.

      Die KLASSISCHE Oberflaeche bekommt solche Felder ABSICHTLICH nicht
      (siehe darstellung-feld-faellt-still-heraus, Praezisierung 19.08.).
    related: ["darstellung-feld-faellt-still-heraus", "normalisieren-ersetzt-nicht", "dieselbe-zuordnung-dreimal-im-code"]

  - id: orakel-fuer-den-kacheltipp
    kind: falle
    title: "Woran man am UI erkennt, was ein Tipp TUT: nicht am aria, nicht an /player/state"
    match: "aria label kein verb|sprichdann beschriftet nicht|player state als orakel|klick spielt woran erkennen|kacheltipp messen|abspielbefehl mitschneiden"
    sev: medium
    tags: ["messwerkzeug", "headless", "e44", 2026]
    source: "E44-Messung 19.08.2026, tools/bruecke-schau.mjs — drei Anlaeufe bis zum tragfaehigen Orakel."
    symptom: >
      Ein Messwerkzeug soll pruefen, ob ein Kacheltipp SPIELT oder OEFFNET —
      und scheitert dreimal: die aria-Labels tragen kein Verb, /player-
      Anfragen kommen auch ohne jeden Tipp, und /api/spotify/laeuft bleibt
      bei ARD-Kacheln stumm.
    cause: >
      sprichDann() SPRICHT den Satz ("Titel zeigen"/"abspielen"), es
      BESCHRIFTET nicht — aria-label traegt nur den Namen. /player/state
      und /player/local sind der LESE-TAKT der Seite (laufen im Sekunden-
      rhythmus); Befehle gehen ueber /player/current/... . Und das Spotify-
      Orakel sieht nur Spotify — der Dienst steckt im Werk.
    fix: >
      Das tragfaehige Orakel ist die TAT im Netz-Mitschnitt (CDP
      Network.requestWillBeSent), gefiltert auf /player/current/... —
      dienstunabhaengig, und der Kiosk der Box ist ein ANDERER Browser,
      dessen Verkehr nicht hereinfunkt. Dazu das DOM als zweiter Zeuge
      (oeffnet sich .lane-tief?). Vorher/Nachher an DERSELBEN Kachel, und
      der AUS-Lauf gehoert dazu: er belegt, dass der Tipp OHNE das Feld
      eben NICHT spielt.
    related: ["messinstrument-gegen-bekanntes-signal", "gegenprobe-statt-gruen-glauben"]

  - id: headless-schirm-ist-kleiner-als-bestellt
    kind: falle
    title: "--window-size 800,480 gibt dem Inhalt nur 337 px — und scrollTo snappt nicht"
    match: "innerheight stimmt nicht headless|window-size viewport unterschied|100vh falsch im headless|scroll-snap testet nicht|scrollto rastet nicht|synthesizescrollgesture"
    sev: medium
    tags: ["messwerkzeug", "headless", "chromium", "e44", 2026]
    source: >
      E44 Block 2, 19.08.2026: die Blaetter-Kachel mass 147 px statt 290 —
      exakt innerHeight(337) - 190. Die Formel stimmte, der Schirm nicht.
    symptom: >
      Ein vh-abhaengiges Mass ist im Headless-Lauf zu klein, obwohl
      --window-size=800,480 gesetzt ist; und ein scroll-snap rastet im Test
      nicht ein, obwohl er unter dem Finger am Geraet einrastet.
    cause: >
      --window-size bemisst das FENSTER, nicht den Inhalt: innerHeight war
      337 statt 480 (gemessen 19.08.2026, headless=new). Und ein
      programmatisches element.scrollTo() zieht Chromium NICHT auf den
      Schnapp-Punkt nach — snap greift bei GESTEN, nicht bei Skript-Scroll.
    fix: >
      Beides gehoert in den Messaufbau, nicht in den Code:
      Emulation.setDeviceMetricsOverride {width:800, height:480} setzt das
      Viewport-Mass, auf das es ankommt; Input.synthesizeScrollGesture
      wischt echt und laesst den Snap greifen (Rast-Beweis: Kachelmitte
      liegt danach 0 px neben der Buehnenmitte). Muster in
      tools/blaettern-schau.mjs.
    related: ["orakel-fuer-den-kacheltipp", "xvfb-hat-keinen-fenstermanager"]

  - id: dienst-ist-zulieferer-nicht-besitzer
    kind: pattern
    title: "Eine Entitaet der Box gehoert der Box: die Dienst-Kennung darf nicht die Eintrittskarte sein"
    match: "meta interpret|interpret gehoert der box|ohne spotify-id oeffnen|dienst wegbricht entitaet weg|service tags anzeigen|komposition aus diensten"
    sev: high
    tags: ["architektur", "interpreten", "e45", "muster", 2026]
    source: >
      E45, 19.08.2026. Betreiber: „baue die interpreten so um das es meta
      interpreten der box sind nicht nur von Spotify … Falls Spotify wegbricht
      soll es immer noch interpeten geben können." Am Geraet bewiesen mit
      tools/meta-interpret-schau.mjs.
    body: >
      SYMPTOM DES ALTZUSTANDS: Eine Entitaet (hier der Interpret) hat eine
      Ansicht, die eine DIENST-KENNUNG verlangt. Wer keine hat, bekommt eine
      ANDERE Ansicht (bei uns: das Regal) — und damit ist die Entitaet in
      Wahrheit eine Funktion dieses Dienstes mit Beifang.

      DER UMBAU IN VIER SCHRITTEN:

        1. EIN PLATZHALTER FUER „KEINE KENNUNG" statt einer Weiche. Bei uns
           `-`: ein einzelnes Zeichen, das durch keine Regex fuer echte
           Kennungen rutscht. Die Route nimmt ihn, fragt den Dienst GAR NICHT
           und meldet KEINEN Ausfall — nicht gefragt ist nicht ausgefallen.
           (Wer hier `null` in einer Reihe meldet, laesst die Oberflaeche
           „ließ sich nicht laden" ueber einen Dienst sagen, den niemand
           angerufen hat.)
        2. DER NAME IST DIE IDENTITAET, nicht die Kennung — er reist als
           Query mit, und die Zuordnung Werk->Entitaet bleibt an EINER Stelle
           im Server.
        3. HERKUNFT ALS DATENFELD (`dienste: string[]`), im SERVER gezaehlt,
           an Kopf UND je Kachel. Nicht in der Oberflaeche rechnen: dort
           entstuende eine zweite Fassung der Zuordnung.
        4. RUECKFALL AUF DEN EIGENEN BESTAND: fehlt das Bild des Dienstes,
           nimmt der Kopf das Cover des ersten eigenen Werks. Ein grauer
           Kasten ist kein Gesicht.

      MITZUDENKEN: (a) ein gemeinsamer Platzhalter macht alle kennungslosen
      Entitaeten UNUNTERSCHEIDBAR — Wettlauf-Waechter und Ersatzfarben, die
      auf der Kennung beruhten, brauchen jetzt Laufnummer bzw. Schluessel
      ([[gemeinsamer-platzhalter-macht-ununterscheidbar]]); (b) die
      Kennungspruefung steht oft MEHRFACH (bei uns: Kachel-Weiche, Waechter
      der Seite, Route) — alle zugleich lockern, sonst widersprechen sie sich;
      (c) ein neuer Dienst (Deezer) braucht danach nur Zeichen + Farbe und
      eine Quelle im Werk.
    related: ["normalisieren-ersetzt-nicht", "dienst-steckt-im-schluessel", "client-frist-muss-groesser-sein"]

  - id: client-frist-muss-groesser-sein
    kind: falle
    title: "Gleiche Frist bei Client und Server heisst: die ehrliche Teilantwort sieht nie jemand"
    match: "timeout beide 8 sekunden|teilantwort kommt nicht an|abortsignal timeout server frist|ausfall teilseite unerreichbar|frist client server gleich"
    sev: high
    tags: ["netz", "oberflaeche", "e45", "falle", 2026]
    source: "E45-Bestandsreview, 19.08.2026 (app.js json() 8000 gegen INTERPRET_FRIST_MS 8000)."
    symptom: >
      Der Server ist sorgfaeltig gebaut: faellt ein fremder Dienst aus, bricht
      er nach seiner Frist ab und liefert eine EHRLICHE Teilantwort (das
      Lokale vollstaendig, den Ausfall benannt). Im Betrieb sieht das niemand
      — die Oberflaeche zeigt nur „antwortet gerade nicht" und verliert auch
      die Teile, die GAR KEIN Netz brauchen.
    cause: >
      Beide Fristen standen auf demselben Wert. Der Timer des Browsers laeuft
      aber ab dem ABSENDEN, der des Servers erst ab dem EMPFANG — und danach
      baut der Server seine Antwort noch. Die Server-Antwort kommt damit
      IMMER nach dem Abbruch des Browsers; der gebaute Teilausfall-Pfad ist
      unerreichbar.
    fix: >
      Die Client-Frist MUSS groesser sein als die Server-Frist plus Laufzeit
      plus Bauzeit (bei uns 14 s gegen 8 s). Faustregel: Server-Frist + 50 %
      Aufschlag, mindestens aber + 4 s. Gegenprobe: den fremden Dienst
      haengen lassen (nicht ablehnen — ein schnelles ECONNREFUSED verdeckt
      den Fall) und pruefen, ob die netzlose Haelfte der Seite ankommt.
    related: ["dienst-ist-zulieferer-nicht-besitzer"]

  - id: gemeinsamer-platzhalter-macht-ununterscheidbar
    kind: falle
    title: "Ein Platzhalter fuer viele: Waechter, die auf der Kennung beruhen, verwechseln danach die Laeufe"
    match: "stale waechter kennung|zwei schnelle tipps falsche seite|platzhalter id kollision|ersatzfarbe alle gleich|laufnummer statt kennung"
    sev: medium
    tags: ["oberflaeche", "wettlauf", "e45", "falle", 2026]
    source: "E45, 19.08.2026 — beim Einbau von `-` als Kennung fuer „gehoert der Box\" aufgefallen."
    symptom: >
      Nach dem Einfuehren eines gemeinsamen Platzhalters („-\", „unbekannt\",
      0) malt eine verspaetete Antwort in die Ansicht einer ANDEREN Entitaet;
      Ersatzfarben, die aus der Kennung gerechnet werden, sind ploetzlich
      ueberall gleich.
    cause: >
      Der Waechter gegen verspaetete Antworten verglich die KENNUNG
      (`if (offen.id !== kennung) return`). Solange jede Entitaet eine eigene
      hatte, war das eindeutig. Mit einem gemeinsamen Platzhalter teilen sich
      beliebig viele dieselbe — zwei schnelle Tipps genuegen.
    fix: >
      Was einen LAUF identifiziert, muss vom Inhalt unabhaengig sein: eine
      hochgezaehlte Laufnummer je Oeffnung, gegen die sowohl der Erfolgs- als
      auch der FEHLER-Pfad prueft (der catch wurde bei uns im selben Zug
      gefunden: er schrieb ungeprueft in die Kopfzeile). Fuer abgeleitete
      Optik (Farben, Buchstaben) den fachlichen Schluessel nehmen, nicht die
      Kennung — dann stimmt sie auch zwischen Uebersicht und Detailseite.
    related: ["dienst-ist-zulieferer-nicht-besitzer"]

  - id: cdp-antwort-faelschen-braucht-response-stage
    kind: falle
    title: "Fetch.enable ohne requestStage Response: der Eingriff faellt still aus"
    match: "fetch requestpaused kein body|getResponseBody schlaegt fehl|antwort faelschen headless|cdp fetch interception messung|ausfall simulieren browser"
    sev: medium
    tags: ["messwerkzeug", "headless", "cdp", "e45", 2026]
    source: "E45-Messung 19.08.2026, tools/meta-interpret-schau.mjs."
    symptom: >
      Ein Messlauf soll den Ausfall eines Dienstes simulieren, indem er eine
      Antwort im Flug veraendert — und misst danach exakt dasselbe wie ohne
      Eingriff. Es sieht aus, als haette der Umbau nicht gewirkt.
    cause: >
      `Fetch.enable` pausiert ohne `requestStage` in der ANFRAGE-Phase. Dort
      gibt es noch keinen Koerper; `Fetch.getResponseBody` scheitert, und ein
      stiller Rueckfall auf `continueRequest` reicht die echte Antwort durch.
    fix: >
      `patterns: [{ urlPattern: '…', requestStage: 'Response' }]`, dann
      getResponseBody -> aendern -> `Fetch.fulfillRequest`. Und den
      Fehlschlag LAUT machen (console.error), statt still weiterzureichen —
      sonst ist ein misslungener Eingriff von einem misslungenen Umbau nicht
      zu unterscheiden. So simuliert man einen Dienst-Ausfall, ohne die
      echte Box anzufassen.
    related: ["headless-schirm-ist-kleiner-als-bestellt", "orakel-fuer-den-kacheltipp"]

  - id: knapp-gemerkt-heisst-spaeter-blind
    kind: falle
    title: "Was beim Merken als 'braucht niemand' wegfaellt, fehlt dem Verbraucher, der spaeter kommt"
    match: "weiterhoeren falsches bild|folgenbild fehlt|sendungscover statt folge|knapp gemerkt feld fehlt|plakette richtig bild falsch"
    sev: medium
    tags: ["ard", "weiterhoeren", "e46", "falle", 2026]
    source: >
      E46, 19.08.2026. Betreiber: „beim weiterspielen ist beim mauszoom nicht
      das richtige bild." Am Geraet bewiesen (Kachel zeigt jetzt
      urn:ard:image:d3461a05..., das Bild von Folge 4).
    symptom: >
      Eine Kachel widerspricht SICH SELBST: ihre Beschriftung nennt die
      richtige Folge, ihr Bild zeigt die ganze Sendung. Die Schwester-Ansicht
      (Titelliste) zeigt dasselbe Ding korrekt — was den Verdacht auf die
      Kachel lenkt, wo er nicht hingehoert.
    cause: >
      Beim MERKEN wurde die Liste auf das Noetige eingedampft
      (`ardFolgenKnapp`: nur Kennung und Titel), mit dem ausdruecklichen
      Kommentar „Bild und Dauer werden hier von niemandem gebraucht: das Bild
      holt die Oberflaeche aus derselben /inhalt-Antwort".

      DER SATZ WAR RICHTIG UND IST ES NICHT MEHR. Er galt fuer den
      Verbraucher, der die Folgenliste WAEHREND des Zeichnens in der Hand hat
      (die Titelliste). Der zweite Verbraucher kommt Stunden spaeter und hat
      sie nicht — fuer ihn ist das weggelassene Feld nicht sparsam, sondern
      blind. Solche Kommentare altern still: sie beschreiben den Bestand zum
      Zeitpunkt des Schreibens, nicht die Regel.
    fix: >
      Das Feld die ganze Kette mitreisen lassen, wenn es OHNE zusaetzlichen
      Abruf zu haben ist (hier lag es in derselben Antwort wie Kennung und
      Titel). Beim MERKEN mitschreiben, nicht beim Lesen aufloesen: die
      Weiterhoeren-Reihe wird im Minutentakt geholt, eine Aufloesung je Zeile
      waere ein Netzgang je Zeile und Minute fuer eine Anzeige.

      ZWEI FELDER, NICHT EIN UEBERSCHRIEBENES, wenn die Quellen verschieden
      belastbar sind: das Folgenbild ist eine FREMDE Adresse (haengt am Netz),
      das Werkbild kommt ueber die Box (Coverspeicher). Der Fremd-Wert geht
      VOR, der eigene bleibt Rueckfall fuer „laedt nicht" — sonst ist die
      Kachel bei schlechtem Netz LEERER als vor der Reparatur, und das faellt
      erst Wochen spaeter auf.

      UND: Wer eine Stelle fuer ungueltig erklaert, muss das neue Feld
      MITRAEUMEN (bei uns `VON_VORN`) — sonst traegt eine Kachel, die von
      vorn anfaengt, das Gesicht der Folge, die nicht mehr gilt. Genau dieser
      Fehler war beim Folgentitel schon einmal da.
    related: ["normalisieren-ersetzt-nicht", "gegenprobe-statt-gruen-glauben"]

  - id: normalisieren-wirft-zeichenwoerter-weg
    kind: falle
    title: "Wer beim Schluesselbauen alle Satzzeichen wegwirft, wirft echte Namen zusammen"
    match: "die drei fragezeichen ausrufezeichen|zwei interpreten eine kachel|freischaltung loescht andere|normal wirft satzzeichen weg|gleicher schluessel zwei serien|interpretSchluessel kollision"
    sev: high
    tags: ["interpreten", "schluessel", "medien", "e46", "falle", 2026]
    source: >
      19.08.2026, E46 (Fund aus dem E45-Bestandsreview). Am Geraet
      192.168.178.57 vor und nach dem Ausliefern gemessen mit
      tools/interpret-kollision-am-geraet.mjs.
    symptom: >
      Zwei verschiedene Interpreten stehen als EINE runde Kachel da, mit der
      Werkzahl beider. Wer den einen freischaltet, loescht STILL die
      Freischaltung des anderen — kein Fehler, keine Meldung —, und die
      ueberlebende Kachel traegt danach den einen Namen und fuehrt auf die
      Spotify-Seite des anderen. Eine Ablehnung versteckt beide; auf der
      Interpretenseite stehen unter „In deiner Box" die Werke beider.
    cause: >
      Der Schluessel entstand mit `normal()` (medien.ts), und das ersetzt ALLES
      ausser a-z0-9 durch ein Leerzeichen:

          normal("Die drei ???")  ->  "die drei"
          normal("Die drei !!!")  ->  "die drei"

      Beides sind echte Kosmos-Kinderserien. Die Entdopplung je Schluessel
      (`gesehenFrei` in interpretenAblageAus) ist dabei RICHTIG — sie traf nur
      zwei verschiedene Namen.

      Zwei weitere Schaeden derselben Funktion, beim Nachmessen gefunden:
      (a) sie schreibt klein VOR NFKD, und Zierschrift hat keine Kleinform —
      `normal("𝓛𝓮𝓸𝓷𝓲𝓮♡")` ergibt "eonie", der Anfangsbuchstabe faellt weg;
      (b) sie kennt nur a-z — "Straße" wird zu "stra e", ein kyrillischer Name
      zur LEEREN Zeichenkette, und leer heisst in `werkAus()` „gar kein Regal".
    fix: >
      Ein EIGENER Schluessel fuer die Entitaet (`interpretSchluesselAus`,
      medien.ts), nicht `normal()` verbessern — aus `normal()` entsteht der
      Ersatz-WERKschluessel, und darauf steht die Medienauswahl jedes Kindes.

      DIE REGEL: Der Name zerfaellt in WOERTER (`\p{L}`/`\p{N}`) und
      ZEICHENWOERTER (alles andere, was kein blosses Trennzeichen ist); beide
      bleiben stehen, durch je ein Leerzeichen getrennt. Die TRENNZEICHEN sind
      eine kurze, aufgezaehlte Liste des Klebstoffs, der in echten Namen
      vorkommt: `- . , : ; _ & + / \ | @ ' " ( ) [ ] { }` und die
      Anfuehrungs-/Gedankenstrich-Varianten. NFKD ZUERST, dann kleinschreiben.
      Ein Schluessel OHNE einen einzigen Buchstaben bleibt leer ("???" allein
      ist der Platzhalter fuer „unbekannt", kein Name).

      DIE RICHTUNG DES ZWEIFELSFALLS ist der eigentliche Inhalt dieser Regel:
        (A) zwei Entitaeten unter EINEM Schluessel — stiller, nicht
            zurueckholbarer Schaden;
        (B) eine Entitaet unter ZWEI Schluesseln („Wham!" neben „Wham") —
            eine sichtbare Kachel zu viel, mit einem berichtigten Namen
            aufgeraeumt.
      Also im Zweifel das Zeichen STEHENLASSEN. Beim GRUPPIEREN gilt anderswo
      genau umgekehrt „grosszuegig" ([[regal-je-interpret-gegen-verschmelzen]])
      — der Unterschied ist, ob der Fehler ein Regal zu wenig kostet oder eine
      fremde Person im Regal des Kindes.
    related: ["schluesselwechsel-ohne-wanderung", "regal-je-interpret-gegen-verschmelzen", "interpreten-freischalten-ablage", "dienst-steckt-im-schluessel"]

  - id: schluesselwechsel-ohne-wanderung
    kind: pattern
    title: "Ein Schluessel, der ueberall neu gebildet wird, laesst sich aendern — aber das muss man nachsehen, nicht glauben"
    match: "schluessel aendern wanderung|migration ablage schluessel|key change bestandsdaten|schluessel abgeleitet oder abgelegt|wanderung messen vor ausrollen"
    sev: high
    tags: ["architektur", "schluessel", "wanderung", "e46", "muster", 2026]
    source: >
      19.08.2026, E46. Werkzeuge: tools/interpret-schluessel-wanderung.mjs
      (rechnend + lesend) und `--nachziehen-am-geraet` (Neustart am Geraet).
    body: >
      EIN SCHLUESSELWECHSEL IST SONST DER TEUERSTE EINGRIFF DIESES BAUMS. An
      `medienSchluessel` haengen Verlauf, Weiterhoeren und die Medienauswahl
      jedes Kindes; jede Zeile muesste umgeschrieben werden, und ein
      abgebrochener Lauf hinterliesse eine halb gewanderte Box.

      DER INTERPRETENSCHLUESSEL WAR BILLIG — aus einem Grund, den frueher
      jemand aufgeschrieben hat und der sich hier auszahlte:

        `Werk.interpretSchluessel`   entsteht bei JEDEM Abruf neu (werke.ts)
        `Freischaltung.schluessel`   wird beim Lesen NEU GEBILDET und der Datei
                                     ausdruecklich NICHT geglaubt
                                     (interpretenAblageAus, mit Begruendung)
        profile/<kind>/auswahl.json  steht auf WERKschluesseln, nicht auf
                                     Interpretenschluesseln (auswahl.ts)

      DIE FRAGE VOR JEDEM SCHLUESSELWECHSEL lautet deshalb nicht „wie wandere
      ich?", sondern: IST DIESER SCHLUESSEL ABGELEITET ODER ABGELEGT? Abgeleitet
      heisst, er wandert von selbst. Abgelegt heisst, jede Ablage einzeln.

      UND MAN GLAUBT ES NICHT, MAN SIEHT ES NACH. Das Werkzeug liest die
      Auswahldateien der Profile MIT und meldet jeden Schluessel ohne
      Dienstvorsatz — findet es einen, ist die ganze billige Rechnung
      widerlegt. Auf der Box: 15 Schluessel, alle mit Vorsatz.

      WAS TROTZDEM BLEIBT, ist der veraltete Schluessel IN der Datei. Die soll
      von Hand lesbar und aenderbar sein; wer dort „die drei" liest, waehrend
      das Werk „die drei ???" traegt, sucht den Fehler an der falschen Stelle.
      Dafuer eine reine Funktion (`interpretenAblageWandern`), die sagt, WAS
      sich verschiebt, und ein EINMALIGER Nachzieher beim Serverstart, der nur
      schreibt, wenn wirklich etwas dasteht — und jede Zeile ins Journal legt
      (`"die drei" -> "die drei ???"`). Eine Wanderung, die niemand nachlesen
      kann, ist von einem Datenverlust nicht zu unterscheiden.

      VIER BEFUNDE GEHOEREN IN SO EIN WERKZEUG, und jeder heisst etwas
      anderes: KOLLISION (zwei Namen teilten einen alten Schluessel — der
      Schaden), WANDERT (der Schluessel eines Namens aendert sich — zaehlen),
      GETRENNT (die Reparatur), VERSCHMOLZEN (zwei alte Schluessel werden
      einer — der EINZIGE Befund, der wehtun kann; jede Zeile einzeln ansehen).

      MESSEN HEISST ZWEIMAL FAHREN: derselbe Lauf vor und nach dem Ausliefern.
      Auf der Box .57 meldete er vorher dreimal NEIN und nachher dreimal JA.
      Ein Werkzeug, das nur den guten Zustand kennt, ist keine Gegenprobe.

      ZWEI MESSFALLEN AUF DEM WEG:
      (1) Frisch angelegte Probeeintraege sind fuer `/api/interpreten`
          UNSICHTBAR, weil es nach der MEDIENAUSWAHL des aktiven Profils
          filtert (E18). Der erste Lauf meldete „keine Kachel" — und das sah
          aus wie ein Befund ueber den Schluessel. Ueber
          POST /api/profil/auswahl/werk hinein, danach wieder heraus; meldet
          der Endpunkt `unveraendert`, hat das Profil gar keine Auswahl und
          sieht ohnehin alles. Siehe [[dienst-steckt-im-schluessel]].
      (2) Die Probekennungen muessen 22 Zeichen haben. `interpretTaugt`
          (werke.ts) vergibt einem Spotify-Eintrag mit kuerzerer Kennung GAR
          KEINEN Interpretenschluessel — die Messung waere aus dem falschen
          Grund gruen geworden.
    related: ["normalisieren-wirft-zeichenwoerter-weg", "dienst-steckt-im-schluessel", "gegenprobe-statt-gruen-glauben", "medienschluessel-kollision-gleiche-playlistid"]

  - id: ausliefern-meldet-getauscht-und-hat-vertauscht
    kind: falle
    title: "Nach dem Ausliefern die md5 am Geraet selbst nachsehen — die Erfolgsmeldung hat schon einmal gelogen"
    match: "ausliefern gelaufen aber alter stand|zurueck haelt den neuen bau|deploy wirkungslos server.js|schritt 11 identisch aber alt|md5 am geraet nachsehen"
    sev: high
    tags: ["ausliefern", "messwerkzeug", "e46", "falle", 2026]
    source: >
      19.08.2026, E46, Box 192.168.178.57, tools/ausliefern.py --nur server.
      URSACHE NICHT GEKLAERT — hier steht der Befund und der Umgang damit,
      nicht die Erklaerung.
    symptom: >
      `ausliefern.py` laeuft sauber durch, Schritt 11 meldet „am Geraet
      identisch mit dem Bau (<md5>)", die Standwache entwarnt regulaer — und
      die Box verhaelt sich unveraendert. Nachgesehen:

          server.js          = der ALTE Bau
          server.js.zurueck  = der NEUE Bau

      Also genau verkehrt herum. Beide Dateien mit `links=1`, also KEINE harte
      Verknuepfung, wie sie der Tauscher anlegt. Kein Rollback im Journal, kein
      Fehler in der Ausgabe. Der Rueckweg zeigte damit auf die neue Fassung —
      ein spaeteres `--zurueckdrehen` haette „zurueck" nach VORN gedreht.
    cause: >
      Offen. Der Tauscher legt fuer Dateien `os.link(ziel, zurueck)` an und
      `os.replace(neu, ziel)`; danach muesste `.zurueck` den ALTEN Inhalt
      halten und mit `ziel` zeitweise denselben Inode teilen. Beobachtet wurde
      das Gegenteil. Die Zeitstempel passen zum Tauschzeitpunkt (kein spaeterer
      Eingriff), das Journal zeigt nur Stellen und Entwarnen der Standwache.
    fix: >
      NACH JEDEM AUSLIEFERN DIE WIRKUNG SELBST MESSEN, und zwar am Geraet:

          ssh <box> md5sum <ziel> <ziel>.zurueck

      Die md5 des Baus steht in der Ausgabe von Schritt 3/11. Stimmt sie am
      Ziel nicht, hilft ein zweiter Lauf mit `--erzwingen`: er raeumt
      `.zurueck` weg und legt beides richtig an (nachgemessen: danach
      `server.js` = Bau-md5, `.zurueck` = alter Stand, der Rueckweg ist damit
      auch repariert).

      ALLGEMEINER: Eine Messung der WIRKUNG (verhaelt sich die Box anders?)
      haette es sofort gezeigt — die Messung des ZUSTANDS durch das Werkzeug,
      das gerade selbst getauscht hat, nicht. Wer ausliefert, um etwas zu
      beweisen, misst danach mit einem ANDEREN Werkzeug nach.
    related: ["gegenprobe-statt-gruen-glauben", "schluesselwechsel-ohne-wanderung"]

  - id: geerbte-bilder-sind-geerbte-risiken
    kind: falle
    title: "Aus dem Fork geerbte Themes und Bilder: in die DATEI sehen, nicht auf den Dateinamen"
    match: "themes rechtlich bedenklich|pikachu supermario spiderman|geschuetzte figuren im theme|pixabay deckt figur nicht|fork bilder uebernommen"
    sev: high
    tags: ["recht", "themes", "fork", "e47", 2026]
    source: >
      E47, 19.08.2026. Betreiber: „gerade die bilder waren ja auch rechtlich
      bedenklich." Sechs Themenpakete aus dem Ursprungsprojekt entfernt.
    symptom: >
      Ein geforktes Projekt bringt Themen/Assets mit, die niemand je
      angesehen hat. Die Dateinamen klingen harmlos („pikachu-bg.jpg"), die
      beiliegenden Readme nennen eine freie Quelle (Pixabay/Pexels) — und im
      Bild steht eine geschuetzte Figur.
    cause: >
      Eine freie BILDlizenz deckt das Bild, nicht das DARGESTELLTE. Ein
      KI-erzeugter oder abfotografierter Pikachu bleibt Pikachu; ein Foto von
      Sammelfiguren macht die Figuren nicht frei. Dazu kommen SCHRIFTEN, die
      Marken-Logotypen nachbilden (PokemonXandY, NewSuperMarioFontU,
      Starjedi) — und die Sammelfalle „free for personal use" von dafont:
      eine weitergegebene SD-Karte ist keine private Nutzung mehr.
    fix: >
      In die Dateien SEHEN, nicht in die Namen — und zwar bevor man
      weitergibt. Beim Entfernen gehen die NAMEN ueberall mit, nicht nur die
      Dateien: Auslieferungs-Vorlage, Nachtrag-Bloecke der Update-Skripte,
      Kopierzeilen und Ordner-Anlage BEIDER Installationswege (autosetup UND
      update — sie laufen gern auseinander). Sonst zeigt eine frische Box auf
      Themen, die es nicht mehr gibt.
      VORHER PRUEFEN, was passiert, wenn ein Geraet gerade auf einem
      entfernten Thema steht: hier war es gefahrlos, weil der Server eine
      fehlende Theme-Datei ausdruecklich ohne Fehler behandelt.
      GRENZFAELLE benennen statt wegzudiskutieren: ein Thema, dessen NAME
      eine Marke ist (matrix, ironman), aber dessen Bild generisch ist, ist
      billig zu ersetzen — also ersetzen.
    related: ["fork-erbt-auch-die-wege"]

  - id: fork-erbt-auch-die-wege
    kind: falle
    title: "Der Altbestand ist zur Laufzeit tot und im Installationsweg quicklebendig"
    match: "php tot aber autosetup installiert|altlast kommt auf frischer karte zurueck|zur laufzeit erledigt installationsweg nicht|www-data nopasswd|admin schalter vorgabe php"
    sev: high
    tags: ["installer", "sicherheit", "fork", "e47", 2026]
    source: >
      E47, 19.08.2026. Betreiber: „auch php und fallback kann weg … das
      backend hat sich bewaehrt." 86 Dateien, 8032 Zeilen ausgebaut.
    symptom: >
      Eine abgeloeste Technik ist auf der laufenden Box nachweislich weg
      (kein Paket, kein Dienst, kein Prozess, Port tot) — und trotzdem nicht
      erledigt. Der AUSLIEFERUNGSWEG holt sie auf jeder frischen Karte
      zurueck.
    cause: >
      Aufgeraeumt wird meist die Box, nicht das Rezept. Hier installierte
      autosetup.sh lighttpd+PHP BEDINGUNGSLOS und legte dabei
      `www-data ALL=(ALL:ALL) NOPASSWD: ALL` an — eine Rechteausweitung, die
      es nur gab, weil der alte Admin exec('sudo …') aufrief.
      ZWEITE FALLE: Ein Schalter, der die Altlast abwaehlbar macht, hat gern
      den ALTEN Wert als Vorgabe (`${MUPI_ADMIN:-php}`). Der Rezeptkopf setzt
      node — wer das Rezept ohne dieses env aufruft, bekommt php.
      DRITTE FALLE: es gibt zwei Wege (Erstinstallation UND Update), und der
      Kommentar im einen erwaehnt den anderen nicht.
    fix: >
      Erst am Geraet MESSEN, dass die Technik wirklich tot ist (Paket,
      Dienst, Prozess, Port, Verzeichnis). Dann BEIDE Wege ausbauen, die
      Rechte-Zugestaendnisse mitnehmen (sudoers, chown-Gruppen — pruefen, ob
      die Gruppe ueberhaupt noch etwas haelt: hier gehoerte die Konfig
      dietpi:www-data 644, und dietpi war gar nicht in der Gruppe) und den
      Schalter samt Vorgabewert entfernen, wenn es keinen zweiten Zweig mehr
      gibt.
      WAS BLEIBEN MUSS: der AUFRAEUM-Schritt fuer Altgeraete. Solange eine
      Box im Bestand noch mit der alten Technik laeuft, ist sein Wegfall kein
      Aufraeumen, sondern ein Vergessen.
    related: ["geerbte-bilder-sind-geerbte-risiken", "fallback-heisst-fuenf-dinge"]

  - id: fallback-heisst-fuenf-dinge
    kind: pattern
    title: "„Fallback kann weg“ — erst zaehlen, wie viele verschiedene Dinge so heissen"
    match: "fallback aufraeumen|catch-all route loeschen|rueckfall entfernen gefaehrlich|zurueck dateien nicht loeschen|stichwortsuche loeschen"
    sev: medium
    tags: ["aufraeumen", "e47", "muster", 2026]
    source: "E47, 19.08.2026 — Inventur vor dem Loeschen, vier Spuren."
    body: >
      Ein Aufraeum-Auftrag nennt oft ein WORT, keine Sache. „Fallback" traf
      in diesem Baum fuenf verschiedene Dinge, und nur EINES gehoerte zum
      Auftrag:

        (a) API-Rueckfall (Spotify-API -> Scraper) — lebt, bleibt
        (b) Oberflaeche bei stillem Backend („es bleibt beim zuletzt
            Gesehenen") — kein Rueckfall auf Aelteres, bleibt
        (c) catch-all-Routen des Servers und des Angular-Routers — reine
            Hygiene, bleiben
        (d) Dateien mit Rueckfall-Namen (`*.zurueck`) — das SICHERHEITSNETZ
            des Ausrollers. Wer nach dem Stichwort sucht und loescht, nimmt
            genau das weg, was einen misslungenen Ausrollvorgang rettet
        (e) Abspiel-Rueckfaelle (mpv ohne force-media-title bei aelterem mpv)
            — der Ton haengt daran

      VORGEHEN: die Lesarten zuerst AUFZAEHLEN und je Fundstelle zuordnen,
      dann fragen, welche der Betreiber gemeint hat. Beim Zweifel die Frage
      stellen, statt die haeufigste Lesart zu nehmen — ein geloeschter
      Rueckfall faellt erst auf, wenn genau der Fehler eintritt, gegen den er
      gebaut war.
    related: ["fork-erbt-auch-die-wege"]

  - id: drei-ausrollwege-keiner-kennt-den-anderen
    kind: falle
    title: "Die Box hat drei Ausrollwege — und ein Werkzeug, das nur zwei kennt, meldet falsche Luecken"
    match: "kein ausrollweg fasst an|soloist nicht ausgerollt|deckung meldet luecke die keine ist|drei wege installer|rezept kennt datei nicht"
    sev: high
    tags: ["installer", "werkzeug", "e48", "falle", 2026]
    source: >
      E48, 19.08.2026, DietPi-Inventur. Zwei Messspuren widersprachen sich
      ueber soloist; aufgeloest wurde es ueber einen dritten Weg, den keine
      von beiden durchsucht hatte.
    symptom: >
      Eine Datei liegt im Repo und laeuft nachweislich auf der Box — und das
      Deckungswerkzeug meldet trotzdem „kein Ausrollweg fasst das an". Oder
      umgekehrt: eine Datei fehlt auf einer frischen Karte, obwohl das
      BACKLOG behauptet, ein Rezeptschritt lege sie.
    cause: >
      Es gibt DREI Arten, wie etwas auf die Box kommt, und sie kennen
      einander nicht:

        1. autosetup/autosetup.sh und update/start_mupibox_update.sh —
           die Schalen-Wege, die eine SD-Karte BAUEN
        2. remote-step-installer/recipes/*.yaml — der Rezept-Weg (put: + run:)
        3. tools/ausliefern.py und scripts/systemd/einrichten.sh —
           die Wege, die eine LAUFENDE Box beliefern

      Wer nur eine Gruppe durchsucht, bekommt ein falsches Bild in BEIDE
      Richtungen. Dazu kommt: eine Box im Bestand kann mit dem einen Weg
      gebaut sein und mit dem anderen gepflegt werden — auf der Messbox war
      es der Installer, nicht autosetup (Node 26 statt 22, systemd statt pm2,
      13 Paketposten der autosetup-Liste gar nicht vorhanden).
    fix: >
      Beim Suchen ALLE DREI Gruppen greifen. Beim Bauen eines Werkzeugs die
      Gruppen aber TRENNEN: die Regel „angelegt ist nicht eingeschaltet" gilt
      nur fuer die BAU-Wege — ein Weg, der eine laufende Box beliefert, muss
      nichts enable-n, dort ist die Unit schon an. Der erste Anlauf warf alle
      fuenf Wege in einen Topf und meldete jede Unit dreifach; ein Werkzeug,
      das dreifach Laerm macht, wird beim vierten Mal nicht mehr gelesen —
      und dann meldet es die ECHTE Luecke an niemanden.

      UND: „im BACKLOG steht, der Schritt sei gebaut" ist kein Beleg. Bei
      soloist stand es seit E42 da, `git log -S` fand keine einzige Fassung.
      Geschrieben ist nicht gebaut.
    related: ["aufraeumen-nachmessen", "fork-erbt-auch-die-wege"]

  - id: kinderbox-horcht-nach-aussen
    kind: falle
    title: "Fernblick und Diagnoseseiten binden auf 0.0.0.0 — auf einer Kinderbox ist das die falsche Tuer"
    match: "x11vnc ohne passwort|vnc offen kinderbox|0.0.0.0 gebunden diagnose|flask host 0.0.0.0|offener port box"
    sev: high
    tags: ["sicherheit", "e48", "falle", 2026]
    source: >
      E48, 19.08.2026. Am Geraet gemessen: LISTEN 0 32 0.0.0.0:5900,
      ps: `x11vnc -ncache 10 -forever -display :0` — kein -rfbauth, kein
      ~/.vnc.
    symptom: >
      Ein Fernblick- oder Diagnosedienst horcht auf allen Netzkarten. Beim
      VNC heisst das: jeder im WLAN sieht den Bildschirm des Kindes UND
      bedient ihn mit — Tastatur und Maus inbegriffen.
    cause: >
      Zwei Gewohnheiten aus der Entwicklung, die auf ein Geraet wandern:
      `x11vnc` ohne `-localhost` (die Vorgabe ist offen) und Flasks
      `app.run(host="0.0.0.0")`. Beide fallen nicht auf, weil sie tun, was
      sie sollen — nur eben fuer alle.

      ERSCHWEREND: der VNC wurde nicht ueber seine Unit gestartet (die war
      disabled), sondern fest aus chromium-autostart.sh. Wer in
      `systemctl` nach ihm sucht, findet ihn nicht.
    fix: >
      An 127.0.0.1 binden und den Zugang ueber den SSH-Tunnel fuehren
      (`ssh -L 5900:localhost:5900 …`). Damit haengt er an demselben
      Schluessel, ueber den ohnehin ausgeliefert wird — eine Berechtigung
      statt zwei. Einen Schalter in der Konfiguration dazulegen, wer ihn gar
      nicht will.

      NICHT LOESCHEN, WO DER DIENST MEHR TUT ALS ANZEIGEN: bei mupihat.py ist
      `app.run` die Hauptschleife, an der auch der Schreiber von
      /tmp/mupihat.json haengt — und daran die Tiefentlade-Abschaltung. Dort
      ist Binden richtig und Streichen falsch.

      GEGENPROBE: `ss -tlnp` auf der Box. Betrieblich gebraucht werden 8200
      und 8443, SSH, der librespot-Port und 5005 (auf 127.0.0.1). Alles
      andere gehoert begruendet.
    related: ["fork-erbt-auch-die-wege"]

  - id: ramlog-macht-protokoll-zu-arbeitsspeicher
    kind: falle
    title: "Auf DietPi kostet jede Protokollzeile Arbeitsspeicher, nicht Karte"
    match: "journal frisst ram|dietpi-ramlog tmpfs|logging info zu viel|protokoll arbeitsspeicher box|168 zeilen je minute"
    sev: medium
    tags: ["dietpi", "speicher", "e48", 2026]
    source: >
      E48, 19.08.2026: mupihat.py schrieb 21 Registerzeilen alle 4 s = 168
      je Minute, 35,6 MB — bei 197 MB freiem Arbeitsspeicher.
    body: >
      dietpi-ramlog legt /var/log auf ein tmpfs (auf der Messbox 50 MB). Das
      schont die SD-Karte und dreht dafuer die Rechnung um: ein
      gespraechiger Dienst kostet keinen Plattenplatz, sondern RAM — auf
      einer 2-GB-Box die knappe Ware.

      WORAUF ZU ACHTEN IST:
        * Dienste, die einen ZUSTAND periodisch vollstaendig protokollieren
          (Registersaetze, JSON-Abzuege), gehoeren auf WARNING; die
          Fehlersuche holt man ueber eine Umgebungsvariable zurueck.
        * Timer mit kurzem Takt, die auch dann drucken, wenn nichts ansteht
          (gemessen: bootwache 15,3/min, bt-reconnect 17,1/min,
          network-info 37/min).
        * `sudo` in einer Schleife: jeder Aufruf kostet Prozess, PAM-Sitzung
          und drei Journalzeilen. Gemessen 19 wpa_cli-Aufrufe je Minute aus
          dem Server heraus.

      Und: der Betriebszustand, den jemand WIRKLICH liest, steht meist
      woanders (hier /tmp/mupihat.json). Das Protokoll ist dann reine
      Beruhigung — mit Preis.
    related: ["kinderbox-horcht-nach-aussen"]

  - id: webkit-zieht-den-reglergriff-nicht
    kind: falle
    title: "Unter WPE/Cog laesst sich kein <input type=range> ZIEHEN — Tippen geht"
    match: "regler laesst sich nicht ziehen|slider drag geht nicht wpe|progress bar kaputt|helligkeit regler reagiert nicht|springen im titel geht nicht cog"
    sev: high
    tags: ["kiosk", "cog", "wpe", "webkit", "touch", "oberflaeche", "e55", 2026]
    source: >
      E55, 20.08.2026, am Geraet gemessen: Cog auf DRM zeigte die Oberflaeche
      sauber und nahm Tipp-Bedienung an (ein Hoerspiel liess sich starten),
      aber kein Regler liess sich ziehen. Betroffen waren alle drei der Box —
      Helligkeit, Lautstaerke und das Springen im Titel.
    body: >
      IN WEBKIT BEWEGT DIE EINGEBAUTE MAUS-BEHANDLUNG DEN GRIFF, und die dafuer
      noetigen Ereignisse entstehen bei einer Beruehrung erst beim TIPPEN, nicht
      waehrend eines Zuges. Blink (Chromium) bringt fuer den Griff eine eigene
      Beruehrungs-Behandlung mit und braucht das nicht. Wer von Chromium auf
      WPE wechselt, verliert also genau die Bedienelemente, die man ZIEHT.

      SCROLLEN IST NICHT BETROFFEN: das macht die Zeichen-Maschine selbst,
      ohne Umweg ueber Ereignisse. Im Versuch liess sich die Oberflaeche also
      wischen, aber nicht regeln — was die Suche zuerst in die falsche
      Richtung schickt, weil „Touch geht ja".

      DIE LOESUNG IST EINE BAUART FUER BEIDE BROWSER, nicht zwei: den
      eingebauten Weg mit preventDefault abschalten und den Wert selbst aus der
      Zeigerstelle rechnen (NewDesign/app.js, „Ziehen am Schieberegler"). Zwei
      Wege, die sich je nach Browser abwechseln, ergeben Fehler, die nur auf
      einem der beiden Geraete auftreten. EINE Delegation am document statt
      eines Aufrufs je Regler — sonst hat der vierte Regler es wieder nicht.

      ZWEI FALLEN AUF DEM WEG DORTHIN, beide gemessen:
        * GRIFFBREITE: fuer ein Pseudo-Element ohne eigene Stilregel gibt
          Chromium den Stil des ELEMENTS zurueck. Gemessen 400 px Griff auf
          400 px Regler — die begehbare Bahn (Breite minus Griff) wurde 0, und
          jede Zeigerstelle ergab denselben Wert. Der Regler stand still, OHNE
          dass irgendetwas warf: keine Meldung, kein Ereignis. Eine Breite
          ueber der halben Bahn ist unglaubwuerdig; dann ist die Elementhoehe
          der ehrlichere Wert.
        * DAS ENDE DES ZUGES: ein Bedienelement, das seinen Zug bei
          pointerdown BEGINNT und nur bei change BEENDET, braucht ein change
          auch ohne Wertaenderung. Sonst laesst ein Tipp auf die Stelle, wo
          der Regler schon steht, einen offenen Zug zurueck — und die Anzeige
          zeigt von da an fuer immer das alte Ziel. Gemeldet als „im mini
          player ist die progress bar kaputt"; sie war eingefroren, nicht
          kaputt.
    related: ["cog-drm-kiosk-statt-chromium", "drei-orte-eine-anzeige"]

  - id: cog-drm-kiosk-statt-chromium
    kind: howto
    title: "Kiosk ohne X-Server: Cog/WPE auf DRM spart 180 MB — was dafuer fehlen muss"
    match: "Couldn't open libGLESv2.so.2|failed to schedule a page flip: Permission denied|Renderer 'modeset' does not support rotation|The renderer process crashed|cog platform drm"
    sev: medium
    tags: ["kiosk", "cog", "wpe", "drm", "speicher", "chromium", "e54", "e55", 2026]
    source: >
      E54/E55, 20.08.2026, an der Box gemessen (Pi 5, 800x480 ueber DSI-2,
      drm-rp1-dsidrmf): Chromium + Xorg 511 MB PSS ueber 8 Prozesse plus
      X-Server; Cog auf DRM mit GLES-Renderer 331 MB ueber 6 Prozesse, kein
      X-Server, 0 Abstuerze.
    body: >
      DREI HUERDEN STEHEN VOR DER ERSTEN ZAHL, und jede erzeugt eine Messung,
      die nach Ergebnis AUSSIEHT und keines ist:

        1  „Couldn't open libGLESv2.so.2" — das Paket libgles2 fehlte (18 kB).
           libegl1 und libegl-mesa0 waren da, der GLES-Lader nicht. Folge: der
           Zeichen-Prozess stuerzte in einer Schleife ab („The renderer process
           crashed"), und die Speicherzahl der Leiche war natuerlich niedrig.
        2  Der Renderer wird mit `-O renderer=gles` gesetzt, NICHT ueber eine
           Umgebungsvariable: COG_PLATFORM_DRM_RENDERER gibt es nicht (im
           Modul stehen nur COG_PLATFORM_DRM_CURSOR, _MODE_MAX, _VIDEO_MODE,
           COG_PLATFORM_NAME, COG_PLATFORM_PARAMS). Eine erfundene Variable
           faellt still durch — die Warnung „Renderer 'modeset' does not
           support rotation 0" ist der Hinweis, dass weiter der alte lief.
        3  „failed to schedule a page flip: Permission denied" heisst: den
           DRM-Master haelt jemand anderes. Meist die eigene Vorgaenger-
           Instanz, die `pkill -x cog` nicht vollstaendig getroffen hat (die
           WPEWebProcess-/WPENetworkProcess-Kinder ueberleben). Das Bild wird
           dann zwar gezeichnet, aber nie umgeschaltet: auf dem Schirm stehen
           STREIFEN.

      WAS NICHT VON SELBST MITKOMMT: das Ziehen an Schiebereglern — siehe
      [[webkit-zieht-den-reglergriff-nicht]]. Touch als solches funktioniert
      (Cog haelt das Eingabegeraet ueber libinput offen, geprueft mit
      `ls -l /proc/<pid>/fd`), und Scrollen ebenso.

      SICHERHEITSNETZ BEIM VERSUCHEN: Wer den Kiosk fuer einen Test anhaelt,
      setzt vorher `systemd-run --on-active=<n> systemctl restart getty@tty1`.
      Ohne das haengt eine Box ohne Tastatur an einem schwarzen Schirm, sobald
      der Versuch schiefgeht.

      NOCH NICHT GEMESSEN: Cage (Wayland-Kiosk-Compositor, Paket vorhanden,
      zieht libwlroots-0.18 und xwayland nach) mit cogplatform-wl. Der Weg ist
      interessant, WEIL ein Compositor Beruehrungen als Zeiger weiterreicht —
      genau das, was Cog unter reinem DRM fehlt. Zahlen dazu gibt es hier
      ausdruecklich noch keine.
    related: ["webkit-zieht-den-reglergriff-nicht", "ramlog-macht-protokoll-zu-arbeitsspeicher"]

  - id: webkit-button-flex-streckt-nicht
    kind: falle
    title: "Flex-Spalte in einem <button>: WebKit macht die Kinder inhaltsbreit"
    match: "titel schreibt bis in die knoepfe|titel laeuft in die knoepfe|balken 0 breit|progressbar fehlt mini player|figur klebt am anfang|meerjungfrau haengt am anfang"
    sev: medium
    tags: ["kiosk", "cog", "wpe", "webkit", "css", "flexbox", "oberflaeche", "e55", 2026]
    source: >
      E55, 20.08.2026, per Geometrie-Sonde in Cog auf der Box gemessen
      (getBoundingClientRect alle 2,5 s an einen oertlichen Mithoerer auf
      127.0.0.1:8123): .mp-titel 294 px in einem 263-px-Vater, .mp-leiste
      63 px, .mp-balken 0 px. Nach dem Fix: 263 / 263 / 200.
    body: >
      DER MINI-PLAYER-TEXT IST EIN <button> mit display:flex und
      flex-direction:column. WebKit gibt Knoepfen aus der eigenen Vorgabe eine
      mittige Ausrichtung mit; die Kinder der Spalte werden dann INHALTSBREIT
      statt gestreckt. Drei Symptome aus EINEM Bruch:
        * der Titel (white-space: nowrap) wird so breit wie sein Text und ragt
          ueber den Vater in die Knoepfe hinein,
        * die Fortschrittsleiste schrumpft auf ihre Zeitanzeige,
        * der Balken darin (flex: 1) bekommt 0 px — die Fuellung steht dann
          bei "83 % von null", und die gewaehlte Fortschritts-Figur klebt
          sichtbar links, weil ihr ::after als einziges herausragt.
      Chromium streckt an derselben Stelle stillschweigend, deshalb faellt es
      dort nie auf. Wer eine Oberflaeche auf BEIDEN Engines betreibt, schreibt
      die Absicht hin: align-items: stretch am Container UND align-self:
      stretch an den Kindern — der Griff an den Kindern haelt auch dann, wenn
      eine Knopf-Vorgabe das align-items am Container ueberstimmt.

      ZUR FEHLERSUCHE hat sich die SONDE bewaehrt, weil Cog kein CDP spricht:
      ein kleines Skript in der Seite meldet Zaehler und Geometrie per fetch
      (mode no-cors) an einen python3 -m http.server auf 127.0.0.1 — dessen
      Zugriffsprotokoll IST die Messreihe. Damit war in einer Messung klar,
      dass WPE Pointer-Ereignisse fuer Touch liefert (typ=touch) und wie der
      Player wirklich layoutet wird.
    related: ["webkit-zieht-den-reglergriff-nicht", "cog-drm-kiosk-statt-chromium"]

  - id: gb-feld-mit-byte-wert-befuellt
    kind: falle
    title: "Soloist startet nicht: im GB-Feld maxcachesize steht ein Byte-Wert"
    match: "--cache-size must be 0 (no limit) or >= 100|meldet sich an haengt|soloist restart schleife|soloist meldet sich nur an"
    sev: high
    tags: ["soloist", "spotify", "konfig", "einheiten", "e55", 2026]
    source: >
      E55, 20.08.2026, an der Box: soloist.service drehte mit Neustart-Zaehler
      10+ in der Schleife, die Oberflaeche stand dauerhaft auf "meldet sich
      an". Im Journal stand die Ursache woertlich: "Error: --cache-size must
      be 0 (no limit) or >= 100".
    body: >
      spotify.maxcachesize IST IN GB GEMEINT - alle Leser deuten es so:
      librespot-start.sh haengt "G" an, soloist-start.sh rechnet mal 1000 in
      MB. In der Konfig stand aber 1073741824 - 1 GB in BYTES. Daraus wurden
      1,07 Billionen MB, Soloist lehnte ab, systemd startete alle 12 s neu.

      DIE QUELLE WAR NICHT DIE BOX, SONDERN DIE VORLAGE: das Template
      config/templates/mupiboxconfig.json trug den Byte-Wert, und
      update/conf_update.sh schrieb ihn als VORGABE sogar aktiv nach (als
      Zeichenkette obendrein). Eine Korrektur nur am Geraet waere beim
      naechsten Update wieder ueberschrieben worden - wer so einen Wert
      findet, sucht die Vorlage und den Updater dazu.

      DREI GRIFFE, ALLE DREI NOETIG:
        * Vorlage und conf_update auf GB (Vorgabe 1), samt Migration fuer
          Bestandsboxen: eine Zahl ueber 64 ist als GB unglaubwuerdig -> 1.
        * soloist-start.sh nimmt nur glaubwuerdige GB an (keine Zahl oder
          ueber 64 -> Vorgabe 2). Der Dienst darf an einem Konfig-Feld nicht
          in der Neustart-Schleife enden.
        * Der Blick ins JOURNAL DES DIENSTES statt auf die Anzeige: "meldet
          sich an" hiess hier nicht "Anmeldung laeuft", sondern "der Prozess
          stirbt vor der Anmeldung".
    related: ["cog-drm-kiosk-statt-chromium"]

  - id: kiosk-browser-umschaltbar-mit-rueckfall
    kind: howto
    title: "Kiosk-Browser umschaltbar machen — und warum es ohne Rueckfall verboten waere"
    match: "kiosk browser umstellen|cog statt chromium einstellen|kioskBrowser|chromium-autostart umbenennen|mixpi-kiosk-pakete"
    sev: medium
    tags: ["kiosk", "cog", "chromium", "konfig", "installation", "e56", 2026]
    source: >
      E56, 20.08.2026, an der Box gebaut und geprueft: Umschalten ueber den
      echten Startweg (getty@tty1 neu) ergab 5 Cog-Prozesse, 0 Chromium,
      0 Xorg, 1174 MB frei. Rueckfall durch Sabotage nachgewiesen (cog
      beiseitegelegt): 0 Cog, 8 Chromium, Xorg an, Box bedienbar.
    body: >
      DER SCHALTER IST mupibox.kioskBrowser ("chromium" | "cog"), gelesen in
      chromium-autostart.sh mit '// "chromium"'. Er steht im Eltern-Bereich
      neben der Oberflaechen-Wahl, weil beide dasselbe beantworten: WAS der
      Kiosk zeigt und WOMIT.

      DER RUECKFALL IST DIE BEDINGUNG, NICHT DIE KUER. Diese Box hat keine
      Tastatur. Startet Cog nicht — fehlendes Paket, fremde Hardware —, dann
      steht ein Kind vor einem schwarzen Schirm und niemand kann etwas
      eintippen. Das Kioskskript gibt Cog deshalb fuenf Sekunden (gemessen:
      "Loaded successfully" nach 0,4 s, ein Fehlstart endet sofort) und
      startet sonst Chromium. Ohne diese Zusicherung duerfte man den Schalter
      gar nicht anbieten.

      DIE PAKETE HOLT DIE BOX SELBST: mixpi-kiosk-pakete.sh prueft mit
      dpkg-query, was fehlt (cog UND libgles2 — das Paket cog zieht den
      GLES-Lader nicht mit, siehe [[cog-drm-kiosk-statt-chromium]]), und holt
      nur das. Sie laeuft an ZWEI Stellen mit derselben Datei: beim
      Umstellen (Server, ohne zu warten) und beim Kioskstart. Doppelt gerufen
      kostet nichts. Ohne Netz bricht sie sofort ab, statt vor dem ersten
      Bild in apt-Zeitgrenzen zu laufen.

      DIE DATEI HEISST WEITER chromium-autostart.sh, obwohl sie zwei Browser
      startet. Das ist FREMDBESTIMMT und keine Nachlaessigkeit: DietPi ruft
      sie fest auf (/boot/dietpi/dietpi-login Zeile 82: exec
      /var/lib/dietpi/dietpi-software/installed/chromium-autostart.sh) und
      legt sie in dietpi-software sogar selbst an. Wer auf mixpi-kiosk.sh
      umbenennt, hat beim naechsten Start eine Box ohne Bild UND ohne
      Tastatur, um es zu bemerken. Neues traegt den MixPi-Namen; dieser eine
      nicht.
    related: ["cog-drm-kiosk-statt-chromium", "webkit-zieht-den-reglergriff-nicht", "drei-ausrollwege-keiner-kennt-den-anderen"]

  - id: messwerkzeug-muss-sich-selbst-pruefen
    kind: falle
    title: "Ein Messwerkzeug, das seine eigene Kette nicht prueft, liefert Zahlen ohne Bedeutung"
    match: "nicht gemessen|benchmark liefert striche|sonde meldet nicht|zeit bis zum bild falsch|pgrep findet sich selbst"
    sev: high
    tags: ["messen", "werkzeug", "benchmark", "e57", 2026]
    source: >
      E57, 20.08.2026: tools/kiosk-benchmark.py brauchte vier Laeufe, bis die
      Zahlen stimmten. Die Speicherwerte waren von Anfang an richtig (sie
      kommen aus ps und smaps_rollup); alles, was durch die Sonde lief, war
      still kaputt.
    body: >
      VIER FEHLER, JEDER FUER SICH UNAUFFAELLIG:

        1  `pkill -f 'http.server 8124'` traf die EIGENE Befehlszeile — das
           Muster stand darin. Die Shell erschoss sich, bevor sie den
           Mithoerer startete. Danach lauschte niemand.
           (`pkill -f` trifft immer auch die Zeile, die es selbst enthaelt —
           beim Wrapper wie beim Muster. `fuser -k <port>/tcp` trifft nur,
           wer den Port wirklich haelt.)
        2  `python3 -m http.server` schreibt sein Zugriffsprotokoll GEPUFFERT,
           sobald stderr in eine Datei geht. Beim Beenden ging alles verloren.
           `-u` behebt es.
        3  `grep -c muster datei || echo 0` gibt bei LEERER Datei zweimal "0"
           aus (einmal grep, einmal der Zweig, weil grep Exit 1 liefert).
           Der Vergleich `!= "0"` war damit sofort wahr — gemessen wurde die
           Dauer eines ssh-Aufrufs. Herausgekommen sind 0,4 bis 0,7 s: Zahlen,
           die plausibel aussahen und nichts bedeuteten. Nach dem Fix standen
           dort 1,5 s (Cog) und 2,7 s (Chromium).
        4  Im Melde-Takt lief die Messung VOR dem Melden; ein Wurf dort
           verschluckte die ganze Zeile.

      DIE LEHRE IST NICHT „sorgfaeltiger sein", sondern eine BAUREGEL: Ein
      Messwerkzeug prueft beim Start, ob seine eigene Kette steht, und BRICHT
      AB, wenn nicht. Hier heisst das: nach dem Start des Mithoerers einmal
      anklopfen (curl gegen den Port) statt `pgrep` zu zaehlen — pgrep findet
      die eigene Zeile, ein Port antwortet oder nicht.
      UND: „nicht gemessen" gehoert in die Ausgabe, nicht ein Strich, den man
      als „kein Unterschied" liest. Eine fehlende Zahl muss sich von einer
      gleichen Zahl unterscheiden.
    related: ["einmal-hinsehen-ist-keine-messung", "gegenprobe-statt-gruen-glauben"]

  - id: cog-spart-267-mb-gemessen
    kind: knowhow
    title: "Cog gegen Chromium, ueber die Zeit gemessen: 267 MB und 2 Prozentpunkte CPU"
    match: "cog spart wieviel|chromium gegen cog speicher|kiosk browser vergleich|lohnt sich cog"
    sev: medium
    tags: ["kiosk", "cog", "chromium", "speicher", "messen", "e57", 2026]
    source: >
      E57, 20.08.2026, tools/kiosk-benchmark.py an der Box (Pi 5, 800x480 ueber
      DSI-2, jeweils Kiosk-Neustart, dann Ruhezeit). Drei Laeufe mit 60 bis
      180 s Ruhezeit, uebereinstimmend.
    body: >
      GEMESSEN (PSS ueber alle Prozesse des jeweiligen Kiosks, bei Chromium
      inklusive X-Server):

                            Chromium + Xorg      Cog auf DRM
        Speicher Anlauf        547-557 MB         269-275 MB
        Speicher nach 1-3 min  537-548 MB         270-285 MB
        Prozesse                    13                 6
        CPU ueber die Spanne     7,1-9,1 %         5,1-7,5 %
        Zeit bis zum Bild          2,7 s             1,5 s

      Also rund 267 MB und zwei Prozentpunkte CPU. Auf einer 2-GB-Box ist das
      die Groessenordnung, in der sonst einzelne Dienste muehsam beschnitten
      werden — hier ist es EIN Schalter (mupibox.kioskBrowser).

      BEIDE SIND STABIL: Chromium schrumpfte ueber die Ruhezeit leicht
      (-9 bis -11 MB), Cog wuchs leicht (0 bis +10 MB). Kein Leck auf beiden
      Seiten; die Startzahl taeuscht hier ausnahmsweise nicht.

      WAS DIE ZAHL NICHT SAGT: dass unter WebKit einzelne Bedienelemente
      anders reagieren (siehe [[webkit-zieht-den-reglergriff-nicht]] und
      [[webkit-button-flex-streckt-nicht]] — beide am selben Tag gefunden und
      behoben). Wer nur auf die Speicherzahl sieht, stellt um und sucht den
      Fehler danach in der Oberflaeche.
    related: ["cog-drm-kiosk-statt-chromium", "kiosk-browser-umschaltbar-mit-rueckfall", "webkit-zieht-den-reglergriff-nicht"]

  - id: kiosk-wartete-auf-wlan-ueber-user-sessions
    kind: falle
    title: "Der Kiosk wartete 5 s aufs WLAN — ueber systemd-user-sessions, nicht ueber sich selbst"
    match: "kiosk startet spaet|bildschirm bleibt schwarz beim start|getty wartet auf netz|boot dauert wegen wlan|systemd-user-sessions network.target"
    sev: medium
    tags: ["boot", "systemd", "kiosk", "wlan", "e59", 2026]
    source: >
      E59, 20.08.2026, an der Box ueber echte Kaltstarts gemessen. Vorher:
      getty@tty1 bei 8,17 s. Nachher: 2,98 s. Der Server, dessen Seite der
      Kiosk zeigt, war schon bei 4,05 s bereit.
    body: >
      DIE KETTE, die niemand sucht, weil sie ueber DREI Ecken geht:
        ifup@wlan0 (3,55 s DHCP) -> network.target -> systemd-user-sessions
        -> getty@tty1 -> Kiosk

      `getty@tty1` hat KEIN network.target in seiner After-Liste. Wer dort
      nachsieht, findet nichts und sucht weiter. Der Blocker ist
      systemd-user-sessions.service: der Dienst entfernt /run/nologin, und
      seine Original-Unit traegt `After=... network.target ...` — gedacht fuer
      Systeme, deren Benutzer aus dem Netz kommen (LDAP, NIS). Eine Box mit
      `passwd: files` in /etc/nsswitch.conf wartet dort auf nichts.

      EIN DROP-IN GENUEGT NICHT. Ordnungs-Abhaengigkeiten (After=/Before=)
      lassen sich in systemd NICHT mit einer leeren Zuweisung zuruecksetzen,
      anders als etwa ExecStart=. Der Versuch bleibt wirkungslos, und
      `systemctl show -p After` zeigt network.target unveraendert weiter —
      man haelt das leicht fuer einen Tippfehler und sucht am falschen Ende.
      Was hilft: eine VOLLE KOPIE der Unit nach /etc/systemd/system/ (gewinnt
      gegen /usr/lib/systemd/system/), mit der einen geaenderten Zeile. Preis:
      sie erbt kuenftige Aenderungen von systemd nicht.

      WAS SICH NICHT AENDERT: Die gespeicherten WLANs, die Verbindung selbst,
      SSH. Das Netz kommt weiterhin — nur eben NACH dem Bild statt davor
      (network.target 7,12 s, WLAN verbunden). Fuer eine Box, die an
      wechselnden Orten eingeschaltet wird, ist das sogar der bessere Fall:
      wo kein bekanntes Netz steht, wartete sie vorher, bis DHCP aufgab.

      NICHT UEBER ssh.service GEHEN, auch wenn multi-user.target dort haengt:
      ein Fehlgriff an SSH sperrt einen aus einer Box aus, die keine Tastatur
      hat.
    related: ["cog-drm-kiosk-statt-chromium", "kiosk-browser-umschaltbar-mit-rueckfall"]

  - id: vier-zahlen-haengen-am-messtakt
    kind: falle
    title: "Wer den Messtakt aendert, verschiebt vier Zahlen — und reisst zwei Grenzen"
    match: "messtakt aendern|oefter messen|systemverlauf takt|sd karte schreiblast|verlaufsdatei zu gross|MESS_MS"
    sev: high
    tags: ["systemverlauf", "messen", "sd-karte", "bootzeit", "e61", 2026]
    source: >
      E61, 20.08.2026: „koennen wir cpu speicher oefter aktualisieren?" Der
      erste Anlauf stellte MESS_MS von 60_000 auf 20_000 und wurde nach acht
      Runden zurueckgerollt. Der zweite rechnete vorher — und war in einem Zug
      fertig.
    body: >
      VIER ZAHLEN BEDINGEN SICH GEGENSEITIG, und drei davon stehen nicht dort,
      wo man den Takt aendert:
        MESS_MS      wie oft gemessen wird
        MAX_PUNKTE   wie lang die Historie reicht
        SCHREIB_JE   wie viele Punkte gesammelt werden, bevor geschrieben wird
        UEBERHANG    wie weit die Datei ueber den Ring wachsen darf, bevor sie
                     GANZ neu geschrieben wird (Verdichten)
      Wer nur den Takt verdreifacht, verdreifacht Schreiblast, Dateigroesse
      UND Verdichtungsfrequenz gleich mit.

      ZWEI GRENZEN, DIE DABEI FALLEN — beide von Tests gefangen, beide echt:
        * 5,1 GB Schreiblast im Jahr statt 1,7. SD-Karten haben endliche
          Schreibzyklen; eine Telemetriekurve ist es nicht wert.
        * 1,9 MB Verlaufsdatei. Der Server liest sie beim Start Zeile fuer
          Zeile — und der Kiosk-Start war am selben Tag um fuenf Sekunden
          schneller geworden. Die holt man sich nicht zurueck, indem man eine
          Telemetriedatei aufblaeht.

      WAS HILFT: tools/verlauf-takt-rechnen.mjs spielt die Kombinationen mit
      DEMSELBEN Kostenmodell durch, das die Tests benutzen (aufwandAnhaengen),
      und meldet, welche alle drei Grenzen haelt. Gewaehlt wird nicht der
      feinste Takt, sondern die beste RESERVE: 20 s bei zwei Tagen Historie
      lastet keine Grenze staerker aus als der Minutentakt vorher (Karte 88 %,
      Datei 69 %, Verfahrensfaktor 29 gegen ein Minimum von 20).

      UND DIE TESTS MUESSEN ZEITRAEUME PRUEFEN, NICHT ZAHLEN. `MAX_PUNKTE ===
      10080` nagelt den Takt fest: wer ihn aendert, sieht ein rotes Testfeld
      und nicht, was wirklich zu pruefen ist. Richtig ist `MAX_PUNKTE *
      MESS_MS === HISTORIE_MS`. Dasselbe gilt fuer Schrittweiten in
      Testdaten: `n * 60_000` prueft nach einer Taktaenderung Punkte im
      falschen Abstand, und „ein ausgefallener Punkt ist keine Luecke" wird
      zur Luecke, ohne dass sich am geprueften Code etwas geaendert hat.
    related: ["einmal-hinsehen-ist-keine-messung", "messwerkzeug-muss-sich-selbst-pruefen"]

  - id: fremde-unit-ersetzen-braucht-alle-drei-wege
    kind: falle
    title: "Eine ERSETZTE System-Unit faellt durch zwei der drei Ausrollwege"
    match: "unit ueberlebt neuinstallation nicht|systemd-user-sessions fehlt|kein ausrollweg legt die unit an|ausrollweg-deckung ungedeckt"
    sev: high
    tags: ["ausrollwege", "systemd", "installation", "e59", "e61", 2026]
    source: >
      E61, 20.08.2026, beim Abschluss mit tools/ausrollweg-deckung.py gefunden:
      „systemd-user-sessions.service: kein Ausrollweg legt die Unit an." Die
      Datei lag im Repo, das Rezept nahm sie mit — autosetup.sh und
      update/start_mupibox_update.sh nicht.
    body: >
      DIE DREI WEGE BEHANDELN DIENSTE VERSCHIEDEN, und das faellt genau dann
      auf die Fuesse, wenn man eine FREMDE Unit ersetzt:
        * Das Rezept kopiert pauschal: `install -m 644 services/*.service`.
          Was im Ordner liegt, kommt mit — auch Neues, ohne Zutun.
        * autosetup.sh und start_mupibox_update.sh benennen JEDE Unit
          einzeln (`mv -f .../mupibox-boot-splash.service ...`). Wer eine
          Datei hinzulegt, ohne dort zwei Zeilen zu ergaenzen, hat sie auf
          einer frisch bespielten Karte nicht.

      WARUM ES BEI EIGENEN DIENSTEN AUFFAELLT UND HIER NICHT: Ein eigener
      Dienst wird auch eingeschaltet (`systemctl enable`), und das fehlt dann
      sichtbar. Eine ERSETZUNG einer System-Unit braucht kein enable — systemd
      zieht sie ohnehin. Sie fehlt also lautlos, und die Box laeuft
      weiter, nur eben mit dem alten Verhalten. Hier hiesse das: der Kiosk
      startet wieder erst nach dem WLAN, fuenf Sekunden spaeter, und niemand
      wuesste warum.

      WAS HILFT: tools/ausrollweg-deckung.py am Ende eines Blocks laufen
      lassen. Es vergleicht, was im Repo liegt, mit dem, was die Wege
      ausrollen, und nennt die Luecken beim Namen.
    related: ["drei-ausrollwege-keiner-kennt-den-anderen", "kiosk-wartete-auf-wlan-ueber-user-sessions"]

  - id: messwerkzeug-baut-sich-den-befund-selbst
    kind: falle
    title: "Der Titel wurde als zweiter Strom mitgezaehlt — ein erfundener Beweis fuer die eigene Vermutung"
    match: "zwei stroeme gleichzeitig|zwei spielende stroeme|stromzahl springt beim titelwechsel|echo-spur meldet zwei"
    sev: high
    tags: ["messwerkzeug", "echo-spur", "pipewire", "e63", "falle", 2026]
    source: >
      E63, 20.08.2026, beim AUSWERTEN von tools/echo-spur.sh bemerkt, nicht
      beim Schreiben. Der Befund war schon formuliert und stand kurz vor der
      Weitergabe an den Betreiber.
    body: >
      WAS PASSIERTE: Das Werkzeug haengte den laufenden Titel an denselben
      String, aus dem danach die Stroeme gezaehlt wurden. Gezaehlt wurde durch
      Trennen an einem senkrechten Strich. Der Titel galt damit als zweiter
      Strom — das Werkzeug meldete „zwei Stroeme gleichzeitig", wo genau einer
      lief.

      WARUM DAS SCHLIMMER IST ALS EIN GEWOEHNLICHER FEHLER: Die Vermutung, die
      ueberhaupt geprueft werden sollte, lautete „zwei Player gleichzeitig".
      Der Fehler erzeugte GENAU diesen Befund. Ein Werkzeug, dessen Fehler
      zufaellig die Hypothese bestaetigt, wird nicht hinterfragt — es wird
      geglaubt, und der falsche Befund wandert ungeprueft weiter.

      DIE REGEL DARAUS: Der gemessene Wert und sein Etikett duerfen nie in
      denselben String, wenn danach an einem Trennzeichen gezaehlt wird.
      Zaehlung und Beschriftung gehoeren in getrennte Felder. Und die zweite
      Haelfte: Wenn ein Messwerkzeug die eigene Vermutung bestaetigt, ist das
      der Moment fuer die Gegenprobe — nicht der Moment fuer den Bericht.
    related: ["messwerkzeug-muss-sich-selbst-pruefen", "spotify-connect-behaelt-den-strom"]

  - id: spotify-connect-behaelt-den-strom
    kind: falle
    title: "Bei Spotify Connect bleibt der Strom ueber den Titelwechsel hinweg bestehen"
    match: "titelwechsel nicht erkannt|keine neue stromnummer|sink-input index unveraendert|wechsel wird nicht bemerkt"
    sev: med
    tags: ["spotify", "connect", "pipewire", "pulseaudio", "e63", 2026]
    source: >
      E63, 20.08.2026: Der erste Entwurf von tools/echo-spur.sh erkannte einen
      Titelwechsel an einer neuen Stromnummer und haette den Wechsel deshalb
      gar nicht bemerkt.
    body: >
      Wer auf einen Titelwechsel warten will, darf NICHT auf eine neue
      Stromnummer warten. Bei Spotify Connect laeuft derselbe sink-input
      weiter; es aendern sich nur die Metadaten. Eine neue Nummer gaebe es
      erst beim Quellenwechsel oder beim Neuaufbau des Knotens.

      WORAUF STATTDESSEN: auf den Titel selbst (media.name beziehungsweise die
      Metadaten) — oder auf das Verschwinden und Wiederkommen des
      Quellknotens. Der wird beim Wechsel tatsaechlich neu aufgebaut und ist
      damit der zweite brauchbare Anker.
    related: ["messwerkzeug-baut-sich-den-befund-selbst", "soloist-erste-messung"]

  - id: entzerrer-bleibt-verkabelt-wenn-aus
    kind: architektur
    title: "Der Equalizer haengt weiter an der Senke, auch wenn er ausgeschaltet ist"
    match: "entzerrer aus aber verbunden|drei leitungen an der bluetooth-senke|move-sink-input bleibt|equalizer aus trotzdem geroutet"
    sev: med
    tags: ["audio", "pulseaudio", "equalizer", "bluetooth", "e63", 2026]
    source: >
      E63, 20.08.2026, an der Box ausgelesen: an der Bluetooth-Senke enden
      drei Leitungen — Entzerrer, Kombi-Senke „Ueberall" und Spotify direkt.
      Der Entzerrer stand dabei auf an: false.
    body: >
      WIE ES DAZU KOMMT: entzerrerRouten() nagelt den Ausgang mit
      move-sink-input fest. Beim Ausschalten wird nur die VORGABE
      zurueckgestellt — die gesetzte Nadel bleibt. Ein ausgeschalteter
      Entzerrer ist damit weiter verkabelt.

      WAS DAS ERKLAERT UND WAS NICHT: Es erklaert, warum an der Senke drei
      Leitungen enden. Es erklaert NICHT von selbst einen doppelten Ton: beide
      Zusatzwege standen auf Corked, und ein gekorkter Strom fuehrt keinen Ton.
      Fuer ein Echo fehlt der Nachweis, dass beim Wechsel einer davon aufwacht;
      dann redet auch module-stream-restore mit, das sich je Strom das zuletzt
      benutzte Ziel merkt.

      UND EIN FIX, DER KEINER WAR: Gebaut wurde einer, der den
      Entzerrer-Ausgang auf dieselbe Senke haengt, an der er ohnehin haengt —
      wirkungslos. Zurueckgerollt vor der Auslieferung. Wer eine Leitung
      korrigiert, muss vorher ausgelesen haben, wohin sie schon zeigt.
    related: ["messwerkzeug-baut-sich-den-befund-selbst"]

  - id: halbe-aufnahme-nicht-anzeigen
    kind: entscheidung
    title: "Ein unvollstaendiger TITEL wird verworfen — das ALBUM wird seit E89 gezeigt und markiert"
    match: "ACHTUNG unvollstaendig|KACHEL ANGELEGT|aufnahme bricht ab|unvollstaendige aufnahme sichtbar"
    sev: high
    tags: ["mitschnitt", "aufnahme", "kacheln", "e64", "entscheidung", 2026]
    source: >
      E64a, 20.08.2026. Im Journal stand beides direkt untereinander:
      „ACHTUNG unvollstaendig" und „KACHEL ANGELEGT" — der Mitschnitt erkannte
      die Luecke und legte die Kachel trotzdem an.
    body: >
      DIE ALTE BEGRUENDUNG STAND IM CODE: „zum Puffern ist ein Titel zu
      neunzig Prozent mehr wert als gar keiner." Fuer einen Puffer stimmt das.
      Fuer ein Kind nicht: es tippt die Kachel an, hoert mitten im Satz Stille
      — und meldet das nicht als Fehler. Es tippt die Kachel nie wieder an.
      Der Schaden ist unsichtbar und dauerhaft, und keine Fehlerliste zeigt ihn.

      WIE ES JETZT IST: Schwelle 97 Prozent JE TITEL, geprueft mit den schon
      vorhandenen Werkzeugen (vollstaendigkeit, unvollstaendigZusatz). Ein
      Album, dessen letzter Titel abbricht, behaelt seine ganzen Stuecke —
      verworfen wird der Titel, nicht das Album.

      DIE AUSNAHME, DIE BLEIBEN MUSS: ein Anteil von null wird NICHT verworfen.
      Ohne Solldauer waere das ein Verwurf auf Verdacht — dann faellt weg, was
      lediglich nicht gemessen werden konnte.

      WAS AN DIESEM EINTRAG SEIT E89 (23.08.2026) NICHT MEHR GILT — und was
      sehr wohl: Der alte Titel sagte „nicht als Kachel angeboten". Das war
      auf TITEL-Ebene gemeint und stimmt dort weiter (`index.mjs`, der
      97-%-Zweig verwirft das Stueck und merkt es vor). Auf ALBUM-Ebene ist
      es seit E89 falsch: die Kachel entsteht nach dem ersten gelungenen
      Stueck und traegt `unvollstaendig`, statt zu fehlen. Der Betreiber hat
      sich nach dem 101-Meerjungfrauen-Fund dafuer entschieden, weil eine
      fehlende Kachel dem Erwachsenen nichts sagt, eine markierte schon.
      Zwei Ebenen, zwei Regeln — siehe
      unvollstaendig-wird-gesetzt-nicht-am-ende-behauptet.
    related: ["messwerkzeug-muss-sich-selbst-pruefen", "unvollstaendig-wird-gesetzt-nicht-am-ende-behauptet"]

  - id: werkzeug-meldet-ohne-empfaenger
    kind: falle
    title: "Das Sicherungswerkzeug nannte die Luecke — nur las sie niemand"
    match: "nicht eingeordnet, NICHT gesichert|NICHT gesichert|datei faellt aus der sicherung"
    sev: high
    tags: ["sicherung", "backup", "e65", "falle", 2026]
    source: >
      E65, 20.08.2026: beim Anlegen des Sicherungsnetzes meldete das Werkzeug
      drei Dateien als „nicht eingeordnet, NICHT gesichert" — darunter
      ausgerechnet interpreten.json, um die es im naechsten Block ging.
    body: >
      DIESELBE ZEILE STEHT IM `match:` VON
      [[profile-json-fiel-aus-der-sicherung]] — dort meldete sie
      server/config/profile.json, hier interpreten.json, albumstop.json und
      profile/*/verlauf.json. Der Dateiname in der Meldung sagt, welcher
      Eintrag gilt; die Lehre unten gilt fuer beide. Wer nur den anderen
      liest, haelt einen Einzelfall fuer behoben, statt zu sehen, dass die
      Meldung weiterhin niemanden erreicht.


      DIE DREI, DIE HINEINGEHOEREN UND FEHLTEN:
        * interpreten.json — die Entscheidungen der Box ueber ihre
          Interpreten. Steht NIRGENDWO sonst: Spotify kennt sie nicht,
          data.json auch nicht. Ohne sie kommen abgelehnte Verschmelzungen
          alle wieder.
        * albumstop.json — wo ein Album enden soll. Je Album entschieden und
          aus nichts ableitbar.
        * profile/*/verlauf.json — was ein Kind gehoert hat.

      BEWUSST DRAUSSEN GEBLIEBEN: systemverlauf.jsonl (Telemetrie ueber die
      MASCHINE — nach einem Kartenschaden beschreibt sie ein Geraet, das es
      nicht mehr gibt), die Sicherungskopien mit vor-Praefix und die
      Stuecklisten.

      DIE EIGENTLICHE LEHRE: Es ist dieselbe Zeile, die einst bei profile.json
      niemand las — der Kommentar dort nennt sie den teuersten einzelnen
      Verlust im ganzen Stand. Das Skript hatte es damals gesagt, nur ohne
      Empfaenger. Eine Warnung ohne Empfaenger ist keine Warnung. Wer ein
      Werkzeug baut, das etwas meldet, muss festlegen WER die Meldung liest
      und WANN — sonst ist die Zeile nur ein Alibi.
    related: ["sicherung-ausgelassene-schluessel-nie-schreiben", "sicherung-mitten-in-der-wiederherstellung", "profile-json-fiel-aus-der-sicherung"]

  - id: einmal-hinsehen-ist-keine-messung
    kind: falle
    title: "Eine Momentaufnahme kann einen Anlauf nicht von einem Zustand unterscheiden"
    match: "progressTime null|timePos null|duration null|dauer wird verworfen|wert steht auf null"
    sev: high
    tags: ["messwerkzeug", "mpv", "wiedergabe", "falle", 2026]
    source: >
      14.08.2026: /player/local wurde EINMAL gelesen, wenige Sekunden nach dem
      Start eines Podcast-Stroms. progressTime, timePos und duration standen auf
      null. Daraus wurden ein Befund, ein Commit, ein README-Eintrag und ein
      naechster Schritt — der Abspielzweig rss werfe die Dauer weg.
    body: >
      DAS WAR FALSCH. Ueber 32 Sekunden gepollt lief alles: timePos 2,94 auf
      31,21, duration 1382,12 — exakt der Wert aus dem Feed. Die Nullen waren
      mpv BEIM PUFFERN.

      WARUM ES SO LEICHT PASSIERT: Ein laufender Vorgang hat einen Anlauf. Eine
      Momentaufnahme darin sieht aus wie ein Dauerzustand — und sie fuegt sich
      bereitwillig in die Geschichte, die man schon im Kopf hat. Es ist dieselbe
      Regel wie bei einer Zahl, die der Erwartung widerspricht: sie ist zuerst
      ein Verdacht gegen die Messung. Nur faellt das viel schwerer auf, wenn die
      Zahl der Erwartung ENTGEGENKOMMT.

      DER ZWEITE FEHLER KAM OBENDRAUF: Die Ursache wurde aufgeschrieben, ohne
      sie nachzulesen. Die drei Werte haengen gar nicht am Abspieltyp; sie
      kommen aus mpv-Eigenschaften, die ein Intervall abfragt und die ungeprueft
      durchgereicht werden.

      WAS GILT: Bei allem, was anlaeuft — Wiedergabe, Verbindungen, Dienste,
      Puffer — mindestens ZWEIMAL MIT ABSTAND messen und die Werte nebeneinander
      zeigen, eine Zeile je Zeitpunkt. Und bevor eine Ursache in einen Commit
      geschrieben wird, die Stelle im Code lesen, die sie angeblich verursacht.
    related: ["messwerkzeug-muss-sich-selbst-pruefen", "messwerkzeug-baut-sich-den-befund-selbst"]

  - id: mpv-puffer-ist-fuer-video-gebaut
    kind: falle
    title: "mpv belegt im Leerlauf 150 MB — seine Voreinstellung rechnet mit Video"
    match: "mpv speicher leerlauf|demuxer-max-bytes|mpv 150 mb|player frisst ram|puffer nicht freigegeben"
    sev: medium
    tags: ["speicher", "mpv", "e51", 2026]
    source: >
      E51, 20.08.2026. Am Geraet gemessen: mpv 152,2 MB PSS mit LEEREM Player
      (get_property path -> "property unavailable"), davon 134 MB anonym.
    symptom: >
      Der Abspieler steht im Leerlauf und belegt dreistellige Megabyte. Auf
      einer 2-GB-Box ist er damit nach dem Browser der groesste Posten,
      obwohl gerade nichts spielt.
    cause: >
      `--demuxer-max-bytes` steht bei mpv auf 150 MiB — die Auslegung fuer
      VIDEO. Der Puffer waechst beim Abspielen bis dorthin und wird danach
      NICHT ans System zurueckgegeben; der Prozess laeuft ja weiter
      (`--idle=yes`). Bei Audio ist das absurd: 128-kbps-MP3 sind 16 kB/s,
      32 MiB fassen damit rund 33 Minuten Vorlauf.
    fix: >
      `--demuxer-max-bytes=32MiB` und `--demuxer-max-back-bytes=8MiB` beim
      Start mitgeben. NICHT kleiner: bei einer Netzstoerung liest mpv aus dem
      Puffer weiter, und 4 MiB waeren bei jedem WLAN-Zucken eine Luecke.

      BEIM MESSEN NICHT TAEUSCHEN LASSEN — hier sind drei Fallen:
        * Ein FRISCH gestarteter mpv ist mit und ohne Grenze gleich gross
          (34 MB gemessen). Der Puffer fuellt sich erst beim Abspielen.
        * An einer kurzen Datei (3 Minuten) wirkt die Grenze gar nicht
          (50 MB, beides gemessen) — sie greift erst bei langen Stuecken.
        * Der Sofort-Gewinn nach dem Ausrollen kommt vom NEUSTART des
          Prozesses, nicht von der Grenze. Wer das verwechselt, schreibt eine
          Zahl auf, die die naechste Messung nicht bestaetigt.
      Am Strom laesst sich die Wirkung schwer zeigen, wenn die Tonadresse
      ablaeuft (ARD): mpv beendet sich dann vorzeitig.
    related: ["ramlog-macht-protokoll-zu-arbeitsspeicher"]


  - id: kiosk-browser-was-er-wirklich-koennen-muss
    kind: pattern
    title: "Bevor man den Kiosk-Browser tauscht: zaehlen, was die Oberflaeche wirklich braucht"
    match: "chromium ersetzen|kiosk browser leichter|cog wpe webkit|braucht chrome|browser speicher kiosk"
    sev: medium
    tags: ["kiosk", "speicher", "e54", 2026]
    source: "E54, 20.08.2026 — Chromium 458 MB PSS ueber 8 Prozesse, gemessen an der Box."
    body: >
      Chromium ist auf einer 2-GB-Box der groesste Posten, und dahinter steht
      Xorg nur, um ihn zu tragen. Ob ein leichterer Browser genuegt,
      entscheidet NICHT der Geschmack, sondern eine Zaehlung:

      WAS DIESE OBERFLAECHE NICHT BRAUCHT (nachgesehen, nicht vermutet):
        * Ton — den machen mpv und librespot; die Seite hat genau EIN
          `new Audio()`, und das gibt die vorgelesenen Saetze wieder.
        * Sprachsynthese — das Vorlesen erzeugt Piper im Server
          (/api/vorlesen). `speechSynthesis` steht in app.js nur im Kommentar.
        * Video, Kamera, Service Worker, WebGL fuer Inhalte.

      WAS SIE BRAUCHT: localStorage, backdrop-filter, scroll-snap, color-mix,
      aspect-ratio, requestAnimationFrame — alles gewoehnliche Standards, die
      WebKit beherrscht.

      DER PRUEFWEG, der wirklich entscheidet, ist nicht der Speicher, sondern
      die EINGABE: unter X liefert den Beruehrungsschirm ein eigener Dienst.
      Ein Browser, der ohne X direkt auf DRM zeichnet (cog --platform=drm),
      spart zusaetzlich den X-Server — muss den Finger dann aber selbst
      ueber libinput bekommen. Daran haengt der Versuch, nicht an den MB.

      NICHT VERWECHSELN: CEF ist Chromium als Bibliothek (gleiche Engine,
      gleicher Speicher, keine ARM64-Builds), und „headless"-Browser sind
      per Definition ohne Bild — fuer Automatisierung, nicht fuer einen Kiosk.
    related: ["headless-schirm-ist-kleiner-als-bestellt"]


  - id: headless-messen-misst-den-absturz-mit
    kind: falle
    title: "Headless gemessen, Renderer abgestuerzt — die schoene Zahl war ein toter Prozess"
    match: "renderer process crashed|headless speicher messen|browser vergleich zu gut|cog headless test"
    sev: medium
    tags: ["messwerkzeug", "e54", "falle", 2026]
    source: "E54, 20.08.2026: cog headless meldete 76 MB gegen Chromiums 458 MB — im Protokoll stand 'The renderer process crashed.'"
    symptom: >
      Ein Vergleichstest liefert einen ueberraschend guten Wert fuer die
      Alternative. Die Zahl haelt keiner Nachpruefung stand, weil der
      gemessene Prozess seine Arbeit gar nicht getan hat.
    cause: >
      Headless wurde gewaehlt, um den laufenden Kiosk nicht zu stoeren — und
      genau das entzog dem Browser die Grafikanbindung, an der er haengt. Der
      Renderer stuerzte ab, die Hauptprozesse liefen weiter, und die
      Speichermessung fand nur noch die Huelle.
    fix: >
      Bei jedem Browser-Vergleich ZUERST pruefen, ob die Seite wirklich steht:
      Protokoll auf 'crash' lesen, und einen Beweis verlangen, dass gerendert
      wurde (Bildschirmfoto, ein per JavaScript ausgelesener Text, eine
      gemessene Zeichenzeit). Eine Speicherzahl ohne Bildbeweis ist keine
      Messung.

      UND: Ein Browser, der auf dem echten Schirm laufen soll, gehoert auf dem
      echten Schirm gemessen — mit exklusivem Zugriff und jemandem davor. Der
      bequeme Weg (headless, aus der Ferne, neben dem laufenden Betrieb) misst
      etwas anderes als die Frage.
    related: ["kiosk-browser-was-er-wirklich-koennen-muss", "einmal-hinsehen-ist-keine-messung"]

  - id: interpretenkennung-gehoert-der-box
    kind: entscheidung
    title: "Der Interpret braucht eine Kennung, die NICHT aus seinem Namen abgeleitet ist"
    match: "interpret in zwei quellen anders benannt|interpret umbenannt verweise weg|aufnahme und spotify verknuepfen|box interne interpreten id"
    sev: high
    tags: ["interpreten", "identitaet", "e64b", "e45", "e49", "entscheidung", 2026]
    source: >
      E64b, 20.08.2026, Auftrag des Betreibers: „koennen wir nicht box intern
      eine interpreten ID machen, an die wir einfach aufnahme und Spotify usw
      knuepfen". Gebaut als src/backend-api/src/interpretenkennung.ts, 34
      Zeugen, ohne einen einzigen Verbraucher umzustellen.
    body: >
      WARUM E45 UND E49 NICHT GENUEGEN, obwohl beide richtig waren: E45 machte
      den NAMEN zur Identitaet, damit Interpreten nicht mit Spotify
      verschwinden. E49 schaerfte den Schluessel, bis „Die drei ???" und „Die
      drei !!!" auseinanderfielen. Beide lassen dieselbe Luecke offen, weil der
      Schluessel AUS DEM NAMEN abgeleitet wird:

        1. Heisst derselbe Interpret in zwei Quellen verschieden — bei Spotify
           „ARD Sounds", im Aufnahmeordner „Die Sendung mit der Maus" —, gibt
           es keine Stelle, an der beide zusammenkommen. Ein besserer
           Schluessel hilft nicht: die Namen SIND verschieden.
        2. Aendert sich ein Name, aendert sich der Schluessel, und alles, was
           daran hing, verwaist still.

      DIE FORM: eine Kennung `int_` + zwoelf Hexziffern, die der Box gehoert,
      mit MEHREREN Namen und MEHREREN Dienstverweisen daran (spotify,
      aufnahme, jellyfin …). Zufaellig erzeugt, nicht gezaehlt: zwei Boxen, die
      je fuer sich zaehlen, vergeben dieselbe Nummer an verschiedene
      Interpreten, und das faellt erst beim Zusammenfuehren auf.

      DIE RICHTUNG DES ZWEIFELSFALLS BLEIBT DIE AUS E49 — und sie ist der
      Grund, warum das kein rein technischer Umbau ist: Zwei Interpreten unter
      EINER Kennung sind ein stiller, nicht zurueckholbarer Schaden; ein
      Interpret unter ZWEI Kennungen ist eine sichtbare Kachel zu viel. Deshalb
      verweigert das Anhaengen eines fremden Namens, sobald eine MASCHINE es
      will (stufe erkannt/wanderung); nur ein Mensch (stufe hand) darf
      zusammenlegen, und danach steht `hand` in der Ablage und ist ablesbar.

      DIE WANDERUNG LEGT NICHTS ZUSAMMEN. Sie gibt jedem Namen, den die Box
      heute kennt, eine Kennung — mehr nicht. Ob „ARD Sounds" und „Die Maus"
      derselbe sind, ist gerade die Frage, die ein Mensch beantworten soll;
      sie beim Wandern still zu entscheiden hiesse, den Schaden in die Ablage
      zu schreiben, bevor jemand gefragt wurde.

      DIE ABLAGE wohnt in interpreten.json neben `frei` und `abgelehnt`, die
      unberuehrt bleiben — dieselbe Datei, die seit E65 im Sicherungsnetz liegt.
    related: ["werkzeug-meldet-ohne-empfaenger", "namen-liste-string-erfindet-interpreten"]

  - id: namen-liste-string-erfindet-interpreten
    kind: falle
    title: "String(x) auf einer Namensliste macht aus der Zahl 42 einen Interpreten"
    match: "interpret 42|fremder eintrag im regal|name aus zahl entstanden|kennung fuer unsinn angelegt"
    sev: med
    tags: ["interpreten", "eingabepruefung", "e64b", "falle", 2026]
    source: >
      E64b, 20.08.2026: von einem Zeugen gefunden, nicht beim Schreiben
      bedacht. `nameSauber` benutzte String(x ?? '') und machte damit aus einer
      Zahl in der Namensliste einen gueltigen Namen.
    body: >
      DER BEQUEME WEG IST DER FALSCHE. `String(x)` nimmt alles an und gibt
      immer etwas zurueck — aus der Zahl 42 wird der Name „42", aus ihm ein
      gueltiger Interpretenschluessel, und daraus ein Eintrag mit eigener
      Kennung im Regal des Kindes.

      WARUM DAS TEURER IST ALS EIN VERWORFENER EINTRAG: Ein Datenfehler, den
      man in einen EINTRAG verwandelt, ist hinterher nicht mehr von einer
      ENTSCHEIDUNG zu unterscheiden. Wer die Ablage spaeter liest, sieht einen
      Interpreten „42" und muss raten, ob den jemand angelegt hat.

      DIE REGEL: An Stellen, wo aus einer Eingabe ein dauerhafter Eintrag
      wird, `typeof x !== 'string'` pruefen statt zu wandeln. Ziffern IM Namen
      bleiben davon unberuehrt — „EMMA6" ist ein String und ein echter Name
      dieser Box. Es geht allein darum, dass eine Zahl KEIN Name ist.

      UND DIE ZWEITE HAELFTE: Gefunden hat das ein Test, der Muell in die
      Liste kippte, nicht das Nachdenken beim Schreiben. Solche Zeugen
      („uebersteht Muell") lohnen sich genau deshalb.
    related: ["interpretenkennung-gehoert-der-box"]

  - id: von-hand-aenderbare-datei-braucht-zusammenfuehren
    kind: falle
    title: "Wer nur seine eigenen Schluessel zurueckschreibt, loescht die der anderen"
    match: "schluessel nach dem schreiben weg|von hand eingetragen verschwunden|kennungen nach freischalten weg|json feld still geloescht"
    sev: high
    tags: ["konfiguration", "json", "e64b", "interpreten", "falle", 2026]
    source: >
      E64b, 20.08.2026, in server.ts gefunden: `interpretenSchreiben` schrieb
      `JSON.stringify(ablage)` mit ablage = {frei, abgelehnt}. Jeder
      Schreibvorgang entfernte damit jeden anderen Schluessel aus
      config/interpreten.json.
    body: >
      DAS BESONDERE AN DIESEM FALL: Dieselbe Datei traegt im Kopfkommentar des
      Servers ausdruecklich die Begruendung „VON HAND KORRIGIERBAR" — sie ist
      dafuer GEBAUT, dass ein Mensch sie oeffnet und etwas hineinschreibt. Und
      genau das ueberlebte den naechsten Klick in der Verwaltung nicht. Die
      Absicht stand im Kommentar, die Umsetzung widersprach ihr, und niemand
      merkte es, weil das Loeschen still geschieht.

      Es ist die Regel aus [[sicherung-ausgelassene-schluessel-nie-schreiben]],
      nur an anderer Stelle: dort war es die Wiederherstellung, hier ein
      gewoehnlicher Schreibvorgang. Sie gilt ueberall, wo ein Schreiber weniger
      versteht als die Datei enthaelt.

      DIE FORM, DIE TRAEGT: erst das ROHE JSON lesen, dann nur die eigenen
      Schluessel darueberlegen — `{ ...roh, frei, abgelehnt }`. Wer neue Felder
      dazulegt (E64b legte `kennungen` daneben), braucht dann keine Zeile am
      Schreiber zu aendern.

      UND EINE ENTSCHEIDUNG, DIE DARAN HAENGT: „Zuruecksetzen" nimmt seither
      nur noch die Entscheidungen zurueck, nicht die Identitaeten. Sonst
      verwaiste mit einem Klick jede Aufnahme, die an einer Kennung haengt —
      und das hat niemand gemeint, der auf „zuruecksetzen" drueckt. Wer
      wirklich alles will, loescht die Datei.
    related: ["sicherung-ausgelassene-schluessel-nie-schreiben", "interpretenkennung-gehoert-der-box"]

  - id: dfs-kanal-laesst-die-box-auf-totem-kanal-zurueck
    kind: falle
    title: "Die Box haengt auf 5 GHz an einem Kanal, den der Router laengst verlassen hat"
    match: "wlan verbunden aber kein ping|box nicht im netz nach neustart|get_tx_power: error \\(-512\\)|wlan stottert dann tot"
    sev: high
    tags: ["wlan", "5ghz", "dfs", "brcmfmac", "e68", "falle", 2026]
    source: >
      E68, 20.08.2026: Die Box war unter keiner IP im Subnetz zu finden. Die
      Netzabriss-Sonde zeigte, dass sie sich um 15:42 auf 5180 (Kanal 36) bei
      -68 dBm gehaengt hatte, waehrend der Router auf 5620 (Kanal 124) sendete
      — 5180 war im Scan gar nicht mehr vorhanden.
    body: >
      DIE FALLE HAT ZWEI HAELFTEN, und einzeln faellt keine auf.

      ERSTENS: 5 GHz reicht nicht weit. Was auf dem Schreibtisch mit -55 dBm
      laeuft, sind im Holzgehaeuse ein paar Meter weiter -68 dBm. Um -70 dBm
      wird die Assoziation unzuverlaessig, und dann genuegt eine Kleinigkeit —
      etwa eine parallele Bluetooth-Suche, die ein paar dB kostet — um sie zu
      kippen.

      ZWEITENS: Viele 5-GHz-Kanaele sind DFS-Kanaele (in Europa 52-64 und
      100-140). Der Router MUSS sie bei Radarerkennung sofort raeumen und
      wechselt dann das Band. Eine Box, die gerade auf dem alten Kanal haengt
      und nicht neu sucht, redet danach ins Leere: das Interface meldet unter
      Umstaenden weiter „verbunden", aber es kommt nichts mehr durch.

      WIE MAN ES ERKENNT: Nicht am Wort „verbunden", sondern an `freq` und
      `signal` aus `iw dev wlan0 link` — und daran, dass ein Scan vom NACHBARN
      aus den Kanal gar nicht mehr zeigt, auf dem die Box zu haengen glaubt.
      Die Kernmeldung `brcmf_cfg80211_get_tx_power: error (-512)` erschien
      genau bei dem Start, nach dem nichts mehr ging.

      WAS HILFT, in dieser Reihenfolge: (1) die Box per `freq_list` in der
      Netzblock-Sektion von wpa_supplicant.conf auf 2,4 GHz festnageln — fuer
      Audio-Streaming reicht die Bandbreite um Groessenordnungen, die
      Reichweite ist besser, und DFS gibt es dort nicht. (2) Im Router einen
      festen Nicht-DFS-Kanal einstellen.

      ACHTUNG, DIE FOLGENDE ZAHL IST VERFALLEN und steht nur noch als Beispiel:
      am 20.08.2026 wurden -68 dBm auf 5180 zu -53 dBm auf 2462 gemessen, also
      15 dB mehr Reserve. WENIGE STUNDEN SPAETER war es umgekehrt (2,4 GHz
      -70 dBm gegen 5 GHz -66 dBm) — die Box hatte sich bewegt. Vor dem
      Festnageln per `freq_list` also IMMER neu messen, nicht diese Zahl
      uebernehmen: [[feldstaerke-ist-kein-wissen-sondern-ein-messwert]].

      NICHT VERWECHSELN mit Stromproblemen: `throttled` blieb bei 0 und die
      Spannung stabil. Und nicht mit einer Koexistenzstoerung: die
      Bluetooth-Suche korrelierte zwar mit dem Stottern, aber der Abriss blieb
      bestehen, waehrend der BT-Zyklus unveraendert weiterlief.
    related: ["messwerkzeug-muss-sich-selbst-pruefen", "kiosk-bereitschaft-fragt-nach-festem-browsernamen"]

  - id: kiosk-bereitschaft-fragt-nach-festem-browsernamen
    kind: falle
    title: "Wenn der Browser waehlbar wird, muss jeder, der nach ihm sucht, mitwandern"
    match: "knopf blinkt immer|led pulst endlos|start wird nie fertig gemeldet|dimmen geht nicht mehr"
    sev: med
    tags: ["kiosk", "cog", "chromium", "led", "e56", "e68", "falle", 2026]
    source: >
      E68, 20.08.2026: Der Betreiber meldete „der button blinkt immer". Am
      Geraet gemessen: `ps -ef | grep chromium | grep http` ergab 0 Treffer,
      dieselbe Suche nach `cog` genau einen.
    body: >
      DER MECHANISMUS: `led_control.py` pulst die LED, SOLANGE der Kiosk noch
      startet — eine Schleife, die endet, wenn der Browser im Prozessbaum
      auftaucht. Seit der Kiosk-Browser waehlbar ist (E56), faehrt diese Box
      Cog. Die Suche nach `chromium` trifft ihn nie, also endet die Schleife
      nie.

      DIE FOLGE IST GROESSER ALS DAS BLINKEN: Die Startroutine erreicht ihre
      Hauptschleife nicht mehr. Damit faellt auch alles aus, was DANACH kaeme —
      hier das Dimmen bei ausgeschaltetem Schirm. Ein haengender Anlauf sieht
      aus wie ein kosmetischer Fehler und ist ein funktionaler.

      DERSELBE FEHLER ZUM ZWEITEN MAL: Im Kommentar direkt ueber der Zeile
      steht die Aufarbeitung vom 07.08.2026 — damals stand dort
      `chromium-browser`, die Box aber hiess `chromium`. Wer eine
      Bereitschaftspruefung auf einen festen Prozessnamen stellt, baut eine
      Zeitbombe fuer den Tag, an dem der Name konfigurierbar wird.

      UND DIE FALLE BEIM BEHEBEN: Den Namen aus der Konfiguration zu lesen ist
      FALSCH. Der Start hat einen Rueckfall (kommt Cog nicht hoch, startet
      Chromium) — dann stuende in der Konfiguration `cog`, waehrend Chromium
      laeuft, und die Pruefung waere wieder blind. Sie muss BEIDE Namen
      treffen, nicht den konfigurierten.

      WO ES UEBERALL SITZT: nicht nur an der einen Stelle. Am 20.08. waren es
      VIER — die LED-Steuerung, die WLED-Warteschleife, das Wiederkommen nach
      dem Heimholen und, zuletzt gefunden, das BEENDEN dort (`pkill -x
      chromium` traf einen laufenden Cog nicht, also beendete das Heimholen
      gar nichts). Die vierte fiel erst auf, weil eine Gegenprobe ein
      unerwartetes Ergebnis lieferte und nachgesehen wurde, statt es
      abzuhaken.

      WAS DAGEGEN WIRKLICH HILFT, ist nicht die einzelne Korrektur, sondern
      eine Pruefung, die das AUSEINANDERLAUFEN findet: ein Test, der jede
      Fundstelle liest und verlangt, dass alle DIESELBEN Namen kennen
      (tools/knopflicht-probe.py, Teil 4). Jede Stelle fuer sich sah richtig
      aus; falsch war nur, dass sie verschiedene Dinge wussten.
    related: ["dfs-kanal-laesst-die-box-auf-totem-kanal-zurueck"]

  - id: unsichtbares-trennzeichen-im-quelltext
    kind: falle
    title: "Ein rohes NUL-Byte als Trenner sieht im Quelltext aus wie nichts"
    match: "trennzeichen unsichtbar|nul byte im schluessel|zusammengesetzter schluessel raetsel|verweis schluessel trenner"
    sev: med
    tags: ["interpreten", "e64b", "quelltext", "falle", 2026]
    source: >
      E64b Block 2b, 20.08.2026, in
      src/backend-api/src/interpretenkennung.ts gefunden: der Schluessel eines
      Dienstverweises wurde aus Dienst und Kennung zusammengesetzt, das
      Trennzeichen dazwischen war ein rohes NUL-Byte.
    body: >
      ES WAR KEIN FEHLER, und das ist gerade das Problem. Alle drei
      Fundstellen benutzten dasselbe Zeichen, der Code arbeitete richtig. Aber
      ein Trennzeichen, das man im Quelltext NICHT SIEHT, ist in jedem Diff
      ein Raetsel und wird beim naechsten Anfassen versehentlich geaendert —
      und dann passen gespeicherte Schluessel nicht mehr zu neu gebildeten,
      still, ohne Fehlermeldung.

      DIE FORM, DIE TRAEGT: eine benannte Konstante (hier VERWEIS_TRENNER),
      an EINER Stelle definiert, mit der Begruendung daneben. Alle
      Fundstellen ziehen sie, keine schreibt das Zeichen noch einmal hin.

      UND WARUM NICHT EINFACH EIN LEERZEICHEN: weil die Werte, die getrennt
      werden, selbst welche enthalten duerfen — ein Aufnahmeordner heisst
      „music/Die Sendung mit der Maus". Ein Trenner muss ein Zeichen sein,
      das im Nutzwert NICHT vorkommen kann; das ist der eigentliche Grund fuer
      das NUL-Byte gewesen, nur stand er nirgends.
    related: ["interpretenkennung-gehoert-der-box"]

  - id: herkunftsstufe-nie-vorbelegen
    kind: entscheidung
    title: "Eine Stufe, die ueber Befugnis entscheidet, darf keine Vorgabe haben"
    match: "stufe fehlt 400|default hand|herkunft vorbelegen|zusammenlegen ohne stufe|wer darf zusammenlegen"
    sev: high
    tags: ["interpreten", "e64b", "schnittstelle", "entscheidung", 2026]
    source: >
      E64b Block 2b, 20.08.2026: alle drei Schreibrouten unter
      /api/interpretenkennungen weisen ohne ausdrueckliche `stufe` mit 400 ab,
      nach dem Vorbild aus verschmelzung.ts.
    body: >
      DIE REGEL AUS verschmelzung.ts LAUTET: „Eine Zuordnung mit unbekannter
      stufe wird ABGEWIESEN, nicht auf hand gebogen … sie zu erfinden waere
      eine Luege ueber die Herkunft." Die Stufe sagt, WER etwas entschieden
      hat — Mensch (hand), Erkennung (erkannt) oder Wanderung. Eine Vorgabe
      erfindet diese Antwort.

      AN /name WIEGT ES SCHWERER ALS SONST: dort entscheidet die Stufe, ob
      zwei Namen zusammengelegt werden DUERFEN — nur ein Mensch darf das
      (siehe [[interpretenkennung-gehoert-der-box]]). Eine Vorgabe `hand`
      gaebe damit jedem Aufrufer, der das Feld schlicht vergisst, genau die
      Befugnis, die einem Menschen vorbehalten sein soll. Der bequeme
      Standardwert waere hier keine Bequemlichkeit, sondern ein offenes Tor.

      DIE PRUEFUNG, DIE DAS FESTHAELT: ein Zeuge je Route, der OHNE stufe
      ruft und 400 verlangt — sonst wandert die Vorgabe beim naechsten Umbau
      unbemerkt wieder hinein.
    related: ["interpretenkennung-gehoert-der-box", "von-hand-aenderbare-datei-braucht-zusammenfuehren"]

  - id: keine-wanderung-beim-start-sondern-luecke-melden
    kind: entscheidung
    title: "Beim Hochfahren nichts anlegen — melden, was fehlt"
    match: "beim start ids vergeben|wanderung beim hochfahren|katalog unvollstaendig beim start|ohneKennung"
    sev: med
    tags: ["interpreten", "e64b", "start", "entscheidung", 2026]
    source: >
      E64b Block 2b, 20.08.2026: GET /api/interpretenkennungen liefert die
      Ablage PLUS `ohneKennung`. Eine Wanderung beim Start gibt es bewusst
      nicht.
    body: >
      DER NAHELIEGENDE WEG WAERE, beim Hochfahren jedem bekannten Namen eine
      Kennung zu geben. Zwei Dinge sprechen dagegen:

        1. Der Katalog ist beim Start NICHT VERLAESSLICH VOLLSTAENDIG. Wer
           dann wandert, wandert ueber einen Ausschnitt und weiss hinterher
           nicht, welcher.
        2. Jeder Name, der je KURZ in der Bibliothek stand — eine Aufnahme,
           die geloescht wurde, ein Tippfehler —, hinterliesse dauerhaft
           einen Eintrag in einer Ablage, die absichtlich lange lebt.

      STATTDESSEN SAGT DAS LESEN, WAS FEHLT: `ohneKennung` ist der eigentliche
      Nutzen des GET. Eine Liste der Kennungen ALLEIN liesse offen, wen die
      Box ausserdem kennt; die Oberflaeche kann aber nur zusammenlegen, was
      sie sieht. Angelegt wird erst, wenn jemand etwas daran haengen will.

      DIE VERALLGEMEINERUNG: Wo ein Start etwas SCHREIBEN wuerde, das ein
      Mensch spaeter nicht mehr von einer Entscheidung unterscheiden kann,
      lieber die Luecke melden als sie fuellen. Dieselbe Ueberlegung wie in
      [[namen-liste-string-erfindet-interpreten]], nur eine Ebene hoeher.
    related: ["interpretenkennung-gehoert-der-box", "namen-liste-string-erfindet-interpreten"]

  - id: selbstbeendender-prozess-statt-schwellenwert
    kind: entscheidung
    title: "Ein Prozess, der sich am Stueckende selbst beendet, schlaegt jede Prozent-Schwelle"
    match: "97 prozent schwelle aufnahme|titel vollstaendig erkennen|mitschnitt abgeschnitten titelwechsel|single-track soloist"
    sev: high
    tags: ["mitschnitt", "soloist", "e66", "e64a", "spotify", "entscheidung", 2026]
    source: >
      E66 Vorbereitung, 20.08.2026, beim Lesen der Soloist-Hilfe gefunden:
      `-s, --single-track <uri>` spielt EIN Stueck und beendet sich danach;
      `-p, --pair` meldet einmalig an, die Anmeldung bleibt im Datenordner.
    body: >
      WAS HEUTE SCHIEF GEHT: Der Mitschnitt ist ein PASSIVER Abgriff am
      Familienstrom — er hoert den Zustand des Abspielers ab und klemmt sich
      an dessen Quellknoten. Wer waehrenddessen den Titel wechselt,
      schneidet die Aufnahme ab. E64a entscheidet deshalb ueber eine
      97-%-Schwelle, ob eine Aufnahme vollstaendig ist: ein GERATENES
      Kriterium, das eine kurz abgebrochene Aufnahme nicht von einem Stueck
      unterscheidet, das schlicht frueher endet.

      DIE ANDERE FORM: je Titel ein KURZLEBIGER Prozess mit --single-track
      statt einer zweiten Dauerinstanz. Dass er sich beim Ende des Stuecks
      SELBST beendet, ist ein HARTES Signal — kein Schaetzwert, sondern eine
      Aussage des Abspielers selbst. Das ersetzt die Schwelle, statt sie zu
      verfeinern.

      DIE ALLGEMEINE LEHRE: Wo ein Schwellenwert eine Ja/Nein-Frage
      beantwortet, lohnt die Frage, ob irgendein Beteiligter die Antwort
      SICHER WEISS und sie nur nicht mitteilt. Meist ist die bessere Loesung
      nicht die bessere Schwelle, sondern der andere Aufbau.

      NOCH NICHT GEBAUT (Stand 20.08.): erst wird gemessen, ob ein zweiter
      Zugang parallel spielen darf —
      [[annahme-vor-dem-bauen-messen-nicht-glauben]].
    related: ["halbe-aufnahme-nicht-anzeigen", "soloist-erste-messung", "annahme-vor-dem-bauen-messen-nicht-glauben"]

  - id: annahme-vor-dem-bauen-messen-nicht-glauben
    kind: pattern
    title: "Haengt ein ganzer Umbau an einer Annahme, wird sie zuerst gemessen — mit einem Werkzeug, das OFFEN sagen darf"
    match: "spotify zweiter zugang parallel|zwei stroeme ein konto|zweitzugang probe|annahme vor umbau pruefen"
    sev: high
    tags: ["mitschnitt", "spotify", "e66", "messen", "pattern", 2026]
    source: >
      E66 Vorbereitung, 20.08.2026: tools/box/zweitzugang-probe.py, 399
      Zeilen, gebaut BEVOR am Mitschnitt eine Zeile geaendert wurde. Der
      zweite Schluessel gehoert nach .spotify.soloistApiKeyMitschnitt und
      steht bewusst NICHT im Repo.
    body: >
      DIE ANNAHME, AN DER ALLES HAENGT: Spotify spielt je Konto nur EINEN
      Strom. Ist sie falsch, faellt der ganze Plan „eigener Zugang fuer den
      Mitschnitt" in sich zusammen — und zwar erst, nachdem gebaut wurde.
      Also beantwortet ein Werkzeug vorher drei Fragen und nichts sonst:
      spielt die Familie ungestoert weiter (F1), entsteht ein
      unterscheidbarer Knoten (F2), beendet sich der Prozess von selbst (F3).

      DREI DINGE ABSICHTLICH SO GEBAUT — sie sind die eigentliche Lehre:

        * F1 DARF „OFFEN" AUSGEBEN. Laeuft die Familie nicht auf Soloist, ist
          ihr Zustand ueber `ctl` gar nicht lesbar. Ein leeres Ergebnis saehe
          aus wie „spielt nicht" und waere ein stiller Falschbefund an genau
          der Stelle, die zaehlt.
        * GRUEN IST NICHT GENUG. Sitzen F2 und F3, ist F1 aber offen, meldet
          das Werkzeug „HALB" und verlangt den Lauf mit hoerbarer Musik. Ein
          Beleg fuer Parallelbetrieb OHNE laufende Wiedergabe ist keiner.
        * DER SCHAEDLICHE FALL BRICHT AB, statt zu warnen: derselbe Zugang
          fuer Familie und Mitschnitt waehrend der Wiedergabe naehme der
          Familie die Musik weg. Das ist Schaden am laufenden Betrieb, kein
          Messfehler — eine Warnung waere hier zu wenig.

      ZWEI FALLEN, DIE DIE MESSUNG SCHON EINBAUT: Beide Soloist-Knoten heissen
      `spotify` ([[soloist-erste-messung]]), deshalb wird ueber
      application.process.id gegen die EIGENE PID unterschieden — ueber den
      Namen griffe die Probe womoeglich den Familienstrom ab und saehe dabei
      aus, als arbeite sie. Und ohne `-d` auf eine Leersenke hoerte die
      Familie den Mitschnitt mit, also genau die Form, die in E63 als Echo
      gemeldet ist.

      Zahl und Etikett stehen in GETRENNTEN Feldern, nie in einem String, der
      danach zerlegt wird —
      [[messwerkzeug-baut-sich-den-befund-selbst]].
    related: ["soloist-erste-messung", "messwerkzeug-baut-sich-den-befund-selbst", "selbstbeendender-prozess-statt-schwellenwert", "messwerkzeug-muss-sich-selbst-pruefen"]

  - id: verweis-zeigt-auf-den-plattennamen
    kind: entscheidung
    title: "Ein Verweis auf die Platte traegt den ORDNERNAMEN, nicht den Anzeigenamen"
    match: "interpret verknuepfen aufnahme|verweis kennung ordner|saeubern albumOrdner kennung|anzeigename statt ordnername"
    sev: high
    tags: ["interpreten", "mitschnitt", "e64b", "entscheidung", 2026]
    source: >
      E64b Block 3a, 20.08.2026, plugins/mixpi-mitschnitt/index.mjs:
      `kennungAnfragen()` setzt `verweis.kennung = saeubern(name)`.
    body: >
      Der Mitschnitt ist der erste Verbraucher der box-eigenen
      Interpretenkennung ([[interpretenkennung-gehoert-der-box]]): nach dem
      Anlegen einer Kachel meldet er den Interpreten an und setzt einen
      Verweis `dienst: aufnahme`.

      WAS IN DEN VERWEIS GEHOERT: Der Verweis soll sagen, WORAN dieser
      Interpret auf der PLATTE wiederzuerkennen ist. Das ist `saeubern(name)`
      — derselbe Wert, den `albumOrdner` in den Pfad schreibt und unter dem
      der Abspieldienst sucht. Er kann vom Spotify-Namen abweichen
      (Satzzeichen, Emoji), und GENAU DIESE ABWEICHUNG ist der Grund, aus dem
      es die Kennung ueberhaupt gibt. Wer den huebschen Anzeigenamen
      hineinschreibt, legt die Abweichung wieder zu — und die Kennung erklaert
      nichts mehr.

      NICHT DER ALBUMORDNER: Eine Kennung gehoert einem INTERPRETEN, nicht
      einem Werk.

      OHNE KATEGORIE: Derselbe Interpret kann unter `music/` und unter
      `audiobook/` liegen. Die Kategorie gehoert zum Werk, nicht zur Person —
      eine Kennung, die `music/` mittraegt, spaltet einen Menschen in zwei.

      AUCH BEI 409 ANMELDEN: Ein 409 auf die Kachel heisst, ein frueherer Lauf
      hat sie angelegt — die Kennung kann juenger sein als die Kachel. Der
      Fall „gibt es schon" ist deshalb KEIN Grund, die Verknuepfung zu
      ueberspringen.

      OFFEN (Block 3b/3c): Verschmelzung und Meta-Interpret, die
      Verwaltungsseite zum Zusammenlegen, und die Spotify-Haelfte des
      Verweises — die muss aus den Freischaltungen im Server kommen statt vom
      Plugin geraten zu werden.
    related: ["interpretenkennung-gehoert-der-box", "maschine-nimmt-nie-die-menschen-stufe", "namen-liste-string-erfindet-interpreten"]

  - id: maschine-nimmt-nie-die-menschen-stufe
    kind: falle
    title: "Wer automatisch verknuepft, nimmt NIE die Stufe, die Zusammenlegen erlaubt"
    match: "stufe hand erkannt automatisch|interpreten zusammenlegen maschine|herkunftsstufe plugin|automatisch verschmolzen"
    sev: high
    tags: ["interpreten", "e64b", "e49", "sicherheit", "falle", 2026]
    source: >
      E64b Block 3a, 20.08.2026: der Mitschnitt setzt `stufe: 'erkannt'`;
      gegengeprobt durch Drehen auf `hand` — genau der eine Test wurde rot.
    symptom: >
      Zwei verschiedene Interpreten stehen ploetzlich als einer da, und
      niemand hat es angeordnet. Rueckgaengig zu machen ist es nur von Hand
      und nur, wenn jemand merkt, dass es passiert ist.
    cause: >
      Die Herkunftsstufe sieht wie eine Formsache aus, ist aber eine
      Berechtigung: bei `hand` darf der Server zwei Kennungen ZUSAMMENLEGEN
      (Regel aus E49, festgehalten in interpretenkennung.ts), bei `erkannt`
      legt er hoechstens eine neue an oder nennt die vorhandene. Ein
      automatischer Verbraucher, der die bequemste Stufe waehlt, verschafft
      sich damit unbemerkt das Recht, Menschen zu verschmelzen.
    fix: >
      In jedem automatischen Aufrufer `erkannt` fest verdrahten und dafuer
      einen Zeugen halten, der beim Drehen auf `hand` rot wird. `hand` bleibt
      der Oberflaeche vorbehalten, an der ein Mensch sitzt.

      DIE GRENZE VERLAEUFT NICHT ZWISCHEN INNEN UND AUSSEN, sondern zwischen
      Mensch und Maschine: Auch Code der eigenen Box ist eine Maschine. Die
      Serverseite haelt dieselbe Linie, indem sie ohne ausdrueckliche Stufe
      mit 400 abweist statt etwas vorzubelegen
      ([[herkunftsstufe-nie-vorbelegen]]).
    related: ["herkunftsstufe-nie-vorbelegen", "interpretenkennung-gehoert-der-box", "verweis-zeigt-auf-den-plattennamen"]

  - id: entscheidung-aus-dem-http-aufruf-herausloesen
    kind: pattern
    title: "Eine Entscheidung, die in einem HTTP-Aufruf steckt, ist nur an einer laufenden Box pruefbar"
    match: "logik in fetch aufruf testen|reine funktion herausziehen|nur an laufender box pruefbar|entscheidung im request body"
    sev: medium
    tags: ["testbarkeit", "e64b", "pattern", "plugin", 2026]
    source: >
      E64b Block 3a, 20.08.2026: `kennungAnfragen()` in
      plugins/mixpi-mitschnitt/index.mjs — exportiert, rein, von vier Zeugen
      gefahren; der Netzweg bleibt in `kennungVerknuepfen()`.
    body: >
      DIE FORM, DIE SICH ANBIETET: Man baut den Rumpf der Anfrage direkt im
      Aufruf zusammen — Name, Stufe, Verweis, alles im selben `await`. Das ist
      kuerzer und liest sich fluessig.

      WAS MAN DAMIT VERLIERT: Die eigentliche Entscheidung (welche Stufe,
      welcher Verweis, welcher Name) ist dann nur noch zu pruefen, wenn eine
      Box laeuft, ein Server antwortet und ein Mitschnitt tatsaechlich
      stattfindet — also genau dort NICHT, wo man sie braucht: beim Schreiben,
      im Zeugen, in der Gegenprobe.

      DIE TRENNUNG: Eine reine, exportierte Funktion liefert den Rumpf; der
      Aufrufer verschickt ihn nur noch. Die Zeugen fahren die Funktion, nicht
      das Netz.

      DAS ERKENNUNGSZEICHEN: Wenn ein Zeuge einen HTTP-Zwilling bauen muesste,
      um eine Ja/Nein-Entscheidung zu pruefen, steckt die Entscheidung an der
      falschen Stelle. Der Aufwand des Zwillings ist nicht das Problem — die
      Gegenprobe ist es: sie muss die Entscheidung drehen koennen, ohne die
      Box zu starten.
    related: ["maschine-nimmt-nie-die-menschen-stufe", "gruene-pruefung-sieht-keine-gezeichnete-klasse"]

  - id: zusatznutzen-darf-den-erfolg-nicht-mitreissen
    kind: entscheidung
    title: "Was NACHTRAEGLICH verknuepft, darf leise scheitern — aber nie stumm"
    match: "verknuepfung fehlgeschlagen aufnahme verwerfen|nebenwirkung reisst hauptweg mit|leise scheitern melden"
    sev: medium
    tags: ["mitschnitt", "e64b", "fehlerbehandlung", "entscheidung", 2026]
    source: >
      E64b Block 3a, 20.08.2026: `kennungVerknuepfen()` faengt jeden Fehler
      und schreibt ins Protokoll, statt den Mitschnitt scheitern zu lassen.
    body: >
      DIE FRAGE bei jedem nachtraeglichen Schritt: Was ist schlimmer — der
      fehlende Zusatz, oder ein Hauptweg, der daran zerbricht? Beim Mitschnitt
      ist die Antwort eindeutig: die Kachel steht, die Datei liegt, die
      Kennung laesst sich jederzeit nachtragen. Eine geglueckte Aufnahme daran
      scheitern zu lassen, ob eine Verknuepfung ankam, waere ein
      Rueckschritt.

      DIE ANDERE HAELFTE, DIE MAN GERN VERGISST: „leise" heisst NICHT „stumm".
      Wird nichts gemeldet, faellt ein DAUERHAFT kaputter Weg niemandem auf —
      und dann fehlen die Kennungen aller Aufnahmen eines halben Jahres, ohne
      dass es je einen Hinweis gab. Jeder geschluckte Fehler bekommt eine
      Zeile im Protokoll, auch der Fall „Antwort ohne id".

      DIE PROBE: Kann man den Zusatzschritt dauerhaft kaputt machen, ohne dass
      irgendwo etwas sichtbar wird? Dann ist er zu leise.
    related: ["verweis-zeigt-auf-den-plattennamen", "interpretenkennung-gehoert-der-box"]

  - id: springende-tastermeldung-macht-jede-frist-unerreichbar
    kind: falle
    title: "„Die eingestellte Haltedauer hat keinen Effekt“ — die Meldung springt, der Zaehler faengt von vorn an"
    match: "pressDelay ohne wirkung|Button released after|haltedauer keine wirkung|immer gleich lang herunterfahren|off_trigger bricht ab"
    sev: high
    tags: ["taster", "herunterfahren", "e70", "mupihat", "falle", 2026]
    source: >
      E70, 20.08.2026 (OFFEN): Konfiguration 5 s, Protokoll „Press delay set
      to 5 seconds", beim Halten von 3–4 s dann „Button released after 1766
      ms, aborting shutdown".
    symptom: >
      Egal welche Haltedauer eingestellt wird — es dauert immer gleich lang,
      bis die Box aus ist. Die Einstellung scheint wirkungslos.
    cause: >
      Der Wert KOMMT AN und wird bei jedem Druck neu gelesen; das ist
      gemessen, daran liegt es nicht. Die Kette ist
      `Knopf -> GPIO17 -> taster_wache.py -> /run/mupibox/taster.zustand ->
      off_trigger.sh`, und `off_trigger.sh` bricht beim ERSTEN „losgelassen"
      ab. Springt die Wache waehrend des Haltens auch nur kurz auf
      „losgelassen", ist JEDE Frist oberhalb dieser Strecke unerreichbar —
      und die Box faehrt nur noch ueber den 6-SEKUNDEN-RIEGEL DES MuPiHAT
      herunter, der von der Einstellung nichts weiss. Daher die immer gleiche
      Dauer.

      WO es verlorengeht (Mechanik, GPIO, Wache, Auswertung), ist Stand
      20.08.2026 NICHT belegt.
    fix: >
      Die Kette an der Zustandsdatei aufschneiden und 100-mal je Sekunde
      abtasten (`tools/box/taster-abtasten.py`, dafuer geschrieben). Die Zahl,
      auf die es ankommt, ist die LAENGSTE ZUSAMMENHAENGENDE Strecke
      „gedrueckt" — mehr als eine Strecke heisst: die Meldung springt.

      NICHT VERWECHSELN mit [[mupi-haltedauer-bruchzahl]] (Bruchzahl in der
      Einstellung). Hier ist der Wert richtig und wirkt trotzdem nicht.

      ZWEI NEBENBEFUNDE: `tools/knopflicht-probe.py` Teil 3 prueft genau diese
      Haltedauer und stuerzt seit dem Umbau von `off_trigger.sh` mit
      `ValueError` ab (sie sucht eine `case`-Zeile, die es nicht mehr gibt) —
      dass die Haltedauer nicht wirkt und ihr Pruefwerkzeug blind ist, ist
      vermutlich kein Zufall. Und `mupi_taster.service` behauptet in der
      Beschreibung „3 s halten", waehrend 5 eingestellt sind
      ([[fest-eingebaute-zahl-in-der-beschreibung-luegt]]).
    related: ["mupi-haltedauer-bruchzahl", "knopf-ohne-rueckmeldung-heisst-raten", "fest-eingebaute-zahl-in-der-beschreibung-luegt", "messung-mit-mensch-als-sensor-gehoert-ihm"]

  - id: messung-mit-mensch-als-sensor-gehoert-ihm
    kind: pattern
    title: "Ist der Mensch der Sensor, muss er die Messung ausloesen — nicht die Ferne"
    match: "jetzt druecken aufforderung fern|messung ins leere gelaufen|betreiber muss selbst starten messung"
    sev: medium
    tags: ["messen", "e70", "taster", "pattern", 2026]
    source: >
      E70, 20.08.2026: `taster-abtasten.py` aus der Ferne gestartet — die
      Aufforderung „jetzt druecken" erreichte den Betreiber erst, als die
      Messung vorbei war. Zwoelf Sekunden ins Leere gemessen.
    body: >
      Ein Werkzeug, das eine menschliche Handlung misst, hat einen
      Startzeitpunkt und ein Zeitfenster. Wird es aus der Ferne gestartet,
      liegt beides beim Falschen: die Ausgabe erscheint gepuffert, die
      Aufforderung kommt zu spaet, und das Fenster laeuft leer ab. Das
      Ergebnis ist nicht etwa leer — es sieht aus wie ein BEFUND („nie
      gedrueckt"), und der ist falsch.

      DIE REGEL: Wo der Mensch der Sensor ist, gehoert der Start ihm. Das
      Werkzeug wird ihm als Befehl in die Hand gegeben; aus der Ferne wird
      hoechstens vorbereitet.

      DIE ZWEITE HAELFTE: Ein solches Werkzeug muss den Fall „in diesem
      Fenster ist nichts passiert" ausdruecklich als OFFEN melden koennen und
      darf ihn nicht als Messwert ausgeben.
    related: ["einmal-hinsehen-ist-keine-messung", "springende-tastermeldung-macht-jede-frist-unerreichbar", "annahme-vor-dem-bauen-messen-nicht-glauben"]

  - id: knopf-ohne-rueckmeldung-heisst-raten
    kind: entscheidung
    title: "Ein Knopf, den man halten muss, ohne Rueckmeldung — und beide Angaben waren ehrlich"
    match: "visuelle rueckmeldung knopf halten|fortschritt herunterfahren anzeigen|kind haelt knopf nichts passiert"
    sev: high
    tags: ["bedienung", "taster", "e69", "kinder", "entscheidung", 2026]
    source: >
      E69, 20.08.2026 (OFFEN), Betreiber waehrend der E70-Untersuchung: „ich
      habe ueberlegt ob wir eine visuelle rueckmeldung ueber den screen geben
      sollen … dann waere es eindeutig".
    body: >
      DER BELEG STECKT IN DER UNTERSUCHUNG SELBST: Der Betreiber hielt nach
      eigener Wahrnehmung 3–4 Sekunden, das Protokoll notierte 1766 ms. BEIDE
      ANGABEN WAREN EHRLICH, und keine liess sich gegen die andere pruefen.
      Einen Knopf ohne Rueckmeldung zu halten heisst raten, ob ueberhaupt
      etwas ankommt.

      FUER EIN KIND SCHAERFER: Es haelt, nichts passiert, es laesst los — und
      hat keine Moeglichkeit zu lernen, dass es FAST geklappt haette.

      DREI DINGE BESTIMMEN DEN ENTWURF:
        * Die Anzeige muss die ECHTE Frist zeigen, nicht eine fest eingebaute
          Zahl ([[fest-eingebaute-zahl-in-der-beschreibung-luegt]]).
        * Der 6-Sekunden-Riegel des MuPiHAT gehoert sichtbar dazu. Er nimmt
          den Strom hart weg, unabhaengig von jeder Software; eine Anzeige,
          die bei 5 s „gleich geschafft" verspricht und bei 6 s vom
          Hardware-Aus ueberholt wird, erklaert weniger, als sie verspricht.
        * Der Weg zum Schirm darf NICHT am Kiosk haengen, der gerade
          abgeschaltet werden soll — ein Browser, der Teil des
          Heruntergefahrenen ist, ist der falsche Bote fuer „ich fahre gleich
          herunter".

      NEBENNUTZEN, UND ER IST DER EIGENTLICHE PUNKT: Mit sichtbarer
      Rueckmeldung haette sich E70 in einem EINZIGEN Druck gezeigt — der
      Balken waere zurueckgesprungen, sobald die Wache faelschlich
      „losgelassen" meldet. Eine Anzeige, die den echten Zustand zeigt,
      ersetzt eine Messreihe.
    related: ["springende-tastermeldung-macht-jede-frist-unerreichbar", "fest-eingebaute-zahl-in-der-beschreibung-luegt", "mupi-haltedauer-bruchzahl"]

  - id: fest-eingebaute-zahl-in-der-beschreibung-luegt
    kind: falle
    title: "Die Beschreibung sagt „3 s halten“, eingestellt sind 5 — eine abgeschriebene Zahl veraltet lautlos"
    match: "3 s halten beschreibung|mupi_taster.service description|zahl in beschreibung stimmt nicht|hinweistext feste sekunden"
    sev: medium
    tags: ["taster", "e69", "e70", "dokumentation", "falle", 2026]
    source: >
      E70, 20.08.2026: `mupi_taster.service` traegt in der Beschreibung „3 s
      halten = sauber herunterfahren", waehrend `timeout.pressDelay` auf 5
      steht.
    symptom: >
      Ein Hinweistext, eine Dienstbeschreibung oder eine Anzeige nennt eine
      Sekundenzahl, die mit der eingestellten nicht uebereinstimmt. Wer sich
      darauf verlaesst, laesst zu frueh los und haelt die Bedienung fuer
      kaputt.
    cause: >
      Die Zahl wurde beim Schreiben abgeschrieben, statt aus derselben Quelle
      gelesen zu werden wie das Verhalten. Ab dann veraltet sie lautlos: die
      Einstellung aendert sich, der Text nicht, und niemand bemerkt einen
      Widerspruch, weil beide Seiten fuer sich plausibel sind.
    fix: >
      Jede Zahl, die ein Nutzer als Zusage liest, kommt aus der laufenden
      Konfiguration oder wird gar nicht genannt. Wo das nicht geht (statische
      Dienstbeschreibung), gehoert die Zahl heraus und der Text auf
      „eingestellte Haltedauer".

      DIESELBE FALLE VORAB VERMEIDEN: Eine geplante Anzeige, die die Frist
      SICHTBAR macht ([[knopf-ohne-rueckmeldung-heisst-raten]]), waere mit
      eingebauter Zahl nur die groessere Version desselben Fehlers — dann
      luegt sie in Balkenform.
    related: ["knopf-ohne-rueckmeldung-heisst-raten", "springende-tastermeldung-macht-jede-frist-unerreichbar"]

  - id: vierzehn-kopien-und-die-abweichung-ist-der-fehler
    kind: falle
    title: "Nicht die Wiederholung ist der Schaden, sondern dass die Kopien VERSCHIEDEN sind"
    match: "halb geschriebene json|tmp datei bleibt liegen|zwei anfragen gleichzeitig schreiben|datei mitten im speichern kaputt"
    sev: high
    tags: ["doppelung", "atomar", "wettlauf", "server", "c1", 2026]
    source: >
      Kritiker-Lauf 20.08.2026: in server.ts standen VIERZEHN Stellen mit
      „daneben schreiben, dann umbenennen", davon drei zeichengleich — und alle
      drei trugen denselben Kommentar „wie bei darstellung.json".
    body: >
      DER NAHELIEGENDE BEFUND WAERE „dreimal dasselbe, zusammenlegen". Der
      teure Befund ist ein anderer: Die vierzehn Stellen waren NICHT gleich.
      Mal mit fortlaufendem Zaehler im Namen der Zwischendatei, mal ohne, mal
      mit `mode`, mal ohne. Und JEDE Abweichung ist eine eigene Wettlaufstelle,
      die niemand einzeln geprueft hat.

      ZWEI STELLEN SCHRIEBEN NACH `${ziel}.${process.pid}.tmp` — ohne Zaehler.
      Zwei gleichzeitige Anfragen teilen sich damit DIESELBE Zwischendatei und
      schreiben sich gegenseitig mitten hinein. Der Kommentar bei einer der
      anderen Stellen hielt genau das schon fest, am Geraet gemessen; er stand
      nur nicht dort, wo der Fehler noch war.

      DIE LEHRE FUER DAS SUCHEN: Wenn dieselbe Idee an vielen Stellen steht,
      ist die Frage nicht „wie oft", sondern „WORIN unterscheiden sie sich".
      Die Kopien, die gleich sind, kosten Platz. Die Kopien, die fast gleich
      sind, kosten Daten.

      DIE LEHRE FUER DAS ZUSAMMENLEGEN: Eine Naht, die nur die drei
      zeichengleichen einsammelt, ist keine halbe Loesung, sondern die richtige
      Reihenfolge — die Abweichler gehoeren EINZELN darauf gezogen und einzeln
      gemessen. Wer alle vierzehn in einem Zug umstellt, weiss hinterher nicht,
      welche Umstellung was bewirkt hat.

      UND DER TEST, DER SICH LOHNT, ist nicht „schreibt die Datei", sondern
      „vierzig gleichzeitige Schreibvorgaenge auf dieselbe Datei" — mit einer
      Nutzlast, die gross genug ist, dass ein Schreibvorgang NICHT in einem Zug
      durchgeht. Gegengeprobt: den Zaehler aus dem Namen genommen, und genau
      dieser Test faellt.
    related: ["messwerkzeug-muss-sich-selbst-pruefen"]

  - id: doppelung-lassen-auseinanderlaufen-messen
    kind: pattern
    title: "Die Doppelung stehen lassen — und das AUSEINANDERLAUFEN messen"
    match: "gemeinsame funktion herausziehen|kiosk-laeuft.sh|doppelter code zusammenlegen|wer kennt beide browsernamen"
    sev: medium
    tags: ["kiosk", "doppelung", "werkzeug", "c2", "pattern", 2026]
    source: >
      Kritiker-Lauf C2, 20.08.2026: `tools/kiosk-browser-stellen.py` sucht den
      GANZEN Baum nach Prozessblicken auf den Kiosk-Browser ab und meldet
      jede Stelle, die nur EINEN der beiden Namen (chromium, cog) kennt.
    body: >
      DER NAHELIEGENDE VORSCHLAG war eine gemeinsame `kiosk-laeuft.sh`, von
      allen aufgerufen. Genau das lehnt dieses Projekt an anderer Stelle mit
      Begruendung ab (Kommentar ueber `schirm_zustand` in
      `mupi_start_led.sh`): eine gemeinsame Datei muss vom Update zuverlaessig
      mitkommen, und faellt sie aus, starten BEIDE Dienste nicht mehr. Auf
      einer Box, die niemand wieder einschalten kann, ist die doppelte
      Funktion das kleinere Uebel.

      DIE ANTWORT IST DESHALB NICHT „aufloesen", SONDERN „messen": die
      Doppelung bleibt, ein Werkzeug prueft, ob die Fassungen noch dasselbe
      sagen. Dasselbe Muster steht schon hinter `tools/schirm-sandkasten.py`.

      DER ENTSCHEIDENDE ZUSCHNITT: ueber den GANZEN Baum suchen, nicht ueber
      eine Liste bekannter Stellen — sonst findet das Werkzeug nur, was
      ohnehin schon jemand wusste. E68 hatte drei Stellen gefunden, eine
      vierte fiel durch Zufall auf. Das Werkzeug fand VIER WEITERE, die weder
      E68 noch der Kritiker-Lauf genannt hatten: `mupi-check.py` meldete auf
      dieser Cog-Box „Browser laeuft nicht" (die Diagnose log), `strom-messen.py`
      mass den Browserverbrauch als null, `kiosk-benchmark.py` toetete beim
      Wechsel nur `cog`.

      ES MELDET, ES VERBIETET NICHT. Nicht jede Stelle MUSS beide Namen
      kennen; wer bewusst einen fuehrt, setzt die Marke
      `# kiosk-browser: nur chromium, weil …` darueber.

      ZWEI EIGENFEHLER BEIM BAUEN, beide durch Nachsehen statt Abhaken
      gefunden: der erste Entwurf meldete fuenf Treffer im EIGENEN Docstring
      von `led_control.py` (Python-Docstrings beginnen nicht mit `#`), und
      eine Ersetzung setzte mitten in einer Zeile an, weil der Suchtext mit
      vier statt acht Leerzeichen eingerueckt war — eine Teilzeichenkette der
      echten Zeile. ERSETZUNGEN GEHOEREN AN DEN ZEILENANFANG VERANKERT.

      NOCH NICHT AN `pruefen.sh` GEHAENGT, und zwar mit Absicht: ein Schritt,
      der mit zwei bekannten Befunden rot startet, waere entweder dauerhaft
      rot oder muesste sie verschweigen — beides schlechter als kein Schritt
      ([[pruefen-schritte-die-still-luegen]]).
    related: ["kiosk-browser-was-er-wirklich-koennen-muss", "vierzehn-kopien-und-die-abweichung-ist-der-fehler", "pruefen-schritte-die-still-luegen", "alter-prozessname-macht-den-schub-wirkungslos"]

  - id: alter-prozessname-macht-den-schub-wirkungslos
    kind: falle
    title: "`renice` auf `chromium-browser` — den Prozess gibt es nicht mehr, der Schub lief ins Leere"
    match: "renice chromium-browser|pgrep chromium-browser leer|prioritaet wirkt nicht browser|alter paketname prozessname"
    sev: high
    tags: ["kiosk", "chromium", "prioritaet", "c2", "falle", 2026]
    source: >
      Kritiker-Lauf C2, 20.08.2026: `chromium-autostart.sh:291,300` ruft
      `renice -n -10` auf `chromium-browser` — den Namen fuehrt diese Box seit
      dem 07.08.2026 nicht mehr.
    symptom: >
      Eine Massnahme, die auf einen Prozessnamen zielt (renice, pkill, pgrep,
      Speichermessung), meldet keinen Fehler und bewirkt nichts. Das Skript
      laeuft durch, das Protokoll ist still, die Wirkung fehlt.
    cause: >
      Der Prozessname hat sich mit dem Paket geaendert (`chromium-browser` ->
      `chromium`), oder der Browser ist ueberhaupt ein anderer (`cog`). Ein
      Namensblick auf einen Prozess, den es nicht gibt, ist kein Fehlerfall —
      er liefert leer, und leer sieht aus wie „nichts zu tun".
    fix: >
      Jede Stelle, die auf den Kiosk-Browser blickt, muss BEIDE heutigen Namen
      kennen; `tools/kiosk-browser-stellen.py` findet sie
      ([[doppelung-lassen-auseinanderlaufen-messen]]).

      `chromium-autostart.sh` ist Stand 20.08.2026 ABSICHTLICH NICHT
      umgestellt: daran haengt der Bildschirm, ein Fehler dort heisst
      schwarzer Schirm. Es braucht einen eigenen, am Geraet gemessenen
      Durchgang und steht bis dahin als offener Befund im Werkzeug.

      DIE ALLGEMEINE FORM: Wer auf einen Namen zielt, prueft, ob er getroffen
      hat. `pgrep` ohne Treffer und „Massnahme wirkt" sind von aussen nicht zu
      unterscheiden.
    related: ["doppelung-lassen-auseinanderlaufen-messen", "kiosk-browser-was-er-wirklich-koennen-muss"]

  - id: warteliste-laeuft-muss-sich-erholen
    kind: pattern
    title: "Ein Zustand „laeuft“ ueberlebt den Stromausfall — und blockiert dann fuer immer"
    match: "eintrag steht auf laeuft|warteliste haengt|status in bearbeitung nach neustart|liste geht nicht weiter"
    sev: high
    tags: ["mitschnitt", "e66", "warteliste", "zustand", "pattern", 2026]
    source: >
      E66, 20.08.2026 im Kern gebaut als `mitschnittliste.ts`; NOCH AM SELBEN
      TAG mit E73 (Commit b8abd047) ins Plugin gezogen. Der Ort ist heute
      `plugins/mixpi-mitschnitt/liste.mjs` (Grenze `HOECHSTENS_VERSUCHE`
      dort und in `auftrag.mjs`) — die Liste der Titel, die noch
      mitgeschnitten werden sollen. Berichtigt am 23.08.2026 durch
      `tools/doku-pfade-pruefen.py`: der alte Pfad stand hier drei Tage und
      zeigte ins Leere. Die drei Regeln unten gelten unveraendert, sie haengen
      nicht am Ort.
    body: >
      DREI REGELN, DIE SICH ERST BEIM DRITTEN NACHDENKEN ZEIGEN — sie gelten
      fuer JEDE Warteliste, die einen Neustart ueberlebt:

      1. `laeuft` MUSS SICH BEIM LADEN ERHOLEN. Faellt der Strom mitten in
      einer Aufnahme, bleibt ein Eintrag auf `laeuft` stehen und blockiert
      dann fuer immer, weil niemand ihn je abschliesst. Beim Laden wird
      `laeuft` deshalb wieder zu `offen`. Eine Aufnahme, die zweimal beginnt,
      kostet Zeit; eine Liste, die stillsteht, kostet alles Weitere.

      2. EIN FEHLER DARF NICHT EWIG WIEDERKEHREN. Ein Titel, den der Dienst
      nicht mehr hergibt, haelt die Liste sonst bei jedem Durchgang neu auf.
      Nach HOECHSTENS_VERSUCHE (3) bleibt er auf `fehler` liegen — sichtbar,
      mit Grund, aber nicht mehr im Weg. WEGGEWORFEN WIRD ER NICHT: ein
      Eintrag, der verschwindet, sieht aus wie einer, den es nie gab.

      3. DIE HERKUNFT STEHT DRAN: `automatisch` (die Familie hat es
      angespielt) oder `hand` (jemand hat es nachgelegt). Wer spaeter
      aufraeumt, muss „das wollte jemand" von „das ist nebenbei
      hereingelaufen" unterscheiden koennen — dieselbe Ueberlegung wie `stufe`
      in `interpretenkennung.ts`.

      WARUM DIE LISTE UEBERHAUPT: Der Mitschnitt ist heute ein PASSIVER
      ABGRIFF am Knoten des Familien-Abspielers — wer den Titel wechselt,
      schneidet die Aufnahme ab (E63/E64). Mit der Liste wird VORGEMERKT statt
      live abgegriffen und spaeter vollstaendig nachgeschnitten. Das
      Abschneiden verschwindet damit als PROBLEMKLASSE, statt gemildert zu
      werden.

      UND SIE HAENGT AN NICHTS: Die Liste weiss nur, WAS ansteht und WIE ES
      AUSGING — nicht, wie aufgenommen wird. Sie funktioniert deshalb auch,
      solange es nur einen Schluessel gibt. Ihre Ablage gehoert ins
      Sicherungsnetz (Lehre aus E65: eine Datei, die Entscheidungen der Box
      traegt und nirgends sonst steht, ist beim naechsten Kartenschaden weg).
    related: ["interpretenkennung-gehoert-der-box", "zusatznutzen-darf-den-erfolg-nicht-mitreissen", "stroeme-duerfen-sich-keinen-zugang-teilen"]

  - id: geraetename-geht-an-den-dienst
    kind: entscheidung
    title: "Der Geraetename steht nicht nur im Haushalt — er geht an Spotify"
    match: "connect geraet name mitschnitt|soloistNameMitschnitt|geraetename verraet zweck|Stream 1 Stream 2 benennen"
    sev: high
    tags: ["mitschnitt", "e66", "spotify", "benennung", "entscheidung", 2026]
    source: >
      E66, 20.08.2026, Betreiber: das zweite Geraet heisst NICHT „Mitschnitt".
      Schema stattdessen `<Boxname> Stream <n>` („MixPiBox_1 Stream 1 und
      MixPiBox_1 Stream 2 — dann kann es mehrere geben").
    body: >
      ZWEI GRUENDE, EINER DAVON NICHT OFFENSICHTLICH:

      DISKRETION: Connect-Geraete tauchen in der Geraeteliste des KONTOS auf.
      Ein Geraet namens „Mitschnitt" sagt dem Dienst woertlich, was es tut —
      und der Warntext des Plugins nennt genau das als Risiko („es kann zum
      Verlust des Zugangs kommen"). Der Name soll aussehen wie ein
      gewoehnlicher zweiter Lautsprecher.

      STRUKTUR (der bessere Grund, nachgereicht): `<Boxname> Stream <n>`
      traegt zwei Erweiterungen, die ein blosses „Box 2" nicht kann —
      MEHRERE STROEME je Box (Stream 1 die Familie, Stream 2 der Mitschnitt,
      ein dritter braucht keinen erfundenen Namen mehr) und MEHRERE BOXEN im
      Haushalt, die ueber den BOXNAMEN hereinkommen: ein zweiter Kasten heisst
      `MixPiBox_2` und bekommt `MixPiBox_2 Stream 1/2` von selbst.

      WARUM DIE BOXNUMMER NICHT IN DEN GERAETENAMEN GEHOERT: `.mupibox.host`
      haengt an 29 Stellen — Systemhostname, Geraete-Kennung, Namen der
      Sicherungsdateien, Boot-Splash, Boxname in BEIDEN Oberflaechen. Ein `_1`
      dort benennt nicht ein Connect-Geraet um, sondern die ganze Box. Wer das
      will, tut es dort; das Schema folgt automatisch.

      DIE HARTE PRUEFUNG: Der Name darf NICHT derselbe sein wie der der
      Familienbox — zwei Connect-Geraete mit gleichem Namen sind in der App
      nicht zu unterscheiden, und dann verbindet sich jemand versehentlich mit
      dem falschen. `zweitzugang-probe.py` bricht in diesem Fall ab.
    related: ["gemeinsamer-platzhalter-macht-ununterscheidbar", "stroeme-duerfen-sich-keinen-zugang-teilen", "soloist-erste-messung"]

  - id: stroeme-duerfen-sich-keinen-zugang-teilen
    kind: falle
    title: "Zwei Stroeme, ein Konto — der zweite nimmt dem ersten die Wiedergabe weg"
    match: "musik hoert mitten im stueck auf|zweiter strom uebernimmt|ein spotify konto zwei geraete|platzhalter als schluessel"
    sev: high
    tags: ["stroeme", "e72", "spotify", "schluessel", "falle", 2026]
    source: >
      E72 (Zwei Kinder, eine Box), 20.08.2026:
      `src/backend-api/src/stroeme.ts` prueft Schluessel auf FORM, nicht auf
      Verschiedenheit.
    symptom: >
      Ein Kind hoert Musik, die mitten im Stueck aufhoert, ohne dass jemand
      etwas getan hat. Auf der anderen Seite der Wohnung hat gerade jemand
      losgespielt.
    cause: >
      Ein Spotify-Konto spielt genau EINEN Strom. Startet ein zweiter mit
      demselben Schluessel, uebernimmt er die Wiedergabe. Zwei Kinder
      gleichzeitig heisst deshalb ZWEI KONTEN — das ist keine Bau-, sondern
      eine Tarifentscheidung (ein Family-Tarif traegt bis zu sechs).

      DIE FALLE STECKT IN DER PRUEFUNG: Am 20.08.2026 stand fuer eine Weile
      der PLATZHALTER `SPAK_HIER_EINSETZEN` als zweiter Schluessel in der
      Konfiguration. Er ist VERSCHIEDEN vom ersten — jede naive Pruefung haette
      „zwei Konten" gemeldet. Erst die LAENGE (19 statt 37) verriet ihn.
    fix: >
      Nicht auf Gleichheit pruefen, sondern auf FORM: `^spak_[A-Za-z0-9]{32}$`.
      Ein Platzhalter faellt durch jede Gleichheitspruefung und durch diese
      nicht.

      UND: `librespot` braucht gar keinen Schluessel (Zeroconf-Anmeldung) —
      ein leeres Feld ist dort kein Mangel. Die Pruefung muss die Tonmaschine
      kennen, sonst meldet sie den Normalfall als Fehler.

      DER UMDREHER DAHINTER: E66 war entworfen als „der Mitschnitt bekommt
      einen eigenen Zugang" — damit waere der zweite Strom ein SONDERFALL und
      jeder weitere ein neuer Sonderfall daneben. In E72 ist er der
      NORMALFALL: die Box fuehrt nummerierte Stroeme, jeder mit eigenem Zugang
      und eigenem Tonziel, der Mitschnitt ist EINER davon. Er darf deshalb
      nicht als „der zweite" gebaut werden.
    related: ["geraetename-geht-an-den-dienst", "warteliste-laeuft-muss-sich-erholen", "zwei-a2dp-stroeme-auf-einem-baustein", "dienst-steckt-im-schluessel"]

  - id: typpraedikat-engt-den-falsch-zweig-ein
    kind: falle
    title: "`x is string` als Rueckgabe engt den Falsch-Zweig auf `never` ein"
    match: "typpraedikat never|is string falsch-zweig|Property length does not exist on type never|type guard schraenkt ein"
    sev: medium
    tags: ["typescript", "e72", "falle", 2026]
    source: >
      E72, 20.08.2026: `istSchluessel()` in `stroeme.ts` war zuerst als
      `x is string` geschrieben — danach liess sich `s.schluessel.length` in
      der Fehlermeldung nicht mehr uebersetzen.
    symptom: >
      Nach dem Einbau eines Typpraedikats meldet der Uebersetzer im
      SONST-Zweig, eine Eigenschaft gebe es auf `never` nicht — obwohl der
      Wert dort offensichtlich noch ein String ist.
    cause: >
      Ein Praedikat wirkt in BEIDE Richtungen. Ist das Feld schon als `string`
      deklariert und das Praedikat sagt ebenfalls `x is string`, bleibt im
      Falsch-Zweig nichts uebrig: `string` minus `string` ist `never`. Das
      Praedikat grenzt nicht ein, es loescht.
    fix: >
      Ein Praedikat gehoert dorthin, wo es wirklich etwas eingrenzt — auf
      `unknown` oder eine echte Vereinigung. Wo nur eine Ja/Nein-Antwort
      gebraucht wird, gibt die Funktion schlicht `boolean` zurueck.
    related: []

  - id: zwei-a2dp-stroeme-auf-einem-baustein
    kind: knowhow
    title: "Zwei Bluetooth-Tonstroeme gleichzeitig: es geht — der USB-Baustein ist ein Hebel, keine Bedingung"
    match: "zwei bluetooth lautsprecher gleichzeitig|zwei a2dp stroeme|zwei kinder eine box|bluez mehrere a2dp"
    sev: medium
    tags: ["bluetooth", "stroeme", "e72", "pipewire", "knowhow", 2026]
    source: >
      E72, 20.08.2026, an der Box: zwei Funkbausteine (`hci1` UART im SoC,
      `hci0` USB-Steckmodul), zwei gepaarte Lautsprecher.
      `tools/box/zwei-stroeme-probe.py` misst es mit Testton ueber Zeit.
    body: >
      WAS TRAEGT: BlueZ haelt mehrere A2DP-Verbindungen, PipeWire fuehrt dann
      zwei `bluez_output`-Senken. Zwei Stroeme auf EINEM Baustein gehen —
      erst falsch notiert, vom Betreiber sofort berichtigt. Rechnerisch passt
      es ohnehin (SBC rund 330 kbit/s je Strom, EDR traegt ueber 2 Mbit/s);
      was kippt, ist die FUNKZEIT.

      WOZU DANN DER USB-BAUSTEIN: Beim Pi teilen sich WLAN und der eingebaute
      Bluetooth denselben Chip — genau die Koexistenz, die im Stotter-Fenster
      von E68 sichtbar wurde. Ein Strom auf dem USB-Baustein nimmt dem WLAN
      NICHTS weg. Er ist damit ein HEBEL FUER DIE GUETE, keine Bedingung.

      EIN EINWAND, DER HIER NICHT GILT: Der uebliche Vorbehalt gegen zwei
      A2DP-Stroeme ist der LAUFZEITVERSATZ zwischen den Geraeten. Der zaehlt
      bei der Kombi-Senke „Ueberall", wo dasselbe Stueck aus zwei
      Lautsprechern kommt. Bei zwei Kindern laeuft VERSCHIEDENER Inhalt — es
      darf driften, so viel es will. Deshalb ist dieser Anwendungsfall
      leichter als der, fuer den Bluetooth-Mehrfachwiedergabe sonst
      kritisiert wird.

      WARUM TESTTON UND NICHT SPOTIFY: Die Frage lautet „traegt der
      Bluetooth-Stapel zwei Stroeme", nicht „traegt Spotify das". Ein selbst
      erzeugter Ton isoliert die Variable: kein Netz, kein Konto, keine
      Paarung, kein zweiter Zugang noetig. Scheitert es MIT Testton, braucht
      man Spotify gar nicht erst zu fragen.

      STAND DER MESSUNG (20.08.2026, VORSICHT): Der Lauf ueber 180 s meldete
      „keine Aussetzer" — der Aussetzer-Zaehler las zu diesem Zeitpunkt
      allerdings NULL Knoten
      ([[pw-top-format-spalte-ist-verschieden-breit]]). Dass beide
      Lautsprecher hoerbar spielten, steht; die Zahl „keine Aussetzer" ist
      NICHT belegt und gehoert mit dem berichtigten Werkzeug nachgemessen —
      samt Vergleichslauf mit EINEM Strom (`--nur-einer`), sonst weiss man
      nicht, ob gezaehlte Aussetzer am zweiten Strom liegen.
    related: ["pw-top-format-spalte-ist-verschieden-breit", "stroeme-duerfen-sich-keinen-zugang-teilen", "einmal-hinsehen-ist-keine-messung"]

  - id: pw-top-format-spalte-ist-verschieden-breit
    kind: falle
    title: "`pw-top`: die FORMAT-Spalte ist mal leer, mal vier Felder breit — nach Feldzahl filtern wirft die Messung weg"
    match: "pw-top spalten auswerten|ERR spalte auslesen|aussetzer keine obwohl|split feldzahl mindestens 11"
    sev: high
    tags: ["pipewire", "messen", "e72", "werkzeug", "falle", 2026]
    source: >
      E72, 20.08.2026: `zwei-stroeme-probe.py` verlangte mindestens 11 Felder
      je Zeile. Am Geraet gezaehlt: 23 Zeilen mit 10 Feldern, zwei mit 11,
      zwei mit 13, eine mit 14.
    symptom: >
      Ein Messwerkzeug meldet ueber mehrere Laeufe hinweg „AUSSETZER: keine".
      Die Laeufe bestaetigen sich gegenseitig — und sind alle leer.
    cause: >
      Die Zeilen von `pw-top -b` sind
      `S ID QUANT RATE WAIT BUSY W/Q B/Q ERR FORMAT NAME`, und FORMAT ist bei
      Knoten ohne ausgehandeltes Format LEER, bei anderen mehrere Felder
      breit. Eine Mindest-Feldzahl warf GENAU DIE ZEILEN WEG, um die es geht.
      Ein Zaehler, der immer null liefert, sieht aus wie ein Ergebnis — das
      ist die schlimmste Sorte Fehler.
    fix: >
      Ueber die ID gehen, nicht ueber den Namen: sie steht immer an Feld 2 und
      ist innerhalb eines Laufs eindeutig; ERR ist Feld 9. Den Namen getrennt
      holen und dabei alles verwerfen, was nach einem FORMAT-Feld aussieht.
      Mindestbedingung auf 9 Felder.

      DIE FRAGE, DIE VOR DEN ERSTEN LAUF GEHOERT: „Kann dieses Werkzeug
      ueberhaupt einen Aussetzer SEHEN?" Hier fiel es erst nach dem zweiten
      Lauf auf. Ein Messwerkzeug wird gegen ein bekanntes Signal geprueft,
      bevor man ihm ein unbekanntes vorlegt
      ([[messinstrument-gegen-bekanntes-signal]]).
    related: ["messwerkzeug-muss-sich-selbst-pruefen", "messinstrument-gegen-bekanntes-signal", "zwei-a2dp-stroeme-auf-einem-baustein", "einmal-hinsehen-ist-keine-messung"]

  - id: spak-schluessel-traegt-das-konto-nicht
    kind: falle
    title: "Zwei verschiedene spak_-Schluessel sind noch lange nicht zwei Konten"
    match: "beide stroeme dasselbe konto|logged in as derselbe nutzer|zweiter zugang nimmt dem ersten die wiedergabe|soloist paart falsches konto"
    sev: high
    tags: ["soloist", "spotify", "connect", "e66", "e72", "falle", 2026]
    source: >
      20.08.2026 an der Box gemessen: zwei verschiedene spak_-Schluessel
      eingetragen, zweimal gepaart — und beide Male meldete sich Soloist mit
      DEMSELBEN Konto an.
    body: >
      DIE VERWECHSLUNG: Der `spak_`-Schluessel ist die Entwickler-Registrierung.
      WELCHES KONTO sich anmeldet, entscheidet er NICHT — das kommt allein vom
      Geraet, das sich per Connect verbindet.

      NACHGEMESSEN, statt aus dem Wiki geschlossen: Soloist mit dem Schluessel
      in einem FRISCHEN, leeren Datenordner gestartet, ohne `-p`. Ergebnis:

          ready, device_name=Schluesseltest
          waiting for login — connect to "Schluesseltest" from your Spotify app

      Es meldet sich nicht an. Der Schluessel traegt kein Konto. (Das Wort
      „kontogebunden" in [[soloist-erste-messung]] meint nur, dass er aus einem
      Konto mit Soloist-Zugang stammen muss.)

      WARUM DAS TEUER IST: Die naheliegende Pruefung „sind die beiden
      Schluessel verschieden?" sagt JA und ist trotzdem wertlos. Am selben Tag
      stand fuer eine Weile der Platzhalter `SPAK_HIER_EINSETZEN` als zweiter
      Schluessel — auch verschieden, auch gruen, und ebenfalls kein zweites
      Konto. Zweimal dieselbe Falle: Verschiedenheit ist nicht Identitaet.

      WAS WIRKLICH ZAEHLT, steht im Protokoll der Instanz und nirgends sonst:

          logged in as 31nncq3ce5ijscgkegeoxf23pssm

      Nicht der Rueckgabewert (Soloist gibt 0 zurueck, auch wenn es eine
      bestehende Sitzung nur wiederherstellt), nicht die Ordnerliste unter
      `settings/Users/` (dort sammeln sich ALLE Konten, die je verbunden
      waren), und nicht die Ordner-Zeitstempel — die fuehrten hier zweimal in
      die Irre, geschrieben wird in den DATEIEN darin.

      DAS VERFAHREN, DAS TRAEGT: jeden Strom EINZELN paaren und danach die
      Zeile `logged in as` lesen. Zwei Stroeme mit verschiedenen Konten laufen
      dann nachweislich gleichzeitig — am 20.08. gemessen, 18 Sekunden
      parallel, ohne dass der erste seine Sitzung verlor.

      UND WENN ES NICHT DAS GEWUENSCHTE KONTO WIRD: Es liegt am verbindenden
      Geraet, nicht am Schluessel. Ein Geraet, das in zwei Konten angemeldet
      ist, gibt nicht zwingend das her, in dem gerade die Oberflaeche steht.
    related: ["soloist-erste-messung"]

  - id: zwei-richtige-takte-eine-falsche-statistik
    kind: falle
    title: "Zwei fuer sich richtige Takte ergeben zusammen eine Statistik, die um Faktor sechs luegt"
    match: "hoerzeit zu kurz|verlauf zaehlt zu wenig|alle eintraege 10 sekunden|statistik plausibel aber falsch"
    sev: high
    tags: ["verlauf", "statistik", "takt", "e74", "falle", 2026]
    source: >
      E74, 20.08.2026: Der Hoerverlauf der Box schrieb zehn Sekunden pro Minute
      Hoerzeit auf. Aufgefallen an der Verteilung — 65 % aller Eintraege lagen
      zwischen 10 und 20 Sekunden, zwischen 60 und 300 Sekunden gar nichts.
    body: >
      DIE ZWEI ENTSCHEIDUNGEN, JEDE FUER SICH RICHTIG UND BEGRUENDET:

        * Die Oberflaeche meldet ihre Abspielstelle alle 60 s statt haeufiger —
          begruendet mit „Die Karte ist das Verschleissteil dieser Box".
        * Die Verlaufsauswertung beendet eine Sitzung nach 30 s ohne Meldung
          und kappt einen einzelnen Schlag bei 10 s — damit ein haengender
          Schlag keine Stunden erfindet.

      ZUSAMMEN: 60 s Abstand ist groesser als 30 s Luecke, also beginnt JEDE
      Meldung eine neue Sitzung, und jede zaehlt gekappt 10 s. Aus einer Stunde
      Hoeren werden zehn Minuten.

      DER EIGENTLICHE FEHLER STAND IM KOMMENTAR. Die Auswertung beschrieb ihren
      Eingang als Takt „alle 2 s". Der Sender war aber nie ein Herzschlag,
      sondern eine „merk dir meine Stelle"-Meldung mit eigenem, viel langsamerem
      Takt — und mit einer Sperre gegen unveraenderte Staende obendrein. Wer
      sich an einen fremden Aufruf haengt, erbt dessen Takt, nicht den, den er
      sich vorstellt.

      WARUM ES NIEMAND SAH: Die Zahlen waren plausibel. Zehn bis zwanzig
      Sekunden je Eintrag sieht aus wie ein Kind, das viel wechselt. Eine
      Statistik faellt nicht auf, wenn sie falsch ist — nur wenn sie UNMOEGLICH
      aussieht. Hier verriet es erst die VERTEILUNG: dass zwischen einer und
      fuenf Minuten NICHTS lag, kann kein Verhalten sein.

      WAS DARAUS FOLGT, ueber diesen Fall hinaus: Wer eine Dauer aus einem
      fremden Takt ableitet, muss die Konstanten GEGENEINANDER pruefen —
      Luecke und Kappung gegen den echten Sendetakt, nicht gegen den
      angenommenen. Und wer sie in verschiedenen Dateien ablegt, schreibt in
      beide, welche andere Zahl dazugehoert.
    related: ["einmal-hinsehen-ist-keine-messung", "messwerkzeug-baut-sich-den-befund-selbst"]

  - id: entwicklerbox-ist-keine-nutzungsdatenquelle
    kind: falle
    title: "Nutzungsdaten der Entwicklerbox sind kein Nutzungsverhalten"
    match: "schwelle aus dem verlauf ableiten|nutzungsdaten auswerten|so benutzen kinder die box"
    sev: med
    tags: ["messung", "daten", "e73", "e74", 2026]
    source: >
      20.08.2026, Betreiber: „diese box ist ja auch hauptsaechlich
      Entwicklerbox, keine Kinderbox bisher — das darf man nicht vergessen."
    body: >
      Beim Entwurf einer Schwelle („ab wann blaettert ein Kind?") lag der
      Griff nahe: die Box schreibt doch mit, wie sie benutzt wird — dann liest
      man es dort ab, statt zu raten. Das war richtig gedacht und trotzdem
      falsch.

      DIE DATEN DIESER BOX SIND ENTWICKLUNGSBETRIEB. Anspielen, abbrechen,
      neu starten, messen, wieder abbrechen. Wer daraus eine Schwelle fuer
      KINDER ableitet, hat die Gewohnheiten des Entwicklers zur Norm gemacht —
      und zwar mit dem guten Gefuehl, gemessen statt geraten zu haben.

      DAS IST TUECKISCHER ALS RATEN. Eine geratene Zahl bleibt als geraten
      erkennbar; eine aus Daten abgeleitete traegt den Anschein von Belegtheit
      und wird nicht wieder hinterfragt.

      DIE FRAGE VOR JEDER AUSWERTUNG lautet deshalb nicht „habe ich Daten",
      sondern „bilden diese Daten den Fall ab, um den es geht". Bei einer Box,
      die noch nicht im Kinderzimmer steht, lautet die Antwort nein — und dann
      ist eine ehrlich als vorlaeufig benannte Annahme besser als eine
      scheinbelegte Zahl.
    related: ["zwei-richtige-takte-eine-falsche-statistik"]

  - id: liste-ist-vorrat-platte-ist-wahrheit
    kind: falle
    title: "Eine Arbeitsliste schuetzt nicht gegen Doppeltes — das tut nur das Dateisystem"
    match: "schon aufgenommen|doppelt aufnehmen|warteliste vorrat|liste verloren"
    sev: high
    tags: ["mitschnitt", "e73", "e66", "plugin", "falle", 2026]
    source: >
      20.08.2026, E73/E66, beim Aufschreiben der Aufnahme-Regel aufgefallen —
      vor dem Bauen, nicht danach. Umgesetzt als abgleichen() in
      plugins/mixpi-mitschnitt/liste.mjs.
    body: >
      DIE VERLOCKUNG: Eine Liste, die mitschreibt, was schon aufgenommen wurde,
      sieht aus wie der Schutz gegen doppeltes Aufnehmen. Sie ist es nicht.

      SIE WEISS NUR, WAS SIE SELBST GETAN HAT. Geht sie verloren —
      Kartenschaden, Neuaufsetzen, oder sie existierte zum Zeitpunkt der ersten
      Aufnahmen schlicht noch nicht —, gilt jeder Titel wieder als offen, und
      die Box nimmt die ganze Bibliothek ein zweites Mal auf.

      DIE ROLLENTEILUNG, die daraus folgt und die allgemein gilt: die Liste ist
      ein ARBEITSVORRAT und sagt, was ansteht. Das Dateisystem ist die WAHRHEIT
      und sagt, was da ist. Vor dem Aufnehmen wird deshalb auf der Platte
      nachgesehen, nicht in der Liste — im Plugin ein stat auf den Pfad, den
      albumOrdner und spurname ohnehin bilden.

      DER GEWINN: eine verlorene Liste kostet dann nichts ausser der
      REIHENFOLGE. Genau die Eigenschaft, die E65 fuer die Interpretenkennung
      erst mit einem Eintrag im Sicherungsnetz erzwingen musste, ist hier
      gratis zu haben.

      DIE PROBE FUER JEDE SOLCHE LISTE lautet: „Was passiert, wenn sie morgen
      leer ist?" Ist die Antwort „es wird alles noch einmal getan", steht der
      Schutz an der falschen Stelle.
    related: ["aufnehmen-ist-investition-nicht-laufende-last", "pruefung-die-wirft-heisst-unbekannt"]

  - id: pruefung-die-wirft-heisst-unbekannt
    kind: falle
    title: 'Eine Pruefung, die wirft, heisst „unbekannt“ — nicht „nein“'
    match: "existiert nicht|stat schlaegt fehl|catch false|pruefung fehler behandeln"
    sev: high
    tags: ["robustheit", "e66", "plugin", "falle", 2026]
    source: >
      20.08.2026, E66/E73, beim Bau von abgleichen() in
      plugins/mixpi-mitschnitt/liste.mjs entschieden.
    body: >
      Der uebliche Griff bei einer Existenzpruefung ist
      `try { stat(p); return true } catch { return false }`. Damit wird JEDER
      Fehler zur Aussage „liegt nicht vor" — auch ein abgezogener Datentraeger,
      ein Rechtefehler oder ein noch nicht eingehaengter Ordner.

      DAS SIND DREI VERSCHIEDENE ANTWORTEN, und nur eine davon ist „nein":
      ja, nein, unbekannt. Wer „unbekannt" zu „nein" macht, handelt auf einer
      Auskunft, die er gar nicht hat.

      WELCHE RICHTUNG SCHADET, HAENGT AM FALL. Beim Platten-Abgleich der
      Mitschnitt-Liste waere „nein" harmlos (dann wird eben aufgenommen), aber
      die umgekehrte Fehldeutung teuer. Deshalb: wirft die Pruefung, bleibt der
      Eintrag stehen, statt stillschweigend als erledigt zu gelten. Ein
      Eintrag, der wiederkommt, kostet einen zweiten Blick; einer, der zu
      Unrecht verschwindet, kostet die Aufnahme.

      DIE REGEL: Bevor ein catch einen Wahrheitswert zurueckgibt, muss
      dastehen, welche der drei Antworten hier die sichere ist — und warum.
    related: ["liste-ist-vorrat-platte-ist-wahrheit", "erledigtes-markieren-nicht-entfernen"]

  - id: erledigtes-markieren-nicht-entfernen
    kind: entscheidung
    title: "Erledigtes wird markiert, nicht entfernt — ein verschwundener Eintrag sieht aus wie einer, den es nie gab"
    match: "eintrag entfernen|aus der liste loeschen|erledigt zustand"
    sev: med
    tags: ["mitschnitt", "e66", "plugin", "nachvollziehbarkeit", 2026]
    source: >
      20.08.2026, E66/E73, als Kommentar im Quelltext festgehalten
      (plugins/mixpi-mitschnitt/liste.mjs, abgleichen()).
    body: >
      Als der Platten-Abgleich Eintraege fand, die schon aufgenommen waren, lag
      das Entfernen nahe — die Arbeit ist getan, der Eintrag ueberfluessig.

      ER IST NICHT UEBERFLUESSIG, SONDERN DER BELEG. Ein Eintrag, der
      verschwindet, ist von einem, den es nie gab, nicht zu unterscheiden. Wer
      spaeter fragt „warum wurde dieser Titel nicht aufgenommen", findet in
      beiden Faellen nichts.

      DESHALB: Zustand `fertig` mit einem Grund („lag schon auf der Platte")
      statt Entfernen. Die Liste kennt vier Zustaende — offen, laeuft, fertig,
      fehler —, und `fertig` sagt „liegt vor", ist also zugleich die Antwort
      auf die Frage, die den Abgleich ueberhaupt ausgeloest hat.

      GILT UEBERALL, wo eine Liste eine Absicht festhaelt: Ein Zustandsfeld ist
      billiger als eine spaetere Suche nach dem, was nicht mehr dasteht.
    related: ["pruefung-die-wirft-heisst-unbekannt", "liste-ist-vorrat-platte-ist-wahrheit"]

  - id: aufnehmen-ist-investition-nicht-laufende-last
    kind: entscheidung
    title: "Lokal Aufnehmen ist eine Investition, keine laufende Last — die Stromrechnung dreht sich um"
    match: "aufnehmen kostet strom|akku aufnahme|lohnt sich mitschnitt"
    sev: med
    tags: ["mitschnitt", "e73", "akku", "energie", 2026]
    source: >
      20.08.2026, Betreiber zu E73: „wenn ueber laengerer sicht viele titel
      lokal sind spart man auch strom, nur zu beginn ist es mehr."
      Akku-Werte am selben Tag am MuPiHAT gemessen: -550 bis -900 mA.
    body: >
      DER EINWAND WAR ZU KURZ GEDACHT: Im Leerlauf aufzunehmen heisst
      Bluetooth aktiv, WLAN aktiv, Entschluesselung laeuft — waehrend niemand
      da ist. Das sieht nach reiner Zusatzlast am Akku aus.

      DIE GEGENRECHNUNG: Ein lokal liegender Titel kostet beim Abspielen KEIN
      WLAN, KEIN Spotify, KEINE Entschluesselung — er kommt von der Karte.
      Aufnehmen ist damit einmal teuer und danach jedes Mal billiger. Ueber die
      Lebensdauer der Box spart es vermutlich Strom.

      TEUER IST ALLEIN DIE ANFANGSZEIT, in der die Bibliothek noch duenn ist.
      Genau dafuer bleibt die Bedingung sinnvoll: im Leerlauf nur am Netz oder
      ueber einer Ladeschwelle aufnehmen. Die Werte liest der MuPiHAT ohnehin.

      UND DIE BEDINGUNG, AN DER DIE RECHNUNG HAENGT: zweimal bezahlt
      amortisiert sich nie — siehe den Platten-Abgleich.

      ALS DENKFIGUR: Bevor eine Hintergrundarbeit als „Last" verworfen wird,
      gehoert gefragt, ob sie eine WIEDERKEHRENDE Last ANDERSWO abloest. Dann
      ist die richtige Frage nicht ob, sondern wann.
    related: ["liste-ist-vorrat-platte-ist-wahrheit"]

  - id: testfinder-auf-einen-dateinamen-verengt
    kind: falle
    title: "Der Testlauf fand nur index.spec.mjs — ein zweites Modul im Plugin blieb ungeprueft"
    match: "test:plugins|node --test glob|spec wird nicht gefunden"
    sev: med
    tags: ["tests", "plugin", "falle", 2026]
    source: >
      20.08.2026, E66: beim Herueberziehen der Mitschnitt-Liste ins Plugin
      aufgefallen. package.json „test:plugins" stand auf
      plugins/*/index.spec.mjs, geaendert auf plugins/*/*.spec.mjs.
    body: >
      `test:plugins` STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die trennt:
      STIMMT DIE ZAHL DER ZEUGEN, ODER RUFT SIE NIEMAND?

      Der Befehl laeuft und meldet gruen, aber ueber ZU WENIGE Dateien -
      das ist DIESER Eintrag: der Finder zeigte auf einen Dateinamen
      statt auf ein Muster. Der Befehl laeuft richtig und vollstaendig,
      nur ruft ihn kein Laeufer und keine CI - das ist
      [[testmenge-in-keinem-laeufer]].

      Beide enden mit „gruen, und trotzdem ungeprueft". Der Unterschied
      liegt darin, ob man die ZAHL der Zeugen nachzaehlt oder die
      AUFHAENGUNG sucht.

      Solange jedes Plugin aus genau einer Datei bestand, war
      `node --test plugins/*/index.spec.mjs` richtig. Mit dem zweiten Modul
      (liste.mjs neben index.mjs) war es still falsch: liste.spec.mjs lief
      nicht mit, und der Lauf meldete trotzdem gruen.

      DIE BAUART DES FEHLERS: Ein Testfinder, der auf einen DATEINAMEN zeigt
      statt auf ein MUSTER, waechst nicht mit. Er meldet keine Luecke, sondern
      einen Erfolg ueber weniger Zeugen — die schlimmste Form, weil sie
      Sicherheit vortaeuscht.

      DIE PROBE, die es sofort zeigt: die Zahl der Zeugen vor und nach dem
      Umbau vergleichen. Hier 22 (Backend) gegen 29 (Plugin) — waere der Finder
      nicht mitgezogen worden, haette der Lauf danach WENIGER geprueft als
      vorher, ohne es zu sagen.

      ALLGEMEIN: Wenn ein Modul geteilt wird, gehoert der Testfinder mit auf
      die Liste der Dinge, die nachziehen muessen.
    related: [messwerkzeug-muss-sich-selbst-pruefen, einmal-hinsehen-ist-keine-messung, testmenge-in-keinem-laeufer]
  - id: mupihat-bricht-i2c-selbst-heraus
    kind: kniff
    title: "RTC an die Box: der MuPiHAT bricht I2C auf J5 heraus — kein Stapelstecker, nichts loeten"
    match: "rtc|ds3231|echtzeituhr|hwclock|i2c anschliessen|j5|stapelstecker"
    sev: low
    tags: ["hardware", "mupihat", "rtc", "i2c", "verdrahtung", 2026]
    source: >
      20.08.2026, am Geraet abgelesen (Fotos der Platine, Pi 5 + MuPiHAT).
      Der Betreiber wollte ein DS3231-Modul anschliessen; die erste Annahme
      war, dass die GPIO-Leiste unter dem HAT liegt und ein Stapelstecker
      noetig ist. Stimmt nicht.
    body: >
      DER MuPiHAT FUEHRT I2C SELBST HERAUS. Stiftleiste J5, zweireihig, die
      linke Gruppe ist auf der Platine mit `I2C, 3V, 5V` beschriftet, die
      rechte mit `GPIO`. Belegung, von der Platinenkante nach innen gelesen:

        obere Reihe:   5V    3V3   SCL  |  24   5    6   12   25
        untere Reihe:  GND   GND   SDA  |  26  10    9   11    8

      Alles, was ein I2C-Geraet braucht, liegt damit frei zugaenglich OBEN auf
      dem HAT. Der Reflex, den GPIO-Header des Pi zu suchen, kostet nur Zeit.

      DAS DS3231-MODUL `for Pi` (das kleine mit 5-poliger Buchse und gelber
      LIR2032) ist neben der Buchse mit `+ D C N G` bedruckt:

        +  = 3V3        D  = SDA        C  = SCL
        N  = frei (liegt beim Direktstecken auf GPIO4, ungenutzt)
        G  = GND

      Es ist gebaut fuer das Aufstecken auf die Pi-Pins 1/3/5/7/9. Mit Kabeln
      an J5 bleibt der `N`-Draht weg — vier Adern reichen.

      DIE EINE STELLE, AN DER EIN FEHLER TEUER WIRD: `+` gehoert an 3V3. Auf
      J5 liegt 5V direkt daneben, ganz aussen an der Kante. Nach dem AUFDRUCK
      gehen, nicht nach der Drahtfarbe, und vorher den Strom wegnehmen.

      KEIN ADRESSKONFLIKT: DS3231 auf 0x68, sein EEPROM auf 0x57, der
      Ladeleser BQ25792 des MuPiHAT auf 0x6b. Ein Bus, drei Teilnehmer.
      `dtparam=i2c_arm=on` und `i2c_arm_baudrate=50000` stehen durch
      scripts/mupihat/enable_mupihat.sh schon in der config.txt; 50 kHz
      reichen dem DS3231 muehelos. Dazu nur noch `dtoverlay=i2c-rtc,ds3231`.

      AUF EINEM PI 5 GIBT ES DANN ZWEI UHREN — der Pi 5 hat selbst eine
      eingebaute. `hwclock` nimmt ohne `--rtc` immer rtc0, und welche das ist,
      entscheidet die Bindungsreihenfolge, nicht die Absicht. Vor dem Stellen
      nachsehen. Wer nur Zeit ueber den Stromausfall retten will, braucht auf
      einem Pi 5 gar kein Modul: eine Akkuzelle am BAT-Stecker genuegt — dann
      aber `dtparam=rtc_bbat_vchg=…` setzen, sonst wird sie nie geladen.

      GEPRUEFT WIRD MIT tools/box/rtc-pruefen.py. Das Werkzeug sagt auch, was
      es NICHT kann: dass /dev/rtc0 da ist, heisst nichts. Der Kernel legt das
      Geraet an, sobald er den Chip findet — ob eine Zelle dranhaengt, sieht er
      nicht. Der Beweis ist Strom weg, warten, ohne Netz hochfahren.
    related: ["pi5-hat-16k-seiten", "einmal-hinsehen-ist-keine-messung"]
  - id: laden-hat-drei-ausgaenge-nicht-zwei
    kind: regel
    title: "Beim Laden einer Bestandsdatei gibt es DREI Ausgaenge — fehlt, unlesbar, kaputt"
    match: "readFile ENOENT|liste laden|bestand ueberschrieben|leere liste geschrieben|catch return leer"
    sev: high
    tags: ["daten", "verlust", "plugin", "mitschnitt", "regel", 2026]
    source: >
      20.08.2026, E66: beim Anschliessen der Mitschnitt-Liste an den
      Datenordner geschrieben und mit vier Sabotagen gegengeprobt
      (plugins/mixpi-mitschnitt/ablage.spec.mjs).
    body: >
      DER UEBLICHE CODE HAT ZWEI AUSGAENGE: `try { return JSON.parse(...) }
      catch { return LEER }`. Das ist falsch, und zwar teuer.

      Denn „leer" ist die Antwort auf genau EINEN der drei Faelle:

        DATEI FEHLT           -> leer. Richtig. Erster Start, kein Fehler.
        DATEI DA, NICHT LESBAR-> NICHT leer. Rechte, Ein-/Ausgabefehler, Karte
                                 zickt. Der Bestand IST da, wir kommen nur
                                 nicht heran. Wer jetzt eine leere Liste
                                 darueberschreibt, LOESCHT ihn.
        DATEI KAPUTT          -> leer, aber die Datei vorher BEISEITELEGEN.
                                 Ueberschreiben vernichtet den einzigen Zeugen;
                                 liegen lassen laesst jeden Start neu daran
                                 scheitern.

      DIE MITTLERE IST DIE, DIE MAN VERGISST — und die einzige, bei der der
      naechste Schreibvorgang Daten vernichtet. Deshalb gibt `listeLaden` ein
      Feld `unsicher` zurueck, und `mitListe` schreibt bei `unsicher` NICHT.

      Es ist dieselbe Regel wie bei einer Pruefung, die wirft: ein Fehler heisst
      „unbekannt", nicht „nichts da". Bei unbekannt wird nichts angetastet.

      DIE GEGENPROBE, die es zeigt: Bestand schreiben, Datei auf 0o000 setzen,
      eine Aenderung anstossen, Datei wieder lesbar machen — der Bestand muss
      unversehrt sein. Setzt man `unsicher` faelschlich auf false, wird genau
      dieser Test rot und sonst keiner.
    related: ["pruefung-die-wirft-heisst-unbekannt", "liste-ist-vorrat-platte-ist-wahrheit"]

  - id: richtung-des-zweifels-bei-treffern
    kind: regel
    title: "Ersatzwerte machen aus einer Suche einen Fehltreffer — die Richtung des Zweifels entscheidet"
    match: "Unbekannt|Unbenannt|ersatzwert|fallback name|schon vorhanden|doppelt aufgenommen"
    sev: med
    tags: ["mitschnitt", "e66", "e73", "pfad", "regel", 2026]
    source: >
      20.08.2026, E66/E73: beim Bau von `spurpfade` in
      plugins/mixpi-mitschnitt/index.mjs. Der eigene Test fand dabei sofort
      einen Fehler — „   " ist wahrheitswert-wahr und baute einen Pfad.
    body: >
      `albumOrdner` und `spurname` haben ERSATZWERTE: ohne Interpret entsteht
      der Ordner „Unbekannt", ohne Titel die Datei „Unbenannt.flac". Beim
      SCHREIBEN ist das richtig — irgendwohin muss es ja.

      BEIM SUCHEN KEHRT SICH DAS UM. Wer mit denselben Funktionen fragt „liegt
      das schon vor?", fragt bei fehlenden Angaben nach
      `Unbekannt/Ohne Album/Unbenannt.flac` — und findet dort womoeglich einen
      FREMDEN Titel. Der eigene gilt dann als erledigt und wird NIE aufgenommen.

      DESHALB GIBT `spurpfade` OHNE TITEL ODER OHNE INTERPRET GAR KEINEN PFAD
      ZURUECK. Nicht aus Ordnungsliebe, sondern wegen der RICHTUNG DES ZWEIFELS:

        falsches „liegt vor" -> der Titel ist still und dauerhaft verloren
        falsches „offen"     -> eine ueberfluessige Aufnahme, einmal

      Die beiden Fehler sind nicht gleich teuer. Wo sie es nicht sind, gehoert
      die Unsicherheit auf die billige Seite — und das muss im Code stehen, denn
      aus dem Aufruf allein sieht man es nicht.

      NEBENBEFUND, den der Test fand: `if (!eintrag.titel)` reicht nicht.
      „   " ist wahr. Getrimmt pruefen, sonst greift der Ersatzwert doch.
    related: ["laden-hat-drei-ausgaenge-nicht-zwei", "gegenprobe-statt-gruen-glauben"]

  - id: modellnummer-steht-vor-dem-wort-model
    kind: regel
    title: "Modellerkennung per Teilstring: die Nummer steht VOR dem Wort Model — und die Revision luegt"
    match: "ist_pi5|Raspberry Pi 5 Model B|split\\('Model'\\)|/proc/device-tree/model|modellerkennung|pi 4 rev 1.5|compute module"
    sev: med
    tags: ["hardware", "pi5", "werkzeug", "erkennung", "regel", 2026]
    source: >
      20.08.2026, Commit 56e90093 an tools/box/rtc-pruefen.py. Anlass war die
      Frage, ob der Pi 4 eine RTC hat (er hat keine — der Pi 5 war das erste
      Modell der B-Reihe mit eingebauter Uhr). Beim Nachsehen, was das Werkzeug
      auf einem Pi 4 tut, fiel die Zeile auf.
    body: >
      DIE FALSCHE ZEILE sah hinter dem Wort „Model" nach der 5:

          pi5 = '5' in modell.split('Model')[-1][:12]

      Die Modellnummer steht aber DAVOR. „Raspberry Pi 5 Model B Rev 1.0"
      zerfaellt zu `["Raspberry Pi 5 ", " B Rev 1.0"]`, und in „ B Rev 1.0" ist
      keine 5.

      ZWEIMAL FALSCH, IN BEIDE RICHTUNGEN — das ist das Lehrreiche:

        echter Pi 5      -> False. Der Abschnitt zur Zellenladung fiel STILL
                            aus. Ohne `dtparam=rtc_bbat_vchg` wird die
                            Knopfzelle nie geladen — genau der Mangel, den das
                            Werkzeug finden soll, waere unentdeckt geblieben.
        Pi 4 Rev 1.5     -> True. Die 5 stammt aus der REVISION. Das Werkzeug
                            haette an einem Geraet OHNE eingebaute Uhr nach
                            deren Ladeeinstellung gesucht und ihr Fehlen
                            gemeldet.

      Die zweite Richtung ist die unangenehmere: ein Fehlalarm sieht aus wie ein
      Fund und schickt jemanden auf die Suche nach etwas, das es nicht gibt.
      Die erste ist die haeufigere: ein Pruefwerkzeug, das eine Pruefung STILL
      ueberspringt, meldet „nichts Auffaelliges" und ist damit schlimmer als
      keines.

      DIE REGEL, die daraus folgt: eine Modellzeichenkette ist kein Freitext,
      in dem man nach Ziffern sucht. In `/proc/device-tree/model` stehen
      mindestens drei Zahlen — Modellnummer, Board-Revision, oft noch RAM oder
      Rev-Suffix. Wer irgendwo eine `5` sucht, findet irgendwann eine.

        * Wortgrenze setzen, nicht `in` verwenden: `\bPi 5\b`, verankert am
          Modellnamen, nicht an der Revision.
        * In eine EIGENE, benannte Funktion (`ist_pi5(modell)`) — eine Zeile
          mitten im Ablauf ist nicht pruefbar, eine Funktion schon.
        * Gegen ECHTE Zeichenketten pruefen, und zwar in BEIDE Richtungen: ein
          echter Pi 5 muss True geben, ein Pi 4 Rev 1.5 muss False geben.
          Nur-positive Tests haetten hier gar nichts gefunden — die kaputte
          Zeile war ja auf dem Zielgeraet falsch.
        * Compute Module 5 nicht vergessen: enthaelt „5", ist aber nicht
          gemeint, wenn es um die B-Reihe geht.

      Geprueft wird jetzt gegen zehn echte Modellzeichenketten, darunter beide
      Fallen. Dieselbe Falle steckt in jeder Erkennung per Teilstring —
      Kernelversion, Distributionsname, Geraetebaum.
    related: ["mupihat-bricht-i2c-selbst-heraus", "pruefung-die-wirft-heisst-unbekannt", "pruefen-schritte-die-still-luegen", "attrappe-luegt-durch-weglassen"]
  - id: offener-port-ist-keine-kennung
    kind: falle
    title: "welche-box.sh meldete einen MiniDLNA-Server als Box - Port 8200 ist doppelt belegt"
    match: "welche-box|8200|box suchen|nicht im inventar|minidlna"
    sev: med
    tags: ["werkzeug", "netz", "falle", "diagnose", 2026]
    source: >
      20.08.2026: Die Box war nach einem Adresswechsel nicht mehr erreichbar.
      `tools/welche-box.sh --suchen` meldete 192.168.178.199 als Box, samt
      Hinweis, sie ins Inventar nachzuziehen. Dort lief ein amd64-Rechner mit
      MiniDLNA 1.3.3.
    body: >
      MINIDLNA HORCHT AB WERK AUF PORT 8200 - genau dem Port des
      MuPiBox-Backends. Das Werkzeug pruefte nur, OB jemand antwortet, und der
      Kommentar behauptete ausdruecklich: „wer darauf antwortet, ist eine Box".

      DER FEHLER WAR DIE BAUART, NICHT DIE ZAHL. Geprueft wurde Anwesenheit
      statt Identitaet. Ein offener Port sagt, dass dort ein Dienst laeuft -
      nicht welcher. Bei einem so gewoehnlichen Port wie 8200 ist die
      Verwechslung keine Ausnahme, sondern zu erwarten.

      SCHLIMMER ALS EIN LEERES ERGEBNIS: Das Werkzeug meldete nicht „nichts
      gefunden", sondern einen FALSCHEN TREFFER mit Handlungsaufforderung. Wer
      dem folgt, traegt ein fremdes Geraet ins Inventar ein und richtet spaetere
      Messungen darauf aus.

      DIE BEHEBUNG: nach einem Endpunkt fragen, den nur diese Box hat -
      /api/darstellung, denselben, den die oertliche Pruefung im selben Skript
      schon benutzt. 200 heisst Box; 404 heisst irgendein anderer Dienst auf
      demselben Port. Die Auskunft im Fehlerfall nennt den Grund jetzt mit.

      ALLGEMEIN: Wer ein Geraet im Netz bestimmt, muss es ANTWORTEN LASSEN,
      nicht bloss anklopfen. Dasselbe gilt fuer Prozessnamen, Dateinamen und
      jedes andere billige Merkmal - siehe die Kioskerkennung, die auf
      `chromium` prueft, waehrend Cog laeuft.
    related: ["gegenprobe-statt-gruen-glauben", "einmal-hinsehen-ist-keine-messung"]

  - id: stillgelegtes-repo-lebt-im-fetch-cache-weiter
    kind: falle
    title: "Zwei Packs heissen mupibox - --wiki llmwiki_mupibox serviert v160 ohne Warnung"
    match: "wiki fetch|llmwiki_mupibox|--wiki all|\\.rsi/wiki|pack.yaml|stillgelegt|v160|pack-abgleich|nur in der quelle|eintrag nur im toten zweig"
    sev: high
    tags: ["wiki", "werkzeug", "falle", "rsi", "doku", 2026]
    source: >
      20.08.2026, beim stuendlichen Doku-Abgleich nachgemessen: nicht am Code
      abgelesen, sondern `controller/wiki.py list` fuer beide Namen laufen
      lassen. Der Kopf sagt es selbst - `--wiki mupibox` meldet "mupibox v212",
      `--wiki llmwiki_mupibox` meldet "mupibox v160".
    body: >
      IM FETCH-CACHE ~/.rsi/wiki/ LIEGEN ZWEI ORDNER MIT DEMSELBEN PACK:

          mupibox/            v212, 689 Eintraege  (die lebende Kopie)
          llmwiki_mupibox/    v160, Stand 10.08.2026, seitdem tot

      Beide pack.yaml tragen `id: mupibox` und denselben Titel. Der einzige
      Unterschied im Kopf ist die Versionsnummer - sonst sieht die alte Kopie
      exakt aus wie die richtige.

      WARUM DER FALSCHE NAME GEWINNT: `resolve()` in controller/wiki.py sucht
      ZUERST einen ORDNER dieses Namens unter den Wurzeln und erst danach ein
      Pack, dessen `id` passt. Weil der Ordner llmwiki_mupibox existiert,
      greift der Ordnertreffer - die id-Suche kommt nie dran. Kein Fehler,
      keine Warnung, 52 Fassungen altes Wissen.

      DIE README FUEHRT GENAU DAHIN. remote-step-installer/README.md nennt zum
      Holen weiterhin das Repo llmwiki_mupibox.git - das am 10.08.2026
      STILLGELEGTE. Wer der Anleitung folgt, legt den veralteten Ordner neu an
      oder frischt ihn auf und arbeitet danach gegen v160/v192.

      UND `--wiki all` MISCHT BEIDE. `_merge()` haengt die Eintragslisten nur
      aneinander, es entdoppelt NICHT nach id. Der Kopf lautet dann
      "generic v5, mupibox v160, mupibox v212", und jeder Eintrag erscheint
      zweimal - einmal mit dem alten, einmal mit dem heutigen Text. Ein Leser,
      der den ersten Treffer nimmt, bekommt die alte Fassung.

      WAS AM 20.08.2026 NICHT PASSIERT WAR: Wissen ging keines verloren.
      Nachgemessen ueber die Eintrags-ids waren die 633 Eintraege des
      stillgelegten Repos eine echte TEILMENGE der 689 im Hauptbaum - nichts
      stand nur dort. Die Gefahr war nicht der Verlust, sondern das stille
      ALTE LESEN.

      NACHTRAG 22.08.2026, STUENDLICHER DOKU-LAUF: DIE TEILMENGE IST GEBROCHEN.
      Ein 34. Commit ging um 17:08 Uhr in das stillgelegte Repo (de053e8,
      Fassung 193). Stand jetzt: Baum 283/776, stillgelegtes Repo 193/634,
      Fetch-Cache 160/583. Der Cache ist weiter reine Teilmenge, das
      stillgelegte Repo NICHT MEHR - genau ein Eintrag steht nur dort:
      kein-plugin-weg-in-die-medienliste. Gemessen mit
      `python3 tools/pack-abgleich.py` (meldet ihn unter "NUR in der Quelle").

      UND ER DARF NICHT VERBATIM GEHOLT WERDEN. Sein Inhalt ("es gibt keinen
      generischen Plugin-Weg in die Medienliste, deshalb hat mixpi-archive
      keinen Aufnehmen-Knopf") war beim Schreiben richtig und ist WENIGE
      STUNDEN SPAETER FALSCH geworden: E87 (ed97b41c) baute den Weg, E88
      (4b188a02) den Knopf, beide am 22.08. im Hauptbaum. Im Baum steht die
      gueltige Fassung als plugin-inhalt-kommt-in-die-medienliste. Ein
      `--holen` haette dem Pack einen Eintrag zugefuegt, der dem Nachbarn
      widerspricht - und der neuere von beiden waere der falsche gewesen.

      DIE ZWEITE LEHRE, GENAU HIER: Was im toten Zweig steht, ist nicht nur
      unerreichbar, es ALTERT AUCH UNBEOBACHTET. Der Hauptbaum korrigiert sich
      beim naechsten Bau; ein Eintrag daneben nicht. Deshalb ist der Rueckhol-
      Griff kein Automatismus - jeder Fund wird gegen den heutigen Baum
      GELESEN, bevor er hereinkommt.

      DASS ES NOCH LEBT, IST BELEGT: In ~/Downloads/llmwiki_mupibox liegt
      STILLGELEGT.md ("hier bitte nicht mehr arbeiten", 10.08.2026) - und
      trotzdem tragen die juengsten Commits dort das Datum 22.08.2026. Genau
      die "zweite Wahrheit", vor der die Datei warnt, wird weiter beschrieben -
      zwoelf Tage nach der Stilllegung und zwei Tage nachdem dieser Eintrag sie
      benannt hat. Eine Textdatei haelt niemanden auf; der Ordner muss weg.

      DIE REGEL: Ein stillgelegtes Repo ist erst dann stillgelegt, wenn auch
      seine ABGELEITETEN KOPIEN weg sind - der Fetch-Cache ist eine davon. Eine
      Textdatei, die "nicht mehr benutzen" sagt, wird von keinem Werkzeug
      gelesen. Wer eine Quelle aufgibt, muss den Cache-Ordner entfernen und die
      Anleitung umschreiben, sonst zeigt die Ablage weiter auf die Leiche.


      „WIKI FETCH" STEHT AUCH IN [[nachziehen-endet-erst-an-der-fernkopie]],
      und beide Fragen kommen bei derselben Handlung auf. Diese hier lautet:
      welchen NAMEN hast du gefetcht (`mupibox` oder das tote
      `llmwiki_mupibox`)? Die andere: bis zu welcher STUFE ist der Stand
      gekommen (Baum, Spiegel, origin)? Ein Fetch des richtigen Namens kann
      trotzdem alt sein, weil origin hinterherhinkt - und ein frischer origin
      hilft nichts, wenn der falsche Ordner gelesen wird.


      NACHTRAG 29.08.2026, STUENDLICHER DOKU-LAUF: ES WURDE EIN DRITTES MAL
      HINEINGESCHRIEBEN. Am 25.08.2026 gingen zwei weitere Commits in das
      stillgelegte Repo (66397d2 "Das Sektions-Vokabular und die echten Orte
      sind in BEIDE Richtungen auseinander", 9f94247 "Die Sektions-Wache war
      seit ihrem ersten Tag rot") - drei Tage NACHDEM der Nachtrag vom 22.08.
      hier festhielt, der Ordner muesse weg. Stand jetzt: Baum 390/889,
      stillgelegtes Repo 195/635, Fetch-Cache 387/886. `pack-abgleich.py`
      meldet nicht mehr einen, sondern ZWEI Eintraege unter "NUR in der
      Quelle": kein-plugin-weg-in-die-medienliste und neu
      sektionen-vokabular-passt-nicht-zu-den-orten.


      AUCH DER ZWEITE DARF NICHT GEHOLT WERDEN, UND ZWAR AUS DEMSELBEN GRUND
      WIE DER ERSTE - er ist im Baum laengst ueberholt. Seine Aussage lautet,
      `SEKTIONEN` und die echten Orte seien "nie gegeneinander gemessen
      worden". Beides gilt heute nicht mehr: `tools/sektionen-deckung.py`
      MISST sie gegeneinander und meldet gruen, weil `plugins/README.md:233-245`
      `ton` und `medien` ausdruecklich als **nirgends** ausweist - aus der
      Absicht gelesen, nicht aus einer Liste im Skript. Die zweite Lehre des
      toten Zweigs (die Wache war seit ihrem ersten Tag rot) steht im Baum
      eigenstaendig als [[dauerrote-wache-ist-keine]]. Es ist also wieder
      NICHTS verloren gegangen - beide Funde sind im Hauptbaum in ihrer
      gueltigen Fassung da, nur unter anderen ids.


      WAS DAS FUER DEN STUENDLICHEN LAUF HEISST: "NUR in der Quelle (2)" ist
      KEIN Befund mehr, sondern ein Dauerbefund - er wird bei jedem Lauf
      wiederkommen und jedes Mal Arbeit kosten, solange der Ordner steht.
      Nach [[wiederkehrender-befund-ist-ablauffehler]] gehoert er an die Naht
      behoben, nicht bei jedem Lauf neu untersucht: der Ordner
      ~/Downloads/llmwiki_mupibox und der Cache-Ordner ~/.rsi/wiki/llmwiki_mupibox
      muessen weg, und remote-step-installer/README.md darf nicht mehr
      dorthin zeigen. Das ist ein Eingriff am Arbeitsplatz des Betreibers -
      hier nur gemeldet, nicht getan.


      NACHTRAG 30.08.2026, STUENDLICHER DOKU-LAUF: DIE NAHT IST JETZT
      BEHOBEN - IM WERKZEUG, NICHT AM ORDNER. Der Absatz darueber verlangt,
      den Dauerbefund an der Naht zu beheben, und nennt als Weg das
      Wegraeumen des Ordners. Das bleibt ein Eingriff am Arbeitsplatz des
      Betreibers und ist weiterhin nicht getan. Es gibt aber eine zweite
      Naht, die dem Laufenden gehoert: `pack-abgleich.py` selbst hat den
      Unterschied nicht gekannt, den dieser Eintrag beschreibt. Er warf
      "geht beim Leser verloren" und "steht laengst da, nur unter anderer
      id" in DIESELBE Liste - und schickte damit jeden Lauf in dieselbe
      Untersuchung.

      WAS DAS WERKZEUG JETZT TRENNT: `teile_besprochene()` fragt, ob das
      ZIEL-Paket ueber die fehlende id REDET. Die zwei bekannten stehen
      seitdem unter "BESPROCHEN" mit dem Hinweis, im Ziel nachzulesen;
      "NUR in der Quelle" steht auf 0 und meint wieder, was es sagt.

      DAS MERKMAL IST DIE SORTE, NICHT EINE LISTE IM SKRIPT. Eine
      hartkodierte Ausnahmeliste haette dieselbe Ausgabe erzeugt und waere
      die schlechtere Wache gewesen: sie schweigt auch dann noch, wenn der
      Grund laengst weg ist. Gefragt wird stattdessen nach der Eigenschaft,
      die den Fall ausmacht - jemand hat hier darueber geschrieben. Taucht
      im Klon je ein wirklich neuer Eintrag auf, ueber den niemand
      geschrieben hat, steht er weiter oben als Befund. Gegengeprobt am
      30.08.: eine erfundene id landet unter "offen", und
      `dauerrote-wache` gilt NICHT als besprochen, obwohl
      `dauerrote-wache-ist-keine` im Paket steht - die Wortgrenze im Muster
      haelt, sonst waere jede kurze id von einer laengeren verdeckt worden.

      WAS WEITER OFFEN IST: `pack-abgleich.py` haengt in KEINEM Laeufer -
      weder in `doku-luecken-probe.sh` noch in `pruefen.sh`. Dieser Fund kam
      nur zustande, weil der Lauf nach [[ungerufene-wache-driftet-in-den-fehlalarm]]
      hinter der gruenen Probe nach ungerufenen Wachen gesucht hat.
      Einhaengen geht nicht ohne Weiteres: die Quelle
      ~/Downloads/llmwiki_mupibox liegt nur auf DIESER Maschine, und ein
      fehlender Pfad beendet das Werkzeug heute mit `sys.exit('fehlt: ...')`.
      Wer es einhaengen will, muss vorher aus dem fehlenden Ordner ein
      "nichts zu tun" machen statt eines Fehlers.
    related: [offener-port-ist-keine-kennung, gegenprobe-statt-gruen-glauben, einmal-hinsehen-ist-keine-messung, nachziehen-endet-erst-an-der-fernkopie, anleitung-zeigt-ins-stillgelegte-repo, dauerrote-wache-ist-keine, wiederkehrender-befund-ist-ablauffehler, wortzahl-ist-kein-ausschluss-ueber-den-gegenstand, ungerufene-wache-driftet-in-den-fehlalarm]
  - id: plugin-laufwerk-ist-eigenes-ausrollziel
    kind: falle
    title: "Plugin-Aenderungen wirken nicht: plugin-laufwerk.js ist ein EIGENES Ausrollziel"
    match: "datenOrdner kommt nicht an|kontext fehlt|plugin liest nicht|ausliefern --nur|plugin-laufwerk"
    sev: high
    tags: ["plugin", "ausliefern", "falle", "diagnose", 2026]
    source: >
      20.08.2026, E66: nach `ausliefern.py --nur server,plugins` blieb der
      Datenordner beim Plugin unsichtbar. Am Geraet gemessen mit
      tools/box/plugin-datenordner-probe.py.
    body: >
      DER WIRT LIEGT IM SERVER-BUENDEL, DAS LAUFWERK NICHT. plugin-wirt.ts wird
      in server.js hineingebaut; plugin-laufwerk.js ist eine EIGENE Datei neben
      server.js und ein eigenes Ziel in ausliefern.py (`--nur laufwerk`).

      WER NUR `server,plugins` AUSLIEFERT, laesst das Laufwerk auf altem Stand.
      Am Geraet stand es sechs Tage still: server.js enthielt frisch
      `datenOrdner:t.datenOrdner??""`, plugin-laufwerk.js enthielt das Wort
      NULL MAL. Der Wirt schickte den Ordner also korrekt los, und das Laufwerk
      legte ihn nie auf den Kontext.

      ES FAELLT NICHT AUF, WEIL NICHTS BRICHT. Ein Plugin ohne Datenordner
      verhaelt sich nach aussen genau wie eines mit LEEREM Datenordner: beide
      melden "nichts vorgemerkt". Kein Fehler im Journal, keine Ausnahme, ein
      gruenes Befinden.

      DIE PROBE, die den Unterschied sichtbar macht: eine erkennbare Liste IN
      den Datenordner legen und dann das Befinden abfragen. Zeigt es die Probe,
      ist die Naht dicht. Vorhandene Liste vorher sichern und danach
      zurueckstellen - sonst ist der Erfolg der Probe zugleich der Schaden.

      MERKSATZ: Bei allem, was den Plugin-KONTEXT betrifft (neue Felder, neue
      Rechte, neue Uebergaben), gehoeren server UND laufwerk zusammen
      ausgeliefert. Der Kontext entsteht im Laufwerk, nicht im Wirt.
    related: ["offener-port-ist-keine-kennung", "einmal-hinsehen-ist-keine-messung"]

  - id: feldstaerke-ist-kein-wissen-sondern-ein-messwert
    kind: regel
    title: "Eine Feldstaerke gehoert nicht ins Gedaechtnis: -53 dBm wurden binnen Stunden zu -70"
    match: "welches band|2,4 oder 5 ghz|dbm|freq_list|welcher adapter|wlan-stick oder eingebaut|funkvergleich"
    sev: med
    tags: ["wlan", "messen", "haltbarkeit", "werkzeug", "e68", "regel", 2026]
    source: >
      20.08.2026, im Anschluss an E68. Im Wiki stand aus derselben Sitzung
      "-68 dBm auf 5180 wurden zu -53 dBm auf 2462, also 15 dB mehr Reserve"
      (siehe [[dfs-kanal-laesst-die-box-auf-totem-kanal-zurueck]]). Wenige
      Stunden spaeter lieferte dieselbe Box auf 2,4 GHz -70 dBm, waehrend
      5 GHz -66 dBm zeigte. Die Box hatte sich bewegt.
    body: >
      DIE EMPFEHLUNG HAETTE DIE LAGE VERSCHLECHTERT. Wer die alte Zahl
      uebernimmt und die Box per `freq_list` auf 2,4 GHz festnagelt, nagelt sie
      an diesem Platz auf das SCHWAECHERE Band - und zwar dauerhaft, weil ein
      Festnageln kein Nachmessen kennt.

      WARUM DIE ZAHL SO SCHNELL VERFAELLT: Feldstaerke ist keine Eigenschaft
      des Bandes, sondern des PLATZES. 5 GHz verliert mehr an Waenden und
      Moebeln als 2,4 GHz - aber 2,4 GHz teilt sich das Band mit allem
      anderen im Haus. Welcher Nachteil ueberwiegt, entscheidet sich pro
      Standort und aendert sich, sobald die Box umzieht, das Gehaeuse gedreht
      wird oder der Router einen Kanal wechselt.

      WAS IM WIKI HALTBAR IST UND WAS NICHT:

        haltbar   - dass DFS-Kanaele geraeumt werden und eine haengende Box
                    danach ins Leere redet
        haltbar   - dass um -70 dBm die Assoziation unzuverlaessig wird
        haltbar   - dass Audio-Streaming auf 2,4 GHz bandbreitenseitig
                    muehelos passt
        VERFALLEN - jede konkrete dBm-Zahl, jede daraus abgeleitete
                    Bandempfehlung, jede Aussage "hier ist X besser als Y"

      DAHER EIN WERKZEUG STATT EINER ZAHL: tools/box/funk-vergleich.py laesst
      JEDE Funkschnittstelle der Box einzeln eine Umschau halten und legt die
      Ergebnisse nebeneinander - dieselben Sender, von zwei Antennen gehoert.
      Es sagt je Sender, welcher Adapter ihn besser hoert und um wie viel.

          python3 tools/box/funk-vergleich.py --box dietpi@…
          python3 tools/box/funk-vergleich.py --box … --anwerfen   # schlafende wecken

      DREI DINGE, DIE DAS WERKZEUG BEWUSST TUT:

      (1) ES STELLT NICHTS UM. Der Adapter, an dem die aktuelle Verbindung
      haengt, wird per `ip route get` ERKANNT und in Ruhe gelassen. Wer die
      Leitung kappt, auf der er sitzt, kommt nicht mehr an die Box. Laesst
      sich der Traeger nicht erkennen, wird vorsichtshalber KEINER angefasst.

      (2) MIT `--anwerfen` wird ein schlafender Adapter kurz hochgefahren und
      danach wieder hingelegt, wie er lag. Eine Schnittstelle ohne
      Netzzuordnung verbindet sich nicht von selbst - deshalb aendert auch das
      keine Verbindung.

      (3) UNTER 5 dB UNTERSCHIED IST KEINE ENTSCHEIDUNG. So viel schwankt
      zwischen zwei Umschauen am selben Platz; darunter meldet das Werkzeug
      bewusst keinen Sieger. Vgl. [[einmal-hinsehen-ist-keine-messung]].

      NEBENBEI GEPRUEFT WIRD DAS STROMSPAREN: das Werkzeug zeigt je
      Schnittstelle Treiber und `power_save`. Steht dort `on`, ist der Rest
      der Messung mit Vorsicht zu lesen - ein schlafendes Modul hoert
      schlechter. Siehe die power_save-Regel im Pack.

      MERKSATZ: Zustandszahlen gehoeren in ein Werkzeug, das sie neu holt,
      nicht in einen Satz, der sie behauptet.
    related: ["dfs-kanal-laesst-die-box-auf-totem-kanal-zurueck", "einmal-hinsehen-ist-keine-messung", "annahme-vor-dem-bauen-messen-nicht-glauben"]
  - id: rueckweg-verfaelscht-die-messung
    kind: falle
    title: "Zwei Schnittstellen im selben Teilnetz: der Ping misst den RUECKWEG, nicht die gemessene Strecke"
    match: "zwei wlan|wlan1|zweite schnittstelle|ping falsch|adapter vergleichen|paketverlust"
    sev: high
    tags: ["netz", "messen", "falle", "selbstkorrektur", 2026]
    source: >
      20.08.2026 an der Box. Diese Falle hat mich zweimal hintereinander eine
      falsche Aussage kosten lassen; die erste Fassung dieses Eintrags stand
      schon im Pack und war selbst falsch.
    body: >
      DER AUFBAU: Box mit zwei Funkschnittstellen im SELBEN Teilnetz, wlan0
      mit 192.168.178.57 und wlan1 mit .194. Vom Arbeitsrechner beide Adressen
      angepingt, um die Strecken zu vergleichen. Ergebnis: .194 war fast tot,
      .57 lief. Schluss: der Stick taugt nicht.

      DER SCHLUSS WAR FALSCH, UND ZWAR AUS EINEM STRUKTURELLEN GRUND. Ein Ping
      an .194 kommt ueber wlan1 herein — die ANTWORT geht aber ueber die
      VORGABEROUTE hinaus, und die lag auf wlan0. Gemessen wurde also
      wlan1-hin-und-wlan0-zurueck, also im Wesentlichen wlan0. Der Adapter,
      dem ich das Ergebnis anlastete, kam darin fast nicht vor.

      DIE MESSUNG, DIE ES ZEIGT, LAEUFT AUF DER BOX:

          ping -I wlan0 -c10 192.168.178.1
          ping -I wlan1 -c10 192.168.178.1

      `-I` bindet an die Schnittstelle, hin UND zurueck. Von dort aus gemessen
      hatte wlan1 0 % Verlust und wlan0 50 % — das genaue Gegenteil.

      UND DANN DER ZWEITE FEHLER: Zehn Minuten spaeter war es 0 % auf wlan0 und
      10 % auf wlan1. Die Strecken SCHWANKEN so stark, dass auch die richtige
      Messung nichts traegt, wenn sie kurz ist. Wer aus einem Zehn-Paket-Lauf
      eine Empfehlung ableitet, hat Rauschen gelesen.

      ZWEI REGELN, beide teuer bezahlt:

        1. Wer eine STRECKE messen will, muss sie in BEIDE Richtungen erzwingen.
           Sonst misst man den Rueckweg. `rp_filter` stand hier ueberall auf 0,
           es wurde also nichts verworfen — das Verfahren war einfach falsch.
        2. In einer schwankenden Umgebung ist eine Kurzmessung keine Messung.
           Vergleiche gehoeren ueber Stunden gefahren, unbeaufsichtigt, oder
           gar nicht.

      HINWEIS AN MICH SELBST: Die erste Fassung dieses Eintrags hiess
      „pegel-ist-nicht-durchsatz" und behauptete, der Stick hoere 14 dB besser
      und trage 89 % schlechter. Der Pegel-Teil stimmt (er hoert wirklich
      besser). Die Durchsatz-Zahl war ein Messfehler. Ein Wiki-Eintrag aus
      einer fehlerhaften Messung ist schlimmer als keiner: er wird geglaubt.

  - id: nachziehen-endet-erst-an-der-fernkopie
    kind: falle
    title: "Der Spiegel hat drei Stufen — und geprueft wurden bisher nur zwei"
    match: "wiki fetch|pack.yaml|--wiki mupibox|version 205|nachgezogen|gespiegelt"
    sev: med
    tags: ["wiki", "doku", "werkzeug", "falle", "git", 2026]
    source: >
      20.08.2026, 23:07, beim stuendlichen Doku-Abgleich nachgemessen: die
      Kopfzeile jeder der drei Kopien einzeln gelesen, dazu
      `git rev-list --count origin/main..main` und ein `curl` auf die rohe
      pack.yaml ueber Gitea.
    body: >
      DIE DREI KOPIEN, im selben Augenblick gemessen:

          Baum      llmwiki/pack.yaml           v214, 693 Eintraege
          Spiegel   ~/.rsi/wiki/mupibox/        v214, 693 Eintraege
          origin    git.local:3000/achim/box    v205, 659 Eintraege

      Die rohe Datei ueber Gitea bestaetigt es von aussen: HTTP 200,
      2 103 111 Byte, `version: "205"`.

      DAZWISCHEN LIEGEN 34 COMMITS. Der letzte Stand auf origin/main ist
      fbbf4b38 von 16:39; der Baum steht auf a919593b von 22:13. Neun
      Pack-Fassungen und 34 Eintraege sind lokal fertig und ausserhalb dieses
      Rechners nicht vorhanden — darunter alles zu E72/E73/E74 und die vier
      juengsten Funde (Datenordner als eigenes Ausrollziel, Fetch-Cache,
      Feldstaerke, Pegel/Durchsatz).

      WARUM DAS TROTZ SAUBERER PFLEGE PASSIERT: Die Regel lautet bisher
      „Baum schreiben, dann nach ~/.rsi spiegeln" — und genau das ist jedesmal
      geschehen. Beide geprueften Stufen stehen auf 214. Die dritte Stufe hat
      nie jemand geprueft, weil sie nicht Teil der Regel war.

      DAS PRUEFWERKZEUG BESTAETIGT DIE LUECKE, STATT SIE ZU FINDEN.
      `tools/pack-abgleich.py` vergleicht Baum gegen Zwischenspeicher und
      meldet „NUR in der Quelle (0)" — also alles gut. Es sieht die Fernkopie
      nicht an. Ein gruener Abgleich heisst hier: die beiden Kopien auf DIESEM
      Rechner sind einig.

      WEN ES TRIFFT: nicht diese Sitzung, sondern jeden Leser ohne diesen
      Rechner — eine frische Karte, ein zweiter Arbeitsplatz, `wiki fetch`
      nach der Anleitung des Installers. Der bekommt v205 und misst 34
      Erkenntnisse ein zweites Mal heraus. Dieselbe Bauart wie
      [stillgelegtes-repo-lebt-im-fetch-cache-weiter], nur eine Stufe weiter
      draussen: dort las jemand still eine ALTE Kopie, hier holt er sie.

      DIE REGEL: Nachziehen endet an der Kopie, die der LESER anfasst, nicht an
      der, die am naechsten liegt. Wer eine Ablage in Stufen betreibt, muss
      jede Stufe messen — und das Werkzeug, das den Abgleich behauptet, muss
      jede Stufe ansehen, sonst ist sein Gruen eine Aussage ueber die falsche
      Strecke.

      WAS BEWUSST NICHT GESCHEHEN IST: gepusht. Was das Haus verlaesst,
      entscheidet der Betreiber; ein Abgleichslauf darf die Luecke benennen,
      nicht sie im Vorbeigehen schliessen.
    related: ["stillgelegtes-repo-lebt-im-fetch-cache-weiter", "werkzeug-meldet-ohne-empfaenger", "einmal-hinsehen-ist-keine-messung", "wiki-zustand-hat-haltbarkeit"]
  - id: einzelne-integrationsspec-braucht-node-env-test
    kind: falle
    title: "Eine einzelne *.integration.spec.ts direkt aufrufen: ohne NODE_ENV=test wird alles ABGEBROCHEN, nicht rot"
    match: "test did not finish before its parent|cancelled 8|__dirname is not defined|integration spec einzeln"
    sev: med
    tags: ["tests", "backend", "falle", "werkzeug", 2026]
    source: >
      21.08.2026, E72: eine neue Routenpruefung schien vollstaendig kaputt zu
      sein. Sie war es nie - der Aufruf war falsch. Kostenpunkt rund zwanzig
      Minuten Fehlersuche am Test statt am Aufruf.
    body: >
      DAS BILD: Jede Pruefung der Datei erscheint mit einem Kreuz, und unten
      steht

          ℹ pass 0   ℹ fail 0   ℹ cancelled 8
          'test did not finish before its parent and was cancelled'

      NULL BESTANDEN, NULL DURCHGEFALLEN, ALLES ABGEBROCHEN. Das sieht nach
      einem Aufbaufehler in der Spec aus - falsche Verschachtelung, ein
      haengender before-Haken - und genau dort sucht man dann.

      DIE URSACHE LIEGT WOANDERS: `npm test` laeuft als
      `cross-env NODE_ENV=test tsx --test src/*.spec.ts`. Ohne NODE_ENV ist
      `productionServe` wahr, und dann laeuft in server.ts der Block

          app.use(express.static(path.join(__dirname, 'www'), …))

      `__dirname` gibt es im ES-Modul nicht. Der Import wirft, der before-Haken
      scheitert, und node:test bricht daraufhin alle Kinder ab - OHNE den
      Grund an der Spec anzuzeigen.

      RICHTIG AUFRUFEN (fish kennt kein VAR-Praefix):

          env NODE_ENV=test npx tsx --test src/meine.integration.spec.ts

      DIE ALLGEMEINE LEHRE: `cancelled` ist kein Testergebnis, sondern ein
      Hinweis auf die UMGEBUNG. Wer bei „alles abgebrochen" anfaengt, die Spec
      umzubauen, behandelt das Symptom. Der Haken selbst wird nie einzeln
      ausgefuehrt - also einmal von Hand ausfuehren, was er tut; dort steht der
      echte Fehler in einer Zeile.
    related: ["fish-kennt-kein-var-praefix", "gegenprobe-statt-gruen-glauben"]
  - id: umschalten-gehoert-dorthin-wo-die-verbindung-nicht-haengt
    kind: regel
    title: "Ein Schalter, der die eigene Adresse aendert, gehoert auf den Schirm der Box - nicht in die Browser-Verwaltung"
    tags: ["netzwerk", "wlan", "verwaltung", "newdesign", "bedienung", "regel", 2026]
    source: >
      20.08.2026, E71: die Wahl zwischen eingebautem Funk und WLAN-Stick gab es
      bisher nur in der Browser-Verwaltung. Nachgezogen auf NewDesign/app.js,
      Vertrag festgehalten in src/backend-api/src/wlan-adapter-schirm.spec.ts.
    body: >
      DIE URSACHE, DIE DEN ORT BESTIMMT: Der Adapterwechsel gibt der Box eine
      ANDERE Adresse. Die Verwaltung im Browser haengt an der alten - sie
      verliert ihre Verbindung in dem Moment, in dem der Knopf wirkt, und muss
      den Benutzer unter einer neuen Adresse suchen lassen, die er noch nicht
      kennt. Der Schirm der Box redet mit 127.0.0.1 und merkt vom Adresswechsel
      nichts. Er ist damit nicht der bequemere, sondern der einzige gefahrlose
      Ort.

      DIE ALLGEMEINE FORM: Wer eine Bedienung platziert, fragt nicht "wo passt
      sie hin", sondern "welcher Weg wird durch ihre Wirkung gekappt". Jede
      Bedienung, die Adresse, Netz oder Dienst der Box umstellt, faellt unter
      dieselbe Regel.

      DIE ZEILE ERSCHEINT NUR, WENN ES ETWAS ZU UNTERSCHEIDEN GIBT. Ohne Stick
      am USB waere sie eine Wahl, die es nicht gibt, und der Knopf liefe in den
      409 der Route. Ein Knopf, der zuverlaessig scheitert, ist schlimmer als
      keiner. Ohne Auskunft (`wlanAdapter === null`) erscheint sie ebenfalls
      nicht - ein Umschalter, der nicht weiss, worauf er steht, laedt ein, auf
      genau den Adapter zu wechseln, an dem man ohnehin haengt.

      DER SATZ DARUNTER NENNT DIE FOLGE, statt sie vorauszusetzen: andere
      Adresse, samt der neuen, und dass DIESER Bildschirm davon nichts merkt,
      eine Verwaltung im Browser aber schon.

      NACHGEFRAGT WIRD 9 s NACH DEM UMSCHALTEN, weil die Route selbst 8 s
      wartet, bevor sie das alte Geraet hinlegt. Frueher zu fragen liefert den
      Stand von vorhin und sieht aus, als sei nichts passiert.

      DER TEST IST EIN VERTRAGSTEST, KEIN TEXTFUND - und die erste Fassung war
      zu schwach: sie sammelte alle Vorkommen von 'intern'/'extern' im Block
      und blieb GRUEN, als der GESENDETE Wert auf 'usb' verbogen wurde, weil
      die Vergleiche daneben (`ad.aktiv === 'extern'`) die Menge vollstaendig
      hielten. Geprueft wird jetzt die Zuweisung, die im Aufruf landet. Vier
      Sabotagen, vier rote Zeugen.
    related: ["gegenprobe-statt-gruen-glauben", "endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok", "usb-stecker-brauchen-keine-treiber"]
  - id: ein-wort-zwei-bedeutungen-nicht-pauschal-ersetzen
    kind: falle
    title: "\"Tonmaschine\" war DOPPELT vergeben - ein pauschales Umbenennen haette zwei Begriffe verschmolzen"
    match: "Tonmaschine|Spotify Engine|spotify.engine|umbenennen"
    sev: med
    tags: ["begriffe", "verwaltung", "spotify", "oberflaeche", "falle", 2026]
    source: >
      20.08.2026, Anmerkung des Betreibers: "koennen wir Tonmaschine auf
      Spotify Engine umtaufen". Umgesetzt in
      src/frontend-admin/src/app/seiten/spotify-maschine.ts.
    body: >
      DER GUTE TEIL: "Spotify Engine" macht Oberflaeche und Konfiguration
      einig - der Schluessel heisst dort ohnehin `spotify.engine`.

      DIE FALLE: Das Wort war auf dieser Box ZWEIMAL vergeben. In
      spotify-control.ts bezeichnet `Tonmaschine` die Wahl zwischen Spotify und
      lokaler Wiedergabe (siehe [[dienste-umschalten]]) - etwas voellig
      anderes als der Spotify-Client-Schalter librespot/Soloist. Ein
      pauschales Ersetzen ueber alle Dateien haette zwei Begriffe verschmolzen,
      die nur zufaellig gleich hiessen, und die Verwechslung dauerhaft
      festgeschrieben.

      UMBENANNT WURDE DESHALB NUR DIE EINE BEDEUTUNG: der
      librespot/Soloist-Schalter. Die fuenf Stellen in spotify-control.ts, die
      eine Zeile in server.ts und die in NewDesign bleiben, wie sie sind.

      DIE REGEL: Vor jedem Umbenennen einmal zaehlen, WIE VIELE Bedeutungen das
      Wort im Baum hat. `grep -rn` auf den alten Namen ist billig; ein
      verschmolzener Begriff wird nie wieder getrennt.

      ZWEITE ANMERKUNG, DIESELBE URSACHE: "client id und lautsprecher ton wirkt
      als ob es auch zu soloist gehoert". Zu Recht - in EINER Karte standen
      hintereinander der Assistent, die Engine-Wahl und ohne jede Trennung die
      allgemeinen Dienstfelder. Wer von oben liest, ordnet sie dem letzten
      Ueberschriftsblock zu, und das war Soloist. Es stand sogar ein
      erklaerender Satz dagegen ("gilt fuer BEIDE Maschinen").

      EIN SATZ, DER EINE FALSCHE GRUPPIERUNG ERKLAERT, IST SCHWAECHER ALS DIE
      RICHTIGE GRUPPIERUNG - er wird gelesen, NACHDEM der Blick die Zuordnung
      schon getroffen hat. Beide Bloecke haben jetzt eine sichtbare Grenze und
      eine Ueberschrift, die die Zugehoerigkeit ausspricht, statt sie der
      Anordnung zu ueberlassen.

      DABEI IN DIE DOKUMENTIERTE FALLE GETRETEN: `.abschnitt` in Backticks in
      einem HTML-Kommentar INNERHALB des Template-Literals - das reisst die
      Vorlage ab, tsc meldete einen Eigenschaftszugriff auf die
      Vorlagenzeichenkette. Im Kopf DERSELBEN Datei steht seit jeher "KEINE
      BACKTICKS in dieser Vorlage".
    related: ["dienste-umschalten", "backticks-beenden-jede-vorlage"]
  - id: eine-funkmessung-ist-keine-funkmessung
    kind: regel
    title: "Zwei RICHTIGE Funkmessungen zehn Minuten auseinander ergaben das Gegenteil - Adaptervergleich braucht Stunden, nicht Sekunden"
    tags: ["wlan", "messen", "haltbarkeit", "werkzeug", "sonde", "regel", 2026]
    source: >
      20.08.2026 an der Box, im Anschluss an E68. Werkzeuge:
      tools/box/funk-vergleich.py (eine Momentaufnahme) und
      tools/box/funkstrecken-sonde.py (Dauermessung als systemd-Dienst).
    body: >
      DER BEFUND, DER DIE REGEL ERZWANG - zwei methodisch richtige Messungen,
      zehn Minuten auseinander:

          22:4x   wlan0  50 % Verlust      wlan1   0 %
          23:1x   wlan0   0 % Verlust      wlan1  10 %

      Wer daraus eine Empfehlung ableitet, liest Rauschen. Die Frage "welcher
      Adapter ist besser" laesst sich an dieser Box in Sekunden nicht
      beantworten - sie braucht Runden ueber Stunden.

      EMPFANG UND STRECKE SIND ZWEI VERSCHIEDENE FRAGEN. Die Sonde haelt je
      Runde BEIDES fest: Empfangspegel und tatsaechlichen Verlust. Erst die
      Gegenueberstellung ueber viele Runden zeigt, ob der bessere Empfang auch
      der bessere Weg ist - an diesem Abend war er es zweimal nicht. Der
      Momentanvergleich (funk-vergleich.py) beantwortet nur die erste Frage:
      der Stick hoerte den Hauptsender auf 2,4 GHz mit -53 statt -67 dBm
      (14 dB) und einen vierten Sender, den der eingebaute gar nicht wahrnimmt.

      SIE LAEUFT AUF DER BOX, nicht vom Arbeitsrechner - das ist keine
      Bequemlichkeit, sondern Bedingung: siehe
      [[rueckweg-verfaelscht-die-messung]]. Deshalb `ping -I`, das bindet hin
      UND zurueck.

      SIE SCHREIBT NACH /home/dietpi/funkstrecken.jsonl, also unter /home und
      NICHT in den App-Ordner: eine Auslieferung tauscht dort Baeume aus, und
      die Messung waere weg. Eine Zeile je Schnittstelle und Runde mit fsync,
      damit ein Abbruch hoechstens die letzte Runde kostet.

      KEIN WERKZEUG STELLT UM. Der Adapter, an dem die aktuelle Verbindung
      haengt, wird aus der Vorgabewegtabelle erkannt und in Ruhe gelassen - wer
      die Leitung kappt, auf der er sitzt, kommt nicht mehr an die Box. Ein
      schlafender Adapter wird mit --anwerfen kurz geweckt, horcht, und wird
      danach wieder hingelegt, wie er lag.

      SCHWELLEN, DIE EIN NICHTERGEBNIS AUSSPRECHEN: unter 5 dB Unterschied im
      Empfang und unter ein paar Prozentpunkten im Verlust sagen beide
      Werkzeuge ausdruecklich, dass das KEINE Entscheidung ist. So viel
      schwankt zwischen zwei Umschauen am selben Platz.

      "active" IST KEIN BELEG: nach dem Einrichten als Dienst wurde am Geraet
      nachgesehen, dass sie auch WIRKLICH schreibt.
    related: ["rueckweg-verfaelscht-die-messung", "feldstaerke-ist-kein-wissen-sondern-ein-messwert", "einmal-hinsehen-ist-keine-messung", "werkzeug-statt-wegwerfbefehl"]
  - id: befehl-ohne-rechte-und-rueckruf-ohne-blick
    kind: falle
    title: "Der WLAN-Umschalter hat nie geschaltet und trotzdem ok gemeldet - ifdown lag nicht im PATH"
    match: "ifdown not found|wlan-adapter schaltet nicht|ok true aber nichts passiert|execFile ohne sudo"
    sev: high
    tags: ["backend", "netz", "falle", "diagnose", 2026]
    source: >
      21.08.2026 am Geraet gemessen. Der Betreiber hatte am Vorabend in der
      Verwaltung umgeschaltet und gefragt: „warum erreiche ich die box auf der
      anderen ip nicht obwohl ich umgestellt habe". Er hatte umgestellt; es war
      nur nichts passiert.
    body: >
      ══ BERICHTIGT AM 21.08.2026, WENIGE STUNDEN SPAETER ══

      DIE URSACHE UNTEN IST FALSCH. Es IST ein Rechteproblem. Der PATH des
      Dienstes wurde nachgemessen — aus dem laufenden Prozess, nicht aus einer
      SSH-Sitzung:

          PATH des Dienstes:   /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin
          PATH der Sitzung:    /usr/local/bin:/usr/bin:/bin:/usr/games
          /usr/sbin/ifdown     existiert

      DER DIENST FINDET ifdown. Das `not found` unten stammt aus der
      SSH-Sitzung, in der gemessen wurde. Die tragende Probe liefert
      `failed to open lockfile /run/network/ifstate.…: Permission denied`,
      mit `sudo -n` rc=0. Alles Weitere in
      [[der-eigene-eintrag-warnte-vor-genau-dieser-fehlmessung]]; der Fall war
      seit dem 07.08. vollstaendig beschrieben in
      [[endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok]], samt der Warnung vor
      genau dieser Fehlmessung.

      DER FIX (`sudo -n`) BLEIBT RICHTIG — nur die Begruendung war es nicht.
      Alles ab hier gilt weiter, AUSSER dem Absatz „NICHT EINMAL EIN
      RECHTEPROBLEM".

      ══ URSPRUENGLICHE FASSUNG ══

      POST /api/wlan-adapter fuehrte aus:

          sh -c "ifup wlan1 2>/dev/null; sleep 8; … ifdown wlan0 2>/dev/null"

      und zwar mit dem Rueckruf `() => undefined`.

      AM GERAET NACHGEMESSEN, was dabei herauskommt:

          $ sh -c "ifdown wlan1"
          sh: 1: ifdown: not found        rc=127

      NICHT EINMAL EIN RECHTEPROBLEM. Der Server laeuft als `dietpi`, und
      /sbin steht in dessen PATH nicht drin. Mit `sudo` geht es sofort - dessen
      secure_path enthaelt /sbin. Alle anderen privilegierten Aufrufe im
      Server benutzen `werkzeug('sudo', …)`; nur diese eine Route nicht.

      ZWEI FEHLER, EINE URSACHE - und der zweite ist der schlimmere:

        1. Der Befehl hatte keine Rechte.
        2. `2>/dev/null` UND ein Rueckruf, der nichts ansieht, machten den
           ersten unsichtbar.

      Die Route antwortet ABSICHTLICH vor dem Schalten (sonst liefe der Browser
      in einen Fehler, weil die Umstellung die Verbindung kappt). Damit war
      `{ok:true}` die einzige Rueckmeldung, die es je gab - unabhaengig davon,
      ob etwas geschah.

      DIE LEHRE: Ein Rueckruf, der nicht hinsieht, ist keine Nachlaessigkeit,
      sondern eine Tarnkappe fuer den Fehler daneben. Wo ein Befehl die
      Antwort nicht mehr tragen kann, gehoert sein Ausgang ins Protokoll -
      auch der gute, sonst weiss man nicht, ob geschwiegen wurde oder nichts
      geschah.

      NEBENBEFUND, der zwanzig Minuten gekostet hat: `journalctl -u dienst`
      OHNE sudo meldet auf dieser Box 0 Zeilen, weil `dietpi` nicht in der
      Gruppe `adm` ist. Der Hinweis steht in der Ausgabe („You are currently
      not seeing messages from other users"), aber nur, wenn man ihn nicht
      wegfiltert. 0 Zeilen heisst hier NICHT „es wird nichts protokolliert".
    related:
      - der-eigene-eintrag-warnte-vor-genau-dieser-fehlmessung
      - endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok
      - behoben-in-ist-eine-zustandsaussage-und-war-falsch
      - gegenprobe-statt-gruen-glauben
      - einmal-hinsehen-ist-keine-messung
  # ══ Was ein Eintrag behauptet, und was am Geraet steht (21.08.2026) ══════
  - id: behoben-in-ist-eine-zustandsaussage-und-war-falsch
    kind: regel
    title: "Der Eintrag sagte \"behoben in server.ts\" - der Fehler stand dort noch 13 Tage spaeter, und der Betreiber lief hinein"
    sev: high
    tags: ["wiki", "haltbarkeit", "wlan", "netzwerk", "verwaltung", "regel", 2026]
    source: >
      21.08.2026, stuendlicher Wiki-Abgleich. Nachgemessen an der Box
      (192.168.178.194, wlan1). Betrifft
      [[endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok]] vom 07.08.2026.
    body: |
      DER FUND: Der Eintrag vom 07.08. traegt

          source: "… behoben in src/backend-api/src/server.ts,
                   Test wlan-adapter.integration.spec.ts"

      Beides stimmte nicht. Am 21.08. stand in server.ts unveraendert

          `ifup ${neu} 2>/dev/null; … ifdown ${alt} 2>/dev/null;` , () => undefined

      also GENAU der Code, den der Eintrag als behoben ausweist. Eine Datei
      `wlan-adapter.integration.spec.ts` gibt es im Baum nicht und hat es nie
      gegeben. Die Ursachenanalyse war richtig und vollstaendig — nur
      angewandt wurde sie nie.

      WAS DAS GEKOSTET HAT: 13 Tage. Am 20.08. fragte der Betreiber „warum
      erreiche ich die box auf der anderen ip nicht obwohl ich umgestellt
      habe". Er hatte umgestellt; es passierte nichts — derselbe Fehler, den
      das Pack seit dem 07.08. als erledigt fuehrte. Wer den Eintrag las,
      strich die Stelle aus der Suche.

      ── WARUM DAS SCHLIMMER IST ALS EINE LUECKE ─────────────────────────

      Eine Luecke schickt niemanden weg. Ein falsches „behoben" schickt jeden
      weg, der sonst hingesehen haette — es wirkt wie eine Messung und ist
      eine Absicht. Dieselbe Bauart wie der Fehler, den derselbe Eintrag
      beschreibt: `{ok:true}` VOR der Wirkung. Der Eintrag ist seinem eigenen
      Gegenstand aufgesessen.

      ── DIE REGEL ───────────────────────────────────────────────────────

      In einem Eintrag sind URSACHE und FIX zweierlei Ware:

        * Die Ursache ist ein Grund. Sie gilt weiter, siehe
          [[wiki-zustand-hat-haltbarkeit]].
        * „behoben in <Datei>" ist eine ZUSTANDSAUSSAGE ueber einen Baum.
          Sie verfaellt, und sie war hier von Anfang an falsch.

      Wer sie hinschreibt, hat sie vorher zu MESSEN — den Baum lesen, den
      Test laufen lassen — oder sie anders zu formulieren:

          fix:    was zu tun ist                    (haltbar)
          stand:  "07.08.: erkannt, NICHT behoben"  (datiert, ehrlich)

      EIN GENANNTER TEST WIRD GEGENGEPRUEFT. `ls` auf den Dateinamen kostet
      nichts. Ein Test, den es nicht gibt, ist der billigste Weg, eine
      Behauptung glaubwuerdig zu machen — und der teuerste beim Aufraeumen.

      OFFEN GEBLIEBEN, auch nach dem Fix vom 21.08.: der Eintrag verlangt
      „`sudo -n true` -> geht nicht? dann 503, nicht ok". Die neue Fassung
      protokolliert den Ausgang, prueft aber VOR der Antwort weiterhin
      nichts. Der Endpunkt antwortet also weiter `{ok:true}`, bevor er weiss,
      ob er darf. Das ist kein Versehen dieser Sitzung, sondern ein
      halbierter Fix — hier notiert, damit er nicht wieder als ganzer gilt.
    related:
      - endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok
      - wiki-zustand-hat-haltbarkeit
      - der-eigene-eintrag-warnte-vor-genau-dieser-fehlmessung
      - aufraeumen-nachmessen
  - id: der-eigene-eintrag-warnte-vor-genau-dieser-fehlmessung
    kind: falle
    title: "\"ifdown: not found\" kam aus der SSH-Sitzung, nicht vom Dienst - die Falle stand seit dem 07.08. wortwoertlich im Pack"
    sev: high
    match: "ifdown: not found|ifup: not found|rc=127|command not found.*ifup|/sbin steht nicht im PATH"
    cause: "Die SSH-Sitzung des Benutzers dietpi hat kein sbin im PATH, der systemd-Dienst schon. Wer in der Sitzung misst, bekommt 'not found' statt der Rechtefrage — und haelt einen PATH-Fehler fuer die Ursache."
    fix: "Den PATH des DIENSTES lesen (`tr '\\0' '\\n' < /proc/$(systemctl show <unit> -p MainPID --value)/environ | grep ^PATH=`), nicht den der Sitzung. Fuer die Rechtefrage die Probe mit eigener Schnittstellendatei fahren."
    tags: ["messen", "rechte", "sudo", "wlan", "netzwerk", "path", "falle", 2026]
    source: >
      21.08.2026 an der Box 192.168.178.194 nachgemessen. Anlass: die
      Begruendung, die in server.ts und wlan-umschalten.spec.ts geschrieben
      wurde, benennt die falsche Ursache.
    body: |
      DIE BEHAUPTUNG, die am 21.08. in Quelltext, Test, Commit-Titel
      (981686bd „ifdown lag nicht im PATH") UND als eigener Pack-Eintrag
      [[befehl-ohne-rechte-und-rueckruf-ohne-blick]] festgeschrieben wurde:

          „Nicht einmal ein Rechteproblem — der Server laeuft als `dietpi`,
           und /sbin steht in dessen PATH nicht."

      AM GERAET GEMESSEN, dieselbe Stunde:

          PATH des Dienstes (aus /proc/<MainPID>/environ):
              /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin
          PATH der SSH-Sitzung:
              /usr/local/bin:/usr/bin:/bin:/usr/games

          /usr/sbin/ifdown  ->  existiert (Verweis auf ifup)

      DER DIENST FINDET ifdown. Er hat /usr/sbin im PATH. Das `not found`
      stammt aus der SSH-Sitzung, in der die Messung lief — und dort fehlt
      sbin, weil dietpi kein Login mit sbin-Pfad bekommt.

      DIE ECHTE URSACHE, mit der Probe aus dem Eintrag vom 07.08. bestaetigt:

          $ /sbin/ifdown --force -i /tmp/probe.interfaces mupi-probe0
          ifdown: failed to open lockfile /run/network/ifstate.mupi-probe0:
                  Permission denied            rc=1
          $ sudo -n /sbin/ifdown … mupi-probe0
                                               rc=0
          /run/network                         drwxr-xr-x root root

      Es IST ein Rechteproblem, genau wie am 07.08. beschrieben.

      ── WAS DARAN LEHRREICH IST ─────────────────────────────────────────

      [[endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok]] warnt seit dem
      07.08. woertlich davor, unter der Ueberschrift „ZWEI FRAGEN, DIE MAN
      NICHT VERWECHSELN DARF": „Wer in der Sitzung misst, misst das Falsche
      und bekommt ‚command not found' statt der Rechtefrage." Die Messung vom
      21.08. lief in die Falle, die das Pack als einzige zu dieser Route
      fuehrt — weil der Eintrag nicht gelesen wurde, und der wurde nicht
      gelesen, weil er sich selbst als erledigt auswies (siehe
      [[behoben-in-ist-eine-zustandsaussage-und-war-falsch]]).

      ── DER FIX IST RICHTIG, DIE BEGRUENDUNG NICHT ──────────────────────

      `sudo -n` gehoert davor — aus dem Rechtegrund, nicht aus einem
      PATH-Grund. Beides fuehrt zum selben Befehl, und GERADE DESHALB ist
      die falsche Begruendung gefaehrlich: nichts wird rot. Sie steht in
      einem Kommentar und in einem Test, also an den beiden Stellen, die der
      naechste Leser fuer belegt haelt. Wer spaeter den PATH des Dienstes
      „repariert", nimmt sudo als ueberfluessig heraus — und der Fehler ist
      zurueck.

      MERKSATZ: Zwei Ursachen, die denselben Fix verlangen, sind nicht
      dieselbe Ursache. Welche zutrifft, entscheidet erst der naechste
      Umbau — und dann liest jemand den Kommentar.

      ── DIE MESSUNG, DIE TRAEGT, in einer Zeile ─────────────────────────

          PID=$(systemctl show mupibox-server.service -p MainPID --value)
          sudo -n tr '\0' '\n' < /proc/$PID/environ | grep ^PATH=

      Der PATH eines Dienstes steht NICHT in der Unit und NICHT in der
      Sitzung. Er steht im laufenden Prozess.
    related:
      - endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok
      - behoben-in-ist-eine-zustandsaussage-und-war-falsch
      - befehl-ohne-rechte-und-rueckruf-ohne-blick
      - gegenprobe-statt-gruen-glauben
      - einmal-hinsehen-ist-keine-messung
  # ══ Ein erhobener Messwert, der beim Urteil fehlte (21.08.2026) ══════════
  - id: erhoben-aber-nicht-gewertet-ist-schlimmer-als-nie-gemessen
    kind: regel
    title: "Die Sonde sammelte die Laufzeit 9,7 Stunden lang und wertete nur den Verlust - Faktor 39 lag in der Tabelle und stand in keinem Urteil"
    tags: ["messen", "werkzeug", "wlan", "sonde", "urteil", "regel", 2026]
    source: >
      21.08.2026, 148 Runden aus /home/dietpi/funkstrecken.jsonl an der Box
      (Beginn 1787261076, Ende 1787296007 — 9,7 Stunden). Werkzeug:
      tools/box/funkstrecken-sonde.py, Auswertung nachgezogen.
    body: |
      DER BEFUND: Nach 9,7 Stunden Dauermessung standen die beiden Adapter
      beim VERLUST praktisch gleich — 7,6 % gegen 5,5 %, und das Werkzeug
      sagt selbst, dass unter ein paar Prozentpunkten keine Strecke besser
      ist. Danebengestanden hat, unbeachtet:

          Laufzeit im Mittel   wlan0  ~195 ms
                               wlan1    ~5 ms

      Faktor 39, im eigenen LAN. Die Sonde hat `ms_min`, `ms_mittel` und
      `ms_max` von der ersten Runde an je Schnittstelle mitgeschrieben — und
      beim Urteil ausschliesslich den Verlust betrachtet.

      ── WARUM DAS EINE EIGENE REGEL WERT IST ────────────────────────────

      EIN WERKZEUG, DAS EINE ZAHL ERHEBT UND BEIM URTEIL WEGLAESST, IST
      SCHLIMMER ALS EINES, DAS SIE NIE ERHOBEN HAT. Der Wert steht in der
      Tabelle. Er ist damit sichtbar, wirkt beruecksichtigt, und niemand
      fragt nach ihm — weder der, der die Zusammenfassung liest, noch der,
      der das Werkzeug spaeter erweitert. Eine fehlende Spalte faellt auf;
      eine erhobene, aber stumme nicht.

      ── VERLUST UND LAUFZEIT WIEGEN NICHT GLEICH ────────────────────────

      Sie brauchen verschiedene Schwellen, und das ist der Grund, warum das
      Weglassen hier nicht neutral war:

        * VERLUST: verlorene Pakete holt TCP zurueck. Das kostet Bandbreite
          und faellt bei ein paar Prozent nicht auf. Unter ein paar
          Prozentpunkten Unterschied ist es Rauschen — siehe
          [[eine-funkmessung-ist-keine-funkmessung]].
        * LAUFZEIT: kostet JEDE Anfrage, einzeln und immer. 195 ms Umlauf im
          eigenen LAN ist der Grund, warum sich eine Box „zaeh" anfuehlt und
          warum Handreichungen ins Zeitlimit laufen. Ein VIELFACHES ist hier
          kein Rauschen, sondern das Ergebnis.

      Die Sonde spricht das jetzt aus (Schwelle: Faktor 3) und sagt dazu,
      welche der beiden Zahlen schwerer wiegt. Die Entscheidung trifft sie
      weiterhin nicht.

      ── EINE FALLE BEIM NACHRECHNEN ─────────────────────────────────────

      `ms_mittel` ist schon je Runde ein Mittel, und die Runden tragen
      Ausreisser: die allererste Zeile steht mit `ms_min 3,0 / ms_mittel
      686 / ms_max 2561`. Ein Mittel ueber Mittel gewichtet solche Runden
      voll durch. Der Faktor 39 ist deshalb robust in der RICHTUNG, nicht
      auf die Stelle genau. Wer eine genaue Zahl braucht, nimmt den Median
      ueber `ms_min`. Fuer die Frage „welcher Adapter" reicht die Richtung.

      ── DIE ALLGEMEINE FORM ─────────────────────────────────────────────

      Beim Bau eines Messwerkzeugs zaehlt man am Ende die erhobenen Felder
      gegen die im Urteil verwendeten. Was uebrig bleibt, ist entweder
      unnoetig erhoben — dann weg — oder es fehlt im Urteil. Ein drittes
      gibt es nicht.
    related:
      - eine-funkmessung-ist-keine-funkmessung
      - werkzeug-statt-wegwerfbefehl
      - einmal-hinsehen-ist-keine-messung
      - mit-echten-werten-rechnen
  - id: zwei-schnittstellen-ein-teilnetz-arp-flux
    kind: falle
    title: "Box pingt unter der zweiten Adresse, nimmt dort aber keine TCP-Verbindung an"
    match: "ping geht ssh nicht|zwei wlan gleichzeitig|arp_ignore|zweite adresse erreichbar aber|ifdown nach neustart weg"
    sev: high
    tags: ["netz", "falle", "mixpi", "diagnose", 2026]
    source: >
      21.08.2026 an der Box gemessen, nachdem wlan0 und wlan1 beide oben waren.
    body: >
      DAS BILD: Die Box antwortet unter der Adresse des USB-Sticks auf Ping,
      sauber und schnell — aber SSH laeuft ins Zeitlimit. Andere Dienste
      ebenso. Unter der Adresse des eingebauten Funks geht alles.

      DIE URSACHE IST ARP-FLUX. `arp_ignore` steht auf dieser Box ueberall auf
      0. Damit beantwortet JEDE Schnittstelle ARP-Anfragen fuer JEDE Adresse
      des Rechners — auch fuer die der anderen Karte. Der Router traegt die
      Adresse des Sticks dann mit der MAC der falschen Karte ein, schickt
      Pakete dorthin, und die Antwort geht ueber die andere hinaus. Ein
      einzelnes ICMP-Paket uebersteht das haeufig; ein TCP-Handschlag mit
      seinen drei Schritten nicht.

      WER DARAUS EINE MESSUNG ABLEITET, MISST UNSINN. Genau daran ist am
      20.08. die Bewertung des USB-Sticks gescheitert (siehe
      [[rueckweg-verfaelscht-die-messung]]): der Stick schien 89 % Verlust zu
      haben und war in Wahrheit der bessere Weg.

      DIE BEHEBUNG IST NICHT arp_ignore, SONDERN EINDEUTIGKEIT: nur eine
      Schnittstelle im Teilnetz oben lassen. POST /api/wlan-adapter macht das
      mit `ifdown` — aber das haelt nur bis zum Neustart, weil in
      /etc/network/interfaces `allow-hotplug` fuer beide steht.

      DESHALB mixpi-wlan-adapter: /etc/mupibox/mixpi-wlan-adapter haelt die
      Wahl, die gleichnamige Unit wendet sie nach dem Start an. Die Datei wird
      ERST GESCHRIEBEN, WENN DER WECHSEL BELEGT IST (die Ausgabe enthaelt
      „UMGESCHALTET") — eine Absicht festzuschreiben statt eines Ergebnisses
      hiesse, beim naechsten Start eine Schnittstelle hinzulegen, weil jemand
      einmal auf einen wirkungslosen Knopf gedrueckt hat.

      DIE REGEL IM SKRIPT, die nicht verhandelbar ist: Es wird NIE die letzte
      funktionierende Schnittstelle abgeschaltet. Steht „extern" in der Datei,
      hat wlan1 aber keine Adresse (Stick abgezogen), passiert NICHTS. Eine
      Box, die sich beim Start selbst aussperrt, ist schlimmer als eine mit
      zwei Schnittstellen oben — an die erste kommt niemand mehr heran.
    related: ["rueckweg-verfaelscht-die-messung", "befehl-ohne-rechte-und-rueckruf-ohne-blick"]

  - id: mixpi-tragbar-der-entwurf
    kind: architektur
    title: "Tragbare MixPi: die Umkehrung, und die zwei Grenzen, die sich gegenseitig aufheben"
    match: "tragbare Box|tragbare MixPi|MixPi portable|MuPiBox2|192.168.178.99|ESP-NOW|Kinderknoten|Funkbudget|Knoten ohne Schirm|ESP32-C6"
    sev: med
    tags: ["mixpi", "tragbar", "hardware", "funk", "knoten", "entwurf", 2026]
    source: >
      21.08.2026, Gespraech mit dem Betreiber. Anlass: E72 traegt zwei Stroeme
      ueber zwei Senken — daraus die Frage nach einer TRAGBAREN Box (moeglichst
      wenig: Platine, Akku, Bluetooth-Stick, KEINE Lautsprecher an Bord, das
      loesen mitgebrachte BT-Lautsprecher) und nach kleinen, bewusst dummen
      Geraeten, mit denen Kinder ihren Strom steuern. Hardware-Angaben aus den
      Herstellerseiten, Zustandsangaben aus dem Repo. NICHTS DAVON IST AM
      TRAGBAREN GERAET GEMESSEN — es gibt noch keins.
    body: >
      DIE UMKEHRUNG, um die es geht: bei der Standbox haengen Schirm und
      Bedienung AN der Box. Tragbar wird die Box zur Tonzentrale ohne Gesicht,
      und die Knoten SIND die Bedienung. Das macht die Knoten wichtiger, nicht
      unwichtiger.

      ══ ZWEI GRENZEN, DIE SICH AUFHEBEN ════════════════════════════════════

      Vorhanden sind ein Pi 4 und ein MuPiHAT, der „nur ohne PD laedt".
      Einzeln sind beide zweite Wahl, zusammen passen sie:

        * Die Eingangsstromgrenze des HAT liegt bei 1,75 A bzw. 2,2 A — der
          Wert, aus dem `hatdiagnose.ts` den PLATINENSTAND ableitet, weil die
          Chip-Kennung (REG 0x48) auf beiden Platinen 0x08 war und nichts
          taugt. Bei 5 V sind das rund 9 bis 11 W am Eingang.
        * Ein Pi 4 zieht 3–4 W. Bleiben 5–7 W zum Laden IM BETRIEB.
        * Ein Pi 5 mit 6–7 W haette genau diese Reserve aufgefressen — er
          wuerde am schwachen Eingang im Betrieb gar nicht mehr laden.

      Zum Defekt selbst, VERMUTUNG, nicht gemessen: eine CC-Leitung. Eine
      PD-Quelle schaltet VBUS erst ein, wenn sie am CC-Pin den richtigen
      Pull-down sieht; ein dummes 5-V-Netzteil fragt nicht. Genau das ergibt
      „laedt ohne PD, nicht mit PD". Nachsehen laesst sich das ohne Loetkolben:
      `/api/mupihat/diagnose` fuehrt die hoechste je gesehene Vbus mit (ueber
      5500 mV heisst „PD hat ausgehandelt") und decodiert VBUS_STAT im
      Klartext. Der MuPiHAT hat dafuer sogar eine eigene LED: CAP_MIS.

      ══ DER EIGENTLICHE BLOCKER IST NICHT DIE HARDWARE ═════════════════════

      Der Pi 4 ist kein leeres Ersatzteil. Er steht als MuPiBox2 auf
      192.168.178.99 in `remote-step-installer/boxen.yaml`, und
      ANGLEICH-PI4-PI5.md (30.07.2026) haelt fest, was ihm fehlt:

        * PulseAudio 17.0, KEIN PipeWire, kein WirePlumber, kein `wpctl` — und
          woertlich: „Der Pi 4 hat ueberhaupt keinen Bluetooth-Tonweg", weder
          bluealsa noch PipeWire-A2DP, `pulseaudio-module-bluetooth` fehlt
          auch. Das ganze Stroeme-Konzept steht auf PipeWire-Senken und
          `pw-link`. Auf dieser Box ist davon NICHTS da — das ist kein
          Konfigurationsthema, der Stapel fehlt.
        * Kein librespot, kein mpv. `spotify-control.js` ist 777 KB kleiner,
          also der Baustand VOR der librespot-Anbindung: ein anderer
          Wiedergabeweg, nicht bloss eine fehlende Datei.
        * Die Reihenfolge ist nicht frei: Kiosk als `dietpi` statt als root
          (Ausfall 4) ist die VORAUSSETZUNG fuer PipeWire als Benutzerdienst
          und fuer die librespot-Unit (`User=dietpi`,
          `XDG_RUNTIME_DIR=/run/user/1000`). Also 4 → 3 → 1.

      Und: der Probelauf gegen den Pi 4 wurde am 30.07. BEWUSST
      zurueckgestellt. Die Rezepte sind umgebaut und geprueft
      (remote-step-installer `b4be1d1`), aber nie gegen diese Box belegt. Das
      tragbare Vorhaben gibt dem zurueckgestellten Punkt endlich einen Grund —
      aber GETRENNT fahren. Sonst weiss man bei einer misslungenen
      Zwei-Strom-Messung nicht, ob der Funk oder der fehlende Tonstapel schuld
      war.

      GUTE NACHRICHT nebenbei: auf dem Pi 4 sitzt schon ein MuPiHAT und er
      ANTWORTET — `i2cdetect -y 1` zeigt 0x6b. Nur `mupi_hat.service` und
      `mupi_hat_control.service` sind disabled, weil das Rezept die
      `mupi_*`-Dienste pauschal abschaltet, statt die Hardware zu pruefen.

      ══ DAS FUNKBUDGET IST DER ENTWURF ═════════════════════════════════════

      Unterwegs gibt es keine Infrastruktur, und alles liegt in 2,4 GHz: zwei
      A2DP-Stroeme raus, Internet rein (der Handy-Hotspot ist ein weiterer AP
      direkt daneben), die Kinderknoten rein.

      Gemessen ist bisher nur: zwei gleichzeitige A2DP-Stroeme auf EINEM
      Funkbaustein, 180 Sekunden, keine Aussetzer, WLAN unveraendert
      (`stroeme.ts`, 20.08.2026). Das war im Stand, am eigenen WLAN, auf der
      Pi-5-Box. Es traegt WEDER auf den Pi 4 (anderer Funkbaustein) NOCH auf
      die Strasse.

      Daraus die Regel fuer die Knoten: WEDER ueber WLAN NOCH ueber denselben
      Bluetooth-Baustein, der A2DP macht.

        * WLAN-AP auf wlan1 ginge (der Adapter ist oben), kostet aber Funkzeit
          und ein bis drei Sekunden Anmeldung aus dem Kaltstart — die falsche
          Reaktionszeit fuer einen Knopf, den ein Kind drueckt.
        * BLE auf dem A2DP-Baustein ist genau die Koexistenz, die als Ursache
          ohnehin schon verfolgt wird.
        * ESP-NOW mit einem kleinen ESP32 als Bruecke am USB der Box: keine
          Anmeldung, kein AP, keine Infrastruktur — funktioniert im Auto.
          Pakete winzig und selten, Funkzeit vernachlaessigbar. Der Knoten
          wacht auf, sendet, schlaeft: Laufzeit in Monaten. Der Pi kann
          ESP-NOW nicht selbst, daher der eine Stecker.
        * Das Bluetooth-Staebchen aus dem urspruenglichen Entwurf ist richtig:
          A2DP runter vom Kombibaustein des Pi 4.

      ══ DIE KNOTEN SIND KEIN BILDSCHIRM ════════════════════════════════════

      Was ein Kind unterwegs braucht: Pause, weiter, lauter/leiser — das sind
      Knoepfe. Nur beim AUSSUCHEN wuerde ein Schirm helfen, und dafuer gibt es
      die bessere Antwort seit Jahren: RFID-Marken. Tonuino, Phoniebox und die
      Toniebox leben davon, weil man nichts lesen koennen und nichts treffen
      muss und es in die Hand nehmen kann.

      Also: drei bis vier grosse Knoepfe, ein RFID-Leser, ein LED-Ring, KEIN
      Display. Faellt runter und lebt weiter.

      Damit bekommt das Waveshare ESP32-C6-Touch-AMOLED-1.8 (368×448) seinen
      richtigen Platz: STATIONAER zu Hause, nicht in der Tasche. Und sein
      RAM-Problem faellt ersatzlos weg. Es waere sonst der Knackpunkt:

        * ESP32-C6 hat 512 KB SRAM und KANN kein PSRAM (die Schnittstelle
          fehlt dem Chip, anders als beim S3). Ein volles Bild in RGB565 sind
          368·448·2 = 322 KB. LVGL geht dort nur mit Teilpuffer.
        * Das ist keine Theorie: die fertigen Panels des Oekosystems
          (ESPHome Media Player) unterstuetzen ausschliesslich S3- und
          P4-Boards MIT PSRAM. Aus der HA-Runde sinngemaess: Cover-Anzeige
          braucht genug PSRAM, fehlt sie, ist der RAM die Ursache.
        * ZWEI HARDWARESTAENDE, und man waehlt nicht: V1 mit SH8601-Display
          und FT3168/FT6146-Touch, V2 mit CO5300 und CST820. Anderer
          Displaytreiber UND anderer Touch-Chip.

      Was das Board sehr wohl hat (und im Gespraech zuerst falsch verneint
      wurde): ES8311-Codec mit Lautsprecher und Mikrofon, AXP2101-PMIC mit
      MX1.25-Akkubuchse und Ladefunktion, dazu einen PCF85063-RTC mit Pads
      fuer eine Stuetzbatterie. „No Batt" heisst nur: keine Zelle beiliegend.

      Ebenfalls zu weit war die Aussage „Spotify Connect gibt es auf ESP32
      nicht". Es gibt es: cspot, eingebaut in squeezelite-esp32. Nur zielt das
      auf ESP32 und ESP32-S3 MIT PSRAM, nicht auf den C6 — fuer DIESES Board
      gilt die Aussage also doch.

      ══ DIE VERSORGUNG DER TRAGBAREN BOX ═══════════════════════════════════

      Unterwegs gibt es kein Internet. Lokale Dateien sind dort keine
      Alternative zu Spotify, sondern das Einzige, was ueberhaupt geht. „Box B
      holt von Box A" ist deshalb NICHT Bandbreitensparen (das waere marginal),
      sondern VERSORGUNG: vor der Fahrt nimmt die tragbare Box einen Ausschnitt
      der Mediathek mit.

      DIE FALLE, DIE MAN DABEI ERBT: `abgleich.ts` beantwortet schon „welche
      Werke sind dasselbe?", warnt aber selbst, dass Stufe 1 genau dort irrt,
      wo es weh tut — Deluxe, Remaster und Live tragen denselben Interpreten
      und denselben Titel, und der Schutz waere die TITELANZAHL, die es noch
      nicht gibt. Holt Box B „denselben Titel" per Namensvergleich, bekommt das
      Kind eine ANDERE Aufnahme als die, die es ausgesucht hat. Also exakt
      ueber den Schluessel abgleichen, nicht ueber einen Namensvergleich.

      Spotify-Inhalte lassen sich nicht weiterreichen — librespot
      entschluesselt den Strom, ihn weiterzugeben waere Weiterverbreitung
      geschuetzten Materials. Zu holen gibt es dort auch nichts, beide Boxen
      haben ihr eigenes Konto. Es geht also um lokale Dateien, Mitschnitte,
      ARD Audiothek und Jellyfin — genau den Offline-Fall.

      ══ WAS ZUERST GEMESSEN GEHOERT, UND WARUM NICHTS GEKAUFT WIRD ═════════

      `tools/box/strom-messen.py` liest ueber `/api/mupihat`, also ueber den
      BQ25792. OHNE HAT GIBT ES KEINE VERBRAUCHSMESSUNG. Und genau die
      entscheidet, welcher Pi und welche Kapazitaet ins tragbare Geraet
      gehoeren.

      Also mit dem vorhandenen HAT den TRAGBAREN Betriebsfall messen: zwei
      Stroeme, kein Heim-WLAN, Hotspot als Nachbar-AP, auf Akku, ueber eine
      Stunde statt ueber drei Minuten. Danach ist „welcher Akkubaustein"
      Rechnen statt Raten.

      Zu den USV-Alternativen, falls es doch soweit kommt: der MuPiHAT+ kostet
      60 € (BQ25792, USB-C PD bis 4 A, 2S 7,2–7,4 V, dazu zwei 3-W-Endstufen),
      der Geekworm X1209 45 $ (5,1 V bis 6 A, Maxim-Fuel-Gauge ueber I2C,
      GPIO-Netzausfallerkennung, Zellen extern). Rund 15 € Unterschied — DER
      AUDIOTEIL IST NICHT DER KOSTENTREIBER. Und der Stolperstein ist die
      Topologie: MuPiHAT ist 2S, der X1209 laedt auf 4,23 V Schlussspannung,
      also 1S. Vorhandene 2S-Packs passen dort nicht.

      Ein Wechsel kostet die Lesernaht: `hatdiagnose.ts` (Register 0x20/0x21)
      hat beim Ersatz keine Entsprechung, und `i2cget -y 1 0x6b` steht in
      `server.ts` fest verdrahtet. Er BRINGT aber auch etwas: `akkulernen.ts`
      rekonstruiert den Ladestand nur deshalb muehsam, WEIL der BQ25792 kein
      Fuel-Gauge ist. Mit einem echten Gauge liesse sich das eher loeschen als
      portieren.
    related: ["konten-als-vorrat-nicht-je-box", "mupihat-bricht-i2c-selbst-heraus", "modellnummer-steht-vor-dem-wort-model", "quellen-verschmelzen"]

  - id: konten-als-vorrat-nicht-je-box
    kind: offen
    title: "Konten als VORRAT statt fest je Box — und warum dafuer kein Main gewaehlt wird"
    match: "Konto leihen|Zugang leasen|Kontovorrat|zwei Boxen ein Konto|/v1/me|aufDieserBox|zusammen hoeren|dazukommen|Main aushandeln"
    sev: med
    tags: ["spotify", "stroeme", "mehrere-boxen", "entwurf", "offen", 2026]
    source: >
      21.08.2026, Gespraech mit dem Betreiber, im Anschluss an E72 und an den
      Entwurf der tragbaren Box. Idee des Betreibers: zwei Boxen im selben WLAN
      handeln aus, wer „main" ist, die andere bezieht den Zugang als Client —
      und, falls die Zugaenge VERSCHIEDEN sind, teilen sie gegenseitig. NICHTS
      DAVON IST GEBAUT. Was hier steht, ist der Entwurf samt der einen
      Vorbedingung, die heute fehlt.
    body: >
      DIE IDEE: nicht jede Box haelt ihren festen Satz Konten, sondern beide
      Boxen teilen sich einen VORRAT. Wer einen Strom starten will, nimmt sich
      ein freies Konto.

      ══ DREI KORREKTUREN AM MECHANISMUS ════════════════════════════════════

      1. NICHT DEN SCHLUESSEL VERLEIHEN, SONDERN DAS RECHT ZU STARTEN. Der
         `spak_`-Schluessel ist auf beiden Boxen ohnehin dasselbe Konto. Ihn
         hin- und herzuschieben bringt Geheimnisse auf die Leitung,
         Neuanmeldungen und ungueltige Zwischenspeicher — und gewinnt nichts.
         Knapp ist der WIEDERGABEPLATZ, nicht der Schluessel. Also: jede Box
         haelt alle Schluessel, verhandelt wird nur die Benutzung.

      2. KEINEN MAIN WAEHLEN. Zwei Knoten koennen keine Mehrheit bilden. Eine
         Wahl zwischen genau zwei Boxen ueber eine wacklige Verbindung ist der
         Lehrbuchfall fuer Split-Brain — und der Ausgang ist ausgerechnet der
         Fehler, den man verhindern will: beide halten sich fuer berechtigt,
         beide starten, Uebernahme, die Geschichte des Kindes hoert mitten
         drin auf.

      3. DIE ANTWORT LIEGT SCHON VOR. `server.ts:15409`, `/api/spotify/laeuft`
         liefert `{ aktiv, aufDieserBox }` und bei `:15465` zusaetzlich
         `positionMs`. `aktiv && !aufDieserBox` heisst woertlich: das Konto
         spielt gerade, aber nicht hier. Das ist der Sperrzustand — gelesen
         bei der einen Instanz, die sich nicht spalten kann. SPOTIFY CONNECT
         IST DIE SPERRTABELLE. Drei Sekunden gemerkt, also billig genug fuer
         den Startpfad.

      Nebenwirkung, die den Ausschlag gibt: das funktioniert auch UNTERWEGS,
      solange Netz da ist. Jeder Entwurf, der Box-zu-Box-Verhandlung braucht,
      faellt genau dann aus, wenn die Box tragbar ist. Und ohne Internet ist
      man bei lokalen Dateien, die brauchen weder Konto noch Sperre.

      ══ DIE VORBEDINGUNG, DIE HEUTE FEHLT ══════════════════════════════════

      `pruefen()` in `stroeme.ts` fasst die Stroeme nach der ZEICHENKETTE des
      Schluessels zusammen (`jeSchluessel`). Regel 1 faengt „derselbe Schluessel
      zweimal", Regel 2 die kaputte Form. KEINE VON BEIDEN FAENGT: zwei
      VERSCHIEDENE Schluessel, EIN Konto — den Fall vom 20.08.2026, der
      zweimal zuschlug (siehe [spak-schluessel-traegt-das-konto-nicht]).

      Fuer einen Vorrat ist das toedlich: „vier verschiedene Schluessel"
      koennen drei Konten sein oder zwei. Der Vorrat waere STILL kleiner, als
      er aussieht, und jede Uebernahme kaeme zurueck — jetzt ueber
      Boxgrenzen hinweg, also noch schwerer zu sehen.

      DER FIX ist klein: den Schluessel einmal zu seiner KONTOKENNUNG
      aufloesen. `GET /v1/me` gibt sie zu einem Token zurueck. Im Code stehen
      heute `/v1/me/player`, `/v1/me/player/queue` und `/v1/me/player/devices`
      — `/v1/me` SELBST wird nirgends gerufen. Die Identitaet wird also nie
      erfragt. Kennung neben den Strom legen, in `pruefen()` nach Kennung
      statt nach Zeichenkette zusammenfassen. Dann faengt die Pruefung den
      ECHTEN Fall statt des sichtbaren, und der Vorrat wird ehrlich.

      ══ DER WETTLAUF, DER DANN NOCH BLEIBT ═════════════════════════════════

      Zwei Boxen sehen gleichzeitig „Konto 3 frei" und starten beide. Fenster:
      Sekunden. Zwei billige Gegenmittel statt einer Sperre:

        * jede Box bekommt eine FESTE Reihenfolge ueber den Vorrat — dann
          stossen sie erst zusammen, wenn er wirklich leer ist;
        * nach dem Start einmal `aufDieserBox` nachfragen. Ist es nicht deins,
          hast du verloren und ziehst zurueck.

      ══ WAS ES BRINGT ══════════════════════════════════════════════════════

      Mehr als „ein drittes Kind". Nach E72 ist DER MITSCHNITT EINER DER
      STROEME und konkurriert heute um ein Konto. Mit Vorrat laeuft er auf dem
      Konto der tragbaren Box, solange die zu Hause steht und nichts tut — und
      nimmt den Kindern nichts weg.

      ══ ZUSAMMEN HOEREN STEHT AUF DEMSELBEN FUSS ═══════════════════════════

      Spotify kennt kein „zwei Geraete, ein Konto, derselbe Inhalt". Zusammen
      hoeren heisst ZWEI KONTEN — der Vorrat ist also auch dafuer die
      Vorbedingung. Dahinter stecken zwei Dinge, die technisch nichts
      miteinander zu tun haben:

      LOSE: gleicher Titel, ungefaehr gleiche Stelle, jeder auf seinem
      Lautsprecher. Ein paar hundert Millisekunden Versatz merkt niemand,
      SOLANGE DIE BEIDEN SICH NICHT GEGENSEITIG HOEREN (zwei Kinder mit je
      eigenem Kopfhoerer im Auto: voellig egal). „Komm dazu" ist dann klein:
      die andere Box nach `laeuft` fragen, URI und `positionMs` nehmen, ein
      freies Konto aus dem Vorrat, starten, plus ein kleiner Vorlauf fuer die
      Anlaufzeit. `weiterhoeren` mit `resume.json` ist die Maschinerie fuer
      „starte X an Stelle P" und seit E18 pro Kind gebunden.

      ENG: dasselbe Audio, im selben Raum, hoerbar synchron. DIE GRENZE IST
      A2DP, NICHT DIE SOFTWARE. BT-Lautsprecher puffern 100–250 ms, jeder Typ
      anders, und der Wert liegt nicht fest. Zwei Boxen heisst zwei Uhren, das
      ergibt DRIFT, und Drift laesst sich ueber A2DP nicht wegregeln (Snapcast,
      AirPlay 2 und Sendspin holen ihre Mikrosekunden alle ueber eigene
      Transporte). EINE Box mit zwei Senken hat dagegen keine Drift, sondern
      nur einen FESTEN Versatz je Lautsprecher — und der ist korrigierbar:
      einmal ausmessen, als Verzoegerung an der Senke hinterlegen.

      FOLGERUNG: zusammen hoeren im selben Raum ist KEIN Zwei-Boxen-Feature.
      Es ist eine Box mit zwei Senken, und das kann E72 im Prinzip schon. Was
      fehlt, ist der Fall „beide Senken bekommen DENSELBEN Strom" statt zwei
      verschiedener, plus die Versatzkorrektur.

      ══ STAND ══════════════════════════════════════════════════════════════

      Nichts davon ist entschieden oder gebaut. Das einzige Stueck, das vor
      allem anderen steht und klein ist: die Kontoaufloesung ueber `/v1/me`.
      Vorrat, Nicht-Uebernehmen und Dazukommen stehen alle darauf.
    related: ["mixpi-tragbar-der-entwurf", "spak-schluessel-traegt-das-konto-nicht"]

  - id: klangwerk-geschlossenes-vokabular-statt-freier-conf
    kind: entscheidung
    title: "Ein Ton-Plugin beschreibt GLIEDER, es schreibt keine PipeWire-Conf"
    match: "klangkette|Klangwerk|filter.graph|convolver|ladspa|Stereobasis|bq_peaking|libspa-filter-graph|klang-Recht|mixpi-klang"
    sev: high
    tags: ["plugin", "ton", "pipewire", "sicherheit", "entwurf", 2026]
    source: >
      21.08.2026, Auftrag des Betreibers („bitte implementiere ein plugin für
      den sound"), Anlass war seitliche Abstrahlung der Lautsprecher. Gebaut in
      derselben Sitzung: klangkette.ts, plugins/mixpi-klang, das Recht `klang`,
      die Durchgangssenke `klangwerk` in ton.ts. STAND ZUM ZEITPUNKT DES
      SCHREIBENS: im Arbeitsbaum, NOCH NICHT COMMITTET UND NICHT AUF EINER BOX
      GEMESSEN. Die Messungen unten stammen vom Entwicklerrechner
      (PipeWire 1.6.8), nicht vom Geraet.
    body: >
      DER NAHELIEGENDE ENTWURF WAERE FALSCH GEWESEN: das Plugin liefert
      `filter.graph`-Text, der Kern schreibt ihn in eine Datei. Das ist genau
      die Falle, die der Plugin-Vertrag sonst ueberall vermeidet — die Datei
      wird von einem Prozess gelesen, der MODULE NACHLAEDT. `convolver` nimmt
      einen DATEIPFAD, `ladspa` eine fremde `.so`. Ein Plugin schriebe sich
      damit am Kern vorbei beliebigen Code in den Tonstapel.

      Deshalb beschreibt ein Plugin GLIEDER aus einer geschlossenen Liste
      (`basis hochpass tiefpass kuhschwanz glocke vorpegel`), und der Kern
      uebersetzt sie. WAS NICHT IN DER LISTE STEHT, GIBT ES NICHT. Dieselbe
      Haltung wie `fundPruefen`: im Zweifel wird nicht gebaut. Die Liste ist
      die Grenze, nicht eine Bequemlichkeit.

      ══ WARUM DIE HAERTE NOETIG IST — GEMESSEN ═════════════════════════════

      Eine Filterkette mit einem unbekannten Baustein VERHAELT SICH NICHT
      SCHLECHT, sie laedt GAR NICHT: `pipewire -c` bricht ab, und die Senke
      erscheint nicht einmal. Waere der Ton schon dorthin geroutet, stuende
      ein Kind vor einer STUMMEN BOX — und der Fehler faellt erst dann auf.
      Folge im Entwurf: ein Plugin mit krummer Kette faellt HERAUS, statt die
      ganze Kette scheitern zu lassen; die uebrigen spielen weiter, der Grund
      steht im Journal. Geprueft wird im Hauptstrang (`kettePruefen`), nicht
      im Worker — was von drueben kommt, ist ein Vorschlag.

      ══ WARUM DER ENTZERRER DAS NICHT KONNTE ═══════════════════════════════

      Der Fuenfband-Entzerrer RECHNET JE KANAL, ER MISCHT NICHT ZWISCHEN
      IHNEN. Die Stereobasis zu verengen braucht eine KANALMATRIX — das ist
      keine Einstellungssache am Vorhandenen, sondern eine andere Bauart.
      Daher eine zweite Durchgangsstation statt eines neuen Reglers.

      ══ DIE REIHENFOLGE IST EINE ENTSCHEIDUNG ══════════════════════════════

      Quellen -> klangwerk -> entzerrer -> Ziel, jede ausgeschaltete Station
      uebersprungen. Das Klangwerk steht VORN, damit die Basis auf das
      GEMISCHTE Signal wirkt und der Entzerrer danach auf das Ergebnis.
      Andersherum entzerrte man einen Kanal, den die Matrix gleich darauf mit
      dem anderen verrechnet.

      ══ DIE FALLE, DIE SICH MIT JEDER STATION WIEDERHOLT ═══════════════════

      „Ist diese Senke ein ZIEL oder nur eine STATION?" wird an fuenf Stellen
      gefragt. EINE VERGESSENE ZEIGT DEM BETREIBER „Klangwerk" ALS
      LAUTSPRECHER AN — genau das passierte dem Entzerrer am 15.08.2026.
      Deshalb stehen `DURCHGANG_SENKEN` und `istDurchgang` in ton.ts und
      nicht im Server: eine Liste, fuenf Leser. Wer eine dritte Station baut,
      traegt sie DORT ein und nirgends sonst.

      ══ WAS SOFTWARE NICHT KANN (steht so in den Feldhinweisen) ════════════

      Die ABSTRAHLRICHTUNG. Hochtonabfall neben der Achse laesst sich im
      Pegel ausgleichen, aber was physisch an den Ohren vorbeigeht, kommt als
      RAUMREFLEXION zurueck — es wird LAUTER, NICHT KLARER. Die Chassis zehn
      bis fuenfzehn Grad nach vorn zu kippen schlaegt jeden Regler. Und
      KAMMFILTER (beide Chassis dasselbe Signal, verschiedene Laufzeit) kann
      das Plugin gar nicht heilen; dagegen hilft nur ein Chassis abschalten
      oder die Aufstellung aendern. Das gehoert in die HINWEISE der Felder,
      damit es liest, wer die Box einstellt — nicht nur in den Quelltext.

      ══ AM GERAET NACHGEMESSEN (21.08.2026, spaeter am selben Tag) ═════════

      Die Zeile darueber hiess bis dahin „am Geraet nicht gemessen". Inzwischen
      ist sie es — auf der MixPiBox (Pi 5, MuPiHAT, PipeWire 1.4.2):

        * Die erzeugte Kette LAEDT. `pipewire -c` mit einer Kette aus
          Stereobasis + Hochpass + Kuhschwanz + Vorpegel brachte die Senke
          `klangwerk` (float32le 2ch 48000Hz) hoch, ohne eine einzige
          Meldung im Log.
        * KOSTEN: 8 MB RSS, 0,0 % CPU im Leerlauf. Der Filterprozess ist
          billiger als ein Plugin-Worker (11,4 MB).
        * Alle benutzten builtin-Label sind auf der Box vorhanden
          (`mixer`, `copy`, `bq_highpass`, `bq_lowpass`, `bq_lowshelf`,
          `bq_highshelf`, `bq_peaking`) — geprueft in
          libspa-filter-graph-plugin-builtin.so. Die Entwicklung lief gegen
          1.6.8, die Box hat 1.4.2; die Label sind in beiden gleich.

      NICHT gemessen: der Uebergang Klangwerk->Entzerrer im Betrieb (dazu
      muesste der Server mit dem neuen Code auf der Box laufen), und die
      HOERBARE Wirkung — die haengt am naechsten Eintrag.

      ══ WICHTIGER ALS ALLES OBEN ═══════════════════════════════════════════

      Ein erster Kanaltest meldete "Chassis mono gebrueckt" — DAS WAR EIN
      MESSFEHLER des Werkzeugs, die Box ist Stereo. Warum der Test nichts
      taugte und wie man es richtig misst: [[kanaltest-mit-rauschen-misst-nichts]].
      Die Stereobasis ist auf dieser Box also sehr wohl der richtige Hebel.
    related:
      [
        "plugin-laufwerk-ist-eigenes-ausrollziel",
        "aufnehmen-ist-investition-nicht-laufende-last",
        "kanaltest-mit-rauschen-misst-nichts",
      ]

  # ══ GERAETEBEFUND: DIE BOX IST MONO GEBRUECKT (21.08.2026) ═══════════════
  - id: kanaltest-mit-rauschen-misst-nichts
    kind: lehre
    title: "Der Kanaltest mit Rauschen misst nichts — die Box IST Stereo"
    tags: [audio, mupihat, klang, geraet, messfehler, gemessen]
    stand: "am Geraet geprueft und BERICHTIGT, 21.08.2026 (MixPiBox, Pi 5, MuPiHAT)"
    body: |
      ══ ACHTUNG: DIESER EINTRAG BERICHTIGT SICH SELBST ═════════════════════

      Hier stand am 21.08.2026 zuerst der Befund „beide Chassis bekommen
      dasselbe Signal, das komische Hoergefuehl ist ein Kammfilter". DAS WAR
      FALSCH. Die Box ist Stereo — vom Betreiber am selben Tag am offenen
      Geraet bestaetigt. Was hier steht, ist deshalb keine Aussage ueber die
      Hardware mehr, sondern ueber die MESSMETHODE.

      ══ WIE DER FALSCHE BEFUND ZUSTANDE KAM ════════════════════════════════

      `tools/box/kanaltest.py` spielte in seiner ersten Fassung
      BANDBEGRENZTES RAUSCHEN, erst nur links, dann nur rechts, und fragte:
      „aus welcher Box kam es?". Der Betreiber hoerte beide Male beide Boxen.
      Zwei Fehler wirkten zusammen:

        1. DER TIEFPASS NAHM DEN HOCHTON WEG — also genau das, woran das Ohr
           RICHTUNG erkennt. Unterhalb von etwa 500 Hz ist Ortung kaum
           moeglich. Eingebaut war er mit der gut gemeinten Begruendung
           „damit kleine Chassis es sauber koennen"; er machte den Test blind.
        2. BEIDE CHASSIS SITZEN IN EINEM GEHAEUSE, wenige Zentimeter
           auseinander. Spielt eines, schwingt das Gehaeuse mit. Aus
           Hoerabstand klingt das auch bei perfektem Stereo nach „kommt aus
           beiden".

      Der Gegentest mit `--direkt` (an Entzerrer und Klangwerk vorbei) aenderte
      nichts — was den Verdacht scheinbar erhaertete. In Wahrheit war er
      genauso blind: derselbe untaugliche Testton.

      ══ WAS DEN FEHLER AUFDECKTE ═══════════════════════════════════════════

      NICHT eine Messung, sondern ein EINWAND des Betreibers: „aber ich denke
      es ist stereo warum sollte ein komisches gefühl sonst enstehen". Dazu
      die Doku: SW3 steht ab WERK auf Stereo, und er hatte nie geschraubt.
      Beides zusammen war staerker als das Messergebnis — und es stimmte.

      ══ WIE MAN ES RICHTIG MISST ═══════════════════════════════════════════

      Nicht die RICHTUNG unterscheiden (schwer, und vom Gehaeuse verfaelscht),
      sondern die TONHOEHE (kinderleicht, aus jedem Winkel gleich):

          links  = 220 Hz  (brummt)
          rechts = 2200 Hz (pfeift)

      Dazu DAS OHR AN EIN CHASSIS halten, nicht aus Hoerabstand raten. Bei
      Stereo ist an einem Chassis genau EIN Ton laut. Kommen aus jedem
      Chassis BEIDE Toene, ist es Mono — und DAS kann kein Gehaeuseschwingen
      vortaeuschen. So macht es das Werkzeug jetzt, samt `--wandern` fuer
      einen langen Durchgang (5 s reichen nicht, um von einem Chassis zum
      anderen zu gehen).

      ══ DAS BESTE MITTEL KAM VOM BETREIBER: SPRACHE ════════════════════════

      „man könnte auch so eine left right ansage machen" — und das schlaegt
      jeden Testton. `alsa-utils` bringt die Ansagen mit, sie liegen auf der
      Box bereit:

          /usr/share/sounds/alsa/Front_Left.wav
          /usr/share/sounds/alsa/Front_Right.wav

      WARUM SPRACHE GEWINNT: sie traegt Konsonanten, also Hochton mit
      Struktur — im Raum viel leichter zu orten als ein Sinus. Und der Hoerer
      muss sich nichts merken: er hoert „Front Left" und weiss sofort, welche
      Seite gemeint ist, statt eine Tonhoehe einer Seite zuordnen zu muessen.
      Genau damit fiel die Entscheidung: `--ansage`, beide Seiten im Wechsel,
      und der Betreiber hoerte jede Ansage aus der Box, die sie nennt.

      ERGEBNIS: STEREO, und die Kanaele sind richtig herum (links ist links,
      vom Hoerplatz aus gesehen).

      Fuer den EINZELKANAL-Test gilt ausserdem: dort ist wieder die RICHTUNG
      die Frage, also gehoert der HOHE Ton dorthin, nicht der tiefe. Ein
      tiefer Ton allein auf einem Kanal wandert als Koerperschall durchs
      Gehaeuse und klingt aus beiden Chassis.

      ══ DIE LEHRE ══════════════════════════════════════════════════════════

      EIN HOERTEST IST EINE MESSUNG UND BRAUCHT DIESELBE SORGFALT wie eine
      Zahl aus /proc. „Der Betreiber hat es gehoert" ist kein Beweis, wenn das
      Testsignal die Frage gar nicht beantworten KANN. Vor dem Auswerten
      fragen: Kann dieses Signal die beiden Faelle ueberhaupt unterscheiden?
      Beim Rauschen lautete die Antwort nein — und niemand hat gefragt, bis
      der Betreiber widersprach.

      UND: WIDERSPRICHT DER BETREIBER DER MESSUNG, IST DAS EIN DATENPUNKT,
      kein Missverstaendnis. Hier hatte er recht und das Werkzeug unrecht.

      ══ WAS DAMIT WIEDER GILT ══════════════════════════════════════════════

      Die urspruengliche Diagnose: seitlich abstrahlende Lautsprecher, direkt
      davor sitzend -> ZERFALLENE PHANTOMMITTE. Die STEREOBASIS des Klangwerks
      ([[klangwerk-geschlossenes-vokabular-statt-freier-conf]]) ist genau
      dafuer gebaut und auf dieser Box der richtige Hebel. Einstieg: Breite 50.
    related: ["klangwerk-geschlossenes-vokabular-statt-freier-conf", "mupi-max98357a-audio", "einmal-hinsehen-ist-keine-messung"]

  - id: wischgesten-vom-rand
    kind: howto
    title: "Die Gesten der Box: zwei Finger ueberall, ein Streichen ueber den Player, ein Zug vom Rand"
    tags: [oberflaeche, neu, geste, touch, 800x480, darstellung, 2026]
    source: "Session 2026-08-21; gemessen mit tools/wischrand-schau.mjs gegen tools/neu-vorschau.mjs"
    body: |
      GEWUENSCHT, woertlich: „Implementiere ein wischgesten vom rand feature 1
      2 3 fingern vom rand den rand kann man bestimmen. als erste gibt es
      player verschwinden lassen, lautstäre regler, quicksettings panel."

      GEBAUT ist ein Erkenner in NewDesign/app.js (`wisch`), drei Taten und
      ein Schnellfenster (`schnell`). AB WERK AUS — eine Bedienung, die man
      nicht sieht, soll man eingeschaltet haben. Werksbelegung, wenn man sie
      einschaltet: alle drei vom RECHTEN Rand, unterschieden nach Fingerzahl.

      ── WARUM RECHTS ─────────────────────────────────────────────────────
      Es ist der EINZIGE freie Rand dieser Oberflaeche. Links liegt die
      Kategorienleiste (88 px, rollt senkrecht), unten das Kissen, oben die
      Kopfzeile. Zwei und drei Finger waeren auch dort unbedenklich; ein
      Finger nicht.

      ── FALLE 1: DER LINKE RAND IST SCHON VERGEBEN ───────────────────────
      `randwisch` holt seit dem 03.08.2026 die eingefahrene Leiste zurueck —
      linker Rand, EIN Finger, nach rechts
      ([[randwisch-holt-die-leiste-zurueck]]). Wer „Player ausblenden" auf links/1 legt, loeste beide
      zugleich aus. Der Erkenner streicht deshalb den linken Rand aus seinen
      Kandidaten, solange `platzBeimBlaettern.eingefahren` gilt — also genau
      dann, wenn es die aeltere Geste ueberhaupt gibt. NUR den linken, nicht
      den ganzen Zug: in der oberen linken Ecke steht der obere Rand zur Wahl,
      und der ist nicht betroffen.

      ── FALLE 2: EINE ECKE GEHOERT ZWEI RAENDERN ─────────────────────────
      `wischRaenderVon` gibt eine LISTE zurueck, keine Antwort. Welcher der
      zwei gemeint war, entscheidet erst die Richtung (`wischZugRand`). Wer
      beim Aufsetzen entscheidet, muss eine Muenze werfen — und ein
      Elternteil, das von der Ecke nach rechts zieht, bekaeme die Tat des
      oberen Randes. Genau diagonal ergibt NICHTS; lieber keine Geste als eine
      geratene.

      ── FALLE 3: ZWEIMAL AUSLOESEN SIEHT AUS WIE KEINMAL ─────────────────
      `player-weg` ist ein Umschalter. Loest ein Zug zweimal aus, steht am
      Ende derselbe Zustand da wie vorher, und JEDE Ablesung meldet „nichts
      passiert". Das Messwerkzeug zaehlt deshalb die WECHSEL (MutationObserver
      auf genau diese Klasse) und liest zusaetzlich die Meldung „Der Player
      ist ausgeblendet …" als zweiten Zeugen — sie wird bei jedem Ausblenden
      neu gestellt und verraet den verschwiegenen Treffer.

      ── FALLE 4: EIN EINZELNES EREIGNIS DARF NICHT UEBER DEN SCHIRM ─────
      `wischSprung` verwirft einen Zug, wenn EIN Ereignis den Finger weiter
      traegt als die halbe Kante. Anlass war ein Nachzuegler in der
      Chromium-Nachbildung (siehe unten); die Regel steht aber aus eigenem
      Recht da: Die Beruehrungen dieser Box kommen NICHT aus einem
      Kerneltreiber, sondern aus einer selbstgebauten Bruecke im Benutzerraum
      ([[mupi-touch-ohne-interrupt]]), und eine verspaetete Meldung von dort
      wuerde sonst den Player verschwinden lassen.

      ── DAS SCHNELLFENSTER, UND WO DIE SPERRE VERLAEUFT ──────────────────
      Vier Zeilen: Helligkeit und Lautstaerke/Stumm wirken SOFORT, Hoerzeit
      und Funk gehen durch das EINE Tor des Eltern-Bereichs. Die Trennlinie
      ist nicht Geschmack: Eine Geste ist kein Schloss, und ein Kind findet
      einen Wisch schneller, als Erwachsene glauben. Waere Funk frei, koennte
      es die Box aus dem Netz nehmen; waere die Hoerzeit frei, waere die
      Kinderzeit mit einer Wischgeste abgeschafft.

      DAS TOR HAT DAFUER EINEN DRITTEN AUSGANG BEKOMMEN: neben „andere
      Oberflaeche" (`danach`) und „Unterseite" (`fach`) jetzt „Tat" (`tat`).
      „15 Minuten dazugeben" ist keine Seite, und es gibt im Eltern-Bereich
      keine, auf der es stuende. Ein ZWEITES Tor im Schnellfenster waere die
      Alternative gewesen — und damit eine zweite Sperrlogik, die beim
      naechsten Anfassen von der ersten abweicht.

      ── WO ES STEHT ──────────────────────────────────────────────────────
        NewDesign/app.js        `wisch` (Erkenner), `schnell` (Fenster),
                                die sieben reinen Regeln `wisch*`
        NewDesign/index.html    `#wisch-laut`, `#schnell`
        NewDesign/app.css       ab „WISCHGESTEN VOM RAND"
        Eltern-Bereich          Punkt „Wischgesten" unter Darstellung,
                                FUENF Zeilen (mehr traegt die Karte nicht)
        Admin-Board             seiten/darstellung.ts, die ganze Matrix
                                plus die Randbreite
        tools/pruef-neu-regeln.js       die Regeln, ohne Browser
        tools/wischrand-schau.mjs       die Geste, mit echten Beruehrungen

      ── EINSTELLEN ───────────────────────────────────────────────────────
      SIEBEN flache Felder in `darstellung.json`:

        zweiFinger          die Steuerung, die ueberall gilt
        playerStreichen     Streichen ueber das Kissen blendet es aus
        playerZurueckSek    nach so vielen Sekunden kommt es zurueck (10)
        wischGesten         die Randgeste ueberhaupt
        wischRandBreite     Breite des Randstreifens (28 px)
        wischSchnellRand    wo das Schnellfenster liegt
        wischSchnellFinger  mit wie vielen Fingern

      FLACH und nicht verschachtelt, mit Absicht: Ein Objekt faellt bei einer
      feldweisen Pruefung als Ganzes durch, und eine HALB angekommene
      Belegung — Rand da, Fingerzahl weg — saehe aus wie eine Einstellung, die
      jemand so gewollt hat ([[darstellung-feld-faellt-still-heraus]]).

      HIER STANDEN BIS ZUM 22.08.2026 ACHT FELDER, naemlich je Tat
      `wisch<Weg|Laut|Schnell><Rand|Finger>` — der erste Entwurf, bei dem alle
      drei Taten am Rand hingen. Der Nachtrag weiter unten beschreibt laengst,
      dass Lautstaerke und Player abgewandert sind; diese Liste hier ist beim
      alten Stand geblieben, und wer nur sie liest, sucht `wischLautRand` und
      findet nichts. Es gibt sie NICHT mehr — auf einer frueh aktualisierten
      Box koennen sie aber noch in `darstellung.json` STEHEN: Das
      Zusammenfuehren im PUT nimmt Fremdes nicht weg, gelesen werden sie
      trotzdem nicht mehr.

      ══ NACHTRAG VOM SELBEN ABEND: DAS VOKABULAR IST GEWECHSELT ═══════════

      GEMELDET NACH DER ERSTEN AUSLIEFERUNG, woertlich: „beim 2finger bewegt
      sich auch der hintergrund ich hab ueberlegt das 2 finger hoch runter
      ideal waeren fuer lautstaerke ein doppel tipp start stop mit 2 fingern
      vor zurueck mit fingern vor zurueck" und „kann man auch definieren wenn
      man ueber den player schnell streicht das er verschwindet ... und
      vielleicht auch wieder rein kommt nach einer zeit".

      DER FEHLER UND DER ENTWURF HABEN DIESELBE URSACHE, und das ist der Kern
      dieses Nachtrags: Chromium entscheidet an den ERSTEN
      `touchmove`-Ereignissen, ob eine Beruehrung rollt. Wer erst abfaengt,
      wenn seine Geste feststeht (bei 44 px), faengt zu spaet ab — der Schirm
      rollt bereits, und `preventDefault` wird ab da ignoriert. Am Rand LAESST
      SICH DAS NICHT FRUEHER TUN: dort liegt auch der Ein-Finger-Zug, mit dem
      gerollt wird. Mit ZWEI Fingern schon, denn zwei Finger bedeuten in
      dieser Oberflaeche nie „weiterrollen".

      SO SIEHT ES JETZT AUS:
        zwei Finger hoch/runter   Lautstaerke, der Finger bleibt am Regler
        zwei Finger links/rechts  naechster / voriger Titel (links = vorwaerts)
        Doppeltipp, zwei Finger   anhalten oder weiter
        ueber den Player streichen  schnell -> das Kissen geht, kommt nach
                                  10 s (einstellbar) von selbst zurueck
        drei Finger vom Rand      das Schnellfenster

      DREI FINGER AM RAND UND NICHT ZWEI: Zwei gehoeren jetzt der Steuerung,
      die ueberall gilt. Waere das Schnellfenster auf zwei, kaeme es bei jedem
      Lautstaerke-Zug mit heraus, der zufaellig am Rand beginnt.

      DIE GESTE LIEGT AUF DEM DING, DAS SIE BETRIFFT. Das ist der Grund, warum
      der Player vom Rand auf den Player gewandert ist — und warum die Meldung
      „dieselbe Geste holt ihn zurueck" entfallen konnte: Wer das Kissen
      angefasst hat, weiss, was verschwunden ist.

      SCHNELL IST EINE BEDINGUNG, KEIN SCHMUCK: Auf dem Kissen liegen drei
      Knoepfe und ein Balken. Ein langsames Ziehen darueber ist ein Fehlgriff
      beim Zielen (56 px in hoechstens 400 ms trennt beides), und ein
      Fehlgriff darf den Player nicht kosten.

      ZWEI FEHLER, DIE DIE MESSUNG WIEDER GEFUNDEN HAT:
        * `zweiTipp(null, null)` war WAHR. `Number(null)` ist 0, und 0 ms bei
          0 px ist der perfekte Tipp — ein Aufrufer mit einem leeren Zug haette
          die Wiedergabe angehalten. Dieselbe Falle wie bei `prozent: null`
          und `restMin: null`; sie ist in diesem Haus damit die siebte.
        * `wischTat` loeste fuer den Rand 'aus' aus, also fuer eine
          ausdruecklich abgeschaltete Geste.

      UND EINE, DIE NUR AM GERAET SICHTBAR IST: Der Rollfehler laesst sich mit
      CDP NICHT nachstellen (siehe [[cdp-beruehrungen-nachzuegler]]). Die
      Messung dazu ist trotzdem drin und gruen — ihr Gruen beweist nur nichts.
      Was zaehlt, ist das Abfangen ab dem ersten Ereignis, und das steht im
      Quelltext.

      ══ NACHTRAG: DREI FINGER GINGEN NIE — UND WARUM NIEMAND ES SAH ══════

      GEMELDET (21.08.2026): „die menu geste funktioniert nicht mit 1 oder 3
      fingern probiert".

      DIE URSACHE WAR EINE SCHNITTMENGE. `nieder` verlangte, dass ALLE Finger
      im Randstreifen liegen — also alle drei innerhalb von 28 px. DAS KANN
      EINE HAND NICHT: Zeige-, Mittel- und Ringfinger sind unterschiedlich
      lang, und wer sie an den rechten Rand legt, hat drei verschiedene
      x-Werte; der hinterste liegt gut hundert Pixel weiter innen. Die
      Bedingung war fuer EINEN Finger richtig und fuer drei unerfuellbar.

      DIE MESSUNG WAR TROTZDEM GRUEN, und das ist der eigentliche Eintrag
      hier: Sie setzte alle Finger auf dieselbe Stelle der ZUGACHSE und
      spreizte sie nur quer dazu — eine Hand mit drei gleich langen Fingern.
      Dieselbe Falle wie bei [[randwisch-holt-die-leiste-zurueck]], eine Ebene
      weiter: dort Maus statt Finger, hier IDEALE Finger statt echter.

      DIE NEUE REGEL heisst `wischRaenderDerHand`: der VORDERSTE Finger muss im
      Streifen liegen, der hinterste in Reichweite einer Hand (160 px, rund
      22 mm). Bei EINEM Finger ist das dieselbe Frage wie vorher — `min` und
      `max` fallen zusammen —, und genau das prueft eine Schleife ueber neun
      Stellen, damit die Aenderung den Ein-Finger-Fall nicht nebenbei
      verschiebt.

      UND EIN ZWEITER FEHLER IM WERKZEUG, gefunden beim Beheben des ersten:
      Die neue Staffelung zeigte in die falsche Richtung. Eine Hand spreizt
      sich nach INNEN — wer rechts aufsetzt und nach links zieht, hat die
      uebrigen Finger bei KLEINEREM x. Mit dem falschen Vorzeichen landeten
      sie bei 820 und 844, also ausserhalb des 800 px breiten Schirms, und die
      Messung meldete einen Fehler der Oberflaeche, wo sie eine unmoegliche
      Hand geschickt hatte. Beide Fehler sind einzeln nachgewiesen: mit
      berichtigtem Vorzeichen faellt die ALTE Regel, mit der neuen Regel steht
      sie.

      WAS „1 FINGER" ANGEHT — UND EINE FALSCHE DIAGNOSE DAZU:

      Der Betreiber hatte inzwischen auf links/1 umgestellt. Der erste Verdacht
      war die Kollisionsregel: Der Erkenner strich den linken Rand fuer EINEN
      Finger, solange die Kategorienleiste eingefahren ist, damit `randwisch`
      sie zurueckholen kann. Also wurde sie umgedreht — `randwisch` steht
      seitdem zurueck, wo links/1 belegt ist.

      DAS WAR NICHT DIE URSACHE. Gemessen war die Leiste gar nicht eingefahren;
      der Fehler lag im WERKZEUG: Es setzte `#schnell` auf `hidden`, statt es
      zu schliessen. Der Merker `schnell.offen` blieb dabei wahr, die naechste
      Geste rief `umschalten()` — also `zu()` —, und die Messung sah ein
      Fenster, das nicht aufgeht.

      DIE UMGEDREHTE REGEL BLEIBT TROTZDEM, aus eigenem Recht: Mit der alten
      haette links/1 nur die 1400 ms nach einem Blaettern nicht funktioniert —
      mal ja, mal nein, ohne dass etwas den Grund nennt. Der Fall ist jetzt
      gemessen („auch bei eingefahrener Leiste"). Das Wiki sagt ueber
      `randwisch` selbst: „Sie ist eine Abkuerzung, kein einziger Weg. Die
      Ruhezeit bringt die Leiste ohnehin zurueck." Eine verlorene Abkuerzung
      kostet eine Wartezeit; eine eingestellte Geste, die unberechenbar
      ausfaellt, kostet das Vertrauen in die Einstellung.

      LEHRE: Wer eine Regel aendert, waehrend die Diagnose noch wackelt, muss
      hinterher pruefen, ob sie ohne die Diagnose noch traegt. Hier tut sie es —
      aber das war Glueck, bis es gemessen war.

      UND ZUR URSPRUENGLICHEN FRAGE: Ein Finger tut nur dann nichts, wenn eine
      ANDERE Fingerzahl eingestellt ist. Das ist keine Stoerung, sondern die
      Einstellung — Eltern-Bereich, Darstellung, Gesten, Zeile „Finger".
    related: [randwisch-holt-die-leiste-zurueck, mupi-touch-ohne-interrupt, darstellung-feld-faellt-still-heraus, eltern-bereich-muster-ohne-ionic, platz-beim-blaettern]

  - id: cdp-beruehrungen-nachzuegler
    kind: falle
    sev: mittel
    title: "Chromiums Beruehrungs-Nachbildung schleppt Punkte in den naechsten Zug"
    match: "dispatchTouchEvent|touchmove n=2|Geste loest zu frueh aus|Geste loest gar nicht aus|Input.dispatchTouchEvent"
    tags: [messen, cdp, touch, werkzeug, 2026]
    source: "am 21.08.2026 beim Bau von tools/wischrand-schau.mjs gemessen (--spur)"
    cause: >
      `Input.dispatchTouchEvent` mit `type: touchEnd` und LEERER Punktliste
      nimmt nur EINEN Punkt weg. Nach einem Mehrfinger-Zug bleibt der Rest in
      der Nachbildung stehen und taucht mitten im NAECHSTEN Zug wieder auf —
      mit der Stelle von damals.
    fix: >
      Die Seite zwischen zwei Messreihen NEU LADEN. Alles andere ist
      gemessen und wirkungslos: `touchCancel` hinterher, einzelnes Abmelden
      der Punkte mit der Liste der noch liegenden, fortlaufende Kennungen je
      Zug (macht es schlimmer — ab dem zweiten Zug kommt gar nichts mehr an),
      Warten auf zwei Bilder, 250 ms Ruhe.
    body: |
      DER FUND SAH AUS WIE EIN FEHLER DER OBERFLAECHE, und zwar zweimal
      hintereinander in ENTGEGENGESETZTE Richtungen:

        * Ein 20-px-Zug am rechten Rand loeste eine Geste aus, die 44 px
          verlangt. Im Mitschnitt stand mitten in der Folge ein
          `touchmove` auf x=240 — die Endstelle des ZUGES DAVOR. Aus 796
          wurden in EINEM Ereignis 240, also 556 px nach innen.
        * Nach einer Gegenprobe mit zwei Fingern blieb ein 120-px-Zug
          WIRKUNGSLOS: Der zweite Punkt von damals lag noch in der
          Nachbildung, und aus dem Ein-Finger-Zug wurde unterwegs ein
          Zwei-Finger-Zug (`touchmove n=2 [751,169 280,169]`).

      BEIDE MALE HAT DIE OBERFLAECHE RICHTIG GEHANDELT. Ohne Mitschnitt haette
      man an ihr gesucht.

      DIE LEHRE IST NICHT „CDP taugt nichts", sondern: Wer Gesten misst, muss
      mitschreiben koennen, WAS am Ziel ankommt. Ein Werkzeug, das nur
      „ausgeloest / nicht ausgeloest" kennt, kann seinen eigenen Nachbau nicht
      von der Sache unterscheiden. `tools/wischrand-schau.mjs --spur` haengt
      dafuer eigene Zuhoerer in die Fangphase und schreibt Typ, Punktzahl und
      Koordinaten mit.

      AUF DER BOX GIBT ES DIESEN ZUSTAND NICHT: dort kommen die Beruehrungen
      aus einem echten uinput-Geraet durch den Kernel (scripts/box/
      touch-bridge.py), und dessen Punkte sind weg, wenn der Finger weg ist.

      ══ NACHTRAG: ER ERZEUGT AUCH KEINEN KLICK ═══════════════════════════

      DRITTES GERAETEVERHALTEN, DAS DER NACHBAU NICHT ZEIGT (21.08.2026).
      Gemeldet wurde: „das problem beim 2 finger tippen waehlt man aus oder
      verschiebt" — ein Zwei-Finger-Tipp schlaegt auf die Kachel darunter
      durch. Die Messung dazu (Doppeltipp ueber einer echten Kachel, mit einem
      Faenger auf `click`) war GRUEN, bevor die Abhilfe ueberhaupt existierte:
      Chromiums Nachbildung synthetisiert bei zwei Fingern gar keinen Klick.

      DIE LEHRE IST DIESELBE WIE OBEN, nur schaerfer: Ein gruener Lauf gegen
      CDP beweist ueber BERUEHRUNGSABGELEITETE Ereignisse (Klick, Zeiger,
      Auswahl) NICHTS. Was dort gemessen werden kann, sind die Ereignisse
      selbst — `touchstart`, `touchmove`, `touchend` — und was die Seite
      daraufhin an Befehlen hinausschickt. Alles, was der BROWSER daraus
      ableitet, gehoert ans Geraet.
    related: [wischgesten-vom-rand, mupi-touch-ohne-interrupt, im-kiosk-messen-statt-raten]

  # ══ BETREIBER-REGEL: DAS PRAEFIX (21.08.2026) ════════════════════════════
  - id: mixpi-praefix-fuer-alles-neue
    kind: regel
    title: "Alles NEUE heisst mixpi — Bestehendes behaelt seinen Namen"
    match: "mixpi praefix|wie soll das heissen|neues skript benennen|mupibox oder mixpi|namensregel|umbenennen mupibox"
    sev: med
    tags: ["namen", "mixpi", "betreiber-regel", "ausliefern", 2026]
    source: >
      21.08.2026, Betreiber-Regel, erstmals angewandt in Commit 0dfa6d2d
      (mixpi-wlan-adapter).
    body: >
      DIE REGEL: Was NEU entsteht — Skripte, systemd-Units, Konfigurationsdateien
      unter /etc/mupibox, Plugins, Werkzeuge — traegt das Praefix `mixpi`.
      Beispiele aus dem Bestand: `scripts/mixpi/mixpi-wlan-adapter.sh`,
      `mixpi-wlan-adapter.service`, `/etc/mupibox/mixpi-wlan-adapter`,
      `plugins/mixpi-klang`, `plugins/mixpi-mitschnitt`.

      WAS AUS DEM URSPRUNGSPROJEKT STAMMT, BLEIBT WIE ES HEISST: `mupibox-server`,
      `mupibox-player`, `mupibox-alsa-init`, `mupibox-podcast`, das Verzeichnis
      /home/dietpi/.mupibox — und alles andere, was aus dem Upstream kommt.

      DER GRUND IST NICHT GESCHMACK, SONDERN DIE UEBERNAHME. Wer Bestehendes
      umbenennt, macht jede kuenftige Aktualisierung aus dem Ursprungsprojekt zu
      einem Konflikt, den jemand von Hand aufloesen muss — und zwar dauerhaft,
      Datei fuer Datei. Das Praefix trennt stattdessen sauber: `mixpi` ist unser
      Teil, `mupibox` ist geliehener.

      FOLGE FUER DIE PLUGIN-KENNUNG: die `kennung` in plugin.json ist zugleich
      Namensraum und Ordnername (Schema `<kennung>:…`, Route
      /api/plugins/<kennung>/). Neue Plugins heissen darum `mixpi-…`, die
      mitgelieferten Muster aus der Anfangszeit bleiben `mupibox-…`.
    related: ["mixpi-tragbar-der-entwurf", "ein-wort-zwei-bedeutungen-nicht-pauschal-ersetzen"]

  # ══ E66/E72: DER LEERLAUF-ARBEITER (21.08.2026) ══════════════════════════
  - id: wer-nichts-tut-muss-sagen-warum
    kind: regel
    title: "Ein Arbeiter, der 'nichts zu tun' meldet, ist von einem kaputten nicht zu unterscheiden"
    match: "leerlauf arbeiter|nichts zu tun|gibt null zurueck|warum nimmt er nicht auf|mitschnitt startet nicht|wasJetzt|auftrag.mjs|grund statt null"
    sev: med
    tags: ["entwurf", "mitschnitt", "stroeme", "diagnose", "regel", 2026]
    source: >
      21.08.2026, beim Bau von plugins/mixpi-mitschnitt/auftrag.mjs (Commit
      85d49e1b) — der erste Baustein des Leerlauf-Arbeiters, und der einzige,
      der sich ohne Geraet pruefen laesst.
    body: >
      DIE REGEL: Wer im Hintergrund arbeitet, gibt NIE bloss `null` zurueck,
      sondern immer auch den GRUND. Sonst sieht ein Arbeiter, der nichts zu tun
      hat, genau so aus wie einer, der kaputt ist — beide tun nichts. Der Grund
      ist das einzige, was das Befinden im Eltern-Bereich anzeigen kann; ohne ihn
      beginnt jede Frage „warum nimmt die Box nicht auf?" mit Quelltextlesen.

      DIE ARBEITSTEILUNG DAHINTER: Die Liste (`liste.mjs`) weiss, WAS ansteht.
      Sie weiss nicht, ob es GEHT — das haengt an Dingen ausserhalb ihrer selbst.
      `wasJetzt(lage)` in `auftrag.mjs` ist der Aufrufer, der das entscheidet, und
      er ist rein: hinein ein Lagebild, heraus `{ auftrag, grund }`. Regeln, die
      im Ablauf versteckt sind, kann niemand nachlesen.

      DIE FUENF GRUENDE STEHEN IN EINER REIHENFOLGE VOM HARMLOSEN ZUM ERNSTEN:
      (1) nichts vorgemerkt — der Normalfall, kein Mangel und soll auch nicht wie
      einer klingen; (2) es spielt gerade jemand; (3) kein Strom fuer den
      Mitschnitt eingerichtet; (4) der Strom hat keinen brauchbaren Zugang;
      (5) der Zugang ist DERSELBE wie der der Wiedergabe. Liegengebliebene Fehler
      werden mitgezaehlt — sonst sieht eine Liste mit drei gescheiterten Titeln
      aus wie eine leere.

      DIE TEUERSTE REGEL IST DIE FUENFTE: zwei Stroeme duerfen sich keinen Zugang
      teilen. Spotify spielt je Konto genau EINEN Strom; startet ein zweiter mit
      demselben Schluessel, nimmt er dem ersten die Wiedergabe weg — fuer ein Kind
      hoert die Musik mitten im Stueck auf.

      ZWEI FALLEN, DIE BEIM BAU AUFFIELEN:

        * GEPRUEFT WIRD AUCH, WENN GERADE NICHTS SPIELT. Der Leerlauf endet, wenn
          jemand eine Kachel antippt — und dann laeuft die Aufnahme noch. Wer nur
          „spielt gerade?" fragt, baut eine Falle mit Zeitzuender.
        * „GESETZT" REICHT BEIM ZUGANG NICHT, es zaehlt die FORM
          (`/^spak_[A-Za-z0-9]{32}$/`). Der Platzhalter SPAK_HIER_EINSETZEN war
          gesetzt, verschieden vom anderen Schluessel und trotzdem kein Zugang —
          am 20.08.2026 an der Box.

      Und: nicht „irgendein Strom mit Zweck mitschnitt", sondern der ERSTE in
      NUMMERNFOLGE. Die Reihenfolge in der Datei aendert sich beim Bearbeiten.
    related:
      [
        "liste-ist-vorrat-platte-ist-wahrheit",
        "aufnehmen-ist-investition-nicht-laufende-last",
        "konten-als-vorrat-nicht-je-box",
        "erledigtes-markieren-nicht-entfernen",
      ]
  - id: tor-aus-der-route-die-man-gerade-loeschte
    kind: falle
    title: "Nach ifdown kein Internet: das Tor wurde aus der Vorgaberoute gelesen, die eine Zeile vorher entfernt wurde"
    match: "Network is unreachable|kein internet nach ifdown|default route weg|ip route replace leer"
    sev: high
    tags: ["netz", "falle", "mixpi", "shell", 2026]
    source: >
      21.08.2026 an der Box. Der Betreiber meldete „kein internet"; die Box
      erreichte den Router, alles dahinter meldete „Network is unreachable".
    body: >
      DIE ZEILE, die es verursacht hat:

          ifdown "${WEG}"
          ip route replace default via "$(ip route | awk '/^default/ {print $3}')" \
              dev "${BEHALTEN}" 2>/dev/null || true

      Das TOR wurde aus der VORHANDENEN Vorgaberoute gelesen — und die gehoerte
      der Schnittstelle, die eine Zeile darueber gerade hingelegt worden war.
      Die Ersetzung lieferte eine leere Zeichenkette, der Befehl war
      unvollstaendig, und `2>/dev/null || true` hat es verschluckt.

      HENNE UND EI, VERSTECKT HINTER EINER TARNKAPPE. Beide Teile allein waeren
      harmlos gewesen: der Denkfehler ohne die Unterdrueckung haette sofort
      gemeldet; die Unterdrueckung ohne den Denkfehler haette nichts zu
      verstecken gehabt.

      WOHER DAS TOR STATTDESSEN KOMMT: aus dem DHCP-Mietvertrag der behaltenen
      Schnittstelle, /var/lib/dhcp/dhclient.<iface>.leases, Zeile
      `option routers`. Der steht dort unabhaengig davon, welche Routen gerade
      existieren — genau die Eigenschaft, die hier fehlte.

      UND DIE MELDUNG WIRD EINGESAMMELT, NICHT WEGGEWORFEN:

          if meldung=$(ip route replace default via "${tor}" dev "${IF}" 2>&1); then

      Der RUECKGABEWERT entscheidet, die MELDUNG erklaert. Beides wird
      gebraucht: ohne den Wert merkt niemand den Fehler, ohne die Meldung weiss
      niemand, woran es lag. `2>/dev/null` haette hier nur den Grund gekostet,
      nicht die Erkennung — aber genau der Grund ist es, den man um zwei Uhr
      nachts sucht.

      ALLGEMEIN: Wer eine Route entfernt, muss die Ersatzroute aus einer
      Quelle nehmen, die das Entfernen UEBERLEBT. Das gilt fuer Adressen,
      Namensserver und alles andere, was an derselben Schnittstelle haengt.
    related: ["befehl-ohne-rechte-und-rueckruf-ohne-blick", "zwei-schnittstellen-ein-teilnetz-arp-flux"]
  - id: zwei-ausrollwege-eine-handgefuehrte-liste
    kind: falle
    title: "Die Server-Units erneuert der Update-Weg NIE - geaendert wird nur, was auf frische Karten kommt"
    match: "unit aendert sich nicht|service alt nach update|autosetup und update auseinander|start_mupibox_update|Bestandsbox bekommt nicht|systemd unit nicht aktualisiert"
    sev: high
    tags: ["ausrollen", "autosetup", "update", "systemd", "falle", 2026]
    source: >
      21.08.2026, nachgemessen mit tools/ausrollwege-vergleich.py auf dem Stand
      28d9babc. Der Fund stammt aus dem Kritiker-Lauf AUDIT-2026-08-21.md (1.3);
      die dortige Aufzaehlung ist nachgeprueft und hier BERICHTIGT — sie nannte
      config/templates/asound.conf mit, das der Update-Weg absichtlich anders
      behandelt.
    body: >
      ES GIBT ZWEI AUSROLLWEGE, UND JEDER FUEHRT SEINE EIGENE LISTE VON HAND:

        autosetup/autosetup.sh           105 Ablagen — die frische SD-Karte
        update/start_mupibox_update.sh    94 Ablagen — die Box im Haus

      94 Eintraege sind in beiden. Die Raender laufen auseinander, und niemand
      merkt es, weil beide Wege fuer sich gruen sind.

      DER TEUERSTE EINZELFALL: `config/services/mupibox-server.service` und
      `mupibox-player.service`. autosetup.sh:1069/1070 legt sie hin. Der
      Update-Weg STOPPT sie (Zeile 698/699) und STARTET sie wieder (1517/1518) —
      er ERNEUERT sie nie. Wer eine Server-Unit im Repo aendert, sieht die
      Wirkung nur auf frisch bespielten Karten; jede Bestandsbox laeuft weiter
      mit der Unit, die bei ihrer Installation aktuell war. Kein Fehler, keine
      Meldung, nur eine Aenderung, die nirgends ankommt.

      DAS STOP/START IST DABEI DIE TARNUNG. Es sieht im Skript nach Pflege der
      Unit aus, und der Dienst laeuft danach ja auch. Er laeuft nur nach der
      ALTEN Beschreibung.

      OHNE JEDE ERWAEHNUNG IM UPDATE-WEG stehen ausserdem:
      `config/templates/98-dietpi-disable_dpms.conf`, `add_wifi.json` und
      `spotifycontroller.json`.

      NICHT ALLES, WAS FEHLT, IST EIN FEHLER — die drei Faelle, die es NICHT
      sind, und woran man sie erkennt:

        * `splash.txt`, `splash.png`, `MuPiLogo.jpg` stehen im Update-Weg
          AUSKOMMENTIERT da (Zeilen 970/999/1000). Ein auskommentiertes `mv`
          ist eine ENTSCHEIDUNG: der Betreiber hat sein Bild vielleicht selbst
          getauscht.
        * `crontab.template` wird anders eingespielt — nicht kopiert, sondern
          per `crontab <pfad>` (Zeile 1325-1327).
        * `asound.conf` und `mupiboxconfig.json` nennt der Update-Weg nur unter
          ihrem ZIELPFAD (/etc/asound.conf beim PipeWire-Umstieg, Zeile 1268;
          /etc/mupibox/mupiboxconfig.json als Konfiguration, die zu
          ueberschreiben ein Fehler waere).

      DAS WERKZEUG DAZU: `python3 tools/ausrollwege-vergleich.py`. Es zaehlt nur
      echte Ablagen (cp/mv/install/rsync mit ${MUPI_SRC}-Pfad, auskommentierte
      Zeilen zaehlen NICHT) und trennt "gar nicht erwaehnt" von "erwaehnt, aber
      nicht abgelegt". `--streng` gibt 1 zurueck, solange die erste Liste nicht
      leer ist. Es ist ein SCHIRM, kein Urteil: wer eine Datei nur unter ihrem
      Ziel nennt, faellt hindurch — deshalb gehoert jede Zeile einmal angesehen.

      ES ERSETZT `tools/units-decken-sich.sh` NICHT, es ergaenzt es. Jenes
      fragt: hat jede Unit, die autosetup.sh anfasst, auch eine Datei im Repo?
      (Daran hing die pm2-Umstellung.) Dieses fragt die andere Haelfte: kommt
      die Datei auch bei einer Box an, die schon laeuft? Gleicher Ausgang —
      eine Aenderung, die nirgends ankommt —, aber keines der beiden findet
      die Faelle des anderen. Am 21.08. war `units-decken-sich.sh` gruen,
      waehrend die zwei Server-Units seit je nicht auf Bestandsboxen kamen.

      ALLGEMEIN: Zwei handgefuehrte Listen derselben Dateien laufen immer
      auseinander; die Frage ist nur, wie lange es unbemerkt bleibt. Solange
      es sie gibt, gehoert der Vergleich in die Pruefbatterie — sonst ist der
      naechste Fund wieder ein Zufallsfund.
    related:
      [
        "repariertes-update-skript-laeuft-nie",
        "gebaut-und-unerreichbar",
        "was-die-sicherung-nicht-traegt",
      ]
  - id: pw-cli-fuehrt-die-prozesskennung-nicht
    kind: kniff
    title: "Zwei gleichnamige PipeWire-Knoten unterscheiden: pw-cli zeigt die Prozesskennung nicht, pw-dump schon"
    match: "zwei knoten gleicher name|application.process.id|pw-cli ls Node|falscher strom abgegriffen|spotify knoten doppelt"
    sev: med
    tags: ["pipewire", "mitschnitt", "e66", "kniff", 2026]
    source: >
      21.08.2026 an der Box gemessen, beim Bau des Leerlauf-Arbeiters. Die
      Messung hat den Entwurf geaendert.
    body: >
      DAS PROBLEM: Zwei Soloist-Instanzen erzeugen BEIDE einen Knoten namens
      `spotify`. Wer ueber den Namen abgreift, erwischt womoeglich den
      Familienstrom — und SIEHT DABEI AUS, ALS ARBEITE ER: es kommt Ton, er
      wird aufgenommen, nur eben der falsche. Ein Fehler, der sich als Erfolg
      tarnt.

      DER BACKLOG SCHLUG VOR, ueber `application.process.id` zu gehen. Richtig
      — aber nicht mit dem Werkzeug, das das Plugin sonst benutzt:

          pw-cli ls Node   ->  0 Knoten fuehren application.process.id
          pw-dump          ->  8 Knoten fuehren sie

      `pw-cli ls Node` zeigt sie GAR NICHT. Der Abgriff muss also ueber
      `pw-dump` gehen, also JSON statt Text — und das ist ein anderer Parser
      als der vorhandene `knotenSuchen`.

      ZWEITER STOLPERSTEIN IM JSON: nicht nur Knoten tragen die Kennung. Links
      und andere Objekte desselben Prozesses stehen mit derselben Nummer da,
      teils mit demselben `node.name`. Ohne Filter auf
      `type == "PipeWire:Interface:Node"` greift man an ein Objekt, das gar
      keine Ports hat. Der Test dafuer braucht ein Beispiel, in dem der Link
      VOR dem Knoten steht und denselben Namen traegt — sonst faengt ihn schon
      die Namenspruefung und der Typ-Riegel bleibt ungeprueft.

      ALLGEMEIN: Bevor man einen Entwurf auf eine Eigenschaft stuetzt, einmal
      nachsehen, ob das vorgesehene Werkzeug sie ueberhaupt ausgibt. Hier haette
      der Bau sonst mit einem Parser begonnen, der nie etwas findet.
    related: ["messwerkzeug-muss-sich-selbst-pruefen", "einmal-hinsehen-ist-keine-messung"]
  - id: zwei-aufrufe-ein-suchbereich
    kind: falle
    title: "Ein Test, der zwei Aufrufe im selben Ausschnitt sucht, prueft in Wahrheit keinen"
    match: "quelltexttest|indexOf slice match|auskommentierter aufruf|test bleibt gruen obwohl entfernt"
    sev: med
    tags: ["tests", "falle", "gegenprobe", 2026]
    source: >
      21.08.2026 beim Bau des Leerlauf-Arbeiters. Zwei Gegenproben blieben
      gruen, obwohl je ein Aufruf entfernt war.
    body: >
      DER AUFBAU: Zwei Tests sollten pruefen, dass ein Aufruf an ZWEI Stellen
      steht — beim Start der Wiedergabe und beim Anhalten. Beide schnitten
      denselben grossen Block heraus:

          const block = quelle.slice(quelle.indexOf('async ereignis('))
          match(block.slice(0, block.indexOf('async befinden(')), /leerlaufTakt\(true/)

      DARIN LAGEN BEIDE AUFRUFE. Also deckte jeder den anderen: einen
      auszukommentieren liess beide Tests gruen. Zwei Zeugen, die dasselbe
      sehen, sind ein Zeuge.

      ZWEI GETRENNTE FEHLER, HINTEREINANDER GEFUNDEN:

        1. Die Suche lief auf dem ROHEN Quelltext, also erfuellte
           `// leerlaufTakt(true, …)` das Muster weiter. Kommentare gehoeren
           vor der Suche abgestreift.
        2. Auch danach blieb es gruen — weil die AUSSCHNITTE sich
           ueberlappten. Erst das Trennen der Bereiche machte die Gegenprobe
           scharf.

      Der erste Fehler steht schon im Pack. Der zweite ist der subtilere und
      war ohne Gegenprobe nicht zu sehen: der Test war gruen, der Code war
      richtig, und trotzdem prueften die beiden Tests nichts.

      DIE REGEL: Wer mehrere Vorkommen desselben Musters pruefen will, braucht
      je Vorkommen einen ausschliessenden Ausschnitt — Anfang UND Ende an
      etwas festgemacht, das nur dort steht. Und die Probe dafuer ist, JEDES
      Vorkommen EINZELN zu entfernen und zu sehen, ob genau ein Test rot wird.
    related: ["gegenprobe-statt-gruen-glauben", "messwerkzeug-muss-sich-selbst-pruefen"]

  # ══ DER SIGNALWEG VERRUTSCHT — EXISTENZ IST NICHT VERBINDUNG ═════════════
  - id: signalweg-verrutscht-passive-sucht-standardsenke
    kind: befund
    title: "Eine Filterstation verrutscht still aus dem Weg — Existenz bewachen reicht nicht"
    tags: [audio, pipewire, klangwerk, wache, geraet, gemessen]
    stand: "am Geraet gemessen UND per Gegenprobe belegt, 21.08.2026 (MixPiBox, Pi 5)"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      Eine Stunde nach dem Aufbau zeigte `klangwerk.ausgang` wieder auf die
      Soundkarte statt auf den Entzerrer. Das Klangwerk uebersprang ihn — der
      Fuenfband-Equalizer war damit WIRKUNGSLOS. Ohne Neustart, ohne Fehler,
      ohne eine Zeile im Journal. Aufgefallen nur, weil beim Nachmessen einer
      ganz anderen Frage der Weg mit ausgegeben wurde.

      ══ URSACHE: node.passive SUCHT SICH DIE STANDARD-SENKE ════════════════

      Der Ausgang einer filter-chain traegt `node.passive = true`. Verbindet
      er sich NEU (Reconnect, Umschalten, ein Zucken im Tonstapel), haengt er
      sich an die Standard-Senke. Die ist bei uns die VORDERSTE Station, also
      das Klangwerk selbst — das scheidet aus, und dann landet der Ausgang
      eine Station zu weit hinten, direkt auf der Karte.

      ══ WARUM ES NIEMAND SAH — DIE EIGENTLICHE LEHRE ═══════════════════════

      Die Wache prueft, OB die Senke da ist:

          if (klangwerkAn && !knoten.has(KLANGWERK_SINK)) neuAufbauen()

      Die Senke WAR die ganze Zeit da. Wer die EXISTENZ bewacht, sieht eine
      falsche VERBINDUNG nie. Das ist derselbe Denkfehler wie „Prozess laeuft
      also spielt es" ([[wiki-zustand-hat-haltbarkeit]]): der bewachte Zustand
      und der gemeinte Zustand sind zweierlei.

      Dasselbe galt fuer den Entzerrer schon vorher. Dort fiel es nur nie auf,
      weil er die VORDERSTE Station war und die Standard-Senke ohnehin auf ihn
      zeigte — der Fehler wurde erst sichtbar, als eine zweite Station davor
      kam.

      ══ WAS JETZT GILT ═════════════════════════════════════════════════════

      `signalwegWachen()` prueft im selben 30-s-Takt, ob JEDE Station auf die
      naechste zeigt, und legt den Weg NUR bei echter Abweichung neu — ein
      bedingungsloses Neurouten im Takt schoebe laufende Stroeme umher. Die
      Abweichung kommt mit ins Journal, statt stumm zu bleiben.

      ══ GEGENPROBE, AM LAUFENDEN GERAET ════════════════════════════════════

      Nicht geglaubt, sondern kaputtgemacht: `pactl move-sink-input` haengte
      den Klangwerk-Ausgang von Hand auf die Soundkarte. Innerhalb eines
      Wache-Takts stand im Journal

          Signalweg verrutscht (klangwerk zeigte auf alsa_output… statt auf
          entzerrer) — wird neu gelegt

      und danach zeigte `klangwerk.ausgang` wieder auf `entzerrer`.

      ══ FUER DEN NAECHSTEN, DER EINE STATION EINBAUT ═══════════════════════

      Drei Dinge sind fluechtig und brauchen JE eine Wache: die Senke (kann
      verschwinden), die Knoten-Eigenschaften (sterben mit jedem Reconnect,
      dafuer gibt es klangAnwenden) — und der WEG. Der dritte ist der, den
      man vergisst.
    related: ["klangwerk-geschlossenes-vokabular-statt-freier-conf", "wiki-zustand-hat-haltbarkeit", "gegenprobe-statt-gruen-glauben"]

  - id: volles-wiki-ist-kein-beleg-fuer-gepflegte-doku
    kind: falle
    title: "Ein volles Wiki ist kein Beleg fuer gepflegte Doku - zwei Ablagen, zwei Ausfallarten"
    match: "doku veraltet|wiki gepflegt aber doku nicht|dokumentation prueft sich nicht|feature nicht dokumentiert|welche ablage bekommt den fund|handbuch drift"
    sev: mid
    tags: [doku, wiki, arbeitsweise, drift]
    stand: "gemessen am Repo, 21.08.2026 (MixPiBox, HEAD 763bdd85)"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      Das Wiki war lueckenlos, die Doku hatte ein Loch von der Groesse eines
      Untersystems. Gezaehlt in `dokumentation/mixpibox.md` gegen
      `llmwiki/pack.yaml`:

          "Sicherung"    Doku:  0 Treffer   Wiki: 236
          "Leerlauf"     Doku:  0 Treffer   Wiki:  29
          "Cog"          Doku:  0 Treffer   Wiki:  66
          "Wischgeste"   Doku:  0 Treffer   Wiki:  15

      Die Sicherung ist dabei kein Nebenzweig: ein Python-Werkzeug, FUENF
      systemd-Units (Dienst, Zeitgeber, Pfad-Wachhund, Probe-Dienst,
      Probe-Zeitgeber), ein eigener Router (`sicherung.ts`), eine Seite im
      Eltern-Bereich, ein Spec. Vollstaendig gebaut, vollstaendig im Wiki,
      und im Handbuch nicht ein einziges Mal erwaehnt.

      ══ WARUM DAS SYSTEMATISCH PASSIERT ════════════════════════════════════

      Die beiden Ablagen fuellen sich aus VERSCHIEDENEN Anlaessen:

          Wiki  fuellt sich beim ARBEITEN.  Jeder Fund, jede Falle, jede
                Gegenprobe hat einen Autor, der gerade dort stand. Die
                Hausregel "Wiki immer nachziehen" haengt am Commit.
          Doku  fuellt sich beim ERKLAEREN.  Sie braucht jemanden, der sich
                fragt, was ein Fremder wissen muesste - und diesen Anlass
                erzeugt kein Commit.

      Deshalb ist das Wiki der schlechteste denkbare Indikator fuer den Stand
      der Doku: es ist genau dann voll, wenn viel gebaut wurde - also genau
      dann, wenn die Doku am weitesten hinterherhinkt. Wer "wir schreiben ja
      alles auf" als Entwarnung nimmt, misst die falsche Ablage.

      ══ EIN ZWEITER FALL: WAS KEINE METHODE HAT, FAELLT DURCH ══════════════

      Die Plugin-Doku zaehlte "drei Anmeldepunkte" auf. Es gibt aber ein
      fuenftes Recht, `aufnahme`, das ABSICHTLICH keine Methode hat - es ist
      eine Ansage an den Eltern-Bereich, kein Schluessel zu einer
      Schnittstelle. Eine Aufzaehlung, die an Methoden entlanggeht, kann es
      gar nicht finden. Und es war ausgerechnet das rechtlich heikelste
      (`mixpi-mitschnitt`, Familienkonto).

      Wer eine Doku aus einer Tabelle speist, dokumentiert nur, was die
      Tabelle abbildet. Was aus der Reihe faellt, faellt auch aus der Doku -
      und das ist meist das, was einer Erklaerung am meisten beduerfte.

      ══ WAS DARAUS FOLGT ═══════════════════════════════════════════════════

      Der Gegencheck ist billig und dauert eine Minute: die Titel der letzten
      Wiki-Eintraege gegen die Doku greppen. Wo das Wiki dreistellig ist und
      die Doku null, steht ein ganzes Untersystem ohne Erklaerung da.

      Und: Zahlen in der Doku sind Momentaufnahmen. `NewDesign/app.js` stand
      am Vormittag des 21.08. auf 26 842 Zeilen, am Nachmittag auf 28 562 -
      dieselbe Doku, derselbe Tag. Sie taugen fuer die GROESSENORDNUNG, nie
      als Wert, auf den sich etwas beruft.

      ══ DRITTER FALL, 22.08.2026: EIN FIX WIDERRUFT EINE ZUSAGE ════════════

      Die schaerfere Bauart. Bisher stand hier: Doku FEHLT, wo das Wiki voll
      ist. Am 22.08. war die Doku nicht leer, sondern AKTIV FALSCH - und
      falsch geworden ist sie durch eine Verbesserung.

      `plugins/README.md` sagte im Abschnitt "Auf die Box bringen" zu, eine
      frische Karte kopiere die Plugins mit `cp -rn`, "ein vorhandener Ordner
      wird nicht ueberschrieben". Das war am 21.08. richtig. Am 22.08. wurde
      genau dieses `cp -rn` als Falle erkannt und durch den versionierten
      Kopierer ersetzt (siehe
      [[cp-rn-friert-plugins-auf-dem-erststand-ein]]): hoehere `fassung`
      ersetzt den Ordner jetzt GANZ. Die Zusage im Handbuch war damit nicht
      veraltet, sondern widerrufen - und ein Plugin-Bauer, der sich darauf
      verlaesst, plant gegen das Gegenteil.

      DAS MUSTER: eine Doku, die ein VERHALTEN zusagt ("wird nicht
      ueberschrieben", "wird nicht angefasst", "bleibt liegen"), wird durch
      jede Aenderung an diesem Verhalten zur Falschaussage. Eine Doku, die nur
      AUFZAEHLT, veraltet dagegen still und harmlos. Zusagen sind die
      teuerste Sorte Satz in einem Handbuch - wer eine schreibt, muss sie beim
      naechsten Anfassen der Stelle wiederfinden. Dieselbe Stelle war schon
      einmal betroffen (Commit `15d85650`, "E82 kam an, das Plugin-Handbuch
      wusste nichts davon"); zweimal dasselbe Handbuch ist kein Zufall,
      sondern der Ort, an dem am meisten zugesagt wird.

      PRAKTISCH: nach einem Fix an einem Ausrollweg, einer Kopier- oder
      Loeschregel einmal `grep -rn "wird nicht\|bleibt\|unangetastet" *.md
      plugins/README.md` - jede dieser Zeilen ist eine Zusage, die der Fix
      gebrochen haben koennte. Beim Handbuch ist der Kreis erst zu, wenn die
      neue Regel dort als TABELLE steht (was passiert wann), nicht als
      Erzaehlung: der Kopierer hat fuenf Faelle, und die Erzaehlung nannte
      einen.

      ══ VIERTER FALL, 22.08.2026: DIE LISTE IST PROSA, DAS VERZEICHNIS IST WAHR ══

      Die billigste Bauart, und die einzige, gegen die es einen MECHANISCHEN
      Test gibt. `plugins/mupibox-subsonic/` liegt seit dem 14.08.2026 im Baum
      (Commit `8b30d4bb`), 18 Zeugen gruen, Pruefstand HAELT - und wurde im
      Handbuch `plugins/README.md` acht Tage lang mit NULL Treffern genannt.
      Fuer jeden Leser existierte es nicht.

      Ausgerechnet dieses: es ist das einzige Musterplugin, das `suchen()`
      benutzt, und das einzige, das ein `geheim`-Feld vorfuehrt (Subsonic mit
      Salz und Pruefsumme, das Passwort steht in keiner Adresse). Wer im
      Handbuch nach beidem sucht, findet nichts und baut es neu.

      WARUM ES DURCHFAELLT: der "Stand"-Block oben im Handbuch ist ein von
      Hand gepflegter PROSA-Absatz ("je ein Musterplugin dafuer liegt
      daneben ..."). Er ist nicht aus `ls plugins/` abgeleitet, also faellt
      ein neuer Ordner nicht auf. Der Commit, der das Plugin brachte, fasste
      das Handbuch nicht an - und niemand vermisst einen Namen, den er nie
      gelesen hat.

      DER TEST steht jetzt als Werkzeug da und hat KEIN Urteilsvermoegen:

          bash tools/doku-luecken-probe.sh

      Er vergleicht drei Quellen im Code gegen die beiden Handbuecher:
      `ls plugins/` gegen plugins/README.md, und `RECHTE` / `EREIGNISSE` aus
      `plugin-vertrag.ts` gegen beide. Gesucht wird der Name IN BACKTICKS,
      nicht das blosse Wort - sonst quittiert die Probe das Wort
      "Medienquelle" als Beleg fuer das Recht `medienquelle`, und eine Probe,
      die so hereinfaellt, meldet gruen und wird zu Recht ignoriert. Was sie
      nicht kann: ein Handbuch, das den Namen nennt und Falsches darueber
      sagt, faellt hier nicht auf - dagegen hilft nur Lesen (dritter Fall
      oben).

      Am 22.08. fand die Probe beim ersten Lauf vier Rechte, die
      `dokumentation/mixpibox.md` nur als Begriff kannte (`medienquelle`,
      `ereignisse`, `klang`) oder gar nicht (`netz` - die Erlaubnis fuer
      `kontext.holen`, ohne die eine Medienquelle nie ins Netz kommt); die
      Tabelle dort nannte den Anmeldepunkt, nie den Namen, der ins
      `plugin.json` gehoert. Nach dem Nachtragen verschwanden genau diese vier
      Meldungen - das ist zugleich die Gegenprobe, dass die Probe unterscheidet
      und nicht bloss gruen sagt.

      Gleiche Sorte, gleicher Tag: der Vorschlagstyp `radio` aus E84
      (ARD-Radiosender) stand nach dem Commit nur im Code und im Wiki; das
      Handbuch nannte fuer `mixpi-ardsounds` weiterhin "Regale, Sammlungen,
      Suche, Folgen". Und `dokumentation/mixpibox.md` zaehlte am 22.08. abends
      immer noch "drei Anmeldepunkte" und fuenf Rechte, waehrend
      `plugin-vertrag.ts` SECHS fuehrt (`geraetestand` kam mit E82) und vier
      Gesichter der Box selbst zu Plugins geworden waren (E79/E81/E82).
      Merksatz: Aufzaehlungen, die eine Quelle im Code haben (`RECHTE`,
      `ls plugins/`), gehoeren gegen diese Quelle geprueft, nicht gelesen.

      DRITTE AUSFALLART, 22.08.2026 abends: die Probe meldete KEINE LUECKE,
      und die Doku hatte trotzdem eine. Der Betreiberfund vom selben Abend
      ([mitschnitt-stummel-kachel-vor-inhalt]) landete beim Commit nur im
      Wiki; beide Handbuecher beschrieben `mixpi-mitschnitt` weiter ohne ein
      Wort darueber, dass Playlist und Kachel nach dem ERSTEN Stueck
      entstehen. Kein Name fehlte, keine Aufzaehlung war falsch - es fehlte
      eine EIGENSCHAFT, und Eigenschaften haben keine Liste im Code, gegen
      die sich zaehlen liesse. Regel daraus: nach jedem Commit, der eine
      llmwiki-Falle anlegt, gehoert die Frage gestellt, welches Handbuch
      dieselbe Sache beschreibt - die Probe beantwortet sie nicht.

      VIERTE AUSFALLART, 22.08.2026 nachts: DIE KORREKTUR SELBST WAR
      UNVOLLSTAENDIG. Die Aussage "ein medienquelle-Plugin kann keine Kachel
      in die Medienliste legen" stand an DREI Stellen - plugins/README.md,
      BACKLOG.md und dokumentation/mixpibox.md. E87/E88 widerlegten sie um
      18:58. Der Doku-Lauf um 19:11 fand sie, nannte im Commit ZWEI Stellen
      namentlich und korrigierte genau diese zwei. Die dritte stand vier
      Stunden spaeter unveraendert da und sagte einem Leser weiter, der Weg
      sei gesperrt und E84 der naechste Schritt - beides falsch, und beides
      genau die Auskunft, die jemanden den Weg ein zweites Mal bauen laesst.

      WARUM DIE DRITTE STELLE UNSICHTBAR WAR: gesucht wurde nach dem THEMA,
      und das Thema kam in allen drei Dateien vor. Jede Datei sah damit
      "behandelt" aus. REGEL: wer eine falsche Aussage korrigiert, sucht nach
      dem SATZ, nicht nach dem Thema - `grep -rn` auf die woertliche
      Formulierung, ueber alle Handbuecher UND pack.yaml, bevor der Commit
      faellt.

      DIE WACHE DAZU:

          bash tools/doku-widerruf-probe.sh

      Sie fuehrt Muster widerrufener Aussagen und meldet jeden Treffer, der
      sich nicht als historisch ausweist. Zwei Formen gelten als Ausweis, und
      beide kommen im Baum vor: INLINE (plugins/README.md: "Bis dahin hatte
      das Plugin bewusst keinen Aufnehmen-Knopf") und ABSCHNITT (BACKLOG.md:
      "Alles oberhalb dieses Absatzes beschreibt den Stand VOR E87" zieht den
      ganzen Abschnitt zurueck, statt jeden Satz einzeln anzufassen). Beim
      ersten Lauf kannte sie nur INLINE und meldete die sauber
      zurueckgezogenen BACKLOG-Saetze als Fehler - ein Pruefer, der gefuehrte
      Historie anschwaerzt, wird zu Recht ignoriert und wurde korrigiert.

      DER BLOCKWIDERRUF IST ENGER GEFASST als die Inline-Marker, und das mit
      Absicht: wuerde "Nachtrag" allein einen Abschnitt entlasten, spraeche
      jede spaetere Ergaenzung rueckwirkend jeden falschen Satz darueber frei.
      Es braucht den ausdruecklichen Satz, dass das Obige historisch ist.

      Gegenprobe gelaufen: der falsche Satz unmarkiert ins zweite Handbuch
      gesetzt -> rot; der NACHTRAG-Block im BACKLOG entfernt -> rot an beiden
      Saetzen; beide Dateien danach byteweise wiederhergestellt. Eine erste
      Sabotage blieb gruen und war KEIN Fund, sondern ein Fehler der Probe-
      Anlage: sie loeschte nur "Alles oberhalb", waehrend derselbe Satz mit
      "beschreibt den Stand VOR" ein zweites Muster traegt.

      FUENFTE AUSFALLART, 22.08.2026 nachts: DIE WACHE SELBST HATTE EINEN
      BLINDEN FLECK - ES SIND DREI HANDBUECHER, NICHT ZWEI.

      Drei Doku-Lauf-Commits in Folge (2213398b, a01788fe, 574c6f50) schrieben
      "beide Handbuecher" und meinten plugins/README.md und
      dokumentation/mixpibox.md. Es gibt ein DRITTES:
      `dokumentation/benutzerhandbuch.html` - und es ist das einzige, das ein
      Betreiber wirklich liest. Es stand in KEINER der beiden Proben:
      doku-luecken-probe.sh kannte nur HANDBUCH und DOKU,
      doku-widerruf-probe.sh fuehrte fuenf Dateien ohne dieses. Auch die
      Ausfallart-Nummer VIER ist damit selbst ein Beispiel fuer sich: die
      Korrektur, die aus DREI Stellen zwei anfasste, wurde mit einer Wache
      beantwortet, die aus DREI Handbuechern zwei ansieht.

      WAS DARIN VERALTET WAR (am Baum nachgezaehlt, nicht geschaetzt):

          Verwaltungstabelle   14 Seiten gefuehrt, Kopfleiste hat 17
          `Profile`            fehlte; hiess dort noch "Kinderzeit" und
                               stand unter der falschen Gruppe
          `Ton` `Plugins`      fehlten ganz
          `Leistung`           fehlte ganz
          `Sicherung`          fehlte ganz
          Plugins/Erweiterungen   kamen mit NULL Treffern vor - das ganze
                               Erweiterungssystem existierte fuer den Leser
                               nicht, samt Internet Archive (seit E88 die
                               erste komplett ueber ein Plugin laufende
                               Medienquelle, mit Suche und Aufnehmen-Knopf)
          Quellen-Abschnitt    fuehrte "Vier Quellen", es sind fuenf
          Fussnote             "Stand 19.08.2026, 632 Eintraege" bei
                               tatsaechlich Fassung 287 / 777 Eintraegen

      WARUM GERADE DIESES HANDBUCH DRIFTET: es ist das einzige, das NICHT im
      Entwicklerpfad liegt. Wer am Vertrag oder an einem Plugin arbeitet,
      hat plugins/README.md ohnehin offen; benutzerhandbuch.html faellt
      niemandem beim Arbeiten in die Hand. Genau deshalb braucht es die
      mechanische Wache und nicht den guten Willen.

      DIE WACHE IST NACHGEZOGEN: doku-luecken-probe.sh vergleicht seither die
      Menuepunkte aus `src/frontend-admin/src/app/rahmen.ts` (die
      BESCHRIFTUNG aus `<a routerLink=...>`, nicht den Weg - das Handbuch
      nennt "Profile", der Weg heisst noch /kinderzeit) gegen das
      Benutzerhandbuch; doku-widerruf-probe.sh hat die Datei in DATEIEN.
      Bewusst NICHT eingefordert werden dort `RECHTE`, `EREIGNISSE` und die
      Konfigurationsfelder: das sind Namen fuer Plugin-Bauer, und eine Probe,
      die sie in einem Benutzerhandbuch verlangt, meldet ewig rot und wird zu
      Recht ignoriert - dieselbe Ueberlegung wie beim ardsounds-Fall in
      [plugin-kette-reisst-an-sechs-stellen].

      Gegenprobe beide Male gelaufen: `Plugins` und `Leistung` aus der
      Tabelle entfernt -> rot an genau diesen zwei Namen; ein widerrufener
      Satz unmarkiert ans Handbuch gehaengt -> rot mit Zeilennummer. Datei
      danach aus der Sicherung zurueck, `grep -c` auf die Sabotagespur = 0.
      Die Sicherung lag VOR der Sabotage im selben Befehlsblock - die Datei
      trug uncommittete Arbeit, und `git checkout` haette sie mitgenommen.

      REGEL: `ls dokumentation/` zaehlen, ehe man "beide Handbuecher"
      schreibt. Eine Wache, die eine Ablage nicht kennt, meldet fuer diese
      Ablage ewig gruen.
    related: ["mitschnitt-stummel-kachel-vor-inhalt", "wiki-zustand-hat-haltbarkeit", "was-die-sicherung-nicht-traegt", "zwei-ausrollwege-eine-handgefuehrte-liste", "cp-rn-friert-plugins-auf-dem-erststand-ein", "plugin-kette-reisst-an-sechs-stellen"]

  - id: klangkurve-ist-die-echte-wirkung
    kind: howto
    title: "Die Klangkurve ist gerechnet, nicht gemalt - und die Pegelanzeige zeigt die Kette, nicht das Stueck"
    match: "frequenzgang anzeigen|eq kurve zeichnen|biquad rbj|uebersteuern anzeigen|pegelanzeige klangwerk|kuhschwanz ueberschwinger|shelf overshoot|klangkette.ts"
    sev: mid
    tags: [klangwerk, ton, pipewire, biquad, eltern-bereich, admin-board, 2026]
    stand: "gebaut 21.08.2026, Commit 5c7ed1b8; Formeln in src/backend-api/src/klangkette.ts, Test klangkette.spec.ts"
    body: |
      GEWUENSCHT (Betreiber, 21.08.2026): eine Kurve, die sich beim Ziehen
      mitbewegt, und eine Anzeige, „damit man Uebersteuern durch Anhebungen
      sieht statt hoert".

      ══ DIE KURVE ZEIGT, WAS DIE KETTE TUT ═══════════════════════════════════

      PipeWires `bq_*`-Glieder sind Biquads nach dem RBJ Audio EQ Cookbook.
      Dieselben Formeln stehen jetzt in `klangkette.ts` (`biquadVon`,
      `biquadDb`, `frequenzgang`, `spitzeDb`) — die Kurve ist damit die
      gerechnete Wirkung der Filterkette und keine hingemalte Ahnung.

      DIE PROBE AUFS EXEMPEL steht im Test: ein Hochpass mit Butterworth-Guete
      liegt an seiner Eckfrequenz bei -3 dB. Stimmte das nicht, waeren die
      Koeffizienten falsch und die ganze Kurve loege. Wer die Formeln anfasst,
      prueft gegen diese Stelle, nicht gegen das Aussehen.

      Zahlen, die feststehen: Abtastrate 48 kHz (am Geraet abgelesen),
      Stuetzstellen LOGARITHMISCH von 20 Hz bis 20 kHz (96 fuer die Kurve, 192
      fuer die Spitze; linear verteilt laegen fast alle Punkte im Hochton, und
      der Bereich eines Hochpasses haette drei davon), Boden bei -60 dB (was
      darunter faellt, ist gleich still, staucht aber den interessanten
      Bereich).

      ══ WAS NICHT IN DIE KURVE GEHOERT ═══════════════════════════════════════

      Die STEREOBASIS. Sie mischt zwischen den KANAELEN und aendert am
      Frequenzgang eines Kanals nichts — wer sie einzeichnete, behauptete eine
      Wirkung, die es nicht gibt. Dafuer gibt es die Buehne auf der Ton-Seite.
      Der Vorpegel dagegen gehoert hinein: er ist ein flacher Versatz und
      genau das, was gegen Uebersteuern hilft.

      ══ DIE PEGELANZEIGE IST DIE SPITZE DER KURVE, KEIN MESSWERT ═════════════

      Ein Monitor-Strom vom Sink haette gezeigt, wie laut das STUECK gerade
      ist — das schwankt mit der Musik. Beim Einstellen will man wissen,
      wieviel Luft die KETTE frisst, unabhaengig vom Material. Deshalb steht
      dort die hoechste Anhebung des Frequenzgangs.

      Und sie sagt „kann uebersteuern", nicht „uebersteuert": ueber 0 dB heisst
      nur, dass ein bereits voll ausgesteuertes Signal keine Luft mehr hat.

      ══ NACHGEMESSEN STATT ERWARTET: DER UEBERSCHWINGER ══════════════════════

      Ein Kuhschwanz mit Guete 1 hat AUCH BEI REINER ABSENKUNG einen
      Ueberschwinger: bei -6 dB Shelf gemessene +0,44 dB (bei 1976 Hz). Das ist
      korrektes RBJ-Verhalten, kein Rechenfehler. Der erste Entwurf des Tests
      verlangte „unter null" und war rot — richtig war, den TEST zu
      berichtigen, nicht die Formel.

      Fuer die Anzeige heisst das: erst ab +1 dB von Uebersteuern reden. Sonst
      warnt sie bei jeder Absenkung, und eine Warnung, die immer leuchtet,
      liest niemand. Das ist die allgemeine Lehre dieses Eintrags — eine
      Schwelle, die aus der Erwartung stammt statt aus der Messung, erzeugt
      entweder Dauerwarnung oder Blindheit.

      ══ GERECHNET WIRD IM SERVER ════════════════════════════════════════════

      Nicht im Browser: die Formeln stehen an EINER Stelle und sind dort
      geprueft. Die Kurve kommt mit der Live-Antwort mit, die bei jedem
      Reglerzug ohnehin geholt wird — und zwar IMMER, auch wenn nicht live
      gestellt werden kann. Sonst stuende die Anzeige ausgerechnet beim ersten
      Reglerzug leer, naemlich dann, wenn das Klangwerk noch gar nicht laeuft.

      ══ IN DIE EIGENE FALLE GETRETEN ════════════════════════════════════════

      Ein Backtick-Paar in einem Kommentar INNERHALB des
      `styles`-Template-Literals riss `seiten/ton.ts` ab (TS1005, Bau
      gescheitert) — genau [[backticks-beenden-jede-vorlage]]. Der Hinweis
      steht jetzt an der Stelle selbst. Vor dem Ausliefern als Bild
      gegengelesen (rsvg-convert): Hochpass schneidet unter 120 Hz weg, Mitte
      auf -3 dB (Vorpegel), ab 4 kHz auf +3 dB — passt zur Spitze 3,44 dB.
    related: ["klangwerk-geschlossenes-vokabular-statt-freier-conf", "signalweg-verrutscht-passive-sucht-standardsenke", "backticks-beenden-jede-vorlage", "gegenprobe-statt-gruen-glauben"]

  - id: erzeugte-suchliste-von-hand-ergaenzt
    kind: falle
    title: "Erzeugte Suchliste, von Hand ergaenzt - und die Wache dafuer ist rot, also stumm"
    match: "suche der verwaltung findet nichts|such-bestand.ts|verwaltung-suchbestand|schalter nicht auffindbar|erzeugte datei von hand geaendert|--schreiben wirft weg|rote wache faellt nicht auf|suchbestand drift"
    sev: high
    tags: [verwaltung, admin-board, suche, erzeugte-dateien, pruefungen, doku, 2026]
    stand: "gemessen 21.08.2026 nach Commit 46782de3, mit tools/suchbestand-drift.mjs; NACHGEMESSEN 29.08.2026: Drift auf 0/0 geschlossen, Generator laeuft wieder je Seitenaenderung, Wache 20/20 gruen"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      `src/frontend-admin/src/app/such-bestand.ts` traegt im Kopf „ERZEUGT von
      tools/verwaltung-suchbestand.mjs — NICHT von Hand aendern" und den
      Hinweis, dass `tools/verwaltung-suche-vollstaendig.test.mjs` das
      bewacht. Beides stimmt. Trotzdem war am 21.08.2026:

          aus den Seiten erhoben        304 Eintraege
          in der abgelegten Datei       275 Eintraege
          nur in den Seiten              37   <- gebaut, ausgeliefert, UNAUFFINDBAR
          nur in der Datei                8   <- von Hand nachgetragen

      Die 37 sind keine Randfaelle. Darunter die ganze Seite **Leistung**
      (sechs Eintraege, einschliesslich ihres eigenen Seitentitels — die Seite
      ist ueber die Suche nicht zu erreichen) und elf Schalter der
      **Darstellung**, genau die Wischgesten, die am selben Tag gebaut wurden
      (`Gesten`, `Zwei Finger`, `Vom Rand`, `Randstreifen` …), dazu achtzehn
      auf **Profile** (`Schloss`, `Neues Passwort`, `Gast`, `2 Stunden` …).

      ══ WARUM DIE WACHE NICHT GESCHRIEN HAT ════════════════════════════════

      Sie hat geschrien — vor Tagen, und seitdem ununterbrochen. Der Schritt
      steht in `tools/pruefen.sh:277` und ist einer der roten aus dem
      21.08.-Audit. **Eine Wache, die schon rot ist, kann nicht noch einmal
      rot werden.** Der naechste Fehler geht in ihrem Dauerton unter; sie
      bewacht ab dem ersten unbeachteten Tag gar nichts mehr. Das ist der
      Unterschied zwischen „wir haben eine Pruefung" und „wir haben eine
      Pruefung, die gruen ist" — und nur die zweite Aussage traegt.

      Der zweite rote Fall derselben Datei zeigt, wie sie rot wurde: der Test
      nagelt den NAMEN einer Seite fest („der Schlummer-Timer steht bei der
      Kinderzeit"), und die Seite heisst seit `dab6837c` **Profile**. Also
      [[tests-duerfen-keine-zahlen-festnageln]], nur mit einem Namen statt
      einer Zahl: geprueft gehoerte „der Schlummer-Timer liegt auf derselben
      Seite wie die Spielzeiten", nicht wie diese Seite heisst.

      ══ DIE FALLE IN DER BEHEBUNG ══════════════════════════════════════════

      Der Kopf der Datei nennt die Behebung in einer Zeile:

          node tools/verwaltung-suchbestand.mjs --schreiben

      Sie holt die 37 fehlenden — und **wirft die 8 von Hand nachgetragenen
      weg**. Das sind heute die sieben Begriffe aus `46782de3` (`Stereobasis`,
      `Mono`, `Frequenzgang`, `Hochpass`, `Hoehen`, `Bass`, `Vorpegel`) plus
      `Leistung` auf der MuPiHAT-Seite. Wer der Anweisung folgt, hat danach
      eine gruene Pruefung und weniger Suchbegriffe als vorher — und niemand
      sieht es, weil das Ergebnis wieder „erzeugt" ist und damit richtig
      aussieht.

      Deshalb gibt es `tools/suchbestand-drift.mjs`: es zeigt beide Richtungen
      getrennt und benennt die zweite ausdruecklich als **Verlustliste**.

          node tools/suchbestand-drift.mjs           # beide Listen, je Seite
          node tools/suchbestand-drift.mjs --streng  # Rueckgabewert 1

      ══ DIE EIGENTLICHE LEHRE ══════════════════════════════════════════════

      Die Hand-Eintraege waren nicht Schlamperei, sondern ein echtes
      Beduerfnis: gesucht wird nach dem PROBLEM (`Mono`, `Bass`), nicht nach
      dem Namen des Dings (`Klangwerk (Ton-Plugins)`). Diese Begriffe stehen
      nirgends auf der Seite, also kann die Erhebung sie nicht finden.

      Eine erzeugte Datei, die etwas braucht, das die Quelle nicht hergibt,
      hat kein Hand-Problem, sondern ein Quellen-Problem. Der Ausweg ist
      nicht, die Hand zu verbieten (das steht schon im Kopf und hat nicht
      gewirkt), sondern der Quelle einen ORT dafuer zu geben — ein Feld an der
      Karte, aus dem die Erhebung Synonyme mitnimmt. Solange es den nicht
      gibt, ist jedes `--schreiben` ein stiller Datenverlust.

      Und der Grund, warum das ueberhaupt auffiel, gehoert dazu: das Klangwerk
      war **gebaut, ausgeliefert und unauffindbar**. Ausgeliefert ist nicht
      benutzbar, solange der Weg dorthin nur dem bekannt ist, der es gebaut
      hat — siehe [[volles-wiki-ist-kein-beleg-fuer-gepflegte-doku]], dieselbe
      Ausfallart eine Ebene tiefer.
    related: ["volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "tests-duerfen-keine-zahlen-festnageln", "gegenprobe-statt-gruen-glauben", "wiki-zustand-hat-haltbarkeit", "werkzeug-statt-wegwerfbefehl"]

  - id: gegenprobe-statt-gruen-glauben
    kind: regel
    title: "Ein gruener Test beweist nichts, bis man ihn absichtlich kaputtgemacht hat"
    match: "test ist gruen|gegenprobe|absichtlich sabotieren|anschlusspruefung|aufruf auskommentiert und test bleibt gruen|68 pruefungen gruen|beweist der test etwas"
    sev: high
    tags: [tests, pruefen, arbeitsweise, gegenlesen, werkzeug, 2026]
    stand: "gemessen 10.08.2026 an scripts/box/mupibox-standwache.py; Regel gilt weiter"
    body: |
      Ein Test, der gruen ist, kann aus dem falschen Grund gruen sein. **Vor dem
      Fertigmelden das Gepruefte absichtlich sabotieren und nachsehen, ob er rot
      wird** — ein Durchlauf je Schutzregel, in einer Wegwerfschleife, die die
      Dateien danach wiederherstellt.

      ══ DIE ZWEI FEHLALARME, DIE ES ZEIGTEN ════════════════════════════════

      Am 10.08.2026 an der Standwache (`scripts/box/mupibox-standwache.py`):

      * Die Anschlusspruefung suchte mit `"wache_stellen(b, box," in QUELLE`.
        Zur Gegenprobe wurde der Aufruf **auskommentiert** — der Test blieb
        gruen, denn die Zeichenkette steht im Kommentar ja weiterhin da. Ein
        Anschlusstest, der einen abgeklemmten Anschluss nicht bemerkt, ist
        wertlos.
      * Der Anker `b.schritt("—", "Zurueckdrehen` steht **zweimal** in
        `tools/ausliefern.py`. `.index` fand den ersten (die
        scripts-Variante); der gepruefte Abschnitt spannte 432 Zeilen und traf
        den richtigen Zweig nur, *weil* er so weit gefasst war.

      ══ WIE MAN ES STATTDESSEN MACHT ═══════════════════════════════════════

      * Aufrufe in Python ueber den **Syntaxbaum** pruefen, nicht ueber `in`:
        `ast.walk` nach `ast.Call` mit `func.id == "name"`, und `lineno` fuer
        Reihenfolge-Bedingungen („die Frist wird VOR dem Neustart gestellt").
      * In Shell-Dateien gibt es keinen Baum — dann wenigstens **zeilenweise**
        pruefen und `z.lstrip().startswith("#")` ausschliessen, statt die ganze
        Datei zu durchsuchen.
      * Anker auf **eindeutige** Zeichenketten setzen und die Breite des
        geprueften Abschnitts selbst mitpruefen (`< 80 Zeilen`).
      * Die Gegenprobe gehoert in die Meldung: „zwoelf Gegenproben, jede macht
        den Test rot" ist eine Aussage, „68 Pruefungen gruen" ist keine.

      **Achtung beim Aufraeumen danach:** Eine Gegenprobe, die per
      `git checkout` zuruecksetzt, wirft uncommittete Arbeit mit weg — die
      Sicherung gehoert in denselben Befehlsblock.
    related: ["aufraeumen-nachmessen", "tests-duerfen-keine-zahlen-festnageln", "xvfb-hat-keinen-fenstermanager", "messwerkzeug-muss-sich-selbst-pruefen", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku"]

  - id: tests-duerfen-keine-zahlen-festnageln
    kind: regel
    title: "Tests pruefen die Absicht, nicht abgeleitete Zahlen - sonst blockieren sie jede Aenderung"
    match: "test nagelt zahl fest|MAX_PUNKTE|messtakt geaendert tests rot|abgeleitete konstante im test|acht tests verdeckten einen|test prueft seitennamen"
    sev: high
    tags: [tests, pruefen, arbeitsweise, messen, 2026]
    stand: "gemessen 20.08.2026 bei E61; Regel gilt weiter"
    body: |
      Ein Test, der `MAX_PUNKTE === 10080` prueft, nagelt den Messtakt fest. Wer
      den Takt aendert, sieht ein rotes Testfeld statt dessen, was wirklich zu
      pruefen ist. Richtig: `MAX_PUNKTE * MESS_MS === HISTORIE_MS`.

      ══ WAS ES GEKOSTET HAT ════════════════════════════════════════════════

      Bei E61 (20.08.2026) waren neun Tests rot, von denen **acht nur den alten
      Takt festhielten** — und einer einen echten Fehler zeigte
      (SD-Schreiblast). Die acht verdeckten den einen. Genau dieselbe Bauart
      wie eine Wache, die schon rot ist: sie kann nicht noch einmal rot werden.

      Dasselbe bei Testdaten: `t: T0 + n * 60_000` prueft nach einer
      Taktaenderung Punkte im falschen Abstand, und „ein ausgefallener Punkt
      ist keine Luecke" wird zur Luecke, ohne dass sich am geprueften Code
      etwas geaendert hat.

      ══ NICHT NUR ZAHLEN — AUCH NAMEN ══════════════════════════════════════

      Am 21.08.2026 dieselbe Falle mit einem Namen statt einer Zahl:
      `tools/verwaltung-suche-vollstaendig.test.mjs` nagelte fest, dass „der
      Schlummer-Timer bei der Kinderzeit steht" — und die Seite heisst seit
      `dab6837c` **Profile**. Geprueft gehoerte „der Schlummer-Timer liegt auf
      derselben Seite wie die Spielzeiten", nicht wie diese Seite heisst.

      ══ DIE FRAGE BEIM SCHREIBEN ═══════════════════════════════════════════

      Ist diese Zahl (dieser Name) die **Absicht**, oder folgt sie aus einer
      Konstanten? Folgt sie — dann die Absicht pruefen (Zeitraum, Verhaeltnis,
      Regel, Nachbarschaft) und die Konstante importieren. Gilt auch fuer
      Schrittweiten in Testdaten.
    related: ["gegenprobe-statt-gruen-glauben", "vier-zahlen-haengen-am-messtakt", "erzeugte-suchliste-von-hand-ergaenzt", "mit-echten-werten-rechnen"]

  - id: werkzeug-statt-wegwerfbefehl
    kind: regel
    title: "Untersuchen heisst ein Werkzeug schreiben - kein Wegwerf-Einzeiler in der Shell"
    match: "wegwerfbefehl|einzeiler statt werkzeug|ping-sweep von hand|wohin gehoert das werkzeug|WOZU-kopfkommentar|hausform werkzeug"
    sev: medium
    tags: [werkzeug, werkzeuge, arbeitsweise, doku, 2026]
    stand: "Regel des Betreibers, 31.07.2026; gilt unbefristet"
    body: |
      **Untersuchen und Testen heisst: ein Werkzeug schreiben.** Keine
      Wegwerf-Einzeiler in der Shell, sondern eine benannte Datei in `tools/`,
      die beim naechsten Mal wieder laeuft — plus Eintrag in diesem Paket.

      ══ WARUM ════════════════════════════════════════════════════════════════

      Ein Werkzeug kostet einmal, ein nachgebauter Einzeiler jedes Mal neu —
      und man baut ihn beim zweiten Mal **schlechter**, weil die Feinheiten des
      ersten Laufs nicht mehr da sind. Genau so passiert: die Boxen wurden per
      Ping-Sweep von Hand gesucht, waehrend `tools/welche-box.sh` seit Tagen
      existierte und denselben Zweck erfuellt.

      ══ WOHIN ══════════════════════════════════════════════════════════════

      * `tools/` — Untersuchen, Messen, Pruefen am Entwicklungsrechner
        (`mupi-check.py`, `welche-box.sh`, `mitmessen.mjs`, `pruefen.sh`,
        `mupi-ton.py`)
      * `remote-step-installer/tools/` — was auf der Box laeuft und dort
        eingerichtet wird (`mupibox-network-sync.sh`,
        `mupibox-touch-bridge.py`, `force-dsi-output.sh`)

      ══ DIE HAUSFORM ═══════════════════════════════════════════════════════

      Jedes vorhandene Werkzeug beginnt mit einem Kopfkommentar
      **„WOZU/WOFUER"**, der die *konkreten Kosten* nennt, die zu ihm gefuehrt
      haben („kostete eine halbe Stunde und zwei Richtigstellungen"), dazu
      **„WAS ES NICHT TUT"**. Diese Form beibehalten — sie sagt dem naechsten
      Leser, wann das Werkzeug das richtige ist und wann nicht.

      ══ ANWENDEN ═══════════════════════════════════════════════════════════

      Vor einer Untersuchung in `tools/` nachsehen, ob es schon etwas gibt —
      dann erweitern statt neu bauen. Entsteht dabei eine Erkenntnis ueber das
      *Messen selbst* (etwa: 254 gleichzeitige Pings liefern unzuverlaessige
      Ergebnisse, in Bloecken scannen), gehoert sie in das Werkzeug, nicht in
      eine Antwort.
    related: ["messwerkzeug-muss-sich-selbst-pruefen", "werkzeug-meldet-ohne-empfaenger", "gegenprobe-statt-gruen-glauben", "offener-port-ist-keine-kennung"]

  - id: aufraeumen-nachmessen
    kind: regel
    title: "Wer sagt, er habe zurueckgestellt, hat es meist nur vorgehabt - selbst nachmessen"
    match: "zustand wiederhergestellt|aufgeraeumt gemeldet|helligkeit stand auf 20|aus 9 diensten wurden 10|ausgangszustand messen|gemerkter wert ueberlebt neustart"
    sev: high
    tags: [messen, box, arbeitsweise, dienste, sicherung, 2026]
    stand: "gemessen in der Nacht zum 08.08.2026 an der Box; Regel gilt weiter"
    body: |
      Nach einem Lauf, der am Geraet etwas verstellt hat, den Ausgangszustand
      **selbst** nachmessen — nicht dem Bericht glauben.

      ══ WAS PASSIERT IST ═══════════════════════════════════════════════════

      In der Nacht zum 08.08.2026 meldeten **drei Laeufe unabhaengig
      voneinander**, sie haetten die Box wieder hergerichtet. Die
      Bildschirmhelligkeit stand danach auf 20 % — und zwar *gemerkt* in
      `mupibox.displayBrightness`, also ueber jeden Neustart hinweg. Am Morgen
      haette das Kind vor einem dunklen Schirm gesessen.

      Keiner der drei hat gelogen: jeder hat zurueckgestellt, was er **selbst**
      angefasst hatte, und keiner hat das **Ganze** gemessen. Dasselbe Muster
      bei den Diensten: aus 9 wurden 10, weil ein Lauf einen neuen anlegte und
      startete. Das war richtig — aber es aendert den Ausgangszustand, und nur
      ein Blick auf die ganze Liste zeigt es.

      ══ ANWENDEN ═══════════════════════════════════════════════════════════

      Vor der Nachtarbeit den Zustand als **Liste** festhalten (Dienste,
      Konfigurationsschluessel, Helligkeit, Lautstaerke, Profile, Medienzahl).
      Danach dieselbe Liste erheben und Zeile fuer Zeile vergleichen — nicht
      stichprobenartig.

      Besonders auf Werte achten, die **gemerkt** werden: ein zurueckgesetzter
      Laufzeitwert ist harmlos, ein gespeicherter kommt nach dem Neustart
      wieder.
    related: ["gegenprobe-statt-gruen-glauben", "einmal-hinsehen-ist-keine-messung", "behoben-in-ist-eine-zustandsaussage-und-war-falsch", "erhoben-aber-nicht-gewertet-ist-schlimmer-als-nie-gemessen"]

  - id: xvfb-hat-keinen-fenstermanager
    kind: falle
    title: "Xvfb hat keinen Fenstermanager - Modal-Dialoge laufen dort durch und stuerzen auf dem Schreibtisch ab"
    match: "grab failed: window not viewable|_tkinter.TclError grab|xvfb bildschirmfoto|grab_set nach transient|toplevel modal stuerzt|kein fenstermanager installiert"
    sev: high
    tags: [tests, installer, oberflaeche, gegenlesen, werkzeug, 2026]
    stand: "gemessen 08.08.2026 an sdstart.py; Ursache gilt weiter"
    body: |
      Unter Xvfb laeuft **kein Fenstermanager**. Ein `tk.Toplevel` ist dort
      sofort sichtbar, deshalb gelingt `grab_set()` unmittelbar nach
      `transient()`. Auf einem echten Schreibtisch zieht erst der
      Fenstermanager das Fenster auf, und derselbe Aufruf stirbt:

          _tkinter.TclError: grab failed: window not viewable

      ══ WAS ES GEKOSTET HAT ════════════════════════════════════════════════

      Am 08.08.2026 beim MixPi-Karteninstaller (`sdstart.py`) genau so
      passiert: **vier Bildschirmfotos aus Xvfb sahen richtig aus**, der erste
      Klick des Betreibers auf seinem Rechner brach ab. Dieselbe Falle lag seit
      Laengerem auch im aelteren `sdgui.py`.

      ══ BEHEBUNG UND GRENZE ════════════════════════════════════════════════

      Bei Modal-Dialogen immer `fenster.wait_visibility()` **vor** `grab_set()`,
      und den Griff ans Ende des Aufbaus stellen.

      Und grundsaetzlich: Ein Bildschirmfoto aus Xvfb beweist, wie etwas
      **aussieht** — nicht, dass es auf einem Schreibtisch **laeuft**. Alles,
      was mit Fenster-Aufzug, Fokus, Stapelreihenfolge, Modalitaet oder
      Fensterdekoration zu tun hat, ist dort **nicht geprueft**. Auf diesem
      Entwicklungsrechner ist kein Fenstermanager installiert (kein
      openbox/metacity/i3/twm), der Fall laesst sich lokal also gar nicht
      nachstellen — dann gehoert das ausdruecklich gesagt statt „getestet".
    related: ["gegenprobe-statt-gruen-glauben", "messwerkzeug-muss-sich-selbst-pruefen", "einmal-hinsehen-ist-keine-messung"]

  - id: pi5-hat-16k-seiten
    kind: falle
    title: "Der Pi 5 hat 16-KB-Seiten - /proc/statm mit 4096 umzurechnen ist um Faktor 4 daneben"
    match: "getconf PAGESIZE|statm seiten umrechnen|4096 fest verdrahtet|speichermessung /proc|fork kostet genauso wenig wie worker_thread|smaps seitengroesse"
    sev: high
    tags: [pi5, messen, perf, box, dietpi, werkzeug, 2026]
    stand: "gemessen 14.08.2026 an der Box (Pi 5 Model B Rev 1.1, DietPi arm64)"
    body: |
      Die MuPiBox (Raspberry Pi 5 Model B Rev 1.1, DietPi arm64) hat
      `getconf PAGESIZE` = **16384**. Der Entwicklungsrechner (x86, CachyOS)
      hat 4096.

      ══ WARUM DAS NICHT KRACHT, SONDERN LUEGT ══════════════════════════════

      `/proc/<pid>/statm` und `/proc/<pid>/smaps` zaehlen **Seiten, nicht
      Bytes**. Wer beim Umrechnen 4096 fest verdrahtet, misst auf der Box um
      Faktor 4 zu wenig — und das Ergebnis **sieht plausibel aus**, statt zu
      krachen.

      Am 14.08.2026 lieferte das den Schluss „`child_process.fork` kostet auf
      dem Pi genauso wenig wie ein `worker_thread` (Faktor 1,0)", waehrend es
      auf x86 das 4,2-Fache war. Korrigiert: **43,9 MB gegen 11,4 MB,
      Faktor 3,8** — also die umgekehrte Entscheidungsgrundlage.

      ══ ANWENDEN ═══════════════════════════════════════════════════════════

      Bei jeder Speichermessung ueber `/proc` die Seitengroesse **abfragen**
      (`getconf PAGESIZE` bzw. `os.sysconf`), nie annehmen — und sie in die
      **Ausgabe schreiben**, damit die Zahl spaeter nachpruefbar bleibt.
      `process.memoryUsage().rss` ist davon nicht betroffen, das sind Bytes.

      Und allgemeiner: eine Zahl, die der Erwartung widerspricht, ist zuerst
      ein Verdacht gegen die **Messung**, nicht gegen die Erwartung. Aufgefallen
      ist der Fehler nur, weil 11,7 × 4 verdaechtig nahe am x86-Wert lag.

      Werkzeug: `tools/plugin-box-probe.mjs` (eigenstaendig, per scp auf die
      Box).
    related: ["gegenprobe-statt-gruen-glauben", "mit-echten-werten-rechnen", "richtung-des-zweifels-bei-treffern", "rueckweg-verfaelscht-die-messung"]

  - id: mit-echten-werten-rechnen
    kind: regel
    title: "Layout- und Groessenrechnungen mit den Werten des Geraets pruefen, nicht mit angenommenen"
    match: "angenommenes logo 64 px|blockhoehe korrigiert wirkungslos|max\\(1, 160//256\\)|boot-screen block hoeher als schirm|kiosk-marke lag auf dem zaehler|layout mit echten werten"
    sev: medium
    tags: [messen, boot, anzeige, oberflaeche, box, 2026]
    stand: "gemessen 20.08.2026 bei E60 am Boot-Screen; Regel gilt weiter"
    body: |
      Bevor eine Rechnung ueber Pixel, Bytes oder Zeiten „korrigiert" wird: die
      echten Werte **am Geraet auslesen** und die Rechnung damit durchspielen.

      ══ WAS PASSIERT IST ═══════════════════════════════════════════════════

      Beim Boot-Screen (E60, 20.08.2026) wurde die Blockhoehe korrigiert —
      formal richtig, praktisch **wirkungslos**, weil mit einem angenommenen
      Logo von 64 px gerechnet wurde. Das echte ist **256×256 auf einem 480 px
      hohen Schirm**, und die Skalierungswahl konnte nur ganzzahlig
      VERGROESSERN (`max(1, 160//256)` = 1). Der Block wurde 520 px hoch —
      hoeher als der Schirm.

      **Die Zahl war nie verrutscht, sie hatte keinen Platz.** Ein zweiter
      Anlauf mit gelesenen Werten fand es sofort.

      ══ ANWENDEN ═══════════════════════════════════════════════════════════

      Ein kurzes Skript auf der Box, das die Werte **ausgibt UND die Rechnung
      damit durchspielt** (vorher/nachher). Erst dann editieren.

      Bei Layouts zusaetzlich pruefen, ob zwei Elemente auf **derselben Hoehe**
      landen — die Kiosk-Marke lag exakt auf dem Zaehler und war deshalb
      unsichtbar.
    related: ["pi5-hat-16k-seiten", "tests-duerfen-keine-zahlen-festnageln", "gegenprobe-statt-gruen-glauben", "vier-zahlen-haengen-am-messtakt"]

  - id: pkill-toetet-die-eigene-shell
    kind: falle
    title: "pkill -f toetet die eigene Shell mit - Ende 144, bevor sonst etwas passiert"
    match: "pkill -f|pgrep -f xargs kill|Ende 144|exit 144|shell beendet sich selbst|vorschau haengt angeblich"
    sev: medium
    tags: [werkzeug, arbeitsweise, pruefen, 2026]
    stand: "gemessen 04.08.2026 zweimal hintereinander; Ursache gilt weiter"
    body: |
      `pkill -f <muster>` und `pgrep -f <muster> | xargs kill` beenden in dieser
      Umgebung die **eigene Shell** mit: Der Befehl laeuft als
      `zsh -c '… eval "<der ganze Befehl>" …'`, die Befehlszeile der Shell
      enthaelt das Muster also selbst. Der Lauf bricht mit **Ende 144** ab, und
      zwar bevor irgendetwas anderes passiert ist.

      ══ WARUM ES NICHT ZU SEHEN IST ════════════════════════════════════════

      Am 04.08.2026 zweimal hintereinander passiert (`pkill -f
      neu-vorschau.mjs`), jedes Mal mit dem Eindruck, ein **Werkzeug haenge** —
      dabei war die Shell tot. An der Ausgabe ist die Ursache nicht zu sehen:
      `ps | grep` zeigt danach nur den Wrapper.

      ══ DIE BEIDEN SICHEREN WEGE ═══════════════════════════════════════════

      `-x` verlangt, dass die GANZE Befehlszeile passt, und die einer Shell ist
      immer laenger:

          pkill -x -f "(/usr/bin/)?node tools/neu-vorschau.mjs"

      Oder gezielt ueber den Prozess, der den Port haelt: `ss -ltnp | grep
      <port>` liefert die PID, dann `kill <pid>`.

      Dieselbe Familie wie die Backtick-Falle in Commit-Nachrichten: **die
      Shell liest den Befehl anders, als er gemeint war.** Steht als Kommentar
      auch in `tools/leihgabe.mjs`, wo die Warnmeldung diesen Befehl
      vorschlaegt.
    related: ["fish-kennt-kein-var-praefix", "werkzeug-statt-wegwerfbefehl", "befehl-ohne-rechte-und-rueckruf-ohne-blick"]

  - id: fish-kennt-kein-var-praefix
    kind: falle
    title: "fish kennt kein VAR=wert-Praefix - die Variable kommt leer an, der Fehler sieht anders aus"
    match: "No binary for Chrome browser|CHROME_BIN leer|VAR=wert befehl fish|env VAR=wert|parse error \\(eval\\):1|welche shell laeuft wirklich"
    sev: medium
    tags: [tests, werkzeug, arbeitsweise, angular, 2026]
    stand: "gemessen 15.08.2026 an diesem Entwicklungsrechner"
    body: |
      `CHROME_BIN="$browser" npx ng test …` lief in der fish-Shell dieses
      Rechners scheinbar durch, aber **CHROME_BIN kam LEER an** — fish kennt die
      Bourne-Form „Variable vor dem Befehl" nicht. Der Testlauf scheiterte mit
      `No binary for Chrome browser`, obwohl der Pfad stimmt
      (Playwright-Chromium unter
      `~/.cache/ms-playwright/chromium-*/chrome-linux64/chrome`).

      ══ WARUM MAN AN DER FALSCHEN STELLE SUCHT ═════════════════════════════

      Der Fehler sieht aus wie ein **fehlender Browser**, nicht wie ein
      Shell-Problem. In bash-Skripten (`tools/pruefen.sh`) funktioniert die
      Praefix-Form weiterhin; nur direkte Befehlsaufrufe laufen durch fish.

      ══ ANWENDEN ═══════════════════════════════════════════════════════════

      `env VAR=wert befehl …` schreiben, nie das nackte Praefix.

      **Nachtrag 15.08.2026:** In jener Sitzung wertete das Werkzeug ueber
      **zsh** aus (`/usr/bin/zsh -c source …/snapshot-zsh-…`), obwohl die
      Umgebung `/bin/fish` meldet — fish-Syntax (`for pid in (…); …; end`)
      scheiterte mit `(eval):1: parse error`. **Welche Shell wirklich laeuft,
      sagt erst die Fehlermeldung bzw. `ps`**; POSIX-Formen
      (`for …; do …; done`, `$(…)`) laufen in beiden. `env VAR=wert` bleibt der
      sichere Weg.
    related: ["pkill-toetet-die-eigene-shell", "werkzeug-statt-wegwerfbefehl", "einzelne-integrationsspec-braucht-node-env-test"]

  - id: halbe-pruefung-meldet-wie-eine-ganze
    kind: falle
    title: "Die Verweiswache sah nur den Fliesstext - 32 tote related:-Verweise blieben jahrelang gruen"
    match: "wiki-schau verweis ins leere|related zeigt ins leere|nur eine haelfte geprueft|verweis auf merkdatei|wissen nur im gedaechtnis nicht im paket|pruefung meldet fuer beide haelften"
    sev: high
    tags: [doku, werkzeug, pruefen, tests, gegenlesen, 2026]
    stand: "gemessen 21.08.2026 im stuendlichen Doku-Lauf, behoben in derselben Fassung 236"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      `tools/wiki-schau.py` meldete am 21.08.2026 **einen** Verweis ins Leere.
      Das stimmte — und war trotzdem irrefuehrend. Geprueft hatte es nur die
      Verweise im **Fliesstext** (`[[id]]` und `[id]`). Die `related:`-Listen
      am Ende jedes Eintrags sah es gar nicht an. Dort standen:

          related-Verweise gesamt        761
          davon ins Leere                 32   auf 9 verschiedene Namen

      Neunzehn davon allein auf `gegenprobe-statt-gruen-glauben`.

      ══ WOHIN SIE ZEIGTEN ══════════════════════════════════════════════════

      Alle neun Namen waren **private Merkdateien ausserhalb des Repos**
      (`~/.claude/.../memory/*.md`). Fuer den Schreibenden loesten sie auf, fuer
      jeden anderen Leser waren es Sackgassen — und zwar die neun Regeln, auf
      die sich das Paket am haeufigsten beruft: Gegenprobe, Werkzeug statt
      Wegwerfbefehl, Aufraeumen nachmessen, Pi-5-Seitengroesse, fish-Praefix,
      pkill, Xvfb, echte Werte, Tests ohne festgenagelte Zahlen.

      **Das ist dieselbe Ausfallart wie `werkzeug-meldet-ohne-empfaenger`, eine
      Ebene tiefer:** das Wissen existierte, es war nur nicht dort, wo der
      Verweis hinzeigt. Alle neun sind mit Fassung 236 als Eintraege
      nachgetragen.

      ══ DIE EIGENTLICHE LEHRE ══════════════════════════════════════════════

      Eine Pruefung, die eine **Teilmenge** ansieht, meldet ihr Ergebnis in
      Worten, die nach der **Gesamtmenge** klingen. „1 Verweis ins Leere" liest
      sich wie eine Aussage ueber alle Verweise; es war eine Aussage ueber 516
      von 1277. Niemand luegt dabei, und genau deshalb faellt es nicht auf.

      Beim Bauen einer Wache also mitschreiben, **was sie NICHT ansieht** — und
      diese Grenze in ihre Ausgabe stellen, nicht nur in den Kopfkommentar. Die
      Uebersicht nennt seit dem 21.08. alle drei Formen mit ihrer Zahl
      nebeneinander; eine fehlende Form faellt dort sofort auf.

      Gegengeprobt: ein erfundener Name **nur** in einer `related:`-Liste
      (Fliesstext unberuehrt) macht `--pruefen` rot (Ende 1); ohne die
      Sabotage bleibt es gruen. Vorher waere derselbe Fall unbemerkt
      durchgelaufen.
    related: ["gegenprobe-statt-gruen-glauben", "werkzeug-meldet-ohne-empfaenger", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "erzeugte-suchliste-von-hand-ergaenzt", "messwerkzeug-muss-sich-selbst-pruefen", "nachziehen-endet-erst-an-der-fernkopie"]

  - id: aktiver-weg-braucht-kein-beschneiden
    kind: erkenntnis
    title: "Der Leerlauf-Arbeiter liefert dichte Dateien - die Beschneide-Regel des passiven Wegs gilt hier nicht"
    match: "stille am ende abschneiden|beschneiden der aufnahme|tail silence mitschnitt|dauer stimmt nicht mit titel|single-track endet genau|nachbearbeitung flac noetig"
    sev: mid
    tags: [mitschnitt, ton, messen, e66, 2026]
    stand: "gemessen 21.08.2026 an der ersten fertigen Datei der Box"
    body: |
      ══ DIE ERSTE FERTIGE DATEI ════════════════════════════════════════════

      Am 21.08.2026 hat der Leerlauf-Arbeiter unbeaufsichtigt seinen ersten
      Titel auf die Platte gelegt. Nachgemessen, nicht dem Protokoll geglaubt:

          Signatur   fLaC
          Format     48000 Hz
          Dauer      196,65 s (3:16)
          Groesse    17,7 MB

      Und dann die Frage, die vor 30 weiteren Dateien beantwortet gehoert:
      **haengt am Ende Stille an?** `silencedetect` ueber die ganze Datei,
      Schwelle -50 dB, Mindestlaenge 0,5 s:

          KEINE EINZIGE Stillephase — weder vorne noch hinten.

      ══ WARUM DAS KEIN ZUFALL IST ══════════════════════════════════════════

      Der PASSIVE Weg (Mithoeren, waehrend die Familie spielt) schneidet einen
      DURCHLAUFENDEN Strom in Stuecke. Er weiss nicht, wo ein Titel endet — er
      raet es aus Pegel und Zeit. Deshalb braucht er Beschneiden, und deshalb
      steht das dort im Code.

      Der AKTIVE Weg besitzt den Prozess. `--single-track <uri>` spielt genau
      einen Titel und **beendet sich selbst**; der Mitschneider wird an diesem
      Ende gestoppt. Der Schnitt kommt nicht aus einer Schaetzung, sondern aus
      dem Lebensende eines Prozesses. Da ist nichts zu beschneiden.

      ══ DIE LEHRE ══════════════════════════════════════════════════════════

      **Eine Regel aus dem passiven Weg gilt im aktiven nicht automatisch.**
      Beide heissen „Mitschnitt", beide erzeugen FLAC — aber die eine Seite
      raet und die andere weiss. Wer die Beschneide-Regel mitnimmt, baut eine
      Nachbearbeitung gegen ein Problem, das dieser Weg nicht hat, und
      verliert dabei die ersten und letzten Sekunden echten Tons.

      Vor dem Uebertragen einer Regel also fragen, WORAUS sie kam. Kam sie aus
      einer Unsicherheit, die der andere Weg nicht teilt, gehoert sie nicht
      mit.
    related: ["mit-echten-werten-rechnen", "einmal-hinsehen-ist-keine-messung", "gegenprobe-statt-gruen-glauben"]

  - id: zwei-wachen-eine-datei-verschiedene-antwort
    kind: falle
    title: "Zwei Wachen ueber derselben Datei - die eine gruen, die andere rot, und die rote hatte unrecht"
    match: "pack-verweise meldet was wiki-schau nicht meldet|toter verweis der keiner ist|beispiel in backticks als verweis gezaehlt|zwei pruefer widersprechen sich|wache wird rot beim beschreiben des fehlers"
    sev: med
    tags: [doku, werkzeug, pruefen, gegenlesen, 2026]
    stand: "gemessen und behoben 21.08.2026 im stuendlichen Doku-Lauf, Fassung 238"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      Ueber `llmwiki/pack.yaml` wachen ZWEI Werkzeuge mit derselben Aufgabe:

          tools/wiki-schau.py --pruefen     still, Ende 0   → gruen
          tools/pack-verweise.py            BEFUND: 1       → rot

      Dieselbe Datei, dieselbe Minute, gegensaetzliche Antwort. Rot war:

          halbe-pruefung-meldet-wie-eine-ganze: Fliesstext `[[id]]` GIBT ES NICHT

      Es gibt keine Kennung `id`. Der Eintrag ERKLAERT die Verweisformen und
      schreibt dazu „(`[[id]]` und `[id]`)" hin — ein ZITAT, kein Verweis.

      ══ WARUM NUR EINE DER BEIDEN ES WUSSTE ════════════════════════════════

      `wiki-schau.py` hat diese Konvention am **02.08.2026** gelernt, mit
      einem ausfuehrlichen Kommentar an der Stelle (`ZITAT`-Muster: was in
      Backticks steht, ist zitiert und nicht gemeint). `pack-verweise.py` kam
      spaeter, mit demselben Zweck — und ohne diese Lehre. Das Wissen lag im
      Kopf des einen Werkzeugs und nie im anderen.

      **Ein zweiter Pruefer erbt die Lehren des ersten nicht.** Er erbt nur
      die Aufgabe. Wer eine zweite Wache neben eine bestehende stellt, muss
      deren Sonderfaelle mitnehmen — sonst hat man nicht zwei Meinungen,
      sondern eine gute und eine veraltete.

      ══ WAS DAS KOSTET ═════════════════════════════════════════════════════

      Der Befund war NICHT abstellbar: Solange der Eintrag existiert, der die
      Verweisform beschreibt, meldet die Wache ihn. Ein Dauerbefund erzieht
      dazu, die ganze Ausgabe wegzuschauen — und dann faellt der echte tote
      Verweis daneben auch nicht mehr auf. Dieselbe Ausfallart wie bei
      `halbe-pruefung-meldet-wie-eine-ganze`, nur andersherum: dort sah eine
      Wache zu wenig, hier meldete eine zu viel. Beide Male ist die Zahl in
      der Ausgabe nicht das, was sie zu sein scheint.

      ══ BEHOBEN UND GEGENGEPROBT ═══════════════════════════════════════════

      `pack-verweise.py` schneidet Backtick-Spannen jetzt vor der Suche weg,
      mit demselben Muster und derselben Begruendung wie das aeltere Werkzeug.

          ohne Sabotage                             keine toten Verweise, Ende 0
          erfundene Kennung OHNE Backticks
          in denselben Eintrag gesetzt              BEFUND: 1, Ende 1

      Die Sabotage lief auf einer WEGWERFKOPIE unter dem Sitzungsordner, nicht
      auf `llmwiki/pack.yaml` — eine Gegenprobe, die schreibt, braucht ein
      Ziel, dessen Verlust egal ist.

      ══ DIE REGEL ══════════════════════════════════════════════════════════

      Widersprechen sich zwei Wachen, ist das kein Grund, eine davon zu
      glauben — es ist der Befund. Und beim Bau einer Wache gilt der Satz von
      der aelteren Schwester weiter: **eine Pruefung, die beim BESCHREIBEN
      eines Fehlers rot wird, erzieht dazu, sie wegzuschauen.**

      ══ NACHTRAG: DIESER EINTRAG IST SELBST DARUEBER GESTOLPERT ════════════

      Beim ersten Ablegen wurde er dreimal rot — und jede Meldung stimmte:

      * Die Ausgabe der Wache stand hier als EINGERUECKTER Block. Die
        Konvention deckt nur BACKTICKS, nicht Einrueckung; wer eine Wache
        zitiert, muss ihre Zeilen also zusaetzlich in Backticks setzen.
      * `related:` zeigte auf einen Namen aus den privaten Merkdateien unter
        `~/.claude/.../memory/` statt auf eine Kennung im Paket — genau die
        Ausfallart aus `halbe-pruefung-meldet-wie-eine-ganze`, keine drei
        Wochen nach ihrer Beschreibung.

      Der Eintrag, der eine Fehlerklasse erklaert, ist ihr wahrscheinlichster
      naechster Fall. Das ist kein Zufall: er ist die einzige Stelle, an der
      man die Form absichtlich hinschreibt.
    related: ["halbe-pruefung-meldet-wie-eine-ganze", "gegenprobe-statt-gruen-glauben", "messwerkzeug-muss-sich-selbst-pruefen", "werkzeug-statt-wegwerfbefehl"]

  - id: stiller-maler-am-falschen-fach
    kind: falle
    title: "Sie holte ihre Zahlen und zeichnete die Nachbarseite - ein Aufruf, der brav nichts tut"
    match: "seite bleibt auf wird geholt|daten kommen an aber nichts erscheint|leistung bleibt leer|malen kehrt sofort zurueck|falsches fach im wachposten|kopierte zeile zeichnet die falsche seite"
    sev: high
    tags: [admin, oberflaeche, werkzeug, pruefen, 2026]
    stand: "gemessen am Geraet 21.08.2026, behoben mit tools/malpaare-schau.mjs, Fassung 238"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      Gemeldet als „system/leistung klappt nicht so": die Seite stand auf
      „Wird geholt …" — dauerhaft.

      **Die Daten kamen die ganze Zeit an.** `/api/leistung` antwortete am
      Geraet mit 200 und vollen Gruppen, `this.leistung` wurde gesetzt. Am
      Ende von `leistungHolen()` stand aber

          this.boxMalen()

      und `boxMalen` beginnt mit `if (this.fach !== 'info') return`. Wer auf
      „leistung" steht, ruft also einen Maler, der sofort zurueckkehrt. Die
      Seite, auf der man steht, zeichnet niemand.

      ══ WARUM NICHTS DAS FING ══════════════════════════════════════════════

      Der Aufruf ist syntaktisch fehlerlos. Die Funktion GIBT es. Sie tut
      brav nichts. Keine Ausnahme, keine Warnung, kein roter Test — nur eine
      leere Seite. Das ist die unangenehmste Sorte Abschreibfehler: er sieht
      an jeder einzelnen Stelle richtig aus.

      Er entsteht auch systematisch. Die beiden Nachbarn `boxHolen` und
      `sysHolen` enden **voellig zu Recht** auf `boxMalen` — ihr Fach heisst
      „info". Beim Kopieren des Musters wandert die letzte Zeile mit, und nur
      der Wachposten am Anfang des Malers merkt, dass sie nicht mehr passt —
      und der schweigt.

      ══ WERKZEUG STATT BLOSSER BERICHTIGUNG ════════════════════════════════

      `tools/malpaare-schau.mjs` prueft fuer jede `…Holen()`, auf welches Fach
      sie WACHT und welches `…Malen()` sie RUFT. 19 Paare geprueft, genau
      dieses eine war krumm.

      Beim Bau fielen zwei Fehler AM PRUEFER auf, beide behoben — und beide
      sind die eigentliche Lehre:

      * Er las `this.boxMalen()` aus dem KOMMENTAR, mit dem die Berichtigung
        erklaert war, und gab prompt Entwarnung. **Ein Pruefer, den ein
        Kommentar beruhigt, prueft nichts.** Kommentare fallen jetzt vor der
        Suche weg.
      * Er meldete `boxHolen` und `sysHolen` als Verdacht, obwohl beide
        gesund sind — das Fach heisst „info", sein Maler `boxMalen`. **Ein
        Pruefer, der bei gesundem Code Alarm schlaegt, wird abgeschaltet.**
        Die Zuordnung steht jetzt in `MALER_ZU_FACH`.

      Gegenprobe: den Fehler in einer KOPIE wieder eingebaut → Rueckgabe 1; am
      berichtigten Stand → Rueckgabe 0.

      ══ DIE REGEL ══════════════════════════════════════════════════════════

      Wo ein Wachposten (`if (fach !== …) return`) am Anfang einer Funktion
      steht, ist jeder Aufruf dieser Funktion eine **stille Bedingung**. Solche
      Paare gehoeren maschinell gegengelesen, nicht im Kopf — im Kopf sehen
      neunzehn richtige und ein falsches Paar gleich aus.
    related: ["pruefen-schritte-die-still-luegen", "werkzeug-statt-wegwerfbefehl", "messwerkzeug-baut-sich-den-befund-selbst", "gegenprobe-statt-gruen-glauben", "halbe-pruefung-meldet-wie-eine-ganze"]

  - id: value-am-select-greift-nicht-bei-for-optionen
    kind: falle
    title: "Das Auswahlfeld hatte die richtige Option und zeigte die falsche - [value] greift vor dem @for"
    match: "select zeigt falschen wert|auswahlfeld springt auf standard|option nicht vorgewaehlt angular|value am select wirkungslos|dropdown zeigt ersten eintrag|senke wird nicht angezeigt"
    sev: high
    tags: [oberflaeche, angular, verwaltung, falle, 2026]
    stand: "gemessen 21.08.2026 an der Stroeme-Karte, behoben in derselben Fassung"
    body: |
      ══ DAS BILD ═══════════════════════════════════════════════════════════

      Die neue Stroeme-Karte zeigte fuer Strom 2 „der Standard-Ausgabe folgen",
      obwohl in der Konfiguration `mixpi-mitschnitt` stand und die
      Schnittstelle es auch so lieferte. Am Baum nachgelesen:

          {"id":"senke2","wert":"","gewaehlt":0,
           "optionen":["","entzerrer","mixpi-mitschnitt","ueberall",…]}

      DIE RICHTIGE OPTION WAR DA. Nur ausgewaehlt war sie nicht.

      ══ WARUM ══════════════════════════════════════════════════════════════

      `[value]` am `<select>` setzt die DOM-Eigenschaft. Stehen die Optionen
      aus einem `@for` zu diesem Zeitpunkt noch nicht im Baum, findet die
      Zuweisung nichts — und faellt STILL auf den ersten Eintrag zurueck. Keine
      Warnung, kein Fehler, nur eine falsche Anzeige.

      Der Unterschied war in derselben Karte zu sehen: „Wofuer" und „Client"
      stimmten, ihre Optionen stehen fest in der Vorlage. Nur das Feld mit
      `@for`-Optionen log.

      ══ DIE BEHEBUNG, DIE ES IM HAUS SCHON GAB ═════════════════════════════

          <option [value]="a.name" [selected]="a.name === z.senke">

      Genau so machen es konfiguration.ts, ton.ts, streaming.ts und
      darstellung.ts — an VIER Stellen. Das Wissen war also da; es stand nur
      nirgends geschrieben, und deshalb liess es sich neu erfinden.

      ══ WARUM DAS GEFAEHRLICH IST, NICHT NUR HAESSLICH ═════════════════════

      Hier hielt der Signalwert die Wahrheit, gespeichert wurde also richtig.
      Das ist Glueck und keine Eigenschaft: sobald ein Formular seinen Stand
      AUS DEM DOM liest, schreibt das naechste Speichern die stille
      Ruecksetzung fest. Beim Mitschnitt-Strom hiesse das: der zweite Strom
      ginge auf den Familienlautsprecher — genau das Echo, gegen das die
      Leersenke gebaut wurde.

      GEGENGEPROBT mit `tools/verwaltungsseite-abbilden.mjs --lies`: vor der
      Behebung `["",""]`, danach `["","mixpi-mitschnitt"]`. Am BILD allein war
      es nicht zu sehen — ein Auswahlfeld sieht mit jedem Wert gleich aus.
    related: ["mit-echten-werten-rechnen", "gegenprobe-statt-gruen-glauben", "halbe-pruefung-meldet-wie-eine-ganze"]

  - id: pruefwerkzeug-erfand-eine-zweite-lage
    kind: falle
    title: "Zwei identische Bilder, hell und dunkel genannt - der Umschalter gehoerte einer anderen Oberflaeche"
    match: "hell und dunkel gleich|data-licht wirkt nicht|zwei bilder identisch|umschalter ohne wirkung|lage gemessen die es nicht gibt"
    sev: mid
    tags: [werkzeug, oberflaeche, pruefen, gegenlesen, 2026]
    stand: "gemessen 21.08.2026 beim ersten Lauf des neuen Werkzeugs"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      Ein neues Abbild-Werkzeug lief stur ueber `['hell','dunkel']` und setzte
      dazwischen `data-licht` am Wurzelelement. Es meldete zwei Lagen, zwei
      Bilder, zwei Haken — und beide Bilder waren DASSELBE dunkle Bild.

      `data-licht` ist der Umschalter der SPIELER-Oberflaeche. Die Verwaltung
      kennt ihn nicht; im ganzen `frontend-admin` steht er nur in einer
      Testdatei.

      ══ WARUM DAS SCHLIMMER IST ALS EIN FEHLENDES BILD ═════════════════════

      Ein Werkzeug, das eine Lage nicht abbilden KANN, sagt das. Dieses hier
      hat eine Lage ERFUNDEN und ihr einen Haken gegeben. Das ist dieselbe
      Ausfallart wie [[halbe-pruefung-meldet-wie-eine-ganze]], nur eine Stunde
      spaeter und vom selben Schreibenden — der Eintrag war zu dem Zeitpunkt
      schon committet.

      ══ DIE BEHEBUNG ═══════════════════════════════════════════════════════

      Nicht die Liste der Lagen raten, sondern die WIRKUNG messen: die
      Hintergrundfarbe vorher und nachher vergleichen. Bleibt sie gleich, sagt
      das Werkzeug „UEBERSPRUNGEN — data-licht bewirkt hier nichts" und macht
      ein Bild statt zwei.

      DIE REGEL: ein Umschalter, dessen Wirkung nicht nachgemessen wird, ist
      keine Messung, sondern eine Behauptung mit Bild daneben.
    related: ["halbe-pruefung-meldet-wie-eine-ganze", "gegenprobe-statt-gruen-glauben", "messwerkzeug-muss-sich-selbst-pruefen"]

  - id: pool-statt-rolle-am-strom
    kind: entwurf
    title: "Der Zweck war am Strom festgeschweisst - ein Pool macht aus zwei Rollen N Zugaenge"
    match: "pool von stroemen|zweck am strom|mehrere mitschnitte gleichzeitig|hoeren hat vorrang|verdraengung mitschnitt|stream frei picken|abarbeiten schneller"
    sev: high
    tags: [stroeme, e74, entwurf, mitschnitt, spotify, 2026]
    stand: "entschieden 22.08.2026 mit dem Betreiber; Modell und Zeugen stehen, Verdrahtung offen"
    body: |
      ══ DIE FRAGE ══════════════════════════════════════════════════════════

      Betreiber: „kann man nicht einfach einen pool von strömen haben und
      falls da pickt sich einer den zum hören" — dazu die Regeln: „hören hat
      vorrang zu mitschnitt", „mitgeschnitten werden kann wenn keiner hört
      bzw ein stream frei ist", „wenn mehr als einer frei ist geht das
      abarbeiten schneller".

      ══ WARUM ES VORHER ANDERS WAR ═════════════════════════════════════════

      Nicht durch Entscheidung, sondern durch Wachstum. E66 war „der
      Mitschnitt bekommt einen eigenen Zugang", E72 machte daraus nummerierte
      Stroeme — aber der ZWECK blieb am Strom stehen (`zweck: 'wiedergabe' |
      'mitschnitt'` in der Konfiguration). Ein Strom WAR eine Rolle. Folge:
      der zweite Zugang lag still, waehrend die Liste 22 Titel tief war, und
      ein dritter haette gar nichts beschleunigt.

      ══ DAS GEGENARGUMENT, DAS HIER NICHT GILT ═════════════════════════════

      Ueblicherweise sind Konten NICHT austauschbar — jedes hat seine eigene
      Bibliothek. Fuer diese Box gilt das nicht: sie spielt keine „deine
      Playlists", sondern die URIs ihrer EIGENEN Medienliste, und jedes
      Premium-Konto kann jede URI abspielen. Was bleibt, ist kosmetisch: der
      „zuletzt gehoert"-Verlauf verteilt sich auf die Konten, die dran waren.

      Das ist der Punkt, an dem der Entwurf steht oder faellt — deshalb steht
      er hier und nicht als Nebensatz im Code.

      ══ DIE DREI REGELN (`vergeben()` in stroeme.ts) ════════════════════════

      1. HOEREN GEWINNT IMMER. Ist nichts frei, weicht ein Mitschnitt.
      2. VERDRAENGT WIRD DER JUENGSTE. Er hat am wenigsten gesammelt; die halbe
         Datei ist ohnehin verloren (Umbenennen erst bei Erfolg), also soll es
         moeglichst wenig sein. Die aeltere Aufnahme wird fertig.
      3. DER MITSCHNITT HAELT `reserve` STROEME FREI — er nimmt einen nur,
         wenn DANACH noch so viele frei sind. Damit ist Verdraengung der
         Sonderfall statt des Normalbetriebs.

      Die Entscheidung ist PUR: keine Prozesse, keine Ordner, keine Uhr. Pool
      + Belegung + Nachfrage rein, Nummer + GRUND raus — nie ein nacktes
      `null`, das spaeter jemand raten muss.

      ══ WAS ES KOSTET (am Geraet gemessen, 21.08.2026) ═════════════════════

          ein Soloist   49 MB (Hauptprozess) + 20 MB (Helfer)  ~69 MB
          die Box       2010 MB gesamt, 1100 MB verfuegbar, 4 Kerne
          fuenf Stroeme ~350 MB — passt

      CPU ist kein Thema (0,3 %). Die Grenze ist der Speicher, nicht die Zeit.

      ══ WAS NOCH FEHLT ═════════════════════════════════════════════════════

      Modell und Zeugen stehen (Gegenprobe: aeltesten statt juengsten
      verdraengen -> rot; Reserve falsch gerechnet -> rot). OFFEN ist die
      Verdrahtung: das Plugin nimmt heute EINEN Titel und findet seinen Strom
      ueber `fuerZweck(…, 'mitschnitt')`. Die Vergabe gehoert zum WIRT, nicht
      ins Plugin — nur der Wirt sieht alle Nachfrager. Das Plugin fragt, der
      Wirt teilt zu.
    related: ["mit-echten-werten-rechnen", "gegenprobe-statt-gruen-glauben", "aktiver-weg-braucht-kein-beschneiden"]

  # ══ DYNAMIK IM KLANGWERK: LADSPA IM KERN, NICHT IM PLUGIN ════════════════
  - id: klangwerk-dynamik-ladspa-im-kern
    kind: howto
    title: "Kompressor und Begrenzer — LADSPA darf der Kern, nicht das Plugin"
    tags: [audio, klangwerk, ladspa, pipewire, dynamik, gemessen]
    stand: "am Geraet gebaut und gemessen, 21.08.2026 (MixPiBox, Pi 5)"
    body: |
      ══ WOZU ═══════════════════════════════════════════════════════════════

      Betreiber: „ich denke wir machen 1 und 2". Der Fall sind Hoerspiele —
      leiser Dialog, dann Musik. Das Kind dreht wegen der Dialoge auf, und die
      naechste Szene knallt. Der Kompressor holt die Spitzen herunter und hebt
      mit `makeup` wieder an; der Begrenzer ist die Decke darueber und
      schuetzt die 3-W-Chassis.

      ══ PIPEWIRE HAT DAFUER NICHTS EIGENES ══════════════════════════════════

      Die builtins kennen nur `clamp`, und das ist hartes Clipping. Ein
      „Begrenzer", der Verzerrung erzeugt, waere eine Falle. Gebraucht werden
      echte Dynamikwerkzeuge, und die kommen aus LADSPA:

          swh-plugins, 6,7 MB
          sc4_1882.so                    -> Kompressor
          fast_lookahead_limiter_1913.so -> Begrenzer

      ══ DER RIEGEL GILT WEITER — FUER PLUGINS ══════════════════════════════

      In klangkette.ts steht: kein freier `filter.graph`-Text, weil
      `convolver` und `ladspa` Dateien nachladen. Das bleibt so. Ein
      Fremdplugin beschreibt nur `{ art: 'kompressor', … }` und kann KEINEN
      Pfad angeben; welche .so geladen wird, entscheidet allein der Kern aus
      einer geschlossenen Liste.

      DER UNTERSCHIED IST DER ZWISCHEN „das Plugin darf eine Datei benennen"
      und „der Kern benutzt eine Datei, die er selbst gewaehlt hat". Wer die
      Grenze fuer eine Inkonsequenz haelt, hat die falsche Haelfte gelesen.

      ══ DREI DINGE, DIE MAN NICHT RATEN KANN ════════════════════════════════

      Alle drei scheiterten beim ersten Versuch:

        1. `plugin` braucht den VOLLEN PFAD. Mit dem Kurznamen „sc4" bricht
           PipeWire ab („failed to load plugin 'sc4'"), und ohne LADSPA_PATH
           sucht es nirgends.
        2. Die LADSPA-LABEL heissen anders als die Dateien: `sc4` und
           `fastLookaheadLimiter` (nicht `fast_lookahead_limiter`).
        3. BEIDE SIND STEREO-KNOTEN. Sie tragen „Left input"/„Right input"
           bzw. „Input 1"/„Input 2" und werden EINMAL gebaut — anders als die
           `bq_*`, die je Kanal instanziiert werden. Zwei getrennte
           Kompressoren regelten UNABHAENGIG: ein lauter Ton links zoege nur
           links den Pegel herunter, und das wandert hoerbar in der Mitte.

      Gemessen: volle Kette (Basis, Hochpass, Hoehen, Kompressor, Begrenzer)
      laedt mit 0,6 % CPU und 9,4 MB.

      ══ FEHLT DIE .so, FAELLT NUR DAS GLIED HERAUS ══════════════════════════

      Eine Kette, die auf eine fehlende Datei zeigt, laedt GAR NICHT — dann
      waere die Senke weg, auf die der Ton schon geroutet ist. `ketteConfBauen`
      bekommt deshalb eine `gibtDatei`-Pruefung hereingereicht (der Server
      gibt `fs.existsSync`, Tests geben eine Attrappe) und laesst ein Glied
      ohne seinen Baustein einfach aus.

      ══ SIE LAUFEN BEI VORGABE NICHT MIT ═══════════════════════════════════

      Anders als die fuenf Filter, deren neutrale Fassung nichts kostet: die
      LADSPA-Bausteine laden eine fremde .so und rechnen. Der Preis ist
      ehrlich zu nennen — Ein- und Ausschalten aendert die STRUKTUR, der Kern
      muss neu bauen statt live zu stellen, und der Ton setzt kurz aus. Am
      REGLER ziehen (Verhaeltnis, Schwelle, Decke) bleibt live.

      ══ UND DAS PAKET STEHT IN BEIDEN AUSROLLWEGEN ══════════════════════════

      `swh-plugins` wurde am 21.08. von Hand nachinstalliert und stand danach
      in KEINEM der beiden Wege — eine Neuinstallation haette es verloren, und
      zwar STILL (das Glied faellt heraus, der Ton laeuft, die Regler stehen
      da und tun nichts). Jetzt in `autosetup.sh` (AUDIO_PAKETE) und in
      `update/start_mupibox_update.sh` bei BEIDEN PipeWire-Zweigen; am
      PulseAudio-Zweig fehlt es mit Absicht, dort gibt es kein Klangwerk.
    related: ["klangwerk-geschlossenes-vokabular-statt-freier-conf", "unit-nur-in-autosetup-erreicht-keine-bestehende-box"]

  # ══ GLEICH LAUT: ZWEI ORTE, ZWEI MITTEL ══════════════════════════════════
  - id: lautheit-zwei-orte-kompressor-und-replaygain
    kind: howto
    title: "„Gleich laut“ heisst zweierlei — und wird an zwei Orten gemacht"
    tags: [audio, lautheit, replaygain, mpv, klangwerk]
    stand: "gebaut 21.08.2026, mpv-Optionen am Geraet geprueft"
    body: |
      Die Frage „alles gleich laut" hat zwei Bedeutungen, und sie werden an
      verschiedenen Stellen beantwortet. Wer das vermischt, baut das eine und
      wundert sich, dass das andere bleibt.

      ══ INNERHALB EINES STUECKS -> KOMPRESSOR, im Klangwerk ═════════════════

      Leiser Dialog, dann knallende Musik. Das ist Dynamik INNERHALB einer
      Datei; dagegen hilft nur ein Kompressor im Signalweg
      ([[klangwerk-dynamik-ladspa-im-kern]]). Er wirkt auf ALLES, auch auf
      Spotify, weil alles durch das Klangwerk laeuft.

      ══ ZWISCHEN DEN STUECKEN -> REPLAYGAIN, im Abspieler ═══════════════════

      Ein Hoerspiel ist leise gemastert, das naechste laut. Das ist KEINE
      Aufgabe der Filterkette: die sieht nur einen Strom und weiss nicht, dass
      gerade ein anderes Stueck begonnen hat. Es gehoert zum Abspieler.

          mpv:      --replaygain=track --replaygain-clip=yes
          Spotify:  GAR NICHTS — siehe die Berichtigung gleich darunter

      ══ BERICHTIGUNG 22.08.2026: SPOTIFY NORMALISIERT NICHT ════════════════

      Hier stand „soloist normalisiert selbst". DAS WAR FALSCH — angenommen,
      nicht nachgesehen. `soloist --help` kennt genau EINEN Lautstaerkeschalter,
      `--initial-volume`, und keine Normalisierung. Die Vorlagen, die welche
      tragen, gelten fuer ANDERE Abspieler: `config/templates/spotifyd.conf`
      hat `volume-normalisation`/`normalisation-pregain`, env-librespot
      ebenfalls — auf dieser Box laeuft aber soloist.

      FOLGE: seit dem 21.08. sind die lokalen Dateien untereinander ausgeglichen
      (ReplayGain), Spotify ist es nicht — und der Sprung liegt jetzt AM
      QUELLENWECHSEL. Die Verbesserung an der einen Seite hat die Kante an der
      anderen sichtbarer gemacht. Gemessen ist der Abstand noch nicht:
      [[pegelsprung-quellenwechsel-noch-ungemessen]].

      REPLAYGAIN UND NICHT `loudnorm`: mpv kann beides, aber loudnorm (ffmpeg,
      EBU R128) ANALYSIERT beim Abspielen und braucht Vorlauf — auf einem Pi
      Rechenzeit bei jedem Titel. ReplayGain liest ein Tag, das im Stueck
      schon steht, und kostet nichts.

      `track` UND NICHT `album`: die Box spielt Hoerspielkapitel und einzelne
      Lieder, oft quer durcheinander. Album-Werte hielten die Unterschiede
      INNERHALB eines Albums absichtlich fest — richtig fuer eine
      durchgehoerte Platte, falsch fuer eine Kachelbox.

      ══ EHRLICH DAZU ═══════════════════════════════════════════════════════

      OHNE TAG PASSIERT NICHTS. Die meisten gerippten und geladenen Dateien
      tragen ReplayGain-Tags; wo keine sind, spielt mpv wie bisher. Es ist
      eine Verbesserung fuer den Grossteil der Bibliothek, keine Garantie je
      Datei.

      ══ WAS NICHT GEHT: ebur128 ALS REGLER ═════════════════════════════════

      PipeWire bringt eine eigene Filterfamilie `ebur128` mit — sie MISST aber
      nur (momentary, shortterm, global, range, peak). Ein Regelkreis daraus
      braeuchte eine Ruecklaufkante im Graphen (die filter-chain ist azyklisch)
      und eine dB-nach-linear-Umrechnung; die builtins kennen weder `log` noch
      `exp`. Nachgesehen, nicht vermutet.
    related: ["klangwerk-dynamik-ladspa-im-kern"]

  - id: artefakte-veralten-still-vor-dem-kartenlauf
    kind: falle
    title: "Eine frische Karte traegt den Stand des Zips, nicht den des Baums"
    tags: [installer, kartenlauf, deploy, artefakt, pruefwerkzeug, 2026]
    source: "Session 2026-08-22; tools/kartenlauf-bereit.py am Baum gemessen, deploy.zip neu gebaut (ba6adf29)"
    body: |
      DIE FRAGE WAR: „ist der Lautstaerkeausgleich auch bei einer
      Neuinstallation da?" IN DER IMPLEMENTIERUNG ja — auf einer frischen
      KARTE nein. `bin/nodejs/deploy.zip` war vom 20.08. 14:15, der
      Klangwerk-Commit vom 21.08. 23:34. Das Zip enthielt weder `klangwerk`
      noch `klangkette` noch `replaygain`, und die NewDesign-Dateien wichen ab.

      DAS IST DIE EIGENTLICHE FALLE: Was im Repo steht, ist nicht, was der
      Installer ausrollt. Der Installer rollt ein GEBAUTES Artefakt aus. Es ist
      immer da, es sieht nie kaputt aus, und sein Datum liest niemand
      freiwillig nach. Zwischen „im Baum behoben" und „auf der Karte" liegt ein
      Bauschritt, den nichts erzwingt.

      DAS ZIP IST NICHT DER EINZIGE WEG. Wer eine bestehende Box mit
      `tools/ausliefern.py` bespielt, bekommt den Baumstand direkt — genau
      deshalb faellt der Abstand so lange nicht auf: Der Entwickler liefert
      aus, die frische Karte tut es nie.

      ── MESSEN STATT DATUM LESEN ─────────────────────────────────────────
      `python3 tools/kartenlauf-bereit.py` (`--bauen` baut gleich). Es
      vergleicht ZUERST den Inhalt und erst dann die Uhrzeit, denn die Uhrzeit
      luegt in beide Richtungen: Der Angular-Bau fasst Asset-Quellen an, ohne
      sie zu aendern. Verglichen wird nur, was 1:1 durchgereicht wird
      (`NewDesign/*` -> `www/neu/*`); alles andere ist gebuendelt, minifiziert
      und gehasht und hat keine vergleichbare Quelle. Ein kleiner sicherer
      Ausschnitt schlaegt eine grosse Heuristik: stimmen DIESE Dateien nicht,
      ist das Zip garantiert alt.

      Gebaut wird mit `src/deploy.sh` — es packt und legt das Ergebnis selbst
      nach `bin/nodejs/deploy.zip`. Die Handarbeit in
      [[mupi-app-lauf-checkliste]] ist nur noch der Notnagel.

      ══ UND DIE ZWEITE LEHRE: EINE WACHE UEBER EINER LEICHE ════════════════

      `kartenlauf-bereit.py` verlangte an ZWEI Stellen
      `AdminInterface/release/www.zip` mit `mupi.php` und `admin.php` — die
      alte PHP-Verwaltung. Die hat das Projekt am 19.08.2026 verlassen
      (52d21406), samt Verzeichnis und Bauskript. Die Pruefung blieb stehen.

      WAS DAS ANRICHTETE: Unter JEDER Ausgabe stand „NICHT startklar", egal wie
      sauber der Baum war — und `--bauen` brach mit „Verzeichnis fehlt" ab,
      NACHDEM deploy.zip schon gebaut war. Ein Pruefer, der bei gesundem
      Zustand Alarm schlaegt, wird abgeschaltet oder ueberlesen; und dann sieht
      niemand mehr den ECHTEN Befund, der danebensteht. Genau der stand hier:
      das veraltete deploy.zip.

      ALSO: Wer etwas aus dem Projekt entfernt, muss die WACHEN darueber
      mitentfernen. Eine Wache ohne Bewachtes ist schlimmer als keine — sie
      kostet die Glaubwuerdigkeit der uebrigen. Verwandt, eine Ebene weiter:
      [[halbe-pruefung-meldet-wie-eine-ganze]] und
      [[zwei-wachen-eine-datei-verschiedene-antwort]].

      NICHT VERWECHSELN: Die NEUE Verwaltung (Angular, `src/frontend-admin`)
      steckt als `www-admin/` IM deploy.zip und wird mitgebaut. Sie brauchte
      nie einen eigenen Eintrag im Pruefer.
    related: ["mupi-app-lauf-checkliste", "halbe-pruefung-meldet-wie-eine-ganze", "zwei-wachen-eine-datei-verschiedene-antwort", "unit-nur-in-autosetup-erreicht-keine-bestehende-box"]

  - id: dienst-haelt-seinen-strom-dauerhaft
    kind: falle
    title: "Strom 1 war nie frei - soloist.service haelt ihn, und die Vergabe wusste es nicht"
    match: "use --data-dir to specify a writable location|strom 1 zugeteilt scheitert|pool teilt belegten strom zu|zwei soloist ein datenordner|reserve zaehlt doppelt"
    sev: high
    tags: [stroeme, e74, mitschnitt, spotify, systemd, 2026]
    stand: "am Geraet gemessen und behoben 22.08.2026"
    body: |
      ══ DER ERSTE POOL-LAUF SCHEITERTE ══════════════════════════════════════

      Die Vergabe teilte dem Mitschnitt STROM 1 zu — den niedrigsten freien.
      Soloist antwortete mit seinen eigenen Worten:

          Soloist: Use --data-dir to specify a writable location.

      Zwei Ursachen in einer Zeile, und die zweite ist die wichtige.

      ══ 1. DER DATENORDNER, ZWEIMAL GEREGELT ═══════════════════════════════

      Das Plugin rechnet `/var/lib/mixpi-strom-${nr}`, das Modell rechnet fuer
      Strom 1 `/var/lib/soloist` (die StateDirectory der Unit). Zwei Regeln
      fuer dieselbe Sache, und sie widersprachen sich. `/var/lib/mixpi-strom-1`
      gibt es gar nicht, und anlegen darf der Server dort nicht.

      ══ 2. STROM 1 IST KEIN FREIER PLATZ ═══════════════════════════════════

      Das ist der eigentliche Fund. `soloist.service` LAEUFT DAUERHAFT mit dem
      Zugang von Strom 1 und haelt `/var/lib/soloist` samt `.lock`. Zwei
      Soloist-Prozesse teilen sich keinen Datenordner.

      DIE MITGLIEDER DES POOLS SIND ALSO NICHT GLEICHARTIG: einer wird von
      einem dauerhaften Dienst gehalten, die uebrigen werden bei Bedarf
      gestartet. Ein Pool-Entwurf, der alle fuer austauschbar haelt, verteilt
      einen Platz, auf dem schon jemand sitzt.

      BEHOBEN, indem der Wirt eintraegt, was der Dienst haelt
      (`dienstBelegungSichern()`), bevor er verteilt.

      ══ UND DAMIT ZAEHLTE DIE RESERVE DOPPELT ══════════════════════════════

      „Immer einen fuers Hoeren freihalten" ist bei dieser Bauart schon
      erfuellt: der Dienst HAELT ihn. Mit `reserve = 1` obendrauf kam der
      Mitschnitt bei zwei Stroemen NIE zum Zug — dieselbe Zusicherung zweimal
      gerechnet. Vorgabe ist deshalb 0; wer einen zweiten Platz fuer ein
      zweites Kind freihalten will, setzt `spotify.stromReserve`.

      ══ WAS DARAN ALLGEMEIN IST ════════════════════════════════════════════

      Wer eine feste Zuordnung durch eine Vergabe ersetzt, muss ZUERST
      aufschreiben, wer schon etwas haelt. Die alte Welt hatte keine
      Buchfuehrung noetig, weil nichts sich bewegte — und genau deshalb steht
      der Bestand nirgends. Die Vergabe faengt nicht bei null an.

      Nachgemessen: der Titel, der am Datenordner scheiterte, liegt nach der
      Behebung als 18,7-MB-FLAC auf der Platte, und das Buch zeigt Strom 1 bei
      soloist.service, Strom 2 beim Mitschnitt.
    related: ["pool-statt-rolle-am-strom", "mit-echten-werten-rechnen", "einmal-hinsehen-ist-keine-messung"]

  - id: verdraengen-durch-fragen-statt-toeten
    kind: erkenntnis
    title: "Der Verdraengte fragt selbst, ob er noch dran ist - niemand muss einen fremden Prozess toeten"
    match: "verdraengung mitschnitt umsetzen|laufende aufnahme abbrechen|hoeren hat vorrang umsetzen|lebenszeichen ok false|prozess abschiessen vermeiden"
    sev: mid
    tags: [stroeme, e74, mitschnitt, entwurf, 2026]
    stand: "am Geraet bewiesen 22.08.2026"
    body: |
      ══ DIE AUFGABE ════════════════════════════════════════════════════════

      „Hoeren hat Vorrang zu Mitschnitt" heisst: eine laufende Aufnahme muss
      weichen, wenn ein Kind anfangen will. Der naheliegende Weg waere, den
      Aufnahmeprozess von aussen abzuschiessen — der Wirt kennt ja die
      Prozesskennung nicht einmal.

      ══ DER WEG, DER STATTDESSEN GEWAEHLT WURDE ════════════════════════════

      Der Wirt nimmt dem Verdraengten nur den EINTRAG weg. Der Arbeiter fragt
      waehrend der Aufnahme alle fuenf Sekunden „habe ich den Strom noch?" —
      dasselbe Lebenszeichen, das ohnehin die Frist verlaengert. Kommt
      `ok: false`, hoert er von selbst auf.

      DREI DINGE WERDEN DAMIT EINS: Frist verlaengern, Verdraengung erfahren
      und „lebe ich noch" melden. Eine Frage, drei Antworten.

      UND ES GREIFT AUCH DANN, wenn die Verdraengung von ganz woanders kommt —
      etwa weil die Frist ablief oder jemand von Hand freigab. Wer stattdessen
      Prozesse abschoesse, muesste jede dieser Stellen einzeln bedienen.

      ══ EINE AUSGEFALLENE FRAGE HEISST „JA" ════════════════════════════════

      Vorsichtige Richtung: wer beim ersten Netzhaenger die halbe Aufnahme
      wegwirft, verliert Arbeit an einem Zweifel. Bleibt der Wirt wirklich weg,
      laeuft ohnehin die Frist ab.

      ══ VERDRAENGUNG IST KEIN FEHLSCHLAG ═══════════════════════════════════

      Der Titel hat nichts falsch gemacht. Wuerde er als `scheitern` verbucht,
      zaehlte der Versuch — und nach drei Kindern, die zwischendurch Musik
      anmachen, laege er auf `fehler`, ohne dass je etwas an ihm falsch war.
      JE BELIEBTER DIE BOX, DESTO KAPUTTER DIE LISTE.

      Dafuer gibt es `zuruecklegen()`: zurueck auf `offen` und genau der EINE
      Versuch zurueck, den dieser Lauf gekostet hat — nicht `zuruecksetzen`,
      das auch echte fruehere Fehlschlaege vergaebe.

      ══ AM GERAET BEWIESEN (22.08.2026) ════════════════════════════════════

          Aufnahme laeuft (Marke 2)
          POST /api/stroeme/vergabe {fuer: wiedergabe}
            -> {"nr":2,"grund":"nichts frei — der juengste Mitschnitt weicht",
                "verdraengt":2,"marke":3}
          nach 8 s: Plugin meldet „bereit", kein --single-track-Prozess mehr
          Liste:    Zustand offen, Grund LEER (also zuruecklegen, nicht scheitern)

      Der leere Grund ist der Beleg: `scheitern` setzt immer einen, und sei es
      „ohne Angabe".
    related: ["pool-statt-rolle-am-strom", "dienst-haelt-seinen-strom-dauerhaft", "gegenprobe-statt-gruen-glauben"]

  - id: pegelsprung-quellenwechsel-noch-ungemessen
    kind: offen
    title: "Der Sprung Spotify -> lokale Datei ist real, die Zahl dazu fehlt noch"
    match: "spotify lauter als mpv|lautstaerke springt beim quellenwechsel|wie laut ist welche quelle|lufs messen box|pegelvergleich"
    sev: mid
    tags: [audio, lautheit, replaygain, soloist, messen, offen, 2026]
    stand: "Werkzeug angelegt 22.08.2026 (8ec2b87f), NOCH NICHT LAUFFAEHIG"
    body: |
      ══ DER STAND, UND ER IST UNFERTIG ═════════════════════════════════════

      Betreiberfrage vom 22.08.: „und von spotify zu mpv?". Der Sprung ist
      real und nicht ausgeglichen:

          lokale Dateien   mpv       ReplayGain track (seit 21.08.)
          Spotify          soloist   NICHTS — nur --initial-volume

      Dazu kommt ein zweiter Abstand, selbst wenn soloist es koennte:
      ReplayGain zielt auf etwa -18 LUFS, Spotify liefert typisch lauter. Zwei
      Ziele, nicht eines. Siehe [[lautheit-zwei-orte-kompressor-und-replaygain]].

      DER REGLER IST LAENGST DA: „Wie laut — je Quelle" auf der Ton-Seite,
      WirePlumber merkt den Wert je Anwendung. WAS FEHLT, IST DIE ZAHL. Nach
      Gehoer raten trifft es nicht, ein geratener dB-Wert im Quelltext waere
      Pfusch — deshalb misst ein Werkzeug, statt dass jemand schaetzt.

      ══ DAS WERKZEUG: tools/box/pegelvergleich.py ══════════════════════════

      Misst LUFS je Strom, und STUMM: der Strom wird fuer die Messdauer auf
      eine `module-null-sink` umgeleitet und dort am Monitor abgegriffen
      (`pw-record --target=<senke>.monitor`), ffmpeg rechnet `ebur128`. Nachts
      messbar, ohne dass jemand aufwacht — der Preis ist ehrlich zu nennen:
      waehrend der Messung hoert man NICHTS, wer danebensteht haelt die Box
      fuer abgestuerzt. Das Werkzeug sagt vorher, wie lange es still bleibt.

      Es kennt KEINE Dienstnamen — es misst, was der Tonserver als Strom
      fuehrt. Heisst der naechste Dienst „Jellyfin", steht er ohne Aenderung
      in der Liste.

      ── WAS TRAEGT ───────────────────────────────────────────────────────
      Die Einzelteile sind am Geraet geprueft: `pactl load-module
      module-null-sink` legt die Senke an, `pw-record` nimmt auf (761900 Bytes
      in 4 s), ffmpeg kann ebur128.

      UND VOR ALLEM DER RUECKWEG. Nach einem Lauf, der mitten in der Messung
      am Netzabriss zerbrach, standen 0 Null-Senken und die Standard-Senke
      wieder auf `klangwerk`. Das `finally` greift auch beim Wegbrechen der
      Verbindung. Ein Messwerkzeug, das die Box stumm zuruecklaesst, ist
      schlimmer als gar keine Messung: niemand sucht die Ursache spaeter in
      etwas, das „nur gemessen" hat.

      ── WAS NICHT TRAEGT (HIER WEITERMACHEN) ─────────────────────────────
      Die VERKETTUNG in `messen()` liefert „nicht messbar". Verdaechtig ist
      das Zusammenspiel aus mpv im Hintergrund, `sleep` und der Pipe ueber
      SSH — nicht die Messidee selbst, die Teile funktionieren einzeln.

      ── IN DIE EIGENE HAUSFALLE GETRETEN ─────────────────────────────────
      Beim Aufraeumen fand `pgrep -f mixpi-pegelprobe` die EIGENE Shell (der
      Suchbegriff steht in deren Kommandozeile), und `kill` darauf beendete
      die eigene SSH-Verbindung — [[pkill-toetet-die-eigene-shell]], zum
      zweiten Mal. `pgrep -x mpv` statt dessen.

      ══ WAS DER NAECHSTE NICHT NOCHMAL PRUEFEN MUSS ════════════════════════

      Dass soloist normalisieren kann. Kann es nicht, `--help` gelesen. Und
      die Normalisierungswerte in `config/templates/spotifyd.conf` und
      env-librespot gelten fuer spotifyd/librespot, nicht fuer soloist — sie
      zu setzen aendert auf dieser Box nichts.
    related: ["lautheit-zwei-orte-kompressor-und-replaygain", "pkill-toetet-die-eigene-shell", "werkzeug-statt-wegwerfbefehl"]

  - id: commit-ohne-main-wirkt-nirgends
    kind: falle
    title: "Fertige Arbeit, die in einem Arbeitsbaum liegen bleibt — git status sieht sie nie"
    match: "commit fehlt in main|arbeitsbaum vergessen|worktree commit nicht uebernommen|fix gebaut aber wirkt nicht|zweig niemand sieht"
    sev: high
    tags: ["git", "worktree", "arbeitsweise", "verlust", 2026]
    source: >
      19.08.2026, beim Nachsehen gefunden und daraufhin als
      tools/worktrees-schau.py festgehalten (Commit f6e07b0e).
    symptom: >
      Ein Fehler, der als behoben galt, ist immer noch da. Der Fix wurde
      gebaut, am Geraet gemessen und committet — er wirkt trotzdem nirgends.

      Am Gefundenen: acht Arbeitsbaeume unter `.claude/worktrees/`, 3,4 GB, und
      darin DREI Commits, die es nie nach main geschafft hatten. Der juengste
      war 85 Minuten alt und behob genau den Fehler, den die Inventur desselben
      Tages als „hoch" eingestuft hatte. Die beiden anderen lagen fuenf und
      sechs Tage.
    cause: >
      Sitzungen dieses Projekts arbeiten in eigenen Arbeitsbaeumen und
      committen dort auf einen EIGENEN ZWEIG. Niemand fuehrt Buch, ob diese
      Commits je in main ankommen.

      DIE FRAGE „IST ALLES COMMITTED" BEANTWORTET `git status` — aber nur fuer
      den Baum, in dem man gerade steht. Sie ist ausserdem die falsche Frage.
      Es gibt zwei sehr verschiedene Sorten Verlust, und die gefaehrlichere
      sieht sauber aus:

        AENDERUNG OHNE COMMIT   kennt jeder, `git status` zeigt es, der Baum
                                sieht schmutzig aus und mahnt von selbst.
        COMMIT OHNE MAIN        kennt niemand. Der Baum ist sauber, die Arbeit
                                ist „erledigt", die Sitzung ist zu Ende — und
                                der Zweig wird nie wieder angesehen.

      Je gruendlicher gearbeitet wurde, desto unauffaelliger der Verlust: eine
      fertige, gemessene, committete Aenderung hinterlaesst keinerlei Spur, die
      zum Nachsehen auffordert.
    fix: >
      Regelmaessig ueber ALLE Baeume fragen, nicht nur ueber den eigenen:

          python3 tools/worktrees-schau.py

      Es meldet in dieser Reihenfolge: FEHLT IN MAIN (Commits auf dem Zweig,
      die main nicht hat — der Kern), UNCOMMITTET (Aenderungen ohne Commit),
      UNBERUEHRT (wie lange der Baum still liegt).

      ES RAEUMT NICHT AUF, mit Absicht. Ein Baum kann zu einer LAUFENDEN
      Sitzung gehoeren; `git worktree remove` mitten hinein kostet genau die
      Arbeit, die sichtbar zu machen der Zweck ist. Es zeigt, der Mensch
      entscheidet.

      DER GUTE ZEITPUNKT ist vor dem Ausliefern und nach jeder laengeren
      Sitzung — dieselbe Stelle, an der auch `tools/pruefen.sh` laeuft.
    related: ["zweig-kann-unter-dir-wechseln", "werkzeug-statt-wegwerfbefehl", "aufraeumen-nachmessen"]

  - id: heikle-testfunktion-nicht-ueber-ssh-schalten
    kind: regel
    title: "Was eingeschaltet Schaden anrichtet, wird nicht ueber eine Leitung geschaltet, die abreissen kann"
    match: "testfunktion blieb an|mitschnitt lief weiter|ssh abgerissen mitten im test|schalter nicht zurueckgestellt"
    sev: high
    tags: ["mitschnitt", "ssh", "wlan", "arbeitsweise", "recht", 2026]
    source: >
      16.08.2026 an dieser Box passiert; die Regel steht seither im Kopf von
      tools/box/mitschnitt-durchlauf.py und haengt dort an `finally`.
    body: >
      DER FALL: Ein Testlauf fuer das Mitschnitt-Plugin lief ueber ssh. Die Box
      trennt sich derzeit alle paar Minuten selbst vom WLAN (reason=3,
      locally_generated=1). Der Lauf brach mittendrin ab — und zwar an der
      schlechtestmoeglichen Stelle: NACH dem Einschalten und VOR dem
      Ausschalten.

      Zurueck blieb eine rechtlich heikle Testfunktion, eingeschaltet, weil
      eine Verbindung abriss. Das ist keine Frage von Sorgfalt: der Abbruch
      passiert ausserhalb des Programms, und ein Programm, das gar nicht mehr
      laeuft, kann auch nichts zuruecksetzen.

      DIE REGEL, in zwei Teilen:

        1. AUF DER BOX LAUFEN LASSEN, nicht ueber die Leitung. Der Durchlauf
           wird per `nohup … &` angeworfen und schreibt in eine Logdatei; die
           wird geerntet, wenn die Verbindung wieder steht. Reisst das WLAN ab,
           laeuft der Test weiter und schaltet zu Ende.

               nohup python3 mitschnitt-durchlauf.py > /home/dietpi/probe.log 2>&1 &

        2. DAS AUSSCHALTEN AN `finally` HAENGEN, nie ans Ende des glatten
           Wegs. Es muss auch dann passieren, wenn oben etwas wirft und wenn
           jemand Strg-C drueckt. Dazu gehoert das Aufraeumen der Kindprozesse
           (`pkill -x pw-record` — `-x`, nie `-f`, siehe
           pkill-toetet-die-eigene-shell).

      WORAUF SIE SONST NOCH ZUTRIFFT: alles, dessen EINGESCHALTETER Zustand
      teurer ist als ein misslungener Test — Mitschnitt, Kindersicherung
      ausgehaengt, ein Limit hochgesetzt, eine Wache stillgelegt. Nicht
      dagegen auf Messungen, die nur lesen; die duerfen abbrechen.

      DIE VERWANDTE HALBWAHRHEIT: „Ich stelle es danach zurueck" ist eine
      Absicht, kein Mechanismus. Wer sie im Kopf haelt statt im `finally`, hat
      sie beim naechsten Abriss nicht.
    related: ["pkill-toetet-die-eigene-shell", "plugin-ereignis-darf-nicht-warten", "mitschnitt-plan", "aufraeumen-nachmessen"]
  - kind: howto
    id: audit-2026-08-22-kritiker
    title: "Kritiker-Lauf 22.08.2026 — Plugin-Fixes erreichen Bestandsboxen nie, Klangwerk fuenffach, Spotify-Frist verstrichen"
    source: "AUDIT-2026-08-22.md im Fork-Repo; 5 unabhaengige Pruefer (Abgleich zum 21.08., toter Code, Doppelungen, Ueber-Bau, Internet) + eigene Messungen, HEAD a2d2257d"
    tags: [audit, plugins, klangwerk, spotify, ausrollen, 2026]
    body: |
      GEPRUEFT: das Delta seit 59cb204b (55 Commits, ~17.700 Zeilen — Klangwerk/
      LADSPA, E66-Mitschnitt, E74-Pool, Wischgesten). Volltext und alle Belege:
      AUDIT-2026-08-22.md im Repo. Vom Kritiker-Lauf des VORTAGES (21.08.) war
      beim Nachmessen KEIN Vorschlag umgesetzt — dessen Rangliste gilt fort.

      DIE VIER HOCH-BEFUNDE:

      1. `cp -rn` LIEFERT PLUGIN-KORREKTUREN NIE AN BESTANDSBOXEN
         (autosetup.sh:966 UND start_mupibox_update.sh:1284). -n = no-clobber:
         eine Box, die mixpi-mitschnitt schon hat, bekommt keinen Fix mehr —
         auch die Soloist-Umstellung (afa15677) nicht. Der Commit 1da7f982
         baute den Update-Weg und versperrte ihn im selben Zug. Fix:
         mitgelieferte Kennungen per Liste mit -f, Fremdes weiter mit -n.

      2. config/templates/62-mixpi-mitschnitt.conf STEHT IN KEINEM AUSROLLWEG
         (grep '62-mixpi' in beiden Skripten: 0 Treffer; die Schwester
         61-entzerrer.conf steht in beiden). Ohne die Datei existiert die
         Leersenke auf keiner ausgerollten Box — der Leerlauf-Arbeiter (E66)
         laeuft dort ins Leere. Dritter Fund derselben Klasse binnen zwei
         Tagen; das eigentliche Argument fuer EINE gemeinsame Ausrolltabelle.

      3. DAS KLANGWERK EXISTIERT FUENFFACH und die Kopien widersprechen sich:
         Regler-Tabelle in plugin.json (ohne Grenzen), index.mjs (klemmt
         kompressor 0..20), admin ton.ts (bietet 0..8), box klang.page.ts
         (kennt nur 5 der 8 Regler — Kompressor/Schwelle/Begrenzer fehlen an
         der Box), NewDesign app.js. Dazu die Zeichen-Mathematik dreifach
         zeichengleich. Naht: Grenzen ins Manifest (plugin-vertrag um
         min/max/schritt erweitern), GET /api/ton/klangwerk liefert regler[],
         alle Oberflaechen rendern daraus.

      4. SPOTIFY-ZWANGSMIGRATION WAR AM 09.03.2026 — die Box nutzt weiter
         playlists/{id}/tracks (server.ts:8092/:8481, spotify-auth.service.ts
         :552). Entfernt sind auch ALLE Batch-Abrufe und Browse-Endpunkte;
         Suchlimit max 10 statt 50. Extended-Quota-Apps sind NICHT betroffen —
         darum spielt die Box noch. ERSTE MESSUNG: in welchem Modus laufen die
         Apps? @spotify/web-api-ts-sdk 1.2.0 (01/2024) ist tot — als
         eingefroren behandeln, duenner eigener Wrapper. librespot/Soloist
         nicht betroffen (eigenes Protokoll). go-librespot ist bei v0.9.0
         (16.08.2026): Platten-Cache mit LRU, /player/output — beides fuer
         diese Box relevant (SD-Last, Bluetooth-Wechsel).

      KLEINERES, FESTGEHALTEN: Klangwerk-Kuhschwaenze ueberlappen den
      Fuenfband-Entzerrer dahinter, und die Frequenzgang-Kurve rechnet den
      Entzerrer NICHT mit ein — die Spitze-Anzeige ist zu optimistisch.
      Loeschliste mit 8 belegten Posten (wischWort, Belegungsbuch.leeren,
      dateiname, andereStroeme, fuerZweck+mitschnittPlaetze, .kw-ohren,
      stromReserve, .server-leck-probe.9992.ts). pruefen.sh-Schritt 7
      (PHP-Admin) prueft gegen einen Baum, den es nicht mehr gibt.

      VERFAHRENS-LEHRE: zwei Kritiker-Laeufe hintereinander, deren erster
      vollstaendig liegen blieb, heisst: Audit-Top-5 als eigene Backlog-
      E-Nummern eintragen, sonst veralten sie als Datei neben dem Backlog.
    related: ["mitschnitt-plan", "zwei-ausrollwege-eine-handgefuehrte-liste", "aufraeumen-nachmessen"]

  - id: koppelwerkzeug-schrieb-in-den-dienstordner
    kind: falle
    title: "Ein Koppel-Werkzeug mit festen Pfaden legte Konto 2 in die Sitzung des Dienstes — die Box verschwand zwei Tage spaeter wortlos aus Spotify Connect"
    match: "logged in: no|box nicht mehr in spotify connect|soloist koppeln|falsches konto nach neustart|strom 2 gekoppelt|soloist-koppeln"
    sev: high
    tags: ["soloist", "spotify", "stroeme", "werkzeug", "falle", 2026]
    source: >
      22.08.2026, tools/box/soloist-koppeln.py. Der Schaden entstand am 20.08.
      beim Koppeln von „Strom 2" und fiel erst am 22.08. um 01:33 beim
      Neustart auf. Behoben in f51b1c8b, zwei Folgefehler in a6a0e99d.
    body: >
      ══ WAS PASSIERTE ══════════════════════════════════════════════════════

      Das Werkzeug benutzte FESTE Pfade — `/var/lib/soloist` und
      `/var/cache/soloist`, die Ordner DES DIENSTES — egal welcher Strom
      gemeint war. Schritt 2 loescht dort `settings/`. „Strom 2 koppeln" schrieb
      also Konto 2 in die Sitzung des Familien-Dienstes.

      SICHTBAR WURDE ZWEI TAGE LANG NICHTS: der laufende Dienst hatte seine
      Sitzung schon im Speicher. Erst der Neustart stellte die gespeicherte
      Sitzung (Konto 2) wieder her, lief aber mit dem Schluessel von Konto 1
      und kam mit `logged in: no` hoch. Die Box war aus Spotify Connect
      verschwunden, ohne eine einzige Fehlermeldung.

      DERSELBE FEHLER BEIM SCHLUESSEL: gelesen wurde stets
      `.spotify.soloistApiKey`, also der von Strom 1. Der Aufruf hiess „Strom 2
      koppeln" und tat: Konto 2 mit Strom-1-Schluessel in den Dienst-Ordner
      schreiben.

      ══ DIE LEHRE, DIE UEBER DIESES WERKZEUG HINAUSGEHT ════════════════════

      Ein Werkzeug, das einen Parameter ANNIMMT, aber intern feste Pfade
      benutzt, ist gefaehrlicher als eines ohne Parameter: der Aufrufer glaubt,
      er habe gesteuert. Und weil ein laufender Dienst seinen Zustand im
      Speicher haelt, ist der SCHREIBENDE Eingriff vom SICHTBAREN Schaden durch
      einen Neustart getrennt — Tage koennen dazwischenliegen. Wer nach dem
      Koppeln „laeuft ja" sagt, hat nichts geprueft.

      ══ WIE ES JETZT AUSSIEHT (--strom N) ══════════════════════════════════

      - eigener Datenordner je Strom: 1 = der Dienst (`/var/lib/soloist`),
        ab 2 `/var/lib/mixpi-strom-N` bzw. `/var/cache/mixpi-strom-N`
      - Schluessel aus `.spotify.stroeme[] | select(.nr == N) | .schluessel`;
        NUR Strom 1 faellt auf `soloistApiKey` zurueck
      - Geraetename wie er wirklich laeuft: Strom 1 blosser Boxname, sonst
        „<Box> Stream N"
      - ab Strom 2 wird der Dienst NICHT mehr angehalten — das naehme der
        Familie beim Koppeln die Musik weg

      ══ DER UNTERSTUETZTE WEG STATT DES HANDGESCHNITZTEN ═══════════════════

      Die Spotify-Doku nennt `-p/--pair`: „Authenticate through Spotify
      Connect, store credentials, and exit". Bisher lief Soloist im Vordergrund
      unter `timeout` und wurde nach dem Antippen abgeschossen — fast dasselbe,
      aber es endete mit einem Kill, und ob die Sitzung dabei vollstaendig
      geschrieben war, stand nirgends. `--pair` beendet sich selbst. Das
      `timeout` bleibt als Notnagel, falls niemand antippt.

      Doku: developer.spotify.com/documentation/soloist/concepts/authentication
      „Keep the same data directory if you want the device to stay paired."

      ══ DIE AUFNAHME HAT DIESE FALLE NICHT (am Geraet gegengeprueft) ═══════

      `mupibox-server.service` hat KEIN `StateDirectory`, und der Serverprozess
      traegt keins in der Umgebung. Ein Aufnahme-Soloist ohne `-D` landet also
      unter `~/.local/share/soloist`, nicht im Ordner des Dienstes.

      ══ ZWEI FOLGEFEHLER AUS DEM UMBAU SELBST ══════════════════════════════

      Beide erst beim ersten ECHTEN Lauf gefunden, nicht beim Lesen:

      1. NameError VOR JEDEM EINGRIFF. `wer()` griff noch auf den frueheren
         Modulwert `DATEN` zu, den `--strom` zu einer lokalen Variable gemacht
         hatte. Das Werkzeug starb im Schritt „VORHER" — diesmal bevor es den
         Dienst anhielt, es haette genauso gut danach sein koennen. Der Ordner
         kommt jetzt als Parameter herein.

      2. DIE ERGEBNIS-MUSTER WAREN VOM ALTEN WEG. Geprueft wurde auf
         „waiting for login" — die Zeile des NORMALEN Starts. `--pair` sagt
         stattdessen „cleared existing session before pairing" und
         „pairing mode - connect to ... from your Spotify app". Ein Lauf, in
         dem niemand antippte, meldete deshalb „? Weder waiting for login noch
         logged in as in der Ausgabe" und riet an Schluessel, Build und
         Tonserver herum — obwohl der Grund zwei Zeilen darueber im Klartext
         stand. Wer den Aufrufweg wechselt, muss die AUSWERTUNG mitwechseln.
    related: ["spak-schluessel-traegt-das-konto-nicht", "dienst-haelt-seinen-strom-dauerhaft", "selbstbeendender-prozess-statt-schwellenwert", "soloist-erste-messung"]

  - id: zweig-kann-unter-dir-wechseln
    kind: falle
    title: "Der ausgecheckte Zweig kann sich mitten in der Sitzung aendern — der Commit landet dann auf main"
    match: "commit auf main statt zweig|falscher branch|checkout -b hat nicht gehalten|zweig gewechselt|branch --show-current"
    sev: mid
    tags: ["git", "arbeitsweise", "parallele-sitzungen", "falle", 2026]
    source: >
      14.08.2026 an diesem Baum. Im reflog sichtbar als „checkout: moving from
      claude/plugin-skelett to main". Bis 22.08.2026 stand das nur im
      Gedaechtnis der Sitzungen und war aus dem Pack heraus ein toter Verweis.
    body: >
      An diesem Baum arbeiten MEHRERE SITZUNGEN GLEICHZEITIG. Ein
      `git checkout -b` am Anfang ist keine Zusage fuer spaeter: der Zweig
      wechselte zwischen dem Anlegen und dem Commit zurueck auf `main`, und die
      fertige Arbeit landete dort.

      WARUM DAS WEHTUT: Ein Zweig wird ausdruecklich verlangt, damit `main`
      unberuehrt bleibt, falls der Versuch scheitert. Ein Commit auf `main`
      hebelt genau diese Absicherung aus — und faellt nicht auf, weil
      `git commit` froehlich Vollzug meldet.

      VOR JEDEM `git add`/`git commit`: `git branch --show-current`.

      REPARATUR OHNE DATENVERLUST, wenn es doch passiert ist:

      1. `git log --oneline <basis>..main` — sicherstellen, dass NUR der eigene
         Commit darueberliegt (andere Sitzungen committen hier ebenfalls).
      2. `git branch -f <zweig> <mein-commit>`
      3. `git checkout <zweig>` — gleicher Baum, keine Dateiaenderung.
      4. `git branch -f main <basis>` — Zeiger verschieben, ohne den
         Arbeitsbaum anzufassen.

      KEIN `reset --hard`: das wirft fremde Aenderungen im Arbeitsbaum weg —
      bei zwei parallelen Sitzungen steht dort regelmaessig etwas offen.
    related: ["commit-ohne-main-wirkt-nirgends", "aufraeumen-nachmessen"]

  - id: soloist-pair-endet-nicht-und-meldet-sich-spaet
    kind: falle
    title: "Zweimal in derselben Stunde falsch gelesen - --pair beendet sich nicht, und die Anmeldung braucht eine Minute"
    match: "soloist pair beendet sich nicht|pairing complete aber prozess laeuft|logged in no nach neustart|anmeldung kommt verzoegert|niemand hat verbunden obwohl doch"
    sev: high
    tags: [spotify, soloist, koppeln, messen, falle, 2026]
    stand: "am Geraet gemessen 22.08.2026, beide Fehldeutungen im selben Lauf"
    body: |
      ══ ZWEI FEHLDEUTUNGEN, EINE URSACHE ═══════════════════════════════════

      Beim Wiederherstellen der Spotify-Anmeldung habe ich zweimal aus einem
      Blick geschlossen und zweimal das Falsche gemeldet. Beide Male war der
      Zustand in Ordnung und meine Lesart daneben.

      ══ 1. `--pair` BEENDET SICH NICHT NACH DEM SPEICHERN ═══════════════════

      Die Doku sagt: „clears the existing stored session, stores the new one,
      and exits". Am Geraet sieht es so aus:

          09:53:59  pairing mode - connect to "MixPiBox" from your Spotify app
          09:54:46  pairing complete, credentials stored
          09:54:46  logged in as 31nncq…
          09:54:51  pairing settled
          09:54:51  login failed: make sure --api-key is valid
          09:54:51  logged out
          09:55:40  logged in as 31nncq…
          10:03:59  shutting down          ← erst durch `timeout 600`

      DIE KOPPLUNG WAR NACH 47 SEKUNDEN DURCH. Der Prozess lief noch acht
      Minuten weiter. Wer, wie ich, „laeuft noch" als „wartet noch" liest,
      meldet dem Betreiber „niemand hat verbunden" — obwohl er verbunden hatte.

      DIE PRUEFUNG GEHOERT AN DIE AUSGABE, NICHT AN DEN PROZESS: `pairing
      complete, credentials stored` und `logged in as <konto>` sind die
      Wahrheit. Ob der Prozess noch lebt, sagt darueber nichts.

      Nebenbei sichtbar: dazwischen steht ein `login failed: make sure
      --api-key is valid` und ein `logged out`, und 49 Sekunden spaeter ist er
      doch angemeldet. Ein einzelner Fehlversuch in diesem Protokoll ist also
      KEIN Befund.

      ══ 2. DIE ANMELDUNG BRAUCHT NACH DEM DIENSTSTART EINE MINUTE ═══════════

      `soloist ctl status` meldete direkt nach `systemctl start` sauber
      `logged in: no`. Zwanzig Sekunden spaeter stand dort `yes`. Wer einmal
      hinsieht, diagnostiziert eine kaputte Anmeldung, wo eine langsame ist —
      und faengt an, Schluessel zu vergleichen und Konten zu zaehlen.

      Das ist [[einmal-hinsehen-ist-keine-messung]] in Reinform, und es ist mir
      trotz eigenem Eintrag passiert.

      ══ WAS DAGEGEN HILFT ══════════════════════════════════════════════════

      Beim Fragen nach `logged in` IMMER eine Schleife mit Abstand, nie einen
      einzelnen Aufruf. Und beim Koppeln auf die Ausgabezeile warten, nicht auf
      das Prozessende.

      ══ UND DIE ANTWORT AUF DIE EIGENTLICHE FRAGE ══════════════════════════

      Betreiber: „muss man das immer tuen? das ist ja keine zuverlaessige
      kinderbox". Gemessen:

          vorher                          logged in: yes
          systemctl restart soloist.service
          nach 15 s                       logged in: yes

      NEIN. Die Anmeldung uebersteht den Neustart. Was am 22.08. geschah, war
      ein einmaliger Schaden aus dem Koppel-Werkzeug — kein Betriebszustand.
    related: ["einmal-hinsehen-ist-keine-messung", "gegenprobe-statt-gruen-glauben", "wiki-zustand-hat-haltbarkeit"]

  - id: auskunft-aus-fremdem-zwischenspeicher
    kind: falle
    title: "Der Hinweis waere auf einer frischen Box unsichtbar gewesen - er las aus einem Zwischenspeicher, den eine ANDERE Seite fuellt"
    match: "zwischenspeicher leer beim ersten aufruf|feld bleibt leer ohne fehler|cache einer anderen seite gelesen|hinweis fehlt auf frischer box|netzlageGemerkt"
    sev: high
    tags: [entwurf, oberflaeche, falle, gegenlesen, 2026]
    stand: "beim Bauen bemerkt 22.08.2026, vor der Auslieferung behoben"
    body: |
      ══ DER FALL ═══════════════════════════════════════════════════════════

      Die Spotify-Karte sollte beim Koppeln sagen, in WELCHEM WLAN die Box
      haengt. Der erste Entwurf griff dafuer auf `netzlageGemerkt` zu — den
      Zwischenspeicher der Route `/api/netzwerk`.

      Der fuellt sich aber erst, wenn jemand DIE NETZWERKSEITE oeffnet. Auf
      einer frisch aufgesetzten Box, wo der Hinweis am noetigsten ist, waere er
      schlicht nicht dagewesen: kein Fehler, keine Meldung, kein leerer Wert
      mit Begruendung — nur ein Feld, das fehlt.

      ══ WARUM DAS SCHWER ZU FINDEN IST ═════════════════════════════════════

      BEIM ENTWICKELN IST DER SPEICHER IMMER WARM. Wer die Verwaltung
      durchklickt, hat die Netzwerkseite laengst geoeffnet — und sieht den
      Hinweis brav dastehen. Die Luecke zeigt sich nur bei jemandem, der
      GENAU DIESE eine Seite zuerst aufruft. Das ist der Betreiber einer neuen
      Box, also der einzige, auf den es ankommt.

      Dieselbe Ausfallart wie [[halbe-pruefung-meldet-wie-eine-ganze]] und
      [[werkzeug-meldet-ohne-empfaenger]]: nichts schlaegt fehl, es ist nur
      nichts da.

      ══ DIE REGEL ══════════════════════════════════════════════════════════

      **Eine Auskunft, die nur da ist, wenn vorher zufaellig jemand anders
      gefragt hat, ist keine Auskunft.**

      Wer einen Zwischenspeicher liest, muss ihn auch FUELLEN koennen. Behoben,
      indem die Berechnung zu `netzlageHolen()` wurde: sie prueft den Speicher,
      rechnet sonst selbst, und beide Aufrufer bekommen dasselbe.

      Die Probe dafuer ist billig: Dienst neu starten (Speicher kalt), dann
      GENAU die eine Seite aufrufen — nicht erst durchklicken.
    related: ["halbe-pruefung-meldet-wie-eine-ganze", "werkzeug-meldet-ohne-empfaenger", "einmal-hinsehen-ist-keine-messung"]

  - id: quelle-nach-faehigkeit-nicht-nach-motor
    kind: erkenntnis
    title: "Der Spielstand kommt jetzt lokal - und die Weiche fragt nach dem Draht, nicht nach der Maschine"
    match: "spielstand websocket statt webapi|soloist ws 5033 nutzen|pollen gegen spotify sparen|quelle nach faehigkeit|is_active statt namensvergleich|fortschrittsbalken bleibt stehen"
    sev: mid
    tags: [spotify, soloist, entwurf, e75, 2026]
    stand: "am Geraet bewiesen 22.08.2026"
    body: |
      ══ WAS UNGENUTZT DALAG ════════════════════════════════════════════════

      Soloist haelt auf dieser Box seit jeher einen WebSocket offen
      (`-w 127.0.0.1:5033`) und legt Adresse und Port in seinen Datenordner.
      NICHTS im Projekt hat ihn je benutzt. Stattdessen fragte
      `/api/spotify/laeuft` alle drei Sekunden `api.spotify.com/v1/me/player` —
      ueber das Internet, mit einem Token, das ablaufen kann.

      ══ DIE WEICHE (Betreiber, 22.08.2026) ═════════════════════════════════

      „normal ist webapi falls verfuegbar wechselt man in beiden auf websocket
      egal woher."

      Der erste Entwurf wollte nach der MASCHINE verzweigen (Soloist →
      WebSocket, librespot → Web-API). Nach der FAEHIGKEIT zu fragen ist
      richtiger, und der Beweis lag am selben Tag vor: waehrend des Koppelns
      war Soloists Draht ZEHN MINUTEN weg (`ws: not available`), obwohl
      „Soloist" als Engine eingestellt blieb. Eine Weiche nach Motor haette in
      dieser Zeit ins Leere gehorcht.

      ══ DREI STELLEN, AN DENEN ES STILL FALSCH GEWORDEN WAERE ══════════════

      1. DIE ALBUMKENNUNG. Die Web-API liefert `38EnlK9R5vIKyMnIXZ8Ipy`, der
         WebSocket `spotify:album:38EnlK9R5vIKyMnIXZ8Ipy`. Daran haengt das
         „Quick Add" fuer boxfremde Inhalte — mit Praefix sieht die Kachel
         richtig aus und startet nichts.

      2. DAS BILD. Die Web-API gibt `images[0]`, und das ist bei Spotify das
         GROESSTE. Der WebSocket gibt benannte Groessen (small, default, large,
         xlarge) in unbestimmter Reihenfolge. Wer das erste nimmt, bekommt den
         Daumennagel auf den Kinderschirm.

      3. DIE POSITION. Die Web-API wird GEFRAGT und antwortet mit dem Stand von
         jetzt. Der WebSocket MELDET nur bei Aenderung — dazwischen liegt sein
         `position_ms` fest. Roh uebernommen bliebe der Fortschrittsbalken
         stehen und spraenge dann. Soloist liefert deshalb `timestamp_ms` und
         `speed` mit; es muss hochgerechnet werden.

      DER ZEUGE, DER DAS FAENGT: dieselbe Wiedergabe aus BEIDEN Formen, Feld
      fuer Feld verglichen (`assert.deepEqual`). Zwei Quellen, die sich in
      einem Feld unterscheiden, faellt am Schirm niemandem auf.

      ══ WAS DAZUKOMMT, NICHT NUR WAS WEGFAELLT ═════════════════════════════

      `aufDieserBox` kam bisher aus einem NAMENSVERGLEICH (Geraetename gegen
      Boxname). Der WebSocket sagt `is_active` direkt.

      Und `logged_in` gibt es zum ersten Mal ueberhaupt — die Auskunft, die am
      22.08. zwei Tage lang gefehlt hat, als die Box stumm aus Spotify Connect
      verschwand ([[koppelwerkzeug-schrieb-in-den-dienstordner]]).

      ══ AM GERAET GEMESSEN, waehrend ein Kind hoerte ═══════════════════════

          /api/spotify/quelle -> {"quelle":"websocket","angemeldet":true}
          Position ueber 32 s: 130168 → 138204 → 146237 → 154275
          dabei angekommene Ereignisse: 8

      Also acht Meldungen fuer eine halbe Minute Wiedergabe — alles dazwischen
      hochgerechnet, kein einziger Aufruf an Spotifys Server.

      ══ DIE GRENZE ═════════════════════════════════════════════════════════

      Der Draht ist NICHT die Wahrheit ueber die Box, sondern ueber SOLOIST.
      Faellt er, wird der letzte Stand VERGESSEN und die Web-API uebernimmt —
      ihn weiterzumelden hiesse, einen Titel zu zeigen, der laengst vorbei ist.
    related: ["koppelwerkzeug-schrieb-in-den-dienstordner", "soloist-pair-endet-nicht-und-meldet-sich-spaet", "mit-echten-werten-rechnen"]

  - id: pegel-eine-stelle-soloist-steht-weit-offen
    kind: entscheidung
    title: "Soloist laeuft auf -i 100 und wird nie geregelt - der Pegel gehoert an EINE Stelle"
    match: "warum initial-volume 100|soloist lautstaerke regeln|zwei lautstaerkeregler|pegelkette box|set_volume soloist|spotify zu leise trotz regler"
    sev: mid
    tags: [audio, lautheit, soloist, spotify, entscheidung, 2026]
    stand: "Kette am Geraet gezaehlt 22.08.2026"
    body: |
      ══ DIE ZAHL, DIE OHNE EIN WORT DASTAND ════════════════════════════════

      `scripts/soloist/soloist-start.sh` startet mit `-i 100`. Das ist KEINE
      Nachlaessigkeit und kein Standardwert: Soloist wird bewusst weit
      aufgedreht und sein Regler DANN NICHT BENUTZT.

      Soloist HAT naemlich einen (`ctl volume`, WebSocket `set_volume`,
      Ereignis `volume_changed`). Geregelt wird trotzdem eine Stufe spaeter in
      PipeWire (`pactl set-sink-volume`) — dort sitzt auch das Daempfen beim
      Vorlesen und der Pegeldeckel fuer Kinderohren.

      ══ WARUM NICHT EINE STUFE FRUEHER ═════════════════════════════════════

      Am 22.08.2026 an der Box gezaehlt liegen zwischen Soloist und dem
      Lautsprecher OHNEHIN vier Stufen:

          entzerrer  →  klangwerk  →  alsa_output  →  ALSA Master

      Eine fuenfte, die woanders bedient wird, waere ein zweiter Knopf fuer
      dieselbe Sache — und zwar der, den man vergisst, wenn es zu leise ist.
      Wer spaeter „Spotify ist zu leise" sucht, soll GENAU EINE Stelle finden.

      Das ist dieselbe Trennung wie in
      [[lautheit-zwei-orte-kompressor-und-replaygain]]: Soloist normalisiert
      nicht und regelt nicht, er liefert. Was mit dem Pegel geschieht,
      entscheidet der Tonserver.

      ══ OFFEN UND NICHT GEMESSEN: DER REGLER IN DER SPOTIFY-APP ════════════

      Was passiert, wenn jemand IN DER SPOTIFY-APP am Regler zieht? Connect
      schickt das ans Geraet, Soloist ginge also von 100 herunter — die
      Oberflaeche der Box liest aber den PipeWire-Pegel und saehe davon
      nichts. Zwei Regler, die sich nicht kennen: der eine steht auf 30, der
      andere zeigt 80. Beim naechsten Dienststart stuende wieder 100, ohne
      dass jemand etwas geaendert haette.

      NICHT AM GERAET GEPRUEFT — als Moeglichkeit hergeleitet, nicht gesehen.
      Wer es misst: `soloist ctl status` vor und nach dem Schieben in der App.

      Seit [[quelle-nach-faehigkeit-nicht-nach-motor]] horcht der Server am
      WebSocket und koennte `volume_changed` mitlesen — der Weg dahin ist also
      offen, falls sich das je als Aerger zeigt.
    related: ["lautheit-zwei-orte-kompressor-und-replaygain", "pegelsprung-quellenwechsel-noch-ungemessen", "quelle-nach-faehigkeit-nicht-nach-motor"]

  - id: koppelkarte-nennt-das-netz-statt-zu-mahnen
    kind: entscheidung
    title: "Die Koppel-Anleitung nennt die SSID, in der die Box gerade haengt - eine Mahnung ist keine Auskunft"
    match: "gleiches wlan wie die box|ssid auf der spotify karte|handy findet box nicht spotify connect|getrennte baender 2G 5G router|netz feld api spotify maschine"
    sev: mid
    tags: [spotify, koppeln, wlan, oberflaeche, mdns, 2026]
    stand: "am Geraet gemessen 22.08.2026, Bild nicht verifiziert"
    body: |
      ══ WAS DIE KARTE VORHER SAGTE ═════════════════════════════════════════

      „Spotify-App auf dem Handy oeffnen (gleiches WLAN wie die Box)". Eine
      Mahnung, die dem Lesenden nichts gibt, was er NACHSEHEN koennte. Er weiss
      danach nicht, in welchem Netz die Box haengt — nur, dass es das gleiche
      sein soll.

      Die Box weiss es aber. Betreiber, 22.08.2026: „wir muessen noch den
      hinweis mit aufnehmen auf der box das man im gleichen wlan sein sollte
      auch ggf gleiche frequenz".

      ══ WAS SIE JETZT SAGT, UND WOHER ══════════════════════════════════════

      `/api/spotify/maschine` traegt ein Feld `netz`; am Geraet gemessen:

          netz: {"ssid": "ganzschnellimnetz2_2G", "band": "2,4 GHz"}

      Die Karte schreibt daraus „Handy ins gleiche WLAN wie die Box:
      **ganzschnellimnetz2_2G** · 2,4 GHz". `null` (Kabel, kein `iw`, kein
      Name) faellt still auf den alten Satz zurueck — ein Zubrot darf die
      Karte nie kippen.

      ══ DER NAME IST DIE AUSKUNFT, NICHT DAS BAND ══════════════════════════

      Wer „verbinde dich mit ganzschnellimnetz2_2G" liest, muss NICHTS
      verstehen — er sucht diesen Namen in seiner WLAN-Liste. Das Band steht
      nur daneben, weil es erklaert, WARUM es zwei Netze mit aehnlichem Namen
      gibt. An dem `_2G` sieht man, dass dieser Router die Baender in GETRENNTE
      Netze legt, und ein Handy im 5-GHz-Netz findet die Box dann unter
      Umstaenden gar nicht: Spotify Connect sucht ueber mDNS, und das ist
      Multicast im lokalen Netz.

      ══ EHRLICH DAZU: ZWEI DINGE SIND NICHT BELEGT ═════════════════════════

      1. DASS ES JE DARAN GESCHEITERT WAERE. Das Bandproblem ist eine
         begruendete Moeglichkeit, kein gemessener Fall. Die Koppelprobleme
         vom 22.08. sind woanders erklaert
         ([[koppelwerkzeug-schrieb-in-den-dienstordner]],
         [[soloist-pair-endet-nicht-und-meldet-sich-spaet]]). Ein Kommentar in
         `spotify-maschine.ts` behauptete es kurz — am 22.08. berichtigt.

      2. DAS BILD. Der Hinweis erscheint nur, SOLANGE DIE ANMELDUNG FEHLT. Ihn
         zu fotografieren hiesse, die Kopplung mutwillig kaputtzumachen —
         deshalb steht hier kein Schirmbild, sondern die Antwort der Route.

      Die Falle, die beim Bauen dieses Hinweises fast zugeschnappt waere,
      steht getrennt: [[auskunft-aus-fremdem-zwischenspeicher]].
    related: ["auskunft-aus-fremdem-zwischenspeicher", "koppelwerkzeug-schrieb-in-den-dienstordner", "soloist-pair-endet-nicht-und-meldet-sich-spaet"]

  - id: abschalten-trifft-jede-ebene-der-buendelung
    kind: erkenntnis
    title: "Der Anbieter-Schalter (E76) - eine Tuer zum Verweigern, und die Markierung muss jede Buendelungs-Ebene erfassen"
    match: "anbieter abschalten|spotify aus schalter|kachel ausgrauen quelle|dienst deaktivieren box|403 anbieterAus|regal kachel grau|quelleAus"
    sev: mid
    tags: [entwurf, oberflaeche, e76, anbieter, 2026]
    stand: "am Geraet bewiesen 22.08.2026 (ARD ab- und wieder angeschaltet, waehrend ein Hoerspiel lief)"
    body: |
      ══ WAS GEBAUT WURDE ═══════════════════════════════════════════════════

      Je Streaming-Anbieter (spotify/jellyfin/ard) ein Schalter `<gruppe>.aktiv`
      in der Konfiguration. Modul `mixpi-anbieter.ts`, drei Verbraucher:

        1. `anbieterLage` - eigener Stand `abgeschaltet` (NICHT `aus`: das
           heisst „nie eingerichtet", und die Unterscheidung „da ist nichts"
           gegen „da ist alles, es soll nur gerade nicht" ginge verloren).
        2. `/api/werke` - Werke abgeschalteter Dienste tragen `quelleAus`,
           nach dem Vorbild von `fehlt`: gekennzeichnet, nie versteckt.
        3. Der /player-PROXY verweigert Startbefehle mit 403 und dem Anker
           `anbieterAus` - DIESELBE Tuer und dasselbe Formular wie die
           Kinderzeit. Eine Tuer, nicht fuenf Zweige im Abspieldienst; Stop,
           Zustand und Lautstaerke gehen weiter durch.

      Drei Regeln: fehlender Schalter = AN (Bestandsboxen!), nur ein echtes
      `false` schaltet ab (kein Biegen von 0/'false'), kaputte Konfiguration
      verweigert NICHTS.

      ══ DIE LEHRE, DIE ERST DAS GERAET ZEIGTE ══════════════════════════════

      Am Schirm blieb die Schaerpe zunaechst AUS, obwohl /api/werke sauber
      markierte. Grund: die sechs ARD-Werke steckten in REGALEN (fuenf im
      Maus-Regal, eins im WDR-Regal), und Regal-Kacheln baut eine ANDERE
      Funktion, die von `quelleAus` nichts wusste.

      **Eine Markierung am Einzelding erfasst nicht seine Buendelungen.** Das
      ist dieselbe Regel dreimal: beim VERSCHMELZEN (Werk aus zwei Diensten),
      beim REGAL (Werke eines Interpreten) und ueberall, wo kuenftig
      aggregiert wird - die Markierung bleibt nur, wenn ALLE Teile sie tragen.
      Das WDR-Regal (Spotify + ARD gemischt) blieb deshalb zu Recht bunt,
      das Maus-Regal (nur ARD) wurde grau.

      Wer eine Kennzeichnung einfuehrt, suche also alle Stellen, die Dinge
      BUENDELN - nicht nur die, die sie zeigen.

      ══ DIE NAHT WAR NICHT GEFAHRLOS MESSBAR - ALSO ZOG SIE IN EINEN ZEUGEN ═

      Die Proxy-Verweigerung am Geraet zu testen hiesse: schlaegt sie fehl,
      erreicht der Testbefehl den Abspieldienst und reisst dem hoerenden Kind
      die Wiedergabe ab. Deshalb ein Integrationszeuge (supertest, ohne
      Player dahinter): 403 unterscheidet sich sauber vom 502 der
      Durchleitung. Gegenprobe: Pruefung im Proxy lahmgelegt -> 3 rot.

      Danach war der Geraetetest gefahrlos - und der 403 kam, waehrend das
      Hoerspiel weiterlief.

      ══ NEBENFUND AM WERKZEUG ══════════════════════════════════════════════

      `verwaltungsseite-abbilden.mjs --lies` stringifizierte async-Ausdruecke
      zu woertlich „[object Promise]": `String(zusage)` VOR dem Aufloesen.
      `awaitPromise` wickelt nur die aeussere Zusage ab - erst `await`, dann
      `String()`.
    related: ["gegenprobe-statt-gruen-glauben", "mit-echten-werten-rechnen", "halbe-pruefung-meldet-wie-eine-ganze"]

  - id: plugins-melden-sich-an-statt-gekannt-zu-werden
    kind: erkenntnis
    title: "E77: Sektion, Icon, Aktionen, freie Route - das Plugin sagt, wohin es gehoert, und drei Funde von der Naht"
    match: "plugin sektion anmelden|plugin icon mitbringen|plugin eigene route|steckleiste verwaltung|plugin aktionen knopf|mixpi-plugin-abschnitt|pruefstand plugin"
    sev: mid
    tags: [plugins, entwurf, e77, verwaltung, 2026]
    stand: "am Geraet bewiesen 22.08.2026 (mixpi-ardsounds in der ARD-Karte, alle Flaechen gemessen)"
    body: |
      ══ DER VERTRAG, ERWEITERT UM VIER STUECKE ═════════════════════════════

      Manifest: `sektion` (geschlossene Liste SEKTIONEN — das PLUGIN sagt „ich
      gehoere in streaming/ardsounds", keine Karte muss es kennen), `icon`
      (Datei im Plugin-Ordner, nur svg/png, ausgeliefert unter
      /api/plugins/<k>/icon), `aktionen` (deklarative Knoepfe -> Methode
      `aktion(kennung)`, nur Angemeldetes erreicht den Worker — der Wirt
      prueft gegen das Manifest). Dazu `http(anfrage)` als freie Flaeche
      unter /api/plugins/<k>/http/… — nur GET/POST, nur JSON, Status 2xx/4xx,
      Deckel 256 KB, alles GEPRUEFT IM WIRT (der Worker teilt den Speicher
      mit dem Plugin).

      Verwaltungsseite: EINE generische Steckleiste (mixpi-plugin-abschnitt),
      die je Sektion fragt „wer hat sich gemeldet?" — statt je Plugin eine
      fest verdrahtete Stelle (so wie es der Klangwerk-Abschnitt noch ist).

      Testumgebung: tools/mixpi-plugin-pruefstand.mjs prueft ein
      Plugin-Verzeichnis OHNE Box und OHNE Server — und importiert dafuer
      manifestPruefen, statt die Regeln nachzubauen: ein Pruefstand mit
      eigener Kopie segnete Manifeste ab, die die Box abweist.

      ══ DREI FUNDE VON DER NAHT — alle erst der Integrationszeuge ══════════

      1. DOTFILES: res.sendFile verweigert in der Vorgabe JEDEN Pfad mit
         Punkt-Segment (Schutz gegen .env/.htaccess). Die Plugins liegen
         unter /home/dietpi/.mupibox/… — das Icon kam am Geraet als 404
         „Not Found", waehrend die Datei lag. Der lokale Test war gruen,
         weil der Temp-Pfad punktfrei war. DER ZEUGE BILDET JETZT DIE
         GERAETEUMGEBUNG NACH (Ordner mit Punkt-Segment); Behebung
         `dotfiles: 'allow'` — vertretbar, weil der Pfad doppelt geriegelt
         und die Endung begrenzt ist.

      2. DER KOMMENTAR LOG: „Status 200-499, 3xx faellt mit heraus" stand
         ueber einem Code, der 302 durchliess. Der Zeuge nahm den Kommentar
         beim Wort und ueberfuehrte den Code — eine 302 aus einem Plugin
         waere ein stiller Weg nach draussen. Jetzt 2xx und 4xx.

      3. DER WORKER HAELT DEN TESTPROZESS: ohne after() mit allesBeenden()
         hing der Spec fuenf Minuten im Timeout, obwohl alle Zeugen durch
         waren. Das bestehende Muster (plugin-einstellungen.integration)
         hatte es vorgemacht — nachsehen lohnt vor dem Neuschreiben.

      ══ EHRLICHE GRENZE ════════════════════════════════════════════════════

      mixpi-ardsounds ist der PILOT des Vertrags, nicht die ARD-Migration.
      Kacheln, Folgenlisten, Weiterhoeren, Vorspann wohnen weiter im Kern;
      die Kartierung vom 22.08. nennt die vier Strukturluecken, die vor der
      echten Migration zu schliessen sind (Folgenlisten-Aufloesung,
      Kachel-Integration, Browse-Routen, Weiterhoeren-Naht).
    related: ["abschalten-trifft-jede-ebene-der-buendelung", "gegenprobe-statt-gruen-glauben", "auskunft-aus-fremdem-zwischenspeicher"]

  - id: inhalt-im-vertrag-und-ard-delegiert
    kind: erkenntnis
    title: "E78: inhalt() schliesst die Folgenlisten-Luecke - und der ARD-Kern delegiert an das Plugin, ohne Rueckfall"
    match: "plugin folgenliste inhalt|sendung als warteschlange plugin|ard migration plugin|pluginInhalt|zweite wahrheit migration|folgen regeln dupliziert"
    sev: mid
    tags: [plugins, ard, entwurf, e78, migration, 2026]
    stand: "am Geraet bewiesen 22.08.2026: Kachel -> Plugin -> Wirt -> mpv, Ton lief und stoppte; 60 Folgen ueber pluginInhalt"
    body: |
      ══ DIE LUECKE ═════════════════════════════════════════════════════════

      `aufloesen` liefert genau EINEN Fund. Eine Sendung, ein Album, ein Feed
      sind LISTEN, und die Oberflaeche spielt sie als Warteschlange. Die
      Kartierung nannte das als erste der vier Strukturluecken der
      Anbieter-Migration. Neu im Vertrag:

          inhalt(rest, kontext) -> { titel, kuenstler?, folgen[], vollstaendig }

      Je Folge: kennung (dauerhaft, NIE die Tonadresse), name, quelle. Der
      Reihenfolge-Wunsch reist hinter `#` im rest (wie beim Podcast die
      Folgennummer): WER ihn wuenscht, entscheidet der Kern (er kennt den
      data.json-Eintrag), WAS er bedeutet, das Plugin.

      DER WIRT PRUEFT JEDE FOLGE wie einen Fund (dieselbe fundPruefen-Kette,
      wiederverwendet): die 40. Folge mit file:///etc/shadow waere dieselbe
      Luecke wie beim Einzelfund, nur besser versteckt. Kaputte Folgen fallen
      heraus, die Liste bleibt; ein Deckel (500) haelt Kataloge draussen.
      Leere Liste ist ein ERGEBNIS (alles abgelaufen), kein Fehler.

      ══ DIE DELEGATION - OHNE RUECKFALL ════════════════════════════════════

      Der ard-Zweig von /api/werke/:s/inhalt ruft jetzt
      pluginInhalt('mixpi-ardsounds', kennung#reihe). Beim Kern bleibt, was
      KERN-Infrastruktur ist: der Eintrag (Reihenfolge-Wunsch), der
      Weiterhoeren-Speicher, der Vorspann-Sprung, der Befehlsbau. KEIN
      Rueckfall auf den alten Weg: zwei Wege fuer dieselbe Sache laufen genau
      dann auseinander, wenn es darauf ankommt.

      AUCH DER ZWEITE AUFRUFER ZOG UM: ardFolgeAnNummer (Weiterhoeren-
      Nachholen nach Serverneustart) holte die Liste noch ueber den alten
      Weg. Die Liste, gegen die eine Warteschlangennummer aufgeloest wird,
      muss ZEICHEN FUER ZEICHEN dieselbe sein wie die, aus der die
      Warteschlange gebaut wurde - sonst zeigt die gemerkte Stelle still auf
      die falsche Folge, exakt an dem Tag, an dem eine der Ordnungsregeln
      sich aendert.

      ══ DIE BEWUSSTE ZWEITE WAHRHEIT ═══════════════════════════════════════

      Die Folgen-Regeln (Ton-Nachsicht, Verweildauer, Sperrliste statt
      Erlaubnisliste, Ordnung, Bild-Platzhalter) sind aus ard.ts ins Plugin
      UEBERNOMMEN - Plugins sind eigenstaendig, keine Imports aus src/. Das
      ist waehrend der Migration eine zweite Wahrheit, WISSENTLICH und mit
      Verweis im Code: sie endet, wenn der Kern-Zweig faellt. Suche,
      Sammlungen, Regale und Vorspann-Messung nutzen ard.ts weiter (Browse
      ist der naechste Block).
    related: ["plugins-melden-sich-an-statt-gekannt-zu-werden", "abschalten-trifft-jede-ebene-der-buendelung", "gegenprobe-statt-gruen-glauben"]
  - id: anleitung-hinkt-dem-vertrag-hinterher
    kind: falle
    title: "Der Vertrag wuchs, die Anleitung nicht - drei Stellen in plugins/README.md, die E77/E78 nicht mitbekamen"
    match: "plugin anleitung veraltet|inhalt methode nicht dokumentiert|ApiRouteProvider gibt es nicht|keine allgemeine plugin seite|einstellungen per curl|plugins README stand"
    sev: mid
    tags: [plugins, doku, vertrag, e77, e78, arbeitsweise, 2026]
    stand: "am 22.08.2026 nachgezaehlt und berichtigt"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      E77 und E78 haben den Plugin-Vertrag um vier bzw. eine Flaeche
      erweitert. `plugins/README.md` - die Anleitung, die ein fremder
      Plugin-Autor liest - kam nur bei E77 mit. Der stuendliche Doku-Lauf am
      22.08.2026 fand drei Stellen:

        1. `inhalt(rest, kontext)` STAND NIRGENDS. Nicht in der
           Vertrags-Tabelle, nicht als Abschnitt. Wer die Anleitung liest,
           haette die Methode nur gefunden, indem er `plugin-vertrag.ts`
           liest - also genau das, was die Anleitung ersparen soll. Dass
           `inhalt` im Text VORKAM (in `{ status?, inhalt }` der
           http-Antwort), machte es schlimmer: eine Textsuche traf und
           lieferte die falsche Sache.

        2. „`ApiRouteProvider` (eigene Endpunkte) ... gibt es nicht" stand
           in derselben Datei, die 300 Zeilen weiter oben `http()` unter
           `/api/plugins/<k>/http/…` erklaert. Ein WIDERSPRUCH IN EINER
           DATEI - und der Leser glaubt eher der kurzen, harten Fassung am
           Ende, weil sie wie eine Zusammenfassung aussieht.

        3. „noch keine allgemeine Seite im Eltern-Bereich, Einstellungen
           gehen bis dahin per `curl`" - die Seite gibt es seit dem
           14.08.2026 (`/plugins` und `/plugins/<kennung>`, mit An/Aus und
           den `felder`-Eingaben). Acht Tage falsch, ohne dass es jemandem
           auffiel.

      Auch `--inhalt` im Pruefstand war undokumentiert.

      ══ WARUM ES DURCHRUTSCHT ══════════════════════════════════════════════

      Das Wiki wurde bei JEDEM der drei Commits nachgezogen (E77, E78,
      E78-Nachtrag) - die Gewohnheit sitzt. `plugins/README.md` ist aber
      keine Wiki-Datei, sondern eine gewoehnliche Datei im Baum, und fuer
      sie gibt es keine Gewohnheit. Wer „ist das dokumentiert?" mit „steht
      es im Wiki?" beantwortet, bekommt JA und hat trotzdem einen
      Plugin-Autor vor der Wand stehen.

      ZWEI LESER, ZWEI ORTE. Das Wiki erzaehlt einer spaeteren Sitzung, WAS
      entschieden wurde und WARUM; die Anleitung sagt einem fremden Autor,
      WIE er es benutzt. Ein Eintrag im einen ersetzt den anderen nie.

      ══ DIE REGEL ══════════════════════════════════════════════════════════

      Waechst der VERTRAG (`plugin-vertrag.ts`: neue Methode, neues
      Manifest-Feld, neue Route), dann gehoert im selben Commit angefasst:

          plugins/README.md   Tabelle + Abschnitt + Pruefstand-Aufruf
          llmwiki/pack.yaml   die Entscheidung und ihr Warum

      Und die Gegenprobe dazu kostet nichts:
      `grep -n "<neuer-name>" plugins/README.md` - kein Treffer heisst
      nicht dokumentiert, und EIN Treffer heisst noch gar nichts (siehe
      Fall 1 oben, wo das Wort im falschen Zusammenhang stand).

      ══ NACHTRAG 22.08.2026: DIE REGEL HIELT EINEN COMMIT ══════════════════

      Die Regel oben wurde in 14a7904f aufgeschrieben. E80 - der NAECHSTE
      Commit, der den Vertrag anfasste - hat sie schon wieder gerissen:
      `konfig` (Manifest-Feld) und `kontext.konfig` (Kontextfeld) kamen in
      `plugin-vertrag.ts` und `plugin-wirt.ts` an, `plugins/README.md` blieb
      unberuehrt. Die Gegenprobe aus der Regel zeigte es in einer Zeile:

          grep -ci konfig plugins/README.md   ->  1   (ein Zufallstreffer)
          grep -ci konfig plugin-vertrag.ts   -> 18

      DIE LEHRE IST NICHT „nochmal aufschreiben". Eine Regel, die nur in
      einem Wiki-Eintrag steht, wird beim naechsten Mal wieder vergessen -
      der, der E80 baute, hatte den Eintrag nicht offen. Was fehlte, war eine
      Naht, die von selbst zieht - deshalb gibt es sie jetzt:

          node tools/mixpi-vertrag-doku-abgleich.mjs

      Es liest die Namen, die ein FREMDENTWICKLER hinschreiben muss
      (Manifest-Felder, Kontext-Felder aus `plugin-laufwerk.ts`, die
      geschlossenen Vokabulare) und sucht sie in `plugins/README.md`.
      Exit 1, wenn einer fehlt. Beim ERSTEN Lauf fand es sofort einen
      zweiten, aelteren Fall, den niemand auf der Liste hatte:
      `kontext.datenOrdner` war nie dokumentiert.

      DIE TREFFERZAHL STEHT MIT DABEI, und das ist der Kern: ein Treffer
      heisst „das Wort kommt vor", nicht „es ist erklaert" - genau daran
      ging E80 vorbei, `konfig` stand EINMAL da, im falschen Zusammenhang.
      Das Werkzeug warnt bei genau einem Treffer, statt ihn durchzuwinken;
      das Urteil bleibt beim Menschen. Was es NICHT verlangt, steht als
      `NICHT_VERLANGT` im Kopf, mit Begruendung: interne Pruefnamen ruft
      kein Fremder, sie in die Anleitung zu zwingen hiesse, das Werkzeug
      laut zu machen, wo nichts kaputt ist.

      Gegengeprobt (ein erfundenes Manifest-Feld in `plugin-vertrag.ts`,
      Werkzeug rot, Datei zurueckgespielt) - ein gruener Lauf, den niemand
      kaputtgemacht hat, beweist nichts.

      Nachgezogen im Doku-Lauf vom 22.08.2026: Kontext-Tabelle,
      eigener Abschnitt „Kern-Konfiguration einsehen - `konfig` (E80)",
      zwei Zeilen in der Abweis-Tabelle (beide Meldungen woertlich
      gegengelesen), und der Stand-Block oben, der E79/E81 verschwieg -
      dass die BEIDEN echten Anbieter der Box (ardsounds, jellyfin)
      inzwischen Plugins sind, stand nirgends in der Anleitung.
    related: ["inhalt-im-vertrag-und-ard-delegiert", "plugins-melden-sich-an-statt-gekannt-zu-werden", "artefakte-veralten-still-vor-dem-kartenlauf", "wiki-zustand-hat-haltbarkeit", "kern-konfig-reist-in-den-kontext"]

  - id: browse-ist-pluginsache-und-die-einzelabfrage-log
    kind: erkenntnis
    title: "E79: Regale, Sammlungen und Suche wohnen im Plugin - und die Migration deckte einen still kaputten Bestand auf"
    match: "ard sammlung leer|editorialCollection document null|browse routen plugin|ard kategorien plugin|schonDa wer rechnet|profil alter adapter"
    sev: mid
    tags: [plugins, ard, migration, e79, 2026]
    stand: "am Geraet bewiesen 22.08.2026 (Adapter 323 Sammlungen, Regale 18, Suche 8 Treffer, alte Routen -> SPA-Fallback)"
    body: |
      ══ DER SCHNITT ════════════════════════════════════════════════════════

      Das Stoebern der Verwaltung (Regale, Sammlungen, Sendungssuche) laeuft
      ueber die E77-http-Flaeche des Plugins. Die Wissens-Grenzen:

        PLUGIN   Altersfenster im Titel, Kinderbezug, drei Raenge, Teaser,
                 der data.json-VORSCHLAG (die eine Stelle dieser Form)
        KERN     Profil -> Alter (ein duenner Adapter: WER sieben ist, weiss
                 der Kern, WAS sieben bedeutet, das Plugin) und die
                 uebergreifende Suche (schonDa + Schluessel = Bibliothek)
        SEITE    rechnet `schonDa` aus den EIGENEN Eintraegen - sie hat die
                 Bibliothek ohnehin; ein Plugin, das die Medienliste liest,
                 waere ein Plugin mit Kern-Wissen

      Fuenf Kernrouten sind ersatzlos gefallen (/kategorien, /kategorie/:id,
      /sammlung/:id, /suche, /kinder - die letzten beiden hatten laengst
      keinen Aufrufer). Wer sie ruft, bekommt den SPA-Fallback: HTML, kein
      JSON - beim Nachmessen NICHT vom Statuscode taeuschen lassen, 200 heisst
      hier „Einzelseiten-Anwendung", nicht „Route lebt".

      ══ DER BESTAND WAR STILL KAPUTT - DIE MIGRATION DECKTE ES AUF ═════════

      `editorialCollection(id:…)` liefert `document: null` - fuer DIESELBEN
      Sammlungen, fuer die die LISTE `editorialCollections` ein volles
      document mit teasers traegt (am 22.08.2026 gemessen: 33 von 40 mit
      Inhalt ueber die Liste, ALLE null ueber die Einzelabfrage). Der alte
      Kern-Weg nutzte die Einzelabfrage: Titel ja, Sendungen leer, kein
      Fehler, niemand merkte es. Das Plugin holt die URNs aus der
      Listen-Abfrage (im 15-min-Speicher ohnehin da) - „100 Jahre Radio"
      liefert wieder 34 Sendungen.

      Die Lehre ist die alte, in neuem Gewand: eine Fremd-API, die dasselbe
      Ding auf zwei Wegen liefert, darf auf einem davon still verarmen. Wer
      den Weg wechselt (oder migriert), MISST beide - die Migration war hier
      der Zufallsfund, der den Schaden aufdeckte.

      ══ NEBENFUND AM PRUEFSTAND ════════════════════════════════════════════

      `--http "sammlungen?alter=5"` steckte den Query-Teil in den PFAD - das
      Plugin meldete voellig zu Recht 404, und der Pruefstand sah aus, als
      haette das Plugin einen Fehler. Ein Pruefwerkzeug muss die Anfrage
      GENAU SO bauen wie die echte Route (Query getrennt in `abfrage`).
    related: ["inhalt-im-vertrag-und-ard-delegiert", "plugins-melden-sich-an-statt-gekannt-zu-werden", "einmal-hinsehen-ist-keine-messung"]

  - id: loeschskript-riss-bis-zur-naechsten-klammer
    kind: falle
    title: "Ein Aufraeum-Skript nahm 220 Zeilen zu viel - die Bis-zur-naechsten-Klammer-Heuristik kennt keine Bloecke"
    match: "loeschskript zu viel geloescht|bis zur naechsten klammer|funktion entfernen skript|aufraeumen riss route mit|tote exporte entfernen"
    sev: high
    tags: [werkzeug, aufraeumen, falle, 2026]
    stand: "22.08.2026 beim ard.ts-Aufraeumen passiert, vor dem Commit bemerkt und praezise wiederhergestellt"
    body: |
      ══ WAS PASSIERT IST ═══════════════════════════════════════════════════

      Beim Stutzen von server.ts sollte die tote Konstante
      `ARD_REGAL_FUER_KINDER` fallen. Das Python-Skript suchte ab der Fundzeile
      das naechste `}` AM ZEILENANFANG — bei einer Konstanten (kein Block!)
      lag das naechste solche `}` 220 Zeilen tiefer, hinter der kompletten
      /api/werke/:schluessel/alben-Route. Alles dazwischen fiel mit.

      BEMERKT hat es erst tsc — an Folgefehlern (INTERPRET_FRIST_MS,
      webApiHolen fehlten), die mit dem Geloeschten nur NACHBARSCHAFT teilten.
      Ein Skript, das eine Konstante loeschen soll und eine Route mitnimmt,
      macht keinen Fehler, den man an der Stelle des Auftrags sucht.

      ══ DIE REGELN ═════════════════════════════════════════════════════════

      1. „Bis zum naechsten `}`" gilt NUR fuer Funktions-/Block-Definitionen —
         und auch dort nur, wenn die Klammer auf Spalte 0 steht. Fuer
         Konstanten, let-Zeilen und Kommentare: exakte Zeilen loeschen, nie
         Heuristik.
      2. NACH JEDEM SKRIPT-EINGRIFF IN GROSSE DATEIEN: `git diff` auf
         GELOESCHTE Definitionen durchsehen (`grep '^-' | grep -E 'function|
         const|app\.'`), BEVOR der naechste Schritt kommt. tsc faengt nur, was
         Typen beruehrt — eine mitgeloeschte Route ohne weitere Verbraucher
         waere still verschwunden.
      3. Die Wiederherstellung lief PRAEZISE aus `git show HEAD:` mit Ankern
         (nicht `git checkout` — das haette die GEWOLLTEN Aenderungen der
         Datei mitgerissen; dieselbe Richtung wie die Wegwerfziel-Regel bei
         Gegenproben).

      ══ DER RAHMEN: WOHIN DIE ZEUGEN BEIM UMZUG GEHOEREN ═══════════════════

      Anlass war das ard.ts-Aufraeumen nach E79 (980 -> 75 Zeilen). Die
      REGELN zogen ins Plugin um — und ihre ZEUGEN MIT ihnen: 12 neue in
      plugins/mixpi-ardsounds/index.spec.mjs, gegen gefaelschtes GraphQL ueber
      die echten Flaechen (inhalt/http). Eine Regel ohne Zeugen waere keine
      Migration, sondern ein Verlust. Nebenfund: der 15-min-Modulspeicher des
      Plugins ueberlebt zwischen Zeugen — Testnaht `zwischenspeicherLeeren()`.
    related: ["gegenprobe-statt-gruen-glauben", "halbe-pruefung-meldet-wie-eine-ganze", "einmal-hinsehen-ist-keine-messung"]

  - id: kern-konfig-reist-in-den-kontext
    kind: erkenntnis
    title: "E80: Anbieter-Zugaenge bleiben in der Kern-Konfiguration - das Plugin meldet Gruppen an, der Wirt reicht sie"
    match: "plugin zugangsdaten woher|jellyfin apikey plugin|kern konfig kontext|konfig gruppen manifest|plugin neustart nach konfigaenderung"
    sev: mid
    tags: [plugins, entwurf, e80, jellyfin, 2026]
    stand: "lokal bewiesen 22.08.2026 (12 Integrationszeugen samt Neustart-Weg)"
    body: |
      ══ DIE FRAGE ══════════════════════════════════════════════════════════

      Ein Jellyfin-Plugin braucht Server und API-Schluessel. Die wohnen in
      mupiboxconfig.json und werden von der Streaming-Karte verwaltet — samt
      E76-Schalter. Eigene Plugin-Felder hiessen: dieselben Werte an zwei
      Orten, ein Migrationsskript je Bestandsbox, zwei Verwaltungsflaechen
      fuer eine Sache.

      ══ DIE ANTWORT: DAS STROEME-MUSTER ════════════════════════════════════

      Manifest: `konfig: ["jellyfin"]` — das Plugin MELDET AN, der Wirt
      reicht die Gruppe eingefroren in `kontext.konfig`. Eine GESCHLOSSENE
      Liste (KONFIG_GRUPPEN = jellyfin, ard): `spotify` steht bewusst nicht
      darin — dort liegen refreshToken und clientSecret, und kein Plugin
      braucht sie, um zu spielen (Abspielen laeuft ueber den Kern). Was ein
      Plugin einsehen darf, ist eine Kern-Entscheidung je Gruppe.

      NACH JEDER KONFIG-AENDERUNG startet der Wirt genau die betroffenen
      Plugins neu (Haken in konfigAendernSicher -> kernKonfigAktualisieren):
      der Kontext ist eingefroren, und ohne Neustart saehe ein Plugin bis zum
      Boxneustart die alten Zugangsdaten — der Fehler traete erst beim
      Serverumzug auf und saehe aus wie ein kaputter Server.

      ══ DER WETTLAUF, DEN DER ZEUGE FING ═══════════════════════════════════

      Der erste Wurf feuerte terminate() und kehrte zurueck — ein
      `warteBereit` direkt danach sah den STERBENDEN Worker noch als
      „bereit", und die Antwort kam vom alten Kontext. Die Behebung ist das
      vorhandene Muster aus einstellungenSetzen, Zeile fuer Zeile: Zustand
      VOR dem terminate umstellen (sonst startet der exit-Behandler doppelt),
      terminate ABWARTEN, versuche zuruecksetzen, selbst neu starten. Wer
      einen Neustart-Weg baut, schaut erst, ob das Haus schon einen hat.
    related: ["plugins-melden-sich-an-statt-gekannt-zu-werden", "inhalt-im-vertrag-und-ard-delegiert", "auskunft-aus-fremdem-zwischenspeicher"]

  - id: jellyfin-zieht-ins-plugin
    title: "Jellyfin wohnt im Plugin - der Kern baut nur noch Befehle aus mitgereisten Stroemen"
    kind: loesung
    match: "mixpi-jellyfin|jfBefehle|strom.*Direct Play|titelEinesWerks.*jellyfin|jellyfinArt"
    tags: [jellyfin, plugins, e80, migration, 2026]
    source: "am 2026-08-22 an der Box gemessen: Suche (lumpenpack -> 2 Alben nur ueber den Interpreten-Weg), /inhalt (12 Titel, static=true im Befehl), art (MusicAlbum/Audio)"
    body: |
      Seit 22.08.2026 liegen die Jellyfin-Regeln in plugins/mixpi-jellyfin
      (Vertrag E77/E80): Titelliste (inhalt), Einzeltitel (aufloesen),
      Zwei-Wege-Suche und Objektart-Frage (http: suche, art), Server-Befinden
      und die Pruef-Aktion. Der Zugang kommt aus kontext.konfig.jellyfin
      (Gruppe im Manifest angemeldet) - NICHT mehr aus den Coveradressen der
      Bibliothek. Die gemessenen Regeln reisten mit: static=true (Direct
      Play, sonst falsche Laengen in mpv), RunTimeTicks/10^7, searchTerm
      findet nur Namen (Interpreten ueber /Artists; Titel via ArtistIds,
      Alben via AlbumArtistIds).

      DAS MUSTER FUER SPIELWEGE: das Plugin liefert je Folge die FERTIGE
      Stromadresse (folgen[].quelle.adresse), der Werk-Aufloeser des Kerns
      reicht sie als `strom` am Titel weiter, und `jfBefehle` baut daraus
      nur noch die beiden Befehlsformen (jellyfin/... und jfqueue/...).
      Der Befehlsbau braucht damit KEINE Zugangsdaten mehr - wer einen
      neuen Strom-Anbieter anschliesst, gibt ihm Adressen mit, keinen
      Schluessel-Klaubeweg.

      IM KERN VERBLIEBEN (bewusst): QuickConnect, LAN-Suche
      (/api/jellyfin/suchen), adresse-pruefen und die Bild-/Strom-
      Weiterleitung (/api/bild/jellyfin, /api/jellyfin/strom) - letztere
      klauben weiter per jellyfinZugangAusListe, siehe
      [jellyfin-schluessel-kommt-vom-ERSTEN-eintrag-der-liste].

      OFFEN: der Hoertest am Geraet stand am 22.08. noch aus - waehrend der
      Messung lief eine ARD-Sendung (Position wanderte, also hoerte jemand),
      und ein Test haette sie verdraengt. Der Befehl ist byte-gleich zur
      alten Form, der Abspieldienst unangetastet.
    related: ["browse-ist-pluginsache-und-die-einzelabfrage-log", "kern-konfig-reist-in-den-kontext", "medienverwaltung"]

  - id: engine-plugins-sind-gesichter-der-hebel-bleibt-im-kern
    title: "E82: mixpi-librespot und mixpi-soloist sind GESICHTER der Tonmaschinen - der Hebel bleibt im Kern"
    kind: loesung
    match: "mixpi-librespot|mixpi-soloist|geraetestand|kontext.geraet|maschine.*berechnet|Engine-Plugin"
    tags: [plugins, spotify, engine, e82, 2026]
    source: "am 2026-08-22 an der Box gemessen: beide Plugins bereit in streaming/spotify, GET /api/spotify/maschine voll aus den Plugins gespeist (87 Resttage, anmeldung ja, 160 kbps), Rueckfall bei abgeschaltetem Plugin: volle Form + hinweis"
    body: |
      Seit E82 liefern zwei Plugins die MASCHINEN-AUSKUNFT (Dienststand,
      Build-Verfall, Anmeldung, Bitrate, befinden-Saetze, Aktion "Stand
      pruefen") in der Sektion streaming/spotify. Der HEBEL bleibt im Kern:
      spotify.engine + ExecConditions + maschinenWahlPruefen + PUT
      /api/spotify/maschine. Begruendung: die Zusicherung "genau eine
      Maschine laeuft, der Rueckweg ist immer frei" haengt an systemd-Units,
      Timern (soloist-updater!) und drei Ausrollwegen, die ein Plugin-Worker
      nur behaupten koennte - und die klassischen Ausrollwege kennen soloist
      heute gar nicht. Ein Engine-Plugin ABSCHALTEN nimmt deshalb NIE den
      Ton, nur die Auskunft; es gibt keine zweite Schalterachse.

      DIE NEUEN VERTRAGSSTUECKE:
      1. Recht `geraetestand` -> kontext.geraet (plugin-geraet.ts): BENANNTE
         Rezepte in geschlossenen Listen (dienstZustand fuer drei Units,
         ausfuehren fuer soloist -V / ctl status, lesen fuer Warnungsdatei
         und env-librespot). 2,5-s-Fristen JE Blick, weit unter den 8 s des
         Rufs - die alte Route reihte bis ~16 s Serienarbeit, das waere im
         Worker ein Terminate-Karussell. Ehrlich benannt: BESCHRIFTUNG, kein
         Kaefig (wie der holen-Zaun). systemctl-Deutung: das WORT zaehlt
         ("inactive" kommt mit Exit != 0), nie der Exit-Code.
      2. KONFIG_GRUPPEN + `maschine` = {engine, hatSchluessel} - BERECHNET in
         kernKonfigGruppen() (server.ts), steht in KEINER Datei. Ein Boolean
         ist kein Geheimnis; der spak_ erreicht kein Plugin. Weil es eine
         Gruppe wie jede andere ist, greift der E80-Neustart: der PUT
         schreibt engine, konfigAendernSicher rechnet neu, beide
         Engine-Plugins starten mit dem neuen Wert (Integrationszeuge mit
         eingebauter Gegenprobe). LAUTLOSE FALLE: wer nur KONFIG_GRUPPEN
         erweitert und die Synthese vergisst, bekommt nie einen Neustart -
         null wird mit null verglichen.
      3. GET /api/spotify/maschine SCHRUMPFT NIE: die Engine-Karte
         dereferenziert m.soloist.anmeldung ohne doppelten Boden. Faellt ein
         Plugin aus (aus/laedt/gescheitert), bleiben alle Felder stehen
         (Plugin-Anteile null, Dienststand direkt per systemctl) und
         `hinweis` sagt es in Worten. Davor pluginWarteBereit(3 s) - der PUT
         startet beide Plugins hart neu, und die Karte laedt sofort danach.
      4. Die Karte pollt im 3-s-Takt, solange die Anmeldung fehlt: die
         Plugins MERKEN ihren Stand 2,5 s, der Poll hat jetzt einen
         Wiedereintritts-Riegel.

      UMGEZOGEN, NICHT KOPIERT: bauDatumAus/verfallAus (90-Tage-Rechnung)
      wohnen jetzt in plugins/mixpi-soloist samt Zeugen;
      spotify-maschine.ts behaelt nur maschinenWahlPruefen fuer den PUT.

      VORAB GEFIXT (Kartierungsfunde, Commit 0197d9a2): tonerzeugerNeustarten
      ohne soloist.service (Box mit engine=soloist nach Bluetooth-Wechsel
      stumm), dienstBelegungSichern trug soloist.service bedingungslos als
      Strom-1-Halter ein (librespot-Box konnte nie mitschneiden),
      quellenAnzeigeName kannte den Soloist-Knoten "spotify" nicht.
    related: ["kern-konfig-reist-in-den-kontext", "jellyfin-zieht-ins-plugin", "soloist-erste-messung"]

  - id: laufwerk-worker-strippt-nur-typen
    title: "Der Plugin-Worker laeuft unter Nodes Type-Stripping: Typ-Importe ueberleben, WERT-Importe brauchen die Endung"
    kind: falle
    sev: mittel
    match: "Cannot find module.*plugin-laufwerk|type-stripping|allowImportingTsExtensions|Wert-Import.*Worker"
    cause: "new Worker(plugin-laufwerk.ts) laeuft im Test unter Nodes eingebautem TS-Support, der endungslose relative Importe NICHT aufloest"
    fix: "Wert-Importe im Laufwerk mit echter .ts-Endung schreiben; tsconfig braucht dafuer noEmit + allowImportingTsExtensions (tsc prueft nur, esbuild buendelt)"
    tags: [plugins, worker, typescript, node, 2026]
    source: "am 2026-08-22 beim E82-Bau: der erste WERT-Import des Laufwerks (plugin-geraet) brach ALLE Integrationszeugen; die Typ-Importe davor hatten jahrelang funktioniert, weil sie gestrichen werden"
    body: |
      plugin-wirt startet das Laufwerk als Worker-DATEI (laufwerkPfad():
      im Betrieb das esbuild-Buendel plugin-laufwerk.js, im Test die
      .ts-QUELLE). Die Quelle laeuft dann NICHT unter tsx, sondern unter
      Nodes eingebautem Type-Stripping - und das loest endungslose relative
      Importe nicht auf. Jahrelang fiel das nicht auf, weil das Laufwerk nur
      TYP-Importe hatte (werden ersatzlos gestrichen). Der erste
      Wert-Import ("Cannot find module .../plugin-geraet") legte ALLE
      Plugins im Test still - der Kern laeuft ruhig weiter, die Zeugen
      werden rot, die Meldung steht nur im Journal.

      DER TRAGENDE WEG: `import { geraetBauen } from './plugin-geraet.ts'`
      (echte Endung). Dafuer in src/backend-api/tsconfig.json `noEmit: true`
      und `allowImportingTsExtensions: true` - gefahrlos, weil tsc dort nie
      emittiert (esbuild baut server.js UND plugin-laufwerk.js als zwei
      Ausgaenge, siehe package.json build).
    related: ["engine-plugins-sind-gesichter-der-hebel-bleibt-im-kern"]

  - id: spak-schluessel-lagen-in-config-und-sicherung
    title: "Die spak_-Schluessel gingen ueber /api/config und die Sicherung im Klartext hinaus - drei Striche, und die Attrappe prueft nur, was sie kennt"
    kind: falle
    sev: hoch
    match: "spak_.*Klartext|soloistApiKey.*ohneGeheimnisse|soloistApiKeyMitschnitt|Sicherung.*spak"
    cause: "ohneGeheimnisse strich nur die flachen Alt-Felder; stroeme[*].schluessel liegt in einer LISTE, soloistApiKey/soloistApiKeyMitschnitt fehlten in Streichliste UND Sicherungs-GEHEIM"
    fix: "ohneGeheimnisse: + soloistApiKey/+soloistApiKeyMitschnitt/+stroeme-Listenlauf; Sicherung GEHEIM: /spotify/soloistApiKey, /spotify/soloistApiKeyMitschnitt, /spotify/stroeme/*/schluessel; Ring-1-Werkzeug um Marken erweitert"
    tags: [sicherheit, spotify, soloist, sicherung, 2026]
    source: "am 2026-08-22 an der Box: /api/config lieferte beide Schluessel + Strom-Schluessel im Klartext; frisches Archiv nach Fix 1: soloistApiKeyMitschnitt WEITER drin - erst die Geraete-Gegenprobe fand das Feld, das kein Werkzeug kannte. Nach Fix 2: keine spak_-WERTE im Archiv"
    body: |
      Waehrend /api/stroeme muehsam maskiert (stromNachAussen) und
      kontext.stroeme hinter dem Recht `aufnahme` liegt, gaben ZWEI andere
      Tueren dieselben Schluessel frei heraus: GET /api/config (an jeden
      Kiosk-Browser, ohne Anmeldung) und jede Sicherung im Download-Ordner.

      DIE LEHRE DER GEGENPROBE: Das Ring-1-Werkzeug
      (tools/sicherung-was-geht-hinaus.ts) war nach dem ersten Fix gruen -
      und das frische Archiv AM GERAET trug weiter einen spak_. Die
      Attrappe prueft nur Felder, die sie KENNT; das echte Geraet hatte
      zusaetzlich .spotify.soloistApiKeyMitschnitt (Zweitzugang fuer den
      Mitschnitt, tools/box/zweitzugang-probe.py). Eine gruene Attrappe
      ersetzt die Messung am Geraet nicht - sie ergaenzt sie
      ([einmal-hinsehen-ist-keine-messung], [attrappe-luegt-durch-weglassen]).

      VORSICHT BEIM NACHMESSEN: `grep -c spak_` im Archiv zaehlt auch die
      BESCHREIBUNGEN der ausgelassenen Schluessel in LIESMICH.txt/stand.json
      ("Soloist-Schluessel (spak_)..."). Zaehlen muss man WERTE, nicht
      Woerter - z.B. JSON laden und auf startswith("spak_") pruefen.

      REST-RISIKO, namentlich: eine Wiederherstellung OHNE --mit-zugangsdaten
      auf frischer Box mit engine=soloist ergibt eine stumme Spotify-Box bis
      zur Schluessel-Neueingabe - derselbe Preis, den der refreshToken
      schon immer zahlt; der Rueckweg meldet es namentlich.
    related: ["engine-plugins-sind-gesichter-der-hebel-bleibt-im-kern"]

  - kind: howto
    id: darstellung-zeigt-nur-was-der-schirm-liest
    title: "Darstellungsseite: sie blendet aus, was die laufende Oberflaeche gar nicht liest"
    tags: [darstellung, verwaltung, oberflaeche, neu, klassisch]
    body: |
      DER BEFUND, der den Umbau ausloeste (22.08.2026, Betreiber): „noch ist
      keine vorschau für das new design möglich, möchte auswahlmöglichkeiten
      welche nicht auf das aufgewählte theme funktionieren ausblenden.
      insgesammt ist es sehr unübersichtlich."

      GEMESSEN, nicht geschaetzt (tools/darstellung-felder-wer.mjs):

          59 Felder in `interface Darstellung`
          16 liest NUR die neue Oberflaeche (NewDesign, /neu)
          30 liest NUR die klassische (frontend-box, Ionic)
          13 lesen beide
           0 liest niemand

      Wer die NEUE Oberflaeche faehrt, sah also dreissig Schalter, die auf
      seiner Box nachweislich nichts tun; wer die klassische faehrt, sechzehn.
      Das ist die Haelfte der Seite, und es stand nirgends.

      ── ZWEI DINGE HEISSEN „THEMA", UND DAS IST DIE HALBE VERWIRRUNG ─────

        `mupibox.oberflaeche`   'klassisch' | 'neu' — WELCHE APP der Kiosk
                                startet. Steht in mupiboxconfig.json, wirkt
                                erst nach einem Neustart. DAS ist die Achse,
                                nach der ausgeblendet wird.
        `aussehen` im Thema     '' | 'classic' — nur ein anderes AUSSEHEN
                                INNERHALB der Ionic-App. Die Themen (MuPiBox
                                Classic, New MuPiBox, Tiger Mupi, MuPi-Bruecke)
                                tragen es mit; siehe mupi-themen-und-classic.

      Ein „New MuPiBox"-Thema auf einer Box mit `oberflaeche: klassisch` ist
      also NICHT die neue Oberflaeche — es ist die alte, die anders aussieht.

      ── WIE DIE SEITE JETZT GEBAUT IST ──────────────────────────────────

      Reiter ganz oben (Vorschau · Farben · Elemente · Bedienung · Themen ·
      Box); die VORSCHAU steht ausserhalb und bleibt immer sichtbar, weil man
      beim Stellen sehen will, was man stellt. Der Reiter merkt sich seinen
      Stand in `mupibox_admin_darstellung_reiter`.

      GANZE REITER verschwinden, wenn dahinter nichts liegt: „Farben" und
      „Bedienung" gehoeren vollstaendig der neuen Oberflaeche. Ebenso die
      Element-Gruppe „Icons" — alle vierzehn Anzeigen-Felder liest nur die
      klassische. Ein Reiter, der einen leeren Kasten zeigt, ist
      unuebersichtlicher als gar keiner.

      DER SCHALTER „auch Unwirksames zeigen (30)" holt alles zurueck, blass
      und durchgestrichen. Er MUSS es geben: `schreiben()` schickt den ganzen
      Stand per PUT, ein ausgeblendetes Feld reist unveraendert mit und gilt
      wieder, sobald die andere Oberflaeche laeuft. Ohne den Schalter saehe
      Ausblenden aus wie Datenverlust — und wer seine Kopfleisten-Einstellung
      nach dem Umschalten sucht, faende sie nicht.

      KEIN `pointer-events: none` auf dem Schlafenden: man DARF es stellen.
      Vor dem Umschalten schon einmal vorbereiten ist ein sinnvoller Handgriff.

      ── DIE GEFAEHRLICHE RICHTUNG IST NICHT DIE OFFENSICHTLICHE ─────────

      Ein Schalter, der faelschlich stehenbleibt, ist ein Aergernis: man
      drueckt ihn, nichts passiert, man meldet es. Ein Schalter, der
      faelschlich VERSCHWINDET, meldet sich nie — niemand sucht etwas, von dem
      er nicht weiss, dass es das gab.

      Deshalb sind die Listen NUR_NEU/NUR_KLASSISCH in darstellung.ts nicht
      von Hand gepflegt, sondern gegen die Messung verriegelt:

          node tools/darstellung-felder-wer.mjs            Tabelle
          node tools/darstellung-felder-wer.mjs --pruefen  meldet jede Abweichung

      Der Pruefschritt haengt in tools/pruefen.sh („Darstellung je Schirm").
      Gegengeprobt am 22.08.2026: ein Feld faelschlich in die Liste gesetzt
      -> exit 1; ein Feld herausgenommen -> exit 1.

      WAS NICHT IN DEN LISTEN STEHT, WIRKT UEBERALL. Das ist die richtige
      Vorbelegung: ein neues Feld, das noch niemand eingeordnet hat,
      verschwindet nicht still.

      FALLE BEIM MESSEN: `grep -c` taugt hier NICHT. Der Baum ist dicht
      kommentiert — `grep -c platzBeimBlaettern NewDesign/app.js` meldet 28
      Treffer, gelesen wird es an ZWEI Stellen, der Rest ist Fliesstext. Das
      Werkzeug entfernt deshalb Kommentare, bevor es zaehlt, und nimmt die drei
      Stellen aus, die jedes Feld nennen, ohne es zu benutzen (Interface,
      STANDARD, `pruefen()`).

      ZWEITE FALLE, beim ersten Anlauf prompt hineingetappt: der Dienst
      `darstellung.service.ts` wurde als „nur Weiterreichung" ausgenommen —
      daraufhin meldeten sechs Felder (kopfIcons, kopfHoehe, kopfBreite,
      statusLaenge, abstandBez, netzAnzeige) „niemand liest das". Sie werden IM
      DIENST angewandt: `imKopf()` fragt kopfIcons, `anwenden()` schreibt die
      anderen als CSS-Variablen. Haette die Seite danach gehandelt, waeren
      sechs funktionierende Schalter verschwunden.

      ── DIE VORSCHAU ZEIGT JETZT DIE RICHTIGE OBERFLAECHE ───────────────

      Der Rahmen laedt `/neu/` statt `/`, wenn `oberflaeche: neu` gilt. Vorher
      stand dort fest `/` — man stellte etwas ein und sah sich das Ergebnis
      auf einer ANDEREN Oberflaeche an als der auf dem Tisch. Beide liegen auf
      Port 8200, ein relativer Pfad genuegt.

      `?seite=player` kann NewDesign/app.js seit dem 22.08.2026 auch (es macht
      `$('gross').hidden = false`); am Geraet gegengeprueft: mit Parameter
      offen, ohne Parameter zu.

      ── DER AUFBAU DER SEITE (zweiter Schritt, 22.08.2026) ──────────────

      Betreiber: „ich hätte es gerne noch mehr als editor, es steht sehr viel
      text da und erstmal die optik im ersten schritt, das verhalten kann man
      in einer anderen ansicht machen."

      VIER REITER, Optik zuerst: **Optik · Verhalten · Themen · Box**. Farben
      und Elemente sind IN der Optik aufgegangen (Unterreiter Mini-Player /
      Kacheln & Knoepfe / Anzeigen / Farben) — eine Kachel hat eine Form UND
      eine Farbe, und wer dafuer die oberste Ebene wechseln muss, stellt sie
      nacheinander statt nebeneinander. „Verhalten" ist die alte „Bedienung"
      und faellt auf der klassischen Oberflaeche ganz weg.

      WERKBANK: ab 1100 px stehen Leinwand und Regler NEBENEINANDER, die
      Leinwand `position: sticky`. Vorher stand das Bild oben und die Regler
      darunter — wer einen Wert verstellte, war weit genug weggerollt, um die
      Wirkung nicht mehr zu sehen. Darunter bleibt es einspaltig.

      ACHTUNG, ZWEI ORTE FUER DREI ZAHLEN: `maxMass()` rechnet die groesste
      Vorschau aus und muss die Reglerspalte abziehen (WERKBANK_AB 1100,
      REGLER_MIN 420, SPALT 16 — dieselben Werte wie im CSS). Wer nur das CSS
      aendert, bekommt eine Vorschau, die Platz beansprucht, den das Raster ihr
      nicht gibt. Am Bild gemessen: 1280 px -> 764 px Leinwand + 420 px Regler,
      kein waagerechtes Rollen; 768 px -> eine Spalte, 724 px Leinwand.

      DER TEXT STEHT AB WERK NICHT MEHR DA. Gemessen waren 894 Woerter
      Fliesstext in der Vorlage neben rund siebzig beschrifteten
      Bedienelementen; der Knopf „? Erklaerungen" holt sie zurueck (gemerkt in
      `mupibox_admin_darstellung_text`). Sichtbar bleiben 196 Woerter — 78 %
      weniger.

      WAS TROTZDEM IMMER DASTEHT und NICHT eingeklappt wurde: Aussagen ueber
      den JETZIGEN Stand — der gemessene Kontrast, unbekannte Farben im Thema,
      die Warnung bei doppelt belegter Geste, der Grund fuer einen fehlenden
      Regler, der Satz statt des Anordnen-Knopfes. Das sind Rueckmeldungen,
      keine Erlaeuterungen; sie wegzuklappen hiesse, die Seite verschweigt
      etwas ueber den Zustand der Box.

      ── DIE VERWALTUNG ANSEHEN, OHNE BOX ────────────────────────────────

      `node tools/admin-vorschau.mjs [--port 8298] [--schirm neu|klassisch]`

      Das Gegenstueck zu neu-vorschau.mjs. Die Verwaltung haengt hinter der
      Anmeldung — `ng serve` allein zeigt nur das Anmeldefenster, weil
      `/api/auth/state` niemand beantwortet. Der Stub spielt das Backend mit
      erfundenen Daten (kein Passwort im Spiel, es gibt dort nichts zu
      schuetzen) und bedient auch `/neu/`, damit der Vorschaurahmen etwas zu
      zeigen hat. `--schirm` schaltet zwischen beiden Oberflaechen um, ohne
      etwas umzustellen.

      ER BEDIENT DEN GEBAUTEN STAND aus `src/deploy/www-admin` — wer eine
      Aenderung nicht sieht, hat nicht gebaut. Genau darauf am 22.08.2026
      hereingefallen: die Seite zeigte einen Reiterbalken ohne aktiven Reiter
      und darunter nichts, und der Fehler war eine Viertelstunde alt und schon
      behoben.

      ANORDNEN GIBT ES AUF DER NEUEN OBERFLAECHE NICHT — und der Knopf steht
      dort deshalb nicht da, sondern ein Satz. Das ist kein vergessener
      Parameter: NewDesign liest `plaetze` ueberhaupt nicht (nur `skalen.titel`
      und `skalen.cover`), und die Funktion `anordnen()` in ihrer app.js ist
      eine gleichnamige SORTIERFUNKTION fuer Medienlisten, kein
      Bearbeiten-Modus. Ein Anordnen fuer /neu ist ein Neubau (Ziehen, Halten
      zum Umschalten, Groesse, Ausblenden, Ablage in `plaetze`), keine
      Nachruestung.
    related: ["mupi-themen-und-classic", "darstellung-elemente-und-anzeigen", "darstellung-einstellungen", "mupi-wolke-frei-stehend"]

  - id: ausgangswechsel-schont-leersenken
    kind: falle
    title: "Der Ausgangswechsel nahm ALLE Stroeme mit - auch den stummen Mitschnitt in der Leersenke"
    match: "mitschnitt ploetzlich hoerbar|stummer strom toent|leersenke.*mitverschoben|move-sink-input.*alle stroeme|aufnahme falscher weg|bluetooth wechsel mitschnitt"
    sev: med
    tags: ["ton", "pipewire", "mitschnitt", "tonausgang", "leersenke", "e83", 2026]
    source: >
      22.08.2026, Box .62, tools/box/leersenke-bleibt-probe.py: Probestrom in
      mixpi-mitschnitt (Senke 33) gelegt, POST /api/ton/ausgang auf den
      eingebauten Ausgang. Alter Stand: "5 Strom/Stroeme mitgenommen",
      Probestrom danach auf Senke 65 - MITGERISSEN. Nach dem Fix: "3
      mitgenommen, 1 in Leersenke gelassen", Probestrom blieb auf 33.
    body: |
      POST /api/ton/ausgang schiebt beim Wechsel jeden laufenden Strom auf
      den neuen Ausgang (move-sink-input) - sonst hoerte man den Wechsel
      erst beim naechsten Stueck. Aber NICHT JEDER STROM WILL FOLGEN: der
      Mitschnitt (plugins/mixpi-mitschnitt) parkt seinen Aufnahme-Strom
      absichtlich still in der Leersenke mixpi-mitschnitt. Wanderte der
      mit, toente der stumme Mitschnitt ploetzlich auf dem Lautsprecher,
      und die Aufnahme griff den falschen Weg ab.

      DIE REGEL SEIT DEM FIX (tonausgang.ts, mixpiLeersenken/stroemeAus):
      Stroeme, deren Senke mit `mixpi-` beginnt, werden beim
      Ausgangswechsel GESCHONT. Erkannt wird am NAMEN, denn die
      Kurzausgabe von pactl verraet nicht, ob hinter einer Senke Hardware
      steckt. Alles Eigene der Box heisst mixpi-…; die gewachsenen Wege
      (alsa_output, bluez_output, entzerrer, klangwerk, ueberall) heissen
      es nie und wandern weiter mit. Das Serverlog zaehlt beides:
      "N mitgenommen, M in Leersenke gelassen".

      BEIM ZERLEGEN NICHT VERWECHSELN: In `pactl list short sink-inputs`
      ist die zweite Spalte die Senken-KENNUNG (Zahl), nicht der Name -
      die Schonliste muss deshalb Kennungen tragen, aus der Senkenliste
      uebersetzt (dort: Spalte 1 Kennung, Spalte 2 Name).

      DIE PROBE IST WIEDERVERWENDBAR und raeumt hinter sich auf: sie
      sichert Vorgabe und Strom-Lage, legt den Probestrom per mpv in die
      Leersenke, geht den echten Elternweg ueber HTTP und dreht alles
      zurueck. Der Probestrom stirbt ueber seine PID, nicht per pkill -f.

      NACHTRAG 22.08.2026 (Ableger-Pruefung): Die Schonung machte einen
      alten Fehltritt DAUERHAFT — wer die Leersenke selbst als Ziel
      waehlte, parkte alle Stroeme im Stummen, und der Rueckweg liess sie
      neuerdings liegen. Seither: mixpi-Senken stehen nicht mehr im
      Angebot (ausgaenge() filtert) und der POST weist sie ab. GRENZE DER
      ERKENNUNG, mit Absicht offen: geschont wird per Namens-PRAEFIX
      mixpi-, nicht per Eigenschaft. Bekommt eine HOERBARE Station beim
      Umbau einmal den mixpi-Namen (Branding-Regel!), schont der Wechsel
      sie faelschlich still — dann auf Eigenschafts-Erkennung umstellen
      (pactl -f json, null-audio-sink hat kein device.api).
    related: ["mitschnitt-plan", "entzerrer-bleibt-verkabelt-wenn-aus", "pipewire-abgriff-je-quelle", "mixpi-praefix-fuer-alles-neue"]

  - id: fetch-leiht-nach-serverwechsel-die-tote-verbindung
    title: "Serverwechsel auf demselben Port: der globale fetch-Pool leiht die tote Keep-Alive-Verbindung weiter"
    kind: signature
    sev: med
    match: "fetch failed|UND_ERR_SOCKET|other side closed|triggerUncaughtException"
    cause: "ein Messwerkzeug schliesst einen HTTP-Server und startet im SELBEN Node-Prozess einen neuen auf demselben Port; undicis globaler fetch-Pool haelt je HERKUNFT Keep-Alive-Verbindungen und reicht eine zum ALTEN Server gehoerende weiter — der naechste fetch stirbt mit «other side closed»"
    fix: "dem zweiten Server einen EIGENEN Port geben (frische Herkunft = frischer Pool); dazu fetch-Fehler im Messring fangen (roter Befund statt Absturz) und unhandledRejection/uncaughtException auf eine Bilanz-Funktion legen"
    tags: [werkzeug, node, fetch, undici, keep-alive, sicherung, falle, 2026]
    source: "22.08.2026 an tools/sicherung-was-geht-hinaus.ts (Node v26.7.0) gefunden und mit zwei Sabotage-Gegenproben belegt; per git stash als Altlast bestaetigt (bestand ohne die Tagesaenderungen)"
    body: |
      DER BELEG, DASS ES DIE GEERBTE VERBINDUNG WAR: der sterbende Socket im
      Stacktrace meldete bytesWritten 4202879 — das sind die zwei rund
      2-MB-Uploads aus Ring 6. Ring 3 hatte auf dieser Verbindung nie etwas
      geschickt, er hat sie nur aus dem Pool GELIEHEN. server.close()
      schliesst (seit Node 19) auch die muessigen Verbindungen mit, aber der
      Pool des Clients erfaehrt davon asynchron — wer direkt danach auf
      demselben Port neu lauscht und fetcht, faengt die alte Verbindung im
      Fenster zwischen FIN und Poolpflege. Ring 2 gegen den ERSTEN Server
      lief deshalb immer durch; erst der Serverwechsel riss.

      WARUM DER PROZESS STARB, STATT ROT ZU WERDEN: `void lauf()` am
      Dateiende warf die Ablehnung weg — unhandledRejection,
      triggerUncaughtException und keine Bilanz. (Die Sandkaesten raeumte
      schon die Basisfassung im finally weg — das lief auch beim Absturz,
      an der Nachpruefung vom 22.08. gemessen.) Seitdem: lauf().catch(...),
      Fallen fuer unhandledRejection UND uncaughtException, alle drei muenden
      in bilanz(), und bilanz() raeumt den Sandkasten auch auf dem
      Absturzweg weg.

      DIE BEHEBUNG IM WERKZEUG: Ring 3 lauscht auf RING3_PORT 9877 (9876
      gehoert Karma, src/frontend-box/karma.conf.js) — eine frische Herkunft
      ist ein frischer undici-Pool, deterministisch, ohne auf Zeitfenster zu
      hoffen. Der fetch des Rings steht zusaetzlich in try/catch: ein
      Netzfehler ist ein ROTER BEFUND mit Ursache (fehlerText liest
      error.cause — «fetch failed» allein sagt nichts).

      GEGENPROBEN (22.08.2026, beide danach zurueckgebaut): fetch absichtlich
      gegen einen toten Port -> roter Befund «ECONNREFUSED», Bilanz, Exit 1.
      Absichtlich liegengelassenes Promise.reject -> Falle faengt, zaehlt
      rot, Bilanz, Exit 1. Endstand des ehrlichen Laufs: 47 gruen, 0 rot,
      kein Sandkasten uebrig.

      VERALLGEMEINERUNG: jedes Werkzeug, das in EINEM Prozess einen Server
      stoppt, neu startet und mit dem globalen fetch misst, traegt diese
      Falle. Auswege der Reihe nach: eigener Port je Serverleben
      (deterministisch, hier gewaehlt), eigener undici-Dispatcher je Phase,
      oder closeAllConnections() abwarten (laesst ein Restfenster). Eine
      Connection:-close-Kopfzeile ist KEIN Ausweg — fetch streicht sie als
      verbotene Kopfzeile.

  # ══ Ausrollweg-Angleichung: Soloist + Plugins (22.08.2026) ═══════════════
  - kind: falle
    id: soloist-fehlte-in-beiden-schalen-wegen
    title: "Die Schalen-Wege kannten Soloist nicht — und das Werkzeug sah die ganze Quellklasse nicht"
    sev: high
    match: "spotify\\.engine|soloist.*(update|autosetup)|scripts/systemd/.*\\.(service|timer)|engine\\.conf"
    cause: "update/start_mupibox_update.sh und autosetup/autosetup.sh hatten NULL Treffer fuer 'soloist' — nur Rezept und Handweg (einrichten.sh) rollten E42 aus; und tools/ausrollweg-deckung.py kannte scripts/systemd/ nicht als Unit-Quelle, die Luecke fiel durch alle vier Etagen"
    fix: "beide Schalen-Wege legen jetzt Skripte, Units, Drop-In und Timer; das Werkzeug kennt zwei Quellordner, meldet Doppelquellen und prueft das Drop-In (Abschnitt 3b)"
    tags: ['soloist', 'ausrollweg', 'autosetup', 'update', 'systemd', 'falle', 'e42', 2026]
    source: "Kartierung 22.08.2026, am Quelltext gemessen; gruen geprueft mit tools/ausrollweg-deckung.py"
    body: |
      DER BEFUND IN EINEM SATZ: eine Box, die per mupibox.de-Update oder
      frischer SD-Karte kommt, bekam weder soloist.service noch das
      librespot-Drop-In (den Engine-Schalter) noch den soloist-updater.timer
      — der Schluessel spotify.engine existierte dort schlicht nicht. Die
      Verwaltung haette den Wechsel angeboten, und der Schalter haette auf
      NICHTS gezeigt: exakt die mupi_fan-Fehlerklasse, nur eine ganze
      Tonmaschine gross.

      WAS JETZT IN ALLEN DREI WEGEN STEHT (Schalen-Wege, einrichten.sh
      UND Rezept — alle aus DENSELBEN Box-Repo-Dateien: der soloist-Schritt
      in mupibox-app.yaml zieht per ${MUPI_REPO}/scripts/... dieselben
      Quellen. Am 22.08. NACHGEMESSEN, nachdem eine Pruefer-Behauptung
      ueber Eigenkopien unter remote-step-installer/dateien/systemd/ hier
      kurz stand: dieses Verzeichnis EXISTIERT NICHT, die Behauptung war
      falsch und ungeprueft uebernommen — plausibel klingende Prueferworte
      sind Befunde zum NACHMESSEN, keine Wiki-Saetze):

          scripts/soloist/soloist-start.sh      -> /usr/local/bin/mupibox/
          scripts/soloist/soloist-updater.sh    -> /usr/local/bin/mupibox/
          scripts/systemd/soloist.service       -> /etc/systemd/system/
          scripts/systemd/soloist-updater.*     -> /etc/systemd/system/
          scripts/systemd/librespot-engine.conf -> librespot.service.d/engine.conf
          enable soloist.service                (NIE starten — die ExecCondition
                                                 entscheidet, Vorgabe librespot)
          enable + start soloist-updater.timer  (eigene Zeilen! die Schleifen
                                                 haengen ".service" an)
          Binary holen, wenn es fehlt           (verfaellt 90 Tage nach dem BAU
                                                 — mitliefern waere halb verfallen)

      An einer Box, die nie umschaltet, aendert sich NICHTS: die Condition
      des Drop-Ins ist wahr, librespot startet wie bisher.

      WARUM DAS WERKZEUG DIE LUECKE NIE MELDETE: ausrollweg-deckung.py las
      Units nur aus config/services/. Die E42-Units liegen unter
      scripts/systemd/ (neben einrichten.sh) — keine Tabellenzeile, kein
      Befund, nichts. Ein Werkzeug, das eine ganze Quellklasse nicht kennt,
      meldet deren Luecken an niemanden. Jetzt: BEIDE Ordner sind
      Unit-Quellen, eine Unit in beiden ist ein Befund (zwei Fassungen
      liefen auseinander — genau die librespot-Frage 'drei Quellen', die
      sich nur deshalb nicht stellt, weil ALLE Wege dieselbe Repo-Datei
      config/services/librespot.service kopieren), und Handweg-Units
      (Taster, Knopflicht, Sonde, Adapterwahl) stehen mit Grund in
      NUR_HANDWEG_MIT_GRUND — geprueft gegen die DIENSTE-Zeile von
      einrichten.sh, damit der Grund keine Falschauskunft wird.

      DIE REGEX-LUECKE OBENDRAUF: `systemctl enable --now X` fing die
      Namensgruppe als "--now" — der Bindestrich steht in der Zeichenklasse.
      mupibox-server/-player waren deshalb fuer die Einschalt-Pruefung
      unsichtbar. tools/units-decken-sich.sh war am 14.08. in DIESELBE
      Falle gelaufen und traegt seither den Vermerk; jetzt schluckt auch
      hier `(?:--[\\w-]+\\s+)*` die Optionen.

      NEBENFUND DERSELBEN KARTIERUNG: der Update-Weg STOPPTE
      mupibox-server/-player oben und STARTETE sie unten — GELEGT hat er
      sie nie. Eine Box aus der pm2-Aera lief damit ins Leere, ohne
      Meldung. Jetzt legt er beide Units und sagt `enable --now` (die alte
      Begruendung "ein Update schreibt den Einschaltzustand nicht um"
      setzte voraus, dass die Units schon liegen — einen bewusst
      abgeschalteten Hauptdienst gibt es nicht, ohne die beiden ist die
      Box keine Box).

      NACHMESSEN:
        python3 tools/ausrollweg-deckung.py
        sh tools/units-decken-sich.sh
    related: [update-ueberschreibt-librespot, piper-fehlt-im-ausrollweg, unit-nur-in-autosetup-erreicht-keine-bestehende-box, angelegt-ist-nicht-eingeschaltet]

  - kind: falle
    id: cp-rn-friert-plugins-auf-dem-erststand-ein
    title: "`cp -rn` rollt Plugins genau EINMAL aus — danach nie wieder"
    sev: med
    match: "cp -rn.*plugins|Plugin.*(veraltet|alte Fassung)|plugin\\.json.*fassung"
    cause: "no-clobber klang wie Ruecksicht ('was da liegt, gehoert dem Betreiber'), hiess aber: einmal ausgerollt wird ein Plugin NIE aktualisiert, waehrend Wirt und Plugin-Vertrag weiterwandern — und die Kopie sass obendrein im PipeWire-Zweig, eine ALSA/PulseAudio-Box bekam GAR keine"
    fix: "scripts/mixpi/mixpi-plugins-nachziehen.sh in beiden Schalen-Wegen, ausserhalb des Tonstapel-case: kopiert Neues, ersetzt bei hoeherer fassung (plugin.json), laesst Gleichstand und Betreiber-Eigenes in Ruhe"
    tags: ['plugins', 'update', 'autosetup', 'ausrollweg', 'falle', 'mixpi', 2026]
    source: "22.08.2026, am Quelltext (update:1284, autosetup:966); Regeln belegt mit tools/mixpi-plugins-nachziehen-probe.sh"
    body: |
      DIE FEHLERKLASSE ist die Gegenrichtung von
      [[update-ueberschreibt-librespot]]: dort ersetzte das Update etwas
      Gutes durch Schlechteres, hier BRACHTE es etwas Besseres mit und
      liess es liegen. Beides sieht am Ende gleich aus — der Fehler wird
      beim Plugin gesucht, nicht beim Kopierbefehl. Ein eingefrorenes
      Plugin von vor einem Jahr muss irgendwann mit einem Wirt reden,
      dessen Vertrag weitergewandert ist.

      DIE REGELN von mixpi-plugins-nachziehen.sh (jede einzeln in der
      Probe belegt):

          Ziel fehlt                  -> kopieren (Erstausrollung)
          fassung(Quelle) NEUER       -> ERSETZEN, ganz: rm + cp. Ein Plugin
                                         ist ein Stand, kein Schichtkuchen —
                                         Drueberkopieren liesse Dateien des
                                         alten Standes liegen (dieselbe
                                         Klasse wie chunk-<hash>.js beim
                                         Angular-Bau)
          gleich oder aelter          -> NICHTS (Downgrade-Schutz: ein altes
                                         Installationspaket darf nichts
                                         zurueckdrehen)
          Ziel ohne plugin.json       -> ersetzen (halber Stand ist Schutt,
                                         kein Betreiber-Eigentum)
          Ordner NUR im Ziel          -> unangetastet (gehoert dem Betreiber)

      Verglichen wird `fassung` numerisch je Stelle (sort -V): 0.10.0 ist
      NEUER als 0.9.0 — ein Textvergleich saehe es andersherum, und genau
      diese Sorte Fehler faellt erst nach Monaten auf, wenn die zehnte
      Unterfassung kommt.

      DER VERTRAG DARAUS, und er ist keine Formsache: WER EIN PLUGIN
      AENDERT, LAESST fassung IN plugin.json HUEPFEN. Gleichstand wird mit
      Absicht nicht angefasst (Betreiber-Anpassungen ueberleben) — eine
      Aenderung ohne Fassungs-Sprung erreicht deshalb KEINE Box.

      Der Aufruf steht in BEIDEN Wegen und ist in AUFRUF_NOETIG von
      tools/ausrollweg-deckung.py eingetragen — kopiert ist nicht
      aufgerufen. DER DRITTE WEG IST SEIT 22.08.2026 AUCH ZU: der
      Rezeptschritt `plugins` (mupibox-app.yaml, vor dem ersten
      Serverstart) liefert ${MUPI_REPO}/plugins als Verzeichnis-put und
      ruft DENSELBEN Kopierer — Erstausrollung kopiert, ein Rezept-Lauf
      auf einer bestehenden Box ersetzt nur Neueres und laesst
      Betreiber-Eigenes stehen. Gegenprobe: paket_bauen sammelt plugins/
      samt Wurzel ins Paket (Probe-Bau am Entwicklerrechner). Die Probe (tools/mixpi-plugins-nachziehen-probe.sh)
      prueft alle Regeln, die Wiederholbarkeit (zweiter Lauf bewegt
      nichts) und wurde per Sabotage gegengeprueft: Downgrade-Schutz
      entfernt -> Probe rot.
    related: [plugin-laufwerk-ist-eigenes-ausrollziel, update-ueberschreibt-librespot, soloist-fehlte-in-beiden-schalen-wegen]

  - id: zwei-engine-blindstellen-telegram-und-playerstate
    title: "Zwei offene Engine-Blindstellen: Telegram meldet nur bei librespot, und /tmp/playerstate sieht Connect-Wiedergabe nicht"
    kind: befund
    match: "telegram.*soloist|onevent.*fehlt|playerstate.*connect|Kinderzeit.*Handy|idle_shutdown.*spotify"
    tags: [spotify, engine, telegram, kinderzeit, offen, 2026]
    source: "Kartierung + Vollstaendigkeits-Kritik vom 22.08.2026 (E82-Vorarbeit); bewusst NICHT im E82-Block behoben — beide brauchen eine Ereignis-Entscheidung, keine Eile"
    body: |
      1. TELEGRAM-ASYMMETRIE: Der onevent-Hook (telegram_Track_Spotify.py)
         haengt an librespot (config/templates/env-librespot); Soloist
         startet ohne jeden Ereignis-Haken (soloist-start.sh). Mit
         engine=soloist fallen Telegram-Titelmeldungen still weg — kein
         Fehler sichtbar, sie kommen einfach nicht mehr.
      2. /tmp/playerstate SIEHT NUR BOX-GESTARTETES: Nur spotify-control
         schreibt es; Kinderzeit (kzSpieltGerade), idle_shutdown, Waechter,
         mqtt und mupi_shutdown lesen es. Wirft ein Handy per Connect Musik
         auf die Box, zaehlt die Kinderzeit nicht mit und die Leerlauf-
         Abschaltung haelt die Box fuer untaetig. Soloists WebSocket und
         librespots onevent WISSEN es besser.

      DER GEMEINSAME KERN: beiden fehlt EIN kanonisches Spielt-Signal, das
      die Engines nach Faehigkeit speisen (Vertragspunkt ereignis()/E75-
      Draht waeren die Kandidaten). Das ist eine Entwurfsentscheidung fuer
      einen eigenen Block — die Engine-Plugins (E82) sind dafuer die
      natuerliche Heimat, [engine-plugins-sind-gesichter-der-hebel-bleibt-im-kern].
    related: ["engine-plugins-sind-gesichter-der-hebel-bleibt-im-kern"]

  - id: fundpruefen-baut-den-fund-neu-zusatzfelder-fallen-still
    title: "fundPruefen gibt nicht zurueck, was das Plugin liefert — es baut den Fund aus sechs Feldern NEU, alles andere faellt still heraus"
    kind: falle
    match: "zusatzfeld.*weg|songtext.*kommt nicht an|kapitelmarken.*fehlen|fund.*feld verschwunden|plugin liefert mehr als ankommt"
    sev: medium
    tags: [plugins, vertrag, fund, falle, e84, 2026]
    source: "22.08.2026, beim Pruefen, ob ein Songtext-Plugin ohne Vertragsaenderung baubar waere (E84). Gelesen in plugin-vertrag.ts, nicht vermutet."
    symptom: >
      Ein Plugin gibt neben `titel` und `quelle` ein weiteres Feld zurueck —
      Songtext, Kapitelmarken, Altersfreigabe — und im Kern kommt davon nichts
      an. Keine Fehlermeldung, kein Journaleintrag, kein abgewiesener Fund.
      Das Plugin gilt als gruen, die Nutzlast ist trotzdem weg.
    cause: >
      `fundPruefen` (plugin-vertrag.ts) ist keine Pruefung, die durchreicht,
      sondern eine, die NEU BAUT. Am Ende steht woertlich
      `const titel: Titel = { name }`, danach werden genau drei weitere Felder
      einzeln uebernommen, wenn sie den Typ haben: `kuenstler`, `bild`,
      `dauerSek`. Dazu `quelle.art` und `quelle.adresse`. Sechs Felder, sonst
      nichts. Ein siebtes wird nicht abgewiesen — es wird nie angesehen.
    fix: >
      Nicht versuchen, am Fund vorbeizuschmuggeln. Wer eine neue Sorte
      Nutzlast braucht, braucht eine Vertragsergaenzung: eigene Methode,
      eigene Pruefung, eigener Weg in die Oberflaeche — dieselbe Bauart wie
      `inhalt()` (E78). Fuer den Songtext-Fall kommt erschwerend hinzu, dass
      auch `http()` (E77/E79) nicht traegt: es liegt hinter dem
      Verwaltungstor und erreicht den Kinderschirm ausdruecklich nicht.
    body: |
      DAS IST GEWOLLT UND GUT SO — die Haltung ist dieselbe wie beim
      geschlossenen Klangvokabular: was nicht in der Liste steht, gibt es
      nicht, und im Zweifel wird nicht gebaut. Der Eintrag steht hier nicht
      als Mangel, sondern weil die Stille die Falle ist: ein abgewiesener
      Fund meldet sich, ein verworfenes Feld nicht.

      ZUM PRUEFEN, ob ein Wunsch ohne Vertragsaenderung geht: die sechs
      Felder sind die ganze Antwort. Steht die gewuenschte Angabe nicht
      darunter, ist die Frage keine Plugin-Frage mehr, sondern eine
      Entwurfsfrage.
    related: ["plugins-melden-sich-an-statt-gekannt-zu-werden", "klangwerk-geschlossenes-vokabular-statt-freier-conf", "inhalt-im-vertrag-und-ard-delegiert"]

  - id: anbieter-filter-nur-was-eine-http-adresse-liefert
    title: "Welcher Streamingdienst als Plugin baubar ist, entscheidet EINE Zeile: quelle.art strom laesst nur http/https zu"
    kind: regel
    match: "tidal.*plugin|apple music.*plugin|deezer.*plugin|audible.*plugin|drm.*plugin|streamingdienst anbinden"
    sev: low
    tags: [plugins, anbieter, vertrag, entwurf, e84, 2026]
    source: "22.08.2026, beim Abgleich der Music-Assistant-2.9-Providerliste gegen den MuPiBox-Vertrag (E84)."
    body: |
      DIE FRAGE VOR ALLEN ANDEREN. Bevor ueber Geschmack, Zielgruppe oder
      Aufwand geredet wird, entscheidet `fundPruefen`: bei
      `quelle.art: 'strom'` sind nur `http:` und `https:` erlaubt. Ein
      Anbieter ist also genau dann baubar, wenn am Ende eine DIREKT
      ABSPIELBARE URL herauskommt — kein DRM, kein nativer Client, keine
      Entschluesselung im Plugin.

      ══ GEHT ══════════════════════════════════════════════════════════════
      Audiobookshelf, Plex, Emby, Subsonic/Navidrome, WebDAV, Internet
      Archive, Radio Browser, Podcast-Verzeichnisse (iTunes Search, Podcast
      Index), SomaFM, Radio Paradise. Gemeinsamer Nenner: offene oder
      dokumentierte API, Datei- oder Stromadresse im Klartext.

      ══ GEHT NICHT ════════════════════════════════════════════════════════
      Apple Music und Tidal (FairPlay/Widevine), Deezer (Blowfish-
      verschluesselte Stroeme), Audible (AAX, Activation Bytes), YouTube
      Music (Signaturextraktion — reisst ausserdem 8-s-Frist und 48 MB).
      Spotify faellt nicht unter diese Regel, weil es im Kern sitzt.

      ══ EIN ZWEITER RIEGEL, DER OFT FALSCH GELESEN WIRD ════════════════════
      `adresseErlaubt()` im Laufwerk sperrt NUR `localhost`, `::1`, `127.*`
      und die EIGENEN Adressen der Box — nicht das LAN. Ein Plex auf
      192.168.178.30 ist fuer ein Plugin erreichbar. Die Handbuchformel „und
      die eigenen LAN-Adressen" laedt zur Fehllesart ein, das ganze Netz sei
      zu; genau diese Fehllesart haette beim Entwurf von E84 die halbe Liste
      gestrichen und wurde erst durch Nachlesen im Quelltext korrigiert.

      FOLGE FUER RCLONE: `rclone serve` auf 127.0.0.1 ist fuer ein Plugin
      NICHT abfragbar. Deshalb ist `rclone mount` in den Medienordner der
      einfachere Weg — dann sieht die Box Dateien und braucht gar kein
      Plugin.
    related: ["plugin-laufwerk-ist-eigenes-ausrollziel", "fundpruefen-baut-den-fund-neu-zusatzfelder-fallen-still"]

  - id: ard-radiosender-eigener-einstieg-nicht-gelockerte-sperre
    title: "E84: Die ARD-Radiosender kommen aus permanentLivestreams — die Sperre gegen Livestreams in Folgenlisten bleibt zu"
    kind: erkenntnis
    match: "ard radiosender|permanentLivestreams|ard live radio|sender aus ard sounds|kiraka|radiosender plugin"
    sev: mid
    tags: [plugins, ard, radio, e84, 2026]
    stand: "gegen die echte ARD gemessen 22.08.2026: 195 Sender, Vorschlaege als type radio, 20/20 Zeugen gruen samt drei Sabotagen"
    source: "22.08.2026, Betreiberwunsch „aus ard sounds koennen auch radiosender abgerufen werden\"; sondiert mit tools/ard-sender-probe.mjs"
    body: |
      DER ERSTE REFLEX WAERE FALSCH GEWESEN. `folgenAus()` wirft
      `EVENT_LIVESTREAM` weg, und wer die Sender will, moechte diese Sperre
      lockern. Das waere ein Rueckschritt: ein nicht endender Strom zwischen
      den Folgen einer Hoerspielreihe ist fuer ein Kind eine Kachel, die
      niemand mehr ausbekommt ([ard-items-ohne-ton-und-die-falschen-zahlen]).

      DIE ARD HAT DAFUER EINEN EIGENEN EINSTIEG. Das Schema nennt 89 Felder
      unter Query, fuenf davon klingen nach Live; das richtige ist
      `permanentLivestreams` (daneben `permanentLivestream(id)`,
      `permanentLivestreamByCoreId`, `…ByNodeId`, `eventLivestream`).
      Gemessen: 195 Sender. Die Sperre bleibt damit unangetastet — es kommt
      eine zweite Sorte daneben, statt die erste aufzuweichen.

      ══ WAS IM DATENSATZ STEHT ═════════════════════════════════════════════
      `audios` fuehrt ZWEI Adressen, und die Reihenfolge ist verlaesslich:
      erst MP3 (`audio/mp3`, Icecast der Anstalt), dann HLS
      (`application/vnd.apple.mpegurl`). Das vorhandene `tonAus()` nimmt
      genau deshalb die richtige, ohne geaendert zu werden: es siebt auf
      `audio/` und laesst HLS liegen. Ein Sender OHNE MP3 wird gar nicht
      angeboten — eine Kachel, die beim Druck nichts tut, ist schlimmer als
      eine fehlende.

      ══ DER VORSCHLAG TRAEGT `radio`, NICHT `ard` ══════════════════════════
      `artVon()` (werke.ts) bildet `dienst === 'ard'` UNBEDINGT auf `show` ab,
      also auf „eine Folge nach der anderen". Ein Sender ist das nie; die
      Oberflaeche erwartete hinter der Kachel eine Folgenliste, die es nicht
      gibt. `radio` ist die Art, die der Kern fuer „laeuft, bis jemand
      aufhoert" schon hat, und sie traegt ihre Adresse in `id`.

      DAMIT WIRD HIER DIE ADRESSE GESPEICHERT, obwohl `aufloesen()` daneben
      das Gegenteil predigt („Kennung speichern, Adresse holen"). Jene Regel
      gilt FOLGEN und dort zu Recht: eine Folge traegt `availableTo` und
      verschwindet. Ein Sender hat kein Ablaufdatum. Fuer `radio` fragt der
      Kern ausserdem gar kein Plugin — der andere Weg ginge nur mit einer
      Kernaenderung.

      ══ ZUR ZIELGRUPPE — UND WARUM NICHTS AUSGEBLENDET WIRD ════════════════
      Von 195 Sendern sind ZWEI ausdruecklich fuer Kinder gemacht: „Die Maus
      – Dein Radio" (so heisst KiRaKa heute — die Suche nach „kiraka" findet
      nichts) und MDR TWEENS. Die Suche nach „kinder" liefert 0 Treffer.

      DARAUS FOLGT NICHT, dass der Rest Beifang waere. Betreiber, 22.08.2026:
      „die box ist auch fuer grosse Kinder" — an ihr hoeren Erwachsene mit,
      und fuer die sind die uebrigen 193 der eigentliche Bestand. Die
      Verwaltung bekommt deshalb ein SUCHFELD und keine kuratierte
      Kinderliste, und sie blendet nichts aus. Der Punkt ist allgemeiner und
      gilt ueber diesen Eintrag hinaus: „Kinderbox" heisst nicht
      „ausschliesslich Kinderinhalte" — wo gesiebt wird (Sammlungen nach
      Alter), behaelt der Erwachsene das letzte Wort, und wo es nichts zu
      sieben gibt, wird gar nicht erst gesiebt.
    related: ["ard-items-ohne-ton-und-die-falschen-zahlen", "browse-ist-pluginsache-und-die-einzelabfrage-log", "inhalt-im-vertrag-und-ard-delegiert"]

  - id: wortsack-findet-was-feldgleichheit-verfehlt
    title: "E85: Derselbe Podcast blieb getrennt, weil Titel und Interpret die Woerter TAUSCHTEN - nicht weil eines fehlte"
    kind: falle
    match: "podcast doppelt|zwei kacheln derselbe podcast|verschmilzt nicht|science cops|interpret passt nicht|gruppiereTreffer trennt"
    sev: mid
    tags: [verschmelzung, abgleich, medien, ard, spotify, e85, 2026]
    source: "Betreiberfund 22.08.2026, am Geraet gegen die ARD-Audiothek nachgemessen"
    symptom: >
      Ein Podcast liegt in zwei Diensten und bekommt zwei Kacheln, obwohl
      Titel und Bild fuer einen Menschen offensichtlich dasselbe zeigen. Die
      Verschmelzung greift nicht, und es sieht aus, als sei der Abgleich
      kaputt.
    cause: >
      `gruppiereTreffer` verlangte GLEICHHEIT des normalisierten Titels UND
      dass ein Interpret im anderen vorkommt. Beides scheitert, wenn zwei
      Dienste dieselbe Angabe in VERSCHIEDENE Felder schreiben. Gemessen:
      die ARD-Audiothek fuehrt die Sendung als Titel „Quarks Science Cops"
      bei der Anstalt „WDR", Spotify als Titel „Science Cops" beim Interpreten
      „Quarks". „quarks science cops" ist nicht „science cops", und „wdr"
      steckt nicht in „quarks" - zwei Absagen, obwohl es DIESELBEN WOERTER
      sind.
    fix: >
      `meinenDasselbe()` in medien.ts vergleicht seit E85 den WORTSACK aus
      Titel UND Interpret. Zwei Regeln, und es braucht beide: (1) der kleinere
      Wortsack muss GANZ im groesseren stecken - das trennt „Greatest
      Hits/Alpha" von „Greatest Hits/Beta", wo jede Seite ein eigenes Wort
      hat; (2) der kuerzere TITEL muss mindestens die Haelfte des laengeren
      decken - ohne sie risse Regel 1 die eigenstaendige WDR-Sendung „Quarks"
      mit, deren Sack {quarks, wdr} vollstaendig in {quarks, science, cops,
      wdr} steckt. Ein Titelwort von dreien sind 33 Prozent und damit zu wenig,
      „science cops" gegen „quarks science cops" kommt auf 67.
    body: |
      ══ WARUM NICHT DAS BILD, obwohl es der Betreiber daran erkannt hat ═════
      Es waere das naechstliegende Merkmal und ist das teuerste. Gemessen:
      die ARD liefert ein JPEG von api.ardmediathek.de (512 px, 79 397 Bytes),
      Spotify eines von i.scdn.co in anderer Groesse und mit anderem Encoder.
      „Gleich" heisst hier VISUELL gleich, nie byteweise - das braucht einen
      Wahrnehmungs-Hash und damit einen JPEG-Dekoder. Das Projekt hat KEINE
      Bildbibliothek (kein sharp, kein jimp), und Node bringt keinen mit. Der
      Wortsack loest denselben Fall ohne Netz, ohne Dekoder und ohne neue
      Abhaengigkeit.

      ══ DIE ALTE SPERRE GEGEN ARD IST DAMIT BEANTWORTET ════════════════════
      Der Kopf von verschmelzung.ts begruendete den ARD-Ausschluss unter
      anderem damit, „die ARD liefert kein Merkmal, an dem sie dasselbe waere".
      Dieser Fund widerspricht dem: bei SENDUNGEN gibt es eines, und es steht
      in den Woertern. Fuer ALBEN bleibt die Warnung des Plans
      [quellen-verschmelzen] unangetastet - Deluxe und Remaster tragen
      denselben Wortsack, und dagegen haelt weiterhin die Titelzahl.

      ══ ZWEI REIHENFOLGEN, DIE IN VERSCHIEDENE RICHTUNGEN ZEIGEN ═══════════
      Ebenfalls mit E85: `METADATEN_REIHENFOLGE` ist spotify -> jellyfin ->
      lokal und damit die UMKEHRUNG von `QUELLEN_REIHENFOLGE`. Das ist kein
      Widerspruch, sondern eine andere Frage. Die Abspiel-Reihenfolge sichert
      gegen AUSFALL ab (Spotify fiel an einem Tag dreifach aus). Beim
      BESCHRIFTEN gibt es keinen Ausfall - der Titel steht laengst in der
      Ablage -, und dort ist Spotify die gepflegteste Quelle, waehrend eine
      lokale Datei traegt, was ihr Tagger hinterliess, und die ARD den
      Anstaltsnamen als Interpret fuehrt.

      DIE IDENTITAET WANDERT NICHT MIT. `schluessel` bleibt beim fuehrenden
      Werk - an ihm haengen Verlauf, Weiterhoeren und Favoriten. Nur die
      Beschriftung wird zusammengesetzt, und das Bild zeigt danach auf den
      Schluessel des Mitglieds, dessen Cover gewonnen hat. Gewechselt wird
      aber NUR gegen ein Mitglied mit `hatBild` - sonst tauschte man ein
      vorhandenes Cover gegen den Anfangsbuchstaben der Kachel.

      ══ ES BLEIBT EINE VERMUTUNG ═══════════════════════════════════════════
      Der Plan [quellen-verschmelzen] verlangt unter Punkt e, dass ein Mensch
      „das ist dasselbe" und „das ist NICHT dasselbe" festschreiben kann, weil
      Automatik sich irrt. Diese Regel macht den Handbetrieb NICHT
      ueberfluessig - sie verschiebt nur, wie oft er gebraucht wird.
    related: ["quellen-verschmelzen", "verschmelzung-aufloesung-stufe1"]

  - id: abstufung-ohne-toten-schalter
    title: "E86: Der Abgleich ist abstufbar - und `bild` steht bewusst NICHT im Regelsatz, weil der Schalter nichts abschalten wuerde"
    kind: entscheidung
    match: "abgleich abstufung|verschmelzung zu locker|bildvergleich abschalten|regeln streng normal locker|schalter ohne wirkung"
    sev: low
    tags: [verschmelzung, abgleich, medien, konfiguration, e86, 2026]
    source: "Betreiberwunsch 22.08.2026 („vielleicht mit abstufungs moeglichkeit zum ausschalten von bild vergleich oder so\")"
    body: |
      DREI STUFEN, und jede ist eine Aussage ueber den Preis:

        streng  Wortsack AUS - nur gleiche Titel. Der Stand vor E85, fuer den,
                dem die Automatik zu viel ist.
        normal  Vorgabe. Wortsack an, beide Sicherungen an.
        locker  Titeldeckung auf 33 Prozent. Fasst dann auch „Quarks" und
                „Quarks Science Cops" zusammen - KEIN Versehen, sondern der
                Preis: wer sie waehlt, will lieber einmal zu viel
                zusammenfassen und von Hand trennen, als Paare zu uebersehen.

      ══ WAS NICHT DRINSTEHT, UND WARUM DAS DER EIGENTLICHE EINTRAG IST ══════
      Der Wunsch hiess ausdruecklich „zum Ausschalten von Bildvergleich". Ein
      `bild: false` waere trotzdem falsch gewesen: es gibt keinen
      Bildvergleich, den es abschalten koennte. Gemessen 22.08.2026 liefert
      die ARD ein JPEG von api.ardmediathek.de (512 px, 79 397 Bytes), Spotify
      eines von i.scdn.co in anderer Groesse und mit anderem Encoder - „gleich"
      heisst VISUELL gleich, nie byteweise, und das braucht einen
      Wahrnehmungs-Hash samt JPEG-Dekoder. Das Projekt hat keine
      Bildbibliothek, Node bringt keinen Dekoder mit.

      EIN SCHALTER, DER NICHTS SCHALTET, IST EINE ZUSAGE, DIE DIE BOX NICHT
      EINLOEST - dieselbe Sorte wie der Dienstschalter aus BACKLOG X13, der auf
      einer frisch bespielten Karte ins Leere zeigte und dessen Widerspruch
      („eingeschaltet false" bei „zustand active") niemand verstand. Deshalb
      stehen `bild` und `fingerabdruck` NUR in `STUFEN` (verschmelzung.ts) als
      moegliche HERKUNFT einer Zuordnung, nicht als Regelschalter. Wer sie
      baut, traegt sie nach; die Form steht.

      ══ DER UNTERSCHIED ZWISCHEN `laenge` UND `bild` ═══════════════════════
      `laenge` IST im Regelsatz, obwohl heute kein Aufrufer Dauern liefert -
      und das ist kein Widerspruch zum Absatz darueber. Sie ist eine Regel
      OHNE DATEN, kein Schalter ohne Wirkung: sie trennt, wenn BEIDE Seiten
      eine Dauer nennen und die auseinandergeht, und schweigt sonst - genau
      wie `titelzahl` schweigt, wenn eine Seite ihre Titelzahl verschweigt.
      Bei `bild` fehlt ein WERKZEUG, bei `laenge` fehlen DATEN. Das erste
      kann man nicht wegkonfigurieren, das zweite fuellt sich, sobald ein
      Dienst Dauern mitgibt.

      GEMESSEN, dass die Daten fehlen: `Werk` fuehrt keine Gesamtdauer, und
      `/api/medien` reicht die Eintraege durch, die auch keine tragen.
      `dauerMs` gibt es nur an einzelnen Titeln eigener Listen.

      ══ WO SIE EINGESTELLT WIRD, UND WARUM DORT ════════════════════════════
      `mupibox.abgleichStufe` in der mupiboxconfig, Feld `abgleichStufe` im
      Bereich „medien" der Verwaltung. NICHT in verschmelzung.json: jene Datei
      haelt fest, WAS entschieden wurde („zuordnungen") und was ausdruecklich
      nicht („getrennt"). Beides bleibt gueltig, wenn jemand strenger
      vergleichen will - nur die VORSCHLAEGE des naechsten Laufs fallen anders
      aus. Eine Einstellung in die Ablage zu schreiben hiesse, sie als Befund
      auszugeben.

      SIE AENDERT NICHTS VON SELBST: der Abgleich laeuft nur auf Knopfdruck,
      und `getrennt` schlaegt JEDE Stufe - auch `locker`. Ein Zeuge haelt das
      fest, sonst waere der Knopf „trennen" auf der falschen Stufe folgenlos.

      DIE ZWEI LISTEN MUESSEN DIESELBEN SEIN. Ein vierter Wert im Feld, den
      `istAbstufung()` nicht kennt, faellt im Server still auf „normal" zurueck
      - die Einstellung zeigte dann etwas an, das nirgends ankommt. Ein Zeuge
      in konfiguration.spec.ts vergleicht die Auswahlliste gegen
      `ABSTUFUNGEN`; dieselbe Vorsicht wie beim dritten Kiosk-Browser.

      ══ WIDERSPRUCH SCHLAEGT AEHNLICHKEIT ══════════════════════════════════
      Beide Sicherungen laufen VOR dem Titelvergleich, nicht danach. Sonst
      naehmen gleiche Titel den kurzen Weg ueber `interpretenPassen` und kaemen
      nie dort an - zwei Ausgaben mit verschiedener Titelzahl waeren
      verschmolzen, obwohl die Sicherung eingeschaltet ist. Ein Zeuge haelt
      das fest.
    related: ["wortsack-findet-was-feldgleichheit-verfehlt", "quellen-verschmelzen"]

  - kind: howto
    id: konfigfeld-zwei-namen-luefterkurve
    title: "Ein Konfigfeld hat zwei Namen - und die Luefterkurve stand in keinem Handbuch"
    tags: [konfiguration, mupiboxconfig, luefter, telegram, spotify, doku]
    body: |
      ZWEI NAMEN JE FELD, UND NUR EINER STEHT IM WIKI. Jedes Feld der
      Verwaltung fuehrt in `FELDER` (konfiguration.ts) eine deutsche `id`
      (`startLautstaerke`) und einen `pfad` in die mupiboxconfig, dessen
      letztes Glied englisch ist (`mupibox.startVolume`). Das Wiki ist aelter
      als die deutschen Ids und beschreibt fast alles unter dem
      JSON-Schluessel. Wer nach der Id sucht, findet nichts und haelt ein
      dokumentiertes Feld fuer eine Luecke: die erste Fassung der
      Feldpruefung in doku-luecken-probe.sh meldete so 22 Luecken, von denen
      16 keine waren. `pfad` IST die Uebersetzungstabelle - eine zweite
      braucht es nicht, aber wer suchen laesst, muss beide Namen suchen.

      ══ DIE SECHS, DIE WIRKLICH NIRGENDS STANDEN (22.08.2026) ══════════════
      `luefter25` -> `fan.fan_temp_25`, `luefter50` -> `fan.fan_temp_50`,
      `luefter75` -> `fan.fan_temp_75`, `luefter100` -> `fan.fan_temp_100`,
      gelesen von
      `scripts/fan/fan_control.py` (Zeilen 37-40, `int(...)`, denn die Vorlage
      legt sie als ZEICHENKETTEN ab: "45"/"55"/"65"/"75"). Vier Schwellen in
      Grad Celsius, ab denen der Luefter 25/50/75/100 % dreht; darunter steht
      er still. Der Pi drosselt sich selbst ab etwa 80 Grad - eine Kurve, die
      erst bei 85 anfaengt, ist wirkungslos.

      `telegramChat` -> `telegram.chatId`, gelesen von sechs Python-Skripten
      unter scripts/telegram/ und geprueft in server.ts. Eine Zahl, oft mit
      Minus davor (Gruppenchats). Ohne sie schweigt die Box, auch wenn Token
      und Schalter stimmen.

      `spotifyZwischenspeicher` -> `spotify.cachestate`, gelesen von
      `scripts/librespot/librespot-start.sh` (und der Vorlage env-librespot)
      als `jq -r`, dann `if ! $cachestate`. Es ist also ein ECHTER
      JSON-Boolean, keine Zeichenkette - anders als die Luefterschwellen
      danebenan. Aus = `no_audio_cache`, spart Platz auf der Karte und kostet
      Datenverkehr.

      ══ ZWEI WAECHTER, DIE MAN AM EINZELWERT NICHT SIEHT ═══════════════════
      `pruefeKonfig` verweigert eine Luefterkurve, die nicht ANSTEIGT (auch
      Gleichstand): stuende 50 % ueber 75 %, liefe der Luefter bei mittlerer
      Waerme voll und bei hoher gar nicht. Fehlt die Gruppe `fan` ganz, stoert
      das nicht - geprueft wird nur, was dasteht.

      Derselbe Gedanke beim Knopflicht: `shim.ledBrightnessMin` (gedimmt, wenn
      der Bildschirm aus ist) darf `ledBrightnessMax` nicht uebersteigen, sonst
      leuchtet der Knopf HELLER, sobald es dunkel wird. Beide Zahlen sind fuer
      sich gueltig; der Dreher faellt erst im dunklen Zimmer auf, wenn das
      Kind schon liegt.

      ══ WIE ES AUFFIEL ═════════════════════════════════════════════════════
      Nicht durch Lesen, sondern durch eine vierte Runde in
      `tools/doku-luecken-probe.sh`: `FELDER` ist eine Liste im Code, die
      Handbuecher sind Prosa - dieselbe Luecke wie bei den Plugins, eine Ebene
      hoeher. Die Probe zaehlt ab `export const FELDER`, NICHT ab
      Dateianfang: davor steht `BEREICHE` mit derselben `id:`-Schreibweise,
      und Bereichsnamen wie "ton" oder "medien" kommen in jedem Handbuch
      zufaellig vor - wer sie mitnimmt, faerbt die Probe gruen.

      SIE HAT EINE BEKANNTE SCHWAECHE: Schluessel, die generische Woerter
      sind (`telegram.active`, `spotify.aktiv`), treffen ueberall und melden
      nie. Das ist die vorsichtige Richtung - kein Fehlalarm, dafuer ein
      blinder Fleck. Wer sie schaerfen will, sucht den Pfad im Ganzen
      (`telegram.active`) statt nur das letzte Glied.
    related: ["volles-wiki-ist-kein-beleg-fuer-gepflegte-doku"]

  - id: plugin-inhalt-kommt-in-die-medienliste
    title: "E87: Ein Eintrag `type: plugin` traegt die volle Plugin-Medienkennung in `id` - damit spielt Plugin-Inhalt aus einer Kachel"
    kind: architektur
    match: "plugin kachel spielt nicht|kein media provider fuer|type plugin|plugin in medienliste|anderes statt show|generischer plugin weg"
    sev: high
    tags: [plugins, medien, box, architektur, e87, 2026]
    stand: "22.08.2026 gebaut; drei Schichten kompilieren, 2457/2474 Backend-Zeugen und 374/374 Box-Zeugen gruen. Die NAHT (Server-Zweig gegen echtes Plugin) hat noch keinen Zeugen."
    source: "Befund beim Bau von mixpi-archive: das Plugin konnte suchen, auflisten und aufloesen - und keine Kachel spielte."
    body: |
      ══ DIE FRAGE, DIE DAS HANDBUCH OFFEN LIESS ════════════════════════════
      „Wie kommt Plugin-Inhalt in die Medienliste?" - bis E87 gar nicht.
      `mixpi-ardsounds` kam nur durch, weil ARD im Kern EIGENE Dienste hat
      (ard.service.ts in der Box, ein fest verdrahteter Zweig in
      /api/werke/<s>/inhalt, der `ard`-Fall in artVon). Jedes weitere
      Medien-Plugin haette denselben Sonderweg gebraucht - genau die Bauart,
      die das Plugin-System abschaffen sollte.

      ══ DIE FORM ══════════════════════════════════════════════════════════
      Ein Eintrag traegt `type: "plugin"` und in `id` die VOLLE Medienkennung
      des Plugins:

          { "type": "plugin", "id": "mixpi-archive:faust1teil_1412_librivox" }

      Daraus baut medienSchluessel() ohne Zutun
      `plugin:mixpi-archive:faust…` - eine stabile Identitaet fuer Verlauf,
      Weiterhoeren und Favoriten.

      WARUM NICHT `type: "<plugin-kennung>"` DIREKT, was verlockender aussieht:
      `dienstVon()` ist eine REINE Funktion ohne Plugin-Register. Sie kann
      nicht entscheiden, ob „mixpi-archive" ein geladenes Plugin ist oder ein
      Tippfehler - beide sehen gleich aus. Sie muesste die Liste der geladenen
      Plugins hereingereicht bekommen und waere damit weder rein noch ohne Box
      pruefbar. Ein festes Wort davor kostet ein Feld und beantwortet die
      Frage syntaktisch.

      ══ DIE VIER STELLEN, DIE ES BRAUCHTE ══════════════════════════════════
        medien.ts     `dienstVon` kennt `plugin`; `pluginKennungAus()` liest
                      die Kennung und weist Halbes ab (ohne Doppelpunkt ist
                      es keine Medienkennung).
        werke.ts      `artVon` gibt `show` - eine Folge nach der anderen, wie
                      bei ARD. Bis E87 kam `anderes` heraus: eine Kachel, die
                      sich nicht oeffnen laesst.
        server.ts     ein dienstblinder Zweig in /api/werke/<s>/inhalt, der
                      pluginInhalt(<kennung>, <rest>) ruft und je Folge
                      `plugin/…` und `pluginqueue/…` baut.
        Box           plugin-inhalt.ts + .service.ts (Gegenstueck zu ard.ts),
                      eine Weiche in der iif-Kette von media.service.ts und
                      ein Anbieter in media-provider.ts, der den Befehl NUR
                      DURCHREICHT.
        Abspieldienst ein Verb `plugin` (Kopie von `ard`/`jellyfin`) und
                      `pluginqueue` im ohnehin dienstblinden Anhaeng-Zweig.

      EIN EIGENES VERB statt `jellyfin` mitzubenutzen, obwohl der Zweig
      identisch aussieht: `currentType` steht in jeder Auskunft des Dienstes
      (/state, Telegram, Wiedergabeleiste). „jellyfin" fuer einen
      Archive.org-Mitschnitt waere eine Falschaussage, die genau dann
      auffaellt, wenn jemand einem Fehler nachgeht.

      ══ DIE STELLE, AN DER ES SCHON EINMAL STILL SCHIEFGING ════════════════
      Die iif-Kette in media.service.ts fragt nach FELDERN, nicht nach dem
      TYP. Ein unbekannter Eintrag faellt bis ganz nach innen auf „Single
      album" durch - die Kachel erscheint, und beim Antippen findet
      player.service.ts keinen Abspielweg. Kein Fehler, keine Meldung. Genau
      so verschwand `type: 'ard'` bis zum 04.08.2026, und genau so war der
      Zustand JEDES Medien-Plugins bis E87.

      ══ DER NAHTZEUGE HAT SICH SOFORT BEZAHLT GEMACHT ══════════════════════
      Beim Schreiben von plugin-inhalt.integration.spec.ts fiel auf, dass
      `plugins/mixpi-archive` seine Folgen OHNE `kennung` zurueckgab. Das
      Plugin war gruen (22 eigene Zeugen), und `inhaltPruefen` haette jede
      einzelne Folge STILL uebergangen („Folge ohne Kennung uebergangen") —
      die Kachel haette eine leere Liste gezeigt, ohne dass irgendwo ein
      Fehler stand. Gefunden hat es kein Plugin-Zeuge, sondern der Zeuge
      gegen den ECHTEN Kern.

      DIE KENNUNG IST DER GRUPPENSCHLUESSEL, also der Name der QUELLDATEI
      (`faust1_01_goethe.mp3`), nicht der der gespielten Fassung: `original`
      zeigt von jeder Ableitung auf dieselbe Quelle. Naehme man den Namen der
      gespielten Fassung, aenderte sich die Kennung — und mit ihr Verlauf und
      gemerkte Stelle —, sobald das Archiv eine Fassung nachliefert.

      ══ UND EINEN UNERREICHBAREN RIEGEL AUFGEDECKT ═════════════════════════
      Im Server-Zweig standen zwei Formpruefungen hintereinander:
      `pluginKennungAus` und danach `kennungZerlegen`. Gemessen weist der
      zweite ALLE Formen ab, die auch der erste abweist — die Sabotage konnte
      den ersten entfernen, ohne dass ein Zeuge rot wurde. Er war
      unerreichbar und ist weg. `pluginKennungAus` bleibt als benannter
      LESER (es sagt, WO die Kennung steht), die FORM prueft der Zerleger.

      DAS IST DIE ZWEITE HAELFTE DESSEN, WOFUER GEGENPROBEN DA SIND: sie
      finden nicht nur blinde Zeugen, sondern auch toten Code — eine
      Sabotage, die gruen bleibt, sagt entweder „der Zeuge sieht nicht hin"
      oder „diese Zeile tut nichts".

      ══ WAS WEITERHIN FEHLT ════════════════════════════════════════════════
      Die GEMERKTE STELLE fuer Plugin-Inhalt. Sie braucht einen Eintrag in
      der Medienliste je FOLGE, und den gibt es nicht; `weiterAb` ist im
      Plugin-Zweig deshalb immer null. Und: der Server-Zweig hat noch KEINEN
      Zeugen gegen ein echtes Plugin - die Einzelteile sind gruen, die Naht
      dazwischen ist es, die am 22.08.2026 bei den Plugin-Flaechen (E77)
      schon einmal am Geraet scheiterte.
    related: ["plugins-melden-sich-an-statt-gekannt-zu-werden", "inhalt-im-vertrag-und-ard-delegiert", "browse-ist-pluginsache-und-die-einzelabfrage-log"]

  - id: plugin-kette-reisst-an-sechs-stellen
    title: "E88: Ein Medien-Plugin ist erst brauchbar, wenn SECHS Stellen passen - und jede kann fuer sich gruen sein"
    kind: howto
    match: "plugin kachel spielt nicht|vorschlag type|folge ohne kennung|plugin kette pruefen|neues medien plugin"
    sev: mid
    tags: [plugins, medien, werkzeug, e88, 2026]
    stand: "22.08.2026: mixpi-archive geht die Kette vollstaendig, gegen das echte Archiv gemessen"
    source: "Zwei Fehler am selben Tag, beide erst an der Naht gefunden"
    body: |
      DIE KETTE, DIE EIN MEDIEN-PLUGIN GEHEN MUSS:

        1. `http/suche` liefert Werke MIT `vorschlag`
        2. der Vorschlag traegt `type: "plugin"` (oder einen Kern-Dienst)
        3. `dienstVon()` erkennt ihn - sonst wird er `anderes`
        4. `medienSchluessel()` baut eine aufloesbare Identitaet
        5. `artVon()` gibt `show`, nicht `anderes`
        6. `inhalt()` liefert Folgen MIT `kennung` und mit Tonadresse

      JEDE DIESER STELLEN KANN FUER SICH GRUEN SEIN, waehrend die Kette
      reisst. Zweimal am 22.08.2026 passiert:

        * mixpi-archive lieferte Folgen OHNE `kennung`. Plugin gruen (22
          Zeugen), `inhaltPruefen` haette jede Folge STILL uebergangen.
        * Der Vorschlag trug `type: "mixpi-archive"`. `dienstVon()` machte
          daraus `anderes` - eine Kachel, die nicht spielt.

      Beide haetten in EINEM Lauf von `tools/plugin-kette-probe.mts` gestanden.

      VOR DER KETTE STEHT DIE FREMDE SCHNITTSTELLE: `tools/archive-probe.mjs`
      hat gegen das echte Internet Archive gemessen, was ueberhaupt zu holen
      ist — Suche ohne Anmeldung (advancedsearch.php, JSON), welche der oft
      dreissig Dateien eines Werks wirklich Ton sind (die Haelfte sind Bilder,
      Pruefsummen, XML), und ob das fuer eine geordnete Folgenliste reicht.
      Wer ein weiteres Medien-Plugin baut, misst die Quelle GENAUSO zuerst;
      sonst verhandelt Stelle 6 mit einer Annahme.

      ══ ZWEI WEGE SIND RICHTIG, DER DRITTE NICHT ═══════════════════════════
        GENERISCH  `type: "plugin"` - der Weg seit E87, fuer jedes NEUE
                   Medien-Plugin.
        KERN       `type: "ard"|"spotify"|"jellyfin"|"radio"|"rss"|"library"`
                   - Dienste mit eigenem Weg im Kern. mixpi-ardsounds liefert
                   `type: "ard"`, und das ist KORREKT: die ARD-Dienste koennen
                   mehr als der generische Weg (Reihenfolge, Vorspann,
                   gemerkte Folge).
        FALSCH     alles andere - wird `anderes`, Kachel spielt nicht.

      Das Werkzeug unterscheidet die drei. Ein Pruefer, der den Kern-Weg als
      Fehler meldet, wird zu Recht ignoriert - beim ersten Lauf tat er genau
      das und wurde korrigiert.

      ══ ZWEI EIGENHEITEN DES WERKZEUGS, die man wissen muss ════════════════
      `.mts` UND NICHT `.ts`: das Verzeichnis hat kein `"type": "module"`,
      esbuild uebersetzt `.ts` nach CJS, und dort ist Top-Level-`await` ein
      Uebersetzungsfehler - der Lauf bricht ab, bevor eine Zeile laeuft.

      DER FELDNAME DER TREFFERLISTE IST NICHT VERTRAGLICH. `http()` ist eine
      FREIE Flaeche (E77); mixpi-archive nennt seine Liste `werke`,
      mixpi-ardsounds `sendungen`. Das Werkzeug sucht deshalb die erste Liste,
      deren Eintraege einen `vorschlag` tragen, statt einen Namen zu erzwingen.
    related: ["plugin-inhalt-kommt-in-die-medienliste", "plugins-melden-sich-an-statt-gekannt-zu-werden"]
  - id: mitschnitt-stummel-kachel-vor-inhalt
    title: "Eine lokale Aufnahme spielt nicht, weil fast nichts drin ist - die Kachel entsteht nach dem ERSTEN Stueck, nicht am Ende"
    kind: falle
    match: "lokale aufnahme spielt nicht|kein cover bild|album faengt bei 2 an|mitschnitt unvollstaendig|kachel ohne inhalt|stummel"
    sev: high
    tags: [mitschnitt, medien, lokal, aufnahme, 2026]
    source: "Betreiberfund 22.08.2026 („101 Meerjungfrauen\", „101 Dinos\"), am Geraet .62 nachgemessen"
    symptom: >
      Eine lokale Kachel tut beim Antippen scheinbar nichts, oder sie zeigt
      kein Bild. Andere Kacheln derselben Reihe gehen. Die Daten sehen heil
      aus - und sind es auch.
    cause: >
      Der Mitschnitt ist abgebrochen. `mixpi-mitschnitt` schreibt Playlist UND
      Kachel nach JEDEM einzelnen gelungenen Stueck (playlistSchreiben +
      kachelAnlegen), nicht am Ende des Albums. Das ist eine Entscheidung fuer
      „sofort hoerbar\" - und ihr Preis zeigt sich erst am Regal.
    fix: >
      `python3 tools/mitschnitt-stummel.py --box <box>` nennt alle Faelle,
      aendert nichts. Danach von Hand entscheiden - und NICHT waehrend ein
      Mitschnitt laeuft.
    body: |
      ══ GEMESSEN AUF .62 (22.08.2026) ══════════════════════════════════════
      29 Alben auf der Platte, 18 mit Befund. Die drei genannten:

        101 Meerjungfrauen  ~25 Sekunden  - es SPIELT, nur ist dann Schluss
        101 Feen            ~16 Sekunden, kein cover.jpg, KEINE Kachel
        101 Dinos           faengt bei „02 Kapitel 2\" an, Kapitel 1 fehlt

      Dazu „Die 30 besten Schlaflieder\": gar kein spielbarer Ton im Ordner.

      DIE DATEN SIND NICHT KAPUTT: FLAC-Koepfe gueltig (`fLaC`), playlist.m3u
      richtig, /api/bild liefert 200 mit einem echten JPEG. Wer dort sucht,
      sucht falsch.

      ══ DREI SYMPTOME, EINE URSACHE ════════════════════════════════════════
        bricht nach Stueck 1 ab        -> Kachel mit einem kurzen Stueck
        Stueck 1 wird verworfen        -> Album faengt bei 2 an; fuer ein Kind
           (zu leise/zu klein, pegelMessen)  sieht das aus wie „geht nicht\"
        bricht VOR dem ersten ab       -> Dateien ohne Kachel (Waise)

      ══ WARUM EIN ZUSATZFELD AM EINTRAG NICHT REICHT ═══════════════════════
      Die naheliegende Abhilfe waere, die Kachel „unvollstaendig\" zu
      beschriften. Das geht heute NICHT ohne Vertragsaenderung: `neuerEintrag`
      (medien.ts) ist eine strenge WEISSLISTE - `type`, `category`, `title`,
      `artist` und sechs Kennungsfelder. Alles andere faellt still heraus,
      dieselbe Klasse wie bei [fundpruefen-baut-den-fund-neu-zusatzfelder-fallen-still].
    related: ["fundpruefen-baut-den-fund-neu-zusatzfelder-fallen-still"]

  - id: menuelose-seiten-stehen-in-keiner-doku-wache
    title: "Vier Verwaltungsseiten haben keinen Leisteneintrag - und eine Wache, die die Leiste liest, kann sie nie vermissen"
    kind: falle
    match: "seite fehlt in doku|verschmelzung|interpreten|doppelte seite|menuelose seite|doku wache blind|app.routes gegen rahmen"
    sev: mid
    tags: [doku, verwaltung, wache, routen, 2026]
    source: "Doku-Lauf 22.08.2026, tools/doku-luecken-probe.sh erweitert"
    symptom: >
      Die Doku-Luecken-Probe meldet gruen, und dokumentation/mixpibox.md
      enthaelt trotzdem NULL Treffer fuer zwei vollwertige Verwaltungsseiten.
      Dieselbe Datei behauptet zwei Zeilen darueber „21 Seiten": sie ZAEHLT
      die Seiten, ohne sie zu kennen.
    ursache: >
      Die Pruefung „Verwaltungsseiten, die das Benutzerhandbuch nicht nennt"
      liest ihre Liste aus `rahmen.ts` - also aus der KOPFLEISTE. Eine Seite
      ohne Leisteneintrag steht in dieser Liste nicht und kann darum auch
      nicht als fehlend auffallen. Die Wache ist gegen ihre eigene Quelle
      vollstaendig und gegen den Baum blind.

      `rahmen.ts` fuehrt 17 Eintraege, `app.routes.ts` 21 Seiten. Die
      Differenz sind vier: die Uebersicht (leerer Pfad), die Plugin-
      Unterseite (`/plugins/:kennung`) - und zwei echte Seiten mit eigener
      Ablage, die nur ueber einen Link auf der Medienseite erreichbar sind:

        /verschmelzung   „Doppelte"      config/verschmelzung.json
        /interpreten     „Interpreten"   config/interpreten.json

      Beide sind seit 03.08.2026 Unterseiten von „Medien"; `rahmen.ts:51`
      bildet sie fuer die Markierung auf `/medien` ab, weil der Pfad nicht
      mit `/medien` anfaengt. Keine von beiden ruehrt die Bibliothek an - sie
      schreiben eine Liste DANEBEN, und die Datei zu loeschen heisst wirklich
      „wie vorher". Genau deshalb sind es eigene Seiten und keine Karten auf
      „Medien": die Karte darueber LOESCHT Eintraege.

      Das Pack kannte beide laengst (verschmelzung.json 6, interpreten.json 9
      Treffer), das Handbuch nicht - der vierte Fall von
      [volles-wiki-ist-kein-beleg-fuer-gepflegte-doku].
    abhilfe: >
      `tools/doku-luecken-probe.sh` prueft seit 22.08.2026 zusaetzlich die
      Seitentitel aus `app.routes.ts` gegen BEIDE Handbuecher.

      ZWEI Dinge daran sind nicht beliebig, und ohne sie ist die Pruefung
      wertlos:

      GEZAEHLT WIRD DER TITEL, NICHT DER PFAD. Die Handbuecher schreiben
      „Doppelte", nie `/verschmelzung`. Eine Probe auf den Pfad meldet rot,
      obwohl die Seite dasteht.

      GEPRUEFT WIRD NUR, WAS KEINEN LEISTENEINTRAG HAT. Verlangte die Probe
      jede der 17 Menueseiten auch im ARCHITEKTURhandbuch, meldete sie auf
      Dauer rot („Systemdienste", „Profile", „Netzwerk" - Namen, die dort
      nichts zu suchen haben) und waere nach der zweiten Woche Rauschen.
      Dieselbe Falle wie ein Benutzerhandbuch, dem man `RECHTE` abverlangt,
      nur andersherum. Die erste Fassung machte genau diesen Fehler und
      meldete sofort 7 Luecken, von denen 5 keine waren.

      Gegengeprobt: mit entfernten Woertern meldet sie „FEHLT ... Doppelte /
      Interpreten", mit der ergaenzten Fassung gruen.

      MERKSATZ: wer die Seiten der Verwaltung zaehlt oder aufzaehlt, zaehle
      `app.routes.ts`, nicht die Kopfleiste. Und eine Wache, deren Liste aus
      derselben Datei stammt wie ihr Pruefgegenstand, ist tautologisch - sie
      kann nur finden, was ohnehin schon gefuehrt wird.
    body: |
      „DOKU WACHE BLIND" FUEHRT ZU DREI EINTRAEGEN, und sie meinen drei
      verschiedene blinde Flecken derselben Probe. Die Frage, die trennt:
      WAS hat die gruene Probe nicht gefragt?

          Seiten ohne Leisteneintrag        -> DIESER Eintrag
          was auf der Box LAEUFT (Units)    ->
                        [[doku-wachen-pruefen-die-anwendung-nicht-die-box]]
          WO der Bestand liegt (Dateien)    ->
                        [[ablage-stand-in-keiner-doku-wache]]

      Gemeinsam ist allen dreien die Bauart, nicht der Gegenstand: eine
      Wache, deren Liste aus derselben Quelle stammt wie ihr Pruefgegenstand,
      kann den Fehlenden nicht vermissen. Wer nur einen der drei liest, haelt
      den eigenen blinden Fleck fuer den einzigen.
    related: ["volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "erzeugte-suchliste-von-hand-ergaenzt", "doku-wachen-pruefen-die-anwendung-nicht-die-box", "ablage-stand-in-keiner-doku-wache"]

  - id: schnittstelle-stand-in-keiner-doku-probe
    kind: falle
    title: "Die REST-Schnittstelle stand in keiner Doku-Probe - 195 Endpunkte, sieben genannt"
    match: "endpunkt nicht dokumentiert|api doku fehlt|welcher endpunkt|rest schnittstelle mixpibox|doku-luecken-probe gruen"
    sev: mid
    tags: [doku, api, probe, drift]
    stand: "gemessen am Repo, 23.08.2026 (MixPiBox, HEAD c28af145)"
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════

      `tools/doku-luecken-probe.sh` meldete gruen - sechs Pruefungen, keine
      Luecke. Alle sechs pruefen aber dasselbe Muster: etwas, das ein MENSCH
      in der Oberflaeche SIEHT. Plugins, Rechte, Ereignisse, Konfigurations-
      felder, Menuepunkte, Seitentitel.

      Die REST-Schnittstelle stand in keiner davon:

          Endpunkte in src/backend-api/src/*.ts       195
          davon in dokumentation/mixpibox.md            7   (alle /api/stroeme)
          davon in dokumentation/benutzerhandbuch.html  0
          in KEINER der vier Dokus genannt             83

      Die sieben genannten stehen dort nicht, weil jemand die Schnittstelle
      dokumentiert haette, sondern weil bei den Stroemen die REGEL erklaerungs-
      beduerftig war (Kapitel 4.9). Ein eigenes Kapitel zur Schnittstelle gab
      es nicht.

      WARUM DAS SCHWERER WIEGT ALS EIN FEHLENDER MENUEPUNKT: wer die
      Verwaltung aufmacht, findet eine Seite auch ohne Handbuch. Wer ein
      Skript, ein Plugin oder ein LLM auf die Box loesst, hat NUR die Liste.
      Ungenannt waren u.a. die ganze Tonkette (13 Endpunkte unter `/api/ton`),
      die Profile (7 unter `/api/profil`), das Netz (5 unter `/api/netzwerk`),
      die Spotify-Durchreiche (14) und `/api/plugins/:kennung/*` - letzteres
      ausgerechnet im PLUGIN-Handbuch.

      ══ DIE FALLE IN DER PROBE SELBST ══════════════════════════════════════

      Express schreibt Platzhalter (`/api/plugins/:kennung/einstellungen`),
      die Handbuecher schreiben BEISPIELE
      (`/api/plugins/mupibox-wled/einstellungen`). Ein wortwoertlicher
      Vergleich meldet das als Luecke. Von 93 zunaechst gemeldeten Luecken
      waren 10 genau dieser Art. `tools/api-doku-deckung.sh` weitet darum
      jedes `:xyz` zu `[^/]+` auf und sucht mit `grep -E`.

      ══ UND DIE FALLE IM SCHLIESSEN ════════════════════════════════════════

      Die erste Fassung des neuen Kapitels kuerzte Tabellenzeilen ab:
      `POST /api/verschmelzung/abgleichen`, `…/verbinden`, `…/trennen`. Die
      Probe fiel sofort auf 21 ungenannte zurueck - zu Recht: wer nach
      `/api/verschmelzung/trennen` sucht, findet ein `…/trennen` nicht. In
      einer NACHSCHLAGE-Liste ist die Abkuerzung kein Stil, sondern ein
      Fehler. Alle Pfade stehen jetzt voll da.
    abhilfe: >
      `dokumentation/mixpibox.md` hat seit 23.08.2026 Kapitel 4.10 „Die
      REST-Schnittstelle": alle 195 Endpunkte, nach Sachthema gruppiert, je
      Gruppe die Quelldatei.

      `tools/api-doku-deckung.sh` misst nach (0 = alle genannt) und laeuft am
      Ende von `tools/doku-luecken-probe.sh` mit, damit ein Lauf reicht.

      EIN TREFFER IN EINER DER VIER DOKUS REICHT, anders als bei den Rechten
      in der grossen Probe. Ein Endpunkt ist kein Kapitel: wo er steht, ist
      zweitrangig, solange er ueberhaupt irgendwo steht. Wer je Datei getrennt
      pruefte, verlangte `/api/mupihat/batterie` auch vom Benutzerhandbuch und
      meldete auf Dauer rot - dieselbe Rauschfalle wie bei den Seitentiteln.

      Gegengeprobt: ein einzelner verfremdeter Pfad im Handbuch
      (`/api/ton/klangwerk/live` -> `…/LIVE-WEG`) laesst die Probe mit
      Rueckgabe 1 und genau diesem Namen melden; zurueckgesetzt wieder gruen.

      MERKSATZ: eine Doku-Probe, die nur prueft, was man SIEHT, ist blind fuer
      alles, was man RUFT. Bei jeder neuen Wache fragen, ob ihr Gegenstand die
      Oberflaeche ist oder die Schnittstelle - es sind zwei.
    related: ["volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "erzeugte-suchliste-von-hand-ergaenzt"]

  - id: doku-wachen-pruefen-die-anwendung-nicht-die-box
    title: "Sieben Wachen prueften die Anwendung - was auf der Box LAEUFT, stand in keiner"
    kind: falle
    match: "dienst fehlt in doku|systemd doku|laufende dienste|mupi_offtrigger|unit nicht dokumentiert|ausschalter nicht dokumentiert|doku wache blind"
    sev: mid
    tags: [doku, systemd, dienste, wache, betrieb, 2026]
    source: "Doku-Lauf 23.08.2026, tools/dienste-doku-deckung.sh neu"
    symptom: >
      tools/doku-luecken-probe.sh meldet KEINE LUECKE - und
      dokumentation/mixpibox.md fuehrt unter der Ueberschrift 4.1 „Die
      laufenden Dienste" eine Tabelle mit SIEBEN Units, waehrend die
      Ausrollwege ZWANZIG mit `systemctl enable` scharf schalten.

      Die Tabelle nennt sich „Auf einer eingerichteten Box:" und behauptet
      damit Vollstaendigkeit. Nicht darin standen unter anderem die
      Kioskwache, die Standwache, das Bluetooth-Nachverbinden, die
      Luefter- und Knopflichtsteuerung, die Boot-Animation, die
      Wiederherstellung von der Karte - und `mupi_offtrigger`.
    ursache: >
      ══ DIE WACHEN HATTEN ALLE DENSELBEN GEGENSTAND ════════════════════════

      Die Probe war bis dahin auf sieben Pruefungen gewachsen: Plugins,
      Rechte, Ereignisse, Konfigurationsfelder, Menuepunkte, Seitentitel,
      REST-Endpunkte. Am 23.08.2026 kam die REST-Pruefung dazu, ausdruecklich
      mit dem Merksatz „wer nur prueft, was man SIEHT, ist blind fuer alles,
      was man RUFT" - siehe [schnittstelle-stand-in-keiner-doku-probe].

      Der Merksatz war richtig und trotzdem zu kurz. Oberflaeche UND
      Schnittstelle sind beide die ANWENDUNG. Was auf der Box als DIENST
      laeuft, ist eine dritte Sache, und sie hatte keine Wache - obwohl das
      Handbuch ein eigenes Kapitel dafuer fuehrt.

      ══ DER TEURE FALL: mupi_offtrigger ════════════════════════════════════

      Der weiche Ausschalter am Knopf (GPIO17, 2 s Druck) stand in KEINEM
      Handbuch und mit NULL Treffern im Wiki - waehrend `off_trigger.sh`
      selbst dort ein Dutzend Mal vorkommt. Das Wiki kannte das SKRIPT und
      seine Geschichte; die UNIT, die es seit dem 07.08.2026 startet, kannte
      es nicht.

      Das ist ausgerechnet der Dienst, dessen Fehlen ein Jahr lang niemand
      gemerkt hat: `autosetup.sh` legte das Skript nach
      /var/lib/dietpi/postboot.d/, wo es nie ankam - und wo es ohnehin nicht
      haette laufen koennen, weil DietPi die Skripte dort NACHEINANDER
      abarbeitet und auf ihr Ende wartet, das Skript aber nie endet
      (`while true`). Bis dahin wirkte nur der 6-Sekunden-Hardware-Schnitt
      der Platine, mitten in laufende Schreibvorgaenge hinein.

      Ein Dienst, der genau deshalb eine Unit bekam, damit sein Ausbleiben
      nicht mehr still ist, war anschliessend in der Doku still.

      ══ WARUM config/services/ NICHT DER MASSSTAB IST ══════════════════════

      Dort liegen 34 .service-Dateien. Ein guter Teil ist Bestand, der auf
      keiner heutigen Box scharf ist (`mupi_vnc`, `mupi_novnc`,
      `mupi_telegram`, der alte `mupi_startstop`). Eine Wache, die fuer jede
      DATEI einen Handbucheintrag verlangt, meldet auf Dauer rot und wird zu
      Recht ignoriert - dieselbe Rauschfalle, die in doku-luecken-probe.sh
      schon zweimal beschrieben steht.

      Der Massstab ist stattdessen: jede Unit, die ein AUSROLLWEG mit
      `systemctl enable` scharf schaltet. Das ist die woertliche Antwort auf
      „was laeuft auf einer eingerichteten Box" - und genau das ist die
      Ueberschrift der Tabelle, die geprueft wird. Die Liste wandert mit,
      wenn ein Ausrollweg sich aendert.

      ══ ZWEI KLEINIGKEITEN, DIE DIE PROBE BRAUCHT ══════════════════════════

      GESUCHT WIRD DER BASISNAME OHNE ENDUNG. Dieselbe Sache heisst mal
      `librespot-waechter.timer`, mal `.service`, mal gar nichts; wer auf den
      vollen Namen prueft, meldet rot, obwohl die Zeile dasteht.

      NUR DAS ARCHITEKTURHANDBUCH, nicht das Benutzerhandbuch.
      `pipewire.socket` gehoert in kein Handbuch fuer Betreiber - die
      Rauschfalle von oben, nur andersherum.
    abhilfe: >
      `tools/dienste-doku-deckung.sh` (neu, 23.08.2026) liest die
      `enable`-Aufrufe aus den fuenf Ausrollwegen und haelt sie gegen
      dokumentation/mixpibox.md. Sie laeuft am Ende von
      tools/doku-luecken-probe.sh mit, damit ein Lauf reicht.

      Sie unterscheidet dabei zwei Befunde, was am Ergebnis nichts aendert,
      aber sagt, welche Arbeit ansteht: „das Wiki kennt sie - nur nicht
      uebertragen" (11 Faelle) gegen „auch im Wiki nicht - echtes Loch"
      (1 Fall, `mupi_offtrigger`).

      Kapitel 4.1 fuehrt jetzt alle 20 Units, nach Aufgabe gruppiert
      (Anwendung / Wachen mit ihrem Takt / Hardware am GPIO / Start, Ton und
      Wiederherstellung) statt in einer flachen Tabelle.

      ZWEIMAL GEGENGEPROBT: eine gedeckte Unit aus dem Handbuch entfernt
      (`mupibox-touch-bridge`) -> genau diese wird gemeldet; einen Ausrollweg
      weggenommen -> WARNUNG statt stiller Kuerzung der Liste. Danach beides
      zurueckgesetzt, `git diff --stat` leer.

      ZWEI BEHAUPTUNGEN BEIM SCHREIBEN VERWORFEN, weil sie geraten waren: der
      Port von `dietpi-dashboard` (die Unit setzt keinen - der 5252-Treffer im
      Wiki gehoert zu server.ts) und ein Verweis „Luefterkurve siehe 4.x" (die
      Kurve steht gar nicht im Handbuch, sondern nur im Wiki unter
      [konfigfeld-zwei-namen-luefterkurve] - die Feld-Pruefung laesst einen
      Wiki-Treffer als Beleg gelten). Beide ersetzt durch das, was nachmessbar
      ist.

      MERKSATZ: bei jeder neuen Doku-Wache fragen, welche der DREI Ebenen ihr
      Gegenstand ist - die Oberflaeche, die Schnittstelle, oder das, was auf
      der Box laeuft. Der Merksatz von heute Morgen kannte erst zwei.
    body: |
      ZWEITER VON DREI EINTRAEGEN AUF „DOKU WACHE BLIND". Alle drei melden
      eine gruene Probe mit einem Loch daneben; verschieden ist, WAS die
      Probe nicht gefragt hat:

          was auf der Box LAEUFT (Units)  -> DIESER Eintrag
          Seiten ohne Leisteneintrag      ->
                        [[menuelose-seiten-stehen-in-keiner-doku-wache]]
          WO der Bestand liegt (Dateien)  ->
                        [[ablage-stand-in-keiner-doku-wache]]

      Der Merksatz unten nennt drei Ebenen — die Ablage ist die vierte, und
      sie wurde erst am Tag darauf gefunden.
    related: ["schnittstelle-stand-in-keiner-doku-probe", "menuelose-seiten-stehen-in-keiner-doku-wache", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "gegenprobe-statt-gruen-glauben", "konfigfeld-zwei-namen-luefterkurve", "ablage-stand-in-keiner-doku-wache"]
  - id: ablage-stand-in-keiner-doku-wache
    title: "Neun Wachen, und keine fragte, WO der Bestand liegt"
    kind: falle
    match: "ablage fehlt in doku|server/config nicht dokumentiert|data.json doku|profile.json doku|welche json dateien|wo liegt der bestand|doku wache blind"
    sev: mid
    tags: [doku, ablage, sicherung, wache, betrieb, 2026]
    source: "Doku-Lauf 23.08.2026, tools/ablage-doku-deckung.sh neu"
    symptom: >
      tools/doku-luecken-probe.sh meldet KEINE LUECKE - und in
      dokumentation/mixpibox.md steht nirgends, WO der Bestand der Box liegt.

      Gemessen: von 34 Aufnahmen der Sicherung standen 27 in KEINEM der
      beiden Handbuecher. Darunter die drei teuersten:

        data.json     die Bibliothek - das IST die Box      Wiki: 102 Treffer
        profile.json  das Verzeichnis der Kinder            Wiki:  17
        resume.json   die gemerkten Stellen                 Wiki: 113

      Die Box hat KEINE DATENBANK. Ihr ganzer Bestand liegt als rund dreissig
      JSON-Dateien unter `server/config` und `/etc/mupibox`. Wer das Handbuch
      liest, erfaehrt davon nichts - er findet 195 Endpunkte, 20 Dienste, 21
      Seiten und keinen Satz darueber, was passiert, wenn eine dieser Dateien
      fehlt.
    ursache: >
      ══ ALLE NEUN WACHEN GINGEN VON EINEM EINGANG AUS ══════════════════════

      Der Merksatz vom 23.08.2026 ("bei jeder neuen Wache fragen, welche der
      DREI Ebenen ihr Gegenstand ist - Oberflaeche, Schnittstelle, oder was
      auf der Box laeuft") war richtig und immer noch zu kurz. Alle drei
      Ebenen beschreiben, wie man die Box ANSPRICHT: mit dem Finger, per HTTP,
      per systemd. Keine beschreibt, was dabei ENTSTEHT und liegen bleibt.

      Es ist der VIERTE Fall von [volles-wiki-ist-kein-beleg-fuer-gepflegte-doku]
      in derselben Probe. Das Wiki kannte die Ablagen laengst; uebertragen hat
      sie niemand, weil keine Wache danach fragte.

      ══ WORAUS DIE LISTE KOMMT, UND WORAUS NICHT ═══════════════════════════

      `ls server/config` geht NICHT: das Verzeichnis gibt es im Repo gar
      nicht, es entsteht erst auf der Box.

      `grep '\.json'` ueber den Quelltext geht auch nicht - es faengt
      Testbeispiele mit ein. `ip.json` und `neuartige-ablage.json` sahen in
      der ersten Messung wie Ablagen aus und stehen ausschliesslich in
      `*.spec.ts` bzw. in Sicherungs-Testfixtures. Eine Wache mit zwei
      erfundenen Eintraegen wird beim ersten Lauf zu Recht ignoriert.

      DIE MASSGEBLICHE LISTE IST `HINEIN` in
      scripts/mupibox/mupibox-sicherung.py. Sie ist von Hand gefuehrt, jede
      Zeile begruendet, und sie ist die einzige Stelle, die entscheidet, was
      einen Kartenschaden ueberlebt. Was dort steht, ist Bestand - und was
      Bestand ist, gehoert ins Handbuch. Damit deckt sich der Massstab der
      Doku-Wache mit dem der Sicherung, und die beiden koennen nicht
      auseinanderlaufen.

      ══ DIE FALLE BEIM EXTRAHIEREN ════════════════════════════════════════

      KOMMENTARE ZUERST WEG. In den Begruendungen von `HINEIN` stehen Saetze
      in Anfuehrungszeichen ("nicht eingeordnet"). Eine Extraktion ueber alle
      `"..."` haelt einen halben Absatz fuer einen Dateinamen - die erste
      Fassung meldete 40 Eintraege, von denen sechs Fliesstext waren.

      DAS fnmatch-MUSTER IST NICHT DER SUCHBEGRIFF. Kein Handbuch schreibt
      `profile/*/resume.json`; es schreibt `resume.json` und sagt daneben,
      dass die Datei je Kind liegt. Aus `profile/*/verlauf.json` wird darum
      `verlauf.json`, aus `gespielt.*.json` ebenfalls `gespielt.json`, aus
      `plugin-daten/*` wird `plugin-daten`. Dass zwei Muster auf denselben
      Namen fallen, ist gewollt: die flache Form (E18/S1) und die Form je
      Kind sind dieselbe Ablage.
    abhilfe: >
      `tools/ablage-doku-deckung.sh` (neu, 23.08.2026) leitet die Liste aus
      `HINEIN` ab, fuehrt die Muster auf den blossen Namen zurueck und haelt
      sie gegen dokumentation/mixpibox.md. Sie laeuft am Ende von
      tools/doku-luecken-probe.sh mit, damit ein Lauf reicht. 29 Namen, beim
      ersten Lauf 22 rot.

      NUR DAS ARCHITEKTURHANDBUCH, nicht das Benutzerhandbuch. Ein Betreiber
      muss `offline_resume.json` nicht kennen; eine Wache, die den Namen dort
      einfordert, meldet ewig rot. Dieselbe Rauschfalle wie `RECHTE` im
      Benutzerhandbuch, nur eine Ebene tiefer.

      Neues Kapitel 4.11 „Die Ablage: wo der Bestand der Box liegt" - die
      zwei Baeume mit ihren Wurzeln auf der Box, eine Tabelle box-weit, eine
      je Kind, was ausdruecklich draussen bleibt, und der Verweis darauf, dass
      die massgebliche Liste NICHT im Handbuch steht, sondern in der
      Sicherung. Ein Handbuch, das die Liste ein zweites Mal fuehrt, ist die
      zweite Wahrheit, gegen die die Kopfzeile des Dokuments schreibt.

      ZWEIMAL GEGENGEPROBT: eine erfundene Ablage in `HINEIN` eingetragen ->
      genau diese gemeldet; `HINEIN` in `HINEINX` umbenannt -> WARNUNG statt
      still leerer Liste. Danach beides zurueckgesetzt,
      `git diff --stat` leer.

      ══ EIN ZWEITER FUND NEBENBEI ═════════════════════════════════════════

      Die Kopfzeile von dokumentation/mixpibox.md nannte das Wissenspaket mit
      „743 Eintraege, Fassung 248". Gemessen: 780 Eintraege, Fassung 291. Die
      Zahlen standen in keiner Wache und waren um 37 Eintraege und 43
      Fassungen alt.

      MERKSATZ: eine Doku-Wache fragt bisher immer, wie man die Box
      ANSPRICHT. Die naechste Frage ist, was dabei ENTSTEHT - und wo es
      liegt, wenn der Strom weg ist.
    body: |
      DRITTER VON DREI EINTRAEGEN AUF „DOKU WACHE BLIND". Getrennt wird nach
      dem Gegenstand, den die gruene Probe nicht kannte:

          WO der Bestand liegt (Dateien)  -> DIESER Eintrag
          was auf der Box LAEUFT (Units)  ->
                        [[doku-wachen-pruefen-die-anwendung-nicht-die-box]]
          Seiten ohne Leisteneintrag      ->
                        [[menuelose-seiten-stehen-in-keiner-doku-wache]]

      Wer nur hier liest, ergaenzt die Ablagen und haelt die Probe danach fuer
      vollstaendig — die anderen beiden Fluchten bleiben offen.
    related: ["doku-wachen-pruefen-die-anwendung-nicht-die-box", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "gegenprobe-statt-gruen-glauben", "wiki-zustand-hat-haltbarkeit", "menuelose-seiten-stehen-in-keiner-doku-wache"]
  - kind: howto
    id: audit-2026-08-23-kritiker
    title: "Kritiker-Lauf 23.08.2026 — Typ-Drift dreifach beim Medien-Eintrag, Token-Refresh dreifach, totes Spotify-SDK in der Box, tools/ ohne SSH-Schicht"
    source: "AUDIT-2026-08-23.md im Fork-Repo; 6 unabhaengige Pruefer (Backlog+Doku, Wiki, Frontends, Backend+Skripte, tools-Inventur, Internet) + Detail-Lauf frontend-box, HEAD 43b68b5f. Alles Quelltext-Befunde, nichts am Geraet gemessen."
    tags: [audit, typen, spotify, tools, abhaengigkeiten, 2026]
    body: |
      METABEFUND ZUERST: die Ranglisten vom 21./22.08. sind weiterhin
      unumgesetzt (stichprobenhaft gegengelesen: pruefen.sh:817-829 prueft
      weiter den ausgebauten PHP-Baum). Dieser Lauf wiederholt sie nicht,
      stellt sie aber in der Rangliste vor alles Neue.

      DIE NEUEN KERN-BEFUNDE (Volltext und Belege: AUDIT-2026-08-23.md):

      1. TYP-DRIFT: der Medien-Eintrag (data.json-Vertrag) ist DREIFACH
         definiert (box media.ts:22, admin seiten/medien.ts:25, backend
         verstreut); MupiboxConfig existiert ZWEIMAL UNTER GLEICHEM NAMEN
         mit disjunkten Feldern (box vs. backend-api models/). Naht: ein
         shared-types-Workspace — die Root-package.json hat workspaces
         ["src/*"] BEREITS, es fehlt nur das Paket. NewDesign bleibt
         aussen vor (kein Bundler — die bekannte Grenze, kein Versaeumnis).

      2. SPOTIFY-TOKEN-REFRESH DREIFACH: spotify-control.ts:551-620 (liest
         dazu die Config-Datei des ANDEREN Dienstes, :587) plus drei eigene
         fetch-Bloecke in server.ts:16321/:16562/:16891. Ein Token-Modul
         ist zugleich die Vorarbeit der offenen Token-Richtungs-Entscheidung.

      3. TOTE ABHAENGIGKEITEN, MASCHINELL FINDBAR: @spotify/web-api-ts-sdk
         in frontend-box hat 0 Treffer in src (die Box laeuft ueber die
         /api/spotify-Durchreiche); dazu @types/jasminewd2, karma-coverage,
         lodash-es fuer genau einen cloneDeep. Lehre: knip als stehende
         Wache haette das alles gefunden — einfuehren, dazu ShellCheck und
         jscpd. ts-prune/depcheck sind tot bzw. ersetzt.

      4. FRONTEND-BOX-TOTCODE, per Reachability-Scan belegt: current.episode/
         .playlist/.show.ts (0 Importeure), tutorial.ts (138 Zeilen Feature,
         nur der eigene Spec nutzt es), utils.ts:179 sprechenStoppen,
         Route /favorites (der Code sagt selbst "ruft sie niemand mehr"),
         Assets shapes.svg/nocover.png/favicon.png_bak. ABER: /jellyfin und
         /spotify sind absichtliche Einrichtungs-Deeplinks — nie loeschen.

      5. TOOLS/ HAT 448 WERKZEUGE OHNE GEMEINSAME SCHICHT: 84 Dateien rufen
         SSH, mind. 11 kopierte def ssh(), Box-Adresse fest verdrahtet auf
         ZWEI verschiedene Boxen (.169 an 24, .57/.99 an 13 Stellen) —
         welche-box.sh loest genau das und wird von KEINEM Werkzeug benutzt.
         Die JS-Seite beweist mit leihgabe.mjs (120 Nutzer), dass geteilte
         Helfer angenommen werden; der Python-Seite fehlt das Pendant.

      6. WIDERSPRUCH ZUM MERKEN: GET /api/stroeme/belegung ist in diesem
         Lauf ohne Aufrufer, steht aber im AUDIT-21 auf der "ausdruecklich
         NICHT tot"-Liste. Vor dem Loeschen am Geraet klaeren — zwei
         Messungen, die sich widersprechen, sind ein Messauftrag, kein Freibrief.

      RANGLISTE (Kurzform): 1. Rueckstand 21./22.08. abtragen, 2. knip/
      ShellCheck/jscpd, 3. shared-types+zod, 4. Config- und Token-Modul,
      5. gemeinsame Ausrolltabelle, 6. Totes aus dem Baum, 7. tools-SSH-
      Schicht+Index, 8. backend-player angleichen, 9. admin HTTP+Angular 21
      (Ionic 8 blockiert die Box bis Ionic 9, Q3/2026 — Angular-20-LTS
      endet November 2026: Terminrisiko), 10. go-librespot nur als Experiment.

      RESPEKTIERT, NICHT NEU AUFGEROLLT: alle Verworfen-Listen, kiosk-
      laeuft.sh-Doppelung, ALSA-Rueckfallebene, mplayer-Rueckfall, C4 und
      Token-Richtung bleiben Betreiber-Entscheidungen.
    related: ["audit-2026-08-22-kritiker", "dienstliste-an-fuenf-orten", "vierzehn-kopien-und-die-abweichung-ist-der-fehler", "drei-ausrollwege-keiner-kennt-den-anderen", "werkzeug-statt-wegwerfbefehl", "gegenprobe-statt-gruen-glauben"]

  - id: api-medien-nimmt-den-eintrag-flach
    title: "POST /api/medien liest req.body DIREKT - der ARD-Aufnehmen-Knopf schickte ihn in `eintrag` verpackt und legte NIE etwas an"
    kind: falle
    match: "unvollstaendig|api/medien 400|aufnehmen legt nichts an|eintrag verpackt|neuerEintrag req.body"
    sev: high
    tags: [verwaltung, medien, api, ard, falle, 2026]
    source: "Am Geraet .62 gemessen 23.08.2026, unmittelbar nach dem Ausrollen"
    symptom: >
      Ein Aufnehmen-Knopf in der Verwaltung tut scheinbar nichts. Keine
      Fehlermeldung auf der Seite, kein neuer Eintrag in der Bibliothek. Die
      Antwort ist HTTP 400 mit {"error":"unvollstaendig"} - aber der
      Aufrufer wertet sie nicht aus.
    cause: >
      `app.post('/api/medien')` ruft `neuerEintrag(req.body)` - der Eintrag
      steht also FLACH im Rumpf. Wer ihn in `{ eintrag: {...} }` verpackt,
      schickt ein Objekt ohne `type` und ohne `title`, und `neuerEintrag`
      gibt null zurueck.
    fix: >
      Flach posten. Gemessen: flach -> HTTP 200 mit dem Schluessel, verpackt
      -> HTTP 400 "unvollstaendig".
    body: |
      ══ WIE LANGE DAS SO WAR ═══════════════════════════════════════════════
      Seit es den Knopf gibt. `sendungAufnehmen()` in seiten/medien.ts hat nie
      einen Eintrag angelegt - und ZWEI Zeilen weiter unten in DERSELBEN
      Datei stand die richtige, flache Form die ganze Zeit daneben
      (`neuerEintragAnlegen`, Zeile ~2441).

      ══ WARUM ES SO LANGE UNSICHTBAR BLIEB ═════════════════════════════════
      Der Aufrufer steht in einem try/catch, das die Antwort nicht ansieht:
      bei 400 wird nicht geworfen, also faellt nichts auf. Die Zeile darunter
      setzt `schonDa: true` in der Anzeige - die Kachel sagt danach „schon
      da", obwohl nichts angelegt wurde. Der Knopf LUEGT also aktiv.

      ══ WIE ES GEFUNDEN WURDE ══════════════════════════════════════════════
      Nicht durch einen Zeugen, sondern weil der Fehler beim Bau zweier NEUER
      Knoepfe (E84 Sender, E88 Archiv) KOPIERT wurde - und die Geraeteprobe
      ihn dann dreifach zeigte. Ein Zeuge gegen eine Attrappe haette ihn nie
      gesehen: die Attrappe haette genommen, was sie bekommt.

      ES GIBT DAFUER KEINEN ZEUGEN, und das gehoert benannt: die
      Verwaltungsseite hat keine Probe, die eine ECHTE Antwort des Servers
      auswertet. Solange das so ist, faellt diese Fehlerklasse nur am Geraet
      auf.
    related: [plugin-inhalt-kommt-in-die-medienliste, doku-wachen-zaehlen-endpunkte-nicht-ihre-form]
  - id: doku-wachen-zaehlen-endpunkte-nicht-ihre-form
    kind: falle
    sev: medium
    title: Neun Doku-Wachen zaehlen ENDPUNKTE — keine liest, welche Form der Rumpf hat
    tags: [doku, wiki, api, wache, verwaltung, 2026]
    match: endpunkt steht in der doku|api-doku-deckung|doku-luecken-probe|unvollstaendig
    symptom: >
      `tools/doku-luecken-probe.sh` meldet KEINE LUECKE, und trotzdem ruft jemand
      einen Endpunkt falsch auf. Der Endpunkt steht in
      `dokumentation/mixpibox.md` — mit Pfad, Methode und einem Satz, wofuer er
      da ist. Wie sein Rumpf aussieht, steht dort nicht.
    cause: >
      Alle neun Wachen der Probe pruefen DECKUNG: kommt jeder Name aus dem Code
      irgendwo in der Doku vor. `tools/api-doku-deckung.sh` sammelt die
      `app.<methode>(...)`-Aufrufe und sucht den Pfad im Text. Ein Treffer
      genuegt. Ob der Text die Aufrufform richtig beschreibt — oder ueberhaupt
      beschreibt — kann eine Namenssuche nicht sehen.
    fix: >
      Bei jedem Endpunkt, der einen Rumpf NIMMT, die Form in die Tabelle
      schreiben, nicht nur den Zweck. Fuer `POST /api/medien` am 23.08.2026
      nachgetragen (flach, nicht in `eintrag` verpackt).
    body: >
      WORAN ES SICHTBAR WURDE

      `api-medien-nimmt-den-eintrag-flach`: `POST /api/medien` stand die ganze
      Zeit in der Doku-Tabelle, Zeile "die Medienliste". Der Aufnehmen-Knopf
      der Verwaltung schickte den Eintrag trotzdem seit jeher verpackt und legte
      NIE etwas an. Beides zugleich wahr: der Endpunkt war dokumentiert, und die
      Doku half keinem, der ihn aufrufen wollte.


      WAS DAS UEBER DIE PROBE SAGT

      "KEINE LUECKE" heisst nach dem heutigen Stand: kein Name aus dem Code
      fehlt im Text. Es heisst NICHT: der Text stimmt. Die Probe ist eine
      Vollstaendigkeits-, keine Richtigkeitsprobe — und das gehoert beim Lesen
      ihres Ergebnisses mitgedacht.


      WAS ES NICHT GIBT

      Eine Wache, die fuer jeden Endpunkt mit Rumpf verlangt, dass die Doku
      seine Form nennt. Sie waere baubar (die `app.post`-Stellen sind schon
      gesammelt), aber sie braucht ein Urteil darueber, was "die Form nennen"
      heisst — eine Namenssuche reicht dafuer nicht. Bis dahin faellt diese
      Klasse nur auf, wenn jemand den Endpunkt falsch aufruft.
    related: ["api-medien-nimmt-den-eintrag-flach", "schnittstelle-stand-in-keiner-doku-probe"]

  - id: attrappe-kann-die-falsche-form-nicht-merken
    title: "Ein Zeuge gegen eine Attrappe kann nicht merken, dass die FORM falsch ist - sie nimmt jede an"
    kind: regel
    match: "attrappe nimmt jede form|HttpTestingController|knopf tut nichts|form falsch aber test gruen|vertrag zwischen zwei seiten"
    sev: high
    tags: [zeugen, verwaltung, api, arbeitsweise, 2026]
    source: "23.08.2026, aus dem Altfehler am POST /api/medien - siehe [api-medien-nimmt-den-eintrag-flach]"
    body: |
      ══ DIE LUECKE ═════════════════════════════════════════════════════════
      Die Verwaltung prueft ihre HTTP-Aufrufe gegen HttpTestingController. Der
      nimmt JEDE Form an - er kann gar nicht merken, dass der Server sie
      abweist. Ein gruener Zeuge sagt hier nur: „die Komponente hat etwas
      geschickt", nicht „das Richtige".

      Deshalb ueberlebte der verpackte Eintrag am ARD-Knopf so lange, und
      deshalb fiel er erst am GERAET auf - nachdem er in zwei neue Knoepfe
      kopiert war.

      ══ DIE ABHILFE: DEN VERTRAG AUF BEIDEN SEITEN PINNEN ══════════════════
      Nicht einen dritten Zeugen gegen dieselbe Attrappe, sondern ZWEI, die
      von verschiedenen Seiten auf dieselbe Zusage zeigen:

        medien-vertrag.integration.spec.ts   haelt die ROUTE fest: flach ->
             200, verpackt -> 400. BEIDE Richtungen, sonst koennte jemand die
             Route „grosszuegig" machen und beide Formen annehmen - dann
             stuende wieder nirgends, welche gilt.
        tools/verwaltung-vertrag-probe.mjs   liest die AUFRUFE der Verwaltung
             und beurteilt die Form statisch, ohne Netz und ohne Box.

      ══ UND DAS WERKZEUG WAR SELBST ZUERST BLIND ═══════════════════════════
      Der erste Wurf pruefte mit `[{,]\s*type\s*:` - und traf damit das
      `type` im INNEREN Objekt. Der wieder eingebaute Altfehler kam mit
      Rueckgabewert 0 durch. Der Unterschied zwischen `{type:…}` und
      `{eintrag:{type:…}}` liegt in der TIEFE, und eine Regex sieht die nicht;
      es braucht klammerweises Zaehlen.

      EIN WERKZEUG, DAS FALSCHE SICHERHEIT GIBT, IST SCHLIMMER ALS KEINS.
      Gefunden hat es die Gegenprobe - und das ist die allgemeine Lehre: eine
      Gegenprobe gehoert auch auf den PRUEFER, nicht nur auf den geprueften
      Code.
    related: ["api-medien-nimmt-den-eintrag-flach", "gegenprobe-statt-gruen-glauben"]

  - id: unvollstaendig-wird-gesetzt-nicht-am-ende-behauptet
    title: "E89: Die Marke am unvollstaendigen Mitschnitt wird beim ANLEGEN gesetzt und am sauberen Ende geloescht - nie andersherum"
    kind: entscheidung
    match: "unvollstaendig|kachel sagt es|mitschnitt abgebrochen marke|album unvollstaendig anzeigen"
    sev: mid
    tags: [mitschnitt, medien, verwaltung, e89, 2026]
    source: "Betreiberwahl nach dem 101-Meerjungfrauen-Fund (22.08.2026), gebaut 23.08."
    body: |
      ══ DIE RICHTUNG IST DIE GANZE ENTSCHEIDUNG ════════════════════════════
      `Eintrag.unvollstaendig` wird beim ANLEGEN der Kachel gesetzt und am
      Ende eines SAUBEREN Laufs geloescht. Andersherum gedacht („am Ende
      setzen, wenn etwas fehlt") bliebe genau der Abbruchfall stumm - und der
      ist der einzige, auf den es ankommt: ein sterbender Prozess kann sich
      nicht selbst melden.

      Stirbt der Mitschnitt, laeuft `marken_abraeumen()` nie, und die Marke
      bleibt stehen. Das ist kein Versaeumnis, sondern der Entwurf.

      ══ WARUM EIN FLAG UND KEINE STUECKZAHL ════════════════════════════════
      Eine Zahl waere aus der playlist.m3u ABLEITBAR - dieselbe Frage haette
      zwei Antworten, die auseinanderlaufen. Was NUR der Mitschnitt weiss,
      ist, ob sein Lauf zu Ende kam: ein Album mit einem Stueck kann eine
      Single sein oder ein Stummel, und von aussen sieht das gleich aus.

      ══ ZWEI FEINHEITEN, DIE ZEUGEN HABEN ══════════════════════════════════
        * NUR DAS ECHTE `true` zaehlt beim Anlegen. `'true'` als Zeichenkette
          waere in JavaScript wahr und truege eine Behauptung, die niemand
          geschickt hat.
        * `false` LOESCHT das Feld, statt false zu speichern. Ein gespeichertes
          false hiesse „dieses Album ist vollstaendig" - das kann der
          Mitschnitt gar nicht wissen, er weiss nur, dass SEIN Lauf durch ist.
          Fehlt das Feld, ist nichts behauptet (dieselbe Zurueckhaltung wie
          bei `fehlt` in werke.ts).

      ══ DER 409-FALL, DEN MAN LEICHT UEBERSIEHT ════════════════════════════
      Ab dem zweiten Stueck antwortet POST /api/medien mit 409 - der POST
      aendert nichts mehr. Ein Album, das ein frueherer Lauf sauber
      abgeschlossen hat und das jetzt weiterwaechst, ist wieder
      unvollstaendig; ohne PATCH im 409-Zweig bliebe es als fertig markiert
      stehen.

      Die IDENTITAET aendert sich durch die Marke nicht - sonst verloere die
      Box Verlauf und Favoriten in dem Moment, in dem ein Mitschnitt fertig
      wird. Ein Zeuge haelt das fest.
    related: ["mitschnitt-stummel-kachel-vor-inhalt", "api-medien-nimmt-den-eintrag-flach", "halbe-aufnahme-nicht-anzeigen"]

  - id: zaehlende-probe-sieht-keinen-widerspruch
    title: "Eine Doku-Probe, die ZAEHLT, kann einen Widerspruch nicht sehen - beide Namen kommen ja vor"
    kind: falle
    match: "doku-luecken-probe|doku vollstaendig aber falsch|zwei eintraege widersprechen|veralteter wiki-eintrag"
    sev: mid
    tags: [doku, wiki, werkzeug, probe, 2026]
    source: "Doku-Lauf 23.08.2026, direkt nach E89. tools/wiki-zwei-wahrheiten.mjs gebaut und gelaufen."
    body: |
      ══ DER BEFUND ═════════════════════════════════════════════════════════
      Nach E89 meldete `tools/doku-luecken-probe.sh` genau EINE Luecke: eine
      veraltete Zahl in der Kopfzeile. Im selben Moment standen im Pack zwei
      Entscheidungen nebeneinander, die derselben Frage zwei Antworten gaben:

        halbe-aufnahme-nicht-anzeigen (20.08.)
          „Eine unvollstaendige Aufnahme wird verworfen, nicht als Kachel
          angeboten"
        unvollstaendig-wird-gesetzt-nicht-am-ende-behauptet (23.08., E89)
          Die Kachel entsteht und traegt eine Marke.

      Beide sind wahr - auf VERSCHIEDENEN EBENEN (Titel bzw. Album). Nur
      stand das nirgends, und der aeltere Eintrag stand mit dem
      allgemeineren Titel da. Wer nach „unvollstaendige Aufnahme" suchte,
      fand zuerst die Antwort von vorgestern.

      ══ WARUM DIE ALTE PROBE DAS NICHT FINDEN KANN ═════════════════════════
      Sie fragt: kommt der Name vor? Bei einem Widerspruch kommt er ZWEIMAL
      vor. Zaehlende Deckungspruefungen koennen nur Schweigen finden, nie
      Falschheit - dieselbe blinde Stelle wie bei
      volles-wiki-ist-kein-beleg-fuer-gepflegte-doku, nur eine Ebene tiefer:
      dort war das Pack voll und die Doku leer, hier ist das Pack voll und
      in sich uneins.

      ══ DAS WERKZEUG ═══════════════════════════════════════════════════════
      `tools/wiki-zwei-wahrheiten.mjs` vergleicht die `match:`-Muster aller
      Eintraege. Zwei, die sich in mindestens zwei aussagekraeftigen Woertern
      ueberschneiden und einander NICHT in `related:` nennen, kommen auf eine
      Leseliste. Nicht als Fehler - als Stelle, an der niemand entschieden
      hat, welcher Eintrag gilt.

      `match:` und nicht der Rumpf, weil `match:` die FRAGE ist, auf die der
      Eintrag antworten will: zwei Eintraege mit aehnlichem `match:` werden
      demselben Sucher vorgelegt, und der liest den ersten.

      AUSBEUTE BEIM ERSTEN LAUF: 786 Eintraege, 469 mit `match:`, drei Paare.
      Eines echt (das obige), zwei schwache Treffer ueber Allerweltswoerter.
      Drei Zeilen lesen, um einen falschen Titel zu finden, ist ein guter
      Tausch; eine Probe, die dreissig meldet, liest niemand.

      ══ DIE REGEL ══════════════════════════════════════════════════════════
      Wer eine Entscheidung UMDREHT, muss den alten Eintrag anfassen - nicht
      nur einen neuen schreiben. Ein Pack, in dem beide Fassungen
      unverbunden stehen, ist schlechter als eines, in dem nur die alte
      steht: dort weiss man wenigstens, dass man nachsehen muss.
    related: ["volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "unvollstaendig-wird-gesetzt-nicht-am-ende-behauptet", "wiki-zustand-hat-haltbarkeit"]

  - id: attrappe-am-falschen-ende-nock-erreicht-keinen-worker
    title: "17 Zeugen waren monatelang rot: nock flickt den HAUPTprozess, das Plugin holt im WORKER"
    kind: falle
    match: "nock greift nicht|502 im integrationstest|pluginInhalt 502|zeuge dauerhaft rot|worker faelschen"
    sev: high
    tags: [zeugen, plugins, ard, e78, migration, 2026]
    source: "23.08.2026, beim Abarbeiten der offenen Punkte; die Faelle waren seit E78 rot"
    symptom: >
      Ein Integrationszeuge, der frueher lief, antwortet mit HTTP 502, obwohl
      die Faelschung des Netzes richtig aussieht.
    cause: >
      `nock` flickt das http-Modul des HAUPTprozesses. Seit E78 holt das
      Plugin die Daten in einem WORKER-Thread, und der hat sein eigenes
      Modul - die Faelschung laeuft ins Leere. Dazu fehlte
      `MUPIBOX_PLUGIN_DIR`, es war also gar kein Plugin geladen.
    fix: >
      Die Attrappe an die NAHT setzen, die es heute gibt: ein
      Plugin-Stummel im Temp-Verzeichnis, der seine Antwort aus einer Datei
      liest, die die Spec je Fall schreibt.
    body: |
      ══ WAS DABEI SONST NOCH HERAUSKAM ═════════════════════════════════════
      Weil die Faelle ohnehin rot waren, fiel zweierlei nicht auf:

        * ZWEI ZEUGEN RIEFEN EINE ROUTE, DIE ES NICHT MEHR GIBT.
          `GET /api/ard/suche` ist bei E79 ins Plugin gewandert; sie
          scheiterten seither an 404, nicht an der Suche.
        * DIE FEHLERNAMEN HATTEN SICH GEAENDERT. Frueher unterschied der
          Server `nichtErreichbar` von `sendungUnbekannt`; seit E78 meldet er
          EINEN Namen (`pluginInhalt`) und legt den Grund im Klartext daneben.
          Das ist die ehrlichere Auskunft - welcher Fall vorliegt, weiss der
          Server gar nicht mehr.

      Der Unterschied, auf den es ankommt, blieb: 502 heisst „ich konnte
      nicht fragen", NICHT „die Sendung ist leer". Eine leere Sendung ist 200
      mit leerer Liste.

      ══ UND EINE REGEL IST UMGEZOGEN, NICHT GESTRICHEN ═════════════════════
      „laesst die tonlose Dublette und die abgelaufene Folge aus" prueft eine
      ARD-REGEL. Die wohnt seit E78 im Plugin und wird dort gehalten; sie hier
      ein zweites Mal zu pruefen hiesse, dieselbe Regel an zwei Orten zu
      fuehren.

      ══ DIE ALLGEMEINE LEHRE ═══════════════════════════════════════════════
      EIN ZEUGE, DER IMMER ROT IST, WIRD UEBERSEHEN WIE EINER, DER IMMER GRUEN
      IST. Wer eine Schicht verschiebt, verschiebt auch die Naht, an der
      gefaelscht wird - sonst bleibt eine Reihe Zeugen zurueck, die nichts
      mehr aussagt und trotzdem jeden Lauf rot faerbt.
    related: ["attrappe-kann-die-falsche-form-nicht-merken", "inhalt-im-vertrag-und-ard-delegiert"]

  - id: tsx-test-streift-typen-ab-der-bau-nicht
    title: "Ein Spec kann gruen laufen und trotzdem den BAU blockieren - tsx streift Typen ab, `npm run build` prueft sie"
    kind: falle
    match: "rc=2 aber ausgaben aelter|check-types failed|possibly undefined im spec|bau scheitert test gruen"
    sev: high
    tags: [zeugen, bau, typen, ausliefern, 2026]
    source: "23.08.2026: ein selbst geschriebener Integrationszeuge blockierte den Bau, und nur die Veraltet-Pruefung des Ausrollwegs zeigte es"
    symptom: >
      Alle Zeugen sind gruen, aber `tools/ausliefern.py` meldet „Bau 1: rc=2,
      aber diese Ausgaben sind AELTER als der Baubeginn". Wer nur die Zeugen
      ansieht, sieht nichts.
    cause: >
      `tsx --test` uebersetzt mit esbuild und STREIFT TYPEN AB - ein
      `possibly undefined` faellt dort nicht auf. `npm run build` ruft
      dagegen `check-types` (tsc --noEmit) ZUERST; schlaegt das fehl, bricht
      der Bau ab, und in `src/deploy/` bleibt der ALTE Stand liegen.
    fix: >
      `npm run check-types` im Backend laufen lassen, nicht nur die Zeugen.
      Der Bau ist die schaerfere Pruefung.
    body: |
      ══ WARUM DAS GEFAEHRLICH IST ══════════════════════════════════════════
      Der Ausrollweg vergleicht md5 zwischen Arbeitsrechner und Box. Liegt in
      `src/deploy/` ein alter Stand, meldet er brav „am Geraet identisch mit
      dem Bau" - identisch mit dem ALTEN Bau. Ohne die Veraltet-Pruefung
      („Ausgaben sind AELTER als der Baubeginn") waere ein Ausrollen still
      wirkungslos gewesen.

      DIESE PRUEFUNG IST DAMIT DER EIGENTLICHE FUND: sie hat einen Fehler
      gefangen, den weder 2496 gruene Zeugen noch der md5-Vergleich sahen.

      ══ VERWANDT, ABER ANDERS ══════════════════════════════════════════════
      [esbuild-schreibfehler-endet-mit-null] beschreibt denselben ZUSTAND aus
      anderer Ursache: dort endet esbuild mit 0, obwohl es an
      root-Eigentuemern scheitert. Gemeinsam ist beiden: in `src/deploy/`
      liegt Altes, und alles danach sieht gruen aus.
    related: ["esbuild-schreibfehler-endet-mit-null", "mupi-tsc-prueft-keine-vorlagen", "attrappe-kann-die-falsche-form-nicht-merken"]

  - id: gemerkte-stelle-fuer-plugin-inhalt
    title: "E90: Die gemerkte Stelle fuer Plugin-Inhalt haengt an der FOLGENKENNUNG - und der Leseweg filterte sie still weg"
    kind: architektur
    match: "weiterAb null plugin|plugin stelle wird nicht gemerkt|blauer knopf fehlt plugin|resumeardfolge plugin"
    sev: mid
    tags: [plugins, weiterhoeren, medien, e90, 2026]
    stand: "23.08.2026 gebaut, 14 Nahtzeugen gruen, Backend 2499/2499"
    source: "Der letzte offene Punkt aus dem E87-Nachtrag"
    body: |
      ══ DER MECHANISMUS WAR SCHON DA, NUR ARD-GEFAERBT ══════════════════════
      Ein Plugin-Werk ist dieselbe Bauart wie eine ARD-Sendung: eine Liste,
      die zwischen zwei Blicken anders aussehen darf. Wer die POSITION merkt,
      springt nach dem naechsten Zuwachs in ein fremdes Stueck. Die Loesung
      stand seit E4/A10 fertig da - `resumeardfolge` + `folgeWiederfinden` -
      und musste nur generisch werden.

      DIE FELDER HEISSEN WEITER `resumeard…`. Sie stehen so in resume.json auf
      jeder Box; sie umzubenennen waere eine Wanderung aller vorhandenen
      Staende fuer einen Namen. Der Name ist inzwischen ungenau („die Folge
      einer rollenden Liste"), die Bedeutung nicht.

      ══ FUENF STELLEN, UND EINE DAVON HAETTE MAN UEBERSEHEN ═════════════════
        ueberMpv              plugin laeuft ueber mpv wie ard
        nummerVerlaesslich    ohne Folgenkennung gilt eine Plugin-Stelle nicht
        stelleGiltFuer        die Kennung ist ausserhalb bedeutungslos
        stelleAus             der Schreibweg nimmt plugin wie ard
        istWeiterhoerbar      DIE STELLE, DIE FAST GEFEHLT HAETTE

      `istWeiterhoerbar` fuehrt eine Positivliste der Dienste. Ohne `plugin`
      darin wird eine Stelle zwar GESCHRIEBEN, aber nie wieder herausgegeben:
      der blaue Knopf bleibt aus, und niemand sieht einen Fehler. Der
      Schreibweg war fertig, der Leseweg filterte still weg - gefunden nur,
      weil der Nahtzeuge die ganze Kette geht.

      ══ WAS DER PLUGIN-ZWEIG NICHT TUT, UND WARUM ══════════════════════════
      Er holt die Folgenliste NICHT nach, wenn der Speicher leer ist (etwa
      nach einem Serverneustart). Der ARD-Zweig darf das: der Server weiss,
      in welcher Reihenfolge er eine Sendung spielt. Fuer ein fremdes Plugin
      weiss er es nicht - es ordnet nach eigenen Regeln. Eine geratene Liste
      waere schlimmer als keine Stelle: sie zeigte in ein fremdes Stueck.
      Also lieber nichts merken, bis die Kachel einmal geoeffnet wurde.

      ══ ZWEI SABOTAGEN WAREN FALSCH HERUM GEBAUT ═══════════════════════════
      Beim ersten Durchgang blieben zwei gruen - sie machten die Regel
      GROSSZUEGIGER statt strenger und konnten deshalb nichts brechen. Eine
      Gegenprobe muss die Regel VERLETZEN, nicht lockern; sonst prueft sie,
      dass Erlaubtes erlaubt bleibt.
    related: ["plugin-inhalt-kommt-in-die-medienliste", "ard-folgenliste-rollt"]

  - kind: lesson
    id: doku-korrektur-nur-an-der-ersten-fundstelle
    title: "Eine Doku-Korrektur haelt nur an der Stelle, an der man sie anbringt - dieselbe Zusage steht meist dreimal"
    sev: med
    tags: [doku, wiki, pflege, doku-lauf, luecken-probe]
    source: "Doku-Lauf 23.08.2026, eine Stunde NACH Commit a24d9af7 - der vorige Lauf korrigierte die E90-Zusage an zwei Stellen und liess vier weitere stehen"
    body: |
      Commit a24d9af7 raeumte die durch E90 falsch gewordene Zusage auf: in
      `dokumentation/mixpibox.md` (Abschnitt 4.7, der E87-Absatz) und in
      `plugins/README.md` (die Tabelle „Was noch fehlt"). Beide Stellen waren
      danach richtig, die Luecken-Probe meldete KEINE LUECKE - und trotzdem
      stand dieselbe ueberholte Aussage noch VIER MAL im Bestand:

        mixpibox.md   Abschnitt 9   „Bei den Plugins fehlen drei Dinge:
                                     die gemerkte Stelle …"
        README.md     Zeile ~99     „(Reihenfolge, Vorspann, gemerkte Folge)"
        README.md     Zeile ~114    „ApiRouteProvider und UiExtension gibt es
                                     nicht"
        README.md     „Was NICHT geht"  „Eine gemerkte Stelle fuer
                                     Plugin-Inhalte auch nicht."

      ══ DER ABSCHNITT 9 WAR GLEICH DREIFACH FALSCH ═════════════════════════
      Er nannte drei fehlende Dinge. Die gemerkte Stelle gibt es seit E90
      (23.08.), die Verwaltungsseite seit dem 14.08. (`/plugins` und
      `/plugins/<kennung>` - der Satz „Einstellungen gehen bis dahin per curl"
      war seit NEUN TAGEN falsch), und eigene Endpunkte gibt es seit E77
      eingeschraenkt. Uebrig ist genau ein Punkt: eine Flaeche, die der
      Kinderschirm erreicht.

      ══ WARUM DIE PROBE DAS NIE FINDET ═════════════════════════════════════
      `tools/doku-luecken-probe.sh` prueft STRUKTUREN gegen die Doku: gibt es
      dieses Plugin, dieses Recht, dieses Ereignis, diese Route, dieses
      Konfigurationsfeld im Text? Ein Satz, der ein VERHALTEN zusagt, ist
      keine Struktur. Er kann beliebig falsch werden, ohne dass eine Zeile
      der Probe rot wird - und die Probe sagt danach beruhigend „KEINE
      LUECKE".

      ══ DIE REGEL ══════════════════════════════════════════════════════════
      Wer eine Zusage korrigiert, sucht sie VORHER im ganzen Bestand:

        grep -rn "<der Begriff>" dokumentation/ plugins/README.md

      und korrigiert ALLE Treffer im selben Zug. Eine Korrektur an der ersten
      Fundstelle ist keine Korrektur, sondern ein Widerspruch: `mixpibox.md`
      sagte in Abschnitt 4.7 „gibt es seit E90" und in Abschnitt 9 „fehlt" -
      im selben Dokument, dessen Kopfzeile ausdruecklich verspricht, dass
      zwei Wahrheiten ueber dieselbe Sache schlimmer sind als eine
      unvollstaendige.

      ══ WO SOLCHE SAETZE SICH SAMMELN ══════════════════════════════════════
      In Kaesten, die man fuer gepflegt haelt: „Was noch fehlt", „Was NICHT
      geht", „Was gerade im Bau ist", Klammer-Aufzaehlungen von Vorteilen.
      Genau die liest beim Bauen niemand - sie werden erst gelesen, wenn
      jemand entscheidet, ob er etwas bauen muss, das laengst da ist.
    related: ["gemerkte-stelle-fuer-plugin-inhalt", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "doku-wachen-zaehlen-endpunkte-nicht-ihre-form"]

  - kind: lesson
    id: genau-eine-stufe-regelt-den-ton
    title: "Genau eine Stufe regelt — der Rest strandet"
    sev: high
    tags: [ton, pipewire, lautstaerke, geraet, messen]
    body: |
      Der Betreiber meldete, die Lautstaerke sei „stark gedeckelt". Der Regler
      der Box stand dabei auf ANSCHLAG. Am Geraet gemessen (23.08.2026):

          klangwerk (Vorgabe-Senke, DER REGLER) ....  100 %     0,0 dB
          entzerrer ................................   60 %   -13,3 dB
          alsa_output (MAX98357A) ..................    7 %   -69,3 dB
                                                              ─────────
                                        82 dB, die der Regler nie erreicht

      Die 69 dB der Tonkarte teilten sich auf in -51 dB im ALSA-Mischer
      (`amixer -c 2 sget Master` stand auf 0 von 255) und -18 dB PipeWire-
      Restdaempfung (`softVolumes`). PipeWire schiebt so viel wie moeglich in
      die Hardware und legt den Rest selbst an — beides zusammen ergibt
      `channelVolumes`.

      ══ DIE URSACHE IST EIN BEWEGLICHES ZIEL ═══════════════════════════════
      `mupi-lautstaerke.sh` setzt `@DEFAULT_AUDIO_SINK@`. WELCHE Senke das ist,
      AENDERT SICH IM BETRIEB: beim Anlauf die Tonkarte, dann der Entzerrer,
      dann das Klangwerk. Jeder Griff an den Regler trifft die Stufe, die
      GERADE vorne steht. Waechst spaeter eine neue Stufe davor, bleibt der
      alte Wert liegen — und WirePlumber merkt ihn sich ueber den Neustart.
      Weder der Entzerrer- noch der Kartenwert wird IRGENDWO im Code gesetzt;
      es sind reine Altlasten.

      ══ EIN FEHLER, ZWEI BESCHWERDEN ═══════════════════════════════════════
      Derselbe Tag, zweiter Befund: „beim Runterfahren gibt es beim Spielen
      einen kurzen Anstieg der Lautstaerke." `off_trigger.sh` stellte
      `startVolume` zurueck, WAEHREND das Kind noch hoerte (die Musik stoppt
      erst mit `service mupi_startstop stop`, weiter unten). War das Klangwerk
      da schon abgebaut, traf dieses `set` die KARTE und hob sie von 7 % auf
      20 %. Genau der gehoerte Anstieg — und zugleich der Weg, auf dem die
      Altlast immer wieder neu entsteht.

      ══ WAS DAGEGEN HILFT ══════════════════════════════════════════════════
      Es gibt genau EINE regelnde Stufe: die Vorgabe-Senke. Alles andere im
      Weg ist Durchreiche und steht auf 100 %. `einheit_nachziehen` in
      mupi-lautstaerke.sh zieht das bei JEDEM `set` nach — nicht einmalig
      repariert, denn die Reihenfolge, in der die Stufen hochkommen, laesst
      sich nicht festnageln. Und das Zuruecksetzen in `off_trigger.sh` steht
      jetzt HINTER dem Anhalten der Wiedergabe, wo es niemand hoert.

      ══ WARUM ES NIEMAND GESEHEN HAT ═══════════════════════════════════════
      `wpctl get-volume @DEFAULT_AUDIO_SINK@` — die Frage, die jedes Werkzeug
      der Box stellt — antwortete „1.00". Von vorne war alles in Ordnung. Eine
      Kette misst man GANZ; dafuer gibt es jetzt `tools/tonweg-pegel.mjs`.

      ══ WER DIE AUSSAGE NACHPRUEFEN WILL ═══════════════════════════════════
      `tools/lautstaerke-einheit-probe.sh` haelt die zwei Haelften am echten
      mupi-lautstaerke.sh fest: die gestrandeten Stufen WERDEN angefasst, und
      die regelnde Stufe wird dabei NICHT mitgezogen. Sie ist die Gegenprobe
      zu dieser Lektion, nicht nur ihre Illustration.
    related: ["zwei-zaehlweisen-fuer-eine-lautstaerke", "einmal-hinsehen-ist-keine-messung"]

  - kind: lesson
    id: zwei-zaehlweisen-fuer-eine-lautstaerke
    title: "wpctl rechnet kubisch, channelVolumes ist linear"
    sev: med
    tags: [ton, pipewire, messen, fallstrick]
    body: |
      Dieselbe Senke, drei Zahlen, alle richtig:

          wpctl get-volume 62      ->  Volume: 0.07
          pw-dump channelVolumes   ->  0.000343
          pactl list sinks         ->  4588 / 7 % / -69,29 dB

      `wpctl` und `pactl` zeigen die WAHRNEHMUNGSZAHL, `channelVolumes` ist die
      Verstaerkung, die WIRKLICH auf die Abtastwerte geht. Zwischen beiden
      liegt die dritte Potenz: 0,07³ = 0,000343. Die dB-Angabe von `pactl` ist
      aus dem LINEAREN Wert gerechnet, nicht aus den Prozent.

      WAS DAS KOSTET, wenn man es nicht weiss: 7 % klingt nach „ein bisschen
      leiser". Es sind -69 dB. Wer die Prozentzahl fuer den Pegel haelt, sucht
      den Fehler um zwei Groessenordnungen daneben — oder haelt eine echte
      Deckelung fuer harmlos.

      ══ UND DIE FALLE BEIM NACHMESSEN ══════════════════════════════════════
      Der Monitor-Abgriff einer Senke sitzt VOR deren Regler
      (`monitorVolumes` steht eigenstaendig auf 1.0). Eine Durchgangsmessung
      ueber `<senke>.monitor` sieht also alles AUSSER der letzten Stufe. Auf
      der Box ergab sie +10 dB, waehrend die eingestellten Werte -82 dB
      sagten; beide Zahlen stimmten, sie massen nur nicht dasselbe.
      `tools/tonweg-durchgang.py` sagt das inzwischen dazu.
    related: ["genau-eine-stufe-regelt-den-ton"]

  - kind: lesson
    id: aufholung-braucht-die-schwelle
    title: "Die Aufholung eines Kompressors haengt an der Schwelle, nicht am Verhaeltnis"
    sev: med
    tags: [ton, klang, plugin, rechnen]
    body: |
      `mixpi-klang` rechnete die Aufholung (makeup) aus dem VERHAELTNIS allein:

          makeup = min(12, round(verhaeltnis * 1,5))

      Am Geraet gemessen (`pw-dump` der laufenden Kette): Schwelle -18 dB,
      Verhaeltnis 3, Makeup 5. Ein Gipfel bei 0 dBFS kommt als
      `-18 + 18/3 = -12 dB` heraus und wird um 5 dB angehoben — es fehlen 7 dB.

      Richtig ist `|Schwelle| * (1 - 1/Verhaeltnis)`; fuer -18 dB bei 3:1 sind
      das 12 dB. Die alte Regel konnte zwei verschiedene Schwellen bei
      gleichem Verhaeltnis gar nicht unterscheiden — sie gab beide Male 5.

      Der Deckel von 12 dB BLEIBT: mehr hiesse Rauschen aus den kleinen
      Chassis, und die Spitzen faengt ohnehin der Begrenzer.
    related: ["genau-eine-stufe-regelt-den-ton"]

  - kind: open
    id: klangwerk-senke-nimmt-keine-lautstaerke
    title: "GELOEST (05.09.2026): die klangwerk-Senke nimmt keine Lautstaerke an"
    sev: high
    tags: [ton, pipewire, klangwerk, geraet, geloest]
    body: |
      NOCH NICHT VERSTANDEN, am 23.08.2026 am Geraet gemessen. Steht hier, weil
      der naechste, der die Lautstaerke sucht, sonst dieselbe Stunde verliert.

      Die Senke `klangwerk` ist die Vorgabe-Senke — dort greift der Regler der
      Box. Sie nimmt KEINEN Wert an:

          wpctl set-volume 75 0.3      -> Ruecklauf 0, danach 1.00
          wpctl set-volume @DEFAULT…   -> Ruecklauf 0, danach 1.00
          pactl set-sink-volume klang… -> Ruecklauf 0, danach 1.00
          auch mit echtem TTY und XDG_RUNTIME_DIR

      Es ist KEIN Sitzungs- oder Rechteproblem: dieselbe SSH-Sitzung setzt die
      Tonkarte (`alsa_output…`) problemlos, und der Wert haelt ueber 8 s.
      Es ist auch keine Schleife: das Protokoll zeigt drei Kettenneubauten in
      vier Minuten, alle von Dienst-Neustarts. `deckelAnwenden` senkt nur,
      `klangAnwenden` fasst die Senkenlautstaerke nicht an, und die erzeugte
      /tmp/mixpi-klangwerk.conf enthaelt keinen Lautstaerkewert.

      WAS STUTZIG MACHT: eine FRUEHERE Ausgabe derselben Kette (Knoten 77) hat
      denselben Aufruf angenommen — `mupi-lautstaerke.sh set 20` setzte sie auf
      0.20, nachgemessen mit tonweg-pegel.mjs. Nach einem Dienst-Neustart
      (Knoten 75) geht es nicht mehr. Der Unterschied zwischen den beiden
      Ausgaben ist der offene Punkt.

      WAS DAS BEDEUTET, wenn es der Normalfall ist: der Regler der Box bewegt
      NICHTS, solange das Klangwerk vorne steht. Das waere die eigentliche
      Antwort auf „die Lautstaerke ist stark gedeckelt" — nicht nur zu leise,
      sondern gar nicht regelbar.

      ZUSTAND, IN DEM DIE BOX STEHT (23.08.2026, 15:0x): weil die gestrandete
      Daempfung entfernt ist und der Regler nicht greift, stuende die Box sonst
      auf VOLLAUSSCHLAG mit +12 dB Aufholung. Als Bremse ist die Tonkarte auf
      0.50 (-18 dB) gesetzt — von Hand, NICHT im Code. Wer den Reglerfehler
      loest, nimmt die Bremse wieder heraus; `einheit_nachziehen` tut das beim
      naechsten `set` ohnehin von selbst.

      NAECHSTER SCHRITT: die beiden Knoten (angenommen/abgelehnt) nebeneinander
      dumpen und vergleichen — PropInfo, flags, node.lock, wer den Knoten
      besitzt.

      ══ DAS WERKZEUG DAFUER GIBT ES SCHON ══════════════════════════════════
      Wer hier weitermacht, baut nichts neu. Zwei STRENG LESENDE Proben stehen
      bereit und setzen selbst nichts:

          tools/lautstaerke-zieher-probe.sh        zaehlt jede Ausfuehrung von
              mupi-lautstaerke.sh nach Unterbefehl und protokolliert jede
              Wertaenderung an der beobachteten Senke — sie widerlegt oder
              bestaetigt den Verdacht „GET /volume/-5 zieht mit".
          tools/box/lautstaerke-schreiber-finden.sh  erwischt den Urheber am
              Prozessbaum. PipeWire verraet NICHT, wer einen Prop gesetzt hat;
              `pw-mon` zeigt die Aenderung, nicht den Schreiber.

      ══ DIESER ZUSTAND IST VOM 23.08. UND NICHT NACHGEMESSEN ═══════════════
      `neue-senke-faengt-bei-eins-an` (29.08.2026) beschreibt eine frisch
      gebaute klangwerk-Senke, deren Regler GESETZT wird — die ganze dortige
      Loesung besteht darin, WANN man ihn setzt. Das passt nicht zu „nimmt
      keinen Wert an". Entweder hat die Reihenfolge-Arbeit das mit erledigt,
      oder die beiden Messungen trafen verschiedene Knoten-Ausgaben (genau der
      Unterschied, der hier offen ist). NICHT ALS GELOEST BUCHEN, BEVOR ES AM
      GERAET WIEDERHOLT IST; die Aussage oben ist ein Zustand vom 23.08., und
      Zustaende haben Haltbarkeit.

      ══ AUFLOESUNG (05.09.2026, am Geraet bewiesen) ═════════════════════════
      Das „Wetterwendische" war nie die klangwerk-Senke allein — es war die
      TONKARTE dahinter: PipeWire schob jeden Reglerwert in einen
      Hardware-Mixer, den der MAX98357A nicht hat. Ob ein Stellbefehl
      „hielt", entschied der Zufall der Software-Nachfuehrung — deshalb nahm
      dieselbe Kette denselben Befehl mal an und mal nicht. Wurzel, Fix
      (api.alsa.soft-mixer am DEVICE) und Messwerkzeug stehen in
      [[karten-mixer-ist-eine-attrappe]]; seither regelt die Hardware-Senke
      nachweisbar (gestellt == angewandt), und die Nutzerlautstaerke wohnt
      dort — die klangwerk-Senke muss gar keine Lautstaerke mehr annehmen.
    related: ["genau-eine-stufe-regelt-den-ton", "zwei-zaehlweisen-fuer-eine-lautstaerke", "neue-senke-faengt-bei-eins-an", "karten-mixer-ist-eine-attrappe"]

  - kind: falle
    id: kind-werte-taugen-nicht-als-inhaltsverzeichnis
    title: "Die kind-Werte des Pakets sind auseinandergelaufen"
    sev: med
    tags: [wiki, doku, fallstrick]
    body: |
      `dokumentation/mixpibox.md` fuehrte in Abschnitt 8 eine Tabelle „kind ->
      Anzahl" als Wegweiser fuer die Fehlersuche. Beim Nachzaehlen am
      23.08.2026 stimmte keine der fuenf Zahlen mehr (falle 86 -> 326,
      entscheidung 22 -> 62), und schlimmer als die Drift war der Befund
      darunter: das Paket kennt 27 verschiedene `kind`-Werte fuer im Grunde
      sechs Arten.

          offen / open            gotcha / falle
          knowhow / lesson / erkenntnis / befund / wissen / lehre
          regel, pattern, kniff, loesung, entwurf, idea, plan, ...
          und 14 Eintraege ganz OHNE kind

      Es gibt kein Schema, das `kind` einschraenkt — jeder Lauf hat das Wort
      gewaehlt, das gerade passte. Fuer die Ablage ist das folgenlos; als
      INHALTSVERZEICHNIS taugt es nicht: wer `kind: offen` filtert, uebersieht
      den Eintrag mit `kind: open`, und das war ausgerechnet der schwerste
      offene Punkt (klangwerk-senke-nimmt-keine-lautstaerke).

      ══ WAS JETZT GILT ═════════════════════════════════════════════════════
      Gesucht wird ueber die TAGS, nicht ueber `kind`. Die Tabelle in
      Abschnitt 8 steht weiter da, sagt aber jetzt dazu, dass sie das Paket
      nicht abdeckt. Nachtraeglich vereinheitlichen waere ein Massenumbau an
      795 Eintraegen fuer einen Gewinn, den ein Tag billiger hat.
    related: ["klangwerk-senke-nimmt-keine-lautstaerke", "zaehlende-probe-sieht-keinen-widerspruch"]

  - kind: falle
    id: pruefer-endet-immer-mit-null
    title: "Der README-Pruefer meldete gruen, seit es ihn gibt"
    sev: high
    tags: [doku, wache, gegenprobe, fallstrick, readme]
    body: |
      `tools/readme-behauptungen-pruefen.sh` zaehlt jeden Fehlschlag sauber in
      `FEHLER` mit und druckt am Ende „N Behauptung(en) ohne Beleg". Danach
      stand, bedingungslos:

          exit 0

      Er hat damit seit seinem ersten Tag GRUEN gemeldet, egal was er fand.
      Der Lauf vom 23.08.2026 druckte „1 Behauptung ohne Beleg" und endete
      trotzdem mit 0.

      ══ WARUM DAS MEHR IST ALS EIN VERGESSENES ZEICHEN ══════════════════════
      Der Doku-Lauf ruft solche Werkzeuge mit `if ! bash …` auf. Ein Werkzeug,
      das nie scheitert, macht aus JEDEM solchen Aufrufer eine Attrappe: der
      Aufrufer sieht aus, als pruefe er, und prueft nichts. Genau das ist die
      Bauart von [[zaehlende-probe-sieht-keinen-widerspruch]], nur eine Ebene
      tiefer.

      ══ WIE ES AUFFIEL ═════════════════════════════════════════════════════
      NICHT beim Lesen — beim Zurueckdrehen. Die alte, falsche Zahl (71
      Werkzeuge) wurde wieder eingesetzt, die neue Wache meldete „516 %
      daneben", und der Rueckgabestand blieb 0. Ohne die Gegenprobe waere die
      frisch gebaute Wache an einen Aufrufer gehaengt worden, der sie nie
      haette rot werden sehen. [[gegenprobe-statt-gruen-glauben]]

      ══ WAS ZU PRUEFEN IST ═════════════════════════════════════════════════
      Bei jedem Werkzeug in `tools/`, das ein anderes per `if !` aufruft:
      einmal absichtlich kaputtmachen und den Rueckgabestand ansehen, nicht
      die Ausgabe.
    related: ["gegenprobe-statt-gruen-glauben", "zaehlende-probe-sieht-keinen-widerspruch", "readme-zahlen-waren-um-das-sechsfache-daneben"]

  - kind: falle
    id: readme-zahlen-waren-um-das-sechsfache-daneben
    title: "Sechs Zahlen in der README, sechsmal falsch — gemessen und gedruckt, nie geurteilt"
    sev: med
    tags: [doku, readme, wache, zahlen]
    body: |
      Am 23.08.2026 meldeten alle neun Pruefungen in
      `tools/doku-luecken-probe.sh` gruen. Die README, die Datei, die jeder
      zuerst liest, fuehrte im selben Moment SECHS falsche Zahlen:

          tools/                 71   ->  438   (Faktor 6)
          NewDesign ausgeliefert ~9100 -> 38734 (Faktor 4)
          Wissenspaket           690  ->  796
          backend-api Tests      51   ->  113
          frontend-admin Seiten  16   ->   21
          systemd-Units          22   ->   41

      Dazu ein Widerspruch aus zwei Saetzen, die zu verschiedenen Zeiten
      geschrieben und nie zusammen gelesen wurden: „der alte PHP-Admin ist am
      19.08.2026 ausgebaut worden (E47). Der Rueckbau des PHP-Admins steht
      aus."

      ══ DER EIGENTLICHE BEFUND ═════════════════════════════════════════════
      `tools/readme-behauptungen-pruefen.sh` hat alle sechs Zahlen GEMESSEN.
      Es druckte sie als `ZAHL …  README: 71  gemessen: 438` und urteilte
      nicht. Der Doku-Lauf rief die Datei gar nicht auf. Ein Werkzeug, das den
      Befund neben den Sollwert schreibt und dem Leser das Vergleichen
      ueberlaesst, wird nach der zweiten Woche ueberlesen.

      ══ WAS JETZT GILT ═════════════════════════════════════════════════════
      `zahl()` urteilt, aber ueber ein BAND, nicht ueber die Zahl: erst wenn
      die Abweichung die Toleranz reisst (Vorgabe 15 %, weiter fuer das, was
      schnell waechst), wird gemeldet. Eine Wache, die auf „438" besteht, ist
      am naechsten Tag rot und wird abgeschaltet; gefangen werden soll „um ein
      Vielfaches daneben", nicht „drei mehr als gestern".
      [[tests-duerfen-keine-zahlen-festnageln]]

      Der Pruefer laeuft seither als zehnte Pruefung im Doku-Lauf mit.
    related: ["pruefer-endet-immer-mit-null", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku"]

  - kind: falle
    id: ausgebaute-komponente-lebt-in-der-doku-weiter
    title: "AdminInterface war vier Tage weg und stand in drei Dateien noch im Praesens"
    sev: med
    tags: [doku, php-admin, e47, aufraeumen]
    body: |
      Mit E47 (19.08.2026) hat der alte PHP-Admin das Projekt verlassen:
      `git ls-tree -r HEAD | grep -c '^AdminInterface/'` zaehlt 0, und in
      `autosetup/autosetup.sh` (Z. 707 ff.) steht an seiner Stelle ein
      Kommentar. Am 23.08.2026 stand er trotzdem noch als lebende Komponente:

        * `dokumentation/mixpibox.md` §4.1: „der alte PHP-Admin ruft weiter
          pm2 … kommt aber ueber autosetup.sh auf eine frische Karte" —
          beide Haelften falsch.
        * `dokumentation/mixpibox.md` §4.4 „Zwei Verwaltungen": beschrieb eine
          Wahl zwischen zwei Dingen, von denen es nur noch eines gibt, und
          nannte als Schalter `MUPI_ADMIN` — den es im Baum nicht mehr gibt
          (einzige Fundstelle ist `MUPI_ADMIN_DEBUG` in
          `harness/admin-entrypoint.sh`, anderer Name, andere Sache).
        * `BACKLOG.md` G7 stand auf „TEILERLEDIGT … der Auslieferungsweg
          selbst steht unveraendert", waehrend genau dieser Weg seit vier
          Tagen leer war.
        * `tools/readme-behauptungen-pruefen.sh` verlangte `AdminInterface`
          als Pfad und meldete FEHLT — der einzige rote Punkt des Laufs zeigte
          auf eine Luecke im PRUEFER, nicht in der README.

      ══ DIE REGEL DAHINTER ═════════════════════════════════════════════════
      Ein Ausbau ist erst fertig, wenn die Doku ihn im PRAETERITUM erzaehlt.
      Solange irgendwo „ruft weiter" steht, sucht der naechste Leser nach
      etwas, das es nicht gibt — und die Probe faellt darauf nicht herein,
      weil sie nur prueft, ob ein Name VORKOMMT, nie ob er noch stimmt.
      `AUDIT-2026-08-22.md:217` wusste es bereits („gegenstandslos:
      AdminInterface/ existiert im Baum nicht mehr") und niemand zog nach.
    related: ["volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "readme-zahlen-waren-um-das-sechsfache-daneben"]

  - id: geschlossenes-vokabular-ohne-konsumenten
    title: "SEKTIONEN: zwei von fuenf Werten haengen ins Leere - und ein Ort hat gar keinen Wert"
    kind: architektur
    match: "sektion haengt nirgends|plugin steckleiste unsichtbar|mixpi-archive sektion medien|streaming/deezer abgelehnt|SEKTIONEN vokabular"
    sev: mid
    tags: [plugins, doku, wachen, sektionen, 2026]
    stand: "23.08.2026 gemessen; seit 25.08.2026 sind alle drei Faelle als ABSICHT in plugins/README.md ausgenommen, die Wache meldet gruen — der Befund selbst (ton/medien ohne Ort) bleibt offen"
    source: "Stuendlicher Doku-Lauf; alle elf bisherigen Wachen meldeten gruen"
    body: |
      `tools/doku-luecken-probe.sh` haelt seit Tagen `RECHTE` und `EREIGNISSE`
      aus `src/backend-api/src/plugin-vertrag.ts` gegen die Handbuecher.
      Dieselbe Datei fuehrt DREI weitere geschlossene Listen — `SEKTIONEN`,
      `FELDARTEN`, `KONFIG_GRUPPEN` — und keine stand in einer Wache.
      `streaming/jellyfin` hatte in `plugins/README.md` null Treffer, waehrend
      der Abschnitt daneben `SEKTIONEN` „ein geschlossenes Vokabular" nennt
      und es nicht aufzaehlt: wer ein Plugin baut, musste den TypeScript
      aufmachen.

      ══ DER GROESSERE FUND ═════════════════════════════════════════════════
      Eine reine Namenspruefung haette das mit drei Zeilen README geschlossen
      — und zwei tote Werte durchgewunken. Die EINZIGE Stelle, die Plugins
      nach `sektion` einhaengt, ist `<mixpi-plugin-abschnitt>` in
      `src/frontend-admin/src/app/seiten/streaming.ts`. Damit gilt:

        * `ton` und `medien` sind Vokabular ohne Ort. Die Ton-Seite baut ihren
          Abschnitt „Klangwerk" aus dem RECHT `klang` (`klangPluginsLaden()`
          filtert `p.rechte.includes('klang')`), nicht aus der Sektion; die
          Medien-Seite ruft ihre Plugins beim Namen
          (`/api/plugins/mixpi-ardsounds/http/…`).
        * Das trifft schon ein ausgeliefertes Plugin:
          `plugins/mixpi-archive/plugin.json` meldet sich unter
          `"sektion": "medien"` an. `manifestPruefen` nimmt es an, und die
          Steckleiste ist trotzdem nirgends zu sehen.
        * Umgekehrt fehlt `streaming/deezer`: die Streaming-Seite baut ihre
          Anker aus `'streaming/' + anbieter.id`, und `streaming.ts` im
          Backend fuehrt VIER Anbieter (`spotify`, `jellyfin`, `ardsounds`,
          `deezer`). Im Vokabular stehen drei. ACHTUNG vor der schnellen
          Korrektur: `plugin-vertrag.spec.ts:379` nagelt genau diese Ablehnung
          als Gegenbeispiel fuer einen unzulaessigen Wert fest — wer `deezer`
          aufnimmt, nimmt dem Test sein Gegenbeispiel weg. Das ist eine
          Entscheidung, kein Versehen.

      ══ DIE REGEL DAHINTER ═════════════════════════════════════════════════
      Eine Doku-Wache fragt „steht der Name irgendwo?". Die Frage daneben ist
      „hat der Name einen KONSUMENTEN?" — und die stellte bis hierher keine.
      Ein Wert, den die Pruefung annimmt und niemand einloest, ist schlimmer
      als ein undokumentierter: der Plugin-Bauer haelt sich an den Vertrag,
      bekommt gruenes Licht und sieht nichts. Beim Erweitern einer Wache
      deshalb nicht nur fragen, woher die Liste kommt
      (`doku-wachen-zaehlen-endpunkte-nicht-ihre-form`), sondern auch, wer sie
      am anderen Ende liest.

      Gewacht von `tools/sektionen-deckung.py` (beide Richtungen, laeuft in
      `doku-luecken-probe.sh` mit). Die Wache warnt ausdruecklich, wenn sie
      GAR KEINEN `[sektion]`-Anker findet — sonst meldete sie gruen, sobald
      jemand die Komponente umbenennt. Beide Gegenproben (Anker umbenannt,
      `SEKTIONEN` zu `SEKTIONENX`) loesen sie aus.

      ══ NACHTRAG 25.08.2026: DIE WACHE WAR SEIT IHREM ERSTEN TAG ROT ════════
      Alle drei Befunde oben sind ABSICHT und standen schon im selben Commit
      (76ac10e8) in `plugins/README.md` begruendet — die Wache konnte also nie
      gruen werden, und `doku-luecken-probe.sh` meldete zwei Tage lang in jedem
      Lauf „1 LUECKE(N)". Ein VIERTER, echter Befund waere in derselben Zeile
      untergegangen. Seit dem 25.08. fuehrt die Wache keine eigene
      Ausnahmeliste, sondern liest die Marken, die in der README schon
      dastanden (`**nirgends**` in der Wert->Ort-Tabelle, „Umgekehrt fehlt
      `…`") — und prueft die Ausnahme in der GEGENRICHTUNG mit
      (`DOKU VERALTET`, sobald jemand `ton` einhaengt oder `deezer` aufnimmt,
      ohne die README nachzuziehen). Vier Sabotagen einzeln gegengeprobt. Die
      Lehre getrennt unter [[dauerrote-wache-ist-keine]]. WICHTIG fuer den,
      der hier weiterliest: gruen heisst seither „begruendet hingenommen",
      NICHT „behoben" — `ton` und `medien` haengen weiterhin ins Leere.
    related: ["volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "doku-wachen-zaehlen-endpunkte-nicht-ihre-form", "gegenprobe-statt-gruen-glauben", "dauerrote-wache-ist-keine"]

  - id: doku-zeigt-auf-dateien-die-es-nicht-gibt
    kind: falle
    title: "Zwoelf Doku-Wachen fragten, ob ein Name DASTEHT — keine, ob das Ziel noch EXISTIERT"
    match: "No such file or directory.*tools/|Pfad im Wiki|nachmessen mit tools/|steht im Wiki aber"
    sev: high
    tags: ["doku", "wiki", "wache", "pfade", "umzug", "subtree", "falle", 2026]
    source: >
      Doku-Lauf 23.08.2026. Gemessen mit der neuen Wache
      `tools/doku-pfade-pruefen.py` gegen `llmwiki/pack.yaml`, `README.md`,
      `plugins/README.md` und `dokumentation/mixpibox.md`: 21 Fundstellen,
      davon 4 tote Pfade und 17 mehrdeutige.
    body: >
      DIE LUECKE IN DER FRAGESTELLUNG: `doku-luecken-probe.sh` fuehrte zwoelf
      Wachen, und alle zwoelf fragen dasselbe in EINE Richtung — steht der
      Name aus dem Code irgendwo in einem Handbuch? Die Gegenrichtung stellte
      keine: GIBT ES NOCH, WORAUF DIE DOKU ZEIGT?

      Der Unterschied ist nicht akademisch. Ein Handbuch, das ein neues Plugin
      nicht nennt, ist unvollstaendig — der Leser merkt es und sucht weiter.
      Ein Wiki-Eintrag, der zum Nachmessen ein Werkzeug empfiehlt, das es
      nicht gibt, ist SCHLIMMER: der Leser tippt es ab, bekommt „No such
      file", und weiss danach weder, ob die Messung je stattfand, noch wie er
      sie wiederholt.

      ══ DIE VIER TOTEN PFADE ═══════════════════════════════════════════════

        tools/rechte-am-geraet.sh    stand seit dem 07.08. als „Nachmessen:"
                                    in `endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok` — und gab es im ganzen Baum nie. Ausgerechnet in dem Eintrag,
        der schon eine Korrektur einer falschen „behoben in"-Zusage traegt.
        Am 23.08. aus den im Eintrag protokollierten Zahlen NEU GEBAUT (nur
        lesend, `ifdown --no-act`, jedes sudo mit `-n` wegen
        [[sudo-ohne-terminal-sperrt-das-konto]]).

        src/backend-api/src/mitschnittliste.ts  -> plugins/mixpi-mitschnitt/liste.mjs
        Der lehrreiche Fund: die E66-Warteliste ist am SELBEN TAG, an dem der
        Eintrag geschrieben wurde, mit E73 (b8abd047) aus dem KERN in ein
        PLUGIN gezogen. Wer dem Wiki folgt, sucht ein Stueck Architektur da,
        wo es nicht mehr wohnt — und schliesst womoeglich, es gaebe es nicht.

        scripts/mupibox/mqtt.py      -> scripts/mqtt/mqtt.py   (vertauscht)
        scripts/mupi-lautstaerke.sh  -> scripts/mupibox/mupi-lautstaerke.sh

      ══ DIE ZWEITE KLASSE: DIE UMZUGS-SCHULD ═══════════════════════════════
      Am 10.08.2026 sind `remote-step-installer/` und `llmwiki/` per
      `git subtree` in den Hauptbaum gezogen (siehe `ZUGEZOGEN.md`). Damit
      wurde ein `tools/qr.py`, das im alten EIGENEN Installer-Repo eindeutig
      war, hier zweideutig: `tools/` gibt es im Hauptbaum auch — mit ueber
      440 anderen Dateien und ohne qr.py. SIEBZEHN solcher Pfade standen in
      Paket und Handbuch (zwoelf Installer-Werkzeuge, dazu `src/server.ts`,
      `src/spotify-control.ts`, `src/test.ts`, `src/auth.integration.spec.ts`,
      `src/app/seiten/darstellung.ts` aus der Zeit vor der Aufteilung in
      backend-api/backend-player/frontend-*).

      DAS IST DIE TEUERE SORTE, weil sie nicht wie ein Fehler aussieht: der
      Pfad zeigt auf einen ECHTEN Ordner, nur den falschen. Er wird abgetippt,
      findet nichts, und der Leser haelt den Eintrag fuer alt statt fuer
      falsch adressiert. Ein Umzug bezahlt sich nicht mit dem Verschieben,
      sondern mit den Verweisen darauf — und die standen in keiner Wache.

      ══ DIE REGEL ══════════════════════════════════════════════════════════
      Wer eine Datei verschiebt, umbenennt oder in ein Plugin zieht, hat die
      Doku noch nicht nachgezogen, wenn er den Namen dort eingetragen hat: er
      muss die ALTEN Verweise suchen. Und wer eine Doku-Wache baut, frage
      NEBEN „steht der Name da?" immer auch „zeigt der Verweis noch irgendwo
      hin?" — das ist dieselbe zweite Frage wie bei
      [[geschlossenes-vokabular-ohne-konsumenten]], nur andersherum: dort
      fehlte dem Namen der Leser, hier dem Verweis das Ziel.

      ══ DERSELBE LAUF, DIESELBE BAUART, EINE ETAGE TIEFER ══════════════════
      `tools/pack-verweise.py` prueft seit dem 08.08.2026, ob jedes `[[…]]`
      im Paket eine Kennung trifft, und meldete seither gruen. ZWEI Verweise
      waren dabei ueber den Zeilenrand gerutscht
      (`[[benennung-mixpibox-` + Umbruch + `drei-toepfe]]`). YAML faltet den
      Umbruch zu einem LEERZEICHEN, und das Muster der Wache
      (`[a-z0-9][a-z0-9-]*`) kannte keinen Leerraum: sie sah die Verweise GAR
      NICHT. Gruen, weil sie nichts fand — nicht, weil nichts kaputt war.
      Beides berichtigt: die Verweise stehen wieder in einer Zeile, und die
      Wache faltet den Leerraum zurueck und meldet den Knick ausdruecklich.

      ══ DIE DRITTE KLASSE: DER PFAD, DEN ES NIE GAB ════════════════════════
      Doku-Lauf 27.08.2026, gemeldet von derselben Wache: ein Verweis war
      weder umgezogen noch zweideutig — er war ein SAMMELNAME, in Prosa
      gebildet und nie ein Pfad. Der Kritiker-Eintrag vom selben Tag fasste
      drei Auswertungsskripte unter `scripts/mupihat/` (`parse_log.py`,
      `plot_charge.py`, `plot_discharge.py`) zu einem „…-Plots" zusammen und
      schrieb den Ordnernamen davor. Das liest sich wie ein Pfad, ist einer
      der Form nach, und zeigt doch auf nichts. Die begleitende Aussage —
      drei Werkzeuge ohne Aufrufer — stimmte; nur ihre Adresse nicht.
      DAS IST DIE HEIMTUECKISCHE VARIANTE, weil kein Umzug ihr vorausgeht:
      es gibt keinen Commit, an dem man sie haette bemerken muessen, und
      keine alte Fassung, in der der Verweis noch gestimmt haette. Sie
      entsteht beim SCHREIBEN, aus dem Wunsch nach Kuerze. Die Regel dazu
      ist knapp: mehrere Dateien werden AUFGEZAEHLT, nicht getauft. Wer in
      Doku-Prosa einen Sammelbegriff bildet, gebe ihm keine Pfadgestalt —
      sonst tippt ihn jemand ab. Berichtigt am 27.08. durch Ausschreiben
      aller drei Namen; die Wache meldet den Eintrag seither gruen.

      ══ WIE DIE WACHE MIT DIESEM EINTRAG UMGEHT ════════════════════════════
      Beim ersten Lauf meldete sie genau DIESEN Eintrag rot — er zaehlt ja die
      toten Pfade auf. Die Ausnahme gilt darum JE QUELLE und nur fuer die hier
      genannten Pfade: taucht `scripts/mupibox/mqtt.py` irgendwo sonst wieder
      auf, ist es wieder rot. Eine pauschale Ausnahme fuer den ganzen Eintrag
      waere ein Loch, das mit ihm mitwuechse.

      Gewacht von `tools/doku-pfade-pruefen.py` (laeuft in
      `doku-luecken-probe.sh` mit). Die Ausnahmeliste dort traegt je Zeile
      ihren Grund — Platzhalter aus Anleitungen, Laufzeitablage unter
      `server/config/`, fremder Quelltext aus node_modules. Wer eine Ausnahme
      nicht begruenden kann, hat einen Fund. Gegenproben: eine zitierte Datei
      umbenannt (meldet TOT), der Paketschluessel `entries` umbenannt (meldet
      WARNUNG statt abzustuerzen).
    related: ["geschlossenes-vokabular-ohne-konsumenten", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "behoben-in-ist-eine-zustandsaussage-und-war-falsch", "wiki-zustand-hat-haltbarkeit"]

  - id: sudo-ohne-terminal-sperrt-das-konto
    kind: falle
    title: "Ein `sudo` aus einem Hintergrundprozess sperrt nach drei Laeufen das Konto"
    match: "faillock|Account locked due to|incorrect password attempts|sudo: no tty present|a password is required"
    sev: high
    cause: >
      `sudo` ohne Terminal kann nicht nach dem Passwort fragen und zaehlt den
      Versuch trotzdem als Fehlschlag. pam_faillock zaehlt mit; nach dem
      voreingestellten Limit ist das Konto gesperrt — auch fuer den Menschen,
      der danach am Rechner sitzt.
    fix: >
      In allem, was ohne Terminal laeuft (Tests, Wachen, Werkzeuge, Code, den
      man lokal gegen die Box laufen laesst), AUSSCHLIESSLICH `sudo -n`: es
      scheitert sofort und ohne Fehlversuch. Sperre nachsehen und loesen mit
      `faillock --user <name>` bzw. `faillock --user <name> --reset`.
    tags: ["sudo", "faillock", "tests", "werkzeug", "falle", 2026]
    source: >
      Am Entwicklungsrechner erlebt: lokal laufender MuPiBox-Code rief `sudo`
      ohne `-n`, drei Laeufe genuegten, und das Konto `achim` war gesperrt.
    body: >
      DIE FALLE IST DIE RICHTUNG: man erwartet, dass ein `sudo` ohne Rechte
      einfach NICHTS tut. Es tut aber etwas — es erzeugt einen Fehlversuch.
      Ein Werkzeug, das man zwanzigmal laufen laesst, um eine Messung zu
      wiederholen, sperrt dabei das Konto, und die Sperre sieht danach aus wie
      ein ganz anderes Problem („mein Passwort geht nicht mehr").

      WO ES BESONDERS BEISST: Code, der FUER DIE BOX geschrieben ist, wo
      `sudo` selbstverstaendlich ist, und der zum Messen einmal LOKAL laeuft.
      Auf der Box steht NOPASSWD, hier nicht. Dieselbe Zeile ist dort harmlos
      und hier teuer. Dieselbe Klasse wie ein `reboot` im getesteten Code: was
      auf dem Zielgeraet richtig ist, braucht beim Messen auf dem
      Entwicklungsrechner einen Riegel — eine benannte Naht ODER einen
      Umgebungsschalter, nie das blosse Vertrauen darauf, dass es schon nicht
      ausgeloest wird.
    related: ["endpunkt-ohne-sudo-schaltet-nichts-und-sagt-ok", "doku-zeigt-auf-dateien-die-es-nicht-gibt"]

  - id: doku-schickt-zur-falschen-oberflaeche
    title: "Die Beschriftung ist echt — nur auf der anderen Oberflaeche"
    match: "Benutzerhandbuch|Beschriftung|Knopf heisst|Verwaltung statt Box|Ton anmelden|Code anzeigen"
    sev: medium
    cause: >
      Alle Doku-Wachen fragten, ob ein Name aus dem CODE in einem Handbuch
      steht. Keine fragte, ob es den KNOPF gibt, den die Doku ZITIERT — und
      ob er auf der Oberflaeche sitzt, in die der Satz den Leser schickt. Die
      Box hat ZWEI Oberflaechen (`src/frontend-admin` und `src/frontend-box`);
      eine Beschriftung der einen, der anderen zugeschrieben, kommt durch jede
      Namenspruefung, weil das Wort im Baum steht.
    fix: >
      `python3 tools/beschriftungen-deckung.py` (laeuft in
      `tools/doku-luecken-probe.sh` mit). Sie liest die Zitate in deutschen
      Anfuehrungszeichen aus `dokumentation/benutzerhandbuch.html`, bestimmt je
      Zitat die gemeinte Oberflaeche nach dem NAECHSTEN VORGAENGER (Menuepunkt
      aus `rahmen.ts` oder eine Box-Wendung) und haelt die Beschriftung gegen
      genau diese Oberflaeche. OBERFLAECHE = Wort existiert, aber drueben;
      ERFUNDEN = nirgends.
    tags: ["doku", "wache", "benutzerhandbuch", "oberflaeche", "falle", 2026]
    source: >
      Doku-Lauf 23.08.2026: dreizehn Wachen meldeten gruen, und das
      Benutzerhandbuch schickte an FUENF Stellen in die Verwaltung fuer
      Ablaeufe, die es nur an der Box gibt.
    body: >
      DIE DREI FUNDE, alle nach demselben Muster. „In der Verwaltung:
      Streaming-Dienste -> Spotify -> ,Ton anmelden' -> ,Code anzeigen'" —
      beide Beschriftungen stehen in `frontend-box/app/spotify`; in der
      Verwaltung heisst der Knopf `Code holen` und sitzt im Abschnitt
      `Lautsprecher (Ton)` von `seiten/spotify-assistent.ts`. Dasselbe bei den
      Themen: „Voreinstellungen bearbeiten" -> „Original" ist die Box-Seite;
      die Verwaltung kennt nur `Zuruecksetzen`, und das nimmt die GANZE
      Darstellung, nicht ein Thema. Dazu eine gekuerzte Beschriftung:
      „Im Netz suchen" heisst wirklich `Jellyfin im Netz suchen`.

      WARUM DAS TEURER IST ALS EINE FEHLENDE ZEILE: wer eine Seite im Handbuch
      vermisst, merkt es sofort. Wer der Anweisung folgt, sucht in der
      Verwaltung nach einem Knopf, den es dort nicht gibt, und haelt danach die
      Software fuer kaputt statt die Doku.

      ZWEI FALLEN BEIM BAUEN DER WACHE, beide beim Messen aufgefallen. Erstens:
      der blosse Teilstring winkt kurze Beschriftungen durch — „Original" steht
      in `verschmelzung.ts` mitten in einem Erklaersatz. Die Wache verlangt
      seither, dass der Text als Beschriftung dasteht (allein auf seiner Zeile
      oder von `>`/Anfuehrungszeichen umschlossen). Zweitens: ein Absatz nennt
      regelmaessig BEIDE Oberflaechen nacheinander, und ein Zitat enthaelt
      manchmal selbst einen Menuenamen („Ton anmelden" enthaelt `Ton`) — beim
      Bestimmen der Oberflaeche werden Zitate darum laengentreu ausgeblendet.

      Zitate in Absaetzen OHNE Menuepunkt werden nicht geprueft: das Handbuch
      zitiert auch Schalteraufdrucke der Platine und Ausgaben fremder
      Werkzeuge. Die vier verbleibenden Ausnahmen tragen ihren Grund im
      Quelltext, und eine Ausnahme, auf die kein Zitat mehr passt, wird
      GEMELDET statt still mitgeschleppt.
    related: ["doku-zeigt-auf-dateien-die-es-nicht-gibt", "geschlossenes-vokabular-ohne-konsumenten", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku"]

  - id: doku-zitiert-endpunkte-die-es-nicht-gibt
    title: "Der Endpunkt in der Doku — und die Route, die es nie gab"
    match: "api-doku-deckung|Endpunkt in der Doku|Route gibt es nicht|/api/ard/inhalt|Gegenrichtung Endpunkt"
    sev: medium
    cause: >
      `tools/api-doku-deckung.sh` zaehlt 195 Routen aus dem Backend und fragt
      fuer jede, ob die Doku sie nennt — Richtung CODE -> DOKU. Die
      GEGENRICHTUNG prueft niemand: ein `/api/…`, das in der Doku steht und im
      Backend fehlt, verbessert dort sogar die Trefferquote.
      `tools/doku-pfade-pruefen.py` stellt die Gegenfrage, aber nur fuer
      DATEIEN. Ein Endpunkt ist kein Pfad im Baum und faellt durch beide Netze.
    fix: >
      `python3 tools/endpunkt-zitate-pruefen.py` (laeuft in
      `tools/doku-luecken-probe.sh` mit). Sie sammelt jedes `/api/…`-Zitat aus
      den drei Handbuechern und haelt es gegen die Routen aus
      `src/backend-api/src` — Platzhalter auf BEIDEN Seiten aufgeweitet
      (`:schluessel`, `<s>`, `{kennung}`), Zitate mit `/` am Ende als
      Namensraum gewertet.
    tags: ["doku", "wache", "rest", "gegenrichtung", "falle", 2026]
    source: >
      Doku-Lauf 23.08.2026 (21:xx): vierzehn Wachen meldeten gruen, und
      `plugins/README.md` schickte zum Nachlesen an eine Route, die es nie gab.
    body: >
      DER FUND. Der Abschnitt „Ohne Rueckfall auf einen Kern-Weg" begruendet,
      warum der ARD-Zweig seit E78 nur noch ueber `pluginInhalt()` geht, und
      belegt das mit: „Der Grund steht bei `/api/ard/inhalt` in `server.ts`".
      Diese Route existiert nicht — `grep -rn "ard/inhalt" src/` liefert null
      Treffer. Gemeint ist `/api/werke/:schluessel/inhalt` (server.ts ab
      10635). Wer die Regel nachpruefen will, greppt den zitierten Namen,
      findet nichts und haelt entweder den Abschnitt fuer alt oder die Regel
      fuer unbelegt. Zweiter Fund derselben Sorte: `/api/bild` als
      Fehlersuchtipp — die Route heisst `/api/bild/:schluessel`, ein Aufruf
      des Zitats gaebe 404 und liesse den Sucher an der falschen Stelle graben.

      DIE TEUERSTE FALLE BEIM BAUEN: NICHT JEDE ROUTE IST EIN TEXT. Die erste
      Fassung las Routen wie `api-doku-deckung.sh`, als Zeichenkette in
      `app.get('/api/…')`, und meldete `/api/plugins/<kennung>/http/` an FUENF
      Stellen als tot. Die Route steht in `server.ts:2559` und ist ein
      regulaerer Ausdruck (`app.all(/^\/api\/plugins\/([a-z][a-z0-9-]{2,63})\/http\/(.*)$/)`),
      weil das Reststueck frei ist — es ist die Durchreiche des ganzen
      Plugin-Systems. Fuenf Falschmeldungen auf zwei echte Funde. Regex-Routen
      werden seither eingesammelt und ein Zitat gegen sie PROBIERT, mit einem
      Wert, der durch die Kennungsregel passt (`beispiel`, nicht `x` — drei
      Zeichen Mindestlaenge). Dieselbe Blindheit hat `api-doku-deckung.sh` bis
      heute; sie faellt dort nur nicht auf, weil jene Richtung zu WENIG
      einfordert statt zu viel.

      `llmwiki/pack.yaml` steht bewusst NICHT in der Quellenliste, obwohl
      `api-doku-deckung.sh` es mitliest: das Paket ist zu grossen Teilen Prosa
      ueber FRUEHERE Zustaende und fuehrt bewusste Nicht-Beispiele
      (`/api/gibtesnicht`, `/api/xyz` als Muster fuer 404-Tests). Eine Wache,
      die das anmahnt, meldet auf Dauer rot und wird zu Recht ignoriert —
      dieselbe Falle wie `RECHTE` im Benutzerhandbuch. Widerrufene Aussagen im
      Paket bleiben Sache von `tools/doku-widerruf-probe.sh`.
    related: ["doku-zeigt-auf-dateien-die-es-nicht-gibt", "doku-schickt-zur-falschen-oberflaeche", "doku-wachen-zaehlen-endpunkte-nicht-ihre-form"]

  - kind: lesson
    id: mitschnitt-nahm-den-lautsprecher-auf
    title: "Der Mitschnitt griff den Lautsprecher ab, nicht den eigenen Strom"
    sev: high
    tags: [mitschnitt, pipewire, plugin, geraet, messen, zeugen]
    body: |
      Am 22.08.2026 zwischen 19:06 und 19:13 entstanden vier Mitschnitte mit
      DIGITALER NULL — mittel = spitze = -91,0 dB, ueber die VOLLE Laufzeit
      (175 s, 61 s, 89 s, 179 s). Sie galten als `fertig` und lagen als
      spielbare Kacheln in der Mediathek. Der Betreiber meldete es als
      „101 Meerjungfrauen spielt nicht".

      ══ ZWEI FEHLER, DIE EINANDER GEDECKT HABEN ════════════════════════════
      1. `pw-record` lief OHNE `--target`. Dann sucht es sich die Standard-
         quelle SELBST, und das ist der LAUTSPRECHER-MONITOR. Am Geraet
         vorgefuehrt (23.08.2026), drei Laeufe nebeneinander:
             --target <object.serial>  ->  |<- mixpi-mitschnitt:monitor_FL
             --target <id>             ->  |<- mixpi-mitschnitt:monitor_FL
             OHNE --target             ->  |<- alsa_output…fallback:monitor_FL
      2. Der `pw-link` dahinter sprach den Quellknoten ueber den NAMEN an.
         BEIDE Soloist-Instanzen heissen `spotify`. Die saubere Aufloesung
         ueber die Prozesskennung (`knotenFuerPid`) lieferte `{id, name}` —
         und der Aufrufer warf die Eindeutigkeit genau dort weg.

      ══ WARUM ES MONATE UNBEMERKT BLIEB ════════════════════════════════════
      Solange die Familie hoerte, kam am Lautsprecher-Monitor Ton an: die
      Datei sah gesund aus und enthielt DAS FALSCHE — die Familienwiedergabe.
      Nachts lief nur EIN Soloist, der Name war eindeutig, und alle 31
      Aufnahmen der Nachtcharge wurden gut.

      DIE SIGNATUR, an der man es erkennt: eine gute Aufnahme beginnt mit
      `silence_start: 0` (Vorlaufstille, dann Ton). Die kranken haben KEINE
      Vorlaufstille — der Ton lief schon, als der Mitschneider aufmachte. Zwei
      als gesund gefuehrte Dateien desselben Abends sind so zur Haelfte still:
      213 s mit Stille ab 119 s, 206 s mit Stille ab 152 s. Die Grenze liegt
      nicht bei 19:02/19:06, sondern bei 19:01:17 — mitten IN einer Datei, und
      genau dann hoerte die Familie auf.

      ══ WARUM DIE WACHE NICHT ANSCHLUG ═════════════════════════════════════
      Der Arbeiterweg hatte als einzige Inhaltspruefung `gross < 10 000 B`.
      Eine digital stille FLAC wiegt 177 B/s — an allen vier Fundstuecken
      derselbe Wert. Die Grenze faellt damit bei 56,5 SEKUNDEN; alles ab ~57 s
      Stille rutscht durch. EINE GROESSENPRUEFUNG MISST DIE KOMPRIMIERBARKEIT,
      NICHT DEN TON. Die echte Pegelpruefung (`pegelMessen`/`urteil`) gab es
      laengst — aber nur im passiven Weg.

      ══ SERIENNUMMER, NICHT ID ═════════════════════════════════════════════
      `pw-record --target` nimmt laut eigener Hilfe „node target serial or
      name". Dass Id und `object.serial` in der Messung uebereinstimmten
      (33 = 33), war Zufall. Wer die Id einsetzt, bekommt im Allgemeinen einen
      stillen Rueckfall auf die Standardquelle — also genau diesen Fehler.

      ══ ACHTUNG, DIESELBE EIGENHEIT IST WOANDERS DER VERTRAG ═══════════════
      Wer diesen Eintrag liest und im Baum nach `pw-record` ohne `--target`
      sucht, findet eine ZWEITE Fundstelle, die KEIN Fehler ist:
      src/backend-player/src/pegel.ts (E99, 30.08.2026) ruft absichtlich ohne
      `--target` auf. Dort soll gezeigt werden, „was die Familie gerade
      hoert" — Piper-Ansagen eingeschlossen; eine Welle, die bei einer Ansage
      flach bliebe, waere eine Welle, die luegt. Genau der Rueckfall auf den
      Monitor der Standard-Senke ist da das Gewuenschte.

      DIE UNTERSCHEIDUNG IST DER ZWECK, NICHT DER AUFRUF: ein ARCHIV will
      den einen Strom (`--target` auf die Seriennummer, Ansagen draussen),
      eine ANZEIGE will den Lautsprecher (kein `--target`). Wer pegel.ts
      „repariert", indem er `--target` ergaenzt, baut den Fehler ein, den
      dieser Eintrag verhindern soll — nur andersherum.
    related: ["was-in-einer-unreinen-funktion-steht-hat-keinen-zeugen", "gegenprobe-statt-gruen-glauben", "leinwand-misst-sich-selbst-nicht-nach"]

  - kind: lesson
    id: was-in-einer-unreinen-funktion-steht-hat-keinen-zeugen
    title: "Was zwischen spawn und Warteschleife steht, kann kein Zeuge erreichen"
    sev: high
    tags: [zeugen, gegenprobe, arbeitsweise]
    body: |
      Beim Reparieren der stillen Mitschnitte wurden drei Eingriffe gebaut und
      jeder einzeln sabotiert. Zwei fielen sofort auf. Der DRITTE — `--target`
      aus dem `pw-record`-Aufruf entfernen, also der SCHWERSTE Fehler des
      ganzen Befundes — liess ALLE 195 Tests GRUEN.

      Der Grund war nicht, dass die Zeugen schlecht waren. Der Aufruf stand
      mitten in `einenAufnehmen`, zwischen `spawn` und Warteschleife. Dorthin
      kommt kein Test, ohne einen Tonserver, einen Soloist und drei Minuten
      Wartezeit mitzubringen.

      DIE ANTWORT IST NICHT EIN GROESSERER TEST, SONDERN EINE KLEINERE
      FUNKTION. Die Argumentliste wanderte in `mitschnittArgumente()`, die
      Annahmeentscheidung in `annahmeUrteil()` — beide rein, beide in einer
      Zeile pruefbar. Danach faellt jede der fuenf Sabotagen:

          --target entfernen ................ 2 rot
          Seriennummer nicht mehr pruefen ... 1 rot
          Pegelpruefung entfernen ........... 3 rot
          Byte-Grenze entfernen ............. 1 rot
          Schwelle -50 auf -95 aufweichen ... 2 rot

      Das Muster gab es im selben Verzeichnis schon: `aufnahmeArgumente` fuer
      die Soloist-Schalter, mit denselben Nullrueckgaben bei fehlenden Feldern.
      Es war nur beim zweiten Prozess nicht wiederholt worden.

      DIE FRAGE, DIE MAN SICH STELLEN MUSS, ist nicht „habe ich getestet",
      sondern „welche Sabotage macht das rot". Wer darauf keine Antwort hat,
      hat keinen Zeugen — er hat eine gruene Zeile.
    related: ["mitschnitt-nahm-den-lautsprecher-auf", "gegenprobe-statt-gruen-glauben"]

  - id: doku-die-nur-sagt-was-nicht-hilft
    kind: falle
    title: "Doku, die nur nennt, was NICHT hilft, wird als „weglassen\" gelesen"
    match: "doku sagt was nicht geht|negative anweisung doku|target luegt|warnung statt anleitung|doku ursache eines fehlers"
    sev: high
    tags: ["doku", "wiki", "mitschnitt", "pipewire", "doku-lauf", 2026]
    source: >
      23.08.2026, Doku-Lauf nach e67d6ed5. Der Satz stand seit dem 16.08. in
      plugins/README.md und im Wiki-Titel von pipewire-abgriff-je-quelle.
    body: |
      Der ueberholte Satz, hier als Zitat und NICHT als Anweisung —
      `plugins/README.md` sagte sieben Tage lang: „`--target` luegt,
      `PIPEWIRE_NODE` auch, nur `pw-link` trifft". Jedes Wort davon war am
      Geraet gemessen und richtig — gemessen war es mit der KNOTEN-ID.

      Der Code, der danach entstand, enthielt gar kein `--target` mehr. Ohne
      `--target` sucht `pw-record` sich die Standardquelle selbst: den
      Lautsprecher-Monitor. Ergebnis waren vier Mitschnitte mit digitaler Null
      und zwei halb stille, alle als `fertig` in der Mediathek
      (`mitschnitt-nahm-den-lautsprecher-auf`).

      DIE FALLE IST NICHT DIE MESSUNG, SONDERN DIE FORM. Ein Satz, der nur
      sagt, was nicht hilft, laesst genau eine Handlung offen: weglassen. Er
      nennt weder die Bedingung, unter der die Aussage gilt (mit der Id), noch
      was der Aufruf sonst noch tut (Rueckfall auf die Standardquelle, wenn er
      fehlt). Beim Lesen wird aus „hilft nicht" ein „schadet nicht".

      REGEL: eine Warnung braucht die Bedingung UND den Ersatz in derselben
      Zeile. Nicht „X luegt", sondern „X mit <id> luegt; mit <object.serial>
      ist X Pflicht, weil sonst still auf die Standardquelle zurueckgefallen
      wird". Wo das nicht in einen Satz passt, gehoert eine Tabelle mit den
      Faellen hin — bei diesem Befund drei Zeilen, die den ganzen Irrtum
      aufloesen.

      ZWEITER TEIL DESSELBEN LAUFS: „Die Daten sind dabei nicht kaputt" war
      als Aussage ueber DREI benannte Symptome gemeint und las sich als
      Blankoaussage ueber jeden Mitschnitt-Befund. Ein gueltiger FLAC-Kopf ist
      kein Beleg fuer Ton. Eine Entwarnung braucht ihren Geltungsbereich im
      selben Satz, sonst waechst sie.

      Beide Saetze stehen seither in `tools/doku-widerruf-probe.sh`.
    related: ["mitschnitt-nahm-den-lautsprecher-auf", "pipewire-abgriff-je-quelle", "doku-korrektur-nur-an-der-ersten-fundstelle"]

  - id: sackgasse-ohne-ausweg-in-der-tabelle
    kind: falle
    title: "Eine Tabellenzeile „nirgends\" ist eine Sackgasse, wenn der Ausweg 160 Zeilen entfernt steht"
    match: "sektion medien|sektion ton haengt ins leere|plugin erscheint nirgends|tabelle nirgends|doku sackgasse"
    sev: mid
    tags: ["doku", "plugins", "sektionen", "doku-lauf", 2026]
    source: >
      23.08.2026, Doku-Lauf nach fcd6d94e. Beide Proben meldeten sauber;
      der Befund kam aus dem Weiterlesen von `doku-die-nur-sagt-was-nicht-hilft`.
    body: |
      `plugins/README.md` fuehrt das geschlossene Vokabular `SEKTIONEN` als
      Tabelle und markierte zwei der fuenf Werte als „nirgends". Fuer `ton`
      stand der Ersatz im Kasten darunter (`rechte: ["klang"]`). Fuer `medien`
      stand dort nur, WARUM es nicht geht — „die Medien-Seite ruft ihre Plugins
      beim Namen" —, und daraus liest ein Plugin-Bauer: Medien-Plugins gehen
      nicht.

      SIE GEHEN. Der generische Weg heisst E87 und stand in DERSELBEN Datei,
      rund 160 Zeilen weiter oben: ein Eintrag `type: "plugin"` mit der vollen
      Medienkennung in `id` traegt Verlauf, Weiterhoeren, Favoriten und seit
      E90 die gemerkte Stelle — ohne Kernaenderung. Hart verdrahtet ist allein
      die SUCHMASKE in `seiten/medien.ts` (`mixpi-ardsounds` und
      `mixpi-archive` stehen dort woertlich in den URLs). Spielen ist
      generisch, Finden noch nicht — zwei verschiedene Aussagen, und die
      Tabellenzeile fasste sie zu einem „nirgends" zusammen.

      DIE FALLE IST DIE ENTFERNUNG. `doku-die-nur-sagt-was-nicht-hilft` sagt:
      Bedingung und Ersatz in denselben Satz. Der Fall hier ist die schwaechere
      Variante davon, die keine Probe findet: der Ersatz IST dokumentiert, nur
      nicht dort, wo die Sackgasse steht. Wer die Tabelle liest, liest den
      Kasten 160 Zeilen hoeher nicht — Tabellen werden gescannt, nicht
      durchgelesen.

      REGEL: eine Zeile, die „geht nicht" sagt, traegt den Verweis auf den Weg,
      der geht, IN DER ZEILE. Nicht „siehe unten", sondern „nimm den E87-Weg,
      siehe unten". Und: pruefen, ob das „geht nicht" zwei Faelle vermengt —
      hier Spielen (geht) und Finden (geht nicht).
    related: ["doku-die-nur-sagt-was-nicht-hilft", "geschlossenes-vokabular-ohne-konsumenten", "gemerkte-stelle-fuer-plugin-inhalt"]

  - id: fehlt-noch-verdeckt-den-weg-der-schon-geht
    kind: falle
    title: "Ein „fehlt noch\" ueber die EIGENE Flaeche wird als „Inhalte gehen nicht\" gelesen"
    match: "flaeche die der kinderschirm erreicht|plugin kinderschirm geht nicht|uiextension gibt es nicht|was noch fehlt plugins|apirouteprovider kinderschirm"
    sev: mid
    tags: ["doku", "plugins", "e87", "doku-lauf", 2026]
    source: >
      24.08.2026, Doku-Lauf nach d570ee1a. Beide Proben meldeten sauber
      (nur die Paketzahl in der Kopfzeile war alt). Der Befund kam aus der
      Methode von `sackgasse-ohne-ausweg-in-der-tabelle`: den Eintrag des
      letzten Laufs als Muster nehmen und im Baum nach derselben Form suchen.
    body: |
      Gegriffen wurde nach „geht nicht|nirgends|fehlt noch|nicht zu bauen" in
      den drei Handbuechern. Drei Treffer trugen dieselbe Form:

        * `plugins/README.md`, Tabelle „Was noch fehlt", Zeile
          `ApiRouteProvider`: „Was noch fehlt, ist eine Flaeche, die der
          Kinderschirm erreicht."
        * `plugins/README.md`, Abschnitt „Was NICHT geht": „Eine Route, die der
          Kinderschirm erreicht, ist damit weiterhin nicht zu bauen."
        * `dokumentation/mixpibox.md`, Abschnitt 9: „Bei den Plugins fehlt noch
          eine Flaeche, die der Kinderschirm erreicht."

      Jeder Satz ist woertlich WAHR und trotzdem irrefuehrend. Was fehlt, ist
      eine Flaeche, auf der der Kinderschirm PLUGIN-CODE DIREKT fragt. Was seit
      E87 sehr wohl geht, ist der Kinderschirm mit Plugin-INHALT zu fuellen:
      ein Eintrag `type: "plugin"` in der Medienliste, aufgeloest ueber
      `/api/werke/<schluessel>/inhalt` (Zweig `plugin` in `server.ts`),
      abgespielt ueber `spielweg.ts`, angezeigt ueber `PluginInhaltService` in
      `src/frontend-box`. Kachel, Folgenliste, Verlauf, Weiterhoeren und seit
      E90 die gemerkte Stelle tragen mit.

      DER UNTERSCHIED ZUM VORGAENGER-BEFUND: dort stand ein „nirgends" ohne
      Ausweg. Hier steht ein „fehlt noch" ueber eine SCHMALE Sache (eigener
      Code am Kinderschirm), das der Leser auf die BREITE bezieht (ueberhaupt
      etwas auf den Kinderschirm bringen). Ein Kasten „Was noch fehlt" wird
      genau von dem gelesen, der entscheidet, ob er etwas bauen muss — die
      teuerste Stelle fuer eine zu weit gelesene Verneinung.

      REGEL: bei jedem „fehlt noch" pruefen, ob der Satz das Fehlende ENG genug
      benennt, dass die Nachbarschaft nicht mitverneint wird. Das Wort, das
      hier fehlte, war „eigene". Und wie beim Vorgaenger: der Verweis auf den
      Weg, der geht, gehoert IN DIE ZEILE, nicht in einen Abschnitt daneben.

      Mitgefunden und im selben Zug behoben: „`UiExtension` (Widgets) gibt es
      nicht" stand ohne Ersatz da, obwohl `icon` + `befinden()` + `aktionen` +
      `felder` die Steckleiste in der Verwaltung ohne fremdes JS ergeben —
      genau die Lehre aus dem Klangwerk, nur 150 Zeilen entfernt.
    related: ["sackgasse-ohne-ausweg-in-der-tabelle", "doku-die-nur-sagt-was-nicht-hilft", "gemerkte-stelle-fuer-plugin-inhalt"]
  - id: befund-der-oberflaeche-dem-server-zugeschrieben
    kind: falle
    title: "Ein Befund gehoert der SCHICHT, in der gemessen wurde — sonst baut jemand den Endpunkt nach"
    match: "kinderzeit regeln box-weit|regeln bleiben boxweit|zeitbudget box-weit|resume.json box-weit|listen.json ohne kennung|je kind noch nicht gebaut|api/kinderzeit kennt kein profil"
    sev: high
    tags: ["doku", "backlog", "profile", "kinderzeit", "doku-lauf", 2026]
    source: >
      24.08.2026, Doku-Lauf nach d967edc3. Beide Proben meldeten sauber. Der
      Befund kam wieder aus der Methode von
      `fehlt-noch-verdeckt-den-weg-der-schon-geht`: den Eintrag des letzten
      Laufs als Muster nehmen und nach derselben FORM greppen — hier
      "in Arbeit|noch nicht|steht aus|gibt es nicht" ueber die drei
      Handbuecher, danach "box-weit" ueber den ganzen Baum.
    body: |
      ZWEI ZITATE, beide widerlegt — sie stehen hier als Belegstuecke, nicht
      als Stand. `BACKLOG.md` fuehrte unter "Was fuer Stufe 2 offen bleibt": "Das
      ZEITBUDGET ist noch box-weit. Gemessen am 05.08.: der VERBRAUCH liegt je
      Profil, die REGELN nicht. Liam und Kalea teilen sich also dieselben
      Grenzen." Und `dokumentation/benutzerhandbuch.html` sagte im Kasten "Was
      gerade im Bau ist": "Eigener Bereich je Kind (Profile mit getrenntem
      Verlauf, Weiterhoeren und Kinderzeit je Kind) ist in Arbeit."

      DIE MESSUNG WAR ECHT UND DIE ZUORDNUNG FALSCH. Am 05.08. gemessen wurde
      die VERWALTUNGSSEITE: `seiten/kinderzeit.ts` haengt in `laden()` und
      `speichern()` den `wemAnhang()` nicht an, bearbeitet also immer
      `standard`. Zugeschrieben wurde der Befund dem SERVER — und der konnte
      es zu diesem Zeitpunkt schon drei Tage: `GET` UND `PUT
      /api/kinderzeit?profil=<kennung>` schreiben und lesen `RegelSatz.je`,
      `regelnFuer(satz, kennung)` zieht sie beim Stand heran, und
      `erbschaft.integration.spec.ts` misst es nach (d66ee6fc, 02.08.2026).

      DIE KOSTEN. Der Kommentar in `seiten/kinderzeit.ts` hatte den falschen
      Befund uebernommen und daraus eine Begruendung gemacht:
      "`/api/kinderzeit` kennt kein Profil; das hier vorzugeben waere eine
      Zusage, die der Server nicht einloest." So verhindert eine falsche
      Zuordnung genau den Bau, der noch fehlt — das FELD —, indem sie ihn als
      Server-Arbeit erscheinen laesst. Wer den Satz las und handelte, baute
      einen Endpunkt nach, der stand.

      Derselbe Fehler ohne die Zuordnungs-Haelfte, nur veraltet: drei Stellen
      im BACKLOG behaupteten `resume.json` sei box-weit ("nachweislich",
      "bestaetigt"), eine weitere dasselbe fuer `listen.json`. Beide stehen
      seit E18/S3 in `BEREICH_ABLAGEN` und liegen unter `profile/<kennung>/`;
      `/api/resume` liest `resumeLesePfad(profilAktiv())`.

      DAS BENUTZERHANDBUCH WIDERSPRACH SICH SELBST: Abschnitt 4 und 8 sagten
      "Verlauf, Weiterhoeren und Favoriten haben einen Besitzer" und "ein Tipp
      auf das Bild oben links wechselt das Profil", Abschnitt 11 sagte
      dasselbe sei "in Arbeit". Wer entscheidet, ob er bauen muss, liest den
      Kasten "Was gerade im Bau ist" — das ist die teuerste der beiden Stellen.
      Ausserdem verkaufte es die Wochentabelle als "Einstellungen je Kind",
      also den Fehler in beide Richtungen an einer Seite.

      REGEL: ein Befund traegt die SCHICHT, in der gemessen wurde. "Die Seite
      schickt keine Kennung mit" und "der Server kennt keine Kennung" sind zwei
      verschiedene Saetze, und nur der erste war gemessen. Wer einen offenen
      Punkt notiert, schreibt dazu, WO er offen ist — sonst waechst aus einem
      fehlenden Eingabefeld eine Server-Baustelle.

      MITGEFUNDEN: der Kommentar behauptete "die Tabelle unten gilt fuer alle
      Kinder, und das steht auch dort". Es stand dort NICHT — ein Kommentar,
      der einen Schutz als vorhanden ausgibt, ist selbst eine Verneinung, die
      niemand nachprueft. Der Satz steht jetzt unter der Tabelle.
    related: ["fehlt-noch-verdeckt-den-weg-der-schon-geht", "sackgasse-ohne-ausweg-in-der-tabelle", "doku-die-nur-sagt-was-nicht-hilft", "zaehlende-probe-sieht-keinen-widerspruch"]
  - id: ablage-tabelle-nennt-einen-ort-der-umgezogen-ist
    kind: falle
    title: "Eine Ablagen-Tabelle nennt einen ORT — ein Umzug macht sie still falsch, und zwei Tabellen widersprechen sich ungestraft"
    match: "listen.json box-weit|listen.json server/config|kinderzeit.json zeitkonten|welche datei liegt wo|server/config tabelle"
    sev: med
    tags: ["doku", "profile", "ablagen", "doku-lauf", 2026]
    source: >
      24.08.2026, Doku-Lauf nach dc1988f9. Beide Proben meldeten sauber, seit
      dem letzten Lauf lag kein neuer Commit im Baum. Der Befund kam wieder aus
      der Methode von `fehlt-noch-verdeckt-den-weg-der-schon-geht`: den Eintrag
      des letzten Laufs als Muster nehmen und nach derselben FORM greppen —
      hier "box-weit|fuer alle Kinder|kennt kein" ueber die drei Handbuecher.
    body: |
      DREI ZITATE, alle widerlegt — Belegstuecke, nicht Stand.
      `dokumentation/mixpibox.md` fuehrte in der Tabelle "Box-weit
      (server/config/)" die Zeile "`listen.json` | eigene Listen, box-weit
      (Form vor E18/S2)" — und 20 Zeilen weiter, in der Tabelle "Je Kind",
      dieselbe Datei noch einmal: "`listen.json` | die eigenen Listen". Beide
      Tabellen im selben Abschnitt, eine davon ueberholt.

      WARUM DAS NICHT AUFFIEL: die Klammer "(Form vor E18/S2)" ist als
      historische Notiz gemeint. Eine Tabelle wird aber GESCANNT — wer die
      Frage "wo liegt das?" hat, liest die Spalte links und die erste Haelfte
      rechts. Dieselbe Falle wie `sackgasse-ohne-ausweg-in-der-tabelle`, nur
      umgekehrt: dort fehlte der Ausweg in der Zeile, hier steht der WIDERRUF
      in der Zeile und wird trotzdem nicht mitgelesen. Ein Eintrag, dessen
      Aussage nur noch historisch gilt, gehoert NICHT in eine Tabelle, die
      einen Ist-Zustand aufzaehlt — er gehoert in die Zeile am neuen Ort
      ("seit E18/S3 hier, vorher box-weit").

      UND DER UNTERSCHIED ZUR NACHBARZEILE IST DER TEURE TEIL: `resume.json`
      steht mit Recht in BEIDEN Tabellen — am alten Ort liegt eine BRUECKE
      (ein Verweis auf den Bereich des aktiven Kindes, `resumeBrueckeRichten`),
      weil vier root-Skripte den alten Pfad nie verlassen. `listen.json` hat
      keine Bruecke: `bereich.integration.spec.ts` misst "und nichts wieder
      box-weit". Zwei Dateien, dieselbe Stufe, verschiedener Ausgang — genau
      dann traegt eine Tabellenzeile, die nur den Namen nennt, die falsche
      Vermutung weiter.

      ZWEITER TREFFER IN DERSELBEN TABELLE, andere Sorte: "`kinderzeit.json` |
      die Zeitkonten". Es ist der REGELSATZ `{standard, je}` — die KONTEN
      liegen je Kind in `kinderzeit-verbrauch.json`, zwei Tabellen weiter. Die
      Zeile verwechselte also nicht nur den Inhalt, sie versteckte auch das
      Feld `je`: dass die Regeln je Kind gehen (seit 02.08.2026,
      `regelnFuer()`), war aus der Tabelle nicht zu sehen. Damit setzte sie
      genau den Fehlschluss fort, den der Eintrag
      `befund-der-oberflaeche-dem-server-zugeschrieben` einen Lauf vorher
      aufgeloest hatte. (Auch dieses Zitat ist widerlegt.)

      DRITTER: `benutzerhandbuch.html` sagte "Diese drei gelten fuer alle
      Kinder gemeinsam". Woertlich richtig fuer die SEITE, gelesen als
      Eigenschaft der BOX. Der Ausweg (`PUT /api/kinderzeit?profil=`) stand 95
      Zeilen weiter unten. Jetzt nennt der Satz die Schicht ("stellt DIE SEITE
      fuer alle Kinder gemeinsam ein") und verweist in derselben Zeile.

      REGEL: nach einem Umzug nicht nur die NEUE Zeile schreiben, sondern nach
      dem Dateinamen greppen — eine Aufzaehlung von ORTEN hat immer eine
      Gegenstelle, und die alte bleibt stehen, weil sie beim Schreiben der
      neuen nicht im Blick ist. Und: ein "gilt fuer alle" ohne Subjekt ist
      dieselbe Aussage wie ein "geht nicht" ohne Schicht.
    related: ["befund-der-oberflaeche-dem-server-zugeschrieben", "sackgasse-ohne-ausweg-in-der-tabelle", "fehlt-noch-verdeckt-den-weg-der-schon-geht", "doku-korrektur-nur-an-der-ersten-fundstelle"]

  - id: ablage-an-zwei-orten-ohne-gegenstelle
    kind: falle
    title: "Eine Ablage liegt zu RECHT an zwei Orten — und keine der beiden Tabellenzeilen weiss von der anderen"
    match: "darstellung.json box-weit|darstellung.json je kind|themen liegen wo|OHNE_UMZUG|farbsatz je profil|welche ablage zieht nicht um"
    sev: med
    tags: ["doku", "profile", "ablagen", "darstellung", "doku-lauf", 2026]
    source: >
      24.08.2026, Doku-Lauf nach b104094d. Beide Proben sauber, kein neuer
      Commit seit dem Vorlauf. Der Befund kam erneut aus der Muster-Methode:
      den Eintrag des letzten Laufs (`ablage-tabelle-nennt-einen-ort-der-
      umgezogen-ist`) als Form nehmen und im selben Abschnitt nach weiteren
      Dateinamen suchen, die in BEIDEN Ablagen-Tabellen stehen.
    body: |
      DIE SCHWAECHSTE STUFE DER FORM, und sie ist die einzige, die eine
      zaehlende Wache niemals findet: der Ort ist RICHTIG, die Zeile ist
      RICHTIG — sie sagt nur nicht, dass es eine zweite gibt.

      `darstellung.json` steht in `dokumentation/mixpibox.md` in der Tabelle
      "Box-weit" UND in "Je Kind", und beides zu Recht. Sie ist die EINZIGE
      Ablage, die beim Start nicht in den Bereich umzieht (`OHNE_UMZUG` in
      `server.ts:1416`, begruendet in `profile.ts:837-853`): box-weit liegen
      die THEMEN — eine Palette gehoert der Box, wer eine baut teilt sie — und
      `aktuell` als Rueckfall fuer jedes Profil, das noch nichts Eigenes hat;
      je Kind liegt NUR `aktuell`, die Wahl dieses Kindes. Die Je-Kind-Datei
      entsteht ueberhaupt erst, wenn jemand fuer dieses Profil etwas
      verstellt (15.08.2026).

      WAS DASTAND (beide ueberholt, Belegstuecke): box-weit "Farbsaetze und
      der Rueckfall fuer Profile ohne eigene Wahl", je Kind "wie es fuer
      dieses Kind aussieht: Farbsatz, hell/dunkel, Kachelform". Kein Satz ist
      falsch. Aber eine Tabelle wird GESCANNT: wer die Frage "wo liegt das?"
      hat, findet einen Ort und hoert auf zu suchen. Die je-Kind-Zeile
      verspricht dabei den Farbsatz SELBST — wer ein Thema bearbeiten will,
      sucht am falschen Ort; und wer die box-weite Datei fuer einen Rest des
      Umzugs haelt und aufraeumt, nimmt allen Kindern die Paletten.

      DER ABSCHNITT ERKLAERT DEN UMZUG NUR ZUR HAELFTE: die Zeile
      `listen.json` sagt "der Umzug laesst nichts box-weit zurueck", und
      `resume.json` nennt die Bruecke. Es gibt also DREI Faelle — zieht um /
      zieht um mit Bruecke / zieht gar nicht um — und der dritte stand in
      keinem Handbuch. `OHNE_UMZUG` hatte ausserhalb von `BACKLOG.md` null
      Treffer.

      DIE WACHE DAZU: `tools/ablage-tabellen-gegenstelle.py` (laeuft in
      `doku-luecken-probe.sh` mit). Sie fragt nicht "steht der Name da?",
      sondern "weiss diese Zeile, dass es die andere gibt?". Beim Bauen die
      teure Falle: die erste Fassung las JEDEN Dateinamen in Backticks als
      Ablage — auch die im Beschreibungstext. Daraus wurde ein Phantom-Eintrag
      (`kinderzeit.json` grenzt sich gegen `kinderzeit-verbrauch.json` ab) und,
      schlimmer, ein ZUGEDECKTER echter Treffer: die Je-Kind-Zeile von
      `listen.json` zitiert `resume.json` als Gegenbeispiel und ueberschrieb
      damit dessen eigene Zeile, die tatsaechlich keine Gegenstelle nannte.
      Gelesen wird deshalb nur die erste SPALTE. Ein Name im Fliesstext ist
      ein Verweis, kein Ort.

      UND DIE GEGENPROBE MUSS DIE GANZE ZEILE TREFFEN: der erste Versuch nahm
      nur die Wendung "auch je Kind (siehe unten)" heraus und blieb gruen —
      im selben Satz stand noch das Zitat "alles je Kind liegt". Wer eine
      Wache mit einer Teilloeschung prueft, prueft die Wache nicht.

      REGEL: bei einer Aufzaehlung von ORTEN reicht es nicht, dass jede Zeile
      wahr ist. Sie muss auch sagen, was sie NICHT ist — sonst liest der
      Scanner sie als "hier und sonst nirgends". Der Verweis gehoert IN DIE
      ZEILE (`sackgasse-ohne-ausweg-in-der-tabelle`), und die Ausnahme von
      einer Regel gehoert dorthin, wo die Regel steht.
    related: ["ablage-tabelle-nennt-einen-ort-der-umgezogen-ist", "sackgasse-ohne-ausweg-in-der-tabelle", "fehlt-noch-verdeckt-den-weg-der-schon-geht", "befund-der-oberflaeche-dem-server-zugeschrieben", "gegenprobe-statt-gruen-glauben"]
  - id: shell-string-mit-medienpfad-im-player
    kind: falle
    title: "Der Player zaehlt Titel per Shell-String — ein Anfuehrungszeichen im Medienpfad bricht aus"
    match: "shell injection player|exec find media|decodeURIComponent exec|titelzaehlung find|spotify-control injektion|medienpfad shell"
    sev: high
    tags: ["sicherheit", "backend-player", "exec", "kritiker-lauf", 2026]
    source: >
      24.08.2026, Kritiker-Lauf (AUDIT-2026-08-24.md), HEAD b104094d. Fund des
      Backend-Pruefers, vom Hauptlauf selbst am Quelltext verifiziert
      (sed auf beide Stellen), nicht am Geraet ausprobiert.
    body: |
      `spotify-control.ts:1494` und `:1534` bauen die Titelzaehlung als
      Shell-String:

        find "/home/dietpi/MuPiBox/media/${decodeURIComponent(pfad)}" … | wc -l

      und geben ihn an `exec`. Der Pfad kommt aus der Kommando-URL des
      Players (unauthentifizierte GET-API). Ein `"` oder `$(…)` im Pfad
      verlaesst das Zitat — beliebige Befehle als Dienstnutzer, erreichbar
      aus dem LAN. Beide Stellen sind wortgleiche Kopien; die zweite kam
      als Kopie der ersten in den Baum (Warteschlangen-Zaehlung).

      DER FIX IST KLEINER ALS DAS PROBLEM: `execFile('find', [pfad, …])`
      nimmt den Pfad als Argument statt als Shell-Text — und weil dann kein
      `| wc -l` mehr geht, zaehlt man die Zeilen in JS, was die Kopie
      gleich mit eindampft. Vorsicht beim Umbau: der Kommentar an Stelle 1
      warnt zu Recht, dass ein WURF im exec-Rueckruf den ganzen Dienst
      beendet — Fehler dort nur loggen.

      WARUM DAS DREI AUDITS UEBERLEBT HAT: die Laeufe 21.-23.08. suchten
      tote Endpunkte, tote Dateien, Doppelungen — alles Fragen an NAMEN
      (wer ruft das?). Ein Injektionsrisiko ist eine Frage an den INHALT
      einer lebenden, viel benutzten Stelle. Wer nur Erreichbarkeit prueft,
      haelt die gefaehrlichste Zeile fuer die gesundeste, weil sie taeglich
      laeuft. server.ts selbst ist sauberer: 60 exec-Stellen, fast alle
      execFile mit Argumentliste — das Muster ist im Haus bekannt, nur der
      Player ist aelterer Boden.

      ══ NACHTRAG 31.08.2026 (Kritiker-Lauf, HEAD df9700ef) — noch offen,
         und der Datenfluss hat sich verschoben, nicht das Risiko ══════════
      Die Stellen heissen jetzt `spotify-control.ts:1657` und `:1699` und
      bauen den find-String mit `${currentMeta.path}` bzw. `${rel}`. Der
      Pfad wird NICHT mehr inline mit `decodeURIComponent(pfad)` dekodiert
      (E106, b9715528) — er kommt bereits als KLARTEXT aus `medienPfadAus`
      (Kommentar bei :1652 bestaetigt es). Wer den alten Eintrag liest und
      nach `decodeURIComponent(` in der find-Zeile sucht, findet nichts und
      haelt den Punkt faelschlich fuer erledigt: das Zeichen wandert, das
      Loch bleibt. Der Fund ist inzwischen VIER Laeufe alt (24., und jetzt
      31.08. als Dublette wiedergefunden) und weiter offen —
      [[wiederkehrender-befund-ist-ablauffehler]]: er wandert von Rangliste
      zu Rangliste, weil KEINE WACHE dahinter steht. Beim execFile-Umbau
      also GLEICH die Naht mitbauen — eine grep-Wache in tools/, die
      `exec(`…`find "`…`${` in backend-player faengt — auf das MERKMAL
      (exec + Shell-String mit Template-Einschub), nicht auf die konkrete
      Zeile, die schon zweimal gewandert ist
      ([[dynamischer-pfad-macht-die-verbwache-blind]]) — sonst kommt der
      Befund im naechsten Audit ein fuenftes Mal.
    related: ["pkill-toetet-die-eigene-shell", "was-in-einer-unreinen-funktion-steht-hat-keinen-zeugen", "wiederkehrender-befund-ist-ablauffehler", "dynamischer-pfad-macht-die-verbwache-blind"]
  - id: zahlenwache-kannte-nur-eine-datei
    kind: falle
    title: "Die Zahlenwache korrigierte 51 in der README — dieselbe 51 stand drei Wochen daneben in mixpibox.md"
    match: "zahlen veraltet handbuch|readme-behauptungen geltungsbereich|testbestand stand 2026-08-03|wache kennt nur eine datei|zaehlende behauptung zweite ablage"
    sev: med
    tags: ["doku", "wache", "mixpibox.md", "readme", 2026]
    source: >
      24.08.2026, Doku-Lauf, HEAD bdf50e19. Beide Proben meldeten sauber
      (1 Luecke = bekannter SEKTIONEN-Code-Stand); der Fund kam allein aus
      der Muster-Methode. Alle Zahlen im Baum nachgemessen.
    body: |
      `tools/readme-behauptungen-pruefen.sh` hat am 23.08.2026 sechs falsche
      Zahlen der README gefangen, darunter „51 Testdateien" → 113. Am 24.08.
      stand dieselbe `51` unkorrigiert in `dokumentation/mixpibox.md` §7.3, in
      einem Block „Der Bestand (Stand 2026-08-03)", dessen SECHS Zeilen ALLE
      falsch waren:

        backend-api  51/113   frontend-box 35/41   frontend-admin 8/18
        backend-player 5/9    NewDesign 7/28       installer 5/27

      Einundzwanzig Tage, vierzehn gruene Wachen. Der Fehler war nicht die
      Zahl, sondern der GELTUNGSBEREICH: die Wache heisst nach einer Datei und
      prueft auch nur die. **Eine Wache, die eine Ablage nicht kennt, meldet
      dort ewig gruen** — und `mixpibox.md` ist die Ablage, die driftet, weil
      sie als einziges Handbuch nicht im Entwicklerpfad liegt.

      MERKSATZ FUER DEN NAECHSTEN LAUF: nach der KORRIGIERTEN ZAHL greppen,
      nicht nach dem Thema. `grep -rn "51 Testdatei" .` haette es am 23.08.
      in derselben Minute gezeigt. Das ist dieselbe Regel wie „vor der ersten
      Korrektur greppen, nicht nach der letzten", nur auf Zahlen statt Saetze.

      ZWEITER FUND DESSELBEN LAUFS, andere Form, gleiche Datei: §4 rechnete
      „21 Seiten − 17 Leisteneintraege = vier, die niemand im Menue findet".
      Gemessen fuehrt `rahmen.ts` **18** Eintraege; die Uebersicht steht als
      einzige ausserhalb der `<section class="gruppe">`-Bloecke, und wer die
      Gruppen zaehlt, uebersieht sie. Die Doku fuehrte damit die STARTSEITE
      als unauffindbar. **Eine Rechnung, deren Ergebnis passt, belegt nicht,
      dass ihre Summanden stimmen** — 21 − 17 = 4 ging auf, und genau deshalb
      hat es drei Wochen niemand nachgeschlagen. Beim Pruefen darum jede Zahl
      EINZELN messen, nie die Differenz.

      SEITHER: Abschnitt 3b in derselben Wache (nicht in einer neuen Datei —
      wer eine Wache abschreibt, erbt ihre Blindheit und ihren `exit 0`).
      Toleranz 20 % fuer die Testbestaende, **0 %** fuer die Kopfleiste: ein
      Menuepunkt mehr ist kein Wachstum, das ist ein Ereignis. Zwei Fallen
      beim Bauen gemessen: `grep -c 'test('` zaehlt in der e2e-Datei die
      Hilfsaufrufe mit (nur der Fall am Zeilenanfang zaehlt), und
      `find tests/ -type f` zaehlt `__pycache__` mit (nur `*_test.py`).
      Gegenprobe gefahren: alte Zahlen eingesetzt → „121 % daneben",
      „300 % daneben", Rueckgabestand 1.
    related: ["readme-zahlen-waren-um-das-sechsfache-daneben", "geschlossenes-vokabular-ohne-konsumenten", "ablage-an-zwei-orten-ohne-gegenstelle", "gegenprobe-statt-gruen-glauben", "doku-zeigt-auf-dateien-die-es-nicht-gibt"]
  - id: doku-zeigt-auf-zeilen-die-wandern
    kind: lehre
    sev: medium
    title: "Eine Wache prueft den Pfad und schneidet den Doppelpunkt ab - `server.ts:2584` bleibt gruen, wenn die Zeile wandert"
    match: "zeilenverweis|pfad:zeile|zeilennummer in der doku|doku-zeilenzitate|zitierte zeile|beleg zeigt woandershin"
    cause: >
      `tools/doku-pfade-pruefen.py` war bis zum 24.08.2026 die einzige Wache,
      die die Gegenfrage stellt - gibt es noch, worauf die Doku zeigt? Ihr
      Muster endet aber am Dateinamen; die Zeilennummer dahinter wird nicht
      gelesen. Damit faellt eine ganze Sorte Verrottung durch: die Datei gibt
      es weiter, der Verweis zeigt trotzdem ins Falsche.
    fix: >
      `python3 tools/doku-zeilenzitate-pruefen.py`, eingehaengt in
      `bash tools/doku-luecken-probe.sh`. Sie loest jeden Verweis der Form
      `pfad:zeile` gegen `git ls-files` auf und prueft, ob die genannte Zeile
      innerhalb der Datei liegt. Gegenprobe eingebaut: `--sabotage <zahl>`
      hebt alle Zeilennummern an, und die Wache MUSS rot melden.
    tags: ['doku', 'wache', 'werkzeug', 'lehre', 2026]
    source: "24.08.2026, Doku-Lauf. Am Baum erhoben: 201 Verweise der Form `pfad:zeile` in README.md, plugins/README.md, dokumentation/mixpibox.md, dokumentation/benutzerhandbuch.html, BACKLOG.md und llmwiki/pack.yaml."
    body: |
      DER ERSTE LAUF FAND NICHTS, UND DAS IST DER BERICHT.
      201 Zeilenzitate geprueft, 0 tot, 14 nicht entscheidbar. Anders als
      `doku-pfade-pruefen.py`, die beim ersten Lauf drei Funde hatte, schliesst
      diese Wache eine Luecke, BEVOR sie einen Schaden angerichtet hat. Wer
      spaeter liest, soll die Null nicht fuer eine ungemessene Stelle halten.

      WARUM DIE STELLE ETWAS ANDERES IST ALS DIE DATEI.
      Ein Zeilenverweis steht in diesem Paket fast nie als Fussnote, sondern
      als EINZIGER BELEG einer Behauptung: `kein-plugin-weg-in-die-medienliste`
      belegt mit `server.ts:2584`, dass es genau einen Aufrufer gibt. Wandert
      der Code - und dass er wandert, misst die Pfad-Wache staendig, die
      Warteliste aus E66 zog aus dem Kern in ein Plugin -, dann bleibt der
      Dateiname richtig, die Zeile zeigt woandershin, und der Beleg belegt
      nichts mehr. Der Leser tippt ihn ab, findet fremden Code und weiss
      danach weniger als vorher: weder ob die Messung stattfand noch wie er
      sie wiederholt.

      WAS SIE BEWUSST NICHT PRUEFT.
      Ob an Zeile 2584 noch DASSELBE steht. Das ist aus dem Text nicht
      ableitbar, und eine Wache, die es zu raten versucht, meldet Rauschen und
      wird nach dem zweiten Lauf ignoriert. Die Wache ist damit eine
      UNTERGRENZE - sie faengt jede Schrumpfung (geloeschter Block,
      ausgelagerte Datei), aber keine Verschiebung innerhalb einer Datei, die
      lang genug geblieben ist. Wer mehr will, braucht eine mitgefuehrte
      Pruefsumme des zitierten Bereichs; das ist eine zweite Handpflege und
      war den Aufwand hier nicht wert.

      DER KNIFF MIT DEN NAMENSVETTERN.
      `streaming.ts` gibt es zweimal (Backend und Verwaltungsseite),
      `index.mjs` zehnmal, `index.html` fuenfmal. Fuer einen blossen Pfad ist
      das nicht aufloesbar - `doku-pfade-pruefen.py` meldet solche Faelle als
      MEHRDEUTIG. MIT Zeilennummer schrumpft die Menge: ein Kandidat, der
      kuerzer ist als die genannte Zeile, kann nicht gemeint sein. Bleibt
      DANACH kein einziger uebrig, ist das ein Fund, egal welche Datei gemeint
      war. Bleiben mehrere, schweigt die Wache - 14 der 201 Zitate landen
      dort. Nicht entscheidbar ist eine ehrlichere Meldung als geraten.

      DIE GEGENPROBE GEHOERT IN DIE DATEI, NICHT IN DEN LAUF.
      Eine Wache, die am sauberen Baum gruen meldet, hat nichts bewiesen
      ([[gegenprobe-statt-gruen-glauben]]). Darum kennt sie `--sabotage
      <zahl>`: alle Zeilennummern werden kuenstlich angehoben. Gemessen bei
      +100000 meldete sie 201 von 201 rot, bei +50 genau 14 - die Zitate nahe
      am Dateiende. Dazu die zweite Sicherung, dieselbe wie in
      `sektionen-deckung.py`: findet sie GAR KEIN Zitat, meldet sie eine
      Warnung statt gruen. Aendert jemand die Schreibweise der Verweise, saehe
      "nichts zu pruefen" sonst genauso aus wie "alles in Ordnung".
    related: ["doku-zeigt-auf-dateien-die-es-nicht-gibt", "gegenprobe-statt-gruen-glauben", "wiki-zustand-hat-haltbarkeit", "zaehlende-probe-sieht-keinen-widerspruch", "geschlossenes-vokabular-ohne-konsumenten"]
  - id: anleitung-zeigt-ins-stillgelegte-repo
    kind: falle
    sev: high
    title: "Sechzehn Wachen fragen, ob ein Ort EXISTIERT - ein totes Repo besteht jede davon"
    match: "stillgelegt|llmwiki_mupibox|zweite wahrheit|totes repo|herkunftsrepo|wohin gehoeren aenderungen|wo liegt das wissenspaket"
    cause: >
      Ein stillgelegter Ordner liegt weiter auf der Platte. Jede Wache, die
      fragt "gibt es den Pfad noch?", nickt ihn also ab - `doku-pfade-pruefen`,
      `doku-zeilenzitate`, `pack-verweise`, alle. Die Frage, die keine stellte,
      ist eine andere: schickt die Doku den Leser DORTHIN? Ein Satz, der sagt
      was zu TUN ist, wiegt schwerer als ein Absatz darueber, was gilt - und
      hier standen beide sieben Zeilen auseinander auf derselben Seite.
    fix: >
      `python3 tools/stillgelegte-orte-pruefen.py`, eingehaengt in
      `bash tools/doku-luecken-probe.sh`. Sie liest die toten Orte aus dem
      Abschnitt "Die Herkunftsrepos sind stillgelegt" in `ZUGEZOGEN.md` (wer
      stilllegt, schreibt es dort hin - keine zweite Handpflege) und ruegt
      jeden SATZ in Handbuechern und Wissenspaket, der einen davon mit einem
      anweisenden Verb nennt. Gegenprobe: `--sabotage` haengt jeder Quelle
      einen solchen Satz an und die Wache MUSS rot melden.
    tags: ['doku', 'wache', 'werkzeug', 'falle', 'wiki', 2026]
    source: "24.08.2026, Doku-Lauf. Gemessen in dokumentation/mixpibox.md (Zeile 70 gegen Zeile 77) und am Commit-Datum von ~/Downloads/llmwiki_mupibox."
    body: |
      DER FUND, IN ZWEI ZEILEN.
      `dokumentation/mixpibox.md` Zeile 70: "Die zwei Herkunftsrepos sind
      stillgelegt ... wer dort arbeitet, legt eine zweite Wahrheit an."
      Zeile 77, derselbe Abschnitt: "Aenderungen gehoeren nach
      `~/Downloads/llmwiki_mupibox`". Die zweite ist die, der man folgt: sie
      beantwortet die Frage, mit der man die Seite aufmacht ("wohin schreibe
      ich das?"), waehrend die erste nur einen Zustand beschreibt.

      DASS ES GEWIRKT HAT, IST BELEGT.
      Das stillgelegte Repo trug am 22.08.2026 noch Commits - zwoelf Tage nach
      der Stilllegung vom 10.08. und zwei Tage nachdem
      [[stillgelegtes-repo-lebt-im-fetch-cache-weiter]] genau das benannt und woertlich
      verlangt hat, "die Anleitung umzuschreiben". Die Anleitung stand
      trotzdem da. EIN WIKI-EINTRAG, DER EINE HANDLUNG VERLANGT, IST KEINE
      WACHE: er wird gelesen, wenn jemand danach sucht, und die Doku wird
      gelesen, wenn jemand etwas tun will. Nur die zweite Sorte Text kommt dem
      Fehler in die Quere.

      AUSGEGANGEN IST ES GUT, UND AUCH DAS GEHOERT HIERHER.
      Der juengste Eintrag im toten Repo (`kein-plugin-weg-in-die-medienliste`)
      war schon Stunden nach dem Schreiben falsch; der Baum fuehrt die
      gueltige Fassung als `plugin-inhalt-kommt-in-die-medienliste`. Wer der
      falschen Anleitung gefolgt und spaeter "nachgezogen" haette, haette den
      AELTEREN Irrtum hereingeholt. Der Schaden eines toten Zweigs ist nicht,
      dass Arbeit verschwindet, sondern dass sie unbeobachtet altert und
      spaeter als "neuer" gilt.

      WARUM DIE WACHE NICHT JEDE NENNUNG RUEGT.
      Ueber die Herkunft zu schreiben ist richtig; `ZUGEZOGEN.md` besteht zur
      Haelfte daraus, und dieser Eintrag nennt den toten Pfad selbst. Geruegt
      wird nur, was ANWEIST - eine kurze, im Kopfkommentar begruendete Liste
      von Verben. Zwei Ausnahmen daneben: Verneinung ("dort bitte nicht mehr
      arbeiten") und Zitat ("bis zum ... stand hier ..."), sonst faengt die
      Wache genau die Saetze, die vor dem Fehler warnen. Gemessen: 7 Nennungen
      in sechs Dateien, davon 1 Anweisung - der Fund. Nach der Korrektur 7
      Nennungen, 0 Funde, und `--sabotage` meldet 8 von 8 rot.

      DIE LEHRE UEBER DIESEN FALL HINAUS.
      Existenzpruefungen sind blind fuer Gueltigkeit. Ein Pfad, eine Zeile,
      ein Endpunkt, ein Repo koennen alle DA sein und trotzdem der falsche
      Ort. Wo eine Doku nicht beschreibt, sondern ANWEIST, braucht es eine
      Wache, die den Zielort gegen eine Liste haelt, welche die Aufgabe des
      Ortes ausspricht - hier `ZUGEZOGEN.md`. Und diese Liste muss dort
      stehen, wo die Entscheidung faellt, nicht in der Wache
      ([[wiki-zustand-hat-haltbarkeit]]).
    related: ["stillgelegtes-repo-lebt-im-fetch-cache-weiter", "doku-zeigt-auf-zeilen-die-wandern", "doku-zeigt-auf-dateien-die-es-nicht-gibt", "gegenprobe-statt-gruen-glauben", "wiki-zustand-hat-haltbarkeit"]
  - id: doku-nennt-den-pfad-und-das-falsche-verb
    kind: falle
    title: "Siebzehn Wachen pruefen den Endpunkt - keine das VERB davor"
    match: "404 obwohl endpunkt existiert|PUT /api/konfiguration|falsches http verb doku|endpunkt antwortet 404 nachmessen|api-doku-deckung liest nur den pfad"
    sev: mittel
    tags: ["doku", "wiki", "wache", "api", "verben", "doku-lauf", 2026]
    stand: "24.08.2026 gemessen am Repo mit tools/endpunkt-verben-pruefen.py; nach der Korrektur 0 Funde, --sabotage 6 von 6 rot"
    source: >
      Doku-Lauf 24.08.2026. Erster Lauf der neuen Wache
      `tools/endpunkt-verben-pruefen.py` gegen `README.md`,
      `plugins/README.md`, `dokumentation/mixpibox.md`,
      `dokumentation/benutzerhandbuch.html`, `BACKLOG.md` und
      `llmwiki/pack.yaml`: 199 Routen aus `src/backend-api/src`, 414
      dokumentierte Verb/Pfad-Paare mit Route im Kern, 1 Fund.
    body: |
      DIE LUECKE IN DER FRAGESTELLUNG: `tools/api-doku-deckung.sh` haelt 200
      REST-Routen gegen vier Handbuecher und meldet seit Tagen gruen. Sie
      liest dabei AUSSCHLIESSLICH den Pfad — ihr grep-Muster
      (`(app|router)\.(get|post|…)\(\s*['"`]\K[^'"`]+`) beginnt hinter dem
      Anfuehrungszeichen, das Verb steht davor und faellt weg. Die
      siebzehn Wachen darueber fragen alle nach EXISTENZ: gibt es die Datei,
      die Zeile, die Beschriftung, den Endpunkt. Keine fragt, ob die Doku ihn
      richtig AUFRUFT.

      WARUM DAS NICHT DERSELBE SCHADEN IST wie ein ungenannter Endpunkt: wer
      eine Route nicht dokumentiert findet, sucht weiter. Wer ein falsches
      Verb abtippt, bekommt von Express ein `404` — dieselbe Antwort wie fuer
      einen Pfad, den es gar nicht gibt. Der naheliegende Schluss ist „die
      Schnittstelle ist weg", und er ist falsch.

      ══ DER FUND ═══════════════════════════════════════════════════════════
      `llmwiki/pack.yaml` schrieb `PUT /api/konfiguration`. Der Kern kennt
      dort `GET` (`src/backend-api/src/server.ts:5202`) und `POST` (`:5716`),
      kein PUT.

      Und es ist nicht irgendeine Zeile. Sie steht im Eintrag ueber den
      Themenweg und ist dort der Beleg dafuer, dass `www/active_theme.css`
      NUR nachgezogen wird, wenn die Verwaltung speichert — der eine lebende
      Aufrufer von `setting_update.sh`. Wer die am Geraet gemessene Kette
      nachpruefen will, macht es mit dem Verb, das dort stand, bekommt 404,
      und die einzige belegte Aussage ueber den Themenweg sieht aus wie
      widerlegt. Korrigiert im selben Lauf.

      ══ WARUM VERNEINUNGEN DURCHKOMMEN MUESSEN ═════════════════════════════
      `BACKLOG.md` haelt fest: „Es gibt **kein** `PATCH /api/darstellung` —
      nur `GET` und `PUT`." Das ist die Wahrheit ueber ein Verb, keine falsche
      Anleitung. Ohne die Verneinungs-Ausnahme ruegte die Wache genau die
      Zeile, die den Fehler bereits benennt — derselbe Grund und dieselbe
      Loesung wie bei `tools/stillgelegte-orte-pruefen.py` einen Lauf zuvor.
      Der Preis ist dokumentiert: wer ein falsches Verb schreibt und im selben
      Satz „nicht" oder „kein" unterbringt, kommt durch. Untergrenze, kein
      falsches Gruen.

      ══ DIE REGEL DAHINTER ═════════════════════════════════════════════════
      Eine Wache, die einen Namen aus dem Code gegen die Doku haelt, prueft
      immer nur so viel, wie ihr Muster einfaengt. Was VOR dem Fundort steht —
      das Verb, der Empfaenger, die Bedingung — ist fuer sie nicht vorhanden.
      Beim Uebernehmen eines Musters aus einer anderen Wache deshalb fragen,
      was es ABSCHNEIDET, nicht nur, was es findet. Dieselbe Familie wie
      `doku-zeigt-auf-zeilen-die-wandern` (Pfad geprueft, Zeile nicht) und
      `geschlossenes-vokabular-ohne-konsumenten` (Name geprueft, Konsument
      nicht).

      Gewacht von `tools/endpunkt-verben-pruefen.py` (laeuft in
      `tools/doku-luecken-probe.sh` mit). Findet sie keine Route im Backend
      oder kein einziges dokumentiertes Verb/Pfad-Paar, meldet sie WARNUNG
      statt gruen. Sie liest nur den Kern ohne `*.spec.ts`; Plugin-Routen
      stehen nicht darin und fallen stumm durch.
    related: ["doku-zeigt-auf-zeilen-die-wandern", "geschlossenes-vokabular-ohne-konsumenten", "anleitung-zeigt-ins-stillgelegte-repo", "gegenprobe-statt-gruen-glauben", "thema-erreicht-die-neue-oberflaeche-nur-mit-important"]
  - id: wache-sieht-die-schleife-nicht-die-scharf-schaltet
    kind: falle
    title: "Achtzehn Wachen lesen `systemctl enable` - keine las die SCHLEIFE, die es aufruft"
    match: "dienste-doku-deckung meldet gruen|unit fehlt im handbuch|systemctl enable in schleife|netzabriss-sonde nicht dokumentiert|DIENSTE=\\( |for service in mupi_wifi"
    sev: mittel
    tags: ["doku", "wiki", "wache", "systemd", "dienste", "doku-lauf", 2026]
    stand: "24.08.2026 am Repo gemessen: vorher 20 Units geprueft, nachher 27; drei davon standen in KEINER Zeile des Handbuchs"
    source: >
      Doku-Lauf 24.08.2026. Beide Proben meldeten sauber, seit dem Vorlauf kein
      neuer Commit. Gefunden ueber die Muster-Methode: die Gegenrichtung zu
      einer ZAEHLENDEN Wache suchen — `tools/dienste-doku-deckung.sh` haelt die
      `enable`-Aufrufe der fuenf Ausrollwege gegen `dokumentation/mixpibox.md`
      und meldete „KEINE LUECKE (20 Units geprueft)".
    body: |
      DIE WACHE HATTE IHRE BLINDSTELLE SELBST AUFGESCHRIEBEN. In
      `tools/dienste-doku-deckung.sh` stand als Kommentar:

          # `--now` ist ein Schalter, keine Unit; `$VARIABLE` waere geraten
          # statt gemessen. Beides faellt raus.

      Der zweite Satz ist als Vorsicht gemeint und war eine Luecke. Zwei der
      fuenf Ausrollwege schalten naemlich GENAU SO scharf:

          scripts/systemd/einrichten.sh
            DIENSTE=(mupi_taster mupi_knopflicht netzabriss-sonde soloist
                     mixpi-wlan-adapter)
            for d in "${DIENSTE[@]}"; do systemctl enable "${d}.service"

          autosetup/autosetup.sh
            for service in mupi_wifi mupi_check_internet mupi_check_monitor \
                mupi_idle_shutdown librespot mupi_startstop mupi_powerled \
                mupi_offtrigger dietpi-dashboard wifi-powersave-off; do
              systemctl enable ${service}.service

      Der `grep -v '\$'` warf beide Zeilen weg. Sieben Units waren fuer die
      Wache unsichtbar; sie meldete daneben gruen und nannte dabei stolz ihre
      Zahl — „20 Units geprueft". Eine Zahl in einer gruenen Meldung liest sich
      wie ein Deckungsgrad und ist nur die Groesse dessen, was das Muster
      gefangen hat.

      ══ WAS DAHINTER LAG ═══════════════════════════════════════════════════
      Drei Units standen in KEINER Zeile von `dokumentation/mixpibox.md`,
      dessen Kapitel 4.1 sich „Auf einer eingerichteten Box" nennt und damit
      Vollstaendigkeit behauptet:

        * `netzabriss-sonde`  — `Restart=always`, laeuft als `dietpi`,
          `/home/dietpi/netzabriss/sonde.py`. KEIN Dienst der Anwendung,
          sondern das Messgeraet aus dem Netzabriss-Aufbau. Ein Dauerlaeufer,
          den niemand im Handbuch findet, ist bei jeder Strom- oder
          Funkmessung ein stiller Mitspieler.
        * `mupi_idle_shutdown` — FAEHRT DIE BOX HERUNTER. Ab Werk scharf. Wer
          eine Messung aufsetzt und dabei wartet, misst gegen diese Unit.
        * `mupi_wifi` — liest `/boot/add_wifi.json` bei jedem Start; der Weg,
          auf dem eine Karte ohne Bildschirm ins WLAN kommt.

      ══ DER TEURERE NEBENBEFUND: DER NAMENSTREFFER ALS ALIBI ═══════════════
      `mupi_startstop` meldete die Wache NICHT — der Name steht im Handbuch.
      Er steht dort aber in der Aufzaehlung „Bestand, der auf keiner heutigen
      Box laeuft", und `autosetup.sh` schaltet ihn auf einer frischen Karte in
      derselben Schleife wie `mupi_wifi` scharf. Eine Wache, die auf VORKOMMEN
      prueft, kann von einem Satz zufriedengestellt werden, der das Gegenteil
      behauptet. Dieselbe Familie wie
      `doku-schickt-zur-falschen-oberflaeche` (Wort da, Ort falsch).

      Gegenrichtung ebenfalls gemessen: die Zeile
      „`mupi_taster`, `mupi_knopflicht` | ueber `scripts/systemd/einrichten.sh`,
      NICHT ueber die Ausrollwege" war eine Notluege um genau diese
      Blindstelle herum — `einrichten.sh` steht seit dem Bau der Wache in ihrer
      `WEGE`-Liste. Wer eine Wache nicht erweitern kann, schreibt die Ausrede
      ins Handbuch, und dort ueberlebt sie den Grund.

      ══ DIE REGEL ══════════════════════════════════════════════════════════
      Ein Muster, das aus Vorsicht etwas WEGWIRFT (`grep -v`), ist eine
      Blindstelle mit guter Begruendung. Beim Lesen einer Wache darum nicht
      fragen, was sie faengt, sondern was ihr `grep -v` kostet — und ob die
      Schreibweise, die sie ausschliesst, im Baum wirklich nicht vorkommt.
      Hier kam sie in zwei von fuenf Quellen vor.

      ══ DER BAU, ZWEI LEHREN AUS DER GEGENPROBE ════════════════════════════
      (1) Die benutzte Variable ist die LAUFVARIABLE, nicht die Liste. Die
      erste Fassung suchte ein Feld `d=( … )` und `service=( … )` und meldete
      zwei Warnungen statt sieben Units — man muss ueber
      `for d in "${DIENSTE[@]}"` erst zur Quelle. Beide Schreibweisen kommen
      im Baum vor (Feld-Zuweisung und woertliche Wortliste hinter `in`).
      (2) Die Gegenprobe (`DIENSTE=($MUPI_TASTER …`) zeigte die zweite Haelfte:
      ein `$` in einem EINZELNEN Eintrag rutschte als Unitname durch, und die
      Wache suchte `$MUPI_TASTER` im Handbuch — eine Luecke, die keine ist.
      Sie warnt jetzt und laesst den Eintrag fallen, statt ihn zu melden.

      Korrigiert im selben Lauf: die drei Units stehen in 4.1, die Notluege
      ist weg, `mupi_startstop` steht mit dem, was der Ausrollweg tut, und die
      Dateizahl in der Einleitung (34) war ebenfalls alt — es sind 42.
    related: ["doku-nennt-den-pfad-und-das-falsche-verb", "doku-schickt-zur-falschen-oberflaeche", "geschlossenes-vokabular-ohne-konsumenten", "gegenprobe-statt-gruen-glauben", "taster-wird-von-niemandem-ueberwacht", "sonde-in-tmp-stirbt-mit"]
  - id: wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm
    title: "stilnamen-pruefen.py war rot auf einen Fehlalarm - gemerkt hat es niemand, weil sie in keinem Laeufer stand"
    kind: architektur
    match: "stilnamen-pruefen rot|fp-bild malt nichts|VON_JS handliste|wache laeuft in keiner probe|17 Schritte pruefen.sh|leitplanken-zahl"
    sev: mid
    tags: [wachen, doku, css, laeufer, doku-lauf, 2026]
    stand: "24.08.2026 gemessen und behoben; stilnamen-pruefen.py laeuft jetzt in tools/pruefen.sh, leitplanken-zahl-pruefen.py in tools/doku-luecken-probe.sh"
    source: "Stuendlicher Doku-Lauf; alle achtzehn Doku-Wachen meldeten gruen"
    body: |
      ZWEI BEFUNDE MIT DERSELBEN URSACHE: eine Wache, die kein Laeufer ruft,
      ist keine Wache. Sie faellt nicht auf, wenn sie rot wird, und sie faellt
      erst recht nicht auf, wenn sie AUF EINEN FEHLALARM rot wird.

      ══ 1. Die Wache, die auf einen Fehlalarm rot stand ══════════════════

      `tools/stilnamen-pruefen.py` haelt jedes `var(--x)` ohne Ersatzwert
      gegen ein `--x:` im Stilblatt — eine Zeile ohne Erklaerung malt nichts.
      Eigenschaften, die JavaScript zur Laufzeit setzt, stehen naturgemaess
      nicht in der CSS; sie standen deshalb in einer HANDGEPFLEGTEN Liste
      `VON_JS`, und daneben im Kopf der Datei der Satz:

        „WER EINE NEUE SOLCHE EINFUEHRT, TRAEGT SIE HIER EIN — sonst wird
         dieses Werkzeug rot und jemand schaltet es ab, und das waere
         schlimmer als der Fehler, den es fangen soll."

      Genau das war eingetreten. `anwenden()` in `NewDesign/app.js:1392` setzt
      seit dem Bildwahl-Umbau `--fp-bild` ueber `document.body.style
      .setProperty`. Niemand trug es nach. Die Wache mahnte
      `NewDesign/app.css:7736` als tote Zeile an — eine Zeile, die malt.

      DIE HANDLISTE WAR DIE URSACHE, nicht die Nachlaessigkeit. Sie wird jetzt
      GELESEN statt gepflegt: jede `.setProperty('--x', …)` in den
      Oberflaechen-JS gilt als Erklaerung, mit Datei und Zeile.
      `VON_JS_HAND` bleibt nur fuer Eigenschaften, die anders gesetzt werden.

      EIN DETAIL, DAS DIE ERSTE FASSUNG FAST WIEDER BLIND GEMACHT HAETTE: das
      Muster verlangte `style.setProperty` und fand 5 von 21 Stellen —
      `anwenden()` haelt `const s = document.body.style` und ruft
      `s.setProperty`. Das `--` im Namen genuegt als Beleg; das `style.`
      davor zu verlangen war eine Annahme ueber den Schreibstil, nicht eine
      Messung. Jetzt 16 gefunden, Wache gruen.

      Gegenprobe zweimal gefahren: Namen in app.js verdreht -> rot, mit der
      richtigen Zeile 7736. JS-Quelle umbenannt -> WARNUNG statt gruen.

      ══ 2. Warum es niemand gemerkt hat ═════════════════════════════════

      `tools/stilnamen-pruefen.py` lag seit dem 08.08.2026 im Baum und wurde
      von KEINEM Laeufer gerufen — weder von `tools/pruefen.sh` (73 Schritte)
      noch von `tools/doku-luecken-probe.sh` (18 Wachen). Sie lief nur, wenn
      jemand sie von Hand aufrief, und das tat drei Wochen niemand.

      Gemessen: 77 statische Werkzeuge in `tools/` ruft kein Laeufer auf. Die
      allermeisten sind Einmalwerkzeuge (`-sandkasten`, `-nachstellen`,
      `-linse`) und gehoeren auch nirgendwo hin — eine Wache, die 77 Zeilen
      meldet, wird zu Recht ignoriert. EINE AUTOMATISCHE ROSTER-WACHE UEBER
      ALLE 77 WAERE DESHALB FALSCH. Die Regel ist von Hand zu halten: WER EINE
      STEHENDE WACHE BAUT, HAENGT SIE IM SELBEN COMMIT IN EINEN LAEUFER.

      ══ 3. Nebenbefund: die Zahl ueber dem Laeufer selbst ════════════════

      Abschnitt 7.1 von `dokumentation/mixpibox.md` sagte seit der ersten
      Fassung „17 Schritte". Gemessen: 73 unbedingte plus 6, die nur mit
      `--box` bzw. am echten Geraet laufen — 79. Um 62 daneben, direkt ueber
      dem Aufrufblock, den jeder liest, der das Projekt zum ersten Mal baut.

      Es ist dieselbe Bauart wie die 51 in der README: eine Zahl ueber den
      Baum, von Hand geschrieben, danach nie wieder angefasst. Achtzehn
      Doku-Wachen pruefen NAMEN — Pfade, Zeilen, Endpunkte, Verben, Units,
      Vokabular. Keine prueft eine ZAHL, und eine Zahl ist der Satzteil, den
      niemand nachrechnet.

      Neue Wache `tools/leitplanken-zahl-pruefen.py`, laeuft als neunzehnte in
      `tools/doku-luecken-probe.sh`. Sie zaehlt in ZWEI Toepfen (unbedingt am
      Zeilenanfang, bedingt eingerueckt im `if`), weil eine Doku sonst nicht
      zugleich fuer `--schnell` und fuer `--box` stimmen kann. Gegenprobe
      dreimal: Zahl zurueck auf 17 -> rot; ein Schritt dazu -> rot mit 74/80;
      Aufrufwort `schritt` umbenannt -> WARNUNG statt gruen.

      ══ WAS DAS FUER DEN NAECHSTEN LAUF HEISST ══════════════════════════

      Gruen von `tools/doku-luecken-probe.sh` heisst NICHT, dass die Wachen
      gruen sind — es heisst, dass die achtzehn Wachen gruen sind, DIE DARIN
      STEHEN. Wer eine Wache sucht, die rostet, sucht sie unter denen, die
      nirgends laufen. `tools/grenzruf-verb-deckung.py` stand noch so da und
      meldete 2 Abweichungen; beide sind dynamisch gebaute Pfade
      (`API + '/funk/' + was`), die das Werkzeug als `/api/funk/X` las —
      Rauschen, kein Fehler, aber ungeklaert und darum ungewacht.
      ERLEDIGT am 24.08.2026 im naechsten Lauf: die zwei Fehlalarme sind
      abgestellt, die Wache haengt in `tools/pruefen.sh`. Die zweite Ursache
      war NICHT dieselbe wie die erste — siehe
      `dynamischer-pfad-macht-die-verbwache-blind`.
    related: ["gegenprobe-statt-gruen-glauben", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "readme-zahlen-waren-um-das-sechsfache-daneben", "geschlossenes-vokabular-ohne-konsumenten"]
  - id: dynamischer-pfad-macht-die-verbwache-blind
    title: "Die zweite ungerufene Wache: grenzruf-verb-deckung.py las gebaute Pfade als Segment und war auf zwei Fehlalarme rot"
    kind: architektur
    match: "grenzruf-verb-deckung|api/funk/X|api/medienX|verb statt pfad|GET auf POST-Route|catch-all liefert HTML"
    sev: mid
    tags: [wachen, grenze, express, laeufer, doku-lauf, 2026]
    stand: "24.08.2026 gemessen und behoben; die Wache laeuft jetzt in tools/pruefen.sh und meldet 0 Abweichungen bei 113 Aufrufen gegen 228 Routen"
    source: "Stuendlicher Doku-Lauf; Doku-Probe gruen bis auf die bereits dokumentierte SEKTIONEN-Luecke"
    body: |
      Der Lauf davor (siehe wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm)
      hinterliess EINEN ungeklaerten Rest, woertlich: „`tools/grenzruf-verb-deckung.py`
      steht noch so da und meldet 2 Abweichungen; beide sind dynamisch gebaute
      Pfade — Rauschen, kein Fehler, aber ungeklaert und darum ungewacht."

      Dieser Lauf hat den Rest nachgemessen. Beides war wirklich Rauschen, aber
      aus ZWEI verschiedenen Gruenden, und nur einer davon war der vermutete.

      ══ WAS DIE WACHE KANN, UND WARUM SIE NICHT ERSETZBAR IST ══════════════

      Sie prueft die Grenze Oberflaeche->Server nicht nach PFAD, sondern nach
      VERB. Express unterscheidet `app.get` von `app.post`. Ein GET auf eine
      nur als POST registrierte Route faellt in den Catch-all am Dateiende und
      liefert die Angular-Seite — die Oberflaeche bekommt keinen Fehlercode,
      sondern einen JSON-Parse-Fehler an ganz anderer Stelle. Achtzehn
      Doku-Wachen zaehlen Endpunkte; keine zweite prueft das Verb im Aufruf.

      ══ FEHLALARM 1: das Segment, das erst zur Laufzeit entsteht ═══════════

      `fetch(API + '/funk/' + was, {method: 'POST'})` wurde zu `/api/funk/X`
      gelesen, und `X` passte auf kein Mustersegment -> „KEIN PFAD". `was` ist
      aber an allen vier Aufrufstellen woertlich `bluetooth`, `wlan` oder
      `flug`, und alle drei Routen stehen als POST im Server.

      Behoben: `X` auf der AUFRUFSEITE deckt jetzt jedes Mustersegment.
      WAS DABEI NICHT VERLORENGEHT — und das ist der Punkt, an dem eine
      Lockerung sonst zur Attrappe wird: die Verbpruefung bleibt. `/api/funk/X`
      trifft jetzt alle drei `/api/funk/*`-Routen, und es wird weiterhin
      verlangt, dass das Verb in mindestens einer davon vorkommt. Verloren geht
      nur die Aussage „genau DIESES Segment gibt es nicht" — die ist bei einem
      zur Laufzeit gebauten Wert statisch ohnehin nicht zu treffen.

      ══ FEHLALARM 2: das X war ueberhaupt kein Pfadstueck ══════════════════

      Der zweite war NICHT dieselbe Ursache, obwohl er gleich aussah. Bei
      `json(API + '/medien' + (suche ? '?q=' + … : ''))` steckt der Abfrageteil
      in einem BEDINGUNGSAUSDRUCK. Die Wache schneidet den Pfad an `?` ab —
      aber das `?` entsteht erst nach dem Ersetzen des Ausdrucks durch `X`, das
      Abschneiden greift also zu frueh, und uebrig bleibt `/api/medienX`: ein
      Pfad, den es nicht gibt, gebaut aus einem Zeichen, das nie im Pfad stand.

      Behoben mit einer zweiten Runde: ein `X`, das OHNE `/` an ein Segment
      klebt, ist kein eigenes Segment, sondern ein unscharfer Rest — erst
      woertlich versuchen, dann ohne ihn.

      ══ GEGENPROBE ═════════════════════════════════════════════════════════

      Auf einer WEGWERFKOPIE von app.js (die Sabotage haette sonst echte
      Arbeit getroffen), zwei Faelle in einem Lauf:
        * `method: 'POST'` -> `'DELETE'` auf dem dynamischen Zweig: rot mit
          „VERB FEHLT DELETE /api/funk/X -> Server kennt nur: POST …" — die
          Lockerung hat die Verbpruefung also NICHT blind gemacht.
        * `/medien` -> `/gibtsnicht` hinter demselben unscharfen Ende: rot mit
          „KEIN PFAD" — die zweite Runde verschluckt keinen echten Fehlpfad.

      ══ UND DIE WACHE UEBER DER WACHE HAT SOFORT GEFEUERT ══════════════════

      Das Einhaengen in `tools/pruefen.sh` machte aus 73 unbedingten Schritten
      74. `tools/leitplanken-zahl-pruefen.py` — im Lauf davor gebaut — meldete
      das im selben Zug rot (74/80 gegen 73/79 in `mixpibox.md`). Eine Zahl,
      die eine Stunde vorher noch von Hand nachgerechnet werden musste, hat
      sich diesmal selbst gemeldet. Abschnitt 7.1 steht jetzt auf 74/80.

      ══ WAS DAS FUER DEN NAECHSTEN LAUF HEISST ═════════════════════════════

      Der offene Rest aus dem letzten Eintrag ist damit zu. Die Regel dahinter
      gilt weiter und ist die eigentliche Ausbeute: EIN UNGEKLAERTER REST IN
      EINEM WIKI-EINTRAG IST EINE ARBEITSANWEISUNG, KEINE FUSSNOTE. Er stand
      woertlich da, war in einem Lauf abzuarbeiten — und haette sonst
      beliebig lange dort gestanden, weil die Wache, um die es ging, in keinem
      Laeufer haengt und sich also nie von selbst meldet.

      Zweitens: zwei Befunde, die identisch AUSSEHEN (`X` im Pfad), koennen
      verschiedene Ursachen haben. Wer den ersten erklaert und den zweiten
      mitnimmt, laesst einen halben Fehler stehen.
    related: ["wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm", "doku-nennt-den-pfad-und-das-falsche-verb", "gegenprobe-statt-gruen-glauben", "geschlossenes-vokabular-ohne-konsumenten"]
  - id: sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts
    title: "Drei ungerufene Wachen waren Geraete-Sonden - zwei faellten an der ABGESCHALTETEN Box ein Urteil"
    kind: architektur
    match: "WIDERLEGT ohne Box|Probe urteilt ohne Messung|KEIN Profil hat eine listen.json|kinderzeit-je-kind-pruefen|eigene-listen-je-profil|stand-bitdreher-deckung FileNotFoundError|nicht gemessen ist nicht null"
    sev: mid
    tags: [wachen, doku, sonden, box-unerreichbar, doku-lauf, 2026]
    stand: "24.08.2026 gemessen und behoben; alle drei Sonden haben jetzt einen Riegel, Gegenprobe in beide Richtungen gelaufen"
    source: "Stuendlicher Doku-Lauf; tools/doku-luecken-probe.sh meldete nur die bereits bekannte SEKTIONEN-Luecke"
    body: |
      NICHT GEMESSEN IST NICHT DASSELBE WIE NULL GEMESSEN. Eine Sonde, die
      diesen Unterschied nicht kennt, ist gefaehrlicher als gar keine: sie
      liefert ein Urteil in derselben Form wie ein echtes.

      ══ WIE SIE GEFUNDEN WURDEN ════════════════════════════════════════════

      Die Doku-Luecken-Probe meldete nur die schon bekannte SEKTIONEN-Luecke
      (`geschlossenes-vokabular-ohne-konsumenten`), also weiter nach dem
      Muster des Laufs davor: welche Wache haengt in KEINEM Laeufer? Von 216
      Dateien in `tools/` tragen die Namen `*deckung*`, `*pruefen*`, `*halten*`
      die Wachen-Bedeutung; fuenf davon nennt kein anderer Ort. Vier liefen,
      eine starb. Der Reihe nach ausgefuehrt — und genau da lag es:

        * `sprachdeckung-piper-google.py` — gruen und ehrlich (rein lokal:
          21 von 21 Google-Sprachen von Piper gedeckt).
        * `bilder-pruefen.py` — braucht ein Argument, kein Wachen-Fall.
        * `stand-bitdreher-deckung.py` — starb beim IMPORT.
        * `eigene-listen-je-profil-pruefen.py` — Ausgang 1, mit Befund.
        * `kinderzeit-je-kind-pruefen.py` — Ausgang 1, mit Urteil.

      ══ DIE ZWEI URTEILE WAREN UNGEDECKT ═══════════════════════════════════

      Beide Sonden lesen ueber `ssh` an .169. Die Box war aus — kein ping,
      „No route to host". Nur: KEINE der beiden fragt das vorher. `ssh`
      scheitert leise, die Hilfsfunktion gibt leeren Text (bzw. `-1`), und
      JEDER Test unten liest leeren Text als „nichts gefunden". Gedruckt kam
      darum heraus:

        * „✗ KEIN Profil hat eine listen.json — die Funktion wurde nie
          benutzt" — ein Befund ueber eine Ablage, die die Sonde nie gesehen
          hat.
        * „WIDERLEGT -- die Verwaltung ruft KEINEN einzigen
          /api/profil*-Endpunkt" — ein Urteil ueber ein Bundle, das nie
          geladen wurde.

      Der Ausgangswert 1 ist dabei das Tueckische: er ist derselbe wie bei
      einem echten Fund. Wer diese Sonden je in einen Laeufer haengt, holt
      sich rote Meldungen, die von der Stromversorgung der Box abhaengen —
      und gewoehnt sich sie abzuschalten.

      BEHOBEN: beide haben jetzt `erreichbar(box)` — EIN `ssh echo da` vor
      jeder Messung. Schlaegt es fehl, sagen sie „ABBRUCH: … es wurde NICHTS
      gemessen" und gehen mit 2. Der Ausgang 2 ist bewusst NEU und von 1
      verschieden: unmessbar ist ein dritter Zustand neben gruen und rot.

      ══ DIE DRITTE STARB AM FESTEN PFAD ════════════════════════════════════

      `stand-bitdreher-deckung.py` laedt `mupibox-sicherung.py` per
      `importlib` von `/usr/local/bin/mupibox/` — dem Pfad, den es NUR auf
      der Box gibt. Am Arbeitsplatz endete die Datei in einem
      FileNotFoundError-Rueckverfolg, bevor sie ihre eigene Hilfe zeigen
      konnte. Dabei liegt dieselbe Quelle im Baum unter
      `scripts/mupibox/mupibox-sicherung.py`.

      BEHOBEN: erst Box-Pfad, dann Baum-Kopie, sonst ein Satz statt eines
      Rueckverfolgs. Welche Fassung es wurde, wird auf stderr GESAGT — die
      beiden laufen erfahrungsgemaess auseinander (siehe die Zwei-Orte-Frage
      zwischen Box und Installer), und eine Messung am Baum darf niemand fuer
      eine an der Box halten.

      ══ GEGENPROBE ═════════════════════════════════════════════════════════

      Ein Riegel, der immer zuschlaegt, ist auch kaputt — also beide
      Richtungen gemessen:

        * Box aus (echter Zustand): beide Sonden -> Ausgang 2, erste Zeile
          „ABBRUCH: … antwortet nicht auf ssh".
        * Mit einem SCHEIN-`ssh` im PATH, das auf `echo da` mit `da`
          antwortet: beide Sonden laufen durch bis zu ihrem eigenen Urteil,
          NULL Abbruchzeilen. Der Riegel blockiert also nicht die Messung an
          einer stehenden Box.
        * `stand-bitdreher-deckung.py` mit erzwungenem
          `MUPIBOX_SICHERUNG=/nix/da.py`: der eine Abbruchsatz statt des
          Rueckverfolgs. Ohne Umgebungsvariable laeuft sie bis in ihre eigene
          Logik (an einem Wegwerf-Archiv unter /tmp bis „der unveraenderte
          Stand faellt schon durch" — richtig, ein zufaelliges tar.gz ist
          kein Stand).

      ══ DIE REGEL ══════════════════════════════════════════════════════════

      JEDE SONDE, DIE UEBER SSH ODER HTTP AN DIE BOX GEHT, BRAUCHT EINEN
      ERREICHBARKEITSRIEGEL VOR DER ERSTEN MESSUNG — und einen eigenen
      Ausgang fuer „unmessbar". Sonst ist der Unterschied zwischen „die
      Funktion gibt es nicht" und „die Box war aus" im Ausdruck nicht mehr
      zu sehen.

      Und die Regel des Laufs davor gilt weiter, jetzt mit einer Verschaerfung:
      eine Wache, die in keinem Laeufer haengt, rostet nicht nur — sie kann
      ROT ROSTEN UND DABEI EINE FALSCHE BEHAUPTUNG AUFSTELLEN, die spaeter
      jemand zitiert.
    related: ["wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm", "gegenprobe-statt-gruen-glauben", "geschlossenes-vokabular-ohne-konsumenten"]

  - id: namenssuche-nach-wachen-laesst-die-geschwister-stehen
    title: "Die Regel wurde aufgeschrieben und an drei Dateien angewandt - die Suche lief ueber NAMEN, die Geschwister heissen anders"
    kind: architektur
    match: "Sonde ohne Riegel|urteilt an der abgeschalteten Box|boxlisten-je-profil-probe|weiterhoeren-je-profil-probe|spotify-erneuerung-wer|sonden-riegel-deckung|falsche Bestaetigung|Namenssuche nach Wachen"
    sev: mid
    tags: [wachen, sonden, box-unerreichbar, doku-lauf, gegenprobe, 2026]
    stand: "24.08.2026 gemessen und behoben; vier Sonden haben jetzt einen Riegel, die Regel haengt als tools/sonden-riegel-deckung.py in tools/pruefen.sh"
    source: "Stuendlicher Doku-Lauf; beide Doku-Proben sauber, kein neuer Commit seit dem Vorlauf"
    body: |
      EINE REGEL, DIE AN EINER HANDVOLL DATEIEN ANGEWANDT WIRD, IST KEINE
      REGEL — SIE IST EINE REPARATUR. Der Unterschied faellt erst auf, wenn
      man fragt, WORAUS die Handvoll bestand.

      ══ WAS DER LAUF DAVOR GETAN HAT ═══════════════════════════════════════

      dc2bfc21 fand drei ungerufene Wachen, zwei davon urteilten an der
      ABGESCHALTETEN Box, und stellte die Regel auf
      ([[sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts]]):

          JEDE SONDE, DIE UEBER SSH ODER HTTP AN DIE BOX GEHT, BRAUCHT EINEN
          ERREICHBARKEITSRIEGEL VOR DER ERSTEN MESSUNG.

      Die Regel steht seither im Pack. Angewandt wurde sie auf genau die drei
      Dateien, die die damalige Suche gefunden hatte — und die Suche lief ueber
      die NAMEN `*deckung*`, `*pruefen*`, `*halten*`, weil das die Wachen-
      Bedeutung traegt. Das war fuer ihre Frage („welche Wache haengt in keinem
      Laeufer?") richtig. Fuer DIESE Frage ist es ein `grep -v`: die naechsten
      Geschwister heissen `*-probe`, `*-messen`, `*-wer`.

      ══ DREI, DIE WEITER URTEILTEN ═════════════════════════════════════════

      Box aus (ping: „No route to host"), alle drei ohne Argument gestartet:

        * `boxlisten-je-profil-probe.py` druckte den vollen Bericht und
          darunter „URTEIL: WIDERLEGT (zu optimistisch) — die Kinder-
          Oberflaeche liest die Listen NIE — sie erscheinen nicht auf der
          Box". Kein einziges Byte kam von der Box. AUSGANG: 0.
        * `weiterhoeren-je-profil-probe.py`: „Der alte Ort ist GERADE eine
          echte Datei" — eine Aussage ueber den JETZIGEN Dateibaum einer
          Box, die aus war — und daraus „=> „gilt je Profil" ist NICHT
          dicht." Ausgang 1.
        * `spotify-erneuerung-wer.py`: „1 NEIN / 2 NEIN" und dazu
          „Zugangsdaten nicht mehr abrufbar (= Ziel von E15/S4)". Ausgang 0.

      ══ ZWEI VERSCHAERFUNGEN GEGENUEBER DEM VORLAUF ════════════════════════

      (1) DER AUSGANG 0 IST SCHLIMMER ALS DIE 1. Die zwei Sonden des Vorlaufs
      gingen mit 1 und sahen wenigstens aus wie ein Fund. Diese hier melden
      GRUEN und tragen die falsche Behauptung im TEXT. Wer sie je in einen
      Laeufer haengt, bekommt nie einen Anlass hinzusehen.

      (2) EINE FALSCHE BESTAETIGUNG WIEGT SCHWERER ALS EINE FALSCHE
      WIDERLEGUNG. `spotify-erneuerung-wer.py` las den toten Draht nicht als
      „kaputt", sondern als „= Ziel von E15/S4" — der Umbau sei geglueckt.
      Eine falsche Widerlegung macht eine Frage auf; eine falsche Bestaetigung
      SCHLIESST eine. Sie wird zitiert und nicht mehr nachgemessen.

      Die vierte derselben Familie, `hoerzeit-je-profil-probe.py`, luegt
      nicht — sie STIRBT (`json.loads("")` -> JSONDecodeError-Rueckverfolg).
      Das ist die harmlose Haelfte, aber ein Rueckverfolg liest sich wie ein
      Fehler IM WERKZEUG; wer ihn sieht, sucht an der falschen Stelle. Sie
      hat jetzt denselben Riegel — und `sys.exit(main())`, denn ihr `main()`
      wurde ohne Ausgang gerufen: die 2 waere im Nichts gelandet.

      ══ DIE REGEL HAENGT JETZT SELBST IM LAEUFER ═══════════════════════════

      `tools/sonden-riegel-deckung.py`, in `tools/pruefen.sh`. Rot ist nur,
      wer alle drei Eigenschaften traegt: redet im CODE mit der Box (1),
      SCHLUCKT den Fehlschlag (2), faellt danach ein URTEIL (3). Ohne (2)
      stuerzt sie ab — laut, aber nicht luegend. Ohne (3) druckt sie
      Rohwerte, die niemand zitiert.

      ZWEI LEHREN AUS DEM BAU, beide uebertragbar:

        * DER FEHLALARM KAM AUS DER REICHWEITE, NICHT AUS DEM MUSTER. Die
          erste Fassung suchte das Schlucken im GANZEN Dateitext und meldete
          `wiki-pflege.py` rot — irgendwo darin steht ein `except: return ""`
          ueber einer Zeichenkette, mit der Box hat es nichts zu tun. Deren
          Box-Tuer (`stand_holen`) prueft `returncode` und WIRFT, also genau
          richtig. Seither wird nur in der FUNKTION gesucht, die ssh/curl/
          urlopen anfasst (ueber `ast`, nicht ueber Zeilenabstand).
        * EINE ADRESSE IM FLIESSTEXT IST EIN VERWEIS, KEIN ZUGRIFF — dieselbe
          Falle wie bei den Ablagen-Tabellen
          ([[ablage-an-zwei-orten-ohne-gegenstelle]]). `spotify-erneuerung-
          wer.py` nennt die Box-Adresse dreimal, zweimal davon in der eigenen
          Beschreibung. Doku-Bloecke und Kommentare fallen darum vorher weg.

      Und die Wache sagt ihre AUSSCHLUESSE selbst an (360 reden nicht mit der
      Box, 47 brechen ab, 11 urteilen nicht, 42 haben einen Riegel) — eine
      Wache liest man an ihren Ausschluessen, nicht an ihren Treffern.

      ══ GEGENPROBE ═════════════════════════════════════════════════════════

        * Box aus: alle vier Sonden -> Ausgang 2, erste Zeile „ABBRUCH: …
          es wurde NICHTS gemessen".
        * Mit SCHEIN-`ssh` im PATH bzw. einem Wegwerf-Server auf 127.0.0.1
          fuer die HTTP-Sonde: alle vier laufen durch bis zu ihrem eigenen
          Urteil, NULL Abbruchzeilen. Der Riegel blockiert also nicht.
        * Die Wache selbst: Riegel aus `boxlisten-je-profil-probe.py`
          herausgeschnitten (Sicherung im selben Befehlsblock) -> rot mit
          genau dieser Datei; zurueckgespielt -> gruen, `diff` identisch.

      ══ DIE UEBERTRAGBARE FRAGE ════════════════════════════════════════════

      WENN EIN LAUF EINE REGEL AUFSTELLT: woran hat die Suche gehangen, die
      ihre Faelle fand — und traegt die naechste Datei denselben Namen? Eine
      Regel gehoert in eine Wache, sonst gilt sie fuer die Dateien, die man
      zufaellig offen hatte.
    related: ["sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts", "wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm", "wache-sieht-die-schleife-nicht-die-scharf-schaltet", "gegenprobe-statt-gruen-glauben"]

  - kind: falle
    id: warntext-den-niemand-liest
    title: "HTTP 200 + 'locked' im Rumpf — die Sperre wird korrekt gemeldet und von drei Rufstellen nie gelesen"
    sev: mittel
    match: "res.status\\(200\\).send\\('locked'|getResponse\\(\\)|addRawResume|editRawResumeAtIndex|File locked, please try again"
    cause: "`player.page.ts` schreibt gemerkte Stellen im Vorbeigehen (kein `getResponse()`), und `media.service.ts` legt jede Antwort in EIN gemeinsames Feld `response`, das beim Lesen geleert wird"
    fix: "wer einen dieser Endpunkte ruft, wertet die Antwort im selben Zug aus — wie `add.page.ts`/`edit.page.ts` es tun; die Wache ist `tools/antworttext-ausgewertet.py --pruefen` in `tools/pruefen.sh`"
    tags: ['server', 'resume', 'frontend-box', 'falle', 'wache', 2026]
    source: "Doku-Lauf 24.08.2026, gemessen mit tools/antworttext-ausgewertet.py, Rufstellen von Hand gegengelesen"
    body: |
      DER UMBAU WAR RICHTIG UND KOMMT TROTZDEM NICHT AN. Seit dem 02.08.2026
      antwortet `/api/addresume` mit `locked`, statt still eine gemerkte
      Stelle zu verlieren ([[addresume-sperre-nur-ueber-das-lesen]]). Das
      Szenario, das dort beschrieben ist — Box-Schirm und Telefon merken
      gleichzeitig eine Stelle —, laeuft in der klassischen Oberflaeche
      ausschliesslich ueber `player/player.page.ts`. Und genau die liest die
      Antwort nicht.

      GEMESSEN (24.08.2026, rein statisch, tools/antworttext-ausgewertet.py):
      fuenf Endpunkte antworten mit HTTP 200 UND einem Wort im Rumpf.

          /api/add         geprueft von add/add.page.ts
          /api/delete      geprueft von edit/edit.page.ts
          /api/addresume   BLIND: player/player.page.ts
          /api/edit        geprueft von add.page.ts, BLIND: player.page.ts
          /api/editresume  BLIND: player/player.page.ts

      Von Hand gegengelesen: `player.page.ts:1017` ruft
      `editRawMediaAtIndex(...)`, `:1167` `editRawResumeAtIndex(...)`,
      `:1169` `addRawResume(...)` — jeweils als einzelne Anweisung, ohne
      Rueckgabe und ohne folgendes `getResponse()`. Der Statuscode ist 200,
      also sieht auch das Netzwerkfenster nach Erfolg aus.

      DIE ZWEITE HAELFTE, DIE ERST BEIM NACHLESEN AUFFAELLT: `media.service.ts`
      ist `providedIn: 'root'` — EIN Objekt fuer die ganze Oberflaeche — und
      alle fuenf Methoden schreiben ihre Antwort in DASSELBE Feld
      `response`. `getResponse()` gibt es zurueck UND leert es. Wer nicht
      liest, laesst seine Antwort also im gemeinsamen Fach liegen. Die
      Stellen, die lesen, lesen nicht ihre eigene Antwort, sondern die
      zuletzt eingetroffene: `add.page.ts:477` fragt nach einem `setTimeout`
      nach — ein Fenster, in das eine Antwort des Players fallen kann.

      WAS DAS FUER DEN BENUTZER HEISST: server-seitig geht seit dem 02.08.
      nichts mehr verloren, das bleibt richtig. Am Schirm ist eine
      abgewiesene Schreibung aber weiterhin von einer erfolgreichen nicht zu
      unterscheiden — die Kachel steht da und startet von vorn. Der Text
      „File locked, please try again in a moment" existiert, er haengt nur an
      den beiden Seiten, die nie gleichzeitig schreiben.

      NICHT BETROFFEN ist die neue Oberflaeche: sie schreibt ueber
      `POST /api/weiterhoeren`, einen anderen Weg mit eigener Sperrfuehrung.

      ══ WARUM ES SO LANGE STILL WAR ════════════════════════════════════════

      Das Werkzeug, das diese fuenf Zeilen druckt, lag im Baum und wurde von
      KEINEM Laeufer gerufen — und gab bei jedem Fund trotzdem 0 zurueck.
      Dieselbe Geschichte wie bei `stilnamen-pruefen.py` und
      `grenzruf-verb-deckung.py`
      ([[wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm]]), nur eine
      Stufe schlimmer: hier haette selbst ein Laeufer nichts gemeldet.

      Seit dem 24.08.2026 hat es `--pruefen` und haengt in `tools/pruefen.sh`.
      Die drei offenen Stellen stehen als `BEKANNT_BLIND` in der Datei; rot
      wird eine NEUE blinde Rufstelle — und ebenso das Schliessen einer
      bekannten, ohne sie auszutragen. Die gruene Zeile nennt die Zahl der
      offen bleibenden Stellen mit, damit „ok" nicht als „alles wird gelesen"
      gelesen wird.

      ══ GEGENPROBE (alle drei rot, Sicherung im selben Befehlsblock) ═══════

        * `// getResponse()` an `player.page.ts` angehaengt -> 3x ERLEDIGT,
          Ausgang 1.
        * `// this.mediaService.addRawResume(x)` in `rueckweg.ts` -> NEU
          BLIND mit genau dieser Datei, Ausgang 1.
        * im Server `send('locked')` -> `send('gesperrt')` -> „kein einziger
          Endpunkt mit Warnwort gefunden", Ausgang 1. Ohne diese dritte
          Frage waere die Wache gruen, sobald jemand die Antwortwoerter
          umbenennt.

      ══ DIE UEBERTRAGBARE FRAGE ════════════════════════════════════════════

      EIN ENDPUNKT, DER SEINEN FEHLER MIT 200 MELDET, IST ERST FERTIG, WENN
      DIE RUFSTELLE IHN LIEST. Der Serverteil eines solchen Umbaus ist
      pruefbar und wird geprueft; die Rufstelle faellt durch jeden Test, weil
      dort nichts falsch ist — dort ist nur nichts.
    related: ["addresume-sperre-nur-ueber-das-lesen", "wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm", "gegenprobe-statt-gruen-glauben"]
  - id: abgelegt-ist-nicht-eingeschaltet
    kind: falle
    title: "Drei Dienste liegen fertig auf der Karte und laufen nie — ablegen, einschalten und starten sind drei Pruefungen"
    match: "service installiert aber tot|enable fehlt|unit liegt da laeuft nicht|mupi_hat akkuanzeige tot|bootwache laeuft nie|change_checker fehlt|installiert ist nicht eingeschaltet"
    sev: high
    tags: ["autosetup", "systemd", "ausrollwege", "kritiker-lauf", 2026]
    source: >
      25.08.2026, Kritiker-Lauf (AUDIT-2026-08-25.md), HEAD 4eac5375. Funde des
      Skript-Pruefers, vom Hauptlauf am Quelltext gegengelesen (enable-Schleife
      autosetup:1204, einrichten.sh:77ff.); nicht am Geraet gemessen.
    body: |
      Ein Lauf, drei unabhaengige Faelle derselben Klasse:

        * `mupi_hat.service` + `mupi_hat_control.service` werden abgelegt
          (autosetup.sh:1032-1033), stehen aber in KEINER enable-Zeile —
          Akkuanzeige und Abschaltwarnung sind auf jeder frischen Karte tot.
          Der einzige Einschalter (`enable_mupihat.sh`) wird nur in
          Kommentaren erwaehnt.
        * `scripts/box/bootwache.py` (808 Zeilen) wird nach /opt kopiert
          (autosetup.sh:629), aber keine Unit im Repo startet sie; die
          einzige Unit dazu liegt im Installer-Rezept und startet eine
          197-Zeilen-ALTFASSUNG.
        * `config/services/mupi_change_checker.service` installiert kein
          Weg — waehrend Wiederherstellungs-Unit, neustart-ueberlebt.py und
          das Verwaltungsfeld "Medien pruefen alle N s" ihn als lebend
          voraussetzen.

      Dazu der Spiegelfall: `scripts/systemd/einrichten.sh:77` verlangt eine
      Sonde (`/home/dietpi/netzabriss/sonde.py`), die KEIN Ausrollweg ablegt,
      und reisst mit Exit 3 Taster, Knopflicht und Soloist mit — eine
      Diagnose-Sonde blockiert den Ausschalter.

      DIE UEBERTRAGBARE FRAGE: eine Datei am Zielort beweist nur den KOPIER-
      Schritt. Ein Dienst ist erst fertig, wenn drei Fragen getrennt
      beantwortet sind — liegt er da? ist er enabled? und wer startet ihn
      beim ERSTEN Lauf (restart ohne enable ueberlebt keinen Neustart,
      mupibox.yaml:435)? Die vorhandenen Wachen pruefen fast alle nur
      Frage 1; genau dort hat der Doku-Lauf 41005aa9 schon einmal gegraben
      (achtzehn Wachen lasen systemctl enable, keine die Schleife). Eine
      Wache, die config/services/ gegen die enable-Stellen ALLER Ausrollwege
      haelt, faengt die ganze Klasse.
    related: ["wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm", "drei-ausrollwege-keiner-kennt-den-anderen", "zwei-ausrollwege-eine-handgefuehrte-liste"]
  - id: ungerufene-wache-driftet-in-den-fehlalarm
    title: "52 Werkzeuge urteilen ueber ihren Ausgang und haengen in keinem Laeufer - zwei davon waren rot"
    kind: wachen
    match: "wache laeuft nirgends|ungerufene wache|werkzeug haengt in keinem laeufer|pruefen.sh ruft nicht|wache rostet"
    sev: high
    tags: [wachen, werkzeuge, pruefen, fehlalarm, 2026]
    stand: "25.08.2026 gemessen; tools/ungerufene-wachen.py haengt in tools/pruefen.sh und meldet gruen"
    source: "Stuendlicher Doku-Lauf; die vier Laeufe davor fanden je EINE solche Wache von Hand"
    body: |
      Vier Laeufe hintereinander fanden je EIN Werkzeug, das eine Frage
      bewacht und von keinem Laeufer gerufen wird — `stilnamen-pruefen.py`
      (seit 08.08. rot, Fehlalarm), `grenzruf-verb-deckung.py` (zwei
      Fehlalarme), `sonden-riegel-deckung.py`, `antworttext-ausgewertet.py`
      (gab bei jedem Fund trotzdem 0 zurueck). Jeder dieser Funde war ein
      Zufall beim Danebenschauen, und jedes Mal lautete der Satz danach „eine
      Wache, die nirgends laeuft, ist keine". Vier Mal denselben Fund von Hand
      zu machen ist eine fehlende Wache.

      ══ DIE FRAGE LAESST SICH NICHT IN NAMEN STELLEN ═══════════════════════
      Die Suche des 24.08. lief ueber `*deckung*` / `*pruefen*` / `*halten*`
      und uebersah die Geschwister `*-probe`, `*-messen`, `*-wer`. Gefragt
      werden muss nach dem VERHALTEN: ein Werkzeug, dessen AUSGANG von einem
      Fund abhaengt (`sys.exit(1)`, `process.exit(1)`, `exit 1`), behauptet
      damit, eine Wache zu sein. `tools/ungerufene-wachen.py` haelt genau das
      gegen die Erreichbarkeit von `tools/pruefen.sh` und
      `tools/doku-luecken-probe.sh` aus — TRANSITIV, denn ein Werkzeug, das
      ein gerufenes Werkzeug ruft, ist gedeckt. Ergebnis beim ersten Lauf:
      52 Stueck.

      ══ ZWEI DAVON WAREN ROT, UND ZWAR AUF FEHLALARM ═══════════════════════
        * `tools/schirm-sandkasten.py` meldete „1 Lauf der NEUEN Fassung
          sitzt nicht": im Fall `mit-wled` stand der Schirm auf „an" statt
          „aus" UND `wled_send_data.py` wurde nicht gerufen. Beides EIN
          Ursprung: `kiosk_laeuft()` in `scripts/mupibox/mupi_start_led.sh`
          fragt seit dem Zwei-Browser-Umbau mit
          `pgrep -f '(^|/)(chromium[a-z-]*|cog)[[:space:]].*https?://'`,
          frueher mit `pidof`. Der Sandkasten stellt nur `pidof` nach und gibt
          dem Skript einen EIGENEN PATH — `pgrep` fehlte dort ganz, der Aufruf
          schlug fehl, `while ! kiosk_laeuft; do sleep 3; done` lief endlos,
          und der Stillstand wurde als Befund an der gemessenen Fassung
          ausgegeben. Am Code der Box war nichts.
        * `tools/update-frische-box-probe.sh` meldete „19 gruen, 1 rot": der
          rote Punkt hiess „der neue Code ist ausgepackt" und dahinter stand
          `zip: Kommando nicht gefunden`. Ein fehlendes Paket sah aus wie ein
          Befund am Update.

      ══ DIE REGEL DAHINTER ═════════════════════════════════════════════════
      Eine Wache, die niemand ruft, wird nicht bloss uebersehen — sie DRIFTET.
      Das Gemessene aendert sich weiter, die Attrappen daneben nicht, und der
      erste Mensch, der sie Monate spaeter startet, liest einen Fehlalarm als
      Befund. Beide Faelle hier hatten dieselbe Form: nicht das Gepruefte war
      kaputt, sondern die Vorrichtung, die es prueft.

      Zwei Riegel sind daraus geworden:
        * `schirm-sandkasten.py` bricht jetzt AB (statt rot zu melden), wenn
          das gepruefte Skript mit `pidof`/`pgrep`/`ps`/`systemctl` nach einem
          Prozess fragt und der Sandkasten den Befehl nicht nachstellt. In
          Befehlsstellung gesucht — `{"ps":` im WLED-Datensatz ist ein
          JSON-Schluessel und hatte die erste Fassung des Riegels zu Fall
          gebracht.
        * `update-frische-box-probe.sh` prueft `command -v zip` VOR der ersten
          Messung und geht mit 2 hinaus. Dieselbe Regel wie fuer die
          Box-Sonden ([[sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts]]), nur fuer den
          Werkzeugkasten statt fuer die Leitung.

      ══ WAS JETZT LAEUFT UND WAS BEGRUENDET DRAUSSEN BLEIBT ════════════════
      Elf hermetische Wachen sind neu in `tools/pruefen.sh` (kein Geraet, kein
      Browser, kein Netz, zusammen unter einer halben Minute): der
      Sicherungsring ohne SSH (73 Aussagen), Papierkorb/Leerlaufuhr/
      Update-Bestand unter Beschuss, die Lautstaerkestufe, das Plugin-Geruest,
      das Nachziehen der Plugins, der Verwaltungsvertrag, die Malpaare, der
      Schirm-Sandkasten und die Wache ueber den Wachen selbst.

      51 bleiben mit GENANNTEM Grund in `BEKANNT` — Box-Sonden, Browser- und
      Vorschau-Proben, drei, die ins Internet gehen (Archive, ARD), und
      Einmalmessungen zu einem Vorschlag. `--alle` druckt die Liste mit ihren
      Gruenden. Rot wird die Wache bei einer NEUEN ungerufenen Wache, bei
      einem BEKANNT-Eintrag, den es nicht mehr gibt, bei einem, der inzwischen
      doch gerufen wird, bei einem fehlenden Laeufer und wenn ihr Suchmuster
      gar keinen Ruf mehr findet. Alle fuenf Richtungen gegengeprobt.

      ══ ZWEI FALLEN BEIM BAU DIESER WACHE ══════════════════════════════════
        * Kommentare zaehlen NICHT als Ruf. `pruefen.sh` erklaert ueber jedem
          Schritt, warum es ihn gibt, und nennt dabei reihenweise andere
          Werkzeuge; die erste Fassung hielt zehn davon faelschlich fuer
          gedeckt — darunter `sicherung-ohne-ssh-ring.ts` mit seinen 73
          Aussagen. Es ist derselbe Irrtum, der `antworttext-ausgewertet.py`
          drei Wochen unsichtbar hielt (es stand in der README).
        * Blockkommentare nur fuer JS/TS entfernen. In einem Shell-Skript
          steht `"$BAU"/*` und irgendwo weiter unten `*/`; ein
          `/\*.*?\*/` ueber DOTALL frisst alles dazwischen und meldete
          `dienste-doku-deckung.sh` als ungerufen, obwohl der Ruf in
          `doku-luecken-probe.sh:238` steht.

      Was `git` noch nicht kennt, wird nicht mitgezaehlt: ein Werkzeug, das
      nebenan gerade entsteht, ist Arbeit im Gang und keine ungerufene Wache
      (fremde Arbeit gehoert nicht in den eigenen Befund — dieselbe Trennlinie, andere
      Richtung). `--alle` nennt auch diese Namen, damit die Auslassung nicht
      still ist.
    related: ["wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm", "namenssuche-nach-wachen-laesst-die-geschwister-stehen", "sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts", "warntext-den-niemand-liest", "gegenprobe-statt-gruen-glauben"]

  - id: audit-liste-ist-keine-dokumentation
    title: "Der Einstieg, den die README nennt, stand in keinem Handbuch - und ein totes Skript sass 22 Tage darin"
    kind: doku
    match: "npm run test:frontend-api|No workspaces found|mupibox-frontend-api|npm run test haengt|npm run lint ueberspringt admin|npm-Skripte Wurzel"
    sev: mid
    tags: [doku, wachen, npm, arbeitsbereiche, 2026]
    stand: "25.08.2026 gemessen; tools/npm-skripte-deckung.py meldet gruen, nachdem Abschnitt 7.4 geschrieben wurde"
    source: "Stuendlicher Doku-Lauf; die Probe meldete diesen Bereich gar nicht, weil ihn keine Wache kannte"
    body: |
      `README.md`, Abschnitt 5, nennt die npm-Skripte des Wurzelverzeichnisses
      als DEN Weg, hier zu entwickeln, und verweist fuer „Einzelheiten" auf
      `dokumentation/mixpibox.md`, Abschnitt 7. Abschnitt 7 beschrieb
      `tools/pruefen.sh` und die Vorschauen — und KEINES der zwanzig Skripte.
      Gemessen: null Treffer fuer jeden einzelnen Namen in allen Handbuechern.
      Der Verweis lief ins Leere, und niemandem fiel es auf, weil elf
      Doku-Wachen pruefen, was die Doku SAGT, und keine, was sie VERSCHWEIGT.

      ══ WAS IN DEM LOCH SASS ═══════════════════════════════════════════════
      `test:frontend-api` rief `--workspace=mupibox-frontend-api`. Den
      Arbeitsbereich gibt es nicht — es sind `mupibox-backend-api`,
      `mupibox-backend-player`, `mupibox-frontend-box`,
      `mupibox-frontend-admin`. `npm run test:frontend-api` bricht mit
      `npm error No workspaces found` ab.

      DAS EIGENTLICHE ERGEBNIS IST NICHT DAS SKRIPT, SONDERN SEIN ALTER. Der
      Befund steht wortgleich in `AUDIT-2026-08-03.md` (Zeile 105) UND in
      `AUDIT-2026-08-23.md` (Punkt 11). Zweimal gefunden, sauber
      aufgeschrieben, 22 Tage nicht behoben. Eine Audit-Datei wird einmal
      gelesen und altert dann still; sie ruft niemanden. **Ein Befund in einer
      Audit-Liste ist keine Dokumentation.** Wer etwas findet, schreibt es ins
      Handbuch UND baut die Wache — sonst findet es die naechste Sitzung wieder.

      ══ ZWEI LOECHER, DIE BLEIBEN (bewusst, mit Grund) ══════════════════════
        * `npm run test` in der Wurzel LAEUFT NIE DURCH. Die Faecherung geht
          in `frontend-box` und `frontend-admin`, deren eigenes `test` das
          nackte `ng test` ist: Karma steht im Beobachtungsmodus und endet
          nicht. Dazu braucht Karma einen Chrome, der hier nicht installiert
          ist. Geprueft wird ueber `tools/pruefen.sh` — es loest den Browser
          selbst auf und ruft `ng test --watch=false` bzw. `--configuration ci`.
        * `npm run lint` und `lint:fix` faechern ueber `--workspaces`, und
          `frontend-admin` hat gar kein `lint`. npm ueberspringt es
          KOMMENTARLOS — die Verwaltung wird von der Wurzel aus nie geprueft.
          (Zustand 25.08.2026: die beiden Backends melden 130 Fehler und 48
          Warnungen. Der Zustand hat Haltbarkeit, die Ursache nicht.)

      Beide zu schliessen ist eine Aenderung am BAU, nicht an der Doku. Die
      Wache zaehlt sie darum nicht rot, sondern druckt sie als HINWEIS — eine
      Wache, die dauerhaft rot steht, wird abgeschaltet. Sichtbar halten
      schlaegt gruen luegen.

      ══ DIE WACHE ══════════════════════════════════════════════════════════
      `tools/npm-skripte-deckung.py` (laeuft in `tools/doku-luecken-probe.sh`)
      prueft zwei Richtungen rot: jedes `--workspace=` gegen die
      Arbeitsbereiche, die sich unter `src/*` wirklich SO NENNEN (der Name
      steht in deren `package.json`, nicht im Ordnernamen), und jeden
      Skriptnamen in Backticks gegen `dokumentation/mixpibox.md`. Backticks
      aus demselben Grund wie bei `RECHTE` und `SEKTIONEN`: `build`, `test`,
      `serve` und `lint` stehen in jeder Entwicklerdoku zufaellig herum.
      Ohne Skripte oder ohne Arbeitsbereiche meldet sie Warnung statt gruen.
      Beide Richtungen gegengeprobt (ein Skript umbenannt und auf einen
      erfundenen Bereich gezeigt: 2 LUECKEN, beide Wachen bissen).
    related: ["geschlossenes-vokabular-ohne-konsumenten", "volles-wiki-ist-kein-beleg-fuer-gepflegte-doku", "gegenprobe-statt-gruen-glauben", "wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm"]
  - id: umgebung-ist-vertragsflaeche
    title: "64 Stellschrauben in der Umgebung, drei in der Doku - darunter der Riegel gegen das Herunterfahren"
    kind: doku
    match: "MUPIBOX_STANDWACHE_NIE_HANDELN|MUPIBOX_SICHERUNG_PW|Umgebungsvariable nicht dokumentiert|welche MUPIBOX_ variablen gibt es|env variable box|MUPI_IDLE_NUR_FUNKTIONEN|MUPIBOX_HERKUNFT_AUS"
    sev: mid
    tags: [doku, wachen, umgebung, riegel, 2026]
    stand: "25.08.2026 gemessen; tools/umgebungsvariablen-deckung.py meldet gruen, nachdem Abschnitt 7.8 geschrieben wurde"
    source: "Stuendlicher Doku-Lauf. Die Doku-Luecken-Probe meldete diesen Bereich gar nicht - keine ihrer elf Wachen kannte die Umgebung."
    body: |
      Elf Doku-Wachen halten die Handbuecher gegen den Quelltext: Endpunkte,
      Rechte, Ereignisse, Konfigurationsfelder, Dienste, Ablagen, Plugins,
      npm-Skripte. Keine kannte die UMGEBUNG. Gemessen am 25.08.2026: `src/`
      und `scripts/` lesen 64 Variablen mit `MUPIBOX_`/`MUPI_`-Praefix, DREI
      davon standen in einem Handbuch (`MUPIBOX_CONFIG_DIR`, `MUPI_SRC`,
      `MUPI_LOCAL_SRC`). Die uebrigen 61 fand nur, wer den Quelltext aufmachte
      - und wer ueberhaupt ahnte, dass es sie gibt.

      ══ DER TEURE FALL ══════════════════════════════════════════════════════
      `MUPIBOX_STANDWACHE_NIE_HANDELN` (`scripts/box/mupibox-standwache.py`).
      Die Hausregel „was am Geraet dreht, braucht eine benannte Naht UND einen
      Umgebungsriegel" ist dort sauber umgesetzt:
      benannte Naht (`Welt`) UND Umgebungsriegel, und der Riegel ist absichtlich
      herum gedreht - er SPERRT das Handeln, statt es freizugeben, damit ein
      verlorener Riegel keine stumme tote Wache hinterlaesst.

      Nur stand er in keinem Handbuch. Ein Sicherheitsriegel, den man erst
      findet, wenn man die Datei liest, schuetzt genau die Person nicht, fuer
      die er gebaut wurde: die, die einen Test DANEBEN schreibt. Die
      Hausregel war halb umgesetzt, und die halbe Haelfte war die Doku.

      Zweiter Fall: `MUPIBOX_SICHERUNG_PW` ist der EINZIGE Weg, die Sicherung
      ohne Tastatur zu entschluesseln (kein Befehlszeilenschalter, weil `ps`
      Argumente jedem Benutzer zeigt). Ein Weg, den die Doku nicht kennt,
      existiert fuer den Betreiber nicht.

      ══ DIE WACHE SUCHT DEN ZUGRIFF, NICHT DEN NAMEN ════════════════════════
      `tools/umgebungsvariablen-deckung.py`. Drei Befunde aus dem Bau, jeder
      eine eigene Falle:

        1. EINE NAMENSSUCHE HAETTE `MUPIBOX_STANDWACHE_ECHT` GEMELDET. Der Name
           steht in der Standwache - im KOMMENTAR, als die verworfene
           Gegenrichtung des Riegels. Eine Variable, die es nicht gibt, zu
           dokumentieren ist schlimmer als sie wegzulassen: dann setzt sie
           jemand. Dieselbe Falle wie bei [[ungerufene-wache-driftet-in-den-fehlalarm]].

        2. DIE SUCHE NACH DER ZUGRIFFSFORM LIESS AUSGERECHNET DIE ZWEI
           WICHTIGSTEN AUS. `RIEGEL = "MUPIBOX_STANDWACHE_NIE_HANDELN"` und
           `PW_UMGEBUNG = "MUPIBOX_SICHERUNG_PW"` werden ueber eine Konstante
           gelesen; im `os.environ.get(RIEGEL)` steht kein Name mehr. Genau die
           zwei, denen jemand einen eigenen benannten Platz gab WEIL sie wichtig
           sind, waren unsichtbar. Die Wache liest darum zusaetzlich
           Konstanten-Zuweisungen - aber nur in Dateien, die die Umgebung
           ueberhaupt anfassen.

        3. NICHT JEDER NAME MIT PRAEFIX IST EINE STELLSCHRAUBE.
           `MUPI_LOCAL_SRC` (`make-boot-sd.sh`) setzt das Skript sich selbst -
           es exportiert es in Z. 126 fuer sein Kind und liest es in Z. 128.
           Wer es von aussen setzt, sieht seinen Wert ueberschrieben. In der
           Shell zaehlt darum `${NAME:-vorgabe}` immer, ein nacktes `$NAME` nur,
           wenn die Datei den Namen nirgends selbst setzt.

           NACHTRAG 25.08.2026: `MUPI_URL` stand hier daneben und war falsch
           (ueberholt). Die Ausnahme gilt fuer `chromium-autostart.sh:152` -
           eine Zuweisung ohne Bedingung; die hier genannte Zeile 101 ist
           ausserdem ein anderer Ort (dort steht `CHROMIUM_OPTS`). Sie gilt
           NICHT fuer `tools/pruefen.sh`, das `${MUPI_URL:-https://mupibox:8443}`
           an vier Stellen von AUSSEN liest (Z. 971, 978, 986, 1002) und sich
           `MUPI_HOST` daraus ableitet. **Eine Ausnahme gilt je DATEI, nicht je
           NAME** - und eine Aufzaehlung leiht dem falschen Eintrag die
           Glaubwuerdigkeit des richtigen Nachbarn. Seither
           `tools/box-adresse-deckung.py`, siehe
           [[ausnahmeliste-gilt-je-datei-nicht-je-name]].

      ══ GEGENGEPRUEFT ═══════════════════════════════════════════════════════
      Beide Richtungen, je mit Sicherung und Wiederherstellung: einen Namen aus
      Abschnitt 7.8 entfernt -> rot; eine neue `process.env.MUPIBOX_...`-Zeile
      in `herkunft.ts` -> rot, und die Gesamtzahl stieg von 64 auf 65. Danach
      beide Dateien byteweise zurueck. Eine Gegenprobe, deren ERFOLG der Schaden
      ist, braucht die Sicherung im selben Befehlsblock
      ([[gegenprobe-statt-gruen-glauben]]).

      ══ WO ES STEHT ═════════════════════════════════════════════════════════
      `dokumentation/mixpibox.md` Abschnitt 7.8, nach Wirkung gegliedert statt
      alphabetisch: 7.7.1 Riegel und Geheimnis zuerst (die vier, die etwas TUN),
      dann Netz/TLS/Ports, Pfade, Identitaet/Plugins, Box-Skripte. Die Wache
      haengt in `tools/doku-luecken-probe.sh`.

  - kind: lehre
    id: kommentar-behauptet-fremden-fehler
    sev: medium
    title: "Ein Kommentar, der einen Fehler in einer FREMDEN Datei behauptet, wird nicht mit ihr behoben - er stand 19 Tage"
    match: "veralteter kommentar|luegt bis heute|faellt weiterhin zurueck|kommentar ueber andere datei|stale comment|befund veraltet"
    cause: >
      `konfiguration.ts` trug seit dem 05.08.2026 (ea2c09bf) den Satz, `alsModus`
      in `src/frontend-box/.../sperrlogik.ts` falle "weiterhin auf aus zurueck",
      und dieses Feld "luege deshalb bis heute". Behoben wurde das EINEN TAG
      SPAETER (06.08.2026, f51833ed): seither gibt `alsModus` fuer alles
      Unbekannte `rechnen` zurueck. Der Satz blieb 19 Tage stehen. Wer ihn las,
      hielt eine Box mit fehlendem Schluessel fuer offen - sie fragt.
    fix: >
      Wer einen Fehler in einer fremden Datei im Kommentar behauptet, schreibt
      das DATUM dazu; wer den Fehler behebt, greppt nach dem, was ihn behauptet.
      Am Baum gefunden mit `git log -S` auf beide Textstellen - das datiert
      Behauptung und Behebung und entscheidet, welcher Kommentar der alte ist.
    tags: ['doku', 'kommentar', 'lehre', 'kindersicherung', 2026]
    source: "25.08.2026, Doku-Lauf. Erhoben an src/backend-api/src/konfiguration.ts, src/frontend-box/src/app/einstellungssperre/sperrlogik.ts, git log -S."
    body: |
      DIE TEURERE HAELFTE VON [[drei-orte-eine-anzeige]].
      Dort driften WERTE auseinander, und dagegen helfen Wachen, die Werte
      vergleichen. Hier driften BEFUNDE ueber Werte - und die kann keine
      Namens- oder Wertepruefung fassen. Ein Kommentar in Datei A, der eine
      Aussage ueber Datei B macht, hat keine Naht, an der er mitgeaendert
      wuerde: er faellt nur auf, wenn jemand nachsieht.

      WARUM GERADE HIER TEUER: die Aussage betraf die Kindersicherung. Der
      Satz erklaerte einem Leser, das Verwaltungs-Board zeige "jeder kommt
      hinein" ueber eine Box, die in Wahrheit nachfragt. Das ist die Sorte
      Behauptung, nach der jemand etwas "repariert", das steht.

      DREI WEITERE FUNDE DERSELBEN BAUART, im selben Lauf, alle um die
      Einstellungssperre:
        1. `app.routes.ts` sprach von "diesen fuenf Routen" (geschrieben
           31.07.2026). `/klang` bekam am 21.08.2026 die `canActivate` und ist
           die SECHSTE. Die Liste wuchs, die Zahl daneben nicht.
        2. `app.routes.ts` UND `einstellungssperre.guard.ts` nannten "aus" die
           VORGABE. Seit dem 06.08.2026 ist die Vorgabe `rechnen`: ein
           FEHLENDER Schluessel ist keine Entscheidung fuer offen, sondern eine
           fehlende Entscheidung, und die darf nicht die schwaechste sein.
        3. Der Seitenbestand der klassischen Oberflaeche stand nirgends: 15 der
           16 Wege aus `src/frontend-box/src/app/app.routes.ts` kamen in
           `dokumentation/mixpibox.md` mit NULL Treffern vor. Die Probe hielt
           bis dahin nur die Routen der VERWALTUNG - zwei Routentabellen, eine
           Wache. Eine gruene Probe deckt nur, was in ihr haengt.

      ══ WAS DAGEGEN STEHT ═══════════════════════════════════════════════════
      `tools/box-seiten-deckung.py`, eingehaengt in
      `bash tools/doku-luecken-probe.sh`. Sie prueft BEIDE Richtungen: jeden
      `path` gegen die Doku, UND die Menge der `canActivate`-Wege gegen die
      Aufzaehlung im Text. Die zweite Richtung ist die, die hier gefehlt hat -
      eine reine Namenspruefung waere gruen gewesen, sobald die 16 Namen
      irgendwo stehen, auch ohne ein Wort darueber, welche bewacht sind.

      ══ GEGENGEPRUEFT ═══════════════════════════════════════════════════════
      Vier Sabotagen, Sicherung und Wiederherstellung im selben Befehlsblock -
      eine Gegenprobe, deren ERFOLG der Schaden ist, braucht das Wegwerfziel
      daneben. Und weil die Arbeit noch nicht committet war, NICHT ueber
      `git checkout`, der die uncommittete Arbeit mitgerissen haette
      ([[rotprobe-war-der-test-ohne-die-aenderung-rot]]): einen Weg aus der Doku entfernt ->
      rot; einen bewachten Weg zu viel behauptet -> rot; eine `canActivate`
      aus der Routentabelle entfernt -> rot; die Routentabelle verschoben ->
      WARNUNG statt gruen.

      DIE ERSTE FASSUNG DER WACHE WAR SELBST EINE LEHRE: sie band die
      Aufzaehlung mit `^Hinter der Sperre liegen` an den Zeilenanfang und
      meldete rot, obwohl der Satz dastand - Markdown hatte ihn mitten in die
      Zeile umgebrochen und die Liste ueber zwei verteilt. Eine Wache, die am
      Zeilenumbruch der Doku scheitert, erzieht dazu, sie zu ignorieren.

      ══ WO ES STEHT ═════════════════════════════════════════════════════════
      `dokumentation/mixpibox.md` Abschnitt 4.3.1 - der Seitenbestand als
      Tabelle, die sechs bewachten Wege, warum `/add` mit Absicht fehlt (nur
      ueber das bewachte `/edit` erreichbar), die Vorgabe `rechnen`, und die
      gemeinsame Freigabe ueber `NewDesign/freigabe.json` (zwei Minuten,
      `sessionStorage`, kein gleitendes Fenster - ein gleitendes machte daraus
      "solange jemand tippt", und der Tippende koennte das Kind sein).

  - kind: lehre
    id: begruendung-liest-sich-als-stand
    sev: medium
    title: "Die BEGRUENDUNG eines Umbaus beschreibt die Welt davor - und wird als heutiger Stand gelesen"
    match: "heute wird je datei|box-weit geblieben|warum ein ordner|begruendung veraltet|vorschlag laengst gebaut|noch offener punkt laengst gebaut"
    cause: >
      Der Absatz "WARUM EIN ORDNER UND KEIN NAMENSZUSATZ MEHR" in
      `src/backend-api/src/profile.ts` begruendete den Bereichs-Umbau vom
      05.08.2026 damit, dass `kinderzeit.json` und `listen.json` "box-weit
      geblieben" seien, "obwohl sie es nicht sein sollten". Beides stimmt heute
      nicht mehr - und fuer `kinderzeit.json` hat es nie gestimmt: 23 Zeilen
      TIEFER IN DERSELBEN DATEI steht im Kasten "WAS HIER (NOCH) NICHT STEHT,
      MIT GRUND", dass es box-weit gehoert, weil `regelnFuer()` die Hausregel
      sonst nicht mehr faende. `listen.json` liegt seit E18/S3 im Bereich
      (`ABLAGE_LISTEN`, gleich darunter). Dieselben zwei Saetze standen
      wortgleich in `BACKLOG.md` - dort im Praesens ("Heute wird je DATEI ein
      Namenszusatz angehaengt") als OFFENER Punkt.
    fix: >
      Eine Begruendung im Praesens datieren oder ins Praeteritum setzen, und den
      Satz "das ist die Begruendung, nicht der Stand" danebenschreiben. Wer eine
      Ablage bewusst NICHT mitnimmt, nennt sie in der Begruendung nicht in
      derselben Reihe wie die vergessenen - sonst liest der naechste einen
      offenen Umzug.
    tags: ['doku', 'backlog', 'kommentar', 'lehre', 'profile', 2026]
    source: "25.08.2026, Doku-Lauf. Erhoben an src/backend-api/src/profile.ts, BACKLOG.md, src/backend-api/src/server.ts."
    body: |
      DIE SPIEGELUNG VON [[kommentar-behauptet-fremden-fehler]].
      Dort behauptet ein Kommentar einen Fehler in einer FREMDEN Datei. Hier
      behauptet er einen Zustand, der in DERSELBEN Datei widerlegt ist - und
      genau deshalb faellt er nicht auf: wer die Datei liest, liest die
      Begruendung oben und den Kasten unten in verschiedenen Sitzungen.

      DREI SCHICHTEN DERSELBEN STELLE, alle drei falsch gelesen:
        1. `listen.json` war bis E18/S2 box-weit. Der Satz stimmte am
           05.08.2026 und ist seit E18/S3 ueberholt.
        2. `kinderzeit.json` stand in derselben Aufzaehlung - und gehoert
           NICHT dazu. Es ist der Regelsatz `{standard, je}`, box-weit MIT
           Absicht. Eine Aufzaehlung macht aus dem einen Vergessenen und dem
           einen Gewollten zwei Vergessene.
        3. Der Punkt "EIN EIGENER BEREICH JE NUTZER" stand im `BACKLOG.md`
           unter "Was fuer Stufe 2 offen bleibt", im Praesens, obwohl er am
           05.08.2026 gebaut wurde. Der Nachtrag-Kasten vom 24.08.2026
           korrigierte die DREI Punkte darunter (Listen, Anmeldebildschirm,
           Zeitbudget) und liess diesen stehen - waehrend seine eigene
           Kopfzeile "von den vier Punkten oben" zaehlt.

      WARUM DER NACHTRAGS-KASTEN DEN PUNKT UEBERSPRANG: die drei korrigierten
      Punkte hatten je einen SATZ, der widerlegbar war ("ist noch box-weit").
      Dieser hier war als VORSCHLAG formuliert ("vielleicht legt man beim
      anlegen gleich einen eigenen bereich pro nutzer ein") - ein Vorschlag
      wird nicht falsch, wenn man ihn umsetzt, er wird nur still erfuellt.
      Wer eine Liste offener Punkte durchgeht und nach falschen AUSSAGEN sucht,
      findet Vorschlaege nicht. Nach dem gebauten ARTEFAKT greppen
      (`BEREICH_ORDNER`), nicht nach der Behauptung.

      NEBENBEFUND IN DEMSELBEN ABSATZ: "Gemeinsam bleibt ... Bibliothek,
      Darstellung, Themen" - die Darstellung gehoert seit dem 15.08.2026 dem
      Kind (`ABLAGE_DARSTELLUNG` in `BEREICH_ABLAGEN`); box-weit sind nur noch
      die Themen und der Rueckfall ([[ablage-an-zwei-orten-ohne-gegenstelle]]).
      Und der Pfad war falsch geschrieben: `profil/<kennung>` statt `profile/`.

      ══ GEGENGEPRUEFT ═══════════════════════════════════════════════════════
      Jede genannte Naht am Baum nachgesehen, nicht aus dem Text uebernommen:
      `BEREICH_ORDNER = 'profile'` (profile.ts), `ABLAGE_DARSTELLUNG` und
      `BEREICH_ABLAGEN` (profile.ts), `OHNE_UMZUG` und `bereicheHerrichten()`
      (server.ts:1416ff), `bereichAnlegen()` an vier Rufstellen. Die erste
      Fassung des BACKLOG-Nachtrags nannte `bestandUmziehen()` - die Funktion
      gibt es nicht, sie heisst `bereichUebernehmen()`. Eine Korrektur, die
      einen Namen ERFINDET, ist die naechste Fundstelle. `npx tsc --noEmit`
      sauber, beide Doku-Proben unveraendert.

      ══ WO ES STEHT ═════════════════════════════════════════════════════════
      `src/backend-api/src/profile.ts` (Kopf des Bereichs-Abschnitts, jetzt mit
      dem Riegel "DIESER ABSATZ IST DIE BEGRUENDUNG DES ORDNERS, NICHT DER
      STAND") und `BACKLOG.md` E18/Stufe 2 - der Punkt als GEBAUT markiert, der
      Nachtrags-Kasten um seine vierte Zeile ergaenzt.

  - id: wache-stirbt-still-wenn-sie-nirgends-haengt
    title: "Zwei Wachen schnitten an einer Naht, die vor 9 Tagen umzog - und meldeten seither gar nichts"
    kind: fehler
    match: "ValueError: substring not found|helfer_laden|skriptweg-sandkasten|skriptweg-rueckweg-luecken|mupibox-tauscher.py|BOXHELFER"
    sev: high
    tags: [wachen, ausliefern, skriptweg, e42, doku, 2026]
    stand: "25.08.2026 repariert und in tools/pruefen.sh eingehaengt; beide melden gruen (43 bzw. 25 Proben)"
    source: "Stuendlicher Doku-Lauf. Die Doku-Luecken-Probe meldete gruen; gefunden beim Nachsehen, welche Wachen in keinem Laeufer haengen."
    body: |
      `tools/skriptweg-sandkasten.py` und `tools/skriptweg-rueckweg-luecken.py`
      pruefen den Skript-Ausrollweg gegen den ECHTEN Boxhelfer statt gegen eine
      Nachbildung. Sie holten ihn sich, indem sie die Zeichenkette `BOXHELFER`
      (ein rohes Mehrzeilen-Literal) aus `tools/ausliefern.py` herausschnitten.

      Mit E42 (16.08.2026) zog der Helfer in eine eigene Datei um:
      `scripts/box/mupibox-tauscher.py`. `ausliefern.py` LIEST sie seither nur
      noch ein. Beide Wachen suchten weiter die alte Naht, fanden sie nicht und
      starben in `helfer_laden` mit `ValueError: substring not found` - nicht
      rot, sondern ohne jedes Urteil. Neun Tage lang.

      ══ WARUM ES NIEMAND MERKTE ═════════════════════════════════════════════
      Sie hingen in KEINEM Laeufer. `tools/pruefen.sh` rief sie nicht, die
      Doku-Luecken-Probe auch nicht. Und `tools/ungerufene-wachen.py` - die
      Wache ueber den Wachen, die genau diesen Fall finden soll - sah sie
      ebenfalls nicht: ihr `urteilt()` erkennt nur den woertlichen Ausgang
      (`sys.exit(1)`, `process.exit(1)`, `exit 1`). Beide Dateien urteilen ueber
      `sys.exit(main())` mit `return 1` in `main()`. Fuer die Wachenwache waren
      sie Messgeraete, keine Wachen.

      ══ WARUM DER BLINDE FLECK NICHT EINFACH ZUGEMACHT WURDE ════════════════
      Gemessen: `sys.exit(main())` in `urteilt()` aufzunehmen bringt diesen
      Schritt von 0 auf 89 Luecken. Fast alle zu Recht ungerufen -
      `mupi-check.py` laeuft auf der Box, `wiki-suche.py` ist eine Abfrage,
      `librespot-anmelden.py` will einen Menschen. Eine Wache, die immer rot
      ist, ist dieselbe Nullnummer wie eine, die immer gruen ist. Die
      Erweiterung ist erst eine Verbesserung, wenn die 89 einzeln durch die
      BEKANNT-Liste gegangen sind. Der Befund steht so lange im Docstring von
      `ungerufene-wachen.py` - mit der Zahl, damit niemand ihn zweimal messen
      muss.

      ══ WAS GEMACHT WURDE ═══════════════════════════════════════════════════
      * Beide `helfer_laden` lesen jetzt `scripts/box/mupibox-tauscher.py`.
        Der Sandkasten kennt BEIDE Formen, weil seine Probe 7 absichtlich eine
        alte Fassung aus der Geschichte laedt - die hat die Zeichenkette noch.
      * Fehlt die Datei oder der `main()`-Aufruf am Ende, brechen sie mit einer
        Ansage ab statt mit einem Stapelabzug. Gegengeprobt: Naht verstellt ->
        Ausgang 1 mit Klartext, Datei danach wiederhergestellt.
      * Beide haengen jetzt in `tools/pruefen.sh` (89 unbedingte Schritte statt
        87). Sie fassen die Box nicht an, alles laeuft unter /tmp.

      ══ DIE LEHRE ═══════════════════════════════════════════════════════════
      Eine Wache, die an einer Naht im Quelltext eines ANDEREN Werkzeugs
      schneidet, muss den Fall "Naht weg" als Abbruch mit Ansage behandeln -
      sonst ist der Umzug dieser Naht ein stiller Ausfall. Und sie gehoert in
      einen Laeufer, sonst faellt der Ausfall erst auf, wenn jemand zufaellig
      danebenschaut. Siehe `gruene-probe-deckt-nur-ihre-wachen` und
      `ungerufene-wache-driftet-in-den-fehlalarm`.

  - id: eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen
    title: "Die Pfadwache las drei Dateien und hiess HANDBUECHER - im BACKLOG stand ihr eigener Altfund noch da"
    kind: fehler
    match: "doku-pfade-pruefen|HANDBUECHER|mupi-lautstaerke.sh|BACKLOG.md|MEHRDEUTIG|Umzugs-Schuld"
    sev: mittel
    stand: "25.08.2026 erweitert: die Wache liest jetzt fuenf Dateien; BACKLOG.md um vier Pfade berichtigt, drei Nennungen begruendet ausgenommen"
    tags: [doku, wachen, backlog, pfade, remote-step-installer, 2026]
    source: "Stuendlicher Doku-Lauf. Beide Proben meldeten sauber; gefunden ueber die Muster-Methode, diesmal auf die AUSSCHLUSSLISTE einer Wache angesetzt statt auf die Handbuecher."
    body: |
      `tools/doku-pfade-pruefen.py` prueft, ob die Pfade, auf die die Doku
      zeigt, im Baum liegen - und ob sie auf den RICHTIGEN Ordner zeigen. Ihre
      Quellen waren `llmwiki/pack.yaml` und eine Liste namens `HANDBUECHER`
      mit drei Eintraegen: `README.md`, `plugins/README.md`,
      `dokumentation/mixpibox.md`.

      `BACKLOG.md` war nie dabei. Es ist mit rund 8000 Zeilen die groesste
      Prosa-Ablage des Baums und die einzige, in der Vorschlaege, Messungen und
      Ausrollwege nebeneinanderstehen.

      ══ WAS DORT UEBERLEBT HAT ══════════════════════════════════════════════
      `scripts/mupi-lautstaerke.sh`. Richtig ist `scripts/mupibox/…`. Genau
      dieser Pfad steht im Wiki-Eintrag `doku-zeigt-auf-dateien-die-es-nicht-gibt`
      als toter Verweis - die Korrektur von damals lief ueber die drei
      Handbuecher und ging an der vierten Ablage vorbei. **Dieselbe Wache,
      dieselbe Datei, andere Ablage, ewig gruen.**

      Dazu vier MEHRDEUTIGE, alle aus demselben Abschnitt (I9-I11): die vier
      Dateien `qr.py`, `einrichtung-ap.py`, `einrichtung-schirm.py` und
      `kleiner-dhcp.py` standen dort mit `tools/`-Vorsatz und liegen samt und
      sonders unter `remote-step-installer/tools/`.
      Der Absatz nennt das Repo im ersten Satz,
      danach stehen die Pfade kurz - fuer den, der DEN SATZ liest, eindeutig,
      fuer den, der den Namen greppt, ein Fund von null Treffern. Das ist die
      Umzugs-Schuld, gegen die diese Wache gebaut wurde; sie stand in der
      einzigen Datei, die sie nicht las.

      ══ DIE REGEL ═══════════════════════════════════════════════════════════
      **Eine Wache heisst nach dem, was ihr Autor im Kopf hatte, und prueft
      auch nur das.** `HANDBUECHER` klingt vollstaendig. Die Frage beim Lesen
      einer Wache ist nicht "was findet sie", sondern "welche Ablage kommt in
      ihrer Quellenliste NICHT vor" - dort meldet sie nicht falsch, sie meldet
      gar nicht. Dieselbe Form wie
      `zahlenwache-kannte-nur-eine-datei` (eine Wache, die nach einer Datei
      heisst) und wie der Befund um `dokumentation/benutzerhandbuch.html`, das
      lange in keiner Probe stand.

      Die guenstige Haelfte gehoert dazu: `benutzerhandbuch.html` wurde beim
      selben Zug eingehaengt und meldete **null** tote Pfade. Eine Ablage
      einzuhaengen kostet wenig, auch wenn sie sauber ist - erst dann weiss
      man, dass sie sauber IST.

      ══ WAS DIE ERWEITERUNG KOSTET, UND WARUM SIE TROTZDEM ENG BLEIBT ═══════
      Nach dem Einhaengen meldete BACKLOG.md drei weitere tote Pfade, die zu
      Recht dastehen: `src/backend-api/src/.server-leck-probe.16211.ts` (E71/A1
      beschreibt das ENTFERNEN dieser Datei; ihre Abwesenheit ist das Ergebnis)
      und `tools/werkzeug-inventur.py` + `tools/archiv` (ein Vorschlag von
      E71/A3). Sie stehen als `AUSNAHMEN_JE_QUELLE["BACKLOG.md"]` drin - **je
      Pfad, nicht je Datei**. Ein Freibrief fuer die groesste Quelle der Wache
      waere ein Loch, das mit ihr mitwaechst; ein erfundener Pfad an anderer
      Stelle im BACKLOG meldet weiterhin rot.

      Gegengeprobt: je eine erfundene Zeile in `BACKLOG.md` und in
      `dokumentation/benutzerhandbuch.html`, beide gemeldet, Rueckgabe 1,
      danach aus der Sicherung zurueckgeholt.

  - id: vorschlag-wird-still-erfuellt-unter-anderem-namen
    title: "E71/A3 verlangte tools/werkzeug-inventur.py - gebaut wurde es als ungerufene-wachen.py, der Punkt merkte es nicht"
    kind: muster
    match: "werkzeug-inventur|ungerufene-wachen|216 Werkzeuge|ohne Empfaenger|ohne Empfänger|A3"
    sev: mittel
    stand: "25.08.2026 im BACKLOG als Nachtrag festgeschrieben, mit neu gemessenen Zahlen"
    tags: [backlog, wachen, doku, muster, 2026]
    source: "Stuendlicher Doku-Lauf, gefunden im selben Zug wie eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen - die drei toten Pfade des BACKLOG waren der Anlass, den Punkt zu lesen."
    body: |
      E71/A3 ("216 Werkzeuge ohne Empfaenger", 20.08.2026) fuehrte vier
      Schritte. Schritt 1: `tools/werkzeug-inventur.py` schreiben - die Messung
      selbst als Werkzeug. Schritt 2: was noch misst, an `pruefen.sh`.

      Beides ist seit dem 25.08.2026 erledigt, unter dem Namen
      `tools/ungerufene-wachen.py`. Der Punkt stand trotzdem unveraendert unter
      der Ueberschrift "Offen - die uebrigen Punkte, ausgeschrieben".

      ══ WARUM ER NICHT MITGEZOGEN WURDE ═════════════════════════════════════
      Aus demselben Grund wie in `begruendung-liest-sich-als-stand`: **ein
      Vorschlag wird nicht falsch, wenn man ihn umsetzt - er wird still
      erfuellt.** Und er nennt den Namen, den er sich gedacht hat, nicht den,
      der entsteht. Wer nach "A3" oder nach "Werkzeuge ohne Empfaenger" greppt,
      findet den Punkt; wer nach `ungerufene-wachen.py` greppt, findet ihn
      nicht. Die Bruecke fehlt in beide Richtungen.

      ══ DER TEURE NEBENBEFUND: EINE AUSNAHMELISTE IST TEXT IM BAUM ══════════
      Die Ausnahme fuer `tools/werkzeug-inventur.py` in `doku-pfade-pruefen.py`
      liess die Schlusszahl von `ungerufene-wachen.py` von 130 auf 131
      steigen - **ohne dass ein Werkzeug dazugekommen waere**. Ihr `erreicht`
      sammelt NAMEN aus dem Text der Laeufer, nicht Dateien; wer einen
      Werkzeugpfad nur ERWAEHNT, hat ihn fuer die Zaehlung gerufen. Beim
      Nachsehen waren es nicht eins, sondern **sechs** Phantome, die seit jeher
      mitzaehlten: `index.mjs`, `mupibox-standwache.py`,
      `mupibox-touch-bridge.py`, `qr.py`, `wachen-ohne-probe.py`,
      `werkzeug-inventur.py`. Die richtige Zahl ist **125**.

      Fuer das URTEIL war `erreicht` immer richtig (wer genannt wird, ist
      gerufen) - falsch war nur die Zahl daneben. Genau die Form aus
      `audit-liste-ist-keine-dokumentation` und aus dem `grep -v`-Befund:
      **eine Wache meldet die Groesse dessen, was ihr Muster gefangen hat, und
      es liest sich wie ein Deckungsgrad.** Seit dem 25.08. filtert die
      Schlusszeile auf Dateien, die es gibt; gegengeprobt mit einem
      Phantomnamen in einem Laeufer - die Zahl bewegt sich nicht mehr.

      Aufgefallen ist es nicht beim Lesen, sondern weil
      `doku-pfade-pruefen.py` nach dem Einhaengen von `BACKLOG.md` genau die
      beiden erfundenen Pfade meldete: `tools/werkzeug-inventur.py` und
      `tools/archiv`. **Ein Vorschlag, der einen Dateinamen nennt, hinterlaesst
      eine pruefbare Spur** - die Pfadwache findet aus Versehen, was keine
      Backlog-Wache sucht.

      ══ DASS DER NEUE NAME ENGER FRAGT, IST DER EIGENTLICHE INHALT ══════════
      A3 fragte "wer wird nirgends erwaehnt". `ungerufene-wachen.py` fragt
      "wessen AUSGANG haengt von einem Fund ab, und ruft ihn trotzdem
      niemand". Ein Werkzeug, das nur druckt, ist ein Messgeraet und braucht
      keinen Empfaenger. Die engere Frage ist die bessere, aber sie deckt A3
      nicht ganz ab - **Schritt 3 und 4 (`tools/archiv/`, Wegwerfen) stehen
      weiter aus**, und das gehoert in den Punkt geschrieben, sonst liest ihn
      der naechste als komplett offen oder als komplett erledigt.

      ══ UND DIE ZAHLEN ══════════════════════════════════════════════════════
      Am 25.08. nachgemessen: `tools/` hat **519** versionierte Dateien mit
      **173 219** Zeilen (nicht ~80 000), **221** davon werden in `BACKLOG.md`,
      `llmwiki/pack.yaml`, den drei Handbuechern und den beiden Laeufern
      nirgends genannt - 43 %, nicht "ueber die Haelfte". Der Bestand ist
      gewachsen, der ungenannte Anteil gesunken. Eine Ueberschrift, die eine
      Zahl fuehrt, bekommt ihr Datum dazu: siehe `zahlenwache-kannte-nur-eine-datei`.
  - id: router-unter-anderem-namen-ist-fuer-die-zaehlwache-unsichtbar
    title: "sicherung.ts haengt fuenf Wege an einen Router namens r - beide Endpunktwachen suchten app. und router. und meldeten 195 von 195"
    kind: muster
    match: "api-doku-deckung|endpunkt-zitate|api/sicherung|sicherung.ts|195 von 195|Router|app\\.get|Endpunkt.*Wache"
    sev: hoch
    stand: "25.08.2026 beide Wachen verbreitert (199 Routen), die Sicherungsflaeche in mixpibox.md 4.10 aufgeschrieben, tools/kommentar-endpunkte-pruefen.py gebaut und in die Luecken-Probe gehaengt"
    tags: [doku, wachen, api, sicherung, muster, 2026]
    source: "Stuendlicher Doku-Lauf. Beide Proben meldeten sauber; der Einstieg war die Muster-Methode des Vorlaufs (eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen), diesmal auf die QUELLENLISTE einer Wache angesetzt."
    related:
      - eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen
      - wache-sieht-die-schleife-nicht-die-scharf-schaltet
      - audit-liste-ist-keine-dokumentation
      - zahlenwache-kannte-nur-eine-datei
      - begruendung-liest-sich-als-stand
    body: |
      ══ DER BEFUND ══════════════════════════════════════════════════════════
      `tools/api-doku-deckung.sh` sammelt die Routen des Backends mit

          grep -rhoP "(app|router)\.(get|post|put|delete|patch)\(\s*['\"`]\K…"

      und meldete damit seit dem 23.08.2026 "195 von 195 Endpunkten genannt".
      `tools/endpunkt-zitate-pruefen.py` (Gegenrichtung) benutzt dasselbe
      Muster. `src/backend-api/src/sicherung.ts` haengt seine Wege aber an
      einen Router, der schlicht **`r`** heisst:

          r.use('/api/sicherung', …)          Riegel gegen cross-site
          r.get('/api/sicherung')             welche Staende liegen da
          r.post('/api/sicherung/anlegen')    Stand erzeugen + herunterladen
          r.get('/api/sicherung/stand/:name') einen VORHANDENEN Stand holen
          r.post('/api/sicherung/pruefen')    Archiv ansehen (express.raw)
          r.post('/api/sicherung/zurueckspielen')

      Fuenf Routen, unsichtbar fuer beide Wachen. **Vier davon standen in
      keiner der vier Ablagen** (Handbuecher, plugins/README.md, Wissenspaket);
      das Paket nannte nur `/api/sicherung` in einem Nebensatz. Es ist die
      Flaeche, die die GANZE Konfiguration herausgibt und wieder einspielt.

      ══ WARUM DAS DIE TEUERSTE FORM IST ═════════════════════════════════════
      Die Wache meldete nicht "unbekannt", sondern **195 von 195**. Eine Zahl,
      die aufgeht, belegt nicht, dass ihre GRUNDMENGE stimmt - dieselbe Form
      wie in `zahlenwache-kannte-nur-eine-datei` (Rechnung geht auf, Summanden
      falsch) und im `grep -v`-Befund (`wache-sieht-die-schleife-nicht-die-
      scharf-schaltet`). Der Unterschied hier: dort war die Ausnahme
      begruendet aufgeschrieben, hier war sie eine STILLE Annahme im Muster.
      **Eine Wache liest man an dem, was ihr Muster NICHT fangen kann** - und
      die Frage dazu lautet: welcher Empfaenger, welche Schreibweise, welcher
      Ort kommt im Baum vor und nicht im Muster?

      ══ DIE ZWEITE HAELFTE: NICHT JEDE ROUTE PASST IN EINE ZEILE ═════════════
      Nach dem Verbreitern auf jeden Empfaenger fehlte immer noch
      `/api/sicherung/pruefen`: ihr Pfad steht hinter der offenen Klammer auf
      einer EIGENEN Zeile (weil `express.raw(…)` als zweites Argument folgt),
      und beide Wachen lasen zeilenweise. Seither `perl -0777` bzw. ein Lauf
      ueber den ganzen Dateitext. Wer eine Wache verbreitert, prueft danach,
      ob der Fund vollstaendig ist - der erste Schritt fand vier von fuenf.

      ══ DREI FALLEN BEIM VERBREITERN, ALLE GEMESSEN ═════════════════════════
      1. **`[\w$]` ist in Perl keine Zeichenklasse.** Perl setzt `$]` (die
         Perl-Version) ein; das Muster zerfiel still und meldete **21 231
         "ungenannte Endpunkte"**, die in Wahrheit Kommentarzeilen waren. Ein
         Muster, das ploetzlich Tausende findet, ist kaputt und nicht
         fleissig. In der Shell darum `\$` maskieren.
      2. **`printf '%s\n' $var` globbt.** Solange alle Treffer Wortzeichen
         waren, fiel es nie auf; mit einem `/` in der Liste druckte die Wache
         den Inhalt des Arbeitsverzeichnisses als "FEHLT:". Anfuehrungs-
         zeichen setzen, oder ueber `sed` praefixen.
      3. **Anfuehrungszeichen als `\x27\x22\x60` schreiben.** Der erste
         Versuch schrieb sie aus und wurde von der Shell zerlegt - verwandt
         mit `backticks-beenden-jede-vorlage`.
      Der verlangte Schraegstrich am Anfang (`(\/[^…]*)`) haelt `map.get('x')`
      draussen und hat nebenbei `env` aus der alten Liste geworfen: die
      Zaehlung war 195, richtig sind **199**.

      ══ DIE FRAGE, DIE DARAUS FOLGT: WO STEHT DIE DICHTESTE PROSA? ══════════
      `endpunkt-zitate-pruefen.py` fragt "gibt es die Route, auf die die Doku
      zeigt?" und liest drei Handbuecher. In DIESEM Baum stehen aber **156**
      `/api/…`-Zitate in Kommentaren von `.ts`-Dateien - mehr als in den
      Handbuechern - und keines war je gegen den Router gehalten. Seither
      `tools/kommentar-endpunkte-pruefen.py` (in der Luecken-Probe). Erster
      Lauf, drei Funde, alle auf Kernrouten, die mit E77 ins Plugin
      mixpi-ardsounds gewandert sind:

        * `server.ts:8799` begruendete, warum die ARD KEIN eigenes
          Suchformular in der Verwaltung bekommt, mit "`/api/ard/suche` gibt
          es schon" - 700 Zeilen tiefer, bei `/api/ard/sammlungen`, steht in
          derselben Datei aufgeschrieben, dass genau diese Route ersatzlos
          gefallen ist. Zwei Stellen einer Datei, die eine im Praesens ueber
          eine Route, die die andere begraebt. Der Fall aus
          `begruendung-liest-sich-als-stand`, nur mit einem toten BELEG statt
          einer toten Behauptung: **die Entscheidung haelt, ihr Beweis ist
          weg** - und wer sie nachlesen will, greppt den Namen und findet
          nichts.
        * `streaming.ts:173` und `streaming.spec.ts:28` belegen "ARD Sounds
          IST gebaut" mit `/api/ard/suche` und `/api/ard/kinder`.

      ══ WIE DIE WACHE GEBAUT IST, UND WARUM SO ══════════════════════════════
      * **Per Import, nicht per Kopie.** `routen()`, `deckt()` und
        `probestring()` kommen aus der Schwesterwache. Die hat vier Fallen
        hinter sich (Platzhalter beidseitig, Praefix-Zitate, Prosa-Abbruch,
        `app.all(/regex/)` fuer die Plugin-Durchreiche). Wer abschreibt, erbt
        die Blindheit von heute und bekommt die Verbesserungen von morgen
        nicht. Umgekehrt gilt: der `r`-Fund oben hat mit EINER Korrektur beide
        Wachen geheilt.
      * **Nur Kommentare, nicht Code.** Ein `/api/…` in einer Zeichenkette ist
        ein AUFRUF - zeigt der ins Leere, ist das ein Fehler, keine
        Doku-Luecke.
      * **Historisches darf dastehen**, wenn es sich als vergangen ausweist;
        Marker klein geschrieben wie in `doku-widerruf-probe.sh`. Neu ist ein
        Fenster von **zwei Zeilen nach vorn**: ein Kommentar bricht mitten im
        Satz um, und der Marker landet dann unter dem Zitat. Nicht der ganze
        Block - ein Aufsatz von zwanzig Zeilen wuerde sonst jedes Zitat darin
        weisswaschen.
      * **Namensraum ohne Schlussstrich** gilt als erfuellt, wenn eine Route
        damit beginnt ("fuer COVER ist das laengst gebaut (`/api/bild`)").
        Der Preis ist benannt: faellt ein Elternweg und bleiben die Kinder,
        faellt es hier nicht auf - dafuer zaehlt `api-doku-deckung.sh` von der
        anderen Seite.

      ══ WAS BEWUSST DRAUSSEN BLEIBT, DAMIT ES NIEMAND NOCHMAL MISST ══════════
      `BACKLOG.md` gehoert NICHT in die Quellenliste von
      `endpunkt-zitate-pruefen.py`, obwohl es 40 verschiedene Endpunkte
      zitiert. Gemessen: 10 Meldungen, **null davon eine Luecke**. Die Datei
      ist ein PROTOKOLL - ihre Tabellen halten fest, was an einem Tag gebaut
      wurde ("E38/S7 … `GET /api/ard/kategorien` | FERTIG"); dass die Route
      spaeter ins Plugin gewandert ist, macht die Zeile nicht falsch. Dazu
      bewusste Nicht-Beispiele (`/api/quatsch` als 404-Gegenprobe) und
      Vorschlaege. Das steht seit dem 25.08. im Kopf der Wache, damit der
      naechste Lauf es nicht ein zweites Mal misst - der Unterschied zu
      `doku-pfade-pruefen.py`, wo `BACKLOG.md` einen Tag vorher zu Recht
      DAZUkam: dort geht es um Pfade, die es geben MUSS.

      ══ NEBENBEFUND: EINE ASCII-KLASSE BRICHT AM UMLAUT ═════════════════════
      `ZITAT` in `endpunkt-zitate-pruefen.py` las `[A-Za-z0-9/:_<>{}.-]`. Im
      Baum steht `<schlüssel>`, das Zitat brach zu `/api/werke/<schl` ab, und
      die Wache meldete zwei richtige BACKLOG-Zeilen rot. Jetzt `\w` mit
      `re.UNICODE`. In einem deutschsprachigen Baum ist eine ASCII-Klasse
      immer eine Annahme, nie eine Beschreibung.

  - id: felder-liste-der-verwaltung-ist-nicht-die-konfigurationsvorlage
    title: "FELDER deckt nur, was eine Maske hat - 15 von 95 Schluesseln der ausgelieferten mupiboxconfig standen nirgends"
    kind: muster
    match: "mupiboxconfig|konfig-vorlage-deckung|shim.triggerPin|shim.cutPin|shim.ledPin|fan_gpio|wled.com_port|battery_types|FELDER|Stellschraube ohne Maske"
    sev: mittel
    stand: "25.08.2026 gemessen, tools/konfig-vorlage-deckung.py gebaut und in tools/doku-luecken-probe.sh gehaengt, Abschnitt 4.12 in dokumentation/mixpibox.md geschrieben - die Wache meldet seither gruen (95 Schluessel)"
    tags: [doku, wachen, konfiguration, gpio, wled, muster, 2026]
    source: "Stuendlicher Doku-Lauf. Die Luecken-Probe meldete nur die bekannte SEKTIONEN-Luecke; der Fund kam aus der Frage, welche Flaeche gar keine Wache hat."
    related:
      - eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen
      - volles-wiki-ist-kein-beleg-fuer-gepflegte-doku
      - zahlenwache-kannte-nur-eine-datei
      - gegenprobe-statt-gruen-glauben
    body: |
      ══ DAS MUSTER ═════════════════════════════════════════════════════════
      Seit dem 23.08.2026 haelt `tools/doku-luecken-probe.sh` die Liste
      `FELDER` aus `src/backend-api/src/konfiguration.ts` gegen Doku und
      Wissenspaket und meldet dort gruen. `FELDER` ist die Liste dessen, was
      die VERWALTUNG als Feld anbietet. Die Datei, die auf der Box liegt, ist
      `config/templates/mupiboxconfig.json` — 95 Blattschluessel, und `FELDER`
      ist nur der Teil davon mit Maske.

      Gemessen standen 15 Schluessel in KEINEM der vier Handbuecher
      (mixpibox.md, benutzerhandbuch.html, plugins/README.md, README.md) und
      mit null Treffern im Paket. Es sind genau die, die keine Oberflaeche
      haben — also die, fuer die die Datei die einzige Bedienung ist. Wer
      `startVolume` sucht, findet den Schieber und braucht kein Handbuch; wer
      den Taster auf eine andere Leitung legen will, hat nur die Datei.

      ══ DIE TEUERSTEN VIER: DIE GPIO-LEITUNGEN ══════════════════════════════
      `shim.triggerPin` (17, der weiche Ausschalter), `shim.cutPin` (27, der
      Stromschnitt beim Abschalten), `shim.ledPin` (13, das Licht im Knopf)
      und `fan.fan_gpio` (12, der Luefter).

      DIE NUMMERN STANDEN SEHR WOHL IN DER DOKU — Abschnitt 4.1 nennt „GPIO17"
      und „GPIO13" seit langem. Nur als feste Tatsache, ohne den Hebel, der sie
      aendert. Das ist die Klasse Luecke, die jede NAMENSpruefung durchwinkt:
      die Zahl steht da, die Aussage ist richtig, und der Leser haelt die
      Verdrahtung fuer geloetet.

      NEBENBEFUND, im Code stehengeblieben: `scripts/fan/fan_control.py:44`
      kommentiert die Luefterleitung mit „Initialize GPIO13 as our fan output
      pin". 13 ist der KNOPF; der Luefter steht auf 12. Der Kommentar ist alt,
      der Code liest den Schluessel richtig.

      ══ DIE ANDEREN ELF ═════════════════════════════════════════════════════
      Sechs davon sind die WLED-Kette hinter dem Schalter (`wled.active` hat
      eine Maske, alles danach nicht): `com_port`, `baud_rate`, `main_id`,
      `shutdown_id`, `shutdown_active`, `brightness_default`/`_dimmed`. Dazu
      `mupibox.ip_control_backend` (traegt die eigene IP in die
      node-sonos-http-api, bei `false` wird das Feld dort GELEERT),
      `spotify.cachepath` (`LIBRESPOT_CACHE`), `chromium.cachepath` und
      `mupihat.battery_types` (die gemessenen Spannungskennlinien je Akkutyp).

      ══ WARUM KEINE ZWEITE LISTE, SONDERN EINE ZWEITE WACHE ═════════════════
      Die naheliegende Reparatur — `FELDER` um die 15 erweitern — ist falsch.
      Die beiden Listen sind absichtlich verschieden: `FELDER` waechst mit der
      VERWALTUNG, die Vorlage mit der BOX. `wled.com_port` und
      `mupihat.battery_types` gehoeren in keine Maske. Eine Wache, die beides
      in einen Topf wirft, meldet auf Dauer rot fuer Felder, die zu Recht
      keine Maske haben, und wird nach der zweiten Woche ignoriert.

      ══ WAS DIE WACHE MACHT ═════════════════════════════════════════════════
      `tools/konfig-vorlage-deckung.py`, laeuft in der Luecken-Probe mit.

      * BLAETTER, NICHT KNOTEN: `mupihat.battery_types` ist eine Liste von
        Objekten und zaehlt als EIN Schluessel. Die Schwellen darin (`v_75`,
        `th_shutdown`) sind Daten, keine Stellschrauben — wer sie einzeln
        einfordert, verlangt ein Handbuch ueber Akkukennlinien.
      * VOLLER SCHLUESSEL ODER LETZTES GLIED: die Handbuecher schreiben mal
        `mupibox.startVolume`, mal nur `startVolume`. Eine Wache auf den
        vollen Pfad meldete 60 Luecken, von denen 55 keine waren — dieselbe
        Falle, in die die FELDER-Pruefung schon getreten ist (dort „22
        Luecken, davon 16 keine").
      * DER FINGERZEIG sucht ERST den vollen Schluessel, dann das letzte
        Glied: `cachepath` gibt es zweimal (`spotify.`, `chromium.`), und die
        Suche auf das blosse Glied zeigte fuer beide auf dieselbe Zeile. Ein
        Fingerzeig in die falsche Richtung ist schlechter als keiner.
      * GEGENPROBE gefahren, fuenf Wege: Vorlage weg, Vorlage leer, Vorlage
        kein JSON, neuer Schluessel in der Vorlage, Name aus der Doku
        entfernt. Alle fuenf melden rot. Eine Wache, die das Verschwinden
        ihrer Quelle ueberlebt, ist keine.

      ══ DIE FRAGE, DIE ZUM FUND FUEHRTE ═════════════════════════════════════
      Die Luecken-Probe meldete an diesem Tag nur die bekannte
      SEKTIONEN-Luecke. Der Einstieg war nicht ihr Ausgabetext, sondern die
      Gegenfrage: welche FLAECHE des Baums haengt in gar keiner Wache? Die
      Antwort war eine Datei, die eine benachbarte Wache laengst liest — nur
      von der anderen Seite. Dasselbe Vorgehen wie bei
      `router-unter-anderem-namen-ist-fuer-die-zaehlwache-unsichtbar`.

  - id: sicherheitsannahme-mit-datum-ist-keine-wache
    title: "Der Herkunftsriegel berief sich auf ein Nachgesehen vom 07.08. - seit dem 22.08. gab es das ws wieder"
    kind: muster
    match: "herkunft.ts|Herkunftsriegel|herkunft-annahme-probe|websocket am riegel vorbei|text/event-stream|WebSocketServer|Soloist ws.addr|CORS gehorcht nicht|annahme mit datum"
    sev: mittel
    stand: "25.08.2026 gemessen, tools/herkunft-annahme-probe.py gebaut und in tools/doku-luecken-probe.sh gehaengt, Punkt 3 in herkunft.ts und Abschnitt 4.10 in dokumentation/mixpibox.md berichtigt - die Wache meldet seither gruen"
    tags: [doku, wachen, sicherheit, herkunft, websocket, muster, 2026]
    source: "Stuendlicher Doku-Lauf. Die Luecken-Probe meldete nur die bekannte SEKTIONEN-Luecke; der Fund kam aus der Frage, welcher Satz eine MESSUNG behauptet, ohne dass sie jemand wiederholt."
    related:
      - wiki-zustand-hat-haltbarkeit
      - gegenprobe-statt-gruen-glauben
      - felder-liste-der-verwaltung-ist-nicht-die-konfigurationsvorlage
      - herkunftsriegel-und-der-namenstausch
    body: |
      ══ DAS MUSTER ═════════════════════════════════════════════════════════
      Ein Riegel, der aufzaehlt, was er NICHT kann, ist gut. Aber unter diesen
      Punkten steht manchmal keine Grenze, sondern eine MESSUNG mit Datum —
      und die verfaellt, waehrend der Satz stehen bleibt.

      `src/backend-api/src/herkunft.ts` fuehrte unter „WAS DIESER RIEGEL NICHT
      KANN" als Punkt 3:

        „Websockets und Eventstreams gehorchen CORS nicht — die Box fuehrt
         keine (am 07.08.2026 in server.ts nachgesehen: kein `ws`, kein
         `text/event-stream`, kein `upgrade`)."

      Am 22.08.2026 kam mit E75 ein `ws` dazu: `server.ts` haengt sich an
      Soloists Draht (`ws://` aus `/var/lib/soloist/ws.{addr,port}`). Am
      25.08. stand die Nachsehen-Zeile vom 07.08. woertlich noch da. 18 Tage
      lang berief sich ein Sicherheitskommentar auf eine Messung, die nicht
      mehr stimmte.

      ══ WARUM DER RIEGEL TROTZDEM HAELT ════════════════════════════════════
      UND WARUM DAS DER GRUND FUER EINE WACHE IST, NICHT FUER EINE KORREKTUR.
      Der Soloist-Draht geht HINAUS. Ein Client, den die Box selbst oeffnet,
      hat keine fremde Herkunft — niemand von draussen kann ihn ausloesen.
      Gefaehrlich waere der umgekehrte Fall: ein Draht, den die Box ANBIETET.
      Der ginge am Riegel vorbei, und die einzige Stelle, die davor warnte,
      war ein Kommentar mit abgelaufenem Datum.

      Die Richtung ist also der ganze Unterschied — und genau die Unterschei-
      dung, die eine blosse Wortsuche nach `WebSocket` nicht trifft. Eine
      Wache, die jedes `WebSocket` ruegt, waere ab dem 22.08. ewig rot; eine,
      die alle durchwinkt, haette den Umbau genauso verschlafen wie der Satz.

      ══ WAS DIE WACHE MACHT ═════════════════════════════════════════════════
      `tools/herkunft-annahme-probe.py`, laeuft in der Luecken-Probe mit.

      * ANGEBOTEN = ROT: `WebSocketServer`, `ws.Server`, ein `upgrade`-
        Horcher, `express-ws`, `socket.io`, `text/event-stream` in den
        Quellen des Backends.
      * AUSGEHEND = VORLEGEN, NICHT VERBIETEN: jeder `new WebSocket(` muss in
        der Liste `AUSGEHEND` mit Grund stehen. Ein neuer faellt auf, ohne
        dass jemand ihn untersagt — wer ihn eintraegt, hat ihn angesehen.
      * DIE ANDERE RICHTUNG: ein AUSGEHEND-Eintrag ohne Draht im Baum wird
        ebenfalls rot, sonst wuechse dort still eine Liste toter Namen.
      * NUR GEGEN QUELLEN: `src/deploy/server.js` ist ein Bau-Ergebnis (in
        `.gitignore`) und buendelt Fremdbibliotheken mit. Dort nach
        `WebSocketServer` zu suchen hiesse, jede Abhaengigkeit zu ruegen, die
        je einen anbietet.
      * SELBSTPRUEFUNG: findet die Wache `herkunft.ts` nicht oder darin den
        Annahme-Absatz nicht, meldet sie FEHLER statt gruen. Sonst waere sie
        ab der ersten Umbenennung eine Nullnummer — und zwar eine, die ueber
        Sicherheit gruen meldet.
      * GEGENPROBE gefahren, drei Wege: ein `WebSocketServer` in eine Quelle
        gelegt (rot: ANGEBOTEN), ein `new WebSocket(` in eine Datei ohne
        Eintrag (rot: NEUER DRAHT), den Annahme-Satz umbenannt (rot: FEHLER).
        Alle drei melden rot, der Baum war danach wieder sauber.

      ══ DIE FRAGE, DIE ZUM FUND FUEHRTE ═════════════════════════════════════
      Nicht der Ausgabetext der Probe — die meldete nur die bekannte
      SEKTIONEN-Luecke. Sondern: WELCHER SATZ IM BAUM BEHAUPTET EINE MESSUNG,
      OHNE DASS SIE JEMAND WIEDERHOLT? Ein „am <Datum> nachgesehen" ist eine
      Zustandsaussage und hat Haltbarkeit wie jede andere
      ([[wiki-zustand-hat-haltbarkeit]]) — nur faellt sie hier ueber einem
      Riegel an, wo das Ablaufen niemandem auffaellt, weil nichts kaputtgeht.

      NEBENBEFUND, ein verworfener Verdacht — hier, damit ihn niemand ein
      zweites Mal verfolgt: die Schalter der ausgelieferten Skripte
      (`mupibox-sicherung.py --trotzdem`, `mupibox-altlasten.py --wirklich`)
      stehen in KEINEM Handbuch, und drei Skripte fuehren einen handgepflegten
      AUFRUF-Block, der sie auch nicht nennt. Das sieht nach Luecke aus und ist
      keine: `argparse` baut die Optionsliste selbst, `--help` zeigt jeden
      Schalter samt Hilfetext. Gemessen, nicht vermutet — der AUFRUF-Block ist
      Zugabe, nicht die Quelle. Eine Wache darauf waere Arbeit an einer
      Flaeche, die sich selbst dokumentiert.

  - id: ausnahmeliste-gilt-je-datei-nicht-je-name
    title: "Das Handbuch nahm MUPI_URL als hausintern aus - der Laeufer liest es an vier Stellen von aussen"
    kind: muster
    match: "MUPI_URL|MUPI_HOST|MUPI_BOX|MUPIBOX_BOX|MUPI_BASIS|box-adresse-deckung|welche box faesst das werkzeug an|ausnahmeliste im handbuch|hausinterne variable|umgebungsvariablen-deckung laesst tools draussen"
    sev: mittel
    stand: "25.08.2026 gemessen, tools/box-adresse-deckung.py gebaut und in tools/doku-luecken-probe.sh gehaengt, Abschnitt 7.8 in dokumentation/mixpibox.md berichtigt, Abschnitt 7.9 neu und um eine Adress-Tabelle erweitert, Punkt 3 im Eintrag umgebung-ist-vertragsflaeche mit Nachtrag versehen - die Wache meldet seither gruen"
    tags: [doku, wachen, umgebung, werkzeuge, muster, 2026]
    body: >
      ══ DER BEFUND ══════════════════════════════════════════════════════════
      `dokumentation/mixpibox.md` Abschnitt 7 fuehrte einen Kasten „Zwei Namen,
      die hier absichtlich fehlen" und sagte ueber `MUPI_URL` und
      `MUPI_LOCAL_SRC`: „das Skript setzt sie sich selbst. Wer sie von aussen
      setzt, sieht seinen Wert ueberschrieben."

      Fuer `MUPI_LOCAL_SRC` stimmt das (`make-boot-sd.sh` exportiert es in
      Z. 126 fuer sein Kind und liest es in Z. 128). Fuer `MUPI_URL` stimmt es
      an der BELEGTEN Stelle - `chromium-autostart.sh:152` weist ohne Bedingung
      zu - und ist an der Stelle falsch, die ein Entwickler tippt:
      `tools/pruefen.sh` liest `${MUPI_URL:-https://mupibox:8443}` an VIER
      Stellen von aussen (Z. 971, 978, 986, 1002) und leitet sich `MUPI_HOST`
      daraus ab. Damit war der EINZIGE dokumentierte Name, mit dem man den
      ganzen Prueflauf auf eine andere Box richten kann, als „bringt nichts"
      dokumentiert.

      ══ DIE LEHRE ═══════════════════════════════════════════════════════════
      EINE AUSNAHME GILT JE DATEI, NICHT JE NAME. „Das Skript setzt sie sich
      selbst" ist eine Aussage ueber EIN Skript; als Eigenschaft des NAMENS
      gelesen deckt sie jede andere Datei mit, die denselben Namen liest. Und
      eine AUFZAEHLUNG leiht dem falschen Eintrag die Glaubwuerdigkeit des
      richtigen Nachbarn - dieselbe Form wie in
      [[begruendung-liest-sich-als-stand]], nur in einer Ausnahmeliste statt in
      einer Begruendung. Eine Ausnahmeliste ist Text im Baum und gehoert gegen
      den Baum gehalten, genau wie die Liste, die sie ausnimmt.

      ══ DIE ZWEITE HAELFTE: VIER NAMEN FUER EINE SACHE ══════════════════════
      Gemessen richten FUENF Namen ein Werkzeug auf eine Adresse, mit
      unvereinbaren Vorgaben:
      `MUPI_URL` -> https://mupibox:8443 (tools/pruefen.sh, 4 Stellen),
      `MUPI_HOST` -> mupibox (aufraeumen.sh, mitmessen.mjs),
      `MUPI_BOX` -> 192.168.178.169 (anlauf-am-geraet.mjs,
      verwaltung-systemkurve-schau.mjs),
      `MUPIBOX_BOX` -> 192.168.178.169:8200 (vorlesen-am-geraet.mjs),
      `MUPI_BASIS` -> http://127.0.0.1:8200 (mupi-check.py).
      KEINER stand in einem Handbuch. Der teure Fall ist nicht der Absturz,
      sondern die stille Fehlmessung: wer `MUPI_HOST` setzt und danach
      `anlauf-am-geraet.mjs` startet, misst `192.168.178.169` ohne Hinweis - auf
      einem fremden Netz ein anderes Geraet. Eine falsche BESTAETIGUNG wiegt
      schwerer als eine falsche Widerlegung, weil sie eine Frage schliesst
      ([[gegenprobe-statt-gruen-glauben]]). Rund 108 Werkzeuge tragen eine
      Box-Adresse ganz ohne Umgebungsausweg fest im Quelltext; das ist der
      groessere Rest und steht offen.

      ══ WIE MAN DARAUF KOMMT ════════════════════════════════════════════════
      Nicht aus dem Ausgabetext der Proben - beide meldeten sauber, und es gab
      seit dem Vorlauf keinen neuen Commit. Sondern mit der Muster-Methode auf
      die AUSSCHLUSSLISTE einer Wache angesetzt, dieselbe Frage wie in
      [[wache-sieht-die-schleife-nicht-die-scharf-schaltet]]: WAS KOSTET DAS
      `grep -v` DIESER WACHE? `tools/umgebungsvariablen-deckung.py` laesst
      `tools/` VOLLSTAENDIG draussen, begruendet mit „Attrappenschalter wie
      `MUPI_STUB_AUS` steuern eine Vorrichtung, nicht die Box". Nachgemessen
      liest `tools/` 31 Namen mit Praefix. Fuer den grossen Teil stimmt die
      Begruendung - `MUPI_STROMAUS` in `update-bestand-probe.py` ist wirklich
      eine Stromausfall-Falle im Sandkasten, und dieser Verdacht ist damit
      verworfen. Fuer EINE Klasse stimmt sie genau nicht: die Namen, die sagen,
      WELCHE ADRESSE angefasst wird. Die steuern keine Vorrichtung, sie richten
      das Werkzeug auf ein echtes Geraet. Eine Wache liest man an ihren
      Ausschluessen, nicht an ihren Treffern - und prueft die ausgeschlossene
      Klasse einzeln nach, statt der Begruendung zu glauben.

      ══ DIE WACHE ═══════════════════════════════════════════════════════════
      `tools/box-adresse-deckung.py`, in `tools/doku-luecken-probe.sh`.

      * RICHTUNG 1: jeder Name, dessen VORGABE AN DER LESESTELLE eine Adresse
        ist (IP, `mupibox`, `localhost`, `host:port`, `http(s)://`), steht in
        Backticks in einem Handbuch. Die Liste kommt aus dem WERT, nicht aus
        dem Namen - `MUPI_HORT` klingt nach Adresse und ist ein Verzeichnis,
        `MUPIBOX_BOX` klingt nach Schalter und ist eine Adresse
        ([[namenssuche-nach-wachen-laesst-die-geschwister-stehen]]).
      * RICHTUNG 2, der eigentliche Anlass: jeder Name, den ein Handbuch als
        hausintern ausnimmt, wird nirgends von AUSSEN gelesen. Eine Datei, die
        den Namen selbst setzt, ehe sie ihn liest, zaehlt nicht - sonst meldet
        die Wache den RICHTIGEN Teil der Ausnahme als Fehler und macht sich
        unglaubwuerdig.
      * `BACKLOG.md` steht ABSICHTLICH NICHT in der Quellenliste, und das ist
        hier messbar richtig: `MUPI_HOST` steht dort genau einmal (Z. 1727) und
        meint etwas ANDERES - einen Schluessel in
        `remote-step-installer/boxen.yaml`. Mit `BACKLOG.md` in der Liste galt
        der wichtigste Name als dokumentiert. Ein Namenstreffer taugt als
        Alibi.
      * HINWEIS statt Zaehlung fuer „mehrere Namen, eine Sache": das zu
        vereinheitlichen ist eine Aenderung am CODE, keine an der Doku. Sonst
        meldet die Wache ewig rot und wird zu Recht ignoriert.
      * SELBSTPRUEFUNG dreifach: kein Umgebungszugriff, keine Adress-Schraube
        oder kein Ausnahme-Kasten gefunden -> FEHLER statt gruen.

      ══ VIER FALLEN BEIM BAU, ALLE TEUER GEMESSEN ═══════════════════════════
      1. `MUPIBOX?_[A-Z0-9_]+` als Praefix gemeint als „MUPI_ oder MUPIBOX_" -
         Python liest „MUPIBO_ oder MUPIBOX_", und `MUPI_BOX`, `MUPI_HOST`,
         `MUPI_URL` fielen lautlos heraus. Uebrig blieb EIN Treffer, und der
         sah wie ein vollstaendiger Befund aus. Ein Muster, das ploetzlich fast
         nichts findet, ist kaputt und nicht sauber - richtig ist
         `MUPI(?:BOX)?_`.
      2. DIE WACHE BELEGTE IHREN BEFUND MIT SICH SELBST: ihr eigener
         Kopfkommentar ZITIERT `${MUPI_URL:-...}` als Beleg und wurde als
         Zugriff gezaehlt. Ein Name im Fliesstext ist ein Verweis, kein
         Zugriff - die eigene Datei gehoert uebergangen.
      3. DIE KORREKTUR ZITIERT DEN WIDERRUFENEN SATZ, um zu erklaeren, was
         falsch war - und die Wache fiel darueber. Loesung wie bei der
         Widerruf-Probe: ein Absatz, der sich selbst als historisch ausweist
         (`ueberholt`, `NACHTRAG`, `stand hier bis`, ...), ist ein Rueckblick,
         kein Anspruch. Das MARKER-Vokabular aus
         `tools/doku-widerruf-probe.sh` uebernommen, keine zweite Liste - zwei
         Listen laufen auseinander.
      4. Ein Kasten ist ein ABSATZ und trifft das Satzmuster mehrfach ("setzt
         sie sich selbst" und "von aussen setzt" stehen in Folgezeilen) -
         derselbe Befund stand zweimal da und die Schlusszahl war doppelt so
         gross wie die Sache.

      ══ GEGENGEPRUEFT ═══════════════════════════════════════════════════════
      Fuenf Wege, jeder mit Sicherung im selben Befehlsblock und byteweiser
      Wiederherstellung ([[gegenprobe-statt-gruen-glauben]]): einen
      Namen dokumentiert -> Fund verschwindet; den Ausnahme-Kasten entfernt ->
      FEHLER statt gruen; beide Vorgaben von `MUPI_BOX` auf einen Nicht-Ort
      gesetzt -> Wertsuche verliert ihn; eine LEBENDE falsche Ausnahme ohne
      Marker eingesetzt -> rot; die einzige Nennung von `MUPI_BASIS` umbenannt
      -> rot. Der Baum war danach wieder sauber (nur die Dateien einer
      parallelen Sitzung blieben unangetastet).

      NEBENBEFUND, im selben Zug berichtigt: der Wiki-Eintrag
      [[umgebung-ist-vertragsflaeche]] trug denselben falschen Satz - in
      ANDERER WORTFOLGE ("setzt das Skript sich selbst"), weshalb die Suche
      nach dem SATZ ihn nicht fand und nur die Suche nach dem NAMEN. Wer eine
      Aussage an einer Stelle berichtigt, greppt nach dem NAMEN, nicht nur nach
      dem Satz. Ausserdem zitierte er `chromium-autostart.sh:101` fuer eine
      Zeile, die bei 152 steht (dort steht `CHROMIUM_OPTS`);
      `tools/doku-zeilenzitate-pruefen.py` liest das Pack, prueft aber nur, ob
      die Zeile EXISTIERT, nicht ob dort das Zitierte steht.

  - id: testmenge-in-keinem-laeufer
    title: "367 Plugin-Tests, gruen, 230 Millisekunden - und kein Laeufer rief sie"
    kind: falle
    match: "test:plugins|plugins/\\*/\\*.spec.mjs|npm run test --workspaces &&|ci.yml|github actions|welche tests setzt die ci durch|ng test braucht einen browser|ci-deckung"
    sev: hoch
    stand: "26.08.2026 gemessen, tools/ci-deckung.py gebaut und in tools/doku-luecken-probe.sh gehaengt, Abschnitt 7.5 in dokumentation/mixpibox.md neu (die alten 7.5-7.8 eine Nummer weiter), tools/pruefen.sh ruft die Plugin-Tests seither als eigenen Schritt - die Wache meldet gruen"
    tags: [doku, wachen, tests, ci, plugins, 2026]
    related: [wache-stirbt-still-wenn-sie-nirgends-haengt, gegenprobe-statt-gruen-glauben, ungerufene-wache-driftet-in-den-fehlalarm, testfinder-auf-einen-dateinamen-verengt]
    body: >
      DIESER EINTRAG SETZT VORAUS, DASS DER BEFEHL DAS RICHTIGE PRUEFT -
      er handelt davon, dass ihn niemand ruft. Zaehlt der Lauf dagegen zu
      WENIGE Testdateien, obwohl er gerufen wird, ist es der andere
      Eintrag mit `test:plugins` im `match:`:
      [[testfinder-auf-einen-dateinamen-verengt]]. Die Frage, die
      trennt, ist die Zahl der Zeugen gegen die Zahl der Laeufer.

      ══ DER BEFUND ══════════════════════════════════════════════════════════
      Die 14 Testdateien unter `plugins/*/` - 367 Tests, alle gruen, zusammen
      230 Millisekunden - liefen in KEINEM Laeufer. Drei Wege fuehren an
      ihnen vorbei, und jeder sieht fuer sich richtig aus:

      1. `npm run test` in der Wurzel ist `npm run test --workspaces && npm
         run test:plugins`. Die Faecherung bleibt in `frontend-box` und
         `frontend-admin` bei `ng test` im Beobachtungsmodus stehen (Karma
         endet nie, und ein Chrome ist ohnehin nicht da). Das `&&` wird nie
         erreicht: der zweite Teil ist unerreichbarer Code in einer
         `package.json`.
      2. `tools/pruefen.sh` - der Weg, den das Handbuch als „prueft wirklich"
         fuehrt - ruft `plugin-geruest-probe.sh` (das GERUEST fuer Fremde)
         und `mixpi-plugins-nachziehen-probe.sh`. Beide pruefen etwas mit
         Plugins, keines ruft diese 14 Dateien. Ein Namenstreffer taugt als
         Alibi.
      3. `.github/workflows/ci.yml` ruft `test` nur fuer `backend-api` und
         `backend-player`.

      ══ DIE BAUART ══════════════════════════════════════════════════════════
      Das ist [[wache-stirbt-still-wenn-sie-nirgends-haengt]] eine Ebene hoeher: nicht
      eine WACHE haengt in keinem Laeufer, sondern eine ganze TESTMENGE. Wer
      nach ungerufenen Wachen sucht, zaehlt Werkzeuge in `tools/`; eine
      Testmenge steht woanders und faellt durch dieselbe Suche. Die Frage, die
      beides findet, lautet nicht „welche Dateien gibt es", sondern „WER RUFT
      diesen Einstieg" - und sie ist fuer jeden Testeinstieg des Baums zu
      stellen, nicht nur fuer den, an den man gerade denkt.

      Und der Grund, warum es niemandem auffiel: `npm run test` in der Wurzel
      NENNT `test:plugins`. Wer die `package.json` liest, sieht Deckung. Dass
      ein `&&` hinter einem Befehl steht, der nie zurueckkehrt, sieht man der
      Zeile nicht an.

      ══ DIE ZWEITE HAELFTE: DIE CI STAND IN KEINEM HANDBUCH ═════════════════
      Von den 30 Wachen in `tools/doku-luecken-probe.sh` las keine
      `.github/workflows/ci.yml`, und die Woerter „CI" und „GitHub Actions"
      kamen in KEINEM Handbuch vor. Abschnitt 7.3 zaehlt 197 Testdateien auf
      und liest sich wie eine Deckung; welche davon ein Fremder mit einem PR
      wirklich ausloest, war nirgends nachlesbar. Gemessen: die CI faehrt 122
      der 197. Nicht durchgesetzt sind `ng test` beider Oberflaechen (42 + 19
      Dateien, Browser fehlt - begruendet), `lint` fuer `frontend-admin` (der
      Bereich hat gar kein `lint`, `--workspaces` ueberspringt ihn stumm) und
      eben die Plugin-Tests.

      ══ DIE FALLE IM AUSNAHME-KASTEN ════════════════════════════════════════
      `tools/ci-deckung.py` liest die ABSICHT aus einem Kasten im Handbuch
      statt aus einer Liste im Code ([[sicherheitsannahme-mit-datum-ist-keine-wache]]) - ein
      Einstieg, der dort in Backticks steht, gilt als bewusst nicht
      durchgesetzt. Genau das machte die Wache zunaechst blind: `test:plugins`
      stand im Kasten, also meldete sie gruen, AUCH nachdem der neue Schritt
      aus `pruefen.sh` wieder entfernt wurde. Aufgefallen ist es nur in der
      Gegenprobe. Ein Kasten, der zugleich „das erklaeren wir" und „das
      entschuldigen wir" heisst, entschaerft die Wache, die ihn liest. Der
      Kasten nennt die Plugin-Tests jetzt in Prosa und sagt in einem Satz
      dazu, warum ihr Skriptname dort NICHT stehen darf.

      ══ GEGENGEPRUEFT ═══════════════════════════════════════════════════════
      Vier Wege, jeder mit Sicherung im selben Befehlsblock
      ([[gegenprobe-statt-gruen-glauben]], [[gegenprobe-statt-gruen-glauben]]):
      einen Auftragsnamen aus der Doku genommen -> rot; `npm run docker:build`
      in die CI gehaengt -> rot; der Doku einen Ruf zugeschrieben, den
      `ci.yml` nicht hat -> rot; den Plugin-Schritt aus `pruefen.sh` genommen
      -> rot. Zwei davon bissen im ERSTEN Anlauf NICHT, und beide Male lag es
      an einer zu weiten Suche: die Wache akzeptierte einen Namen aus dem
      GANZEN Handbuch (Abschnitt 7.4 zaehlt die zwanzig Wurzel-Skripte
      ohnehin alle auf, also war jeder CI-Ruf „dokumentiert"), und sie las
      `--workspace=NAME` irgendwo in `ci.yml` als Beleg, dass die TESTS des
      Bereichs laufen - `backend-player` steht dort zweimal, einmal fuer
      `test` und einmal fuer `check-types`. Beides ist eingeengt.

      NEBENBEFUND, nicht behoben: der neue Abschnitt schob die alten 7.5-7.8
      je eine Nummer weiter, und vier `Abschnitt 7.x`-Verweise IN DIESEM PACK
      zeigten danach auf die falsche Stelle (im selben Zug berichtigt). Keine
      Wache prueft das: `tools/doku-zeilenzitate-pruefen.py` fragt, ob eine
      zitierte ZEILE existiert, `tools/doku-pfade-pruefen.py` ob ein PFAD
      existiert - dass eine zitierte ABSCHNITTSNUMMER noch den genannten
      Abschnitt trifft, fragt keine. Eine Einfuegung in der Mitte eines
      Handbuchs entwertet still jeden Verweis darunter.
  - id: der-waechter-der-wachen-erkennt-nur-den-woertlichen-ausgang
    title: "Kritiker-Lauf 26.08.: ungerufene-wachen meldet gruen, weil urteilt() nur exit(1) woertlich kennt - 56 Wachen sind fuer ihren eigenen Waechter unsichtbar"
    kind: muster
    match: "ungerufene-wachen|urteilt()|sys.exit(main())|process.exit ausdruck|waechter der wachen|luecken zaehlt rote schritte|doku-luecken-probe zaehlt falsch|wachen_lib|routen_aus_server|neun regex fassungen server.ts|kritiker 26.08|AUDIT-2026-08-26"
    sev: mittel
    stand: "26.08.2026 Kritiker-Lauf, AUDIT-2026-08-26.md geschrieben; nichts behoben, nur befundet - HEAD 58a7a0e1"
    tags: [wachen, werkzeuge, muster, audit, 2026]
    body: >
      ══ DER BEFUND ══════════════════════════════════════════════════════════
      `tools/ungerufene-wachen.py` (gebaut 25.08. als Antwort auf den
      Audit-Befund "Wachen, die in keinem Laeufer haengen, rosten unbemerkt")
      meldete am 26.08. "Keine ungerufene Wache" - und war fuer genau das
      Revier blind, das es bewachen soll. `urteilt()` (Z. 203-205) erkennt nur
      die WOERTLICHEN Ausgaenge `sys.exit(1)` / `process.exit(1)` / `exit 1`.
      Die Wachen-Familie (107 Dateien, ~15.700 Zeilen) urteilt aber
      ueberwiegend ueber `sys.exit(main())` und `process.exit(x ? 1 : 0)` -
      56 Wachen haengen in keinem Laeufer, stehen in keiner BEKANNT-Ausnahme
      und fallen trotzdem durch das Raster. Das ist die naechste Stufe von
      [[ungerufene-wache-driftet-in-den-fehlalarm]] und
      [[dauerrote-wache-ist-keine]]: auch der WAECHTER DER WACHEN prueft nur,
      was sein Muster kennt. Ein Werkzeug, das eine Luecke VERMISST, hat sie
      nicht geschlossen - die Blindstelle stand sogar im eigenen Kopfkommentar
      ("89 statt 0 Luecken") und blieb einen Tag spaeter unbearbeitet, waehrend
      8 neue Wachen dazukamen.

      ══ ZWEI GESCHWISTER-BEFUNDE AM SELBEN ORGAN ════════════════════════════
      * `tools/doku-luecken-probe.sh` zaehlt ROTE SCHRITTE, nicht Luecken: 37
        Zaehlstellen `luecken=$((luecken + 1))`, jede addiert eins, egal ob das
        Kind eine oder fuenfzehn Luecken meldet. Die Schlusszahl ist eine Zahl
        ueber die Wrapper, nicht ueber den Baum.
      * Die Laeufer-Kind-Schnittstelle ist ein handgepflegtes `grep -E` mit je
        eigener Wortliste in sieben Wrappern - wer im Kind eine Meldezeile
        umformuliert, bekommt einen roten Schritt OHNE TEXT.

      ══ DIE WURZEL: KEIN GETEILTES GERUEST ═══════════════════════════════════
      Neun unabhaengige Regex-Fassungen extrahieren Routen aus server.ts; der
      "Router hiess r"-Fund (fd90fd89) wurde in ZWEI davon repariert, sieben
      kennen `r.` bis heute nicht. `WURZEL = ...parent.parent` steht 104x im
      Werkzeugkasten, die Doku-Dateiliste 18x. Vorschlag im Audit:
      `tools/wachen_lib.py` (repo_wurzel, routen_aus_server - der fertige
      Rumpf liegt in endpunkt-zitate-pruefen.py:110-131 -, dateien,
      HANDBUECHER, Meldehelfer mit maschinenlesbarer Schlusszeile
      `LUECKEN: n`, die der Laeufer LIEST statt +1 zaehlt).

      ══ DIE UEBRIGEN SAEULEN DES LAUFS (Details in AUDIT-2026-08-26.md) ═════
      * Rueckstand: dritter Tag null umgesetzte Audit-Punkte; die 13 Commits
        des Tages sind Doku-Laeufe, 0 Codezeilen.
      * Ecken: 416-MB-Geisterworktree unter .claude/worktrees/ verfaelscht
        jeden Repo-grep; .git ist 621 MB (deploy.zip in 811 Fassungen);
        AdminInterface/ als root-eigener Wiedergaenger zurueck; homeassistant-
        Karte liest _current_playback, die Box sendet _play_text.
      * Querschnitt: fuenf Medien-Datenmodelle ohne Import untereinander;
        dasselbe lokale Album heisst "Lokal", "Von der Box" und "Auf der Box";
        VIER unabhaengige Spotify-Token-Erneuerungen, drei schreiben zurueck -
        ein echter Wettlauf um ein rotierendes Token (spotify-api.service:96,
        server.ts:17014, spotify-control:609, spotify-auth:168).
      * Werkzeuge: von den Empfehlungen des 23.08. ist nur biome umgesetzt;
        vier Skripte tragen shellcheck-Direktiven ohne installiertes Werkzeug.
  - id: dauerrote-wache-ist-keine
    title: "Eine Wache, die seit ihrem ersten Tag rot meldet, ist ein Schild - der vierte, echte Befund erscheint in derselben Zeile"
    kind: lehre
    match: "dauerrote wache|wache dauerhaft rot|1 LUECKE(N) seit tagen|ausnahme gehoert in die README|ausnahmeliste im skript|DOKU VERALTET gegenrichtung|probe taugt nicht als tor"
    sev: mittel
    stand: "26.08.2026 nachgetragen; der Fall (sektionen-deckung.py) ist seit 25.08.2026 abgestellt, doku-luecken-probe.sh geht seither wieder mit 0 heraus"
    tags: [wachen, doku, werkzeuge, lehre, ausnahmen, 2026]
    body: >
      ══ DER FALL, AN DEM ES AUFFIEL ═════════════════════════════════════════
      `tools/sektionen-deckung.py` fand am 23.08.2026 drei Abweichungen im
      Vokabular `SEKTIONEN` (siehe [[geschlossenes-vokabular-ohne-konsumenten]]):
      `ton` und `medien` haengen ins Leere, `streaming/deezer` hat einen Anker
      ohne Wert. ALLE DREI SIND ABSICHT und wurden im SELBEN Commit (76ac10e8)
      in `plugins/README.md` begruendet - `deezer` aufzunehmen naehme
      `plugin-vertrag.spec.ts` sein Gegenbeispiel weg, und fuer Klang-Plugins
      ist `rechte: ["klang"]` der Weg.

      Damit konnte die Wache NIE gruen werden. `tools/doku-luecken-probe.sh`
      meldete zwei Tage lang in JEDEM Lauf „1 LUECKE(N)" und ging mit 1 heraus.
      Die Folge ist nicht Laerm, sondern Blindheit an genau einer Stelle: die
      Probe taugte als Ja/Nein-Tor nicht mehr, und ein VIERTER, echter Befund
      waere in derselben Zeile erschienen wie die drei bekannten.

      ══ DIE LEHRE ═══════════════════════════════════════════════════════════
      EINE DAUERROTE WACHE IST KEINE WACHE, SONDERN EIN SCHILD. Sie erzieht
      dazu, ihre Zeile zu ueberlesen - dieselbe Not wie in
      `halbe-pruefung-meldet-wie-eine-ganze`, nur von der anderen Seite. Wer
      eine Abweichung hinnehmen will, muss sie ABSTELLEN oder AUSNEHMEN; sie
      stehen zu lassen und die Wache rot zu belassen ist die dritte, schlechte
      Moeglichkeit.

      ══ WOHIN DIE AUSNAHME GEHOERT (und wohin nicht) ════════════════════════
      Nicht ins Skript. Seit dem 25.08.2026 fuehrt `sektionen-deckung.py`
      KEINE eigene Liste; es liest die Ausnahmen aus `plugins/README.md` -
      die Marken `**nirgends**` in der Wert->Ort-Tabelle und den Satz
      „Umgekehrt fehlt `…`". Dieselbe Regel wie bei `box-adresse-deckung.py`.
      Der Grund: eine Abweichung muss dort erklaert stehen, WO DER BETROFFENE
      SIE FINDET - der Plugin-Bauer liest die README, nicht das Python.

      ══ UND DIE GEGENRICHTUNG, SONST VERROTTET SIE ══════════════════════════
      Eine Ausnahmeliste, die nur ENTSCHAERFT, verrottet: wer `ton` wirklich
      einhaengt oder `deezer` ins Vokabular aufnimmt und die README nicht
      nachzieht, bekaeme sonst still gruen. Die Wache prueft die Ausnahme
      darum in beide Richtungen und meldet `DOKU VERALTET`, sobald der Grund
      weg ist. Vergleiche [[ausnahmeliste-gilt-je-datei-nicht-je-name]]: eine
      Ausnahmeliste ist Text im Baum und gehoert gegen den Baum gehalten,
      genau wie die Liste, die sie ausnimmt.

      ══ GEGENGEPROBT ════════════════════════════════════════════════════════
      25.08.2026 mit vier Sabotagen, jede einzeln rot: README-Zeile weg,
      „Umgekehrt fehlt" weg, Anker fuer `ton` eingebaut, `streaming/deezer`
      ins Vokabular aufgenommen. Danach `doku-luecken-probe.sh`: KEINE LUECKE
      (Ausgang 0), zum ersten Mal seit dem 23.08.

      ══ WIE MAN DEN NAECHSTEN FALL FINDET ═══════════════════════════════════
      Nicht am Text der Wache, sondern an ihrer GESCHICHTE: eine Wache, deren
      Meldung in mehreren Laeufen hintereinander WORTGLEICH dasteht, ist
      verdaechtig. Die Frage lautet dann nicht „wie behebe ich den Befund?",
      sondern „ist der Befund eine Absicht, die ich als Ausnahme aufschreiben
      muss?". Schwesterfall auf der gruenen Seite:
      [[ungerufene-wache-driftet-in-den-fehlalarm]] - eine Wache, die in
      keinem Laeufer haengt, rostet ebenso unbemerkt.

  - id: skript-das-niemand-ruft-verrottet-lautlos
    kind: falle
    title: "Ein npm-Skript, das kein Laeufer ruft, zeigt ein Jahr lang auf geloeschte Dateien"
    match: "ENOENT.*\\.proto|pbjs|build-proto|npm error code 1.*workspace|Skript bricht ab"
    sev: med
    cause: "ein Skript wird nur beim Aufruf geprueft - ruft es niemand, faellt sein Tod nie auf; Wachen lasen nur die Wurzel-package.json, nicht die der Arbeitsbereiche"
    fix: "tools/npm-skripte-deckung.py prueft seit 26.08.2026 JEDEN Eingabe-Pfad JEDES Skripts in Wurzel UND Arbeitsbereichen gegen die Platte"
    tags: [doku, wache, npm, aufraeumen, backend-player]
    body: |
      ══ DER FUND ═══════════════════════════════════════════════════════════
      26.08.2026, Doku-Lauf. `src/backend-player/package.json` traegt:

        "build-proto": "npx pbjs -t static-module -w commonjs -p proto
                        -o src/spotify-proto.js proto/spotify/login5/v3/login5.proto"

      Der ganze `proto/`-Baum (sieben `.proto`-Dateien) und die daraus
      erzeugte `spotify-proto.js` (5806 Zeilen) sind seit `ce63e126` („Use
      official spotify Playback SDK") weg - geloescht am **26.08.2025**, auf
      den Tag ein Jahr. Das Skript blieb stehen. `npm run build-proto` bricht
      seither mit ENOENT ab.

      Mitgeschleppt: `protobufjs` (^7.4.0) und `protobufjs-cli` (^1.1.3),
      beide unter `dependencies`, NICHT `devDependencies`. Kein Quelltext im
      Baum enthaelt noch das Wort „protobuf" - nachgesehen ueber alle `.ts`,
      `.js`, `.mjs` ausserhalb von node_modules. `npm ls` zeigt keinen
      zweiten Halter, auch keinen transitiven. Gemessen: 3,1 MB + 372 KB =
      **3,5 MB**, die bei jeder Installation auf die Box wandern, ohne dass
      eine Zeile sie importiert.

      ══ WARUM ES NIEMAND FAND ═══════════════════════════════════════════════
      Ein Skript ist nur so wach wie sein Aufrufer. `build-proto` haengt in
      keinem Laeufer, keiner CI-Zeile, keiner Wache - es lief zuletzt, als
      jemand es von Hand tippte. Ein Fehler, den nur der Aufruf zeigt, und
      ein Aufruf, den niemand macht, ergeben zusammen Stille.

      Die zustaendige Wache gab es sogar: `npm-skripte-deckung.py`, geschrieben
      am 25.08.2026 fuer GENAU diese Sorte Leiche (`test:frontend-api` rief
      einen Arbeitsbereich, den es nie gab). Sie las aber **nur die
      Wurzel-package.json**. Die Arbeitsbereiche haben eigene Skripte, die von
      der Wurzel aus niemand faechert - und dort lag die zweite Leiche, eine
      Ebene unter dem Scheinwerfer. Vergleiche
      [[eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen]]: eine
      Wache deckt, was sie LIEST, nicht was sie heisst - und meldet ueberall
      sonst ewig gruen.

      ══ DIE ERWEITERUNG ═════════════════════════════════════════════════════
      Seit 26.08.2026 prueft die Wache zusaetzlich: jeder EINGABE-Pfad jedes
      Skripts - Wurzel UND jeder Arbeitsbereich, jeweils gegen SEIN eigenes
      Verzeichnis aufgeloest - liegt wirklich auf der Platte.

      AUSGENOMMEN sind Ausgabeziele (`-o`, `--outfile=`, `--output-path=`,
      `--metafile=`): die entstehen erst beim Bau, und ohne diese Ausnahme
      meldete die Wache jedes esbuild-Skript falsch rot. Sternchen werden als
      Muster aufgeloest; ein Muster OHNE einen einzigen Treffer zaehlt als
      Loch (sonst uebersteht `src/*.spec.ts` das Verschwinden aller Tests).

      Ein Wort gilt nur dann als Pfad, wenn es einen `/` UND eine bekannte
      Endung hat. Absichtlich eng: `-p proto` (Ordner ohne Endung) faengt die
      Wache NICHT, die Datei dahinter aber schon. Ein weiteres Netz meldet
      jeden Schalter als Pfad - und eine Wache, die schreit, wird weggeschaut
      ([[halbe-pruefung-meldet-wie-eine-ganze]]).

      ══ GEGENGEPROBT ════════════════════════════════════════════════════════
      Fuenf Faelle gegen `eingabe_pfade()` einzeln, alle richtig: Eingabe
      neben `--outfile=` erkannt und Ausgabe verworfen; das Ausgabeziel hinter `-o`
      verworfen (diese Datei gibt es NICHT - ohne die Ausnahme waere
      sie ein Fehlalarm gewesen, die Ausnahme ist also wirklich geprueft und
      nicht nur behauptet); Muster durchgereicht; `ng build --configuration
      production` ergibt leer. Gegen den echten Baum: genau EIN Fund
      (`build-proto`), kein Fehlalarm ueber fuenf Pakete. Findet die Wache gar
      keinen Pfad mehr, meldet sie WARNUNG statt gruen
      ([[gegenprobe-statt-gruen-glauben]]).

      Und die AUSNAHME selbst, drei Sabotagen in einem Wegwerf-Baum (nie am
      echten - eine Probe, deren Erfolg ein Schaden ist, braucht ein
      Wegwerfziel), jede einzeln
      rot: (1) Aufraeum-Abschnitt aus `BACKLOG.md` entfernt -> aus HINWEIS
      wird gezaehlter TOTER PFAD; (2) `proto/`-Datei wieder angelegt, Backlog
      bleibt -> BACKLOG VERALTET; (3) `build-proto` ganz gestrichen, Backlog
      bleibt -> ebenfalls BACKLOG VERALTET. Fall (3) faengt sie nur, weil die
      Gegenrichtung am ERGEBNIS haengt („hat ueberhaupt noch ein Skript einen
      toten Pfad?") und nicht am Namen `build-proto` - eine Namenspruefung
      haette das gestrichene Skript stumm durchgelassen.

      ══ WIE MAN DEN NAECHSTEN FALL FINDET ═══════════════════════════════════
      Die Frage ist nicht „steht der Name da?", sondern „ruft das jemand?".
      Fuer jedes Stueck Automatik im Baum - Skript, Werkzeug, Unit, Timer -
      gilt: wer es zuletzt ausgefuehrt hat und wann, entscheidet, ob sein
      Inhalt noch stimmt. Schwesterfaelle:
      [[doku-zeigt-auf-dateien-die-es-nicht-gibt]] (Namen da, Ziele fehlen) und
      [[ungerufene-wache-driftet-in-den-fehlalarm]] (die Wache selbst haengt
      in keinem Laeufer). Das Streichen ist Arbeit am Bau, nicht an der Doku,
      und steht im `BACKLOG.md`; nachher messen, nicht schaetzen
      ([[aufraeumen-nachmessen]]).

  - id: bauanleitung-ist-eine-vierte-sorte-datei
    kind: falle
    title: "Die Dockerfile ist weder Doku noch Test noch Skript - 31 Wachen lasen sie nie"
    match: "docker build.*cp.*No such file|npm run docker:build|COPY.*mupisrc|cp: cannot stat"
    sev: med
    cause: "Wachen sind nach Datei-SORTEN gebaut (Doku, Test, Skript, Unit); eine Bauanleitung faellt in keine davon und wird von keiner gelesen"
    fix: "tools/abbild-pfade-pruefen.py prueft seit 26.08.2026 jede COPY- und cp/mv-Quelle der Dockerfile gegen den Baum UND gegen das, was ueberhaupt ins Abbild kopiert wird"
    tags: [doku, wache, docker, aufraeumen, bauanleitung]
    body: |
      ══ DER FUND ═══════════════════════════════════════════════════════════
      26.08.2026, Doku-Lauf. `npm run docker:build` bricht ab und tut das seit
      **22 Monaten**. Zwei Zeilen der `Dockerfile` lesen aus Orten, die der
      Arbeitsbereichs-Umbau `5f5ea724` am 23.10.2024 geraeumt hat:

        Dockerfile:74   cp $mupisrc/dev/customize/mplayer-wrapper/index.js …
        Dockerfile:103  cp $mupisrc/bin/nodejs/spotify-control.js …

      Ein `cp` auf eine fehlende Quelle endet mit 1; `docker build` bricht an
      dieser Schicht ab. In der Doku stand dazu genau eine Zeile: „das
      Docker-Abbild aus dem Ursprungsprojekt".

      ══ WARUM ES NIEMAND FAND ═══════════════════════════════════════════════
      Das ist die naechste Stufe von
      [[skript-das-niemand-ruft-verrottet-lautlos]], und zwar genau eine Datei
      weiter. Die Wache von gestern (`npm-skripte-deckung.py`) prueft jeden
      EINGABE-Pfad jedes npm-Skripts - aber das Skript hier ist
      `docker build -t mupibox .`: **kein Pfad mit Endung, nichts zu pruefen.**
      Der tote Pfad steht nicht im Skript, sondern in dem, was das Skript
      aufruft.

      Und keine der 31 Wachen der Luecken-Probe las die `Dockerfile`. Der
      Grund ist ihre Bauart: Wachen entstehen je Datei-SORTE - Handbuch,
      Testdatei, `package.json`, systemd-Unit, Route, Umgebungsvariable. Eine
      Bauanleitung ist eine **vierte Sorte** und faellt durch jedes Raster.
      Verwandt, aber nicht dasselbe:
      [[eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen]] meint
      eine Ablage, die in einer LISTE fehlt; hier fehlt die ganze KLASSE.
      Fragen, die den Fall finden: welche ausfuehrbare Datei im Baum wird von
      keiner Wache gelesen, und wann hat sie zuletzt jemand laufen lassen?

      ══ DIE ZWEITE FRAGE, DIE DER BAUM NICHT BEANTWORTEN KANN ═══════════════
      Die Wache prueft zwei Dinge, und das zweite ist das teurere: liegt die
      Quelle im Baum - UND legt ueberhaupt ein `COPY` ihren obersten Ordner
      ins Abbild? Sieben `COPY`-Zeilen legen `autosetup`, `bin`, `config`,
      `dev`, `media`, `scripts`, `themes` unter `$mupisrc`; `src/` ist NICHT
      dabei. Wer die zwei toten Pfade auf die heutigen Orte umbiegt, tauscht
      „Quelle fehlt im Baum" gegen „Quelle fehlt im Abbild" - und keine reine
      Existenzpruefung merkt das, weil die Datei ja dann DA ist. Ein Pfad in
      einem Abbild hat zwei Wahrheiten, eine im Baum und eine im Container.

      ══ WAS DIE KORREKTUR SELBST NOCH LEHRTE ════════════════════════════════
      Der erste Entwurf des BACKLOG-Eintrags nannte als neue Orte zwei
      Dateien unter `src/backend-player/src/` - mit der Endung `.js`, wie sie
      2024 hiessen. Beide sind seit `15dd9ad4` (B2, TypeScript-Umbau) `.ts`.
      `tools/doku-pfade-pruefen.py` meldete die erfundenen Namen sofort - eine
      Korrektur, die einen Namen aus dem Kopf schreibt, ist die naechste
      Fundstelle. Und inhaltlich haengt daran mehr als ein Buchstabe: die
      neuen Quellen sind TypeScript, ein `cp` ins Abbild kopierte etwas, das
      `node` nicht startet. Der gebaute Stand liegt unter `src/deploy/`.

      ══ GEGENGEPROBT ════════════════════════════════════════════════════════
      Vier Sabotagen, jede einzeln rot, danach der Baum zurueckgesetzt (eine
      Probe, deren Erfolg ein Schaden ist, braucht ein Wegwerfziel und eine
      Sicherung im selben Befehlsblock): (1) `COPY ./bin` auf einen
      Ordner umgebogen, den es nicht gibt -> TOTE QUELLE; (2) den toten Pfad
      auf den heutigen Ort unter `src/` gerichtet -> NICHT IM ABBILD, obwohl
      die Datei im Baum liegt; (3) beide Dateien wieder angelegt -> BACKLOG
      VERALTET (die Gegenrichtung haengt am ERGEBNIS, nicht am Namen, und
      faengt darum auch „Zeile ganz gestrichen"); (4) `ARG mupisrc=`
      umbenannt -> FEHLER mit Rueckgabe 2 statt stumm gruen
      ([[gegenprobe-statt-gruen-glauben]]).

      Auskommentierte Zeilen zaehlen nicht: der `# TODO: Copy media files`-
      Block der `Dockerfile` nennt zwoelf Pfade, die es nicht gibt - er ist
      absichtlich stillgelegt, und eine dauerrote Wache ist keine
      ([[dauerrote-wache-ist-keine]]). Die Ausnahme fuer die zwei echten Funde
      wohnt aus demselben Grund im `BACKLOG.md` unter „Aufraeumen", nicht im
      Python.

      ══ WAS NICHT GEMESSEN IST ══════════════════════════════════════════════
      Ob `docker build` nach einer Reparatur durchlaeuft. Gemessen ist nur,
      was `cp` mit einer fehlenden Quelle tut - der erste Abbruch, nicht der
      letzte. Ein echter Lauf gehoert zum Aufraeumen
      ([[aufraeumen-nachmessen]]).
  - id: urteil-steht-in-der-pruefenden-datei
    kind: falle
    title: "Ein Audit erklaert eine Datei fuer ueberholt - und die Datei erfaehrt es nie"
    match: "no CI|falsche Zustandsaussagen|Kopf .ueberholt am|MODERNIZATION"
    sev: med
    cause: "Das Urteil wird in der pruefenden Datei notiert (Audit, Backlog); die geprueste Datei bleibt unveraendert und wird weiter als Gegenwart gelesen"
    fix: "tools/analyse-md-stand-pruefen.py verlangt seit 26.08.2026: wen ein AUDIT-*.md oder BACKLOG.md fuer ueberholt erklaert, der traegt die Marke im eigenen Kopf"
    tags: [doku, wache, audit, analyse-md, stand]
    body: |
      ══ DER FUND ═══════════════════════════════════════════════════════════
      26.08.2026, Doku-Lauf. `MODERNIZATION.md` (558 Zeilen) behauptet in
      seiner Komponenten-Tabelle fuenf Dinge, die alle nicht mehr stimmen -
      am selben Tag nachgemessen, nicht aus den Audits uebernommen:

        „no CI"                          -> .github/workflows/ci.yml lief bereits
        AdminInterface/ als PHP-Komponente -> null versionierte Dateien, null .php im Baum
        backend-player „Plain JS, no tests" -> 20 .ts, null .js, Test neben jedem Modul
        „~61 shell + ~22 python"          -> 69 shell + 35 python
        „Dockerfile/.devcontainer already assemble a working box container"

      Die letzte ist die teuerste und war die einzige, die noch NIEMAND
      gemeldet hatte: §5 ist das Kapitel „koennen wir das auf der
      Entwicklungsmaschine fahren?", und es verweist als Tier-A-Weg auf genau
      den Container, der seit dem 23.10.2024 nicht baut
      ([[bauanleitung-ist-eine-vierte-sorte-datei]]).

      ══ WARUM ES DREI TAGE LIEGENBLIEB ══════════════════════════════════════
      Die ersten vier waren **nicht unentdeckt**. `AUDIT-2026-08-23.md` §6.3
      und `BACKLOG.md` A4 hatten sie am 23.08. aufgeschrieben und woertlich
      einen Kopf „ueberholt am …" empfohlen. Das Urteil war gefaellt, notiert -
      und folgenlos, weil es in der **pruefenden** Datei stand und nie in der
      **gepruefteN**. Wer `MODERNIZATION.md` oeffnet, oeffnet nicht das Audit
      daneben; er liest einen undatierten Text im Praesens und haelt ihn fuer
      den Stand. Dieselbe Bauart wie `a9ed42c1`, wo die Lehre nur in einer
      Commit-Nachricht stand.

      Merksatz: **ein Befund ist erst angekommen, wenn er dort steht, wo er
      stoert** - nicht dort, wo er gefunden wurde.

      ══ DIE LUECKE IN DEN WACHEN ════════════════════════════════════════════
      `tools/doku-widerruf-probe.sh` findet genau solche widerrufenen
      Aussagen - aber ihre Liste `DATEIEN` ist von Hand gepflegt und nennt
      FUENF Handbuecher. Daneben liegen in der Wurzel **neun Analyse-MDs**
      (`AUDIT-*`, `MODERNIZATION`, `KONZEPT-SIGNAL`, `ADMIN-BOARD-ANALYSE`,
      `BOX-MENUE-ANALYSE`, `NEUE-OBERFLAECHE-PLAN`, `ANGLEICH-PI4-PI5`,
      `ZUGEZOGEN`) mit ueber 3 000 Zeilen, und fuer KEINE der 32 Wachen
      existieren sie. Eine **fuenfte Sorte Datei**, direkt nach der
      Bauanleitung von gestern: nicht Handbuch, nicht Test, nicht Skript,
      nicht Unit - sondern **Momentaufnahme**. Verwandt, aber enger:
      [[eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen]] meint
      einen fehlenden Eintrag in einer Liste; hier fehlt die ganze Klasse
      ([[skript-das-niemand-ruft-verrottet-lautlos]] ist die Skript-Variante).

      Die Frage, die den Fall findet: **welcher Text im Baum beschreibt einen
      ZUSTAND, ohne zu sagen, von wann er ist?**

      ══ WAS DIE WACHE PRUEFT ════════════════════════════════════════════════
      Zwei Dinge, und das zweite ist der eigentliche Wert:

        1. jeder Analyse-MD traegt ein Datum in den ersten 15 Zeilen;
        2. **wen ein `AUDIT-*.md` oder `BACKLOG.md` im selben Satz mit einem
           Urteilswort nennt (`falsche Zustandsaussagen`, `ueberholt`,
           `veraltet`), der traegt eine Ueberholt-Marke im eigenen Kopf.**

      Teil 2 ist die Richtung, die am 23.08. gefehlt hat. Er braucht keine
      gepflegte Liste: er liest die Urteile aus den Audits selbst, waechst
      also mit jedem neuen Audit mit - anders als das `DATEIEN`-Feld der
      Widerruf-Probe, das genau deshalb hier blind war.

      `AUDIT-*.md` sind vom Datums-Teil ausgenommen (sie tragen ihr Datum im
      Dateinamen), nicht vom Urteils-Teil: ein Audit kann ein aelteres
      ueberholen. `README.md`, `LICENSE.md` und `BACKLOG.md` sind ganz
      ausgenommen, jeweils mit Grund im Kopf der Wache
      ([[dauerrote-wache-ist-keine]]).

      ══ GEGENGEPROBT - UND DIE PROBE FAND EINEN FEHLER IN DER WACHE ═════════
      Erster Lauf: rot auf `MODERNIZATION.md`, obwohl der Kopf da schon
      **ÜBERHOLT AM 26.08.2026** trug. Ursache war das Muster
      `\bü?ue?berholt\b`: nach dem optionalen `ü` verlangt `ue?` noch ein `u`,
      also matcht es „ueberholt", aber NICHT „ÜBERHOLT". Ein Umlaut-Muster,
      das beide Schreibweisen fangen soll, braucht eine **Alternative**
      `(?:überholt|ueberholt)`, keine optionalen Einzelzeichen - der Baum
      fuehrt beide Formen nebeneinander, und dasselbe Muster sass auch im
      Urteils-Regex, wo es still nur ueber „veraltet" traf.

      Danach zwei Sabotagen mit Sicherung im selben Befehlsblock, weil die
      Dateien nicht committet waren: (1) Marke im Kopf entfernt -> URTEIL OHNE
      FOLGE; (2) das Datum aus `ADMIN-BOARD-ANALYSE.md` -> OHNE STAND. Kein
      Fund, kein Audit -> Rueckgabe 2 statt stumm gruen
      ([[gegenprobe-statt-gruen-glauben]]).

      **Der erste Sabotage-Versuch war selbst falsch gewaehlt** und ist die
      Nebenlehre: er nahm `KONZEPT-SIGNAL.md` das Datum - dessen Kopf nennt
      aber DREI Datumsangaben, die Wache blieb zu Recht gruen. Eine Gegenprobe
      auf einer Datei, die den Fehler gar nicht annehmen KANN, meldet
      „nicht erkannt" und sieht aus wie eine blinde Wache. Wer sabotiert,
      prueft vorher, dass das Ziel den Fehler ueberhaupt tragen kann.

      ══ WAS OFFEN BLEIBT ════════════════════════════════════════════════════
      Die Wache prueft, ob eine Ueberholt-MARKE dasteht - nicht, ob der Text
      inhaltlich stimmt. Sie haette den §5-Satz ueber den „working box
      container" nie gefunden; den fand Lesen. Und `.devcontainer/` bleibt ein
      offener Posten fuer das Aufraeumen: es provisioniert weiterhin PHP 8.3 +
      Composer + die PHP-Erweiterung fuer einen Baum mit null `.php`-Dateien,
      und es installiert Chrome, waehrend die Doku daneben eine
      `CHROME_BIN`-Aufloesung ueber das Playwright-Binary beschreibt. Beides
      ist nicht nachgemessen ([[aufraeumen-nachmessen]]).
    related: ["bauanleitung-ist-eine-vierte-sorte-datei", "skript-das-niemand-ruft-verrottet-lautlos", "dauerrote-wache-ist-keine", "gegenprobe-statt-gruen-glauben", "eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen", "ungerufene-wache-driftet-in-den-fehlalarm", "doku-zeigt-auf-dateien-die-es-nicht-gibt", "aufraeumen-nachmessen"]

  - id: simulationsrezept-ist-die-sechste-sorte-datei
    kind: falle
    title: "Das Rezept, mit dem alle anfangen, liest keine Wache"
    match: "docker-compose|harness|seed_state|mupictl|Tier-A|bind mount"
    sev: med
    cause: "Wachen entstehen je Datei-SORTE (Handbuch, Test, package.json, Unit, Route, Umgebungsvariable, Bauanleitung, Momentaufnahme); das Simulationsrezept ist keine davon und faellt durch jedes Raster"
    fix: "tools/simulationsrezept-pruefen.py prueft seit 26.08.2026 jeden Bind-Mount gegen `git ls-files` UND gegen das, was `seed_state()` anlegt; dazu Dienste gegen harness/README.md in beide Richtungen"
    tags: [doku, wache, harness, docker, tier-a, simulation]
    body: |
      ══ DER FUND ═══════════════════════════════════════════════════════════
      26.08.2026, zweiter Doku-Lauf des Tages. Beide Proben meldeten sauber,
      kein neuer Commit - der Einstieg war die Muster-Methode auf den Vorlauf:
      der hatte die Analyse-MDs als FUENFTE Sorte Datei gefunden, der Tag davor
      die `Dockerfile` als vierte. Also weitergefragt: **welche Sorte fehlt
      noch?** Die Antwort ist `harness/` - das Rezept, das sagt, wie der ganze
      Stapel auf der Entwicklungsmaschine faehrt. Es ist der Tier-A-Weg aus
      `MODERNIZATION.md` §5, also genau der Weg, den ein Neuzugang als ERSTES
      geht, und keine der 33 Wachen las ihn.

      Drei Funde in einer Datei-Familie von vierzehn Dateien:

      **(1) Zwei eingehaengte Dateien legte niemand an.**
      `harness/docker-compose.yml` mountet `./state/config-api.json` und
      `./state/mupiboxconfig.json` SCHREIBBAR - dorthin schreibt der
      `/spotify`-Login den Refresh-Token. `seed_state()` in `mupictl` schrieb
      `mupihat.json`, `resume.json`, `active_resume.json` und kopierte
      `data.json`; diese zwei nicht, obwohl es `harness/seed/config-api.json`
      und `harness/seed/mupiboxconfig.json` gibt. **Docker legt fuer einen
      fehlenden Bind-Mount ein VERZEICHNIS an** - auf einem frischen Klon
      bekommt `backend-api` also einen Ordner, wo seine `config.json` liegen
      soll. Behoben, zwei Zeilen nach dem Muster der `data.json`-Zeile.

      **(2) Der Dienst `admin` ist tot und sah lebendig aus.** Er mountet
      `../AdminInterface/www`, mit E47 (19.08.2026) ausgebaut - null
      versionierte Dateien darunter, null `.php` im ganzen Baum. `AUDIT-2026-
      08-25.md` §10 hat das gemeldet, und wieder stand das Urteil nur in der
      pruefenden Datei ([[urteil-steht-in-der-pruefenden-datei]]).

      **(3) Und der Satz, der es zudeckte.** `harness/README.md` fuehrte unter
      „Notes / limitations": „No Chromium kiosk, **no PHP admin**, no systemd".
      Der Satz war beim Schreiben FALSCH - das Profil war da - und ist durch
      E47 **von allein richtig** geworden. **Eine Verneinung, die zufaellig
      wahr wird, ist keine Korrektur**: sie liest sich wie eine bewusste
      Abgrenzung und beantwortet damit genau die Frage, die zum toten Profil
      gefuehrt haette. Vier Wochen unsichtbar.

      ══ DIE REGEL DER WACHE, UND WARUM SIE NICHT „ALLES MUSS DA SEIN" IST ═══
        Quelle liegt im Baum (git)                -> gut
        Quelle legt `seed_state()` an             -> gut
        Quelle ohne Endung (= Verzeichnis)        -> gut, Docker legt es an
        sonst (Datei mit Endung)                  -> FUND
        Quelle beginnt mit `../`                  -> IMMER Baumpfad, muss liegen

      Die letzte Zeile traegt den zweiten Fund: `../AdminInterface/www` haette
      als „Verzeichnis, legt Docker an" durchgesehen. **`./` ist Laufzeit,
      `../` zeigt in den Baum** - der Unterschied ist die halbe Wache.

      ══ DIE TEUERSTE FALLE BEIM BAU: `os.path.exists` IST NICHT DER BAUM ════
      Die erste Fassung fragte das Dateisystem und meldete **gruen** - auf
      einer Maschine, auf der beide Funde offen dalagen. `AdminInterface/www`
      steht hier als leerer, root-eigener Ordner herum (E47 hat den INHALT
      entfernt, nicht die Huelle), und `harness/state/config-api.json` liegt
      seit dem 24.07. von Hand da. Beides ist auf einem frischen Klon nicht da.
      **Der Baum ist, was git fuehrt**; alles andere ist Ruecklass dieser einen
      Maschine - und genau der Ruecklass macht eine Wache blind fuer den Fall,
      den sie finden soll ([[einmal-hinsehen-ist-keine-messung]]).

      ══ WIE DIE WACHE NICHT DAUERROT WURDE ══════════════════════════════════
      Fund (2) laesst sich nicht reparieren - der Baum, den der Dienst
      einhaengt, ist weg, und ob das Profil geloescht wird, ist eine
      Entscheidung ([[dauerrote-wache-ist-keine]]). Loesung: **die Ausnahme
      steht im Rezept, nicht in der Wache.** Wer einen Dienst fuer tot
      erklaert, schreibt `TOT`/`historisch`/`stillgelegt` UEBER den Dienst; die
      Wache meldet ihn dann als VERMERKT weiter - sichtbar, aber nicht rot.
      Eine Namensliste in der Wache waere eine Ausnahme, die niemand sieht,
      der das Rezept liest ([[ausnahmeliste-gilt-je-datei-nicht-je-name]]).

      ══ GEGENGEPROBT ════════════════════════════════════════════════════════
      Fuenf Sabotagen, Sicherung im selben Befehlsblock (die Dateien waren
      nicht committet): Todesvermerk entfernt -> rot; `seed_state()`-Zeile
      entfernt -> rot; Dienstname im README geaendert -> rot; toter Pfad ins
      `harness/README.md` -> rot; blanker toter Pfad in einen Kommentar der
      `docker-compose.yml` -> rot, derselbe Pfad in der `command:`-Zeile ->
      gruen (richtig: container-relativ). Baum danach zurueckgesetzt
      ([[gegenprobe-statt-gruen-glauben]]).

      **Eine Sabotage war falsch gewaehlt** und wiederholt die Nebenlehre des
      Vorlaufs: `./tools/gibt-es-nicht.sh` in eine `volumes:`-Zeile geschrieben
      blieb gruen - das Muster von `doku-pfade-pruefen.py` hat einen
      Lookbehind, der einen Pfad hinter `./` nicht anfasst, und Mount-Pfade
      sind ohnehin harness-relativ. Das Ziel konnte den Fehler nicht tragen;
      die richtige Form ist der blanke Pfad im Kommentar, so wie der echte Fund
      (`spotify-control.js`, die Datei heisst seit dem TS-Umbau `.ts`).
    related: ["bauanleitung-ist-eine-vierte-sorte-datei", "urteil-steht-in-der-pruefenden-datei", "einmal-hinsehen-ist-keine-messung", "dauerrote-wache-ist-keine", "ausnahmeliste-gilt-je-datei-nicht-je-name", "gegenprobe-statt-gruen-glauben", "doku-zeigt-auf-dateien-die-es-nicht-gibt", "eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen"]

  - id: korrektur-endet-am-code-block
    kind: falle
    title: "Ein Bild traegt kein Durchgestrichen - dort ueberlebt jeder Widerruf"
    match: "Architekturbild|ASCII|code block|durchgestrichen|~~|Diagramm"
    sev: med
    cause: "Eine Korrektur wird mit Markdown-Auszeichnung gesetzt (~~alt~~ -> neu); in einem Code-Block rendert die Auszeichnung nicht, also fasst sie niemand an - und ein Bild wird zuerst angesehen"
    fix: "Wer eine Behauptung widerruft, greppt nach ihr im ganzen Text UND sieht in die Code-Bloecke; dort wird im Klartext nachgezogen, mit einem Kasten darunter"
    tags: [doku, korrektur, markdown, diagramm, analyse-md]
    body: |
      HIER GEHT ES UM DEN ORT DES WIDERRUFS, nicht um seine Zeichen.
      Wird ein `~~durchgestrichenes~~` Wort von einem WERKZEUG mitgezaehlt
      und die Summe dadurch plausibel und falsch herum, ist es der andere
      Eintrag: [[durchgestrichenes-statuswort-zaehlt-mit]]. In diesem hier
      ist die Auszeichnung richtig gesetzt — sie kommt nur an der zweiten
      Fundstelle nicht an, weil dort ein Code-Block steht.

      ══ DER FUND ═══════════════════════════════════════════════════════════
      26.08.2026, zweiter Doku-Lauf. Der Lauf davor hatte `MODERNIZATION.md`
      einen Kopf „UEBERHOLT AM 26.08.2026" gegeben und darin versprochen, die
      fuenf falschen Zustandsaussagen seien „unten **am Ort** korrigiert". In
      der Komponenten-Tabelle in §2 waren sie das auch - sauber
      durchgestrichen. **Zwei davon stehen im Text aber ZWEIMAL**, und die
      zweite Stelle ist das ASCII-Architekturbild in §1:

          backend-player (Express 4 / JS, legacy) :5005    PHP admin (lighttpd) :80/443

      Beide Behauptungen hatte der Kasten ganz oben am selben Tag als falsch
      gemessen (20 `.ts`, null `.js`; PHP-Admin mit E47 ausgebaut). Im Bild
      standen sie unveraendert - und §1 ist die Stelle, die ein Leser als
      erste ansieht.

      ══ WARUM AUSGERECHNET DORT ═════════════════════════════════════════════
      **Ein Code-Block traegt kein `~~durchgestrichen~~`.** Wer eine Aussage
      per Markdown-Auszeichnung widerruft, kann in einer Zaeunung nichts tun -
      also wird die Zaeunung uebersprungen, meist ohne den Gedanken zu
      bemerken. Dazu kommt der Leseunterschied: eine Tabelle wird gescannt,
      ein Bild wird ANGESEHEN und geglaubt. Die Korrektur landet in der Prosa,
      die niemand mehr braucht, und bleibt in dem Bild stehen, aus dem alle
      ihre Vorstellung von der Architektur ziehen.

      ══ DIE REGEL ═══════════════════════════════════════════════════════════
      Dieselbe Form wie [[urteil-steht-in-der-pruefenden-datei]] und wie „wo
      steht diese Zahl NOCH?", nur INNERHALB einer Datei: **vor der ersten
      Korrektur greppen, nicht nach der letzten** - und in die Code-Bloecke
      sehen, weil das Auszeichnungs-Werkzeug dort aufhoert. Im Bild selbst im
      Klartext nachziehen, darunter ein Kasten mit dem Gemessenen; die
      Auszeichnung fehlt dort ohnehin, also muss der Klartext sie ersetzen.

      Nebenbefund derselben Datei, gleiche Ursache in Prosa: §5 empfahl als
      „Starting point: the existing `Dockerfile` + … `php -S` for admin" -
      zwoelf Zeilen unter dem Absatz, in dem seit demselben Tag steht, dass die
      Wurzel-`Dockerfile` seit 22 Monaten nicht baut. **Ein Kasten „so faengst
      du an" wird gelesen, wenn jemand anfaengt**, und der schlaegt vorher
      nicht nach, ob zwoelf Zeilen darueber ein Widerruf steht.

      Dieselben zwei Saetze standen ausserdem in `harness/README.md` und
      `harness/docker-compose.yml` - siehe
      [[simulationsrezept-ist-die-sechste-sorte-datei]].
    related: [urteil-steht-in-der-pruefenden-datei, simulationsrezept-ist-die-sechste-sorte-datei, warntext-den-niemand-liest, begruendung-liest-sich-als-stand, audit-liste-ist-keine-dokumentation, durchgestrichenes-statuswort-zaehlt-mit]

  - id: entwicklerabbild-ist-die-siebte-sorte-datei
    kind: falle
    title: "Die Datei, die ein Neuzugang zuerst anfasst, las keine Wache"
    match: "devcontainer|codespace|features/php|installComposer|Dockerfile|COPY"
    sev: med
    cause: "Die Abbild-Wache haelt WURZEL/'Dockerfile' fest; ein zweites Abbild eine Ebene tiefer (.devcontainer/) ist fuer sie unsichtbar, und kein Werkzeug nannte das Wort devcontainer"
    fix: "tools/entwicklerabbild-pruefen.py prueft seit 27.08.2026 jede COPY-Quelle gegen den Bau-Kontext UND jedes Sprach-Feature/jede Erweiterung gegen `git ls-files`; haengt in tools/doku-luecken-probe.sh"
    tags: [doku, wache, devcontainer, docker, codespace, php]
    body: |
      ══ DER FUND ═══════════════════════════════════════════════════════════
      `.devcontainer/` ist die SIEBTE Sorte Datei, die keine Wache las - nach
      der Bauanleitung (`Dockerfile`), der Momentaufnahme (Analyse-MDs), dem
      Urteil (`ci.yml`) und dem Simulationsrezept (`harness/`). Am 27.08.2026
      nannte im ganzen `tools/`-Verzeichnis KEIN EINZIGES Werkzeug das Wort
      `devcontainer`.

      Der Grund ist praezise und lehrreich: die Wache fuer Abbilder gibt es
      laengst - `tools/abbild-pfade-pruefen.py`, gebaut am 26.08. fuer genau
      diese Bauart von Fehler. Sie haelt aber in ihrer zweiten Zeile
      `ABBILD = WURZEL / "Dockerfile"` fest. **Eine Wache, die ihren
      Gegenstand als PFAD festhaelt statt als SORTE, deckt genau ein
      Exemplar.** Ein zweites Abbild eine Verzeichnisebene tiefer ist fuer sie
      kein Grenzfall, sondern gar nicht vorhanden - dieselbe Form wie
      [[eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen]], nur
      dass hier die SCHWESTERDATEI fehlt, nicht die Ablage.

      ══ WAS DIE NEUE WACHE MASS ════════════════════════════════════════════
      `BACKLOG.md` A4b fuehrte seit dem 26.08.2026 einen Fund ausdruecklich
      als **„nicht nachgemessen"**: `.devcontainer/` provisioniere den mit E47
      ausgebauten PHP-Stack. Genau die Sorte Satz, die liegenbleibt
      ([[aufraeumen-nachmessen]]). Nachgemessen am 27.08.:

        * `git ls-files '*.php'`            -> 0
        * `git ls-files '*composer.json'`   -> 0
        * dagegen `features/php:1` (8.3.0, `installComposer: true`) und
          `devsense.phptools-vscode` in `devcontainer.json`

      Die Behauptung stimmt also. Was WEITERHIN nicht gemessen ist: ob der
      Container baut - es wurde kein `docker` gerufen. Der Unterschied gehoert
      in den Text, sonst liest sich „nachgemessen" als „gebaut".

      Die COPY-Probe war die eigentliche Ueberraschung: sie ist GRUEN.
      `.devcontainer/Dockerfile` kopiert `first-run-notice.txt`, und die liegt
      da. Die Wache haette also nichts gefunden, wenn man sie nur wegen der
      Wurzel-`Dockerfile` gebaut haette - **gebaut wurde sie wegen der SORTE,
      nicht wegen eines Verdachts**, und der Fund kam aus der zweiten Haelfte.

      ══ WAS DIE GEGENPROBE KORRIGIERTE ═════════════════════════════════════
      Fuenf Sabotage-Proben ([[gegenprobe-statt-gruen-glauben]]). Die erste
      DECKTE EINEN FEHLER DER WACHE AUF: die Anmeldung im Backlog wurde mit
      `kandidat.startswith("A4b")` gesucht - `A4bXX` traf damit auch. Wer den
      Abschnitt umbenennt oder daneben einen `A4b2` anlegt, haette einen Fund
      unter FREMDER Anmeldung stillgestellt. Jetzt steht dort eine Grenze
      (`(?![0-9A-Za-z])`). Ohne die Gegenprobe waere das nie aufgefallen -
      beide Laeufe melden ja gruen.

      Die uebrigen vier: Abschnitt entfernt -> UEBERFLUESSIG statt HINWEIS;
      COPY-Quelle weggezogen -> TOTE QUELLE; `features` geleert -> die Wache
      meldet sich BLIND (Rueckgabe 2); PHP-Feature entfernt, Anmeldung bleibt
      -> BACKLOG VERALTET. Die letzte ist die Gegenrichtung und haengt am
      ERGEBNIS, nicht am Namen ([[dauerrote-wache-ist-keine]]).

      ══ DIE REGEL ══════════════════════════════════════════════════════════
      Beim Bauen einer Wache nicht fragen „welche DATEI pruefe ich?", sondern
      „welche SORTE?" - und dann `find`/`git ls-files` nach GESCHWISTERN
      derselben Sorte fragen, bevor der Pfad festgenagelt wird. Ein
      `WURZEL / "Dockerfile"` ist eine Annahme, die sich als Konstante
      tarnt. Und wer eine Behauptung „nachgemessen" nennt, schreibt dazu, WAS
      gemessen wurde: hier die Provisionierung, nicht der Bau.
    related: ["bauanleitung-ist-eine-vierte-sorte-datei", "simulationsrezept-ist-die-sechste-sorte-datei", "aufraeumen-nachmessen", "dauerrote-wache-ist-keine", "gegenprobe-statt-gruen-glauben", "eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen", "skript-das-niemand-ruft-verrottet-lautlos"]
  - id: aehnlichkeit-ist-das-gegenteil-von-schwere
    kind: falle
    title: "Die Zwillingswache meldete den mildesten von drei Zwillingen"
    match: "zwillingsdateien-abgleich|SCHWELLE|aehnlich|namensgleich|asound.conf|bootwache|remote-step-installer/tools"
    sev: high
    cause: "Eine Wache, die Drift ueber die AEHNLICHKEIT zweier Dateien misst, verliert genau die schlimmste Drift: das Merkmal, an dem sie ihren Gegenstand erkennt, verschwindet mit dem gesuchten Fehler"
    fix: "tools/zwillinge-nach-ziel.py paart seit 27.08.2026 ueber das ZIEL AUF DER BOX (put:-dest der Rezepte gegen die ${MUPI_SRC}-Kopierzeilen von autosetup/update) — unabhaengig von Name und Aehnlichkeit; haengt in tools/pruefen.sh neben der alten Wache"
    tags: [wache, installer, zwillinge, drift, doku, asound, bootwache]
    body: |
      ══ DER FUND ═══════════════════════════════════════════════════════════
      `tools/zwillingsdateien-abgleich.py` gibt es seit dem 05.08.2026 fuer
      genau eine Frage: welche Datei liegt in BEIDEN Baeumen (box-Repo und
      remote-step-installer) und ist auseinandergelaufen? Am 27.08.2026 meldete
      sie EINEN Befund. Am selben Tag von Hand nachgemessen waren es DREI - und
      der gemeldete war der MILDESTE:

          Ziel auf der Box                     box     installer   aehnlich
          /opt/mupibox-tools/bootwache.py      808 Z.  197 Z.       3 %   still
          /etc/asound.conf                      10 Z.   23 Z.       6 %   still
          .../mupibox-boot-splash.py           907 Z.  770 Z.      90 %   GEMELDET

      ══ WARUM ══════════════════════════════════════════════════════════════
      Die Wache urteilt ueber `SCHWELLE = 0.5`: unter der Haelfte gemeinsamer
      Zeilen gilt ein Paar als "nur namensgleich - Rauschen".

      **AEHNLICHKEIT IST DAS GEGENTEIL VON SCHWERE.** Je weiter zwei Fassungen
      auseinanderlaufen, desto sicherer faellt das Paar unter JEDE Schwelle.
      Das Merkmal, an dem die Wache ihren Gegenstand erkennt, verschwindet
      zusammen mit dem Fehler, den sie sucht. Eine kleinere Zahl hilft dagegen
      nicht - 3 % erreicht keine Schwelle, die nicht jedes Repo mit jedem Repo
      paart.

      Der Kommentar der Wache benennt die Richtung sogar selbst ("wird
      ausgerechnet dann uebersehen, wenn sie am weitesten auseinander ist") und
      zieht daraus den falschen Schluss: eine niedrigere Konstante statt eines
      anderen Merkmals. Und `asound.conf` steht woertlich in ihrem Kopf als
      BELEG dafuer, dass Namensgleichheit nichts heisst ("hat 6 % gemeinsame
      Zeilen") - die 6 % sind aber nicht der Beweis, dass es kein Zwilling ist,
      sondern das Mass, wie weit der Zwilling auseinandergelaufen ist. Der
      schlimmste echte Fund stand als Musterbeispiel fuer Rauschen im Text.

      `bootwache.py` fiel doppelt durch: der Installer nennt sie
      `mupibox-bootwache.py`, also greift der Namensdurchgang nicht, und fuer
      den Inhaltsdurchgang sind 197 gegen 808 Zeilen kein Kandidat.

      ══ DAS MERKMAL, DAS BLEIBT ════════════════════════════════════════════
      Zwei Dateien sind Zwillinge, wenn beide Ausrollwege sie an DIESELBE
      Stelle auf der Box legen - egal, wie sie heissen und wie verschieden sie
      inzwischen sind. Das Ziel steht in einer Datei und ist nachlesbar:

        Installer   `remote-step-installer/recipes/*.yaml`, die Schluessel
                    `src` und `dest` unter `put:`.
                    Landet das put: in /tmp, steht das echte Ziel im `run:`
                    DESSELBEN Schritts (`install -m 644 /tmp/x /etc/x`).
        box-Repo    `autosetup/autosetup.sh` und
                    `update/start_mupibox_update.sh`, Zeilen
                    `mv|cp|install [-flags] ${MUPI_SRC}/QUELLE ZIEL`
                    (Glob und Ziel-Schraegstrich aufgeloest).

      Dieselbe Bauart wie [[entwicklerabbild-ist-die-siebte-sorte-datei]]: dort
      hielt eine Wache ihren Gegenstand als PFAD statt als SORTE fest, hier als
      AEHNLICHKEIT statt als ZIEL. Beide Male deckt die Wache genau die Faelle,
      die ohnehin harmlos sind.

      ══ WAS OFFEN BLEIBT (Stand 27.08.2026) ════════════════════════════════
      Die neue Wache ist ROT, und zwar richtigerweise - die zwei stillen
      Zwillinge sind ECHTE offene Befunde, keine Absicht:

        * `/opt/mupibox-tools/bootwache.py` - eine Rezept-Box bekommt die
          197-Zeilen-Altfassung, eine autosetup-Box die 808er. Der
          Kopierblock in `autosetup.sh:613` waehlte `/opt/mupibox-tools/`
          ausdruecklich, "damit nicht ZWEI Fassungen entstehen" - der Pfad
          stimmt seitdem, der Inhalt nie.
        * `/etc/asound.conf` - box 10 Zeilen, Installer 23 (Stand 2022).

      WELCHE Fassung die richtige ist, ist eine Entscheidung, keine Messung
      (der Installer richtet eine FRISCHE Karte ein und darf dort tun, was auf
      einer laufenden Box falsch waere). Die Wache zeigt den Befund und den
      diff-Befehl; entschieden wird von Hand. Von Hand steht der Befund seit
      dem 22.08. in `AUDIT-2026-08-22.md` §1.5 und seit dem 25.08. in
      `AUDIT-2026-08-25.md` Punkt 3 - beide Male gefunden, weil jemand
      hinsah, nicht weil eine Wache anschlug.

      Wer die Liste liest, muss wissen: DREI genannte Ursachen sind zaehlbar.
      Ein VIERTER Zwilling waere neu und geht sonst in der Dauerroete unter -
      siehe [[dauerrote-wache-ist-keine]].

      ══ WAS DIE ALTE WACHE WEITER KANN ═════════════════════════════════════
      Sie bleibt stehen und laeuft weiter. Sie findet Zwillinge, die ueber
      KEINEN der beiden Ausrollwege gehen, und umbenannte Paare ueber den
      Inhalt. Die neue findet die, die durch die Schwelle fallen. Zwei Wachen
      auf denselben Dateien mit verschiedener Antwort sind hier Absicht und
      kein Widerspruch - vgl. [[zwei-wachen-eine-datei-verschiedene-antwort]].
      Die `ABSICHT`-Listen BEIDER Wachen gehoeren zusammen gepflegt; die
      Splash-Unit steht in beiden mit derselben Begruendung.
  - id: audit-2026-08-27-kritiker
    kind: howto
    title: "Kritiker-Lauf 27.08.2026 — Spotify-Doppelcache im selben Prozess, die Abstraktionen enden an der falschen Grenze, Blick nach draussen 2026"
    match: "spotify-api.service|spotify-media-info|Doppelcache|media-provider|MedienQuelle|zwillinge-nach-ziel|go-librespot|cloud-init|mpv-ipc|AUDIT-2026-08-27"
    tags: [audit, spotify, schnittstellen, dopplung, 2026]
    body: |
      Quelle: AUDIT-2026-08-27.md (sieben Pruefer; Reviere der Laeufe
      21.-26.08. bewusst nicht wiederholt). Rueckstand: VIERTER Tag null
      abgetragene Audit-Punkte, die sechs Ranglisten haben am siebten Tag
      weiter keine E-Nummern - Rang 1 der Vorschlagsliste bleibt deshalb
      diese Entscheidung selbst.

      DER STAERKSTE NEUFUND (D1): das backend-api betreibt ZWEI
      Spotify-Cache-Schichten im selben Prozess - services/
      spotify-api.service.ts (679 Z., cache/spotify-api) und
      spotify-media-info.service.ts (420 Z., cache/spotify). Gleiche Bauart
      bis in die Emoji-Logzeilen des Hintergrund-Abarbeiters (400-420 vs.
      375-395), beide in server.ts instanziiert (718, 803, 16959), und
      dieselbe playlist_<id>.json liegt md5-gleich in BEIDEN Caches.
      Doppelte Netzlast, doppelter Platz, und wer einen Cache leert, hat
      den anderen noch. Lag eine Ebene UNTER den vier bekannten
      Token-Erneuerern - die Dienste-Schicht war nie auditiert.

      DER ROTE FADEN: die Abstraktionen sind fast immer schon GEBAUT und
      dann nicht durchgezogen. media-provider.ts (frontend-box:1-99) IST
      die gesuchte Quellen-Registry, endet aber an der Backend-Grenze -
      dahinter kennen 26 Dateien alle vier Quellnamen (spotify-control.ts
      2346-2516 als Verb-Kette mit 46 Literalen). atomar.ts importiert nur
      server.ts, 13 rohe writeFileSync daneben ([[vierzehn-kopien-und-die-abweichung-ist-der-fehler]]).
      mpv-protokoll.ts kann die mpv-Ereignisschiene, der Wrapper pollt
      trotzdem im Sekundentakt. Der naechste Schritt ist nirgends
      Erfindung, ueberall Vollendung.

      WEITERE NEUFUNDE: themen.ts:205-258 = themen-wahl.ts:29-78
      zeichengleich; Weiterhoeren-Anzeige doppelt in frontend-box
      (resume.page 78-271 vs. resume-overlay 246-328, Drift belegt);
      Bildschirmtastatur fuenffach; SD-Vorbereitung zweimal unabhaengig
      (make-boot-sd.sh vs. sdprep.py, gleiche AUTO_SETUP-Tabelle) - die
      vierte Auspraegung von [[drei-ausrollwege-keiner-kennt-den-anderen]];
      autosetup<->update 45 gemeinsame Bloecke mit Drift (ln -sf vs. ln -s,
      fuenf Nur-autosetup-Templates, toter librespot.conf-Verweis
      start_mupibox_update.sh:974); setting_update.sh schreibt in
      Vor-Fork-Pfade (Sonos-Kids-Controller-master);
      die drei matplotlib-Auswertungen parse_log.py, plot_charge.py und
      plot_discharge.py (damals unter scripts/mupihat, seit 29.08.2026 als
      Analysewerkzeuge unter tools/mupihat-analyse/) sowie zwei
      config/templates-Vorlagen ohne jeden Weg. Zwillinge nach
      Ziel: siehe [[aehnlichkeit-ist-das-gegenteil-von-schwere]] -
      dieser Lauf hat die Wache mitgelaufen und ihren ersten, noch
      fehlerhaften Ausdruck (wifi-powersave-off "16 gegen 14 Zeilen") per
      diff+md5sum als Baustellen-Zwischenstand entlarvt.

      NEGATIV-BEFUND, der eine Sorge erledigt: awk-Scan ueber alle .ts/.sh/
      .py nach auskommentierten Codebloecken (>=8 Kommentarzeilen mit
      Code-Zeichen) - null Treffer. Diese Klasse muss niemand mehr absuchen.
      Und /api/stroeme/belegung hat jetzt die ZWEITE unabhaengige
      Null-Messung gegen die "nicht tot"-Notiz von AUDIT-21.

      BLICK NACH DRAUSSEN (Stand 2025/26, Verworfenes der Vorlaeufe NICHT
      wiederholt): mpv JSON-IPC statt Sekundentakt (Werkzeug liegt im
      Haus); go-librespot bringt REST+WS-Steuerung mit (als DRITTE
      Tonmaschine pruefen, kein Umsturz); cloud-init ist seit 11/2025
      offiziell in Raspberry Pi OS (betrifft den AUTO_SETUP-Doppelweg);
      audiocontrol-Muster (Phoniebox future3/HiFiBerryOS) als
      Langfrist-Form des Abspieler-Interfaces; better-sqlite3 erst NACH
      der ablage.ts-Konsolidierung erwaegen.

      Vorschlagsliste mit Raengen und Aufwand: AUDIT-2026-08-27.md par. 6.
    related: ["audit-2026-08-23-kritiker", "aehnlichkeit-ist-das-gegenteil-von-schwere", "drei-ausrollwege-keiner-kennt-den-anderen", "vierzehn-kopien-und-die-abweichung-ist-der-fehler", "dienstliste-an-fuenf-orten", "doppelung-lassen-auseinanderlaufen-messen"]

  - id: die-binaerdatei-hat-keinen-bezeichner-zum-greppen
    kind: falle
    title: "Vierzehn Schriften im Baum, vier ohne eine Zeile darueber, woher sie kommen"
    match: "woff2|ttf|otf|Schrift|Font|Baloo|Nunito|schriften|themes/.*Readme|OFL|Herkunft"
    sev: med
    cause: "Alle 34 Doku-Wachen fragen 'nennt die Doku, was im Baum steht?' fuer TEXT - Wege, Zeilen, Endpunkte, Units, Vokabular, Schluessel. Eine mitgelieferte Binaerdatei hat keinen Bezeichner, den man greppen koennte, und faellt durch jede dieser Fragen hindurch"
    fix: "python3 tools/schrift-herkunft-pruefen.py - jede von git ls-files gemeldete ttf/otf/woff/woff2 braucht eine Font:-Zeile mit http-Adresse in einer Readme NEBEN der Datei oder einen Eintrag mit Namensprobe in der Wurzel-README/LICENSE.md. Haengt in tools/doku-luecken-probe.sh"
    tags: [doku, wache, schriften, lizenz, fremddatei, sorte]
    body: |
      ══ DER FUND ═══════════════════════════════════════════════════════════
      Die MITGELIEFERTE FREMDDATEI ist die ACHTE Sorte, die keine Wache las -
      nach Bauanleitung (`Dockerfile`), Momentaufnahme (Analyse-MDs), Urteil
      (`ci.yml`), Simulationsrezept (`harness/`) und Entwicklerabbild
      (`.devcontainer/`, siehe [[entwicklerabbild-ist-die-siebte-sorte-datei]]).

      Das Repo befolgt eine Regel, die es nirgends aufgeschrieben hatte, aber
      ZEHNMAL einhaelt: neben jedem Themenordner mit fremden Bildern und
      Schriften liegt eine `Readme.md` mit `# Sources` und je einer Zeile
      `Background:` / `Font:` samt Adresse. Die Danksagung der `README.md`
      nennt jede fremde Zutat mit Adresse - Katzenbild, Startklang,
      Ausschaltklang, jq, Symbole.

      Gebrochen war die Regel an genau EINER Stelle, und es war die
      sichtbarste: seit `3c59c80f` (31.07.2026) liegen unter
      `NewDesign/schriften/` VIER woff2, rund 135 kB, auf jeder Box, aus
      denen JEDER Buchstabe der neuen Oberflaeche besteht. Kein Readme
      daneben, kein Lizenztext im Baum, `LICENSE.md` erwaehnt keine Schrift,
      und die eine Zeile in der Danksagung nannte "Baloo 2 und Nunito" als
      einzige Eintraege der ganzen Liste OHNE Adresse.

      ══ WIE DIE ANGABEN BELEGT WURDEN ══════════════════════════════════════
      NICHT aus dem Netz abgeschrieben, sondern AUS DEN DATEIEN SELBST
      gelesen - `fontTools.ttLib.TTFont`, `name`-Tabelle, Eintrag 0
      (Copyright) und 14 (Lizenz-Adresse):

          Baloo 2 -> "Copyright 2019 The Baloo 2 Project Authors
                      (https://github.com/EkType/Baloo2)", OFL
          Nunito  -> "Copyright 2014 The Nunito Project Authors
                      (https://github.com/googlefonts/nunito)", OFL

      Die Datei ist damit ihr eigener Beleg, auch wenn eine Adresse im Netz
      einmal umzieht. Fuer eine Binaerdatei ist das der einzige Weg, der
      nicht Vermutung ist - `strings` reicht nicht, woff2 ist komprimiert.

      ══ WARUM NUR SCHRIFTEN UND NICHT "ALLE FREMDDATEIEN" ══════════════════
      Im Baum liegen 298 PNG, 40 STL, 32 SKP; der weit ueberwiegende Teil ist
      selbst gebaut (3D-Teile, Bildschirmfotos, Maskottchen). Eine Wache, die
      fuer jedes Bild einen Herkunftsnachweis verlangt, meldet auf Dauer rot
      und wird zu Recht ignoriert ([[dauerrote-wache-ist-keine]]). Bei
      Schriften stellt sich die Frage nicht: EINE SCHRIFTDATEI ZEICHNET IN
      DIESEM PROJEKT NIEMAND SELBST, jede ist fremd - und der Baum belegt es,
      zehn von vierzehn hatten ihre Quelle von Anfang an. Die Sorte ist damit
      die einzige, bei der "ohne Quelle" zuverlaessig "Luecke" heisst.

      Die Wache haengt an der ENDUNG, nicht an `NewDesign/schriften/` -
      dieselbe Lehre wie bei der Abbild-Wache, die `WURZEL/'Dockerfile'`
      festhielt ([[entwicklerabbild-ist-die-siebte-sorte-datei]]): eine
      Schrift, die morgen unter `src/` liegt, wird genauso gefragt.

      ══ DIE ZWEITE LEHRE: NAMENSGLEICHHEIT IST ZU STRENG ═══════════════════
      Der erste Entwurf verlangte, dass die Adresse den Dateinamen nennt. Er
      meldete SECHS statt vier - `themes/comic` liefert
      `snaphand-v1-free.ttf` und nennt `dafont.com/de/snaphand.font`,
      `themes/mystic` liefert `ylee_Mortal_Heart.ttf` und nennt
      `dafont.com/de/ylee-mhim.font`. Schriftgiessereien benennen ihre Seite
      anders als die Datei. Zwei Fehlalarme aus zehn richtigen Eintraegen -
      genau die Quote, an der eine Wache ignoriert wird. Die Fassung
      unterscheidet jetzt zwei Wege: NEBEN der Datei genuegt eine
      `Font:`-Zeile mit Adresse (der Ordner ist klein und eindeutig), in der
      Wurzel-README ist die Namensprobe noetig (dort stehen Dutzende fremder
      Zutaten, sonst ginge irgendeine Adresse als Beleg durch).

      ══ WAS DIE WACHE NICHT TUT ════════════════════════════════════════════
      Sie prueft NICHT, WELCHE Lizenz das ist und ob sie eingehalten wird.
      Das ist eine Rechtsfrage und keine Wache; sie stellt nur fest, dass
      ueberhaupt jemand aufgeschrieben hat, woher die Datei kommt.

      ══ NEBENBEFUND, NICHT BEHOBEN ═════════════════════════════════════════
      NACHTRAG 27.08.2026: behoben, dieser Absatz ist ueberholt - die Wache
      leitet ihre Quellen seither aus `git ls-files` ab statt aus einer
      Namensliste, und der Fund lag prompt darin
      ([[wache-liest-sieben-von-neunundvierzig-prosadateien]]).
      `tools/doku-pfade-pruefen.py` haelt eine Liste von SIEBEN Handbuechern.
      Die zehn `themes/*/Readme.md`, die vier Anleitungen unter
      `documentation/` (das ENGLISCH benannte zweite Doku-Verzeichnis, zuletzt
      14.08.2026 angefasst), `remote-step-installer/{README,ARCHITECTURE}.md`,
      `scripts/librespot/README.md`, `src/backend-player/README.md` und
      `tools/IDEEN.md` stehen nicht darauf und werden von keiner Wache
      gelesen. Auf tote Wege abgesucht (Handprobe dieses Laufs): sauber, bis
      auf Prosa-Artefakte und die Audit-Momentaufnahmen, die alte Namen
      nennen DUERFEN. `themes/README.md` besteht aus zwei nackten Zahlen
      ("102", "131") und einem auskommentierten `ln -s` auf
      `~/.mupibox/Sonos-Kids-Controller-master/www/styles.242c97d50a9a860d.css`
      - ein Vor-Fork-Rest. Kein Loch mit Folgen, aber der naechste Lauf, der
      die Liste auf die SORTE "Prosadatei im Baum" umstellt, findet es dort.

      GEGENPROBEN GEFAHREN (alle drei, [[gegenprobe-statt-gruen-glauben]]):
      Adressen aus dem neuen Readme entfernt -> 4 rot; `Font:`-Zeile aus
      `themes/mystic/Readme.md` entfernt -> 1 rot; die Endungen der Wache auf
      `*.gibtsnicht` gedreht -> WARNUNG statt gruen. Erste Fassung der
      dritten Probe schlug fehl, weil das sed-Muster nicht traf und die Wache
      unveraendert gruen meldete - eine Gegenprobe, deren Sabotage nicht
      greift, sieht aus wie eine bestandene.
    related: ["entwicklerabbild-ist-die-siebte-sorte-datei", "simulationsrezept-ist-die-sechste-sorte-datei", "dauerrote-wache-ist-keine", "gegenprobe-statt-gruen-glauben", "eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen"]

  - id: wache-liest-sieben-von-neunundvierzig-prosadateien
    kind: falle
    title: "Eine Wache mit zwei Namenslisten hat zwei Blindstellen - und die eine zu verbreitern heilt die andere nicht"
    match: "doku-pfade-pruefen|Handbuecher|HANDBUECHER|OBERSTE|MODERNIZATION|toter Pfad|stil.css|Prosadatei|Namensliste|Ableitung"
    sev: med
    cause: "tools/doku-pfade-pruefen.py fuehrte ZWEI von Hand gepflegte Listen - sieben Quelldateien (von 49 getrackten .md) und neun oberste Ordner. Beide wuchsen je Fund um einen Eintrag, also genau dort, wo schon einmal jemand hingesehen hatte"
    fix: "Beide Listen aus git ls-files ableiten statt fuehren; jede Ableitung braucht einen eigenen Riegel (KERN/KERN_ORDNER), weil der vorhandene Wachhund 'kein einziger Pfad gefunden' den Ausfall EINER Quelle nicht sieht"
    tags: [doku, wache, ausschlussliste, ableitung, gegenprobe, sorte]
    body: |
      ══ DIE AUSGANGSLAGE ═══════════════════════════════════════════════════
      `tools/doku-pfade-pruefen.py` stellt die Gegenfrage zu allen anderen
      Wachen: gibt es noch, worauf die Doku zeigt? Sie tat das fuer eine
      Liste von SIEBEN Dateien - erst drei, dann fuenf, dann sieben, je Lauf
      um den Namen erweitert, in dem der letzte Fund lag. Im Baum liegen 49
      getrackte `.md`: 14 % Deckung, und zwar die 14 %, in denen schon einmal
      jemand nachgesehen hatte. Der Vorlauf hatte das als Nebenbefund notiert
      ([[die-binaerdatei-hat-keinen-bezeichner-zum-greppen]]) - dieser
      Eintrag loest ihn ein.

      ══ FUND 1: MODERNIZATION.md 8, die DRITTE Stelle desselben Satzes ═════
      `MODERNIZATION.md` ist der Tier-A-Einstieg fuer jeden Neuzugang und
      stand nie auf der Liste. In Abschnitt 8 ("Decisive code references")
      fuehrte es die Wiedergabe-Maschine als
      `src/backend-player/src/spotify-control.js` samt `mplayer-wrapper.js`
      und `parsers.js` - waehrend Abschnitt 9 DERSELBEN DATEI den Umbau auf
      `.ts` als erledigt und in der Tier-A-Simulation nachgemessen meldet.

      Zwei Laeufe hatten dieselbe Behauptung schon korrigiert: einer in der
      Tabelle in 2, der naechste im ASCII-Architekturbild in 1
      ([[korrektur-endet-am-code-block]]). Abschnitt 8 ist die dritte und die
      folgenreichste - es ist der Kasten, in dem jemand nachsieht, WO DER
      CODE LIEGT. Dieselbe Form wie die Saetze, die an DREI und an SECHS
      Stellen standen: wer nach dem THEMA greppt, sieht jede Datei behandelt.

      ACHTUNG BEIM KORRIGIEREN: auf der Box heisst das ausgelieferte Artefakt
      WIRKLICH `spotify-control.js` (esbuild-Ausgabe, `deploy.zip`,
      `ausliefern.py`). Die Nennungen in `BACKLOG.md`, `ANGLEICH-PI4-PI5.md`
      und `remote-step-installer/README.md` sind darum RICHTIG. Falsch war
      nur, wo von der QUELLE die Rede ist. Ein Name, zwei Schichten -
      dieselbe Frage wie "in welcher Schicht wurde das gemessen?".

      ══ FUND 2: DIE ZWEITE LISTE, UND SIE WAR DIE TEURERE ══════════════════
      Beim Verbreitern der Quellenliste faellt auf, dass sie allein NICHTS
      geheilt haette. Die Wache fuehrt eine zweite Namensliste, `OBERSTE` -
      die obersten Ordner, unter denen ein Pfad ueberhaupt liegen darf. Neun
      Namen. `NewDesign/` (261 getrackte Dateien, die GANZE zweite
      Oberflaeche) war keiner davon, `themes/` (65) auch nicht.

      Der Pack-Eintrag [[laufende-vorschau-teilt-zustand]] schickt seit dem
      03.08.2026 zum Nachmessen in `NewDesign/stil.css`. Die Datei hiess NIE
      so, sie heisst `app.css` (seit 3c59c80f, demselben Commit). Das
      Wissenspaket stand die ganze Zeit auf der Quellenliste und wurde in
      jedem Lauf gelesen - unsichtbar war das ORDNER-PRAEFIX.

      DIE LEHRE, und sie gilt fuer jede Wache mit mehr als einer Liste: die
      gelesene QUELLE beweist nichts ueber den gelesenen PFAD. Wer eine
      Ausschlussliste verbreitert, sieht nach, ob eine zweite daneben liegt,
      die denselben Fund weiter zudeckt. [[wache-sieht-die-schleife-nicht-die-scharf-schaltet]]
      fragte nach dem Preis EINES Ausschlusses; hier sind es zwei, die
      einander decken.

      Zwei kleinere Funde derselben Verbreiterung, beide korrigiert:
      `autosetup/enable_mupihat.sh` im Pack (die Datei liegt unter
      `scripts/mupihat/`), und drei MEHRDEUTIGE in den Analyse-Texten
      (`src/styles.css` -> `src/frontend-admin/src/…`, zweimal
      `tools/mupibox-*` -> `remote-step-installer/tools/…`, die Umzugs-Schuld
      vom 10.08.).

      ══ DIE GEGENPROBE, DIE DIE DRITTE LUECKE FAND ═════════════════════════
      Eine NAMENSLISTE kann man nicht versehentlich leeren, eine ABLEITUNG
      schon. Die Gegenprobe (Endung auf `*.gibtsnicht` gedreht, also KEINE
      Prosadatei mehr in der Liste) meldete weiter "KEINE LUECKE": das
      Wissenspaket allein liefert genug Pfade, damit der vorhandene Wachhund
      (`geprueft == 0`) schweigt. Der Ausfall der ganzen zweiten Quelle waere
      unsichtbar gewesen. Dasselbe noch einmal eine Liste weiter: `OBERSTE`
      auf die leere Menge gedreht - wieder gruen, es blieben ein paar
      `server/…`-Treffer uebrig, und eine Handvoll genuegt dem Wachhund.

      Seither zwei eigene Riegel (`KERN`, `KERN_ORDNER`): die sechs Ablagen
      und die sechs Ordner, die es seit Jahren gibt, muessen in der Ableitung
      ANKOMMEN. ABSICHTLICH ohne Mindestzahl - eine festgenagelte Zahl waere
      beim naechsten neuen README falsch
      ([[tests-duerfen-keine-zahlen-festnageln]]). MERKSATZ: **jede
      Ableitung braucht ihren eigenen Riegel; der Wachhund der Nachbarliste
      faengt sie nicht.** [[gegenprobe-statt-gruen-glauben]] - hier hat die
      Gegenprobe nicht die Korrektur bestaetigt, sondern eine dritte Luecke
      aufgedeckt, die ohne sie mit eingebaut worden waere.

      ══ WAS DIE VERBREITERUNG KOSTET, UND WARUM ES GEMELDET WIRD ═══════════
      Die `AUDIT-*.md` bleiben DRAUSSEN. Sie sind Momentaufnahmen mit Datum
      im Namen, nennen Vorschlaege (`tools/wachen_lib.py`), Zwischenstaende
      und Pfade, deren Abwesenheit ihr Befund IST. Nachgemessen: 27 der 30
      toten Pfade der ersten breiten Fassung standen in ihnen
      ([[dauerrote-wache-ist-keine]]). Die Wache DRUCKT in jedem Lauf, wie
      viele sie uebersprungen hat - ein stiller Ausschluss liest sich sonst
      wie Deckung.

      Drei Fehlalarm-Klassen, erst durch die Verbreiterung sichtbar:
      1. EIN SCHRAEGSTRICH IN PROSA HEISST OFT "ODER". `autosetup/update`
         sind zwei Ausrollwege, `config/sysinfo/diagnose/shutdown` eine
         Aufzaehlung von SSE-Wegen, `src/dest/mode` drei Rezept-Schluessel,
         `config/templates-Vorlagen` ein deutsches Kompositum. Eine
         allgemeine Regel gibt es NICHT - `tools/archiv` ist ebenso
         endungslos und zu Recht gemeldet -, also je Fall mit Begruendung.
      2. DERSELBE NAME OBEN IM BAUM UND UNTEN AUF DER BOX. Unter `config/`
         liegen im Baum genau drei Ordner; alles andere, was die Doku
         `config/…` nennt, ist die LAUFENDE Ablage der Box. Zehn tote und
         vier "mehrdeutige" auf einen Schlag, kein einziger davon ein Fund -
         der mehrdeutige Zweig zeigte sogar auf `src/backend-api/config/…`,
         den Ort, den der Leser gerade NICHT meint. Ein Ordnername im Baum
         belegt nicht, dass ein Pfad darunter einen Ort im Baum meint.
      3. Umbruch (`tools/eltern-masse-` + Zeilenende), Klammer-Aufzaehlung
         (`…mupibox-{server,player}.service`), Platzhalter (`…-<ISO>.json`)
         und der Verzeichnis-Schraegstrich (`media/nachrichten/`) - was das
         Muster ABSCHNEIDET, sieht aus wie ein toter Pfad.

      Und wieder zugeschlagen: der neue Doku-Abschnitt 7.11 muss die vier
      toten Pfade WOERTLICH nennen, um den Fund zu beschreiben - die Wache
      fiel prompt darueber. Ausnahme je QUELLE und je PFAD, nie pauschal fuer
      die Datei ([[doku-zeigt-auf-dateien-die-es-nicht-gibt]]).

      Doku: `dokumentation/mixpibox.md` 7.11 (neu), 7.10 mit Nachtrag;
      `themes/README.md` neu geschrieben (zwei nackte Zahlen von 2022 und ein
      auskommentierter Vor-Fork-`ln -s` - der Nebenbefund des Vorlaufs).
    related: ["die-binaerdatei-hat-keinen-bezeichner-zum-greppen", "korrektur-endet-am-code-block", "wache-sieht-die-schleife-nicht-die-scharf-schaltet", "gegenprobe-statt-gruen-glauben", "dauerrote-wache-ist-keine", "eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen", "doku-zeigt-auf-dateien-die-es-nicht-gibt", "laufende-vorschau-teilt-zustand"]
  - id: verweis-ins-gedaechtnis-steht-ueberall-nur-nicht-im-paket
    kind: falle
    title: "Die Wache gegen Gedaechtnis-Slugs prueft nur pack.yaml - dieselben Slugs stehen in 288 Kommentaren daneben"
    match: "llmwiki:|pack-verweise|Gedaechtnis|memory/|toter Verweis|Kennung|slug|Verweis ins Leere"
    sev: med
    cause: "tools/pack-verweise.py wurde gebaut, weil Dateinamen aus dem privaten Gedaechtnisordner (~/.claude/.../memory/) als Wiki-Kennungen geschrieben wurden. Es liest aber NUR llmwiki/pack.yaml. Die Schreibweise `(llmwiki: kennung)` steht laengst in Quelltextkommentaren, Tests, Werkzeugkoepfen, Rezepten und Handbuechern - 288 Nennungen, keine davon je nachgeschlagen"
    fix: "tools/wiki-verweise-im-code-pruefen.py haelt jede Nennung ausserhalb von llmwiki/ gegen entries[].id und haengt in tools/doku-luecken-probe.sh. Umbrochene Kennungen (Bindestrich am Zeilenende) werden vor dem Urteil zusammengefuegt, einwortige Nennungen gelten als Prosa"
    tags: [doku, wache, wiki, verweis, gedaechtnis, kommentar]
    body: |
      ══ DIE LAGE ═══════════════════════════════════════════════════════════
      [[pack-verweise-werkzeug]] gibt es seit dem 08.08.2026, und sein Kopf
      benennt die Ursache schon damals richtig: `vorschau-wird-geliehen` und
      `wiki-zustand-hat-haltbarkeit` sind DATEINAMEN aus dem Gedaechtnis-
      ordner, keine Wiki-Kennungen. Zwei Wissensspeicher, EINE
      Verweis-Schreibweise.

      Die Wache prueft `related`-Ziele INNERHALB von pack.yaml. Gemessen am
      27.08.2026 stehen ausserhalb davon 288 Nennungen derselben Form -
      `(llmwiki: \`kennung\`)` in Kommentaren, Tests, Werkzeugkoepfen,
      Rezepten und Handbuechern. Neunzehn zeigten ins Leere.

      ══ DER SCHADEN IST NICHT „EIN LINK GEHT NICHT" ════════════════════════
      Der Satz davor BEHAUPTET etwas und nennt einen Beleg. Wer das Paket
      holt, nach der Kennung greppt und nichts findet, hat zwei Lesarten: der
      Beleg wurde geloescht, oder es gab ihn nie. Beide fuehren dazu, die
      Behauptung fuer unbelegt zu halten.

      In sechs Faellen war sie das AUCH. Diese Kennungen sind woertlich
      Dateinamen aus dem privaten Gedaechtnisordner:

          gruene-probe-deckt-nur-ihre-wachen    3x  ci-deckung.py, skriptweg-*
          was-kostet-das-grep-v-dieser-wache    1x  doku-pfade-pruefen.py
          verweis-wache-las-nur-verfolgtes-und-verfehlte-die-neue-datei        1x  schrift-herkunft-pruefen.py
          tests-fahren-nichts-herunter          1x  src/backend-api/src/server.ts
          git-add-schleppt-fremdes-mit          1x  ungerufene-wachen.py
          eltern-bereich-fernsteuern-geht-nicht 1x  bildwahl-gruppen-schau.mjs

      Die Lektion liegt in einem Ordner, den ausser dem Schreiber niemand
      lesen kann - und der Kommentar verkauft sie als geteiltes Wissen.

      ══ ZWEI SORTEN FIX, UND DIE UNTERSCHEIDUNG IST DER PUNKT ══════════════
      Nicht jeder tote Verweis wird umgebogen. Wo ein Eintrag dieselbe
      Lektion traegt, wird umgehaengt (soloist-knoten ->
      [[soloist-erste-messung]], stillgelegtes-repo-lebt-weiter ->
      [[stillgelegtes-repo-lebt-im-fetch-cache-weiter]], die drei
      gruene-probe-Stellen -> [[ungerufene-wache-driftet-in-den-fehlalarm]]
      bzw. [[wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm]]).

      Wo KEINER sie traegt, kommt die Quellenangabe WEG und der Satz sagt die
      Sache selbst. Einen Beleg auf einen ungefaehr passenden Eintrag zu
      biegen macht den Satz subtil falsch - schlimmer als gar kein Beleg.

      Ein Sonderfall war `llmwiki \`sektionen-deckung\`` in
      simulationsrezept-pruefen.py: das ist ein WERKZEUGNAME
      (tools/sektionen-deckung.py), kein Wiki-Eintrag. Zwei Namensraeume,
      eine Schreibweise, dritter Fall derselben Verwechslung.

      ══ DIE ZWEITE KLASSE: DER ZEILENUMBRUCH ═══════════════════════════════
      Neun weitere Nennungen sahen tot aus und waren es nicht:

          // (llmwiki `vorgabewert-ueberlebt-den-umzug-
          // seines-ordners`). Deshalb wird der Ordner …

      Die Kennung stimmt, sie ist nur ungreppbar. [[pack-verweise-werkzeug]]
      kennt diese Falle seit dem 23.08.2026 - INNERHALB von pack.yaml. Wer
      die Wache auf den Rest des Baums ausdehnt, muss die Lehre mitnehmen,
      sonst meldet sie neun Fehlalarme und wird zu Recht ignoriert
      ([[dauerrote-wache-ist-keine]]).

      ══ DIE WACHE SCHRIEB SICH SELBST IN DIE FALLE ═════════════════════════
      Der bemerkenswerteste Teil: DREI der neunzehn toten Verweise entstanden
      im selben Lauf, in der Kopfzeile des Werkzeugs, das eine Stunde vorher
      gegen eine ANDERE Luecke gebaut worden war
      (tools/zitierte-schalter-pruefen.py). Aus dem Gedaechtnis zitiert,
      ohne Gegenprobe gegen pack.yaml - genau die Bewegung, die der Eintrag
      beschreibt, waehrend er beschrieben wurde.

      Das ist der Beleg dafuer, dass die Regel „IDs einzeln gegen pack.yaml
      pruefen" ohne WACHE nicht haelt. Sie ist bekannt, aufgeschrieben und
      wurde trotzdem im selben Atemzug gebrochen.

      ══ GEGENPROBE ═════════════════════════════════════════════════════════
      `--sabotage` haengt `(llmwiki: \`diese-kennung-gibt-es-nicht\`)` im
      SPEICHER an README.md an; nichts wird auf Platte geschrieben. Sie
      beisst (20 statt 19). Ohne diesen Schritt waere „KEINE LUECKE" nach der
      Richtung nur eine Vermutung ([[gegenprobe-statt-gruen-glauben]]).

      ══ WAS OFFEN BLEIBT ═══════════════════════════════════════════════════
      Die Wache prueft die EXISTENZ der Kennung, nicht ob der Eintrag die
      Behauptung im Satz davor auch traegt. Dagegen hilft nur Lesen - dieselbe
      Grenze wie bei jeder Namenswache
      ([[wache-liest-sieben-von-neunundvierzig-prosadateien]]).

      Und: die sechs Gedaechtnis-Lektionen oben sind damit NICHT ins Paket
      geholt, nur die falschen Belege entfernt. Wer eine davon braucht,
      findet sie weiterhin nirgends.

      ══ „VERWEIS INS LEERE" GIBT ES ZWEIMAL ════════════════════════════════
      Derselbe Satz steht im `match:` von
      [[wiki-doppelte-id-faellt-durch-safe-load]]. Die Frage, die trennt:
      WO steht der tote Verweis?

          INNERHALB von llmwiki/pack.yaml (related-Ziel, `[[kennung]]`)
              -> der andere Eintrag, Wache `tools/wiki-schau.py --pruefen`
          AUSSERHALB, in Code, Test, Werkzeugkopf, Handbuch
              -> DIESER Eintrag, Wache
                 `tools/wiki-verweise-im-code-pruefen.py`

      Zwei Wachen, zwei Reichweiten, ein Symptom. Wer nur die erste laufen
      laesst, hat 288 ungepruefte Nennungen daneben.
    related: [wiki-doppelte-id-faellt-durch-safe-load]
  - id: gegenprobe-der-baumweiten-wache-liegt-im-baum
    kind: falle
    title: "Eine Wache, die den ganzen Baum liest, liest ihre eigene Attrappe mit - und ist ab dem ersten Lauf dauerrot"
    match: "sabotage|Gegenprobe|dauerrot|git ls-files|Attrappe|eigene Datei|Wache liest sich selbst"
    sev: med
    cause: "tools/wiki-verweise-im-code-pruefen.py sammelt seine Dateien per `git ls-files` - also auch sich selbst. Die Attrappe der Gegenprobe stand als EIN Stueck im Quelltext (`(llmwiki: \\`diese-kennung-gibt-es-nicht\\`)`), und das eigene Suchmuster greift darauf zu. Der Fund ist von einem echten nicht zu unterscheiden"
    fix: "Marke und Kennung getrennt halten (MARKE / SABOTAGE_KENNUNG) und erst zur Laufzeit per f-String zusammensetzen. Dann kann NENNUNG nicht ueber das Anfuehrungszeichen greifen; --sabotage beisst weiter. Danach beide Richtungen messen: ohne Schalter gruen, mit Schalter rot"
    tags: [doku, wache, gegenprobe, dauerrot, werkzeug]
    related: [dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben, verweis-ins-gedaechtnis-steht-ueberall-nur-nicht-im-paket, messwerkzeug-muss-sich-selbst-pruefen, pfad-in-die-bauablage-ist-nie-im-baum]
    body: |
      ══ DIE LAGE ═══════════════════════════════════════════════════════════
      Der Doku-Lauf vom 27.08.2026 (c4971722) lieferte
      tools/wiki-verweise-im-code-pruefen.py aus: sie haelt jede Nennung der
      Form `(llmwiki: \`kennung\`)` im ganzen Baum gegen entries[].id in
      pack.yaml ([[verweis-ins-gedaechtnis-steht-ueberall-nur-nicht-im-paket]]).
      Der Lauf eine Stunde spaeter fand sie ROT - mit genau einem Fund:

          TOTER VERWEIS: tools/wiki-verweise-im-code-pruefen.py:151
            → diese-kennung-gibt-es-nicht

      Das ist die Attrappe ihrer EIGENEN Gegenprobe. Sie sammelt ihre Dateien
      per `git ls-files`, liest also auch sich selbst, und ihr Muster trifft
      den Literal im `--sabotage`-Zweig - auch wenn der Zweig gar nicht laeuft.

      ══ WARUM DAS MEHR IST ALS EIN SCHOENHEITSFEHLER ═══════════════════════
      Die Wache haengt in tools/doku-luecken-probe.sh. Deren Schlusszeile ist
      die Zahl, an der ein Lauf entscheidet, ob er etwas zu tun hat. Ab dem
      ersten Lauf stand dort dauerhaft „1 LUECKE(N)" - der naechste echte Fund
      waere in einer Zahl verschwunden, die ohnehin nie null wird
      ([[dauerrote-wache-ist-keine]]). Und der Fund selbst ist von einem echten
      nicht zu unterscheiden: gleiche Zeile, gleiche Form, gleiche Kennung.

      ══ DIE FORM, UND SIE IST UEBERTRAGBAR ═════════════════════════════════
      Eine Gegenprobe braucht eine Attrappe, und die Attrappe ist Text. Solange
      die Wache eine ENGE Menge liest (nur Handbuecher, nur package.json, nur
      Units), liegt ihr Quelltext ausserhalb dieser Menge und nichts passiert.
      Sobald sie den GANZEN Baum liest, liegt sie drin. Der Prueffall wandert
      damit in die Pruefmenge - dieselbe Bewegung wie bei
      [[urteil-steht-in-der-pruefenden-datei]], nur eine Ebene frueher.

      Gegenbeispiele im selben Baum, beide zufaellig heil:
        * tools/doku-zeilenzitate-pruefen.py sabotiert mit einer ZAHL
          (`--sabotage 100000` verschiebt Zeilennummern) - eine Zahl ist kein
          Muster, es gibt nichts, was sich selbst treffen koennte.
        * tools/zitierte-schalter-pruefen.py haengt `bash tools/pruefen.sh
          --gibtsnicht` an - liest aber nur PROSA-Dateien, und ihr eigener
          Quelltext ist keine. Heil aus Zuschnitt, nicht aus Vorsicht: wer
          diese Wache eines Tages auf den ganzen Baum ausdehnt, erbt die Falle.

      ══ FIX ════════════════════════════════════════════════════════════════
      Nicht die eigene Datei ausnehmen - dann verliert die Wache die echten
      Verweise im eigenen Kopf (drei Stueck, und genau dort sind schon einmal
      welche verrottet). Stattdessen die Attrappe so ablegen, dass das eigene
      Muster nicht ueber sie greifen kann:

          MARKE = "llmwiki"
          SABOTAGE_KENNUNG = "diese-kennung-gibt-es-nicht"
          ...
          text += f"\n\nBelegt ist das ({MARKE}: \`{SABOTAGE_KENNUNG}\`).\n"

      `NENNUNG` verlangt hinter der Marke nur `[-:\s]*` - ein
      Anfuehrungszeichen dazwischen bricht den Treffer. Zusammengesetzt wird
      erst zur Laufzeit, die Gegenprobe bleibt vollstaendig.

      ══ GEGENPROBE ═════════════════════════════════════════════════════════
      Zwei Richtungen, sonst beweist gruen nichts
      ([[gegenprobe-statt-gruen-glauben]]):
        ohne Schalter   → KEINE LUECKE, rc=0, 293 Verweise geprueft
        mit --sabotage  → TOTER VERWEIS: README.md → …, rc=1, 294 geprueft
      Die Zahl steigt um genau eins; das belegt, dass die Attrappe wirklich
      ankommt und nicht bloss der Zaehler wackelt.

      ══ MERKSATZ ═══════════════════════════════════════════════════════════
      Nach dem Bau einer baumweiten Wache sie EINMAL ohne Schalter laufen
      lassen und in der Trefferliste nach dem eigenen Dateinamen suchen. Ein
      Werkzeug, das sich selbst meldet, hat entweder recht oder ist kaputt -
      beides muss man wissen, bevor es in eine Sammelprobe gehaengt wird
      ([[messwerkzeug-muss-sich-selbst-pruefen]]).
  - id: ausgerollte-vorlage-ohne-prosa-faellt-nach-oben-durch
    kind: falle
    title: Eine Vorlage, die kein Text erwaehnt, verbessert die Trefferquote der Doku-Wache
      - deshalb faellt sie nie auf
    match: config/templates|wireplumber.conf.d|81-bluez-nur-a2dp|HFP|Freisprech|A2DP.*abgehackt|Vorlage
      nicht dokumentiert|Gegenrichtung
    sev: med
    cause: '`tools/doku-pfade-pruefen.py` prueft die Richtung DOKU -> BAUM: existiert
      jeder Pfad, auf den ein Handbuch zeigt? In dieser Richtung ist eine Datei, die
      NIEMAND erwaehnt, kein Fehler - sie erzeugt keinen toten Zeiger und hebt die
      Quote sogar. Gemessen 27.08.2026: `config/templates/81-bluez-nur-a2dp.conf`
      geht von autosetup.sh:940 und start_mupibox_update.sh:1296 nach
      /etc/wireplumber/wireplumber.conf.d/. NACHTRAG 27.08.2026, spaeterer Lauf:
      der Zusatz "und liegt damit auf jeder Box" ist widerlegt und hier gestrichen -
      es gibt DREI Ausrollwege, und der dritte (remote-step-installer/recipes/,
      laut mixpibox.md §5.1 der Weg heute) legt diese Vorlage nicht ab; eine frisch
      aufgesetzte Box hat sie erst nach dem ersten Update. Siehe
      vereinigung-der-wege-belegt-keine-allgemeinheit. Sie
      nimmt Bluetooth-Geraeten die Freisprech-Rollen (HFP/HSP) weg - ein Lautsprecher
      mit Mikrofon bietet danach keines mehr an, dauerhaft und mit Absicht. Null Treffer
      in dokumentation/, plugins/README.md, README.md, BACKLOG.md und pack.yaml; die
      ganze Begruendung stand nur im Kopf der Vorlage selbst und in zwei Kommentarzeilen
      der Ausrollskripte - also genau dort, wo sie liest, wer die Antwort schon hat.
      Verdeckt wurde es von der Nachbarschaft: die drei Schwestern desselben Kopierblocks
      (80-bluez-ohne-seat.conf, 61-entzerrer.conf, 62-mixpi-mitschnitt.conf) stehen
      alle in Prosa. Eine Aufzaehlung leiht dem fehlenden Eintrag die Glaubwuerdigkeit
      seiner Nachbarn. Kein Handbuch nannte WirePlumber ueberhaupt (grep: 0 Treffer
      in dokumentation/*.md, *.html, plugins/README.md), obwohl die conf.d-Schicht
      die einzige Stelle ist, an der sich diese Einstellungen aendern lassen - keine
      Verwaltungsseite bietet sie an'
    fix: 'Jede zaehlende Wache braucht ihre Gegenrichtung - dieselbe Regel wie bei
      api-doku-deckung.sh. Seit 27.08.2026 `tools/ausgerollte-vorlagen-deckung.py`
      (in doku-luecken-probe.sh): sie sammelt jede `${MUPI_SRC}/config/templates/<name>`-Kopierzeile
      der beiden Ausrollwege und haelt den DATEINAMEN gegen die Prosa-Ablagen. Vier
      Zuschnitt-Entscheidungen, alle uebertragbar: (1) die Grundmenge ist der AUSROLLWEG,
      nicht `ls config/templates/` - dort liegen auch Vorlagen, die kein Weg kopiert
      (proftpd.conf, mqtt-discovery.json, asound.conf_mono, offene Entscheidung seit
      AUDIT-2026-08-24 §7); sie mitzuverlangen machte die Wache dauerrot fuer etwas
      Gewolltes. (2) Ein Treffer im Ausrollskript selbst zaehlt NICHT als Beleg, sonst
      belegt die Wache ihren Gegenstand mit sich selbst. (3) Auskommentierte Kopierzeilen
      sind kein Ausrollen (der tote librespot.conf-Verweis in start_mupibox_update.sh:974
      wuerde sonst als Luecke gemeldet). (4) Die Ausnahme steht im REZEPT, nicht in
      der Wache: `# UNDOKUMENTIERT MIT GRUND:` ueber der Kopierzeile, dann meldet sie
      VERMERKT. Doku nachgezogen an beiden Enden: `dokumentation/mixpibox.md` §4.5
      bekam eine Tabelle der drei Tonstapel-Vorlagen samt Ziel auf der Box, das Benutzerhandbuch
      einen dritten Punkt bei "sieht aus wie ein Fehler, ist keiner" - denn fuer den
      Betreiber ist der Befund nicht "eine Datei fehlt in der Doku", sondern "mein
      Lautsprecher hat ein Mikrofon und die Box nimmt es nicht"'
    refs:
    - config/templates/81-bluez-nur-a2dp.conf
    - tools/ausgerollte-vorlagen-deckung.py
    - dokumentation/mixpibox.md
  - id: vereinigung-der-wege-belegt-keine-allgemeinheit
    kind: falle
    title: Eine Wache, die mehrere Ausrollwege zusammenwirft, kann "auf jeder Box"
      nie belegen - nur "irgendwo kopiert es jemand"
    match: auf jeder Box|Ausrollweg|remote-step-installer|81-bluez-nur-a2dp|61-entzerrer|Vereinigung|frisch
      aufgesetzt|Weg heute|Durchschnitt der Wege
    sev: high
    cause: 'Gemessen 27.08.2026 (spaeterer Lauf). `tools/ausgerollte-vorlagen-deckung.py`
      war eine Stunde alt und fuehrte ZWEI Ausrollwege (autosetup/autosetup.sh,
      update/start_mupibox_update.sh). Die Begruendung fuer das Weglassen des dritten
      stand als Kommentar in der Wache: der Installer "fuehrt seine Dateien in einem
      YAML-Rezept statt in einer Kopierzeile". Das ist eine Aussage ueber die
      SCHREIBWEISE, keine ueber den Gegenstand - und genau dieser Weg ist laut
      dokumentation/mixpibox.md §5.1 der Weg HEUTE. Die Wache mass also die
      Vergangenheit und ueberschrieb ihre Trefferliste zugleich mit "Vorlagen, die auf
      jede Box gehen". Nachgemessen: das Rezept legt 7 von 14 ausgerollten Vorlagen
      NICHT ab, darunter 81-bluez-nur-a2dp.conf (die A2DP-Einschraenkung) und
      61-entzerrer.conf (der Equalizer). Der Satz "liegt damit auf jeder Box" war
      damit in DREI Ablagen falsch: mixpibox.md §4.5, benutzerhandbuch.html (in
      Betreiber-Sprache: "Die Einschraenkung gilt auf jeder Box") und im Pack selbst.
      Der Kern ist eine Mengenoperation, kein Tippfehler: die VEREINIGUNG mehrerer
      Wege belegt Existenz ("irgendein Weg legt die Datei"), die Allgemeinheit
      ("jede Box hat sie") braucht den DURCHSCHNITT. Eine Wache, die Wege
      zusammenwirft, kann den zweiten Satz strukturell nicht pruefen - und wird
      trotzdem als Beleg fuer ihn zitiert'
    fix: 'Bei jeder Wache ueber mehrere Wege/Quellen fragen, ob ihre Aussage eine
      EXISTENZ- oder eine ALLGEMEINHEITSAUSSAGE traegt, und die Ueberschrift danach
      waehlen. Seit 27.08.2026 liest die Wache alle vier Rezept-/Skriptwege und meldet
      einen eigenen Abschnitt "Nicht auf einer heute frisch aufgesetzten Box (erst
      nach dem ersten Update)" - bewusst KEIN Fehler, weil 7 von 14 so sind und das
      dauerrot waere (dauerrote-wache-ist-keine), aber jeder Satz "auf jeder Box"
      ueber eine dieser Vorlagen ist damit widerlegbar. Drei Fallen beim Erweitern,
      alle uebertragbar: (1) die Variable heisst nicht ueberall gleich - das Rezept
      schreibt `${MUPI_REPO:-$HOME/Downloads/MuPiBox}/config/templates/`, das Muster
      war auf `${MUPI_SRC}` festgenagelt; wer den Weg einhaengt und das Muster laesst,
      liest ihn und findet garantiert nichts. (2) Derselbe Inhalt liegt unter einem
      anderen QUELLPFAD: der Installer traegt 80-bluez-ohne-seat.conf in seinem
      eigenen Baum (remote-step-installer/tools/wireplumber/, Wort fuer Wort
      identisch, diff leer) - wer nur nach dem Quellordner sucht, meldet sie
      faelschlich als "geht den heutigen Weg nicht"; fuer die Frage WELCHER Weg eine
      Vorlage traegt, zaehlt der NAME im Ziel. (3) Zwei Wege koennen dieselbe Datei
      mit GEGENLAEUFIGER Absicht anfassen: beide setzen /boot/cmdline.txt, der
      Monolith schiebt die Konsole auf tty3 mit loglevel=0 (gar keine Meldungen), das
      Rezept laesst console=tty1 absichtlich stehen und nimmt nur quiet/loglevel=3 -
      "beide Wege fassen X an" ist deshalb keine Auskunft, erst die Absicht ist eine'
    refs:
    - tools/ausgerollte-vorlagen-deckung.py
    - remote-step-installer/recipes/mupibox.yaml
    - dokumentation/mixpibox.md
    - dokumentation/benutzerhandbuch.html
    related: [mupi-ausliefern-nur-vier-dinge]
    body: |
      „Ausrollweg" STEHT IN ZWEI EINTRAEGEN. Die eine Frage, die trennt:
      WIRD EINE DATEI VERMISST ODER EIN SATZ GEPRUEFT?

          Meine Aenderung wirkt auf der Box nicht — welcher Weg traegt
          sie ueberhaupt?          -> [[mupi-ausliefern-nur-vier-dinge]]
                                      (ausliefern.py kennt VIER Ziele,
                                      `scripts/` ist keines davon)

          Irgendwo steht „liegt auf jeder Box" und stuetzt sich auf eine
          Wache ueber mehrere Wege -> DIESER Eintrag (die Vereinigung
                                      belegt Existenz, nie Allgemeinheit)

      Der eine Eintrag beantwortet „wohin kopiert wer?", der andere
      „was darf man aus der Antwort schliessen?". Wer beim ersten landet
      und den zweiten nicht kennt, haelt eine Aufzaehlung von Wegen fuer
      eine Aussage ueber jede Box.
  - id: eingriff-in-fremde-datei-hat-keinen-namen
    kind: falle
    title: Ein Ausrollweg legt nicht nur ab, er greift ein - und ein Eingriff hat
      keinen eigenen Namen, nur ein Ziel
    match: sed -i|tee -a|noplugin=sap|bluetooth.service|pam.d/login|cmdline.txt|Xwrapper|modules-load|Eingriff|fremde
      Datei|dpkg ueberschreibt
    sev: med
    cause: 'Gemessen 27.08.2026. Die Wache des Vorlaufs prueft, was ein Ausrollweg
      auf die Box LEGT (eine Vorlage aus config/templates/). Ein Ausrollweg aendert
      aber auch fremde Dateien an Ort und Stelle: `sed -i` in eine Datei des
      Betriebssystems, `tee -a` an die Firmware-Konfiguration, `>>` an eine
      Modulliste. Das ist dieselbe Frage eine Datei-SORTE weiter, und diese Sorte ist
      die unsichtbarere: eine Vorlage hat einen eigenen NAMEN (etwas zum Greppen, eine
      Zeile fuer die Tabelle "Vorlage | Ziel | was sie festlegt"), ein Eingriff hat nur
      ein ZIEL. Er faellt nicht durch das Raster - er passt nicht hinein. Nachgemessen:
      21 Eingriffe in den Ausrollwegen, elf davon mit null Treffern in dokumentation/,
      plugins/README.md, README.md, BACKLOG.md und pack.yaml. Der teuerste:
      autosetup.sh:700 haengt --noplugin=sap an das ExecStart= in
      /lib/systemd/system/bluetooth.service. Direkt daneben in mixpibox.md §4.5 stand
      die Tabelle "Was die Ausrollwege in den Tonstapel legen" mit drei Vorlagen - die
      vierte Einstellung desselben Stapels fehlte, weil sie keine Vorlage ist. Zwei
      Eigenschaften machen den Eingriff zusaetzlich fluechtig: die Datei gehoert dem
      PAKET bluez (kein conffile, ein Upgrade stellt sie ohne Rueckfrage wieder her,
      und der Eingriff ist still weg - waehrend dieselbe Box fuer librespot laengst ein
      Drop-in unter /etc/systemd/system/librespot.service.d/ benutzt), und KEIN
      anderer Weg traegt ihn nach. Eine Vorlage ueberschreibt der Update-Weg einfach
      neu; ein Eingriff muss eigens wiederholt werden'
    fix: 'Seit 27.08.2026 `tools/ausgerollte-eingriffe-deckung.py` (in
      doku-luecken-probe.sh): sammelt sed -i / tee -a / >> auf Ziele ausserhalb des
      eigenen Baums und haelt den ZIELPFAD gegen die Prosa. Zuschnitt: (1) Beleg ist
      der VOLLE Pfad, nicht der Dateiname - bei einer Vorlage ist der Name der
      Handgriff, bei einem Eingriff der Ort, und die Basisnamen dieser Sorte sind
      reihenweise generisch (config.toml, daemon.conf, client.conf, config.txt,
      login); wer sie als Beleg zaehlt, meldet gruen fuer jede gleichnamige Datei im
      Baum. (2) Pfadvariablen werden aus dem Weg selbst aufgeloest und koennen MEHRERE
      Werte haben (BOOT_CONFIG ist je nach Systemgeneration /boot/config.txt oder
      /boot/firmware/config.txt) - ein Beleg fuer einen Wert reicht. (3) Das
      Protokoll-Umlenken `>&3 2>&3` haengt an fast jeder Zeile und wird sonst als Ziel
      gelesen. (4) Ausnahme im Rezept, nicht in der Wache: `# UNDOKUMENTIERT MIT
      GRUND:` in die Zeile darueber. Doku nachgezogen: mixpibox.md §5.3 als Tabelle
      mit Spalte "Weg" (nur Monolith / beide / alle), §4.5 verweist fuer die
      Tonstapel-Eingriffe dorthin. Nebenbefund derselben Messung: drei sed-Zeilen
      richten /etc/pulse/{daemon,client}.conf ein, und 280 Zeilen spaeter entfernt
      DERSELBE Weg PulseAudio - Eingriffe altern unbemerkt, weil sie keinen Namen
      haben, an dem eine Wache oder ein Leser haengenbleibt'
    refs:
    - tools/ausgerollte-eingriffe-deckung.py
    - autosetup/autosetup.sh
    - dokumentation/mixpibox.md
  - id: abgeschriebene-wegeliste-erbt-die-blindheit
    kind: falle
    title: Eine Wache, die ihre Quellenliste von der Schwesterwache ABSCHREIBT,
      erbt deren Blindheit - und meldet gruen, weil sie die Haelfte nicht ansieht
    match: Wegeliste|WEGE|Schwesterwache|abgeschrieben|importiert|ausgerollte-eingriffe-deckung|ausgerollte-vorlagen-deckung|/etc/hosts|dietpi-autologin|--wege|erbt
    sev: high
    cause: 'Gemessen 27.08.2026 (Doku-Lauf danach). `tools/ausgerollte-eingriffe-deckung.py`
      hatte ihre Liste der Ausrollwege aus `tools/ausgerollte-vorlagen-deckung.py`
      kopiert, als die noch ZWEI Wege kannte (autosetup.sh, start_mupibox_update.sh).
      Eine Stunde spaeter lernte die Schwester, dass es VIER sind - die beiden
      Installer-Rezepte sind der Weg, den eine HEUTE frisch aufgesetzte Box geht
      (siehe vereinigung-der-wege-belegt-keine-allgemeinheit). Die Kopie blieb bei
      zwei und meldete weiter "KEINE LUECKE". Sie war nicht gruen, weil nichts fehlte,
      sondern weil sie die Haelfte nicht ansah: verbreitert auf alle vier Wege standen
      statt 26 dann 47 Eingriffe zur Pruefung, und die Tabelle mixpibox.md §5.3, die
      aus der schmalen Wache entstanden war, hatte VIER fehlende Zeilen (/etc/hosts,
      ${BOOT_DIR}/dietpi.txt, /etc/dhcp/dhclient.conf,
      /var/lib/dietpi/dietpi-software/installed/chromium-autostart.sh) und EINE mit
      falschem Weg: dietpi-autologin.conf stand als "nur Monolith" da, obwohl das
      Rezept dieselbe Datei neu schreibt - mit ANDEREM Inhalt (Monolith:
      agetty --skip-login; Rezept: schlichtes -a dietpi). Die Konsole verhaelt sich
      also je nach Weg anders, und genau das verdeckte die Tabelle. Der teuerste
      Einzelfund war /etc/hosts: das Rezept zieht die 127.0.1.1-Zeile auf den neuen
      Rechnernamen nach, weil `hostnamectl` nur /etc/hostname schreibt - ohne das
      haengt jedes sudo erst einmal'
    fix: 'Die Liste steht seit 27.08.2026 NICHT MEHR in der Kopie: die Wache laedt
      WEGE/WEGE_HEUTE/PROSA per importlib aus der Schwesterwache, eine Korrektur
      heilt damit beide. Vier Fallen beim Verbreitern, alle beim Nachmessen
      aufgeschlagen und alle uebertragbar: (1) Das Muster fuer `sed -i` muss die Zeile
      POSITIONELL lesen, nicht absuchen - der sed-AUSDRUCK sieht selbst wie ein Pfad
      aus (`sed -i -E "/vc4-fkms-v3d/d" "$CFG"`), und ein absuchendes Muster holte
      /bluetoothd/ExecStart, /motd.dynamic/g und /ALGO als angebliche Ziele heraus:
      18 Funde, von denen zwei echt waren. (2) Der Befehl steht auch mit vollem Pfad
      da (`/usr/bin/sed -i` in start_mupibox_update.sh:625) - wer auf das WORT sed
      prueft, verliert die Zeile und meldet dann "nur Monolith" fuer eine Datei, die
      beide Wege anfassen. (3) "Diese Datei hat der Schritt selbst angelegt" braucht
      eine GELTUNGSGRENZE: im Rezept ist es der Schritt, im Monolithen gibt es keine,
      dort muss die Naehe (10 Zeilen) begrenzen - sonst deckt ein `>` am Dateianfang
      jeden Eingriff bis zum Dateiende zu. Und `curl -o` zaehlt NICHT als Anlegen:
      der Inhalt kommt von fremder Seite, das Skript patcht ihn nur. (4) Fuer die
      Frage "hat eine frisch aufgesetzte Box das?" zaehlt ANLEGEN genauso wie
      EINGREIFEN - wer nur Eingriffe zaehlt, meldet /etc/X11/Xwrapper.config als
      "nicht auf einer frischen Box", obwohl das Rezept sie vollstaendig hinschreibt.
      Dieselbe Mengenfrage wie beim Vorlauf, eine Ebene weiter innen. Neu ausserdem
      `--wege`: listet je Zieldatei die Wege, die sie anfassen - die Spalte "Weg" der
      Tabelle in §5.3 ist damit gemessen statt fortgeschrieben, und die Doku sagt
      das auch dazu. Die Gegenprobe (Zeile aus der Prosa loeschen) schlug beim ersten
      Versuch NICHT an: `sed s|…|…|` ersetzt nur den ERSTEN Treffer je Zeile, und die
      Tabellenzeile nennt /etc/hosts zweimal - eine Gegenprobe muss die GANZE Zeile
      treffen'
    related:
    - vereinigung-der-wege-belegt-keine-allgemeinheit
    - eingriff-in-fremde-datei-hat-keinen-namen
    refs:
    - tools/ausgerollte-eingriffe-deckung.py
    - tools/ausgerollte-vorlagen-deckung.py
    - remote-step-installer/recipes/mupibox-app.yaml
    - dokumentation/mixpibox.md
  - id: kopfzeilenzahl-driftet-weil-die-wache-vor-dem-anhaengen-laeuft
    title: "Die Kopfzeilenzahl war neun Commits am Stueck veraltet - die Wache laeuft, bevor der Lauf seinen eigenen Eintrag anhaengt"
    kind: architektur
    match: "VERALTET in dokumentation/mixpibox.md|dort .* Eintraege/Fassung|Umfang des Wissenspakets in der Kopfzeile|kopfzeile-nachziehen|Kopfzeile sagt .* das Paket hat"
    sev: mid
    tags: [wachen, doku, wiki, kopfzeile, abgeleitete-zahl, doku-lauf, 2026]
    stand: "27.08.2026 gefunden und behoben; wiki-anhaengen.py zieht die Kopfzeile jetzt selbst nach, Gegenprobe in beide Richtungen gelaufen"
    source: "Stuendlicher Doku-Lauf; tools/doku-luecken-probe.sh meldete 1 Luecke — dieselbe wie in den neun Laeufen davor"
    body: |
      EINE WACHE, DIE IN JEDEM LAUF DENSELBEN BEFUND MELDET, MELDET NICHT DEN
      FEHLER — SIE MELDET IHRE EIGENE STELLUNG IM ABLAUF.

      ══ DER BEFUND ═════════════════════════════════════════════════════════

      Die Doku-Luecken-Probe meldete genau eine Luecke:

        VERALTET in dokumentation/mixpibox.md: dort 855 Eintraege/Fassung 357,
        wirklich 856/358

      Das sieht nach einem vergessenen Handgriff aus. `git log -p` auf die
      Kopfzeile zeigt aber, dass es keiner ist — allein am 27.08. schob jeder
      von NEUN Commits hintereinander die Zahl um genau eins weiter:

        c2c41dc0  843 -> 845     19f07c9f  849 -> 850
        f65f9bb8  845 -> 846     d2a27736  851 -> 852
        d1d27c40  846 -> 847     8f80bbca  852 -> 853
        e3d5d95f  847 -> 848     ce6c11bb  853 -> 855
        003a7c34  848 -> 849     280d245c  (gar nicht) -> blieb 855

      Neun Laeufe, neun Korrekturen, und der zehnte fand sie wieder veraltet.

      ══ WARUM SIE NIE GRUEN BLEIBEN KANN ═══════════════════════════════════

      Die Wache ist nicht kaputt. Sie steht nur hinter dem Ereignis, das sie
      pruefen soll:

        Lauf N  : Probe laeuft   -> gruen
                  Lauf haengt seinen Wiki-Eintrag an
                  -> Paket hat N+1 Eintraege, Kopfzeile sagt weiter N
        Lauf N+1: Probe meldet die Luecke, die Lauf N erzeugt hat

      DER DOKU-LAUF ERZEUGT SEINEN EIGENEN NAECHSTEN BEFUND. Der Commit, der
      die Kopfzeile ungueltig macht, passiert NACH der Wache, die sie prueft
      — sie kann ihn prinzipiell nicht mehr sehen. Von Hand ist das nicht
      loesbar: wer die Zahl vor dem Anhaengen setzt, setzt sie falsch; wer sie
      danach setzt, muss daran denken, und neun Mal hintereinander hat das
      genau ein Mal geklappt.

      Dazu kommt: die Zahl ist ABGELEITET. Sie steht in `llmwiki/pack.yaml`
      selbst und wurde in die Doku abgeschrieben. Eine abgeschriebene
      abgeleitete Zahl ist eine zweite Wahrheit ueber dieselbe Sache — genau
      das, was die Kopfzeile drei Zeilen weiter unten selbst verbietet.

      ══ BEHOBEN AN DER NAHT, NICHT AN DER WACHE ════════════════════════════

      NACHTRAG 27.08.2026 — ueberholt: das Werkzeug hiess damals
      `kopfzeile-nachziehen.py` und kannte genau einen Pfad. Es heisst seit
      demselben Tag `tools/paket-angaben-nachziehen.py` und sucht nach dem
      Merkmal statt nach dem Pfad; der Grund steht in
      [[umfangsangabe-stand-an-drei-orten-die-wache-kannte-einen]]. Alles
      Folgende gilt weiter, nur unter dem neuen Namen und fuer JEDE Datei,
      die die Angabe fuehrt, statt nur fuer die Doku.

      Es LIEST das Paket (per YAML, nicht per grep — `id:` steht dort auf
      zwei Einrueckungen) und schreibt genau die beiden Zahlen. Kein
      `safe_dump` auf das Paket, das die Handformatierung einebnen wuerde.

      `tools/wiki-anhaengen.py` ruft es jetzt selbst auf, sofort nach dem
      erfolgreichen Anhaengen (nicht bei `--pruefen`). WER DIE KOPFZEILE
      UNGUELTIG MACHT, RICHTET SIE AUCH. Damit ist die Wache in der Probe
      nicht mehr die einzige Verteidigung, sondern nur noch die Gegenprobe.

      Drei Ausgaenge, alle gemessen:
        0  stimmt bzw. gerichtet
        1  Drift (nur bei `--pruefen`)
        2  UNMESSBAR — Paket/Doku fehlt oder die Kopfzeile hat ihre Form
           verloren. Bewusst von 1 verschieden: „ich konnte nicht nachsehen"
           ist kein „alles in Ordnung".

      ══ GEGENPROBE ═════════════════════════════════════════════════════════

      Nicht dem Gruen geglaubt, sondern in beide Richtungen gefahren: Zahl auf
      999/Fassung 111 verdreht -> `--pruefen` meldet Drift und geht mit 1;
      Richten setzt zurueck und die Datei ist BYTEGLEICH zur Sicherung (nur
      die zwei Zahlen angefasst, sonst nichts). Kopfzeile in Prosa umgeschrieben
      -> Ausgang 2 statt eines stillen 0.

      Sabotiert wurde dabei auf eine Kopie im Ablagefach, nicht per
      `git checkout`: die Korrektur der Kopfzeile war zu dem Zeitpunkt selbst
      noch uncommittet und waere mit weggeworfen worden.

      ══ WAS DAS FUER DEN NAECHSTEN LAUF HEISST ═════════════════════════════

      Wenn eine Wache in mehreren Laeufen hintereinander DENSELBEN Befund
      meldet, ist die Frage nicht „wer hat das wieder vergessen", sondern:
      LAEUFT SIE UEBERHAUPT AN DER RICHTIGEN STELLE? Ein Befund, der sich
      jedes Mal von selbst neu herstellt, ist ein Ablauffehler, kein
      Schlamperei-Fehler — und er ist an der Naht zu beheben, die den Wert
      erzeugt, nicht an der Wache, die ihn beklagt.

      Der Verdachtsanfang dafuer ist billig: `git log -p` auf die gemeldete
      Zeile. Wiederholt sich derselbe Ein-Schritt-Diff, ist es dieses Muster.
    related: ["wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm", "sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts", "gegenprobe-statt-gruen-glauben", "doku-nennt-den-pfad-und-das-falsche-verb"]
  - id: umfangsangabe-stand-an-drei-orten-die-wache-kannte-einen
    kind: falle
    sev: medium
    title: "Die Umfangsangabe des Wissenspakets stand in drei Dateien — Wache und Naht kannten genau einen Pfad"
    match: "Einträge, Fassung|Umfang des Wissenspakets|kopfzeile-nachziehen|paket-angaben-nachziehen|799 Einträge|777 Einträge|llmwiki_mupibox.*Fassung"
    cause: >
      `tools/kopfzeile-nachziehen.py` (gebaut am 27.08.2026) und die Pruefung
      daneben in `tools/doku-luecken-probe.sh` waren beide auf den Pfad
      `dokumentation/mixpibox.md` verdrahtet. Dieselbe abgeleitete Angabe —
      Eintragszahl und Fassung des Pakets — stand aber an DREI Stellen. Die
      zwei ungewachten liefen auseinander, ohne dass irgendetwas anschlug.
    fix: >
      `tools/paket-angaben-nachziehen.py` (Nachfolger, gleicher Aufrufer
      `tools/wiki-anhaengen.py`) sucht nach dem MERKMAL statt nach dem Pfad:
      jede Prosadatei aus `git ls-files`, die die Form
      "llmwiki/pack.yaml (<Zahl> Einträge, Fassung <Zahl>)" fuehrt. Eine
      vierte Kopie wird ab ihrem Entstehen mitgezogen, ohne Aenderung am
      Werkzeug. Es haengt in `tools/doku-luecken-probe.sh` und in
      `tools/readme-behauptungen-pruefen.sh`.
    tags: ['doku', 'wache', 'wiki', 'falle', 2026]
    source: "27.08.2026, stuendlicher Doku-Lauf. Alle drei Proben (Luecken, Widerruf, Pack-Abgleich) meldeten gruen; der Fund kam aus der Muster-Methode: die Form des Vortags-Befundes (abgeleitete Zahl von Hand abgeschrieben) im Baum gegreppt."
    related: [umfangsdrift-kehrt-wieder-weil-die-naht-an-einem-aufrufer-haengt]
    body: |
      ZWEI EINTRAEGE ANTWORTEN AUF EINE DRIFTENDE „N Einträge, Fassung M".
      Dieser ist der REICHWEITEN-Fall (die Wache kannte nur einen Pfad),
      [[umfangsdrift-kehrt-wieder-weil-die-naht-an-einem-aufrufer-haengt]] ist
      der ABLAUF-Fall (die Naht war richtig, wurde aber nie gerufen). Die
      Frage, die trennt, ist ein Lauf:

          python3 tools/paket-angaben-nachziehen.py

      Meldet es die driftende Datei GAR NICHT -> Reichweite, DIESER Eintrag.
      Findet es sie und richtet sie sofort -> das Werkzeug war in Ordnung,
      es lief nur nicht: der andere Eintrag. Zweimal derselbe Befund in
      Folge heisst immer das zweite.

      DER STAND AM MORGEN — dieselbe Angabe, drei Zahlen:

        Datei                                 sagte              Rueckstand
        ────────────────────────────────────  ─────────────────  ──────────
        dokumentation/mixpibox.md             857 / Fassung 359  gewacht, richtig
        README.md                             799 / (keine)      58 Eintraege
        dokumentation/benutzerhandbuch.html   777 / Fassung 287  80 Eintraege

      Das Benutzerhandbuch nannte als Quelle ausserdem `llmwiki_mupibox` —
      das seit dem 10.08.2026 stillgelegte Nachbarrepo. Wer dort nachsieht,
      landet im toten Zweig. `tools/stillgelegte-orte-pruefen.py` liess das
      durch, und zu Recht: sie ruegt nur Saetze, die etwas ANWEISEN, und eine
      Herkunftsangabe weist nichts an. Die Luecke war nicht ihre.

      DIE FRAGE, DIE DEN FUND MACHT, ist nicht "steht die Zahl richtig da",
      sondern "an WIE VIELEN Stellen steht sie ueberhaupt". Der Vortag hatte
      die Naht gebaut, die die Zahl nach dem Anhaengen nachzieht
      ([[kopfzeilenzahl-driftet-weil-die-wache-vor-dem-anhaengen-laeuft]]) —
      und sie mit demselben Blick gebaut, der die Wache blind gemacht hatte:
      auf den Pfad, den der Autor gerade offen hatte. Eine Naht erbt die
      Blindheit der Wache, aus der sie entsteht.

      WORAN MAN ES BEIM BAU MERKT: steht im Werkzeug ein Dateiname als
      Konstante (`DOKU = WURZEL / "dokumentation" / "mixpibox.md"`), ist die
      Frage faellig, ob der Gegenstand an diesen Namen gebunden ist oder nur
      dort zufaellig gefunden wurde. Bei einer Zahl, die jemand ABSCHREIBEN
      kann, ist die Antwort immer: nur zufaellig.

      GEGENPROBE, sechs Teile, alle gemessen: jede der drei Dateien einzeln
      sabotiert (jedesmal Ausgang 1 und gerichtet); eine vierte, untrackte
      Datei mit der Angabe wird GEFUNDEN, aber nicht geschrieben; dieselbe
      Datei nach `git add` wird gerichtet; fehlendes Paket meldet UNMESSBAR
      (Ausgang 2) statt gruen. Das Werkzeug findet sich selbst nicht — das
      Muster steht in seinem Kopftext mit `<Zahl>` statt mit Ziffern.
  - id: wache-prueft-ihre-eigene-abschrift
    kind: falle
    sev: high
    title: "Eine Wache, die die geprüfte Behauptung als Literal führt, misst den Baum gegen sich selbst — die Doku liest sie nie"
    match: "readme-behauptungen|zahl \"|behauptet: .*gemessen:|Toleranz|796|rund 440 Werkzeuge"
    cause: >
      `tools/readme-behauptungen-pruefen.sh` heisst danach, die Behauptungen
      der README zu pruefen. Ihre Hilfsfunktion `zahl` bekommt aber drei
      Argumente: einen Namen, das BEHAUPTETE als LITERAL im Skript, und das
      am Baum GEMESSENE. Die README wird dabei nicht geoeffnet. Verglichen
      wird also der Baum gegen eine Abschrift, die jemand beim Bau der Wache
      angefertigt hat — und die seither genauso altert wie die Doku, nur
      unsichtbar.
    fix: >
      Fuer die Paketgroesse ist die Pruefung am 27.08.2026 aus der Wache
      entfernt und an `tools/paket-angaben-nachziehen.py` abgegeben: das
      LIEST die Behauptung aus jeder Datei, die sie fuehrt, und vergleicht
      exakt. Die uebrigen `zahl`-Aufrufe (Seiten, Units, Werkzeuge,
      Testdateien) tragen den Fehler weiter — sie stehen im BACKLOG.
    tags: ['wache', 'doku', 'falle', 'messfehler', 2026]
    source: "27.08.2026, stuendlicher Doku-Lauf. Aufgefallen beim Umbau der Umfangsangabe: die README fuehrte 799 Eintraege, das Paket 857, und die Wache meldete gruen."
    body: |
      ZWEI FEHLER UEBEREINANDER, und der zweite verdeckt den ersten.

      ERSTENS wird die geprueste Datei nicht gelesen. Der Aufruf lautete

          zahl "Eintraege im Wissenspaket (yaml.safe_load)" "796" "$n" 20

      — `"796"` ist das, was der Autor beim Bau in der README gesehen hatte.
      Die README sagte am 27.08. aber 799 und das Paket 857. Beide Zahlen,
      die im Bericht unter "behauptet" und "gemessen" nebeneinander standen,
      waren damit falsch bzw. nicht die der Doku. Eine Korrektur AN DER
      README haette hier nichts geaendert, und eine Verschlechterung ebenso
      wenig: die Wache kann den Zustand ihres Gegenstands nicht sehen.

      ZWEITENS die Toleranz. `zahl` erlaubt hier 20 % Abweichung. 796 gegen
      857 sind 7,7 % — dauerhaft innerhalb, dauerhaft gruen. Eine ABGELEITETE
      Zahl braucht keine Toleranz: sie ist gleich oder veraltet. Toleranz
      gehoert zu Groessen, die man schaetzt ("rund 440 Werkzeuge"), nicht zu
      solchen, die man ausrechnen kann.

      DIE ALLGEMEINE FORM: eine Wache ist tautologisch, wenn ihre Sollwerte
      aus derselben Hand stammen wie ihr Pruefgegenstand — hier schlimmer, sie
      stammen aus derselben Hand UND der Gegenstand wird gar nicht angesehen.
      Zu erkennen an der Signatur: nimmt eine Pruefroutine das Erwartete als
      ARGUMENT entgegen, statt es aus der Datei zu holen, prueft sie ihre
      eigene Abschrift. Verwandt mit
      [[urteil-steht-in-der-pruefenden-datei]], aber nicht dasselbe: dort
      stand das Ergebnis am falschen Ort, hier wird die Frage nie gestellt.

      WAS OFFEN BLEIBT (im BACKLOG unter dieser Kennung): die uebrigen sieben
      `zahl`-Aufrufe derselben Datei. Jeder einzelne muss die Zahl aus der
      README lesen statt sie zu fuehren — und wo die README eine Schaetzung
      schreibt ("rund 440"), muss die Wache das als Schaetzung behandeln
      duerfen, ohne die exakten Faelle mit weichzuspuelen.
  - kind: lesson
    id: praefix-in-der-wache-ist-die-namenssuche-durch-die-hintertuer
    title: Die Umgebungswache suchte den Zugriff — aber nur auf Namen mit Hauspraefix
    sev: med
    source: "Doku-Lauf 27.08.2026, tools/umgebungsvariablen-deckung.py"
    related: ["namenssuche-nach-wachen-laesst-die-geschwister-stehen", "gegenprobe-statt-gruen-glauben", "dauerrote-wache-ist-keine"]
    body: |
      DER KOPF DER WACHE HATTE RECHT UND DER CODE HIELT SICH NICHT DARAN.
      `tools/umgebungsvariablen-deckung.py` erklaert seit dem 25.08. ausfuehrlich,
      warum sie nach dem ZUGRIFF sucht und nicht nach dem NAMEN — eine
      Namenssuche haette `MUPIBOX_STANDWACHE_ECHT` gemeldet, das nur im
      Kommentar steht. Richtig. Nur stand in JEDEM ihrer Zugriffsmuster

          PRAEFIX = r"(?:MUPIBOX|MUPI)_[A-Z0-9_]+"

      Gesucht wurde also der Zugriff auf einen Namen MIT HAUSPRAEFIX. Die
      Namenssuche kam durch die Hintertuer zurueck, eine Stufe subtiler als in
      [[namenssuche-nach-wachen-laesst-die-geschwister-stehen]]: dort suchte
      eine Wache nach Namen STATT nach der Sache, hier nach der Sache — aber
      nur bei Namen einer bestimmten Machart.

      DAS IST DIE SCHLECHTESTE MOEGLICHE BLINDSTELLE. Wer sich an die
      Namenskonvention haelt, wird geprueft; wer von ihr abweicht, ist
      unsichtbar. Die Abweichung ist aber genau das, was auch dem Menschen
      durchrutscht, der das Handbuch pflegt: er sucht ebenfalls nach
      `MUPIBOX_`. Die Wache war dort blind, wo sie am meisten gebraucht wurde.

      ══ WAS SIE UEBERSAH ═══════════════════════════════════════════════════

      21 Stellschrauben, keine davon in irgendeiner Doku-Datei — mehrere nicht
      einmal in einem Audit. Die teuerste:

          PLAYER_PROXY_HOST / PLAYER_PROXY_PORT   src/backend-api/src/server.ts:2116

      Sie sagen, wohin die API den Wiedergabedienst weiterreicht (127.0.0.1:5005).
      Dieselbe Sorte Stellschraube wie `MUPIBOX_HTTP_PORT`, das im Handbuch
      zwei Tabellenzeilen weiter oben steht — nur ohne Praefix, und darum seit
      jeher unsichtbar. Dazu die ganze Leerlaufuhr (`TAKT`, `STARTKARENZ`,
      `ABSCHALT_BEFEHL`, `EINGABE_SYS`/`EINGABE_DEV`, …), die drei
      `MIXPI_WLAN_*`-Naehte und `MUPIHAT_LOGLEVEL`.

      ══ WIE DAS MERKMAL JETZT LAUTET ═══════════════════════════════════════

      Gelesen wird JEDER Grossbuchstabenname. Wer nicht gemeldet werden soll,
      steht im Werkzeug mit Grund daneben (`SYSTEM` = gehoert dem
      Betriebssystem, `VORRICHTUNG` = steuert eine Attrappe). Eine stille
      Obergrenze waere schlimmer als der Praefix, den sie ersetzt: sie saehe
      aus wie Deckung.

      ══ DREI FEHLBEFUNDE, DIE DER PRAEFIX BISHER VERDECKT HATTE ════════════

      Das Verbreitern legte drei Schwaechen frei, die vorher niemand sehen
      konnte, weil kein Name ohne Praefix je in die Muster kam:

      1. `${NAME:-vorgabe}` wurde NICHT gegen ein Selbstsetzen der Datei
         geprueft (nur das nackte `$NAME` wurde das). `GELESEN`,
         `SCHREIBGRUND`, `QUELLE`, `DEV` sind Ausweichmeldungen fuer den Fall,
         dass ein `NAME=$(…)` davor nichts lieferte — keine Stellschrauben.
      2. Die Zuweisung an sich selbst ist KEIN Setzen.
         `TMP_DIR="${TMP_DIR:-/tmp}"` beginnt mit dem Namen und sah nach
         Selbstsetzen aus; gemeint ist das Gegenteil. Es zaehlt nur eine
         Zuweisung, in der der Name nicht auch rechts steht.
      3. Auskommentierte Zeilen zaehlten mit. Der erste Treffer war `URL` aus
         `chromium-autostart.sh:115` — einer STILLGELEGTEN Startzeile; der
         lebende Weg darunter benutzt `MUPI_URL`. Der Praefix hatte diese
         Luecke nie geschlossen, nur zufaellig nie getroffen.

      In der Shell behaelt das NACKTE `$NAME` den Praefix, und das ist kein
      Rueckfall: `process.env.X`, `os.environ[X]` und `${X:-vorgabe}` sagen
      durch ihre FORM, dass der Wert von aussen kommen darf. Ein blankes `$X`
      sagt das nicht — ohne Praefix meldete dieser Zweig `TAKT`, `LOG`,
      `BEREICH`: Schleifenvariablen, deren Dauerrot die naechste echte Luecke
      verdeckt haette ([[dauerrote-wache-ist-keine]]).

      ══ GEGENPROBE ═════════════════════════════════════════════════════════

      Eine Wegwerfdatei mit `${GANZ_OHNE_PRAEFIX_ERFUNDEN:-/tmp/nichts}` unter
      `scripts/` — gemeldet, wie sie muss; nach dem Loeschen wieder gruen. Die
      alte Fassung haette den Namen nicht einmal gesehen.

      ══ DIE REGEL ══════════════════════════════════════════════════════════

      Wenn eine Wache ein Praefix, eine Endung oder ein Verzeichnis als
      Vorfilter benutzt, prueft sie nicht die Sache, sondern die Konvention.
      Das faellt nie auf, weil der Bestand die Konvention meist einhaelt — und
      der eine Fall, der sie bricht, ist der, den man dokumentiert haben wollte.
  - kind: lesson
    id: gegenprobe-der-unitwache-sah-nur-namen-mit-hauspraefix
    title: Die Gegenprobe der Ausrollwache prüfte nur Units mit Hauspräfix — fünf eigene tragen keins
    sev: med
    source: "Doku-Lauf 27.08.2026, tools/ausrollweg-deckung.py Abschnitt 4"
    related: ["praefix-in-der-wache-ist-die-namenssuche-durch-die-hintertuer", "namenssuche-nach-wachen-laesst-die-geschwister-stehen", "gegenprobe-statt-gruen-glauben", "dauerrote-wache-ist-keine"]
    body: |
      DIESELBE BLINDSTELLE, EINE WACHE WEITER. Am selben Tag flog aus
      `tools/umgebungsvariablen-deckung.py` ein `(?:MUPIBOX|MUPI)_`-Praefix,
      das aus einer Sachsuche wieder eine Namenssuche gemacht hatte
      ([[praefix-in-der-wache-ist-die-namenssuche-durch-die-hintertuer]]).
      Die Muster-Methode fragt danach: WO STEHT DIESE FORM NOCH? Ein Grep
      ueber alle Wachen nach `startswith`/Praefix-Literalen fand sie in der
      Gegenprobe von `tools/ausrollweg-deckung.py`:

          if not unit.startswith(("mupi_", "mupibox-", "librespot",
                                  "soloist", "mixpi-")):
              continue

      Die Gegenprobe fragt: was schaltet ein Ausrollweg per `systemctl enable`
      ein, das auf einer frischen Karte gar nicht liegt? Der Befehl meldet
      dazu nichts Sichtbares — deshalb gibt es die Wache ueberhaupt. Sie sah
      aber nur Units mit Hauspraefix.

      ══ DIE BEGRUENDUNG WAR AN IHREM EIGENEN BEISPIEL FALSCH ════════════════

      Darueber stand: „Fremde Units (smbd, dietpi-*) bringt das Betriebssystem
      mit — nur die eigenen sind hier gemeint." `dietpi-dashboard.service`
      liegt in `config/services/`. WIR bringen sie mit. Die Wache erklaerte
      ihren Ausschluss an einer Datei, die im eigenen Baum liegt.

      Gemessen statt geglaubt: von 42 ausgerollten Units tragen FUENF kein
      Hauspraefix — `dietpi-dashboard`, `pulseaudio`, `spotifyd`,
      `systemd-user-sessions`, `wifi-powersave-off`. Zwoelf Prozent des
      Bestands, und genau die Machart, die auch dem Menschen durchrutscht,
      der nach `mupibox-` greppt.

      ══ GEGENPROBE ═════════════════════════════════════════════════════════

      Nicht am Bestand gemessen (der ist gruen und bleibt es), sondern mit
      einer erfundenen `systemctl enable`-Zeile im autosetup-Text — die Wache
      muss ROT werden:

          mupibox-neuling.service          alt MELDET   neu MELDET
          kioskwache.service               alt STILL    neu MELDET
          wifi-powersave-off-neu.service   alt STILL    neu MELDET
          dietpi-neu.service               alt STILL    neu MELDET

      Drei von vier Faellen waren still. Wer den Bestand allein misst, sieht
      davon nichts: heute erreicht KEINE eingeschaltete Unit diese Stelle,
      jede liegt in einem Quellordner oder wird namentlich kopiert. Eine
      Blindstelle, die noch nichts verdeckt, ist trotzdem eine.

      ══ WIE ES JETZT AUSSIEHT ══════════════════════════════════════════════

      Gelesen wird JEDE eingeschaltete Unit. Wer draussen bleibt, steht in
      `FREMD_MIT_GRUND` mit Begruendung — dieselbe Form, die die
      Schwesterwache seit demselben Tag benutzt (`SYSTEM` = gehoert dem
      Betriebssystem). Die Liste ist LEER und das ist gemessen, nicht bequem:
      es gibt heute keinen Fall. Eine leere Ausnahmeliste ist die ehrliche
      Fassung eines Filters, der nichts auszuschliessen hat — und der naechste
      Eintrag traegt dann eine pruefbare Behauptung („liegt auf jeder frischen
      Karte, bevor unser Skript laeuft") statt einer Namensform.

      ══ WAS DAS FUER DEN NAECHSTEN LAUF HEISST ═════════════════════════════

      EINE BEHOBENE BLINDSTELLE IST EIN SUCHMUSTER, KEIN ABSCHLUSS. Die Form
      „Wache filtert nach der Machart des Namens statt nach der Sache" stand
      an mindestens zwei Stellen, gebaut von zwei verschiedenen Anlaessen.
      Nach jedem Fund dieser Art einmal baumweit nach der FORM greppen
      (`startswith(`, Praefix-Literale, `removeprefix`), nicht nach dem
      Gegenstand — und die ausgeschlossene Klasse EINZELN nachmessen, nie der
      Begruendung im Kommentar glauben. Hier war die Begruendung an ihrem
      eigenen Beispiel widerlegbar, und es hatte sie ueber Wochen niemand
      nachgeschlagen.
  - id: audit-2026-08-28-kritiker
    kind: howto
    title: "Kritiker-Lauf 28.08.2026 — der Docker-Bau stirbt vor den bekannten Fehlern, die Naht zwischen den zwei Node-Diensten, und zwei Berichtigungen an gestern"
    match: "Dockerfile:6|curl vor apt|readTlsCert|mupi-lautstaerke|befehlspfad|plugin-laufwerk|cross-env|hwapply|playerstate|karma-to-vitest|AUDIT-2026-08-28"
    tags: [audit, docker, backend-player, dopplung, abhaengigkeiten, 2026]
    body: |
      Quelle: AUDIT-2026-08-28.md (vier Pruefer: Sim-Weg aus `harness/`,
      `docker/`, `bin/` und `dev/`,
      Abhaengigkeits-Inventur, backend-player-Innenleben, Netz-Recherche;
      Reviere 21.-27.08. nicht wiederholt). Rueckstand: FUENFTER Tag null,
      und die Stufe ist neu - auch was den Sprung ins BACKLOG geschafft hat
      (die zwei Aufraeum-Punkte des Doku-Laufs 26.08.), bewegt sich nicht.
      Der Engpass ist der Abtrag, nicht die Form. Vgl.
      [[audit-2026-08-27-kritiker]].

      DER STAERKSTE NEUFUND: Dockerfile:6 ruft curl VOR dem apt-get, das
      curl erst installiert - debian:bookworm-slim hat keins, der Bau
      stirbt seit 11.11.2024 in Schicht 2 mit Exit 127. Die zwei bekannten
      toten COPY-Pfade (Backlog-Punkt 26.08., Dockerfile:74/:103) liegen
      DAHINTER und sind unerreichbar - wer nur sie repariert, hat nichts
      repariert. tools/abbild-pfade-pruefen.py prueft nur COPY-Quellen und
      meldet gruen am toten Bau vorbei. Die ehrliche Frage ist "lebt der
      Docker-Weg?", nicht "reparieren" - 21 Monate hat ihn niemand
      vermisst, der lebende Sim-Weg ist harness/ + mupictl.

      ZWEI BERICHTIGUNGEN AN GESTERN (der Kritiker prueft auch sich):
      (1) AUDIT-27 D8 zaehlte src/backend-player/config/mupiboxconfig.json
      als fuenfte Konfig-Fixtur - falsch: beide JSONs dort sind 0 Byte,
      root-eigen, .gitignore'd; versioniert ist nur .gitkeep. Der ECHTE
      Befund daneben: spotify-control.ts:55-56 zeigt in der Entwicklung
      genau auf diese leeren Dateien, require() einer 0-Byte-JSON wirft
      SyntaxError - npm run serve des Players scheitert auf jedem frischen
      Klon, kein Seed fuellt den Ordner. (2) Der path-Unit-Vorschlag
      (AUDIT-27 Rang 8) braucht eine Auflage: systemd-path-Units sind
      NICHT rekursiv (PathModified sieht Unterordner nicht). Er haelt
      trotzdem, weil change_checker.sh heute selbst nur Top-Level-ctimes
      prueft; rekursiv hiesse inotifywait -r als Dienst.
      AUSSERDEM VERNEINT: backend-player ist NICHT abgeloest und keine
      Doppelimplementierung - eigener Dienst (mupibox-player.service, Port
      5005), beide Ausrollwege legen UND schalten ihn, server.ts:1097
      proxyt /player/* dorthin, server.ts:564 begruendet die Trennung.

      DIE NAHT ZWISCHEN DEN ZWEI DIENSTEN ist die Fundgrube: readTlsCert
      (server.ts:615 "Shared with the backend-player") ist in Wahrheit
      Copy-Paste (spotify-control.ts:106-114, ohne try/catch); die
      Lautstaerke geht ueber DASSELBE mupi-lautstaerke.sh auf zwei Arten
      (Player: Shell-Strings + amixer-Fallback + eigener Prozent-Parser;
      api: execFileSync ohne Fallback) - und server.ts:7222-7231 begruendet
      selbst, warum es EINEN Weg geben soll; der Player liest die Konfig
      auf VIER Wegen (require-Snapshot :60, Kandidatenliste :578-604,
      hartkodiertes readFileSync :741-748, waehrend die api zentral
      aufloest); /tmp/playerstate wird 3x roh geschrieben
      (spotify-control.ts:490/506/529) und von mqtt.py:729 mit head -n1
      gelesen - Wettlauf, atomar.ts loest es, die 13er-Zaehlung von
      gestern zaehlte nur die api ([[vierzehn-kopien-und-die-abweichung-ist-der-fehler]]).
      Das Befehlsformat hat EINEN Parser (befehlspfad.ts) und VIER
      unabhaengige Bauer (ard.ts:72, server.ts:2307, server.ts:10977,
      player.service.ts:184). Das VORBILD existiert schon:
      spotify-control.ts:46-47 importiert geraetWaehlen aus
      backend-api/src/spotify-web.ts - der einzige echte Code-Share.

      BAU- UND ABHAENGIGKEITS-WELT: tools/pruefen.sh:959-961 ist die
      VIERTE Kopie der esbuild-Kommandozeile und baut plugin-laufwerk NIE
      - die Pruef-Pipeline verifiziert einen anderen Artefaktsatz als npm
      run build, gegen den Warnkommentar in backend-api/package.json:9.
      express 4 (Player) gegen 5 (api) im selben Baum -
      spotify-control.ts:2255 baut das Promise-Auffangnetz von Hand nach,
      das 5 mitbringt. esbuild "*" gegen Pin 0.25.10. cross-env wird in
      vier Skripten benutzt und nirgends deklariert (Hoisting-Zufall);
      frontend-admin deklariert NULL Abhaengigkeiten bei sechs Importen.
      npm run serve startet drei von vier Diensten; lint --workspaces
      ueberspringt den Admin STILL (npm schweigt bei fehlendem Skript).
      Tote Abhaengigkeiten neu: baseline-browser-mapping (Wurzel),
      jasmine-spec-reporter, @types/bcryptjs (redundant zu bcryptjs 3.x
      UND in dependencies statt dev). mupictl hwapply schreibt in die
      git-getrackte Saatdatei (mupictl:147 -> harness/seed/), der Sim
      liest aber state/ - Flagge landet im Repo, wirkt nicht.

      NETZ 2026: Karma ist deprecated, Angular 21 (11/2025) macht Vitest
      zum Standard mit Schematic karma-to-vitest - erst Angular-21-Update,
      dann Migration; CHROME_BIN-Kruecke faellt. SSE-Vorschlag bestaetigt
      mit zwei Auflagen: EIN Endpunkt mit event-Typen (HTTP/1.1 deckelt
      ~6 Verbindungen/Origin) plus ~15-s-Heartbeat (WLAN-Abriss!).
      node:sqlite erst ab Node 24 ohne Flag - Box pinnt 22
      (autosetup.sh:316). simple-keyboard (MIT, 0 deps, ~5 kB) nur falls
      die D4-Eigenfassung >300 Zeilen kostet; Touch unter Cog/WebKit am
      Geraet pruefen. Negativ: nativer WS-Client, fs.glob, styleText,
      --env-file ersetzen hier nichts.

      NEGATIV-BEFUNDE (geprueft, kein Fund): harness/seed ist echte
      Teilmenge (33/108 Schluessel, null nur-Saat) - Absicht, keine Drift;
      mupictl baut scripts/ nicht nach, es ruft sie; sprechen.ts ist das
      GEGENTEIL einer TTS-Dopplung; der Lockfile ist sauber (fuenf
      package.json Schluessel-fuer-Schluessel abgeglichen).
  - id: umfangsdrift-kehrt-wieder-weil-die-naht-an-einem-aufrufer-haengt
    kind: falle
    title: "Derselbe Drift am zweiten Tag: die Naht hing an EINEM Aufrufer, und wer von Hand ins Paket schreibt, geht daran vorbei"
    match: "Einträge, Fassung|paket-angaben-nachziehen|wiki-anhaengen|pre-commit|core.hooksPath|Umfangsangabe|--index"
    tags: [doku, wiki, wache, ablauf, git, 2026]
    related: [umfangsangabe-stand-an-drei-orten-die-wache-kannte-einen]
    body: |
      DERSELBE SATZ IM `match:` WIE BEIM VORTAGS-EINTRAG
      [[umfangsangabe-stand-an-drei-orten-die-wache-kannte-einen]] — und
      absichtlich, denn wer die driftende Zahl sucht, braucht BEIDE. Trennen
      laesst sich mit einer Frage: kennt `paket-angaben-nachziehen.py` die
      Datei? Kennt es sie nicht, fehlt der Wache Reichweite (Vortag). Kennt
      es sie und richtet sie auf Zuruf, dann lief nur niemand — das ist
      dieser Eintrag.

      Doku-Lauf 28.08.2026. Die Luecken-Probe meldete den Umfangsdrift ZUM
      ZWEITEN MAL IN FOLGE: README.md, dokumentation/benutzerhandbuch.html
      und dokumentation/mixpibox.md sagten 861 Eintraege/Fassung 363, das
      Paket fuehrte 862/364. Am 27.08. war genau dieser Fund schon behandelt
      worden — damals als Reichweitenfehler der Wache, die nur EINEN Pfad
      kannte ([[umfangsangabe-stand-an-drei-orten-die-wache-kannte-einen]]).
      Die Reparatur war richtig und hat trotzdem nicht getragen.

      DER UNTERSCHIED ZWISCHEN FUND UND ABLAUFFEHLER. Ein Befund, der nach
      seiner Reparatur wiederkehrt, ist kein Doku-Fehler mehr. Die drei
      Zahlen waren nicht falsch abgeschrieben — sie wurden gar nicht erst
      angefasst. `tools/paket-angaben-nachziehen.py` richtet zuverlaessig,
      aber gerufen wird es an GENAU EINER NAHT: aus
      `tools/wiki-anhaengen.py`, direkt nach dem Anhaengen
      ([[kopfzeilenzahl-driftet-weil-die-wache-vor-dem-anhaengen-laeuft]]).
      Der Kritiker-Lauf `179d69a7` hat seinen Eintrag von Hand in
      `llmwiki/pack.yaml` geschrieben — der Commit fasst nur `AUDIT-*.md`
      und `pack.yaml` an. Damit lief die Naht nie. Sie war nicht kaputt, sie
      war unerreicht.

      Das ist die allgemeine Form: EINE NAHT, DIE AN EINEM AUFRUFER HAENGT,
      SICHERT NICHT DIE SACHE, SONDERN EINEN WEG ZU IHR. Solange ein zweiter
      Weg zum selben Ergebnis existiert (hier: der Texteditor), ist die
      Absicherung eine Empfehlung. Die Frage beim Bauen einer Naht ist nicht
      "wo faellt die Aenderung an?", sondern "durch welche ENGSTELLE muss
      jede Aenderung, egal wer sie macht?".

      DIE ENGSTELLE IST DER COMMIT. Neu gebaut:
      `tools/git-hooks/pre-commit`, eingehaengt mit
      `git config core.hooksPath tools/git-hooks` (aushaengen:
      `git config --unset core.hooksPath`, einmal umgehen:
      `git commit --no-verify`). Er greift NUR, wenn `llmwiki/pack.yaml`
      gestaged ist, und verweigert den Commit, solange die Kopfzeilen
      hinterherhinken.

      DREI ENTSCHEIDUNGEN AM HAKEN, JEDE MIT GRUND:
      (1) ER MISST DEN INDEX, NICHT DEN BAUM. Dafuer hat
      `paket-angaben-nachziehen.py` einen `--index`-Modus bekommen, der die
      Blobs ueber `git show :pfad` liest — Paket UND Prosa. Eine im Baum
      gerichtete, aber nicht gestagte README ginge einer Baum-Pruefung als
      gruen durch und der Commit driftete trotzdem.
      (2) ER SCHREIBT NICHTS. Automatisch nachziehen und nachstagen hiesse,
      waehrend eines Commits fremde Dateien in den Index zu heben; auf dieser
      Maschine laufen regelmaessig parallele Sitzungen im selben Baum. Er
      meldet und nennt den Befehl.
      (3) UNMESSBAR (Ausgang 2) LAESST ER DURCH. Ein Haken, der auch dann
      blockiert, wenn jemand die Form der Angabe absichtlich umschreibt, wird
      beim ersten Mal mit `--no-verify` abgeschaltet und danach nie wieder
      eingeschaltet.

      NACHTRAG 29.08.2026 — ENTSCHEIDUNG (3) STAND NUR IM KOMMENTAR. Der
      Zweig war toter Code: `ausgang=$?` stand NACH einem `fi` und las damit
      den Ausgang des `if` (immer 0), nicht den von `python3`. Der Haken
      blockierte bei "unmessbar" und nannte den falschen Grund. Behoben und
      auf allen drei Ausgaengen gegengeprueft am 29.08.:
      [[ausgang-nach-fi-ist-null-der-durchlass-zweig-war-tot]]. Die
      Begruendung oben gilt unveraendert — das Verhalten gab es bis dahin
      nicht.

      DIE GEGENPROBE WAR GESCHENKT. Zum Zeitpunkt des Baus war der Baum schon
      gerichtet (862/364), der Index stand noch auf dem Commit-Stand
      (861/363). `--index` meldete Drift und Ausgang 1, waehrend `--pruefen`
      gruen war. Beweis in einem Lauf, dass der Modus wirklich den Index
      liest und nicht den Baum daneben.

      ZWEITER FUND DESSELBEN LAUFS — DER SAMMELNAME, VIERTES MAL. Die
      Pfadwache meldete die vier mit Schraegstrichen verketteten Ordnernamen
      `harness`+`docker`+`bin`+`dev` als toten Pfad (genannt in
      [[audit-2026-08-28-kritiker]]). Der Pfad existiert nicht und hat nie
      existiert: gemeint waren VIER Reviere — `harness/`, `docker/`, `bin/`
      und `dev/` —, mit Schraegstrichen zu einem Wort verkettet. Genau die
      dritte Klasse toter Verweise aus
      [[doku-zeigt-auf-dateien-die-es-nicht-gibt]]: nicht umgezogen, nicht
      zweideutig, sondern beim Schreiben aus dem Wunsch nach Kuerze
      entstanden. Berichtigt durch Ausschreiben. Die Regel steht seit dem
      27.08. da und hat den naechsten Fall trotzdem nicht verhindert —
      Regeln in Prosa wirken erst, wenn eine Wache sie durchsetzt; hier tut
      es `tools/doku-pfade-pruefen.py`, einen Lauf spaeter.

      Die Quelle `AUDIT-2026-08-28.md` traegt denselben Sammelnamen und
      bleibt ABSICHTLICH stehen: `AUDIT-*.md` sind datierte Momentaufnahmen,
      die Pfadwache ueberspringt sie begruendet.

      ══ FUER DEN NAECHSTEN LAUF ═══════════════════════════════════════════
      Wenn die Luecken-Probe einen Befund meldet, der schon einmal behoben
      wurde, NICHT noch einmal beheben. Erst fragen: welcher Weg hat die
      Reparatur umgangen? Der Fund ist dann die Engstelle, nicht die Zahl.
  - id: ausgang-nach-fi-ist-null-der-durchlass-zweig-war-tot
    kind: falle
    title: "Der Haken hielt seine eigene dritte Entscheidung nicht: `$?` nach `fi` ist 0, nicht der Ausgang des Aufrufs"
    match: "ausgang=\\$\\?|if ausgabe=\\$\\(|pre-commit|unmessbar|Ausgang 2|core.hooksPath|doppelte Buchfuehrung"
    tags: [bash, wache, git, doku, aussage-haelt-nicht, 2026]
    body: |
      Doku-Lauf 29.08.2026. Der am Vortag gebaute `tools/git-hooks/pre-commit`
      nennt in seinem Kopfkommentar drei Entscheidungen mit Begruendung. Die
      dritte lautete: UNMESSBAR (Ausgang 2) LAESST ER DURCH — mit dem
      ausdruecklichen Grund, dass ein Haken, der auch beim absichtlichen
      Umschreiben der Angabenform blockiert, beim ersten Mal mit
      `--no-verify` abgeschaltet und nie wieder eingeschaltet wird
      ([[dauerrote-wache-ist-keine]]).

      DIE ENTSCHEIDUNG STAND IM KOMMENTAR, IM WIKI UND IN DER
      COMMIT-NACHRICHT — UND NICHT IM CODE. Geschrieben war:

          if ausgabe=$(python3 "$werkzeug" --index 2>&1); then
            exit 0
          fi
          ausgang=$?

          if [ "$ausgang" -eq 2 ]; then ... exit 0; fi

      `$?` nach `fi` ist NICHT der Ausgang von `python3`. Ein `if`, dessen
      Bedingung falsch war und das keinen `else`-Zweig hat, liefert SELBST 0
      — und genau diese 0 stand danach in `ausgang`. Der Vergleich gegen 2
      konnte nie zutreffen. Der Durchlass-Zweig war toter Code; der Haken
      blockierte bei "unmessbar" und nannte dabei den FALSCHEN Grund ("die
      Umfangsangaben stehen noch auf dem alten Stand"), samt eines
      Richtbefehls, der an dieser Lage nichts geaendert haette.

      NACHGEMESSEN, NICHT ERSCHLOSSEN:

          $ bash -c 'if a=$(exit 2); then echo then; fi; echo "nach fi: $?"'
          nach fi: 0
          $ bash -c 'if a=$(exit 2); then :; else echo "im else: $?"; fi'
          im else: 2

      Im `else`-Zweig steht die 2 sehr wohl. Der Ausgang geht also nicht
      verloren — er wird vom `fi` ueberschrieben. Wer `$?` erst NACH dem
      Block liest, liest den Ausgang des Blocks.

      DIE REGEL: DEN AUSGANG IN DERSELBEN ANWEISUNG SICHERN, IN DER DER
      AUFRUF STEHT. Nicht der Aufruf gehoert in die Bedingung, sondern die
      schon gesicherte Zahl:

          ausgabe=$(python3 "$werkzeug" --index 2>&1)
          ausgang=$?
          if [ "$ausgang" -eq 0 ]; then exit 0; fi

      (`set -u` ist gesetzt, `set -e` nicht — die nackte Zuweisung ist
      deshalb gefahrlos. Mit `set -e` haette der Aufruf ausserhalb einer
      Bedingung das Skript beendet, und man braeuchte `|| ausgang=$?`.)

      DIE GEGENPROBE, DIE DEN FUND UEBERHAUPT ERST ZULIESS. Der Fehler ist
      unsichtbar, solange man den Haken nur im gruenen und im roten Fall
      laufen laesst: 0 und 1 kommen richtig heraus, ALLE DREI Pfade sind erst
      an ihren Ausgaengen unterscheidbar. Geprueft wurde der ECHTE
      Hakenscript mit zwei Schienen in `PATH` — ein `git`, das ein gestagtes
      `llmwiki/pack.yaml` behauptet, und ein `python3`, das der Reihe nach 0,
      1 und 2 zurueckgibt. Erwartet und seit der Reparatur gemessen:
      0->0 (gruen), 1->1 (blockt), 2->0 (durchgelassen). Vorher: 2->1.

      Das ist billiger als es klingt und der einzige Weg, einen Zweig zu
      pruefen, den das echte Werkzeug im Alltag nie ausloest — vgl.
      [[gegenprobe-statt-gruen-glauben]].

      ══ DIE ALLGEMEINE FORM ═══════════════════════════════════════════════
      Der Vortag hat die Naht vom Werkzeugaufruf an den Commit verlegt, weil
      eine Absicherung an EINEM Aufrufer nur einen Weg sichert
      ([[umfangsdrift-kehrt-wieder-weil-die-naht-an-einem-aufrufer-haengt]]).
      Richtig — aber die neue Wache wurde im selben Zug nur auf dem Pfad
      erprobt, auf dem sie gerade gebraucht wurde. EINE FRISCH GEBAUTE WACHE
      IST SELBST UNGEPRUEFTER CODE. Ihr Kopfkommentar ist eine ABSICHT, und
      Absicht und Verhalten fallen genau dort auseinander, wo niemand
      hinsieht: im Zweig fuer den seltenen Fall.

      Und: eine Entscheidung, die an drei Orten behauptet wird (Kommentar,
      Wiki, Commit-Nachricht), wird durch die Wiederholung nicht wahrer. Die
      dreifache Nennung hat hier sogar geschadet — sie las sich wie
      Bestaetigung. Der Zwilling dieses Fundes auf der Zustandsseite:
      [[behoben-in-ist-eine-zustandsaussage-und-war-falsch]].
  - id: audit-2026-08-29-kritiker
    kind: howto
    title: "Kritiker-Lauf 29.08.2026 — ein hartkodiertes false liess zwei Features still sterben, die Skript-Flotte liefert ihre Leichen mit aus, und sieben Prosa-Saetze widersprechen dem Code"
    match: "shouldUsePlayer|trackChangeDetected|external-playback-navigator|disable_mupihat|save_rrd|mupi_taster|mupi_knopflicht|check_network.sh:83|mupi-check.*pm2|AUDIT-2026-08-29"
    tags: [audit, frontend-box, scripts, mqtt, doku, totcode, 2026]
    body: |
      Quelle: AUDIT-2026-08-29.md (vier Pruefer: frontend-box klassisch
      erstmals als eigenes Revier, Skript-Welt mit Aufrufer-Karte,
      Benutzerhandbuch gegen den Code, Netz-Recherche; Stichproben vom
      Hauptlauf gegengeprueft). Rueckstand: SECHSTER Tag null Abtrag,
      hoechste E-Nummer weiter E90; Bewegung nur in der Wachen-Spur (der
      Doku-Lauf d1edf5d5 haengte den pre-commit-Hook ein). Vgl.
      [[audit-2026-08-28-kritiker]].

      DER STAERKSTE FUND: Totcode mit WIRKUNG, nicht nur Ballast.
      spotify-player.service.ts:113 gibt shouldUsePlayer() seit der
      SDK-Rente (27.07.) konstant false zurueck; initializePlayer hat
      null Aufrufer, das einzige trackChangeDetected$.next() (:582)
      feuert nie. Still gestorben sind dadurch die Telegram-"Jetzt
      laeuft"-Meldung (media.service.ts:204-239) und die Cast-Erkennung
      (external-playback-navigator.service.ts, wird trotzdem injiziert).
      Erst ENTSCHEIDEN (wiederbeleben via /player/state-Polling oder
      ausbauen), dann die Kaskade abtragen (~645-Zeilen-Service,
      115-Zeilen-Zweig in media.service.ts:50-167, 14er-Fassade).

      SKRIPT-WELT (Aufrufer-Karte ueber Repo inkl. Ausrollwege):
      autosetup.sh:567-574 schiebt scripts/-Ordner WHOLESALE nach
      /usr/local/bin — ~15 tote Dateien landen auf jeder frischen Karte,
      darunter disable_mupihat.sh, das per Copy-Paste die ENABLE-Zeilen
      an /boot/config.txt anhaengt (scharfe Falle), und save_rrd.sh, das
      per installierter Crontab 4x/min fuer NIEMANDEN misst (kein Leser
      von /tmp/.rrd im Repo). led_control.c wird je Setup UND Update
      kompiliert (gcc+pigpio), der einzige Aufruf ist auskommentiert.
      HOCH-BEFUND: zwei Unit-Familien ohne Kreuz-Riegel auf dieselben
      Skripte (mupi_offtrigger vs mupi_taster auf off_trigger.sh,
      mupi_powerled vs mupi_knopflicht auf mupi_start_led.sh) — Handweg
      plus Update ergibt zwei Prozesse um GPIO17/GPIO13, der
      dokumentierte "GPIO not allocated"-Tod. check_network.sh:83 ist
      KEIN Vergleich, sondern fuehrt das Kommando `true == online` aus —
      Zufalls-Korrektheit. mqtt.py: fan-Discovery ohne command_topic
      (kaputte HA-Entitaet), Lautstaerke-LESEN via alsaaudio am
      mupi-lautstaerke.sh-Weg vorbei (zwei Wahrheiten auf PipeWire),
      scrot je Schleifendurchlauf. Vgl.
      [[skript-das-niemand-ruft-verrottet-lautlos]].

      DOKU: sieben Prosa-Saetze widerlegt der Code, alle Wachen gruen
      (sie pruefen Zeilen-Existenz, nicht Aussagen): Handbuch behauptet
      PHP-Admin "noch an Bord" (:831, drei Tage nach E47 geschrieben),
      lehrt die geloeschte Zahnrad-Geste mit falscher Dauer (:712),
      behauptet die Browser-Verwaltung haenge hinter der
      Einstellungssperre (:763 — sie hat eine EIGENE Passwort-Anmeldung,
      sicherheitsrelevant), zaehlt drei statt vier Themen; mixpibox.md
      nennt espeak/Google-TTS statt Piper (:413); tools/mupi-check.py
      fragt weiter pm2 (:115-125) — der FUENFTE pm2-Rest, den
      [[pm2-reste-nach-der-systemd-umstellung]] nicht kennt.

      RECHERCHE 2026: shellcheck+shfmt mit Ratchet in den neuen
      pre-commit-Hook (ja; check_network.sh:83 ist die Fehlerklasse);
      systemd-Watchdog fuer die zwei Node-Dienste (ja, sd_notify-Weg
      vorher klaeren, Kern-Node kann keine Unix-Datagram-Sockets);
      HA Device-based Discovery + paho-v2 beim mqtt.py-Umbau; Angular 21
      ja, zoneless nur am Geraet messen; mpv-Bibliotheken VERWORFEN
      (eigener Wrapper besser), Gewinn laege in langlebiger Instanz/
      Gapless; PipeWire erst NACH der Netzabriss-Messkampagne.

      Rangliste (14 Punkte) in AUDIT-2026-08-29.md §6; Rang 1 bleibt die
      Abtrage-Entscheidung (neunter Tag offen).

  - kind: lesson
    id: neue-senke-faengt-bei-eins-an
    title: "Eine frisch gebaute Senke faengt bei 1.00 an — und das hoert man"
    sev: high
    tags: [ton, pipewire, lautstaerke, geraet, kinderschutz]
    body: |
      Der Betreiber meldete: „springt die lautstaerke beim spielen".

      AM GERAET NACHGEZAEHLT (29.08.2026): fuenf Kettenneubauten in vierzig
      Minuten, jeder ausgeloest durch eine Auslieferung. `klangwerkNeuAufbauen`
      wirft die alte Kette weg und legt eine neue an — und eine NEUE
      PipeWire-Senke faengt bei 1.00 an, also bei VOLLEM Pegel.

      Die Wiederherstellung der Hoerlautstaerke stand HINTER
      `signalwegNachbessern()`. Damit gab es ein Fenster, in dem die Musik
      schon durch die neue Senke lief und der Regler noch auf Anschlag stand.
      Der Sprung zeigt nach OBEN, an einem Lautsprecher neben einem Kind.

      ══ DIE REIHENFOLGE IST DIE GANZE LOESUNG ══════════════════════════════
      Die Senke gibt es, sobald die Warteschleife sie gefunden hat — sie
      traegt nur noch kein Signal. GENAU DANN stellt man den Regler; danach
      ist es zu spaet. Der zweite Aufruf hinter dem Umrouten bleibt trotzdem
      stehen (`signalwegNachbessern` kann die Vorgabe-Senke wechseln), er ist
      dann nur nicht mehr zu hoeren. `set` ist idempotent, zweimal kostet
      nichts.

      ══ WAS DARAN ALLGEMEIN IST ════════════════════════════════════════════
      Jede Stelle, die einen Tonknoten NEU anlegt, erbt dessen Vorgabewerte —
      nicht die des Vorgaengers. Wer einen Knoten ersetzt, muss den Zustand
      MITNEHMEN, und zwar bevor Signal fliesst. Das gilt fuer die Lautstaerke
      wie fuer alles andere, was am Knoten haengt.

      Vorher war das folgenlos, aber aus dem falschen Grund: hinter der Senke
      lagen 82 dB gestrandete Daempfung, die jeden Sprung auffingen. Als die
      weg war, wurde aus einer stillen Unsauberkeit ein hoerbarer Fehler —
      und aus einem harmlosen 1.00 der volle Pegel.
    related: ["genau-eine-stufe-regelt-den-ton", "mitschnitt-nahm-den-lautsprecher-auf", "klangwerk-senke-nimmt-keine-lautstaerke"]

  - kind: lesson
    id: mehrzeiliges-protokoll-verliert-den-rest
    title: "Ein mehrzeiliger Protokolleintrag kommt einzeilig im Journal an"
    sev: med
    tags: [protokoll, journald, plugin, messen]
    body: |
      Die Nachschau ueber den Mitschnitt-Bestand meldete:

          Nachschau fertig: 64 nachgemessen, 13 kaputt:

      Die dreizehn Namen standen in denselben String, durch `\n` getrennt —
      und kamen im Journal NICHT an. Was ankam, war die erste Zeile: die Zahl.
      Also genau das, wofuer man nicht nachsieht; die LISTE war der Zweck.

      Ein Protokolleintrag ist eine Zeile. Wer mehrere braucht, ruft mehrmals:

          for (const z of text.split('\n')) if (z.trim()) protokoll(`… ${z}`)

      DIE FALLE IST NICHT DER VERLUST, SONDERN DASS ER AUSSIEHT WIE ERFOLG.
      Die Meldung war da, sie war sogar richtig — sie war nur unvollstaendig,
      und nichts daran sagte das.
    related: ["was-in-einer-unreinen-funktion-steht-hat-keinen-zeugen"]

  - kind: lesson
    id: plugin-aktion-hat-acht-sekunden
    title: "Eine Plugin-Aktion hat acht Sekunden — laenger dauert nichts, es faellt nur aus"
    sev: med
    tags: [plugin, vertrag, aktion, geraet]
    body: |
      Die erste Fassung der Bestands-Nachschau mass IM Knopf und bekam:

          mixpi-mitschnitt hat 8000 ms nicht geantwortet

      Der Wirt gibt jedem Plugin-Aufruf acht Sekunden. Ein `ffmpeg`-Lauf je
      Datei braucht ein Vielfaches, und der Bestand hatte vierundsechzig.

      DIE FORM, DIE TRAEGT: der Knopf STOESST AN und kommt sofort zurueck; das
      Ergebnis liegt danach in `befinden` und im Protokoll. Dieselbe Bauform,
      die das Plugin fuers Aufnehmen ohnehin nutzt (`starten` ohne `await`).
      Dazu ein Riegel, der einen zweiten Lauf ablehnt, solange einer laeuft.

      NEBENBEI, AUS DERSELBEN STUNDE: Aktionskennungen duerfen KEINE
      Bindestriche tragen (`SCHLUESSEL`-Regel in plugin-vertrag.ts: Buchstabe
      zuerst, dann Buchstaben, Ziffern, Unterstrich). `bestand-pruefen` liess
      das GANZE Plugin abweisen — nicht nur den Knopf. Der Mitschnitt war
      offline, und 212 gruene Zeugen des Plugins merkten nichts davon: sie
      pruefen den Code, nicht das Manifest. Seither haelt
      `plugin-vertrag.spec.ts` JEDES plugin.json im Baum gegen die echte
      Regel — die einzige Datei, die ein Plugin komplett ausschalten kann,
      ohne dass eine Zeile Code falsch waere.
    related: ["was-in-einer-unreinen-funktion-steht-hat-keinen-zeugen", "messwerkzeuge-hingen-nicht-an-pruefen-sh"]

  - kind: lesson
    id: vorlaufstille-ist-gesund
    title: "Stille am Anfang ist gesund, Stille am Ende ist der Fehler"
    sev: med
    tags: [mitschnitt, messen, ffmpeg]
    body: |
      Beim Suchen kaputter Mitschnitte ist `silence_start` das schaerfste
      Merkmal — aber nur mit dem richtigen Vorzeichen:

          silence_start fehlt   keine Stille gefunden ................. gut
          silence_start: 0      VORLAUFSTILLE, dann Ton — der Normalfall  gut
          silence_start: 119    Stille ab der Mitte bis zum Ende ... KAPUTT

      Jede gesunde Aufnahme der Nachtcharge vom 21.08.2026 beginnt mit
      `silence_start: 0`. Wer das fuer einen Fehler haelt, wirft einunddreissig
      einwandfreie Dateien weg.

      Der Anteil muss ins Gewicht fallen: Hoerspiele enden oft leise, und ein
      Ausklang ist kein Defekt. Bei 15 % gemessen liegt die Grenze richtig —
      die Fundstuecke lagen bei 18 %, 26 %, 44 % und 77 %, ein normaler
      Ausklang deutlich darunter.

      UND DER MITTELWERT VERRAET NICHTS. „So viele Feelings" misst -17,5 dB —
      ein voellig normaler Pegel — und ist zu 44 % digitale Null. Wer nur den
      Schnitt prueft, findet genau die Dateien nicht, um die es geht.
    related: ["mitschnitt-nahm-den-lautsprecher-auf"]

  - kind: lesson
    id: kompositum-am-schraegstrich-ist-kein-pfad
    title: "`scripts/-Ordner` ist kein Pfad — deutsche Prosa haengt die Sache an den Ordner"
    sev: low
    tags: [doku, wachen, tools, regex, deutsch, 2026]
    match: "TOT: .*-Ordner|/-Ordner|doku-pfade-pruefen|Kompositum.*Pfad"
    body: |
      Der Doku-Lauf 29.08.2026 fand GENAU EINE Luecke, und sie war keine:

          TOT: scripts/-Ordner   (genannt in llmwiki:audit-2026-08-29-kritiker)

      `scripts/` liegt seit jeher im Baum. Gemeldet wurde ein Satz aus dem
      Kritiker-Lauf von gestern: „autosetup.sh schiebt den scripts/-Ordner
      WHOLESALE nach /usr/local/bin". Deutsche Prosa haengt die Sache direkt
      an den Ordnernamen, und `PFAD` in `tools/doku-pfade-pruefen.py` liest
      daraus `scripts/-Ordner`.

      ══ WARUM KEINE EINZELAUSNAHME ════════════════════════════════════════
      Die Datei fuehrt zwei Komposita schon als Einzelfall
      (`config/templates-Vorlagen`, `autosetup/make-boot-sd-Kette`) mit dem
      Kommentar, eine generische Regel gebe es nicht. Fuer DIESE Sorte gibt
      es sie doch — und das ist der Unterschied:

          config/templates-Vorlagen   Kompositum auf einem ECHTEN Pfad
                                      → nur am Satz zu erkennen, Einzelfall
          scripts/-Ordner             Bindestrich DIREKT hinter dem Schraeg-
                                      strich → im Baum unmoeglich, Sorte

      `git ls-files | grep '/-'` zaehlt NULL. Ein Segment, das hinter dem
      Schraegstrich mit `-` anfaengt, kann kein Ort sein. Die Wache prueft
      deshalb das MERKMAL, nicht den Pfad: eine Ausnahme verschwindet mit dem
      einen Satz, den sie stumm schaltet — eine Sortenregel bleibt.

      ══ ES WAREN SIEBEN GESCHWISTER, NICHT EINS ═══════════════════════════
      Die Suche nach der Sorte statt nach dem Fund brachte in Prosa und Paket
      acht Stellen derselben Form ans Licht:

          tools/-Inventur     tools/-Dateien      api/-Pfade
          func/-Dateien       player/-Antworten   documentation/-Umzug
          ard/-Befehl         scripts/-Ordner

      Nur `scripts/-Ordner` schlug an, weil die anderen unter keinem der
      gewachten obersten Ordner liegen oder in einer uebersprungenen
      Momentaufnahme stehen. Als Einzelausnahme eingetragen waere die Wache
      beim naechsten Umbau von OBERSTE sofort wieder rot geworden — mit dem
      naechsten Geschwister.

      ══ GEGENPROBE ════════════════════════════════════════════════════════
      Eine Regel, die etwas verschweigt, muss zeigen, dass sie nicht ZU viel
      verschweigt. In die README gehaengt und wieder entfernt:

          tools/gibt-es-nicht-wirklich.py   → weiter gemeldet   (richtig)
          scripts/-Testkompositum           → still             (richtig)

      Ohne diesen zweiten Teil waere „KEINE LUECKE" nicht zu unterscheiden
      von einer Wache, die nichts mehr sieht. Vgl.
      [[gegenprobe-statt-gruen-glauben]].

      ══ ZWEI BEFUNDE, DIE KEINE WAREN ═════════════════════════════════════
      Im selben Lauf gegengeprueft und VERWORFEN, damit sie nicht beim
      naechsten Mal wieder Zeit kosten:

      * Zwoelf `tools/*.sh` tragen kein Ausfuehrbit (u. a. die Doku-Probe
        selbst). Kein Fund: alle zwoelf werden in der Doku ausnahmslos als
        `bash tools/…` aufgerufen, nie als `./tools/…`. Das Ausfuehrbit
        fehlt nur dort, wo es niemand braucht — dokumentierte Absicht, keine
        Luecke.
      * `tools/verwaltung-vorschau.mjs` ohne Ausfuehrbit sieht nach demselben
        Fehler aus. Es ist eine Proxy-KONFIGURATION (`ng serve
        --proxy-config`), die niemand ausfuehrt; der Treffer entstand nur,
        weil `../../tools/…` wie `./tools/…` aussieht.

  - kind: lesson
    id: knopf-ohne-ort-ist-kein-knopf
    title: "Zwei Plugin-Aktionen hatten Knoepfe, die niemand druecken konnte"
    sev: med
    tags: [plugin, verwaltung, angular, vertrag]
    body: |
      Ein Plugin meldet seine Knoepfe im Manifest an (`aktionen`), der Wirt
      reicht sie durch, die Route `POST /api/plugins/:kennung/aktion/:aktion`
      nimmt sie an — und gezeichnet wurden sie von genau EINER Komponente:
      `mixpi-plugin-abschnitt`. Die haengt an genau EINER Stelle im ganzen
      Baum (`streaming.ts`) und filtert auf `sektion === 'streaming/<anbieter>'`.

      AM 29.08.2026 NACHGEZAEHLT: zwei Plugins hatten damit unerreichbare
      Knoepfe.
        * `mixpi-mitschnitt` — zwei Aktionen, gar keine `sektion`
        * `mixpi-archive`    — `sektion: medien`, aber `medien` ist NIRGENDS
                               eingehaengt

      NICHTS DARAN WAR KAPUTT, und genau deshalb fiel es nicht auf: das
      Manifest war gueltig, das Plugin lud, die Route antwortete, `curl` tat
      es. Es fehlte nur der ORT. Ein Knopf ohne Ort ist kein Knopf.

      ══ WAS DAGEGEN HILFT ══════════════════════════════════════════════════
      Die Aktionen stehen jetzt auf der Seite `/plugins/<kennung>` der
      Verwaltung — die gehoert JEDEM Plugin und braucht keine Sektion. Damit
      kann eine Aktion nicht mehr verwaisen; die Sektionen bleiben fuer den
      Fall, dass ein Knopf ZUSAETZLICH dort stehen soll, wo man ihn gerade
      braucht.

      Und sie stehen in der VERWALTUNG, nicht auf der Box: sie liegt hinter
      `torBauen(…anmeldungNoetig…)`. Auf der Kinderoberflaeche haben
      Wartungsknoepfe nichts zu suchen.

      ══ DIE ALLGEMEINE FORM ════════════════════════════════════════════════
      Wo etwas sich ANMELDEN kann, muss es genau eine Stelle geben, die alles
      Angemeldete auch ZEIGT — sonst haengt die Sichtbarkeit daran, dass
      jemand daran gedacht hat, einen zweiten Ort einzuhaengen. Eine Liste,
      die nur zeigt, was zufaellig gefiltert wird, verschweigt den Rest
      lautlos.
    related: ["plugin-aktion-hat-acht-sekunden", "messwerkzeuge-hingen-nicht-an-pruefen-sh"]

  - kind: lesson
    id: befund-im-paket-instrument-nicht-genannt
    title: "Der Befund steht im Paket, das Messgeraet dazu nennt niemand"
    sev: med
    tags: [doku, werkzeug, paket, messen, prozess]
    body: |
      Doku-Lauf 29.08.2026. Die Luecken-Probe war gruen, `ungerufene-wachen.py`
      war gruen — und trotzdem fehlte etwas, das keine der beiden fragt.

      GEFUNDEN AN DEN WERKZEUGEN DER LETZTEN SIEBEN TAGE: 55 neu, vier davon
      in KEINER Doku und in keinem Paketeintrag genannt.

          tools/lautstaerke-einheit-probe.sh        haelt genau die Aussage von
                                                   `genau-eine-stufe-regelt-den-ton`
          tools/lautstaerke-zieher-probe.sh         beantwortet den NAECHSTEN
          tools/box/lautstaerke-schreiber-finden.sh SCHRITT von
                                                   `klangwerk-senke-nimmt-keine-lautstaerke`
          tools/archive-probe.mjs                   die Vormessung der fremden
                                                   Schnittstelle vor E88

      DER SCHADEN IST NICHT DER FEHLENDE LINK. Der Eintrag behauptet etwas,
      das jemand GEMESSEN hat, und verschweigt, WOMIT. Wer nachmessen will,
      baut das Instrument neu — und misst dabei mit ziemlicher Sicherheit
      etwas anderes. Beim offenen Punkt ist es noch teurer: dort stand
      woertlich „NAECHSTER SCHRITT: die beiden Knoten vergleichen", und die
      zwei streng lesenden Proben dafuer lagen fertig im Baum.

      ══ WARUM DARAUS KEINE WACHE WIRD ══════════════════════════════════════
      Die naheliegende Wache — „jedes Werkzeug mit datiertem Befund im Kopf
      muss irgendwo genannt sein" — wurde gebaut und VERWORFEN, weil sie
      gemessen dauerrot ist: von 280 datierten Werkzeugen nennt die Doku 89
      nicht. Die allermeisten davon zu Recht; ein `kinder-platz-messen.mjs`
      hat als Ergebnis ein Bild, keinen Satz. Eine Wache, die 89 Zeilen
      ausgibt, von denen vier zaehlen, verdeckt die vier
      (llmwiki: `dauerrote-wache-ist-keine`).

      ES GILT ALSO DIE ENGERE REGEL, und sie greift beim SCHREIBEN, nicht im
      Lauf: WER EINEN BEFUND INS PAKET SCHREIBT, NENNT IM SELBEN EINTRAG DAS
      WERKZEUG, MIT DEM ER IHN GEMESSEN HAT. `genau-eine-stufe-regelt-den-ton`
      hat das fuer `tonweg-pegel.mjs` von Anfang an getan und fuer die Probe
      derselben Aussage nicht — der Unterschied ist reine Aufmerksamkeit, und
      genau deshalb steht die Regel hier statt in einem Skript.

      ══ WIE MAN ES NACHZAEHLT, wenn man doch einmal will ═══════════════════
      Kein Werkzeug, drei Zeilen; die Doku-Ablagen sind `*.md` im Baum plus
      `llmwiki/pack.yaml`. Und eine Warnung aus dem eigenen Lauf: in `fish`
      geht `docs=$(find …)` STILL leer aus. Der Korpus war dann 0 Bytes, und
      die Zaehlung meldete 280 von 280 „ohne Nennung" — eine Zahl, die wie ein
      Befund aussieht und nur bedeutet, dass die Suchflaeche leer war
      (llmwiki: `fish-kennt-kein-var-praefix`). ALLES ROT IST SO VERDAECHTIG
      WIE ALLES GRUEN.
    related: ["messwerkzeuge-hingen-nicht-an-pruefen-sh", "dauerrote-wache-ist-keine", "genau-eine-stufe-regelt-den-ton", "klangwerk-senke-nimmt-keine-lautstaerke"]
  - kind: lesson
    id: zwillings-angleich-hat-keine-feste-richtung
    title: "Zwillinge angleichen heisst erst LESEN, wer weiter ist - die Richtung wechselt je Paar"
    sev: med
    tags: [zwillinge, installer, ausrollweg, prozess]
    body: |
      Beim Abtrag der Zwillings-Befunde am 29.08.2026 (drei Paare, alle von
      `tools/zwillinge-nach-ziel.py` gemeldet) lag die naheliegende Regel
      "das box-Repo ist die Quelle, der Installer zieht nach" zweimal richtig
      und einmal FALSCH:

          bootwache.py           box 808 Zeilen (Totmannschalter ausgebaut)
                                 installer 197 Zeilen        -> Box gewinnt
          mupibox-boot-splash.py box mit teiler-Parameter und
                                 Kiosk-Marke, installer alt  -> Box gewinnt
          asound.conf            box-VORLAGE plughw:0 (der belegte
                                 58-Neustarts-Vorfall vom 25.07.!),
                                 installer Karte-per-NAMEN   -> INSTALLER gewinnt

      Die Installer-Fassung von asound.conf entstand NACH dem Vorfall und
      traegt die Begruendung im Kopf; die Box-Vorlage unter config/templates/
      war nie nachgezogen worden. Wer hier "Richtung Box->Installer" als
      Automatismus gefahren waere, haette die schlechtere Fassung ueber die
      bessere geschrieben und den dokumentierten Absturz auf jede per
      Installer gebaute Karte zurueckgeholt.

      DIE REGEL: Ein Zwillings-Befund nennt zwei Staende, keine Richtung.
      Vor dem Kopieren beide Koepfe lesen - die weitergebaute Fassung erkennt
      man an datierten Begruendungen fuer das, was die andere nicht hat.
      Erst wenn die Richtung je PAAR entschieden ist, wird kopiert.
    related: ["aehnlichkeit-ist-das-gegenteil-von-schwere", "mupi-asound-hardcoded-card0", "dauerrote-wache-ist-keine"]

  - id: rueckschleife-macht-die-tor-probe-blind
    kind: pitfall
    title: "supertest kann nicht messen, ob eine Route hinter dem Anmelde-Tor steht — torBauen laesst 127.0.0.1 immer durch"
    match: "torBauen|istRueckschleife|supertest|api/wartung|MIXPI_WARTUNG_DATEI|interfacelogin"
    tags: [backend, auth, tests, doku, wachen, 2026]
    body: |
      Gefunden am 29.08.2026 im Doku-Lauf, an der frisch committeten
      Wartungsmodus-Route (fa29d5e1).

      DER FUND: `POST /api/wartung` steht in server.ts rund achtzig Zeilen
      VOR `app.use(torBauen(...))`. Express entscheidet nach Reihenfolge,
      also ist die Schreibroute ohne Anmeldung erreichbar — jeder im
      selben Netz kann die Box fuer die Kinder sperren. Der Kopfkommentar
      der Route behauptet woertlich das Gegenteil ("laeuft wie jede
      Schreibroute hinter dem Anmelde-Tor"). Eine Zeile Prosa, die die
      Zeilennummer widerlegt.

      WARUM ES KEIN TEST FING, und das ist der eigentliche Merksatz:
      `torBauen` (auth.ts:184) laesst JEDE Rueckschleifen-Adresse ohne
      Anmeldung durch — `istRueckschleife(req.socket?.remoteAddress)`.
      supertest verbindet ueber 127.0.0.1. Dort antwortet eine KORREKT
      getorte Route genauso mit 200 wie eine ungetorte. Eine supertest-
      Probe kann offen und zu grundsaetzlich nicht unterscheiden; sie
      misst an dieser Frage NICHTS, egal wie gruen sie leuchtet. Vgl.
      [[kanaltest-mit-rauschen-misst-nichts]] und
      [[sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts]].

      Zweiter, kleinerer Riegel am selben Ort: wartung.integration.spec.ts
      faehrt mit `interfacelogin: {state: false}`. Damit gibt
      `anmeldungNoetig()` false zurueck und torBauen steigt in Zeile 1
      wieder aus — das Tor ist gar nicht scharf. Wer ueber Anmeldung
      messen will, muss `state: true` UND ein Passwort setzen.

      DIE MESSUNG, die wirklich misst (tools/wartung-tor-probe.ts):
      den Server an 0.0.0.0 binden und ueber die LAN-Adresse der Maschine
      anrufen, damit remoteAddress keine Rueckschleife ist. UND EINE
      KONTROLLE MITFUEHREN: `GET /api/dienste` steht nachweislich hinter
      dem Tor und MUSS 401 antworten. Tut sie das nicht, misst der Aufbau
      nichts und die Probe meldet "BLIND" statt Entwarnung. Gemessen:
      Kontrolle 401, `POST /api/wartung` 200.

      NOCH NICHT IN doku-luecken-probe.sh EINGEHAENGT, mit Absicht: die
      Probe ist rot, solange der Mangel offen ist, und eine dauerrote
      Wache liest bald niemand mehr ([[dauerrote-wache-ist-keine]]).
      Wer die Routen hinter `app.use(torBauen(...))` schiebt, haengt sie
      im selben Zug ein. Das LESEN darf davor bleiben — es ist so harmlos
      wie /api/auth/state.
    related: ["kanaltest-mit-rauschen-misst-nichts", "sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts", "dauerrote-wache-ist-keine", "tor-aus-der-route-die-man-gerade-loeschte", "audit-2026-08-29-kritiker"]
  - kind: falle
    id: behaviorsubject-ohne-schreiber-liefert-brav-null
    title: "Bildschirm-aus-bei-Leerlauf lief NIE - sein BehaviorSubject hatte seit der SDK-Rente keinen Schreiber"
    match: "playerState|DisplayManager|checkIdleState|BehaviorSubject.*null|spieltGerade"
    sev: med
    cause: "Ein BehaviorSubject, in das niemand mehr schreibt, wirft keinen Fehler - es liefert jedem Leser hoeflich seinen Anfangswert. value?.paused === false war damit konstant false, und der Zweig dahinter lief kein einziges Mal"
    fix: "display-manager.service.ts speist sich seit 29.08.2026 aus den LEBENDEN Wegen (current$ /player/state + local$ /local, dieselben Quellen wie now-playing.ts); das Subject ist ersatzlos entfallen"
    tags: [frontend-box, rxjs, totcode, sdk-rente, 2026]
    source: "Fund des SDK-Ausbau-Laufs 29.08.2026 (AUDIT-29 Rang 2), am Code belegt"
    body: |
      Beim Ausbau der SDK-Kaskade fiel auf: `DisplayManagerService.checkIdleState()`
      las `spotifyService.playerState$.value?.paused` - ein BehaviorSubject,
      dessen einziger `.next()`-Aufrufer im Web-Playback-SDK-Listener sass.
      Der Listener wurde seit dem 27.07.2026 nie mehr verkabelt
      (initializePlayer() hatte keinen Aufrufer). Das Subject blieb auf
      seinem Anfangswert `null` stehen.

      DIE FALLE: Nichts davon war als Fehler sichtbar. Kein Kompilierfehler
      (der Typ stimmt), kein Laufzeitfehler (`null?.paused` ist undefined),
      kein roter Test (die Specs mockten den Service). Die Funktion
      "Bildschirm aus, wenn Musik laeuft und niemand bedient" tat schlicht
      seit einem Monat nichts - derselbe stille Funktionsverlust wie bei
      Telegram-je-Titel und Cast-Erkennung, nur eine Ebene tiefer.

      DIE LEHRE: Wer ein Observable/Subject loescht oder seinen Schreiber
      ausbaut, fragt nicht nur "wer LIEST es?" sondern auch andersherum -
      "welche Leser haben KEINEN Schreiber mehr?". Ein grep nach `.next(`
      je Subject beantwortet das in Sekunden. Die Audit-Zeile "injiziert
      wird er trotzdem" war das Warnsignal; dieselbe Frage an playerState$
      gestellt haette den Fund einen Monat frueher gebracht.
    related: ["pm2-reste-nach-der-systemd-umstellung", "gegenprobe-statt-gruen-glauben"]
  - kind: falle
    id: structuredclone-wirft-auf-observables
    title: "cloneDeep durch structuredClone ersetzen scheitert, wenn ein Observable im Datensatz steckt"
    match: "structuredClone|DataCloneError|could not be cloned|cloneDeep|lodash-es"
    sev: low
    cause: "structuredClone kann keine Funktionen klonen - ein RxJS-Observable traegt intern welche, und der Wurf kommt erst zur LAUFZEIT beim ersten Klonversuch"
    fix: "Erst fragen, WOZU der Klon da ist: dient er nur neuen Objekt-Identitaeten (Angulars @for trackt per Identitaet), reicht eine flache Kopie je Eintrag - this.data().map((d) => ({...d})). lodash-es kann dann trotzdem gehen"
    tags: [frontend-box, angular, rxjs, abhaengigkeiten, 2026]
    source: "Dedup-Lauf 29.08.2026 (AUDIT-23 Paragraf-1 Punkt 10), standalone in Node reproduziert"
    body: |
      Der Plan klang wie ein Selbstlaeufer: lodash-es haengt fuer genau EINEN
      cloneDeep im Baum (swiper.component.ts), structuredClone() ist
      eingebaut - Paket raus, Aufruf getauscht, fertig.

      REPRODUZIERT, WARUM DAS NICHT GEHT: SwiperData.imgSrc ist ein echtes
      RxJS-Observable (artwork.service.ts baut es mit new Observable(...)).
      structuredClone() wirft darauf DataCloneError, weil die interne
      _subscribe-Funktion nicht klonbar ist. lodash.cloneDeep tolerierte das
      nur, weil es Funktionen flach kopiert statt tief.

      DIE EIGENTLICHE FRAGE war nicht "welcher Kloner", sondern "wozu der
      Klon": der Kommentar an der Stelle sagt es selbst - er existiert nur,
      damit Angulars @for neue Objekt-Identitaeten sieht und neu malt. Dafuer
      reicht die flache Kopie je Eintrag, das Observable bleibt geteilt, und
      die bestehende Spec (toEqual, nicht toBe) beweist die Absicht.

      Verallgemeinert: Wer cloneDeep abloest, prueft die DATEN, nicht nur die
      Aufrufstelle - alles mit Funktionen, DOM-Knoten oder Klasseninstanzen
      im Bauch faellt bei structuredClone erst zur Laufzeit um, und zwar
      genau bei den Eintraegen, die im Test-Fixture gern fehlen.
    related: ["gegenprobe-statt-gruen-glauben"]
  - id: was-nie-ausgeht-faellt-der-ausgangswache-trotzdem-auf
    title: "Ein Vorschau-Server und ein Fernschalter standen als ungerufene Wachen da - beide urteilen gar nicht"
    kind: wachen
    match: "ungerufene wache neues werkzeug|vorschau-server in einem laeufer|listen haengt im laeufer|BEKANNT nachziehen|werkzeug schaltet statt zu urteilen"
    sev: medium
    tags: [wachen, werkzeuge, pruefen, doku-lauf, bekannt-liste, 2026]
    stand: "29.08.2026 gemessen; tools/ungerufene-wachen.py meldet wieder gruen, 151 erreichbar / 53 begruendet draussen"
    source: "Stuendlicher Doku-Lauf; tools/doku-luecken-probe.sh gruen, die Luecke fand erst tools/ungerufene-wachen.py"
    fix: "Beide in BEKANNT eingetragen, mit Grund. Wer ein Werkzeug mit exit-Zweig hinzufuegt, zieht BEKANNT im SELBEN Commit nach - sonst meldet der naechste Doku-Lauf es als Wache."
    body: |
      DIE DOKU-PROBE WAR GRUEN, DIE LUECKE LAG DANEBEN. `tools/doku-luecken-probe.sh`
      meldete ueber alle 40 Abschnitte KEINE LUECKE; erst
      `tools/ungerufene-wachen.py` (haengt in `tools/pruefen.sh`, nicht in der
      Doku-Probe) zeigte zwei Werkzeuge, die kein Laeufer ruft — beide am
      SELBEN Tag dazugekommen:

        * `tools/admin-vorschau.mjs` (aus dce59da2, Darstellungs-Werkbank)
        * `tools/mixpi-wartung.sh` (aus fa29d5e1, Wartungsmodus)

      KEINES VON BEIDEN IST EINE WACHE, und das ist der Punkt. Die Wache
      erkennt Werkzeuge am woertlichen Ausgang (`process.exit(1)`, `exit 1`);
      ein Vorschau-Server hat so einen Zweig fuer „Port belegt", ein
      Fernschalter reicht den Rueckgabewert von `curl` durch. Damit sehen
      beide aus wie ein Urteil auf Vorrat und sind doch keins.

      DER BOESERE VON BEIDEN IST DER SERVER. `admin-vorschau.mjs` endet mit
      `.listen()` und laeuft bis Strg-C. Waere er „zur Sicherheit" in
      `pruefen.sh` gehaengt worden — die naheliegende Reaktion auf eine rote
      Wachenmeldung —, haette der Lauf nicht rot gemeldet, sondern bis zum
      Timeout GEHANGEN. Eine ungerufene Wache einzuhaengen ist also nicht
      immer die richtige Antwort; die Frage davor lautet, ob das Werkzeug
      ueberhaupt urteilt oder nur etwas TUT (ansehen, schalten, ausliefern).

      DAMIT HAT BEKANNT ZWEI SORTEN MEHR, die vorher nur implizit da waren:
        * laeuft nicht zu Ende (Server, Beobachter) — gehoert nie in einen Laeufer
        * greift ein statt zu messen (`mixpi-wartung.sh` schickt POST
          /api/wartung an die ECHTE Box) — dasselbe Argument wie bei den
          Box-Sonden, siehe [[sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts]]

      GEGENGEPROBT, nicht geglaubt: ein erfundener Name in BEKANNT
      (`gibtesnichtmehr-erfunden.sh`) macht die Wache rot („BEKANNT ZEIGT INS
      LEERE"), RC=1. Die Sabotage lief auf einer Kopie, weil der Eintrag zu
      dem Zeitpunkt noch uncommittet war.

      DER WIEDERKEHRENDE TEIL: das ist der fuenfte Lauf, der neue ungerufene
      Werkzeuge findet (siehe [[ungerufene-wache-driftet-in-den-fehlalarm]] und
      [[namenssuche-nach-wachen-laesst-die-geschwister-stehen]]). Der Befund ist
      jedes Mal ein anderer, weil er am NEUZUGANG des Tages haengt — das ist
      Normalbetrieb der Wache, kein Ablauffehler. Was man sich sparen kann,
      ist die Runde: wer `tools/` um etwas mit exit-Zweig erweitert,
      entscheidet gleich, ob es in einen Laeufer oder mit Grund in BEKANNT
      gehoert.
    related: ["ungerufene-wache-driftet-in-den-fehlalarm", "namenssuche-nach-wachen-laesst-die-geschwister-stehen", "sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts"]
  - id: arbeitsbereich-der-nichts-deklariert-ist-unsichtbar
    title: "frontend-admin fuehrt NULL Abhaengigkeiten - und laeuft, bis die Schwester aufraeumt"
    kind: falle
    match: "karma-coverage fehlt|ng test startet nicht|Cannot find module karma|workspace hoisting|package.json ohne dependencies|phantom dependency|geliehene abhaengigkeit"
    sev: high
    tags: [npm, arbeitsbereich, abhaengigkeiten, wachen, frontend-admin, doku-lauf, 2026]
    stand: "29.08.2026 gemessen: 12 Leihen in frontend-admin, 1 in frontend-box (ionicons); alle in dokumentation/mixpibox.md 7.13 vermerkt, Aufraeumen im BACKLOG"
    source: "Stuendlicher Doku-Lauf nach ef946f34. doku-luecken-probe.sh, doku-widerruf-probe.sh und pack-abgleich.py meldeten alle sauber; der Fund kam aus dem Lesen des letzten Commits."
    cause: >
      Ein npm-Arbeitsbereich hebt alles in ein gemeinsames
      Wurzel-node_modules. Ein Bereich laeuft damit auch mit Paketen, die er
      nirgends deklariert - solange irgendein anderer sie holt.
    fix: >
      Jeder Bereich deklariert, was er benutzt; geteiltes Werkzeug gehoert in
      die Wurzel. Gewacht von tools/arbeitsbereich-abhaengigkeiten-deckung.py
      (haengt in tools/doku-luecken-probe.sh), Ausnahmetabelle in
      dokumentation/mixpibox.md 7.13.
    body: |
      DER AUSFALL ERSCHIEN DORT, WO NIEMAND ETWAS GEAENDERT HATTE. `390880f5`
      strich `karma-coverage` aus `src/frontend-box/package.json` - voellig zu
      Recht, dessen `karma.conf.js` laedt nur den istanbul-Reporter. Kaputt
      ging davon das `ng test` der VERWALTUNG. Die hat gar keine
      `karma.conf.js`; der Angular-Karma-Bauer erzeugt sich eine und laedt
      dabei fest `karma-jasmine`, `karma-chrome-launcher`,
      `karma-jasmine-html-reporter` und `karma-coverage` (Literal in
      node_modules/@angular/build/src/builders/karma/application_builder.js:604-606).
      `ef946f34` stellte das Paket in die Wurzel: der Ausfall war behoben, die
      BUCHHALTUNG nicht.

      DIE EIGENTLICHE LEHRE IST DIE UEBER DIE WACHE, NICHT DIE UEBER KARMA:

      **Ein Arbeitsbereich, der nichts deklariert, ist nicht sauber, sondern
      unsichtbar.** `src/frontend-admin/package.json` fuehrt bis heute NULL
      Abhaengigkeiten - kein `@angular/core`, kein `rxjs`, kein `karma`. Jede
      Inventur ueber `package.json` misst dort eine leere Menge und meldet
      gruen. Es ist dieselbe Bauart wie ein Ordner, den kein Handbuch nennt:
      die Wache sieht die Abwesenheit nicht, weil sie an der Anwesenheit
      entlanglaeuft. Und weil die Verwaltung der JUENGSTE Bereich ist, hat sie
      am wenigsten Prosa und am wenigsten Deklaration - der Neuzugang faellt
      durch das Raster, das um den Bestand gewachsen ist.

      DAMIT IST DER `package.json` EINES ARBEITSBEREICHS DIE NAECHSTE
      DATEI-SORTE in derselben Reihe wie [[bauanleitung-ist-eine-vierte-sorte-datei]],
      [[simulationsrezept-ist-die-sechste-sorte-datei]] und
      [[entwicklerabbild-ist-die-siebte-sorte-datei]]. Er ist weder Doku noch
      Test noch Skript, sondern eine ERKLAERUNG - und eine leere Erklaerung
      liest sich wie eine erfuellte.

      DIE ZWEI BENUTZUNGEN, DIE KEIN GREP FINDET. `karma` steht in
      `frontend-admin` in keiner einzigen Zeile Quelltext. Eine Wache, die nur
      Importe zaehlt, meldet fuer den gefaehrlichsten Teil gruen. Es sind drei
      Sorten Benutzung:

        1. der blanke Import im Quelltext
        2. was der BAUER voraussetzt (`angular.json` nennt ihn als
           `paket:ziel`; fuer `:karma` ohne eigene `karmaConfig` die fuenf
           Pakete oben)
        3. das WERKZEUG eines npm-Skripts (`ng build` braucht `@angular/cli`)

      Die Liste fuer Sorte 2 ist NICHT geraten - sie steht als Literal im
      Bauer. Eine Wache, die voraussetzt, was sie fuer plausibel haelt, ist
      dieselbe handgepflegte Prosa wie das, was sie pruefen soll.

      DIE ANDERE SORTE FUND, HAERTER ALS DIE LEIHE: `ionicons` deklariert
      KEIN Bereich und auch die Wurzel nicht. `add.page.ts` importiert es
      direkt; im Baum liegt es, weil `@ionic/angular` es mitbringt. Ein
      Nebenwerkzeug, das seine Abhaengigkeit einmal aendert, nimmt der
      Box-Oberflaeche einen Import weg, den sie fuer ihren eigenen hielt.

      WAS ALS DEKLARATION ZAEHLT, ist die Entscheidung, an der die Wache
      haengt: der eigene package.json ODER die Wurzel - bewusst NICHT der
      Schwesterbereich, denn genau der ist der Gegenstand. Und gemessen wird
      gegen die geforderten Staende, nicht gegen `npm ls`: das zeigt, was
      installiert wurde, also das ERGEBNIS der Leihe, nicht die Leihe.

      DIE FALLE BEIM BAU DER WACHE (einmal aufgelaufen): `freigabe.spec.ts`
      erklaert in seinem Kopfkommentar, warum dort KEIN
      `import { describe } from 'vitest'` steht - und zitiert dabei die Zeile,
      die es nicht gibt. Die erste Fassung las das mit und meldete `vitest`
      als undeklarierte Abhaengigkeit; doppelt daneben, denn das Paket ist
      nicht einmal installiert. Dieselbe Bauart wie eine Probe, die ueber das
      eigene Zitat einer widerrufenen Aussage faellt. Kommentare gehoeren
      weggeschnitten, bevor gemessen wird - und ein `//` nur dann, wenn kein
      `:` davorsteht, sonst faellt jedes `'https://…'` mit.

      AUSNAHME IN DER DOKU, NICHT IM SKRIPT: die dreizehn offenen Leihen
      stehen als Tabelle in `dokumentation/mixpibox.md` 7.13, zwischen
      HTML-Marken, die der Leser nicht sieht. Damit steht die Erklaerung dort,
      wo sie findet, wer die package.json aufmacht - und die Wache wird wieder
      rot, wenn jemand sie loescht. Gegenrichtung mitgeprueft (`DOKU
      VERALTET`): wer eine Leihe endlich deklariert oder ihren letzten Nutzer
      loescht, ohne die Zeile zu streichen, faellt auf. Siehe
      [[dauerrote-wache-ist-keine]].

      GEGENGEPROBT, NICHT GEGLAUBT - fuenf Sabotagen auf dem committeten
      Stand, jede einzeln rot (RC=1), danach `git checkout`:
        * eine Tabellenzeile weg          -> GELIEHEN rxjs
        * die Marken weg                  -> WARNUNG, Ausnahmetabelle fehlt
        * erfundene Zeile in der Tabelle  -> DOKU VERALTET
        * rxjs deklariert, Zeile bleibt   -> DOKU VERALTET
        * neuer Import ohne Halter        -> NIRGENDS DEKLARIERT
      Die vierte ist die wichtigste: sie prueft die Gegenrichtung, ohne die
      eine Ausnahmeliste zur Ablage wird.
    related: ["dauerrote-wache-ist-keine", "skript-das-niemand-ruft-verrottet-lautlos", "gegenprobe-statt-gruen-glauben", "entwicklerabbild-ist-die-siebte-sorte-datei"]

  - id: ergaenzung-am-falschen-ende-des-eintrags-wirkt-nicht
    kind: falle
    sev: high
    title: "Ein zweites `related:` sechzig Zeilen tiefer - die Ergaenzung stand im Commit und war unsichtbar"
    match: "related.*wirkt nicht|doppelter Schluessel im Eintrag|safe_load behaelt den letzten|pack-doppelte-schluessel|Ergaenzung kommt nicht an|Wache bleibt rot trotz Aenderung"
    cause: "YAML erlaubt denselben Schluessel zweimal in einer Abbildung, und `yaml.safe_load` behaelt kommentarlos den LETZTEN. Der Eintrag `kachel-sieht-richtig-aus-und-spielt-nicht` fuehrte `related:` schon weit unten als mehrzeilige Liste; das oben hinter `tags:` angehaengte `related: [...]` wurde beim Laden verworfen."
    fix: "`tools/pack-doppelte-schluessel.py` liest den TEXT des Pakets (nicht das geladene Paket - safe_load hat die Doppelung da schon aufgeloest) und meldet jeden Schluessel, der in einem Eintrag zweimal auf Feldebene steht. Haengt in `tools/doku-luecken-probe.sh`."
    tags: [wiki, yaml, wache, falle, werkzeug, 2026]
    source: "Doku-Lauf 29.08.2026: nach dem Anhaengen blieb tools/symptom-kollision-probe.py bei derselben Gruppe rot, obwohl das related: nachweislich in der Datei stand."
    related: [wiki-doppelte-id-faellt-durch-safe-load, symptom-in-zwei-eintraegen-bleibt-unverbunden, gegenprobe-statt-gruen-glauben]
    body: |
      DIE GEFAEHRLICHE FORM EINES FEHLERS IST NICHT DIE MELDUNG, SONDERN DIE
      AENDERUNG, DIE STILL NICHT STATTFINDET. Die Zeile stand in der Datei.
      Sie stand im `git diff`. Sie waere so committet worden. Nur beim Laden
      gab es sie nicht. Weder YAML-Parser noch Diff noch Auge melden etwas -
      beide Vorkommen sind fuer sich genommen richtig.

      AUFGEFALLEN IST ES DURCH EINE ZWEITE WACHE. `symptom-kollision-probe.py`
      meldete nach der Ergaenzung unveraendert dieselbe Gruppe. Der erste
      Gedanke war, die Wache sei falsch. Der Griff, der es klaerte, war das
      Laden des Pakets statt das Lesen der Datei:

        python3 -c "import yaml; d=yaml.safe_load(open('llmwiki/pack.yaml'));
        print([e for e in d['entries'] if e['id']=='<kennung>'][0]['related'])"

      Steht dort etwas anderes als in der Datei, ist der Schluessel doppelt.
      DIE ALLGEMEINE LEHRE: eine Wache, die nach einer Aenderung UNVERAENDERT
      rot bleibt, ist der bessere Zeuge als das Diff. Sie liest das Ergebnis,
      das Diff nur die Absicht.

      VERWANDT, GLEICHE WURZEL: [[wiki-doppelte-id-faellt-durch-safe-load]] -
      dort verschluckt derselbe Mechanismus einen GANZEN Eintrag, hier nur ein
      Feld. Das Feld ist der leisere Fall, weil die Eintragszahl stimmt.

      GEGENGEPROBT auf einer Sicherung ([[gegenprobe-statt-gruen-glauben]]).
      NICHT ueber `git checkout`: das Paket trug zu dem Zeitpunkt uncommittete
      Arbeit, das Zuruecksetzen haette sie mitgerissen. Wer sabotiert, braucht
      erst ein Wegwerfziel: ein zweites `sev:` in einen Eintrag
      gesetzt -> DOPPELT mit beiden Zeilennummern und Ausgang 1; Sicherung
      zurueckgespielt -> gruen, Baum bitweise identisch. Blocktext (`|`) wird
      uebersprungen, sonst meldet die Wache Prosa, die wie ein Schluessel
      aussieht.

  - id: symptom-in-zwei-eintraegen-bleibt-unverbunden
    kind: falle
    sev: medium
    title: "Ein Symptom, zwei Eintraege, verschiedene Ursache - und der Suchende liest nur den ersten"
    match: "zwei Eintraege dieselbe Zeile|Symptom doppelt belegt|symptom-kollision-probe|halber Weg fuer den ganzen|welcher Eintrag gilt"
    cause: "Das Paket wird ueber `match:` durchsucht. Fuehren zwei Eintraege dieselbe Zeile, entscheidet die Reihenfolge, welchen der Suchende sieht - und nichts im Text sagt ihm, dass es einen zweiten gibt."
    fix: "`tools/symptom-kollision-probe.py` meldet jede mehrwortige `match:`-Alternative, die zwei Eintraege der Art `signature`/`falle` teilen, solange die sich nicht GEGENSEITIG in `related:` nennen. Haengt in `tools/doku-luecken-probe.sh`."
    tags: [wiki, suche, wache, werkzeug, falle, 2026]
    source: "Doku-Lauf 29.08.2026, nachdem am selben Tag drei solche Paare von Hand verbunden worden waren; die Wache fand elf weitere."
    related: [ergaenzung-am-falschen-ende-des-eintrags-wirkt-nicht, dauerrote-wache-ist-keine, umfangsangabe-stand-an-drei-orten-die-wache-kannte-einen]
    body: |
      DER ANLASS: drei Paare, jedes mit einer geteilten Zeile und zwei
      verschiedenen Ursachen - 'Cannot ensure player ready' (fehlende
      Zustandsdatei vs. fehlendes Widevine), 'Touch reagiert nicht'
      (Ausgangswahl vs. fehlende Unterbrechung), die durchgestrichene Wolke
      (404 vs. fehlendes ip-Feld). Bei den letzten beiden liegen die Ursachen
      sogar HINTEREINANDER: wer nur den ersten Eintrag las, richtete die Haelfte
      und suchte den Rest an der falschen Stelle.

      DAS MERKMAL IST GEMESSEN, NICHT GERATEN. Erster Entwurf: jede geteilte
      Alternative melden -> 46 Gruppen, unbrauchbar. Der Blick in die Liste
      zeigte, WARUM: die Mehrzahl waren geteilte WERKZEUGNAMEN
      ('api-doku-deckung', 'doku-luecken-probe'). Die stehen mit Absicht in
      mehreren `match:` - wer den Werkzeugnamen sucht, SOLL alles dazu finden.
      Ein Handle ist keine Verwechslungsgefahr, ein gelesener Satz schon.

        Bedingung                                    Gruppen
        ───────────────────────────────────────────  ───────
        jede geteilte Alternative                         46
        nur mehrwortige                                   17
        nur mehrwortige, nur signature/falle              14

      Die drei zwischen 17 und 14 waren Audit-Protokolle, die den Wortlaut
      eines Fundes zitieren - kein Suchender verwechselt sie. Gewaehlt wurde
      die dritte Zeile. Wer eine Schwelle setzt, ohne die Kandidaten zu
      zaehlen, waehlt zwischen Fehlalarm und Blindheit im Dunkeln.

      KEINE AUSNAHMELISTE, MIT ABSICHT. Stillgestellt wird eine Gruppe nur
      dadurch, dass die Eintraege sich GEGENSEITIG nennen - also durch die
      Reparatur selbst. Eine separate Liste wuerde altern, ohne dass es
      jemand merkt ([[dauerrote-wache-ist-keine]]). Einseitige Verweise
      zaehlen nicht: sie helfen dem nicht, der beim anderen Eintrag landet.

      SIE IST BEIM EINHAENGEN ROT (zehn offene Gruppen), und das ist eine
      Entscheidung: die Liste ist ENDLICH UND BENANNT, kein Dauerzustand. Wer
      sie stillstellt, ohne zu verbinden, verliert genau die Sorte Fund, die
      der Tag zutage gefoerdert hat.

      WAS EINE VERBINDUNG LEISTEN MUSS: nicht 'siehe auch', sondern DIE EINE
      FRAGE, DIE TRENNT, mit Befehl und beiden Ausgaengen. Beispiele im Paket:
      curl auf /api/data (Timeout vs. 200), `cat /dev/input/event*` (0 Byte vs.
      mehr), die Geraeteliste von Spotify (MuPiBox drin vs. leer),
      `librespot-konto.py` (fremdes vs. eigenes Konto).
  - id: neue-wache-bekommt-nur-die-zahl-nicht-die-prosa
    kind: falle
    title: "Eine neue Wache zieht die Umfangszahl der Doku nach - und beschreibt sich selbst nirgends"
    match: "neue Wache nicht dokumentiert|Wache ohne Abschnitt|nur die Fassung nachgezogen|doku-luecken-probe.sh gewachsen|Werkzeug ohne Handbuchstelle"
    sev: med
    cause: >
      Wer eine Wache baut, haengt sie in tools/doku-luecken-probe.sh und
      haengt den Wiki-Eintrag an. Das Anhaengen ruft
      tools/paket-angaben-nachziehen.py, und das aendert in README.md,
      dokumentation/mixpibox.md und benutzerhandbuch.html GENAU EINE Zeile:
      die Umfangsangabe. Danach steht in allen drei Prosadateien eine frische
      Zahl - und in keiner ein Wort ueber die Wache. Der Commit sieht
      dokumentiert aus, weil die Doku im Diff steht.
    fix: >
      Beim Einhaengen einer Wache dieselbe Frage stellen wie bei einem Feld:
      WO steht sie in der Prosa? Fuer Wachen am Baum ist das Kapitel 7 von
      dokumentation/mixpibox.md (7.11 bis 7.14). Ein Wiki-Eintrag ersetzt das
      nicht - er erzaehlt den FUND, das Handbuch nennt den ZUSTAND.
    tags: ['doku', 'wache', 'werkzeug', 'falle', 2026]
    source: >
      Stuendlicher Doku-Lauf 29.08.2026. tools/doku-luecken-probe.sh meldete
      KEINE LUECKE; gegengelesen wurde stattdessen der letzte Commit gegen
      die Prosa (grep nach den beiden neuen Werkzeugnamen in dokumentation/
      und README.md - null Treffer, waehrend das Schwesterwerkzeug desselben
      Tages eine eigene Handbuchstelle hat).
    related: [wache-liest-sieben-von-neunundvierzig-prosadateien, umfangsdrift-kehrt-wieder-weil-die-naht-an-einem-aufrufer-haengt]
    body: |
      DER BEFUND, an zwei Werkzeugen desselben Tages gemessen (29.08.2026):

        tools/arbeitsbereich-abhaengigkeiten-deckung.py  -> Abschnitt 7.13,
                                                            mit Tabelle
        tools/symptom-kollision-probe.py                 -> NULL Treffer in
                                                            dokumentation/
        tools/pack-doppelte-schluessel.py                -> NULL Treffer

      Alle drei kamen am selben Tag, alle drei haengen in derselben Probe.
      Der Unterschied ist keine Entscheidung, sondern ein Zufall: bei der
      einen war die Doku Teil der Arbeit, bei den anderen beiden hat das
      Nachziehen der Umfangsangabe die Prosadateien BEREITS ANGEFASST - und
      ein Diff, in dem `dokumentation/mixpibox.md` steht, liest sich wie
      erledigte Doku.

      WARUM KEINE WACHE ES FINDET. Die Wachen von
      tools/doku-luecken-probe.sh pruefen den GEGENSTAND der Anwendung -
      Endpunkte, Dienste, Ablagen, Seiten, Felder, Schalter. Ihre eigene
      Menge prueft keine: was in die Probe eingehaengt wird, ist danach
      Pruefer, nicht Prueflingsgegenstand. Dieselbe Bauart wie
      [[menuelose-seiten-stehen-in-keiner-doku-wache]] - eine Liste, die aus
      derselben Quelle stammt wie ihr Pruefgegenstand, kann den Fehlenden
      nicht vermissen; hier ist es die Wache, die sich selbst nicht sieht.

      DIE GEGENFRAGE, DIE DEN FUND MACHT, steht nicht in der Probe, sondern
      im Ablauf: nach einem gruenen Lauf den LETZTEN COMMIT gegen die Prosa
      halten. Was er an tools/ hinzugefuegt hat, muss irgendwo in Worten
      stehen; ist der einzige Doku-Anteil des Commits die Umfangszeile, ist
      die Antwort nein.

      NICHT GEBAUT, und mit Grund: eine Wache "jedes Werkzeug aus
      doku-luecken-probe.sh kommt in mixpibox.md vor" waere in einer Zeile
      erfuellbar (Namensliste im Anhang) und misst dann nichts mehr - der
      Gegenstand ist die BESCHREIBUNG, nicht der Name. Bis dafuer ein
      Merkmal gemessen ist, ist der Ablauf oben die Wache, und dieser
      Eintrag ihre Beschreibung.

  - id: wortzahl-ist-kein-ausschluss-ueber-den-gegenstand
    kind: falle
    title: "Einwortig heisst nicht Werkzeugname: ein Ausschluss ueber die Schreibweise schnitt 31 echte Kollisionen weg"
    match: "MINDESTWOERTER|einwortig|symptom-kollision-probe|Indexbegriff|Ausschluss ueber die Schreibweise"
    tags: ["wiki", "wache", "werkzeug", "falle", "doku-lauf", 2026]
    source: "Doku-Lauf 29.08.2026, 18 Uhr: beide Proben grün, deshalb die ausgeschlossene Klasse der jüngsten Wache einzeln nachgemessen"
    related: [symptom-in-zwei-eintraegen-bleibt-unverbunden, neue-wache-bekommt-nur-die-zahl-nicht-die-prosa]
    body: |
      DER ANLASS WAR EIN GRUENER LAUF. `doku-luecken-probe.sh` und
      `doku-widerruf-probe.sh` meldeten beide sauber, seit dem letzten Lauf
      gab es keinen neuen Commit. Also die Regel angewandt, eine Wache an
      ihren AUSSCHLUESSEN zu lesen statt an ihren Treffern — und die
      ausgeschlossene Klasse einzeln nachzumessen, statt der Begruendung im
      Kommentar zu glauben.

      DIE WACHE: `tools/symptom-kollision-probe.py`, tags zuvor
      `MINDESTWOERTER = 2`. Ihre Begruendung im Quelltext war ehrlich und
      gemessen: ohne die Bedingung meldete sie 46 Gruppen, „die Mehrzahl
      davon geteilte WERKZEUGNAMEN". Ein Handle ist wirklich keine
      Verwechslungsgefahr — wer `api-doku-deckung` sucht, SOLL alle
      Eintraege dazu finden.

      NUR IST DIE WORTZAHL EINE AUSSAGE UEBER DIE SCHREIBWEISE, KEINE UEBER
      DEN GEGENSTAND. Nachgemessen: von 36 einwortigen Kollisionen waren
      genau FUENF Namen aus dem Baum (`pack.yaml`, `pruefen.sh`,
      `doku-luecken-probe`, `81-bluez-nur-a2dp`, `modernization`). Die
      anderen 31 waren Code-Bezeichner, Konfigurationsschluessel und
      API-Wege:

        track_number             zwei Eintraege, zwei Zaehlweisen
        erkennung_ausfall_ms     zwei Eintraege, zwei Ursachen
        interpretenerkannt       dieselben zwei
        /api/wlan                Route haengt  vs.  Route schweigt
        weiter_anzahl            Deckel vor dem Sieb  vs.  Kachel passt nicht

      Also genau der Fall, fuer den die Wache gebaut worden war. „Die
      Mehrzahl waren Werkzeugnamen" stimmte fuer die 46-Gruppen-Messung von
      damals und war zum Merkmal erhoben worden; nachgezaehlt hat es
      niemand, weil die Wache danach gruen lief.

      ── DAS MERKMAL, DAS TRAEGT ───────────────────────────────────────
      Ist die Alternative ein NAME aus dem Baum? Dann ist sie ein
      Indexbegriff, keine Beobachtung. Gefragt wird `git ls-files` und
      nicht das Dateisystem — auf der Arbeitsmaschine liegt Ruecklass
      herum, und eine Wache, die ihn mitliest, spricht Namen frei, die kein
      anderer hat. Antwortet git nicht, meldet die Wache UNMESSBAR statt
      ersatzweise „keine Namen": eine leere Namensmenge haette jeden
      Indexbegriff zum Fund gemacht.

      IHRE BLINDHEIT, benannt statt verschwiegen: teilt ein echtes Symptom
      zufaellig den Wortlaut eines Dateinamens, sieht sie es nicht.
      `pack.yaml` steht so in drei Eintraegen mit drei Ursachen.

      ── WAS DER LAUF DAVON ABGEARBEITET HAT ───────────────────────────
      31 Kollisionen gefunden, 14 verbunden, 17 bleiben offen. Verbunden
      wurden zuerst die, deren `body` den Zwilling laengst NANNTE und denen
      nur der `related:`-Eintrag fehlte — die Verbindung war da, nur nicht
      fuer den, der beim anderen Eintrag landet. Zwei Cluster haben
      zusaetzlich die trennende Frage im Kopf bekommen: die vier
      `track_number`-Eintraege (welche Oberflaeche, welche Stufe) und die
      drei `routerLinkActive`-Eintraege (Fall, Regel oder Messluecke).

      Das Nachtragen selbst macht jetzt `tools/pack-zwillinge-verbinden.py`
      — zeilenweise, weil `yaml.safe_dump` die Kommentarzeilen des Pakets
      loescht, und mit `safe_load` plus Kommentarzaehlung gegengelesen,
      bevor es schreibt. Den `body` fasst es absichtlich NICHT an: ein
      Verweis ohne den trennenden Satz ist die billige Erfuellung der
      Wache, nicht ihre Absicht.
  - id: umweg-im-fix-ueberlebt-seine-reparatur
    kind: falle
    title: "Ein `fix:`, der ein UMWEG ist, wird von der Reparatur nicht widerrufen — er bleibt als Rat stehen"
    match: "nie tief einsteigen|Umweg statt Reparatur|fix ist ein Rat|Umgehung im Eintrag|veralteter Rat"
    sev: med
    tags: [wiki, doku, wachen, falle, 2026]
    source: "29.08.2026, Doku-Lauf: beim Verbinden der Symptom-Kollision \"player.*json\" aufgefallen; gegengelesen an src/backend-api/src/server.ts (istSeitenaufruf, Zeile ~2187)"
    related: [player-pfad-ist-im-backend-belegt, player-route-liefert-json, symptom-in-zwei-eintraegen-bleibt-unverbunden]
    body: |
      ZWEI EINTRAEGE ZUM SELBEN PFAD, EIN JAHR AUSEINANDER:

        player-pfad-ist-im-backend-belegt  27.07.2026
          fix: "nie auf /player tief einsteigen"        <- ein UMWEG
        player-route-liefert-json          spaeter
          fix: Accept/Sec-Fetch-Mode-Weiche im Backend  <- eine REPARATUR

      Die Weiche ist gebaut (`istSeitenaufruf` in
      src/backend-api/src/server.ts, gerufen aus `app.use('/player', …)`).
      Der Tiefeneinstieg funktioniert damit — und der Rat „nie tief
      einsteigen" stand trotzdem unveraendert da.

      ── DIE BAUART ────────────────────────────────────────────────────
      Eine Reparatur widerruft die URSACHE, aber niemand geht zurueck und
      nimmt den Rat zurueck, der aus ihr folgte. Der Umweg-Eintrag wird
      dabei nicht falsch im Kern — die Beobachtung stimmt weiter, und
      seine allgemeine Regel („eine Backend-Route kann einen
      Frontend-Pfad verdecken, `grep \"app.use('/\"`") gilt unveraendert.
      Falsch ist nur die HANDLUNGSANWEISUNG, und genau die liest man
      zuerst.

      ── WARUM KEINE PROBE DAS FINDET ──────────────────────────────────
      `tools/doku-widerruf-probe.sh` sucht Aussagen, die anderswo
      ausdruecklich widerrufen sind. Hier hat NIEMAND widerrufen: der
      zweite Eintrag beschreibt seine eigene Reparatur, ohne den ersten
      zu erwaehnen. Aufgefallen ist es erst, als
      `tools/symptom-kollision-probe.py` beide wegen „player…json" in
      EINE Gruppe warf und nach der trennenden Frage verlangte. Beim
      Formulieren dieser Frage muss man den heutigen Stand am Baum
      nachsehen — und dabei faellt der veraltete Rat auf.

      ── DIE FRAGE FUER JEDEN EINTRAG MIT EINEM UMWEG IM `fix:` ────────
      Ist das eine Reparatur oder eine Vermeidung? Steht dort eine
      Vermeidung („nie X tun", „stattdessen Y aufrufen", „von Hand
      nachlegen"), gehoert die Naht, an der die Reparatur haengen
      WUERDE, in den Eintrag — mit dem Befehl, der sie nachsieht. Dann
      merkt der naechste Leser in einer Zeile, dass der Umweg nicht mehr
      noetig ist. Hier: `grep -n istSeitenaufruf
      src/backend-api/src/server.ts`.
  - id: medien-zentrale-suche-speist-alle-siebe
    kind: entscheidung
    title: "Medien-Seite: EIN Suchfeld oben speist alle fuenf Kaesten — tippen siebt oertlich, Enter geht ans Netz"
    match: "zentrale Suche|ueberall suchen|fuenf Suchfelder|Suchfeld oben|Medien unuebersichtlich"
    sev: low
    tags: [verwaltung, medien, suche, bedienung, 2026]
    source: "29.08.2026, Betreiberwunsch: 'unuebersichtlich, ich haette gerne eine zentrale Suche oben'; gebaut in src/frontend-admin/src/app/seiten/medien.ts, belegt durch medien.zentrale-suche.spec.ts (181/181 gruen)"
    related: [medienverwaltung, neue-ueberschrift-erbt-fremde-sucheintraege]
    body: |
      Die Medien-Seite hatte FUENF Suchfelder mit je eigenem Wirkungskreis
      und eigener Ausloesung: Meta-Suche (Netz, Enter), Internet Archive
      (Netz, Enter), Regal-Sieb und Sender-Sieb (oertlich, je Tastendruck)
      und der Bibliotheksfilter (oertlich, je Tastendruck). Wer nicht
      wusste, in WELCHEM Kasten das Gesuchte wohnt, konnte nicht tippen —
      wer "Maus" in die Meta-Suche tippte, fand den Maus-Radiosender nie.

      ── DIE BAUART ────────────────────────────────────────────────────
      Ein Feld direkt unter der Ueberschrift (klebt per position:sticky):

        TIPPEN   speist alle oertlichen Siebe zugleich (filter,
                 regalSuche, senderSuche, archivSuche, suchtext) —
                 kein Netzabruf.
        ENTER    feuert die zwei Netz-Suchen (Dienste ueber
                 /api/medien/suche, Archiv uebers Plugin) und holt die
                 Sender EINMALIG nach, wenn sie noch nicht liegen.
        DARUNTER je Rubrik ein Zaehler-Knopf (Bibliothek/Dienste/Sender/
                 Regal/Archiv) als Absprung: scrollIntoView auf
                 anker-ids an den h2s.

      Die Einzelfelder in den Abschnitten BLEIBEN: sie tragen
      Einstellungen (Art/Dienst-Auswahl der Meta-Suche), die eine
      schlichte Leiste nicht tragen soll.

      ── DIE FALLEN BEIM WEITERBAUEN ───────────────────────────────────
      1. Das Template dieser Seite duldet KEINE Backticks (der
         Suchbestand-Generator schneidet an ihnen ab), und die
         h2-Beschriftungen sind Bestands-Bereiche: nach jeder Aenderung
         `node tools/verwaltung-suchbestand.mjs --schreiben`, sonst
         schlaegt verwaltung-suche-vollstaendig.test.mjs an.
      2. Die Zusagen des Feldes stehen als Tests in
         medien.zentrale-suche.spec.ts — wer die Verkabelung aendert
         (z. B. ein sechstes Suchfeld ergaenzt), traegt es DORT nach,
         sonst prueft der Test eine alte Zusage.
  - id: api-medien-wendet-zuordnungen-an
    kind: entscheidung
    title: "GET /api/medien gruppiert nicht mehr nur heuristisch — bestaetigte Zuordnungen wirken, getrennt gewinnt"
    match: "Dopplungen in der Verwaltung|doppelte Zeilen Medien|gruppiereMitVerschmelzung|festschreiben|Mitschnitt doppelt"
    sev: med
    tags: [verwaltung, medien, verschmelzung, mitschnitt, backend, 2026]
    source: "29.08.2026, Betreiber-Befund 'Dopplungen jetzt da': am Geraet gemessen 70 Verwaltungszeilen bei 37 Eintraegen, 12 inhaltliche Paare fast alle spotify+lokal (Mitschnitt-Kacheln)"
    related: [quellen-verschmelzen, mitschnitt-stummel-kachel-vor-inhalt, medienverwaltung, mupi-kerntests-node26]
    body: |
      DER BEFUND: Die Mitschnitt-Flotte legt je aufgenommenem Spotify-Album
      eine zweite Kachel type library an - mit dem Spotify-ORIGINALnamen
      und notfalls artist Unbekannt. Die Medien-Ansicht der Verwaltung
      gruppierte NUR heuristisch (gruppiereTreffer): war der Spotify-
      Eintrag umbenannt oder wich der Interpret ab, zerfiel das Paar in
      zwei sichtbare Zeilen. Selbst unter /verschmelzung BESTAETIGTE
      Paare blieben in der Verwaltung zwei Zeilen - der Schalter wirkte
      nur auf /api/werke (die Box), nie auf die Anzeige der Verwaltung.

      ── DIE ZWEI WURZEL-FIXE (29.08.2026) ─────────────────────────────
      W1  gruppiereMitVerschmelzung() (src/backend-api/src/medien.ts)
          ruft gruppiereTreffer unveraendert und korrigiert danach per
          Union-Find: bestaetigte Zuordnungen (jede Stufe, auch hand)
          verbinden Schluessel auch ueber heuristisch verschiedene
          Gruppen; GETRENNTE Paare werden einzeln herausgeloest - eine
          bewusste Trennung schlaegt jede Aehnlichkeit. auchIn wird aus
          den ENDGUELTIGEN Gruppen neu gebildet. Ohne Angaben ist das
          Ergebnis identisch mit gruppiereTreffer (Test belegt das).
          Nur GET /api/medien nutzt sie; /api/medien/suche und
          /:schluessel/andere bleiben reine Heuristik (Suchtreffer haben
          keinen Platz in der Ablage).
      W2  Der Mitschnitt RAET nicht mehr: kachelAnlegen() schreibt die
          Zuordnung zum Spotify-Quell-Eintrag fest, ueber den NEUEN
          Endpunkt POST /api/verschmelzung/festschreiben (Body
          schluessel/auch, Stufe immer hand, idempotent). Die Quelle
          kennt er aus context.uri/context.type des laufenden Stuecks -
          nicht aus der Track-URI, die traegt die falsche Kennung.
          Vorher gab es KEINEN Schreibweg fuer eine beliebige
          Hand-Zuordnung: /api/verschmelzung/verbinden hebt nur eine
          Trennung auf. handVerbinden() (abgleich.ts) traegt beides.

      ── WAS OFFEN BLEIBT ──────────────────────────────────────────────
      Der BESTAND am Geraet (die 12 gemessenen Paare) ordnet sich nicht
      von selbst zu: die alten Kacheln entstanden ohne Festschreibung.
      Nach dem naechsten Ausrollen entweder unter /verschmelzung von
      Hand bestaetigen oder per POST /api/verschmelzung/festschreiben
      nachtragen (Schluessel aus /api/medien ablesen).

      ── DIE NEBENFUNDE DES UMBAUS ─────────────────────────────────────
      1. Ein ECHTES NUL-Byte statt der sechs Zeichen \u0000 in einer
         TypeScript-Datei macht sie fuer file/grep zur Binaerdatei -
         ugrep verweigert dann jede Suche in ihr. Byteweise finden:
         python3, data.find auf das Nullbyte.
      2. mock.method(http, request, ...) aus node:test aendert einen
         bereits als benannten ESM-Import gebundenen Namen NICHT
         (Node 26.7.0, nachgemessen: same ref? false). Wer eine
         Funktion testen will, die intern http.request ruft, braucht
         eine INJIZIERBARE Abhaengigkeit (Default-Parameter statt
         Modul-Mock) - so gebaut in verschmelzungFestschreiben() des
         Mitschnitts (viertes Argument schicken = serverSchicken).
  - id: oberflaechen-wache-ist-innerhalb-der-oberflaeche-blind
    kind: falle
    sev: medium
    title: Das Handbuch schickte zum Archiv auf die Plugin-Seite — die Knoepfe sitzen
      auf der Medien-Seite, und die Beschriftungs-Wache meldete gruen
    match: Verwaltung . Plugins . Internet Archive|archiv suchen aufnehmen|knopf nicht
      gefunden verwaltung|beschriftungen-deckung meldet gruen|falsche verwaltungsseite
    symptom: Man folgt dem Benutzerhandbuch zu `Verwaltung → Plugins → Internet Archive`,
      um ein Hoerspiel zu suchen, und findet dort nur `Eingeschaltet`, `Einstellungen`
      und `Aktionen` — kein Suchfeld, keinen Aufnehmen-Knopf.
    cause: '`dokumentation/benutzerhandbuch.html` nannte den Weg zur Plugin-Unterseite
      und zaehlte darunter die Schritte „auf **Suchen** klicken" und „auf **Aufnehmen**
      klicken" auf. Beide Knoepfe gibt es wirklich, aber in
      `src/frontend-admin/src/app/seiten/medien.ts` (Abschnitt `anker-archiv`,
      Zeile 1094). Die Plugin-Unterseite `seiten/plugin-eine.ts` zeichnet
      ausschliesslich vier Ueberschriften aus dem Manifest — `Eingeschaltet`,
      `Warum es nicht laedt`, `Einstellungen`, `Aktionen` — und hat nie ein Suchfeld
      gehabt. Nur EIN Satz des Absatzes war falsch: die Einstellungen (`Sprache`,
      `Treffer je Suche`) liegen tatsaechlich auf der Plugin-Seite. Die richtige
      Nachbarzeile lieh der falschen ihre Glaubwuerdigkeit.

      '
    fix: 'Im Handbuch den Weg fuer Suche und Aufnahme auf `Verwaltung → Medien`,
      Abschnitt „Internet Archive" stellen und den Einstellungs-Satz seinen eigenen
      Weg nennen lassen, statt ihn per „dort" an den vorigen zu haengen. Dauerhaft:
      `tools/handbuch-ort-deckung.py`, es haengt in `tools/doku-luecken-probe.sh`.

      '
    tags:
    - doku
    - wachen
    - handbuch
    - plugins
    - verwaltung
    - 2026
    source: '29.08.2026, stuendlicher Doku-Lauf. Am Quelltext erhoben:
      benutzerhandbuch.html:640 (alter Stand), seiten/medien.ts:1094 und 1099-1101,
      seiten/plugin-eine.ts:93/112/119/158, plugins/mixpi-archive/plugin.json.
      Gegenprobe: die neue Wache gegen `git show HEAD:dokumentation/benutzerhandbuch.html`
      meldet die zwei Zeilen und nennt medien.ts als richtigen Ort; gegen den
      korrigierten Stand gruen.'
    body: "DER EIGENTLICHE FUND IST NICHT DER SATZ, SONDERN WARUM VIERZIG WACHEN\n\
      ihn durchliessen. `tools/beschriftungen-deckung.py` prueft genau diese Sorte —\n\
      zitiert das Handbuch einen Knopf, den es gibt, und sitzt er dort, wohin der\n\
      Text schickt? Es beantwortet die zweite Frage aber nur bis zur OBERFLAECHE:\n\
      steht die Beschriftung in `frontend-admin` oder bloss in `frontend-box`?\n\
      „Suchen\" und „Aufnehmen\" stehen in `frontend-admin`. Also gruen.\n\n\
      Innerhalb der Verwaltung kann diese Wache die Plugin-Unterseite nicht von der\n\
      Medien-Seite unterscheiden. **Ein Befund traegt die Schicht, in der gemessen\n\
      wurde** — „der Knopf ist in der Verwaltung\" und „der Knopf ist auf DIESER\n\
      Seite der Verwaltung\" sind zwei Saetze, und das Handbuch behauptet den\n\
      zweiten.\n\n\
      DIE NEUE WACHE misst die Seite. Menuepunkt → Weg kommt aus `rahmen.ts`,\n\
      Weg → Bauteil aus `app.routes.ts` (eine weitere Stufe hinter dem Menuepunkt\n\
      trifft die Kind-Route, `plugins/:kennung` → `plugin-eine.ts`). Gegenstand ist\n\
      die Prosa des Handbuchs, die Liste kommt aus dem Code — nicht tautologisch.\n\n\
      IHR AUSSCHNITT, eng und mit Absicht: nur ein `<ol class=\"schritte\">`, dessen\n\
      einleitender Absatz einen `<span class=\"pfad\">` mit einem Menuepunkt der\n\
      Verwaltung traegt. Nur dort ist Fettdruck verlaesslich ein Bedienelement; die\n\
      uebrigen Schritt-Listen des Handbuchs beschreiben Hardware-Montage und das\n\
      Einbacken der Karte und nennen gar keinen Weg.\n\n\
      IHRE RICHTUNG, und was sie ausdruecklich NICHT findet: gemeldet wird nur, was\n\
      anderswo in der Verwaltung ein Bedienelement IST, aber nicht auf der genannten\n\
      Seite. Fettdruck, der nirgends ein Bedienelement ist, bleibt still — im\n\
      Zweifel Betonung („weder Konto noch Server im Haus\"). Erfundene\n\
      Beschriftungen bleiben Sache der Schwesterwache, die dafuer die\n\
      Anfuehrungszeichen liest.\n\n\
      NICHT nach Wortzahl ausgeschlossen, obwohl es hier nahelag: der lange\n\
      Fettdruck „Suchfeld ganz oben auf der Medien-Seite\" ist Prosa, der kurze\n\
      „Suchen\" ein Knopf. Ein Ausschluss ueber die Schreibweise ist trotzdem keiner\n\
      ueber den Gegenstand (siehe `wortzahl-ist-kein-ausschluss-ueber-den-gegenstand`).\n\
      Der Ausschluss lautet deshalb „ist anderswo ein Bedienelement\" und ist am Baum\n\
      gemessen.\n\n\
      BEIM BAU SOFORT SELBST AUFGELAUFEN: die Wache kam mit einer Ausnahme fuer den\n\
      Box-Weg „Eltern-Bereich → Darstellung → Gesten\" auf die Welt. Sie meldete sie\n\
      im ersten Lauf als „Ausnahme ohne Fall\" — der Weg steht in keiner\n\
      Schritt-Liste, die Ausnahme war aus dem Kopf erfunden statt am Baum gemessen.\n\
      Gestrichen. Wege fremder Oberflaechen fallen ohnehin schon durch die\n\
      Menuepunkt-Frage.\n\n\
      NEBENBEI MITGENOMMEN: die zentrale Suche der Medien-Seite (Fassung 385) war\n\
      dem Betreiber-Handbuch nur als Kopfzahl nachgezogen worden, nicht als\n\
      Bedienung. Jetzt steht sie dort, wo jemand sie sucht — beim Abschnitt Medien,\n\
      nicht bloss im Archiv-Ablauf.\n\n\
      VERWANDT: `geschlossenes-vokabular-ohne-konsumenten` — `mixpi-archive` traegt\n\
      `\"sektion\": \"medien\"`, und `medien` haengt ins Leere. Die Seite, auf der das\n\
      Plugin wirkt, erreicht es also NICHT ueber seine Sektion; der Archiv-Abschnitt\n\
      in `medien.ts` ist fest verdrahtet. Beides zusammen erklaert, warum der\n\
      falsche Weg so plausibel aussah.\n"
  - id: inhalt-rueckfall-backend-kapselt-fusion
    kind: entscheidung
    title: "/inhalt: der Server liest den Verschmelzen-Schalter selbst und weicht selbst auf die naechste Quelle aus"
    match: "Die Folgen liessen sich nicht laden|Spotify 404 Hoerbuch|Quellen-Rueckfall|verschmelzen fehlt in der Query|inhaltFuerEintrag|waehleInhalt|erster Erfolg gewinnt|nur die aufgezeichneten Titel"
    sev: med
    tags: [backend, verschmelzung, ard, spotify, folgen, 2026]
    source: "29.08.2026 abends, live am Geraet: verschmolzenes Werk Quarks Science Cops (spotify-Hoerbuch + ard), /inhalt auf dem Fuehrer gab 502 Spotify 404, waehrend quelle=ard 60 Folgen lieferte; die Folgen-Lane zeigte 'Die Folgen liessen sich nicht laden.'"
    related: [api-medien-wendet-zuordnungen-an, quellen-verschmelzen]
    body: |
      DER BETREIBER-SATZ, der die Architektur entschied: "eigentlich
      sollte das backend doch eh fusionieren und liefern ... die ui muss
      nur titel holen das backend weiss schon woher." Vorher war beides
      der Oberflaeche aufgebuerdet:

        1. Der Verschmelzen-Schalter galt bei /inhalt nur, wenn die UI
           ihn als Query mitschickte. albumSpielen tat das - die
           Folgen-Lane (folgenOeffnen, app.js) NICHT, und ihr Aufruf
           landete beim Fuehrer-Eintrag ohne Gruppenwissen.
        2. Scheiterte die bevorzugte Quelle, gab es keinen Server-
           Rueckfall - das Ausweichen war der quelle=-Retry der UI,
           den nur der Abspielweg fuhr.

      Ein Spotify-HOERBUCH als Fuehrer (audiobookid) laesst den
      Spotify-Zweig mit 404 sterben; die ard-Quelle derselben Gruppe
      haette 60 Folgen geliefert. Ergebnis am Schirm: "Die Folgen
      liessen sich nicht laden."

      ── SEIT 29.08.2026 ───────────────────────────────────────────────
      * Fehlt die Query `verschmelzen` GANZ, liest der Handler den
        Schalter aus der Darstellung (aktuell.verschmelzen, Vorgabe
        AUS). Ein ausdrueckliches 0/1 uebersteuert IMMER - bestehende
        Aufrufer bleiben byte-gleich bedient.
      * Ohne quelle=-Wunsch nimmt der Server die Quellen der Gruppe in
        werk.quellen-Reihenfolge (inhaltFuerEintrag liefert
        {status, body} statt direkt zu antworten). Die Antwort traegt
        wie immer `dienst` - daran sieht jeder, woher es kam.
      * MIT quelle=-Wunsch weiterhin KEIN Rueckfall: wer ausdruecklich
        fragt, bekommt keine stille Ersatzantwort (sonst haengte die
        UI denselben Titel zweimal an, einmal je Quelle).

      ── NACHTRAG 30.08.2026: „ERSTER ERFOLG" GILT NICHT MEHR ──────────
      DIESER EINTRAG SAGTE BIS HIER „der erste Erfolg gewinnt". Das ist
      seit E95 Stufe 1 (Commit d00cddb0, 29.08.2026 nachts) FALSCH und
      war der zweite Fehler, den derselbe Rueckfall einschleppte: „Nah"
      (Alin Coen) fuehrt LOKAL, dessen Mitschnitt-m3u aber nur einen
      Teil der Titel traegt - der erste Erfolg war die lokale Antwort,
      und die Box zeigte nur die aufgezeichneten Stuecke, waehrend
      Spotify daneben die volle Liste hatte.

      SEITHER holt der Handler ALLE Kandidaten PARALLEL (Promise.all -
      die Zweige in inhaltFuerEintrag sind unabhaengig, keiner teilt
      Zustand), und die reine `waehleInhalt()` (verschmelzung.ts)
      entscheidet in dieser Ordnung:
        1. kein Erfolg dabei -> liste[0], also der Fehler der
           BEVORZUGTEN Quelle (dieselbe Antwort wie ohne Rueckfall),
        2. erster Erfolg mit `vollstaendig === true` in
           Bevorzugungs-Reihenfolge,
        3. sonst der Erfolg mit den MEISTEN Titeln (bei Gleichstand
           bleibt der frueheste stehen - `>` , nicht `>=`).
      VORAUSSETZUNG DAFUER, dass Regel 3 ueberhaupt greift: der lokale
      Zweig behauptet seit demselben Commit KEIN `vollstaendig` mehr -
      die m3u eines Mitschnitts ist der potenziell lueckenhafte
      BESTAND, nicht das Album. Wer das zurueckdreht, gewinnt mit
      Regel 2 wieder die kurze lokale Liste.
      Der Stufenplan, in dem das Stufe 1 von fuenf ist, steht in
      [[e95-stufenplan-titelweise-quelle]].

      Belegt in verschmelzung-inhalt-rueckfall.integration.spec.ts mit
      den ECHTEN Kennungen des Geraete-Falls (dazu Fall lokal-3 gegen
      spotify-12) und 7 Unit-Faellen fuer waehleInhalt; Gegenprobe
      gefahren (erst Rueckfall-Schleife gekappt -> Tests rot, dann Wahl
      gekappt -> 2 rot). Werkzeug fuer den Geraete-Blick:
      tools/verschmolzene-werke-probe.py.

  - id: kacheltitel-ist-ordnername-nie-zeichengleich
    kind: entscheidung
    title: "Aufnahme-Kachel und Spotify-Quelle koennen nie zeichengleich sein - der Kacheltitel IST der Ordnername"
    match: "gleiche zeichen wie spotify|Unterstrich statt Doppelpunkt|mitschnitt-zeichen-probe|NUR-FORM|unsichtbare Zeichen Kachel"
    sev: med
    tags: [mitschnitt, verschmelzung, spotify, zeichen, e93, 2026]
    source: "29.08.2026 abends, Messung an mixpibox.local mit tools/mitschnitt-zeichen-probe.py ueber /api/medien (37 Eintraege, 12 inhaltliche Paare); Code: plugins/mixpi-mitschnitt/index.mjs saeubern/albumOrdner/dateiname."
    related: [api-medien-wendet-zuordnungen-an, verweis-zeigt-auf-den-plattennamen, quellen-verschmelzen, normal-macht-aus-scharfem-s-eine-luecke]
    body: |
      DIE BETREIBERFRAGE: "noch zu pruefen ist ausserdem ob die aufnahmen
      die gleiche zeichen haben." Verdacht: unsichtbare Differenzen
      (NBSP, NFC/NFD, ss statt scharfem s) zerlegen die Paare still, und
      die Verschmelzungs-Heuristik findet nicht mehr zusammen, was
      zusammengehoert.

      DIE MESSUNG WIDERLEGT DEN VERDACHT: KEINE einzige NUR-FORM-Differenz
      zwischen lokalen Aufnahme-Kacheln und ihren Spotify-Quellen - kein
      NBSP, kein Nullbreiten-Zeichen, keine Unicode-Zerlegung, keine
      scharfes-s-Falle. Wer hier weitersucht, sucht am falschen Ort.

      WAS ES STATTDESSEN IST - und warum es kein Fehler ist: Sechs Paare
      unterscheiden sich systematisch durch `_` an genau den Stellen, wo
      der Spotify-Titel `:` oder `/` fuehrt. Das ist `saeubern()`
      (index.mjs): verbotene Pfadzeichen `<>:"/\|?*` und Steuerzeichen
      werden zu `_`, Leerraum wird eingedampft, bei 120 Zeichen ist
      Schluss, Leeres wird `Unbekannt`.

      DER GRUND, aus dem das nie heilbar ist: Der Kacheltitel IST der
      Ordnername. `albumOrdner` schreibt `saeubern(kategorie)/
      saeubern(kuenstler)/saeubern(album)` auf die Platte, `dateiname`
      denselben Wert fuer den Titel - das ist der Vertrag mit dem
      Abspieldienst. Ein Kacheltitel mit `:` waere ein Pfad, der sich
      selbst zerlegt. Die Zeichen ANZUGLEICHEN hiesse also, die
      Ordnerstruktur zu brechen; die Abweichung ist der Preis dafuer,
      dass die Aufnahme ueberhaupt auffindbar liegt - dieselbe Logik wie
      bei [[verweis-zeigt-auf-den-plattennamen]].

      FOLGE FUER DIE VERSCHMELZUNG: Diese Paare wird KEINE Heuristik ueber
      Namensgleichheit je einsammeln. Sie haelt nur die FESTGESCHRIEBENE
      Zuordnung zusammen - beim Anlegen automatisch (der Mitschnitt ruft
      `POST /api/verschmelzung/festschreiben`, siehe
      [[api-medien-wendet-zuordnungen-an]]), fuer Altbestand von Hand
      unter `/verschmelzung`.

      DAS WERKZEUG: `tools/mitschnitt-zeichen-probe.py` (Vorgabe
      `mixpibox.local`, `--box <host>`, `--datei antwort.json` fuer den
      Blick ohne Geraet). Rueckgabe 0 = keine NUR-FORM-Paare, 1 =
      mindestens eines, 2 = Box nicht erreichbar. Erst wenn diese Probe
      1 meldet, ist ein Zeichenproblem ueberhaupt die richtige Spur.
  - id: e95-stufenplan-titelweise-quelle
    kind: entscheidung
    title: "PLAN E95: EIN Werk, titelweise die beste Quelle - fuenf Stufen, am Code gerichtet"
    match: "titelweise Quelle|Titel-Merge|gemischte Warteschlange|nur die aufgezeichneten Titel|Vollstaendigkeit schlaegt Reihenfolge"
    sev: med
    tags: [plan, verschmelzung, player, mitschnitt, offline, 2026]
    source: "29.08.2026 nachts: zwei unabhaengige Entwuerfe + Richter (der Richter hat jede Anknuepfstelle am Code stichprobenhaft geprueft); Anlass die zwei Nah-Messfaelle des Betreibers (BACKLOG E95)"
    related: [inhalt-rueckfall-backend-kapselt-fusion, quellen-verschmelzen, api-medien-wendet-zuordnungen-an, mitschnitt-plan]
    body: |
      DER BETREIBER-SATZ: unvollstaendige Alben zeigen ALLE Titel; das
      Backend wechselt je Titel die Quelle (lokal/spotify/ard/...), ohne
      dass man es merkt; die Oberflaeche zeigt die spielende UND die
      verfuegbaren Quellen; kaputte Aufnahmen fallen still auf Streaming
      zurueck.

      STAND 30.08.2026: STUFE 1 IST GEBAUT (Commit d00cddb0), Stufen
      2-5 sind weiterhin Plan. Wie Stufe 1 am Ende WIRKLICH entscheidet,
      steht bei [[inhalt-rueckfall-backend-kapselt-fusion]] - dort ist
      die Beschreibung des laufenden Codes, hier die des Vorhabens.

      ── DIE FUENF STUFEN (je mit Abbruchkriterium) ────────────────────
      1  [GEBAUT 29.08.2026] VOLLSTAENDIGKEIT SCHLAEGT REIHENFOLGE (ein
         Abend). Der
         /inhalt-Rueckfall (server.ts, E93-Naht) nimmt nicht mehr den
         ersten Erfolg, sondern holt alle Kandidaten und waehlt: erste
         Quelle mit vollstaendig=true bzw. hoechster Titelzahl; meldet
         die bevorzugte WENIGER Titel als eine andere, gewinnt die
         vollere. Reine Wahlfunktion neben rang/istGanz. Kein Player-,
         kein UI-Umbau. Behebt beide Nah-Messfaelle.
      2  BEFUND-VERTRAG DES MITSCHNITTS (1-2 Tage). Das Plugin schreibt
         befunde.json NEBEN die playlist.m3u (versioniert, Schreiber:
         Annahme + bestandDurchsehen) - dieselbe Bauart wie die m3u:
         Wissen als Datei, kein Plugin-Aufruf je Abruf (8-s-Frist!).
         Der lokal-Zweig liest sie; kaputte Titel stufen die lokale
         Quelle zurueck, auch wenn sie zahlenmaessig vollstaendig ist.
      3  TITEL-MERGE + ANZEIGE (2-4 Tage). Neues reines Modul
         titel-verschmelzung.ts: Rueckgrat = vollstaendigste Quelle,
         Zuordnung Nummer+normalisierter Name, Befund-Regel; Regel 1
         (Album am Stueck, wenn EINE Quelle ganz+heil - jeder
         Maschinenwechsel kostet 1-2 s) bleibt vorgeschaltet. /inhalt
         additiv: je Titel quelle/quellen, gemischt, abspiel-Koerper.
         /tracklist liefert dazu die ROHE m3u-Zeile (path.parse
         schneidet heute die Endung ab). app.js: Quellen-Marken je
         Titel, Einzel-Tipp folgt t.quelle statt quellen[0] des Werks.
      4  WARTESCHLANGEN-VERTRAG IM PLAYER (5-8 Tage, zwei
         Geraetemessungen mit Abstand). POST /player/:raum/warteschlange
         (EIN Aufruf statt 30 Anhaenge-Befehle; Nachzuegler-Klasse
         entfaellt an der Wurzel). Abschnitte gleicher Maschine;
         mpv-Ende ueber das bisher UNGENUTZTE playlist-finish-Ereignis
         (mpv-wrapper.ts); Spotify-Titel EINZELN per uris
         [spotify:track:...] - nie context_uri, sonst spielt Spotify
         selbststaendig den naechsten; Titelende-Waechter mit den
         Luegen-Regeln aus fortschritt.ts. ACHTUNG RICHTER-FUND: die
         Kinderzeit-Tuer sitzt im API-PROXY (spielweg/istStartbefehl in
         server.ts), NICHT im Player - der POST muss durch den Proxy,
         internes Weiterschalten zaehlt nicht als Start. VOR der
         Auslieferung E94 (Lautstaerkesprung) klaeren - sonst kein
         Quellwechsel mitten im Album.
      5  WEITERHOEREN + LEISTE + VERWALTUNG (2-3 Tage). Gemerkte Stelle
         auf gemischten Werken braucht ein eigenes Einheiten-Modell
         (mpv Prozent vs. Spotify Millisekunden) - bis es steht, merkt
         ein gemischtes Werk nur die globale Titelnummer ohne Position.

      ── WAS DER RICHTER GERADEGEZOGEN HAT ─────────────────────────────
      * config/verschmelzung.json ist KEINE Repo-Datei - die Zuordnungen
        kommen zur Laufzeit vom Geraet (verschmelzungLesen()).
      * QUELLEN_REIHENFOLGE = [lokal, jellyfin, spotify] - lokal vorn
        ist der GRUND, warum die kaputte Nah-Fassung gewann.
      * istGanz() zaehlt heute unvollstaendige lokale Alben als ganz,
        weil titelDa/titelGesamt niemand fuellt (werke.ts) - Stufe 1/3
        fuellen sie aus m3u und Rueckgrat.
  - id: wiederkehrender-befund-ist-ablauffehler
    kind: note
    title: "Ein wiederkehrender Befund ist ein Ablauffehler - an der Naht beheben, nicht je Lauf neu untersuchen"
    match: "Dauerbefund|kommt bei jedem Lauf wieder|schon wieder derselbe Befund|immer wieder gemeldet"
    sev: low
    tags: [arbeitsweise, wachen, doku, 2026]
    source: "Lehre aus mehreren Doku-/Kritiker-Laeufen 08/2026; in den Arbeitsnotizen der Sitzungen laengst in Gebrauch, im Pack erst seit 29.08.2026 - ein Nachtrag des stuendlichen Doku-Laufs verwies darauf, ehe es den Eintrag gab"
    related: [dauerrote-wache-ist-keine, ungerufene-wache-driftet-in-den-fehlalarm, stillgelegtes-repo-lebt-im-fetch-cache-weiter]
    body: |
      Meldet ein Lauf (Doku-Probe, Kritiker, Pruefskript) DENSELBEN Befund
      ein zweites oder drittes Mal, ist die eigentliche Erkenntnis nicht
      der Befund - sondern dass die Wache HINTER dem Ereignis steht. Sie
      sieht das Symptom, nachdem es entstanden ist, und wird es wieder
      sehen, solange die Naht offen bleibt, durch die es entsteht.

      DIE REGEL: beim zweiten Auftreten nicht den Befund abarbeiten,
      sondern die Naht suchen und DORT beheben oder verriegeln - ein
      Schreibweg, der ins falsche Ziel zeigt, ein Ordner, der nicht weg
      ist, ein Erzeuger, der Altes neu erzeugt. Wer den Befund nur
      wegputzt, bucht dieselbe Arbeit beim naechsten Lauf wieder.

      ABGRENZUNG zu [[dauerrote-wache-ist-keine]]: dort ist die WACHE
      falsch (sie meldet dokumentierte Absicht als Fehler und faerbt sich
      dauerrot); hier ist die Wache RICHTIG und der Ablauf erzeugt den
      Fehler immer neu. Vor dem Beheben also erst entscheiden, welcher
      der beiden Faelle vorliegt.
  - id: doku-zitiert-paketstaende-die-niemand-nachhaelt
    kind: note
    title: "Die Prosa zitiert Versionspins - und keine Wache liest die Zahl neben dem Dateinamen"
    match: "package.json|Versionspin|\\^4\\.17\\.1|npm update|Abhaengigkeitsstand in der Doku"
    sev: low
    tags: [doku, wachen, abhaengigkeiten, 2026]
    source: "Stuendlicher Doku-Lauf 30.08.2026: Probe und ungerufene-wachen.py beide gruen, danach die Suche nach der ungedeckten ACHSE; neue Wache tools/paketstand-zitate-pruefen.py"
    related: [doku-zitiert-endpunkte-die-es-nicht-gibt, doku-nennt-den-pfad-und-das-falsche-verb, dauerrote-wache-ist-keine, schnittstelle-stand-in-keiner-doku-probe, wiederkehrender-befund-ist-ablauffehler]
    body: |
      Die Prosa nennt Abhaengigkeiten MIT IHREM PIN:

          Was bleibt: Express 4 stimmt (`package.json` `^4.17.1`),
          `backend-api` steht auf `^5.1.0`.          (MODERNIZATION.md:86)

          | `"protobufjs": "^7.4.0"` | steht unter dependencies … |
                                                     (BACKLOG.md:8788)

      doku-pfade-pruefen.py sieht in dieser Zeile NUR `package.json` und sagt:
      gibt es. Die Zahl daneben las bis 30.08.2026 keine Wache. Ein `npm
      update`, das den Pin hebt, laesst die Datei am Platz - der Satz wird
      still falsch, und es ist genau der Satz, aus dem eine spaetere Sitzung
      schliesst, ob der Express-Sprung noch aussteht. Derselbe Schadenstyp wie
      beim toten Schalter: keine Fehlermeldung, nur eine Auskunft, die nicht
      mehr stimmt.

      GEHALTEN SEIT 30.08.2026 durch tools/paketstand-zitate-pruefen.py, in
      tools/doku-luecken-probe.sh eingehaengt.

      ── WARUM NUR PIN-FORM, NIE EINE NACKTE ZAHL ──────────────────────
      Der Ausschlag faellt auf `^` und `~`. Das ist der Unterschied zwischen
      zwei Aussagen im selben Text:

          `spotify-web-api-node` … is abandoned (last release 4.0.0)

      Das ist eine Aussage ueber den UPSTREAM - unser Pin ist `^5.0.0`. Eine
      Wache auf nackte x.y.z meldete diesen Satz JEDEN Lauf und waere zu Recht
      ignoriert ([[dauerrote-wache-ist-keine]]). `^`/`~` schreibt nur, wer den
      Eintrag im package.json meint.

      ── WAS DER ERSTE LAUF FAND ───────────────────────────────────────
      NICHTS. Drei aufloesbare Zitate, alle richtig; eine Nennung ohne
      aufloesbaren Paketnamen (MODERNIZATION.md:87 nennt links vom Pin nur
      `backend-api`, nicht `express`) wird als ZAHL gemeldet, nicht als
      Deckung. Gegenprobe gemacht: `^4.17.1` auf `^4.99.9` verbogen, die Wache
      meldet VERALTETER PIN und Rueckgabe 1.

      ── DIE ARBEITSWEISE DAHINTER ─────────────────────────────────────
      Eine gruene Probe belegt nur, dass IHRE Wachen gruen sind. Ist die Probe
      leer UND ungerufene-wachen.py leer, ist der naechste produktive Schritt
      nicht "nichts zu tun", sondern die Frage: welche Achse hat ueberhaupt
      keine Wache? Hier war es die Zahl neben einem Dateinamen, den eine
      andere Wache schon als "existiert" abhakt - eine gedeckte Datei mit
      ungedecktem Inhalt.
  - id: verworfener-weg-bleibt-eine-einladende-anleitung
    kind: note
    title: "Eine verworfene Anleitung besteht jede Existenz-Wache - sie existiert ja, sie gilt nur nicht mehr"
    match: "build-spotifyd|spotifyd|verworfen|Riegel-Kopf|Alt-Unit|compile_scripts"
    sev: medium
    tags: [doku, wachen, backlog, 2026]
    source: "Stuendlicher Doku-Lauf 30.08.2026 03:00: Probe und ungerufene-wachen.py beide gruen, danach die Suche nach der ungedeckten ACHSE; neue Wache tools/verworfene-wege-markiert.py"
    related: [wiederkehrender-befund-ist-ablauffehler, audit-liste-ist-keine-dokumentation, doku-zitiert-paketstaende-die-niemand-nachhaelt, dauerrote-wache-ist-keine, stillgelegtes-repo-lebt-im-fetch-cache-weiter]
    body: |
      `documentation/build-spotifyd.md` ist eine vollstaendige, saubere,
      101-zeilige Bauanleitung fuer spotifyd. Sie beginnt mit einer
      Quellenangabe und einem `apt install`, und wer ihr folgt, baut ein
      Programm, das die Box nicht startet: MixPiBox spielt Spotify ueber
      **librespot**; spotifyd ist eine tote Alt-Unit, die das OS-Update sogar
      abschaltet.

      Das steht seit Wochen im BACKLOG, Abschnitt "Verworfen - nicht erneut
      recherchieren". Es stand NICHT in der Datei. Wer die Datei aufmacht,
      sieht die Entscheidung nicht - er sieht eine Anleitung.

      ── WARUM KEINE DER 45 DOKU-WACHEN DAS SAH ────────────────────────
      Alle fragen nach EXISTENZ: gibt es den Pfad, die Zeile, die
      Beschriftung, den Endpunkt, den Schalter. Ein verworfener Weg besteht
      das muehelos - seine Pfade stimmen, seine Befehle laufen, seine
      Zeilennummern passen. Die Achse "gilt das noch?" fragte keine. Derselbe
      Schadenstyp wie bei den zitierten Paketstaenden
      ([[doku-zitiert-paketstaende-die-niemand-nachhaelt]]): keine
      Fehlermeldung, nur eine Auskunft, die nicht mehr stimmt.

      ── DER ABLAUFFEHLER, NICHT DER EINZELFUND ────────────────────────
      AUDIT-2026-08-23 §6.4 forderte woertlich "Riegel-Kopf oder loeschen".
      Der Rang war gebucht, und die Buchung galt als Erledigung - nur die
      Datei wusste nichts davon und stand SIEBEN TAGE unveraendert da. Eine
      Zeile im Audit haelt niemanden auf, eine Wache schon
      ([[wiederkehrender-befund-ist-ablauffehler]]).

      GEHALTEN SEIT 30.08.2026 durch tools/verworfene-wege-markiert.py, in
      tools/doku-luecken-probe.sh eingehaengt. Betroffen und jetzt markiert:
      documentation/build-spotifyd.md und dev/compile_scripts/spotifyd.sh
      (letzteres ohne Shebang, letzte Zeile ist Prosa).

      ── DIE WACHE ERBT IHRE LISTE, SIE PFLEGT SIE NICHT ───────────────
      Sie liest die Namen aus dem Verworfen-Abschnitt des BACKLOG - der
      Stelle, an der die Entscheidung ohnehin faellt. Wer morgen etwas
      verwirft, ist mitgedeckt, ohne die Wache anzufassen. Verlangt wird im
      KOPF (erste 12 nichtleere Zeilen) ein Riegel-Wort und ein Verweis auf
      BACKLOG.md; ein Riegel in Zeile 80 erreicht den Leser nicht mehr, der
      dem ersten Codeblock folgt.

      PREIS, und er steht auch im BACKLOG: die Wache kann Aufzaehlung nicht
      von Prosa unterscheiden. JEDER Dateiname mit Endung in diesem Abschnitt
      gilt als verworfen. Beim Bauen ist genau das passiert - ein
      erklaerender Nebensatz nannte tools/pruefen.sh, und die Wache verlangte
      prompt einen Riegel in der Pruefbatterie. Die Alternative waere eine
      zweite handgepflegte Liste, und genau die laeuft auseinander.

      Kein Artefakt zum Namen zu finden ist ausdruecklich GRUEN: geloescht
      ist die andere gueltige Antwort auf "verworfen".
  - id: verweis-wache-las-nur-verfolgtes-und-verfehlte-die-neue-datei
    kind: bug
    title: "git ls-files zeigt keine neue Datei - die Verweis-Wache lief blind an genau der Datei vorbei, fuer die es sie gibt"
    match: "git ls-files|unverfolgt|untracked|toter Verweis|wiki-verweise-im-code|exclude-standard"
    sev: high
    tags: [wachen, wiki, git, ablauffehler, 2026]
    source: "Stuendlicher Doku-Lauf 30.08.2026 03:00: der Fehler von Commit bb6611b2 (02:11) trat eine Stunde spaeter sofort wieder auf"
    related: [wiederkehrender-befund-ist-ablauffehler, gegenprobe-statt-gruen-glauben, dauerrote-wache-ist-keine, verworfener-weg-bleibt-eine-einladende-anleitung]
    body: |
      `tools/wiki-verweise-im-code-pruefen.py` sammelte seine Dateien mit

          git ls-files -z

      also NUR Verfolgtes. Eine frisch geschriebene Wache ist unverfolgt, bis
      sie committet wird - und in ihrem Kopf steht der Verweis, den eine
      Sitzung AUS DEM GEDAECHTNIS hinschreibt, statt ihn im Paket
      nachzuschlagen. Die Wache lief also blind an der einen Datei vorbei, an
      der sie gebraucht wurde, und meldete gruen.

      ZWEIMAL BELEGT, eine Stunde auseinander:
        * bb6611b2 (30.08., 02:11) musste den Fehler von Hand nachbessern -
          der Kopf der neuen Wache trug einen Merkzettel-Slug.
        * Im Lauf um 03:00 sofort wieder: tools/verworfene-wege-markiert.py
          zitierte `verweis-wache-las-nur-verfolgtes-und-verfehlte-die-neue-datei`, eine Kennung, die es im
          Paket nie gab. Die Wache: KEINE LUECKE.

      Zweimal derselbe Befund heisst nicht "unaufmerksam", sondern: die Wache
      steht hinter dem Ereignis ([[wiederkehrender-befund-ist-ablauffehler]]).

      BEHOBEN 30.08.2026 an der Naht:

          git ls-files -z --cached --others --exclude-standard

      `--others` holt das Unverfolgte, `--exclude-standard` haelt draussen,
      was `.gitignore` ohnehin nicht meint (node_modules und Baugut kommen so
      gar nicht erst herein). Gemessen: 313 gepruefte Verweise vorher, 317
      nachher, und der tote Verweis fiel im selben Lauf auf - ohne Sabotage,
      am echten Baum.

      ── DIE ZWEITE LEHRE: NICHT MIT grep NACHSCHLAGEN ─────────────────
      Beim Pruefen der Verweise fiel eine eigene Fehlmessung auf.

          grep -c "^  - id: vorschau-wird-geliehen$" llmwiki/pack.yaml   -> 0

      Die Kennung EXISTIERT. Ihr Eintrag ist nur anders eingerueckt (`- ` und
      `id:` auf getrennten Zeilen, also `    id: …`). Wer die Kennung so
      pruefte, haette einen gueltigen Verweis geloescht und dabei geglaubt,
      er raeume auf. Nachgeschlagen wird ueber den Parser:

          yaml.safe_load(...)  ->  {e['id'] for e in d['entries']}

      Das Paket hat 892 Eintraege in mehr als einer Schreibweise; eine
      Suche auf Formatierung ist eine Aussage ueber die Formatierung, nicht
      ueber den Bestand.
  - id: werkzeug-erklaert-sich-selbst-und-veraltet-dabei
    kind: note
    title: "Die Aufrufzeile im eigenen Kopf ist Doku - und die einzige, die beim Umbenennen NICHT mitwandert"
    match: "Aufrufzeile|Aufruf:|pssmess|import-abdruck-messen|Selbstauskunft|umbenannt|Werkzeugkopf"
    sev: medium
    tags: [doku, wachen, werkzeuge, 2026]
    source: "Stuendlicher Doku-Lauf 30.08.2026 04:00: Probe gruen ueber 46 Wachen, danach die Suche nach der ungedeckten RICHTUNG; neue Wache tools/aufrufzeile-eigenname-pruefen.py"
    related: [verworfener-weg-bleibt-eine-einladende-anleitung, wiederkehrender-befund-ist-ablauffehler, doku-zitiert-paketstaende-die-niemand-nachhaelt, dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben]
    body: |
      `tools/import-abdruck-messen.py` trug in Zeile 3:

          Aufruf:  python3 pssmess.py <name> [modul ...]

      Eine Datei dieses Namens gibt es im ganzen Baum nicht. Der Name stammt
      aus der Zeit vor der Umbenennung: beim Umbenennen wandert die Datei im
      Dateisystem mit, der Name IN ihrem Kopf nicht. Wer der Zeile folgt,
      bekommt "No such file or directory" und haelt das Werkzeug fuer kaputt -
      dabei laeuft es, es heisst nur anders.

      ── WARUM 46 DOKU-WACHEN DAS NICHT SAHEN: DIE RICHTUNG ────────────
      Alle messen die Doku GEGEN den Code - Pfade, Zeilenzitate, Endpunkte,
      Beschriftungen, Schalter, Paketstaende. Sie schauen von der Doku nach
      AUSSEN. Was ein Werkzeug UEBER SICH SELBST behauptet, galt in keiner
      als Doku, obwohl es die einzige Anleitung ist, die ein Leser garantiert
      sieht: er hat die Datei ja schon offen.

      `doku-pfade-pruefen.py` haette den Fund technisch sehen koennen. Sie
      sieht ihn nicht an, weil ihr Suchraum `dokumentation/` und die
      Markdown-Dateien sind, nicht die Kopfzeilen von Code. Das ist dieselbe
      Form wie beim verworfenen Weg
      ([[verworfener-weg-bleibt-eine-einladende-anleitung]]): nicht eine
      falsche Antwort, sondern eine Frage, die keine Wache stellte.

      GEHALTEN SEIT 30.08.2026 durch tools/aufrufzeile-eigenname-pruefen.py,
      in tools/doku-luecken-probe.sh eingehaengt. Gemessen: 66 Aufrufzeilen in
      720 Skripten, ein Fund, keine Fehlmeldung.

      ── DAS MERKMAL IST DAS ETIKETT, NICHT DER ORT ────────────────────
      Geprueft wird "Zeile beginnt mit Aufruf/Nutzung/Usage/Verwendung +
      Doppelpunkt und nennt einen Skriptnamen". Nicht "liegt in tools/". Jedes
      neue Werkzeug bringt seine Pruefung selbst mit, sobald es sich erklaert.

      Drei Ausnahmen, jede gemessen und keine davon kosmetisch:
        * "Gegenprobe:" ist NICHT dabei. Dort nennt ein Werkzeug zu Recht ein
          FREMDES - scripts/box/touch-bridge.py und sein Zwilling im
          Installer schicken den Leser auf tools/zwillingsdateien-abgleich.py.
          Ohne diese Trennung waeren beide Dauerfehlalarm
          ([[dauerrote-wache-ist-keine]]).
        * `$(basename "$0")` wird ueberlesen. Das ist die sh-Form derselben
          Aussage, und sie kann per Bauart nicht veralten - genau das
          Verhalten, das die Wache erzwingen will.
        * `<platzhalter>` fallen vor dem Lesen weg: "prog <modul.py>" nennt
          ein Argument, kein Werkzeug.

      Verglichen wird ueber den BASISNAMEN, nicht den Pfad; ob das Praefix
      stimmt, ist Revier von doku-pfade-pruefen.py. Findet die Wache im ganzen
      Baum keine einzige Aufrufzeile, meldet sie WARNUNG statt gruen
      ([[gegenprobe-statt-gruen-glauben]]).

      ── DIE GEGENPROBE, DIE ZAEHLT ───────────────────────────────────
      `--sabotage` nimmt den eigenen Namen aus dem Vergleich; danach fallen
      alle 66 Zeilen auf, Rueckgabe 1. Wichtiger war die zweite Probe: den
      ALTEN Kopf zurueckgespielt und geprueft, dass die Wache genau den Fund
      meldet, fuer den es sie gibt - "pssmess.py, und im Baum gibt es die
      nicht". Gruen am reparierten Baum haette nur bewiesen, dass sie laeuft.

  - id: gatter-das-nein-sagt-ohne-sich-zu-erklaeren
    kind: note
    title: "Neun Schritte durften den Commit aufhalten, ohne dass das Wiki sie kannte - vor allem die Ratschen"
    match: "pruefen.sh|pre-commit|Ratsche|shellcheck|shellcheck-ratsche|Baseline|einfrieren|Deckel|auslieferprobe|fetch-frist-schau|neu-groessen-schau|leihgabe-pruefen|thema-werkstatt|durchkommen-loeschen|durchkommen-leerlauf|durchkommen-update|Gatter"
    sev: medium
    tags: [doku, wachen, werkzeuge, pruefstrecke, 2026]
    source: "Stuendlicher Doku-Lauf 30.08.2026 06:00: doku-luecken-probe.sh gruen ueber 47 Wachen, danach die ungerufenen Wachen gefahren (alle gruen) und die Pflichtstrecken gegen das Pack gehalten; neue Wache tools/gatter-im-pack-deckung.py"
    related: [werkzeug-erklaert-sich-selbst-und-veraltet-dabei, dauerrote-wache-ist-keine, wiederkehrender-befund-ist-ablauffehler, gegenprobe-statt-gruen-glauben, verweis-wache-las-nur-verfolgtes-und-verfehlte-die-neue-datei]
    body: |
      `tools/pruefen.sh` und `tools/git-hooks/pre-commit` sind die zwei Wege,
      auf denen dieser Baum NEIN sagt. Von den 78 Werkzeugen, die sie als
      Schritt rufen, kam am 30.08.2026 NEUN im ganzen Wissenspaket nicht vor.
      Wer dort rot laeuft, hat dann nur den Werkzeugkopf - und der erklaert
      das Werkzeug, nicht das RITUAL.

      ── WARUM DAS BEI RATSCHEN TEUER IST ──────────────────────────────
      Vier der Gatter sind Ratschen: der Bestand darf schlecht sein, er darf
      nur nicht SCHLECHTER werden. Rot heisst dort nicht "kaputt", sondern
      "mehr als eingefroren". Wer das nicht weiss, hat zwei falsche
      Auswege - blind neu einfrieren (die Ratsche ist tot) oder die eigene
      Aenderung zurueckdrehen (obwohl sie richtig war). Die Zahlen und der
      erlaubte Griff, Stand 30.08.2026:

        * `tools/shellcheck-ratsche.sh` - 161 eingefrorene Befunde ueber 120
          Dateien in `tools/shellcheck-baseline.txt`, gezaehlt ab
          `--severity=warning` (info/style bleiben draussen). Blockt zwei
          Faelle: mehr Befunde in einer Datei als eingefroren, ODER Befunde
          in einer Datei, die die Baseline nicht kennt - NEUE .sh-Dateien
          muessen sauber sein. Neu einfrieren nur nach bewusster
          VERBESSERUNG: `tools/shellcheck-ratsche.sh --einfrieren`. Der Haken
          misst mit `--index` den GESTAGTEN Inhalt, nicht den Baum. Ohne
          installiertes shellcheck laesst der Haken laut durch, `pruefen.sh`
          bricht hart.
        * `tools/fetch-frist-schau.mjs` - Backend-`fetch(` ohne
          AbortSignal, erlaubt sind 13 in `server.ts` und 1 in
          `spotify-control.ts`. Die Zahl 13 ist kein Schaetzwert: die
          dreizehnte Stelle fand erst dieses Werkzeug. Wer eine Stelle mit
          Frist versieht, setzt die Zahl HERUNTER.
        * `tools/neu-groessen-schau.mjs` - Deckel je Datei roh (nicht gzip):
          app.js 1.450.000 B, app.css 375.000 B, index.html 95.000 B. Der
          Kiosk parst die ROHE Groesse, auch mit serverseitiger compression.
          Bewusstes Wachsen hebt den Deckel MIT, im selben Commit.

      ── DIE HALBE RATSCHE, DIE NIEMAND HOERT ──────────────────────────
      `fetch-frist-schau.mjs --pruefen` meldete am 30.08. "nur noch 12 von 13
      erlaubten Stellen - herabsetzen" und ging trotzdem mit 0 hinaus. Das
      ist Absicht: eine Ratsche blockt nur die Verschlechterung. Es heisst
      aber, dass die Aufforderung zum ANZIEHEN in einer gruenen Zeile steht
      und darum liegenbleibt. Wer das Werkzeug laufen sieht, liest die
      Zeile - Ende 0 ist hier kein "nichts zu tun".

      ── DIE UEBRIGEN FUENF, KURZ ──────────────────────────────────────
        * `tools/auslieferprobe.py` - haelt die frische Karte ohne Nacharbeit
          fuer moeglich: jede `${MUPI_SRC}`-Quelle der mv/cp/install-Zeilen
          existiert; kein Programm ist ExecStart zweier Units (die
          Doppelfamilie mupi_taster/mupi_offtrigger auf demselben GPIO);
          jeder Crontab-Aufruf kommt aus einem wirklich kopierten Ordner;
          poweroff/reboot nur in den benannten Endstufen. Die mv-Zeilen
          leiten ihre Fehler nach `>&3` - ohne diese Probe bricht die Karte
          STILL.
        * `tools/leihgabe-pruefen.mjs` - borgt ein Werkzeug die Vorschau oder
          einen festen Debug-Port und legt es nicht zurueck? Der Schaden
          meldet sich nicht als Fehler, sondern als falsches MESSERGEBNIS bei
          einem anderen Werkzeug ([[vorschau-wird-geliehen]]).
        * `tools/thema-werkstatt.mjs --pruefen` - die Themenfelder gegen
          `app.css`; ein Thema traegt zwei Haelften (`--ion-*` fuer die
          klassische, der erzeugte Block fuer die neue Oberflaeche).
        * die drei "komm trotzdem durch" - nicht der gewoehnliche Lauf,
          sondern der Angriff darauf, jeder im frischen Sandkasten:
          `tools/durchkommen-loeschen.ts` (trifft `deletelocal` noch etwas
          ausserhalb von `media/<kat>/<int>/<titel>` - und loescht der
          gewollte Fall noch?), `tools/durchkommen-leerlauf.sh` (was tut die
          Box, wenn die Beruehrung NICHT erkannt wird - und SAGT sie das?),
          `tools/durchkommen-update.sh` (was liegt im Hort, wenn er anders
          aussieht als nach dem letzten Lauf - die beiden Haelften pruefen
          VERSCHIEDEN). Ihre gutmuetigen Zwillinge sind
          `tools/loeschen-sandkasten.ts`, `tools/leerlauf-uhr-nachspielen.sh`
          und `tools/update-bestand-probe.py`.

      ── DAS MERKMAL IST DER AUFRUF, NICHT DIE LISTE ───────────────────
      GEHALTEN SEIT 30.08.2026 durch `tools/gatter-im-pack-deckung.py`, in
      `tools/doku-luecken-probe.sh` eingehaengt. Sie liest die beiden
      Pflichtstrecken und nimmt jeden Werkzeugpfad auf einer
      NICHT-Kommentarzeile - keine gepflegte Liste
      ([[verweis-wache-las-nur-verfolgtes-und-verfehlte-die-neue-datei]]). Ein neuer Schritt bringt seine
      Pflicht zur Doku selbst mit; faellt ein Schritt weg, faellt die Pflicht
      mit ihm. Kommentare zaehlen nicht: was nur im Fliesstext von
      pruefen.sh steht, haelt niemanden auf.

      Findet sie in den Strecken ueberhaupt keine Werkzeugzeile, meldet sie
      WARNUNG statt gruen ([[gegenprobe-statt-gruen-glauben]]). `--sabotage`
      blendet den Pack-Text aus; danach muessen ALLE 79 Gatter auffallen,
      Rueckgabe 1 - gemessen.
  - id: dritter-horcher-den-die-doku-nie-kannte
    kind: note
    title: "Die Box horcht auf einem dritten Port - 5588, seit dem Bau, in keiner Zeile Doku"
    match: "5588|tonloginHorcher|TONLOGIN_RUECKWEG|TONLOGIN_FRIST_MS|Spotify.*Anmeldung|redirect_uri|Rueckleitung|Rueckweg|listen\\(|horchende-ports|offener Port|Firewall"
    sev: medium
    tags: [doku, ports, spotify, sicherheit, wachen, 2026]
    source: "Stuendlicher Doku-Lauf 30.08.2026 07:00: doku-luecken-probe.sh und ungerufene-wachen.py beide gruen, danach nach einer Sorte Behauptung gesucht, die KEINE Wache prueft - zitierte Ports; neue Wache tools/horchende-ports-deckung.py"
    related: [kinderbox-horcht-nach-aussen, offener-port-ist-keine-kennung, frontend-api-port-hart-verdrahtet, sackgasse-ohne-ausweg-in-der-tabelle, gegenprobe-statt-gruen-glauben]
    body: |
      Die Doku kannte zwei Ports: 8200 und 8443. `mupibox-server` macht aber
      einen DRITTEN auf - `s.listen(5588, '127.0.0.1')` in
      `src/backend-api/src/server.ts`. Der steht dort seit dem Bau, mit
      langen Kommentaren im Code, und kam am 30.08.2026 in README, BACKLOG,
      `dokumentation/`, `documentation/` und im Wissenspaket in KEINER Zeile
      vor. 30 Doku-Stellen nennen 8200, keine einzige 5588.

      ── WOZU ER DA IST ────────────────────────────────────────────────
      Spotify laesst fuer den Client dieser Box genau eine Rueckleitung zu:
      `http://127.0.0.1:5588/login`. Vom Handy aus fuehrt das ins Leere -
      dort ist `127.0.0.1` das Handy. AUF DER BOX IST ES DIE BOX SELBST. Es
      fehlte nur jemand, der horcht. Also horcht die Box waehrend einer
      Anmeldung kurz selbst: der Kiosk geht zu Spotify, kommt mit dem Code
      zurueck, der Horcher loest ihn ein. Kein Abtippen, kein zweites
      Geraet, kein Tunnel.

      ── WARUM DAS ZU DOKUMENTIEREN IST, NICHT NUR ZU KOMMENTIEREN ─────
      Ein Port, auf dem etwas horcht, ist AUSSENFLAECHE. Wer die Box
      absichert, eine Firewall-Regel schreibt oder eine fehlgeschlagene
      Anmeldung debuggt, liest die Doku - nicht 19000 Zeilen server.ts. Drei
      Eigenschaften entscheiden dabei, und alle drei standen nirgends:

      * NUR `127.0.0.1`, ausdruecklich nicht `0.0.0.0` und nicht `::`. Der
        Horcher tauscht einen Code gegen ein Token; im Netz hat er nichts zu
        suchen. Es braucht also KEINE Firewall-Regel - auch das muss man
        wissen koennen, sonst schreibt jemand vorsichtshalber eine.
      * NUR waehrend eines Anlaufs, hoechstens 15 Minuten
        (`TONLOGIN_FRIST_MS`). Wer zwischen zwei Anmeldungen nachsieht,
        findet den Port zu. DAS IST KEIN FEHLER - ohne diesen Satz sucht
        jemand stundenlang einen Dienst, der gar nicht laufen soll.
      * Wer ihn geschlossen vorfindet, OBWOHL ein Anlauf laeuft, landet im
        Kiosk in einer Sackgasse ohne Adresszeile. Genau das passierte bis
        zum 13.08.2026: der Wecker eines alten Anlaufs schloss den frischen
        Horcher eines zweiten mit.

      ── DIE WACHE GEHT AUF DIE SORTE ──────────────────────────────────
      `tools/horchende-ports-deckung.py` sucht `.listen(` im Backend - nicht
      die Zahl 5588, nicht die Datei `server.ts`. Sonst waere das Merkmal
      mit dem Fehler verschwunden und ein neuer Horcher an neuer Stelle
      faellt nicht auf. Der Port darf als Zahl dastehen oder ueber eine
      Konstante kommen (`app.listen(httpPort)` mit
      `const httpPort = ... || 8200`); beide Formen werden aufgeloest.

      Gegenprobe gefahren: zwei erfundene Horcher (einmal als Zahl, einmal
      ueber eine Konstante) ins committete `server.ts` gehaengt - beide
      wurden gemeldet, danach die Datei zurueckgeholt.

      ── DIE LEHRE FUER DEN NAECHSTEN LAUF ─────────────────────────────
      Beide Laeufer waren gruen. Gruen heisst nur: die 47 Wachen haben
      nichts gefunden. Die Luecke lag in einer SORTE BEHAUPTUNG, die keine
      Wache prueft - Ports. Wenn die Laeufer gruen sind, ist die Frage
      nicht "welche Wache ist rot", sondern "welche Sorte Aussage im Baum
      prueft ueberhaupt niemand".

  - id: ergaenzung-faellt-zwischen-zwei-wachen
    kind: note
    title: "Die Ergaenzung faellt zwischen zwei Wachen - sie legt eine eigene Datei an UND wirkt auf eine fremde"
    match: "service\\.d|\\.d/.*\\.conf|Drop-?In|drop-?in|nur-wenn-da|nur-wenn-aktiv|nicht-blockieren\\.conf|engine\\.conf|stop\\.conf|dietpi-autologin|ExecCondition|ExecStop=|ergaenzungen-deckung|mupihat-da|Akkuanzeige laeuft nicht"
    sev: medium
    tags: [doku, systemd, wachen, mupihat, ausrollweg, 2026]
    source: "Stuendlicher Doku-Lauf 30.08.2026 08:00: doku-luecken-probe.sh (48 Wachen) und ungerufene-wachen.py beide gruen; danach nach einer Sorte ausgerollter Datei gesucht, die keine Wache kennt - systemd-Ergaenzungen; neue Wache tools/ergaenzungen-deckung.py"
    related: [eingriff-in-fremde-datei-hat-keinen-namen, drop-in-reihenfolge-ueberstimmt, dritter-horcher-den-die-doku-nie-kannte, dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben, mupihat-bricht-i2c-selbst-heraus]
    body: |
      Zwei Wachen teilen sich die Frage "was macht ein Ausrollweg auf der
      Box":

        ausgerollte-vorlagen-deckung.py   was er ABLEGT (Datei aus
                                          config/templates/)
        ausgerollte-eingriffe-deckung.py  was er an einer fremden Datei
                                          AENDERT (sed -i, tee -a, >>)

      Eine systemd-Ergaenzung `/etc/systemd/system/UNIT.d/NAME.conf` ist
      beides nicht - und deshalb sah sie keine von beiden:

        * KEINE VORLAGE: es gibt keine Datei im Baum, die sie waere. Das
          Rezept schreibt sie zur Laufzeit mit `cat > … <<'ENDE'`.
        * KEIN EINGRIFF: sie legt eine EIGENE, neue Datei an und fasst die
          Unit nicht an. Die Eingriffs-Wache stuft `cat >` ausdruecklich als
          ANLEGEND ein und stellt daran keine Doku-Forderung.

      Sie ist aber die WIRKSAMSTE der drei Sorten: sie aendert das Verhalten
      einer dokumentierten Unit, OHNE deren Datei anzufassen.

      ── DER FUND ──────────────────────────────────────────────────────
      `mupi_hat.service.d/nur-wenn-da.conf` (recipes/mupibox.yaml:426) haengt
      `ExecCondition=/usr/local/bin/mupihat-da` an, dazu `Restart=on-failure`
      / `RestartSec=30`. DIESE DATEI ENTSCHEIDET ALLEIN, OB DIE AKKUANZEIGE
      ANLAEUFT - und hatte am 30.08.2026 null Treffer in dokumentation/,
      README.md, BACKLOG.md und im Wissenspaket. Wer
      `config/services/mupi_hat.service` liest, sieht keine Bedingung.
      Sechs Ergaenzungen fand die neue Wache insgesamt; fuenf waren
      irgendwo genannt, aber in keiner Tabelle ihrer Sorte, weil es die
      Tabelle nicht gab.

      ── DER WIDERSPRUCH, DER DABEI MITKAM ─────────────────────────────
      Der Rezeptschritt `mupihat-stopp-reparieren` (mupibox.yaml:319 ff.)
      repariert ein `ExecStop=/bin/kill -TERM $(cat
      /run/mupi_hat_control.pid)` der "mitgelieferten Unit". Das gibt es
      hier nicht mehr: `config/services/mupi_hat_control.service` hat seit
      E52 (20.08.2026) GAR KEIN ExecStop, mit einem Kommentar, der sagt
      "ExecStop IST ENTFALLEN" - und die PID-Datei schreibt niemand. Der
      Weg haengt es also wieder an. Harmlos durch `2>/dev/null || true`,
      aber die Box widerspricht der Datei, und die `note:` des Schritts
      zitiert einen Unit-Text, den es nicht mehr gibt. OFFEN: den Schritt
      streichen oder seine note: auf den heutigen Stand ziehen.

      ── DIE WACHE ─────────────────────────────────────────────────────
      `tools/ergaenzungen-deckung.py`, haengt in
      `tools/doku-luecken-probe.sh`. Gesucht wird DIE SORTE: ein Pfad unter
      `/etc/systemd/system/` mit einem `.d/` darin - egal ob `cat`,
      `printf`, `install`, `cp` oder ein `put:` ihn schreibt. Verlangt wird
      der DATEINAME in der Prosa, nicht der Unit-Name: der steht ohnehin
      ueberall und wuerde jede Ergaenzung fuer gedeckt erklaeren, sobald
      ihre Unit einmal erwaehnt ist. Wege und Prosa-Ablagen werden aus
      ausgerollte-vorlagen-deckung.py IMPORTIERT, nicht kopiert.
      Findet sie NULL Ergaenzungen, meldet sie eine Warnung statt gruen -
      am 30.08. waren es sechs, null hiesse "das Muster greift nicht mehr".

      Gegenprobe gefahren: eine erfundene `mupi_fan.service.d/zz-probe.conf`
      ans committete Rezept gehaengt, sie wurde gemeldet, Datei zurueckgeholt.

      Dokumentiert in `dokumentation/mixpibox.md` §4.1, Tabelle "Was die
      Ausrollwege an fremde Units anhaengen" - alle sechs mit Weg, Zeile und
      der Zeile, die sie umschreiben.

      ── DIE LEHRE ─────────────────────────────────────────────────────
      Wer eine Wache aus zwei bestehenden ableitet, erbt die LUECKE
      ZWISCHEN IHNEN. Beide Schwestern waren gruen und beide hatten recht -
      der Gegenstand lag in keiner der beiden Definitionen. Die Frage nach
      einem gruenen Lauf ist nicht "welche Wache ist rot", sondern "welche
      Sorte Datei passt in keine ihrer Formen".

  - kind: falle
    id: protokolltabelle-ohne-schreiber
    title: "Die Protokollseite bot zwei Dateien an, die niemand schreibt"
    match: "protokolle.ts|DATEIEN|Protokoll nicht lesbar|idle_shutdown.log|shutdown_control.log|/var/log/mupibox"
    sev: med
    cause: "Die feste Tabelle DATEIEN in protokolle.ts wurde beim Schreiben der Seite EINMAL ausgedacht und nie gegen die Schreiber gehalten; die Schreiber sind Shell-Dateien in einem ganz anderen Verzeichnis"
    fix: "Der Leser folgt dem Schreiber, nicht umgekehrt - Pfad aus der Shell-Datei uebernehmen (/tmp), nicht den Schreibweg fuer eine Leseseite umbauen. Wache: tools/protokoll-pfade-deckung.py"
    tags: [admin, protokolle, wache, doku, 2026]
    source: "Doku-Lauf 30.08.2026 (09:00), am Baum gemessen"
    related: [doku-zeigt-auf-zeilen-die-wandern, mupi-admin-system-und-protokolle]
    body: |
      GEMESSEN am 30.08.2026: zwei der drei Dateieintraege der Protokollseite
      zeigten auf /var/log/mupibox/. Geschrieben wird an beiden Stellen nach
      /tmp:

        scripts/mupibox/idle_shutdown.sh:108  : "${LOG:=/tmp/idle_shutdown.log}"
        scripts/OnOffShim/off_trigger.sh:10   LOGFILE="/tmp/shutdown_control.log"

      LOG setzt niemand von aussen - mupi_idle_shutdown.service hat keine
      Environment=-Zeile. autosetup.sh:397 legt /var/log/mupibox/ an, und
      danach schreibt dort NIE JEMAND hinein. Die Verwaltung antwortete
      also fuer BEIDE Dateieintraege dauerhaft "Protokoll nicht lesbar" -
      seit die Seite existiert (c2a5263a).

      ── ES WAR SCHON GESEHEN WORDEN ───────────────────────────────────
      Der Kopf von src/backend-api/src/fehlergrund.ts zitiert als BEISPIEL
      fuer eine durchgereichte Fehlermeldung genau:

        ENOENT: no such file or directory, stat
        '/var/log/mupibox/idle_shutdown.log'

      am 07.08.2026 an einem echten Serverprozess gemessen. Gelesen wurde
      daraus "der PFAD steht in der Antwort nach draussen" - richtig, und
      es wurde behoben. Dass der Pfad schlicht FALSCH ist, hat niemand
      mitgelesen. Ein Messwert beantwortet die Frage, mit der man ihn
      anschaut, und nur die.

      ── WARUM DER TEST GRUEN WAR ──────────────────────────────────────
      protokolle.spec.ts prueft die Tabelle sehr wohl - aber auf das
      SYMPTOM des VORIGEN Falls: d.pfad.includes('.pm2') === false. Die
      vier pm2-Protokolle waren eingefroren, also wurde auf "pm2" geprueft.
      Der naechste Fehler heisst nicht pm2. Siehe
      doku-zeigt-auf-zeilen-die-wandern.

      ── DIE RICHTUNG DER KORREKTUR ────────────────────────────────────
      Nicht beliebig. off_trigger.sh laeuft am Knopf und greift fuer alles
      Erhoehte einzeln zu sudo; ein Schreibziel unter /var/log haette ihm
      Rechte abverlangt, die er nicht hat. Den Ausschaltweg fuer eine
      Leseseite umzubauen waere der teurere und riskantere von zwei Wegen.
      Also folgt die Tabelle den Schreibern: /tmp/idle_shutdown.log und
      /tmp/shutdown_control.log. Der hinweis: sagt jetzt dazu, dass die
      Dateien nach einem Neustart leer sind - sonst sieht /tmp wie ein
      Fehler aus.

      ── DIE WACHE UND IHRE ZWEI FEHLSTARTS ────────────────────────────
      tools/protokoll-pfade-deckung.py, haengt in
      tools/doku-luecken-probe.sh. Sie fragt fuer jeden Pfad der Tabelle:
      kennt ihn ausser der Tabelle noch irgendwer im ausgelieferten Baum?
      Zwei Fassungen meldeten vorher gruen, beide auf die schlimmste Art:

        1. KOMMENTARE. /var/log/mupibox/idle_shutdown.log kommt zweimal im
           Baum vor - in fehlergrund.ts:11 und server.ts:4980, beide Male
           als zitierte Fehlermeldung IN EINEM KOMMENTAR, die gerade WEGEN
           des falschen Pfades entstand. Der Beleg FUER den Fehler galt der
           Probe als Beleg GEGEN ihn.
        2. DAS GEBAUTE BUENDEL. src/deploy/server.js ist die kompilierte
           Fassung ebenjener Tabelle, 1,4 MB minifiziert - der Pfad steht
           dort drin, WEIL er in protokolle.ts steht. Ein Erzeugnis der
           geprueften Datei ist keine Gegenstelle zu ihr.

      chromium steht als benannte AUSNAHME drin: chrome_debug.log legt
      Chromium selbst an. Der Freibrief ist nicht blanko - die Wache prueft,
      dass --enable-logging noch in scripts/chromium-autostart.sh:98 steht.
      Faellt der Schalter weg, faellt die Ausnahme auf.

      Gegenprobe gefahren, alle drei Faelle rot: erfundener Pfad, Pfad nur
      im Kommentar, Ausnahme ohne Grundlage. Dazu ein neuer Test in
      protokolle.spec.ts, der die Tabelle GEGEN DIE SHELL-DATEI liest -
      auch der gegengeprueft (alter Pfad zurueck -> rot).

      ── DIE LEHRE ─────────────────────────────────────────────────────
      Eine FESTE TABELLE ist gegen Leseloecher richtig und gegen Verrotten
      wehrlos: sie wird einmal ausgedacht und danach nie wieder befragt.
      Wo eine Tabelle Pfade nennt, die woanders entstehen, gehoert die
      Gegenstelle geprueft - und die Probe darf weder Prosa noch das eigene
      Kompilat als Beleg nehmen.
  - id: die-aenderung-war-richtig-das-umfeld-blieb-stehen
    kind: note
    title: "Die Aenderung war richtig, ihr Umfeld blieb stehen - dreimal am selben Tag dieselbe Fehlerklasse"
    match: "Zwillings-Angleich|zwillinge-nach-ziel|bootwache_test|touchbridge_test|PLAYER_PROXY_HOST|nur Rueckschleife|vollstaendig.*GRENZE|waehleInhalt|Umfeld nachziehen|Testlaeufer|runtests"
    sev: medium
    tags: [audit, ablauf, tests, harness, verschmelzung, 2026]
    source: "Kritiker-Lauf 30.08.2026 (AUDIT-2026-08-30.md): vier Pruefer auf remote-step-installer, Harness/Randwelt, Wochen-Delta E93-E98, Netz-Recherche; tragende Funde vom Hauptlauf gegengeprueft (Tests ausgefuehrt, waehleInhalt am Code, Player-Bindung live)"
    related: [dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben, einmal-hinsehen-ist-keine-messung, wiederkehrender-befund-ist-ablauffehler]
    body: |
      Drei Funde EINES Laufs, ein Muster. Jede Aenderung fuer sich war
      richtig - was fehlte, war der Blick auf das, was an der geaenderten
      Stelle HING:

        1. Der Zwillings-Angleich (276e42ea, 29.08.) machte die
           Installer-Kopien byte-identisch - und zerriss dabei zwei
           Installer-Tests, die gegen die ALTE 197-Zeilen-Bootwache und
           das duennere touch-bridge-Double geschrieben waren
           (tests/bootwache_test.py: bw.FRIST_S existiert nicht mehr;
           tests/touchbridge_test.py: _Ctl hat kein lesen()). Niemand
           merkte es, weil der Installer KEINEN Testlaeufer hat - jeder
           Test ist Einzelaufruf, kein runtests, kein pruefen.sh-Anschluss.
        2. Der Loopback-Umbau des Players (7944bb41, 14.08., harte
           127.0.0.1-Bindung) zaehlte im eigenen Kommentar auf, was NICHT
           kaputtgeht (mqtt, telegram, mupictl) - den Harness vergass er.
           Dessen compose spricht backend-player:5005 ueber das
           Containernetz an: seither ECONNREFUSED, waehrend README und
           mupictl die Achse weiter behaupten. 16 Tage tot, unbemerkt.
        3. Das Blaettern der Titellisten wurde am 01.08. auf 1000 erhoeht
           (titelEinesWerks) - die vollstaendig-Berechnung im /inhalt
           blieb auf der alten Praemisse "hoechstens 100" stehen
           (GRENZE-Konstante samt Kommentar). Als E95 spaeter WAHLREGELN
           auf dieses Flag baute (waehleInhalt: erster Kandidat mit
           vollstaendig gewinnt VOR dem Titelzahl-Vergleich), wurde aus
           der stehengebliebenen Zahl ein Funktionsfehler: der komplette
           lokale Mitschnitt verliert immer gegen Spotify.

      Die Lehre, praezise: NACH einer Reparatur oder einem Angleich ist
      die Frage nicht "stimmt meine Aenderung?" (das prueft die
      Gegenprobe), sondern "WER HAENGT AN DER STELLE, die ich geaendert
      habe - Tests, Doku, Kommentare, abgeleitete Konstanten, die andere
      Haelfte des Repos?" Und: eine stehengebliebene Praemisse ist
      harmlos, BIS jemand Neues darauf baut (Fall 3) - deshalb gehoert
      sie beim Bauen geprueft, nicht nur beim Aendern.

      Wo eine Wache stehen kann, steht sie vor das Ereignis: die
      Zwillings-Wache prueft Gleichstand - sie muesste auch die Tests der
      Zwillinge LAUFEN LASSEN (oder ein Testlaeufer haengt in der
      Pruefstrecke). Fuer Kommentar-Praemissen gibt es keine Wache; dort
      gilt Fall-3-Disziplin beim Bauen.
  - id: oberflaeche-ohne-routentabelle-faellt-durch-jede-seitenwache
    kind: falle
    title: "Die neue Box-Oberflaeche hat keine Routentabelle - jede Wache, die Seiten zaehlt, meldet dort gruen, ohne je hingesehen zu haben"
    match: "data-anzeigeort|Anzeigeort|NewDesign|Mini-Player|Cover-Vollbild|grosser Player|Routentabelle|app.routes|Seiten der Oberflaeche"
    sev: med
    cause: "Die Doku-Wachen fuer Oberflaechen suchen nach `path:` in einer Routentabelle (tools/box-seiten-deckung.py fuer src/frontend-box, die FELDER-Pruefung fuer frontend-admin). NewDesign/ ist eine EINZIGE Seite - was man sieht, entscheidet ein `hidden` an einer Huelle. Es gibt dort kein `path:`, die Wachen finden nichts und schweigen. Am 30.08.2026 standen `Mini-Player`, `grosser Player` und `Cover-Vollbild` mit null Treffern in dokumentation/mixpibox.md, waehrend die KLEINERE klassische Oberflaeche ihre 16 Wege seit dem 25.08. aufgezaehlt und bewacht fuehrte"
    fix: "tools/anzeigeorte-deckung.py: das MERKMAL `data-anzeigeort` aus NewDesign/index.html gegen die Tabelle in dokumentation/mixpibox.md, beide Richtungen. HTML-Kommentare vorher entfernen (die Datei tippt `data-anzeigeort=\"<Name>\"` als Muster ab). Die TABELLE lesen, nicht den Fliesstext - der nennt die Namen ohnehin. Ohne Fund: Warnung statt gruen. In tools/doku-luecken-probe.sh gehaengt"
    tags: [doku, wache, oberflaeche, newdesign, anzeigeort, luecke]
    related: [drei-orte-eine-anzeige, menuelose-seiten-stehen-in-keiner-doku-wache, ablage-stand-in-keiner-doku-wache, dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben, eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen, wache-stirbt-still-wenn-sie-nirgends-haengt]
    body: |
      ZWILLING AM SELBEN MERKMAL. `data-anzeigeort` steht auch in
      [[drei-orte-eine-anzeige]] - dem Laufzeitfehler vom 03.08.2026, aus
      dessen Reparatur das Merkmal ueberhaupt erst entstand. Die Frage, die
      trennt: zeigt ein Ort das FALSCHE (dann dort), oder ist er richtig
      gebaut und steht nur in keiner Doku (dann hier)? Der Zusammenhang ist
      keine Aehnlichkeit, sondern Ursache: die Selbstverdrahtung, die dort
      die Loesung war, ist hier der Grund des Driftens.

      ══ DIE LAGE ═══════════════════════════════════════════════════════════
      Der Doku-Lauf vom 30.08.2026 (10:00) fand tools/doku-luecken-probe.sh
      gruen und tools/ungerufene-wachen.py ohne Befund: 163 Werkzeuge von den
      Laeufern erreichbar, keine ungerufene Wache. Trotzdem fehlte etwas, das
      groesser ist als die meisten bisherigen Funde.

      Die Box hat ZWEI Oberflaechen. Die klassische (src/frontend-box,
      Angular) fuehrt 16 Wege in einer Routentabelle; sie stehen seit dem
      25.08. im Handbuch und werden in beide Richtungen bewacht
      ([[menuelose-seiten-stehen-in-keiner-doku-wache]]). Die NEUE
      (NewDesign/, 28 562 Zeilen, die aktiv gebaute) hat gar keine
      Routentabelle - sie ist EINE Seite, und was zu sehen ist, entscheidet
      ein `hidden` an einer Huelle.

      Eine Wache, die nach `path:` sucht, findet dort nichts. Sie meldet
      nicht ROT und nicht "unbekannt", sondern GRUEN - sie hat die Datei nie
      angefasst.

      ══ WAS DEM HANDBUCH FEHLTE ════════════════════════════════════════════
      Gemessen am 30.08.2026 in dokumentation/mixpibox.md:

          Vollbild        0 Treffer
          Mini-Player     0 Treffer
          Cover-Vollbild  0 Treffer
          grosser Player  0 Treffer

      Das Handbuch beschrieb die neue Oberflaeche (4.3: kein Bundler, keine
      externe Adresse, die harte /player-Regel) und ihre Gesten (4.8, sieben
      flache Felder). Nirgends stand, dass sie DREI Orte hat, die dasselbe
      ueber das laufende Stueck zeigen.

      ══ WARUM AUSGERECHNET DIESE ACHSE TEUER IST ═══════════════════════════
      Sie hat schon einmal Geld gekostet. Bis zum 03.08.2026 hatte jeder der
      drei Orte EIGENE Zuweisungen: zwei standen in `mpMalen` und liefen im
      2-Sekunden-Takt, die des Vollbilds standen in `albumGrossAuf` und
      liefen nur BEIM OEFFNEN. Beim naechsten Titel blieben dort Titel,
      Interpret und Cover des vorigen stehen. Gemeldet wurde es woertlich:
      "auch im voll bild aktualiierte es die infromationen nicht".

      Die Lehre war nicht die vergessene Zeile, sondern dass es DREI Stellen
      gab, an denen man sie vergessen konnte.

      ══ DIE BEHOBENE BAUART IST GENAU DER GRUND FUER DIE WACHE ═════════════
      Seither steht die Liste im BAUM, nicht im Skript: jede Huelle traegt
      `data-anzeigeort`, ihre Teile `data-np="titel|unter|cover|symbol"`.
      `anzeigeOrte()` liest sie einmal aus dem Baum und malt jeden gefundenen
      Ort. Ein VIERTER Ort wird mitgemalt, ohne dass in app.js eine Zeile
      dazukommt.

      Das ist die richtige Bauart - und sie ist der Grund, warum die Doku
      still veraltet: ein Ort, der sich selbst verdrahtet, zwingt niemanden,
      irgendwo etwas nachzutragen. Er faellt beim Bauen nicht auf, weil alles
      funktioniert. Nur die Doku weiss nichts von ihm.

      ══ WIE WEIT DIE ZUSAMMENFUEHRUNG GEHT (und warum genau so weit) ═══════
      Die Grenze ist Absicht und gehoert mit dokumentiert, sonst liest der
      naechste sie als Unfertigkeit und "vervollstaendigt" sie:

        MIT DABEI  Titel, Unterzeile, Cover, Spiel/Pause-Symbol - die vier
                   Dinge, die jeder Ort aus DERSELBEN Meldung ableitet.

        NICHT DABEI: der Fortschritt (`balkenMalen`). Er hat das Problem
                   nicht - genau EIN Schreiber, und die Orte zeigen ihn
                   ABSICHTLICH verschieden: Mini-Player ein Balken, grosser
                   Player ein ziehbarer Regler mit zwei Zeiten, Vollbild gar
                   nichts. Ein gemeinsames Schema haette mehr Ausnahmen als
                   Faelle.

        NICHT DABEI: die Lautstaerke-Anschlaege. Sie haengen nicht am
                   laufenden Titel, sondern am Lautstaerkewert, und sind
                   ueber `data-laut` an ihrer eigenen einen Stelle
                   zusammengefasst. Zwei Verzeichnisse fuer zwei Quellen sind
                   richtig; eines fuer beide waere nur kuerzer.

      ══ DIE WACHE ══════════════════════════════════════════════════════════
      tools/anzeigeorte-deckung.py, in tools/doku-luecken-probe.sh gehaengt
      ([[wache-stirbt-still-wenn-sie-nirgends-haengt]]). Drei Entscheidungen,
      die jede fuer sich eine Gruen-Falle vermieden haben:

      AUF DIE SORTE, NICHT AUF DEN PFAD. Gesucht wird das Merkmal
      `data-anzeigeort`, nicht `#mp` und keine Liste bekannter Namen. Eine
      Namensliste im Skript waere derselbe Fehler eine Ebene hoeher - das
      Merkmal darf nicht mit dem Fehler zusammen verschwinden koennen.

      HTML-KOMMENTARE VORHER ENTFERNEN. index.html erklaert das Verfahren
      ueber der Stelle und tippt dabei `data-anzeigeort="<Name>"` als Muster
      ab. Wer den Kommentar mitliest, verlangt einen Ort namens `<Name>`. Ein
      zitierender Kommentar ist keine Gegenstelle - wer ihn als solche gelten
      liesse, bekaeme gruen von genau der Datei, die er pruefen will.
      Entfernt wird mit `re.S` ueber `<!--.*?-->`, nicht zeilenweise: der
      erklaerende Block ist ein Dutzend Zeilen lang.

      DIE TABELLE LESEN, NICHT DEN FLIESSTEXT. Die Namen stehen im Abschnitt
      ohnehin auch im Fliesstext ("Mini-Player einen Balken, der grosse
      Player einen ziehbaren Regler"). Eine Suche ueber das ganze Dokument
      meldete deshalb auf Dauer gruen, ohne je die AUFZAEHLUNG geprueft zu
      haben - und die ist das, was altert
      ([[eine-wache-die-eine-ablage-nicht-kennt-meldet-dort-ewig-gruen]]).
      Dieselbe Lehre wie bei den bewachten Wegen in box-seiten-deckung.py.

      ══ DIE GEGENPROBE, SECHS RICHTUNGEN ═══════════════════════════════════
      Auf KOPIEN in einem Wegwerfbaum gefahren, weil die Aenderung noch nicht
      committet war:

        A  vierter Ort im HTML, nicht in der Doku   -> FEHLT, exit 1   ✓
        B  Zeile aus der Doku-Tabelle entfernt      -> FEHLT, exit 1   ✓
        C  Doku fuehrt einen erfundenen Ort         -> GENANNT, exit 1 ✓
        D  Merkmal im HTML umbenannt                -> WARNUNG, exit 1 ✓
        E  Tabelle weg, Fliesstext bleibt           -> WARNUNG, exit 1 ✓
        F  Ort nur im HTML-Kommentar                -> gruen, exit 0   ✓

      D und E sind die beiden, die eine naive Fassung bestanden HAETTE, ohne
      etwas zu pruefen ([[gegenprobe-statt-gruen-glauben]]).

      ══ WAS DIE WACHE AN SICH SELBST FAND ══════════════════════════════════
      Der erste Probelauf nach dem Einhaengen war ROT, mit einem Fund in der
      neuen Datei selbst:

          TOTER VERWEIS: tools/anzeigeorte-deckung.py:25
            -> wache-auf-sorte-nicht-auf-pfad

      Zwei der drei llmwiki-Kennungen, die im Kopf der neuen Wache standen,
      gibt es im Paket nicht - sie stammten aus dem Gedaechtnis des
      schreibenden Laufs, nicht aus pack.yaml. Nur
      `gegenprobe-statt-gruen-glauben` existiert wirklich. Die Lehre steht
      schon im Paket, sie hat sich hier nur ein weiteres Mal bewahrheitet:
      eine Kennung wird VOR dem Schreiben gegen pack.yaml geprueft, jede
      einzeln. Die Formulierungen sind jetzt ohne Kennung ausgeschrieben.

      ══ WO ES STEHT ════════════════════════════════════════════════════════
      dokumentation/mixpibox.md 4.3.2 "Die Anzeigeorte der neuen
      Oberflaeche" - direkt hinter 4.3.1, das dieselbe Frage fuer die
      klassische Oberflaeche beantwortet.

      ══ WAS ALS NAECHSTES ZU PRUEFEN WAERE ═════════════════════════════════
      Die Achse ist groesser als diese drei Huellen. NewDesign/ hat weitere
      Sammel-Merkmale, die nach demselben Muster gebaut sind und ebenfalls in
      keiner Wache haengen - `data-laut` (die Lautstaerke-Anschlaege) und
      `data-np` (die Teile innerhalb einer Huelle) sind die beiden, die beim
      Lesen auffielen. Wer dort weitermisst, fange bei `data-laut` an: es ist
      das einzige, das eine eigene Sammelstelle (`laut.knoepfe`) hat und
      damit dieselbe Selbstverdrahtung wie die Anzeigeorte.

  - id: newdesign-kopierregel-nimmt-alles-mit-was-niemand-austraegt
    kind: falle
    title: "angular.json kopiert NewDesign/ mit `**/*` - was nicht mit soll, muss von Hand in die ignore-Liste, und genau das vergisst man"
    match: "NewDesign|angular.json|assets|ignore|glob|neu/|Messseite|wellen-messseite|Buendel|ausgeliefert|LIESMICH|Readme.md"
    sev: low
    cause: "Der assets-Eintrag in src/frontend-box/angular.json kopiert NewDesign/ mit `\"glob\": \"**/*\"` nach `neu/` - ALLES. Die Gegenliste `ignore` daneben wird von Hand gepflegt, und zwar in dem Moment, in dem jemand eine neue Datei anlegt und an alles andere denkt als an den Bauplan. Am 30.08.2026 lagen drei Dateien im gebauten Buendel, die niemand dort haben wollte: wellen-messseite.html (eine Messseite fuer die Wellen-Animation), schriften/Readme.md und bilder/figuren/LIESMICH.md"
    fix: "tools/newdesign-mitlieferung-deckung.py: liest die ignore-Liste AUS angular.json, deutet die glob-Muster selbst (`**` ueber Verzeichnisse, `*` innerhalb eines Namens) und haelt dagegen, was in NewDesign/ kein Laufzeit-Baustein der EINEN Seite sein kann - jede *.html ausser index.html, jede *.md, jede *.json die index.html/app.js nicht nennt. In tools/doku-luecken-probe.sh gehaengt. Behoben durch `wellen-messseite.html` und `**/*.md` in der ignore-Liste"
    tags: [bau, newdesign, angular, ausliefern, wache, luecke]
    related: [oberflaeche-ohne-routentabelle-faellt-durch-jede-seitenwache, dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben, doku-zeigt-auf-zeilen-die-wandern, wache-stirbt-still-wenn-sie-nirgends-haengt, pfad-in-die-bauablage-ist-nie-im-baum]
    body: |
      ══ DIE DATEI BEHAUPTETE DAS GEGENTEIL ═════════════════════════════════
      Der Fund faengt nicht bei einer Wache an, sondern bei einem Satz. Der
      Kopf von NewDesign/wellen-messseite.html sagte:

          Wird von keinem Bau mitgenommen (angular.json kopiert NewDesign/,
          aber die Seite haengt an nichts und stoert nichts)

      Der Klammersatz nennt die Tatsache, die den Hauptsatz widerlegt, und
      zieht daraus trotzdem den beruhigenden Schluss. „Haengt an nichts" und
      „wird nicht kopiert" sind zwei verschiedene Aussagen; der Bau kopiert
      nach Muster, nicht nach Erreichbarkeit.

      NACHGEMESSEN im lokalen Bau, 30.08.2026:

          src/deploy/www/browser/neu/wellen-messseite.html
          src/deploy/www/browser/neu/schriften/Readme.md
          src/deploy/www/browser/neu/bilder/figuren/LIESMICH.md

      Alle drei lagen im Buendel. `src/deploy/` ist nicht verfolgt, der
      Schaden also nie committet worden - aber gebaut wurde er, und auf einer
      Box haette er unter `/neu/` vom selben Server gestanden wie die
      Oberflaeche.

      ══ WARUM KEINE WACHE HINSAH ═══════════════════════════════════════════
      Es gab keine. Die Doku-Wachen fragen, ob etwas Gebautes BESCHRIEBEN ist;
      diese Frage ist die umgekehrte - geht etwas mit, das gar nicht gebaut
      werden sollte? Der Unterschied zu
      [[oberflaeche-ohne-routentabelle-faellt-durch-jede-seitenwache]]: dort
      war die Wache vorhanden und sah am Merkmal vorbei, hier fehlte die
      Achse ganz.

      Eine ignore-Liste ist dieselbe Sorte Ding wie die feste Protokolltabelle
      und die Routentabelle: EINMAL ausgedacht, danach nie wieder gegen den
      Baum gehalten. Sie hatte am 30.08. drei Eintraege und haette fuenf
      gebraucht.

      ══ WORAUF DIE WACHE STEHT ═════════════════════════════════════════════
      Auf der Sorte, nicht auf den drei Namen von heute
      ([[doku-zeigt-auf-zeilen-die-wandern]]). Die Sorte ist: die
      Box-Oberflaeche ist EINE Seite - index.html mit app.js, app.css und
      Anhaengseln. Also kann eine zweite *.html nie Laufzeit sein, eine *.md
      nie, und eine *.json nur, wenn index.html oder app.js sie beim Namen
      nennt.

      Die Probe liest die ignore-Liste aus dem Bauplan und deutet die Muster,
      statt sie abzutippen: wer `MASKOTTCHEN.md` durch `**/*.md` ersetzt,
      bleibt gruen, weil dieselben Dateien gedeckt bleiben.

      DER GEBAUTE BAUM IST KEIN BELEG. src/deploy/ und
      src/frontend-box/www/ sind Erzeugnisse ebendieses Kopiervorgangs. Dass
      eine Datei dort liegt, belegt den Fehler - nicht seine Abwesenheit. Die
      Probe sieht nur in NewDesign/ und in angular.json.

      ══ DIE GEGENPROBE ═════════════════════════════════════════════════════
      Die Wache war beim ersten Lauf gruen, weil die Reparatur schon stand -
      also nichts wert, bis sie absichtlich kaputtgemacht wurde
      ([[gegenprobe-statt-gruen-glauben]]). Jedes Muster einzeln aus der
      ignore-Liste entfernt, gegen eine Sicherung ausserhalb des Baums:

          ohne `wellen-messseite.html`  -> rot, 1 Fund (die Messseite)
          ohne `**/*.md`                -> rot, 3 Funde (alle drei LIESMICH)
          ohne `bilder/quellen/**`      -> GRUEN, und das ist der Haken

      ══ WAS DIE WACHE NICHT SIEHT ══════════════════════════════════════════
      Bilder nicht. `bilder/quellen/` haelt 117 PNG - dieselbe Endung wie die
      129 ausgelieferten PNG daneben, kein Formatunterschied, an dem sich
      Werkstatt und Laufzeit trennen liessen. Die naheliegende Regel „ein
      PNG, das niemand beim Namen nennt, ist Werkstatt" faellt aus:
      index.html, app.js, app.css und maskottchen.json nennen zusammen 54
      Namen, die Maskottchen-Figuren kommen zur Laufzeit ueber
      zusammengesetzte Namen dazu. Die Regel meldete also rund 75 richtige
      Dateien als Fund und waere binnen einer Woche zu Recht ueberlesen
      ([[dauerrote-wache-ist-keine]]).

      `bilder/quellen/**` bleibt deshalb ungedeckt: faellt der Eintrag weg,
      gehen 117 Vorlagen mit, ohne dass etwas rot wird. Das ist eine bewusste
      Luecke und steht so auch im Kopf der Probe - nicht, damit sie hinnehmbar
      wird, sondern damit der naechste Lauf sie nicht fuer erledigt haelt.

      ══ WAS ALS NAECHSTES ZU PRUEFEN WAERE ═════════════════════════════════
      Zwei Faeden. Erstens die Bilderseite: eine Probe gegen
      tools/maskottchen-bauen.py, die als Einzige weiss, welche Namen sie
      erzeugt - damit liesse sich „genannt ODER erzeugt" pruefen und die
      Luecke oben schliessen. Zweitens die ANDEREN assets-Eintraege: derselbe
      Bauplan kopiert `./../../MuPiBox/media` als `cover` und
      `src/frontend-box/src/assets`;
      ob dort dieselbe Sorte Werkstatt mitgeht, hat noch niemand gemessen.

  - id: pfad-in-die-bauablage-ist-nie-im-baum
    kind: falle
    title: "Eine Doku-Stelle, die ein MESSERGEBNIS aus src/deploy/ zitiert, meldet die Pfadwache dauerhaft rot - der Ort steht in .gitignore"
    match: "doku-pfade-pruefen|src/deploy|gitignore|TOT|Bauablage|dauerrot|Messergebnis|check-ignore|Buendel"
    sev: low
    cause: "tools/doku-pfade-pruefen.py fragt zu jedem Pfad in der Doku: liegt er im Baum? Fuer einen Pfad unter `src/deploy/` ist die Antwort strukturell NEIN - der Ordner steht in .gitignore (`# Temporary deploy files`), der Bau legt ihn an, danach ist er weg. Der Eintrag [[newdesign-kopierregel-nimmt-alles-mit-was-niemand-austraegt]] zitiert drei solcher Pfade als NACHGEMESSENES Ergebnis; die Wache meldete sie am 30.08.2026 prompt als TOT und haette das bei jedem weiteren Lauf getan"
    fix: "In tools/doku-pfade-pruefen.py die Sorte statt der Namen ausgenommen: `_bauablage(p)` fragt `git check-ignore -q -- <pfad>`. Was git absichtlich nicht fuehrt, weil ein Bau oder eine Laufzeit es anlegt, ist kein Baumpfad und wird uebersprungen - MIT Meldung je Pfad (`IGNORIERT: …`), nicht still. Gegenprobe: `src/deploy` aus .gitignore genommen -> die drei Pfade sind sofort wieder TOT"
    tags: [wache, doku, bau, gitignore, luecke]
    related: [newdesign-kopierregel-nimmt-alles-mit-was-niemand-austraegt, dauerrote-wache-ist-keine, gegenprobe-der-baumweiten-wache-liegt-im-baum, doku-zeigt-auf-dateien-die-es-nicht-gibt, gegenprobe-statt-gruen-glauben, halbe-pruefung-meldet-wie-eine-ganze, wache-liest-sieben-von-neunundvierzig-prosadateien]
    body: |
      ══ DER BEFUND WAR DIE FOLGE DER REPARATUR ═════════════════════════════
      Der Doku-Lauf am 30.08.2026 (14:00) meldete genau eine Luecke, und sie
      war das Echo des Laufs davor:

          TOT: src/deploy/www/browser/neu/bilder/figuren/LIESMICH.md
          TOT: src/deploy/www/browser/neu/schriften/Readme.md
          TOT: src/deploy/www/browser/neu/wellen-messseite.html
               (genannt in llmwiki:newdesign-kopierregel-nimmt-alles-mit-
                was-niemand-austraegt)

      Drei Stunden vorher hatte derselbe Lauf gemessen, dass diese drei
      Dateien im gebauten Buendel LAGEN, und die `ignore`-Liste in
      `src/frontend-box/angular.json` um `wellen-messseite.html` und
      `**/*.md` ergaenzt. Die Reparatur wirkte. Und weil sie wirkte, zeigte
      der Eintrag, der sie beschreibt, nun auf drei Dateien, die es nicht
      mehr gibt.

      Das ist die bekannte Not aus [[halbe-pruefung-meldet-wie-eine-ganze]]:
      die Stelle, die einen Fehler BESCHREIBT, ist nicht der Fehler. Neu ist
      der Grund, warum eine Ausnahme je Pfad hier die falsche Antwort waere.

      ══ DER ORT KANN NIE GRUEN WERDEN ══════════════════════════════════════
      `.gitignore` fuehrt seit jeher:

          # Temporary deploy files.
          src/deploy

      `src/deploy/` ist das Ergebnis des Baus, nicht seine Quelle. Ein Pfad
      dorthin ist NIE im Baum - nicht weil jemand ihn vergessen hat, sondern
      weil der Ordner erst beim Bauen entsteht und beim Aufraeumen wieder
      geht. Schlimmer: ob die Wache ihn meldet, hing bis heute davon ab, ob
      zufaellig ein lokaler Bau herumlag. `src/deploy/server.js` und
      `src/deploy/www/3rdpartylicenses.txt` standen laengst in der Doku und
      wurden NIE gemeldet - `os.path.exists` fand sie, weil auf dieser
      Maschine gerade gebaut worden war. Dieselbe Wache, dieselbe Sorte
      Pfad, zwei Urteile je nach Zustand des Arbeitsbaums.

      ══ WARUM DIE AUSNAHME AUF DIE SORTE GEHT ══════════════════════════════
      Die bequeme Loesung waere ein Eintrag unter `AUSNAHMEN_JE_QUELLE`
      gewesen: drei Dateinamen, eine Zeile, gruen. Sie haette den naechsten
      Bau-Pfad wieder gemeldet - und den uebernaechsten -, denn das Merkmal
      der drei ist nicht ihr Name. Es ist: **git fuehrt diesen Ort
      absichtlich nicht.**

      Genau das steht schon irgendwo geschrieben, naemlich in `.gitignore`,
      und wird von dort GELESEN statt im Werkzeug nachgebaut:

          def _bauablage(p):
              return subprocess.run(
                  ["git", "check-ignore", "-q", "--", p]
              ).returncode == 0

      Damit faellt die Ausnahme automatisch weg, wenn `src/deploy` je aus der
      Ignorierliste faellt - das Merkmal verschwindet mit dem Grund und nicht
      mit dem Fund. Nebenbei deckt dieselbe Zeile `node_modules/`,
      `.angular/`, `harness/state/` und `src/backend-api/cache/` mit ab; alle
      vier sind dieselbe Sorte und alle vier haetten frueher oder spaeter
      denselben Zufallsbefund erzeugt.

      Die uebersprungenen Pfade werden EINZELN gemeldet (`IGNORIERT: …`) und
      nicht stumm geschluckt. Ein stiller Ausschluss liest sich wie Deckung -
      dieselbe Regel, die weiter oben im Werkzeug schon fuer die
      uebersprungenen `AUDIT-*.md` gilt.

      ══ DIE GEGENPROBE ═════════════════════════════════════════════════════
      Eine Ausnahme, die alles ausnimmt, ist keine. Deshalb zweimal
      gegengeprueft ([[gegenprobe-statt-gruen-glauben]]):

        1. `src/deploy` aus `.gitignore` entfernt, Wache gelaufen: die drei
           Pfade stehen sofort wieder als TOT da, `3 LUECKE(N)`. Danach
           `.gitignore` aus der Sicherung zurueckgespielt und mit
           `git diff --stat` bewiesen, dass nichts haengen blieb.
        2. Gegen die bekannten toten Pfade der Wache geprueft
           (`tools/gibt-es-nicht-wirklich.py`, `scripts/mupi-lautstaerke.sh`,
           `NewDesign/stil.css`): keiner davon wird von `check-ignore`
           getroffen, alle drei laufen weiter durch die Pruefung. Die neue
           Regel nimmt genau die Sorte heraus, die sie herausnehmen soll.

      ══ ZWEI DAUERROTE, ZWEI VERSCHIEDENE URSACHEN ═════════════════════════
      [[gegenprobe-der-baumweiten-wache-liegt-im-baum]] beschreibt ebenfalls
      eine Wache, die ab dem ersten Lauf dauerrot meldet - und wer nur das
      Symptom „dauerrot" sucht, landet abwechselnd auf beiden. Die eine
      Frage, die entscheidet:

          Liegt der gemeldete Pfad im Baum oder nicht?

      Dort LIEGT er (die Wache liest ihre eigene Attrappe mit; behoben wird
      im WERKZEUG, indem Marke und Kennung getrennt werden). Hier liegt er
      strukturell NIE, weil `.gitignore` den Ort fuehrt - und behoben wird in
      der FRAGE der Wache, nicht in dem, was sie liest. Wer die beiden
      verwechselt, sucht die Attrappe in einer Datei, die es nie gab.

      ══ WAS NOCH OFFEN IST ═════════════════════════════════════════════════
      `src/deploy/` taucht in der Doku an mehreren Stellen als ANLEITUNG auf
      („nach dem Bau liegt X unter …"). Diese Wache kann darueber nichts mehr
      sagen - sie hat den Ort verlassen, und das ist richtig so. Wer solche
      Aussagen pruefen will, braucht eine Wache, die BAUT und dann hinsieht,
      so wie `tools/newdesign-mitlieferung-deckung.py` es ohne Bau ueber den
      Bauplan tut. Bis dahin gilt: eine Aussage ueber das Buendel ist eine
      Zustandsaussage und hat die Haltbarkeit einer solchen.

  - id: doku-deckung-der-verwaltung-hoert-bei-der-seite-auf
    kind: falle
    title: "Ein neuer Kasten auf einer schon dokumentierten Seite ist fuer jede Doku-Wache unsichtbar"
    match: "neuer Abschnitt nicht dokumentiert|Doku-Lauf meldet gruen obwohl Feature neu|Seite genannt Abschnitt fehlt|h2 nicht im Handbuch|Steuerpult undokumentiert|KEINE LUECKE trotz neuem Feature"
    sev: med
    cause: "Die Code-nach-Doku-Deckung der Verwaltung misst SEITEN: `rahmen.ts` und `app.routes.ts` gegen das Benutzerhandbuch. Ein `<h2>`-Kasten INNERHALB einer laengst genannten Seite hat kein Merkmal, auf das eine dieser Wachen zeigt. Die Seite ist genannt, also gruen - egal wie gross der neue Abschnitt ist"
    fix: "Auf der Ebene messen, auf der gebaut wird: `tools/admin-abschnitte-deckung.py` zieht die `<h2>` aus `seiten/*.ts` und haelt sie gegen das Handbuch. NICHT als Gatter scharf schalten, solange der Rueckstand (44 von 70 am 30.08.2026) nicht abgetragen ist - siehe [[dauerrote-wache-ist-keine]]"
    tags: [doku, wache, handbuch, verwaltung, deckung, schicht, 2026]
    related: [dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben, gegenprobe-der-baumweiten-wache-liegt-im-baum, oberflaeche-ohne-routentabelle-faellt-durch-jede-seitenwache]
    body: |
      ══ DIE LAGE ═══════════════════════════════════════════════════════════
      Am 30.08.2026 brachte 7b929d32 das Aufnahme-Steuerpult: `Verwaltung →
      Medien` bekam einen aufklappbaren Kasten `Aufnahmen` (Lage aller
      vorgemerkten Titel, `offen · fertig · mit Fehler`) und an jeder
      Spotify-Zeile einen Knopf `Aufnehmen`. Zwei neue Bedienwege fuer den
      Betreiber. Das Benutzerhandbuch kannte keinen davon.

      Der stuendliche Doku-Lauf eine Stunde spaeter meldete:

          KEINE LUECKE.

      Zweiundfuenfzig Wachen, alle gruen, und die groesste Neuerung des Tages
      lag undokumentiert daneben.

      ══ WARUM KEINE WACHE ANSCHLUG ═════════════════════════════════════════
      Nicht weil eine kaputt war. Weil ALLE eine Schicht zu hoch messen:

          Die Code→Doku-Deckung der Verwaltung hoert bei der SEITE auf.

      `medien.ts` steht seit Monaten im Handbuch - Bibliothek, Regale,
      Internet Archive. Die Wache „Verwaltungsseiten aus rahmen.ts, die das
      Benutzerhandbuch nicht nennt" fragt: ist die SEITE genannt? Ja. Gruen.
      Was auf der Seite alles WAECHST, sieht sie nicht. Ein Abschnitt kann
      beliebig gross dazukommen, ohne je gemeldet zu werden.

      Die Achse ist im Baum schon zweimal beschrieben, jeweils eine Schicht
      tiefer, und die drei gehoeren zusammengelesen:

          tools/beschriftungen-deckung.py   misst die OBERFLAECHE (admin/box)
          tools/handbuch-ort-deckung.py     misst die SEITE (welche Unterseite)
          tools/admin-abschnitte-deckung.py misst den ABSCHNITT (dieser Fund)

      Dazu kommt die Richtung. Die beiden Schwestern sind ZITATGETRIEBEN: sie
      pruefen, was die Doku BEHAUPTET. Ueber etwas, das die Doku gar nicht
      erst erwaehnt, koennen sie strukturell nichts sagen. Wer nur solche
      Wachen hat, misst die Doku gegen sich selbst.

      ══ DIE WACHE HAETTE IHREN EIGENEN ANLASS VERFEHLT ═════════════════════
      Beim Bau nachgemessen, und das ist der unangenehmere Teil des Fundes:
      der Textvergleich gegen das Handbuch traegt fuer `Aufnahmen` NICHT.
      Das Wort stand dort laengst, in ganz anderer Sache - Abschnitt 11,
      „sechs betroffene **Aufnahmen** liegen noch in der Mediathek". Mit und
      ohne den neuen Absatz meldet die Wache dieselben 26 gedeckten
      Abschnitte. Sie haette den Kasten als gedeckt durchgewunken.

      Auch das Auszaehlen der AUSZEICHNUNG rettet es nicht: die Fehlstelle
      steht selbst in `<strong>`. Ein Ein-Wort-Kasten ist mit Textsuche gegen
      Fliesstext nicht entscheidbar, Punkt.

      Die Lehre ist nicht „besser matchen". Sie ist: die blinde Stelle
      AUSWEISEN statt sie als Deckung auszugeben. Das Werkzeug hat drei
      Toepfe - `sicher genannt`, `ungenannt`, `UNENTSCHEIDBAR` - und
      Ein-Wort-Ueberschriften landen im dritten. Gemessen am 30.08.2026:
      1 sicher, 44 ungenannt, 25 unentscheidbar. Ein Werkzeug, das seine
      eigene Unsicherheit als gruen verkauft, ist schlimmer als keins.

      ══ WARUM SIE NICHT IM GATTER HAENGT ═══════════════════════════════════
      44 von 70 Abschnitten sind ungenannt, und ein Teil davon ist ABSICHT:
      `leistung.ts` („Speicher je Posten", „Zeitachse des Starts") ist
      Diagnosewerkzeug fuer den Entwickler, kein Betreiberstoff. Eine Wache,
      die ab dem ersten Lauf 44 Zeilen rot meldet, verdeckt den naechsten
      echten Fund, statt ihn zu zeigen - genau der Fehler aus
      [[dauerrote-wache-ist-keine]]. Deshalb laeuft sie als MESSINSTRUMENT
      (Rueckgabe 0) und wird erst mit `--streng` scharf, wenn der Rueckstand
      abgetragen und eine Ausnahmeliste MIT BEGRUENDUNG gepflegt ist.

      ══ DIE GEGENPROBE ═════════════════════════════════════════════════════
      `--sabotage` haengt einen Kasten an, den es nirgends gibt; er muss als
      ungedeckt auftauchen, sonst misst das Werkzeug nichts. Marke und
      Kennung sind dabei getrennt und werden erst zur Laufzeit gefuegt -
      sonst greift das eigene Suchmuster auf die Attrappe im Quelltext zu
      ([[gegenprobe-der-baumweiten-wache-liegt-im-baum]]).

  - kind: signature
    id: netzloecher-im-minutentakt-sind-oft-thermik
    title: "Die Box faellt im Minutentakt aus dem Netz - und das Funkbild steht"
    match: "No route to host im Minutentakt|Box kurz weg dann wieder da|SSH und HTTP gleichzeitig weg|ping faellt aus WLAN steht|Funkloch aber Signal gut|mDNS antwortet in Loechern"
    sev: high
    cause: "Nicht das Funk, sondern die DROSSEL. Steht die CPU unter Dauerlast am Temperaturlimit, antwortet der ganze Netzstack (mDNS, SSH, HTTP) in Loechern - das Funkbild bleibt dabei tadellos, die Verbindung bricht nie ab. Wer am WLAN sucht, sucht an der Stelle, die als einzige in Ordnung ist"
    fix: "Erst das Drosselregister lesen, dann das Funk anfassen: `vcgencmd get_throttled`. Die NIEDRIGEN Bits (0-3) heissen GERADE AKTIV, die Bits ab 16 nur WAR MAL - 0xe0000 ist Entwarnung, 0xe0008 ist der laufende Befund. Die Sonde protokolliert es je Messung; `python3 tools/netzabriss-auswerten.py /home/dietpi/netzabriss/sonde.log` zaehlt Abrisse, Ping-Ausfaelle und aktive Drossel aus und stellt sie nebeneinander"
    tags: [netz, wlan, thermik, drossel, pi5, sonde, diagnose, 2026]
    related: [zwei-luefterkurven-und-die-grenze-darunter, mupi-pi4-pi5-configs-angleichen, im-kiosk-messen-statt-raten]
    body: |
      ══ DER TAG, AN DEM ES AUFFIEL (30.08.2026, E101) ══════════════════════
      Die Box war den ganzen Tag im Minutentakt kurz weg - SSH und HTTP
      gleichermassen "No route to host". Verdaechtigt wurde das Funk, dann
      die Koexistenz von WLAN und Bluetooth. Die Sonde auf der Box schrieb
      alle 5 s ihr Funkbild mit; ausgewertet ueber 8.021 Messungen
      (03:12-14:31) sah es so aus:

          genau EINE Still-Luecke      22 s um 11:05 (Dienst-Neustart)
          Ping-Ausfaelle              263
          Spitzen ueber 100 ms        353
          Drossel GERADE aktiv      2.717 Messungen
            davon Soft-Limit (e0008) 1.763
            davon Frequenz  (e0006)   954
          Bluetooth-Senke an            0 Messungen

      Die Box war also fast nie WEG. Sie war LANGSAM, und zwar in Schueben.
      Und die Hauptspur - Bluetooth-Koexistenz - kam in keiner einzigen
      Messung des Tages vor.

      ══ DIE KETTE ══════════════════════════════════════════════════════════
          Kiosk-Dauerlast (E100: eine width-Transition, ~92 % CPU)
        + Mitschnitt-Nachaufnahmen
        -> Box kocht
        -> CPU drosselt
        -> Netzstack antwortet in Loechern
        -> "No route to host", waehrend das Funkbild tadellos steht

      Das Tueckische ist die Reihenfolge der Verdaechtigen. Ein Netzfehler
      zeigt sich am Netz, also sieht man am Netz nach - und findet dort
      nichts, weil dort nichts ist. Die Verbindung besteht durchgehend, die
      Signalstaerke stimmt, der Kanal ist ruhig. Nur ANTWORTET niemand.

      ══ DIE ZWEI BITS, DIE MAN VERWECHSELN KANN ════════════════════════════
      `vcgencmd get_throttled` liefert ein Register, und der haeufigste
      Auswertungsfehler ist, es als eine Zahl zu lesen:

          Bits 0-3    JETZT gerade   (0x1 Unterspannung, 0x2 Frequenz
                                      gedrosselt, 0x4 gedrosselt,
                                      0x8 Soft-Temperatur-Limit)
          Bits 16-19  seit dem Start SCHON MAL vorgekommen

      `0xe0000` sieht alarmierend aus und ist Entwarnung: nur
      Vergangenheits-Bits, gerade laeuft nichts. `0xe0008` ist derselbe
      Verlauf MIT laufendem Soft-Limit. Wer nur auf "ungleich 0" prueft,
      meldet eine Box als heiss, die vor Stunden einmal heiss war.
      `tools/netzabriss-auswerten.py` maskiert deshalb ausdruecklich mit
      `& 0xF`.

      ══ WAS DAS WERKZEUG BEANTWORTET ═══════════════════════════════════════
          python3 tools/netzabriss-auswerten.py <sonde.log> [--seit HH:MM]

      Vier Fragen, die von Hand muehsam sind: Luecken in der Messfolge (Sonde
      kam nicht zum Schreiben = Box hing wirklich), Ping-Ausfaelle und
      -Spitzen, aktive gegen vergangene Drossel, und das Muster um jede
      Luecke herum (war Bluetooth an? drosselte sie gerade? wie hoch war die
      Last?). Rueckgabe immer 0 - reine Auskunft, keine Wache.

      ══ GRENZE DER SONDE, BEIM AUSWERTEN GEMESSEN ══════════════════════════
      `wlan.verbunden` steht in JEDER Messung auf false, obwohl das Netz
      durchgehend stand: die Sonde fragt den eingebauten Adapter, waehrend
      der USB-Stecker traegt. Ein Feld, das immer dasselbe sagt, misst
      nichts - und haette hier fast als Beleg fuer den Funkverdacht gedient.

  - kind: falle
    id: zwei-luefterkurven-und-die-grenze-darunter
    title: "Der Luefter gehorcht dem Kernel, nicht der Verwaltung - und die Drosselgrenze liegt UNTER seiner Kurve"
    match: "Luefter dreht nicht hoch|luefter25 aendert nichts|fan_temp bleibt wirkungslos|Box drosselt bei 65|temp_limit|Luefterkurve eingestellt keine Wirkung|pwm-fan Stufe"
    sev: med
    cause: "Es gibt ZWEI Luefterregelungen, und auf der Box laeuft die andere. Die Verwaltung stellt die MuPiHAT-Kurve (`fan.fan_temp_25/50/75/100`, `scripts/fan/fan_control.py` ueber `mupi_fan.service`); tatsaechlich regelt der Kernel-Treiber `pwm-fan` nach der Firmware-Kennlinie. Dazu setzt unser eigenes Rezept `temp_limit=65` - also eine Grenze UNTERHALB der Temperatur, ab der die Kennlinie richtig aufdreht"
    fix: "Vor jedem Eingriff messen, WER regelt: `cat /sys/class/thermal/cooling_device*/type` (Typ `pwm-fan` = Kernel) und `systemctl is-active mupi_fan`. Laeuft der Kernel-Treiber, sind die vier Felder der Verwaltung wirkungslos - dann gehoert die Kennlinie in die config.txt (`dtparam=fan_temp0..3`), und zwar NUR mit der Bootwache aus [[mupi-bootwache-drehung]]"
    tags: [luefter, thermik, config-txt, pwm-fan, mupihat, konfiguration, pi5, 2026]
    related: [konfigfeld-zwei-namen-luefterkurve, schalter-ohne-unit-luefter, mupi-pi4-pi5-configs-angleichen, netzloecher-im-minutentakt-sind-oft-thermik]
    body: |
      ══ AM GERAET NACHGEMESSEN (30.08.2026) ════════════════════════════════
          /sys/class/thermal/cooling_device0  type=pwm-fan  cur=2  max=4
          /sys/class/hwmon/hwmon2/fan1_input  5177 U/min
          thermal_zone0                       58,4 Grad
          vcgencmd get_throttled              0xe0000 (nur Vergangenheit)

          systemctl is-active mupi_fan        inactive
          /etc/mupibox/mupiboxconfig.json     KEINE Gruppe `fan`
          /boot/firmware/config.txt Zeile 78  temp_limit=65

      Der Luefter dreht. Er gehorcht nur niemandem, den die Verwaltung kennt.

      ══ ZWEI REGELUNGEN, EINE LAEUFT ═══════════════════════════════════════
      [[konfigfeld-zwei-namen-luefterkurve]] beschreibt vier Schwellen, die
      der Betreiber einstellen kann: `luefter25/50/75/100` ->
      `fan.fan_temp_25..100`, gelesen von `scripts/fan/fan_control.py`. Das
      ist die MuPiHAT-Regelung. Sie ist auf dieser Box zweifach tot:

          die Unit `mupi_fan.service` LIEGT da, ist aber inactive
          die Gruppe `fan` fehlt in der mupiboxconfig ueberhaupt

      Und beides faellt niemandem auf. `pruefeKonfig` prueft die Kurve nur
      auf Anstieg, WENN sie dasteht - eine fehlende Gruppe `fan` stoert
      nicht. Der Schalter fuer die Unit hat schon einmal ins Leere gezeigt
      ([[schalter-ohne-unit-luefter]]); heute existiert die Unit, laeuft aber
      nicht. Wer also die vier Felder in der Verwaltung verstellt, verstellt
      Zahlen in einer Datei, die kein laufender Prozess liest.

      Geregelt wird stattdessen vom Kernel: `pwm-fan` als
      thermal cooling_device, vier Stufen, gefahren nach der
      Firmware-Kennlinie (`dtparam=fan_temp0..3` in der config.txt - hier
      NICHT gesetzt, also Vorgabe).

      ══ DER EIGENTLICHE FUND: DIE GRENZE LIEGT UNTER DER KURVE ═════════════
      `temp_limit=65` steht nicht zufaellig in der config.txt. Unser eigenes
      Rezept setzt sie (Schritt `system-werte` in
      remote-step-installer/recipes/mupibox.yaml), und die Begruendung ist
      gut: die Box sitzt im Kinderzimmer in einem geschlossenen Gehaeuse,
      frueher drosseln ist dort richtig ([[mupi-pi4-pi5-configs-angleichen]],
      dort als `temp_limit 75 -> 65` gefuehrt).

      Nur steht danebenan eine Kennlinie, die davon nichts weiss:

          ~60 Grad   sanftes Drosseln setzt ein (Soft-Limit, Bit 3)
           65 Grad   unsere gesetzte Grenze - CPU wird gebremst
          ~67 Grad   erst HIER dreht die Vorgabe-Kennlinie richtig auf

      Der Luefter wartet also auf eine Temperatur, die die Box wegen unserer
      eigenen Grenze gar nicht mehr erreichen DARF. Unter Dauerlast pendelt
      sie in genau dieser Zone: Luefter haelt Stufe 2 von 4, CPU bremst,
      und niemand von beiden loest den Zustand auf. Am 30.08. war das die
      Wurzel der Netzloecher
      ([[netzloecher-im-minutentakt-sind-oft-thermik]]).

      Keiner der beiden Werte ist fuer sich falsch. Die Grenze ist bewusst
      gesetzt, die Kennlinie ist die Vorgabe des Herstellers. Falsch ist,
      dass sie nie gegeneinander gelesen wurden - genau das Muster aus
      [[konfigfeld-zwei-namen-luefterkurve]], wo `ledBrightnessMin` und
      `-Max` einzeln gueltig und zusammen verdreht sind.

      ══ WAS DARAUS FOLGT ═══════════════════════════════════════════════════
      Wer die Kuehlung verbessern will, hat drei Hebel und muss den richtigen
      Topf treffen:

          Kennlinie frueher/steiler   dtparam=fan_temp0..3 in der config.txt
                                      - wirkt, braucht aber die Bootwache
                                      [[mupi-bootwache-drehung]], denn
                                      config.txt-Eingriffe haben hier
                                      Vorgeschichte
          Grenze anheben              temp_limit hoch - macht die bewusste
                                      Entscheidung von 2026-07 rueckgaengig,
                                      also nur mit Begruendung
          MuPiHAT-Kurve              wirkungslos, solange mupi_fan nicht
                                      laeuft und die Gruppe `fan` fehlt

      Erst messen, welcher Topf ueberhaupt greift. Die drei sehen in jeder
      Doku gleich aus und schliessen einander am Geraet aus.
    source:
      - "am Geraet nachgemessen (30.08.2026): cooling_device0=pwm-fan Stufe 2/4, mupi_fan inactive, keine fan-Gruppe in der mupiboxconfig, temp_limit=65 in Zeile 78 der config.txt"
      - "Herkunft der Grenze: remote-step-installer/recipes/mupibox.yaml, Schritt system-werte"

  - kind: regel
    id: leinwand-misst-sich-selbst-nicht-nach
    title: "Eine Leinwand, die nur auf `resize` hoert, nagelt sich auf eine falsche Groesse fest"
    match: "canvas zu klein|canvas bleibt auf alter breite|leinwand falsche groesse|resize feuert nicht|canvas.width leert|canvas wird schwarz beim zeichnen|kontext-einstellungen weg|setTransform verloren"
    sev: med
    tags: [canvas, newdesign, oberflaeche, zeichnen, dpr, e99, 2026]
    source: "30.08.2026, E99 (Commit 55dd4f1e) - beim Bauen des Pegel-Zeichners in NewDesign/app.js gemessen, Funktion `wellenGroesseMessen`"
    symptom: >
      Der Wellen-Zeichner in der Fortschrittszeile stand nach einer
      Fenster-Verkleinerung fuer den REST DER SEITE auf der alten, zu
      kleinen Breite - er hat sich nie wieder erholt. Kein Fehler in der
      Konsole, kein leeres Bild: nur eine Zeichenflaeche, die dauerhaft
      neben der Wirklichkeit lag.
    cause: >
      ZWEI FALLEN, DIE SICH GEGENSEITIG DECKEN:

      1. `resize` IST KEINE ZUSAGE. Die Groessenaenderung kam ohne
         begleitendes `resize`-Ereignis an (Emulation, Zoom, eine Messseite
         in einem verkleinerten Fenster). Wer die Leinwandgroesse EINMAL
         beim Start und danach nur noch im `resize`-Hoerer setzt, hat keine
         zweite Gelegenheit - ein einziges verpasstes Ereignis gilt bis zum
         naechsten Neuladen.

      2. `canvas.width`/`.height` zu SETZEN LEERT DIE FLAECHE - auch dann,
         wenn der neue Wert dem alten gleicht. Die naheliegende Reparatur
         ("dann setze ich die Groesse eben bei jedem Zeichnen") loescht
         damit vor jedem einzelnen Bild die Leinwand UND wirft die
         Kontext-Einstellungen weg, allen voran das `setTransform` fuer den
         DPR. Harmlos fuers Bild, solange direkt danach voll gezeichnet
         wird - stille Zusatzarbeit bei jedem Tonwert, und ein Fallstrick,
         sobald irgendwann inkrementell gezeichnet werden soll.
    fix: >
      BEI JEDEM ZEICHNEN NACHMESSEN, ABER NUR BEI ABWEICHUNG ZUWEISEN.

      `getBoundingClientRect()` je Zeichnung lesen (fuer ein einzelnes,
      isoliertes Element billig), und `canvas.width`/`.height` nur dann
      schreiben, wenn sich der Pixelwert wirklich geaendert hat - im selben
      `if` dann auch `setTransform(dpr,0,0,dpr,0,0)` erneuern, denn genau
      dort ist es verloren gegangen.

      Zwei Randfaelle, die dazugehoeren: misst das Rechteck 0x0 (Element
      unsichtbar, Elternteil `hidden`), NICHT auf null klemmen, sondern die
      letzte gueltige Groesse stehen lassen. Und die Rechnung laeuft
      hoechstens so oft wie gezeichnet wird - was in Ordnung ist, solange
      der Zeichner selbst gedeckelt ist ([[im-kiosk-messen-statt-raten]]:
      keine Dauerlast, hier pausiert er nach 3 s Flachheit ganz).
    body: |
      ══ WARUM DAS HIER UEBERHAUPT AUFTRAT ═══════════════════════════════
      E99 gibt der Fortschrittszeile vier echte Frequenzbaender (Goertzel
      ueber `pw-record`, siehe src/backend-player/src/pegel.ts). Gezeichnet
      wird bewusst NUR bei einem neuen Pegel-Wert - kein rAF, kein Timer.
      Genau diese Sparsamkeit hat die Falle aufgedeckt: ein Zeichner, der
      ohnehin jeden Frame laeuft, haette die falsche Groesse beim naechsten
      echten `resize` beilaeufig geheilt und den Fehler nie gezeigt.

      ══ DIE ALLGEMEINE FORM ═════════════════════════════════════════════
      Ereignisse sind eine Benachrichtigung, keine Wahrheit. Wo ein Wert
      guenstig direkt ablesbar ist, ist Ablesen dem Zuhoeren vorzuziehen -
      und wo das Ablesen einen Nebeneffekt hat (hier: Leeren der Flaeche),
      gehoert ein Vergleich davor, nicht ein Verzicht aufs Ablesen.
    related: [im-kiosk-messen-statt-raten, mit-echten-werten-rechnen]

  - kind: regel
    id: umgezogener-schalter-laesst-seinen-pfad-zurueck
    title: "Ein Schalter, der umzieht, laesst seinen alten Weg in der Doku stehen"
    match: "schalter nicht gefunden|schalter ist nicht da|reiter stimmt nicht|doku zeigt auf falschen reiter|einstellung nicht auffindbar|pfad im handbuch falsch|knopf woanders"
    sev: med
    tags: [doku, handbuch, verwaltung, darstellung, wache, e99, 2026]
    source: "30.08.2026, Doku-Lauf nach E99 - in dokumentation/benutzerhandbuch.html und dokumentation/mixpibox.md gemessen, Wache tools/darstellungs-pfade-pruefen.py"
    symptom: >
      Die Doku schickt den Leser zu einer Einstellung, die dort nicht steht:
      "Eltern-Bereich -> Darstellung -> Verhalten -> ,Wellen im Player'". Im
      Reiter Verhalten ist der Knopf nicht; er sitzt in Optik unter
      Mini-Player. Der Leser haelt die Funktion fuer nicht vorhanden.

      DASS DAS TEUER IST, war am selben Tag schon bewiesen, nur nicht am
      Papier: der Betreiber hat den Schalter im alten Reiter selbst nicht
      gefunden ("der schalter ist nicht da ich haette ihn im player
      vermutet") - deswegen wurde er ja verschoben. Der Umzug behob die
      Sucherei auf der Oberflaeche und legte sie in die Doku.
    cause: >
      KEINE DER VORHANDENEN WACHEN FRAGT NACH DEM WEG.
      `beschriftungen-deckung.py` fragt, ob es den zitierten Knopf GIBT und
      ob er auf der genannten OBERFLAECHE sitzt (Verwaltung oder Box);
      `handbuch-ort-deckung.py` fragt nach der Seite hinter einem Menuepunkt;
      `doku-pfade-pruefen.py` nach Dateien. Ein Pfad
      "Seite -> Reiter -> Gruppe -> ,Knopf'" ist fuer alle drei gruen,
      solange jedes einzelne Wort irgendwo vorkommt.

      Die Bewegung selbst ist dabei nicht der Fehler, sondern die Regel:
      Reiter und Gruppen der Darstellungsseite werden auf Zuruf umsortiert
      (Optik wurde aus Farben und Elementen zusammengelegt, Wellen wanderten
      aus Verhalten heraus). Jede solche Bewegung entwertet still jeden
      Doku-Satz, der den alten Ort nennt.
    fix: >
      DEN WEG PRUEFEN, NICHT NUR DIE WOERTER:
      `tools/darstellungs-pfade-pruefen.py` (in
      `tools/doku-luecken-probe.sh` gehaengt) liest jede
      Pfadangabe der Doku, die `Darstellung` nennt - im Handbuch am Merkmal
      `<span class="pfad">`, in der Architekturdoku an der kursiven Kette mit
      Pfeil -, und haelt sie gegen `darstellung.ts`.

      DREI ENTSCHEIDUNGEN, DIE DIE WACHE TRAGEN:

      1. NAMEN AUS DER QUELLE, nicht aus einer Liste im Skript: Reiter und
         Gruppen kommen aus `REITER`/`OPTIK_TEILE`, Ueberschriften aus den
         `<h3>` der Vorlage. Ein umbenannter Reiter nimmt die Wache mit
         ([[praefix-in-der-wache-ist-die-namenssuche-durch-die-hintertuer]]).

      2. ZUGEHOERIGKEIT AUS DEM UMSCHLIESSENDEN `@if`-BLOCK, nicht aus der
         Feldliste einer Gruppe: `OPTIK_TEILE[].felder` zaehlt nur die
         FUEHRENDEN Felder auf, nachgeschobene Knoepfe stehen dort nie. Und
         die Bedingung wird am WERT erkannt (`=== 'mp'`), nicht am Namen des
         Signals - der hiess `reiter`, heisst `reiterAktiv` und darf morgen
         anders heissen.

      3. NUR PRUEFEN, WAS DIE DOKU BEHAUPTET. Die Darstellungsseite hat rund
         60 Felder; das Handbuch beschreibt absichtlich nicht jedes. Eine
         Wache, die zu jedem Schalter einen Absatz einforderte, waere
         dauerrot und wuerde zu Recht ignoriert
         ([[dauerrote-wache-ist-keine]]).
    body: |
      ══ DIE BESCHRIFTUNG STEHT NICHT IMMER IM PFAD ══════════════════════
      Das Benutzerhandbuch schreibt sie hinein
      ("... -> Optik -> Mini-Player -> ,Wellen im Player'"), die
      Architekturdoku davor ("Beschriftung ,Wellen im Player' unter
      *Darstellung -> Optik -> Mini-Player*"). Die erste Fassung der Wache
      las nur den Pfad - und liess damit in der Architekturdoku genau den
      Fehler durch, den sie fangen soll: ein Pfad auf einen FALSCHEN, aber
      EXISTIERENDEN Reiter hat keinen Knopf, an dem er scheitern koennte.
      Erst ein Vorfeld von 140 Zeichen vor der Pfadangabe, in dem nach einem
      Zitat gesucht wird, schliesst das. Gefunden hat das die Gegenprobe,
      nicht das Nachdenken ([[gegenprobe-statt-gruen-glauben]]).

      ══ WAS AM SELBEN TAG SONST NOCH VERALTET WAR ═══════════════════════
      Derselbe Lauf fand, dass beide Doku-Dateien die Wellen weiter als
      REINE CSS-ANIMATION beschrieben - mit der Begruendung, eine
      Tonanalyse sei "strukturell unmoeglich". Fuer den BROWSER stimmt das
      und stimmt es weiter; der Irrtum war, eine Aussage ueber den Browser
      fuer eine Aussage ueber die BOX zu halten. E99 misst auf der Box
      (`pw-record` ohne `--target`, Goertzel, vier Baender, ~10/s) und
      liefert `pegel[]` additiv im Zustands-Poll mit; fehlt das Feld, faellt
      die Anzeige auf die CSS-Fassung zurueck. BEIDE Auspraegungen gehoeren
      in die Doku, nicht nur die neue - sonst steht die alte Box ohne
      Erklaerung da, warum ihre Wellen immer gleich aussehen.

      Eine Begruendung, die "unmoeglich" sagt, hat eine Haltbarkeit
      ([[wiki-zustand-hat-haltbarkeit]]): sie gilt fuer die Schicht, an der
      sie gemessen wurde, und eine Ausbaustufe kann sie an einer ANDEREN
      Schicht einloesen, ohne sie zu widerlegen.
    related: [praefix-in-der-wache-ist-die-namenssuche-durch-die-hintertuer, dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben, wiki-zustand-hat-haltbarkeit, leinwand-misst-sich-selbst-nicht-nach]
  - id: luefter-kennlinie-rastet-in-der-hysterese
    kind: falle
    title: "Eine Luefter-Stufe im Wohnbereich der Box rastet ein - die 5-Grad-Hysterese ist im Geraetebaum fest"
    match: "Luefter dauernd laut|Luefter bleibt auf hoher Stufe|pwm bleibt 150|Luefter geht nicht mehr runter|5900 U/min|Kennlinie zu scharf|cur_state eingefroren|warum ist die Box so laut"
    sev: med
    cause: "Die Pi-5-Trip-Hysterese liegt fest bei 5 Grad (sysfs verweigert das Schreiben auch als root). Eine Kennlinien-Stufe, deren Schwelle im NORMALBETRIEBSBEREICH der Box liegt (Ruhelage 53-58 Grad), wird einmal erklommen und erst unter (Schwelle - 5) wieder verlassen - also praktisch nie. Die scharfe Kennlinie vom 30.08. vormittags (150 ab 52 Grad) machte so aus einer Kinder-Hoerbox ein Dauergeblaese: 5.902 U/min bei 56 Grad, throttled=0x0"
    fix: "Schwellen so legen, dass die RAST-Stufe die leise ist: Grundstufe 90 ab 42 Grad traegt den Alltag, 110/220/255 erst ab 59/62/64 Grad - Vollgas genau VOR temp_limit=65 (siehe [[zwei-luefterkurven-und-die-grenze-darunter]]). Die Stufen-PWMs laedt NUR der Boot aus der config.txt; zur Laufzeit lassen sich nur die SCHWELLEN verschieben (trip_point_N_temp, Monotonie-Reihenfolge beachten). Und: cooling_device/cur_state NIE von Hand schreiben - das friert die Regelung ein (gemessen: 60,9 Grad bei PWM 90, Soft-Limit-aufgetreten 0x80000; weder Trip-Retrigger noch mode-Toggle weckten sie nachweisbar). Sauber wird es erst mit dem naechsten Neustart"
    tags: [luefter, pi5, thermik, hysterese, kennlinie, config-txt, 2026]
    related: [zwei-luefterkurven-und-die-grenze-darunter, mupi-bootwache-drehung]
    body: |
      ══ DER ABEND DES 30.08.2026 ("kannst du checken warum es so laut ist") ══
      Vormittags war die Kennlinie 42/52/58/65 mit Stufen 90/150/220/255
      eingespielt worden, damit der Luefter VOR dem temp_limit=65 aufdreht
      (E101: Drossel traf das WLAN). Am Abend stand die Box bei 56,0 Grad,
      throttled=0x0 - und PWM 150 mit 5.902 U/min. Nicht weil es noetig war,
      sondern weil die Stufe nicht mehr LOSLIESS: einmal ueber 52 Grad, und
      der Rueckweg beginnt erst unter 47.

      Die leise Fassung nutzt die Rast als Bauprinzip statt sie zu bekaempfen:
      die Stufe, in der die Box wohnt, ist die leise (PWM 90, ~3.600 U/min);
      die hoeheren Schwellen liegen ausserhalb des Wohnbereichs und duerfen
      ruhig rasten - wer 62 Grad erreicht hat, dem schadet Nachlauf nicht.

      ══ ZWEI LAUFZEIT-FALLEN ═══════════════════════════════════════════════
      1. `trip_point_N_temp` ist schreibbar und wirkt sofort - aber die
         PWM-Werte je Stufe (fan_tempN_speed) stehen im Geraetebaum vom
         Boot. Wer nur die config.txt aendert, misst danach die ALTE
         Stufentabelle und wundert sich (150 statt 110 auf Stufe 2).
      2. `cooling_device/cur_state` von Hand zu schreiben SIEHT aus wie ein
         Entrasten - tatsaechlich uebernimmt danach niemand mehr die
         Regelung: 20 Minuten spaeter stand die Box bei 60,9 Grad weiter
         auf PWM 90, und das Drosselregister trug frisch das
         Soft-Limit-aufgetreten-Bit. Der Griff ist verlockend und falsch.
  - id: soloist-meldet-sich-genau-einmal-an
    kind: falle
    title: "Scheitert Soloists eine Anmeldung in der Boot-Phase, bleibt die Box fuer immer ohne Spotify - bei 16 Prozent CPU"
    match: "keine Verbindung zu Spotify|login failed make sure --api-key|soloist stumm nach Boot|soloist hohe CPU|Spotify tot nach Neustart|api-key is valid"
    sev: high
    cause: "Soloist versucht die Anmeldung genau EINMAL beim Start. Faellt die in die unruhige Boot-Phase (Uhr unsynchron, Netz sortiert sich), kommt 'login failed: make sure --api-key is valid' - und der Prozess laeuft danach einfach WEITER: kein Exit, also greift Restart=always nie, und die Unit steht gruen auf 'active (running)'. Am 30.08.2026 zweimal am selben Abend passiert (Boots 17:58 und 18:33); der Schluessel war beide Male gueltig - derselbe Dienst meldete sich nach einem Neustart in fuenf Sekunden an"
    fix: "Die soloist-anmeldewache (scripts/soloist/ + Timer alle 2 min in scripts/systemd/, E104): sie liest das Logbuch DES LAUFENDEN Dienststarts (journalctl --since ActiveEnterTimestamp - der Fehlversuch von vorgestern zaehlt nicht); steht dort 'login failed' ohne 'logged in as', genau ein Neustart. Beide Suchtexte sind am Geraet gemessene Ausgaben von soloist 1.3.7.485. Verankert in allen Ausrollwegen (einrichten.sh, autosetup, Update-Schale, ausliefern-Listen; Deckungs-Wache prueft das). ABER: den Neustart nicht fuer harmlos halten - siehe [[soloist-neustart-kann-wiedergabe-ausloesen]]"
    tags: [soloist, spotify, boot, anmeldung, wache, systemd, 2026]
    related: [soloist-erste-messung, soloist-fehlte-in-beiden-schalen-wegen, soloist-neustart-kann-wiedergabe-ausloesen]
    body: |
      ══ WARUM DIE UNIT-MECHANIK HIER BLIND IST ══════════════════════════════
      systemd heilt Prozesse, die STERBEN. Soloist stirbt nicht: er traegt
      seinen Anmeldefehler als inneren Zustand und laeuft weiter - die
      WebSocket-API antwortet, der Prozess brennt ~16 Prozent CPU in einer
      stillen Schleife, und jede Statuspruefung ("active", "running",
      Prozess existiert) meldet gruen. Der einzige Ort, an dem der Zustand
      sichtbar wird, ist das eigene Logbuch. Genau dort liest die Wache.

      ══ DAS FENSTER IST KLEIN, ABER ES TRIFFT JEDEN MORGEN ══════════════════
      Der Fehlversuch kam 16 s nach dem Boot. Eine Box, die morgens
      eingeschaltet wird, wuerfelt jeden Tag neu, ob Uhr und Netz
      rechtzeitig stehen. Am 30.08. hiess das Ergebnis zweimal: "es gibt
      auch keine verbindung zu spotify" - den ganzen Abend, bis jemand von
      Hand neu startete. Mit der Wache dauert derselbe Fehler maximal zwei
      Minuten.
  - id: soloist-neustart-kann-wiedergabe-ausloesen
    kind: falle
    title: "systemctl restart soloist ist kein neutraler Befehl - Connect-Resume kann Wiedergabe starten, die keine Oberflaeche stoppen kann"
    match: "Box spielt von selbst|kann nicht mehr stoppen|Spotify unstoppbar|Stopp-Knopf wirkungslos|restoring session|Wiedergabe ohne Player|Abspiel-Engine kaputt"
    sev: high
    cause: "Soloist stellt beim Start seine Connect-Sitzung wieder her ('restoring session'). Hatte das Spotify-Konto einen aktiven Wiedergabe-Kontext, setzt Spotify die Wiedergabe VON SELBST fort - am 30.08.2026 gemessen: Tonstrom 'Spotify' auf der hoerbaren Klangkette, waehrend das Player-Logbuch still war. Der Player hat diese Wiedergabe nie angestossen, sein Zustand kennt sie nicht - also erreicht sie auch kein Pause- oder Stopp-Knopf der Oberflaeche. Die Box ist fuer konto-seitige Ereignisse blind: die soloist-WebSocket 127.0.0.1:5033 steht seit E42 offen und wird von niemandem gelesen ([[pegel-eine-stelle-soloist-steht-weit-offen]])"
    fix: "Fuer den Eingriff am lebenden Geraet: stop -> 20 s Karenz -> start. Die Abwesenheit laesst den aktiven Kontext bei Spotify veralten; nach der Karenz kam der Strom gemessen nicht wieder (die Sitzung wird als 'paused' wiederhergestellt). Die naechste Ausbaustufe steht in E104: den 5033-WebSocket abonnieren und konto-seitige Wiedergabe im Spielstand sichtbar UND stoppbar machen - erst dann ist ein soloist-Neustart bei laufender Box wieder harmlos"
    tags: [soloist, spotify, connect, resume, neustart, wiedergabe, 2026]
    related: [pegel-eine-stelle-soloist-steht-weit-offen, soloist-meldet-sich-genau-einmal-an, soloist-erste-messung]
    body: |
      ══ DIE KETTE VOM 30.08.2026 ("ich kann nicht mehr stoppen") ═══════════
      18:18 heilte ein Hand-Neustart die haengende Anmeldung (siehe
      [[soloist-meldet-sich-genau-einmal-an]]). Fuenf Sekunden spaeter war
      die Box angemeldet - und Spotify Connect setzte die Wiedergabe fort,
      die vor dem Ausfall lief. Fuer den Betreiber sah das so aus: Musik
      aus dem Nichts, und "die abspiel engine haben wir irgendwie kaputt
      bekommen", weil kein Knopf wirkte. Beides war korrektes Verhalten
      zweier Systeme, die nichts voneinander wussten.

      ══ WER STOPPEN WILL, MUSS WISSEN, WER SPIELT ══════════════════════════
      Der Stopp-Knopf der Oberflaeche stoppt DIE WIEDERGABE DES PLAYERS.
      Eine Wiedergabe, die Spotify selbst (App eines Elternteils,
      Connect-Resume, anderes Geraet derselben Anmeldung) auf die Box legt,
      hat im Player keinen Vertreter. Diagnose am Geraet in einer Zeile:
      `pactl -f json list sink-inputs` - ein Strom namens "Spotify" auf der
      hoerbaren Senke, waehrend der Player nichts spielt, IST der Befund.

      ══ WARUM 20 SEKUNDEN KARENZ ═══════════════════════════════════════════
      Ein schneller stop/start stellt dieselbe Sitzung samt aktivem Kontext
      wieder her - das Resume-Risiko bleibt. Nach ~20 s Abwesenheit meldet
      die wiederhergestellte Sitzung den alten Titel als 'paused'
      (gemessen: "paused ... [2:04 / 3:14]") und bleibt still, bis jemand
      aktiv Wiedergabe anfordert.
  - id: absolutpegel-taugen-nicht-zum-zeichnen
    kind: muster
    title: "Wellen aus Absolutpegeln sind fuer genau ein Material richtig - der Zeichner normiert auf den juengsten Spitzenwert"
    match: "Animation zu flach|man erkennt nichts|Wellen kaum sichtbar|Pegel zu klein gezeichnet|Kalibrier-Konstante nachdrehen|Ausschlag zu gering"
    sev: low
    cause: "Die vier Band-Pegel aus dem Player sind ehrlich normierte Absolutwerte 0..1 - aber ihr NUTZBEREICH haengt am Material: gemessene Sprache mit Musikbett ~0,09 Spitze, ein leises Album ~0,04-0,06. Wer solche Werte direkt als Anteil der Zeichenhoehe nimmt, bekommt bei 28 px Leinwand ein 1-2-px-Kraeuseln ('die animation ist zu flach. man erkennt nichts'). Eine nachgedrehte Konstante im LIEFERANTEN richtet genau ein Material und kappt das naechste"
    fix: "Automatische Verstaerkung im ZEICHNER, nicht im Lieferanten: Bezug = lautester juengster Ausschlag, Angriff sofort, Abklingen langsam (0,995 je Wert bei ~10/s), Boden 0,04 damit Grundrauschen nicht zur grossen Welle wird; echte Stille bleibt exakt flach (0/Bezug=0, und die Stille-Pause greift weiter). Skalieren BEIM ZEICHNEN, Puffer tragen Rohwerte - so waechst die Historie konsistent mit statt am Gelenk zu springen. Beweis in Bildpunkten messen (getImageData-Spannweite), nicht mit dem Auge: 12 von 14 px Hub nach dem Umbau gegen ~1 px davor"
    tags: [wellen, pegel, canvas, verstaerkung, agc, ui, 2026]
    related: [mit-echten-werten-rechnen, leinwand-misst-sich-selbst-nicht-nach]
    body: |
      ══ WARUM DIE VERSTAERKUNG IN DIE UI GEHOERT ════════════════════════════
      Der Lieferant (pegel.ts im Player) soll VERGLEICHBARE Werte liefern -
      wer dort verstaerkt, macht die Werte fuer jeden anderen Abnehmer
      (Statistik, Aufnahme-Pruefung, spaetere Anzeigen) unbrauchbar. Die
      Frage "wie gross male ich das" ist eine Darstellungsfrage; sie wird
      dort beantwortet, wo gemalt wird. Der Messzugang der Wellen-Messseite
      (window.mupiWellenMessung) bekam dafuer ein bezug()-Fenster: der
      Regelzustand ist ablesbar, die Gegenprobe speist bekannte Folgen und
      prueft die gezeichnete Spannweite in Bildpunkten.

      ══ DIE ZAHLEN VOM 30.08.2026 ═══════════════════════════════════════════
      ARD-Sprache: pegel-Spitze 0,088. Kalea-Album am Abend: 0,042-0,062.
      Leinwand der Statuszeile: 28 px hoch, Hub 11 px. Direkt gezeichnet
      waeren das 1,0 bzw. 0,7 px Ausschlag. Mit Bezug-Normierung fuellt
      dasselbe Material 12 px - und die vier Baender bleiben zueinander
      ehrlich, weil ALLE durch DENSELBEN Bezug geteilt werden (das
      staerkste Band definiert die Skala, die anderen ordnen sich ein).
  - id: quelle-kennt-ihre-kategorie-selbst
    kind: falle
    title: "Die verschmolzene Kachel erbt die Kategorie des Fuehrenden - und der lokale Abspielweg sucht dann im falschen Ordner"
    match: "kann keine alben abspielen die zusammengefasst sind|verschmolzene Kachel spielt nichts|aufgenommenes Album startet nicht|musicsearch findet nichts|falscher Medienordner|music statt audiobook"
    sev: high
    cause: "Der lokale Abspielbefehl heisst musicsearch/library/album/<kategorie>:<interpret>:<titel>, und die Oberflaeche nahm die Kategorie des WERKS. Ein verschmolzenes Werk fuehrt aber mit den Metadaten des Fuehrenden - bei 'Nah' (30.08.2026) Spotify mit `music`, waehrend der Mitschnitt unter media/audiobook/Alin Coen/ liegt. Gesucht wurde media/music/Alin Coen/, den es nicht gibt; die bevorzugte Quelle ist lokal, der Fehlgriff kommt vor jedem Rueckfall - die Kachel spielte NICHTS. Gleichkategorige Paare (und damit auch der Pruef-Schritt 'Verschmolzene Kachel spielt') treffen die Falle nie"
    fix: "Die Kategorie an die QUELLE, nicht nur ans Werk: `Quelle.kategorie` (werke.ts), gesetzt an der Geburtsstelle `werkAus` - die Verschmelzung reicht Quellen unveraendert durch, also reist sie von selbst mit. `abspielBefehl` (NewDesign/app.js, lokal-Zweig) nimmt `q.kategorie || w.kategorie`. Test zuerst ROT gesehen (actual: undefined), dann gruen; am Geraet nachgemessen: die Hand-verschmolzene Kachel traegt lokal->audiobook, spotify->music"
    tags: [verschmelzung, kategorie, lokal, abspielen, mitschnitt, 2026]
    related: [mit-echten-werten-rechnen, gegenprobe-statt-gruen-glauben]
    body: |
      ══ WARUM DIE GRUENE PROBE NICHTS SAH ═══════════════════════════════════
      Der Pruef-Schritt "Verschmolzene Kachel spielt" verschmilzt ein Paar
      DERSELBEN Kategorie - dort sind Werk- und Quell-Kategorie identisch,
      und der Befehl stimmt zufaellig. Die Falle oeffnet sich erst, wenn die
      Aufnahme (immer `audiobook`) ein Spotify-Album anderer Kategorie
      schluckt oder von ihm gefuehrt wird. Wer die Probe erweitert, nimmt
      ein music/audiobook-Paar - das ist der gemessene Fall.

      ══ DER BEWEISWEG OHNE EINEN TON ═══════════════════════════════════════
      1. /api/medien: beide Kacheln des Paars ansehen (Kategorien!).
      2. ls media/<werk-kategorie>/<interpret>/ - existiert der Ordner, den
         der Befehl bauen wuerde? Bei 'Nah': media/music/Alin Coen FEHLT,
         media/audiobook/Alin Coen traegt alle Aufnahmen.
      3. abspielBefehl-Grammatik gegenlesen (verb `musicsearch`,
         spotify-control.ts) - der Dienst waehlt nichts, er fuehrt aus.
      Wer stattdessen 'zur Probe' abspielt, uebertoent das Kind - die
      Ablage beantwortet dieselbe Frage lautlos.

  - id: mediathek-ist-die-zweite-laufzeitablage
    kind: falle
    title: "media/ ist zur Haelfte Baum und zur Haelfte Betreiber-Sammlung - eine Pfadwache ohne diese Trennung wird dauerrot"
    match: "TOT: media/|media/audiobook|media/music|doku-pfade-pruefen meldet Medienordner|Mediathek nicht im Baum"
    sev: low
    cause: "`tools/doku-pfade-pruefen.py` fragt die Gegenrichtung (gibt es noch, worauf die Doku zeigt?) und kannte bis zum 30.08.2026 nur ZWEI Sorten Ort, die git absichtlich nicht fuehrt: die Box-Ablage (`server/…`, `config/…`) und die Bau-Ablage (aus .gitignore). Die dritte fehlte: `media/` traegt im Baum nur `images/` und `sound/` (Logos, Signaltoene) - alles andere darunter ist die SAMMLUNG des Betreibers auf der Box (`/home/dietpi/MuPiBox/media/<kategorie>/<interpret>/`). Der E105-Eintrag `quelle-kennt-ihre-kategorie-selbst` nennt `media/audiobook/Alin Coen` und `media/music/Alin Coen` als Beweis - genau ihr Unterschied WAR der Fund - und wurde prompt zweimal als TOT gemeldet"
    fix: "`_mediathek()` neben `_laufzeit()`: ein Pfad `media/<x>/…` wird uebersprungen, wenn `<x>` KEIN vom Baum gefuehrter Unterordner ist. Welche das sind, wird aus `git ls-files media` gelesen statt hier nachgebaut - kommt ein mitgeliefertes `media/themes/` dazu, prueft die Wache es sofort wieder. Gegenprobe gefahren: eine erfundene Datei unter dem mitgelieferten Bilderordner, in BACKLOG.md eingeschleust, wird weiterhin gemeldet - eine erfundene Aufnahme unter der Kategorie-Ablage nicht"
    tags: [wache, doku, pfade, mediathek, laufzeit, 2026]
    related: [dauerrote-wache-ist-keine, doku-zeigt-auf-dateien-die-es-nicht-gibt, quelle-kennt-ihre-kategorie-selbst]
    body: |
      ══ WARUM NICHT EINFACH DIE ZWEI NAMEN AUSNEHMEN ════════════════════════
      Eine Ausnahme auf `media/audiobook/Alin Coen` haette den Lauf gruen
      gemacht und beim naechsten Interpreten wieder gemeldet - jeder Eintrag,
      der eine Ablage am Beispiel erklaert, braeuchte seine eigene Zeile. Das
      Merkmal ist nicht der Name, sondern: **der Ordner entsteht erst, wenn
      jemand Musik aufspielt oder aufnimmt.** Dieselbe Lehre wie bei der
      Bau-Ablage (`src/deploy`) einen Absatz darueber.

      ══ DER RIEGEL AN DER ABLEITUNG ═════════════════════════════════════════
      Waere `git ls-files media` je leer, wuerde die Regel die GANZE Mediathek
      durchwinken - inklusive echter Tippfehler in `media/images/…` - und
      dabei gruen aussehen. Darum meldet die Wache eine WARNUNG, wenn die
      Ableitung nichts findet; dieselbe Vorsichtsmassnahme wie bei `KERN` und
      `OBERSTE` (llmwiki `gruene-probe-deckt-nur-ihre-wachen`).

  - id: ausbleibende-daten-sind-ein-zustand
    kind: muster
    title: "Eine Animation, die aus einem Poll gespeist wird, braucht einen Abfluss - und der fliesst im DATENTAKT, nicht im Bildtakt"
    match: "Welle bleibt stehen|Ausschlag friert ein|Animation haengt beim Stopp|letztes Bild bleibt|Welle ruckelt|flache Schnellschuebe|Balken bewegen sich nach dem Stopp nicht"
    sev: low
    cause: "Beim Stopp traegt der Zustands-Poll das `pegel`-Feld GAR NICHT MEHR - es kommt keine Null, es kommt nichts. Ein Zeichner, der nur auf neue Werte reagiert und sich nach einer Nachlauffrist selbst anhaelt, laesst damit das letzte Wellenbild stehen ('wenn ich das lied stoppe soll auch der ausschlag null werden wie beim liedwechsel', 30.08.2026). Der naheliegende Fix - nach kurzer Frist Nullen nachschieben - hat eine zweite Falle: schiebt er sie im BILDTAKT (25/s), waehrend die Daten im Poll-Takt kommen (Spotify-Web-API: regelmaessig 300-800 ms), rasten mitten im laufenden Lied flache Schnellschuebe ins Band ('schiebt es immer eine teil ohne ausschlag schnell und dann ein ausschlag')"
    fix: "Zwei Groessen, beide aus dem gemessenen Datentakt abgeleitet: (a) Abfluss erst nach DREI ausgebliebenen Takten und mindestens 1,2 s - Poll-Zittern loest ihn nicht aus; (b) Nullen im DATENTAKT nachschieben, nicht je Bild - Ausfliessen ist damit exakt so schnell wie normales Wandern. Die Nullen NICHT durch den normalen Wert-Eingang schicken (das erneuert den Zeitstempel und bremst den Abfluss selbst aus). Erst auf flacher Linie haelt das Band an - so bleibt die Ersparnis aus E100 (keine Leinwand-Arbeit im Ruhezustand) erhalten"
    tags: [wellen, animation, poll, canvas, takt, ui, 2026]
    related: [absolutpegel-taugen-nicht-zum-zeichnen, mit-echten-werten-rechnen]
    body: |
      ══ EINE SEMANTIK FUER DREI EREIGNISSE ══════════════════════════════════
      Stopp, Liedwechsel und Netzloch sehen fuer den Zeichner GLEICH aus:
      es kommen keine Werte mehr. Wer sie einzeln behandelt (Stopp-Signal
      abfangen, Wechsel abfangen, Zeitablauf abfangen), baut drei Wege, von
      denen zwei ungetestet bleiben. Der Abfluss beantwortet alle drei mit
      derselben Regel - und das Netzloch, das niemand nachstellt, ist
      automatisch mit versorgt.

      ══ WARUM DER BILDTAKT DIE FALSCHE UHR IST ══════════════════════════════
      Der Bildtakt gehoert dem Zeichner, der Datentakt dem Lieferanten. Jede
      Groesse, die etwas ueber die DATEN aussagt ("sind sie ausgeblieben?",
      "wie schnell fliessen sie aus?"), muss an der Uhr der Daten haengen.
      Am 30.08.2026 kostete diese Verwechslung eine Runde: 600 ms Frist und
      25 Nullen/s waren gegen einen 300-800-ms-Poll ein Fehlalarm im
      Sekundentakt - sichtbar als ruckelnde Luecken mitten im Lied.

      ══ DER BEWEIS OHNE AUGENMASS ═══════════════════════════════════════════
      Messseite speisen (400-ms-Folge = langsamer Poll) und die gezeichnete
      Spannweite in Bildpunkten lesen: voller Ausschlag waehrend der
      Speisung, nach dem Ende Tinte nur noch auf der Mittellinie, danach
      keine weitere Zeichnung. Das Auge haelt beides fuer "sieht ok aus".

  - id: medienwache-sieht-nur-die-kategorieebene
    kind: falle
    title: "Der Medien-Waechter stat't nur media/<kategorie> — ein neues Album unter einem vorhandenen Interpreten loest ihn NIE aus"
    match: "neue Musik taucht nicht auf|Album erscheint nicht|change_checker|mediaCheckTimer|Medien neu einlesen|kopierte Dateien fehlen|m3u_generator laeuft nicht von selbst"
    sev: mid
    cause: "`scripts/mupibox/change_checker.sh` laeuft ueber `for dir in \"${MEDIA}/\"*` — das ist die ERSTE Ebene unter `media/`, also die drei Kategorieordner (`music`, `audiobook`, `other`) — und vergleicht deren `stat -c %Z` gegen `mediaCheckTimer`. Legt jemand ein neues Album unter einem Interpreten ab, den es schon gibt (`media/music/Alin Coen/Neues Album/`), aendert sich nur die Zeit von `media/music/Alin Coen` — der Kategorieordner darueber bleibt unberuehrt. Der Waechter schlaegt also nie an, `m3u_generator.sh` laeuft nicht, es entsteht keine `playlist.m3u` und kein Eintrag in `data.json`. Nur ein NEUER Interpret (ein neuer Eintrag direkt im Kategorieordner) hebt die Zeit"
    fix: "Von Hand anstossen: Verwaltung → Medien → **Medien neu einlesen** (`system.ts`, `AKTIONEN['medien-neu']` → `sudo /usr/local/bin/mupibox/m3u_generator.sh`). Im Benutzerhandbuch steht das seit dem 30.08.2026 im Abschnitt „Lokale Dateien\" — vorher stand dort „kopieren und eintragen — fertig\", was genau diesen Fall nicht kennt. Eine echte Behebung muesste den Waechter eine Ebene tiefer schauen lassen (Interpretenordner) oder auf inotify umstellen; das ist NICHT geschehen und bleibt offen"
    tags: [medien, change_checker, m3u_generator, lokale-alben, handbuch, 2026]
    related: [mediathek-ist-die-zweite-laufzeitablage, quelle-kennt-ihre-kategorie-selbst, dauerrote-wache-ist-keine, conf-update-loescht-schluessel-den-noch-jemand-liest]
    source:
      - gemessen am 2026-08-30 im Arbeitsbaum
      - scripts/mupibox/change_checker.sh:26-38
      - scripts/mupibox/m3u_generator.sh:131,181,231
    body: |
      ══ NACHGEMESSEN, NICHT ERSCHLOSSEN ═════════════════════════════════════
      Zwei Ordner angelegt und `stat -c %Z` gelesen:

        vor dem neuen Album      media/music            1788116880
        neues Album unter        media/music            1788116880  ← STEHT
        vorhandenem Interpreten  media/music/Alin Coen  1788116881
        neuer Interpret          media/music            1788116881  ← hebt sich

      Die Zeit eines Ordners aendert sich nur, wenn sich seine EIGENEN
      Eintraege aendern. Ein Album liegt zwei Ebenen tiefer.

      ══ WARUM ES SO LANGE NICHT AUFFIEL ═════════════════════════════════════
      Wer die Box einrichtet, kopiert MEHRERE Interpreten auf einmal — dabei
      entstehen neue Eintraege im Kategorieordner, der Waechter schlaegt an,
      und alles wirkt richtig. Der Fehlerfall ist der ZWEITE Besuch: ein
      einzelnes Album zu einem Kuenstler, den das Kind schon hat. Genau der
      Fall, den ein Betreiber als „die Box findet meine Musik nicht\" meldet.

      ══ DREI KATEGORIEN, NICHT BELIEBIG VIELE ═══════════════════════════════
      `m3u_generator.sh` hat die Kategorien fest verdrahtet (`:131` audiobook,
      `:181` music, `:231` other). Ein vierter Ordner unter `media/` wird beim
      Einlesen stillschweigend uebergangen — er wird nie zur Kachel, und es
      gibt keine Meldung darueber. Auch das steht seit dem 30.08.2026 im
      Benutzerhandbuch.

      ══ NICHT VERWECHSELN MIT DEM GELOESCHTEN SCHLUESSEL ════════════════════
      [[conf-update-loescht-schluessel-den-noch-jemand-liest]] teilt sich mit
      diesem Eintrag das Symptom „neue Musik taucht nicht auf" und den Namen
      `mediaCheckTimer`, hat aber eine andere Ursache: dort ist der WAECHTER
      SELBST kaputt (der Schluessel ist weg, `sleep` bekommt nichts).

      DIE EINE FRAGE, DIE ENTSCHEIDET: **wird ein NEUER INTERPRET bemerkt?**
      Einen leeren Ordner `media/music/Testkuenstler/` anlegen und warten.
      Taucht er auf, laeuft der Waechter richtig — dann ist es die Ebene
      (dieser Eintrag). Passiert auch dann nichts, ist der Waechter selbst
      dran (der andere Eintrag).

  - id: handbuch-versprach-eine-freigabe-die-nie-installiert-wurde
    kind: falle
    title: "Das Benutzerhandbuch versprach an zwei Stellen eine Samba-/FTP-Freigabe — kein Ausrollweg installiert eine"
    match: "Netzwerkfreigabe|Samba/FTP|Dateifreigabe|Musik aufspielen|smb.conf|proftpd.conf|wie kommt die Musik auf die Box"
    sev: mid
    cause: "Der Abschnitt „Lokale Dateien\" begann mit „Die Box hat eine Netzwerkfreigabe (Samba/FTP, Zugang siehe Abschnitt 9)\", und die Zugangstabelle fuehrte eine Zeile „Dateifreigabe (Samba/FTP) — Benutzer dietpi, Passwort mupibox\". BEIDES ist falsch: samba wurde nie installiert (E12/X13(c), am 04.08.2026 an der Box gemessen und aus `autosetup.sh` entfernt), und proftpd genauso wenig — `config/templates/proftpd.conf` wird von KEINEM Ausrollweg kopiert (AUDIT-2026-08-24 §7), das Paket steht weder in `packages2install` noch hinter einem `dietpi-software`-Aufruf. Im remote-step-installer (119 verfolgte Dateien) kommen samba und proftpd ueberhaupt nicht vor. Damit war der ERSTE Schritt von „wie bekomme ich eigene Musik auf die Box\" eine Sackgasse"
    fix: "Beide Stellen am 30.08.2026 richtiggestellt. Der Abschnitt nennt jetzt den Weg, den es wirklich gibt — den SSH-Zugang aus Abschnitt 9, also `scp` oder `sftp://dietpi@<box-ip>` — und beschreibt dazu die Ordnerebenen und das Nachladen. Die Tabellenzeile heisst jetzt „Musik aufspielen\", sagt ausdruecklich, dass es keine Samba-/FTP-Freigabe gibt, und verweist IN DER ZEILE auf den Abschnitt (Tabellen werden gescannt, nicht gelesen)"
    tags: [handbuch, doku, samba, ftp, ausrollweg, betreiber, 2026]
    related: [windows-freigabe-war-nie-da, medienwache-sieht-nur-die-kategorieebene, dauerrote-wache-ist-keine]
    source:
      - geprueft am 2026-08-30 im Arbeitsbaum und in remote-step-installer
      - AUDIT-2026-08-24.md:60-62 (proftpd.conf ohne Ausrollweg)
      - autosetup/autosetup.sh:497-506,781,1203-1221 (samba herausgenommen)
    body: |
      ══ WARUM DIE WIDERRUF-PROBE ES NICHT FAND ══════════════════════════════
      Es hat ja niemand widerrufen. Am 04.08.2026 wurde das REZEPT korrigiert
      (drei samba-Schritte heraus, mit langer Begruendung im Kommentar) — der
      Satz im Handbuch, der aus dem alten Rezept FOLGTE, blieb stehen. Genau
      die Klasse „ein fix, der ein Umweg ist, ueberlebt seine eigene
      Reparatur\": die Wache sucht nach markierten Widerrufen, und hier gab es
      keinen, weil niemand das Handbuch als betroffen erkannt hat.

      ══ DIE AUFZAEHLUNG HAT GETRAGEN ════════════════════════════════════════
      Die falsche Tabellenzeile stand zwischen SSH („Standard des Images\" —
      wahr) und „Wichtige Dienste\" (wahr). Beide Nachbarn stimmen, also sieht
      die Zeile dazwischen geprueft aus. Derselbe Einzelbefund wie so oft.

      ══ WAS DIE VORLAGEN-WACHE HIER NICHT LEISTET ═══════════════════════════
      `tools/ausgerollte-vorlagen-deckung.py` nimmt `proftpd.conf` AUSDRUECK-
      LICH aus („bekannt aus AUDIT-2026-08-24 §7, dort als offene Entscheidung
      gefuehrt\") — richtig fuer ihre eigene Frage, aber genau diese Ausnahme
      hat den Handbuch-Satz gedeckt. Die fehlende Wache ist eine andere Sorte:
      **ein Zugang, den die Betreiber-Doku als vorhanden ZUSAGT, ohne dass ein
      Ausrollweg ihn herstellt.** Es gibt eine Wache fuer die Gegenrichtung
      (`tools/horchende-ports-deckung.py`: Ports, die horchen und nicht
      dokumentiert sind) — diese Richtung fehlt. Als offener Punkt in
      BACKLOG.md notiert; hier bewusst KEINE schnelle Wache gebaut, weil sie
      ohne Zusage-Liste entweder tautologisch oder dauerrot wuerde
      ([[dauerrote-wache-ist-keine]]).
  - id: mitschnitt-kachel-braucht-plattenform-und-auswahlbruecke
    kind: falle
    title: "Eine Mitschnitt-Kachel scheitert in Schichten - Anzeige-Metadaten statt Plattenform, und die Profil-Auswahl kennt ihre Zuordnungs-Partner nicht"
    match: "aufgenommenes Album spielt falsches Lied|zeigt falsche Daten beim Wechsel|verschmilzt nicht fuer dieses Profil|Mitschnitt-Kachel fehlt|musicsearch findet aehnliches|Zuordnung greift nicht|Sonderzeichen Ordnername"
    sev: high
    cause: "Zwei getrennte Schichten, am 30.08.2026 am selben Abend gemessen. (1) PLATTENFORM: die Aufnahme sanitisiert Ordnernamen ('/' wird '_'), das verschmolzene Werk fuehrt aber mit Spotify-Metadaten - ein musicsearch-Befehl daraus sucht Ordner, die es nie gab, und faengt im Zweifel etwas AEHNLICHES (falsches Lied, falsche Anzeige, Spruenge). (2) AUSWAHL: gefiltert wird VOR der Verschmelzung, und die Profil-Medienauswahl kennt nur die Schluessel vom Anhaken - die spaeter angelegte Mitschnitt-Kachel faellt heraus, die Hand-Zuordnung findet ihr Mitglied nicht ('wird uebergangen', verschmelzung.ts), nichts verschmilzt. Tueckisch: fuer das AUFNEHMENDE Profil stimmt alles (sichtbarMachen traegt den Schluessel dort ein), nur andere Profile sehen den Fehler"
    fix: "(1) Quelle.lokalPfad (werke.ts): Kategorie/Interpret/Titel exakt wie auf der Platte, gesetzt an der Geburtsstelle werkAus; abspielBefehl nimmt ihn VOR den Werk-Metadaten. (2) auswahlUmZuordnungenErweitern (abgleich.ts, rein): wer EIN Mitglied einer Zuordnung gewaehlt hat, hat sie ganz gewaehlt, in beide Richtungen - verdrahtet an ALLEN Filterstellen. Achtung: /api/werke filtert SELBST, waehrend der Kommentar an sichtbareMedienLesen das Gegenteil behauptete - Aufruferlisten in Kommentaren altern ([[wiki-zustand-hat-haltbarkeit]])"
    tags: [mitschnitt, verschmelzung, auswahl, profil, plattenform, sanitisierung, 2026]
    related: [quelle-kennt-ihre-kategorie-selbst, wiki-zustand-hat-haltbarkeit, gegenprobe-statt-gruen-glauben]
    body: |
      ══ WARUM ES IN SCHICHTEN KAM ══════════════════════════════════════════
      Der Abend fand erst die Kategorie (music/audiobook, Eintrag
      [[quelle-kennt-ihre-kategorie-selbst]]), dann die Plattenform, dann
      die Auswahl - jede Schicht verdeckte die naechste, und jede Probe
      am 'freundlichen' Beispiel (Nah: keine Sonderzeichen, Profil des
      Aufnehmenden) blieb gruen. Wer eine Mitschnitt-Meldung prueft,
      nimmt das UNFREUNDLICHSTE Paar: Sonderzeichen im Titel, anderes
      Profil als das aufnehmende, andere Kategorie als der Fuehrende.

      ══ DER BEWEISWEG (ohne einen Ton) ═════════════════════════════════════
      1. /api/medien: Schluessel BEIDER Kacheln des Paars.
      2. /api/verschmelzung: steht die Zuordnung, stimmen die Mitglieder
         ZEICHENGENAU mit den Schluesseln ueberein?
      3. /api/werke?verschmelzen=1 UNTER DEM BETROFFENEN PROFIL: erscheint
         das Werk mit stufe und BEIDEN Quellen samt lokalPfad?
      4. ls media/<kategorie>/<interpret>/ - existiert der Plattenform-Ordner?
      Schicht fuer Schicht; die erste Stufe, die abweicht, ist die Baustelle.
  - id: soloists-konto-ist-nicht-das-webapi-konto
    kind: falle
    title: "Eine Web-API-Pause trifft Soloists Wiedergabe nie, wenn die Konten getrennt sind - der Hebel ist soloist ctl"
    match: "pause wirkt nicht spotify|stop stoppt spotify nicht|Geraeteliste ohne MixPiBox|fremde Wiedergabe bleibt stehen|soloist ctl pause|zwei spotify konten box|web api device fehlt"
    sev: high
    cause: "Soloist meldet sich mit einem EIGENEN kontogebundenen spak_-Schluessel an; das Web-API-Token des Players kann zu einem ANDEREN Konto gehoeren. Am 30.08.2026 am lebenden Geist gemessen: /api/spotify/laeuft sagte 'spielt auf dieser Box', die Geraeteliste des Web-API-Kontos enthielt aber nur eine fremde Soundbar und KEINE MixPiBox. Jede spotifyApi.pause() laeuft dann ins Leere - mit Antwort 200 und ohne eine Spur, wenn der logLevel (Vorgabe 'error') alle info/warn-Zeilen verschluckt"
    fix: "Fuer Wiedergabe, die Soloist selbst traegt, dessen EIGENEN Steuerkanal nehmen: `soloist ctl pause -D /var/lib/soloist` (das -D zeigt aufs StateDirectory mit der WebSocket-Portdatei; ctl play/now gibt es ebenso). Im Player (E104, anhaltenNachSpieler): erst ctl, dann GEGENMESSEN (/api/spotify/laeuft frisch), erst bei Fortbestand der Web-API-Weg mit aufgeloestem Geraet. Beweisfuehrung ohne Ton: Standard-Senke auf mixpi-mitschnitt, Strom per pactl move-sink-input nachziehen, hinterher zuruecksetzen - und Senken IMMER am Namen aufloesen, die Nummern wandern ueber Neustarts"
    tags: [soloist, spotify, konto, pause, e104, ctl, 2026]
    related: [soloist-erste-messung, soloist-neustart-kann-wiedergabe-ausloesen, soloist-meldet-sich-genau-einmal-an]
    body: |
      ══ WARUM ES NIEMAND SAH ════════════════════════════════════════════════
      Drei Verdeckungen uebereinander: die Pause antwortete 200 (Spotify
      lehnt eine gerätelose Pause nicht ab, sie verpufft), der
      Player-logLevel 'error' verschluckte jede eigene Meldung, und der
      Stopp-Antwortkoerper meldete 'nichts-zu-tun' - was nach "war ja
      nichts" aussieht statt nach "kam nicht ran". Erst der Blick in die
      GERAETELISTE des Web-API-Kontos machte die Konten-Trennung sichtbar:
      keine MixPiBox darin, obwohl die Box hoerbar spielte.

      ══ DER EINGEBAUTE WEG (E104) ═══════════════════════════════════════════
      Vor jedem Anhalten fragt der Player die lokale Spielstand-Auskunft
      (E75-Draht des Servers). Spielt etwas AUF DIESER BOX, das die eigene
      Buchfuehrung nicht kennt: soloist ctl pause, Gegenmessung, sonst
      Web-API mit Geraete-Nachschlag. /state und /local tragen additiv ein
      `fremd`-Feld - die Oberflaeche KANN die Geisterwiedergabe damit
      anzeigen (Ausbaustufe). Fremde GERAETE beruehrt weiterhin niemand:
      angehalten wird nur, was `aufDieserBox` traegt.
  - id: die-ui-spielt-das-backend-entscheidet
    kind: entscheid
    title: "Betreiber-Entscheid: die UI hat EINE Spielfunktion - Fusion und Playerwahl entscheidet das Backend nach eingestellten Regeln"
    match: "wo gehoert die quellenwahl hin|abspielBefehl erweitern|ui baut befehle|playerwahl|spielfunktion backend|kapselung fusion|neue quelle anschliessen"
    sev: high
    cause: "Betreiber, 30.08.2026 woertlich: 'also wichtig die kapselung [ui] ->[backend] fusion und playerwahl. die ui spielt mit der spielfunktion das backend entscheidet nach eingestellten regeln.' Der Abend davor ist der Beweis: drei Fehlschichten in Folge (Kategorie, Plattenform, Nummernraum - E105) waren alle Reparaturen an ENTSCHEIDUNGSLOGIK, die in der UI wohnte (abspielBefehl, weiterSpielen). Jede Quelle, jede Sanitisierung, jeder Nummernraum, den die UI kennt, ist eine kuenftige Falle in einer Schicht, die niemand serverseitig testen kann"
    fix: "Der Vertrag steht in BACKLOG E95/V: POST /api/spielen {schluessel, titelNr?, positionMs?, quelle?} - das Backend loest das Werk auf (Verschmelzung, Auswahl-Bruecke), waehlt die Quelle nach den eingestellten Regeln (inkl. Nummernraum-Uebersetzung), schickt die Befehle selbst durch die bestehenden Tore (Kinderzeit, Anbieter-Schalter, Plugin-Ereignisse) und antwortet, WAS gespielt wird. Die UI zeigt nur an. Der Player behaelt die MASCHINEN-Schlichtung (tonmaschineUebernehmen, E104-Fremdanhalten); die WAHL zieht zum Server. Wer eine neue Quelle anschliesst, fasst danach GENAU EINE Stelle an"
    tags: [architektur, kapselung, verschmelzung, playerwahl, e95, entscheid, 2026]
    related: [quelle-kennt-ihre-kategorie-selbst, mitschnitt-kachel-braucht-plattenform-und-auswahlbruecke, soloists-konto-ist-nicht-das-webapi-konto]
    body: |
      ══ DIE REGEL IN EINEM SATZ ═════════════════════════════════════════════
      Die UI sagt "spiele dieses Werk (ab Titel N, ab Position M)" - alles
      andere (welche Quelle, welche Maschine, welcher Pfad, welche Zaehlung)
      ist Backend-Sache und dort nach den EINGESTELLTEN Regeln entschieden,
      pruefbar mit reinen Funktionen, die es groesstenteils schon gibt
      (bevorzugteQuelle, waehleInhalt, quellenOrdnen).

      ══ WORAN MAN ERKENNT, DASS MAN DAGEGEN VERSTOESST ═════════════════════
      Ein UI-Diff, der einen Dienstnamen, eine Kategorie, einen Ordnernamen
      oder eine Titelnummer-Regel anfasst. Genau solche Diffs gab es am
      30.08.2026 dreimal in vier Stunden - jeder einzelne war notwendig,
      und keiner waere es gewesen, haette der Vertrag schon gegolten.

      ══ DAS ABNAHMEKRITERIUM (Betreiber-Nachsatz, 30.08. spaet) ═════════════
      Woertlich: "es soll ja auch einfach moeglich sein neue dienste
      einzupflegen - wenn wir damit dann wieder gui schaschlik haben ist
      nichts gewonnen." Daraus die Abnahme von E95/V:

        EIN NEUER DIENST BERUEHRT DIE UI MIT NULL VERHALTENS-ZEILEN.

      Erlaubt bleibt in der UI nur GENERISCHE Darstellung: ein Dienst-
      Kennzeichen, das als DATEN vom Backend kommt, und eine Farbpalette im
      CSS. Keine Weiche, kein Befehlsbau, kein Pfadwissen, keine
      Titelnummern-Regel. Der Standard-Anschluss fuer Neues ist der
      PLUGIN-Weg (E87: type 'plugin', Schluessel `plugin:<kennung>:<rest>`,
      Inhalt ueber den Plugin-Vertrag) plus Regeln im Backend - der existiert
      genau dafuer.

      ══ DIE ZWEI ZAHLEN, GEGEN DIE ABGENOMMEN WIRD ══════════════════════════
      Beide misst `node tools/ui-spiellogik-messen.mjs` (Stand 30.08.2026);
      mit `--deckel <zahl>` laeuft es als Wache und steigt bei 1 aus.

        552 Zeilen Spiel-Entscheidungslogik in NewDesign/app.js
            abspielBefehl 113, weiterSpielen 157, ardSendungAb 64,
            spielen 61, albumSpielen 157
        558 Dienstnamen-Vorkommen in derselben Datei
            spotify 221, Spotify 206, ARD 67, mpv 54, radio 10

      Die zweite Zahl ist die RATSCHE der Stufe 4 (Muster
      tools/dienste-deckung.mjs): sie darf nur SINKEN, bis eine kleine,
      begruendete Sichtliste uebrig ist. Sie HAENGT seit dem siebten
      Doku-Lauf (30.08.2026) in tools/pruefen.sh - Schritt "Dienstwissen in
      der UI (Ratsche E95/V)", `--deckel 558`, neben der fetch-Frist-Ratsche.
      Bis dahin war sie eine Wache, die nie rot werden konnte: geschrieben,
      urteilsfaehig, in keinem Laeufer - `ungerufene-wachen.py` meldete sie
      als die einzige Luecke des Laufs. Wer den Rueckbau der Stufe 2 macht,
      setzt den Deckel dort HERAB, sonst hat der Rueckbau keine Gegenstelle. Nach dem Umbau bleibt von den 552
      ein Aufruf plus Anzeige - der app.js-Deckel darf zum ersten Mal sinken
      statt zu steigen, und E102 (Android-Media-App) bekommt seine
      Schnittstelle geschenkt, statt die Logik in Kotlin nachzubauen.

      VORSICHT MIT ZITIERTEN STAENDEN: der BACKLOG-Nachsatz nannte zuerst
      405 Zeilen, weil `albumSpielen` mit 10 statt 157 gefuehrt war. Deshalb
      liegt die Erhebung jetzt in einem Werkzeug und nicht in einem Satz -
      wer eine der Zahlen zitiert, misst sie nach.
  - id: kinderzeit-erlaubnisliste-kennt-neue-verben-nicht
    kind: falle
    title: "Ein Verb, das nicht in der START-Liste steht, spielt nach dem Zubettgehen weiter - E87 hat plugin/pluginqueue verpasst"
    match: "plugin spielt trotz kinderzeit|kinderzeit greift nicht|istStartbefehl|neues verb abspieldienst|erlaubnisliste start|wiedergabeGestartet fehlt plugin"
    sev: high
    cause: "istStartbefehl (kinderzeit.ts) ist eine ERLAUBNISLISTE per URL-Muster, und ihr eigener Kommentar warnt: 'Ein Verb, das niemand eintraegt, ist kein Startbefehl - damit spielt der neue Dienst als einziger auch nach dem Zubettgehen weiter.' Genau das ist bei E87 passiert: der /inhalt-Zweig baut `plugin/...` und `pluginqueue/...`, der Abspieldienst fuehrt beide Verben (spotify-control.ts) - nur die Liste kannte sie nicht. Eine Plugin-Kachel lief damit an Kinderzeit UND Anbieter-Tor vorbei, und ihr Start feuerte kein wiedergabeGestartet-Ereignis (ereignisAusBefehl haengt am selben istStartbefehl). Gefunden am 31.08.2026 beim E95-Umbau, nicht durch eine Wache"
    fix: "pluginqueue|plugin in die START-Alternation (laengeres Wort zuerst), Messfall in kinderzeit.spec.ts. Die STRUKTURELLE Abhilfe ist E95/V: POST /api/spielen fragt die Tore als FUNKTIONEN (kzStand, dienstAktiv) statt ueber URL-Muster - sobald die UI nur noch diesen Weg nimmt, kann kein neues Verb mehr an der Liste vorbeilaufen. Bis dahin gilt: wer ein Verb in den Abspieldienst einbaut, traegt es in DERSELBEN Aenderung in die START-Liste ein"
    tags: [kinderzeit, plugin, e87, e95, erlaubnisliste, 2026]
    related: [die-ui-spielt-das-backend-entscheidet]
    body: |
      ══ WARUM KEINE WACHE ES FING ═══════════════════════════════════════════
      tools/kachel-spielt-je-dienst.mjs prueft, dass jede Kachel SPIELT -
      nicht, dass sie sich SPERREN laesst. Die Luecke ist unsichtbar gruen:
      kein Test rot, kein Bau bricht, das Kind hoert einfach weiter. Der
      Kommentar ueber der Liste hat den Fall 27 Tage vor dem Fund woertlich
      vorhergesagt.

      ══ DIE LEHRE ═══════════════════════════════════════════════════════════
      Eine Erlaubnisliste altert mit jedem neuen Verb. Funktionen altern
      nicht: spielweg.ts (Plugins) und POST /api/spielen (E95/V) fragen die
      Kinderzeit direkt. Der Proxy-Weg mit istStartbefehl bleibt fuer die
      Alt-Oberflaeche das erste Netz - und diese Falle sein Preisschild.
  - id: decodeuri-ist-nicht-das-gegenstueck-von-encodeuricomponent
    kind: falle
    title: "decodeURI laesst %2C %26 %2B stehen - ein Komma im Interpreten machte jeden Mitschnitt stumm, bei gruener Buchfuehrung"
    match: "lokales album spielt nicht|mitschnitt spielt nicht|mplayer startet nicht|playlist.m3u nicht gefunden|decodeURI|komma im interpreten|kein ton aber play|musicsearch stumm"
    sev: high
    cause: "Die Oberflaechen kodieren die Segmente des musicsearch-Befehls mit encodeURIComponent; `playList` im Abspieldienst dekodierte mit decodeURI - und decodeURI laesst die reservierten Zeichen KODIERT stehen (%2C Komma, %26 &, %2B +, %23 #). Am 31.08.2026 am Geraet gemessen (Interpret 'Team Karacho, Rola'): der m3u-Pfad hiess 'Team Karacho%2C Rola', den Ordner gibt es nicht, mplayer bekam nichts - und die Buchfuehrung meldete trotzdem play (writeplayerstatePlay steht VOR dem Laden), totalTracks stimmte sogar, weil die Titelzaehlung ZWEISTUFIG dekodierte (decodeURIComponent auf das decodeURI-Ergebnis) und den echten Ordner fand. Betroffen war jeder Mitschnitt mit Komma im Interpreten - die halbe Aufnahme-Bibliothek"
    fix: "medienPfadAus in befehlspfad.ts (Player): ERST an den rohen `:` trennen, DANN jedes Segment einzeln decodeURIComponent (mit Rueckfall aufs rohe Segment statt URIError - ein Wurf risse den Dienst um). playList und queueAlbum nutzen sie; currentMeta.path ist seither KLARTEXT, das zweite Dekodieren in der Titelzaehlung ist raus. Wachen: befehlspfad.spec.ts (Komma-Messfall woertlich). Wer irgendwo decodeURI auf encodeURIComponent-Kodiertes trifft: dasselbe Loch"
    tags: [player, lokal, mitschnitt, kodierung, e106, befehlspfad, 2026]
    related: [mitschnitt-kachel-braucht-plattenform-und-auswahlbruecke, quelle-kennt-ihre-kategorie-selbst]
    body: |
      ══ WARUM ES NIEMAND SAH ════════════════════════════════════════════════
      Drei gruene Anzeigen ueber einem stummen Lautsprecher: der Befehl
      antwortete 200 (der Dienst prueft den Pfad nicht), der Spielzustand
      stand auf play (vor dem Laden geschrieben), und totalTracks stimmte
      (die Zaehlung dekodierte doppelt und fand den Ordner, den der
      Abspieler nie bekam). Zwei Wahrheiten in EINER Funktion - die falsche
      machte den Ton. Erst der Blick in den mpv (playlist-count 0,
      core-idle true) trennte Buchfuehrung von Wirklichkeit.

      ══ DIE REGEL ═══════════════════════════════════════════════════════════
      Wer encodeURIComponent kodiert, muss decodeURIComponent dekodieren -
      und zwar JE SEGMENT, nach dem Trennen an den rohen Trennzeichen.
      decodeURI ist das Gegenstueck von encodeURI und laesst ; / ? : @ & =
      + $ , # kodiert stehen. Ein `:` IM Inhalt kommt als %3A an und wird
      erst NACH dem Split zum Zeichen - so entsteht nie ein neuer Trenner.
  - id: post-api-spielen-ist-die-spielfunktion
    kind: architektur
    title: "POST /api/spielen (E95/V Stufe 1): das Backend waehlt Quelle, Befehl, Folge und Sprung - die Tore sind Funktionen, nicht URL-Muster"
    match: "wie spielt man ein werk|api spielen|spielfunktion aufrufen|neuen dienst anschliessen abspielen|quellenwahl server|nachlauf tracknr seekpos|kachel tipp backend"
    sev: high
    cause: "Betreiber-Entscheid vom 30.08.2026 ('die ui spielt mit der spielfunktion das backend entscheidet nach eingestellten regeln'), gebaut am 31.08.2026. Die Entscheidungsregeln aus NewDesign/app.js (abspielBefehl, weiterSpielen, ardSendungAb, angekommen) leben jetzt als reine Funktionen in backend-api/spielfunktion.ts (startPlan, versuchsQuellen, folgeWaehlen, seekZiel, sprungAngekommen - 21 Tests), die Route in server.ts fuehrt den Ablauf: Werk aufloesen ueber die /api/werke-Kette (Profil-Auswahl + Zuordnungs-Partner + Verschmelzung nach Darstellungs-Schalter), Kinderzeit ZWEIMAL als Funktion (spielweg-Muster), Anbieter-Schalter als SIEB (abgeschaltete Quelle wird uebersprungen, solange eine andere spielt - vorher beendete ihr 403 den ganzen Versuch), Befehle direkt an :5005, Plugin-Ereignis je gutem Befehl, Anhaengen+Sprung als Nachlauf NACH der Antwort mit spielLauf-Abloese (jeder Proxy-Start/stop loest ab)"
    fix: "Aufruf: POST /api/spielen {schluessel, titelNr?, positionMs?, positionProzent?, folge?, ausStelle?, quelle?, gemerktBei?}. Antworten: 200 {ergebnis:'ok', dienst, gestartetNr, gestartetFolge?, titel?}, 200 {ergebnis:'seite'} (Interpret), 403 kinderzeit/anbieterAus, 404 unbekanntes-werk, 409 abgeloest, 502 geht-nicht+grund. Am Geraet bewiesen (31.08.2026, stumm ueber mixpi-mitschnitt): lokal-Plattenform mit Komma-Interpret, ARD-Sendung 60 Folgen komplett angehaengt, Folgen-Sprung per Kennung auf 40% (mpv time-pos 117s = 97.8 + Spielzeit). Ein neuer Dienst braucht kuenftig: Plugin (E87) + Regeln im Backend - null UI-Verhaltenszeilen"
    tags: [architektur, e95, spielfunktion, kapselung, server, 2026]
    related: [die-ui-spielt-das-backend-entscheidet, kinderzeit-erlaubnisliste-kennt-neue-verben-nicht, mitschnitt-kachel-braucht-plattenform-und-auswahlbruecke]
    body: |
      ══ DIE BELEGE DES NACHLAUFS - beide am Geraet bezahlt ═════════════════
      Nach `tracknr:` raeumt der Abspieldienst weder timePos noch duration
      weg; beide zeigen die VORIGE Folge, bis die neue geladen ist. Zwei
      Fallen daraus, beide am 31.08.2026 gemessen (MausZoom, Ziel 40%):
        1. Lief die vorige LAENGER als das Ziel, hielt sprungAngekommen die
           alte Stelle fuer den gelungenen Sprung (Folge 1 bei 150 s, Ziel
           98 s) - Abbruch ohne je zu springen. Derselbe Fehlgriff steckt
           im UI-Vorbild titelUndStelle.
        2. Die 9-s-Anlauf-Frist der Oberflaeche riss: Start + Titelwechsel
           sind ZWEI Netz-Ladungen, dazu der Poll-Takt des Abspieldienstes.
      Deshalb: nach einem Wechsel gilt der Doppel-Beleg duration>0 UND
      timePos<=5 (die frische Folge faengt bei null an), Deckel 20 s
      (SPIEL_WECHSEL_FRIST_MS); ohne Beleg bleibt es beim einen Blindsprung.

      ══ OFFENE SPUR ═════════════════════════════════════════════════════════
      /local hinkt bei ARD dem mpv um viele Sekunden nach (timePos stand
      97.8, waehrend mpv bei 117 war) - der Nachlauf-Beleg waere schaerfer,
      wenn der Abspieldienst seinen mpv-Stand frischer meldete. Und die UI
      nimmt den neuen Weg noch NICHT: Kachel-Tipp, Weiterhoeren und der
      Rueckbau der UI-Spiellogik sind die naechste Stufe - die Masse dafuer
      misst tools/ui-spiellogik-messen.mjs (Start: 552 Zeilen, 558
      Dienstnamen-Vorkommen; nur Sinken erlaubt).
  - id: ui-ruft-die-spielfunktion
    kind: architektur
    title: "E95/V Stufe 2: Raster, Weiterhoeren und Folgen-Lane schicken EINEN POST /api/spielen - die Wache liest zwei Leitungen"
    match: "kachel tipp umbauen|spielenAnfordern|ui spielt nicht mehr selbst|wache misst alten draht|kachel-spielt-je-dienst rot|vorschau api spielen|interpretenseite alt-weg"
    sev: high
    cause: "Stufe 2 des Betreiber-Entscheids [[die-ui-spielt-das-backend-entscheidet]], gebaut 31.08.2026: spielen(), weiterSpielen(), albumSpielenAusLane(), titelWeiter() und die Folgen-Kacheln sind Huellen ueber EINER Funktion spielenAnfordern(w, wunsch, kachel) - sie schickt POST /api/spielen und uebersetzt die Ausgaenge in Anzeige (403->verweigertMelden, grund-Codes->die alten Saetze via spielenFehlersatz, titel-Antwort->laufendeFolgen fuers Cover, gespieltMelden NUR nach Erfolg). ausQuelleSpielen und spotifySpielteEben sind geloescht; die UI-Spiellogik sank von 552 auf 373 Zeilen, die Dienstnamen-Vorkommen von 558 auf 533 (tools/ui-spiellogik-messen.mjs, am 31.08.2026 nachgemessen). ACHTUNG: die Wache in tools/pruefen.sh steht weiter auf `--deckel 558` - zwischen erreichtem Stand und Deckel liegen 25 Vorkommen Luft, die unbemerkt zurueckwachsen koennen. Wer die Ratsche anziehen will, setzt den Deckel auf den gemessenen Stand"
    fix: "Drei Nebenpflichten, alle am selben Abend bezahlt: (1) tools/kachel-spielt-je-dienst.mjs liest jetzt ZWEI Leitungen mit - /player-Befehle UND den /api/spielen-Wunsch samt Schluessel; wer nur die alte laese, meldete jede umgestellte Kachel als stumm (Fehlalarm). (2) tools/neu-vorschau.mjs braucht einen /api/spielen-Stub (absichtlich duenn: Echo+Protokoll wie /api/weiterhoeren) - eine geliehene Vorschau ALTER Fassung antwortet 404 und die Seite meldet 'nicht mehr in der Bibliothek', obwohl alles stimmt: nach einem Vorschau-tragenden Umbau die Dauer-Vorschau NEU STARTEN. (3) Die INTERPRETENSEITE baut ihre Befehle noch selbst (abspielBefehl/starte/ardSendungAb, Zeilen ~3738/3746/3865) - sie spielt auch KUENSTLICHE Werke ohne Bibliothekseintrag (spotify:<kennung>), die der Server heute per 404 abwiese; ihre Umstellung braucht eine Server-Erweiterung (Wunsch per Kennung statt Schluessel) und ist Stufe 3"
    tags: [architektur, e95, spielfunktion, ui, wache, vorschau, 2026]
    related: [die-ui-spielt-das-backend-entscheidet, post-api-spielen-ist-die-spielfunktion, attrappe-luegt-durch-weglassen]
    body: |
      ══ WAS IN DER UI BLEIBT - Anzeige, keine Entscheidung ══════════════════
      stelleMerken() VOR dem POST (der Server stoppt als Erstes, danach ist
      die Stelle des Vorigen weg), das Kachel-Leuchten (1,4 s im Raster,
      bis-Antwort im Weiterhoeren), zuletztGestartet/gemerktSchreiben,
      laufendeFolgen aus der titel-Antwort, gespieltMelden nur nach Erfolg,
      die Nachzieher (weiterHolen nach 2 s, lokalHolen, dienstHolen). Die
      Interpreten-Weiche (Kachel oeffnet die Seite, E45) steht VOR dem POST.

      ══ AUSGAENGE-UEBERSETZUNG (spielenFehlersatz) ══════════════════════════
      403 -> verweigertMelden (Kinderzeit UND anbieterAus, ein Urteil);
      404 -> "nicht mehr in der Bibliothek"; 409 abgeloest -> STILL (der
      ueberholende Tipp meldet sich selbst); grund 'keine-folge'/'keine-titel'
      /'spotify-anmeldung'/'unspielbar' -> die woertlichen Saetze der alten
      Wege; 'inhalt-*' -> dienstgenau ("ARD Sounds antwortet nicht" usw.).
  - id: geschriebene-darstellung-macht-lage-schalter-taub
    kind: falle
    title: "Nach einem PUT /api/darstellung an die Vorschau greifen /vorschau/marke-* nicht mehr - der Schnappschuss traegt das Feld seit dem 31.08.2026 mit"
    match: "vorschau lage wirkt nicht|marke tanzt rot|phantom rot vorschau|darstellung geschrieben|lage schalter taub|leihgabe legt nicht zurueck|messung nicht moeglich vorschau"
    sev: medium
    cause: "Die Vorschau (tools/neu-vorschau.mjs) merkt sich nach einem PUT /api/darstellung die GESCHRIEBENE Darstellung (DARSTELLUNG.geschrieben) und serviert fortan sie - die Lage-Schalter (/vorschau/marke-ruhig, verschmolzen-an usw.) erreichen aktuell.* dann nie mehr. Bis zum 31.08.2026 deckte der Lage-Schnappschuss dieses Feld NICHT: zurueckgeben() stellte die Lagen zurueck, die geschriebene Darstellung blieb. Das kostete DREI Phantom-Rots in EINEM Probelauf (marke-tanzt, marke-grenzfaelle, verschmelzung-befehle): ein frueher Schritt schrieb, drei spaete massen die Fassung von gestern - alle drei Werkzeuge waren am frischen Stand mehrfach gruen"
    fix: "ZWEI Ebenen, beide gebaut. WURZEL (behoben): der Schnappschuss in neu-vorschau.mjs traegt seit dem 31.08.2026 `darstellung: structuredClone(DARSTELLUNG)` als fuenftes klebriges Feld, und schnappschussSetzen() legt es GANZ zurueck - samt `geschrieben`, denn nur so greifen die Lage-Schalter danach wieder. Rundlauf am eigenen Port nachgemessen (31.08.2026): PUT {aktuell:{...}} -> geschrieben=true, der Lage-Schalter laeuft danach ins Leere (serviert bleibt die geschriebene Fassung), Schnappschuss zurueck -> geschrieben=false. NETZ (bleibt): marke-tanzt und marke-grenzfaelle brechen bei Abweichung zwischen serviertem aktuell.* und gestellter Lage mit 'Messung nicht moeglich' ab (Ende 2, ueber ein Flag statt exit im try, damit das finally die Lage zurueckLEGT) - sie fangen die alte Vorschau, die den Schnappschuss noch nicht kennt. Schnellheilung von Hand bleibt: die Dauer-Vorschau neu starten"
    tags: [vorschau, leihgabe, werkzeug, darstellung, phantom-rot, 2026]
    related: [ui-ruft-die-spielfunktion]
    body: |
      ══ WORAN MAN SIE ERKENNT ═══════════════════════════════════════════════
      Ein Werkzeug ist einzeln gruen und in der grossen Probe rot - oder
      andersherum -, und der Unterschied ist NICHT der Code, sondern welcher
      Lauf der geliehenen Vorschau zuvor etwas geschrieben hat. Dieselbe
      Sorte Befund wie [[vorschau-wird-geliehen]]: der Zustand der
      Messumgebung wandert zwischen Laeufen, und die falsche Ursache steht
      dann taeuschend plausibel im Befundtext des Werkzeugs.

      ══ WAS SEIT DEM WURZELFIX NOCH UEBRIG IST ══════════════════════════════
      Der Schnappschuss heilt den Fall nur, wenn die LAUFENDE Vorschau ihn
      schon kennt. Eine Dauer-Vorschau, die vor dem 31.08.2026 gestartet
      wurde, gibt bei /vorschau/lage kein Feld `darstellung` zurueck - dann
      legt das beste zurueckgeben() nichts zurueck, was es nie bekommen hat,
      und der alte Fehler steht wieder. Das ist derselbe Riss wie in
      [[attrappe-luegt-durch-weglassen]]: die fehlende Antwort sieht aus wie
      eine leere, nicht wie eine alte. Deshalb bleiben die zwei
      Abbruch-Wachen stehen, und deshalb gilt nach jedem Umbau an
      neu-vorschau.mjs weiter: die Dauer-Vorschau NEU STARTEN.
  - id: kennung-wunsch-fuer-katalog-werke
    kind: architektur
    title: "E95/V Stufe 3: der Kennung-Wunsch spielt Katalog-Alben ohne Bibliothekseintrag - und der letzte /player-Bauer der UI ist Geschichte"
    match: "interpretenseite abspielen|kennung wunsch|katalog album spielen|kuenstliches werk|albumGanzSpielen|player bauer ui|stufe 3 spielfunktion|grosser rueckbau app.js"
    sev: high
    cause: "Die Interpretenseite spielt auch Werke, die nicht in der Bibliothek stehen (Alben/Singles/Sammlungen/Top-Titel direkt aus dem Spotify-Katalog) - ein Schluessel-Wunsch endete fuer sie in 404, und bis Stufe 3 baute die Seite deshalb ihre spotify/now-Befehle selbst. Seit 31.08.2026 nimmt POST /api/spielen alternativ {kennung:{dienst:'spotify',art:'album'|'playlist'|'show',id}, titelNr?, positionMs?}: der Server baut ein Kunstwerk, laesst es durch DENSELBEN spielVersuch laufen (Kinderzeit zweimal, Anbieter-Schalter, Anhalten, Ereignis, Abloese) und antwortet gleichfoermig. AUSDRUECKLICH OHNE Bibliotheks-Zaun - die Seite ist das Katalogfenster, der Alt-Weg spielte diese Kacheln ebenso ungeprueft; fuer Schluessel-Wuensche gilt der Zaun unveraendert"
    fix: "In der UI ging damit ein SEMANTIK-Fehler gleich mit: das Box-eigene Spotify-Album baute seinen Befehl aus der KENNUNG und lief an der Verschmelzung vorbei (ein verschmolzenes Album haette weiter Spotify gespielt, waehrend das Raster laengst den Mitschnitt bevorzugt). Seit Stufe 3 geht JEDES Box-Werk per Schluessel, NUR eintragslose Katalog-Alben per Kennung (albumGanzSpielen). Der grosse Rueckbau danach: abspielBefehl, albumSpielen, ardSendungAb, starte, titelUndStelle, nachsehenBis, angekommen, spotifyBereit und die startLauf-Maschinerie sind GELOESCHT (~700 Zeilen) - die UI-Spiellogik sank von 552 auf 39 Zeilen, die Dienstnamen-Ratsche von 558 auf 465. Wachen: interpretseite-befehle prueft jetzt Wunsch-Rumpfe (Schluessel- UND Kennung-Form, titelNr 1-basiert unveraendert), spielen.integration.spec.ts deckt den Kennung-Weg (Startbefehl mit Nummer, Anbieter-403, 400 fuer Unbekanntes)"
    tags: [architektur, e95, spielfunktion, interpretenseite, rueckbau, 2026]
    related: [ui-ruft-die-spielfunktion, post-api-spielen-ist-die-spielfunktion, die-ui-spielt-das-backend-entscheidet]
    body: |
      ══ WAS VON DER UI-SPIELLOGIK UEBRIG IST ════════════════════════════════
      spielenAnfordern (die eine Tuer, mit Kennung-Zweig im Body-Bau),
      spielenFehlersatz (Ausgaenge -> Kindersaetze), die drei Huellen
      (spielen, weiterSpielen, albumSpielenAusLane), albumGanzSpielen
      (Schluessel-oder-Kennung-Weiche) und anhaltenUndAbstand fuer den
      Profilwechsel. spielerBefehl bleibt fuer die MASCHINEN-Bedienung
      (play/pause/setvolume/seekpos des Mini-Players) - das ist Steuerung
      des Laufenden, kein Start, und gehoert nicht in die Spielfunktion.

      ══ WER EINEN NEUEN DIENST ANSCHLIESST ══════════════════════════════════
      schreibt ein Plugin (E87) und Regeln im Backend - die UI-Seite dieses
      Vertrags ist seit Stufe 3 vollstaendig: kein Befehlsbau, kein
      Dienstwissen im Spielweg, nur generische Wuensche und Darstellung.
  - id: wellen-regler-wirken-ueber-zwei-naehte
    kind: howto
    title: "Hoehe und Tempo der Wellen sind Eltern-Regler - die Hoehe ist eine CSS-Variable, das Tempo eine Pufferlaenge"
    match: "wellen hoehe einstellen|wellen tempo|wellenHub|wellenTempo|frequenzfeld groesse|wellen regler|band schneller langsamer"
    sev: medium
    cause: "Betreiber beim Bau der Wellen (30.08.2026): 'vielleicht einstell parameter wie hoch und wie schnell'. Gebaut am 31.08.2026 als zwei Weiterschalt-Regler (Darstellung -> Mini-Player, je drei Stufen, Stufe 2 = exakt der Stand davor - wer nie tippt, sieht keine Aenderung). Die zwei Wirkwege sind bewusst verschieden: wellenHub stellt --mupi-wellen-hoehe (40/56/78 px) und der Hub des Zeichners folgt der Leinwand von selbst (hub = mitte - 3); wellenTempo stellt die PUFFERLAENGE des Foerderbands (90/60/40 Punkte) - weniger Punkte auf derselben Breite heisst groesserer Schritt je Datentakt, also schnelleres Band. Beim Verkleinern schrumpft der Ringpuffer ueber seine shift-Grenze von selbst, beim Vergroessern waechst er mit den naechsten Werten nach - nichts wird getrimmt"
    fix: "Wache: tools/wellen-regler-schau.mjs (im Pruefen-Laeufer) stellt je Stufe per PUT /api/darstellung und misst BEIDE Wirkungen - die Hoehe an der CSS-Variable, das Tempo ueber den Messzugang window.mupiWellenMessung.puffer() (die Pufferlaenge steckt im Schritt der Zeichnung, nicht im DOM). Dazu die Rueckfall-Messung: ohne Regler-Wert gilt Stufe 2. Am Geraet unter Realbedingungen gemessen (31.08.2026, echte Vormittags-Wiedergabe): WPE gesamt 36,4 % CPU ueber 60 s BEI spielender Musik samt Wellen (E100-Referenz: 92 % vor dem Transition-Fix, 28 % ruhend), /pegelstrom liefert live echte Pegel (0.072/0.044/0.031/0.017 - Bass vorn)"
    tags: [wellen, e99, darstellung, regler, vorschau, 2026]
    related: [ui-ruft-die-spielfunktion, geschriebene-darstellung-macht-lage-schalter-taub]
    body: |
      ══ WO ALLES WOHNT ═══════════════════════════════════════════════════════
      app.js: WELLEN_HOEHE_JE_STUFE / WELLEN_PUFFER_JE_TEMPO / wellenStufe()
      (kaputte Werte werden zur Vorgabe 2, stellen nie etwas um), Anwendung
      in anwenden(); die zwei Felder in der Optik-Feldliste hinter
      miniPlayer. app.css: height: var(--mupi-wellen-hoehe, 56px) am
      .wellen-feld. Attrappe: DARSTELLUNG.aktuell in tools/neu-vorschau.mjs.

      ══ BEKANNTE GRENZE DES SSE-NACHWEISES ══════════════════════════════════
      Das 60-s-Bytes-Delta auf den 5005-Verbindungen (574 KB bei laufender
      Musik) beweist LEBENDIGEN Player-Verkehr, trennt aber Pegelstrom nicht
      von Poll-JSON - die Kiosk-Seite laesst sich nicht instrumentieren
      (eltern-bereich-fernsteuern-geht-nicht gilt sinngemaess). Der Strom
      selbst ist an der Route bewiesen; die UI-seitige Poll-Daempfung bleibt
      Code-gedeckt statt geraetegemessen.
  - id: schrittzahl-wache-hing-nur-im-doku-lauf
    title: "Die Schrittzahl der Leitplanke lief viermal davon - die Wache stand im Doku-Lauf, nicht im Laeufer, den sie zaehlt"
    kind: architektur
    match: "FEHLT in mixpibox.md.*Schritte|leitplanken-zahl-pruefen|Schrittzahl der Leitplanke|Schritte laufen immer"
    sev: mid
    tags: [wachen, doku, leitplanke, pruefen-sh, abgeleitete-zahl, doku-lauf, 2026]
    stand: "31.08.2026 gefunden und an der Naht behoben; leitplanken-zahl-pruefen.py haengt jetzt zusaetzlich als eigener Schritt in tools/pruefen.sh"
    source: "Stuendlicher Doku-Lauf; tools/doku-luecken-probe.sh meldete 1 Luecke — dieselbe Sorte wie in vier Laeufen davor"
    related: ["kopfzeilenzahl-driftet-weil-die-wache-vor-dem-anhaengen-laeuft", "zahlenwache-kannte-nur-eine-datei", "wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm", "gegenprobe-statt-gruen-glauben"]
    body: |
      DIESELBE LEHRE WIE BEI DER KOPFZEILE, AN EINER ZWEITEN STELLE: Wer die
      Zahl pflegt, ist nicht der, der sie kaputt macht — solange die Wache im
      falschen Laeufer haengt.

      ══ DER BEFUND ═════════════════════════════════════════════════════════

      Die Doku-Luecken-Probe meldete genau eine Luecke:

        FEHLT in mixpibox.md: 96 (Schritte, die immer laufen)
        FEHLT in mixpibox.md: 102 (Schritte insgesamt)
        gemessen: 96 unbedingt + 6 am Geraet = 102

      Ursache im Commit davor (7ef3548c, E99-Feinschliff): Der Bau haengte die
      Dauerwache `schritt "Wellen-Regler wirken"` in `tools/pruefen.sh` und
      fasste `dokumentation/mixpibox.md` sogar an — aber nur die Paket-Kopfzeile,
      nicht Abschnitt 7.1 zwei Bildschirme weiter unten.

      ══ WARUM DAS KEIN VERGESSENER HANDGRIFF IST ═══════════════════════════

      `git log -S` auf die Zeile zeigt die Reihe: 17 (drei Wochen lang, um 62
      daneben) -> 79 -> 94 -> 95 -> 96. JEDE Korrektur kam von einem Doku-Lauf,
      KEINE vom Bauenden. Die Wache `tools/leitplanken-zahl-pruefen.py` gibt es
      seit dem 24.08.2026 und sie ist nicht kaputt — sie hing nur in
      `tools/doku-luecken-probe.sh`:

        Bau     : `schritt`-Zeile dazu, Commit  -> Doku ist ab jetzt falsch
        Doku-Lauf (Stunden bis Tage spaeter)    -> rot, wird nachgetragen

      Zwischen beiden Punkten liegt ein ausgelieferter Stand, in dem die Doku
      ueber dem Aufrufblock eine Zahl behauptet, die der Leser beim Zusehen
      widerlegt bekommt. Genau das ist die Sorte Fehler, bei der man eher dem
      eigenen Aufruf misstraut als der Doku.

      ══ DIE NAHT, AN DER ES HAENGEN MUSS ═══════════════════════════════════

      Die Zahl beschreibt `tools/pruefen.sh`. Also gehoert die Wache IN
      `tools/pruefen.sh` — dorthin, wo derjenige vorbeikommt, der sie davonlaufen
      laesst, und zwar VOR dem Ausliefern:

        schritt "Schrittzahl der Leitplanke" python3 tools/leitplanken-zahl-pruefen.py

      Der huebsche Nebeneffekt: Der neue Schritt zaehlt sich selbst mit. Das
      Hinzufuegen machte die Wache im selben Moment rot (96 -> 97), was der beste
      Beweis ist, dass sie an der richtigen Stelle steht. Sie bleibt zusaetzlich
      in der Doku-Luecken-Probe — zwei Laeufer, ein Urteil, kein Widerspruch.

      ══ GEGENPROBE ═════════════════════════════════════════════════════════

      Schritt wieder herausgenommen -> rot (meldet 96/102), zurueck -> gruen
      (97 unbedingt + 6 am Geraet = 103). Doku-Luecken-Probe danach vollstaendig
      gruen, `ungerufene-wachen.py` unveraendert ohne Befund.

      ══ DIE ALLGEMEINE REGEL ═══════════════════════════════════════════════

      Bei einer abgeleiteten Zahl entscheidet nicht, OB eine Wache sie prueft,
      sondern WELCHER LAEUFER sie ruft. Die Wache gehoert in den Lauf, den der
      Verursacher ohnehin macht. Steht sie nur im nachgelagerten Aufraeum-Lauf,
      ist sie ein Protokoll des Drifts, keine Bremse dagegen. Erkennungszeichen
      im Alltag: derselbe Befund taucht ueber mehrere Laeufe immer wieder auf.
  - kind: howto
    id: karte-in-anderes-geraet-umsetzen
    title: "Karte aus Box A in Box B — was VOR dem Einschalten zu pruefen ist"
    source: "am 31.08.2026 an Achims Karte gemessen (Pi-5-Klon vom 08.08. -> Pi 4), rein lesend im Kartenleser"
    tags: [karte, pi4, pi5, display, dsi, umzug, namen, werkzeug]
    body: |
      Eine Karte, die auf einem Geraet grossgezogen wurde, in ein anderes zu
      stecken sieht aus wie „einstecken und gut". Der teure Teil ist die
      REIHENFOLGE: zeigt das andere Geraet danach nichts, hat man kein Bild,
      keine Tastatur und kein SSH, um nachzusehen — man muss die Karte wieder
      herausnehmen. Deshalb im Leser pruefen, solange jede Datei lesbar ist:

          tools/karte-fuer-anderes-geraet.py --ziel pi4 --netz

      Es schreibt NICHTS auf die Karte und nennt die Befunde nach Gewicht
      (HART = kommt nicht hoch, WICHTIG = laeuft, ist aber still falsch).

      DIE ZWEI, DIE WIRKLICH TREFFEN:

      1. DSI-ANSCHLUSSZAHL. Der Pi 5 hat ZWEI DSI-Anschluesse, der Pi 4 hat
         EINEN. Ein `,dsi1`-Zusatz, der auf dem Pi 5 richtig ist, zeigt auf dem
         Pi 4 auf einen Anschluss, den es nicht gibt -> schwarz. Umgekehrt
         nimmt der Overlay ohne Zusatz auf dem Pi 5 den Vorgabe-Anschluss, was
         nur stimmt, wenn das Flachband auch dort steckt. Siehe
         [[mupi-waveshare-dsi-config]] und [[achim-display]].

      2. NAMENSGLEICHSTAND — der gefaehrlichere, weil er sich nicht meldet.
         Ein Klon traegt denselben Rechnernamen UND denselben
         `mupibox.host` (Spotify-Geraetename). Laufen beide Geraete
         gleichzeitig, meldet sich der Zweite unter demselben mDNS-Namen, und
         die Spotify-Auswahl laeuft ueber den NAMEN: der Ton landet im
         falschen Raum. Das ist derselbe harte Ausfall, den ANGLEICH-PI4-PI5.md
         schon fuer den alten Pi 4 beschrieb („Ein Fehler, der sich nicht als
         Fehler meldet"). Vor dem ersten Einschalten umbenennen — beide Namen,
         nicht nur einen.

         BEHOBEN MIT: `sudo python3 tools/karte-umbenennen.py --neu mixpibox2`
         Der Name steht an DREI Stellen: `/etc/hostname`, die `127.0.1.1`-Zeile
         in `/etc/hosts` und `.mupibox.host` in der mupiboxconfig
         (`/etc/librespot/env-librespot` liest den Spotify-Namen von dort — es
         gibt keine vierte DATEI).

         NACHTRAG 31.08.2026: „keinen vierten Ort" galt nur fuer die Karte IM
         LESER, und so uneingeschraenkt war der Satz falsch. Am laufenden
         System — `--wurzel /` ueber SSH, der bequemste Weg, und genau so
         heute gebraucht — sind drei Dateien NICHT genug: der Kernel haelt
         seinen Namen selbst (`hostnamectl set-hostname`), und librespot liest
         `mupibox.host` NUR beim Start (`systemctl restart librespot`). Ohne
         diese zwei Schritte heisst die Box in der Spotify-Geraeteliste weiter
         wie vorher — also genau der Gleichstand, gegen den man umbenannt hat.
         Das Werkzeug nennt seit heute je nach `--wurzel` die passenden
         Schlusssaetze; vorher riet es auch am laufenden System dazu, `/`
         auszuhaengen. Eine Datei-Aufzaehlung ist eben keine
         Schritt-Aufzaehlung.

         Das Werkzeug fasst ALLE DREI Dateien an oder KEINE:
         wer nur `/etc/hostname` aendert und `/etc/hosts` vergisst, hat ein
         System, in dem der eigene Name nicht mehr aufloest, und `sudo` laeuft
         danach bei jedem Aufruf erst in eine Zeitueberschreitung. Die drei
         Dateien gehoeren ausserdem verschiedenen Eigentuemern (zwei root, die
         Konfiguration auf Achims Karte `achim`) — ein Lauf ohne die noetigen
         Rechte schafft sonst genau die Haelfte.

      WAS SICH ALS HARMLOS ERWIES (am 31.08. gemessen, nicht geraten):
      * `/etc/X11/xorg.conf.d/02-dietpi-rpi5.conf` — die Pi-5-Beigabe fuer den
        PRIME-Offload. Am Pi 4 haengt DSI an derselben DRM-Karte, die Datei ist
        dort Ballast, kein Ausfall.
      * `usr/local/bin/mupibox/force-dsi-output.sh` — sucht den DSI-Ausgang
        DYNAMISCH (`awk '/ connected/ && /DSI/'`), nicht als festen `DSI-2`.
        Es laeuft am Pi 4 (dort `DSI-1`) unveraendert. Wer solche Skripte
        schreibt: genau so, nie auf den Connector-Namen festnageln.
      * fehlendes `/usr/bin/cog` — `chromium-autostart.sh` faellt selbst auf
        Chromium zurueck (Zeile 239). Kein Handgriff noetig.

      BELEGT AM GERAET (31.08.2026, Pi 4 Model B Rev 1.4, frisches DietPi
      10.6.2 / Trixie): Das FREENOVE 5" DSI (800x480, 5-Punkt, „driver-free")
      meldet sich wie das offizielle Panel. `dtoverlay=vc4-kms-dsi-7inch` OHNE
      DSI-Zusatz ist richtig; Bild, Touch und Helligkeit kommen von selbst:

          fb0                800,480
          Connector          card1-DSI-1 = connected   (HDMI beide getrennt)
          Touch              10-0038 generic ft5x06 (00)
          Hintergrundlicht   10-0045, bl_power 0, 255/255

      DIE ZAHLEN WANDERN ZWISCHEN DEN BRETTERN, die Kennungen nicht:

                        Pi 5 (laufende Box)   Pi 4
          Connector     DSI-2                 DSI-1
          I2C-Bus       11-0038 / 11-0045     10-0038 / 10-0045

      Wer einen Connector-Namen oder eine Busnummer fest eintraegt, baut damit
      etwas, das auf genau einem Brett laeuft. `force-dsi-output.sh` sucht den
      DSI-Ausgang deshalb ueber `awk '/ connected/ && /DSI/'` und trifft beide.

      Die Freenove-Anleitung nennt uebrigens KEINE config.txt-Zeile, sie
      beschreibt nur die Montage — die Overlay-Zeile kommt hier aus der
      Boot-Vorbereitung des Karten-Installers.
  - kind: falle
    id: rezeptschleife-nennt-acht-von-siebzehn
    title: "Die Skript-Sammelkopie der Rezepte nennt 8 von 16 Verzeichnissen"
    source: "am 31.08.2026 gegen die laufende Box gemessen (tools/rezepte-gegen-box.py); Zahlen am selben Tag statisch am Baum nachgezaehlt (Doku-Lauf)"
    tags: [installer, rezepte, ausrollweg, scripts, pi4, neuaufbau]
    body: |
      `remote-step-installer/recipes/mupibox-app.yaml` legt die Skripte NICHT
      einzeln hin, sondern in einer Sammelkopie:

          for d in mupibox bluetooth wled telegram mupihat fan wifi mqtt; do
            [ -d "mupi-x/scripts/$d" ] && cp -a mupi-x/scripts/$d/* /usr/local/bin/mupibox/
          done

      Das ist gut fuer die Pflege — neue Dateien in einem GENANNTEN Verzeichnis
      kommen ohne Rezeptaenderung an. Es ist aber still fuer alles andere:
      `scripts/` hat 16 Verzeichnisse, die Schleife nennt 8. Nicht darin sind
      OnOffShim, box, hwdetect, librespot, mixpi, online, soloist, systemd.

      ZUR ZAHL 16: die Kennung dieses Eintrags und die erste Fassung sagten 17.
      Am 31.08.2026 nachgezaehlt (`find scripts -maxdepth 1 -type d`, und
      gegengeprueft mit `git ls-files`) sind es 16; die Kennung traegt die alte
      Zahl weiter, damit Verweise halten. Die acht ungenannten stimmen.

      DIE SCHLEIFE IST NICHT DIE GANZE WAHRHEIT — sonst meldet man zu viel.
      Einzelne Dateien aus genau diesen acht Verzeichnissen liegen weiter unten
      im SELBEN Rezept namentlich als `put:` (`box/mupibox-standwache.py`,
      `librespot/librespot-waechter.sh`, `soloist/soloist-start.sh`, mehrere
      `systemd/*.service`, `mixpi/mixpi-plugins-nachziehen.sh`). Wer „acht
      Verzeichnisse fehlen" als „alles darin fehlt" liest, hat einen zu grossen
      Befund.

      DATEIWEISE GEZAEHLT (statisch am Baum, 31.08.2026) sind **21 Dateien an
      KEINER Stelle der Rezepte genannt** — weder in der Schleife noch einzeln:

          box       4 von 6   ard-vorspann-messen.py, bootwache.py,
                              bt-reconnect.py, touch-bridge.py
          hwdetect  2 von 2   hat-apply.sh, hwdetect.sh
          librespot 2 von 4   librespot-start.sh, README.md
          mixpi     1 von 2   mixpi-wlan-adapter.sh
          online    1 von 1   install_rtl88x2bu.sh
          OnOffShim 3 von 3   off_trigger.sh, poweroff.sh, taster_wache.py
          soloist   1 von 3   soloist-anmeldewache.sh
          systemd   5 von 9   einrichten.sh, mixpi-wlan-adapter.service,
                              netzabriss-sonde.service,
                              soloist-anmeldewache.service + .timer

      WAS DAS KOSTET (Stand 31.08.2026, gegen die laufende Box gemessen): 27
      Einrichtungen der Box kommen ueber KEIN Rezept, darunter der
      WLAN-Adapter-Dienst (`scripts/mixpi/`), die Soloist-Anmeldewache
      (`scripts/soloist/`) und die Taster-Bedienung (`scripts/OnOffShim/`).
      Die 27 und die 21 sind ZWEI GRUNDMENGEN, keine widerspruechlichen Zahlen:
      die 27 zaehlt Einrichtungen der laufenden Box (auch solche, die gar nicht
      aus `scripts/` stammen), die 21 zaehlt Dateien im Baum.
      Ein Installer-Lauf meldet trotzdem Erfolg — er weiss nicht, dass es sie
      gibt. Genau deshalb faellt so etwas erst am Geraet auf, und dort am
      teuersten.

      WARUM KEINE WACHE ANSCHLAEGT: der Schritt `extras` endet mit einer
      Zaehlzeile (`ls /usr/local/bin/mupibox/ | wc -l`) und einer `check:`, die
      auf ZWEI Dateien testet. Beides ist gruen, solange die acht genannten
      Verzeichnisse Inhalt haben. Es gibt keinen Zustand, in dem der Installer
      „unvollstaendig" sagt — die Box laeuft, sie kann nur weniger.

      BESONDERS TUECKISCH ist `scripts/mixpi/`: nach der Regel
      [[benennung-mixpibox-drei-toepfe]] traegt alles NEUE diesen Namen. Das Verzeichnis, in
      dem die neuen Sachen landen, ist also ausgerechnet das, das kein
      Sammelweg mitnimmt.

      PRUEFEN mit `tools/rezepte-gegen-box.py --box dietpi@<adresse>`. Es loest
      die Sammelwege auf, statt Dateinamen im Rezepttext zu suchen — die erste
      Fassung tat das und meldete 80 Befunde, von denen 71 falsch waren, weil in
      einer Sammelkopie kein einziger Dateiname steht. Eine dauerrote Wache
      verdeckt den naechsten echten Fund.

      NICHT VERWECHSELN mit den fuenf harten Ausfaellen aus ANGLEICH-PI4-PI5.md
      (30.07.2026): librespot, mpv, PipeWire, Kiosk-Benutzer, zram. Die sind
      inzwischen ALLE repariert — die Rezepte tragen eigene Schritte dafuer, und
      die stillen Wachposten (`MUPI_AUDIO=pipewire`, das nie gesetzt wurde) sind
      heraus. Wer nur diese fuenf nachprueft, haelt die Rezepte fuer aktuell.

  - id: bedienschalter-hat-keine-doku-gegenrichtung
    kind: falle
    sev: medium
    title: "Ein neuer Bedien-Schalter kommt durch jede Doku-Wache — die Gegenrichtung CODE → DOKU gibt es fuer diese Sorte nicht"
    match: "schalter undokumentiert|darstellung.json feld doku|neuer schalter handbuch|statusWellen|wellen im player|vollbildBlaettern|bedienschalter doku|betreiber findet schalter nicht"
    cause: >
      `tools/doku-luecken-probe.sh` haelt ueber drei Dutzend Wachen, aber keine
      einzige fragt: gibt es zu einem BEDIEN-SCHALTER der Verwaltung einen Satz
      im Handbuch? Die naechstliegende Wache traegt den passenden Namen und
      meint etwas anderes: `tools/zitierte-schalter-pruefen.py` prueft
      KOMMANDOZEILEN-Schalter (`--schnell`) und laeuft in die andere Richtung
      (DOKU → CODE, tippt die Doku einen Schalter ab, den es nicht mehr gibt).
      Ein Feld in `darstellung.json` faellt durch beide Raster.
    fix: >
      Wer einen Schalter der Verwaltung baut, schreibt ihn im selben Zug ins
      BETRIEBSHANDBUCH (`dokumentation/benutzerhandbuch.html`) — nicht nur ins
      Architekturhandbuch. Die Probe wird gruen melden; sie sieht diese Sorte
      nicht. Wer eine Wache dafuer baut, misst VORHER die Grundmenge: eine
      Wache ueber alle 60 Felder meldete heute 43 Fundstellen und waere ab dem
      ersten Lauf dauerrot.
    tags: ['doku', 'wachen', 'darstellung', 'falle', 2026]
    source: "30.08.2026, am Baum gemessen: interface Darstellung in src/frontend-admin/src/app/seiten/darstellung.ts (60 Felder) gegen dokumentation/mixpibox.md, dokumentation/benutzerhandbuch.html, plugins/README.md und BACKLOG.md. Aufgefallen am Wellen-Bau (3ed8bdae, c00d2bbd)."
    body: |
      GEFUNDEN AN EINEM STILLEN LAUF. Beide Proben meldeten sauber, und
      trotzdem war die Funktion, die am Vortag gebaut worden war, in KEINEM
      der drei Handbuecher erwaehnt: „Wellen im Player" (`statusWellen`,
      Vorgabe aus) — ein sichtbarer Schalter, den ein Betreiber sucht, sobald
      sich auf seinem Schirm etwas bewegt, das vorher stillstand.

      DIE ZAHL, DIE DAHINTER LAG (30.08.2026, je Feldname gegriffen):

        Felder in `interface Darstellung`                60
        davon unter ihrem Feldnamen nirgends genannt     43

      Die 43 sind eine OBERGRENZE des Schadens, kein Deckungsgrad: gemessen
      wurde der interne Feldname, und ein Betriebshandbuch nennt zu Recht die
      Beschriftung („Wellen im Player"), nicht `statusWellen`. Die
      Stichprobe haelt trotzdem — auch die Beschriftungen fehlten, und die
      Vorlage, nach der der Schalter gebaut wurde (`vollbildBlaettern`), steht
      in KEINEM der drei Handbuecher.

      (KORRIGIERT 31.08.2026, ueberholt: hier stand „3x im Architekturhandbuch
      und 0x im Betriebshandbuch". Das ist falsch und war es schon beim
      Schreiben. `git log -S'vollbildBlaettern' -- dokumentation/mixpibox.md`
      meldet NULL Commits — der Name hat diese Datei nie beruehrt. Baumweit
      liegt er in BACKLOG.md (1), NewDesign/app.js (1), darstellung.ts (6),
      tools/blaettern-schau.mjs (7) und in diesem Pack selbst; wer im ganzen
      Baum zaehlt statt je Datei, kommt auf eine Handbuchzahl, die es nicht
      gibt. Der Befund wird dadurch GROESSER, nicht kleiner — nicht einmal das
      Entwicklerhandbuch nennt die Vorlage. Aber wer die alte Zahl las, suchte
      in `mixpibox.md` nach drei Stellen, die dort nie standen.)

      WARUM AUSGERECHNET DAS BETRIEBSHANDBUCH LEER BLEIBT: es liegt nicht im
      Entwicklerpfad. Wer einen Schalter baut, hat `mixpibox.md` offen, weil
      dort die Architektur steht, die er gerade anfasst. Die Seite, auf der
      ein Elternteil nachschlaegt, macht er nicht auf.

      DER NAME DER NACHBARWACHE IST DIE FALLE. Auf die Frage „ist ein
      Schalter bewacht?" antwortet `zitierte-schalter-pruefen.py` mit ja —
      sie heisst so. Sie meint `--zuruecknehmen` an einem Skript, nicht einen
      Knopf in der Verwaltung, und sie laeuft in die Gegenrichtung. Zwei
      verschiedene Sorten unter einem Wort.

      WAS EINE WACHE HIER BRAUCHT, falls sie einmal gebaut wird: die Liste
      darf nicht aus derselben Quelle stammen wie ihr Gegenstand.
      `src/frontend-admin/src/app/such-bestand.ts` waere die richtige — sie
      fuehrt die Beschriftungen so, wie der Betreiber sie in der Suche
      eintippt, und wird beim Bau eines Schalters ohnehin nachgezogen. Nicht
      alle Eintraege gehoeren geprueft (`Name`, `Farben` sind keine
      Funktionen); der Zuschnitt ist die eigentliche Arbeit, und ohne ihn
      wird die Wache dauerrot und damit ignoriert. Wie viele es sind, wird
      NICHT hier festgehalten — `node tools/verwaltung-suchbestand.mjs`
      (ohne `--schreiben`) sagt es und ist die einzige Stelle, die nicht
      veraltet (am 31.08.2026: 322 Eintraege aus 20 Seiten; im Pack stand an
      anderer Stelle noch „190 aus 17, Stand 04.08.2026").

      ══ NACHTRAG 31.08.2026 — WARUM DIESER EINTRAG ERST JETZT HIER STEHT ═══

      Er wurde am 30.08.2026 geschrieben und blieb im QUELL-Pack liegen
      (`~/Downloads/llmwiki_mupibox/pack.yaml`); `tools/pack-abgleich.py`
      fuehrte ihn seither als einzigen Posten unter „NUR in der Quelle —
      kommen beim Leser nicht an". Das ist die Lehre ueber der Lehre: ein
      Doku-Befund, der nur im Werkstatt-Pack steht, ist fuer jeden, der das
      ausgelieferte Pack liest, nicht passiert. Die Abgleich-Liste am Anfang
      des Laufs mitlesen, nicht nur die beiden Proben.

      STAND HEUTE, nachgemessen statt uebernommen:

        Felder in `interface Darstellung`                61  (waren 60)
        davon unter ihrem Feldnamen nirgends genannt     41  (waren 43)
        Wachen in tools/doku-luecken-probe.sh            52
        davon auf Bedien-Schalter der Verwaltung          0

      DER AUSLOESER IST ERLEDIGT, DIE LUECKE NICHT. „Wellen im Player" steht
      inzwischen 2x im Betriebshandbuch und `statusWellen` 1x im
      Architekturhandbuch — der konkrete Schalter ist dokumentiert. Die
      Sorte ist es nicht: keine der 52 Wachen wuerde den naechsten melden.
      Die tiefste gebaute Schicht bleibt `tools/admin-abschnitte-deckung.py`
      (Oberflaeche → Seite → ABSCHNITT); der SCHALTER liegt eine Schicht
      darunter und ist unbewacht. Und die Abschnitts-Wache haengt selbst in
      keinem Laeufer (`grep admin-abschnitte tools/doku-luecken-probe.sh`
      ist leer, absichtlich: 70 Abschnitte, 1 sicher genannt, 44 ungenannt,
      25 unentscheidbar — heute unveraendert gegenueber dem 30.08.). Wer
      „ist das bewacht?" fragt, bekommt hier zweimal nein hintereinander,
      siehe [[wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm]] und
      [[dauerrote-wache-ist-keine]].
    related: ["bedienung-wohnt-in-der-darstellung", "darstellung-feld-faellt-still-heraus"]
  - kind: falle
    id: rpi4-heisst-bei-dietpi-rpi234
    title: 'Kein Abbild fuer RPi4 gefunden — DietPi kennt kein RPi4'
    match: "Kein Abbild für RPi4 gefunden|Kein Abbild für .* gefunden"
    source: "am 31.08.2026 vom Betreiber beim ersten echten Pi-4-Versuch mit ./sdstart erlebt"
    tags: [installer, sdstart, sdgui, sdprep, abbild, pi4, dietpi]
    symptom: >-
      Der Karten-Installer bricht nach der Board-Wahl „Raspberry Pi 4" ab und
      meldet „Kein Abbild für RPi4 gefunden." Mit „Raspberry Pi 5" laeuft
      derselbe Weg durch.
    body: |
      DietPi liefert EIN 64-Bit-Abbild fuer Pi 2, 3, 4 und Zero 2 und nennt es
      `RPi234`. Auf dietpi.com gibt es die Boards RPi1, RPi2, RPi234, RPi5 —
      ein `RPi4` existiert dort NICHT.

      Die Oberflaechen boten es trotzdem an:
        controller/sdstart.py  Board-Seite mit den Kennungen 'RPi5' und 'RPi4'
        controller/sdgui.py    BOARDS = ["RPi5", "RPi4", "RPi3", "RPi2", "RPi1"]
      und `sdprep.select()` vergleicht auf GLEICHHEIT
      (`e["board"].lower() == board.lower()`). Die Auswahl traf also nie etwas.

      WARUM ES EIN JAHR UNBEMERKT BLIEB: der Pi-5-Weg stimmt zufaellig, weil
      DietPi das Board dort wirklich `RPi5` nennt. Alles, was je gefahren wurde,
      war ein Pi 5 — ANGLEICH-PI4-PI5.md haelt seit dem 30.07.2026
      ausdruecklich fest, dass der Probelauf gegen den Pi 4 aussteht. Eine
      Kennung, die nur auf einem Geraet stimmt, sieht auf diesem Geraet wie eine
      richtige aus.

      Auch `tests/sdprep_test.py` haette es fangen koennen und tat es nicht: die
      Auswahl wurde ausschliesslich mit „RPi5" geprueft. Eine Pruefung, die nur
      den Normalfall kennt, misst die Schreibweise des Testautors, nicht die
      Gegenstelle.

      BEHOBEN (31.08.2026) mit `sdprep.BOARD_ALIAS` (RPi4/RPi3 -> RPi234) und
      `sdprep.board_aufloesen()`, das den Alias AUFLOEST UND EINEN HINWEIS
      ZURUECKGIBT — die Oberflaeche zeigt ihn an, statt still etwas anderes zu
      nehmen. Der Alias steht in sdprep und nicht in den drei Oberflaechen, weil
      sdgui, sdtui und sdstart alle durch `select()` gehen. Gegenprobe gefahren:
      erfundene Boards (`RPi9`) bleiben leer — der Alias ist keine Maschine, die
      immer irgendetwas findet.
  - kind: falle
    id: gegenprobe-scheitert-am-bytecode-zwischenspeicher
    title: 'Gegenprobe bleibt rot, obwohl die Datei zurueckliegt — Pythons .pyc'
    source: "am 31.08.2026 bei der Gegenprobe zu sdprep.BOARD_ALIAS erlebt"
    tags: [gegenprobe, python, pycache, methodik, test]
    symptom: >-
      Nach einer Sabotage-Gegenprobe wird die Datei zurueckgelegt, der Test
      bleibt aber rot. Ein grep zeigt, dass der richtige Inhalt wieder dasteht.
    body: |
      Python haelt Bytecode in `__pycache__/*.pyc` und prueft die Gueltigkeit
      ueber MTIME UND GROESSE der Quelldatei — nicht ueber den Inhalt. Eine
      Sabotage, die genau so viele Zeichen ersetzt, wie sie entfernt
      (`"RPi4"` -> `"RPiX"`), laesst die Groesse gleich. Wird die Sicherung in
      DERSELBEN SEKUNDE zurueckkopiert, ist auch die Mtime gleich — und Python
      fuehrt weiter den sabotierten Bytecode aus.

      AM GERAET BELEGT (31.08.2026): sdprep.py und sdprep.cpython-314.pyc
      trugen beide die Mtime 1788182902. Der Test meldete 189/191, obwohl
      `grep` den richtigen Inhalt fand. Nach `rm -rf __pycache__`: 191/191.

      DAS IST DIE GEFAEHRLICHE RICHTUNG. Die Gegenprobe soll BEWEISEN, dass
      eine Wache ausloest — hier haette sie beim Zurueckdrehen einen Fehler
      vorgetaeuscht, der keiner ist. Umgekehrt kann derselbe Mechanismus ein
      GRUEN vortaeuschen: wer erst die Sabotage einbaut und sofort testet,
      misst unter Umstaenden noch den heilen Bytecode und haelt die Wache fuer
      blind.

      REGEL: Bei jeder Sabotage-Gegenprobe an Python-Code den Bytecode
      wegwerfen, bevor gemessen wird —

          find . -name __pycache__ -prune -exec rm -rf {} +

      oder den Lauf mit `python3 -B` fahren (schreibt keinen Cache). Und die
      Rueckgabe OHNE Pipe messen: `python3 test.py | grep …` liefert den Status
      von `grep`, nicht den des Tests — auch das hat an diesem Tag zweimal ein
      falsches „0" erzeugt.
  - kind: falle
    id: grosses-timeout-macht-stillstand-unsichtbar
    title: 'Ein grosses Timeout ist nicht robust - es macht den Stillstand unsichtbar'
    source: "am 31.08.2026 am Karten-Installer erlebt (Balken stand 29 Minuten bei 7 %)"
    tags: [netz, download, timeout, rueckmeldung, sdstart, sdprep, methodik]
    symptom: >-
      Ein Fortschrittsbalken steht sehr lange still. Es kommt keine Meldung,
      kein Fehler, kein Abbruch - der Vorgang sieht aus, als arbeite er noch.
    body: |
      DER FEHLER WAR EINE ZAHL. `sdprep.download()` holte die Verbindung mit
      `timeout=120`. Reisst das Netz MITTEN im Laden ab, blockiert `r.read()`
      bis zu zwei Minuten - und weil die Leseschleife nicht zurueckkommt, gibt
      es keine Stelle, an der ein Hinweis ENTSTEHEN koennte. Bei einem halb
      offenen TCP dauert es noch laenger.

      DARAUS DIE REGEL: Ein grosses Timeout fuehlt sich geduldig an, ist aber
      das Gegenteil von robust. Es verlaengert genau das Fenster, in dem das
      Programm nichts sagen kann. Geduld gehoert nicht in EINEN langen
      Leseversuch, sondern in die WIEDERHOLUNG kurzer:

          LESEFENSTER_S = 15    # so lange darf EIN read() blockieren
          GEDULD_S     = 120    # so lange ohne ein einziges Byte, dann Schluss

      Nach jedem leeren Fenster kommt die Schleife zurueck, kann melden und
      weitermachen. Gemessen wird der ZEITPUNKT DES LETZTEN BYTES, nicht die
      Zahl der Fehlversuche: bei schwankendem Netz wechseln sich leere und
      volle Fenster ab, und solange Bytes kommen, ist das kein Stillstand,
      sondern langsam.

      WAS DIE MELDUNG LEISTEN MUSS: Sie kommt nach 15 s, NICHT erst beim
      Abbruch - wer zusieht, soll waehrend des Wartens erfahren, worauf
      gewartet wird, und selbst entscheiden koennen, ob er das WLAN richtet
      oder abbricht. Und die Erholung wird ebenfalls gemeldet, sonst bleibt die
      letzte Zeile auf dem Schirm eine Warnung, waehrend laengst alles laeuft.

      DAZU `sdprep.netz_pruefen()`: VOR dem Laden gefragt, nicht danach. Zwei
      Minuten in ein totes Netz zu warten und DANN zu melden, ist dieselbe
      Auskunft - nur zwei Minuten spaeter. Es trennt "Name nicht aufloesbar"
      (DNS/kein Netz) von "niemand nimmt ab" (Strecke da, Gegenstelle weg),
      weil das zwei verschiedene Handgriffe sind.

      UND `sdprep.KeinNetz` statt einer nackten URLError: Die Oberflaechen
      zeigten sonst "<urlopen error [Errno -3]>" im allgemeinen
      Traceback-Fenster - 1500 Zeichen Python, aus denen niemand "dein Netz ist
      weg" liest.

      HALB GELADEN IST SCHLIMMER ALS GAR NICHT: Bricht das Laden ab, entsteht
      die Zieldatei NICHT (es wird ueber `.part` gearbeitet und erst am Ende
      umbenannt). Ein halbes Abbild als fertig liegenzulassen ergaebe beim
      naechsten Lauf eine Karte, die halb bootet - und der Grund waere dann
      nicht mehr zu sehen.
  - kind: falle
    id: bt-skripte-lagen-nie-im-baum
    title: 'Kein Bluetooth-Pairing auf frischer Box - vier Skripte lagen nie im Baum'
    match: "No agent is registered|br-connection-profile-unavailable|Pairable: no"
    source: "am 31.08.2026 an frisch installiertem Pi 4 gegen den gewachsenen Pi 5 gemessen"
    tags: [bluetooth, pairing, installer, rezepte, ausrollweg, pi4]
    symptom: >-
      Auf einer frisch installierten Box laesst sich kein Bluetooth-Geraet
      koppeln. bluetoothctl meldet "No agent is registered", der Adapter bleibt
      auf "Pairable: no", und ein connect endet mit
      org.bluez.Error.Failed br-connection-profile-unavailable.
    body: |
      GEMESSEN (31.08.2026), Pi 5 gegen frisch installierten Pi 4:

          /usr/local/bin/mupibox/  Pi 5: pair_ remove_ scan_ start_ stop_bt.sh
                                   Pi 4: pair_bt.sh
          scripts/bluetooth/ im Baum:    pair_bt.sh   <- NUR EINES

      Der Ausrollweg war NICHT das Problem: `bluetooth` steht in der
      Sammelkopie von recipes/mupibox-app.yaml. Die vier anderen Skripte lagen
      schlicht NIE im Baum - der Pi 5 hatte sie noch aus seiner
      Erstinstallation mit dem Original-autosetup. Wer nur die gewachsene Box
      ansieht, haelt sie fuer vorhanden.

      DAS IST DIE ALLGEMEINE FORM: Eine von Hand grossgezogene Box ist KEIN
      Beleg dafuer, dass etwas ausgerollt wird. Sie belegt nur, dass es
      irgendwann einmal dorthin kam. Der Unterschied faellt erst auf, wenn eine
      frische Karte bootet - siehe [[rezeptschleife-nennt-acht-von-siebzehn]]
      und `tools/rezepte-gegen-box.py`.

      ZWEI FEHLER IN DEN SKRIPTEN SELBST, beide im Original, beide beim
      Uebernehmen behoben:
        * `defaut-agent` statt `default-agent` (start_bt.sh, remove_bt.sh).
          bluetoothctl verwirft den Befehl, es wird KEIN Standard-Agent
          registriert - und ohne Agent nimmt der Adapter keine Kopplung an.
          Genau das ist die Meldung "No agent is registered" und der Grund fuer
          `Pairable: no`.
        * `ouput=$(cat <&${COPROC[0]})` gefolgt von `echo $output` (drei
          Skripte). Zuweisung in die eine Variable, gelesen die andere: die
          Ausgabe war IMMER leer. Jeder Aufruf sah damit erfolgreich aus, egal
          was bluetoothctl gesagt hatte.

      NICHT VERWECHSELN mit "gekoppelt, aber kein Ton". Steht die Kopplung,
      taucht der Lautsprecher als PipeWire-Senke auf
      (`bluez_output.<MAC>.1`), und der Ton geht trotzdem nicht dorthin,
      solange die Vorgabe-Senke etwas anderes ist (am Pi 5 gemessen:
      `node.name = "klangwerk"`). Das ist die Ausgabewahl der Verwaltung, kein
      Bluetooth-Fehler.
  - kind: falle
    id: ohne-linger-kein-ton-auf-frischer-box
    title: 'Kein Ton auf frisch installierter Box - loginctl enable-linger fehlt im Rezept'
    match: "Dummy Output|wpctl status.*keine Sinks|kein Ton auf frischer Box"
    source: "am 31.08.2026 Pi 5 gegen frisch installierten Pi 4 gemessen, Fix am Geraet belegt"
    tags: [ton, pipewire, systemd, linger, installer, rezepte, pi4, bluetooth]
    symptom: >-
      Auf einer frisch aufgesetzten Box kommt nirgends Ton an - weder am
      internen Lautsprecher noch ueber Bluetooth. `wpctl status` zeigt nur
      "Dummy Output" und keine ALSA- oder bluez-Geraete. speaker-test direkt
      auf die ALSA-Karte spielt aber hoerbar.
    body: |
      URSACHE: `loginctl enable-linger dietpi` fehlt. Ohne Linger laeuft
      `systemd --user` nur, solange eine Sitzung offen ist. Mit ihr sterben
      PipeWire und WirePlumber; beim naechsten Login starten sie neu. Die
      Audio-Geraete haengen damit an der Anwesenheit einer Anmeldung.

      GEMESSEN (31.08.2026):
          loginctl show-user dietpi -p Linger   Pi 5: yes   frischer Pi 4: no
      Nach `enable-linger` auf dem Pi 4 sofort: statt "Dummy Output" ein
      echter Sink ("Built-in Audio Stereo").

      WARUM ES NIEMANDEM AUFFIEL: `autosetup/autosetup.sh` (Z. 985) und
      `update/start_mupibox_update.sh` setzen es - KEIN Rezept des
      remote-step-installer tat es. Der Pi 5 trug es aus seiner
      Erstinstallation mit dem alten Weg. Wer nur die gewachsene Box ansieht,
      haelt es fuer vorhanden.

      DIE TRAGWEITE, vom Betreiber sofort erkannt: „also auch neuaufsetzen von
      p5 haette nicht geklappt". Genau so. Der Fehler lag die ganze Zeit da und
      war unsichtbar, weil niemand neu aufgesetzt hat. Dieselbe Fehlerklasse
      wie [[bt-skripte-lagen-nie-im-baum]] und
      [[rezeptschleife-nennt-acht-von-siebzehn]]: EINE VON HAND GROSSGEZOGENE
      BOX IST KEIN BELEG DAFUER, DASS ETWAS AUSGEROLLT WIRD. Sie belegt nur,
      dass es irgendwann einmal dorthin kam.

      ZWEI MESSFALLEN, die dabei fast in die Irre gefuehrt haben:
      * `wpctl status` UEBER SSH misst die Sitzung des SSH-Logins, nicht die
        Instanz, in der die Box arbeitet. Ohne Linger bekommt jeder SSH-Aufruf
        eine frische, leere Instanz - wechselnde PIDs und "Active since 200ms"
        sahen aus wie ein Absturz-Kreisel. PipeWire lief im Vordergrund
        tadellos durch (nur RTKit-Warnungen).
      * Die zweite PipeWire-Instanz aus `/tmp/mixpi-ueberall.conf` sah wie die
        Ursache aus (sie beenden brachte kurz Geraete zum Vorschein), war es
        aber nicht: den Socket haelt systemd ueber `pipewire.socket`.

      BEHOBEN im Schritt `kiosk-als-benutzer` (recipes/mupibox.yaml).

      NACHTRAG VOM SELBEN ABEND — der Satz „samt `check:` auf Linger=yes" ist
      ueberholt, und der Fix, den er beschrieb, war schaedlich: ein blankes
      `loginctl enable-linger` im Rezept BRACH DIE INSTALLATION AB (21 von 56
      Schritten, dann Halt). Wer diesen Eintrag als Bauanleitung nimmt, baut
      den Schaden nach. Der heutige Weg legt notfalls die Datei
      `/var/lib/systemd/linger/<benutzer>` selbst an, und der `check:` prueft
      genau diese Datei statt `loginctl show-user`. Warum, steht in
      [[rezeptschritt-hat-keine-benutzersitzung]] — das ist die eigentliche
      Lehre des Tages, nicht der Linger-Schalter.
  - id: der-titel-ist-das-kleinste-teil
    kind: howto
    title: "Titelweise Quellen (E108): die Karte ist ABGELEITET, jellyfin wird gemischt, Spotify nur gestempelt, die Reihenfolge ist einstellbar"
    match: "titel kleinste teil|titelweise quelle|mischliste|titelkarte|titelMischen|quelle je titel|abspielen reihenfolge|quellen reihenfolge einstellen|plakette quelle|titel quellen"
    sev: medium
    cause: "Betreiber (31.08.2026): 'Der titel ist das kleinste teil nicht das album ... ein titel kann im album schon lokal vorliegen der naechste muss dann ggf ueber einen anderen dienst geholt werden.' Gebaut als E108 Stufe 1 OHNE vierte Wahrheit: die Identitaet eines Titels ueber Quellen hinweg ist die NUMMER (die Aufnahme schreibt NN <Titel>.<endung>, und die NN stammt aus der Quell-Liste des Meta-Albums - dieselbe Zaehlung wie titel[].nr in /inhalt); der Name ist nur GEGENPROBE (Sanitisierung / zu _ macht ihn unscharf). Gelesen wird das VERZEICHNIS, nicht die playlist.m3u - die hinkt der Aufnahme nach (gemessen 31.08.: 2 von 3 Dateien)"
    fix: "titelkarte.ts (rein, 18 Spec-Faelle): spurNummerAus/lokaleKarteAus/spurNamePasst/titelMischen/titelQuellenStempeln. Server: lokaleSpurenKarte (readdir + mtime-Cache je Albumordner), inhaltFuerEintrag(mischen) - jellyfin wird GEMISCHT (lokal gewinnt je Titel nach Reihenfolge -> datei/...-Befehle), Spotify nur GESTEMPELT (Maschinen-Physik: jeder soloist-Wechsel kostet 1-2 s Stille, der EINE Wechselpunkt ist Stufe 3), lokal/ard/plugin stempeln ihre Herkunft. JEDER /inhalt-Titel traegt quelle (gewaehlt) und quellen (vorhanden); /api/spielen erbt denselben Bau - EINE Wahrheit fuer Anzeige und Ton. Reihenfolge: mupiboxconfig.abspielen.reihenfolge (abspielReihenfolge(), Rueckfall QUELLEN_REIHENFOLGE, Unbekanntes ans Ende) wirkt an allen verschmelzeWerke-Stellen UND im Merge - Integrationsbeweis: dasselbe Werk spielt ohne Einstellung lokal, mit spotify-zuerst ueber Spotify. Plakette am Player-Cover: #gr-quelle, laufendeQuelle() mit drei Stufen (titel[].quelle je laufendem Titel -> Spotify-Lage -> currentType-Rueckfall, 'datei' heisst lokal); Wache tools/quellen-plakette-schau.mjs im Laeufer, Sabotage-Gegenprobe bestanden"
    tags: [e108, verschmelzung, server, ui, architektur, 2026]
    related: [quellen-verschmelzen, dienste-umschalten, post-api-spielen-ist-die-spielfunktion, datei-verben-spielen-eine-spur-mit-zaun]
    body: |
      ══ DER MISCH-KERN (titelMischen) ════════════════════════════════════════
      Je Titel der Gewinner-Liste: quellen sammeln (lokal aus der Karte per
      nr + Namens-Gegenprobe; der eigene Dienst, wenn der Titel einen befehl
      traegt), dann gewinnt die ERSTE Quelle der eingestellten Reihenfolge,
      die den Titel HAT. lokal gewinnt -> Befehle werden ersetzt
      (datei/dateiqueue, Form der jfBefehle). Ein Jellyfin-Titel OHNE
      Stromadresse wird lokal bedient, wenn die Spur liegt. mischen gilt NUR
      bei eingeschalteter Verschmelzung ohne exklusiven quelle=-Wunsch.

      ══ DIE GEGENPROBE, DIE BEIM BAU BEINAHE NICHTS MASS ═════════════════════
      Die erste Sabotage der Plaketten-Wache sass im dienst-Wechsel-Zweig von
      quelleSetzen - der naechste Anzeige-Takt (gleicher Dienst) heilte sie,
      die Wache blieb gruen. Erst die Sabotage am echten Sichtbarmachen
      (ziel.hidden = false -> true) machte drei Messungen rot. Lehre im
      Muster gegenprobe-statt-gruen-glauben: die Sabotage muss den WIRKWEG
      treffen, nicht irgendeine Zeile der Funktion.

      ══ GRENZEN VON STUFE 1, EHRLICH ═════════════════════════════════════════
      jellyfin-lokal-Mischweg End-zu-End steht als Geraetebeweis aus (Box
      durchgehend bespielt); currentType-Rueckfall der Plakette ist
      Code-gedeckt (die Vorschau-Attrappe fuehrt das Feld nicht);
      titelQuellenStempeln sagt bei Jellyfin-Titeln ohne Strom trotzdem
      jellyfin in quellen (kosmetisch, Matrix Stufe 2 praezisiert das).
      Stufe 2: Titel-Matrix im Aufnahme-Steuerpult. Stufe 3: der EINE
      Spotify-Wechselpunkt (PipeWire-Cue, siehe BACKLOG E108).
  - id: datei-verben-spielen-eine-spur-mit-zaun
    kind: howto
    title: "datei/dateiqueue spielen EINE lokale Datei ueber mpv - mit Medienordner-Zaun und eigenem currentType"
    match: "datei verb|dateiqueue|einzelne datei abspielen|dateiPfadErlaubt|currentType datei|mischliste player|lokale spur in warteschlange"
    sev: medium
    cause: "E108 Stufe 1 (31.08.2026): die Mischliste (Titel 1 lokal, Titel 2 Jellyfin) braucht einen Player-Weg fuer EINE Datei - musicsearch/library spielt nur GANZE Alben ueber die playlist.m3u. Die neuen Verben sind baugleich zu plugin/pluginqueue (spotify-control.ts), nehmen aber einen VOLLEN Dateipfad entgegen - und ein Pfad-Verb ohne Grenze spielte jede lesbare Datei der Karte ab (Port 5005 hoert nur auf 127.0.0.1, aber der /player-Proxy reicht aus dem ganzen Netz durch, derselbe Grund, aus dem die Kinderzeit am Proxy sitzt)"
    fix: "Zaun dateiPfadErlaubt (befehlspfad.ts, rein + Spec): Medienordner-Praefix, '..' als SEGMENT und NUL-Byte fallen durch; Abweisung antwortet 400 mit eigenem Fehler statt des gemeinsamen ok (deletelocal-Muster). Eigener currentType 'datei' mit DREI Hoerer-Weichen: metadata-AUSSCHLUSS (sonst ueberschreibt der Datei-Tag den Befehlsnamen), media_title-EINSCHLUSS (sonst kommt force-media-title beim automatischen Weiterruecken nie an und ab Titel 2 steht der Name von Titel 1 - exakt der F1-Fehler), path-AUSSCHLUSS (val.split('/')[7] eines freien Pfads ist irgendein Segment). Kinderzeit-START kennt dateiqueue|datei (Erlaubnisliste!). Dazu --prefetch-playlist=yes im mpv-Aufsatz: mpv oeffnet den naechsten Warteschlangen-Eintrag schon am Ende des laufenden - der Uebergang Datei->Strom kommt ohne Anlauf-Luecke aus"
    tags: [player, e108, mpv, sicherheit, 2026]
    related: [kinderzeit-erlaubnisliste-kennt-neue-verben-nicht, der-titel-ist-das-kleinste-teil]
    body: |
      ══ BEFEHLSFORM (dieselbe wie jellyfin/jfqueue) ═══════════════════════════
      datei/<enc(vollpfad)>/<enc(titel)>:title:artist:<enc(interpret)>
      dateiqueue/<dasselbe> zum Anhaengen; :ab:N wird wie bei plugin
      weitergereicht (ein Mitschnitt einer Quelle mit Vorspann traegt den
      Vorspann in der Datei).

      ══ WARUM NICHT currentType 'local' ══════════════════════════════════════
      'local' schaltet die Hoerer auf "Titel/Album aus dem DATEIPFAD lesen"
      (der m3u-Weg mit seiner festen Pfadtiefe). Die Misch-Spur bringt ihren
      Namen im Befehl mit (force-media-title) - sie gehoert in die
      Strom-Familie. Und 'jellyfin' waere eine Falschaussage (dieselbe
      Begruendung wie am plugin-Zweig).

      ══ BEKANNTE ALT-GRENZE ══════════════════════════════════════════════════
      Die klassische Angular-Oberflaeche (frontend-box player.page.ts:360)
      behandelt alles ausser radio/rss als 'library' und zerlegt local.path
      positionsabhaengig - fuer 'datei' genauso falsch wie heute schon fuer
      jellyfin/ard. Nicht verschlimmert, nicht angefasst (Bestand bleibt
      mupibox).
  - id: zwei-boxen-teilen-sich-mixpibox-local
    kind: gotcha
    title: "mixpibox.local loest auf ZWEI Geraete auf - Deploys und Waechter nur an die feste Adresse der Arbeits-Box"
    match: "mixpibox.local|zweitbox|zweite box|host key changed|remote host identification|deploy falsche box|192.168.178.78|mdns konflikt"
    sev: high
    cause: "Befund 31.08.2026 beim Deploy-Waechter: mixpibox.local beantwortete mDNS mit den Adressen ZWEIER Geraete - der Arbeits-Box (192.168.178.78, MAC 88:a2:9e:48:f6:4f, E95-Stand, Familien-Spotify) und einer Zweitbox (192.168.178.79, Pi-MAC e4:5f:01:7d:18:d2, ALTER Stand ohne /api/spielen, spielte selbst ARD ueber mplayer, ssh nur ueber IPv6). Jede Namens-Aufloesung ist damit eine Lotterie: der Waechter um 16:31 hielt die freie ZWEITBOX fuer die freie Arbeits-Box, und der folgende ssh-Deploy brach an deren fremdem Hostschluessel ab (REMOTE HOST IDENTIFICATION HAS CHANGED) - zum Glueck, denn sonst waere auf die falsche Box ausgeliefert worden, waehrend die Familie auf der richtigen hoerte"
    fix: "Alles Geraetebezogene an die FESTE Adresse 192.168.178.78 (oder die MAC-stabile ULA fd8e:ca9c:6cb9:0:8aa2:9eff:fe48:f64f), nie an den Namen. Der Hostschluessel der Arbeits-Box (SHA256:cyVF6JC0..., Key-Datei vom 11.08.2026) ist als NEUER known_hosts-Eintrag fuer die Adresse aufgenommen; der alte Namens-Eintrag blieb ABSICHTLICH stehen und blockiert Namens-Deploys mit der Hostkey-Warnung - das ist eine Sperre, kein Schaden. Identitaet der Arbeits-Box im Zweifel per API pruefen: POST /api/spielen mit leerem Rumpf antwortet dort mit dem Fehlertext 'schluessel fehlt', die Zweitbox mit einer Express-404-HTML-Seite. OFFEN: Betreiber entscheidet, ob die Zweitbox versorgt wird und wie der mDNS-Namenskonflikt aufgeloest wird"
    tags: [betrieb, deploy, netz, ssh, 2026]
    related: [post-api-spielen-ist-die-spielfunktion]
    body: |
      ══ WOZU DIE SPERRE GUT IST ══════════════════════════════════════════════
      Wer den alten known_hosts-Eintrag fuer mixpibox.local "repariert"
      (ssh-keygen -R + neu aufnehmen), macht Namens-Deploys wieder moeglich -
      und damit die Lotterie. Solange zwei Geraete den Namen tragen, ist die
      Warnung der einzige Zeuge, der VOR dem Kopieren anschlaegt.

      ══ WIE DER BEFUND ENTSTAND ══════════════════════════════════════════════
      Der Waechter prueft currentPlayer/playing an /player/local. Beide Boxen
      fuehren dieselbe API; frei/besetzt sagt also nichts darueber, WELCHE Box
      geantwortet hat. Erst die Fingerprint-Matrix ueber alle Adressen
      (ssh-keyscan je IP) plus der Feature-Vergleich (/api/spielen ja/nein)
      trennte die Geraete sauber.
  - id: getoetete-leck-probe-laesst-ihre-serverkopie-liegen
    kind: gotcha
    title: "Abgebrochene fehlerbehandler-leck-schau laesst .server-leck-probe.<pid>.ts liegen - und die Herkunfts-Wache meldet sie als neuen Draht"
    match: "server-leck-probe|fehlerbehandler-leck-schau|NEUER DRAHT|herkunft-annahme-probe|liegengebliebene kopie|punktdatei backend-api"
    sev: low
    cause: "Befund im Doku-Lauf 31.08.2026 (siebter): tools/doku-luecken-probe.sh meldete 'NEUER DRAHT: .server-leck-probe.215177.ts:17175 oeffnet einen WebSocket'. Die Datei ist keine Quelle, sondern eine 885-KB-Wegwerfkopie von server.ts, die tools/fehlerbehandler-leck-schau.mjs zum Buendeln NEBEN server.ts legt (die relative Nachbarschaft braucht esbuild). Sie wird im finally geloescht - ein hart getoeteter Lauf (PID 215177 war tot) kommt dort nie an. Liegen geblieben faellt sie unter das Suchmuster von tools/herkunft-annahme-probe.py, denn die sucht auf SORTE (WebSocket-Aufrufe im Backend), nicht auf einer Pfadliste - richtig so, aber die Kopie traegt jeden Draht des Originals ein zweites Mal"
    fix: "Datei loeschen (rm src/backend-api/src/.server-leck-probe.*.ts), wenn kein Lauf mehr laeuft - danach meldet die Probe wieder 'ANNAHME HAELT'. Vor dem Loeschen die Prozessnummer im Namen pruefen (ps -p <pid>): ein LAUFENDER Lauf braucht seine Kopie. Der Punkt am Namensanfang haelt sie aus src/*.ts und src/*.spec.ts heraus, also aus Bau und Tests - nur die Sorten-Wache sieht sie. OFFEN (Code, nicht Doku): die Leck-Schau raeumt beim START keine fremden Altlasten weg; ein `.server-leck-probe.*`-Kehraus mit Lebendpruefung der PID waere die Naht, an der der Befund nicht wiederkehrt"
    tags: [werkzeug, doku-wache, aufraeumen, backend, 2026]
    related: [aufraeumen-nachmessen, ungerufene-wache-driftet-in-den-fehlalarm, pkill-toetet-die-eigene-shell]
    body: |
      ══ WARUM DAS KEIN FEHLALARM DER WACHE IST ═══════════════════════════════
      Die Versuchung ist, tools/herkunft-annahme-probe.py Punktdateien
      uebersehen zu lassen. Das waere genau der Fehler, vor dem die Wache
      schuetzt: Sie misst die SORTE (wer oeffnet im Backend einen ausgehenden
      Draht?), nicht einen Pfad. Eine Ausnahme fuer `.`-Namen macht den
      Ablageort zum Versteck - wer eine echte Quelle so benennt, ist unsichtbar.
      Der Befund ist korrekt; falsch ist die liegengebliebene Datei.

      ══ WORAN MAN SIE ERKENNT ════════════════════════════════════════════════
      * Name `.server-leck-probe.<pid>.ts` in src/backend-api/src/, untracked.
      * Inhalt = server.ts plus ein eingesetzter Block hinter dem
        Fehlerbehandler. `diff <(git show HEAD:src/backend-api/src/server.ts)
        <datei>` zeigt genau die Einspritzung und den eigenen Arbeitsstand.
      * Sie kostet knapp 900 KB je Lauf und haeuft sich, weil jeder Lauf eine
        eigene Prozessnummer traegt.
  - id: vorschau-instanz-traegt-den-code-ihres-starts
    kind: gotcha
    title: "Die laufende Vorschau serviert Dateien frisch, aber ihre STUBS sind vom Start - nach Attrappen-Edits neu starten"
    match: "vorschau alt|neu-vorschau neustart|stub alt|EADDRINUSE 8299|wache rot nur im laeufer|attrappe alter stand|vorschau instanz"
    sev: medium
    cause: "31.08.2026, E108: die neue Plaketten-Wache war solo (eigener Port, frische Vorschau) gruen und im Pruefen-Laeufer rot. Ursache war KEINE klebrige Lage, sondern eine seit 12:17 laufende neu-vorschau-Instanz auf 8299 (aus der Vormittags-Sitzung stehengeblieben): index.html/app.js liest sie je Request von der Platte - ihr /api/spielen-STUB aber ist einkompilierter Node-Code vom Startzeitpunkt, und der kannte das neue quelle-Feld nicht. laufendeFolgen ohne quelle -> Plakette dunkel. Der eigene Nachstell-Versuch auf 8299 scheiterte still mit EADDRINUSE im LOG, waehrend curl gegen die ALTE Instanz 200 sagte - es sah aus wie eine frische Vorschau"
    fix: "Nach jedem Edit an tools/neu-vorschau.mjs die laufende Instanz ERSETZEN, nicht daneben starten: Halter finden (ss -tlnp | grep 8299), Startzeit und cwd pruefen (ps lstart, readlink /proc/PID/cwd - gehoert sie einer PARALLEL-Sitzung im Worktree, Finger weg), dann kill PID (nie pkill -f) und frisch starten. Beim Diagnostizieren zuerst ins eigene Start-LOG sehen (EADDRINUSE steht nur dort), nie dem curl-200 glauben - das beantwortet auch der Alte. Verwandtes Muster: einmal-hinsehen-ist-keine-messung"
    tags: [vorschau, wachen, betrieb, 2026]
    related: [attrappe-luegt-durch-weglassen]
    body: |
      ══ WARUM DAS DOPPELT GEMEIN IST ═════════════════════════════════════════
      Die halbe Vorschau IST live (statische Dateien) - UI-Aenderungen wirken
      sofort, und genau das erzeugt das falsche Vertrauen, ALLES sei live.
      Die Server-Haelfte (Stubs, Lagen, /vorschau-Stellschrauben) ist es
      nicht. Eine Wache, die UI UND Stub braucht, misst dann halb gestern.
  - id: mpv-pause-ist-im-leerlauf-false
    kind: gotcha
    title: "mpvs pause-Property ist im Idle FALSE - wer playing daraus ableitet, spielt nach jedem Stop fuer immer"
    match: "playing true nach stop|playing zombie|idle-active|pause property leerlauf|stop springen titel|anzeige spielt nach stop|E109"
    sev: high
    cause: "Betreiber-Fund Minuten nach der E108-Auslieferung (31.08.2026): 'beim stop springen die titel'. Am Geraet zerlegt (drei Sonden: datei-Weg mitten im Titel, datei-Weg 1,5 s vor dem Uebergang mit scharfem prefetch, m3u-Weg 1,6 s vor dem Uebergang): der TON stoppt in allen Faellen sauber - prefetch-playlist ist NICHT der Taeter. Aber `playing` stand nach JEDEM mpv-Stop dauerhaft wieder auf true: stop() schreibt playing=false, eine Sekunde spaeter fragt der Takt in spotify-control mpvs `pause` ab, und die ist im IDLE false - der Hoerer machte daraus 'spielt'. Mit leerem Titel und leerer Nummer daneben sprang die Anzeige. Unsichtbar blieb das jahrelang, weil die UI-Weiche am geraeumten currentPlayer haengt - erst neue playing-Leser machten es sichtbar"
    fix: "idle-active beobachten (EIGENSCHAFTEN in mpv-protokoll.ts, wertUmsetzen wie pause auf hartes bool) und den Leerlauf GEWINNEN lassen: eigener Hoerer setzt playing=false und ein Riegel im pause-Hoerer schreibt im Idle gar nicht (spotify-control.ts, mpvLeerlauf). mplayer meldet die Eigenschaft nie - dort bleibt alles beim Alten. Geraetebeweis: Sonde C nach dem Fix -> endstand playing=False. Wer den naechsten Zustands-Hoerer baut: mpvs Properties gelten NUR waehrend einer Wiedergabe als Wahrheit; im Idle sind pause/duration/time-pos Reste oder Vorgaben"
    tags: [player, mpv, e109, zustand, 2026]
    related: [datei-verben-spielen-eine-spur-mit-zaun, einmal-hinsehen-ist-keine-messung]
    body: |
      ══ DIE DREI SONDEN, KURZ ════════════════════════════════════════════════
      B  datei+dateiqueue, stop bei t=4 s      -> kein Ton mehr, playing klebt
      A  dito, stop 1,5 s vor dem Uebergang    -> kein Ton mehr, playing klebt
      C  m3u-Album, seekpos:98, stop           -> kein Ton mehr, playing klebt
      Nach dem Fix: C erneut -> playing=False, 0 von 15 Lebenszeichen.

      ══ WARUM NICHT EINFACH stop() HAERTEN ══════════════════════════════════
      stop() SCHREIBT ja schon false - der Poll ueberschreibt es jede
      Sekunde. Eine Wahrheit, die im Takt kaputtgeschrieben wird, repariert
      man an der Quelle des Schreibens, nicht am Opfer.
  - id: fremdstart-anzeige-braucht-den-namenskern
    kind: gotcha
    title: "Start von aussen: die Kiosk-Anzeige erkennt das Werk nur am gemeldeten Namen - und der ist SANITISIERT"
    match: "cover fehlt|fremdstart anzeige|passtZuLaufendem|namenskern|curl start kein cover|telefon start anzeige leer|sanitisierung schraegstrich"
    sev: medium
    cause: "Betreiber (31.08.2026, Testrunde nach E108): 'das cover fehlt ... gerade bei guten morgen' - gestartet war das Werk per curl (POST /api/spielen), nicht am Kiosk. Ohne zuletztGestartet muss passtZuLaufendem (app.js) das Werk am GEMELDETEN Namen finden; der Abspieldienst meldet beim library-Weg aber Ordner- und Dateinamen, also die SANITISIERTE Schreibweise der Aufnahme (Guten Morgen _ Good Morning), waehrend das Werk den Schraegstrich traegt. Der woertliche includes-Vergleich fand nie zusammen - kein Cover, kein Werkstitel, obwohl /api/bild 200 mit 172 KB lieferte (gemessen). Der Interpret half nicht: er steht in keinem /local-Feld"
    fix: "Vergleich ueber den NAMENSKERN (nur Buchstaben und Ziffern, lowercase): namensKern() in app.js, wortgleich zur Server-Regel spurNamePasst in titelkarte.ts - die Sanitisierung /->_ und alle Satzzeichen sind keine Unterschiede mehr. Die Regel gilt damit an ZWEI Naehten (Server-Titelkarte und Kiosk-Anzeige); wer eine dritte baut, nimmt denselben Kern. ZWEITE WURZEL DERSELBEN RUNDE: der Fix lag laengst ausgeliefert, der Kiosk fuhr trotzdem die alte Fassung - /api/oberflaeche/stand bestand NUR aus den Angular-main-Buendeln, die neue Oberflaeche (/neu/app.js, feste Namen) tauchte nie darin auf; ein www-Deploy, der nur sie aendert, liess den Stand gleich und der Selbst-Neulader (standPruefen, 60-s-Takt) schlief. Fix: die mtime der drei /neu-Kerndateien zaehlt in die Stand-Rechnung; Soforthebel am Geraet: POST /api/oberflaeche/neuladen. Bekannte Restunschaerfe des Kerns: Teiltreffer ueber Wortgrenzen theoretisch moeglich - fuer Titel+Interpret-Laengen praktisch irrelevant"
    tags: [ui, anzeige, e109, verschmelzung, 2026]
    related: [der-titel-ist-das-kleinste-teil, mpv-pause-ist-im-leerlauf-false]
    body: |
      ══ WIE MAN DEN FALL NACHSTELLT ══════════════════════════════════════════
      curl -X POST http://<box>:8200/api/spielen -H 'Content-Type: application/json' \
        -d '{"schluessel":"<werk mit sanitisiertem Mitschnitt>"}'
      und auf den Kiosk schauen: VOR dem Fix Platzhalter-Cover und leerer
      Werksbezug, NACH dem Fix Cover + Titel + Quellen-Plakette.
  - id: rezeptschritt-hat-keine-benutzersitzung
    kind: falle
    title: "Ein Rezeptschritt laeuft in KEINER Benutzersitzung — alles ueber D-Bus/logind ist dort ein Wackelkandidat"
    match: "Could not enable linger|failed to load properly|dbus-org.freedesktop.login1|loginctl im Rezept|Schritt bricht Lauf ab|Abbruch bei 22 von 56"
    sev: high
    source: "31.08.2026 am frisch aufgesetzten Pi 4 gemessen: stand.json und lauf.log der Karte aus dem naechsten Durchlauf gelesen"
    tags: [installer, rezepte, systemd, logind, dbus, linger, pi4, 2026]
    related: [ohne-linger-kein-ton-auf-frischer-box]
    symptom: >-
      Ein Rezeptschritt, der von Hand am laufenden System tadellos
      funktioniert, scheitert im Installer-Lauf — und reisst den GANZEN Lauf
      mit: `stand.json` meldet „fertig: 21 Schritte, gescheitert:
      mupibox:kiosk-als-benutzer", `lauf.log` dazu „Could not enable linger:
      Unit dbus-org.freedesktop.login1.service failed to load properly".
    body: |
      DIE URSACHE ist nicht der Befehl, sondern die UMGEBUNG: ein Rezeptschritt
      laeuft nicht in einer angemeldeten Benutzersitzung. Was D-Bus, logind
      oder eine Session voraussetzt, ist dort nicht verlaesslich da. `loginctl`
      spricht logind ueber den System-D-Bus an — im Lauf trifft es ihn
      manchmal nicht an.

      DIE TEUERSTE EINZELHEIT DARAN: der Fehler war ein FIX. Am Nachmittag des
      31.08.2026 wurde `loginctl enable-linger` in den Schritt
      `kiosk-als-benutzer` aufgenommen, um den fehlenden Ton auf frischen Boxen
      zu reparieren ([[ohne-linger-kein-ton-auf-frischer-box]]). Der Ton war
      damit repariert und die INSTALLATION KAPUTT — Abbruch bei 22 von 56
      Schritten. Vorher lief sie durch (nur ohne Ton). Der Fix hat es also
      schlimmer gemacht, und gesehen wurde das erst, als der Betreiber die
      Karte des naechsten Durchlaufs in den Leser legte.

      DER AUSWEG ist fast immer derselbe: nachsehen, was der bequeme Befehl
      INTERN tut, und das direkt tun. `enable-linger` legt eine leere Datei an,
      `/var/lib/systemd/linger/<benutzer>`. Das geht ohne D-Bus und ohne
      laufendes logind, und systemd liest sie beim naechsten Start.

          if loginctl enable-linger "$U" 2>/dev/null; then
            echo "  Dauersitzung ueber logind gesetzt"
          else
            mkdir -p /var/lib/systemd/linger
            touch "/var/lib/systemd/linger/$U"
          fi
          [ -e "/var/lib/systemd/linger/$U" ] || { echo "  FEHLER"; exit 1; }

      ZWEI REGELN, die daraus folgen und ueber diesen Fall hinausgehen:

      * KEIN `|| true` UEBER ALLEM. Scheitern beide Wege, soll es auffallen.
        Ein Schritt, der immer gruen meldet, ist derselbe blinde Fleck in
        neuer Form. Die Naht dafuer ist die Pruefung auf das ERGEBNIS (liegt
        die Datei?), nicht auf den Rueckgabewert des bequemen Befehls.
      * DER `check:` HAT DENSELBEN FEHLER WIE DER SCHRITT, wenn er dieselbe
        Maschinerie benutzt. Hier stand `loginctl show-user … -p Linger`, und
        er waere im Lauf aus genau demselben Grund gescheitert. Wer einen
        Schritt von D-Bus loest, muss seinen `check:` mitloesen — sonst
        verlagert man den Absturz nur eine Zeile weiter.

      WIE MAN SICH SELBST HEREINLEGT: „ich habe es getestet" hiess hier, es
      von Hand ueber SSH am laufenden System zu tun — also in einer
      Benutzersitzung, mit laufendem logind, unter genau den Bedingungen, die
      im Rezeptlauf fehlen. Ein Handtest am laufenden System beweist ueber
      einen Rezeptschritt nichts — dieselbe Denkweise wie bei
      [[bt-skripte-lagen-nie-im-baum]]: der bequeme Beobachtungsort ist nicht
      der, an dem der Fehler wohnt.
  - id: anmeldewache-kam-ohne-ihren-takt-an
    kind: falle
    title: "Die Anmeldewache lag auf jeder frischen Karte — ohne Unit und ohne Timer, und die Wache meldete gruen"
    match: "soloist-anmeldewache|spotify bleibt stumm nach boot|selbstheilung laeuft nicht|skript da unit fehlt|ausrollweg gruen aber fehlt"
    sev: high
    source: "31.08.2026 am Baum nachgemessen (tools/ausrollweg-deckung.py, erweitert um die fuenfte Etage), Rezepte und beide Schalen-Wege gegengelesen"
    tags: [installer, rezepte, soloist, spotify, systemd, wachen, 2026]
    related: [ohne-linger-kein-ton-auf-frischer-box, bt-skripte-lagen-nie-im-baum, rezeptschleife-nennt-acht-von-siebzehn]
    symptom: >-
      Eine ueber den remote-step-installer gebaute Box bleibt nach dem Start
      bei Spotify stumm und heilt sich nie. `/usr/local/bin/mupibox/soloist-
      anmeldewache.sh` liegt da und laeuft von Hand aufgerufen richtig —
      `systemctl status soloist-anmeldewache.timer` kennt die Unit nicht.
    body: |
      DIE E104-ANMELDEWACHE ist die Selbstheilung gegen den nach einem
      Boot-Anmeldefehler stummen Soloist: ein oneshot, den ein Timer alle zwei
      Minuten faehrt (90 s nach dem Start das erste Mal). Sie schliesst die
      Luecke „Box bootet, Spotify bleibt den ganzen Abend stumm" von selbst.

      `autosetup.sh` legt Skript, Unit und Timer und schaltet ihn ein.
      `update/start_mupibox_update.sh` ebenso. DAS REZEPT DES
      REMOTE-STEP-INSTALLER NANNTE SIE NICHT — und das ist der Weg, ueber den
      heute frische Karten entstehen. Der Schritt `soloist` legte
      soloist.service, soloist-updater.service und dessen Timer, nur die
      Anmeldewache fehlte in der Aufzaehlung. Wieder eine Aufzaehlung, die dem
      fehlenden Eintrag die Glaubwuerdigkeit seiner Nachbarn leiht.

      BESONDERS TUECKISCH SEIT DEM 31.08.2026: an diesem Tag kam
      `scripts/soloist/` in die Kopierschleife der Sammelkopie
      ([[rezeptschleife-nennt-acht-von-siebzehn]]). Seither kommt das SKRIPT
      auf jede frische Box, der TAKT nicht. Vorher fehlte sichtbar alles,
      seither liegt die halbe Sache da — und eine halbe Selbstheilung sieht
      von aussen aus wie eine ganze.

      WARUM KEINE WACHE ES FAND, und das ist der eigentliche Fund:
      `tools/ausrollweg-deckung.py` nannte in seiner Konstante `WEGE`
      ausdruecklich „DIE ZWEI WEGE, DIE EINE KARTE BAUEN" — autosetup.sh und
      den Update-Weg. Das Installer-Rezept stand in `WEGE_NUR_DECKUNG`, also
      NUR bei der Frage „kommt die Datei irgendwo an?", ausgenommen von der
      Regel „angelegt ist nicht eingeschaltet". Die Tabelle zeigte brav
      „rollt aus / rollt aus", das Werkzeug wurde gruen. Der Satz in `WEGE`
      war einmal richtig und ist mit dem Installer still falsch geworden:
      DIE UEBERSCHRIFT EINER WACHE IST EINE ZUSTANDSAUSSAGE UND ALTERT.

      Dieselbe Kette hat an einem einzigen Tag viermal zugeschlagen: die vier
      Bluetooth-Skripte, die Sammelkopie mit 8 von 17 Verzeichnissen,
      `loginctl enable-linger` — und diese Unit. Alle vier fehlten GENAU auf
      dem Rezeptweg, alle vier fand ein Mensch. Das ist kein Zufall mehr,
      sondern ein Ablauffehler: die Wachen massen den Weg von gestern.

      BEHOBEN in zwei Haelften, und beide gehoeren zusammen:
      * Der Schritt `soloist` (recipes/mupibox-app.yaml) legt Skript, Unit und
        Timer und schaltet den Timer ein — wortgleich zu dem, was die beiden
        Schalen-Wege laengst tun.
      * `ausrollweg-deckung.py` hat eine FUENFTE ETAGE: jede Unit muss auch
        vom Rezeptweg gedeckt sein. Gemessen wird dabei am ZIEL
        (`/etc/systemd/system/<name>`), nicht am Quellpfad — sonst meldet die
        Wache `mupibox-bt-reconnect.timer` als Luecke, die keine ist (das
        Rezept nimmt sie aus `remote-step-installer/tools/`, inhaltsgleich
        nachgemessen). Die eine Ausnahme steht im Werkzeug samt Messung: das
        Rezept kopiert `config/services` PAUSCHAL, aber nur `*.service` —
        `.timer` und `.path` von dort muessen einzeln benannt sein, und genau
        daran hing schon E29.
  - id: anzeige-und-ton-muessen-dieselbe-quelle-waehlen
    kind: gotcha
    title: "Zwei Wahrheiten ueber ein Werk: /inhalt nahm die vollste Quelle, der Ton die erste der Reihenfolge"
    match: "falscher titel beim abspielen|nicht alle titel|track1 fehlt|springt auf fremdes cover|totalTracks stimmt nicht|halber mitschnitt spielt|E110|zwei wahrheiten werk"
    sev: high
    cause: "Betreiber am Geraet (31.08.2026): 'es springt wenn ich mission erde spiele ploetzlich auf redhot chillipeppers', 'nicht alle titel da', 'track1 fehlt'. EINE Ursache fuer alle drei: 'Mission Erde - Das Fest der Sprachen' spielte LOKAL mit totalTracks 6 (die m3u fuehrt 01,02,06,08,11,12 - ein halber Mitschnitt), waehrend die Lane die SPOTIFY-Liste mit 14 Titeln zeigte. Jede Warteschlangennummer bedeutete auf beiden Seiten etwas anderes: mpv-Position 3 ist die lokale Spur '06 Bunte Socken', die Anzeige zeigte dazu Spotify-Titel 3 - falscher Name, falsches Bild, scheinbar fehlende Titel. Die Inkonsistenz lag im eigenen Haus: waehleInhalt waehlt seit E95 Stufe 1 die VOLLSTE Quelle (gebaut fuer genau diesen Fall), versuchsQuellen nahm aber stur die erste der Reihenfolge"
    fix: "In POST /api/spielen: bei echter Wahl (verschmolzen, mehrere Quellen, bevorzugte laeuft ueber mpv) holt der Server die Kandidaten-Inhalte ueber dieselbe Funktion (inhaltFuerEintrag) und spielt den waehleInhalt-Sieger; der Rest bleibt Rueckfall in Reihenfolge, jeder Fehler faellt auf die Reihenfolge zurueck. Kostet nur bei Werken MIT lokaler Schwester einen Abruf. Geraetebeweis: vorher dienst=lokal/totalTracks=6, nachher dienst=spotify mit voller Liste. Selbstheilung inklusive - sobald der Mitschnitt komplett ist, hat lokal die meisten Titel und gewinnt von selbst. GRENZE: ein Album mit lokalen Luecken spielt damit GANZ aus der Cloud, nicht titelweise gemischt - echtes Mischen ueber die Spotify-Grenze braucht den EINEN Wechselpunkt (E108 Stufe 3), weil jeder Maschinenwechsel 1-2 s Stille kostet; innerhalb der mpv-Familie (lokal<->jellyfin) mischt E108 Stufe 1 bereits titelweise"
    tags: [e110, verschmelzung, server, spielfunktion, 2026]
    related: [der-titel-ist-das-kleinste-teil, quellen-verschmelzen, dienste-umschalten]
    body: |
      ══ DIE REGEL, DIE DARAUS FOLGT ══════════════════════════════════════════
      Wo zwei Wege dieselbe Frage beantworten (WELCHE QUELLE?), muessen sie
      dieselbe Funktion rufen - nicht dieselbe Absicht haben. Anzeige und Ton
      driften sonst genau dann auseinander, wenn eine Quelle unvollstaendig
      ist, und das ist bei einem mitwachsenden Mitschnitt der Normalfall,
      nicht die Ausnahme.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
      curl -X POST .../api/spielen -d '{"schluessel":"<verschmolzenes Werk>"}'
      -> Antwortfeld `dienst` gegen /player/local `totalTracks` und gegen
      GET /api/werke/<s>/inhalt (Titelzahl) halten. Stimmen die drei nicht
      ueberein, ist dieser Fehler zurueck.
  - id: lokaler-ton-ueber-die-liste-nicht-ueber-die-m3u
    kind: gotcha
    title: "Der Ton spielte die m3u, die Anzeige den Ordner - zwei Listen derselben Quelle"
    match: "track1 fehlt|spielt immer noch nur spotify|m3u hinkt nach|musicsearch spielt andere liste|lokale titelliste stimmt nicht|playlist.m3u unvollstaendig|E111|datei statt musicsearch"
    sev: high
    cause: "Betreiber am Geraet (31.08.2026, nach E110): 'ich hab das gefuehl es spielt immer noch nur spotify wenn da', weiter 'titel -> track2 oder 3, track1 fehlt'. Der lokale Bestand wurde an ZWEI Stellen aus der playlist.m3u gelesen, und die hinkt der Aufnahme nach: 'Guten Morgen' hatte DREI Dateien im Ordner, aber nur ZWEI in der m3u - die Aufnahme legt die Datei ab, die m3u zieht spaeter nach. Folge 1: der Titelzahl-Vergleich (waehleInhalt) sah lokal=2 gegen spotify=3 und gab der Cloud recht, obwohl das Album lokal VOLLSTAENDIG dalag; auch die E110-Gegenprobe je Titel half nicht, weil ihre Soll-Liste aus der m3u kam. Folge 2: startPlan schickte fuer lokale Werke den EINEN Befehl musicsearch/library/album/<kat>:<interpret>:<titel>, den der Abspieldienst ueber die m3u aufloest - also eine ANDERE Liste als die angezeigte, und jede Warteschlangennummer bedeutete auf beiden Seiten etwas anderes. Derselbe Fehler wie E110, eine Ebene tiefer: dort standen zwei QUELLEN gegeneinander, hier zwei LISTEN derselben Quelle"
    fix: "Das VERZEICHNIS ist die Wahrheit ueber den lokalen Bestand - dieselbe Karte (lokaleSpurenKarte, readdir + mtime-Cache), die schon die Herkunfts-Haekchen von E108 speist. inhaltFuerEintrag baut die lokale Titelliste daraus, je Titel mit eigenem datei- und dateiqueue-Befehl. startPlan gibt fuer die lokale Quelle deshalb art:'inhalt' zurueck statt art:'befehl' - derselbe Weg wie Jellyfin-Album, ARD und Plugin: Liste holen, ersten Titel starten, Rest anhaengen, Sprung nachziehen. PFLICHT-ABSICHERUNG: startPlan gibt neben art:'inhalt' ein Feld rueckfall mit, den alten musicsearch/library-Befehl Zeichen fuer Zeichen; spielVersuch spielt ihn, sobald die Liste keine Titel mit Befehl hergibt (der m3u-Weg liefert Titel OHNE Befehl, weil die m3u nur Dateinamen kennt). Ohne ihn verstummte jede Box, deren Medien woanders haengen. Nur lokal bringt einen Rueckfall mit - Jellyfin, ARD und Plugin haben keinen Ein-Befehl-Weg, ein leerer waere ein Start ins Nichts"
    tags: [e111, spielfunktion, server, lokal, mitschnitt, 2026]
    related: [anzeige-und-ton-muessen-dieselbe-quelle-waehlen, der-titel-ist-das-kleinste-teil, datei-verben-spielen-eine-spur-mit-zaun]
    body: |
      ══ DIE REGEL, DIE DARAUS FOLGT ══════════════════════════════════════════
      Eine abgeleitete Datei ist keine Zeugin fuer den Bestand. Die
      playlist.m3u wird von der Aufnahme geschrieben, aber SPAETER als die
      Tondatei - wer sie fragt 'welche Titel gibt es', bekommt die Antwort von
      vorhin. Fuer 'was liegt da' gilt das Verzeichnis; die m3u bleibt der
      Abspiel-Vertrag des klassischen musicsearch-Wegs, den die alte
      Oberflaeche weiter benutzt.

      Und: ein Umbau, der einen funktionierenden Weg ersetzt, braucht diesen
      Weg als Rueckfall - nicht aus Hoeflichkeit, sondern weil die neue
      Vorbedingung (ein lesbarer Ordner unter MUPIBOX_MEDIA_DIR) auf fremden
      Boxen nicht gilt. Der Rueckfall greift NUR vor dem ersten gesendeten
      Befehl; danach waere er ein zweiter Start auf einen laufenden, und den
      hoert man.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
      Am Geraet, ohne den Ton zu aendern, nur mitlesen:
        journalctl -u mupibox-backend-api -f | grep -E 'E111|/current/'
      Beim Antippen eines lokalen Werks muss der Startbefehl `datei/…` heissen
      und NICHT `musicsearch/…`; der Rest der Liste kommt als `dateiqueue/…`
      hinterher. Kommt `musicsearch/…`, hat der Rueckfall gegriffen - dann
      steht darueber die E111-Zeile mit dem Grund (kein-eintrag, inhalt-<code>
      oder keine-titel), und der Ordner unter MUPIBOX_MEDIA_DIR ist zu pruefen.

      Auf dem Rechner: src/backend-api/src/spielen.integration.spec.ts legt
      echte Dateien in einem tmp-Medienordner an und misst genau diese drei
      Aussagen gegen den Abspieldienst-Fake.

      ══ WAS OFFEN BLEIBT ═════════════════════════════════════════════════════
      folgeWaehlen nimmt die Titelnummer als INDEX in die Liste (nr - 1). Das
      stimmt, solange die lokale Liste lueckenlos bei 1 beginnt. Bei einem
      Halb-Mitschnitt (Spuren 01, 02, 06, …) stimmt es nicht - dort greift
      heute die GANZ-oder-GAR-NICHT-Regel aus E110 und das Album kommt aus der
      Cloud. Mit der titelweisen Mischliste (E108 Stufe 3) muss der Sprung
      ueber `nr` gehen statt ueber die Position.
  - id: wache-las-nur-prosa-die-anweisung-stand-im-print
    kind: gotcha
    title: "Die Doku-Wachen lasen nur .md - die falsche Anleitung stand in einem print()"
    match: "toter schalter|unrecognized arguments|option gibt es nicht mehr|doku-luecken-probe meldet keine luecke|anleitung im quelltext|zitierte-schalter"
    sev: med
    cause: "Doku-Lauf 31.08.2026 (achter): tools/doku-luecken-probe.sh meldete KEINE LUECKE und lag fuer ihren Korpus richtig - der Korpus war das Problem. sdstart.py:1467 riet dem Betreiber 'Anlegen mit tools/paketbuendel-bauen.py --box <adresse>', und --box gab es seit Commit eb9af74f nicht mehr. Der Satz stand in einem _sagen()-Aufruf, also in einer .py-Datei, und zitierte-schalter-pruefen.py las ausschliesslich .md. Verschaerfend: der Satz erschien GENAU DANN, wenn kein Buendel dalag - dem Leser beim ersten Aufsetzen, im Moment des Bedarfs, und argparse haette ihm den Aufruf abgebrochen. Zusaetzlich riet er zu einer Box, die dieser Leser per Definition nicht hat - genau die Annahme, die eb9af74f entfernt hatte"
    fix: "Korpus von zitierte-schalter-pruefen.py um .py, .sh und .mjs erweitert (99 -> 1183 gepruefte Paare). Damit das nicht dauerrot wird, mussten zwei Fehlalarm-Bauarten raus: (1) das Ziel ist EINGABE statt Befehl - 'esbuild src/server.ts --bundle' laesst die Schalter esbuild gehoeren; dagegen ist_lauffaehig(), das Shebang ODER Ausfuehrbit ODER argv-Lesen fordert (drei Merkmale oder-verknuepft, weil jedes einzeln Loecher hat: ein Edit nimmt das Ausfuehrbit weg, .mjs-Werkzeuge haben nie einen Shebang). (2) der Schalter gehoert einem ZITIERTEN FREMDBEFEHL - \"rsi.py exec 'dpkg --configure -a'\" ist dpkgs Schalter auf der Box; dagegen ohne_fremdbefehl(), das ab dem ersten Anfuehrungszeichen abschneidet. Die Wache nimmt ihre eigene Datei aus dem Korpus, weil ihr Sabotage-Text absichtlich einen toten Schalter zitiert"
    tags: [doku, wache, methodik, quelltext, installer, sdstart, 2026]
    related: [verweis-wache-las-nur-verfolgtes-und-verfehlte-die-neue-datei, oberflaechen-wache-ist-innerhalb-der-oberflaeche-blind, dauerrote-wache-ist-keine, gegenprobe-statt-gruen-glauben, bauanleitung-ist-eine-vierte-sorte-datei]
    body: |
      ══ DIE REGEL, DIE DARAUS FOLGT ══════════════════════════════════════════
      EINE HANDLUNGSANWEISUNG IST DOKU, EGAL IN WELCHER DATEI SIE STEHT. Ein
      `print()` mit einem Befehl darin ist naeher am Leser als jede README -
      er sieht es im Moment des Bedarfs, nicht beim Nachschlagen. Eine
      Doku-Wache, die nur Prosadateien liest, laesst genau die Sorte
      Anweisung ungeprueft, der am ehesten gefolgt wird.

      Das reiht sich in eine Familie: `verweis-wache-las-nur-verfolgtes-…`
      (untracked war unsichtbar), `oberflaechen-wache-ist-innerhalb-der-
      oberflaeche-blind`. Immer dieselbe Bauart - die Wache ist nicht falsch,
      ihr KORPUS ist zu eng, und sie meldet gruen statt „ungeprueft".

      ══ EIN VERWORFENER WEG, DAMIT IHN NIEMAND WIEDER NIMMT ══════════════════
      Bauart (1) laesst sich scheinbar eleganter ueber die BEFEHLSPOSITION
      erschlagen: „steht vor dem Ziel schon ein Befehlsname, ist das Ziel sein
      Argument". Gemessen raeumte das alle 11 Fehlalarme ab - und verschluckte
      den ECHTEN Fund gleich mit. Vor `tools/paketbuendel-bauen.py` steht in
      jenem Satz das deutsche Wort „mit", und das sieht aus wie ein
      Befehlsname. Prosa und Shell teilen sich diese Achse; sie taugt nicht
      zum Trennen. Die Sorte des ZIELS taugt, die Umgebung des Ziels nicht.

      Ebenso verworfen: `\bshift\b` und `$1` als Beleg dafuer, dass eine Datei
      Argumente liest. `.shift()` ist eine Array-Methode - damit galt
      `server.ts` prompt wieder als lauffaehig, samt aller sechs Fehlalarme.
      Es blieben `process.argv`, `sys.argv`, `argparse`, `getopts`.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
      python3 tools/zitierte-schalter-pruefen.py             # erwartet: KEINE LUECKE
      python3 tools/zitierte-schalter-pruefen.py --sabotage  # MUSS rot werden
      Die Bilanzzeile lesen, nicht den Exitcode hinter einer Pipe - der
      gehoert dann `tail`.
  - id: gelesene-variable-ohne-schreibseite
    kind: falle
    sev: medium
    title: "E113: --mupi-cover-f und --mupi-titel-f wurden seit dem ersten Tag gelesen und von niemandem geschrieben - zwei Groessen ohne Bedienelement"
    match: "cover groesse einstellen|titel groesse|mupi-cover-f|mupi-titel-f|skalen.cover|skalen.titel|coverGroesse|titelGroesse|Groesse des Covers|Groesse der Namen|albumbild groesser|schrift unter den kacheln|regler fehlt im menue"
    cause: >
      In NewDesign/app.js setzt `anwenden()` die zwei CSS-Variablen
      `--mupi-titel-f` und `--mupi-cover-f` aus `w.skalen`. Geschrieben hat
      `skalen` in dieser Oberflaeche NIEMAND - es gab keine Zeile dafuer.
      Der Leseweg war vollstaendig, gruen und wirkungslos; gemeldet hat es
      der Betreiber, der die Cover-Groesse im Menue suchte und nicht fand.
      Das ist die Gegenrichtung zu E23 "Falle 1" (Schalter, dessen Variable
      niemand verbraucht): hier verbraucht die CSS sie, nur stellt sie keiner.
      KEINE Wache konnte das sehen - `mupi-variablen-abgleich.mjs` prueft, ob
      BEIDE Oberflaechen dieselbe Variable setzen, und beide taten es.
    fix: >
      Zwei Weiterschalt-Regler in ANZEIGE_SCHALTER (31.08.2026), baugleich zu
      den Wellen-Reglern: drei Stufen, Stufe 2 = Faktor 1 = exakt der Stand
      davor, kaputte Werte fallen auf die Vorgabe. Cover 0,85/1,0/1,3 -
      "Groesse des Covers" unter Darstellung -> Player; Titel 0,85/1,0/1,25 -
      "Groesse der Namen" unter Darstellung -> Indikatoren. Wache:
      tools/groessen-regler-schau.mjs (im Pruefen-Laeufer als "Groessen-Regler
      wirken"). Attrappe: `skalen: {}` in DARSTELLUNG.aktuell von
      tools/neu-vorschau.mjs.
    tags: [darstellung, regler, e113, css-variable, wachen, newdesign, doku, 2026]
    source: "Betreiber 31.08.2026: suchte die Cover-Groesse im Menue und fand sie nicht. Gebaut und gegen die eigene Vorschau (Port 8383) gemessen; die Box wurde nicht angefasst."
    related: [wellen-regler-wirken-ueber-zwei-naehte, drei-orte-eine-anzeige, bedienschalter-hat-keine-doku-gegenrichtung, gegenprobe-statt-gruen-glauben]
    body: |
      ══ GESCHRIEBEN WIRD IN `skalen`, NICHT IN EIN NEUES FELD ════════════════

      `skalen.cover` ist der Ort, an dem der Faktor seit jeher wohnt - die alte
      Oberflaeche (darstellung.service.ts:886) und die Verwaltung
      (darstellung.ts:2967) lesen und schreiben ihn dort. Ein eigenes Feld
      daneben waere [[drei-orte-eine-anzeige]] noch einmal.

      `skalen` ist aber ein UNTEROBJEKT, und der Schreibweg der Optik-Liste
      (`darstSetzen(id, wert)`) setzt genau EIN Feld der obersten Ebene. Die
      zwei Zeilen tragen deshalb `felder(stand, w)` statt `weiter(stand)`: sie
      liefern den ganzen Satz und gehen ueber `darstFelderSetzen` - dieselbe
      Naht, die die Wischgesten schon benutzen. `id` ist bei ihnen nur die
      Marke fuer "gerade beschaeftigt" und KEIN Feldname in darstellung.json.
      Fremde Schluessel (`fav`, `res`, `status` …) werden aus dem gelesenen
      Stand mitgenommen; ein Regler, der `skalen` ersetzte statt zu ergaenzen,
      raeumte beim Cover-Tippen lautlos die anderen Groessen mit ab.

      ══ WO 1,3 HERKOMMT (gerechnet, nicht geraten) ═══════════════════════════

      app.css rechnet am `.gross-bild`:

        --gr-kante: min(260px, calc(200px * var(--mupi-cover-f, 1)))

      200 * 1,3 = 260, also GENAU der Deckel. Eine vierte Stufe darueber waere
      ein Knopf, der sich bewegt und nichts tut - die schlimmere Sorte kaputt,
      weil sie sich nicht meldet.

      ══ DIESELBE VARIABLE, ZWEI BEDEUTUNGEN ══════════════════════════════════

      `--mupi-titel-f` haengt in der NEUEN Oberflaeche an `.kachel-titel` und
      `.kachel-int` (app.css:2193, :2201) - an den Namen UNTER DEN KACHELN. In
      der alten Oberflaeche (player.page.scss:335) skaliert dieselbe Variable
      den TITEL IM GROSSEN PLAYER; der steht in NewDesign mit festen 26 px da.
      Die Verwaltung beschriftet den Regler entsprechend "Titel im Player"
      (darstellung.ts:2946) - fuer die neue Oberflaeche ist das falsch.

      Der neue Regler heisst deshalb "Groesse der Namen", nach dem, was er
      HIER bewirkt, und die Unstimmigkeit wurde NICHT nebenbei umgebogen: wer
      sie aufloest, muss entscheiden, welche der beiden Bedeutungen gewinnt,
      und das ist kein Nebenbei-Schritt. Steht als Restposten in BACKLOG.md.

      ══ WARUM DIE WACHE ZWEI HAELFTEN HAT ════════════════════════════════════

      Die naheliegende Wache stellt je Stufe per PUT und misst die Wirkung.
      Die haette am Baum VOR E113 GENAUSO GRUEN gemessen - der Leseweg stand
      ja. Sie bewachte also nichts von der Arbeit. Darum misst
      tools/groessen-regler-schau.mjs beides:

        Teil 1  PUT je Stufe -> `--mupi-cover-f` UND die Kante von
                `.gross-bild` (170/200/260 px, der Player wird dafuer im
                Browser aufgedeckt) UND die Schriftgroesse von `.kachel-titel`
                (12,75/15/18,75 px). Dazu der Rueckfall ohne `skalen`.
        Teil 2  Durch die OBERFLAECHE: adminAuf(), Darstellung -> Player bzw.
                Indikatoren, Zeile ueber ihr aria-label finden, Knopf
                druecken, danach am Server nachsehen, was in `skalen` steht -
                samt dem fremden `fav: 1.4`, das ueberleben muss.

      GEGENPROBE (beide Haelften, auf committetem Stand gesichert und per
      Kopie zurueckgebaut - kein `git checkout` auf uncommittete Arbeit):
      `s.setProperty('--mupi-cover-f', …)` entfernt -> 5 Meldungen, Ende 1.
      Die zwei Feldeintraege auf eine Seite gelegt, die es nicht gibt -> 8
      Meldungen, Ende 1. Nach dem Rueckbau beide Male gruen.

      ══ ZWEI DOKU-WACHEN WAREN AUF NewDesign BLIND ═══════════════════════════

      Der Handbuch-Absatz zu den zwei Reglern machte im selben Lauf zwei
      Wachen rot, und beide Male lag es NICHT am Absatz:

        beschriftungen-deckung.py  suchte die Box-Oberflaeche nur in
                                   `src/frontend-box/src` und kannte
                                   `NewDesign/` nicht. Jede dort zitierte
                                   Beschriftung galt als ERFUNDEN - der
                                   schaerfste ihrer drei Befunde. Behoben:
                                   BOX ist jetzt ein Paar von Ordnern, `.js`
                                   gehoert zu den Endungen (eingegrenzt auf
                                   NewDesign, NICHT auf das Kompilat in
                                   `src/frontend-box/www`).

        darstellungs-pfade-pruefen.py  las die Reiternamen nur aus
                                   `darstellung.ts`. Die Unterseiten der
                                   Box-Darstellung (Indikatoren, Farbe und
                                   Form, Player, Verhalten) stehen in
                                   NewDesign/app.js; "Darstellung ->
                                   Indikatoren" war deshalb ein erfundener
                                   Reiter. Behoben: die Namen werden aus der
                                   Quelle gelesen (`gruppe: 'anzeige'`) und
                                   als VEREINIGUNG mit den Reitern der
                                   Verwaltung gelten gelassen - keine Weiche
                                   am Wort "Eltern-Bereich", denn der
                                   Gruendungsfall dieser Wache benutzte
                                   dieselbe Einleitung fuer die VERWALTUNG.

      Beide Anpassungen gegengeprobt: ein erfundener Knopfname und ein
      erfundener Reitername fallen weiterhin auf.

      LEHRE: Wer in NewDesign einen Schalter baut und ihn ins Betriebshandbuch
      schreibt (die Regel aus [[bedienschalter-hat-keine-doku-gegenrichtung]]),
      trifft auf Doku-Wachen, die den Ordner nicht kennen. Der erste Reflex -
      den Satz umformulieren, bis die Wache schweigt - macht die Doku falsch
      und die Wache dauerblind. Der Pfad gehoert in die Wache, nicht aus dem
      Satz heraus.
  - id: paketrelativer-pfad-im-zitat-sieht-aus-wie-umzugsschuld
    kind: gotcha
    sev: low
    title: "Ein zitiertes npm-Skript traegt PAKETRELATIVE Pfade - die Pfad-Wache liest sie als mehrdeutig"
    match: "MEHRDEUTIG: src/server.ts|doku-pfade-pruefen meldet mehrdeutig|esbuild src/server.ts|paketrelativ|pfad im npm-skript|umzugsschuld falsch gemeldet"
    cause: >
      Doku-Lauf 31.08.2026 (neunter): tools/doku-pfade-pruefen.py meldete
      genau EINE Luecke - `src/server.ts` als MEHRDEUTIG (aufloesbar nur als
      `src/backend-api/src/server.ts`), genannt im Eintrag
      wache-las-nur-prosa-die-anweisung-stand-im-print. Das sah aus wie die
      Umzugs-Schuld vom 10.08. (ein Pfad aus der Zeit vor der Aufteilung in
      backend-api/backend-player/frontend-*), war aber keine: der Eintrag
      ZITIERT dort woertlich das Bau-Skript aus `src/backend-api/package.json`
      Zeile 8 (`esbuild src/server.ts --bundle …`). npm laeuft im
      PAKETORDNER; dort ist `src/server.ts` der richtige und einzig
      zutreffende Pfad.
    fix: >
      AUSNAHMEN_JE_QUELLE in tools/doku-pfade-pruefen.py um genau dieses Paar
      (Eintrag + `src/server.ts`) erweitert, mit dem Grund daneben. Nicht als
      Regel, weil in Fliesstext - anders als bei CONTAINER_RELATIV in einer
      docker-compose.yml - kein Schluessel davorsteht, an dem die
      Paketrelativitaet erkennbar waere. Gegenprobe gefahren: dieselbe
      Zeichenfolge testweise in README.md gelegt, Wache meldet sie prompt
      wieder ("genannt in README.md"), README danach aus der Sicherung
      zurueckgelegt.
    tags: [doku, wache, methodik, pfade, npm, monorepo, 2026]
    related:
      - doku-zeigt-auf-dateien-die-es-nicht-gibt
      - wache-las-nur-prosa-die-anweisung-stand-im-print
      - dauerrote-wache-ist-keine
      - wache-liest-sieben-von-neunundvierzig-prosadateien
      - pfad-in-die-bauablage-ist-nie-im-baum
    source: "31.08.2026, Doku-Lauf. Erhoben an tools/doku-pfade-pruefen.py, llmwiki/pack.yaml und src/backend-api/package.json:8. Kein Geraet angefasst."
    body: |
      ══ DIE REGEL ═══════════════════════════════════════════════════════════
      IN EINEM MONOREPO HAT EIN PFAD ZWEI WAHRHEITEN, UND DAS ZITAT TRAEGT DIE
      DES ZITIERTEN. Was in `src/backend-api/package.json` steht, gilt vom
      Paketordner aus; wer es in die Doku hebt, hebt die Paketrelativitaet
      mit. Eine Wache, die jeden Pfad von der Wurzel aus aufloest, liest
      solche Zitate als Umzugs-Schuld - richtig gerechnet, falsche Annahme.

      ══ WARUM DAS ZITAT NICHT VERLAENGERT WURDE ═════════════════════════════
      Der naheliegende Griff waere, im Eintrag `src/backend-api/src/server.ts`
      zu schreiben; dann ist die Wache still, ohne dass jemand eine Ausnahme
      pflegt. Das waere die schlechtere Reparatur: das Zitat existiert, damit
      der Leser den Satz in der package.json WIEDERFINDET. Ein Zitat, das
      nirgends mehr woertlich vorkommt, ist keins.

      Umgekehrt hat der ANDERE Eintrag zur selben Zeile
      (`doku-zeigt-auf-dateien-die-es-nicht-gibt`) den langen Pfad zu Recht -
      er redet ueber den Ort im Baum, nicht ueber die Zeile im Skript.
      Derselbe Text, zwei Bedeutungen, und die Ausnahme haengt an der
      Bedeutung.

      ══ WIE MAN ES NACHMISST ════════════════════════════════════════════════
      python3 tools/doku-pfade-pruefen.py       # erwartet: KEINE LUECKE
      Gegenprobe (das Loch darf nur diese eine Quelle decken):

        cp README.md /tmp/README.bak
        printf '\n<!-- Gegenprobe: src/server.ts -->\n' >> README.md
        python3 tools/doku-pfade-pruefen.py     # MUSS "genannt in README.md" melden
        cp /tmp/README.bak README.md

      Die Sicherung gehoert in DENSELBEN Befehlsblock wie die Sabotage -
      der Erfolg der Probe ist der Schaden.
  - id: spotify-cue-link-trennen-statt-node-muten
    kind: entscheidung
    title: "Das PipeWire-Cue trennt den LINK, es mutet nicht den Knoten"
    sev: med
    tags: [pipewire, spotify, cue, mitschnitt, klangwerk, abspieler, pw-link, 2026]
    source: "31.08.2026, BACKLOG E114"
    related:
      - mitschnitt-nahm-den-lautsprecher-auf
      - klangwerk-senke-nimmt-keine-lautstaerke
      - dienstwechsel-und-verstummen-messen
      - spotify-librespot-architektur
      - mupi-pipewire-umstieg
    body: |
      Der Maschinenwechsel mpv -> Spotify kostet 1-2 s Stille. Der Ausweg ist,
      den Spotify-Pfad im Wechselmoment stumm zu legen: Titel im Stummen
      starten, puffern, pausieren, auf 0 springen, erst dann hoerbar machen.
      Alle riskanten Befehle liegen dann in einem Fenster, in dem niemand
      zuhoert.

      ══ DIE KARTIERUNG (am Geraet, bei SPIELENDEM Spotify) ═════════════════

          spotify (Stream/Output/Audio, application.name=Spotify)
              --> klangwerk --> klangwerk.ausgang --> alsa_output…
          entzerrer.ausgang --> klangwerk        [der mpv-Weg]
          spotify --> mixpi-mitschnitt           [die Aufnahme zapft VOR
                                                  dem Klangwerk ab]
          alsa_output --> mixpi-pegel

      Erst diese Karte macht das Vorhaben moeglich: beide Maschinen laufen bis
      zum Klangwerk GETRENNT, der Spotify-Zweig ist einzeln schaltbar.
      ACHTUNG BEIM NACHMESSEN: der Knoten existiert NUR bei laufender
      Wiedergabe. Eine Messung bei pausiertem Spotify zeigt ihn nicht und
      fuehrt zu dem Schluss, die Maschine speise gar nicht ueber PipeWire ein
      — genau dieser Irrtum hat am 31.08. einen halben Tag gekostet.

      ══ DIE ENTSCHEIDUNG: pw-link -d STATT wpctl set-mute ══════════════════

      `wpctl set-mute <spotify-node> 1` wirkt, live geprueft: /pegelstrom
      meldet [0,0,0,0], `set-mute 0` bringt den Ton zurueck, kein Knacken. Es
      ist der bequemere Weg — und trotzdem der falsche.

      Mute wirkt AM KNOTEN und damit auf ALLE seine Abgriffe. Der Mitschnitt
      haengt aber VOR dem Klangwerk direkt am selben Knoten. Das Cue risse
      also ein stilles Loch in genau das Archiv, aus dem die Selbstheilung
      lebt (der Wechselpunkt wandert je Session nach hinten, bis das Album
      lokal liegt). Getrennt wird deshalb der LINK spotify->klangwerk
      (`pw-link -d <link-id>`); die Aufnahme schneidet im stummen Fenster
      weiter mit.

      ZURUECKVERBUNDEN WIRD UEBER DIE PORT-NUMMERN, nicht ueber die
      Link-Nummer: der Link ist nach dem Trennen als Objekt weg, seine Nummer
      bezeichnet nichts mehr. Die Ports gehoeren zu den Knoten und ueberleben.
      `pw-link <ausgang-port> <eingang-port>` — beide Nummern stehen in
      derselben pw-dump-Kante (`output-port-id`, `input-port-id`).

      ══ WIE MAN DEN KNOTEN FINDET, OHNE DEN FALSCHEN ZU TREFFEN ════════════

      DER RIEGEL IST DIE KLASSE, NICHT DER NAME: nur `Stream/Output/Audio`
      speist ein. Das schliesst die Senken aus und — teurer — die eigenen
      Aufnahmeknoten `mixpi-mitschnitt` und `mixpi-pegel`, die als
      `Stream/Input/Audio` laufen. Ein Cue, das versehentlich den Mitschnitt
      trennt, waere die teuerste denkbare Verwechslung.

      DREI NAMEN AUF DREI FELDERN: `spotify`, `librespot`, `soloist`, gesucht
      in `application.name`, `node.name` und `application.process.binary`.
      Welche Maschine laeuft, entscheidet `spotify.engine`; Soloists Knoten
      heisst ausgerechnet `spotify` (nicht `soloist`), librespot traegt meist
      `librespot`, kann seinen application.name aber aus dem Geraetenamen
      bilden. Ein einziger fest verdrahteter Name ist eine Wette darauf,
      welche Maschine der Betreiber gewaehlt hat.

      NUR PUNKT, LEERRAUM UND KLAMMER AUF SIND ALS ANHAENGSEL ERLAUBT
      (`spotify.1`, `Spotify (Kinderzimmer)`). Bindestrich und Unterstrich
      ausdruecklich NICHT: ein Knoten `spotify-mitschnitt` waere sonst eine
      Einspeisung. Im Zweifel findet das Cue nichts — das kostet die 1-2 s
      Stille, die es ohnehin gaebe — statt das Falsche zu treffen.

      ZWEI KANDIDATEN SIND KEIN RANDFALL: beide Soloist-Instanzen heissen
      `spotify` (siehe [[mitschnitt-nahm-den-lautsprecher-auf]], wo genau das
      vier stille Aufnahmen erzeugt hat), und Soloists Knoten BLEIBT in der
      Pause stehen, waehrend librespots verschwand. Entschieden wird deshalb
      ueber den GRAPHEN, nicht ueber den Namen: wer Kanten zur Senke hat,
      spielt. Fuehren zwei Kandidaten Ton, wird ausdruecklich NICHTS geplant.

      ══ WER SO ETWAS BAUT, BRAUCHT EINEN RUECKVERBINDE-ZWANG ═══════════════

      Der schlimmste Ausgang ist nicht ein misslungenes Cue, sondern eine Box,
      die STUMM BLEIBT, weil ein Cue haengengeblieben ist. Sie zeigt dabei
      „spielt", der Fortschritt laeuft, das Kind tippt, und niemand kommt
      darauf, dass eine Kante im Tongraphen fehlt.

      Drei Details machen aus einer Aufraeumzeile erst einen Zwang:

        a) ERST merken und Uhr spannen, DANN trennen. Umgekehrt gaebe es ein
           Fenster, in dem die Kante weg ist und niemand mehr weiss, dass sie
           zurueck muss. Der Preis ist ein `pw-link` auf eine nie getrennte
           Kante — es sagt „File exists" und tut nichts.
        b) DIE FRIST IST ABSOLUT, nicht gleitend. Ein zweites Stummschalten
           darf die Uhr nicht nach hinten schieben, sonst haelt eine Schleife
           die Box beliebig lange stumm — der Zwang waere genau in dem Fall
           unwirksam, fuer den es ihn gibt.
        c) DIE UHR WIRD NICHT `unref()`t. Ein unrefter Zeitgeber laesst Node
           den Prozess beenden, waehrend die Rueckverbindung noch aussteht;
           die Kante bliebe fuer immer getrennt.

      Gegen SIGKILL hilft nichts davon. Die Handreparatur steht im Protokoll:
      `pw-link <ausgang-port> <eingang-port>`, Nummern aus der Cue-Zeile.

      ══ WERKZEUG UND STAND ═════════════════════════════════════════════════

      `tools/cue-messen.mjs` (auf der Box lauffaehig, OHNE Schalter nur
      lesend): druckt Knoten, Kanten und den Plan. `--probe` erbringt den
      Geraetebeweis — Pegel, trennen, Pegel, zurueckverbinden, Pegel — und
      verweigert sich bei stillem Vorher-Pegel, weil eine Probe ohne Ton nur
      misst, dass Stille still bleibt. `--dump <datei>` liest eine
      gespeicherte pw-dump-Ausgabe; damit laeuft das Werkzeug gegen dieselbe
      Vorlage wie die Spec des Abspieldiensts
      (`src/backend-player/src/tonpfad.fixture.json`) — die Gegenstelle gegen
      das Auseinanderlaufen der doppelt gehaltenen Auswahlregeln.

      STAND 31.08.2026: `tonpfad.ts` (rein) und `cue-schalter.ts` (Naht, mit
      Schalter MIXPI_CUE=1 und Rueckverbinde-Zwang) stehen mit 31 Spec-Faellen.
      NICHT verdrahtet, kein Aufruf aus spotify-control.ts, KEIN Geraetebeweis
      — die Box war offline. Solange der fehlt, ist die Kartierung Wissen und
      das Cue eine Behauptung.
  - kind: howto
    id: gespeichertes-netz-braucht-nur-seine-kennung
    title: "Ein gespeichertes WLAN verbindet ueber seine Kennung — das Passwort noch einmal zu verlangen war ein Bau-, kein Sicherheitsgrund"
    tags: ['wlan', 'netzwerk', 'wpa_supplicant', 'verwaltung', 'eingabepruefung', 2026]
    source: "E115, gebaut und lokal geprueft am 31.08.2026 (Commit im Fork-Repo); Gerätebeweis steht aus"
    related: [wlan-im-assistenten-zwei-stufen, heikle-testfunktion-nicht-ueber-ssh-schalten, shell-string-mit-medienpfad-im-player, entscheidung-aus-dem-http-aufruf-herausloesen]
    body: |
      BETREIBER WOERTLICH (31.08.2026): „was wir unbedingt noch einbauen muessen
      ist ein 'netzwerk manager' das man bei gespeicherten verbindungen einfach
      verbinden klicken kann. das geht nicht ueber die box es ist immer ein
      passwort erforderlich".

      DER BEFUND, UND WARUM ER SICH LOHNT ZU MERKEN: das Passwort war nicht aus
      Vorsicht verlangt worden, sondern weil der einzige vorhandene Weg zufaellig
      einen NEUEN Eintrag anlegte. `POST /api/netzwerk/verbinden` ruft
      `add_network` und braucht dafuer zwangslaeufig ein `psk`. Die Box KANNTE
      das Netz die ganze Zeit — `parseListNetworks` las die gespeicherten Netze
      schon fuer die Scan-Liste (Feld `bekannt`), warf aber die KENNUNG weg,
      weil die Anzeige sie nicht brauchte.

      Das ist ein Muster, nicht ein Einzelfall: eine Auskunft wird gelesen, ein
      Feld davon als „brauchen wir nicht" verworfen, und Jahre spaeter sieht die
      fehlende Faehigkeit wie eine Sicherheitsentscheidung aus. Wer eine
      Eingabemaske findet, die etwas verlangt, was das System schon hat, sollte
      zuerst fragen, ob der Weg dorthin einfach nur fehlt.

      DER NEUE WEG (E115):
        GET  /api/netzwerk/gespeichert             id, ssid, aktuell, abgeschaltet
        POST /api/netzwerk/gespeichert/verbinden   {kennung}, KEIN Passwort
        POST /api/netzwerk/gespeichert/vergessen   {kennung}

      ══ DIE KENNUNG IST DER HEIKLE TEIL ═══════════════════════════════════
      `select_network` und `remove_network` kennen Netze AUSSCHLIESSLICH ueber
      eine Zahl. Genau diese Zahl schob `network.php:178` frueher per
      Zeichenkettenverkettung in die Shell. Der neue Weg geht ueber execFile mit
      Argument-Array — aber das allein loest es NICHT:

        * eine falsche Zahl loescht ohne jede Shell ein fremdes Netz;
        * `all` ist bei wpa_cli ein GUELTIGES WORT und taete etwas voellig
          anderes als bestellt.

      Die Regel lautet deshalb nicht „sieht wie eine Zahl aus", sondern:
      STEHT IN DER LISTE, DIE WIR EBEN SELBST VON wpa_supplicant GELESEN HABEN.
      Eine wohlgeformte Zahl, die es nicht gibt, wird HIER abgelehnt — nicht
      durchgereicht und von wpa_cli mit FAIL quittiert. Sonst faellt die
      Entscheidung in einem Fremdprozess statt an der Schranke.

      Und die Kennungen sind NICHT haltbar: nach `remove_network` plus Neustart
      vergibt wpa_supplicant die Nummern nach Dateireihenfolge neu. Eine im
      Browser gemerkte Zahl ist damit ein Loeschbefehl mit Verfallsdatum. Die
      Liste wird bei jedem Aufruf frisch gelesen, und die Oberflaeche holt sie
      nach jeder Tat neu.

      ══ DER VORRANG MUSS GERECHNET WERDEN ═════════════════════════════════
      `/api/netzwerk/verbinden` setzt jedem eingerichteten Netz fest
      `priority 10`. Wer zwei Netze ueber die Verwaltung angelegt hat, hat ZWEI
      Zehner — und zwischen genau denen will man umschalten. Eine feste Zahl
      waere dort ein GLEICHSTAND statt eines Vorrangs, und wpa_supplicant
      duerfte nach dem noetigen `enable_network all` zum staerkeren ALTEN Netz
      zurueckspringen. Also: vorhandene Prioritaeten lesen
      (`get_network <id> priority`, FAIL heisst 0) und strikt eine drauflegen.

      ══ DIE RUECKFALL-SICHERUNG IST DIE VORHANDENE ════════════════════════
      Kein zweiter Rueckweg — ein zweiter waere ein zweiter, den man falsch
      bauen kann. Von aussen wird derselbe Totmannschalter scharf gestellt, und
      alles bleibt im ARBEITSSPEICHER: kein `save_config`. Auf Platte steht bis
      zum Beweis der alte Stand, und DAS ist der Rueckweg — nicht ein spaeteres
      Rueckgaengigmachen. Danach prueft die Box selbst (`traegtDasNetz`:
      COMPLETED, richtige SSID, DHCP-Adresse) und schreibt erst dann fest.

      HIER IST DER RUECKFALL SOGAR BILLIGER als beim Passwort-Weg: es geht kein
      Geheimnis verloren. Das Passwort liegt schon auf Platte; zurueckgerollt
      wird die Verbindung, nicht die Zugangsdaten.

      VERGESSEN BRAUCHT KEINEN WECKER — und das ist keine Nachlaessigkeit,
      sondern eine Bedingung, die durchgesetzt wird: der Wecker schuetzt davor,
      dass ein Eingriff die Box vom Netz nimmt; ein Netz, in dem sie GERADE
      NICHT haengt, kann das nicht. Damit der Satz wahr BLEIBT, wird der Fall,
      in dem er falsch waere, mit 409 abgelehnt: das AKTUELLE Netz laesst sich
      nicht vergessen.

      ══ WIE MAN DEN FERNE-ZWEIG UEBERHAUPT TESTET ═════════════════════════
      Die Route unterscheidet „am Geraet" von „aus der Ferne" an
      `req.socket.remoteAddress` — absichtlich daran, weil es sich nicht
      vortaeuschen laesst. Genau deshalb kommt supertest IMMER als „am Geraet"
      an, und der einzige Zweig mit Totmannschalter waere ungeprueft geblieben.

      DER GRIFF: den Aufruf mit `localAddress: '127.0.0.2'` fahren (node:http).
      Das bindet die QUELLE der Verbindung. Ziel bleibt die Rueckschleife, es
      verlaesst nichts den Rechner — aber 127.0.0.2 faellt nicht unter die drei
      Adressen, die als „jemand steht davor" zaehlen. Kein LAN, keine
      Abhaengigkeit von vorhandener Hardware, und trotzdem der echte Zweig.

      Damit laesst sich die REIHENFOLGE beweisen, auf der die Sicherung beruht:
      erst Wecker scharf, dann waehlen, und VOR dem Beweis kein `save_config`.

      ══ WAS AM GERAET NOCH AUSSTEHT ═══════════════════════════════════════
      `save_config` scheitert STILL, wenn in wpa_supplicant.conf kein
      `update_config=1` steht — dann ist „vergessen" eine Anzeige ohne Wirkung,
      die bis zum naechsten Neustart wie ein Erfolg aussieht. Im Repo gesetzt
      wird es von remote-step-installer/agent/agent.py:207, von
      scripts/mupibox/add_wifi.sh:36 und in startup.sh:52 — letzteres NUR im
      Zweig „alles zuruecksetzen". Auf einer ueber den Installer aufgesetzten
      Box steht es also da; sicher ist es erst nach einem grep am Geraet. Der
      bestehende Passwort-Weg haengt an derselben Zeile — keine neue
      Abhaengigkeit, nur eine bisher nirgends aufgeschriebene.
  - id: die-kachel-zeigt-nicht-was-sie-tut-und-nicht-woher-sie-kommt
    kind: gotcha
    title: "Album-Kachel klappte nie auf, und die Herkunft je Titel lag ungenutzt im Server"
    match: "alben nicht aufklappen|titel sehen|gemischt lokal und spotify|album kachel spielt sofort|quelle quellen je titel|marke-nebenher|albumTitelLaneOeffnen|E112"
    sev: med
    cause: "Betreiber am Geraet (31.08.2026): 'ich kann auch die alben nicht mehr aufklappen und die titel sehen' und 'was ich auch irritierend finde das nicht alle titel da sind ich haette erwartet es gemischt zu sehen lokal und spotify'. ZWEI Ursachen, beide KEINE Regression. (1) Die Weiche in NewDesign/app.js kachelBauen lautete `if (istPlaylist || istArdSendung)` - nur Spotify-Playlisten und ARD-Sendungen klappten auf, ein Album spielte beim Tippen sofort. Seine Titel gab es nur ueber Umwege (Playlist -> Alben -> Titel, oder die dritte Ebene der Interpretenseite). Das war erklaerbar, solange Album- und Playlist-Kacheln verschieden AUSSAHEN; seit die Kacheln verschmolzen sind, sehen zwei gleiche Kacheln gleich aus und tun Verschiedenes. (2) Die Herkunft je Titel liefert der Server seit E108 mit (`quelle` und `quellen` an jedem Titel von /api/werke/<s>/inhalt, titelkarte.ts) - gelesen hat sie NUR die Plakette am Player-Cover, also erst waehrend etwas laeuft. Dort, wo man auswaehlt, stand nichts"
    fix: "Die Album-Kachel bekommt dieselbe Schale wie albenOeffnen/folgenOeffnen (albumTitelLaneOeffnen: EINE .lane per laneEinhaengen, in `lane` eingetragen, ein Tipp auf den Rueckweg macht sie zu) und teilt mit der dritten Ebene der Interpretenseite den INHALT: albumTitelOeffnen ist in albumStuecke() und albumTitelReiheBauen() zerlegt. Die vorhandene Funktion DIREKT zu rufen geht nicht - sie haengt ihre .lane-tief in einen Elternknoten, und im Raster landet sie damit entweder ganz unten (statt hinter der Reihe der Kachel) oder in einer leeren .lane, und dann braeuchte der EINE Rueckweg zwei Tipps fuer eine sichtbare Ebene (naechsterRueckweg kennt 'titelliste' und 'lane' getrennt). Die Herkunft zeigt markenBauen(dienste, gilt) mit neuem zweitem Argument: geltende Quelle kraeftig, weitere blass (.marke-nebenher, opacity 0.45); die Reihenfolge macht quellenGeordnet(quelle, quellen), das Vorlesewort quellenWort(). WER TIPPT WAS, entscheidet weiterhin albumTippVerdrahten (bedienung.albumTippSpielt) - die Raster-Kachel ist die DRITTE Bauart derselben Art und bekommt keine eigene Antwort"
    tags: [e112, neue-oberflaeche, app-js, kachel, titelliste, herkunft, quellen, barrierefreiheit, 2026]
    related: [der-titel-ist-das-kleinste-teil, lokaler-ton-ueber-die-liste-nicht-ueber-die-m3u, attrappe-luegt-durch-weglassen, quellen-verschmelzen, backticks-beenden-jede-vorlage]
    body: |
      ══ DIE REGEL, DIE DARAUS FOLGT ══════════════════════════════════════════
      Ein Feld, das der Server liefert und niemand zeigt, ist kein Fortschritt,
      sondern eine Zusage, die der Benutzer fuer gebrochen haelt. `quelle` und
      `quellen` standen seit E108 an JEDEM Titel; gelesen hat sie ein einziger
      Ort, und ausgerechnet der, der erst WAEHREND der Wiedergabe zaehlt. Wer
      eine Auskunft baut, gehoert verpflichtet zu sagen, WO man sie sieht -
      sonst wird sie beim naechsten Bericht ein zweites Mal gebaut.

      Und: zwei Kacheln, die gleich AUSSEHEN, muessen gleich REAGIEREN - oder
      vorher sagen, dass sie es nicht tun. Solange Album und Playlist
      verschiedene Kacheln waren, war 'die eine spielt, die andere klappt auf'
      unsichtbar richtig. Die Verschmelzung hat die Unterscheidung entfernt und
      das Verhalten stehenlassen; genau dieser Rest ist die Meldung.

      ══ WAS EIN AUFKLAPPBARER TIPP MITBRINGEN MUSS ═══════════════════════════
      Wer einen Tipp vom Ton wegnimmt, schuldet drei Dinge:
        1. den PLAY-KNOPF auf dem Cover (bildkasten.appendChild(spielKnopf(...))),
           sonst ist Musik einen Tipp weiter weg als vorher;
        2. den SATZ, der sagt was passiert (sprichDann: '…, Titel zeigen' statt
           '…, abspielen' - 'oeffnen' waere falsch, das sagen die Kacheln, hinter
           denen wieder KACHELN liegen);
        3. den EINSTELLUNGS-RESPEKT: wer bedienung.albumTippSpielt gestellt hat,
           will beim Tippen spielen. Dann faellt der Play-Knopf weg - ein zweites
           Ziel fuer dieselbe Tat verdeckt nur Cover.

      ══ DIE TEURE FOLGE: SECHS NACHBARWACHEN WURDEN ROT ══════════════════════
      Eine Kachel, die nicht mehr startet, macht JEDE Wache rot, die zum Starten
      auf die KACHEL tippt. Gefunden durch Durchmessen, nicht durch Lesen:
      verschmelzung-befehle (4 Stellen), ard-folgen-lane (nachzuegler),
      marke-titelwechsel-je-dienst (Fall 3), raster-marke-schau (tippeQuell),
      weiterhoeren-atempause (rasterKachelTippen) und neu-sprechen-schau.
      Ueberall dieselbe Reparatur: `k.querySelector('.tipp-spiel') || k`
      anklicken - dann beantwortet der BAUM die Frage 'womit startet man das
      hier', und nicht eine Liste von Arten, die beim naechsten Umbau altert.

      ZWEI AUSNAHMEN, und beide sind lehrreich:
        * Wachen, die OEFFNEN wollen (ard-folgen-lane Fall 'lane',
          marke-titelwechsel Fall 4), duerfen den Knopf NICHT bevorzugen -
          ein pauschaler Vorrang haette dort die Sendung gestartet statt ihre
          Folgen-Lane aufzuklappen, und die Messung haette an einer Lane
          gewartet, die es nicht gibt.
        * neu-sprechen-schau braucht eine Kachel, deren TIPP spielt: der
          Play-Knopf traegt bewusst keinen Lernmodus (dritter Parameter von
          sprichDann leer). Sie wird jetzt ueber den Satz gesucht, den die
          Kachel selbst sagt ('…, abspielen') - das ueberlebt den naechsten
          Umbau, eine Titel- oder Artenliste nicht.

      ══ DIE ATTRAPPE LOG ZWEIMAL DURCH WEGLASSEN ═════════════════════════════
      tools/neu-vorschau.mjs haette die Arbeit unmessbar gemacht:
        * `quellen` gab es gar nicht, nur `quelle` - eine Messung haette NIE
          zwei Plaketten gesehen und die Sache fuer erledigt gehalten.
        * /inhalt OHNE Abfrageteil galt als 'unverschmolzen'. Der echte Server
          liest bei fehlendem verschmelzen= die DARSTELLUNG
          (verschmelzenAusDarstellung, seit E95) - und genau das ist der
          haeufige Fall: weder die Folgen-Lane noch die Titel-Lane haengt
          einen Abfrageteil an. Die Lage-Schalter waeren dagewesen, gewirkt
          haetten sie nicht.
      Nachgebaut sind jetzt BEIDE Server-Wege (titelQuellenStempeln fuer lokal,
      Spotify, ARD, Plugin; titelMischen fuer Jellyfin samt ersetztem
      datei/…-Befehl). Der zweite MUSSTE mit: unter verschmolzen-an fuehren
      beide zweiten Ausgaben dieser Vorschau den Jellyfin-Eintrag, ohne ihn
      gaebe es dort ueberhaupt kein gemischtes Album. Und jeder ZWEITE Titel
      bekommt eine lokale Schwesterspur - nicht alle und nicht keiner, sonst
      zeigte die Messung den Unterschied nicht.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
      Eigene Vorschau, eigener Port (NIE die fremde 8299 anfassen):
        node tools/neu-vorschau.mjs --port 8385 &
        node tools/album-titel-lane.mjs http://127.0.0.1:8385/neu/
      Sechs Faelle: oeffnen, plaketten, gemischt, knopf, vorlesen, bruecke.
      Die Erwartung kommt aus /inhalt und ist in der Wache UNABHAENGIG noch
      einmal formuliert - eine Wache, die die Formel des Geprueften importiert,
      prueft nichts. Der Fall 'gemischt' zaehlt ausdruecklich mit: ohne ihn
      hiesse 'alle Plaketten stimmen' nur 'eine Plakette je Titel stimmt'.

      Gegenprobe, dreimal gemessen und jedes Mal zurueckgebaut:
        istAlbum = false            -> oeffnen, plaketten, gemischt, vorlesen, knopf
        markenBauen ohne 2. Argument-> plaketten ('soll lokal+jellyfin(blass)')
        Attrappe: lokalDa = false   -> gemischt ('kein Titel hat zwei Quellen')

      ══ WAS OFFEN BLEIBT ═════════════════════════════════════════════════════
      Kein Geraetebeweis (die Box war offline): die 0,45 fuer die blasse
      Plakette und die 20 px Plakettengroesse auf einer 96-px-Titelkachel sind
      GEWAEHLT, nicht gemessen. Und albumTitelReiheBauen setzt kein
      data-spielt - die Marke 'spielt gerade' fehlt in dieser Lane, wie auf der
      Interpretenseite seit jeher.
  - id: riegel-haelt-nur-wo-die-datei-auch-benutzt-wird
    kind: gotcha
    title: "Der Riegel gegen das Original hielt zwei Wege nicht — weil sie die geriegelte Datei gar nicht anfassen"
    match: "riegel original|MUPI_ZURUECK_ZUM_ORIGINAL|splitti holen|fork wird ersetzt|autostart.sh bashrc|curl sudo bash installer|autosetup ohne MUPI_LOCAL_SRC|fremdbezug"
    sev: high
    cause: "Am 08.08.2026 bekam `update/start_mupibox_update.sh` einen Riegel: ohne `MUPI_ZURUECK_ZUM_ORIGINAL=1` bricht es ab, statt Upstreams Archiv zu holen und den Fork mit `rm -R` zu ueberschreiben. Das galt als erledigt. Am 31.08.2026 gemessen: die Luecke stand noch an ZWEI weiteren Stellen, und die zweite ist die Sorte, gegen die ein lokaler Riegel grundsaetzlich nicht hilft. (1) `autosetup/autosetup.sh` zog ohne gesetztes `MUPI_LOCAL_SRC` version.json von splitti, daraus die Archiv-Adresse (:258), lud sie (:295) und packte sie aus (:304) — dieselbe Ersetzung auf dem INSTALL-Weg. Dort faellt sie niemandem auf: eine frische Karte ist ohnehin leer, am Ende laeuft alles, nur eben MuPiBox statt MixPiBox. (2) `autosetup/autostart.sh` schrieb `curl -L .../splitti/MuPiBox/main/autosetup/autosetup.sh | sudo bash` in die `.bashrc` und startete neu. Dieser Weg benutzt die lokale, geriegelte Datei NICHT — er holt sich Upstreams Fassung frisch von GitHub, und die traegt keinen Riegel. Genau so war der Update-Knopf des alten PHP-Admins gebaut (`curl … start_mupibox_update.sh | sudo bash`)"
    fix: "Beide Stellen bekamen denselben Riegel unter derselben Variable (eine zu kennen reicht). `autostart.sh` wurde stillgelegt statt umgebogen: Umbiegen braucht eine Fork-Adresse, die eine frische Box im Feld aufloest — `http://git.local:3000` ist LAN-intern, waehrend der Installation ist nicht einmal das sicher. Gehalten wird es seither von `tools/mixpi-fremdbezug-pruefen.py` (haengt in tools/pruefen.sh): sie sucht nicht in einer Dateiliste, sondern nach dem MERKMAL — eine ausfuehrbare Zeile, die eine Upstream-Quelle nennt UND ein Beschaffungsverb traegt (wget/curl/git clone). Eine Dateiliste haette `autostart.sh` nie enthalten; es steht in keinem Ausrollweg-Verzeichnis und wird von nichts im Baum gerufen"
    tags: [update, riegel, fork, autosetup, ausrollweg, wache, fremdbezug, 2026]
    related: [kommentar-und-kompilat-sind-keine-gegenstelle, gegenprobe-statt-gruen-glauben, wache-auf-sorte-nicht-auf-pfad, ohne-linger-kein-ton-auf-frischer-box]
    body: |
      ══ DIE REGEL ════════════════════════════════════════════════════════════
      Ein Riegel in einer Datei schuetzt nur Wege, die DIESE Datei ausfuehren.
      Wer die Datei per `curl | bash` frisch von der fremden Quelle holt, laeuft
      an jedem lokalen Riegel vorbei — er benutzt das geriegelte Stueck nie.

      Daraus folgt fuer jede Stilllegung die Anschlussfrage: „Und wer holt sich
      diese Datei woanders her?" Erst wenn die beantwortet ist, ist der Riegel
      ein Riegel und keine Notiz.

      ══ WARUM DER INSTALL-WEG DIE GEFAEHRLICHERE HAELFTE IST ══════════════════
      Auf dem UPDATE-Weg ist der Schaden laut: eine laufende Box verliert ihre
      Arbeit, jemand merkt es noch am selben Abend. Auf dem INSTALL-Weg ist er
      still. Eine frische Karte hat nichts zu verlieren, sie bootet, spielt
      Musik und sieht richtig aus. Dass sie das ORIGINAL fuehrt und nicht den
      Fork, faellt erst auf, wenn jemand eine Funktion sucht, die es hier gibt
      und dort nicht — Wochen spaeter, und gesucht wird der Fehler dann im Code.

      Das ist dieselbe Klasse wie [[ohne-linger-kein-ton-auf-frischer-box]]:
      eine von Hand grossgezogene Box belegt nicht, dass etwas ausgerollt WIRD.

      ══ WAS DIE WACHE NICHT BEWEIST ══════════════════════════════════════════
      Sie prueft die REIHENFOLGE (Riegel steht vor der Beschaffungszeile), nicht
      den Kontrollfluss. Ein Riegel in einem `if`, das nie zutrifft, besteht sie.
      Und ein Bild vom Original (`installation.jpg`) meldet sie als HINWEIS statt
      als Luecke — es kann den Fork nicht ersetzen. Beides steht so im Kopf des
      Werkzeugs; wer den Fluss aendert, muss weiterhin selbst hinsehen.
  - id: der-packschritt-meldete-erfolg-und-tat-nichts
    kind: gotcha
    title: "deploy.sh endete mit 0, obwohl zip fehlte — das Paket blieb neun Tage alt"
    match: "deploy.sh zip fehlt|Kommando nicht gefunden zip|deploy.zip alt|paket veraltet|frische karte alter stand|jingle ueberspringen fehlt|paketfrische"
    sev: high
    cause: "src/deploy.sh hat kein `set -e`. Schlug `zip -r ../deploy.zip .` fehl, lief es weiter: `cp -f deploy.zip ../bin/nodejs/deploy.zip` fand nichts, `rm -rf deploy` raeumte den frischen Bau weg, das Skript endete mit 0. Am 31.08.2026 auf dem Arbeitsrechner nachgemessen: `zip` ist dort GAR NICHT installiert (unzip, 7z und python3 schon). Der Lauf meldete 'Kommando nicht gefunden', endete mit RC=0, und bin/nodejs/deploy.zip war hinterher unveraendert. Das eingecheckte Paket stammte aus e66e4185 vom 22.08.2026 — 9,2 Tage und 77 Commits hinter dem Quelltext. Weil recipes/mupibox-app.yaml genau dieses Artefakt hochlaedt ($MUPI_REPO/bin/nodejs/deploy.zip), trug jede frisch installierte Karte den Stand des letzten manuellen Bauens; so fehlte 'Jingle ueberspringen' auf einer frischen Box"
    fix: "Zwei Antworten, weil eine nicht reicht. (1) `zip` ist nicht mehr Bedingung: fehlt es, packt python3 mit erhaltenen Rechten (external_attr aus st_mode) — ein Bauweg, der an einem fehlenden Hilfsprogramm haengt, wird von Hand umgangen, und von Hand gebaute Artefakte sind der Grund fuer die ganze Misere. (2) Der Nachweis kommt NACH dem Packen und prueft das ERGEBNIS statt des Rueckgabewerts: Datei da, nicht leer, `unzip -t` lesbar, und die vier Stuecke drin (server.js, plugin-laufwerk.js, herkunft.json, www-admin/index.html). Gehalten wird der Abstand seither von tools/mixpi-paketfrische-pruefen.py: sie liest den Bau-Commit aus dem herkunft.json IM Zip (kein mtime-Raten, das ein `git checkout` ohnehin zunichte macht) und leitet die Eingaenge aus der Baukonfiguration ab"
    tags: [deploy, bauweg, paket, deploy-zip, wache, stille-fehler, 2026]
    related: [esbuild-schreibfehler-endet-mit-null, gegenprobe-statt-gruen-glauben, wache-auf-sorte-nicht-auf-pfad, riegel-haelt-nur-wo-die-datei-auch-benutzt-wird]
    body: |
      ══ WO DIE LUECKE SASS, UND WARUM DAS DIE LEHRE IST ══════════════════════
      Drei Zeilen weiter oben im selben Skript stehen harte Abbrueche fuer
      fehlende www-admin, fehlende www/neu und fehlendes plugin-laufwerk.js —
      jeder mit einem Absatz Begruendung, jeder aus einem echten Schaden
      geboren. Nur der Schritt, der das Paket ueberhaupt HERSTELLT, hatte
      keinen.

      Das ist das Muster: Wachen wachsen dort, wo schon einmal etwas weh tat.
      Der Herstellungsschritt selbst gilt als selbstverstaendlich — bis das
      Hilfsprogramm fehlt, das er still voraussetzt.

      ══ WIE MAN DEN EINGANG ZAEHLT, ENTSCHEIDET DIE ZAHL ══════════════════════
      Der Rueckstand war 61 Commits, wenn man `src/` zaehlt, und 77, wenn man
      alle Eingaenge zaehlt. Der Unterschied ist `NewDesign/`: es haengt als
      Angular-Asset an src/frontend-box/angular.json und landet als `www/neu/`
      im Paket. Eine Handliste 'nur src/' haette ein Sechstel des Rueckstands
      uebersehen — deshalb leitet die Wache die Eingaenge aus `workspaces` und
      den `assets.input`-Eintraegen ab statt sie aufzuzaehlen.

      ══ WARUM NICHT UEBER ZEITSTEMPEL ════════════════════════════════════════
      Ein mtime-Vergleich waere raten: `git checkout` setzt Zeitstempel neu,
      ein frisch geklonter Baum traegt fuer alles dieselbe Sekunde. Das Paket
      weiss es selbst besser — seit dem Herkunftsstempel liegt der Bau-Commit
      IM Zip. Gefragt wird git, was seither an den Eingaengen passiert ist;
      das ist eine Liste, keine Schaetzung.
  - id: wache-auf-sorte-nicht-auf-pfad
    kind: muster
    title: "Eine Wache, die eine Dateiliste fuehrt, findet genau die Datei nicht, wegen der man sie gebaut hat"
    sev: high
    tags: [wachen, werkzeuge, muster, riegel, ausrollweg, 2026]
    related: [riegel-haelt-nur-wo-die-datei-auch-benutzt-wird, kommentar-und-kompilat-sind-keine-gegenstelle, gegenprobe-statt-gruen-glauben, ungerufene-wache-driftet-in-den-fehlalarm, dauerrote-wache-ist-keine]
    body: |
      ══ DIE REGEL ════════════════════════════════════════════════════════════
      Eine Wache prueft auf die SORTE des Fehlers, nicht auf den ORT, an dem er
      zuletzt stand. Wer eine Liste von Pfaden fuehrt, hat die Annahme
      eingebaut, dass der naechste Fall an einem der bekannten Orte auftaucht —
      und genau das ist bei jedem interessanten Fall falsch.

      ══ DER BELEG (31.08.2026) ═══════════════════════════════════════════════
      Anlass war `tools/mixpi-fremdbezug-pruefen.py`, gebaut zu
      [[riegel-haelt-nur-wo-die-datei-auch-benutzt-wird]]. Die naheliegende
      Bauform waere gewesen: „prueft `update/start_mupibox_update.sh` und
      `autosetup/autosetup.sh` auf einen Riegel". Beide Dateien waren bekannt,
      beide standen im Befund, die Liste haette sich richtig angefuehlt.

      Der dritte Fall war `autosetup/autostart.sh` — es schrieb
      `curl -L .../splitti/MuPiBox/main/autosetup/autosetup.sh | sudo bash` in
      die `.bashrc` einer frischen Box. Diese Datei steht in KEINEM
      Ausrollweg-Verzeichnis, wird von nichts im Baum gerufen und stand auf
      keiner Liste, die jemand geschrieben haette. Eine Pfadwache haette sie
      nie angefasst und dabei gruen gemeldet.

      Gebaut wurde deshalb auf das MERKMAL: eine AUSFUEHRBARE Zeile, die eine
      Upstream-Quelle nennt UND ein Beschaffungsverb traegt (wget, curl,
      git clone). Wo beides zusammentrifft, muss in derselben Datei ein Riegel
      stehen, und zwar VOR der Zeile. Das Merkmal kann nicht mit dem Fehler
      umziehen — der Pfad schon.

      ══ WIE MAN DIE SORTE FINDET ═════════════════════════════════════════════
      Die Frage lautet nicht „wo stand es?", sondern „woran haette ich es
      erkannt, wenn ich es nicht schon gewusst haette?". Brauchbar ist ein
      Merkmal, das
        * ohne den konkreten Fund formulierbar ist,
        * beim Umbenennen/Verschieben der Datei erhalten bleibt,
        * und beim Verschwinden des Fehlers ebenfalls verschwindet.
      Zwei Gegenbeispiele aus diesem Baum: `WURZEL / "Dockerfile"` (verliert
      jede zweite Abbild-Datei) und Wachen, die auf AEHNLICHKEIT von Namen
      abstellen — Aehnlichkeit ist keine Sorte, sie ist eine Vermutung.

      ══ DIE ZWEITE HAELFTE: ohne Gegenprobe zaehlt es nicht ═══════════════════
      Eine Merkmalswache, die ihr Muster nicht mehr trifft, meldet still gruen.
      Deshalb gehoert in jede der Satz: findet sie UEBERHAUPT KEINE Fundstelle,
      meldet sie WARNUNG statt Erfolg ([[gegenprobe-statt-gruen-glauben]]) —
      dann ist eher das Muster veraltet als der Baum sauber. In
      `mixpi-fremdbezug-pruefen.py` steht dafuer zusaetzlich ein
      `--sabotage`-Schalter.

      ══ UND DIE DRITTE: sie muss gerufen werden ═══════════════════════════════
      Eine Wache, die in keinem Laeufer haengt, rostet unbemerkt
      ([[ungerufene-wache-driftet-in-den-fehlalarm]]). Der Platz ist
      `tools/pruefen.sh`, nicht nur `tools/doku-luecken-probe.sh`: der
      Doku-Lauf sieht die Abweichung erst einen Lauf zu spaet, wenn der
      Commit schon steht.
  - id: kommentar-und-kompilat-sind-keine-gegenstelle
    kind: falle
    title: "Eine grep-Wache schlaegt auf den Absatz an, der den Fehler ERKLAERT — und auf das Kompilat, das ihn nur mitschleppt"
    sev: med
    tags: [wachen, werkzeuge, falle, grep, deploy, 2026]
    related: [wache-auf-sorte-nicht-auf-pfad, riegel-haelt-nur-wo-die-datei-auch-benutzt-wird, gegenprobe-statt-gruen-glauben, mupi-src-deploy-gehoert-root, admin-abgleich-grep-luegt]
    body: |
      ══ ZWEI FUNDSTELLEN, DIE KEINE SIND ═════════════════════════════════════
      Wer im Baum nach einem Textmuster sucht, findet drei Sorten Treffer, und
      nur eine davon ist der Fehler:

        1. die ECHTE, ausfuehrbare Zeile — die gesuchte,
        2. der KOMMENTAR, der genau diese Zeile zitiert, um vor ihr zu warnen
           (Kopfkommentare von Wachen, Doku-Absaetze, Wiki-Eintraege),
        3. das KOMPILAT unter `src/deploy/`, `bin/`, `www/` — dieselbe Zeile
           noch einmal, weil der Bau sie mitgenommen hat.

      Sorte 2 und 3 sind keine Gegenstellen. Sie sind Abbilder. Wer sie
      mitzaehlt, bekommt eine Wache, die nach der Reparatur ROT bleibt (das
      Kompilat ist noch alt, der Kommentar bleibt fuer immer stehen) — und eine
      dauerrote Wache ist keine ([[dauerrote-wache-ist-keine]]).

      ══ DIE GEFAEHRLICHERE HAELFTE ═══════════════════════════════════════════
      Der Umkehrfall ist schlimmer: eine Wache, die ihren eigenen
      Kopfkommentar als Fundstelle liest, ist ab dem Tag ihrer Entstehung rot
      und wird deshalb entschaerft — meist per Ausnahme auf ihren eigenen
      Pfad. Danach ist sie fuer die halbe Datei blind, und das faellt nie
      wieder auf. Und wer statt der Entschaerfung die Ausnahme falsch setzt
      („alles unter tools/ ignorieren"), macht die Wache fuer genau das Revier
      blind, in dem die naechsten Wachen entstehen.

      ══ WIE ES RICHTIG STEHT ═════════════════════════════════════════════════
      In `tools/mixpi-fremdbezug-pruefen.py` (31.08.2026) so geloest:
        * Kommentar erkannt an der ersten Nicht-Leerstelle `#` sowie ab dem
          ersten ` #` einer Zeile; der Rest der Zeile faellt weg, BEVOR das
          Muster greift. Ein Verb hinter einem `#` INNERHALB einer Zeichenkette
          faellt damit ebenfalls durch — bewusste Untergrenze, im Kopf notiert.
        * Gebaute Ausgaben (`src/deploy/`, `bin/`, `node_modules/`) gehoeren
          nicht in den Suchbestand. Sie sind kein zweiter Ort, an dem etwas
          repariert werden koennte; repariert wird die Quelle, und der Bau
          zieht nach.
      Beides gehoert IN DEN KOPF der Wache geschrieben, nicht nur in den Code:
      die naechste Sitzung sieht sonst eine Ausnahme ohne Begruendung und
      haelt sie fuer Bequemlichkeit.

      ══ ABGRENZUNG ═══════════════════════════════════════════════════════════
      Es gibt den umgekehrten Fall, und er ist eine EIGENE Wachensorte: ein
      Kommentar, der eine Endpunkt- oder Zeilenangabe nennt, die es nicht mehr
      gibt (`tools/kommentar-endpunkte-pruefen.py`,
      `tools/doku-zeilenzitate-pruefen.py`). Dort IST der Kommentar der
      Prueflings — hier ist er es nicht. Wer die beiden vermischt, bekommt eine
      Wache, die auf jedes Zitat anschlaegt.
  - id: deploy-zip-traegt-den-stand-des-letzten-handbaus
    kind: gotcha
    title: "Die frische Karte bekommt nicht den Baum, sondern das zuletzt von Hand gezippte deploy.zip"
    sev: high
    match: "fehlt auf frischer Box|funktion fehlt nach installation|deploy.zip|bin/nodejs/deploy.zip|frisch installiert alter stand"
    cause: "`bin/nodejs/deploy.zip` ist ein EINGECHECKTES, von Hand gebautes Artefakt. `remote-step-installer/recipes/mupibox-app.yaml` holt `$MUPI_REPO/bin/nodejs/deploy.zip` und packt es nach `$MUPI_APP` aus — gebaut wird auf der Karte nichts. Jede frische Box traegt damit den Stand des letzten manuellen `bash src/deploy.sh`, nicht den Stand des Commits, aus dem sie gezogen wurde. Am Arbeitsplatz faellt das nie auf, weil `tools/ausliefern.py` den frischen Stand direkt auf die Box traegt"
    fix: "vor jedem Ausliefern `bash src/deploy.sh` laufen lassen und das Paket MIT COMMITTEN. Gewacht seit 31.08.2026 von `tools/mixpi-paketfrische-pruefen.py` (haengt in `tools/pruefen.sh`): sie liest den Herkunftsstempel IM Zip und fragt git, welche Commits an den Eingaengen seither fehlen"
    tags: [ausrollweg, deploy, installer, wache, frischebox, 2026]
    related: [ohne-linger-kein-ton-auf-frischer-box, bt-skripte-lagen-nie-im-baum, rezeptschleife-nennt-acht-von-siebzehn, fork-bauen-ohne-sudo-ausgabe-umlenken, mupi-src-deploy-gehoert-root, gegenprobe-statt-gruen-glauben, wache-auf-sorte-nicht-auf-pfad]
    body: |
      ══ DER ANLASS (31.08.2026) ══════════════════════════════════════════════
      Auf einer frisch installierten Box fehlte „Jingle ueberspringen". Der Code
      lag seit Tagen im Baum. Gefehlt hat er trotzdem — das Paket war aelter.
      Nichts wurde rot, nichts hat gewarnt, und gesucht wurde der Fehler
      anschliessend im Code.

      Wie gross der Rueckstand wirklich war, sagte erst die Wache: beim
      Nachbauen am selben Abend (Commit `7574dada`) holte
      `bash src/deploy.sh` **77 Commits und 9,2 Tage** auf. So lange trug jede
      frisch bespielte Karte einen Stand, den niemand fuer aktuell gehalten
      haette, wenn er die Zahl gesehen haette.

      Das ist dieselbe Klasse wie [[ohne-linger-kein-ton-auf-frischer-box]],
      [[bt-skripte-lagen-nie-im-baum]] und
      [[rezeptschleife-nennt-acht-von-siebzehn]]: eine von Hand grossgezogene
      Box ist KEIN Beleg dafuer, dass etwas ausgerollt WIRD.

      ══ WARUM NICHT UEBER ZEITSTEMPEL GEMESSEN WIRD ══════════════════════════
      Ein `mtime`-Vergleich waere Raten: `git checkout` setzt Zeitstempel neu,
      ein frisch geklonter Baum traegt fuer alles dieselbe Sekunde. Seit
      `src/deploy.sh` einen Herkunftsstempel schreibt, liegt IM Zip ein
      `herkunft.json` mit dem Commit, aus dem gebaut wurde. Die Wache liest ihn
      und fragt git nach den Commits, die seither an den Eingaengen liegen —
      keine Schaetzung, sondern eine Liste.

      ══ WOHER DIE EINGAENGE KOMMEN ═══════════════════════════════════════════
      Abgeleitet, nicht aufgezaehlt ([[wache-auf-sorte-nicht-auf-pfad]]):
      `package.json` → `workspaces` ergibt die Arbeitsbereiche, jede
      `angular.json` darunter → die `assets`-Eintraege mit `input`. So kommt
      `NewDesign/` herein — es ist ein Asset von `src/frontend-box`, landet als
      `www/neu/` im Paket und waere von einer Handliste „src/ und sonst nichts"
      uebersehen worden.

      ══ DIE STEHENDE WARNUNG, DIE KEINE LUECKE IST ═══════════════════════════
      Die Wache meldet bei jedem Lauf:

          WARNUNG: abgeleiteter Eingang fehlt im Baum: MuPiBox/media

      Das ist RICHTIG SO und keine Nachlaessigkeit. `src/frontend-box/angular.json`
      (Z. 28–32) zieht `./../../MuPiBox/media` als Cover-Asset — ein Pfad, den
      es nur auf dem GERAET gibt, nie im Repo. Er kann folglich auch keinen
      Rueckstand belegen und wird aus der Messmenge genommen.

      Wichtig fuer den, der die Ausgabe liest: diese WARNUNG aendert das URTEIL
      NICHT. Solange mindestens ein Eingang uebrig bleibt, endet der Lauf mit
      `KEINE LUECKE` und Rueckgabe 0. Nur „gar kein Eingang ableitbar" ist rot.
      Wer nur die Bilanzzeile oder den Rueckgabewert liest — etwa hinter einer
      Pipe, wo der Exit dem letzten Glied gehoert —, sieht die Warnung nicht;
      wer sie sieht und fuer neu haelt, meldet einen Befund, der
      seit dem ersten Tag dokumentiert ist ([[dauerrote-wache-ist-keine]]).

      ══ WAS SIE NICHT TUT — Absicht, keine Luecke ════════════════════════════
        * Sie baut nicht. Sie sagt, DASS das Paket hinterherhinkt; der Knopf
          ist `bash src/deploy.sh` und steht im Befund.
        * Sie prueft nicht den INHALT des Pakets gegen den Baum — ob
          `server.js` im Zip aus genau diesem Commit stammt, misst
          `tools/ausliefern.py` gegen die Box. Hier geht es um die Frage davor.
        * Ungetrackte Dateien zaehlen nicht als Rueckstand: sie koennen in
          keinem Commit-Vergleich auftauchen — was `git ls-files` nicht kennt,
          misst diese Wache nicht. Geaenderte VERFOLGTE
          Dateien meldet sie als eigenen, weicheren Befund — auch die sind im
          Paket nicht drin.
        * Gegenprobe eingebaut: `--sabotage` setzt den Bau-Commit auf HEAD,
          danach MUSS sie gruen sein ([[gegenprobe-statt-gruen-glauben]]).

  - id: umzug-liess-die-toten-importe-und-nahm-den-lebenden-mit
    kind: architektur
    title: "tools/beruehrziele-neu.mjs starb an Zeile 1: der Umzug nach leihgabe.mjs liess fuenf tote Importe stehen und trug den einen neuen nie ein"
    sev: mid
    match: "vorschauLeihen is not defined|beruehrziele-neu stirbt|toter Import|port is not defined|leihgabe Import fehlt|Werkzeug in keinem Laeufer"
    tags: [werkzeug, importe, umzug, refaktor, laeufer, beruehrziele, messen, 2026]
    stand: "31.08.2026 repariert und gegengemessen: 14 Schirme, 211 erreichbare Ziele, 7 unter 9 mm, Ende 0 (auf c7eabbf9, also nach E112; auf fde79e16 davor waren es 213/9 — die Zahlen folgen dem Baum)"
    source: "31.08.2026, eigene Vorschau auf 8737 aus einem eigenen Arbeitsbaum; NICHT gegen den Vorgabeport 8299"
    related:
      - beruehrziele-neue-oberflaeche-gemessen
      - wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm
      - messwerkzeug-muss-sich-selbst-pruefen
      - werkzeug-statt-wegwerfbefehl
    body: |
      `node tools/beruehrziele-neu.mjs` endete sofort mit
      `ReferenceError: vorschauLeihen is not defined`. Das Suchen und Starten
      des Browsers stand einmal IN dieser Datei und ist nach
      `tools/leihgabe.mjs` umgezogen. Beim Umzug geschah beides zugleich:

        * die Importe des alten Rumpfes blieben liegen — `spawn`,
          `existsSync`/`readdirSync`, `homedir`, `join` aus `node:path`;
        * `./leihgabe.mjs` wurde nie importiert, obwohl die Datei
          `vorschauLeihen` und `eigenerBrowser` benutzt.

      ══ WARUM MAN DEN TOTEN IMPORT NICHT SIEHT ═════════════════════════════

      `join` sah benutzt aus. Es steht fuenfmal in der Datei —
      `BEKANNTE_FAHNEN.map(...).join(' ')`, `teile.join('')`,
      `z.wodurch.join(' / ')`. Das ist ALLES `Array.prototype.join` und hat
      mit `node:path` nichts zu tun. Eine Namenssuche nach `join` meldet
      Treffer und beruhigt; erst die Frage „welches `join`" trennt sie.
      Dasselbe gilt fuer jedes kurze Wort, das ein eingebauter Prototyp
      ebenfalls traegt (`filter`, `map`, `resolve`, `format`).

      ══ DER ZWEITE FEHLER LAG EINE ZEILE HINTER DEM ERSTEN ═════════════════

      Nach dem Import lief der Lauf bis `ABBRUCH: port is not defined`. Der
      Rumpf wartete noch selbst auf den Browser und fragte
      `http://127.0.0.1:${port}/json/list` — `port` war eine LOKALE Variable
      des ausgezogenen Rumpfes. `eigenerBrowser` wartet dieses Warten laengst
      selbst und liefert `{port, kind, profil, ziele, seite, schliessen}`;
      die Geschwister schreiben durchweg `new WebSocket(await brw.seite())`.

      LEHRE FUER DIE REPARATUR: Ein fehlender Import ist selten allein. Wer
      nur die erste `ReferenceError`-Zeile behebt und dann „laeuft wieder"
      meldet, hat den Lauf nie bis zum Ende gesehen — die naechste tote
      Variable steht direkt dahinter, weil beide aus demselben Umzug stammen.

      ══ WARUM ES MONATE UNBEMERKT BLIEB ════════════════════════════════════

      DAS WERKZEUG HAENGT IN KEINEM LAEUFER. `tools/pruefen.sh` (106 Schritte)
      ruft es nicht auf, weder direkt noch ueber einen anderen Schritt; die
      einzige Nennung `node tools/beruehrziele-neu.mjs --alle > /tmp/e8.txt`
      steht im Docstring von `tools/e8-ordnen.py`, also in Prosa. Ein
      Werkzeug, das nur von Hand laeuft, faellt erst beim naechsten Handgriff
      um — hier auffindbar geworden, weil E112 die Nachbarwachen durchmass.

      Das ist die MESSWERKZEUG-Haelfte von
      [[wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm]]: dort eine
      Wache, die niemand ruft und die auf einen Fehlalarm rot stand; hier ein
      Messwerkzeug, das niemand ruft und das gar nicht mehr startet. Die
      Zahlen, auf denen E8/F6 planen, kamen aus GENAU DIESEM Werkzeug.

      ══ DER BERICHT NENNT JETZT SEINE ADRESSE ══════════════════════════════

      `Gemessen an: http://127.0.0.1:<port>/neu/` steht neu im Kopf. Der
      Dateikopf erzaehlt seit dem 06.08.2026 ausfuehrlich, wie zwei Baeume
      gemessen wurden und beide Male der fremde Vorgabeport 8299 herauskam —
      und der Bericht nannte die Adresse mit keinem Wort. Gerade weil
      `vorschauLeihen` stillschweigend eine schon laufende Vorschau nimmt,
      steht der gemessene Baum sonst nirgends.

      ══ GEGENPROBE, DIE DEN UNTERSCHIED ZU „STUERZT NICHT MEHR AB" MACHT ════

      Nicht abzustuerzen ist kein Beweis, dass gemessen wird. Mit `--probe`
      wurde eine CSS-Regel eingespeist und die Zahl musste ihr folgen:

        ohne Probe                    9,24 mm  Rechteck 66x66  #zurueck  ✓
        .zurueck{width/height:30px}   4,20 mm  Rechteck 30x30  #zurueck  ✗
        Bilanz                        0 -> 1 von 12 unter der Marke
        --pruefen dazu                Ende 1 statt 0

      30 px x 0,14 mm/px = 4,20 mm, auf den Hundertstel. Die Zahl kommt vom
      Schirm, nicht aus einer Konstanten.
  - id: der-zieher-holt-und-der-rueckweg-liegt-schon-da
    kind: architektur
    title: "Die Box holt sich ihre Fassung selbst — und `ohne_wirkung` ist kein Erfolg"
    match: "update ziehen|box holt fassung|mixpi-zieher|updater box|rueckweg ohne netz|abbruch mitten im update|ohne_wirkung|standwache frist update"
    sev: med
    cause: "Bis zum 31.08.2026 brauchte jede neue Fassung einen Menschen mit einem Laptop im selben Netz (tools/ausliefern.py schiebt). Ein Update-Weg, den die Box selbst gehen kann, fehlte — der vorhandene (update/start_mupibox_update.sh) holte beim Original und ist stillgelegt. Beim Bau des Ziehers fiel beim Gegenlesen des vorhandenen Tauschers etwas auf, das jeden Aufrufer betrifft: `mupibox-tauscher.py --zurueckdrehen` meldet `ohne_wirkung` und `ohne_rueckweg` GETRENNT von `fehler`. `ohne_wirkung` heisst, dass Ziel und Rueckweg derselbe Inode sind (entsteht, wenn ein Lauf zwischen `os.link` und `os.replace` abriss) — das Vertauschen hat dann NICHTS getan. Wer nur `fehler` auswertet, meldet dafuer Erfolg und faehrt weiter die kaputte Fassung, im Glauben, zurueckgedreht zu haben"
    fix: "scripts/box/mixpi-zieher.py orchestriert die vorhandenen Stuecke statt sie nachzubauen: mupibox-tauscher.py (renameat2, Rueckweg unter festem Namen), mupibox-standwache.py (Frist, die ohne Menschen zurueckdreht), mupibox-sicherung.py. Neu ist nur Angebot holen, Herkunft beurteilen, Artefakt pruefen, Ablauf abbruchfest schneiden. Alle drei Befund-Listen des Tauschers werden ausgewertet, `ohne_wirkung` als Fehlschlag. tools/mixpi-fassung-schneiden.py fuellt die Kanaele; tools/mixpi-zieher-probe.py faehrt den ECHTEN Zieher in einer Schein-Box unter /tmp (dafuer lesen seine Ortskonstanten Umgebungsvariablen, auf einer Box ist keine gesetzt)"
    tags: [update, zieher, box, rueckweg, abbruchfest, pruefsumme, standwache, 2026]
    related: [riegel-haelt-nur-wo-die-datei-auch-benutzt-wird, der-packschritt-meldete-erfolg-und-tat-nichts, gegenprobe-statt-gruen-glauben]
    body: |
      ══ DIE REGEL: ZWEI ZUSTAENDE, NIE EIN DRITTER ═══════════════════════════
      Der Abbruch ist bei dieser Box der NORMALFALL, nicht der Sonderfall —
      Funkstrecke, und Kinder, die ausschalten. Der Ablauf ist deshalb so
      geschnitten, dass jeder Punkt entweder „alt, unberuehrt" oder „neu,
      vollstaendig" hinterlaesst. Was es NICHT geben darf, ist ein Zwischen-
      zustand, den der naechste Lauf fuer gueltig haelt.

      Konkret heisst das vier Mal dasselbe Muster:
        * Der Download landet unter `….teil` und bekommt den Endnamen ERST
          nach bestandener Summe. Ein halbes Archiv kann nie wie ein ganzes
          aussehen.
        * Ein Archiv mit FALSCHER Summe wird geloescht statt liegengelassen —
          sonst haengt der naechste Lauf weiteren Inhalt an einen kaputten
          Anfang und bekommt nie eine stimmende Summe.
        * Ein liegengebliebenes `….neu` (Abriss nach dem Bereitlegen) wird
          weggeraeumt, nicht getauscht. Es ist wertlos: haette der Tausch es
          angefasst, hiesse es `….zurueck`.
        * Die Frist wird NACH dem Tausch und VOR dem Neustart gestellt. Genau
          dazwischen liegt das Fenster, in dem niemand mehr nachmisst.

      ══ WAS EINE PROBE IM SANDKASTEN KANN UND WAS NICHT ══════════════════════
      Ein Weg, den man nicht ueben kann, wird erst im Ernstfall zum ersten Mal
      gefahren. Deshalb laeuft der ECHTE Zieher gegen eine Schein-Box unter
      /tmp, mit GESTELLTEN Abbruechen (halbes Archiv, Rest-`.neu`).

      Nicht abgedeckt und im Kopf der Probe benannt: Frist, Neustart und der
      Zweig „nach dem Tausch kaputt -> von selbst zurueck" brauchen systemd
      und damit eine echte Box. Gestellte Abbrueche sind ausserdem nicht
      dasselbe wie ein abgeschossener Prozess. Die Probe ist eine Untergrenze,
      kein Freispruch — und sie sagt das selbst.

  - id: zwei-quellen-fuer-dieselbe-tatsache-laufen-am-geraet-auseinander
    kind: architektur
    title: "Dieselbe Einstellung an zwei Orten — die Meldung zeigt dann auf den falschen Fehler"
    match: "kein versionsverzeichnis eingetragen|update einstellung|mixpi-update.json|MUPIBOX_VERSIONSFEED|zwei quellen einstellung|box eingerichtet meldet trotzdem nicht"
    sev: med
    cause: "Die Aktualisierungsseite las ihr Verzeichnis aus der UMGEBUNG des Dienstes (MUPIBOX_VERSIONSFEED/-QUELLE/-KANAL in server.ts), scripts/box/mixpi-zieher.py las es aus /etc/mupibox/mixpi-update.json. Zwei Quellen fuer DIESELBE Tatsache. Am Geraet gemessen (Box .79, 31.08.2026): fuer den Zieher war alles eingetragen, der Zieher konnte holen und tauschen — die Verwaltung meldete trotzdem 'kein Versionsverzeichnis eingetragen'. Das ist die teure Sorte Fehlmeldung: sie ist nicht falsch (DIESE Quelle war wirklich leer), sie zeigt nur auf den falschen Ort. Wer sie liest, sucht im Verzeichnis, im Netz, im Kanal — also ueberall ausser in der Verdrahtung"
    fix: "Eine Quelle, und zwar die DATEI: MUPIBOX_UPDATE_EINSTELLUNG (Vorgabe /etc/mupibox/mixpi-update.json) ist der Ort, den beide Seiten lesen. Die Umgebungsvariablen bleiben, schlagen die Datei aber nur, WENN sie gesetzt sind — fuer Proben und fuer eine Box, die man kurz umbiegen will. Gelesen wird bei JEDER Anfrage, nicht beim Start: eine Einstellung, die erst nach einem Dienstneustart wirkt, ist auf einer Box, die Musik spielt, keine Einstellung"
    tags: [update, zieher, konfiguration, einstellung, geraet, doku, 2026]
    related: [der-zieher-holt-und-der-rueckweg-liegt-schon-da, umfangsangabe-stand-an-drei-orten-die-wache-kannte-einen]
    body: |
      ══ DIE REGEL, ALLGEMEIN ════════════════════════════════════════════════
      Wo ZWEI Stellen dieselbe Tatsache getrennt beschaffen, laufen sie
      auseinander — nicht vielleicht, sondern beim ersten Einrichten, weil
      niemand beide kennt. Der Schaden ist nicht die Abweichung selbst,
      sondern die Meldung: jede Seite sagt die Wahrheit UEBER IHRE QUELLE und
      klingt dabei wie eine Aussage ueber die Box.

      Die Gegenprobe, bevor man einen zweiten Leseweg baut: „Wenn ich nur den
      einen einrichte — was meldet der andere?" Lautet die Antwort etwas
      anderes als „dasselbe", ist es eine Quelle zu viel.

      ══ WAS DAS FUER DIE DOKU HEISST ════════════════════════════════════════
      Die Handbuecher hatten dieselbe Naht: dokumentation/mixpibox.md nannte
      MUPIBOX_VERSIONSFEED und -QUELLE, aber weder MUPIBOX_VERSIONSKANAL noch
      MUPIBOX_UPDATE_EINSTELLUNG, und den Zieher gar nicht (nachgetragen am
      31.08.2026 als Abschnitt 7.7.7). tools/umgebungsvariablen-deckung.py
      fand die neun neuen Namen; die zwoelf ORTSkonstanten des Ziehers fand
      sie NICHT, weil sie ueber einen Helfer `_ort("MIXPI_APPDIR", vorgabe)`
      gelesen werden und die Wache nach dem ZUGRIFFSMUSTER sucht
      (os.environ.get mit Literal). Wer seine Variablen durch eine eigene
      Funktion liest, ist fuer die Deckungswache unsichtbar — dieselbe Sorte
      Loch wie beim Router unter anderem Namen.
  - id: drei-fehler-die-erst-die-echte-box-zeigte
    kind: gotcha
    title: "Update-Knopf am Gerät: drei Fehler, die 21 grüne Sandkasten-Prüfungen nicht fanden"
    match: "update knopf admin|einspielen knopf|keinAngebot trotz feed|installiert 1.0.0 angeboten|update setzt sich nicht|zwei konfigurationen feed|mixpi-zieher bericht"
    sev: high
    cause: "Der Zieh-Weg hatte 21 gruene Pruefungen im Sandkasten und war auf .79 bis vor den Tausch gefahren. Beim Einbau des Einspielen-Knopfes in die Verwaltung fielen am GERAET drei Dinge auf, die keine davon sehen konnte. (1) ZWEI KONFIGURATIONEN: der Server las Feed und Quelle aus der Dienst-Umgebung, der Zieher aus /etc/mupibox/mixpi-update.json. Die Box war fuer den Zieher vollstaendig eingerichtet und meldete auf der Seite trotzdem 'kein Versionsverzeichnis eingetragen' — man sucht den Fehler dann im Verzeichnis statt in der Verdrahtung. (2) DIE EIGENE NAMENSFORM WAR FUER DIE EIGENE SEITE UNLESBAR: vergleicheVersionen() in aktualisierung.ts kannte das fuehrende `v` nicht, das die MixPiBox-Fassungen von den geerbten Upstream-Marken (4.2.4) trennt. `v1.1.0` galt als unvergleichbar, neuestesAngebot() uebersprang den Eintrag als Platzhalter — Urteil 'keinAngebot' bei gefuelltem Verzeichnis und LEEREM feedFehler. Ein Fehlerbild ohne Fehlermeldung. (3) DAS UPDATE SETZTE SICH NIE: nach einem Lauf mit rc=0 und allen sechs getauschten Zielen meldete die Seite weiter 'installiert 1.0.0, angeboten v1.1.0', weil src/deploy.sh die `version` fuer herkunft.json aus package.json nimmt — und die steht seit jeher auf 1.0.0"
    fix: "(1) Es gilt die DATEI; die Umgebung schlaegt sie nur, wenn gesetzt. Gelesen bei jeder Anfrage — eine Einstellung, die erst nach einem Dienstneustart wirkt, ist auf einer Box, die Musik spielt, keine. (2) `v` vor einer Ziffer wird abgestreift; ein `v` OHNE Ziffer dahinter bleibt unvergleichbar, sonst wuerde aus 'version' still eine 0. Test dabei. (3) tools/mixpi-fassung-schneiden.py stempelt das Paket beim Veroeffentlichen um: herkunft.json bekommt die Fassung, unter der es ausgeliefert wird, und die Pruefsumme wird DANACH neu gemessen"
    tags: [update, zieher, admin, aktualisierung, herkunft, geraet, 2026]
    related: [der-zieher-holt-und-der-rueckweg-liegt-schon-da, backticks-beenden-jede-vorlage, gegenprobe-statt-gruen-glauben]
    body: |
      ══ DIE REGEL ════════════════════════════════════════════════════════════
      Alle drei sind NAHTSTELLEN, nicht Denkfehler: zwei Seiten derselben
      Sache, von denen nur eine nachgezogen wurde. Der Zieher beherrschte die
      Namensform von Anfang an — er hat sie eingefuehrt. Die Aktualisierungs-
      seite nicht. Ein Sandkasten prueft eine Seite; die Naht sieht nur, wer
      beide zusammensteckt.

      Und alle drei sind STILL. Keiner wirft eine Fehlermeldung: einmal steht
      da „kein Verzeichnis" (obwohl eins da ist), einmal „kein Angebot" bei
      leerem Fehlerfeld, einmal ein Update, das erfolgreich meldet und sich
      trotzdem wiederholt. Das ist das Muster, gegen das die Wachen dieses
      Baums stehen — und es entsteht genau dort, wo zwei Bausteine sich
      beruehren.

      ══ WARUM DER KNOPF ANDERS GEBAUT IST ALS JEDER ANDERE ═══════════════════
      `--einspielen` startet mupibox-server.service neu, also den Prozess, der
      die Anfrage bedient. Ein Endpunkt, der den Zieher als eigenes Kind
      startet und wartet, bringt sich selbst um — das Kind stirbt mit dem
      Elternteil, schlimmstenfalls zwischen zwei Zielen. Deshalb:
        * Der Lauf haengt in einer EIGENEN systemd-Unit (systemd-run) und
          gehoert niemandem.
        * Der Fortschritt kommt aus einer DATEI (`--bericht`, nach jeder Zeile
          unteilbar geschrieben), nicht aus der Antwort.
        * Die Seite pollt und ueberlebt, dass ihr Server weg ist: ein
          fehlgeschlagener Abruf loescht den letzten bekannten Stand NICHT,
          sonst blinkt die Anzeige bei jedem Neustart auf leer.
      Der Boden bleibt die Standwache: kommt der Server nicht wieder, kann die
      Oberflaeche nichts mehr melden — dann dreht die Box von selbst zurueck.

      ══ WAS DER LAUF AM GERAET BEWIES ════════════════════════════════════════
      Zwei Abbrueche wurden dabei ECHT gefahren, nicht gestellt: ein Archiv
      mit veralteter Summe lag im Zwischenspeicher (wurde verworfen und neu
      geholt) und ein Paket ohne `www/index.html` wurde abgelehnt, ohne dass
      irgendetwas angefasst wurde (rc=3). Erst der dritte Lauf ging durch:
      Sicherung nachgewiesen, sechs Ziele unteilbar getauscht, Frist VOR dem
      Neustart gestellt, Dienste hochgekommen, Frist entwarnt.
  - id: zwei-ausrollwege-zwei-benutzer-einer-vergiftet-den-anderen
    kind: gotcha
    title: "Der Zieher (root) machte den Laptop-Weg (dietpi) handlungsunfähig — sichtbar erst beim nächsten Mal"
    match: "renameat2 permission denied|www-admin root|zieher root eigentuemer|ausliefern scheitert admin|teilweise getauscht permission|beta kanal wirkungslos|kanal einstellen"
    sev: high
    cause: "Es gibt ZWEI Wege, die denselben Anwendungsordner tauschen: tools/ausliefern.py schiebt vom Arbeitsrechner als dietpi, scripts/box/mixpi-zieher.py zieht auf der Box als ROOT (er braucht systemctl, Frist und Sicherung). Was der Zieher auspackt, gehoerte damit root — und `renameat2` auf ein VERZEICHNIS verlangt Schreibrecht IM Verzeichnis, weil dessen `..` fortgeschrieben wird. Der naechste Lauf von ausliefern.py scheiterte deshalb mit PermissionError(13) auf www-admin, und zwar MITTEN IM TAUSCH: halb getauschter Baum, gestellte Frist. Am selben Abend kam ein zweites Symptom derselben Kette dazu: ein Lauf davor war (VPN an) VOR dem Dienstneustart abgerissen, sodass die neue server.js auf der Platte lag und der alte Prozess weiterlief — eine frisch gebaute Route antwortete mit 404 und sah aus wie ein Fehler im Code"
    fix: "Das Neue erbt das Eigentum des ALTEN, nicht das seines Erzeugers (eigentum_uebernehmen() im Zieher, vor dem Tausch auf `.neu` angewandt, rekursiv und ohne Symlinks zu folgen). Gibt es das Alte noch nicht, bleibt alles wie es ist — dann gibt es auch niemanden, dem etwas wegzunehmen waere. Am Geraet gegengeprueft: als root eingespielt, danach sind Ziel UND Rueckweg weiter dietpi:dietpi. Der Sandkasten kann das NICHT pruefen (ein Benutzer, trivial gruen); das steht so in der Probe unter 'was sie nicht abdeckt'"
    tags: [update, zieher, ausliefern, rechte, renameat2, root, kanaele, 2026]
    related: [drei-fehler-die-erst-die-echte-box-zeigte, der-zieher-holt-und-der-rueckweg-liegt-schon-da, spotify-control-liegt-woanders]
    body: |
      ══ DIE REGEL ════════════════════════════════════════════════════════════
      Wo zwei Wege dieselben Dateien anfassen, muessen sie sich auf EINEN
      Eigentuemer einigen — und zwar auf den, der schon da war. Wer als root
      etwas hinlegt, das sonst einem Benutzer gehoert, nimmt diesem Benutzer
      die Datei weg, ohne dass irgendwo etwas rot wird. Der Schaden entsteht
      beim NAECHSTEN Mal und sieht dann nach einem Fehler des anderen Weges
      aus.

      ══ DREI SYMPTOME, EINE URSACHENKETTE ════════════════════════════════════
      An einem Abend, und jedes fuer sich irrefuehrend:
        1. „Cannot POST /api/aktualisierung/kanal" — die Route WAR in der
           server.js auf der Platte (grep fand sie), aber der Dienst lief seit
           48 Minuten, also aus der alten Datei. Ein Lauf war vor Schritt 10
           (Neustart) abgerissen.
        2. „PermissionError renameat2 ... www-admin" — root-eigener Baum,
           dietpi kann ihn nicht umbenennen.
        3. „No route to host" bei erreichbarem Ping — VPN auf dem Arbeitsrechner.
      Wer nur das erste sieht, sucht im Code. Wer nur das zweite sieht, haelt
      den Tauscher fuer kaputt. Erst `stat -c %U:%G` auf den Baum zeigt es.

      ══ WAS DABEI RICHTIG LIEF ═══════════════════════════════════════════════
      Der abgebrochene Tausch hat sich genau so verhalten, wie er soll: Schritt 1
      brach ab, ohne etwas anzufassen (VPN); der Tausch, der in der Mitte
      abriss, stellte die Frist und meldete „Teilweise getauscht. Zurueck mit:
      …"; die Box waere nach 10 Minuten von selbst zurueckgedreht. Der Abriss
      war also kein Schaden, sondern der erste unfreiwillige Beweis, dass die
      Abbruchbehandlung traegt.
  - id: dieselbe-regel-an-zwei-orten-braucht-eine-wache-quer-dazu
    kind: pattern
    title: "Fassungsnummern werden an ZWEI Orten verglichen — zwei gruene Testsuiten, und trotzdem zweimal am selben Tag falsch"
    match: "vergleicheVersionen|mixpi-zieher vergleiche|beta kanal aktualisiert nicht|beta.1 beta.3 gleich|kein angebot obwohl feed da|fassungsvergleich-deckung|zwei implementierungen derselben regel"
    sev: high
    cause: "Dieselbe Regel (welche Fassung ist neuer?) steht zweimal im Baum: `vergleicheVersionen()` in src/backend-api/src/aktualisierung.ts fuer die Verwaltungsseite und `vergleiche()` in scripts/box/mixpi-zieher.py fuer die Box. Jede Seite hat eigene, gruene Tests — die TS-Tests pruefen die TS-Seite, die Zieher-Probe die Python-Seite. KEINE von beiden fragte, ob die zwei DASSELBE sagen. Am 31.08.2026 schlug genau diese Naht zweimal zu: erst verstand nur der Zieher die eigene Namensform `v1.1.0` (Ergebnis am Geraet: gefuelltes Verzeichnis, erreichbarer Feed, `feedFehler` leer — und trotzdem 'kein Angebot'), dann hielten BEIDE `v1.1.0-beta.1` und `v1.1.0-beta.3` fuer gleich, weil sie am `-` abschnitten. Damit war ausgerechnet der beta-Kanal, dessen ganzer Zweck schnell aufeinanderfolgende Fassungen sind, der einzige wirkungslose"
    fix: "Eine Wache QUER zur Naht statt einer dritten Testsuite laengs dazu: tools/mixpi-fassungsvergleich-deckung.py faehrt eine Faelleliste durch BEIDE Fassungen (die TS-Seite ueber tsx GEFAHREN, nicht nachgebaut — eine Nachbildung waere eine dritte Fassung derselben Regel und damit eine dritte Naht) und meldet zweierlei: wo die Urteile sich unterscheiden UND wo sie sich einig sind, aber beide von der Erwartung neben dem Fall abweichen. Sie haengt als Schritt 'Fassungsvergleich-Deckung' in tools/pruefen.sh, laeuft also vor jedem Ausliefern"
    tags: [update, kanaele, fassung, semver, wache, naht, zieher, aktualisierung, 2026]
    related: [zwei-ausrollwege-zwei-benutzer-einer-vergiftet-den-anderen, drei-fehler-die-erst-die-echte-box-zeigte, gegenprobe-statt-gruen-glauben]
    body: |
      ══ DAS MUSTER, ueber diesen Fall hinaus ═════════════════════════════════
      Wo EINE Regel an ZWEI Orten steht, decken zwei gruene Testsuiten die
      gefaehrlichste Frage nicht ab. Jede prueft ihre Seite gegen die eigene
      Erwartung; die Abweichung liegt DAZWISCHEN und ist damit fuer beide
      unsichtbar. Der Riss faellt erst am Geraet auf, und dort sieht er nach
      einem Fehler der jeweils anderen Seite aus.

      Die Wache muss deshalb quer liegen: dieselben Eingaben durch beide
      Fassungen, und ZWEI Urteile daraus. Nur „sind sie sich einig?" reicht
      nicht — zwei Fassungen, die BEIDE falsch liegen, sind sich einig und
      faenden sich nie. Deshalb steht neben jedem Fall die ERWARTUNG.

      ══ WAS DIE DREI KANAELE BEDEUTEN (Betreiber, 31.08.2026) ════════════════
        dev     „alle Aenderungen"      — laeuft mit, sobald etwas fertig ist
        beta    „ein Stable-Kandidat"   — das, was stable werden soll
        stable  „ist dann ja stable"
      Daraus die Ordnung dev → beta → fertig: eine Fassung beginnt als
      `v1.2.0-dev.N`, wird als `v1.2.0-beta.N` Kandidat und kommt als `v1.2.0`
      an. Der Kern bleibt derselbe, nur die Stufe steigt — bei gleichem Kern
      muss also die STUFE entscheiden.

      NICHT ALPHABETISCH, obwohl Semver es so taete: als Text kaeme „beta" vor
      „dev", also verkehrt herum zur Bedeutung hier. Die Ordnung steht deshalb
      ausdruecklich im Code. UNBEKANNTES RANGIERT GANZ UNTEN, nicht in der
      Mitte — damit sich nie eine Box an einem Anhaengsel hochzieht, das
      niemand eingeordnet hat.

      ══ EIN TEST HATTE DEN FEHLER FESTGESCHRIEBEN ════════════════════════════
      Das Abschneiden am `-` stand einen Tag zuvor ausdruecklich in einem Test
      („der Anhang zaehlt hier nicht"). Gemeint war Toleranz gegenueber den
      Anhaengseln des Upstreams, und fuer stable ist das richtig; fuer beta
      war es der Fehler selbst. Ein gruener Test ist kein Beweis, dass die
      festgeschriebene Absicht noch fuer alle Kanaele gilt — beim Erweitern
      der Bedeutung eines Feldes gehoeren die Tests mitgedreht, mit dem Grund
      daneben.
  - id: der-rueckfall-deutete-den-titelwunsch-still-um
    kind: gotcha
    title: "E110 waehlte richtig und das Protokoll sagte es — gespielt wurde trotzdem Kapitel 1"
    match: "jeder tipp spielt kapitel 1|spielt immer den ersten titel|titelNr wird ignoriert|gestartetNr 1 statt|kapitel unerreichbar|dienst lokal obwohl spotify gewaehlt|E117|titel-nicht-in-quelle|spotify-anmeldung|soloist logged in no"
    sev: high
    cause: "Betreiber am Geraet (04.09.2026, Box 192.168.178.62, '101 Meerjungfrauen', spotify:14Xy9Ycf6KfXH6nG2r9Zdb): Kapitel 1 liegt LOKAL, die Kapitel 1..11 bei Spotify; jeder Tipp auf ein beliebiges Kapitel spielte Kapitel 1, die Kapitel 2..11 waren unerreichbar. Die Oberflaeche bestellte korrekt (titelNr 3), E110 waehlte korrekt Spotify UND schrieb genau das ins Journal — die Antwort lautete trotzdem dienst=lokal, gestartetNr=1. DREI Stellen mussten zusammenkommen: (1) `spielVersuch` fragt fuer Spotify NUR bei ausdruecklichem Wunsch (`titelNr > 0 || positionMs > 0`) die Bereitschaft ab — der Tipp auf die ALBUM-Kachel passierte das Tor also, der Tipp auf ein KAPITEL nicht; (2) `spotifyBereitAuskunft` sagte zu Recht nein: soloist lief zwar (`systemctl is-active` = active), war aber nicht angemeldet (`soloist ctl status` = 'logged in: no'), die Box stand nicht in Spotifys Geraeteliste, dort stand nur die fremde 'Smart Soundbar 10158750' — der Versuch endete still mit `spotify-anmeldung`, OHNE Journalzeile; (3) das Ausweichen kam bei der lokalen Quelle an, und `folgeWaehlen` findet fuer Wunsch 3 in einer EIN-Titel-Liste nichts (`nr <= titel.length` ist falsch) und gibt -1 zurueck — woraus `gestartetNr: gewaehlt >= 0 ? gewaehlt + 1 : 1` ein stilles Kapitel 1 machte"
    fix: "ZWEI Naehte, beide in server.ts. (1) `spielVersuch`, `inhalt`-Zweig: ein AUSDRUECKLICHER Titelwunsch (`titelNr >= 1`), den diese Quelle nicht fuehrt (`folgeWaehlen` gab -1), ist eine ABSAGE — `{ergebnis:'geht-nicht', grund:'titel-nicht-in-quelle'}` statt Titel 1. Dadurch laeuft das Ausweichen zur naechsten Quelle WEITER: im Messfall spielt danach Spotify Kapitel 3, und nur wenn keine Quelle die Nummer fuehrt, endet /api/spielen in 502 mit lesbarem Grund. NUR bei `titelNr >= 1` und nur wenn auch `folge`/`weiterAb` nicht getroffen haben: der Tipp auf die Album-Kachel (titelNr 0) spielt unveraendert von vorn, sonst verstummte eine Box, die vorher Ton hatte. (2) Das AUSWEICHEN SELBST schreibt eine Zeile (`E117: <schluessel> — <dienst> spielt nicht (<grund>), weiter zur naechsten Quelle`). Zeuge: src/backend-api/src/rueckfall-titelwunsch.integration.spec.ts (1 lokaler + 11 Anbieter-Titel, Wunsch 3); Gegenprobe gefahren, die Bedingung stillgelegt -> 'Kapitel 1 statt 3 gestartet (dienst=lokal)', also woertlich der Geraetebefund, waehrend der Grenzfall-Test gruen blieb"
    tags: [e117, e110, spielfunktion, server, verschmelzung, soloist, protokoll, 2026]
    related: [anzeige-und-ton-muessen-dieselbe-quelle-waehlen, lokaler-ton-ueber-die-liste-nicht-ueber-die-m3u, der-titel-ist-das-kleinste-teil]
    body: |
      ══ DIE REGEL, DIE DARAUS FOLGT ══════════════════════════════════════════
      EINE ENTSCHEIDUNG ZU PROTOKOLLIEREN IST NICHT DASSELBE WIE IHRE
      AUSFUEHRUNG ZU PRUEFEN. E110 hat hier alles richtig gemacht: die Wahl
      fiel auf Spotify, sie wurde in `wahl.quellen` zurueckgeschrieben (kein
      „nur gemeldet"), und die Zeile stand im Journal. Was FEHLTE, war die
      Zeile daneben — dass dieser Sieger gleich darauf scheiterte und das
      Ausweichen bei einer aermeren Quelle ankam.

      Damit war das Protokoll schlimmer als gar keines: Es sagte „spielt
      spotify", waehrend lokal spielte, und schickte die Fehlersuche in die
      Naht zwischen Wahl und Start, wo nichts kaputt war. Wer eine WAHL
      aufschreibt, muss auch ihr SCHEITERN aufschreiben; sonst belegt das
      Journal eine Absicht und niemand merkt, dass die Wirklichkeit abbog.

      ZWEITE REGEL, unabhaengig von der ersten: EIN RUECKFALL DARF DEN WUNSCH
      NICHT UMDEUTEN. Ausweichen heisst „dieselbe Sache aus anderer Quelle",
      nicht „etwas anderes aus dieser Quelle". Eine Quelle mit weniger Titeln
      ist fuer „Kapitel 3" keine Quelle — auf einer Ein-Titel-Liste hat
      Kapitel 3 kein sinnvolles Bild, und still Kapitel 1 zu spielen ist die
      schlechteste aller Antworten (dieselbe Einsicht wie bei `folgeWaehlen`
      Stufe 3: „oeffnet immer den ersten titel ganz egal was ich tippe").

      ══ DAS TOR, DAS NUR MANCHMAL ZUSCHLAEGT ═════════════════════════════════
      Der Grund, warum das so lange wie ein Verschmelzungs-Fehler AUSSAH: die
      Spotify-Bereitschaftsfrage haengt an `titelNr > 0 || positionMs > 0`.
      Die Album-Kachel schickt keine Nummer und passiert das Tor; erst der
      Tipp auf ein KAPITEL fragt nach — und faellt. Ein Werk, das „als Ganzes
      spielt, aber bei jedem Kapitel spinnt", ist deshalb ein Hinweis auf
      dieses Tor und nicht auf die Titelliste.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
      Am Geraet, bevor man im Code sucht — die Reihenfolge spart die Stunde:
        curl -s http://localhost:8200/api/spotify/bereit
      Kommt `{"bereit":false,"grund":"box-fehlt"}`, ist Spotify fuer die Box
      gar nicht ansprechbar; dann sagt
        curl -s http://localhost:8200/api/spotify/web/me/player/devices
      welche Geraete das Konto ueberhaupt sieht (steht dort nur Fremdes, ist
      die eigene Maschine nicht angemeldet). Bei `engine = "soloist"` ist
      `systemctl is-active soloist` NICHT die Antwort auf diese Frage — der
      Dienst laeuft auch unangemeldet. Die Antwort gibt:
        soloist ctl status -D /var/lib/soloist    # -> „logged in: no"
      Und im Journal (mit `grep -v python3`, sonst deckt der MuPiHAT-Logger
      alles zu) stehen jetzt beide Haelften: die E110-Wahl und daneben die
      E117-Zeile, warum sie nicht gehalten hat.

      ══ ACHTUNG, DIE ABMELDUNG WAR NUR DER AUSLOESER ══════════════════════════
      Noch am selben Tag meldete der Betreiber „spotify ist zurueck", und
      nachgemessen stimmte es: `bereit: true`, zwei Geraete, `logged in: yes`,
      `active device: yes`. WER DIESEN EINTRAG SPAETER FINDET, JAGE ALSO NICHT
      DIE SOLOIST-ANMELDUNG — sie war der Ausloeser, nicht der Fehler.

      Denn mit erreichbarem Spotify passiert der Kapitel-Tipp das Tor, E110
      spielt Spotify, und der lokale Rueckfall wird nie erreicht: DAS SYMPTOM
      VERSCHWINDET VON SELBST, DER FEHLER BLEIBT. Er schlaeft nur, bis Spotify
      das naechste Mal nicht erreichbar ist (Abmeldung, Netz, Drosselung,
      Token) — und sieht dann wieder genauso aus. Das ist die eigentliche
      Gemeinheit dieser Bauart: sie heilt scheinbar, ohne dass jemand etwas
      repariert hat, und kommt beim naechsten Ausfall als vermeintlich neuer
      Fehler zurueck.

      ══ ZWEI SITZUNGEN, DERSELBE FEHLER — UND WAS DARAUS WURDE ═══════════════
      Am 04.09.2026 haben ZWEI Sitzungen diesen Fehler unabhaengig gefunden
      und beide richtig behoben; gemerkt wurde es erst beim Trockenlauf des
      Ausrollens, als die fremde Behebung schon im laufenden `server.js`
      stand. Zusammengefuehrt statt eine weggeworfen:

        * Das URTEIL ist `wunschVerfehlt(titelNr, gewaehlt)` in
          spielfunktion.ts — rein, mit eigenen Faellen geprueft. Die andere
          Fassung war eine gleichwertige Bedingung INNERHALB der Route und
          damit nur ueber den ganzen Abspielweg testbar.
        * Die PROTOKOLLZEILEN und der Integrationszeuge kamen von der
          anderen Seite. Vor allem die Zeile beim AUSWEICHEN fehlte der
          zuerst ausgelieferten Fassung — und genau sie ist der Grund,
          warum die Suche einen halben Tag in die falsche Naht lief.

      Die Lehre steht in [[arbeitsablauf-der-sitzung]] Schritt 1: Nachschlagen
      heisst in diesem Baum auch „arbeitet hier gerade jemand anders daran?".
      Ausgeliefert ist der zusammengefuehrte Stand; am Geraet nachgemessen
      traegt `server.js` seither `E117:` zweimal, `titel-nicht-in-quelle` und
      `weiter zur naechsten Quelle` je einmal.
  - id: arbeitsablauf-der-sitzung
    kind: regel
    title: "Der Ablauf einer Sitzung: erst Wiki, dann Stand von draussen, dann Werkzeuge — bauen kommt spaet"
    match: "wie fange ich an|arbeitsablauf|workflow|reihenfolge der sitzung|erst wiki|womit anfangen|wir wandern vom workflow ab|sitzungsablauf"
    sev: high
    cause: "Betreiber am 04.09.2026, nach der E117-Sitzung: 'ich glaube wir wandern von unserem workflow ab erst wiki, auch internet nach aktuellem stand befragen wenn es sinn ergibt, tools checken ob vorhanden, falls tools benoetigt werden bauen und speichern, doku, wiki nachziehen.' Er hatte recht, und die Ursache lag im Baum: Die EINZELREGELN standen alle im Paket (werkzeug-statt-wegwerfbefehl, tools-verzeichnis-vor-eigenbau, gegenprobe-statt-gruen-glauben, wiki-immer-nachziehen), ihre REIHENFOLGE aber nirgends — und `CLAUDE.md`, die einzige Datei, die eine Sitzung ungefragt liest, gab es gar nicht. Wer die Teile kennt, aber nicht den Ablauf, springt in den Code und holt das Nachschlagen spaeter nach, wenn es nichts mehr spart. In derselben Sitzung wurde der Preis sichtbar: eine NACHBARSITZUNG hatte denselben Fehler (E117) parallel geloest und bereits ausgeliefert; beide Sitzungen haben ihr Wissen nicht geteilt, weil keine zuerst gefragt hat"
    fix: "Die sechs Schritte stehen jetzt an zwei Orten, und beide werden gebraucht: `CLAUDE.md` im Wurzelverzeichnis (wird zu Beginn JEDER Sitzung gelesen, also die einzige Stelle, die von selbst wirkt) und dieser Eintrag (durchsuchbar, mit Begruendung). Damit sie nicht auseinanderlaufen, wacht `tools/arbeitsablauf-deckung.py` QUER darueber: sie vergleicht die Marken `SCHRITT <n>: <kennung>` in beiden Dateien auf gleiche Menge UND gleiche Reihenfolge, haengt in `tools/doku-luecken-probe.sh` und hat ABSICHTLICH keine eigene Meinung darueber, welche Schritte es geben soll — sonst waere sie ein dritter Ort, der mitgepflegt werden muesste und es nicht wuerde. Fehlt die Marke ganz, ist das ein Befund und kein Freispruch"
    tags: [methodik, ablauf, sitzung, wiki, werkzeuge, doku, claude-md, 2026]
    related: [werkzeug-statt-wegwerfbefehl, gegenprobe-statt-gruen-glauben, wer-nichts-tut-muss-sagen-warum, der-rueckfall-deutete-den-titelwunsch-still-um]
    body: |
      ══ DIE SECHS SCHRITTE ═══════════════════════════════════════════════════

      SCHRITT 1: wiki-zuerst — Erst das Wissenspaket fragen, dann den Code

        `python3 tools/wiki-suche.py --text "<begriff>"` (auch --tag, --kind,
        --id … --lang). Vor JEDER Untersuchung und JEDEM Umbau. Das Paket ist
        1,4 MB YAML: mit `grep` findet man die Zeile, aber nicht den Eintrag,
        zu dem sie gehoert — genau dafuer gibt es das Werkzeug. Die teuerste
        Minute einer Sitzung ist die, in der jemand etwas herausfindet, das
        laengst im Paket steht.

      SCHRITT 2: stand-von-draussen — Das Internet fragen, wo sich draussen
        etwas aendert

        Sinnvoll bei fremden Schnittstellen, Bibliotheksfassungen,
        Fehlermeldungen aus Fremdcode, Hardware-Eigenheiten. Nicht sinnvoll
        bei allem, was nur in diesem Baum gilt. WER IHN UEBERSPRINGT, SAGT
        WARUM — dieselbe Pflicht wie in [[wer-nichts-tut-muss-sagen-warum]];
        sonst ist hinterher nicht zu unterscheiden, ob jemand geprueft oder
        vergessen hat.

      SCHRITT 3: werkzeug-suchen — Vor dem Eigenbau `tools/` durchsuchen

        `ls tools/ | grep -i <sache>`, dann `--help`. Fast jede Handschleife
        gibt es schon, und meist besser als den frisch getippten Nachbau: die
        vorhandenen Werkzeuge tragen die Sonderfaelle, ueber die schon einmal
        jemand gestolpert ist.

      SCHRITT 4: werkzeug-bauen — Fehlt eines, wird es gebaut UND gespeichert

        Untersuchen heisst: eine Datei in `tools/`, die ein Zweiter
        wiederverwenden kann ([[werkzeug-statt-wegwerfbefehl]]). Und sie
        gehoert in einen LAEUFER, meist `tools/doku-luecken-probe.sh` — eine
        Wache, die niemand ruft, rostet unbemerkt und ist keine Wache.

      SCHRITT 5: doku — Erst dann bauen, und das Gebaute dokumentieren

        Eine Handlungsanweisung ist Doku, egal in welcher Datei sie steht,
        auch ein `print()` mit einem Befehl darin. Zustandsaussagen bekommen
        ein DATUM, damit ein spaeterer Leser sie nachmisst statt ihnen zu
        glauben.

      SCHRITT 6: wiki-nachziehen — Der Fund ins Paket, im selben Zug wie der
        Commit

        Als Textanhang ueber `tools/wiki-anhaengen.py`, danach
        `tools/pack-verweise.py`. Niemals das Paket neu formatieren
        (`safe_dump` schreibt 2,5 MB um, und der Diff eines einzigen Eintrags
        ist danach vierstellig). Was erst 'spaeter' ins Wiki soll, kommt nie
        hinein — und die naechste Sitzung sucht von vorn.

      ══ WARUM DIE REIHENFOLGE KEIN GESCHMACK IST ═════════════════════════════
      Jeder Schritt macht den naechsten billiger, und zwar nur in dieser
      Richtung. Wer zuerst baut, hat den Preis schon bezahlt, wenn er
      nachschlaegt: Das Wissen aendert dann nichts mehr an der Entscheidung,
      es bestaetigt oder aergert nur noch. Genau daran ist am 04.09.2026 ein
      halber Tag doppelt bezahlt worden.

      ══ DER TEUERSTE EINZELFALL, DER DARAN HAENGT ════════════════════════════
      Es laufen MEHRERE SITZUNGEN parallel in diesem Baum. Schritt 1 ist
      deshalb nicht nur Nachschlagen, sondern auch die Frage 'arbeitet hier
      gerade jemand anders daran?' — `git log --oneline -5 main`, die Zweige
      mit frischem Datum, und vor dem Ausliefern der Blick auf die BOX. Am
      04.09.2026 hatten zwei Sitzungen denselben Fehler gefunden, beide
      richtig behoben, und eine hatte bereits ausgeliefert; gemerkt wurde es
      erst beim Trockenlauf des Ausrollens, als die fremde Behebung schon im
      laufenden `server.js` stand.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
        python3 tools/arbeitsablauf-deckung.py
      Gruen heisst: `CLAUDE.md` und dieser Eintrag nennen dieselben Schritte
      in derselben Reihenfolge. Die Wache prueft NUR das — was ein Schritt
      bedeutet, steht hier und nirgendwo sonst.
  - id: der-wechselpunkt-ist-vorhersagbar-weil-die-liste-bekannt-ist
    kind: architektur
    title: "Die Vorhersage fuer das Cue: linear ist wissbar, freie Wahl kostet den Versatz"
    match: "wechselpunkt|vorhersage|prefetch spotify|naechster titel vorladen|cue spannen|maschinenwechsel vorher wissen|tonmaschine|E108 Stufe 3"
    sev: med
    cause: "Betreiber, 04.09.2026: 'wir kennen ja die liste' — und daran haengt die Machbarkeit von E108 Stufe 3. Das PipeWire-Cue (E114) kann den 1-2-s-Maschinenwechsel nur verstecken, wenn es VOR dem Uebergang gespannt wird: Starten, Puffern und Springen kosten genau die Zeit, die zu verbergen ist. Ein Cue, das erst AM Uebergang merkt, dass die Maschine wechselt, kommt zu spaet. Zweitens fehlte die Grundlage im Code: die Regel 'Spotify ist eine eigene Maschine, alles andere laeuft ueber mpv' stand als PROSA an mindestens drei Stellen (titelkarte.ts, server.ts /inhalt, mpv-wrapper.ts) und nirgends als Funktion — dieselbe Bauart, die als E110 (zwei Wahrheiten ueber ein Werk) und E111 (zwei Listen derselben Quelle) schon zweimal bezahlt wurde"
    fix: "Drei reine Bausteine in spielfunktion.ts, ohne jeden Eingriff in den Tonweg. (1) `tonmaschine(dienst)` — der eine Ort fuer die Maschinenfrage; Spotify ist die einzige Ausnahme, alles andere ist mpv, und ein UNBEKANNTER Dienst ist mpv. Diese Vorgabe ist Absicht: ein faelschlich als mpv gefuehrter Dienst kostet den Wechsel, den es ohnehin gaebe, ein faelschlich als Spotify gefuehrter risse ein stummes Fenster fuer nichts auf. (2) `wechselpunkte(titel)` — alle Uebergaenge einer Mischliste, die die Maschine wechseln. (3) `wechselNach(titel, nr)` — die Frage waehrend der Wiedergabe: steht am Ende DIESES Titels ein Wechsel? Gegenprobe zweimal gefahren: `tonmaschine` auf 'mpv' festgenagelt -> 4 Tests quer ueber alle drei Bloecke rot; den Nachfolger ueber nr+1 statt ueber die Position gesucht -> genau der Halb-Mitschnitt-Test rot"
    tags: [e108, e114, cue, spielfunktion, vorhersage, spotify, mpv, architektur, 2026]
    related: [der-titel-ist-das-kleinste-teil, spotify-cue-link-trennen-statt-node-muten, dienste-umschalten, lokaler-ton-ueber-die-liste-nicht-ueber-die-m3u]
    body: |
      ══ DIE ENTSCHEIDUNG: NUR LINEAR ═════════════════════════════════════════
      Betreiber, 04.09.2026: „falls frei gewaehlt wird gibt es eben einen
      versatz von 1-2s."

      Vorhersagbar ist genau EIN Fall: der Titel laeuft aus, der naechste der
      Liste beginnt. Ein TIPP auf ein beliebiges Kapitel ist es nicht — er
      kommt in dem Moment, in dem das Kind ihn macht, und ein Cue braucht
      seinen Vorlauf. Dort bleibt es beim Versatz, und das ist kein Rest,
      sondern der bezahlte Preis fuer freie Wahl.

      DIESELBE GRENZE HAT MPV: `--prefetch-playlist=yes` laedt beim ERREICHEN
      des Endes und nur den naechsten Eintrag. Die Vorhersage kopiert damit
      kein fremdes Verhalten, sie holt Spotify auf denselben Stand.

      WER ES SPAETER AUFWEICHEN WILL, sollte den Preis kennen: Ein Cue „auf
      Verdacht" muesste den Spotify-Zweig dauerhaft stumm halten oder bei
      jedem Tipp raten. Das erste macht die Box stumm, wenn das Cue haengt;
      das zweite reisst Fenster fuer Uebergaenge auf, die nie kommen. Beides
      ist teurer als 1-2 s an einer Stelle, an der ohnehin gerade jemand
      bewusst etwas Neues gewaehlt hat und auf Ton wartet.

      ══ ZWEI ZAEHLUNGEN, UND SIE SIND NICHT DASSELBE ═════════════════════════
      Gegangen wird ueber die POSITION in der Liste, benannt wird ueber die
      `nr`. Bei einem Halb-Mitschnitt traegt die Liste 01, 02, 06, 08 — der
      Nachfolger von 02 ist dort 06 und NICHT 03. Wer `nr + 1` rechnet, baut
      denselben Fehler ein, der als E110/E111 zweimal bezahlt wurde und im
      E111-Eintrag als offene Fussnote genau fuer diesen Fall vermerkt ist.
      Der Test „DER HALB-MITSCHNITT" haelt es fest; die Gegenprobe mit nr+1
      macht genau ihn rot und sonst keinen.

      Titel OHNE `quelle` werden UEBERSPRUNGEN, nicht geraten: `titelMischen`
      laesst das Feld weg, wenn keine Quelle den Titel hat. So einer wird gar
      nicht gespielt — ihn mitzuzaehlen erfaende einen Uebergang, den es nicht
      gibt, und das Cue spannte ins Leere.

      ══ WAS DAMIT NOCH NICHT GEBAUT IST ══════════════════════════════════════
      Diese drei Funktionen SAGEN nur, wo der Wechsel liegt. Sie rufen
      nichts, sie fassen den Ton nicht an. Es fehlen weiterhin, in dieser
      Reihenfolge (BACKLOG E114):

        1. der GERAETEBEWEIS des Cue (`node tools/cue-messen.mjs`, dann
           `--probe`, bei spielendem Spotify) — samt der Frage, ob es beim
           Link-Trennen KNACKT, was beim Mute nachweislich nicht der Fall war;
        2. die Verdrahtung in spotify-control.ts: Plan holen, `stumm()`,
           Titel starten/pausieren/`seek(0)`, `klar()`, Signalgriff.

      Und erst wenn beides steht, darf `titelMischen` die Spotify-Grenze
      ueberhaupt titelweise ueberschreiten — heute stempelt es dort nur
      (GANZ-oder-GAR-NICHT, [[anzeige-und-ton-muessen-dieselbe-quelle-waehlen]]).

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
      Rein und ohne Geraet:
        npx tsx --test src/backend-api/src/spielfunktion.spec.ts
      Am Geraet sichtbar wird die Vorhersage erst mit der Verdrahtung; bis
      dahin ist sie ausdruecklich totes, aber geprueftes Holz.
  - id: die-ganze-tonstrecke-und-wo-die-pegel-entstehen
    kind: architektur
    title: "Die Tonstrecke am Stueck: zwei Zulaeufe, ein Klangwerk, ein Regler — und wo man richtig misst"
    match: "tonstrecke|signalweg|wo wird lauter|lautstaerke sprung|krasser tonanstieg|pegel messen|klangwerk monitor|entzerrer|welcher messpunkt|tonweg dokumentation"
    sev: med
    cause: "Betreiber, 04.09.2026, waehrend der Messungen zu E108 Stufe 3: 'jetzt ist gerade ein krasser ton anstieg passiert', danach 'vielleicht sollten wir das ueber die gesamte tonstecke dokumentieren'. Der Anlass war berechtigt, die erste Erklaerung falsch: Ich mass den lokalen Weg (Effektiv 9860) gegen den Spotify-Weg (3369), schloss auf +9 dB durch den Entzerrer und hielt es fuer einen Fehler der frisch gebauten Mischwiedergabe. Gemessen wurden dabei aber ZWEI VERSCHIEDENE KAPITEL. Mit demselben Kapitel ueber beide Wege stehen 11389 (lokal) gegen 12491 (Spotify) — die Wege sind gleich laut, der Unterschied lag im Material"
    fix: "Die Strecke einmal am Stueck aufgeschrieben (Rumpf), samt der Regel, WO gemessen wird: `klangwerk.monitor` liegt VOR dem Lautstaerkeregler und ist damit der einzige Punkt, an dem sich zwei Zulaeufe vergleichen lassen; `alsa_output….monitor` liegt DAHINTER und liest bei kindgerechter Lautstaerke jeden Vollausschlag als Stille (am selben Tag gemessen: 32767 gegen 21 bei 7 % Regler). Und die Pegelmessung selbst geht ueber `parec --device=<senke>.monitor`, nicht ueber `pw-record --target …` — letzteres misst hier immer Stille"
    tags: [ton, pipewire, klangwerk, entzerrer, pegel, messen, spotify, mpv, e108, e114, 2026]
    related: [dienste-umschalten, spotify-cue-link-trennen-statt-node-muten, messinstrument-gegen-bekanntes-signal, einmal-hinsehen-ist-keine-messung]
    body: |
      ══ DIE STRECKE, am Geraet aus `pw-dump` gelesen (04.09.2026) ════════════

        ALLE Quellen                    ┐
          mpv      (lokal, jellyfin, ard,│
                    radio, rss, plugin,  ├──> klangwerk ──> klangwerk.ausgang
                    vorlesen)            │    (mischende Senke)      │
          soloist  (Knoten heisst        │                           v
                    „spotify")           ┘                  [ entzerrer ]
                        │                                   Station, DERZEIT AUS
                        │                                           │
                        └──> mixpi-mitschnitt                        v
                             (eigene Kante JE QUELLE,        alsa_output.…
                              vor allem anderen)                     │
                                                                     ├─> Lautsprecher
                                                                     └─> mixpi-pegel

      DREI DINGE, DIE MAN DARAN ABLESEN KANN:

      1. BEIDE MASCHINEN LAUFEN BIS ZUM KLANGWERK GETRENNT. Nur deshalb ist
         der Spotify-Zweig einzeln schaltbar — die Grundlage des Cue (E114).
      2. DER ENTZERRER IST EINE SCHALTBARE STATION HINTER DEM KLANGWERK —
         UND ER IST AUS. Gemessen (04.09.2026): `klang.json` fuehrt
         `entzerrer.an = false`, also nimmt `stationen()` (ton.ts) ihn nicht
         in die Kette, und NIEMAND speist ihn. Im Graphen steht deshalb
         `mpv --> klangwerk` und `spotify --> klangwerk` nebeneinander; die
         eingestellten Baender (tief +5, tmitte +4, mitte +3, hmitte +1,
         hoch -2) wirken auf gar nichts.

         WEIL DIE KETTE HINTER DER MISCHENDEN SENKE LIEGT, bekommt JEDE
         Quelle den Entzerrer, sobald er an ist — lokal, Jellyfin, ARD,
         Radio, RSS, Plugin, Vorlesen und Spotify, und jede kuenftige dazu.
         Es braucht keine Verdrahtung je Quelle. `DURCHGANG_SENKEN =
         [klangwerk, entzerrer]`; die Reihenfolge ist begruendet (Stereobasis
         auf dem GEMISCHTEN Signal, Entzerrung danach auf das Ergebnis).

         DER FEHLSCHLUSS, DEN MAN HIER MACHT: Im Graphen steht eine Kante
         `entzerrer.ausgang --> klangwerk`. Daraus zu schliessen, mpv liefe
         durch den Entzerrer, ist falsch — man muss fragen, wer seinen
         EINGANG speist. Am 04.09. ist mir genau das zweimal passiert.
      3. DER MITSCHNITT HAENGT VOR DEM KLANGWERK, direkt am Spotify-Knoten.
         Deshalb trennt das Cue den LINK zum Klangwerk und mutet nicht den
         Knoten: ein Mute traefe die Aufnahme mit.

      ══ WO GEMESSEN WIRD, UND WARUM DORT ═════════════════════════════════════

        klangwerk.monitor        VOR dem Lautstaerkeregler.
                                 -> zum VERGLEICHEN zweier Zulaeufe,
                                    unabhaengig vom Regler.
        alsa_output.….monitor    HINTER dem Regler.
                                 -> nur fuer „kommt beim Lautsprecher etwas an".

      Am 04.09. gleichzeitig gemessen, bei voll ausgesteuertem Ton und 7 %
      Regler: klangwerk.monitor 32767, alsa_output 21. Wer am falschen Punkt
      misst, haelt eine leise Box fuer eine stumme und sucht den Fehler in
      Spotify.

      DAS MESSWERKZEUG: `parec --device=<senke>.monitor`, Fenster ~3 s.
      `pw-record --target <senke>` misst hier immer Stille, mit und ohne die
      Endung `.monitor` (im selben Augenblick: parec 25757, pw-record 32).
      Kurze Scheiben liefern nichts, weil parec erst anlaufen muss.

      ══ WAS EINEN LAUTSTAERKESPRUNG ERKLAERT — UND WAS NICHT ═════════════════

      NICHT der Wechsel der Quelle. Dasselbe Kapitel ueber beide Wege, am
      selben Punkt gemessen:

        Kapitel 2 lokal     Effektiv 11389
        Kapitel 2 Spotify   Effektiv 12491

      SEHR WOHL das Material. Innerhalb DESSELBEN Hoerspiels:

        Kapitel 2           Effektiv 11389
        Kapitel 5           Effektiv  3369     (~10 dB leiser)

      Ein Sprung von Kapitel 5 auf Kapitel 2 ist damit ein hoerbarer Satz
      nach oben, ohne dass irgendetwas kaputt waere. WER EINEN
      LAUTSTAERKESPRUNG UNTERSUCHT, MISST DESHALB ZUERST DASSELBE STUECK ueber
      beide Wege — sonst misst er den Inhalt und nennt es einen Fehler. Genau
      dieser Fehlschluss ist mir an diesem Tag unterlaufen.

      OFFEN UND UNGEKLAERT: Beide Wege erreichen Spitze 32768, laufen also am
      Anschlag. Ob dabei geklippt wird, ist NICHT gemessen; eine
      Pegelanpassung (Normalisierung ueber die Kapitel) gibt es nirgends.
      Fuer eine Kinderbox waere sie das Naechstliegende — die Zahlen oben
      sagen, dass sie fehlt, nicht dass sie einfach ist.
  - id: der-mitschnitt-zapft-die-quelle-an-nie-eine-senke
    kind: regel
    title: "Der Mitschnitt bleibt PUR: angezapft wird der Quellknoten, niemals eine Senke"
    match: "mitschnitt pur|aufnahme entzerrt|doppelt entzerrt|wo zapft die aufnahme an|mitschnitt senke|archiv klingt anders|aufnahme mit klangregelung"
    sev: high
    cause: "Betreiber, 04.09.2026, waehrend der Ueberlegung, alle Quellen durch den Entzerrer zu fuehren: 'mitschnitt bleibt bei allen quellen auch zukuenftigen pur'. Die Anforderung ist die richtige, und sie ist leicht zu verletzen: Eine Aufnahme an einer SENKE abzugreifen ist bequem (eine Stelle statt einer je Quelle) und waere an zwei Stellen zugleich falsch"
    fix: "So ist es gebaut, und so muss es bleiben — am Geraet nachgemessen (04.09.2026): `spotify [Stream/Output/Audio] --> mixpi-mitschnitt`, je zwei Kanten (FL/FR) auf `mixpi-mitschnitt` und auf `mixpi-mitschnitt-arbeiter-2`. Angezapft wird der AUSGANG DES QUELLKNOTENS, parallel zum Abspielweg, VOR jeder Senke. Wer den Mitschnitt auf weitere Quellen ausdehnt, legt fuer jede eine eigene Kante von deren Ausgang — nicht eine gemeinsame von einer Senke"
    tags: [mitschnitt, aufnahme, pipewire, ton, archiv, entzerrer, klangwerk, regel, 2026]
    related: [die-ganze-tonstrecke-und-wo-die-pegel-entstehen, spotify-cue-link-trennen-statt-node-muten, der-titel-ist-das-kleinste-teil]
    body: |
      ══ ZWEI GRUENDE, UND BEIDE SIND ENDGUELTIG ══════════════════════════════

      1. EINE SENKE FUEHRT DEN MIX. An ihr haengen alle Quellen zugleich
         (`klangwerk` ist genau das).
         Eine Aufnahme von dort enthielte, was sonst noch lief — die Ansage
         des Vorlesens, den zweiten Strom, das Fehlerpiepsen. Ein Archiv, das
         Fremdes mitschneidet, ist kein Archiv.

      2. EINE SENKE LIEGT HINTER DER BEARBEITUNG. Der mpv-Weg geht ueber den
         Klangwerk und danach — sobald er eingeschaltet wird — ueber den
         Entzerrer. Eine dort abgegriffene Aufnahme traegt diese Bearbeitung
         EINGEBRANNT — und weil sie beim Abspielen denselben Weg noch einmal
         nimmt, wird sie ZWEIMAL entzerrt. Der Fehler waechst mit jeder
         Reglerstellung, ist im Nachhinein nicht mehr herausrechenbar, und
         man hoert ihn erst, wenn das Archiv schon voll ist.

      ══ WAS DAS FUER DEN GEPLANTEN UMBAU HEISST ══════════════════════════════
      Der Plan, ALLE Quellen durch den Entzerrer zu fuehren (damit auch
      Spotify die Klangregelung bekommt und kuenftige Quellen sie nicht
      wieder umgehen), aendert die ABSPIELSEITE. Die Aufnahmeseite darf er
      NICHT anfassen: Die Kante `<quelle> --> mixpi-mitschnitt` bleibt, wo
      sie ist, egal in welche Senke die Wiedergabe danach laeuft.

      Praktisch heisst das: Der Mitschnitt haengt an einer Eigenschaft der
      QUELLE, nicht an einer Stelle der Kette. Wer eine neue Abspielmaschine
      einbaut, verdrahtet zwei Dinge — ihren Weg zur Senke und ihre Kante zum
      Mitschnitt. Das zweite vergisst man leicht, weil ohne es alles
      funktioniert; auffallen wuerde es erst daran, dass die Selbstheilung
      fuer diese Quelle nie einsetzt.

      ══ WARUM DAS CUE DAVON ABHAENGT ═════════════════════════════════════════
      Genau diese Trennung ist der Grund, warum das Cue den LINK zur Senke
      trennt und nicht den Knoten mutet: Ein Mute wirkt am Knoten und traefe
      alle seine Abgriffe, also auch den Mitschnitt — es risse ein stilles
      Loch in das Archiv, aus dem die Selbstheilung lebt. `tonpfad.ts` fuehrt
      die Mitschnitt-Kanten deshalb ausdruecklich als `geschont`.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
      Bei laufender Wiedergabe, auf der Box:

        pw-link -l | grep mitschnitt

      Links der Pfeile muss der QUELLKNOTEN stehen (`spotify`, spaeter auch
      andere), niemals `klangwerk`, `entzerrer` oder eine `alsa_output`-Senke.
      Steht dort eine Senke, ist diese Regel gebrochen und das Archiv ab
      diesem Zeitpunkt bearbeitet.
  - id: currentplayer-mplayer-ist-ein-altname
    kind: falle
    title: "`currentPlayer: mplayer` sagt NICHT, welche Maschine spielt — es laeuft mpv"
    match: "warum mplayer|currentPlayer mplayer|welcher player laeuft|mplayer oder mpv|playerEngine|Tonmaschine mplayer"
    sev: med
    cause: "Betreiber, 04.09.2026, beim Mitlesen einer Messung: 'ähm warum mplayer?' Berechtigt: `/local` meldet `currentPlayer: 'mplayer'`, waehrend im PipeWire-Graphen ein Knoten `mpv` steht. Nachgemessen laeuft wirklich mpv (`mpv --idle=yes --no-video --prefetch-playlist=yes …`), ein mplayer-Prozess existiert gar nicht"
    fix: "Es ist ein ALTNAME, kein Fehler. `spotify-control.ts` fuehrt `type Tonmaschine = 'spotify' | 'mplayer'` — zwei Werte, und 'mplayer' bedeutet darin 'der Datei-Spieler', unabhaengig von der Maschine. WELCHE laeuft, entscheidet `mupibox.playerEngine` in mupiboxconfig.json (`createPlayer = playerEngine === 'mpv' ? require('./mpv-wrapper') : require('./mplayer-wrapper')`); auf dieser Box steht `\"playerEngine\": \"mpv\"`. Wer wissen will, was spielt, fragt `ps` oder den PipeWire-Knoten — nicht das Zustandsfeld"
    tags: [player, mpv, mplayer, abspieldienst, altname, falle, 2026]
    related: [dienste-umschalten, die-ganze-tonstrecke-und-wo-die-pegel-entstehen]
    body: |
      ══ WAS DAS FELD KANN UND WAS NICHT ══════════════════════════════════════
      `currentPlayer` beantwortet die Frage „Datei oder Spotify?" — und nur
      die. Es beantwortet NICHT „welches Programm laeuft". Seit dem Umstieg
      auf mpv sind das zwei verschiedene Fragen, und das Feld ist bei der
      alten Antwort stehengeblieben.

      UND `/local` IST NUR DER KANAL DES DATEI-SPIELERS. Bei laufendem
      Spotify stehen dort `playing: false` und ein leerer
      `currentTrackname` — das ist kein Fehler, sondern die Aufteilung:
      Spotify hat seinen eigenen Kanal `/state` (Titel, Album, Dauer,
      `is_playing`), und die Oberflaeche liest ihn auch von dort
      (`toNowPlaying`: erst `istDateiWiedergabe`, sonst `sp.item.name`). Am
      04.09.2026 habe ich `/local`s Bedeutung auf Spotify uebertragen und
      eine Luecke gemeldet, die es nicht gab.

      Verlaesslich ist:

        curl localhost:5005/state    # Spotify: Titel, Dauer, is_playing
        curl localhost:5005/local    # Datei-Spieler: dasselbe fuer mpv
        ps -eo comm,args | grep -E 'mpv|mplayer'
        pactl list short sink-inputs        # der Knoten heisst `mpv`
        grep playerEngine /etc/mupibox/mupiboxconfig.json

      ══ ZWEI VOKABULARE FUER DIESELBE SACHE ══════════════════════════════════
      Seit E108 Stufe 3 gibt es daneben `tonmaschine()` in spielfunktion.ts,
      und die liefert `'mpv' | 'spotify'`. Der Abspieldienst fuehrt
      `'spotify' | 'mplayer'`. Beide meinen dasselbe.

      HEUTE HARMLOS, WEIL SIE SICH NICHT BEGEGNEN: `uebergabeWachen`
      (server.ts) prueft `playing` und `currentTracknr`, nicht
      `currentPlayer`. Wer die beiden Vokabulare aber einmal gegeneinander
      vergleicht — etwa um zu entscheiden, ob gewechselt werden muss —,
      bekommt fuer denselben Zustand zwei verschiedene Woerter und einen
      Vergleich, der nie zutrifft. Das ist die Fehlerfamilie aus E110/E111,
      eine Ebene hoeher: zwei Namen fuer eine Wahrheit.

      ══ WAS MAN TUN KOENNTE, UND WARUM ES NICHT UMSONST IST ══════════════════
      Das Feld umzubenennen sieht billig aus und ist es nicht: `'mplayer'`
      steht in `/local`, das die Oberflaeche liest, und in `resume.json`
      landet der Wert mittelbar ueber die Buchfuehrung. Wer umbenennt, muss
      beide Leser mitnehmen und alte Dateien vertragen. Solange das nicht
      geschieht, ist der Altname zu DOKUMENTIEREN statt zu glauben.
  - id: drei-skalen-fuer-eine-lautstaerke
    kind: falle
    title: "pactl, wpctl und channelVolumes zeigen DREI Skalen — wer sie mischt, beweist alles"
    match: "lautstaerke misst falsch|channelVolumes|wpctl get-volume|pactl get-sink-volume|kubisch|0.614138|welche skala|pegel stimmt nicht"
    sev: high
    cause: "Am 04.09.2026 bei der Suche nach E94 zweimal in die Irre gefuehrt. Dieselbe Senke, im selben Augenblick: `pactl get-sink-volume` meldete 100 %, `wpctl get-volume` meldete 1.00, und `channelVolumes` aus `pw-dump` stand auf 0.614138. Aus der letzten Zahl schloss ich, eine Aenderung habe gewirkt (0,85 hoch 3 = 0,614) — und baute die naechste Ueberlegung darauf"
    fix: "channelVolumes ist KUBISCH (der Wuerfel des Bruchs), `wpctl` zeigt den BRUCH (0..1), `pactl` zeigt PROZENT. Wer die Wirkung eines `set` prueft, misst mit `wpctl get-volume <knoten-id>` — eine Skala, ein Werkzeug, und ueber die ID statt ueber den Namen (der Name kann mehrdeutig sein, die ID nicht). Fuer den Vergleich zweier Stufen gilt dieselbe Regel wie beim Ton: erst das Messgeraet gegen ein bekanntes Signal pruefen"
    tags: [ton, lautstaerke, pipewire, pactl, wpctl, messen, falle, e94, 2026]
    related: [die-ganze-tonstrecke-und-wo-die-pegel-entstehen, messinstrument-gegen-bekanntes-signal, einmal-hinsehen-ist-keine-messung]
    body: |
      ══ DIE DREI ZAHLEN, am Geraet gleichzeitig gemessen ═════════════════════

        pactl get-sink-volume <name>     100%        Prozent
        wpctl get-volume <id>            1.00        Bruch 0..1
        pw-dump … channelVolumes[0]      0.614138    KUBISCH (0,85^3)

      Drei Zahlen, eine Senke, derselbe Augenblick. Keine ist falsch — sie
      beantworten verschiedene Fragen, und keine sagt dazu, welche.

      ══ WORAN MAN SCHEITERT ══════════════════════════════════════════════════
      Der Fehlschluss ist verlockend, weil er RECHNET: 0,614138 ist exakt
      0,85 hoch 3, und wenn man gerade versucht hat, eine Senke auf 85 % zu
      stellen, sieht das wie ein Beleg aus. Es war keiner — die Senke stand
      unveraendert, und die kubische Zahl gehoerte zu einem ganz anderen
      Zustand. Eine Zahl, die zur Erwartung passt, ist der gefaehrlichste
      Messwert.

      ══ DIE REGEL ════════════════════════════════════════════════════════════
      EIN WERKZEUG, EINE SKALA, UND UEBER DIE ID. `wpctl get-volume <id>`
      beantwortet „wie laut steht diese Stufe" ohne Umrechnung; die ID holt
      man sich aus `pw-dump` ueber `node.name`, weil Namen mehrdeutig sein
      koennen und Senken beim Neustart neue IDs bekommen.

      UND: `wpctl set-volume` kann eine Senke stillschweigend NICHT stellen.
      Am 04.09. blieb die ALSA-Senke auf 1.00, ohne Fehlermeldung und ohne
      Wirkung. Wer setzt, LIEST ZURUECK — sonst haelt man einen wirkungslosen
      Befehl fuer erledigt.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
        id() { pw-dump | jq -r '.[]|select(.type=="PipeWire:Interface:Node")
                 |select(.info.props["node.name"]==$n)|.id' --arg n "$1" | head -1; }
        wpctl get-volume "$(id klangwerk)"
      Und zum Gegenlesen dieselbe Stufe mit `pactl` — stimmen die beiden
      Zahlen nicht ueberein, misst man gerade zwei verschiedene Dinge.
  - kind: howto
    id: arbeitsablauf-steht-in-agents-md
    title: "Der Arbeitsablauf steht in AGENTS.md — CLAUDE.md verweist nur"
    source: "Betreiber, 04.09.2026; umgesetzt und mit Gegenprobe geprueft"
    tags: [arbeitsablauf, doku, wache, 2026]
    body: |
      Betreiber, 04.09.2026: „haben wir nicht eine agent.md von claude.md
      verweisen wir darauf? Dann waeren wir llm unabhaengig vom projekt."

      SEITHER GILT: `AGENTS.md` im Wurzelverzeichnis traegt den Ablauf.
      `CLAUDE.md` daneben ist ein reiner VERWEIS und enthaelt mit Absicht
      keine einzige eigene Regel.

      WARUM ZWEI DATEIEN UEBERHAUPT: `AGENTS.md` ist die
      werkzeuguebergreifende Konvention und wird von mehreren Agenten-
      Werkzeugen von selbst gelesen; `CLAUDE.md` liest Claude Code von
      selbst. Der Inhalt gehoert deshalb in die neutrale Datei — sonst haengt
      der Ablauf dieses Baums am Namen eines Werkzeugs.

      WARUM DER VERWEIS ALS ANWEISUNG FORMULIERT IST („Lies sie jetzt") und
      nicht als Fussnote: Ein Werkzeug, das nur `CLAUDE.md` einliest, bekommt
      sonst gar keinen Ablauf. Der Satz muss also etwas ausloesen, nicht nur
      etwas mitteilen.

      DIE WACHE PRUEFT SEITHER DREI DINGE (tools/arbeitsablauf-deckung.py):
        1. `AGENTS.md` und dieser Eintrag nennen dieselben SCHRITT-Marken in
           derselben Reihenfolge (wie vorher, nur der Pfad hat gewechselt)
        2. `CLAUDE.md` nennt `AGENTS.md` ueberhaupt — sonst ist der Verweis
           tot und niemand merkt es
        3. `CLAUDE.md` traegt KEINE eigene SCHRITT-Marke — sonst gaebe es
           wieder zwei Wahrheiten ueber denselben Ablauf, und die Wache
           verglichen nur eine davon mit dem Paket

      GEGENPROBE GEMACHT: Eine erfundene Zeile `SCHRITT 7: schmuggel` in
      `CLAUDE.md` wird als Befund gemeldet. Ohne Punkt 3 waere die
      Umstellung eine Verschlechterung gewesen — zwei Orte fuer denselben
      Ablauf sind schlimmer als ein falsch benannter.

      WAS AELTERE EINTRAEGE NOCH SAGEN: `arbeitsablauf-der-sitzung` nennt an
      drei Stellen `CLAUDE.md` als den Ort, der „zu Beginn JEDER Sitzung
      gelesen" wird. Gemeint ist seit dem 04.09.2026 `AGENTS.md`; der Verweis
      sorgt dafuer, dass beide Wege dorthin fuehren.
  - kind: falle
    id: halbmigration-die-naht-steht-die-altstellen-bleiben
    title: "Die Halbmigration als Hausmuster: die Naht wird gebaut, die Altstellen-Suche findet nicht statt"
    source: "Kritiker-Lauf 05.09.2026 (AUDIT-2026-09-05.md), vier unabhaengige Pruefer, tragende Funde je einzeln gegengeprueft"
    tags: [kritiker, audit, migration, ausrollen, falle, 2026]
    body: |
      FUENF FAELLE DESSELBEN ABLAUFFEHLERS, vier davon aus den letzten
      14 Tagen vor dem 05.09.2026 — jeweils wurde die neue Naht gebaut
      und die Suche nach den Altstellen danach uebersprungen:

        1. atomar.ts gebaut (20.08.), 3 von 18 Schreibstellen migriert —
           es sind 18, nicht 14: neben `.tmp` gibt es `.bruecke`,
           `.uebernommen`, `.teil` und dreimal `.neu` als Zwischennamen.
        2. melden() gebaut, 5 von 228 Log-Praefixen migriert.
        3. Altname `mplayer` umgebaut (f1a0fa81), aber
           player.page.ts:358 vergleicht weiter `=== 'mplayer'` — unter
           mpv zeigt die Box "External Playback" statt der lokalen
           Wiedergabe.
        4. E94: maxVolume-Klemme in die Shell gelegt (53cc7f39), die
           Player-Klemme liest weiter die Konfig vom Prozessstart —
           Aenderungen greifen dort erst nach Dienst-Neustart.
        5. E114: Cue-Weg verdrahtet — aber `MIXPI_CUE` kommt in config/,
           scripts/, autosetup/, update/ NULL Mal vor. ~830 Zeilen
           (tonpfad, cue-schalter, cue-fahren) sind auf jeder aus dem
           Repo installierten ODER aktualisierten Box unerreichbar. Das
           BACKLOG nennt ein Drop-in, das nicht im Baum liegt.

      DERSELBE FEHLER AUF DER KONFIG-SEITE: update/conf_update.sh kennt
      `mupibox.playerEngine` gar nicht — frische Karte faehrt mpv, die
      aktualisierte Bestandsbox faellt auf mplayer zurueck
      (spotify-control.ts:133). Und tools/ausrollwege-vergleich.py
      zeigt: FUENF Vorlagen (darunter mupiboxconfig.json selbst)
      erreichen ueber den Update-Weg KEINE Bestandsbox.

      DIE REGEL DAGEGEN, zweiteilig:
        1. Eine Extraktion ist erst fertig, wenn eine Wache die Altform
           verbietet (stand schon im Lauf vom 04.09., ist weiter nicht
           umgesetzt).
        2. Nach jedem Umbenennen/Umschalten einmal quer ueber ALLE
           Baeume nach dem Altwert greifen (Oberflaechen, scripts,
           config, tools) — vier der fuenf Faelle haette ein einziger
           solcher Grep gefunden. Und: jede im Code abgefragte
           MIXPI_*-Umgebungsvariable muss in config/ oder einem
           Ausrollweg vorkommen, sonst ist der Codepfad tot.

      Vollstaendige Rangliste (12 Raenge, dazu Rueckstands-Bilanz gegen
      den 04.09.): AUDIT-2026-09-05.md im Repo.
  - id: lrclib-fuehrt-dieselbe-nummer-vielfach
    kind: falle
    title: "LRCLIB fuehrt dieselbe Nummer vielfach — wer den ersten Treffer nimmt, bekommt die Zeitmarken einer fremden Aufnahme"
    match: "songtext laeuft versetzt|lyrics falsch synchron|lrclib erster treffer|zeitmarken passen nicht|karaoke haengt hinterher|songtext eine minute zu frueh"
    sev: med
    tags: ["songtext", "lrclib", "e84", "b2", "plugin", "medien", "2026"]
    source: >
      05.09.2026, gemessen mit tools/songtext-quellen-probe.py gegen die echte
      Schnittstelle lrclib.net (Arbeitsplatz, ohne Box). Vorarbeit zu BACKLOG
      E84/B2 „Songtext-Anmeldepunkt", der LRCLIB als erste Wahl nennt.
    symptom: >
      Der Songtext stimmt Wort fuer Wort und laeuft trotzdem versetzt — bei
      einem Lied um Sekunden, bei einem anderen um mehr als eine Minute. Das
      sieht aus wie ein Fehler der eigenen Zeitbasis, und dort sucht man dann
      auch: am Abfragetakt, an der Interpolation, am Pausenzustand.

      Dort ist der Fehler nicht.
    cause: >
      LRCLIB IST NUTZERBEFUELLT, und derselbe Song liegt vielfach darin — als
      Studiofassung, als Single-Edit, als Live-Mitschnitt, als Wiegenlied-
      Bearbeitung. JEDE FASSUNG IST FUER SICH RICHTIG und traegt eigene,
      korrekte Zeitmarken. Gemessen am 05.09.2026 (je 20 Treffer):

          Radiohead - Creep                 12 Dauern   236-311 s   Spanne  75 s
          Nena - 99 Luftballons             10 Dauern   229-284 s   Spanne  55 s
          Queen - Bohemian Rhapsody         14 Dauern   263-415 s   Spanne 152 s
          Rolf Zuckowski - Weihnachtsbaeckerei  8 Dauern 139-176 s  Spanne  37 s

      Die SPANNE ist die groesstmoegliche Verschiebung bei einem Fehlgriff.
      Bei „Bohemian Rhapsody" sind das zweieinhalb Minuten.

      UND DER API-ABGLEICH NIMMT EINEM DAS NICHT AB. Der naheliegende Gedanke
      ist, `/api/get` mit allen vier Feldern zu rufen und dem 404 zu vertrauen.
      Gemessen, und die erste Messung war selbst eine Falle:

          duration=239 (richtig)   -> 200
          duration=30  (absurd)    -> 200   (!)
          duration=600 (absurd)    -> 404
          album falsch             -> 404
          track erfunden           -> 404

      Das ALBUM riegelt streng ab, die DAUER waehlt nur unter den Dubletten
      aus und weist nicht zuverlaessig zurueck. Wer sich auf den 404 verlaesst,
      bekommt still die falsche Fassung statt einer Fehlermeldung.

      DIE ERSTE FASSUNG DIESER MESSUNG BEHAUPTETE DAS GEGENTEIL. Sie fragte
      nach einer „Dauer-Toleranz" und bekam auf +0 bis +10 s lauter 200 — das
      sah nach grosszuegiger Toleranz aus und hiess in Wahrheit „die Dauer
      riegelt gar nicht ab". Sichtbar wurde es erst mit ABSURDEN Werten und
      einem falschen Album als Gegenprobe. Siehe
      [[einmal-hinsehen-ist-keine-messung]].
    fix: >
      DIE DAUER IST DAS EINZIGE MERKMAL, DAS DIE FASSUNGEN TRENNT. Ein
      Songtext-Weg muss deshalb selbst abgleichen, statt der Schnittstelle zu
      glauben:

        * ueber `/api/search` holen (liefert bis zu 20 Treffer mit Feld
          `duration`), NICHT den ersten Treffer nehmen,
        * den Treffer waehlen, dessen `duration` der eigenen am naechsten
          liegt, und
        * bei einer Abweichung ueber wenigen Sekunden LEER BLEIBEN.

      Leer bleiben ist hier die richtige Antwort, nicht die faule: B2 verlangt
      es fuer Hoerspiele ohnehin („die Anzeige muss leer bleiben, nicht
      ‚nicht gefunden' melden"), und ein versetzter Text ist schlimmer als
      keiner.

      WAS LRCLIB FUER EINE KINDERBOX TAUGT (Stichprobe 15 Titel, 05.09.2026 —
      eine Stichprobe, keine Quote der echten Medienliste):

          pop          5/5 synchron (100 %)
          kinderlied   3/5 synchron ( 60 %)   Volker Rosin, Detlev Joecker: nichts
          hoerspiel    0/5 synchron (  0 %)   ein Treffer war ein Fehlgriff

      Bei Hoerspielen ist ein TREFFER gefaehrlicher als ein Fehlschlag: die
      Probe fand zu „Bibi Blocksberg - Die neue Schule" einen unsynchronen
      Text, der mit der Folge nichts zu tun hat. Ein Namensabgleich allein
      taugt dort nicht.

      ZUR ZEITBASIS, damit niemand sie zweimal baut: der gemeldete Wert kann
      ZWEI Sekunden alt sein (zwei Ein-Sekunden-Takte, siehe
      [[mupi-fortschritt-zwei-sekundentakte]]) — fuer Zeilensync zu grob. Das
      Gegenmittel ist schon da und rein getestet: `fortschrittJetzt(anker,
      jetzt)` in `now-playing.ts:116` zaehlt zwischen den Meldungen lokal
      weiter und friert bei Pause ein.

      NOCH FEHLT die IDENTIFIKATION: die `NowPlaying`-Naht fuehrt `title`,
      `subtitle` (= Album), `elapsed` und `duration`, aber KEINEN Interpreten
      — und LRCLIB verlangt ihn. Vorhanden ist er anderswo: bei Spotify unter
      `item.artists[]`, lokal als `artist` am Medieneintrag und ueber die
      `:title:artist:`-Konvention im Befehlspfad des Abspieldienstes. Wer B2
      baut, erweitert die Naht, statt eine neue Abfrage danebenzustellen.

      Zugang gemessen: kein Schluessel noetig, zehn Anfragen hintereinander
      ohne Drosselung. LRCLIB VERLANGT eine `User-Agent`-Kennung mit Namen und
      Verweis — die gehoert gesetzt, sonst ist die Box ein anonymer
      Massenabrufer.

      RECHTLICH bleibt es, was B2 sagt: Songtexte sind geschuetzt, LRCLIB ist
      nutzerbefuellt. Fuer die eigene Box ist das normale Nutzung,
      Weiterverteilen waere etwas anderes — insbesondere sollte kein
      Ausrollweg Textdateien mitschleppen.
    related: ["mupi-fortschritt-zwei-sekundentakte", "mupi-fortschrittsbalken-springt", "plugin-inhalt-kommt-in-die-medienliste", "einmal-hinsehen-ist-keine-messung"]
  - id: spotify-anmeldeseite-eigenstaendig
    kind: note
    title: "Die Spotify-Anmeldung lebt eigenstaendig unter /spotify — nicht mehr in der alten Angular-App"
    tags: [spotify, anmeldung, pkce, neue-oberflaeche, e118, 2026]
    source: >
      E118/1a, 05.09.2026: erster Entkopplungsschritt vor dem Loeschen der
      alten Oberflaeche. Am Vorschau-Server gegengeprueft (Aufbau, stille
      Box, MIME-Falle); der Anmeldefluss selbst braucht den Geraetebeweis.
    related: [mupi-fork-ausliefern-ablauf, neue-oberflaeche-ohne-box-ansehen]
    body: |
      WO SIE LIEGT: NewDesign/spotify-anmeldung.html (Seite, Verdrahtung
      inline) + NewDesign/spotify-anmeldung-logik.mjs (reine PKCE-Logik,
      ES-Modul). Ausgeliefert wie alles aus NewDesign/ nach www/neu/; die
      Route `app.get('/spotify')` in server.ts (vor dem Catch-all) liefert
      die Seite unter dem alten Pfad aus — auf 8200 UND 8443.

      WARUM DER PFAD NICHT WANDERN DARF: http://127.0.0.1:8200/spotify ist
      bei Spotify als redirect_uri registriert, und Spotify vergleicht
      ZEICHENGENAU (E22). Wer die Seite verschiebt, ohne die Route zu
      behalten, bricht jede kuenftige Anmeldung.

      WAS SIE TRAEGT: die Web-API-Anmeldung (PKCE, geheimnislos) mit allen
      E22-Wegen — fester Rueckweg als Loopback-LITERAL, Einfuegefeld fuer
      die Fehlerseiten-Adresse, HTTPS-Wechsel auf 8443, Verwerfen-Ausgang,
      Klartext-Fehler in zwei Toepfen — und die TON-Anmeldung (librespot)
      ueber /api/spotify/ton/... (Geraetecode, Start, Einloesen), weil der
      Einrichtungs-QR Handys direkt auf /spotify schickt.

      WAS SIE ABSICHTLICH NICHT TRAEGT: Blaettern/Suchen/Hinzufuegen von
      Spotify-Inhalten. Das ist Medienverwaltung und lebt in der Verwaltung
      (/admin, streaming.ts) — eine zweite Abschrift liefe still auseinander.

      SPEICHER: dieselben localStorage/sessionStorage-Schluessel wie die
      alte Angular-Seite (mupibox_spotify_*). Ein konfigurierter Rueckweg
      und eine bestehende Browser-Anmeldung ueberleben den Umstieg.

      GETESTET: tools/e2e/spotify-anmeldung-logik.test.mjs (node --test,
      Faelle 1:1 aus dem Karma-Spec der alten Seite uebernommen, samt
      RFC-7636-Testvektor), eingehaengt in tools/pruefen.sh als Schritt
      "Spotify-Anmeldelogik".

      DIE MIME-FALLE DABEI: Browser pruefen den MIME-Typ von ES-Modulen
      STRENG. Die Vorschau-Attrappe (tools/neu-vorschau.mjs) lieferte .mjs
      als application/octet-stream — der Import scheiterte OHNE laute
      Konsolenmeldung, die Seite blieb bei "Frage die Box...". Die echte Box
      liefert .mjs richtig (express.static/mime-db: text/javascript); die
      TYPEN-Tabelle der Attrappe kennt .mjs seit E118/1a auch. Wer eine
      weitere Attrappe baut: .mjs gehoert in ihre MIME-Tabelle.

      DIE ALTE ANGULAR-SEITE (src/frontend-box/src/app/spotify/...) ist ab
      jetzt UNERREICHBAR fuer direkte Aufrufe (die Server-Route faengt
      vorher) und faellt mit E118/1e.
  - kind: falle
    id: dokumentierte-ausnahme-friert-den-mangel-ein
    title: "Die Sektion `medien` hing 13 Tage ins Leere — die Wache meldete gruen, weil die README es als Absicht auswies"
    sev: high
    tags: [plugins, sektionen, wachen, doku, verwaltung, 2026]
    quelle: >-
      05.09.2026, Betreiber: „es gibt das plugin internet archive aber es ist
      nicht nutzbar wir muessen es zu medien verdraten". Gemessen:
      tools/plugin-kette-probe.mts mixpi-archive „ruebezahl" — alle sechs
      Stellen aus E88 gruen; tools/sektionen-deckung.py — gruen. Gefunden hat
      es weder das eine noch das andere, sondern der Betreiber am Geraet.
    body: |
      DER MANGEL WAR BEKANNT, BENANNT UND BEWACHT — und blieb trotzdem
      dreizehn Tage liegen. `plugins/mixpi-archive/plugin.json` meldet sich
      seit dem ersten Tag unter `"sektion": "medien"` an.
      `<mixpi-plugin-abschnitt>` hing im ganzen Baum an genau EINER Stelle,
      in `seiten/streaming.ts`. Also: kein Icon, kein Zustand, kein Befinden,
      keine angemeldete Aktion, kein Weg zu den Einstellungen — der Wert war
      gueltig und loeste nichts ein.

      ══ WARUM DIE WACHE DAS NICHT MELDETE ══════════════════════════════════
      `tools/sektionen-deckung.py` prueft genau das („Sektionen ohne Ort").
      Sie fand es auch — am 23.08.2026, zusammen mit `ton` und
      `streaming/deezer`. Weil eine dauerrote Wache keine ist
      ([[dauerrote-wache-ist-keine]]), liest sie ihre Ausnahmen seit dem
      25.08.2026 AUS `plugins/README.md`: die Tabellenzeile, die `medien` mit
      **nirgends** ausweist, entschaerft den Befund zu einem `bekannt:`.

      DAS WAR RICHTIG UND HAT TROTZDEM GESCHADET. Die Ausnahme wurde
      geschrieben, um die Wache wieder scharf zu machen; sie hat den Mangel
      dabei in einen ZUSTAND verwandelt. Drei Werte standen ab da
      gleichberechtigt nebeneinander:

        `streaming/deezer`  — Absicht, mit Grund (nimmt dem Vertragstest sein
                              Gegenbeispiel weg)
        `ton`               — Absicht, mit Ersatz (`rechte: ["klang"]`)
        `medien`            — KEIN Ersatz, nur ein Verweis auf den E87-Weg,
                              der eine ANDERE Frage beantwortet

      Der E87-Weg bringt Plugin-INHALT in die Medienliste. Die Sektion traegt
      die STEUERUNG — Schalter, Einstellungen, Aktionen. Die README fasste
      beides zu einem „gewinnt nichts und darf ersatzlos wegbleiben"
      zusammen, und damit las sich ein echter Mangel wie eine Entscheidung.

      ══ DIE REGEL ══════════════════════════════════════════════════════════
      EINE AUSNAHME BRAUCHT NICHT NUR EINEN GRUND, SONDERN EINEN ERSATZ ODER
      EIN ABLAUFDATUM. Wer eine Wache ueber die Doku entschaerft, schreibt in
      dieselbe Zeile, WAS STATTDESSEN GEHT — und wenn es nichts gibt, ist es
      keine Ausnahme, sondern eine offene Arbeit, die in den Backlog gehoert
      und nicht in die Ausnahmeliste. Sonst prueft die Wache ab da nur noch,
      ob die Ausnahme noch dasteht.

      Verwandt, aber nicht dasselbe: [[wiki-zustand-hat-haltbarkeit]] (eine
      Zustandsaussage veraltet) und [[sackgasse-ohne-ausweg-in-der-tabelle]]
      (der Ausweg steht 160 Zeilen entfernt). Hier stand der Ausweg
      DANEBEN — er passte nur nicht zur Frage.

      ══ WAS GEBAUT WURDE (05.09.2026) ══════════════════════════════════════
      `seiten/medien.ts` haengt `<mixpi-plugin-abschnitt [sektion]="'medien'">`
      unter eine eigene Ueberschrift „Erweiterungen", direkt UEBER dem
      Archiv-Kasten — der sucht ueber das Plugin, und ist es abgeschaltet,
      steht der Grund fuer „nicht erreichbar" eine Zeile hoeher.

      DIE LEISTE BEKAM EINEN `leerHinweis`: meldet sich niemand fuer die
      Sektion, stand vorher NICHTS unter der Ueberschrift. Eine leere
      Ueberschrift sagt nicht, ob nichts angemeldet ist oder die Anzeige
      kaputt. Auf den Streaming-Karten bleibt der Eingang ungesetzt und die
      Leiste unsichtbar wie bisher — dort traegt die Karte schon Text.

      GEGENGEPROBT: die eine Zeile aus `medien.ts` entfernt — sechs neue
      Zeugen in `medien.plugin-leiste.spec.ts` rot, `sektionen-deckung.py`
      rot mit `TOTES VOKABULAR`. Zurueckgenommen ueber einen gezielten Edit
      und NICHT ueber `git checkout`: der haette die unversionierte Arbeit
      derselben Sitzung mitgenommen.

      DER ZEUGE MISST DIE NAHT, NICHT DIE LEISTE. Ein Test gegen
      `MixpiPluginAbschnitt` allein bliebe gruen, wenn jemand die Zeile aus
      `medien.ts` wieder herausnimmt — genau die Sorte Naht, die hier schon
      zweimal zugeschlagen hat ([[plugin-kette-reisst-an-sechs-stellen]]).

      ══ UND EIN WERKZEUG, DAS DIE PLUGINS NICHT KANNTE ══════════════════════
      `tools/admin-vorschau.mjs` beantwortete `/api/plugins` mit dem leeren
      Objekt, mit dem es jeden unbekannten API-Weg beantwortet — die Leiste
      sah dort IMMER so aus, als haette sich niemand gemeldet. Es liest jetzt
      die echten `plugins/*/plugin.json` und liefert Liste, Befinden und
      Icon. Erfunden ist nur der Zustand (`bereit`), denn ohne Wirt laeuft
      kein Worker.
  - id: newdesign-kopiert-ein-eigenes-werkzeug
    kind: note
    title: "NewDesign kommt seit E118/1b aus tools/newdesign-kopieren.py — nicht mehr als Angular-Asset der alten App"
    tags: [auslieferung, neue-oberflaeche, deploy, e118, 2026]
    source: >
      E118/1b, 05.09.2026: zweiter Entkopplungsschritt. Kopier-Werkzeug am
      Scratch-Ziel geprobt (143 Dateien, null Werkstatt im Ziel), Wache
      umgebaut und gruen; der volle deploy.sh-Lauf ist die Blockende-Messung.
    related: [spotify-anmeldeseite-eigenstaendig, mupi-fork-ausliefern-ablauf, www-altlasten-und-gehashte-dateinamen]
    body: |
      WARUM: Die Asset-Regel in src/frontend-box/angular.json war der EINZIGE
      Weg, auf dem NewDesign/ nach www/neu/ kam — die Auslieferung der neuen
      Oberflaeche hing damit am Bau der alten, die geloescht werden soll.
      Wer frontend-box entfernt haette, haette der neuen Oberflaeche still
      den Ausrollweg genommen.

      JETZT: tools/newdesign-kopieren.py ist der EINE Kopierschritt.
        * traegt AUSSEN_VOR (die Ausschlussliste: Standalone- und Messseite,
          **/*.md, bilder/quellen/**) und KERNSTUECKE (index.html, app.js,
          app.css, spotify-anmeldung.html, spotify-anmeldung-logik.mjs)
        * loescht das Ziel vorher (frischer Stand statt Altlasten)
        * CLI: python3 tools/newdesign-kopieren.py ZIEL  (oder --liste)

      ZWEI RUFER:
        * src/deploy.sh kopiert nach ./deploy/www/neu (nach dem Abflachen)
          und weist www/neu/index.html jetzt auch IM ZIP nach
        * tools/ausliefern.py kopiert in bauen() nach BAU/www/neu — NEBEN
          browser/, das Schnueren nimmt es beim Abflachen mit; die
          Da-Pruefung zeigt auf BAU/www/neu/index.html (nicht mehr
          .../browser/neu/)

      DIE WACHE tools/newdesign-mitlieferung-deckung.py prueft seitdem DREI
      Dinge: Deckung der Werkstatt-Sorten durch AUSSEN_VOR (per importlib
      GELADEN, nicht als Text erraten), angular.json traegt KEINE
      NewDesign-Regel mehr (zwei Wege = zwei Listen = Drift), und beide
      Rufer rufen wirklich (ausserhalb von Kommentarzeilen). Sie kennt
      ZWEI Laufzeit-Seiten: index.html und spotify-anmeldung.html.

      WER EINE DATEI IN NewDesign/ ANLEGT, die nicht auf die Box soll:
      Muster in AUSSEN_VOR im Kopier-Werkzeug eintragen — nirgendwo sonst.
  - id: farbkanal-feste-datei-statt-symlink
    kind: note
    title: "Der Farbwaehler schreibt seit E118/1c in EINE feste Datei — /farben.css ersetzt den active_theme-Symlink"
    tags: [farben, neue-oberflaeche, verwaltung, e118, 2026]
    source: >
      E118/1c, 05.09.2026: dritter Entkopplungsschritt. Backend-Typpruefung
      und Admin-Bau gruen; Wache tools/farbkanal-verdrahtung.py haelt die
      Kette, Gegenproben beim Bau. Geraetebeweis am Ende von Stufe 1.
    related: [newdesign-kopiert-ein-eigenes-werkzeug, mupi-themawechsel-teilstring, mupi-neues-thema-anlegen]
    body: |
      DER NEUE WEG, drei Glieder, keine beweglichen Teile:
        1. PUT /api/farbthema schreibt den Marken-Block (farbthema.ts,
           unveraendert: !important unter :root:not([data-licht='dunkel']))
           in die FESTE Datei <configBasePath>/mixpi-farben.css — atomar
           ueber .tmp + Umbenennen. Kein sudo, kein setting_update.sh,
           kein Themenname, keine .vor-farben-Sicherung (die Datei gehoert
           ganz dem Waehler).
        2. GET /farben.css liefert die Datei als text/css mit
           Cache-Control no-store; FEHLT sie, kommt 200 mit LEEREM Blatt —
           der neutrale Zustand ist kein Fehler, und an frischen Geraeten
           haengt keine rote Konsolenzeile.
        3. /neu/index.html bindet /farben.css VOR app.css (die Reihenfolge
           traegt die ganze !important-Logik).

      SANFTE UEBERNAHME: Solange mixpi-farben.css noch nicht existiert,
      liest GET /api/farbthema den alten Block aus themes/<mupibox.theme>.css
      (NUR lesend, altFarbthemaLesen in server.ts, faellt mit E118/1e) —
      eine eingerichtete Box verliert ihre Farben nicht, und der naechste
      Speichervorgang legt die neue Ablage an.

      WAS WEGGEFALLEN IST: verweisZielFalsch(), der sudo-Nachzug von
      setting_update.sh im PUT, das verweis-Feld der Antwort und die
      Symlink-Warnung der Verwaltung. Die Verwaltung zeigt keinen
      Themennamen mehr an (farbThema-Signal entfernt) — die Ablage ist
      themenunabhaengig.

      DIE WACHE tools/farbkanal-verdrahtung.py (in pruefen.sh:
      "Farbkanal-Verdrahtung") prueft die Kette ausserhalb von Kommentaren:
      index.html-Einbindung samt Reihenfolge und ohne active_theme-Rueckfall,
      Route und feste Datei in server.ts, und dass der PUT ohne
      sudo/setting_update auskommt.

      ACHTUNG BIS E118/1e: Die ALTE Oberflaeche liest weiter ihren Symlink
      www/active_theme.css (relativ eingebunden) — der Alt-Mechanismus
      (setting_update.sh, themes/) bleibt bis zu ihrer Loeschung stehen und
      faellt dann GESAMMELT. Nur das NewDesign haengt schon am neuen Kanal.
  - kind: falle
    id: da-gueltig-und-unsichtbar
    title: "Fuenf Plugin-Zeichen waren da, gueltig und unsichtbar — currentColor im img-Element ist Schwarz"
    sev: high
    tags: [plugins, verwaltung, svg, wachen, zeichen, 2026]
    quelle: >-
      05.09.2026, Betreiber: „wir brauchen noch ein icon fuer internet
      archive". Gemessen im Browser (Canvas ueber das geladene img): 626
      gedeckte Bildpunkte, alle rgb(0,0,0), Untergrund rgb(18,18,18) —
      Kontrast 1,12:1. Unabhaengig nachgemessen mit rsvg-convert: dieselbe
      Tinte. Nach der Reparatur 16,9:1, in beiden Renderern.
    body: |
      DAS ZEICHEN GAB ES SCHON. `plugins/mixpi-archive/icon.svg` lag im
      Ordner, das Manifest nannte es, die Route `/api/plugins/<k>/icon`
      antwortete mit 200, das Bild war im Browser geladen
      (`complete: true`, `naturalWidth: 150`). Zu sehen war trotzdem nichts —
      und dasselbe galt fuer alle vier Geschwister.

      ══ DIE URSACHE IST EINE ZEILE, DIE UEBERALL SONST RICHTIG IST ══════════
      Die Zeichen malen mit `stroke="currentColor"` — der uebliche Weg, damit
      ein Strichzeichen zur Schriftfarbe passt. Geladen werden sie aber ueber

          <img src="/api/plugins/<kennung>/icon">

      und EIN SVG IM `img`-ELEMENT IST EIN EIGENES DOKUMENT. Es hat keinen
      Zugriff auf die `color` der einbettenden Seite; `currentColor` faellt
      auf den Anfangswert zurueck, und der ist Schwarz. Auf dem Untergrund
      der Verwaltung (`#121212`) heisst das: unsichtbar.

      DASSELBE SVG INLINE EINGEFUEGT waere richtig gewesen. Die Datei ist
      nicht falsch — der WEG, auf dem sie geladen wird, entscheidet ueber
      ihre Bedeutung. Wer eine Grafik als eigenstaendiges Dokument
      ausliefert, muss ihr alles mitgeben, was sie zum Malen braucht.

      ══ WARUM ES KEINE WACHE FAND ══════════════════════════════════════════
      Es gab Pruefungen fuer Zeichen: liegt die Datei da, nennt das Manifest
      sie, laesst sie sich parsen. ALLE DREI WAREN WAHR. „Da, gueltig und
      unsichtbar" ist ein Zustand, den keine Frage nach EXISTENZ findet — er
      entsteht erst beim Malen.

      DIE VERALLGEMEINERUNG: Eine Zusicherung ueber eine DATEI (sie ist da,
      sie ist wohlgeformt) ist keine Zusicherung ueber ihre WIRKUNG. Wo das
      Ergebnis ein Bild, ein Klang oder ein Layout ist, muss die Wache es
      herstellen und messen — sonst prueft sie nur die Verpackung. Es ist
      dieselbe Sorte Luecke, vor der AGENTS.md bei Diensten warnt:
      `systemctl is-active` beantwortet fast nie die Frage, die man wirklich
      hat.

      ══ WAS GEBAUT WURDE ═══════════════════════════════════════════════════
      Jedes der fuenf Zeichen bringt seine Tinte jetzt selbst mit:

          <style> svg { color: #f2f2f7; } </style>

      `currentColor` bleibt im Rest der Datei stehen — es loest sich jetzt
      nur gegen einen Wert auf, den das Zeichen selbst gesetzt hat. Der Wert
      ist die Schriftfarbe der Verwaltung (`--schrift`).

      NICHT UEBER EINE MASKE IM WIRT, obwohl das alle Zeichen auf einen
      Schlag eingefaerbt haette: `mask-image` plus `background-color:
      currentColor` macht aus JEDEM Zeichen ein einfarbiges. Ein Plugin, das
      ein farbiges Logo mitbringt, saehe danach anders aus, als es gemeint
      war — und der Wirt faerbt fremde Inhalte nicht um (E77).

      ══ DIE WACHE MISST, WAS MAN SIEHT ═════════════════════════════════════
      `tools/plugin-icon-kontrast.py` rendert jedes angemeldete Zeichen mit
      rsvg-convert in der Groesse, in der es dasteht (24 px), und rechnet den
      WCAG-Kontrast der hellsten deckenden Tinte gegen den Untergrund.
      Schwelle 3:1 (Grafik, nicht Fliesstext). Der Untergrund wird AUS
      `styles.css` gelesen und nicht abgeschrieben — sonst misst die Wache
      nach dem naechsten Umfaerben gegen eine Farbe, die es nicht mehr gibt.

      Gegengeprobt mit drei Sabotagen, jede einzeln rot: SVG zerschossen
      (Parserfehler), Zeichen geleert (malt nichts), und der Ausgangszustand
      selbst — die fuenf unveraenderten Dateien aus HEAD meldete sie mit
      1,12:1. Sie haengt in `tools/doku-luecken-probe.sh`.

      MESSEN, NICHT ANSEHEN, REICHT AUCH NICHT: der Kontrastwert sagt nichts
      darueber, ob aus drei Strichen ein Tempelgiebel wird oder ein Fleck.
      Deshalb zusaetzlich ein Bogen aller fuenf Zeichen bei 96 und 24 px auf
      dem echten Untergrund, und hingesehen — dieselbe Regel wie bei
      `tools/zeichen-musterbogen.mjs`.
  - id: songtext-anmeldepunkt-statt-plugin-sonderweg
    kind: architektur
    title: "Songtexte: warum es ein Kern-Anmeldepunkt wurde und kein Plugin-Sonderweg (E84/B2)"
    match: "songtext plugin|lyrics anzeigen|api/songtext|recht songtext|liedtext synchron|karaoke mupibox"
    sev: low
    tags: ["songtext", "plugin", "vertrag", "e84", "b2", "architektur", "2026"]
    source: >
      05.09.2026, gebaut. Vorarbeit: tools/songtext-quellen-probe.py gegen
      lrclib.net. Zeugen: songtext.spec.ts (20), plugin-vertrag.spec.ts
      (songtextPruefen, 8), now-playing.spec.ts (interpretFuerLokal, 11).
    symptom: >
      Die naheliegende Frage lautet „kann ein Plugin Songtexte anzeigen?", und
      die naheliegende Antwort waere `http()`. Beides fuehrt in die Irre.
    cause: >
      EIN PLUGIN KANN ES NICHT ALLEIN, und zwar doppelt versperrt:

        * `http()` liegt HINTER DEM TOR DER VERWALTUNG und erreicht den
          Kinderschirm ausdruecklich nicht.
        * `fundPruefen` gibt nicht zurueck, was das Plugin liefert — es BAUT
          DEN FUND NEU AUF, aus sechs Feldern. Ein Text laesst sich nicht
          anhaengen; er faellt still heraus.

      Genau diese doppelte Sperre nennt BACKLOG E84/B2 als Grund, warum es ein
      Anmeldepunkt wird. Die Form dafuer stand schon im Plugin-Handbuch als
      LEHRE AUS DEM KLANGWERK: ein schmaler Anmeldepunkt, kein `UiExtension`,
      kein fremdes JS in der Seite.
    fix: >
      GEBAUT AM 05.09.2026, vier Teile:

        1. RECHT `songtext` + METHODE `songtext(frage, kontext)` im Vertrag.
           Die Frage traegt `{ interpret, titel, dauerSek }`, zurueck kommt
           `{ zeilen: [{ zeitMs, text }] }`.
        2. `songtextPruefen()` in plugin-vertrag.ts — baut aus der Weissliste
           NEU AUF (wie fundPruefen), siebt kaputte Zeilen aus, sortiert,
           Deckel 600 Zeilen. LEERER Text MIT Zeitmarke bleibt: das sind die
           Pausen zwischen den Strophen.
        3. `pluginSongtext()` im Wirt — fragt nur Plugins mit Recht UND
           Methode, erstes Ergebnis gewinnt. Zusammengefuehrt wird NICHTS:
           zwei Quellen haben verschiedene Zeitmarken, ineinandergeschoben
           ergeben sie Kauderwelsch.
        4. Route `GET /api/songtext` — eine BOX-Route, nicht hinter dem
           Verwaltungstor. Das ist der ganze Zweck.

      WARUM EIN EIGENES RECHT UND NICHT `medienquelle`: die beiden beantworten
      verschiedene Fragen. `medienquelle` heisst „ich sage, WAS gespielt wird" —
      das Plugin bestimmt den Ton. `songtext` heisst „ich sage etwas UEBER
      das, was ohnehin laeuft"; es bestimmt nichts, es beschriftet. Im
      Eltern-Bereich soll dieser Unterschied lesbar sein.

      LEER IST DIE NORMALE ANTWORT, nicht der Ausnahmefall — `{ zeilen: [] }`
      mit Status 200, NIE ein 404. Fuer Hoerspiele findet sich nichts
      (gemessen: 0/5), fuer deutsche Kinderlieder oft auch nicht (3/5), bei
      Popmusik dagegen 5/5. Eine Fehlermeldung waere eine Behauptung ueber die
      Box; der Grund gehoert ins Journal, nicht ins Bild.

      ZWEI NAEHTE, die es dafuer BRAUCHTE und die man sonst neu baut:

        * DER INTERPRET fehlte in `NowPlaying` (sie fuehrt Titel, Album,
          elapsed, duration). Neu: `interpretFuerLokal()` in now-playing.ts —
          Pfad, dann Praefix vor " - ", dann `album` (der Jellyfin-Fall). Sie
          RAET NICHT: ohne belastbaren Weg kommt Leeres zurueck, denn ein
          falscher Interpret liefert einen fremden Text.
        * DIE ZEITBASIS war schon da: `fortschrittJetzt()` rechnet zwischen
          den Meldungen weiter. Der gemeldete Wert kann zwei Sekunden alt sein
          ([[mupi-fortschritt-zwei-sekundentakte]]) — fuer Zeilensync zu grob.

      DAS QUELL-PLUGIN STEHT (05.09.2026, noch am selben Tag):
      `plugins/mixpi-lrclib` (0.1.0), Rechte `songtext` + `netz`, 26 Zeugen.
      Es nimmt `/api/search` und NICHT `/api/get` — `get` verlangt einen
      Albumnamen, den die Box bei lokaler Wiedergabe oft nicht kennt, und
      riegelt ausgerechnet auf ihn ab statt auf die Dauer. Der Spielraum ist
      ein `felder`-Eintrag (Vorgabe 5 s), also im Eltern-Bereich einstellbar.

      AM ECHTEN DIENST DURCHGESTOCHEN (`tools/plugin-pruefen.mjs
      plugins/mixpi-lrclib --songtext "Nena|99 Luftballons|233"`, neuer
      Schalter): 48 Zeilen, erste Marke 3420 ms, letzte 221100 ms.

      UND DIE GEGENPROBE AN ECHTEN DATEN, die den Riegel rechtfertigt: mit
      entferntem Dauerabgleich waehlt derselbe Ruf fuer ein 300-s-Stueck die
      284-s-Fassung — 48 Zeilen, letzte Marke bei 221 s, also 79 Sekunden zu
      frueh zu Ende. Genau der Fehler, der wie ein Fehler der Zeitbasis
      aussieht.

      EINE KORREKTUR ZUM MITNEHMEN: beim Hoerspiel-Fall („Bibi Blocksberg")
      greift NICHT der Dauerabgleich, sondern die Bedingung „nur Treffer mit
      Zeitmarken" — LRCLIB hat dort 20 Treffer, aber nur unsynchronen Text.
      Wer nur den Dauerabgleich einbaut und plain-Text zulaesst, zeigt dem
      Kind einen fremden Text.

      ZWEI FORMEN STATT EINER (Nachtrag 05.09.2026, auf Betreiberwunsch „falls
      es nur lyrics unsynchron gibt dann nur vollscreen zum selber scrollen"):
      der Vertrag traegt `{ zeilen: [{zeitMs, text}] }` ODER
      `{ absaetze: ["…"] }`, die Route meldet `synchron: true|false`.
      GETRENNTE FELDER, KEIN OPTIONALES `zeitMs` — sonst muesste jede Stelle,
      die mitlaufen will, erst nachsehen, ob die Marke da ist, und die eine,
      die es vergisst, zeigt bei Sekunde 0 den ganzen Text auf einmal.

      UND DER DAUER-RIEGEL GILT AUCH FUER UNSYNCHRONEN TEXT. Das ist der
      Punkt, an dem man ihn am ehesten weglaesst („ohne Zeitmarken kann ja
      nichts verrutschen") und am teuersten bezahlt: dieselbe Abfrage nach
      „Bibi Blocksberg — Die neue Schule" liefert mit dauerSek=95 das
      TITELLIED (36 unsynchrone Zeilen) und mit dauerSek=2400, der echten
      Folge, NICHTS. Ohne Riegel bekaeme die Hoerspielfolge den Liedtext.

      WAS NOCH FEHLT (Stand 05.09.2026): allein die Anzeige. ACHTUNG, das Ziel
      hat sich am selben Tag verschoben: Commit 980eaed3 („E118/1d — Eine
      Oberflaeche") hat `src/frontend-box/src/app/` GELOESCHT. Die Anzeige
      gehoert damit nach `NewDesign/app.js` (reines JS), nicht in die
      Angular-Box; dort liegt auch die Welle (E99). `interpretFuerLokal()` aus
      now-playing.ts muss dorthin mitwandern.

      DIE GEFAEHRLICHSTE STELLE liegt nicht hier, sondern bei der Wahl der
      Fassung: [[lrclib-fuehrt-dieselbe-nummer-vielfach]].
    related: ["lrclib-fuehrt-dieselbe-nummer-vielfach", "mupi-fortschritt-zwei-sekundentakte", "plugin-inhalt-kommt-in-die-medienliste"]
  - id: eine-oberflaeche-kiosk-und-konfiguration
    kind: note
    title: "Seit E118/1d gibt es EINE Oberflaeche: Kiosk laedt fest /neu/, der Schalter und das Farbthema-Feld sind Geschichte"
    tags: [kiosk, konfiguration, neue-oberflaeche, e118, 2026]
    source: >
      E118/1d, 05.09.2026. Backend-Typpruefung, konfiguration.spec (105/105)
      und Admin-Bau gruen; Kiosk-Skript per bash -n. Geraetebeweis am Ende
      von Stufe 1.
    related: [farbkanal-feste-datei-statt-symlink, newdesign-kopiert-ein-eigenes-werkzeug]
    body: |
      WAS SICH GEAENDERT HAT:
        * chromium-autostart.sh laedt fest ${MUPI_URL}/neu/ — der Block, der
          mupibox.oberflaeche las (Vorgabe "klassisch"), ist raus. Die
          Vorgabe haette nach der Loeschung der alten App jede Box mit
          fehlendem Schluessel auf ein leeres www/ geschickt.
        * Der Catch-all in server.ts leitet alles Unbekannte nach /neu/ um
          (statt die Angular-index.html zu liefern). AUSNAHME: Pfade unter
          /neu/ selbst bekommen ehrlich 404 — sonst Schleife.
        * konfiguration.ts: die Felder "oberflaeche" und "thema (Farbthema)"
          sind aus der Verwaltung gefallen (Grabstein-Kommentare an Ort und
          Stelle). Bestandskonfigurationen behalten ihre Schluessel.
        * config/templates/mupiboxconfig.json: oberflaeche, theme und die
          25er installedThemes-Liste raus — eine frische Box faengt nicht
          mit toten Schluesseln an.
        * update/conf_update.sh: die 18 Bloecke, die installedThemes
          nachpflegten (xmas ... axolotl, customTheme), sind raus.
          ACHTUNG BEIM NACHBAU SOLCHER LOESCHUNGEN: einer hiess DINO= statt
          DINOSAUR= — wer nach Namensmuster loescht, zaehlt hinterher die
          jq-Treffer nach (grep -c "installedThemes?" muss 0 sein).
        * Verwaltung darstellung.ts: schirm() ist fest 'neu' (der Typ
          behaelt beide Worte, die klassisch-Zweige fallen mit 1e).

      DIE WACHE ist der konfiguration.spec: er prueft jetzt NEGATIV, dass
      die Vorlage KEIN oberflaeche/theme/installedThemes mehr traegt — die
      Schluessel der Erprobungszeit koennen nicht still zurueckkommen. Die
      auswahlAus-Mechanik (letzter Nutzer war das Farbthema-Feld) bleibt
      samt Tests erhalten, die Tests bauen sich ihre Felder selbst.
  - id: alte-oberflaeche-geloescht
    kind: note
    title: "E118/1e: Die alte Angular-Oberflaeche und die 29 CSS-Themen sind geloescht — was dabei zu lernen war"
    tags: [neue-oberflaeche, aufraeumen, e118, loeschung, 2026]
    source: >
      05.09.2026, der grosse Schnitt von E118. Referenz der Uebernahmen:
      dokumentation/ALT-UEBERNAHMEN.md (eigener Commit VOR der Loeschung);
      den vollen Baum traegt die git-Historie.
    related: [spotify-anmeldeseite-eigenstaendig, newdesign-kopiert-ein-eigenes-werkzeug, farbkanal-feste-datei-statt-symlink, eine-oberflaeche-kiosk-und-konfiguration]
    body: |
      WAS FIEL: src/frontend-box/ (252 Dateien), themes/ (29 CSS + 10
      Asset-Ordner), die Theme-Verteilung in autosetup und Update-Skript,
      der Symlink-Teil von setting_update.sh, der MupiNew-put des Installers,
      zwei optionale Installer-Schritte (api-port-fix, leisten-ton: flickten
      Angular-Ausgaben), sieben Werkzeuge der alten App und vier
      pruefen.sh-Schritte. Der Catch-all liefert keine Angular-index mehr;
      /api/oberflaeche/stand rechnet nur noch ueber die drei /neu/-Dateien.

      DER NACHZUEGLER: src/frontend-box/src/app/now-playing.ts (+spec) LEBT —
      eine parallele Sitzung erweitert sie gerade (Songtext, E84/B2).
      git rm hat die Loeschung ihretwegen VERWEIGERT (lokale Aenderungen),
      und genau das war der richtige Riegel: erzwungen haette er fremde,
      uncommittete Arbeit vernichtet. Regel im Ordner-LIESMICH: nach dem
      Nachbar-Commit zieht die Datei um, dann faellt der Ordner ganz.

      DIE LEHREN DES SCHNITTS (jede am 05.09. selbst bezahlt):
      * Ein GELOESCHTER Baum macht jede historische Nennung zur
        Wachen-Klage. tools/doku-pfade-pruefen.py kennt jetzt GEFALLEN +
        GESCHICHTSQUELLEN: Journale (BACKLOG), das Wissenspaket und die
        Uebernahme-Referenz DUERFEN tote Pfade nennen — Aktiv-Doku
        (README, mixpibox.md) nicht. 23 Geschichts-Faelle, 4 echte.
      * Wachen ankern an KOMMENTAREN: die Fehlerbehandler-Leck-Schau fand
        ihren Catch-all nicht mehr, weil 1d dessen Kommentar umformuliert
        hatte; ihre Verdeckt-Erkennung erwartete die Angular-index statt
        des Redirects. Wer einen Rueckfall umbaut, sucht seine Anker:
        grep nach dem alten Kommentar-Wortlaut in tools/.
      * Specs, die das ABGELOESTE Verhalten pruefen, muessen im SELBEN Zug
        fallen wie das Verhalten (farbthema.integration.spec pruefte noch
        Themenname/Sicherung/Verweis aus der Vor-1c-Welt — mein Versaeumnis
        bei 1c: nur Typen gemessen, nicht die Suite).
      * Nach dem Loeschen von Werkzeugen die Ungerufene-Wachen-Probe lesen:
        zwei Geraetemess-Helfer standen im Windschatten eines geloeschten
        Vorfuehr-Werkzeugs und wurden erst dadurch als ungerufen sichtbar.
      * Muster-Loeschungen NACHZAEHLEN: von 18 conf_update-Bloecken hiess
        einer DINO= statt DINOSAUR= und ueberlebte den ersten Durchgang.

      VORGEFUNDENE NACHBAR-BEFUNDE (nicht Teil von E118, stehen gelassen):
      dienste-deckung meldet 'nebenher' (app.css kennt es, dienstVon nicht)
      — der Eintrag steht schon in HEAD und gehoert zur Plugin-Baustelle;
      rueckweg-schau traegt drei Maskottchen-Bild-Beanstandungen, die auch
      am reinen HEAD stehen.
  - id: bloecke-format-mixpi-thema
    kind: howto
    title: "Das Bloecke-Format mixpi-thema/1 — Aussenhaut ueber den flachen Darstellungsfeldern"
    tags: [themen, darstellung, format, neue-oberflaeche, e119, 2026]
    source: >
      E119, 05.09.2026. Modul src/backend-api/src/mixpi-thema.ts, Wache
      mixpi-thema.spec.ts (21 Faelle, laeuft in den Backend-Kerntests);
      volle Suite 2726 gruen. Betreiber-Wunsch: "Bloecke ... im Prinzip
      haben wir es ja schon, nur eben klar und einfach strukturiert."
    related: [alte-oberflaeche-geloescht, mupi-themen-und-classic, farbkanal-feste-datei-statt-symlink]
    body: |
      DAS PRINZIP: Die ~40 flachen Felder in darstellung.json und anwenden()
      im NewDesign bleiben UNANGETASTET (samt Merge-Vertrag: Weglassen
      schaltet nichts ab). mixpi-thema.ts ist die lesbare Aussenhaut:
        vonBloecken(bloecke) -> flach     (Import/Anwenden)
        zuBloecken(flach)    -> Bloecke   (Export/Anzeige)
        pruefeThema(dok)     -> {ok,fehler[],bloecke,name}   (Tor-Wache)
        alsDokument(name, flach) -> die tauschbare .mixpi-thema-Datei

      DIE BLOECKE in v1 (nur Felder, die anwenden() WIRKLICH liest):
        farben{satz} licht kacheln{form,randAn,randFarbe,randBreite,groesse,
        abstand,namen,namenGroesse} reihen{groesse,albumTipp,diskografie}
        kissen{stufe,groesse,breite,hoehe,tasten,glas,titel,album,streifen}
        player{cover,akzent,glas,wellenAn,wellenHoehe,wellenTempo,
        wellenRegenbogen,fortschrittForm,fortschrittPunkt}
        kopf{uhr,restzeit,akku,lautstaerke,internet,wlan,bluetooth}
        verhalten{blaettern,platzBeimBlaettern,ruhigeMarke}

      ZWEI UMBENENNUNGEN MIT ABSICHT: kissen.streifen uebersetzt statusLeiste
      (die dokumentierte Namenskollision); reihen.albumTipp 'spielt'|'lanes'
      uebersetzt albumTippSpielt — eine WAHL, damit E121 'karte' anbauen
      kann. SONDERFALL: kissen.stufe (voll|micro|aus) und kissen.groesse
      teilen sich das flache miniPlayer (0 = aus, sonst Faktor) plus
      kissenMicro.

      ANGEKUENDIGT STATT STILL GESCHLUCKT: pruefeThema lehnt Felder der
      spaeteren Stufen mit Satz und Stufen-Nummer ab (kopf.schlummer ->
      E121, farben.ton -> E120, albumTipp=karte -> E121, leiste.* ->
      Politik/Achse). Die ANGEKUENDIGT-Tabelle im Modul ist die eine Liste.

      DIE MITGELIEFERTEN THEMEN stehen seit E119 in themen.ts ALS BLOECKE
      (THEMEN_BLOECKE = die Wahrheit); die flache THEMEN-Konstante wird beim
      Modul-Laden uebersetzt — Server-Vertrag unveraendert. Dabei fielen
      die toten Angular-Felder (aussehen, beimVerlassen, flipKarte,
      kopfIcons, plaetze, ...); Classic traegt jetzt NUR Wirkendes
      (runder Ring, groesse 1.8, Namen, Kissen aus) — seine Neufassung mit
      Farben ist E120.

      DIE WACHE (mixpi-thema.spec.ts) haelt DREI Dinge: jedes Whitelist-Feld
      hat einen LESER in NewDesign/app.js (gegen die 23-tote-Felder-Klasse);
      der Roundtrip Bloecke->flach->Bloecke ist verlustfrei fuer alle
      Mitgelieferten; das Tor nimmt die Mitgelieferten an und lehnt
      Unbekanntes mit Satz ab. WER EIN FELD ERGAENZT: Whitelist, Wirkung in
      anwenden() und ggf. ANGEKUENDIGT-Austrag im SELBEN Zug.

      STOLPERSTEIN BEIM TESTEN: tsx faehrt ESM — __dirname gibt es nicht;
      ein describe-Body, der damit wirft, erscheint als Suite-Fehler mit
      verwirrender Zaehlung (19 tests / 1 fail / 2 rote Zeilen).
      fileURLToPath(import.meta.url) nehmen.
  - id: classic-und-themen-tauschen
    kind: howto
    title: "E120: Classic mit Ur-Farben als Festwert-Satz — und Themen als Datei tauschen"
    tags: [themen, classic, farben, tausch, verwaltung, e120, 2026]
    source: >
      E120, 05.09.2026. Farbsatz-Generator + app.css + FARB_SAETZE +
      Einzeiler im Gleichklang (farbsaetze-bauen --pruefen ok, messen.mjs
      ohne classic-Verstoss); Tausch-Roundtrip an echtem Server
      (thema-tausch.integration.spec, 6 Faelle); volle Suite 2732 gruen.
    related: [bloecke-format-mixpi-thema, farbkanal-feste-datei-statt-symlink, mupi-themen-und-classic]
    body: |
      CLASSIC IST DER ELFTE FARBSATZ — als FESTWERT-Satz, nicht gedreht:
      tools/farbsaetze-bauen.mjs kennt seit E120 `werte: {hell,dunkel}` je
      Satz; classic traegt die Ur-Toene der geloeschten Oberflaeche
      (Flaeche #121212, Leiste #1F1F1F, Schrift #F4F5F8 — das fast-weisse
      Ring-Weiss). Die helle Fassung ist neutralgrau abgeleitet (die alte
      Oberflaeche WAR dunkel; wer hell schaltet, bekommt denselben unbunten
      Charakter statt Creme). KEINE zweite Farbmechanik: derselbe Wahler,
      dieselben Muster-Knoepfe, /farben.css-Ueberlagerung unveraendert.
      Wer einen Satz ergaenzt: Generator, app.css-Bloecke, FARB_SAETZE in
      app.js UND der ERLAUBT-Einzeiler in index.html — `--pruefen` haelt
      alle vier zusammen.

      LICHT UND FARBSATZ WIRKEN BEIM THEMENWECHSEL SOFORT: anwenden() reicht
      w.licht ans data-licht-Attribut und w.farbe an farbeSetzen(_, false)
      durch — OHNE Meldung (das Echo waere die Falle aus aussehenHolen).
      Ein Thema ohne die Felder laesst die Wahl des Kindes in Ruhe.

      DAS CLASSIC-THEMA (themen.ts, Bloecke) traegt jetzt:
      farben.satz='classic' + licht='dunkel' + runder weisser Ring 6px +
      groesse 1.8 + Namen an + Kissen aus. Zusammen IST das der Ur-Look.

      TAUSCHEN:
        GET  /api/thema/export/:name   -> <name>.mixpi-thema.json
             (Content-Disposition attachment; Bloecke aus themenErgaenzen,
             Mitgelieferte also immer exportierbar)
        POST /api/thema/import {dokument, ueberschreiben?}
             -> pruefeThema am Tor (400 mit SAETZEN, Stufen-Nummer bei
             angekuendigten Feldern); Namenskollision = 409 + name, erst
             ueberschreiben:true ersetzt; geschrieben wird atomar, aktuell
             bleibt unangetastet.
      In der Verwaltung (Darstellung -> Themen): je Thema ein ⬇, darunter
      „Aus Datei" mit Konflikt-Knopf, der den NAMEN nennt. Fehlersaetze des
      Tors stehen woertlich auf der Seite (white-space: pre-line).

      OFFEN (E120b, bewusst geschnitten): farben.ton/werte IM THEMA — die
      serverseitige Paletten-Rechnung fuer eigene Farbtoene. Der Weg ist
      vorgezeichnet (palette() aus farbsaetze-bauen.mjs), das Tor lehnt die
      Felder bis dahin mit E120-Wort ab.
  - id: uebernahmen-und-titelkarte
    kind: howto
    title: "E121: Zehn Uebernahmen aus dem Alten — und die Titelkarte, die aus der Kachel waechst"
    tags: [themen, uebernahmen, titelkarte, flip, neue-oberflaeche, e121, 2026]
    source: >
      E121, 05.09.2026, in drei Commits (4a+4c, 4b, 4d). e2e 29/29 (neuer
      Karten-Fall), Backend-Suite 2732; Fundstellen der Vorbilder:
      dokumentation/ALT-UEBERNAHMEN.md.
    related: [bloecke-format-mixpi-thema, classic-und-themen-tauschen, alte-oberflaeche-geloescht, vorschau-wird-geliehen]
    body: |
      DIE KLEINEN NEUN (je Feld: Whitelist + Wirkung im selben Zug, die
      Leser-Wache erzwingt es): kissen.endeZeit ("endet 19:42", aus DEMSELBEN
      Wert wie Balken+Zeit), kopf.titelRest (Zahl schreibt der
      Balken-Fueller, kopfMitte traegt nur den Schalter), kopf.schlummer
      (im Kinderzeit-Takt; LEER statt 0:00), kopf.btAkku (Kopfhoerer-Akku
      via bluetoothctl Battery Percentage; aus|prozent, balken angekuendigt),
      kissen.streifenLaenge, kacheln.namenAbstand, verhalten.beimVerlassen
      (stopp NUR am Nutzer-Rueckweg der Lane — nie in den vorsorglichen
      laneSchliessen-Aufrufern der Takte), verhalten.startKategorie (EINMAL,
      nur solange das Kind auf der Vorgabe steht; nimmt den echten
      Knopf-Weg), leiste.kategorien 'einer' (nur der aktive Knopf steht,
      der Tipp schaltet im Kreis; tote Alt-Werte reihe/aus wirken wie die
      Vorgabe). GESTRICHEN: player.lautKnoepfe — gr-leiser/gr-lauter gibt
      es seit je; ein Schalter ohne Unterschied waere eine Attrappe.

      DIE TITELKARTE (reihen.albumTipp='karte'): albumTippVerdrahten hat
      drei Antworten (spielt|lanes|karte). Die Karte besitzt KEINE
      Titel-Logik — weg.zeigen() baut die ECHTE Lane, die Karte hebt
      lane.element in ein fixed-Blatt und waechst per transform aus der
      angetippten Kachel (Start = getBoundingClientRect der Kachel, Ziel =
      das zentrierte Quadrat der alten Flip-Karte). KEIN rotateY — die
      3D-Drehung ist am Pi 5 als Zeichenschicht-Fresser gemessen.
      laneSchliessen() raeumt per element.remove() ortsunabhaengig; der
      Rueckweg-case 'lane' schliesst Karte UND Lane. Flach reist
      albumTipp (string) MIT dem bool-Alias albumTippSpielt — alte Leser
      und alte Dateien sehen dieselbe Antwort.

      DIE MESS-FALLE DES TAGES: Ein Browser-PANE, das nicht angezeigt wird,
      hat visibilityState 'hidden' — sichtbarerTakt() schaltet dann ALLE
      Takte ab, die Seite laedt scheinbar nichts ("Medien werden geladen",
      kein --mupi-mp), und es sieht exakt aus wie ein kaputter eigener
      Umbau. Der A/B-Beweis (HEAD-app.js zeigte DASSELBE) hat den Verdacht
      entlastet; der massgebliche Beleg ist der e2e mit eigenem, sichtbarem
      Browser. Wer im Pane misst: erst sichtbar schalten.
  - id: platzmachen-gilt-nur-fuer-das-kachelraster
    kind: gotcha
    title: "Die Leiste fuhr auch im Spiel ein — Platzmachen gehoert dem Raster, nicht jeder Rollbewegung"
    match: "menue verschwindet im spiel|leiste faehrt in der app ein|platzBeimBlaettern app|scrollen im spiel|menu weg beim spielen|geruehrt app"
    sev: med
    cause: "Betreiber (04.09.2026): 'wenn ich in einem spiel bin und aktiviert ist das beim scrollen das menue verschwindet, verschwindet es auch im spiel wenn ich scrolle.' `platzBeimBlaettern.geruehrt()` haengt am Rollereignis und fragte nur, ob die Sache eingeschaltet ist — nicht, WORIN gerollt wird. Die App-Buehne liegt aber vor dem Raster: Wer dort rollt, blaettert nicht durch Kacheln, und die eingefahrene Leiste schafft ihm keinen Platz, sondern nimmt ihm die Orientierung"
    fix: "Eine Zeile in `geruehrt()`: `if (imBild($('sch-buehne'))) return`. GEFRAGT, NICHT GEMERKT — dieselbe Regel, die in derselben Funktion schon die alte Sperre ersetzt hat; ein gemerktes Flag muesste an jedem Weg zurueckgesetzt werden, der die Buehne schliesst, und genau daran ist die Sperre viermal gescheitert. NUR DAS EINFAHREN wird verhindert, nicht das Zurueckholen: war die Leiste beim Oeffnen der App schon eingefahren, laeuft ihre Uhr weiter und bringt sie zurueck — sonst waere aus der Behebung eine neue Klemme geworden. Vom Betreiber am Geraet bestaetigt"
    tags: [newdesign, leiste, apps, scrollen, ui, 2026]
    related: [der-mitschnitt-zapft-die-quelle-an-nie-eine-senke, wache-auf-sorte-nicht-auf-pfad]
    body: |
      ══ DIE GRENZE DIESER BEHEBUNG, ausdruecklich ════════════════════════════
      Geprueft wird auf EINE Buehne (`sch-buehne`), nicht auf die SORTE
      „etwas liegt vor dem Raster". Die Lage kennt aber acht Ueberlagerungen:

          eltern, schnell, appBuehne, schubladeOffen,
          albumGross, grosserPlayer, titelListe, lane

      Fuer `lane` und `titelListe` ist gesorgt (sie waehlen die Ruhezeit),
      fuer die App-Buehne jetzt auch. Ob `eltern`, `schnell`, `albumGross`
      oder der grosse Player dasselbe Problem haben, ist NICHT GEMESSEN — es
      wurde nur nicht gemeldet. Wer dort einmal rollt und die Leiste
      einfahren sieht, hat denselben Fehler vor sich und braucht dieselbe
      Zeile.

      DAS IST DIE BEKANNTE SCHWAECHE [[wache-auf-sorte-nicht-auf-pfad]], hier
      auf der Oberflaechenseite: Ein Merkmal am NAMEN einer Buehne haelt nur,
      solange es diese eine Buehne gibt. Sauberer waere eine Frage nach der
      Sorte — „liegt irgendeine Vollbild-Ebene vor dem Raster?". Dass sie
      nicht gebaut wurde, hat einen Grund: Welche der acht Ebenen das Rollen
      wirklich fuer sich beanspruchen, ist eine HOER- und SEH-Entscheidung am
      Geraet, keine, die man am Quelltext faellt. Eine pauschale Sperre haette
      leicht das Platzmachen dort abgeschaltet, wo es gewollt ist.

      ══ WIE MAN ES NACHMISST ═════════════════════════════════════════════════
      `tools/leiste-platz-schau.mjs` gegen eine Vorschau. UND DIE GEGENPROBE
      IST PFLICHT (das Werkzeug sagt es selbst): Bevor ein „bleibt draussen"
      etwas wert ist, muss dieselbe Messung zeigen, dass gewoehnliches Rollen
      im Raster ueberhaupt EINFAEHRT. Sonst prueft die ruhige Messung das
      Nichts — die Vorschau hatte `platzBeimBlaettern` schon einmal gar nicht
      eingeschaltet, und die Probe meldete brav „ruhig".

      Fuer diese Behebung gibt es KEINEN automatischen Zeugen:
      `pruef-neu-regeln.js` prueft die reinen Regeln der Apps, nicht das
      Zeitverhalten der Leiste. Bestaetigt wurde sie am Geraet, vom
      Betreiber.
  - id: nachtippen-macht-den-anlauf-laenger-nicht-kuerzer
    kind: gotcha
    title: "Der zweite Tipp macht es LANGSAMER — der Server nimmt jeden an, und jeder faengt von vorn an"
    match: "verzoegerung beim tippen|mehrfach gefeuert|doppelt getippt spielt zweimal|titel tippen dauert|reagiert nicht sofort spielen|ungeduldig nachtippen|spielen langsam ui"
    sev: high
    cause: "Betreiber (04.09.2026): „beim klicken von titeln gibt es eine verzoegerung das man nochmal klickt dadurch wird es mehrfach gefeuert.\" `POST /api/spielen` weist einen zweiten Tipp NICHT ab — er antwortet dreimal HTTP 200 `ok`. Die Abloese im Server (`spielLaufBeginnen`) rettet nur die HINTERGRUNDARBEIT des aelteren Laufs vor dem Durcheinander; den Anlauf (Anhalten, Spotify-Atempause, Startbefehl) bezahlt jeder Tipp voll"
    fix: "Ein Riegel in `spielenAnfordern` (NewDesign/app.js, `tippLaeuft`) — an der EINEN Stelle, durch die jeder Tipp geht, nicht an den neun Rufstellen. Geschluckt wird nur der IDENTISCHE Tipp (Werk plus ganzer Wunsch); ein anderer Titel ist ein neuer Entschluss und geht durch. Der geschluckte bekommt das VERSPRECHEN des ersten zurueck, nicht einen Ersatzwert. Nachklang 800 ms, aus dem gemessenen Abstand zwischen Antwort und Ton. Zeuge: tools/titeltipp-mehrfach-probe.mjs (vier Faelle), Messung: tools/titeltipp-verzoegerung.py"
    tags: [newdesign, spielen, ui, perf, tippen, e118, 2026]
    related: [ui-ruft-die-spielfunktion, wache-auf-sorte-nicht-auf-pfad, klick-sturm-laedt-nichts-mehr, gegenprobe-statt-gruen-glauben]
    body: |
      ══ DIE ZAHLEN, am Geraet (Box .62, Lautstaerke 31) ══════════════════════

                           ein Tipp    drei Tipps im 900-ms-Abstand
        Spotify-Kapitel     596 ms          3312 ms   bis Ton
        lokales Kapitel    1340 ms          3144 ms   bis Ton

      DIE LESEWEGE SIND UNSCHULDIG: /api/werke antwortet in 6 ms,
      /api/state in 1,6 ms, /inhalt in 108-615 ms. Die Zeit steckt im
      START, und der ist bei EINEM Tipp ertraeglich.

      DAS IST EIN VERSTAERKER, KEIN ADDIEREN. 600 ms sind aergerlich, 3,3
      Sekunden sind kaputt — und die 3,3 Sekunden entstehen erst durch die
      Reaktion auf die 600. Wer nur die Grundverzoegerung sucht, findet die
      halbe Sache.

      ══ WARUM DIE ABLOESE IM SERVER NICHT REICHT ═════════════════════════════
      `spielLaufBeginnen()` setzt eine Laufnummer, und jeder aeltere Lauf
      steigt beim naechsten `meinLauf !== spielLauf` aus. Das verhindert, dass
      zwei Laeufe durcheinander `tracknr:` in fremde Werke schicken — eine
      wichtige Sache, aber eine ANDERE. Es verhindert NICHT, dass drei Tipps
      dreimal anhalten und dreimal starten. Wer die Abloese fuer eine
      Entprellung haelt, sucht den Fehler an der falschen Stelle.

      ══ DREI ENTSCHEIDUNGEN, DIE DEN RIEGEL TRAGEN ═══════════════════════════

      NUR DER IDENTISCHE TIPP. Das Ziel ist `schluessel|JSON(wunsch)`.
      Kapitel 5 zweimal ist Ungeduld; Kapitel 5 und dann Kapitel 8 ist ein
      neuer Entschluss. Ein pauschales „waehrend etwas laeuft geht gar
      nichts\" naehme dem Kind das Umentscheiden — es tippt Kapitel 8, es
      passiert nichts, und niemand versteht warum. EIN RIEGEL, DER ZU VIEL
      SCHLUCKT, IST SCHLIMMER ALS KEINER.

      DAS VERSPRECHEN DES ERSTEN, KEIN ERSATZWERT. Drei Rufstellen halten
      ihre Kachel selbst hell, bis das Versprechen eingeloest ist (ARD-Folge,
      ARD-Weiterhoeren, Weiterhoeren-Reihe). Ein sofort erfuellter Wert
      loeschte ihr Leuchten mitten im noch laufenden Start — der zweite Tipp
      haette die Anzeige des ersten kaputtgemacht. So wartet er mit und
      erfaehrt DESSEN Ausgang; keine Rufstelle muss vom Riegel wissen.

      ER SCHWEIGT NICHT. Der geschluckte Tipp laesst die Kachel ERNEUT
      pulsen (`tippEcho`, mit erzwungener Neuberechnung — sonst laeuft die
      Animation beim zweiten Mal nicht noch einmal ab). Ein Knopf, der sich
      tot anfuehlt, ist ja der GRUND fuers Nachtippen; ihn stumm zu machen
      hiesse, das Symptom zu verstecken und die Ursache zu verschaerfen.

      GELOEST WIRD IM `finally`, nicht an den Ausgaengen. Es gibt sechs davon
      (kein Netz, 403, 404, 409, Fehlschlag, Erfolg); einer wird beim
      naechsten Umbau vergessen, und dann klemmt das Werk stumm, bis jemand
      etwas anderes tippt. Ein Riegel, der haengenbleibt, ist schlimmer als
      gar keiner.

      ══ WAS BLEIBT ═══════════════════════════════════════════════════════════
      Die GRUNDVERZOEGERUNG. 596 ms bei Spotify und 1340 ms lokal sind
      weiterhin da; der Riegel verhindert nur, dass sie sich vervielfacht.
      Woher die 690 ms des lokalen `POST /api/spielen` kommen, ist NICHT
      aufgeschluesselt.
  - id: die-rueckmeldung-allein-bezeugt-den-riegel-nicht
    kind: falle
    title: "Der Test prueft die RUECKMELDUNG und meint den RIEGEL — beides ist gleichzeitig da, auch ohne Riegel"
    match: "gegenprobe bleibt gruen|test misst nicht was er meint|zeuge prueft das falsche|echo bleibt gruen|zwei wirkungen ein test|entprellung testen"
    sev: med
    cause: "Am 04.09.2026 beim Riegel gegen Mehrfachtippen (E118). Der Zeuge hatte vier Faelle; einer hiess `echo` und fragte: „laesst der geschluckte Tipp die Kachel erneut pulsen?\" In der Gegenprobe (Riegel lahmgelegt) fiel `ungeduld` sauber auf 3 Wuensche — und `echo` blieb GRUEN. Zu Recht: Die Kachel pulst auch ohne Riegel, dann eben fuer den zweiten ECHTEN Start. Der Fall mass `tippEcho`, nicht den Riegel"
    fix: "Wo eine Behebung ZWEI Wirkungen hat, die am selben Element sichtbar werden, prueft eine Frage nach EINER von ihnen gar nichts. Die Aussage ist das PAAR: „die Kachel antwortet UND es ist trotzdem nur ein Wunsch draussen.\" Beides im selben Augenblick ablesen, beides in dieselbe Bedingung. Danach faellt der Fall in der Gegenprobe mit"
    tags: [test, gegenprobe, zeuge, falle, ui, e118, 2026]
    related: [gegenprobe-statt-gruen-glauben, nachtippen-macht-den-anlauf-laenger-nicht-kuerzer, zaehlende-probe-sieht-keinen-widerspruch]
    body: |
      ══ DIE FORM DES FEHLERS ═════════════════════════════════════════════════
      Eine Behebung tut zwei Dinge auf einmal:

          1. sie SCHLUCKT den zweiten Start
          2. sie ANTWORTET dem zweiten Tipp trotzdem

      Am Bildschirm sieht (2) gleich aus, ob (1) passiert ist oder nicht —
      denn ohne Riegel geht ein echter Start hinaus, und der leuchtet
      genauso. Der Zeuge sah also eine leuchtende Kachel und war zufrieden.

      DAS IST NICHT DASSELBE WIE EIN SCHLECHT GESCHRIEBENER TEST. Der Fall
      war richtig gedacht und richtig benannt; nur beantwortet das, was er
      ABLIEST, seine Frage nicht eindeutig. Solche Faelle bestehen jede
      Durchsicht, weil sie plausibel klingen.

      ══ WORAN MAN SIE ERKENNT ════════════════════════════════════════════════
      Die Frage stellen: „Gibt es einen Zustand, in dem mein Messwert
      DASSELBE zeigt, obwohl die Behebung fehlt?\" Bei einer Rueckmeldung,
      die ohnehin bei jedem Erfolg kommt, lautet die Antwort fast immer ja.

      DIE GEGENPROBE FINDET ES — ABER NUR, WENN MAN JEDEN FALL EINZELN
      ANSIEHT. Hier fiel einer von vier, und der Lauf war rot; „rot\" haette
      als Beleg gereicht, wenn man nur auf die Bilanz gesehen haette. Erst
      die Zeile fuer Zeile gelesene Ausgabe zeigte, dass DER FALL, DER DIE
      RUECKMELDUNG PRUEFT, gar nicht mitgefallen ist. Eine Gegenprobe ist
      erst ausgewertet, wenn man weiss, WELCHE Faelle gefallen sind — nicht
      WIE VIELE.

      ══ DIE BEHEBUNG IM TEST ═════════════════════════════════════════════════
      Beide Messwerte im selben Augenblick nehmen und UND-verknuepfen:

          const pulst   = <Kachel traegt .laeuft>
          const wuensche = <Zahl der abgeschickten Starts>
          merk('echo', pulst && wuensche.length === 1, …)

      Und der Fehlertext muss die beiden Faelle UNTERSCHEIDEN — „pulst nicht\"
      und „pulst, aber es gingen zwei Starts hinaus\" sind verschiedene
      Befunde, und wer sie zusammenfasst, steht beim naechsten Mal wieder vor
      derselben Frage.
  - id: rueckmeldung-beim-aufsetzen-nicht-erst-beim-loslassen
    kind: gotcha
    title: "Der Knopf antwortete erst beim Loslassen — und die Titelkachel gar nicht"
    match: "keine rueckmeldung beim tippen|fuehlt sich tot an|reagiert nicht sofort|optische rueckmeldung fehlt|kachel gibt nicht nach|druckbewegung fehlt|active greift nicht"
    sev: high
    cause: "Betreiber (04.09.2026), nachdem der Riegel gegen Mehrfachtippen ausgeliefert war und sich nichts besser anfuehlte: „ich denke man klickt mehrfach wenn nichts passiert auch eine schnelle optische rueckmeldung fehlt.\" `click` faellt erst beim LOSLASSEN — bis dahin passierte nichts. Und ausgerechnet die Kachel aus der Meldung, die Titelkachel, stand in keiner einzigen `:active`-Regel: `.kachel:active .kachel-bild` gab es seit jeher, `.lane-kachel` nie"
    fix: "Ein Zuhoerer auf `pointerdown` (NewDesign/app.js, `DRUCK_ZIELE`) setzt die Klasse `gedrueckt`; die bestehenden `:active`-Regeln teilen sich mit ihr EINE Deklaration. NICHT auf `:active` allein bauen — dass es unter einem echten Finger greift, liess sich nicht nachweisen. Geloest wird bei `pointercancel`, bei Bewegung ueber 12 px und beim Loslassen, dazu ein Notloeser nach 2 s. Gemessen: tools/druck-rueckmeldung-schau.mjs"
    tags: [newdesign, ui, rueckmeldung, tippen, css, e119, 2026]
    related: [nachtippen-macht-den-anlauf-laenger-nicht-kuerzer, wache-auf-sorte-nicht-auf-pfad, messinstrument-gegen-bekanntes-signal, beruehrziele-neue-oberflaeche-gemessen]
    body: |
      ══ ZWEI RUECKMELDUNGEN, ZWEI ZEITPUNKTE ═════════════════════════════════

        beim AUFSETZEN   reine Darstellung, kein Handler, kein Netz. Sie ist
                         da, bevor irgendetwas anderes passiert.
        beim LOSLASSEN   `click` faellt erst dann, und erst dann setzt
                         `tippEcho` die Klasse `laeuft`.

      Es gab nur die zweite. Wer den Finger einen Moment liegen laesst, sah
      bis dahin NICHTS — und tippte noch einmal.

      DAS IST DIE URSACHE, NICHT DIE FOLGE. Der Riegel gegen Mehrfachtippen
      [[nachtippen-macht-den-anlauf-laenger-nicht-kuerzer]] verhindert, dass
      der zweite Tipp Schaden anrichtet; er beantwortet nicht, WARUM ein
      zweiter kommt. Beides wird gebraucht, aber nur eines davon fuehlt sich
      anders an.

      ══ WARUM NICHT EINFACH `:active` ════════════════════════════════════════
      Weil sich nicht NACHWEISEN liess, dass die Zeichen-Maschine sie unter
      einem echten Finger setzt. Das Messwerkzeug haelt die Frage an ein
      bekanntes Signal — einen Testknopf, der beim Druck Farbe UND Matrix
      wechselt:

          Eichung   :active greift NICHT   pointerdown-Klasse greift

      Ob das nur an der Messumgebung liegt oder auch auf der Box gilt, ist
      von aussen nicht zu entscheiden. GENAU DESHALB darf die Behebung nicht
      darauf bauen: Eine Regel ausliefern, deren Wirkung man nicht nachweisen
      kann, heisst hoffen. Die Klasse ist nachweisbar, in jeder Maschine, mit
      Maus wie mit Finger.

      ZWEI AUSLOESER, EINE DEKLARATION — kein Widerspruch zur Regel „eine
      Wahrheit an einem Ort": Was passiert (`transform: scale(0.95)`) steht
      genau einmal da, nur der Weg dorthin ist zweifach abgesichert.

      ══ WER ROLLT, DRUECKT NICHT ═════════════════════════════════════════════
      Ein Finger, der die Lane verschiebt, setzt zuerst auf einer Kachel auf.
      Bliebe sie gedrueckt stehen, waere aus der Rueckmeldung eine LUEGE
      geworden — sie verspraeche einen Tipp, den es nie gab. Drei Wege loesen
      sie: `pointercancel` (die Maschine hat den Finger fuers Rollen
      beansprucht), eine Bewegung ueber 12 px, und das Loslassen. Dazu ein
      Notloeser nach 2 s fuer den verlorenen Zeiger: Auf einer Kinderbox darf
      keine Kachel dunkel stehenbleiben.

      Gemessen wird das mit einer echten ZUGBEWEGUNG, nicht mit einem
      Ereignis von Hand: beim Aufsetzen 1 gedrueckt, nach 48 px Zug 0, nach
      dem Loslassen 0.

      ══ WAS DAS NICHT LOEST ══════════════════════════════════════════════════
      Die Grundverzoegerung bis zum TON. Aufgeschluesselt (Box .62):

          /local am Abspieldienst              0,8 ms
          /current/stop, waehrend etwas laeuft  224-315 ms
          /current/stop, im Leerlauf              4-7 ms
          /api/spotify/bereit                   79-172 ms

      Das ANHALTEN ist der teure Teil, und es kostet nur, wenn wirklich etwas
      laeuft. `spielenAnhalten` macht dafuer ZWEI Anfragen nacheinander an
      den Abspieldienst (erst `/local`, um zu erfahren ob Spotify lief, dann
      `/stop`); bei Spotify steht `/api/spotify/bereit` als dritte davor. Ob
      sich davon etwas zusammenlegen oder vorziehen laesst, ist NICHT
      untersucht.
  - id: touch-emulation-wirkt-erst-beim-naechsten-laden
    kind: falle
    title: "Erst den Finger einschalten, dann laden — sonst misst man eine Seite ohne Beruehrungsschirm"
    match: "ontouchstart false|setTouchEmulationEnabled wirkt nicht|finger kommt nicht an|touch probe misst nichts|active greift nicht cdp|fingerAufbau reihenfolge"
    sev: high
    cause: "Am 04.09.2026 beim Messen der Druckantwort: `fingerAufbau` (Emulation.setTouchEmulationEnabled) wurde NACH `Page.navigate` gerufen. Das Schalten wirkt auf `window.ontouchstart` aber erst beim naechsten Aufbau der Seite. Die Probe meldete daraufhin drei Kachelsorten als „ohne Druckantwort\" — gegen eine Seite, die gar keinen Beruehrungsschirm zu haben glaubte. `fingerBestaetigen` sagte es im Rueckgabewert (`ontouchstart: false, taugt: false`); geprueft worden war nur auf „truthy\", und ein Objekt ist immer truthy"
    fix: "REIHENFOLGE: fingerAufbau -> Page.navigate -> fingerBestaetigen, und das Ergebnis auf `.taugt` pruefen, nicht auf Vorhandensein. Und danach das Messgeraet an ein BEKANNTES SIGNAL halten: einen Testknopf einhaengen, der beim Druck Farbe und Matrix wechselt. Liest die Messung IHN als stumm, misst sie sich selbst"
    tags: [cdp, chromium, touch, messen, falle, werkzeug, 2026]
    related: [messinstrument-gegen-bekanntes-signal, attrappe-luegt-durch-weglassen, rueckmeldung-beim-aufsetzen-nicht-erst-beim-loslassen]
    body: |
      ══ DREI FEHLER DESSELBEN LAUFS, und alle drei lasen sich wie Befunde ════

      1. REIHENFOLGE. `setTouchEmulationEnabled` nach dem Laden gerufen —
         `ontouchstart` blieb false, drei Sorten galten als stumm.

      2. TRUTHY STATT `.taugt`. `fingerBestaetigen` gibt ein OBJEKT zurueck,
         kein Ja/Nein. `if (!ergebnis)` ist damit immer wahr, auch wenn
         drinsteht, dass nichts taugt. Wer eine Auskunft einholt und dann nur
         prueft, DASS sie kam, hat sie nicht gelesen.

      3. DAS MESSWERK BLIND GEWAEHLT. Die erste Kachel des Rasters klappt
         nicht zwangslaeufig eine Liste auf; „gibt es hier nicht" las sich wie
         ein Ergebnis. Das Messwerk kommt aus der Attrappe (art=album MIT
         mehreren Quellen), nicht aus dem Raster.

      DAZU EIN VIERTER, der erst spaeter auffiel: Die eigenen Messtipps sind
      echte TIPPS (Aufsetzen UND Loslassen). Der Tipp auf die Raster-Kachel
      machte die Lane wieder zu, und die Rollmessung danach mass das Nichts
      und sagte brav „ohne Titel-Kachel".

      ══ DIE EICHUNG IST DIE ANTWORT AUF ALLE VIER ════════════════════════════
      Ein Testknopf mit unuebersehbarer Druckregel, eingehaengt und wieder
      entfernt, VOR jeder Aussage. Er beantwortet die Frage, die sonst offen
      bleibt: Kann diese Messung ueberhaupt sehen, wonach sie sucht?

      UND SIE HAT MEHR GESAGT ALS ERWARTET. Geeicht wurde auf ZWEI Wegen —
      `:active` und eine per `pointerdown` gesetzte Klasse. Ergebnis:

          :active  greift NICHT        pointerdown-Klasse  greift

      Aus einer Selbstpruefung des Werkzeugs wurde damit ein BEFUND ueber die
      Umgebung, und der hat die Behebung entschieden: nicht auf `:active`
      bauen. Ein Messgeraet, das seine eigene Blindheit meldet, sagt einem
      damit auch, worauf man sich verlassen darf.
  - id: fristloser-fetch-aus-der-bootminute-verstopft-den-prozess
    kind: falle
    title: "Ein fetch ohne Frist aus der Bootminute haengt fuer immer — und der Prozess traegt seinen toten Ausgang bis zum Neustart"
    match: "spotify haengt nach reboot|fetch ohne timeout|bereit antwortet nicht|server haengt nach boot|AbortSignal timeout fehlt|alles langsam nach neustart|boot stau"
    sev: high
    cause: "04.09.2026, nach einem Box-Neustart: JEDER Spotify-Weg des Servers hing (bereit stumm, Durchreiche 12 s Leere, Tipps im Timeout, der lokale Rueckfall wartete 4 s auf den haengenden Versuch). Der Abspieldienst daneben antwortete in 0,2 s, ein nacktes node-fetch in 0,6 s, Netz und Uhr sauber. Der Server war in der BOOTMINUTE gestartet, als das WLAN noch nicht stand — seine Verfuegbarkeitspruefung feuert beim Start zwei Dutzend fetches, und sechs von acht Spotify-fetches trugen KEINE Frist. Eine haengende Verbindung ohne Frist gibt nie auf"
    fix: "AbortSignal.timeout an JEDEM fetch, der das Haus verlaesst (5-15 s je Weg), dazu eine Eine-Anfrage-Sperre am Token. Eingrenzung nach dem Muster: Nachbarprozess fragen, nacktes node -e fetch fragen, Netz und Uhr pruefen — antwortet alles ausser dem einen Prozess, ist ES der Prozess, und ein Dienst-Neustart bestaetigt es. Seit E120 schreibt die E118-Uhr je langsamem Tipp die Phasen ins Protokoll"
    tags: [server, fetch, frist, boot, spotify, undici, e120, 2026]
    related: [klick-sturm-laedt-nichts-mehr, einmal-hinsehen-ist-keine-messung, soloist-pair-endet-nicht-und-meldet-sich-spaet]
    body: |
      ══ WARUM ES NACH DEM NEUSTART SCHLIMMER WAR ALS VORHER ══════════════════
      Der Neustart sollte aufraeumen (zram voll, Spotify-Geraet weg) — und
      genau er erzeugte die Lage: Dienste starten in der Bootminute, das
      WLAN kommt Sekunden SPAETER. Alles, was in diesem Fenster fristlos
      hinausruft, haengt — nicht eine Weile, sondern FUER IMMER. Von aussen
      sieht das aus wie „Spotify kaputt", und die naechstliegende Antwort
      (noch ein Neustart) wuerfelt nur neu, welche Verbindungen es trifft.

      ══ DIE EINGRENZUNG, die zum Fund fuehrte ════════════════════════════════
        Abspieldienst (Nachbarprozess)   0,2 s     gesund
        node -e 'fetch(accounts…)'       0,6 s     gesund
        date / DNS / TLS per curl        sauber    gesund
        DERSELBE Aufruf aus dem Server   haengt    krank

      Wenn alles antwortet ausser dem einen Prozess, liegt es nicht am Netz,
      nicht an der Gegenstelle und nicht an der Maschine — es liegt an dem,
      was DIESER Prozess mit sich herumtraegt.

      ══ DIE ZWEI HEILUNGEN, und warum beide noetig sind ══════════════════════
      Der DIENST-NEUSTART heilt sofort (frischer Prozess, Netz steht) — aber
      nur bis zum naechsten Boot. Die FRISTEN heilen dauerhaft: eine
      Verbindung, die haengt, stirbt nach Sekunden, und der naechste Aufruf
      beginnt frisch. Wer nur neu startet, hat das Muster nicht behoben;
      wer nur Fristen setzt, dem hilft es am kranken Prozess erst nach dem
      naechsten Start.

      ══ NEBENFUNDE DESSELBEN NACHMITTAGS ═════════════════════════════════════
      * Die playing-FLAGGE ist nicht das OHR: t_post 319 ms und Ton nach
        11 s kommen im selben Tipp vor. Gemessen wird bis zum PEGEL
        (> 0.02 in /player/local), sonst misst man den Server und nennt es
        Hoererlebnis. Was nach der Serverantwort bleibt, ist der
        Spotify-Stromstart selbst (soloist, 2-11 s je Laune) — dem Weg
        dorthin nicht anzulasten.
      * Dieselbe unveraenderte Titelliste wurde je Kapitel-Tipp neu von
        Spotify geholt (672-3952 ms), im E110-Wahlfall doppelt. Seit E120:
        45-s-Merker, nur Status 200, 3-8 ms.
      * Die Geraetelisten-Frage stand in jedem Tipp (563 ms). Seit E120:
        15-s-Merker, NUR DAS JA — ein gemerktes Nein hielte die Box nach
        einem soloist-Neustart fuer tot, obwohl sie laengst zurueck ist.
  - id: klangwerk-nimmt-keine-lautstaerke-an
    kind: falle
    title: "E94 hat eine nackte Wurzel: die Filter-Ketten-Senke klangwerk ignoriert jedes set-volume — die ALSA-Senke nimmt es an"
    match: "lautstaerke laesst sich nicht stellen|wpctl set wirkungslos|klangwerk volume|boot laut 100|lautstaerketasten tot|filter-chain volume|e94 wurzel"
    sev: high
    cause: "04.09.2026, nach dem Box-Neustart: Lautstaerke auf 100 % (Kinderbox!), und JEDER Stellweg lief ins Leere — wpctl auf @DEFAULT_AUDIO_SINK@, wpctl auf die ID, pw-cli set-param Props, pactl, mupi-lautstaerke.sh set, setvolume ueber den Abspieldienst: alle ohne Fehlermeldung, alle ohne Wirkung. Die Standardsenke ist `klangwerk` — eine Filter-Kette (node.description „Klangwerk (Ton-Plugins)\"), und DIE nimmt schlicht keine Lautstaerke an. Die echte ALSA-Senke daneben nimmt sie an: `wpctl set-volume 65 31%` stand und hielt"
    fix: "Stellen und lesen an der ALSA-Senke (node.name alsa_output…), nicht an der Standardsenke — und wer setzt, LIEST ZURUECK, im Sekundentakt mehrfach (sofort ignoriert und spaeter zurueckgedreht sehen gleich aus). SEIT DEM 04.09.2026 ABENDS GEBAUT (E121-Commit): mupi-lautstaerke.sh regelt die Hardware-Senke (bluez vor alsa, ueber die ID), einheit_nachziehen nimmt sie aus und stellt Durchreichen auf 100, die Ruecklese prueft jedes Setzen (0,3 s Geduld). Der Dienst-Neustart meldete danach erstmals: Lautstaerke vom Neustart unberuehrt"
    tags: [ton, lautstaerke, pipewire, klangwerk, filterchain, e94, e120, 2026]
    related: [drei-skalen-fuer-eine-lautstaerke, die-ganze-tonstrecke-und-wo-die-pegel-entstehen, einmal-hinsehen-ist-keine-messung]
    body: |
      ══ DER VERSUCHSSTAND, damit ihn niemand wiederholt ══════════════════════

        wpctl set-volume @DEFAULT_AUDIO_SINK@ 31%   still ignoriert
        wpctl set-volume 87 31%   (klangwerk-ID)    still ignoriert
        pw-cli set-param 87 Props {channelVolumes}  nur `mute` kam an
        pactl set-sink-volume @DEFAULT_SINK@ 31%    Cookie-Fehler, wirkungslos
        mupi-lautstaerke.sh set 31                  laeuft durch, wirkungslos
        /current/setvolume:31 (Abspieldienst)       status ok, wirkungslos
        wpctl set-volume 65 31%   (ALSA-Senke)      WIRKT und haelt

      Die Sofortkontrolle gehoert dazu: nach dem Setzen MEHRFACH lesen
      (fuenfmal im 0,4-s-Takt). „Wird sofort ignoriert" und „wird von etwas
      zurueckgedreht" saehen bei einem einzelnen Blick gleich aus — hier war
      es das Ignorieren: nie auch nur einen Augenblick auf 0.31.

      ══ WARUM DAS BIS HEUTE VERBORGEN BLIEB ══════════════════════════════════
      Im LAUFENDEN Betrieb stand die Senkenlautstaerke irgendwann richtig
      (gestellt zu einem Zeitpunkt, als der Weg noch trug — vermutlich vor
      dem Einbau der Filter-Kette als Standardsenke) und BLIEB so. Erst der
      Boot legt die Kette neu an, mit 1.0 — und dann zeigt sich, dass kein
      Stellweg mehr existiert. Deshalb sah E94 wie „der Neustart reisst die
      Lautstaerke hoch" aus: In Wahrheit reisst der Neustart nichts hoch,
      er legt eine UNSTELLBARE Senke auf Vorgabe an.

      ══ WAS DAS FUER DIE TONSTRECKE HEISST ═══════════════════════════════════
      Hoerbar ist klangwerk x ALSA. klangwerk bleibt fest auf 1.0 (gut so:
      der Mitschnitt zapft die QUELLE an und bleibt von alledem unberuehrt,
      und die Mischsenke soll gar nicht regeln). Die NUTZERLAUTSTAERKE
      gehoert auf die ALSA-Senke — genau die nimmt sie an. Das kollidiert
      mit `einheit_nachziehen`, das die ALSA-Senke heute auf 85 % angleicht;
      die Ausnahme dort ist Teil des Fixes, sonst stellt die naechste
      Angleichung die Nutzerwahl zurueck.

      NACHTRAG VOM SELBEN ABEND: Die Kette ist WETTERWENDISCH, nicht immun —
      mittags ignorierte klangwerk fuenf Stellbefehle in Folge, abends nahm
      es eine 85-%-Angleichung an. Deshalb duerfen Durchreichen nicht auf
      der Obergrenze stehen (0,85 x 0,31 = heimlich leisere Box), sondern
      auf 100: seit die LETZTE Stufe regelt, haelt die Zusage strukturell.

      NACHTRAG VOM SPAETEN ABEND (E125): Es gilt fuer ALLE hausgebauten
      virtuellen Senken. Auch `ueberall` (Kombi) nahm wpctl erst an (0.40
      hielt) und ignorierte denselben Aufruf nach ihrem Neuaufbau. Auch der
      AUSGANGSSTROM des Klangwerks gehorcht nicht (gesetzt 31, stand 100) —
      „Stroeme gehorchen pactl immer" gilt fuer CLIENT-Stroeme, nicht fuer
      Filter-Ausgaenge. Und pactl LIEST die Kombi dauerhaft als 100 %,
      egal was der Knoten traegt. DIE ORDNENDE REGEL: Lautstaerke nur auf
      Hardware-Senken (alsa/bluez) und Client-Stroemen — hausgebaute
      Senken sind Durchreichen, keine Regler, und wer sie stellt, baut
      einen Schalter, der mal wirkt und mal nicht.

      NICHT GEKLAERT und ehrlich benannt: WARUM die Filter-Kette Stellbefehle
      mal annimmt und mal nicht. Der Fix braucht diese Antwort nicht; wer
      die Kette eines Tages regeln lassen will, muss sie zuerst finden.
  - id: anmeldewache-holt-soloist-in-die-geraeteliste-zurueck
    kind: feature
    title: "Die Anmeldewache (E121): faellt die Box still aus Spotifys Geraeteliste, startet der Server soloist von selbst neu"
    match: "box fehlt geraeteliste|spotify weg von selbst|soloist abgemeldet|spotify-anmeldung dauernd|box nicht in spotify|anmeldewache|wer startet soloist neu"
    sev: med
    cause: "Dreimal am 04.09.2026: soloist sagt „logged in\", systemd sagt „active\", und die Box steht trotzdem nicht in Spotifys Geraeteliste — jeder Spotify-Tipp endet in `spotify-anmeldung`. `systemctl restart soloist` registriert binnen sechs Sekunden neu; es merkte nur niemand, DASS es noetig war. `systemctl is-active` ist fuer diese Frage wertlos (E117)"
    fix: "server.ts fragt im Minutentakt die Bereitschaftsauskunft; das URTEIL ist rein (anmeldewache.ts, 8 Faelle): nur engine=soloist, nur das echte `box-fehlt` zaehlt (nicht-pruefbar zaehlt nicht UND entwarnt nicht), ZWEI Treffer in Folge, NIE in laufende Spotify-Musik, zehn Minuten Abstand zwischen Neustarts. Am Geraet bewiesen: soloist gestoppt -> 100 s spaeter von der Wache zurueckgeholt, `bereit: true`"
    tags: [spotify, soloist, wache, server, e121, 2026]
    related: [soloist-pair-endet-nicht-und-meldet-sich-spaet, spotify-uebernahme-legt-librespot-lahm, fristloser-fetch-aus-der-bootminute-verstopft-den-prozess, dauerrote-wache-ist-keine]
    body: |
      ══ WARUM DIE VERBOTS-REGELN VORN STEHEN ═════════════════════════════════
      Die gefaehrlichste Fehlform dieser Wache ist nicht „startet nie neu",
      sondern „startet falsch": in laufende Musik hinein (Tonabriss), im Takt
      einer Netzstoerung (die keine Auskunft ueber die Box ist), oder als
      Schleife (wenn der erste Neustart nicht hilft, ist der Fehler woanders).
      Jede dieser Richtungen hat einen eigenen Testfall, und die Gegenprobe
      (Schwelle auf 99 sabotiert) macht zwei davon rot.

      ══ DER GERAETEBEWEIS, nachfahrbar ═══════════════════════════════════════
        sudo systemctl stop soloist          # die Sabotage
        # 18:06:22  Anmeldewache: box-fehlt (1/2)
        # 18:07:22  Anmeldewache: box-fehlt zum 2. Mal — soloist wird neu gestartet
        # 18:07:29  soloist aktiv, /api/spotify/bereit -> bereit: true
      100 Sekunden von Ausfall bis Selbstheilung. Das Protokoll traegt jede
      Stufe — wer „spotify ging ploetzlich wieder" untersucht, findet hier,
      ob die Wache die Hand im Spiel hatte.

      ══ GRENZEN, ehrlich ═════════════════════════════════════════════════════
      * WARUM soloist sich abmeldet, ist weiter unbekannt — die Wache heilt
        das Symptom schnell, sie erklaert es nicht. Die Haeufung (dreimal an
        einem Tag) blieb unerklaert.
      * Sie schuetzt nur den soloist-Weg. Mit engine=librespot tut sie
        NICHTS — dessen Anmeldefragen sind anders gebaut (E104).
      * Ein `box-fehlt` waehrend Spotify hoerbar spielt laesst sie
        ausdruecklich stehen (Widerspruch: eher luegt die Liste als die
        Wiedergabe) — dieser Fall landet nur im Protokoll.
  - id: der-griff-stempel-fand-die-vier-sekunden-im-vorlesen
    kind: falle
    title: "Vier Sekunden je Tipp — und der Taeter war das Vorlesen vom Vortag, nicht der Umbau: der Griff-Stempel zerlegt den Weg vom Finger bis zur Anfrage"
    match: "tipp dauert sekunden|oberflaeche traege|abtastproblem|vom fingerdruck bis zur anfrage|griff stempel|E118-Griff|ui latenz messen|vorlesen bremst"
    sev: high
    cause: "04.09.2026, nach einem Tag Backend-Optimierung: „ich merke keinen unterschied.\" Der Griff-Stempel (pointerdown-Zeit reist im Wunsch mit, der Server druckt sie) zeigte 3380-4189 ms VOR der Anfrage. Dreigeteilt: Finger lag 80-182 ms auf, click-Zustellung 14-18 ms, WEG 4001-4021 ms — exakt der Deckel VL_WARTE_MAX_MS. VORLESEN stand auf „antippen\", eingerichtet am 3.09., EINEN TAG vor der ersten Meldung; der Umschlag hielt jeden Tipp bis zum Ansage-Ende, und WPE meldete das Ende nie — jede Wartezeit lief auf den vollen Deckel"
    fix: "Der Griff-Stempel bleibt eingebaut (app.js pointerdown -> griffMs/griffKontaktMs/griffZustellungMs/griffWegMs im Wunsch; server.ts druckt ueber 250 ms eine E118-Griff-Zeile): „die Box ist traege\" laesst sich damit in Oberflaeche und Server zerlegen, ohne Geraet in der Hand. Und die Lehre davor: WENN ETWAS SEIT KURZEM LANGSAM IST, ZUERST FRAGEN, WAS SEIT KURZEM ANDERS IST — die Vorlesen-Einrichtung vom Vortag haette Stunden gespart. Das Warte-Modell selbst ist ersetzt: Bild spielt sofort, Text spricht nur (sprichDann, SPRICH_TEXTZONEN)"
    tags: [ui, latenz, messen, vorlesen, wpe, e118, 2026]
    related: [fristloser-fetch-aus-der-bootminute-verstopft-den-prozess, nachtippen-macht-den-anlauf-laenger-nicht-kuerzer, einmal-hinsehen-ist-keine-messung, klick-sturm-laedt-nichts-mehr]
    body: |
      ══ DIE JAGD IN VIER STUFEN, weil das Muster wiederkommt ═════════════════

        1. E118-Uhr (Server): jeder langsame Tipp druckt seine Phasen.
           Ergebnis: der Server war laengst schnell — die Zeit lag DAVOR.
        2. Griff-Stempel: pointerdown-Zeit reist im Wunsch mit.
           Ergebnis: 4 s zwischen Fingerdruck und Anfrage.
        3. Dreiteilung: Kontakt / Zustellung / Weg.
           Ergebnis: Finger normal, WPE sofort — der WEG (eigener Code) war es.
        4. Die Zahl selbst sprach: 4001, 4005, 4007 — WERTE DICHT AN EINER
           RUNDEN KONSTANTE SIND EIN TIMEOUT, KEINE ARBEIT. grep nach 4000
           fand VL_WARTE_MAX_MS.

      ══ WARUM ES NIEMAND SAH ═════════════════════════════════════════════════
      Das Vorlesen war am VORTAG eingerichtet worden. Die Meldung kam als
      „seit dem Umbau ist es langsam" — und der Umbau war gross, verdaechtig
      und frisch im Kopf. Ein halber Tag Backend-Arbeit war NICHT umsonst
      (Boot-Stau, Listen, Wahl, Sprung — alles echt und gemessen), aber die
      SPUERBARE Bremse war der Kalender-Nachbar. Erste Frage bei „ploetzlich
      langsam": WAS WURDE ZULETZT EINGESCHALTET?

      ══ DAS ENDE, DAS NIE KAM ════════════════════════════════════════════════
      Der Umschlag wartete auf das `ended` der Ansage. Unter Cog/WPE spielt
      `new Audio()` aber GAR NICHT (siehe
      [[cog-kiosk-hat-keine-tonbruecke]]) — kein ended, kein error mit
      Wirkung, nur der 4-s-Zaun. Ein Warten auf ein Ereignis braucht den
      Beweis, dass das Ereignis auf DIESER Maschine je feuert.
  - id: cog-kiosk-hat-keine-tonbruecke
    kind: falle
    title: "Unter Cog/WPE spielt `new Audio()` LAUTLOS ins Leere — gstreamer1.0-pipewire fehlt, und niemand meldet es"
    match: "browser spielt kein audio|ansage nicht hoerbar|new Audio stumm|wpe kein ton|cog audio|gstreamer pipewire fehlt|vorlesen unhoerbar kiosk"
    sev: high
    cause: "04.09.2026: „vorlesen klappt nicht.\" Die Ansage spielt der KIOSK-BROWSER (`new Audio(url)` in stimme.sprich, app.js). Auf der Box ist als Kiosk Cog/WPE geschaltet — und dem fehlt die Bruecke: installiert ist nur `gstreamer1.0-alsa`, kein `gstreamer1.0-pipewire`, das Tongeraet gehoert aber PipeWire. 90 Sekunden pw-dump-Mithoeren waehrend Tipp-Versuchen: KEIN einziger Tonstrom vom Browser. Kein Fehler, keine Meldung — der Klang verpufft, und `ended` feuert nie (daran hing die 4-s-Bremse, siehe [[der-griff-stempel-fand-die-vier-sekunden-im-vorlesen]])"
    fix: "Kurzfristig unterscheiden: unter CHROMIUM toent Browser-Audio (die gruenen Sprech-Messungen vom August liefen dort), unter COG nicht — kiosk-zwei-browser. Der richtige Fix ist architektonisch und steht als E123 im Backlog: DIE BOX SPRICHT SELBST — der Server hat die WAV ohnehin erzeugt und spielt sie ueber PipeWire ins Klangwerk, browser-unabhaengig, mit Daempfung in Haushand. Wer stattdessen nur das Paket nachziehen will, muss danach MESSEN, dass ein WPE-Strom wirklich auftaucht"
    tags: [kiosk, cog, wpe, audio, gstreamer, pipewire, vorlesen, 2026]
    related: [kiosk-browser-umschaltbar-mit-rueckfall, der-griff-stempel-fand-die-vier-sekunden-im-vorlesen, webkit-zieht-den-reglergriff-nicht, die-ganze-tonstrecke-und-wo-die-pegel-entstehen]
    body: |
      ══ WIE MAN ES NACHMISST, ohne Ohr am Geraet ═════════════════════════════
      Waehrend jemand am Geraet eine Ansage ausloest:

        pw-dump | jq '.[] | select(.info.props["media.class"]//"" | test("Stream"))
                       | .info.props["application.name"]'

      Taucht kein WPE/Cog-Strom auf, hat der Browser nie bei PipeWire
      angeklopft. Die Paketseite dazu:

        dpkg -l | grep gstreamer     # gstreamer1.0-pipewire fehlt?

      ══ WARUM ES SO LANGE UNENTDECKT BLIEB ═══════════════════════════════════
      * Der Fehlerpfad ist STILL: `a.play()` wird nicht abgewiesen, es gibt
        keinen error — es kommt schlicht nichts an der Senke an.
      * Die Sprech-Wege waren unter CHROMIUM gebaut und gemessen; der
        Kiosk-Schalter auf Cog kam spaeter, und niemand mass die Ansagen
        danach erneut [Zustand im Wiki hat Haltbarkeit].
      * Und die 4-s-Wartezeit, die daraus entstand, sah aus wie eine
        LEISTUNGSFRAGE — ein halber Tag Backend-Jagd lag dazwischen, der
        eigene echte Funde brachte, nur nicht diesen.

      ══ DIE DOPPELTE LEHRE ═══════════════════════════════════════════════════
      1. Ton, der im BROWSER entsteht, haengt an dessen Medienstapel — und
         der ist je Kiosk-Browser verschieden. Ton, der im HAUS entsteht
         (Server -> PipeWire), ist ueberall gleich. Fuer eine Box mit
         austauschbarem Kiosk gehoert Ton ins Haus (E123).
      2. Ein Feature, das auf Maschine A gemessen wurde, ist auf Maschine B
         UNGEMESSEN — der Browserwechsel des Kiosks ist ein Maschinenwechsel.
  - kind: lesson
    id: nutzerskala-ist-das-limit
    title: "Die Nutzerlautstaerke wohnt auf der letzten Senke — und 100 ist das Limit"
    sev: high
    tags: [ton, pipewire, lautstaerke, klangwerk, geraet, kinderschutz]
    body: |
      Loest den offenen Eintrag `klangwerk-senke-nimmt-keine-lautstaerke`
      (23.08.2026) auf und beendet vier Versuche an vier verschiedenen Orten.

      ══ DAS KONZEPT IN EINEM SATZ ══════════════════════════════════════════
      Die Nutzerlautstaerke wohnt auf der Senke, die das Signal ZULETZT
      verlaesst (Bluetooth-Geraet oder Tonkarte). Alles davor — klangwerk,
      entzerrer, ueberall — steht fest auf 100 % und gehoert der Einmessung.
      Genau EIN Weg schreibt sie: `mupi-lautstaerke.sh`.

      ══ WARUM DIE KETTE NICHT IN FRAGE KOMMT ═══════════════════════════════
      Eine Filter-Kette nimmt keine Lautstaerke an. Die Doku nennt dafuer
      `capture.volumes` — aber das filter-chain-Modul dieser PipeWire-Version
      (1.4.2, aarch64) KENNT DAS WORT NICHT. Am Geraet nachgezaehlt:

          strings libpipewire-module-filter-chain.so | grep -c volumes  -> 0
          dieselbe Datei: capture.props -> 2, filter.graph -> 5

      Die Gegenprobe gehoert dazu: ohne sie waere „nichts gefunden" auch mit
      einer kaputten Suche vereinbar — ein leeres Ergebnis ist erst dann ein
      Befund, wenn dieselbe Suche etwas findet, das da sein MUSS. Wer die Doku
      liest, findet die Option trotzdem — sie ist NEUER als das, was auf der
      Box laeuft. Ein Umbau in diese Richtung wurde deshalb wieder entfernt,
      statt zwei wirkungslose Filterknoten auf dem Pi mitzuschleppen.

      ══ WARUM ES „VOR EIN PAAR WOCHEN" NOCH GING ═══════════════════════════
      Bis zum 07.08.2026 war `@DEFAULT_AUDIO_SINK@` die Tonkarte, und das
      Skript traf sie. Am 21.08. wurde `klangwerk` zur Vorgabe-Senke. Seitdem
      zielte jeder Lautstaerkebefehl in die taube Kette — ohne Fehlermeldung,
      denn `set-volume` meldet Erfolg und tut nichts. Nichts war „kaputt
      gegangen"; der Boden hatte sich unter einem funktionierenden Skript
      wegbewegt. Das ist der allgemeine Teil: wer eine Vorgabe-Senke
      einschiebt, aendert JEDEN Aufruf, der `@DEFAULT_AUDIO_SINK@` sagt.

      ══ 100 IST DAS LIMIT, NICHT DER ANSCHLAG (05.09.2026) ══════════════════
      `nutzer_zu_echt` / `echt_zu_nutzer` rechnen die Nutzerskala 0..100 auf
      0..`mupibox.maxVolume`. Erst dadurch wirkt das Limit ueberhaupt, und die
      Tastenschritte verteilen sich ueber den erlaubten Bereich statt darueber
      hinauszulaufen (gemeldet als „die abstufung ist zu grob, es wird zu
      schnell laut"). Am Geraet: 100 -> 60 %, 80 -> 48, 50 -> 30, 15 -> 9.

      WER DARAN RECHNET, GEHT DURCH BEIDE RICHTUNGEN: steht die Karte von Hand
      ueber dem Limit, muss die Anzeige auf 100 klemmen, sonst zeigt der
      Regler 117. Geprueft in `tools/lautstaerke-skala-probe.sh` (21 Faelle,
      gegengeprueft mit sabotierter Rechnung: 8 werden rot).

      ══ WERKZEUG STATT VERMUTUNG ═══════════════════════════════════════════
      `tools/senke-gehorcht-probe.sh` beantwortet „nimmt diese Senke
      Lautstaerke an?" durch ZURUECKLESEN mit Abstand. Ein `set-volume` mit
      Exit 0 ist kein Beweis — genau diese Taeuschung hat hier Wochen
      gekostet.
    related: ["klangwerk-senke-nimmt-keine-lautstaerke", "neue-senke-faengt-bei-eins-an", "drei-skalen-fuer-eine-lautstaerke", "zwei-zaehlweisen-fuer-eine-lautstaerke"]
  - kind: lesson
    id: auffangregel-verschluckt-plugin-senken
    title: "„Alles andere ist intern\" — die Auffangregel machte die Aufnahme-Senke zum Regler"
    sev: high
    tags: [ton, pipewire, lautstaerke, plugins, verwaltung, geraet]
    body: |
      Der Betreiber wollte in TON das Maximum setzen: „der regler ist darauf
      gesprungen, kurz darauf wieder auf 100". Sein Wert war gespeichert —
      gesprungen ist der Regler DANEBEN.

      ══ WAS PASSIERT IST ═══════════════════════════════════════════════════
      `zielAusSinkName` deutet alles, was nicht `ueberall` und nicht
      `bluez_output.*` ist, als „intern". Die Regel stammt aus einer Zeit mit
      genau zwei Ausgaengen. Inzwischen liegen DREI virtuelle Senken
      dazwischen, und eine davon gehoert einem Plugin:

          mixpi-mitschnitt   art=intern   prozent=100   <- gewann
          alsa_output…       art=intern   prozent=9     <- die echte Karte
          klangwerk          art=intern   prozent=100

      Die Oberflaeche nahm die ERSTE mit passender Art. Das war die stille
      Aufnahme-Senke: sie steht immer auf 100 %, MUSS dort bleiben (der
      Mitschnitt bleibt pur) und nimmt keinen Wert an. Beim Verschieben der
      roten Grenze wandert die Gesamtlautstaerke im Verhaeltnis mit — dieser
      Zug lief gegen die Aufnahme-Senke und schnappte zurueck.

      ══ DIE PRUEFUNG GEHT AUF DIE SORTE, NICHT AUF EINE NAMENSLISTE ════════
      `istDurchgang` zaehlte zwei Namen auf (klangwerk, entzerrer) und kannte
      `mixpi-mitschnitt` deshalb nicht. Eine Liste haette man nachpflegen
      muessen — und das naechste Plugin haette denselben Fehler ausgeloest.
      `istHardwareSenke` erkennt echte Ausgaenge stattdessen am PRAEFIX
      (`alsa_output.` / `bluez_output.`); alles Virtuelle hat keines. So faellt
      auch eine noch nicht erfundene Plugin-Senke durch, und der Server muss
      keinen Plugin-Namen kennen — die Grenze zwischen Kern und Plugin bleibt.

      Ein Zeuge prueft genau das: eine ERFUNDENE Senke (`mixpi-irgendwas-neues`)
      muss ohne Nachpflege als „keine Hardware" gelten.

      ══ UND DIE OBERFLAECHE RAET NICHT MEHR SELBST ═════════════════════════
      Welche Senke die Nutzerlautstaerke traegt, sagt jetzt der Server
      (`regelndeSenkeWaehlen`, Bluetooth vor Karte — dieselbe Reihenfolge wie
      `regelnde_senke()` im Skript). Zwei Regeln waeren zwei Wahrheiten.

      ══ DERSELBE TAG, ZWEITER FUND: DOPPELT GEDECKELT ══════════════════════
      Die Box-Oberflaeche klemmte ihren Regler zusaetzlich auf `maxVolume`
      (`this.max = Math.min(100, m)`). Seit die Nutzerskala 0..100 auf
      0..maxVolume abbildet, ist das eine zweite Deckelung auf die erste: bei
      maxVolume 75 kam das Kind auf 75 % von 75, also 56 % der Karte, und
      erreichte das erlaubte Maximum NIE. Wer eine Grenze in eine Skala
      verlegt, muss jede Stelle suchen, die sie vorher selbst durchsetzte —
      sonst wirkt sie zweimal.
    related: ["nutzerskala-ist-das-limit", "klangwerk-senke-nimmt-keine-lautstaerke", "zwei-zaehlweisen-fuer-eine-lautstaerke"]
  - kind: lesson
    id: gesenkte-grenze-muss-den-ton-mitnehmen
    title: Wer die Grenze senkt, senkt den Ton — sonst steht die Karte stabil darueber
    sev: high
    tags: [ton, lautstaerke, kinderschutz, verwaltung, geraet, werkzeug]
    body: |
      Der Betreiber: „ich habe das maximum gesetzt und konnte an der box ueber
      das limit schieben." Er hatte recht — und die Wurzel war keine der
      Stellen, die an diesem Tag schon repariert waren.

      ══ DER HERGANG ════════════════════════════════════════════════════════
      Die Karte stand auf 75 % (maxVolume war 75, Regler auf Anschlag). Dann
      senkte er maxVolume auf 65. NICHTS zog die Karte nach:

        - Das Skript klemmt nur beim NAECHSTEN set — wer nicht dreht,
          behaelt den alten Wert.
        - Der 30-s-Waechter (deckelAnwenden) kannte nur den GERAETE-Deckel
          (70), nicht maxVolume — er drueckte 75 auf 70 und liess es dort.
        - POST /api/ton/senke klemmte ebenfalls nur auf den Deckel: der
          Gesamt-Regler der TON-Seite konnte AKTIV ueber die rote Grenze.

      Ergebnis: eine Box, die dauerhaft lauter spielt als die frisch gesetzte
      Zusage — bis zufaellig jemand am Regler dreht.

      ══ DIE EINE REGEL ═════════════════════════════════════════════════════
      Die Karte UEBER der wirksamen Grenze min(maxVolume, Deckel der Ausgabe)
      ist NIE ein stabiler Zustand. Drei Stellen setzen sie durch:

        1. deckelAnwenden klemmt auf min(Deckel, maxVolume) — im 30-s-Takt,
           und NUR auf Hardware-Senken (istHardwareSenke).
        2. POST /api/ton/senke klemmt Anfragen auf dieselbe Grenze und weist
           Durchreichen mit 409 ab.
        3. POST /api/konfiguration ruft bei geaendertem maxLautstaerke den
           Waechter SOFORT — eine halbe Minute ueber dem Limit ist an einem
           Kinderlautsprecher keine Randnotiz.

      Allgemein: Eine Grenze, die nur beim SETZEN prueft, prueft nicht. Sie
      braucht einen Weg, der den BESTAND nachzieht, sobald sie selbst sich
      aendert — sonst ist jede Grenzsenkung ein stilles Ueber-dem-Limit.

      ══ DAS BLEIBENDE WERKZEUG ═════════════════════════════════════════════
      tools/regler-alle-wege-probe.sh <benutzer@box> prueft ALLE Stellwege in
      einer Tabelle: Skript (set/up/down, Klemmen beider Enden), /api/ton/senke
      (Klemme + 409 fuer Durchreiche und Kombi), Waechter (pactl von Hand auf
      90 % -> gedrueckt; Durchreichen bleiben 100), Grenze senken bei stehender
      Karte (folgt binnen 5 s), Player-Daemon (:5005/current/setvolume:120).
      Sie schaltet die Karte fuer die Dauer STUMM und stellt per trap alles
      wieder her — sie aendert Werte, keine Ohren. Entstanden, nachdem drei
      Regler-Funde an EINEM Vormittag einzeln nachgemeldet wurden; der
      Betreiber: „bitte teste alles von reglern". Nach jeder Aenderung an
      Lautstaerke-Wegen laufen lassen, VOR der Meldung „fertig".
    related: ["auffangregel-verschluckt-plugin-senken", "nutzerskala-ist-das-limit", "klangwerk-senke-nimmt-keine-lautstaerke"]
  - kind: lesson
    id: durchreichen-riegel-fand-fuenf-wege
    title: Der 409-Riegel war richtig — er machte fuenf ratende Aufrufer sichtbar
    sev: high
    tags: [ton, lautstaerke, verwaltung, plugins, werkzeug, geraet]
    body: |
      Kaum stand der Riegel „Durchreichen sind kein Regler" (409 in POST
      /api/ton/senke), meldete der Betreiber: „Diese Senke ist eine
      Durchreiche — kann nicht sein oder? wieder ein schritt zurueck." Es war
      kein Schritt zurueck: der Riegel machte LAUT, was vorher still ins
      Leere lief. Ein Audit ueber ALLE 34 Lautstaerke-Stellwege (drei Finder,
      je riskanten Weg ein adversarialer Pruefer) fand fuenf Wege, die auf
      geratene oder falsche Ziele stellten:

        1. ausgaben() der TON-Seite: die intern-Zeile nahm den ERSTEN Treffer
           mit art=intern — je nach Reihenfolge die Aufnahme-Senke. Fix:
           Hardware-Praefix (alsa_output.), dieselbe Regel wie im Server.
        2. umschalten(): die Lautstaerke-Mitnahme POSTete auf sinkName
           'ueberall' — den die Kombi-Sperre seit dem 04.09. ablehnt. Die
           Mitnahme brach genau an der Stelle, die sie versprach. Fix: je
           GEWAEHLTEM Mitglied dessen Hardware-Senke stellen.
        3. zuDaempfen() der Ansage-Daempfung: klangwerk.ausgang und
           entzerrer.ausgang sind sink-inputs OHNE application.name — die
           Daempfung haette die KETTE gesenkt statt einer Quelle. Fix: kein
           Name, keine Quelle (Regel aus quellenAusPactlJson uebernommen).
        4. POST /api/ton/quelle nahm jede lebende Kennung — auch die
           Verbindungs-Stroeme der Kette. Fix: nur Kennungen, die in
           quellenAusPactlJson vorkommen; alles andere 404.
        5. chromium-autostart.sh (gilt auch fuer Cog!): der Rueckfall
           schrieb den ROHEN startVolume auf @DEFAULT_SINK@ — seit dem
           21.08. das klangwerk, und ohne maxVolume-Klemme. Fix: Klemme
           rechnen und die erste alsa_output-Karte adressieren.

      ══ DIE LEKTION ════════════════════════════════════════════════════════
      Ein neuer Riegel an einer zentralen Naht ist ein MESSGERAET: jeder
      Aufrufer, der jetzt 409 erntet, hat vorher still das Falsche getan.
      Die Meldung des Betreibers war kein Rueckschritt, sondern der erste
      sichtbare Beleg. Wer den Riegel dann lockert, statt die Aufrufer zu
      richten, wirft das Messgeraet weg.

      Und: die Verwaltung auf der Box war drei Wochen alt (Buendel vom
      15.08., Quellcode kannte das regelnd-Flag laengst). Ein offener
      Browser-Tab laedt das neue Buendel erst nach einem Neuladen — „der
      regler huepft wild rum" war die ALTE Fassung, die die Senke im
      Sekundentakt neu riet. Bei Angular liegt eine Seite als EIGENER
      chunk-*.js — wer nur main-*.js greppt, haelt ausgelieferten Code
      faelschlich fuer fehlend.
    related: ["auffangregel-verschluckt-plugin-senken", "gesenkte-grenze-muss-den-ton-mitnehmen", "nutzerskala-ist-das-limit"]
  - kind: lesson
    id: wartungs-schirm-nicht-doppelt-bauen
    title: "Der Wartungs-Schirm existierte schon — das Duplikat war still unerreichbar"
    sev: medium
    tags: [verwaltung, kiosk, werkzeug, messung]
    body: |
      Der Betreiber wollte einen Wartungsbildschirm („die kinder druecken
      draufrum das kann verfaelschen") — und er war RICHTIG gewuenscht:
      zweimal drehte ein Tester mitten in eine Messung.

      Gebaut wurde er trotzdem doppelt: Endpunkte /api/wartung mit Schema
      { an, text } und ein eigenes Overlay — obwohl seit dem 29.08.2026
      derselbe Pfad mit Schema { aktiv } existiert, samt Sperr-Schirm im
      NewDesign (Poll alle 10 s) und Datei /tmp/.mixpi-wartung als Wahrheit.
      Express nimmt die ZUERST registrierte Route: das Duplikat war
      unerreichbar, sein POST erntete 400 („aktivFehlt") — und die erste
      „geschuetzte" Messung lief in Wirklichkeit OHNE Schirm, gruen aus
      Glueck. Ein Duplikat faellt nicht auf, es LUEGT.

      ══ REGELN ═════════════════════════════════════════════════════════════
      1. Vor jedem neuen Endpunkt: grep nach dem Pfad. Ein zweiter Treffer
         ist ein Befund, kein Zufall.
      2. Werkzeuge mit SSH-Zugang schalten den Schirm ueber die DATEI
         (/tmp/.mixpi-wartung) — der POST steht hinter dem Anmelde-Tor, die
         Datei nicht, und /tmp raeumt beim Neustart auf.
      3. Der Schirm wirkt erst mit dem NAECHSTEN Poll (10 s). Wer sofort
         nach dem Einschalten misst, misst ungeschuetzt — tools/wartung.sh
         und die Proben warten den Takt ab.

      Werkzeuge: tools/wartung.sh (an|aus|status);
      tools/regler-alle-wege-probe.sh und tools/ton-gesamtcheck.sh schalten
      den Schirm selbst und raeumen ihn per trap ab. Die Regler-Probe wartet
      zusaetzlich auf eine RUHIGE Buehne (Klangwerk steht, Karte drei
      Sekunden unbewegt): direkt nach einer Auslieferung stellt die
      Kettenneubau-Rettung die gemerkte Lautstaerke zurueck — zweimal
      mitten in die ersten Probenfaelle gemessen.
    related: ["durchreichen-riegel-fand-fuenf-wege", "im-kiosk-messen-statt-raten"]
  - kind: lesson
    id: karten-mixer-ist-eine-attrappe
    title: Die Anzeige log nie — die Karte hat nie gehorcht (Hardware-Mixer-Attrappe)
    sev: high
    tags: [ton, pipewire, lautstaerke, geraet, kinderschutz, werkzeug]
    body: |
      Der Betreiber: „angeblich ist es 7% das ist gut aber es ist 100 laut …
      hast du es auch gemessen?" Nein — bis dahin lasen alle Proben nur
      ZURUECK, was pactl meldet: was GESTELLT wurde, nicht was die Karte
      ANWENDET. Der Blick in die Karten-Props zeigte die Schere:

          channelVolumes = [ 6.4e-05 ]   <- gestellt (4 %)
          softVolumes    = [ 0.0227  ]   <- wirklich angewandt (~28 %)

      ══ DIE WURZEL ═════════════════════════════════════════════════════════
      PipeWire hielt hw:0 fuer eine Karte mit Hardware-Lautstaerke und schob
      den Reglerwert in deren Mixer — aber der MAX98357A-DAC HAT keinen
      Lautstaerkeregler. Der Wert verschwand in einer Attrappe; hoerbar blieb
      ein nachhinkender Software-Rest. DAS war die „wetterwendische" Senke,
      die den ganzen Sommer mal gehorchte und mal nicht: jede Anzeige stimmte,
      nur der Ton nicht.

      Fix: config/templates/82-karte-software-regler.conf —
      `api.alsa.soft-mixer = true` fuer alsa_card.platform-soc_107c000000_sound.
      Danach regelt PipeWire IMMER in Software, und „gestellt" == „angewandt"
      ist NACHPRUEFBAR (Nutzer 20 -> 60 bewegte die Route exakt um Faktor 27,
      die kubische Skala).

      ══ DREI FALLEN AUF DEM WEG (alle am Geraet bezahlt) ═══════════════════
      1. AM DEVICE, NICHT AM NODE: der erste Wurf matchte node.name — die
         Prop landete brav am Sink-Knoten und bewirkte NICHTS; ausgewertet
         wird sie von der Kartenverwaltung (ACP) am Audio/Device
         `alsa_card...`. Danach stand die Karte sogar stur auf 1.0.
      2. MIT soft-mixer WOHNT DAS VOLUMEN AN DER ROUTE des Devices; die
         Node-Props bleiben 1.0. Wer nur den Node liest, haelt einen
         funktionierenden Regler fuer verpufft.
      3. pactl-INDEX IST KEINE PipeWire-ID: `wpctl set-volume <pactl-index>`
         trifft ein fremdes oder kein Objekt („Object not found") — die
         dritte Gestalt der Drei-Skalen-Falle.

      ══ WAS DIE PROBEN DARAUS LERNTEN ══════════════════════════════════════
      tools/karten-regler-wirkt-probe.sh prueft jetzt GESTELLT gegen
      ANGEWANDT an der wirksamen Stelle (Node ODER Route) und behandelt
      LEERE Messwerte als „nicht messbar" — nie als gruen: ein
      WLAN-Aussetzer hatte beide Werte leer gemacht, float('' or 0) machte
      daraus 0==0 und die Probe meldete „deckungsgleich" fuer eine Messung,
      die nie stattfand. tools/tonlage-schau.py zeigt die ganze Tonlage;
      die Erhebung laeuft AUF der Box (tools/box/tonlage-helfer.py), weil
      der volle pw-dump (~1 MB) uebers stotternde WLAN in den Timeout lief.

      Merksatz: Wer Lautstaerke prueft, prueft die ANGEWANDTE Zahl — die
      gestellte kann jeder melden.
    related: ["klangwerk-senke-nimmt-keine-lautstaerke", "drei-skalen-fuer-eine-lautstaerke", "gesenkte-grenze-muss-den-ton-mitnehmen", "durchreichen-riegel-fand-fuenf-wege"]
  - kind: lesson
    id: signalweg-ziel-wird-gerettet-und-gemerkt
    title: Das Ziel des Signalwegs wird gerettet wie die Lautstaerke - und die Wache prueft auch das letzte Glied
    sev: high
    tags: [ton, pipewire, klangwerk, geraet, werkzeug]
    body: |
      Am Geraet reproduziert (05.09.2026): JEDER Klangwerk-Neubau verlegte
      klangwerk.ausgang neu nach Vorgabe-Logik — die Vorgabe ist das
      Klangwerk selbst, scheidet aus, und der Ausgang landet eine Station
      zu weit hinten. Niemand korrigierte das, aus zwei Gruenden:

        1. `signalwegWachen` stieg bei WENIGER ALS ZWEI Stationen aus
           („eine Station allein kann nichts verfehlen"). Doch: ihr AUSGANG
           kann. Mit ausgeschaltetem Entzerrer war die Kette nur
           [klangwerk], und das letzte Glied blieb unbewacht.
        2. Das SOLL kam aus `echtesZiel` — und das folgt dem IST-Weg. Ein
           falsch gelegter Weg erklaerte sich damit selbst zum Soll: wer den
           falschen Weg abliest, legt ihn nur ordentlicher.

      ══ DIE LOESUNG: DAS ZIEL IST JETZT EXPLIZIT ═══════════════════════════
      `signalwegZiel` merkt sich das zuletzt GELUNGEN gelegte Ziel — gesetzt
      an der EINEN Stelle, die legt (signalwegLegen). Der Neubau rettet es
      VOR dem Abriss (dasselbe Muster wie `vorherLaut` fuer die
      Lautstaerke: nach dem Abriss ist es nicht mehr zu erfahren). Die
      Wache prueft seither ALLE Glieder inklusive des letzten gegen dieses
      Soll. Sabotage-Beleg: klangwerk.ausgang von Hand auf den Entzerrer
      geschoben — nach einem 30-s-Takt lag er zurueck.

      ══ UND EINE FEHLDIAGNOSE ZUM MITSCHREIBEN ═════════════════════════════
      „klangwerk.ausgang zeigt DIREKT auf die Karte, an der Ueberall-Kombi
      vorbei" sah aus wie der Fehler — war aber das SOLL: bei EINZEL-Ausgabe
      routet die letzte Station direkt auf die gewaehlte Senke; die Kombi
      ist nur bei Mehrfach-Ausgabe das Ziel (der Server sagt es selbst:
      „Kein Bluetooth-Lautsprecher verbunden — Ueberall waere Stille").
      Die leer mitlaufende Kombi ist kein Befund. Wer den Signalweg
      beurteilt, braucht erst die GEWAEHLTE Ausgabe, dann das Urteil.
    related: ["karten-mixer-ist-eine-attrappe", "gesenkte-grenze-muss-den-ton-mitnehmen", "klangwerk-senke-nimmt-keine-lautstaerke"]
  - id: erster-rollout-nach-dem-schnitt
    kind: falle
    title: "Erster Rollout nach dem E118-Schnitt: die Lieferkette glaubte an die Wurzel-index, der Tauscher trug das Alte zurueck"
    tags: [ausliefern, tauscher, rollout, uebernahme, redirect, e118, 2026]
    source: >
      Erster Ausliefer-Lauf nach dem E118-Schnitt, 05.09.2026, Box .62:
      zwei rote Probelaeufe, dann gruener Tausch; Nachmessung per curl
      (GET / lieferte 200 mit der alten index statt des Redirects);
      Probe-Lauf des Raeumwerkzeugs am Geraet (364 fallen, 0 unbekannte).
    related: [arbeitsablauf-der-sitzung, classic-und-themen-tauschen]
    body: |
      Wer eine Oberflaeche aus dem Repo loescht, hat sie damit noch nicht
      vom Geraet: die LIEFERKETTE traegt eigene Annahmen ueber den Baum,
      und der TAUSCHER traegt den alten Bestand zurueck.

      Die Probe brach ZWEIMAL an derselben Annahme an zwei Stellen:
      paket_schnueren (ausliefern.py) pruefte www/index.html an der
      Wurzel — die gehoerte der geloeschten Angular-App, seit E118/1e
      heisst die Wahrheit neu/index.html. Dann dieselbe Annahme am
      Geraet: mupibox-tauscher.py verglich muss_enthalten WOERTLICH mit
      "index.html" gegen ein fest verdrahtetes Wurzel-Flag. Fix
      beidseitig: der Auftrag NENNT die Datei (neu/index.html fuer www,
      index.html fuer admin), die Geraetepruefung folgt dem Auftrag. Der
      Tauscher reist bei jedem Lauf frisch mit — kein Henne-Ei.

      Danach die leisere Falle: der Tauscher uebernimmt, was im
      laufenden Baum liegt und im Paket fehlt — sein Nutzerdaten-Schutz
      (www/cover ueberlebt so). Auf der Bestandsbox holte er damit die
      KOMPLETTE geloeschte Angular-Oberflaeche zurueck (364 Eintraege),
      und die uebernommene Wurzel-index.html war nicht nur Ballast:
      express.static liefert sie VOR dem Catch-all aus und verdeckte den
      Redirect / -> /neu/ — die eine Oberflaeche waren wieder zwei.
      Wache seither in Schritt [11] von ausliefern.py (warnt, wenn
      www/index.html unter den Uebernahmen ist). Geraeumt wird mit
      tools/box/mixpi-angular-nachlass.py nach POSITIVLISTE: es kennt
      den Nachlass beim Namen, Unbekanntes bleibt liegen und wird
      gemeldet, neu/ und cover/ sind unantastbar, Probe vor --wirklich.
      Der scharfe Lauf bleibt Betreibersache — Loeschen am Geraet wird
      nicht automatisiert; Rueckweg (….zurueck) und die Sicherung aus
      Schritt [8] decken ihn doppelt.

      Dritter Befund: themen.'MuPiBox Classic' am Geraet ist noch der
      alte Feldbestand, weil ergaenzen() die GESPEICHERTE Fassung
      gewinnen laesst (dokumentierter Schutz vor dem Ueberschreiben von
      Handanpassungen, siehe Kachelgroesse 1,8). Die neue Fassung liegt
      als auslieferung bei — nach Umbauten mitgelieferter Themen zeigt
      eine Bestandsbox die ALTE Fassung, bis in der Verwaltung "Original
      wiederherstellen" getippt wird.

      Merksatz: Nach einem Schnitt jede Stelle suchen, die den alten
      Baum beim Namen kennt — im Schnuerer, im Tauscher, in den
      Uebernahmen — und den Gegenbeweis am Geraet messen: GET / muss
      umleiten, nicht 200 liefern.
  - id: plymouth-holt-nichts-die-dunkelphase-liegt-woanders
    kind: falle
    title: "Plymouth holt hier nichts: die Untergrenze ist der DRM-Treiber, und der ist ein Modul"
    sev: high
    tags: [boot, splash, plymouth, dunkelphase, drm, wlan, pi5, 2026]
    match: "plymouth|screen lange dunkel|schirm dunkel beim start|splash frueher|boot splash schneller"
    quelle: "Am Geraet gemessen (Pi 5 / DietPi Trixie, Box .62, 05.09.2026) mit tools/box/dunkelphase-schau.py und tools/bootkette-schau.py"
    symptom: >
      Der Schirm ist beim Start "sehr lange dunkel". Der naheliegende Gedanke
      ist ein anderes Splash-Programm — Plymouth zeigt doch frueher und mehr.
    cause: |
      DIE FRAGE IST EINE MESSFRAGE, KEINE PROGRAMMWAHL. Zwei Zahlen
      entscheiden sie, und beide standen vorher nirgends:

      UNTERGRENZE 11,78 s. `drm`, `vc4` und `drm_rp1_dsi` sind auf dieser Box
      MODULE aus dem Root-Dateisystem (in /proc/modules nachgesehen), nicht in
      den Kernel gebaut. systemd laedt sie ueber `modprobe@drm.service`. Vor
      diesem Zeitpunkt gibt es KEIN Ausgabegeraet — kein Programm zeigt da
      etwas, auch Plymouth nicht. Wer "frueher anzeigen" will, muss den
      TREIBER frueher haben, nicht das Anzeigeprogramm wechseln.

      DAZU: es gibt hier gar kein initramfs (/boot/initramfs* und
      /boot/firmware/initramfs* fehlen beide). Genau daraus zieht Plymouth
      anderswo seinen Vorsprung. Ohne initramfs startet es ebenfalls nur als
      systemd-Dienst — also im selben Fenster wie die eigene Animation, die
      heute um 13,345 s anlaeuft. Moeglicher Gewinn: rund 1,5 s.

      UND ES ZEIGT WENIGER. Die eigene Animation
      ([[boot-animation-mit-echtem-fortschritt]]) nennt echte Meilensteine mit
      Beschriftung und sogar die Kioskumgebung (cog oder chromium). Plymouth
      liefert per Vorgabe Logo und Balken ohne Text.

      DASS ES SCHADET, IST HIER SCHON BEZAHLT: siehe
      [[mupi-weiss-beim-uebergang]] — plymouthd haelt die DRM-Karte als
      MASTER, X bekam sie nicht, die Box startete OHNE BILD; alle drei
      Auswege (deactivate, quit --retain-splash, daneben starten) durch,
      keiner trug; Kosten rund 2 s Startzeit, danach `systemctl mask`.

      WO DIE DUNKELHEIT WIRKLICH LIEGT (gemessen, 05.09.2026):

          0,000 ->  13,345 s   VORN   13,34 s schwarz
                               davon 11,78 s unvermeidbar (kein DRM-Treiber)
         13,345 ->  22,767 s   Animation sichtbar (9,42 s)
         22,767 ->  33,484 s   HINTEN 10,72 s schwarz
                               Animation WEG, Kiosk noch nicht da
          ------------------   SUMME 24,06 s Schwarz

      Der HINTERE Block ist der eigentliche Fund und war vorher unbenannt: die
      Animation erreicht ihre Meilensteine bei 22,8 s (80 ms nach
      `ifup@wlan0`) und beendet sich — waehrend die Box noch neun Sekunden auf
      `ifup@wlan1` wartet. Danach ist die Konsole schwarz, weil cmdline.txt
      `quiet loglevel=3 logo.nologo vt.global_cursor_default=0
      systemd.show_status=false` traegt. Es steht also nichts mehr da, obwohl
      noch etwas dastehen KOENNTE. Das ist eine Laufzeitfrage der vorhandenen
      Animation, keine Programmfrage.

      DIE ZWEI NETZPOSTEN dahinter (tools/bootkette-schau.py):

          ifup@wlan1.service   14,562 s   <- traegt die IP (USB-Realtek)
          ifup@wlan0.service    8,202 s   <- bekommt NIE eine Adresse

      wlan0 steht in /etc/network/interfaces auf `iface wlan0 inet dhcp` und
      hat im Betrieb keine Adresse; `ip -4 -o addr show wlan0` ist leer.
      `network.target` wartet trotzdem auf beide, und der Kiosk haengt daran
      ([[kiosk-wartet-auf-dhcp]]).

      UND VORN, IM KERNEL, LIEGT DER GANZE REST — BELEGT, nicht vermutet.
      Ein erster Durchgang nannte das noch "zeitlich benachbart"; die Marken
      fuer Anfang UND Ende der Wiederherstellung stehen aber beide da:

          [ 0,606] EXT4-fs: recovery required on readonly filesystem
          [10,366] EXT4-fs: recovery complete          <- 9,76 s
          [10,374] EXT4-fs: mounted filesystem ... ro
          [10,375] Run /sbin/init as init process
          [11,780] [drm] Initialized drm-rp1-dsi

      DARAUS FOLGT DIE ANTWORT AUF "kann man am TREIBER etwas machen?": Der
      Treiber ist gar nicht langsam. Zwischen `Run /sbin/init` und dem
      fertigen DRM liegen 1,4 s — mehr ist an ihm nicht zu holen. Die
      Untergrenze von 11,78 s besteht zu 9,76 s aus einer Wartezeit, die VOR
      jedem Treiber liegt: das Root-Dateisystem laesst sich nicht mounten,
      also laeuft kein init, also laedt niemand ein Modul. Wer die
      Wiederherstellung abstellt, bekommt den Treiber bei rund 2,2 s
      geschenkt, ohne einen einzigen Grafikschalter anzufassen.

      OFFEN BLEIBT, WARUM das Dateisystem unsauber war. Die Vermutung "die Box
      geht hart aus" ([[taster-wird-von-niemandem-ueberwacht]]) traegt auf
      DIESER Box NICHT: `mupi_taster.service` und `mupi_idle_shutdown.service`
      sind beide `active` (05.09.2026, .62). Der aeltere Eintrag beschreibt
      Box .169 im August — ein Zustandssatz mit Haltbarkeitsdatum, und die
      war abgelaufen. Bleiben Stromausfall, gezogener Stecker oder ein
      Absturz. Das Journal hilft nicht weiter: `journalctl --list-boots` kennt
      nur den laufenden Start, die Historie fehlt.
    fix: |
      Plymouth NICHT einbauen. Die drei Hebel, nach Wirkung geordnet:

      1. Die Animation laenger stehen lassen — bis der Kiosk wirklich malt,
         nicht bis ihre Meilensteine voll sind. Holt die hinteren 10,7 s,
         ohne Risiko, und ist eine Aenderung an mupibox-boot-splash.py.
      2. wlan0 aus /etc/network/interfaces nehmen (oder auf manual setzen).
         Holt rund 8 s, die heute fuer eine Schnittstelle ohne Adresse
         verwartet werden.
      3. Sauberes Herunterfahren, damit die EXT4-Wiederherstellung entfaellt.
         Bis zu 9,5 s vorn — erst nachmessen, dann glauben.

      AM TREIBER SELBST sind nur 1,4 s zu holen (siehe cause) — mit einer
      Ausnahme, und das ist der EINZIGE Weg, der die vorderen Sekunden
      wirklich fuellt, falls die EXT4-Wiederherstellung bleibt:

      4. Ein initramfs mit den DRM-Modulen. Es wird VOR dem Root-Mount
         geladen, also vor der Wiederherstellung — ein Bild koennte bei rund
         1 s stehen, WAEHREND unten das Journal zurueckgerollt wird. Die
         Voraussetzungen sind da: `initramfs-tools 0.148.4+rpt2` ist
         installiert, /etc/initramfs-tools vollstaendig, in config.txt steht
         KEINE `initramfs`-Zeile (der Bootloader laedt heute also keines).
         Zu tragen waeren rund 2 MB: drm (737 KB), vc4 (442 KB),
         drm_kms_helper (246 KB), drm_rp1_dsi (49 KB) plus tc358762,
         panel_simple, drm_dma_helper, drm_shmem_helper, drm_display_helper,
         drm_client_lib, drm_exec.

         DAS RISIKO STEHT IN [[mupi-weiss-beim-uebergang]] und ist hier
         SCHAERFER, nicht milder: dort nahm plymouthd der X-Sitzung die
         DRM-Karte als Master weg, und die Box startete ohne Bild. Auf .62
         laeuft ueberhaupt kein X mehr — der Kiosk ist
         `cog -O renderer=gles --platform=drm`, also DIREKT auf DRM. Wer
         frueh malt, muss die Karte sauber freigeben, bevor cog sie nimmt,
         sonst bleibt der Schirm ganz schwarz statt nur eine Weile.

      REIHENFOLGE MIT VERSTAND: Hebel 3 macht Hebel 4 ueberfluessig. Ohne die
      Wiederherstellung faellt die Untergrenze von selbst auf rund 2,2 s —
      dann braucht es kein initramfs, keinen DRM-Master-Streit und keine
      zweite Stelle, die bei jedem Kernel-Update mitgepflegt sein will.
      Erst 3 klaeren, dann ueber 4 reden.

      MESSEN:  python3 tools/box/dunkelphase-schau.py --box <ip>
    refs: [boot-animation-mit-echtem-fortschritt, mupi-weiss-beim-uebergang, kiosk-wartet-auf-dhcp]
  - id: darstellung-nur-wirksame-felder
    kind: entscheidung
    title: "E128: Die Darstellungs-Seite kennt nur noch wirksame Felder — und die Wache misst beide Richtungen"
    tags: [verwaltung, darstellung, felder, wache, e128, e121, 2026]
    source: >
      E128, 05.09.2026. Messung vor dem Umbau: 23 wirksame Format-Felder
      ohne Regler, 28 tote Angular-Felder im Interface. Nach dem Umbau:
      darstellung-felder-wer --pruefen 55/55 mit Leser, Admin-Suite
      208/208, tsc sauber.
    related: [erster-rollout-nach-dem-schnitt, bloecke-format-mixpi-thema, classic-und-themen-tauschen]
    body: |
      Betreiber-Entscheidung nach dem E118-Schnitt: "da kein altes
      Theme-System mehr da, gibt es nur noch wirksame." Die
      Darstellungs-Seite der Verwaltung fuehrt seither KEINE
      Ausblende-Listen mehr (NUR_NEU/NUR_KLASSISCH, zeigen/unwirksam,
      "auch Unwirksames zeigen", schirm() — alles gefallen): jedes Feld
      im interface Darstellung hat einen Leser in NewDesign/app.js, was
      keinen hat, fliegt aus dem Interface statt in eine Liste.

      Die Messung davor zeigte BEIDE Richtungen der Luecke: 28 tote
      Angular-Felder standen noch im Interface (kopfIcons, zeige*,
      mpVariante, plaetze ...), und 23 wirksame Felder FEHLTEN — nicht
      nur die acht E121-Uebernahmen, auch der ganze Kopfzeilen- und
      Player-Block, der bis dahin NUR im Eltern-Bereich der Box stellbar
      war (uhrzeit, restzeit, stAkku/stLautstaerke/stInternet/stWlan/
      stBluetooth, mpGlas, grossGlas, playerAkzent, wellenHub/Tempo,
      spielTitel/spielAlbum). Ohne diese Regler liess sich ein Thema nur
      importieren, nie in der Verwaltung BAUEN — die E120-Entscheidung
      verlangte aber genau das.

      Regeln, die dabei galten und weitergelten:
      - STANDARD der Seite spiegelt die LESE-Stellen von app.js (was
        gilt, wenn das Feld fehlt) — nicht die historischen Werte. Drei
        Vorgaben drehten sich dabei: endeZeit true→false, kategorien
        'reihe'→'liste', btAkkuArt 'prozent'→btAkku 'aus'.
      - Zahlen-Klemmen kommen aus mixpi-thema.ts (zahl(...)): was das
        Format annimmt, laesst sich stellen, nichts darueber hinaus.
      - albumTipp wird DREIWEGIG bedient und schreibt albumTippSpielt
        mit ('spielt' zu true) — dieselbe Doppelschreibung wie der
        Uebersetzer, damit Bestandsdaten stimmen.
      - Alte Feldnamen leben unter neuem flachem Namen weiter
        (zeigeTimer→schlummer, zeigeRest→titelRest, btAkkuArt→btAkku,
        startseite→startKategorie, abstandBez→namenAbstand,
        statusLaenge→streifenLaenge); die UI-Bloecke wurden umgewidmet,
        nicht neu erfunden.

      Die Wache tools/darstellung-felder-wer.mjs wurde umgewidmet: die
      Zwei-Schirm-Messung (neu/klassisch/beide) ist raus, --pruefen ist
      jetzt ROT in beide Richtungen — Interface-Feld ohne app.js-Leser
      (Schalter ohne Wirkung) UND Format-Anker ohne Verwaltungs-Regler
      (Thema nicht baubar). Ausnahmen nur benannt mit Grund
      (OHNE_REGLER: farbe, licht — die haengen am Profil und am
      Farbwaehler, nicht an dieser Seite).

      Falle des Tages, zum wiederholten Mal trotz zweier Warnkommentare
      in derselben Datei: ein Backtick in einem NEUEN Kommentar INNERHALB
      des Template-Literals beendet das Literal (TS1005 zwei Zeilen
      spaeter). Wer in darstellung.ts Kommentare schreibt, schreibt sie
      backtickfrei.
  - id: buehne-platz-aus-der-einstellung
    kind: architektur
    title: "E129: Die Bühne über den Tasten — Platz aus der Einstellung, Bewohner aus einer Registratur"
    tags: [player, buehne, wellen, songtext, e129, e99, 2026]
    source: >
      E129, 05.09.2026, gebaut. Zeugen: e2e-Fall „die Buehne haelt den
      Platz" (30/30), mixpi-thema.spec 53, darstellung-felder-wer 56/56.
      Vorgeschichte im selben Baum: E99 (das Frequenzfeld) und die
      Betreiber-Saetze vom 30.08. und 05.09.2026.
    related: [darstellung-nur-wirksame-felder, bloecke-format-mixpi-thema, songtext-anmeldepunkt-statt-plugin-sonderweg]
    body: |
      Betreiber 05.09.2026: „einmal sollen die bedienelemente dauerhaft
      hoch ruecken auch wenn die welle aus ist. der bereich soll zum
      anzeigen von weiteren animationen und auch songtext oder titel von
      alben dienen." Und auf die Rueckfrage: „also man kann umschalten
      welle oder songtext oooder,,, oder ,,,"

      DER UMBAU IST DIE BEDINGUNG, NICHT DAS AUSSEHEN. Das Frequenzfeld
      aus E99 sah genauso aus, aber seinen Platz gab `body.wellen-echt` —
      eine LAUFZEIT-Tatsache: die Klasse setzt der Zeichner, sobald im
      Poll wirklich ein `pegel`-Feld ankam. Damit sprangen die
      Bedienelemente hoch und runter, je nachdem was die Box gerade
      lieferte (Spotify Connect ohne Pegel: tief; naechstes Stueck: hoch).
      Seit E129 gibt `body.gross-buehne-an` den Platz, und die Klasse
      kommt aus der Darstellung. `wellen-echt` bleibt — aber nur noch fuer
      die Frage, ob der Fortschrittsbalken wieder normal aussehen darf.

      MERKSATZ: Wenn Bedienelemente „manchmal" woanders stehen, haengt ihr
      Platz an einer Laufzeit-Tatsache statt an einer Einstellung. Das ist
      fast nie gewollt und faellt beim Bauen nicht auf, weil die
      Entwicklungs-Attrappe entweder immer oder nie liefert.

      DIE DREI PUNKTE DES BETREIBERS SIND EINE BAUANWEISUNG. „oooder,,,
      oder ,,," heisst: es kommen weitere. Deshalb stehen die Bewohner in
      EINER Registratur (BUEHNE_BEWOHNER in app.js, je Eintrag
      `{leinwand, fuellen}`) und nicht in einer if-Kette. Ein neuer
      Bewohner ist drei Zeilen an drei Orten: Registratur (app.js), Wahl
      im Format (mixpi-thema.ts, `player.buehne`), Knopf in der
      Verwaltung (`buehnenBewohner`). Es zeigt sich IMMER GENAU EINER —
      auf 40 bis 78 px waeren zwei kein Angebot, sondern Krach.

      DER PLATZ HAENGT NICHT AM INHALT. Auch ein Bewohner ohne Inhalt
      (kein Songtext gefunden — bei Hoerspielen der Normalfall) behaelt
      seine Flaeche. Aus demselben Grund faellt bei
      `prefers-reduced-motion` nur die LEINWAND und nicht die ganze
      Buehne: wer Bewegung nicht vertraegt, soll ruhige Knoepfe bekommen,
      nicht wandernde.

      ══ DIE FALLE, DIE FAST DIE GANZE OBERFLAECHE AUSGEBLENDET HAETTE ══

      Die neue Klasse hiess zuerst `.buehne` — und `.buehne` ist in
      NewDesign seit je die HAUPTFLAECHE der App (das main-Element,
      app.css:2726, dazu `body.blaettern .buehne`). Die frisch getippte
      Regel `.buehne { display: none; }` (Grundzustand des neuen Feldes)
      haette beim ersten Laden die komplette Oberflaeche unsichtbar
      gemacht. Gefunden beim Gegenlesen VOR dem ersten Lauf, weil ein
      bestehender e2e-Fall `document.getElementById('buehne')` benutzte
      und die Kollision damit im Blickfeld lag.

      REGEL: Eine neue CSS-Klasse im NewDesign vorher gegen den Bestand
      pruefen — `grep -n "class=\"<name>\"" NewDesign/index.html` und
      `grep -n "^\.<name>" NewDesign/app.css`. Die Datei ist 440 kB gross;
      dass ein kurzes, naheliegendes Wort noch frei ist, ist eine
      Annahme, keine Tatsache. Wo es ein Praefix gibt, das Praefix nehmen
      (hier `gross-` fuer den grossen Player, wie gross-blatt/gross-vorn).
  - id: zwei-deckel-auf-der-lautstaerke
    kind: falle
    title: "Die Tasten kamen nur bis maxVolume: zwei Grenzen für dieselbe Lautstärke, in zwei Skalen"
    tags: [lautstaerke, player, maxVolume, skala, e130, 2026]
    source: >
      05.09.2026, Betreiber-Meldung, am Geraet nachgemessen: maxVolume der
      Box = 55. Behoben und belegt — Tasten fahren jetzt von 51 ueber die
      alte Grenze bis 100 (curl gegen /player/volume/+5, Karte per
      mupi-lautstaerke.sh get gegengelesen), Spec lautstaerke-stufe (5),
      Player-Suite 237.
    related: [buehne-platz-aus-der-einstellung, gegenprobe-statt-gruen-glauben]
    body: |
      Betreiber: „die knoepfe volumen in dem grossen player und auch im
      titelbild [gehen] nicht bis 100 prozent nur bis ca 50, der
      schieberegler vom miniplayer geht bis 100." Das „ca 50" war
      `mupibox.maxVolume` = 55.

      ZWEI WEGE, ZWEI GRENZEN, ZWEI SKALEN. Der Schieber schickt absolut
      (`setvolume:<v>`), die Tasten relativ (`volume/+5`). Seit die
      Ton-Serie `mupi-lautstaerke.sh` eingefuehrt hat, IST die Nutzerskala
      0..100 die ganze Miete: das Werkzeug bildet sie selbst auf
      0..maxVolume der Karte ab. Der absolute Weg und die Oberflaeche
      (`grenzeAus` in NewDesign/app.js) wurden darauf umgestellt — der
      relative Weg in `setVolume()` (spotify-control.ts) blieb zurueck:

          if (currentMeta.volume < muPiBoxConfig.mupibox.maxVolume) { +5 }
          else { currentMeta.volume = maxVolume; setz(maxVolume) }

      Links steht die NUTZERSKALA (0..100), rechts die Grenze der KARTE.
      Bei maxVolume 55 hoerten die Tasten also bei 55 der Nutzerskala auf,
      und das Werkzeug machte daraus 55 % von 55 — rund 30 % der Karte.
      Zwei Deckel uebereinander; das Kind kam nie an das erlaubte Maximum.

      MERKSATZ: Wo zwei Wege dieselbe Groesse stellen, muessen sie
      dieselbe Skala sprechen, und die Grenze gehoert an GENAU EINE
      Stelle. Wird eine Umrechnung eingefuehrt, sind ALLE Wege zu suchen,
      die dieselbe Groesse anfassen — nicht nur der, an dem man gerade
      arbeitet. Der absolute Weg war umgestellt und sah richtig aus; der
      Fehler stand einen Bildschirm weiter unten in derselben Datei.

      WARUM ES NIEMAND GEMESSEN HAT: Die Rechnung stand INNERHALB von
      `setVolume()`, zwischen zwei `exec()`-Aufrufen, und
      spotify-control.ts laesst sich nicht importieren, ohne Express,
      Spotify-Anmeldung und DNS loszutreten. Sie ist deshalb jetzt eine
      eigene reine Datei (`lautstaerke-stufe.ts`, `naechsteStufe`) mit
      Spec. Ein Fall darin liest die QUELLE des Aufrufers und schlaegt an,
      sobald `maxVolume` in spotify-control.ts wieder vorkommt — sonst
      blieben alle Rechen-Faelle gruen, waehrend der Dienst den alten
      Vergleich zurueckbaut (`gegenprobe-statt-gruen-glauben`).

      NEBENFUND: `import.meta.dirname` ist unter tsx undefined — beim
      Lesen der Nachbardatei im Test dieselbe Falle wie bei `__dirname`.
      `dirname(fileURLToPath(import.meta.url))` nehmen.
  - id: cover-wirtsname-loest-auf-tote-schnittstelle
    kind: falle
    title: 'Manche Cover laden nicht — die absolute Cover-Adresse scheitert am eigenen Boxnamen'
    tags: [cover, dns, ipv6, wlan, medien, 2026]
    source: >
      05.09.2026, Betreiber-Meldung, an Box .62 belegt mit
      tools/box/cover-adressen-probe.py: 35 von 42 Cover-Adressen absolut
      mit Wirtsnamen, alle Dateien vorhanden (ueber 127.0.0.1 alle 200),
      aber getent hosts MixPiBox lieferte auf der Box selbst eine
      IPv6-Adresse, die zu KEINER aktiven Schnittstelle gehoerte.
    related: [herkunftsriegel-und-der-namenstausch, keine-cover-in-der-album-ansicht]
    body: |
      Die Medienliste traegt Cover als ABSOLUTE Adressen mit Wirtsnamen
      (`http://MixPiBox:8200/cover/...`, Alt-Fork-Konvention). Der Browser
      auf der Box muss den Namen also aufloesen — und der Router-DNS gab
      eine IPv6-Adresse heraus, die zu einer gerade ABGESCHALTETEN
      Schnittstelle gehoerte (die Box hat zwei WLAN-Karten, die Adapterwahl
      legt eine nach dem Boot hin; der DNS-Eintrag stammt von frueher).
      Ergebnis: dieselben Cover laden mal ja, mal nein — je nachdem, welche
      Antwort der Namensdienst gerade gibt. Die 7 data-URIs und alles
      Relative laedt immer.

      DIE PROBE TRENNT DREI FRAGEN, die sonst zusammenfallen: Wie viele
      Adressen tragen ueberhaupt einen Wirtsnamen (Konstruktionsfehler)?
      Antworten sie, WIE SIE DASTEHEN (Sicht des Browsers)? Antworten sie
      ueber 127.0.0.1 (liegt die Datei da)? Fehl bei 2 mit Erfolg bei 3
      ist ein Namens-/Netzproblem, kein Dateiproblem.

      FIX-RICHTUNG: Cover-Adressen relativ ablegen (`/cover/...`) oder beim
      Ausliefern von /api/data auf relativ normalisieren — dann haengt das
      Bild immer am selben Wirt wie die Seite, unabhaengig von DNS und
      Schnittstellen-Stand. Bestandseintraege einmalig migrieren. Danach
      zaehlt die Probe `mit_wirt` auf 0.

      MERKSATZ: Eine Box mit zwei Funkkarten darf sich auf die Aufloesung
      ihres EIGENEN Namens nicht verlassen — auch nicht sich selbst
      gegenueber. Was die eigene Seite laedt, gehoert relativ verlinkt.
  - id: werk-zwillinge-verstecken-fehlende-titel
    kind: falle
    title: "Aufnahme-Seite zeigt nicht alle Albumtitel: die Namenssuche erwischt das kleine Zwillings-Werk"
    tags: [mitschnitt, werke, verschmelzung, admin, medien, 2026]
    source: >
      05.09.2026, Betreiber-Meldung, an Box .62 belegt mit
      tools/box/werk-zwillinge-probe.py: 15 Zwillingsgruppen unter 43
      Werken, 9 mit auseinanderliegenden Titelzahlen (Folge 13: spotify=18
      gegen lokal=7; Stadium Arcadium: 29 gegen 2).
    related: [halbe-aufnahme-nicht-anzeigen, regal-je-interpret-gegen-verschmelzen]
    body: |
      Ein teils aufgenommenes Album existiert als ZWEI (oder mehr) Werke:
      das Spotify-Werk mit allen Titeln und das Lokal-Werk mit nur den
      aufgenommenen — die Verschmelzung vereint sie in /api/werke nicht.
      Die Aufnahme-Seite der Verwaltung (aufzeichnen.ts) sucht „das Werk
      zum Album" per Namensvergleich ueber ALLE Werke (`alle.find(...)`).
      Trifft `find` zuerst die LOKALE Fassung, liefert deren /inhalt nur
      die schon aufgenommenen Titel: nichts gilt mehr als fehlend, nichts
      wird zum Nachladen angeboten. „Laed nicht alle Titel" heisst also
      nicht „die Liste bricht ab", sondern „es wird die falsche, kleinere
      Liste gefragt".

      FIX-RICHTUNG: bei mehreren Namenstreffern die Fassung mit
      Spotify-/Dienst-Quelle (oder schlicht die titelreichste) bevorzugen —
      eine Sortierung vor dem `find`. Die Probe misst danach weiter, denn
      die Zwillinge selbst bleiben.

      NEBENBEFUND MIT PLATTENFOLGEN: dieselben Aufnahmen liegen DOPPELT auf
      der Karte (media/audiobook/<interpret> UND media/music/<interpret>;
      ein Album sogar 1x audiobook + 3x music). Doppelter Platz, laengere
      Werkliste, und der Naehrboden der Verwechslung. Herkunft der
      Kategorie-Doppelung ungeklaert — erst messen (die Probe haelt den
      Bestand fest), dann aufraeumen.
  - id: wlan-flattern-boxseitig-und-doppel-ifup
    kind: howto
    title: 'WLAN brutal lange — box-seitiges Flattern auf 2,4 GHz, und 21 der 22 Boot-Sekunden sind Warten auf zwei ifups'
    tags: [wlan, boot, arp-flux, usb3, koexistenz, e5, 2026]
    source: >
      05.09.2026, Box .62 (Pi 5, USB3-AC1200-Stick wlan1 + BT-Dongle),
      gemessen: Doppel-Ping Router/Box 4 min (Router stabil, Box 6 Abrisse
      a 3-5 s), 12 Disconnects in 15 min Journal, systemd-analyze zweimal
      deckungsgleich (22,2/22,6 s, ifup@wlan1 13 s + ifup@wlan0 8 s),
      warmer Neustart bis API 18 s.
    related: [kiosk-wartet-auf-dhcp, boot-nur-ein-hebel-dhcp, doppelung-lassen-auseinanderlaufen-messen]
    body: |
      ZWEI PHAENOMENE, DIE SICH ALS EINES ANFUEHLEN:

      1. DAS FLATTERN traegt die Schuld am „brutal lange": Kurzabrisse von
         3-5 s etwa alle 30-60 s, eindeutig box-seitig (Doppel-Ping: der
         Router blieb die ganze Zeit erreichbar). Nach Kaltstart war die
         Box einmal >10 min nicht erreichbar („IP da, keine Verbindung").
         Umfeld: 2,4 GHz KANAL 13, USB3-Stick (USB3 stoert 2,4 GHz),
         BT-Dongle daneben (Koexistenz). Powersave beidseitig aus,
         USB-Autosuspend aus — beides scheidet aus. Hebel: 5-GHz-SSID fuer
         den AC-Stick, Kanal 1/6/11, Stick per Verlaengerung vom
         USB3-Port wegruecken.

      2. DER BOOT ist mit 22,6 s ehrlich, davon aber ~21 s WLAN: BEIDE
         Karten fahren hoch (allow-hotplug), obwohl die Adapterwahl
         (/etc/mupibox/mixpi-wlan-adapter = extern) laengst gefallen ist —
         mixpi-wlan-adapter.service legt die Verliererin erst NACH beiden
         ifups hin. Kostet 8 s Boot UND oeffnet das ARP-Flux-Fenster
         (zwei Adressen im selben Teilnetz: Ping ja, TCP nein). Die
         Sicherheitsregel „nie die letzte tragende Schnittstelle kappen"
         bleibt richtig — sie liesse sich aber VOR dem ifup durchsetzen
         (Gewaehlte zuerst; die andere nur, wenn die Gewaehlte binnen
         Frist nicht traegt). Der Betreiber erinnert „~20 s mit nur einer
         Karte" — passt: ohne den zweiten ifup waeren es ~14 s Netzanteil.

      DREI NEBENBEFUNDE DERSELBEN MESSUNG:
      * Der E5/B1-Hebel (dhcp-schneller.sh) ist in BEIDEN Ausrollwegen
        verdrahtet, aber /etc/dhcp/dhclient.conf der Box traegt den Block
        NICHT - die Box hat seit dem Einbau kein Update gefahren. Die
        schnellsten 3-4 s liegen also im Ausrollen, nicht im Bauen.
      * wifi-powersave-off.service beendet sich nach der ERSTEN
        erfolgreichen Karte (exit 0 in der inneren Schleife) - eine zweite
        wl*-Schnittstelle bliebe im Sparmodus, ein spaeter gesteckter
        Stick sowieso (oneshot). Heute zufaellig folgenlos, aber genau das
        im Unit-Kommentar beschriebene Symptom wartet dort.
      * Die Funkstrecken-Sonde ruft alle 5 s sudo wpa_cli - jeder Aufruf
        ein sudo-Journaleintrag; zusammen mit dem MuPiHAT-Logger reicht
        das Journal nur ~20 min zurueck (so verschwanden mittags die
        OOM-Belege). wpa_cli ginge ueber die netdev-Gruppe ganz ohne sudo.

      UHR-FALLE am Rand: die Box geht nach dem Boot mehrere Minuten nach,
      bis NTP steht (keine RTC). Prozess-Alter gegen die eigene Uhr
      gehalten ergibt Fehlbefunde („Kiosk laeuft nicht" - er lief).
  - id: wiedergabe-leck-im-wpe-renderer
    kind: signature
    match: "Out of memory.*WPEWebProcess|renderer process crashed"
    title: 'Der Kiosk-Renderer stirbt nach ~30 min Musik — das Leck gehoert zur WIEDERGABE, +40 MB/min, Stopp gibt nichts frei'
    tags: [freeze, oom, wpe, cog, wiedergabe, speicher, 2026]
    source: >
      05.09.2026 an Box .62 (Pi 5, 2 GB, cog/WPE, oberflaeche neu) in zwei
      Fenstern gemessen: Leerlauf 26 min = +0,22 MB/min (flach); leise
      Wiedergabe (lokales Album, 7 Titel, Pegel floss) = +39,7 und
      +39,0 MB/min in zwei parallelen Proben (240 -> 890 MB). Stopp: 928 ->
      927 MB nach 45 s - NICHTS wird frei. Passt exakt zum OOM-Kill von
      13:37 (WPEWebProcess, anon-rss 1,08 GB + 310 MB Swap, Box 33 min wach).
    related: [einmal-hinsehen-ist-keine-messung]
    body: |
      DIE SIGNATUR: Schirm zeigt "renderer process crashed" mit
      Try-again-Knopf; dmesg traegt den OOM-Kill des WPEWebProcess. Die Box
      war dabei um die 30 Minuten am SPIELEN - nicht einfach 30 Minuten an.

      WAS GEMESSEN IST (nicht vermutet):
      * Leerlauf ist unschuldig: +0,22 MB/min ueber 26 min. Bei dem Tempo
        braeuchte 1 GB drei Tage. Wer nur im Leerlauf misst, findet nichts.
      * Wiedergabe frisst +40 MB/min - unabhaengig von der Lautstaerke
        (gemessen bei Stufe 9). Hochgerechnet: OOM nach ~25-30 min Spielzeit
        auf der 2-GB-Box. Deckt sich mit dem echten Absturz (33 min).
      * STOPP GIBT NICHTS FREI (928 MB vor, 927 MB nach 45 s). Es ist kein
        Zwischenspeicher unter Druck, sondern gehaltener Speicher. Der
        Renderer traegt die Last bis zum Absturz; nur Seiten-Neuladen oder
        Neustart raeumen auf.

      WAS NOCH OFFEN IST - die naechste Messung entscheidet:
      dieselbe leise Wiedergabe unter CHROMIUM (kioskBrowser umstellen).
      Waechst Chromium genauso, liegt das Leck im App-Code der Wiedergabe-
      Pfade (Wellen-Zeichner 25 fps, 1-s-/local-Poll mit pegel, Cover);
      bleibt Chromium flach, ist es WPE-spezifisch (Rasterungs-/Canvas-
      Puffer) und der Ausweg heisst kioskBrowser=chromium oder WPE-Update.
      Chromium war im LEERLAUF flach (~173 MB) - Wiedergabe dort ist
      UNGEMESSEN, daraus folgt noch nichts.

      MESSAUFBAU (wiederholbar): tools/box/renderer-speicher-verlauf.py
      oder auf der Box ein 30-s-Sampler ueber /proc/<pid>/status (VmRSS,
      NICHT statm - 16-KB-Seiten!); Wiedergabe leise per
      curl /player/current/<befehl aus /api/werke/<s>/inhalt>, danach
      stop + setvolume zurueck. Nach dem Test Kiosk neu laden, sonst
      laeuft die Box mit dem gehaltenen Speicher in der Nacht ins OOM
      (pkill -x cog braucht sudo, und der Autostart holt cog NICHT von
      selbst zurueck - Neustart ist der saubere Weg, 18-25 s).
  - id: wiedergabe-leck-ist-wpe-chromium-bleibt-flach
    kind: entscheidung
    title: 'Die Gegenmessung entscheidet: das Wiedergabe-Leck ist WPE — Chromium bleibt unter identischer Last flach; Kiosk auf chromium gestellt'
    tags: [freeze, oom, wpe, cog, chromium, kiosk, entscheidung, 2026]
    source: >
      06.09.2026 ~00:30 an Box .62: kioskBrowser auf chromium, Neustart,
      DIESELBE leise Wiedergabestrecke wie bei [wiedergabe-leck-im-wpe-renderer]
      (lokales Album, 7 Titel, Pegel floss). Chromium-Renderer nach ueber
      20 min Wiedergabe bei 171/151 MB, Steigung +0,13/-0,05 MB/min —
      Cog/WPE lag bei +40 MB/min und 890 MB nach 16 min.
    related: [wiedergabe-leck-im-wpe-renderer, cog-drm-kiosk-statt-chromium]
    body: |
      DAMIT IST DIE OFFENE FRAGE AUS [wiedergabe-leck-im-wpe-renderer]
      BEANTWORTET: nicht der App-Code (Wellen-Zeichner, 1-s-Poll, Cover)
      haelt den Speicher — WPEs Rendering tut es. Derselbe Code, dieselbe
      Last, zwei Browser: Faktor ~300 in der Steigung.

      DIE ENTSCHEIDUNG: `mupibox.kioskBrowser = chromium` auf der Box
      (06.09.2026). Der Grund ist eine Abwaegung, keine Vorliebe: Cog ist
      schlanker und bootet schneller (siehe [cog-drm-kiosk-statt-chromium]) — aber
      ein Kiosk, der jedem Hoerspiel nach ~30 Minuten mit "renderer
      process crashed" stirbt, ist fuer ein Kind schlicht kaputt. Mehr
      Grundverbrauch schlaegt toedliches Leck.

      WER COG ZURUECKHOLEN WILL, braucht vorher eines davon:
      * ein WPE/Cog-Update, unter dem die Messstrecke flach bleibt
        (die Strecke ist im Quell-Eintrag beschrieben und wiederholbar), oder
      * den Nachweis, dass eine App-Aenderung (z. B. Wellen ganz aus,
        Cover statisch) WPE flach macht — dann waere es doch ein
        vermeidbarer App-Pfad. Erste Kandidaten stehen im Quell-Eintrag.

      MERKSATZ: Bei einem Leck mit zwei moeglichen Schuldigen (App oder
      Plattform) ist der billigste Beweis der PLATTFORM-TAUSCH bei
      identischer Last — eine Konfigzeile, ein Neustart, zwanzig Minuten.
      Jede Code-Vermutung haette laenger gedauert als die Messung.
  - id: warteschlange-aus-pfaden-vergiftet-die-tags
    kind: falle
    title: 'Die Aufnahme-Schlange wird aus Pfaden geboren — und die Ablage schreibt Pfad-Namen in die FLAC-Tags'
    tags: [mitschnitt, tags, metadaten, saeubern, 2026]
    source: >
      06.09.2026, Box .62: 8 von 26 library-Kacheln trugen gesaeuberte Namen
      („Folge 13_ …" statt „Folge 13: …"). Betreiber-Anforderung woertlich:
      „der tag muss identisch sein." Behoben mit 5e4843cb; Zielbild in
      BACKLOG E132.
    related: [wiedergabe-leck-im-wpe-renderer]
    body: |
      DIE KETTE: `bestandDurchsehen`/liste.mjs bauen Warteschlangen-Eintraege
      aus `<kategorie>/<interpret>/<album>/<nn titel>.flac` — also aus Namen,
      denen `saeubern()` die Doppelpunkte laengst genommen hat. Wer aus so
      einem Eintrag neu aufnimmt, schreibt den PFAD-Namen in die Tags; die
      Datei erbt den Makel ihres Ordners. Live-Mitschnitte waren nie
      betroffen (`titelAus` liest den Spotify-ZUSTAND).

      DIE LOESUNG (5e4843cb): vor der Ablage EINMAL je Titel das Kanonische
      ueber die BESTEHENDE bewachte Lese-Durchreiche holen
      (`/api/spotify/web/tracks/<id>` — steht schon in WEB_API_ERLAUBT,
      kein neuer Endpunkt noetig). `kanonischesAus()` dampft die Antwort auf
      die Ablage-Felder ein (album.artists, nicht die Gaeste des Titels),
      `mitKanonischem()` laesst das Kanonische gewinnen, die Schlange ist
      nur Rueckfall. Der Ordner verschiebt sich nicht: `saeubern` ist
      idempotent (per Spec belegt).

      ZWEI LEHREN:
      * Wer eine Liste aus dem DATEISYSTEM rekonstruiert, traegt dessen
        Verluste weiter — Pfade sind ein verlustbehaftetes Format fuer
        Namen. Die Wahrheit gehoert in Metadaten (BACKLOG E132: ein
        Aufnahmeordner, SQLite ueber node:sqlite — am Geraet nachgemessen:
        Node v26.6 hat DatabaseSync eingebaut).
      * VOR dem Bau eines neuen Endpunkts die bestehende Erlaubnisliste
        lesen: `tracks/<id>` war laengst freigegeben. Ein neuer Weg waere
        „eine Erlaubnis zu viel" gewesen (Wortlaut der Liste selbst).

      NEBENFUND, eigener Punkt (E132/5): Teil 05 der Folge 13 hatte GAR
      keine Tags — er stammt vom E114-Cue-Testlauf (mpv-Mitschnitt), der
      bisher ueberhaupt keine Tags schreibt.
  - id: helfer-ohne-einzugsgebiet
    kind: lehre
    title: 'Ein Helfer ohne Wache ist eine Empfehlung — kopiert wird trotzdem (Kritiker-Lauf 06.09.)'
    tags: [kritiker, doppelung, helfer, wachen, 2026]
    source: >
      Kritiker-Lauf 06.09.2026 (AUDIT-2026-09-06.md), vier parallele Pruefer,
      nur gelesen und gemessen; Zahlen per grep am Baum belegt.
    related: [wiederkehrender-befund-ist-ablauffehler, vierzehn-kopien-und-die-abweichung-ist-der-fehler, klick-sturm-laedt-nichts-mehr]
    body: |
      DAS MUSTER (Vorstufe der Halbmigration): der Helfer existiert, aber
      niemand hat festgelegt, dass er GILT — kein Einzugsgebiet, keine
      Wache. Vier Belege aus einem einzigen Lauf:

      * atomar.ts: 3 Nutzer, ~25 Handstellen daneben (waechst: 18 am 04.09.),
        plus ein ZWEITER Helfer kzSchreiben (8 Aufrufer) fuer dieselbe Frage.
        Dazu ein echter Fehler: server.ts:7633 legt das Zwischenstueck in
        os.tmpdir() — rename ueber Dateisystemgrenze ist NICHT atomar.
      * tools/leihgabe.mjs: 136 Nutzer — aber die zwei meistkopierten
        Bloecke fehlen ihm: der CDP-send-Wrapper liegt 94x kopiert
        (~1030 Zeilen), die Vorschau-Warteschleife 88x (~530 Zeilen).
      * NewDesign/app.js json(): 43 Nutzer, aber kein POST-Geschwister —
        ~34 handkopierte fetch-Bloecke, 27 von 55 fetch OHNE Frist.
      * tools/welche-box.sh: 5 Nutzer, 53 Werkzeuge bauen ssh selbst,
        177 Dateien mit festen IP-Literalen.

      KONSEQUENZ: ein neuer Helfer bekommt IM SELBEN COMMIT eine Wache, die
      Neuzugaenge des alten Musters meldet (Bauart shellcheck-Ratsche:
      Bestand einfrieren, Zuwachs rot). Sonst steht er in sechs Wochen auf
      der Kritiker-Liste, wie atomar.ts seit dem 20.08.

      ZWEITER FUND DES LAUFS, andere Sorte: tools/audit-raenge-gebucht.py
      bewachte die FALSCHE FORM — sie liest Raenge nur als Tabellenzeile
      oder nummerierte Liste, die seit 04.09. genutzte Ueberschriftform
      "### Rang n" ist ihr unsichtbar. Nebenwirkung: ein Zeilenumbruch vor
      "105." in Prosa wurde als Rang 105 gemeldet (Phantom). Eine Wache,
      die die Form von gestern prueft, ist gruen auf die gefaehrlichste
      Art; die 22 offenen Buchungen hat der Lauf nachgetragen, die
      Parser-Reparatur ist AUDIT-2026-09-06 Rang 2.

      GROESSTES EINZELRISIKO des Laufs: die Spotify-Token-Erneuerung lebt
      DREIFACH (spotify-api.service.ts:90, server.ts:19951,
      spotify-control.ts:690) und ist schon auseinandergelaufen — Frist und
      In-Flight-Sperre gegen den Token-Sturm hat nur server.ts; der Player
      friert seine Config per require() bis zum Neustart ein. API und
      Player koennen mit verschiedenen Refresh-Token laufen.
  - id: ifup-riegel-execcondition-statt-execstartpre
    kind: falle
    title: 'Der ifup-Riegel wirkt — aber ExecStartPre hinterlaesst eine dauerhaft ROTE Unit; ExecCondition ist der gebaute Weg'
    tags: [wlan, systemd, boot, riegel, ifup, e131, 2026]
    source: >
      06.09.2026 an Box .62 (systemd 257) beim Einbau des WLAN-Adapterwahl-
      Riegels (E131/C1). Drei Messboots mit Riegel: 20,1 / 19,5 / 21,5 s
      Gesamtstart gegen 23,4 s ohne; ifup@wlan0 verschwindet ganz aus
      `systemd-analyze blame` (vorher 8,0-10,0 s), und `ip -br addr` zeigt
      nur noch EINE Adresse statt zwei.
    related: [einmal-hinsehen-ist-keine-messung, dauerrote-wache-ist-keine]
    body: |
      ZWEI FALLEN AUF EINMAL, beide erst am GERAET sichtbar:

      1. EXECSTARTPRE MACHT DIE UNIT ROT. Ein ExecStartPre mit Rueckgabe != 0
         bricht den Start richtig ab — markiert die Instanz aber als FAILED.
         Danach steht `ifup@wlan0` dauerhaft in `systemctl --failed`, obwohl
         nichts kaputt ist. Eine rote Zeile, die keinen Fehler meint, macht
         die naechste ECHTE unsichtbar (dieselbe Falle wie
         [dauerrote-wache-ist-keine]). Die Loesung heisst ExecCondition
         (systemd >= 243): Rueckgabe 1-254 bedeutet „uebersprungen", die Unit
         bleibt sauber INACTIVE.

      2. DER RIEGEL KOMMT ZU FRUEH FUER EINEN USB-STICK. Beim ersten
         Geraetelauf griff er gar nicht: `ifup@wlan0` startet, BEVOR der Stick
         enumeriert ist — /sys/class/net/wlan1 existierte noch nicht, die
         Sicherheitsregel („gewaehlte Karte nicht da -> die andere darf hoch")
         liess wlan0 korrekt durch, und der Riegel lief ins Leere. Fix: kurz
         auf das ERSCHEINEN der gewaehlten Karte warten (Vorgabe 10 s,
         MIXPI_WLAN_STICK_WARTEN), dann erst urteilen. Der Preis faellt nur
         im Stick-gezogen-Fall an.

      WAS DIE MESSUNG SONST NOCH LEHRTE: Ein einzelner Boot log. Der erste
      Lauf mit greifendem Riegel zeigte 45 s Gesamtstart und liess den
      Riegel wie eine Verschlechterung aussehen — im Journal steckten
      32 s zwischen dhclient-Start und dem ersten DHCPDISCOVER, also
      Funk-Assoziation des Sticks (das bekannte Flattern), nicht der Riegel.
      Drei Boots hintereinander zeigten 20/19,5/21,5 s. Siehe
      [einmal-hinsehen-ist-keine-messung].

      DIE RETTUNG WIRD NICHT AM GERAET GEFAHREN. Der Rettungszweig im
      Adapter-Dienst (traegt die Gewaehlte nicht, faehrt er die verriegelte
      Karte per direktem `ifup` hoch) laesst sich nur ausloesen, indem man
      die tragende Verbindung kaputtmacht — genau der Fall, in dem ein
      Fehler die Box aussperrt. Er steht deshalb in
      tools/mixpi-wlan-adapter.test.sh mit Attrappen fuer ip/ifup/ifdown.
      Am Geraet gefahren wurden Normalfall und die ungefaehrliche
      Gegenprobe (Wahl-Datei weg -> beide Karten kommen hoch wie frueher).
  - id: spotify-abschaltung-2026-trifft-die-box-nicht
    kind: note
    title: 'Spotifys Abschaltungen 02/2026 treffen die Box NICHT — sie faehrt mit Nutzer-Token, nicht mit Client-Credentials (am Geraet gemessen)'
    tags: [spotify, api, messung, audit, 2026]
    source: >
      06.09.2026, Box .62, mit dem Token der Box gemessen
      (tools/spotify-endpunkt-probe.sh). Anlass: AUDIT-2026-09-06 Rang 3
      meldete die Spotify-Umbauten vom Februar/Maerz 2026 (Deadline
      09.03.2026) als Risiko — ein halbes Jahr nach der Frist, ohne dass
      an der Box je etwas ausgefallen waere.
    related: [wiki-zustand-hat-haltbarkeit, einmal-hinsehen-ist-keine-messung]
    body: |
      DIE MESSUNG: Alle fuenf Endpunkte, die die Box wirklich benutzt,
      antworten mit HTTP 200 —

          artists/<id>                                200
          artists/<id>/top-tracks?market=DE           200   (angeblich entfernt!)
          artists/<id>/albums?include_groups=…        200
          search?q=…&type=artist                      200
          albums/<id>?market=DE                       200

      DIE ERKLAERUNG, und sie ist der eigentliche Merksatz: Spotify hat im
      Februar 2026 die CLIENT-CREDENTIALS auf Metadaten-Endpunkten
      gestrichen — den Weg „App ohne Nutzer". Die Box geht diesen Weg
      NICHT. `webApiToken()` (server.ts) holt ihren Token mit
      `grant_type=refresh_token` aus dem Refresh-Token eines ANGEMELDETEN
      Kontos; das ist ein Nutzer-Token, und fuer den gelten die
      Streichungen nicht.

      DIE LEHRE FUERS NAECHSTE MAL: Eine Netz-Recherche sagt, WAS sich
      draussen geaendert hat — ob es DIESE Box trifft, entscheidet der
      Zugangsweg, und den beantwortet nur eine Messung am Geraet. Die
      Gegenfrage lautete hier: „welchen grant_type benutzen wir
      ueberhaupt?" — erst die Messung machen, die die eigene Erklaerung widerlegen wuerde. Ohne sie waere
      ein mehrtaegiger Umbau auf go-librespot gegen ein Problem gelaufen,
      das die Box nicht hat.

      WAS OFFEN BLEIBT (der zweite Teil desselben Rangs): die neun
      Spotify-Durchreiche-Routen ohne jeden Aufrufer sind unabhaengig
      davon faellbar — sie sind tot, nicht kaputt.

      HALTBARKEIT: Zustandsaussage vom 06.09.2026. Spotify kann auch
      Nutzer-Token-Wege aendern; wer sie spaeter braucht, misst mit
      demselben Werkzeug nach (`tools/spotify-endpunkt-probe.sh <box>
      '<pfad>' …`, rein lesend, der Token verlaesst die Box nicht).
  - id: hoerdaten-empfehlen-fuer-kinderhoerspiele-das-falsche
    kind: falle
    title: "Aehnliche Interpreten: Hoerdaten-Dienste liefern fuer Kinderhoerspiele nichts oder Rammstein — nur Deezer traegt"
    match: "aehnliche interpreten|similar artists|empfehlung kinderhoerspiel|listenbrainz leer|lastfm kinderserie|deezer related|was noch hoeren|recommendation kinder"
    sev: med
    tags: ["empfehlung", "similar", "plugins", "deezer", "listenbrainz", "musicbrainz", "kinder", "mixpi-similar", "2026"]
    source: >
      06.09.2026, gemessen mit tools/similar-quellen-probe.py gegen die echten
      Schnittstellen (Arbeitsplatz, ohne Box), beim Bau von
      plugins/mixpi-similar. Vier deutsche Kinderserien gegen zwei Popmusiker
      als Kontrolle.
    symptom: >
      Ein Empfehlungsweg wird gebaut, gegen Popmusik geprueft und sieht
      hervorragend aus — je 100 Treffer, plausible Namen. Auf der Box liefert
      er dann zu „Pettersson und Findus" NICHTS, zu „Conni" NICHTS, und zu
      „Benjamin Bluemchen" zwei Treffer, von denen einer RAMMSTEIN ist.

      Der Fehler sieht aus wie ein kaputter Aufruf. Er ist keiner.
    cause: >
      HOERDATEN MESSEN, WER DASSELBE HOERT — und wer abends Kinderhoerspiele
      auflegt, hoert nachts etwas anderes. Das Geraet ist dasselbe, das Konto
      ist dasselbe, also steht die Kinderserie im selben Hoerprofil wie die
      Musik der Eltern. Fuer eine Kinderbox ist das keine schwache Empfehlung,
      sondern eine falsche.

      Dazu kommt schiere Duenne der Daten. Gemessen (4 Kinderserien:
      Pettersson und Findus, Bibi Blocksberg, Conni, Benjamin Bluemchen;
      Kontrolle: Nena, Die Toten Hosen):

          Quelle             Kinderserien   Kontrolle
          MusicBrainz            4/4          2/2     (nur Identitaet)
          Deezer /related        3/4          2/2
          ListenBrainz           1/4          2/2

      DIE KONTROLLE IST DER PUNKT DIESER TABELLE. Ohne sie haette man
      ListenBrainz fuer kaputt gehalten; der Dienst ist voellig in Ordnung, er
      weiss ueber diesen Gegenstand nur nichts. Wer nur mit dem eigenen
      Anwendungsfall prueft, kann „Dienst tot" nicht von „Dienst kennt das
      nicht" unterscheiden — siehe [[einmal-hinsehen-ist-keine-messung]].

      Deezer dagegen traegt, und die Treffer sind brauchbar statt nur
      vorhanden:

          Pettersson und Findus -> Benjamin Bluemchen, Die Fuchsbande,
                                   Pumuckl, Astrid Lindgren Deutsch
          Bibi Blocksberg       -> Bibi und Tina, Deine Freunde,
                                   Die Schule der magischen Tiere
    fix: >
      DIE EIGENE INHALTS-QUELLE IST NICHT DIE ZUGABE, SONDERN DIE HAUPTQUELLE.
      Eine von Hand gepflegte Themenkarte (Name, Schlagworte, Beschreibung,
      Vergleich ueber Jaccard) ist die einzige Quelle, die „ruhiger Bauernhof"
      von „Rettungsfahrzeuge mit Martinshorn" unterscheiden kann. In
      plugins/mixpi-similar wiegt sie deshalb 1,5, Deezer 1,0, Last.fm 1,0 und
      ListenBrainz 0,3 — nicht abgeschaltet, aber leise.

      WER HOERDATEN HOCHDREHT, SETZT `mindestensQuellen` AUF 2. Dann braucht
      ein Vorschlag eine zweite Stimme, und der einzelne Ausreisser aus dem
      Hoerprofil der Eltern faellt heraus.

      DEEZERS GEWICHT GEHOERT NICHT HERABGESTUFT, WEIL ES KEINEN SCORE HAT.
      Der Entwurf sah dafuer 0,7 vor; bei Reciprocal Rank Fusion ist ein
      fehlender Score aber gleichgueltig, weil RRF ohnehin nur den RANG
      benutzt. Herabgestuft wuerde damit ausgerechnet die beste Quelle.

      DREI FALLEN AN DEN DIENSTEN SELBST, alle am selben Tag gemessen:

        * DEEZERS ERSTER TREFFER IST NICHT DER BESTE. `/search/artist?q=Conni`
          liefert als Ersten „Conni & Co" (id 57582222) mit LEEREM
          `/related`; die gesuchte Hoerspielserie heisst ebenfalls „Conni"
          (id 75255, 95 Alben, 29964 Anhaenger). `nb_fan` und `nb_album`
          stehen im Suchergebnis schon drin und kosten keine Anfrage.
        * DEEZER MELDET FEHLER MIT HTTP 200 und einem `error`-Feld. Wer nur
          den Status prueft, traegt die Fehlermeldung als leere Liste in den
          Zwischenspeicher — dort steht sie dann bis zum Ablauf.
        * DER LISTENBRAINZ-ALGORITHMUSNAME MUSS VOLLSTAENDIG SEIN. Am Dienst
          nachgestellt, dieselbe MBID:

              …_threshold_10_limit_100_filter_True_skip_30   200
              …_threshold_15_limit_50_skip_30                200
              …_threshold_10_limit_100                       400

          Der dritte ist der zweite mit abgeschnittenem Ende und keine
          gueltige Kurzform. Es gibt weder Vorgabe noch Ergaenzung: was nicht
          Zeichen fuer Zeichen stimmt, ist ein 400 — und der sieht aus wie ein
          toter Dienst. Genau daran ist die erste Fassung von
          quellen/listenbrainz.mjs gescheitert.

        * MUSICBRAINZ DROSSELT TROTZ EINGEHALTENER 1 ANFRAGE/S (5 von 18
          Anfragen kamen als 503). Wer 503 als „gibt es nicht" liest,
          streicht Serien, die es gibt, und merkt sich das Nichts.
    lesson: >
      Bei einer fremden Empfehlungsquelle ist die erste Frage nicht „antwortet
      sie?", sondern „antwortet sie ueber MEINEN Gegenstand?" — und die
      Kontrollmessung mit einem Gegenstand, von dem der Dienst sicher etwas
      weiss, ist der einzige Weg, „kaputt" von „kennt das nicht" zu trennen.

      Und: eine Empfehlung auf einer Kinderbox hat eine Fehlerklasse, die es
      auf einer Musikbox nicht gibt. Ein schlechter Vorschlag ist dort ein
      Achselzucken, hier steht er vor einem Kind.
  - id: artist-feld-der-box-ist-nicht-ein-interpret
    kind: falle
    title: "Das artist-Feld der Medienliste traegt oft MEHRERE Interpreten — und der Namensabgleich findet dann keinen einzigen"
    match: "interpret nicht gefunden|artist feld mehrere|kommagetrennt interpret|namensabgleich medienliste|hoerspiele umlaut nicht gefunden|oe statt umlaut|interpret abgleich schlaegt fehl"
    sev: med
    tags: ["medien", "interpreten", "normalisierung", "bestand", "mixpi-similar", "empfehlung", "2026"]
    source: >
      06.09.2026, gemessen gegen den echten Bestand von Box .62
      (GET /api/data, 66 Eintraege) beim Bau der Themenkarte von
      plugins/mixpi-similar. Betreiberhinweis: „du kannst auch im daten
      bestand schauen wenn die box online ist".
    symptom: >
      Ein Weg, der Interpreten der Box gegen eine eigene Liste oder einen
      fremden Katalog abgleicht, findet einen grossen Teil des Bestands nicht
      — und zwar ausgerechnet die haeufigsten Eintraege. Die Namen sehen im
      Log richtig aus, die Liste enthaelt den Eintrag sichtbar, und der
      Abgleich sagt trotzdem nein.
    cause: >
      ZWEI URSACHEN, DIE UNABHAENGIG VONEINANDER BEISSEN. Beide fallen erst am
      echten Bestand auf, nicht an selbst ausgedachten Beispielen.

      (1) DAS FELD `artist` IST NICHT EIN INTERPRET, SONDERN EINE AUFZAEHLUNG.
      Im Bestand von .62 gemessen:

          "Ruby van der Bogen, 101 fabelhafte Freunde"      9 Eintraege
          "Team Karacho, ANOTHER NGUYEN"                    2 Eintraege
          "Team Karacho, Rola"                              2 Eintraege
          "Die Maus, Eva mit Gitarre, Der Elefant"          1 Eintrag
          "Gabby’s Dollhouse Deutschland, Gabby's Dollhouse" 1 Eintrag

      Der erste Posten ist der GROESSTE im ganzen Bestand. Wer das Feld als
      einen Namen nimmt, verliert ihn vollstaendig.

      (2) UMLAUT UND AUSGESCHRIEBENE FORM FALLEN NICHT VON SELBST ZUSAMMEN.
      Die uebliche Normalisierung (NFD, dann kombinierende Zeichen weg) macht
      aus „Hoerspiele" ein „hoerspiele" und aus „Hörspiele" ein „horspiele" —
      zwei verschiedene Schluessel fuer dieselbe Sache. Die Box fuehrt
      „Hello Kitty Hörspiele", ein selbst gepflegter Datensatz schreibt eher
      „Hoerspiele", und der Abgleich scheitert an einem Buchstaben.

      DIE REIHENFOLGE IST DIE DRITTE FALLE, und sie ist die teuerste, weil sie
      erst NACH dem Beheben der ersten beiden zuschlaegt: wer den Namen erst
      normalisiert und dann am Komma zerlegen will, findet KEIN Komma mehr —
      die Normalisierung hat jedes Satzzeichen zu einem Leerzeichen gemacht.
      Der Zerlegeschritt gehoert auf den ROHEN Namen.
    fix: >
      DREI STUFEN, IN DIESER REIHENFOLGE (so gebaut in
      plugins/mixpi-similar/quellen/inhalt.mjs):

        1. Ueber die MBID, wo es eine gibt — sie ist die Wahrheit.
        2. Ueber den Namen ODER einen Alias. Die Box nennt Serien anders als
           jeder Katalog: „Die Maus" statt „Die Sendung mit der Maus",
           „Major Tom" statt „Der kleine Major Tom", „Ruby van der Bogen"
           statt der Serie. Ein Alias-Feld (`auch`) ist keine Bequemlichkeit,
           sondern die halbe Abdeckung.
        3. Ueber die TEILE eines Mehrfachfeldes, zerlegt am ROHEN Namen an
           `, ; / &` und `feat.`. Teile unter vier Zeichen werden nicht
           probiert, sonst trifft ein „der" aus „Der Elefant" irgendwann
           irgendetwas.

      BEI DER NORMALISIERUNG die deutschen Umlaute AUSSCHREIBEN (ae, oe, ue,
      ss), und zwar VOR dem NFD-Schritt; alle uebrigen Diakritika danach
      wegnehmen. Dann fallen „Hörspiele" und „Hoerspiele" auf dieselbe Form.
      Der Preis ist bekannt und in einem deutschen Hoerspielbaum die richtige
      Seite des Handels.

      Ebenfalls noetig, ebenfalls am Bestand gesehen: die BEIDEN Apostrophe
      (U+0027 und U+2019) muessen gleich behandelt werden — „Gabby's" und
      „Gabby’s" stehen nebeneinander in derselben Liste.

      WIRKUNG, gemessen: von 21 verschiedenen `artist`-Feldern werden 20
      aufgeloest, von 66 Bestands-Eintraegen sind 64 abgedeckt. Der einzige
      Fehlschlag ist „ROSÉ, Bruno Mars" — echte Popmusik, die in einer
      Kinder-Themenkarte nichts verloren hat.
    lesson: >
      Eine Namensnormalisierung gegen selbst ausgedachte Beispiele zu pruefen
      beweist nichts. Der Bestand der Box enthaelt Formen, auf die man nicht
      kommt — Mehrfachfelder, zwei Apostrophe, beide Umlautschreibweisen
      nebeneinander. Ist die Box erreichbar, ist `GET /api/data` die billigste
      Messung im ganzen Vorhaben.

      Und: bei einer Kette aus Normalisieren und Zerlegen entscheidet die
      REIHENFOLGE, nicht die einzelne Regel. Was die Normalisierung wegwirft,
      steht dem Zerlegen nicht mehr zur Verfuegung.
  - id: wache-die-nur-index-spec-kennt-waechst-nicht-mit
    kind: falle
    title: "Eine Zeugen-Wache, die nur `index.spec.mjs` kennt, uebersieht jede zweite Testdatei — und meldet dabei gruen"
    match: "zeugen laufen nicht|spec wird nicht gerufen|test nicht im laeufer|plugin tests rosten|index.spec.mjs laeufer|testdatei uebersehen"
    sev: med
    tags: ["wachen", "tests", "plugins", "laeufer", "prozess", "mixpi-similar", "2026"]
    source: >
      06.09.2026, beim Bau von plugins/mixpi-similar. Die Wache
      tools/plugin-zeugen-probe.sh entstand am selben Tag und lief noch
      denselben Tag in genau die Falle, gegen die sie gebaut war.
    symptom: >
      Ein Laeufer meldet „alle Plugins gruen", und trotzdem laeuft ein
      erheblicher Teil der Zeugen nie. Es faellt nicht auf, weil die Wache
      gruen ist — sie prueft ja, was sie findet, und sie findet zu wenig.
    cause: >
      DIE ERSTE FASSUNG SUCHTE EINEN DATEINAMEN, nicht eine Sorte:

          zeuge="$ordner/index.spec.mjs"

      Das ist dieselbe Falle wie „Wache auf Sorte, nicht auf Pfad", nur eine
      Ebene hoeher: Das Merkmal ist „ist eine Zeugendatei", nicht „heisst
      index.spec.mjs". Am selben Tag gemessen, nach der Umstellung auf
      `find -name '*.spec.mjs'`:

          mixpi-mitschnitt   131 -> 254 Zeugen (5 Dateien)
          mixpi-similar       59 ->  98 Zeugen (2 Dateien, spaeter 3)

      Bei einem FREMDEN Plugin liefen also 123 Zeugen in keinem Laeufer, und
      das schon laenger — die neue Wache hat sie nicht kaputtgemacht, sondern
      sichtbar. Aufgefallen ist es nur, weil im eigenen Plugin eine zweite
      Zeugendatei entstand und jemand nachsah, ob der Laeufer sie ruft.
    fix: >
      DIE WACHE AUF DIE SORTE STELLEN, nicht auf den erwarteten Namen:

          while IFS= read -r datei; do zeugen+=("$datei"); done \
            < <(find "$ordner" -maxdepth 2 -name '*.spec.mjs' | sort)
          node --test "${zeugen[@]}"

      UND DIE ANZAHL DER DATEIEN AUSGEBEN, wenn es mehr als eine ist
      („254 Zeugen in 5 Dateien"). Ohne diese Zahl sieht ein Plugin mit fuenf
      Testdateien genauso aus wie eines mit einer, und der naechste, der eine
      Datei dazulegt, kann nicht sehen, ob sie mitlaeuft.

      DASSELBE GILT FUER DIE GEGENPROBE. `tools/similar-gegenprobe.sh` fuhr
      anfangs auch nur `index.spec.mjs` und haette fuer jede
      Wachlisten-Vorrichtung „ungeprueft" gemeldet, obwohl der Zeuge
      danebenlag — eine Gegenprobe, die nicht alle Zeugen faehrt, misst die
      falsche Sache und schickt den naechsten auf die Suche nach einem Fehler,
      den es nicht gibt.
    lesson: >
      Wer eine Wache baut, die eine Sorte Datei einsammelt, prueft sie am
      Tag ihres Entstehens gegen einen Baum, in dem es mehr als ein Beispiel
      gibt — und sieht nach, ob die Zahl stimmt, statt nur auf „gruen" zu
      sehen. Eine zu enge Wache ist gefaehrlicher als keine: sie erzeugt
      Vertrauen, das sie nicht deckt. Siehe [[wache-auf-sorte-nicht-auf-pfad]],
      [[zahlenwache-kannte-nur-eine-datei]] (dieselbe Enge, andere Sorte) und
      [[wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm]].
  - id: benachrichtigung-ohne-grundlinie-meldet-den-altbestand
    kind: falle
    title: "Eine Wachliste ohne Grundlinie meldet beim ersten Lauf den ganzen Altbestand — und wird sofort wieder abgeschaltet"
    match: "hunderte benachrichtigungen|wachliste meldet alles|neuerscheinung altbestand|watchlist erste pruefung|zu viele meldungen|release benachrichtigung flut"
    sev: med
    tags: ["benachrichtigung", "wachliste", "plugins", "mixpi-similar", "entwurf", "2026"]
    source: >
      06.09.2026, beim Bau der Wachliste von plugins/mixpi-similar
      (Entwurf §9). Als Zeuge festgehalten, nicht am Geraet erlitten — die
      Rechnung dahinter ist aber die des Entwurfs selbst.
    symptom: >
      Die erste Pruefung einer frisch angelegten Wachliste erzeugt mehrere
      hundert Meldungen. Alle sind formal richtig: es sind wirklich
      Veroeffentlichungen dieser Interpreten. Nur ist keine davon NEU — es ist
      der Katalog.

      Die Folge ist nicht „viele Meldungen", sondern „keine Meldungen": der
      Benutzer schaltet die Benachrichtigung ab, bevor sie je etwas Nuetzliches
      gesagt hat, und erfaehrt die eine echte Neuerscheinung nie.
    cause: >
      Ein Release-Endpunkt liefert IMMER den ganzen Katalog; „neu" ist keine
      Eigenschaft der Antwort, sondern eine Frage an den eigenen Speicher.
      Benjamin Bluemchen hat bei MusicBrainz 235 Release-Groups, Bibi
      Blocksberg 186 (gemessen am selben Tag). Zwanzig Wachlisten-Eintraege
      sind damit leicht mehrere tausend Datensaetze, von denen ohne Grundlinie
      jeder als Neuigkeit durchgeht.
    fix: >
      BEIM EINTRAGEN DEN AKTUELLEN STAND ALS BEKANNT VERMERKEN, und nur melden,
      was DANACH erscheint. Drei Feinheiten, die alle drei einen eigenen Zeugen
      wert sind:

        * LAESST SICH DIE GRUNDLINIE NICHT HOLEN, WIRD NICHT EINGETRAGEN.
          Ein Eintrag mit leerer Grundlinie sieht aus wie ein normaler und
          meldet beim naechsten Lauf alles. Lieber einen 502 mit Klartext.
        * AUCH ALTES WIRD ALS BEKANNT VERMERKT, nicht nur das Gemeldete. Sonst
          wird es bei jedem Durchlauf erneut geprueft — und ein Dienst, der
          sein Datumsformat einmal aendert, laesst den ganzen Altbestand auf
          einen Schlag durchgehen.
        * EIN BLOSSES JAHR ZAEHLT NUR, WENN ES ECHT SPAETER IST als das Jahr
          der Grundlinie. Im selben Jahr waere es geraten, und geraten heisst
          hier „meldet den Altbestand". Der Fehler in die andere Richtung
          verzoegert eine Meldung um eine Runde — die beiden sind nicht gleich
          teuer.

      DEDUPLIZIERT WIRD UEBER RELEASE-GROUP-MBID, ersatzweise UPC, ersatzweise
      normalisierter Titel PLUS JAHR — nicht plus Datum. MusicBrainz fuehrt bei
      Hoerspielen ueberwiegend nur „2026" (Bibi Blocksberg: 8 taggenau gegen 15
      nur Jahr), Deezer immer „2026-03-14". Auf das Datum verglichen waeren das
      zwei Werke, und dieselbe Folge kaeme zweimal.
    lesson: >
      Bei jeder Benachrichtigung ist die erste Frage nicht „wie melde ich?",
      sondern „woran erkenne ich, dass etwas NEU ist?" — und die Antwort ist
      fast immer ein Stand, der beim Einrichten festgehalten wird. Wer sie
      spaeter nachruestet, hat den Benutzer schon verloren.

      Die zweite Frage ist die nach der GENAUIGKEIT, die beide Quellen
      gemeinsam haben. Der groesste gemeinsame Nenner ist die richtige Wahl,
      nicht der feinste Wert einer einzelnen Quelle.
  - id: api-data-ist-nicht-data-json
    kind: falle
    title: "`GET /api/data` liefert eine gefilterte Teilmenge — wer den BESTAND messen will, liest die Datei"
    match: "api/data eintraege|medienliste unvollstaendig|bestand zu klein|data.json unterschied|weniger eintraege als erwartet|medienliste gefiltert"
    sev: med
    tags: ["medien", "api", "bestand", "messung", "plugins", "mixpi-similar", "2026"]
    source: >
      06.09.2026, an Box .78 gemessen beim Ausrollen von
      plugins/mixpi-similar: derselbe Bestand ueber die Route und ueber die
      Datei abgefragt.
    symptom: >
      Eine Auswertung des Medienbestands (Abdeckungsquote, Interpretenliste,
      Wachlisten-Vorschlag) kommt auf plausible, aber zu kleine Zahlen. Nichts
      wirkt kaputt — die Liste ist ja da, die Eintraege stimmen, es sind nur
      weniger, als der Betreiber im Regal sieht.
    cause: >
      DIE ROUTE IST NICHT DIE DATEI. Am selben Geraet, im selben Moment:

          server/config/data.json    144 Eintraege, 36 artist-Felder
          GET /api/data               44 Eintraege, 11 artist-Felder
          server/config/active_data.json  144 Eintraege

      Zwei Drittel fehlen in der Antwort. Die Route liefert, was gerade
      spielbar ist; die Datei fuehrt den Bestand.

      DAS IST BESONDERS HEIMTUECKISCH, WEIL DIE ROUTE DER BEQUEMERE WEG IST:
      sie ist von aussen abfragbar, braucht keinen Pfad und keinen
      SSH-Zugang. Wer eine Themenkarte, eine Abdeckungsquote oder eine
      Wachliste dagegen misst, misst die halbe Wahrheit und merkt es nicht —
      die Zahl sieht aus wie eine Zahl.

      UND `active_data.json` IST HIER NICHT DER UNTERSCHIED. Sie war im
      Messmoment gleich gross wie `data.json`; der Unterschied zwischen den
      beiden Dateien (server.ts:9141) ist ein anderer Fall (Offline-Betrieb)
      und erklaert diese Luecke nicht.
    fix: >
      WER DEN BESTAND MEINT, LIEST `server/config/data.json`. Fuer ein Plugin
      ist das ohnehin der einzige Weg — `kontext.holen` verwehrt die eigene
      Box, die Route ist gar nicht erreichbar. Der Pfad laesst sich aus dem
      eigenen Datenordner ableiten, weil beide am selben `configBasePath`
      haengen:

          dataFile     = `${configBasePath}/data.json`      (server.ts:792)
          datenWurzel  = `${configBasePath}/plugin-daten`   (server.ts:2600)

      also `join(datenOrdner, '..', '..', 'data.json')`.

      WER DIE ROUTE BENUTZT, SCHREIBT DAZU, DASS ES DIE ROUTE WAR. Eine
      Messung „66 Eintraege im Bestand" ist etwas anderes als „66 Eintraege in
      der Antwort von /api/data", und der Unterschied faellt drei Wochen
      spaeter jemand anderem auf die Fuesse.
    lesson: >
      Vor jeder Auswertung ueber „den Bestand" die Gegenfrage stellen: messe
      ich die Daten oder eine Sicht darauf? Zwei Wege zu derselben Sache sind
      selten dieselbe Sache — hier trennt sie ein Filter, der nirgends
      angeschrieben steht. Verwandt:
      [[zwei-wachen-eine-datei-verschiedene-antwort]] (dieselbe Datei, zwei
      Antworten) und [[einmal-hinsehen-ist-keine-messung]].
  - id: wlan-rettung-am-geraet-belegt-durch-ortswechsel
    kind: note
    title: 'Die WLAN-Rettung ist am Geraet belegt — ein Ortswechsel hat den Zweig ausgeloest, den man nicht gefahrlos provozieren kann'
    tags: [wlan, riegel, rettung, e131, messung, 2026]
    source: >
      06.09.2026, 13:28, Box unter 192.168.178.78. Wahl `extern`,
      `ifup@wlan0` inactive (vom Riegel uebersprungen), `ifup@wlan1` active,
      Adapter-Dienst mit Status 0 — und TROTZDEM traegt wlan0 die
      Verbindung: 192.168.178.78 an `ganznahamnetz_5G`. wlan1 steht auf
      `wpa_state=SCANNING`, weil seine wpa-Datei nur die Netze des alten
      Standorts kennt (`ganzschnellimnetz2_2G/_5G`).
    related: [ifup-riegel-execcondition-statt-execstartpre]
    body: |
      WARUM DAS EIN GLUECKSFALL IST: Der Rettungszweig feuert genau dann,
      wenn die GEWAEHLTE Karte nicht traegt — und um ihn absichtlich
      auszuloesen, muesste man die tragende Verbindung kaputtmachen, also
      genau den Fall bauen, in dem ein Fehler die Box aussperrt. Deshalb
      stand er bis heute nur mit Attrappen im Test
      (tools/mixpi-wlan-adapter.test.sh). Ein Ortswechsel der Box hat ihn
      umsonst geliefert: der Stick fand sein Netz nicht mehr, die Rettung
      hat wlan0 per direktem `ifup` hochgefahren, und die Box war ueber
      ssh erreichbar, statt still im Regal zu stehen.

      DER ZUSTAND IST DIE SIGNATUR — daran erkennt man es wieder:

          Wahl:        extern          (also soll wlan1 tragen)
          ifup@wlan0:  inactive        (der Riegel HAT verriegelt)
          wlan0:       UP mit Adresse  (die Rettung hat sie doch geholt)
          wlan1:       DOWN, SCANNING  (findet sein Netz nicht)

      `ifup@wlan0 inactive` UND `wlan0 UP` schliessen sich scheinbar aus —
      genau das ist der Fingerabdruck der Rettung: sie ruft `ifup` direkt,
      nicht ueber die Unit. Wer das sieht, hat keinen Fehler vor sich,
      sondern die Sicherheitsregel bei der Arbeit.

      WAS DARAUS FOLGT: Die Rettung darf niemals stillschweigend
      geschehen — sonst haelt man die Box fuer gesund, waehrend die
      GEWAEHLTE Karte tot ist (hier: der bessere Empfaenger, 14 dB, laeuft
      seit dem Ortswechsel ins Leere). Sie protokolliert deshalb; wer
      Verbindungsfragen untersucht, liest ZUERST `ip -br addr` gegen die
      Wahl-Datei.
  - id: http-200-ist-keine-wirkung
    kind: falle
    title: 'HTTP 200 heisst „jemand hat zugehoert" — nicht „es ist etwas passiert" (der playpause-Fall)'
    tags: [messung, abspieldienst, fernbedienung, api, 2026]
    source: >
      06.09.2026 beim Anschliessen der Fernbedienung (E134). Die Taste
      Wiedergabe/Pause tat nichts, obwohl jede Probe gruen war:
      `curl -o /dev/null -w '%{http_code}' /player/current/playpause` gab 200 —
      genau wie play, pause, stop, next und previous. Der Abspieldienst kennt
      `playpause` aber gar nicht; im ganzen spotify-control.ts kommt das Wort
      nicht ein einziges Mal vor.
    related: [einmal-hinsehen-ist-keine-messung]
    body: |
      DIE FALLE: Ein unbekannter Pfad faellt bei dieser Bauart nicht auf 404,
      sondern wird angenommen und verworfen. Die Antwort sagt also nur, dass
      der Dienst LEBT — ueber die Wirkung sagt sie nichts. Wer mit
      Statuscodes misst, misst in so einem Fall den Briefkasten und nicht den
      Brief.

      WORAN MAN ES FRUEHER GEMERKT HAETTE: Die Oberflaeche macht es seit jeher
      anders. Sie schickt `play` ODER `pause`, je nachdem was laeuft
      (`spielerBefehl` in app.js) — nie einen Umschaltbefehl. Das war der
      Hinweis, und er stand die ganze Zeit da: WER SCHON EINEN WEG HAT, DEN
      MAN NACHBAUEN WILL, SOLLTE IHN ZUERST LESEN, statt eine plausible
      Adresse zu erfinden. Ein Umschaltbefehl ist eine naheliegende Annahme —
      und genau deshalb eine.

      DIE MESSUNG, DIE ES GEZEIGT HAETTE: Zustand vorher lesen, Befehl
      schicken, Zustand nachher lesen. Nur diese drei Schritte zusammen
      beantworten „hat es gewirkt". Genau so wurde es dann gefunden.

      DIE REGEL DARAUS: Fuer jeden Befehl an einen fremden Dienst gilt — der
      Beweis ist die ZUSTANDSAENDERUNG, nicht der Statuscode. Und bevor man
      einen Endpunkt errät: nachsehen, welchen der eigene Hauptnutzer ruft.
  - id: rueckfallbild-macht-den-fehler-unsichtbar
    kind: falle
    title: 'Ein Rueckfall, der GUELTIG aussieht, loescht den Fehler aus jeder Messung (der Cover-Platzhalter-Fall)'
    tags: [messung, cover, bilder, box, m3u, rueckfall, 2026]
    source: >
      06.09.2026, Box .81. Der Betreiber meldete zum ZWEITEN Mal fehlende
      Titelbilder („Conni backt", „101 Wichtel"). Beim ersten Mal (BACKLOG
      E135/1) hatte ich die Kette gemessen und fuer heil erklaert: 302 auf
      /cover/…, dort HTTP 200, image/jpeg, 58289 Byte. Genau diese 58289 Byte
      WAREN der Fehler — es ist www/neu/bilder/mixpi-spielt.png, das
      Maskottchen der Box, das m3u_generator.sh unter dem Namen cover.jpg
      ablegt, wenn im Medienordner kein Bild liegt (Zeile 161/162).
      Nachgemessen mit tools/cover-platzhalter-probe.py: 60 von 97 Alben
      tragen dieses Bild.
    related: [http-200-ist-keine-wirkung, spa-antwortet-immer-200, einmal-hinsehen-ist-keine-messung]
    body: |
      DIE FALLE: Ein Rueckfall behebt das SYMPTOM, das man sieht, und zerstoert
      dabei das MERKMAL, an dem man den Fehler noch erkennen konnte. Vorher
      (bis 20.08.2026) zeigte der Rueckfall ins Leere, `cp` scheiterte still,
      und die Kachel blieb ohne Bild — haesslich, aber ehrlich: `hatBild` war
      falsch, /api/bild antwortete mit 404, jede Probe schlug an. Seit der
      Rueckfall auf ein Bild zeigt, das es WIRKLICH gibt, ist die Datei echt,
      der Inhaltstyp stimmt, der Status ist 200 und `hatBild` ist true.
      Der Fehler ist nicht behoben, sondern unsichtbar geworden.

      WARUM DIE UEBLICHEN PRUEFUNGEN ALLE VERSAGEN: Status, Inhaltstyp und
      Groesse sind an so einer Datei EINWANDFREI. `tools/bilder-wirklich-da.py`
      (15.08.) faengt die Stufe davor — „200, aber text/html". Hier ist es
      200 und image/jpeg. Auch eine Mindestgroesse hilft nicht: 58 KB liegen
      weit ueber jeder Schwelle. Es gibt genau EIN Merkmal, das noch traegt:
      der INHALT, verglichen gegen das Rueckfallbild selbst.

      DIE REGEL DARAUS: Wer einen Rueckfall einbaut, muss ihn ZAEHLBAR machen
      — im selben Zug. Sonst tauscht man einen sichtbaren Fehler gegen einen
      stummen, und der naechste, der misst, meldet die Kette als heil (ich,
      am selben Tag, im selben Baum). Ein Platzhalter braucht eine Probe, die
      ihn erkennt, oder eine Kennzeichnung an der Datei; „sieht aus wie ein
      Bild" ist kein Zustand, den ein System ueber sich selbst verlieren darf.

      DIE MESSUNG: tools/cover-platzhalter-probe.py --box dietpi@… [--liste]
      vergleicht jedes ausgelieferte Album-Cover gegen die Pruefsumme des
      Rueckfallbildes. Bilanz statt Kachel-Raten.

      NEBENBEI ZWEIMAL IN DIESELBE GRUBE GEFALLEN, beide beim Zaehlen:
      `ls "$d"/*.jpg "$d"/*.jpeg` schlaegt fehl, sobald EIN Muster nicht
      trifft — die Bilanz meldete daraufhin „alle ohne Bild", und die
      Kreuztabelle wurde sinnlos. Und `find media -mindepth 2` trifft die
      INTERPRETEN-Ebene, nicht die Album-Ebene: unter media steht zuerst die
      Kategorie. Beide Male sah das Ergebnis plausibel aus und war falsch;
      aufgefallen ist es nur, weil die Zahlen sich widersprachen (27 Alben mit
      echtem Cover, angeblich keines mit Bilddatei).
  - id: feature-abend-endet-am-geraet-nicht-am-ausrollweg
    kind: falle
    title: 'Ein Feature gilt als fertig, wenn es am Geraet laeuft — aber die Box war von Hand bestueckt (Kritiker-Lauf 07.09.)'
    tags: [audit, ausrollweg, fernbedienung, wache, pegel, e138, 2026]
    source: >
      Kritiker-Lauf 07.09.2026 (AUDIT-2026-09-07.md): vier unabhaengige
      Pruefer plus eigene Nachmessung am Baum. Nichts am Code geaendert.
    related: [wiederkehrender-befund-ist-ablauffehler, kommentar-und-kompilat-sind-keine-gegenstelle, klick-sturm-laedt-nichts-mehr]
    body: |
      DAS MUSTER DES TAGES: Der Controller-Abend vom 06.09. hat vier Dinge
      hinterlassen, die alle am Geraet funktionieren und auf jeder FRISCH
      geschriebenen Karte tot waeren — weil die Entwicklungsbox alles von
      Hand bekommen hat:

      1. config/services/mixpi-fernbedienung.service:31 startet
         /usr/local/bin/mupibox/fernbedienung.py — beide Ausrollwege legen
         scripts/box/* aber nach /opt/mupibox-tools/ (autosetup.sh:640,
         start_mupibox_update.sh:800). Frische Karte: 203/EXEC in der
         StartLimit-Schleife. tools/ausrollweg-deckung.py hat das NICHT
         gemeldet — sie prueft ExecStart-Pfade nicht gegen die Ausrollwege.
      2. config/fernbedienungen/ (Geraeteprofile) kopiert KEIN Weg auf die
         Box; server.ts:18212 faellt aufs cwd zurueck, das dort
         ~/.mupibox/Sonos-Kids-Controller-master ist. /api/eingabegeraete
         liefert auf jeder ausgerollten Box leere Listen.
      3. Die zwei neuen Wachen (fernbedienung-bild-deckung.py,
         cover-platzhalter-probe.py) haengen in keinem Laeufer UND rutschen
         durch die Blindstelle von tools/ungerufene-wachen.py: die matcht
         nur woertliches sys.exit(1), beide nutzen sys.exit(main()).
      4. Die drei SVG-Schemata unter config/fernbedienungen/bilder/ hat
         ausser der (ungerufenen) Wache nie jemand gelesen.

      E138 NEBENBEI GEKLAERT (Einbau gehoert dem laufenden Vorgang): Die
      Welle laeuft bei Pause weiter, weil ZWEI Fuetterer existieren und nur
      einer ein Tor hat. wellenAusZustandFuettern() nullt korrekt (Messung
      a1216264 entlastete den Zustand) — aber pegelStrom.onmessage
      (app.js:25495) schreibt alle 100 ms ungeprueft drueber. Und die
      Quelle friert ein: pegel.ts ueberschreibt `aktuell` nur bei vollem
      100-ms-Fenster; liefert pw-record nichts mehr (PipeWire suspendiert
      den Monitor bei Pause), reicht lesen() das letzte Fenster ewig
      weiter — der Kopfkommentar verspricht [0,0,0,0] bei Pause, der
      Vertrag ist am Emitter gebrochen. Fix-Skizze: Zeitstempel je
      Fenster, lesen() liefert Nullen ab ~300 ms Alter. Drei
      Frontend-Anlaeufe scheiterten, weil sie am falschen Ende bauten.

      DIE REGEL: Im selben Commit, der einen Dienst, eine Config-Familie
      oder eine Wache anlegt, den Ausrollweg UND den Laeufer mitziehen —
      und die Meta-Wachen so erweitern, dass sie das Fehlen melden
      (ausrollweg-deckung um ExecStart, ungerufene-wachen um das
      sys.exit(main())-Idiom). Eine frisch geschriebene SD-Karte ist die
      einzige ehrliche Gegenprobe.

      AUSSERDEM GEMESSEN: Tastenbelegung lebt dreifach und ist schon
      auseinandergelaufen (Profil-vorbelegung 96/103/108 widerspricht dem
      Template; null Leser fuer vorbelegung); Aktions-Ids dreifach ohne
      Wache (box-aktionen.ts / fernbedienung.py / app.js anweisungHolen —
      an genau dieser Naht lag lauter/leiser schon monatelang tot);
      /proc/bus/input/devices-Parser doppelt (Python+TS, der
      server.ts-Kommentar :18200 verbietet woertlich, was er dann tut).
      Stand von draussen: DietPi v10.6 traegt Kernel-CVE-Fixes; Raspberry
      Pi OS hat passwortloses sudo abgeschaltet; ERTM-Falle beim
      Xbox-Pairing gehoert in die Doku; python-evdev v2.0.0 waere da,
      der Stdlib-Weg bleibt trotzdem richtig (Fremdpaket-Regel).
  - id: qa-robustheitslauf-2026-09-07
    kind: note
    title: 'QA-Robustheitslauf 07.09.: das Kinderzeit-Fenster ueber Mitternacht sperrt IMMER — und die Verwaltung glaubt der Antwortform'
    tags: [qa, robustheit, kinderzeit, verwaltung, neue-oberflaeche, eltern-tor, wache, 2026]
    source: >
      QA-Sitzung 07.09.2026: Box-Oberflaeche gegen tools/neu-vorschau.mjs
      (--port 8399), Verwaltung gegen tools/admin-vorschau.mjs (8398),
      Serverfragen am Code beantwortet (server.ts, kinderzeit.ts).
      Klick-Sturm Saat 20260907 und admin-doppeltipp-probe als Regression
      gefahren, beide gruen.
    related: [klick-sturm-laedt-nichts-mehr, attrappe-luegt-durch-weglassen, dauerrote-wache-ist-keine]
    body: |
      DER FUND DES TAGES (Funktionsfehler, immer reproduzierbar): Ein
      Kinderzeit-Fenster mit ab NACH bis — 22:00 bis 06:00 — sperrt das
      Kind zu JEDER Uhrzeit. `pruefen()` in kinderzeit.ts fragt
      nacheinander `nun < ab` (zuFrueh) und `nun >= bis` (zuSpaet); bei
      ab > bis deckt eines von beiden jede Minute des Tages. Weder die
      Profile-Seite der Verwaltung noch PUT /api/kinderzeit sagen dazu ein
      Wort — der Autosave nimmt das Fenster wortlos an, und fuer die
      Eltern sieht es aus wie „Box kaputt". Entweder Ueber-Mitternacht
      rechnen oder ab<bis erzwingen; heute passiert stillschweigend das
      Schlimmste von beidem.

      ZWEITER FUND (Verwaltung, Fehlerklasse „200 mit falscher Form"):
      Die System-Seite warf am Stub zwei TypeErrors in die Konsole.
      (a) `this.lage()?.aktionen.filter(...)` — das `?.` schuetzt nur vor
      lage==null; fehlt einer 200-Antwort das Feld `aktionen`, wirft
      `.filter` auf undefined, und weil es in einem computed() steht, bei
      JEDER Change Detection (dieselbe Bauart wie Weg 1 in
      [[klick-sturm-laedt-nichts-mehr]]). (b) `schrauben.set(a.schrauben)`
      uebernimmt undefined, danach wirft `schrauben().length` im Template.
      mupihat.ts traegt dasselbe Muster (`diagnose()?.revisionen.find`).
      Eintritt: Stub, Versionsversatz Verwaltung/Server, Fehlerseite mit
      200. NEUE WACHE: tools/verwaltung-formabweichung-schau.py (im
      Laeufer doku-luecken-probe.sh; kennt die gemeldeten Stellen, faellt
      bei neuen um UND bei veralteten Ausnahmen — [[dauerrote-wache-ist-keine]]).
      Verwandtes Restrisiko dokumentiert: kinderzeit.ts (Verwaltung)
      macht aus `stand.profile ?? []` bei einer 200-Antwort OHNE Feld eine
      leere Liste und wuerde sie PUTten — die Frisch-GET-Kette schuetzt
      vor Ladefehlern, nicht vor Formabweichung.

      DRITTER BEFUND — BEKANNT UND WEITERHIN OFFEN: der Eltern-Tor-
      TypeError vom 06.08. (torMalen liest aufgabe.a bei aufgabe null).
      Fenster jetzt praezise: Initial stehen modus 'rechnen' und aufgabe
      null; auf() zeigt das Tor SOFORT („wird geprueft …"), das
      Ziffernfeld ist nicht verdeckt — jeder Tipp vor der /api/config-
      Antwort laeuft durch torTaste in torMalen und wirft. Selbstheilend
      nach der Antwort; auf einer Box mit startendem Backend ist das
      Fenster Sekunden lang.

      VIERTER BEFUND (frischer Code vom 06.09.): der Fernbedienungs-Poll
      laeuft als rohes setInterval(150 ms) — nicht ueber sichtbarerTakt,
      also auch bei versteckter Seite weiter (der sichtbarerTakt-Kommentar
      nennt genau das „Rechenzeit und Waerme fuer nichts"), und ohne
      Ueberlappungsschutz: haengt der Server, stapeln sich alle 150 ms
      neue fristlose fetches in den 6er-Pool. Und die ATTRAPPE kennt
      /api/fernbedienung/anweisung gar nicht — jede Vorschau-Messung
      traegt seit dem 06.09. ein 404-Dauerrauschen (~6,7/s), Fall 8 fuer
      [[attrappe-luegt-durch-weglassen]].

      KLEINVIEH, gemessen: Bildschirmtastatur nimmt 63 Zeichen an (PSK-
      Grenze gilt fuer ALLE Aufrufer), der Profilname wird danach still
      auf 40 gekappt (app.js wie Server) — 23 Tipps verschwinden wortlos;
      das Namensfeld der Verwaltung hat gar keine Client-Grenze, der
      Server kappt kommentarlos. „Timer starten" (Schlummer) ist nicht
      entprellt: drei schnelle Klicks = drei POSTs (konvergiert, Server
      killt den Vorgaenger — harmlos, aber Muster). Klick-Sturm Saat
      20260907: 6 Runden gruen, dabei EIN Reload durch wahllos
      getroffenen Profilwechsel — Profile liegen VOR dem Eltern-Tor
      (Design, kein Fehler; wers anders will, braucht ein Tor davor).

      ENTLASTET (nicht nochmal verdaechtigen): Server ganz weg -> neue
      Oberflaeche wirft NICHTS, meldet „Das hat gerade nicht geklappt.",
      Takte laufen weiter, Erholung strukturell sicher (werkeHolen ohne
      haengendes Flag). /api/werke-500 laesst die alte Liste stehen
      (Absicht). Config unlesbar -> Tor „aus" ist DOKUMENTIERTE Absicht
      vom 06.08. PUT /api/profile traegt den nenntListe-Riegel; 4x
      Pause-Tipp = 1 Befehl (In-Flight-Sperre); PIN-Weg prueft 4-8 und
      „laeuft die Box danach noch"; kaputtes JSON faengt der zentrale
      Fehlerbehandler (am echten Prozess gemessen, 07.08.). Suchfelder
      der Verwaltung sind regex-sicher (kein new RegExp auf Eingaben).

      MESSAUFBAU-LEHRE fuer die naechste Sitzung: Im Browser-Pane-Tab
      ohne sichtbare Pane ist visibilityState 'hidden' — sichtbarerTakt
      startet NIE (werkeHolen laeuft nicht, „Medien werden geladen …"
      bleibt stehen) und Timer sind gedrosselt (lange await-Ketten
      reissen das 30-s-Limit des javascript_tool). Der Hebel: visibility
      per defineProperty auf 'visible' faelschen und visibilitychange
      dispatchen — hidden->visible stoesst jede sichtbarerTakt-Arbeit
      SOFORT einmal an. Fuer Takt- und Sturmmessungen stattdessen die
      tools/ mit eigenem Chrome nehmen (leihgabe.mjs).
  - id: audit-2026-09-08-kritiker
    kind: howto
    title: "Kritiker-Lauf 08.09.2026 — der Update-Weg loescht einen versprochenen Dienst, die Akku-Reihe hat den Zeitsprung-Schutz nie bekommen, abgeloeste Wege sterben nicht sondern werden still"
    match: "mupi_change_checker|uhrGesprungen|akkuverlauf|zeitreihe|wlanHinterlegen|/api/netzwerk/wlan|ton/start|tonlogin|5588|playerMode|physicalDevice|pwUmgebung|tonUmgebung|XDG_RUNTIME_DIR 1000|m3u_generator dreifach|X-Emby-Token|MediaBrowser Token|kurzGemerkt|profilPflicht|AUDIT-2026-09-08"
    tags: [audit, toter-code, doppelung, schnittstellen, ausrollen, 2026]
    related: ["audit-2026-08-28-kritiker", "vierzehn-kopien-und-die-abweichung-ist-der-fehler", "doppelung-lassen-auseinanderlaufen-messen", "zwei-ausrollwege-eine-handgefuehrte-liste", "dauerrote-wache-ist-keine"]
    body: |
      Quelle: AUDIT-2026-09-08.md (vier Pruefer plus eigene Nachmessung;
      alle tragenden Behauptungen selbst am Baum gemessen). Rueckstand:
      vom 07.09. ist praktisch alles offen, server.ts (22.377) und app.js
      (32.559) stehen EXAKT auf dem Vortagesstand - der Lauf ging deshalb
      mit frischen Blickwinkeln ueber den Bestand, nicht ueber die
      Tagesdifferenz.

      RANG 1: update/start_mupibox_update.sh:1565-1567 stoppt, disabled
      und LOESCHT mupi_change_checker.service ersatzlos - autosetup
      installiert die Unit NIE (grep leer), aber dienste.ts:538/763
      bietet den Dienst der Verwaltung als "Neue Musik bemerken" an
      (samt Zustandsaussage "Auf .169 eingeschaltet" ohne Datum) und
      dienst-rueckweg-probe.mjs:80 erwartet ihn active. Nicht "vergessen
      auszurollen" wie 07.09. Rang 1, sondern aktiv entfernt und die
      Versprechen stehen gelassen.

      RANG 2 (die Lehre aus [[vierzehn-kopien-und-die-abweichung-ist-der-fehler]]
      in Reinform): anhaengen/bereinigen/spanne/ausduennen sind in
      akkuverlauf.ts und systemverlauf.ts ZEICHENGLEICH - aber
      uhrGesprungen() existiert nur in systemverlauf und wird nur fuer
      die System-Reihe gerufen (server.ts:5239, einziger Aufruf,
      nachgemessen). Die am Geraet gemessene Diagnose (Uhr springt beim
      NTP-Abgleich zurueck, Reihe lehnt bis 45 min jeden Punkt ab) gilt
      wortgleich fuer die Akku-Reihe, nur ohne Fix. Wo Kopien gleich
      sind, wandert die Sicherung nicht von selbst mit.

      DAS MUSTER DES TAGES (Nachlauf zu "Helfer ohne Einzugsgebiet" und
      "Feature-Abend ohne Ausrollweg"): DER ABGELOESTE WEG STIRBT NICHT,
      ER WIRD STILL. PKCE-Ton-Login samt Horcher :5588 tot neben dem
      Geraetecode-Weg (~250 Z.), playerMode und physicalDevice tote
      Schluessel nach dem Fall der alten Oberflaeche (conf_update.sh:61
      schreibt physicalDevice bei JEDEM Update, Bedingung immer wahr),
      /api/netzwerk/wlan schreibt Klartext-Passwoerter fuer einen
      abgeschalteten Dienst und der Admin-Client wlanHinterlegen wird
      allein von der eigenen Spec getragen, der ng-serve-Weg zeigt an
      vier Stellen auf das gefallene src/frontend-box. Gegenmassnahme:
      im Commit, der einen Weg abloest, den alten faellen oder datiert
      als abgeloest kennzeichnen; Wache fuer Vorlagen-Schluessel und
      Antwortfelder ohne Leser.

      WEITERE BELEGTE FUNDE: remote-step-installer/tools/ als dritter
      Spiegel mit vier byte-gleichen Python-Kopien und bereits
      divergenter boot-splash-Unit (ExecStart ein Verzeichnis daneben,
      203/EXEC-Klasse) -> Wache mit harter Paarliste, siehe
      [[doppelung-lassen-auseinanderlaufen-messen]]. XDG_RUNTIME_DIR
      12x in zwei Meinungen (8x hart 1000, 4x getuid) - unter fremder
      UID geht der halbe Ton. pegel.ts baut pwUmgebung nach, obwohl
      cue-schalter.ts sie im SELBEN Paket exportiert (die
      Paketgrenzen-Begruendung im Kommentar traegt dort nicht).
      m3u_generator.sh: 49 Zeilen dreimal byte-gleich. Profil-Pruefung
      fuenffach, eine Stelle antwortet 404 statt 400 (server.ts:14960).
      kurzGemerkt hat EINEN Nutzer neben ~14 Selbstbau-Zwischenspeichern
      ohne In-Flight-Sperre. Verlaufs-Rechenlogik app.js/frontend-admin
      divergiert bereits (minutenWort "0 min" vs "—"). Stand von
      draussen: Jellyfin kuendigt X-Emby-Token ab (drei Sendestellen),
      backend-player buendelt Express 4 mit offenen Advisories, Node 22
      nur noch Maintenance, Piper-Nachfolger laengst installiert aber
      undokumentiert.

      ENTLASTET (nicht neu verdaechtigen): 763 Exporte im backend-api
      alle mit externem Leser; parsers.ts sieht nur wegen
      import-require-Sonderform tot aus (mplayer-wrapper.ts:13); keine
      tote Shell-Funktion im Baum; app.js hat ausser den Kurvengeruesten
      KEINE Selbstkopie >=15 Zeilen; das Quellen-Interface existiert und
      wird genutzt (plugin-vertrag.ts, ard.ts auf 74 Z. geschrumpft);
      ein gemeinsames Shell-Konfigmodul bleibt aus Ausfallsicherheit
      ABGELEHNT.
  - id: audit-2026-09-09-kritiker
    kind: howto
    title: "Kritiker-Lauf 09.09.2026 — der Fall der alten Oberflaeche wirkt nach: Wachen degradieren leise per existsSync, die README erzaehlt Gefallenes als lebend, Angular 20 hat eine Frist"
    match: "existsSync Quellenliste|pruef-neu-regeln|newdesign-mitlieferung|ALTPLAN|routen-rufer-deckung|Angular 20 LTS|karma-to-vitest|telepot|riegel-sperrt-betreiber-aus|herkunftsriegel-probe|wischBelegen|wischWort|wischRaenderVon|bilder/figuren|Dot-Werkzeuge|biomejs.*\\*|wachen_lib|AUDIT-2026-09-09"
    tags: [audit, toter-code, wachen, doku-drift, abhaengigkeiten, 2026]
    related: ["audit-2026-09-08-kritiker", "wache-auf-sorte-nicht-auf-pfad", "wiederkehrender-befund-ist-ablauffehler", "ungerufene-wache-driftet-in-den-fehlalarm", "dauerrote-wache-ist-keine"]
    body: |
      Quelle: AUDIT-2026-09-09.md (vier Pruefer: Werkstatt-Inventur,
      Doku-Drift, Routen-Inventur, Stand von draussen; alle tragenden
      Behauptungen selbst am Baum nachgemessen). Rueckstand: NULL
      Commits seit dem Kritiker-Commit 86c8a49c, server.ts (22.377)
      und app.js (32.559) unveraendert - alle 16 Raenge von gestern
      stehen offen. Der Lauf ging deshalb mit vier NEUEN Blickwinkeln
      ueber den Bestand.

      RANG 1 (die gefaehrlichste Sorte gruen, wortgleich
      [[wache-auf-sorte-nicht-auf-pfad]]): pruef-neu-regeln.js:905-913
      filtert seine Quellenliste mit fs.existsSync - 4 der 6 Quellen
      fielen mit E118/1e, die Freigabe-Namen-Wache lief seither in
      jedem pruefen.sh-Lauf gruen und prueft still nur noch NewDesign.
      newdesign-mitlieferung-deckung.py:75 haelt einen toten
      ALTPLAN-Anker (src/frontend-box/angular.json), das
      rotprobe-Register nennt 4 Gegenproben auf gefallene Dateien.
      LEHRE: Wachen duerfen auf fehlende Anker nie mit Ueberspringen
      antworten, nur mit Befund.

      RANG 2 (einziger terminierter Druck): Angular 20 faellt am
      28.11.2026 aus dem LTS (installiert 20.3.30, aktuell stabil 22).
      Im selben Sprung Karma->Vitest per offiziellem Schematic
      (ng generate @angular/core:karma-to-vitest) - die
      CHROME_BIN-Kruecke der Admin-Tests entfaellt mit.

      RANG 3 ([[wiederkehrender-befund-ist-ablauffehler]] in Zahlen):
      die 17 toten Routen wurden am 23.08., 31.08., 06.09. und heute
      unabhaengig IDENTISCH gemessen - keine fuenfte Meldung, sondern
      routen-rufer-deckung.py als Ratsche in den Laeufer. Beifang der
      Inventur: KEIN Klienten-Ruf ohne Route (168 geprueft), und
      /api/werke/:s/download wird per String-Konkatenation gerufen -
      wer nur Literale sucht, meldet falsche Tote.

      DAS MUSTER DES TAGES (Spiegelseite von gestern): WO WIRKLICH
      GELOESCHT WURDE, HAT NIEMAND DIE ZITATE EINGESAMMELT. README.md
      widerspricht sich selbst (Z. 53 geloescht vs. Z. 64 noch da),
      empfiehlt serve:frontend-box, verweist fuer Schrift-Lizenzen auf
      das gefallene themes/; drei Kommentare begruenden lebenden Code
      mit toten Nachbarn (server.ts:21565 media-cache als einziger
      Verbraucher; app.js:23429 figuren LEER vs. 79 Dateien;
      app.js:27277 wischWort-Drei-Orte-Behauptung - die
      Wisch-Maschinerie lebt nur noch vom generierten Test-Auszug).
      GEGENMASSNAHME: zu jedem Faellungs-Commit gehoert ein
      Zitate-Lauf (grep nach dem gefallenen Namen ueber Baum UND
      Doku).

      WEITERE BELEGTE FUNDE: telepot (tot seit ~2019, Repo archiviert)
      wird per pip --break-system-packages auf jede frische Box
      installiert (autosetup:224, 6 Skripte, mupi_telegram.service) -
      Migration auf python-telegram-bot (apt in Trixie). Das
      Herkunftsriegel-Wachenpaar (riegel-sperrt-betreiber-aus.mjs 1465
      Z. + herkunftsriegel-probe.mjs) haengt an NICHTS - die teuerste
      Fehlerrichtung des Dauerbestands-Riegels ungeprueft; 33 von 91
      --pruefen-Modi laeuferlos, ungerufene-wachen.py sieht nur 7 von
      ~25 urteilenden Unreferenzierten ([[ungerufene-wache-driftet-in-den-fehlalarm]]).
      13 Meshy-Beinahe-Doppel stehen als waehlbare Figuren im
      Kinder-Angebot (bilder/figuren, der Ordner entscheidet). 7 von
      14 media/images-Bildern und screenshots/ komplett ohne Verweis
      im Baum (vor dem Faellen upstream pruefen). Sieben versteckte
      Dot-Werkzeuge in tools/ getrackt, fuer Shell-Globs unsichtbar.
      biome haengt an "*" (package.json:42), Schema-URL auf 2.2.4.
      frontend-box-Riss: kein package.json in Git, aber Lockfile und
      Wurzel-Skripte fuehren den Workspace weiter - fremde laufende
      Arbeit, nicht angefasst.

      KEIN FUND (gegengeprueft): app.js-Binnenbestand sauber (639
      Definitionen, 5 ohne Rufer, 2 davon mit datierter Begruendung);
      documentation/ vs dokumentation/ keine Dopplung; RPi.GPIO
      bewusst ueber rpi-lgpio; esbuild/TS aktuell genug; pruefen.sh
      nennt 4 tote Werkzeuge nur in HIER-STAND-Kommentaren.
  - id: der-stille-zweitweg-verliert-die-beilage
    kind: falle
    title: 'Zwei Wege zum selben Ergebnis - und der stille verliert die Beilage (der Nachschnitt-Cover-Fall)'
    tags: [mitschnitt, cover, nachschnitt, messung, 2026]
    source: >
      07.09.2026, E135/1c. 42 von 69 aufgenommenen Alben zeigten das
      Maskottchen statt eines Covers. Kreuztabelle am Geraet (je erste FLAC
      per ffprobe gegen die Ordner-cover.jpg): 18x Embed+JPG, 9x nur JPG,
      42x nichts - und KEIN Fall "Embed ohne JPG". Die 42 haeufen sich zu
      Nachtstunden; am 30.08. liegen zwischen einem vollen und einem leeren
      Album sieben Minuten.
    related: [rueckfallbild-macht-den-fehler-unsichtbar, einmal-hinsehen-ist-keine-messung]
    body: |
      DIE LAGE: Der Mitschnitt hat zwei Wege. Der LIVE-Weg liest den
      Spielerzustand, holt album.images, bettet das Bild in die FLAC ein und
      schreibt die Ordner-cover.jpg. Der NACHSCHNITT (Leerlauf-Arbeiter,
      meist nachts) baut die Aufnahme aus dem VORMERK-EINTRAG - und der
      speichert uri, titel, interpret, album, nummer. Kein bild. Das Feld
      war beim Vormerken VERFUEGBAR und wurde schlicht nicht mitgenommen.

      DIE FALLE, allgemein: Wer einen zweiten Weg zum selben Ergebnis baut
      (Wiederholung, Warteschlange, Import), uebertraegt die PFLICHTFELDER
      und vergisst die BEILAGEN - Bild, Songtext, Herkunft. Der zweite Weg
      laeuft dann jahrelang "erfolgreich": die Datei entsteht, die Kachel
      erscheint, kein Fehler faellt. Nur die Beilage fehlt, und zwar genau
      bei den Stuecken, die ueber den stillen Weg kamen - ein Muster, das
      wie Zufall aussieht (gleiche Tage, gemischte Ergebnisse), bis man die
      Uhrzeiten ansieht.

      WIE MAN ES FINDET: Nicht am einzelnen Fall raten, sondern die
      KREUZTABELLE ueber den ganzen Bestand ziehen (hier: eingebettetes
      Bild x Ordnerdatei). Eine leere Zelle ist ein Beweis: "Embed ohne
      JPG = 0" hiess, der Kachel-Weg versagt nie allein - also lag es vor
      beiden, beim Aufnehmen selbst. Danach die Zeitachse: Nachtstunden
      verrieten den Leerlauf-Arbeiter.

      DIE REGEL: Beim Bau eines Zweitwegs die Feldliste des Erstwegs
      DANEBENLEGEN und jedes Feld einzeln entscheiden - uebernehmen oder
      begruendet weglassen. "Was der Eintrag halt hat" ist keine
      Entscheidung, sondern die Falle.
  - id: tote-adapterwahl-vergiftet-jede-bt-sitzung
    kind: signature
    title: 'Bluetooth-Verbinden uebers Box-Menue meldet sofort "not available" - die Adapterwahl zeigt auf einen Stick, den es nicht mehr gibt'
    sev: high
    match: 'Controller [0-9A-F:]{17} not available|Bluetooth-Adapterwahl .* ist nicht vorhanden'
    cause: '/etc/mupibox/bt-adapter nennt eine Adapter-MAC, die kein hci mehr hat (Stick getauscht/gezogen); das select-Echo wurde als Befehlsergebnis gelesen und brach pair/connect nach 400 ms ab'
    fix: 'seit 09.09.2026 filtert btctl() die Fehlzeile (ohneSelectFehlzeile) und warnt im Journal; Wahl in der Verwaltung neu setzen oder /etc/mupibox/bt-adapter beiseitelegen'
    tags: [bluetooth, adapter, box, menue, falle, 2026]
    source: >
      09.09.2026, MixPiBox .62, Betreiber: "mixpi bluetooth verbindung klappt
      nicht ueber das menue der box". Am Geraet belegt: Wahl-Datei las
      08:BF:B8:56:CE:44, bluetoothctl list kannte nur 88:A2:9E:48:F6:50
      (eingebaut, default) und B0:82:E2:1F:9C:E5 (USB). Gegenprobe: connect
      OHNE select lief 6 s bis br-connection-page-timeout (Lautsprecher aus),
      ueber den Server-Weg kam die Absage sofort. Fix ausgeliefert und beide
      Messungen wiederholt.
    related: [mupi-bluetooth-skripte-kaputt, bt-skripte-lagen-nie-im-baum, neustart-deckt-auf]
    body: |
      SYMPTOM: Jedes Koppeln/Verbinden im Bluetooth-Fach des Eltern-Bereichs
      scheitert SOFORT mit "not available" ("Hat nicht geklappt. Ist das
      Geraet eingeschaltet?") - waehrend Geraeteliste und Suche voellig
      normal aussehen. Dazu meldet GET /api/bluetooth als adapter eine MAC,
      die kein hciconfig kennt. Wer nur die Liste ansieht, sieht nichts.

      DIE URSACHE IST EINE EINZIGE ZEILE. /etc/mupibox/bt-adapter (die
      Adapterwahl der Verwaltung, hier gesetzt am 15.08.) zeigte auf den
      USB-Stick 08:BF:B8:56:CE:44. Am 20.08. wechselte der Stick - der
      ASUS-Dongle heisst seither B0:82:E2:1F:9C:E5, die Datei blieb.
      btctl() beginnt jede bluetoothctl-Sitzung mit `select <wahl>`, und
      BlueZ antwortet:

          Controller 08:BF:B8:56:CE:44 not available

      Diese Zeile vergiftete DREI Auswertungen zugleich:
        1. ergebnis() las "not available" als Ausgang des eigentlichen
           Befehls - das Menue bekam die Absage, obwohl der Versuch auf
           dem Standard-Adapter laengst lief.
        2. Die Abbruch-Erkennung in btctl() (fertigWorte) sah das Wort in
           den ersten Daten und schloss die Sitzung 400 ms spaeter - der
           echte Verbindungsaufbau wurde abgewuergt.
        3. parseShow() nahm die Zeile als Controller-Kopf: /api/bluetooth
           meldete die tote MAC als Adapter (Name und Zustand kamen vom
           echten Default darunter - eine Chimaere).

      SEIT DEM SELBEN TAG GEFIXT: ohneSelectFehlzeile() in bluetooth.ts
      tilgt genau die Controller-Zeile der GEWAEHLTEN MAC; ein
      "Device ... not available" (Geraet aus/weg) bleibt stehen. btctl()
      filtert an beiden Auswertungsstellen und warnt EINMAL je Serverlauf
      im Journal ("Adapterwahl ... ist nicht vorhanden"). Die Wache dazu:
      bluetooth.spec.ts, Gruppe "ohneSelectFehlzeile" (Sabotage-Probe rot
      gesehen). Auf der Box liegt die alte Wahl beiseite
      (bt-adapter.tot-20260909); die Lautsprecher-Kopplungen (Xiaomi, Sony)
      wohnen ohnehin am eingebauten 88:A2..., der Default ist. Die
      E134-Fernbedienungen sind am USB-Stick gebondet und von allem
      unberuehrt - eingehende Verbindungen brauchen kein select.

      MERKE: Eine Adapterwahl NACH MAC ueberlebt den Stick nicht. Wer den
      Dongle tauscht (oder er sich selbst - siehe
      [[neustart-deckt-auf]]), muss die Wahl in der Verwaltung neu setzen;
      seit dem Fix sagt es das Journal, statt leise zu degradieren. Und:
      "not available" hat ZWEI Absender - Controller (Wahl tot, sofort)
      und Device (Geraet aus, nach Sekunden). Die Wartezeit unterscheidet
      sie schneller als jedes Log.

      RANDNOTIZ, MITGEMESSEN: Die pa_context_connect-refused-Flut im
      Journal (19:53-20:11 Uhr) gehoerte zum ALTEN laufenden Prozessstand
      (Boot 19:19) und ist seit dem Neustart auf den aktuellen Build weg -
      /api/ton/ausgaenge liefert 4 Ausgaenge bei 0 Fehlern. Nicht weiter
      verfolgt.
  - id: ausrollweg-und-laeufer-zum-feature-abend-nachgezogen
    kind: note
    title: 'Die vier Punkte des Feature-Abends sind abgetragen - und was der Geraetebeweis wirklich beweist'
    tags: [ausrollweg, fernbedienung, wache, rezept, 2026]
    source: >
      09.09.2026, beim Aufraeumen vor dem Push. Ausgangspunkt war der
      Kritiker-Befund vom 07.09. (feature-abend-endet-am-geraet-nicht-am-
      ausrollweg): vier Dinge liefen am Geraet und waeren auf jeder frischen
      Karte tot gewesen.
    related: [feature-abend-endet-am-geraet-nicht-am-ausrollweg, rueckfallbild-macht-den-fehler-unsichtbar, der-stille-zweitweg-verliert-die-beilage]
    body: |
      STAND 09.09.2026 (Zustandsaussage - nachmessen, nicht glauben):

      1. mixpi-fernbedienung.service zeigt jetzt auf
         /opt/mupibox-tools/fernbedienung.py - den Ort, an den beide
         Ausrollwege scripts/box/* wirklich legen. Der alte ExecStart
         (/usr/local/bin/mupibox/) stammte von der von Hand bestueckten
         Entwicklungsbox; eine frische Karte starb dort mit 203/EXEC.
      2. config/fernbedienungen/ (Profile + SVG-Schemata) rollen autosetup.sh
         und der Update-Weg jetzt nach /etc/mupibox/fernbedienungen aus -
         ueberschreibend (cp -rf), denn es sind AUSGELIEFERTE Daten; nur die
         Zuordnung fernbedienung.json bleibt Betreiber-Arbeit (cp -n).
      3. fernbedienung-bild-deckung.py haengt in tools/doku-luecken-probe.sh;
         cover-platzhalter-probe.py haengt in tools/pruefen.sh --box - dort
         ERWARTET ROT, bis die E135/1c-Schuld (42 Maskottchen-Alben)
         nachgefuellt ist. Das Rot ist die gemessene Schuld, kein Rauschen.
      4. Die SVG-Schemata haben seit E137/1 einen echten Leser
         (/api/eingabegeraete/profil/:id/schema), nicht nur die Wache.

      DIE LEHRE HINTER DER WIEDERHOLUNG: "Am Geraet verifiziert" beweist nur
      den HANDBESTAND dieser einen Box. E137/1 wurde am 07.09. mit curl am
      Geraet bewiesen - und die Dateien, die die Routen lasen, hatte eine
      Sitzung von Hand hingelegt. Der Beweis war echt und die Aussage
      trotzdem zu breit. Deshalb gehoert zur Geraeteverifikation die
      REZEPTFRAGE in denselben Zug: "Woher hat die Box das - und bekommt es
      eine frische Karte auf demselben Weg?" Wer sie erst beim Aufraeumen
      stellt, findet die Luecke wie hier zwei Tage spaeter; wer sie nie
      stellt, findet sie beim naechsten Kartenschaden.
  - id: memory-spielarten-wechseln-immer
    kind: entscheidung
    title: 'Memory E139: Zu zweit wechselt IMMER - und das Vollbild verdoppelt die Paare statt sie zu waehlen'
    tags: [newdesign, apps, memory, spielarten, ui, 2026]
    source: >
      E139, 09.09.2026. Betreiber woertlich: "im 2 player wechselt es immer
      und zaehlt jeweils die punkte" und "einen full screen mode mit mehr
      karten". In der Vorschau (Port 8319) belegt: Treffer gibt Blau den
      Punkt und Rot den Zug; Verrueckt tauschte 7 von 11 verdeckten Karten,
      die offene blieb; Ohne Fehler deckte alles zu bei unveraenderten
      Positionen.
    related: [platzmachen-gilt-nur-fuer-das-kachelraster, ein-rueckweg-fuer-alle-ebenen, pi5-hat-16k-seiten]
    body: |
      DIE STILLSTE REGEL DER SPIELART "ZU ZWEIT": Es wechselt nach JEDEM
      Zug, auch nach einem Treffer. Das ist die BESTELLUNG ("wechselt es
      immer"), nicht die Turnierregel "wer trifft, ist nochmal dran" - die
      belohnt das bessere Gedaechtnis doppelt, und an diesem Geraet sitzen
      Geschwister mit Altersabstand. Wer die Turnierregel "berichtigt",
      baut ein Brett, das genauso richtig aussieht und das Bestellte nicht
      tut. Die Entscheidung steht deshalb als eigene Funktion
      (memNaechsterDran in NewDesign/apps.js) im Auszug und im Pruefstand
      (tools/pruef-neu-regeln.js) - der Rufer kennt keinen
      Treffer-Parameter, die Turnierregel ist dort also nicht einmal
      baubar.

      DIE VIER SPIELARTEN SIND EIN SCHIRM, KEIN BAUM: Alleine, Zu zweit,
      Verrueckt, Ohne Fehler als vier app-antwort-Knoepfe (240x66), jeder
      ueber sprichDann vorgelesen. Erst Spielerzahl, dann Modus waeren zwei
      Fragen vor dem Spielen gewesen.

      VERRUECKT MISCHT NUR DIE ZUGEDECKTEN Karten (offene und gefundene
      bleiben), und die getauschten wackeln (mem-wirbel, nur transform -
      Rueckseiten sind gleich, ein unsichtbares Mischen saehe aus, als
      luege das Gedaechtnis des Kindes). Die Uhr faellt von 30 s je Runde
      um 5 s, nie unter 10 s: darunter mischte es schneller, als ein Zug
      samt Zeigezeit (900 ms) dauert - das Spiel waere nicht schwer,
      sondern unmoeglich, und am Schirm sieht beides gleich aus.

      OHNE FEHLER deckt bei einem Fehlpaar ALLES wieder zu, mischt aber
      nicht ("mischen aber nicht") - das Wissen, wo alles liegt, bleibt
      der Trost. Die Zuege zaehlen weiter.

      DAS VOLLBILD HEISST JETZT app-voll, NICHT MEHR mal-voll: Die
      Body-Klasse benennt seit E139 die SORTE (eine App liegt ueber allem),
      nicht die erste App, die sie wollte. Sie haengt an DREI Orten:
      app.css (Regeln), apps.js (Malen UND Memory schalten sie),
      appBuehneZu in app.js (raeumt sie beim Verlassen ab). Wer eine
      weitere App gross macht, nimmt dieselbe Klasse - eine zweite waere
      die vergessene Stelle von morgen. Im Vollbild verdoppelt Memory die
      Paare (MEM_VOLL_PAARE 12 = 24 Karten); der Pruefstand rechnet die
      KNAPPSTE Buehne nach (688x320: 800x480 minus Rueckweg-Spalte 96,
      Polster, Knopfzeile ~50, Uhr ~20) und verlangt Karte >= Griff 66 px.
      Mehr Paare gingen rechnerisch - verdoppeln ist die eine Stufe, die
      ein Kind als "jetzt ist es gross" versteht.

      DER UMSCHALTER BAUT NEU STATT UMZUMISCHEN: Ob Vollbild ist, sagt der
      Baum (body.app-voll), und der Knopf leert die Buehne und baut das
      Spiel in derselben Art neu auf - mehr Karten in ein laufendes Brett
      zu mischen waere ein anderes Spiel, kein anderer Blick darauf.
  - id: kartensaetze-kuratieren-heisst-hinsehen
    kind: falle
    title: 'CC0 beschafft ist nicht kindgerecht kuratiert — was ein Stichwortfilter garantiert durchlaesst'
    tags: [memory, bilder, cc0, lizenz, kuratieren, newdesign, apps, 2026]
    source: >
      E140, 09.09.2026. 64 CC0-Bilder aus Cleveland Museum of Art und
      Metropolitan Museum geholt und auf Kontaktboegen angesehen; 11 davon
      wieder verworfen, darunter zwei wegen Nacktheit. Zwei weitere Funde
      kamen vom Betreiber am selben Bogen.
    related: [rueckfallbild-macht-den-fehler-unsichtbar, memory-spielarten-wechseln-immer, wache-auf-sorte-nicht-auf-pfad]
    body: |
      DREI SORTEN FEHLER STECKEN IN EINEM FRISCH BESCHAFFTEN BILDERSATZ, und
      nur die erste laesst sich programmieren.

      1. LIZENZ — programmierbar, und zwar JE OBJEKT. Beide Haeuser sagen es
         selbst (Met `isPublicDomain`, Cleveland `share_license_status`), und
         beide fuehren geschuetzte Leihgaben zwischen den freien Stuecken.
         "Die Sammlung ist CC0" ist deshalb falsch; die Pruefung gehoert vor
         den Download und nicht danach.

      2. KINDGERECHT — NICHT programmierbar. Gefallen sind „Turtle Baby"
         (Edith Barretto Parsons: ein nacktes Kleinkind auf einer
         Schildkroete) und die Bronze „Dancer". Beide sind harmlos gemeint,
         beide tragen einen voellig unverdaechtigen Titel, und ein Filter auf
         Stichworte haette beide durchgelassen — es gibt kein Wort, an dem
         man sie erkennt. Nur Hinsehen findet sie. Museen fuehren Akt, Krieg
         und Grabbeigaben; das ist kein Ausrutscher, sondern ihr Bestand.

      3. ERKENNBAR IM KLEINEN — auch nicht programmierbar, und der Posten mit
         den meisten Ausfaellen. Der erste Tier-Satz bestand aus Museums-
         SKULPTUREN: ein Entenkopf-Beschlag als „Ente", eine Tierkreisfigur
         mit Menschenkoerper als „Hase", eine abstrakte Saeule als „Vogel".
         Fuer einen Erwachsenen sind das Tiere, WEIL DER TITEL ES SAGT; auf
         einer 105-px-Karte sieht ein Vierjaehriger graue Klumpen. Ersetzt
         durch farbige BILDER (Drucke, Malerei). Die Lehre in einem Satz:
         Fuer ein Kind zaehlt, was das Bild ZEIGT, nicht was das Objekt IST.

      DAS WERKZEUG DAFUER IST EIN KONTAKTBOGEN, nicht eine Liste:
      `tools/memory-bilder-holen.py --kontaktbogen` legt alle Bilder eines
      Satzes als EIN PNG nebeneinander. In einem Blick sieht man, was
      64 Einzelabrufe nicht zeigen — dass zwei Motive sich aehneln (von vier
      Blumenstillleben blieb eines), dass eines im Kleinen zu Brei wird, dass
      eines nicht hierher gehoert. Der Betreiber hat am selben Bogen zwei
      Stuecke gefunden, die ich uebersehen hatte: eine Mondaufnahme mit
      aufgedrucktem Bildunterschrift-Streifen und eine flach eingravierte
      Sonnenuhr. Ein zweites Paar Augen auf demselben Bogen ist billig und
      faengt etwas.

      DIE ZWEITE HAELFTE DERSELBEN ARBEIT: die EIGENEN Cover. Sie lassen sich
      nicht kuratieren, weil sie dem Betreiber gehoeren — dort siebt statt
      dessen ein 8x8-Mittelwerthash (`memBildHash` in NewDesign/apps.js) nach
      INHALT aus. Er erwischt beide gemeldeten Faelle mit einem Handgriff:
      zwei Titel desselben Albums (gleiches Cover, verschiedene Adresse) und
      die 60 Platzhalter-Alben aus
      [[rueckfallbild-macht-den-fehler-unsichtbar]] — und er muss dafuer
      NICHT wissen, welches Bild der Platzhalter ist. Eine Wache auf die
      Sorte, nicht auf einen Pfad ([[wache-auf-sorte-nicht-auf-pfad]]).

      AN DER ATTRAPPE GEMESSEN (09.09.2026, Vorschau auf 8319): acht
      verschiedene Cover-ADRESSEN, genau EIN Bildinhalt. Das ist der Realfall
      im Kleinen — und der Beleg, dass die alte Pruefung auf die Adresse an
      ihm vorbeilaufen MUSSTE.

      WENN DIE AUSSIEBUNG DANN ALLES WEGNIMMT, MUSS DIE MELDUNG DAS SAGEN.
      „Zu wenige Bilder" waere gelogen — es sind genug, sie sehen nur gleich
      aus. Der Unterschied ist fuer den Erwachsenen, der es liest, der ganze
      Hinweis: das eine heisst „lege Musik auf die Box", das andere „deinen
      Alben fehlen die Cover".
  - id: audit-2026-09-10-kritiker
    kind: howto
    title: "Kritiker-Lauf 10.09.2026 — der Feature-Abend liefert an die Werkstatt statt an die Box, der BT-Fix ist halb entstoert, und Met schaltet v1/search am 01.10. ab"
    match: "mixpi-paketfrische|deploy.zip veraltet|btOhneSelectFehlzeile|ohneSteuerzeichen 7148|memory-bilder-holen|v1.1/search|herkunft.json Stummel|MEM_VOLL_PAARE|memWortAusDatei|eingabegeraete/profile Rufer|nachtmodusGilt Import|gespieltSichern atomar|verfuegbarkeitSichern|profilSchreiben Frist|backlog-status-schau Kopfzeile|gross-griff-figur|Node 22 EOL|AUDIT-2026-09-10"
    tags: [audit, ausrollen, bluetooth, memory, plugins, doku-drift, 2026]
    related: ["audit-2026-09-09-kritiker", "wiederkehrender-befund-ist-ablauffehler", "dauerrote-wache-ist-keine", "zwei-ausrollwege-eine-handgefuehrte-liste", "rueckfallbild-macht-den-fehler-unsichtbar"]
    body: |
      Quelle: AUDIT-2026-09-10.md (vier Pruefer: Tagesdifferenz E137-E140,
      Schnittstellen der drei Codekoerper und plugins/, Doku/Backlog-Deckung,
      Stand von draussen; tragende Behauptungen selbst nachgemessen).
      Acht Commits seit gestern - nach zwei unbewegten Tagen echte
      Tagesarbeit: E137/1, E135/1c-Diagnose, BT-Adapterwahl-Fix,
      Feature-Abend-Ausrollweg, E139/E140 Memory.

      RANG 1, DIE KROENUNG DES TAGES: E139/E140 erreichen KEINEN
      Ausrollweg. deploy.zip steht auf 98e44bdc, apps.js darin 44 statt
      73 kB, kein bilder/memory/ im Paket - beide Wege packen genau
      dieses Zip. Die Wache mixpi-paketfrische-pruefen.py war ROT und
      haengt in pruefen.sh; niemand hat sie nach dem Feature-Abend
      gefragt. Wiederholung von 09-07 Rang 1 in verschaerfter Form,
      also [[wiederkehrender-befund-ist-ablauffehler]]: die Naht ist
      "ein Feature-Abend endet mit pruefen.sh", nicht der naechste
      Kritiker.

      RANG 2: Der BT-Fix 98e44bdc filtert am Sitzungsende richtig
      (server.ts:7100 ohneSteuerzeichen VOR btOhneSelectFehlzeile),
      im STROM-Pfad aber nicht - :7148 prueft fertigWorte auf rohen
      stdout-Stuecken. Eine ANSI-Sequenz zwischen Controller und MAC,
      und die 400-ms-Abbrucherkennung feuert wieder aufs select-Echo.
      Die Spec (68 pass) prueft die richtige Sache, beruehrt diese
      Zeile aber nicht.

      RANG 3, DIE EINZIGE FRIST: Met schaltet /v1/search am 01.10.2026
      ab (angekuendigt 04.09.); memory-bilder-holen.py:265 ruft ihn.
      Dazu der Stummel-Bug: der Ueberspring-Pfad (:187) schreibt
      e.get("_herkunft", Stummel), und _herkunft setzt NIEMAND - alle
      53 Bilder in allen sechs herkunft.json tragen nur datei/id/quelle,
      der Kommentar verspricht das Gegenteil. Ein Erzeuger, dessen
      Wiederholungs-Pfad den Rueckfall statt des Bestands schreibt,
      hoehlt seine eigene Dokumentation bei jedem Lauf weiter aus.

      MEMORY-QUALITAET (Rang 4): MEM_VOLL_PAARE=12 gegen Satzgroessen
      12/10/10/8/7/6 - bei Wissenschaft aendert der Vollbild-Knopf
      NICHTS. Und das vorgelesene Kartenwort kommt aus dem ASCII-
      Dateinamen (memWortAusDatei: "Taenzerin mantel", "Maske bwoom") -
      die Zielgruppe liest nicht, die Sprachausgabe ist der einzige
      Kanal, und NIRGENDS gibt es ein Feld, in dem ein Mensch das Wort
      setzen koennte. Die Formwache im Pruefstand zementiert den
      Defekt: wer den Umlaut richtig macht, macht sie rot.

      SCHNITTSTELLEN-ERNTE (Praefer B, erstmals systematisch):
      server.ts:592 importiert nachtmodusGilt aus dem Mitschnitt-Plugin
      ueber die Ausrollgrenze - auf der Box zwei Fassungen (Buendel +
      nachgezogenes Plugin), nichts misst sie -
      [[zwei-ausrollwege-eine-handgefuehrte-liste]] als Import-Variante.
      Zwei rohe
      writeFileSync im 2-s-Sammelfenster (gespielt :16353,
      verfuegbarkeit :20657) neben der fertigen jsonAtomarSchreiben-
      Naht. In app.js: 28/55 fetch ohne Frist, 32/41 Schreibstellen
      ohne 401-Zweig, die Profil-Familie (9 zeichennahe Stellen) ohne
      Frist und 5/9 stumm - Kind-Umschalten schreibt still aufs alte
      Profil. Plugins: archive/ardsounds/jellyfin rufen .json() am
      Pruefstand vorbei (der faelscht absichtlich nur text()),
      lastfm/musicbrainz je ein fehlendes Netz, librespot/soloist
      unterscheiden sich in EINEM Zeichen unsymmetrisch.

      KORREKTUR AN 09-08 R15: die 9 Backlog-Zellen "ausserhalb der
      Konventionen" sind ALLE Fehlalarme - backlog-status-schau liest
      Tabellen an der Spaltenzahl statt an der Kopfzeile und haelt
      Prosa-Spalten fuer Status. Echt kaputt sind nur BACKLOG.md:2427
      und :2433 (ungeschuetztes Pipe). Eine Wache, die falsch misst
      und in keinem Laeufer haengt, erzeugt Befunde, die Audits
      weiterreichen - dieselbe Sorte gruen wie
      [[dauerrote-wache-ist-keine]], nur als Dauer-Fehlalarm.

      AUSDRUECKLICH SAUBER: die Tagesarbeit riss keine neue Doku-Luecke
      (18 statt 20, bei einer Pruefung mehr), die Feature-Abend-
      Ausrollwege sind symmetrisch (die Zwei-Listen-Falle vermieden),
      E139/E140 handwerklich gut (memNaechsterDran treffer-blind
      festgenagelt, saetze.json aus dem Ordner erzeugt, Lizenzwache je
      Objekt), BT-Spec prueft die Sache statt der Form, BlueZ-Parsing
      bleibt (Trixie friert 5.82 ein, node-bluez waere Rueckschritt).
      Rueckstands-Bilanz: 2 von 41 Raengen der letzten drei Audits
      umgesetzt - genau die zwei, fuer die der Feature-Abend antrat.

      DIE URSACHE HINTER DEN FUNDEN: Erzeugnisse driften, weil ihr
      Erzeuger nicht zum Abschluss gehoert - deploy.zip zwei Commits
      alt, herkunft.json-Stummel, Standdatum bleibt beim nachgezogenen
      Pack-Zaehler stehen, Rang-Buchungen kennen keinen Vollzug. Was
      ein Commit noetig macht, gehoert in den Commit.
  - id: der-fehler-zerstoert-die-quelle-die-der-fix-braucht
    kind: falle
    title: 'Der Fixplan setzte eine Quelle voraus, die der Fehler selbst zerstoert hatte'
    tags: [mitschnitt, cover, tags, plan, messung, e135, 2026]
    source: >
      10.09.2026, E135/1c Schritt 3. Der drei Tage alte Fixplan stand
      woertlich auf "fuer die 42 steht die Spotify-uri in den FLAC-Tags".
      Am Geraet (.62) nachgemessen, bevor das Werkzeug gebaut wurde: 68
      Alben ohne cover.jpg, davon EINES mit uri im Tag, 67 ganz ohne Tags.
    related: [der-stille-zweitweg-verliert-die-beilage, einmal-hinsehen-ist-keine-messung, rueckfallbild-macht-den-fehler-unsichtbar]
    body: |
      DIE LAGE: Der Leerlauf-Nachschnitt legte Aufnahmen ohne Cover ab. Der
      Fixplan sah drei Schritte vor; der dritte — den Bestand nachfuellen —
      wollte die Spotify-Adresse aus den FLAC-Tags der betroffenen Dateien
      lesen. Klang zwingend: die Adresse steht ja im comment-Tag, der
      Live-Weg schreibt sie seit jeher.

      DER DENKFEHLER: Genau die Dateien, denen das Cover fehlt, sind die,
      die der defekte Weg erzeugt hat — und der schrieb GAR KEINE Tags.
      Eine Ursache hatte zwei Symptome (kein Bild, keine Tags), und der Fix
      fuers erste Symptom setzte die Abwesenheit des zweiten nicht ein,
      sondern dessen Anwesenheit voraus. Der Plan war also in dem
      Augenblick falsch, in dem er geschrieben wurde — er beschrieb den
      HEILEN Bestand und wandte ihn auf den kaputten an.

      DIE REGEL: Wer einen Bestand reparieren will, darf als Datenquelle
      nichts benutzen, das derselbe Fehler beruehrt haben koennte. Vor dem
      Bau des Reparaturwerkzeugs EINMAL zaehlen, wie viele der kaputten
      Faelle die vorausgesetzte Quelle ueberhaupt tragen. Hier: eine
      SSH-Zeile, zwei Minuten — gegen ein Werkzeug, das anschliessend bei
      67 von 68 Faellen schweigend nichts gefunden haette.

      WAS STATTDESSEN TRUG: die Vormerkliste des Plugins selbst
      (plugin-daten/mixpi-mitschnitt/liste.json) — 303 Eintraege, ALLE mit
      uri, 102 Alben. Sie entsteht VOR der Aufnahme und ist vom Fehler
      unberuehrt. Die brauchbare Quelle lag also vor der defekten Stelle,
      nicht dahinter; das ist der Ort, an dem man bei dieser Art Schaden
      zuerst suchen sollte.

      NEBENBEI, ZUR MESSUNG SELBST: Die erste Zaehlung meldete 880 Ordner
      ohne Cover und 0 mit FLAC — beides falsch. `for d in $(find ...)`
      zerfaellt an Leerzeichen in Pfaden, und Albumnamen sind voller
      Leerzeichen. Aufgefallen ist es nur, weil die naechste Zahl groesser
      war als ihre Grundmenge (874 von 112). ZWEI ZAHLEN, DIE ZUEINANDER
      NICHT PASSEN KOENNEN, sind der billigste Selbsttest einer Messung —
      eine einzelne Zahl haette man geglaubt.
  - id: das-reparaturwerkzeug-lief-in-denselben-fehler
    kind: falle
    title: 'Das Werkzeug gegen den Rueckfall fragte selbst nur, OB die Datei da ist'
    tags: [cover, werkzeug, messung, pruefsumme, e135, 2026]
    source: >
      10.09.2026, E135/1c Schritt 3, beim Nachfuellen der fehlenden Cover
      auf Box .62. Nach einem Lauf, der 65 von 65 Cover meldete, standen
      immer noch drei Alben mit Maskottchen da.
    related: [rueckfallbild-macht-den-fehler-unsichtbar, der-fehler-zerstoert-die-quelle-die-der-fix-braucht, sonde-ohne-erreichbarkeitsriegel-urteilt-ueber-nichts]
    body: |
      ZWEI FEHLER IM SELBEN WERKZEUG, beide von der Sorte "meldet Erfolg".

      1. EXISTENZ STATT INHALT. Das Werkzeug suchte Alben ohne Cover mit
         `[ -f "$d/cover.jpg" ]` — und uebersprang damit genau die Ordner,
         in denen `m3u_generator.sh` das MASKOTTCHEN unter diesem Namen
         abgelegt hatte. Also derselbe Fehler, gegen den das Werkzeug
         gebaut war, im Werkzeug selbst. Die Schwesterprobe
         (cover-platzhalter-probe.py) machte es von Anfang an richtig und
         verglich Pruefsummen; beim Bauen des Nachfuellers wurde die
         schnellere Frage gestellt statt der richtigen.

      2. EIN FREMDER RUECKLAUF ALS URTEIL. Die Pruefung lief als
         `while IFS= read -r d; do … md5sum … done` ueber alle Ordner. Eine
         Shell-Schleife traegt den Status ihres LETZTEN Befehls, und
         `md5sum` scheitert an jedem Ordner ohne Bild. Der Code sagte
         `if rc != 0: return []` — und meldete daraufhin "kein Album ohne
         cover.jpg", waehrend drei ohne dastanden. EINE LEERE LISTE UND
         "NICHTS ZU TUN" SEHEN GLEICH AUS.

      DIE REGEL FUER SAMMELLAEUFE: Der Rueckgabewert eines Sammelbefehls
      ueber viele Gegenstaende sagt nichts ueber die Vollstaendigkeit
      seiner Ausgabe. Statt ihm zu glauben: ZAEHLEN, wie viele Antworten
      ankamen, und die Zahl gegen die Zahl der Gegenstaende halten. Fehlt
      etwas, laut sagen — eine unvollstaendige Bilanz, die sich als
      vollstaendig ausgibt, ist schlimmer als ein Abbruch.

      NEBENBEFUND, der zum Aufraeumen gehoert: Von den drei Resten hatten
      ZWEI ueberhaupt keinen Ton mehr im Ordner, nur eine `playlist.m3u`.
      Ein fehlendes Cover ist dort kein Fehler, sondern die Spur einer
      geloeschten Aufnahme. Wer Reste zaehlt, sollte pruefen, ob der
      Gegenstand ueberhaupt noch existiert, bevor er ihn repariert.
  - id: audit-2026-09-11-kritiker
    kind: howto
    title: 'Kritiker-Lauf 11.09.2026 — die Zusage im Kommentar ersetzt die Messung'
    tags: [audit, cover, mitschnitt, spotify, wache, 2026]
    source: >
      Kritiker-Lauf 11.09.2026 (AUDIT-2026-09-11.md), vier unabhaengige
      Pruefer plus eigene Nachmessung am Baum. Tagesdifferenz: nur E135/1c
      (Nachschnitt-Veredelung + Cover-Nachfueller).
    related: [dauerrote-wache-ist-keine, wiederkehrender-befund-ist-ablauffehler, audit-2026-09-10-kritiker, das-reparaturwerkzeug-lief-in-denselben-fehler]
    body: |
      ELF RAENGE, Details in AUDIT-2026-09-11.md. Das verbindende Muster:
      an VIER Stellen der Tagesarbeit steht eine ZUSAGE im Text, die keine
      Messung deckt.

      1. pruefen.sh:1145 sagt "SIE IST NICHT MEHR DAUERROT" — aber die
         cover-platzhalter-probe liefert bei jedem Platzhalter Exit 1, und
         drei erklaerte Reste stehen noch. Der Schritt bleibt rot; ein
         NEUER Befund erhoeht nur 3 auf 4 und sieht identisch aus. Dazu am
         11.09. gemessen: bei unerreichbarer Box meldet die Probe
         "kein Rueckfallbild gefunden" (rc 2) — ein Netzausfall verkleidet
         sich als Sachbefund.
      2. Der Docstring des Nachfuellers verspricht "erst daneben, dann
         umbenennen" — drei Zeilen tiefer laeuft `mv` von /tmp (tmpfs)
         ueber die Dateisystemgrenze und schreibt das Ziel stueckweise.
      3. Der Tag MIXPI_VOLLSTAENDIG sagt "100%" — gerechnet hat es niemand;
         der Live-Weg misst den Anteil, der Nachschnitt behauptet ihn.
      4. BACKLOG sagt "damit ist auch 1b erledigt" — drei Absaetze tiefer
         steht 1b unveraendert offen.

      DIE REGEL: Eine Zusage, die eine Wache sein will, gehoert ALS Wache
      formuliert — Ausnahmeliste in der Probe statt Prosa daneben, tmp im
      Zielordner statt Docstring-Beteuerung, Anteil rechnen statt 100%
      schreiben, EINE Backlog-Zeile statt zwei. Keine der vier Zusagen war
      gelogen; sie waren beim Schreiben plausibel und wurden nie gegen das
      Verhalten geprueft.

      GEWICHTIGSTER EINZELFUND daneben: Wettlauf um `.cover.jpg` — der
      Bildpfad ist je Ordner fest, bis zu drei Nachschnitte desselben
      Albums laufen parallel, und `rm` nach dem rename zieht dem
      Nachbarjob das Bild weg. Der am 10.09. behobene Fehler (Aufnahme
      ohne Bild) kehrt so sporadisch als Wettlauf zurueck.

      VON DRAUSSEN: Spotify hat am 06.02.2026 den Development Mode
      umgebaut — fuer neue Client-IDs sind artists/top-tracks,
      artists/albums, playlists/tracks, browse/* gestrichen und Search auf
      10 gedeckelt; fuer Bestandsapps "postponed" OHNE Datum.
      tracks/{id} und album.images bleiben — das Cover-Werkzeug steht auf
      sicheren Endpunkten. Kein Umbau jetzt; eine Inventar-Wache soll die
      gerufenen Pfade gegen die Streichliste halten, damit der Fristtag
      kein Suchtag wird.

      RUECKSTAND: 1 von 16 Raengen des 10.09. teilweise umgesetzt (R15a),
      14 offen; die Paketfrische war den dritten Lauf in Folge rot —
      KEIN neuer Rang dafuer, die Naht steht als 09-10 R1 (ein
      Feature-Abend endet mit pruefen.sh).
  - id: ein-eingefrorener-wert-braucht-ein-alter
    kind: falle
    title: 'Drei Anlaeufe am Empfaenger, weil der Sender einen alten Wert ewig weiterreichte (E138)'
    tags: [pegel, wellen, player, vertrag, e138, 2026]
    source: >
      10.09.2026, E138 behoben. Gemeldet vom Betreiber am 07.09.
      ("laeuft bei stopp"); drei Frontend-Anlaeufe davor scheiterten, die
      Messung a1216264 hatte den Zustand schon entlastet.
    related: [einmal-hinsehen-ist-keine-messung, rueckfallbild-macht-den-fehler-unsichtbar]
    body: |
      DIE LAGE: Die Pegel-Quelle (src/backend-player/src/pegel.ts) fuellt ein
      100-ms-Fenster und rechnet daraus vier Bandwerte. Ueberschrieben wird
      `aktuell` NUR, wenn ein Fenster voll ist. Bleibt der Zulauf aus — und
      PipeWire suspendiert den Monitor, sobald nichts mehr spielt —, liefert
      `lesen()` denselben Wert weiter, unbegrenzt lange. Die Welle im Player
      lief bei gestoppter Wiedergabe also weiter, mit den Pegeln des letzten
      gehoerten Augenblicks.

      WARUM DREI ANLAEUFE IM FRONTEND SCHEITERN MUSSTEN: Sie versuchten, den
      falschen Wert zu ERKENNEN (laeuft gerade etwas? steht `ton-laeuft`?)
      statt ihn gar nicht erst zu bekommen. Jeder Versuch machte die Welle
      entweder ganz aus oder gar nicht aus, weil der Empfaenger die
      Information nicht hatte, die nur der Sender kennt: ob der Wert eben
      gemessen wurde oder eine Erinnerung ist.

      DIE REGEL: Wer einen zuletzt gemessenen Wert herausgibt, gibt sein
      ALTER mit heraus — oder verfallen laesst. Ein `letzterWert`, der keinen
      Zeitstempel traegt, ist eine Zusage, die der Sender nicht halten kann:
      "das ist jetzt so". Beim Sender kostet es drei Zeilen (Zeitstempel beim
      Schreiben, Frist beim Lesen), beim Empfaenger ist es gar nicht loesbar.

      WORAN MAN ES IM VORAUS ERKENNT: Der Kopfkommentar der Datei versprach
      bereits "[0,0,0,0] bei Pause". Wo eine Zusage in der Prosa steht, die
      im Code an keiner Stelle durchgesetzt wird, ist sie eine Absicht, kein
      Vertrag — und das ist die Stelle, an der man zuerst nachsieht, bevor
      man am anderen Ende zu raten beginnt.

      NICHT EINEBNEN: "kein Abgriff moeglich" (undefined, das Feld faellt aus
      der Antwort) und "gerade still" (Nullen) bleiben verschieden. Der
      Verfall darf das erste nicht zum zweiten machen — sonst zeigte eine Box
      ohne pw-record eine stehende Nullwelle statt gar keiner. Ein eigener
      Zeuge haelt genau das fest.
  - id: warnung-ohne-urteil-ist-dekoration
    kind: falle
    title: 'Die Wache warnte „nur 113 von 114" — und meldete darunter „nichts zu tun"'
    tags: [wachen, werkzeuge, cover, shell, 2026]
    source: >
      11.09.2026 beim Aufraeumen gefunden: tools/mixpi-cover-nachfuellen.py
      meldete gruen, waehrend genau der verlorene Ordner der eine ohne Cover
      war ("Der Glitzerweg").
    related: [gegenprobe-statt-gruen-glauben, mupi-journal-leer-ist-keine-antwort]
    body: |
      DIE LAGE: Das Werkzeug holte 114 Album-Ordner, schickte sie als Heredoc
      zurueck an die Box und liess dort eine `while IFS= read -r d`-Schleife
      je Ordner ein `md5sum` in einer Befehlssubstitution rufen. Zurueck kamen
      113 Antworten. Eine Pruefung dafuer gab es sogar — sie druckte brav
      "WARNUNG: nur 113 von 114 Ordnern beantwortet". Die Zeile DARUNTER sagte
      trotzdem "Kein Album ohne cover.jpg — nichts nachzufuellen", und der
      Lauf endete mit 0.

      DIE REGEL: Eine Warnung, die den AUSGANG nicht aendert, ist Dekoration.
      Gelesen wird das Urteil, nicht der Absatz davor — und bei einem
      Werkzeug, das man alle paar Wochen ruft, ist der Absatz davor nach dem
      zweiten Mal unsichtbar. Wer feststellt, dass er unvollstaendig gemessen
      hat, muss WERFEN, nicht drucken: der Unterschied zwischen "gemessen,
      nichts gefunden" und "nicht zu Ende gemessen" ist genau der, an dem
      Wachen gruen luegen.

      WAS ES BESONDERS BITTER MACHT: dieselbe Datei trug im Kommentar bereits
      die Lehre aus dem ERSTEN Mal ("der Ruecklauf der Schleife ist hier kein
      Urteil ... hat schon einmal gruen gelogen"). Die Zaehlung war die
      Korrektur dazu — sie wurde nur nicht bis zum Ausgang durchgezogen. Eine
      halb eingebaute Lehre sieht im Code aus wie eine ganze.

      DER VERLUST SELBST, und warum er nicht ausgemessen wurde: Ausgeschlossen
      wurden der Reihe nach der typografische Apostroph im Namen (die Zeile
      allein durchgeschickt kam an), das Heredoc (dieselbe Liste ohne den
      md5sum-Zweig kam vollstaendig an), stdin der Kindprozesse (`</dev/null`
      aenderte nichts) und `sort -zu` (gibt auf der Box nachweislich 114 aus).
      Nur Schleife UND Befehlssubstitution zusammen verlieren einen Eintrag.

      An dieser Stelle ist Weitersuchen der Fehler. Die Frage lautet nicht
      "welcher Puffer frisst den Happen", sondern "welcher Ordner hat ein
      echtes Cover" — und die beantwortet `find -name cover.jpg | xargs
      md5sum` ohne jede Schleife, in einem Aufruf. Zwei Mengen holen und in
      Python voneinander abziehen ist auch das kuerzere Programm. Wer eine
      Bauart nicht erklaeren kann, ersetzt sie, statt sie zu umzingeln.
  - id: oberflaeche-kennt-keine-dienste
    kind: entscheidung
    title: 'Die Oberflaeche fragt nach dem Bild — nicht danach, welcher Dienst spielt'
    tags: [architektur, vertrag, cover, oberflaeche, dienste, 2026]
    source: >
      Betreiber, 11.09.2026, auf den Befund „das cover laed nicht wenn ich
      abspiele": „die oberfläche sollte garnichts vom dienst direkt bekommen"
      und, auf einen zu engen Gegenvorschlag: „es kann ja auch ein anderer
      dienst sein ... die oberfläche soll keine dienste kennen müssen".
    related: [quellen-verschmelzen, ard-anschluss-an-beide-oberflaechen]
    body: |
      DIE REGEL: Die Oberflaeche fragt „welches Bild gehoert zum laufenden
      Titel" und bekommt eine Antwort. Sie fragt NICHT „welcher Dienst spielt
      gerade" und verzweigt danach. Welcher Dienst dahintersteht, weiss der
      Server ohnehin — und nur er weiss es vollstaendig.

      WAS DAGEGEN VERSTOESST (Stand 11.09.2026, NewDesign/app.js ~26515):
      Dort steht eine Kette mit drei Zweigen. Zweig 1 nimmt bei `lokal` die
      Coverkarte der Box, Zweig 2 bei Spotify die CDN-Adresse aus dem
      Dienstzustand (`zustand.dienst.item.album.images[0].url`), Zweig 3 waere
      der Weg ueber `/api/bild`.

      ZWEIG 2 IST DER AUFFAELLIGE, ABER NICHT DER EIGENTLICHE FEHLER. Sein
      Kommentar nennt den Bruch sogar ausdruecklich („eine DIENSTADRESSE, die
      der Vertrag fuer `werk.bild` ausschliesst — hier gilt sie trotzdem") und
      begruendet ihn mit „wer Spotify hoert, hat ohnehin Netz". Das ist eine
      Ausnahme mit Begruendung, und solche laden dazu ein, sie stehenzulassen.

      DER EIGENTLICHE FEHLER IST DIE VERZWEIGUNG SELBST. Zweig 1 ist dieselbe
      Sorte Sonderwissen, nur zufaellig ohne externe Adresse. Wer nach Dienst
      verzweigt, hat die Dienstliste im Frontend — und muss sie bei JEDEM
      neuen Dienst nachziehen. Die ARD hat das vorgefuehrt: sie kam im August
      dazu, und die Kette kennt sie bis heute nicht. Ein Podcast ueber die ARD
      faellt in keinen der drei Zweige.

      WORAN MAN ES ERKENNT: ein `if (dienst === '…')` im Frontend. Die Frage
      ist dann immer dieselbe — warum muss DIESE Seite das wissen? Fast immer
      lautet die Antwort: weil der Server es nicht liefert. Dann gehoert es in
      den Server, nicht in ein weiteres `else if`.

      WAS DER SERVER DAFUER BRAUCHT: eine Bildadresse zum laufenden Titel,
      auch fuer etwas, das die Box nicht selbst gestartet hat (Spotify vom
      Handy). Der Zustand traegt die Kennung — daraus eine boxeigene Adresse
      zu machen ist Serverarbeit, und danach ist die Antwort fuer jeden Dienst
      gleich geformt.

      NICHT EINEBNEN: „kein Bild bekannt" und „Bild bekannt, laedt gerade
      nicht" bleiben verschieden. Die Oberflaeche darf beides unterscheiden —
      das ist keine Dienstkenntnis, sondern der Unterschied zwischen einer
      leeren Antwort und einer scheiternden.
  - id: audit-2026-09-12-kritiker
    kind: howto
    title: 'Kritiker-Lauf 12.09.2026 — die Wache stand, aber niemand liess sie laufen'
    tags: [audit, wache, spotify, cover, oberflaeche, 2026]
    source: >
      Kritiker-Lauf 12.09.2026 (AUDIT-2026-09-12.md), vier unabhaengige
      Pruefer plus eigene Nachmessung am Baum. Tagesdifferenz: E138,
      Aufraeumen samt Altlasten-Werkzeug, Entwurfsentscheidung
      oberflaeche-kennt-keine-dienste samt laufendes-bild.ts, laufender
      Rahmen. Box am 11. UND 12.09. unerreichbar.
    related: [oberflaeche-kennt-keine-dienste, mupi-spotify-idle-noplay, doppelung-lassen-auseinanderlaufen-messen]
    body: |
      DER KERNBEFUND: Drei Wachen taten ihren Dienst, und keine wurde
      gefragt. Die Groessen-Wache riss exakt wie am Vortag vorhergesagt
      (app.css 101,6 % des Deckels nach 3ada5638) — ungemessen, weil
      pruefen.sh nach dem letzten Commit nicht lief. Die Paketfrische war
      zum VIERTEN Mal rot. Und Schritt 6 (Wiki im selben Zug) fiel beim
      letzten Commit des Tages aus. Alle drei haben dieselbe Naht: der
      Arbeitszug hat kein verbindliches Ende. Ein Laeufer mit 106
      Schritten, der an nichts haengt, wird gelaufen, wenn jemand daran
      denkt — an Feature-Abenden denkt niemand daran.

      DIE NEUIGKEIT VON DRAUSSEN MIT FRIST: Spotify-Refresh-Tokens
      verfallen seit 2026 nach SECHS Monaten ab der URSPRUENGLICHEN
      Autorisierung; Refresh setzt den Timer NICHT zurueck (Bestands-Apps
      seit 20.07.2026; danach 400 invalid_grant, nur neuer Sign-in hilft;
      Spotify-Dev-Blog 18.06.2026). Jede Box braucht spaetestens sechs
      Monate nach dem letzten Login eine neue Anmeldung — das kuenftige
      Dauerbild hinter „spielt nach langer Pause kein Spotify mehr".
      KORREKTUR zum Vortag: die notierte „Gnadenfrist ohne Datum" fuer
      Bestands-Apps gab es nicht — sie wurden am 09.03.2026 migriert.

      NEUE FUNDE IM TAGESCODE (Auszug, Belege im Audit): das
      Altlasten-Werkzeug verspricht „wird gemeldet" und meldet nicht
      (Zusagen-Muster des Vortags im frischesten Code); der
      Interpret-Rueckfall im neuen Bild-Weg nimmt bei mehreren Alben
      desselben Interpreten den ERSTEN Treffer; der namensKern-
      Zwillingszeuge prueft Muster und NFC, aber nicht Ersetzung und
      Flaggen — genau der historisch belegte Fehler bliebe gruen; und
      „dienstneutral" endet am Spotify-only-Riegel von /api/bild/extern.

      MESSAUFTRAG STATT UMBAU: der laufende Rahmen (@property in
      conic-gradient) ist per Bauart Dauer-Repaint, nicht compositor-
      faehig — CPU-Last am Geraet messen, sobald die Box erreichbar ist;
      WPE 2.50 (27.11.2025) mildert genau diese Stelle.
  - id: mixpi-vpn-heimweg
    kind: architektur
    title: 'VPN-Heimweg (E30): die Box als WireGuard-Klient an der FRITZ!Box — und die vier Regeln beim Uebernehmen'
    tags: [netzwerk, vpn, wireguard, verwaltung, sicherheit, e30, 2026]
    source: >
      Gebaut 12.09.2026 (Betreiberwunsch "vpn moeglichkeit ... wireshark" =
      WireGuard, wie BACKLOG E30 schon 2026-08-04 klarstellte). Regeln rein in
      src/backend-api/src/vpn.ts, 22+13 Tests; ganzer Kreis in der Vorschau
      durchgeklickt (tools/admin-vorschau.mjs fuehrt /api/vpn-Zustand). DNS-
      Verhalten draussen nachgelesen: Debian-Fehler 968683. Beim Bau war die
      Box unerreichbar; am selben Nachmittag ausgeliefert und AM GERAET
      nachgemessen (Box .62) — siehe NACHTRAG am Ende des Eintrags.
    related: [mupi-admin-dienste-und-netzwerk-neu, box-im-netz-suchen-zwei-fallen]
    body: |
      DIE FORM: Die Box ist KLIENT (der Server gehoert auf Router/NAS, kein
      Port zeigt nach Hause auf die Box), verwaltet wird GENAU EIN Heimweg:
      wg0 ueber die Einheit wg-quick@wg0. Routen /api/vpn* in server.ts nach
      dem Funk-Muster (sudo -n, execFile mit Array, Einheitenname ist
      Konstante); Seite "VPN" neben "Netzwerk". Konfiguration kommt als TEXT
      vom FRITZ!Box-Export ("Einstellungen fuer das Geraet exportieren") und
      geht durch konfigLesen(), bevor ein Byte irgendwohin darf.

      DIE VIER REGELN BEIM UEBERNEHMEN (alle in vpn.ts, je ein Test):
      1. KEIN VOLLTUNNEL (W1): AllowedIPs mit Praefix <= 1 wird ABGELEHNT —
         /0 ist der Export mit "gesamten Verkehr", das /1-PAAR (0.0.0.0/1 +
         128.0.0.0/1) ist derselbe Volltunnel ohne /0. Die Meldung nennt den
         Haken am FRITZ!Box-Dialog, denn DORT entsteht die Datei.
      2. KEINE BEFEHLSZEILEN: PostUp/PreUp/PostDown/PreDown fuehrt wg-quick
         als ROOT aus — eine hochgeladene Datei ist Daten, keine Befehle.
         SaveConfig (wg-quick ueberschriebe die Datei beim Anhalten) und
         Table (verstellt die Routen: der Hebel zum Volltunnel) ebenso raus.
      3. DNS-ZEILEN STREICHEN, MIT ANSAGE: ohne resolvconf bricht wg-quick
         an einer DNS-Zeile HART ab (Debian #968683) — die Schnittstelle
         entsteht gar nicht. Auf der Box gibt es kein resolvconf, und der
         Heimweg braucht kein Heim-DNS. Ehrliche Folge fuer die Oberflaeche:
         fritz.box loest unterwegs nicht auf, Geraete ueber Adressen.
      4. KEEPALIVE 25 ERGAENZEN, wenn er fehlt: unterwegs sitzt die Box
         hinter fremdem NAT; ohne Lebenszeichen raeumt der fremde Router die
         Zuordnung ab und die Verwaltung ist von daheim aus tot, obwohl der
         Tunnel "steht".

      WAS NIE HINAUSGEHT (W3): `wg show wg0 dump` traegt in Zeile 1 den
      PRIVATEN Schluessel und je Peer-Zeile in Feld 2 den PresharedKey —
      dumpLesen() ueberspringt beide Felder, und der Integrationstest prueft
      JSON.stringify der Antworten dagegen. Die Datei liegt als root/0600
      unter /etc/wireguard und wird vom Dienst nach dem Ablegen nie gelesen;
      die Oberflaeche bekommt eine Begleitdatei (config/vpn.json) ohne
      Geheimnisse. Die Sicherung fasst /etc/wireguard nicht an. Weg des
      Schreibens: Zwischendatei 0600 im Config-Ordner, dann
      sudo -n install -D -m 600 -o root -g root, Zwischendatei weg in JEDEM
      Ausgang.

      DAS URTEIL KOMMT VOM HANDSCHLAG, NICHT VON is-active: WireGuard
      erneuert ihn unter Last spaetestens alle 120 s (Keepalive 25 sorgt
      fuer Last), Grenze 180 s. steht / stand (vor X, Heimnetz gerade weg) /
      nie (Endpunkt, Schluessel, Portfreigabe oder Internet). Ein Dienst,
      der laeuft, tut nicht, was er soll — hier woertlich.

      TON BLEIBT UNBERUEHRT (W2): der Server startet nichts im Bootpfad;
      wg-quick@wg0 haengt an network-online.target; scheitert der Aufbau,
      fehlt der Tunnel, mehr nicht.

      TESTNAEHTE (nur ausserhalb productionServe, wie MUPIBOX_CONFIG_DIR):
      MUPIBOX_VPN_ZIEL, MUPIBOX_VPN_SYS_MODUL; Attrappen-sudo im PATH fuehrt
      install/rm/systemctl wirklich aus (auf Wegwerfdateien) und liefert den
      wg-dump aus einer Datei — vpn.integration.spec.ts.

      OFFEN: Tunnel gegen echte FRITZ!Box (die Exportdatei traegt den
      privaten Schluessel und entsteht nur am Router des Betreibers) und
      das W2-Verhalten beim Boot ohne Heimnetz.

      NACHTRAG 12.09.2026, AM GERAET (Box .62, Kern 6.18.39+rpt-rpi-2712):
      W5 ist beantwortet — der Pi-Kern fuehrt wireguard.ko.xz als MODUL
      (/usr/sbin/modinfo: version 1.0.0), es laedt beim ersten wg-quick up.
      UND DIE AUFLOESUNG DES 04.08.-RAETSELS ("modinfo meldete nichts —
      fehlt ODER fest eingebaut?"): weder noch. Eine SSH-NICHT-LOGIN-SHELL
      HAT /usr/sbin NICHT IM PATH, und modinfo wohnt genau dort. Wer
      `ssh box modinfo ...` ruft, bekommt "command not found" und haelt
      das fuer eine Aussage ueber das Modul. Die Statusroute des Servers
      traf dagegen sofort "als-modul-da" — der Dienst hat /usr/sbin im
      Pfad. Messbefehl, der traegt: `ssh box /usr/sbin/modinfo wireguard`.
      wireguard-tools 1.0.20210914-3 seither installiert; GET /api/vpn dort:
      werkzeugDa true, kern als-modul-da. AUSGELIEFERT wurde aus einem
      SAUBEREN HEAD-WORKTREE (git worktree add + node_modules-Verweise,
      tools/ausliefern.py --nur server,admin): so faehrt die uncommittete
      Arbeit einer Nachbarsitzung nicht mit, und die Herkunftszeile des
      Werkzeugs beweist den Commit (63a8ec79).
  - id: start-gedaechtnis-des-servers
    kind: architektur
    title: 'Der Server sagt, was laeuft - das Start-Gedaechtnis hinter dem laufenden Rahmen'
    tags: [architektur, oberflaeche, neu, marke, rahmen, server, e112, 2026]
    quelle: 'Auftrag 12.09.2026: "cover animiert wenn der player spielt ... ueberall, ueber interpreten wie lokale alben, ard sounds, jellyfin". Am Geraet .62 bewiesen (lokal + ARD + Spotify), Vorschau-Wachen alle gruen.'
    body: |
      DER AUFTRAG, woertlich: "alben und interpreten sollen mit einem bewegten
      rahmen zeigen das in dem albujm was spielt udn dann beim aufklappen
      welcher titel. es soll ueberall funktionieren" - Spotify-Interpreten,
      lokale Alben, ARD Sounds, Jellyfin. Der Rahmen selbst lief seit dem
      11.09.; woran es hing, war die ZUORDNUNG "was laeuft -> welche Kachel".

      ══ DER KONSTRUKTIONSFEHLER, DEN DER UMBAU BEHEBT ══════════════════════

      Die Zuordnung lebte in DREI Gedaechtnissen der SEITE (zuletztGestartet,
      laufendeFolgen, localStorage GEMERKT) - und der Kiosk laedt staendig
      neu, waehrend mpv weiterspielt. Nach jedem Neuladen war die Marke taub,
      bis wieder jemand antippte ([[flip-liste-nach-neuladen-ohne-bezug]],
      tools/marke-ohne-eigenen-start.mjs nennt es zu Recht den Alltag).
      Dabei weiss der SERVER es laengst: jeder Start laeuft durch ihn, und im
      Startaugenblick kennt er Werk UND Titelliste. Er vergass es nur.

      ══ DIE BAUART ═════════════════════════════════════════════════════════

      * laufendes-werk.ts (pur, wie laufendes-bild.ts): titelZumMerken zieht
        nr/id/uri/name/quelle aus der /inhalt-Liste; laufendeAuskunft urteilt
        aus gemerktem Start + /local-Meldung. Spielt Spotify, gilt nur das
        WERK (E108-Uebergabe wechselt die Maschine, das Werk bleibt); spielt
        mpv bei gemerktem Spotify, ist die Antwort null (die Richtung gibt es
        vom Server nicht). Titel nur mit Nummer IN der Liste UND passendem
        Namenskern - lieber keine Auskunft als die falsche Kachel.
      * server.ts: `gemerkterStart` neben `letzterStart` (ZWEI Fragen, zwei
        Gedaechtnisse - letzterStart ist der SPRUNG und wird beim
        Ein-Befehl-Start bewusst geleert, obwohl das Werk laeuft). Gefuellt an
        den drei Startstellen von /api/spielen; geraeumt am /player-Proxy bei
        fremdem Start und /stop (blankes `play` ist FORTSETZEN - es steht in
        der istStartbefehl-Erlaubnisliste, und ohne die Ausnahme loeschte
        jedes Pause-Aufheben die Marke), in kzStoppen und
        spielerAnhaltenUndWarten (gehen an :5005 vorbei am Proxy) und in
        fundAnDenSpieler (Plugin-Tuer). Mit Protokollzeile beim Raeumen.
      * Eigene Route GET /player/local VOR dem Proxy: liest den Player,
        haengt `laeuft` {werk, quelle, titelNr, titelId, titelUri, titelName,
        titelQuelle} an. EIN Abruf, den die Seite ohnehin im Sekundentakt
        macht - kein neuer Takt.
      * app.js: laufendesWerkMitBeweis hat den neuen Beweis 'server' ZWISCHEN
        'zusammenhang' (live, staerker) und 'gestartet'; bei Spotify-Ton wird
        er wie 'gestartet' per passtZuLaufendem nachgeprueft (der Server
        nennt dort nur das gemerkte Werk), bei mpv-Ton NICHT (der Server hat
        Nummer und Namenskern schon geprueft - ein zweiter Vergleich waere
        die zweite Antwort auf dieselbe Frage). laufendeFolgeJetzt nimmt den
        Server-Titel zuerst, laufendeFolgen bleibt Rueckfall.

      ══ DER EIGENTLICHE LOKAL-KILLER WAR EIN LEERZEICHEN ═══════════════════

      `data-spielt` ist eine LEERZEICHEN-getrennte Liste (join/split). Der
      lokale Ersatzschluessel (`lokal:t:kapelle petra|hamm` - normal() macht
      Sonderzeichen zu Leerzeichen) und die neue Titelkennung `pfad#nr`
      tragen Leerzeichen: solche Kennungen zerbrachen beim split in
      Fragmente, die NIE trafen. Spotify/ARD/Jellyfin-Kennungen sind
      leerzeichenfrei - deshalb ging ueberall alles, nur lokal nie, durch
      drei Reparatur-Anlaeufe hindurch. Seit dem 12.09. kodiert
      `kennungsWort` (app.js) in allen drei Kennungsbauern GENAU Leerraum und
      `%` (alles andere bleibt zeichengleich, die Wachen sehen dieselben
      Werte). Die Sabotage-Signatur der Wache: Traeger 14/14, markiert 0.

      ══ ZWEI FUNDE AM RAND, BEIDE VON DER NEUEN WACHE GEFANGEN ═════════════

      * albumTitelReiheBauen las `a.schluessel` - beide Aufrufer liefern den
        Schluessel aber IM WERK (`{kennung, titel, bild, werk, fremd}`), und
        der eigene Kommentar behauptete das Gegenteil. `folgeKennung` bekam
        undefined, JEDE Titelkachel eines lokalen oder Jellyfin-Albums stand
        ohne data-spielt da; bei Spotify unsichtbar, weil die stueck|-Form
        nur uri braucht. Jetzt fragen beide Ablagen.
      * Die runden Interpreten-Kacheln (#leute-reihe) trugen weder Kennung
        noch CSS-Anschluss - `.leute-kachel.auf.spielt` war tote Vorbereitung.
        Jetzt: data-spielt mit allen werkKennungen des Interpreten (dieselbe
        Mengenform wie die Regal-Kachel) und `.spielt .leute-bild::before` in
        allen vier Rahmen-Regelbloecken.

      ══ WACHEN NACHGEZOGEN STATT DAUERROT ══════════════════════════════════

      marke-tanzt mass die seit 11.09. tot gestellten Balken (Hoehe 0,
      dauerrot fuer heilen Code, [[dauerrote-wache-ist-keine]]) - sie liest
      jetzt den Momentanwert von `--cover-rahmen-winkel` am ::before zweimal
      im Abstand (39 -> 76 Grad in 250 ms). raster-marke-schau misst den
      Fall "mpv laeuft, nichts von hier gestartet" in BEIDEN Lagen
      (laeuft-aus: nichts markiert; laeuft-an: GENAU das Server-Werk) und
      ueberspringt die Eckenmessung der toten Marke ehrlich.
      marke-ohne-eigenen-start hat Fall 4 (lokales Album, Leerzeichen-
      Kennung, nur der Server weiss es) samt Interpreten-Kreis-Erwartung
      (genau "WDR", animationName cover-rahmen-lauf). Die Attrappe
      neu-vorschau.mjs spielt den Vertrag mit: `laeuft` GEKOPPELT an ihren
      /api/spielen-Stub (der erste, starre Wurf markierte das falsche Werk
      mit dem staerksten Beweis - eine Attrappe, die sich nicht an den
      eigenen Start haelt, prueft das Gegenteil,
      [[attrappe-luegt-durch-weglassen]]), Schalter laeuft-an/aus und
      gestartet-grund, lokale Titel-Ids MIT Leerzeichen.

      ══ AM GERAET BEWIESEN (Box .62, 12.09.2026) ═══════════════════════════

      Lokal ("Hello Kitty", Ersatzschluessel UND Pfad-Kennung mit
      Leerzeichen): laeuft mit Werk+Titel, Sprung tracknr:4 -> titelNr 4
      folgt, Stop raeumt (Journalzeile "Start-Gedaechtnis geraeumt ...").
      ARD-Sendung (quelle=ard erzwungen): 60 Folgen gemerkt, titelId ist die
      Audiothek-Folgenkennung. Spotify-Quelle: nur Werk, wie entworfen.
      GRENZEN, ehrlich: Jellyfin hat diese Box nicht (0 Werke) - der Weg ist
      ueber dieselbe mpv-Mechanik und die Wachen gedeckt, am Geraet
      ungemessen. Ein Kiosk-Foto scheiterte an ausgeschaltetem x11vnc.
      Einmal verschwand die Zuordnung Sekunden nach dem Start und liess sich
      nach dem Server-Neustart nicht nachstellen (Schirm war aus, Tipps am
      Geraet moeglich) - dafuer gibt es jetzt die Raeum-Protokollzeile.
      Beobachtet ausserdem: /inhalt desselben lokalen Werks lieferte binnen
      Minuten ZWEI Fassungen (Ordner-Zweig mit Pfad-Ids, Mitschnitt-Liste
      ohne Ids) - das ist die offene Naht der Kennungs-Baustelle
      (Commit 7bb64903), nicht dieses Umbaus.

      Verwandt: [[spielt-marke-eine-frage-zwei-formen]] (die Kennungs-
      Architektur), [[oberflaeche-kennt-keine-dienste]] (dieselbe Bewegung
      beim Bild).
  - id: duplikat-eintraege-lassen-den-rahmen-kaputt-wirken
    kind: signature
    match: rahmen klappt nicht|marke fehlt bei|indikator fehlt bei|mehrere kacheln fuer dasselbe album|doppelte eintraege data.json
    title: 'Team Karacho: "der Rahmen klappt nicht" - und der Code war heil. Duplikate in data.json trennen die Kacheln'
    tags: [signature, marke, rahmen, verschmelzung, datenpflege, medien, 2026]
    quelle: 'Betreiber 12.09.2026: "konkret team karacho klappt es nicht". Am Geraet .62 gemessen: Mechanismus heil (laeuft.werk == Kachel-Schluessel beim lokalen Start), Ursache in data.json.'
    body: |
      DAS SYMPTOM: Ein Album spielt, und die Kachel, die man ansieht, traegt
      keinen laufenden Rahmen. Sieht aus wie ein Fehler im Indikator - und
      der Indikator ist unschuldig.

      DIE MESSUNG (12.09.2026, "Team Karacho"): data.json fuehrt "Mission
      Erde: Das Fest der Sprachen" DREIMAL (1x spotify/music, 1x
      library/audiobook, 1x library/music) und "Die Welt klingt bunt" sogar
      VIERMAL (zwei Schreibweisen, zwei Kategorien). Zwei library-Eintraege
      desselben Albums ergeben IDENTISCHE Ersatzschluessel - und
      verschmelzeWerke weist mehrdeutige Zuordnungen ausdruecklich ab
      ([[medienschluessel-kollision-gleiche-playlistid]]). Ergebnis: DREI
      getrennte Kacheln fuer ein Album. Der Rahmen sitzt korrekt an der
      Fassung, die WIRKLICH laeuft - wer die andere Kachel ansieht, sieht
      nichts. Beweis der Unschuld: lokale Fassung per /api/spielen
      gestartet, laeuft.werk identisch mit dem Kachel-Schluessel, Titel-
      Kennung da.

      ZWEITER TEIL DESSELBEN BEFUNDS: Mehrinterpreten-Alben ("Team Karacho,
      ANOTHER NGUYEN", "Team Karacho, Rola", "Honigkuchenpferde, ..., Team
      Karacho") tragen den GANZEN String als interpretSchluessel und fallen
      damit aus dem Kreis "Team Karacho" heraus - /api/interpreten zaehlt
      sie dort nicht, die Rundkachel kann fuer sie nie mitlaufen. Das ist
      SERVER-Zuordnungslogik (interpretSchluesselAus kennt keine Kommas),
      keine Datenpflege - der Interpreten-String der Alben ist ja richtig.

      DIE REGEL FUER DIE DIAGNOSE: Wenn die Marke "an einem bestimmten
      Album" fehlt, ZUERST data.json auf Mehrfach-Eintraege dieses Albums
      pruefen (gleicher Interpret+Titel-Kern in mehreren Zeilen/Kategorien),
      DANN erst den Indikator verdaechtigen. Der Weg zur Heilung ist
      Datenpflege: je Album EIN library-Eintrag in EINER Kategorie; danach
      kann die Verschmelzung Spotify- und Lokal-Fassung wieder zusammenlegen
      und der Rahmen gilt fuer die eine gemeinsame Kachel.
  - id: maschine-ist-nicht-quelle-und-kommas-zaehlen-mit
    kind: architektur
    title: 'Zwei Nachzuegler vom Geraet: die Maschine ist nicht die Quelle (E108-Mischweg), und Mehrnamen-Alben zaehlen beim Solo-Kreis mit'
    tags: [architektur, marke, rahmen, server, interpreten, e108, 2026]
    quelle: 'Beide am Geraet .62 gefunden und bewiesen (12.09.2026, Team Karacho), nachdem die Dubletten-Bereinigung die Verschmelzung wieder scharf gemacht hatte.'
    body: |
      ══ FUND 1: DIE MASCHINE IST NICHT DIE QUELLE ═══════════════════════════

      Das Start-Gedaechtnis ([[start-gedaechtnis-des-servers]]) verwarf am
      verschmolzenen "Mission Erde" seine eigene Zuordnung: Der E108-Mischweg
      startet unter der QUELLE `spotify` (die gewaehlte Quelle des
      verschmolzenen Werks), die Strecke spielt aber ueber mpv (lokale
      Schwesterspuren). Ein Urteil, das "mpv spielt, aber Spotify gemerkt"
      an der QUELLE festmacht, verwirft genau den Start, den es benennen
      soll — die Kachel blieb ohne Rahmen, und KEINE Attrappe konnte das
      zeigen (der Mischweg braucht echte Schwesterspuren auf der Platte).

      Seitdem traegt `GemerkterStart` das Feld `maschine: 'mpv' | 'spotify'`,
      und NUR daran haengt die Verwerfung. Gesetzt wird es an den drei
      Startstellen: E108-Mischweg -> 'mpv' (immer — er existiert nur, wenn
      die Strecke bei mpv beginnt), inhalt-Weg -> 'mpv', Ein-Befehl-Start ->
      nach dem Dienst. `quelle` bleibt Auskunft fuers Frontend.

      ══ FUND 2: "TEAM KARACHO, ANOTHER NGUYEN" GEHOERT AUCH TEAM KARACHO ════

      Mehrnamen-Alben tragen den GANZEN artist-String als
      interpretSchluessel und fielen damit aus dem Solo-Kreis — die
      Rundkachel konnte fuer sie nie den Rahmen zeigen, `anzahl` zaehlte
      sie nicht, und die Interpretenseite fand sie unter "In deiner Box"
      nicht ([[duplikat-eintraege-lassen-den-rahmen-kaputt-wirken]], Teil 2).

      `interpretSchluesselTeile` (medien.ts) zerlegt NUR AM KOMMA — `&` und
      `feat.` bleiben zusammen ("Karsten Glueck & die Kita-Froesche" ist EIN
      Name; das `&`-Zeichen selbst faellt als Wort-Klebstoff ohnehin aus dem
      Schluessel). `interpretenReihe` (Schritt 1b) legt Kombi-Werke NUR zu
      Kreisen, die durch Solo-Werke OHNEHIN bestehen — jeder Feature-Gast
      als eigener Kreis flutete die Reihe mit Fremden. Die Interpretenseite
      filtert "In deiner Box" mit derselben Funktion: Kreis und Seite geben
      dieselbe Antwort, sonst tippt das Kind auf einen markierten Kreis und
      das laufende Album fehlt dahinter. Die Rundkachel (app.js) baut ihre
      `data-spielt`-Kennungen seitdem aus `p.werke` (der Server-Liste, die
      auch `anzahl` speist) statt aus einem eigenen Filter — eine Zuordnung,
      EIN Ort. GRENZE, dokumentiert im Code: nur freigeschaltete Kreise ohne
      eigene Werke bekommen Kombi-Werke nicht dazugelegt.

      AM GERAET: Kreis "Team Karacho" anzahl 2 -> 4; das Kombi-Album laeuft
      ueber die lokale Quelle, `laeuft.werk` liegt in der Kreis-Werkliste.
      Werkzeug des Tages daneben: tools/medien-dubletten.py (library-
      Dubletten finden, tote und lueckenhafte Fassungen regelbasiert aus
      data.json nehmen — Ordner bleiben liegen, .bak als Rueckweg).
  - id: rahmen-steht-erst-die-drei-bremsen-fragen
    kind: signature
    match: animation laeuft nicht|rahmen steht|dreht sich nicht|bewegt sich nicht|marke steht still
    title: '"Die Animation laeuft nicht" - der Rahmen stand, weil der Stillstell-Schalter AN war'
    tags: [signature, marke, rahmen, darstellung, schalter, 2026]
    quelle: 'Betreiber 12.09.2026: "es sieht besser aus aber leider laeuft die animation noch immer nicht". Box .62: Chromium-Kiosk, ruhigeMarke stand auf true.'
    body: |
      DAS SYMPTOM: Der laufende Rahmen SITZT an der richtigen Kachel, aber er
      DREHT sich nicht. Wer dann im Animations-Code sucht, sucht am falschen
      Ende: der stehende Rahmen ist zweimal ein GEBAUTER Zustand, kein
      Fehler.

      DIE DREI BREMSEN, in der Reihenfolge der Pruefung:

        1. `ruhigeMarke` in der Darstellung (GET /api/darstellung,
           `aktuell.ruhigeMarke`) - der Verwaltungsknopf "Spielt-Marke
           bewegt sich / steht still", gebaut fuer schwaechere Hardware
           ([[schalter-negativ-benennen-statt-regel-brechen]]). Am
           12.09.2026 war GENAU DAS die Ursache: der Schalter stand noch aus
           der Zeit der alten Balken-Marke auf true. Abstellen wirkt OHNE
           Neuladen - die Seite gleicht die Darstellung im Takt ab
           (sichtbarerTakt/darstellung.abgleichen, app.js).
        2. `prefers-reduced-motion` des Kiosk-Browsers - gewinnt sogar
           ueber den Schalter, mit Absicht.
        3. Browser OHNE `@property`-Animation (Cog/aelteres WebKit,
           [[optim-chromium-cog]]): der Bogen steht als dokumentierter
           Rueckfall. Auf der Box .62 laeuft Chromium - dort scheidet das
           aus; erst per ps nachsehen, WELCHER Kiosk laeuft, statt es aus
           der Konfiguration zu erinnern.

      Und `body.ton-pausiert` haelt den Rahmen bei PAUSE absichtlich an
      ("hier bist du stehengeblieben") - wer bei pausierter Box misst, misst
      keine der drei Bremsen.
  - id: werk-markiert-titel-nicht-uri-ohne-id
    kind: signature
    match: werk markiert titel nicht|beim titel fehlt der rahmen|album hat rahmen titel nicht
    title: 'Werk markiert, Titel nicht: verschmolzene Titel tragen uri OHNE id - die Stueck-Form kommt jetzt aus der Server-Auskunft'
    tags: [signature, marke, rahmen, verschmelzung, kennung, 2026]
    quelle: 'Betreiber 12.09.2026: "beim titel fehlt der rahmen obwohl das stueck laeuft". Box .62, verschmolzenes Mission Erde: uri JA, id LEER, befehl datei/... - gemessen an /inhalt.'
    body: |
      DAS SYMPTOM: Die Album-Kachel traegt den Rahmen, die aufgeklappte
      Titel-Kachel des LAUFENDEN Stuecks nicht — bei einem VERSCHMOLZENEN
      Werk, dessen Strecke ueber mpv spielt.

      DIE URSACHE, gemessen an /inhalt: verschmolzene Titel tragen `uri`
      (die Spotify-Adresse der Schwester) OHNE `id` — der Kommentar an
      `folgeKennung` behauptete "entweder uri ODER id, nie beides; lokale
      haben id", und seit die Verschmelzung wieder greift, stimmt beides
      nicht mehr. Die Kachel bekommt darum die stueck|-Form (ODER-Kette in
      albumTitelReiheBauen/titelListeBauen). Bei mpv-Ton baute
      laufendeKennungen aber nur die folge|-Form, und die braucht eine id,
      die es nicht gibt: kein Match, nie.

      DER SCHNITT: Die Adresse des laufenden Titels kennt bei mpv-Ton NUR
      das Start-Gedaechtnis ([[start-gedaechtnis-des-servers]]) —
      `laeuft.titelUri` stammt aus DERSELBEN /inhalt-Liste wie die Kachel.
      laufendeKennungen baut daraus zusaetzlich die stueck|-Form, mit
      derselben Zusammenhang-Formel wie die Kachel (zusammenhangVon(werk)
      bzw. spotifyId(schluessel)). Keine Nachlauf-Luege: die Quelle ist die
      geprueft e Server-Zuordnung, nicht der nachlaufende /player/state.

      GEMESSEN am Geraet: laeuft.titelNr 1, titelUri spotify:track:4czp...,
      titelId leer — exakt die Felder, an denen die alte Kette riss.
  - id: katalogweg-warf-die-stueckadresse-weg
    kind: signature
    match: ueber interpret titel klappt nicht|interpretenseite titel keine marke|katalog album keine markierung
    title: 'Interpretenseite: Titel ohne Rahmen bei eigenen Spotify-Alben - der Katalog-Weg warf die uri weg'
    tags: [signature, marke, rahmen, interpretenseite, spotify, kennung, 2026]
    quelle: 'Betreiber 12.09.2026: "nur ueber interpet titel klappt nicht bspw alin coean". Gemessen: /api/spotify/album liefert tracks.items MIT uri, das Mapping in albumStuecke uebernahm nur nr/titel/dauerMs.'
    body: |
      DAS SYMPTOM: Dieselben Titel tragen im RASTER den laufenden Rahmen,
      ueber die INTERPRETENSEITE nicht — bei eigenen Spotify-Alben (Alin
      Coen: 11 reine Spotify-Werke).

      WARUM ES ZWEI WEGE GIBT: Das Raster setzt `fremd: true` fuer JEDES
      Bibliothekswerk und holt die Titel ueber die Box (/inhalt — dort
      reisen uri, quelle, quellen mit). Die Interpretenseite laesst eigene
      REINE Spotify-Alben absichtlich den kuerzeren Katalog-Weg gehen
      (`/api/spotify/album/<id>`, "der Weg des Rasters" fuers ABSPIELEN,
      nicht fuers Aufklappen). Deren Mapping in `albumStuecke` uebernahm
      aber nur nr/titel/dauerMs — `t.uri` fiel weg. Ohne uri gibt
      `stueckKennung` '' zurueck, eine id gibt es im Katalog nicht: die
      Titelkachel trug KEIN data-spielt und war unmarkierbar.

      DER FIX ist eine Zeile im Mapping (`uri: String(t.uri || '')`), und
      die Regel dahinter ist die eigentliche Lehre: Ein Feld, das eine
      Naht wegwirft, fehlt hinterher an einer Stelle, die niemand mit der
      Naht in Verbindung bringt — dieselbe Sorte wie
      [[die-kachel-zeigt-nicht-was-sie-tut-und-nicht-woher-sie-kommt]]
      ("ein Feld, das der Server liefert und niemand zeigt").

      BEKANNTE GRENZE, unveraendert: Bei Spotify-"Relinking" traegt die
      Kachel die Katalog-uri, /player/state meldet die getauschte
      (linked_from) — dann bleibt nur die Werk-Marke. Gilt fuer beide Wege
      gleichermassen.
  - id: welcher-zweig-antwortet-verraten-die-felder
    kind: methode
    title: >-
      Welcher Zweig antwortet, verraten die FELDER der Antwort — nicht das
      Lesen des Codes
    tags: [methode, messen, backend, routen, e112, 2026]
    quelle:
      - 'gemessen am 2026-09-12 an der Box 192.168.178.62, /api/werke/<lokal>/inhalt'
      - 'zwei Fehlanlaeufe davor, beide ohne Wirkung am Geraet'
    body: |
      DER FALL. Lokale Titel bekamen in der Liste keinen Indikator, weil ihnen
      `id` und `uri` fehlten. Zwei Anlaeufe setzten die Kennung — beide ohne
      jede Wirkung am Geraet:

        1. im `/tracklist`-Zweig von server.ts
        2. in `titelQuellenStempeln` (titelkarte.ts), beschrieben als „die
           Naht, durch die JEDE lokale Titelliste laeuft"

      Beide Male: Typen sauber, Zeugen gruen, Server ausgeliefert und stabil,
      und die Route lieferte unveraendert `id: None`.

      DIE MESSUNG, DIE ES IN EINEM ZUG KLAERTE. Nicht „wo koennte es fehlen",
      sondern: WELCHE FELDER hat die Antwort ueberhaupt?

        curl .../api/werke/<schluessel>/inhalt
        → anhaengen befehl interpret nr quelle quellen titel

      Der geaenderte `/tracklist`-Zweig baut `{nr, titel, interpret, id}` —
      OHNE `anhaengen`, OHNE `befehl`. Die Felder passten also auf keinen der
      beiden geaenderten Zweige. Damit stand in einer einzigen Messung fest,
      dass ein DRITTER antwortet, ohne eine Zeile Code zu lesen.

      Es war der Ordner-Zweig (`lokaleSpurenKarte`, server.ts ~12228): er baut
      seine Titel VON HAND und ruft die Stempelfunktion gar nicht auf. Der
      `/tracklist`-Weg darunter ist nur sein Rueckfall — er greift erst, wenn
      der Ordner nichts hergibt, also im Alltag nie.

      DIE REGEL. Eine Hilfsfunktion ist keine Naht, nur weil sie danach
      aussieht. „Alle rufen X" laesst sich aus den Aufrufstellen NICHT
      belegen — belegbar waere nur, wer NICHT ruft, und genau das sieht man
      beim Draufschauen nicht. Die Antwort dagegen ist die Gegenstelle: ihre
      Feldmenge ist ein Fingerabdruck des Zweigs, der sie gebaut hat.

      ALSO, BEVOR MAN AENDERT: erst die Antwort abholen und ihre Felder mit
      dem vergleichen, was die verdaechtige Stelle baut. Stimmen sie nicht
      ueberein, ist die Stelle falsch — egal wie ueberzeugend der Code liest.

      UND DIE WACHE GEHOERT AN DIE ROUTE. Ein Test auf
      `titelQuellenStempeln` waere beim zweiten Anlauf GRUEN gewesen und
      haette den Irrtum bestaetigt. Nur ein Test gegen
      `GET /api/werke/:schluessel/inhalt` mit echtem Album-Ordner
      (`MUPIBOX_MEDIA_DIR` lenkt ihn um) misst, wer wirklich antwortet —
      inhalt.integration.spec.ts, „gibt jedem Titel des Ordner-Zweigs eine
      eigene Kennung". Gegenprobe doppelt: Kennung ganz weg → rot, Kennung
      fuer alle gleich → rot.

      VERWANDT: [[kommentar-und-kompilat-sind-keine-gegenstelle]] — grep auf
      das Kompilat beweist gar nichts, esbuild wirft Kommentare weg und
      minifiziert Namen. Auch dieser Falle bin ich im selben Lauf erst
      aufgesessen: `grep -c 'WIEDERZUERKENNEN' server.js` gab 0, und das
      hielt ich kurz fuer „der Code ist nicht auf der Box". Die md5-Summe
      gegen den Bau war die richtige Frage, und sie stimmte.
      Und [[wiederkehrender-befund-ist-ablauffehler]] — derselbe Befund nach
      dem Fix heisst: die Wache stand hinter dem Ereignis.
  - id: audit-2026-09-13-kritiker
    kind: howto
    title: 'Kritiker-Lauf 13.09.2026 — committet ueber den gerissenen Deckel, und ein Loeschwerkzeug mit Quoting-Loch'
    tags: [audit, wache, kiosk, wireguard, spotify, 2026]
    source: >
      Kritiker-Lauf 13.09.2026 (AUDIT-2026-09-13.md), vier unabhaengige
      Pruefer plus eigene Nachmessung. Tagesdifferenz: 15 Commits vom 12.09.
      (E30/VPN, E112, Rahmen aus der Server-Auskunft, lokale Titel-Kennung,
      Interpretenseite). Box .62 wieder erreichbar, lief exakt den Baumstand.
      Waehrend des Laufs landete 54f3eada (Kennung im Ordner-Zweig).
    related: [wiederkehrender-befund-ist-ablauffehler, start-gedaechtnis-des-servers, mixpi-vpn-heimweg]
    body: |
      DER KERNBEFUND: Es wurde ueber die ROTE Groessen-Wache committet —
      beide Deckel am HEAD ueberschritten (app.js 100,9 %, app.css 101,8 %),
      vier Oberflaechen-Commits, kein Anheben, keine Begruendung. Eine
      Wache, ueber die man committet, schuetzt nichts mehr. Der billigste
      Schrumpf liegt daneben: spieltMarkieren() baut die per display:none
      stillgelegte .spielt-marke im Sekundentakt weiter, obwohl der eigene
      app.css-Kommentar den Abbau ab dem Rahmen-Umzug (28c80033) selbst
      zusagt. Erst abbauen, dann messen, dann Deckel neu begruenden.

      DER GEFAEHRLICHSTE EINZELFUND: tools/medien-dubletten.py baut seine
      SSH-Ordnerprobe per join von f-String-Single-Quotes (:138). Ein
      Apostroph im Titel (Guns N' Roses) zerreisst das Quoting, die
      Wortliste der for-Schleife verrutscht, und die POSITIONSBASIERTE
      JA/NEIN-Zuordnung trifft falsche Eintraege — ein --wirklich-Lauf
      loeschte dann falsche Ordner. Doppel-Fix: shlex.quote UND vor der
      Zuordnung len(Probezeilen) == len(pfade) erzwingen. Die Regel dahinter:
      wer Urteile ueber POSITION zuordnet, braucht eine Laengen-Wache.

      VON DRAUSSEN, DATIERT: WPE 2.54 (Damage-Tracking fertig) stand am
      13.09. unmittelbar bevor (RCs 2.53.92 vom 07.09.2026) — ABER Cog
      nutzt die alte WPE-API und kann die neue Platform-API nicht (Bericht
      04.01.2026); vor einem Sprung pruefen. Spotify: KEINE neuen
      Deprecations seit Juni 2026; Juli-Changelog nur Verwaltung (25
      Client-IDs je Konto, Quota je KONTO, reason QUOTA_EXCEEDED an 429);
      die im Februar gestrichenen external_ids fuer Alben/Tracks wurden im
      Maerz ZURUECKGENOMMEN. FRITZ!Box schreibt importierte
      WireGuard-Konfigurationen beim Anlegen UM (Address/DNS), und ihr
      DNS-Rebind-Schutz blockiert Namen auf private IPs (Ausnahmeliste
      Heimnetz->Netzwerkeinstellungen). Leerbefunde: mpv 0.40/0.41 ohne
      IPC-/metadata-Aenderung; librespot weiter v0.8.0 („v0.9" in
      Suchtreffern ist go-librespot, anderes Projekt).

      AUSSERDEM: laufendes-werk.ts verspricht „Nummer ohne Namen ergibt nur
      das Werk" und liefert bei leerem currentTrackname den vollen Titel
      (Verwerfung braucht BEIDE Namen) — im Startaugenblick Titelmarke an
      der falschen Kachel moeglich. E139 ist im Backlog doppelt vergeben
      (Memory FERTIG vs Schluessel-Wanderung OFFEN; der neue Punkt muss
      E141 heissen), und die Wanderungs-Entscheidung fehlt im Pack —
      Schritt-6-Verstoss. E112: Backlog-Kopf sagt „Geraetebeweis steht
      aus", der Pack-Eintrag sagt „am Geraet bewiesen" — eine der beiden
      Aussagen ist falsch, am Geraet klaeren.
      Verwandt: [[wiederkehrender-befund-ist-ablauffehler]] — die
      Paketfrische ist den fuenften Lauf rot, der Rang wird nicht mehr
      wiederholt, nur die Zaehlung fortgeschrieben.
  - id: box-admin-menue-wlan-vpn
    kind: architektur
    title: 'Box-Admin-Menue: bekannte Netze ohne Passwort, gespeicherte Netze, VPN-Fach (13.09.2026)'
    tags: [box, oberflaeche, netzwerk, wlan, vpn, wireguard, eltern, e115, e30, 2026]
    source: >
      Betreiberwunsch 13.09.2026: "das verbinden zu wlans wie im admin auch im
      admin menü auf der box, auch vpn möchte ich dort verwalten können".
      Gebaut in NewDesign/app.js (Fach wlan erweitert, Fach vpn neu), alle
      Fluesse gegen tools/neu-vorschau.mjs durchgespielt (Port 8321), Struktur
      von tools/admin-menue-schau.mjs gemessen. Am Geraet noch nicht.
    related: [mixpi-vpn-heimweg, wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm, wiederkehrender-befund-ist-ablauffehler]
    body: |
      WAS DIE BOX JETZT KANN (vorher nur die Verwaltung, E115/E30):

      1. BEKANNTES NETZ OHNE PASSWORT: wlanTippen geht bei n.bekannt ueber
         POST /api/netzwerk/gespeichert/verbinden - Kennung FRISCH ueber die
         SSID aus der eben geholten Liste aufgeloest, nie gemerkt
         (wpa_supplicant vergibt sie neu). Fehlt die Auskunft (gespeicherte
         === null), faellt es auf die Tastatur zurueck: schlechter, nie
         Sackgasse. Die Rueckfall-Sicherung ist die des Passwort-Wegs
         (derselbe Wecker, dieselbe amGeraet-Sofortspeicherung).
      2. ABSCHNITT "GESPEICHERTE NETZE" unter der Funkliste, gebaut fuer den
         Fall, den der Scan NICHT zeigt (verstecktes/abwesendes Netz) - darum
         endet wlanMalen bei leerem Scan nicht mehr mit return (dieselbe
         Falle wie einst bei WPS, nur andersherum). EINE Zeile hat EINEN
         Knopf (9-mm-Marke): aktuell = kein Knopf, sichtbar = Vergessen
         (Verbinden steht schon oben), unsichtbar = Verbinden. Der Randfall
         "unsichtbares Netz loswerden" bleibt der Verwaltung - Entscheidung,
         keine Luecke.
      3. VERGESSEN mit eigener Rueckfrage-Ebene (wlanVergessenFrage) samt
         Doppeltipp-Frist STROM_SCHARF_MS und eigenem Rueckweg in zurueck() -
         das Muster von funkFrageAuf, denn "Ja, vergessen" liegt dort, wo
         eine Zeile vorher ein anderer Knopf lag.
      4. VPN-FACH (Gruppe Verbindung, 4. Punkt, Zeichen 'haus' = Heimweg):
         GET /api/vpn im 5-s-Takt (vpnTakt nach dem infoTakt-Muster, faellt
         in blattRaeumen UND raeumen), Urteil kommt vom SERVER (Handschlag,
         nie is-active nachgerechnet). Zwei Schalter via POST /api/vpn/aktiv
         (an / beimStart). Uebernehmen/Entfernen der Datei BEWUSST nicht am
         Geraet: Datei entsteht am Router, kein Upload am Touch, privater
         Schluessel wird nicht abgetippt; Entfernen folgt der
         Bluetooth-Regel (was nur die Verwaltung wiederherstellen kann,
         nimmt auch nur sie weg). Kommentar "VPN - keine Route" bei
         ELTERN_PUNKTE war seit E30 ueberholt: aus der Vier-die-es-nicht-
         gibt-Liste bleiben drei (Hotspot, Account, QR).

      DIE WACHE HING IN KEINEM LAEUFER UND WAR DREIFACH VERALTET:
      tools/admin-menue-schau.mjs fuehrte VPN noch in DARF_NICHT und kannte
      Klang (21.08.), Gesten (21.08.), Leistung (E58) nicht - sieben rote
      Aussagen, die niemand sah
      ([[wache-die-nirgends-laeuft-rostet-auf-einem-fehlalarm]]).
      Beim Nachziehen fand sie einen ECHTEN Altfund:
      punktUnterzeile hatte keinen klang-Zweig, die Zeile stand seit 21.08.
      als einzige ohne Unterzeile da (jetzt statisch, wie "Suchen und
      verwalten" - die Uebersicht holt das Klang-Plugin mit Absicht nicht).
      NOCH OFFEN dort: drei 10-px-Aussagen auf Indikatoren/Farbe/Player
      (alle Zeilen angeblich unter 10 px sichtbar) - Altbestand, nicht
      geklaert, Werkzeug weiter in keinem Laeufer.

      VORSCHAU-ATTRAPPE (tools/neu-vorschau.mjs): GESPEICHERT ist die EINE
      Quelle - auch fuer bekannt im Scan. Solange WLAN_NETZE ein eigenes
      bekannt-Flag trug, sagte der Scan nach einem Vergessen weiter
      "gespeichert", und die Oberflaeche suchte eine Kennung, die es nicht
      mehr gab. Lagen: wlan-kommtnicht/-ungesichert gelten auch fuer den
      Weg ohne Passwort; vpn-nie/-stand/-leer/-werkzeug-fehlt/-schalten-
      kaputt/-weg, Zustand zurueck mit vpn-zurueck; wlan-zurueck stellt
      jetzt auch GESPEICHERT wieder her.

      AUSSERDEM AN DER NAHT: tools/neu-regeln-auszug.py musste kennungsWort
      in NAMEN aufnehmen (folgeKennung ruft es seit der Leerzeichen-
      Wanderung) - ohne das fiel tools/pruef-neu-regeln.js mit
      ReferenceError um, fuer JEDE Sitzung. Dasselbe boxIpAus-Muster steht
      dort seit dem 06.08. als Kommentar.
  - id: audit-2026-09-14-kritiker
    kind: howto
    title: 'Kritiker-Lauf 14.09.2026 — die Antwort ueberlebt das Zusperren, und die reparierte Wache haengt an nichts'
    tags: [audit, wache, netzwerk, wlan, vpn, oberflaeche, 2026]
    source: >
      Kritiker-Lauf 14.09.2026 (AUDIT-2026-09-14.md), drei unabhaengige
      Pruefer plus eigene Nachmessung. Tagesdifferenz: genau 1 Commit
      (1313ce4d, Box-Admin-Menue WLAN/VPN, +697 Zeilen app.js). Box heute
      NICHT erreichbar (.62/.78/.81, Port 8200 zu) — alle Geraeteauftraege
      blockiert. Sieben Raenge gebucht (Sammelpunkt E91).
    related: [box-admin-menue-wlan-vpn, dauerrote-wache-ist-keine, wiederkehrender-befund-ist-ablauffehler, vierzehn-kopien-und-die-abweichung-ist-der-fehler]
    body: |
      DER KERNBEFUND: Die Antwort ueberlebt das Zusperren. vpnHolen
      (app.js:18771) und wlanHolen (:17950-:17972) schreiben nach dem
      await OHNE Lebenszeichen-Wache — und vpnHolen malt bei
      fach=''/gruppe='verbindung' (:18783) ungewacht die Uebersicht auf
      den zugesperrten Schirm. raeumen() leert vpnStand und gespeicherte
      ausdruecklich aus Datenschutz (Tunnelgegenstelle, Netzliste); die
      LETZTE laufende 5-s-Antwort schreibt beides zurueck. Alle
      Nachbar-Holer tragen die Wache woertlich (infoHolen :19462,
      akkuHolen :20194, uebersichtHolen :16966) — die zwei neuen sind
      die einzigen Ausreisser: fast gleich, und die Abweichung ist der
      Fehler ([[vierzehn-kopien-und-die-abweichung-ist-der-fehler]]).
      Dazu KLEBT die VPN-Fehlermeldung: der catch setzt sie, der
      Erfolgspfad loescht sie nie (wlanHolen :17962 macht es vor).

      DIE ZWEITE LEHRE: reparieren ohne anschliessen. Der Commit
      repariert admin-menue-schau (sieben rote Aussagen) und laesst
      beide URSACHEN stehen: die Wache haengt weiter in keinem Laeufer,
      und drei 10-px-Aussagen bleiben dauerrot (selbst gemessen: „3
      AUSSAGE(N) GEFALLEN") — [[dauerrote-wache-ist-keine]]. Derselbe
      Verfall ist damit zum zweiten Mal angelegt. Spiegelbildlich der
      Deckel: app.js vorbildlich begruendet angehoben (Altschuld
      ausdruecklich NICHT legalisiert), aber app.css blieb rot und wurde
      zum fuenften Mal uebercommittet — ein halber Deckel-Entscheid
      sieht aus wie „erledigt" und ist es nicht.

      VORSCHAU-LUECKE MIT MUSTERWERT: neu-vorschau.mjs haengt Scan UND
      /api/netzwerk/gespeichert am selben Hebel (lage.wlan==='weg') —
      der im Commit-Text versprochene Tastatur-Rueckfall („Scan liefert,
      gespeichert nicht") ist damit UNSPIELBAR, die Behauptung unbelegt.
      Wer eine Attrappe baut, gibt jeder Auskunft ihren EIGENEN Hebel,
      sonst kann die Vorschau genau die Lage nicht zeigen, fuer die der
      Rueckfall geschrieben wurde. Dazu: 503 statt 500 und abweichender
      409-Wortlaut unter einem Kopf, der „wortgleich zum Server" sagt.

      SSID-RANDFALL: netzwerk.ts:401 trimmt die GESPEICHERTE SSID, die
      Scan-SSID nicht — Rand-Leerzeichen ergibt bekannt:true ohne
      gespeicherten Treffer, wortloser Rueckfall auf die Tastatur (der
      Fehler, den E115 beseitigen sollte). Robust: ueber die
      Netz-KENNUNG verbinden, nicht ueber SSID-Textvergleich; das
      erledigt auch Duplikat-SSIDs (wpa_supplicant erlaubt sie, find
      nimmt sonst den ersten, womoeglich [DISABLED]).

      BUCHUNGS-KORREKTUR: der 09-13-R4-Rat „E139-Doppel auf E141
      umbenennen" war beim Aufschreiben schon ueberholt — 54f3eada
      vergab E141 vier Minuten VOR dem Audit-Commit an den
      Lauf-Indikator. Die Schluessel-Wanderung muss E142 heissen.
      Verfahrenslehre: wer eine freie Nummer vergibt, prueft sie gegen
      den COMMIT-Stand des eigenen Laufs, nicht gegen den Lesestand.

      VON DRAUSSEN, DATIERT (14.09.): WPE 2.54 weiter NICHT final
      (neueste RC 2.53.92 vom 03.09., stabil 2.52.6); zu Cog kein Fund
      einer Anpassung an die neue Platform-API. Spotify: August-/
      September-Changelog EXISTIERT NICHT (Leerbefund), Juli nur
      Verwaltung — keine neuen Deprecations. WireGuard-Handschlag-
      Urteil des VPN-Fachs ist Stand der Technik (gesund < 120 s,
      Alarm ~180 s, wg show dump fuer Monitore; Leitfaden 23.06.2026).
      mpv bleibt 0.41, librespot v0.8.0 (Leerbefunde). nmcli-
      Sonderzeichen-Fallstricke unveraendert offen (Arch-Forum
      07.07.2025).
  - id: audit-2026-09-19-kritiker
    kind: howto
    title: 'Kritiker-Lauf 19.09.2026 — Breiten-Lauf: der Updater saegt am frischen VPN, und der Haupt-Laeufer ist 17-fach rot'
    tags: [audit, updater, wache, werkzeug, kiosk, server, 2026]
    source: >
      Kritiker-Lauf 19.09.2026 (AUDIT-2026-09-19.md), Breiten-Lauf ohne
      Tages-Diff (NULL Commits seit dem 14.09., fuenf Tage Stillstand):
      drei unabhaengige Pruefer (Oberflaeche, Server, Werkzeuge) plus
      Netz-Recherche und eigene Messungen; tragende Behauptungen einzeln
      nachgemessen. Box .62 erreichbar (Pi 5). pruefen.sh lief KOMPLETT.
      Zwoelf Raenge gebucht (Sammelpunkt E91).
    related: [zwei-ausrollwege-eine-handgefuehrte-liste, vierzehn-kopien-und-die-abweichung-ist-der-fehler, dauerrote-wache-ist-keine, ungerufene-wache-driftet-in-den-fehlalarm, mupi-rpi-lgpio, kiosk-browser-umschaltbar-mit-rueckfall, cog-spart-267-mb-gemessen, mupi-werkzeug-ohne-sammellaeufer]
    body: |
      DER SCHWERSTE FUND: Die APT-Liste lebt zweimal und ist dreifach
      auseinander ([[zwei-ausrollwege-eine-handgefuehrte-liste]] in der
      Paket-Form). start_mupibox_update.sh:398 installiert
      python3-rpi.gpio — genau das Paket, dessen Austausch gegen
      rpi-lgpio das Wiki als Pi-5-Reparatur fuehrt ([[mupi-rpi-lgpio]])
      und das autosetup laengst traegt: Update-Boxen bekommen den
      kaputten Zustand ZURUECK. wireguard-tools fehlt dem Updater —
      der E30-VPN-Heimweg kann auf Bestandsboxen nie laufen. Und die
      packages2remove-Schleife (:541) prueft -eq 0 statt -gt 0: sie
      hat seit ihrem Entstehen NIE etwas entfernt (von der
      Install-Schleife kopiert, Richtung nicht gedreht).

      DER SYSTEMISCHE FUND: pruefen.sh bricht in 17 Schritten, und die
      Roete hat DREI SORTEN — echt (Gesten-Doppeltipp startet die
      Kachel, dreimal seriell bestaetigt; neu-sprechen 6 Abweichungen),
      falsch-rot aus Wachen-Schwaechen (dienste-deckung frisst die
      Nicht-Dienst-Klassen marke-gilt/-nebenher; auslieferprobe
      normalisiert das cp-Idiom ordner/. nicht), und bekannt/gebucht.
      [[dauerrote-wache-ist-keine]] gilt hier fuer den LAEUFER als
      Ganzes: bei 17 Roten prueft niemand den achtzehnten. Dazu die
      Inventur: 110 von 220 Wachen haengen in einem Laeufer, die
      Meta-Wache hat einen dokumentierten blinden Fleck von ~86
      (kennt sys.exit(main()) nicht) und ist selbst rot
      ([[ungerufene-wache-driftet-in-den-fehlalarm]],
      [[mupi-werkzeug-ohne-sammellaeufer]]).

      MESSLEHRE, SELBST BEZAHLT: Zwei direkte wischrand-Laeufe liefen
      parallel zum noch laufenden pruefen.sh — beide leihen sich die
      Vorschau. Folge: „Jede Kachel spielt" stand rot (ard/plugin ohne
      Startbefehl), war aber SERIELL gruen. Vorschau-Wachen niemals
      parallel messen; einen Laeufer-Roten erst glauben, wenn er
      seriell reproduziert ist.

      WEITERE KERNFUNDE (Details im Audit): wg-uebernahme.pid.tmp ohne
      Zaehler — zwei gleichzeitige VPN-Uebernahmen teilen sich die
      Zwischendatei MIT privatem Schluessel, das finally-rm der einen
      loescht unter dem install der anderen
      ([[vierzehn-kopien-und-die-abweichung-ist-der-fehler]]: der
      atomar.ts-Zaehler fehlt an vier Stellen). /api/monitor oeffnet
      am Host-Kopf (host.indexOf('localhost'), Client-Ware); drei
      Loopback-Nachbauten kennen 127.0.1.1 nicht, das der Installer
      selbst in /etc/hosts schreibt. Der WPS-Takt ist als einziger
      nicht parkbar und schreibt nach dem Zusperren die aus
      Datenschutz geleerte Netzliste zurueck. ~1000 Zeilen totes
      CSS/Token/HTML (--line 24x deklariert, 0x benutzt) — app.css
      kann damit UNTER den Deckel schrumpfen statt angehoben zu
      werden. VORSICHT bei Klassen-Loeschungen: pwf-*/pw-*/marke-*
      werden zusammengesetzt und sehen in jeder Literal-Suche tot aus.

      VON DRAUSSEN, DATIERT 19.09.: WPE 2.54.0 FINAL seit 16.09.2026
      (WPEPlatform stabil, Skia-Compositor +36 % MotionMark Pi 4) und
      Cog ist ABGEKUENDIGT (Igalia/cog#799, nichts ueber 0.18.x) —
      der zweite Kiosk-Browser ([[kiosk-browser-umschaltbar-mit-rueckfall]],
      [[cog-spart-267-mb-gemessen]]) haengt an einem toten Ast;
      Entscheid noetig, aber erst nach der Rahmen-Paarmessung (die
      Leerlauf-Haelfte steht seit heute: Chromium 5-10 %, load 0,35).
      librespot 0.8.0, mpv 0.41, Spotify-Changelog: Leerbefunde.
      Fuer tote Funktionen in einer buendlerlosen 1,6-MB-Datei gibt
      es KEIN Fertigwerkzeug (knip greift nicht) — acorn-Eigenbau
      plus PurgeCSS-Befundliste ist der realistische Weg.

      GERAETESTAND: Die Box faehrt den Stand vom 12.09. 20:08 — der
      komplette 13.09.-Commit (Box-Admin-Menue, „E115+E30 aufs
      Geraet") ist NIE angekommen; E112 dagegen ist im Bundle. Wer
      eine Commit-Botschaft „aufs Geraet" liest, misst am Geraet nach.
  - id: aufraeum-runde-2026-09-19
    kind: note
    lang: de
    title: >-
      Aufraeum-Runde 19.09.2026 — sechs parallele Agenten, und das Audit
      wurde an sechs Stellen berichtigt
    tags: [audit, wachen, werkzeuge, parallel, korrektur, 2026]
    quelle: >-
      Am selben Tag wie AUDIT-2026-09-19 gefahren, sechs Agenten mit
      disjunkter Dateihoheit; jeder Befund vor dem Eingriff selbst
      nachgemessen. Die Berichtigungen sind Messungen, keine Meinungen.
    body: |
      SECHS RAENGE parallel abgetragen, nach DATEIEN geschnitten statt nach
      Raengen — parallele Bearbeiter duerfen sich keine Datei teilen, sonst
      ueberschreiben sie einander. Raenge 3/7/10/11 liegen alle in server.ts
      bzw. app.js und mussten deshalb in eine zweite Welle.

      WICHTIGER ALS DIE FIXES: Der Befehl "miss nach, bevor du glaubst" hat
      das Audit an SECHS Stellen berichtigt. Wer den Audit-Text als Wahrheit
      liest, baut auf sechs falsche Annahmen.

      1. ~1000 ZEILEN TOTES CSS GIBT ES NICHT. Einzeln belegbar sind ~1,6 kB
         Regeln. app.css faellt von 473.145 auf 472.718 B — der Deckel von
         465.000 B ist NICHT erreicht, es fehlt etwa das Doppelte. Der Satz
         "damit ist der halbe Deckel-Entscheid von 09-14 R5 als SCHRUMPFEN
         beendbar" traegt nicht; jener Entscheid bleibt offen.
      2. --line IST NICHT TOT, SONDERN BLOCKIERT. tools/farbsaetze-bauen.mjs
         erzeugt die 24 Deklarationen und prueft sie zurueck. Allein in
         app.css geloescht waere der Erzeuger 24x rot und der naechste Lauf
         schriebe alles wieder hinein. Siehe
         [[erzeugte-deklaration-ist-nicht-tot]].
      3. DIE ANGRIFFSNUTZLAST AUF /api/monitor WAR FALSCH. "Host:
         localhost.beliebig" kommt NICHT durch — der Namensriegel in
         herkunft.ts weist sie vorher mit eigener 403 ab. Durch kommt "Host:
         localhost", weil localhost ein echter Name der Box ist. Das Loch ist
         real, der Weg dahin im Audit falsch: EINE 403 BEWEIST HIER NICHTS.
      4. "RISIKO: KEINS" WAR FALSCH. Die Erweiterung auf 127/8 reisst einen
         bestehenden Test, der "Ferne" durch Bind auf 127.0.0.2 faelschte —
         eine Adresse, die danach zu Recht als lokal gilt.
      5. ZWOELF setInterval IN app.js, NICHT VIERZEHN (zwei Treffer waren
         Kommentar-Erwaehnungen), und "einziger nicht an this" gilt nur
         INNERHALB des Eltern-Bereichs. Ebenso stimmt "einziger Loescher ohne
         offen/frei-Wache" nicht: wlanVergessenTun:18717 hat sie auch nicht.
      6. DER BLINDE FLECK WAR VIERMAL SO GROSS wie geschaetzt: nicht ~86,
         sondern 363 verdeckte Werkzeuge. Und die zwei angeblich
         "verrotteten" BEKANNT-Eintraege sind NICHT verrottet — beide
         Gegenstaende existieren; Austragen haette zwei richtige Ausnahmen
         vernichtet. Siehe [[wache-mit-blindem-fleck-braucht-ratsche]].

      WAS DARAUS FOLGT: Ein Audit ist eine Fundliste, kein Befundprotokoll.
      Seine Zeilennummern wandern, seine Zaehlungen zaehlen Kommentare mit,
      und seine Risiko-Urteile sind ungetestet. Wer einen Rang abtraegt, misst
      ihn zuerst nach — das kostet Minuten und hat hier sechsmal einen
      falschen Umbau verhindert. Die Gegenfrage, die das ausloest, lautet:
      "welche Messung wuerde meine eigene Erklaerung widerlegen?" — und sie
      gehoert VOR den ersten Edit, nicht hinter ihn.

      AUCH GELERNT, ZWEIMAL: Die GEGENPROBE hat zweimal einen eigenen Fund
      geliefert, weil eine frisch gebaute Wache GRUEN blieb, wo sie rot sein
      musste — einmal haette die Haertung der Auslieferprobe sie stumpf
      gemacht (cp .../datei/. waere durchgegangen, eine Zeile, die zur
      Laufzeit an "Not a directory" stirbt), einmal fehlte der neuen
      Stil-Wache die siebte Dienst-Plakette .marke-plugin. MERKSATZ: Eine
      Gegenprobe, die GRUEN bleibt, ist nicht bestanden — sie ist der Fund.
      Verwandt, aus der Test-Sicht:
      [[die-rueckmeldung-allein-bezeugt-den-riegel-nicht]].
  - id: wache-mit-blindem-fleck-braucht-ratsche
    kind: falle
    lang: de
    title: >-
      Eine Wache, die zu WENIG meldet, ist die schlechtere Nullnummer — und
      die ehrliche Zahl braucht sofort eine Ratsche
    tags: [wachen, werkzeuge, ratsche, messen, 2026]
    quelle: >-
      Am 19.09.2026 an tools/ungerufene-wachen.py gemessen: das woertliche
      Muster erkannte 116 von 559 Werkzeugen, die gehaertete Fassung 479 —
      verdeckt waren 363, nicht die im Audit geschaetzten ~86.
    body: |
      ZWEI FEHLER, DIE ZUSAMMENGEHOEREN. Eine Wache kann auf zwei Arten
      nutzlos sein, und die Heilung der ersten erzeugt sofort die zweite.

      FEHLER 1 — SIE MELDET ZU WENIG. tools/ungerufene-wachen.py suchte nach
      WOERTLICHEM `sys.exit(1)` und uebersah damit jedes Werkzeug, das
      `sys.exit(main())` oder — die in Wahrheit haeufigste Form —
      `process.exit(fehler ? 1 : 0)` benutzt. Gemessen: 116 statt 479
      erkannt. Das ist die gefaehrlichere Sorte Fehler, weil niemand sie
      bemerkt: eine Wache, die 9 Luecken meldet, sieht gesund aus. Erst das
      Zaehlen von Hand brachte es heraus.

      DIE HEILUNG: auf die SORTE pruefen, nicht auf den Wortlaut — "kann
      dieses Werkzeug fallen?", je Dateiendung eigene Formen. Ausdruecklich
      OHNE `main` im Muster: sonst haengt das Merkmal am Funktionsnamen und
      verschwindet beim naechsten `haupt()`. Vgl.
      [[wache-auf-sorte-nicht-auf-pfad]].
      Nebenfund beim Bauen: die Regel "Zeile beginnt mit * ist ein Kommentar"
      gilt nur fuer JS/TS — in Shell ist das der case-Alles-Zweig, und ein
      `*) echo ... ; exit 1 ;;` fiel dadurch ganz aus der Wachenliste.

      FEHLER 2 — SIE MELDET JETZT 252 STATT 9. Genau das ist der Moment, in
      dem die ehrliche Wache nach [[dauerrote-wache-ist-keine]] selbst keine
      mehr ist: sie verdeckt den 253. Fund so zuverlaessig, wie sie vorher
      durch Blindheit den 10. verdeckt hat. Die ehrliche Zahl allein ist also
      KEIN Fortschritt.

      DIE ANTWORT IST EINE RATSCHE, und dieser Baum hat das Muster schon:
      tools/shellcheck-ratsche.sh mit seiner Baseline-Datei. Der Bestand darf
      schlecht sein, er darf nur nicht SCHLECHTER werden.

      DABEI DER ENTSCHEIDENDE ENTWURFSFEHLER, DEN MAN LEICHT MACHT: NICHT die
      ZAHL einfrieren, sondern den ZUSTAND JE WERKZEUG. Eine Zahl-Ratsche ist
      blind — faellt eine Wache aus ihrem Laeufer, waehrend eine andere
      eingehaengt wird, bleibt die Summe gleich und der Rost-Fall geht durch.
      Umgesetzt mit drei Zustaenden und einem Rang (laeufer 2, bekannt 1,
      offen 0); die Ratsche blockt, sobald ein Rang SINKT. Damit fangen beide
      Verschlechterungen mit EINEM Vergleich: die neue, nirgends eingehaengte
      Wache (unbekannt gilt als Rang 1) und die aus dem Laeufer gefallene —
      samt dem Schleichweg laeufer->bekannt, denn wer eine laufende Wache
      still entschuldigt statt sie zu reparieren, bekommt kein Gruen.

      UND EINE REGEL, DIE MAN ERST BEIM ZWEITEN NACHDENKEN SIEHT: Wo ein
      bekannter Fehlalarm existiert, muss die Ausnahme den gemessenen Zustand
      SCHLAGEN. Sonst wird die Ratsche in dem Moment falsch rot, in dem
      jemand den Fehlalarm behebt — und eine Ratsche, die eine Verbesserung
      bestraft, wird beim naechsten Mal abgeschaltet.

      GEGENPROBE, die den Beweis erst traegt: fuenf Laeufe mit fuenf
      UNTERSCHEIDBAREN Urteilen (gruen / neu-nirgends rot / eingehaengt gruen
      / aus dem Laeufer gefallen rot / fehlende Baseline Exit 2 mit
      Anleitung). Dazu ein Wegwerf-MESSGERAET ohne Urteil, das in KEINEM der
      fuenf Laeufe auftauchen darf — ohne diese Haelfte bewiese der Treffer
      nichts. Und Fall "aus dem Laeufer gefallen" kausal pruefen (erst mit
      Laeufer-Kopie einfrieren, dann die Kopie weglassen), nicht durch eine
      von Hand verbogene Baseline.

      ENDSTAND 19.09.2026: 174 von 479 Werkzeugen mit Urteil haengen in einem
      Laeufer, 57 sind begruendet entschuldigt, 250 sind eingefrorener
      Bestand — nicht geschlossen, nur gegen Verschlechterung verriegelt.
  - id: erzeugte-deklaration-ist-nicht-tot
    kind: falle
    lang: de
    title: >-
      Bevor du Totes loeschst, frag nicht nur wer es LIEST — frag, wer es
      SCHREIBT
    tags: [toter-code, css, werkzeuge, generator, 2026]
    quelle: >-
      Am 19.09.2026 beim Abtragen von AUDIT-2026-09-19 Rang 6 gemessen:
      --line ist 24x deklariert und 0x benutzt, und trotzdem nicht loeschbar.
    body: |
      DIE UEBLICHE PRUEFUNG AUF TOTEN CODE FRAGT: liest das noch jemand?
      Sie hat eine zweite Haelfte, die regelmaessig vergessen wird: SCHREIBT
      das noch jemand?

      DER FALL. Das CSS-Token `--line` steht 24x in NewDesign/app.css (in
      jedem Farbsatz einmal) und wird nirgends benutzt — nach jeder
      Textsuche also tot, und ein Audit hat es genau so gemeldet. Es ist
      trotzdem nicht loeschbar: tools/farbsaetze-bauen.mjs fuehrt `line` in
      seiner VORLAGE und in ROLLEN, ERZEUGT die 24 Zeilen und prueft sie mit
      --pruefen gegen app.css zurueck. Allein in app.css geloescht waere der
      Erzeuger 24x rot, und der naechste Lauf schriebe alles wieder hinein.
      Die Deklaration ist nicht tot, sie ist BLOCKIERT: ihr Abbau geht nur
      zusammen mit ihrem Erzeuger.

      DIE SUCHE, DIE DAS FINDET, IST NICHT DIE OFFENSICHTLICHE. Im Erzeuger
      heisst das Token `line:` — ein blanker Objektschluessel OHNE die zwei
      Striche. Jede Suche nach `--line` geht daran vorbei. Wer die Verbindung
      finden will, muss nach dem Namen OHNE seine Syntax suchen. Dasselbe
      Muster traf im selben Lauf `#mp-cover`: gelesen wird es ueber eine
      Regex-ALTERNATIVE in einem Werkzeug, ohne Anfuehrungszeichen — und ein
      Helfer `$('id')` machte weitere 44 scheinbar tote id lebendig. Von 48
      gemeldeten toten id blieben 4.

      DIE DREI FRAGEN VOR JEDER LOESCHUNG:
        1. Liest es jemand? (die uebliche Suche)
        2. SCHREIBT oder ERZEUGT es jemand? (Generatoren, Vorlagen,
           Farbsatz-Bauer, Schema-Ableiter)
        3. Wird der Name ZUSAMMENGESETZT? (`marke-${d}`, `pwf-${i}` — siehe
           die Warnung unten)

      ZUSAMMENGESETZTE NAMEN sind die dritte Falle derselben Familie und
      haben hier fast eine lebende Dienst-Plakette gekostet: `.marke-plugin`
      hat keinen einzigen woertlichen Treffer im Code, weil der Name aus
      `marke-` plus Dienstschluessel gebaut wird. Eine Wache, die solche
      Namen ueber eine LISTE schuetzt, ist schon beim Schreiben veraltet —
      die Liste vergass genau diesen siebten Dienst. Richtig ist, die
      moeglichen Werte AUS DEM CODE zu lesen (die Schluessel des
      Dienst-Verzeichnisses, die Laenge der Farbtabelle) und die
      GEGENRICHTUNG zu urteilen: jeder Wert, den eine Zusammensetzung bauen
      kann, MUSS seine Regel haben. Die Loeschkandidaten bleiben dann ein
      Befund ohne Urteil — sonst steht die Wache dauerrot
      ([[dauerrote-wache-ist-keine]]).

      MASS FUER DEN ERTRAG: Von "~1000 Zeilen totem CSS" blieben nach dieser
      Pruefung ~1,6 kB uebrig. Die Schaetzung war um mehr als das Zehnfache
      daneben, weil sie Lesbarkeit mit Benutzung verwechselte. Vgl.
      [[kommentar-und-kompilat-sind-keine-gegenstelle]].
  - id: fetch-verwirft-den-host-kopf-still
    kind: falle
    lang: de
    title: >-
      Mit fetch laesst sich kein Host-Kopf faelschen — die Messung sieht
      gruen aus und hat nichts geprueft
    tags: [messen, sicherheit, server, werkzeuge, 2026]
    quelle: >-
      Am 19.09.2026 beim Nachmessen von AUDIT-2026-09-19 Rang 4 aufgefallen:
      der erste Messversuch mit fetch war wertlos, erst der rohe Sockel
      zeigte das Loch.
    body: |
      WER EINE HERKUNFTS-PRUEFUNG AM Host-KOPF ANGREIFEN ODER PRUEFEN WILL,
      darf dafuer nicht `fetch` nehmen. `Host` ist dort ein VERBOTENER
      Kopfname (forbidden header name): die Laufzeit verwirft ihn STILL —
      keine Ausnahme, keine Warnung, die Anfrage geht mit dem echten Host
      hinaus. Die Messung sieht danach aus, als sei die Weiche zu. Sie hat
      nur nie daran geruettelt.

      RICHTIG: am rohen Sockel schreiben (net.connect und die Kopfzeilen von
      Hand), oder curl mit -H 'Host: ...'. Erst damit wurde sichtbar, dass
      `req.hostname` tatsaechlich den gefaelschten Wert traegt.

      DIE ZWEITE HAELFTE DERSELBEN MESSUNG, und sie ist die lehrreichere:
      Die im Audit genannte Nutzlast `Host: localhost.beliebig` kam NICHT
      durch — ein vorgelagerter Namensriegel weist unbekannte Host-Namen mit
      EIGENER 403 ab. Wer nur auf den Statuscode sieht, haelt die Luecke fuer
      geschlossen. Durch kam `Host: localhost`, weil localhost ein ECHTER
      Name der Box ist:

          curl -H 'Host: localhost' http://<box>:8200/api/monitor

      LEHRE: EINE 403 BEWEIST NICHT, DASS DIE GEPRUEFTE REGEL GEGRIFFEN HAT.
      Sie kann von einer ganz anderen Schicht kommen. Wer eine Abweisung
      misst, muss belegen, WELCHE Stelle abgewiesen hat — sonst prueft er
      eine Weiche und misst einen Riegel davor. Dieselbe Falle aus der
      Test-Sicht: [[die-rueckmeldung-allein-bezeugt-den-riegel-nicht]].

      UND DIE URSACHE DAHINTER, die den Fix erst richtig macht: `req.hostname`
      ist CLIENT-WARE. Ein Kopf kann keine Herkunft beweisen. Die Herkunft
      steht am Sockel (`req.socket.remoteAddress`), und die Pruefung dafuer
      existierte in diesem Baum laengst als getestete Funktion — sie wurde
      nur an genau einer von fuenf Stellen benutzt, die uebrigen waren
      Handnachbauten mit einer zu engen Adressliste. Vgl.
      [[vierzehn-kopien-und-die-abweichung-ist-der-fehler]].
  - id: umbau-ohne-wachen-nachzug-macht-die-wache-zur-luegnerin
    kind: falle
    lang: de
    title: >-
      Wer den Weg umbaut und die Wache stehen laesst, bekommt eine Wache, die
      die ABSICHT als Fehler meldet
    tags: [wachen, ablauffehler, audit, test, 2026]
    quelle: >-
      Am 19.09.2026 gemessen: sieben rote Befunde des Haupt-Laeufers, die ein
      Audit als "ECHT und reproduzierbar" einstufte, waren 7 von 7
      Wachen-Fehler. Der Produktivcode blieb unangetastet.
    body: |
      DIE GEFAEHRLICHSTE SORTE VERALTETER WACHE meldet nicht zu wenig (das
      waere [[wache-mit-blindem-fleck-braucht-ratsche]]) und nicht zu viel —
      sie meldet die ABSICHT des neuen Codes als Fehler. Und weil sie
      reproduzierbar rot ist, sieht sie glaubwuerdiger aus als jede echte
      Wache.

      DER FALL, dreifach am selben Tag: `tools/neu-sprechen-schau.mjs` wurde
      zuletzt am 31.08.2026 angefasst. Am 04.09.2026 fielen ZWEI Umbauten:
        * E123 — die Box spricht selbst: die Daempfung setzt jetzt der
          SERVER (`ansageAbspielen` -> `tonDaempfenSetzen(true/false)` samt
          Notloeser); die Seite schickt ihr `POST /api/ton/daempfen` mit
          Absicht nicht mehr.
        * "Bild spielt, Text spricht" (woertlicher Betreiberwunsch): die
          Silben-Einblendung haengt seitdem an der TEXTZONE, das Bild spielt
          sofort.
      Beide Umbauten fassten app.js, server.ts und EINE Nachbarprobe an — die
      Wache nicht. Sie tippte weiter die ganze Kachel an (also das Bild) und
      meldete sechs Abweichungen. Jede einzelne war die neue, gewollte
      Bedienung.

      DAZU DERSELBE FEHLER IN EINER ZWEITEN WACHE: Seit E95/V Stufe 2
      (31.08.2026) startet eine Kachel ueber `POST /api/spielen` statt ueber
      `/player/…`. `tools/wischrand-schau.mjs` schrieb nur `/player/` mit und
      war damit fuer ihre EIGENE Frage ("hat dieser Zug eine Kachel
      gestartet?") blind. Was sie stattdessen zaehlte, war
      `${SPIELER}/state` aus `dienstHolen()` — eine ZUSTANDSABFRAGE, als
      Startbefehl gewertet, deterministisch 1 in jedem Lauf.

      MERKSATZ DARAUS: EINE ABFRAGE IST KEIN BEFEHL. Wer Wege mitschreibt,
      trennt Lesen von Schreiben, sonst zaehlt jedes Nachfragen als Tat.

      WORAN MAN ES ERKENNT, BEVOR MAN DEN CODE "REPARIERT":
        1. `git log -1 --format=%ad -- <die Wache>` gegen
           `git log -1 --format=%ad -- <das Geprueefte>`. Liegt die Wache
           MONATE zurueck, ist sie zuerst verdaechtig, nicht der Code.
        2. Welche Umbauten liegen dazwischen? `git log --oneline <wachen-datum>..HEAD -- <das Geprueefte>`
           Findet man dort einen Umbau, der genau den gemessenen Weg
           betrifft, ist der Fall entschieden.
        3. Ein roter Befund, der bei JEDEM Lauf exakt dieselbe Zahl zeigt
           (hier: "1 erwartet 0"), ist eher eine Konstante als ein Fehler.
           Echte Nebenlaeufigkeitsfehler flackern.

      UND DIE LEHRE FUER DEN UMBAU SELBST: Wer einen Weg ersetzt, sucht im
      selben Zug nach den Wachen, die den ALTEN Weg mitschreiben. Der Zug
      vom 04.09. hat eine Nachbarprobe nachgezogen und zwei vergessen — es
      war also niemandem unbekannt, dass es solche Wachen gibt. Dieselbe
      Familie, von der Naht her gedacht:
      [[umfangsdrift-kehrt-wieder-weil-die-naht-an-einem-aufrufer-haengt]].

      WAS ES GEKOSTET HAETTE: Das Audit vom 19.09. stufte diese sieben
      Befunde als "(a) ECHT und reproduzierbar" ein und empfahl, sie zu
      beheben. Wer dem gefolgt waere, haette den Produktivcode an eine
      kaputte Wache angepasst und damit zwei bewusste Design-Entscheidungen
      rueckgaengig gemacht — eine davon ein woertlicher Betreiberwunsch.
  - id: wache-ohne-gegenstand-bricht-ab-statt-gruen
    kind: regel
    lang: de
    title: >-
      Verliert eine Wache ihren Gegenstand, bricht sie mit ANLEITUNG ab — nie
      gruen, nie dauerrot
    tags: [wachen, werkzeuge, doku-drift, messen, 2026]
    quelle: >-
      Am 19.09.2026 an vier Werkzeugen gemessen, deren Gegenstand mit dem
      PHP-Ausbau E47 (19.08.2026) verschwunden war oder deren Bezugszahl
      gewandert ist.
    body: |
      DREI AUSGAENGE, UND NUR EINER IST RICHTIG, wenn das Geprueefte nicht mehr
      da ist:
        * GRUEN ist Betrug — die Wache sieht aus, als pruefte sie.
        * DAUERROT ist nutzlos — nach [[dauerrote-wache-ist-keine]] verdeckt
          sie den naechsten echten Fund.
        * ABBRUCH MIT ANLEITUNG ist richtig: sie sagt, dass sie nichts mehr
          messen kann, und was zu tun ist.
      Und wer das nachprueft, prueft MIT, dass die Anleitung wirklich
      dasteht — nicht nur, dass der Ausgangswert stimmt. Ein Abbruch ohne
      Wegweiser wird beim naechsten Lauf wieder ignoriert.

      DESHALB MUSS EINE PROBE DEN GENAUEN AUSGANG ERWARTEN, nicht bloss
      "nicht null": 1 = Fund, 2 = Abbruch. Ein Abbruch, der als Fund
      durchginge, ist genau derselbe Fehler noch einmal — die Probe glaubt
      dann, sie habe etwas gemessen.

      ZWEITER TEIL, UND DER KOSTET MEHR ZEIT: EINE DATIERTE ZUSTANDSAUSSAGE
      IST KEIN DAUERROT-GRUND. Eine Wache mass "51 von 79", in vier
      Prosastellen stand "44 von 70", und sie meldete das seit Wochen als
      Luecke. Nachgezaehlt war die MESSUNG richtig — und die Prosa auch: sie
      war die datierte Aufnahme vom 30.08.2026. Es gab nie einen Fehler,
      nur zwei Zeitpunkte.
      Die Heilung ist nicht, die Zahl nachzuziehen (das macht die Wache bei
      jeder Aenderung rot, und genau deshalb wurde sie nie eingehaengt),
      sondern die FORM zu pruefen:
        R1  jeder Ort fuehrt ueberhaupt einen Stand
        R2  jeder Stand ist DATIERT
        R3  Staende mit demselben Datum sind identisch
      Die heutige Messung wird geliehen und als DRIFT gedruckt, nicht als
      Urteil. Vgl. [[wache-auf-sorte-nicht-auf-pfad]].

      DRITTER TEIL, EIN MESSFEHLER, DEN NIEMAND VERMUTET: Ein "Ort", den
      eine Wache in ihrer eigenen Liste fuehrt, gehoert einmal gegen die
      Geschichte geprueft, bevor man seine fehlende Zahl abtraegt:

          git log -S '<der genannte Name>' -- <die genannte Datei>

      War das leer, hat dieser Ort die Zahl NIE getragen. Ein Viertel der
      Dauerrot-Meldungen dieser Wache war die Abwesenheit einer Angabe an
      einer Stelle, die es so nie gab.

      UND DIE URSACHE DAHINTER, die alles erklaert: der Zaehler, der die
      Zahl haette drucken sollen, hing in KEINEM Laeufer. Die Zahl wurde
      nie ausgegeben, deshalb fiel die Drift ueber sieben Abschnitte
      niemandem auf. Siehe [[wache-mit-blindem-fleck-braucht-ratsche]].

      ZULETZT, ZUM AUFRAEUMEN SELBST: Bevor ein "Zwilling" geloescht wird,
      misst man, was NUR er findet. Von zwei Kinderzeit-Proben war die
      aeltere formal die schlechtere (gibt immer 0 zurueck, kein
      Erreichbarkeitsriegel) — und fand trotzdem drei Dinge allein, darunter
      einen Zaehler im BROWSERSPEICHER, also die einzige Frage, die der
      Server gar nicht beantworten kann. Erst einziehen, dann loeschen.
  - id: aufraeum-runde-2026-09-19-zweite-welle
    kind: note
    lang: de
    title: >-
      Zweite Welle 19.09.2026 — der stille VPN-Wettlauf, die dritte
      Paketliste, und zwoelf weitere Audit-Berichtigungen
    tags: [audit, wachen, vpn, installer, korrektur, 2026]
    quelle: >-
      Vier parallele Bearbeiter mit disjunkter Dateihoheit, jeder Befund vor
      dem Eingriff selbst nachgemessen. Fortsetzung von
      [[aufraeum-runde-2026-09-19]].
    body: |
      DER SCHWERSTE EINZELFUND: DER VPN-WETTLAUF IST NICHT LAUT, SONDERN
      STILL. Zwei gleichzeitige Uebernahmen teilten sich eine Zwischendatei
      MIT privatem Schluessel. Erwartet wurde ein Fehlschlag — gemessen
      meldeten BEIDE Anfragen 200, und `install` trug ZWEIMAL DIESELBE (die
      fremde) Konfiguration nach /etc/wireguard. Nichts im Journal. Wer nur
      auf Statuscodes sieht, findet so einen Schaden nie.
      Der Test dazu musste zweimal gebaut werden: die erste Fassung prueste
      "beide antworten 200" und blieb unter Sabotage GRUEN. Erst die
      Mitschrift des INHALTS, den `install` in der Hand hatte, misst den
      Schaden.

      ZWEITER FUND MIT FOLGEN FUER FRISCHE KARTEN: `wireguard-tools` fehlte
      nicht nur dem Updater (Rang 1 des Audits), sondern AUCH allen
      Installer-Rezepten. Der VPN-Heimweg E30 konnte damit auf einer frisch
      installierten Karte genauso wenig laufen wie auf einer Bestandsbox.
      Dabei kam heraus, dass das Rezept eine DRITTE Paketliste fuehrt
      (`mupibox.yaml`, von Hand), die keine der bestehenden Wachen sah —
      neben den zwei Shell-Listen in autosetup und Updater.
      Wichtig fuer die Wache darueber: die drei Listen sind NICHT
      mengengleich und sollen es nicht sein (32 Pakete nur in den
      Shell-Wegen, 15 nur im Rezept — anderer Ton-, Kiosk- und Touch-Aufbau).
      Eine Gleichheitswache waere ab Minute eins dauerrot; geprueft wird nach
      MERKMAL.

      DRITTER FUND, EIN AUSFALL OHNE FEHLERMELDUNG: Die Beschreibung des
      Installer-Pakets nannte vier von sieben Stuecken und verschwieg
      `plugin-laufwerk.js`. Fehlt diese Datei, startet die Box tadellos und
      laedt STILL kein Plugin. Die Einspiel-Pruefung deckt jetzt alle fuenf
      Stuecke ab, fuer die der Bau hart abbricht.

      WEITERE BERICHTIGUNGEN AM AUDIT (alle gemessen):
        * Der atomare Zaehler ist an 13 Stellen nachgeruestet, nicht elf.
        * `mode` gab es in atomar.ts schon — und es reicht NICHT: die umask
          beschneidet `writeFile({mode})`, 0664 kommt als 0644 an. Es fehlte
          ein `chmod`.
        * "Die Schwesterroute schickt nur die Meldung" ist falsch: beide
          Schreibweisen lecken dieselben absoluten Pfade, `String(e)` stellt
          nur `Error: ` voran. Angleichen haette nichts behoben.
        * Die doppelte /mupihat-Uebersetzung steht gar nicht im Server,
          sondern zweimal in app.js.
        * Bei der Helligkeitswache war der Vorwurf "sagt gruen" falsch — sie
          brach mit 2 ab, nur ohne ein Wort dazu, was zu tun ist.
        * Die "Zwillinge" der Vorschau-Werkzeuge sind keine: eine
          `ng serve`-Weiterleitung auf den Quelltext und ein eigener Server
          fuer das gebaute Buendel. Beide in Gebrauch.

      UND DIE LEHRE, DIE UEBER DEN TAG HINAUSGEHT: In beiden Wellen hat die
      GEGENPROBE fuenfmal einen Fehler in der FRISCH GEBAUTEN WACHE gefunden,
      nicht im geprueften Code — jedes Mal, weil sie gruen blieb, wo sie rot
      sein musste. Ein zaehlender Test, der kumulativ statt auf Zuwachs
      misst, ist dafuer das wiederkehrende Muster
      ([[die-rueckmeldung-allein-bezeugt-den-riegel-nicht]]). Wer eine Wache
      baut und ihre Gegenprobe ueberspringt, hat keine Wache gebaut, sondern
      eine Meinung aufgeschrieben.
  - id: nichtfinden-beweist-nichts-ohne-positivkontrolle
    kind: regel
    lang: de
    title: >-
      Wer beweisen will, dass etwas NICHT da ist, muss zeigen, dass seine
      Methode es finden wuerde
    tags: [messen, gegenprobe, toter-code, geraet, 2026]
    quelle: >-
      Am 19.09.2026 beim Faellen von sieben Server-Routen angewandt (Rang 7);
      am selben Tag zweimal am eigenen Netz-Scan durchgefallen.
    body: |
      EIN NEGATIVES MESSERGEBNIS IST NUR SO VIEL WERT WIE DAS MESSGERAET.
      „Ich habe keinen Rufer gefunden" heisst ohne Zusatz nur: ich habe nicht
      gefunden. Ob das an der Abwesenheit lag oder am Werkzeug, steht da
      nicht.

      DIE POSITIVKONTROLLE IST BILLIG UND ENTSCHEIDET ALLES: Lass dieselbe
      Suche einmal auf etwas los, von dem du WEISST, dass es da ist. Findet
      sie es, traegt ihr Schweigen etwas. Findet sie es nicht, hast du
      gerade nichts gemessen.

      SO GING ES RICHTIG (Rang 7, sieben Routen ohne Klienten): Die
      dateisystemweite Suche auf der Box fand nebenbei, dass
      `www-admin/chunk-*.js` zwei Wege ruft, die im Repo NULL Rufer haben.
      Damit war belegt: diese Methode KANN Fremdrufer finden. Ihr Schweigen
      zu den sieben war danach ein Befund.
      Dazu ein zweiter, unabhaengiger Weg ueber Nebenwirkungen statt ueber
      Text: eine Datei von 3 Byte mit dem mtime des Einrichtungstags beweist,
      dass die Route, die sie schreiben wuerde, dort nie gerufen wurde.

      UND WAS DABEI SONST NOCH SCHIEFGEHT:
        * DAS JOURNAL TAUGT OFT NICHT. Hier: der Dienst hatte nur einen Boot,
          und es gibt gar keine Request-Protokollierung — Routentreffer
          werden nie festgehalten. Wer im Journal nichts findet, hat dann
          nicht „keine Rufe" gemessen, sondern eine fehlende Messstelle.
        * DIE ALTEN BUENDEL MITSUCHEN. Auch `.zurueck`-Ruecklagen und
          ausgelieferte Stände: ein Geraet im Haus kann eine alte Fassung im
          Speicher halten und ruft dann Wege, die im Baum laengst niemand
          mehr nennt.
        * EINE LEBENDPROBE, DIE IMMER GELINGT, PRUEFT NICHTS. Im selben Lauf
          gefunden: eine `curl`-Probe auf eine Route endete auch auf
          `/api/gibtsnicht` mit 0, weil ein Catch-all jeden Pfad mit 302
          beantwortet. Sie stand jahrelang als Beleg im Installer-Rezept.
        * „NICHTS GEFUNDEN" UND „NICHT GEMESSEN" MUESSEN VERSCHIEDEN
          AUSSEHEN. Eine Wache derselben Runde meldete „keine Stelle auf der
          Box", waehrend ihr `grep` in Wahrheit an fehlenden Leserechten mit
          2 abgebrochen war.

      DASSELBE AM EIGENEN LEIB, am selben Tag: Ein Subnetz-Scan meldete
      zweimal die falsche Antwort — erst „Port offen" (das traf ein fremdes
      Apache-Geraet), dann „antwortet JSON" (das traf den eigenen Rechner,
      der selbst ein Backend faehrt und mit `{"ok":false,…}` antwortete).
      Beide Male waere dieselbe Frage die Rettung gewesen: GIBT ES ETWAS,
      DAS GENAUSO ANTWORTET, OHNE DAS GESUCHTE ZU SEIN? Und beim dritten
      Lauf, der wirklich nichts fand, fehlte weiterhin die Kontrolle gegen
      einen bekannten Treffer.

      Verwandt, aus der Test-Sicht:
      [[die-rueckmeldung-allein-bezeugt-den-riegel-nicht]].
  - id: unsichtbares-bedienteil-klasse-ans-ereignis
    kind: falle
    lang: de
    title: >-
      Eine Klasse, die Hintergrund und Schatten wegnimmt, gehoert ans
      `load`-Ereignis — sonst steht dort ein Griff, den niemand sieht
    tags: [ui, css, bedienung, wachen, 2026]
    quelle: >-
      Am 19.09.2026 beim Bauen des Figur-Schalters am Fortschrittsgriff
      (Betreiber-Entscheid, Rang 6) — die Falle wurde beim Verdrahten
      gesehen, nicht erlitten.
    body: |
      DIE GEFAEHRLICHSTE SORTE OBERFLAECHENFEHLER macht nichts kaputt. Die
      Seite laedt, die Geometrie stimmt, kein Test schlaegt an — und an der
      Stelle, auf die der Finger zielt, ist nichts zu sehen.

      DER FALL: `.mit-figur` nimmt dem Fortschrittsgriff Hintergrund UND
      Schatten und macht ihn 48x48, weil dort ein Bild liegen soll. Die
      Klasse ist also nur so lange harmlos, wie das Bild wirklich da ist.
      Wird sie gesetzt, sobald ein PFAD feststeht, dann steht sie auch:
        * waehrend das Bild noch laedt (auf einem Pi nicht kurz),
        * wenn die Datei fehlt,
        * wenn der Server auf die Bildanfrage anders antwortet als auf die
          Auskunft.
      In allen drei Faellen: ein unsichtbarer Griff, der sich weiterhin
      greifen und ziehen laesst.

      DIE REGEL: DIE KLASSE HAENGT AM `load`-EREIGNIS, NIE AM PFAD. Der
      Maler darf sie nur ENTFERNEN (der Rueckweg auf die schlichte Form
      braucht nichts abzuwarten), setzen darf sie allein der Zuhoerer. Und
      `error` nimmt sie wieder weg.

      DAS PASST ZU DER REIHENFOLGE, die in diesem Baum ohnehin gilt: erst
      `hidden` setzen, DANN `src` — andersherum stuende zwischen dem Setzen
      und dem Fehlschlag ein zerbrochenes Bildzeichen auf dem Schirm.
      Beides ist dieselbe Denkweise: der sichtbare Zustand folgt dem
      Ereignis, nicht der Absicht.

      WORAUF MAN BEIM RUECKFALLBILD AUFPASST: Ein Helfer, der bei leerer
      Wahl auf ein Ersatzbild zurueckfaellt, ist fuer ein Kopfzeichen
      richtig und fuer ein Bedienteil falsch. Der Griff soll ohne Figur sein
      eigener Griff bleiben, nicht bei jedem Kind ohne Figur dasselbe
      Ersatzbild tragen. Zwei Pfad-Helfer nebeneinander sind hier kein
      Duplikat, sondern zwei Antworten auf zwei Fragen — das gehoert
      hingeschrieben, sonst raeumt es die naechste Sitzung zusammen.

      DIE WACHE DAFUER IST STATISCH, und das ist Absicht: Ein Browser-Lauf
      koennte nur zeigen, dass es HEUTE gutgeht — mit einem Profil, das eine
      Figur hat, und einem Bild, das laedt. Der Schaden entsteht im
      Gegenteil. Die Naht selbst ist die Aussage, nicht ein gelungener
      Durchlauf. Sie prueft deshalb: der Maler setzt die Klasse NICHT, der
      `load`-Zuhoerer setzt sie, der `error`-Zuhoerer nimmt sie weg — und
      zum Schluss ueber die GANZE Datei, dass sie sonst nirgends gesetzt
      wird. Eine Positivpruefung allein uebersaehe die zweite, ungewachte
      Stelle daneben — eine Gegenprobe, die GRUEN bleibt, ist nicht
      bestanden, sondern der Fund. Verwandt:
      [[die-rueckmeldung-allein-bezeugt-den-riegel-nicht]] und
      [[nichtfinden-beweist-nichts-ohne-positivkontrolle]].
  - id: zeuge-ist-gruen-weil-der-fehler-da-ist
    kind: falle
    lang: de
    title: >-
      Ein Zeuge kann gruen sein, WEIL der Fehler da ist — und faellt, sobald
      man ihn behebt
    tags: [test, wachen, gegenprobe, ablauffehler, 2026]
    quelle: >-
      Am 19.09.2026 zweimal an einem Tag getroffen: einmal im e2e-Fall der
      Buehne, einmal an einer Wache, die ihren Gegenstand nachbildete.
    body: |
      DIE UEBLICHE SORGE IST, DASS EIN TEST ETWAS NICHT MERKT. Die teurere
      Lage ist umgekehrt: Er MERKT es, hat es festgeschrieben, und meldet die
      BEHEBUNG als Fehler.

      FALL 1 — DER ZEUGE HIELT DEN FEHLER FEST. Ein e2e-Fall sicherte zu: „bei
      `aus` nimmt die Buehne KEINEN Platz". Der Betreiber hatte zwoelf Tage
      vorher das Gegenteil verlangt („bei Aus sollen sie nicht runtergehen"),
      und der Code setzte den Platz auch — aber erst ab der ZWEITEN Wahl, weil
      die Reservierung hinter einer fruehen Rueckkehr stand. Der Fall lud
      vorher NEU und traf damit genau die Luecke. Er war gruen, weil der
      Fehler da war; die Behebung machte ihn rot.

      WORAN MAN ES ERKENNT: Ein Test wird rot, NACHDEM man etwas repariert
      hat, und seine Zusicherung beschreibt den Zustand VOR der Reparatur.
      Dann ist die Frage nicht „was habe ich kaputtgemacht", sondern „welchen
      Stand hat dieser Fall festgenagelt, und gilt er noch".

      DIE NEUE ZUSICHERUNG SOLL SCHAERFER SEIN ALS DIE ALTE, sonst hat man nur
      eine Zahl getauscht. Hier: nicht „bei aus ist die Hoehe 0", sondern „die
      Hoehe ist IMMER DIESELBE" — das ist genau das, was der Betreiber meint,
      wenn er sagt, es soll nicht huepfen.

      FALL 2 — DIE WACHE BILDETE IHREN GEGENSTAND NACH. Ein Werkzeug prueste,
      ob ein Shell-Skript den Profilordner zerstoert. Es enthielt dafuer eine
      ABSCHRIFT des Skripts mit festem `renameSync`. Solange das Skript so
      aussah, war die Abschrift richtig — und in dem Augenblick, in dem das
      Skript auf den richtigen Schreibweg umgestellt wurde, meldete die Wache
      weiter den alten Schaden. EINE WACHE, DIE IHREN GEGENSTAND NACHBILDET
      STATT IHN ZU LESEN, MISST AM ENDE NUR NOCH SICH SELBST.
      Geheilt, indem sie das echte Skript liest und nach der SORTE des
      Schreibwegs urteilt (folgt dem Verweis / ersetzt ihn) — und bei
      fehlendem Skript abbricht statt gruen zu sagen
      ([[wache-ohne-gegenstand-bricht-ab-statt-gruen]]).

      UND DIE DRITTE FORM, im selben Fall gefunden: EIN WARTEN AUF ETWAS, DAS
      IMMER GILT, IST KEIN WARTEN. Der e2e-Fall wartete darauf, dass eine
      Klasse erscheint — die nach der Behebung von Anfang an dasteht. Das
      Warten kehrte sofort zurueck, noch bevor die Einstellung angewandt war,
      und der Fall mass den vorigen Zustand. Gewartet wird auf das, was sich
      WIRKLICH aendert.

      Verwandt: [[umbau-ohne-wachen-nachzug-macht-die-wache-zur-luegnerin]] —
      dort meldet die Wache die ABSICHT des neuen Codes als Fehler; hier
      meldet sie die BEHEBUNG als Fehler. Dieselbe Familie, zwei Richtungen.
  - id: prosa-ist-kein-ruf
    kind: regel
    lang: de
    title: >-
      Ein Name in einem Kommentar ist kein Aufruf — Erreichbarkeit braucht
      Wortform UND grammatische Stellung
    tags: [wachen, werkzeuge, erreichbarkeit, messen, 2026]
    quelle: >-
      Am 19.09.2026 geschlossen, nachdem der blinde Fleck am selben Tag
      zugeschlagen hatte: 228 Nennungen fielen weg, kein einziger echter Ruf.
    body: |
      DER FEHLER: Eine Erreichbarkeits-Pruefung hielt ein Werkzeug fuer
      gerufen, sobald sein NAME irgendwo in einer gerufenen Datei vorkam —
      auch als blosse Prosa in einem Docstring.

      WAS ES GEKOSTET HAETTE: Eine gruene, schnelle Wache einzuhaengen liess
      prompt eine ZWEITE als gedeckt gelten, allein weil deren Name in einem
      Kommentar der ersten stand. Jene zweite war gemessen ROT und lief
      nirgends — ihr Gegenstand war Wochen vorher geloescht worden.
      Eingehaengt haette man eine KAPUTTE Wache als „laeuft" eingefroren, und
      wer spaeter den blinden Fleck schloss, bekaeme dafuer eine rote Ratsche.

      DIE PRUEFUNG BRAUCHT ZWEI MERKMALE, und beide muessen stimmen:
        1. WORTFORM — der Pfad ist ein ganzes Wort, kein Satzteil. Steht er in
           einem Literal, darf darin nichts als ein Pfad stehen (kein
           Leerzeichen). Das toetet Docstrings, `print()`-Hinweise und
           `echo`-Saetze, egal welche Verben darin vorkommen.
        2. STELLUNG — dort, wo die Sorte einen Wert weiterreicht: `.sh` als
           eigenes Wort einer Kommandozeile; `.py`/`.mjs`/`.ts`/`.js` als
           vollstaendiges Argument in der Klammer eines Rufs, hinter
           `import … from` / `require(`, oder als Bindung an einen Namen.

      AUF DIE SORTE, NICHT AUF EINE VERBLISTE: Im Muster steht kein einziger
      Starter-Name — kein `subprocess.run`, `spawn`, `execSync`, `npx`.
      Gefragt ist die grammatische Stellung. Ein neuer Starter faellt
      automatisch darunter; eine Verbliste waere bei ihm veraltet und haette
      eine gute Wache falsch rot gemeldet. Dieselbe Ueberlegung wie
      [[wache-auf-sorte-nicht-auf-pfad]].

      DIE GEGENRICHTUNG IST DIE EIGENTLICHE ARBEIT, und sie ist der Grund,
      warum „Zeichenketten pauschal ausschliessen" falsch waere: EIN ECHTER
      RUF STEHT HIER OFT SELBST IN EINER ZEICHENKETTE —
      `subprocess.run(["bash", "tools/x.sh", …])`. Und zwei Rufe liefen ueber
      eine VARIABLE (Pfad in eine Konstante, dann `importlib`); ohne die
      Bindungs-Regel waeren zwei LAUFENDE Wachen falsch rot gemeldet worden.
      Umgekehrt bleiben Sammlungen draussen — Ausnahmelisten und
      Prueferlisten NENNEN einen Pruefer, sie rufen ihn nicht.

      GEMESSEN WIRD IN DREI RICHTUNGEN, sonst beweist der Treffer nichts:
        * Prosa im Docstring einer gerufenen Datei -> weiter gemeldet
        * echter Ruf ueber eine Zeichenkette       -> Meldung verschwindet
        * Datenliste `["tools/…"]`                 -> weiter gemeldet
      Die dritte ist die Gegenkontrolle: ohne sie waere „Anfuehrungszeichen
      zaehlen" schon die Loesung, und sie ist es nicht.

      WAS DANACH SCHLECHTER AUSSIEHT UND EHRLICHER IST: 27 Werkzeuge fielen
      von „haengt in einem Laeufer" auf „haengt nirgends". Das ist keine
      Verschlechterung im Baum, sondern die Korrektur einer Fehlbuchung —
      und drei Meldungen der Sorte „BEKANNT, ABER GERUFEN" verschwanden, denn
      sie WAREN dieser Fehlalarm.
      Vgl. [[wache-mit-blindem-fleck-braucht-ratsche]].
  - id: was-eine-textsuche-am-zeichen-nicht-sieht
    kind: falle
    lang: de
    title: >-
      Ein Zeichen als Escape geschrieben ist fuer jede Textsuche unsichtbar —
      und eine Bedingung sieht aus wie ein Name
    tags: [wachen, zeichen, box, suche, 2026]
    quelle: >-
      Am 19.09.2026 beim Abtragen von zwei gemeldeten Zeichen gefunden: es
      waren drei, und das dritte hatte die Wache nie gesehen.
    body: |
      DIE BOX HAT KEINE EMOJI-SCHRIFT. Ein Zeichen ohne Schrift erscheint dort
      als leeres Rechteck, und ein leeres Rechteck sieht aus wie Absicht.
      Deshalb gibt es eine Karte mit SVG-Ersatz und eine Wache, die im
      Quelltext nach Zeichen ohne Schrift sucht. Sie hat zwei gemeldet.

      DAS DRITTE STAND ALS ESCAPE DA. In der Quelle lauter ASCII
      (Backslash-u mit geschweiften Klammern), auf dem Schirm ein leeres
      Rechteck — DIREKT NEBEN einem der beiden gemeldeten, in derselben
      Kopfzeile. Haette man nur die zwei gerichtet, stuende jetzt ein
      gezeichnetes Sinnbild neben einem leeren Rechteck: schlimmer als vorher,
      weil der Unterschied dann nach Absicht aussieht.

      MERKSATZ: EINE TEXTSUCHE NACH ZEICHEN FINDET NUR, WAS ALS ZEICHEN
      DASTEHT. Wer sie baut, muss die Escape-Formen der Sprache mitlesen —
      oder wenigstens wissen, dass er sie nicht liest. Gegengeprueft wird das
      nicht im Quelltext, sondern gegen die SCHRIFTEN DES GERAETS: welche
      Punkte hat die Box wirklich?

      DER ZWEITE FEHLER DERSELBEN WACHE, umgekehrte Richtung: Sie las hinter
      dem Feld `zeichen:` jede Zeichenkette als Namen — auch die BEDINGUNG
      eines Ternaers (`zeichen: f.art === 'geheim' ? 'a' : 'b'`). `geheim` ist
      dort eine Feldart, kein Sinnbild. Die Wache meldete es als fehlenden
      Karteneintrag; einen anzulegen waere eine Luege gewesen. Ein Dauerbefund
      dieser Art verdeckt den naechsten echten
      ([[dauerrote-wache-ist-keine]]).

      DIE ALLGEMEINE FORM, die auch anderswo gilt: Eine Wache, die aus
      QUELLTEXT Werte liest, muss die Grammatik der Sprache mitdenken. Was sie
      sonst findet, ist eine Zeichenfolge an der richtigen Stelle — und das
      ist etwas anderes als ein Wert. Vgl. [[prosa-ist-kein-ruf]], wo
      derselbe Denkfehler in die andere Richtung faellt.

      NEBENBEI, ZUM ERSATZ SELBST: Zwei der drei kosteten NULL neue Bytes, weil
      die passenden Karteneintraege laengst existierten. Wer ein Sinnbild
      braucht, sieht erst in der Karte nach; ein zweites Bild fuer denselben
      Gedanken ist schon der Anfang des Auseinanderlaufens.
  - id: abbruch-gibt-die-leihgabe-nicht-zurueck
    kind: falle
    lang: de
    title: >-
      Ein abgebrochener Lauf gibt zurueck, was sein `finally` kennt — und das
      Aufraeumen kann den Schaden weitertragen
    tags: [vorschau, wachen, signale, pruefstrecke, 2026]
    quelle: >-
      Am 19.09.2026 als Ursache von fuenf roten Schritten im Haupt-Laeufer
      gemessen und geschlossen.
    body: |
      DIE LAGE: Mehrere Wachen teilen sich eine laufende Vorschau
      ([[vorschau-wird-geliehen]]). Wer sie borgt, merkt sich ihren Zustand
      und legt ihn am Ende zurueck. Das haelt — solange der Lauf zu Ende
      laeuft.

      ZWEI DINGE GEHEN DABEI SCHIEF, und beide sehen wie Aufraeumen aus.

      ERSTENS: EIN `finally` LAEUFT BEI EINEM SIGNAL NICHT. Bei Strg-C oder
      `timeout 400 node …` kommt ein SIGTERM von aussen, und der Vorgabeumgang
      ist „sofort sterben". Wer dagegen einen Signalgriff baut, muss darin
      ALLE Leihgaben zurueckgeben — hier kannte er nur eine von zweien
      (einen eigenen Browser), die geliehene Vorschau nicht. Ein einziger
      abgebrochener Lauf liess sie in einem kaputten Zustand stehen, und der
      naechste Nutzer bekam von einem Endpunkt HTTP 500 mit einem nackten
      Wort statt JSON. Fuenf rote Schritte gingen darauf zurueck.

      ZWEITENS, UND DAS IST DIE FEINERE: DAS AUFRAEUMEN TRUG DEN SCHADEN
      WEITER. Die Wache, die danach kam, nahm einen Schnappschuss — vom
      bereits kaputten Zustand — und legte ihn am Ende brav wieder hin. Ihr
      `finally` lief vollstaendig und machte es dadurch schlimmer.
      Wer nur „beim Verlassen zuruecksetzen" baut, konserviert also, was er
      vorgefunden hat.

      DARAUS FOLGT DIE REGEL: DEN AUSGANGSPUNKT SELBST HERSTELLEN, nicht bloss
      den vorgefundenen zuruecklegen. Am Anfang messen, ob die Lage die
      erwartete ist; ist sie es nicht, das BENENNEN, selbst stellen und
      gegenpruefen — und am Ende den kaputten Wert nicht zurueckgeben.
      Auszuweichen (eigene Vorschau auf eigenem Port) rettet nur den eigenen
      Lauf: die Nachbarn messen weiter gegen die geteilte, und wer ausweicht,
      hoert auf, ihr Zeuge zu sein.

      EINE STELLE FUER ALLE LEIHGABEN, NICHT ZWEI HOERER: Zwei Signalgriffe,
      die beide `process.exit()` rufen, sind ein Wettlauf — der erste beendet
      den Prozess, waehrend der zweite noch zurueckgibt. Richtig ist EIN Paar
      Hoerer, das alle offenen Leihgaben einsammelt und mit `allSettled` auf
      ALLE wartet, bevor es aussteigt. Nebenbei loest das ein zweites Problem:
      frueher hing je Aufruf ein Hoererpaar, und ab dem elften warnte node
      ueber ein Leck, das keines war.
      GEMESSEN: asynchrone Arbeit in einem Signalgriff LAEUFT zu Ende, wenn
      der Ausstieg im `finally` der Sammelzusage steht. Das ist pruefbar und
      war die Annahme, auf der der ganze Umbau ruhte.

      UND EINE MESSFALLE AUF DEM WEG DORTHIN: Die erste Gegenprobe meldete
      falsch rot, weil `kill -TERM $(pgrep -f "probe.mjs")` die eigene Shell
      mittrifft — deren Kommandozeile enthaelt den Namen. Man schickt sich das
      Signal selbst. Mit der echten Kind-PID lief sie durch. Fast haette das
      einen richtigen Umbau als gescheitert verworfen; gerettet hat, die
      ANNAHME isoliert zu pruefen statt am grossen Aufbau weiterzuraten.
  - id: aufraeum-runde-2026-09-19-dritte-welle
    kind: note
    lang: de
    title: >-
      Dritte Welle 19.09.2026 — ein Kind verlor seine Hoerpositionen, und eine
      Begruendung berief sich auf einen Eintrag, den es nicht gibt
    tags: [audit, wachen, profile, korrektur, 2026]
    quelle: >-
      Fortsetzung von [[aufraeum-runde-2026-09-19-zweite-welle]]; alle Funde
      am Baum gemessen, der Profil-Fehler in einem Sandkasten nachgestellt.
    body: |
      DER SCHWERSTE FUND TRIFFT EIN KIND, nicht einen Test.
      `clearresume.sh` schrieb sein Ergebnis mit `mv …tmp ziel`. Die Datei
      `server/config/resume.json` ist aber ein VERWEIS auf
      `profile/<aktiv>/resume.json` — `mv` ersetzt ihn durch eine gewoehnliche
      Datei mit dem Bestand des Kindes, das gerade dran war. Beim naechsten
      Profilwechsel richtet sich die Bruecke nach der schon gesetzten NEUEN
      Kennung und holt diese Fremddatei in den Ordner des NEUEN Kindes: Das
      Kind verliert seine gemerkten Stellen UND erbt fremde.
      `cat > ziel` folgt dem Verweis. Genau deshalb steht es seit laengerem in
      `remove_max_resume.sh`, wo der Grund ausfuehrlich aufgeschrieben ist —
      dieses Skript wurde damals vergessen. DIE ZWISCHENDATEI BLEIBT noetig:
      `jq` LIEST die Zieldatei, ein `cat >` im selben Zug leerte die Quelle,
      bevor jq sie gelesen hat.

      EINE BEGRUENDUNG, DIE SICH AUF NICHTS BERIEF. Eine Testdatei benutzte
      `import.meta.url`, nur um ihren eigenen Ordner zu bekommen, und machte
      damit die Typpruefung eines CommonJS-Pakets rot. Der Kommentar daneben
      rechtfertigte das mit: „unter tsx ist `__dirname` undefined, siehe
      llmwiki <eintrag>". DEN EINTRAG GIBT ES NICHT (0 von 1061), und eine
      Probe unter genau jenem Testlaeufer liefert `__dirname` als korrekten
      String. Eine erfundene Quellenangabe hat dort zwei Wochen lang eine rote
      Pruefung getragen.
      LEHRE: Ein `siehe llmwiki …` im Kommentar ist eine PRUEFBARE Behauptung.
      Wer eine liest, prueft sie — und wer eine schreibt, schreibt den Eintrag.

      MASS HALTEN BEIM FAELLEN: Die zwei Alternativen wurden gemessen statt
      geschaetzt. Das Modulformat des Pakets zu drehen machte aus EINEM Fehler
      ACHT (drei Produktivmodule der Box benutzen `export =`). Die Testdateien
      aus der Typpruefung zu nehmen waere das Aufhoeren hinzusehen — der
      Schritt traegt im Laeufer seine eigene Begruendung („faengt, was die
      Tests nicht sehen").

      EIN GROESSENDECKEL, ERREICHT OHNE EINEN SATZ ZU VERLIEREN: Zwei Dateien
      lagen ueber ihrer Grenze. Gefallen ist ausschliesslich DEKORATION — die
      Zierlinien der Abschnittskoepfe, je drei Byte pro Kastenzeichen. Dass es
      nur das war, ist MECHANISCH geprueft: zieht man die Kastenzeichen ab und
      normalisiert den Leerraum, sind die entfernten und die eingefuegten
      Zeilen identisch. 60 kB ueber drei Dateien. Da diese Dateien
      UNGEBUENDELT ausgeliefert werden, kosten ihre Bytes auf dem Pi wirklich
      Ladezeit — und Zierrat ist der einzige Teil, dessen Verlust nichts
      kostet. Der Deckel-Entscheid war damit ohne Anheben beendet.

      WEITERE BERICHTIGUNGEN AN AUDIT-BEFUNDEN, alle gemessen:
        * Eine Wache zaehlte `.kat` ueber das ganze Blatt statt in der Liste
          und rechnete deshalb mit neun Knoepfen statt vier; die gemeldeten
          714 px sind exakt 9*66 + 8*15.
        * Ein Testfall rief einen Endpunkt OHNE Schalter und erwartete den
          alten Vorgabewert — der folgt seit zwoelf Tagen der Einstellung.
        * Eine erzeugte Datei sollte von Hand gerichtet werden; richtig war,
          sie neu zu erheben (ihr eigener Kopf sagt es).
        * Was als „fuenf Rote" gemeldet war, hatte eine Ursache — und von den
          vier Folgeschaeden starb nachweislich nur einer daran.
  - id: piper-laeuft-auf-der-box-19-09
    kind: knowhow
    title: Piper ist auf der Box vollstaendig eingebaut und eingeschaltet — nachgemessen am 19.09.2026
    tags: [vorlesen, piper, tts, box, messung, 2026]
    quelle: 19.09.2026 an Box 192.168.178.62 gemessen (tools/vorlesen-am-geraet.mjs, piper-einrichten.sh --pruefen)
    lang: |
      AUFTRAG (19.09.2026): „piper in der box aktivieren/integrieren". Die
      Antwort nach dem Messen: es ist BEIDES schon getan, und zwar vollstaendig.
      Der Auftrag beschreibt einen Zustand vom August.

      [[piper-fehlt-im-ausrollweg]] hielt am 04.08.2026 fest, dass Piper ueber
      KEINEN Weg auf eine Box kam. Diese Falle ist geschlossen. Die URSACHE des
      Eintrags gilt weiter (ein `fs.existsSync` als Bereitmeldung verschweigt
      jeden verschobenen Pfad), seine ZUSTANDSAUSSAGE nicht mehr.

      ── WAS AM 19.09.2026 AN BOX .62 GEMESSEN WURDE ─────────────────────

      Der Code-Weg, lokal:

          tools/piper-installationsweg-abgleich.py     deckungsgleich, Exit 0
          autosetup/autosetup.sh                       ruft piper-einrichten.sh
          update/start_mupibox_update.sh               ruft piper-einrichten.sh

      Das Geraet, ueber `piper-einrichten.sh --pruefen`:

          venv      da        piper     da
          flask     DA        <- die stille Falle aus dem alten Eintrag ist zu
          Stimme    de_DE-ramona-low da (Modell UND .onnx.json)

      Und das Skript auf der Box ist BYTE-IDENTISCH mit dem im Baum
      (md5 95da73a8678bf70b0c5dd8e5546d4c94) — kein Auseinanderlaufen.

      EINGESCHALTET ist es auch. GET /api/vorlesen meldet modus `antippen`,
      nicht die Werksvorgabe `aus`; im Zwischenspeicher liegen 300 Dateien,
      die juengste vom selben Tag. Die Box spricht also im Betrieb, nicht nur
      auf dem Papier.

      ── DIE ZEITEN, DIE ES ENTSCHEIDEN ──────────────────────────────────

          kalt (neuer Satz)        743 ms
          warm (zweiter neuer)     750 ms
          nochmal (Speicher)        37 ms

      Der Zaun der Oberflaeche liegt bei 4000 ms — funfmal so weit weg. Zum
      Vergleich nennt [[mupi-vorlesen-schnell]] fuer `-high`-Stimmen rund 6 s;
      die eingestellte `de_DE-ramona-low` ist der Grund fuer den Abstand, nicht
      eine schnellere Box. Wer die Stimme hochdreht, zahlt das zurueck.

      Mitgemessen und ebenfalls grün: /api/vorlesen/silben trennt
      („Pummeleinhorn" -> „Pum-me-lein-horn"), /api/ton/daempfen antwortet und
      laesst die Box danach nicht leise stehen.

      ── UND WAS EIN ZWEITER NICHT NOCHMAL SUCHEN MUSS ───────────────────

      Es gibt keinen zweiten Sprechweg mehr, an dem noch etwas zu integrieren
      waere. `speechSynthesis` kommt im ganzen Baum genau einmal vor — als
      KOMMENTAR in NewDesign/app.js, der festhaelt, dass es ihn nicht gibt.
      Google TTS ist seit [[google-tts-abgeloest]] weg, und
      tools/sprachdeckung-piper-google.py bestaetigt, dass Piper alle 21
      Google-Sprachen abdeckt.

      OFFEN BLEIBT NUR, was schon im BACKLOG steht: die Lizenz der einzelnen
      Stimme ist nicht nachgeschlagen (keine Stimme auf ein weitergegebenes
      Abbild packen), und auf 32 Bit ist `pip install piper-tts` ungeprueft.

      NACHMESSEN — die Box-Adresse wechselt per DHCP, also erst suchen
      (Port 8200 antwortet, Ping schweigt; .169 aus aelteren Eintraegen ist tot):
        node tools/vorlesen-am-geraet.mjs --box <ip>:8200

  - id: gemessen-bis-zur-datei-ist-nicht-gemessen-bis-zum-ton
    kind: knowhow
    title: Vorlesen war stumm, weil dem Dienst XDG_RUNTIME_DIR fehlte — und weil meine Messung vor dem Lautsprecher endete
    tags: [vorlesen, piper, systemd, pipewire, xdg-runtime-dir, messung, wache, 2026]
    quelle: 19.09.2026, Box 192.168.178.62 — journalctl, systemctl show, loginctl, tools/tonweg-umgebung-deckung.py
    lang: |
      Betreiber, Stunden nach [[piper-laeuft-auf-der-box-19-09]]: „wenn ich
      vorlesen aktiviert habe klappt das vorlesen dennoch nicht beim klick auf
      das vorlese icon."

      Beide Saetze stimmten. Der Eintrag davor hatte den Sprechweg gemessen —
      Modell da, Stimme da, 743 ms kalt, 37 ms aus dem Speicher, Silben
      getrennt, alles gruen. Nur: er hatte bis zur WAV-DATEI gemessen und dort
      aufgehoert. Der Weg hat ein Glied mehr, und genau dort war er ab.

      ── DIE KETTE, UND WO SIE RISS ──────────────────────────────────────

      `GET /api/vorlesen/sprich` liefert 200 und 32 kB audio/wav. Das
      Erzeugen ging also die ganze Zeit. Das ABSPIELEN nicht:

          Failed to create secure directory (/home/dietpi/.config/pulse): Permission denied
          pa_context_connect() failed: Connection refused

      DIESE ZEILE IST DIE FOLGE, NICHT DER GRUND — und sie schickt jeden, der
      sie liest, in die falsche Richtung (Rechte an ~/.config reparieren).
      `ansageAbspielen` startet `pw-play` (src/backend-api/src/server.ts:17274).
      PipeWire-Klienten finden ihren Socket ueber $XDG_RUNTIME_DIR/pipewire-0.
      Die Unit setzte GAR KEINE Umgebung — `systemctl show -p Environment
      mupibox-server` gab blank `Environment=` zurueck. Also suchte pw-play
      den Server nicht, fiel auf Pulse zurueck, wollte sich ~/.config/pulse
      anlegen — und /home/dietpi/.config gehoert root seit dem Einrichtungstag.
      Die Rechte zu richten haette das Symptom verschoben, nicht behoben.

      ── WARUM ES MONATE NIEMANDEM AUFFIEL ───────────────────────────────

      Musik spielte weiter. mpv haengt am Abspieldienst, und DESSEN Unit
      traegt die Zeile seit je, ebenso librespot.service. Am Geraet abgelesen:
      mpv hat XDG_RUNTIME_DIR=/run/user/1000, der Server nur HOME. Von den
      drei Diensten mit Ton war genau EINER der Ausreisser — ausgerechnet der,
      dessen Tonweg am seltensten benutzt wird. Ein Fehler, der nur den
      seltenen Pfad trifft, sieht wie kein Fehler aus.

      Behoben in config/services/mupibox-server.service:

          Environment=XDG_RUNTIME_DIR=/run/user/1000

      Beide Ausrollwege legen die Datei ab (`autosetup.sh:1231`,
      `start_mupibox_update.sh:1168`, beide `mv -f` aus config/services/), die
      Box .62 hat sie direkt bekommen; die alte Unit liegt daneben als
      `.vor-19-09`. 1000 statt %U, weil die beiden Geschwister-Units es ebenso
      ausschreiben — drei Fassungen derselben Zahl waeren schlimmer als eine.
      Und der Fix traegt auch ohne Anmeldung: `loginctl show-user dietpi -p
      Linger` meldet Linger=yes, /run/user/1000 bleibt also bestehen. Ohne
      Linger zeigte die Variable ins Leere, sobald sich niemand anmeldet — das
      ist die Pruefung, die man NICHT weglassen darf, wenn man diese Zeile
      irgendwo nachtraegt.

      ── DIE WACHE: tools/tonweg-umgebung-deckung.py, in pruefen.sh ───────

      Regel: laeuft eine Unit nicht als root UND macht sie Ton, MUSS sie
      XDG_RUNTIME_DIR setzen. Auf die SORTE, nicht auf eine Namensliste
      ([[wache-auf-sorte-nicht-auf-pfad]]) — eine Liste waere beim naechsten
      Dienst veraltet und haette GENAU DIESEN Fehler nicht gefunden, denn er
      entstand dadurch, dass einer von dreien vergessen wurde.

      „Macht Ton" auf zwei Wegen, und der zweite ist der eigentliche Punkt:
      die Unit nennt selbst ein Ton-Programm, ODER ihr ExecStart zeigt auf ein
      Programm, dessen QUELLE im Baum eines startet. Letzteres ist der
      Serverfall — in der Unit steht nur `node server.js`, der pw-play-Aufruf
      liegt drei Verzeichnisse weiter. Eine Wache, die nur die Unit liest,
      haette hier gruen gemeldet. Root ist ausgenommen (der Systemton-Server
      laeuft selbst als root), sonst entstuende ein Dauerbefund
      ([[dauerrote-wache-ist-keine]]).

      DIE GEGENPROBE FAND EINEN ECHTEN FEHLER IN DER WACHE. Die erste Fassung
      suchte den Programmnamen nur in Anfuehrungszeichen und meldete deshalb
      `dienste.ts:68 startet librespot`. Die Zeile lautet aber
      `wodurch: 'librespot'` — ein DATENFELD, das nennt, welcher Dienst welchen
      abgeloest hat. Es startet nichts. Das Urteil war zufaellig richtig, die
      BEGRUENDUNG falsch — und wer die zitierte Zeile nachschlaegt, hoert auf,
      der Wache zu glauben. Jetzt wird die STELLUNG verlangt (dem Zitat geht
      eine Klammer oder ein Komma voraus, also die Stelle eines Arguments);
      dieselbe Ueberlegung wie in [[prosa-ist-kein-ruf]] vom selben Tag.
      Danach zeigen die Begruendungen auf die echten Tonwege: server.ts:17274
      pw-play, tonpfad.ts:157 librespot.

      Vier Richtungen gegengeprueft: Server ohne Zeile -> rot mit richtiger
      Begruendung; Player ohne Zeile -> rot; Zuordnung QUELLORTE zeigt ins
      Leere -> Exit 2 mit Anleitung (eine tote Zuordnung machte die Wache
      sonst still gruen); UND die Gegenkontrolle, ohne die die ersten drei
      nichts beweisen: die vier Dienste ohne Ton (Sicherung, Sicherungsprobe,
      Netzpruefung, VNC) werden NICHT gemeldet. Dateien danach byte-identisch.

      ── WAS ICH MIR FUER DIE NAECHSTE MESSUNG MITNEHME ──────────────────

      Ich hatte „Piper ist vollstaendig eingebaut" geschrieben und dabei die
      Kette bis zum Zwischenergebnis verfolgt. Wer eine Wirkungskette misst,
      muss am WIRKENDEN ENDE messen — beim Ton also hoeren (oder wenigstens
      den Abspielprozess pruefen), nicht die erzeugte Datei zaehlen. Dieselbe
      Verwechslung wie in [[der-griff-stempel-fand-die-vier-sekunden-im-vorlesen]],
      wo die gefuehlte Kette an einer anderen Stelle endete als die technische.
      Die Tonstrecke selbst steht in
      [[die-ganze-tonstrecke-und-wo-die-pegel-entstehen]].

      NACHMESSEN (Box-Adresse wechselt per DHCP):
        ssh dietpi@<ip> "systemctl show -p Environment mupibox-server"
        python3 tools/tonweg-umgebung-deckung.py --pruefen

  - id: einstufung-ist-eine-behauptung-und-wurde-nie-geprueft
    kind: falle
    title: Eine Wache verlangte Einstufungen und prüfte nie, ob der Code sie einhält — und der richtige Leser machte die Stelle unsichtbar
    tags: [wache, gegenprobe, bibliothek, profil, boxsicht, 2026]
    quelle: 19.09.2026, tools/bibliothek-ausgaenge.mjs, Fund GET /api/bild/laufend
    lang: |
      `tools/bibliothek-ausgaenge.mjs` zaehlt jede Stelle aus, an der die
      Medienliste den Server verlaesst, und verlangt fuer jede eine
      schriftliche Einstufung: `boxsicht` (muss durch den Profil-Filter),
      `technisch` (darf NICHT gefiltert werden) oder `verwaltung`. Der Zweck
      ist gut — der Unterschied zwischen „bewusst ungefiltert" und „vergessen"
      soll aufgeschrieben sein und nicht in jemandes Erinnerung liegen.

      SIE HAT AUCH ETWAS GEFUNDEN: `GET /api/bild/laufend` hing am `<img>` des
      KINDERSCHIRMS und las `medienLesen()` — den VERWALTUNGS-Leser auf
      data.json, als einzige box-seitige Stelle. Und das war mehr als
      Unsauberkeit: `bildFuerLaufendes` faellt in Schritt 2 auf einen
      unscharfen Vergleich zurueck und laeuft dafuer die GANZE Karte durch,
      nur Interpretenkern ODER nur Albumkern. Ungefiltert konnte ein Werk,
      das dem Kind gehoert, auf den Eintrag eines NICHT zugewiesenen fallen:
      falsches Bild, und im Location-Kopf der 302 ein Werkschluessel, den
      dieses Profil nie gewaehlt hat. Die Einstufung von
      `/api/bild/:schluessel` als technisch traegt auf dem Satz „wer den
      Schluessel hat, hat die Kachel schon gesehen"
      ([[medienschluessel-kollision-gleiche-playlistid]] zeigt, wie wenig der
      traegt) — diese Route hob ihn auf.

      ── UND DANN DIE GEGENPROBE, DIE GRUEN BLIEB ────────────────────────

      Zeile gerichtet, Wache gruen. Zeile zum Nachmessen zurueckgedreht —
      WACHE WEITER GRUEN. Das ist der eigentliche Eintrag.

      Die Wache verlangte eine Einstufung und zaehlte am Ende „BOXSICHT: n —
      sie alle brauchen den Filter". OB SIE IHN HABEN, HAT SIE NIE GEFRAGT.
      Die Einstufung ist ein TEXT, den ein Mensch geschrieben hat; die Wache
      verglich ihn mit nichts. Sie war ein Inhaltsverzeichnis, das sich als
      Pruefung ausgab — und jeder gruene Lauf bestaetigte nur, dass das
      Verzeichnis vollstaendig ist.

      DREI FEHLER, ALLE ERST BEIM GEGENMESSEN SICHTBAR:

      1. `sichtbareMedienLesen` — der RICHTIGE Leser — stand nicht in ihrer
         Leserliste. Wer eine Stelle vom rohen auf den richtigen Leser
         umstellte, liess sie damit aus der Wache VERSCHWINDEN. Das Richtige
         zu tun machte blind. Fuenf Stellen lagen so unbeobachtet da
         (`interpret/:id`, `interpreten`, `interpretenkennungen`,
         `verlaufMitWerken`, `weiterhoeren`) — alle fuenf tun das Richtige,
         aber niemand haette gemerkt, wenn eine davon es aufgibt.
      2. Sie zaehlte KOMMENTARE als Fundstellen. Nach dem Richten fand sie
         die Route nicht mehr am Aufruf, sondern an dem Kommentar, der die
         alte Fassung zitiert — richtiges Urteil, falsche Begruendung, wie
         schon in [[prosa-ist-kein-ruf]] und
         [[kommentar-und-kompilat-sind-keine-gegenstelle]].
      3. Die fehlende Pruefung selbst.

      ── DIE REGEL, DIE JETZT GILT, UND WARUM SIE SO ENG IST ─────────────

      In einer boxsicht-Stelle darf der rohe `medienLesen()` nicht vorkommen.
      Bewusst die ENGSTE Regel, die trotzdem beisst. Die naheliegende („jede
      boxsicht-Stelle MUSS gefiltert lesen") waere zu streng: `spielVersuch()`
      bekommt die Freigabe vom Aufrufer und holt hier nur noch den Roheintrag,
      begruendet und aufgeschrieben. Eine Regel, die solche Stellen mitroetet,
      ist in einer Woche abgeschaltet [[dauerrote-wache-ist-keine]].

      DIE UEBERTRAGBARE FRAGE, bei jeder Wache, die eine BEGRUENDUNG
      einsammelt: Vergleicht sie den Text mit dem Code — oder sammelt sie nur
      ein? Wenn Letzteres, richtet sie genau den Schaden an, gegen den sie
      antritt: Sie erzeugt das Gefuehl, die Sache sei durchdacht.

      NACHMESSEN:
        node tools/bibliothek-ausgaenge.mjs        # 31 Umgebungen, 14+3 boxsicht
        node tools/bibliothek-ausgaenge.mjs --pruefen

  - id: eine-gedrehte-vorgabe-macht-weglassen-zum-gegenteil
    kind: falle
    title: Die Gegenprobe im Test verglich den Fall mit sich selbst, weil die Vorgabe sich gedreht hatte
    tags: [test, gegenprobe, verschmelzen, vorgabe, ard, 2026]
    quelle: 19.09.2026, src/backend-api/src/ard.integration.spec.ts gegen 25b55123
    lang: |
      Der Kerntest „VERSCHMOLZEN MIT SPOTIFY: die Zeile faengt von vorn an"
      war rot, und DER CODE WAR RICHTIG.

      Der Test stellte zwei Faelle gegenueber. Fuer „mit Verschmelzung" rief
      er `?verschmelzen=1`, fuer „ohne" liess er den Schalter WEG — damals
      richtig, denn fehlte er, galt AUS.

      AM 06.09.2026 DREHTE SICH DIE VORGABE (25b55123, auf Betreiberwunsch:
      „ich wuerde default immer verschmolzen machen"). Fehlt der Schalter,
      gilt seither die Darstellung, und die steht auf verschmolzen. Aus dem
      Gegenfall wurde ein ZWEITER verschmolzener Aufruf: der Test verglich
      den verschmolzenen Fall mit sich selbst. Die eigentlich bewachte
      Behauptung — dass beim Verschmelzen die gemerkte Stelle weggenommen
      wird — war seither gruen, WEIL SIE NIE GESTELLT WURDE.

      DIE REGEL: Eine Gegenprobe, die einen ZUSTAND meint, muss ihn
      VERLANGEN. „Den Parameter weglassen" ist kein Zustand, sondern eine
      Wette auf eine Vorgabe — und Vorgaben drehen sich, mit gutem Grund und
      ohne dass jemand an die Tests denkt. Gerichtet mit `?verschmelzen=0`.

      WAS DIE SEITE VERRAET, an der man es merkt: der Test war rot, obwohl
      niemand an ihm oder am gemessenen Code etwas geaendert hatte. Die
      Historie sagt es sofort — Wache zuletzt am 31.08. angefasst, die
      gemessene Naht am 06.09. Diese Reihenfolge ist fast immer das Urteil
      ([[gemessen-bis-zur-datei-ist-nicht-gemessen-bis-zum-ton]] vom selben
      Tag ist der Gegenfall: dort war der CODE schuld).

      GEGENPROBE IN ZWEI RICHTUNGEN, und die zweite ist die wichtigere:
      Schalter wieder weglassen -> rot wie zuvor. Den VERSCHMOLZENEN Ruf auf
      `=0` stellen (also: der Code nimmt die Stelle nicht mehr weg) -> rot
      bei der echten Behauptung. Ohne die zweite waere offen, ob das Richten
      die Wache bloss entschaerft hat.

  - id: ein-neuer-schalter-darf-nichts-wegnehmen
    kind: falle
    title: Ein nachgerüsteter Schalter muss „nicht gesagt" als „wie bisher" lesen — sonst ist er kein Schalter, sondern ein Verlust
    tags: [umstieg, einstellungen, vorlesen, spielecke, vorgabe, 2026]
    quelle: 19.09.2026, src/backend-api/src/vorlesen.ts (spiele), Betreiberwunsch
    lang: |
      Betreiber: „bei vorlesen noch die spiele als einschalt option mit
      reinnehmen." Die Spielecke gab es seit dem 06.09.2026, sie oeffnete
      sich ueber die Fernbedienungs-Anweisung `spielecke` und war NIRGENDS
      abschaltbar.

      DIE EINE ZEILE, AN DER ALLES HAENGT:

          spiele: r.spiele !== false        // und NICHT `=== true`

      Jede gespeicherte `vorlesen.json` auf jeder Box im Haus hat den
      Schluessel NICHT — er ist ja neu. Mit `=== true` waere die Spielecke
      beim naechsten Start stillschweigend verschwunden, ueberall, ohne
      Meldung. Ein Schalter, der beim Einbau eine vorhandene Funktion
      wegnimmt, ist kein Schalter, sondern ein Verlust mit Bedienoberflaeche.

      DIE REGEL: Beim Nachruesten eines Schalters muss „nicht gesagt" heissen
      „wie bisher", nicht „Werksvorgabe". Welche Richtung das ist, entscheidet
      der BESTAND und nicht die Aesthetik — im selben Objekt steht
      `interpret: r.interpret === true`, und das ist ebenso richtig: dessen
      Vorgabe IST aus, und es gab die Funktion vorher nicht.

      UND DER ZEUGE DAZU STELLT DIE ALTE FORM, nicht `{}`:

          const alteFassung = { modus: 'antippen', stimme: '…', interpret: false, tempo: 1.1 }
          assert.equal(einstellungenNormalisieren(alteFassung, DA).spiele, true)

      Wer den Umstieg misst, muss messen, was WIRKLICH auf den Platten liegt.
      Ein `{}` beweist nur, dass die Vorgabe greift.

      ZWEI WEITERE ENTSCHEIDUNGEN AM SELBEN SCHALTER, aus derselben Familie:

      * DIE SPERRE SITZT AN DER TUER, NICHT AM FLUR. Es gibt heute genau
        einen Weg in die Spielecke. Die Pruefung steht trotzdem in
        `spieleckeOeffnen()` und nicht beim Aufrufer: wer morgen eine Kachel
        oder einen Menuepunkt dazubaut, denkt an den Schalter nicht. Eine
        Sperre am Aufrufer ist so viele Sperren, wie es Aufrufer gibt.
      * DIE ZUWEISUNG VOR DEM FRUEHEN AUSSTIEG. `stimme.laden()` steigt aus,
        wenn sich der MODUS nicht geaendert hat. Stuende `this.spiele = …`
        dahinter, liesse sich der Spiele-Schalter nie umlegen, ohne
        gleichzeitig das Vorlesen umzustellen — ein Schalter, der nur in
        Begleitung wirkt. Eine eigene Gegenprobe steht darauf.

      Gemessen wird die ganze Kette in tools/spiele-schalter-schau.mjs
      ([[nachsehen-ohne-zu-nehmen]]).

  - id: nachsehen-ohne-zu-nehmen
    kind: falle
    title: Ein Messwerkzeug, das verbraucht, was es misst, macht jedes „ist nichts passiert" zweideutig
    tags: [messen, gegenprobe, vorschau, fernbedienung, wartezeit, 2026]
    quelle: 19.09.2026, tools/spiele-schalter-schau.mjs, zwei Fassungen
    lang: |
      Die erste Fassung von `tools/spiele-schalter-schau.mjs` war
      FLATTERHAFT: zweimal derselbe Sabotage-Lauf, zweimal ein anderes
      Ergebnis — einmal eine Abweichung, einmal drei, und beim ersten Mal auf
      der FALSCHEN Zeile.

      GEMESSEN STATT VERMUTET, bevor irgendetwas geaendert wurde:
      `document.visibilityState` ist `visible`, und das Anweisungsfach leert
      sich in unter 400 ms. Drosselung durch den Browser und ein langsamer
      Weg schieden damit aus. Es blieben zwei Fehler im WERKZEUG:

      1. FESTE WARTEZEITEN. `warte(1200)` nach einem Tastendruck ist eine
         Wette darauf, dass die Seite ihn in dieser Zeit holt. Verliert man
         sie, meldet das Werkzeug „blieb zu" — also den ERFOLG DES FEHLERS.
         Auf einen Zustand warten, mit Frist; laeuft die Frist ab, ist DAS
         der Befund und nicht ein stilles Weiter.
      2. ZUSTAND AUS DEM VORIGEN SCHRITT. `spieleckeOeffnen()` steigt bei
         `spiel.an` sofort aus. Blieb ein Spiel aus Schritt 1 offen,
         urteilte Schritt 2 ueber eine Tuer, die nie gefragt wurde. Jede
         Messung beginnt jetzt mit frischem Seitenaufbau.

      ── DIE ZWEIDEUTIGKEIT, DIE DAS ERST MOEGLICH MACHTE ────────────────

      „Das Spiel blieb zu" kann zweierlei heissen: der Schalter wirkt — oder
      der Tastendruck ist nie angekommen. Nachsehen liess sich das nicht,
      denn `GET /api/fernbedienung/anweisung` ENTNIMMT (so haelt es der echte
      Server, und so muss es sein, sonst fuehrte die Oberflaeche dieselbe
      Anweisung in jedem Takt erneut aus). Ein Werkzeug, das damit nachsieht,
      NIMMT DER SEITE DIE ANWEISUNG WEG und misst danach, dass nichts
      passiert ist. Es erzeugt genau den Befund, den es sucht.

      DIE ANTWORT: eine zweite, LESENDE Sicht auf dasselbe Fach —
      `/vorschau/anweisungen` in der Attrappe, die den Inhalt nennt, ohne ihn
      zu verbrauchen. Erst wenn feststeht, dass die SEITE den Druck geholt
      hat, wird ueber die Tuer geurteilt.

      UEBERTRAGBAR: Ueberall, wo etwas eine Warteschlange, ein Fach oder
      einen Zaehler LEERT, braucht die Messung eine eigene, lesende Sicht.
      Sonst ist sie Teil des Vorgangs, den sie beobachten will.

      UND DIE DRITTE ZUTAT: jede Lage-Umstellung prueft ihre eigene Wirkung
      an `GET /api/vorlesen` nach. Eine geliehene Vorschau
      ([[vorschau-wird-geliehen]]) kann einem anderen Arbeitsbaum gehoeren,
      in dem es den Schalter noch gar nicht gibt — dann misst der Lauf den
      EINGESCHALTETEN Zustand und nennt ihn „ausgeschaltet".

      ERST DANACH WAREN DIE GEGENPROBEN ETWAS WERT: vier Sabotagen, jede mit
      gezaehltem Treffer, jede auf der RICHTIGEN Zeile rot — und dreimal
      hintereinander gruen, bevor ich es geglaubt habe. Vorher fiel dieselbe
      Wache mal auf der einen, mal auf der anderen Zeile um, und beides sah
      nach einem Fund aus. Dasselbe Muster wie in [[prosa-ist-kein-ruf]] und
      [[einstufung-ist-eine-behauptung-und-wurde-nie-geprueft]]: richtiges
      Urteil, falsche Begruendung — und wer die Begruendung nachschlaegt,
      hoert auf, der Wache zu glauben.

  - id: gegenprobe-bleibt-gruen-ist-der-fund
    kind: falle
    title: Bleibt die Sabotage grün, ist nicht der Code in Ordnung — dann ist die Messung kaputt
    tags: [gegenprobe, messen, wache, instrument, 2026]
    quelle: 19.09.2026, drei Fälle an einem Tag (bibliothek-ausgaenge, auswahl-sagt-sich-an)
    lang: |
      AN EINEM TAG DREIMAL DASSELBE. Jedes Mal sah es zuerst so aus, als sei
      alles in Ordnung — und jedes Mal war die MESSUNG das Problem.

      ── FALL 1: DIE WACHE PRUEFTE IHRE EIGENE BEHAUPTUNG NICHT ──────────

      `tools/bibliothek-ausgaenge.mjs` verlangt fuer jede Stelle, an der die
      Medienliste den Server verlaesst, eine schriftliche Einstufung, und
      zaehlt am Ende „BOXSICHT: n — sie alle brauchen den Filter". Nach dem
      Richten von `GET /api/bild/laufend` habe ich die Zeile zum Nachmessen
      zurueckgedreht: WACHE WEITER GRUEN. Sie hatte nie geprueft, ob der Code
      die Einstufung einhaelt — sie sammelte Prosa ein.
      Ausfuehrlich: [[einstufung-ist-eine-behauptung-und-wurde-nie-geprueft]].

      ── FALL 2: DIE MESSUNG STAND AUF DEM FALSCHEN KNOPF ────────────────

      `tools/auswahl-sagt-sich-an.mjs` prueft, dass der Ring der
      Fernbedienung ansagt, wo er steht, OHNE etwas zu starten. Die Sabotage
      („die Ansage ruft zusaetzlich `click()`") blieb gruen. Grund: nach
      einem frischen Aufbau steht der Ring auf dem Filter „Alles", und der
      SPIELT nichts, er filtert. Die Verneinung „es wurde nichts gestartet"
      konnte an dieser Stelle gar nicht scheitern.

      REGEL: Eine Verneinung muss dort gemessen werden, wo ihr Gegenteil
      etwas KOSTET. Sonst ist sie wahr aus Mangel an Gelegenheit.

      ── FALL 3: DAS FALSCHE BUCH ───────────────────────────────────────

      Dieselbe Messung sah ins BEFEHLSPROTOKOLL der Vorschau. Das fuehrt
      SPIELERBEFEHLE (`/player/…`). Am Geraet nachgemessen erzeugt ein Tipp
      auf eine Kachel aber `POST /api/weiterhoeren`, `POST /api/spielen`,
      `POST /api/gespielt` — und KEINEN einzigen Spielerbefehl. Die Messung
      schlug in einem Buch nach, in dem der Vorgang nie steht.

      REGEL: Bevor man prueft, dass etwas NICHT passiert ist, einmal
      hinsehen, WO es stehen wuerde, wenn es passiert waere. Am billigsten
      geht das, indem man es absichtlich passieren laesst und nachsieht,
      welche Zeile dann auftaucht.

      ── UND DIE FRIST, DIE DAZUGEHOERT ─────────────────────────────────

      Eine Verneinung braucht ein Beobachtungsfenster. „Nichts gestartet",
      unmittelbar nach dem Ereignis gelesen, ist eine Aussage ueber einen
      Augenblick, nicht ueber den Vorgang: ein Klick aus derselben
      Zeitschaltung ist dann noch unterwegs.

      ── DIE UEBERTRAGBARE REIHENFOLGE ──────────────────────────────────

      1. Sabotieren, und die TREFFERZAHL der Ersetzung pruefen — eine still
         gescheiterte Sabotage sieht aus wie eine bestandene Gegenprobe —
         dieselbe Wurzel, nur einen Schritt frueher.
      2. Bleibt es gruen, NICHT erleichtert sein. Fragen: Konnte diese
         Messung an dieser Stelle ueberhaupt scheitern?
      3. Zeigen, dass das Instrument lebt: dieselbe Funktion muss an einer
         Stelle, wo das Ereignis WIRKLICH eintritt, etwas melden.
      4. Und wenn sich eine Messung partout nicht rot bekommen laesst, das
         AUFSCHREIBEN statt es zu verschweigen. In `auswahl-sagt-sich-an.mjs`
         steht genau das: drei Anlaeufe, keiner gelang, bewiesen ist nur,
         dass der Zaehler lebt. Schwaecher als eine gelungene Gegenprobe,
         staerker als ein stilles Gruen.

      SCHWESTERFALL fuer Messwerkzeuge, die ihren eigenen Gegenstand
      verbrauchen: [[nachsehen-ohne-zu-nehmen]].

  - id: umzugsquelle-kann-verderblich-sein
    kind: falle
    title: Wer einen Wert aus einer alten Quelle übernimmt, muss prüfen, ob die Quelle das überlebt
    tags: [umstieg, einstellungen, migration, spiele, 2026]
    quelle: 20.09.2026, spieleLaden in server.ts, am Gerät bewiesen
    lang: |
      Der Spiele-Schalter wohnte einen Tag lang in `vorlesen.json` und zog am
      20.09.2026 in eine eigene Datei ([[ein-neuer-schalter-darf-nichts-wegnehmen]]
      erklaert, warum er ueberhaupt so eingebaut wurde). Der Umzug las den
      alten Wert beim Start: gibt es noch keine `spiele.json`, gilt das, was
      in `vorlesen.json` steht.

      DAS SAH RICHTIG AUS UND HATTE EIN LOCH, das erst Wochen spaeter
      aufgefallen waere:

      `vorlesen.json` KENNT DEN SCHLUESSEL SEIT DEM UMZUG NICHT MEHR. Ihre
      Normalisierung wirft alles Unbekannte weg. Speichert der Betreiber also
      irgendetwas auf der Vorlesen-Seite — Stimme, Tempo, Modus —, schreibt
      `PUT /api/vorlesen` das saubere Objekt zurueck, OHNE `spiele`. Der alte
      Wert ist damit fort. Beim naechsten Start faende die Uebernahme nichts
      mehr, und `!== false` heisst dann „an": aus einem abgeschalteten
      Spielbereich wuerde stillschweigend ein eingeschalteter — ausgeloest
      durch eine voellig unverwandte Aenderung an der Stimme.

      AM GERAET NACHGESTELLT (Box .62), und genau so trat es ein:

          PUT /api/spiele {an:false}      -> spiele.json {"an":false}
          PUT /api/vorlesen {…}           -> vorlesen.json: SCHLUESSEL WEG
          systemctl restart mupibox-server
          GET /api/spiele                 -> {"an":false}   (haelt)

      Die dritte Zeile ist der Beweis: OHNE den Fix staende dort `true`.

      DIE ANTWORT: Der Umzug schreibt die neue Datei SOFORT, nicht erst, wenn
      jemand den Schalter anfasst. Danach steht der Wert an seinem eigenen Ort
      und haengt an nichts mehr. Das Journal haelt fest, dass uebernommen
      wurde — sonst ist ein einmaliger Vorgang spaeter nicht nachweisbar.

      MISSLINGT DAS SCHREIBEN (Platte voll, Rechte), laeuft die Box trotzdem
      weiter: der Wert gilt fuer diese Sitzung, beim naechsten Start wird es
      erneut versucht. Ein Start, der an einer Einstellungsdatei scheitert,
      waere die schlechtere Antwort.

      ── DIE REGEL, UEBER DIESEN FALL HINAUS ────────────────────────────

      Eine Uebernahme hat zwei Fragen, nicht eine. Die erste — „lese ich den
      alten Wert richtig?" — stellt jeder. Die zweite wird uebersehen:
      **WIE LANGE GIBT ES DIE QUELLE NOCH?** Hier war sie im selben Zug zur
      Wegwerfquelle geworden, der den Schalter herausnahm; die Uebernahme
      las aus einer Datei, die gerade dabei war, den Wert zu vergessen.

      Zwei Antworten taugen: sofort festschreiben (hier), oder die Quelle
      ausdruecklich am Leben halten und das an ihr vermerken. Was NICHT
      taugt, ist „wir uebernehmen beim naechsten Zugriff" — dann entscheidet
      der Zufall, was zuerst passiert.

      VERWANDT: [[gemessen-bis-zur-datei-ist-nicht-gemessen-bis-zum-ton]] —
      auch dort endete die Pruefung eine Station zu frueh. Hier haette „die
      Uebernahme liefert den richtigen Wert" gereicht, um sich sicher zu
      fuehlen; gemessen werden musste, was NACH der naechsten fremden
      Aenderung noch dasteht.
  - id: unit-im-heredoc-entgeht-der-unit-wache
    kind: falle
    title: Eine Unit, die ein Rezept per Heredoc schreibt, liegt ausserhalb jeder Unit-Wache — der Fix erreicht den dritten Ausrollweg nicht
    tags: [ausrollen, systemd, remoteinstaller, wache, tonweg, 2026]
    quelle: Kritiker-Lauf 20.09.2026 (AUDIT-2026-09-20.md Rang 1), Fundstellen selbst nachgemessen am Stand 5ed02d55
    lang: |
      Am 19.09. bekam `config/services/mupibox-server.service` die Zeile
      `Environment=XDG_RUNTIME_DIR=/run/user/1000` — der Unterschied zwischen
      Ton und stillem Scheitern beim Vorlesen. Am selben Abend entstand
      `tools/tonweg-umgebung-deckung.py`, die genau dieses Merkmal wacht.
      Beide Arbeiten waren richtig, und beide zusammen haben ein Loch:

      `remote-step-installer/recipes/mupibox-app.yaml` schreibt DIESELBE
      Unit ein zweites Mal, als Heredoc (`cat > /etc/systemd/system/...`),
      und dieses Exemplar hat die Zeile NICHT. Eine per Rezept bespielte Box
      traegt also weiter exakt den Fehler, dessen Behebung committet,
      bewacht und im Wiki dokumentiert ist. Die Wache meldet dazu KEINE
      LUECKE — ihr Sichtfeld ist `config/services/*.service`, und eine Unit
      im Heredoc eines Rezepts ist fuer jeden Glob unsichtbar.

      Das ist [[zwei-ausrollwege-eine-handgefuehrte-liste]] ein Stockwerk
      hoeher: nicht zwei PAKETLISTEN laufen auseinander, sondern zwei
      FASSUNGEN DERSELBEN UNIT. Und es ist die gefaehrlichere Form, weil
      diesmal eine Wache existiert und gruen ist: Eine Wache, die nur den
      einen Ort einer Doppelablage prueft, beweist die Haelfte und beruhigt
      fuers Ganze.

      DIE REGEL FUER JEDEN UNIT-FIX UND JEDE UNIT-WACHE: erst fragen, an
      wie vielen Orten diese Unit ENTSTEHT. In diesem Baum sind es bis zu
      drei — die Datei in `config/services/` (autosetup + Update-Weg) und
      der Heredoc im Rezept. Eine Deckungs-Wache muss die Heredocs der
      Rezepte mitlesen (das Muster `cat > /etc/systemd/system/` ist
      greifbar), sonst gehoert in ihren Kopf der Satz, dass sie den
      Rezept-Weg NICHT sieht.

  - id: die-tuer-neben-der-tuer-e39
    kind: falle
    title: Das eigene Profil ließ sich ohne Passwort betreten — der kürzeste Weg am Anmeldefenster vorbei
    tags: [profil, passwort, anmeldung, e39, kaltstart, 2026]
    quelle: 20.09.2026, POST /api/profil/aktiv gegen /api/profil/anmelden
    lang: |
      Nach einem Kaltstart zeigt die Box „Wer hoert?", sobald der Gast
      abgeschaltet ist. Der Grund steht in app.js: „nach einem Kaltstart
      stuende die Box sonst einfach im letzten Kinderprofil, AM SCHLOSS
      VORBEI."

      DANEBEN STAND EINE ZWEITE TUER, UND SIE WAR OFFEN. `POST
      /api/profil/aktiv` prueft das Passwort nur bei einem ECHTEN Wechsel:

          if (ziel.passwort && kennung !== profilStand.aktiv) { … }

      Wer in eben diesem Fenster sein EIGENES, bereits aktives Profil
      antippte, loeste also gar keinen Wechsel aus — und kam ohne Passwort
      hinein. Ein Kind musste dafuer nichts wissen und nichts ausprobieren,
      nur sich selbst wiedererkennen. Im Fenster-Code stand die Ecke sogar
      als Kommentar („offener Rest an E39 im Backlog") — gesehen, benannt,
      und trotzdem jahrelang die bequemste Umgehung des eigenen Schlosses.

      ── WIE SIE AUFFIEL: DURCH EINEN WUNSCH, DER SIE BRAUCHTE ───────────

      Betreiber am 20.09.2026: „letztes profil startet automatisch, falls
      kein passwort eingestellt ist komplett, ansonsten in die passwort
      abfrage." Die zweite Haelfte liess sich NICHT BAUEN: Das Profil, nach
      dessen Passwort gefragt werden soll, ist ja schon das aktive — und
      fuer das aktive prueft der Server nichts.

      DAS IST DER ALLGEMEINE TEIL: Ein neuer Wunsch ist ein Messgeraet. Wo
      er auf eine Wand laeuft, steht oft keine fehlende Funktion, sondern
      eine vorhandene Luecke. Wer an dieser Stelle „geht nicht" meldet oder
      sich eine Abkuerzung baut, laesst die Luecke stehen und legt etwas
      darauf.

      ── DIE ANTWORT: PRUEFEN, OHNE ZU TUN ───────────────────────────────

      `POST /api/profil/anmelden` beantwortet eine Frage — „gehoert dieses
      Passwort zu diesem Profil?" — und wechselt NICHTS. Bewusst eine eigene
      Route statt die Bedingung oben zu aendern: `/api/profil/aktiv` wird
      auch gerufen, wenn gar nichts zu tun ist; ein Passwortzwang bei jedem
      Ruf braeche Wege, die heute zu Recht durchlaufen.

      DIESELBE BREMSE, DERSELBE ZAEHLER wie beim Wechsel. Eine zweite Tuer
      mit eigener (oder gar keiner) Bremse waere die bequemere Tuer zum
      Raten — man haette das Schloss geschlossen und den Rahmen offen
      gelassen.

      EIN ZEUGE HAELT DIE ALTE LUECKE AUSDRUECKLICH FEST
      (profile.integration.spec.ts: „der alte Weg laesst das AKTIVE Profil
      weiter ohne Passwort durch — deshalb gibt es die neue Route"). Ohne
      ihn liest sich der Code so, als sei die Ecke ueberall zu; sie ist es
      nur an der Stelle, die die Oberflaeche jetzt benutzt.

  - id: was-die-attrappe-nie-schickt-ist-nicht-messbar
    kind: falle
    title: Die Vorschau sendete `gastAktiv` nie — damit war der ganze Kaltstart samt Schloss nicht vorführbar
    tags: [vorschau, attrappe, messen, anmeldung, 2026]
    quelle: 20.09.2026, tools/neu-vorschau.mjs gegen NewDesign/app.js
    lang: |
      Die Oberflaeche zeigt ihr Anmeldefenster genau dann, wenn die
      Profil-Antwort `gastAktiv === false` traegt:

          if (profil.gastAktiv === false && !marke) …

      `profil.gastAktiv` ist im Anfangszustand NICHT GESETZT, und die
      Attrappe schickte das Feld ueberhaupt nicht. `undefined === false` ist
      falsch — also erschien an der Vorschau NIE ein Anmeldefenster. Der
      ganze Kaltstart samt Schloss lag damit ausserhalb dessen, was sich
      messen liess, und zwar seit es ihn gibt.

      DAS BESONDERE DARAN: Es gab keinen Fehler zu sehen. Keine Wache wurde
      rot, keine Meldung kam, kein Werkzeug scheiterte. Es fehlte schlicht
      eine VERHALTENSKLASSE im Vorfuehrbaren — und was man nicht vorfuehren
      kann, prueft man auch nicht. Aufgefallen ist es erst, als jemand
      genau dieses Verhalten aendern wollte.

      SCHWESTERFALL vom Vortag: `/api/eingabegeraete` und
      `/api/fernbedienung/anweisung` fehlten der Attrappe ganz. Dort machte
      sich die Luecke wenigstens als 404-Rauschen bemerkbar; hier war sie
      vollkommen still, weil ein FEHLENDES FELD kein Fehler ist, sondern
      nur eine andere Antwort.

      ── DIE REGEL, DIE BEIDE FAELLE ABDECKT ─────────────────────────────

      Eine Attrappe muss nicht nur die Routen kennen, sondern auch die
      FELDER, an denen die Oberflaeche ihr Verhalten festmacht. Prueft der
      Code auf `=== false`, ist „Feld fehlt" ein DRITTER Zustand, den es am
      Geraet nicht gibt — der echte Server schickt hier immer etwas
      (`gastAktiv: stand.gastAktiv !== false`).

      Praktisch: Wer ein Verhalten nicht an der Vorschau vorfuehren kann,
      hat entweder die Lage nicht gefunden oder sie gibt es nicht. Der
      zweite Fall ist der gefaehrlichere und sieht genauso aus wie der
      erste. Im Zweifel die Antwort der Vorschau neben die des echten
      Servers legen und die FELDER vergleichen, nicht nur den Statuscode
      ([[vorschau-wird-geliehen]] hat dieselbe Wurzel: die Vorschau wird
      fuer die Wirklichkeit gehalten).

      Gestellt wird der Fall jetzt mit `/vorschau/gast-aus`; gemessen wird
      er in tools/start-modus-schau.mjs.

  - id: etwas-antwortet-auf-dem-port-ist-keine-identifikation
    kind: falle
    title: Die Box im Netz finden — ein Portscan-Treffer beweist nichts, und mDNS war reines Glück
    tags: [netzwerk, auffindbarkeit, mdns, avahi, dhcp, werkzeug, 2026]
    quelle: 20.09.2026, tools/box-finden.py, GET /api/box, beide Ausrollwege
    lang: |
      „Welche IP hat die Box?" ist in diesem Projekt eine Dauerfrage: Die
      Adresse wechselt per DHCP, 155 Werkzeuge nehmen sie von Hand entgegen,
      und im Wissenspaket stehen deshalb Saetze mit Verfallsdatum
      („.62/.78/.81 binnen Tagen").

      ── DER NAHELIEGENDE AUSWEG IST EINE FALLE ──────────────────────────

      Am 20.09.2026 war die Box nach einer Auslieferung verschwunden. Ich
      habe das ganze Netz auf Port 8200 abgesucht und genau EINEN Treffer
      bekommen: 192.168.178.199. Das war ein FREMDER Rechner, der dort
      schlichte 404er liefert — beinahe haette ich ihn fuer die Box
      gehalten und Werkzeuge darauf gerichtet.

      „ETWAS ANTWORTET AUF 8200" IST KEINE IDENTIFIKATION, SONDERN EINE
      VERMUTUNG. Und im selben Haus stehen zwei Geraete mit aehnlichem Namen
      (`MuPiBox.local` und `MixPiBox.local`) — ein Suchlauf ohne
      Namensabgleich liefert dann die falsche, und zwar ueberzeugend.

      ── MDNS WAR DIE GANZE ZEIT GLUECK ──────────────────────────────────

      Die Box war im Haus unter `MixPiBox.local` erreichbar. `avahi-daemon`
      stand aber auf KEINEM der beiden Ausrollwege — weder in autosetup.sh
      noch in start_mupibox_update.sh. Es lief, weil DietPi es mitbringt.
      Auf einer frisch geflashten Karte konnte es anders ausgehen, und
      gemerkt haette es niemand: „nicht auffindbar" fuehlt sich an wie
      „falsche IP notiert".

      Schlimmer: `remote-step-installer/recipes/perf-tune.yaml` bietet
      ausdruecklich an, avahi ABZUSCHALTEN. Das Projekt hatte also einen
      Schalter gegen die Auffindbarkeit und keinen dafuer.

      ── DIE DREI TEILE, DIE ES BRAUCHT ──────────────────────────────────

      1. MELDEN: avahi wird jetzt auf beiden Wegen installiert UND
         eingeschaltet. Installiert ist nicht eingeschaltet — ein Dienst,
         der liegt und nicht laeuft, beantwortet keine Anfrage.
      2. BEWEISEN: `GET /api/box` nennt Name, Quelle, Commit, Zweig und die
         eigenen Adressen. BILLIG UND OHNE NEBENWIRKUNG ist Bedingung: ein
         Suchlauf fragt bis zu 253 Adressen. `/api/aktualisierung` traegt
         dieselbe Herkunft, holt aber nebenbei den Angebots-Feed — als
         Kennmarke waere es ein Werkzeug, das beim Suchen die Box
         beschaeftigt.
      3. FINDEN: `tools/box-finden.py` in vier Stufen (genannt, gemerkt,
         mDNS, Suchlauf), und JEDE prueft positiv auf `box == 'mixpibox'`.

      ── WAS DAS WERKZEUG SAGT, WENN ES SUCHEN MUSSTE ────────────────────

      Es meldet den Suchlauf als BEFUND, nicht als Erfolg: Wer ihn braucht,
      hat am Geraet ein Problem (avahi aus, Name geaendert). Ohne diesen
      Satz gewoehnt man sich das Suchen an und behebt nie die Ursache.

      ── ZWEI FEHLER BEIM BAUEN, BEIDE DERSELBEN SORTE ───────────────────

      * DIE VORSCHAU GAB SICH ALS BOX AUS. Ich hatte die Kennmarke in
        `tools/neu-vorschau.mjs` gelegt, damit sich der Finder daran
        vorfuehren laesst — mit `box: 'mixpibox'`. Eine Vorschau auf Port
        8200 haette sich damit als Box ausgegeben und der Finder
        `127.0.0.1` gemeldet: genau die Verwechslung, gegen die er gebaut
        ist, nur im eigenen Haus statt im fremden Netz. Beim ersten Lauf
        landete sie prompt im Zwischenspeicher. Sie heisst jetzt
        `mixpibox-vorschau`.
      * DER ZWISCHENSPEICHER WAERE MITEINGECHECKT WORDEN. `.box-adresse.json`
        haelt eine Adresse, die auf DIESEM Rechner in DIESEM Netz gilt.
        Eingecheckt waere sie die Sorte Zustandsangabe, gegen die das
        Werkzeug gebaut wurde [[wiki-zustand-hat-haltbarkeit]].

      REGEL AUS BEIDEM: Wer eine Identifikation baut, muss pruefen, wer sie
      SONST NOCH ausspricht. Eine Marke, die auch die Attrappe traegt,
      unterscheidet nichts.

      NACHMESSEN:
        python3 tools/box-finden.py
        python3 tools/auffindbarkeit-deckung.py

  - id: ein-flatterhafter-zeuge-ist-ein-befund
    kind: falle
    title: Der Test fiel in jedem dritten Lauf — und der Fehler lag im Ratenbegrenzer, nicht im Test
    tags: [test, flattern, ratenbegrenzer, zeit, plugin, 2026]
    quelle: 20.09.2026, plugins/mixpi-similar/takt.mjs, Zeuge „wartet, wenn es sich lohnt"
    lang: |
      Im Gesamtlauf fiel `Kerntests Plugins` mit einem einzigen Zeugen:
      „wartet, wenn es sich lohnt — in 50 ms ist wieder eine da". Nachgemessen
      (dreimal laufen lassen): 2 von 3 gruen, 1 rot. Ein wackliger Test also,
      und die bequeme Antwort waere gewesen, ihn grosszuegiger zu machen.

      DER CODE WAR SCHULD. `Eimer.nehmen` in `plugins/mixpi-similar/takt.mjs`
      rechnete die Wartezeit aus, schlief EINMAL so lange, fuellte nach — und
      gab `false`, wenn dann immer noch keine ganze Marke da war:

          const warten = this.wartezeit()   // Math.ceil(…)
          await new Promise((f) => setTimeout(f, warten))
          this.#nachfuellen()               // rechnet mit Date.now()
          if (this.#marken < 1) return false

      `wartezeit()` rundet AUF, `#nachfuellen()` misst mit `Date.now()` in
      ganzen Millisekunden — und ein `setTimeout(…, 50)` darf bei 49,7 ms
      feuern, gemessen als 49. Dann sind 0,98 Marken da, `< 1` greift, und der
      Aufrufer bekommt `false`, OBWOHL er das Zehnfache der noetigen Zeit
      mitgebracht hatte (500 ms Budget fuer ein 50-ms-Fenster).

      IM BETRIEB HEISST DAS: eine Anfrage, die hinausgehen duerfte, wird
      verworfen, und das Plugin laesst eine Empfehlung still aus. Nur unter
      Last, nie mit einer Meldung — die Sorte Fehler, die man nie sucht.

      BEHOBEN, indem bis zum Zeitbudget WIEDERHOLT wird statt einmal zu raten.
      Das ist auch die ehrlichere Auslegung von `hoechstensMs`: Der Aufrufer
      sagt, wie lange er warten WILL, nicht wie oft er es versuchen darf. Die
      Frist bleibt hart (ein Fristriss terminiert den Worker samt allen
      offenen Rufen). Danach sechs Laeufe hintereinander gruen.

      ── DIE UEBERTRAGBARE REGEL ────────────────────────────────────────

      EIN FLATTERHAFTER ZEUGE IST EIN BEFUND, KEIN RAUSCHEN. „Mal gruen, mal
      rot" heisst: irgendetwas haengt an der Uhr, und sehr oft ist es nicht
      der Test. Wer an dieser Stelle die Toleranz hochsetzt, hat den Fehler
      nicht behoben, sondern die Wache abgeschaltet.

      UND DIE ENGERE: Wer eine Wartezeit AUSRECHNET und dann EINMAL so lange
      schlaeft, wettet darauf, dass die Uhr feiner ist als seine Rechnung.
      Zwei Zeitquellen (Timer und `Date.now()`) mit verschiedener Koernung
      treffen sich nicht zuverlaessig auf der letzten Stelle. Bis zum
      Zeitbudget wiederholen kostet nichts und macht die Zusage wahr.

      Gegengeprueft in beide Richtungen: gar nicht warten -> „wartet, wenn es
      sich lohnt" faellt; die Frist ignorieren -> „sagt nein, statt die Frist
      zu reissen" faellt, nach 60022 ms, was beweist, dass wirklich gewartet
      wurde.

  - id: skript-mit-rm-rf-muss-seinen-ort-kennen
    kind: falle
    title: Das Bau-Skript begann mit `rm -rf ./deploy` — und die Anleitung rief es aus dem falschen Verzeichnis
    tags: [bau, skript, rm, ausrollweg, anleitung, 2026]
    quelle: 20.09.2026, src/deploy.sh gegen tools/mixpi-paketfrische-pruefen.py
    lang: |
      `Paketfrische` meldete, dass `bin/nodejs/deploy.zip` fuenf Commits alt
      ist, und nannte den Weg zurueck: `bash src/deploy.sh` — also aus der
      WURZEL. Das Skript arbeitet aber durchweg mit RELATIVEN Pfaden
      (`./deploy`, `./backend-player/README.md`, `../bin/nodejs/deploy.zip`)
      und laeuft nur aus `src/`.

      WAS ES DANN SAGTE, war doppelt irrefuehrend:

          cp: ./backend-player/README.md nicht gefunden
          FEHLER: ./deploy/www-admin/index.html fehlt.
            Die neue Verwaltung wurde nicht gebaut.

      Eine Zeile darueber hatte derselbe Lauf gemeldet, dass er www-admin
      ERZEUGT hat. Zwei widerspruechliche Meldungen, und keine nennt die
      Ursache: das Verzeichnis, aus dem man ruft.

      UND DIE ERSTE ZEILE WAR EIN `rm -rf` AUF EINEN RELATIVEN PFAD. Aus der
      Wurzel traf es `./deploy`, das es dort nicht gibt — diesmal folgenlos.
      Ein Skript, das mit Loeschen beginnt und seinen Ort nicht kennt, ist
      eine Falle, die irgendwann zuschnappt; sie haette nur in einem Baum mit
      einem `deploy/` in der Wurzel anders ausgehen muessen.

      BEHOBEN mit zwei Zeilen am Anfang: erst in das eigene Verzeichnis, und
      bricht das ab, wird NICHT geloescht.

          cd "$(dirname "${BASH_SOURCE[0]}")" || { echo "FEHLER: …"; exit 1; }

      ── DIE REGEL ──────────────────────────────────────────────────────

      Jedes Skript, das relative Pfade benutzt, gehoert an den Anfang seines
      eigenen Verzeichnisses — und erst recht eines, das mit `rm -rf`
      beginnt. Die Alternative („man muss es eben aus src/ rufen") ist keine:
      Die Anleitung, die es empfiehlt, stand hier schon falsch da, und der
      naechste Mensch liest sie genauso.

      NEBENBEI GELERNT: `tools/paketbuendel-bauen.py` ist NICHT der Weg zur
      Paketfrische — es baut das 350-MB-Debian-Buendel fuer die SD-Karte
      (git-ignoriert). Gemeint ist `bin/nodejs/deploy.zip`, und das baut
      `src/deploy.sh`.

  - id: die-schublade-traegt-ihren-inhalt-nicht-voraus
    kind: falle
    title: Die Apps waren vor der Schublade da — `display` kennt kein Dazwischen
    tags: [oberflaeche, schublade, animation, clip-path, fernbedienung, 2026]
    quelle: 20.09.2026, NewDesign/app.css `.sch-apps`, am Browser gemessen
    lang: |
      Betreiber: „die app schublade beim rausziehen — die apps sind zuerst
      sichtbar und dann erscheint die schublade, das sieht seltsam aus."

      GEMESSEN (Flaechenkante gegen die App-Spalte, 800x480):

          zu          Flaeche bis  88   Spalte: display none
          Zug  92 px  Flaeche bis  92   Spalte 88..176   -> 84 px in der Luft
          Zug 110 px  Flaeche bis 110   Spalte 88..176   -> 66 px
          Zug 140 px  Flaeche bis 140   Spalte 88..176   -> 36 px

      `.sch-apps` stand auf `display: none` und sprang unter
      `sch-offen`/`sch-zieht` auf `display: flex` — also beim ERSTEN Pixel des
      Zuges an seinen ENDPLATZ, waehrend die Flaeche darunter noch wuchs.

      `display` KENNT KEIN DAZWISCHEN. Alles, was per `display` erscheint,
      erscheint GANZ, und zwar sofort. In einer Bewegung, die Zwischenstaende
      hat (ein Zug am Finger, ein Uebergang), ist das immer falsch: Der Inhalt
      ist vor seinem Behaelter da.

      BEHOBEN MIT `clip-path`: Die Spalte steht im Layout und wird von rechts
      beschnitten, genau bis zur Kante der Flaeche (`--sch-jetzt` am Finger,
      `--sch-breit` offen). Ueberstand danach 0 px in jeder Lage. Geschlossen
      `visibility: hidden` und NICHT `display: none` — ein Uebergang kann aus
      `display: none` nicht starten, und beim blossen Antippen des Griffs
      spraenge die Spalte sonst wieder auf einmal da.

      ── DIE KEHRSEITE, DIE MAN MITNEHMEN MUSS ──────────────────────────

      `visibility: hidden` LAESST DEN KASTEN IM LAYOUT. Der Ring der
      Fernbedienung (`auswahlBewegen`) suchte Knoepfe nur nach `offsetParent`
      und Breite — er haette die App-Knoepfe im GESCHLOSSENEN Zustand
      gefunden und seit dem 19.09.2026 sogar ANGESAGT: die Box haette Namen
      von Apps vorgelesen, die niemand sieht. Wer `display: none` gegen
      `visibility: hidden` tauscht, muss jede Stelle mitnehmen, die
      Sichtbarkeit an `offsetParent` festmacht.

      ── UND DIE GEGENPROBE, DIE GRUEN BLIEB ────────────────────────────

      In die neue Wache hatte ich eine Zeile geschrieben, die behauptete, der
      Ring finde bei geschlossener Schublade nichts — und dafuer den FILTER
      VON `auswahlBewegen` ABGESCHRIEBEN. Als ich die Pruefung dort zum
      Gegenmessen entfernte, blieb die Wache GRUEN: Sie mass ihre eigene
      Abschrift, nicht den Code
      [[gegenprobe-bleibt-gruen-ist-der-fund]].

      Die Zeile misst jetzt eine DOM-TATSACHE (bei geschlossener Schublade
      darf es keinen sichtbaren App-Knopf geben) und behauptet nicht mehr,
      den Ring zu pruefen. Wer die Ring-Seite messen will, muss den Ring
      FAHREN — das gehoert in ein eigenes Werkzeug, nicht als Nebensatz in
      eines, das Geometrie misst.

      NACHMESSEN:  node tools/schublade-deckt-auf.mjs
  - id: mediathek-dieselbe-folge-liegt-dreimal-da
    kind: falle
    title: Die ARD liefert dieselbe Folge dreimal — und die Hoerfassung liegt in DERSELBEN Gruppe
    tags: [ard, mediathek, video, plugin, dubletten, e-video, 2026]
    quelle: 20.09.2026, beim Bau von plugins/mixpi-mediathek. Am echten Dienst gemessen (Suche „Sendung mit der Maus", Werkseite „Klima-Maus Teil 6", 15 Adressen) mit tools/mediathek-probe.mjs.
    lang: |
      DREI EBENEN VON DUBLETTEN, und jede sieht anders aus. Wer nur eine
      kennt, baut zweimal nach.

      ── EBENE 1: DIE TREFFERLISTE ──────────────────────────────────────

      `widgets/ard/search/vod` lieferte auf „Sendung mit der Maus" DREI
      Teaser fuer EINE Folge:

          Klima-Maus Teil 6
          Klima-Maus Teil 6 (Audiodeskription)
          Klima-Maus Teil 6 (mit Gebaerdensprache)

      Gleiches Bild, gleiche Dauer (1626 s), gleiche Sendung. Fuer ein
      Elternteil, das eine Belohnung aussucht, sind das drei Kacheln, die
      sich nicht unterscheiden lassen — die Wahl zwischen ihnen ist keine,
      die jemand treffen will.

      DAS MERKMAL STEHT NICHT AM TEASER. Nachgesehen: `binaryFeatures`
      (["AD","DGS"]) haengt am WERK, nicht am Teaser; der Teaser traegt nur
      `longTitle`, `duration`, `show`, `images`, `isChildContent`. Also ist
      der TITEL das einzige, was da ist — eine Sperrliste auf den Klammer-
      Zusatz, keine Erlaubnisliste auf einem Feld, das es nicht gibt.

      ── EBENE 2: DIE HOERFASSUNG LIEGT IN DERSELBEN GRUPPE ──────────────

      Das ist die teure. `mediaCollection.embedded.streams[0]` (`kind:
      'main'`) enthielt ZEHN Adressen: fuenf normale und fuenf mit
      `audios[0].kind === 'audio-description'` — IN DENSELBEN AUFLOESUNGEN
      (360p, 540p, 720p, 1080p, HLS). Wer die beste Adresse nur nach
      `maxHResolutionPx` sucht, bekommt die Hoerfassung mit rund 50 %
      Wahrscheinlichkeit, und zwar STILL: das Video laeuft, das Bild
      stimmt, und eine Stimme beschreibt dem Kind, was es sieht.

      Die DGS-Fassung ist dagegen harmlos, weil sichtbar getrennt:
      `streams[1].kind === 'sign-language'`.

      ── EBENE 3: DIE GROESSTE ADRESSE IST KEINE DATEI ───────────────────

      In derselben Liste steht `application/vnd.apple.mpegurl` (HLS) mit
      1920 px — die GROESSTE Breite von allen. Ein `<video>`-Element in
      Chromium kann sie nicht von sich aus spielen (Safari schon); es
      braucht eine Bibliothek. Wer „nimm die beste" als „nimm die breiteste"
      liest, bekommt eine Adresse, die auf der Box schwarz bleibt.

      ── DIE VIER REGELN, DIE DARAUS FOLGEN ─────────────────────────────

          GRUPPE   streams[].kind === 'main'
          FORM     nur mimeType video/mp4, nie HLS
          TONSPUR  audios[].kind === 'standard'
          BREITE   groesste Stufe unter dem Deckel; keine darunter -> die
                   kleinste ueberhaupt (lieber zu gross als gar nichts)

      Sie stehen als reine, exportierte Funktionen in
      `plugins/mixpi-mediathek/index.mjs` (`besteQuelle`, `nebenfassung`)
      und haben je einen Zeugen, der die Hoerfassungs-Adressen AUS DER
      VORLAGE zieht statt sie abzuschreiben — so bleibt der Zeuge richtig,
      wenn die Vorlage erneuert wird.

      DECKEL 960: der Schirm ist 800x480, und der Pi 5 dekodiert H.264 in
      Software (ihm fehlt der Hardware-Dekoder des Pi 4). 540p ist die
      kleinste Stufe, die den Schirm noch fuellt. Gemessen: 1268 kbit/s,
      245 MB fuer 27 Minuten.

      NACHMESSEN (geht ins Netz, gehoert deshalb in KEINEN Laeufer):
          node tools/mediathek-probe.mjs
          node tools/mediathek-probe.mjs --vorlage-erneuern   # dann node --test
  - id: mediathek-plugin-weiss-kern-zaehlt
    kind: entscheidung
    title: "Die Belohnungs-Videos: das Plugin weiss, der Kern zaehlt"
    tags: [ard, mediathek, video, plugin, kinderzeit, belohnung, entwurf, 2026]
    quelle: 20.09.2026, Auftrag des Betreibers („ard videos einzelne videos freischalten mit abspiel haeufigkeit es soll quasi eine belohnung sein. Mal ein freigegebenes Video nicht zum dauer konsumieren. eigenes plugin"). Entwurfsentscheidung, getroffen VOR dem Bau des Kernteils.
    lang: |
      DER AUFTRAG SAGT „eigenes plugin", UND DAS GEHT NICHT GANZ — aus
      einem Grund, der im Plugin-Vertrag ausdruecklich steht: die
      `http()`-Routen eines Plugins liegen HINTER DEM TOR DER VERWALTUNG.
      Der Kinderschirm erreicht sie nicht, und `UiExtension` gibt es nicht.
      Ein Plugin kann also weder die Kachel zeigen noch den Knopf annehmen.

      DAS IST HIER KEIN HINDERNIS, SONDERN DIE RICHTIGE GRENZE: ein
      Zaehler, den die Seite des Kindes selbst fuehrt, ist kein Zaehler
      — dieselbe Lehre wie bei der Kinderzeit, die genau deshalb aus
      `localStorage` in das Backend gezogen wurde [[kinderzeit]].

      DIE TEILUNG, die daraus folgt (dieselbe wie bei den Engine-Plugins,
      „Plugin zeigt, Kern schaltet"):

        mixpi-mediathek   WEISS, was die ARD hat. Suchen, eine Kennung zu
                          einer MP4-Adresse aufloesen, Titel/Bild/Dauer/
                          Verfuegbarkeit nennen. Zaehlt nichts, entscheidet
                          nichts. Rechte: nur `netz`.

        der Kern          FUEHRT BUCH. Welches Video fuer welches Profil
                          freigegeben ist, wie oft es noch laufen darf, ob
                          ein Start erlaubt ist. Er fragt das Plugin
                          STELLVERTRETEND — und erst, NACHDEM die Freigabe
                          steht.

      WARUM DAS PLUGIN KEIN `aufloesen()` HAT: `aufloesen()` fuehrt nach
      `spielweg.ts`, also in den TON-Weg mit mpv (`--no-video`). Ein Video
      dort hineinzugeben ergaebe eine Tonspur ohne Bild. Der Bildweg ist
      der Kiosk-Browser. Ohne `aufloesen()` und `suchen()` braucht das
      Plugin auch `medienquelle` nicht — ein Recht, das man nicht braucht,
      holt man sich nicht.

      WARUM DIE ADRESSE NICHT BEI DER FREIGABE GESPEICHERT WIRD: die ARD
      zieht Folgen zurueck (`availableTo`), und die Freigabe geschieht Tage
      vor dem Anschauen. Gespeichert wird die KENNUNG; die Adresse wird bei
      jedem Start frisch geholt. Was fehlt, faellt dann mit einem Grund auf
      („nicht mehr in der Mediathek") statt als schwarzer Schirm.

      DIE VIER ENTSCHEIDUNGEN DES BETREIBERS (20.09.2026, auf Rueckfrage):
        Bildweg     im Kiosk-Browser, Vollbild — nicht mpv auf DRM (dort
                    streiten zwei Prozesse um den Schirm).
        Zaehlweise  ein Mal ist erst bei rund 90 % verbraucht. Abgebrochen
                    nach zwei Minuten kostet nichts.
        Kinderzeit  Video zaehlt NICHT auf das Minutenguthaben. Eine
                    Belohnung soll nicht die Hoerzeit auffressen.
        Geltung     je Profil — eigene Freigaben, eigene Zaehler.
  - id: kaputte-datei-darf-nicht-aufsperren
    kind: regel
    title: Eine kaputte Datei darf nicht aussperren — aber auch nicht AUFSPERREN
    tags: [robustheit, kinderzeit, videofreigabe, regel, fehlerfall, 2026]
    quelle: 20.09.2026, beim Bau von src/backend-api/src/videofreigabe.ts neben dem schon vorhandenen kinderzeit.ts. Beide Richtungen stehen im Code und haben je einen Zeugen.
    lang: |
      IN DIESEM BAUM STEHEN ZWEI DATEIEN NEBENEINANDER, die im Fehlerfall
      GENAU ENTGEGENGESETZT handeln. Das ist kein Versehen, und wer eine von
      beiden liest, ohne die andere zu kennen, haelt es fuer eines.

        kinderzeit.ts / regelnNormalisieren
            Unlesbares wird auf die FREUNDLICHE Seite gebogen: frei, kein
            Fenster, unbegrenzt. Eine kaputte Datei darf kein Kind
            AUSSPERREN.

        videofreigabe.ts / freigabeNormalisieren
            Unlesbares faellt WEG. Eine kaputte Datei darf kein Video
            AUFSPERREN.

      DIE REGEL DAHINTER IST DIESELBE, nicht die umgekehrte: DER FEHLERFALL
      MUSS DORTHIN ZEIGEN, WO ER NICHTS KAPUTT MACHT. Nur liegt dieses
      „dorthin" je nach Sache woanders.

        Bei einer GRENZE (Hoerzeit, Zeitfenster) ist der Schaden das
        Aussperren: das Kind steht vor einer Box, die nicht mehr spielt, und
        niemand weiss warum. „Es laeuft weiter" ist die harmlose Richtung.

        Bei einer ERLAUBNIS (ein freigegebenes Video, eine Auswahl, ein
        Recht) ist der Schaden das Aufsperren: das Kind bekommt, was es nicht
        bekommen sollte, und es faellt NIEMANDEM auf — kein Fehler, keine
        Meldung, nur ein Kind, das mehr sieht. „Gibt es nicht" ist hier die
        harmlose Richtung.

      DIE FRAGE, DIE MAN SICH VOR JEDEM `?? VORGABE` STELLT, lautet deshalb
      nicht „was ist freundlich", sondern: WAS KOSTET ES, WENN DIESE ZEILE IM
      FEHLERFALL JA SAGT, und was, wenn sie nein sagt? Dieselbe Abwaegung
      steht schon in [[pruefung-die-wirft-heisst-unbekannt]] — dort fuer eine
      einzelne Pruefung, hier fuer eine ganze Datei.

      EIN KAPUTTER EINTRAG NIMMT TROTZDEM NICHT DIE LISTE MIT. Die Freigaben
      sind die Arbeit der Eltern, oft ueber Wochen; sie wegen einer
      unlesbaren Zeile alle zu verlieren waere der teurere Fehler. Die Grenze
      laeuft also zwischen EINTRAG (faellt weg) und LISTE (bleibt), nicht
      zwischen Datei und Nichts.

      NACHMESSEN:
          npx cross-env NODE_ENV=test tsx --test \
            src/backend-api/src/videofreigabe.spec.ts \
            src/backend-api/src/videofreigabe.integration.spec.ts
      Der Integrationszeuge schreibt ausdruecklich Muell in die Ablage und
      verlangt danach 403 `unbekannt` — nicht 200 mit einer Liste.
  - id: gesehen-braucht-eine-laufkennung
    kind: falle
    title: Eine Sichtung, die der Browser meldet, kommt zweimal — der Lauf braucht eine Kennung
    tags: [videofreigabe, browser, zaehler, idempotenz, falle, 2026]
    quelle: 20.09.2026, beim Bau der Belohnungs-Videos (videofreigabe.ts `verbrauchen`, POST /api/video/gesehen). Vor dem Bau erkannt und mit zwei Zeugen festgenagelt, nicht nach einem Vorfall.
    lang: |
      DER ZAEHLER STEHT SERVERSEITIG, UND DAS REICHT NICHT. Wer „ein Mal
      verbraucht" an eine MELDUNG haengt (der Browser sagt „zu Ende
      gesehen"), hat den Zaehler gegen Faelschung geschuetzt und nicht gegen
      WIEDERHOLUNG. Dieselbe Meldung kommt naemlich regelmaessig zweimal:

        * Das Kind laedt die Seite neu, waehrend das Video noch steht.
        * Das `ended`-Ereignis feuert, und daneben feuert ein eigener
          Fortschrittsmelder ueber der Schwelle.
        * Die Antwort geht verloren, der Browser schickt erneut.
        * Ein Kind drueckt einen Knopf zweimal — das ist auf diesem Geraet
          kein Sonderfall, sondern die Regel.

      DAS ERGEBNIS WAERE DER TEUERSTE FEHLER DIESER FUNKTION: eine Belohnung
      „noch dreimal" ist nach einem Anschauen bei eins. Das faellt keinem
      Erwachsenen auf und dem Kind sehr wohl.

      DIE LOESUNG IST EINE KENNUNG JE LAUF. `POST /api/video/start` gibt eine
      frische `lauf`-Kennung (randomUUID) heraus; `POST /api/video/gesehen`
      muss sie mitschicken, und die Freigabe merkt sich die ZULETZT
      GEZAEHLTE. Kommt dieselbe noch einmal, lautet die Antwort
      `schonGezaehlt` — 200, kein Fehler, der Rest unveraendert. Ein NEUER
      Lauf zaehlt sehr wohl: zweimal anschauen kostet zweimal.

      WARUM NICHT „einmal je Video und Tag": das waere eine Grenze, wo keine
      sein soll. Wer drei Mal freigegeben bekommen hat, darf sie auch an
      einem Nachmittag sehen.

      WARUM UEBERHAUPT DER BROWSER MELDET, statt dass der Server mitzaehlt:
      der Server weiss nicht, ob das Video laeuft, pausiert, stumm ist oder
      hinter einem anderen Schirm liegt. Eine serverseitige Zeitmessung waere
      genau die Buchfuehrung, die in all diesen Faellen falsch liegt. Was der
      Server sehr wohl haelt: die SCHWELLE (90 %), die LAUFKENNUNG und den
      REST. Gemeldet wird ein Anteil, ENTSCHIEDEN wird im Kern.

      DIE SCHWAECHE, DIE BLEIBT UND DIE MAN KENNEN MUSS: wer die Meldung
      faelschen kann, faelscht den Zaehler — in die eine Richtung (mehr
      verbrauchen) schadet er nur sich selbst, in die andere (nie melden)
      bekommt er unbegrenzt. Hinnehmbar, weil am anderen Ende ein Kind sitzt
      und kein Angreifer; die Alternative waere eine Messung, die bei jedem
      pausierten Video falsch liegt.
  - id: kinderschirm-video-eigene-datei
    kind: architektur
    title: Video auf dem Kinderschirm — eigene Datei, eigener Deckel, und der Ton geht vorher aus
    tags: [video, kinderschirm, newdesign, belohnung, vorschau, messen, 2026]
    quelle: 20.09.2026, gebaut als NewDesign/video.js + video.css, gemessen mit tools/video-belohnung-schau.mjs gegen tools/neu-vorschau.mjs (800x480, Chromium 148).
    lang: |
      DIE DRITTE HAELFTE DER BELOHNUNGS-VIDEOS: Plugin weiss, Kern zaehlt
      [[mediathek-plugin-weiss-kern-zaehlt]] — und der KINDERSCHIRM zeigt.

      ══ WARUM EINE EIGENE DATEI ═══════════════════════════════════════

      app.js lag bei 1,64 MB und damit auf 99 % ihres Deckels, app.css bei
      99 %. Dieselbe Lage wie bei den Apps am 03.09.2026, und dieselbe
      Antwort: eine eigene Datei, ein eigener Deckel, eine eigene Zeile in
      `tools/neu-groessen-schau.mjs`. Neu ist, dass diesmal AUCH das CSS
      mitgezogen ist (video.css) — apps.js empfiehlt das in seinem eigenen
      Kommentar ausdruecklich fuer den naechsten Schnitt.

      In app.js bleiben rund 40 Zeilen: `videoWerk()` reicht den Kontext
      herein, `videoZeichnen()` haengt in `zeichnen()`. Fehlt video.js, gibt
      es keine Reihe und sonst nichts.

      ══ DER KONTEXT ENTHAELT EINEN ABSPIELWEG — UND DAS MUSS SO ════════

      apps.js reicht ausdruecklich KEINEN Abspielweg herein („keine App
      spielt Musik ab"). video.js bekommt `spielerBefehl`, und zwar genau
      dafuer: VOR dem ersten Bild geht `pause` an den Abspieldienst. Ohne
      das liefen Hoerspiel und Video uebereinander, und die
      Lautstaerketasten regelten das Falsche.

      Das ist keine Umgehung der Kinderzeit: `spielerBefehl` ist derselbe
      Flaschenhals (/player/<raum>/<befehl>), durch den jeder Befehl dieser
      Oberflaeche geht. Und Anhalten wird ohnehin nie abgewiesen.

      ══ DIE REIHENFOLGE BEIM START ════════════════════════════════════

        1. beim Kern anfragen (POST /api/video/start)
        2. den Ton anhalten
        3. den Schirm aufziehen und abspielen

      Umgekehrt saehe das Kind einen schwarzen Vollbildschirm und danach
      eine Absage — und der Ton liefe weiter, waehrend die Absage steht.

      ══ ZWEI FALLEN BEIM MESSEN, BEIDE HABEN ZEIT GEKOSTET ════════════

      1. `Runtime.evaluate` OHNE `userGesture: true` DARF NICHT ABSPIELEN.
         Chrome haelt seine Autoplay-Regel auch am Fernsteuerungsweg; ein
         `element.click()` von dort ist KEINE Benutzergeste. Gemessen:
         duration 0, currentTime 0, auf dem Schirm „Das Video startet
         nicht." Die Wache meldete einen Fehler der OBERFLAECHE, und der
         Fehler lag in der MESSUNG. Der Schalter `--autoplay-policy` waere
         der schlechtere Weg: er misst dann eine Regel, die auf der Box
         nicht gilt — dort IST der Tipp des Kindes eine echte Geste.

      2. EIN KOMMENTAR, DER EIN MUSTER ZITIERT, IST EIN RUF.
         In den Kopf von video.js kam der Satz, dass
         `tools/vorschau-routen-deckung.mjs` die lesenden Routen am Muster
         ``json(`${API}/…`)`` sammelt — woertlich, mit Backticks. Die Wache
         las ihren eigenen Beispieltext als Aufruf und verlangte von der
         Attrappe eine Antwort auf den Pfad „…". Dieselbe Krankheit wie
         [[prosa-ist-kein-ruf]], nur andersherum: nicht ein Ruf, der wie
         Prosa aussieht, sondern Prosa, die wie ein Ruf aussieht.

      ══ WAS DIE WACHE MISST ═══════════════════════════════════════════

      `tools/video-belohnung-schau.mjs` (in tools/pruefen.sh) faehrt drei
      Zusagen, die man nicht sieht, wenn sie gebrochen sind:

        * aufgebrauchtes verschwindet, und ohne Freigabe gibt es die Reihe
          gar nicht,
        * EIN Durchlauf verbraucht GENAU EINS — auch wenn `ended` und der
          Fortschrittsmelder beide feuern [[gesehen-braucht-eine-laufkennung]],
        * eine Absage kommt MIT GRUND an („Dieses Video ist aufgebraucht.")
          und setzt keine Adresse.

      Die Attrappe liefert dafuer ein zwei Sekunden langes, echtes MP4
      (base64 in tools/neu-vorschau.mjs): ein Durchlauf dauert Sekunden
      statt 27 Minuten, und es braucht kein Internet.

      GEGENGEPROBT (20.09.2026), beide auf Committetem und per gezieltem
      Edit zurueckgenommen: das Melden abgeschaltet -> „ein Durchlauf hat 0
      verbraucht"; die Grundzuordnung der Absage auf `false` gesetzt -> „die
      Absage nennt den Grund nicht".

      ══ WAS NOCH NICHT GEMESSEN IST ═══════════════════════════════════

      ALLES AM GERAET. Ob der Pi 5 eine 540p-H.264-Spur in Software
      fluessig dekodiert, waehrend der Kiosk laeuft, sagt nur die Box —
      dasselbe gilt fuer den Ton ueber PipeWire und dafuer, ob der
      Wechsel zurueck zum Hoerspiel klappt. Stand 20.09.2026: im
      Arbeitsbaum, gegen die Vorschau gemessen, NICHT auf einer Box.
  - id: ard-tonspur-markierung-ist-vertauscht
    kind: falle
    title: Die ARD markiert die Hoerfassung als „standard" — und `isGeoBlocked` ist keine Sperre
    tags: [ard, mediathek, video, plugin, tonspur, geosperre, geraet, falle, 2026]
    quelle: 20.09.2026, beide Funde bei der ERSTEN Freigabe auf der echten Box (.62), nachdem alles gegen Zeugen und Vorschau gruen war. Nachgemessen mit tools/mediathek-probe.mjs und einem HEAD auf die Adressen.
    lang: |
      ZWEI FEHLER, DIE KEIN ZEUGE GEFUNDEN HAT, und beide fielen innerhalb
      von zwei Minuten auf, als die Box das erste Mal ein echtes Video
      freigeben sollte. Das ist die Lehre vor den Einzelheiten: 38 Zeugen
      gegen eine gekuerzte echte Antwort und eine gruene Browser-Messung
      haben eine Vorlage geprueft — nicht den Dienst. Die zweite Folge, die
      man anfasst, sieht anders aus als die erste.

      ── FUND 1: `isGeoBlocked` IST EIN MERKMAL, KEINE SPERRE ────────────

      Das Plugin zaehlte `geoblocked` / `isGeoBlocked` als vierte Sperre neben
      FSK, Anmeldung und Ablaufdatum. Die erste Freigabe („Kommissar
      Wisting") antwortete damit:

          POST /api/video/start  ->  404 {"grund":"nur in Deutschland abrufbar"}

      GEGENPROBE AM SELBEN ORT: ein HEAD auf genau die MP4-Adresse dieses
      Werks, aus demselben Wohnzimmer, antwortete `200 video/mp4`, 275 MB.

      DAS FELD BESCHREIBT DAS WERK, NICHT DEN FRAGENDEN. Es heisst „gibt es
      nur im Inland" und nicht „du darfst nicht". Eine Box in Deutschland
      spielt es. Wer es als Sperre liest, sperrt die eigenen Kinder von
      Inhalten aus, die ihnen offenstehen — unsichtbar, weil die Absage
      plausibel klingt.

      Es reist jetzt als Merkmal `nurInland` mit. Steht die Box wirklich im
      Ausland, scheitert der Abruf zur Laufzeit, und das faengt der
      Video-Schirm ab. Ein Fehler, wenn es soweit ist, ist ehrlicher als eine
      Vorhersage, die fast immer falsch ist.

      ── FUND 2: `audios[].kind` KANN VERTAUSCHT SEIN ────────────────────

      Nach der Behebung von Fund 1 loeste dasselbe Werk auf — und lieferte
      ausgerechnet die HOERFASSUNG. In DERSELBEN Gruppe `main` lagen drei
      vollstaendige Saetze:

          JOB_..._audiodeskription_960x540.mp4    kind: standard          (!)
          JOB_..._internationalerton_960x540.mp4  kind: audio-description (!)
          JOB_..._sendeton_960x540.mp4            kind: standard

      Die Hoerfassung hiess „standard", der internationale Ton (Musik und
      Geraeusche, KEIN Dialog) hiess „audio-description". Dazu stand
      `languageCode: 'nor'` an einer deutschen Fassung. Bei der Maus-Folge
      stimmt dasselbe Feld — DAS ist das Teure: die Regel funktioniert, bis
      sie es nicht tut.

      DIE REIHENFOLGE TRAEGT AUCH NICHT: hier stand die Hoerfassung VORN.

      WAS TRAEGT, IST DER DATEINAME (`audiodeskription`, `hoerfassung`,
      `internationalerton`, `klarsprache`). Eine Kruecke, und sie wird auch
      so behandelt: SPERRLISTE MIT RUECKFALL. Bleibt nach ihr nichts uebrig,
      gilt erst der Dateiname nicht mehr, dann auch das Feld nicht — lieber
      die falsche Tonspur als ein schwarzes Bild. Eine ERLAUBNISLISTE auf
      „sendeton" haette jedes Werk stumm gestellt, das das Wort nicht
      benutzt; die Maus-Folgen tun es nicht.

      ── WAS DARAUS FOLGT, UEBER DIESE ZWEI FUNDE HINAUS ─────────────────

      EINE VORLAGE IST EIN FALL, KEINE GATTUNG. Wer eine fremde Schnittstelle
      gegen EINE gespeicherte Antwort prueft, prueft, dass er sie damals
      richtig gelesen hat. Die zweite Vorlage
      (`plugins/mixpi-mediathek/ard-wisting.fixture.json`) steht jetzt neben
      der ersten, und sie ist ausdruecklich die HAESSLICHE: sie traegt genau
      die beiden Faelle, an denen die schoene nichts sagt.

      NACHMESSEN:
          node --test plugins/mixpi-mediathek/index.spec.mjs      # 38 Zeugen
          node tools/mediathek-probe.mjs --begriff "Kommissar Wisting"
      Gegengeprobt: die Dateinamen-Sperrliste ausgeschaltet -> der
      Wisting-Zeuge wird rot; `isGeoBlocked` wieder zur Sperre gemacht ->
      zwei Zeugen werden rot. Beides per gezieltem Edit zurueckgenommen.
  - id: video-spielleiste-vierter-regler
    kind: entscheidung
    title: Die Spielleiste des Videos — vierter Regler am selben Flaschenhals, und kein Sprungbalken
    tags: [video, kinderschirm, lautstaerke, flaschenhals, belohnung, gegenprobe, 2026]
    quelle: 20.09.2026, Betreiber („das video braucht noch einen player menü mit volume"). Gebaut in NewDesign/video.js + video.css + index.html, gemessen mit tools/video-belohnung-schau.mjs gegen die Vorschau.
    lang: |
      DREI ENTSCHEIDUNGEN, und die mittlere ist die, die man nicht sieht.

      ══ 1. DER REGLER IST DER VIERTE, NICHT DER ERSTE EIGENE ═══════════

      Der Regler in der Video-Leiste traegt `data-laut-regler` — und damit
      ist er fertig verdrahtet: app.js haengt BEIM START einen Zuhoerer an
      jeden `[data-laut-regler]` (`laut.gezogen()` -> `setzen()` ->
      entprellt `setvolume:N` -> `mupi-lautstaerke.sh`, wo `maxVolume`
      sitzt), und `reglerAngleichen()` stellt jeden davon nach, sobald sich
      der Wert irgendwo aendert.

      `spieler.volume` WIRD NICHT ANGEFASST. Das waere eine zweite,
      unsichtbare Skala neben der des Hauses: die Tasten der Box, die Geste
      vom Rand und die drei anderen Regler wuessten nichts von ihr, und „ich
      habe lauter gemacht, es wird nicht lauter" waere die Folge. Dass
      dasselbe Haus an genau dieser Stelle schon zweimal falsch lag
      (`volume/+5` am Flaschenhals vorbei), steht bei `laut.reglerAngleichen`.

      DER GRIFF IM KONTEXT LIEST NUR: `wert()` und `max()`, damit der Regler
      beim Aufziehen auf dem Stand des Hauses steht statt auf 0 — ein Kind
      liest aus „0 %", der Ton sei aus.

      ══ 2. KEIN SPRUNGBALKEN — DIE ZAEHLUNG HAENGT DARAN ═══════════════

      Gezaehlt wird bei 90 % Fortschritt [[gesehen-braucht-eine-laufkennung]].
      Ein Balken zum Ziehen oeffnet damit ZWEI Loecher auf einmal, je
      nachdem, wie man rechnet:

        * zaehlt die POSITION, springt man ans Ende und hat die Belohnung
          verbraucht, ohne etwas gesehen zu haben;
        * zaehlt die WIRKLICH GESEHENE Zeit, springt man durch und hat
          UNBEGRENZT geschaut, ohne je zu verbrauchen — genau das, was der
          Auftrag ausschliesst („nicht zum dauer konsumieren").

      Der Balken zeigt deshalb nur. Ein Kind, das zurueckspulen will, drueckt
      „Fertig" und faengt neu an; das kostet ein Mal, und das ist die
      ehrliche Buchung.

      ══ 3. DIE LEISTE GEHT VON SELBST WEG — UND `play` MACHT SIE AUF ═══

      Nach 4,5 s ohne Beruehrung blendet sie aus (nur solange etwas laeuft);
      jede Beruehrung des Bildes holt sie zurueck. Ein Kind macht eine
      Leiste nicht wieder zu, und ueber einem 27-Minuten-Film staende sie
      sonst 27 Minuten im Bild.

      DAMIT AENDERT SICH, WAS EIN TIPP AUFS BILD TUT: bis dahin hielt er an,
      weil es keinen Knopf dafuer gab. Jetzt gibt es einen sichtbaren.

      AUFGEMACHT WIRD SIE VOM `play`-EREIGNIS, NICHT VOM AUFZIEHEN DES
      SCHIRMS. Der Schirm geht auch OHNE Video auf — bei einer Absage steht
      dort ein Satz und sonst nichts, und eine Spielleiste mit „0:00 / 0:00"
      ueber einer Absage waere eine Bedienung fuer etwas, das nicht laeuft.

      ══ WIE DIESE ZEILE GEFUNDEN WURDE: EINE GRUENE GEGENPROBE ═════════

      Im ersten Wurf stand `leisteZeigen(true)` an BEIDEN Stellen. Die
      Gegenprobe („Zeile aus `schirmZeigen` heraus") blieb GRUEN — weil
      `play` die Leiste ohnehin aufmachte. Zwei Wege auf denselben Zustand,
      und der zweite tat nichts, ausser den ersten zu verdecken
      [[gegenprobe-bleibt-gruen-ist-der-fund]].

      DASSELBE EIN ZWEITES MAL, im selben Zug: der eigene `input`-Zuhoerer
      am Regler rief `lautstaerke.setzen()` — und war ueberfluessig, weil
      app.js den Regler laengst am Flaschenhals hat. Auch das fiel nur auf,
      weil die Gegenprobe nach dem Abschalten gruen blieb. ZWEIMAL DIESELBE
      KRANKHEIT AN EINEM NACHMITTAG: wer eine Naht neu baut, prueft zuerst,
      ob das Haus sie schon hat.

      NACHMESSEN:
          node tools/video-belohnung-schau.mjs --pruefen
      Es misst jetzt auch: Leiste beim Abspielen da, KEINE Leiste ueber
      einer Absage, der Knopf haelt an und dreht sein Zeichen, der Regler
      schickt `setvolume:42` durch den Flaschenhals und laesst
      `spieler.volume` in Ruhe.
  - id: kiosk-browser-hatte-nie-ton
    kind: falle
    title: Der Kiosk-Browser hatte nie Ton — er laeuft als root, PipeWire gehoert dietpi
    tags: [kiosk, ton, pipewire, chromium, video, root, geraet, falle, 2026]
    quelle: 20.09.2026, Betreiber nach dem ersten Belohnungs-Video („beim spielen vom video gibt es keinen ton"). An Box .62 gemessen mit tools/box/kiosk-ton-probe.py; der Weg mit PULSE_SERVER als root dort auch GEFAHREN (paplay erschien als sink-input).
    lang: |
      BILD LIEF, TON NICHT — und die Box spielte im selben Augenblick ein
      Hoerspiel. Genau diese Gleichzeitigkeit ist der Hinweis: es ist KEIN
      Tonproblem der Box, sondern eines des BROWSERS.

      ── DIE URSACHE ────────────────────────────────────────────────────

      PipeWire ist ein SITZUNGSDIENST. Es gehoert `dietpi` (uid 1000) und
      haelt seine Sockets unter `/run/user/1000/`. Der Kiosk wird aber aus
      der Autostart-Kette von DietPi als ROOT gestartet
      (`dietpi-login` -> `chromium-autostart.sh`). Gemessen am Geraet:

          Kiosk laeuft als       root (pid 1291271)
          PULSE_SERVER           (nicht gesetzt)
          XDG_RUNTIME_DIR        /run/user/0

      Unter `/run/user/0/` steht nichts. libpulse findet keinen Server,
      Chromium faellt auf ALSA zurueck — und dort haelt PipeWire die Karte.
      Der Browser hatte damit GAR KEINE Tonausgabe. Kein Fehler, keine
      Meldung im Journal, kein Ton.

      ── WARUM ES JAHRELANG NICHT AUFFIEL ───────────────────────────────

      WEIL NICHTS IM BROWSER JE TON GEMACHT HAT. Die Musik kommt aus
      mpv/librespot (eigene Dienste, eigener Benutzer), und das Vorlesen
      spielt seit E123 der SERVER — `/api/vorlesen/sprich?abspielen=1`
      liefert nur die Dauer zurueck. Das Belohnungs-Video ist der ERSTE Ton
      aus dem Browser dieser Box.

      DAS IST DIE ALLGEMEINE LEHRE: Ein Weg, den noch nie jemand gegangen
      ist, ist nicht „wahrscheinlich in Ordnung" — er ist UNGEMESSEN. Wer
      eine neue Sorte Ausgabe einbaut (Ton aus dem Browser, Bild aus dem
      Server, Netz aus einem Plugin), prueft ZUERST, ob dieser Weg auf dem
      Geraet ueberhaupt existiert. Hier war die Antwort: nein, seit jeher.

      Und sie stand sogar schon im Paket: [[mupi-vorlesen-lautstaerke]]
      endet mit dem Satz „Lautstaerke ist nichts Globales auf dieser Box.
      Jeder Tonweg (librespot, mpv, Browser) hat seinen eigenen Regler. Wer
      einen neuen Tonweg dazunimmt, muss ihn ausdruecklich anbinden." Genau
      das war zu tun — nur eine Ebene tiefer, als der Satz vermuten laesst:
      es ging nicht um die Lautstaerke, sondern um die VERBINDUNG.

      ── DIE BEHEBUNG (der kleine Weg) ──────────────────────────────────

      `chromium-autostart.sh` setzt vor dem Start:

          export PULSE_SERVER="unix:/run/user/<uid von dietpi>/pulse/native"

      ROOT DARF AUF DEN FREMDEN SOCKET: `/run/user/1000` ist 0700, aber root
      umgeht die Rechtepruefung. NICHT BEHAUPTET, SONDERN GEFAHREN:

          sudo -n env PULSE_SERVER=unix:/run/user/1000/pulse/native \
            paplay /usr/share/sounds/alsa/Front_Center.wav

      erschien als `application.name = "paplay"` unter den sink-inputs und
      war hoerbar. Eine Anmeldung (PULSE_COOKIE) verlangt der
      PipeWire-Pulse-Server dabei nicht.

      DIE ZEILE STEHT NACH DER WARTESCHLEIFE auf den Webserver (bis 30 s) —
      dann ist PipeWire laengst oben. `Linger=yes` fuer dietpi sorgt
      ausserdem dafuer, dass seine Benutzerdienste ohne Anmeldung starten.

      ── WARUM NICHT GLEICH DER GROSSE WEG ──────────────────────────────

      [[mupi-kiosk-als-dietpi-anleitung]] beschreibt das Richtige: den Kiosk
      als Benutzer fahren. Das ruehrt an X, Autologin und DSI — eine
      Tonfrage ist kein Anlass, die Bootkette umzubauen. Der kleine Weg ist
      drei Zeilen, zurueckdrehbar und wirkt sofort.

      ── EIN ZUSTAND, DEN MAN DABEI SIEHT ───────────────────────────────

      DIE BOX-KOPIE VON `chromium-autostart.sh` IST AELTER ALS DAS REPO
      (Stand 05.09.2026, vor E118/1d) — und `ausliefern.py --nur scripts`
      fasst sie NICHT an: es liefert `scripts/mupibox/*`, diese Datei liegt
      unter `/var/lib/dietpi/dietpi-software/installed/`. Die Aenderung
      wurde deshalb GEZIELT in die Box-Kopie eingesetzt (mit Sicherung
      `*.vor-ton-20260920`), nicht die Repo-Fassung darueberkopiert: die
      haette nebenbei vier weitere Unterschiede mitgebracht, und diese Datei
      entscheidet, ob beim naechsten Start ueberhaupt ein Bild kommt.

      NACHMESSEN:
          scp tools/box/kiosk-ton-probe.py dietpi@<box>:/tmp/
          ssh dietpi@<box> 'python3 /tmp/kiosk-ton-probe.py'
      Nach einem Neustart muss dort „PULSE_SERVER unix:/run/user/1000/…"
      stehen — und waehrend ein Video laeuft ein Strom des Browsers unter
      den sink-inputs.
  - id: zweiter-const-im-block-legt-den-ganzen-weg-stumm
    kind: falle
    title: "Ein zweites `const args` im selben Block hat zehn Tage lang JEDE Aufnahme abgebrochen"
    match: "Cannot access .* before initialization|aufnahme bricht sofort ab|tdz todeszone const verdeckt|zweimal const gleicher name block|aufnahmeweg tot seit commit"
    sev: high
    tags: ["mitschnitt", "soloist", "plugin", "javascript", "wache", "falle", "e135", 2026]
    source: >
      20.09.2026, Betreibermeldung „Aufnahme hat Probleme" mit drei Zeilen aus
      der Liste. Zwei davon waren alt; die dritte lautete
      `Cannot access 'args' before initialization`. Nachgestellt in vier Zeilen
      Node, behoben in plugins/mixpi-mitschnitt/index.mjs, Wache dazu
      tools/tdz-schatten-schau.mjs.
    body: >
      DER FEHLER: In `einenAufnehmen` stand `const args = aufnahmeArgumente(…)`
      auf Funktionsebene, und dreihundert Zeilen weiter unten — INNERHALB des
      grossen `try`-Blocks — ein zweites `const args = veredelungsArgumente(…)`
      fuer die Tags. Eine `const` verdeckt den aeusseren Namen ab BLOCKANFANG,
      nicht erst ab ihrer Zeile. Der `spawn('/usr/local/bin/soloist', args, …)`
      ganz oben im selben `try` griff damit in die Todeszone der unteren
      Deklaration und warf sofort.

      WARUM ES NICHT AUFFIEL: Der `catch` um den ganzen Ablauf macht aus jedem
      Fehler den `grund` eines gescheiterten Titels. Es sah aus wie ein
      Aufnahme- oder Tonproblem, nicht wie ein Absturz. Jeder vorgemerkte Titel
      verbrauchte daran seine drei Anlaeufe und blieb auf `fehler` stehen.
      Eingeschleppt mit 60d39e58 (10.09.2026, Tags und Cover fuer den
      Nachschnitt), ausgeliefert und auf der Box (a4e4d019, 19.09.) enthalten —
      zehn Tage ohne eine einzige Aufnahme des Arbeiters. Der PASSIVE Live-Weg
      lief die ganze Zeit weiter, was den Ausfall zusaetzlich gedeckt hat.

      WARUM KEIN ZEUGE HALF: Die Stelle liegt hinter `spawn`, PipeWire und
      Spotify; kein Test dieses Plugins betritt `einenAufnehmen`, alle 265
      blieben gruen. Die SORTE dagegen sieht ein Parser sofort — deshalb wurde
      eine Wache gebaut statt eines Zeugen: `tools/tdz-schatten-schau.mjs`
      liest alle verfolgten .mjs/.js mit `acorn` und meldet jeden Namen, der in
      seinem Block VOR der eigenen Deklaration gelesen wird. Sie haengt in
      `tools/doku-luecken-probe.sh`. Gegengeprobt an einer Kopie mit wieder
      eingebautem `const args`: Fund in genau der `spawn`-Zeile, Rueckgabe 1.
      Vgl. [[wache-auf-sorte-nicht-auf-pfad]].

      DER FIX ALLEIN HOLT KEINEN TITEL ZURUECK: `naechster()` nimmt nur
      `offen`, und 77 Eintraege lagen auf `fehler`. Dafuer gibt es jetzt
      `tools/mitschnitt-fehler-befreien.py --box dietpi@<adresse>` — es liest
      die Liste ueber die Plugin-Flaeche, waehlt die Eintraege mit genau
      diesem Grund und schickt sie durch `http/neuladen` (befreit und merkt
      mit Vorrang vor). Ohne `--wirklich` zeigt es nur. Am Dateipfad vorbei zu
      schreiben waere falsch: `liste.json` gehoert dem laufenden Dienst.

      WAS DIE WACHE BEIM BAUEN SELBST LEHRTE: Ihre erste Fassung kannte nur
      Bloecke und meldete sieben Tote — `for (const w of …)` und
      Funktionsparameter gegen ein spaeteres `const w` der umgebenden Funktion.
      Parameter, Schleifenkoepfe und `catch`-Namen binden genauso und gehoeren
      als eigene Bereiche ins Modell, sonst ist die Wache nach drei Tagen
      abgeschaltet.

      ZWEI ALTE GRUENDE AUS DERSELBEN MELDUNG, beide aus dem Zweig „der Knoten
      des Aufnahmelaufs kam nicht": `Soloist: Error: --single-track requires a
      valid playable Spotify URI` ist ein TITELFEHLER (die URI ist formal
      gueltig, Spotify gibt das Stueck nicht her). `Soloist: became active
      device` ist dagegen gar keine Fehlermeldung, sondern Soloists
      Erfolgsmeldung: der Zweig nimmt mit `.pop()` die LETZTE Ausgabezeile, und
      wenn Soloist sauber startet, aber binnen 20 s kein PipeWire-Knoten
      auftaucht, wird die Statuszeile als Grund gemeldet. Der wirkliche Grund
      (Knotensuche lief in die Frist) geht dabei verloren — noch offen.
      Vgl. [[selbstbeendender-prozess-statt-schwellenwert]].
  - id: aufgefuellt-wird-nur-zwischen-zwei-eintraegen
    kind: falle
    title:
      de: "Aufgefuellt wird nur zwischen ZWEI Eintraegen — ein Mitschnitt ohne Spotify-Zeile bleibt bei seinem Bestand"
    summary:
      de: "Die Kachel zeigte 9 von 15 Titeln: `waehleInhalt` waehlt die vollere Quelle, aber es gab nur eine"
    tags: [mitschnitt, verschmelzung, spotify, medien, falle, 2026]
    source:
      de: "20.09.2026, Betreiber-Befund an Box 192.168.178.62; gemessen mit tools/mitschnitt-quelle-anlegen.py und curl gegen /api/werke/<k>/inhalt"
    body:
      de: |
        DER BETREIBER-SATZ, woertlich: „die box zeigt von ohrwuermer und
        kinderlieder nur die aufgezeichneten aber nicht die fehlenden. Warum
        wird nicht aufgefuellt mit den internet verfuegbaren."

        Das ist DERSELBE SATZ wie am 29.08.2026 zu „Nah" von Alin Coen, der
        [e95-stufenplan-titelweise-quelle] ausgeloest hat — und trotzdem eine
        ANDERE Ursache. Die eine Frage, die trennt: WIE VIELE QUELLEN HAT DAS
        WERK?

          zwei Quellen, die falsche gewinnt  -> `waehleInhalt`, Stufe 1, behoben
          nur EINE Quelle                    -> dieser Eintrag

        == AM GERAET GEMESSEN (20.09.2026, Box .62) ==
          Ohrwuermer und Kinderlieder mit Eva und dem Elefanten   9 von 15
            Dateien 01,05,06,08,10,11,12,14,15 — 02,03,04,07,09,13 fehlen
          Die 30 besten Kinderlieder 2025                         4 von 30

        Der Mitschnitt ist ein PASSIVER Abgriff: aufgenommen wird, was gespielt
        wurde. Ein halb gehoertes Album liegt danach halb auf der Platte.

        == DIE KETTE, GLIED FUER GLIED ==
        1. `/api/werke/<k>/inhalt` holt bei eingeschalteter Verschmelzung ALLE
           Kandidaten und nimmt den volleren (`waehleInhalt`, verschmelzung.ts).
           Das IST das Auffuellen, und es ist gebaut.
        2. „Alle Kandidaten" heisst `eintraegeInBevorzugung` (server.ts): zu
           jedem Schluessel des verschmolzenen Werks der EINTRAG aus data.json.
           Ein Schluessel OHNE Eintrag wird uebergangen — still.
        3. Fuer diese Alben gab es keinen Spotify-Eintrag. Das Kind hatte sie
           nicht von einer Album-Kachel aus gespielt. Also EIN Kandidat, und
           der war der Mitschnitt. Es gab nichts, wovon aufzufuellen war.

        DIE LEHRE, allgemeiner: Eine Auswahlregel kann nur waehlen, was ihr
        jemand hinlegt. Wer eine Auswahl baut, muss auch sagen, WER die
        Kandidaten beschafft — sonst ist die beste Regel folgenlos, und sie
        sieht dabei aus, als arbeite sie.

        == WARUM DER MITSCHNITT SIE NICHT BESCHAFFT HAT ==
        `spotifyQuellSchluessel` (plugins/mixpi-mitschnitt/index.mjs) las nur
        `context.uri` — die Quelle, AUS DER gespielt wurde. Zwei Folgen:

          * Kein Kontext (Kuenstlerseite, Radio) -> gar keine Zuordnung.
          * Kontext `playlist`/`show` -> eine Zuordnung, die
            `verschmelzeWerke()` wegen verschiedener `art` still abweist
            (Fall 2). Auf der Box lagen VIER solcher toten Zuordnungen, alle
            Stufe „hand", alle ohne `auchSchluessel` in /api/werke. Eingebaut,
            gruen, wirkungslos. Haette eine GEGRIFFEN, waere es schlimmer
            gewesen: zwei verschiedene Folgen als EINE Kachel, dahinter die
            ganze Playlist.

        Links steht immer ein ORDNER, und der gehoert einem ALBUM
        (`albumOrdner`). Also gehoert rechts ein ALBUM hin — nicht das, woraus
        gerade gespielt wurde. Seit dem 20.09.2026 nimmt `titelAus` deshalb
        `album.uri` mit (`albumUri`, kostet keinen Abruf), und
        `SPOTIFY_KONTEXT_ARTEN` kennt nur noch `album`.

        == UND DIE ZUORDNUNG ALLEIN REICHT NICHT ==
        Sie zeigt sonst auf einen Schluessel ohne Eintrag (Glied 2 oben).
        `verschmelzungFestschreiben` legt deshalb ZUERST die Quelle an
        (`quelleAnfrage` -> POST /api/medien, 409 ist der Normalfall ab dem
        zweiten Stueck) und schreibt DANN fest. Die Kategorie reist von der
        Kachel mit: verschiedene Kategorien liesse `KATEGORIE_TRENNT`
        (abgleich.ts) gar nicht zusammen.

        == DER BESTAND: tools/mitschnitt-quelle-anlegen.py ==
        Fuer Kacheln, die vor dieser Aenderung entstanden. Es holt die Kennung
        NICHT aus einer Namenssuche (die haelt „Deluxe"/„Live"/„Remaster" nicht
        auseinander), sondern aus der Vormerkliste des Mitschnitts: sie fuehrt
        je aufgenommenem Stueck die `spotify:track:`-Kennung, und `GET
        tracks/<id>` nennt das Album dazu. Eine Tatsache, keine Aehnlichkeit.
        GEGENPROBE: zwei Stuecke werden aufgeloest und muessen dasselbe Album
        nennen; sonst bleibt der Ordner liegen.

        `tools/mitschnitt-zuordnungen-nachtragen.py` daneben PAART nur mit
        einer vorhandenen Zeile — es kann keine beschaffen. Das ist der
        Unterschied, und er war die ganze Luecke.

        == WAS DER ERSTE LAUF GEKOSTET HAT ==
        Er legte auch fuer „Die 30 besten Lieder fuer Maedchen" eine Quelle an.
        Dieses Album steht ZWEIMAL lokal da (einmal `audiobook`, einmal
        `music`) unter DEMSELBEN Schluessel — der entsteht aus Interpret und
        Titel, nicht aus der Kategorie. `verschmelzeWerke()` weist jede
        Zuordnung auf einen doppelten Schluessel ab (Fall 3), also verschmolz
        die neue Zeile mit nichts und stand als DRITTE Kachel im Regal. Zwei
        Zeilen wurden von Hand wieder entfernt; das Werkzeug ueberspringt
        mehrdeutige Schluessel seitdem und sagt es.

        == ERGEBNIS, gemessen ==
          21 verwaiste Alben bekamen ihre Quelle.
          /api/werke/spotify:0sTjN1YO9XfCOkJx9VXVVV/inhalt?verschmelzen=1
            -> 15 Titel, 9 `quelle: lokal`, 6 `quelle: spotify`, in der
               richtigen Reihenfolge. Genau das, was der Betreiber wollte.
          Keine einzige neue Kachel: alle 21 tragen `auchSchluessel`.

        Verwandt: [inhalt-rueckfall-backend-kapselt-fusion] (der Rueckfall
        selbst), [e95-stufenplan-titelweise-quelle] (die Stufen),
        [verschmelzung-legt-zu-viel-zusammen] (die andere Richtung).
  - kind: entscheidung
    id: uhr-sagt-die-zeit-beim-antippen
    title: "Ein Tipp auf die Uhr sagt die Zeit - der Schalter steht in BEIDEN Menues"
    tags: [vorlesen, tts, uhrzeit, kopfzeile, darstellung, kinder, wache, 2026]
    source: "20.09.2026; gemessen mit tools/uhr-sagt-die-zeit.mjs gegen eine eigene Vorschau (Port 8321), Wache tools/menue-schalter-deckung.py"
    body: |
      DER AUFTRAG, woertlich (Betreiber, 20.09.2026): "ich moechte einen
      schalter in beiden menues haben uhrzeit beim anklicken vorlesen."

      == WAS GEBAUT WURDE ==
      Ein Darstellungs-Schluessel `uhrzeitSprechen` (Vorgabe AUS). Steht er an
      UND steht die Uhr oben in der Mitte (`uhrzeit`), sagt ein Tipp auf die
      Uhr die Zeit: "Es ist 15 Uhr 43." Gesprochen wird ueber Piper
      (`/api/vorlesen/sprich?abspielen=1`, also die Box selbst - im Cog-Kiosk
      verpufft `new Audio()` lautlos [cog-kiosk-hat-keine-tonbruecke]).

      DIE DREI ORTE:
        NewDesign/app.js   `uhrSatz()` (reine Regel), `kopfMitte.ansagen()`,
                           `kopfMitte.sprechen` (gesetzt in `anwenden()`) und
                           ein Eintrag in ANZEIGE_SCHALTER (Seite "Anzeigen").
        NewDesign/app.css  `.km-uhr.km-sprechend` - siehe unten, das ist der
                           stille Teil.
        darstellung.ts     Feld, Vorgabe und der Knopf "Tipp sagt die Zeit"
                           (Optik -> Anzeigen), ausgegraut ohne `uhrzeit`.

      == "BEIDE MENUES" IST EINE BAUREGEL UND KEINE LAUNE ==
      Box-Eltern-Bereich und Verwaltung schreiben in DIESELBE
      darstellung.json. Ein Schalter, den es nur an einem Ort gibt, ist eine
      Einstellung, die je nach Standpunkt zu existieren scheint oder nicht.
      Deshalb wacht seit heute `tools/menue-schalter-deckung.py` darueber
      (in tools/pruefen.sh): jede `id` aus ANZEIGE_SCHALTER muss in
      darstellung.ts eine SCHREIBNAHT haben.

      GESUCHT WIRD `setz({ feld` UND NICHT DER NAME - der Unterschied ist der
      ganze Wert dieser Wache. Jedes Feld steht in darstellung.ts dreimal (im
      Interface, in STANDARD, vielleicht an einem Knopf). Die erste Fassung
      suchte den Namen und war deshalb gruen, sobald das Feld im Interface
      stand [gegenprobe-bleibt-gruen-ist-der-fund]. Mit der scharfen Suche
      fielen FUENF Einseitige auf, datiert vermerkt in `NUR_AM_GERAET`:
      `lautGlas`, `torGlas`, `kissenMicro`, `kinderzeitPassung` - und
      `verschmelzen`, das in darstellung.ts Feld UND Vorgabe hat, aber nie
      einen Knopf hatte.

      == DER STILLE FEHLER: pointer-events ==
      `.kopf-mitte` hat `pointer-events: none` (app.css, mit Absicht: die
      Zahlen oben sollen das Regal dahinter nicht abschirmen). Eine Uhr, die
      nicht ausdruecklich `pointer-events: auto` zurueckholt, SIEHT antippbar
      aus und ist es nicht - am Bildschirm ist das unsichtbar, die Uhr steht
      ja da. Die Klasse `km-sprechend` holt es zurueck, legt die
      Trefferflaeche (gemessen 68x39 px statt 44x23 - 9-mm-Regel) und setzt
      `role="button"`; ohne Ansage wird beides wieder ABGERAEUMT, denn ein
      `role="button"` an einem Text, der nichts tut, kuendigt einer
      Sprachausgabe einen Knopf an, den es nicht gibt.

      == ES HAENGT NICHT AM VORLESE-MODUS DER KACHELN ==
      `config/vorlesen.json` steht ab Werk auf "aus", weil eine Box, die bei
      JEDEM Tipp einen Namen sagt, beim Spielen stoert [vorlesen-tts]. Die Uhr
      sagt nur etwas, wenn jemand sie ausdruecklich antippt - sie kann also
      nichts unterbrechen, was man nicht selbst bestellt hat. An den
      Kachelmodus gebunden hiesse: wer die Zeit hoeren will, bekommt jeden
      Albumnamen dazu. Gebraucht wird PIPER, nicht der Modus.

      == DER SATZ IST EINE GEPRUEFTE REGEL, KEINE VORLAGE ==
      `uhrSatz(std, min)` steht in NAMEN von tools/neu-regeln-auszug.py und
      hat in tools/pruef-neu-regeln.js sieben Messungen, darunter alle 1440
      Minuten des Tages. Zur vollen Stunde OHNE Minute ("Es ist 15 Uhr", nicht
      "15 Uhr 0"), die Minute OHNE fuehrende Null (die Anzeige braucht sie fuer
      die feste Breite, Piper laese sie als eigene Zahl), 24 Stunden wie die
      Anzeige daneben, und bei einer unmoeglichen Zeit ein LEERER Satz - dann
      bleibt die Box still, statt "Es ist 24 Uhr 70" genauso ueberzeugt zu
      sagen wie die Wahrheit. Wer die Ansage braucht, kann die Ziffern nicht
      lesen und hat keine zweite Quelle.
      NICHT VERWECHSELN mit `uhrWort` im Uhr-LERNSPIEL: das kennt nur den
      Zwoelferkreis mit voller und halber Stunde ("halb 4").

      == GEMESSEN, MIT EINEM ECHTEN FINGER ==
      `tools/uhr-sagt-die-zeit.mjs` (in tools/pruefen.sh) fahrt drei Lagen
      gegen die Attrappe: an/an, an/aus, aus/an. Es tippt mit
      `Input.dispatchTouchEvent` (tools/finger.mjs) und liest, WAS gesagt
      wurde (`/vorschau/gesprochen`) - Sprache bewegt kein Pixel, und genau
      hier war das Vorlesen schon einmal eingebaut, verdrahtet und stumm
      [vorlesen-beim-halten]. Ergebnis am 20.09.2026: "16:16" am Schirm,
      "Es ist 16 Uhr 16." gesagt, ausgeschaltet nichts.

      DREI GEGENPROBEN, und eine war der eigentliche Fund:
        Trefferflaeche abgeschaltet   -> 6 Fehler (Klasse, pointer-events,
                                         role, Groesse, keine Ansage).
        `uhrSatz` ohne Vollstunden-Fall -> 3 Fehler im Regel-Pruefer.
        `ansagen()` ignoriert den Schalter -> BLIEB GRUEN. Ausgeschaltet
          halten die Uhr ZWEI Riegel still, und solange der CSS-Riegel haelt,
          ist der JS-Riegel mit einem Finger gar nicht messbar. Deshalb tippt
          das Werkzeug im zweiten Abschnitt ZUSAETZLICH per `element.click()`
          am `pointer-events` vorbei - damit fiel die Sabotage auf.

      == WAS NOCH NICHT AM GERAET GEMESSEN IST ==
      Alles oben ist an der Vorschau gemessen, nicht an der Box: ob die
      Piper-Stimme "15 Uhr 43" gut ausspricht, sagt nur der Lautsprecher.
      Die Naht ist dieselbe, die die Kacheln benutzen (Zwischenspeicher, erste
      Ansage rund 2,9 s, danach 0,013 s [vorlesen-tts]) - eine Minute wird
      also beim ersten Mal des Tages kurz warten.
  - id: spielbereich-vier-werke-und-eine-stimme
    kind: knowhow
    title: Vier Spiele mit Einzelschaltern und eine eigene Stimme — was dabei je Schalter anders herum gilt
    tags: [spielecke, spiele, vorlesen, einstellungen, vorgabe, kinder, wache, 2026]
    quelle: 20.09.2026, Betreiberwunsch; gebaut und in der Vorschau gemessen (Spiele-Schau sechs Messungen, zweimal gruen, Sabotage dreimal rot)
    lang: |
      Betreiber: „in der spiele sektion will ich das vorlesen noch
      einschalten koennen und auch verschiedene spiele ein und ausschalten
      koennen." Aus einem Spiel (Schlange) wurden vier, aus einem Schalter
      sechs, und der Spielbereich bekam eine eigene Stimme.

      WAS ES JETZT GIBT (Verwaltung -> Spiele, `config/spiele.json`):
        an        der ganze Bereich (seit 19.09.2026)
        spiele    je Spiel einer: schlange, memory, dreigewinnt, farben
        vorlesen  spricht der Spielbereich? Spielname, aufgedeckte Karte,
                  Farbfolge, Ausgang

      ══ DIE RICHTUNG DES ZWEIFELS IST JE SCHALTER EINE ANDERE ══

      `an` und die einzelnen Spiele: `!== false`. Die Stimme: `=== true`.
      Das sieht nach Schlamperei aus und ist der Kern:

        * Ein Schalter, der eine VORHANDENE Funktion abschaltbar macht, darf
          sie nicht wegnehmen — jede heute gespeicherte Datei kennt den
          Schluessel nicht [[ein-neuer-schalter-darf-nichts-wegnehmen]].
          Das gilt auch fuer ein Spiel, das erst heute dazukommt: die Datei
          von gestern kannte es nicht, ABGESCHALTET hat es trotzdem niemand.
        * Ein Schalter, der eine NEUE Funktion bringt, darf sie nicht
          aufdraengen. Mit `!== false` faenge jede Box im Haus nach dem
          Einspielen an zu sprechen — eine Aenderung, keine Einstellung.

      Beide Zeilen stehen absichtlich nebeneinander in `spiele.ts`: nur der
      Unterschied erklaert sie.

      ══ DIE LISTE STEHT AN DREI ORTEN, UND DIE KENNUNG IST DER SCHLUESSEL ══

        src/backend-api/src/spiele.ts   SPIELE_WERKE — die Wahrheit
        NewDesign/app.js                SPIEL_WERKE / SPIEL_ORDNUNG
        tools/neu-vorschau.mjs          SPIEL_WERKE_VORSCHAU (Attrappe)

      Laufen sie auseinander, passiert NICHTS SICHTBARES: Der Server wirft
      unbekannte Kennungen beim Normalisieren weg, `darf()` liest eine
      fehlende als „an" — das Spiel laesst sich also nicht mehr abschalten,
      und der Schalter in der Verwaltung wirkt trotzdem, nur auf nichts.
      Dagegen wacht `tools/spiele-liste-deckung.py` (in `tools/pruefen.sh`).
      Sie meldet auch eine LEERE Fundliste als Fehler — sonst waere die
      naechste Umbenennung eine gruene Wache, die gar nichts mehr misst
      [[gegenprobe-bleibt-gruen-ist-der-fund]].

      ══ DIE TUER MUSS BEIM ERSTEN MAL AUF DIE ANTWORT WARTEN ══

      IN DER VORSCHAU GEMESSEN, nicht befuerchtet: Direkt nach dem
      Seitenaufbau ist die Schalterliste in app.js noch leer, und leer heisst
      „alle an". Wer in genau diesem Augenblick die Spiele-Taste drueckt,
      bekam die Auswahl MIT den abgeschalteten Spielen — und bei „jedes Spiel
      einzeln aus" ging die Tuer sogar auf. Nach einem Kiosk-Neustart liegt
      zwischen erstem Bild und erstem Tastendruck oft weniger als eine
      Sekunde.

      Die Antwort ist NICHT „im Zweifel zu" (ein Schalter, der im Zweifel
      zumacht, erzeugt Fehler, die niemand nachstellen kann), sondern:
      `spieleckeOeffnen` ist `async` und wartet BEIM ALLERERSTEN MAL auf
      `spielbereich.laden()`. Danach nie wieder — der Wert ist dann
      hoechstens einen Takt alt. Faellt die Abfrage aus, bleibt `geladen`
      falsch und es gilt weiter die Vorgabe „an": das fruehe Ja wird nicht
      abgeschafft, es wird nur nicht mehr GERATEN.

      ══ WARUM KEIN PONG, OBWOHL ER IHN VORGESCHLAGEN HAT ══

      Die Eingabekette (Tastendruck -> Prellsperre 250 ms -> Fach im Server
      -> Abholtakt 50 ms im Spiel) liefert rund vier Richtungswechsel je
      Sekunde. Ein Schlaeger, der einem Ball folgen muss, braucht mehr — das
      Spiel waere gebaut, ausgeliefert und erst an der Box als unspielbar
      aufgefallen. Die drei neuen sind deshalb ZUGWEISE: Paare, Drei gewinnt,
      Farben merken. Bei keinem kostet eine halbe Sekunde Verzug etwas.

      ══ KEINE EMOJI AUF DER BOX ══

      `fillText('einen Apfel')` gibt auf DietPi leere Kaesten: kein
      Emoji-Schriftschnitt installiert, und zwar lautlos. „Paare" malt
      deshalb Formen (Kreis, Quadrat, Dreieck, Herz, Stern, Raute) — die
      haben nebenbei einen Namen, den die Stimme sagen kann. Aus demselben
      Grund gibt es `spielKasten()` statt `ctx.roundRect()`: im Kiosk laeuft
      wahlweise Chromium ODER Cog/WPE [[cog-kiosk-hat-keine-tonbruecke]], und in
      aelterem WebKit ist `roundRect` eine Exception mitten im Zeichnen —
      also ein schwarzes Feld, kein huebscher Kasten weniger.

      ══ DIE BOX HAT ZWEI SPIELORTE — UND EINER WAR NIRGENDS ABSCHALTBAR ══

      BEIM BAUEN GEFUNDEN, nicht gesucht: Neben der Spielecke gibt es seit
      dem 03.09.2026 die SCHUBLADE mit sechs Tipp-Apps (Memory, Puzzle,
      Rechnen, Uhr, Lesen, Malen, in `NewDesign/apps.js`). Sie hatte genau
      die Luecke, die die Spielecke bis zum 19.09. hatte: kein Schalter,
      nirgends. Fuer einen Betreiber ist „was darf mein Kind spielen" EINE
      Frage, auch wenn die Box zwei Spielorte hat — beide stehen deshalb auf
      der Spiele-Seite, in zwei Karten, und in `spiele.json` in zwei
      Faechern (`spiele`, `apps`).

      `memory` steht in BEIDEN Listen, und das ist kein Fehler: die
      Schubladen-App zeigt Bilder aus der Bibliothek und will einen Finger,
      „Paare" in der Spielecke zeigt Formen und will ein Steuerkreuz. Die
      Wache prueft jede Liste FUER SICH.

      DER SCHUBLADEN-SCHALTER MUSS IM LAUF WIRKEN: `schubladeBauen()` steigt
      bei gleicher Werkliste frueh aus und weiss von einem Schalter nichts.
      Ohne einen ausdruecklichen Neubau bei Aenderung saehe ein Betreiber
      das Abschalten erst beim naechsten echten Wechsel der Bibliothek, also
      unter Umstaenden nie — dieselbe Falle wie beim Vorlese-Modus und dem
      Eintrag „Lesen". Die Messung dazu laeuft deshalb OHNE Seitenaufbau.

      ══ UND EINE LEHRE UEBER DIE WACHE SELBST ══

      Der erste Lauf der erweiterten Spiele-Schau meldete, die Tuer gehe bei
      eingeschalteten Spielen nicht auf. Sie tat es doch — in der geliehenen
      Vorschau stand aus einer frueheren Messung noch „nur ein Spiel an"
      [[vorschau-wird-geliehen]]. Ein `finally`, das aufraeumt, hilft nur dem
      NAECHSTEN Lauf, und auch nur, wenn der vorige heil zu Ende kam. Jede
      Messreihe stellt ihre Ausgangslage jetzt am ANFANG her.

      Und der zweite Fehllauf war noch aelter: ein zweites Browserfenster auf
      derselben Vorschau HOLT DIE ANWEISUNGEN WEG. Wer misst, waehrend ein
      Pane-Tab auf demselben Port steht, misst die Haelfte.
  - kind: falle
    id: zwei-daempfungen-und-ein-ok-das-nichts-beweist
    title: "Nichts gehoert - zwei Daempfungen hintereinander, und ein ok:true, das keinen Ton beweist"
    tags: [ton, pipewire, alsa, lautstaerke, vorlesen, geraet, messen, werkzeug, 2026]
    source: "20.09.2026, Box .62 am Geraet gemessen (tools/ansage-kommt-an.py, tools/tonweg-pegel.mjs, tools/tonweg-durchgang.py)"
    body: |
      DIE LAGE: Die Box sollte nach dem Antippen der Uhr die Zeit sagen. Die
      Route antwortete `{"ok":true,"dauerMs":2528}`, im Zimmer war NICHTS zu
      hoeren. Zwei Stunden spaeter war es zu hoeren, ohne eine Zeile Code.

      == ERSTE LEHRE: `ok:true` IST KEIN TON ==
      `GET /api/vorlesen/sprich?abspielen=1` sagt ok, sobald die WAV ENTSTANDEN
      ist. `dauerMs` ist aus dem WAV-Kopf gerechnet, nicht gemessen. Und
      `ansageAbspielen` (server.ts) startet `pw-play` mit `stdio: 'ignore'`;
      sein `error`-Hoerer loest nur die Daempfung. Ein Abspielen, das ausfaellt,
      sieht von aussen genauso aus wie eines, das gelingt - dieselbe Sorte
      Fehler wie [vorlesen-beim-halten], nur eine Schicht tiefer.
      DESHALB GIBT ES JETZT `tools/ansage-kommt-an.py`: es loest die ECHTE
      Naht aus und schneidet gleichzeitig den Monitor der Endstation mit. Am
      20.09. gemessen: RMS 0,046, Spitze 0,38 - es lief also ein Signal,
      waehrend niemand etwas hoerte. Genau diese Trennung war der Fortschritt.

      == ZWEITE LEHRE: ES WAREN ZWEI DAEMPFUNGEN, UND EINE KANNTE NIEMAND ==
      Die Kette hatte HINTER PipeWire noch eine Station, die kein Werkzeug des
      Hauses las - den ALSA-Mischer der Karte:

          PipeWire, regelnde Senke (Box-Lautstaerke 24 von 55) ..... -36 dB
          ALSA-Mischer MAX98357A „Master" 44 % (softvol) ........... -28,8 dB
                                                                    ─────────
          zusammen rund -65 dB, also praktisch stumm

      `mupi-lautstaerke.sh` fasst `amixer` NUR an, wenn es gar kein PipeWire
      gibt (Rueckfallebene) - auf dieser Box also nie. Die 44 % sind damit eine
      Altlast, die kein Regler der Box erreicht: dieselbe Krankheit wie in
      [genau-eine-stufe-regelt-den-ton], nur eine Ebene TIEFER als dort
      behandelt. Behoben und gesichert:
          amixer -c 2 sset Master 255 && sudo alsactl store
      (`/var/lib/alsa/asound.state` traegt jetzt 255/255, alsa-restore zieht es
      beim Start.) Danach war dieselbe Ansage bei derselben Lautstaerke hoerbar.

      == DRITTE LEHRE: DAS MESSGERAET LOG, UND ZWAR SEIT ZWEI WOCHEN ==
      `tools/tonweg-pegel.mjs` nannte die VORGABE-Senke (`klangwerk`) „den
      Regler" und buchte die Hardware-Senke als „stille Daempfung, -53 dB".
      Seit dem 04.09.2026 traegt aber die HARDWARE-Senke die Nutzerlautstaerke
      (`regelnde_senke_name`). Das Werkzeug meldete also auf JEDER normalen Box
      einen Fehler, den es nicht gab - und verdeckte damit den, den es gab
      [dauerrote-wache-ist-keine]. Beides gerichtet: es fragt die regelnde
      Stufe wie mupi-lautstaerke.sh, und es liest den ALSA-Mischer mit
      (Karte ueber den NAMEN aus `aplay -l`, nicht ueber `-c 2` - die Nummer
      wandert). Gegenprobe: Mischer auf 44 % -> Exit 1 und der Befund,
      zurueck auf 100 % -> Exit 0.

      == WAS DABEI SONST NOCH AUFFIEL ==
      `pw-record` am Monitor der ALSA-Senke zeigt Signal, OBWOHL danach noch
      -65 dB kommen: der Monitor haengt VOR der Lautstaerke-Anwendung. Ein
      Pegel dort beweist „die Kette fuehrt Signal", NICHT „es ist laut".
      Und `/proc/asound/card<n>/pcm0p/sub0/status` sagt beim Abspielen
      `RUNNING` - auch das beweist nur, dass die Karte laeuft.
      Die Reihenfolge, die traegt: Signal in der Kette (Monitor) -> Karte
      laeuft (procfs) -> Pegel je Stufe (tonweg-pegel, JETZT mit ALSA) -> Ohr.

      == UND EIN ALTBEKANNTER, DER WIEDER ZUSCHLUG ==
      `node werkzeug | tail` meldete EXIT=0, obwohl das Werkzeug 1 gab - der
      Exit gehoert `tail`, nicht dem Werkzeug (die Regel steht in AGENTS.md
      unter „Eine Probe hinter einer Pipe verliert ihr Urteil"). Die Gegenprobe
      war damit fast als bestanden verbucht.
      Ebenso: `ssh host "python3 - " "ein Satz"` uebergibt die Zeile an die
      REMOTE-Shell, die sie neu zerlegt - aus „Hallo, hoerst du mich?" wurde
      das Wort „Hallo,". `shlex.quote` gehoert dazwischen.
  - id: videoquellen-jenseits-der-ard
    kind: entscheidung
    title: "Weitere Videoquellen: ZDF ja, YouTube nur eingebettet, Netflix nie"
    tags: [video, mediathek, zdf, youtube, netflix, belohnung, quellen, plugin, 2026]
    quelle: 20.09.2026, Frage des Betreibers („video feature ausbauen kann man noch von weiteren quellen videos beziehen ? zdf, youtube, netflix was ist moeglich"). Die Senderzahlen sind mit tools/videoquellen-probe.mjs gegen die echten Dienste gemessen, nicht geschaetzt.
    lang: |
      DIE FRAGE IST NICHT „hat die Quelle Videos", SONDERN: kommt am Ende eine
      progressive `video/mp4`-Adresse heraus? Der Bildweg der Belohnungs-Videos
      ist ein `<video>`-Element im Kiosk [[mediathek-plugin-weiss-kern-zaehlt]],
      und Chromium spielt HLS NICHT von sich aus. An dieser einen Linie
      zerfaellt die Antwort in drei sehr verschiedene Faelle.

      GEMESSEN AM 20.09.2026 (node tools/videoquellen-probe.mjs), ueber den
      oeffentlichen Suchdienst von MediathekView (mediathekviewweb.de/api/query
      — ein Tor fuer ALLE oeffentlich-rechtlichen Sender):

          ARD ZDF KiKA 3Sat ARTE.DE Funk   progressive MP4, HEAD 200
          SRF ORF                          NUR HLS (.m3u8) — braucht hls.js

      KiKA antwortet mit `application/octet-stream` statt `video/mp4`. Chromium
      schnueffelt und spielt es trotzdem; am Geraet ist das aber ungemessen.

      DIE DREI FAELLE:

        ZDF/KiKA/3sat/arte/funk   GEHT, und zwar als ZWEITES PLUGIN neben
            mixpi-mediathek, nicht als Umbau darin. Zwei Wege stehen offen:
            entweder je Sender die eigene Schnittstelle (api.zdf.de braucht
            einen aus der Webseite gepflueckten `apiToken` — Schnueffelei, die
            bei jedem Umbau der ZDF-Seite bricht), oder EIN Plugin gegen
            MediathekViewWeb, das alle auf einmal mitbringt. Der zweite Weg ist
            der kleinere: eine fremde Schnittstelle statt fuenf, und die
            MP4-Adresse steht schon im Suchergebnis.
            PREIS IM KERN: `VIDEO_PLUGIN = 'mixpi-mediathek'` in server.ts ist
            HEUTE EINE KONSTANTE, und die Freigabe speichert nur die Kennung
            ohne Quelle. Ein zweiter Lieferant heisst also: ein Feld `quelle`
            in der Freigabe, ein Rueckfall auf `mixpi-mediathek` fuer alle
            alten Eintraege, und eine Weiche beim Start.

        YouTube   GEHT NUR EINGEBETTET. Die MP4-Adresse herausloesen (yt-dlp)
            verstoesst gegen die Nutzungsbedingungen; wer es einbaut, baut eine
            Abhaengigkeit, die sich woechentlich selbst repariert und trotzdem
            regelmaessig steht. ERLAUBT ist der IFrame-Player: das Video laeuft
            in einem eingebetteten Rahmen, und seine Ereignisse
            (`onStateChange`, `getCurrentTime`) reichen fuer die 90-%-Schwelle.
            WAS MAN DABEI KAUFT: Werbung vor dem Video (auf einem Kinderschirm
            die eigentliche Frage), eine Suche nur mit Google-API-Schluessel und
            Tageskontingent, Videos die Einbettung verbieten, und die Pflicht,
            den „made for kids"-Stand zu pruefen (YouTube-Bedingungen, Stand
            Juni 2026). Dazu ein zweiter Abspielweg im Kinderschirm: nicht mehr
            `<video src>`, sondern ein fremdes Skript im Kiosk.

        Netflix   GEHT NICHT, und zwar nicht „schwierig", sondern gar nicht.
            Jeder Strom ist mit Widevine verschluesselt, und die Entschluesselung
            verlangt ein zertifiziertes Geraet plus Konto-Anmeldung. Es gibt
            keine Schnittstelle, die eine Adresse herausgibt. Dasselbe gilt fuer
            Disney+, Prime Video und jeden anderen DRM-Dienst. Wer hier Zeit
            hineinsteckt, steckt sie in einen Bruch der Verschluesselung.

      GEBAUT AM SELBEN TAG, und zwar genau so (20.09.2026): das Plugin
      `mixpi-mediathekview` steht, die Freigabe traegt ein Feld `quelle`, und
      der Kern haelt eine Erlaubnisliste `VIDEO_PLUGINS`. Wie die Kennung
      aussieht und warum sie so aussehen MUSS, steht in
      [[mediathekview-kennung-ist-der-suchweg-zurueck]]. YouTube und Netflix
      bleiben, wie sie unten stehen.

      DIE EMPFEHLUNG, DIE DAZU FUEHRTE: EIN Plugin `mixpi-mediathekview` gegen
      mediathekviewweb.de, das ZDF, KiKA, 3sat, arte und funk auf einen Schlag
      mitbringt und dieselbe Antwortform liefert wie mixpi-mediathek
      (`{ok, kennung, name, quelle:{adresse,breite,hoehe}}`). Dann bleibt der
      Kern bis auf die Weiche unberuehrt, und der Kinderschirm merkt nichts.

      NACHMESSEN (geht ins Netz, gehoert deshalb in KEINEN Laeufer):
          node tools/videoquellen-probe.mjs
          node tools/videoquellen-probe.mjs --sender ZDF,KiKA --begriff Maus
  - id: mediathekview-kennung-ist-der-suchweg-zurueck
    kind: falle
    title: Der Suchdienst durchsucht seine eigene Kennung nicht — also traegt die Kennung den Weg zurueck
    tags: [mediathek, mediathekview, zdf, video, plugin, kennung, belohnung, 2026]
    quelle: 20.09.2026, beim Bau von plugins/mixpi-mediathekview. Jede Zahl gegen mediathekviewweb.de gemessen, der ganze Weg mit tools/mediathekview-probe.mjs gefahren.
    lang: |
      JEDER EINTRAG VON mediathekviewweb.de TRAEGT EIN FELD `id` (base64 eines
      Hashes, 44 Zeichen). Es sieht aus wie der Schluessel, den man speichert
      und spaeter nachschlaegt. ES IST KEINER: der Dienst durchsucht dieses
      Feld nicht. GEMESSEN am 20.09.2026 — eine Anfrage mit
      `fields:["id"]` und genau der Kennung eines eben gelieferten Eintrags
      antwortet mit NULL Treffern.

      DAS IST NICHT EGAL, weil die Belohnungs-Videos die Adresse ABSICHTLICH
      nicht speichern: die Sender ziehen Folgen zurueck, und zwischen Freigabe
      und Anschauen liegen Tage [[mediathek-plugin-weiss-kern-zaehlt]]. Es
      MUSS also einen Weg von der gespeicherten Kennung zurueck zu einer
      frischen MP4-Adresse geben.

      DIE KENNUNG IST DESHALB KEIN FREMDER SCHLUESSEL, SONDERN DER SUCHWEG:
      base64url von Sender, Sendung und Titel (Trennzeichen 31). Beim
      Aufloesen wird nach dem TITEL gesucht und auf ALLEN DREI Teilen streng
      verglichen.

      WARUM DER SENDER MITMUSS: dieselbe Maus-Folge liegt unter ZWEI Sendern
      (KiKA als `AVC-540`, WDR als `AVC-720`, gleiche Datei-UUID). Ohne ihn
      entschiede die Reihenfolge der Antwort, welche Fassung laeuft.

      WARUM DER VERGLEICH STRENG IST UND NICHT UNGEFAEHR: ein geaenderter
      Titel heisst dann „nicht mehr in der Mediathek" — mit Grund, vor dem
      Kind. Ein unscharfer Vergleich gaebe ihm heimlich ein anderes Video.
      Dieselbe Richtung wie in [[kaputte-datei-darf-nicht-aufsperren]].

      DREI WEITERE EIGENHEITEN, alle am selben Tag gemessen:
        KEINE BILDER   Der Dienst fuehrt kein Bildfeld. Die Kacheln der
                       Elternflaeche bleiben ohne Vorschau.
        NUR HLS        SRF und ORF liefern ausschliesslich `.m3u8`. Ein
                       `<video>` in Chromium spielt das nicht — solche
                       Eintraege fallen schon in der SUCHE weg, nicht erst
                       beim Start.
        AUFLOESUNG     Steht bestenfalls im Dateinamen (`_AVC-720.mp4`,
                       `_960x540.mp4`). ZDF und 3sat nennen keine
                       (`_3360k_p36v17.mp4`); dort gilt die mittlere Stufe,
                       und `?stufe=klein|mittel|gross` macht diese ANNAHME am
                       Geraet widerlegbar.

      UND DER KOPF MUSS LUEGEN: mit `Content-Type: application/json` antwortet
      der Dienst 400. Der Rumpf IST JSON, der Kopf muss `text/plain` sein.

      NACHMESSEN (geht ins Netz, gehoert deshalb in KEINEN Laeufer):
          node tools/mediathekview-probe.mjs
          node tools/mediathekview-probe.mjs --vorlage-erneuern   # dann node --test
  - id: zeuge-der-nur-die-zweige-liest-die-er-kennt
    kind: falle
    title: 27 gruene Zeugen und trotzdem „undefined" — der dritte Zweig war ungelesen
    tags: [zeugen, gegenprobe, mediathekview, probe, 2026]
    quelle: 20.09.2026, gefunden von tools/mediathekview-probe.mjs beim ERSTEN Lauf gegen den echten Dienst, nachdem 26 Zeugen gruen waren.
    lang: |
      `besteAdresse()` in mixpi-mediathekview hat DREI Ausgaenge: die
      gewuenschte Stufe, die groesste lesbare unter dem Deckel, und — wenn
      keine Aufloesung lesbar ist — die mittlere. Zwei davon setzten den Namen
      der Stufe ausdruecklich; der dritte gab `gewaehlt.stufe` zurueck, und
      die Kandidaten hiessen dort `name`. Die Antwort trug also still
      `stufe: undefined`.

      26 ZEUGEN WAREN GRUEN. Sie lasen die Stufe in den beiden Zweigen, die
      sie setzten, und im dritten nur die HOEHE. Das ist der Fehler hinter
      dem Fehler: WER EINEN ZWEIG PRUEFT, PRUEFT NICHT AUTOMATISCH SEINE
      RUECKGABE — geprueft wird, was man abfragt, und ein Feld, das kein
      Zeuge liest, kann jeden Wert haben.

      GEFUNDEN HAT ES DIE PROBE GEGEN DEN ECHTEN DIENST, beim allerersten
      Lauf, weil sie das Ergebnis AUSDRUCKT statt es zu pruefen: in der Zeile
      stand „Stufe „undefined"". Das ist der Wert solcher Proben neben den
      Zeugen — sie behaupten nichts und zeigen alles.

      DIE LEHRE FUER DEN NAECHSTEN ZEUGENSATZ: zu jeder Funktion mit mehreren
      Ausgaengen EINEN Zeugen, der in ALLEN Ausgaengen DASSELBE Feld liest.
      Nicht je Ausgang ein anderes. Dieselbe Familie wie
      [[gegenprobe-bleibt-gruen-ist-der-fund]]: eine Pruefung, die nur die
      halbe Antwort ansieht, ist gruen aus dem falschen Grund.
  - id: kinderzeit-spec-faellt-ueber-neue-konstruktor-abrufe
    kind: falle
    title: "Die 14 Zeugen der Kinderzeit-Seite fallen ueber JEDEN neuen Abruf im Konstruktor"
    tags: [tests, frontend-admin, kinderzeit, falle, ablauf, 2026]
    quelle: ["gemessen und behoben 2026-09-20, Stand a3288a7c"]
    body: |
      GEMESSEN 2026-09-20: `ng test` im `src/frontend-admin` meldete
      14 FAILED / 223 SUCCESS, alle in `KinderzeitSeite`, alle mit DERSELBEN
      Meldung:

          Error: Expected no open requests, found 1: GET /api/start

      DIE SEITE WAR NICHT KAPUTT, DIE ZEUGEN WAREN VERALTET. Am selben Tag
      hatte 49087e6e den Start-Modus („letztes Profil macht weiter") in
      `kinderzeit.ts` gebaut und `void this.startLesen()` in den Konstruktor
      gestellt. Der Commit fasste `kinderzeit.spec.ts` nicht an. Jeder Test
      der Datei laeuft am Ende in `afterEach -> http.verify()`, und eine
      Anfrage, die niemand beantwortet hat, faellt dort ALLEN Tests auf die
      Fuesse — nicht nur denen, die mit dem neuen Ding zu tun haben.

      DAS IST DAS DRITTE MAL. Der Kopf der Spec zaehlt es selbst mit:
      03.08.2026 kam /api/schlummer dazu, 08.08.2026 /api/profile,
      20.09.2026 /api/start. Das Muster ist immer gleich, und die Zahl im
      Kommentar („die vier Abrufe des Konstruktors") ist der Zaehler.

      WARUM ES NIEMAND BEIM BAUEN SAH — das ist der eigentliche Fund:
      `tools/pruefen.sh` FAEHRT die Verwaltungstests (`admin_tests`,
      Schritt „Tests neue Verwaltung"), aber ERST NACH dem Ausstieg von
      `SCHNELL=1`. Wer „Kerntests gruen" meldet, hat diese Datei nicht
      gefahren. Wer eine Angular-Seite unter `src/frontend-admin` anfasst,
      muss den VOLLEN Lauf nehmen oder wenigstens:

          cd src/frontend-admin
          env CHROME_BIN=<chrome> npx ng test --watch=false --browsers=ChromeHeadless

      (Chrome wird nicht mitgeliefert; `browser_finden` in `tools/pruefen.sh`
      zeigt, wo gesucht wird — meist Playwright unter
      ~/.cache/ms-playwright/chromium-*/chrome-linux64/chrome.)

      WIE ES BEHOBEN WURDE, und warum nicht anders:
      `/api/start` kam als ausdrueckliches `http.expectOne('/api/start')` in
      die Aufbauhilfe `aufbauen()` und in die DREI Tests, die sich ihre
      Seite selbst bauen (Fehlerzweig „bleibt bedienbar", `mitEinemKind`
      unter „Bild aendern"). NICHT in die Liste `NEBENWEGE`, die
      `nebenkartenAbraeumen()` still wegflusht: der Start-Modus ist eine
      Einstellung DIESER Seite, keine Nebenkarte. Wer ihn abraeumt statt ihn
      zu benennen, macht die naechste Aenderung an ihm unsichtbar — genau
      der Zustand, aus dem dieser Eintrag entstanden ist.

      Dazu vier neue Zeugen unter `describe('Start-Modus')`, weil der
      Schalter in der Verwaltung ueberhaupt keine hatte: „letztes" nur bei
      genau diesem Wort, Unsinn faellt auf „fragen" zurueck, der Schalter
      springt bei misslungenem PUT zurueck, und gespeichert gilt, was der
      SERVER zurueckgibt. Danach 241 SUCCESS, tsc rc=0.

      GEGENPROBE (2026-09-20, jede einzeln, Seite danach byte-gleich):
        `void this.startLesen()` raus  -> 18 rot (alle, die aufbauen nutzen)
        Fehlerzweig setzt `vorher` nicht -> genau „nimmt den Schalter zurueck"
        `startLesen` glaubt jedem Wert   -> genau „faellt auf fragen zurueck"

      SIEHE AUCH [kinderzeit] fuer die Seite selbst.
  - id: stueck-schwelle-rechnet-gegen-das-stueck
    kind: falle
    title: Ein Stueck, das gegen die Videolaenge gezaehlt wird, laeuft unbegrenzt oft
    tags: [video, belohnung, stueck, marker, schwelle, kinderzeit, 2026]
    quelle: 20.09.2026, Auftrag des Betreibers („marker im video setzen zu koennen um definerte stuecke zumachen eine 25 min maus ist ggf zu lange und da wuerde ich es gerne teilen"). Beide Richtungen am Ende von tools/video-belohnung-schau.mjs gemessen, beide mit Gegenprobe.
    lang: |
      DIE BELOHNUNGS-VIDEOS ZAEHLEN BEI 90 % [[mediathek-plugin-weiss-kern-zaehlt]].
      Seit ein Video in STUECKE zerfallen kann, ist die Frage „90 % WOVON"
      keine Formalie mehr, sondern die ganze Sache — und sie geht in BEIDE
      Richtungen schief.

      RICHTUNG EINS, DIE OFFENSICHTLICHE: gegen die VIDEOLAENGE gerechnet
      erreicht ein Fuenf-Minuten-Stueck eines 25-Minuten-Videos die Schwelle
      NIE (480 von 1500 Sekunden sind 32 %). Das Stueck laeuft dann
      unbegrenzt oft, und es faellt NIEMANDEM auf: kein Fehler, keine
      Meldung, nur ein Kind, das mehr sieht. Dieselbe Sorte Schaden wie in
      [[kaputte-datei-darf-nicht-aufsperren]].

      RICHTUNG ZWEI, DIE TEURERE — sie kam beim Beheben der ersten herein und
      wurde noch am selben Tag von der Wache gefunden: wer daraufhin IMMER
      ueber Sekunden rechnet, bricht das GANZE Video. `dauerSek` stammt aus
      der Suche und kann falsch sein; ein Video, das laut Ablage 1626 s hat
      und in Wahrheit 2 s lang ist, verbrauchte sich nie mehr.

      DIE REGEL, DIE BEIDES LOEST, LAUTET: WEM GEHOERT DIE LAENGE?

        EINEM STUECK gehoert sie dem SERVER. `abSek`/`bisSek` DEFINIEREN das
        Stueck; der Browser koennte die Bezugsgroesse gar nicht kennen. Also
        meldet er SEKUNDEN, und der Server teilt durch `laenge(f)`.

        EINEM GANZEN VIDEO gehoert sie der DATEI. Der Anteil des Browsers
        misst, was wirklich lief. Also gilt weiter der Anteil.

      DAS STEHT IN `anteilAus()` in videofreigabe.ts, in drei Zeilen, und
      jede davon hat einen Zeugen.

      DREI WEITERE DINGE, DIE EIN STUECK BRAUCHT, und alle drei sind
      unsichtbar, wenn sie fehlen:
        SPRINGEN    `currentTime` VOR `loadedmetadata` verpufft lautlos.
        ANHALTEN    Ohne Halt bei `bisSek` laeuft Teil 1 in Teil 2 hinein.
        LEISTE      Sie zeigt das STUECK. Ein voller Balken in dem Moment, in
                    dem das Bild steht, ist die einzige ehrliche Anzeige.

      NACHMESSEN:
          npx cross-env NODE_ENV=test tsx --test \
            src/backend-api/src/videofreigabe.spec.ts
          node tools/neu-vorschau.mjs --port 8341 &
          node tools/video-belohnung-schau.mjs http://127.0.0.1:8341/neu/
      Die Wache hat dafuer zwei eigene Lagen: `videos-stueck` (Sekunde 0 bis
      1) und `videos-stueck-hinten` (1 bis 2). Die zweite gibt es nur, weil
      bei `abSek: 0` ein Schirm, der NICHT springt, genauso aussieht wie
      einer, der richtig springt.
  - id: attrappe-ohne-bereiche-verhindert-das-spulen
    kind: falle
    title: Eine Attrappe ohne Accept-Ranges laesst Chromium nicht springen — und die Wache klagt die Box an
    tags: [vorschau, attrappe, video, messen, chromium, range, 2026]
    quelle: "20.09.2026, beim Bau der Stueck-Messung in tools/video-belohnung-schau.mjs. Am Geraet der Messung belegt — mit 200 ohne Accept-Ranges blieb der Spieler bei 0,5 s, mit 206 stand er bei 1,51 s."
    lang: |
      DIE WACHE MELDETE „er ist nicht auf Sekunde 1 gesprungen", und der
      Fehler lag in der ATTRAPPE. `tools/neu-vorschau.mjs` lieferte ihr
      Probevideo mit `200 OK` und ohne `accept-ranges`. Chromium haelt eine
      solche Quelle fuer NICHT SPULBAR: `video.currentTime = 1` verpufft dann
      lautlos — kein Fehler, kein Ereignis, das Video laeuft von vorn.

      DER ECHTE SENDER KANN ES: `Accept-Ranges: bytes` steht an den
      MP4-Adressen der ARD (nachgemessen). Die Attrappe mass also eine Box,
      die es nicht gibt — und haette einen Schirm angeklagt, der richtig
      arbeitet.

      DIE LEHRE IST NICHT „Range einbauen", SONDERN ALLGEMEINER: eine
      Attrappe darf WENIGER Inhalt haben als das Original, aber nicht
      WENIGER FAEHIGKEITEN. Fehlt ihr eine, misst die Wache die Attrappe
      statt der Sache — und zwar in der gefaehrlichen Richtung, weil das
      Ergebnis wie ein echter Befund AUSSIEHT.

      DIESELBE FRAGE STELLT SICH VOR JEDER MESSUNG mit einer Attrappe: was
      kann der echte Gegenstand, das meine Nachbildung nicht kann? Hier waren
      es Bereichsabrufe; anderswo sind es Umleitungen, Fristen, Kopfzeilen.
      Es ist dieselbe Gegenfrage wie bei jeder Ursachensuche: WAS WUERDE
      MEINE ERKLAERUNG WIDERLEGEN? Hier war es ein `curl` mit
      Range-Kopfzeile — eine Minute Arbeit, und sie haette die halbe Stunde
      gespart.

      NACHMESSEN:
          curl -sI -H 'Range: bytes=100-199' http://127.0.0.1:8341/vorschau/probe.mp4
      206 ist richtig, 200 heisst: die Attrappe kann nicht spulen.
  - id: stand-belohnungs-videos-20-09-2026
    kind: note
    title: "Stand 20.09.2026: zwei Mediatheken und Stuecke, an Box .62 gemessen"
    tags: [stand, video, belohnung, mediathek, stueck, box, 2026]
    quelle: "20.09.2026, 22:05 an Box 192.168.178.62 gemessen, unmittelbar nach dem Ausliefern von 41eba054 (Rueckgabe 0, Standwache entwarnt)."
    lang: |
      ZUSTANDSAUSSAGE MIT HALTBARKEIT. Wer sie spaeter liest, misst nach,
      statt ihr zu glauben — die Ursachen weiter unten gelten weiter, die
      Zahlen nicht.

      WAS AUF DER BOX LIEGT (41eba054):
        14 Plugins geladen, darunter `mixpi-mediathek` UND das neue
        `mixpi-mediathekview` (ZDF, KiKA, 3sat, arte, funk, ARD-Anstalten).
        Suche und Aufloesung gemessen: „Maus" -> 3 Treffer aus KiKA, WDR und
        BR; Aufloesung liefert Stufe „mittel", Hoehe 540, HEAD 200 video/mp4
        mit `Accept-Ranges: bytes`.

      DIE WANDERUNG DER ALTEN ABLAGE HAT GEHALTEN. Die eine Freigabe, die
      vorher dalag („Wie kommt das Muster in die Murmel?", verbraucht 1 von
      1), trug nach dem Tausch `id == kennung`, `teil: ''`, `bisSek: 0` und
      denselben Zaehler. Genau das war der Zweck von `idAus()`
      [[stueck-schwelle-rechnet-gegen-das-stueck]] — keine Wanderung, kein
      Sonderfall.

      DAS ZAEHLEN GEGEN DAS STUECK IST AM GERAET BELEGT, nicht nur in der
      Vorschau. Mit zwei Mess-Stuecken (je 300 s eines 1770-s-Videos):
          280 s gemeldet -> `gezaehlt`   (gegen die Videolaenge waeren das
                                          16 % und damit NIE die Schwelle)
           60 s gemeldet -> `zuWenigGesehen`
      Beide Mess-Stuecke danach wieder entzogen.

      WAS AM SELBEN ABEND SCHON JEMAND ANDERES TAT: um 22:03 lagen vier
      Stuecke einer Maus-Folge in der Ablage (Marker bei 398, 872 und 1395 s,
      je einmal freigegeben, Quelle `mixpi-mediathekview`). Die hat der
      Betreiber in der Elternflaeche geschnitten — sie sind der erste echte
      Gebrauch der Funktion und wurden deshalb NICHT angefasst.

      WAS WEITERHIN UNGEMESSEN IST:
        * OB DAS BILD AM SCHIRM KOMMT. Gesprungen und angehalten wird hier
          gegen ein zwei Sekunden langes Probevideo der Attrappe. Ob ein
          Sprung in eine 700-MB-Datei ueber WLAN auf der Box fluessig ist,
          sagt nur die Box.
        * DER HOEHENDECKEL 720 in mixpi-mediathekview ist GESETZT und nicht
          gemessen. `?hoehe=` und `?stufe=` haengen daneben, damit man ihn am
          Geraet widerlegen kann, ohne Code zu aendern.
        * KiKA antwortet mit `application/octet-stream` statt `video/mp4`.
          Chromium schnueffelt und spielt es vermutlich trotzdem — am Schirm
          ungeprueft.
  - id: mixpi-netzlaufwerk-nas
    kind: architektur
    title: 'Netzlaufwerk (E28/N6-N9, E29/B4): eine SMB-Freigabe fuer Sicherungen — und die fuenf Regeln, die verhindern, dass sie die Box anhaelt'
    tags: [nas, smb, cifs, sicherung, netzwerk, systemd, verwaltung, e28, e29, 2026]
    source: >
      Gebaut 20.09.2026 auf Betreiberwunsch ("eine neue moeglichkeit die
      bibliothek und backups auf ein nas zu speichern"). Entscheidung desselben
      Tages: SMB3 mit eigenem Nutzer, Sicherungen als Spiegelung der
      vorhandenen Staende; Bibliothek-vom-NAS-lesen wurde NICHT gewaehlt,
      Mitschnitte sind vorgemerkt. Regeln rein in
      src/backend-api/src/netzlaufwerk.ts (27 Tests, zwei Sabotage-Gegenproben
      rot geworden), Routen /api/netzlaufwerk* in server.ts, Seite
      "Netzlaufwerk", Spiegelung in scripts/mupibox/mupibox-sicherung.py
      (--aufs-nas, 9 Selbsttests). Backend-Lauf 3001 Tests gruen. IM NETZ
      GEMESSEN mit tools/nas-sonde.py (neu): 192.168.178.1 horcht auf 445,
      192.168.178.199 auf 445 und 2049 und sagt anonym
      NT_STATUS_ACCESS_DENIED; showmount liefert dort eine leere Exportliste
      (typisch fuer reines NFSv4). AM GERAET IST NICHTS DAVON GEMESSEN — es
      gibt noch keine Freigabe und keinen Zugang.
    related: [mixpi-vpn-heimweg, sicherung-ausgelassene-schluessel-nie-schreiben, windows-freigabe-war-nie-da]
    body: |
      DIE FORM: Die Box bindet eine FREMDE Freigabe EIN (sie bietet keine an —
      das waere das andere Thema, siehe [[windows-freigabe-war-nie-da]]).
      Verwaltet werden ZWEI systemd-Einheiten, erzeugt aus der Eingabe der
      Verwaltung: <punkt>.mount und <punkt>.automount. Die Zugangsdaten gehen
      getrennt nach /etc/mupibox/nas-zugang (root, 0600), die Beschreibung
      ohne Geheimnis nach server/config/nas.json — dieselbe Teilung wie beim
      VPN ([[mixpi-vpn-heimweg]]).

      DIE FUENF REGELN (alle in netzlaufwerk.ts, je ein Test):

      1. ES HAENGT NIE BEIM START EIN. Die .mount-Einheit bekommt KEINEN
         [Install]-Abschnitt — sie zieht damit kein Ziel und wird beim
         Hochfahren von niemandem verlangt. Eingeschaltet wird nur die
         .automount daneben, und die haengt erst beim ZUGRIFF ein. Das ist
         Struktur, nicht Kommentar (E28/N7).

      2. ES DARF NICHT HAENGEN (E28/N6). `soft` statt `hard`, TimeoutSec=10,
         und der Einhaengepunkt MUSS unter /mnt/ liegen: ein Punkt im
         Anwendungsordner wird abgelehnt. Was die Box zum Abspielen braucht,
         liegt nie auf dem Netzlaufwerk — CIFS blockiert bei Zeitueberschreitung
         hart, und ein blockierender Zugriff im Tonweg froere die Wiedergabe
         ein. WIE LANGE es im Ernstfall wirklich haengt, ist NICHT gemessen;
         dafuer gibt es tools/nas-sonde.py --haengen (das Werkzeug misst, der
         Mensch zieht den Stecker).

      3. DAS PASSWORT STEHT IN KEINER EINHEIT (E28/N8, E15). Die Einheit nennt
         nur credentials=<pfad>. UND: mupibox-sicherung.py fuehrt nas-zugang
         ausdruecklich unter DRAUSSEN, wie tls — sonst truege eine Sicherung,
         die auf DIESES Netzlaufwerk gespiegelt wird, den Schluessel zu dem
         Ort mit, an dem sie liegt. nas.json (ohne Passwort) geht dagegen MIT,
         sonst wuesste nach einer Wiederherstellung niemand mehr, wohin
         gespiegelt wurde.

      4. KEIN PROZENTZEICHEN, KEIN STEUERZEICHEN in irgendeinem Wert. systemd
         ersetzt %-Kuerzel BEIM LESEN der Einheit (%h, %i und ein Dutzend
         mehr) — ein Freigabename mit % zeigte still woanders hin; ein
         Zeilenumbruch waere eine Direktive mehr. Beides wird ABGELEHNT, nicht
         entschaerft. Die Pruefung laeuft als Schleife ueber charCodeAt, NICHT
         als Zeichenklasse mit Backslash-u-Escapes: die wurden beim SCHREIBEN
         der Datei zu echten Steuerbytes: in der Zeichenklasse standen danach
         die Steuerzeichen SELBST statt ihrer Schreibweise, und die Pruefung
         traf etwas anderes als gemeint. Wer eine solche Klasse braucht,
         vergleicht die Zahl (charCodeAt < 0x20) statt ein Escape zu tippen.

      5. KEIN STILLER RUECKFALL AUF DIE KARTE (E28/N15). Ohne nas.json oder
         mit spiegeln:false passiert NICHTS, und das ist kein Fehler
         (Rueckgabe 0). Die Karte bleibt die Wahrheit und der Rueckweg ohne
         SSH (E29/B3): ein NAS hilft niemandem, dessen Bildschirm schwarz
         bleibt. Deshalb steht --aufs-nas als DRITTER ExecStart mit `-` in
         mupibox-sicherung.service — ein ausgeschaltetes NAS faerbt den
         Sicherungslauf nicht rot.

      WAS BEIM UEBERTRAGEN ZAEHLT: nebenan schreiben (.<name>.teil-<pid>),
      DANN innerhalb der Freigabe umbenennen. Karte -> NAS ist ein KOPIEREN
      ueber Dateisystemgrenzen; wer den Zielnamen sofort vergibt, hat fuer die
      Dauer der Uebertragung eine unvollstaendige Datei, die vollstaendig
      AUSSIEHT (E28/N10, dieselbe Regel wie beim Ausliefern).

      DIE EHRLICHE PROBE IST /api/netzlaufwerk/pruefen: eine Datei schreiben
      und wieder wegnehmen. "Einheit aktiv" beantwortet die Frage nicht — eine
      Freigabe kann erreichbar und trotzdem nur lesbar sein, und das merkt man
      sonst erst, wenn die erste Sicherung nicht ankommt. Aus demselben Grund
      liest der Zustand mit `findmnt --target`: ein stat auf einen
      Automount-Punkt LOEST DAS EINHAENGEN AUS und koennte die Zustandsabfrage
      bis zur Frist aufhalten.

      ZWEI FALLEN BEIM BAU, beide belegt:
      * Der Test "die Einheit traegt kein [Install]" war rot, obwohl die Regel
        hielt: er traf den erklaerenden KOMMENTAR, der das Wort absichtlich
        nennt. Kommentarzeilen vorher wegfiltern
        ([[kommentar-und-kompilat-sind-keine-gegenstelle]]).
      * `node:assert`s assert.equal(p.ok, true) verengt den Typ danach auf
        `never` — die naechste Zeile ist fuer TypeScript unerreichbar. In
        Testhilfen stattdessen `if (!p.ok) throw`.

      WAS FEHLT: alles am Geraet. Es gibt keine Freigabe, keinen Nutzer, kein
      Passwort — die Messungen zu N6 (Haengen), N9 (nimmt Jellyfin einen
      wachsenden Titel auf?) und die erste echte Spiegelung stehen aus.

      ── NACHTRAG 20.09.2026: WEBDAV ALS ZWEITE SORTE ──────────────────────
      Auf Wunsch des Betreibers kam WebDAV (mount.davfs) neben SMB. Die
      Unterscheidung ist EIN Feld (`art`); Optionen, Einheitentyp und
      Zugangsdatei werden daraus abgeleitet, und die fuenf Regeln oben gelten
      unveraendert fuer beide — die Tests fahren sie auch fuer beide, weil
      eine Regel, die nur fuer die zuerst gebaute Sorte gilt, keine ist.
      21 zusaetzliche Tests, zwei Sabotagen rot geworden.

      WAS BEI WEBDAV ANDERS IST, und beides gehoert vor die Wahl:

      * ES LAEDT UEBER EINEN ZWISCHENSPEICHER AUF DER KARTE HOCH. davfs2
        schreibt jede Datei zuerst lokal (Vorgabe 50 MiB unter
        /var/cache/davfs2) und laedt sie dann. Fuer Staende von 12 KB
        folgenlos; fuer grosse Dateien laufen sie ZWEIMAL ueber die Karte.
        Wer ein Netzlaufwerk wegen des Kartenverschleisses will (E28/N15),
        nimmt SMB. Begrenzt auf 64 MiB, damit eine grosse Datei die Karte
        nicht unbemerkt fuellt.
      * DIE ANMELDUNG LIEGT NICHT IN /etc/davfs2/secrets. Die systemweite
        Datei gehoert dem Paket und womoeglich fremden Einhaengungen. Die Box
        legt stattdessen eine EIGENE davfs2-Konfiguration
        (/etc/mupibox/davfs2.conf, Mount-Option conf=) an, und darin zeigt
        `secrets` auf /etc/mupibox/nas-webdav-zugang. Dazu `ask_auth 0` —
        sonst fragt mount.davfs interaktiv nach dem Passwort, und beim
        Einhaengen durch systemd steht dort niemand: es HINGE, statt zu
        scheitern. DASS DIESE UMLENKUNG AM GERAET TRAEGT, IST NICHT GEMESSEN
        (davfs2 ist auf der Arbeitsmaschine nicht installiert).

      DIE ADRESSE WIRD ZERLEGT UND NEU ZUSAMMENGESETZT, nicht durchgereicht:
      abgelehnt werden Anmeldedaten in der URL (die stuenden offen in der
      Einheit), `?` und `#`, fremde Protokolle. Und die `..`-Pruefung gehoert
      auf die ROHE Eingabe — `new URL()` rechnet `..` vorher weg, aus
      https://nas/dav/../../etc wird /etc. Genau diese Regel stand erst hinter
      dem Parser, war gruen und wirkungslos; gefunden hat es der Test mit
      dieser Adresse. Dieselbe Sorte Fehler wie eine Sabotage, die nicht
      griff — nur an der Regel selbst statt an der Gegenprobe: sie stand da,
      war gruen und traf nie etwas.

      Das Passwort geht bei davfs2 in EINE Zeile aus drei Feldern
      (`<url> <nutzer> <passwort>`), deshalb stehen Name und Passwort in
      Anfuehrungszeichen: eines mit Leerzeichen waere sonst zwei Felder, und
      mount.davfs naehme die erste Haelfte.

      ── NACHTRAG 20.09.2026 (2): DIE PAKETE GEHOEREN AUF ALLE DREI WEGE ───
      Nach dem Ausliefern auf Box .62 (bee80ea1) meldete /api/netzlaufwerk am
      GERAET `werkzeuge {smb:false, webdav:false}`: weder cifs-utils noch
      davfs2 waren installiert, und in keiner der drei Paketlisten standen
      sie. Eine ausgelieferte Seite, die nur sagen kann „das Werkzeug fehlt",
      ist auf einer frisch bespielten Karte dasselbe wie keine Seite — genau
      der Fall, den [[zwei-ausrollwege-eine-handgefuehrte-liste]] beschreibt,
      hier mit DREI Listen (autosetup, Update-Weg,
      remote-step-installer/recipes/mupibox.yaml).

      Beide Pakete stehen jetzt auf allen dreien, und tools/rezept-deckung.py
      haelt sie dort fest (MERKMALS_PAKETE, Anker im reinen Modul statt im
      Ausrollweg — sonst pruefte man im Kreis). Gegenprobe gefahren: cifs-utils
      aus dem Update-Weg genommen, `--pruefen` wurde 1 und paketlisten-deckung
      meldete die Abweichung. ACHTUNG BEIM LESEN DIESER WACHE: ohne
      `--pruefen` gibt rezept-deckung.py trotz `!!`-Befund eine 0 zurueck; der
      Laeufer tools/pruefen.sh ruft sie deshalb mit dem Schalter. Wer sie von
      Hand ohne ihn ruft, liest ein Urteil, das sie gar nicht faellt.

      DIE FALLE DABEI: `davfs2` stellt beim Einrichten eine debconf-Frage
      („mount.davfs setuid root?"). Die beiden Shell-Wege riefen
      `apt-get --yes install` OHNE DEBIAN_FRONTEND — ein Dialog in einem
      Rezept, dessen Ausgabe nach >&3 laeuft und vor dem niemand sitzt, faellt
      nicht auf, er steht einfach. Beide Aufrufe tragen jetzt
      `DEBIAN_FRONTEND=noninteractive`; das Installer-Rezept tat es laengst.
      Die Vorgabe der Frage (KEIN setuid) ist hier die richtige: eingehaengt
      wird ueber eine systemd-Einheit, also als root. Und beide Pakete sind
      KLIENT-Pakete — sie binden eine fremde Freigabe ein und oeffnen nichts,
      anders als das samba aus [[windows-freigabe-war-nie-da]].

      AM GERAET NACHGEMESSEN (20.09.2026, Box .62, nach `apt-get install -y
      cifs-utils davfs2`): /api/netzlaufwerk meldet jetzt
      `werkzeuge {smb:true, webdav:true}`. Die Simulation vorher sagte drei
      neue Pakete (cifs-utils, davfs2, libneon27t64), null Aktualisierungen,
      null Entfernungen — deshalb ohne Wartungsschirm gefahren, und es spielte
      nichts. Die horchenden Ports sind Zeichen fuer Zeichen dieselben
      geblieben, kein neuer Dienst laeuft, mupibox-server unbeirrt aktiv.

      EIN DETAIL, DAS DIE BEHAUPTUNG „oeffnet nichts" PRAEZISIERT:
      /sbin/mount.davfs kam OHNE setuid-Bit (rwxr-xr-x) — die debconf-Vorgabe
      hat gegriffen, genau wie beabsichtigt. /sbin/mount.cifs dagegen traegt
      es (rwsr-xr-x), das bringt das Debian-Paket so mit. Es oeffnet keinen
      Port, aber es ist ein setuid-Programm mehr auf der Box; wer das nicht
      will, braucht SMB nicht zu installieren — die Seite sagt dann nur, dass
      das Werkzeug fehlt, und WebDAV bleibt benutzbar.

      ── NACHTRAG 20.09.2026 (3): DAS SELBSTSIGNIERTE ZERTIFIKAT ───────────
      Beim Vorbereiten der ersten echten Freigabe gemessen, mit
      `tools/nas-sonde.py --webdav`: das NAS im Haus (.199) ist ein UGREEN und
      zeigt ein SELBSTSIGNIERTES Zertifikat (SHA256 C7:37:F2:...:E9:F1,
      Aussteller UGREEN). mount.davfs lehnt so einen Server ab, und mit
      `ask_auth 0` fragt es auch nicht nach — die Freigabe waere NIE
      eingehaengt, ohne dass jemand erfahren haette, warum. WebDAV gegen das
      eigene NAS waere also genau daran gescheitert, und zwar stumm.

      DER WEG, DEN davfs2 DAFUER VORSIEHT, ist `trust_server_cert`, und das
      will eine PEM-DATEI, KEINEN Fingerabdruck (nachgelesen in davfs2.conf(5),
      nicht geraten). Die Box misst deshalb beim Uebernehmen den TLS-Handschlag
      selbst, und wenn er nicht prueft, antwortet sie 409 MIT DEM
      FINGERABDRUCK statt still anzunehmen oder stumm zu scheitern. Erst ein
      zweiter Aufruf mit `zertifikatVertrauen: true` legt das Zertifikat ab.
      `trust_server_cert` schaltet danach JEDE weitere Pruefung ab (Gueltigkeit
      UND Name) — deshalb steht `zertifikatVertraut` in der Beschreibung und
      dauerhaft in der Oberflaeche: eine abgeschaltete Pruefung, die man nicht
      mehr sieht, ist eine, an die sich in einem halben Jahr niemand erinnert.

      ZWEI FUNDE AUS DER GEGENPROBE (drei Wege, ein Fingerabdruck: openssl
      s_client, Pythons ssl, node:tls — alle drei dieselbe Zahl, und die vom
      Servercode erzeugte PEM liest openssl sauber):

      * `tls.connect` WIRFT, wenn `servername` eine IP-ADRESSE ist
        (ERR_INVALID_ARG_VALUE, „Setting the TLS ServerName to an IP address is
        not permitted"). Im Heimnetz ist die Adresse fast immer eine IP — das
        waere also der Normalfall gewesen, nicht die Ausnahme, und die Route
        haette 500 statt einer Antwort gegeben. SNI wird jetzt nur gesetzt,
        wenn der Wirt KEINE IP ist (net.isIP).
      * Ein deutsches schliessendes Anfuehrungszeichen beendet einen
        Python-String NICHT — ein gerades `"` mitten im deutschen Zitat aber
        sehr wohl. Zweimal an einem Tag in nas-sonde.py aufgelaufen
        (SyntaxError erst Zeilen spaeter). In Fliesstext im Code entweder
        durchgaengig typografische Zeichen oder einfache Anfuehrungszeichen.
  - id: nachrichten-an-die-box
    kind: note
    title: "Nachrichten AN die Box: drei Wege, ein Tor — und die vier Fallen darin"
    tags: [nachrichten, matrix, signal, telegram, erlaubnisliste, vorlesen, kinderschutz, 2026]
    quelle: "20./21.09.2026 gebaut; 83 Zeugen in vier Modulen plus Integrationsprobe, 29 Gegenproben. AM GERAET NOCH NICHT GEMESSEN — dafuer gibt es tools/nachrichten-sonde.py."
    lang: |
      Telegram gibt es in diesem Baum seit Jahren, aber nur HINAUS (die Box
      meldet Start, Abschalten, Titelwechsel). Die Gegenrichtung — jemand
      schreibt dem KIND — ist neu und ein anderer Gegenstand: eigene
      Konfigurationsgruppe `nachrichten`, eigene Schalter, EIGENER Bot.

      ── DIE ERLAUBNISLISTE IST DER GANZE PUNKT ───────────────────────────
      Ein offener Kanal auf einen Kinderschirm ist ein Kanal, ueber den ein
      Fremder einem Kind etwas sagt. Betreiber am 20.09.2026 dazu befragt:
      feste Erlaubnisliste. EINE LEERE LISTE HEISST NIEMAND, nicht „alle" —
      das ist die unbequeme Richtung und die richtige: eine frisch
      eingeschaltete Box darf nicht fuer jeden offen sein, der die
      Raumkennung kennt. Ein Abgewiesener wird GEZAEHLT (damit im
      Eltern-Bereich steht, dass jemand klopft), sein TEXT wird nicht
      aufgehoben.

      Bewacht von `tools/nachrichten-erlaubnis-schau.mjs` (in pruefen.sh).
      Sie misst die SORTE, nicht den Pfad: sie sucht JEDE exportierte
      `aus…`-Funktion, statt drei bekannte Namen abzuhaken. Der teure Fehler
      ist naemlich der VIERTE Weg, den jemand danebenbaut und dabei die
      Pruefung vergisst — jeder bestehende Test bliebe dabei gruen.

      ── VIER FALLEN, JEDE EINMAL BEZAHLT ─────────────────────────────────

      1. DIE FRIST DARF NICHT KUERZER SEIN ALS DER LONG POLL. Matrix und
         Telegram ANTWORTEN BESTIMMUNGSGEMAESS ERST NACH 30 s, wenn nichts
         vorliegt. `AbortSignal.timeout(POLL_MS)` baut daraus einen
         Dauerabbruch, der aussieht wie ein kaputter Server. Hier:
         FRIST_MS = POLL_MS + 10 s.

      2. EINE NATIONALE TELEFONNUMMER LAESST SICH NICHT UMRECHNEN. `0170 …`
         — welches Land? Wer eines raet, baut die Erlaubnisliste, die
         MANCHMAL greift: die Eltern tragen die Mutter ein, Signal meldet
         sie international, nichts kommt an, und niemand sucht den Fehler
         bei der Schreibweise. `absenderSchluessel` gibt deshalb LEER
         zurueck, und der Eltern-Bereich WEIST DEN EINTRAG AB, statt ihn
         stumm aufzunehmen. `+49…` und `0049…` sind dasselbe.

      3. TELEGRAM PRUEFT DEN MENSCHEN (`from.id`), NICHT DEN CHAT
         (`chat.id`). In einer Gruppe sind die beiden verschieden — wer den
         Chat freigibt, gibt jedes Mitglied darin frei. Und: `date` kommt in
         SEKUNDEN; wer das uebersieht, legt jede Nachricht auf den
         20. Januar 1970 und sortiert die Liste verkehrt.

      4. EIN TCP-LESEVORGANG LIEFERT BYTES, KEINE NACHRICHTEN. signal-cli
         schiebt JSON-Zeilen; eine Zeile kann in zwei Paketen ankommen und
         zwei Zeilen in einem. `JSON.parse` direkt auf den Socket-Inhalt
         verliert bei Last genau die Nachricht, auf die jemand wartet —
         unregelmaessig. Deshalb `zeilenSchneiden` mit Rest-Puffer.

      ── ZWEI GRUENE GEGENPROBEN WAREN DIE EIGENTLICHEN FUNDE ─────────────
      (Die Regel dazu: [[gegenprobe-bleibt-gruen-ist-der-fund]].)

      a) „Ruf ohne Frist" BLIEB GRUEN. Der Test pruefte
         `o.signal instanceof AbortSignal` — und das ist auch ein Signal,
         das NIE ausloest. Seitdem ist die Frist ueber die Umgebung
         einstellbar, der Test setzt 30 ms und BELEGT den Abbruch, statt
         ihn zu behaupten. Eine Probe ist kein grep.

      b) Beim Sabotieren fiel auf, dass die Schleife OHNE PAUSE dreht, wenn
         ein Dienst sofort und leer antwortet (falsch eingestellter Server,
         ein Zwischenspeicher davor, ein Spiegel ohne Long Poll). Auf einer
         passiv gekuehlten Box ist das ein ganzer Kern. Seitdem
         MINDEST_MS = 1000; im Normalfall kostet es nichts, weil die Runde
         laengst laenger ist. Gefunden hat es die Gegenprobe, nicht das
         Nachdenken: der sabotierte Lauf HING (Mikrotask-Hunger — ohne
         echtes Datei-I/O kommt kein `setTimeout` mehr dran).

      ── WAS DIE OBERFLAECHE NOCH NICHT KANN ──────────────────────────────
      VERSCHLUESSELTE MATRIX-RAEUME GEHEN NICHT. `m.room.encrypted` traegt
      keinen Klartext und faellt durch; der Raum fuer die Box muss
      unverschluesselt sein. Das gehoert in den Eltern-Bereich GESCHRIEBEN —
      ein Raum, in dem die Mutter schreibt und nichts ankommt, ist sonst ein
      Fehler, den niemand findet.

      EIN EIGENER TELEGRAM-BOT, NICHT DER VON `telegram.token`. `getUpdates`
      liefert jede Nachricht GENAU EINMAL; laeuft daneben noch
      `scripts/telegram/telegram_receiver.py` mit demselben Token, holen sich
      die beiden die Nachrichten gegenseitig weg — mal kommt sie an, mal
      nicht. Das ist die unangenehmste Sorte Fehler, weil sie aussieht wie
      Funk.

      SIGNAL BRAUCHT `signal-cli` ALS DIENST auf der Box (JSON-RPC ueber TCP,
      Vorgabe 127.0.0.1:7583). Der Dienst DARF FEHLEN — das war die
      ausdrueckliche Vorgabe; dann sagt `/api/nachrichten/stand`
      `signalDa: false` samt Einrichtungssatz, statt dass ein Schalter
      ANsteht und nichts passiert. Bewusst NICHT in die Paketlisten
      aufgenommen (anders als cifs-utils/davfs2 am 20.09.): eine
      Java-Laufzeit auf jeder frischen Karte fuer ein Feature, das die
      wenigsten benutzen.

      ── VORLESEN JE PROFIL ───────────────────────────────────────────────
      Der Empfaenger liest nicht. Also spricht die Box die Nachricht aus
      (Piper, dieselbe Kette wie bei den Kacheln) — ein- und ausschaltbar JE
      PROFIL (`nachrichtenVorlesen` in profile.json). FEHLT DAS FELD, GILT
      DIE BOX-VORGABE, und `null` setzt es auf genau das zurueck: dieselbe
      Regel wie bei `merken`, und aus demselben Grund — ein Kind, das nie
      etwas eingestellt hat, verhaelt sich wie am Tag davor.

      ZWEI DECKEL, DIE NICHT ZUSAMMENPASSEN: die Nachricht darf 500 Zeichen
      haben, die Sprachausgabe nimmt 300 (VL_TEXT_MAX). Ein laengerer Satz
      kaeme als FEHLER zurueck und wuerde GAR NICHT gesprochen — deshalb
      kuerzt der Server fuer den Ton, statt abzulehnen. Auf dem Schirm steht
      sie vollstaendig.

      `gesprochen` IST NICHT `gelesen`. Waeren es dieselbe Marke, wuerde
      eine Nachricht, die das Kind wegtippt, bevor der Satz zu Ende ist,
      beim naechsten Takt erneut gesprochen. Ein doppelt vorgelesener Satz
      ist auf einer Kinderbox schlimmer als ein verpasster: er weckt.

      ── WO ES STEHT ──────────────────────────────────────────────────────
      `nachrichten.ts` (was eine Nachricht ist, wer durchkommt — rein),
      `nachrichten-holer.ts` (wie die drei Dienste zu lesen sind — rein),
      `nachrichten-ablage.ts` (was auf die Karte geht, was eingetragen
      werden darf), `nachrichten-dienst.ts` (die Schleifen; alles mit der
      Welt, nichts mit einer Entscheidung). Verdrahtung und Routen in
      server.ts, Handbuch in `dokumentation/mixpibox.md` 4.13.
      Messen am Geraet: `tools/nachrichten-sonde.py`.
  - id: nachrichten-oberflaechen
    kind: note
    title: "Die Nachrichtenkarte auf dem Kinderschirm — und was die Vorschau dabei gezeigt hat"
    tags: [nachrichten, oberflaeche, newdesign, verwaltung, vorschau, 2026]
    quelle: "21.09.2026 gebaut und in einer eigenen Vorschau auf Port 8291 durchgeklickt (gebautes server.js, eigene Konfiguration). 5 Zeugen in der Verwaltung, 5 Gegenproben. NICHT auf einer Box gemessen."
    lang: |
      Der Hintergrund steht in [[nachrichten-an-die-box]]; hier nur, was beim
      Bauen der beiden Oberflaechen gelernt wurde.

      ── DIE KARTE GEHT NICHT VON SELBST WEG ──────────────────────────────
      `.meldung` verschwindet nach 4,5 Sekunden, und fuer einen Hinweis auf
      einen misslungenen Tipp ist das richtig. Eine Nachricht von Mama ist
      etwas anderes: das Einzige auf diesem Schirm, das von einem MENSCHEN
      kommt. Verschwaende sie von selbst, haengt es am Zufall, ob das Kind
      gerade hinsieht.

      DAS HAT EINE FOLGE, DIE MAN MITDENKEN MUSS: `.meldung` liegt auf Stufe
      10 und damit UEBER dem einen Rueckweg (9) — vertretbar, weil sie ihn
      nur fuer Sekunden verdeckt (tools/rueckweg-nachlese.mjs, 03.08.2026).
      Die Nachrichtenkarte verdeckt ihn, BIS jemand tippt. Deshalb steht auf
      ihr ein eigener Satz, wie man sie loswird („Tippen, wenn du sie gelesen
      hast") — ohne ihn waere sie fuer ein Kind, das den Ausgang oben links
      gelernt hat, eine Sackgasse.

      ── AELTESTE ZUERST, UND NUR EINE ────────────────────────────────────
      Die Liste kommt neueste zuerst; gezeigt wird die AELTESTE ungelesene.
      Und solange eine steht, wird keine zweite darueber gelegt: ein Kind,
      dem der Text unter den Augen wechselt, hat die erste nie zu Ende
      gelesen — und sie gilt danach als gesehen. In der Vorschau
      durchgeklickt: zwei ungelesene, „Erste" kommt zuerst, nach dem Tippen
      rueckt „Zweite" nach, und `gelesen: true` steht danach in der Datei.

      ── ERST VERSTECKEN, DANN MELDEN ─────────────────────────────────────
      Die Karte geht beim Tippen SOFORT weg, die Meldung an die Box laeuft
      danach. Umgekehrt stuende sie bei einem haengenden Ruf bis zur Frist —
      und das Kind tippt gegen eine Karte, die sich nicht wegtippen laesst.
      Ob die Meldung ankam, entscheidet nur, ob die Karte beim naechsten Takt
      wiederkommt.

      ── `json()` UND NICHT `sendeJson` FUER DEN TAKT ──────────────────────
      Nur `json()` setzt `cache: 'no-store'`. Bei einem Takt, der alle fuenf
      Sekunden dieselbe Adresse fragt, ist das kein Feinschliff: ein
      Zwischenspeicher liesse die Karte einer eingetroffenen Nachricht
      beliebig lange aus.

      ── WAS DIE WACHE GEFUNDEN HAT ───────────────────────────────────────
      `tools/fetch-frist-schau.mjs` meldete den `fetch` in der Verdrahtung
      (`holen: (adresse, o) => fetch(adresse, o)`) als fristlos. Er IST es
      nicht — die Frist kommt in `o.signal` mit —, aber das stand nirgends.
      Die Wache sieht nur ein Fenster; sie hatte also recht, etwas zu
      verlangen. WICHTIG DABEI: der Marker „KEINE FRIST" muss in den FUENF
      Zeilen direkt darueber stehen. Eine siebenzeilige Begruendung mit dem
      Marker ganz oben zaehlt nicht ([[wiederkehrender-befund-ist-ablauffehler]]
      in klein: die Wache misst ein Fenster, nicht den Absatz).

      ── DIE VORSCHAU: WELCHER MODUS WELCHE DATEI LIEST ───────────────────
      Eine EIGENE Vorschau auf einem EIGENEN Port zu fahren (und nicht die
      des Betreibers mitzubenutzen) ist hier unerwartet kniffelig, und der
      Grund steht in server.ts:
        * `MUPIBOX_CONFIG_DIR` greift NUR ausserhalb des Produktivbetriebs
          (bewusst: sonst waere es ein Hebel von aussen).
        * `www/` (und damit `/neu/`) wird NUR im Produktivbetrieb
          ausgeliefert.
      Beides zusammen geht also nicht. Der Weg fuer die Box-Oberflaeche:
      NODE_ENV=production, `MUPIBOX_CONFIG` auf die eigene Datei zeigen
      lassen (das gilt unbedingt) und die Datenablage nach
      `src/deploy/server/config/` legen — das ist der Pfad, den der
      Produktivbetrieb relativ zum Arbeitsverzeichnis nimmt.
      Ausserdem: `tsx src/server.ts` startet NICHT (`__dirname` gibt es im
      ES-Modul nicht); es muss das gebuendelte `src/deploy/server.js` sein.
      Und die Verwaltung liegt unter `/admin/`, nicht unter `/`.

      ── EINE ALTE FALLE, ZWEIMAL AN EINEM ABEND ──────────────────────────
      `pgrep -f "node server.js"` trifft die eigene Wrapper-Shell mit
      ([[pkill-toetet-die-eigene-shell]]) — der Aufraeumbefehl hat sich
      selbst abgeschossen. Der brauchbare Weg beim Beenden einer Vorschau
      ist die PID AUS DEM PORT: `ss -ltnp | grep <port>`.
      Und ein gerades Anfuehrungszeichen INNERHALB eines deutschen Zitats
      („noch nie") beendet einen Python-String — beim Schreiben der
      Gegenproben-Namen aufgelaufen, wie schon am 20.09. in nas-sonde.py.
      In Fliesstext im Code entweder durchgaengig typografische Zeichen oder
      einfache Anfuehrungszeichen.

      ── NACHTRAG 21.09.2026: DER UMWEG HAT ZWEI DINGE VERDECKT ───────────
      Die eigene Vorschau war der falsche erste Griff — `tools/neu-vorschau.mjs`
      gibt es seit jeher und bedient `/neu/` mit einer Attrappen-API
      (die Regel dazu: [[arbeitsweise-werkzeug-statt-wegwerfbefehl]] —
      Schritt 3 des Ablaufs, `tools/` durchsuchen, BEVOR man tippt). Der Umweg hat den
      echten Weg Ende zu Ende belegt und war insofern nicht umsonst; zwei
      Dinge hat er aber verdeckt:

      1. DIE ATTRAPPE KANNTE /api/nachrichten NICHT, und `nachrichtenHolen`
         faengt einen Fehlschlag still ab. Die Karte war in der Vorschau des
         Projekts also UNSICHTBAR — wer an ihr arbeitet, haelt sie fuer
         kaputt. `tools/vorschau-routen-deckung.mjs --pruefen` haette es beim
         naechsten Gesamtlauf gemeldet. ACHTUNG BEIM NACHFAHREN: ohne
         `--pruefen` DRUCKT es die Luecke und endet trotzdem mit 0.

      2. DIE EIGENE VORSCHAU HATTE EINE LEERE BIBLIOTHEK. Vor zwoelf Kacheln
         sah die Karte ganz anders aus: der Satz der Mutter stand mitten in
         den Bildern, und beides war gleich gut zu lesen. rgba 0.82 war in
         der leeren Vorschau eine gute Zahl und davor keine (jetzt 0.94).
         EINE EINBLENDUNG VOR LEEREM GRUND ZU BEURTEILEN IST KEINE MESSUNG —
         dasselbe Muster wie [[einmal-hinsehen-ist-keine-messung]], nur
         raeumlich statt zeitlich.
  - kind: entscheidung
    id: readme-fuer-github-und-die-vier-toten-behauptungen
    title: "Die README bekommt eine Installationsanleitung — und die Wache lernt vier Behauptungen, die sie nie gelesen hat"
    tags: ['doku', 'readme', 'github', 'installation', 'wache', 2026]
    source: "umgesetzt 23.09.2026; Wache tools/readme-behauptungen-pruefen.sh danach gruen (87 Belege, exit 0)"
    body: |
      DER AUFTRAG (Betreiber, 23.09.2026): „eine readme md welche dezent auf
      das mupi projekt hinweisst, alle features auflistet und ein howto
      install von grund auf. einen check ob alles aktuell ist, rezepte und
      files. laeuft der installer auch auf windows?"

      ── DIE EINE INHALTLICHE KEHRTWENDE ────────────────────────────────────
      Die README sagte seit dem 03.08.2026 ausdruecklich: „keine
      Installationsanleitung fuer Fremde" ([[readme-fork-drei-heikle-stellen]]).
      Das gilt nicht mehr — Abschnitt 4 „Installation von Grund auf" beschreibt
      den Weg von der leeren Karte bis zum ersten Start. WAS BLEIBT: kein
      Support, keine Releases, keine Gewaehr; der Abweis-Kasten auf Englisch
      steht weiter ganz oben, die Paypal-/Support-/Urheber-Loesung von damals
      ist unveraendert uebernommen. Wer die Anleitung wieder herausnimmt,
      nimmt auch die Zeile in Abschnitt 1 zurueck.

      ── VIER TOTE BEHAUPTUNGEN, ALLE VON DER WACHE NICHT GESEHEN ───────────
      Die alte README meldete 9 unbelegte Behauptungen; beim Nachziehen kamen
      vier heraus, die KEINE Zahl waren und deshalb nie jemand mass:

      1. `plugin-system` — die README verwies auf einen Wissenspaket-Eintrag
         dieses Namens. ES GIBT IHN NICHT und gab ihn nie (1104 Eintraege,
         keiner heisst so). Abschnitt 4 der Wache prueft seither JEDEN in der
         README genannten Eintrag; die richtigen Namen sind
         [[plugins-melden-sich-an-statt-gekannt-zu-werden]] und
         [[plugin-inhalt-kommt-in-die-medienliste]].

      2. `npm run serve:frontend-box` — die Wache sah den NAMEN in
         package.json und meldete gruen. Gemessen (23.09.2026):
             npm error No workspaces found: --workspace=mupibox-frontend-box
         Der Arbeitsbereich ist mit E118/1e gefallen, das Skript blieb stehen.
         Das ist [[readme-befehl-der-nichts-startet]] in der Gegenrichtung:
         dort tat ein Befehl nichts, hier faellt er um — beide Male stand er
         in der README, beide Male war der Beleg nur ein Dateiname. Die Wache
         prueft jetzt, dass die README ihn NICHT mehr anbietet.

      3. „NewDesign ausgeliefert" — gezaehlt wurden DREI Dateien
         (app.js+app.css+index.html, 47 806 Zeilen), ausgeliefert sind FUENF:
         `apps.js` (die Schublade) und `video.js`/`video.css` kamen im
         September dazu und haengen in index.html. Die Wache mass also eine
         Groesse, die es so nicht mehr gab (50 064). Wer eine Datei in
         index.html haengt, haengt sie auch in die Wache.

      4. „die klassische Box-Oberflaeche ist noch da" — sie ist seit E118/1e
         (05.09.2026) geloescht; in `src/frontend-box/` liegen ein LIESMICH.md
         und ein Nachzuegler-Modul. Derselbe Satz stand auch in
         `dokumentation/mixpibox.md` als UEBERSCHRIFT von 4.3 („Zwei
         Oberflaechen fuer die Box") — vierzig Zeilen ueber dem eigenen
         Unterabschnitt 4.3.1, der die Loeschung beschreibt. Zwei Wahrheiten
         in einem Kapitel, die aeltere oben, wo man zuerst liest.

      ── DIE GEGENPROBE HING AM grep, NICHT AN DER REGEL ────────────────────
      Beim Sabotieren der neuen Pruefungen meldete eine Zeile rot, obwohl die
      Datei stimmte: das Muster enthaelt `${MUPI_URL}`, und `grep` ist in einer
      Agenten-Sitzung oft ugrep, das die Klammern anders liest als das
      GNU-grep im Skript. DIESELBE WACHE URTEILTE ALSO VERSCHIEDEN, JE NACHDEM
      WER SIE RIEF. Behoben mit `-qF` (feste Zeichenkette). Wer ein Muster mit
      `${...}` oder `{n,m}` in eine Wache schreibt, nehme `-F` oder messe
      beide Richtungen — sonst ist die Gegenprobe wertlos.

      ── WAS AUSSERDEM NACHGEZOGEN WURDE ────────────────────────────────────
      Zahlen (gemessen 23.09.2026): backend-api 142 Testdateien (vorher 113),
      backend-player 15, frontend-admin 27 Seiten + Anmeldung (vorher 23),
      Kopfleiste 24 Eintraege, tools/ ueber 560 Skripte (vorher „rund 440"),
      NewDesign 33 Verhaltenstests. Neu in der Wache: die Schrittzahlen der
      Rezepte (30 + 24), die Pluginzahl (14), die Karten-Werkzeuge
      (sdstart/sdgui/sdtui/tui/stepctl ausfuehrbar), `lauf_paket=True` in
      sdstart.py und der feste Kiosk-Pfad `/neu/`.

      NICHT ERLEDIGT, weil es eine Entscheidung des Betreibers ist:
      `bin/nodejs/deploy.zip` liegt 19 Commits hinter dem Baum
      ([[deploy-zip-traegt-den-stand-des-letzten-handbaus]] bzw.
      tools/mixpi-paketfrische-pruefen.py) — eine heute geschriebene Karte
      traegt den alten Stand. Die README sagt das in Abschnitt 4.3 und nennt
      den Weg (`bash src/deploy.sh`).
  - kind: howto
    id: karte-schreiben-unter-windows-geht-die-installation-nicht
    title: "Windows: die Karte schreiben geht, die Installation treiben nicht — und was dazwischen still wegfaellt"
    tags: ['installer', 'windows', 'sdstart', 'sdprep', 'karte', 2026]
    source: "am 23.09.2026 am Code nachgemessen (keine Windows-Maschine im Haus); tests/geraete_test.py 35 gruen"
    body: |
      DIE FRAGE KAM VOM BETREIBER („laeuft der installer auch auf windows?")
      und war nirgends beantwortet. Die Antwort ist zweigeteilt, und die
      Trennlinie liegt nicht dort, wo man sie vermutet.

      ── WAS GEHT: DIE KARTE ────────────────────────────────────────────────
      Der Windows-Zweig ist gebaut, nicht zufaellig lauffaehig:
        * `sdstart.cmd` / `sdgui.cmd` — pruefen SELBST auf Administratorrechte
          (`net session`) und sagen, was zu tun ist, statt mitten im Schreiben
          zu scheitern; sie finden Python ueber `py`, dann `python`.
        * `geraete.windows_schreiben` entpackt und schreibt SELBST: Windows
          bringt weder `dd` noch `xz` mit, `lzma` steht in der
          Standardbibliothek. Roh auf `\\.\PhysicalDriveN`, und NUR in ganzen
          Sektoren (4096) — ein krummer WriteFile endet mit
          ERROR_INVALID_PARAMETER. Nachgerechnet am 10.08.2026: bei 4-MiB-
          Happen waren 0 von 8 sektorgerecht, es waere beim ERSTEN Byte
          gescheitert.
        * Karten finden: PowerShell `Get-Disk | ConvertTo-Json` statt `lsblk`.
          Die Boot-Partition bekommt einen Laufwerksbuchstaben statt eines
          Mountpoints (`geraete.boot_mounten`); `sdprep.mount_boot` zweigt
          dafuer bei `os.name == "nt"` ab.
        * `os.geteuid` GIBT ES AUF WINDOWS NICHT — die Stelle ist mit
          `hasattr(os, "geteuid")` abgesichert, die `os.getuid()`-Zeile liegt
          im Linux-Zweig hinter der Abzweigung. Modulebene: keiner der vier
          Karten-Module importiert etwas Unix-Eigenes (nachgemessen per AST).

      ── WAS STILL WEGFAELLT ────────────────────────────────────────────────
      1. DAS INSTALLATIONSPAKET. `sdprep.prepare_boot` baut `lauf.tar.gz` nur,
         wenn `import laufpaket` durchkommt — und `laufpaket.rezept_wandeln`
         braucht **PyYAML**. Ein frisches python.org-Python hat es nicht.
         Der Fehler wird GEFANGEN und als Zeile gemeldet
         („lauf.tar.gz NICHT gebaut: …"), die Karte entsteht trotzdem. Ergebnis:
         eine Karte, die nicht selbst installiert — und der ferngesteuerte Weg
         ist auf Windows gerade nicht eingerichtet (Punkt 3). `pip install
         pyyaml` loest es; `setup-controller.sh` kennt nur Distro-Paketmanager.
      2. DAS WLAN. `sdprep.wifi_active`/`wifi_secret` lesen `nmcli` — also
         Linux mit NetworkManager. Unter Windows (und macOS) kommt '' zurueck,
         SSID und Schluessel werden getippt. Kein Fehler, aber genau der
         Schritt, an dem ein Tippfehler erst beim stummen Erstboot auffaellt
         ([[mupi-wlan-schluessel-ohne-klartext]]).
      3. DER FERNGESTEUERTE LAUF. `./tui`, `./stepctl`, `./connect`, `./rsi`
         sind bash-Starter, und der Weg setzt einen SSH-Tunnel voraus. Es gibt
         keine `.cmd`-Entsprechung.
      4. DER FORK-PFAD. `sdstart` ruft `prepare_boot` OHNE `fork=`, der
         Standard ist `~/Downloads/MuPiBox`. Wer woandershin klont, bekommt
         „N Rezept-Quelle(n) fehlten — diese Schritte laufen ins Leere".
         `MUPI_REPO` hilft nur dem ferngesteuerten Weg, nicht `sdstart`.

      ── WIE WEIT DAS GEMESSEN IST ──────────────────────────────────────────
      Die REINEN Teile des Zweigs haben Tests (`tests/geraete_test.py`,
      `windows_auswerten` gegen echte Get-Disk-Ausgaben, 35 gruen, laufen auf
      Linux). EINEN ECHTEN DURCHLAUF AUF EINEM WINDOWS-RECHNER HAELT NICHTS
      FEST — weder ein Test noch ein Eintrag hier. Wer einen faehrt, schreibt
      das Ergebnis in diesen Eintrag; bis dahin ist „geht" eine Aussage ueber
      den Code, nicht ueber die Wirklichkeit.
  - kind: gotcha
    id: leerer-catch-um-eine-zugabe-verdeckt-den-vertragsbruch
    title: "Die Video-Stimme hat nie gesprochen — ein leerer catch um eine Zugabe ist ein nie gefahrener Weg"
    tags: [newdesign, video, vorlesen, catch, vertrag, audit, 2026]
    quelle: "Kritiker-Lauf 23.09.2026 (AUDIT-2026-09-23 Rang 1); Signatur und TypeError-Kette am HEAD 29d449f8 selbst nachgemessen, nicht behoben — nur befundet."
    lang: |
      WAS PASSIERT IST. `NewDesign/video.js` bekam beim Bau des Video-Schirms
      die Hausfunktion `sprichDann` in den Kontext gereicht und rief sie mit
      EINEM String: `sprichDann(text)`. Die Funktion heisst aber
      `sprichDann(k, satz, name, beiTipp, gleich)` — das erste Argument ist
      ein DOM-ELEMENT, an das sie einen Zuhoerer haengt. Durchgerechnet:
      `satz` ist undefined, der Satz wird leer, dann
      `"text".addEventListener(...)` -> TypeError. Der Aufruf steht in

          try { sprichDann(text) } catch { /* die Stimme ist Zugabe, kein Weg */ }

      und genau dieser catch hat den Bruch VOM ERSTEN TAG AN verschluckt.
      Alle sechs Absagen des Video-Schirms erschienen nur als Text; der
      Kommentar darueber beschreibt eine Vorlesestimme, die es nie gab. Kein
      Test wurde rot, kein Fehler stand in der Konsole — der Weg war als
      Zugabe deklariert und deshalb nie gemessen.

      ══ DIE REGEL, DIE DARAUS FOLGT ══════════════════════════════════════
      WER `catch {}` UM EINE ZUGABE LEGT, MUSS DEN WEG EINMAL OHNE DEN CATCH
      GEFAHREN SEIN. Ein leerer catch ist die Erlaubnis, dass etwas
      scheitern darf — nicht der Beweis, dass es je funktioniert hat. Die
      beiden sehen von aussen exakt gleich aus. Bei einer durchgereichten
      Hausfunktion heisst das konkret: die Signatur an der DEFINITION lesen,
      nicht am Nachbarn raten — dieselbe Sorte Fehler wie bei
      [[zwei-ausrollwege-eine-handgefuehrte-liste]], nur nicht zwischen zwei
      Dateien, sondern zwischen Aufrufer und Vertrag. Und wie bei
      [[gegenprobe-bleibt-gruen-ist-der-fund]] gilt: ein Weg, der nie rot
      werden KANN, ist kein gemessener Weg.

      Nebenbefund derselben Stelle: die Video-Kacheln setzen nur ein
      `aria-label` und rufen `sprichDann` gar nicht — als einzige Kacheln
      der Oberflaeche sind sie damit aus dem Lern-/Antippmodus gefallen.
      Eine Zugabe, die alle anderen Stellen tragen, faellt beim Nachbau
      still weg, wenn niemand sie als Vertragsbestandteil prueft.
  - id: mupibox-ng-die-zweite-linie-des-ursprungs
    kind: referenz
    title: MuPiBox-NG — der Ursprung hat seit 2026 ZWEI Linien
    tags: [ursprung, upstream, ng, fork, readme]
    quelle:
      - https://mupibox.de/mupibox-ng/
      - https://mupibox.de/classic-vs-ng/
      - https://api.github.com/repos/splitti/MuPiBox/releases
    body: |
      STAND 23.09.2026, an der Quelle nachgesehen (mupibox.de + GitHub-API).

      Der Ursprung fuehrt seit 2026 ZWEI Linien nebeneinander, und wer nur
      "splitti/MuPiBox" sagt, meint nur die eine:

        Classic  — veroeffentlicht, lebendig. Release 5.0.0 am 2026-09-20,
                   5.0.1 am 2026-09-21 (vorher 4.2.4 vom 2026-05-05).
                   5.0.0 bringt: NAS/WebDAV als Medienquelle, Theme
                   "coverflow", Cover-Thumbnails mit Zwischenspeicher,
                   schnellere Podcasts, neue WLAN- und Bluetooth-Seiten,
                   Titelliste per Halten, sichereres Update.
        NG       — "Coming soon", NICHT veroeffentlicht. Eigene Seiten
                   mupibox.de/mupibox-ng/ und /classic-vs-ng/.

      NG IST NICHT NACHLESBAR. Die Website verlinkt
      github.com/splitti/MuPiBox-NG; die GitHub-API liefert dort 404, und
      die Repo-Suche nach "MuPiBox-NG" gibt total_count 0. Also: kein
      Quelltext, keine Commits, kein Release, keine Lizenz einsehbar. Jede
      Aussage ueber NGs TECHNIK waere geraten — die Website nennt keinen
      Stack. Was belegbar ist, ist die ABSICHT.

      WAS NG LAUT SEITEN SEIN WILL: "technisch neu entwickelt", Pi 3 und
      neuer, mehrere Bedienwege (Touch + Tasten + RFID), Browser-Verwaltung,
      lokales TTS ohne Cloud. Der Funktionsvergleich nennt als NG-EIGEN
      gegenueber Classic: Buttons, RFID, Smart Discovery, Home Assistant via
      HACS, lokales TTS, Android-App (geplant) — und MQTT faellt weg
      (Classic ja, NG nein). Die Roadmap ist in vier Stufen geteilt
      (Schon moeglich / In Entwicklung / Als Naechstes / Vision) und sagt
      selbst dazu: "Roadmap statt Versprechen".

      DREI NEBEN-REPOS passen dazu und sind oeffentlich (alle zuletzt
      2026-09-19 bewegt): splitti/MuPiBox-Control-Android (Kotlin),
      -Control-iOS (Swift), -homeassistant (Python, HACS).

      WARUM DAS FUER DIESEN FORK ZAEHLT: Die Abzweigung begann am
      2026-07-23 (erster eigener Commit) von einem Stand um 4.2.4. NG ist
      also KEIN Vorlaeufer dieses Forks und dieser Fork kein Vorlaeufer von
      NG — es sind zwei Neubauten aus derselben Idee, die voneinander nichts
      uebernehmen. Und Classic steht nicht still: 5.0.0 hat unabhaengig
      Dinge bekommen, die der Fork ebenfalls gebaut hat (Cover-Cache,
      Coverflow, NAS, Bluetooth-Seite). Wer im README schreibt "so machen
      wir es anders", muss das seit dem 2026-09-20 gegen 5.0.x pruefen,
      nicht gegen 4.2.4.

      NICHT BELEGT und deshalb nicht zu behaupten: NGs Programmiersprache,
      Architektur, Lizenz, Termin — und ob NG ueberhaupt aus Classic
      hervorgeht oder daneben steht.
  - kind: entscheidung
    id: readme-mit-eigenen-schirmbildern-und-die-drei-funde
    title: "Die README zeigt jetzt die eigene Oberflaeche — und drei Funde, die erst das Abbilden sichtbar gemacht hat"
    tags: ['doku', 'readme', 'schirmbilder', 'werkzeug', 'github', 2026]
    source: "umgesetzt 23.09.2026; Werkzeuge tools/schirmbilder-readme.mjs, tools/schirmbilder-verwaltung.mjs, remote-step-installer/tools/schirmbilder-sdstart.py"
    body: |
      DER AUFTRAG (Betreiber, 23.09.2026): „kann man screen shots auch
      einbetten? bei github" — und danach „auch vom sdtool", „machst du auch
      screens von der admin page?", „weg mit dem alten kram".

      ── WAS DER ALTE KRAM WAR ──────────────────────────────────────────────
      `screenshots/` trug 23 Bilder des URSPRUNGS: die Angular-Oberflaeche,
      ihre zwoelf Farbthemen, die Spotify-Suchsyntax. Diese Oberflaeche ist
      mit E118/1e am 05.09.2026 GELOESCHT. Ein Bild, das etwas anderes zeigt
      als das, was man bekommt, ist schlimmer als kein Bild — geloescht (die
      Geschichte traegt sie weiter), ersetzt durch frische.

      ── DREI WERKZEUGE STATT DREIER HANDGRIFFE ─────────────────────────────
      Aufgenommen wird gegen die Attrappen, nicht gegen eine Box:
        * `tools/schirmbilder-readme.mjs` — Kinderschirm, grosser Spieler,
          Schublade, Eltern-Bereich. Gegen `tools/neu-vorschau.mjs`, in
          800x480 (der Groesse des Geraets), ueber CDP wie die Messwerkzeuge
          des Hauses (`leihgabe.mjs` + `ws`).
        * `tools/schirmbilder-verwaltung.mjs` — sechs Seiten der Verwaltung
          gegen `tools/admin-vorschau.mjs`. KEIN `ng serve` noetig: der Stub
          liefert den GEBAUTEN Stand aus; vorher `npm run build:frontend-admin`.
        * `remote-step-installer/tools/schirmbilder-sdstart.py` — die sieben
          Seiten des SD-Assistenten im Probemodus, unter Xvfb mit ImageMagick
          `import` abgelichtet und auf die Tk-Geometrie zugeschnitten. Tk kann
          sich nicht selbst fotografieren.
      Wer die Oberflaeche aendert, ruft sie neu und die README stimmt wieder.

      ── DREI FUNDE, DIE ERST DAS BILD ZEIGTE ───────────────────────────────

      1. EIN KOMMENTAR, DER AUS VERSEHEN AUFHOERTE. Auf der Profilseite der
         Verwaltung stand mitten im Text ein halber Kommentar:
         `src/frontend-admin/src/app/seiten/kinderzeit.ts` zitierte den
         Betreiber mit „kinderzeit umbauen in Profile --> Profil auswahl" —
         und die drei Zeichen `-->` sind das KOMMENTARENDE. Alles danach stand
         sichtbar auf der Seite, bis zur richtigen Klammer vier Zeilen tiefer.
         Keine Wache konnte das sehen (es ist gueltiges HTML), kein Test
         (niemand prueft Prosa im Template) — nur ein Mensch, der hinsieht.
         REPARIERT, und beim ersten Versuch GLEICH NOCH EINMAL hineingelaufen:
         die Erklaerung, die ich in denselben Kommentar schrieb, enthielt
         wieder `-->`. Wer in einem HTML-Kommentar einen Pfeil braucht,
         schreibt → und niemals die drei Zeichen.

      2. UNBENUTZTE BILDER SIND MUELL AUF GITHUB. Die drei Werkzeuge nehmen
         17 Bilder auf, die README zeigt 11. Die uebrigen sechs sind
         geloescht — „nur das Benutzte" gilt auch fuer das, was man selbst
         gerade erzeugt hat. Beide Werkzeuge sagen das jetzt im Kopf.

      3. DIE WACHE PRUEFT DIE BILDER SELBST. In
         `tools/readme-behauptungen-pruefen.sh` steht KEINE Liste von
         Dateinamen (die veraltete beim naechsten Bild), sondern Abschnitt 1b
         liest die README und sieht jede `![…](pfad)`-Zeile nach. Ein fehlendes
         Bild ist auf GitHub ein leeres Kaestchen und meldet sich sonst nie.

      ── NEBENBEI GEMESSEN ──────────────────────────────────────────────────
      Die Verwaltung hat 27 Seiten (+ Anmeldung); die Kopfleiste fuehrt 24.
      `config/services` haelt 42 Einheiten, nicht 41 — die `.path` fiel durch
      die Zaehlung beider Wachen. Beides nachgezogen.
  - kind: falle
    id: der-ersatzwert-eines-pfades-ist-der-rechner-dessen-der-ihn-schrieb
    title: "Zwei Karten-Wege, die nur auf EINEM Rechner liefen — und ein Boot-Paket, das von 11 auf 164 MB gewachsen war"
    tags: ['installer', 'karte', 'autosetup', 'rezepte', 'mupi_repo', 'ausrollweg', 2026]
    source: "gemessen und repariert am 23.09.2026; Belege: laufpaket.paket_bauen 54 Schritte/73 Dateien/0 fehlend, make-boot-sd.sh 164 MB -> 14 MB"
    body: |
      ZWEI FUNDE DERSELBEN SORTE, beide beim Schreiben der GitHub-README
      aufgefallen und keiner von einer Wache: eine Angabe, die einmal stimmte,
      altert still weiter — und beide Karten-Wege waren davon betroffen.

      ══ 1. `${MUPI_REPO:-$HOME/Downloads/MuPiBox}` ═════════════════════════
      Die Rezepte holen ihre Dateien aus dieser Form. Der Ersatzwert dahinter
      ist der Pfad EINES Rechners. Auf jedem anderen — und auf jedem Klon von
      GitHub — zeigt er ins Leere, und der Lauf stirbt nicht beim Start,
      sondern erst in dem Schritt, der die Datei hochladen soll
      („Datei fuer '<schritt>' nicht gefunden"). Beim Ein-Knopf-Weg war es noch
      leiser: `sdstart` rief `prepare_boot` OHNE `fork=`, das Paket wurde
      trotzdem gebaut, und die einzige Meldung war eine Zeile im Protokoll —
      „N Rezept-Quelle(n) fehlten, diese Schritte laufen ins Leere". Die Box
      installierte sich dann halb.

      REPARIERT MIT EINEM FINDER STATT EINER ANNAHME: `core.fork_wurzel()`
      (zuerst in sdprep, am selben Tag nach `core.py` gezogen, wo `repo_root()`
      wohnt) fragt der Reihe nach: ausdrueckliche Vorgabe (`--fork`),
      `$MUPI_REPO`, ELTERNORDNER DES INSTALLERS, `~/Downloads/mixpibox`,
      `~/mixpibox`, alter Standard. Genommen wird der erste Ordner, der
      WIRKLICH wie der Fork aussieht — geprueft an drei Merkmalen, die nur er
      hat (`config/templates/mupiboxconfig.json`, `scripts/`, `plugins/`).

      DER ELTERNORDNER IST DER EIGENTLICHE TRICK: seit dem 08.08.2026 liegt
      der remote-step-installer IM Fork (ZUGEZOGEN.md), also
      kennt er dessen Ort immer — egal wohin jemand klont.

      UND DER FERNGESTEUERTE LAUF FRAGT DENSELBEN FINDER: `core.expand_path()`
      loest `${MUPI_REPO:-…}` seither ueber ihn auf, statt den Ersatzwert zu
      nehmen. Vorher gaben Karte und Controller VERSCHIEDENE Antworten auf
      dieselbe Frage. Eine gesetzte Variable schlaegt den Finder weiterhin;
      findet er nichts, gilt wieder der Ersatzwert aus dem Rezept (damit die
      alte, bekannte Fehlermeldung kommt und keine neue).
      Der Ersatzwert selbst heisst jetzt ueberall `mixpibox` (51 Stellen) —
      Betreiber, 23.09.2026: „MuPiBox ist alt."

      ══ 2. `scripts/make-boot-sd.sh` BAUTE EINE KARTE, DIE ABBRICHT ════════
      Das Skript schloss `bin/librespot` aus dem Boot-Tarball aus, mit der
      Begruendung daneben: „autosetup wget's the librespot binary from GitHub
      itself". DIESEN RUECKFALL GIBT ES NICHT MEHR — `autosetup.sh:510-517`
      bricht hart ab („FEHLER: bin/librespot/0.8.0/librespot-arm64 fehlt im
      Paket", exit 1), und sein eigener Kommentar sagt „UND DER RUECKFALL AUF
      dev_0.6 IST AUCH WEG". Jede mit diesem Skript gebaute Karte lief also
      garantiert in den Abbruch.

      BEIM REPARIEREN KAM DER ZWEITE, GROESSERE FUND: der Tarball war nicht
      mehr 11 MB, sondern 164 MB. Das Skript packte den GANZEN Baum und zaehlte
      auf, was NICHT mitsoll — und `NewDesign/bilder/quellen` (126 MB
      KI-Rohrender) kam spaeter dazu und stand in keiner Zeile. Auf eine FAT-
      Boot-Partition passt das nicht (auf der Karte des Betreibers sind 94 MB
      frei); gemerkt haette man es erst beim Kopieren.

      UMGEDREHT, UND ZWAR GEMESSEN: autosetup liest aus dem entpackten Paket
      ausschliesslich ueber `${MUPI_SRC}/<ordner>/…`. Das Skript sammelt genau
      diese Ordner aus autosetup.sh ein (heute: bin, config, media, plugins,
      scripts) und packt NUR sie; welcher librespot-Stand mitkommt, liest es
      ebenfalls aus autosetup.sh statt aus einer eigenen Liste. Ergebnis
      gemessen: 164 MB -> 14 MB, mit librespot 0.8.0 drin. Dazu drei Wachen im
      Skript: der librespot-Pfad muss in autosetup stehen UND im Repo liegen,
      jeder eingesammelte Ordner muss existieren, und das fertige Paket darf
      80 MB nicht ueberschreiten.

      ── DIE LEHRE, DIE BEIDE TEILEN ────────────────────────────────────────
      Eine Ausschlussliste altert schlecht, eine Einschlussliste, die aus der
      WIRKLICHEN Quelle gelesen wird, nicht. Und ein Ersatzwert, der den
      Rechner seines Autors beschreibt, ist keine Vorgabe, sondern eine
      Zeitbombe mit langer Zuendschnur: er faellt erst auf, wenn ein Zweiter
      das Repo anfasst.
  - kind: entscheidung
    id: readme-ohne-paypal-und-ohne-wesen-die-huldigung-faellt
    title: "Die README huldigt nicht mehr — Paypal-Adressen und die Geschichte der Wesen sind raus, Neuheiten und der heutige Unterbau rein"
    tags: ['doku', 'readme', 'fork', 'herkunft', 'lizenz', 2026]
    source: "Betreiber, 23.09.2026: „ein bisschen weniger Huldigung vom vorprojekt. keine paypal links nur verweis aufs ursprungsprojekt“; umgesetzt am selben Tag"
    body: |
      DIESER EINTRAG STOESST [[readme-fork-drei-heikle-stellen]] IN EINEM PUNKT
      UM. Dort war am 03.08.2026 entschieden: die Paypal-Adressen der Urheber
      BLEIBEN, mit einer Ueberschrift, die klarstellt, dass dieser Fork kein
      Geld sammelt. Die Begruendung („Entfernen sieht nach Undank aus") war
      damals richtig gedacht — und ist vom Betreiber am 23.09.2026 ausdruecklich
      verworfen worden. Wer die Adressen zurueckholt, tut es gegen diese
      Anweisung.

      WAS BLEIBT (und das ist die Lizenz, nicht die Hoeflichkeit): MIT,
      Copyright MuPiBox.de in LICENSE.md, ein Verweis auf den Ursprung
      (github.com/splitti/MuPiBox), der englische Abweis-Kasten ganz oben.
      Discord, Youtube und Paypal des Ursprungs stehen nicht mehr drin — ein
      Fork verlinkt sein Vorbild, er bewirbt es nicht.

      WAS GEFALLEN IST:
        * Der Abschnitt „Woher der Name kommt" (die MixPis vom Planeten Mixpi,
          die Zustaende, die Beschriftungs-Geschichte). Betreiber: „auch die
          geschichte der wesen muss nicht drin sein". Uebrig ist EIN Satz in
          3.1: das Maskottchen und dass jedes Kind seine Figur waehlt. Die
          Wiki-Eintraege dazu (mixpi-die-geschichte, mixpi-maskottchen-familie,
          benennung-mixpibox-drei-toepfe) bleiben — die README nennt sie nur
          nicht mehr, und die Wache prueft sie deshalb nicht mehr.
        * Die Verbeugung in Abschnitt 1 („ohne deren Arbeit gaebe es dieses
          Projekt nicht … die halbe Werkzeugkiste kommt von dort") und der
          Rat in „Mitarbeiten", man sei beim Ursprung besser aufgehoben.

      WAS DAZUKAM:
        * Abschnitt 2 „Was zuletzt dazugekommen ist" — datierte Zeilen aus dem
          Commit-Log (September: Nachrichten ans Kind, Netzlaufwerk,
          Belohnungs-Videos, Spielecke, Auffindbarkeit, Fernbedienung/Controller,
          Buehne; August in einem Satz). Betreiber: „mir fehlt im readme noch
          die neuheiten".
        * „Aufgebaut auf" beschreibt den HEUTIGEN Unterbau, gemessen am Baum:
          DietPi Trixie, Node 26/TypeScript/Express 5/esbuild/Angular/Biome,
          mpv, librespot 0.8.0 UND Soloist (Betreiber: „auch solist ist mit
          drin"), PipeWire/WirePlumber, Piper, Chromium/Cog, die angedockten
          Dienste, fbv/initramfs-splash/jq/WLED. GEFALLEN, weil nirgends mehr
          benutzt (git grep ueber src/, config/, scripts/, NewDesign/):
          mplayer-wrapper, pi-blaster, SpotifyController, Sonos Kids
          Controller, die Schrift Pan Pizza, das pixabay-Bild, Material Design
          Icons. Die Liste des Ursprungs war die Liste des Ursprungs.

      DIE LEHRE fuer die naechste README-Runde: eine geerbte Danksagung ist
      keine Bestandsliste. Wer „Aufgebaut auf" schreibt, greppt jede Zeile.
  - kind: architektur
    id: github-veroeffentlichung-kuratiert-elternlos-und-die-wache-in-beide-richtungen
    title: "Nach GitHub geht ein kuratierter, elternloser Zweig — und die Wache davor fragt in BEIDE Richtungen"
    tags: ['github', 'veroeffentlichung', 'ausrollweg', 'wache', 'geheimnisse', 2026]
    source: "gebaut 23.09.2026 (tools/github-veroeffentlichen.py, tools/github-ausschluss.txt); die zweite Richtung nach der gegnerischen Nachpruefung desselben Tages, die einen echten Jellyfin-Schluessel und die BSSID des Heim-WLANs im veroeffentlichten Baum fand"
    body: |
      DER WEG. Der Fork liegt vollstaendig auf den eigenen Ablagen (nas, origin).
      Nach GitHub (Gegenstelle `github`, scoutr2d2/mixpibox) geht ein Auszug:
      `tools/github-veroeffentlichen.py` liest `git ls-files -s` (den INDEX, nicht
      den Arbeitsbaum), streicht, was `tools/github-ausschluss.txt` nennt (mit
      Grund je Zeile), holt namentlich gebrauchte Bildquellen gemessen zurueck
      (maskottchen.json, sdstart-bilder.py), baut ueber einen EIGENEN Index in
      /tmp einen Baum aus den vorhandenen Blobs und haengt einen Commit an den
      Zweig `veroeffentlichung` — beim ersten Mal elternlos. Gepusht wird
      `veroeffentlichung:main`. Nichts wird kopiert, nichts umgeschrieben, die
      anderen Ablagen behalten ihre Geschichte. Gemessen am 23.09.2026:
      1732 Dateien / 120 MB hinaus, 236 Dateien / 306 MB zurueck.

      WARUM NICHT EINFACH PUSHEN: die Dateien haengen an der Geschichte, nicht am
      Arbeitsbaum — ein .gitignore von heute entfernt kein CAD-Modell aus dem
      Commit von gestern. Und filter-repo haette jede Kennung geaendert, also
      nas und origin vor ein anderes Repo gestellt.

      ══ DIE WACHE FRAGT IN BEIDE RICHTUNGEN — DAS WAR NICHT VON ANFANG AN SO ══

      RICHTUNG 1, „fehlt etwas?": jede Quelle, die Rezepte und autosetup aus dem
      Repo holen (`${MUPI_REPO:-…}/…`, `${MUPI_SRC}/…`, 113 Stueck), muss im
      Auszug liegen; ein Pflicht-ORDNER gilt ganz (eine einzige ueberlebende
      Datei genuegte der ersten Fassung — Befund der Nachpruefung).

      RICHTUNG 2, „ist etwas drin, das nicht hinaus darf?": die erste Fassung
      stellte diese Frage NICHT. Der ganze Schutz hing daran, dass ein Mensch
      vorher den richtigen Pfad in die Ausschlussliste schreibt. Die gegnerische
      Nachpruefung fand daraufhin im veroeffentlichten Baum:
        * einen ECHTEN Jellyfin-API-Schluessel (32 hex) in zwei Testdateien,
          samt Serveradresse — die Spec sagte im eigenen Kopf, die Werte seien
          der echten Box entnommen. Ersetzt durch 0123…cdef; der Betreiber muss
          den Schluessel im Jellyfin ZURUECKZIEHEN, er ist in der Git-Geschichte
          der eigenen Ablagen weiterhin lesbar.
        * die BSSID des Heim-WLANs (dc:39:6f:…) in BACKLOG, Wissenspaket und
          einem Skript-Kommentar — ueber oeffentliche WLAN-Ortungsdatenbanken
          auf eine Hausadresse aufloesbar. Das ist eine andere Sorte Leck als
          eine private 192.168-Adresse, die ausserhalb des Netzes nichts
          bedeutet. Die letzten drei Oktette sind jetzt xx.
      Seither laeuft `lecks_suchen()` ueber den TEXT jedes hinausgehenden Blobs:
      API-Schluessel-Parameter, Bearer-Token, private Schluessel, WLAN-PSKs
      (ausser Platzhaltern), bcrypt-Abdruecke (ausser der abcdef-Attrappe der
      Tests), Spotify-Zugaenge, und die eine BSSID namentlich. Treffer = Abbruch;
      Ausnahmen stehen mit Grund in LECK_AUSNAHMEN. Beide Richtungen haengen
      ueber den Trockenlauf in tools/doku-luecken-probe.sh.

      WAS BEWUSST NICHT GESCRUBBT WIRD: private Adressen (192.168.178.x) und der
      interne Hostname git.local stehen in 235 Dateien und verraten nichts, was
      ausserhalb des Netzes traegt. `.claude/` bleibt trotzdem draussen — wegen
      der Sitzungsfreigaben, nicht wegen der Adressen.

      DREI KLEINERE HAERTUNGEN aus derselben Nachpruefung: `--push` zieht
      `--bauen` nach sich (vorher tat es still nichts), `--fern origin|nas|
      upstream` wird verweigert (die Zusage stand nur in der Doku), und der
      Selbsttest der Musterlogik haengt im Doku-Laeufer.

      LEHRE: eine Wache, die nur fragt, ob etwas FEHLT, bewacht die halbe Tuer.
      Wer etwas hinausgibt, fragt auch, was NICHT hinausgehoert — und zwar am
      Inhalt, nicht am Dateinamen.
  - kind: entscheidung
    id: readme-in-zwei-sprachen-die-deutsche-fuehrt-die-englische-wird-gemessen
    title: "Zwei README-Fassungen: die deutsche fuehrt, die englische ist uebersetzt — und ein Werkzeug haelt beide dort gleich, wo man es nachrechnen kann"
    tags: ['doku', 'readme', 'englisch', 'wache', 'github', 2026]
    source: "Betreiber, 23.09.2026: „ich moechte auch noch eine englische fassung und dann hoch damit“; umgesetzt am selben Tag (README.en.md, tools/readme-paritaet-pruefen.py)"
    body: |
      DIE ENTSCHEIDUNG VOM 03.08. („Sprache: Deutsch, mit englischem Kasten
      oben", [[readme-fork-drei-heikle-stellen]]) bleibt in ihrem Kern und wird
      um eine Fassung erweitert: README.md ist die MASSGEBLICHE, README.en.md
      ist aus ihr uebersetzt. Weichen beide ab, gilt die deutsche — das steht
      in beiden Dateien im letzten Abschnitt („Warum die deutsche Fassung
      fuehrt" / „Why the German file leads"). GitHub zeigt README.md; die erste
      Zeile darunter verlinkt die englische, die englische verlinkt zurueck.

      WIE UEBERSETZT WURDE: 15 Abschnitte parallel, mit festen Regeln — alles
      in Backticks, alle Codebloecke, Pfade, Befehle, Bild-Einbettungen und
      Zahlen bleiben WOERTLICH (einzige erlaubte Aenderung: Dezimalkomma zu
      Dezimalpunkt); deutsche Bezeichner im Code bleiben deutsch, weil es die
      Namen im Code SIND (kennung, fassung, rechte, aufloesen …); die
      deutschen Kommentare in Codebloecken bleiben, damit die Bloecke gleich
      sind.

      WAS DAS ANFAELLIGE DARAN IST, UND WIE ES GEWACHT WIRD: zwei Fassungen
      driften nicht bei der Prosa (das ist erwartbar), sondern bei dem, was
      man NACHRECHNEN kann — eine Zahl, die nur in einer nachgezogen wurde,
      ein Pfad, der in der anderen noch alt heisst, ein Bild, das nur eine
      einbettet. `tools/readme-paritaet-pruefen.py` vergleicht mechanisch,
      ohne Sprachverstand: Bilder, Backticks, Zahlen (als Multimenge, Komma
      = Punkt), Codebloecke zeilenweise, Ueberschriften-Nummern in Folge.
      Gemessen am 23.09.2026: 11 Bilder, 184 Backticks, 247 Zahlen, 11 Bloecke,
      26 Ueberschriften deckungsgleich. Haengt in tools/doku-luecken-probe.sh;
      die README-Wache prueft zusaetzlich die Bilder der englischen Datei.

      DIE ERSTE DRIFT KAM NACH ZEHN MINUTEN: `tools/wiki-anhaengen.py` zieht
      die Wissenspaket-Zahl in README.md nach, kannte README.en.md aber nicht —
      die englische sagte 1111/613, die deutsche 1112/614. Der Nachzieher
      (`tools/paket-angaben-nachziehen.py`) kennt seither auch die englische
      Form „(N entries, version N)". Genau die Sorte Drift, fuer die die Wache
      gebaut wurde — und sie hat sie gefunden, bevor etwas gepusht war.

      AUCH NEU IN DER README (derselbe Tag, dieselbe Anweisung): ein
      Unterbau-Vergleich am Fork-Punkt gegen heute (gemessen an
      autosetup.sh des Fork-Punkts, 605 Zeilen: Node 22 -> 26, pm2 -> systemd,
      PulseAudio -> PipeWire, mplayer -> mpv, librespot-dev -> 0.8.0/Soloist,
      PHP/lighttpd -> Angular) und eine Messtabelle mit den Gewinnen aus dem
      Wissenspaket ([[mupi-startzeiten-gemessen]], [[mupi-boot-32s-auf-13s]],
      [[grundmessung-zeit-bis-zum-bild]]) — jede Zahl mit Geraet und Datum.
