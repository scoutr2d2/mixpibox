# Sim-only mplayer config: the harness containers have no ALSA sound card, so
# send audio to the null device. This lets mplayer load + "play" a playlist so
# the local-media path (playlist resolution, track counting, state) is testable
# without real audio hardware. The real box uses its own ALSA/MAX98357A output.
ao=null
really-quiet=yes
